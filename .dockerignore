# Default-deny the Docker build context. The production image only needs the
# Aura binary build inputs; runtime data is mounted into /workspace and /data.
**

# Go module metadata.
!go.mod
!go.sum

# Production command and embedded application packages.
!cmd/
!cmd/aura/
!cmd/aura/**
!internal/
!internal/**

