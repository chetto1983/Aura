---
phase: 25-chat-approval-center
plan: 07
type: execute
wave: 7
depends_on: ["25-03", "25-06"]
files_modified:
  - internal/agui/server.go
  - internal/agui/conversations_api.go
  - internal/agui/conversations_api_test.go
  - cmd/aura/serve_webui.go
  - cmd/aura/serve_webui_test.go
  - web/src/chat/BranchPicker.tsx
  - web/src/chat/ExternalStoreChat.tsx
  - web/src/chat/__tests__/BranchPicker.test.tsx
  - web/src/i18n/resources.ts
  - web/e2e/chat.spec.ts
autonomous: true
requirements: [CHAT-05, CHAT-01]
must_haves:
  truths:
    - "Editing a user turn or regenerating an assistant turn produces a new branch (full tree, all branches queryable) over the path-aware backend"
    - "The branch picker navigates sibling branches (Previous/Next/Number/Count) bound to the selected-path backend"
    - "Re-run-from-a-point continues the run over the rehydrated selected-branch history (continue-after-resume / no-Resume agent run)"
    - "An end-to-end Playwright test proves: type a prompt -> stream -> resolve an inline approval card -> run resumes, asserts >=1 streamed-token chunk, and hard-fails under CI when neither a live stack nor the golden-events.json fixture is available (no silent skip-as-green)"
    - "The messages[0] KV-cache head stays byte-identical after a branch switch (cache-invariant gate green)"
  artifacts:
    - path: "web/src/chat/BranchPicker.tsx"
      provides: "BranchPickerPrimitive bound to the D-09 tree backend"
    - path: "internal/agui/conversations_api.go"
      provides: "branch-list + branch-select REST over the path-aware loader"
    - path: "web/e2e/chat.spec.ts"
      provides: "Playwright E2E: prompt -> stream -> inline approval resolve -> resume"
  key_links:
    - from: "web/src/chat/BranchPicker.tsx"
      to: "useExternalStoreRuntime branch nav"
      via: "getBranches/switchToBranch over the selected-path backend"
      pattern: "BranchPicker|switchToBranch"
    - from: "internal/agui/conversations_api.go"
      to: "conversations.Store branch path"
      via: "branch-select drives LoadManagedHistory(selected branch)"
      pattern: "branch"
---

<objective>
Complete D-09: wire edit/regenerate -> branch creation + the re-run-from-a-point agent semantics
(backend) and the `BranchPickerPrimitive` branch navigation (frontend) over the path-aware loader
from plan 25-06, then ship the phase-proving Playwright E2E (type -> stream -> resolve inline card ->
resume). This is the deliberate operator-chosen scope addition (D-09 / CHAT-05).

Purpose: CHAT-05 (branch trees) needs the edit/re-run wiring + the UI to navigate the tree the
plan-25-06 backend now stores. The E2E is the goal-backward proof of the whole phase's Core-Value
loop end-to-end.
Output: branch-select REST on the conversation adapter, the re-run agent wiring, `BranchPicker.tsx`
bound to the external-store branch nav, the ExternalStoreChat edit/reload affordances, and
`web/e2e/chat.spec.ts`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-chat-approval-center/25-CONTEXT.md
@.planning/phases/25-chat-approval-center/25-RESEARCH.md
@.planning/phases/25-chat-approval-center/25-PATTERNS.md
@.planning/phases/25-chat-approval-center/25-UI-SPEC.md
@.planning/phases/25-chat-approval-center/25-03-SUMMARY.md
@.planning/phases/25-chat-approval-center/25-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan:
- Route(s) on the conversation adapter for branch list + branch select (e.g.
  `GET /api/conversations/{id}/branches`, `POST /api/conversations/{id}/branches/{branchId}/select`)
  driving `LoadManagedHistory(selected branch)`
- The re-run-from-a-point agent wiring (continue-after-resume `Turn(…, userMsg=nil)` over the
  selected-branch rehydrated history; edit a user turn creates a new branch then re-runs)
- `web/src/chat/BranchPicker.tsx` (BranchPickerPrimitive) + ExternalStoreChat `onEdit`/`onReload`/branch nav
- `web/e2e/chat.spec.ts` (Playwright)
- i18n keys for branch nav (Copy/Edit/Reload action-bar labels) in en+it
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch list/select REST + re-run-from-a-point agent wiring (CHAT-05 backend)</name>
  <files>internal/agui/server.go, internal/agui/conversations_api.go, internal/agui/conversations_api_test.go, cmd/aura/serve_webui.go, cmd/aura/serve_webui_test.go</files>
  <read_first>
    - internal/agui/conversations_api.go (plan 25-01 — the thin-adapter file to extend), internal/agui/server.go (the widened ConversationStore interface from plan 25-01; add the branch methods)
    - internal/conversations/store_branch.go + context.go (plan 25-06 — the branch-pointer write + `LoadManagedHistory(selected branch)`)
    - internal/runner/runner.go:354 (`Turn(…, userMsg=nil)` continue-after-resume — the re-run-from-a-point primitive)
    - cmd/aura/serve_webui.go (the `/api/conversations/` mount from plan 25-01 — the new branch sub-routes ride the same subtree, no new bare /api/)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"store_branch.go + context.go" + §"conversations_api.go" + RESEARCH Open Question 3 (full tree, all branches persisted)
  </read_first>
  <behavior>
    - Test: editing a user turn creates a NEW branch (sibling under the parent) — the old branch stays queryable (full tree, RESEARCH OQ3)
    - Test: GET /api/conversations/{id}/branches lists the sibling branches at the divergence point; select drives `LoadManagedHistory(selected)`
    - Test: re-run-from-a-point continues over the rehydrated selected-branch history (no-Resume agent Turn); the messages[0] head is unchanged (Pitfall 3 — inherits plan 25-06's invariant)
    - Test (edge): a non-UUID id/branchId → 404; an unknown branch → 404
  </behavior>
  <action>
    Extend `conversations_api.go` (plan 25-01) with the branch list + branch-select routes (ride the
    existing `/api/conversations/` subtree — NO new bare /api/). Widen the `ConversationStore` interface
    (server.go) to add the branch-write + branch-list + `LoadManagedHistory(selected branch)` methods
    (plan 25-06). The edit-a-user-turn path creates a new branch (sibling under the parent — full tree,
    all branches persisted, RESEARCH OQ3) then re-runs from that point; regenerate creates a new
    assistant-turn branch. Re-run uses the continue-after-resume primitive (`Turn(…, userMsg=nil)`,
    runner.go:354) over the selected-branch rehydrated history so the messages[0] head stays byte-identical
    (inherits the plan-25-06 cache invariant — do NOT re-implement the walk; call the loader). Guard ids
    with `uuid.Parse`; redact errors with `sanitizeErr`. Mount the new sub-routes in serve_webui (same
    subtree, behind RequireAuth; the mutating branch-select/edit re-run may interpose RequireCapability
    like /agent/run). Extend `conversations_api_test.go` (db_integration) + `serve_webui_test.go`. Keep
    every touched Go file ≤600 LOC — split `conversations_api.go` by concern (e.g.
    `conversations_branch_api.go`) if it would exceed the cap.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./internal/agui/ ./cmd/aura/ && go build ./... && go test -tags db_integration ./internal/agui/ -run 'TestConversationsAPI|TestBranch' -race && go test ./cmd/aura/ -run 'ServeWebui|Branch' -race && bash scripts/cache_invariant_audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: editing a user turn creates a new sibling branch AND the old branch remains queryable (full-tree assertion)
    - Behavior: branch-select drives `LoadManagedHistory(selected)` and the re-run continues over that history (asserted)
    - [BLOCKING] `bash scripts/cache_invariant_audit.sh` EXITS 0 after the branch-switch wiring (the head is unchanged across a branch switch)
    - Source assertion: no bare `mux.Handle("/api/",` added (`grep -v '^//'`); branch routes ride `/api/conversations/`
    - Every touched Go file ≤600 LOC (`wc -l` on each)
    - `go test -tags db_integration ./internal/agui/ -run TestBranch` and `./cmd/aura/` PASS
  </acceptance_criteria>
  <done>Edit/regenerate create persisted branches, branch-select drives the path loader, re-run continues over the selected history with the cache head unchanged, routes ride the existing subtree, files ≤600 LOC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branch picker UI + edit/reload affordances (CHAT-05 frontend)</name>
  <files>web/src/chat/BranchPicker.tsx, web/src/chat/ExternalStoreChat.tsx, web/src/chat/__tests__/BranchPicker.test.tsx, web/src/i18n/resources.ts</files>
  <read_first>
    - web/src/chat/ExternalStoreChat.tsx (plan 25-03 — the `useExternalStoreRuntime` provider + the documented branch/edit/reload integration seam)
    - .planning/phases/25-chat-approval-center/25-UI-SPEC.md §Runtime adapter (BranchPickerPrimitive `.Previous`/`.Next`/`.Number`/`.Count`; ActionBarPrimitive `.Copy`/`.Edit`/`.Reload`; feedback group beyond Copy is OUT — Phase 26) + §Color (accent reserved item 6 — branch-picker active indicator) + §Component Inventory (Action bar — ship Copy minimum + Edit→branch + Reload)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"ExternalStoreChat.tsx + BranchPicker.tsx (NEW-PATTERN)" + §"a11y component idioms"
    - assistant-ui.com/llms.txt (exact 0.14.22 BranchPickerPrimitive + external-store `getBranches`/`switchToBranch`/`onEdit`/`onReload` API)
  </read_first>
  <behavior>
    - Test: BranchPicker renders `{{current}} / {{count}}` and Previous/Next controls; switching calls the external-store branch nav (`switchToBranch`) which POSTs the branch-select (Task 1) and re-loads the selected path
    - Test: ActionBar Edit on a user turn enters edit mode and on submit creates a branch (onEdit -> branch-create + re-run); Reload on an assistant turn regenerates (onReload -> assistant branch)
    - Test: only Copy + Edit + Reload ship (no feedback rating group — Phase 26); the active branch indicator uses the accent (reserved item 6)
    - Test (a11y): picker controls have `aria-label`; the active branch is announced; icon SVGs are `aria-hidden`
  </behavior>
  <action>
    Build `BranchPicker.tsx` using `BranchPickerPrimitive` (`.Previous`/`.Next`/`.Number`/`.Count`) bound
    to the external-store branch navigation (`getBranches`/`switchToBranch`) wired in `ExternalStoreChat.tsx`
    so a switch POSTs the Task-1 branch-select and re-loads the selected path. Wire `onEdit` (edit a user
    turn -> create a branch + re-run) and `onReload` (regenerate an assistant turn -> assistant branch) on
    the runtime. Add the ActionBar with Copy + Edit + Reload ONLY — the feedback rating group is OUT
    (Phase 26, UI-SPEC). The active-branch indicator uses the accent (reserved item 6). Picker controls
    carry `aria-label`, the active branch is announced, icon SVGs are `aria-hidden`. Copy in en+it. Rebuild
    dist. Do NOT introduce a bespoke branch state machine — the runtime models the tree; you supply the
    DB-walked message list + `setMessages` (RESEARCH Don't Hand-Roll).
  </action>
  <verify>
    <automated>cd /d/Aura/web && npm run lint && npm run typecheck && npm run test -- BranchPicker && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: switching a branch calls `switchToBranch` and triggers the branch-select POST + path re-load (asserted via mock)
    - Behavior: Edit on a user turn creates a branch (onEdit); Reload regenerates (onReload) — asserted
    - Source assertion: NO feedback rating group beyond Copy (`grep` shows Copy/Edit/Reload only; no thumbs/rating) — Phase 26 boundary held
    - Source assertion: every new branch-nav i18n key exists under BOTH en+it
    - `npm run test -- BranchPicker` PASSES; `npm run build` succeeds; coverage ≥85% on touched files
  </acceptance_criteria>
  <done>The branch picker navigates the tree over the external-store nav, edit/reload create branches and re-run, only Copy/Edit/Reload ship, copy en+it, dist rebuilt.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-proving Playwright E2E (prompt -> stream -> inline approval resolve -> resume)</name>
  <files>web/e2e/chat.spec.ts</files>
  <read_first>
    - web/src/chat/ExternalStoreChat.tsx + Composer.tsx (plan 25-03 — the composer + stream surface to drive)
    - web/src/approvals/InlineApprovalCard.tsx (plan 25-05 — the inline card the E2E resolves)
    - .planning/phases/25-chat-approval-center/25-RESEARCH.md ## Validation Architecture (the `web/e2e/chat.spec.ts` Wave-0 gap — Playwright; CHAT-01 stream a real turn)
    - internal/agui/testdata/golden-events.json (EXISTS — the captured real translator frames; the deterministic CI replay source for the E2E, fed through the real sseAdapter/runtime, never synthetic)
    - existing web/e2e/ harness (if present — mirror its setup; if absent, scaffold the Playwright config minimally per the Phase-23 toolchain)
  </read_first>
  <action>
    Write `web/e2e/chat.spec.ts` (Playwright). The spec's setup MUST be provably run-or-fail
    (CLAUDE.md no-skip-as-green — a skip that fires under `$CI` is a falsely-green job exercising nothing):
    in a Playwright fixture/`beforeAll`, resolve the stream source in this order — (1) a live `aura serve`
    if reachable, else (2) replay of the REAL captured frames at `internal/agui/testdata/golden-events.json`
    (the deterministic CI path — captured translator output, NEVER synthetic-only). If `process.env.CI`
    is set AND neither a live stack NOR the golden-events.json fixture is available, the setup MUST
    `throw` (hard-fail / non-zero exit) — it must NEVER `test.skip()` to a silent 0-tests pass under CI.
    Drive the goal-backward Core-Value loop and COUNT the streamed-token assertions as they execute
    (a local counter incremented per asserted token chunk): (1) type a prompt in the composer and assert
    the assistant response streams token-by-token (CHAT-01) — assert the token-chunk counter is ≥1 at the
    end so a no-op run fails; (2) trigger an `ask_user` interrupt and assert the inline approval card
    renders in-thread (D-03); (3) resolve it (Answer) and assert the run resumes and completes (APRV-02);
    (4) assert the footer shows non-zero tokens/cost after the turn (CHAT-04). The replay path feeds the
    golden frames through the REAL sseAdapter/runtime (not a reducer stub), so both the live and CI paths
    exercise the real stream mapping.
  </action>
  <verify>
    <automated>cd /d/Aura/web && npm run lint && npm run typecheck && npx playwright test chat.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the E2E asserts (a) streamed assistant text appears incrementally, (b) the inline approval card renders, (c) resolving resumes the run, (d) the footer shows non-zero metrics
    - [BLOCKING] no-skip-as-green guard: the spec setup `throw`s (non-zero exit) when `process.env.CI` is set AND neither a live stack NOR `internal/agui/testdata/golden-events.json` is available — asserted by a unit/meta test or a grep gate (`grep -c "process.env.CI" web/e2e/chat.spec.ts` ≥ 1 AND `grep -c "test.skip" web/e2e/chat.spec.ts` == 0 — no silent CI skip)
    - Counted-assertion guard: the spec asserts a streamed-token counter ≥ 1 at the end (`grep -c "toBeGreaterThan" web/e2e/chat.spec.ts` ≥ 1) so a no-op (0-tokens) run FAILS rather than passing silently
    - Deterministic CI path = golden replay: `grep -c "golden-events.json" web/e2e/chat.spec.ts` ≥ 1 (replays REAL captured frames through the real adapter, never synthetic-only)
    - `npx playwright test chat.spec.ts` PASSES on the golden-replay path with a non-zero number of token-chunk assertions executed
  </acceptance_criteria>
  <done>The Playwright E2E proves the Core-Value loop end-to-end (prompt -> stream -> inline approval resolve -> resume, footer updating), hard-fails under CI when neither a live stack nor the golden fixture is available (no silent skip), and asserts ≥1 streamed-token assertion executed on the golden-replay path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> branch-select / edit re-run | Operator drives which branch path rehydrates + re-runs the agent |
| Branch switch -> history reconstruction | A branch switch re-feeds the path-aware loader (inherits the cache invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-24 | Tampering | a branch switch mutates messages[0] -> poisons the KV-cache | mitigate | The re-run calls the plan-25-06 path loader (head byte-identical); [BLOCKING] `cache_invariant_audit.sh` re-run green after the wiring |
| T-25-25 | Elevation of Privilege | unauthenticated/uncapable branch-select or edit re-run | mitigate | Branch routes ride the `/api/conversations/` subtree behind `RequireAuth`; the mutating select/edit re-run interposes `RequireCapability` like /agent/run |
| T-25-26 | Information Disclosure | malformed id/branchId -> 500 leak | mitigate | `uuid.Parse` guard -> clean 404; errors via `sanitizeErr` |
| T-25-SC | Tampering | Playwright install (if net-new to web/) | mitigate | Playwright is the Phase-23 E2E toolchain (already vetted); no new untrusted dep introduced |
</threat_model>

<verification>
- `go test -tags db_integration ./internal/agui/ -run TestBranch -race` (branch list/select + re-run)
- `bash scripts/cache_invariant_audit.sh` exits 0 after the branch-switch wiring ([BLOCKING])
- `npm run test -- BranchPicker` + `npx playwright test chat.spec.ts` green
- `make quality` clean; every touched Go file ≤600 LOC
</verification>

<success_criteria>
- CHAT-05: edit/regenerate produce a navigable branch tree (full tree) over path-aware history; branch picker navigates it; re-run continues over the selected branch
- The messages[0] KV-cache head is byte-identical across a branch switch (gate green)
- CHAT-01 (E2E proof): the Playwright test proves prompt -> stream -> inline approval resolve -> resume with the footer updating
- Coverage ≥85%; files ≤600 LOC; copy en+it
</success_criteria>

<output>
Create `.planning/phases/25-chat-approval-center/25-07-SUMMARY.md` when done
</output>
