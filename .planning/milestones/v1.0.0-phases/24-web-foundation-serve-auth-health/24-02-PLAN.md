---
phase: 24-web-foundation-serve-auth-health
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/webui/embed.go
  - internal/webui/embed_test.go
  - internal/webui/doc.go
  - cmd/aura/serve_webui.go
  - cmd/aura/serve_webui_test.go
autonomous: true
requirements: [WEB-01]
must_haves:
  truths:
    - "WEB-01: an unknown client route (a path with no embedded asset and no API prefix) is served index.html (200), so React Router resolves deep links"
    - "WEB-01 / SC1: an excluded API/agent/health prefix (e.g. /api/nope, /agent/typo, /api/integrations/x) returns a real 404, NEVER the SPA index.html"
    - "WEB-01: the forward-compat /api/ carve-out (CONTEXT Discretion) is an SPA-fallback EXCLUSION prefix only, not a new mux registration (it must not collide with the existing /api/integrations/ subtree)"
    - "the AG-UI route prefixes keep Go 1.22 ServeMux precedence over the / catch-all (existing contract preserved)"
    - "internal/webui stays leaf-level — it imports only embed/io/fs/net/http/strings (no internal/*); scripts/agui_boundary_check.sh passes"
  artifacts:
    - path: "internal/webui/embed.go"
      provides: "spaFallback handler replacing the bare http.FileServerFS in Handler()"
      contains: "func spaFallback"
    - path: "cmd/aura/serve_webui.go"
      provides: "single-source route-exclusion list incl. the new /api/ carve-out, passed into webui.Handler"
      contains: "/api/"
    - path: "internal/webui/embed_test.go"
      provides: "SPA-fallback unit cases (client-route → index.html; excluded-prefix → 404)"
      contains: "TestSPAFallback"
  key_links:
    - from: "cmd/aura/serve_webui.go newServeHandler"
      to: "internal/webui spaFallback"
      via: "the exclusion prefix list passed into webui.Handler(prefixes) so the two consumers cannot drift"
      pattern: "webui\\.Handler\\("
---

<objective>
Turn the Phase-23 static placeholder into the real single-binary SPA host (WEB-01).
Upgrade `internal/webui.Handler()` from a bare `http.FileServerFS` into an
SPA-fallback handler: an excluded API/agent/health prefix returns a real 404, an
unknown CLIENT route returns `index.html` (React Router resolves it), and a real
asset is served as before. Add the forward-compat `/api/` carve-out (CONTEXT
Discretion) as an EXCLUSION prefix only — NOT a new mux registration, because
`/api/integrations/` is already mounted on the parent mux and a second `/api/`
registration would collide. Keep `internal/webui` leaf-level and keep the
exclusion list single-sourced so it cannot drift from `aguiRoutePrefixes`.

Purpose: WEB-01 SC1 — the operator's deep client-route links work AND API 404s
stay real errors (never the SPA shell leaking HTML to an API client).
Output: the `spaFallback` handler, the widened/single-sourced exclusion list,
the updated leaf-embed doc comments, and the flipped + extended tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-web-foundation-serve-auth-health/24-CONTEXT.md
@.planning/phases/24-web-foundation-serve-auth-health/24-RESEARCH.md
@.planning/phases/24-web-foundation-serve-auth-health/24-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 02 owns the starred items)

- ⭐ `internal/webui.spaFallback(...)` (or equivalent) — the SPA-fallback handler (this plan)
- ⭐ the single-source API/agent/health exclusion prefix set incl. `/api/` — derived from `aguiRoutePrefixes` + `integrationsRoutePrefix` (this plan)
- `config.GuardWebBind`, `Config.WebAuthSecret`/`WebTrustProxy` — Plan 01
- `internal/agui/auth.go` (`RequireAuth`, `validateSecret`, `signSession`/`verifySession`, login/logout) — Plan 03
- React login page, `RuntimeHealthPanel`, `NotFoundView`, router + react-query — Plan 04

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SPA-fallback handler in internal/webui (client-route → index.html, excluded prefix → 404)</name>
  <files>internal/webui/embed.go, internal/webui/embed_test.go, internal/webui/doc.go</files>
  <read_first>
    - internal/webui/embed.go (the file being modified: `Handler()` at 30-36 returning a bare `http.FileServerFS`; the `all:dist` embed at 14; `Sub()` at 21; the "DO NOT add fallback logic here" comment at 27-29 that this plan supersedes)
    - internal/webui/embed_test.go (the test template — stdlib testing + httptest, no testify)
    - internal/webui/doc.go (the package doc comment — also references "Phase 24 / DO NOT add fallback")
    - cmd/aura/integrations_proxy.go (lines 78-88) — the `strings.TrimPrefix` + `strings.HasPrefix` dispatch idiom to copy for the prefix check
    - .planning/phases/24-web-foundation-serve-auth-health/24-RESEARCH.md §"Pattern 1" (lines 201-241) — the exact spaFallback body + the single-source note
    - .planning/phases/24-web-foundation-serve-auth-health/24-PATTERNS.md §"internal/webui/embed.go" (lines 40-75) — the analog mapping + the comment-to-update
  </read_first>
  <behavior>
    - GET "/" → index.html (200, text/html)
    - GET "/some/client/route" (no such asset) → index.html (200) — React Router deep link
    - GET "/assets/app-xyz.js" (real embedded asset) → that asset (200, correct MIME)
    - GET "/api/nope" → 404, body NOT containing index.html markers
    - GET "/agent/typo" → 404, never index.html
    - GET "/healthz-typo"/"/readyz-typo"/"/metrics-bad" via an excluded prefix → 404
    - the served index.html still carries the data-theme marker (theme-before-paint contract preserved)
  </behavior>
  <action>
    Change `webui.Handler()` to accept the exclusion prefix set from the caller — signature `Handler(apiPrefixes []string) (http.Handler, error)` (single-source: the caller passes the trimmed copy so `internal/webui` does not hard-code a second list, per RESEARCH §Pattern 1 "single source of truth" + Pitfall 6). Inside, build the `fs.Sub` once and return an `http.HandlerFunc` (`spaFallback`) that: trims the leading `/` from `r.URL.Path`; for each api prefix, if `strings.HasPrefix(rest, prefix)` → `http.NotFound(w, r); return` (real 404, never the shell — WEB-01/SC1); if the trimmed path is empty → use `index.html`; `fs.Stat(sub, p)` and on error set `p = "index.html"` (deep client-route → SPA shell); then set `r.URL.Path = "/" + p` and delegate to the existing `http.FileServerFS(sub)` so MIME + Content-Type stay stdlib-driven. Normalize the incoming prefixes to the trimmed (no-leading-slash) form the comparison expects (the caller may pass `/agent/run` etc.). Keep imports to `embed`/`io/fs`/`net/http`/`strings` ONLY — no `internal/*` (leaf invariant; `scripts/agui_boundary_check.sh` enforces).

    DEEP REFACTOR ON TOUCH: rewrite the embed.go:27-29 "Phase-23 scope is the static placeholder only … DO NOT add fallback logic here" comment AND the matching note in `doc.go` — WEB-01 is the sanctioned supersession; the comments now describe the SPA-fallback + the leaf-level API-exclusion contract (RESEARCH §State of the Art line 510).
  </action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/webui/ && go vet ./internal/webui/ && bash scripts/agui_boundary_check.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/webui/embed.go` contains a `spaFallback` handler and `Handler(apiPrefixes []string) (http.Handler, error)`; it imports no `internal/*` package.
    - Test: a new `TestSPAFallback` in `embed_test.go` asserts: unknown client route → index.html (200); an excluded prefix → 404 (body has no `<div id="root"` / index.html marker); a real asset → 200.
    - Behavior: `go test ./internal/webui/` exits 0.
    - CLI: `bash scripts/agui_boundary_check.sh` exits 0 (leaf invariant intact).
    - Source: the "DO NOT add fallback logic here" comments in embed.go AND doc.go are rewritten to describe the SPA-fallback.
  </acceptance_criteria>
  <done>internal/webui serves index.html for unknown client routes, 404s excluded prefixes, stays leaf-level, and its tests + boundary check are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-source the exclusion list in serve_webui.go + add the /api/ carve-out + flip the no-fallback test</name>
  <files>cmd/aura/serve_webui.go, cmd/aura/serve_webui_test.go</files>
  <read_first>
    - cmd/aura/serve_webui.go (the file being modified: `aguiRoutePrefixes` at 33-40; `newServeHandler` at 47-61; the `integrationsRoutePrefix` mount at 58; the Phase-23 "no SPA catch-all" header comment at 12-18)
    - cmd/aura/serve_webui_test.go (the file being modified: the fake-AG-UI-handler precedence harness at 20-34; the "GET bogus asset -> 404 (no SPA-fallback, Phase 24)" case at 95-108 that MUST flip)
    - cmd/aura/integrations_proxy.go:34 (`integrationsRoutePrefix = "/api/integrations/"` — already mounted; the /api/ carve-out must NOT re-register /api/ on the mux)
    - .planning/phases/24-web-foundation-serve-auth-health/24-PATTERNS.md §"cmd/aura/serve_webui.go" (lines 78-125) + the load-bearing /api/ reconciliation note at the top (lines 14-23) + §"existing test to FLIP" (lines 326-331)
    - .planning/phases/24-web-foundation-serve-auth-health/24-RESEARCH.md §"Pattern 1 — exclusion-list shape definitive recommendation" (lines 240-241) + Pitfall 1/6
  </read_first>
  <behavior>
    - newServeHandler still routes /healthz, /readyz, /debug/vars, /metrics, /agent/run, /threads/ to the AG-UI handler (precedence preserved)
    - /api/integrations/<x> still routes to the integrations proxy (unchanged)
    - GET an unknown client route (e.g. /investigations/42) → the embed handler serves index.html (200)
    - GET /api/nope → real 404 (the /api/ carve-out is in the exclusion list; NOT served the SPA, NOT colliding with /api/integrations/)
    - GET /agent/typo → real 404
    - GET a missing static asset under a client path → index.html (200) (the old "bogus asset → 404" case is now the deep-link case)
  </behavior>
  <action>
    In `cmd/aura/serve_webui.go`: build the single-source exclusion set for the fallback by combining `aguiRoutePrefixes` + `integrationsRoutePrefix` + the new forward-compat `"/api/"` carve-out, and pass it (trimmed to the no-leading-slash form `Handler` expects, or pass as-is if `Handler` normalizes) into `webui.Handler(...)`. CRITICAL (PATTERNS.md load-bearing note): do NOT `mux.Handle("/api/", ...)` — `/api/integrations/` is already registered and a `/api/` registration would shadow/collide; `/api/` lives ONLY in the fallback exclusion list so `/api/anything` 404s today and a real route can register tomorrow. Keep all existing `mux.Handle` registrations exactly as-is (AG-UI prefixes loop + the integrations proxy + `mux.Handle("/", static)` where `static` is now the SPA-fallback handler from `webui.Handler(exclusionPrefixes)`).

    DEEP REFACTOR ON TOUCH: rewrite the serve_webui.go:12-18 header comment that says "Phase-23 scope is the static placeholder ONLY. No SPA catch-all / index.html fallback … is added here … deliberately Phase 24 (WEB-01)" — it now describes the shipped SPA-fallback + the single-source exclusion list + the /api/ carve-out rationale.

    In `cmd/aura/serve_webui_test.go`: FLIP the "GET bogus asset -> 404 (no SPA-fallback, Phase 24)" subtest (lines 95-108). The contract intentionally changed (WEB-01) — this is a sanctioned test rewrite, NOT a test-massage: an unknown client route now returns index.html (200), so rename/repoint that case to assert the SPA-fallback (200 + index.html markers, AG-UI handler NOT hit). ADD a new subtest asserting `/api/nope` and `/agent/typo` → real 404 (never index.html), and a subtest asserting `/api/integrations/...` still reaches the integrations proxy (precedence unbroken). Justify the flip explicitly in the SUMMARY + commit message (CLAUDE.md NEVER MODIFY TESTS TO MAKE THEM PASS — here the contract genuinely changed per WEB-01/SC1).
  </action>
  <verify>
    <automated>cd /d/Aura && go test ./cmd/aura/ -run TestServeWebui && go vet ./cmd/aura/ && go build ./cmd/aura/</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cmd/aura/serve_webui.go` passes a combined exclusion list (incl. `/api/`) into `webui.Handler(...)`; there is NO `mux.Handle("/api/", ...)` registration (only the existing `integrationsRoutePrefix` mount remains for /api/integrations/).
    - Behavior: `go test ./cmd/aura/ -run TestServeWebui` exits 0 with the flipped fallback case + the new `/api/`-404 + `/agent/typo`-404 cases + the integrations-precedence case.
    - Source: the old "GET bogus asset -> 404 (no SPA-fallback, Phase 24)" subtest no longer asserts a 404 for a client route; it asserts index.html.
    - Source: the serve_webui.go:12-18 header comment is rewritten to describe the shipped fallback.
    - Test: `go build ./cmd/aura/` exits 0.
  </acceptance_criteria>
  <done>The parent mux serves the SPA-fallback at "/", excludes API/agent/health + the new /api/ prefix (no mux collision), AG-UI + integrations precedence is preserved, and the flipped + added tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → parent mux `/` catch-all | An arbitrary path decides asset-vs-index.html-vs-404; an API client and a browser cross here. |
| embed FS read | The fallback resolves paths against the embedded `fs.FS`, never the host filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Information Disclosure / contract break | SPA index.html served for a real API 404 (SC1) | mitigate | `apiPrefixes` exclusion in `spaFallback` returns `http.NotFound` for `/api/`, `/agent/`, `/threads/`, health prefixes; asserted by the `/api/nope`/`/agent/typo` → 404 tests (Task 1+2). |
| T-24-06 | Tampering | path traversal via `../` to read host files | mitigate | Resolution is `fs.Stat`/`http.FileServerFS` over the embedded `fs.FS` (sandboxed), never `os.Open` on the host FS; `http.FileServerFS` already cleans paths. |
| T-24-07 | Elevation of Privilege | a new `/api/` route accidentally re-registered, shadowing /api/integrations/ | mitigate | `/api/` is added to the fallback EXCLUSION list only, never `mux.Handle("/api/",…)`; Task 2 asserts /api/integrations/ precedence is unbroken. |
| T-24-08 | Tampering | exclusion list drifts from aguiRoutePrefixes, shadowing a real route | mitigate | Single-source: the fallback exclusion is derived from `aguiRoutePrefixes` + `integrationsRoutePrefix` + `/api/`, not a second hard-coded list (RESEARCH Pitfall 6). |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO packages (stdlib `embed`/`io/fs`/`net/http`/`strings` only); no install task. |
</threat_model>

<verification>
- `go test ./internal/webui/ ./cmd/aura/` green; `go test -race ./internal/webui/ ./cmd/aura/` clean.
- `bash scripts/agui_boundary_check.sh` exits 0 (leaf invariant).
- `go vet ./...` + `go build ./...` clean.
- Manual sanity (optional, documented in SUMMARY): `curl -i .../api/nope` → 404 with non-HTML body; `curl -i .../some/route` → 200 text/html.
</verification>

<success_criteria>
- WEB-01 / SC1 satisfied: unknown client routes serve the SPA shell; API/agent/health/`/api/` prefixes 404 as real errors.
- The leaf-embed invariant holds (no `internal/*` import in `internal/webui`).
- The single-source exclusion list prevents drift; no `/api/` mux collision.
- Coverage on `internal/webui` holds ≥85% (spaFallback fully unit-covered).
</success_criteria>

<output>
Create `.planning/phases/24-web-foundation-serve-auth-health/24-02-SUMMARY.md` when done.
</output>
