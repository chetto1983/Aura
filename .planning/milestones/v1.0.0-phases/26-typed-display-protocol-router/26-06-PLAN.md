---
phase: 26-typed-display-protocol-router
plan: 06
type: execute
wave: 4
depends_on: ["26-02", "26-03", "26-05"]
files_modified:
  - web/src/chat/displays/SourceExplorerSheet.tsx
  - web/src/chat/displays/SourcesButton.tsx
  - web/src/chat/ExternalStoreChat.tsx
  - web/src/chat/displays/__tests__/SourceExplorerSheet.test.tsx
  - web/src/chat/displays/__tests__/SourcesButton.test.tsx
  - web/tests/e2e/replay.spec.ts
  - web/src/i18n/resources.ts
  - stryker.config.json
  - internal/webui/dist
autonomous: true
requirements: [DISP-03, DISP-05]
must_haves:
  truths:
    - "A read-only Source Explorer sheet opens from a citation click-through AND an answer-level 'Sources (N)' button, with Table / Metadata / Configuration views (D-03/D-13/DISP-05)"
    - "The Table view sorts/searches/paginates sources; Metadata + Configuration are read-only (no PATCH/destructive controls); a warning banner shows for incomplete sources"
    - "The Source Explorer and citations share ONE source registry (citations show cited; the Table shows all consulted)"
    - "A reopened thread replays text + tool turns + typed displays identically to live (D-06 e2e)"
    - "The new web/src/chat/displays/ dir is in the Stryker mutation scope and the committed web/dist is rebuilt"
  artifacts:
    - path: "web/src/chat/displays/SourceExplorerSheet.tsx"
      provides: "read-only fullscreen Source Explorer (Table/Metadata/Configuration)"
      contains: "Table"
    - path: "web/src/chat/displays/SourcesButton.tsx"
      provides: "answer-level 'Sources (N)' affordance opening the sheet"
    - path: "web/tests/e2e/replay.spec.ts"
      provides: "Playwright reopen-thread rehydration + display-replay e2e"
  key_links:
    - from: "web/src/chat/displays/CitationBubble.tsx"
      to: "SourceExplorerSheet"
      via: "onOpenSource(refId) opens the sheet to that source"
      pattern: "onOpenSource|SourceExplorer"
    - from: "web/src/chat/displays/SourcesButton.tsx"
      to: "SourceExplorerSheet"
      via: "answer-level 'Sources (N)' opens the sheet"
      pattern: "Sources"
---

<objective>
Complete the deep-search evidence surface: a READ-ONLY Source Explorer fullscreen sheet (Table / Metadata / Configuration views, D-03) reachable two ways — a citation click-through (the `onOpenSource` callback from 26-05) and an answer-level "Sources (N)" button (D-13) — backed by the SAME source registry the citations use. Plus the Playwright replay e2e that proves D-06 end-to-end (reopen → rehydrate → displays render identically to live), the Stryker mutation-scope update for the new `displays/` dir, and the committed `web/dist` rebuild + i18n finalization.

Purpose: This closes DISP-05 (Source Explorer) and the DISP-03 source-view path, and proves the D-06 prerequisite (26-02) end-to-end. It is the final wave — it consumes the registry (26-01), the rehydration (26-02), the image-proxy (26-03), and the CitationBubble click-through (26-05).

Output: The read-only Source Explorer, the "Sources (N)" affordance, the citation click-through wiring, the replay e2e, the mutation-scope update, and a fresh committed `dist`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-typed-display-protocol-router/26-CONTEXT.md
@.planning/phases/26-typed-display-protocol-router/26-RESEARCH.md
@.planning/phases/26-typed-display-protocol-router/26-UI-SPEC.md
@.planning/phases/26-typed-display-protocol-router/26-02-SUMMARY.md
@.planning/phases/26-typed-display-protocol-router/26-05-SUMMARY.md
@.claude/skills/frontend-design/SKILL.md
@.claude/skills/accessibility/SKILL.md
@.claude/skills/vercel-react-best-practices/SKILL.md
</context>

<artifacts_produced>
NEW symbols this plan creates (excluded from downstream drift verification):
- `web/src/chat/displays/SourceExplorerSheet.tsx` (`SourceExplorerSheet`) with the read-only Table/Metadata/Configuration tabs + warning banner.
- `web/src/chat/displays/SourcesButton.tsx` (`SourcesButton`, the answer-level "Sources (N)" affordance).
- `web/src/chat/ExternalStoreChat.tsx`: the answer-chrome "Sources (N)" mount + the source-explorer open state shared with CitationBubble's `onOpenSource`.
- `web/tests/e2e/replay.spec.ts` (the Playwright replay spec).
- `stryker.config.json`: the `web/src/chat/displays/` mutation-scope inclusion.
- New i18n keys under `source.*` (en + it).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SourceExplorerSheet (read-only Table/Metadata/Configuration) + warning banner</name>
  <files>web/src/chat/displays/SourceExplorerSheet.tsx, web/src/chat/displays/__tests__/SourceExplorerSheet.test.tsx, web/src/i18n/resources.ts</files>
  <read_first>
    - web/src/chat/displays/types.ts (the `Source` registry interface from 26-02 — the sheet's data; `cited` vs consulted)
    - web/src/chat/displays/DisplayPagination.tsx (from 26-02 — the Table view paginates with it)
    - web/src/shell/Drawer.tsx (the committed fullscreen-sheet pattern + `--shadow-drawer` + the `font-display text-base` sheet title :88 — the Source Explorer title is `font-display text-lg`/18px per UI-SPEC)
    - D:/tmp/elysia-frontend/...DataExplorer.tsx / DataConfig.tsx / DataMetadata.tsx (the Table/Metadata/Configuration shapes — take the READ subset ONLY; elysia's Re-Analyze/Clear/PATCH-write are DEFERRED to Phase 29, do NOT port them)
    - .planning/phases/26-typed-display-protocol-router/26-UI-SPEC.md §"Component Inventory" (SourceExplorerSheet row) + §"Copywriting Contract" (Source Explorer tabs, table headers, source status tags Cited/Consulted, warning banner) + §"Typography" (18px Fraunces section title) + §"Color" (cited=accent tag, consulted=neutral; active tab accent underline) + §"Security & Safety Posture" (read-only, no PATCH/write/destructive)
    - .planning/phases/26-typed-display-protocol-router/26-CONTEXT.md D-03 (read-only), D-13 (fullscreen sheet)
  </read_first>
  <behavior>
    - Test (DISP-05): the sheet renders three tabs — Table, Metadata, Configuration — all read-only; the Table view sorts/searches/paginates the `Source` rows (# / Type / Title / Source / Status with a Cited/Consulted tag).
    - Test (D-03 negative): the Metadata + Configuration panes contain NO PATCH/destructive controls (no Re-Analyze/Clear/Save button) — assert absence.
    - Test: a source flagged incomplete/unprocessed shows the warning banner (`text-warning`, read-only notice).
    - Test: Esc closes the sheet; the title is the 18px Fraunces section title; focus is trapped/returned (sheet a11y).
  </behavior>
  <action>
    Create `SourceExplorerSheet.tsx`: a fullscreen sheet (committed `--shadow-drawer` + the 18px `font-display` title) with a Table/Metadata/Configuration tab strip (accent underline on the active tab). Table view: a native `<table>` over the `Source` registry (columns # / Type / Title / Source / Status), client-side sort/search + `DisplayPagination`, a `Cited` (accent) / `Consulted` (neutral) status tag. Metadata + Configuration: READ-ONLY panes (the read subset of elysia's DataMetadata/DataConfig — NO PATCH, NO Re-Analyze/Clear; those are Phase 29). A warning banner for incomplete/unprocessed sources. Esc closes; focus trap + return. Add `source.*` i18n (en + it). Tests including the read-only-negative assertion.
  </action>
  <verify>
    <automated>cd web && npm run test -- SourceExplorerSheet && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `cd web && npm run test -- SourceExplorerSheet` passes the Table sort/search/paginate + Metadata/Configuration read-only + warning-banner + Esc-close assertions (VALIDATION.md DISP-05 row).
    - BEHAVIOR: a test asserts NO PATCH/destructive control in Metadata/Configuration (D-03 read-only).
    - SOURCE: `grep -nE "Table|Metadata|Configuration" web/src/chat/displays/SourceExplorerSheet.tsx` confirms the three views.
    - CLI: `cd web && npm run lint` clean (native table, tab a11y, focus trap).
  </acceptance_criteria>
  <done>The read-only Source Explorer sheet renders Table/Metadata/Configuration over the shared registry with sort/search/paginate, a warning banner, no write controls, and sheet a11y; tests + lint pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 'Sources (N)' answer affordance + citation click-through wiring</name>
  <files>web/src/chat/displays/SourcesButton.tsx, web/src/chat/ExternalStoreChat.tsx, web/src/chat/displays/__tests__/SourcesButton.test.tsx, web/src/i18n/resources.ts</files>
  <read_first>
    - web/src/chat/displays/CitationBubble.tsx (from 26-05 — the `onOpenSource(refId)` callback contract recorded in 26-05-SUMMARY; the click-through opens the sheet to that source)
    - web/src/chat/ExternalStoreChat.tsx (the answer action bar region :444+ Copy/Edit/Reload — the "Sources (N)" affordance mounts at the answer level here; the rehydration state from 26-02)
    - web/src/chat/displays/SourceExplorerSheet.tsx (from Task 1 — the sheet the button + citation open)
    - .planning/phases/26-typed-display-protocol-router/26-UI-SPEC.md §"Copywriting Contract" ('Sources (N)' affordance key + aria-label) + §"Color" (accent #4 — "Sources (N)" text + active view-toggle) + §"Interaction" (keyboard/tap; not hover-only)
    - .planning/phases/26-typed-display-protocol-router/26-CONTEXT.md D-13 (two open paths) + D-04 (citation click opens the Source Explorer)
  </read_first>
  <behavior>
    - Test (DISP-05/D-13): `SourcesButton` shows "Sources (N)" with N = consulted-source count and opens the Source Explorer sheet on click/Enter/tap; N=0 hides the button (empty-state: no sources).
    - Test (D-04): a CitationBubble `onOpenSource(refId)` opens the sheet scrolled/focused to that source.
    - Test: the two paths share ONE sheet-open state (opening from a citation and from the button render the same sheet over the same registry).
  </behavior>
  <action>
    Create `SourcesButton.tsx`: the answer-level "Sources (N)" affordance (accent text, aria-label "View N sources"), hidden when N=0, opening the `SourceExplorerSheet`. In `ExternalStoreChat.tsx`, mount `SourcesButton` in the answer chrome and lift a single `sourceExplorerOpen`/`focusRefId` state shared between `SourcesButton` and the CitationBubble `onOpenSource` callback (one sheet, two entry points, one registry — D-13). Wire the CitationBubble click-through to set `focusRefId` + open. Add the `source.sources` i18n key (en + it). Tests for both open paths + the N=0 hide.
  </action>
  <verify>
    <automated>cd web && npm run test -- SourcesButton && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `cd web && npm run test -- SourcesButton` passes the "Sources (N)" open + N=0 hide + click/Enter/tap assertions (VALIDATION.md DISP-05/D-13).
    - BEHAVIOR: a test asserts a CitationBubble `onOpenSource(refId)` opens the same sheet focused to that source (D-04).
    - SOURCE: `grep -nE "onOpenSource|SourceExplorer" web/src/chat/ExternalStoreChat.tsx` confirms the shared-state wiring.
    - CLI: `cd web && npm run typecheck && npm run lint` clean.
  </acceptance_criteria>
  <done>The "Sources (N)" answer affordance and the citation click-through both open one shared read-only Source Explorer over one registry; N=0 hides the button; tests + typecheck + lint pass.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright replay e2e (D-06) + Stryker mutation scope + committed dist rebuild</name>
  <files>web/tests/e2e/replay.spec.ts, stryker.config.json, internal/webui/dist</files>
  <read_first>
    - web/playwright.config.ts (the existing e2e config + the Phase-25 e2e specs as the pattern; how a thread is opened/seeded)
    - web/src/chat/displays/snapshotToMessages.ts + ExternalStoreChat.tsx (from 26-02 — the rehydration the e2e exercises)
    - .planning/phases/26-typed-display-protocol-router/26-RESEARCH.md §"Validation Architecture" (the D-06 e2e row: `npm run test:e2e -- replay`) + §"Wave 0 Gaps" (Stryker config must include the new displays/ dir)
    - CLAUDE.md (committed dist = `internal/webui/dist` NOT `web/dist`; rebuild after copy/dist changes; the CI dist-freshness gate)
    - .planning/phases/26-typed-display-protocol-router/26-VALIDATION.md (the full Per-Task Verification Map this e2e closes)
  </read_first>
  <behavior>
    - Test (D-06 e2e): run a web_search-bearing turn, confirm a typed display renders live; reopen the thread (reload/navigate) and confirm `GET /threads/{id}/messages` is fetched and the text + tool turns + the typed display re-render identically (no blank viewport).
    - Behavior: Stryker's mutation scope includes `web/src/chat/displays/` so the new components are mutation-tested (≥70% killed, project policy).
    - Behavior: the embedded `internal/webui/dist` is rebuilt from `web/` so the CI dist-freshness gate stays green.
  </behavior>
  <action>
    Create `web/tests/e2e/replay.spec.ts`: a Playwright spec that opens a thread, triggers a turn that emits a typed display (or seeds a thread with one), reloads/reopens, and asserts the `GET /threads/{id}/messages` rehydration fetch fires and the display re-renders identically to live (D-06). Update `stryker.config.json` to include `web/src/chat/displays/**` in the mutation scope. Rebuild the embedded `internal/webui/dist` from `web/` (the committed-dist invariant) so the dist-freshness CI gate passes. Run the full frontend gate to confirm Vitest ≥85% + Stryker ≥70% on the new dir.
  </action>
  <verify>
    <automated>cd web && npm run test:e2e -- replay && npm run test && npm run mutation</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `cd web && npm run test:e2e -- replay` passes the reopen→rehydrate→display-replay assertion (VALIDATION.md D-06 e2e row).
    - BEHAVIOR: `cd web && npm run test` reports ≥85% coverage including `web/src/chat/displays/`; `cd web && npm run mutation` reports ≥70% killed on the displays dir (project frontend gate).
    - SOURCE: `grep -n "displays" stryker.config.json` confirms the mutation scope inclusion.
    - CLI: the committed `internal/webui/dist` is rebuilt (the dist-freshness gate passes; confirm `git status` shows the rebuilt dist staged).
  </acceptance_criteria>
  <done>The Playwright replay e2e proves D-06 end-to-end; the new displays dir is in the Stryker scope at ≥70% killed; Vitest ≥85% holds; the committed dist is rebuilt; the frontend gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source registry → Source Explorer | The explorer renders the trusted registry read-only; no write path crosses back to the backend |
| reopened thread → rehydrated render | Replay must render the same trusted normalizer output as live (no new trust granted on replay) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-19 | Elevation | `SourceExplorerSheet` | mitigate | READ-ONLY this milestone (D-03): no PATCH/write/destructive control; a test asserts the absence of Re-Analyze/Clear/Save — the governance-write surface is deferred to Phase 29 |
| T-26-20 | Information Disclosure | Source Explorer `Source` rows | mitigate | The sheet renders only the registry fields (title/url/snippet/type/cited); it surfaces no SSRF internals or raw backend error text — the registry is built by the trusted normalizer (26-01) |
| T-26-21 | Spoofing | citation click-through | mitigate | `onOpenSource(refId)` resolves against the code-owned registry; a refId not in the registry opens nothing — no fabricated source target (D-05) |
| T-26-SC | Tampering | npm installs | mitigate | NO new dependency in this plan (reuses 26-05's Radix hovercard + DisplayPagination + native sheet); slopcheck N/A |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run lint` clean.
- `cd web && npm run test -- SourceExplorerSheet SourcesButton` green; the read-only-negative + two-open-path assertions pass.
- `cd web && npm run test:e2e -- replay` green (D-06 end-to-end).
- `cd web && npm run test` ≥85% incl. displays/; `cd web && npm run mutation` ≥70% killed on displays/.
- `bash scripts/cache_invariant_audit.sh` still green; `npm run contrast` green for new pairings; `internal/webui/dist` rebuilt.
</verification>

<success_criteria>
- The read-only Source Explorer (Table/Metadata/Configuration) opens from a citation click-through AND "Sources (N)", over one shared registry.
- No write/destructive control exists (D-03).
- A reopened thread replays text + tool turns + typed displays identically to live (D-06 e2e green).
- The new displays dir is in the Stryker scope at ≥70% killed; Vitest ≥85% holds; the committed dist is rebuilt.
- All targeted Vitest + Playwright + mutation + lint + typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/26-typed-display-protocol-router/26-06-SUMMARY.md` when done. Record the final phase verification state (every DISP-01..05 + SWARM-01 requirement → its green test command) for `/gsd-verify-work`.
</output>
