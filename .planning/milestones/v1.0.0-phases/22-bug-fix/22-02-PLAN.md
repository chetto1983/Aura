---
phase: 22-bug-fix
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - internal/secret/envkey.go
  - internal/secret/envkey_test.go
  - internal/agent/tools/shell_exec.go
  - internal/agent/tools/shell_exec_env.go
  - internal/agent/tools/shell_exec_mergeenvcap_test.go
  - internal/agent/hooks_command.go
  - internal/agent/hooks_command_test.go
  - internal/reasoningtrace/reasoningtrace.go
  - internal/reasoningtrace/reasoningtrace_test.go
  - internal/reasoningtrace/trace_rowcap_test.go
  - internal/agent/metrics.go
  - internal/agent/tracing.go
  - internal/agent/tracing_test.go
  - internal/agent/llm_agent.go
  - internal/agent/llm_agent_dispatch.go
  - internal/agent/llm_agent_completion.go
  - internal/agent/llm_agent_finalize.go
  - internal/agent/llm_agent_hooks_test.go
autonomous: true
requirements: [HARDEN-01, HARDEN-04, HARDEN-05, HARDEN-12]
must_haves:
  truths:
    - "D-01: automated per-finding gate applies here; secret and observability fixes must have named regression tests."
    - "D-04a: evidence and ledger artifacts stay in docs/audit, not scattered."
    - "D-09: stay in internal/agent plus explicitly named one-liners; do not take unrelated persistence-layer redaction unless AG-034 proves an agent-side shape change."
    - "Credentials never cross into shell_exec child env, command-hook child env, shell output, or default reasoning trace rows."
    - "Telemetry cannot crash the daemon; entropy failure falls back to a safe span ID and increments/logs a failure signal."
  artifacts:
    - path: "internal/secret/envkey.go"
      provides: "DSN/URL/URI/CONN/PWD/COOKIE/SESSION/JWT secret classification"
      contains: "IsSecretEnvKey"
    - path: "internal/agent/tools/shell_exec_env.go"
      provides: "DSN credential output redaction"
      contains: "postgres"
    - path: "internal/reasoningtrace/reasoningtrace.go"
      provides: "default non-verbatim trace rows with field caps"
      contains: "history"
    - path: "internal/agent/metrics.go"
      provides: "turn, LLM, tool, hook, token, panic, and span-export metrics"
      contains: "aura_agent_turn_total"
    - path: "internal/agent/tracing.go"
      provides: "never-panic span ID and exporter boot/failure observability"
      contains: "mintSpanID"
---

<objective>
Close the secrets and observability P1 cluster: AG-010/AG-009/AG-047 plus the AG-003 minimal-env slice, and AG-012/AG-013/AG-033/AG-056/AG-057. After this wave, credentials do not leak by default and production has metrics/logs for later hardening failures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bug-fix/22-SPEC.md
@.planning/phases/22-bug-fix/22-CONTEXT.md
@.planning/phases/22-bug-fix/22-RESEARCH.md
@docs/audit/bug-report.md
@docs/audit/proposed-patches.md
@docs/audit/testing-strategy.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Secret boundary and default trace privacy for AG-010, AG-009, AG-047, AG-003-minimal-env</name>
  <files>internal/secret/envkey.go, internal/agent/tools/shell_exec*.go, internal/agent/hooks_command.go, internal/reasoningtrace/reasoningtrace.go, related tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-003, AG-009, AG-010, AG-047 and `docs/audit/proposed-patches.md` PP-4/PP-5.
    - `internal/secret/envkey.go` and `internal/secret/envkey_test.go`.
    - `internal/agent/tools/shell_exec.go`, `shell_exec_env.go`, `shell_exec_mergeenvcap_test.go`.
    - `internal/agent/hooks_command.go` `run` and env construction.
    - `internal/reasoningtrace/reasoningtrace.go` and trace tests.
    - `.claude/skills/golang-security/SKILL.md` secrets/logging guidance.
  </read_first>
  <action>
    Extend secret classification so `IsSecretEnvKey("AURA_DB_URL")` is true and DSN-shaped names/values do not reach shell children. Add focused helpers if needed, for example a key-only classifier preserved for compatibility plus a key/value classifier used while building child env.

    Add DSN credential redaction to shell output redaction, including `scheme://user:pass@host` forms. Keep useful non-secret URL text where possible, but mask credentials.

    In command hooks, default `cmd.Env` to a minimal allowlist such as `PATH` plus explicit hook config env. Do not inherit provider, DB, Neo4j, Telegram, cookie, JWT, DSN, URI, CONN, or PWD-shaped secrets from `os.Environ()`.

    Change reasoning trace default behavior so full `history` and raw user prompt text are not written verbatim unless an explicit loud full-trace mode is enabled. Log hashes/counts/lengths by default, cap per-field size before redaction, and document the full-trace PII warning in code comments or docs near the env handling.
  </action>
  <verify>
    <automated>go test ./internal/secret -run 'TestIsSecretEnvKey|TestSecret' -count=1</automated>
    <automated>go test ./internal/agent/tools -run 'TestShellExec.*Env|TestRedact.*DSN|TestMergeEnv' -count=1</automated>
    <automated>go test ./internal/agent -run 'TestCommandHook.*Env|TestCommandHook.*Secret|TestLlmAgentHooks' -count=1</automated>
    <automated>go test ./internal/reasoningtrace -run 'Test.*History|Test.*Redact|Test.*RowCap' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `IsSecretEnvKey("AURA_DB_URL") == true`.
    - A `shell_exec` child cannot read a composed credential-bearing DSN from inherited env.
    - Shell output redacts `postgres://u:p@h`-style credentials.
    - A command-hook child env carries no secret-named variables by default.
    - Default reasoning trace does not write full `history` verbatim and caps large fields.
  </acceptance_criteria>
  <done>Secret-bearing env and trace data are stripped/redacted by default with named regression tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Observability minimum and never-panic telemetry for AG-012, AG-013, AG-033, AG-056, AG-057</name>
  <files>internal/agent/metrics.go, internal/agent/tracing.go, internal/agent/llm_agent*.go, hook/dispatch/finalize tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-012, AG-013, AG-033, AG-056, AG-057 and `docs/audit/proposed-patches.md` PP-6.
    - `internal/agent/metrics.go` existing promauto/non-default registry shape.
    - `internal/agent/tracing.go` exporter setup and `mintSpanID`.
    - `internal/agent/llm_agent.go`, `llm_agent_dispatch.go`, `llm_agent_completion.go`, `llm_agent_finalize.go` terminal reason paths.
    - `internal/agent/hooks.go` and `hooks_command.go` hook boundaries.
    - `.claude/skills/golang-error-handling/SKILL.md` and `.claude/skills/golang-lint/SKILL.md`.
  </read_first>
  <action>
    Extend the existing metric substrate instead of creating a second registry. Add or complete:
    - `aura_agent_turn_total{outcome}`
    - `aura_agent_llm_call_duration_seconds`
    - `aura_agent_llm_errors_total{kind}`
    - `aura_agent_tool_errors_total{tool}`
    - token/cost counters if token data is available at call sites
    - hook counters by hook point/outcome
    - `aura_agent_panic_total{site}` from Plan 22-01 recovery sites
    - `aura_agent_span_export_failures_total`

    Emit `turn_total` exactly once for every terminal `turnReason`. Emit LLM latency around the actual model call, not only stream-open setup. Emit tool/hook errors at the boundary where the error is classified.

    Add structured `slog` at turn start/end, LLM call end/error, tool error, hook error, and recovered panic boundaries. Keep log messages low-cardinality and use request/thread/tool/hook IDs as attributes.

    Replace `mintSpanID` hard panic with zero-ID fallback plus metric/log. Add a boot log stating exporter mode and endpoint, and expose/export failure visibility without crashing.
  </action>
  <verify>
    <automated>go test ./internal/agent -run 'Test.*Metric|Test.*Turn.*Counter|Test.*LLM.*Duration|Test.*Tool.*Error|Test.*Hook.*Metric' -count=1</automated>
    <automated>go test ./internal/agent -run 'TestMintSpanID|TestTracing|TestSpan' -count=1</automated>
    <automated>go test ./internal/agent -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Every terminal `turnReason` has a metric-contract test proving exactly one labeled counter increment.
    - LLM latency histogram is registered and observeable in the package registry.
    - Tool errors and hook failures increment bounded-label counters.
    - Injected entropy failure in `mintSpanID` does not panic and records a fallback signal.
    - Re-registering metrics in tests does not panic; custom/non-default registry behavior remains safe.
  </acceptance_criteria>
  <done>Production observability minimum exists and telemetry cannot crash the daemon.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-22-04 | Information Disclosure | child process env | mitigate | secret key/value classification strips DSN and provider credentials |
| T-22-05 | Information Disclosure | reasoning trace | mitigate | default trace stores hashes/counts/capped fields, not full conversation history |
| T-22-06 | Denial of Service | telemetry | mitigate | entropy/export failures log and count, never panic |
| T-22-07 | Repudiation | production operation | mitigate | turn/tool/hook/LLM metrics and slog provide traceable outcomes |
</threat_model>

<verification>
- `go test ./internal/secret ./internal/reasoningtrace -count=1`
- `go test ./internal/agent/tools -run 'TestShellExec.*Env|TestRedact|TestMergeEnv' -count=1`
- `go test ./internal/agent -run 'TestCommandHook|Test.*Metric|TestTracing|TestMintSpanID' -count=1`
- `go test ./internal/agent/... -count=1`
</verification>

<success_criteria>
- HARDEN-04: DSN/env/trace/hook secrets do not leak by default.
- HARDEN-05: production metrics/logging exists for turn outcomes, LLM latency, errors, hooks, tokens where available, panics, and span export failures.
- HARDEN-01: recovered panic metrics from Plan 22-01 remain wired.
- HARDEN-12: AG-003 minimal-env, AG-009, AG-010, AG-012, AG-013, AG-033, AG-047, AG-056, AG-057 have fixed+test ledger entries.
</success_criteria>

<output>
Create `.planning/phases/22-bug-fix/22-02-SUMMARY.md` with metric names, secret-boundary behavior, trace-default behavior, and tests added.
</output>
