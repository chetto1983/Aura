---
phase: 22-bug-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/llm_agent_parallel.go
  - internal/agent/llm_agent_parallel_test.go
  - internal/agent/llm_agent.go
  - internal/agent/main_test.go
  - internal/agent/metrics.go
  - internal/agent/workflow/parallel.go
  - internal/agent/workflow/parallel_test.go
  - internal/swarm/swarm.go
  - internal/swarm/swarm_test.go
  - internal/agent/tools/shell_bg.go
  - internal/agent/tools/shell_bg_test.go
  - internal/agent/budget_dedup.go
  - internal/agent/budget_dedup_test.go
autonomous: true
requirements: [HARDEN-01, HARDEN-02, HARDEN-09, HARDEN-12]
must_haves:
  truths:
    - "D-00a: every AG-### touched in this crash/race cluster is fixed+test or explicitly ledgered; no audit residue is allowed."
    - "D-00b: build the minimal real panic firewall and dedup hardening; no doc-only downgrade."
    - "D-04: deterministic fail-before/pass-after is required; panic and race regressions must fail on the pre-fix tree and pass after."
    - "D-11: commits stay atomic by finding or tight cluster, with AG-001 / AG-002 / AG-039 / AG-040 in commit messages."
    - "A panic recovered from executeBatch, workflow parallel, swarm wave, or shell_bg reaper becomes a model-visible per-call/per-child error, not a process crash."
    - "dedupRing safety does not rely on dispatch staying serial forever; the ring is concurrency-safe by construction."
  artifacts:
    - path: "internal/agent/llm_agent_parallel.go"
      provides: "panic firewall around executeBatch parallel and inline execution paths"
      contains: "recover"
    - path: "internal/agent/workflow/parallel.go"
      provides: "panic-to-child-error conversion in parallel child execution"
      contains: "recover"
    - path: "internal/swarm/swarm.go"
      provides: "panic-to-failed-child report in swarm wave execution"
      contains: "recover"
    - path: "internal/agent/tools/shell_bg.go"
      provides: "panic-safe background reaper goroutine"
      contains: "recover"
    - path: "internal/agent/budget_dedup.go"
      provides: "mutex-protected dedup ring with bounded result retention and period-3+ cycle handling"
      contains: "sync.Mutex"
---

<objective>
Remove the two crash-class defects at the front of Phase 22: AG-001 (unrecovered spawned-goroutine panics) and AG-002 (dedupRing data-race fatal). While touching `budget_dedup.go`, also close the local dedup hardening findings AG-039 and AG-040 so the executor does not double-touch the same core loop file later.

Purpose: a panicking tool, workflow child, swarm child, or `shell_bg` reaper must never crash `aura serve`, and the dedup ring must be safe even if future code calls it from parallel branches. This is the base wave for every later hardening change.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bug-fix/22-SPEC.md
@.planning/phases/22-bug-fix/22-CONTEXT.md
@.planning/phases/22-bug-fix/22-RESEARCH.md
@docs/audit/bug-report.md
@docs/audit/proposed-patches.md
@docs/audit/testing-strategy.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Goroutine panic firewall for AG-001</name>
  <files>internal/agent/llm_agent_parallel.go, internal/agent/workflow/parallel.go, internal/swarm/swarm.go, internal/agent/tools/shell_bg.go, internal/agent/metrics.go, related tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-001 and `docs/audit/proposed-patches.md` PP-1.
    - `internal/agent/llm_agent_parallel.go` full file; current `executeBatch` has an inline single-call path and spawned goroutines for batch calls.
    - `internal/agent/workflow/parallel.go` `Run` and child runner paths; preserve serial yield and leak-safety invariants.
    - `internal/swarm/swarm.go` `runWave` and `runChild`; preserve ordered reports.
    - `internal/agent/tools/shell_bg.go` reaper goroutine, `finish`, and registry methods.
    - Existing `goleak` TestMain files in `internal/agent`, `internal/agent/workflow`, `internal/swarm`, and `internal/agent/tools`.
  </read_first>
  <action>
    Add package-local recovery wrappers at each goroutine boundary instead of introducing import cycles. `executeBatch` must recover both the inline single-call path and each spawned call, writing the recovered panic into the corresponding `toolRunResult` with `Err`, a safe preview, `ToolCallID`, `ToolName`, and `EndedAt`.

    In workflow parallel execution, wrap each child body so a panic becomes one failed child event/error surfaced once through the iterator contract. Preserve the existing rule that the iterator frame is the only goroutine that calls `yield`.

    In `swarm.runWave`, recover inside each child goroutine and write a deterministic failed `ChildReport` for the affected goal only. Siblings must keep running.

    In the `shell_bg` reaper goroutine, recover and mark the shell done with a failure status rather than leaving it permanently running or crashing the process.

    Add or extend a low-cardinality panic metric seam in `internal/agent/metrics.go` such as `panic_total{site}`. Plan 22-02 will expand the broader observability surface, but this plan must record recovered panics from the start.

    Add a Runner-level per-turn recover backstop at the outer agent run-consumption boundary. The backstop is not a substitute for child-goroutine recovery; it catches remaining same-goroutine panics and converts them into one terminal error event.
  </action>
  <verify>
    <automated>go test -race ./internal/agent -run 'TestExecuteBatch.*Panic|TestLlmAgent.*Panic' -count=1</automated>
    <automated>go test -race ./internal/agent/workflow -run 'TestParallel.*Panic|TestParallel.*Leak' -count=1</automated>
    <automated>go test -race ./internal/swarm -run 'TestSwarm.*Panic|TestRunWave.*Panic' -count=1</automated>
    <automated>go test -race ./internal/agent/tools -run 'TestBackgroundShell.*Panic|TestShellBg.*Panic' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A deliberately panicking fake tool through `executeBatch` returns a model-visible per-call error and the test process survives under `-race`.
    - A deliberately panicking workflow child through `parallel.Run` surfaces as a failed child/error without leaking goroutines.
    - A deliberately panicking swarm child produces a failed child report, keeps sibling reports ordered, and does not crash the process.
    - A deliberately panicking shell background reaper leaves the shell in a terminal failed/done state and does not leak.
    - The panic metric increments with a bounded `site` label for each recovered site.
  </acceptance_criteria>
  <done>AG-001 has child-goroutine recovery at every named boundary, plus a per-turn backstop and focused `-race`/`goleak` tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dedupRing mutex + bounded cycle state for AG-002, AG-039, AG-040</name>
  <files>internal/agent/budget_dedup.go, internal/agent/budget_dedup_test.go</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-002, AG-039, AG-040 and `docs/audit/proposed-patches.md` PP-2.
    - `internal/agent/budget_dedup.go` full file, including `dedupRing`, `BeforeToolCall`, `AfterToolResult`, `push`, `countConsecutive`, and `isPingPong`.
    - `internal/agent/budget_dedup_test.go` existing period-1 and period-2 tests.
    - `.claude/skills/golang-concurrency/SKILL.md` mutex guidance: keep critical sections short, never hold locks across I/O.
  </read_first>
  <action>
    Add `mu sync.Mutex` to `dedupRing`. Lock at the top of public mutation/read methods that access `entries` or `results`; keep helpers unexported and documented as "caller holds lock" if they are invoked under the same critical section.

    When `push` evicts an old fingerprint from `entries`, prune the corresponding `results` entry if no remaining entry references that fingerprint. This closes AG-039 while the map/slice ownership is already being made explicit.

    Generalize cycle detection beyond period-1 and period-2 to catch repeated subsequences within the configured dedup window, including A-B-C-A-B-C. Preserve existing period-1/period-2 semantics and error strings unless tests intentionally pin a new reason.

    Keep `Budget.Child` semantics unchanged: children still fork distinct dedup rings and share only the atomic step counter.
  </action>
  <verify>
    <automated>go test -race ./internal/agent -run 'TestBudget_Dedup|TestDedupRing' -count=1</automated>
    <automated>go test -race ./internal/agent -run 'TestBudget_BeforeAfterToolResult_Concurrent|TestDispatch.*Parallel.*Dedup' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Direct concurrent `BeforeToolCall` / `AfterToolResult` hammer test is clean under `-race`.
    - Multi-tool dispatch with more than one parallel tool remains clean under `-race`.
    - Existing period-1 and period-2 dedup tests still pass.
    - New period-3+ oscillation test terminates before budget exhaustion.
    - New pruning test proves `results` does not grow beyond live ring state across evictions.
  </acceptance_criteria>
  <done>AG-002 is fixed by construction; AG-039/AG-040 are closed in the same file-touch with race-clean tests.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-22-01 | Denial of Service | spawned goroutines | mitigate | recover at each goroutine boundary and convert panic to per-call/per-child failure |
| T-22-02 | Denial of Service | dedup map/slice | mitigate | mutex protects mutable ring state; concurrent-map-write fatal eliminated |
| T-22-03 | Repudiation | recovered panic observability | mitigate | panic_total records site so recovered panics are not invisible |
</threat_model>

<verification>
- `go test -race ./internal/agent -run 'TestExecuteBatch.*Panic|TestBudget_Dedup|TestDedupRing' -count=1`
- `go test -race ./internal/agent/workflow -run 'TestParallel' -count=1`
- `go test -race ./internal/swarm -run 'TestSwarm|TestRunWave' -count=1`
- `go test -race ./internal/agent/tools -run 'TestBackgroundShell|TestShellBg' -count=1`
- `go test -race ./internal/agent/... ./internal/swarm/... -count=1`
</verification>

<success_criteria>
- HARDEN-01: panicking tool / workflow child / swarm child / `shell_bg` reaper cannot crash `aura serve`; panic is model-visible and leak-free.
- HARDEN-02: dedup ring is mutex-protected and race-clean.
- HARDEN-09: dedup growth and period-3+ loop hardening are closed while the file is touched.
- HARDEN-12: AG-001, AG-002, AG-039, and AG-040 have fixed+test ledger entries.
</success_criteria>

<output>
Create `.planning/phases/22-bug-fix/22-01-SUMMARY.md` when done, with the exact recovery sites, test names, and AG ledger status for AG-001, AG-002, AG-039, and AG-040.
</output>
