---
phase: 22-bug-fix
plan: 05
type: execute
wave: 5
depends_on: ["22-01", "22-02", "22-03", "22-04"]
files_modified:
  - internal/agent/tools/skill.go
  - internal/agent/tools/skill_write.go
  - internal/agent/tools/skill_test.go
  - internal/agent/tools/skill_write_test.go
  - internal/agent/event.go
  - internal/agent/event_test.go
  - docs/audit/22-finding-ledger.md
  - docs/audit/22-LIVE-SIGNOFF-2026-06-15.md
  - docs/audit/README.md
  - docs/audit/audit-index.json
  - .planning/phases/22-bug-fix/22-VALIDATION.md
  - .planning/phases/22-bug-fix/22-UAT.md
autonomous: true
requirements: [HARDEN-01, HARDEN-02, HARDEN-03, HARDEN-04, HARDEN-05, HARDEN-06, HARDEN-07, HARDEN-08, HARDEN-09, HARDEN-10, HARDEN-11, HARDEN-12]
must_haves:
  truths:
    - "D-00a: fix everything, zero audit residue; every AG-### is fixed+test, accepted+rationale, or confirmed+routed."
    - "D-00b: build minimal-real machinery; no doc-only downgrade for actionable internal/agent fixes."
    - "D-01: full live sign-off pass is required on top of per-finding automated gates."
    - "D-02: live harness parity means aura serve with PG, Neo4j, MCP, embed sidecar, SearXNG bridge, host aura chat trace, metrics scrape, and CDP Telegram round-trip."
    - "D-03: live pass drives the full tool surface including GLM OCR multimodal and empirically validates AURA_FS_MAX_READ_BYTES."
    - "D-04: strict fail-before/pass-after for deterministic findings; race/goleak pre/post proof for concurrency findings."
    - "D-04a: finding ledger and live evidence live in docs/audit."
    - "D-05: fs cap starts at 10 MiB and is tunable based on live pass evidence."
    - "D-06: MCP timeout migration note is included in close-out."
    - "D-07: router remains on by default and bounded."
    - "D-08: MCP breaker defaults are documented."
    - "D-09: cross-package findings are confirmed and routed rather than silently fixed outside scope."
    - "D-10: execute phase uses full GSD wave execution."
    - "D-11: atomic commits name AG-### findings."
    - "D-12: master-direct workflow is respected unless the operator says otherwise."
    - "D-13: each wave is automated-green before the next; full live sign-off runs once at close."
  artifacts:
    - path: "internal/agent/tools/skill.go"
      provides: "single honest skill schema"
      contains: "skillParamsSchemaHonest"
    - path: "internal/agent/tools/skill_write.go"
      provides: "documented/alerted self-extension behavior and no dormant ask_user pause contradiction"
      contains: "pending_approval"
    - path: "docs/audit/22-finding-ledger.md"
      provides: "AG-001..AG-064 disposition ledger"
      contains: "AG-001"
    - path: "docs/audit/22-LIVE-SIGNOFF-2026-06-15.md"
      provides: "full live sign-off evidence"
      contains: "metrics"
---

<objective>
Close Phase 22: reconcile skill self-extension behavior (AG-011/044/051), confirm/reroute ambiguous findings, produce the finding-coverage ledger, run the full automated and live validation bars, and record phase evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bug-fix/22-SPEC.md
@.planning/phases/22-bug-fix/22-CONTEXT.md
@.planning/phases/22-bug-fix/22-RESEARCH.md
@docs/audit/bug-report.md
@docs/audit/action-plan.md
@docs/audit/audit-index.json
@docs/audit/testing-strategy.md
@docs/audit/19-LIVE-SIGNOFF-2026-06-10.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skill self-extension reconcile for AG-011, AG-044, AG-051</name>
  <files>internal/agent/tools/skill.go, internal/agent/tools/skill_write.go, skill tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-011, AG-044, AG-051 and `proposed-patches.md` PP-7.
    - `internal/agent/tools/skill.go` duplicate schema consts.
    - `internal/agent/tools/skill_write.go` activation and dormant pause-sentinel path.
    - `internal/agent/tools/skill_test.go`, `skill_write_test.go`, and `ask_user` pause invariant tests.
    - `22-CONTEXT.md` multi-tenant deferral: do not implement full AG-011 multi-tenant gating here; land dead-schema/honest-docs/alert slice.
  </read_first>
  <action>
    Delete the unused duplicate `skillParamsSchema` and keep exactly one live schema. The schema text and comments must honestly describe the actual single-operator behavior.

    If self-authored skills remain immediately active by policy, emit an operator-visible alert/audit record and document the trust boundary honestly. If local config supports pending approval without multi-tenant scope creep, keep the minimal pending/approval path and test it. Do not falsely claim "you cannot approve your own changes" unless code enforces it.

    Reconcile the dormant `ask_user` pause sentinel path so `TestAskUserOnlyPauseConstraint` remains true. The skill write tool must not introduce a second pause source unless the invariant is intentionally updated with tests and rationale.

    Run `deadcode` or the nearest available dead-code check to confirm the duplicate schema and AG-028 candidate status as part of the ledger.
  </action>
  <verify>
    <automated>go test ./internal/agent/tools -run 'TestSkill|TestSkillWrite|TestAskUserOnlyPauseConstraint' -count=1</automated>
    <automated>go test ./internal/agent/tools -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one skill schema is referenced.
    - Documented activation behavior matches code and has a test.
    - Operator alert/audit or pending behavior is present for self-authored mutation, matching the documented policy.
    - `TestAskUserOnlyPauseConstraint` remains green.
    - AG-011/044/051 ledger entries are fixed+test or accepted+rationale as scoped.
  </acceptance_criteria>
  <done>Skill self-extension is honest, tested, and no longer carries dead schema or pause-invariant drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm/reroute NEEDS-CONFIRMATION and accepted findings</name>
  <files>internal/agent/event.go, docs/audit/22-finding-ledger.md, relevant tests</files>
  <read_first>
    - `docs/audit/bug-report.md` NEEDS CONFIRMATION findings: AG-028, AG-034, AG-041, AG-043.
    - `22-CONTEXT.md` D-09 cross-package boundary.
    - `internal/agent/event.go` and event tests for AG-034 agent-side shape/capping.
    - `internal/agent/mcptools/mount.go` for AG-028 dead code.
    - `cmd/aura/agent.go` and Plan 22-03 summary for AG-041.
    - `internal/agent/workflow/parallel.go` and Plan 22-04 summary for AG-043.
  </read_first>
  <action>
    Confirm AG-028 with dead-code tooling or equivalent symbol search. Remove if dead; if reachable, document the path and test/ledger why no fix is needed.

    Confirm AG-034 boundary. If an `internal/agent/event.go` shape change is needed to redact/cap raw `ToolInvocation` arguments/result/meta before persistence, land it here with tests. If redaction is purely in the DB/projection layer, route that part out with a clear ledger rationale.

    Confirm AG-041 is closed by Plan 22-03 active deadline wiring; if not, finish the wiring here.

    Confirm AG-043 with the Plan 22-04 `goleak` stress test; if it reveals a leak, fix it here before ledger close.

    For accepted/no-fix or explicitly tracked findings such as AG-021, AG-042, AG-048, AG-049, AG-053, AG-055, AG-059, AG-060, AG-063, AG-064 when appropriate, record precise rationale and whether a future phase owns them.
  </action>
  <verify>
    <automated>go test ./internal/agent -run 'TestEvent|Test.*ToolInvocation|Test.*Budget|Test.*Deadline' -count=1</automated>
    <automated>go test -race ./internal/agent/workflow ./internal/agent/mcptools -run 'Test.*Parallel|Test.*Dead|Test.*Mount|Test.*Reconnect' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - AG-028, AG-034, AG-041, AG-043 are explicitly confirmed and fixed or routed.
    - Accepted findings have rationale tied to the single-operator model or explicit future scope.
    - No NEEDS-CONFIRMATION item remains ambiguous in the ledger.
  </acceptance_criteria>
  <done>Ambiguous findings are confirmed and no item is silently dropped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Finding ledger and docs/audit close-out</name>
  <files>docs/audit/22-finding-ledger.md, docs/audit/README.md, docs/audit/audit-index.json</files>
  <read_first>
    - `docs/audit/bug-report.md` complete AG-001..AG-064 list.
    - All `22-*-SUMMARY.md` files produced by Plans 22-01 through 22-04.
    - `docs/audit/audit-index.json` and `risk-register.md`.
    - `docs/audit/19-LIVE-SIGNOFF-2026-06-10.md` for evidence style.
  </read_first>
  <action>
    Create `docs/audit/22-finding-ledger.md` with one row per in-scope AG-###. Columns must include ID, severity, disposition, code/test evidence, commit(s), and notes. Disposition values are constrained to `fixed+test`, `accepted+rationale`, or `confirmed+routed`.

    Update audit index/readme links if the project convention expects new ledger/live sign-off docs to be discoverable.

    Ensure every HARDEN-01..12 requirement maps to at least one fixed/test row or accepted/routed row with explicit rationale.
  </action>
  <verify>
    <automated>rg -n 'AG-0(0[1-9]|[1-5][0-9]|6[0-4])' docs/audit/22-finding-ledger.md</automated>
    <automated>rg -n 'fixed\\+test|accepted\\+rationale|confirmed\\+routed' docs/audit/22-finding-ledger.md</automated>
  </verify>
  <acceptance_criteria>
    - Ledger includes every AG-001 through AG-064 exactly once or with intentional grouped rows that still name every ID.
    - Every row has evidence or rationale; no blank disposition.
    - Deferrals from `22-CONTEXT.md` are named as routed/accepted with scope rationale.
  </acceptance_criteria>
  <done>Finding coverage ledger proves nothing was silently dropped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Full CI, coverage, mutation spot-check, and live sign-off</name>
  <files>docs/audit/22-LIVE-SIGNOFF-2026-06-15.md, .planning/phases/22-bug-fix/22-VALIDATION.md, .planning/phases/22-bug-fix/22-UAT.md</files>
  <read_first>
    - `22-CONTEXT.md` D-01 through D-04a and D-13.
    - `docs/audit/testing-strategy.md` sections 4 and 5.
    - `docs/audit/19-LIVE-SIGNOFF-2026-06-10.md`.
    - Project scripts: `scripts/coverage_gate.sh`, `cache_invariant_audit.sh`, Makefile targets if present.
  </read_first>
  <action>
    Run the automated phase-close bar:
    - `go test ./...`
    - `go test -race ./internal/agent/... ./internal/swarm/...`
    - `go vet ./...`
    - `golangci-lint run ./...`
    - `govulncheck ./...`
    - `bash scripts/cache_invariant_audit.sh`
    - `make coverage` or `bash scripts/coverage_gate.sh`, proving >=85% owned-surface.

    Run mutation spot-checks on `internal/agent/llm_agent_parallel.go`, `internal/agent/budget_dedup.go`, and `internal/agent/mcptools/bridge_reconnect.go`; record >=70% killed or document any tooling blocker with raw output and rerun path.

    Run the full live sign-off once: `aura serve` with PG, Neo4j, MCP, embed sidecar, and SearXNG bridge; host `aura chat` tool trace; `/metrics` scrape; CDP Telegram round-trip; full tool surface including GLM OCR multimodal; empirical fs cap and MCP timeout evidence.

    Write `docs/audit/22-LIVE-SIGNOFF-2026-06-15.md` and phase validation/UAT docs with exact commands, timestamps, environment, pass/fail output, and links to ledger rows.
  </action>
  <verify>
    <automated>go test ./...</automated>
    <automated>go test -race ./internal/agent/... ./internal/swarm/...</automated>
    <automated>go vet ./...</automated>
    <automated>golangci-lint run ./...</automated>
    <automated>govulncheck ./...</automated>
    <automated>bash scripts/cache_invariant_audit.sh</automated>
    <automated>make coverage</automated>
  </verify>
  <acceptance_criteria>
    - All automated commands are green or any blocker is recorded as a real blocker, not ignored.
    - Coverage is >=85% owned-surface.
    - Mutation spot-check is >=70% killed on all three critical files or has a documented tooling blocker.
    - Live sign-off evidence proves the hardened perimeter under real stack use.
    - `/metrics` scrape shows the new turn/LLM/hook/tool/panic metrics.
  </acceptance_criteria>
  <done>Phase 22 has automated, mutation, and live evidence sufficient for Gate-3 close.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-22-16 | Tampering | skill self-extension | mitigate/accept | honest schema, alert or pending policy, invariant tests, ledgered single-operator trust boundary |
| T-22-17 | Repudiation | audit findings | mitigate | one-row-per-finding ledger with evidence/rationale |
| T-22-18 | Quality escape | phase close | mitigate | full automated CI, coverage, mutation, and live sign-off evidence |
</threat_model>

<verification>
- `go test ./internal/agent/tools -run 'TestSkill|TestSkillWrite|TestAskUserOnlyPauseConstraint' -count=1`
- `go test ./internal/agent/... ./internal/swarm/... -count=1`
- `go test -race ./internal/agent/... ./internal/swarm/... -count=1`
- `go test ./...`
- `go vet ./...`
- `golangci-lint run ./...`
- `govulncheck ./...`
- `bash scripts/cache_invariant_audit.sh`
- `make coverage` or `bash scripts/coverage_gate.sh`
</verification>

<success_criteria>
- HARDEN-11: skill docs/schema/behavior match and dead schema is removed.
- HARDEN-12: every AG-### has fixed+test, accepted+rationale, or confirmed+routed status in `docs/audit/22-finding-ledger.md`.
- HARDEN-01..10: prior wave summaries and tests are represented in ledger and live sign-off.
- Full CI, coverage >=85%, cache invariant, mutation spot-check, and live sign-off evidence are complete.
</success_criteria>

<output>
Create `.planning/phases/22-bug-fix/22-05-SUMMARY.md`, `docs/audit/22-finding-ledger.md`, `docs/audit/22-LIVE-SIGNOFF-2026-06-15.md`, and validation/UAT docs when done.
</output>
