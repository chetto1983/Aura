---
phase: 28-governance-boards-web-onboarding
plan: 05
type: execute
wave: 3
depends_on: ["28-01", "28-02", "28-04"]
files_modified:
  - internal/agui/onboarding_api.go
  - internal/agui/onboarding_session.go
  - internal/agui/onboarding_provision.go
  - internal/agui/onboarding_qr.go
  - internal/agui/onboarding_api_test.go
  - internal/agui/onboarding_provision_test.go
  - internal/agui/onboarding_session_test.go
  - cmd/aura/serve_webui.go
  - cmd/aura/serve.go
  - internal/agui/server.go
autonomous: true
requirements: [ONBD-01, ONBD-02]
must_haves:
  truths:
    - "POST /api/onboarding/start (behind RequireCapability(identity.create)) creates a server-held onboarding.Session keyed by an opaque sessionToken and returns the D-06 capability picker options = the creator's own grants minus '*' (ONBD-01a)"
    - "POST /api/onboarding/{sessionToken}/step applies an onboarding.Intent (answer/confirm/edit/skip) to the server-held session and returns {content, step, status, draft?, preferences?} (ONBD-02, D-03)"
    - "Exactly one LLM extraction runs per inbound free-text answer (LLMAnswerExtractor.Extract once); replaying the same step does NOT emit a second LLM turn; edit re-renders the draft from the SAME extracted facts via ExtractDraft (no re-prompt) (ONBD-02, R4 adjacency)"
    - "An empty or skipped step is recorded as empty/omitted without error; skip ends without writing a profile (ONBD-02, R4 empty)"
    - "Completing the wizard writes an Agent.md whose 8 sections match the LoopAgent output, stored at ~/.aura/agents/<id>/ via profile.render (ONBD-02)"
    - "POST /api/onboarding/{sessionToken}/provision (behind RequireCapability(identity.create)) runs the ordered cross-store saga atomically: Leg B Authula user-create (PasswordService.Hash + UserService.Create + AccountService.Create) then Leg A aura pgx tx (identities + capability_grants + LinkOperator) then Leg C telegram InsertPending then one immutable identity_audit row on success (ONBD-01a/01b, D-07b)"
    - "The server re-validates no-escalation before the saga write: the requested capabilities are a subset of the creator's grants AND contain no '*'; an operator without identity.create is rejected 403 with no row written; GrantCapability rejects '*' at the store as a backstop (ONBD-01a, no-escalation)"
    - "Each of the 6 RESEARCH failure-injection points leaves NO orphan: B1 fail (no aura rows/token), B2 fail (compensate Authula user), A fail (compensate Authula user), C fail (delete identity + Authula user), abandoned-before-confirm (saga never ran so 0 rows), double-submit (UNIQUE name to a clean 409, not a 2nd identity) (ONBD-01a, R5 concurrency/idempotency)"
    - "Exactly one immutable audit row is written per successful create and none for a rolled-back flow (RESEARCH L8: a final db.WithTx after Leg C succeeds) (ONBD-01a)"
    - "The provision response returns the Telegram deep-link t.me/<bot>?start=<token> plus a server-rendered scannable QR SVG; the bot token is NEVER in the QR or any response (only the onboarding-token-bearing deep-link URL is) (ONBD-01b, D-08)"
    - "GET /api/onboarding/{sessionToken}/telegram-status returns {linked:bool} via a REST poll over PendingConsumed (not SSE); a replayed/expired token consume is rejected by the existing ConsumeOnboarding single-use chokepoint (ONBD-01b, R6)"
    - "The Authula password is write-only — hashed immediately via PasswordService.Hash, never persisted raw, never echoed in a response, never logged; provision failures log a fixed message (never err.Error() verbatim) (cross-cutting no-leak)"
    - "A log scan over a full provisioning run contains no secret value (Authula password, Telegram bot token, MCP env) (cross-cutting)"
    - "The onboarding session store is goroutine-free with a 15-minute idle TTL swept lazily on access (no background goroutine; goleak-clean)"
  artifacts:
    - path: "internal/agui/onboarding_provision.go"
      provides: "The ordered cross-store provisioning saga + per-leg compensation"
      contains: "func"
    - path: "internal/agui/onboarding_session.go"
      provides: "Server-held TTL session store wrapping onboarding.Session"
      contains: "sessionToken"
    - path: "internal/agui/onboarding_api.go"
      provides: "start/step/provision/telegram-status handlers + registerOnboardingRoutes"
      contains: "registerOnboardingRoutes"
    - path: "internal/agui/onboarding_qr.go"
      provides: "Server-side QR SVG render via the vendored rsc.io/qr"
      contains: "qr"
  key_links:
    - from: "internal/agui/onboarding_provision.go"
      to: "webauth Authula CoreServices (UserService.Create/AccountService.Create/PasswordService.Hash/UserService.Delete)"
      via: "Leg B + compensation"
      pattern: "UserService"
    - from: "internal/agui/onboarding_provision.go"
      to: "identity.Store (db.WithTx identities + GrantCapability + LinkOperator + InsertIdentityAuditTx) and DeleteIdentity"
      via: "Leg A + compensation"
      pattern: "WithTx|DeleteIdentity"
    - from: "internal/agui/onboarding_provision.go"
      to: "telegram.Store.InsertPending"
      via: "Leg C mint for the NEW identity"
      pattern: "InsertPending"
    - from: "internal/agui/onboarding_api.go"
      to: "onboarding.LLMAnswerExtractor.Extract + Session.Apply"
      via: "one extraction per free-text answer, then Apply"
      pattern: "Extract"
  prohibitions:
    - statement: "MUST NOT render or return any raw secret (Authula password, Telegram bot token) in any wizard/API response or write it to logs"
      status: resolved
      verification: test
    - statement: "MUST NOT allow identity creation to escalate privilege — no '*' grant, and no capability the creating operator lacks"
      status: resolved
      verification: test
    - statement: "MUST NOT create a loginable identity without writing an immutable audit row"
      status: resolved
      verification: test
---

<objective>
The onboarding wizard + provisioning backend: a server-held TTL session store driving the existing 5-step LoopAgent over REST (one LLM extraction per step), and the ordered cross-store provisioning saga (Authula then aura tx then Telegram token then immutable audit) with per-leg compensation, no-escalation re-validation, server-side QR render, and a Telegram-link poll. The high-risk track.

Purpose: ONBD-02 drives the SAME onboarding.Session over REST without duplicate LLM turns; ONBD-01a/01b fully provision a loginable identity atomically. Authula's separate pool makes provisioning a saga, not a single tx — RESEARCH §Hard Problem 1 gives the exact ordering + the 6 failure-injection points.
Output: internal/agui/onboarding_{api,session,provision,qr}.go + the serve_webui.go mounts (start/provision behind RequireCapability(identity.create), step/telegram-status behind RequireAuth) + the serve.go onboarding-service wiring + the registerOnboardingRoutes hook in Mux().
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-governance-boards-web-onboarding/28-RESEARCH.md
@.planning/phases/28-governance-boards-web-onboarding/28-PATTERNS.md
@.planning/phases/28-governance-boards-web-onboarding/28-04-SUMMARY.md
@.planning/phases/28-governance-boards-web-onboarding/28-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols created by this plan (excluded from drift verification):
- REST endpoints: `POST /api/onboarding/start`, `POST /api/onboarding/{sessionToken}/step`, `POST /api/onboarding/{sessionToken}/provision`, `GET /api/onboarding/{sessionToken}/telegram-status`
- `internal/agui/onboarding_api.go`: `registerOnboardingRoutes` + handlers `handleOnboardingStart`/`handleOnboardingStep`/`handleOnboardingProvision`/`handleTelegramStatus`
- `internal/agui/onboarding_session.go`: the TTL session store (`sessionStore`, `sessionEntry`, opaque `sessionToken` minting, lazy sweep)
- `internal/agui/onboarding_provision.go`: the saga orchestrator + per-leg compensation; the consumer-side narrow onboarding-service interface
- `internal/agui/onboarding_qr.go`: `renderQRSVG(deepLink string) (string, error)` via vendored `rsc.io/qr`
- The capability name `identity.create` (the create-mutation gate; documented in Plan 04, mounted here)
- `cmd/aura/serve_webui.go`: the four onboarding route consts (two RequireCapability, two RequireAuth)
- `cmd/aura/serve.go`: the onboarding-service build block + `aguiServer.SetOnboardingService(...)`
(Consumed by Plan 06 frontend wizard.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-held TTL session store + the step/start handlers (one LLM extraction per step)</name>
  <files>internal/agui/onboarding_session.go, internal/agui/onboarding_api.go, internal/agui/server.go, internal/agui/onboarding_session_test.go, internal/agui/onboarding_api_test.go</files>
  <read_first>
    - internal/onboarding/session.go (Session fields session.go:112-124 incl. the unexported prompted latch; Session.Apply session.go:132-158; Intent consts session.go:15-28; Step/Status consts session.go:33-62; refreshDraft session.go:235-246,349-357 — edit re-renders deterministically; mergeAnswers no-op-on-empty session.go:295-322; ErrTerminal session.go:136)
    - internal/onboarding/extractor_llm.go (LLMAnswerExtractor.Extract extractor_llm.go:49-85 — one-shot, Temperature:0, MaxTokens:256, NEVER errors, raw-text fallback; call ONCE per inbound free-text answer)
    - internal/onboarding/extractor.go (ExtractDraft — the deterministic 8-section render over Answers, NOT a per-answer LLM round-trip)
    - internal/profile/render.go (the 8-section Agent.md render + ~/.aura/agents/<id>/ store — confirm via render_test.go the 8-section output)
    - internal/skills/loader.go (the goroutine-free 1s TTL cache loader.go:17,113-127 — mirror the no-background-goroutine, mutex-guarded, lazy-sweep discipline for the 15-min session TTL)
    - internal/agui/graph_api.go (the body-cap MaxBytesReader + decode→400 + validate→sanitized-400 + one-call shape graph_api.go:95-116) + internal/agui/server.go (handleRun body-cap maxRunBodyBytes server.go:30; principalIdentityID/principalFrom server.go:226-230, auth.go:285-289; the SetOnboardingService field/setter from Plan 01; the Mux() register block server.go:148-158 — add s.registerOnboardingRoutes(mux))
    - internal/agui/conversations_api.go (writeJSON/writeJSONStatus conversations_api.go:64-74) + internal/agui/server_redact.go (sanitizeErr server_redact.go:41-46)
    - .planning/phases/28-governance-boards-web-onboarding/28-RESEARCH.md §Hard Problem 4 (the per-step JSON contract + the no-duplicate-LLM mechanism + the session TTL + the saga-on-final-confirm sequencing recommendation A6)
  </read_first>
  <behavior>
    - start: creates a session keyed by an opaque sessionToken; returns the first step content + status + the capability picker options (creator grants minus '*'). Behind RequireCapability(identity.create) — an operator without the cap is 403 and no session is created.
    - step(answer with free text): calls LLMAnswerExtractor.Extract ONCE, merges into the session, Apply(IntentAnswer) advances the step; returns {content, step, status, draft?, preferences?}.
    - step(replay same step): the step pointer has advanced so no second LLM extraction for that step; a terminal session returns a clean terminal status (ErrTerminal handled, not a 500).
    - step(edit): refreshDraft then ExtractDraft re-renders from the SAME Answers (no per-answer LLM call, no re-prompt).
    - step(skip): StatusSkipped, no profile written; an empty answer is recorded empty without error.
    - The session store: 15-min idle TTL refreshed per step; expired entries swept lazily on access; no background goroutine (goleak-clean).
  </behavior>
  <action>
    Create internal/agui/onboarding_session.go: a sessionStore = mutex-guarded map[string]*sessionEntry{session *onboarding.Session, identityID, creatorIdentityID, email, capabilities, linkTelegram, expiresAt time.Time} with an opaque sessionToken (crypto/rand), a 15-min idle TTL refreshed on each access, and a lazy sweep on access (mirror skills/loader.go's goroutine-free TTL — NO background goroutine). Provide get/put/refresh/sweep helpers. The session accumulates the interview Answers AND the provisioning inputs (email / password-hash-or-deferred / capabilities / telegram-intent) so the saga can run at the final confirm (RESEARCH A6: run the full saga at confirm so an abandoned wizard leaves ZERO rows).
    Create internal/agui/onboarding_api.go (start + step handlers in this task; provision + telegram-status in Task 2). Each POST follows graph_api.go:95-116: MaxBytesReader(maxRunBodyBytes) then decode→400 then validate (intent in {answer,confirm,edit,skip}; field lengths) then sanitized-400, then ONE service call. handleOnboardingStart reads principalIdentityID (server.go:226-230) as the creator, calls identity.Store.ListCapabilities(creator) and filters out '*' for the picker options, mints the session, returns {sessionToken, step, content, capabilityOptions}. handleOnboardingStep loads the session by token (404/expired→sanitized error), and on an answer carrying free text calls LLMAnswerExtractor.Extract ONCE then session.Apply; on edit/skip/confirm calls Apply directly; projects {content, step, status, draft?, preferences?} via writeJSON. Add the SetOnboardingService field consumption + wire s.registerOnboardingRoutes(mux) into Mux() (server.go:148-158); the narrow onboarding-service interface is declared consumer-side (only the methods the handlers call).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/agui/ -run TestOnboarding && go test -race ./internal/agui/ -run TestSessionTTL</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agui/onboarding_session.go` has a goroutine-free, mutex-guarded, lazy-sweep TTL store (a -race + goleak test passes)
    - `go test ./internal/agui/ -run TestNoDuplicatePrompt` proves exactly one LLM extraction per free-text answer, no second turn on replay, and edit re-renders without a re-prompt (mock the LLM client, count calls)
    - `internal/agui/onboarding_api.go` registers POST /api/onboarding/start + POST /api/onboarding/{sessionToken}/step via registerOnboardingRoutes wired into Mux()
    - handleOnboardingStart returns capabilityOptions = creator grants with '*' excluded (a test with a creator holding '*' + two named caps returns only the two named caps)
    - A skip step writes no profile; an empty answer is recorded without error
  </acceptance_criteria>
  <done>The TTL session store + start/step handlers drive the LoopAgent over REST with the verified one-extraction-per-step guarantee; the no-duplicate-prompt + TTL behaviors are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The cross-store provisioning saga + compensation + QR + telegram-status + mounts</name>
  <files>internal/agui/onboarding_provision.go, internal/agui/onboarding_qr.go, internal/agui/onboarding_api.go, cmd/aura/serve_webui.go, cmd/aura/serve.go, internal/agui/onboarding_provision_test.go</files>
  <read_first>
    - .planning/phases/28-governance-boards-web-onboarding/28-RESEARCH.md §Hard Problem 1 (the EXACT ordered saga pseudo-code + the 6 failure-injection points + the L8 audit-row-on-success recommendation + the compensation correctness notes)
    - internal/webauth/authula.go (CoreServices() authula.go:201-203 — the gateway) + the verified create sequence from RESEARCH §Authula: PasswordService.Hash then UserService.Create(ctx, name, email, true, nil, nil) then AccountService.Create(ctx, user.ID, user.Email, "email", and-hash); UserService.Delete(ctx, id) for compensation
    - internal/webauth/authula_integration_test.go (lines 76-87 — the proven UserService.Create/Delete calls + GetByEmail dup pre-check shape)
    - internal/identity/store.go (New store.go:48; GrantCapability store.go:145-185 rejects '*'; HasCapability store.go:128-138; DeleteIdentity store.go:118 FK-cascade compensation; GetIdentityByName store.go:87 — aura.identities.name is NOT NULL UNIQUE, so a double-submit is a clean 23505) + internal/identity/audit_store.go (Wave-0 InsertIdentityAuditTx)
    - internal/db/tx.go (WithTx tx.go:22 — Leg A is one db.WithTx; the audit row is a tiny final db.WithTx after Leg C per L8)
    - internal/webauth/identity_link.go (LinkOperator identity_link.go:60-72 — binds the new Authula user to the new identity in identity_auth_links)
    - internal/channels/telegram/store.go (InsertPending store.go:84-99 with IdentityID = the NEW identity UUID + ExpiresAt now+1h; PendingConsumed store.go:155-164 for the poll; ConsumeOnboarding store.go:101-141 single-use chokepoint — the consume happens Telegram-side, the web wizard only mints + polls)
    - internal/setup/handlers.go (deepLink handlers.go:183-185 → t.me/%s?start=%s — reuse the exact format; the handleToken handlers.go:44 no-secret-logging precedent) + internal/setup/qr.go (qrSVG qr.go:10-13 is a STUB returning "" — replace with rsc.io/qr)
    - go.mod (confirm rsc.io/qr v0.2.0 is vendored indirect — render server-side; add NO new dependency)
    - cmd/aura/serve_webui.go (the POST /agent/run RequireCapability mount serve_webui.go:213 with agentRunCapability serve_webui.go:99 — the start+provision mounts mirror this with "identity.create"; the read-GET mount serve_webui.go:266-267 for telegram-status + step; the whole-mux RequireAuth serve_webui.go:289) + cmd/aura/serve.go (the SetXxx composition block serve.go:285-312; authulaProvider serve.go:325 buildAuthDeps exposes CoreServices(); chat.pool the pgxpool; the telegram.Store + identity.Store construction sites)
    - internal/agui/auth.go (RequireCapability auth.go:259-273)
  </read_first>
  <behavior>
    - provision(valid): pre-validate (creator HasCapability(identity.create); capabilities subset of creator-grants; no '*'; email non-empty + Authula GetByEmail==none) then Leg B Authula create then Leg A aura tx (identities + GrantCapability per cap + LinkOperator) then Leg C telegram InsertPending(new identity, +1h) then a final tiny db.WithTx writes ONE identity_audit row, returns {identityID, deepLink, qrSvg}. Password hashed immediately, never echoed/logged.
    - provision(no cap): 403, no row written (RequireCapability + a server re-check).
    - provision(escalation: '*' or a creator-lacked cap): 400, no row written.
    - provision failure-injection: B1 fail to 0 aura rows/0 token; B2 fail to COMP_B (delete Authula user); A fail to COMP_B; C fail to DeleteIdentity + COMP_B; double-submit to UNIQUE name 409 (not a 2nd identity). Each leaves NO orphan of anything.
    - telegram-status: {linked:bool} via PendingConsumed (REST poll, not SSE). A replayed/expired consume is rejected by ConsumeOnboarding (already enforced).
    - QR: renderQRSVG(deepLink) returns a scannable SVG; the bot token is never in the QR (only the t.me/<bot>?start=<onboarding-token> URL).
  </behavior>
  <action>
    Create internal/agui/onboarding_provision.go implementing RESEARCH §Hard Problem 1's ordered saga as a service method (declare the consumer-side narrow onboarding-service interface it satisfies). Order: pre-validate (no writes) then Leg B Authula (PasswordService.Hash then UserService.Create then AccountService.Create against authulaProvider.CoreServices(), with COMP_B = UserService.Delete) then Leg A db.WithTx (INSERT identities; GrantCapability each cap — store rejects '*'; LinkOperator(new identity, authula user id)) with COMP_B on failure then Leg C telegram.InsertPending(OnboardingToken: uuid, IdentityID: new id, ExpiresAt: now+1h) with COMP = DeleteIdentity + COMP_B then a tiny final db.WithTx writing exactly one identity_audit row via InsertIdentityAuditTx (L8: only on success). The server re-validates no-escalation BEFORE Leg B (capabilities subset of ListCapabilities(creator) AND no '*'). Hash the password immediately; never persist/echo/log it; on any failure log a FIXED message (never err.Error() verbatim — handlers.go:44 precedent). Build the deepLink via the setup format (handlers.go:183-185).
    Create internal/agui/onboarding_qr.go: func renderQRSVG(deepLink string) (string, error) using the vendored rsc.io/qr (qr.Encode then matrix then emit SVG rects, ~30 LOC). Put ONLY the deepLink URL in the QR — never the bot token. (Optionally replace the internal/setup/qr.go stub with this shared helper.)
    Add handleOnboardingProvision + handleTelegramStatus to internal/agui/onboarding_api.go (body-cap + decode + validate, then the saga / a PendingConsumed poll).
    In serve_webui.go: add the four onboarding route consts; mount POST /api/onboarding/start and POST /api/onboarding/{sessionToken}/provision as agui.RequireCapability(aguiHandler, auth, "identity.create") (mirror serve_webui.go:213); mount POST /api/onboarding/{sessionToken}/step and GET /api/onboarding/{sessionToken}/telegram-status as read-GET-style siblings inheriting the whole-mux RequireAuth (serve_webui.go:266-267,289).
    In serve.go: build the onboarding service after agui.NewServer wiring chat.pool (identity.Store + audit store), authulaProvider.CoreServices(), telegram.Store, the onboarding LLMAnswerExtractor + profile renderer; call aguiServer.SetOnboardingService(...). Best-effort: a missing backend leaves the routes at 503, never aborts boot.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -tags db_integration ./internal/agui/ -run TestProvision && go test -tags db_integration ./internal/agui/ -run TestTelegramStatus</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags db_integration ./internal/agui/ -run TestProvisionSaga` injects each of B1/B2/A/C failures and asserts 0 orphan identities/grants/links/Authula-users/Telegram-tokens for each
    - `go test ./internal/agui/ -run TestNoEscalation` asserts a '*' request and a creator-lacked-cap request are both 400 with no row; an operator without identity.create is 403 with no row
    - `go test -tags db_integration ./internal/agui/ -run TestIdentityAuditImmutable` asserts exactly one immutable audit row on success and none after a rolled-back flow
    - `go test -tags db_integration ./internal/agui/ -run TestProvisionIdempotent` asserts a double-submit yields one identity + a clean 409 (UNIQUE name)
    - `go test -tags db_integration ./internal/agui/ -run TestProvisionNoSecretInLogs` captures slog over a full run and asserts no Authula password / Telegram bot token / MCP env value appears
    - `internal/agui/onboarding_qr.go` renderQRSVG returns a non-empty SVG containing the deep-link but NOT the bot token; serve_webui.go mounts start+provision behind RequireCapability("identity.create")
  </acceptance_criteria>
  <done>The provisioning saga + compensation + the immutable audit row + QR + telegram-status + the gated mounts exist; all 6 failure-injection points and the no-leak/no-escalation/idempotency ACs are integration-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /api/onboarding/* | The create mutation crosses an authz boundary; capabilities/email/password are untrusted input |
| aura pool ↔ Authula pool ↔ Telegram token | Three independent stores; no shared tx — atomicity is a saga with compensation |
| Password/bot token ↔ response + logs | Secrets must never be echoed or logged |
| Capability picker input ↔ grant write | A requested grant must be re-validated server-side (no client trust) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Elevation of privilege | handleOnboardingProvision | mitigate | RequireCapability(identity.create) gate + a server re-validation that requested caps are a subset of ListCapabilities(creator) AND contain no '*'; GrantCapability rejects '*' at the store (three-way defense); no-escalation unit test |
| T-28-05-02 | Tampering / Repudiation | the cross-store saga | mitigate | Ordered saga (Authula-first to fail cheap on dup email) + per-leg compensation (UserService.Delete + DeleteIdentity FK-cascade); the 6 failure-injection integration tests assert zero orphans; exactly one immutable identity_audit row on success |
| T-28-05-03 | Information disclosure | password handling | mitigate | Password hashed immediately via PasswordService.Hash, never persisted raw, never echoed, write-only field; provision failures log a FIXED message (never err.Error() verbatim — handlers.go:44 precedent); the no-secret-in-logs integration test |
| T-28-05-04 | Information disclosure | QR / deep-link | mitigate | The bot token is never in the QR or any response — only the t.me/<bot>?start=<onboarding-token> deep-link URL is rendered (server-side rsc.io/qr) |
| T-28-05-05 | Spoofing (token replay) | telegram-status / consume | mitigate | The web wizard only mints + polls (PendingConsumed); the single-use ConsumeOnboarding chokepoint (ErrTokenConsumed) rejects a replayed/expired consume — already enforced |
| T-28-05-06 | Repudiation | identity creation | mitigate | One immutable aura.identity_audit row per successful create (Wave-0 append-only table); no create path bypasses it (MUST-NOT prohibition) |
| T-28-05-07 | Denial of service (resource) | session store | mitigate | Goroutine-free 15-min idle TTL swept lazily on access (no background goroutine, goleak-clean); a -race test covers concurrent access |
| T-28-05-SC | Tampering | dependency surface | accept | QR reuses the already-vendored rsc.io/qr v0.2.0 (RESEARCH §Environment); zero net-new deps — the executor confirms no new require lands |
</threat_model>

<verification>
- `go build ./...` + `go vet ./...` clean.
- `go test -tags db_integration ./internal/agui/ ./internal/identity/ ./internal/webauth/ ./internal/channels/telegram/` green (stack up; composed DSNs).
- `go test -race ./internal/agui/ -run TestProvision` + `-run TestSessionTTL` goleak-clean.
- The no-secret-in-logs test (slog capture over a full provision) passes.
- A daemon boot smoke (onboarding service unavailable → routes 503, not fatal) passes.
</verification>

<success_criteria>
- The wizard drives the 5-step LoopAgent over REST with exactly one LLM extraction per step.
- Provisioning is atomic on confirm with per-leg compensation; all 6 failure-injection points leave no orphan.
- No-escalation is enforced three ways; exactly one immutable audit row per success.
- The Telegram deep-link + server-rendered QR work; the bot token + password never leak to a response or log.
</success_criteria>

<output>
Create `.planning/phases/28-governance-boards-web-onboarding/28-05-SUMMARY.md` when done.
</output>
