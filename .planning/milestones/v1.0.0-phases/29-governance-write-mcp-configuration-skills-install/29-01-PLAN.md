---
phase: 29-governance-write-mcp-configuration-skills-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - internal/db/migrations/0022_mcp_audit.up.sql
  - internal/db/migrations/0022_mcp_audit.down.sql
  - internal/db/queries/mcp_audit.sql
  - internal/mcp/manager/audit.go
  - internal/mcp/manager/audit_test.go
  - internal/mcp/manager/audit_store_integration_test.go
  - cmd/aura/serve_webui.go
  - internal/agui/auth_test.go
autonomous: true
requirements: [MCPW-02, SKW-01]
must_haves:
  truths:
    - "The D-09 SPEC-amendment commit lands FIRST as its own commit before any skills-install code, reconciling ALL THREE locked artifacts atomically: (a) 29-SPEC.md lines naming `--ignore-scripts` (SKW-01 target, the 'Install is RISKY' constraint, the acceptance 'six/six' counts, prohibition #5, the canon-referral breadcrumb), (b) REQUIREMENTS.md SKW-01 row, and (c) ROADMAP.md Phase 29 goal prose + Success Criterion 4 — each is replaced with the container-isolation framing (five-item checklist: sanitized env, SKILL.md parse, body cap, injection-literal blocklist, sanitized name/path; install scripts permitted; control = approval gate + Writer validation); after the commit none of the three files presents `--ignore-scripts` as a checklist item or as safe"
    - "An UPDATE against aura.mcp_audit raises insufficient_privilege (append-only row trigger); a DELETE raises insufficient_privilege; a TRUNCATE raises insufficient_privilege (separate statement trigger)"
    - "aura_app holds SELECT + INSERT on aura.mcp_audit only (no UPDATE/DELETE/TRUNCATE grant); aura_migrate owns DDL"
    - "InsertMCPAuditTx appends exactly one row inside a caller-supplied db.WithTx transaction (tx-bound *sqlc.Queries), so it commits atomically with a surrounding write"
    - "MCPAuditStore.List returns rows newest-first (created_at DESC, id DESC stable tiebreak)"
    - "RequireCapability(next, deps, \"governance.write\") returns 403 when the principal lacks the capability and passes when granted (the seeded `local` identity holds `*`)"
    - "The governanceWriteCapability const equals the string \"governance.write\" (the value already asserted at internal/agui/auth_test.go:494)"
  artifacts:
    - path: "internal/db/migrations/0022_mcp_audit.up.sql"
      provides: "Append-only aura.mcp_audit ledger (role grant + row trigger + separate TRUNCATE statement trigger)"
      contains: "CREATE TRIGGER"
    - path: "internal/db/migrations/0022_mcp_audit.down.sql"
      provides: "Reverses 0022 (drop triggers + function + table)"
    - path: "internal/mcp/manager/audit.go"
      provides: "MCPAuditStore + InsertMCPAuditTx + MCPAuditInsert + List (newest-first)"
      exports: ["MCPAuditStore", "InsertMCPAuditTx", "MCPAuditInsert"]
    - path: "internal/db/queries/mcp_audit.sql"
      provides: "InsertMcpAudit + ListMcpAudit sqlc queries on the 0022 table"
  key_links:
    - from: "internal/mcp/manager/audit.go"
      to: "internal/db/tx.go (db.WithTx)"
      via: "InsertMCPAuditTx accepts the tx-bound *sqlc.Queries"
      pattern: "InsertMCPAuditTx\\(ctx"
    - from: "cmd/aura/serve_webui.go"
      to: "internal/agui/auth.go (RequireCapability)"
      via: "governanceWriteCapability const passed to RequireCapability at each new mutating mount (mounts land in plans 02/03)"
      pattern: "governanceWriteCapability"
  prohibitions:
    - statement: "MUST NOT present a skill install as 'safe' — after the amendment the cockpit always renders install as RISKY supply-chain input, gated by the approval queue and Writer validation, isolated by Aura's container boundary (install scripts are permitted; the control is NOT `--ignore-scripts`)"
      status: resolved
      verification: "judgment — 29-SPEC.md prohibition #5, REQUIREMENTS.md SKW-01, and ROADMAP.md Phase 29 (goal prose + Success Criterion 4) all read the container-isolation framing; no `--ignore-scripts`-as-safe wording remains in any of the three"
    - statement: "MUST NOT mutate MCP config without the means to append an immutable mcp_audit row — the ledger is append-only via role grant (SELECT+INSERT only) + dual UPDATE/DELETE/TRUNCATE triggers"
      status: resolved
      verification: "test — live-PG: UPDATE/DELETE/TRUNCATE against aura.mcp_audit each raise insufficient_privilege"
---

<objective>
Land the BLOCKING D-09 SPEC-amendment FIRST, then build the `mcp_audit` append-only
foundation and the `governance.write` capability that every Phase-29 write surface depends on.

This is Wave 1: pure backend + the docs amendment. No HTTP routes yet — the capability const +
mount wiring are introduced here so plans 02/03 only add route lines. Per CLAUDE.md PRD-first,
the SPEC/REQUIREMENTS/ROADMAP amendment is a SEPARATE commit that MUST precede any skills-install
implementation (plan 03); it is sequenced here as the very first task of the very first wave, and it
reconciles ALL THREE locked truth-source artifacts in one atomic docs commit (else /gsd-verify-work
and /gsd-complete-milestone read a permanent contradiction).

Purpose: establish the tamper-evident audit ledger (the one net-new MCP gap-filler #1), the
capability gate (gap-filler #4), and reconcile the LOCKED SPEC + REQUIREMENTS + ROADMAP with the
operator's D-06/D-07 container-isolation directive — before the write code that depends on all three.
Output: the amended `29-SPEC.md` + `REQUIREMENTS.md` + `ROADMAP.md` (one commit) +
`0022_mcp_audit.{up,down}.sql` + `internal/db/queries/mcp_audit.sql` + sqlc regen +
`internal/mcp/manager/audit.go` (`MCPAuditStore`/`InsertMCPAuditTx`) + `governanceWriteCapability` const.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-CONTEXT.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-09 BLOCKING amendment — replace `--ignore-scripts` framing with container-isolation across 29-SPEC.md + REQUIREMENTS.md + ROADMAP.md (SEPARATE FIRST COMMIT, atomic)</name>
  <files>.planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md (the LOCKED requirements — the `--ignore-scripts` occurrences are at lines 37/39/40 (SKW-01 target + acceptance), 87 (constraint), 147 (prohibition #5), 152 (canon-referral breadcrumb))
    - .planning/REQUIREMENTS.md (the SKW-01 row at ~line 102 — carries the six-item `--ignore-scripts` checklist)
    - .planning/ROADMAP.md (Phase 29: the goal prose at ~line 262 names `--ignore-scripts`; Success Criterion 4 at ~line 270 lists the six-item checklist with `--ignore-scripts` as item #1)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"D-09 SPEC-Amendment Specifics (BLOCKING — planner lands FIRST)" (the exact edits, each quoted)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-CONTEXT.md (D-06/D-07/D-09 — the operator directive + the amendment text + the canonical_refs note that REQUIREMENTS.md + ROADMAP.md carry the same wording)
  </read_first>
  <action>
    Make SURGICAL, targeted edits across the THREE locked truth-source artifacts (NEVER a blind full-file
    overwrite — use the Edit tool on each named string, or a targeted Bash sed on the specific lines). PRD-
    first (CLAUDE.md): all three MUST be reconciled in the SAME atomic commit so no downstream gate reads a
    contradiction (precedent: Phase-28 D-07).

    (A) `29-SPEC.md` — apply the six edits enumerated in 29-RESEARCH §"D-09 SPEC-Amendment Specifics":
    (1) SKW-01 "Target" sentence (~line 39): replace the six-item checklist `(--ignore-scripts, sanitized
        env, SKILL.md parse, body cap, injection-literal blocklist, sanitized name/path)` with the FIVE-item
        `(sanitized env, SKILL.md parse, body cap, injection-literal blocklist, sanitized name/path)`;
        replace "install is always rendered as RISKY supply-chain input — `--ignore-scripts` is never
        presented as safe" with "install isolation = Aura's container boundary; install scripts are
        permitted; install remains RISKY + gated (approval queue) + Writer-validated".
    (2) SKW-01 acceptance line (~line 40): replace "all six validation-checklist items" → "all five
        validation-checklist items".
    (3) Constraints "Install is RISKY supply-chain" line (~line 87): replace the parenthetical `(incl.
        --ignore-scripts)` with the container-isolation framing (runs inside Aura's container; scripts
        permitted; control = approval gate + Writer validation; install remains RISKY supply-chain input).
    (4) Prohibitions table row #5 (~line 147): replace with "MUST NOT present a skill install as 'safe' —
        the cockpit always renders install as RISKY supply-chain input, gated by the approval queue and
        Writer validation, isolated by Aura's container boundary (install scripts are permitted; the
        control is NOT `--ignore-scripts`)".
    (5) Any `## Acceptance Criteria` checklist item that says "the six validation-checklist items" → "the
        five".
    (6) Canon-referral breadcrumb (~line 152): replace `RISKY+`--ignore-scripts`+gated` → `RISKY +
        container-isolated + approval-gated + Writer-validated`.

    (B) `REQUIREMENTS.md` — the SKW-01 row (~line 102): replace the validation-checklist parenthetical
    `(--ignore-scripts, sanitized env, SKILL.md parse, body cap, injection-literal blocklist, sanitized
    name/path)` with the FIVE-item `(sanitized env, SKILL.md parse, body cap, injection-literal blocklist,
    sanitized name/path)`. Keep the rest of the SKW-01 sentence intact (source/hash/preview/risk-tier/
    destination/RISKY remain).

    (C) `ROADMAP.md` Phase 29 — TWO edits:
    (c1) The goal prose (~line 262): replace "never presents `--ignore-scripts` install as safe — install
         remains RISKY supply-chain input" with the container-isolation framing ("install isolation =
         Aura's container boundary; install scripts are permitted; install remains RISKY supply-chain input,
         gated by the approval queue and Writer validation"). Do NOT drop the RISKY-honest intent.
    (c2) Success Criterion 4 (~line 270): replace the six-item checklist `(--ignore-scripts, sanitized env,
         SKILL.md parse, body cap, injection-literal blocklist, sanitized name/path)` with the FIVE-item
         `(sanitized env, SKILL.md parse, body cap, injection-literal blocklist, sanitized name/path)`.

    Then COMMIT all three files as ONE atomic commit with subject
    `docs(29): amend SPEC/REQUIREMENTS/ROADMAP — container-isolation supersedes --ignore-scripts (D-09)`
    and a body explaining the operator directive D-06/D-07 and that PRD-first requires all three locked
    artifacts reconciled together. This commit MUST be made before any skills-install code in plan 03
    (precedent: Phase-28 D-07). Do NOT edit 29-UI-SPEC.md — RESEARCH confirms its §4 already renders the
    post-amendment five-item checklist + container-isolation note.
  </action>
  <acceptance_criteria>
    - In ALL THREE files (29-SPEC.md, REQUIREMENTS.md, ROADMAP.md Phase 29), `--ignore-scripts` no longer appears as a presented-safe checklist item: any residual `ignore-scripts` occurrence exists ONLY inside the negation wording ("the control is NOT `--ignore-scripts`" / "supersedes --ignore-scripts") — never a standalone checklist item, an "as safe" framing, or a Success-Criterion checklist entry
    - 29-SPEC.md SKW-01 target lists exactly FIVE checklist items (no `--ignore-scripts`); the acceptance + `## Acceptance Criteria` lines read "five", not "six"; prohibition #5 contains "always renders install as RISKY supply-chain input" and "isolated by Aura's container boundary"
    - REQUIREMENTS.md SKW-01 row lists exactly the FIVE checklist items (sanitized env, SKILL.md parse, body cap, injection-literal blocklist, sanitized name/path); no `--ignore-scripts`
    - ROADMAP.md Phase 29 goal prose carries the container-isolation framing (not "never presents `--ignore-scripts` install as safe"); Success Criterion 4 lists the FIVE checklist items, no `--ignore-scripts`
    - `git log --oneline -1` after this task shows subject `docs(29): amend SPEC/REQUIREMENTS/ROADMAP — container-isolation supersedes --ignore-scripts (D-09)` and the commit touches EXACTLY 29-SPEC.md + REQUIREMENTS.md + ROADMAP.md (the one atomic docs amendment)
  </acceptance_criteria>
  <verify>
    <automated>SPEC=.planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md; REQ=.planning/REQUIREMENTS.md; RM=.planning/ROADMAP.md; grep -q 'isolated by Aura.s container boundary' "$SPEC" && grep -q 'five validation-checklist items' "$SPEC" && grep -q 'injection-literal blocklist, sanitized name/path)' "$REQ" && ! grep -E -q '\(\s*`?--ignore-scripts`?,' "$REQ" && ! grep -E -q '\(\s*`?--ignore-scripts`?,' "$RM" && ! grep -q "presents .--ignore-scripts. install as safe" "$RM"</automated>
  </verify>
  <done>All three locked artifacts (29-SPEC.md + REQUIREMENTS.md + ROADMAP.md Phase 29) are reconciled to the five-item container-isolation framing in ONE atomic docs commit; `--ignore-scripts` no longer appears as a presented-safe checklist item or "as safe" wording in any of them; the commit precedes any plan-03 code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 0022_mcp_audit migration + sqlc queries (append-only ledger, mirror 0021 verbatim)</name>
  <files>internal/db/migrations/0022_mcp_audit.up.sql, internal/db/migrations/0022_mcp_audit.down.sql, internal/db/queries/mcp_audit.sql, internal/mcp/manager/audit_store_integration_test.go</files>
  <read_first>
    - internal/db/migrations/0021_identity_audit.up.sql (the EXACT template — role grant + reject function + BEFORE UPDATE OR DELETE row trigger + SEPARATE BEFORE TRUNCATE statement trigger + indexes)
    - internal/db/migrations/0021_identity_audit.down.sql (the down template)
    - internal/db/queries/skill_audit.sql (the sqlc query idiom: `-- name: InsertSkillAudit :one` + `-- name: ListSkillAudit :many` ordered created_at DESC)
    - internal/db/migrate.go (the `//go:embed migrations/*.sql` — new migration files are auto-picked-up; no registration code)
    - sqlc.yaml (the sqlc config — `make sqlc` regenerates internal/db/sqlc/ from queries/)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md §1 (the field-for-field adaptation for mcp_audit: id/created_at/actor_identity_id/action/server_name/reason)
  </read_first>
  <behavior>
    - Append-only invariant (live PG): INSERT one row succeeds; UPDATE → insufficient_privilege; DELETE → insufficient_privilege; TRUNCATE → insufficient_privilege
    - Schema: id uuid PK default gen_random_uuid(); created_at timestamptz NOT NULL default now(); actor_identity_id text NOT NULL; action text NOT NULL (one of install/edit/enable/disable/remove/trust); server_name text NOT NULL; reason text NULL (NULL except on trust)
    - down migration drops both triggers + the function + the table cleanly (re-applies up after down)
  </behavior>
  <action>
    Create `0022_mcp_audit.up.sql` mirroring `0021_identity_audit.up.sql` field-for-field (D-02: the new append-only MCP-mutation ledger): table
    `aura.mcp_audit` with columns `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `created_at
    timestamptz NOT NULL DEFAULT now()`, `actor_identity_id text NOT NULL` (D-03: the capability-layer
    principal `principalFrom`, NOT the raw Authula user id — same choice as 0021), `action text NOT NULL`,
    `server_name text NOT NULL`, `reason text` (nullable). Keep the flat schema (NO D-29 coherence CHECK
    — that matrix is skill-specific). Add two plain (non-CONCURRENT) indexes: `mcp_audit_server_idx
    (server_name)` + `mcp_audit_created_at_idx (created_at DESC)`. Rename the reject function →
    `aura.reject_mcp_audit_mutation()`, the triggers → `mcp_audit_no_update_delete` (BEFORE UPDATE OR
    DELETE FOR EACH ROW) and `mcp_audit_no_truncate` (SEPARATE BEFORE TRUNCATE FOR EACH STATEMENT —
    Pitfall 3: a row trigger never fires for TRUNCATE). Keep `GRANT SELECT, INSERT ON aura.mcp_audit TO
    aura_app` + `GRANT ALL ON aura.mcp_audit TO aura_migrate`. Create `0022_mcp_audit.down.sql` mirroring
    `0021`'s down (drop triggers → table → function). Add `internal/db/queries/mcp_audit.sql` with
    `-- name: InsertMcpAudit :one` (insert actor_identity_id/action/server_name/reason, returning the row)
    and `-- name: ListMcpAudit :many` (optional server_name + since filters, ORDER BY created_at DESC, id
    DESC, LIMIT $n) — mirror the skill_audit.sql idiom. Run `make sqlc` (sqlc v1.31.1) to regenerate
    `internal/db/sqlc/`. Write the live-PG integration test (build tag `db_integration`) asserting the
    append-only invariant + the down/up round-trip.
  </action>
  <acceptance_criteria>
    - `internal/db/migrations/0022_mcp_audit.up.sql` contains `CREATE TRIGGER mcp_audit_no_truncate` and `BEFORE TRUNCATE` and `FOR EACH STATEMENT`, plus `GRANT SELECT, INSERT ON aura.mcp_audit TO aura_app`
    - `internal/db/queries/mcp_audit.sql` contains `-- name: InsertMcpAudit :one` and `-- name: ListMcpAudit :many` with `ORDER BY` and `created_at DESC`
    - `make sqlc` regenerates without error and `internal/db/sqlc/` gains `InsertMcpAudit`/`ListMcpAudit` query methods
    - `go test -tags db_integration ./internal/mcp/manager/ -run TestMCPAuditAppendOnly` exits 0 (UPDATE/DELETE/TRUNCATE each raise insufficient_privilege against the live stack)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'BEFORE TRUNCATE' internal/db/migrations/0022_mcp_audit.up.sql && grep -q 'GRANT SELECT, INSERT ON aura.mcp_audit TO aura_app' internal/db/migrations/0022_mcp_audit.up.sql && grep -q 'name: ListMcpAudit' internal/db/queries/mcp_audit.sql</automated>
  </verify>
  <done>The 0022 migration creates an append-only aura.mcp_audit table mirroring 0021 (dual triggers, role grant); sqlc queries exist and regenerate; the live-PG append-only integration test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MCPAuditStore + InsertMCPAuditTx (tx-bound insert, newest-first list) + governance.write capability + mount wiring</name>
  <files>internal/mcp/manager/audit.go, internal/mcp/manager/audit_test.go, cmd/aura/serve_webui.go, internal/agui/auth_test.go</files>
  <read_first>
    - internal/skills/audit_store.go (the analog: AuditStore + AuditInsert + toParams + InsertAuditTx (tx-bound) + List (newest-first, limit clamp) — mirror the SHAPE, drop the D-29 NULL-mapping / classifyAuditErr which is skill-specific)
    - internal/db/tx.go (db.WithTx signature — InsertMCPAuditTx must accept the tx-bound *sqlc.Queries so it participates in the surrounding tx)
    - cmd/aura/serve_webui.go:95-104 (the agentRunCapability + governanceReadCapability const idiom) + :317-334 (the RequireCapability mount discipline — method+path-specific siblings under the /api/ carve-out)
    - internal/agui/auth.go:261 (RequireCapability — already built; the const is the only new code) + :296 (principalFrom)
    - internal/agui/auth_test.go (locate the existing `governance.write` reference at ~line 494 — confirm the assertion shape so the new const matches the string the test already expects)
  </read_first>
  <behavior>
    - InsertMCPAuditTx(ctx, q, MCPAuditInsert{ActorIdentityID, Action, ServerName, Reason}) appends exactly one row via the tx-bound q; a surrounding db.WithTx rollback leaves zero rows
    - MCPAuditStore.List(ctx, filter) returns rows newest-first (created_at DESC, id DESC); limit<=0 clamps to 100; limit>MaxInt32 clamps to MaxInt32
    - RequireCapability(next, deps, governanceWriteCapability) → 403 when ungranted (mirrors auth_test.go:494), passes for the `*`-holding `local` identity
  </behavior>
  <action>
    Create `internal/mcp/manager/audit.go`: `type MCPAuditStore struct { pool *pgxpool.Pool; q *sqlc.Queries }`,
    `NewMCPAuditStore(pool)`, `type MCPAuditInsert struct { ActorIdentityID, Action, ServerName, Reason string }`,
    `func InsertMCPAuditTx(ctx context.Context, q *sqlc.Queries, in MCPAuditInsert) error` (call
    `q.InsertMcpAudit(ctx, …)`; map an empty `Reason` to a NULL pgtype.Text; wrap errors), `type
    MCPAuditRow struct {…}`, and `func (s *MCPAuditStore) List(ctx, filter MCPAuditFilter)
    ([]MCPAuditRow, error)` (newest-first via `ListMcpAudit`, limit clamp identical to skills AuditStore.List).
    Mirror `internal/skills/audit_store.go` but OMIT the D-29 coherence machinery (no ApprovalSource/token
    NULL-mapping, no classifyAuditErr — the flat 0022 schema only has the append-only triggers).
    In `cmd/aura/serve_webui.go` add `const governanceWriteCapability = "governance.write"` with a doc
    comment (parity with agentRunCapability/governanceReadCapability) — placed near the other capability
    consts. Do NOT add route consts or mounts yet (those land in plans 02/03); just introduce the const so
    the later plans only add `mux.Handle(<route>, agui.RequireCapability(aguiHandler, auth,
    governanceWriteCapability))` lines. In `internal/agui/auth_test.go` confirm (or add, if missing) the
    table assertion that `RequireCapability(next, deps, "governance.write")` returns 403 when the identity
    lacks it — the value `governance.write` must match the new const exactly (A4: grep found only this test
    reference; lock it). Write `internal/mcp/manager/audit_test.go` (unit) covering MCPAuditInsert→params
    mapping (empty reason → NULL) and the List limit clamp.
  </action>
  <acceptance_criteria>
    - `internal/mcp/manager/audit.go` exports `MCPAuditStore`, `InsertMCPAuditTx`, `MCPAuditInsert`; `InsertMCPAuditTx` takes a `*sqlc.Queries` (tx-bound) argument
    - `grep -n 'governanceWriteCapability = "governance.write"' cmd/aura/serve_webui.go` matches
    - `go build ./...` and `go vet ./...` pass
    - `go test ./internal/mcp/manager/ -run TestMCPAudit` and `go test ./internal/agui/ -run TestRequireCapability` exit 0 (the auth_test.go:494 `governance.write` 403 assertion is green against the new const)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'governanceWriteCapability = "governance.write"' cmd/aura/serve_webui.go && grep -q 'func InsertMCPAuditTx' internal/mcp/manager/audit.go && go build ./...</automated>
  </verify>
  <done>MCPAuditStore/InsertMCPAuditTx exist and mirror the skills audit store (tx-bound insert, newest-first list); the governance.write capability const is defined and matches the auth_test.go:494 assertion; build+vet+unit tests green. No routes mounted yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cockpit browser → SSR mux | untrusted operator input crosses into the write surface (capability-gated downstream; no routes mounted in THIS plan) |
| app role → aura.mcp_audit | the DB role boundary that makes the ledger tamper-evident (aura_app has SELECT+INSERT only) |
| SPEC/REQUIREMENTS/ROADMAP truth-source → implementation | the D-09 amendment reconciles ALL THREE LOCKED artifacts with the operator directive before code depends on them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | aura.mcp_audit ledger | mitigate | role grant SELECT+INSERT only + BEFORE UPDATE OR DELETE row trigger + SEPARATE BEFORE TRUNCATE statement trigger (0021 template); live-PG test asserts insufficient_privilege on UPDATE/DELETE/TRUNCATE |
| T-29-01-02 | Repudiation | MCP config mutations | mitigate | InsertMCPAuditTx participates in the surrounding db.WithTx so a mutation cannot commit without its audit row (atomicity wired in plans 02/03; the store + tx primitive land here) |
| T-29-01-03 | Elevation of Privilege | governance.write capability | mitigate | RequireCapability + HasCapability + `*`-rejection invariant (auth.go:261, unchanged); the const is the only new code; no operator can grant a capability they lack |
| T-29-01-SC | Tampering | npm/pip/cargo installs | mitigate | NO package installs in this plan (audit table + Go store + a docs amendment only); `skills` npx CLI legitimacy is gated in plan 03's threat model where the install transport lands |
| T-29-01-04 | Information Disclosure | mcp_audit.reason free-text | accept | the reason is operator-authored, stored, and surfaced only behind governance.read/write; no secret is placed in reason by construction (the env-edit path in plan 02 never routes a value here) |

SSRF (MCP probe + skill-source fetch) and skill name/path traversal are NOT in this plan's
scope (no probe, no fetch, no skill path here); they are owned by `/gsd-secure-phase` — chokepoints:
the `/api/image-proxy` SSRF guard precedent and the `SanitizeName` grammar.
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean.
- `go test ./internal/mcp/manager/ -run TestMCPAudit` (unit) green.
- `go test -tags db_integration ./internal/mcp/manager/ -run TestMCPAuditAppendOnly` (live PG) green — UPDATE/DELETE/TRUNCATE each raise insufficient_privilege.
- `go test ./internal/agui/ -run TestRequireCapability` green (the auth_test.go:494 governance.write 403 assertion holds against the new const).
- The amendment-verify command (Task 1) exits 0: 29-SPEC.md + REQUIREMENTS.md + ROADMAP.md all carry the container-isolation framing and none presents `--ignore-scripts` as a checklist/"as safe" item.
- The D-09 amendment is a SEPARATE commit (`docs(29): amend SPEC/REQUIREMENTS/ROADMAP …`) preceding all skills-install code.
</verification>

<success_criteria>
- D-09 amendment committed FIRST as one atomic docs commit reconciling 29-SPEC.md + REQUIREMENTS.md + ROADMAP.md; no `--ignore-scripts`-as-safe wording remains in any of the three.
- aura.mcp_audit is append-only (dual triggers + role grant), proven on the live stack.
- InsertMCPAuditTx + MCPAuditStore.List exist, tx-bound, newest-first.
- governance.write capability const defined and matched to the existing auth_test.go assertion.
- No HTTP routes mounted yet (plans 02/03 add them); build+vet+unit+integration green.
</success_criteria>

<output>
Create `.planning/phases/29-governance-write-mcp-configuration-skills-install/29-01-SUMMARY.md` when done.
</output>
</content>
