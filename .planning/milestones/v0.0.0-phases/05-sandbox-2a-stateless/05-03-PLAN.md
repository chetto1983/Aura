---
phase: 05-sandbox-2a-stateless
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - internal/sandbox/sandbox.go
  - internal/sandbox/errors.go
  - internal/sandbox/docker.go
  - internal/sandbox/docker_test.go
  - internal/config/config.go
  - internal/agent/tools/execute.go
  - internal/agent/tools/execute_test.go
  - cmd/aura/main.go
  - cmd/aura/exec.go
  - cmd/aura/exec_test.go
autonomous: true
requirements: [CAP-01]
must_haves:
  truths:
    - "aura exec python \"print(2+2)\" prints 4 and exits 0 (CAP-01 SC#1)"
    - "ptrace via ctypes returns EPERM and open('/proc/self/root/etc/shadow') returns ENOENT/EPERM through the runner (CAP-01 SC#2)"
    - "socket().connect(('1.1.1.1',80)) and unshare(CLONE_NEWNET) both return EPERM through the runner (CAP-01 SC#3)"
    - "a non-zero script exit / seccomp EPERM / timeout / OOM / pids-cap is a ToolResult, never a Go error (D-18)"
    - "ErrSandboxUnreachable surfaces after a docker-CLI-gated one-shot auto-start fails; aura exec exits 70 on that infra failure (D-09/D-18/D-19)"
    - "execute is a Deferred:true tool validating lang in {python,shell}, clamping timeout_sec<=600, and rejecting session_id as reserved-for-Phase-8 (D-19)"
    - "the per-call timeout_sec is carried end-to-end: tool -> Runner.RunPython/RunShell(ctx,code,timeoutSec) -> {\"code\",\"timeout_sec\"} HTTP body, clamped <=600 with config default when omitted (D-16/D-19)"
    - "the lean preview routes the whole string through tools.NewResult (D-17/D-25) with zero new spillover code"
  artifacts:
    - path: "internal/sandbox/docker.go"
      provides: "DockerRunner HTTP client (goleak-safe) + one-shot docker-CLI-gated auto-start"
      min_lines: 120
      contains: "DisableKeepAlives"
    - path: "internal/sandbox/errors.go"
      provides: "ErrSandboxUnreachable + ErrSandboxProtocol sentinels"
      contains: "ErrSandboxUnreachable"
    - path: "internal/agent/tools/execute.go"
      provides: "deferred execute tool delegating to sandbox.Runner with lean preview"
      contains: "Deferred:"
    - path: "cmd/aura/exec.go"
      provides: "aura exec [--session] <lang> <code|-> CLI, exit 70 on ErrSandboxUnreachable"
      contains: "70"
    - path: "internal/sandbox/docker_test.go"
      provides: "//go:build sandbox_integration happy + negative + limit + sentinel tests + goleak TestMain"
      contains: "sandbox_integration"
  key_links:
    - from: "internal/agent/tools/execute.go"
      to: "sandbox.Runner"
      via: "injected Runner field, RunPython/RunShell(ctx,code,timeoutSec) delegation"
      pattern: "e\\.Runner\\.(RunPython|RunShell)"
    - from: "internal/sandbox/docker.go"
      to: "AURA_SANDBOX_URL/exec/<lang>"
      via: "http.NewRequestWithContext POST with {code,timeout_sec} body"
      pattern: "/exec/"
    - from: "cmd/aura/main.go buildRegistry"
      to: "tools.Execute"
      via: "reg.Register(&tools.Execute{Runner: dockerRunner})"
      pattern: "tools\\.Execute"
---

<objective>
Replace `sandbox.Stub` with the real `DockerRunner` (a goleak-safe HTTP client that talks to the 05-02 sidecar over `AURA_SANDBOX_URL`, with a docker-CLI-gated one-shot auto-start), add the two typed sentinels (`ErrSandboxUnreachable`/`ErrSandboxProtocol`), EXTEND the internal `Runner` interface to carry the per-call `timeoutSec` (D-16 wire contract requires `timeout_sec` in the body; D-19 exposes it on the tool — see Task 1), wire the three `AURA_SANDBOX_*` config fields, build the repo's FIRST `Deferred:true` tool (`execute`) with the lean D-17 preview routed through `tools.NewResult`, and add the hand-rolled `aura exec` CLI (exit 70 on infra failure, `session_id`/`--session` reserved-but-inert). All Go code is a near-verbatim copy of existing repo analogs (`openai_compat/client.go`, `ask_user.go`, `askuser/store.go` sentinels, `agent_test.go` re-exec pattern). Includes the Wave-0 test infrastructure (`docker_test.go` integration tier + unit tests + goleak `TestMain`) so every CAP-01 success-criterion has an automated signal.

Purpose: Deliver the Go integration surface for CAP-01 — the runner, the model-facing tool, the CLI, the config, and the security negative-test tier that proves the isolation boundary holds (EPERM/ENOENT).
Output: `internal/sandbox/{errors,docker,docker_test}.go`, modified `sandbox.go` (Stub removed, Runner signature extended, Result extended), `internal/agent/tools/execute{,_test}.go`, `internal/config/config.go` (+`defaultRuntimeForArch`), `cmd/aura/{exec,exec_test}.go`, modified `cmd/aura/main.go`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md
@.planning/phases/05-sandbox-2a-stateless/05-RESEARCH.md
@.planning/phases/05-sandbox-2a-stateless/05-PATTERNS.md
@internal/sandbox/sandbox.go
@internal/llm/openai_compat/client.go
@internal/agent/tools/ask_user.go
@internal/agent/tools/result.go
@cmd/aura/main.go
@cmd/aura/agent_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sentinels + extended Runner interface + DockerRunner HTTP client + config + integration test tier</name>
  <files>internal/sandbox/errors.go, internal/sandbox/docker.go, internal/sandbox/sandbox.go, internal/config/config.go, internal/sandbox/docker_test.go</files>
  <behavior>
    - The internal Runner interface is EXTENDED to carry the per-call timeout: `RunPython(ctx context.Context, code string, timeoutSec int) (Result, error)` and `RunShell(ctx context.Context, cmd string, timeoutSec int) (Result, error)`. This is REQUIRED — D-16's wire body is `{"code","timeout_sec"}` and D-19 exposes an optional `timeout_sec` on the tool, so the value MUST reach DockerRunner.exec; the interface is internal and already being changed (Stub -> DockerRunner), so extending the signature is in-scope and the cleanest path. When the caller passes timeoutSec <= 0 (tool omitted it) the runner substitutes cfg.SandboxTimeoutSec; the effective value is then clamped to <=600.
    - RunPython/RunShell POST to {url}/exec/{python|shell} with {code,timeout_sec}; decode {stdout,stderr,exit_code,elapsed_ms,truncated,limit_hit} into Result (+ carry truncated/limit_hit); a non-zero exit_code returns a populated Result with nil error (D-18).
    - A malformed (non-2xx or undecodable) response returns a wrapped ErrSandboxProtocol; a transport failure after a failed one-shot auto-start returns a wrapped ErrSandboxUnreachable (errors.Is-friendly).
    - timeout_sec is resolved (default-from-config when omitted) then clamped to <=600 runner-side; the per-call total timeout rides context.WithTimeout(ctx, effectiveTimeout), NOT http.Client.Timeout, AND the SAME effective value is marshalled into the {"code","timeout_sec"} body so the sidecar's subprocess timeout matches the runner's ctx deadline.
    - INTEGRATION (sandbox_integration tag, sidecar up): TestRunner_PythonHappy (print(2+2)->"4", exit 0); TestRunner_PtraceBlocked (ctypes ptrace -> EPERM text in stderr/non-zero, never a Go error); TestRunner_ProcRootDenied (/proc/self/root/etc/shadow -> ENOENT/EPERM); TestRunner_SocketBlocked + TestRunner_UnshareBlocked (EPERM); TestRunner_TimeoutLimitHit (infinite loop -> limit_hit=="timeout"); TestRunner_BakedPackagesImport (D-20 positive control: `import numpy,pandas; print(numpy.arange(9).reshape(3,3).sum())` -> exit 0 with the expected numeric output, proving the curated baked set imports + runs C-extensions under the hardened seccomp/gVisor runtime, i.e. the allowlist is not too tight for the batteries-included libs).
    - UNIT (no sidecar): TestDockerRunner_UnreachableSentinel (errors.Is ErrSandboxUnreachable against a dead URL with docker absent), TestRunShell_MalformedProtocol (httptest server returns junk -> errors.Is ErrSandboxProtocol), TestRunner_TimeoutClampedAndBodied (httptest echoes the request body -> assert timeout_sec>600 is sent as 600, and a 0/omitted value is sent as cfg.SandboxTimeoutSec).
    - The sandbox package test file declares `func TestMain(m *testing.M) { goleak.VerifyTestMain(m) }` (CLAUDE.md goleak mandate — the DisableKeepAlives HTTP client manages goroutines; mirror cmd/aura/agent_test.go).
  </behavior>
  <read_first>
    - internal/llm/openai_compat/client.go lines 3-12 (imports), 36-48 (HTTP client construction — COPY: dialer connect-timeout + DisableKeepAlives:true, NO http.Client.Timeout; the ctx carries the total timeout), 79-106 (NewRequestWithContext POST + Do + non-2xx -> typed error + Body.Close), line 28 (var _ llm.Client = (*Client)(nil) assertion shape).
    - internal/sandbox/sandbox.go lines 13-23 (the Runner interface + Result struct) + lines 25-36 (the Stub to remove) + lines 1-3 (package doc-comment to update — drop "First version is a stub"). NOTE: the Runner signatures ARE being changed in this task — see <action> for the new `RunPython(ctx, code, timeoutSec)` / `RunShell(ctx, cmd, timeoutSec)` shape (D-16/D-19 require per-call timeout_sec on the wire; the interface is internal and already mutating Stub->DockerRunner, so extending it is in-scope, not a violation).
    - internal/askuser/store.go lines 49-50 (bare errors.New sentinel pattern for errors.go) — and RESEARCH § Code Examples lines 354-360 (the exact two sentinel names).
    - internal/config/config.go lines 32-47 (Config struct — add SandboxURL/SandboxTimeoutSec/SandboxRuntime fields), lines 150-181 (envDefault/envIntDefault/defaultRunDir — add a sibling defaultRuntimeForArch() reading runtime.GOARCH: amd64->"runsc", arm64->"runc", per D-07), and RESEARCH § Code Examples lines 346-351 (the templated AURA_SANDBOX_* wiring + the 600 cap comment).
    - cmd/aura/agent_test.go envOrSkip / t.Fatal-under-$CI discipline (the no-skip-as-green pattern other integration tiers use) — replicate for the sandbox_integration tier so an unset AURA_SANDBOX_URL under $CI is a t.Fatal, not a silent skip — AND its `TestMain`/`goleak.VerifyTestMain(m)` shape to mirror in docker_test.go.
    - .planning/phases/05-sandbox-2a-stateless/05-PATTERNS.md § docker.go (lines 31-92 — the full per-call shape + auto-start helper note) + § errors.go (lines 96-118).
    - .planning/phases/05-sandbox-2a-stateless/05-RESEARCH.md Open Question 3 (auto-start best-effort + docker-CLI-gated; NEVER mount the docker socket).
  </read_first>
  <action>
    DECISION (apply, do not re-litigate — locked by D-16 wire contract + D-19 CLI): EXTEND the internal Runner interface to carry the per-call timeout. In internal/sandbox/sandbox.go change the two signatures to `RunPython(ctx context.Context, code string, timeoutSec int) (Result, error)` and `RunShell(ctx context.Context, cmd string, timeoutSec int) (Result, error)`, and EXTEND the Result struct with `Truncated bool` + `LimitHit string` (keep the four original fields Stdout/Stderr/ExitCode/ElapsedMs intact so the agent-loop binding is unbroken). This is the cleanest path because the interface is internal and is already being changed (Stub -> DockerRunner) — there is no external consumer to break. Update the package doc-comment (drop "First version is a stub"). Write internal/sandbox/errors.go: two bare errors.New sentinels ErrSandboxUnreachable ("sandbox sidecar unreachable (auto-start failed)") and ErrSandboxProtocol ("sandbox sidecar returned a malformed response"), per D-18 / PATTERNS. Add the three config fields to internal/config/config.go's Config struct and populate them in the loadBase() literal using envDefault/envIntDefault: SandboxURL default http://127.0.0.1:18901, SandboxTimeoutSec default 30 (the 600s cap is clamped runner-side, NOT here), SandboxRuntime default defaultRuntimeForArch() (new helper alongside defaultRunDir, reads runtime.GOARCH -> amd64="runsc"/arm64="runc", D-07). Write internal/sandbox/docker.go: a DockerRunner struct holding a *http.Client (constructed EXACTLY like client.go lines 36-48 — dialer connect-timeout + DisableKeepAlives:true, NO http.Client.Timeout), a base url, and the default+cap timeout (carry cfg.SandboxTimeoutSec as the per-runner default); var _ Runner = (*DockerRunner)(nil); a NewDockerRunner(cfg) constructor. RunPython/RunShell share a private exec(ctx, lang, code string, timeoutSec int) that: (a) resolves the effective timeout = (timeoutSec if >0 else r.defaultTimeoutSec) then clamps to <=600; (b) derives ctx = context.WithTimeout(ctx, time.Duration(effective)*time.Second); (c) marshals {"code":code,"timeout_sec":effective} (the SAME effective value the ctx uses); (d) POSTs to url+"/exec/"+lang; (e) on transport error calls the private autoStart() helper ONCE then retries the POST once. autoStart() is gated on exec.LookPath("docker") succeeding (RESEARCH OQ3) — if docker is absent it returns immediately so the caller wraps ErrSandboxUnreachable; when present it shells exec.CommandContext(ctx,"docker","compose","up","-d","aura-sandbox") then health-probes GET {url}/healthz. It MUST NEVER reference or mount the docker socket. Decode the response into Result{Stdout,Stderr,ExitCode,ElapsedMs,Truncated,LimitHit} (1:1 per D-16). A non-2xx or undecodable body -> fmt.Errorf("...: %w", ErrSandboxProtocol). A non-zero exit_code is a normal Result, NEVER an error (D-18). Then REMOVE Stub from sandbox.go (grep sandbox.Stub first — RESEARCH Runtime State Inventory says zero live .go usages, re-verify; if the agent-loop smoke wired it, switch that to a test double that satisfies the NEW 3-arg signature). Write internal/sandbox/docker_test.go with: a `func TestMain(m *testing.M) { goleak.VerifyTestMain(m) }` at top (CLAUDE.md goleak mandate, mirror cmd/aura/agent_test.go); the //go:build sandbox_integration tier (happy + negative + limit tests from <behavior>, each asserting the boundary errno per the negative-test inventory — a PASS that does not trigger the boundary is a false-green) using the t.Fatal-under-$CI skip-helper; PLUS the unit sentinel tests (no build tag, httptest + dead-URL) AND TestRunner_TimeoutClampedAndBodied (httptest echoes the body so the test asserts >600 -> 600 and 0 -> cfg.SandboxTimeoutSec on the wire) so the unit tier covers the typed-error paths + the timeout-delivery contract.
  </action>
  <verify>
    <automated>cd /home/user/Aura && go vet ./internal/sandbox/... && go build ./... && go test ./internal/sandbox/ && go test -race ./internal/sandbox/ && grep -q 'DisableKeepAlives' internal/sandbox/docker.go && grep -q 'ErrSandboxUnreachable' internal/sandbox/errors.go && grep -q 'ErrSandboxProtocol' internal/sandbox/errors.go && grep -Eq 'timeoutSec int' internal/sandbox/sandbox.go && grep -q 'sandbox_integration' internal/sandbox/docker_test.go && grep -q 'goleak.VerifyTestMain' internal/sandbox/docker_test.go && grep -q 'TestRunner_TimeoutClampedAndBodied' internal/sandbox/docker_test.go && grep -q 'TestRunner_BakedPackagesImport' internal/sandbox/docker_test.go && ! grep -q 'type Stub' internal/sandbox/sandbox.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - internal/sandbox/sandbox.go declares `RunPython(ctx context.Context, code string, timeoutSec int)` and `RunShell(ctx context.Context, cmd string, timeoutSec int)`, and Result carries Truncated bool + LimitHit string (the four original fields intact).
    - internal/sandbox/docker.go contains DisableKeepAlives: true and has NO http.Client{Timeout: (ctx-rides-timeout — grep for Timeout: shows only the dialer); it marshals the SAME effective timeout into the {"code","timeout_sec"} body that it puts on context.WithTimeout.
    - go test ./internal/sandbox/ (unit) passes including TestDockerRunner_UnreachableSentinel (errors.Is ErrSandboxUnreachable), TestRunShell_MalformedProtocol (errors.Is ErrSandboxProtocol), and TestRunner_TimeoutClampedAndBodied (>600 -> 600, 0 -> cfg default on the wire).
    - internal/sandbox/docker_test.go carries //go:build sandbox_integration on its integration tests, a goleak TestMain, and the negative tests assert the expected errno (ptrace/socket/unshare -> EPERM-shaped, /proc/self/root -> ENOENT/EPERM).
    - sandbox.go no longer declares Stub; var _ Runner = (*DockerRunner)(nil) compiles.
    - internal/config/config.go resolves AURA_SANDBOX_URL default http://127.0.0.1:18901 and defaultRuntimeForArch() returns runsc on amd64 / runc on arm64.
  </acceptance_criteria>
  <done>The internal Runner carries timeoutSec end-to-end; DockerRunner is a goleak-safe HTTP client with a docker-CLI-gated socket-free auto-start that bodies the same effective timeout it puts on the ctx; the two sentinels + three config fields exist; Stub is gone; the sandbox_integration negative-test tier + goleak TestMain + unit sentinel/timeout tests are in place (Wave-0 infra for SC#1/2/3 + D-18).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: execute deferred tool (lean preview, timeout pass-through) + execute_test.go</name>
  <files>internal/agent/tools/execute.go, internal/agent/tools/execute_test.go</files>
  <behavior>
    - Spec() returns Deferred:true with Name "execute", a lang enum {python,shell}, a code string, an optional timeout_sec integer, and a reserved session_id (described as reserved for Phase 8 / Slice 2b).
    - Execute() rejects lang not in {python,shell} (error ToolResult so the model self-corrects), clamps timeout_sec>600 to 600, and rejects a non-empty session_id with a "reserved for Phase 8 / Slice 2b" message.
    - The tool's optional timeout_sec is passed THROUGH to Runner.RunPython/RunShell as the new third arg (the Runner resolves 0/omitted to the config default and re-clamps <=600 — the tool clamps too as a defense-in-depth, both per D-16/D-19); a non-zero exit is NOT an error.
    - On success it delegates to Runner.RunPython(ctx,code,timeoutSec)/RunShell(ctx,cmd,timeoutSec) and builds the lean preview (D-17): stdout verbatim as primary content (no fence/label); "stderr:"+stderr appended only if stderr non-empty; "exit_code: N" line ONLY when exit_code != 0; "[limit: timeout|oom|pids]" + elapsed_ms ONLY when a limit was hit; "(no output, exit 0)" when stdout+stderr both empty and exit 0; then the WHOLE string -> tools.NewResult.
    - A typed sandbox error (ErrSandboxUnreachable/Protocol) propagates as the Execute error (loop sees "sandbox unavailable").
  </behavior>
  <read_first>
    - internal/agent/tools/ask_user.go lines 18-20 (struct tool — Execute holds an injected Runner like ToolSearch does), 87-107 (Spec() with inline json.RawMessage schema + enum), 114-130 (Execute() arg-unmarshal + the switch a.Kind {…default: error} enum-validation pattern to copy for lang).
    - internal/agent/tools/search.go lines 18-20 (ToolSearch{Registry} — the injected-dependency tool shape Execute{Runner} mirrors) + lines 58/68 (a tool builds a string then hands the WHOLE thing to NewResult — the reuse site).
    - internal/agent/tools/result.go lines 86-133 (NewResult — route the lean string through it; ZERO new truncation/spillover code; it reads the tool-call ctx the agent injected via WithToolCallContext).
    - internal/agent/tools/spec.go lines 18-46 (Spec.Deferred contract + Tool interface signatures — execute is the FIRST Deferred:true tool).
    - internal/sandbox/sandbox.go (the NEW Runner signature `RunPython(ctx,code,timeoutSec)` / `RunShell(ctx,cmd,timeoutSec)` from Task 1 — the third arg the tool forwards).
    - .planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md <decisions> D-16/D-17/D-18/D-19 + .planning/phases/05-sandbox-2a-stateless/05-PATTERNS.md § execute.go (lines 133-180).
  </read_first>
  <action>
    Write internal/agent/tools/execute.go. type Execute struct { Runner sandbox.Runner } (injected dependency, mirroring ToolSearch{Registry}). Spec() returns Deferred: true (CLAUDE.md mandatory partition — execute is the repo's FIRST deferred tool; copy the inline json.RawMessage schema shape from ask_user.go) with a lang enum ["python","shell"], a code string, an optional timeout_sec integer, and a reserved session_id string described as "reserved for Phase 8 / Slice 2b — leave unset". Summary one line; Description long with safety notes + one python and one shell example. Execute(ctx, raw): unmarshal args; validate lang via a switch (default -> fmt.Errorf error ToolResult so the model self-corrects, the ask_user.go pattern); if session_id non-empty return an error ToolResult "session_id is reserved for Phase 8 / Slice 2b (session-bound sandbox); omit it in 2a"; clamp the tool's timeout_sec to <=600 (defense-in-depth — the Runner re-clamps and substitutes the config default for 0/omitted); delegate to e.Runner.RunPython(ctx,code,timeoutSec) or RunShell(ctx,cmd,timeoutSec) — pass the (possibly 0) timeout_sec straight through as the new third arg; a typed sandbox error -> return ToolResult{}, err (propagates to the loop, D-18); build the lean preview via a private formatLean(res) per D-17 (stdout verbatim; stderr:+stderr only if non-empty; exit_code: N only when non-zero; [limit: …]+elapsed_ms only when res.LimitHit != ""; "(no output, exit 0)" when both streams empty and exit 0); then return NewResult(ctx, preview) — route the WHOLE string, ZERO spillover code. Write internal/agent/tools/execute_test.go (unit, NO build tag) with a fake sandbox.Runner (implements the NEW 3-arg signature, captures the timeoutSec it was called with, returns canned Results): TestExecute_LeanPreview table-driving the D-17 shapes (silent success, stderr-appended, non-zero exit_code line, limit-hit label, empty->"(no output, exit 0)"); TestExecute_DeferredSpec (asserts Spec().Deferred == true and the lang enum); TestExecute_LangEnumRejected (lang="ruby" -> error ToolResult); TestExecute_SessionIdReserved (non-empty session_id -> reserved error); TestExecute_TimeoutPassThrough (timeout_sec arg reaches the fake Runner; >600 clamped to 600); TestExecute_TypedErrorPropagates (fake Runner returns ErrSandboxUnreachable -> Execute returns the error). Use WithToolCallContext to give NewResult its ctx (mirror how search_test.go / result_test.go set it up).
  </action>
  <verify>
    <automated>cd /home/user/Aura && go vet ./internal/agent/tools/... && go build ./... && go test ./internal/agent/tools/ && go test -race ./internal/agent/tools/ && grep -q 'Deferred: *true' internal/agent/tools/execute.go && grep -Eq 'e\.Runner\.(RunPython|RunShell)\(ctx, *(code|cmd), *timeout' internal/agent/tools/execute.go && grep -q 'NewResult' internal/agent/tools/execute.go && grep -F -q '(no output, exit 0)' internal/agent/tools/execute.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - internal/agent/tools/execute.go sets Deferred: true (the repo's first deferred tool), validates a lang enum {python,shell}, and forwards timeout_sec as the third arg to Runner.RunPython/RunShell.
    - go test ./internal/agent/tools/ passes including TestExecute_LeanPreview (all D-17 shapes), TestExecute_SessionIdReserved, TestExecute_TimeoutPassThrough, and TestExecute_TypedErrorPropagates.
    - The lean preview routes the whole string through NewResult (grep NewResult present); no truncation/spillover logic is re-implemented in execute.go.
    - A non-zero exit produces a ToolResult (with the "exit_code: N" line), NOT a Go error (D-18).
  </acceptance_criteria>
  <done>execute is a Deferred:true tool that validates lang/session_id, clamps + forwards timeout_sec to the Runner's new third arg, delegates, formats the D-17 lean preview, and routes it through NewResult; unit tests cover every preview shape + the reserved-session + timeout-pass-through + typed-error paths.</done>
</task>

<task type="auto">
  <name>Task 3: aura exec CLI (exit 70) + buildRegistry wiring + exec_test.go</name>
  <files>cmd/aura/exec.go, cmd/aura/main.go, cmd/aura/exec_test.go</files>
  <read_first>
    - cmd/aura/main.go lines 1-17 (package doc-comment subcommand list — add `aura exec`), lines 32-57 (the switch dispatcher — add `case "exec": runExec(os.Args[2:])` mirroring `case "agent"`), line 60 (usage() string — add exec), lines 63-71 (buildRegistry — append the Execute registration like the existing reg.Register lines).
    - cmd/aura/agent_test.go lines 116-143 (TestParseDryRunArgs_* — the arg-parse unit-test shape) + lines 208-236 (TestRunAgent_ExitPaths — the re-exec subprocess pattern for asserting os.Exit(N) without killing the test process; copy verbatim for the exit-70 path).
    - internal/sandbox/sandbox.go (Runner interface — NEW 3-arg signature) + internal/sandbox/docker.go (NewDockerRunner from this plan's Task 1) + internal/config/config.go (config.Load sandbox fields).
    - .planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md <decisions> D-19 (aura exec [--session <id>] <lang> <code|->; lang positional in {python,shell}; code positional or `-`->stdin; exits with sandbox exit_code; ErrSandboxUnreachable -> exit 70; --session parsed-but-inert pointing to Phase 8) + .planning/phases/05-sandbox-2a-stateless/05-PATTERNS.md § main.go (lines 183-201) + § exec_test.go (lines 204-219).
  </read_first>
  <action>
    Write cmd/aura/exec.go with runExec(args []string): hand-rolled flag parse (NO cobra, repo convention) for an optional --session <id> (parsed-but-INERT — if set, print "—session is reserved for Phase 8 / Slice 2b (session-bound sandbox); not available in 2a" to stderr and exit non-zero), a required positional lang in {python,shell}, and a positional code (a single string, OR "-" to read the whole snippet from stdin for big/multi-line input). Build the runner from config.Load() sandbox fields via sandbox.NewDockerRunner(cfg); call RunPython(ctx,code,0)/RunShell(ctx,code,0) by lang (the CLI has no --timeout flag in 2a, so it passes 0 and lets the Runner substitute cfg.SandboxTimeoutSec). Render the SAME lean format as the tool (reuse a shared formatter — either export tools.FormatLean or duplicate the tiny D-17 formatter; prefer reuse to avoid drift) to stdout. Process exit code policy (D-19): exit with the sandbox res.ExitCode on a normal run; on errors.Is(err, sandbox.ErrSandboxUnreachable) print the message to stderr and os.Exit(70); on any other infra error (ErrSandboxProtocol etc.) exit non-zero (a distinct code, e.g. 71, or reuse 70 — document the choice). In main.go add case "exec": runExec(os.Args[2:]) to the switch, add exec to usage() and the package doc-comment, and in buildRegistry() append reg.Register(&tools.Execute{Runner: sandbox.NewDockerRunner(config.Load())}) (construct the runner from config; Execute is a pointer-receiver tool like &tools.ToolSearch{...}). Write cmd/aura/exec_test.go: TestParseExecArgs_* (lang enum, missing code, --session-inert error, stdin "-" path) as plain unit tests, and TestRunExec_Exit70 using the re-exec subprocess pattern from agent_test.go (set AURA_SANDBOX_URL to a dead loopback port with docker absent so auto-start is skipped -> ErrSandboxUnreachable -> assert the child process exits 70 via exec.ExitError.ExitCode()).
  </action>
  <verify>
    <automated>cd /home/user/Aura && go vet ./cmd/aura/... && go build ./... && go test ./cmd/aura/ && grep -q 'case "exec"' cmd/aura/main.go && grep -q 'tools.Execute' cmd/aura/main.go && grep -q '70' cmd/aura/exec.go && grep -Eq 'reserved for Phase 8|Slice 2b' cmd/aura/exec.go && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - cmd/aura/exec.go parses lang in {python,shell} + a code positional (or "-" for stdin) WITHOUT cobra, and treats --session as inert (errors pointing to Phase 8 / Slice 2b).
    - On errors.Is(err, sandbox.ErrSandboxUnreachable) the process exits 70; on a normal run it exits with the sandbox exit_code (D-19).
    - cmd/aura/main.go switch has `case "exec"`, usage() lists exec, and buildRegistry() registers &tools.Execute{Runner: ...}.
    - go test ./cmd/aura/ passes including TestRunExec_Exit70 (re-exec subprocess asserts ExitCode()==70) and the arg-parse units.
  </acceptance_criteria>
  <done>aura exec runs python|shell snippets (string or stdin) with the lean output, exits with the sandbox exit_code, exits 70 on ErrSandboxUnreachable, and rejects --session as reserved; execute is registered in the live tool registry; exit-70 + arg-parse tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop / CLI -> DockerRunner | The model (via execute) or the operator (via aura exec) hands untrusted code to the runner; the runner is a pure HTTP client and never spawns user code itself. |
| DockerRunner -> sidecar (HTTP, loopback) | The wire is loopback-only (127.0.0.1:18901); a malformed response is a typed protocol error, never a silent partial result. |
| runner host -> docker CLI (auto-start) | The one-shot auto-start shells `docker compose up`; it is gated on docker being on PATH and MUST NEVER mount the docker socket (escape vector #1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-EOP-SOCKET | Elevation of Privilege | auto-start helper | mitigate | autoStart() gated on exec.LookPath("docker"); shells `docker compose up -d aura-sandbox` only; NEVER references/mounts the docker socket (RESEARCH OQ3; asserted by code review + the never-mount-socket grep in 05-01). |
| T-05-03-V5-INPUT | Tampering | execute lang/timeout args | mitigate | lang strict enum {python,shell} (V5); timeout_sec clamped <=600 at BOTH the tool and the Runner; code itself is intentionally arbitrary (isolation, not validation, is the control — V10). |
| T-05-03-V13-WIRE | Tampering | sidecar JSON response | mitigate | non-2xx / undecodable -> ErrSandboxProtocol (V13); loopback-only port; the runner never trusts a partial body. |
| T-05-03-D18 | Repudiation | error taxonomy | mitigate | code's fault -> ToolResult (non-zero exit / EPERM / timeout / OOM / pids); environment's fault -> typed sentinel; a script exiting 1 is never a Go error (D-18) — proven by TestExecute_TypedErrorPropagates + the non-zero-exit preview test. |
| T-05-03-SC | Tampering | go module installs | mitigate | This plan installs NO new Go modules (stdlib net/http + the already-present go.uber.org/goleak test dep only — RESEARCH § Package Legitimacy Audit "installs no Go modules"). No npm/pip/crates registry package -> no [ASSUMED]/[SUS]/[SLOP] -> no blocking legitimacy checkpoint. |
</threat_model>

<verification>
- go vet ./... + go build ./... + go test -race ./internal/sandbox/ ./internal/agent/tools/ ./cmd/aura/ all green (unit tier).
- The internal Runner interface carries timeoutSec end-to-end; the same effective value lands on both the ctx deadline and the {"code","timeout_sec"} body.
- The sandbox_integration tier (negative tests) is created and tagged with a goleak TestMain (runs against the 05-02 sidecar at wave merge).
- DockerRunner copies the goleak-safe openai_compat HTTP-client shape (DisableKeepAlives, ctx-rides-timeout, no Client.Timeout).
- execute is the repo's first Deferred:true tool; the lean preview routes through tools.NewResult (no new spillover).
- aura exec exits 70 on ErrSandboxUnreachable; --session is reserved-but-inert.
</verification>

<success_criteria>
- CAP-01 SC#1/2/3 have automated signals: the sandbox_integration happy + negative tests assert exec(2+2)=4 and the EPERM/ENOENT boundaries.
- D-16/D-17/D-18/D-19 are implemented and unit-tested (timeout pass-through + delivery, lean preview shapes, typed sentinels, exit-70, reserved session_id).
- No file exceeds 600 LOC; all files pass golangci-lint with 0 issues; Stub removed.
</success_criteria>

<output>
Create `.planning/phases/05-sandbox-2a-stateless/05-03-SUMMARY.md` when done.
</output>
