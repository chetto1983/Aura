---
phase: 13-channels-telegram-multimodal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/db/migrations/0012_telegram.up.sql
  - internal/db/migrations/0012_telegram.down.sql
  - internal/db/queries/telegram_accounts.sql
  - internal/db/queries/telegram_setup_pending.sql
  - internal/channels/telegram/store.go
  - internal/channels/telegram/store_integration_test.go
  - internal/channels/main_test.go
  - internal/channels/telegram/main_test.go
  - internal/setup/main_test.go
autonomous: false
requirements: [UX-02, UX-03]
user_setup:
  - service: telegram-bot
    why: "Telegram channel transport (telebot.v4) + live integration tier"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather → /newbot → copy the HTTP API token (already in .env per spike-017)"

must_haves:
  truths:
    - "Migration 0012 applies cleanly on top of the 0011 floor and creates aura.telegram_accounts + aura.telegram_setup_pending"
    - "telegram.Store round-trips an onboarding token: INSERT pending → consume-and-INSERT-account atomically → cleanup-expired removes a stale row"
    - "go.mod pins gopkg.in/telebot.v4 v4.0.0-beta.9, golang.org/x/image v0.41.0 (direct), github.com/mdp/qrterminal/v3 v3.2.1"
  artifacts:
    - path: "internal/db/migrations/0012_telegram.up.sql"
      provides: "telegram_accounts + telegram_setup_pending DDL with aura_app/aura_migrate grants + FK to aura.identities"
      contains: "CREATE TABLE aura.telegram_accounts"
    - path: "internal/channels/telegram/store.go"
      provides: "Store{pool,q} CRUD for telegram_accounts + telegram_setup_pending, atomic consume-and-INSERT"
      min_lines: 80
    - path: "internal/db/queries/telegram_setup_pending.sql"
      provides: "sqlc queries incl. atomic consume-and-return"
      contains: "-- name:"
  key_links:
    - from: "internal/db/migrations/0012_telegram.up.sql"
      to: "aura.identities"
      via: "FK identity_id REFERENCES aura.identities(id)"
      pattern: "REFERENCES aura.identities"
    - from: "internal/channels/telegram/store.go"
      to: "telegram_setup_pending"
      via: "db.WithTx consume-pending-then-INSERT-account"
      pattern: "WithTx"
---

<objective>
Lay the Phase-13 data substrate and dependency floor: add the three external deps (gated on a legitimacy checkpoint), ship migration 0012 (`telegram_accounts` + `telegram_setup_pending`) with sqlc queries, build the `telegram.Store` CRUD adapter (copying the canonical `askuser.Store` pattern), and create the three goleak `TestMain` harnesses every later plan's package needs.

Purpose: Onboarding (UX-03) and the channel itself (UX-02) both persist against migration 0012; the Store is the sole DB seam for both. The deps must land first so every later plan compiles. The goleak TestMains satisfy amendment #15 (bot polling, SSE pump, async convert are all on the explicit goleak list).
Output: go.mod/go.sum updated, migration 0012 pair + 2 query files, `internal/channels/telegram/store.go`, 3 `main_test.go` goleak harnesses, a db_integration round-trip test.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md
@.planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md
@.planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate + add deps</name>
  <read_first>
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§Package Legitimacy Audit — the four [ASSUMED] packages + dispositions)
    - go.mod (current dep set; x/image is already an indirect dep to promote)
  </read_first>
  <action>BLOCKING-HUMAN gate before any go.mod mutation (package legitimacy, slopcheck-unavailable graceful-degradation per RESEARCH §Package Legitimacy Audit). On operator approval, run exactly `go get gopkg.in/telebot.v4@v4.0.0-beta.9 && go get golang.org/x/image@v0.41.0 && go get github.com/mdp/qrterminal/v3@v3.2.1` then `go mod tidy`; do NOT add skip2/go-qrcode (qr_svg deferred, OQ4). Then assert the telebot pin literal in go.mod + the x/image direct-require promotion (no `// indirect`). The exact steps + registry-verify URLs are in how-to-verify below.</action>
  <what-built>
    slopcheck was unavailable during research, so each `go get` is gated here per the graceful-degradation protocol. The planner pre-verified all four against their official registries (telebot.v4 = github.com/go-telebot/telebot, official, MIT, beta.9 published 2026-06-02; x/image = first-party golang.org/x; qrterminal/v3 = github.com/mdp/qrterminal, MIT, v3.2.1; skip2/go-qrcode = DEFERRED this phase per OQ4 — qr_svg body is next-milestone). This checkpoint confirms the operator accepts the three installs before they touch go.mod.
  </what-built>
  <how-to-verify>
    1. Confirm the three packages at their registries: npmjs/pypi N/A (Go) — verify `https://pkg.go.dev/gopkg.in/telebot.v4` (latest = v4.0.0-beta.9, repo go-telebot/telebot), `https://pkg.go.dev/golang.org/x/image` (first-party), `https://pkg.go.dev/github.com/mdp/qrterminal/v3` (v3.2.1, repo mdp/qrterminal).
    2. On approval, run exactly: `go get gopkg.in/telebot.v4@v4.0.0-beta.9 && go get golang.org/x/image@v0.41.0 && go get github.com/mdp/qrterminal/v3@v3.2.1` then `go mod tidy`.
    3. Do NOT add `skip2/go-qrcode` (qr_svg deferred per OQ4; the JSON field stays for forward-compat returning empty).
    4. Assert: `grep 'gopkg.in/telebot.v4 v4.0.0-beta.9' go.mod` matches the literal (the CI pin gate, amendment #58); `grep 'golang.org/x/image v0.41.0' go.mod` shows it promoted to the require block (direct, not `// indirect`).
  </how-to-verify>
  <resume-signal>Type "approved" to install the three deps, or name a package to reject.</resume-signal>
  <acceptance_criteria>
    - `go.mod` contains the literal line `gopkg.in/telebot.v4 v4.0.0-beta.9` (VALIDATION CI pin gate).
    - `golang.org/x/image v0.41.0` is in the direct require block (no `// indirect` suffix).
    - `github.com/mdp/qrterminal/v3 v3.2.1` present.
    - `go build ./...` still succeeds after `go mod tidy`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0012 + sqlc queries + telegram.Store</name>
  <files>internal/db/migrations/0012_telegram.up.sql, internal/db/migrations/0012_telegram.down.sql, internal/db/queries/telegram_accounts.sql, internal/db/queries/telegram_setup_pending.sql, internal/channels/telegram/store.go, internal/channels/telegram/store_integration_test.go</files>
  <read_first>
    - internal/db/migrations/0009_scheduler.up.sql (the grant + COMMENT + FK + partial-index pattern to copy verbatim)
    - internal/db/migrations/0004_identity.up.sql (the FK-to-aura.identities pattern)
    - internal/db/queries/identity.sql (the `-- name: X :one/:many/:exec` sqlc idiom)
    - internal/db/queries/paused_states.sql (atomic consume-and-return query shape)
    - internal/askuser/store.go (the canonical Store{pool,q} pattern: sqlc.New(pool), SQLSTATE via errors.As+pgErr.Code, sentinel errors, pgtype at boundary, db.WithTx for atomic multi-row)
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§Migration 0012 — the exact schema)
  </read_first>
  <behavior>
    - Migration 0012 up creates aura.telegram_accounts (PK telegram_user_id bigint, FK identity_id REFERENCES aura.identities(id), username/first_name text, added_at/last_seen_at timestamptz) and aura.telegram_setup_pending (PK onboarding_token text, FK identity_id, generated_by text, created_at, expires_at, consumed_at NULL), plus the partial index `telegram_setup_pending_active ON (expires_at) WHERE consumed_at IS NULL`, aura_app DML grants + aura_migrate GRANT ALL, and COMMENT ON TABLE on both.
    - Migration 0012 down DROPs both tables (reverse order, FK-safe).
    - Store.InsertPending writes a pending row; Store.ConsumeOnboarding atomically (db.WithTx) marks consumed_at + INSERTs telegram_accounts in one tx; re-consuming the same token (consumed_at already set) returns a sentinel ErrTokenConsumed, not a duplicate account.
    - Store.CleanupExpired deletes rows past expires_at with consumed_at NULL and returns the deleted count.
    - A unique-violation on telegram_accounts INSERT classifies via errors.As+pgErr.Code 23505 → ErrAccountExists sentinel (never message-match).
  </behavior>
  <action>Create the 0012 up/down pair mirroring the 0009 footer (aura_app gets SELECT/INSERT/UPDATE/DELETE, aura_migrate gets GRANT ALL; COMMENT citing PRD §Slice 9a + amendment #58; FK `REFERENCES aura.identities(id)`). The number is 0012 — the floor is 0011_tool_invocations and 0008 is proxied_child_id_text (Pitfall 3). Write `telegram_accounts.sql` (~6 named queries: InsertAccount, GetAccountByTelegramID, GetAccountByIdentity, TouchLastSeen, CountAccounts, ListAccounts) and `telegram_setup_pending.sql` (~3: InsertPending, ConsumePending atomic UPDATE...RETURNING, DeleteExpiredPending). Regenerate the sqlc client. Build `store.go` as `Store{pool *pgxpool.Pool, q *sqlc.Queries}` with `New(pool)` per the askuser.Store pattern; implement InsertPending, ConsumeOnboarding (db.WithTx: ConsumePending then InsertAccount), CleanupExpired, GetAccountByTelegramID, CountAccounts, ListAccounts; classify SQLSTATE via errors.As(&pgErr)+pgErr.Code; declare sentinels ErrTokenConsumed, ErrTokenNotFound, ErrAccountExists; convert pgtype at the boundary in a fromRow helper. Tag the integration test file `//go:build db_integration` and gate env via the envOrSkip/t.Fatal-under-$CI helper used by askuser's integration tests.</action>
  <verify>
    <automated>cd /d/Aura && go build ./... && go test -tags db_integration ./internal/channels/telegram/ 2>&1 | tail -20</automated>
  </verify>
  <done>Migration 0012 applies on the 0011 floor; store_integration_test.go proves InsertPending → ConsumeOnboarding (account row exists, pending consumed) → re-consume returns ErrTokenConsumed → CleanupExpired removes a stale unconsumed row; SQLSTATE classification covered.</done>
  <acceptance_criteria>
    - `go test -tags db_integration ./internal/channels/telegram/` passes the onboarding round-trip (VALIDATION UX-03 "onboarding round-trip INSERT telegram_accounts + cleanup expired").
    - `grep 'CREATE TABLE aura.telegram_accounts' internal/db/migrations/0012_telegram.up.sql` matches; the file contains `REFERENCES aura.identities` and both `GRANT ... TO aura_app` and `GRANT ALL ... TO aura_migrate`.
    - The partial index `WHERE consumed_at IS NULL` exists in the up migration.
    - Re-consuming a consumed token yields the `ErrTokenConsumed` sentinel (single-use, T-TokenReplay in VALIDATION).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: goleak TestMain harnesses for the three new packages</name>
  <files>internal/channels/main_test.go, internal/channels/telegram/main_test.go, internal/setup/main_test.go</files>
  <read_first>
    - internal/agent/tools/main_test.go (the exact TestMain + goleak.VerifyNone idiom to copy)
    - .planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md (§Wave 0 Requirements — the 3 TestMain files + amendment #15 mandate)
  </read_first>
  <action>Create one `main_test.go` per new package (`internal/channels`, `internal/channels/telegram`, `internal/setup`) with `func TestMain(m *testing.M)` calling `goleak.VerifyTestMain(m)` exactly like `internal/agent/tools/main_test.go`. These are the leak gates amendment #15 mandates for the bot polling goroutine (telegram), the SSE pump (setup), and the registry/async-convert paths (channels/telegram). Keep them minimal — no other content; later plans add real tests into these packages and inherit the leak check. NOTE for executors: plans 13-03/13-04 also write tests into `internal/channels` and `internal/channels/telegram`; these TestMain files must exist before those tests run, which is why they live here in the Wave-1 substrate plan.</action>
  <verify>
    <automated>cd /d/Aura && go vet ./internal/channels/... ./internal/setup/... && go test ./internal/channels/... ./internal/setup/... 2>&1 | tail -10</automated>
  </verify>
  <done>All three packages compile and their (currently trivial) test binaries run under goleak.VerifyTestMain; later plans drop tests in and get leak detection for free.</done>
  <acceptance_criteria>
    - `internal/channels/main_test.go`, `internal/channels/telegram/main_test.go`, `internal/setup/main_test.go` each contain `goleak.VerifyTestMain(m)`.
    - `go test ./internal/channels/... ./internal/setup/...` exits 0 (VALIDATION Wave-0 "TestMain with goleak.VerifyNone").
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres role separation | `aura_app` (runtime DML) vs `aura_migrate` (DDL) — migration 0012 enforces it |
| onboarding token → account | the single-use credential crossing from setup-API issuance to channel-side consumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-TokenReplay | Spoofing | telegram_setup_pending | mitigate | single-use `consumed_at` + 1h `expires_at`; ConsumeOnboarding is one db.WithTx; re-consume → ErrTokenConsumed sentinel (Task 2 acceptance) |
| T-13-01-RoleEsc | Elevation | migration 0012 grants | mitigate | aura_app gets DML only, aura_migrate gets DDL — mirrors 0009 (Task 2 grant footer) |
| T-13-01-SC | Tampering | go get telebot/x/image/qrterminal | mitigate | blocking-human legitimacy checkpoint (Task 1) before any go.mod mutation; all 3 registry-verified |
| T-13-01-FKOrphan | Integrity | telegram_accounts.identity_id | accept | FK `REFERENCES aura.identities(id)`; single-user `local` identity is seeded by Slice 1.7 — no orphan path this phase |
</threat_model>

<verification>
- `go build ./...` green after deps + sqlc regen.
- `go test -tags db_integration ./internal/channels/telegram/` proves the onboarding round-trip live against PG.
- `go test ./internal/channels/... ./internal/setup/...` green under goleak TestMains.
- `grep 'gopkg.in/telebot.v4 v4.0.0-beta.9' go.mod` matches the CI pin literal.
</verification>

<success_criteria>
- Deps installed behind the legitimacy gate; telebot pin literal present.
- Migration 0012 applies on the 0011 floor; both tables + partial index + grants + COMMENT present.
- telegram.Store round-trips onboarding atomically with single-use enforcement.
- Three goleak TestMains in place for the downstream packages.
</success_criteria>

<output>
Create `.planning/phases/13-channels-telegram-multimodal/13-01-SUMMARY.md` when done.
</output>
