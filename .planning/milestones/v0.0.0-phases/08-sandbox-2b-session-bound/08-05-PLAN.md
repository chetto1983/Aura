---
phase: 08-sandbox-2b-session-bound
plan: 05
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - internal/sandbox/sessions.go
  - internal/sandbox/sessions_test.go
  - internal/sandbox/workspace.go
  - internal/sandbox/workspace_test.go
  - internal/sandbox/cleanup.go
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "SessionManager.Acquire reuses an existing container for a conversation_id or creates one via `docker run` (gVisor runtime + all 2a hardening flags); a 6th concurrent session returns ErrSessionCapReached (no silent LRU)"
    - "Concurrent execute calls within one session serialize on a per-session mutex (D-07); the reaper evicts idle sessions after TTL and is goleak-clean"
    - "WorkspaceManager.walkSize quota + PurgeConversationDir use os.Root openat2 walks; a symlink planted in workspace/ is removed as a link, never followed (host /etc untouched)"
    - "Boot recovery marks active rows terminated + cleans stray aura-sandbox-sess-* containers; workspace files survive restart, in-memory interpreter state does not"
  artifacts:
    - path: "internal/sandbox/sessions.go"
      provides: "SessionManager: Acquire/Release, docker-CLI lifecycle (D-05, never socket), per-session lock (D-07), reaper (D-03), hard cap (D-12), boot recovery (D-06)"
      contains: "func (m *SessionManager) Acquire"
      min_lines: 80
    - path: "internal/sandbox/workspace.go"
      provides: "WorkspaceManager: EnsureDir, walkSize (os.Root quota), PurgeConversationDir (manual no-follow openat cascade)"
      contains: "os.OpenRoot"
    - path: "internal/sandbox/cleanup.go"
      provides: "ConversationCleaner interface impl that Conversations.Delete will call (08-08 wires it; avoids sandbox→conversations import cycle)"
  key_links:
    - from: "internal/sandbox/sessions.go reaper"
      to: "MarkTerminated + docker stop/rm"
      via: "TTL sweep (synctest-testable injectable clock)"
      pattern: "MarkTerminated|docker.*stop"
    - from: "internal/sandbox/workspace.go PurgeConversationDir"
      to: "os.Root no-follow walk"
      via: "openat2 RESOLVE_BENEATH"
      pattern: "OpenRoot|Lstat"
---

<objective>
Build the session control plane (`sessions.go`) and the symlink-safe workspace walkers (`workspace.go` + the `cleanup.go` interface impl). This is the heart of CAP-02: per-`conversation_id` gVisor containers with lifecycle owned by the SessionManager, a per-session lock, an idle-TTL reaper, a hard concurrency cap, boot recovery, and `os.Root`/openat2 walks that refuse attacker-planted symlinks.

Maps ROADMAP success criteria 1 (session persistence — the container reuse + workspace mount), 2 (symlink escape — the os.Root cascade), 3 (TTL reap). Criterion 4 (network) is 08-06; the live integration assertions are 08-09. This plan ships the LOGIC + unit/synctest coverage.

Purpose: the durable per-conversation execution boundary. Output: SessionManager + WorkspaceManager + the cleanup interface for Conversations.Delete.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/08-sandbox-2b-session-bound/08-PATTERNS.md
@.planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md
@.claude/skills/golang-concurrency/SKILL.md
@.claude/skills/golang-context/SKILL.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionManager control plane (lifecycle, lock, reaper, cap, boot recovery)</name>
  <read_first>
    - internal/sandbox/docker.go (lines 162-179 `autoStart`/`probeHealth`) — the LookPath-gated, NEVER-socket, `//nolint:gosec // fixed argv, no socket` shellout idiom to copy for `docker run`/`stop`/`rm`.
    - internal/web/dnspin.go (lines 28-44) — the mutex-guarded per-key map + `now func() time.Time` injectable clock to copy for the reaper's synctest determinism.
    - 08-RESEARCH.md `## Architecture Patterns` Pattern 1 (the `SessionManager`/`session` struct shapes) + Pitfall 6 (gVisor flags not inherited by ad-hoc docker run — replicate `--runtime --user 65532:65532 --read-only --tmpfs /tmp --cap-drop ALL --security-opt no-new-privileges --security-opt seccomp=<session-profile> --pids-limit 64 --memory 512m --cpus 1.0 --ulimit nofile=64`) + Pitfall 1 (session container connect+bridge posture, the seccomp variant lives with 08-07/08-08 compose, this task just passes the flag).
    - internal/config/config.go — the new SandboxSessionTTLSec / SandboxMaxConcurrentSessions / SandboxRuntime / PrivacyMode / SandboxNetworkAllowHosts fields from 08-02.
    - internal/db/sqlc generated SandboxSession + the 4 queries from 08-02 (InsertSession/TouchLastUsed/MarkTerminated/ListActive); internal/conversations/store.go parseUUID idiom (string conv_id → uuid at the boundary, landmine #1).
    - internal/sandbox/docker_integration_test.go (lines 25-29 goleak TestMain, 35-42 no-skip-as-green helper) — the test scaffolding to mirror.
  </read_first>
  <action>
    Create `internal/sandbox/sessions.go`. Define `session{ mu sync.Mutex; containerID string; port int; lastUsed atomic.Int64 }` and `SessionManager{ sessions sync.Map; capMu sync.Mutex; count int; maxN int; ttl time.Duration; now func() time.Time; runtime string; q <sqlc querier>; ... }`. `func NewSessionManager(ctx context.Context, deps...) *SessionManager` starts the reaper bound to `ctx`; `Close()` cancels + waits on a done channel (goleak-clean — RESEARCH Anti-Patterns "letting the reaper leak"). `func (m *SessionManager) Acquire(ctx context.Context, sessionID string) (*Session, error)`: parse sessionID→uuid; load-or-create the entry; under `capMu`, if creating and `count >= maxN` → return `ErrSessionCapReached` (NO silent LRU, D-12); on create shell `docker run --runtime=<m.runtime> <all 2a hardening flags from Pitfall 6> --name aura-sandbox-sess-<conv> -d <image>` via `exec.CommandContext` gated on `exec.LookPath("docker")` with the `//nolint:gosec // fixed argv, no socket` comment (NEVER mount the socket, D-05) + `InsertSession`; lock the session's `mu` (D-07 serialize); `TouchLastUsed`. `Release` unlocks. `func (m *SessionManager) reap()`: `time.Ticker(60s)` (or the injectable `now` for synctest) → `sessions.Range`, if `now-lastUsed > ttl` → `docker stop`/`docker rm` + `MarkTerminated` + delete from map + decrement count under capMu. Boot recovery `func (m *SessionManager) RecoverOnBoot(ctx)`: `ListActive` → `MarkTerminated` each (lazy recreate on next Acquire, D-06 parity with agent_job_runs) + a `docker ps --filter name=aura-sandbox-sess-` sweep that `docker rm`s strays (A7 — crash leftovers). Privacy-mode fail-fast (D-10 / 08-DECISIONS-WAVE0): at Acquire, if `cfg.PrivacyMode == "local-only"` AND the resolved allowlist is non-empty → return a typed error (explicit operator error). If sessions.go nears ~400 LOC, split `sessions_reaper.go`/`sessions_recovery.go` in the SAME commit (600-LOC ceiling). tdd: write `sessions_test.go` covering hard-cap→ErrSessionCapReached, concurrent-Acquire-same-session serialization (race), reaper-evicts-after-TTL via `testing/synctest` (verify `go doc testing/synctest` is GA in 1.26 — A1; fall back to driving the injectable `now` if not), boot-recovery marks terminated (db tier). goleak TestMain.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test -race -run 'TestSessions_HardCapEnforced|TestReaper_EvictsAfterTTL|TestSessions_ConcurrentSerialized' ./internal/sandbox/</automated>
  </verify>
  <acceptance_criteria>
    - `TestSessions_HardCapEnforced` asserts the 6th concurrent Acquire returns `ErrSessionCapReached` (errors.Is), no LRU eviction.
    - `TestReaper_EvictsAfterTTL` (synctest or injectable now) asserts a session past TTL is removed from the map + `MarkTerminated` called; goleak TestMain green (reaper exits on Close).
    - `TestSessions_ConcurrentSerialized` under `-race` shows two Acquires on the same session serialize (no data race).
    - grep confirms `exec.LookPath("docker")` gate + `--runtime=` + the full hardening flag set in the docker run argv; NO `/var/run/docker.sock` string anywhere.
    - Privacy-mode fail-fast path returns a typed error when local-only + non-empty allowlist.
  </acceptance_criteria>
  <done>SessionManager creates/reuses/reaps per-conv gVisor containers with cap + lock + boot recovery; reaper goleak-clean; never mounts the socket.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkspaceManager os.Root walks + cleanup interface (symlink-escape guard)</name>
  <read_first>
    - internal/conversations/orphan_scan.go (lines 32-43) — the existing `O_NOFOLLOW`/`Lstat` symlink-guarded walk (the pattern to UPGRADE to os.Root) over the same per-conversation dir.
    - internal/conversations/store.go (lines 431-454 `Store.Delete`, esp. line 449 `os.RemoveAll(dir)` — the call D-14 forbids for the workspace subtree; line 63 `RunDir`, sidecarDir helper).
    - internal/agent/tools/result.go (line ~70) — where `.result` spillover is written under the same `<run_dir>/conversations/<id>/` dir (co-tenancy landmine #4).
    - 08-RESEARCH.md Pattern 4 + `## Code Examples` "os.Root no-follow cascade delete" (the `purgeBeneath` post-order openat walk) + golang/go#67002 (no Root.RemoveAll) + #52745 (RemoveAll TOCTOU).
    - 08-PATTERNS.md "os.Root / openat2 symlink guard" Shared Pattern + landmine #4 (import-direction: sandbox must NOT import conversations).
  </read_first>
  <action>
    Create `internal/sandbox/workspace.go`: `WorkspaceManager{ runDir string; maxBytes int }`. `EnsureDir(convID) (string, error)` creates `<runDir>/conversations/<id>/workspace/` owner-writable (the container mounts it RW as `/workspace`, owner 65532; the host dir is created by Aura). `walkSize(convID) (int64, error)`: open the workspace dir with `os.OpenRoot(workspacePath)` → `*os.Root`; recurse via `root.ReadDir`/`root.Open` (openat2 RESOLVE_BENEATH — rejects `..` + escaping symlinks); `Lstat` each entry, sum regular-file sizes ONLY (never follow symlinks); return the total for the quota check (caller compares to `maxBytes` → `ErrWorkspaceQuotaExceeded`). `PurgeConversationDir(convID) error`: a MANUAL post-order no-follow openat walk over the whole `<id>/` dir (covers BOTH the attacker-controlled `workspace/` subtree AND the Aura-controlled `.result` files — landmine #4) using `os.OpenRoot` + `Lstat` + `root.Remove` (unlinks the link itself, NEVER the target); NEVER `os.RemoveAll` on the workspace subtree (D-14). Create `internal/sandbox/cleanup.go` exposing a concrete type implementing a small interface (e.g. `type ConversationCleaner interface { PurgeConversationDir(convID string) error }`) — DEFINED in `conversations` and wired in main.go (08-08), so `sandbox` does NOT import `conversations` (import-cycle guard). tdd: `workspace_test.go` — `TestWorkspace_SymlinkEscapeCascade`: create `<id>/workspace/`, `os.Symlink("/etc", "<workspace>/escape")` (or a temp dir standing in for /etc), call `PurgeConversationDir`, assert the symlink target survives (the temp /etc still exists) and the symlink entry is gone; `TestWorkspace_QuotaWalkSize`: assert walkSize sums regular files and does not traverse a symlinked dir.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test -run 'TestWorkspace_SymlinkEscapeCascade|TestWorkspace_QuotaWalkSize' ./internal/sandbox/ && grep -c "OpenRoot" internal/sandbox/workspace.go</automated>
  </verify>
  <acceptance_criteria>
    - `TestWorkspace_SymlinkEscapeCascade` asserts: after planting `ln -s <target> <workspace>/escape` then `PurgeConversationDir`, the `<target>` (stand-in for /etc) is UNTOUCHED and the `escape` symlink entry is removed (ROADMAP criterion 2 host-side gate).
    - `walkSize` uses `os.OpenRoot`/openat2 (grep `OpenRoot`), never `os.RemoveAll`, never follows symlinks (`Lstat` on entries).
    - `PurgeConversationDir` is a manual post-order walk (no `os.RemoveAll` on the workspace subtree).
    - `internal/sandbox` does NOT import `internal/conversations` (`go list -deps` / grep import block) — the cleanup interface is defined in conversations, wired in main.go (08-08).
  </acceptance_criteria>
  <done>Workspace quota + cascade use os.Root openat2; symlink escape refused host-side; no import cycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md / AR-05-01)

| Boundary | Description |
|----------|-------------|
| SessionManager ↔ docker runtime | Go shells `docker run/stop/rm` for LIFECYCLE only (D-05); execution stays HTTP (08-07). NEVER mounts the docker socket (inherited T-05-03-EOP-SOCKET). |
| host FS walker ↔ attacker-controlled workspace | `PurgeConversationDir`/`walkSize` are the unsandboxed host actors over a sidecar-writable tree; os.Root openat2 is the confinement (V4 access control). |
| reaper goroutine ↔ process lifecycle | reaper is bound to a cancelable ctx; Close waits (goleak — no leak). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-EOP-SYMLINK | Elevation/Tampering | workspace symlink escape (CVE-2026-39861 shape) | mitigate | `os.Root`/`os.OpenInRoot` openat2 RESOLVE_BENEATH for walkSize + a manual no-follow openat post-order cascade in PurgeConversationDir; `root.Remove` unlinks the link, never the target; `TestWorkspace_SymlinkEscapeCascade` gates it (ROADMAP crit 2). |
| T-08-05-EOP-SOCKET | Elevation of Privilege | docker-lifecycle carve-out | mitigate | `docker run/stop/rm` via exec.CommandContext, LookPath-gated, fixed argv, `//nolint:gosec // fixed argv, no socket`; NEVER `/var/run/docker.sock` (D-05, extends T-05-03-EOP-SOCKET). |
| T-08-05-EOP-RUNTIME | Elevation of Privilege | gVisor not inherited by ad-hoc docker run | mitigate | docker run argv passes `--runtime=<cfg.SandboxRuntime>` + ALL 2a hardening flags (Pitfall 6) — extends T-05-02-EOP-RUNTIME to the per-session container. |
| T-08-05-DOS-CAP | Denial of Service | session/RAM exhaustion | mitigate | hard cap 5 → `ErrSessionCapReached` (no silent LRU) + per-container 512m/1cpu/64pids in argv + 1800s TTL reaper (extends T-05-02-DOS). |
| T-08-05-DOS-QUOTA | Denial of Service | workspace fills host disk | mitigate | `walkSize` quota vs `AURA_SANDBOX_WORKSPACE_MAX_BYTES` → `ErrWorkspaceQuotaExceeded`. |
| T-08-05-INFO-XCONV | Information Disclosure | cross-conversation state leak | mitigate | one container + one workspace dir per conv (D-04); reaper removes both on eviction. |
| T-08-05-INFO-PRIVACY | Information Disclosure | privacy-mode bypass | mitigate | Acquire fails fast when `local-only` + non-empty allowlist (D-10 / 08-DECISIONS-WAVE0). |
| T-08-05-SC | Tampering | go module installs | accept | stdlib (os.Root, sync, testing/synctest) + existing goleak/sqlc/pgx; no new module. |
</threat_model>

<verification>
- `go test -race ./internal/sandbox/` unit + synctest tiers green; goleak TestMain clean.
- grep confirms no docker.sock; os.Root used; no sandbox→conversations import.
</verification>

<success_criteria>
The session control plane + symlink-safe workspace walkers ship with cap/lock/reaper/boot-recovery + the os.Root cascade, unit + synctest covered; live container assertions deferred to 08-09.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-05-SUMMARY.md` when done
</output>
