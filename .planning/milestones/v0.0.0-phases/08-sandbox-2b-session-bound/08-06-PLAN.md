---
phase: 08-sandbox-2b-session-bound
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-04"]
files_modified:
  - internal/sandbox/network.go
  - internal/sandbox/network_test.go
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "The forward proxy accepts CONNECT to an allowlisted host (e.g. pypi.org) and tunnels it; deny-wins glob precedence means a denied glob beats an allowed glob"
    - "A non-allowlisted host (1.1.1.1, example.com) is refused at CONNECT"
    - "An allowlisted host that resolves to a private/metadata/loopback IP is rejected (resolve-then-pin reuses the Slice-5 classify-every-fail-closed)"
    - "No MITM: the proxy validates the CONNECT target hostname only and tunnels opaque bytes; a global * in the deny list is rejected (Codex footgun)"
  artifacts:
    - path: "internal/sandbox/network.go"
      provides: "host-side CONNECT forward proxy: deny-wins glob allowlist, resolve-then-pin via internal/web export, Hijack+io.Copy tunnel, per-session policy keyed by conversation_id"
      contains: "http.Hijacker"
      min_lines: 60
  key_links:
    - from: "internal/sandbox/network.go CONNECT handler"
      to: "internal/web export (ClassifyIP / ResolveAndPin)"
      via: "resolve-then-pin reuse (no copy)"
      pattern: "web\\.(ClassifyIP|NewDialGuard|ResolveAndPin)"
---

<objective>
Build the host-side forward-proxy egress allowlist (`network.go`) that replaces the PRD's impossible in-container iptables (D-08 — iptables needs `CAP_NET_ADMIN`, contradicts the locked `cap_drop: ALL`; under gVisor iptables touches only the virtual netstack). A stdlib `net/http` CONNECT handler on a loopback host port: deny-wins glob allowlist (modeled on Codex `network_policy.rs`), resolve-then-pin via the `internal/web` export from 08-04 (NO copy), `Hijack()` + bidirectional `io.Copy`. NO MITM (D-10) — validate the CONNECT hostname only.

Maps ROADMAP criterion 4 (network allowlist). The LIVE pip-to-pypi assertion + the egress-bridge reachability spike are 08-09; this plan ships the proxy LOGIC + unit coverage (glob precedence, SSRF reject, malformed-CONNECT reject).

Purpose: untrusted code never owns its own firewall; egress is contained host-side. Output: `network.go` + `network_test.go`. Shares no file with the parallel Wave-3 plans (08-05 sessions/workspace, 08-07 sidecar/docker).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/08-sandbox-2b-session-bound/08-PATTERNS.md
@.planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md
@.claude/skills/golang-security/SKILL.md
@.claude/skills/golang-concurrency/SKILL.md
@.claude/skills/golang-context/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CONNECT forward proxy with deny-wins glob allowlist + resolve-then-pin</name>
  <read_first>
    - internal/web/export.go (from 08-04) — the exported `ClassifyIP` + resolve-then-pin entry (`NewDialGuard`/`ResolveAndPin`) the proxy MUST reuse (do NOT copy classify).
    - internal/web/transport.go (lines 80-90) — `hardenedTransport.dialContext` resolve-then-pin gate shape to mirror in the CONNECT handler.
    - internal/web/ssrf.go (lines 96-106) — classify-every-fail-closed (NEVER cherry-pick a public IP from a mixed DNS result).
    - 08-RESEARCH.md Pattern 3 (CONNECT handler + deny-wins glob, modeled on Codex `network_policy.rs`: `if p.denySet.Match(host){return false}; return p.allowSet.Match(host)`; reject a GLOBAL `*` in the deny list) + Pitfall 1 (the proxy must be reachable at the BRIDGE GATEWAY IP, not container-local 127.0.0.1 — the env wiring is 08-08, this task makes the proxy listen + tunnel) + Pitfall 7 (per-CONNECT resolution vs the 60s dnsPin TTL — prefer per-CONNECT or reuse the export's TTL).
    - 08-DECISIONS-WAVE0.md — proxy shape = CONNECT handler (not SOCKS5); privacy-mode interaction (the fail-fast lives in 08-05 Acquire; the proxy just enforces the per-session policy it is given).
    - D:/tmp/codex/codex-rs/network-proxy/src/network_policy.rs (grounding for deny-wins + global-wildcard-in-deny rejection) — read for the precedence rule, do not import.
  </read_first>
  <action>
    Create `internal/sandbox/network.go`. Define a `policy{ allowSet, denySet <glob matcher> }` with `func (p *policy) allow(host string) bool { if p.denySet.Match(host) { return false } ; return p.allowSet.Match(host) }` (deny ALWAYS wins, baseline-deny if not allowed — D-09). Glob semantics: `*.pythonhosted.org` matches `files.pythonhosted.org` but NOT the parent `pythonhosted.org`; reject a GLOBAL `*` in the deny list at policy-build time (Codex footgun). Use a careful suffix-match helper (or a small globset) — do NOT hand-roll fragile `strings.HasSuffix` edge cases. Build the per-session policy from the CSV `AURA_SANDBOX_NETWORK_ALLOW_HOSTS` (parsed at this boundary), keyed by `conversation_id`. `type Proxy struct{...}` with `func NewProxy(addr string, guard <web export dial-guard>) *Proxy` + `func (p *Proxy) Serve(ctx) error` running an `http.Server` on a loopback host port. On `CONNECT host:port`: (1) `net.SplitHostPort`; (2) `policy.allow(host)` → if false, reply 403 and close (refused); (3) reject malformed CONNECT (no host parsing of a body — CONNECT is an opaque tunnel; reject anything that is not a clean host:port); (4) resolve-then-pin via the `internal/web` export (`ResolveAndPin(ctx, convID, host)` → pinned IP or a blocked reason; reject if ANY resolved IP classifies private/metadata/loopback — fail-closed); (5) `http.Hijacker` to take the client conn, dial the PINNED IP:port, write `200 Connection Established`, `io.Copy` bidirectionally with the conn closed on ctx cancel. NO TLS decryption / NO MITM (D-10). tdd: `network_test.go` — `TestProxy_AllowlistGlobAndSSRF`: table over allow/deny precedence (`*.pythonhosted.org` allowed, parent denied-by-deny-glob beats allow, global `*` in deny rejected at build), a host that resolves to a private IP rejected (inject a resolver/guard stub), a malformed CONNECT rejected. Use `httptest`/an in-process listener; no live network in the unit tier (the live pypi test is 08-09).
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test -race -run 'TestProxy_AllowlistGlobAndSSRF' ./internal/sandbox/ && grep -c "web\.\(ClassifyIP\|NewDialGuard\|ResolveAndPin\)" internal/sandbox/network.go</automated>
  </verify>
  <acceptance_criteria>
    - `TestProxy_AllowlistGlobAndSSRF` asserts: deny-wins (`*.pythonhosted.org` allowed; a deny glob beats an allow glob); `*.pythonhosted.org` does NOT match the bare parent; a GLOBAL `*` in the deny list is rejected at policy build; a CONNECT to a host resolving to a private/metadata IP is refused; a malformed CONNECT is refused.
    - `network.go` reuses the `internal/web` export for resolve-then-pin (grep `web.ClassifyIP`/`web.NewDialGuard`/`web.ResolveAndPin`) — NO copy of `classify` (dupl-clean).
    - NO TLS decryption / no CA injection in network.go (grep shows no `tls.Config` cert minting); the proxy tunnels opaque bytes via Hijack + io.Copy.
    - The proxy listens on a loopback host port (the bridge-gateway reachability + env wiring is 08-08; this task's serve loop binds + tunnels).
  </acceptance_criteria>
  <done>The CONNECT forward proxy enforces deny-wins glob allowlist + resolve-then-pin (reusing the web export, no copy), no MITM; unit-tested for precedence + SSRF + malformed CONNECT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md / AR-05-01)

| Boundary | Description |
|----------|-------------|
| sidecar container ↔ host proxy | all opt-in egress crosses the host-side CONNECT proxy (untrusted code never owns its firewall, D-08); empty allowlist keeps the 2a egressless posture (no proxy env). |
| proxy ↔ external host | resolve-then-pin (Slice-5 reuse) + deny-wins glob: only allowlisted hostnames resolving to public IPs are tunneled; pinned IP guards DNS-rebinding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-INFO-EXFIL | Information Disclosure | egress allowlist bypass / data-exfil (Claude Code bypass lesson) | mitigate | CONNECT-time hostname validation + deny-wins glob + resolve-then-pin + classify-every-fail-closed (Slice-5 export); per-session policy keyed by conversation_id; the documented Claude-Code-bypass caveat is restated in 08-SECURITY (08-09). |
| T-08-06-INFO-REBIND | Information Disclosure | DNS rebinding | mitigate | resolve-then-pin (per-CONNECT or 60s dnsPin TTL) reuses the Slice-5 `validateAndPin`/`dnsPin`; rejects ANY blocked record (no cherry-pick). |
| T-08-06-TAMPER-SMUGGLE | Tampering | request smuggling through the proxy | mitigate | proxy validates the CONNECT target host:port only, parses no body/forwards no request (opaque tunnel via Hijack+io.Copy); malformed CONNECT rejected. |
| T-08-06-EOP-IPTABLES | Elevation of Privilege | in-container firewall (iptables anti-pattern) | mitigate | egress is host-side proxy ONLY (D-08); no `CAP_NET_ADMIN`, no in-container iptables (would contradict `cap_drop: ALL` + only touch gVisor virtual netstack). |
| T-08-06-INFO-PRIVACY | Information Disclosure | privacy-mode bypass | mitigate | session-create fail-fast (08-05) prevents a non-empty allowlist under `local-only`; the proxy enforces only the policy it is handed. |
| T-08-06-SC | Tampering | go module installs | accept | stdlib net/http (Hijacker) + the internal/web export; if a globset lib is chosen it must pass the Package Legitimacy Gate — prefer a stdlib suffix-match helper to avoid a new dep. |
</threat_model>

<verification>
- `go test -race ./internal/sandbox/` proxy unit tier green.
- grep: web export reused (no classify copy); no tls cert minting.
</verification>

<success_criteria>
The forward proxy enforces a deny-wins hostname allowlist + resolve-then-pin with no MITM, reusing the Slice-5 SSRF guard via the 08-04 export; live pypi reachability deferred to 08-09.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-06-SUMMARY.md` when done
</output>
