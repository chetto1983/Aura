---
phase: 08-sandbox-2b-session-bound
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .planning/DECISIONS.md
  - .planning/ROADMAP.md
  - .planning/phases/05-sandbox-2a-stateless/05-SECURITY.md
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "Operator greps prd.md and observes the 5 P8 amendments present (persistent-interp tier, docker-lifecycle carve-out, host-proxy egress, scoring home-slice, os.Root symlink guard)"
    - "Operator greps prd.md and observes 'migration 0008' for sandbox_sessions and 'conversation_id uuid' (NOT 0010, NOT text)"
    - "Operator reads DECISIONS.md and observes D-01..D-14 logged with amendment numbers"
    - "Wave-0 open questions (privacy-mode read site, SSRF export-vs-extract, egress-bridge posture) are resolved on paper before any code wave"
  artifacts:
    - path: "prd.md"
      provides: "Amended §Slice 2b (migration 0008, uuid FK, host-proxy egress, persistent-interp) + §Risk-Based Governance home-slice note"
      contains: "0008_sandbox_sessions"
    - path: ".planning/DECISIONS.md"
      provides: "D-01..D-14 logged + 5 amendment IDs"
    - path: ".planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md"
      provides: "Resolved Wave-0 open questions (privacy mode, SSRF reuse mechanism, egress posture)"
  key_links:
    - from: "prd.md §Slice 2b"
      to: ".planning/DECISIONS.md amendments"
      via: "cited amendment IDs"
      pattern: "0008|host-side proxy|os\\.Root"
---

<objective>
PRD-first gate (doc-only, FIRST — blocks every code wave). Apply the five Phase-8 PRD/DECISIONS amendments and resolve the three Wave-0 open questions on paper, so no later task writes code that deviates from the PRD without the amendment landing first.

Five amendments (per 08-CONTEXT `### Required PRD Amendments` + 08-RESEARCH landmines):
1. **D-01** — session sidecar holds a long-lived per-`session_id` Python interpreter (extends 2a `subprocess.run`-per-call); the two PRD persistence claims are the two tiers, not a contradiction.
2. **D-05** — contained docker-lifecycle carve-out: `SessionManager` shells `docker run`/`stop`/`rm` for lifecycle, execution stays HTTP-only; NEVER mounts the docker socket.
3. **D-08** — egress is a Go host-side forward proxy + hostname allowlist, NOT in-container iptables (replaces PRD "iptables OUTPUT rules" / "network_mode: bridge + iptables"; CAP-02 "via iptables" wording superseded).
4. **D-11** — `internal/scoring/` full module home-slice migrates from Slice 6 to Phase 8.
5. **D-13** — host-side workspace walkers use `os.Root`/`os.OpenInRoot` (openat2), NOT literal `O_NOFOLLOW`.
Plus the two schema landmines: **migration 0010 → 0008** (repo is at 0007) and **`conversation_id text` → `uuid`** (PK is uuid).

Purpose: PRD truth-source must match the code that lands. Output: amended prd.md + DECISIONS.md + ROADMAP.md goal wording + a Wave-0 decisions doc.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the 5 PRD/DECISIONS amendments + 2 schema landmine fixes</name>
  <read_first>
    - prd.md §Slice 2 (lines ~1200–1376) — the 2b acceptance, file targets, migration "0010", "conversation_id text", iptables egress wording (the strings to amend).
    - prd.md §Risk-Based Governance (lines ~4459–4548) — the `internal/scoring/` spec whose home slice moves to P8 (D-11).
    - prd.md §Persistence "Migration numbering — fonte di verità" — the migration-numbering source CLAUDE.md cites.
    - .planning/DECISIONS.md — the D12 amendment #36 entry format to mirror for the new D-01..D-14 + amendment IDs.
    - .planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md `### Required PRD Amendments` (the 5 items, verbatim intent) + 08-RESEARCH `## Common Pitfalls` (landmines #1/#2).
  </read_first>
  <action>
    Edit prd.md §Slice 2b: (a) change "migration 0010" → "migration 0008_sandbox_sessions" per the repo's actual next-free number (verify with `ls internal/db/migrations/` → highest is 0007); (b) change `conversation_id text NOT NULL REFERENCES aura.conversations(id)` → `conversation_id uuid NOT NULL REFERENCES aura.conversations(id) ON DELETE CASCADE` (PK is uuid, verified 0005:9; paused_states had the same text→uuid promotion); (c) replace the "iptables OUTPUT rules"/"network_mode: bridge + iptables" egress text with the D-08 host-side Go forward proxy + hostname-CONNECT allowlist + resolve-then-pin model (cite the gVisor/`CAP_NET_ADMIN` impossibility under the locked `cap_drop: ALL` floor); (d) note D-01 persistent per-session Python interpreter (in-memory `x=42` survives, prd.md:1252) PLUS the RW workspace mount as the TWO tiers; (e) note D-05 contained docker-lifecycle carve-out (lifecycle via docker CLI, execution HTTP-only, NEVER the socket); (f) note D-13 `os.Root`/openat2 supersedes `O_NOFOLLOW` for host walkers. Edit §Risk-Based Governance: add a home-slice note that `internal/scoring/` ships in Phase 8 (D-11), consumed by Scheduler P10 / Skills P11. Add D-01..D-14 to DECISIONS.md with 5 amendment IDs (mirror the #36 entry format). Update ROADMAP.md §"Phase 8" Goal line: "via iptables" → "via host-side forward-proxy allowlist", "O_NOFOLLOW" → "os.Root openat2". Do NOT touch any `.go`/`.sql`/`.py` file in this task — doc-only commit, mirroring the Phase 5 05-01 doc-gate.
  </action>
  <verify>
    <automated>cd /d/Aura && git diff --name-only | grep -vE '\.(go|sql|py)$' | grep -E 'prd\.md|DECISIONS\.md|ROADMAP\.md' && grep -c "0008_sandbox_sessions" prd.md && grep -c "conversation_id uuid" prd.md && grep -c "host-side" prd.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "0008_sandbox_sessions" prd.md` matches (migration renumber landed).
    - `grep "conversation_id uuid" prd.md` matches; no remaining `conversation_id text ... REFERENCES` for sandbox_sessions.
    - `grep -i "iptables" prd.md` no longer describes the 2b egress mechanism (host-proxy text present instead).
    - DECISIONS.md contains D-01..D-14 entries with amendment IDs.
    - `git diff --name-only` shows ZERO `.go`/`.sql`/`.py` changes (doc-only gate).
  </acceptance_criteria>
  <done>prd.md + DECISIONS.md + ROADMAP.md reflect all 5 amendments + 2 schema landmine fixes; zero code files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the 3 Wave-0 open questions into a decisions doc</name>
  <read_first>
    - 08-RESEARCH.md `## Open Questions` (1–5) + `## Assumptions Log` (A2 egress-bridge HIGH risk, A4 SSRF export vs extract, A5 privacy-mode fail-fast).
    - internal/config/config.go (lines 49-55 sandbox fields, 187-208 envDefault/envIntDefault) — confirm `AURA_PRIVACY_MODE` is NOT currently read anywhere (grep `PRIVACY_MODE` across internal/ returns nothing — RESEARCH OQ4).
    - internal/web/ssrf.go + dnspin.go + transport.go — confirm `classify`/`guard`/`validateAndPin`/`dnsPin` are unexported (landmine #5).
    - .planning/phases/05-sandbox-2a-stateless/05-SECURITY.md AR-05-01 — the 2a egressless bridge + connect-denied seccomp posture the 2b proxy route must deviate from.
  </read_first>
  <action>
    Write `.planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md` recording three resolutions the code waves consume: (1) **SSRF reuse mechanism (OQ2/A4)** — choose Option (a) export a minimal surface from `internal/web` (`web.ClassifyIP(netip.Addr) (string,bool)` + a dial-guard/`validateAndPin` constructor) over Option (b) extract `internal/netguard`, for scope control; mandate a Slice-5 web-tier re-test after export; FORBID copy-pasting `classify` (dupl/audit flags). (2) **Privacy-mode handling (OQ4/A5/D-10)** — `AURA_PRIVACY_MODE` is currently unread; decision: add a `PrivacyMode string` config field (`envDefault("AURA_PRIVACY_MODE","")`) and FAIL-FAST at session-create when it equals `local-only` AND `AURA_SANDBOX_NETWORK_ALLOW_HOSTS` is non-empty (explicit operator error, not silent-inert). (3) **Session-container network+seccomp posture (OQ1/A2, HIGHEST landmine #3)** — session containers need a seccomp variant that ALLOWS `connect(2)` (egress contained host-side by the proxy, not by connect-denial); the proxy must be reachable at the BRIDGE GATEWAY IP (not container-local `127.0.0.1`); an empty allowlist keeps the 2a egressless posture; this deviation EXTENDS AR-05-01 and must be re-stated in 08-SECURITY (authored in 08-09); flag the live reachability spike as a Wave-5 gate item. This doc is the contract Waves 2-5 cite; it is planning state, not a PRD amendment.
  </action>
  <verify>
    <automated>cd /d/Aura && test -f .planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md && grep -c "ClassifyIP\|netguard" .planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md && grep -c "local-only" .planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md && grep -c "connect(2)\|bridge gateway" .planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md</automated>
  </verify>
  <acceptance_criteria>
    - 08-DECISIONS-WAVE0.md exists and resolves all three OQs with a chosen option each.
    - SSRF decision = export-minimal-surface (Option a), with re-test mandate + no-copy-paste rule stated.
    - Privacy-mode decision = fail-fast under `local-only` + non-empty allowlist; names the new `PrivacyMode` config field.
    - Egress posture decision = connect-allowed session seccomp variant + bridge-gateway-IP reachability + empty-allowlist-keeps-egressless + extends-AR-05-01.
  </acceptance_criteria>
  <done>Three Wave-0 open questions resolved on paper; code waves have an unambiguous contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md)

| Boundary | Description |
|----------|-------------|
| PRD/DECISIONS ↔ implementation | doc-only this plan; the amendments make the truth-source match the code that lands in Waves 2-5 (non-repudiation, mirrors AR-05-01's reconciliation obligation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-DOC | Repudiation | PRD/DECISIONS drift (5 amendments + 2 schema landmines) | mitigate | This plan's Task 1 amends prd.md/DECISIONS.md/ROADMAP.md before any code commit; cites D-01/05/08/11/13 + amendment IDs; explicitly closes the AR-05-01-style "shipped silently" gap by landing the egress-pivot doc FIRST. |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | Doc-only plan — no package manifest touched; "no installs" holds (mirrors T-05-01-SC). |
</threat_model>

<verification>
- `git diff --name-only` shows only prd.md/DECISIONS.md/ROADMAP.md + the new 08-DECISIONS-WAVE0.md (zero code files).
- All 5 amendments + 2 landmine fixes greppable in prd.md.
</verification>

<success_criteria>
The PRD truth-source matches the code Waves 2-5 will write; the three Wave-0 open questions are resolved on paper so no code task stalls on an unmade decision.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-01-SUMMARY.md` when done
</output>
