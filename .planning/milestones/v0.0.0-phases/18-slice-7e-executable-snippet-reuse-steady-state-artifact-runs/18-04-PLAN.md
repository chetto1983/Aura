---
phase: 18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs
plan: 04
type: execute
wave: 4
depends_on: ["18-01", "18-02", "18-03"]
files_modified:
  - internal/eval/scenarios_skills.go
  - internal/eval/skills_cot_eval_test.go
  - internal/eval/skills_snippet_reuse_cot_eval_test.go
  - internal/eval/classify_cot_eval_test.go
  - docs/aura-quality-snapshot.md
autonomous: false
requirements: [CAP-08.1]
user_setup:
  - service: openrouter
    why: "The steady-state reuse E2E drives the live DeepSeek-V4 loop twice and asserts the 2nd-run ledger window — paid, operator-run, NOT CI"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard -> Keys (already in .env)"

must_haves:
  truths:
    - "The snippet-reuse eval registry MIRRORS production: it registers the production `skill` tool (live loader + writer over a temp skills root) alongside shell_exec + the read/web/fs tools, so a snippet-reuse scenario can resolve action=use/save_snippet (Pitfall 3 / #52-#53 rule 4)"
    - "The reuse gate runs the scenario TWICE (or pre-seeds the snippet) and asserts the STEADY-STATE (2nd) run's window from the aura.tool_invocations ledger — distinct request_id count <= the grounded target (~5 per D-03) AND wall-clock < 40s (Pitfall 5: never the first authoring run)"
    - "Every hard-floor assertion reads STRUCTURED ground truth — the ledger rows + the .xlsx artifact read-back — NEVER r.Reply (artifact-not-reply discipline)"
    - "The structural no-key slot (TestRegistry parity assertion) verifies the eval registry registers the skill tool WITHOUT a paid run, so a parity regression is caught even when the paid tier is gated off"
  artifacts:
    - path: "internal/eval/skills_snippet_reuse_cot_eval_test.go"
      provides: "The steady-state 2-run + ledger-window snippet-reuse gate (TestSnippetReuseE2E)"
      contains: "tool_invocations"
    - path: "internal/eval/scenarios_skills.go"
      provides: "The snippet-reuse scenario (reuse of a saved snippet on the 2nd run)"
    - path: "docs/aura-quality-snapshot.md"
      provides: "The Phase-18 steady-state row (call count + wall-clock, grounded by D-03)"
      contains: "Phase 18"
  key_links:
    - from: "internal/eval/skills_snippet_reuse_cot_eval_test.go"
      to: "internal/toolinvocations.Store.ListByConversation"
      via: "the gate counts distinct request_ids + sums/maxes duration_ms over the 2nd-run window"
      pattern: "ListByConversation"
    - from: "internal/eval/skills_snippet_reuse_cot_eval_test.go"
      to: "runner.Runner"
      via: "runner.New(deps) with Deps.ToolInvocations = toolinvocations.New(pool) wired (Option A — the production persist path)"
      pattern: "runner\\.New|Deps\\.ToolInvocations|toolinvocations\\.New"
    - from: "internal/eval/skills_cot_eval_test.go (buildSeamFreeSkillsRegistry)"
      to: "internal/agent/tools.SkillTool"
      via: "the eval registry registers the production skill tool (loader+writer) — parity"
      pattern: "SkillTool|newSkillTool"
---

<objective>
Make the steady-state acceptance machine-checkable and close the eval↔production registry parity gap.

Two changes: (1) the eval registry must register the production `skill` tool (live loader + writer) so a snippet-reuse scenario can resolve action=use / action=save_snippet — today `buildSeamFreeSkillsRegistry` registers NO skill tool (Pitfall 3, the #52/#53 rule-4 violation); (2) a new steady-state gate runs the xlsx scenario TWICE (or pre-seeds the snippet), and asserts the STEADY-STATE (2nd) run's window from the aura.tool_invocations ledger — distinct request_id count ≤ the D-03-grounded ~5-call target AND wall-clock < 40s — with the .xlsx artifact read-back as the artifact-not-reply ground truth.

Purpose: the phase goal — "steady-state artifact runs sotto i 40s / ~5 calls" — only becomes a release-blocking, ground-truthed acceptance if the gate reads the durable ledger (not r.Reply) on the REUSE run (not the authoring run). This is the measurement payoff of the whole phase.
Output: the parity-fixed eval registry + the ledger-asserted steady-state gate + the Phase-18 quality-snapshot row.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-CONTEXT.md
@.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-RESEARCH.md
@.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-PATTERNS.md
@docs/phase-18-xlsx-call-breakdown.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eval registry parity — register the production skill tool (Pitfall 3) + structural no-key assertion</name>
  <files>internal/eval/skills_cot_eval_test.go, internal/eval/classify_cot_eval_test.go</files>
  <read_first>
    - internal/eval/skills_cot_eval_test.go (buildSeamFreeSkillsRegistry lines 231-244 — the seam-free registry that omits the skill tool; the WHY comment 19-44; buildSkillsRegistry 197-219)
    - internal/eval/scenarios_skills.go (the existing xlsx North-Star scenario shape)
    - internal/eval/classify_cot_eval_test.go (find it — the no-key TestRegistry_SeamFree + TestClassify* structural tests that run WITHOUT OPENROUTER_API_KEY)
    - cmd/aura/serve_adapters.go (newSkillTool 253-271 + skillLoaderAdapter + skillWriterAdapter — the production wiring the eval registry must mirror; note these live in package main, so the eval test must construct the equivalent via internal/skills.Loader + internal/skills.Writer directly, OR a shared helper)
    - internal/skills package (NewLoader, NewWriter signatures — the eval builds them over a temp skills root)
    - internal/agent/tools/skill.go (SkillTool struct + the skillLoader/skillWriter seams the eval must satisfy with an in-test adapter)
  </read_first>
  <action>
    Extend the eval registry so it registers the production `skill` tool, mirroring production (RESEARCH Pitfall 3 / #52-#53 rule 4). Because cmd/aura.newSkillTool lives in package main (not importable from internal/eval), construct the equivalent in the eval package: build a skills.Loader + skills.Writer over a temp skills root (t.TempDir-rooted active/pending/archived/export dirs + a pool for the writer's audit when db_integration is available — OR a loader-only skill tool with a nil writer for the read/use path if the reuse scenario pre-seeds the snippet via the loader). Add an in-eval adapter satisfying tools.skillLoader (List/Body/ManifestDescription/Snippet) + tools.skillWriter (SaveSnippet/Restore/ArchiveSnippet/WriteMutation).
    REUSABLE-CODE constraint (CLAUDE.md "Never duplicate; extract a helper"): the production adapters in cmd/aura/serve_adapters.go are package-main-private, so they are NOT directly importable. Do NOT blindly copy the ~15 LOC adapter bodies into the eval test as silent duplication. Resolve it ONE of two ways, and STATE which in the task SUMMARY:
      (a) PREFERRED — extract the minimal skill adapters (skillLoaderAdapter/skillWriterAdapter over *skills.Loader/*skills.Writer) into a small shared internal package (e.g. internal/skilladapters) importable from BOTH cmd/aura (serve_adapters.go switches to use it) AND internal/eval. This is the deep-refactor-on-touch move and removes the duplication at its root.
      (b) FALLBACK (only if (a) widens blast radius beyond this phase) — keep an eval-test-local adapter, but add an explicit one-line justification comment in the test file: "// eval-test-only adapter; never a production surface; mirrors serve_adapters.go skillLoaderAdapter (~15 LOC). Kept local to avoid touching cmd/aura wiring in a measurement-only plan." Then file a STATE.md follow-up to extract the shared package when serve_adapters.go is next touched.
    Register `&tools.SkillTool{Loader: evalLoaderAdapter, Writer: evalWriterAdapter}` into buildSeamFreeSkillsRegistry (or a new buildSnippetReuseRegistry sibling so the existing find-skills xlsx gate is unchanged — prefer a sibling constructor to avoid perturbing the shipped TestSkillsE2E).
    Update the structural no-key test (classify_cot_eval_test.go TestRegistry*): add an assertion that the snippet-reuse registry registers a tool named "skill" (parity check, runs key-free). This catches a parity regression even when the paid tier is skipped — the no-skip-as-green discipline.
    Update the registry's WHY comment block to note the snippet-reuse path needs the skill tool (the existing comment at lines 19-44 explains why the find-skills path deliberately omitted it — add the reuse-path exception).
  </action>
  <verify>
    <automated>go vet -tags cot_eval ./internal/eval/ && go test -tags cot_eval -run 'TestRegistry|TestClassify' ./internal/eval/</automated>
  </verify>
  <acceptance_criteria>
    - A snippet-reuse eval registry constructor registers a tool named "skill" (loader + writer over a temp root)
    - `go test -tags cot_eval -run TestRegistry ./internal/eval/` passes WITHOUT OPENROUTER_API_KEY and asserts the skill tool is present (structural parity)
    - The existing find-skills TestSkillsE2E registry (buildSeamFreeSkillsRegistry) is unchanged OR the skill tool is added without breaking its structural TestRegistry_SeamFree (verify both still compile + the no-key structural tests pass)
    - The adapter is NOT silently duplicated: either extracted to a shared internal package (preferred) OR carries the explicit eval-test-only justification comment + a STATE.md extraction follow-up (the SUMMARY states which path was taken and why)
    - `go vet -tags cot_eval ./internal/eval/` exits 0
    - internal/agent/tools stays free of an internal/eval import (one-direction)
  </acceptance_criteria>
  <done>The snippet-reuse eval registry registers the production skill tool (loader+writer) with no silent adapter duplication; a key-free structural test asserts the parity; the find-skills gate is undisturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Steady-state ledger-asserted snippet-reuse gate (Pitfall 5 + Pattern 3) + scenario</name>
  <files>internal/eval/scenarios_skills.go, internal/eval/skills_snippet_reuse_cot_eval_test.go</files>
  <read_first>
    - internal/eval/skills_cot_eval_test.go (runSkillsScenario 246-313, driveSkillsLoop 321-366, verifyXlsxArtifact reference, newInspectableWorkspace 172-188, runStart/freshness windowing 271 + mtime>=run-start)
    - internal/eval/scenarios_skills.go (skillsScenarios 62-91, skillsExpect struct — the data-only ground-truth shape to extend for reuse)
    - internal/eval/skills_xlsx_verify_cot_eval_test.go (verifyXlsxArtifact — the artifact-not-reply read-back to reuse)
    - internal/toolinvocations/store.go (New(pool) constructor + ListByConversation 67-81, Event fields RequestID/DurationMS/StartedAt/EndedAt/Seq — the gate's read seam)
    - internal/runner/runner.go (runner.New + Deps struct, Deps.ToolInvocations line ~44 — the production persist injection point)
    - internal/runner/interfaces.go lines 72-77 (the ToolInvocationStore interface toolinvocations.Store satisfies)
    - internal/runner/runner_persist.go lines 69-101 (persistToolInvocation — the production wiring that fires per dispatched tool when Deps.ToolInvocations is set)
    - docs/phase-18-xlsx-call-breakdown.md (the D-03 grounded ~5-call/<40s target — the gate's threshold constant comes from THIS doc, not the registration estimate)
    - internal/db/migrations/0011_tool_invocations.up.sql (the ledger schema — distinct request_id = LLM roundtrip proxy; ts/started_at/ended_at for wall-clock)
    - reference_cot_eval_harness + reference_e2e_full_matrix_invocation (auto-memory — the invocation pattern)
  </read_first>
  <action>
    Add a snippet-reuse scenario to scenarios_skills.go (a SEPARATE entry or a sibling skillsSnippetReuseScenarios() so TestSkillsE2E is untouched): a NATURAL prompt that, on the FIRST run, drives the model to author + SAVE a reusable xlsx-builder snippet (action=save_snippet) and produce the .xlsx; then on the SECOND run, the SAME natural prompt should drive the model to discover the saved snippet (action=list/use) and run it BY PATH via shell_exec (the host frame from 18-02), collapsing the loop. Extend skillsExpect with the reuse thresholds (maxSteadyStateCalls int, maxSteadyStateWallClock time.Duration) sourced from docs/phase-18-xlsx-call-breakdown.md (default ~5 calls / 40s, but use the grounded numbers).
    Add skills_snippet_reuse_cot_eval_test.go (build tag cot_eval, OPENROUTER-gated with a clear t.Skip when unset; the structural slot is Task 1's TestRegistry).
    RUNNER WIRING — RESOLVED TO OPTION A ONLY (key_links): use the production runner.Runner path so persistToolInvocation fires per dispatched tool exactly as in production. Do NOT use a bare LlmAgent + manual persist (that bypasses runner_persist.go — the exact production wiring this measurement validates; it would make the gate measure a non-production path). Concretely:
    - Boot a real pool (LoadDB) so the runner persists into aura.tool_invocations (this gate is db-backed — it reads the ledger).
    - Build the runner via runner.New(deps) with deps.ToolInvocations = toolinvocations.New(pool) (the ToolInvocationStore the runner's persistToolInvocation writes through). The reuse registry from Task 1 is the runner's tool registry.
    - Run the scenario through the runner so each dispatched tool produces append-only ledger facts under the run's conversation_id. Capture run 2's conversation_id from the runner's run result/conversation handle.
    - Run the scenario TWICE against the SAME skills root + workspace so the snippet saved in run 1 is present in run 2 (Pitfall 5: pre-seed via run 1, OR explicitly seed an active snippet before run 1 and assert run 1 USES it — pick the cleaner: pre-seed the snippet active, then measure ONE reuse run as the steady state; document the choice in the SUMMARY).
    - STEADY-STATE ASSERTION (Pattern 3, artifact-not-reply): events := toolinvocations.New(pool).ListByConversation(ctx, run2ConvID); distinctRequestIDs := count of distinct RequestID; wallClock := max(EndedAt) - min(StartedAt) over the window. Assert distinctRequestIDs <= maxSteadyStateCalls AND wallClock < maxSteadyStateWallClock. NEVER assert on the model prose.
    - ARTIFACT ASSERTION: reuse verifyXlsxArtifact — the produced .xlsx exists FRESH in the workspace, opens via openpyxl, contains today's date.
    - Write the observed steady-state numbers (call count, wall-clock) to docs/aura-quality-snapshot.md as a new Phase-18 row (docs/, never /tmp). The operator fills the live TBDs on the checkpoint run.
    Keep goleak discipline; the gate is operator-run + paid (NOT CI), exactly like TestSkillsE2E / TestSwarmE2E.
  </action>
  <verify>
    <automated>go vet -tags cot_eval ./internal/eval/ && go build -tags cot_eval ./internal/eval/</automated>
  </verify>
  <acceptance_criteria>
    - skills_snippet_reuse_cot_eval_test.go compiles under the cot_eval tag and t.Skips cleanly when OPENROUTER_API_KEY is unset (with the operator-invocation message)
    - The gate drives the scenario through runner.New(deps) with deps.ToolInvocations = toolinvocations.New(pool) (Option A — the production persist path) — NOT a bare LlmAgent + manual persist; persistToolInvocation is the source of the ledger rows the assertion reads
    - The steady-state assertion reads aura.tool_invocations via ListByConversation and asserts distinct-request-id count <= the grounded target AND wall-clock < 40s on the REUSE (2nd / pre-seeded) run — never the authoring run, never r.Reply
    - The .xlsx artifact read-back (verifyXlsxArtifact) is the artifact-not-reply ground truth
    - The reuse threshold constants are sourced from docs/phase-18-xlsx-call-breakdown.md (the D-03 grounding), not the registration estimate
    - docs/aura-quality-snapshot.md gains a Phase-18 steady-state row (call count + wall-clock columns)
    - `go vet -tags cot_eval ./internal/eval/` + `go build -tags cot_eval ./internal/eval/` exit 0
  </acceptance_criteria>
  <done>The snippet-reuse gate drives the scenario through the production runner.Runner (Deps.ToolInvocations wired), asserts the 2nd-run ledger window (<=~5 calls / <40s) + the fresh .xlsx, sourced from the D-03 grounding; the quality snapshot has the Phase-18 row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The eval registry now mirrors production (skill tool registered) and a steady-state snippet-reuse gate asserts the 2nd-run ledger window via the production runner.Runner. This checkpoint runs the full live gate + the phase quality gate, fills the Phase-18 quality-snapshot row with real numbers, and confirms the <40s/~5-call steady state holds end-to-end.</what-built>
  <read_first>
    - internal/eval/skills_snippet_reuse_cot_eval_test.go header (the operator invocation block)
    - docs/phase-18-xlsx-call-breakdown.md (the D-03 grounded target — the gate's thresholds)
    - reference_validate_phase_procedure (auto-memory — live-run every gate, watch stale -run names, mutation on WSL go-mutesting, B4 filtered coverage)
    - CLAUDE.md "Quality tooling & gates" + "COVERAGE FLOOR 85%" — the phase-close numbers
  </read_first>
  <how-to-verify>
    1. Bring up the stack per the eval header: `docker compose up -d searxng` (+ Postgres migrated through 0011 — `make neo4j-migrate` / the migrate target; the ledger gate needs the DB). Export `set -a; . ./.env; set +a; export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"; export AURA_SKILLS_DIR=...; export AURA_RUN_DIR=...; export SEARXNG_URL=...` and the composed DSNs (AURA_DB_URL/AURA_DB_MIGRATE_URL from POSTGRES_PASSWORD per memory).
    2. Run the steady-state reuse gate live: `go test -tags 'cot_eval db_integration' -run TestSnippetReuseE2E -timeout 900s -v ./internal/eval/`. Confirm the 2nd-run distinct-request-id count and wall-clock from the ledger meet the grounded target (<=~5 / <40s). OPEN the produced .xlsx visually (feedback_inspect_artifact_visually_not_just_pass_status).
    3. Run the phase quality gate in WSL: `make quality-full` (vet+build+lint+race+coverage>=85% across the tag matrix + integration) — confirm green. Mutation spot-check >=70% on the new handlers (Writer.Restore, actionRestore/actionArchive/actionSaveSnippet, SnippetHostPath) via go-mutesting on WSL.
    4. Fill the Phase-18 row in docs/aura-quality-snapshot.md with the observed steady-state call count + wall-clock + coverage + mutation numbers (replace TBDs).
    5. Run `bash scripts/cache_invariant_audit.sh` — confirm the skill manifest/messages[1] byte-stability still holds (Pitfall 6) after the schema/use-frame edits.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed 2nd-run call count + wall-clock + coverage % + mutation % from the live run, or describe any gate below threshold (e.g. the steady state didn't hit ~5 calls — which would feed back into D-03's provisional target).</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (exclude from drift verification):
- `internal/eval/skills_snippet_reuse_cot_eval_test.go` — TestSnippetReuseE2E + the ledger-window helpers
- `skillsSnippetReuseScenarios()` (or a new reuse scenario entry) in scenarios_skills.go
- `skillsExpect.maxSteadyStateCalls`, `.maxSteadyStateWallClock` — new reuse-threshold fields
- A snippet-reuse eval registry constructor registering the production skill tool (loader+writer) + the skillLoader/skillWriter adapters (shared internal/skilladapters package — preferred — or eval-test-local with justification)
- A new "Phase 18" row in docs/aura-quality-snapshot.md
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval test → live LLM (paid) | The gate sends paid OpenRouter requests; OPENROUTER_API_KEY must not leak into the report/snapshot |
| eval registry → production parity | An eval-only seam that diverges from production silently invalidates the gate's signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11-I | Info Disclosure | eval report / quality snapshot | mitigate | The harness already asserts no OPENROUTER_API_KEY value appears in the final answer (skills_cot_eval_test.go SECRET LEAK check); the reuse gate reuses that check. The ledger window dump records request_id counts + durations, not args containing secrets |
| T-18-12-T | Tampering | eval↔production registry divergence | mitigate | Task 1 registers the PRODUCTION skill tool (loader+writer); Task 2 drives it through the PRODUCTION runner.Runner (Deps.ToolInvocations wired) so the measured path is the shipped one; the key-free TestRegistry asserts the tool's presence so a parity regression fails CI's structural slot even when the paid tier is skipped (no-skip-as-green) |
| T-18-13-R | Repudiation | steady-state measurement | mitigate | The gate reads the append-only aura.tool_invocations ledger (0011 triggers) — the measurement is tamper-evident, not a self-reported count |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs (internal Go; the host xlsx deps openpyxl are the operator/fat-image prerequisite per RESEARCH Environment Availability + Pitfall 7 — not a Go-module dependency) |
</threat_model>

<verification>
- `go vet -tags cot_eval ./internal/eval/ && go build -tags 'cot_eval db_integration' ./internal/eval/` (compiles under the gate tags)
- `go test -tags cot_eval -run 'TestRegistry|TestClassify' ./internal/eval/` (key-free structural parity)
- LIVE (checkpoint): `go test -tags 'cot_eval db_integration' -run TestSnippetReuseE2E -timeout 900s ./internal/eval/` — the 2nd-run ledger window <=~5 calls / <40s + fresh .xlsx
- `make quality-full` (WSL, stack up) — coverage >=85% across the tag matrix + race + lint + integration
- `bash scripts/cache_invariant_audit.sh` (Pitfall 6 — messages[0]/messages[1] byte-stability after the schema/use-frame edits)
</verification>

<success_criteria>
- The eval registry mirrors production (skill tool registered), proven key-free by TestRegistry (Pitfall 3 closed)
- The steady-state gate drives the production runner.Runner (Deps.ToolInvocations wired — Option A) and asserts the REUSE-run ledger window (<=~5 calls / <40s) + the fresh .xlsx — artifact-not-reply (Pitfall 5 closed)
- The reuse thresholds are grounded in the D-03 live breakdown, not an estimate
- docs/aura-quality-snapshot.md has the Phase-18 steady-state row filled live; coverage >=85%; mutation >=70% on the new handlers
</success_criteria>

<output>
Create `.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-04-SUMMARY.md` when done
</output>
</content>
