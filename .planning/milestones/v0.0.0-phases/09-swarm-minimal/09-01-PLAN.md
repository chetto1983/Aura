---
phase: 09-swarm-minimal
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - prd.md
  - .planning/DECISIONS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CAP-03]
must_haves:
  truths:
    - "Operator greps prd.md §Slice 3 and observes the STALE Talk/broadcast/swarm_join/swarm_talk/bus/tier.go acceptance items removed or struck (D-23 supersedes)"
    - "Operator greps prd.md env catalog and observes AURA_SWARM_MAX_GOALS=8 + AURA_SWARM_CHILD_TIMEOUT_SEC=120 added; AURA_MCP_MAIL_SERVER / AURA_MCP_WHATSAPP_SERVER are NOT present (managed config is the path, D-21 supersedes D-23 env list)"
    - "Operator reads DECISIONS.md and observes D-01..D-25 logged with an amendment ID and the OQ1 env-contradiction resolution recorded"
    - "Operator reads ROADMAP.md §Phase 9 and observes SC#2 re-specced to tool-not-available + depth code-guard, SC#3 re-specced to 5 needs_user_input report entries, and an added SC#5 live E2E/MCP criterion"
  artifacts:
    - path: "prd.md"
      provides: "Amended §Slice 3 (cut machinery removed, flat-v1, pause-as-report) + env catalog (2 new AURA_SWARM_* vars, no AURA_MCP_*_SERVER)"
      contains: "AURA_SWARM_MAX_GOALS"
    - path: ".planning/DECISIONS.md"
      provides: "D-01..D-25 logged + amendment ID + OQ1 resolution"
      contains: "D-25"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 SC#2/SC#3 re-specced + SC#5 added"
      contains: "needs_user_input"
  key_links:
    - from: "prd.md §Slice 3"
      to: ".planning/DECISIONS.md amendment"
      via: "cited amendment ID + D-row numbers"
      pattern: "AURA_SWARM_MAX_GOALS|pause-as-report|flat v1"
---

<objective>
PRD-first gate (doc-only, FIRST — blocks every code wave of Phase 9, mirroring the 05-01 / 08-01 doc-gate precedent). Land the D-23 amendment so the PRD truth-source matches the code that follows. No `.go`/`.sql`/`.py` edits in this plan.

The D-23 amendment supersedes the STALE PRD Slice-3 acceptance list (`prd.md:1421-1457` plans `swarm_talk`/`swarm_join`/bus/`tier.go`/`ResumeChild`/`SpawnInteractive`/children-map — all CUT). It also resolves RESEARCH Open Question #1: D-23's env-add list named `AURA_MCP_MAIL_SERVER` + `AURA_MCP_WHATSAPP_SERVER`, but D-21 (the later spike-corrected truth) says there are NO such env vars — the managed config `~/.aura/mcp/servers.json` via `aura mcp` is the registration path. The amendment adds ONLY `AURA_SWARM_MAX_GOALS` + `AURA_SWARM_CHILD_TIMEOUT_SEC` and explicitly drops the two `AURA_MCP_*_SERVER` entries.

Purpose: the PRD must not contain acceptance bullets or env vars that the code intentionally omits. Output: amended prd.md + DECISIONS.md (D-01..D-25 logged) + ROADMAP.md re-specced SCs.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-swarm-minimal/09-CONTEXT.md
@.planning/phases/09-swarm-minimal/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend prd.md §Slice 3 + env catalog (D-23 cuts + 2 new env vars, drop the AURA_MCP_*_SERVER entries)</name>
  <files>prd.md</files>
  <read_first>
    - prd.md §Slice 3 (lines ~1388-1475) — the STALE acceptance list (Talk/broadcast/swarm_join/swarm_talk/bus/tier.go/ResumeChild/SpawnInteractive/children-map), amendments #12/#34, and the file-target list (`bus.go`/`tier.go`/`swarm_talk.go`/`swarm_join.go`) to strike.
    - prd.md env catalog §"Caps & Limits" (lines ~4765-4769) — the existing `AURA_SWARM_MAX_DEPTH`/`AURA_SWARM_MAX_CONCURRENT`/`AURA_SWARM_MODEL_*` rows; the table format to mirror for the 2 new rows.
    - prd.md §"Caps & Limits" naming convention note (lines ~4824, ~4852) — the `AURA_<DOMAIN>_<UNIT>` + `_SEC` suffix rules the 2 new vars must follow.
    - .planning/phases/09-swarm-minimal/09-CONTEXT.md D-23 + D-01..D-22 (the supersession list, verbatim intent).
    - .planning/phases/09-swarm-minimal/09-RESEARCH.md §"Open Questions" #1 (the D-21-vs-D-23 env contradiction + the recommended resolution).
  </read_first>
  <action>
    Edit prd.md §Slice 3 acceptance + file-target list: strike/replace every CUT item — `swarm_talk`, `swarm_join`, the inter-agent message bus, `tier.go` tier→model dispatcher (keep the no-op env note as historical), `ResumeChild`/`SpawnInteractive`/`Responder` live-channel machinery, children-map+RWMutex, nested spawn (replace with flat v1 + `AURA_SWARM_MAX_DEPTH=2` retained as forward-compat code guard). Replace the pause machinery wording with D-04 pause-as-report (a child whose `ask_user` fires terminates → `{status:"needs_user_input", question, options, tool_call_id}` report entry; parent relays via its OWN `ask_user` + re-spawn; NO parked children). Replace the file targets `bus.go`/`tier.go`/`swarm_talk.go`/`swarm_join.go` with the D-16 ephemeral-runner shape (`internal/swarm/{swarm,report,brief}.go` + `internal/agent/tools/swarm_spawn.go`). Note OQ1 (D-07 goal-as-first-user-message supersedes "system prompt parametrizzato dal goal") and OQ5 (D-04 pause-as-report supersedes the Responder design) as resolved. Note #34(B) sandbox-session-serialize wording superseded (sandbox-agent is process-based, no per-session lock — D-14). In the env catalog §"Caps & Limits", ADD two rows mirroring the existing `AURA_SWARM_*` row format: `AURA_SWARM_MAX_GOALS` default `8` (cap; D-13 goals-array cap → model-readable tool error) and `AURA_SWARM_CHILD_TIMEOUT_SEC` default `120` (per-worker ctx deadline; D-11; timeout → `{status:"failed", error:"timeout"}` entry, siblings unaffected). Do NOT add `AURA_MCP_MAIL_SERVER` or `AURA_MCP_WHATSAPP_SERVER` — write a one-line note in the env catalog citing D-21: MCP server registration is via managed config `~/.aura/mcp/servers.json` (`aura mcp {install,add}`), the `AURA_MCP_*_SERVER_JSON` vars remain test-tier overrides only. This task is doc-only — touch NO `.go`/`.sql`/`.py` file.
  </action>
  <verify>
    <automated>cd D:/Aura && grep -c "AURA_SWARM_MAX_GOALS" prd.md && grep -c "AURA_SWARM_CHILD_TIMEOUT_SEC" prd.md && test "$(grep -c 'AURA_MCP_MAIL_SERVER' prd.md)" = "0" && test "$(grep -c 'AURA_MCP_WHATSAPP_SERVER' prd.md)" = "0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AURA_SWARM_MAX_GOALS" prd.md` returns ≥1 and `grep -c "AURA_SWARM_CHILD_TIMEOUT_SEC" prd.md` returns ≥1
    - `grep -c "AURA_MCP_MAIL_SERVER" prd.md` returns 0 AND `grep -c "AURA_MCP_WHATSAPP_SERVER" prd.md` returns 0 (env vars NOT added per D-21)
    - prd.md §Slice 3 contains a "pause-as-report" / "needs_user_input" description and no longer presents `swarm_talk`/`swarm_join`/bus as v1 acceptance items
    - The 2 new env rows use the `AURA_<DOMAIN>_<UNIT>` convention with the `_SEC` suffix on the timeout (per the naming note at prd.md ~4852)
    - No `.go`, `.sql`, or `.py` file appears in this commit's diff (`git diff --name-only` shows only prd.md, DECISIONS.md, ROADMAP.md)
  </acceptance_criteria>
  <done>prd.md §Slice 3 reflects the D-01..D-25 v1 shape; the 2 new env vars are catalogued; the AURA_MCP_*_SERVER vars are explicitly excluded with a D-21 note.</done>
</task>

<task type="auto">
  <name>Task 2: Log D-01..D-25 to DECISIONS.md + re-spec ROADMAP.md SC#2/SC#3 + add SC#5</name>
  <files>.planning/DECISIONS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/DECISIONS.md — the existing amendment-entry format (the latest entry block) to mirror for the new D-01..D-25 + amendment ID.
    - .planning/ROADMAP.md §"Phase 9: Swarm (Minimal)" (lines ~267-280) — the current Goal + SC#1..SC#4 wording to re-spec; SC#2 ("MAX_SPAWN_DEPTH=2 exceeded") and SC#3 (`TestSpawnInteractive_MultiPause_AllResolved`) are the stale ones.
    - .planning/phases/09-swarm-minimal/09-CONTEXT.md D-10 (SC#2 re-spec), D-04 + "ROADMAP SC#3 re-spec" (SC#3 re-spec), D-22 + D-23 (SC#5 add).
  </read_first>
  <action>
    Append a new amendment block to .planning/DECISIONS.md (mirror the latest entry's format) logging D-01..D-25 verbatim-intent (one line each, cite CONTEXT.md as source), plus a "Resolved Open Questions" note recording RESEARCH OQ1: D-21 supersedes D-23's env-add list — `AURA_MCP_*_SERVER` vars dropped, managed config is the registration path; and OQ2/OQ3 design picks (registry `Without` helper is in scope; worker framing rides in messages[1]/UserTurns, NO `SystemPrompt` mutation — byte-stable messages[0]). In .planning/ROADMAP.md §"Phase 9", re-spec the success criteria: SC#2 → "Operator confirms a worker's registry does NOT contain `swarm_spawn` (tool-not-available) AND a unit test drives the code guard at synthetic depth ≥ `AURA_SWARM_MAX_DEPTH` and observes the PRD error string" (D-10, replacing the old runtime "MAX_SPAWN_DEPTH=2 exceeded" attempt-at-depth-3 wording). SC#3 → "Operator runs a 5-children-all-pause swarm and observes 5 `needs_user_input` report entries; resume = re-spawn with answers, cancel = no re-spawn; no stuck/leaked goroutines (goleak clean)" (D-04). ADD SC#5 → "Operator runs the live `cot_eval` swarm E2E (natural prompt, NO 'swarm' word) with mail + WhatsApp MCP mounts and observes: N workers spawned via tool_use blocks, expected facts present, mail/WhatsApp message exists on read-back via MCP, wall-clock < 1.5× single-worker, judge rubric ≥90% average, no over-spawn on a simple control prompt" (D-22). Keep SC#1 and SC#4 as-is (still accurate). Update the §Phase 9 "**Plans**: TBD" line later in the ROADMAP-update step.
  </action>
  <verify>
    <automated>cd D:/Aura && grep -c "D-25" .planning/DECISIONS.md && grep -c "needs_user_input" .planning/ROADMAP.md && grep -c "cot_eval" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-25" .planning/DECISIONS.md` returns ≥1 (D-01..D-25 logged)
    - DECISIONS.md contains the OQ1 resolution string (e.g. "D-21 supersedes" or "AURA_MCP_*_SERVER ... dropped" / "managed config")
    - `grep -c "needs_user_input" .planning/ROADMAP.md` returns ≥1 (SC#3 re-specced)
    - ROADMAP.md §Phase 9 SC#2 no longer reads the old runtime "MAX_SPAWN_DEPTH=2 exceeded at depth 3" attempt wording and instead reads tool-not-available + code-guard
    - ROADMAP.md §Phase 9 contains a 5th success criterion mentioning `cot_eval` + judge ≥90% + mail/WhatsApp read-back
  </acceptance_criteria>
  <done>D-01..D-25 + OQ resolutions are logged in DECISIONS.md; ROADMAP §Phase 9 SC#2/SC#3 re-specced and SC#5 added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — doc-only) | This plan edits planning/PRD markdown only; no runtime input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | prd.md truth-source drift | mitigate | This very plan IS the mitigation: the PRD-first gate forbids code that deviates from the PRD without the amendment landing first (CLAUDE.md PRD-first principle). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (doc-only); the MCP server legitimacy is handled in 09-03 (forks are the user's own / spike-validated, RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `grep` assertions in each task's `<verify>` pass.
- `git diff --name-only` for the plan's commit shows ONLY prd.md, .planning/DECISIONS.md, .planning/ROADMAP.md (no code files — doc-only gate).
</verification>

<success_criteria>
- prd.md §Slice 3 no longer presents cut machinery (bus/talk/join/tier/ResumeChild) as v1 acceptance.
- `AURA_SWARM_MAX_GOALS` + `AURA_SWARM_CHILD_TIMEOUT_SEC` catalogued; `AURA_MCP_*_SERVER` vars absent (D-21).
- D-01..D-25 + OQ1/OQ2/OQ3 resolutions logged in DECISIONS.md.
- ROADMAP §Phase 9 SC#2/SC#3 re-specced + SC#5 added.
</success_criteria>

<output>
Create `.planning/phases/09-swarm-minimal/09-01-SUMMARY.md` when done.
</output>
