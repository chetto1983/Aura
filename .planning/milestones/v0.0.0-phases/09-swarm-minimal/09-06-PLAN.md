---
phase: 09-swarm-minimal
plan: 06
type: execute
wave: 3
depends_on: ["09-05", "09-03", "09-04"]
files_modified:
  - internal/eval/dataset_cot_eval.go
  - internal/eval/scoring_cot_eval.go
  - internal/eval/judge_cot_eval.go
  - internal/eval/harness_swarm_e2e_test.go
  - docs/aura-quality-snapshot.md
autonomous: true
requirements: [CAP-03]
must_haves:
  truths:
    - "Operator runs the live cot_eval swarm E2E with a NATURAL prompt (NO 'swarm' word) and observes the model autonomously spawns N workers via tool_use blocks (SC#5)"
    - "Ground-truth hard floor: expected facts present in the aggregated answer; mail/WhatsApp message exists on read-back via the same MCP server; wall-clock < 1.5× single-worker (D-22)"
    - "Judge rubric ≥90% average across autonomous-parallelization, sub-answer correctness, aggregation quality, and no-over-spawn-on-control (D-22)"
    - "The tier is OPENROUTER-gated + operator-run, NOT CI (the one legitimate skip); CI gates stay on unit/property/race/goleak (no-skip-as-green honored)"
    - "docs/aura-quality-snapshot.md gains a Phase 9 swarm row: a PLACEHOLDER lands in the code commit (harness compiles, row present); the real numbers + judge ≥90% are filled by the operator live run"
  artifacts:
    - path: "internal/eval/harness_swarm_e2e_test.go"
      provides: "TestSwarmE2E — live parent agent + swarm_spawn + mail/WhatsApp MCP mounts, dual gate (ground-truth + judge ≥90%)"
      contains: "cot_eval"
    - path: "internal/eval/dataset_cot_eval.go"
      provides: "swarm scenario(s): natural prompt (no 'swarm'), expected facts, mail/WhatsApp read-back, timing, + a control prompt asserting no over-spawn"
      contains: "swarm"
    - path: "docs/aura-quality-snapshot.md"
      provides: "Phase 9 swarm E2E row — placeholder at commit time, real numbers (worker count, timing ratio, judge average, read-back pass) after the operator run"
      contains: "swarm"
  key_links:
    - from: "internal/eval/harness_swarm_e2e_test.go"
      to: "the swarm_spawn tool + mail/whatsapp MCP mounts"
      via: "live parent agent run over the real client with the MCP-mounted registry"
      pattern: "swarm_spawn|tool_use"
    - from: "internal/eval/harness_swarm_e2e_test.go"
      to: "docs/aura-quality-snapshot.md"
      via: "scored-report write at gate end"
      pattern: "quality-snapshot|aura-.*-eval"
---

<objective>
The Gate-3 live validation surface (SC#5, D-22): a dual-gate swarm E2E extending the shipped `internal/eval` `cot_eval` harness. A NATURAL prompt (with NO mention of "swarm") drives the REAL parent `LlmAgent` over the REAL openai_compat client; the model must choose to parallelize on its own (the judge scores that choice). The mail + WhatsApp MCP servers are mounted (the first production exercise of D-19/D-20/D-21); E2E sends go ONLY to the user's own account/number; ground truth = read-back via the same MCP server.

Dual gate (D-22):
- **Hard floor (deterministic ground-truth assertions):** N workers spawned (visible as `tool_use` blocks); expected facts present in the aggregated answer; the WhatsApp/mail message exists on read-back via MCP (self-chat JID duality — assert the right JID per direction); wall-clock < 1.5× single-worker.
- **Judge rubric ≥90% average:** autonomous parallelization (no "swarm" in the prompt), sub-answer correctness, aggregation quality, and NO over-spawn on a simple control prompt.

This tier is OPENROUTER-gated + operator-run, NOT CI (the ONE legitimate skip — like every shipped live tier; no-skip-as-green: CI gates stay on unit/property/race/goleak). Numbers land in `docs/aura-quality-snapshot.md`. This plan touches NO `internal/swarm/` or tool/boot code — it only extends the eval harness + the snapshot doc.

Purpose: prove CAP-03 end-to-end against a live model with real MCP read-back. Output: the swarm scenario(s) + judge rubric + the `TestSwarmE2E` harness + the snapshot row.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-swarm-minimal/09-CONTEXT.md
@.planning/phases/09-swarm-minimal/09-RESEARCH.md
@.planning/phases/09-swarm-minimal/09-PATTERNS.md
@internal/eval/harness_cot_eval_test.go
@internal/eval/dataset_cot_eval.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: swarm scenario(s) + judge rubric in the cot_eval dataset/scoring/judge</name>
  <files>internal/eval/dataset_cot_eval.go, internal/eval/scoring_cot_eval.go, internal/eval/judge_cot_eval.go</files>
  <read_first>
    - internal/eval/dataset_cot_eval.go (the `scenario` struct + `dimension` consts + the 12-scenario list) — the shape to extend with a swarm scenario + a no-over-spawn control scenario.
    - internal/eval/scoring_cot_eval.go — how a scenario's expected signals are asserted (the hard-floor pattern to mirror for worker-count / expected-facts / read-back / timing).
    - internal/eval/judge_cot_eval.go — the LLM-judge invocation + rubric shape (the ≥90% dimensions: autonomous parallelization, sub-answer correctness, aggregation quality, no-over-spawn).
    - internal/eval/capture_cot_eval.go — how tool_use blocks / metrics are captured per run (worker-count = count of swarm_spawn child reports / tool_use blocks).
    - .planning/spikes/001-*/README.md + 002-*/README.md — the mail `search_emails {query}` shape + the WhatsApp self-chat JID duality (bridge-sent rows under `<phone>@s.whatsapp.net`, phone-sent under `<lid>@lid`) for the read-back assertions.
    - .planning/phases/09-swarm-minimal/09-PATTERNS.md §"Live E2E (cot_eval build tag)" excerpt + 09-RESEARCH.md §"Validation Architecture" SC#5 row + D-22 + Environment Availability.
  </read_first>
  <action>
    Extend `internal/eval/dataset_cot_eval.go`: add a `swarm` scenario whose prompt is NATURAL (no "swarm"/"parallel" — e.g. a multi-part research-and-act task with ≥2 independent self-contained subtasks, at least one of which sends a self-mail or self-WhatsApp message) plus the expected signals (a list of facts the aggregated answer must contain; the mail/WhatsApp content to read back; an expected minimum worker count ≥2; a timing budget < 1.5× single-worker). Add a `control` scenario whose prompt is a SIMPLE single task that MUST NOT trigger swarm_spawn (no-over-spawn). Add any new `dimension` consts the judge needs (e.g. dimAutonomousParallelization, dimSubAnswerCorrectness, dimAggregationQuality, dimNoOverSpawn). In `scoring_cot_eval.go`: add the deterministic hard-floor assertions for the swarm scenario — worker count ≥ expected (from captured tool_use / child reports), expected facts present in the final prose, and a read-back hook (call the mounted mail `search_emails {query}` / WhatsApp `list_messages` and assert the self-message exists at the correct JID per direction), plus wall-clock < 1.5× a single-worker baseline run. In `judge_cot_eval.go`: add the swarm rubric (the 4 D-22 dimensions, gate fixed ≥90% average; exact weights are Claude's Discretion). Keep all swarm scenarios behind the `cot_eval` build tag (they live in files already tagged). Do NOT add the swarm scenario to any CI path.
  </action>
  <verify>
    <automated>cd D:/Aura && go vet -tags cot_eval ./internal/eval/ && go build -tags cot_eval ./internal/eval/ && grep -c "swarm" internal/eval/dataset_cot_eval.go && echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - `go build -tags cot_eval ./internal/eval/` and `go vet -tags cot_eval ./internal/eval/` exit 0
    - `dataset_cot_eval.go` contains a swarm scenario with a NATURAL prompt that does NOT contain the substring "swarm" (assert: the prompt string has no "swarm")
    - A control scenario exists asserting swarm_spawn is NOT called on a simple single task (no-over-spawn dimension)
    - `judge_cot_eval.go` declares the 4 D-22 rubric dimensions and a ≥90% average gate
    - `scoring_cot_eval.go` asserts worker count ≥2, expected facts present, and a mail/WhatsApp read-back via the mounted MCP server (the right JID per direction)
    - The scenario files remain `//go:build cot_eval`-tagged (not reachable from CI)
  </acceptance_criteria>
  <done>The swarm + control scenarios, hard-floor assertions, and the ≥90% judge rubric exist behind the cot_eval tag.</done>
</task>

<task type="auto" gate="non-blocking">
  <name>Task 2: TestSwarmE2E harness (live, operator-run) + quality-snapshot row (placeholder at commit, numbers after the live run)</name>
  <files>internal/eval/harness_swarm_e2e_test.go, docs/aura-quality-snapshot.md</files>
  <read_first>
    - internal/eval/harness_cot_eval_test.go (the `//go:build cot_eval` TestCoTEval entry, the OPENROUTER_API_KEY gate + the one legitimate `t.Skip`, the real `agent.LlmAgent` + `openai_compat` construction, the scored-report write) — the harness shape to mirror for TestSwarmE2E.
    - cmd/aura/main.go `buildRegistryWithMCP` + `buildBaseRegistry` (09-03/09-05) — how the parent registry is built WITH swarm_spawn + the mail/WhatsApp MCP mounts (the harness must construct the same registry).
    - internal/swarm/runner_adapter.go (09-05) — the adapter the harness wires so swarm_spawn.Execute has a live runner.
    - docs/aura-quality-snapshot.md (the §References + the living-matrix format) — where to add the Phase 9 row and the placeholder convention used by prior phases.
    - .planning/phases/09-swarm-minimal/09-VALIDATION.md §"Manual-Only Verifications" (the bridge bring-up + REST :8080 health-check + invocation steps) + 09-RESEARCH.md Environment Availability (bridge health = 405 on GET /api/send).
  </read_first>
  <action>
    Create `internal/eval/harness_swarm_e2e_test.go` (`//go:build cot_eval`): `TestSwarmE2E` gated on `OPENROUTER_API_KEY` (the one legitimate `t.Skip` locally when unset; under `$CI` it must `t.Fatal`-or-skip per the tier's operator-run design — mirror the existing harness gate exactly, do NOT add it to CI). The test: constructs the REAL parent `agent.LlmAgent` over the REAL `openai_compat` client with a registry that includes `swarm_spawn` (live adapter) + the mail + WhatsApp MCP mounts (allowlisted, Deferred — via the same boot path as `aura chat`); runs the natural swarm scenario; applies the hard-floor assertions (worker count, expected facts, mail/WhatsApp read-back at the correct JID, wall-clock < 1.5× single-worker) and the judge rubric (≥90% average); runs the control scenario asserting no over-spawn; writes the scored numbers to a `docs/aura-*-swarm-eval-*.md` report (docs/, never /tmp — CLAUDE.md). At CODE-COMMIT time, add a Phase 9 swarm row to `docs/aura-quality-snapshot.md` as a PLACEHOLDER (the columns present — worker count, timing ratio, judge average, mail+WhatsApp read-back pass/fail — with TBD/pending values + the bridge bring-up note: fork chetto1983/whatsapp-mcp@6de1dcd, REST :8080 health = 405 on GET /api/send), following the placeholder convention prior phases use. The OPERATOR live run then replaces the TBDs with the real observed numbers. Document the operator invocation in the test file header: bring up the bridge + health-check, `set -a; . ./.env; set +a; export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"; go test -tags cot_eval -run TestSwarmE2E -timeout 600s -v ./internal/eval/`.
  </action>
  <verify>
    <human-check>Operator brings up the whatsmeow bridge (fork @6de1dcd) + health-checks REST :8080 (405 on GET /api/send), sources .env (OPENROUTER_API_KEY + mail/WhatsApp creds via managed config), and runs `go test -tags cot_eval -run TestSwarmE2E -timeout 600s -v ./internal/eval/`. PASS requires: ≥2 workers spawned via tool_use on the natural prompt, expected facts present, the self-mail AND self-WhatsApp messages found on read-back via MCP, wall-clock < 1.5× single-worker, judge ≥90% average, and the control prompt did NOT trigger swarm_spawn. The TBD placeholder row in docs/aura-quality-snapshot.md is replaced with the real observed numbers.</human-check>
    <automated>cd D:/Aura && go build -tags cot_eval ./internal/eval/ && grep -c "swarm" docs/aura-quality-snapshot.md && echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    Compile-time (verifiable in the code commit, no operator run):
    - `go build -tags cot_eval ./internal/eval/` exits 0 (the harness compiles)
    - `internal/eval/harness_swarm_e2e_test.go` is `//go:build cot_eval`-tagged and gated on OPENROUTER_API_KEY (the one legitimate skip; NOT in any CI path)
    - `docs/aura-quality-snapshot.md` contains a Phase 9 swarm row (placeholder/TBD values acceptable at commit time) with the columns worker-count / timing-ratio / judge-average / read-back + the bridge bring-up note
    - The test header documents the exact operator invocation (bridge health-check + .env + PATH + go test command)
    Operator-run (captured under the human-check, filled after the live run):
    - The operator live run yields: ≥2 workers via tool_use on the natural prompt, expected facts present, self-mail + self-WhatsApp read-back via MCP, wall-clock < 1.5× single-worker, judge ≥90% average, control prompt no-over-spawn
    - The placeholder/TBD row in docs/aura-quality-snapshot.md is updated with the real observed numbers post-run
  </acceptance_criteria>
  <done>The live dual-gate swarm E2E exists and compiles under the cot_eval tag with a placeholder snapshot row at commit; the operator run records the real Gate-3 numbers in the quality snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live model output → mail/WhatsApp send | The model, during the E2E, sends real messages via the mounted MCP servers (scoped to self only). |
| child reports → parent aggregation | Worker prose (potentially injection-laden) is aggregated by the parent into the final answer + the judge prompt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Spoofing / unintended send | E2E mail/WhatsApp sends reaching a non-self recipient | mitigate | Task 1/2: scenarios send ONLY to the user's own account/number (D-19); read-back targets the self-chat JID; the D-20 allowlist already restricts to send_email/send_message (no broadcast/contact-enumeration footguns beyond the v1 set). |
| T-09-16 | Information disclosure | Gmail app password / creds leaking into the scored report or judge prompt | mitigate | inherited: the cot_eval harness has a release-blocking `secret_redaction` dimension (`scoring_cot_eval.go`); creds live in managed-config Env only, never in the prompt or report. |
| T-09-17 | Tampering (prompt injection) | a child report injecting instructions into the parent aggregation | accept | v1: workers are headless executors producing text the parent reads as data (D-06); the parent's own guardrails apply; full report-sanitization is a v2 concern (no surveyed v1 system sanitizes sibling reports) — documented, low-value single-user target. |
| T-09-SC | Tampering | MCP server source legitimacy in the live run | mitigate | the mail-mcp + whatsapp fork are spike-001/002 VALIDATED, out-of-process, the user's own fork (RESEARCH Package Legitimacy Audit); no go.mod package added. |
</threat_model>

<verification>
- `go build -tags cot_eval ./internal/eval/` + `go vet -tags cot_eval ./internal/eval/` clean (harness + scenarios compile); the placeholder snapshot row is present.
- Operator live run (human-check): dual gate passes (ground-truth read-back + judge ≥90%); the TBD numbers in docs/aura-quality-snapshot.md are replaced with real values.
- CI unaffected (tier is cot_eval-tagged + OPENROUTER-gated; no-skip-as-green — CI gates stay on unit/property/race/goleak).
</verification>

<success_criteria>
- The live dual-gate swarm E2E (natural prompt, no "swarm") exists under the cot_eval tag.
- Hard floor: ≥2 workers via tool_use, expected facts, mail+WhatsApp read-back, timing < 1.5×.
- Judge rubric ≥90% average; control prompt no-over-spawn.
- A placeholder snapshot row lands at commit; real numbers recorded in docs/aura-quality-snapshot.md after the operator run.
</success_criteria>

<output>
Create `.planning/phases/09-swarm-minimal/09-06-SUMMARY.md` when done.
</output>
