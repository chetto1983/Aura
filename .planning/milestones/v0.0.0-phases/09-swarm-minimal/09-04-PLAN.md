---
phase: 09-swarm-minimal
plan: 04
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - internal/agent/tools/ask_user.go
  - internal/agent/tools/ask_user_test.go
  - internal/agent/event.go
  - internal/agent/llm_agent_pause.go
  - internal/askuser/store.go
  - internal/askuser/store_integration_test.go
  - internal/runner/runner_persist.go
  - internal/runner/runner_persist_test.go
autonomous: true
requirements: [CAP-03]
must_haves:
  truths:
    - "ask_user Spec accepts optional proxied_from_child_id + proxied_tool_call_id args the model MAY fill when relaying a child question (D-05)"
    - "A proxied ask_user pause persists proxied_from_child_id + proxied_tool_call_id into aura.paused_states (NULL for direct calls) (D-05)"
    - "The 3-layer plumb is complete: ask_user args → AwaitingInput Event → askuser.InsertParams/Insert → persistPause (RESEARCH Pitfall 3 — not a one-line populate)"
  artifacts:
    - path: "internal/agent/tools/ask_user.go"
      provides: "askUserArgs + Spec params + ErrAwaitingUserInput carry proxied_from_child_id/proxied_tool_call_id (optional)"
      contains: "proxied_from_child_id"
    - path: "internal/agent/event.go"
      provides: "AwaitingInput struct gains ProxiedFromChildID + ProxiedToolCallID"
      contains: "ProxiedFromChildID"
    - path: "internal/askuser/store.go"
      provides: "InsertParams gains ProxiedFromChildID *string + ProxiedToolCallID; Insert sets them on sqlc params"
      contains: "ProxiedFromChildID"
    - path: "internal/runner/runner_persist.go"
      provides: "persistPause reads ai.ProxiedFromChildID/ProxiedToolCallID off the Event and passes to InsertParams"
      contains: "ProxiedFromChildID"
  key_links:
    - from: "internal/agent/tools/ask_user.go askUserArgs"
      to: "internal/agent/event.go AwaitingInput"
      via: "ErrAwaitingUserInput → pauseEvent projection"
      pattern: "ProxiedFromChildID|proxied_from_child_id"
    - from: "internal/runner/runner_persist.go persistPause"
      to: "aura.paused_states.proxied_from_child_id"
      via: "askuser.Store.Insert → sqlc.InsertPausedStateParams"
      pattern: "ProxiedFromChildID"
---

<objective>
Wire D-05 proxied-id pause relay end-to-end. This is the 3-layer plumb RESEARCH Pitfall 3 flags as REAL work (not a one-line populate): the SQL columns + sqlc params already exist (`paused_states.sql.go:88-89` `ProxiedFromChildID`/`ProxiedToolCallID`), but the domain layer DROPS them — `askUserArgs` has no proxied fields, `AwaitingInput` has none, `askuser.InsertParams`/`Store.Insert` never set them, and `persistPause` never passes them.

When the parent LLM relays a child's `needs_user_input` report via its OWN `ask_user`, it MAY fill `proxied_from_child_id` (the child's id) + `proxied_tool_call_id` (the child's tool-call id, both ground-truth from the swarm report). Those stamp into `aura.paused_states` so a future resume can map the answer back to the originating child goal. Best-effort, model-discretionary — the existing `required: ["question","kind"]` stays; direct (non-proxied) pauses persist NULL.

This plan touches NO `internal/swarm/` file and adds no new tool. Output: the 3-layer plumb (ask_user Spec/args → AwaitingInput Event → persistPause → Insert) + a fake-store unit test + a db_integration test.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-swarm-minimal/09-CONTEXT.md
@.planning/phases/09-swarm-minimal/09-RESEARCH.md
@.planning/phases/09-swarm-minimal/09-PATTERNS.md
@internal/askuser/store.go
@internal/runner/runner_persist.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ask_user Spec/args + AwaitingInput Event carry the optional proxied_* ids</name>
  <files>internal/agent/tools/ask_user.go, internal/agent/tools/ask_user_test.go, internal/agent/event.go, internal/agent/llm_agent_pause.go</files>
  <read_first>
    - internal/agent/tools/ask_user.go (lines ~80-108 `askUserArgs` + `Spec()` JSON params; ~62-75 `ErrAwaitingUserInput` struct; ~137 the `&ErrAwaitingUserInput{...}` construction in Execute) — the 3 sites to extend.
    - internal/agent/event.go (lines ~81-95 `AwaitingInput` struct + its doc-comment at ~74-80 that ALREADY anticipates Phase 9 populating proxied_*) — add the 2 fields.
    - internal/agent/llm_agent_pause.go (lines ~104-117 `pauseEvent` — the projection from `ErrAwaitingUserInput` onto `Actions.AwaitingInput`) — carry the 2 new fields.
    - internal/agent/tools/ask_user_test.go — existing arg-parse test patterns to extend for the optional proxied fields.
    - .planning/phases/09-swarm-minimal/09-PATTERNS.md §ask_user.go EDIT excerpt + 09-RESEARCH.md Pattern 4 (the 3-layer plumb, layer 1).
  </read_first>
  <behavior>
    - ask_user JSON `{"question":"q","kind":"clarification","proxied_from_child_id":"<uuid>","proxied_tool_call_id":"tc1"}` parses both proxied fields into askUserArgs.
    - ask_user JSON without the proxied fields → both empty (back-compat; required stays ["question","kind"]).
    - The proxied fields flow onto ErrAwaitingUserInput and then onto the Actions.AwaitingInput Event via pauseEvent.
  </behavior>
  <action>
    In `internal/agent/tools/ask_user.go`: add `ProxiedFromChildID string \`json:"proxied_from_child_id"\`` and `ProxiedToolCallID string \`json:"proxied_tool_call_id"\`` to `askUserArgs`; add both as optional `"string"` properties to the Spec JSON `params` (do NOT add them to `required`) with descriptions noting they are model-discretionary, filled only when relaying a child's `needs_user_input` report (the ids are ground-truth from the swarm report); add `ProxiedFromChildID string` + `ProxiedToolCallID string` to the `ErrAwaitingUserInput` struct and set them in the `&ErrAwaitingUserInput{...}` construction in Execute from the parsed args. In `internal/agent/event.go`: add `ProxiedFromChildID string \`json:"proxied_from_child_id,omitempty"\`` + `ProxiedToolCallID string \`json:"proxied_tool_call_id,omitempty"\`` to the `AwaitingInput` struct (the doc-comment at ~74-80 already anticipates this). In `internal/agent/llm_agent_pause.go` `pauseEvent`: carry `pause.ProxiedFromChildID` + `pause.ProxiedToolCallID` onto the `&AwaitingInput{...}` projection. Extend `ask_user_test.go`: assert the proxied fields parse when present and are empty when absent (required stays ["question","kind"]).
  </action>
  <verify>
    <automated>cd D:/Aura && go build ./... && go test ./internal/agent/tools/ ./internal/agent/ -run 'TestAskUser|TestPauseEvent|TestAwaitingInput' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "proxied_from_child_id" internal/agent/tools/ask_user.go` ≥2 (askUserArgs tag + Spec param)
    - `grep -c "ProxiedFromChildID" internal/agent/event.go` ≥1 AND `grep -c "ProxiedFromChildID" internal/agent/llm_agent_pause.go` ≥1
    - `required` in the ask_user Spec JSON is still exactly `["question", "kind"]` (proxied fields NOT required)
    - ask_user_test asserts a JSON payload with proxied fields parses both, and a payload without them leaves both empty
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>The proxied ids flow from ask_user args through ErrAwaitingUserInput to the AwaitingInput Event; they are optional and back-compat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: askuser.InsertParams/Insert + persistPause stamp proxied_* into paused_states</name>
  <files>internal/askuser/store.go, internal/askuser/store_integration_test.go, internal/runner/runner_persist.go, internal/runner/runner_persist_test.go</files>
  <read_first>
    - internal/askuser/store.go (lines ~86-95 `InsertParams`, ~98 the doc-comment "proxied_* stay NULL ... Phase 9 populates", ~108-130 `Insert` building `sqlc.InsertPausedStateParams`, ~330-340 `parseUUID` for the pgtype boundary) — add the 2 fields + set them.
    - internal/db/sqlc/paused_states.sql.go (lines ~88-89 `ProxiedFromChildID pgtype.UUID` + `ProxiedToolCallID pgtype.Text`) — the sqlc param types to convert into.
    - internal/runner/runner_persist.go (lines ~120-148 `persistPause`, the SOLE paused_states writer) — read ai.ProxiedFromChildID/ProxiedToolCallID off the Event, pass to InsertParams.
    - internal/askuser/store_integration_test.go — the db_integration test patterns (build tag, DSN env) to extend for the proxied round-trip.
    - internal/runner/runner_persist_test.go — the fake-store unit test patterns to extend.
    - .planning/phases/09-swarm-minimal/09-PATTERNS.md §store.go/runner_persist.go EDIT excerpt + 09-RESEARCH.md Pattern 4 (layers 2+3) + Pitfall 3.
  </read_first>
  <behavior>
    - InsertParams{ProxiedFromChildID:&uuid, ProxiedToolCallID:"tc1"} → the persisted row has those values; InsertParams with nil/"" proxied → row columns are SQL NULL.
    - persistPause given an AwaitingInput with proxied ids passes them through to Insert (the SOLE writer).
    - A direct (non-proxied) pause still persists with NULL proxied columns (back-compat, existing tests stay green).
  </behavior>
  <action>
    In `internal/askuser/store.go`: add `ProxiedFromChildID *string` (nil → SQL NULL for direct calls) and `ProxiedToolCallID string` to `InsertParams`; in `Insert`, set `arg.ProxiedFromChildID` and `arg.ProxiedToolCallID` on the `sqlc.InsertPausedStateParams` — convert the `*string` child id via the `parseUUID` boundary helper when non-nil (else leave the zero `pgtype.UUID{}` which is `Valid:false` → NULL), and set `ProxiedToolCallID` as `pgtype.Text{String: p.ProxiedToolCallID, Valid: p.ProxiedToolCallID != ""}`. Update the doc-comment at ~98 to note Phase 9 now populates these. In `internal/runner/runner_persist.go` `persistPause`: read `ai.ProxiedFromChildID` (pass `&ai.ProxiedFromChildID` when non-empty, else nil) and `ai.ProxiedToolCallID` off the `*agent.AwaitingInput` and add them to the `askuser.InsertParams{...}` literal. Extend `internal/runner/runner_persist_test.go`: a fake-store unit test asserting persistPause forwards the proxied ids into InsertParams. Extend `internal/askuser/store_integration_test.go` (db_integration tag): TestInsertProxied — insert with proxied ids, read the row back, assert proxied_from_child_id + proxied_tool_call_id persisted; and a direct insert asserts both columns NULL.
  </action>
  <verify>
    <automated>cd D:/Aura && go build ./... && go test ./internal/runner/ -run 'TestPersistPause' && echo unit-OK; echo "integration: run in WSL with stack up — export PATH=\"\$HOME/.local/bin:\$HOME/go/bin:\$PATH\"; derive AURA_DB_URL/AURA_DB_MIGRATE_URL from POSTGRES_PASSWORD; go test -tags db_integration ./internal/askuser/ -run TestInsertProxied"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ProxiedFromChildID" internal/askuser/store.go` ≥2 (InsertParams field + Insert set) AND `grep -c "ProxiedFromChildID" internal/runner/runner_persist.go` ≥1
    - `go test ./internal/runner/ -run TestPersistPause` exits 0 — a fake-store test asserts persistPause forwards proxied ids; the existing direct-pause test still passes
    - `Insert` sets `arg.ProxiedFromChildID` and `arg.ProxiedToolCallID` on `sqlc.InsertPausedStateParams` (grep shows both assignments)
    - A direct (non-proxied) InsertParams persists with `Valid:false` pgtype (NULL) — asserted in the unit and/or integration test
    - The db_integration TestInsertProxied exists with the build tag and round-trips both columns (runs in WSL with stack up; CI db job exercises it — no-skip-as-green)
  </acceptance_criteria>
  <done>persistPause (the SOLE writer) stamps proxied_from_child_id + proxied_tool_call_id into aura.paused_states; direct pauses stay NULL; the round-trip is integration-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent LLM → paused_states | The model fills proxied_from_child_id (a uuid) + proxied_tool_call_id (a string) that get persisted to the DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Input validation / Tampering | malformed proxied_from_child_id (not a uuid) reaching the DB | mitigate | Task 2: the child id is parsed via the `parseUUID` boundary helper (`store.go:334`) — an invalid uuid is rejected at the domain boundary, never reaching the sqlc layer as a raw string; proxied_tool_call_id is a plain text column (no traversal risk — it is not a path). |
| T-09-11 | Information disclosure | a proxied pause leaks a child's internal tool-call id into the user prompt | accept | the tool_call_id is an opaque dispatch id, not a secret; ask_user already forbids secrets in the prompt (existing Description); proxied ids are model-discretionary metadata, not user-facing. |
</threat_model>

<verification>
- `go test ./internal/agent/tools/ ./internal/agent/ ./internal/runner/` green (proxied plumb, unit tier).
- db_integration TestInsertProxied green in WSL with the stack up (proxied round-trip; CI db job runs it).
- `go build ./...` + `go vet ./...` clean.
</verification>

<success_criteria>
- The 3-layer proxied plumb is complete: ask_user args → AwaitingInput Event → InsertParams/Insert → persistPause.
- A proxied pause persists proxied_from_child_id + proxied_tool_call_id; a direct pause persists NULL.
- The round-trip is integration-tested (db_integration), not just compile-checked.
</success_criteria>

<output>
Create `.planning/phases/09-swarm-minimal/09-04-SUMMARY.md` when done.
</output>
