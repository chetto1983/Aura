---
phase: 02-agent-cornerstone
plan: 04
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - internal/agent/agenttest/mocks.go
  - internal/agent/agenttest/mocks_test.go
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "InfiniteToolCallAgent, EmitNThenEscalate, RecordingAgent, and a counting mock all implement agent.Agent and live in the shared internal/agent/agenttest/ package (D-07) — zero inline mock duplication"
    - "agenttest imports internal/agent in ONE direction only; agent never imports agenttest outside _test.go (no import cycle, RESEARCH OQ#2)"
    - "InfiniteToolCallAgent emits the SAME tool call forever (drives the SC#2 budget-exhaustion fixture)"
    - "the counting mock threads the shared *atomic.Int32 so a sub-agent-exposed-as-tool cannot reintroduce the depth³ trap (SC#3 / scout-1 rec #4)"
    - "each mock's Run iter.Seq2 yield discipline (D-22) is exercised by a drain test — no panic, correct termination — before Plan 05/06 depend on it (W5)"
  artifacts:
    - path: "internal/agent/agenttest/mocks.go"
      provides: "Reusable Agent mocks for Plan 05/06 tests + CLI dry-run + Phase 3/9"
      contains: "InfiniteToolCallAgent"
      min_lines: 60
    - path: "internal/agent/agenttest/mocks_test.go"
      provides: "iter.Seq2 yield-discipline drain test for each mock (D-22 runtime panics)"
      contains: "TestMocks"
  key_links:
    - from: "internal/agent/agenttest/mocks.go"
      to: "internal/agent.Agent"
      via: "all mocks implement the Agent interface (one-direction import)"
      pattern: "agent\\.Agent"
---

<objective>
Build the shared mock-agent package (D-07) that Plan 05 (Sequential/Loop), Plan 06 (Parallel), the CLI dry-run (Plan 07), and future Phase 3/9 all reuse — eliminating mock duplication per CLAUDE.md "reusable code". These mocks ARE the test fixtures for SC#2 (InfiniteToolCallAgent emits the same tool call forever) and SC#3 (a counting mock proves the shared counter holds even when a sub-agent is exposed as a tool).

Purpose: Without a shared mock package, every workflow test re-implements `Agent` stubs — drift and duplication. Placing them in `internal/agent/agenttest/` (importing `internal/agent` one-direction) is the standard Go test-helper pattern with no import cycle (RESEARCH OQ#2 confirmed safe). A drain test (W5) exercises each mock's iter.Seq2 yield discipline NOW so D-22 runtime panics surface here, not in Plan 05/06.
Output: `internal/agent/agenttest/mocks.go` with the four named mocks + `mocks_test.go` drain test.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-cornerstone/02-CONTEXT.md
@.planning/phases/02-agent-cornerstone/02-RESEARCH.md
@.planning/phases/02-agent-cornerstone/02-PATTERNS.md
@.planning/phases/02-agent-cornerstone/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable Agent mocks in internal/agent/agenttest/ (D-07)</name>
  <files>internal/agent/agenttest/mocks.go</files>
  <read_first>
    - internal/agent/tools/text_response.go:12-18 (the minimal-interface-impl idiom: tiny struct, methods returning canned values — mirror this per mock)
    - internal/agent/agent.go + internal/agent/event.go (from Plan 02 — the Agent interface + Event/Actions/LLMResponse shape the mocks must satisfy and emit)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Wave 0 Gaps (the named mocks: InfiniteToolCallAgent, EmitNThenEscalate, RecordingAgent, counting mock for SC#3) + § Common Pitfalls › Pitfall 3 (sub-agent-exposed-as-tool must thread the shared *atomic.Int32 — the counting mock proves it) + § Open Questions OQ#2 (import direction)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § agenttest/mocks.go (text_response.go shape; one-direction import)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-07
  </read_first>
  <action>
    Create package `agenttest` (path `internal/agent/agenttest/`) importing `internal/agent` (and `internal/llm` for ToolCall). Implement four mocks, each satisfying `agent.Agent`: (1) `InfiniteToolCallAgent` — its `Run` yields an Event carrying the SAME `llm.ToolCall` (fixed name+args) on every iteration, forever (no natural termination — the LoopAgent's Budget must stop it; this is the SC#2 fixture). (2) `EmitNThenEscalate` — emits N normal Events then one Event with `Actions.Escalate=true` (for escalate-propagation tests). (3) `RecordingAgent` — records every InvocationContext/Event it sees so tests can assert order and branch labels. (4) a counting mock (e.g. `CountingAgent`) — calls `ic.Budget.ConsumeStep()` and increments a shared external counter, used by the SC#3 depth test to prove total consumption ≤25; crucially, if a future wrapper exposes it as a tool it MUST thread the same `*atomic.Int32` (document the invariant; do NOT call `NewBudgetFromEnv` inside any mock — that would reintroduce the depth³ trap, Pitfall 3). Follow the `text_response.go` minimal-impl shape and repo doc-comment style. `agenttest` must NOT be imported by `internal/agent` outside `_test.go` (one-direction). Mocks use `iter.Seq2` yield discipline (D-22: `if !yield(ev,err){ return }`, never bare yield). For InfiniteToolCallAgent (which never terminates on its own), the yield-guard return path is the ONLY exit — make sure the `if !yield(...){ return }` is present so a consumer break does not panic (the drain test in Task 2 exercises this).
  </action>
  <verify>
    <automated>go build ./internal/agent/agenttest/ && go vet ./internal/agent/agenttest/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/agenttest/mocks.go` defines `InfiniteToolCallAgent`, `EmitNThenEscalate`, `RecordingAgent`, and a counting mock — all implementing `agent.Agent` (compile-asserts `var _ agent.Agent = (*agenttest.InfiniteToolCallAgent)(nil)` etc. present or build proves it)
    - `go build ./internal/agent/agenttest/` exits 0 (no import cycle)
    - `grep -q 'NewBudgetFromEnv' internal/agent/agenttest/mocks.go` returns NOTHING (no mock forks a fresh budget — Pitfall 3 guard)
    - InfiniteToolCallAgent yields the same tool call on repeated Run iterations (verified by the Task 2 drain test + a Plan 05 LoopAgent test)
    - `go vet ./internal/agent/agenttest/` clean
  </acceptance_criteria>
  <done>Four reusable Agent mocks satisfying the interface, one-direction import, no fresh-budget anti-pattern, iter.Seq2 yield-disciplined.</done>
</task>

<task type="auto">
  <name>Task 2: mocks_test.go — iter.Seq2 yield-discipline drain test (W5)</name>
  <files>internal/agent/agenttest/mocks_test.go</files>
  <read_first>
    - internal/agent/agenttest/mocks.go (Task 1 — the four mocks under test)
    - internal/agent/agent.go + internal/agent/budget.go (InvocationContext + NewBudgetFromEnv/SetMaxSteps to seed a small test budget)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-22 (iter.Seq2 yield discipline — bare-yield-after-false panics are RUNTIME-only, must be exercised, not just build/vet)
    - internal/config/config_test.go:10-50 (table-test idiom)
  </read_first>
  <behavior>
    - draining each mock's Run for 3 steps under a small test Budget does not panic and terminates correctly
    - InfiniteToolCallAgent under a 3-step budget yields the same tool call each step; a consumer that breaks after 1 event does NOT panic (D-22 footgun 2 guard path exercised)
    - EmitNThenEscalate yields N events then the escalate event then stops
    - CountingAgent decrements the injected shared *atomic.Int32 (not a fresh budget)
  </behavior>
  <action>
    Create mocks_test.go (package `agenttest` or `agenttest_test`): a table-driven `TestMocks_RunYieldDiscipline` that, for each mock, constructs an `InvocationContext` with a Budget seeded via `SetMaxSteps(3)`, drains `mock.Run(ic)` with the two-var `for ev, err := range` form for up to 3 steps, and asserts (a) no panic (the test itself fails loud on panic — a `defer func(){ if r:=recover(); r!=nil { t.Fatalf(...) } }()` or rely on Go's test-panic-fails behavior), (b) the expected termination per mock (InfiniteToolCallAgent must be bounded by the consumer break at 3; EmitNThenEscalate terminates after escalate; RecordingAgent records the events; CountingAgent consumes from the shared counter). Add a dedicated `TestMocks_InfiniteToolCall_BreakAfterOne_NoPanic` that breaks the range after the first event and asserts no panic (D-22 footgun 2 — the yield-after-false guard). Keep the test fast and goroutine-free (no goleak needed here; mocks are synchronous).
  </action>
  <verify>
    <automated>go test ./internal/agent/agenttest/ -run 'TestMocks' && go vet ./internal/agent/agenttest/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/agenttest/mocks_test.go` contains `TestMocks_RunYieldDiscipline` draining each mock for 3 steps with no panic and correct termination
    - `TestMocks_InfiniteToolCall_BreakAfterOne_NoPanic` passes (consumer break after 1 event does not panic — D-22 yield guard exercised at runtime)
    - CountingAgent test confirms it consumes from the injected shared counter (not a fresh NewBudgetFromEnv)
    - `go test ./internal/agent/agenttest/` exits 0
  </acceptance_criteria>
  <done>Each mock's iter.Seq2 yield discipline exercised at runtime (W5) — drain-3-steps + break-after-one both panic-free — before Plan 05/06 build on the fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → shared counter | mocks consume `ic.Budget` — a mock that forks a fresh budget silently breaks the shared-counter guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Denial of Service (latent) | a mock that exposes a sub-agent as a tool with a fresh budget reintroduces depth³ | mitigate | counting mock threads the shared `*atomic.Int32`; no mock calls `NewBudgetFromEnv` (Pitfall 3); SC#3 test in Plan 06 asserts total ≤25 even via tool-wrapped sub-agent |
| T-02-10 | Tampering | inline mock duplication drifting across test files | accept→mitigate | single shared `agenttest` package (D-07) — one source of truth, zero duplication |
</threat_model>

<verification>
- `go build ./internal/agent/agenttest/` clean (no import cycle, one-direction)
- `go test ./internal/agent/agenttest/ -run TestMocks` green (W5 yield-discipline drain test)
- `go vet ./internal/agent/agenttest/` clean
- `bash scripts/check-file-size.sh` clean (mocks.go ~140 LOC, mocks_test.go ≤120 — both ≤600)
- Mocks further exercised by Plan 05/06 tests (their pass is the integration validation)
</verification>

<success_criteria>
- Four reusable mocks (`InfiniteToolCallAgent`, `EmitNThenEscalate`, `RecordingAgent`, counting mock) implement `agent.Agent` (D-07)
- One-direction import; no `NewBudgetFromEnv` inside any mock (Pitfall 3 guard)
- Each mock's iter.Seq2 yield discipline exercised by mocks_test.go (W5: drain-3 + break-after-one, no panic)
- SC#2 fixture (same-tool-call-forever) and SC#3 fixture (shared-counter counting mock) ready for Plan 05/06
</success_criteria>

<output>
Create `.planning/phases/02-agent-cornerstone/02-04-SUMMARY.md` when done
</output>
</output>
