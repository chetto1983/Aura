---
phase: 02-agent-cornerstone
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - cmd/aura/agent.go
  - cmd/aura/agent_test.go
  - cmd/aura/main.go
  - internal/agent/loop.go
  - scripts/loop_budget_smoke.sh
  - .env.example
  - .planning/codebase/STRUCTURE.md
  - prd.md
  - THIRD_PARTY_NOTICES.md
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "aura agent dry-run --request-id auto emits N Event JSON lines, every line carries a UUIDv7 request_id matching the v7 regex, all lines share the same value (SC#4)"
    - "aura agent dry-run --request-id <uuid> reproduces that request_id verbatim across runs (SC#4)"
    - "CLI flag precedence is CLI flag (non -1) > env > builtin default 25/300/3 (D-06 sentinel -1 fallthrough)"
    - "scripts/loop_budget_smoke.sh exits 0, stdout has exactly 26 Event JSON lines, and greps limit_hit:max_steps (SC#2)"
    - "internal/agent/loop.go is deleted; cmd/aura/main.go no longer dispatches case chat / chatOnce / stubClient and now dispatches case agent (SPEC boundary)"
    - "phase-close coverage across internal/agent/... + internal/canonicaljson/ + cmd/aura/ is ≥85% measured (CLAUDE.md hard floor, overrides SPEC ≥75%) — gated by a real go test -cover assertion that fails below 85% (B4)"
  artifacts:
    - path: "cmd/aura/agent.go"
      provides: "aura agent dry-run subcommand (SC#4)"
      contains: "dry-run"
    - path: "scripts/loop_budget_smoke.sh"
      provides: "SC#2 fixture asserting 26 lines + grep limit_hit:max_steps"
      contains: "loop_budget_smoke"
  key_links:
    - from: "cmd/aura/agent.go"
      to: "internal/agent/workflow.NewLoop + internal/agent/agenttest.InfiniteToolCallAgent"
      via: "builds a mock LoopAgent, iterates Run, prints one Event per JSON line"
      pattern: "NewLoop|InfiniteToolCallAgent"
    - from: "cmd/aura/main.go"
      to: "runAgent"
      via: "switch case \"agent\""
      pattern: "case \"agent\""
---

<objective>
Wire the user-visible entry point and prove the phase end-to-end: the `aura agent dry-run` subcommand (SC#4 — UUIDv7 request_id on every Event, CLI>env>default precedence D-06), the SC#2 smoke script, the destructive cleanup the SPEC mandates (delete `internal/agent/loop.go`, strip `case "chat"`/`chatOnce`/`stubClient` from `main.go`, add `case "agent"`, append the env vars to `.env.example`, fix the dangling README/STRUCTURE reference), the A1-A7 truth-source sync + adk-go attribution (verified PRD-first), AND the phase-close 85% coverage gate (B4).

Purpose: This is the demonstrable cornerstone. `aura agent dry-run` is the operator-facing proof that the Budget tree, the workflow agents, and the OTel-compatible trace IDs all work together. The smoke script is the CI-grep contract for SC#2. The cleanup makes the substitution atomic per the SPEC. The PRD amendments (A1-A7) precede the code per CLAUDE.md PRD-first discipline (W9). The 85% coverage gate is the CLAUDE.md hard floor (overrides SPEC's ≥75%) and must FAIL the phase close below 85% (B4).
Output: `prd.md`/`THIRD_PARTY_NOTICES.md` (verified PRD-first) + `cmd/aura/{agent,agent_test}.go` + `main.go` diff + `loop.go` deletion + `scripts/loop_budget_smoke.sh` + `.env.example` + STRUCTURE.md fix + the 85% coverage gate.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-cornerstone/02-SPEC.md
@.planning/phases/02-agent-cornerstone/02-CONTEXT.md
@.planning/phases/02-agent-cornerstone/02-RESEARCH.md
@.planning/phases/02-agent-cornerstone/02-PATTERNS.md
@.planning/phases/02-agent-cornerstone/02-05-SUMMARY.md
@.planning/phases/02-agent-cornerstone/02-06-SUMMARY.md
@cmd/aura/main.go
@cmd/aura/db.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: PRD-first — verify A1-A7 truth-source sync + adk-go attribution (W9: runs BEFORE the code task)</name>
  <files>prd.md, THIRD_PARTY_NOTICES.md</files>
  <read_first>
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md § PRD-Amendments Required (A1–A7, the exact corrections: A1 exported structs; A2 two-tier dedup + period-2 + exempt; A3 canonicaljson rescope drop RFC-8785; A4 8-byte SpanID + MessageID/ToolCallID/ThreadID; A5 soft cap + decrement-then-check + wallclock-via-deadline + AURA_LOOP_NODE_TIMEOUT_SEC; A6 uuid is a new direct dep + errgroup already present; A7 the four new env vars)
    - CLAUDE.md § PRD-first principle + § Commit discipline (the PRD-amendment commit must come BEFORE/WITH the code commit — W9)
    - prd.md § Slice 0.9 (the section to amend) + § Caps & Limits env catalog (where A7 env vars are catalogued)
    - THIRD_PARTY_NOTICES.md (google/adk-go Apache-2.0 attribution)
  </read_first>
  <action>
    W9 — THIS IS TASK 1 (PRD-FIRST): the A1-A7 amendments + adk-go attribution land/are-verified BEFORE the code in Task 2, honoring CLAUDE.md PRD-first discipline. If a prior Gate-0 amendment commit already landed A1-A7, this task VERIFIES they are present and unchanged (and re-applies any that drifted); if they are not yet in prd.md, APPLY them here, before Task 2 writes any code. For each: A1 — workflow structs are EXPORTED (SequentialAgent etc.), not unexported. A2 — dedup is two-phase/two-tier (sha256(name+canonical_args) pre-check via BeforeToolCall + result-as-veto via AfterToolResult) + period-2 ping-pong + AURA_LOOP_DEDUP_EXEMPT_TOOLS, NOT (name,args,result)-in-hash. A3 — canonical JSON lives in internal/canonicaljson as an internal deterministic serializer; drop RFC-8785 framing. A4 — SpanID/ParentSpanID are 8 bytes (crypto/rand), not UUIDv7; add MessageID/ToolCallID/ThreadID for AG-UI forward-compat. A5 — Budget gains passive per-branch soft cap, decrement-then-check-then-restore ConsumeStep, wallclock via context.WithDeadline + optional AURA_LOOP_NODE_TIMEOUT_SEC. A6 — github.com/google/uuid is a NEW direct dep; golang.org/x/sync errgroup already present. A7 — add AURA_LOOP_DEDUP_EXEMPT_TOOLS, AURA_LOOP_BRANCH_SOFT_FRACTION, AURA_LOOP_NODE_TIMEOUT_SEC, AURA_LOOP_DEDUP_RESULT_CAP to the env catalog. Also ensure `THIRD_PARTY_NOTICES.md` names `google/adk-go`, Apache-2.0, the source URL, and the adapted workflow files. Surgical edits only; touch ONLY prd.md + THIRD_PARTY_NOTICES.md (no .go files).
  </action>
  <verify>
    <automated>grep -q 'AURA_LOOP_DEDUP_EXEMPT_TOOLS' prd.md && grep -q 'AURA_LOOP_BRANCH_SOFT_FRACTION' prd.md && grep -q 'AURA_LOOP_NODE_TIMEOUT_SEC' prd.md && grep -q 'AURA_LOOP_DEDUP_RESULT_CAP' prd.md && grep -q 'google/adk-go' THIRD_PARTY_NOTICES.md</automated>
  </verify>
  <acceptance_criteria>
    - `prd.md` Slice 0.9 reflects exported workflow structs (A1), two-tier dedup (A2), internal/canonicaljson rescope without RFC-8785 (A3), 8-byte SpanID + MessageID/ToolCallID/ThreadID (A4), soft cap + decrement-then-check + wallclock-deadline (A5)
    - `prd.md` notes `github.com/google/uuid` as a new direct dep (A6)
    - `prd.md` env catalog contains all four A7 vars
    - `THIRD_PARTY_NOTICES.md` names `google/adk-go`, Apache-2.0, source URL, adapted workflow files
    - no `.go` files changed by this task (prd.md + THIRD_PARTY_NOTICES.md only) — and this task is sequenced BEFORE the code task (W9)
  </acceptance_criteria>
  <done>A1-A7 truth-source synced + adk-go attribution present, PRD-first (W9 — before any code in this plan), surgical edits only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: aura agent dry-run subcommand (SC#4) + agent_test.go + main.go dispatch wiring + loop.go deletion</name>
  <files>cmd/aura/agent.go, cmd/aura/agent_test.go, cmd/aura/main.go, internal/agent/loop.go</files>
  <read_first>
    - cmd/aura/db.go:19-44 (the `runX(args []string)` dispatcher idiom: sub-subcommand switch, usage-to-stderr + os.Exit(1), exit non-zero on error — copy this shape for runAgent)
    - cmd/aura/main.go:22-50 (the switch to modify: remove `case "chat"`+chatOnce+stubClient L30-35,64-94; add `case "agent": runAgent(os.Args[2:])`; update package doc-comment subcommand list + usage())
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Code Examples › CLI flag sentinel precedence (flags default -1 → fall through to NewBudgetFromEnv; non--1 overrides via SetMaxSteps) + § Coverage Strategy (keep logic out of main() so agent_test.go can capture stdout)
    - .planning/phases/02-agent-cornerstone/02-SPEC.md Req#7 (the dry-run flag set + behavior) + § Acceptance SC#4 (the exact UUIDv7 regex `^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-06 (CLI flag > env > default; -1 sentinel; NewBudgetFromEnv fail-fast)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § cmd/aura/agent.go (db.go handler shape; one Event = one JSON line) + § cmd/aura/main.go (self-analog dispatcher edit; drop now-unused imports)
    - internal/agent/event.go (from Plan 02 — note the custom `Event.MarshalJSON`; the dry-run prints Events THROUGH it, see W7 below) + internal/agent/workflow/loop.go (NewLoop) + internal/agent/agenttest/mocks.go (InfiniteToolCallAgent)
    - internal/agent/loop.go (the 132-LOC Phase-1 `Loop` struct to `git rm`)
  </read_first>
  <behavior>
    - `aura agent dry-run --request-id auto` → every stdout line is a JSON Event with a `request_id` matching the v7 regex, all identical
    - `aura agent dry-run --request-id 0192f000-0000-7000-8000-000000000001` → that exact request_id on every line, reproducible
    - `aura agent dry-run --max-steps 25` → terminates after exactly 25 step Events + 1 budget-exhausted Event (26 lines)
    - flag -1 (unset) falls through to NewBudgetFromEnv; non--1 overrides (CLI>env>default)
  </behavior>
  <action>
    Create cmd/aura/agent.go: `runAgent(args []string)` mirroring db.go's dispatcher; the `dry-run` sub-subcommand parses `--request-id` (string, "auto" or a UUID), `--max-steps`/`--max-wallclock-sec`/`--dedup-window` (int, default -1 sentinel per D-06). Build `Budget` via `agent.NewBudgetFromEnv()` (fail-fast); for each flag != -1, override via the setter (CLI>env>default). `--request-id auto` → `uuid.NewV7()` at boot; a literal UUID → parse verbatim (reproducibility). Build a mock `LoopAgent` over `agenttest.InfiniteToolCallAgent` (the same-tool-call-forever fixture), construct the root `InvocationContext` (RequestID = the chosen uuid, fresh SpanID, nil ParentSpanID, the Budget), iterate `agent.Run(ic)` consuming `iter.Seq2[*Event,error]` with the two-var range form (D-22 footgun 4). W7 — SERIALIZE USER-FACING EVENTS VIA `json.NewEncoder(os.Stdout)` WITH `SetEscapeHTML(false)`, honoring the custom `Event.MarshalJSON` (Plan 02 Task 2) — print each Event as ONE JSON line through `enc.Encode(ev)`. Do NOT use `canonicaljson.Marshal` for the dry-run stdout: canonicaljson is for hashing/dedup-fingerprinting ONLY; routing Events through it would double-encode / drift key order and break the SC#2 smoke grep `'"limit_hit":"max_steps"'`. The single user-facing serialization path is `Event.MarshalJSON`. Exit 0 on natural termination, 1 on error. KEEP the iterate-and-print logic in a testable function (not inline in a `main`-only path) so agent_test.go can capture stdout. Edit main.go: remove `case "chat"`, `chatOnce`, `stubClient`; add `case "agent": runAgent(os.Args[2:])`; update the package doc subcommand list + `usage()` string; drop the now-unused `internal/llm` import. Delete `internal/agent/loop.go` (`git rm`) — verify `go build` clean after. Create agent_test.go: flag-parsing + request-id auto/verbatim tests capturing stdout, asserting the v7 regex and all-lines-equal.
  </action>
  <verify>
    <automated>go build ./... && go test ./cmd/aura/ -run 'TestDryRun|TestAgent' && test ! -f internal/agent/loop.go && go run ./cmd/aura agent dry-run --request-id 0192f000-0000-7000-8000-000000000001 --max-steps 5 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/aura/agent.go` builds a `LoopAgent` over `agenttest.InfiniteToolCallAgent` and prints one JSON Event per line VIA `json.NewEncoder(os.Stdout)` with `SetEscapeHTML(false)` (W7): `grep -q 'json.NewEncoder' cmd/aura/agent.go` and `grep -q 'SetEscapeHTML(false)' cmd/aura/agent.go`; NO canonicaljson on the print path: `grep -c 'canonicaljson' cmd/aura/agent.go` returns 0
    - `cmd/aura/main.go` has `case "agent"` and NO `case "chat"`, NO `chatOnce`, NO `stubClient`: `grep -q 'case "agent"' cmd/aura/main.go` and `grep -c 'stubClient' cmd/aura/main.go` returns 0
    - `internal/agent/loop.go` no longer exists: `test ! -f internal/agent/loop.go`
    - `TestDryRun_RequestIDAuto_IsValidUUIDv7_AndStable` passes (every line matches the v7 regex, all identical)
    - a test asserts `--request-id <fixed>` reproduces verbatim
    - `go build ./...` clean (no unused-import error after dropping llm)
  </acceptance_criteria>
  <done>aura agent dry-run emits UUIDv7-correlated Event JSON lines via Event.MarshalJSON through json.NewEncoder+SetEscapeHTML(false) (W7, NOT canonicaljson); CLI>env>default precedence (SC#4); chat/stubClient removed, loop.go deleted, agent dispatch wired; stdout-capturable; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: SC#2 smoke script + .env.example + dangling-doc fix + 85% phase-close coverage gate (B4)</name>
  <files>scripts/loop_budget_smoke.sh, .env.example, .planning/codebase/STRUCTURE.md</files>
  <read_first>
    - scripts/neo4j_smoke.sh (the `#!/usr/bin/env bash` + `set -euo pipefail` + delegate-to-Go idiom to mirror; Git-Bash compatible) + scripts/check-file-size.sh
    - .planning/phases/02-agent-cornerstone/02-SPEC.md § Acceptance SC#2 (smoke: 26 lines, grep `"limit_hit":"max_steps"`, final Event shape) + § Boundaries (loop.go deletion + main.go cleanup are explicit deliverables) + § Acceptance (`.env.example` must document AURA_LOOP_MAX_STEPS=25 / MAX_WALLCLOCK_SEC=300 / DEDUP_WINDOW=3)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md `<specifics>` smoke (exactly 26 lines = 25 step + 1 budget-exhausted; grep limit_hit:max_steps; final Event author + StateDelta) + A7 (the extra env vars: AURA_LOOP_DEDUP_EXEMPT_TOOLS, AURA_LOOP_BRANCH_SOFT_FRACTION, AURA_LOOP_NODE_TIMEOUT_SEC, AURA_LOOP_DEDUP_RESULT_CAP)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Runtime State Inventory (README.md / STRUCTURE.md L24,L335 reference internal/agent/loop.go — will dangle after deletion; fix the reference) + § Coverage Strategy to Hit 85% Floor (the unit-only 85% target, budget*.go highest leverage)
    - CLAUDE.md § COVERAGE FLOOR 85% (the hard floor that overrides the SPEC ≥75% — B4)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § Shared Patterns › Smoke-script idiom (wc -l == 26, grep)
  </read_first>
  <action>
    Create scripts/loop_budget_smoke.sh: `#!/usr/bin/env bash` + `set -euo pipefail`; run `go run ./cmd/aura agent dry-run --max-steps 25`, capture stdout, assert line count `== 26` via `wc -l` (25 step Events + 1 budget-exhausted), and `grep '"limit_hit":"max_steps"'` must succeed; exit non-zero with a clear message on mismatch. Must actually RUN (no t.Skip / no skip-as-green — CLAUDE.md). Append to `.env.example` the SPEC three vars `AURA_LOOP_MAX_STEPS=25`, `AURA_LOOP_MAX_WALLCLOCK_SEC=300`, `AURA_LOOP_DEDUP_WINDOW=3` plus the A7 vars `AURA_LOOP_DEDUP_EXEMPT_TOOLS=`, `AURA_LOOP_BRANCH_SOFT_FRACTION=`, `AURA_LOOP_NODE_TIMEOUT_SEC=`, `AURA_LOOP_DEDUP_RESULT_CAP=2048`, each with a one-line `AURA_<DOMAIN>_<UNIT>`-convention comment. Fix the dangling `internal/agent/loop.go` reference in `.planning/codebase/STRUCTURE.md` (point it at the new agent.go/workflow layout or remove the stale line). Also remove the `internal/agent/loop.go` exclusion line from `.golangci.yml` if present (the file no longer exists — stale exclusion). B4 — ADD THE 85% PHASE-CLOSE COVERAGE GATE: the smoke script (or a clearly-named companion section in it) runs `go test -coverprofile=cover.out ./internal/agent/... ./internal/canonicaljson/... ./cmd/aura/` then `go tool cover -func=cover.out | tail -1 | awk '{if ($3+0 < 85.0){print "FAIL: total coverage "$3" < 85% (CLAUDE.md floor)"; exit 1}}'`, failing the script below 85%. This is the authoritative phase-close floor (overrides SPEC's ≥75%).
  </action>
  <verify>
    <automated>bash scripts/loop_budget_smoke.sh && grep -q 'AURA_LOOP_MAX_STEPS' .env.example && go test -coverprofile=cover.out ./internal/agent/... ./internal/canonicaljson/... ./cmd/aura/ && go tool cover -func=cover.out | tail -1 | awk '{if ($3+0 < 85.0){print "FAIL: "$3" < 85%"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/loop_budget_smoke.sh` exits 0; its stdout assertion confirms exactly 26 lines and `grep '"limit_hit":"max_steps"'` present (SC#2)
    - `.env.example` contains `AURA_LOOP_MAX_STEPS=25`, `AURA_LOOP_MAX_WALLCLOCK_SEC=300`, `AURA_LOOP_DEDUP_WINDOW=3` plus the four A7 vars
    - `.planning/codebase/STRUCTURE.md` no longer references `internal/agent/loop.go` as a live file
    - the smoke script contains `set -euo pipefail` and actually invokes `aura agent dry-run` (no skip)
    - B4 — `go test -coverprofile=cover.out ./internal/agent/... ./internal/canonicaljson/... ./cmd/aura/ && go tool cover -func=cover.out | tail -1` reports total coverage ≥85.0%, and the awk gate exits non-zero below 85% (proven by running it); the 85% floor is the authoritative phase-close metric (overrides SPEC ≥75%)
  </acceptance_criteria>
  <done>SC#2 smoke green (26 lines + grep), env vars documented (SPEC 3 + A7), dangling doc/lint references fixed, and the 85% phase-close coverage gate (B4) is wired and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags/env → Budget | `--request-id` and `--max-*` flags + `AURA_LOOP_*` env parsed at the entry point |
| dry-run output → CI grep | the smoke script's 26-line + grep contract is the SC#2 machine-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | malformed `AURA_LOOP_*` or `--max-steps` crashing the entry point | mitigate | `NewBudgetFromEnv` fail-fast (D-06) returns an error → `runAgent` exits 1 cleanly; CLI flags validated (-1 sentinel) |
| T-02-16 | Spoofing | predictable/forgeable request_id breaking trace correlation | mitigate | `uuid.NewV7()` from `crypto/rand` (via google/uuid) for `auto`; literal UUID parsed/validated for reproducibility (SC#4) |
| T-02-17 | Tampering | skip-as-green smoke test exercising nothing | mitigate | `scripts/loop_budget_smoke.sh` actually runs `aura agent dry-run` and asserts line count + grep + the 85% coverage gate (CLAUDE.md NO-SKIP-AS-GREEN); a sub-second/empty run fails the assertion |
</threat_model>

<verification>
- `bash scripts/loop_budget_smoke.sh` exits 0 (SC#2: 26 lines + grep limit_hit:max_steps)
- `go run ./cmd/aura agent dry-run --request-id auto` every line UUIDv7, all equal (SC#4)
- B4 — `go test -coverprofile=cover.out ./internal/agent/... ./internal/canonicaljson/... ./cmd/aura/ && go tool cover -func=cover.out | tail -1` reports ≥85.0% total (CLAUDE.md hard floor, overrides SPEC ≥75%); the awk gate exits non-zero below 85%
- `go test ./cmd/aura/` green; `go build ./...` clean; `go vet ./...` clean
- `bash scripts/check-file-size.sh` clean (agent.go ~90, agent_test.go ≤250)
- `golangci-lint run ./internal/agent/... ./cmd/aura/...` clean (config exists; loop.go exclusion removed)
- `test ! -f internal/agent/loop.go` (deletion confirmed)
</verification>

<success_criteria>
- SC#4: `aura agent dry-run` emits UUIDv7-correlated Event lines via Event.MarshalJSON (W7, not canonicaljson), CLI>env>default precedence (D-06)
- SC#2: smoke script green (26 lines + grep), actually runs (no skip)
- B4: phase-close coverage ≥85% gated by a real go test -cover assertion (CLAUDE.md floor, overrides SPEC ≥75%)
- loop.go deleted, chat/stubClient removed, agent dispatch wired, env vars documented; A1-A7 PRD amendments + adk-go attribution landed PRD-first (W9 — Task 1 before code)
</success_criteria>

<output>
Create `.planning/phases/02-agent-cornerstone/02-07-SUMMARY.md` when done
</output>
