---
phase: 02-agent-cornerstone
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - internal/agent/budget.go
  - internal/agent/budget_dedup.go
  - internal/agent/budget_test.go
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "A single *atomic.Int32 shared by pointer across the whole tree bounds total steps; Budget.Child() shares the counter, never forks it (D-10) — depth-3 fan-3 consumes ≤25 total, not 25³"
    - "ConsumeStep decrements-then-checks-then-restores so N concurrent goroutines never over-spend the cap (D-11, TOCTOU)"
    - "NewBudgetFromEnv fail-fasts with an exact error message on malformed AURA_LOOP_* env (D-06), never silently defaulting"
    - "Budget.Child() forks a DISTINCT dedup ring (no cross-branch false positives) while sharing the atomic counter (D-09); Sequential/Loop share the SAME ring via WithSubAgent (cross-iteration repeat detection)"
    - "Two-tier dedup fires on sha256(name+canonical_args) before re-executing; a changed result vetoes dedup (progress) so volatile-result tools fail SAFE not OPEN (D-18)"
  artifacts:
    - path: "internal/agent/budget.go"
      provides: "Budget struct + NewBudgetFromEnv + ConsumeStep + Child + Remaining + wallclock + soft cap"
      contains: "func NewBudgetFromEnv"
      exports: ["Budget", "NewBudgetFromEnv"]
    - path: "internal/agent/budget_dedup.go"
      provides: "dedupRing + BeforeToolCall/AfterToolResult two-phase fingerprint + exempt allowlist"
      contains: "BeforeToolCall"
    - path: "internal/agent/budget_test.go"
      provides: "atomic/TOCTOU race + canonical hash + wallclock + soft-cap + property tests"
      contains: "TestBudget_ConsumeStep"
  key_links:
    - from: "internal/agent/budget_dedup.go"
      to: "internal/canonicaljson.Marshal"
      via: "fingerprint = sha256(name + caller-supplied argsCanonicalJSON)"
      pattern: "argsCanonicalJSON"
    - from: "internal/agent/budget.go"
      to: "sync/atomic.Int32"
      via: "shared step counter by pointer (D-10)"
      pattern: "atomic\\.Int32"
---

<objective>
Implement the cornerstone mechanic: the 3-cap Budget tree (steps + wallclock + dedup) with a single `*atomic.Int32` shared by pointer across the entire agent tree (D-10), TOCTOU-safe `ConsumeStep` (D-11), fail-fast env parsing (D-06), two context derivations (D-09), per-branch passive soft cap (D-12), wallclock via deadline (D-13), and the two-tier dedup fingerprint (D-18/A2). Split across `budget.go` + `budget_dedup.go` per RESEARCH finding #1 (the combined file exceeds 600 LOC — CLAUDE.md no-god-class).

Purpose: This is the resource-exhaustion control (the Budget tree IS a DoS-prevention mechanism, ASVS V11). The shared-atomic pattern is what makes SC#3 true — it avoids the depth³ explosion that fresh-per-child designs (nanobot) fall into. The two-tier dedup (D-18) is the loop-guard that makes the infinite-tool-call fixture terminate; getting it fail-open (result-in-hash) would let runaway loops slip past — that is precisely why A2 reversed the original design.
Output: `internal/agent/{budget,budget_dedup}.go` + `budget_test.go` (atomic invariants, TOCTOU race, canonical hash, wallclock, soft-cap, two property-based tests).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-cornerstone/02-SPEC.md
@.planning/phases/02-agent-cornerstone/02-CONTEXT.md
@.planning/phases/02-agent-cornerstone/02-RESEARCH.md
@.planning/phases/02-agent-cornerstone/02-PATTERNS.md
@.planning/phases/02-agent-cornerstone/02-02-SUMMARY.md
@.planning/phases/02-agent-cornerstone/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Budget struct + NewBudgetFromEnv (fail-fast) + ConsumeStep + Child + wallclock + soft cap (budget.go) + delete the Plan-02 Budget stub</name>
  <files>internal/agent/budget.go</files>
  <read_first>
    - internal/agent/agent.go (from Plan 02 — note the TEMPORARY `type Budget struct{}` stub with the `// TEMP:` comment; THIS task MUST delete that stub line from agent.go and define the real Budget in budget.go, same package)
    - internal/db/migrate.go:28-29 + :39-41 (the fail-fast sentinel-string idiom — NewBudgetFromEnv copies THIS, NOT config.go's silent envIntDefault; tests assert the literal verbatim)
    - internal/db/config.go:17-25 + :114-124 (config struct per-field doc/in-line default idiom AND the `envIntDefault` silent-absorb anti-pattern to DIVERGE from per D-06)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Architecture Patterns › Pattern 5 (ConsumeStep decrement-then-check-then-restore; WithSubAgent copy shares ring; Child forks ring shares atomic; softCap) + the RESEARCH FINDING block on the D-12 soft-cap concurrency hazard (keep hard total bound authoritative; soft cap is a PASSIVE advisory returning (false,"branch_soft_cap"), retries against shared pool; ~50 LOC not ~30; defer DOVA rebalancing)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § budget.go (fail-fast DIVERGE from config.go; error-wrap `<verb> <noun>: %w`; Config-struct doc idiom)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-06, D-09, D-10, D-11, D-12, D-13; A5 (soft cap + decrement-then-check + wallclock via context.WithDeadline + optional AURA_LOOP_NODE_TIMEOUT_SEC); A7 (AURA_LOOP_BRANCH_SOFT_FRACTION, AURA_LOOP_NODE_TIMEOUT_SEC env names)
    - internal/agent/agent.go (the InvocationContext.WithSubAgent copy from Plan 02 — Child/WithSubAgent must agree on ring-sharing semantics, D-09)
  </read_first>
  <behavior>
    - 10 goroutines × 100 ConsumeStep on a 1000-step budget = exactly 1000 successful decrements under -race (TestBudget_ConsumeStep_AtomicDecrement_NoRace)
    - N goroutines vs counter of 1 → exactly one ok==true (TestBudget_ConsumeStep_ExactlyOneWinner_When_CounterIsOne, TOCTOU D-11)
    - parent consumes 5 → child.Remaining()==20 (TestBudget_Child_SharesStepsCounter, shared pointer D-10)
    - past deadlineWallclock → ConsumeStep returns (false,"wallclock") (TestBudget_Wallclock_TerminatesAtDeadline, INJECTABLE FAKE CLOCK — see W8 below)
    - NewBudgetFromEnv with AURA_LOOP_MAX_STEPS=abc → returns a non-nil error containing the exact malformed-env message (D-06 fail-fast)
  </behavior>
  <action>
    FIRST delete the temporary `type Budget struct{}` stub (the `// TEMP:` line) from agent.go that Plan 02 added — the real Budget now lives here. Create budget.go: `type Budget struct { steps *atomic.Int32; deadlineWallclock time.Time; now func() time.Time; dedupWindow int; dedupRing *dedupRing; branchSoftCap int }` (dedupRing type lives in budget_dedup.go, same package — Task 2). W8 — WALLCLOCK USES AN INJECTABLE CLOCK, NOT synctest: include a `now func() time.Time` field defaulting to `time.Now`; tests inject a fake `now` to drive the deadline deterministically. This deliberately AVOIDS Go 1.26 `testing/synctest` (which spawns background goroutines that can trip the workflow_test.go `goleak.VerifyTestMain`, SC#1). Document the choice in a budget.go comment. `NewBudgetFromEnv() (*Budget, error)` reads `AURA_LOOP_MAX_STEPS` (default 25), `AURA_LOOP_MAX_WALLCLOCK_SEC` (default 300), `AURA_LOOP_DEDUP_WINDOW` (default 3), plus A7 vars `AURA_LOOP_BRANCH_SOFT_FRACTION` and optional `AURA_LOOP_NODE_TIMEOUT_SEC`. FAIL-FAST on malformed values with an exact wrapped error message (copy migrate.go's load-bearing-literal idiom, NOT config.go's silent envIntDefault) — define the message strings as consts so tests assert them verbatim (D-06). `ConsumeStep() (ok bool, reason string)`: check wallclock first (`b.now().After(b.deadlineWallclock)` → `(false,"wallclock")`), then `if n := b.steps.Add(-1); n < 0 { b.steps.Add(1); return false, "max_steps" }` — decrement-then-check-then-restore (D-11, prevents TOCTOU over-spend). Add `Remaining() int`, `SetMaxSteps(int32)` (for CLI override D-06). `Child(fanout int) *Budget` shares the SAME `*atomic.Int32` pointer (D-10) + same deadline + same `now`, but a DISTINCT new dedup ring (D-09) and a PASSIVE `branchSoftCap = softCap(remaining, fanout)` (D-12 — tunable via AURA_LOOP_BRANCH_SOFT_FRACTION; pick the default fraction, document the choice). Keep the hard total bound authoritative: `ConsumeStep` only returns hard terminal reasons (`max_steps`, `wallclock`). Soft-cap fairness is exposed through a separate non-terminal helper such as `SoftCapExceeded() bool` or a scheduler-local check in ParallelAgent; LoopAgent MUST NOT emit budget-exhausted for `branch_soft_cap`. Do NOT implement active DOVA rebalancing — deferred. Thread `context.WithDeadline(parent, deadlineWallclock)` so cancellation propagates end-to-end (D-13). Add the adk attribution comment. Carry the repo error-wrap convention everywhere.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/agent/ -run 'TestBudget_(ConsumeStep|Child|Wallclock)' && go build ./internal/agent/ && go vet ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/agent.go` no longer contains `type Budget struct{}` (the Plan-02 TEMP stub deleted): `grep -c 'type Budget struct{}' internal/agent/agent.go` returns 0
    - `internal/agent/budget.go` contains `func NewBudgetFromEnv() (*Budget, error)` and `steps *atomic.Int32`
    - budget.go has an injectable `now func() time.Time` field (fake-clock, NOT synctest) and a comment explaining the goleak-avoidance rationale (W8)
    - `TestBudget_ConsumeStep_AtomicDecrement_NoRace` passes under `-race` (exactly 1000 decrements)
    - `TestBudget_ConsumeStep_ExactlyOneWinner_When_CounterIsOne` passes (TOCTOU: exactly one winner)
    - `TestBudget_Child_SharesStepsCounter` passes (parent consume 5 → child Remaining()==20)
    - `TestBudget_Wallclock_TerminatesAtDeadline` passes using the injected fake clock (returns `(false,"wallclock")` past deadline) — no synctest, no goleak-tripping background goroutines
    - A NewBudgetFromEnv test asserts the verbatim fail-fast error string for `AURA_LOOP_MAX_STEPS=abc`
    - `grep -q 'envIntDefault' internal/agent/budget.go` returns NOTHING (did NOT copy the silent-absorb anti-pattern)
  </acceptance_criteria>
  <done>Shared-atomic Budget with TOCTOU-safe ConsumeStep, fail-fast env, child-shares-counter, fake-clock wallclock deadline (W8: no synctest), passive soft cap; race-clean; Plan-02 Budget stub removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two-tier dedup ring + BeforeToolCall/AfterToolResult + exempt allowlist (budget_dedup.go) + canonical hash tests</name>
  <files>internal/agent/budget_dedup.go, internal/agent/budget_test.go</files>
  <read_first>
    - internal/agent/tools/manifest.go:19-21 (the determinism-rationale comment idiom — document WHY the fingerprint is name+args and WHY result is veto-only, citing the fail-open consequence)
    - internal/canonicaljson/canonicaljson.go (from Plan 01 — the caller produces canonical args via canonicaljson.Marshal; BeforeToolCall RECEIVES the already-canonical bytes, see B2 contract below)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Architecture Patterns › Pattern 7 (two-tier: primary sha256(name+canonical_json(args)) fired BEFORE re-execute; result-preview as progress veto; window=3 period-1 AND period-2 ping-pong; ring ≥ max(window,4); AURA_LOOP_DEDUP_EXEMPT_TOOLS allowlist) + § Common Pitfalls › Pitfall 6 (fail-open on volatile results — the thing to avoid)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § budget_dedup.go (why split out; determinism comment; ring buffer)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-18 (two-tier — reversed from initial), D-19 (AURA_LOOP_DEDUP_EXEMPT_TOOLS), D-20 (window=3, period-1 + period-2, ring≥max(window,4)); A2; A7 (AURA_LOOP_DEDUP_RESULT_CAP result-preview byte cap — default 1-4KB, planner discretion: pick 2KB)
    - internal/config/config_test.go:10-50 (t.Setenv table-test idiom for env-driven dedup-exempt cases)
  </read_first>
  <behavior>
    - {"a":1,"b":2} and {"b":2,"a":1} produce IDENTICAL fingerprints when the caller canonicalizes both (TestBudget_BeforeToolCall_CanonicalHashOrderIndependent)
    - same sha256(name+canon_args) ×3 with UNCHANGED result → BeforeToolCall reports dedup before the next side-effect (period-1, window=3)
    - A-B-A-B same-args pattern → ping-pong dedup detected (period-2)
    - same args + CHANGED result → dedup SUPPRESSED (progress veto, D-18 fail-safe)
    - parent dedup state invisible to Budget.Child() ring (TestBudget_Child_DistinctDedupRing)
    - a tool in AURA_LOOP_DEDUP_EXEMPT_TOOLS never triggers dedup (D-19)
  </behavior>
  <action>
    Create budget_dedup.go (same package `agent`): `type dedupRing struct {...}` ring buffer with capacity `≥ max(dedupWindow, 4)` (D-20 — needs ≥4 slots for ping-pong while WINDOW=3 governs period-1). `newDedupRing(window int) *dedupRing`. B2 — LOCK THE CANONICALIZATION CONTRACT: `(*Budget).BeforeToolCall(name string, argsCanonicalJSON []byte) (dedup bool, reason string)` accepts PRE-CANONICALIZED `argsCanonicalJSON []byte`; the CALLER runs `canonicaljson.Marshal(args)` before calling it. BeforeToolCall computes primary fingerprint `sha256(name + argsCanonicalJSON)` BEFORE the tool re-executes, so repeated side effects can be blocked. `(*Budget).AfterToolResult(name string, argsCanonicalJSON []byte, resultPreview []byte)` records the bounded result preview only as a progress veto for that fingerprint. If the same args repeat but the latest result preview changed, the next BeforeToolCall suppresses dedup and treats it as progress (D-18 fail-safe); if args and preview stay unchanged, period-1/period-2 repeat detection can terminate. Document this contract verbatim in both doc comments so Plan 05 Task 2 (the caller) and this callee agree. Honor `AURA_LOOP_DEDUP_EXEMPT_TOOLS` CSV allowlist (D-19) — exempt tools never dedup. Document WHY (manifest.go rationale-comment idiom): the fail-open trap that A2 reversed. Write budget_test.go covering: `TestBudget_BeforeToolCall_CanonicalHashOrderIndependent` (the test canonicalizes both arg orderings via canonicaljson.Marshal then asserts equal fingerprints — exercising the caller-canonicalizes contract), `TestBudget_Child_DistinctDedupRing`, period-1, period-2 ping-pong, result-veto suppression through AfterToolResult, exempt-allowlist, plus the two property tests `TestBudget_Property_TotalConsumedNeverExceedsMax` (rapid, generates random concurrent consume sequences, asserts invariant) — note the escalate-yielded property test lives with LoopAgent in Plan 05.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/agent/ -run 'TestBudget' && go vet ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/budget_dedup.go` contains `BeforeToolCall(name string, argsCanonicalJSON []byte)` and `AfterToolResult(name string, argsCanonicalJSON []byte, resultPreview []byte)` — the CALLER-canonicalizes contract (B2): doc comments state the caller passes `canonicaljson.Marshal(args)`; these methods do NOT call canonicaljson internally.
    - `TestBudget_BeforeToolCall_CanonicalHashOrderIndependent` passes (`{"a":1,"b":2}` canonicalized == `{"b":2,"a":1}` canonicalized → same fingerprint)
    - `TestBudget_Child_DistinctDedupRing` passes (parent ring state invisible to child)
    - A test asserts same args + changed result → dedup SUPPRESSED (progress veto, D-18)
    - A test asserts an A-B-A-B pattern triggers period-2 dedup
    - A test asserts a tool in `AURA_LOOP_DEDUP_EXEMPT_TOOLS` never dedups
    - `TestBudget_Property_TotalConsumedNeverExceedsMax` (rapid) passes
    - `bash scripts/check-file-size.sh` clean (budget.go AND budget_dedup.go each ≤600 LOC — the RESEARCH#1 split)
  </acceptance_criteria>
  <done>Two-tier dedup fingerprint (name+caller-canonicalized-args primary, result-as-veto) with period-1/period-2 detection, distinct child ring, exempt allowlist; caller-canonicalizes contract locked (B2); fail-SAFE on volatile results; race + property clean; file split holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | `AURA_LOOP_*` parsed at boot — malformed input must not crash mid-loop |
| tool args/results → loop-guard | repeated tool calls drive dedup termination; a fail-open guard lets runaway loops slip |
| agent tree → shared counter | every node mutates one `*atomic.Int32` concurrently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | `ConsumeStep` counter over-spend (TOCTOU) / depth³ explosion | mitigate | decrement-then-check-then-restore (D-11) + single shared `*atomic.Int32` by pointer (D-10) so depth-3 fan-3 ≤25 not 25³; proven by `_ExactlyOneWinner_` + `_Child_SharesStepsCounter` + (SC#3 in Plan 06) |
| T-02-02 | Denial of Service | fail-open dedup allowing infinite tool-call loops | mitigate | two-tier fingerprint fires on name+args BEFORE re-execute (D-18/A2); result-as-veto means volatile results fail SAFE not OPEN; proven by period-1/period-2 + result-veto tests |
| T-02-03 | Denial of Service | malformed `AURA_LOOP_*` crashing mid-loop | mitigate | `NewBudgetFromEnv` fail-fast at boot with exact error (D-06), Phase-1 boot discipline |
| T-02-08 | Denial of Service | in-flight tool/LLM call overruns the wallclock | mitigate | `context.WithDeadline` threaded into leaf calls (D-13) + optional `AURA_LOOP_NODE_TIMEOUT_SEC` per-node soft timeout |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/agent/ -run TestBudget` green (atomic, TOCTOU, canonical, wallclock via fake clock, soft-cap, property)
- `go build ./internal/agent/ && go vet ./internal/agent/` clean (Plan-02 Budget stub removed, real Budget compiles)
- `bash scripts/check-file-size.sh` clean (budget.go + budget_dedup.go split holds, both ≤600)
- mutation spot-check candidate: `budget.go` decrement-then-check `<0` boundary (run at phase gate)
</verification>

<success_criteria>
- Single shared `*atomic.Int32` bounds the tree (D-10); `ConsumeStep` TOCTOU-safe (D-11); wallclock via injectable fake clock not synctest (W8)
- `NewBudgetFromEnv` fail-fast (D-06); two context derivations correct (D-09: WithSubAgent shares ring, Child forks ring)
- Two-tier dedup fail-SAFE (D-18/A2) with caller-canonicalizes contract (B2) + period-1 + period-2 + exempt allowlist; file split holds under 600 LOC
</success_criteria>

<output>
Create `.planning/phases/02-agent-cornerstone/02-03-SUMMARY.md` when done
</output>
</output>
