---
phase: 14-onboarding-agent-md
plan: 3
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - internal/onboarding/interview.go
  - internal/onboarding/session.go
  - internal/onboarding/extractor.go
  - internal/onboarding/interview_test.go
  - internal/onboarding/session_test.go
autonomous: true
requirements: [UX-05]

must_haves:
  truths:
    - "Profile onboarding is implemented as workflow.NewLoop(..., InterviewStepAgent)"
    - "Confirm, skip, and edit all terminate via Event{Actions.Escalate=true}"
    - "Terminal StateDelta carries enough information for the caller to write Agent.md or resume chat"
    - "The onboarding package is channel-agnostic; Telegram owns rendering/buttons, not interview logic"
    - "The interview loop does not require tool calls for normal question steps"
  artifacts:
    - path: "internal/onboarding/interview.go"
      provides: "InterviewStepAgent and Loop builder"
      contains: "InterviewStepAgent"
    - path: "internal/onboarding/session.go"
      provides: "profile-onboarding session state"
      contains: "type Session"
  key_links:
    - from: ".planning/spikes/039-telegram-onboarding-loopagent-prototype/main.go"
      to: "internal/onboarding/interview_test.go"
      via: "confirm/skip/edit scenario structure"
      pattern: "Actions.Escalate"
---

<objective>
Build the channel-agnostic profile onboarding workflow. It should expose an
`InterviewStepAgent` usable inside `workflow.NewLoop`, track interview session
state, emit questions/drafts, and terminate on confirm/skip/edit decisions with
state deltas that Telegram can map to profile writes and normal chat resume.
</objective>

<context>
@.planning/phases/14-onboarding-agent-md/14-RESEARCH.md
@.planning/spikes/039-telegram-onboarding-loopagent-prototype/README.md
@internal/agent/workflow/loop.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: define onboarding session state and transitions</name>
  <files>internal/onboarding/session.go, internal/onboarding/session_test.go</files>
  <read_first>
    - .planning/spikes/039-telegram-onboarding-loopagent-prototype/main.go
    - internal/agent/event.go
  </read_first>
  <behavior>
    - Session tracks identity, current step, collected facts, preferences, draft Agent.md, and terminal status.
    - Supported user intents: answer, confirm, edit, skip, cancel/restart.
    - State transitions are deterministic and testable without Telegram.
    - Session can be serialized if the executor chooses to persist draft state next to the profile; otherwise keep a clear in-memory adapter boundary.
  </behavior>
  <action>Implement session model and transition tests. Keep language/user-facing copy minimal because Telegram can provide final copy.</action>
  <verify>
    <automated>go test ./internal/onboarding/ -run Session -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Confirm cannot happen before a draft exists.
    - Skip and cancel are terminal.
    - Edit reopens the draft and then can confirm.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: implement InterviewStepAgent and Loop builder</name>
  <files>internal/onboarding/interview.go, internal/onboarding/interview_test.go</files>
  <read_first>
    - internal/agent/agent.go
    - internal/agent/workflow/loop.go
    - internal/agent/event.go
  </read_first>
  <behavior>
    - `InterviewStepAgent` implements `agent.Agent`.
    - A package helper returns `workflow.NewLoop("ProfileOnboardingLoop", maxIter, InterviewStepAgent)`.
    - Non-terminal question/draft events carry `LLMResponse.Content` and `Actions.StateDelta`.
    - Terminal events set `Actions.Escalate=true`.
    - No budget/tool-call tricks are needed for natural language steps.
  </behavior>
  <action>Port the spike 039 scenarios into production tests and implement the real agent around Session transitions.</action>
  <verify>
    <automated>go test ./internal/onboarding/ -run 'Interview|Loop|Confirm|Skip|Edit' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Confirm emits `onboarding_completed=true`, `agent_md`, `preferences_json`, and `resume_chat=true`.
    - Skip emits `skipped=true` and `resume_chat=true`.
    - Edit emits revised `agent_md` before confirm.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: add profile extraction/render helpers for onboarding answers</name>
  <files>internal/onboarding/extractor.go, internal/onboarding/interview_test.go, internal/profile/render.go</files>
  <read_first>
    - internal/profile/render.go
    - prd.md Slice 10 profile/preferences section
  </read_first>
  <behavior>
    - Extract name, language, timezone, tone, response length, and voice preference from structured session answers.
    - Produce a profile draft using `internal/profile` render helpers.
    - Avoid LLM extraction in this plan; deterministic structured answers are enough for first-run onboarding.
  </behavior>
  <action>Implement deterministic extraction from session fields and tests for Italian preference, timezone Europe/Rome, response length, and voice mode.</action>
  <verify>
    <automated>go test ./internal/onboarding/ ./internal/profile/ -run 'Extract|Render|Preference' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Draft Agent.md includes Facts and Preferences.
    - preferences.json fields match PRD names.
    - Extractor output stays bounded.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-14-03-StateConfusion | Integrity | onboarding session | mitigate | explicit state machine tests for confirm/edit/skip |
| T-14-03-InfiniteLoop | DoS | LoopAgent | mitigate | finite maxIter and terminal Escalate on all final states |
| T-14-03-ChannelCoupling | Maintainability | onboarding package | mitigate | package imports agent/profile only, not telegram |
| T-14-03-OverExtraction | Privacy | profile draft | mitigate | deterministic user-provided fields only; no background inference in this plan |
</threat_model>

<verification>
- `go test ./internal/onboarding/ ./internal/profile/ -count=1`
- `go test ./internal/agent/workflow/ -run LoopAgent -count=1`
- `go build ./...`
</verification>

<success_criteria>
- A channel can run a `LoopAgent[InterviewStepAgent]` and receive confirm/skip/edit terminal state.
- Profile drafts are deterministic and compatible with the profile store.
</success_criteria>

<output>
Create `.planning/phases/14-onboarding-agent-md/14-03-SUMMARY.md` when done.
</output>
