---
phase: 14-onboarding-agent-md
plan: 4
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - internal/channels/telegram/bot.go
  - internal/channels/telegram/bot_dispatch.go
  - internal/channels/telegram/profile_onboarding.go
  - internal/channels/telegram/profile_onboarding_test.go
  - internal/channels/telegram/commands.go
  - cmd/aura/serve_channels.go
autonomous: true
requirements: [UX-05]

must_haves:
  truths:
    - "Telegram setup onboarding (/start token) remains separate from profile onboarding"
    - "A linked user with no Agent.md enters profile onboarding before normal LLM turn"
    - "/onboard starts or restarts profile onboarding explicitly"
    - "Confirm writes Agent.md through internal/profile.Store, then normal chat resumes"
    - "Skip and edit paths are covered and never drive duplicate LLM turns"
  artifacts:
    - path: "internal/channels/telegram/profile_onboarding.go"
      provides: "Telegram adapter for profile onboarding sessions/buttons/replies"
      contains: "profileOnboarding"
    - path: "cmd/aura/serve_channels.go"
      provides: "profile store/onboarding deps wired into Telegram channel"
      contains: "Profile"
  key_links:
    - from: "internal/channels/telegram/bot_dispatch.go"
      to: "internal/channels/telegram/profile_onboarding.go"
      via: "OnText routing before normal turn"
      pattern: "onText"
---

<objective>
Make Phase 14 user-reachable through Telegram. After account setup, a first-time
Telegram user with no Agent.md must enter profile onboarding; confirm writes the
profile; skip/edit work; and normal chat resumes without losing the existing
Phase 13 command/HITL/media dispatch behavior.
</objective>

<context>
@.planning/phases/14-onboarding-agent-md/14-RESEARCH.md
@.planning/phases/14-onboarding-agent-md/14-03-PLAN.md
@internal/channels/telegram/bot_dispatch.go
@internal/channels/telegram/onboarding.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: add profile onboarding deps to Telegram channel</name>
  <files>internal/channels/telegram/bot.go, cmd/aura/serve_channels.go</files>
  <read_first>
    - internal/channels/telegram/bot.go
    - cmd/aura/serve_channels.go
    - internal/profile/store.go
    - internal/onboarding/interview.go
  </read_first>
  <behavior>
    - Telegram Deps gains profile store and profile onboarding manager/session factory.
    - Composition root builds the Store from `AURA_PROFILE_DIR`.
    - A missing profile store degrades profile onboarding to a clear message, never a panic.
    - Existing Turn/commands/HITL/media deps remain unchanged.
  </behavior>
  <action>Wire profile store and onboarding dependencies through `telegram.Deps` and `bootChannelsAndSetup`.</action>
  <verify>
    <automated>go test ./internal/channels/telegram/ ./cmd/aura/ -run 'Profile|Serve|Deps' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Telegram channel constructs with profile deps.
    - Offline unit tests do not touch the real profile dir.
    - `go build ./...` stays green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: implement Telegram profile onboarding adapter</name>
  <files>internal/channels/telegram/profile_onboarding.go, internal/channels/telegram/profile_onboarding_test.go</files>
  <read_first>
    - internal/channels/telegram/hitl.go
    - internal/channels/telegram/commands.go
    - internal/onboarding/session.go
  </read_first>
  <behavior>
    - Adapter owns chat/identity session lookup and Telegram copy/buttons.
    - Confirm/edit/skip map to onboarding session transitions and final `StateDelta`.
    - Confirm writes Agent.md/preferences/metadata/changelog through profile Store.
    - Edit can use ForceReply or a simple text next-step; no need to reuse ask_user Store.
    - Callback data stays under Telegram's 64-byte button payload limit.
  </behavior>
  <action>Create the profile onboarding adapter and tests for confirm, skip, edit, stale callback, and no-profile-store degradation.</action>
  <verify>
    <automated>go test ./internal/channels/telegram/ -run ProfileOnboarding -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Confirm writes profile and sends a success reply.
    - Skip marks metadata/changelog and resumes normal chat.
    - Edit revises the draft before confirm.
    - Stale/forged callback is a no-op with an ack, not a panic.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: route first normal message and /onboard before LLM turn</name>
  <files>internal/channels/telegram/bot_dispatch.go, internal/channels/telegram/commands.go, internal/channels/telegram/bot_dispatch_test.go</files>
  <read_first>
    - internal/channels/telegram/bot_dispatch.go
    - internal/channels/telegram/commands.go
    - internal/channels/telegram/bot_dispatch_test.go
  </read_first>
  <behavior>
    - Routing order: `/start <token>` setup activation first; explicit commands next; active profile onboarding reply next; no-profile first message starts profile onboarding; else normal turn.
    - `/onboard` starts/restarts profile onboarding explicitly.
    - A message consumed by profile onboarding must not call `deps.Turn`.
    - Existing `/cost`, `/search`, `/cancel`, HITL, voice/photo/document behavior remains intact.
  </behavior>
  <action>Extend OnText routing and command menu. Add tests proving no duplicate normal turn while onboarding consumes a message and proving setup `/start <token>` remains unchanged.</action>
  <verify>
    <automated>go test ./internal/channels/telegram/ -run 'ProfileOnboarding|StartPayload|Command|HITL' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - First linked no-profile text starts onboarding and does not drive LLM.
    - `/onboard` restarts onboarding.
    - `/start <token>` still consumes setup token before profile onboarding.
    - Existing command intercept tests still pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-14-04-SetupMixup | Integrity | `/start <token>` vs profile onboarding | mitigate | routing tests keep setup activation first and separate |
| T-14-04-DuplicateTurn | Integrity/cost | OnText routing | mitigate | consumed onboarding messages return before `runTurn` |
| T-14-04-CallbackForge | Spoofing | profile buttons | mitigate | session/token validation; stale callbacks ack and no-op |
| T-14-04-ProfileWriteFail | Reliability | confirm path | mitigate | clear user-facing failure; profile store handles atomicity |
</threat_model>

<verification>
- `go test ./internal/channels/telegram/ -run 'ProfileOnboarding|StartPayload|Command|HITL' -count=1`
- `go test -race ./internal/channels/telegram/`
- `go build ./...`
</verification>

<success_criteria>
- First-time Telegram user sees profile onboarding.
- Confirm writes Agent.md.
- Skip/edit work.
- Normal Telegram chat resumes after terminal onboarding state.
</success_criteria>

<output>
Create `.planning/phases/14-onboarding-agent-md/14-04-SUMMARY.md` when done.
</output>
