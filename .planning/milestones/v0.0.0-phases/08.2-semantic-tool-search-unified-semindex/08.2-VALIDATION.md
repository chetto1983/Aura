---
phase: 08.2
slug: semantic-tool-search-unified-semindex
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-12
---

# Phase 08.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `08.2-RESEARCH.md` § Validation Architecture (HIGH confidence; mapped to existing
> test anchors + spike-measured thresholds). Per-Task map filled now that PLAN.md files exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go `testing` (stdlib), table-driven; `golang-testing` skill patterns (goleak, `-race`, parallel). No testify in this surface. |
| **Config file** | none — Go convention (`*_test.go` + build tags) |
| **Quick run command** | `go test -race ./internal/semindex/ ./internal/activelearn/ ./internal/toolselectstore/ ./internal/toolselectlearn/ ./internal/agent/tools/ ./internal/agent/prompt/` |
| **Full suite command** | `go test -race ./...` then tagged tiers: `-tags reasoning_live` (granite :8081 up), `-tags 'db_integration neo4j_integration'` (stack up) |
| **Corpus gate (live)** | `go test -tags reasoning_live -run TestReasoningClassifierLive ./internal/agent/prompt/` |
| **Estimated runtime** | unit+race ~tens of s; live gate ~minutes (granite) |

> **`reasoning_live` is an OPERATOR-GATED tier, NOT the standard CI quality gate.** It needs granite
> `:8081` up (and, for the `toolselectstore` round-trip, Neo4j up) and is run by an operator on demand —
> it is excluded from the default CI quality gate. There is therefore NO false-green ambiguity: the
> standard CI gate never depends on a sidecar, and the `reasoning_live` tier is run explicitly with the
> tag. The phase work must keep this tier COMPILING (the stale one-arg call repair lands in Plan 02,
> Wave 2 — see Wave 0 below) so an operator can run it; the live thresholds (90/92 accuracy; the
> `:ToolSelectionExample` round-trip) are Manual-Only rows confirmed under the tag with the stack up.

---

## Sampling Rate (Nyquist)

- **After every task commit:** `go vet ./... && go build ./... && go test -race ./internal/semindex/ ./internal/activelearn/ ./internal/toolselectstore/ ./internal/toolselectlearn/ ./internal/agent/tools/ ./internal/agent/prompt/`
- **After every plan wave:** `go test -race ./...` + `golangci-lint run ./...` (= 0) + owned-surface coverage gate (≥ 85%)
- **Before `/gsd-verify-work`:** full tag matrix green incl. `-tags reasoning_live` (granite up) for `TestReasoningClassifierLive`; deterministic Req-2 corpus gate green; mutation spot-check ≥ 70% on `internal/semindex/core.go` (the math) + the ranker fusion logic
- **Max feedback latency:** quick command < 60s

---

## Phase Requirements → Validation Matrix

> Trackable IDs are SPEC Req-1..Req-8 + CONTEXT D-01..D-07 (ROADMAP lists "Requirements: TBD").
> Per-task IDs (`08.2-NN-NN`) attached below.

| Req | Behavior validated | Tier | Build tag | Concrete assertion | Fixtures | Threshold |
|-----|--------------------|------|-----------|--------------------|----------|-----------|
| **Req-1** | semindex core compiles, math correct, imported by both consumers | unit + -race | (none) | synthetic-vector golden tests of `cosine`/`centroid`/`margin`; both `tools` (search.go) and `prompt` (reasoning_classifier.go) import `semindex` — the dual-consumer compile gate (`go build ./...` + grep both files) | hand-built `[][]float64` + golden margins (no embedder) | `go test -race ./internal/semindex/` green; 0 build errors in both consumers; both import semindex |
| **Req-2** | semantic ranker beats BM25, clears absolute floor, no per-query regression | unit (deterministic, fake embedder seeded with corpus vectors) **+** live confirm | (none) deterministic; `reasoning_live`-style live confirm | top-1 & MRR@10 **strictly exceed** BM25; AND top-1 ≥ 0.50, MRR@10 ≥ 0.60, recall@3 ≥ 0.70; AND **no documented query ranks worse than its BM25 rank** | spike-056 query corpus (13 queries + golds) + precomputed per-tool `searchDocument` embeddings (deterministic, free) | floors as listed; per-query no-regression |
| **Req-3** | mount embeds ONLY the delta; new tools searchable; race-clean | unit + **-race** | (none) | mounting an N-tool server triggers **exactly N** `Embed` calls (counting fake embedder); the N tools rank immediately; concurrent mount+search `-race` clean | counting fake `Embedder`; runtime registry adding N deferred tools | exactly N new embeds (not full re-embed); `-race` clean (extend `TestToolSearchConcurrentExecute`) |
| **Req-4** | reasoning classifier structurally migrated onto semindex **+ accuracy raised** | unit (existing, unchanged) + **behavior-equivalence** + **live corpus gate** | (none) unit; **`reasoning_live`** live | all existing `reasoning_classifier_test.go` pass **unchanged**; NEW equivalence test = identical tier verdicts at SAME seeds; `TestReasoningClassifierLive` RAISED | existing seeds/anchors; fake embedder for unit; live granite for the gate | existing green; equivalence identical; **live accuracy ≥ 90%, none-vs-rest ≥ 92%** (RAISED from 80/88 — 2026-06-12 amendment; tuning sequenced AFTER the equivalence proof). **Automated non-regression proxy:** after tuning, the full unit corpus + equivalence test stay green (the floor is earned, not bumped) |
| **Req-5a** | detection recall ≥ 0.85 on a labeled trace set | unit | (none) | detector flags every mis-route; `sameTool` compares namespaced vs bare (`__`) symmetrically; the detector's `Observe(request, usedTool)` is CALLED live from runner_persist.go (Open-Q #3) | spike-057 12-trace set (7 mis-routes + 5 efficient) + a real-turn wiring test | **detection recall ≥ 0.85**; `Observe` invoked on a real turn |
| **Req-5b** | learned examples raise tool-rank accuracy (held-out) | unit | (none) | fold confirmed `(query→tool)` exemplars, refresh per-tool centroids, assert held-out tool-rank accuracy rises vs no-fold baseline | synthetic per-tool exemplar vectors + a held-out query baseline mis-routes | learned > baseline on held-out |
| **Req-5c** | anti-amplification: poisoned label can't flip a seed-correct routing | unit | (none) | inject a wrong label below min-support floor & with boost-cap; assert a seed-correct routing is **unchanged** | a seed-correct tool + N<3 poisoned exemplars; then N≥3 but boost-capped | routing identical to seed-only; poisoned label never wins |
| **Req-6** | embed sidecar is a hard dep for tool_search; explicit error when down | unit + integration | (none) unit error-surface; live boot health-check | with the embedder erroring, `tool_search` returns an **explicit, model-visible `ToolResult` error** (NOT empty/garbage); reasoning classifier still soft-falls-back | fake embedder returning an error | error path returns non-empty model-facing error; reasoning path returns `("", false)` then falls back |
| **Req-7** | cache invariant: per-query ranking never mutates `messages[0]` or the Description | unit | (none) | across N different queries, `tool_search` `Spec().Description` byte-identical and `messages[0]` byte-stable; no clock/date shape leaks | reuse `TestToolSearchDescriptionDeterministic` + NEW N-query invariant test | byte-identical Description across N queries; `messages[0]` byte-stable; 0 timestamp-shaped substrings |
| **Req-8** | brute-force only, no ANN, no new sidecar, ≤ 20ms query+rank | unit (assert no ANN dep) + benchmark | (none) | no HNSW/ANN import; Neo4j the only learned-example store; benchmark asserts **mean query-embed+rank ≤ 20ms** at live corpus size (measured with `-benchtime=3s`, statistically meaningful) | live corpus size (~53–115 tools); benchmark with real/seeded vectors | mean ≤ 20ms (live total confirm operator-gated) |

---

## CI vs Live-only

- **CI (default + race + integration tags), no sidecar — free & deterministic:** Req-1, Req-2 (deterministic corpus gate w/ precomputed fixtures), Req-3, Req-4 (unit + equivalence + the automated non-regression proxy), Req-5a/b/c, Req-6 (unit error-surface), Req-7, Req-8 (no-ANN assertion + `-benchtime=3s` benchmark).
- **Live-only (operator-authorized, NOT default CI):** `TestReasoningClassifierLive` (tag `reasoning_live`, granite :8081, RAISED to 90/92); live confirm of the Req-2 corpus + the Req-8 ≤ 20ms latency at real corpus size; the `toolselectstore` Neo4j round-trip (tag `reasoning_live` — the SAME tag ALSO gates this `:ToolSelectionExample` round-trip, documented in an in-file comment per W-3). The paid `live_e2e` (OpenRouter) tier is **not** required for this phase's gates.

---

## Wave 0 Requirements (test infra to create before/with implementation)

- [ ] `internal/semindex/core_test.go` — synthetic-vector + golden-margin unit tests (Req-1) → **08.2-01-01**
- [ ] `internal/semindex/{classifier,ranker}_test.go` — wrapper tests vs fake `Embedder` (Req-1) → **08.2-01-02**
- [ ] `internal/activelearn/learner_test.go` — mechanism unit tests (queue/dedup/margin/refresh/Close, goleak) (Req-5) → **08.2-04-01**
- [ ] Deterministic semantic-ranker corpus fixture (spike-056 13-query corpus + precomputed embeddings) — Req-2 runs free in CI → **08.2-03-02**
- [ ] Labeled trace-set fixture (spike-057 12 traces) for the detection-recall test — Req-5a → **08.2-05-02**
- [ ] `internal/toolselectstore/store_e2e_test.go` (tag `reasoning_live`) — `:ToolSelectionExample` round-trip → **08.2-05-01**
- [ ] NEW behavior-equivalence test in `internal/agent/prompt/` — semindex-backed vs shipped tier verdicts (Req-4) → **08.2-02-02**
- [ ] NEW N-query cache-invariant test in `internal/agent/tools/` — extends `TestToolSearchDescriptionDeterministic` (Req-7) → **08.2-03-02**
- [ ] **Fix** the stale `reasoning_classifier_live_test.go` one-arg `NewReasoningClassifier(embedder)` call (compile blocker under `reasoning_live`) → **08.2-02-01** *(moved from 08.2-04-02: the compile repair now lands in Plan 02, Wave 2 — the earliest plan touching `internal/agent/prompt/` — so `-tags reasoning_live` builds through Waves 2-4. The 80/88 → 90/92 floor RAISE stays in 08.2-04-02; only the compile repair moved.)*
- [ ] NEW real-turn wiring proof in `internal/runner/` — asserts the tool-select learner's `Observe(request, usedTool)` is invoked from `runner_persist.go` persistEvent on the `agent.ToolInvocationEnd` branch (Open-Q #3 resolved; the detection loop is wired, not tested-but-not-wired) (Req-5a live wiring) → **08.2-05-03**

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 08.2-01-01 | 01 | 1 | Req-1, Req-8 | unit + -race | `go test -race ./internal/semindex/` | core.go, semindex.go, core_test.go | ⬜ pending |
| 08.2-01-02 | 01 | 1 | Req-1, Req-3(door) | unit + -race | `go test -race ./internal/semindex/` | classifier.go, ranker.go, {classifier,ranker}_test.go | ⬜ pending |
| 08.2-02-01 | 02 | 2 | Req-4(struct), D-01, **stale-call compile repair** | unit + -race + `reasoning_live` COMPILE | `go test -race ./internal/agent/prompt/` + `go build ./...` + `go build -tags reasoning_live ./internal/agent/prompt/` | reasoning_classifier.go, **reasoning_classifier_live_test.go (one-arg→two-arg repair)** | ✅ complete (96c1efa3) |
| 08.2-02-02 | 02 | 2 | Req-4(equiv) | unit | `go test -race -run Equivalence ./internal/agent/prompt/` | reasoning_classifier_equivalence_test.go | ✅ complete (044f14f7) |
| 08.2-03-01 | 03 | 2 | Req-1(dual-consumer), Req-2, Req-6, Req-7, Req-8 | unit + -race | `go test -race ./internal/agent/tools/` + `go build ./...` | search.go, search_fusion.go | ⬜ pending |
| 08.2-03-02 | 03 | 2 | Req-2, Req-3, Req-7 | unit + -race | `go test -race ./internal/agent/tools/` | search.go, search_test.go | ⬜ pending |
| 08.2-03-03 | 03 | 2 | Req-6, Req-8 | unit + bench | `go test -race ./internal/runner/ ./internal/agent/tools/` + `go test -bench=Rank -benchtime=3s` | runner.go, search_test.go | ⬜ pending |
| 08.2-04-01 | 04 | 3 | Req-5(mech), D-05 | unit + -race (goleak) | `go test -race ./internal/activelearn/ ./internal/reasoninglearn/` | activelearn.go, learner.go, learner_test.go, reasoninglearn/learner.go | ⬜ pending |
| 08.2-04-02 | 04 | 3 | Req-4(raise), D-04 | unit (non-regression proxy) + **live** `reasoning_live` | `go build -tags reasoning_live ./internal/agent/prompt/` + `go test -race ./internal/agent/prompt/` | reasoning_classifier_live_test.go (**floor RAISE 80/88→90/92 only — the compile repair landed in 08.2-02-01**), reasoning_classifier.go | ⬜ pending |
| 08.2-05-01 | 05 | 4 | Req-5, D-07 | unit + **live** `reasoning_live` | `go test -race ./internal/toolselectstore/` + `go vet -tags reasoning_live ./internal/toolselectstore/` | toolselectstore/store.go, store_e2e_test.go | ⬜ pending |
| 08.2-05-02 | 05 | 4 | Req-5a, D-05, D-06 | unit + -race | `go test -race ./internal/toolselectlearn/` | toolselectlearn/{detector,oracle}.go, loop_test.go | ⬜ pending |
| 08.2-05-03 | 05 | 4 | Req-5b, Req-5c, D-07, **Open-Q #3 live wiring** | unit + -race | `go test -race ./internal/agent/tools/ ./internal/runner/` + `grep -c 'Observe(' internal/runner/runner_persist.go` | search.go, search_fusion.go, search_learn_test.go, runner.go, **runner_persist.go (Observe capture site)** | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live corpus accuracy ≥ 90% / ≥ 92% | Req-4 | needs live granite :8081 (operator-gated `reasoning_live` tier) | `go test -tags reasoning_live -run TestReasoningClassifierLive ./internal/agent/prompt/` with granite up |
| ≤ 20ms mean query+rank (total) | Req-8 | needs live granite (embed dominates the budget) | benchmark at live corpus size with granite up (`-benchtime=3s`) |
| `:ToolSelectionExample` round-trip | Req-5/D-07 | needs live Neo4j (`reasoning_live` tier — the SAME tag also gates this store, documented in-file per W-3) | `go test -tags reasoning_live -run ToolSelect ./internal/toolselectstore/` with Neo4j up |

---

## Validation Sign-Off

- [x] All tasks have an `<automated>` verify or a Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s (quick command)
- [x] `nyquist_compliant: true` set in frontmatter (every task maps to a matrix row)

**Approval:** planned — ready for `/gsd-execute-phase 08.2`
