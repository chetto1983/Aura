---
phase: 00-prd-amendments
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md  # Slice 11d (HNSW M=32) + new §Cross-cutting subsection or inline Slice 11d acceptance — non-overlapping with plans 01/02/03/04
  - docs/aura-quality-snapshot.md  # NEW file (Amendment #20 seed)
autonomous: true
commit_per_plan: false
phase_commit_aggregator: false
requirements: [PRD-01]

must_haves:
  truths:
    - "Operator reads docs/aura-quality-snapshot.md and observes the file exists with seed schema (Amendment #20 — ROADMAP success criterion #4)"
    - "Operator greps docs/aura-quality-snapshot.md for 'HNSW M=32', 'SandboxEscapeBench', 'recall@5', 'p95', 'cache hit rate' and observes all five metric categories present"
    - "Operator greps prd.md for 'docs/aura-quality-snapshot.md' and observes the living-doc reference + CI gate in Slice 11d acceptance"
    - "Operator greps prd.md for 'HNSW M=32' (or 'M=32') and observes the HNSW config override documented in Slice 11d"
  artifacts:
    - path: "docs/aura-quality-snapshot.md"
      provides: "Living-doc seed: quality matrix for Aura (recall@5, p95 latency, cache hit rate, escape rate, snippet success rate) — CI-gated regression check"
      min_lines: 60
      contains: "HNSW M=32"
    - path: "prd.md"
      provides: "Slice 11d HNSW M=32 + docs/aura-quality-snapshot.md CI gate + cross-link to amendment #20"
      contains: "docs/aura-quality-snapshot.md"
  key_links:
    - from: "prd.md Slice 11d (memory hybrid retrieval)"
      to: "docs/aura-quality-snapshot.md HNSW M=32 metric row"
      via: "pre-merge benchmark + CI regression gate"
      pattern: "docs/aura-quality-snapshot\\.md"
    - from: "docs/aura-quality-snapshot.md seed schema"
      to: "Phase 5 sandbox escape rate + Phase 6 cache hit rate + Phase 15 recall/nDCG"
      via: "per-phase update mandate (CI gate fails if snapshot stale on relevant PRs)"
      pattern: "Phase \\d"
---

<objective>
Apply Quality gate cluster — PRD amendment #20 from `.planning/research/SUMMARY.md` "PRD Amendments Required" table (lines 64-89): establish `docs/aura-quality-snapshot.md` as living-doc with seed schema + CI gate that fails any PR affecting Phase 5 (sandbox), Phase 6 (cache), Phase 11/15 (memory retrieval) which does NOT update the corresponding snapshot row.

Purpose: User memory `feedback_aura_as_product` (cited in SUMMARY.md row #20 source column) makes "Aura come prodotto, non playground" the operating principle — quality matrix obbligatorio, max 2 fasi staged avanti, ogni wave gate su numeri (Recall@5/nDCG@10/p95). The living-doc is the contract surface for that principle. SUMMARY.md also explicitly bumps HNSW `M=32` (not the default 16) as a Slice 11d-specific config (mentioned in row #20 alongside the snapshot).

Output:
- prd.md: Slice 11d acceptance bullets updated to reference docs/aura-quality-snapshot.md + explicit `HNSW M=32` config; cross-link to amendment #20.
- docs/aura-quality-snapshot.md: NEW file with seed schema (5 metric categories × per-phase rows), markers indicating which phases populate which rows, and the CI gate contract documented inline.

NO commit (deferred to 00-06).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aura/.planning/PROJECT.md
@D:/Aura/.planning/ROADMAP.md
@D:/Aura/.planning/STATE.md
@D:/Aura/.planning/research/SUMMARY.md
@D:/Aura/.planning/REQUIREMENTS.md
@D:/Aura/prd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/aura-quality-snapshot.md seed file</name>
  <files>D:/Aura/docs/aura-quality-snapshot.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 89: `| 20 | Establish docs/aura-quality-snapshot.md living doc + CI gate | Pitfall #27 + memory feedback_aura_as_product | New + Gate 1/3 |`
    - D:/Aura/.planning/research/SUMMARY.md line 121 ("Memory (P15) last: most downstream + pre-merge benchmarks (recall@5 / nDCG@10 / p95 @ 1K/10K/100K) gate via `docs/aura-quality-snapshot.md` living doc.")
    - User memory `feedback_aura_as_product`: "Aura come prodotto, non playground — quality matrix obbligatorio, max 2 fasi staged avanti, ogni wave gate su numeri (Recall@5/nDCG@10/p95), `docs/aura-quality-snapshot.md` living doc, CI gates su regressione qualità."
    - D:/Aura/.planning/ROADMAP.md Phase 5 success criteria (sandbox escape rate < 5%)
    - D:/Aura/.planning/ROADMAP.md Phase 6 success criteria (cache hit rate ≥ 80% on DeepSeek-V4 Flash)
    - D:/Aura/.planning/ROADMAP.md Phase 11 success criteria (snippet save/exec via sandbox 2b session)
    - D:/Aura/.planning/ROADMAP.md Phase 15 success criteria (recall@5 ≥ 0.8 @ 1K/10K/100K, p95 vector search ≤ 30ms, HNSW M=32)
    - D:/Aura/.planning/ROADMAP.md Phase 13 success criteria (Telegram MarkdownV2 escape fuzz green per Pitfall #18)
    - PARENT directory `D:/Aura/docs/` does NOT exist (confirmed via `ls docs/` returning "No such file or directory"). MUST be created.
  </read_first>
  <action>
    Step 1 (verify parent directory): Run `mkdir -p D:/Aura/docs` to ensure parent exists before Write.

    Step 2 (create file): Write `D:/Aura/docs/aura-quality-snapshot.md` with the following structure (concrete content; the Write tool — NOT cat/heredoc per CLAUDE.md).

    The file MUST contain ALL of the following sections in order:

    1. YAML front-matter or just title: `# Aura Quality Snapshot (living doc)`
    2. "Created" date `2026-05-29` and "Last updated" placeholder.
    3. **Purpose** paragraph (≈4 sentences): explains that the file is the contract surface between Aura phases — every phase that touches a measured quality dimension (sandbox escape, cache hit, retrieval recall/p95, snippet success, MarkdownV2 escape fuzz) MUST update the relevant row in this file as part of its Gate 3 DoD. CI gate enforces: PRs that modify code under specific paths fail if the corresponding snapshot row's `last_measured_at` date is older than the PR's branch base SHA. Cites amendment #20 (SUMMARY.md table) and user feedback `feedback_aura_as_product`.
    4. **Quality matrix** — 5 rows minimum, table with columns `Metric | Target | Last measured | Last value | Owner phase | CI gate path`:
       - Row 1: `Sandbox escape rate (SandboxEscapeBench UK AISI Mar 2026) | < 5% | YYYY-MM-DD (placeholder — populated by Phase 5) | TBD | Phase 5 Slice 2a | internal/sandbox/**, sandbox/Dockerfile, sandbox/seccomp.json`
       - Row 2: `KV cache hit rate (DeepSeek-V4 Flash, 20-turn replay) | ≥ 80% | YYYY-MM-DD (placeholder — populated by Phase 6) | TBD | Phase 6 Slice 4 | internal/llm/**, scripts/cache_invariant_audit.sh`
       - Row 3: `GraphRAG retrieval recall@5 @ 100K corpus | ≥ 0.8 | YYYY-MM-DD (placeholder — populated by Phase 15) | TBD | Phase 15 Slice 11d | internal/memory/**, internal/db/migrations/neo4j/**`
       - Row 4: `Vector search p95 latency @ 100K corpus | ≤ 30ms | YYYY-MM-DD (placeholder — populated by Phase 15) | TBD | Phase 15 Slice 11d | internal/memory/retrieval/**, sidecar aura-llama-embed config`
       - Row 5: `Telegram MarkdownV2 escape fuzz (10K Unicode inputs, 400 Bad Request rate) | = 0% | YYYY-MM-DD (placeholder — populated by Phase 13) | TBD | Phase 13 Slice 9b | internal/channels/telegram/mdv2.go`
       - Row 6: `Skill snippet exec success rate (sandbox 2b session, Phase 11 corpus 50 snippets) | ≥ 95% | YYYY-MM-DD (placeholder — populated by Phase 11) | TBD | Phase 11 Slice 7e-core | internal/skills/snippet/**, internal/sandbox/sessions/**`
    5. **HNSW configuration baseline (amendment #20 cross-ref)** — short section: documents the override `M=32` (NOT Neo4j's default 16). Rationale: SUMMARY.md "Honorable mentions (P0/P1 hitting specific slices): HNSW M=32 not default 16 (11a+11d)". Higher M = better recall at ingestion cost; chosen because Aura's corpus is bounded (100K chunks typical, 1M max) and recall@5 ≥ 0.8 is non-negotiable per Phase 15 success criterion.
    6. **CI gate contract** — describes the script (to be authored in Phase 15 alongside the first non-placeholder snapshot row): `scripts/quality_snapshot_gate.sh` reads the table, identifies which rows' `CI gate path` glob matches any file changed in the PR, asserts those rows' `Last measured` date is newer than the PR's merge-base commit date. Stale row → fail with explicit error `quality snapshot row '<metric>' stale — owner Phase X must re-measure and update before merge (amendment #20)`.
    7. **How to update** — 4-step operator runbook: (a) run the relevant pre-merge benchmark, (b) record the value, (c) update the `Last measured` and `Last value` columns, (d) commit alongside the implementation change.
    8. **Cross-phase dependency note** — quotes from user memory `feedback_aura_as_product`: "max 2 fasi staged avanti, ogni wave gate su numeri". Lists which phases gate via which rows.

    No markdown code-fenced shell snippets — the script is referenced by path only, authored later.

    Minimum file length: ≥ 60 lines of substantive content (target ~80-100 lines).
  </action>
  <acceptance_criteria>
    - `test -f D:/Aura/docs/aura-quality-snapshot.md` exits 0
    - `wc -l D:/Aura/docs/aura-quality-snapshot.md` reports ≥ 60 lines
    - `head -50 D:/Aura/docs/aura-quality-snapshot.md | grep -c "Aura Quality Snapshot"` returns at least 1
    - `grep -c "HNSW M=32" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1
    - `grep -c "SandboxEscapeBench" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1
    - `grep -c "recall@5" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1
    - `grep -c "p95" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1
    - `grep -c "cache hit rate" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1 (case-insensitive: `grep -ci`)
    - `grep -c "amendment #20" D:/Aura/docs/aura-quality-snapshot.md` returns at least 2
    - `grep -c "Phase 5" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1
    - `grep -c "Phase 6" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1
    - `grep -c "Phase 15" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1
    - `grep -c "quality_snapshot_gate\.sh" D:/Aura/docs/aura-quality-snapshot.md` returns at least 1 (the script reference)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test -f docs/aura-quality-snapshot.md &amp;&amp; test $(wc -l &lt; docs/aura-quality-snapshot.md) -ge 60 &amp;&amp; head -50 docs/aura-quality-snapshot.md | grep -q "Aura Quality Snapshot" &amp;&amp; grep -q "HNSW M=32" docs/aura-quality-snapshot.md &amp;&amp; grep -q "SandboxEscapeBench" docs/aura-quality-snapshot.md &amp;&amp; grep -q "recall@5" docs/aura-quality-snapshot.md &amp;&amp; grep -q "p95" docs/aura-quality-snapshot.md &amp;&amp; grep -qi "cache hit rate" docs/aura-quality-snapshot.md &amp;&amp; test $(grep -c "amendment #20" docs/aura-quality-snapshot.md) -ge 2 &amp;&amp; grep -q "Phase 5" docs/aura-quality-snapshot.md &amp;&amp; grep -q "Phase 6" docs/aura-quality-snapshot.md &amp;&amp; grep -q "Phase 15" docs/aura-quality-snapshot.md &amp;&amp; grep -q "quality_snapshot_gate\.sh" docs/aura-quality-snapshot.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>docs/aura-quality-snapshot.md exists with ≥60 lines, contains all 5 metric categories, references the CI gate script, references amendment #20 at least twice, names phases 5/6/15 as owners.</done>
</task>

<task type="auto">
  <name>Task 2: Wire amendment #20 references into prd.md (Slice 11d HNSW M=32 + living-doc CI gate)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 89 (row #20)
    - D:/Aura/.planning/research/SUMMARY.md line 62 ("HNSW M=32 not default 16 (11a+11d)")
    - D:/Aura/.planning/REQUIREMENTS.md line 50 (UX-08: "HNSW `M=32` (NON default 16). [Slice 11d + amendment #20]")
    - D:/Aura/.planning/ROADMAP.md Phase 15 success criterion: "HNSW `M=32` per Amendment #20"
    - D:/Aura/prd.md lines 3217-3218 (Slice 11a Cypher CREATE VECTOR INDEX statements with `vector.dimensions: 768` — these are where M=32 must be injected via `\`vector.hnsw.m\`: 32` option, or referenced as "set at index creation time per amendment #20")
    - D:/Aura/prd.md lines 3439-3447 (Slice 11d acceptance section)
    - D:/Aura/prd.md line 1455 (existing goleak bullet — context for cross-cutting acceptance patterns)
  </read_first>
  <action>
    Apply Amendment #20 — prd.md edits:

    Edit 1 (Slice 11a Cypher schema block at lines 3217-3218): the CREATE VECTOR INDEX statements currently specify `vector.dimensions: 768, vector.similarity_function: 'cosine'` only. For each of the 4 vector index creation statements (chunk_embedding, entity_embedding, community_embedding, agent_insight_embedding), append the HNSW M=32 option. Replace each `OPTIONS {indexConfig: {`vector.dimensions`: 768, `vector.similarity_function`: 'cosine'}};` with `OPTIONS {indexConfig: {`vector.dimensions`: ${AURA_EMBED_DIMENSIONS}, `vector.similarity_function`: 'cosine', `vector.hnsw.m`: 32, `vector.hnsw.ef_construction`: 200}}; -- amendment #20: M=32 (NOT default 16) per recall@5 ≥ 0.8 @ 100K corpus`

    Note: the env-substitution `${AURA_EMBED_DIMENSIONS}` cross-references amendment #18 work in plan 04 (Slice 11a acceptance bullet about migration-time env substitution). The `vector.hnsw.m: 32` is amendment #20 specific.

    Edit 2 (Slice 11d acceptance at lines 3439-3447): immediately after the existing bullet about LLM re-ranker top-K, insert:
    "- [ ] **HNSW M=32 baseline (amendment #20, UX-08)**: vector index creation MUST set `vector.hnsw.m: 32` (NOT Neo4j default 16) + `vector.hnsw.ef_construction: 200`. Acceptance test: post-migration, `SHOW INDEXES YIELD name, options` returns `m=32` for all 4 vector indexes. Pre-merge benchmark recall@5 ≥ 0.8 @ 1K/10K/100K synthetic corpus (run `scripts/memory_recall_bench.sh` — file authored in Phase 15)."
    "- [ ] **`docs/aura-quality-snapshot.md` CI gate (amendment #20)**: pre-merge for any PR touching `internal/memory/**` or `internal/db/migrations/neo4j/**`, `scripts/quality_snapshot_gate.sh` validates that the Phase 15 row in `docs/aura-quality-snapshot.md` has `Last measured` date ≥ PR base commit date. Stale → CI fail with `quality snapshot row 'GraphRAG retrieval recall@5 @ 100K corpus' stale — owner Phase 15 must re-measure and update before merge (amendment #20)`. Snapshot rows for Phase 5 (sandbox escape rate), Phase 6 (cache hit rate), Phase 11 (snippet success rate), Phase 13 (MarkdownV2 escape fuzz) gated by analogous path globs (see `docs/aura-quality-snapshot.md` CI gate contract section)."

    Edit 3 (Slice 11a schema acceptance section at line 3419 area — the `#### 11a — Schema` block): immediately after the existing bullet about vector index dimensions, insert:
    "- [ ] **HNSW M=32 verification (amendment #20)**: post-migration Cypher `SHOW INDEXES YIELD name, options WHERE name = 'chunk_embedding' RETURN options` MUST return `{vector.hnsw.m: 32, vector.hnsw.ef_construction: 200, vector.dimensions: 768, vector.similarity_function: 'cosine'}`. Same for entity_embedding, community_embedding, agent_insight_embedding."

    Edit 4 (cross-cutting reference in the new §Cross-cutting section authored by plan 04 task 1): IF that section exists (plan 04 wave 1 lands in parallel), add a sentence to the "Pitfall #3 mitigation summary" paragraph (last paragraph of the new §Cross-cutting section): "Living-doc gate amendment #20 (`docs/aura-quality-snapshot.md` + `scripts/quality_snapshot_gate.sh`) is the complementary forward-looking gate — Pitfall #3 catches structural mutation, amendment #20 catches measurement regression." If plan 04 has not landed yet (during parallel wave 1 execution), defer this edit to plan 00-06 aggregator. The executor of THIS plan SHOULD attempt the edit and tolerate failure (re-grep before insert to confirm the §Cross-cutting section is present; if missing, skip this edit and note in SUMMARY.md).

    Edit 5 (CLAUDE.md §Slice Q&A discipline cross-ref at prd.md line 1532 area "Gate 3 — Definition of Done"): immediately after the existing DoD bullet list, insert (locate the section that ends before line 1550 "Q&A revision protocol"):
    "- [ ] **Quality snapshot update (amendment #20)**: if the slice changes any code under a `docs/aura-quality-snapshot.md` CI gate path, the corresponding row's `Last measured` + `Last value` MUST be updated in the same commit. CI gate `scripts/quality_snapshot_gate.sh` runs in every PR; stale row blocks merge. Cross-reference user memory `feedback_aura_as_product` (\"max 2 fasi staged avanti, ogni wave gate su numeri\")."

    Per CLAUDE.md "READ BEFORE EDIT": before inserting in §Slice Q&A discipline Gate 3 section, re-read lines 1532-1549 to confirm the bullet list structure + the trailing `---` divider position.
  </action>
  <acceptance_criteria>
    - `grep -c "docs/aura-quality-snapshot\.md" D:/Aura/prd.md` returns at least 3 (Slice 11d acceptance + Slice 11a schema acceptance + §Slice Q&A discipline Gate 3 bullet)
    - `grep -c "HNSW M=32" D:/Aura/prd.md` returns at least 2 (Slice 11a Cypher comments + Slice 11d acceptance), OR `grep -c "vector\.hnsw\.m\`: 32" D:/Aura/prd.md` returns at least 4 (one per CREATE VECTOR INDEX)
    - `grep -c "amendment #20" D:/Aura/prd.md` returns at least 5
    - `grep -E "vector\.hnsw\.ef_construction\`: 200" D:/Aura/prd.md` returns at least 4 matches (one per CREATE VECTOR INDEX)
    - `grep -c "quality_snapshot_gate\.sh" D:/Aura/prd.md` returns at least 2 (Slice 11d acceptance + §Slice Q&A discipline Gate 3 bullet)
    - `grep -c "feedback_aura_as_product" D:/Aura/prd.md` returns at least 1 (§Slice Q&A discipline cross-ref)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "docs/aura-quality-snapshot\.md" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #20" prd.md) -ge 5 &amp;&amp; test $(grep -cE "vector\.hnsw\.m..: 32" prd.md) -ge 4 &amp;&amp; test $(grep -c "quality_snapshot_gate\.sh" prd.md) -ge 2 &amp;&amp; grep -q "feedback_aura_as_product" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; HNSW M=32 specified in 4 Cypher CREATE VECTOR INDEX statements; Slice 11d acceptance bullets reference quality-snapshot CI gate; Slice 11a schema acceptance verifies M=32 post-migration; §Slice Q&A discipline Gate 3 bullet enforces snapshot freshness on every slice that touches measured paths.</done>
</task>

</tasks>

<verification>
Phase-level grep fingerprints for quality-gate cluster (post Wave 1):
- `docs/aura-quality-snapshot.md` exists, ≥ 60 lines
- prd.md `docs/aura-quality-snapshot.md` ≥ 3 hits
- prd.md `vector.hnsw.m`: 32 ≥ 4 hits (one per CREATE VECTOR INDEX)
- prd.md `quality_snapshot_gate.sh` ≥ 2 hits
- prd.md `amendment #20` ≥ 5 hits
- snapshot file `HNSW M=32` ≥ 1 hit
- snapshot file `SandboxEscapeBench` + `recall@5` + `p95` + `cache hit rate` ≥ 1 hit each
- snapshot file `quality_snapshot_gate.sh` ≥ 1 hit (CI gate contract section)
</verification>

<success_criteria>
- 2 tasks complete
- 1 amendment (#20) reflected in prd.md (5+ references) AND in NEW docs/aura-quality-snapshot.md
- docs/ parent directory created
- 4 Cypher CREATE VECTOR INDEX statements in Slice 11a updated with `vector.hnsw.m: 32` + `vector.hnsw.ef_construction: 200`
- No `.go`, `.sql`, `.yaml`, or `.sh` files created (the gate script lands in Phase 15)
- No git commit created
</success_criteria>

<output>
On completion, create `.planning/phases/00-prd-amendments/00-05-SUMMARY.md` documenting:
- New file: `docs/aura-quality-snapshot.md` (line count, sections present)
- Lines edited in prd.md (Slice 11a Cypher, Slice 11a acceptance, Slice 11d acceptance, §Slice Q&A discipline Gate 3, optional §Cross-cutting cross-ref if plan 04 had landed)
- Acceptance grep counts at final state
- Confirmation: no commit, no code/sql/yaml/sh files created
</output>
