---
phase: 00-prd-amendments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md  # Slice 0.7 + Slice 0.9 + Slice 5 + Slice 8 + Slice 9b — non-overlapping with plans 02/03/04/05
  - CLAUDE.md  # single Go-version reference at line 123
autonomous: true
commit_per_plan: false  # phase emits ONE aggregated commit via 00-06-PLAN.md
phase_commit_aggregator: false
requirements: [PRD-01]

must_haves:
  truths:
    - "Operator greps prd.md for 'Go 1.25' and observes the string present (Amendment #1)"
    - "Operator greps prd.md for '5.26-community' and observes the string present (Amendment #2)"
    - "Operator greps prd.md for 'codeberg.org/readeck/go-readability/v2' and observes the string present (Amendment #3)"
    - "Operator greps prd.md for the custom MarkdownV2 escaper promotion (Amendment #4)"
    - "Operator greps prd.md for SHA-pin language on telebot.v4 and AG-UI Go SDK (Amendments #5, #6)"
  artifacts:
    - path: "prd.md"
      provides: "Stack drift fixes — Go version, Neo4j pin, readability lib swap, MarkdownV2 escaper default, telebot+AG-UI SDK SHA pins"
      contains: "Go 1.25"
    - path: "CLAUDE.md"
      provides: "Go version mention updated to 1.25 in the golang-modernize skills table"
      contains: "Go 1.25"
  key_links:
    - from: "prd.md (Slice 0.9 pre-req)"
      to: "Slice 0.9 acceptance line 465 (Go go.mod enforcement)"
      via: "version string"
      pattern: "Go 1\\.25"
    - from: "prd.md (Slice 0.7 Neo4j)"
      to: "Slice 0.7 acceptance + RAM table"
      via: "container image tag"
      pattern: "neo4j:5\\.26-community|5\\.26-community"
    - from: "prd.md (Slice 5 dependencies + html.go file target)"
      to: "go-readability replacement"
      via: "import path"
      pattern: "codeberg\\.org/readeck/go-readability/v2"
---

<objective>
Apply Stack drift cluster — PRD amendments #1 through #6 from `.planning/research/SUMMARY.md` "PRD Amendments Required" table (lines 64-89). These are precise string replacements and small-block edits in non-overlapping prd.md sections (Slice 0.7, Slice 0.9, Slice 5, Slice 8, Slice 9b) plus one one-line edit in CLAUDE.md.

Purpose: Seal the 6 stack drift items identified by independent research convergence (4 parallel researchers, 2026-05-29) BEFORE any code commit lands. PRD-first principle (CLAUDE.md §"PRD-first principle (absolute)") requires drift fixed in the source-of-truth document, not in code.

Output: prd.md updated with Amendments #1-6 applied; CLAUDE.md golang-modernize row updated. NO commit (deferred to phase aggregator plan 00-06).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aura/.planning/PROJECT.md
@D:/Aura/.planning/ROADMAP.md
@D:/Aura/.planning/STATE.md
@D:/Aura/.planning/research/SUMMARY.md
@D:/Aura/CLAUDE.md
@D:/Aura/prd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Amendments #1, #2 (Go 1.25 bump + Neo4j 5.26-community pin)</name>
  <files>D:/Aura/prd.md, D:/Aura/CLAUDE.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md lines 64-89 — canonical amendment table. The two rows in scope:
      `| 1 | Bump Go minimum to 1.25 (AG-UI requires 1.24.4) | Stack P1 | Slice 0.9 + CLAUDE.md |`
      `| 2 | Pin neo4j:5.26-community LTS (or 2026.05 rolling) | Stack P1 | Slice 0.7 + compose.yaml |`
    - D:/Aura/prd.md lines 247-248 (Slice 0.9 Pre-requisiti block: "Go 1.23+ (per `iter.Seq2` range-over-func)")
    - D:/Aura/prd.md line 465 (Slice 0.9 acceptance: "Go 1.23+ enforced in go.mod (`go 1.23`)")
    - D:/Aura/prd.md line 489 (Slice 0.9 rationale)
    - D:/Aura/prd.md line 518 (Slice 0.9 commit message template body)
    - D:/Aura/prd.md lines 87, 142, 143, 160, 196, 210, 214, 216 (every "Neo4j 5.x" / "neo4j:5-community" mention in Slice 0.5 RAM table and Slice 0.7)
    - D:/Aura/CLAUDE.md line 123 (only Go-version mention in CLAUDE.md, inside the skills table row for golang-modernize)
    - NOTE: there is no compose.yaml in this tabula-rasa repo (verified via `ls compose.yaml` returns "No such file"). Amendment #2's "+ compose.yaml" reference is therefore moot for this phase — prd.md is the only target. The compose.yaml will be authored in Phase 1 Slice 0.7 with the 5.26-community pin already mandated by the amended PRD.
  </read_first>
  <action>
    Apply Amendment #1 (Go 1.25 bump) — string replacements in prd.md and CLAUDE.md:
    - prd.md line 247: replace "Go 1.23+ (per `iter.Seq2` range-over-func)" with "Go 1.25+ (per `iter.Seq2` range-over-func; AG-UI Go SDK requires 1.24.4+ — pin to 1.25 for headroom; amendment #1 from SUMMARY.md table)"
    - prd.md line 465: replace "Go 1.23+ enforced in `go.mod` (`go 1.23`)" with "Go 1.25+ enforced in `go.mod` (`go 1.25`)"
    - prd.md line 489: replace "(Go 1.23+) + workflow agents built-in" with "(Go 1.25+) + workflow agents built-in"
    - prd.md line 518 (commit message template body): replace "Go 1.23 minimum (range-over-func iter.Seq2). Enforce in go.mod." with "Go 1.25 minimum (range-over-func iter.Seq2, AG-UI SDK 1.24.4+ requirement). Enforce in go.mod."
    - prd.md line 241 (Slice 0.9 Goal sentence): replace "Streaming events idiomatico Go 1.23+ via `iter.Seq2[*Event, error]`" with "Streaming events idiomatico Go 1.25+ via `iter.Seq2[*Event, error]`"
    - CLAUDE.md line 123 (skills table row): replace "Tutte slice (Go 1.23+)" with "Tutte slice (Go 1.25+)"
    Leave the single Go 1.24+ synctest reference at prd.md line 1459 ALONE (it is a forward-compat note about Go 1.24+ synctest API, NOT a minimum-version declaration — bumping to 1.25 keeps that true).

    Apply Amendment #2 (Neo4j 5.26-community LTS pin) — string replacements in prd.md ONLY (no compose.yaml exists yet, per `<read_first>`):
    - prd.md line 87 (Slice 0.5 RAM table row): replace "Neo4j 5.x Community" with "Neo4j 5.26-community LTS"
    - prd.md line 142: replace "**Neo4j 5.x Community** (container `neo4j:5-community`, ~1.5-2 GB RAM idle)" with "**Neo4j 5.26-community LTS** (container `neo4j:5.26-community`, ~1.5-2 GB RAM idle; LTS pinned per amendment #2 — avoids CalVer ambiguity post-5.x)"
    - prd.md line 160: replace "auto-download Neo4j Community feature 5.x" with "auto-download Neo4j Community feature 5.26 LTS"
    - prd.md line 196 (sandbox/compose.yaml diff row in file targets table): replace "`neo4j:5-community`" with "`neo4j:5.26-community`"
    - prd.md line 210: replace "Neo4j 5.x Community + APOC + GDS" with "Neo4j 5.26-community LTS + APOC + GDS"

    Per CLAUDE.md "READ BEFORE EDIT" rule, re-read each numbered line above before editing to confirm exact pre-state.
  </action>
  <acceptance_criteria>
    - `grep -c "Go 1\.25" D:/Aura/prd.md` returns at least 5 (the 5 prd.md occurrences listed in &lt;action&gt;: lines 241, 247, 465, 489, 518)
    - `grep -c "Go 1\.25" D:/Aura/CLAUDE.md` returns at least 1
    - `grep -E "^- Go 1\.23\+" D:/Aura/prd.md` returns NO matches (old Slice 0.9 pre-req bullet must be gone)
    - `grep -c "5\.26-community" D:/Aura/prd.md` returns at least 4 (lines 87, 142, 196, 210 plus any inline reference added)
    - `grep -E "neo4j:5-community[^.]" D:/Aura/prd.md` returns NO matches (old unpinned tag must be gone; the new tag `neo4j:5.26-community` is acceptable since `.` is not `[^.]`)
    - `grep -E "Neo4j 5\.x Community" D:/Aura/prd.md` returns NO matches
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "Go 1\.25" prd.md) -ge 5 &amp;&amp; test $(grep -c "Go 1\.25" CLAUDE.md) -ge 1 &amp;&amp; test $(grep -c "5\.26-community" prd.md) -ge 4 &amp;&amp; ! grep -E "^- Go 1\.23\+" prd.md &amp;&amp; ! grep -E "Neo4j 5\.x Community" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks above succeed; no `.go` files modified; only prd.md and CLAUDE.md touched.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Amendment #3 (readability lib migration to codeberg.org/readeck)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 72: `| 3 | Migrate to codeberg.org/readeck/go-readability/v2 | Stack P1 | Slice 5 |` — context: PRD's `go-shiori/go-readability` was deprecated 2025-12-05; readeck is the maintained fork.
    - D:/Aura/.planning/research/SUMMARY.md line 30 (canonical recommendation: "html-to-markdown v2.5.1 + codeberg.org/readeck/go-readability/v2 (PRD's go-shiori/go-readability deprecated 2025-12-05, must migrate)")
    - D:/Aura/prd.md lines 1594-1595 (Slice 5 narrative: "HTML→Markdown via `go-shiori/go-readability`")
    - D:/Aura/prd.md line 1629 (Slice 5 file targets table row for `internal/web/html.go`)
    - D:/Aura/prd.md lines 1664-1665 (Slice 5 open question about `go-readability` maintained status — this OQ is now CLOSED by amendment #3 and should be marked accordingly)
  </read_first>
  <action>
    Apply Amendment #3 — replace every mention of `go-shiori/go-readability` in prd.md with `codeberg.org/readeck/go-readability/v2`:
    - prd.md line 1594-1595: rewrite the sentence segment "HTML→Markdown via `go-shiori/go-readability` + `JohannesKaufmann/html-to-markdown/v2`" to "HTML→Markdown via `codeberg.org/readeck/go-readability/v2` (readeck fork — go-shiori upstream deprecated 2025-12-05 per amendment #3) + `JohannesKaufmann/html-to-markdown/v2`"
    - prd.md line 1629 (`internal/web/html.go` file target row): replace "Wrapper su go-readability + html-to-markdown" with "Wrapper su `codeberg.org/readeck/go-readability/v2` (readeck fork, amendment #3) + `html-to-markdown/v2`"
    - prd.md lines 1664-1665 (open question #2): rewrite to close the OQ — replace the entire OQ block "2. **`go-readability` mantenuto?** È stato fork-ato lentamente; alternative: `dom-distiller` port, scraping minimal manuale. → *Default proposto: SÌ go-readability + html-to-markdown/v2, sono stabili e il pre-rewrite li usava bene.*" with "2. **~~`go-readability` mantenuto?~~ → CHIUSA (amendment #3, 2026-05-29):** go-shiori upstream deprecated 2025-12-05. Migrazione a `codeberg.org/readeck/go-readability/v2` (readeck Go fork, actively maintained, same API surface for `Article` parsing). No-op refactor a livello chiamante; import path swap solo."

    Per CLAUDE.md "NEVER SUPPOSE": after editing, re-grep to confirm zero residual `go-shiori/go-readability` mentions remain.
  </action>
  <acceptance_criteria>
    - `grep -c "codeberg\.org/readeck/go-readability/v2" D:/Aura/prd.md` returns at least 3 (the 3 edits above)
    - `grep -E "go-shiori/go-readability" D:/Aura/prd.md` returns NO matches
    - `grep -c "amendment #3" D:/Aura/prd.md` returns at least 2 (the OQ closure + at least one inline citation)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "codeberg\.org/readeck/go-readability/v2" prd.md) -ge 3 &amp;&amp; ! grep -E "go-shiori/go-readability" prd.md &amp;&amp; test $(grep -c "amendment #3" prd.md) -ge 2 &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks above succeed; zero residual go-shiori references; readeck fork mandated in 3 prd.md sites.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Amendments #4, #5, #6 (MarkdownV2 escaper default + telebot SHA pin + AG-UI SDK SHA pin)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md lines 73-75: rows #4, #5, #6:
      `| 4 | Promote custom ~80 LOC MarkdownV2 escaper to default (or HTML mode) | Stack P2 | Slice 9b |`
      `| 5 | Pin telebot.v4 commit SHA post-2026-05-08 | Stack P3 | Slice 9b |`
      `| 6 | Pin AG-UI Go SDK commit SHA post-2026-05-14 | Stack P3 | Slice 8 |`
    - D:/Aura/.planning/research/SUMMARY.md line 32 ("Custom ~80 LOC MarkdownV2 escaper (PRD's `eekstunt/telegramify-markdown-go` 4-star supply-chain risk — promote port to default OR pivot to HTML mode)")
    - D:/Aura/.planning/research/SUMMARY.md line 29 ("AG-UI Go SDK community (pseudo-version, pin SHA from 2026-05-14+)")
    - D:/Aura/.planning/research/SUMMARY.md line 30 ("`gopkg.in/telebot.v4` (untagged, pin SHA post-2026-05-08)")
    - D:/Aura/prd.md line 2320 (Slice 8 go.mod hint "require github.com/ag-ui-protocol/ag-ui/sdks/community/go v...")
    - D:/Aura/prd.md line 2430 (Slice 8 commit body "Dipendenza go.mod: github.com/ag-ui-protocol/ag-ui/sdks/community/go.")
    - D:/Aura/prd.md lines 2471-2472 (Slice 9b go.mod section: "require gopkg.in/telebot.v4 latest" and "require github.com/eekstunt/telegramify-markdown-go latest")
    - D:/Aura/prd.md line 2489 (Slice 9b decision table row 3: "Markdown library | `eekstunt/telegramify-markdown-go` (no custom port)")
    - D:/Aura/prd.md line 2687 (Slice 9b acceptance: "Markdown via `eekstunt/telegramify-markdown-go` (licenza + active maintenance VERIFICARE pre-merge: cercare commit history ultimi 90gg + LICENSE file MIT/Apache-2.0/BSD — se inactive >180gg O licenza incompat, fallback a port custom ~80 LOC del subset MarkdownV2 escapes)")
    - D:/Aura/prd.md line 2758 (Slice 9b commit body: "Markdown via eekstunt/telegramify-markdown-go (no custom port).")
  </read_first>
  <action>
    Apply Amendment #4 (custom ~80 LOC MarkdownV2 escaper promoted to default, replacing the eekstunt/telegramify dependency):
    - prd.md line 2472 (go.mod section): DELETE the entire line `require github.com/eekstunt/telegramify-markdown-go latest      // Markdown -> MarkdownV2 safe` — the dep is removed in favor of the in-tree escaper.
    - prd.md line 2489 (decision table row 3 "Markdown library"): replace "`eekstunt/telegramify-markdown-go` (no custom port)" with "Custom ~80 LOC MarkdownV2 escaper in `internal/channels/telegram/mdv2.go` (per amendment #4 — promotes port to default. Rationale: `eekstunt/telegramify-markdown-go` is 4-star supply-chain risk per SUMMARY.md Stack P2; in-tree escaper covers MarkdownV2 reserved-char subset, zero external dep)"
    - prd.md line 2687 (acceptance bullet about renderer markdown): replace the full bullet sentence "Markdown via `eekstunt/telegramify-markdown-go` (licenza + active maintenance VERIFICARE pre-merge: cercare commit history ultimi 90gg + LICENSE file MIT/Apache-2.0/BSD — se inactive >180gg O licenza incompat, fallback a port custom ~80 LOC del subset MarkdownV2 escapes), fallback plain text se entity invalid." with "Markdown via in-tree custom MarkdownV2 escaper `internal/channels/telegram/mdv2.go` (~80 LOC, per amendment #4 — promotes the port to default, eliminates `eekstunt/telegramify-markdown-go` dep). Acceptance: fuzz 10K random Unicode inputs → every output round-trips through Telegram `sendMessage` without `400 Bad Request: can't parse entities` (Pitfall #18 mitigation). Fallback plain text if escaping fails."
    - prd.md line 2758 (commit message template body): replace "Markdown via eekstunt/telegramify-markdown-go (no custom port)." with "Markdown via in-tree custom MarkdownV2 escaper (per amendment #4, no external dep)."

    Apply Amendment #5 (telebot.v4 SHA pin post-2026-05-08):
    - prd.md line 2471 (go.mod section): replace "require gopkg.in/telebot.v4 latest                              // bot library (master usato anche)" with "require gopkg.in/telebot.v4 <SHA-post-2026-05-08>                 // bot library — SHA-pinned per amendment #5 (untagged repo; pin to specific commit ≥ 2026-05-08 to avoid silent master drift). At Phase 1 Slice 9b implementation time, resolve to the latest commit SHA via `go list -m -json gopkg.in/telebot.v4@HEAD` and replace the placeholder."

    Apply Amendment #6 (AG-UI Go SDK SHA pin post-2026-05-14):
    - prd.md line 2320 (Slice 8 go.mod hint): replace "require github.com/ag-ui-protocol/ag-ui/sdks/community/go v..." with "require github.com/ag-ui-protocol/ag-ui/sdks/community/go <SHA-post-2026-05-14> // SHA-pinned per amendment #6 (pseudo-version repo; pin to commit ≥ 2026-05-14). At Phase 12 Slice 8 impl time, resolve via `go list -m -json github.com/ag-ui-protocol/ag-ui/sdks/community/go@HEAD`."
    - prd.md line 2430 (commit body): replace "Dipendenza go.mod: github.com/ag-ui-protocol/ag-ui/sdks/community/go." with "Dipendenza go.mod: github.com/ag-ui-protocol/ag-ui/sdks/community/go (SHA-pinned per amendment #6 — no pseudo-version `latest`)."

    Add a new Slice 8 acceptance bullet (insert after the existing acceptance list, before File targets — read the section around line 2370-2390 first to find the exact insertion point):
    - Insert "- [ ] **go.mod pin verification (amendment #6)**: `go.mod` for `github.com/ag-ui-protocol/ag-ui/sdks/community/go` MUST be a specific commit SHA (NOT `latest`, NOT a date-based pseudo-version `v0.0.0-YYYYMMDDHHMMSS-xxxxxxxxxxxx` resolved at install time). CI gate: `grep -E '^require github\\.com/ag-ui-protocol/ag-ui/sdks/community/go [a-f0-9]{40}$' go.mod` must return exactly 1 match."
  </action>
  <acceptance_criteria>
    - `grep -E "eekstunt/telegramify-markdown-go" D:/Aura/prd.md` returns NO matches
    - `grep -c "amendment #4" D:/Aura/prd.md` returns at least 3
    - `grep -c "amendment #5" D:/Aura/prd.md` returns at least 1
    - `grep -c "amendment #6" D:/Aura/prd.md` returns at least 3 (go.mod hint + commit body + new acceptance bullet)
    - `grep -E "SHA-post-2026-05-08" D:/Aura/prd.md` returns at least 1 match
    - `grep -E "SHA-post-2026-05-14" D:/Aura/prd.md` returns at least 1 match
    - `grep -E "telebot\.v4 latest" D:/Aura/prd.md` returns NO matches (untagged `latest` must be gone)
    - `grep -E "ag-ui/sdks/community/go v\.\.\." D:/Aura/prd.md` returns NO matches
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; ! grep -E "eekstunt/telegramify-markdown-go" prd.md &amp;&amp; test $(grep -c "amendment #4" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #5" prd.md) -ge 1 &amp;&amp; test $(grep -c "amendment #6" prd.md) -ge 3 &amp;&amp; grep -q "SHA-post-2026-05-08" prd.md &amp;&amp; grep -q "SHA-post-2026-05-14" prd.md &amp;&amp; ! grep -E "telebot\.v4 latest" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; eekstunt dep removed; SHA-pin placeholders present for telebot.v4 and AG-UI SDK; new Slice 8 acceptance bullet enforcing 40-hex-char SHA pin present.</done>
</task>

</tasks>

<verification>
Phase-level (post-Wave-1 aggregation in 00-06): grep the final prd.md for all 6 stack-drift fingerprints:
- "Go 1.25" (≥ 5 prd.md hits + ≥ 1 CLAUDE.md hit)
- "5.26-community" (≥ 4 hits)
- "codeberg.org/readeck/go-readability/v2" (≥ 3 hits)
- in-tree MarkdownV2 escaper language (`mdv2.go` ≥ 1 hit) + zero residual eekstunt mentions
- telebot.v4 SHA-pin placeholder present
- AG-UI SDK SHA-pin placeholder present + new Slice 8 acceptance bullet
</verification>

<success_criteria>
- 3 tasks complete
- 6 amendments (#1-6) reflected in prd.md and CLAUDE.md
- No `.go`, `.sql`, or `.yaml` files modified by this plan
- No git commit created (commit is aggregated in 00-06)
- File state ready for 00-06 aggregator to stage and commit
</success_criteria>

<output>
On completion, create `.planning/phases/00-prd-amendments/00-01-SUMMARY.md` documenting:
- Lines changed in prd.md (per amendment, list line numbers touched)
- Lines changed in CLAUDE.md (1 line, line 123)
- Acceptance grep counts at final state
- Confirmation that no commit was created and no code files were touched
</output>
