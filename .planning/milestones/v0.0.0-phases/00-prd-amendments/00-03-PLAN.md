---
phase: 00-prd-amendments
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md  # Slice 3 + Slice 7d + Slice 7e + Slice 11 + Slice 11e — non-overlapping with plans 01/02/04/05
autonomous: true
commit_per_plan: false
phase_commit_aggregator: false
requirements: [PRD-01]

must_haves:
  truths:
    - "Operator greps prd.md for ':AgentInsight retrieval cached' and observes the architectural cache rule in Slice 11e (Amendment #11)"
    - "Operator greps prd.md for 'MAX_SPAWN_DEPTH=2' and observes the v1 cap in Slice 3 (Amendment #12 — scope reduction)"
    - "Operator greps prd.md for 'Slice 7e-core' AND 'Slice 7f' and observes the v1/v1.x split (Amendment #13)"
    - "Operator greps prd.md for 'enable-catalog' and observes the opt-in flag for skill.catalog (Amendment #14)"
  artifacts:
    - path: "prd.md"
      provides: "Architecture spec gaps — :AgentInsight cache TTL, swarm scope cap, Slice 7e split, skill.catalog opt-in"
      contains: "AURA_AGENT_INSIGHT_CACHE_TTL_SEC"
  key_links:
    - from: "prd.md Slice 11e :AgentInsight retrieval"
      to: "Slice 4 KV cache invariant (messages[2] stability)"
      via: "N-minute cache TTL on cross-conv insight query"
      pattern: "AURA_AGENT_INSIGHT_CACHE_TTL_SEC"
    - from: "prd.md Slice 3 swarm coordinator"
      to: "ParallelAgent reuse + 2-deep cap"
      via: "MAX_SPAWN_DEPTH=2 v1 cap"
      pattern: "MAX_SPAWN_DEPTH=2"
    - from: "prd.md Slice 7e header"
      to: "v1.x Slice 7f deferred (auto-suggest analyzer)"
      via: "split documentation in Slice 7 atomicity note"
      pattern: "Slice 7e-core|Slice 7f"
    - from: "prd.md Slice 7d skill.catalog acceptance"
      to: "aura skills enable-catalog opt-in command"
      via: "default-disabled flag"
      pattern: "enable-catalog"
---

<objective>
Apply Architecture spec gaps cluster — PRD amendments #11 through #14 from `.planning/research/SUMMARY.md` "PRD Amendments Required" table (lines 64-89). Four edits sealing architectural invariants:
- #11: `:AgentInsight` retrieval must be cached N minutes to preserve KV cache `messages[2]` stability (otherwise Slice 4 invariant breaks when Slice 11e ships).
- #12: Slice 3 swarm scope-reduced to ParallelAgent + 2-deep cap for v1 (full N-deep + DM-by-ID + tier-mapped models deferred to v2 per STATE.md SWARM-V2-01).
- #13: Slice 7e split into 7e-core (v1, save/execute + sandbox 2b) + 7f (v1.x, auto-suggest cluster analyzer) per STATE.md SKILL-V2-01.
- #14: `skill.catalog` hidden behind `aura skills enable-catalog` opt-in (security default-deny: HTML scrape of skills.sh untrusted by default).

Purpose: Lock the architectural decisions BEFORE code lands. #11 in particular is a cross-slice invariant — if Slice 11e ships without the cache, Slice 4 KV cache work is broken (see SUMMARY.md Risk #5, Pitfall #3 cross-cutting).

Output: prd.md updated with Amendments #11-14 applied. NO commit.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aura/.planning/PROJECT.md
@D:/Aura/.planning/ROADMAP.md
@D:/Aura/.planning/STATE.md
@D:/Aura/.planning/research/SUMMARY.md
@D:/Aura/.planning/REQUIREMENTS.md
@D:/Aura/prd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Amendment #11 (:AgentInsight retrieval cache TTL — Slice 11e + Slice 4 cross-ref)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 80: `| 11 | Specify :AgentInsight retrieval caching N minutes | Architecture spec gap (Risk #5) | Slice 11 |`
    - D:/Aura/.planning/research/SUMMARY.md line 16 ("1 architectural spec gap (`:AgentInsight` retrieval must be cached N minutes so `messages[2]` cache-stable — breaks Slice 4 invariant if added naively in Slice 11e)")
    - D:/Aura/.planning/research/SUMMARY.md line 52 ("**spec gap: `:AgentInsight` retrieval must be cached N minutes so `messages[2]` cache-stable** (breaks Slice 4 if added naively in 11e)")
    - User memory `reference_aura_cache_poisoning_sites_2026-05-27` — 6 cache poisoning sites mapped (file:line). Killer #6 = Context.SetAgentNote/SetSearchContext via rebuildSystemMessage. The new `:AgentInsight` injection at `messages[2]` is the 7th potential site if not cached.
    - D:/Aura/prd.md lines 3199, 3205 (Slice 11 Decisioni cumulate table — Storage tier 3-tier + Agent memory rows mentioning AgentInsight)
    - D:/Aura/prd.md lines 3350-3358 (Slice 11e journal.go + insight.go architecture description — note "Inject hook (Slice 0.9 PromptBuilder): Pre-conv: query top-3 :AgentInsight relevant via embedding. Inject come third system message (post Agent.md Slice 10). Cache-friendly: messages[0,1,2] stable cross-turn")
    - D:/Aura/prd.md lines 3448-3453 (Slice 11e Acceptance section)
    - D:/Aura/prd.md line 4291 (env table row `AURA_MEMORY_INSIGHT_INTERVAL_MIN=60`) — context for where new env row goes
    - D:/Aura/prd.md line 1342 (Slice 4 invariant test — "Test invariante: il tool manifest renderizzato 5 volte di seguito è byte-identico (cache poisoning guard, link a reference_aura_cache_poisoning_sites_2026-05-27)")
  </read_first>
  <action>
    Apply Amendment #11 — `:AgentInsight` retrieval cache TTL specification:

    Edit 1 (Slice 11e architecture block at lines 3350-3358): replace the Inject hook description (the lines "Inject hook (Slice 0.9 PromptBuilder): Pre-conv: query top-3 :AgentInsight relevant via embedding / Inject come third system message (post Agent.md Slice 10) / Cache-friendly: messages[0,1,2] stable cross-turn") with this expanded directive prose:

    "Inject hook (Slice 0.9 PromptBuilder):
                    #         Pre-conv: query top-K :AgentInsight relevant via embedding
                    #         Inject come third system message (post Agent.md Slice 10)
                    #         **Cache TTL (amendment #11 — Architecture spec gap):**
                    #         `AURA_AGENT_INSIGHT_CACHE_TTL_SEC=600` (default 10 min).
                    #         In-memory LRU keyed by (identity_id, query_embedding_hash).
                    #         During TTL window the SAME top-K subset is returned → messages[2]
                    #         is byte-identical turn-su-turn → KV cache hit on the third system
                    #         message (parity with messages[0] discipline of Slice 4).
                    #         Cache invalidation: explicit on `:AgentInsight` upsert (insight.go
                    #         analyzer goroutine triggers Invalidate(identity_id)); implicit on
                    #         TTL expiry. Without this cache, every turn re-queries Neo4j HNSW
                    #         and returns slightly different top-K (embedding similarity noise)
                    #         → messages[2] varies → Slice 4 invariant breaks → cache hit rate
                    #         collapses (see Pitfall #3 cross-slice, Risk #5 architectural spec
                    #         gap; reference_aura_cache_poisoning_sites_2026-05-27 site #7)."

    Edit 2 (Slice 11e Acceptance at lines 3448-3453): immediately after the existing PromptBuilder injection bullet, insert:
    "- [ ] **`:AgentInsight` retrieval cache (amendment #11)**: `internal/memory/agent/insight_cache.go` LRU (capacity 1024 entries, TTL `AURA_AGENT_INSIGHT_CACHE_TTL_SEC=600`). `Get(identity_id, query_embedding_hash) ([]Insight, bool)` + `Put(identity_id, query_embedding_hash, []Insight)` + `Invalidate(identity_id)`. Test invariant: 5 consecutive `PromptBuilder.Build(...)` calls within TTL → `messages[2]` SHA-256 identical (cross-link with `scripts/cache_invariant_audit.sh` from amendment #16). Test invalidation: insight.go analyzer Upsert → cache hit returns fresh data on next call within same TTL window."

    Edit 3 (Slice 11 Decisioni cumulate table at line 3205): replace the "Agent memory" row content "**Episodic + Insight + injection**: `:AgentEpisode` post-conv summary + `:AgentInsight` cross-conv pattern. Pre-conv inject top-K Insight relevant nel system prompt (cache-friendly, dopo Agent.md)." with "**Episodic + Insight + cached injection (amendment #11)**: `:AgentEpisode` post-conv summary + `:AgentInsight` cross-conv pattern. Pre-conv inject top-K Insight relevant nel system prompt come `messages[2]`. **In-memory LRU cache TTL `AURA_AGENT_INSIGHT_CACHE_TTL_SEC=600` (10 min)** — preserva byte-identity di `messages[2]` turn-su-turn (Slice 4 invariant cross-slice). Senza cache: Slice 4 KV cache si rompe quando Slice 11e atterra (Pitfall #3, Risk #5)."

    Edit 4 (Caps & Limits env table at line 4291): immediately BEFORE the row `AURA_MEMORY_INSIGHT_INTERVAL_MIN`, insert new row:
    "| `AURA_AGENT_INSIGHT_CACHE_TTL_SEC` | `600` (10 min) | cap | 11e | LRU cache TTL per `:AgentInsight` retrieval (amendment #11). Preserva `messages[2]` byte-identity cross-turn → Slice 4 KV cache hit. **Cross-slice invariant** — modifica solo se Slice 4 `cache_invariant_audit.sh` resta verde. |"

    Edit 5 (Slice 4 cross-reference at line 1342 area): immediately after the existing bullet about "Test invariante: il tool manifest renderizzato 5 volte...", insert:
    "- [ ] **Cross-slice forward-compat (amendment #11)**: `PromptBuilder.Build(history, tools, provider)` accepts optional `messages[2]` payload from `:AgentInsight` cache (Slice 11e injects). When Slice 11e is shipped, the PromptBuilder MUST treat `messages[2]` as a stable-prefix continuation (same byte-identity test extended to indices 0, 1, 2). Until 11e ships, `messages[2]` is absent and the test runs on `[0]` only. Acceptance test re-runs in Phase 15: assert SHA-256 of `messages[0]`, `messages[1]` (if Agent.md set), AND `messages[2]` (if Insight cache hit) ALL constant across 5 consecutive turns."

    Per CLAUDE.md "READ BEFORE EDIT": confirm the multi-line architecture block at 3350-3358 IS inside a fenced code block (it is — opened around 3340 ``` and closed before 3370 ```). The directive replacement preserves the fence and the `# ~XXX  ` comment style of that block.
  </action>
  <acceptance_criteria>
    - `grep -c "AURA_AGENT_INSIGHT_CACHE_TTL_SEC" D:/Aura/prd.md` returns at least 4 (architecture block + acceptance + decision table + env table row)
    - `grep -c "amendment #11" D:/Aura/prd.md` returns at least 5 (architecture block + acceptance + decision table + env table row + Slice 4 cross-ref)
    - `grep -c ":AgentInsight" D:/Aura/prd.md` returns at least 12 (existing 10 plus 2+ new)
    - `grep -E "messages\[2\]" D:/Aura/prd.md` returns at least 3 matches (architecture block + decision table + Slice 4 cross-ref)
    - `grep -E "^\| \`AURA_AGENT_INSIGHT_CACHE_TTL_SEC\`" D:/Aura/prd.md` returns exactly 1 (env table row)
    - `grep -E "insight_cache\.go" D:/Aura/prd.md` returns at least 1 match (acceptance bullet)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "AURA_AGENT_INSIGHT_CACHE_TTL_SEC" prd.md) -ge 4 &amp;&amp; test $(grep -c "amendment #11" prd.md) -ge 5 &amp;&amp; test $(grep -cE "^\| \`AURA_AGENT_INSIGHT_CACHE_TTL_SEC\`" prd.md) -eq 1 &amp;&amp; grep -q "messages\[2\]" prd.md &amp;&amp; grep -q "insight_cache\.go" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; AgentInsight cache TTL specified in 4 cross-referenced sites; new env var landed; Slice 4 forward-compat acceptance bullet added.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Amendments #12, #13 (Slice 3 swarm scope reduction + Slice 7e split)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 81: `| 12 | Reduce Slice 3 swarm v1 to ParallelAgent + 2-deep cap | Features over-scope | Slice 3 |`
    - D:/Aura/.planning/research/SUMMARY.md line 82: `| 13 | Split Slice 7e → 7e-core (v1) + 7f (v1.x auto-suggest) | Features over-scope | Slice 7e |`
    - D:/Aura/.planning/REQUIREMENTS.md line 34 (CAP-03 — locks v1 to ParallelAgent + 2-deep cap, "NO DM-by-ID, NO tier-mapped models in v1")
    - D:/Aura/.planning/REQUIREMENTS.md line 39 (CAP-08 — 7e-core; "NO cross-conv cluster auto-suggest in v1 (deferred a Slice 7f / v1.x)")
    - D:/Aura/.planning/STATE.md "Deferred Items" table: SWARM-V2-01 and SKILL-V2-01 explicitly deferred.
    - D:/Aura/prd.md line 1226 (Slice 3 header "Swarm coordinator (bus + DM-by-ID + tier model)")
    - D:/Aura/prd.md line 1228 (Slice 3 amendment note about AURA_SWARM_MAX_DEPTH=3 — note this needs to change to 2)
    - D:/Aura/prd.md line 1233 (Slice 3 commit body line "AURA_SWARM_MAX_DEPTH=3 enforced")
    - D:/Aura/prd.md line 1256 (Slice 3 acceptance about MaxSpawnDepth)
    - D:/Aura/prd.md line 1260 (Slice 3 tier model mapping — needs to be marked deferred for v1)
    - D:/Aura/prd.md line 1298 (Slice 3 commit body "AURA_SWARM_MAX_DEPTH=3 enforced")
    - D:/Aura/prd.md line 4260 (env table row `AURA_SWARM_MAX_DEPTH` default `3`)
    - D:/Aura/prd.md lines 4261-4263 (env table rows `AURA_SWARM_MODEL_*` — tier overrides, marked deferred)
    - D:/Aura/prd.md line 1820 (Slice 7 atomicity note bullet **7e**: describes v1 7e scope including "pattern analysis multi-conversation auto-suggest" — this is what gets SPLIT into 7f per amendment #13)
    - D:/Aura/prd.md line 2027 (Slice 7e header "Executable code snippets + pattern analysis + TTL archived" — header must reflect split)
    - D:/Aura/prd.md lines 2187-2195 (7e Pattern auto-suggest smoke block — note this entire flow is deferred to 7f)
  </read_first>
  <action>
    Apply Amendment #12 (Slice 3 swarm v1 scope: ParallelAgent + 2-deep cap):

    - prd.md line 1226 (Slice 3 header): replace "## Slice 3 — Swarm coordinator (bus + DM-by-ID + tier model)" with "## Slice 3 — Swarm coordinator (v1 minimal: ParallelAgent + 2-deep cap; full bus/DM-by-ID/tier-mapped → v2 SWARM-V2-01)"
    - prd.md line 1228 (amendment note): immediately after the existing Slice 0.9 amendment block, insert a new ">> **Amendment #12 (v1 scope reduction, 2026-05-29):**" block — paragraph: "v1 ship-scope is ParallelAgent reuse from Slice 0.9 + hard cap `AURA_SWARM_MAX_DEPTH=2` (NOT 3). NO DM-by-ID, NO tier-mapped models in v1. Tier env vars `AURA_SWARM_MODEL_{CHAT,REASONING,WORKER}` documented as v1 no-ops (all resolve to `AURA_LLM_MODEL`); enforcement of distinct tier routing deferred to v2 SWARM-V2-01. Rationale: SUMMARY.md Features over-scope flag + STATE.md Deferred Items table. Saving stimato: −300 LOC vs full v1 design (no bus, no DM router, no tier dispatcher)."
    - prd.md line 1233 (commit body line): replace "AURA_SWARM_MAX_DEPTH=3 enforced" with "AURA_SWARM_MAX_DEPTH=2 enforced (amendment #12 v1 cap; was 3 pre-amendment)"
    - prd.md line 1256 (Slice 3 acceptance about MaxSpawnDepth): replace "with `Depth >= MaxSpawnDepth` → errore `spawn depth exceeded`" with "with `Depth >= MaxSpawnDepth` (default 2 per amendment #12, was 3) → errore `spawn depth exceeded — MAX_SPAWN_DEPTH=2 cap (v1 amendment #12)`"
    - prd.md line 1260 (tier mapping acceptance): replace "Tier → model mapping in `tier.go`: `chat→<AURA_SWARM_MODEL_CHAT>`, `reasoning→<...REASONING>`, `worker→<...WORKER>`. Default tutti = env `AURA_LLM_MODEL`." with "**Tier → model mapping in `tier.go` — v1 NO-OP (amendment #12)**: tutti i tier (`chat`, `reasoning`, `worker`) risolvono a `AURA_LLM_MODEL`. Env vars `AURA_SWARM_MODEL_{CHAT,REASONING,WORKER}` esistono ma sono no-op in v1 (documentate per forward-compat con v2 SWARM-V2-01). Acceptance test: spawn 3 worker tier diversi, asserire tutti chiamano `AURA_LLM_MODEL` (verifica via mock LLM client capture)."
    - prd.md line 1298 (second commit-body line about MAX_DEPTH=3): replace "AURA_SWARM_MAX_DEPTH=3 enforced" with "AURA_SWARM_MAX_DEPTH=2 enforced (v1 amendment #12)"
    - prd.md line 4260 (env table row `AURA_SWARM_MAX_DEPTH`): replace the row "| `AURA_SWARM_MAX_DEPTH` | `3` | cap | 3 | Spawn depth cap (era `MAX_SPAWN_DEPTH` pre-fix). |" with "| `AURA_SWARM_MAX_DEPTH` | `2` | cap | 3 | Spawn depth cap v1 (amendment #12 — was `3` pre-2026-05-29; full N-deep deferred to v2 SWARM-V2-01). |"
    - prd.md lines 4261-4263 (env table rows `AURA_SWARM_MODEL_*`): for each of the three rows, replace the trailing "Tier override." with "Tier override (v1 NO-OP per amendment #12 — all tiers resolve to `AURA_LLM_MODEL`; enforcement deferred to v2 SWARM-V2-01)."

    Apply Amendment #13 (Slice 7e split into 7e-core v1 + 7f v1.x):

    - prd.md line 1820 (Slice 7 atomicity note bullet **7e**): replace the entire bullet "**7e**: **executable code snippets** (multi-lang Python/shell, eseguibili via sandbox Slice 2b) + **pattern analysis multi-conversation auto-suggest** (cross-conv analyzer suggerisce save dopo 3+ pattern simili) + **TTL 90gg archived state** (sweep periodico, archived skip da discovery default). Estende SKILL.md con `type: instruction|snippet` field. ~700 LOC. **Dipende da Slice 2b** (sandbox session-bound + workspace + network allowlist) per esecuzione + da Slice 0.7 Neo4j HNSW per similarity clustering. Pattern reference: Voyager (Wang et al., NeurIPS 2023) skill library + mem0 procedural memory." with this two-bullet split:

    "**7e-core (v1, amendment #13)**: **executable code snippets** (multi-lang Python/shell, eseguibili via sandbox Slice 2b) + **TTL 90gg archived state** (sweep periodico, archived skip da discovery default). Estende SKILL.md con `type: instruction|snippet` field. ~450 LOC. **Dipende da Slice 2b** (sandbox session-bound + workspace + network allowlist) per esecuzione. **NO cross-conv pattern analyzer in v1.**"
    "**7f (v1.x, amendment #13 — deferred SKILL-V2-01)**: **pattern analysis multi-conversation auto-suggest** (cross-conv analyzer suggerisce save dopo 3+ pattern simili via Neo4j HNSW similarity clustering). ~250 LOC. **Dipende da Slice 0.7 Neo4j HNSW + Slice 7e-core + Slice 11 memory** (più downstream di v1 7-slice). Pattern reference: Voyager (Wang et al., NeurIPS 2023) skill library + mem0 procedural memory. Tracked in STATE.md Deferred Items `SKILL-V2-01`."

    - prd.md line 2027 (Slice 7e header): replace "### Slice 7e — Executable code snippets + pattern analysis + TTL archived" with "### Slice 7e-core — Executable code snippets + TTL archived (v1; pattern analyzer → 7f v1.x per amendment #13)"
    - prd.md lines 2187-2195 (smoke block "3. Pattern auto-suggest (background)"): wrap the entire smoke block from "# 3. Pattern auto-suggest (background)" through the closing "[✅ Salva] [✏️ Modifica] [❌ No]" line with delimiter comments — prepend a line "# === SECTION 3 BELOW DEFERRED TO SLICE 7f (v1.x) PER AMENDMENT #13 — kept here for forward-reference only ===" before the block and append "# === END DEFERRED SECTION (Slice 7f / SKILL-V2-01) ===" after it. Do NOT delete the lines (they document the v2 design); annotate so executor of Phase 11 builds 7e-core only.
    - Within Slice 7e acceptance and file targets sections (search for "AURA_SKILL_PATTERN_ANALYSIS" related env vars at lines 4268-4272): on the env table rows for `AURA_SKILL_PATTERN_ANALYSIS_INTERVAL_MIN`, `AURA_SKILL_PATTERN_ANALYSIS_WINDOW_DAYS`, `AURA_SKILL_AUTOSUGGEST_MIN_LOC`, `AURA_SKILL_AUTOSUGGEST_MIN_OCCURRENCES`, `AURA_SKILL_AUTOSUGGEST_SIMILARITY_THRESHOLD`, append " (v1.x deferred Slice 7f per amendment #13)" to the trailing description text of each row. Do NOT delete rows.
  </action>
  <acceptance_criteria>
    - `grep -c "MAX_SPAWN_DEPTH=2" D:/Aura/prd.md` returns at least 2 (acceptance + amendment note)
    - `grep -E "AURA_SWARM_MAX_DEPTH=3 enforced" D:/Aura/prd.md` returns NO matches (old =3 must be replaced)
    - `grep -E "^\| \`AURA_SWARM_MAX_DEPTH\` \| \`2\`" D:/Aura/prd.md` returns exactly 1
    - `grep -c "amendment #12" D:/Aura/prd.md` returns at least 6
    - `grep -c "Slice 7e-core" D:/Aura/prd.md` returns at least 3 (atomicity bullet + header + at least one cross-ref)
    - `grep -c "Slice 7f" D:/Aura/prd.md` returns at least 4 (atomicity bullet + smoke deferred block + 5 env rows annotation grouped — at least 4 distinct lines)
    - `grep -c "amendment #13" D:/Aura/prd.md` returns at least 7 (atomicity bullets + header + smoke wrapper + env table rows)
    - `grep -E "SKILL-V2-01" D:/Aura/prd.md` returns at least 2 matches (Slice 7f bullet + tracking note)
    - `grep -E "SWARM-V2-01" D:/Aura/prd.md` returns at least 2 matches
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "MAX_SPAWN_DEPTH=2" prd.md) -ge 2 &amp;&amp; ! grep -E "AURA_SWARM_MAX_DEPTH=3 enforced" prd.md &amp;&amp; test $(grep -cE "^\| \`AURA_SWARM_MAX_DEPTH\` \| \`2\`" prd.md) -eq 1 &amp;&amp; test $(grep -c "amendment #12" prd.md) -ge 6 &amp;&amp; test $(grep -c "Slice 7e-core" prd.md) -ge 3 &amp;&amp; test $(grep -c "Slice 7f" prd.md) -ge 4 &amp;&amp; test $(grep -c "amendment #13" prd.md) -ge 7 &amp;&amp; test $(grep -c "SKILL-V2-01" prd.md) -ge 2 &amp;&amp; test $(grep -c "SWARM-V2-01" prd.md) -ge 2 &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; swarm cap is 2 (v1), tier overrides marked no-op v1, Slice 7e split into 7e-core + 7f with forward-reference smoke block preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Amendment #14 (skill.catalog opt-in behind aura skills enable-catalog)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 83: `| 14 | Hide Slice 7 skill.catalog behind opt-in | Features over-scope | Slice 7d |`
    - D:/Aura/.planning/REQUIREMENTS.md line 38 (CAP-07: "`skill.catalog` HTML scrape hidden behind `aura skills enable-catalog` opt-in")
    - D:/Aura/prd.md line 1817 (Slice 7 atomicity bullet **7b**: describes catalog as "skills.sh HTML scrape 1:1 pre-rewrite + tool `skill.catalog`. Read-only. ~350 LOC.")
    - D:/Aura/prd.md line 1841 (Read-only tool list: "skill.list, skill.catalog, skill.info")
    - D:/Aura/prd.md line 1878 (smoke block: model calls skill.catalog → finds 3 candidates)
    - D:/Aura/prd.md line 1977 (Slice 7d acceptance: "Tool skill.catalog (deferred). No npx, no ...")
    - D:/Aura/prd.md line 1981 (skill.catalog returns candidate list)
  </read_first>
  <action>
    Apply Amendment #14 — `skill.catalog` default-disabled, opt-in via `aura skills enable-catalog`:

    Edit 1 (Slice 7 atomicity bullet 7b at line 1817): replace "**7b**: catalog (skills.sh HTML scrape 1:1 pre-rewrite) + tool `skill.catalog`. Read-only. ~350 LOC." with "**7b**: catalog (skills.sh HTML scrape 1:1 pre-rewrite) + tool `skill.catalog`. Read-only. **Default DISABLED (amendment #14)** — opt-in via CLI subcommand `aura skills enable-catalog` (writes flag to config). Rationale: HTML scrape of external `skills.sh` is supply-chain attack surface (untrusted HTML → regex parse → install candidate). Default-deny aligns with PROJECT.md Out of Scope "Marketplace skills pubblico". ~350 LOC + ~20 LOC enable/disable plumbing."

    Edit 2 (Read-only tool list at line 1841): replace "Read-only: `skill.list`, `skill.catalog`, `skill.info`" with "Read-only: `skill.list`, `skill.info`. **Gated read-only (default OFF per amendment #14)**: `skill.catalog` — requires `aura skills enable-catalog` opt-in. When disabled, tool not advertised in manifest; if model attempts call, returns explicit error `\"skill.catalog disabled — run 'aura skills enable-catalog' to opt in\"`."

    Edit 3 (Smoke block at line 1878): immediately BEFORE the line "# → modello chiama skill.catalog → trova 3 candidati", insert a comment line:
    "# === Pre-condition: user has run 'aura skills enable-catalog' (amendment #14) ==="

    Edit 4 (Slice 7d acceptance at line 1977 area — locate the bullet about `skill.catalog (deferred)`): the existing text "Tool skill.catalog (deferred)" should be REPHRASED to clarify the opt-in semantics. Replace the surrounding bullet that includes "Tool skill.catalog (deferred). No npx, no ..." with a new explicit acceptance bullet:
    "- [ ] **`skill.catalog` opt-in (amendment #14)**: tool NOT registered in manifest by default. CLI `aura skills enable-catalog` writes `{catalog_enabled: true}` to `~/.aura/config.json`; CLI `aura skills disable-catalog` reverses. On boot, if `catalog_enabled=true`, tool registered. Fresh install acceptance: `aura skills catalog list` returns text `\"catalog disabled — run 'aura skills enable-catalog' to opt in\"`; after enable, returns scraped list. Audit: `aura.skill_audit` INSERT row on enable/disable with `action='catalog_enable'|'catalog_disable'`, `actor_id`, `ts`, `gate_recommended=false`, `gate_taken=true`."

    Edit 5 (Smoke block at line 1981 area "skill.catalog returns candidate list"): leave intact — it documents the enabled-path behavior.
  </action>
  <acceptance_criteria>
    - `grep -c "enable-catalog" D:/Aura/prd.md` returns at least 3 (atomicity bullet + read-only list + acceptance bullet)
    - `grep -c "amendment #14" D:/Aura/prd.md` returns at least 4 (atomicity bullet + read-only list + smoke pre-condition + acceptance bullet)
    - `grep -E "catalog disabled" D:/Aura/prd.md` returns at least 1 match (the error string from read-only list + acceptance bullet)
    - `grep -c "skill\.catalog" D:/Aura/prd.md` returns at least 5 (existing occurrences preserved)
    - `grep -E "catalog_enabled" D:/Aura/prd.md` returns at least 1 match (acceptance bullet config key)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "enable-catalog" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #14" prd.md) -ge 4 &amp;&amp; grep -q "catalog disabled" prd.md &amp;&amp; test $(grep -c "skill\.catalog" prd.md) -ge 5 &amp;&amp; grep -q "catalog_enabled" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; skill.catalog default-disabled with opt-in command documented in atomicity note, read-only list, smoke pre-condition, and a new acceptance bullet.</done>
</task>

</tasks>

<verification>
Phase-level grep fingerprints for architecture-spec-gaps cluster (post Wave 1):
- `AURA_AGENT_INSIGHT_CACHE_TTL_SEC` ≥ 4 hits
- `messages[2]` ≥ 3 hits
- `MAX_SPAWN_DEPTH=2` ≥ 2 hits
- `Slice 7e-core` ≥ 3 hits, `Slice 7f` ≥ 4 hits
- `enable-catalog` ≥ 3 hits, `catalog_enabled` ≥ 1 hit
- `amendment #11` ≥ 5, `amendment #12` ≥ 6, `amendment #13` ≥ 7, `amendment #14` ≥ 4
- `SKILL-V2-01` ≥ 2, `SWARM-V2-01` ≥ 2 (deferred items cross-references intact)
</verification>

<success_criteria>
- 3 tasks complete
- 4 amendments (#11, #12, #13, #14) reflected in prd.md
- No `.go`, `.sql`, or `.yaml` files modified by this plan
- No git commit created (commit is aggregated in 00-06)
- File state ready for 00-06 aggregator
</success_criteria>

<output>
On completion, create `.planning/phases/00-prd-amendments/00-03-SUMMARY.md` documenting:
- Lines edited in Slice 11e architecture block, Slice 11 decision table, Slice 11e acceptance, Slice 4 cross-ref
- Lines edited in Slice 3 (header, amendment block, acceptance, env table rows)
- Lines edited in Slice 7 atomicity bullets, Slice 7e header, smoke block wrapper, env table rows
- Lines edited in Slice 7d acceptance + Slice 7 read-only list
- Acceptance grep counts at final state
- Confirmation: no commit, no code files touched
</output>
