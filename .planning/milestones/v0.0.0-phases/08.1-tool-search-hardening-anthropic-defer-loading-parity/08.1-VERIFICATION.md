---
phase: 08.1-tool-search-hardening-anthropic-defer-loading-parity
verified: 2026-06-03T22:30:00Z
status: passed
score: 10/10
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  gaps_closed:
    - "D-07: namespacedName output is <=64 bytes for ALL inputs including long-namespace path (WR-01)"
    - "D-07: Mount collision-hash disambiguation produces names <=64 bytes even when base is already at cap (WR-02)"
    - "gosec G505/G401 lint gate passes — crypto/sha1 replaced with crypto/sha256 in name.go"
  gaps_remaining: []
  regressions: []
---

# Phase 08.1: Tool Search Hardening — Anthropic defer_loading Parity — Verification Report

**Phase Goal:** Harden Aura's deferred-tool pattern to parity with Anthropic's defer_loading API (Nov 2025): BM25 ranking replaces substring matching in tool_search, max_results top-K cap, MCP bridged tool names namespaced `<server>__<tool>` (sanitize + 64-byte cap + collision hash), registry-derived source-orientation blurb in tool_search Description, fail-closed >= 1-non-deferred registry guard at boot — while preserving the two KV-cache invariants (Render() alphabetical, prompt.go SystemPrompt byte-stable) and adding zero new dependencies.
**Verified:** 2026-06-03T22:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 08.1-04, commits a993ae92/5fcb680b/662c3acb/00e4514d)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01: free-text tool_search ranks by in-process Okapi BM25, no DB, no embedding sidecar, no new dependency | VERIFIED | `bm25.go` is stdlib-only (encoding/json, math, sort, strings). `git diff go.mod go.sum` empty. k1=1.5, b=0.75 consts present. TestBM25Rank, TestBM25Properties pass. |
| 2 | D-02: search document indexes Name, name-with-underscores-as-spaces, full Description, and every parameter name + description recursively from Parameters JSON-Schema | VERIFIED | `searchDocument` pushes Name, `ReplaceAll(Name,"_"," ")`, Description, and calls `appendSchema` which recursively pushes property keys and descriptions. TestSearchDocument asserts nested param names/descriptions are indexed. TestUnderscoreSpacing proves "fetch web" matches `web_fetch`. |
| 3 | D-03: keyword search ranks ONLY deferred tools; select: resolves ANY registered tool by exact name and stays uncapped | VERIFIED | `buildIndex` filters `!s.Deferred`. `match` select: branch resolves from full registry. TestDeferredOnlyFilter: keyword for "apples" does not return `active_apples_tool` (non-deferred), select: does. |
| 4 | D-05: tool_search accepts optional max_results (default 5, reject 0); select: ignores max_results | VERIFIED | `toolSearchArgs.MaxResults *int json:"max_results"`. Execute validates <= 0 → error "greater than zero". TestMaxResults: default=5, explicit=2, zero=error, select with max_results=2 still returns all 8. |
| 5 | D-06: bridged MCP tool model-facing name is namespaced `<server>__<tool>` (double-underscore); Bridge/Mount take namespace as input | VERIFIED | `bridge.go` Bridge/Mount both take `namespace string`. `spec.Name = namespacedName(namespace, d.Name)`. TestBridge_Namespaced passes ("github__create_issue"). |
| 6 | D-07: namespaced name sanitized to [a-zA-Z0-9_-], capped at 64 bytes, collision hash; wire name stays raw | VERIFIED | WR-01 CLOSED: `prefix[:budget]` in name.go:58; budget=51; namespacedName returns <=64 bytes for all inputs including 55-byte ns + 30-byte tool (was 70, now 64). WR-02 CLOSED: bridge.go:123 re-truncates `name = name[:maxToolNameLen-len(suf)]` before appending. TestNamespacedName_LongNamespaceCap, TestNamespacedName_DistinctLongNamespacesNoRecollision, TestNamespacedName_AdversarialLenSweep, TestMount_CollisionHash_RespectsCap all pass. |
| 7 | D-08: all-or-nothing Mount collision refusal preserved as outer guard; native built-in names unchanged | VERIFIED | `bridge.go:128-133` refuses any residual collision after hash-disambiguation. Native registered tools in main.go are unchanged. TestMount_Namespaced confirms re-mount of same namespace errors. |
| 8 | D-09: tool_search Description is registry-derived, deterministic, byte-stable, no clock, not in prompt.go SystemPrompt | VERIFIED | `search.go:Spec()` calls `toolSearchDescription(ts.Registry)` → `toolSearchLeadIn + sourceOrientation(reg)`. sourceOrientation groups by "__" prefix, sorts both source names and per-source tool names. `git diff internal/agent/prompt.go` is empty. TestToolSearchDescriptionDeterministic: two calls byte-identical, sources sorted, no clock shape. TestToolSearchDescription_SelfReferentialNoRecursion passes. |
| 9 | D-10: fail-closed registry guard at boot (excluding tool_search), mirroring Anthropic hard 400 | VERIFIED | `spec.go:94-102` `Registry.Validate()` returns `ErrNoNonDeferredTool` when no non-deferred tool with name != "tool_search" exists. `main.go:97` calls `reg.Validate()` as the last statement in `buildBaseRegistry`. TestNonDeferredGuard, TestValidate_ExcludesToolSearch, TestBuildRegistry_PassesValidate all pass. |
| 10 | Cache invariants preserved: Render() alphabetical after namespacing; prompt.go SystemPrompt byte-stable | VERIFIED | `manifest.go:39` sort by Name is unchanged (git diff empty). TestRenderToolDefs_Namespaced: "github__create_issue" < "github__list_prs" < "web_fetch" in alphabetical order. TestPrompt_ByteStable and TestPrompt_NoTimestamp still pass. |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/agent/tools/bm25.go` | searchDocument + appendSchema + tokenize + bm25Index/newBM25Index/rank/scoreDoc, stdlib-only | VERIFIED | 163 LOC, stdlib imports only (encoding/json, math, sort, strings). BM25 consts present. |
| `internal/agent/tools/bm25_test.go` | table-driven ranking + rapid property tests + searchDocument snapshot | VERIFIED | TestBM25Rank, TestUnderscoreSpacing, TestSearchDocument, TestBM25Properties all pass. |
| `internal/agent/tools/search.go` | BM25 keyword path + max_results arg; select: + NewResult preserved | VERIFIED | 208 LOC. toolSearchArgs.MaxResults *int. sync.Once memoization. |
| `internal/agent/tools/spec.go` | Registry.Validate() >= 1-non-deferred guard excluding tool_search | VERIFIED | 102 LOC. ErrNoNonDeferredTool sentinel. Validate() iterates tools. |
| `internal/agent/tools/spec_test.go` | Validate guard tests incl. exclusion test | VERIFIED | TestNonDeferredGuard, TestValidate_ExcludesToolSearch, TestValidate_EmptyRegistry all pass. |
| `internal/agent/mcptools/name.go` | sanitizeName, namespacedName (cap-correct), hashSuffix (sha256) | VERIFIED | 61 LOC. crypto/sha256 import. `prefix[:budget]` truncation at line 58. budget=maxToolNameLen-len(suffix)=51. |
| `internal/agent/mcptools/name_test.go` | long-namespace cap + adversarial len<=64 sweep + byte-stability | VERIFIED | TestNamespacedName_LongNamespaceCap, TestNamespacedName_DistinctLongNamespacesNoRecollision, TestNamespacedName_ShortNamesByteStable, TestNamespacedName_AdversarialLenSweep all present and passing. |
| `internal/agent/mcptools/bridge.go` | Bridge/Mount with namespace arg; cap-aware collision-hash append | VERIFIED | 154 LOC. Cap-aware collision branch at lines 120-127: `suf := hashSuffix(...)`, re-truncate to `name[:maxToolNameLen-len(suf)]` when overflow, then `name += suf`. |
| `internal/agent/mcptools/bridge_test.go` | collision-path cap case (base already near 64 bytes) | VERIFIED | TestMount_CollisionHash_RespectsCap present and passing (both names <=64 bytes, distinct, retrievable). |
| `internal/agent/mcptools/mount.go` | MountServer passes server name as namespace | VERIFIED | Mount(ctx, reg, name, cli) confirmed at line 21. |
| `cmd/aura/main.go` | reg.Validate() as last call in buildBaseRegistry | VERIFIED | Line 97: `if err := reg.Validate(); err != nil { ... os.Exit(1) }`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `search.go ToolSearch.match` | `bm25.go searchDocument + BM25 scorer` | `buildIndex` → `newBM25Index(specs)` → `ts.index.rank(q)` | WIRED | `buildIndex` builds the index over deferred tools; `match` calls `ts.index.rank(q)`. |
| `cmd/aura/main.go buildBaseRegistry` | `tools/spec.go Registry.Validate` | called last in buildBaseRegistry before return reg | WIRED | `reg.Validate()` at main.go:97. Absent from chat.go (confirmed). |
| `search.go ToolSearch.Spec().Description` | `Registry.All() deferred tools / namespace prefixes` | `toolSearchDescription(ts.Registry)` → `sourceOrientation(reg)` → `reg.All()` | WIRED | Dynamic Description confirmed; sourceOrientation sorts by "__" prefix. |
| `bridge.go Bridge/Mount` | `name.go namespacedName` | `spec.Name = namespacedName(namespace, d.Name)` | WIRED | line 84 in bridge.go. |
| `bridge.go bridgedTool.Execute` | `srv.CallTool` | routes by `b.name` (RAW wire name) | WIRED | line 55: `b.srv.CallTool(ctx, b.name, args)`. bridgedTool.name = d.Name (raw). |
| `mount.go MountServer` | `bridge.go Mount` | `Mount(ctx, reg, name, cli)` passing server name as namespace | WIRED | mount.go:21 confirmed. |
| `name.go namespacedName` | `maxToolNameLen invariant` | `prefix[:budget] + suffix` when `len(prefix) > budget` | WIRED | Lines 57-59. Both overflow branches return exactly 64 bytes. GAP-1/WR-01 closed. |
| `bridge.go Mount collision branch` | `maxToolNameLen invariant` | `name = name[:maxToolNameLen-len(suf)]` before `name += suf` | WIRED | Line 123. GAP-1/WR-02 closed. |

### Data-Flow Trace (Level 4)

This phase adds no components that render dynamic data to the user — it modifies in-process tool-search ranking and manifest construction. Data-flow trace is not applicable (pure in-process algorithmic changes, no DB, no API).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All phase-specific tests pass (race) | `go test -race -count=1 ./internal/agent/mcptools/ ./internal/agent/tools/ ./cmd/aura/` | ok (3 packages) | PASS |
| WR-01 regression test | `go test -race -run TestNamespacedName_LongNamespaceCap ./internal/agent/mcptools/` | PASS | PASS |
| WR-02 regression test | `go test -race -run TestMount_CollisionHash_RespectsCap ./internal/agent/mcptools/` | PASS | PASS |
| Adversarial len sweep | `go test -race -run TestNamespacedName_AdversarialLenSweep ./internal/agent/mcptools/` | PASS (8 subtests) | PASS |
| Byte-stability after sha256 swap | `go test -race -run TestNamespacedName_ShortNamesByteStable ./internal/agent/mcptools/` | PASS | PASS |
| D-09 description determinism | `go test -race -run TestToolSearchDescriptionDeterministic ./internal/agent/tools/` | PASS | PASS |
| D-10 guard test | `go test -race -run TestNonDeferredGuard\|TestValidate_ExcludesToolSearch ./internal/agent/tools/` | PASS | PASS |
| gosec lint gate | `golangci-lint run ./internal/agent/mcptools/` (WSL, golangci-lint v2.12.2) | 0 issues. EXIT:0 | PASS |
| go vet on 08.1 surface | `go vet ./internal/agent/mcptools/ ./internal/agent/tools/ ./cmd/aura/` | EXIT:0 | PASS |
| Zero new dependencies | `git diff go.mod go.sum` | empty | PASS |
| mcptools coverage floor | `go test -count=1 -cover ./internal/agent/mcptools/` | 87.2% | PASS (>=85%) |
| tools coverage floor | `go test -count=1 -cover ./internal/agent/tools/` | 96.1% | PASS (>=85%) |

Note: `go vet ./...` emits an error on `internal/agent/tracing_test.go:120` due to uncommitted working-tree changes from a separate session (not part of phase 08.1, predates the 08.1 commits and is absent from the 08.1-04 commit diffs). The 08.1 surface (mcptools, tools, cmd/aura) is fully vet-clean.

### Probe Execution

Step 7c: SKIPPED — no probe scripts declared in this phase's PLAN/SUMMARY files; `scripts/*/tests/probe-*.sh` not applicable to an in-process algorithm upgrade phase.

### Requirements Coverage

This phase maps to implementation decisions D-01 through D-10 in 08.1-CONTEXT.md rather than REQUIREMENTS.md IDs.

| Decision | Plan | Description | Status | Evidence |
|----------|------|-------------|--------|----------|
| D-01 | 08.1-01 | In-process BM25, no DB, no embedding sidecar, no new dependency | SATISFIED | bm25.go stdlib-only, go.mod/go.sum unchanged |
| D-02 | 08.1-01 | Index-field expansion: Name + name-spaced + Description + recursive param names/descriptions | SATISFIED | searchDocument + appendSchema confirmed; TestSearchDocument + TestUnderscoreSpacing |
| D-03 | 08.1-01 | Keyword search deferred-only; select: resolves any tool, uncapped | SATISFIED | buildIndex filters !Deferred; match select: uses Registry.Get |
| D-04 | 08.1-01 | BM25 implementation choice (in-house) | SATISFIED | In-house Okapi BM25 in bm25.go; no third-party ranker |
| D-05 | 08.1-01 | max_results top-K cap (default 5, reject 0); select: uncapped | SATISFIED | toolSearchArgs.MaxResults; Execute validates <= 0 |
| D-06 | 08.1-03 | `<server>__<tool>` namespacing, Bridge/Mount take namespace | SATISFIED | bridge.go Bridge/Mount with namespace param; TestBridge_Namespaced |
| D-07 | 08.1-03 + 08.1-04 | Sanitize to [a-zA-Z0-9_-], 64-byte cap, collision hash suffix | SATISFIED | WR-01 closed: `prefix[:budget]` in name.go; WR-02 closed: re-truncate in bridge.go. All 4 gap-closure commits present: a993ae92, 5fcb680b, 662c3acb, 00e4514d. TestNamespacedName_LongNamespaceCap + TestMount_CollisionHash_RespectsCap pass. |
| D-08 | 08.1-03 | All-or-nothing Mount refusal preserved; native built-in names unchanged | SATISFIED | Refusal at bridge.go:128-133; no native tool renames in main.go Register calls |
| D-09 | 08.1-02 | tool_search Description registry-derived, deterministic, not in prompt.go | SATISFIED | toolSearchDescription(ts.Registry); sorted sources; prompt.go untouched |
| D-10 | 08.1-02 | >= 1-non-deferred registry guard at boot, excluding tool_search, via buildBaseRegistry | SATISFIED | Registry.Validate() in spec.go; main.go:97; TestBuildRegistry_PassesValidate |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `cmd/aura/main.go` | 67 | `fmt.Println("TODO: implemented by the agent-loop and CLI slices")` | INFO | Pre-existing, predates this phase (present in commit ae11737a). Not a new debt marker. Not a blocker. |
| `internal/agent/tools/bm25.go` | (IN-03) | `searchDocument` indexes Description but not Summary | INFO | A deferred tool whose distinguishing keyword is only in its one-line Summary is not findable by keyword query. Codex parity decision (documented in SUMMARY), no code change required. |
| `internal/agent/tools/search.go` | 137-143 | max_results <= 0 rejected BEFORE dispatch, so select: queries with max_results=0 get an error | INFO (IN-01) | Harmless defensive validation; low model impact. |
| `internal/agent/mcptools/bridge.go` | 121 | Collision-hash suffix keyed on `bt.name` and namespacedName keyed identically but at different call sites | INFO (IN-01 from re-review) | Correct and deterministic; duplicate-keying risk is cosmetic. Optional refactor to shared helper per REVIEW.md. |

### Human Verification Required

None. All deliverables are programmatically verifiable and all checks have been run live.

### Gaps Summary

No gaps remain. Both BLOCKER gaps from the initial verification are confirmed closed by plan 08.1-04:

**GAP-1 (D-07 / WR-01 + WR-02): CLOSED**

- WR-01: `namespacedName` now computes `budget = maxToolNameLen - len(suffix)` (== 51) and returns `prefix[:budget] + suffix` when `len(prefix) > budget`, bounding the OUTPUT length. Confirmed: 55-byte ns + 30-byte tool previously yielded len=70, now yields len=64. `prefix[:budget]` literal present at name.go:58. `TestNamespacedName_LongNamespaceCap` and `TestNamespacedName_AdversarialLenSweep` both pass.
- WR-02: `Mount`'s collision branch now computes `suf := hashSuffix(...)`, truncates `name = name[:maxToolNameLen-len(suf)]` before appending when overflow would occur, then `name += suf`. `maxToolNameLen-len(suf)` literal present at bridge.go:123. `TestMount_CollisionHash_RespectsCap` passes.

**GAP-2 (lint gate / G505 + G401): CLOSED**

`crypto/sha1` import removed from name.go; replaced with `crypto/sha256`. `golangci-lint run ./internal/agent/mcptools/` exits 0 with "0 issues." verified live.

---

_Verified: 2026-06-03T22:30:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes (initial verdict: gaps_found 9/10; updated verdict: passed 10/10)_
