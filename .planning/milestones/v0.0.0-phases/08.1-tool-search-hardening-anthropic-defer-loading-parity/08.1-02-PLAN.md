---
phase: 08.1-tool-search-hardening-anthropic-defer-loading-parity
plan: 02
type: execute
wave: 2
depends_on: ["08.1-01", "08.1-03"]
files_modified:
  - internal/agent/tools/spec.go
  - internal/agent/tools/spec_test.go
  - internal/agent/tools/search.go
  - internal/agent/tools/search_test.go
  - internal/agent/tools/manifest_test.go
  - cmd/aura/main.go
  - cmd/aura/registry_test.go
autonomous: true
requirements: [D-09, D-10]
must_haves:
  truths:
    - "D-09: the tool_search Description is generated dynamically from the registry's mounted tools/sources (sorted, no clock) so it is byte-stable across turns and never busts the OpenRouter implicit cache; it does NOT live in prompt.go SystemPrompt"
    - "D-10: a registry validation run at build/boot errors if zero non-deferred tools exist EXCLUDING tool_search itself, mirroring Anthropic's hard 400"
    - "D-10: tool_search can never defer itself (Deferred hardcoded false); buildBaseRegistry (cmd/aura/main.go:81) calls reg.Validate() after all Register calls — the single shared composition root that buildRegistry, buildChatRegistry, AND buildRegistryWithMCP all delegate to, so the guard covers every boot path"
    - "invariant: Render() manifest ordering stays alphabetical-by-Name after namespacing renames (cache-load-bearing); prompt.go SystemPrompt stays byte-stable"
  artifacts:
    - path: "internal/agent/tools/spec.go"
      provides: "Registry.Validate() ≥1-non-deferred guard excluding tool_search"
      contains: "func (r *Registry) Validate"
    - path: "internal/agent/tools/spec_test.go"
      provides: "all-deferred registry fails Validate(); ≥1 non-deferred passes"
    - path: "internal/agent/tools/search.go"
      provides: "registry-derived deterministic tool_search Spec().Description (sorted sources, grouped by <ns>__ prefix)"
    - path: "cmd/aura/main.go"
      provides: "reg.Validate() boot hook as the last call in buildBaseRegistry (covers buildRegistry, buildChatRegistry, buildRegistryWithMCP — all delegate here)"
      contains: "Validate()"
  key_links:
    - from: "cmd/aura/main.go buildBaseRegistry (shared composition root for buildRegistry, buildChatRegistry, buildRegistryWithMCP)"
      to: "internal/agent/tools/spec.go Registry.Validate"
      via: "called as the LAST statement after all Register calls; fail-closed on zero non-deferred (excl tool_search)"
      pattern: "Validate\\(\\)"
    - from: "internal/agent/tools/search.go ToolSearch.Spec().Description"
      to: "Registry.All() deferred tools / namespace prefixes"
      via: "deterministic sorted source list, no clock"
      pattern: "Description"
---

<objective>
Add the two parity extras: a registry-derived dynamic source-orientation blurb inside the `tool_search` Description (D-09) and a ≥1-non-deferred fail-closed registry guard wired into boot (D-10). Plus verify the two cache invariants survive: `Render()` stays alphabetical after namespacing, and `prompt.go SystemPrompt` stays byte-stable.

Purpose: D-09 lets the model know what tool categories/sources exist without bloating the byte-stable SystemPrompt (categories ride in the non-deferred tool_search Description, which is turn-stable because the Registry is immutable per run). D-10 mirrors Anthropic's hard 400 ("At least one tool must be non-deferred") — a fail-open registry where every capability tool is deferred would let the model only search, never act.
Output: `Registry.Validate()` in spec.go + a new spec_test.go; a registry-derived deterministic Description in search.go (built on the namespacing from 08.1-03 + the BM25-aware search.go from 08.1-01); the Validate() boot hook as the last call inside `buildBaseRegistry` (cmd/aura/main.go:81-93 — the shared composition root for `buildRegistry`, `buildChatRegistry`, and `buildRegistryWithMCP`); an extended manifest_test.go asserting alphabetical order survives namespaced names.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-CONTEXT.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-RESEARCH.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-PATTERNS.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-01-SUMMARY.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols THIS PHASE creates (not drift — newly introduced by 08.1; this plan creates the guard + dynamic-description + boot-hook subset):

This plan (08.1-02):
- `internal/agent/tools/spec.go` — `func (r *Registry) Validate() error` (≥1-non-deferred guard excluding tool_search).
- `internal/agent/tools/spec_test.go` (NEW file) — `TestNonDeferredGuard` / `TestValidate_*`.
- `internal/agent/tools/search.go` — `ToolSearch.Spec().Description` becomes registry-derived (a private helper such as `sourceOrientation(*Registry) string` is executor's discretion); a possible `Sources()`/grouping helper.
- `internal/agent/tools/search_test.go` — `TestToolSearchDescriptionDeterministic`.
- `internal/agent/tools/manifest_test.go` — extended/sibling test with a namespaced-name ordering case.
- `cmd/aura/main.go` — `reg.Validate()` as the LAST call inside `buildBaseRegistry` (the shared composition root; NO edit to chat.go — `buildChatRegistry` is a one-line delegate to `buildBaseRegistry`, so it inherits the guard for free).
- `cmd/aura/registry_test.go` — extended guard test (production buildRegistry passes Validate(); all-deferred fixture fails).

Created by sibling plans (already merged when this runs — do NOT recreate):
- 08.1-01: bm25.go, the BM25 keyword path + max_results in search.go, toolSearchArgs.MaxResults.
- 08.1-03: name.go (sanitizeName/namespacedName/hashSuffix), namespaced Bridge/Mount.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Registry.Validate() ≥1-non-deferred guard + spec_test.go + boot hook in buildBaseRegistry</name>
  <files>internal/agent/tools/spec.go, internal/agent/tools/spec_test.go, cmd/aura/main.go, cmd/aura/registry_test.go</files>
  <read_first>
    - internal/agent/tools/spec.go (FULL — Registry struct, Register/Get/All; Spec struct; the All() iteration idiom to mirror for Validate())
    - internal/agent/tools/search.go (ToolSearch.Spec() returns Deferred:false hardcoded — the structural invariant Validate may optionally assert)
    - cmd/aura/main.go (FULL — buildRegistry is a one-liner at line 76-79 delegating to buildBaseRegistry; buildBaseRegistry holds the Register sequence at lines 81-93 with `return reg` at line 92, tool_search registered line 84; buildRegistryWithMCP at lines 95-120 ALSO calls buildBaseRegistry at line 99 — so a single Validate() inside buildBaseRegistry covers all three paths)
    - cmd/aura/chat.go (buildChatRegistry at lines 165-167 is a one-line `return buildBaseRegistry(cfg)` — DO NOT add a separate Validate() call here; it inherits the guard via buildBaseRegistry. No edit to chat.go in this task.)
    - cmd/aura/registry_test.go (FULL — TestBuildRegistry_* assertion idiom to extend; the production-wiring guard pattern; note TestBuildRegistryWithMCP_MountsConfiguredServer at line 51 exercises buildRegistryWithMCP → buildBaseRegistry and will now also run Validate(), so the guard must pass for the production tool set)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-RESEARCH.md §"Common Pitfalls" Pitfall 5 (guard must EXCLUDE tool_search from the count) + §"Open Questions" #2 (invoke Validate() in buildBaseRegistry after Register; constructor-time impossible since NewRegistry() is empty)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-PATTERNS.md §"internal/agent/tools/spec.go (MODIFY, add Validate())" (lines 116-133) + §"cmd/aura ... (D-10)" (lines 207-222 — note the correct single insertion point is buildBaseRegistry, not the two thin wrappers)
  </read_first>
  <behavior>
    - TestNonDeferredGuard (spec_test.go): a registry containing ONLY tool_search + all-deferred tools fails Validate() with an error mentioning non-deferred; a registry with ≥1 non-deferred capability tool (plus tool_search) passes (nil error).
    - TestValidate_ExcludesToolSearch: a registry whose ONLY non-deferred tool is tool_search itself still FAILS (proves tool_search is excluded from the count — Pitfall 5).
    - TestBuildRegistry guard (registry_test.go): the production buildRegistry() passes Validate() (it has non-deferred text_response/current_time/etc.); optionally a fixture all-deferred registry fails. Because buildRegistry now calls buildBaseRegistry which runs Validate(), a regression that defers every capability tool would surface as a boot failure.
  </behavior>
  <action>
    Add `func (r *Registry) Validate() error` to spec.go: iterate r.tools; count tools where `!t.Spec().Deferred && t.Spec().Name != "tool_search"`; if the count is zero return an error mirroring Anthropic's message intent ("at least one non-deferred tool is required"). The tool_search exclusion is critical (Pitfall 5). Optionally also assert that if tool_search IS registered its Spec().Deferred is false (structural invariant D-10). Mirror the All() small-receiver iteration style; do NOT add fields to the Spec struct.
    Create spec_test.go with the three behaviors above, reusing the searchableTool{Deferred:true} fixture pattern (from search_test.go) and the NewRegistry()/Register idiom. Continue the mutant-kill comment convention (e.g. name the mutant that drops the tool_search exclusion).
    Hook Validate() into the SHARED composition root (D-10 lands in ONE place that covers all boot paths): in cmd/aura/main.go `buildBaseRegistry`, call `reg.Validate()` as the LAST statement AFTER all Register calls and BEFORE `return reg` (currently line 92). This automatically covers `buildRegistry` (line 76-79), `buildChatRegistry` in chat.go (line 165-167), AND `buildRegistryWithMCP` (line 95-120) — all three delegate to buildBaseRegistry. Do NOT add a Validate() call to chat.go. buildBaseRegistry returns `*tools.Registry` with no error today, so on a Validate() error follow the existing boot-error style in main.go (log to os.Stderr + os.Exit, or panic — choose consistent with the surrounding cmd/aura boot code; do NOT silently ignore). Extend registry_test.go to assert the production registry passes Validate() (and, as a guard, that an all-deferred fixture registry fails Validate() directly).
    Keep spec.go ≤600 LOC; deep-refactor-on-touch.
  </action>
  <verify>
    <automated>go vet ./internal/agent/tools/ ./cmd/aura/ && go build ./... && go test ./internal/agent/tools/ ./cmd/aura/ -run 'TestNonDeferredGuard|TestValidate|TestBuildRegistry' -x</automated>
  </verify>
  <acceptance_criteria>
    - spec.go contains `func (r *Registry) Validate() error`.
    - TestValidate_ExcludesToolSearch proves a registry whose only non-deferred tool is tool_search fails Validate() (the exclusion is exercised, not just present).
    - cmd/aura/main.go buildBaseRegistry calls reg.Validate() after the Register calls and before `return reg` (grep `Validate()` in main.go returns a hit; grep `Validate()` in chat.go returns NO hit — the guard is inherited via buildBaseRegistry, not duplicated).
    - `go test ./cmd/aura/ -run TestBuildRegistry` exits 0 (production registry passes Validate(); TestBuildRegistryWithMCP_MountsConfiguredServer still green since buildRegistryWithMCP → buildBaseRegistry now validates and the production set has non-deferred tools).
    - `go test ./internal/agent/tools/ -run 'TestNonDeferredGuard|TestValidate'` exits 0.
  </acceptance_criteria>
  <done>Registry.Validate() fails closed when zero non-deferred capability tools exist (excluding tool_search), enforced once in buildBaseRegistry so it covers buildRegistry, buildChatRegistry, and buildRegistryWithMCP; production wiring passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry-derived deterministic tool_search Description (D-09) + cache-invariant tests</name>
  <files>internal/agent/tools/search.go, internal/agent/tools/search_test.go, internal/agent/tools/manifest_test.go</files>
  <read_first>
    - internal/agent/tools/search.go (CURRENT after 08.1-01 — the Spec() method with the static Description string line 40, now to be made registry-derived; the ToolSearch.Registry field; Deferred:false hardcoded line 42 must stay)
    - internal/agent/tools/spec.go (Registry.All()/Get() to enumerate sources; Spec fields Name/Summary/Deferred — there is NO namespace field, so group by <ns>__ prefix when present else "built-in", per RESEARCH Open Question 1)
    - internal/agent/mcptools/name.go (the nsDelimiter "__" from 08.1-03 — the prefix to split on when grouping sources; note search.go is in package tools and must NOT import mcptools — detect the "__" delimiter by string, do not import the const across the package boundary)
    - internal/agent/prompt.go + internal/agent/prompt_test.go (TestPrompt_ByteStable line 50, TestPrompt_NoTimestamp line 23 — these ALREADY exist and guard SystemPrompt; do NOT modify prompt.go, do NOT duplicate these tests — PATTERNS line 25)
    - internal/agent/tools/manifest.go (Render alphabetical sort line 39 — the invariant; READ-ONLY, do not modify)
    - internal/agent/tools/manifest_test.go (TestRenderToolDefs want slice line 35 — extend with a namespaced-name ordering case, do not rewrite)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-RESEARCH.md §"Architecture Patterns" Pattern 4 (lines 214-221: Codex tool_search_spec.rs template, sorted source list, "None currently enabled." empty case, NO clock/per-turn value) + §"Open Questions" #1 (source definition pre-MCP) + §"Common Pitfalls" Pitfall 1 (no Render reshuffle)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-PATTERNS.md §"Cache-stability invariant" (lines 244-246)
  </read_first>
  <behavior>
    - TestToolSearchDescriptionDeterministic: two calls of ToolSearch.Spec().Description on the same registry return byte-identical strings; the Description lists the deferred tools' sources (grouped by <ns>__ prefix when present, else built-in) in SORTED order; an empty/no-deferred registry yields a deterministic "None currently enabled."-style blurb. No clock/time/random in the output.
    - TestToolSearchSpec_StillNonDeferred (existing TestToolSearchSpecIsVisibleAndSchemaIsValid covers Deferred==false — keep it green): Spec().Deferred stays false (D-10 structural invariant).
    - TestRenderToolDefs_Namespaced (extend manifest_test.go): a registry containing a namespaced name (e.g. "github__create_issue") and a built-in ("web_fetch") still renders alphabetically by Name (github__create_issue < web_fetch); the existing alpha/mid_deferred/zebra ordering case stays. Run this manifest-ordering case as its own subtest/assertion within the task and confirm it green BEFORE wiring the dynamic Description, so a Render regression is isolated from a Description regression.
    - prompt.go byte-stability: TestPrompt_ByteStable / TestPrompt_NoTimestamp stay green (no edit to prompt.go).
  </behavior>
  <action>
    Make ToolSearch.Spec().Description registry-derived (D-09): build it from a helper (e.g. sourceOrientation(ts.Registry)) that enumerates the deferred tools via Registry.All(), groups them by their <ns>__ prefix when the name contains the "__" delimiter (split on the first "__"; the prefix is the source/namespace), else attributes them to a "built-in" source, dedupes, and lists them SORTED ("- <source>: <desc>" lines) — Codex tool_search_spec.rs template (RESEARCH Pattern 4). When there are no deferred tools, emit a deterministic "None currently enabled." line. The Description must be byte-identical on repeated calls (sort everything; NO clock, NO per-turn value — same rule as prompt.go, Pitfall 1/3). Keep the existing "# Tool discovery"-style lead-in describing BM25 search + that some tools must be loaded via tool_search. Spec().Deferred stays hardcoded false. Detect the "__" delimiter as a literal string inside package tools — do NOT import internal/agent/mcptools (would create a cycle); a 2-char string literal is acceptable and documented.
    Add TestToolSearchDescriptionDeterministic to search_test.go (two Spec() calls byte-equal + sorted sources + empty-registry case). Extend manifest_test.go (or add a sibling test) with a namespaced-name ordering case proving alphabetical Render order survives the "__" names — verify this ordering case green as a SEPARATE subtest within the task (it is independent of the Description change) — and do NOT rewrite the existing TestRenderToolDefs.
    Do NOT touch internal/agent/prompt.go. Do NOT modify manifest.go. Keep search.go ≤600 LOC; deep-refactor-on-touch.
  </action>
  <verify>
    <automated>go vet ./internal/agent/tools/ ./internal/agent/ && go build ./... && go test -race ./internal/agent/tools/ ./internal/agent/ -run 'TestToolSearchDescriptionDeterministic|TestToolSearchSpec|TestRenderToolDefs|TestPrompt' -x</automated>
  </verify>
  <acceptance_criteria>
    - ToolSearch.Spec().Description is built from the Registry (search.go references ts.Registry inside Spec(), not a static const).
    - TestToolSearchDescriptionDeterministic asserts two Spec().Description calls are byte-identical AND the source list is sorted AND an empty-deferred registry produces a deterministic "None currently enabled."-style output.
    - TestRenderToolDefs (and the new namespaced case) prove "github__create_issue" sorts before "web_fetch" alphabetically — Render order unchanged in mechanism (manifest.go untouched: `git diff internal/agent/tools/manifest.go` empty). The namespaced-ordering case is exercised as its own subtest/assertion.
    - `git diff internal/agent/prompt.go` is empty (SystemPrompt untouched); TestPrompt_ByteStable / TestPrompt_NoTimestamp still pass.
    - search.go does NOT import internal/agent/mcptools (grep: no mcptools import) — no package cycle.
    - `go test -race ./internal/agent/tools/ ./internal/agent/` exits 0.
  </acceptance_criteria>
  <done>tool_search advertises its sources via a deterministic registry-derived Description (cache-safe, no clock), tool_search stays non-deferrable, and both cache invariants (Render alphabetical, SystemPrompt byte-stable) are re-verified green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry contents → tool_search Description | the mounted tool set determines the per-turn Description shipped in every manifest (non-deferred) |
| boot wiring → live agent | a misconfigured registry (all-deferred) could ship an agent that can only search, never act |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-07 | Information disclosure / DoS (cost) | non-deterministic tool_search Description busting the OpenRouter implicit prefix cache | mitigate | Description built from a SORTED source list with no clock/per-turn value; TestToolSearchDescriptionDeterministic asserts byte-stability; Render() stays alphabetical (manifest.go untouched) — RESEARCH Pitfall 1 |
| T-08.1-08 | Tampering (injection) | a bridged MCP tool's Description/Summary flows into the dynamic tool_search Description | mitigate | the Description only lists source NAMES grouped by namespace prefix + each source's own one-line Summary; the namespace prefix is sanitized by 08.1-03's sanitizeName before it ever reaches the registry, bounding what can appear; no raw multi-line tool description is concatenated unescaped into the orientation blurb |
| T-08.1-09 | Denial of availability | fail-open registry with zero actionable (non-deferred) tools | mitigate | Registry.Validate() fails closed at boot (excluding tool_search) once inside buildBaseRegistry — the shared composition root that buildRegistry, buildChatRegistry, and buildRegistryWithMCP all delegate to — mirroring Anthropic's hard 400 (D-10, RESEARCH Pitfall 5) |
| T-08.1-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs ZERO packages (stdlib only); no install task exists |
</threat_model>

<verification>
- `go vet ./...` clean.
- `go build ./...` succeeds.
- `go test -race ./internal/agent/... ./cmd/aura/` exits 0.
- `git diff internal/agent/prompt.go internal/agent/tools/manifest.go` is empty (both cache invariants untouched).
- No new entry in go.mod/go.sum.
</verification>

<success_criteria>
- The tool_search Description is dynamically derived from the registry's sources, deterministic and cache-safe, and tool_search stays non-deferrable (D-09).
- A ≥1-non-deferred fail-closed guard (excluding tool_search) runs at the shared boot composition root buildBaseRegistry, covering all three boot paths (D-10).
- Render() alphabetical ordering and prompt.go SystemPrompt byte-stability are re-verified intact after namespacing.
</success_criteria>

<output>
Create `.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
