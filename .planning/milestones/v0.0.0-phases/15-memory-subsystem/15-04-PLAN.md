---
phase: 15-memory-subsystem
plan: 04
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - docker/agent-memory/Dockerfile
  - docker/agent-memory/.dockerignore
  - compose.yaml
autonomous: true
requirements: [UX-08]
must_haves:
  truths:
    - "`docker compose build aura-agent-memory-mcp` reproduces the sidecar image from git (no more hand-built `:spike-fixed`)"
    - "The image vendors the fork at the pinned commit c1c2d65 — pip install from src/, NOT the PyPI release (Pitfall 5)"
    - "compose.yaml swaps `image: ...:spike-fixed` for a `build:`/`image:`/`pull_policy: never` stanza, all other service config unchanged"
  artifacts:
    - path: "docker/agent-memory/Dockerfile"
      provides: "reproducible pinned-fork pip-install image for the agent-memory MCP sidecar"
      contains: "pip install"
    - path: "compose.yaml"
      provides: "aura-agent-memory-mcp service with a build: context replacing the spike-fixed image"
      contains: "context: ./docker/agent-memory"
  key_links:
    - from: "compose.yaml aura-agent-memory-mcp build.context"
      to: "docker/agent-memory/Dockerfile"
      via: "build context directory"
      pattern: "context: ./docker/agent-memory"
---

<objective>
Replace the hand-built `aura-agent-memory-mcp:spike-fixed` image with a reproducible compose
`build:` — a `docker/agent-memory/Dockerfile` that `pip install`s the fork at the pinned
commit `c1c2d65`, modeled on `docker/markitdown/Dockerfile` + the fork's
`deploy/cloudrun/Dockerfile`. This is Claude's Discretion (reproducible compose build) and is
load-bearing for Phase 17 packaging; it supports re-scoped UX-08 by making the very sidecar
whose retrieval the advisory snapshot measures rebuildable from git.

Purpose: The `:spike-fixed` image is hand-built and NOT reproducible — it won't rebuild from
git, which blocks Phase 17 packaging and makes the advisory snapshot non-reproducible. The fix
(`c1c2d65`, the provenance-safe-dedup patch, +165 lines in `long_term.py` + 3 schema indexes)
is fork-internal and NOT upstreamed, so the image MUST `pip install -e .` from the pinned fork
commit, never the PyPI release (Pitfall 5 — a PyPI install loses the fix → cross-run over-merge
returns, spike 034 INVALIDATED state).

Output: `docker/agent-memory/Dockerfile` + vendored fork `src/`/`pyproject.toml` at `c1c2d65`,
and a `compose.yaml` edit swapping the image line for a `build:` stanza (markitdown shape).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-memory-subsystem/15-CONTEXT.md
@.planning/phases/15-memory-subsystem/15-RESEARCH.md
@.planning/phases/15-memory-subsystem/15-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the fork at c1c2d65 + write docker/agent-memory/Dockerfile</name>
  <files>docker/agent-memory/Dockerfile, docker/agent-memory/.dockerignore</files>
  <read_first>
    - docker/markitdown/Dockerfile (the canonical in-repo pinned-pip sidecar shape: python:3.x-slim base, apt no-install-recommends, pinned pip install, EXPOSE 8080)
    - D:/tmp/agent-memory @ c1c2d65 — deploy/cloudrun/Dockerfile (the extras the fork installs: pip install -e ".[mcp,google,openai]"), pyproject.toml (version 0.5.0, python_requires), and the src/ tree to vendor
    - compose.yaml aura-agent-memory-mcp command: (lines 123-152 — it ALREADY overrides with `neo4j-agent-memory mcp serve --transport http --profile extended --session-strategy persistent --user-id aura-local --embedding-dimensions 384 --no-auto-preferences`, so the Dockerfile CMD is irrelevant; the entrypoint binary must be installed)
    - .planning/phases/15-memory-subsystem/15-PATTERNS.md "docker/agent-memory/Dockerfile" section
    - .planning/phases/15-memory-subsystem/15-RESEARCH.md Pitfall 5 + Code Examples "Reproducible compose build"
  </read_first>
  <action>
    Create `docker/agent-memory/` and vendor the fork at the pinned commit `c1c2d65`: copy
    `pyproject.toml`, `README.md`, `README-pypi.md`, and the `src/` tree from `D:/tmp/agent-memory`
    (branch `aura/provenance-safe-dedup`, commit `c1c2d65`) into `docker/agent-memory/` so the
    build is reproducible WITHOUT a network git fetch (RESEARCH A2 — vendoring is simpler than a
    submodule for Phase-17 packaging). Write `docker/agent-memory/Dockerfile`: `FROM
    python:3.11-slim` (the fork pins python 3.10+, upstream uses 3.11-slim); `apt-get update &&
    apt-get install -y --no-install-recommends gcc && rm -rf /var/lib/apt/lists/*`; `WORKDIR /app`;
    `COPY pyproject.toml README.md README-pypi.md ./`; `COPY src/ ./src/`; `RUN pip install
    --no-cache-dir -e ".[mcp,google,openai]"`; `EXPOSE 8080`. Do NOT add a CMD that diverges from
    compose — the compose `command:` already drives the full `mcp serve ...` invocation; a CMD is
    optional documentation only. Add a `.dockerignore` excluding tests/`.git`/`__pycache__`/`*.pyc`
    to keep the build context tight. Add a top-of-Dockerfile comment recording the pinned commit
    `c1c2d65`, the branch `aura/provenance-safe-dedup`, and WHY PyPI must NOT be used (Pitfall 5 —
    the provenance-safe-dedup fix is fork-internal). Per CLAUDE.md "Preserve Docker build cache",
    do NOT prune any builder cache.
  </action>
  <verify>
    <automated>test -f /d/Aura/docker/agent-memory/Dockerfile && test -f /d/Aura/docker/agent-memory/pyproject.toml && test -d /d/Aura/docker/agent-memory/src && grep -q "c1c2d65" /d/Aura/docker/agent-memory/Dockerfile && grep -q "pip install" /d/Aura/docker/agent-memory/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `docker/agent-memory/Dockerfile`, `pyproject.toml`, and `src/` exist (fork vendored at c1c2d65)
    - The Dockerfile installs from the vendored src/ via `pip install -e ".[mcp,google,openai]"` (NOT PyPI)
    - The Dockerfile comment records commit `c1c2d65` and the Pitfall-5 reason
    - `.dockerignore` trims the build context
  </acceptance_criteria>
  <done>The fork is vendored at c1c2d65 and the Dockerfile pip-installs it from src/ (never PyPI).</done>
</task>

<task type="auto">
  <name>Task 2: Swap compose.yaml `image:` for a reproducible `build:` stanza</name>
  <files>compose.yaml</files>
  <read_first>
    - compose.yaml aura-agent-memory-mcp service (lines 100-160 — the `image:` line at 101 to replace; the environment/command/healthcheck/ports/depends_on block to keep verbatim)
    - compose.yaml markitdown service (lines 278-286 — the exact `build:`/`image:`/`pull_policy: never` shape to mirror)
    - .planning/phases/15-memory-subsystem/15-PATTERNS.md "compose.yaml edit" section
  </read_first>
  <action>
    In `compose.yaml`, edit the `aura-agent-memory-mcp` service: replace the single `image:
    ${AURA_AGENT_MEMORY_MCP_IMAGE:-aura-agent-memory-mcp:spike-fixed}` line (compose.yaml:101)
    with the markitdown-shaped stanza: `build:` with `context: ./docker/agent-memory`, then
    `image: ${AURA_AGENT_MEMORY_MCP_IMAGE:-aura-agent-memory-mcp:local}` (note the default tag
    changes from `:spike-fixed` to `:local`, matching markitdown's `:local` convention), then
    `pull_policy: never`. Keep EVERY other line of the service (`container_name`, `restart`,
    `depends_on`, the entire `environment` block lines 109-122, the `command` block lines 123-152,
    `ports`, `healthcheck`) byte-for-byte unchanged — only the image line becomes the
    build/image/pull_policy triple. Verify the edited YAML parses with `docker compose config`.
  </action>
  <verify>
    <automated>cd /d/Aura && grep -q "context: ./docker/agent-memory" compose.yaml && grep -q "aura-agent-memory-mcp:local" compose.yaml && docker compose config >/dev/null 2>&1 && echo COMPOSE_PARSES_OK</automated>
  </verify>
  <acceptance_criteria>
    - compose.yaml `aura-agent-memory-mcp` has `build.context: ./docker/agent-memory`, `image: ...:local` default, `pull_policy: never`
    - No `:spike-fixed` reference remains for this service
    - The environment/command/healthcheck/ports/depends_on blocks are unchanged
    - `docker compose config` parses the file without error
  </acceptance_criteria>
  <done>compose.yaml builds the memory sidecar from `./docker/agent-memory`, replacing the hand-built spike image, with all other service config intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install → PyPI transitive deps (build-time) | The `pip install -e ".[mcp,google,openai]"` resolves transitive PyPI deps inside the image build. |
| compose build → host Docker daemon | The build runs on the operator's Docker; the resulting image runs the memory engine loopback-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-SC | Tampering (HIGH) | pinned-fork pip install (supply chain) | mitigate | The image installs from the VENDORED fork `src/` pinned at commit `c1c2d65`, NOT a registry name resolution — strictly safer than a PyPI pull (RESEARCH §Package Legitimacy Audit: "Approved — vendored from a known fork at a pinned commit", the operator's own fork). The Dockerfile comment records the commit + the Pitfall-5 reason. The `.[mcp,google,openai]` transitive resolution happens inside the build; pinning the top-level source to a commit bounds the blast radius. Severity HIGH because it is the phase's only install surface; mitigation is the pinned vendored source. |
| T-15-04-01 | Tampering | Losing the provenance-safe-dedup fix (cross-run over-merge returns) | mitigate | Building from PyPI instead of the fork would drop `c1c2d65` → spike-034 INVALIDATED state. Mitigation: the Dockerfile installs only from the vendored `src/`; 15-05's integration test asserts `action=none` for a genuinely new entity (re-validates the fix survived the rebuild). |
| T-15-04-02 | Info disclosure | Secrets baked into the image | accept | No secret is COPYed or baked; `NAM_LLM_API_KEY`/`NEO4J_PASSWORD` arrive at runtime via compose env (the `aura-memory-mcp-placeholder-key` default keeps compose parseable in CI without a secret). The build context excludes `.git`/tests via `.dockerignore`. |
</threat_model>

<verification>
- `docker compose build aura-agent-memory-mcp` succeeds from git (no `:spike-fixed`).
- `docker compose config` parses the edited file.
- The rebuilt image's live `tools/list` == 16 and dedup `action=none` for a new entity are
  asserted by plan 15-05's `memory_integration` tier (the build is consumed there).
- This plan touches no Go files; coverage floor does not apply (Docker/compose config).
</verification>

<success_criteria>
- The memory sidecar image is reproducible from git via `docker compose build aura-agent-memory-mcp`.
- The image vendors the fork at `c1c2d65` and installs from `src/`, never PyPI (Pitfall 5).
- compose.yaml uses the markitdown-shaped `build:`/`image:`/`pull_policy: never` stanza with all
  other service config unchanged.
</success_criteria>

<artifacts_this_phase_produces>
- `docker/agent-memory/Dockerfile` (new): python:3.11-slim + `pip install -e ".[mcp,google,openai]"` from vendored src/.
- `docker/agent-memory/pyproject.toml`, `docker/agent-memory/README.md`, `docker/agent-memory/README-pypi.md`, `docker/agent-memory/src/` (vendored fork at c1c2d65).
- `docker/agent-memory/.dockerignore` (new).
- `compose.yaml`: `aura-agent-memory-mcp` service `build.context: ./docker/agent-memory` + `image: ...:local` + `pull_policy: never`.
- Default image tag changes from `aura-agent-memory-mcp:spike-fixed` to `aura-agent-memory-mcp:local`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/15-memory-subsystem/15-04-SUMMARY.md` when done.
</output>
