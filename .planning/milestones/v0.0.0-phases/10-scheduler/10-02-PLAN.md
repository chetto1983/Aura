---
phase: 10-scheduler
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - internal/db/migrations/0009_scheduler.up.sql
  - internal/db/migrations/0009_scheduler.down.sql
  - internal/db/queries/scheduler_tasks.sql
  - internal/db/queries/agent_job_runs.sql
  - internal/cron/store.go
  - internal/cron/schedule.go
  - internal/cron/schedule_test.go
  - internal/cron/main_test.go
  - internal/cron/store_test.go
  - internal/agent/tools/registry.go
  - internal/agent/tools/registry_test.go
  - internal/swarm/registry.go
  - internal/swarm/swarm.go
  - go.mod
  - go.sum
autonomous: true
requirements: [CAP-06]
must_haves:
  truths:
    - "Migration 0009 creates aura.scheduler_tasks + aura.agent_job_runs with correct GRANTs and the partial due-index"
    - "tools.Without is a package-level helper in internal/agent/tools (promoted from internal/swarm), and swarm calls the promoted version"
    - "schedule.NextRunAt computes the next fire for at|every|cron in-zone via gronx and returns UTC, never a fixed offset"
    - "A frozen-clock unit test asserts the Europe/Rome DST spring-forward fire time (Open Q1)"
    - "cron.Store copies the identity 04-02 canonical pattern (Store{pool,q}, db.WithTx writes, SQLSTATE classification, sentinel errors)"
  artifacts:
    - path: "internal/db/migrations/0009_scheduler.up.sql"
      provides: "scheduler_tasks + agent_job_runs DDL + GRANTs + partial index"
      contains: "CREATE TABLE aura.scheduler_tasks"
    - path: "internal/cron/schedule.go"
      provides: "at|every|cron parse + tz-aware DST-safe NextRunAt (gronx)"
      contains: "NextTickAfter"
    - path: "internal/cron/store.go"
      provides: "thin sqlc store over scheduler_tasks + agent_job_runs"
      contains: "func New(pool *pgxpool.Pool)"
    - path: "internal/agent/tools/registry.go"
      provides: "promoted tools.Without helper"
      contains: "func Without"
  key_links:
    - from: "internal/cron/schedule.go"
      to: "github.com/adhocore/gronx"
      via: "gronx.NextTickAfter(expr, refInZone, false)"
      pattern: "gronx\\.NextTickAfter"
    - from: "internal/swarm/swarm.go"
      to: "internal/agent/tools"
      via: "tools.Without(parentReg, swarmSpawnTool)"
      pattern: "tools\\.Without"
    - from: "internal/cron/store.go"
      to: "internal/db"
      via: "db.WithTx for atomic claim+recompute"
      pattern: "db\\.WithTx"
---

<objective>
Build the Phase 10 infra foundation (6a part 1): migration 0009, sqlc query files + thin store, the gronx-backed DST-safe schedule engine, the goleak TestMain + DST + store test scaffolds, and the cross-cutting refactor that promotes `Without` from `internal/swarm` to `internal/agent/tools` so `internal/cron` can drop `swarm_spawn` without importing `internal/swarm` (D-24 forbids that import; OQ2 carve-out).

Purpose: Every downstream Phase 10 plan (tick/claim, task tool, handlers, serve) depends on these types and tables. Defining the store domain types, the schedule contract, and the promoted `Without` FIRST prevents a contract scavenger hunt and the swarm import cycle.
Output: Tables + sqlc + Store{pool,q} + schedule.NextRunAt + Wave-0 test scaffolds + promoted tools.Without (swarm callsite updated same commit).
</objective>

<artifacts_produced>
New symbols this plan creates (exclude from drift verification):
- SQL: `aura.scheduler_tasks` table, `aura.agent_job_runs` table, `scheduler_tasks_due_idx` partial index, migration files `0009_scheduler.{up,down}.sql`
- sqlc query names (in `internal/db/queries/scheduler_tasks.sql` / `agent_job_runs.sql`): `CreateTask`, `GetTask`, `ListActiveTasks`, `DueTasks` (FOR UPDATE SKIP LOCKED), `CancelTask`, `UpdateNextRunAt`, `InsertRun`, `UpdateHeartbeat`, `CompleteRun`, `ScanStaleRuns`, `MarkUnknownRecovery` (final names = Claude's discretion; keep the identity.sql `-- name:` annotation shape)
- Go package `internal/cron`: type `Store`, `func New(pool *pgxpool.Pool) *Store`, domain types `Task` + `Run`, converters `taskFromRow`/`runFromRow`, sentinel errors `ErrTaskNotFound` + `ErrAlreadyRunning`
- Go package `internal/cron`: type `TaskKind` (string), constants `KindReminder`/`KindAgentJob`/`KindBackupPostgres`/`KindBackupNeo4j`, type `ScheduleSpec` (the at|every|cron grammar union), `func ParseSchedule(...)`, `func NextRunAt(spec, tz string, after time.Time) (time.Time, error)`, `func (s *Scheduler) Now` clock field NOT created here (lives in 10-03)
- Go package `internal/agent/tools`: `func Without(parent *Registry, names ...string) *Registry`
- Test scaffolds: `internal/cron/main_test.go` (goleak.VerifyTestMain), `internal/cron/schedule_test.go`, `internal/cron/store_test.go`
- go.mod/go.sum: `github.com/adhocore/gronx v1.20.0`
</artifacts_produced>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scheduler/10-CONTEXT.md
@.planning/phases/10-scheduler/10-RESEARCH.md
@.planning/phases/10-scheduler/10-PATTERNS.md
@.claude/skills/golang-database/SKILL.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0009 + sqlc query files</name>
  <read_first>
    - internal/db/migrations/0008_proxied_child_id_text.up.sql — the migration floor; scheduler is 0009
    - internal/db/migrations/0007_cache_metrics.up.sql — the GRANT role-separation block (aura_app DML / aura_migrate ALL) + COMMENT ON convention + in-tx plain (non-CONCURRENT) index pattern
    - internal/db/queries/identity.sql — the sqlc `-- name: X :one|:many|:exec` annotation shape
    - .planning/phases/10-scheduler/10-PATTERNS.md §migration + §sqlc sections — exact GRANT/column/index guidance
    - .planning/phases/10-scheduler/10-RESEARCH.md "Code Examples" (DueTasks claim, orphan scan, heartbeat UPDATE) + §"Schema details" (D-28 fwd-compat columns)
    - internal/db/migrations/0005_*.up.sql or 0007 — the paused_states / identities tables for the FK targets (aura.identities, aura.paused_states(token))
  </read_first>
  <behavior>
    - Test: applying 0009 up then down leaves the schema clean (round-trip); covered by the existing db migration round-trip integration test pattern (extend it for 0009)
    - Test: aura_app can SELECT/INSERT/UPDATE/DELETE scheduler_tasks and SELECT/INSERT/UPDATE agent_job_runs, but cannot run DDL (role separation)
    - Test: DueTasks query returns only status='active' rows with next_run_at<=now() ordered by next_run_at, honoring FOR UPDATE SKIP LOCKED under two concurrent txns (non-overlapping claim) — db_integration
  </behavior>
  <action>
    Create migration 0009_scheduler.up.sql + .down.sql and the two sqlc query files. Per D-28 + 10-PATTERNS:
    - `aura.scheduler_tasks`: id uuid PK, kind text (reminder|agent_job|backup_postgres|backup_neo4j), schedule_kind text (at|every|cron), cron_expr text NULL, every_minutes int NULL, run_at timestamptz NULL (resolved one-shot), tz text NOT NULL DEFAULT 'Europe/Rome', payload jsonb, step_budget int NULL, status text NOT NULL DEFAULT 'active' (active|paused|pending_approval|cancelled), next_run_at timestamptz, notify_route text NULL, identity_id text NOT NULL DEFAULT 'local' FK→aura.identities, origin_conversation_id uuid NULL, created_at/updated_at timestamptz DEFAULT now().
    - `aura.agent_job_runs`: id uuid PK, task_id uuid FK→aura.scheduler_tasks, status text (running|completed|failed|unknown_recovery), step_budget int, started_at timestamptz, last_heartbeat_at timestamptz, completed_with_hash text NULL (idempotency, UNIQUE for the 23505 swallow per SC#2), summary text NULL, last_error text NULL, missed_since timestamptz NULL, paused_state_token text NULL FK→aura.paused_states(token).
    - Partial index: `CREATE INDEX scheduler_tasks_due_idx ON aura.scheduler_tasks (next_run_at) WHERE status = 'active';` (plain, in-tx — NOT concurrent).
    - GRANTs: scheduler_tasks → `GRANT SELECT, INSERT, UPDATE, DELETE ... TO aura_app`; agent_job_runs → `GRANT SELECT, INSERT, UPDATE ... TO aura_app` (no DELETE — audit-forever, PRD OQ4); `GRANT ALL` to aura_migrate on both. COMMENT ON TABLE per the 0007 convention.
    - sqlc queries (scheduler_tasks.sql + agent_job_runs.sql): CreateTask :one, GetTask :one, ListActiveTasks :many, DueTasks :many (with `FOR UPDATE SKIP LOCKED` inline + LIMIT $1), CancelTask :exec, UpdateNextRunAt :exec, InsertRun :one, UpdateHeartbeat :exec, CompleteRun :exec (sets completed_with_hash), ScanStaleRuns :many (last_heartbeat_at < now()-interval), MarkUnknownRecovery :exec. The advisory-lock `pg_try_advisory_lock`/`pg_advisory_unlock` are NOT sqlc — they run via raw conn.QueryRow/Exec in 10-03 (held-conn binding).
    Run `sqlc generate` (or the project's generate target) so internal/db/sqlc reflects the new tables. Do NOT hand-edit generated sqlc.
  </action>
  <verify>
    <automated>go build ./internal/db/... && grep -q "CREATE TABLE aura.scheduler_tasks" internal/db/migrations/0009_scheduler.up.sql && grep -q "FOR UPDATE" internal/db/queries/scheduler_tasks.sql</automated>
  </verify>
  <acceptance_criteria>
    - `internal/db/migrations/0009_scheduler.up.sql` contains `CREATE TABLE aura.scheduler_tasks` and `CREATE TABLE aura.agent_job_runs`
    - The due-index line is the partial form `WHERE status = 'active'` and is NOT `CONCURRENTLY`
    - `grep "GRANT SELECT, INSERT, UPDATE, DELETE" internal/db/migrations/0009_scheduler.up.sql` matches for scheduler_tasks; agent_job_runs GRANT has no DELETE
    - `internal/db/queries/scheduler_tasks.sql` DueTasks contains `FOR UPDATE SKIP LOCKED`
    - `agent_job_runs` has `completed_with_hash` with a UNIQUE constraint and FK `paused_state_token`→aura.paused_states(token)
    - `go build ./internal/db/...` exits 0 after sqlc generate (generated types compile)
  </acceptance_criteria>
  <done>Migration 0009 + the two sqlc query files exist with the D-28 schema, correct GRANTs, the partial due-index, and the FOR UPDATE SKIP LOCKED claim query; generated sqlc compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gronx schedule engine + DST test + goleak/store scaffolds + Without promotion</name>
  <read_first>
    - internal/swarm/registry.go — the current `Without` (the exact function being promoted to internal/agent/tools)
    - internal/swarm/swarm.go (the runChild callsite, ~line 130) — where `Without(rc.ParentRegistry, swarmSpawnTool)` is called; must switch to `tools.Without(...)`
    - internal/agent/tools/spec.go (Registry, NewRegistry, Register, All) — the registry the promoted Without builds against
    - internal/identity/store.go — the canonical Store{pool,q} pattern (constructor, fromRow, sentinel errors, SQLSTATE via errors.As+pgErr.Code)
    - internal/identity/main_test.go — the goleak.VerifyTestMain(m) TestMain to copy verbatim into internal/cron/main_test.go
    - internal/agent/budget.go lines ~64-94 — the W8 injectable `Now func() time.Time` clock rationale (used by tests; the field itself lands in 10-03 scheduler.go)
    - .planning/phases/10-scheduler/10-RESEARCH.md §Pattern 1 (gronx tz-via-ref) + §Open Questions Q1 (DST spring-forward) + §Code Examples (task_hash, injectable clock)
    - .planning/phases/10-scheduler/10-PATTERNS.md §"gronx tz-via-ref DST-safe NextRunAt" + §"Registry-Without"
  </read_first>
  <behavior>
    - Test (TestNextRunAt, unit, frozen Now): `at` returns the stored UTC instant; `every` adds the interval (floor 5 min enforced); `cron "30 9 * * *"` Europe/Rome returns the next 09:30 in-zone converted to UTC
    - Test (DST, Open Q1): a `cron "30 2 * * *"` with ref frozen at the Europe/Rome 2026 spring-forward night returns a deterministic in-zone fire time (assert the chosen normalization, documented in a comment) — proves no ±1h drift
    - Test (gronx.IsValid): an invalid cron expr is rejected before persist (ParseSchedule returns an error)
    - Test (store_test, db_integration): CreateTask→GetTask round-trips the domain Task; GetTask on a missing id returns ErrTaskNotFound
    - Test (registry_test): tools.Without(reg, "x") returns a fresh registry without "x" and does not mutate the parent
  </behavior>
  <action>
    Four pieces, all citing their decision IDs:
    1. Add `github.com/adhocore/gronx v1.20.0` (`go get`), then write `internal/cron/schedule.go`: `ScheduleSpec` union (at|every|cron + tz), `ParseSchedule` (validates via `gronx.IsValid` for cron; enforces `every` floor MinScheduleEveryMinutes=5 default; resolves `at` to UTC), and `NextRunAt(spec, tz, after)` that for recurring specs does `loc,_ := time.LoadLocation(tz); refInZone := after.In(loc); next,_ := gronx.NextTickAfter(spec.CronExpr, refInZone, false); return next.UTC()` (per D-06/D-07/D-08). Import `time/tzdata` (in a small `internal/cron/tzdata.go` or scheduler.go) so the binary is self-contained on hosts lacking zoneinfo (RESEARCH Environment Availability). NEVER store a fixed UTC offset.
    2. Write `internal/cron/store.go` copying the identity canonical pattern: `Store{pool *pgxpool.Pool; q *sqlc.Queries}`, `New(pool)`, `Task`/`Run` domain structs + `taskFromRow`/`runFromRow` (project pgtype→plain Go), sentinels `ErrTaskNotFound`/`ErrAlreadyRunning`, SQLSTATE classification via `errors.As(&pgErr)+pgErr.Code` (NEVER message-match), missing-row→ErrTaskNotFound. Atomic writes (the claim-insert+next_run_at recompute) wrap `db.WithTx`. Keep the file ≤600 LOC; the tick/claim/heartbeat methods land in 10-03 but the store read/write methods this plan owns are CreateTask/GetTask/ListActiveTasks/CancelTask/UpdateNextRunAt and the InsertRun/CompleteRun/UpdateHeartbeat run-row writers.
    3. Promote `Without` to `internal/agent/tools/registry.go` as `func Without(parent *Registry, names ...string) *Registry` (verbatim logic from internal/swarm/registry.go, retargeted to the same package as Registry). DELETE the swarm copy and update `internal/swarm/swarm.go` runChild to call `tools.Without(...)` — deep-refactor-on-touch in the SAME commit (CLAUDE.md). Add `registry_test.go` proving non-mutation.
    4. Scaffold tests: `internal/cron/main_test.go` = `func TestMain(m *testing.M){ goleak.VerifyTestMain(m) }` (copy identity/main_test.go); `internal/cron/schedule_test.go` (the TestNextRunAt + DST + IsValid behaviors above, frozen Now, NO real sleep); `internal/cron/store_test.go` (db_integration tag, `t.Fatal`-under-`$CI` envOrSkip discipline, CreateTask/GetTask round-trip + ErrTaskNotFound).
    Run `go vet ./... && go build ./... && go test -race ./internal/cron/ ./internal/agent/tools/ ./internal/swarm/` after edits (Gate 2).
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./internal/cron/ ./internal/agent/tools/ ./internal/swarm/ && grep -q "func Without" internal/agent/tools/registry.go && ! grep -rq "func Without" internal/swarm/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -run TestNextRunAt ./internal/cron/` passes (at|every|cron + tz + DST behaviors green)
    - The DST test asserts a deterministic Europe/Rome spring-forward fire time and a comment documents the chosen normalization (Open Q1)
    - `grep "func Without" internal/agent/tools/registry.go` matches; `grep -r "func Without" internal/swarm/` returns NO match (promoted, not duplicated)
    - `internal/swarm/swarm.go` runChild calls `tools.Without(...)`; `go test -race ./internal/swarm/` passes (swarm still drops swarm_spawn)
    - `internal/cron/main_test.go` calls `goleak.VerifyTestMain(m)`
    - `internal/cron/store.go` has `Store{pool,q}` + `New(pool)` + `ErrTaskNotFound` + a `db.WithTx`-wrapped atomic write + SQLSTATE classification via errors.As
    - `go.mod` lists `github.com/adhocore/gronx v1.20.0`; `time/tzdata` is imported for self-contained zoneinfo
    - All touched files ≤600 LOC; `go vet`/`go build` clean
  </acceptance_criteria>
  <done>The gronx DST-safe schedule engine, the canonical-pattern cron Store, the goleak/DST/store test scaffolds, and the promoted tools.Without (swarm callsite updated) all exist and pass unit + (env-gated) db_integration tests; no internal/cron→internal/swarm import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/model → DB | task payloads (cron_expr, every_minutes, payload jsonb) reach the DB via the store |
| aura_app role → schema | the running daemon uses aura_app; DDL must stay reserved to aura_migrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | claim/store queries | mitigate | All DB access via sqlc parameterized queries; advisory-lock raw queries (10-03) use $1 bind, never string-concat (Task 1/2) |
| T-10-02 | Elevation | aura_app DDL | mitigate | Migration 0009 GRANTs give aura_app DML-only (no DDL); DDL reserved to aura_migrate (Task 1) |
| T-10-03 | Tampering | aura_app DELETE on audit table | mitigate | agent_job_runs GRANT omits DELETE (audit-forever, PRD OQ4) (Task 1) |
| T-10-04 | DoS | held-conn pool starvation | accept (sized in 10-03) | AURA_SCHEDULER_MAX_CONCURRENT_RUNS < pool MaxConns enforced where the held-conn lifecycle lands (10-03 claim.go); this plan only defines tables/store |
| T-10-05 | Tampering | invalid cron expr persisted | mitigate | ParseSchedule validates via gronx.IsValid BEFORE persist (V5 input validation, Task 2) |
| T-10-SC | Tampering | go get gronx | mitigate | gronx v1.20.0 is Approved in 10-RESEARCH §Package Legitimacy Audit (go-proxy verified, zero-dep, source read); no [ASSUMED]/[SUS]/[SLOP] — no human checkpoint required |
</threat_model>

<verification>
- `go test -race ./internal/cron/ ./internal/agent/tools/ ./internal/swarm/` green (unit tier)
- `go test -tags db_integration -race ./internal/cron/ ./internal/db/...` green with stack up (store + migration round-trip)
- `grep -r "func Without" internal/swarm/` empty; `grep "func Without" internal/agent/tools/registry.go` matches
- No `internal/cron` file imports `internal/swarm` (`go list -deps ./internal/cron | grep swarm` empty)
- golangci-lint 0 on the touched packages; all files ≤600 LOC
</verification>

<success_criteria>
- Migration 0009 applies idempotently under aura_migrate/aura_app role separation; tables + partial index + GRANTs correct
- schedule.NextRunAt is DST-safe (in-zone recompute, UTC store) with a frozen-clock DST test proving it
- cron.Store follows the identity canonical pattern; ErrTaskNotFound/ErrAlreadyRunning sentinels exist
- tools.Without is promoted; swarm consumes it; no cron→swarm import
- goleak TestMain + schedule + store scaffolds exist for downstream waves to extend
</success_criteria>

<output>
Create `.planning/phases/10-scheduler/10-02-SUMMARY.md` when done
</output>
