---
phase: 10-scheduler
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - prd.md
  - .planning/ROADMAP.md
  - .env.example
autonomous: true
requirements: [CAP-06]
must_haves:
  truths:
    - "PRD Slice 6 grammar reads at|every|cron with a per-task IANA tz column (not daily HH:MM / in=10m)"
    - "PRD Slice 6 cites adhocore/gronx as a parser-only cron lib with the DIY tick retained (not 'nessuna libreria cron esterna')"
    - "PRD agent_job spawn section no longer references Coordinator.Spawn / RejectingResponder / TierConfig (direct LlmAgent per D-24/D-25)"
    - "PRD scheduler tool surface is ONE task tool with an action enum (not 5 separate task_* tool files)"
    - "PRD env catalog lists AURA_SCHEDULER_TZ, AURA_SCHEDULER_NOTIFY_DEFAULT (+recipient), AURA_SCHEDULER_QUIET_HOURS, tick/cap/retry knobs, and AURA_BACKUP_DIR"
    - "ROADMAP Phase 10 SC#1 notes the CLI also accepts --at/--every so the one-shot North-Star query is verifiable"
  artifacts:
    - path: "prd.md"
      provides: "Amended Slice 6 (grammar triad, gronx, composite delivery, spawn seam, tool consolidation, env catalog)"
      contains: "at | every | cron"
    - path: ".planning/ROADMAP.md"
      provides: "SC#1 CLI-triad wording note"
      contains: "--at"
    - path: ".env.example"
      provides: "New AURA_SCHEDULER_* + AURA_BACKUP_DIR entries"
      contains: "AURA_SCHEDULER_TZ"
  key_links:
    - from: "prd.md Slice 6"
      to: ".planning/DECISIONS.md §10"
      via: "amendment numbers cite D-06..D-29"
      pattern: "amendment"
---

<objective>
Doc-only PRD-amendment gate (D-29) that MUST land BEFORE any Phase 10 code (precedent: 05-01 / 08-01 / 09-01). It reconciles the PRD truth-source with the 29 locked CONTEXT.md decisions so downstream code plans (10-02..10-06) cite a consistent spec.

Purpose: The PRD's original Slice 6 grammar (`daily HH:MM` / `in=10m`), its "nessuna libreria cron esterna" wording, its `Coordinator.Spawn`/`RejectingResponder`/`TierConfig` agent_job references, and its 5-file `task_*` tool table are all STALE — they cannot express the 4 Italian North-Star queries and reference machinery cut in Phase 9. Code written against the stale PRD would be wrong by construction.
Output: Amended `prd.md` Slice 6 + env catalog, ROADMAP SC#1 note, `.env.example` entries. No Go code, no migrations.
</objective>

<artifacts_produced>
This plan creates NO new code symbols — it is documentation only. It amends prose, env-catalog entries, and success-criteria wording in:
- `prd.md` (Slice 6 grammar/tool-surface/spawn/delivery/env sections)
- `.planning/ROADMAP.md` (Phase 10 SC#1 wording note)
- `.env.example` (new env var entries: `AURA_SCHEDULER_TZ`, `AURA_SCHEDULER_NOTIFY_DEFAULT`, `AURA_SCHEDULER_NOTIFY_RECIPIENT`, `AURA_SCHEDULER_QUIET_HOURS`, `AURA_SCHEDULER_TICK_SECONDS`, `AURA_SCHEDULER_MAX_CONCURRENT_RUNS`, `AURA_SCHEDULER_NOTIFY_RETRY_ATTEMPTS`, `AURA_BACKUP_DIR`)
</artifacts_produced>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scheduler/10-CONTEXT.md
@.planning/phases/10-scheduler/10-RESEARCH.md
@.planning/DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend prd.md Slice 6 — grammar, gronx, spawn seam, tool surface, delivery</name>
  <read_first>
    - prd.md §"Slice 6 — Scheduler" (lines ~1935-2077) — the section being amended; read the current grammar, file-target table, acceptance list, commit templates 6a/6b, OQ1-4
    - prd.md lines ~1973 / ~2008 / ~2044 / ~2075 — the four dead Coordinator.Spawn / RejectingResponder / TierConfig reference sites (D-24)
    - prd.md lines ~1993 (amendment #19 step_budget) and ~1994 (amendment #23 ephemeral agent_job sessions) — already-landed amendments D-24 honors
    - .planning/phases/10-scheduler/10-CONTEXT.md §Implementation Decisions — D-06 (grammar triad), D-08 (gronx), D-09 (one task tool), D-10 (schema discipline), D-12 (tier cut), D-13 (toolsets cut), D-19 (composite delivery / OQ2), D-24/D-25 (spawn seam)
    - .planning/DECISIONS.md §10 (if present) — the canonical D-01..D-29 log for amendment-number cross-citation
  </read_first>
  <action>
    Apply these concrete edits to prd.md Slice 6, citing the corresponding decision ID inline (per D-06, per D-08, etc.):
    - Replace the `daily HH:MM` / `in=10m` grammar with the industrial triad `at | every | cron`: `at` = one-shot resolved to UTC timestamptz; `every` = interval with floor `MinScheduleEveryMinutes` (default 5, configurable); `cron` = standard 5-field expr + per-task IANA `tz` column (default `Europe/Rome` from config). State the DST-safe rule (per D-07): store `(expr, tz)`, recompute `next_run_at` in-zone after each fire, DB stays UTC, NEVER a fixed offset.
    - Change "nessuna libreria cron esterna" to: parser-only cron lib `github.com/adhocore/gronx` (`NextTickAfter`/`IsDue`/`IsValid`), zero transitive deps, DIY 30s tick loop retained (lib computes next-fire-time only) — per D-08.
    - Replace the four dead `Coordinator.Spawn` / `RejectingResponder` / `TierConfig` references with the direct-LlmAgent spawn seam: construct `agent.NewLlmAgent` directly mirroring `internal/swarm/swarm.go` runChild, registry = full minus `swarm_spawn`, budget from the `agent_job_runs.step_budget` row, ephemeral session `agent_job:<run_id>`, ask_user auto-reject = inject-and-continue (per D-24/D-25). Mark the old refs as superseded with the amendment number.
    - Mark `tier ∈ {worker,chat,reasoning}` validated vs `swarm.TierConfig.Available()` as CUT v1 (dead machinery, grep-confirmed) — children use the single configured model; scoring's reasoning modifier becomes a flat default (per D-12).
    - Drop the smoke payload `toolsets:[wiki,web]` field (per D-13); children get the full registry minus swarm_spawn, no per-task toolset scoping in v1.
    - Replace the 5-file `task_*` tool-surface table with ONE non-deferred `task` tool carrying an `action` enum (`schedule|list|cancel|run_now|approve`) via the ActionRouter helper (`internal/agent/tools/action.go`); schema discipline = top-level `required=["action"]` ONLY, per-action requirements in field descriptions, NO root-level `oneOf`/`anyOf`/`enum` (per D-09/D-10/D-11).
    - Resolve OQ2 to composite delivery: scheduled-job output reaches the user via the already-mounted WhatsApp/mail MCP self-send (`send_message`/`send_email`), `agent_job_runs.summary` keeps forensics, stdout Notifier is the fallback + SC#3 24h missed-backup alert; this is output egress, NOT history persistence (no conflict with amendment #23) — per D-19.
    - State migration 0009 as the landing number for `scheduler_tasks` + `agent_job_runs` (floor is 0008) — per D-29.
    Do NOT add fenced code blocks of Go implementation to the PRD; keep prose + identifiers. Do NOT touch any non-Slice-6 PRD section except the env catalog (Task 2).
  </action>
  <verify>
    <automated>grep -q "at | every | cron" prd.md && grep -q "adhocore/gronx" prd.md && ! grep -q "RejectingResponder" prd.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "at | every | cron" prd.md` returns a match in the Slice 6 section
    - `grep "adhocore/gronx" prd.md` returns a match; the old "nessuna libreria cron esterna" wording no longer asserts zero external lib
    - `grep "RejectingResponder" prd.md` returns NO match (all four dead references replaced or struck)
    - `grep "Coordinator.Spawn" prd.md` returns NO live (non-superseded) match
    - The Slice 6 tool surface describes ONE `task` tool with an `action` enum and `required=["action"]` schema discipline; no 5-file `task_*` table remains
    - The agent_job spawn paragraph references `agent_job:<run_id>` ephemeral session and budget-from-row (D-24)
    - OQ2 marked resolved to composite MCP self-send delivery (D-19)
  </acceptance_criteria>
  <done>prd.md Slice 6 reflects the grammar triad, gronx parser-only, direct-LlmAgent spawn seam, one task tool, and composite delivery — all citing their decision IDs; no dead Coordinator/RejectingResponder/TierConfig/tier/toolsets references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Env catalog + ROADMAP SC#1 note + .env.example entries</name>
  <read_first>
    - prd.md §"Caps & Limits → Indice completo env vars" — the env-catalog section new vars are appended to (convention `AURA_<DOMAIN>_<UNIT>`)
    - .env.example — current env-var template (mirror the new entries here)
    - .planning/ROADMAP.md §"Phase 10: Scheduler" lines ~301-314 — SC#1 gets the CLI-triad wording note
    - .planning/phases/10-scheduler/10-CONTEXT.md §"Claude's Discretion" — env-var names/defaults are Claude's discretion within the AURA_ convention; D-14 (CLI triad), D-23 (quiet hours), D-26 (AURA_BACKUP_DIR default ~/.aura/backups/)
  </read_first>
  <action>
    Add these env vars to the prd.md env catalog AND to `.env.example` (with the convention `AURA_<DOMAIN>_<UNIT>` and the noted defaults), per D-29 + Claude's discretion:
    - `AURA_SCHEDULER_TZ` (default `Europe/Rome`) — default IANA tz for cron tasks lacking an explicit tz
    - `AURA_SCHEDULER_NOTIFY_DEFAULT` (default `stdout`) — global fallback notify route (`whatsapp|email|stdout`)
    - `AURA_SCHEDULER_NOTIFY_RECIPIENT` (no default) — default recipient JID/email for the global fallback route
    - `AURA_SCHEDULER_QUIET_HOURS` (e.g. `23:00-07:30`, empty default = off) — non-DESTRUCTIVE notifications defer to window end (D-23)
    - `AURA_SCHEDULER_TICK_SECONDS` (default `30`) — tick loop interval
    - `AURA_SCHEDULER_MAX_CONCURRENT_RUNS` (default sized strictly below pool MaxConns, e.g. `4`) — held-conn concurrency cap (D-03, Pitfall 2)
    - `AURA_SCHEDULER_NOTIFY_RETRY_ATTEMPTS` (default `3`) — bounded delivery retry (D-22)
    - `AURA_BACKUP_DIR` (default `~/.aura/backups/`) — backup dump destination (D-26)
    Then edit ROADMAP Phase 10 SC#1: append a note that the operator CLI also accepts `--at` and `--every` (not just `--cron`) so the one-shot North-Star reminder query (Q3) is verifiable without an LLM (per D-14/D-29). Do NOT change SC#2 (chaos test stays AS WRITTEN per D-02 user override).
  </action>
  <verify>
    <automated>grep -q "AURA_SCHEDULER_TZ" prd.md && grep -q "AURA_BACKUP_DIR" prd.md && grep -q "AURA_SCHEDULER_TZ" .env.example && grep -q -- "--at" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AURA_SCHEDULER_TZ" prd.md` and `grep "AURA_BACKUP_DIR" prd.md` both match (env catalog updated)
    - All 8 new env vars appear in `.env.example`
    - ROADMAP Phase 10 SC#1 contains `--at` and `--every` in the CLI-triad note; SC#2 is byte-unchanged
    - Every new env var follows `AURA_<DOMAIN>_<UNIT>` naming (third-party exceptions not applicable here)
  </acceptance_criteria>
  <done>Env catalog (prd.md) and .env.example carry the 8 new scheduler/backup env vars with defaults; ROADMAP SC#1 notes the --at/--every CLI triad; SC#2 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (doc-only) | This plan writes no executable surface; the only "input" is the planner's prose. No runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-DOC-01 | Tampering | PRD env catalog | accept | Doc-only; the env vars become load-bearing in 10-02..10-06 where their validation/threat-model lands. No code consumes these defaults in this plan. |
| T-10-DOC-02 | Repudiation | amendment provenance | mitigate | Every PRD edit cites its decision ID (D-06..D-29) so the change is auditable against CONTEXT.md / DECISIONS.md. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. gronx legitimacy is audited in 10-RESEARCH §Package Legitimacy Audit (Approved, go-proxy verified) and the install happens in 10-02 with its own threat row. |
</threat_model>

<verification>
- `grep "at | every | cron" prd.md` matches; `grep "RejectingResponder" prd.md` empty
- `grep "AURA_SCHEDULER_TZ" .env.example` and `grep "AURA_BACKUP_DIR" .env.example` match
- ROADMAP SC#1 has the `--at`/`--every` note; SC#2 unchanged
- No `.go` files modified, no migrations added (this is the doc-only gate)
</verification>

<success_criteria>
- prd.md Slice 6 is internally consistent with CONTEXT.md D-06..D-29 (grammar triad, gronx, spawn seam, one task tool, composite delivery, migration 0009)
- prd.md env catalog + .env.example carry the 8 new env vars
- ROADMAP SC#1 carries the CLI-triad note; SC#2 byte-unchanged
- This plan landed BEFORE any 10-02..10-06 code plan (D-29 wave-0 ordering)
</success_criteria>

<output>
Create `.planning/phases/10-scheduler/10-01-SUMMARY.md` when done
</output>
