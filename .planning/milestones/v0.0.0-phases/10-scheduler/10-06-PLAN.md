---
phase: 10-scheduler
plan: 06
type: execute
wave: 4
depends_on: ["10-05"]
files_modified:
  - cmd/aura/chat.go
  - cmd/aura/serve.go
  - cmd/aura/main.go
  - scripts/scheduler_chaos.sh
  - .github/workflows/ci.yml
  - deploy/aura-scheduler.service
  - internal/cron/e2e_test.go
autonomous: false
requirements: [CAP-06]
user_setup:
  - service: openrouter
    why: "Live North-Star smoke drives a real DeepSeek-V4 agent_job (Q1/Q2/Q3 natural-prompt)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard → Keys (already used by Phase 7/9 live smoke)"
  - service: docker-compose-stack
    why: "Chaos test needs the live Postgres + 3 worker daemons; backup smoke needs aura-postgres/aura-neo4j containers"
    dashboard_config:
      - task: "Stack up (compose) + scheduler db_integration env (AURA_DB_URL/AURA_DB_MIGRATE_URL)"
        location: "local WSL / CI DinD"
must_haves:
  truths:
    - "aura serve is a real long-lived daemon hosting the tick loop, replacing the main.go shell/serve TODO"
    - "bootChat is refactored into an error-returning composition root reused by both chat and serve (no os.Exit in the shared boot)"
    - "Graceful shutdown finishes the in-flight tick, joins workers, and reverse-closes MCP closers — goleak-clean"
    - "scripts/scheduler_chaos.sh runs 3 workers, partitions one for 60s, and the survivor picks up the task with no duplicate side-effects (SC#2)"
    - "CI runs the scheduler db_integration tier (no-skip-as-green) and the chaos script as a CI-advisory non-blocking job"
    - "Live North-Star smoke (Q3 reminder + one cron agent_job) passes via natural Italian prompts (no 'cron' word)"
  artifacts:
    - path: "cmd/aura/serve.go"
      provides: "aura serve daemon entrypoint + signal block + graceful shutdown"
      contains: "scheduler"
    - path: "scripts/scheduler_chaos.sh"
      provides: "3-worker 60s-partition chaos test (SC#2)"
      contains: "network disconnect"
    - path: "deploy/aura-scheduler.service"
      provides: "documented systemd unit (Restart=on-failure, D-16)"
      contains: "Restart=on-failure"
  key_links:
    - from: "cmd/aura/serve.go"
      to: "internal/cron Scheduler.Start"
      via: "boot → scheduler.Start(ctx) → signal block → graceful shutdown"
      pattern: "Start\\("
    - from: ".github/workflows/ci.yml"
      to: "scheduler db_integration tier"
      via: "integration-test job runs ./internal/cron with AURA_DB_URL exported"
      pattern: "internal/cron"
---

<objective>
Wire the daemon and the validation surface (final 6b piece): the real `aura serve` daemon (D-15 — first long-lived process, replaces the main.go TODO) built on an error-returning `bootChat` refactor shared by chat + serve, the chaos test (D-02/D-05 SC#2), CI wiring (db_integration no-skip-as-green + chaos CI-advisory), the documented systemd unit (D-16), and the live North-Star smoke (Q3 reminder + one cron agent_job, natural Italian prompts). The final task is a human-verify checkpoint for the operator-gating chaos run + live smoke (the Phase-8/9 Gate-3 precedent).

Purpose: Without a long-lived host, jobs fire only when a chat is open (REPL-piggyback rejected). The daemon is the scheduler. The chaos test is the SC#2 gating record. The live smoke proves the 4 Italian North-Star queries are deliverable end-to-end — the whole point of the phase.
Output: serve.go + bootChat refactor + chaos script + CI + systemd unit + live e2e smoke; checkpoint records the operator-gating evidence.
</objective>

<artifacts_produced>
New symbols this plan creates (exclude from drift verification):
- `cmd/aura/serve.go`: `func runServe(args []string)`, `func bootServe(ctx) (*serveEnv, error)`, `type serveEnv`, the signal block + graceful shutdown sequence
- `cmd/aura/chat.go` refactor: an error-returning `bootChatEnv(ctx) (*chatEnv, error)` shared composition root (the os.Exit wrapper stays at the chat callsite only)
- `scripts/scheduler_chaos.sh`: the 3-worker / 60s-partition / idempotency-check script
- `.github/workflows/ci.yml`: scheduler db_integration added to the integration-test job + a non-blocking chaos-advisory step
- `deploy/aura-scheduler.service`: the documented systemd unit (Restart=on-failure)
- `internal/cron/e2e_test.go`: the env-gated live North-Star smoke (Q3 + Q1/Q2, NOT CI)
</artifacts_produced>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scheduler/10-CONTEXT.md
@.planning/phases/10-scheduler/10-RESEARCH.md
@.planning/phases/10-scheduler/10-PATTERNS.md
@.planning/phases/10-scheduler/10-VALIDATION.md
@.claude/skills/golang-concurrency/SKILL.md
@.claude/skills/golang-context/SKILL.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: aura serve daemon — bootChat refactor + graceful shutdown</name>
  <read_first>
    - cmd/aura/chat.go bootChat (lines 95-160) — the composition root being refactored to error-returning; note EVERY error path currently os.Exit's (Pitfall 6: kills graceful shutdown if reused as-is)
    - cmd/aura/chat.go chatEnv (lines 46-59) + its Close (closeMCPServers reverse-close)
    - cmd/aura/main.go (lines 60-76 switch incl. `case "shell", "serve":` TODO at ~70; closeMCPServers 161-169; buildRegistryWithMCP 115) — the serve wiring point + reverse-close helper
    - internal/cron/scheduler.go (from 10-03) Start(ctx) + internal/cron/dispatch.go (from 10-05) — what the daemon hosts
    - cmd/aura/web.go runWebDoctor — the sysexits/human-readable CLI convention
    - .planning/phases/10-scheduler/10-RESEARCH.md §Pitfall 6 (os.Exit kills graceful shutdown) + §System Architecture Diagram (boot → scheduler.Start → signal block → graceful shutdown)
    - .planning/phases/10-scheduler/10-PATTERNS.md §"cmd/aura/serve.go" (refactor target + switch-case wiring)
    - .planning/phases/10-scheduler/10-CONTEXT.md D-15 (real serve daemon) + D-16 (systemd unit)
  </read_first>
  <action>
    Refactor + wire (cite D-15/D-16):
    - Refactor `bootChat` (chat.go) into an error-RETURNING `bootChatEnv(ctx) (*chatEnv, error)` (no os.Exit inside the shared boot — Pitfall 6). The existing chat callsite wraps it and os.Exit's on error (behavior preserved for `aura chat`). serve must NOT os.Exit on a transient boot error.
    - Write `cmd/aura/serve.go`: `bootServe(ctx) (*serveEnv, error)` reusing the shared boot (pool + MCP mounts + registry + Runner) and adding the cron Scheduler (New + dispatch handlers wired with the live registry for the Notifier MCP self-send + the parent registry for agent_job tools.Without). `runServe(args)`: boot, `scheduler.Start(ctx)`, then a signal block (SIGINT/SIGTERM) → ctx cancel → graceful shutdown: finish the in-flight tick, join workers, `closeMCPServers` reverse-close (main.go:161-169) — goleak-clean. Register the task tool (10-04) into buildBaseRegistry here with the live store injected (the registration deferred from 10-04 lands here so serve owns the live store wiring). A boot scan failure is a WARN, not a blocker (ScanOrphans precedent).
    - Replace `case "shell", "serve":` TODO in main.go with `case "serve": runServe(os.Args[2:])` (keep `shell` separate or as a TODO) and update usage().
    - Write `deploy/aura-scheduler.service`: a documented systemd unit (`Restart=on-failure`, native binary, `ExecStart=/usr/local/bin/aura serve`, EnvironmentFile=~/.aura/env) — D-16 ops posture.
    Run `go vet ./... && go build ./... && go test -race ./cmd/aura/... ./internal/cron/...` (Gate 2). Smoke (stack up): `aura serve` boots, a `*/1 * * * *` reminder fires within the window, SIGINT shuts down clean (no leaked goroutine/subprocess).
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./cmd/aura/... && grep -q "scheduler" cmd/aura/serve.go && grep -q 'case "serve"' cmd/aura/main.go && grep -q "Restart=on-failure" deploy/aura-scheduler.service</automated>
  </verify>
  <acceptance_criteria>
    - `bootChat` is refactored to an error-returning `bootChatEnv`; the shared boot has no os.Exit; `aura chat` still works (its callsite wraps + exits)
    - `cmd/aura/serve.go` boots, calls `scheduler.Start(ctx)`, blocks on SIGINT/SIGTERM, and reverse-closes MCP closers on shutdown
    - `main.go` switch has `case "serve": runServe(...)`; usage() lists serve; the TODO is gone
    - The task tool is registered into buildBaseRegistry with the live store; `reg.Validate()` stays green
    - `deploy/aura-scheduler.service` has `Restart=on-failure` and `ExecStart=.../aura serve` (D-16)
    - Operator smoke: `aura serve` fires a 1-minute reminder and SIGINT shuts down goleak-clean (no leaked goroutine/subprocess)
    - All files ≤600 LOC; go vet/build/test-race clean
  </acceptance_criteria>
  <done>aura serve is a real long-lived daemon on an error-returning shared boot, with graceful goleak-clean shutdown and a documented systemd unit; the TODO is replaced.</done>
</task>

<task type="auto">
  <name>Task 2: chaos script + CI wiring + live North-Star smoke scaffold</name>
  <read_first>
    - .planning/phases/10-scheduler/10-VALIDATION.md §"Wave 0 Requirements" + §"Manual-Only Verifications" + §"Per-Task Verification Map" — the chaos/CI/smoke contract this task fulfills; map the TBD task IDs to 10-06-T2
    - .github/workflows/ci.yml (the integration-test job, ~line 114-132 — AURA_DB_URL/AURA_DB_MIGRATE_URL already exported) — where the scheduler db_integration tier and the chaos-advisory step are added
    - scripts/ (existing chaos/E2E precedents) + the web-tools E2E cross-compile + `docker network disconnect` pattern cited in 10-CONTEXT D-05 (topology = planner's choice)
    - internal/cron/claim.go (from 10-03) — the singleton + idempotency the chaos test exercises
    - internal/eval (cot_eval harness, build-tag cot_eval, OPENROUTER_API_KEY-gated) — the live-smoke harness shape to mirror for the North-Star E2E (Phase 9 swarm E2E precedent)
    - .planning/phases/10-scheduler/10-CONTEXT.md North-Star queries (header Q1-Q4) + §Specifics (natural prompt, no "cron" word; Q3 reminder + one cron agent_job real) + D-02/D-05 (chaos both ways)
  </read_first>
  <action>
    Three pieces (cite D-02/D-05 + North-Star):
    - `scripts/scheduler_chaos.sh`: stand up 3 worker daemons against the live Postgres (topology = planner's choice per D-05 — containerized-aura ×3 + `docker network disconnect` is the cited precedent, or host processes + DB-proxy kill if simpler on the CI runner), schedule a task, partition one worker for 60s, assert a survivor picks it up and there is NO duplicate side-effect (idempotency via completed_with_hash). Exit non-zero on a duplicate or a missed pickup. SC#2 as written (D-02 — do NOT downscope).
    - CI: add the scheduler db_integration tier to the existing integration-test job (`go test -tags db_integration -race ./internal/cron/... ./internal/db/...` with AURA_DB_URL/AURA_DB_MIGRATE_URL already exported; CI=true so the no-skip-as-green `t.Fatal` discipline holds). Add a SEPARATE non-blocking (advisory) chaos step that runs `scripts/scheduler_chaos.sh` and reports without gating the build (D-05 — operator run is the gating record).
    - `internal/cron/e2e_test.go` (build-tag cot_eval or a dedicated env-gate, OPENROUTER_API_KEY + mounted MCP, NOT CI): the live North-Star smoke — Q3 "ricordami di chiamare Monica alle 17:30" (natural prompt, no "cron"/"task" word, asserted) → the model picks the `task` tool → an `at` reminder row lands; and one cron agent_job (Q1 morning mail summary OR Q2 Cuneo news) → fires → delivers via the mounted mail/WhatsApp MCP self-send (read-back at the right JID/recipient, Phase 9 precedent). Placeholder result row in the quality snapshot at commit; the operator run fills it (the checkpoint, Task 3).
    Map these to the 10-VALIDATION Per-Task Verification rows (fill the TBD task IDs with 10-06-T2). Run `go vet ./... && go build ./... && go build -tags cot_eval ./internal/cron/...` (the e2e file compiles green even though it is not run in CI).
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && bash -n scripts/scheduler_chaos.sh && grep -q "internal/cron" .github/workflows/ci.yml && grep -q "network disconnect" scripts/scheduler_chaos.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/scheduler_chaos.sh` runs 3 workers, partitions one for 60s, asserts survivor-pickup + no-duplicate (exit non-zero on failure); `bash -n` clean
    - `.github/workflows/ci.yml` runs `go test -tags db_integration ./internal/cron/...` in the integration-test job (no-skip-as-green, CI=true) AND a non-blocking chaos-advisory step (D-05)
    - `internal/cron/e2e_test.go` compiles under its build tag and contains the Q3 reminder + one cron agent_job natural-prompt smoke (no "cron"/"task" literal in the prompt, asserted)
    - The 10-VALIDATION Per-Task Verification rows for SC#1-SC#4 + North-Star Q3/Q1-Q2 have their TBD task IDs filled with 10-06-T2 (and the SC rows from 10-02..10-05)
    - go vet/build clean; `go build -tags cot_eval ./internal/cron/...` exits 0
  </acceptance_criteria>
  <done>The chaos script (SC#2), CI db_integration + chaos-advisory wiring (no-skip-as-green), and the env-gated live North-Star smoke scaffold exist and compile; VALIDATION rows mapped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator Gate-3 — chaos run + live North-Star smoke sign-off</name>
  <files>.planning/phases/10-scheduler/10-VALIDATION.md, docs/aura-quality-snapshot.md</files>
  <what-built>
    The full scheduler: aura serve daemon, the HA claim/advisory-lock/heartbeat/recover stack, the task tool + CLI, the reminder/agent_job/backup handlers, the composite Notifier, the chaos script, CI wiring, and the live North-Star smoke. The CI db_integration tier is green; the chaos-advisory CI step and the live smoke need an operator-gating run on a real stack (Phase-8/9 Gate-3 precedent — Docker Desktop / native daemon nuance + OPENROUTER_API_KEY).
  </what-built>
  <how-to-verify>
    1. Bring the stack up (compose) and apply migration 0009: `make neo4j-migrate` (or the db migrate target) — verify `aura.scheduler_tasks` + `aura.agent_job_runs` exist.
    2. SC#1: `aura task schedule --cron "*/5 * * * *" --kind reminder --args '{"text":"test"}'`, start `aura serve`, observe the task fires exactly once per 5-min window (no double-exec) — check `aura task runs`.
    3. SC#2 (gating record): run `scripts/scheduler_chaos.sh` with 3 workers; confirm the partitioned worker's task is picked up by a survivor with no duplicate side-effect. Record the result in 10-VALIDATION.md Manual-Only table.
    4. SC#3: schedule nightly `backup_postgres` + `backup_neo4j`; verify dump artifacts in `$AURA_BACKUP_DIR`; confirm the 24h-miss alert log line fires when a run is skipped. Smoke the restore runbook.
    5. SC#4: schedule an agent_job with `--max-steps 10`; observe it terminates at step 10 (not 25); confirm an ask_user inside the job auto-rejects (<30s, audit marker).
    6. Live North-Star (OPENROUTER_API_KEY + mounted MCP): run the e2e smoke for Q3 (Monica reminder, natural Italian, no "cron" word) and one cron agent_job (Q1 mail summary or Q2 Cuneo news) — verify the task row + the WhatsApp/mail delivery read-back. Record numbers in docs/aura-quality-snapshot.md.
    7. Coverage: `make coverage` (owned-surface ≥85%) + mutation spot-check ≥70% on claim/heartbeat/schedule (WSL go-mutesting) — record in 10-VALIDATION.md.
  </how-to-verify>
  <action>PAUSE for the operator. This is a blocking-human Gate-3 checkpoint — the chaos run, backup artifacts, SC#1/SC#3/SC#4 behaviors, and the live North-Star smoke must be executed and signed off on a real stack (compose up + OPENROUTER_API_KEY + mounted MCP). Do NOT auto-approve (gate=blocking-human; workflow.auto_advance ignored). Record evidence in 10-VALIDATION.md Manual-Only table + docs/aura-quality-snapshot.md, then await the operator's "approved".</action>
  <verify>
    <human-check>Operator executes how-to-verify steps 1-7 on a live stack; chaos survivor-pickup (no duplicate) + backup artifacts in $AURA_BACKUP_DIR + SC#1/SC#4 behaviors + live Q3/cron-agent_job delivery read-back + coverage ≥85% + mutation ≥70% recorded in 10-VALIDATION.md / quality snapshot before resume.</human-check>
  </verify>
  <done>Operator signs off with the chaos result, backup artifacts, SC#1/SC#4 behaviors, and the live Q3 + cron agent_job delivery read-back recorded. CAP-06 marked complete only after this sign-off (the operator's call, Phase-8/9 precedent).</done>
  <resume-signal>Type "approved" with the chaos result + live-smoke evidence + coverage/mutation numbers recorded, or describe the failing criterion. CAP-06 is NOT marked complete until this checkpoint passes (the operator's call, Phase-8/9 precedent).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon → OS | aura serve is the first long-lived host process (systemd-managed) |
| chaos test → live stack | the chaos run partitions live workers + the DB network |
| live smoke → real LLM + MCP egress | a real DeepSeek agent_job delivers to real WhatsApp/mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-21 | DoS | os.Exit in shared boot kills graceful shutdown → leaked subprocesses | mitigate | bootChat refactored to error-returning; serve owns reverse-close + goleak-clean shutdown (Task 1, Pitfall 6) |
| T-10-22 | Repudiation | chaos failover not actually exercised (skip-as-green) | mitigate | Chaos script exits non-zero on duplicate/missed-pickup; CI db_integration `t.Fatal`s under CI when env unset; chaos-advisory reports but the operator run is the gating record (Task 2/3, D-05) |
| T-10-23 | Information Disclosure | live smoke leaks job output to the wrong recipient | mitigate | Smoke asserts delivery at the right JID/recipient (Phase 9 read-back precedent); MCP allowlists scope send_message/send_email (Task 2/3) |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan. The blocking human checkpoint (Task 3) is the Gate-3 sign-off; chaos/live evidence is operator-recorded, never auto-approved (gate=blocking-human). |
</threat_model>

<verification>
- `go vet ./... && go build ./...` clean with serve.go + bootChat refactor; `aura chat` still works
- `bash -n scripts/scheduler_chaos.sh` clean; CI yaml has the scheduler db_integration tier + chaos-advisory step
- `go build -tags cot_eval ./internal/cron/...` exits 0 (e2e smoke compiles)
- Checkpoint (Task 3): chaos result + live-smoke evidence + coverage ≥85% + mutation ≥70% recorded in 10-VALIDATION.md / quality snapshot
- golangci-lint 0 module-wide; all files ≤600 LOC
</verification>

<success_criteria>
- aura serve hosts the tick loop with goleak-clean graceful shutdown (D-15); systemd unit documented (D-16)
- SC#2 chaos test passes on the operator-gating run (survivor pickup, no duplicate side-effect)
- CI runs the scheduler db_integration tier (no-skip-as-green) + chaos-advisory (D-05)
- Live North-Star smoke (Q3 + one cron agent_job) delivers end-to-end via WhatsApp/mail
- CAP-06 ready to mark complete pending the operator checkpoint sign-off
</success_criteria>

<output>
Create `.planning/phases/10-scheduler/10-06-SUMMARY.md` when done
</output>
