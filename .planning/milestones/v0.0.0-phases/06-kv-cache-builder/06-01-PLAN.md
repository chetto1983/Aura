---
phase: 06-kv-cache-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CAP-04]

must_haves:
  truths:
    - "prd.md §Slice 4 OQ2 reads Postgres aura.cache_metrics, not in-memory (D-02)"
    - "prd.md §Slice 4 file-target table relocates PromptBuilder to internal/agent/prompt/ (D-01a)"
    - "prd.md §Slice 4 drops the internal/llm/cache_deepseek.go file-target (parsing already shipped)"
    - "A single prd: amendment commit lands BEFORE any Phase-6 code commit (PRD-first)"
  artifacts:
    - path: "prd.md"
      provides: "Amended Slice 4 OQ2 + file-target table + cache_deepseek drop"
      contains: "internal/agent/prompt"
    - path: ".planning/PROJECT.md"
      provides: "CAP-03→CAP-04 KV-cache label drift fixed"
  key_links:
    - from: "prd.md §Slice 4"
      to: "D-01a / D-02 / OQ1"
      via: "amendment text citing the import-cycle + Postgres-persistence rationale"
      pattern: "internal/agent/prompt|cache_metrics"
---

<objective>
Land the two mandatory PRD-amendments and the one doc-label fix that MUST precede any Phase-6 code, per the PRD-first principle (CLAUDE.md: deviations from prd.md require a PRD-amendment commit BEFORE the code commit).

This phase makes two decisions that contradict the locked PRD as written:
1. **D-02** overrides PRD Slice 4 OQ2 ("cache stats in-memory only, debug") → per-turn persistence to a new Postgres `aura.cache_metrics` table, so `aura cache-stats --since=1h` is a real time-windowed query.
2. **D-01a** relocates the PRD file-target `internal/llm/prompt.go` → `internal/agent/prompt/`. Reason: a CONFIRMED import cycle — `internal/agent/tools/manifest.go:7` imports `internal/llm`, so `internal/llm` cannot import `internal/agent/tools`, which `PromptBuilder` needs for `RenderToolDefs()`.
3. Drop the PRD file-target `internal/llm/cache_deepseek.go` — the DeepSeek/OpenRouter usage parsing it described is already shipped in `internal/llm/openai_compat/sse.go` + `usage.go` (D-02a). Nothing remains to build there.

Also fix a doc-drift: `.planning/PROJECT.md` labels the KV-cache requirement CAP-03; `.planning/REQUIREMENTS.md` (authoritative) maps CAP-04 → Phase 6 and CAP-03 → Phase 9 Swarm. One-line label fix.

Purpose: Without these amendments, every subsequent Phase-6 code commit violates PRD-first and the migration would appear in git before any `prd:` commit (Research Pitfall 2 warning sign).
Output: One `prd:` amendment commit editing prd.md + PROJECT.md, zero `.go` files changed.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-kv-cache-builder/06-CONTEXT.md
@.planning/phases/06-kv-cache-builder/06-RESEARCH.md
@.planning/phases/06-kv-cache-builder/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend prd.md §Slice 4 — OQ2 persistence override + file-target relocation + cache_deepseek drop</name>
  <read_first>
    - prd.md §"Slice 4 — KV cache builder" (≈L1469–1549): the OQ2 line ("in-memory only"), the file-target table (`internal/llm/prompt.go`, `internal/llm/cache_deepseek.go`), OQ3 (the `cache_control` proposal), and the commit template.
    - prd.md §Persistence "Migration numbering — fonte di verità" (confirm 0006 is last shipped → 0007 is next, the table cache_metrics migration will use).
    - 06-CONTEXT.md <decisions> D-01a, D-02, D-02a (the locked rationale text to cite).
    - 06-RESEARCH.md §"Common Pitfalls" Pitfall 1 (import-cycle proof) + Pitfall 2 (amendment-before-code ordering) + §"State of the Art" Deprecated/outdated bullets.
    - 06-PATTERNS.md the "PRD deviation" callout at the top of the File Classification table + "No Analog Found" dropped-file note.
  </read_first>
  <action>
    Edit prd.md §Slice 4 in place (do NOT rewrite the whole slice — surgical edits only):
    (a) **OQ2 override (D-02):** Rewrite the OQ2 answer from "in-memory only / stats are debug" to "Postgres `aura.cache_metrics` per-turn (conversation_id, seq, ts, prompt_tokens, cached_tokens, cost_usd); `aura cache-stats --since=<window>` is a time-windowed SQL query. New migration 0007 in the existing sequence (0006 last shipped)." Cite D-02 + that the source data is already in `llm.Usage` (D-02a — `prompt_tokens_details.cached_tokens` + OpenRouter `usage.cost`, no wire-layer work).
    (b) **File-target relocation (D-01a):** In the file-target table, change `internal/llm/prompt.go` → `internal/agent/prompt/` (subpackage: builder.go + hash.go + cache_anthropic.go). State the reason inline: import cycle — `internal/agent/tools/manifest.go` imports `internal/llm`, so `internal/llm` cannot import `internal/agent/tools` for `RenderToolDefs()`.
    (c) **Drop cache_deepseek.go:** Remove the `internal/llm/cache_deepseek.go` file-target; add a note that the DeepSeek/OpenRouter usage parsing it described is already shipped in `internal/llm/openai_compat/sse.go` + `usage.go` (D-02a / OQ1 resolution).
    Leave OQ3 unchanged — D-03 (the `cache_control` seam) aligns with the existing "Proposto: SÌ" answer and needs NO amendment. Do not touch any `.go` file. NEVER hardcode a provider in the amendment text — reference the config-driven Seam D00.
  </action>
  <verify>
    <automated>grep -q "internal/agent/prompt" prd.md && grep -q "cache_metrics" prd.md && grep -vq "in-memory only" <(grep -A2 "OQ2" prd.md) && git diff --name-only --cached prd.md 2>/dev/null; rg -c "\.go$" <(git diff --name-only) | grep -qv . </automated>
  </verify>
  <acceptance_criteria>
    - `grep "internal/agent/prompt" prd.md` matches (relocation present, D-01a).
    - `grep "cache_metrics" prd.md` matches in the Slice 4 OQ2 region (persistence override present, D-02).
    - The string `internal/llm/cache_deepseek.go` no longer appears as a *required* file-target (drop present); if mentioned, it is in a "already shipped / dropped" note.
    - OQ3 wording is unchanged (D-03 needs no amendment).
    - Zero `.go` files changed by this task (`git diff --name-only` lists only prd.md after this task).
  </acceptance_criteria>
  <done>prd.md §Slice 4 reflects Postgres persistence (D-02), the internal/agent/prompt/ relocation (D-01a), and the cache_deepseek.go drop — all citing the locked decision IDs; no Go files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CAP-03→CAP-04 label drift in PROJECT.md + single amendment commit</name>
  <read_first>
    - .planning/PROJECT.md — the Key Decisions / requirement table line that labels the KV-cache requirement CAP-03.
    - .planning/REQUIREMENTS.md line 35 (CAP-04 → Slice 4 KV cache) + line 113 (traceability CAP-04 → Phase 6) + line 34/112 (CAP-03 → Phase 9 Swarm) — the authoritative mapping.
    - 06-RESEARCH.md §"Phase Requirements" — "CAP-03 vs CAP-04 numbering drift — RECONCILED: use CAP-04".
    - prd.md §Slice 4 commit template (the imperative-subject + body + Co-Authored-By convention) for the commit message shape.
  </read_first>
  <action>
    In .planning/PROJECT.md, change the KV-cache requirement label from CAP-03 to CAP-04 (KV cache builder = Slice 4 = Phase 6). This is a doc-only one-line fix; REQUIREMENTS.md is authoritative and already correct (do NOT edit REQUIREMENTS.md). Do not relabel Phase-9 Swarm's CAP-03 (that mapping is correct).
    Then create ONE git commit containing only prd.md + .planning/PROJECT.md with an imperative subject matching the PRD `prd:` convention, e.g. `prd: persist KV cache metrics to Postgres + relocate PromptBuilder (Slice 4 amendments)`. Body explains WHY: OQ2 in-memory→Postgres (D-02), file-target import-cycle relocation (D-01a), cache_deepseek.go drop (D-02a), CAP-03→CAP-04 label fix. Include the `Co-Authored-By` trailer per project convention. This commit MUST precede every Phase-6 code commit (PRD-first; one slice/sub-slice = one commit).
  </action>
  <verify>
    <automated>grep -q "CAP-04" .planning/PROJECT.md && git log --oneline -1 | grep -qi "prd:" && git show --stat HEAD | rg -q "prd.md" && git show --name-only HEAD | rg -cq "\.go$"; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` references CAP-04 for the KV-cache requirement (drift fixed).
    - `git log --oneline -1` shows a `prd:`-prefixed amendment commit.
    - `git show --name-only HEAD` lists prd.md + .planning/PROJECT.md and ZERO `.go` files (PRD-first: doc-only amendment, mirrors Phase-0/05-01 doc-only gate commits).
    - The commit message body cites D-01a, D-02, D-02a and the CAP label fix; includes Co-Authored-By trailer.
  </acceptance_criteria>
  <done>One prd: amendment commit (prd.md + PROJECT.md, no Go files) lands first, unblocking all downstream Phase-6 code plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Doc-only amendment; no code, no input, no DB, no wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | prd.md amendment correctness | mitigate | The amendment that *records* the persistence/relocation decisions is the precondition for the runtime-faithful gate (built in 06-05) that actually mitigates prefix poisoning. This plan only documents; it introduces no executable surface. |
| (T-06-02/03/04) | — | — | n/a | No SQL, no metrics, no migration role touched in this doc-only plan; addressed in 06-03/06-04/06-05. |

No high-severity threats: this plan changes only Markdown docs. ASVS L1 surface is empty.
</threat_model>

<verification>
- `git log --oneline -1` shows the `prd:` amendment commit with zero `.go` files (mirrors Phase-0 + 05-01 doc-only gate pattern).
- `grep "internal/agent/prompt" prd.md` and `grep "cache_metrics" prd.md` both match.
- `.planning/PROJECT.md` reads CAP-04 for the KV-cache requirement.
</verification>

<success_criteria>
The PRD reflects the Postgres-persistence override (D-02), the import-cycle-driven file relocation (D-01a), and the cache_deepseek.go drop (D-02a), committed as a single doc-only `prd:` commit BEFORE any Phase-6 code — satisfying PRD-first. PROJECT.md's CAP label drift is fixed.
</success_criteria>

<output>
Create `.planning/phases/06-kv-cache-builder/06-01-SUMMARY.md` when done.
</output>
