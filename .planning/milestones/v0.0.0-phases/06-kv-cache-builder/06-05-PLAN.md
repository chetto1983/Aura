---
phase: 06-kv-cache-builder
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - scripts/cache_invariant_audit.sh
  - scripts/cache_invariant_negative_test.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [CAP-04]

must_haves:
  truths:
    - "scripts/cache_invariant_audit.sh invokes aura cache-audit, counts exactly 20 turn hash lines, diffs them, and exits 1 with 'messages[0] mutated at <site>' on any drift (SC#1/SC#5, D-06)"
    - "a negative test deliberately mutates messages[0] and asserts the gate exits 1 with the explicit message — without it SC#5 is unproven"
    - "the wrapper uses set -euo pipefail + the |grep -c . || true loud-fail-on-empty discipline (no-skip-as-green)"
    - "the CI gate runs as a Postgres-free step from this phase onward, gating every subsequent merge (amendment #16)"
  artifacts:
    - path: "scripts/cache_invariant_audit.sh"
      provides: "thin wrapper driving aura cache-audit + hash diff + explicit failure"
      contains: "cache-audit"
    - path: "scripts/cache_invariant_negative_test.sh"
      provides: "SC#5 negative proof — mutated [0] makes the gate exit 1"
      contains: "mutated"
    - path: ".github/workflows/ci.yml"
      provides: "cache invariant gate step (Postgres-free)"
      contains: "cache invariant gate"
  key_links:
    - from: "scripts/cache_invariant_audit.sh"
      to: "cmd/aura cache-audit"
      via: "go run ./cmd/aura cache-audit + count 20 lines + diff hashes"
      pattern: "cache-audit"
    - from: ".github/workflows/ci.yml"
      to: "scripts/cache_invariant_audit.sh"
      via: "a named CI step invoking the wrapper"
      pattern: "cache_invariant_audit.sh"
---

<objective>
Ship the cross-slice CI gate (amendment #16, Pitfall #3 P0): `scripts/cache_invariant_audit.sh` — a thin wrapper that drives the hidden `aura cache-audit` (06-04), counts exactly 20 per-turn hash lines, diffs them, and on any drift exits 1 with the explicit `messages[0] mutated at <site>` message (SC#1 + SC#5). Add the SC#5 NEGATIVE proof (`scripts/cache_invariant_negative_test.sh`): deliberately perturb the replay so `messages[0]` mutates and assert the gate exits 1 with the message — WITHOUT this, SC#5 is unproven (the gate could be silently green). Wire the gate into `.github/workflows/ci.yml` as a Postgres-FREE step that runs from this phase onward and gates every subsequent merge.

Purpose: This is the phase's headline deliverable — the runtime-faithful invariant gate that catches a FUTURE slice (1.8b microcompact, 7e, 10, 11e) mutating the assembled prefix. It is deliberately Postgres-free so it runs in any CI job.
Output: `scripts/cache_invariant_audit.sh` + `scripts/cache_invariant_negative_test.sh` + a `cache invariant gate` step in ci.yml.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-kv-cache-builder/06-CONTEXT.md
@.planning/phases/06-kv-cache-builder/06-RESEARCH.md
@.planning/phases/06-kv-cache-builder/06-PATTERNS.md
@.planning/phases/06-kv-cache-builder/06-VALIDATION.md
@scripts/loop_budget_smoke.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/cache_invariant_audit.sh (wrapper) + scripts/cache_invariant_negative_test.sh (SC#5 negative)</name>
  <read_first>
    - scripts/loop_budget_smoke.sh (FULL) — the established runtime-faithful gate: `set -euo pipefail` + `cd "$(git rev-parse --show-toplevel)"` (:17-19); run-the-binary, count real output lines with `grep -c . || true` so EMPTY output aborts with a diagnostic not a bare pipefail, fail loudly with an explicit message + `exit 1` (:22-35). The exact discipline to copy.
    - cmd/aura/cache_audit.go (from 06-04) — the `aura cache-audit` stdout contract: exactly 20 `turn NN: <hex>` lines, exit 0/1/2. The wrapper drives `go run ./cmd/aura cache-audit`.
    - 06-RESEARCH.md Pattern 4 (wrapper + exit codes: 0 pass / 1 mutation with `messages[0] mutated at turn N` / 2 fixture corrupt; belt-and-suspenders — the Go subcommand asserts AND the bash diffs) + Pitfall 3 (a green gate that exercises nothing — the gate must actually invoke the binary and count 20 hash lines; the negative test is mandatory).
    - 06-PATTERNS.md NO-SKIP-AS-GREEN runtime-faithful gate section + scripts/cache_invariant_audit.sh section (the exact grep-count + loud-fail excerpt).
    - 06-VALIDATION.md Wave 0 row: the SC#5 negative-test discipline ("without it SC#5 is unproven").
    - golang-testing skill (no-skip-as-green discipline).
  </read_first>
  <action>
    Create `scripts/cache_invariant_audit.sh` mirroring loop_budget_smoke.sh: `set -euo pipefail`, `cd "$(git rev-parse --show-toplevel)"`. Run `OUT="$(go run ./cmd/aura cache-audit)"`, capture the exit code. Count the `turn NN:` hash lines with the `| grep -c . || true` guard so EMPTY output aborts loudly (not a bare pipefail). If the line count is not exactly 20, OR the subcommand exited non-zero, OR consecutive hashes differ when diffed, print the explicit `messages[0] mutated at <site>` (forward the subcommand's stderr/turn-N detail) to stderr, echo the full captured output for diagnosis, and `exit 1`. Exit 0 only when all 20 lines are present and identical. This is belt-and-suspenders over the Go subcommand's own assertion — both must agree.
    Create `scripts/cache_invariant_negative_test.sh` (the SC#5 NEGATIVE proof): drive the audit in a mode that forces a `messages[0]` mutation (e.g. invoke a `cache-audit --inject-mutation` test hook added in 06-04, OR run against a poisoned fixture set, OR set an env knob the audit honors only under test — choose the least invasive that 06-04 supports; if a hook is needed, note it as a 06-04 dependency and use the Go-level negative test as the canonical proof while this script exercises the bash path). Assert the wrapper exits NON-zero AND its stderr contains `mutated`. If the wrapper exits 0 on a mutated prefix, this script fails loudly (`exit 1`) — that is the "gate is silently green" alarm. `chmod +x` both scripts.
    NEVER let either script `t.Skip`-equivalent silently pass; an empty or 0-line run is a hard failure (no-skip-as-green).
  </action>
  <verify>
    <automated>bash scripts/cache_invariant_audit.sh && bash scripts/cache_invariant_negative_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/cache_invariant_audit.sh` exits 0 and its stdout shows exactly 20 identical `turn NN: <hex>` lines (SC#1).
    - `bash scripts/cache_invariant_negative_test.sh` exits 0 (meaning: it successfully PROVED the gate exits non-zero on a mutated prefix), and confirms the wrapper emitted `mutated` to stderr (SC#5 — the gate is not silently green).
    - The wrapper uses `set -euo pipefail` and the `| grep -c . || true` loud-fail-on-empty guard (verifiable in the script source).
    - Both scripts are executable and Postgres-free (run with the DB stack down).
  </acceptance_criteria>
  <done>The wrapper drives the real cache-audit, counts + diffs 20 hashes, and fails loudly with the SC#5 message on drift; the negative test proves the gate exits 1 on a mutated messages[0] (SC#1 + SC#5, runtime-faithful, no-skip-as-green).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the cache invariant gate into .github/workflows/ci.yml (Postgres-free, gates from this phase onward)</name>
  <read_first>
    - .github/workflows/ci.yml (FULL) — the existing job/step structure; find a Postgres-FREE job (the unit/lint/build job, NOT the db_integration job) to attach the gate to, and how `go run` steps are expressed; the "sqlc generate is in sync" job (line ~135) + "Install sqlc" (line ~148) for the toolchain-pin style; how `CI=true` and other env are exported (no-skip-as-green convention).
    - scripts/cache_invariant_audit.sh (Task 1) — the wrapper the CI step invokes.
    - 06-RESEARCH.md Environment Availability table ("the invariant gate is deliberately Postgres-free, so it runs in any CI job without the DB stack") + D-06 ("CI-wires into .github/workflows/ci.yml from this phase onward").
    - 06-CONTEXT.md D-04/D-06 (runtime-faithful gate; CI from this phase onward gating every subsequent merge — amendment #16).
    - prd.md amendment #16 (the canonical cache_invariant_audit.sh CI gate spec).
  </read_first>
  <action>
    Add a CI step named exactly `cache invariant gate` to a Postgres-free job in `.github/workflows/ci.yml` (the unit/build/lint job — NOT the db_integration job, since the audit needs no DB). The step checks out, sets up Go (reuse the existing toolchain setup), and runs `bash scripts/cache_invariant_audit.sh`. Export `CI=true` so any in-script no-skip guards fire. Optionally also run `bash scripts/cache_invariant_negative_test.sh` as a sibling step `cache invariant gate (negative)` to continuously prove the gate is not silently green. Do NOT gate on a cache-hit percentage (PRD OQ4 — hit-rate is provider-dependent + flaky; CI gates only the byte-identity invariant). This step gates every subsequent merge from Phase 6 onward (amendment #16). Keep the YAML consistent with the existing job style (no new runner image, no DB service for this step).
  </action>
  <verify>
    <automated>grep -q "cache invariant gate" .github/workflows/ci.yml && grep -q "cache_invariant_audit.sh" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a step named `cache invariant gate` invoking `scripts/cache_invariant_audit.sh`.
    - The step lives in a Postgres-FREE job (no DB service dependency — the audit uses in-memory fakes).
    - The step exports `CI=true` (no-skip-as-green) and does NOT gate on any hit-rate percentage (only the invariant).
    - YAML is valid (the workflow parses; `grep` confirms the step + script reference).
  </acceptance_criteria>
  <done>The cache invariant gate runs as a Postgres-free CI step from Phase 6 onward, gating every subsequent merge against prefix poisoning via the runtime-faithful wrapper (D-06, amendment #16, SC#5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future PR diff → CI gate | Every subsequent merge crosses this gate; a PR that mutates `messages[0]` at runtime must be caught here. |
| gate output → pass/fail decision | A silently-green gate (exercising nothing) would let poisoning through — the negative test guards this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | cache_invariant_audit.sh + CI gate | mitigate | The runtime-faithful wrapper drives the real `cache-audit` replay (20-turn `runner.Turn`), counts + diffs hashes, and fails with `messages[0] mutated at <site>`; wired into CI from this phase onward (amendment #16). The negative test proves the gate is not silently green (Pitfall 3). This is the headline mitigation for prefix poisoning. HIGH severity — fully mitigated. |
| T-06-03 | Information Disclosure | gate stdout/stderr | mitigate | The gate prints only per-turn SHA-256 hex + an explicit mutation message — no message content, no secrets. |
| T-06-02 | Tampering | — | n/a | No `--since` parsing in this plan (06-04). |
| T-06-04 | Elevation | — | n/a | No DB/migration in this plan (gate is Postgres-free). |

No high-severity unmitigated threats. T-06-01 (the blocking high-severity threat for this phase) is mitigated here at the CI-gate level and proven by the SC#5 negative test.
</threat_model>

<verification>
- `bash scripts/cache_invariant_audit.sh` exits 0 with 20 identical hash lines (Postgres-free).
- `bash scripts/cache_invariant_negative_test.sh` proves the gate exits non-zero with `mutated` on a perturbed prefix (SC#5).
- `.github/workflows/ci.yml` has a `cache invariant gate` step invoking the wrapper in a Postgres-free job, exporting `CI=true`, not gating on hit-rate.
- The workflow YAML parses (valid).
</verification>

<success_criteria>
The cross-slice cache invariant gate (amendment #16) is live: `scripts/cache_invariant_audit.sh` drives the runtime-faithful 20-turn replay and fails with the explicit `messages[0] mutated at <site>` message (SC#1/SC#5), the negative test proves it is not silently green, and ci.yml runs it Postgres-free from this phase onward — gating every subsequent merge against prefix poisoning (T-06-01).
</success_criteria>

<output>
Create `.planning/phases/06-kv-cache-builder/06-05-SUMMARY.md` when done.
</output>
