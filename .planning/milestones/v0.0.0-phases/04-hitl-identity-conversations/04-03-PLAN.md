---
phase: 04-hitl-identity-conversations
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/agent/tools/ask_user.go
  - internal/agent/tools/ask_user_test.go
  - internal/agent/event.go
  - internal/agent/event_test.go
  - internal/agent/llm_agent.go
  - internal/agent/llm_agent_pause.go
  - internal/agent/llm_agent_pause_test.go
  - internal/askuser/store.go
  - internal/askuser/store_test.go
  - internal/askuser/main_test.go
autonomous: true
requirements: [CORE-02]

must_haves:
  truths:
    - "ask_user.Execute returns the ErrAwaitingUserInput sentinel, never a ToolResult"
    - "ask_user args validation rejects empty question, exactly-1 option, non-distinct labels, priority outside 0-100"
    - "the dispatch catches the sentinel BEFORE the generic error fallback, suppresses the RoleTool, and emits an Actions.AwaitingInput Event (never the iter error slot)"
    - "intra-turn exclusivity: ask_user batched with siblings rewrites the assistant msg to ask_user-only tool_calls; siblings dropped"
    - "askuser.Store ListPending returns rows in priority DESC, created_at ASC, token ASC order and survives a fresh Store instance (crash recovery)"
  artifacts:
    - path: "internal/agent/tools/ask_user.go"
      provides: "non-deferred ask_user tool + ErrAwaitingUserInput sentinel (pure types, no DB)"
      contains: "type ErrAwaitingUserInput"
    - path: "internal/agent/llm_agent_pause.go"
      provides: "pause-detection seam: catch sentinel, suppress RoleTool, rewrite assistant msg, emit Actions.AwaitingInput"
      contains: "ErrAwaitingUserInput"
    - path: "internal/askuser/store.go"
      provides: "Store over paused_states: Insert/GetByToken/ListPending/MarkResumed/MarkResumedBatch/Cleanup/AutoResolve"
      contains: "func (s *Store) ListPending"
  key_links:
    - from: "internal/agent/llm_agent_pause.go"
      to: "tools.ErrAwaitingUserInput"
      via: "errors.As before the generic err!=nil fallback"
      pattern: "errors\\.As.*ErrAwaitingUserInput"
    - from: "internal/agent/event.go"
      to: "Actions.AwaitingInput"
      via: "new pointer field round-tripped byte-identically through eventWire"
      pattern: "AwaitingInput"
---

<objective>
Build the HITL pause primitive (PRD 1.5, agent + store halves): the non-deferred `ask_user` tool + `ErrAwaitingUserInput` sentinel (pure types in `tools`, no DB — D-A1-04), the new `Actions.AwaitingInput` Event field, the pause-DETECTION seam split into `llm_agent_pause.go` (catch sentinel → suppress RoleTool → rewrite assistant msg to ask_user-only tool_calls → emit pause Event — D-A1-03/AM-01), and the `askuser.Store` over `aura.paused_states` (FIFO + crash recovery). Resume orchestration + the SOLE writer of paused_states is the Runner (04-05) — NOT here.

Purpose: This is the genuinely-new mechanism for crash-recoverable pauses; the agent stays DB-free, durability lives in the Store. CORE-02 Req#1-4 (+ #11's Store method).
Output: `ask_user` tool + sentinel; `Actions.AwaitingInput` with byte-identical round-trip; `llm_agent_pause.go` detection seam (intra-turn exclusivity rewrite); `askuser.Store`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hitl-identity-conversations/04-SPEC.md
@.planning/phases/04-hitl-identity-conversations/04-CONTEXT.md
@.planning/phases/04-hitl-identity-conversations/04-PATTERNS.md
@.planning/phases/04-hitl-identity-conversations/04-RESEARCH.md
@.planning/phases/04-hitl-identity-conversations/04-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ask_user tool + ErrAwaitingUserInput sentinel + Actions.AwaitingInput Event field (CORE-02 Req#1)</name>
  <read_first>
    - internal/agent/tools/text_response.go:18-46 (non-deferred Spec/Execute validate-then-act analog, PATTERNS L281-292) + read_tool_output.go (arg shape)
    - internal/agent/errors.go:1-10 (sentinel style — but ask_user must be a STRUCT carrying payload, PATTERNS L295-301)
    - internal/agent/event.go:62-66 (Actions struct) + :87-98 (eventWire) + :105-164 (MarshalJSON/UnmarshalJSON byte-identical round-trip; the MessageID *uuid.UUID omitempty-via-pointer trick at L94/178-183)
    - internal/agent/event_test.go (the decode(encode())==identity round-trip test to extend)
    - 04-SPEC.md Req#1 (args + validation rules), 04-CONTEXT.md D-A3-03 (no-secrets guardrail), D-A1-08 (carry tool_call_id + originating-agent id)
    - 04-PATTERNS.md L277-309 (ask_user.go) + L344-364 (event.go AwaitingInput sibling)
  </read_first>
  <behavior>
    - ask_user.Execute returns `(ToolResult{}, *ErrAwaitingUserInput{...})` — the sentinel, NOT a populated ToolResult
    - validation rejects: empty `question`; `options` count of exactly 1; non-distinct option labels; `priority` outside 0-100
    - valid args (kind=approval, no options) produce a sentinel carrying Question/Kind/Priority/ToolCallID
    - Event with Actions.AwaitingInput set round-trips byte-identically: `decode(encode(e)) == e`; an unset AwaitingInput never appears on the wire (omitempty via pointer)
  </behavior>
  <action>
    Create `internal/agent/tools/ask_user.go`: a non-deferred tool (`Deferred: false`) with `Spec()` (JSON-schema `{question:string, options?:[2-4 string|{label,value}], kind:clarification|approval|choice, priority?:int 0-100}`, no-secrets prohibition text in `Description` per D-A3-03) and `Execute(ctx, raw) (ToolResult, error)` that unmarshals, validates (empty question, exactly-1 option, non-distinct labels, priority 0-100), and returns `&ErrAwaitingUserInput{Question, Options, Kind, Priority, ToolCallID}` on success. Define `type ErrAwaitingUserInput struct{ Question string; Options []Option; Kind string; Priority int; ToolCallID string }` with `Error() string` (PATTERNS L295-301). PURE TYPES — no DB import (D-A1-04). Register it in the tool registry as non-deferred.
    Modify `internal/agent/event.go`: add `AwaitingInput *AwaitingInput \`json:"awaiting_input,omitempty"\`` to `Actions` (sibling to Escalate, D-A1-03). Define the `AwaitingInput` payload type carrying Question/Options/Kind/Priority/ToolCallID + originating-agent id (D-A1-08 swarm forward-compat). Thread it through `eventWire` + `MarshalJSON`/`UnmarshalJSON` so decode(encode())==identity holds and an unset pause field never leaks (mirror the MessageID *pointer omitempty trick). Extend `event_test.go` round-trip coverage.
  </action>
  <verify>
    <automated>go build ./internal/agent/... && go vet ./internal/agent/... && go test ./internal/agent/ ./internal/agent/tools/ && go test -race ./internal/agent/ ./internal/agent/tools/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agent/ ./internal/agent/tools/` and `-race` exit 0
    - table test: empty question, `["only-one"]`, `["dup","dup"]`, priority=-1 and priority=101 each rejected; valid approval call returns a non-nil `*ErrAwaitingUserInput` and a zero ToolResult
    - event round-trip test: an Event with Actions.AwaitingInput set satisfies `decode(encode(e))==e`; an Event without it produces no `awaiting_input` key on the wire (grep the marshaled bytes)
    - `internal/agent/tools/ask_user.go` imports no DB/sqlc package
  </acceptance_criteria>
  <done>ask_user is a validated non-deferred tool returning the sentinel; Actions.AwaitingInput round-trips byte-identically; the tool carries pure types only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pause-detection seam in llm_agent_pause.go — intercept sentinel, suppress RoleTool, intra-turn exclusivity rewrite, emit pause Event (CORE-02 Req#2 intra-turn)</name>
  <read_first>
    - internal/agent/llm_agent.go:191-230 (dispatch — where tool calls are dispatched) + :236-247 (runTool — the fallback that must NOT swallow the sentinel, PATTERNS L317-328) + :121-124 (budget exhaustion is Event-only — the precedent) + :155-157 (yield-after-false guard)
    - internal/agent/tools/ask_user.go (the sentinel from Task 1)
    - internal/agent/event.go (Actions.AwaitingInput from Task 1)
    - internal/agent/agenttest (FakeClient + mock agents — reuse for unit tests, no live OpenRouter)
    - 04-CONTEXT.md D-A1-03 (detection in agent, persistence in Runner), D-A1-07 (intra-turn rewrite to ask_user-only tool_calls for OpenAI wire-correctness), AM-01 (only detection half in agent; LoadHistory NOT here)
    - 04-PATTERNS.md L313-340 (llm_agent_pause.go) + Shared Patterns "Event-only termination" L576-579
    - 04-RESEARCH.md Pattern-3 (L257-260, anti-pattern: NEVER the iter error slot)
  </read_first>
  <behavior>
    - when a dispatched tool returns ErrAwaitingUserInput: the dispatch does NOT append a fake RoleTool for that call, and emits one Event with Actions.AwaitingInput populated (Question/Kind/Priority/ToolCallID)
    - the sentinel is intercepted BEFORE the generic `err != nil → "error: ..."` fallback in runTool
    - intra-turn exclusivity: when ask_user is batched with other tool calls in one assistant message, the persisted/emitted assistant message is rewritten to contain ONLY the ask_user tool_call(s); the sibling tool calls are dropped (re-emitted by the model next round)
    - the pause is NEVER surfaced through the iter.Seq2 error slot (that slot stays for real infra failure only, D-04/D-15)
    - for 2×ask_user + 1×read_tool_output in one turn: exactly 2 pause Events / payloads, read_tool_output dropped
  </behavior>
  <action>
    Split the pause-DETECTION logic into a new `internal/agent/llm_agent_pause.go` (AM-01: only the detection half lives in the agent; the agent stays DB-free; `LoadHistory` is NOT here). In `dispatch`, before the generic error fallback, check `errors.As(err, &target)` for `*tools.ErrAwaitingUserInput`; on a match: suppress the RoleTool for that call, rewrite the assistant message so its `tool_calls` contains only the ask_user call(s) (D-A1-07 OpenAI wire-correctness), and emit an `Event{Actions:{AwaitingInput: ...}}` (Event-only termination, never the error slot). Keep `llm_agent.go` ≤ 600 LOC (it is 320 now; the split is for separation per AM-01, not size). Preserve the existing yield-after-false guard.
    Create `internal/agent/llm_agent_pause_test.go` (unit, agenttest.FakeClient): (a) single ask_user(kind=approval) → one pause Event, no RoleTool appended for it; (b) 2×ask_user + 1×read_tool_output → exactly 2 pause payloads, the read_tool_output sibling dropped, assistant msg rewritten to 2 ask_user tool_calls only; (c) assert the pause is an Event, the iter error slot stays nil.
  </action>
  <verify>
    <automated>go build ./internal/agent/... && go vet ./internal/agent/... && go test ./internal/agent/ && go test -race ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agent/` and `-race` exit 0; goleak clean
    - single-ask_user test: exactly one Event with Actions.AwaitingInput; no `role='tool'` message appended for that call; iter error slot is nil
    - intra-turn test (2×ask_user + 1×read_tool_output): exactly 2 pause payloads; the persisted assistant message's tool_calls length == 2 (ask_user-only); read_tool_output dropped
    - `internal/agent/llm_agent.go` and `internal/agent/llm_agent_pause.go` each ≤ 600 LOC; agent package imports no DB/sqlc
    - `grep -r "error slot" / errors.As` review confirms the sentinel is intercepted before the generic fallback
  </acceptance_criteria>
  <done>Pause detection lives in llm_agent_pause.go: sentinel intercepted, RoleTool suppressed, intra-turn exclusivity rewrite applied, pause emitted as an Event; agent stays DB-free.</done>
</task>

<task type="auto">
  <name>Task 3: askuser.Store over paused_states — FIFO ordering + crash recovery + Resume/Batch + auto-resolve (CORE-02 Req#3, Req#11 store method)</name>
  <read_first>
    - internal/identity/store.go (the proven Store{pool,q} + SQLSTATE pattern from 04-02 — copy it)
    - internal/db/tx.go (WithTx — used by MarkResumedBatch + auto-resolve, from 04-01)
    - internal/db/sqlc/paused_states.sql.go + querier.go (generated surface from 04-01: InsertPausedState/GetByToken/ListPending/MarkResumed/MarkResumedBatch/CleanupResumedOlderThan/AutoResolveForConversation)
    - internal/db/db_test.go:1-45 (db_integration build tag + goleak + envOrSkip)
    - 04-SPEC.md Req#3 (query list, crash recovery, invalid token rejected), Req#4 (NO timed_out), Req#11 (Loop.Stop auto-resolve), AM-02 (resumed_answer = {action,content})
    - 04-PATTERNS.md L104-121 (askuser/store.go) — token ASC tiebreaker MANDATORY; never imports tools (D-A1-04)
    - 04-RESEARCH.md Pitfall 4 (FIFO determinism)
  </read_first>
  <action>
    Create `internal/askuser/store.go`: `Store{pool,q}` (copy 04-02's shape). Methods: `Insert(ctx, paused...)` (plain fields token/conversationID/kind/question/options/priority/toolCallID/resumeContext — NEVER imports `internal/agent/tools`, D-A1-04), `GetByToken`, `ListPending(ctx, convID)` returning rows ordered `priority DESC, created_at ASC, token ASC` (the token tiebreaker is mandatory — rows in one tx share created_at=now(), Pitfall 4), `MarkResumed(ctx, token, answer)` (answer stored as `{action: accept|decline|cancel, content}` JSON per AM-02; invalid/unknown token returns a clear error), `MarkResumedBatch(ctx, map)` (wrap db.WithTx — multi-row atomic), `AutoResolveForConversation(ctx, convID)` (the Req#11 Loop.Stop helper: set resumed_at=now(), resumed_answer='<auto-terminated: conversation ended>' WHERE conversation_id=:id AND resumed_at IS NULL — wrap db.WithTx), `CleanupResumedOlderThan`. Classify errors by SQLSTATE (errors.As + pgErr.Code), wrap with `%w`. NO timed_out anything (Req#4).
    Create `internal/askuser/main_test.go` (goleak) + `internal/askuser/store_test.go` (`//go:build db_integration`): insert 3 pending rows with equal priority in one tx → `ListPending` returns deterministic `token ASC` order (run under `-count=10` to catch flakiness); rows survive a fresh `New(pool)` instance (crash recovery); `MarkResumed` of an invalid token returns a clear error; `MarkResumedBatch` resolves many; `AutoResolveForConversation` leaves zero `resumed_at IS NULL` rows. envOrSkip → t.Fatal under $CI.
  </action>
  <verify>
    <automated>go build ./internal/askuser/ && go vet ./internal/askuser/ && go test ./internal/askuser/ && go test -race ./internal/askuser/ && grep -rL timed_out internal/askuser/ >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/askuser/` and `-race` exit 0
    - db_integration (Wave-2 merge): `go test -tags db_integration -race ./internal/askuser/ -count=10` green (FIFO determinism under repeat)
    - 3 equal-priority rows inserted in one tx → `ListPending` order is total and stable via `token ASC`
    - rows inserted by one Store instance are returned by a fresh `New(pool)` instance (crash recovery)
    - `MarkResumed(badToken)` returns a non-nil clear error; `AutoResolveForConversation` → `SELECT count(*) ... WHERE resumed_at IS NULL` == 0 for that conversation
    - `grep -r timed_out internal/askuser/` returns empty; askuser package does not import internal/agent/tools
  </acceptance_criteria>
  <done>askuser.Store persists/orders/resumes pauses deterministically with crash recovery and auto-resolve; no timed_out state; never imports tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call → ask_user.Execute | model-supplied question/options/priority cross here (untrusted) |
| dispatch → iter.Seq2 consumer | pause must travel as an Event, not the error slot |
| askuser.Store → Postgres | paused_states writes as bound params |
| LLM → ask_user prompt | model could attempt to solicit secrets from the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | secrets captured via ask_user (passwords/keys/tokens) | mitigate | No-secrets guardrail in the tool Description + system-prompt awareness (D-A3-03 / MCP MUST NOT) — Task 1 |
| T-04-09 | Tampering | malformed ask_user args (injection / bad option set) | mitigate | strict arg validation (empty question, option count, distinct labels, priority bounds) before producing the sentinel — Task 1 |
| T-04-10 | Denial of Service | pause leaking through the iter error slot crashing the loop | mitigate | pause is Event-only (Actions.AwaitingInput); error slot reserved for real infra failure (D-04/D-15) — Task 2 |
| T-04-11 | Tampering | non-deterministic FIFO order → wrong pause answered | mitigate | total-order `priority DESC, created_at ASC, token ASC` (Pitfall 4) verified under -count=10 — Task 3 |
| T-04-12 | Tampering | SQLSTATE message-matching breaking invalid-token rejection | mitigate | classify via errors.As(&pgErr)+Code only — Task 3 |
</threat_model>

<verification>
- Unit: `go test -race ./internal/agent/ ./internal/agent/tools/` green; goleak clean
- db_integration (Wave-2 merge, WSL stack up): `go test -tags db_integration -race ./internal/askuser/ -count=10` green (FIFO + crash recovery + auto-resolve)
- `golangci-lint run ./internal/agent/... ./internal/askuser/` == 0; every touched file ≤ 600 LOC
- `grep -r timed_out internal/` (agent+askuser portions) empty (Req#4)
</verification>

<success_criteria>
- ask_user is a validated non-deferred tool returning ErrAwaitingUserInput (pure types, no DB)
- Actions.AwaitingInput round-trips byte-identically; pause is Event-only
- llm_agent_pause.go intercepts the sentinel, suppresses the RoleTool, applies intra-turn exclusivity rewrite; agent stays DB-free
- askuser.Store: deterministic FIFO, crash recovery, Resume/Batch, auto-resolve; no timed_out; never imports tools
</success_criteria>

<output>
Create `.planning/phases/04-hitl-identity-conversations/04-03-SUMMARY.md` when done
</output>
