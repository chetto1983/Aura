---
phase: 11-skills
plan: 07
type: execute
wave: 7
depends_on: ["11-06"]
files_modified:
  - compose.yaml
  - docker/sandbox-agent/Dockerfile
  - internal/sandboxagent/client.go
  - internal/sandboxagent/client_test.go
  - internal/config/config.go
  - internal/skills/snippet.go
  - internal/skills/snippet_test.go
  - internal/cron/store.go
  - internal/cron/handlers/skill_ttl.go
  - internal/cron/handlers/skill_ttl_test.go
  - cmd/aura/serve.go
  - cmd/aura/skills.go
autonomous: true
requirements: [CAP-08]
must_haves:
  truths:
    - "sandbox-agent enforces a bearer token: --token wired, client sends Authorization: Bearer, healthcheck carries it (D-38, spike 008)"
    - "compose adds a read-only /skills bind mount from AURA_SKILL_EXPORT_DIR (D-17, spike 005)"
    - "Snippets save with language enum + risk gate; action=use returns instructions + the in-sandbox /skills path; execution is by-path via sandbox_exec (no new exec code, D-04)"
    - "TTL sweep is a cron skill_ttl_sweep TaskKind seeded daily; archives skills past AURA_SKILL_SNIPPET_TTL_DAYS + de-materializes (D-16)"
    - "Usage/archive state is a per-skill sidecar JSON (status/last_used_at/use_count, atomic-write); snippet_runs forensics optional (D-19)"
  artifacts:
    - path: "internal/cron/handlers/skill_ttl.go"
      provides: "skill_ttl_sweep Handler (Meta+Run), scans sidecars, archives+de-materializes (D-16)"
      contains: "skill_ttl_sweep"
    - path: "internal/skills/snippet.go"
      provides: "snippet save/use + usage sidecar JSON (D-19/D-20)"
      min_lines: 40
    - path: "compose.yaml"
      provides: "/skills ro mount + --token sandbox-agent (D-17/D-38)"
      contains: "/skills:ro"
    - path: "internal/sandboxagent/client.go"
      provides: "Authorization: Bearer from AURA_SANDBOX_AGENT_TOKEN (D-38)"
      contains: "Bearer"
  key_links:
    - from: "internal/sandboxagent/client.go"
      to: "aura-sandbox-agent :2468"
      via: "Authorization: Bearer ${AURA_SANDBOX_AGENT_TOKEN} on every request incl. health"
      pattern: "Authorization|Bearer"
    - from: "internal/cron/handlers/skill_ttl.go"
      to: "cron KindSkillTTLSweep + serve.go seed"
      via: "registered handler + daily-seeded scheduler_tasks row (kind CHECK widened in 0010)"
      pattern: "KindSkillTTLSweep|skill_ttl_sweep"
    - from: "action=use"
      to: "sandbox_exec {command: python3, args: [/skills/<name>.py]}"
      via: "by-path interpreter exec (never the exec bit), /skills ro mount"
      pattern: "/skills/"
---

<objective>
Sub-slice 7e — executable snippets v1 + the sandbox hardening floor Phase 11 depends on. Snippets save with a language enum (python|shell|js) + the SAVE-time risk gate (needs_network/needs_workspace surfaced); `action=use` returns instructions + the stable in-sandbox `/skills/<name>` path; execution is BY-PATH via the shipped `sandbox_exec` (`python3 /skills/x.py`, never the exec bit — spike 005) — NO new exec code (D-04). The TTL sweep is a cron `skill_ttl_sweep` TaskKind (D-16) seeded daily, scanning per-skill usage sidecar JSON (D-19), archiving + de-materializing past `AURA_SKILL_SNIPPET_TTL_DAYS`. The compose `aura-sandbox-agent` service gains the read-only `/skills` bind mount (D-17, spike 005) and the bearer-token auth (D-38, spike 008: `--token` + `Authorization: Bearer` + a healthcheck that carries the bearer).

Purpose: Deliver SC#4 (snippet save→by-path exec→output captured→TTL archive) + close the D-38 portable-hardening dependency (token + /skills ro mount) for model-authored skill code. The gVisor `runsc` overlay + seccomp re-tightening are a Phase-8 sandbox-wide regression — explicitly OUT of this plan (D-38 scope note); Phase 11 depends only on the portable floor.
Output: compose.yaml (/skills ro mount + --token) + sandboxagent bearer wiring + snippet.go (save/use + sidecar) + skill_ttl.go TaskKind + serve.go seed + `aura skills snippet save/exec` CLI + tests (incl. sandbox_integration by-path exec + TTL sweep integration).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
@.planning/phases/11-skills/11-PATTERNS.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-concurrency/SKILL.md
@.claude/skills/golang-context/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: compose /skills ro mount + sandbox-agent --token + sandboxagent Bearer wiring (D-17/D-38)</name>
  <read_first>
    - compose.yaml (lines ~92-118) — the aura-sandbox-agent service: command `["server","--no-token",...]`, volumes `[aura-sandbox-agent:/workspace]`, healthcheck curl `/v1/health`, no-new-privileges already set
    - internal/sandboxagent/client.go (lines ~51-92) — New(cfg) construction + the request build + status guard; where to add the Authorization header
    - internal/config/config.go SandboxAgent config block — where AURA_SANDBOX_AGENT_TOKEN lands; the AURA_SETUP_TOKEN first-boot-gen pattern to mirror
    - .planning/spikes/008-sandbox-token-auth/README.md + compose.token.yaml — the validated --token wiring: 401 unauth (incl. /v1/health → healthcheck MUST carry the bearer), 200 + exec on correct token (~5 LOC client change)
    - .planning/spikes/005-skills-ro-mount/README.md + compose.skills-mount.yaml — the validated ro /skills bind mount (works on Docker Desktop; the device= failure class does NOT apply to bind mounts)
    - .planning/phases/11-skills/11-CONTEXT.md D-17 (ro /skills mount, materialize active-only) + D-38 (token VALIDATED ~5 LOC; gen at first boot mirror AURA_SETUP_TOKEN; healthcheck must carry the bearer; portable floor; gVisor is Phase-8 OUT)
  </read_first>
  <action>
    Edit `compose.yaml` aura-sandbox-agent service:
    - command: replace `--no-token` with `--token`, `"${AURA_SANDBOX_AGENT_TOKEN}"` (keep --no-telemetry --host 0.0.0.0 --port 2468). The token comes from the env (compose interpolates from .env / the process env).
    - volumes: ADD `- ${AURA_SKILL_EXPORT_DIR:-~/.aura/skills/export}:/skills:ro` alongside the existing `aura-sandbox-agent:/workspace` (the stack's first bind mount; spike 005 — the device= class does NOT apply; use a host-path bind, ro). Document the ro/active-only invariant in a comment (D-17).
    - healthcheck: the curl MUST carry the bearer now (`-H "Authorization: Bearer ${AURA_SANDBOX_AGENT_TOKEN}"`) since `/v1/health` becomes 401 without it (spike 008 Pitfall 7 — forgetting this flips the container unhealthy).
    Edit `internal/sandboxagent/client.go`: add a `Token string` to Config; on EVERY request (exec + health), if Token != "" set `req.Header.Set("Authorization", "Bearer "+token)` (~5 LOC per spike 008). A 401 surfaces a clear "sandbox-agent auth failed" error.
    Edit `internal/config/config.go`: add `AURA_SANDBOX_AGENT_TOKEN` to the SandboxAgent block; gen at first boot if absent (mirror the AURA_SETUP_TOKEN first-boot-gen path — read that code first) and persist it where the other first-boot secrets live, so compose + the client read the SAME value.
    Edit `internal/sandboxagent/client_test.go`: an httptest.Server asserting the Authorization: Bearer header is sent on exec AND health; a 401 surfaces the auth error. Race-clean.
    Edit `docker/sandbox-agent/Dockerfile`: bake the xlsx North-Star Python dep set so the 11-08 E2E is deterministic and does NOT rely on Docker Desktop's accidental NAT egress (spike 007 confirmed the image has pip + egress on Docker Desktop ONLY — on native-Linux the non-masquerading bridge has no egress, so an unbaked dep would silently fail the E2E). Ground truth (read the Dockerfile first): the current image installs only `python3 python3-pip python-is-python3` — openpyxl/defusedxml/lxml/validators are NOT present (there is no "Phase-5 baked image"; that bespoke image was killed in the sandbox-agent pivot, amendment #44). Add a `RUN python3 -m pip install --no-cache-dir <pinned versions>` step (as root, before `USER sandbox`) installing the common heavy set `openpyxl defusedxml lxml validators` (the hybrid dep-strategy's bake leg, D-36; the long tail stays on-demand uv). Pin exact versions. This is the offline-resilient default; `deps:` frontmatter (snippet plan Task 2) drives any on-demand long-tail leg.
    Do NOT add the gVisor `compose.gvisor.yaml` overlay or re-tighten seccomp here — that is the Phase-8 regression (D-38 scope note); this plan delivers ONLY the portable floor (token + ro /skills mount + baked common dep set).
  </action>
  <verify>
    <automated>go vet ./internal/sandboxagent/... && go build ./... && go test -race ./internal/sandboxagent/... && docker compose -f compose.yaml config >/dev/null && echo COMPOSE_VALID</automated>
  </verify>
  <acceptance_criteria>
    - compose.yaml aura-sandbox-agent command contains "--token" and does NOT contain "--no-token"
    - compose.yaml aura-sandbox-agent volumes contains "/skills:ro"
    - compose.yaml healthcheck contains "Authorization: Bearer" (spike 008 — health carries the bearer)
    - internal/sandboxagent/client.go contains `Bearer ` and sets the Authorization header on requests
    - client_test.go asserts the Bearer header is present on both exec and health requests AND that a 401 surfaces an auth error
    - `docker compose -f compose.yaml config` validates (prints COMPOSE_VALID)
    - internal/config/config.go contains "AURA_SANDBOX_AGENT_TOKEN"
    - docker/sandbox-agent/Dockerfile installs openpyxl, defusedxml, lxml, validators (pinned); after `make sandbox-up`, `docker exec aura-sandbox-agent python3 -c "import openpyxl,defusedxml,lxml,validators"` exits 0 (the xlsx North-Star deps are baked, NOT reliant on Docker Desktop NAT egress)
  </acceptance_criteria>
  <done>sandbox-agent runs --token, the client sends Authorization: Bearer (incl. health), compose adds the ro /skills bind mount; token gen mirrors AURA_SETUP_TOKEN; gVisor explicitly NOT touched (Phase-8 scope); client tests + compose config validate.</done>
</task>

<task type="auto">
  <name>Task 2: snippet save/use + usage sidecar + by-path exec + aura skills snippet CLI</name>
  <read_first>
    - internal/agent/tools/sandbox_exec.go + internal/agent/tools/skill.go/skill_read.go (11-02) — the existing sandbox_exec seam + the action=use handler to extend for snippet path-return (D-04: use returns instructions + the in-sandbox /skills path; the model calls sandbox_exec {command:"python3", args:["/skills/<name>.py",...]})
    - internal/cron/handlers/backup.go (backupDir, atomic FS write) — the atomic sidecar-JSON-write idiom
    - internal/skills/writer.go + materialize.go (11-04) — snippets materialize on activation only (D-17); de-materialize on archive/delete
    - internal/skills/frontmatter.go (11-02) — the snippet fields: language (required enum python|shell|js, D-20), needs_network/needs_workspace (tier-relevant, surfaced in the SAVE gate), inputs_schema/outputs_desc/deps/tags (docs-only, tolerated)
    - .planning/phases/11-skills/11-CONTEXT.md D-19 (sidecar JSON per skill: status/last_used_at/use_count, atomic-write, the ONE live-state source; snippet_runs DB optional forensics) + D-20 (snippet frontmatter; no runtime arg validation v1) + D-04 (no bespoke run; use returns path; CLI snippet exec stamps usage; model-run usage-stamping via a sandbox_exec /skills-prefix hook = discretion) + D-36/D-37 (deps strategy + egress — needs_network surfaced at the RISKY tier)
    - .planning/spikes/005-skills-ro-mount/README.md — ALWAYS interpreter+path (python3 /skills/x.py), NEVER the exec bit (Docker Desktop masks 777)
    - cmd/aura/skills.go (11-05/11-06) — the runSkills switch to extend with `snippet save`/`snippet exec`
  </read_first>
  <action>
    Create `internal/skills/snippet.go`:
    - `SaveSnippet(ctx, name, language, body string, fm Frontmatter, actor) (status, err)`: validates language ∈ {python,shell,js} (all in the sandbox image), runs the validator (write boundary), computes the SAVE-time risk gate via scoring (RISKY; needs_network/needs_workspace surfaced — D-20), lands in pending/ + audit (the same writer path as instruction skills — a snippet is a skill with type:snippet). On activation, materialize the snippet file into /skills.
    - `UseSnippet(name) (instructions, sandboxPath string, err error)`: returns the docs-rendered instructions (inputs_schema/outputs_desc/deps/tags rendered for the model, D-20) + the stable in-sandbox path `/skills/<name>.<ext>` — the model then calls sandbox_exec with `python3 /skills/<name>.py` (interpreter+path, NEVER the exec bit). Extend the action=use handler (11-02) to return this for type:snippet skills.
    - Usage sidecar: `usageSidecarPath(name)` → a per-skill JSON `{status, last_used_at, use_count}` (D-19), atomic-write (temp+rename, backup.go idiom). `StampUsage(name)` bumps use_count + last_used_at. The deterministic operator stamp is `aura skills snippet exec` (below); the model-run stamp via a sandbox_exec `/skills`-prefix argv hook is OPTIONAL discretion — if shipped, hook it on the structured argv (argv is ground truth, NOT NL regex); otherwise document it as a fast-follow.
    Decide the dep-strategy (D-36): default to HYBRID per RESEARCH recommendation — the xlsx common set (openpyxl/defusedxml/lxml/validators) is BAKED into the sandbox image by Task 1's Dockerfile step (there is NO pre-existing "Phase-5 baked image" — that bespoke image was killed in the sandbox-agent pivot, amendment #44; ground-truth the Dockerfile), so the North-Star resolves offline-deterministically (NOT relying on Docker Desktop's accidental NAT egress, spike 007); the long tail stays on-demand uv with `deps:` frontmatter load-bearing for that leg. needs_network:true is surfaced at the RISKY tier (D-37 egress posture; the enforced proxy is Phase-8, advisory on Docker Desktop) — do NOT build the proxy here.
    Optionally create migration `0011_snippet_runs` ONLY if per-run forensics are wanted now (D-19/A4 calls it optional/discretion) — the sidecar JSON is the live-state source either way; default = skip 0011 unless a forensics need is concrete (keep scope lean).
    Extend `cmd/aura/skills.go`: `case "snippet"` with `save --lang --name` and `exec <name>` (the deterministic operator exec path that runs via the sandbox_exec seam by-path AND stamps usage — SC#4).
    Create `internal/skills/snippet_test.go` (unit + a `sandbox_integration db_integration`-tagged TestSnippetExec): language-enum validation, sidecar atomic-write + StampUsage bump, use returns the /skills path, and the tagged by-path exec captures output (SC#4 — actually RUNS against the stack with the token + /skills mount; t.Fatal-under-CI if env unset). Race-clean, files ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./internal/skills/ && go test -tags 'sandbox_integration db_integration' -run TestSnippetExec ./internal/skills/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - internal/skills/snippet.go validates language ∈ {python,shell,js} and rejects others
    - UseSnippet returns a path under "/skills/" and snippet.go contains no `os/exec`/no new exec path (execution is via the existing sandbox_exec seam — D-04)
    - snippet.go writes the usage sidecar atomically (temp+rename) and StampUsage bumps use_count
    - cmd/aura/skills.go has `case "snippet"` with save + exec subcommands
    - `go test -tags 'sandbox_integration db_integration' -run TestSnippetExec ./internal/skills/` RUNS against the stack (by-path python3 /skills/x.py, output captured) — not skipped under CI
    - needs_network:true is surfaced at the RISKY tier in the save gate (asserted in snippet_test.go)
  </acceptance_criteria>
  <done>Snippets save (language-enum + risk-gated) → activate-materialize → use returns the /skills path → exec by-path via sandbox_exec (no new exec code); usage sidecar atomic-write + stamp; SC#4 by-path exec integration green; hybrid dep-strategy resolves the North-Star with no image change; all green + race-clean.</done>
</task>

<task type="auto">
  <name>Task 3: skill_ttl_sweep cron TaskKind + daily seed (D-16)</name>
  <read_first>
    - internal/cron/handlers/backup.go (lines ~63-69 Meta, ~198-220 sweepRetention) — the Handler Meta+Run + retention-sweep idiom to mirror
    - internal/cron/dispatch.go (lines ~49-52 Handler interface) — the contract Meta()+Run()
    - internal/cron/store.go (lines ~31-36 KindReminder...) — where to add KindSkillTTLSweep next to the existing kinds (the 0010 migration already widened the DB kind CHECK in 11-04)
    - cmd/aura/serve.go (lines ~106-112 the real handler map + ~109-110 the backup daily-seed) — register handlers.SkillTTLSweepHandler + seed the daily sweep task
    - internal/skills/snippet.go (Task 2) — the usage-sidecar scan + Archive/Dematerialize the sweep calls
    - internal/skills/main_test.go (11-02) — goleak; the sweep must not leak
    - .planning/phases/11-skills/11-CONTEXT.md D-16 (TTL sweep = scheduler TaskKind, seeded daily like backups; threshold AURA_SKILL_SNIPPET_TTL_DAYS=90; PRD ttl_sweeper.go amended away) + .planning/phases/11-skills/11-RESEARCH.md §"Pattern 4" + §A2 (the kind CHECK was widened in 0010 — confirm; this TaskKind is system-seeded, NOT model-schedulable, so the task.go tool kind enum stays unchanged)
  </read_first>
  <action>
    Add `KindSkillTTLSweep TaskKind = "skill_ttl_sweep"` to `internal/cron/store.go` next to the existing kinds.
    Create `internal/cron/handlers/skill_ttl.go`: `SkillTTLSweepHandler{Loader/Snippet store deps}` implementing cron.Handler:
    - `Meta() HandlerMeta` → `{Kind: "skill_ttl_sweep", MaxDuration: 2*time.Minute, ReschedulesOnRecovery: false}` (RESEARCH Pattern 4).
    - `Run(ctx, job) (summary, err)`: scan the per-skill usage sidecars (small N, D-19), for each snippet whose last_used_at is older than AURA_SKILL_SNIPPET_TTL_DAYS → Archive (writer) + Dematerialize (remove from /skills export). Best-effort, log-and-skip per skill, return a count summary (mirror backup.go sweepRetention). Audit each auto-archive with the D-29 system row (`auto` source, gate_recommended=false, gate_taken=true).
    Edit `cmd/aura/serve.go`: register `handlers.SkillTTLSweepHandler{...}` in the real kind→handler map; seed a DAILY scheduler_tasks row for it on boot (mirror the backup daily-seed at serve.go:109-110; idempotent — only seed if absent). The kind CHECK widening (0010, 11-04) lets this INSERT succeed.
    Create `internal/cron/handlers/skill_ttl_test.go` (unit + a db_integration-tagged seed test): the sweep archives a snippet past the TTL + leaves a fresh one; the seeded task INSERTs successfully against the 0010-widened CHECK (db_integration). goleak-clean. Files ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./internal/cron/... && go build ./... && go test -race ./internal/cron/... && go test -tags db_integration -run 'TestSkillTTLSweep|TestSkillTTLSeed' ./internal/cron/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/cron/store.go contains `KindSkillTTLSweep` = "skill_ttl_sweep"
    - internal/cron/handlers/skill_ttl.go Meta() returns Kind "skill_ttl_sweep"
    - cmd/aura/serve.go registers SkillTTLSweepHandler in the handler map AND seeds a daily task for it (idempotent)
    - skill_ttl_test.go asserts a snippet past AURA_SKILL_SNIPPET_TTL_DAYS is archived + de-materialized and a fresh one is kept
    - the db_integration seed test confirms the skill_ttl_sweep row INSERTs against the 0010-widened kind CHECK (NOT rejected — A2 landmine closed)
    - the sweep auto-archive writes a D-29 `auto` audit row (gate_recommended=false, gate_taken=true)
  </acceptance_criteria>
  <done>skill_ttl_sweep TaskKind registered + daily-seeded (reuses Phase-10 persistence/HA/catch-up); scans usage sidecars, archives+de-materializes past the TTL, audits with the system row; the 0010 kind CHECK accepts the seed (A2 closed); goleak-clean; all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aura → sandbox-agent :2468 | model-authored snippet code is executed in the container; the loopback exec endpoint must be authenticated |
| /skills bind mount → container | materialized snippet files become visible read-only in the sandbox; must stay ro + symlink-free |
| model snippet code → egress | a needs_network snippet may attempt arbitrary egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-E1 | Elevation of Privilege | unauthenticated exec on loopback :2468 (anyone can exec) | mitigate | bearer token (--token + Authorization: Bearer, D-38 spike 008); healthcheck carries the bearer — Task 1 (HIGH severity, mitigated + client test) |
| T-11-07-T1 | Tampering | snippet code mutates /skills from inside the sandbox | mitigate | /skills mounted READ-ONLY (D-17 spike 005); materialize active-only + symlink-stripped (11-04) — Task 1 |
| T-11-07-I1 | Information Disclosure | arbitrary egress from model snippet code | mitigate | needs_network:false → no proxy env/route; needs_network:true surfaced at RISKY tier + (enforced on native-Linux via the Phase-8 proxy, advisory on Docker Desktop) D-37 — Task 2 (the enforced proxy is Phase-8; this plan surfaces the tier + flags the dependency) |
| T-11-07-T2 | Tampering | exec-bit reliance fails on Docker Desktop (777 masked) → wrong invocation | mitigate | ALWAYS interpreter+path (python3 /skills/x.py), never ./x.py (spike 005) — Task 2, by-path exec test |
| T-11-07-D1 | Denial of Service | snippets accumulate forever, bloat /skills + manifest | mitigate | TTL sweep archives+de-materializes past AURA_SKILL_SNIPPET_TTL_DAYS (D-16) — Task 3 |
| T-11-07-R1 | Repudiation | auto-archive not recorded | mitigate | the sweep writes a D-29 `auto` audit row — Task 3 |
| T-11-07-SC | Tampering | gVisor runsc prod-isolation tier NOT delivered here | accept | D-38 scope note: gVisor + seccomp re-tightening is a Phase-8 sandbox-wide regression, tracked there; Phase 11 depends only on the portable floor (token + ro mount + no-new-privileges + read-only rootfs) delivered in Task 1 |

Sandbox image deps (openpyxl/defusedxml/lxml/validators) are baked into docker/sandbox-agent/Dockerfile by Task 1 (NOT a pre-existing image, and NOT installed by this plan's Go code); the hybrid dep-strategy resolves the North-Star offline-deterministically. The bake is a pinned `pip install` inside the sandbox image build — covered by RESEARCH §"Package Legitimacy Audit" (the four pypi packages); it is NOT an Aura-Go-module install and adds no Go supply-chain surface.
</threat_model>

<verification>
- `go test -race ./internal/sandboxagent/... ./internal/skills/ ./internal/cron/...` all green; goleak clean
- `docker compose -f compose.yaml config` validates (token + /skills:ro + bearer healthcheck)
- `go test -tags 'sandbox_integration db_integration' -run TestSnippetExec ./internal/skills/` RUNS by-path exec against the stack (SC#4, not skipped)
- `go test -tags db_integration -run 'TestSkillTTLSweep|TestSkillTTLSeed' ./internal/cron/...` green (A2 kind CHECK closed)
- golangci-lint 0; all files ≤600 LOC
</verification>

<success_criteria>
- SC#4: snippet save → by-path exec in the sandbox → output captured → TTL archive after AURA_SKILL_SNIPPET_TTL_DAYS
- D-38 portable floor closed: sandbox-agent bearer token + ro /skills mount (gVisor explicitly Phase-8-scoped, NOT here)
- TTL sweep is a cron TaskKind seeded daily; usage sidecar JSON is the live-state source (D-19)
- Coverage toward the ≥85% floor; sandbox_integration tier RUNS live (no-skip-as-green)
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-07-SUMMARY.md` when done
</output>
