---
phase: 11-skills
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - go.mod
  - go.sum
  - internal/skills/loader.go
  - internal/skills/frontmatter.go
  - internal/skills/manifest.go
  - internal/skills/builtin.go
  - internal/skills/embed/skill-creator/SKILL.md
  - internal/skills/loader_test.go
  - internal/skills/frontmatter_test.go
  - internal/skills/manifest_test.go
  - internal/skills/main_test.go
  - internal/agent/tools/skill.go
  - internal/agent/tools/skill_read.go
  - internal/agent/tools/skill_test.go
  - internal/agent/prompt.go
  - internal/config/config.go
  - cmd/aura/main.go
autonomous: true
requirements: [CAP-07]
must_haves:
  truths:
    - "Loader scans multi-root FS, parses SKILL.md frontmatter via a real YAML lib, caches with 1s TTL, skip-logs invalid (no blocklist on load)"
    - "The skill tool is registered non-deferred; its Description is a turn-stable, byte-stable, alphabetical manifest with BM25-overflow tail"
    - "skill action=list|info|use returns skill metadata/body; messages[0] gains exactly ONE frozen mechanism sentence"
    - "embedded skill-creator builtin materializes and appears in the loader"
  artifacts:
    - path: "internal/skills/loader.go"
      provides: "multi-root FS scan + TTL cache + parse-only validation (D-28)"
      min_lines: 40
    - path: "internal/skills/frontmatter.go"
      provides: "goccy/go-yaml parse + CRLF normalize + tolerate optional fields (D-30)"
      min_lines: 30
    - path: "internal/skills/manifest.go"
      provides: "skill Description generation (D-06) + BM25 overflow corpus (D-09)"
      min_lines: 30
    - path: "internal/agent/tools/skill.go"
      provides: "ONE skill tool, ActionRouter, OpenAI-wire-safe schema (D-01/D-05/D-10)"
      exports: ["SkillTool"]
    - path: "internal/agent/prompt.go"
      provides: "ONE frozen mechanism sentence about skills (D-06)"
      contains: "skill"
  key_links:
    - from: "internal/agent/tools/skill.go"
      to: "internal/skills loader"
      via: "consumer-declared interface seam (no internal/skills import in tools)"
      pattern: "interface|Loader"
    - from: "cmd/aura/main.go buildBaseRegistry"
      to: "internal/agent/tools.SkillTool"
      via: "reg.Register(newSkillTool(...))"
      pattern: "reg.Register"
    - from: "internal/agent/tools/skill.go Spec().Description"
      to: "internal/skills/manifest.go"
      via: "renderManifestDescription, turn-stable + alphabetical (cache-load-bearing)"
      pattern: "Description|Render"
---

<objective>
Sub-slice 7a — the read half of the skills system: a greenfield `internal/skills/` loader (multi-root FS scan, 1s TTL cache, goccy/go-yaml frontmatter parse, parse-only validation), the ONE non-deferred `skill` tool (ActionRouter, OpenAI-wire-safe schema, read actions list/info/use), the D-06 manifest-in-Description (turn-stable, alphabetical, BM25 overflow), the embedded skill-creator builtin (D-31), and the ONE frozen mechanism sentence in messages[0].

Purpose: Establish the contracts + read path the write/install/snippet plans build on (interface-first ordering — the loader seam + tool schema are defined here, consumed downstream). NO write boundary, NO blocklist enforcement (disk is operator-trusted, D-28), NO migration.
Output: `internal/skills/{loader,frontmatter,manifest,builtin}.go` + embedded skill-creator + `internal/agent/tools/{skill,skill_read}.go` + prompt.go sentence + config + registry wiring + unit tests. go.mod gains goccy/go-yaml + promotes golang.org/x/text to direct.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
@.planning/phases/11-skills/11-PATTERNS.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps + greenfield loader/frontmatter/manifest + builtin skill-creator</name>
  <read_first>
    - internal/conversations/orphan_scan.go (lines ~70-90) — the Lstat-no-follow symlink-strip FS-scan idiom to reuse in the loader
    - internal/agent/tools/search.go toolSearchDescription (lines ~75-125) + sourceOrientation (113-124) — the registry-derived, byte-stable, SORTED Description precedent for the manifest
    - internal/agent/tools/bm25.go (lines ~96-145) newBM25Index/rank — the D-09 overflow ranker to reuse
    - internal/cron/handlers/main_test.go — the goleak.VerifyTestMain TestMain pattern for internal/skills/main_test.go
    - .planning/spikes/006-xlsx-skill-dry-run/README.md — the real corpus frontmatter shape (double-quoted scalar with escaped quotes + CRLF) goccy must handle
    - .planning/spikes/004b-install-native-clone/README.md — the canonical-hash + symlink-strip notes (loader scan reuses the strip)
    - go.mod — confirm golang.org/x/text v0.37.0 is already indirect; goccy/go-yaml absent
    - D:/tmp/codex/codex-rs/skills/src/assets/samples/skill-creator/SKILL.md — content reference for the embedded builtin (D-31)
  </read_first>
  <behavior>
    - Test: loader scans two roots, returns merged skills with precedence (later root wins on name collision); a malformed SKILL.md is skip-logged, not fatal
    - Test: frontmatter parse handles a double-quoted description with escaped quotes + CRLF line endings (spike 006 corpus shape) and tolerates unknown optional fields (license/compatibility/metadata/allowed-tools) without error
    - Test: loader TTL cache returns cached result within 1s, re-scans after; no goroutine leak (goleak)
    - Test: a skill dir containing a symlink has the symlink stripped/skipped at scan (Lstat-no-follow, Pitfall 4)
    - Test: manifest Render() output is byte-identical across two calls and is alphabetically sorted by skill name; past AURA_SKILL_MANIFEST_CAP_BYTES it ends with "N more — search with skill action=list {query}"
    - Test: the embedded skill-creator builtin appears in the loader's skill set after materialization
  </behavior>
  <action>
    Run `go get github.com/goccy/go-yaml@v1.19.2` and `go get golang.org/x/text@v0.37.0` (promotes the existing indirect dep to direct — verify with `go list -m golang.org/x/text` first that v0.37.0 is in tree; if a newer patch is current, take it).
    Create `internal/skills/frontmatter.go`: a `parseFrontmatter([]byte) (Frontmatter, body string, err error)` that CRLF-normalizes first (replace \r\n→\n), splits the leading `---`...`---` YAML block, parses it with goccy/go-yaml into a `Frontmatter` struct with fields per D-30: `Name` (required, ≤64), `Description` (required, ≤1024), `Always bool` (`always`), `Type string` (`type`, default "instruction", enum instruction|snippet), plus tolerated-inert fields `License`/`Compatibility`/`Metadata map[string]any`/`AllowedTools`, plus snippet docs fields `Language`/`InputsSchema`/`OutputsDesc`/`Deps`/`Tags`/`NeedsNetwork`/`NeedsWorkspace`. Use goccy lax parse — tolerate unknown keys. picobot's hand-roll is the explicit anti-reference; do NOT regex-scan.
    Create `internal/skills/loader.go`: a `Loader` with multi-root config (`[]string` roots, precedence = later wins) + a 1s TTL cache (mutex-guarded snapshot + last-scan timestamp; NO background goroutine — lazy re-scan on read past TTL, so goleak stays clean). `Scan()` walks each root, reads SKILL.md, calls parseFrontmatter, validates parse + structure + name regex (`^[a-z0-9-]{1,64}$`, must match dir name) + body size (≤AURA_SKILL_BODY_CAP_BYTES) ONLY — NO blocklist (D-28, disk operator-trusted). Invalid skills are skip-logged via slog (warn) and excluded, never fatal. Reuse the orphan_scan Lstat-no-follow guard to strip symlinks during the walk (Pitfall 4). Expose `List() []Skill`, `Get(name) (Skill, bool)`.
    Create `internal/skills/builtin.go`: an `embed.FS` (`//go:embed embed/...`) holding the skill-creator meta-skill + a `materializeBuiltins(dir string)` that codex-style fingerprint-materializes embedded skills into AURA_SKILLS_DIR on first boot (write only if absent/changed). ONLY skill-creator is embedded (D-31; find-skills is NOT embedded — it arrives via the install flow).
    Create `internal/skills/embed/skill-creator/SKILL.md`: a spec-compliant instruction skill (English-only) teaching trigger-rich descriptions, when `always:true` is appropriate, and the snippet format — adapted from the codex sample. Frontmatter `name: skill-creator`, trigger-rich `description`.
    Create `internal/skills/manifest.go`: `RenderManifest(skills []Skill, capBytes int) string` building an alphabetically-sorted (sort.Slice by Name) name+description block; once the running byte count would exceed capBytes, stop and append "N more — search with skill action=list {query}" (D-09). Also a `BM25Corpus(skills []Skill) []string` helper feeding spec-shaped name+description docs for the overflow `list` ranker (the ranker itself is tools/bm25.go, called from the tool in Task 2). Byte-stability is load-bearing — any reshuffle cache-busts (D-06).
    Create `internal/skills/main_test.go` with `goleak.VerifyTestMain(m)`. Create `loader_test.go`, `frontmatter_test.go`, `manifest_test.go` covering the behaviors above (table-driven, realistic fixtures under testdata/, race-clean).
    Add the D-34 env vars to `internal/config/config.go`: AURA_SKILLS_DIR (default ~/.aura/skills), AURA_SKILL_BODY_CAP_BYTES (32768), AURA_SKILL_MANIFEST_CAP_BYTES (8192), AURA_SKILL_EXPORT_DIR (~/.aura/skills/export) — following the existing AURA_<DOMAIN>_<UNIT> load pattern in config.go (read the file first to mirror the exact loader shape). Keep files ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./internal/skills/... && go build ./... && go test -race ./internal/skills/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/skills/...` exits 0 with goleak clean
    - internal/skills/frontmatter.go imports github.com/goccy/go-yaml
    - internal/skills/loader.go contains "Lstat" (symlink-strip) and does NOT contain "blocklist" (D-28 — no blocklist on load)
    - go.mod lists `github.com/goccy/go-yaml` and golang.org/x/text as a direct (non-`// indirect`) require
    - manifest_test.go asserts two RenderManifest calls return byte-identical strings AND alphabetical order
    - internal/skills/embed/skill-creator/SKILL.md exists with `name: skill-creator` frontmatter
    - frontmatter_test.go contains a test case with a CRLF-terminated SKILL.md fixture AND a double-quoted scalar `description` field with escaped quotes (the spike 006 corpus edge case that mandated goccy over yaml.v3/picobot)
  </acceptance_criteria>
  <done>Loader/frontmatter/manifest/builtin compile, unit tests + goleak green; deps installed and direct; manifest byte-stable + alphabetical + overflow-tailed; no blocklist on the load path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: skill tool (ActionRouter, read actions) + manifest-in-Description + messages[0] sentence + registry wiring</name>
  <read_first>
    - internal/agent/tools/task.go (lines ~70-160, 96-117) — the copy-from template: taskStore consumer-declared seam, Execute+actionRouter dispatch, taskParamsSchema OpenAI-wire-safe discipline (NO root oneOf/enum; required=[action] only)
    - internal/agent/tools/action.go (lines ~11-58) — ActionRouter (kept generic; its doc comment names Slice 7 as the reuse target)
    - internal/agent/tools/search.go (lines ~60-67 + toolSearchDescription) — the non-deferred Spec + registry-derived Description precedent; sandbox_exec.go:64-67 schema-blindness comment (why skill is non-deferred, D-05)
    - internal/agent/tools/bm25.go newBM25Index/rank — the overflow ranker the list action calls
    - internal/agent/prompt.go (lines ~14-20) — the byte-stable SystemPrompt const + the existing tool_search mechanism sentence (the discipline to mirror)
    - internal/agent/tools/spec.go + manifest.go — Spec/Deferred shape + alphabetical manifest ordering (cache-load-bearing)
    - cmd/aura/main.go buildBaseRegistry (lines ~97-115) — the reg.Register block + newTaskTool adapter pattern (consumer-side seam keeps tools free of internal/skills import)
    - internal/agent/tools/tools.go NewResult — preview+sidecar for the use/info body delivery (D-08)
  </read_first>
  <behavior>
    - Test: SkillTool.Spec() has Name "skill", Deferred false, a schema whose root `required` is exactly ["action"] with NO root oneOf/anyOf/enum, and an `action` property carrying a string enum including list/info/use (and the write/install actions reserved for downstream)
    - Test: reg.Validate() holds with the SkillTool registered (Pitfall 6 — the schema validates)
    - Test: Execute with `{"action":""}` returns "action is required"; an unknown action returns the "valid actions are ..." error
    - Test: action=list returns the manifest/BM25-ranked results; action=info returns plain body; action=use returns the body wrapped in the authority frame ("Follow these skill instructions for the current task: ...") via NewResult (D-08)
    - Test: SkillTool.Spec().Description (the manifest) is byte-identical across two calls for a fixed loader snapshot (turn-stable, D-06)
  </behavior>
  <action>
    Create `internal/agent/tools/skill.go`: `SkillTool` copying task.go's shape. Declare a consumer-side interface seam `skillLoader` (List/Get/manifest-render — whatever the read actions need) IN the tools package so `internal/agent/tools` does NOT import `internal/skills` (the task.go taskStore seam is the exact pattern; the live `internal/skills.Loader` satisfies it, wired by an adapter in cmd/aura). Execute parses only `action` and dispatches via NewActionRouter over the action map (this plan wires `list|info|use`; reserve create|update|delete|install|catalog|restore|archive as router keys returning a "not yet wired" error so the schema enum is complete from the start and downstream plans just fill handlers). Spec(): Name "skill", Deferred false (D-05), Description = `renderManifestDescription()` computed from the loader snapshot at build time (turn-stable, busts the prefix cache ONCE on add/remove — accepted, D-06), Parameters = a `skillParamsSchema` mirroring taskParamsSchema's discipline VERBATIM (top-level required=["action"] only, per-action fields documented in property descriptions, `action` property has a string-level enum, NO root oneOf/anyOf/enum — copy the load-bearing comment from task.go:96-117).
    Create `internal/agent/tools/skill_read.go`: `actionList` (renders the manifest; on an overflow query, builds the BM25 corpus via the loader and ranks with bm25.go), `actionInfo` (plain body read — inspection/diffing variant, D-08), `actionUse` (body wrapped in the authority-frame load-bearing literal "Follow these skill instructions for the current task:\n\n" + body, delivered via NewResult preview+sidecar >2KiB, D-08). Keep ≤600 LOC; split further if needed.
    Edit `internal/agent/prompt.go`: add EXACTLY ONE static mechanism-not-enumeration sentence to the SystemPrompt const — e.g. "Skills extend your capabilities; the skill tool lists, inspects, and applies them." (English-only, mechanism not enumeration, same discipline as the tool_search sentence). This is the ONLY edit ever allowed here (D-06); then frozen. Do NOT enumerate skill names.
    Edit `cmd/aura/main.go` buildBaseRegistry: add a `newSkillTool(loader)` adapter (mirroring `newTaskTool(ts)`) that wires the live `internal/skills.Loader` onto the tool's `skillLoader` seam, then `reg.Register(newSkillTool(skillLoader))` (non-deferred). The manifest auto-sorts — never hand-order. Initialize the loader from config (AURA_SKILLS_DIR roots + builtin materialization) at the appropriate composition point; if bootChat is the right home for loader init, do the minimal wiring there but keep the registry registration in buildBaseRegistry per the existing pattern.
    Create `internal/agent/tools/skill_test.go`: schema discipline assertions, reg.Validate() holds, dispatch errors, list/info/use behavior with a fake loader, Description byte-stability. Race-clean.
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./internal/agent/tools/... && go test -race ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/agent/tools/...` exits 0
    - internal/agent/tools/skill.go does NOT import internal/skills (verify: `go list -deps ./internal/agent/tools | grep -c "internal/skills"` returns 0)
    - skill_test.go asserts root schema `required` == ["action"] and NO root "oneOf"/"anyOf" key
    - skill_test.go asserts reg.Validate() returns nil with SkillTool registered
    - internal/agent/prompt.go SystemPrompt const contains "skill" exactly in ONE new mechanism sentence (no skill-name enumeration)
    - cmd/aura/main.go contains "newSkillTool" and "reg.Register" for the skill tool
    - skill_test.go asserts SkillTool.Spec().Description is byte-identical across two calls (turn-stable)
  </acceptance_criteria>
  <done>skill tool registered non-deferred with a turn-stable alphabetical manifest Description; list/info/use work; schema is OpenAI-wire-safe and reg.Validate()-clean; messages[0] gains one frozen sentence; tools package has no internal/skills import; all green + race-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → loader | SKILL.md files on disk are operator-trusted (CC parity, D-28); the loader validates structure only, never the blocklist |
| loader snapshot → LLM manifest | the manifest enters the model-visible tool Description; content originates from operator-trusted disk |
| model → skill tool | model-supplied `action` + args cross into the tool; only read actions wired here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-T1 | Tampering | symlink escape from a skill dir during FS scan | mitigate | Lstat-no-follow strip in loader.go (orphan_scan idiom, Pitfall 4) — Task 1 |
| T-11-02-I1 | Information Disclosure | cache-busting manifest leaks instability / poisons KV prefix | mitigate | manifest byte-stable + alphabetical; messages[0] gets ONE frozen sentence (CAP-04 invariant) — Tasks 1+2; asserted by Description-byte-identity test |
| T-11-02-D1 | Denial of Service | oversized skill body bloats the manifest/context | mitigate | body size cap (AURA_SKILL_BODY_CAP_BYTES) enforced at load; manifest cap + BM25 overflow (D-09) — Task 1 |
| T-11-02-E1 | Elevation of Privilege | blocklist-bearing skill activates by being on disk | accept | D-28: disk is operator-trusted; blocklist enforced ONLY at write boundaries (plan 11-04), never on load — by design |
| T-11-02-R1 | Repudiation | loader silently drops a malformed skill | mitigate | skip + slog.Warn (auditable log line), never silent — Task 1 |

No npm/pip/cargo installs in this plan's RUNTIME path. Go module adds (goccy/go-yaml, golang.org/x/text) are covered by RESEARCH §"Package Legitimacy Audit" (both `[OK]` on slopcheck full-module-path) — no per-install human checkpoint required.
</threat_model>

<verification>
- `go test -race ./internal/skills/... ./internal/agent/tools/... ./internal/agent/` all green, goleak clean
- `go list -deps ./internal/agent/tools | grep internal/skills` returns nothing (cycle-free boundary held)
- manifest + Description byte-stability tests pass (CAP-04 invariant preserved)
- golangci-lint 0 issues on touched packages; all files ≤600 LOC
</verification>

<success_criteria>
- Greenfield internal/skills loader/frontmatter/manifest/builtin shipped + unit-tested
- skill tool registered non-deferred, read actions (list/info/use) working, schema OpenAI-wire-safe
- messages[0] gains exactly ONE frozen mechanism sentence; manifest turn-stable + alphabetical
- goccy/go-yaml + golang.org/x/text direct deps added; coverage contributes toward the ≥85% phase floor
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-02-SUMMARY.md` when done
</output>
