---
phase: 11-skills
plan: 10
type: execute
wave: 10
depends_on: ["11-09"]
files_modified:
  - internal/eval/skills_cot_eval_test.go
  - internal/eval/scenarios_skills.go
  - internal/eval/skills_adapters_cot_eval.go
  - internal/eval/skills_xlsx_verify_cot_eval_test.go
  - internal/eval/judge_cot_eval.go
  - internal/eval/dataset_cot_eval.go
  - internal/eval/classify_cot_eval_test.go
autonomous: true
requirements: [CAP-07, CAP-08]
must_haves:
  truths:
    - "The xlsx North-Star eval captures tool-call ARGUMENTS (sandbox command lines), not just tool names — the old toolNames-only capture made the requiredSeq structurally unsatisfiable (spike 012b)"
    - "The new D-35 HARD FLOOR is: (a) self-install evidence from structured args (`npx skills add` targeting anthropics/skills + --skill xlsx), (b) artifact ground truth (.xlsx in workspace newer than run start, openpyxl read-back, today's date)"
    - "The JUDGE scores capability_gap_recognition + skill_output_quality ONLY — install_prudence is DROPPED (no install-approval ceremony per #51/D-40)"
    - "The eval registry is built WITHOUT catalog/installer seams (they no longer exist after 11-09): the model self-extends via sandbox_exec + the find-skills-aura always-block, exactly like spike 012a"
    - "go build -tags cot_eval ./internal/eval/... compiles against the post-deletion tree (no references to skillCatalog/skillInstaller/CatalogClient/Installer); go vet -tags cot_eval passes"
    - "classifyCall arg-parsing + the seam-free registry construction are covered by pure-function tests that run WITHOUT OPENROUTER_API_KEY (TestClassify* / TestRegistry*), so a structural break is caught even when the paid live tier is gated off"
  artifacts:
    - path: "internal/eval/scenarios_skills.go"
      provides: "the natural xlsx North-Star scenario + the new D-35 ground-truth signals (self-install + artifact)"
      contains: "xlsx"
    - path: "internal/eval/skills_cot_eval_test.go"
      provides: "action-aware capture + new dual gate (self-install evidence + artifact, install_prudence dropped)"
      contains: "npx skills add"
    - path: "internal/eval/classify_cot_eval_test.go"
      provides: "pure-function tests for classifyCall arg parsing + seam-free registry construction (no API key)"
      contains: "TestClassify"
  key_links:
    - from: "internal/eval/skills_cot_eval_test.go"
      to: "the structured tool-call arguments (sandbox command lines)"
      via: "classifyCall over resp.ToolCalls[].Function.Arguments (the spike 012a pattern)"
      pattern: "Function.Arguments|skills add"
    - from: "internal/eval/skills_cot_eval_test.go"
      to: "the live sandbox workspace artifact"
      via: "docker exec / sandbox_exec find -newermt run-start + openpyxl read-back"
      pattern: "newermt|openpyxl"
---

<objective>
Slice 7g eval rewrite (amendment #51 / D-40, new D-35 dual gate). The shipped xlsx North-Star eval was structurally broken (spike 012b): the capture recorded only tool NAMES (never the skill ACTION), making the catalog→ask_user→install→sandbox_exec subsequence unsatisfiable; the install-approval sequence is also gone under the no-ceremony directive. Rewrite the eval to the spike-012a action-aware shape: capture tool-call ARGUMENTS (sandbox command lines), gate on (a) self-install evidence from structured args (`npx skills add anthropics/skills --skill xlsx`) and (b) artifact ground truth (a fresh .xlsx in the workspace, openpyxl read-back, today's date), and judge on capability_gap_recognition + skill_output_quality only (install_prudence dropped). Build the eval registry WITHOUT the catalog/installer seams that 11-09 deleted.

Purpose: make the D-35 gate MEASURABLE and aligned with the shipped architecture — the model self-extends via the sandbox terminal + the find-skills-aura always-block (spike 012a: 4/4 live PASS, full find→add→use→artifact loop in one turn, 212s), not via a Go catalog/install tool.
Output: a compiling, operator-runnable cot_eval xlsx scenario whose hard floor reads STRUCTURED tool args + the live artifact, never the model's prose — with pure-function coverage of the capture + registry that runs in the no-key structural slot.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.claude/skills/spike-findings-Aura/references/skills-self-extension.md
@.planning/spikes/012a-discovery-skill-driven/main.go
@.planning/spikes/012b-discovery-tool-driven/README.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the xlsx North-Star eval — action-aware capture + new D-35 ground-truth signals + seam-free registry + no-key structural tests</name>
  <files>internal/eval/skills_cot_eval_test.go (or the actual harness filename — confirm via glob), internal/eval/scenarios_skills.go, internal/eval/skills_adapters_cot_eval.go, internal/eval/skills_xlsx_verify_cot_eval_test.go, internal/eval/judge_cot_eval.go, internal/eval/dataset_cot_eval.go, internal/eval/classify_cot_eval_test.go (NEW)</files>
  <read_first>
    - prd.md §Slice 7 — the Amendment #51 "D-35 North-Star gate RISCRITTO" paragraph (search "D-35 North-Star gate RISCRITTO"): the EXACT new hard floor (self-install args targeting anthropics/skills + selector xlsx; artifact .xlsx fresh `-newermt` run-start + openpyxl read-back + today's date) and the dropped install_prudence dimension + the "ONLY ask_user pauses the turn" constraint
    - .planning/spikes/012a-discovery-skill-driven/main.go — THE reference harness: classifyCall (action-aware capture from resp.ToolCalls[].Function.Arguments — parses the sandbox command/args, flags `skills add ...anthropics/skills` as selfInstall+installTarget), freshXlsxInSandbox (docker exec find /workspace -name '*.xlsx' -newermt '<start>'), the success criterion (selfInstall && installTarget && artifactFresh), the seam-free buildSkillDrivenRegistry (text_response + tool_search + read_tool_output + current_time + ask_user + web_search + web_fetch + sandbox_exec — NO skill tool, NO catalog/installer), the messages[1] always-block built from the find-skills SKILL.md body
    - internal/eval/skills_cot_eval_test.go — the CURRENT harness (note: in the shipped tree it may be skills_cot_eval.go — confirm the actual filename via glob and edit that file). It currently: builds buildSkillsRegistry with evalSkillLoader/evalSkillWriter/evalSkillInstaller/evalSkillCatalog (the seams 11-09 deleted → the installer/catalog adapters MUST go); captures c.toolNames only; gates on seqOK (catalog→ask_user→install→sandbox_exec), installApprove, xlsx*; drives an install-approval pause loop. Rewrite per the spike-012a shape
    - internal/eval/skills_adapters_cot_eval.go — evalSkillLoader (KEEP — the loader/manifest seam survives), evalSkillWriter (KEEP if any smoke still authors; otherwise it may go unused → check), evalSkillCatalog (DELETE — tools.skillCatalog/CatalogResult are gone), evalSkillInstaller (DELETE — tools.skillInstaller/InstallSummary are gone). After deletion `go build -tags cot_eval ./internal/eval/...` must compile
    - internal/eval/scenarios_skills.go — skillsExpect (catalogSkill/requiredSeq/forbiddenWords/...). Replace requiredSeq's catalog→ask_user→install→sandbox_exec with the self-install + sandbox_exec signals the new capture asserts; keep the natural prompt + forbiddenWords (skill/install/installa/catalog/catalogo). Add the install-selector ("xlsx") + the install-target repo ("anthropics/skills") as the asserted ground truth
    - internal/eval/skills_xlsx_verify_cot_eval_test.go — verifyXlsxArtifact (the openpyxl read-back / today's-date assertion). It already reads the artifact, not the reply — keep it; ensure it uses the sandbox client (sandbox_exec openpyxl) or the docker-exec find pattern from spike 012a, whichever the harness already wires
    - internal/eval/judge_cot_eval.go — skillsRubrics (dimCapabilityGapRecognition, dimInstallPrudence, dimSkillOutputQuality) + runSkillsJudge. DROP dimInstallPrudence from the rubric map + the dims slice + runSkillsScenario's `dims` list; judgeSkillsGate (0.90) now averages 2 dims. Update the rubric prose for capability_gap_recognition to not assume a catalog/install flow (self-extension is now via the sandbox terminal)
    - internal/eval/dataset_cot_eval.go — the dimension consts (dimSkillsHardFloor, dimCapabilityGapRecognition, dimInstallPrudence, dimSkillOutputQuality) + judgeSkillsGate. dimInstallPrudence becomes unused after the drop → remove the const (golangci-lint flags an unused const) OR leave it only if another consumer exists (it does not — remove it)
    - .claude/skills/golang-testing/SKILL.md — the table-driven + pure-function test patterns: the NEW classify_cot_eval_test.go must be a TABLE-DRIVEN pure test over classifyCall (synthetic resp.ToolCalls[].Function.Arguments JSON in, {selfInstall, installTarget, selector} out) + a registry-construction test that builds the seam-free registry and asserts its tool set — both running WITHOUT OPENROUTER_API_KEY (no t.Skip on the key; they touch no network)
    - the slice_scope note in 11-09: the 11-08 plan (planned, NOT executed — no SUMMARY) authored these eval files referencing the deleted seams. This plan is where they get reconciled; if 11-08 also touches scripts/cache_invariant_audit.sh + .github/workflows/skills.yml + docs/aura-quality-snapshot.md, those are OUT of this plan's scope (they do not reference catalog/installer) — leave them for the 11-08 execution
  </read_first>
  <action>
    Rewrite the eval to the spike-012a action-aware shape, against the post-11-09 tree:
    - Capture: replace the toolNames-only capture with a classifyCall over each resp.ToolCalls[].Function.Arguments — for sandbox_exec, parse {command, args}, flatten, and flag `skills add` (selfInstall), `skills add ...anthropics/skills` (installTarget), `--skill xlsx` (the selector); for ask_user record the pause (no longer a gate). Mirror the spike's classifyCall verbatim where it fits. Keep classifyCall a PURE function of its arguments (no I/O) so the no-key test below can table-drive it.
    - Registry: build the eval registry seam-free — text_response + tool_search + read_tool_output + current_time + ask_user + web_search + web_fetch + sandbox_exec, PLUS the find-skills-aura always-block injected at messages[1] (load the builtin body via skills.MaterializeBuiltins + RenderAlwaysBlock, or read the embed SKILL.md). NO tools.SkillTool with Catalog/Installer (those fields are gone); the loader (evalSkillLoader) MAY still register a SkillTool with read actions if the harness wants list/use, but the discovery+install path is the sandbox terminal + the always-block (spike 012a buildSkillDrivenRegistry had no skill tool at all — match that unless a read-only skill tool adds value). Expose the registry builder so the no-key test can construct it without a live run.
    - Hard floor (D-35 rewritten): selfInstall && installTarget (structured-arg evidence) && artifactFresh (a .xlsx newer than run start) && xlsxOpens (openpyxl read-back) && xlsxToday (today's date). Drop seqOK and installApprove. Keep promptNatural (forbidden-word guard) and the secret-leak guard.
    - Judge: score capability_gap_recognition + skill_output_quality only; remove install_prudence from the rubric map, the dims slice, and the report. judgeSkillsGate stays 0.90 over the 2 dims.
    - Adapters: delete evalSkillCatalog + evalSkillInstaller from skills_adapters_cot_eval.go; keep evalSkillLoader (and evalSkillWriter only if still referenced).
    - dataset: remove the dimInstallPrudence const (now unused).
    - Update the report writer + enforceSkills to the new signals (no seqOK/installApprove columns; add selfInstall/installTarget columns).
    - NEW internal/eval/classify_cot_eval_test.go (build tag `cot_eval`): pure, no-key, table-driven tests — `TestClassifyCall_*` feeding synthetic resp.ToolCalls[].Function.Arguments JSON (a clean `npx skills add anthropics/skills --skill xlsx` line → selfInstall+installTarget+selector=xlsx; a bare `skills add other/repo` → selfInstall but no installTarget; a non-install sandbox_exec → none) and asserting the parsed flags, AND `TestRegistry_SeamFree` constructing the seam-free registry and asserting (a) the expected tool set is present and (b) NO skill tool carries a Catalog/Installer field. These tests must NOT call t.Skip on OPENROUTER_API_KEY (they perform no live call), so a structural regression is caught even when the live tier is gated off.
  </action>
  <verify>
    <automated>go build -tags cot_eval ./internal/eval/... && go vet -tags cot_eval ./internal/eval/... && go test -race -tags cot_eval -run 'TestClassify|TestRegistry' ./internal/eval/ && ! grep -rn "evalSkillCatalog\|evalSkillInstaller\|skillCatalog\|skillInstaller\|CatalogResult\|InstallSummary" internal/eval/ && ! grep -rn "dimInstallPrudence\|install_prudence\|installApprove\|seqOK" internal/eval/</automated>
  </verify>
  <verify_notes>
    The `TestClassify|TestRegistry` run is the no-key structural slot — it compiles the post-11-09 tree, exercises classifyCall arg-parsing + the seam-free registry construction, and passes WITHOUT OPENROUTER_API_KEY (the tests touch no network and must not t.Skip on the key). The two `! grep ...` checks (run under bash/WSL) fail the verify if any deleted seam or the dropped install_prudence/seqOK/installApprove signal survives. The full live xlsx run (`go test -race -tags cot_eval -run TestSkillsCoTEval`) stays OPERATOR-run + OPENROUTER-gated, never CI.
  </verify_notes>
  <acceptance_criteria>
    - `go build -tags cot_eval ./internal/eval/...` exits 0 against the post-11-09 tree; `go vet -tags cot_eval ./internal/eval/...` passes
    - `go test -race -tags cot_eval -run 'TestClassify|TestRegistry' ./internal/eval/` is green WITHOUT OPENROUTER_API_KEY set (the structural slot: classifyCall arg parsing + seam-free registry construction, no live call, no t.Skip on the key)
    - NEGATIVE (machine-runnable): `! grep -rn "evalSkillCatalog\|evalSkillInstaller\|skillCatalog\|skillInstaller\|CatalogResult\|InstallSummary" internal/eval/` passes
    - NEGATIVE (machine-runnable): `! grep -rn "dimInstallPrudence\|install_prudence\|installApprove\|seqOK" internal/eval/` passes (install_prudence + the catalog→install subsequence signals are gone)
    - the capture reads structured args: `grep -rn "Function.Arguments\|skills add\|--skill" internal/eval/` matches — self-install evidence comes from tool ARGS, not tool names
    - the hard floor asserts self-install (`npx skills add` targeting anthropics/skills + xlsx selector) AND the fresh-.xlsx + openpyxl + today's-date artifact ground truth (`grep -rn "newermt\|openpyxl\|anthropics/skills" internal/eval/` matches)
    - the judge averages exactly 2 dims (capability_gap_recognition + skill_output_quality) against judgeSkillsGate=0.90
    - the eval registry has NO tools.SkillTool with a Catalog/Installer field (`grep -rn "Catalog:\|Installer:" internal/eval/` returns no matches)
  </acceptance_criteria>
  <done>The xlsx North-Star eval captures structured tool args, gates on self-install evidence + the fresh-artifact ground truth, judges on 2 dims (install_prudence dropped), builds a seam-free registry that compiles + vets against the post-deletion tree, and ships no-key pure-function tests (classifyCall arg parsing + seam-free registry) that catch a structural break without OPENROUTER_API_KEY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live model → self-extension loop | the eval exercises the full find→add→use→artifact loop the model drives itself via the sandbox terminal |
| model prose → pass/fail decision | a tempting but WRONG signal — the hard floor must read structured tool args + the live artifact, never the reply |
| paid LLM tier → CI | the cot_eval live tier must never run in CI (OPENROUTER_API_KEY leak / cost) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-T1 | Tampering | the eval asserts on the model's prose (false confidence) — the exact 012b failure mode | mitigate | the hard floor reads STRUCTURED tool args (self-install) + the LIVE artifact (fresh .xlsx, openpyxl read-back, today's date) — never r.Reply (feedback_probe_must_verify_artifact_not_reply) |
| T-11-10-R1 | Repudiation | a structurally-unsatisfiable assertion (seqOK over tool NAMES) silently always-fails or always-passes, hiding the real result | mitigate | action-aware capture from Function.Arguments (spike 012a) makes the self-install assertion satisfiable + ground-true; the old name-only seqOK is deleted; the no-key TestClassify table proves the capture parses real arg lines |
| T-11-10-I1 | Information Disclosure | the paid live E2E leaks OPENROUTER_API_KEY into CI | accept/mitigate | the cot_eval live tier is OPERATOR-run, OPENROUTER-gated, behind the cot_eval build tag — explicitly NOT CI (the file header + the t.Skip when the key is unset, unchanged on the LIVE test). The NEW TestClassify/TestRegistry tests perform no live call (no key, no t.Skip) so the structural surface is still covered. The secret-leak guard asserts the key value never appears in the final prose |

No npm/pip/cargo install by Aura's Go code in this plan (the npx CLI runs inside the sandbox during the live run; the eval module gains no dependency).
</threat_model>

<verification>
- `go build -tags cot_eval ./internal/eval/...` + `go vet -tags cot_eval ./internal/eval/...` compile against the post-11-09 tree
- `go test -race -tags cot_eval -run 'TestClassify|TestRegistry' ./internal/eval/` green WITHOUT OPENROUTER_API_KEY (the no-key structural slot: classifyCall arg parsing + seam-free registry construction)
- Machine-runnable NEGATIVE assertions hold IN the `<automated>` block: no evalSkillCatalog/evalSkillInstaller/skillCatalog/skillInstaller/CatalogResult/InstallSummary, no dimInstallPrudence/install_prudence/seqOK/installApprove, no Catalog:/Installer: in the eval registry
- the capture reads Function.Arguments (action-aware); the hard floor reads self-install args + the live .xlsx ground truth (newermt/openpyxl/today)
- the judge averages 2 dims against 0.90
</verification>

<success_criteria>
- The xlsx North-Star eval is MEASURABLE: action-aware capture from structured tool args replaces the unsatisfiable name-only subsequence
- The new D-35 hard floor = self-install evidence (`npx skills add anthropics/skills --skill xlsx`) + artifact ground truth (fresh .xlsx, openpyxl read-back, today's date)
- install_prudence is dropped; the judge scores capability_gap_recognition + skill_output_quality only
- The eval registry is seam-free (no catalog/installer) and compiles + vets under -tags cot_eval against the post-deletion tree
- classifyCall arg parsing + the seam-free registry construction are covered by no-key pure-function tests that catch a structural break without OPENROUTER_API_KEY
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-10-SUMMARY.md` when done
</output>
</task>
</content>
</invoke>
</output>
