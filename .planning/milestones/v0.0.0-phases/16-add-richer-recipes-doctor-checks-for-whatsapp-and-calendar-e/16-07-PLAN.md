---
phase: 16-mcp-sidecar-manager-third-party-trust
plan: 07
type: execute
wave: 2
depends_on: ["16-02", "16-04", "16-06"]
files_modified:
  - internal/mcp/manager/policy.go
  - internal/mcp/manager/policy_test.go
  - internal/agent/mcptools/bridge.go
  - internal/agent/mcptools/mount.go
  - internal/agent/mcptools/mount_test.go
  - cmd/aura/mcp.go
autonomous: true
requirements: [CAP-09]
---

<objective>
Add tool risk labels and enforce tool policy before MCP tools enter Aura's runtime registry. This is the safety layer that keeps destructive or unknown-risk tools out of model reach.
</objective>

<context>
@internal/agent/mcptools/bridge.go
@internal/agent/mcptools/mount.go
@.planning/phases/16-add-richer-recipes-doctor-checks-for-whatsapp-and-calendar-e/16-CONTEXT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement risk-label inference and recipe overrides</name>
  <files>internal/mcp/manager/policy.go, internal/mcp/manager/policy_test.go</files>
  <behavior>
    - Tool names/descriptions infer conservative labels.
    - Recipe/catalog metadata can override or add labels.
    - Unknown tools get `unknown` and are visible as risky.
  </behavior>
  <action>
    Create risk labels for read/write/network/filesystem/destructive/private_data/external_send/unknown. Add heuristics for delete/remove/move/send/post/upload/download/read/list/search patterns and recipe override support. Test mail/WhatsApp/calendar examples and unknown-tool fallback.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/mcp/manager/ -run 'TestRisk|TestPolicy' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce policy at mount time</name>
  <files>internal/agent/mcptools/bridge.go, internal/agent/mcptools/mount.go, internal/agent/mcptools/mount_test.go, internal/mcp/manager/policy.go</files>
  <behavior>
    - Denied tools are not registered in Aura's tool registry.
    - Existing allowlists still work.
    - Status/tools output can explain why a tool was blocked.
  </behavior>
  <action>
    Extend the existing allowlist path into a richer policy decision object or keep a manager-level prefilter that returns an allowlist plus block reasons. Update mount tests to prove destructive/unknown-risk tools are absent from the registry and block reasons are retained for status output.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/agent/mcptools/ ./internal/mcp/manager/ -run 'TestMount|TestPolicy|TestRisk' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Show risk labels and block reasons in CLI</name>
  <files>cmd/aura/mcp.go, cmd/aura/mcp_test.go</files>
  <behavior>
    - `aura mcp tools <name>` shows risk labels.
    - `aura mcp status` shows mounted vs blocked tool counts.
    - `--json` includes risk labels and block reasons.
  </behavior>
  <action>
    Add risk/status columns without printing secrets. Keep output compact enough for terminal use. Add tests for text and JSON output.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./cmd/aura/ -run 'TestMCP.*Tools|TestMCP.*Status' -count=1</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- Risk labels are visible and conservative.
- Blocked tools never enter the runtime registry.
- Existing mail/WhatsApp allowlists continue to protect workers.
</success_criteria>

<output>Create `16-07-SUMMARY.md` when done.</output>
