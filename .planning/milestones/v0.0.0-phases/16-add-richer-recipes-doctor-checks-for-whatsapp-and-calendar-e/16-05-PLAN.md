---
phase: 16-mcp-sidecar-manager-third-party-trust
plan: 05
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - internal/mcp/client.go
  - internal/mcp/http_client.go
  - internal/mcp/http_client_test.go
  - internal/mcp/transport.go
  - internal/mcp/transport_test.go
autonomous: true
requirements: [CAP-09]
---

<objective>
Add Streamable HTTP MCP transport while preserving stdio compatibility. This makes Aura's MCP manager viable for modern remote MCP servers and Docker/HTTP gateways.
</objective>

<context>
@internal/mcp/client.go
@.planning/phases/16-add-richer-recipes-doctor-checks-for-whatsapp-and-calendar-e/16-RESEARCH.md
@https://modelcontextprotocol.io/specification/2025-06-18/basic/transports
@https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Introduce a transport interface and keep stdio green</name>
  <files>internal/mcp/transport.go, internal/mcp/client.go, internal/mcp/transport_test.go</files>
  <behavior>
    - Existing stdio callers keep working.
    - Transport interface supports initialize, list tools, call tool, ping, close.
    - Protocol version handling is tested for backwards compatibility.
  </behavior>
  <action>
    Extract a small common client/transport interface without changing public behavior. Keep `Open` as the stdio entry point and add a profile/config-aware `OpenServer` only if useful. Add tests proving current stdio fake servers still pass.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/mcp/ -run 'Test.*Stdio|Test.*Transport|TestOpen' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement minimal Streamable HTTP client</name>
  <files>internal/mcp/http_client.go, internal/mcp/http_client_test.go</files>
  <behavior>
    - Initialize uses HTTP POST and includes negotiated protocol header on later requests.
    - Session id response header is stored and resent.
    - `tools/list`, `tools/call`, ping, timeouts, 401/404/session-reset, and JSON-RPC errors are handled.
    - Bearer/static headers can be sourced from config/env without logging values.
  </behavior>
  <action>
    Implement a stdlib HTTP MCP client for the Streamable HTTP request/response path. SSE receive streams can be skipped unless a response uses `text/event-stream`; if implemented, keep it minimal and test with httptest. Add tests for protocol header, session header, error paths, timeout/cancel, and auth headers.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/mcp/ -run 'TestHTTP|TestStreamable|TestSession|TestProtocol' -count=1</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- Aura supports stdio and Streamable HTTP MCP servers through a common interface.
- HTTP transport follows the 2025-06-18 session/protocol-header requirements.
- Tests use httptest, not live remote services.
</success_criteria>

<output>Create `16-05-SUMMARY.md` when done.</output>
