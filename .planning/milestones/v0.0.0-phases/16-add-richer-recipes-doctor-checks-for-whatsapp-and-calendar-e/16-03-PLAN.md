---
phase: 16-mcp-sidecar-manager-third-party-trust
plan: 03
type: execute
wave: 1
depends_on: ["16-02"]
files_modified:
  - cmd/aura/mcp.go
  - cmd/aura/mcp_test.go
  - internal/mcp/manager/catalog.go
  - internal/mcp/manager/catalog_test.go
autonomous: true
requirements: [CAP-09]
---

<objective>
Upgrade `aura mcp` into the first usable manager surface: recipe catalog, profile commands, explicit trust approval, richer install prompts, and Calendar fixture recipe.
</objective>

<context>
@cmd/aura/mcp.go
@cmd/aura/mcp_test.go
@.planning/phases/16-add-richer-recipes-doctor-checks-for-whatsapp-and-calendar-e/16-CONTEXT.md
@.planning/phases/09-swarm-minimal/09-CONTEXT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move recipe metadata into a catalog helper</name>
  <files>internal/mcp/manager/catalog.go, internal/mcp/manager/catalog_test.go, cmd/aura/mcp.go</files>
  <behavior>
    - `aura mcp recipes` lists calculator, mail, whatsapp, and calendar.
    - Recipes include source, trust class, runtime, required env placeholders, known risk labels, and tool policy.
    - Calendar recipe has a deterministic fixture mode for CI.
  </behavior>
  <action>
    Extract recipe metadata out of the top-level CLI file into a catalog helper. Keep existing install behavior compatible. Add a Calendar recipe that defaults to fixture/local test mode unless live auth env is supplied. Add table output and `--json` output for recipes.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./cmd/aura/ ./internal/mcp/manager/ -run 'TestMCP.*Recipe|TestCatalog' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add profile and trust CLI commands</name>
  <files>cmd/aura/mcp.go, cmd/aura/mcp_test.go</files>
  <behavior>
    - `aura mcp profile list|create|use|add|remove` works on temp config.
    - `aura mcp trust <name>` records trust after showing command/runtime/source.
    - `aura mcp add` for manual local commands creates `blocked` trust by default unless `--trust local` is explicit.
  </behavior>
  <action>
    Extend the CLI parser with profile and trust subcommands. Preserve old commands. Make `mcp add` record source `manual`, trust `blocked`, and active/default profile membership unless explicit flags choose otherwise.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./cmd/aura/ -run 'TestMCP.*Profile|TestMCP.*Trust|TestMCPAdd' -count=1</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- Users can discover recipes, manage profiles, and explicitly trust servers.
- Manual third-party local commands do not become runnable by accident.
- Calendar enters the recipe set with fixture-first validation.
</success_criteria>

<output>Create `16-03-SUMMARY.md` when done.</output>
