---
phase: 16-mcp-sidecar-manager-third-party-trust
plan: 06
type: execute
wave: 2
depends_on: ["16-02", "16-03"]
files_modified:
  - internal/mcp/manager/runtime.go
  - internal/mcp/manager/runtime_test.go
  - cmd/aura/mcp.go
  - cmd/aura/mcp_test.go
autonomous: true
requirements: [CAP-09]
---

<objective>
Implement runtime isolation and trust gates for third-party MCP servers. The key invariant: an arbitrary local MCP command cannot run at chat boot unless it is explicitly trusted or configured for a sandboxed/container runtime.
</objective>

<context>
@.planning/phases/16-add-richer-recipes-doctor-checks-for-whatsapp-and-calendar-e/16-CONTEXT.md
@internal/mcp/managed_config.go
@cmd/aura/main.go
@cmd/aura/mcp.go
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add runtime policy and Docker command generation</name>
  <files>internal/mcp/manager/runtime.go, internal/mcp/manager/runtime_test.go</files>
  <behavior>
    - Dockerized stdio runtime emits `docker run -i --rm` style commands.
    - No host filesystem mount is generated by default.
    - Mounts, env, network, CPU/memory limits are explicit and test-covered.
    - Docker MCP Gateway command generation is optional and feature-detected.
  </behavior>
  <action>
    Create runtime helpers that turn config metadata into launch configs. For `runtime=docker`, generate deterministic safe arguments. For `runtime=docker_gateway`, generate the gateway profile command when configured. Add tests for no default mounts, explicit mounts, resource flags, and Windows/WSL path handling policy.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/mcp/manager/ -run 'TestDockerRuntime|TestGatewayRuntime|TestRuntimePolicy' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce trust gates before chat boot and doctor launch</name>
  <files>cmd/aura/main.go, cmd/aura/mcp.go, cmd/aura/mcp_test.go, internal/mcp/manager/runtime.go</files>
  <behavior>
    - Blocked servers are skipped by boot mount with a warning/status, not launched.
    - `aura mcp doctor <blocked>` reports blocked/trust-needed without launching the command.
    - `aura mcp trust <name>` records approval only after rendering command/runtime/source.
  </behavior>
  <action>
    Wire profile/trust filtering into the server set used by chat boot and doctor. Preserve fail-soft for broken trusted servers. Add tests with a fake command that would fail if launched, proving blocked servers are not executed.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./cmd/aura/ ./internal/mcp/manager/ -run 'TestTrustGate|TestBlocked|TestBuildRegistry' -count=1</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- Untrusted local commands are blocked by default.
- Docker runtime generation is deterministic and least-privilege by default.
- Fail-soft boot still holds.
</success_criteria>

<output>Create `16-06-SUMMARY.md` when done.</output>
