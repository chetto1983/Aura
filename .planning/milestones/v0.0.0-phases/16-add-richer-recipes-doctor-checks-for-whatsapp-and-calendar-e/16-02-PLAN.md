---
phase: 16-mcp-sidecar-manager-third-party-trust
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - internal/mcp/managed_config.go
  - internal/mcp/managed_config_test.go
  - internal/mcp/manager/config.go
  - internal/mcp/manager/config_test.go
autonomous: true
requirements: [CAP-09]
---

<objective>
Add the backwards-compatible managed config v2 model: profiles, trust classes, runtime metadata, server type, tool policy, risk metadata, and export redaction. Existing `~/.aura/mcp/servers.json` files must continue to load.
</objective>

<context>
@.planning/phases/16-add-richer-recipes-doctor-checks-for-whatsapp-and-calendar-e/16-CONTEXT.md
@.planning/phases/16-add-richer-recipes-doctor-checks-for-whatsapp-and-calendar-e/16-PATTERNS.md
@internal/mcp/managed_config.go
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend managed config with profiles and trust metadata</name>
  <files>internal/mcp/managed_config.go, internal/mcp/managed_config_test.go</files>
  <behavior>
    - Legacy config with only `mcpServers` loads unchanged.
    - New config can define profiles and per-server trust/runtime/type metadata.
    - A server with no trust metadata and `source=recipe:*` becomes `trusted_recipe`.
    - A manual third-party local command defaults to `blocked` until trusted.
  </behavior>
  <action>
    Add config structs for `Version`, `Profiles`, `Trust`, `Runtime`, `Type`, `ToolPolicy`, `RiskLabels`, and export options. Keep JSON tags `omitempty` and preserve the existing command/args/env shape. Add helper methods for active profile resolution and trust normalization. Unit-test migration from legacy config, blocked-default behavior, recipe trust default, and profile membership.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/mcp/ -run 'TestManagedConfig|TestProfile|TestTrust' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add config export/import redaction helpers</name>
  <files>internal/mcp/manager/config.go, internal/mcp/manager/config_test.go</files>
  <behavior>
    - Exported profiles exclude secret env values.
    - Env placeholders survive so teammates know what to configure.
    - Import never overwrites existing credentials unless explicitly requested.
  </behavior>
  <action>
    Create a small manager package for profile export/import helpers. Redact values for env keys containing token, secret, pass, key, auth, bearer, or credential. Preserve key names and recipe/source metadata. Add tests for redaction and non-overwrite behavior.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/mcp/ ./internal/mcp/manager/ -run 'Test.*Export|Test.*Import|Test.*Redact' -count=1</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- Config v2 is backwards-compatible.
- Profiles and trust classes are represented in durable config.
- Secret-bearing exports are redacted by default.
</success_criteria>

<output>Create `16-02-SUMMARY.md` when done.</output>
