---
phase: 19-audit-bug-resolution-e2e-live-test
plan: 02
type: execute
wave: 3
depends_on: ["19-01", "19-03"]
files_modified:
  - internal/agent/llm_agent.go
  - internal/agent/llm_agent_finalize.go
  - internal/agent/llm_agent_completion.go
  - internal/agent/llm_agent_reasoning.go
autonomous: true
requirements: [M-a, M-b]
must_haves:
  truths:
    - "A content-stop veto does NOT append the vetoed hand-off prose as durable RoleAssistant; only the user-role feedback nudge is appended"
    - "On the budget trip, finalize() cannot copy a vetoed 'here is a script, you run it' answer forward and re-emit it as the terminal answer"
    - "The completion critic, finalize synthesis, and reasoning router all open their streams through the shared streamWithOpenRetry helper (a transient open blip retries, not fail-open/stub)"
  artifacts:
    - path: "internal/agent/llm_agent.go"
      provides: "content-stop veto appends only the nudge (M-b)"
      contains: "gateCompletion"
    - path: "internal/agent/llm_agent_stream_retry.go"
      provides: "the shared bounded-retry helper the 3 sites now call (existing, unchanged)"
      contains: "streamWithOpenRetry"
  key_links:
    - from: "internal/agent/llm_agent_completion.go"
      to: "streamWithOpenRetry"
      via: "completion critic stream open"
      pattern: "streamWithOpenRetry"
    - from: "internal/agent/llm_agent_finalize.go"
      to: "streamWithOpenRetry"
      via: "finalize synthesis stream open"
      pattern: "streamWithOpenRetry"
    - from: "internal/agent/llm_agent_reasoning.go"
      to: "streamWithOpenRetry"
      via: "reasoning-router stream open"
      pattern: "streamWithOpenRetry"
---

<objective>
Close the two agent-loop never-answer contributors: M-b (the completion gate's veto re-surfaces
the vetoed "I wrote the script, you run it" prose on the budget trip — the gate's whole purpose
is defeated on the exhaustion path) and M-a (the completion critic, finalize, and reasoning router
open their LLM streams DIRECTLY, bypassing `streamWithOpenRetry`, so a transient open blip makes the
critic fail-open / finalize emit the Italian stub). These two are the agent-loop half of the
"shell never answers" bug bundle (the shell half is 19-01).

Purpose: M-b is the "hands off instead of answering" variant — the user gets exactly the vetoed
non-answer. M-a is the transient-failure variant — an open blip the main loop would have retried
defeats the gate. Both are policy fixes on the existing agent loop files.

Output: the content-stop veto path appends ONLY the user-role nudge (not the durable vetoed
RoleAssistant that finalize copies forward), and the 3 bypass stream-open sites route through the
shared bounded-retry helper.

NOTE (read code FRESH): a parallel session landed `internal/agent/llm_agent_retry.go` (TOOL-execution
retry — orthogonal to this plan). `streamWithOpenRetry` in `llm_agent_stream_retry.go` is intact and
unchanged; the M-b veto site at `llm_agent.go:259-261` is unchanged. Verify both before editing.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-CONTEXT.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-RESEARCH.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-PATTERNS.md
@docs/audit/deep-correctness-audit-2026-06-10.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content-stop veto appends only the nudge, not the vetoed prose (M-b)</name>
  <read_first>
    - internal/agent/llm_agent.go (CURRENT state — the content-stop veto at :259-266: today appends
      BOTH `llm.Message{Role: RoleAssistant, Content: answer}` AND the user-role feedback nudge, then
      `continue`; the non-veto path at :264-266; the tool/dispatch path at :278+)
    - internal/agent/llm_agent_finalize.go (CURRENT — finalize copies history forward at :117 and :202,
      re-emitting the durable RoleAssistant the veto appended)
    - 19-RESEARCH.md §"Code Examples → M-b" (append only the nudge), §"Assumptions Log A3" (verify the
      next request stays wire-valid — a user nudge after an assistant content turn is valid)
    - 19-PATTERNS.md §"Existing-Edit findings → M-b" (the text_response/tool path uses RoleTool and is a
      separate, non-leaking seam — confirm M-b is ONLY the content-stop + finalize copy-forward path)
    - docs/audit/deep-correctness-audit-2026-06-10.md §M-b
    - internal/agent/llm_agent_wire_validity_test.go (the existing wire-validity assertion shape to extend)
    - .claude/skills/golang-error-handling/SKILL.md
  </read_first>
  <action>
    In `llm_agent.go` at the content-stop veto branch (`if veto, feedback := a.gateCompletion(ic, answer); veto {`,
    ~:259): REMOVE the `a.history = append(a.history, llm.Message{Role: llm.RoleAssistant, Content: answer})`
    line so the vetoed `answer` is NOT persisted as durable RoleAssistant. Keep appending ONLY the
    `llm.Message{Role: llm.RoleUser, Content: feedback}` nudge, then `continue`. This prevents
    `finalize` (finalize.go:117,202) from copying the vetoed prose forward and re-emitting it on the
    budget trip.
    A3 verification: confirm the next-turn request is wire-valid — after the change the history ends with
    an assistant content turn (the PRIOR turn) followed by the user nudge; there is no orphan tool_call
    (the content-stop path has no tool_call to attach to). Do NOT touch the text_response/tool dispatch
    path (it uses a RoleTool result and is a separate, non-leaking seam — the audit names only the
    content-stop + finalize copy-forward path).
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/agent/ && go vet ./internal/agent/ && go test -run 'TestGateCompletion|TestVeto|TestFinalize|TestWireValid' ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): a new test drives a content-stop veto then a budget
      trip into finalize and asserts (a) the finalize output does NOT contain the vetoed "you run it" prose,
      and (b) `a.history` has NO durable RoleAssistant carrying the vetoed answer (only the user-role nudge).
      BEFORE the fix the vetoed prose is appended as RoleAssistant and re-emitted by finalize; AFTER it is not.
    - A wire-validity assertion (extend llm_agent_wire_validity_test.go) confirms the post-veto history is
      a valid message sequence (no orphan tool_call; assistant-content → user-nudge is valid).
    - `go vet ./internal/agent/` and `go build ./internal/agent/` clean; ≤600 LOC.
  </acceptance_criteria>
  <done>
    The completion gate holds on the budget-exhaustion path: a vetoed hand-off answer is never re-surfaced
    as the terminal answer; only the feedback nudge persists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route the 3 bypass stream-opens through streamWithOpenRetry (M-a)</name>
  <read_first>
    - internal/agent/llm_agent_stream_retry.go (CURRENT streamWithOpenRetry signature at :20 —
      `(ctx, req llm.Request, requestID string) (<-chan llm.Chunk, error)` — and retryableStreamOpenError)
    - internal/agent/llm_agent_completion.go (CURRENT direct `a.client.Stream` at :94 — the completion
      critic open; the drain loop at :99-103)
    - internal/agent/llm_agent_finalize.go (CURRENT direct `a.client.Stream` at :217 — finalize synthesis;
      drain at :223-230)
    - internal/agent/llm_agent_reasoning.go (CURRENT direct `a.client.Stream` at :45 — reasoning router;
      drain at :51-53)
    - 19-RESEARCH.md §"Code Examples → M-a", §"Shared Patterns → Stream-open bounded retry"
    - docs/audit/deep-correctness-audit-2026-06-10.md §M-a
  </read_first>
  <action>
    Replace each of the 3 direct `a.client.Stream(ctx, req)` calls with
    `a.streamWithOpenRetry(callCtx, req, requestID)`:
    - `llm_agent_completion.go:94` (completion critic) — pass a per-call-site requestID.
    - `llm_agent_finalize.go:217` (finalize synthesis).
    - `llm_agent_reasoning.go:45` (reasoning router).
    The helper already classifies retryable open errors (wsarecv/reset/EOF/timeout) and bounds the
    attempts. The critic/finalize/router KEEP their existing fail-open / Italian-stub / ReasoningTierLow
    fallback behavior on the FINAL error (after the bounded retry is spent) — only the transient first-blip
    behavior changes. Do NOT introduce new retry loops; only call the existing shared helper.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/agent/ && go vet ./internal/agent/ && go test -run 'TestCompletion|TestFinalize|TestReasoning|TestStreamRetry' ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): a test with a fake client whose Stream fails
      transiently once then succeeds asserts that completion / finalize / reasoning each make TWO open
      attempts (the retry fired) rather than failing-open / stubbing on the first transient blip. BEFORE the
      fix each makes a single direct attempt and falls back; AFTER, the shared helper retries the transient open.
    - The FINAL-error behavior is unchanged (critic still fails open, finalize still stubs, router still
      falls back to ReasoningTierLow) — assert the fallback path still works when all attempts fail.
    - `go vet ./internal/agent/` clean; no new retry loop introduced (grep: the 3 sites call
      `streamWithOpenRetry`, not `a.client.Stream`).
  </acceptance_criteria>
  <done>
    The completion critic, finalize, and reasoning router all open through the shared bounded-retry helper;
    a transient open blip no longer defeats the gate or forces the Italian stub.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop → user-facing answer | The terminal Event the user reads crosses out of the agent loop; a vetoed hand-off must not become the answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Repudiation / data integrity | completion gate defeated on budget trip (M-b) | mitigate | The veto no longer persists the vetoed answer as durable RoleAssistant, so finalize cannot copy it forward — the gate's intent (reject "you run it" prose) holds on the exhaustion path |
| T-19-05 | Denial of Service (degraded) | transient stream-open blip defeats the gate / forces stub (M-a) | mitigate | The 3 sites route through the bounded streamWithOpenRetry helper; a single transient blip retries instead of failing open |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit: N/A); go.mod unchanged |

Threats NOT introduced: no new auth/session/crypto/file surface (N/A — agent-loop policy fix). Information-disclosure of secrets is handled in the AG-UI/Telegram sanitize plans (19-04/19-05), not here.
</threat_model>

<artifacts_this_phase_produces>
No new files. Modified behavior only:
- `llm_agent.go`: content-stop veto branch no longer appends the vetoed RoleAssistant (M-b).
- `llm_agent_completion.go`, `llm_agent_finalize.go`, `llm_agent_reasoning.go`: the 3 direct
  `a.client.Stream` calls now call the EXISTING `a.streamWithOpenRetry` (M-a).
No new exported symbols, migration objects, sqlc symbols, or CLI flags. `streamWithOpenRetry` and
`llm_agent_retry.go` are pre-existing (the latter from a parallel session) — NOT created here.
</artifacts_this_phase_produces>

<verification>
- `go vet ./internal/agent/` clean; `go build ./internal/agent/` clean.
- `go test -race ./internal/agent/` green (agent-loop concurrency).
- M-b veto-no-resurface test + M-a transient-retry test both pass (and fail on a pre-fix tree).
</verification>

<success_criteria>
- M-b: vetoed hand-off prose never re-emitted on the budget trip; gate holds on exhaustion.
- M-a: completion/finalize/reasoning open through the shared bounded-retry helper.
- Both findings landed in the veto/never-answer cluster commit; ≤600 LOC each.
</success_criteria>

<output>
Create `.planning/phases/19-audit-bug-resolution-e2e-live-test/19-02-SUMMARY.md` when done.
</output>
