---
phase: 19-audit-bug-resolution-e2e-live-test
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/mcptools/bridge.go
  - internal/agent/mcptools/bridge_reconnect.go
  - internal/mcp/client.go
  - internal/knowledge/client.go
  - cmd/aura/serve_channels.go
autonomous: true
requirements: [H10, M-j]
must_haves:
  truths:
    - "An MCP tool call against a dead transport re-opens the server (re-spawn + initialize once) and retries; a SECOND consecutive transport failure returns a clean inline tool error (not a loop-fatal Go error)"
    - "On reconnect, the dead tool's stale boot-time description is refreshed from a re-ListTools; the deferred-tool BM25 registry is not regressed"
    - "The mcp-neo4j-cypher subprocess stderr is captured into a BOUNDED ring buffer (RSS no longer grows without bound for the client's lifetime); stderrTail() still returns the last 200B"
  artifacts:
    - path: "internal/agent/mcptools/bridge_reconnect.go"
      provides: "reconnectingServer wrapper (lazy reconnect-on-use, study §43)"
      contains: "reconnectingServer"
    - path: "internal/mcp/client.go"
      provides: "bounded-ring safeBuffer (M-j) — shared with knowledge/client.go"
      contains: "safeBuffer"
  key_links:
    - from: "internal/agent/mcptools/bridge_reconnect.go"
      to: "mcp.Open + initialize + ListTools"
      via: "re-open on transport error, refresh description, retry once"
      pattern: "mcp.Open|reconnect"
    - from: "internal/knowledge/client.go"
      to: "bounded-ring safeBuffer"
      via: "the duplicated safeBuffer folds into one shared bounded-ring helper"
      pattern: "safeBuffer|ring"
---

<objective>
Close the MCP cluster (D-02 build-real — the lazy reconnect-on-use is the ALREADY-DECIDED design, study
§43, so it was always build, not downgrade): H10 (`bridgedTool` captures ONE `Server` at mount time and
`Execute` calls `CallTool` against a possibly-dead `stdin` forever — no re-spawn+re-init anywhere; the
dead tool also keeps its boot-time description in the registry; the `Ping` primitive exists but is wired
only to `aura mcp doctor`) and M-j (the mcp-neo4j-cypher subprocess stderr is captured into an UNBOUNDED
`safeBuffer` for the client's lifetime — a chatty/error-looping sidecar grows RSS without bound; the same
`safeBuffer` is duplicated in BOTH `internal/mcp/client.go:317-334` AND `internal/knowledge/client.go:272-283`).

Per RESEARCH §Pattern 5: NO supervisor, NO ping ticker (study §43 + memory forbid it — mini-PC no-busy-poll).
The minimal-real shape is reconnect-on-error: re-`mcp.Open` (which re-runs `initialize`) once on a transport
error, retry, then a clean inline tool error on a second failure.

Purpose: a dead MCP sidecar must self-heal on next use instead of failing forever; a chatty sidecar must
not leak RSS.

Output: the `reconnectingServer` wrapper, the composition-root wiring, and the bounded-ring safeBuffer
folded into one shared helper across both copies (refactor-on-touch).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-CONTEXT.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-RESEARCH.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-PATTERNS.md
@docs/audit/deep-correctness-audit-2026-06-10.md
@docs/research/mcp-sidecar-lifecycle-study.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconnectingServer wrapper — lazy reconnect-on-use (H10)</name>
  <read_first>
    - internal/agent/mcptools/bridge.go (CURRENT — the narrow `Server` interface at :25-28 (ListTools+CallTool);
      `bridgedTool` capturing one `srv` at :35-39; `Execute` at :48-60 (the inline `error: `+err.Error()`
      clean-tool-error pattern at :57); `Bridge`/`bridgeTools` at :74-84; the deferred-tool BM25 namespacing
      doc at :62-73 — DO NOT regress the BM25 registry)
    - internal/mcp/client.go (CURRENT re-callable primitives: `Open(ctx, name, cfg)` at :64 — initialize runs
      INSIDE it; `initialize` at :121; `ListTools` at :138; `CallTool` at :160; `Ping` at :185; `ServerConfig`
      at :36 — the re-open inputs)
    - cmd/aura/serve_channels.go (the composition root where MCP servers are mounted — where ServerConfig +
      name are known; where Bridge is called and the wrapper must be installed)
    - docs/research/mcp-sidecar-lifecycle-study.md §43 (the decided "lazy reconnect-on-use" design)
    - 19-RESEARCH.md §"Pattern 5" (reconnect-on-error, refresh description, NO supervisor/ping ticker),
      §"H10" row, §"Anti-Patterns → Don't add a background MCP supervisor"
    - 19-PATTERNS.md §"NET-NEW 3" (the reconnectingServer shape, the Server interface to satisfy, the reuse
      of Open/initialize/Ping, bridge.go is 187 LOC so a new bridge_reconnect.go keeps it under 600)
    - docs/audit/deep-correctness-audit-2026-06-10.md §H10
    - .claude/skills/golang-concurrency/SKILL.md, .claude/skills/golang-structs-interfaces/SKILL.md, .claude/skills/golang-error-handling/SKILL.md
  </read_first>
  <action>
    Create `internal/agent/mcptools/bridge_reconnect.go` (keeps bridge.go under the 600-LOC cap) defining a
    `reconnectingServer` type that satisfies the existing `Server` interface (ListTools + CallTool) and holds
    the underlying `*mcp.Client`, the server `name`, and the `mcp.ServerConfig` needed to re-`Open`. Behavior
    (RESEARCH §Pattern 5 — minimal-real, NO supervisor/ping ticker):
    - On a `CallTool`/`ListTools` TRANSPORT error (a dead-stdin / process-exited error class — classify by
      type/sentinel, NOT by string-matching opaque messages per [[feedback_no_regex_for_nlp]]):
      `mcp.Open(ctx, name, cfg)` to re-spawn (initialize runs once inside Open), then RETRY the call once.
    - A SECOND consecutive failure → return a clean error the bridge renders as inline `error: ...`
      (mirror bridge.go:57), NOT a loop-fatal Go error.
    - On a successful reconnect, REFRESH the dead tool's boot-time description: re-`ListTools` after reconnect
      and update the bridged tool's `spec` so the registry no longer holds the stale description. Do NOT
      regress the deferred-tool BM25 registry (the namespaced name + BM25 indexing of name/description/arg
      fields must still hold — re-index the refreshed spec the same way the boot path does).
    - `Ping` (client.go:185) is OPTIONAL pre-call liveness; reconnect-on-error alone is sufficient (do not add
      a ping ticker / background loop).
    Wire the wrapper at the composition root (cmd/aura/serve_channels.go where servers are mounted): wrap the
    mounted `*mcp.Client` in a `reconnectingServer` before passing it to `Bridge`, threading the name + cfg.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/agent/mcptools/ ./internal/mcp/ ./cmd/aura/ && go vet ./internal/agent/mcptools/ && go test -run 'TestReconnect|TestBridge|TestBridgedTool' ./internal/agent/mcptools/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): a test with a fake `Server` that errors with a
      transport error ONCE then succeeds after re-open asserts `CallTool` re-opens + re-initializes + retries
      and returns the success result; a SECOND consecutive transport failure returns a clean inline tool error
      (not a Go error). BEFORE the fix `bridgedTool` calls the captured dead `srv` forever and never re-opens;
      AFTER, the wrapper reconnects-on-use.
    - A test asserts the bridged tool's description is REFRESHED after reconnect (a re-ListTools updated the
      spec) and the deferred-tool BM25 indexing of the refreshed spec is intact (name/description/arg fields
      still searchable — no regression).
    - No background supervisor / ping ticker added (grep: no new ticker / goroutine loop in mcptools).
    - `go vet ./internal/agent/mcptools/` clean; bridge.go + bridge_reconnect.go each ≤600 LOC.
  </acceptance_criteria>
  <done>
    A dead MCP transport self-heals on next use (re-open + initialize once, retry, clean error on 2nd fail),
    the tool description refreshes on reconnect, and the BM25 registry is not regressed — no supervisor/ping.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bounded-ring safeBuffer in both copies (M-j)</name>
  <read_first>
    - internal/knowledge/client.go (CURRENT `safeBuffer` at :270-283 — the unbounded mutex-guarded
      bytes.Buffer; `Write` at :277; `String` at :283; `stderrTail` at :250 reads only the last 200B)
    - internal/mcp/client.go (CURRENT DUPLICATE `safeBuffer` at :317-334 — identical type; `Write` at :324;
      `String` at :330; `stderrTail` at :305 reads last ~capped suffix)
    - 19-RESEARCH.md §"M-j" row, §"Don't Hand-Roll" (bounded ring buffer for unbounded growth)
    - 19-PATTERNS.md §"M-j rides the same MCP cluster" (fold the duplicated type into one shared bounded-ring
      type, or fix BOTH copies)
    - docs/audit/deep-correctness-audit-2026-06-10.md §M-j
    - .claude/skills/golang-concurrency/SKILL.md (mutex-guarded writer), .claude/skills/golang-security/SKILL.md (unbounded-buffer DoS)
  </read_first>
  <action>
    Make `safeBuffer.Write` a BOUNDED ring: cap the retained bytes at a constant (e.g. a few KB — large
    enough that `stderrTail()`'s last-200B read is always satisfied, small enough to bound RSS). When a Write
    would exceed the cap, trim the oldest bytes so only the most-recent cap bytes are retained. Since
    `stderrTail()` reads only the last 200B (both copies), the trim is transparent to it.
    Per refactor-on-touch (the type is DUPLICATED in BOTH `internal/mcp/client.go:317-334` AND
    `internal/knowledge/client.go:272-283`): extract ONE shared bounded-ring type into a small shared helper
    (a new tiny file or an existing shared internal package) and have BOTH clients use it — OR, if a shared
    package introduces an awkward import edge, apply the identical bounded-ring fix to BOTH copies and note
    why they stay duplicated. Prefer the single shared helper (CLAUDE.md REUSABLE CODE / no-duplicate rule).
    Keep the mutex guard (it is `cmd.Stderr`, written concurrently with the error-context reader).
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/mcp/ ./internal/knowledge/ && go vet ./internal/mcp/ ./internal/knowledge/ && go test -run 'TestSafeBuffer|TestStderrTail|TestRing|TestBounded' ./internal/mcp/ ./internal/knowledge/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): a test writes MORE than the ring cap to the
      safeBuffer and asserts (a) the retained size is bounded at the cap (RSS does not grow with input), and
      (b) `stderrTail()` still returns the correct last 200B. BEFORE the fix the buffer grows unbounded;
      AFTER, it is capped. The test runs for BOTH copies (or the one shared helper).
    - If a shared helper is extracted, both `internal/mcp/client.go` and `internal/knowledge/client.go` use it
      (grep: one bounded-ring type, two consumers); if kept duplicated, both copies are fixed identically.
    - `go vet ./internal/mcp/ ./internal/knowledge/` clean; ≤600 LOC each file.
  </acceptance_criteria>
  <done>
    The MCP/knowledge sidecar stderr buffer is a bounded ring (RSS bounded), `stderrTail()` still works, and
    the duplicated type is folded into one shared helper (or both copies fixed) per refactor-on-touch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP subprocess transport ↔ client | The stdin/stdout pipe to a spawned sidecar can die; the client must recover without failing forever |
| sidecar stderr → client buffer | A chatty/error-looping sidecar writes unbounded stderr the client retains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-21 | Denial of Service | dead MCP transport fails forever (H10) | mitigate | Lazy reconnect-on-use (re-Open + initialize once, retry, clean error on 2nd fail) self-heals without a background supervisor (mini-PC no-busy-poll) |
| T-19-22 | Denial of Service | unbounded stderr buffer grows RSS without bound (M-j, ASVS V12) | mitigate | Bounded ring buffer caps retained stderr; stderrTail's last-200B read still satisfied |
| T-19-23 | Information Disclosure | refreshed tool description / reconnect error leaking detail | accept | The bridge already renders tool failures as inline `error: ...` (existing contract); reconnect reuses it. The sidecar stderr is already redacted at stderrTail (knowledge/client.go:250). No new leak surface. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new external packages (mcp client + go stdlib only). RESEARCH §Package Legitimacy Audit: N/A. The reconnect re-spawns the SAME pinned mcp-neo4j-cypher binary, no new supply-chain surface. |

Threats NOT introduced: no new auth/session/crypto surface (N/A — transport-robustness + buffer-bound fixes).
</threat_model>

<artifacts_this_phase_produces>
New file: `internal/agent/mcptools/bridge_reconnect.go`.
New types/symbols: `reconnectingServer` (satisfies the existing `Server` interface; holds *mcp.Client + name
+ mcp.ServerConfig); possibly a shared bounded-ring buffer type (if extracted from the duplicated safeBuffer).
Modified behavior: composition-root wiring in `cmd/aura/serve_channels.go` (wrap mounted clients);
`safeBuffer.Write` becomes a bounded ring in `internal/mcp/client.go` + `internal/knowledge/client.go` (or
both replaced by the shared helper). Reuses (does NOT create) `mcp.Open`/`initialize`/`ListTools`/`Ping`/`ServerConfig`.
No new migration objects, sqlc symbols, or CLI flags.
</artifacts_this_phase_produces>

<verification>
- `go vet ./internal/agent/mcptools/ ./internal/mcp/ ./internal/knowledge/` clean; `go build ./...` clean.
- `go test -race ./internal/agent/mcptools/ ./internal/mcp/ ./internal/knowledge/` green (concurrency tier
  for the stderr writer + reconnect).
- H10 reconnect test + M-j bounded-ring test both pass (fail on a pre-fix tree).
</verification>

<success_criteria>
- H10: reconnect-on-use (re-open + initialize once, retry, clean error on 2nd fail), description refreshed,
  BM25 registry intact, no supervisor/ping.
- M-j: bounded-ring stderr (RSS bounded), stderrTail intact, duplicated type folded.
- Both findings in the MCP-cluster commit; bridge.go + bridge_reconnect.go ≤600 LOC.
</success_criteria>

<output>
Create `.planning/phases/19-audit-bug-resolution-e2e-live-test/19-09-SUMMARY.md` when done.
</output>
