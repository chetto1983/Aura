---
id: 260907-fh3
status: verified-locally
risk: medium
description: Correct temporal memory paths, historical mentions and evidence selection
---

# Memory correctness and retrieval

Authorized by the operator's “allora sistema” after the four measured gaps in
spike 102. Execute inline under the current agent's delegation restrictions.
Preserve the unrelated scheduler/frontend working changes. Existing GSD config
uses the shared master branch without worktrees.

## Ready / measured prerequisites

- Spike 102 proves native inline shortest-path filtering; post-filtering loses
  valid alternative routes. The separate keyset prototype admits deleted support.
- Historical MENTIONS are removed by the present sweep while FACT remains.
- Native community metric counterexamples forbid adopting conductance/modularity.
- Reuse the ArcadeDB client, transaction/session support, mention scanner, MCP SDK
  and existing agent-memory evaluation harness. No bespoke graph algorithm.
- No Postgres migration or new environment variable is planned. Any schema or API
  decision must first be measured on a disposable database and recorded in PRD.

## Work

1. Measure native direct temporal predicates and read consistency. Amend PRD from
   evidence, implement temporal graph paths with provenance and honest consistency,
   preserving the no-as_of topology contract and all identity/budget/depth guards.
2. Measure and implement retention/reconstruction of historical mention links,
   with support-aware temporal expansion. Current retrieval must not traverse
   expired or unsupported links, and direct evidence must not be displaced by
   expanded evidence under a result cap.
3. Extend the existing memory evaluation with representative questions, expected
   support, abstention and full-evidence token budgets; keep fact/conversation
   quotas independent. Select a graph retrieval policy only if it beats the
   measured baseline; do not introduce PPR/Leiden merely because available.

## Acceptance and gates

- Live cases: expired shortcut/live alternative, inclusive start/exclusive end,
  future/unknown/missing support, directed/reverse/mixed paths, empty results,
  bounded expansion and concurrent updates. Evidence returned must be admissible
  at the requested instant; no unsupported snapshot claim.
- A historical mention remains/reconstructs after supersession and sweep, but
  cannot leak into current expansion. Replays and incomplete sweeps fail safely.
- SDK schema/auth/isolation calls and operator-mounted read acceptance succeed
  after local deployment. Existing memory regressions remain green.
- Go vet/build, affected unit/race tests, live ArcadeDB/MCP suites, package lint,
  viable mutation spot-check and >=85% ArcadeDB integration coverage. Run the
  existing real-agent evaluation and report its actual score and limits.
- Atomic commits, documentation and state summary, master integration, fresh
  verification, push and CI status. Do not declare the task complete while a
  required acceptance gate remains open.

## Targets

`internal/arcadedb/memory_graph*.go`, existing transaction/mention/facts-about
files, `cmd/arcadedb-mcp/tool_memory_graph.go` and live tests/schema golden,
the existing memory evaluation harness/fixtures, PRD and validation docs.
Keep every touched Go file <=600 lines; split by concern when needed.
