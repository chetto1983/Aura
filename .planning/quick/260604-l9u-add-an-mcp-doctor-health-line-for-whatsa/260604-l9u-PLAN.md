---
status: complete
quick_id: 260604-l9u
slug: add-an-mcp-doctor-health-line-for-whatsa
date: 2026-06-04
validate: true
---

# Quick Task 260604-l9u: WhatsApp MCP Doctor Health Line

Add a WhatsApp-specific health line to `aura mcp doctor whatsapp` so operator preflight catches the Phase 9 bridge gap before live eval runs.

## Must Haves

- Preserve existing `aura mcp doctor <name>` behavior for all MCP servers.
- For `doctor whatsapp`, after the MCP stdio server starts and tools list succeeds, print a bridge health line for the local whatsmeow REST bridge at `http://127.0.0.1:8080/api/send`.
- Treat HTTP `405 Method Not Allowed` from `GET /api/send` as REST reachability success, matching the Phase 9 spike/eval preflight.
- Report connection-state explicitly. Because the current bridge exposes no REST status endpoint, report it as unavailable rather than fabricating a connected verdict.
- Keep the probe bounded and side-effect free: no WhatsApp send, no long polling, no background supervisor.
- Cover the new behavior with a focused unit test using `httptest`; no live bridge required.

## Plan

1. Add a failing `cmd/aura` test that runs `runMCPCommand(..., []string{"doctor", "whatsapp"}, ...)` against the helper MCP server and an `httptest` bridge endpoint returning 405.
2. Add a small probe helper in `cmd/aura/mcp.go` with an injectable base URL for tests.
3. Wire `mcpDoctor` to append the WhatsApp bridge health line only when `name == "whatsapp"`.
4. Run focused tests: `go test ./cmd/aura -run 'TestMCPDoctor'`.
5. Run package tests for the touched CLI package: `go test ./cmd/aura`.

## Validation

- Unit tests must pass.
- Doctor output should contain both the existing MCP success line and a new WhatsApp line:
  `whatsapp bridge: REST :8080 reachable (GET /api/send -> 405); connected-state unavailable (bridge exposes no status endpoint)`.
