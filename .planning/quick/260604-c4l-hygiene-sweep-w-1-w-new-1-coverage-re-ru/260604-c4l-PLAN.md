---
quick_id: 260604-c4l
slug: hygiene-sweep-w-1-w-new-1-coverage-re-ru
description: hygiene sweep (W-1 + W-NEW-1 + coverage re-run)
status: planned
created: 2026-06-04
---

# Quick Task 260604-c4l: hygiene sweep

## Goal

Close the small post-audit hygiene findings W-1 and W-NEW-1, then re-run the official project coverage gate and record the aggregate.

## Tasks

1. Remove the orphaned `scripts/sandbox_escape_bench.sh` bench that targets the deleted bespoke sandbox sidecar and nonexistent `.github/workflows/sandbox.yml`.
2. Update current docs so they no longer point operators at the deleted bench, deleted `internal/sandbox` package, deleted `sandbox/` sidecar files, or missing sandbox workflow.
3. Fix the stale comment in `internal/web/main_test.go` so it mirrors an existing `goleak` test harness.
4. Run `make coverage` and record the owned-surface aggregate in the audit/state evidence.

## Verification

- `rg` confirms no active docs/code references to `scripts/sandbox_escape_bench.sh`, `.github/workflows/sandbox.yml`, or `internal/sandbox/docker_test.go`.
- `make coverage` passes the owned-surface floor.
- `git diff --check` passes.
