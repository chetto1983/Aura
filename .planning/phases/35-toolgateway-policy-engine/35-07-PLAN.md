---
phase: 35-toolgateway-policy-engine
plan: 07
type: execute
wave: 6
depends_on: []
gap_closure: true
autonomous: true
requirements: [GATE-01]
files_modified:
  - internal/gateway/approvals.go
  - internal/gateway/approvals_test.go
  - internal/gateway/approve.go
  - internal/gateway/gateway.go
  - internal/gateway/approve_test.go
  - cmd/aura/serve_adapters.go
  - cmd/aura/gateway_resume_hook_test.go
must_haves:
  truths:
    - "CR-01 CLOSED: a gateway approval is moved from pending→approved (and thus later Consumed into Verdict{Allow}) ONLY when routeApprove previously issued a server-side challenge for the AUTHENTICATED (conversation_id, tool, args_sha256) AND the operator-visible pending.Question equals the gateway-generated question. GatewayApprovals.ApproveChallenge enforces BOTH guards — the byte-for-byte analog of internal/agent/tools/shell_approval.go ShellApprovals.ApproveChallenge (existence check + question==challenge.Question), making the 35-06 'byte-for-byte analog' claim finally true (GATE-01 consent-binding)."
    - "WR-02 CLOSED: newGatewayResumeHook keys ApproveChallenge on the AUTHENTICATED pending.ConversationID (server-stored at pause creation, not model-forgeable) — the model-relayed resume_context conversation_id is no longer trusted as the primary key, eliminating cross-conversation approval transfer via the aggregated approval center."
    - "WR-01 CLOSED: routeApprove evaluates the server_production/no-responder hard-deny BEFORE any cross-turn Consume (deny-before-Consume), so a recorded (or CR-01-fabricated) approval can NEVER be consumed by a headless/production Decide; AND Gateway.ApproveChallenge/RecordResolvedApproval refuse to record under ProfileServerProduction (defense in depth). The fail-closed guarantee no longer depends on the single upstream property 'production never issues an approval-required result'."
    - "WR-03 CLOSED: the consent property is machine-checkable — an adversarial ask_user relay carrying the REAL resume_context (matching fp) but a DIFFERENT/benign question records NOTHING (ApproveChallenge returns a question-mismatch error) and the re-drive stays Approve (withheld). The over-claim 'No model self-approval' now has a driving negative test."
    - "ZERO REGRESSION: GATE-01..04 mechanics still hold — Decide's (Verdict, error) funnel, the single-reservation invariant (reserve.go), the 35-05 reconciler's uniform start∧¬end candidate set, and the live reserve/idempotency db_integration tier all stay green; the legitimate persist→resume round-trip (TestGatewayApprovalRoundTrip, cooperative model copying the question verbatim) still executes exactly once."
    - "ON-TOUCH HYGIENE: IN-02 folded (gatewayArgsFingerprint computed exactly once per approval-required result — the single fp threaded from routeApprove into the challenge record + gatewayApprovalRequiredResult + gatewayApprovalContext); IN-01 folded (the dead GatewayApprovals.Peek — which ShellApprovals does not have — is removed). Every touched/created file ≤600 LOC."
  artifacts:
    - path: "internal/gateway/approvals.go"
      provides: "NEW pending-challenge map + gatewayChallenge{question} type + Challenge(convID,tool,fp,question) recorder + ApproveChallenge(convID,tool,fp,question,ResolvedApproval) error (existence + question-match, mirroring ShellApprovals.ApproveChallenge); Evict now sweeps BOTH the approved and pending maps (shell parity); GatewayApprovals.Peek REMOVED (IN-01). Approve/Consume/gatewayArgsFingerprint/gatewayApprovalKey unchanged."
      contains: "ApproveChallenge"
    - path: "internal/gateway/approve.go"
      provides: "routeApprove reordered to deny-before-Consume (WR-01); records a server-side Challenge keyed on the authenticated (key.ConversationID, spec.Name, fp) with the gateway-generated question when it issues the approval-required result; the single fp + question are computed ONCE and threaded into Challenge + gatewayApprovalRequiredResult + gatewayApprovalContext (IN-02)."
      contains: "approvals.Challenge"
    - path: "internal/gateway/gateway.go"
      provides: "NEW Gateway.ApproveChallenge(convID,tool,fp,question,ResolvedApproval) error — the host-side challenge-gated recorder the resume hook calls; refuses under ProfileServerProduction (WR-01 defense-in-depth) then delegates to approvals.ApproveChallenge. Gateway.RecordResolvedApproval retained as a low-level test-seed seam (parity with ShellApprovals.Approve) but gains the same ProfileServerProduction refuse-guard."
      contains: "func (g *Gateway) ApproveChallenge"
    - path: "cmd/aura/serve_adapters.go"
      provides: "newGatewayResumeHook now calls g.ApproveChallenge(pending.ConversationID, rc.Tool, rc.ArgsSHA256, pending.Question, ResolvedApproval{Approved:true, OperatorID:'local'}) — authenticated conversation id (WR-02) + operator-visible question binding (CR-01); the model-relayed rc.ConversationID primary + fallback are dropped."
      contains: "ApproveChallenge(pending.ConversationID"
    - path: "cmd/aura/gateway_resume_hook_test.go"
      provides: "NEW TestGatewayResumeHookRejectsMismatchedQuestion — the WR-03 adversarial negative: real resume_context (matching fp) + benign question → hook errors, records nothing, re-drive stays Approve."
      contains: "MismatchedQuestion"
    - path: "internal/gateway/approve_test.go"
      provides: "NEW WR-01 proofs (TestRouteApproveProductionDeniesEvenWithLedgerApproval — an injected ledger approval is NOT consumed under production, Deny + reserves==0; TestGatewayApproveChallengeRefusedUnderProduction — g.ApproveChallenge refuses under production, nothing recorded) + a challenge-recorded-on-issue assertion mirroring the shell precedent (pending challenge exists, challenge.question == preview.question)."
      contains: "ProductionDeniesEvenWithLedgerApproval"
  key_links:
    - from: "internal/gateway/approve.go routeApprove"
      to: "internal/gateway/approvals.go GatewayApprovals.Challenge"
      via: "when issuing the approval-required result under single_user_hardened + responder + no ledger hit, routeApprove records the gateway-generated question keyed on the authenticated (key.ConversationID, spec.Name, fp)"
      pattern: "approvals\\.Challenge\\("
    - from: "cmd/aura/serve_adapters.go newGatewayResumeHook"
      to: "internal/gateway.Gateway.ApproveChallenge"
      via: "an operator accept records the approval ONLY IF a server-issued pending challenge exists for the digest AND pending.Question matches the gateway-generated question (keyed on the authenticated pending.ConversationID)"
      pattern: "ApproveChallenge\\(pending\\.ConversationID"
    - from: "internal/gateway/gateway.go Gateway.ApproveChallenge"
      to: "internal/gateway/approvals.go GatewayApprovals.ApproveChallenge"
      via: "profile-guard (refuse under ProfileServerProduction) then delegate to the existence + question-match verifier that moves pending→approved"
      pattern: "question mismatch|approvals\\.ApproveChallenge"
    - from: "internal/gateway/approve.go routeApprove deny gate"
      to: "internal/gateway/approvals.go GatewayApprovals.Consume"
      via: "the server_production/no-responder hard-deny is evaluated BEFORE the cross-turn Consume, so no approval is consumable on a headless/production run (WR-01 deny-before-Consume ordering)"
      pattern: "ProfileServerProduction \\|\\| !responderPresent"
  prohibitions:
    - statement: "An approval recorded under ANY profile MUST NOT be consumed by a ProfileServerProduction or headless (no-responder) Decide — it still Denies with Execute==0 / reserves==0."
      status: must-hold
      verification: "TestRouteApproveProductionDeniesEvenWithLedgerApproval (inject via g.approvals.Approve, Decide → Deny, store.reserves() len 0) + TestGatewayApproveChallengeRefusedUnderProduction (g.ApproveChallenge under production returns error, Consume finds nothing)."
    - statement: "A mismatched/benign relayed question MUST NOT produce a recorded approval — the operator can never be shown a question that differs from the action being authorized."
      status: must-hold
      verification: "TestGatewayResumeHookRejectsMismatchedQuestion (cmd/aura) + TestGatewayApprovalsApproveChallengeQuestionMismatch (approvals_test) — both assert no approval is recorded and the re-drive stays Approve/withheld."
    - statement: "No pre-dispatch intercept — internal/agent/llm_agent_pause.go's ask_user name-gate is not widened; the approve still flows through NORMAL dispatch as a tools.ToolResult."
      status: preserved
      verification: "git diff shows internal/agent/llm_agent_pause.go and internal/agent/llm_agent_retry.go untouched; TestAskUserOnlyPauseConstraint stays green."
    - statement: "No model self-approval — GatewayApprovals is written by the host-side hook ONLY; relaying via ask_user is not approving; no model-supplied arg/ctx/tool schema can move pending→approved."
      status: strengthened
      verification: "TestApproveIsHostSideOnly + the hook is the sole production caller of Gateway.ApproveChallenge (behind the Phase-25 approval center's RequireAuth+RequireCapability)."
    - statement: "No new migration, HTTP route, or env var — internal/agui/approvals_api.go, internal/runner/runner_resume.go, internal/gateway/decide.go, internal/gateway/reserve.go stay UNTOUCHED."
      status: preserved
      verification: "git diff scope equals exactly the 7 files in files_modified; no *.sql / route registration / AURA_* knob added (grep)."
    - statement: "The ledger key MUST NOT include tool_call_id — it binds conversation_id + tool + canonical-args fingerprint only (a re-emit changes the id)."
      status: preserved
      verification: "gatewayApprovalKey(convID, toolName, argsFingerprint) unchanged; the new challenge map reuses the SAME key (grep gatewayApprovalKey)."
---

<objective>
Close the BLOCKING code-review gap that holds Phase 35 open: **CR-01 — the gateway interactive-approval path has no informed-consent binding** (a confused-deputy bypass), plus the three compounding warnings and two on-touch info items. 35-06 shipped the interactive `approve` pause/resume UX by mirroring `internal/agent/tools/shell_approval.go`, but it DROPPED that precedent's server-side challenge/question binding: `GatewayApprovals` has no pending-challenge map and `newGatewayResumeHook` records the operator's accept straight from the **model-relayed** `resume_context`, never checking that the operator-visible `pending.Question` (the only field `approvals_api.go` surfaces) matches the gateway-generated question. An adversarial/prompt-injected model can relay the REAL `resume_context` (so the `args_sha256` fingerprint matches on re-emit) with a FALSE, benign question — the operator approves *"Save your meeting notes?"* and unknowingly authorizes `swarm_spawn{goals:["destroy prod"]}`. This defeats GATE-01's entire interactive-approval purpose under `single_user_hardened`.

**The fix is to make the "byte-for-byte analog of shell_exec" claim TRUE**, not to invent anything new. Port `ShellApprovals`'s `CreateChallenge`/`ApproveChallenge(question==challenge.Question)` existence+question binding into `GatewayApprovals`; have `routeApprove` record the gateway-generated question keyed on the AUTHENTICATED `(conversation_id, tool, args_sha256)` at the moment it issues the approval-required result; and make the resume hook verify **existence + question-match** before recording — keyed on the authenticated `pending.ConversationID` (WR-02). Reorder `routeApprove` so the production/headless hard-deny precedes any cross-turn `Consume` and refuse to record under `server_production` (WR-01, defense in depth). Add the adversarial mismatched-question negative test (WR-03). Fold the single-fingerprint DRY nit (IN-02) and remove the dead `Peek` (IN-01) on touch.

Primary gap sources (read before implementing): `35-VERIFICATION.md` `gaps_remaining` (CR-01/WR-01/WR-02/WR-03 with fixes) and `35-REVIEW.md` (CR-01 carries an end-to-end failure scenario AND a concrete proposed fix with code; each warning carries File + Issue + Fix).

Purpose: deliver GATE-01's informed-consent property so the phase can close and GATE-01/GATE-02 can flip to `[x]`, without disturbing the passing 6/6 mechanics (Decide funnel, single reservation, reconciler candidate set) or violating any 35-06 prohibition (no pre-dispatch intercept, no competing executed Insert, no model self-approval, no new HTTP route/migration/env knob, ledger key excludes tool_call_id).
Output: the `GatewayApprovals` challenge map + `Challenge`/`ApproveChallenge`, the `routeApprove` deny-before-Consume reorder + challenge record + single-fp, `Gateway.ApproveChallenge` with the production refuse-guard, the host-side hook rewire to the authenticated conversation + operator-visible question, the WR-03 adversarial test, the WR-01 fail-closed tests, and a full-matrix (unit + live db_integration) zero-regression proof.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-toolgateway-policy-engine/35-CONTEXT.md
@.planning/phases/35-toolgateway-policy-engine/35-VERIFICATION.md
@.planning/phases/35-toolgateway-policy-engine/35-REVIEW.md
@.planning/phases/35-toolgateway-policy-engine/35-06-PLAN.md

# THE PRECEDENT TO PORT (read before writing — do not invent new shapes)
@internal/agent/tools/shell_approval.go
@internal/agent/tools/shell_exec_approval_test.go

# The files being changed
@internal/gateway/approvals.go
@internal/gateway/approve.go
@internal/gateway/gateway.go
@internal/gateway/approve_test.go
@cmd/aura/serve_adapters.go
@cmd/aura/gateway_resume_hook_test.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port shell_exec's challenge/question binding into GatewayApprovals (CR-01 ledger half + IN-01)</name>
  <files>internal/gateway/approvals.go, internal/gateway/approvals_test.go</files>
  <read_first>
    - internal/agent/tools/shell_approval.go — the EXACT precedent. Study: (a) `ShellApprovals` holds BOTH an `approved map[string]struct{}` AND a `pending map[string]ShellApprovalChallenge`; (b) `CreateChallenge` records the pending challenge (question included) keyed on `shellApprovalKey(sessionID, digest)`; (c) `ApproveChallenge(sessionID, digest, question)` (lines 105-125) rejects unless the pending challenge EXISTS for that key AND `question == challenge.Question`, then moves it to approved and deletes the pending entry, returning a typed `fmt.Errorf("shell approval challenge %q not found" / "... question mismatch", digest)`; (d) `Evict` (lines 184-201) sweeps BOTH the approved AND pending maps under the session prefix.
    - internal/agent/tools/shell_exec_approval_test.go — lines 33-39 show the test shape to mirror: after the approval-required result, `PendingChallenge(...)` returns ok=true and `challenge.Question == payload.Question`. Note `ShellApprovals.Approve` (a direct-grant) is itself used ONLY by tests (lines 72/94/122/131/148) yet ships — so keeping the analogous `GatewayApprovals.Approve` for parity is correct; only `Peek` (which ShellApprovals does NOT have) is the dead code to remove.
    - internal/gateway/approvals.go — the current `GatewayApprovals`: `approved map[string]ResolvedApproval` only (NO pending map), `Approve`/`Consume`/`Peek`/`Evict`, `gatewayArgsFingerprint`, `gatewayApprovalKey`. This file gains the pending map + Challenge/ApproveChallenge, extends Evict, and loses Peek.
    - internal/gateway/approvals_test.go — the current tests (LedgerOneShot, PeekNonDestructive, EvictPrefixSweep, NilSafe, EmptyArgsRejected, FingerprintCanonicalEquality). The Peek test is removed; challenge tests are added.
  </read_first>
  <behavior>
    - gatewayChallenge value type carries the server-generated question (mirrors ShellApprovalChallenge, minus the shell-specific Command/Cwd/Digest the gateway does not have).
    - NewGatewayApprovals initializes BOTH the approved and the new pending map.
    - Challenge(convID, tool, fp, question) records pending[gatewayApprovalKey(convID, tool, fp)] = gatewayChallenge{question}; empty coordinate is a no-op; nil-receiver-safe; guarded by the existing sync.Mutex.
    - ApproveChallenge(convID, tool, fp, question, r) error: returns a typed error when convID/tool/fp is empty OR no pending challenge exists for the key ("gateway approval challenge %q not found") OR question != challenge.question ("gateway approval challenge %q question mismatch"); on success writes approved[key] = r and deletes pending[key]; nil-receiver-safe. Byte-for-byte analog of ShellApprovals.ApproveChallenge, storing a ResolvedApproval instead of a bare marker.
    - Consume is UNCHANGED (one-shot read+delete of approved) — the re-entry mechanics stay intact.
    - Evict(convID) now sweeps BOTH the approved AND the pending maps under the conversation prefix (shell parity), so a resolved-but-unconsumed approval AND an issued-but-unresolved challenge are both reclaimed.
    - Peek is REMOVED (IN-01: no production caller; ShellApprovals has none).
  </behavior>
  <action>
    In internal/gateway/approvals.go: add a `pending map[string]gatewayChallenge` field to `GatewayApprovals` and a `gatewayChallenge struct { question string }` type (analog of ShellApprovalChallenge). Initialize `pending` in `NewGatewayApprovals`. Add `Challenge(convID, toolName, argsFingerprint, question string)` (records the pending challenge, guards + nil-safe, mirrors CreateChallenge but the gateway pre-computes fp so it takes fp+question rather than raw command/cwd). Add `ApproveChallenge(convID, toolName, argsFingerprint, question string, r ResolvedApproval) error` mirroring ShellApprovals.ApproveChallenge (lines 105-125): existence check + question-mismatch check, both returning typed errors keyed on the fingerprint, then `approved[key] = r` + `delete(pending, key)`. Extend `Evict` to also range+delete the `pending` map under the `convID + "\x00"` prefix (mirror shell_approval.go lines 196-200). REMOVE `Peek` (IN-01). Keep `Approve`, `Consume`, `gatewayArgsFingerprint`, `gatewayApprovalKey` unchanged. Update the package/type doc comments so the "byte-for-byte analog of ShellApprovals" claim is now accurate (it records a challenge and verifies the question, exactly as shell does). Do NOT change the `gatewayApprovalKey` shape (tool_call_id stays excluded). Keep the file ≤600 LOC.

    Cite: CR-01 (the challenge/question binding the code claimed to mirror but dropped) + IN-01 (dead Peek). Mirrors internal/agent/tools/shell_approval.go exactly.

    Tests (internal/gateway/approvals_test.go): REMOVE the Peek test. ADD: (1) Challenge→ApproveChallenge(matching question)→Consume returns the ResolvedApproval once, second Consume ok=false (one-shot); (2) TestGatewayApprovalsApproveChallengeQuestionMismatch — Challenge with question "Q1", ApproveChallenge with a DIFFERENT question → error, and Consume finds NOTHING (nothing moved to approved); (3) ApproveChallenge with NO prior Challenge → "not found" error; (4) Evict now drops BOTH a recorded approval AND an un-resolved pending challenge under the prefix (assert a subsequent ApproveChallenge for the evicted challenge → "not found"); (5) keep the fingerprint canonical-equality + nil-safety + empty-coordinate tests. Run -race.
  </action>
  <verify>
    <automated>cd /d/Repo/Aura && go vet ./internal/gateway/ && go test -race ./internal/gateway/ -run 'Approvals|Challenge|Fingerprint|Ledger|Evict' -count=1 && wc -l internal/gateway/approvals.go internal/gateway/approvals_test.go</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: internal/gateway/approvals.go defines a `pending` map field, a `gatewayChallenge` type, `Challenge`, and `ApproveChallenge`; `Evict` ranges BOTH maps; `Peek` is GONE (grep: `pending map`, `func.*Challenge`, `func.*ApproveChallenge`; grep `Peek` returns 0 hits in the file).
    - SOURCE: ApproveChallenge returns a typed error on missing-challenge AND on `question != challenge.question`, then writes approved[key] + delete(pending, key) (grep: `question mismatch`, `not found`).
    - BEHAVIOR: Challenge→ApproveChallenge(match)→Consume yields the approval once; a question mismatch or a no-prior-challenge ApproveChallenge records nothing (Consume ok=false); Evict clears both maps.
    - REGRESSION: gatewayApprovalKey unchanged (tool_call_id excluded); Approve/Consume/gatewayArgsFingerprint behavior unchanged.
    - TEST: `go test -race ./internal/gateway/ -run 'Approvals|Challenge|Evict' -count=1` green with real (non-0-subtest) runs; both files ≤600 LOC.
  </acceptance_criteria>
  <done>GatewayApprovals now carries the server-side challenge the shell precedent has: an approval can only be recorded via ApproveChallenge when a matching pending challenge exists AND the question matches, and Evict reclaims both maps — the in-memory half of the CR-01 fix, unit-proven, dead Peek removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: routeApprove records the challenge on issue + deny-before-Consume reorder + single-fp; Gateway.ApproveChallenge with production refuse-guard (CR-01 PEP half + WR-01 + IN-02)</name>
  <files>internal/gateway/approve.go, internal/gateway/gateway.go, internal/gateway/approve_test.go</files>
  <read_first>
    - internal/gateway/approve.go — routeApprove (lines 85-117): the `resolvedApproval(ctx)` same-ctx fast path (line 86, MUST stay at the very top — it is the same-turn resolved-approval seam: TestApprovePostResumeAllow drives it via `WithResolvedApproval(WithResponder(ctx), ...)`, so a same-context resolved approval short-circuits to Allow BEFORE any profile/responder re-evaluation; moving it below the deny gate would break that seam), the `fp := gatewayArgsFingerprint(rawArgs)` + `g.approvals.Consume(...)` cross-turn re-entry (lines 99-102), the `g.profile == config.ProfileServerProduction || !responderPresent(ctx)` deny gate (line 105), and the single_user_hardened issue branch (lines 109-116). Also gatewayApprovalRequiredResult (line 126, computes fp at :127) and gatewayApprovalContext (line 179, recomputes the identical fp at :187 — the IN-02 duplicate) and gatewayApprovalQuestion (line 154).
    - internal/gateway/gateway.go — Gateway{profile, store, approvals}, RecordResolvedApproval (lines 124-129, delegates to approvals.Approve) — this stays as a test-seed seam but gains a production refuse-guard; add the new Gateway.ApproveChallenge beside it. config.RuntimeProfile + config.ProfileServerProduction are already imported/used.
    - internal/gateway/approve_test.go — the existing routeApprove tests (TestApproveHardenedInteractive expects Approve+ApprovalRequest under hardened+responder and store.calls()==0/reserves()==0; TestApproveHardenedLedgerReEntry seeds via RecordResolvedApproval then expects Allow; TestApproveProductionDenies/HeadlessDenies expect Deny; TestApprovePostResumeAllow uses the same-ctx fast path with NO responder and expects Allow; TestApproveIsHostSideOnly). Trace each against the reorder to confirm no regression.
    - internal/gateway/decide_test.go — the shared test helpers: fakeStore (with .calls()/.reserves()), mutatingRiskySpec() (shell_exec, GateRecommended), testKey(), mustDecideArgs(t, v). Reuse them in the new WR-01 tests.
    - internal/agent/tools/shell_exec_approval_test.go lines 33-39 — the "approval-required result created a pending challenge whose question matches the payload" assertion shape to mirror for the gateway.
  </read_first>
  <behavior>
    - routeApprove ORDER becomes: (1) same-ctx resolvedApproval(ctx) fast path → Allow (UNCHANGED position, top); (2) WR-01 hard-deny: if g.profile == config.ProfileServerProduction || !responderPresent(ctx) → recordDegradedDeny + Deny — now evaluated BEFORE the Consume, so no cross-turn approval is consumable on a headless/production run; (3) fp := gatewayArgsFingerprint(rawArgs) then g.approvals.Consume(...) cross-turn re-entry → Allow (reachable only under hardened+responder now); (4) no hit → compute question := gatewayApprovalQuestion(spec, tier, rawArgs) ONCE, record g.approvals.Challenge(key.ConversationID, spec.Name, fp, question), build result := gatewayApprovalRequiredResult(spec, tier, key, fp, question), return Verdict{Approve, ApprovalRequest:&result}.
    - The single fp and question computed in routeApprove are threaded into Challenge, gatewayApprovalRequiredResult, and gatewayApprovalContext, so gatewayArgsFingerprint is called EXACTLY ONCE per approval-required result (IN-02). gatewayApprovalRequiredResult no longer recomputes fp; gatewayApprovalContext takes fp as a parameter instead of recomputing.
    - Gateway.ApproveChallenge(convID, tool, fp, question, r) error: refuses (returns an error, records nothing) when g.profile == config.ProfileServerProduction (WR-01 defense-in-depth), else delegates to g.approvals.ApproveChallenge(...); nil-Gateway safe.
    - Gateway.RecordResolvedApproval gains the same g.profile == config.ProfileServerProduction refuse-guard (a production run records no approval by any path).
    - NO change to decide.go's funnel, reserve.go, or the (Verdict, error) signatures. The not-GateRecommended auto-allow and the post-resume Allow still converge on the single g.reserve.
  </behavior>
  <action>
    In internal/gateway/approve.go: reorder routeApprove so the `g.profile == config.ProfileServerProduction || !responderPresent(ctx)` deny block (with its recordDegradedDeny call) is placed AFTER the `resolvedApproval(ctx)` fast path and BEFORE the fp/Consume block (WR-01 deny-before-Consume). Keep the fast path at the very top unchanged. After the Consume miss, compute `question := gatewayApprovalQuestion(spec, tier, rawArgs)` once and call `g.approvals.Challenge(key.ConversationID, spec.Name, fp, question)` to record the server-side challenge, THEN build the approval-required result. Change `gatewayApprovalRequiredResult` to accept the already-computed fp and question (drop its internal `fp := gatewayArgsFingerprint(...)` recompute) and change `gatewayApprovalContext` to accept fp as a parameter (drop its `gatewayArgsFingerprint(rawArgs)` recompute at :187) — single computation site (IN-02). Update the routeApprove doc comment to describe the new order (deny-before-Consume) and that it records a challenge the resume hook must match. Keep approve.go ≤600 LOC.

    In internal/gateway/gateway.go: add `func (g *Gateway) ApproveChallenge(convID, toolName, argsFingerprint, question string, r ResolvedApproval) error` — nil-Gateway returns nil; `if g.profile == config.ProfileServerProduction { return fmt.Errorf("gateway approval refused under server_production") }` (WR-01 defense-in-depth); else `return g.approvals.ApproveChallenge(convID, toolName, argsFingerprint, question, r)`. Add the SAME ProfileServerProduction refuse-guard to the existing RecordResolvedApproval (document it as a low-level test-seed seam whose sole faithful production analog is now ApproveChallenge). Keep gateway.go ≤600 LOC.

    Cite: CR-01 (record the server challenge at issue-time keyed on the authenticated triple; the hook verifies it) + WR-01 (deny-before-Consume + refuse-to-record under production) + IN-02 (single fp site). Realises D-03 point 2's consent-binding.

    Tests (internal/gateway/approve_test.go): (1) EXTEND TestApproveHardenedInteractive (or add a sibling) to assert — mirroring shell_exec_approval_test.go:33-39 — that after the Approve verdict, a pending challenge exists for the key and its question equals the preview's question (prove the challenge is recorded on issue; the in-package test can inspect g.approvals via the unexported field). (2) ADD TestRouteApproveProductionDeniesEvenWithLedgerApproval: g := New(config.ProfileServerProduction, &fakeStore{}); adversarially inject an approval via the low-level g.approvals.Approve(testKey().ConversationID, mutatingRiskySpec().Name, gatewayArgsFingerprint(args), ResolvedApproval{Approved:true, OperatorID:"x"}); g.Decide(WithResponder(ctx), mutatingRiskySpec(), args, testKey()) → assert Deny AND len(store.reserves())==0 (the injected approval is NOT consumed — deny-before-Consume). (3) ADD TestGatewayApproveChallengeRefusedUnderProduction: production gateway, g.approvals.Challenge(conv, tool, fp, "q"), then g.ApproveChallenge(conv, tool, fp, "q", ResolvedApproval{Approved:true}) → assert a non-nil error AND Consume finds nothing. (4) Confirm the existing TestApproveHardenedLedgerReEntry / TestApprovePostResumeAllow / TestApproveProductionDenies / TestApproveHeadlessDenies / TestApproveIsHostSideOnly still pass unchanged under the reorder. Run -race.
  </action>
  <verify>
    <automated>cd /d/Repo/Aura && go vet ./internal/gateway/ && go test -race ./internal/gateway/ -run 'Approve|Route|Decide|Challenge|Reserve' -count=1 && wc -l internal/gateway/approve.go internal/gateway/gateway.go internal/gateway/approve_test.go</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: routeApprove evaluates the `ProfileServerProduction || !responderPresent` deny block BEFORE `g.approvals.Consume(` (the deny line precedes the Consume line), and records `g.approvals.Challenge(key.ConversationID, spec.Name, fp, question)` on the issue branch (grep: `approvals.Challenge(`).
    - SOURCE: gatewayArgsFingerprint is called exactly ONCE inside the approval-required result path — gatewayApprovalRequiredResult and gatewayApprovalContext take fp as a parameter (grep: no `gatewayArgsFingerprint(` inside gatewayApprovalContext; single call in routeApprove).
    - SOURCE: `func (g *Gateway) ApproveChallenge` exists and refuses under ProfileServerProduction; RecordResolvedApproval also refuses under production (grep: `ProfileServerProduction` appears in both).
    - BEHAVIOR: production/headless Decide Denies even with a ledger approval present (reserves()==0); g.ApproveChallenge under production returns an error and records nothing; the approval-required Approve verdict records a matching pending challenge.
    - REGRESSION: TestApproveHardenedLedgerReEntry, TestApprovePostResumeAllow, TestApproveProductionDenies, TestApproveHeadlessDenies, TestApproveIsHostSideOnly all still pass; Decide/reserve signatures unchanged.
    - TEST: `go test -race ./internal/gateway/ -run 'Approve|Route|Challenge' -count=1` green; approve.go/gateway.go/approve_test.go each ≤600 LOC.
  </acceptance_criteria>
  <done>The gateway records a server-side challenge (the gateway-generated question, keyed on the authenticated conversation+tool+fp) whenever it issues an approval-required result, evaluates the production/headless hard-deny before any cross-turn Consume, refuses to record approvals under production, and computes the fingerprint once — the PEP half of CR-01 plus WR-01 and IN-02, unit-proven with zero regression to the passing mechanics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire newGatewayResumeHook to the authenticated conversation + operator-visible question (CR-01 host binding + WR-02); adversarial WR-03 test; full-matrix zero-regression validation</name>
  <files>cmd/aura/serve_adapters.go, cmd/aura/gateway_resume_hook_test.go</files>
  <read_first>
    - cmd/aura/serve_adapters.go — newGatewayResumeHook (lines 376-410): today it decodes the model-relayed {type,tool,conversation_id,args_sha256}, does `convID := rc.ConversationID; if convID == "" { convID = pending.ConversationID }` (WR-02), and calls `g.RecordResolvedApproval(convID, rc.Tool, rc.ArgsSHA256, {Approved:true, OperatorID:"local"})` with NO question check (CR-01). Compare with newShellResumeHook (lines 342-365), which calls `approvals.ApproveChallenge(pending.ConversationID, rc.CommandSHA256, pending.Question)` — the authenticated conversation + the operator-visible question. Mirror the shell hook.
    - cmd/aura/gateway_resume_hook_test.go — the existing hook tests + helpers gatewayApprovalPending (drives one real gw.Decide to seed a challenge and projects the REAL preview.Question into pending.Question) and reDecideAllows. TestGatewayResumeHookRecordsAcceptedApproval / IgnoresNonAccept / RejectsMalformedContext / NilGateway / InChain must all still pass after the rewire (they carry the verbatim gateway question, so ApproveChallenge matches).
    - cmd/aura/gateway_approval_roundtrip_test.go — the cooperative-model round-trip (the client copies question + resume_context VERBATIM). Confirm (do NOT edit) it still passes: pending.Question equals the gateway question, so ApproveChallenge succeeds and the spy executes exactly once.
    - internal/askuser/store.go lines 228-230 — askuser.Pending carries ConversationID + Question (server-stored at pause creation from the model's ask_user), the authenticated fields the hook now binds on.
  </read_first>
  <behavior>
    - newGatewayResumeHook, on a gateway_approval + accept pending, calls g.ApproveChallenge(pending.ConversationID, rc.Tool, rc.ArgsSHA256, pending.Question, gateway.ResolvedApproval{Approved:true, OperatorID:"local"}) and RETURNS its error (propagates a question-mismatch/not-found error exactly as newShellResumeHook propagates ApproveChallenge's error). The model-relayed rc.ConversationID is dropped as the key source (WR-02) — the authenticated pending.ConversationID is used unconditionally; rc.ConversationID MAY be retained only as a decoded field for a defensive equality assertion, never as the primary key.
    - The pending.Kind == KindApproval && resp.Action == ActionAccept && len(pending.ResumeContext) > 0 gate, the type != "gateway_approval" early-return, and the rc.Tool == "" || rc.ArgsSHA256 == "" malformed guard are UNCHANGED (they fire before ApproveChallenge). Decline/cancel/wrong-type/wrong-kind still record nothing.
    - Legitimate cooperative relay (question copied verbatim) → ApproveChallenge matches → approval recorded → re-drive Allows (round-trip unchanged). Adversarial relay (real resume_context, benign question) → question mismatch → error, nothing recorded → re-drive stays Approve (withheld).
  </behavior>
  <action>
    In cmd/aura/serve_adapters.go newGatewayResumeHook: replace the `convID := rc.ConversationID; if convID == "" { convID = pending.ConversationID }` block and the `g.RecordResolvedApproval(convID, rc.Tool, rc.ArgsSHA256, ...)` call with `return g.ApproveChallenge(pending.ConversationID, rc.Tool, rc.ArgsSHA256, pending.Question, gateway.ResolvedApproval{Approved: true, OperatorID: "local"})` — the authenticated conversation id (WR-02) + the operator-visible question binding (CR-01), mirroring newShellResumeHook's ApproveChallenge(pending.ConversationID, rc.CommandSHA256, pending.Question). Update the hook's doc comment to state it verifies the gateway-issued challenge exists AND the operator-visible question matches (no longer a blind record). Keep the rc.Tool == "" || rc.ArgsSHA256 == "" guard. Do NOT touch internal/agui/approvals_api.go, internal/runner/runner_resume.go, internal/gateway/decide.go, internal/gateway/reserve.go, internal/agent/llm_agent_pause.go, or internal/agent/llm_agent_retry.go. Keep serve_adapters.go ≤600 LOC (the change is net-neutral/negative — a smaller hook body).

    Cite: CR-01 host-binding + WR-02 (authenticated pending.ConversationID). The SOLE production writer of GatewayApprovals remains this host-side hook, now challenge+question gated.

    Tests (cmd/aura/gateway_resume_hook_test.go): ADD TestGatewayResumeHookRejectsMismatchedQuestion (WR-03): gw := gateway.New(config.ProfileSingleUserHardened, nil); pending, spec, args, key := gatewayApprovalPending(t, gw, "conv-adv") (records the challenge + gives the REAL question); overwrite pending.Question = "Save your meeting notes?" (the benign lie — the resume_context/fp stay REAL); call the hook with ActionAccept → assert it returns a NON-NIL error (question mismatch); then reDecideAllows(t, gw, spec, args, key) → assert v.Decision == gateway.Approve (nothing recorded, the tool stays withheld — the operator could NOT be tricked). Confirm the existing hook tests still pass (they carry the verbatim question). Run -race.

    Full-matrix validation (this is the last task — restore repo-wide green + prove zero regression): run go vet ./..., go build ./..., the unit -race suite across gateway/agent/cmd-aura/runner, the live db_integration reserve/idempotency tier, TestGatewayApprovalRoundTrip (cooperative round-trip still executes once), and TestAskUserOnlyPauseConstraint (pause detector untouched). Re-measure every touched/created file with wc -l to prove ≤600. Also run `go test -cover ./internal/gateway/ ./cmd/aura/` (and, when the stack is up, `make coverage` / `scripts/coverage_gate.sh` for the owned-surface ≥85% floor per CLAUDE.md) and disclose the resulting coverage percentage(s). Disclose in the SUMMARY that approvals_api.go / runner_resume.go / decide.go / reserve.go / llm_agent_pause.go were verified UNCHANGED, and the exact live db_integration command + result.
  </action>
  <verify>
    <automated>cd /d/Repo/Aura && go vet ./... && go build ./... && go test -race ./internal/gateway/ ./internal/agent/ ./cmd/aura/ ./internal/runner/ -run 'Gateway|Approve|Resume|RoundTrip|Challenge|Mismatch|Chain|Evict|AskUserOnly|ExecTool|Pause' -count=1 && wc -l internal/gateway/approvals.go internal/gateway/approve.go internal/gateway/gateway.go internal/gateway/approve_test.go internal/gateway/approvals_test.go cmd/aura/serve_adapters.go cmd/aura/gateway_resume_hook_test.go</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: cmd/aura/serve_adapters.go newGatewayResumeHook calls g.ApproveChallenge(pending.ConversationID, rc.Tool, rc.ArgsSHA256, pending.Question, ...) and no longer calls RecordResolvedApproval or reads rc.ConversationID as the key (grep: `ApproveChallenge(pending.ConversationID`; grep `RecordResolvedApproval` returns 0 hits in serve_adapters.go).
    - SOURCE: internal/agui/approvals_api.go, internal/runner/runner_resume.go, internal/gateway/decide.go, internal/gateway/reserve.go, internal/agent/llm_agent_pause.go, internal/agent/llm_agent_retry.go are UNCHANGED (git diff shows no edits to these files).
    - TEST (WR-03 adversarial): TestGatewayResumeHookRejectsMismatchedQuestion drives a real gateway_approval challenge, relays a benign question, asserts the hook errors and the re-drive stays Approve (grep: `MismatchedQuestion`). A skipped/0-subtest run is a FAIL.
    - BEHAVIOR (cooperative regression): TestGatewayApprovalRoundTrip still executes the spy exactly once (the verbatim-question relay matches the challenge); TestGatewayResumeHookRecordsAcceptedApproval/InChain still record + Allow.
    - BEHAVIOR (full matrix): go vet ./... + go build ./... clean; the unit -race suite green; the live db_integration reserve/idempotency tier green (no-skip-as-green); TestAskUserOnlyPauseConstraint green.
    - LOC: every touched/created file ≤600 LOC (wc -l), including serve_adapters.go and gateway_resume_hook_test.go.
  </acceptance_criteria>
  <done>The host-side resume hook now records an operator approval ONLY when the gateway actually issued a challenge for the authenticated conversation AND the operator-visible question matches the action being authorized — closing the confused-deputy bypass (CR-01) and the cross-conversation transfer (WR-02), proven by the adversarial mismatched-question test (WR-03) with the cooperative round-trip and the full unit + live db_integration matrix green (zero regression). GATE-01's interactive-approval consent property holds end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → gateway | The model emits the tool call and MAY relay the approval request via ask_user, INCLUDING the question text it shows the operator. It MUST NOT be able to make the operator authorize an action different from what the question describes, and MUST NOT self-approve (relaying ≠ approving). |
| operator → approval center | `POST /api/approvals/{token}/resolve` is the only write path to an approval; mounted behind Phase-25 RequireAuth + RequireCapability (cmd/aura/serve_webui.go — reused unchanged). The operator sees ONLY pending.Question (approvals_api.go:86). |
| resume hook → gateway ledger | newGatewayResumeHook is the SOLE production writer of GatewayApprovals; it now binds the operator-visible pending.Question to the server-issued challenge before recording. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan (delivered by this gap-closure) + verifying test |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07-01 (CR-01, HIGH — anchor) | Elevation of Privilege / Confused Deputy | gateway approval informed-consent binding (serve_adapters.go hook + approvals.go + approve.go) | mitigate | routeApprove records a server-side challenge (the gateway-generated question) keyed on the AUTHENTICATED (conversation_id, tool, args_sha256) when it issues the approval-required result; the hook records the approval ONLY IF that challenge exists AND pending.Question == the gateway-generated question (GatewayApprovals.ApproveChallenge, the true byte-for-byte analog of ShellApprovals.ApproveChallenge). A model relaying a benign lie question → question mismatch → error → nothing recorded → re-drive stays Approve (withheld). Maps ASVS L1 V1.2.2/V4.1.1 (enforce authorization at a trusted server layer; consent bound to the action). Verified by: TestGatewayResumeHookRejectsMismatchedQuestion + TestGatewayApprovalsApproveChallengeQuestionMismatch; the cooperative TestGatewayApprovalRoundTrip still passes. |
| T-35-07-02 (WR-02) | Tampering / cross-conversation transfer | resume-hook conversation binding (serve_adapters.go) | mitigate | The hook keys ApproveChallenge on the AUTHENTICATED pending.ConversationID (server-stored at pause creation), never the model-relayed resume_context conversation_id — so an approval surfaced from conv-A can never authorize a re-emit in conv-B. Maps ASVS L1 V4.2.1 (BOLA / authorize on the server-trusted identifier). Verified by: the hook source keying on pending.ConversationID (a mismatched conversation → challenge not found → error), exercised through TestGatewayResumeHookRecordsAcceptedApproval / InChain. |
| T-35-07-03 (WR-01) | Elevation / fail-closed bypass | routeApprove deny ordering + record guard (approve.go, gateway.go) | mitigate | (a) routeApprove's server_production/no-responder hard-deny is evaluated BEFORE the cross-turn Consume, so a recorded/fabricated approval is never consumed on a headless/production run; (b) Gateway.ApproveChallenge and RecordResolvedApproval refuse to record under ProfileServerProduction (defense in depth). Maps ASVS L1 V1.4.1/V4.1.5 (fail securely; deny by default). Verified by: TestRouteApproveProductionDeniesEvenWithLedgerApproval (injected approval → Deny, reserves==0) + TestGatewayApproveChallengeRefusedUnderProduction. |
| T-35-07-04 (inherited, 35-06) | Tampering | a re-emit reusing an approval for DIFFERENT args | mitigate (unchanged) | The ledger key still binds conversation_id + tool + canonical-args fingerprint (args_sha256); a semantically different re-emit → different fingerprint → no Consume hit → fail-closed. tool_call_id stays excluded. Verified by: TestGatewayApprovalRoundTrip fingerprint-match + the existing one-shot/canonical-equality tests (unchanged). |
| T-35-07-05 (inherited, 35-06) | EoP / crash between approve and execute | double-apply of the mutating side effect | mitigate (unchanged) | The in-memory ledger is lost on crash → operator re-approves (fail-closed); the executed-once guarantee remains the 35-04 reservation's job (single start∧¬end + rows-affected idempotency), untouched here. Verified by: the live db_integration reserve/idempotency tier (re-run in Task 3). |

Block-on-high: CR-01 is the sole HIGH-severity threat; its mitigation (challenge + question binding) AND its verifying tests (TestGatewayResumeHookRejectsMismatchedQuestion, TestGatewayApprovalsApproveChallengeQuestionMismatch) are both delivered in this plan — the block-on-high bar is met. No package-manager installs (npm/pip/cargo) in this plan → no T-35-07-SC supply-chain checkpoint and no Package Legitimacy Gate required. No new external dependency.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces (NEW symbols / removed symbols)

NEW:
- `internal/gateway/approvals.go` — `pending map[string]gatewayChallenge` field; `gatewayChallenge struct { question string }` type; `GatewayApprovals.Challenge(convID, tool, fp, question string)`; `GatewayApprovals.ApproveChallenge(convID, tool, fp, question string, r ResolvedApproval) error`. `Evict` extended to sweep the pending map.
- `internal/gateway/gateway.go` — `func (g *Gateway) ApproveChallenge(convID, tool, fp, question string, r ResolvedApproval) error` (production refuse-guard + delegate).
- `internal/gateway/approve_test.go` — `TestRouteApproveProductionDeniesEvenWithLedgerApproval`, `TestGatewayApproveChallengeRefusedUnderProduction`, and a challenge-recorded-on-issue assertion.
- `internal/gateway/approvals_test.go` — `TestGatewayApprovalsApproveChallengeQuestionMismatch` (+ challenge/evict-both/no-challenge cases).
- `cmd/aura/gateway_resume_hook_test.go` — `TestGatewayResumeHookRejectsMismatchedQuestion` (WR-03 adversarial).

MODIFIED (behavior): `routeApprove` (deny-before-Consume reorder + Challenge record + single fp/question thread); `gatewayApprovalRequiredResult`/`gatewayApprovalContext` (take fp/question params — IN-02); `Gateway.RecordResolvedApproval` (production refuse-guard); `newGatewayResumeHook` (ApproveChallenge with authenticated pending.ConversationID + pending.Question).

REMOVED: `GatewayApprovals.Peek` (IN-01 — dead, non-parity with ShellApprovals). The `{"type":"gateway_approval",...}` resume_context wire fields are UNCHANGED (no schema change). No new HTTP route, migration, or env var.

Source-grounding note: `GatewayApprovals.Approve`, `Gateway.RecordResolvedApproval`, and the `{gateway_approval, tool, conversation_id, args_sha256}` resume_context are RETAINED (test seams / wire fields) — do not flag them as drift; only `Peek` is removed.
</artifacts_produced>

<verification>
- `go vet ./...` and `go build ./...` clean (restored at Task 3).
- Unit + -race: `go test -race ./internal/gateway/ ./internal/agent/ ./cmd/aura/ ./internal/runner/ -count=1` green, including TestGatewayApprovalsApproveChallengeQuestionMismatch, TestRouteApproveProductionDeniesEvenWithLedgerApproval, TestGatewayApproveChallengeRefusedUnderProduction, TestGatewayResumeHookRejectsMismatchedQuestion, TestGatewayApprovalRoundTrip, TestAskUserOnlyPauseConstraint; goleak parity preserved.
- Live db_integration (`-p 1 -race`, live stack on 127.0.0.1, per CLAUDE.md WSL setup — prepend `~/.local/bin:~/go/bin` to PATH, export the composed AURA_DB_URL/AURA_DB_MIGRATE_URL DSNs): `go test -tags db_integration -race -p 1 ./internal/gateway/ -run 'GatewayApprovalResume|GatewayApprovalDecline|ApprovedCallReservedAndIdempotent' -count=1` green with real DB work (no-skip-as-green: a sub-second or skipped run is a FAIL). These reserve/idempotency proofs are unchanged by this gap-closure and prove zero regression.
- Prohibition audit: git diff scope equals exactly the 7 files_modified; internal/agui/approvals_api.go, internal/runner/runner_resume.go, internal/gateway/decide.go, internal/gateway/reserve.go, internal/agent/llm_agent_pause.go, internal/agent/llm_agent_retry.go are byte-unchanged; no new *.sql/route/env; gatewayApprovalKey excludes tool_call_id.
- Every touched file ≤600 LOC (wc -l): approvals.go, approve.go, gateway.go, approve_test.go, approvals_test.go, serve_adapters.go, gateway_resume_hook_test.go.
</verification>

<success_criteria>
- Under single_user_hardened, an operator can only authorize a mutating GateRecommended call whose gateway-generated question they were actually shown: a model relaying a benign/false question records NO approval (ApproveChallenge question-mismatch) and the action stays withheld — the confused-deputy bypass (CR-01) is closed and machine-checkably tested (WR-03).
- The resume hook binds on the authenticated pending.ConversationID (WR-02); no cross-conversation approval transfer.
- A server_production/headless Decide never consumes a cross-turn approval (deny-before-Consume) and never records one (production refuse-guard) — WR-01 closed with defense in depth.
- gatewayArgsFingerprint is computed once per approval-required result (IN-02); dead Peek removed (IN-01).
- GATE-01..04 mechanics hold with zero regression: the cooperative persist→resume round-trip still executes exactly once, the live reserve/idempotency tier stays green, and no 35-06 prohibition is violated (no pre-dispatch intercept, no competing executed Insert, no model self-approval, no new migration/route/env knob, ledger key excludes tool_call_id).
- After this ships and re-verification + the code-review gate pass, GATE-01 (and GATE-02) can be flipped to `[x]` and GATE-01 unblocked in REQUIREMENTS.md (orchestrator bookkeeping at phase completion).
</success_criteria>

<output>
Create `.planning/phases/35-toolgateway-policy-engine/35-07-SUMMARY.md` when done, disclosing: that internal/agui/approvals_api.go / internal/runner/runner_resume.go / internal/gateway/decide.go / internal/gateway/reserve.go / internal/agent/llm_agent_pause.go / internal/agent/llm_agent_retry.go were verified UNCHANGED (git diff); the exact CR-01→mitigation→test trace (challenge recorded on issue → hook question-match → adversarial mismatch rejected); the WR-01/WR-02/WR-03/IN-01/IN-02 dispositions with their proving tests; the exact live db_integration command + result (real rows asserted, not skipped); the wc -l re-measure of every touched file; the coverage percentage for the touched packages (CLAUDE.md ≥85% owned-surface floor); and the final GATE-01 consent-binding end-to-end status.
</output>
