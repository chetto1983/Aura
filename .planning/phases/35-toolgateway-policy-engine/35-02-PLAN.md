---
phase: 35-toolgateway-policy-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/hooks_command_hardening_test.go
  - internal/agent/hooks_command_policy_internal_test.go
  - internal/agent/hooks_policy_test.go
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "A command hook that times out DENIES the turn (tool never executes) under the default fail-closed policy"
    - "A command hook that crashes (non-zero exit with no parseable decision) DENIES the turn (cannot silently allow a denied command)"
    - "A crashed `rewrite` payload is rejected (AG-030)"
    - "An explicit `fail_open` per-hook policy ALLOWS (contained) — proving the knob is honored, not that a denied command is silently allowed"
    - "The default (empty/unset) command-hook fail policy resolves to FailClosed"
  artifacts:
    - path: "internal/agent/hooks_command_hardening_test.go"
      provides: "extended timeout/crash/non-zero → DENY matrix"
    - path: "internal/agent/hooks_policy_test.go"
      provides: "explicit fail_open → contained-allow proof"
  key_links:
    - from: "internal/agent/hooks_command.go"
      to: "internal/agent/hooks.go"
      via: "commandHookFailPolicy → HookManager.hookFault (FailClosed denies the turn)"
      pattern: "hookFault|FailClosed"
  prohibitions:
    - "Do NOT add a new global env knob (D-04 — GATE-02 is already satisfied by the FailClosed default; no config surface for a met requirement)"
    - "Do NOT modify production hook code to satisfy the requirement — GATE-02 is verify-only; this plan adds/strengthens tests and pins coverage"
    - "Do NOT weaken any test to make it pass (a failing assertion here means a real fail-open regression)"
---

<objective>
Deliver D-04 (GATE-02): confirm and lock the already-shipped command-hook fail-closed default with strengthened tests. `commandHookFailPolicy` already defaults to `FailClosed`, honors a per-hook JSON `fail_policy`, and rejects a crashed rewrite (AG-030). This plan proves — via extended tests + pinned coverage — that timeout / crash / non-zero-exit-without-decision all DENY and cannot silently allow a denied command, and that an explicit `fail_open` opt-in allows (contained). No production code change; no new env knob.

Purpose: SC-2 requires a timing-out/crashing command hook to deny under hardened/production. The default already over-satisfies this (it denies in ALL profiles — RESEARCH CV-5). Phase 35 pins that guarantee with the test matrix so a future regression to fail-open is caught.

Output: extended `hooks_command_hardening_test.go`, `hooks_command_policy_internal_test.go`, `hooks_policy_test.go`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-toolgateway-policy-engine/35-CONTEXT.md
@.planning/phases/35-toolgateway-policy-engine/35-RESEARCH.md
@.planning/phases/35-toolgateway-policy-engine/35-VALIDATION.md

@internal/agent/hooks_command.go
@internal/agent/hooks.go
</context>

## Artifacts this phase produces (this plan's contributions)

- No new symbols — this plan is verify-only.
- Extended test surface proving GATE-02: the timeout/crash/non-zero → DENY matrix and the `fail_open` → contained-allow proof, plus pinned coverage on the `hooks_command.go` / `hooks.go` fail paths.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen the timeout / crash / non-zero → DENY matrix</name>
  <files>internal/agent/hooks_command_hardening_test.go, internal/agent/hooks_command_policy_internal_test.go</files>
  <acceptance_criteria>
    - A hook timeout under the default policy asserts the turn DENIES via `HookManager.hookFault` and a spy/fake tool's Execute is NOT called
    - A hook non-zero exit with no parseable allow/deny decision asserts DENY (cannot silently allow)
    - A crashed `rewrite` payload is rejected (AG-030), turn aborts
    - An unknown `fail_policy` string resolves to FailClosed + error (not silent allow)
    - `go test -race ./internal/agent/ -run 'CommandHook.*(Timeout|Crash|Deny|FailClosed|Rewrite)'` passes; no production file changed
  </acceptance_criteria>
  <read_first>
    - internal/agent/hooks_command.go (`commandHookFailPolicy` default→FailClosed :167-176; timeout→error :311-313; non-zero-exit deny honored + crashed rewrite rejected :315-320,329-334)
    - internal/agent/hooks.go (`HookManager.hookFault`: FailClosed returns error / FailOpen contains :119-128; `BeforeTool` propagates abort :186-210)
    - internal/agent/hooks_command_hardening_test.go, internal/agent/hooks_command_policy_internal_test.go (existing cases to extend, not duplicate)
    - internal/agent/llm_agent_dispatch.go (:65-66 — how a hook abort ends the turn)
    - 35-RESEARCH.md §CV-5; 35-VALIDATION.md GATE-02 rows
  </read_first>
  <behavior>
    - Hook timeout under default policy → turn aborts; the gated tool's Execute is never called
    - Hook non-zero exit with no parseable allow/deny decision → turn aborts (cannot silently allow)
    - Crashed `rewrite` payload → rejected (AG-030), turn aborts
    - Unknown fail_policy string → FailClosed + error (not silent allow)
  </behavior>
  <action>
    Per D-04: EXTEND (do not rewrite) `hooks_command_hardening_test.go` and `hooks_command_policy_internal_test.go` so timeout, crash(non-zero), and non-zero-no-decision each assert the turn DENIES via `HookManager.hookFault` (FailClosed) and that a spy/fake tool's Execute is never reached. Add the unknown-policy→FailClosed case if not already covered. Use realistic fixtures (a fake `CommandHook` that times out / exits non-zero / emits a malformed rewrite). No production code change; no new env knob.
  </action>
  <verify>
    <automated>go test -race ./internal/agent/ -run 'CommandHook.*(Timeout|Crash|Deny|FailClosed|Rewrite)'</automated>
  </verify>
  <done>timeout/crash/non-zero/unknown-policy all deny; no silent-allow path exists; race-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove explicit fail_open contained-allow + pin coverage</name>
  <files>internal/agent/hooks_policy_test.go</files>
  <acceptance_criteria>
    - A hook registered with explicit `fail_open` that faults asserts the fault is contained and the tool ALLOWS (contrast to Task 1's default deny)
    - Both branches of `hookFault` (FailClosed deny / FailOpen contain) are exercised (coverage pin)
    - A dedicated case/assert confirms NO code path lets a denied command through under the default policy
    - `go test -race ./internal/agent/` green; git diff limited to `_test.go` files
  </acceptance_criteria>
  <read_first>
    - internal/agent/hooks.go (`hookFault` FailOpen containment :119-128; `FailPolicy`/`RegisterWithPolicy`)
    - internal/agent/hooks_command.go (per-hook JSON `fail_policy` opt-in :167-176)
    - internal/agent/hooks_policy_test.go (existing policy cases to extend)
    - 35-VALIDATION.md GATE-02 `fail_open` row
  </read_first>
  <behavior>
    - A hook registered with explicit `fail_open` that faults → the fault is contained and the tool ALLOWS (proving the knob is real, not that a denied command silently passes)
    - The FailClosed vs FailOpen branch of `hookFault` is exercised on both sides (coverage pin)
  </behavior>
  <action>
    Per D-04: EXTEND `hooks_policy_test.go` with a `CommandHookFailOpen` case asserting an explicit `fail_open` hook fault is contained → allow (contrast to Task 1's default deny). Ensure both branches of `hookFault` are covered so the coverage gate pins the fail-closed guarantee. Confirm (assert in a test comment or a dedicated case) that NO code path lets a denied command through under the default policy.
  </action>
  <verify>
    <automated>go test -race ./internal/agent/ -run 'CommandHookFailOpen|Policy'</automated>
    <automated>go test -race ./internal/agent/</automated>
  </verify>
  <done>fail_open contained-allow proven; hookFault both branches covered; full internal/agent race suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command hook subprocess → agent | A configured command hook is external code whose timeout/crash/exit-code must never be interpreted as "allow" for a denied tool. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-F006 | Elevation of Privilege / Repudiation | `commandHookFailPolicy` + `HookManager.hookFault` | mitigate (high) | Default FailClosed denies the turn on timeout/crash/non-zero; the extended test matrix proves a denied command cannot be silently allowed and pins coverage on both `hookFault` branches. Verify-only — no new env knob (D-04). |
| T-35-SC | Tampering | npm/pip/cargo installs | accept (n/a) | No external packages installed this plan. |

block_on: high — the single high threat is mitigated by the existing FailClosed default, locked by the strengthened test matrix.
</threat_model>

<verification>
- `go test -race ./internal/agent/` green.
- No production file changed under `internal/agent` (git diff limited to `_test.go` files) — GATE-02 is verify-only.
- No new `AURA_*` env knob introduced.
- Coverage on `hooks_command.go` / `hooks.go` fail paths is exercised on both FailClosed and FailOpen branches.
</verification>

<success_criteria>
- Timeout / crash / non-zero-no-decision all DENY; the gated tool never executes (SC-2 / GATE-02).
- Explicit `fail_open` allows (contained) — the knob is proven, not silent-allow.
- No new global env knob (D-04).
- No production hook code changed.
</success_criteria>

<output>
Create `.planning/phases/35-toolgateway-policy-engine/35-02-SUMMARY.md` when done.
</output>
