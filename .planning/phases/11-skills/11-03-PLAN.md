---
phase: 11-skills
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - internal/skills/validator.go
  - internal/skills/validator_test.go
  - internal/skills/validator_fuzz_test.go
  - internal/skills/catalog.go
  - internal/skills/catalog_test.go
  - internal/config/config.go
autonomous: true
requirements: [CAP-07]
must_haves:
  truths:
    - "Validator NFKC-normalizes then literal-blocklist-rejects; a fullwidth/compatibility variant of a blocklisted token is rejected (SC#3)"
    - "SanitizeName is the single name chokepoint; validation is a pure function over body+frontmatter (no DB, no FS)"
    - "FuzzSkillValidator runs 10K NFKC/Unicode mutations and rejects every NFKC-collapse-to-blocklist input"
    - "Catalog client hits skills.sh /api/search with lax JSON decode, guards empty queries, ranks by installs"
  artifacts:
    - path: "internal/skills/validator.go"
      provides: "NFKC + literal blocklist + SanitizeName chokepoint (D-27/D-28)"
      min_lines: 40
      contains: "norm.NFKC"
    - path: "internal/skills/validator_fuzz_test.go"
      provides: "FuzzSkillValidator (SC#3, 10K mutations)"
      contains: "func FuzzSkillValidator"
    - path: "internal/skills/catalog.go"
      provides: "skills.sh /api/search client, lax decode (D-11)"
      min_lines: 30
  key_links:
    - from: "internal/skills/validator.go"
      to: "golang.org/x/text/unicode/norm"
      via: "norm.NFKC.String(body) before literal blocklist match (Pitfall 2)"
      pattern: "norm\\.NFKC"
    - from: "internal/skills/catalog.go"
      to: "skills.sh /api/search"
      via: "net/http GET, lax json.Decode (no DisallowUnknownFields)"
      pattern: "api/search"
---

<objective>
Sub-slice 7b — the validator + catalog read paths. The validator is a PURE function over body bytes + frontmatter: NFKC-normalize FIRST, then literal-blocklist-reject (Pitfall 2) + the SanitizeName single chokepoint + name/size structure checks. The fuzz target `FuzzSkillValidator` (SC#3) drives 10K NFKC/Unicode mutations of blocklist patterns and asserts every collapse-to-blocklist input is rejected. The catalog client hits skills.sh `/api/search` with lax JSON decode (D-11), transport-isolated for the future public-API swap.

Purpose: Build the WRITE-boundary enforcement primitive (consumed by the writer in 11-04 + installer in 11-05) and the discovery transport (consumed by the catalog action). The validator is NOT wired into the loader (D-28 — disk is operator-trusted; enforcement is write-boundary only).
Output: `internal/skills/{validator,catalog}.go` + `validator_fuzz_test.go` (SC#3) + unit tests + the D-34 catalog/blocklist/timeout env vars in config.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
@.planning/phases/11-skills/11-PATTERNS.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-security/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validator (NFKC + blocklist + SanitizeName) + FuzzSkillValidator (SC#3)</name>
  <read_first>
    - internal/identity/store.go (lines ~32-33, 160-188) — capNameRe compiled-once regex + validateGrantInput single-chokepoint pattern + the Err* sentinel declarations (39-43) to mirror as ErrInvalidName/ErrBlocklisted
    - internal/skills/frontmatter.go (from 11-02) — the Frontmatter struct the validator checks
    - .planning/phases/11-skills/11-RESEARCH.md §"Code Examples" → "NFKC + blocklist validator" + §"Common Pitfalls" Pitfall 2 (NFKC-collapse) — the exact normalize-then-match contract
    - .planning/phases/11-skills/11-CONTEXT.md D-27 (hard-for-model / operator-override) + D-28 (write-boundary only) + D-34 (AURA_SKILL_INJECTION_BLOCKLIST builtin list)
    - .claude/skills/golang-testing/SKILL.md — the go test -fuzz section (seed corpus, fuzztime, FuzzXxx signature)
  </read_first>
  <behavior>
    - Test: violatesBlocklist rejects a body containing a plain blocklisted token
    - Test: violatesBlocklist rejects a body where the token is encoded with FULLWIDTH Latin codepoints that NFKC-collapse to the literal (Pitfall 2 — the hand-crafted case)
    - Test: SanitizeName accepts `^[a-z0-9-]{1,64}$`, rejects uppercase/underscore/dot/over-64/empty, and rejects a name != its dir name
    - Test: ValidateForWrite returns (matched-sequence, position) detail on a blocklist hit (so the operator gate can show "exactly which sequences matched and where", D-27)
    - Test (fuzz): FuzzSkillValidator — for each generated input, if norm.NFKC.String(input) lowercased contains a blocklist literal, ValidateForWrite MUST reject it (the SC#3 property: no NFKC-collapse-to-blocklist input slips through)
  </behavior>
  <action>
    Create `internal/skills/validator.go`:
    - `var skillNameRe = regexp.MustCompile(\`^[a-z0-9-]{1,64}$\`)` (compiled once, mirror identity capNameRe). `SanitizeName(name, dirName string) error` is the SINGLE name chokepoint: regex match + must-equal-dir-name (D-30). Sentinel errors `ErrInvalidName`, `ErrBlocklisted` mirroring identity/store.go:39-43.
    - `violatesBlocklist(body string, blocklist []string) (matched string, pos int, ok bool)`: `folded := norm.NFKC.String(body)` FIRST (import golang.org/x/text/unicode/norm — promoted to direct in 11-02), then case-fold and `strings.Index` each blocklist literal; return the matched sequence + byte position for the operator gate message (D-27). NFKC normalize is non-negotiably FIRST (Pitfall 2).
    - `ValidateForWrite(fm Frontmatter, body string, blocklist []string, allowBlocklisted bool) error`: structure checks (name regex via SanitizeName, description ≤1024, body ≤AURA_SKILL_BODY_CAP_BYTES, type enum) + the blocklist check UNLESS allowBlocklisted (the D-27 operator override; the writer/CLI passes true only after the gate shows the match — model-authored create/update always passes false, hard-reject no escape).
    - Keep this PURE: no DB, no FS, no env reads (config passes blocklist + cap in). NOT called from the loader (D-28).
    Create `internal/skills/validator_fuzz_test.go`: `func FuzzSkillValidator(f *testing.F)` with a seed corpus of blocklist literals + fullwidth/compatibility variants + benign strings; the fuzz body asserts the SC#3 property above. Provide a deterministic 10K-mutation guard via a companion `TestSkillValidator_NFKCCorpus` table (so CI without -fuzz still exercises the corpus; -fuzz=FuzzSkillValidator -fuzztime=60s is the SC#3 acceptance command).
    Create `internal/skills/validator_test.go`: the non-fuzz behaviors (table-driven, realistic blocklist fixtures).
    Add the D-34 vars to `internal/config/config.go`: AURA_SKILL_INJECTION_BLOCKLIST (a builtin default list — read CONTEXT/PRD for the seed tokens; comma-separated env override) — mirror the existing config loader shape. Keep ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./internal/skills/... && go build ./... && go test -race ./internal/skills/... && go test -run TestSkillValidator_NFKCCorpus ./internal/skills/</automated>
  </verify>
  <acceptance_criteria>
    - internal/skills/validator.go contains "norm.NFKC" and the NFKC call precedes the blocklist Index call (normalize-first)
    - validator.go does NOT import database/sql, pgx, or os (pure function — no DB/FS)
    - `go test -fuzz=FuzzSkillValidator -fuzztime=20s ./internal/skills/` finds no failing input (SC#3 smoke; full 60s is the phase-gate command)
    - validator_test.go asserts a FULLWIDTH-encoded blocklist token is rejected
    - validator_test.go asserts SanitizeName rejects a name that differs from its dir name
    - `go list -deps ./internal/skills | grep -c "internal/skills"` confirms validator is NOT imported by loader.go (grep loader.go: no ValidateForWrite/violatesBlocklist call)
  </acceptance_criteria>
  <done>Validator is a pure NFKC-first blocklist+name+size chokepoint with an operator-override path; FuzzSkillValidator passes; the loader does not call it (D-28); all green + race-clean.</done>
</task>

<task type="auto">
  <name>Task 2: skills.sh /api/search catalog client (lax decode, D-11)</name>
  <read_first>
    - internal/sandboxagent/client.go (lines ~51-92) — the stdlib net/http JSON client: New(cfg) construction, http.Client timeout, status-class guard (resp.StatusCode<200||>299), io.LimitReader error body, json.NewDecoder.Decode
    - .planning/spikes/003-skills-sh-search-api/README.md — the /api/search contract: {skills:[{id,skillId,name,installs,source}]}, schema drift (isDuplicate/count), empty-query→400, semantic multi-word, rank by installs
    - .planning/phases/11-skills/11-RESEARCH.md §"Code Examples" → "Catalog client, lax decode" + §Pitfall 5 (schema drift)
    - .planning/phases/11-skills/11-CONTEXT.md D-11 (JSON endpoint) + D-12 (default-ON, disable-catalog escape hatch) + D-34 (AURA_SKILL_CATALOG_URL, AURA_SKILL_INSTALL_TIMEOUT_SEC)
  </read_first>
  <action>
    Create `internal/skills/catalog.go`: a `CatalogClient` mirroring sandboxagent.Client construction — `New(cfg CatalogConfig)` with `BaseURL` (default https://www.skills.sh, from AURA_SKILL_CATALOG_URL), `TimeoutSec` (from AURA_SKILL_INSTALL_TIMEOUT_SEC or a catalog-specific default), and an injectable `*http.Client`. `Search(ctx, query string) ([]CatalogItem, error)`:
    - GUARD empty query before the call (return a clear error; the server 400s on empty, Pitfall 5).
    - GET `{BaseURL}/api/search?q={url.QueryEscape(query)}`.
    - status-class guard (copy the sandboxagent pattern: non-2xx → error with a LimitReader'd body snippet).
    - LAX decode into `struct{ Skills []CatalogItem }` — NEVER `DisallowUnknownFields` (isDuplicate/count drift in/out). `CatalogItem` = `{ID string json:"id"; SkillID string json:"skillId"; Name string json:"name"; Installs int json:"installs"; Source string json:"source"}`.
    - rank by `Installs` descending (sort.SliceStable) before returning; multi-word queries hit the server-side semantic search (no client-side change).
    Isolate the transport so the future public API (vercel-labs/skills#426) is a drop-in swap — keep the HTTP shape behind the Search method, no leakage of the endpoint into callers. Add a `Disabled bool` honoring the AURA_SKILL_CATALOG default-ON / `disable-catalog` escape hatch (D-12) — when disabled, Search returns a clear "catalog disabled" sentinel error.
    Add AURA_SKILL_CATALOG_URL + AURA_SKILL_INSTALL_TIMEOUT_SEC to config.go.
    Create `internal/skills/catalog_test.go`: a httptest.Server returning the spike-003 shape WITH an extra unknown field (isDuplicate) — assert lax decode succeeds, ranking-by-installs is correct, empty-query is guarded pre-call, non-2xx surfaces an error, and Disabled returns the sentinel. Race-clean.
  </action>
  <verify>
    <automated>go vet ./internal/skills/... && go build ./... && go test -race ./internal/skills/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/skills/catalog.go contains "api/search" and does NOT contain "DisallowUnknownFields"
    - catalog_test.go uses httptest.Server and asserts decode succeeds with an unknown field present (lax decode, Pitfall 5)
    - catalog_test.go asserts results are sorted by Installs descending
    - catalog_test.go asserts an empty query is rejected before any HTTP call (no request reaches the test server)
    - catalog.go has a Disabled path returning a sentinel error (D-12 escape hatch)
  </acceptance_criteria>
  <done>Catalog client hits /api/search with lax decode, guards empty queries, ranks by installs, isolates transport, honors disable-catalog; unit tests green + race-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/operator input → validator | model-authored skill body+name crosses the write-boundary check (NFKC + blocklist) |
| Aura → skills.sh /api/search | external untrusted JSON response crosses into the catalog client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-T1 | Tampering | prompt-injection via NFKC-obfuscated blocklist token in a skill body | mitigate | norm.NFKC.String FIRST, then literal blocklist; FuzzSkillValidator (SC#3) proves no collapse-to-blocklist input slips — Task 1 (HIGH severity threat, has a mitigating task) |
| T-11-03-E1 | Elevation of Privilege | model bypasses the gate by passing allowBlocklisted | mitigate | model-authored create/update always calls ValidateForWrite with allowBlocklisted=false (hard-reject, no escape, D-27); only operator CLI/install passes true after the gate shows the match — enforced by callers in 11-04/11-05, contract set here |
| T-11-03-I1 | Information Disclosure | skills.sh response tampering / schema drift breaks the client | mitigate | lax decode (no DisallowUnknownFields), status-class guard, LimitReader'd error body — Task 2 (Pitfall 5) |
| T-11-03-D1 | Denial of Service | huge catalog response or slow skills.sh hangs Aura | mitigate | http.Client timeout (AURA_SKILL_INSTALL_TIMEOUT_SEC); LimitReader on the error body — Task 2 |
| T-11-03-S1 | Spoofing | skills.sh lookalike host | accept | AURA_SKILL_CATALOG_URL is operator-config (not model-supplied); TOFU canonical-hash pin happens at install (plan 11-05), not at browse |

No npm/pip/cargo installs in this plan. norm package is already vetted (RESEARCH §Package Legitimacy, official x/).
</threat_model>

<verification>
- `go test -race ./internal/skills/...` green; `go test -fuzz=FuzzSkillValidator -fuzztime=60s ./internal/skills/` finds no crasher (SC#3 phase-gate command)
- validator.go is import-pure (no pgx/os) and NOT referenced by loader.go (D-28)
- catalog.go lax-decodes + ranks-by-installs + guards empty queries
- golangci-lint 0 on internal/skills; all files ≤600 LOC
</verification>

<success_criteria>
- Validator: NFKC-first blocklist + SanitizeName chokepoint + operator override; pure, write-boundary-only
- FuzzSkillValidator (SC#3) green at 60s; deterministic NFKC corpus test for CI
- Catalog client: /api/search lax decode, installs-ranked, transport-isolated, disable-catalog honored
- Coverage contributes toward the ≥85% phase floor
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-03-SUMMARY.md` when done
</output>
