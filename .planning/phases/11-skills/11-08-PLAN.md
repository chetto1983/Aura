---
phase: 11-skills
plan: 08
type: execute
wave: 8
depends_on: ["11-07"]
files_modified:
  - internal/eval/skills_cot_eval.go
  - internal/eval/scenarios_skills.go
  - internal/skills/smoke_test.go
  - scripts/cache_invariant_audit.sh
  - docs/aura-quality-snapshot.md
  - .github/workflows/skills.yml
autonomous: false
requirements: [CAP-07, CAP-08]
must_haves:
  truths:
    - "The xlsx North-Star E2E runs live: a natural prompt drives catalog→ask_user→install→sandbox_exec; the .xlsx exists, opens, contains today's data (artifact-not-reply)"
    - "The tool_use sequence is asserted (ground-truth), and the judge rubric scores ≥90% (D-35)"
    - "Two smaller smokes pass: haiku flow (create→approve→messages[1]→next-turn behavior) + snippet save→by-path reuse"
    - "CI gates the unit + db_integration + sandbox_integration tiers (no-skip-as-green); coverage ≥85% across the matrix"
    - "Quality numbers are recorded in docs/aura-quality-snapshot.md"
  artifacts:
    - path: "internal/eval/scenarios_skills.go"
      provides: "xlsx North-Star scenario + ground-truth assertions (D-35)"
      contains: "xlsx"
    - path: ".github/workflows/skills.yml"
      provides: "CI: unit + db_integration + sandbox_integration tiers + coverage fold (no-skip-as-green)"
      contains: "sandbox_integration"
    - path: "docs/aura-quality-snapshot.md"
      provides: "Phase-11 quality rows (E2E judge %, coverage, mutation)"
      contains: "Phase 11"
  key_links:
    - from: "internal/eval/scenarios_skills.go"
      to: "the live agent (catalog→ask_user→install→sandbox_exec)"
      via: "cot_eval harness, OPENROUTER-gated, operator-run NOT CI"
      pattern: "cot_eval"
    - from: ".github/workflows/skills.yml"
      to: "the tagged tiers"
      via: "CI exports composed DSNs + AURA_SANDBOX_AGENT_TOKEN + make sandbox-up; CI=true t.Fatal-on-unset"
      pattern: "db_integration|sandbox_integration|CI=true"
---

<objective>
The Phase-11 dual gate (D-35, Phase-9 style). The HARD floor is ground-truth: the xlsx North-Star E2E drives a NATURAL prompt ("fammi un file Excel con il mercato di Yahoo Finance di oggi", no "skill" word) through the full autonomous loop — recognize the capability gap → discover anthropics/skills/xlsx on skills.sh → ask_user install approval → install+validate → use the skill (bundled Python by-path in the sandbox) → pull today's data via the shipped web tools → produce the `.xlsx`. Ground truth: the file EXISTS, OPENS, CONTAINS today's data (artifact-not-reply, `feedback_probe_must_verify_artifact_not_reply`) AND the tool_use sequence (catalog→ask_user→install→sandbox_exec) is asserted. The judge rubric scores ≥90% (autonomous capability-gap recognition, install prudence, output quality). Plus 2 smaller smokes (haiku create→approve→messages[1]→next-turn; snippet save→by-path reuse) + the CI gate (all tiers, no-skip-as-green, ≥85% coverage) + the quality snapshot.

Purpose: Prove CAP-07 + CAP-08 are TRUE end-to-end against the live substrate, gate regressions in CI, and stamp the numbers. This is the Gate-3 evidence + the human sign-off.
Output: the cot_eval xlsx scenario + 2 smokes + CI workflow + cache-invariant extension + quality snapshot rows + a Gate-3 human-verify checkpoint.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
@.planning/phases/11-skills/11-PATTERNS.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: xlsx North-Star cot_eval scenario + 2 smokes + cache-invariant extension</name>
  <read_first>
    - internal/eval/ (the existing cot_eval harness: the build tag `cot_eval`, OPENROUTER_API_KEY gating, the scenario/judge shape — read scoring_cot_eval.go + the swarm scenario from Phase 9 as the template; reference auto-memory reference_cot_eval_harness + reference_e2e_full_matrix_invocation)
    - .planning/phases/11-skills/11-CONTEXT.md D-35 (the exact dual-gate spec: ground-truth tool_use sequence + .xlsx exists/opens/contains-today + judge ≥90% over 3 dims; the 2 smaller smokes) + §specifics (the xlsx North-Star scenario lens) + §canonical_refs (the find-skills + xlsx skill targets)
    - .planning/spikes/006-xlsx-skill-dry-run/README.md — the xlsx skill ingredients (the bundled-script import set openpyxl/defusedxml/lxml/validators; the YAML+CRLF needs) so the scenario's install+use actually works
    - scripts/cache_invariant_audit.sh — the existing cross-slice messages[0] byte-stability job to EXTEND for the messages[1] always-block + manifest-in-Description turn-stability
    - the live web tools (web_search SearXNG + web_fetch) wiring — the scenario pulls today's data through them (reference_web_tools_e2e_and_agent_run + reference_live_finalize_e2e_socat_bridge for the host-run gotchas)
  </read_first>
  <action>
    Create `internal/eval/scenarios_skills.go` + `internal/eval/skills_cot_eval.go` (build tag `cot_eval`, OPENROUTER+AURA_EVAL_SELF_*-gated, operator-run NOT CI — mirror the Phase-9 swarm cot_eval exactly):
    - `skillsScenarios()` SEPARATE from the existing scenarios(). The xlsx North-Star scenario: a NATURAL Italian prompt ("fammi un file Excel con il mercato di Yahoo Finance di oggi", no "skill"/"install" word — asserted absent). The registry = buildBaseRegistry + the skill tool (live loader/writer/installer/catalog) + web tools + sandbox_exec. find-skills is pre-installed via the real install flow (or the scenario installs it first); the agent must autonomously discover anthropics/skills/xlsx.
    - HARD FLOOR ground-truth assertions (artifact-not-reply): (a) the tool_use sequence includes catalog→ask_user→install→sandbox_exec in order; (b) the produced .xlsx EXISTS in the workspace, OPENS (parse it with a Go xlsx reader OR a sandbox_exec python openpyxl read-back), and CONTAINS today's date / market data (not a stub). NEVER assert on r.Reply alone.
    - Judge rubric ≥90% equal-weight mean over: autonomous capability-gap recognition, install prudence (asked before installing), output quality (D-35). Reuse the Phase-9 reasoning-judge + the dims+gate fix.
    Create the 2 smaller smokes in `internal/skills/smoke_test.go` (these CAN be CI-runnable if they avoid the LLM — use a FakeClient where possible):
    - haiku flow: create an always:true skill → approve (ask_user resume / CLI) → assert it renders into messages[1] → assert next-turn the always-block is present in the assembled context (the create→approve→messages[1]→next-turn behavior).
    - snippet flow: save a snippet → activate → use returns the /skills path → exec by-path → output captured → reuse (a second use stamps usage).
    Extend `scripts/cache_invariant_audit.sh`: add assertions that (a) the messages[1] always-block is byte-stable across a 20-turn replay for a fixed skill set, and (b) the skill tool's manifest-in-Description is turn-stable; messages[0] SHA stays constant (the existing assertion) even with skills loaded.
  </action>
  <verify>
    <automated>go vet ./internal/eval/... ./internal/skills/ && go build -tags cot_eval ./internal/eval/... && go test -race -run 'TestHaikuFlow|TestSnippetReuse' ./internal/skills/ && bash scripts/cache_invariant_audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - `go build -tags cot_eval ./internal/eval/...` exits 0 (the scenario compiles)
    - internal/eval/scenarios_skills.go asserts the prompt does NOT contain "skill"/"install" (natural prompt) AND asserts the catalog→ask_user→install→sandbox_exec tool_use order
    - the scenario asserts the .xlsx exists + opens + contains today's data (NOT a reply-only assertion)
    - `go test -race -run 'TestHaikuFlow|TestSnippetReuse' ./internal/skills/` green (the 2 smokes)
    - `bash scripts/cache_invariant_audit.sh` passes incl. the new messages[1] byte-stability assertion
  </acceptance_criteria>
  <done>The xlsx North-Star cot_eval scenario compiles with ground-truth artifact+sequence assertions + a ≥90% judge; the 2 smokes pass; cache-invariant extended for messages[1] + manifest turn-stability; messages[0] still byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow (all tiers, no-skip-as-green, coverage fold) + quality snapshot rows</name>
  <read_first>
    - .github/workflows/sandbox.yml (the Phase-8 DinD gate — the sandbox-2b-session-gate job: Postgres + sidecar + egress bridge + AURA_RUN_DIR + CI=true no-skip-as-green + the tagged tier + coverage fold) — the template for skills.yml
    - .github/workflows/ (the existing db/neo4j integration jobs — the composed DSN export pattern AURA_DB_URL/AURA_DB_MIGRATE_URL from POSTGRES_*; the make sandbox-up + AURA_SANDBOX_AGENT_TOKEN export)
    - scripts/coverage_gate.sh — the owned-surface ≥85% floor (internal/* minus generated sqlc); how internal/skills folds in
    - docs/aura-quality-snapshot.md — the existing living-doc shape (the Phase-8/9 rows) to append Phase-11 rows
    - .planning/phases/11-skills/11-RESEARCH.md §"Validation Architecture" → Sampling Rate / Phase gate (the exact tiers + mutation spot-check targets validator.go + writer.go ≥70%)
    - CLAUDE.md §"Quality tooling & gates" (the make quality-full gate, the no-skip-as-green discipline, the coverage floor 85%)
  </read_first>
  <action>
    Create `.github/workflows/skills.yml`: a gating job mirroring sandbox.yml that:
    - stands up Postgres (migrate through 0010) + the sandbox-agent (make sandbox-up) with `AURA_SANDBOX_AGENT_TOKEN` set + the ro /skills mount; exports the composed DSNs (AURA_DB_URL/AURA_DB_MIGRATE_URL from POSTGRES_*, NOT just the primitives) + AURA_RUN_DIR + AURA_SKILL_EXPORT_DIR + CI=true.
    - runs the unit tier (`go test -race ./internal/skills/ ./internal/agent/tools/ ./internal/cron/...`), the `db_integration` tier (audit immutability SC#1/SC#2 + migration round-trip + TTL seed), the `sandbox_integration db_integration` tier (SC#4 by-path snippet exec), and the fuzz smoke (`go test -fuzz=FuzzSkillValidator -fuzztime=60s ./internal/skills/` — SC#3).
    - go-mutesting spot-check ≥70% on internal/skills/validator.go + writer.go (the critical files) — same path as the Makefile/Phase-8 jobs.
    - folds internal/skills into the ≥85% coverage gate (scripts/coverage_gate.sh).
    - no-skip-as-green: the tiers t.Fatal under $CI when env unset (verify the skip-helpers honor CI); a sub-second integration runtime is a skip tell — the job must show real execution.
    Append Phase-11 rows to `docs/aura-quality-snapshot.md`: the E2E judge % (placeholder TBD until the operator runs the live cot_eval), unit+integration+sandbox combined coverage, validator.go + writer.go mutation scores, and the SC#1-SC#5 status. Mark the live xlsx E2E as operator-run (NOT CI).
  </action>
  <verify>
    <automated>test -f .github/workflows/skills.yml && grep -c "sandbox_integration\|db_integration\|CI=true\|AURA_SANDBOX_AGENT_TOKEN" .github/workflows/skills.yml && grep -c "Phase 11" docs/aura-quality-snapshot.md</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/skills.yml exists and contains "sandbox_integration", "db_integration", "CI=true", "AURA_SANDBOX_AGENT_TOKEN", and a coverage-gate step
    - skills.yml exports the composed DSNs (AURA_DB_URL / AURA_DB_MIGRATE_URL), not just POSTGRES_* primitives
    - skills.yml runs the FuzzSkillValidator 60s smoke (SC#3) and the go-mutesting spot-check on validator.go + writer.go
    - docs/aura-quality-snapshot.md has a Phase 11 section with coverage + mutation + SC#1-SC#5 status rows
    - the messages[0] cache-invariant job (cache_invariant_audit.sh) is wired into a CI job (existing or skills.yml)
  </acceptance_criteria>
  <done>skills.yml gates unit + db_integration + sandbox_integration + fuzz + mutation + ≥85% coverage with composed DSNs + the sandbox token + CI=true no-skip-as-green; the quality snapshot has Phase-11 rows (live E2E marked operator-run).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate-3 human-verify — live xlsx North-Star E2E sign-off</name>
  <action>Operator brings the stack up and runs the live cot_eval xlsx North-Star E2E, visually verifies the produced .xlsx contains today'''s data, confirms judge >=90% + the catalog->ask_user->install->sandbox_exec sequence + SC#2 purge-denied, then records the numbers into docs/aura-quality-snapshot.md. Exact steps in how-to-verify below.</action>
  <what-built>
    The full Phase-11 skills system: the ONE skill tool (read/write/install/catalog/snippet actions), native-clone installer, append-only audit (Pitfall #6), messages[1] always-block, sandbox bearer token + ro /skills mount, snippet by-path exec, and the cron TTL sweep. CI gates all tiers; the cot_eval xlsx North-Star scenario is wired but the LIVE run is operator-gated (OPENROUTER_API_KEY, NOT CI).
  </what-built>
  <how-to-verify>
    Run the live xlsx North-Star E2E from the host (the Gate-3 sign-off, D-35):
    1. Bring the stack up: `make sandbox-up` (sandbox-agent with --token + the ro /skills mount) + Postgres migrated through 0010. Export OPENROUTER_API_KEY + AURA_EVAL_SELF_* + AURA_SANDBOX_AGENT_TOKEN + the composed DSNs + SEARXNG_URL (socat bridge per reference_live_finalize_e2e_socat_bridge if needed).
    2. `go test -tags cot_eval -run TestSkillsE2E ./internal/eval/` — watch the agent autonomously: discover anthropics/skills/xlsx on skills.sh → pause for ask_user install approval → install + validate → use the skill → pull today's Yahoo Finance data via web tools → produce the .xlsx.
    3. OPEN the produced .xlsx visually (not just PASS status, per feedback_inspect_artifact_visually_not_just_pass_status): confirm it opens in a spreadsheet app, contains TODAY's market data, no mojibake, sensible structure.
    4. Confirm the judge rubric ≥90% and the tool_use sequence (catalog→ask_user→install→sandbox_exec) in the log.
    5. Confirm SC#2 manually: `aura skills audit purge` as the aura_app role → permission denied.
    Record the judge %, coverage, and mutation numbers into docs/aura-quality-snapshot.md (replace the TBD placeholders).
  </how-to-verify>
  <resume-signal>Type "approved" once the .xlsx is visually verified with today's data + judge ≥90% + SC#2 purge-denied, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live model → full skill loop | the E2E exercises every cross-boundary surface (catalog HTTP, install supply-chain, sandbox exec, audit) end-to-end |
| CI runner → the stack | CI exports the exact env the tiers read; a missing var must FAIL (no-skip-as-green), not silently pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08-R1 | Repudiation | a falsely-green CI job (skip-as-green) hides a broken tier | mitigate | CI=true t.Fatal-on-unset in skip-helpers; composed DSNs exported; a sub-second integration runtime is a skip tell — Task 2 (verify real execution) |
| T-11-08-T1 | Tampering | the E2E asserts on the reply, not the artifact (false confidence) | mitigate | artifact-not-reply: the .xlsx is opened + content-verified; the tool_use sequence is asserted (D-35, feedback_probe_must_verify_artifact_not_reply) — Task 1 + checkpoint |
| T-11-08-I1 | Information Disclosure | the live E2E leaks OPENROUTER_API_KEY into CI | mitigate | the cot_eval tier is OPERATOR-run, OPENROUTER-gated, explicitly NOT CI (D-35) — Tasks 1+2 |
| T-11-08-D1 | Denial of Service | regression in a future phase silently breaks skills | mitigate | the CI gate (all tiers + coverage + mutation + cache-invariant) catches regressions — Task 2 |

No npm/pip/cargo install by Aura's Go code in this plan. The xlsx skill's Python deps run inside the sandbox image (baked, Phase-5), not in Aura's module.
</threat_model>

<verification>
- `go build -tags cot_eval ./internal/eval/...` compiles; the 2 smokes + cache-invariant pass in CI
- skills.yml gates unit + db_integration + sandbox_integration + fuzz + mutation + ≥85% coverage, no-skip-as-green
- the LIVE xlsx E2E (operator-run) produces a verified .xlsx with today's data + judge ≥90% + the asserted tool_use sequence (the human-verify checkpoint)
- SC#1-SC#5 all demonstrably TRUE; quality snapshot stamped
</verification>

<success_criteria>
- The xlsx North-Star E2E proves CAP-07 + CAP-08 end-to-end with ground-truth artifact + sequence assertions + ≥90% judge (operator-run)
- The 2 smaller smokes (haiku + snippet) pass; cache-invariant extended for messages[1]
- CI gates all tiers no-skip-as-green at ≥85% coverage + ≥70% mutation on validator.go/writer.go
- Quality numbers recorded; the human Gate-3 sign-off closes CAP-07/CAP-08
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-08-SUMMARY.md` when done
</output>
