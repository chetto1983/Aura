---
phase: 11-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CAP-07, CAP-08]
must_haves:
  truths:
    - "PRD §Slice 7 no longer references the dead sandbox.Runner.Execute seam"
    - "PRD migration numbers say 0010+ (floor 0009), not 0007/0012"
    - "PRD tool surface is ONE skill tool with an action enum, no dotted skill.* names"
    - "PRD describes messages[1] always-block + manifest-in-Description, not skills-in-system-prompt"
    - "ROADMAP SC#1 says native git clone (not npx --ignore-scripts); SC#5 says catalog default-ON"
  artifacts:
    - path: "prd.md"
      provides: "Amended Slice 7 truth-source (D-33 package)"
      contains: "skill"
    - path: ".planning/ROADMAP.md"
      provides: "Re-specced Phase 11 success criteria (SC#1 native clone, SC#5 default-ON)"
      contains: "Phase 11: Skills"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CAP-07 wording aligned to native-clone install + browse-default-ON"
      contains: "CAP-07"
  key_links:
    - from: "prd.md §Slice 7"
      to: "internal/skills (greenfield) + sandboxagent.Client"
      via: "amendment cross-references the shipped sandbox-agent HTTP seam"
      pattern: "sandbox.agent|sandboxagent|:2468"
---

<objective>
Wave-0 doc-only PRD-amendment package (D-32/D-33) — the FIFTH such gate after 05-01/08-01/09-01/10-01. The PRD §Slice 7 is STALE in 4 scout-confirmed places and the ROADMAP/REQUIREMENTS carry stale acceptance wording. This plan amends the truth-source FIRST, before any Phase 11 code, per the PRD-first absolute rule (CLAUDE.md).

Purpose: Eliminate the contradiction surface so every downstream executor reads a PRD that matches the shipped seams and the locked D-01..D-38 decisions. No Go code, no migrations, no compose edits — documentation only.
Output: Amended prd.md §Slice 7 + §Slice 7e-core + §Caps & Limits env catalog; re-specced ROADMAP Phase 11 (SC#1 + SC#5); aligned REQUIREMENTS CAP-07/CAP-08 wording. One atomic commit.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend prd.md §Slice 7 + §Slice 7e-core + env catalog (D-33 package)</name>
  <read_first>
    - prd.md §"Slice 7 — Skills" (lines ~2112-2330) and §"Slice 7e-core" (lines ~2331-2571) — the stale truth-source
    - prd.md §"Caps & Limits" env catalog section — where the D-34 vars land
    - .planning/phases/11-skills/11-CONTEXT.md §decisions (D-01..D-38) + §domain ("STALE in 4 places") — the exact amendment list
    - .planning/phases/11-skills/11-RESEARCH.md §"State of the Art" table — the old→new mapping per item
  </read_first>
  <action>
    Edit prd.md to apply the D-33 amendment package. Each amendment MUST cite its decision ID (D-NN) inline so the decision-coverage gate can trace it. Apply exactly these supersessions:
    1. Sandbox seam (D-15b/amendment #44): replace every `sandbox.Runner.Execute(language, code, args_stdin, SessionID)` reference with the shipped `tools.SandboxExec` → `internal/sandboxagent.Client` HTTP `:2468` by-path-exec seam. Snippets execute by interpreter+path (`python3 /skills/<name>.py`), never injected code, never the exec bit (D-04/D-17).
    2. Migration numbers (D-32): PRD `0007_skill_audit` / `0012_snippet_runs` → floor is `0009_scheduler`; skills land at `0010+` (one 0010_skill_audit, optional 0011_snippet_runs).
    3. Tool names (D-01): the dotted `skill.list`/`skill.create`/`skill.approve` etc. → ONE `skill` tool with an `action` enum (`list|info|use|create|update|delete|install|catalog|restore|archive`). Drop `skill.approve` entirely (D-03, no model-facing approve). Drop a bespoke `run` action (D-04). Note the OpenAI-wire `^[a-zA-Z0-9_-]+$` constraint (DeepSeek rejects dotted names + root oneOf/enum).
    4. Prompt injection (D-06/D-07): "skills enter the system prompt" → manifest lives in the `skill` tool's Description (turn-stable, BM25 overflow past AURA_SKILL_MANIFEST_CAP_BYTES); `always:true` bodies render into ONE user-role block at `messages[1]`; `messages[0]` gets exactly ONE frozen mechanism-not-enumeration sentence (CAP-04 byte-stable invariant preserved).
    5. TTL sweep (D-16): `ttl_sweeper.go` goroutine → cron `skill_ttl_sweep` TaskKind seeded daily (Phase-10 machinery).
    6. Catalog (D-11/D-12/D-14/D-15): HTML scrape (`catalogItemRE`) → skills.sh `/api/search` JSON lax-decode client; amendment #14 FLIPPED → catalog browse default-ON (read-only JSON), `aura skills disable-catalog` is the default-deny escape hatch; install posture = native Go `git clone --depth 1 --single-branch -c core.autocrlf=false` (NOT npx; `--ignore-scripts` was a no-op anyway).
    7. Metadata consolidation (D-19): drop the `skill_audit` ALTER columns (last_used_at/use_count); usage/archive state lives in a per-skill sidecar JSON + optional `snippet_runs` forensics table.
    8. Audit matrix (D-29): replace the three-way equivalence with the 5-row coherence matrix (approval_source/paused_state_token/gate_recommended/gate_taken) from CONTEXT D-29; keep the Pitfall #6 belt-and-suspenders (BEFORE UPDATE/DELETE row trigger + BEFORE TRUNCATE statement trigger + aura_app role separation).
    9. Manifest-packing acceptance (D-09): "ALL skills listed even 100+" → cap + "N more — search with skill action=list {query}" BM25 overflow.
    10. Dep strategy (D-36): add the trichotomy (build-time bake / on-demand uv / hybrid) and the CORRECTED framing — NOT a forced bake; the sandbox already has pip + egress on the dev stack; the xlsx North-Star needs openpyxl/defusedxml/lxml/validators to resolve whichever model ships.
    11. Egress posture (D-37): document the explicit egress decision — needs_network:true routes through the allowlisted host forward-proxy at RISKY tier (advisory on Docker Desktop, enforced on native-Linux); cross-reference the Phase-8 proxy-restoration dependency.
    12. Sandbox token + hardening floor (D-38): document the AURA_SANDBOX_AGENT_TOKEN bearer wiring obligation (today --no-token) and the portable hardening floor (token + tightened seccomp + no-new-privileges + read-only rootfs + egress allowlist); cross-reference the gVisor `runsc` overlay as a Phase-8 sandbox-wide regression (NOT owned by Phase 11).
    In §"Caps & Limits", add the 8 D-34 env vars with defaults: AURA_SKILLS_DIR (~/.aura/skills), AURA_SKILL_BODY_CAP_BYTES (32768), AURA_SKILL_INJECTION_BLOCKLIST (builtin list), AURA_SKILL_MANIFEST_CAP_BYTES (8192), AURA_SKILL_SNIPPET_TTL_DAYS (90), AURA_SKILL_EXPORT_DIR (~/.aura/skills/export), AURA_SKILL_CATALOG_URL (https://www.skills.sh), AURA_SKILL_INSTALL_TIMEOUT_SEC (90) + AURA_SANDBOX_AGENT_TOKEN (gen at first boot, mirror AURA_SETUP_TOKEN). Follow the AURA_<DOMAIN>_<UNIT> convention.
  </action>
  <verify>
    <automated>grep -n "0010" prd.md | grep -iv "^#" | head; grep -c "AURA_SKILL_" prd.md</automated>
  </verify>
  <acceptance_criteria>
    - prd.md contains "skill action=list" (manifest overflow wording, D-09)
    - prd.md contains "messages[1]" and "messages[0]" (D-07 prompt design)
    - prd.md does NOT contain "ttl_sweeper.go" (D-16 amended away)
    - prd.md does NOT contain "skill.approve" (D-03 cut)
    - prd.md contains "AURA_SANDBOX_AGENT_TOKEN" and all 8 "AURA_SKILL_*" vars from D-34
    - prd.md contains "/api/search" (D-11 catalog transport) and "core.autocrlf=false" (D-15 native clone)
    - prd.md contains "paused_state_token" AND "gate_taken" (the D-29 5-row coherence matrix landed post-amendment, not just cited)
    - prd.md §Slice 7 cites the decision IDs D-01, D-06, D-07, D-11, D-15, D-16, D-29, D-36, D-37, D-38 inline
  </acceptance_criteria>
  <done>prd.md §Slice 7/7e-core + env catalog reflect all 12 D-33 supersessions with D-NN citations; no stale references remain (grep-clean for ttl_sweeper.go, skill.approve, sandbox.Runner.Execute).</done>
</task>

<task type="auto">
  <name>Task 2: Re-spec ROADMAP Phase 11 + align REQUIREMENTS CAP-07/CAP-08</name>
  <read_first>
    - .planning/ROADMAP.md §"Phase 11: Skills" (lines ~340-354) — the Goal + 5 success criteria to re-spec
    - .planning/REQUIREMENTS.md CAP-07 (line 38) + CAP-08 (line 39) — the stale wording
    - .planning/phases/11-skills/11-CONTEXT.md D-12 (catalog default-ON, SC#5 re-spec) + D-14/D-15 (native clone, SC#1)
  </read_first>
  <action>
    Edit .planning/ROADMAP.md §"Phase 11: Skills":
    - SC#1: change "invokes `npx skills add --ignore-scripts`" → "invokes a native Go `git clone --depth 1 --single-branch` (D-14/D-15, node dep dropped)" and keep "persists an `aura.skill_audit` INSERT row".
    - SC#5: change the current "catalog disabled — run `aura skills enable-catalog` to opt in (Amendment #14)" → "catalog browse is default-ON; `aura skills catalog list` returns skills.sh `/api/search` JSON results out of the box; `aura skills disable-catalog` is the default-deny escape hatch (D-12, amendment #14 FLIPPED)".
    - Update the **Goal** line to reference the shipped sandbox-agent seam and the ONE `skill` tool (drop "7d installer (`npx skills add --ignore-scripts`...)" → native clone; mention messages[1] + manifest-in-Description + cron TaskKind TTL sweep). Keep CAP-07/CAP-08 + Slices 7a-7e listing.
    Edit .planning/REQUIREMENTS.md:
    - CAP-07: replace "`skill.catalog` HTML scrape hidden behind `aura skills enable-catalog` opt-in" with "catalog browse default-ON via skills.sh `/api/search` JSON (D-12); install via native Go clone (D-15)". Keep "Audit-immutable Postgres trigger BEFORE UPDATE/DELETE/TRUNCATE + role separation" and "Unicode NFKC validator (10K fuzz)".
    - CAP-08: REMOVE the pre-pivot "Reusa sandbox 2b session-bound" wording (superseded by amendment #44/D-15b — the bespoke sandbox 2b was killed; the shipped seam is the rivetdev/sandbox-agent HTTP `:2468` `tools.SandboxExec` by-path-exec, D-04). Replace it with "Reusa il sandbox-agent HTTP `:2468` seam (`tools.SandboxExec`, by-path exec — amendment #44/D-15b)"; add "(D-16 TTL via cron skill_ttl_sweep TaskKind)" to the TTL-archived clause.
    Cite D-12, D-14, D-15, D-16 inline in the edited lines.
  </action>
  <verify>
    <automated>grep -n "default-ON\|disable-catalog\|core.autocrlf\|native" .planning/ROADMAP.md | head; grep -n "D-12\|D-15\|D-16" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md §"Phase 11: Skills" SC#1 contains "git clone" and does NOT contain "npx skills add --ignore-scripts"
    - .planning/ROADMAP.md §"Phase 11: Skills" SC#5 contains "default-ON" and "disable-catalog"
    - .planning/REQUIREMENTS.md CAP-07 does NOT contain "enable-catalog", does NOT contain "HTML scrape" (D-11 kill), and contains "default-ON" + a D-NN citation
    - .planning/REQUIREMENTS.md CAP-08 does NOT contain "session-bound" (pre-pivot wording superseded by amendment #44/D-15b), contains a `:2468` / `SandboxExec` / sandbox-agent seam reference, and contains "skill_ttl_sweep" or "D-16"
  </acceptance_criteria>
  <done>ROADMAP SC#1/SC#5 + Goal and REQUIREMENTS CAP-07/CAP-08 match the locked D-12/D-14/D-15/D-16 decisions; no "npx" / "enable-catalog" stale wording remains in the Phase 11 entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation only | This plan touches no runtime surface; trust boundaries are established by downstream code plans, not here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-R | Repudiation | prd.md amendment integrity | accept | One atomic commit with explicit body explaining each D-NN supersession; git history is the audit trail (CLAUDE.md commit discipline) |
| T-11-01-T | Tampering | stale-doc contradiction surface | mitigate | This plan IS the mitigation — it removes the 4 staleness classes that would otherwise let an executor implement the dead seam; downstream plans reference the amended PRD |

No npm/pip/cargo installs in this plan (doc-only) — no T-{phase}-SC supply-chain checkpoint needed.
</threat_model>

<verification>
- `git diff --stat` shows ONLY prd.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md modified (no code files)
- grep-clean for the 4 staleness classes: no `sandbox.Runner.Execute`, no `ttl_sweeper.go`, no `skill.approve`, no `npx skills add --ignore-scripts` in the Phase 11 / Slice 7 regions
- All 8 AURA_SKILL_* vars + AURA_SANDBOX_AGENT_TOKEN present in prd.md
</verification>

<success_criteria>
- prd.md §Slice 7 + §Slice 7e-core + env catalog reflect all 12 D-33 supersessions, each citing its D-NN
- ROADMAP Phase 11 SC#1 (native clone) + SC#5 (default-ON) re-specced
- REQUIREMENTS CAP-07/CAP-08 aligned
- One atomic doc-only commit (no Go code, no migrations, no compose edits)
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-01-SUMMARY.md` when done
</output>
