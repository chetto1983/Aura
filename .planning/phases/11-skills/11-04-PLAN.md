---
phase: 11-skills
plan: 04
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - internal/db/migrations/0010_skill_audit.up.sql
  - internal/db/migrations/0010_skill_audit.down.sql
  - internal/db/queries/skill_audit.sql
  - internal/skills/audit_store.go
  - internal/skills/audit_store_integration_test.go
  - internal/skills/writer.go
  - internal/skills/writer_test.go
  - internal/skills/materialize.go
  - internal/skills/materialize_test.go
autonomous: true
requirements: [CAP-07]
must_haves:
  truths:
    - "aura.skill_audit is append-only: aura_app has SELECT/INSERT only; UPDATE/DELETE raise via a row trigger; TRUNCATE raises via a separate statement trigger (Pitfall #6)"
    - "The D-29 audit-coherence CHECK enforces approval_source/paused_state_token/gate_recommended/gate_taken consistency"
    - "The 0009 scheduler_tasks.kind CHECK is ALTERed to add skill_ttl_sweep (A2 landmine)"
    - "Writer persists pending→active atomically with an audit INSERT in one db.WithTx; create/update→Risky, delete→Destructive gate via scoring.ComputeSkillTier"
    - "Materialize writes only active skills to the export dir, stripping symlinks; archive/delete de-materializes"
  artifacts:
    - path: "internal/db/migrations/0010_skill_audit.up.sql"
      provides: "skill_audit table + triggers + role grants + D-29 CHECK + kind-CHECK ALTER"
      contains: "BEFORE TRUNCATE"
    - path: "internal/skills/audit_store.go"
      provides: "append-only audit store (canonical identity/askuser pattern)"
      min_lines: 40
    - path: "internal/skills/writer.go"
      provides: "pending→active atomic + audit INSERT + scoring gate (D-29)"
      min_lines: 40
    - path: "internal/skills/materialize.go"
      provides: "activation→export dir, symlink-strip, de-materialize (D-17)"
      min_lines: 30
  key_links:
    - from: "internal/skills/writer.go"
      to: "aura.skill_audit"
      via: "db.WithTx: pending→active FS move + audit INSERT, all-or-nothing"
      pattern: "WithTx"
    - from: "internal/skills/writer.go"
      to: "internal/scoring.ComputeSkillTier"
      via: "create/update/install→Risky, delete→Destructive gate decision"
      pattern: "ComputeSkillTier|GateRecommended"
    - from: "internal/skills/materialize.go"
      to: "AURA_SKILL_EXPORT_DIR (/skills ro mount source)"
      via: "active-only materialization, Lstat-no-follow symlink strip"
      pattern: "Lstat|Symlink"
---

<objective>
Sub-slice 7c (core) — the write/audit/materialize backbone. Migration **0010_skill_audit** creates the append-only audit table (Pitfall #6 belt-and-suspenders: BEFORE UPDATE/DELETE row trigger + BEFORE TRUNCATE statement trigger + aura_app role separation) with the D-29 coherence CHECK, and ALTERs the 0009 `scheduler_tasks.kind` CHECK to add `skill_ttl_sweep` (the A2 landmine). The audit store copies the canonical identity/askuser pattern (INSERT-only for aura_app). The writer performs pending→active atomic FS writes + an audit INSERT in ONE db.WithTx, gated by `scoring.ComputeSkillTier` (its designated Phase-11 consumer). Materialize writes only active skills to the export dir (the /skills ro-mount source), stripping symlinks (Pitfall 4); archive/delete de-materializes.

Purpose: Provide the durable, auditable, gate-aware write primitive + the materialization seam that 11-05 (governance/write-actions), 11-06 (installer), and 11-07 (snippets) all consume. NO tool wiring, NO ask_user resume, NO messages[1] here — those are 11-05.
Output: migration 0010 up/down + sqlc query + audit_store + writer + materialize + db_integration tests (SC#1 INSERT, SC#2 immutability/TRUNCATE/role-denied, migration round-trip).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
@.planning/phases/11-skills/11-PATTERNS.md
@.claude/skills/golang-database/SKILL.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0010 (append-only audit + D-29 CHECK + kind-CHECK ALTER) + sqlc query + audit store</name>
  <read_first>
    - internal/db/migrations/0009_scheduler.up.sql (full) — role-grant precedent (lines ~57-63), plain-non-CONCURRENT index on a fresh table (48-52), and the `scheduler_tasks.kind` CHECK at line ~16 (`IN ('reminder','agent_job','backup_postgres','backup_neo4j')`) to ALTER
    - internal/skills/audit_store.go target analog: internal/askuser/store.go (full) + internal/identity/store.go (lines ~45-57 Store shape, 180-185 SQLSTATE classification via errors.As+pgErr.Code, 39-43 sentinel errors)
    - .planning/phases/11-skills/11-CONTEXT.md D-29 (the exact 5-row coherence matrix — the CHECK SQL source) + D-19 (NO last_used_at/use_count ALTER — sidecar instead)
    - .planning/phases/11-skills/11-RESEARCH.md §"Pattern 3" (append-only + role sep SQL) + §Pitfall 1 (TRUNCATE bypasses row triggers) + §Pitfall 6 (golang-migrate forbids CONCURRENTLY in tx)
    - internal/db/queries/ (any existing *.sql) — sqlc convention; the sqlc.yaml config for query→Go generation
  </read_first>
  <action>
    Create `internal/db/migrations/0010_skill_audit.up.sql`:
    - `CREATE TABLE aura.skill_audit` with columns: id (uuid pk, default gen), created_at (timestamptz default now()), actor_id/identity_id (text/uuid — identity in audit rows only per D-05), skill_name (text), action (text), content_hash (text), approval_source (text NULL — `ask_user`|`cli`|`auto`|NULL), paused_state_token (text NULL), gate_recommended (bool), gate_taken (bool), blocklist_override (bool default false). Add any columns the D-29 matrix references.
    - The D-29 coherence CHECK constraint over (approval_source, paused_state_token, gate_recommended, gate_taken) encoding the 5 matrix rows EXACTLY: (NULL,NULL,true,false) pending; (`ask_user`,NOT NULL,true,true) approve/reject; (`cli`,NULL,true,true) operator; (`auto`,NULL,false,true) system. Write the CHECK as a disjunction of these allowed tuples.
    - A plain (non-CONCURRENT) index on (skill_name) and/or (created_at) for the `aura skills audit --since` query (fresh empty table, Pitfall 6 — no CONCURRENTLY).
    - `CREATE FUNCTION aura.reject_audit_mutation() RETURNS trigger` raising an exception; a `BEFORE UPDATE OR DELETE ... FOR EACH ROW` trigger AND a SEPARATE `BEFORE TRUNCATE ... FOR EACH STATEMENT` trigger (Pitfall 1 — TRUNCATE bypasses row triggers).
    - Role grants: `GRANT SELECT, INSERT ON aura.skill_audit TO aura_app;` (NO UPDATE/DELETE/TRUNCATE) + `GRANT ALL ON aura.skill_audit TO aura_migrate;` (mirror 0009).
    - `ALTER TABLE aura.scheduler_tasks DROP CONSTRAINT <the kind check name>, ADD CONSTRAINT ... CHECK (kind IN ('reminder','agent_job','backup_postgres','backup_neo4j','skill_ttl_sweep'))` — the A2 landmine; read 0009 to get the exact existing constraint name.
    Create `internal/db/migrations/0010_skill_audit.down.sql`: drop triggers/function/table + restore the original kind CHECK.
    Create `internal/db/queries/skill_audit.sql`: sqlc queries — `InsertSkillAudit` (all D-29 columns), `ListSkillAudit` (by tier/since filters for the CLI), and any read the writer needs. Run sqlc generate (mirror the project's generate command/Makefile target).
    Create `internal/skills/audit_store.go`: `Store{pool, q}` via sqlc.New(pool) (identity 04-02 canonical shape); INSERT-only for aura_app; SQLSTATE classification via errors.As(&pgErr)+pgErr.Code (NEVER message-match); sentinel errors. `InsertAudit(ctx, AuditRow)` and `List(ctx, filter)`.
    Create `internal/skills/audit_store_integration_test.go` (build tag `db_integration`, goleak, envOrSkip t.Fatal-under-CI): SC#1 INSERT round-trip; SC#2 immutability — UPDATE and DELETE as aura_app raise (trigger), TRUNCATE as aura_app raises (statement trigger) AND aura_app lacks the grant; the D-29 CHECK rejects an incoherent tuple; `TestMigration0010_SchemaRoundTrip` applies up then down clean. Compose DSNs from POSTGRES_* per CLAUDE.md.
  </action>
  <verify>
    <automated>go vet ./internal/skills/... ./internal/db/... && go build ./... && go test -tags db_integration -run 'TestAuditImmutable|TestInstallAuditRow|TestMigration0010' ./internal/skills/</automated>
  </verify>
  <acceptance_criteria>
    - internal/db/migrations/0010_skill_audit.up.sql contains "BEFORE TRUNCATE" and "FOR EACH STATEMENT" and "GRANT SELECT, INSERT ON aura.skill_audit TO aura_app"
    - 0010_skill_audit.up.sql ALTERs scheduler_tasks kind CHECK to include 'skill_ttl_sweep'
    - 0010_skill_audit.up.sql does NOT contain "CONCURRENTLY" (Pitfall 6)
    - audit_store.go classifies errors via "errors.As" + a pgErr.Code comparison (no message-match)
    - `go test -tags db_integration -run TestAuditImmutable ./internal/skills/` passes (UPDATE/DELETE/TRUNCATE as aura_app all denied) and actually RUNS (sub-second is a skip tell — verify execution under CI env)
    - `go test -tags db_integration -run TestMigration0010_SchemaRoundTrip ./internal/skills/` applies up+down clean
  </acceptance_criteria>
  <done>Migration 0010 ships the append-only audit table (two triggers + role sep + D-29 CHECK + kind-CHECK ALTER); audit store INSERTs/reads via the canonical pattern; db_integration tests prove SC#1 + SC#2 + round-trip + CHECK coherence.</done>
</task>

<task type="auto">
  <name>Task 2: Writer (pending→active atomic + audit INSERT + scoring gate) + materialize (active-only, symlink-strip, de-materialize)</name>
  <read_first>
    - internal/agent/tools/task.go (lines ~187-220) — the GateRecommended→pending_approval flow the writer copies (status decision + immediate-alert threshold)
    - internal/scoring/scoring.go (lines ~48-57 SkillAction constants, ~160-177 ComputeSkillTier/GateRecommended/RequiresImmediateAlert) — pass the threshold as an arg, never read env (scoring is pure)
    - internal/db/db.go WithTx — the single atomic-write seam (read its signature/usage)
    - internal/conversations/orphan_scan.go (lines ~74-88) — the Lstat-no-follow symlink-strip idiom for materialize
    - internal/skills/audit_store.go (from Task 1) — the InsertAudit the writer calls inside the tx
    - internal/cron/handlers/backup.go (backupDir, atomic FS write idiom) — the atomic pending→active rename pattern reference
    - .planning/phases/11-skills/11-CONTEXT.md D-17 (materialize active-only; pending/archived never materialize; archive/delete de-materializes — mount in lockstep with loader state) + D-29 (audit columns) + D-23 (content_hash in every audit row is the recovery path)
  </read_first>
  <action>
    Create `internal/skills/writer.go`: a `Writer` holding the audit store + loader roots + materializer.
    - `WriteMutation(ctx, action SkillAction, name string, fm Frontmatter, body string, actor AuditActor) (status string, err error)`:
      1. compute `tier := scoring.ComputeSkillTier(action, body)` and `gate := scoring.GateRecommended(tier)` (create/update/install→Risky, delete→Destructive).
      2. compute the content_hash (Aura's canonical byte-sorted (relPath,bytes) sha256 — share the helper with the installer; for a single-file skill it's sha256 of name+body).
      3. write the skill into `pending/<name>/` on disk atomically (write to a temp dir then rename); if `!gate` (never happens for create/update/delete/install — all gate — but keep the branch for completeness/auto paths), promote to active + materialize.
      4. INSERT the audit row inside `db.WithTx` with the D-29 tuple for the "written to pending" row: approval_source=NULL, paused_state_token=NULL, gate_recommended=true, gate_taken=false (the pending matrix row). content_hash + blocklist_override recorded. The FS pending-write happens BEFORE the tx (orphan reconciled by boot scan, mirror the conversations sidecar-spill-before-tx pattern); the tx is the audit INSERT (+ any active-promotion bookkeeping) — all-or-nothing.
      Return status `pending_approval` when gate (the model/CLI surfaces "awaiting approval").
    - `Activate(ctx, name string, src ApprovalSource, pausedToken string) error`: the resume/CLI activation half — move pending→active, materialize, INSERT the D-29 approved row (`ask_user`+NOT NULL token+true+true, or `cli`+NULL+true+true). (The ask_user resume HANDLER that calls this is 11-05; the writer exposes the method here.)
    - `Archive(ctx, name)`/`Delete(ctx, name)`: de-materialize (remove from export dir) + move to archived/ or remove + audit. Delete is Destructive-tiered.
    Create `internal/skills/materialize.go`: `Materialize(name string, skillDir, exportDir string) error` copies an ACTIVE skill's files into AURA_SKILL_EXPORT_DIR (the /skills ro-mount source), STRIPPING symlinks via Lstat-no-follow (Pitfall 4 — orphan_scan idiom). `Dematerialize(name, exportDir)` removes them. Materialize ONLY active skills (pending/archived never materialize, D-17) — the mount tracks loader state in lockstep.
    Create `writer_test.go` (unit: gate decision, status, pending-write, content_hash) + `materialize_test.go` (unit + the TestMaterializeStripsSymlink case: a skill dir with a symlink does not survive materialization). Race-clean. Keep all files ≤600 LOC (split writer into writer.go + writer_activate.go if needed).
  </action>
  <verify>
    <automated>go vet ./internal/skills/... && go build ./... && go test -race -run 'TestWriter|TestMaterialize' ./internal/skills/</automated>
  </verify>
  <acceptance_criteria>
    - internal/skills/writer.go calls scoring.ComputeSkillTier and scoring.GateRecommended (not a hand-rolled tier)
    - writer.go wraps the audit INSERT in db.WithTx (atomic)
    - writer_test.go asserts create/update→pending_approval (Risky gate) and the pending audit row uses the (NULL,NULL,true,false) D-29 tuple
    - materialize.go contains "Lstat" and materialize_test.go's TestMaterializeStripsSymlink asserts a symlink does NOT appear in the export dir
    - writer.go records content_hash on every audit row (D-23 recovery path)
    - every modified file ≤600 LOC (`find internal/skills -name '*.go' | xargs wc -l` shows none >600)
  </acceptance_criteria>
  <done>Writer gates via ComputeSkillTier, writes pending→active atomically with the D-29 audit INSERT in db.WithTx, exposes Activate/Archive/Delete; materialize is active-only + symlink-stripped; unit tests + symlink-strip test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer → aura.skill_audit | every mutation crosses into the append-only audit table (Repudiation defense) |
| writer → AURA_SKILL_EXPORT_DIR (/skills ro-mount source) | materialized files become visible in the sandbox; symlinks must not escape |
| aura_app DB role → audit table | the role boundary enforces append-only at the database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-R1 | Repudiation | audit-log tampering / wipe (UPDATE/DELETE/TRUNCATE) | mitigate | BEFORE UPDATE/DELETE row trigger + BEFORE TRUNCATE statement trigger + aura_app no-UPDATE/DELETE/TRUNCATE grant (Pitfall #6 belt-and-suspenders) — Task 1 (HIGH severity, mitigated + SC#2 tested) |
| T-11-04-T1 | Tampering | symlink escape from a skill dir via the /skills export materialization | mitigate | Lstat-no-follow symlink strip in materialize.go (Pitfall 4) — Task 2 (HIGH severity, mitigated + TestMaterializeStripsSymlink) |
| T-11-04-E1 | Elevation of Privilege | a mutation self-activates without a gate | mitigate | WriteMutation always lands gate-recommended mutations in pending (gate_taken=false); Activate is a separate method only callable by the resume handler/CLI (11-05/CLI), never by WriteMutation — D-03/D-26 contract set here |
| T-11-04-T2 | Tampering | incoherent audit tuple recorded (e.g. approved with no token) | mitigate | the D-29 CHECK constraint rejects any tuple outside the 5 allowed rows — Task 1, tested |
| T-11-04-I1 | Information Disclosure | a partial write leaves a half-activated skill | mitigate | pending-write-then-tx atomicity (db.WithTx) + boot-scan orphan reconciliation (mirrors conversations) — Task 2 |

No npm/pip/cargo installs in this plan. The kind-CHECK ALTER (A2 landmine) is explicitly in Task 1.
</threat_model>

<verification>
- `go test -tags db_integration -race ./internal/skills/` green: SC#1 INSERT, SC#2 UPDATE/DELETE/TRUNCATE-denied + role-denied, D-29 CHECK coherence, migration round-trip — all actually RUN (no skip-as-green; verify non-trivial runtime)
- `go test -race -run 'TestWriter|TestMaterialize' ./internal/skills/` green incl. TestMaterializeStripsSymlink
- migration 0010 grep-clean: BEFORE TRUNCATE present, CONCURRENTLY absent, skill_ttl_sweep added to the kind CHECK
- golangci-lint 0 on internal/skills + internal/db; all files ≤600 LOC
</verification>

<success_criteria>
- Migration 0010: append-only audit (two triggers + role sep + D-29 CHECK), kind-CHECK widened for skill_ttl_sweep
- Audit store: canonical INSERT-only pattern, SQLSTATE classification
- Writer: scoring-gated pending→active + atomic audit INSERT + content_hash recovery path; Activate/Archive/Delete exposed
- Materialize: active-only, symlink-stripped, de-materialize on archive/delete
- db_integration + unit tiers green; coverage toward the ≥85% floor; mutation spot-check target = writer.go (≥70%, phase gate)
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-04-SUMMARY.md` when done
</output>
