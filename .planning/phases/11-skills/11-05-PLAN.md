---
phase: 11-skills
plan: 05
type: execute
wave: 5
depends_on: ["11-02", "11-04"]
files_modified:
  - internal/agent/tools/skill_write.go
  - internal/agent/tools/skill.go
  - internal/agent/tools/skill_write_test.go
  - internal/skills/messages.go
  - internal/skills/messages_test.go
  - internal/skills/resume.go
  - internal/skills/resume_test.go
  - internal/conversations/context.go
  - internal/conversations/context_test.go
  - cmd/aura/chat.go
  - cmd/aura/skills.go
autonomous: true
requirements: [CAP-07]
must_haves:
  truths:
    - "skill action=create|update|delete validates at the write boundary, gates via scoring, lands in pending/ and pauses via ask_user (no model-facing approve, D-03)"
    - "ask_user resume (human answered) OR `aura skills approve` CLI activates a pending skill via Writer.Activate; rejection discards pending"
    - "always:true bodies render into ONE byte-stable user-role block at messages[1]; messages[0] stays byte-identical (CAP-04)"
    - "The L2.5 evictor protects the messages[1] always-block like the system L0 turn (D-07 flagged constraint, Pitfall 3)"
  artifacts:
    - path: "internal/agent/tools/skill_write.go"
      provides: "create/update/delete actions: validate→gate→pending→ask_user pause (D-02/D-03)"
      min_lines: 40
    - path: "internal/skills/messages.go"
      provides: "messages[1] always-block rendering, byte-stable (D-07)"
      min_lines: 25
    - path: "internal/skills/resume.go"
      provides: "ask_user resume handler = Writer.Activate + audit (D-03)"
      min_lines: 25
    - path: "internal/conversations/context.go"
      provides: "L2.5 evictor protects messages[1] (Pitfall 3)"
      contains: "messages[1]"
  key_links:
    - from: "internal/agent/tools/skill_write.go"
      to: "internal/askuser (pause) + internal/skills.Writer"
      via: "validate→ComputeSkillTier→WriteMutation(pending)→ask_user pause"
      pattern: "ask_user|AwaitingInput|pending"
    - from: "internal/skills/resume.go"
      to: "internal/skills.Writer.Activate"
      via: "human ask_user resume → Activate + audit (ask_user source)"
      pattern: "Activate"
    - from: "internal/conversations/context.go dropOldestPairs"
      to: "messages[1] always-block"
      via: "preserve the always-block like seq=1 system L0 (Pitfall 3)"
      pattern: "Seq == 1|always"
---

<objective>
Sub-slice 7c (governance) — wire the write actions + the human-only approval channel + the messages[1] always-block. The skill tool's create/update/delete actions validate at the write boundary (NFKC+blocklist hard-reject for the model, D-27), gate via scoring, land in pending/, and PAUSE via ask_user (D-02). There is NO model-facing approve (D-03): activation happens only via an ask_user resume (human answered → Writer.Activate) or the `aura skills approve` CLI. The `always:true` bodies render into ONE byte-stable user-role block at messages[1] (D-07) — the seam Phase-14 Agent.md will share — and the L2.5 evictor is taught to protect it like the system L0 turn (Pitfall 3).

Purpose: Complete the model-facing self-extension loop (gated, human-approved) and land the always-on injection point without breaking the CAP-04 byte-stable messages[0] invariant. NO installer, NO snippets here.
Output: skill_write.go (create/update/delete) + resume.go (ask_user activation) + messages.go (messages[1] render) + context.go L2.5 protection + `aura skills` CLI approve/create/delete/audit subcommands + bootChat messages[1] assembly wiring.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
@.planning/phases/11-skills/11-PATTERNS.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: skill write actions (create/update/delete) + ask_user pause + resume activation + aura skills CLI</name>
  <read_first>
    - internal/agent/tools/skill.go (from 11-02) — the reserved create/update/delete/restore/archive router keys to fill; the skillLoader/writer consumer-seam shape
    - internal/agent/tools/task.go (lines ~187-220) — the GateRecommended→pending_approval + immediate-alert message flow to mirror in skill_write
    - internal/agent/tools/ask_user.go + internal/agent/llm_agent_pause.go — how a tool triggers a pause: the *ErrAwaitingUserInput struct sentinel, Actions.AwaitingInput Event field, intra-turn exclusivity (D-A1-07); the resume seam
    - internal/askuser/store.go (lines ~34-38 ActionAccept/Decline/Cancel) — the resume action constants the resume handler keys off
    - internal/skills/writer.go (from 11-04) — WriteMutation + Activate + Archive + Delete signatures
    - internal/skills/validator.go (from 11-03) — ValidateForWrite (model-authored: allowBlocklisted=false, hard-reject)
    - cmd/aura/identity.go OR cmd/aura/task.go — the hand-rolled switch CLI pattern (runIdentity/runTask precedent — no cobra, CLAUDE.md follows existing patterns)
    - .planning/phases/11-skills/11-CONTEXT.md D-02 (full mutation set, gated) + D-03 (no model approve; ask_user resume OR CLI) + D-26 (headless mutation → pending + IMMEDIATE Notifier alert + gate_taken=false, never self-activates) + D-29 (audit tuples)
  </read_first>
  <action>
    Create `internal/agent/tools/skill_write.go`: the `actionCreate`/`actionUpdate`/`actionDelete` handlers (registered on the skill tool's ActionRouter, replacing the "not yet wired" placeholders from 11-02). Each:
    - parses its action-specific args (name, frontmatter, body for create/update; name for delete).
    - calls the writer seam's validate (model-authored → ValidateForWrite with allowBlocklisted=false; a blocklist hit is a HARD reject returned as a tool error so the model self-corrects — D-27, no escape).
    - calls WriteMutation → lands in pending/ + audit (gate_taken=false). Then triggers an ask_user pause (the *ErrAwaitingUserInput sentinel path) framing the approval question + showing tier + (for update) the unified diff (D-05). Update flow: pending while old version serves; gate shows the diff; reject discards pending.
    - if the immediate-alert threshold is met (RequiresImmediateAlert), note it (mirror task.go). In a HEADLESS context (no interactive resume) the mutation stays pending + an IMMEDIATE Notifier alert fires + audit gate_taken=false — it can NEVER self-activate (D-26). Reuse the Phase-10 composite Notifier + quiet-hours AS-IS.
    Keep the tools package free of internal/skills import — extend the consumer-declared writer seam interface in the tools package; the live writer is wired by the cmd adapter.
    Create `internal/skills/resume.go`: the D-03 ask_user resume handler — on an ActionAccept resume for a skill-approval pause, call `Writer.Activate(ctx, name, ApprovalSourceAskUser, pausedToken)` (audit row `ask_user`+NOT NULL token+true+true); on ActionDecline/Cancel, discard the pending skill + audit the rejection (`ask_user` reject row). This is the activation channel — NO model-facing approve action exists.
    Create `cmd/aura/skills.go`: a hand-rolled `runSkills` switch (mirror runIdentity/runTask — NO cobra) with subcommands: `list`, `info`, `create`, `update`, `delete`, `approve <name>` (operator activation → Writer.Activate with ApprovalSourceCLI, `cli`+NULL+true+true audit), `audit [--tier= --since=]` (lists skill_audit rows via the audit store; SC#2 `aura skills audit purge` as aura_app → permission denied surfaces the role error). Register `runSkills` in cmd/aura/main.go's command switch.
    Create `skill_write_test.go` (unit, fake writer/askuser): create→pending+pause assertion; a model-authored body with a blocklist token → hard reject (tool error, NOT a pause); delete→Destructive tier; the resume handler activates on accept and discards on decline. Create `resume_test.go`. Race-clean. All files ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./internal/agent/tools/... ./internal/skills/</automated>
  </verify>
  <acceptance_criteria>
    - internal/agent/tools/skill_write.go calls ValidateForWrite with the model path passing allowBlocklisted=false (grep: no model-facing override)
    - internal/agent/tools/ has NO `approve` action key in the skill router (`grep -c '"approve"' internal/agent/tools/skill*.go` returns 0) — D-03
    - skill_write_test.go asserts a blocklisted model-authored body returns a tool error (self-correct), NOT an ask_user pause
    - internal/skills/resume.go calls Writer.Activate with an ask_user approval source on accept and discards pending on decline
    - cmd/aura/skills.go contains a `case "approve"` and a `case "audit"`; `runSkills` is registered in main.go
    - `go list -deps ./internal/agent/tools | grep -c internal/skills` returns 0 (boundary held)
  </acceptance_criteria>
  <done>create/update/delete validate→gate→pending→ask_user-pause; model can never self-approve (D-03); ask_user resume + `aura skills approve` CLI activate; headless mutations alert + never self-activate (D-26); all green + race-clean.</done>
</task>

<task type="auto">
  <name>Task 2: messages[1] always-block render + bootChat assembly + L2.5 evictor protection (Pitfall 3)</name>
  <read_first>
    - internal/agent/prompt.go (lines ~14-20) — the byte-stable SystemPrompt (messages[0]); confirm the 11-02 mechanism sentence is the only edit
    - internal/conversations/context.go (lines ~146-165 applyL1 seq=1 skip, ~181-201 dropOldestPairs seq=1 system-L0 preservation) — the EXACT idiom to extend for messages[1] protection
    - internal/skills/loader.go (from 11-02) — List()/the always:true filter source
    - cmd/aura/chat.go bootChat — the composition root where loader init + messages[1] assembly lands (Runner-side); how messages are currently assembled into the turn list
    - .planning/phases/11-skills/11-CONTEXT.md D-07 (always:true → ONE user-role block at messages[1]; the Phase-14-shared seam; the FLAGGED L2.5 constraint) + D-10 (always governance; per-skill 32KiB cap; aura skills list shows the always tier byte size)
    - .planning/phases/11-skills/11-RESEARCH.md §Pitfall 3 (messages[1] silently evicted) + §"Pattern" for context.go
    - D:/tmp/codex/codex-rs/core/src/context/user_instructions.rs + skill_instructions.rs — the user-role fragment injection evidence (D-07); nanobot's system-prompt concat is the documented ANTI-pattern (do not copy)
  </read_first>
  <action>
    Create `internal/skills/messages.go`: `RenderAlwaysBlock(skills []Skill) (block string, present bool)` — filters skills with `always:true`, concatenates their bodies under ONE English header literal (e.g. "Active skill instructions (always-on):\n\n") in a DETERMINISTIC order (alphabetical by name — byte-stable, the same discipline as the manifest). Returns present=false (empty block) when no always skills. This block becomes the messages[1] user-role message (the Phase-14 Agent.md seam: profile first, always-skills after — leave the ordering hook documented). Per-skill body is already ≤32KiB-capped at load (D-10).
    Edit `cmd/aura/chat.go` bootChat: initialize the loader (if not already done in 11-02's wiring) and assemble the messages[1] always-block as a user-role turn positioned immediately after the messages[0] system prompt. The block is rebuilt per turn from current loader state (a skill add/remove changes messages[1], which preserves the messages[0]+tools prefix cache — D-07). Wire it so the conversation context ladder sees it as a protected turn (tag it / position it so the evictor recognizes it — see the context.go edit below).
    Edit `internal/conversations/context.go`: extend `dropOldestPairs` (and `applyL1`'s seq=1 skip) to ALSO protect the messages[1] always-block exactly like the seq=1 system L0 turn (Pitfall 3 / D-07 flagged constraint). The current code preserves `turns[0].Seq == 1 && Role == llm.RoleSystem`; teach it to additionally preserve the messages[1] always-block turn (identify it by position seq==2 + a role/marker convention the bootChat assembly sets, OR a dedicated turn kind — pick the minimal mechanism that survives eviction without dropping it). Neither L1 rewrite nor L2.5 pair-drop may touch the always-block.
    Create `internal/skills/messages_test.go`: byte-stability (two RenderAlwaysBlock calls identical), alphabetical order, empty-when-no-always. Edit `internal/conversations/context_test.go`: a 20+-turn reduction scenario asserting the messages[1] always-block SURVIVES L2.5 eviction (the Pitfall 3 regression test) AND messages[0] stays byte-identical. Race-clean. Files ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./internal/skills/ ./internal/conversations/ ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - internal/skills/messages.go RenderAlwaysBlock returns byte-identical output across two calls (asserted in messages_test.go) and alphabetical ordering
    - internal/conversations/context.go dropOldestPairs preserves the messages[1] always-block (the protection extends the existing Seq==1 system-L0 idiom)
    - context_test.go has a 20+-turn test asserting the always-block survives reduction AND messages[0] SHA is unchanged (CAP-04)
    - cmd/aura/chat.go bootChat assembles the always-block as the post-system user-role turn
    - existing scripts/cache_invariant_audit.sh still passes (messages[0] byte-stable across 20-turn replay) — the messages[1] block does not regress the messages[0] invariant
  </acceptance_criteria>
  <done>always:true bodies render into a byte-stable messages[1] block assembled in bootChat; the L2.5 evictor protects it like system L0 (Pitfall 3 regression test green); messages[0] stays byte-identical (CAP-04); all green + race-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → skill create/update/delete | model-authored skill content crosses the write boundary (NFKC+blocklist hard-reject) |
| pending skill → activation | activation requires a human ask_user resume or operator CLI — the model cannot cross it |
| always-block → every future turn | an always:true skill steers all subsequent model turns (high-trust injection point) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-E1 | Elevation of Privilege | model self-approves its own mutation | mitigate | NO model-facing approve action (D-03); activation only via ask_user resume (human) or `aura skills approve` CLI — Task 1 (HIGH severity, structurally closed + tested: no "approve" router key) |
| T-11-05-E2 | Elevation of Privilege | headless context (cron/swarm) auto-activates a mutation | mitigate | headless mutation lands in pending + IMMEDIATE Notifier alert + audit gate_taken=false; can never self-activate (D-26) — Task 1 |
| T-11-05-T1 | Tampering | model injects a blocklisted prompt via a skill body | mitigate | model path = ValidateForWrite(allowBlocklisted=false) hard-reject, no escape (D-27) — Task 1, tested |
| T-11-05-T2 | Tampering | malicious always:true skill silently steers every turn | mitigate | always:true is gated like any mutation (RISKY, human-approved); install STRIPS always (plan 11-06); the gate flags it loudly (D-10); audit records the flag — Task 1 |
| T-11-05-D1 | Denial of Service | always-block silently evicted → capability loss (not a classic DoS but a correctness loss) | mitigate | L2.5 evictor protects messages[1] like L0 (Pitfall 3); 20-turn regression test — Task 2 |
| T-11-05-I1 | Information Disclosure | messages[1] change busts the messages[0] cache (cost/poison) | mitigate | messages[1] is a separate user-role turn; the messages[0]+tools prefix is preserved by design (CAP-04); cache_invariant_audit.sh still green — Task 2 |

No npm/pip/cargo installs in this plan.
</threat_model>

<verification>
- `go test -race ./internal/agent/tools/... ./internal/skills/ ./internal/conversations/ ./internal/agent/` all green
- no `approve` action key in the skill tool (D-03 structurally enforced)
- 20-turn always-block-survives + messages[0]-byte-stable test green; scripts/cache_invariant_audit.sh still passes
- tools package has no internal/skills import (cycle-free)
- golangci-lint 0; all files ≤600 LOC
</verification>

<success_criteria>
- create/update/delete: validate→gate→pending→ask_user pause; model never self-approves (D-03); headless never self-activates (D-26)
- ask_user resume + `aura skills approve`/`audit` CLI activation/inspection; SC#2 purge-denied surfaces the role error
- messages[1] always-block byte-stable + alphabetical; L2.5 evictor protects it (Pitfall 3); messages[0] CAP-04 invariant intact
- Coverage toward the ≥85% floor
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-05-SUMMARY.md` when done
</output>
