---
phase: 11-skills
plan: 06
type: execute
wave: 6
depends_on: ["11-03", "11-04", "11-05"]
files_modified:
  - internal/skills/installer.go
  - internal/skills/installer_test.go
  - internal/skills/hash.go
  - internal/skills/hash_test.go
  - internal/agent/tools/skill_install.go
  - internal/agent/tools/skill_install_test.go
  - cmd/aura/skills.go
autonomous: true
requirements: [CAP-07]
must_haves:
  truths:
    - "Installer clones via native git (LookPath fixed-argv, --depth 1 --single-branch -c core.autocrlf=false), strips symlinks, computes Aura's canonical hash (NOT the upstream computedHash)"
    - "skill action=install + action=catalog let the model discover (skills.sh /api/search) and install with ask_user governance + validator + always-stripping"
    - "Install surfaces red flags (metadata.*.install[], tool wildcards, bundled-exec count); always:true is stripped unconditionally on install (D-10/D-13)"
    - "skills-lock.json computedHash pinned at install (TOFU) using Aura's canonical hash"
  artifacts:
    - path: "internal/skills/installer.go"
      provides: "native git clone + symlink-strip + validate + always-strip + audit (D-13/D-15)"
      min_lines: 50
    - path: "internal/skills/hash.go"
      provides: "Aura canonical byte-sorted (relPath,bytes) sha256 (D-15)"
      contains: "sha256"
    - path: "internal/agent/tools/skill_install.go"
      provides: "install + catalog actions (D-12), ask_user-gated install (D-13)"
      min_lines: 30
  key_links:
    - from: "internal/skills/installer.go"
      to: "git CLI"
      via: "exec.LookPath(git) fixed-argv clone, repoURL the only interpolated value"
      pattern: "LookPath|core.autocrlf"
    - from: "internal/agent/tools/skill_install.go"
      to: "internal/skills CatalogClient + Installer"
      via: "catalog→ask_user→install→validate→activate, via consumer seam"
      pattern: "catalog|install"
    - from: "internal/skills/installer.go"
      to: "internal/skills/validator.go + always-strip"
      via: "validate body + strip always:true on third-party install (D-10)"
      pattern: "ValidateForWrite|always"
---

<objective>
Sub-slice 7d — the installer. Native Go `git clone --depth 1 --single-branch -c core.autocrlf=false` (spike 004b: native WINS — bit-identical, 3× faster, drops the node/npx host dep), symlink-stripping copy, Aura's OWN canonical hash (byte-sorted (relPath,bytes) sha256 — do NOT interop with the locale/platform-sensitive upstream `skills-lock.json` computedHash), validator at the write boundary, unconditional `always:true` stripping on third-party install (D-10), and the install gate that surfaces red flags (D-13). The model-facing `skill action=catalog {query}` (skills.sh /api/search) + `action=install <repo>` complete the discovery→install loop with ask_user governance (D-12/D-13).

Purpose: Deliver SC#1 (`aura skills install <repo>` → native clone + skill_audit INSERT) and the catalog-browse-default-ON discovery path (SC#5 / D-12) the xlsx North-Star E2E depends on. Reuses the writer/audit (11-04), validator (11-03), catalog client (11-03), and ask_user governance (11-05).
Output: installer.go + hash.go + skill_install.go (install + catalog actions) + `aura skills install` CLI + tests (incl. a real-clone integration test of a small public skill repo).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.planning/phases/11-skills/11-RESEARCH.md
@.planning/phases/11-skills/11-PATTERNS.md
@.claude/skills/golang-security/SKILL.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Native clone installer + canonical hash + red-flag detection + always-strip</name>
  <read_first>
    - internal/cron/handlers/backup.go (lines ~100-103, 133-142) — the LookPath fixed-argv exec.CommandContext + the G204 nolint rationale (operator-config constants, never model output)
    - .planning/spikes/004b-install-native-clone/README.md + main.go (lines ~64-110) — the proven clone+copy+symlink-strip+canonical-hash; GIT_TERMINAL_PROMPT=0; the autocrlf/locale lockfile-hash trap
    - .planning/spikes/005-skills-ro-mount/README.md — symlinks resolve in-container → strip at materialization (the installer copy must reject symlinks)
    - internal/conversations/orphan_scan.go (lines ~74-88) — Lstat-no-follow (in-repo canonical) OR the WalkDir d.Type()&fs.ModeSymlink variant (spike 004b) — either valid
    - internal/skills/validator.go (11-03) — ValidateForWrite; install path = operator-overridable (allowBlocklisted via --allow-blocklisted after the gate shows the match, D-27)
    - internal/skills/writer.go (11-04) — the audit INSERT + Activate the installer feeds
    - .planning/phases/11-skills/11-CONTEXT.md D-13 (tolerate optional frontmatter; gate surfaces red flags: metadata.*.install[], tool wildcards, bundled-exec count; skills-lock.json computedHash TOFU pin) + D-14 (clone+copy+hash, no npm postinstall) + D-15 (native clone WINS; canonical hash; do NOT interop upstream computedHash) + D-34 (AURA_SKILL_INSTALL_TIMEOUT_SEC)
  </read_first>
  <action>
    Create `internal/skills/hash.go`: `CanonicalHash(dir string) (string, error)` — walk the skill dir, collect (relPath, contentBytes) for each regular file (skip/reject symlinks via Lstat-no-follow), SORT entries by relPath, then sha256 over `relPath bytes + content bytes` for each in sorted order. This is Aura's OWN TOFU pin — do NOT read or interop with the upstream `skills-lock.json` computedHash (locale/platform-sensitive, spike 004b). Share this helper with the writer's content_hash (11-04 may already call it — if so, this formalizes it).
    Create `internal/skills/installer.go`: an `Installer` with the install timeout (AURA_SKILL_INSTALL_TIMEOUT_SEC) + the writer/audit + validator.
    - `Install(ctx, repoURL string, opts InstallOpts) (InstallResult, error)`:
      1. `gitBin, err := exec.LookPath("git")` (never a shell string); validate repoURL (the ONLY interpolated value) before the call.
      2. `exec.CommandContext(ctx, gitBin, "clone", "--depth", "1", "--single-branch", "-c", "core.autocrlf=false", repoURL, scratchDir)` with `cmd.Env = append(os.Environ(), "GIT_TERMINAL_PROMPT=0")` (never hang on auth). //nolint:gosec with the G204 rationale comment (fixed argv, repoURL validated).
      3. locate the skill subdir(s), copy into pending/<name>/ REJECTING symlinks (strip — never copy through, spike 005).
      4. parse frontmatter; STRIP `always:true` unconditionally from third-party skills (D-10 — re-enable only via `aura skills always <name>`); record the stripped flag in audit.
      5. detect RED FLAGS for the gate (D-13): `metadata.*.install[]` blocks present, tool wildcards in allowed-tools, bundled executable file count — collect into the InstallResult for the gate to surface (do NOT auto-run anything; bundled scripts only ever run via sandbox_exec).
      6. ValidateForWrite (operator path — allowBlocklisted only if opts.AllowBlocklisted, set after the gate shows the match, D-27).
      7. compute CanonicalHash → the TOFU pin; write skills-lock.json (or record the hash in audit) at install.
      8. land in pending/ + audit INSERT (gate_taken=false) via the writer; activation is the operator/ask_user path (11-05), NOT here.
    Create `installer_test.go`: a unit suite with a LOCAL bare git repo fixture (init a tmp repo with a small SKILL.md, OR use a `file://` URL) so the clone path RUNS deterministically without network — assert: clone succeeds, symlinks stripped, always:true stripped, red flags detected for a metadata.install[] fixture, canonical hash stable + matches an independent recomputation, audit row written. Tag the network-dependent real-skills.sh-repo case as integration if a real clone is wanted; the local-repo unit case is the no-network determinism floor. Create `hash_test.go`: canonical hash is order-independent of FS walk order + symlink-excluded. Race-clean, files ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./internal/skills/... && go build ./... && go test -race -run 'TestInstall|TestCanonicalHash' ./internal/skills/</automated>
  </verify>
  <acceptance_criteria>
    - internal/skills/installer.go contains "LookPath" and "core.autocrlf=false" and "GIT_TERMINAL_PROMPT=0"
    - installer.go does NOT read/parse the upstream skills-lock.json computedHash (grep: no "computedHash" consume path — Aura computes its own)
    - installer.go strips always:true on third-party install (asserted by installer_test.go: an always:true fixture lands with always=false + audit notes the strip)
    - installer_test.go uses a local/file:// git repo and the clone path actually RUNS (not skipped)
    - installer_test.go asserts symlinks are stripped and a metadata.install[] fixture raises a red flag in the InstallResult
    - hash_test.go asserts CanonicalHash matches an independent recomputation and excludes symlinks
  </acceptance_criteria>
  <done>Native git clone (fixed-argv, autocrlf-off, terminal-prompt-off) + symlink-strip + always-strip + red-flag detection + Aura canonical hash TOFU pin + pending+audit; install path operator-overridable validator; local-repo clone test runs without network; all green + race-clean.</done>
</task>

<task type="auto">
  <name>Task 2: skill install + catalog actions (model-facing discovery→install) + aura skills install CLI</name>
  <read_first>
    - internal/agent/tools/skill.go + skill_read.go + skill_write.go (11-02/11-05) — the router keys (install + catalog reserved in 11-02) + the consumer-seam pattern; the ask_user pause idiom from skill_write.go
    - internal/skills/catalog.go (11-03) — Search(ctx, query) → []CatalogItem; the Disabled/escape-hatch sentinel
    - internal/skills/installer.go (Task 1) — Install(ctx, repoURL, opts)
    - cmd/aura/skills.go (11-05) — the runSkills switch to extend with `install`
    - .planning/phases/11-skills/11-CONTEXT.md D-12 (catalog browse default-ON; model-facing action=catalog {query} enabled out of the box; disable-catalog escape hatch) + D-13 (install gate surfaces red flags + ask_user) + D-02 (install is a gated mutation)
    - .planning/phases/11-skills/11-PATTERNS.md §"specifics" — the xlsx North-Star tool_use sequence (catalog→ask_user→install→sandbox_exec) this enables
  </read_first>
  <action>
    Create `internal/agent/tools/skill_install.go`: the `actionCatalog` + `actionInstall` handlers (fill the reserved router keys).
    - `actionCatalog {query}`: calls the CatalogClient.Search seam (default-ON, D-12); returns ranked results (name, source, installs) via NewResult; if catalog is disabled, returns the "catalog disabled — run aura skills disable-catalog/enable" guidance. Empty query guarded.
    - `actionInstall {repo}`: calls the Installer seam → lands in pending/ + surfaces the red-flag summary (D-13) + tier in an ask_user pause framing the install-approval question (governance: RISKY tier + ask_user + validator + always-stripping all happen; activation is the human resume from 11-05 or the CLI). The model cannot self-approve the install (D-03).
    Extend the tools-package consumer seam (catalog + installer methods) so internal/agent/tools still does NOT import internal/skills; wire the live CatalogClient + Installer via the cmd adapter (newSkillTool gains those deps).
    Extend `cmd/aura/skills.go`: add `case "install"` → operator-path Install (with `--allow-blocklisted` flag after showing the match, D-27) → then operator approve activates; `case "catalog"` (list/search; `disable-catalog`/`enable-catalog` toggles, D-12); `case "always"` (re-enable a stripped always flag, D-10).
    Create `skill_install_test.go` (unit, fake catalog + installer): action=catalog returns ranked results; action=install lands pending + pauses (ask_user) + surfaces red flags; the model path cannot activate. Race-clean. Files ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./internal/agent/tools/... ./internal/skills/</automated>
  </verify>
  <acceptance_criteria>
    - internal/agent/tools/skill_install.go has actionCatalog and actionInstall registered on the skill router (the 11-02 placeholders are replaced)
    - skill_install_test.go asserts action=install lands pending + triggers an ask_user pause (not auto-activation) and surfaces red flags
    - skill_install_test.go asserts action=catalog returns installs-ranked results and is enabled by default (D-12)
    - cmd/aura/skills.go has `case "install"`, `case "catalog"`, and `case "always"`
    - `go list -deps ./internal/agent/tools | grep -c internal/skills` returns 0 (boundary held)
    - `go test -race ./internal/agent/tools/...` exits 0
  </acceptance_criteria>
  <done>Model-facing catalog (default-ON) + install (gated, ask_user, red-flags) actions complete the discovery→install loop; `aura skills install/catalog/always` CLI shipped; install never self-activates (D-03); all green + race-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skills.sh repo → Aura FS | a third-party git repo is cloned to local disk (supply-chain boundary) |
| third-party skill body → write boundary | cloned content crosses the validator + always-strip + red-flag gate |
| model → install action | model-supplied repo URL crosses into the installer (gated, never auto-activated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-T1 | Tampering | supply-chain: malicious third-party skill (slopsquat, npm postinstall) | mitigate | native clone has NO npm postinstall surface (D-14); canonical-hash TOFU pin; install gate surfaces red flags; bundled scripts only run via sandbox_exec — Task 1 (HIGH severity, mitigated) |
| T-11-06-T2 | Tampering | symlink escape from a cloned skill dir | mitigate | symlink-strip at copy (Lstat-no-follow / WalkDir skip, spike 005) — Task 1, tested |
| T-11-06-E1 | Elevation of Privilege | a third-party always:true skill silently steers every turn | mitigate | always:true stripped UNCONDITIONALLY on install (D-10); re-enable only via explicit `aura skills always` — Task 1, tested |
| T-11-06-E2 | Elevation of Privilege | model self-installs+activates a malicious skill | mitigate | install lands pending + ask_user gate; no model approve (D-03); activation is human/CLI only — Task 2 |
| T-11-06-T3 | Tampering | git clone hangs on an auth prompt / runs arbitrary shell | mitigate | LookPath fixed-argv (no shell string), GIT_TERMINAL_PROMPT=0, repoURL validated, install timeout — Task 1 |
| T-11-06-SC | Tampering | git CLI is the install transport (not a package manager) | accept | git is an operator-provisioned binary (RESEARCH Environment Availability); no npm/pip/cargo install in this plan → no [ASSUMED]/[SUS] package checkpoint needed; the cloned skill content IS gated (red flags + validator + hash) |

No npm/pip/cargo package-manager installs by Aura's Go code in this plan (D-14 drops npx). The git binary is operator-provisioned; cloned skill repos are content-gated, not trusted.
</threat_model>

<verification>
- `go test -race -run 'TestInstall|TestCanonicalHash' ./internal/skills/` green; the local/file:// clone path RUNS (not skipped)
- `go test -race ./internal/agent/tools/...` green; tools has no internal/skills import
- installer grep-clean: LookPath + core.autocrlf=false + GIT_TERMINAL_PROMPT=0 present; no upstream computedHash consume
- always-strip + red-flag + symlink-strip asserted by tests
- golangci-lint 0; all files ≤600 LOC
</verification>

<success_criteria>
- SC#1: `aura skills install <repo>` → native git clone + skill_audit INSERT row
- Model-facing catalog (default-ON, D-12) + install (gated, ask_user, red-flags surfaced, always-stripped)
- Aura canonical-hash TOFU pin (no upstream computedHash interop); supply-chain red flags surfaced
- Coverage toward the ≥85% floor; mutation spot-check candidate = installer.go
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-06-SUMMARY.md` when done
</output>
