---
phase: 11-skills
plan: 09
type: execute
wave: 1
depends_on: ["11-06"]
files_modified:
  - internal/skills/catalog.go
  - internal/skills/catalog_test.go
  - internal/skills/installer.go
  - internal/skills/installer_test.go
  - internal/skills/installer_select_test.go
  - internal/skills/installer_integration_test.go
  - internal/skills/hash.go
  - internal/skills/hash_test.go
  - internal/agent/tools/skill_install.go
  - internal/agent/tools/skill_install_test.go
  - internal/agent/tools/skill.go
  - internal/agent/tools/skill_read.go
  - internal/agent/tools/skill_test.go
  - cmd/aura/skills.go
  - cmd/aura/serve_adapters.go
  - internal/config/config.go
  - internal/skills/builtin.go
  - internal/skills/embed/find-skills-aura/SKILL.md
  - internal/skills/builtin_test.go
  - internal/skills/loader.go
  - internal/skills/loader_test.go
  - internal/agent/prompt.go
  - docs/system_prompt.txt
  - internal/agent/prompt_test.go
  - internal/agent/llm_agent_pause.go
autonomous: true
requirements: [CAP-07, CAP-08]
must_haves:
  truths:
    - "The model-facing discovery/install Go complex is gone: no catalog.go, no installer.go, no skill_install.go; the skill tool's action enum has no catalog/install; no AURA_SKILL_CATALOG_URL/AURA_SKILL_INSTALL_TIMEOUT_SEC/AURA_SKILL_CATALOG_DISABLE config (per amendment #51/D-40)"
    - "Builtin skill find-skills-aura (always:true) materializes via MaterializeBuiltins and renders into the messages[1] always-block — it teaches npx skills find/add by-path in the sandbox (D-40)"
    - "The Loader runs the NFKC+literal injection blocklist on every body entering the manifest/always-block; a body that hits the blocklist is skipped from load + slog-logged, never crashing the loader (D-27/D-28 amended)"
    - "The Loader ALSO scans the persistent-install root <export>/.agents/skills so an in-sandbox `cd /skills && npx skills add ...` yields a loaded skill; the active SkillsDir root wins on a name collision (#50)"
    - "SystemPrompt §Skills shrinks to a byte-stable pointer at the find-skills-aura always-on skill (no catalog/install action teaching); docs/system_prompt.txt stays in sync; messages[0] has no timestamp and no volatile tool enumeration (D-08/D-09)"
    - "ONLY ask_user pauses the turn — the architectural constraint is documented in code where the pause is name-gated (the dropped-payload dead path dies with the deletions, spike 012b)"
    - "go build ./... and golangci-lint run ./... are clean: no half-deleted seams, no newly-dead code (scoring install branch, tierFromLabel, install-only Alerter usage)"
  artifacts:
    - path: "internal/skills/embed/find-skills-aura/SKILL.md"
      provides: "the proven find-skills builtin body (spike 012a, 4/4 live PASS), always:true"
      contains: "npx skills"
    - path: "internal/skills/loader.go"
      provides: "load-time blocklist scan + persistent-install root scanning"
      contains: "violatesBlocklist"
    - path: "internal/agent/prompt.go"
      provides: "shrunk byte-stable §Skills pointer (no catalog/install teaching)"
      contains: "Skills"
  key_links:
    - from: "internal/skills/loader.go"
      to: "internal/skills/validator.go violatesBlocklist"
      via: "load-time blocklist scan of every body entering the manifest/always-block"
      pattern: "violatesBlocklist"
    - from: "cmd/aura/serve_adapters.go newSkillTool/alwaysBlockProvider"
      to: "the persistent-install Loader root <export>/.agents/skills"
      via: "skills.NewLoader Roots includes the .agents/skills dir, active SkillsDir last (wins)"
      pattern: "agents.*skills|\\.agents"
    - from: "internal/agent/prompt.go SystemPrompt"
      to: "docs/system_prompt.txt"
      via: "byte-for-byte sync (a test asserts it)"
      pattern: "Skills"
---

<objective>
Slice 7g deletion + thin-shape replacement (amendment #51 / D-40). Delete the ~2,050-LOC model-facing discovery/install Go complex (catalog client, native installer, the skill tool's catalog+install actions, their CLI legs, serve/eval adapters, and the AURA_SKILL_CATALOG_URL / AURA_SKILL_INSTALL_TIMEOUT_SEC / AURA_SKILL_CATALOG_DISABLE env). Replace it with: (1) the proven `find-skills-aura` builtin skill (always:true → messages[1], teaches `npx skills find/add` by-path in the sandbox), (2) a Loader-level injection-blocklist scan (the ONE hard security keep — self-installed bodies never pass the Writer), (3) a persistent-install Loader root through the rw `/skills` mount, and (4) a shrunk byte-stable §Skills pointer in the canonical system prompt. The eval rewrite is plan 11-10.

Purpose: realize the spike-012a winner (skill-driven self-extension, full find→add→use→artifact loop in one turn) — supersede the tool-driven flow that still failed its E2E (judge 0.47) and carried a dead pause-payload path (spike 012b). Deletion slices fail by leaving half-deleted seams that still compile — every acceptance criterion below carries a NEGATIVE assertion (symbol absent from the tree) plus the golangci-lint dead-code sweep.
Output: a net-negative Go diff, one builtin SKILL.md, a hardened Loader, and a shrunk byte-stable prompt — all with `go build ./...` + `golangci-lint run ./...` clean.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-skills/11-CONTEXT.md
@.claude/skills/spike-findings-Aura/references/skills-self-extension.md
@.planning/spikes/013-thin-surface-gate-parity/README.md
@.planning/spikes/012b-discovery-tool-driven/README.md
@.claude/skills/golang-lint/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DELETE the model-facing discovery/install complex (catalog + installer + actions + CLI + adapters + env)</name>
  <files>internal/skills/catalog.go (DELETE), internal/skills/catalog_test.go (DELETE), internal/skills/installer.go (DELETE), internal/skills/installer_test.go (DELETE), internal/skills/installer_select_test.go (DELETE), internal/skills/installer_integration_test.go (DELETE), internal/skills/hash.go (DELETE), internal/skills/hash_test.go (DELETE), internal/agent/tools/skill_install.go (DELETE), internal/agent/tools/skill_install_test.go (DELETE), internal/agent/tools/skill.go, internal/agent/tools/skill_read.go, internal/agent/tools/skill_test.go, cmd/aura/skills.go, cmd/aura/serve_adapters.go, internal/config/config.go, internal/scoring/scoring.go</files>
  <read_first>
    - prd.md §Slice 7 — read the whole Amendment #51 block (search "Amendment #51", commit 61dfebbb) + the [SUPERATO #51] / [CANCELLATO #51] / [RIMOSSA #51] markers around lines 2151-2294: the authoritative delete list (catalog client D-11/D-12, installer D-15, actions catalog/install, CLI legs catalog/install/disable-catalog, env AURA_SKILL_CATALOG_URL/AURA_SKILL_INSTALL_TIMEOUT_SEC)
    - .planning/spikes/013-thin-surface-gate-parity/README.md — the exact "Delete table" (files + LOC) AND the "Survival list" (what MUST remain: the Writer authoring path, snippets, the Loader, the audit store, the Alerter seam used by skill_write.go)
    - internal/skills/catalog.go + internal/skills/installer.go + internal/skills/hash.go — the delete targets (hash.go's CanonicalHash/writeLockfile is installer-only; verify HashSkillFiles in snippet.go/contenthash.go is a DIFFERENT symbol that survives before removing hash.go)
    - internal/agent/tools/skill_install.go — actionCatalog + actionInstall + catalogRetryHint + renderInstallGate + tierFromLabel + the skillCatalog/skillInstaller/CatalogResult/InstallSummary seams (all DELETE)
    - internal/agent/tools/skill.go — skillParamsSchema (remove catalog/install from the action enum + the repo/query field docs that only serve them), manifestDescription lead (remove the catalog/install discovery paragraph, keep list/info/use), the actionRouter map (remove the "catalog"/"install" keys + their notYetWired comment), the Catalog/Installer struct fields (DELETE), keep the Alerter field (skill_write.go uses it)
    - internal/agent/tools/skill_read.go — listCatalogTail + the actionList no-match branch (both teach the now-gone catalog/install; rewrite to point at the find-skills-aura always-on skill instead — see Task 2 for the message)
    - cmd/aura/skills.go — runSkills switch (remove `install`/`catalog` cases), skillsInstall + skillsCatalog functions, the skillsUsage string legs (remove install/catalog/disable-catalog), and the `strings` import if it becomes unused after skillsInstall goes
    - cmd/aura/serve_adapters.go — newSkillTool (remove the Catalog/Installer wiring lines 263-270 region, keep the Writer wiring), newSkillCatalog, newSkillInstaller, skillCatalogAdapter, skillInstallerAdapter (all DELETE; keep skillLoaderAdapter, skillWriterAdapter, alwaysBlockProvider, snippetSweeperAdapter)
    - internal/config/config.go lines 96-102 + 218-220 — the SkillCatalogURL/SkillCatalogDisabled/SkillInstallTimeoutSec struct fields + their envDefault/envBoolDefault/envIntDefault wiring (DELETE all three; keep SkillsDir/SkillExportDir/SkillBodyCapBytes/SkillInjectionBlocklist/SkillManifestCapBytes)
    - internal/scoring/scoring.go — ComputeSkillTier's SkillInstall branch + the SkillInstall const: SkillInstall is NO LONGER reached from a tool path, but the installer deletion may leave it dead. Check `grep -rn SkillInstall internal/ cmd/` after deletion; if the ONLY remaining refs are the const+the branch, fold SkillInstall out of the switch and remove the const (golangci-lint will flag it as unused otherwise — memory: linter catches what audit agents miss)
    - the test files for every deleted .go (catalog_test.go, installer_test.go, installer_select_test.go, installer_integration_test.go, hash_test.go, skill_install_test.go) — DELETE wholesale; internal/agent/tools/skill_test.go line ~90 asserts "install" in the action enum map — update that test to the reduced enum (justify: the test asserted the superseded enum)
  </read_first>
  <action>
    Delete the files outright (use the filesystem; do not blank them): internal/skills/catalog.go, internal/skills/catalog_test.go, internal/skills/installer.go, internal/skills/installer_test.go, internal/skills/installer_select_test.go, internal/skills/installer_integration_test.go, internal/skills/hash.go, internal/skills/hash_test.go, internal/agent/tools/skill_install.go, internal/agent/tools/skill_install_test.go.
    In internal/agent/tools/skill.go: drop the Catalog + Installer struct fields; remove `catalog` and `install` from skillParamsSchema's action enum and remove the `repo` property + the `query`-for-catalog half of the `query` property doc (keep `query` for action=list overflow); rewrite the manifestDescription `lead` const to name ONLY list/info/use + the find-skills always-on skill pointer (NO catalog/install verbs — keep it a fixed const, turn-stable, D-06); remove the "catalog"/"install" keys from the actionRouter map and the now-stale notYetWired comment block referencing them.
    In internal/agent/tools/skill_read.go: rewrite listCatalogTail and the actionList queried-miss branch so a list miss points the model at the always-on find-skills skill teaching ("an always-on skill teaches how to discover and install skills from the ecosystem"), NOT at action=catalog/action=install. Keep them fixed per-turn result literals.
    In cmd/aura/skills.go: remove the `install` + `catalog` switch cases, delete skillsInstall + skillsCatalog, and trim the skillsUsage string to drop install/catalog/disable-catalog. Keep list/info/create/update/delete/approve/always/snippet/audit/restore/archive. Remove now-unused imports.
    In cmd/aura/serve_adapters.go: in newSkillTool remove the Catalog + Installer assignment; delete newSkillCatalog, newSkillInstaller, skillCatalogAdapter (+ its Search/Disabled), skillInstallerAdapter (+ its Install). Keep everything else.
    In internal/config/config.go: delete the SkillCatalogURL/SkillCatalogDisabled/SkillInstallTimeoutSec fields + their three Load() lines.
    In internal/scoring/scoring.go: after the deletions, if `grep -rn SkillInstall internal/ cmd/` shows no consumer outside scoring.go itself, fold the SkillInstall case into the default Risky arm and remove the SkillInstall const (refer to it via "amendment #51: install no longer flows through ComputeSkillTier").
    In internal/agent/tools/skill_test.go: update the action-enum assertion (the `want` map at line ~90) to drop "install"; document the change ("the test asserted the superseded enum; #51 removed catalog/install").
    Run `~/go/bin/golangci-lint run ./...` (WSL) — or the repo's golangci-lint — and clear every newly-dead-code finding it surfaces (unused funcs/consts/fields the audit above may have missed).
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/skills/ ./internal/agent/tools/ ./cmd/aura/ ./internal/config/ ./internal/scoring/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 (no half-deleted seam left dangling)
    - NEGATIVE: `grep -rn "skillCatalog\|skillInstaller\|CatalogResult\|InstallSummary\|renderInstallGate\|catalogRetryHint\|tierFromLabel" internal/agent/tools/` returns no matches in non-deleted files
    - NEGATIVE: `grep -rln "package skills" internal/skills/catalog.go internal/skills/installer.go internal/skills/hash.go` errors (files do not exist)
    - NEGATIVE: `grep -rn "SkillCatalogURL\|SkillCatalogDisabled\|SkillInstallTimeoutSec\|AURA_SKILL_CATALOG_URL\|AURA_SKILL_INSTALL_TIMEOUT_SEC\|AURA_SKILL_CATALOG_DISABLE" internal/ cmd/` returns no matches (only docs/PRD/.planning history may keep the strings)
    - NEGATIVE: the skill tool schema enum has no catalog/install — `grep -o '"enum": \[[^]]*\]' internal/agent/tools/skill.go` shows list/info/use/create/update/delete/restore/archive only; `aura tools` output (or a manifest dump) lacks catalog/install in the skill action description
    - NEGATIVE: `grep -rn '"catalog"\|"install"' cmd/aura/skills.go` returns no matches (CLI legs gone)
    - `~/go/bin/golangci-lint run ./...` (or the project linter) reports 0 issues — specifically no `unused` findings for SkillInstall / install-only helpers
  </acceptance_criteria>
  <done>The catalog client, installer, skill_install tool, their CLI legs, serve adapters, and the three catalog/install env knobs are deleted; the skill tool enum + CLI + scoring no longer reference catalog/install; go build + golangci-lint are clean with no dead code.</done>
</task>

<task type="auto">
  <name>Task 2: SHIP find-skills-aura builtin + Loader-level blocklist scan + persistent-install Loader root + architectural-constraint doc</name>
  <files>internal/skills/embed/find-skills-aura/SKILL.md (NEW), internal/skills/builtin.go, internal/skills/builtin_test.go, internal/skills/loader.go, internal/skills/loader_test.go, cmd/aura/serve_adapters.go, cmd/aura/skills.go, internal/agent/llm_agent_pause.go</files>
  <read_first>
    - .planning/spikes/012a-discovery-skill-driven/find-skills-aura/SKILL.md — the PROVEN body to ship (4/4 live PASS). NOTE: its frontmatter is `name: find-skills` and its "How to install and use" section teaches the `.claude/skills/<name>/` landing path; the builtin you ship must use dir `find-skills-aura` + frontmatter `name: find-skills-aura` (the loader enforces name==dirName via SanitizeName) AND its install section must teach the PERSISTENT path through the rw /skills mount (`cd /skills && npx skills add <owner/repo> --skill <name> --copy -y`) so the loader-root wiring below actually loads it; keep always:true, the query-by-format teaching, and the installs+provenance "how to choose" (name-squatting is live)
    - internal/skills/builtin.go — MaterializeBuiltins (fs.WalkDir over the embed tree, fingerprint-idempotent writeIfChanged). The embed root is `embed/`; skill-creator already lives at embed/skill-creator/SKILL.md — add embed/find-skills-aura/SKILL.md as a sibling. Update the package doc comment ("ONLY skill-creator is embedded (D-31)") to note find-skills-aura is now also embedded per #51/D-40
    - internal/skills/builtin_test.go — the materialization+loader assertion for skill-creator; add a parallel assertion that find-skills-aura materializes, loads, and is Always==true
    - internal/skills/loader.go — loadSkillDir + validateStructure: the load path that builds each Skill. The blocklist scan hooks in HERE (after parseFrontmatter, alongside validateStructure) — a body that violatesBlocklist is skip-logged (slog.Warn with the matched sequence) and excluded (ok=false), never fatal. Keep parse-only for everything else (D-28 stays for structure)
    - internal/skills/validator.go — violatesBlocklist(body, blocklist) (matched, pos, ok) is the SAME NFKC+literal chokepoint the Writer uses; the Loader reuses it (do NOT duplicate the logic). The Loader needs the blocklist injected via Config — add a Blocklist []string field to skills.Config and thread it through NewLoader (default to the empty slice → no-op when unset, preserving today's tests that build a Loader without a blocklist)
    - internal/skills/loader_test.go — the multi-root precedence + invalid-skip tests; add a test that a SKILL.md whose body contains a blocklist token (e.g. an Anthropic role marker) is SKIPPED from List() + a non-blocklisted skill in the same root still loads
    - cmd/aura/serve_adapters.go — newSkillTool + alwaysBlockProvider both build a skills.NewLoader over `[]string{cfg.SkillsDir}`. Add the persistent-install root: build Roots as `[]string{filepath.Join(cfg.SkillExportDir, ".agents", "skills"), cfg.SkillsDir}` (persistent-install dir FIRST so the active SkillsDir wins on a name collision — scanRoot iterates roots in order, later root wins) AND pass cfg.SkillInjectionBlocklist as Config.Blocklist so the load-time scan is live in production; do the SAME in cmd/aura/skills.go skillsList/skillsInfo loaders (so `aura skills list` also scans the persistent root + applies the blocklist)
    - internal/skills/snippet.go inSandboxSkillsRoot ("/skills") — confirms the in-container mount point the builtin body references; the export dir host side is cfg.SkillExportDir mounted rw per #50 (the builtin body teaches writing through it)
    - internal/agent/llm_agent_pause.go (lines 26-49, pauseCalls) — the name-gated pause: only ask_user tool calls are pre-executed as pauses. This is where the architectural constraint comment lands
  </read_first>
  <action>
    Create internal/skills/embed/find-skills-aura/SKILL.md: copy the spike 012a body, set frontmatter `name: find-skills-aura` + `always: true` + the trigger-rich description, and adapt the "How to install and use" section to teach the persistent install path (`cd /skills && npx skills add <owner/repo> --skill <name> --copy -y`, lands under the host-visible mount so it persists and loads) alongside the session-scoped (`/workspace`) note; keep the format-token query teaching, the installs+provenance choosing teaching (name-squatting), and run-by-interpreter+path. Body ≤32KiB (the loader body cap).
    Update internal/skills/builtin.go doc comment to record find-skills-aura as a second embedded builtin (#51/D-40); MaterializeBuiltins needs no code change (it walks the embed tree).
    Add a Blocklist []string field to skills.Config; thread it into the Loader struct in NewLoader; in loadSkillDir, after parseFrontmatter + validateStructure succeed, run `if matched, pos, ok := violatesBlocklist(body, l.blocklist); ok { slog.Warn("skills loader: skipping skill with blocklisted body", "path", path, "matched", matched, "pos", pos); return Skill{}, false }`. When l.blocklist is empty the scan is a no-op (keeps existing loader tests green). Run violatesBlocklist on fm.Description too (the manifest renders the description from frontmatter — an injection there reaches the prefix) and skip on a hit.
    In cmd/aura/serve_adapters.go: change newSkillTool's loader + alwaysBlockProvider's loader to use Roots `[]string{filepath.Join(cfg.SkillExportDir, ".agents", "skills"), cfg.SkillsDir}` and Config.Blocklist `cfg.SkillInjectionBlocklist`. Add a small helper `skillLoaderRoots(cfg)` if both call sites want one source of truth (DRY). Mirror the Roots+Blocklist in cmd/aura/skills.go skillsList/skillsInfo.
    In internal/agent/llm_agent_pause.go: add a doc comment at pauseCalls (or askUserToolName) stating the architectural constraint: ONLY ask_user pauses the turn — no other tool may return tools.ErrAwaitingUserInput expecting a pause; the dispatch renders a non-ask_user tool error as `error: <msg>` (spike 012b proved the install gate's payload was dropped), and the dead install-gate path that relied on it was deleted in plan 11-09 Task 1.
    Add the loader_test.go blocklist test + the builtin_test.go find-skills-aura test described above.
  </action>
  <verify>
    <automated>go test -race ./internal/skills/ -run 'TestMaterializeBuiltins|TestLoader|TestFindSkills|TestBlocklist' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal/skills/embed/find-skills-aura/SKILL.md exists, frontmatter `name: find-skills-aura` + `always: true`, body contains "npx skills" and the `/skills` persistent path; `grep -c "npx skills" internal/skills/embed/find-skills-aura/SKILL.md` ≥ 2
    - a test loads find-skills-aura from a MaterializeBuiltins'd temp root AND asserts Always==true (builtin_test.go)
    - a loader test proves a SKILL.md whose body carries a blocklist token is ABSENT from List() while a clean sibling skill is present (loader_test.go) — the load-time blocklist scan
    - `grep -n "violatesBlocklist" internal/skills/loader.go` shows the load-time call; the loader does NOT crash on a blocklisted body (the test asserts List() returns, no panic)
    - `grep -n "\.agents" cmd/aura/serve_adapters.go` shows the persistent-install root in the Loader Roots, ordered before cfg.SkillsDir (active wins)
    - `grep -n "ask_user" internal/agent/llm_agent_pause.go` plus a comment stating ONLY ask_user pauses the turn (the architectural constraint)
    - `go test -race ./internal/skills/` green; `go build ./...` exits 0
  </acceptance_criteria>
  <done>find-skills-aura ships as an always:true builtin teaching the npx self-extension loop; the Loader scans the persistent-install root (active SkillsDir wins) and runs the NFKC+literal blocklist on every body/description entering the manifest (skip+log, no crash); the ask_user-only-pause constraint is documented in code.</done>
</task>

<task type="auto">
  <name>Task 3: SHRINK SystemPrompt §Skills to a byte-stable pointer + keep docs/system_prompt.txt in sync</name>
  <files>internal/agent/prompt.go, docs/system_prompt.txt, internal/agent/prompt_test.go</files>
  <read_first>
    - internal/agent/prompt.go — the SystemPrompt const, lines 37-43 (the "# Skills — capability gaps" 5-step REQUIRED workflow that names skill action=catalog/install). This whole section shrinks to a pointer: skills exist; an always-on skill (find-skills) teaches the discovery/install loop; use a skill by applying it and run bundled scripts by-path; verify artifacts. MUST stay a const with no timestamp and name only structural verbs (skill, ask_user) — NOT action=catalog/action=install (those tools are gone)
    - docs/system_prompt.txt — the byte-for-byte authored source of truth (lines 20-26 mirror the §Skills section). It MUST be updated to match prompt.go exactly — the sync is asserted by a test (or the cache-invariant job). Update both in the same edit
    - internal/agent/prompt_test.go — TestPrompt_NoTimestamp, TestPrompt_MechanismNotEnumeration (asserts read_tool_output/current_time are ABSENT, tool_search + text_response PRESENT), TestPrompt_ByteStable. The shrunk section must keep these passing; check whether any existing test asserts the catalog/install teaching text and update it (justify: the test asserted superseded teaching). Add an assertion (here or in a skills/prompt test) that the §Skills section does NOT contain "action=catalog" or "action=install" (the superseded routing is gone) — a guard so a future edit cannot reintroduce the dead teaching
    - .planning/spikes/012a-discovery-skill-driven/main.go (lines 50-52 alwaysBlockHeader, the messages[1] wire shape) + internal/skills/messages.go RenderAlwaysBlock — confirms the find-skills teaching now LIVES in messages[1] (the always-block), so messages[0] only needs the pointer
    - CLAUDE.md §"NO COMMENTS UNLESS WHY IS NON-OBVIOUS" + the cache-poisoning memory (reference_aura_cache_poisoning_sites) — the prompt is a load-bearing byte-stable const; the shrink must not add per-turn-mutating content
  </read_first>
  <action>
    Rewrite the "# Skills — capability gaps" section of internal/agent/prompt.go SystemPrompt to a short byte-stable pointer (no numbered catalog/install workflow). Keep: skills are packaged capabilities applied via the skill tool; an always-on skill teaches how to discover and install skills from the open ecosystem when you lack a battle-tested method for a task; prefer a vetted skill over ad-hoc code; apply a skill by using it and run its bundled scripts BY PATH; verify produced artifacts before reporting (this last point may already live in "# Working on the machine" — do not duplicate). Name only structural verbs (skill, ask_user); do NOT name action=catalog/action=install (gone) and do NOT enumerate volatile tools (D-08/D-09). Mirror the EXACT same text into docs/system_prompt.txt (the two are kept in sync; a test asserts it).
    Update internal/agent/prompt_test.go: if any assertion checks for the old catalog/install workflow text, replace it; ADD a TestPrompt_NoSupersededSkillRouting that asserts the prompt does NOT contain "action=catalog" or "action=install" (the #51 supersession guard). Ensure TestPrompt_NoTimestamp / TestPrompt_MechanismNotEnumeration / TestPrompt_ByteStable still pass against the shrunk text.
  </action>
  <verify>
    <automated>go test ./internal/agent/ -run 'TestPrompt' && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "action=catalog\|action=install" internal/agent/prompt.go docs/system_prompt.txt` returns 0 (the superseded routing is gone from both)
    - the §Skills section names a "always-on" skill as the discovery/install teaching (the find-skills pointer) — `grep -i "always-on\|always on" internal/agent/prompt.go` matches the §Skills pointer
    - SystemPrompt and docs/system_prompt.txt are byte-identical for the prompt body (the sync test / the existing cache-invariant comparison passes)
    - `go test ./internal/agent/ -run 'TestPrompt'` green: NoTimestamp, MechanismNotEnumeration (tool_search/text_response present, read_tool_output/current_time absent), ByteStable, and the new NoSupersededSkillRouting all pass
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>The §Skills system-prompt section is a byte-stable pointer to the always-on find-skills skill (no catalog/install action teaching); docs/system_prompt.txt matches byte-for-byte; the prompt tests (incl. the new superseded-routing guard) pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party skill body → messages[1]/manifest | a self-installed skill body crosses into the model context WITHOUT passing the Writer (the npx CLI lands it directly on disk) |
| marketplace ranking/name → install choice | skills.sh ranking is mutable and name-squatting is live (a 76K-install find-skills clone outranks the 1.9M original on semantic queries) |
| npx-installed content → host-side Loader walk | the CLI writes arbitrary files (possibly symlinks) under the persistent-install root the Loader then scans |
| skill SKILL.md description → manifest prefix | the frontmatter description renders into the model-visible tool Description |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09-T1 | Tampering | a third-party skill body enters messages[1]/manifest with prompt-injection markers (never scanned by the Writer) | mitigate | the Loader-level NFKC+literal blocklist scan (Task 2): every body AND description entering the manifest/always-block is `violatesBlocklist`-checked at load; a hit skips the skill + slog.Warn, never loads the body |
| T-11-09-S1 | Spoofing | name-squatting on the marketplace (a clone outranks the original) | mitigate | the shipped find-skills-aura body teaches choose-by-installs+reputable-provenance (the model followed it unprompted in all 4 spike-012a live runs) |
| T-11-09-E1 | Elevation of Privilege | a symlink in npx-installed content read host-side by the Loader escapes the install root | mitigate | the Loader already Lstat-no-follows entries + SKILL.md (loader.go scanRoot/loadSkillDir, T-11-02-T1) — a symlinked entry/SKILL.md is skip-logged, never traversed; the persistent-install root reuses the SAME scan |
| T-11-09-I1 | Information Disclosure | prompt-injection via a skill SKILL.md description into the manifest prefix | mitigate | the same Task-2 blocklist scan runs on fm.Description, not just the body |
| T-11-09-R1 | Repudiation | a half-deleted seam still compiles, hiding a broken delete | mitigate | every Task-1 acceptance criterion carries a NEGATIVE assertion (symbol absent) + the golangci-lint dead-code sweep |
| T-11-09-SC | Tampering | npm/npx-driven supply chain (the model installs third-party skills) | accept | per amendment #51/D-40 + #50: full-terminal home, single-trusted-operator posture, self-install with no ceremony; per-identity gating arrives with capability_grants (Slice 1.7). No npm/pip/cargo install by Aura's OWN Go code in this plan (the CLI runs IN the sandbox). No Package Legitimacy Gate applies — Aura's go.mod gains/loses no dependency here (net-negative diff) |
</threat_model>

<verification>
- `go build ./...` + `go vet ./...` clean; `golangci-lint run ./...` reports 0 issues (no half-deleted seams, no dead code)
- NEGATIVE assertions for every deleted symbol/file/env (catalog/installer/skill_install/the three env knobs/the schema enum entries/the CLI legs) all hold
- `go test -race ./internal/skills/ ./internal/agent/` green: builtin materialization, the load-time blocklist skip, the persistent-install root, the byte-stable shrunk prompt + the superseded-routing guard
- `go test -race ./internal/agent/tools/` green with the reduced action enum
</verification>

<success_criteria>
- The model-facing discovery/install Go complex (~2,050 LOC) is deleted with a net-negative diff and zero dead code
- find-skills-aura ships as an always:true builtin and renders into messages[1]
- The injection blocklist runs at Loader scan time on every body+description entering the manifest/always-block (skip+log, no crash)
- The Loader scans the persistent-install root through the rw /skills mount; the active SkillsDir wins on a name collision
- The §Skills system-prompt section is a byte-stable pointer with docs/system_prompt.txt in sync; messages[0] carries no superseded catalog/install routing
- The ask_user-only-pause architectural constraint is documented in code
</success_criteria>

<output>
Create `.planning/phases/11-skills/11-09-SUMMARY.md` when done
</output>
</task>
</content>
</invoke>
