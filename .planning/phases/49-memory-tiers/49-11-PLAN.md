---
phase: 49-memory-tiers
plan: 11
type: tdd
wave: 12
depends_on:
  - 49-06
  - 49-13
  - 49-14
files_modified:
  - internal/arcadedb/memory_batch_live_test.go
  - cmd/arcadedb-mcp/tool_memory_batch.go
  - cmd/arcadedb-mcp/tool_memory_batch_test.go
  - cmd/arcadedb-mcp/main.go
  - internal/agent/mcptools/bridge_risk.go
  - internal/agent/mcptools/bridge_memory_surface_test.go
  - cmd/arcadedb-mcp/memory_live_integration_test.go
  - scripts/agent_memory_eval.py
  - scripts/agent_memory_eval_test.py
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-06
  - TOOL-05
  - AUTO-03
  - CTX-05
  - HARN-05
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One bounded identity-free `memory_batch` schema exposes exactly upsert_fact, supersede_fact, merge_entities, and forget operations and calls the final-state engine once (HARN-05, D-19, D-20)."
    - "Live rollback/concurrency/retry exposes no partial state and preserves first-error/idempotency semantics (D-21)."
    - "The isolated Amendment #201 commit changes only prd.md and precedes the earliest Phase 49 commit touching every enumerated reasoning implementation path (MEM-06, D-01)."
    - "A no-skip conversation against the running Aura process proves beyond-active-context recall, provider-visible reasoning with TOUCHED edges and later model-context exclusion, and durable capture from real shell/file work; all six terminal Aura answers have unique response IDs, each scores strictly above 9.8, and each carries correlated Tempo, PostgreSQL, and ArcadeDB evidence references."
    - "The final no-skip live evaluator and full tagged coverage, package policy, goleak/race, mutation, vet, and build gates pass."
  artifacts:
    - path: cmd/arcadedb-mcp/tool_memory_batch.go
      provides: "bounded typed atomic memory mutation adapter"
    - path: internal/arcadedb/memory_batch_live_test.go
      provides: "live rollback/concurrency/retry proof"
    - path: scripts/agent_memory_eval.py
      provides: "final all-tier no-skip evidence plus six uniquely identified running-Aura terminal-answer records, each scored >9.8 with correlated evidence references"
  key_links:
    - from: cmd/arcadedb-mcp/tool_memory_batch.go
      to: internal/arcadedb/memory_batch.go
      via: "one validated bounded request and one ApplyMemoryBatch call"
      pattern: "ApplyMemoryBatch"
    - from: scripts/agent_memory_eval.py
      to: all published memory paths
      via: "real authenticated Aura turns correlated across response, Tempo, PostgreSQL, and ArcadeDB evidence"
      pattern: "tier all"
---

<objective>
Publish and prove the atomic batch API, then run the complete Phase 49 acceptance and amendment-ancestry gate.

Decision coverage: D-01: final amendment invariant; D-19: final-state transaction; D-20: rollback/first error; D-21: idempotent conflict retry.

Purpose: Close HARN-05 and finish the phase with repository, live quality, coverage, mutation, and score evidence across every requirement.
Output: Public batch adapter/risk contract, live atomicity suite, no-skip running-Aura conversation scenarios, and complete phase gate.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-06-SUMMARY.md
@.planning/phases/49-memory-tiers/49-13-SUMMARY.md
@.planning/phases/49-memory-tiers/49-14-SUMMARY.md
@.planning/phases/49-memory-tiers/49-01-SUMMARY.md
@.planning/phases/49-memory-tiers/49-VALIDATION.md
@CLAUDE.md
@internal/arcadedb/transaction.go
@cmd/arcadedb-mcp/main.go
@scripts/agent_memory_eval.py
</context>

## Artifacts this phase produces

- `cmd/arcadedb-mcp/tool_memory_batch.go`: `memory_batch` schema/handler with exactly `upsert_fact`, `supersede_fact`, `merge_entities`, `forget`, following the existing typed memory mutation adapters.
- `internal/agent/mcptools/bridge_risk.go`: batch mutation/destructive classification and canonical replay policy, preserving `memory_recall` as the sole retrieval primitive.
- `internal/arcadedb/memory_batch_live_test.go`: concurrent isolation, rollback, ambiguous-outcome replay, and cross-identity tests.
- `cmd/arcadedb-mcp/memory_live_integration_test.go`: published-route `TestAgentMemoryMCPLive_BatchAtomicity`.
- `scripts/agent_memory_eval.py`: required all-tier scenarios and an authenticated no-skip `/agent/run` suite whose `responses` arrays enumerate all six terminal Aura answers with unique response/turn IDs, per-response scores strictly greater than 9.8, and correlated Tempo/PostgreSQL/ArcadeDB references; `scripts/agent_memory_eval_test.py` holds no-skip, empty-evidence, stale-image, correlation, per-response score, count, missing, duplicate, and extra-response failures.
- New schema fields: bounded operation enum/list and identity-bound idempotency key; model-facing identity/actor/transaction fields: none. New environment variables: none.

<tasks>

<task type="tracer" tdd="true">
  <name>49-11-T1: Publish one valid atomic batch and reject one spoofed request</name>
  <files>cmd/arcadedb-mcp/tool_memory_batch.go, cmd/arcadedb-mcp/tool_memory_batch_test.go, cmd/arcadedb-mcp/main.go, internal/agent/mcptools/bridge_risk.go, internal/agent/mcptools/bridge_memory_surface_test.go</files>
  <read_first>
    @.planning/phases/49-memory-tiers/49-RESEARCH.md
    @cmd/arcadedb-mcp/tool_memory.go
    @cmd/arcadedb-mcp/main.go
    @internal/agent/mcptools/bridge_risk.go
    @internal/agent/mcptools/bridge_memory_surface_test.go
    @internal/arcadedb/memory_batch.go
  </read_first>
  <behavior>
    - Schema accepts exactly `upsert_fact`, `supersede_fact`, `merge_entities`, and `forget`, a stable idempotency key, and bounded ordered operations.
    - Identity and actor are absent from input and derived from authenticated request/host headers; unknown fields/operations fail before mutation.
    - Handler calls `ApplyMemoryBatch` once and returns only final committed result or the unchanged first error; risk is mutating/conditionally destructive.
  </behavior>
  <action>Start RED with exact JSON-schema/enum, bounds, missing identity, actor spoof, malformed operation, one-engine-call, first-error, risk/replay, and unified-retrieval tests. In GREEN, publish one `memory_batch` MCP mutation using the contract resolved in 49-RESEARCH: `upsert_fact`, `supersede_fact`, `merge_entities`, and `forget`; require identity-bound idempotency key and cap operation count/payload. Derive identity from OAuth and actor from existing host headers, never tool arguments. Call Plan 49-06's engine once; expose no transaction/RID/intermediate state. Classify mutating and destructive when applicable while keeping `memory_recall` the sole retrieval operation.</action>
  <acceptance_criteria>
    - The public schema is bounded, identity-free, requires idempotency, and lists exactly four operation kinds.
    - One handler invocation calls ApplyMemoryBatch exactly once and returns no intermediate state.
    - Risk/replay metadata is correct and memory_recall remains the sole retrieval operation.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(MemoryBatchTool|MemoryBatchRisk|MemorySurfacePolicy_)" ./cmd/arcadedb-mcp ./internal/agent/mcptools | grep -c "^Test")" -gt 0; go test ./cmd/arcadedb-mcp ./internal/agent/mcptools -run "Test(MemoryBatchTool|MemoryBatchRisk|MemorySurfacePolicy_)" -count=1 -v; go vet ./...; go build ./...; go test ./cmd/arcadedb-mcp ./internal/agent/mcptools -count=1; go test -race ./cmd/arcadedb-mcp ./internal/agent/mcptools -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, enum/bounds differ, schema permits identity/actor injection, handler performs multiple engine calls, or risk/retrieval policy regresses.</fails_when>
  </verify>
  <done>Clients have one bounded correctly classified atomic memory batch API with host authority.</done>
</task>

<task type="auto" tdd="true">
  <name>49-11-T2: Prove rollback, concurrency, and retry over live ArcadeDB</name>
  <files>internal/arcadedb/memory_batch_live_test.go, cmd/arcadedb-mcp/memory_live_integration_test.go, scripts/agent_memory_eval.py, scripts/agent_memory_eval_test.py</files>
  <read_first>
    @internal/arcadedb/memory_batch.go
    @internal/arcadedb/transaction.go
    @internal/arcadedb/write_retry.go
    @cmd/arcadedb-mcp/memory_live_integration_test.go
    @scripts/agent_memory_eval.py
  </read_first>
  <behavior>
    - Forced late failure leaves canonical state equal to pre-batch state and returns the same first error.
    - Concurrent conflicts expose only serializable committed states and retry from fresh committed state.
    - Ambiguous-outcome same-key replay produces one logical effect/result and path telemetry after commit.
  </behavior>
  <action>Start RED with real authenticated invalid-final, concurrent-conflict, independent partial observer, cross-identity, commit-response ambiguity, and same-key/different-hash scenarios. In GREEN, add only live fixture/evaluator wiring or engine corrections evidence requires. Observe state from an independent session during conflict, compare canonical before/after state on rollback, and prove retry recompiles after the concurrent commit. Emit telemetry only after commit. Extend the evaluator batch scenario with executed-count and state hashes; missing dependencies, race, changed first error, partial state, duplicate effect, or zero scenarios fails.</action>
  <acceptance_criteria>
    - Concurrent live batches expose only valid serial outcomes and never partial/interleaved state.
    - Late failure and cross-identity attempts leave state unchanged with the exact first error.
    - Ambiguous-outcome replay returns one committed result and missing live evidence fails.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -tags=arcadedb_integration -list "^TestMemoryBatchLive_" ./internal/arcadedb | grep -c "^Test")" -gt 0; test "$(go test -tags=arcadedb_integration -list "^TestAgentMemoryMCPLive_BatchAtomicity$" ./cmd/arcadedb-mcp | grep -c "^Test")" -gt 0; go test -race -tags=arcadedb_integration -count=1 ./internal/arcadedb -run "^TestMemoryBatchLive_" -v; go test -race -tags=arcadedb_integration -count=1 ./cmd/arcadedb-mcp -run "^TestAgentMemoryMCPLive_BatchAtomicity$" -v; pylog=$(mktemp); python -m unittest scripts.agent_memory_eval_test 2&gt;&amp;1 | tee "$pylog"; grep -Eq "Ran [1-9][0-9]* tests?" "$pylog"; rm -f "$pylog"; go vet ./...; go build ./...; go test ./internal/arcadedb ./cmd/arcadedb-mcp -count=1; go test -race ./internal/arcadedb ./cmd/arcadedb-mcp -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, live dependencies are absent, rollback differs, partial/interleaved state is visible, retry/replay duplicates, or evaluator evidence is empty.</fails_when>
  </verify>
  <done>The public batch API is atomic, isolated, idempotent, and concurrency-safe live.</done>
</task>

<task type="auto" tdd="true">
  <name>49-11-T3: Run a real Aura conversation and the final MEM-06, coverage, goleak, and mutation gate</name>
  <files>scripts/agent_memory_eval.py, scripts/agent_memory_eval_test.py</files>
  <precondition>Compose runs Aura built from the current Phase 49 tree with authenticated identity/provider access, Tempo, PostgreSQL, and ArcadeDB reachable; this final acceptance intentionally has no skip-green fallback.</precondition>
  <read_first>
    @.planning/phases/49-memory-tiers/49-VALIDATION.md
    @.planning/phases/49-memory-tiers/49-01-SUMMARY.md
    @CLAUDE.md
    @Makefile
    @scripts/coverage_docker.sh
    @scripts/critical_mutation_gate.py
    @.planning/spikes/098-steer-carries-worker-result/drive.sh
    @web/e2e/chat-real-agent.spec.ts
    @.planning/phases/52-mid-turn-steering/52-08-SUMMARY.md
    @compose.yaml
  </read_first>
  <behavior>
    - Scenario `beyond_active_context_recall` emits exactly one terminal Aura answer; its sole `responses` record carries a stable response/turn ID, a 0-10 score strictly greater than 9.8, and response `retrieval.effective_path`/`retrieval.path` evidence equal to the correlated Tempo span.
    - Scenario `provider_visible_reasoning_exclusion_explicit_recall` emits exactly three terminal Aura answers: the tool-backed work answer, the later ordinary exclusion answer, and the explicit reasoning-recall answer. All three appear separately in `responses`, have unique stable IDs, score strictly greater than 9.8, and carry the evidence/correlation references appropriate to that answer.
    - Scenario `durable_shell_file_capture_later_recall` emits exactly two terminal Aura answers: the shell/file work answer and the later recall answer. Both appear separately in `responses`, have unique stable IDs, score strictly greater than 9.8, and correlate PostgreSQL `aura.tool_invocations`/`aura.conversation_turns` with ArcadeDB fact/provenance state by conversation, run, tool-call, and source IDs.
    - The six observed terminal response IDs and the six scored `responses` IDs form one exact bijection; missing, duplicate, extra, unscored, skipped, mocked, stale-image, empty, uncorrelated, or score-at-most-9.8 evidence makes the evaluator and report assertion fail.
  </behavior>
  <action>Start RED in `scripts/agent_memory_eval_test.py` with fixtures for a missing running-Aura scenario, skipped SSE turn, stale container image, mocked response, absent actual-response rubric, absent or duplicate stable response ID, and absent evidence/correlation references. Add a decisive multi-turn fixture where a later terminal answer scores at most 9.8 while a retained scenario aggregate remains above 9.8; the evaluator must fail on the weak later response. Add separate missing-response, duplicate-response-ID, extra-response, and expected-count-mismatch fixtures; each must fail even if every present score and any aggregate are high. Retain the absent/mismatched Tempo span, absent PostgreSQL tool/turn rows, absent ArcadeDB nodes/edges, missing TOUCHED, reasoning-sentinel leak into a later ordinary model request, and capture-not-durable-after-RUN_FINISHED failures. In GREEN, extend `scripts/agent_memory_eval.py` with a required `running_aura_conversation` suite, borrowing the repository's Authula login, conversation creation, authenticated SSE `/agent/run`, and real-composer assertions from the listed spike/E2E patterns. Verify the running Aura image corresponds to the current Phase 49 tree before accepting evidence; never substitute direct MCP/runner tests for these turns.

Drive three unique, no-skip scenarios through the running agent and freeze their terminal-answer counts. `beyond_active_context_recall` produces exactly one terminal answer: seed an eligible historical fact/conversation beyond the active-context ladder, ask Aura to recall it, require the answer to cite the right evidence, and require response/Tempo equality for both tier `effective_path` and actual backend `path`. `provider_visible_reasoning_exclusion_explicit_recall` produces exactly three terminal answers: select the configured provider/model path that exposes provider-visible reasoning; collect the tool-backed work answer while verifying the persisted ReasoningTrace/Step/ToolCall and TOUCHED edge in ArcadeDB; collect the later ordinary answer while inspecting its captured provider request/model-input evidence to prove the unique reasoning sentinel is absent; then collect the subsequent explicit reasoning-recall answer. `durable_shell_file_capture_later_recall` produces exactly two terminal answers: collect Aura's answer after real shell plus file work whose structured production observation reveals a unique durable fact, wait for RUN_FINISHED, then collect the later recall answer and prove direct AcceptedCapture provenance rather than model-inferred text.

For every terminal Aura answer, apply the existing 0-10 correctness/evidence rubric and require its own `actual_response_score` to be strictly greater than 9.8. Scenario averages, aggregate-only scores, or a high aggregate masking a weak response never satisfy the gate. Correlate trace/run/conversation/tool-call/source IDs across Tempo's query API, RLS-scoped PostgreSQL `aura.tool_invocations` and `aura.conversation_turns`, and identity-scoped ArcadeDB state. Emit `running_aura_conversation.executed` and exactly the three named scenarios. Each scenario records `expected_response_count`, the `terminal_response_ids` observed from the authenticated SSE run, and a `responses` array containing one record per terminal answer with stable `response_id`, `actual_response_score`, `evidence_refs`, and `correlation_refs`. Require exact counts 1/3/2, non-empty globally unique response IDs, and exact set equality between each scenario's observed terminal IDs and scored response IDs; reject missing, duplicate, extra, or unscored terminal answers. Preserve the six response records and their correlation paths in `49-11-SUMMARY.md`, along with positive `tempo`, `tool_invocations`, `conversation_turns`, and `arcadedb` evidence counts in the JSON report. Run the repository invariant again over the same enumerated reasoning paths: locate Amendment #201, prove its tree changes exactly `prd.md`, then require a non-empty earliest Phase 49 commit for each of `internal/runner/runner_reasoning_graph.go`, `internal/arcadedb/memory_reasoning.go`, `cmd/arcadedb-mcp/tool_memory_recall.go`, `internal/config/config_retention.go`, `internal/runner/runner_delete_reconcile.go`, and `cmd/aura/chat_boot_memory.go` and prove the amendment is its ancestor. Do not require these earliest hashes to be equal. Run the all-tier evaluator with the real conversation suite plus the existing past-ladder mixed recall, explicit reasoning, both production capture sources, and batch atomicity. Then run CLAUDE.md's vet/build/unit/race, WSL `make quality-full`, full tagged Docker coverage with aggregate and package-local policy, runner goleak/race cases, and critical mutation gate at at least 70%. Do not weaken thresholds or convert missing dependencies/tests into green.</action>
  <acceptance_criteria>
    - Amendment #201 changes only prd.md; every enumerated protected path has a non-empty earliest Phase 49 commit descended from it, with no cross-path equality requirement.
    - All three named running-Aura scenarios execute through authenticated `/agent/run` without mocks/skips and report exactly 1, 3, and 2 terminal answers respectively; all six observed terminal IDs map one-to-one to six globally unique `responses` records.
    - Every one of the six per-response `actual_response_score` values is strictly above 9.8; averages, aggregate-only scores, missing records, duplicate IDs, extra records, and unscored terminal answers fail.
    - Correlated Tempo, RLS-scoped `aura.tool_invocations`, `aura.conversation_turns`, and ArcadeDB evidence proves beyond-context recall, provider-visible TOUCHED reasoning with later model-input exclusion, and durable real-work capture.
    - Every supporting evaluator scenario executes with no skips or empty evidence.
    - Vet, build, unit, race, goleak, tagged/package coverage, and critical mutation gates all pass.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; amend=$(git log --format=%H --grep="Amendment #201" -n 1 -- prd.md); test -n "$amend"; test "$(git diff-tree --no-commit-id --name-only -r "$amend" | sed "/^$/d")" = "prd.md"; for p in internal/runner/runner_reasoning_graph.go internal/arcadedb/memory_reasoning.go cmd/arcadedb-mcp/tool_memory_recall.go internal/config/config_retention.go internal/runner/runner_delete_reconcile.go cmd/aura/chat_boot_memory.go; do first=$(git log --reverse --format=%H "$amend^..HEAD" -- "$p" | head -n 1); test -n "$first"; git merge-base --is-ancestor "$amend" "$first"; done; pylog=$(mktemp); python -m unittest scripts.agent_memory_eval_test 2&gt;&amp;1 | tee "$pylog"; grep -Eq "Ran [1-9][0-9]* tests?" "$pylog"; rm -f "$pylog"; python scripts/agent_memory_eval.py --tier all; python -c "import json; r=json.load(open(\"artifacts/production-readiness/agent-memory-eval-report.json\", encoding=\"utf-8\")); a=r[\"running_aura_conversation\"]; expected={\"beyond_active_context_recall\":1,\"provider_visible_reasoning_exclusion_explicit_recall\":3,\"durable_shell_file_capture_later_recall\":2}; scenarios=a[\"scenarios\"]; assert a[\"executed\"] and len(scenarios)==len(expected) and {s[\"name\"] for s in scenarios}==set(expected); by_name={s[\"name\"]:s for s in scenarios}; assert len(by_name)==len(scenarios); flat=[]; terminal=[]; [(flat.extend(s[\"responses\"]), terminal.extend(s[\"terminal_response_ids\"]), (_ for _ in ()).throw(AssertionError()) if s[\"expected_response_count\"]!=expected[name] or len(s[\"terminal_response_ids\"])!=expected[name] or len(s[\"responses\"])!=expected[name] or len(set(s[\"terminal_response_ids\"]))!=expected[name] or {x[\"response_id\"] for x in s[\"responses\"]}!=set(s[\"terminal_response_ids\"]) else None) for name,s in by_name.items()]; ids=[x[\"response_id\"] for x in flat]; assert len(flat)==6 and len(terminal)==6 and all(ids) and len(ids)==len(set(ids)) and len(terminal)==len(set(terminal)) and all(x[\"actual_response_score\"] &gt; 9.8 and x[\"evidence_refs\"] and x[\"correlation_refs\"] for x in flat) and all(a[\"evidence\"][k] &gt; 0 for k in (\"tempo\",\"tool_invocations\",\"conversation_turns\",\"arcadedb\"))"; test "$(go test -list "Test.*(Leak|Close|Barrier)" ./internal/runner | grep -c "^Test")" -gt 0; go vet ./...; go build ./...; go test ./internal/agent/tools ./internal/agent/mcptools ./internal/arcadedb ./internal/conversations ./internal/runner ./cmd/arcadedb-mcp ./cmd/aura -count=1; go test -race ./internal/agent/tools ./internal/agent/mcptools ./internal/arcadedb ./internal/conversations ./internal/runner ./cmd/arcadedb-mcp ./cmd/aura -count=1; go test -race ./internal/runner -run "Test.*(Leak|Close|Barrier)" -count=1; wsl.exe --cd /mnt/d/Repo/Aura make quality-full; wsl.exe --cd /mnt/d/Repo/Aura bash scripts/coverage_docker.sh; wsl.exe --cd /mnt/d/Repo/Aura make critical-mutation'</automated>
    <fails_when>The command exits non-zero; amendment isolation/ancestry fails for any path; scenario names or exact 1/3/2 counts differ; any observed terminal answer is missing, duplicated, extra, unscored, aggregate-only, or absent from the `responses` bijection; any per-response score is at most 9.8; the real Aura image is stale; any conversation/evaluator scenario is skipped, mocked, empty, or uncorrelated; TOUCHED/exclusion/capture evidence is absent; vet/build/unit/race/goleak fails; tagged or package coverage fails; or mutation is below 70%.</fails_when>
  </verify>
  <done>Every Phase 49 requirement has repository, unit/race, cross-store observability, coverage, mutation, and provenance evidence; the three real running-Aura scenarios expose exactly six uniquely identified terminal answers and every per-response score is strictly >9.8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| model/client -> memory_batch | Untrusted ordered operations enter a mutating identity-scoped API. |
| running Aura/Tempo/PostgreSQL/ArcadeDB -> final evaluator | Cross-system live evidence and actual model answers control whether the phase can claim success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4911-01 | Spoofing | batch identity/actor | high | mitigate | Host-only authority and identity-free schema tests. |
| T-4911-02 | Tampering | partial/intermediate state | high | mitigate | Final-only validation, one transaction, independent live observer. |
| T-4911-03 | Repudiation | retry/amendment evidence | high | mitigate | Durable idempotency plus exact git tree/ancestry checks. |
| T-4911-04 | Denial of Service | oversized batch/full gate | medium | mitigate | Request bounds and bounded tests; full gate runs once at phase end. |
| T-4911-05 | Repudiation | running-Aura conversation evidence | high | mitigate | Reject stale/mock/skip evidence and correlate run, conversation, tool, trace, and source IDs across Tempo, PostgreSQL, and ArcadeDB. |
</threat_model>

<verification>
Task 3 is the final phase gate and repeats MEM-06 isolation/ancestry plus a no-skip authenticated conversation against running Aura, exact 1/3/2 terminal-answer counts with a six-record observed-to-scored ID bijection, per-response scores strictly >9.8, correlated Tempo/PostgreSQL/ArcadeDB inspection, full tagged coverage/package policy, goleak, mutation, vet, build, unit, and race checks.
</verification>

<success_criteria>
- Public batch schema/authority/risk is exact and the live API is atomic under failure/concurrency/retry.
- Amendment #201 is prd.md-only and precedes the first change to every enumerated reasoning path.
- Three real authenticated Aura conversation scenarios prove recall, reasoning/TOUCHED exclusion, and production capture with correlated Tempo/PostgreSQL/ArcadeDB evidence; their exact 1/3/2 terminal-answer counts yield six unique per-response records and every score is strictly above 9.8.
- All supporting evaluator scenarios execute; full tagged/package coverage, goleak/race, mutation, vet, and build gates pass.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-11-SUMMARY.md` when done.
</output>
