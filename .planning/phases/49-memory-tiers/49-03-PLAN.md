---
phase: 49-memory-tiers
plan: 03
type: tdd
wave: 4
depends_on:
  - 49-07
files_modified:
  - internal/arcadedb/memory_recall.go
  - internal/arcadedb/memory_recall_test.go
  - cmd/arcadedb-mcp/tool_memory_recall.go
  - cmd/arcadedb-mcp/tool_memory_recall_test.go
  - cmd/arcadedb-mcp/tool_memory.go
autonomous: true
requirements:
  - MEM-02
  - TOOL-05
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One `memory_recall` semantic question can return fact and past-conversation evidence in one typed response (MEM-02, TOOL-05, D-09, D-10)."
    - "Facts and conversation turns are retrieved independently, fused by ArcadeDB-native deterministic rank-only RRF through `vector.fuse`, and reported as `facts`, `conversations`, or `mixed` rather than inferred from the query branch (D-07)."
    - "Conversation hits expand into bounded coherent anchor windows with stable IDs, sequence/time/rank/provenance and no duplicate fact prose (D-08)."
    - "Weak evidence produces explicit abstention; response and OTel separately report the evidence-tier contribution as `effective_path` and the actual graph/hybrid/lexical backend as `path` (D-10)."
  artifacts:
    - path: internal/arcadedb/memory_recall.go
      provides: "independent tier retrieval, ArcadeDB-native vector.fuse scoring, windows, cursors, and actual-path telemetry"
    - path: cmd/arcadedb-mcp/tool_memory_recall.go
      provides: "typed unified recall schema and mode dispatch"
  key_links:
    - from: cmd/arcadedb-mcp/tool_memory_recall.go
      to: internal/arcadedb/memory_recall.go
      via: "one input contract dispatches semantic/recent/open/scroll modes"
      pattern: "RecallMemory"
    - from: internal/arcadedb/memory_recall.go
      to: OTel and response retrieval metadata
      via: "same computed tier contribution, executed backend path, and tier counts"
      pattern: "effective_path|retrieval.path"
---

<objective>
Turn the shipped fact-only `memory_recall` into a real mixed-tier conversation-plus-fact retrieval surface with stable browse semantics and observable routing.

Purpose: Complete MEM-02 without adding sibling retrieval tools or echoing the active context.
Output: ArcadeDB-native scored-source fusion, typed MCP schema, bounded browse windows/cursors, abstention, and route metadata; active-context/live proof follows in Plans 49-08/49-13.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-02-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@.planning/phases/49-memory-tiers/49-PATTERNS.md
@cmd/arcadedb-mcp/tool_memory.go
@cmd/arcadedb-mcp/tool_memory_retrieval_test.go
@internal/agent/mcptools/bridge_actor.go
@internal/agent/mcptools/bridge_memory_surface_test.go
@internal/arcadedb/memory.go
@internal/arcadedb/memory_vector.go
</context>

<assumption_delta_decision>
  <primary_noun>memory_recall</primary_noun>
  <decision>no-change</decision>
  <rationale>The unified model-facing tool remains primary; conversation, fact, and explicitly requested reasoning are internal evidence tiers, not sibling model tools.</rationale>
</assumption_delta_decision>

## Artifacts this phase produces

- `internal/arcadedb/memory_recall.go`: `RecallMode`, `RecallRequest`, `RecallEvidence`, `RecallResult`, `RecallCursor`, `RecallMemory`, native `vector.fuse` scored-source queries, and anchor-window expansion.
- `internal/arcadedb/memory_recall_test.go`: native fused-order, dedupe, threshold, window, identity, and cursor tests; there is no Go rank-fusion helper.
- `cmd/arcadedb-mcp/tool_memory_recall.go`: `MemoryRecallInput` and `MemoryRecallOutput` with modes `semantic`, `recent`, `open`, `scroll`, and reserved explicit `reasoning` dispatch for Plan 49-04.
- Recall input fields: `mode`, `query`, `entity`, `conversation_id`, `anchor_seq`, `cursor`, `direction`, and `limit`; output fields: typed `evidence`, `abstained`, `next_cursor`, tier-contribution `retrieval.effective_path`, actual-backend `retrieval.path`, and `retrieval.{fact_count,conversation_count,reasoning_count}`.
- New OTel attributes: `memory.recall.mode`, `memory.recall.effective_path`, `memory.recall.path`, per-tier candidate/result counts, abstention reason, and backend latency. New environment flags: none.

<tasks>

<task type="tracer" tdd="true">
  <name>49-03-T1: Return one fact and one historical conversation span from one recall call</name>
  <files>internal/arcadedb/memory_recall.go, internal/arcadedb/memory_recall_test.go, cmd/arcadedb-mcp/tool_memory_recall.go, cmd/arcadedb-mcp/tool_memory_recall_test.go, cmd/arcadedb-mcp/tool_memory.go</files>
  <read_first>
    @cmd/arcadedb-mcp/tool_memory.go
    @cmd/arcadedb-mcp/tool_memory_retrieval_test.go
    @internal/arcadedb/memory.go
    @internal/arcadedb/memory_vector.go
    @internal/arcadedb/memory_conversation.go
    @internal/arcadedb/memory_integration_test.go
  </read_first>
  <behavior>
    - A semantic query with one eligible fact and one eligible historical conversation hit returns both typed evidence kinds from a single `memory_recall` invocation.
    - Independent fact/conversation scored sources fuse deterministically inside ArcadeDB `vector.fuse`; no fixed per-tier quota or Go fusion layer is applied.
    - Tier contribution reports `effective_path=mixed`; fact-only and conversation-only executions report their corresponding effective contribution while weak candidates abstain.
    - Query, entity, and forced embedding-fallback cases report the backend that actually ran as `retrieval.path=hybrid`, `graph`, and `lexical` respectively, and the matching OTel `memory.recall.path` value is identical.
  </behavior>
  <action>
Begin RED with a handler-level tracer that seeds a fact plus a prior conversation span, calls only `memory_recall`, and expects both typed evidence variants and `retrieval.effective_path=mixed`. Add engine-backed tests for fused order/ties, cross-tier dedupe, confidence thresholds, abstention, and a backend-path table covering a query with embeddings (`hybrid`), entity traversal (`graph`), and forced embedding-unavailable fallback (`lexical`). In GREEN, extract the recall handler, retain backward-compatible `query`/`entity`, and build independent fact/conversation scored-source queries passed to ArcadeDB `vector.fuse` with its required options object and `rrf` strategy per D-07, following `document_retrieval.go`; the engine is the sole fusion layer. Expand bounded anchors per D-08. Derive `retrieval.effective_path` only from evidence tiers that contributed and derive `retrieval.path` only from the backend branch actually executed, then emit `memory.recall.effective_path` and `memory.recall.path` from those same values per D-10. Assert response/OTel equality for all three backend cases; do not infer backend execution from `mixed`, create a Go `ReciprocalRankFuse` helper, fixed quota, second tool, or reasoning query.
  </action>
  <acceptance_criteria>
    - One MCP call returns typed fact and conversation evidence and reports `retrieval.effective_path=mixed` independently of its backend path.
    - Query, entity, and forced fallback tests report `retrieval.path=hybrid|graph|lexical` respectively and equal `memory.recall.path` in OTel.
    - ArcadeDB-native fused order and threshold behavior are deterministic and tested independently of text-query branch choice.
    - Browse/open/scroll identifiers and cursors are stable and bounded.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(MemoryRecallMixedTierTracer|MemoryRecallVectorFuse|MemoryRecallBackendPath|MemoryRecallAbstains)" ./internal/arcadedb ./cmd/arcadedb-mcp | grep -c "^Test")" -gt 0; go test ./internal/arcadedb ./cmd/arcadedb-mcp -run "Test(MemoryRecallMixedTierTracer|MemoryRecallVectorFuse|MemoryRecallBackendPath|MemoryRecallAbstains)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/arcadedb ./cmd/arcadedb-mcp -count=1; go test -race ./internal/arcadedb ./cmd/arcadedb-mcp -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, the tracer returns only one evidence tier, `effective_path` is not `mixed`, query/entity/fallback reports the wrong actual backend, response and OTel disagree, or weak evidence returns an unrelated hit.</fails_when>
  </verify>
  <done>A real unified recall path spans one fact and one historical conversation window end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>49-03-T2: Freeze bounded browse modes and an opaque untrusted cursor contract</name>
  <files>internal/arcadedb/memory_recall.go, internal/arcadedb/memory_recall_test.go, cmd/arcadedb-mcp/tool_memory_recall.go, cmd/arcadedb-mcp/tool_memory_recall_test.go, cmd/arcadedb-mcp/tool_memory.go</files>
  <read_first>
    @cmd/arcadedb-mcp/tool_memory.go
    @cmd/arcadedb-mcp/tool_memory_recall.go
    @internal/arcadedb/memory_conversation.go
    @cmd/arcadedb-mcp/tool_document_retrieval.go
    @.planning/phases/49-memory-tiers/49-RESEARCH.md
  </read_first>
  <behavior>
    - An omitted mode remains compatible with the shipped semantic recall behavior; explicit modes are `semantic`, `recent`, `open`, and `scroll`, with `reasoning` reserved until the reasoning contract ships.
    - `open` and `scroll` use a bounded versioned base64url encoding of canonical JSON and expose no raw ArcadeDB RID.
    - Every cursor use revalidates actor identity, conversation identity, direction, anchor, and server-side page-size caps before querying.
  </behavior>
  <action>
Start RED with table tests for omitted-mode compatibility, all public modes, capped recent windows, malformed cursors, cross-actor/cross-conversation reuse, direction mismatch, anchor mismatch, and a cursor payload scan that rejects RID-shaped values. In GREEN, extend the single `memory_recall` schema per D-08/D-09 and keep `semantic` as the omitted-mode default. Encode cursor state as versioned base64url canonical JSON containing only the bound actor/conversation identity, next stable anchor, direction, and clamped page size. The cursor is deliberately unsigned and untrusted: decode with strict size/version bounds, then revalidate every identity and query constraint server-side before use. Never deserialize a database identifier or accept client authority from the cursor. Reserve `reasoning` in the enum but return the documented not-yet-available response until Plan 49-04 connects it. REFACTOR only after every malformed and replay case fails closed.
  </action>
  <acceptance_criteria>
    - The exact public schema lists bounded `semantic`, `recent`, `open`, `scroll`, and reserved `reasoning` modes while omitted mode preserves semantic recall.
    - Cursor bytes are versioned base64url canonical JSON, bounded before decode, contain no RID, and convey no trusted authorization state.
    - Malformed, oversized, cross-identity, wrong-direction, and stale-anchor cursors fail before a retrieval query.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(MemoryRecallModeContract|RecallCursor|MemoryRecallWindow)" ./internal/arcadedb ./cmd/arcadedb-mcp | grep -c "^Test")" -gt 0; go test ./internal/arcadedb ./cmd/arcadedb-mcp -run "Test(MemoryRecallModeContract|RecallCursor|MemoryRecallWindow)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/arcadedb ./cmd/arcadedb-mcp -count=1; go test -race ./internal/arcadedb ./cmd/arcadedb-mcp -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, omitted mode changes behavior, an input bypasses a cap, a cursor carries a RID, or malformed/cross-identity cursor state reaches a query.</fails_when>
  </verify>
  <done>The unified recall tool has a stable bounded browse contract whose cursor is opaque, unsigned, untrusted, and identity-revalidated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| model -> memory_recall | Untrusted query, cursor, mode, and limits enter the retrieval API. |
| host context -> HTTP header -> MCP | Host-derived active conversation crosses process/transport boundaries. |
| ArcadeDB -> model evidence | Stored text and graph metadata re-enter model-visible output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4903-01 | Spoofing | identity/active conversation | high | mitigate | OAuth subject remains identity; active conversation is host-only HeaderFunc state and only an exclusion filter. |
| T-4903-02 | Information Disclosure | cross-identity/active-context recall | high | mitigate | Fail-closed identity predicates, whole-active-conversation exclusion, and live isolation tests. |
| T-4903-03 | Tampering | cursor/mode/limit | medium | mitigate | Typed enum validation plus bounded versioned base64url canonical JSON treated as unsigned and untrusted; strict decode bounds, deterministic clamping, and server-side actor/conversation/direction/anchor revalidation precede every query. |
| T-4903-04 | Elevation of Privilege | stored prompt injection | high | mitigate | Typed provenance, bounded evidence, no instruction authority, and no execution of recalled text. |
| T-4903-05 | Repudiation | claimed retrieval path | medium | mitigate | Derive response and OTel backend path from the same executed-backend record, keep it separate from tier contribution, and compare both axes live. |
| T-4903-06 | Denial of Service | broad hybrid search/window expansion | medium | mitigate | Per-tier candidate caps, bounded window/limit/cursor validation, and latency assertions. |
</threat_model>

<verification>
Run focused unit/TDD suites, then the live mixed-tier MCP test and evaluator. Confirm no sibling retrieval tool appears in the bridge manifest.
</verification>

<success_criteria>
- One live recall question beyond the deterministic ladder spans recent conversation and facts.
- The result and OTel evidence separately agree on tier `effective_path` and actual graph/hybrid/lexical backend `path` across query, entity, and fallback cases.
- Active context and foreign identities remain excluded, and weak evidence abstains.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-03-SUMMARY.md` when done.
</output>
