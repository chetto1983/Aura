---
phase: 49-memory-tiers
plan: 08
type: tdd
wave: 5
depends_on:
  - 49-03
files_modified:
  - internal/agent/tools/result.go
  - internal/agent/tools/result_test.go
  - internal/agent/mcptools/bridge_recall_context.go
  - internal/agent/mcptools/bridge_actor.go
  - internal/agent/mcptools/bridge_actor_test.go
  - internal/agent/mcptools/mount.go
  - cmd/arcadedb-mcp/tool_memory_recall.go
  - cmd/arcadedb-mcp/tool_memory_recall_test.go
autonomous: true
requirements:
  - MEM-02
  - TOOL-05
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The active conversation exclusion is host-derived per request on reused MCP sessions and is never model-writable (MEM-02, D-06, D-09)."
    - "Existing OAuth identity and actor headers remain intact; absent/ambiguous identity fails closed."
    - "The MCP server decodes only bounded host headers, revalidates every exclusion under the authenticated identity, and passes them only as negative filters (D-06, D-10)."
  artifacts:
    - path: internal/agent/mcptools/bridge_recall_context.go
      provides: "bounded active-source header derived from the current tool-call context"
    - path: internal/agent/mcptools/mount.go
      provides: "composition of actor and recall HeaderFunc values on every request"
    - path: cmd/arcadedb-mcp/tool_memory_recall.go
      provides: "server-side host-header decoding and active-source exclusion"
    - path: cmd/arcadedb-mcp/tool_memory_recall_test.go
      provides: "bounded server decode, identity revalidation, and exclusion-only contract"
  key_links:
    - from: internal/agent/tools/result.go
      to: internal/agent/mcptools/bridge_recall_context.go
      via: "read-only current session/source accessor"
      pattern: "SessionIDFromContext"
    - from: internal/agent/mcptools/mount.go
      to: internal/mcp/sdkclient.go
      via: "per-request SessionOptions.HeaderFunc composition"
      pattern: "HeaderFunc"
---

<objective>
Carry active context through Aura's existing trusted MCP request seam and prove the unified recall behavior live.

Decision coverage: D-06: active-context suppression; D-09: identity-scoped unified recall; D-10: honest typed path evidence.

Purpose: Complete MEM-02/TOOL-05 without a bespoke protocol, model-visible metadata, or sibling retrieval tool.
Output: Per-call header carrier, reused-session isolation tests, production mount/server decode, and exclusion-only unit contract; live/evaluator proof follows in Plan 49-13.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-03-SUMMARY.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@internal/mcp/sdkclient.go
@internal/agent/mcptools/bridge_actor.go
@internal/agent/mcptools/mount.go
@cmd/arcadedb-mcp/tool_memory_recall.go
</context>

## Artifacts this phase produces

- `internal/agent/tools/result.go`: `SessionIDFromContext`, beside `RequestIDFromContext`; analog: the read-only tool-call context accessors mapped in `49-PATTERNS.md`.
- `internal/agent/mcptools/bridge_recall_context.go`: bounded canonical active-source codec and `recallContextHeaderFunc`; analog: `bridge_actor.go`'s per-call host-derived headers.
- `internal/agent/mcptools/bridge_actor.go` and `mount.go`: deterministic actor/recall map composition through the existing `mcp.SessionOptions.HeaderFunc` seam.
- `cmd/arcadedb-mcp/tool_memory_recall.go`: bounded header decode, current-identity ownership validation, and exclusion-only propagation into `RecallMemory`.
- `cmd/arcadedb-mcp/tool_memory_recall_test.go`: fresh-per-call, malformed/over-cap/foreign-ID, actor-preservation, and no-model-input tests.
- New transport field: one bounded header carrying canonical `(conversation_id,turn_id)` source keys. It is absent from `MemoryRecallInput` and MCP `_meta`. New environment variables and schema fields: none.

<tasks>

<task type="tracer" tdd="true">
  <name>49-08-T1: Carry one active conversation through the supported per-call seam</name>
  <files>internal/agent/tools/result.go, internal/agent/tools/result_test.go, internal/agent/mcptools/bridge_recall_context.go, internal/agent/mcptools/bridge_actor.go, internal/agent/mcptools/bridge_actor_test.go</files>
  <read_first>
    @internal/agent/tools/result.go
    @internal/agent/llm_agent_tool.go
    @internal/agent/mcptools/bridge_actor.go
    @internal/agent/mcptools/mount.go
    @internal/mcp/sdkclient.go
    @C:/Users/chett/go/pkg/mod/github.com/modelcontextprotocol/go-sdk@v1.7.0/mcp/shared.go
  </read_first>
  <behavior>
    - Two calls over one reused session emit their own current conversation/source keys and never the prior call's value.
    - Recall headers compose with actor headers; bare contexts omit active-source headers and do not weaken OAuth identity checks.
    - The carrier is absent from tool input and MCP `_meta`; model arguments cannot assert it, and T2 consumes it only as an exclusion.
  </behavior>
  <action>Start RED with a read-only `SessionIDFromContext` contract, two sequential reused-session contexts, bare context, and actor-header preservation. In GREEN, expose the smallest current-session accessor beside `RequestIDFromContext`, create a bounded header codec for active `(conversation_id,turn_id)` source keys, and compose it with `actorHeaderFunc`. Use Aura's shipped `SessionOptions.HeaderFunc`, which runs for every request with its context. The installed MCP SDK inventory is resolved: `AddSendingMiddleware` carries request context, but protocol `_meta` is model-visible and rejected; no bespoke bridge is needed. T2 consumes the header only as an exclusion set and keeps OAuth identity authoritative.</action>
  <acceptance_criteria>
    - Two calls on one reused session emit only their own bounded source-key set and never stale state.
    - Recall and actor headers compose without weakening OAuth/actor behavior; bare context emits no exclusion.
    - No active-source field appears in tool arguments or MCP `_meta`, and over-cap codec input fails before transport.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(SessionIDFromContext|RecallContextHeaders)" ./internal/agent/tools ./internal/agent/mcptools | grep -c "^Test")" -gt 0; go test ./internal/agent/tools ./internal/agent/mcptools -run "Test(SessionIDFromContext|RecallContextHeaders)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/agent/tools ./internal/agent/mcptools -count=1; go test -race ./internal/agent/tools ./internal/agent/mcptools -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, a reused session sends stale keys, actor headers disappear, or model input carries the exclusion.</fails_when>
  </verify>
  <done>The supported per-request carrier transports fresh host-only active keys without a bespoke protocol or model field.</done>
</task>

<task type="auto" tdd="true">
  <name>49-08-T2: Decode and revalidate active-source exclusions on the production recall route</name>
  <files>internal/agent/mcptools/mount.go, cmd/arcadedb-mcp/tool_memory_recall.go, cmd/arcadedb-mcp/tool_memory_recall_test.go</files>
  <read_first>
    @internal/agent/mcptools/mount.go
    @cmd/arcadedb-mcp/tool_memory_recall.go
    @cmd/arcadedb-mcp/tool_memory_recall_test.go
    @cmd/arcadedb-mcp/identity.go
  </read_first>
  <behavior>
    - The memory mount composes actor and recall HeaderFunc values on every call without session-cached exclusions.
    - Server decode rejects malformed/over-cap input and revalidates every conversation/source key under the authenticated identity.
    - Valid active keys reach `RecallMemory` only as an exclusion set; foreign keys fail closed and never influence identity selection.
  </behavior>
  <action>Start RED with production mount/server tests for per-call HeaderFunc composition, bounded decoding, malformed and over-cap values, foreign IDs, and exclusion-only propagation. In GREEN, wire the composed HeaderFunc in the memory mount, decode the header in `tool_memory_recall.go`, require authenticated identity before processing it, and revalidate every referenced conversation/source key under that identity. Pass validated keys only as an exclusion set to the recall engine. Keep `memory_recall` the single retrieval operation and preserve mutation tools/risk classifications. Plan 49-13 owns the live mixed-tier and OTel/evaluator proof so this production plan stays below the scope threshold.</action>
  <acceptance_criteria>
    - Production mount calls receive actor and recall headers fresh from the current context.
    - Malformed, over-cap, or foreign-identity exclusions fail closed; valid keys suppress but never select identity.
    - Active-source data stays absent from the model-visible schema and only `memory_recall` consumes it.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(RecallContextHeaders|MemoryRecallActiveSourceHeader|MemoryRecallSuppressesActiveConversation)" ./internal/agent/mcptools ./cmd/arcadedb-mcp | grep -c "^Test")" -gt 0; go test ./internal/agent/mcptools ./cmd/arcadedb-mcp -run "Test(RecallContextHeaders|MemoryRecallActiveSourceHeader|MemoryRecallSuppressesActiveConversation)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/agent/mcptools ./cmd/arcadedb-mcp -count=1; go test -race ./internal/agent/mcptools ./cmd/arcadedb-mcp -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, the server trusts header identity, malformed/foreign exclusion input reaches a query, the active conversation returns, or the bound historical conversation is suppressed.</fails_when>
  </verify>
  <done>The production recall route consumes fresh host-only active keys as bounded identity-revalidated exclusions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host tool-call context -> MCP HTTP | Trusted active-source keys cross a reused network session. |
| MCP recall -> identity graph | Authenticated requests retrieve identity-scoped fact/conversation evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4908-01 | Spoofing | active-context carrier | high | mitigate | Host-only per-request HeaderFunc; no input/_meta field; reused-session tests. |
| T-4908-02 | Information Disclosure | mixed recall | high | mitigate | OAuth identity predicates on every source and cross-identity live fixtures. |
| T-4908-03 | Tampering | route telemetry | medium | mitigate | Compute one effective path and assert response/OTel equality. |
</threat_model>

<verification>
Run both task commands; Plan 49-13 then runs the split live published-path and evaluator proof.
</verification>

<success_criteria>
- Active source keys are per-call, host-derived, bounded, and absent from model input.
- Production recall validates and applies active exclusions without exposing them to model input.
- Mandatory vet/build/unit/race gates pass for all touched packages.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-08-SUMMARY.md` when done.
</output>
