---
phase: 49-memory-tiers
plan: 13
type: tdd
wave: 6
depends_on:
  - 49-08
files_modified:
  - cmd/arcadedb-mcp/memory_live_integration_test.go
  - scripts/agent_memory_eval.py
  - scripts/agent_memory_eval_test.py
autonomous: true
requirements:
  - MEM-02
  - TOOL-05
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A live identity-scoped memory_recall returns fact plus historical-conversation evidence and excludes the active conversation."
    - "Response metadata, OTel evidence, and evaluator output agree separately on tier-contribution `effective_path` and actual-backend `path`, including query=`hybrid`, entity=`graph`, and forced fallback=`lexical`, plus tier counts."
    - "The model-facing retrieval surface exposes only memory_recall."
  artifacts:
    - path: cmd/arcadedb-mcp/memory_live_integration_test.go
      provides: "non-skipping live mixed-tier exclusion and query/entity/fallback backend-path proof"
    - path: scripts/agent_memory_eval.py
      provides: "mixed_tier_recall evaluator scenario and route-evidence assertions"
  key_links:
    - from: cmd/arcadedb-mcp/memory_live_integration_test.go
      to: cmd/arcadedb-mcp/tool_memory_recall.go
      via: "published MCP call through authenticated production route"
      pattern: "TestAgentMemoryMCPLive_(MixedTierRecall|BackendPath)"
    - from: scripts/agent_memory_eval.py
      to: "response metadata and OTel spans"
      via: "same tier-contribution, executed-backend path, and tier-count assertions"
      pattern: "mixed_tier_recall"
---

<objective>
Prove the production mixed-tier recall route and active-context exclusion against live ArcadeDB.

Purpose: Separate live/integration proof from Plan 49-08's production carrier so every plan stays below ten files.
Output: Live MCP test plus evaluator scenario with honest route evidence and no skip-green behavior.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-08-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@cmd/arcadedb-mcp/memory_live_integration_helpers_test.go
@scripts/agent_memory_eval.py
</context>

## Artifacts this phase produces

- `cmd/arcadedb-mcp/memory_live_integration_test.go`: `TestAgentMemoryMCPLive_MixedTierRecall` plus `TestAgentMemoryMCPLive_BackendPath`, exercising the published `memory_recall` route with active-source exclusion and query/entity/fallback backend selection.
- `scripts/agent_memory_eval.py`: `mixed_tier_recall` scenario with separate `retrieval.effective_path`/`retrieval.path` response and OTel consistency checks, following the existing required-scenario evaluator analog.
- `scripts/agent_memory_eval_test.py`: fixtures for zero-scenario failure, missing route evidence, backend/effective-path disagreement, tier-count disagreement, active-context leakage, and unified-surface drift.
- New schema fields, production symbols, or environment variables: none; this plan proves the contracts from Plans 49-03 and 49-08.

<tasks>

<task type="tracer" tdd="true">
  <name>49-13-T1: Prove one live mixed-tier recall with active-context exclusion</name>
  <files>cmd/arcadedb-mcp/memory_live_integration_test.go</files>
  <read_first>
    @cmd/arcadedb-mcp/memory_live_integration_test.go
    @cmd/arcadedb-mcp/memory_live_integration_helpers_test.go
    @cmd/arcadedb-mcp/tool_memory_recall.go
  </read_first>
  <behavior>
    - One authenticated MCP call beyond the deterministic context ladder returns eligible fact and prior-conversation evidence.
    - The caller's active conversation is wholly absent while an eligible bound historical conversation remains.
    - Mixed evidence reports `retrieval.effective_path=mixed`, while query, entity, and forced embedding-fallback calls report actual `retrieval.path=hybrid`, `graph`, and `lexical` respectively.
    - Response metadata and same-call OTel attributes agree independently on `effective_path`, `path`, and counts.
  </behavior>
  <action>
Start RED by seeding one owner fact, one active conversation, one eligible historical conversation, and a foreign-identity distractor. Invoke the published memory_recall using the per-call host carrier from Plan 49-08. The mixed query case must assert both eligible tiers, `retrieval.effective_path=mixed`, total active-conversation exclusion, foreign isolation, and typed provenance. Add a live backend-path table: an embedding-capable query must run `hybrid`, an entity lookup must run `graph`, and a deliberately forced embedding-unavailable query must fall back to `lexical`. For every case, correlate the same-call span and require response `retrieval.path` to equal OTel `memory.recall.path`; also require response/OTel `effective_path` and tier counts to agree without conflating those axes. Missing ArcadeDB, absent embeddings outside the deliberate fallback case, zero assertions, or fallback to a mock must fail instead of skip-green. Preserve memory_recall as the sole model-facing retrieval operation per D-09.
  </action>
  <acceptance_criteria>
    - The live call returns both eligible tiers and no source from the active or foreign conversation.
    - Reported tier `effective_path` and tier counts equal returned evidence, independently of backend selection.
    - Query, entity, and forced fallback report actual backend `hybrid`, `graph`, and `lexical`, and each response value equals its same-call OTel value.
    - Required live dependencies missing causes a non-zero result, not a skip/pass.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -tags=arcadedb_integration -list "^TestAgentMemoryMCPLive_(MixedTierRecall|BackendPath)$" ./cmd/arcadedb-mcp/ | grep -c "^Test")" -gt 0; go test -race -tags=arcadedb_integration -count=1 -run "^TestAgentMemoryMCPLive_(MixedTierRecall|BackendPath)$" ./cmd/arcadedb-mcp/ -v; go vet ./...; go build ./...; go test ./cmd/arcadedb-mcp -count=1; go test -race ./cmd/arcadedb-mcp -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, dependencies are absent, either tier is missing, active/foreign context leaks, query/entity/fallback takes or reports the wrong backend, tier and backend axes are conflated, or response/OTel metadata disagrees.</fails_when>
  </verify>
  <done>The published route proves live mixed-tier evidence, active-source exclusion, identity isolation, and honest graph/hybrid/lexical execution reporting separate from tier contribution.</done>
</task>

<task type="auto" tdd="true">
  <name>49-13-T2: Make the evaluator reject route-evidence and surface drift</name>
  <files>scripts/agent_memory_eval.py, scripts/agent_memory_eval_test.py</files>
  <read_first>
    @scripts/agent_memory_eval.py
    @scripts/agent_memory_eval_test.py
    @internal/agent/mcptools/bridge_memory_surface_test.go
  </read_first>
  <behavior>
    - mixed_tier_recall is required and fails if no scenario executes or either evidence tier is absent.
    - Captured OTel `memory.recall.effective_path`, `memory.recall.path`, and tier counts equal response metadata and returned provenance for query, entity, and forced fallback cases.
    - A sibling model-facing retrieval tool or active-source evidence fails the evaluation.
  </behavior>
  <action>
Start RED with evaluator fixtures for zero scenarios, missing conversation/fact evidence, active-source leakage, missing either retrieval axis, swapped or conflated axes, wrong query/entity/fallback backend, OTel/result disagreement, and sibling retrieval surface. In GREEN, add mixed_tier_recall to the required scenario registry and evaluate response provenance against captured OTel attributes from the same call. Require the scenario to execute query=`hybrid`, entity=`graph`, and forced fallback=`lexical`; for each, compare response `retrieval.path` with OTel `memory.recall.path`, and separately compare tier `effective_path` plus counts. Keep thresholds/evidence deterministic; missing telemetry or any missing backend case is failure. Reuse the existing evaluator result format and unified-surface analog rather than introducing a new report channel.
  </action>
  <acceptance_criteria>
    - The evaluator succeeds only when at least one complete mixed-tier scenario executes all required query, entity, and fallback backend cases.
    - Missing/mismatched OTel `effective_path` or `path`, a wrong backend, either absent tier, active leakage, or surface drift produces non-zero failure.
    - The direct evaluator command names mixed_tier_recall and reports its tier counts plus effective/backend path evidence.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; pylog=$(mktemp); python -m unittest scripts.agent_memory_eval_test 2&gt;&amp;1 | tee "$pylog"; grep -Eq "Ran [1-9][0-9]* tests?" "$pylog"; rm -f "$pylog"; python scripts/agent_memory_eval.py --tier mixed_tier_recall; test "$(go test -list . ./cmd/arcadedb-mcp | grep -c "^Test")" -gt 0; go vet ./...; go build ./...; go test ./cmd/arcadedb-mcp -count=1; go test -race ./cmd/arcadedb-mcp -count=1'</automated>
    <fails_when>The command exits non-zero, runs zero scenarios, omits a query/entity/fallback backend case, accepts missing/mismatched or conflated path evidence, permits active-context leakage, or accepts a sibling retrieval primitive.</fails_when>
  </verify>
  <done>The evaluator independently fails closed on absent or dishonest tier contribution and actual-backend production evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live client -> published MCP route | Authenticated identity and untrusted query/context cross into recall. |
| recall -> evaluator/OTel | Production evidence is compared across response and telemetry channels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4913-01 | Information Disclosure | live recall | high | mitigate | Active and foreign source fixtures with exact negative assertions. |
| T-4913-02 | Spoofing | route evidence | medium | mitigate | Compare separate effective/backend response fields to captured spans from the same call across query, entity, and fallback. |
| T-4913-03 | Repudiation | evaluator scenario | medium | mitigate | Required named scenario and non-zero evidence counts; missing telemetry fails. |
| T-4913-04 | Denial of Service | live fixture | low | accept | Existing bounded live fixture timeout; this plan adds no production workload. |
</threat_model>

<verification>
Run the exact live race-tag command and the evaluator unit/direct commands; neither may skip on missing evidence.
</verification>

<success_criteria>
- Live recall returns facts plus historical conversation while excluding the active conversation.
- Response, provenance, and OTel separately agree on tier `effective_path`, actual graph/hybrid/lexical backend `path`, and counts for query, entity, and fallback.
- Evaluator and surface checks fail closed on drift.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-13-SUMMARY.md` when done.
</output>
