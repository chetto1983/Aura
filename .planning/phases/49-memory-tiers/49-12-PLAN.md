---
phase: 49-memory-tiers
plan: 12
type: tdd
wave: 7
depends_on:
  - 49-04
files_modified:
  - internal/runner/runner.go
  - internal/runner/runner_persist.go
  - internal/runner/runner_reasoning_graph.go
  - internal/runner/runner_reasoning_graph_test.go
  - internal/arcadedb/memory_reasoning.go
autonomous: true
requirements:
  - MEM-03
  - MEM-06
  - CTX-05
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Only provider-exposed reasoning or provider summaries already authorized for display can become a graph trace; hidden chain-of-thought and post-task synthesis have no producer (MEM-03, CTX-05, D-11)."
    - "One final answer produces one ordered Trace -> Step -> ToolCall graph linked to the initiating turn and trusted entity references (D-12, D-14)."
    - "Retry-discard removes repudiated reasoning, and graph persistence is offered only after the authoritative assistant turn commits."
    - "Amendment #201 changes only `prd.md`; every protected path already committed when this task executes has an earliest Phase 49 commit descended from it, and untouched later-plan paths are permitted (MEM-06, D-01)."
  artifacts:
    - path: internal/runner/runner_reasoning_graph.go
      provides: "provider-visible trace builder, trusted tool-event join, retry reset, and post-commit sink"
    - path: internal/runner/runner_reasoning_graph_test.go
      provides: "production event, ordering, retry-discard, entity-link, redaction, and post-commit tests"
  key_links:
    - from: internal/runner/runner_persist.go
      to: internal/runner/runner_reasoning_graph.go
      via: "commit source turn before offering the finalized trace"
      pattern: "CommitSourceTurn|ReasoningGraphSink"
    - from: internal/runner/runner_reasoning_graph.go
      to: internal/arcadedb/memory_reasoning.go
      via: "one bounded UpsertReasoningTrace call against the schema registered by 49-04"
      pattern: "UpsertReasoningTrace"
---

<objective>
Wire authorized provider reasoning and structured tool events into the registered ArcadeDB reasoning graph after source-turn commit.

Decision coverage: D-01: amendment ancestry; D-11: provider-exposed content only; D-12: ordered graph shape; D-14: bounded/redacted tool metadata and trusted entity links.

Purpose: Complete the production write path after Plan 49-04 freezes and registers the schema/read boundary.
Output: Bounded trace builder, structured tool-event join, retry reset, post-commit graph sink, and production tests.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-04-SUMMARY.md
@.planning/phases/49-memory-tiers/49-01-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@.planning/phases/49-memory-tiers/49-PATTERNS.md
@internal/runner/runner_reasoning_persist.go
@internal/runner/runner_persist.go
@internal/agent/event.go
@internal/toolinvocations/store.go
</context>

## Artifacts this phase produces

- `internal/runner/runner_reasoning_graph.go`: `ReasoningGraphSink`, `ReasoningTraceBuilder`, `ObserveReasoning`, `ObserveToolInvocation`, `CommitSourceTurn`, retry-reset, and bounded redaction helpers; analog: `runner_reasoning_persist.go`'s authorized display accumulator in `49-PATTERNS.md`.
- `internal/runner/runner_reasoning_graph_test.go`: `TestReasoningGraphTracer`, `TestReasoningGraphRetryDiscard`, and `TestReasoningGraphToolMetadata`; analog: `runner_reasoning_persist_test.go` plus the post-commit `runner_persist.go` seam.
- `internal/runner/runner_persist.go`: post-commit offer wiring from the authoritative assistant turn reference.
- `internal/arcadedb/memory_reasoning.go`: consumes `ReasoningTrace`, `ReasoningStep`, and `ReasoningToolCall` values through `UpsertReasoningTrace`; its schema fields and edges were registered in Plan 49-04.
- New producer fields: trusted source turn/run/worker IDs, provider-visible text or provider summary, step ordinal, tool name/status/duration, allowlisted argument digest, bounded redacted observation, artifact/entity refs, and content hash. New environment variables: none.

<tasks>

<task type="tracer" tdd="true">
  <name>49-12-T1: Persist one authorized provider-visible trace after its source turn</name>
  <files>internal/runner/runner.go, internal/runner/runner_persist.go, internal/runner/runner_reasoning_graph.go, internal/runner/runner_reasoning_graph_test.go, internal/arcadedb/memory_reasoning.go</files>
  <precondition>The Phase 49 Amendment #201 commit is an ancestor, changes only `prd.md`, and Plan 49-04 has registered the reasoning schema.</precondition>
  <read_first>
    @prd.md
    @.planning/phases/49-memory-tiers/49-01-SUMMARY.md
    @.planning/phases/49-memory-tiers/49-04-SUMMARY.md
    @internal/agent/event.go
    @internal/runner/runner.go
    @internal/runner/runner_persist.go
    @internal/runner/runner_reasoning_persist.go
    @internal/arcadedb/memory_reasoning.go
  </read_first>
  <behavior>
    - Provider-visible deltas or an exposed provider summary produce one ordered trace linked to the committed initiating turn.
    - A failed PostgreSQL append produces zero graph offers, while an ArcadeDB failure cannot rewrite or invalidate the committed answer.
    - Hidden/internal reasoning, the final answer, and any post-task generated summary have no producer path.
  </behavior>
  <action>Before RED, prove Amendment #201 changes only `prd.md`, then inspect each enumerated protected path independently: require ancestry for every path that already has a Phase 49 commit and permit an empty result for a path reserved to a later plan. Do not require the paths to share an earliest hash; Plan 49-11-T3 owns the final non-empty check. Add a production-event tracer for provider-visible reasoning, source-turn commit ordering, failed append, and fail-soft graph delivery. In GREEN, build a bounded `ReasoningTraceBuilder` beside the existing display accumulator and inject a narrow `ReasoningGraphSink`; accept only provider-exposed reasoning events or provider-supplied summaries per D-11. Finalize and offer the trace only after PostgreSQL commits the authoritative assistant turn. Use the Plan 49-04 types/schema and deterministic source IDs; do not derive reasoning from the final answer, tool output, hidden fields, or a new summarizer.</action>
  <acceptance_criteria>
    - Amendment #201 changes only `prd.md`; every enumerated protected path already committed at this execution point has an earliest Phase 49 commit descended from it, untouched future paths are allowed, and no cross-path hash equality is imposed.
    - One production event stream yields one ordered trace linked to the exact committed source turn.
    - Failed appends and unauthorized/synthetic sources yield zero graph writes; graph failure leaves the source answer durable.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; amend=$(git log --format=%H --grep="Amendment #201" -n 1 -- prd.md); test -n "$amend"; test "$(git diff-tree --no-commit-id --name-only -r "$amend" | sed "/^$/d")" = "prd.md"; for p in internal/runner/runner_reasoning_graph.go internal/arcadedb/memory_reasoning.go cmd/arcadedb-mcp/tool_memory_recall.go internal/config/config_retention.go internal/runner/runner_delete_reconcile.go cmd/aura/chat_boot_memory.go; do first=$(git log --reverse --format=%H "$amend^..HEAD" -- "$p" | head -n 1); test -z "$first" || git merge-base --is-ancestor "$amend" "$first"; done; test "$(go test -list "^TestReasoningGraphTracer$" ./internal/runner | grep -c "^Test")" -gt 0; go test ./internal/runner -run "^TestReasoningGraphTracer$" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner ./internal/arcadedb -count=1; go test -race ./internal/runner ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, Amendment #201 changes another file, an already-committed protected path is not descended from it, Go reports `no tests to run`, persistence precedes the source commit, an unauthorized source writes, or graph failure changes source-turn success.</fails_when>
  </verify>
  <done>The amendment-guarded production path persists one legitimate trace only after its source answer is durable.</done>
</task>

<task type="auto" tdd="true">
  <name>49-12-T2: Join bounded tool calls, touched entities, and retry-discard semantics</name>
  <files>internal/runner/runner_reasoning_graph.go, internal/runner/runner_reasoning_graph_test.go, internal/arcadedb/memory_reasoning.go</files>
  <read_first>
    @internal/agent/event.go
    @internal/runner/runner_reasoning_graph.go
    @internal/runner/runner_reasoning_persist.go
    @internal/toolinvocations/store.go
    @internal/arcadedb/memory_reasoning.go
  </read_first>
  <behavior>
    - Allowed tool events become ordered ToolCall nodes with name/status/duration, allowlisted arguments, bounded redacted observation, and source references.
    - `TOUCHED` edges use only validated trusted entity IDs from structured metadata; free-form reasoning never invents an entity link.
    - A model retry discards every repudiated step/tool/entity edge before the final trace is offered.
  </behavior>
  <action>Start RED with allowed/disallowed tool fixtures, secret/blob/oversize observations, trusted/untrusted entity references, deterministic segmentation, and a mid-stream retry. In GREEN, join structured tool events by source/call ID, apply the repository redaction helpers plus an explicit per-tool allowlist at the persistence chokepoint, bound all stored text/reference lists, and create `TOUCHED` only for validated existing entity IDs. Segment on stable content/tool boundaries, not streaming chunk boundaries. Reset the entire builder on retry exactly as `runner_reasoning_persist.go` resets display reasoning. No raw result, sidecar body, token, or inferred entity enters the graph.</action>
  <acceptance_criteria>
    - ToolCall order and fields are deterministic, bounded, redacted, and carry direct source references.
    - Secret/blob/raw-output fixtures are absent and untrusted entity references create no `TOUCHED` edge.
    - Retry-discard leaves only the final accepted attempt's steps, tools, and entity edges.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(ReasoningGraphRetryDiscard|ReasoningGraphToolMetadata|ReasoningGraphTouchedEntities)" ./internal/runner ./internal/arcadedb | grep -c "^Test")" -gt 0; go test ./internal/runner ./internal/arcadedb -run "Test(ReasoningGraphRetryDiscard|ReasoningGraphToolMetadata|ReasoningGraphTouchedEntities)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner ./internal/arcadedb -count=1; go test -race ./internal/runner ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, tool order/source changes, a sensitive/unbounded payload persists, an untrusted entity edge is created, or repudiated retry state remains.</fails_when>
  </verify>
  <done>The production trace contains only authorized bounded events and trusted entity audit edges from its final accepted attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| provider/agent events -> trace builder | Authorized and unauthorized runtime content reaches the durable-reasoning classifier. |
| trace builder -> ArcadeDB | Bounded reasoning/tool/entity evidence crosses into long-lived per-identity storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4912-01 | Information Disclosure | reasoning/tool payload | high | mitigate | Provider-visible source gate, per-tool allowlist, repository redaction, rune/list caps, and secret/blob negatives. |
| T-4912-02 | Spoofing | source turn/entity refs | high | mitigate | Host source IDs and validated existing entity refs; no model-supplied identity or free-form entity inference. |
| T-4912-03 | Tampering | retry/order | medium | mitigate | Deterministic ordinals/content hashes, whole-builder retry reset, stable source keys, and post-commit offer. |
| T-4912-04 | Repudiation | amendment/source provenance | high | mitigate | Exact Amendment #201 ancestry plus stored run/turn/tool source references. |
| T-4912-05 | Denial of Service | trace volume/backend outage | medium | mitigate | Per-trace/step/tool/reference caps and asynchronous fail-soft delivery after source commit. |
</threat_model>

<verification>
Prove Amendment #201 ancestry before production edits, then run focused producer tests plus repository vet/build/unit/race gates.
</verification>

<success_criteria>
- One authorized provider-visible trace persists after its exact source turn commits.
- Tool metadata and entity edges are bounded, redacted, direct, and retry-correct.
- Hidden/synthetic reasoning has no producer and a graph outage never invalidates the answer.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-12-SUMMARY.md` when done.
</output>
