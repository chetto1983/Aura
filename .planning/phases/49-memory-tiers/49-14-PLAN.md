---
phase: 49-memory-tiers
plan: 14
type: tdd
wave: 11
depends_on:
  - 49-10
files_modified:
  - internal/runner/runner_deps.go
  - internal/runner/runner_stop_leak_test.go
  - internal/runner/runner_memory_capture_live_test.go
  - cmd/aura/chat_boot_memory.go
  - cmd/aura/chat_boot.go
  - internal/arcadedb/memory_capture.go
autonomous: true
requirements:
  - AUTO-03
  - CTX-05
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Production boot owns exactly one capture queue/sink and bounded close joins it without leaks."
    - "Terminal success waits until every accepted sequence is durable; exhausted sink failure cannot report success (D-16)."
    - "Real structured upsert/artifact events are immediately recallable with direct provenance and no prose/reasoning bytes (AUTO-03, D-15)."
  artifacts:
    - path: cmd/aura/chat_boot_memory.go
      provides: "single capture queue/sink composition"
    - path: internal/runner/runner_memory_capture_live_test.go
      provides: "real-event terminal durability and provenance proof"
    - path: internal/runner/runner_stop_leak_test.go
      provides: "bounded drain/close ownership proof"
  key_links:
    - from: cmd/aura/chat_boot_memory.go
      to: internal/runner/runner_deps.go
      via: "one process-lifetime MemoryCaptureQueue injected into Runner"
      pattern: "MemoryCapture"
    - from: internal/runner/runner_memory_capture_live_test.go
      to: internal/arcadedb/memory_capture.go
      via: "real producer -> queue/barrier -> ApplyAcceptedCapture -> immediate recall"
      pattern: "TestMemoryCaptureLive"
---

<objective>
Compose the capture queue/sink in production and prove real structured captures are durable before terminal success.

Purpose: Complete AUTO-03 after producer and sink contracts are independently proven while keeping every plan below ten files.
Output: Production composition, bounded lifecycle, and live explicit-user/durable-artifact terminal-barrier evidence.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-05-SUMMARY.md
@.planning/phases/49-memory-tiers/49-10-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@cmd/aura/chat_boot_memory.go
@internal/runner/runner_memory_capture.go
</context>

## Artifacts this phase produces

- `cmd/aura/chat_boot_memory.go`: `buildMemoryCaptureQueue` using the shared memory boot helper analog for projection/reasoning.
- `internal/runner/runner_deps.go`: one `MemoryCaptureQueue` dependency and owned close/drain lifecycle.
- `internal/runner/runner_stop_leak_test.go`: bounded drain, repeated close, stuck sink, and goleak assertions.
- `internal/runner/runner_memory_capture_live_test.go`: explicit user upsert, `write_file`/`patch` durable artifact, terminal barrier, immediate recall, and negative sentinel tests.
- New schema fields or environment variables: none; this plan composes `AcceptedCapture`, `FlushThrough`, and `ApplyAcceptedCapture` from Plans 49-05/49-10 using existing boot/close patterns.

<tasks>

<task type="tracer" tdd="true">
  <name>49-14-T1: Compose one process-lifetime queue and bounded close</name>
  <files>internal/runner/runner_deps.go, internal/runner/runner_stop_leak_test.go, cmd/aura/chat_boot_memory.go, cmd/aura/chat_boot.go, internal/arcadedb/memory_capture.go</files>
  <read_first>
    @internal/runner/runner_deps.go
    @internal/runner/runner_stop_leak_test.go
    @cmd/aura/chat_boot_memory.go
    @cmd/aura/chat_boot.go
    @internal/runner/runner_memory_capture.go
  </read_first>
  <behavior>
    - Boot constructs/injects exactly one queue backed by ApplyAcceptedCapture when the memory graph is configured.
    - Normal stop drains through the last accepted watermark; stuck/exhausted persistence produces bounded failure and no false success.
    - Repeated close is safe and no configuration path invents a prose/reasoning fallback.
  </behavior>
  <action>
Start RED with construction/injection, normal drain, stuck backend, failure propagation, repeated close, absent-memory, and goleak cases. In GREEN, extend the existing chat_boot_memory composition helper and runner.Deps with one MemoryCaptureQueue backed by ApplyAcceptedCapture. Join it to the runner lifecycle used by projection/reasoning, preserve Plan 49-05 FlushThrough semantics, propagate bounded exhaustion as failure, and close once. Per D-16, accepted work is never abandoned behind successful terminal completion. No configured graph means no queue and no fallback producer.
  </action>
  <acceptance_criteria>
    - Exactly one queue/sink is created and injected per configured process lifetime.
    - Normal stop drains all accepted sequences; stuck/exhausted sink returns bounded failure and cannot report success.
    - Repeated close is leak-free and absent memory creates no fallback capture path.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(MemoryCaptureBoot|MemoryCaptureClose|MemoryCaptureSinkFailure)" ./internal/runner ./cmd/aura | grep -c "^Test")" -gt 0; go test ./internal/runner ./cmd/aura -run "Test(MemoryCaptureBoot|MemoryCaptureClose|MemoryCaptureSinkFailure)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner ./cmd/aura -count=1; go test -race ./internal/runner ./cmd/aura -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, multiple queues exist, accepted work remains after successful close, failure is hidden, or a goroutine leaks.</fails_when>
  </verify>
  <done>Production owns one bounded capture queue/sink with truthful failure and shutdown behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>49-14-T2: Prove real structured captures durable before completion live</name>
  <files>internal/runner/runner_memory_capture_live_test.go, internal/arcadedb/memory_capture.go, cmd/aura/chat_boot_memory.go</files>
  <read_first>
    @internal/runner/runner_memory_capture.go
    @internal/agent/llm_agent_events.go
    @internal/agent/tools/write_file.go
    @internal/agent/tools/patch.go
    @cmd/arcadedb-mcp/tool_memory.go
  </read_first>
  <behavior>
    - A real successful memory__memory_upsert_fact event is durable/recallable before terminal completion returns.
    - A real successful write_file or patch durable_artifact event persists direct tool-call/artifact provenance.
    - Result preview, final prose, summary, and reasoning sentinels never persist or appear as provenance.
  </behavior>
  <action>
Drive the production agent event/runner/queue/ArcadeDB path, never Queue.Accept directly. The explicit-user scenario invokes the existing structured memory__memory_upsert_fact call and proves the host-bound user-turn ref; the reliable-tool scenario executes write_file or patch and proves exact durable_artifact mapping. Hold terminal completion until immediate recall observes both accepted sequences. Put unique sentinels only in preview/final/summary/reasoning and assert none is stored. Missing ArcadeDB/identity, zero real ToolInvocationEnd events, a synthetic producer, or timed-out flush is failure.
  </action>
  <acceptance_criteria>
    - Both real production event classes are immediately recallable before terminal success.
    - Stored provenance equals direct user-turn/tool-call/artifact refs and includes no generated/reasoning source.
    - Missing live dependencies, synthetic bypass, or terminal-before-durable ordering fails.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -tags=arcadedb_integration -list "^TestMemoryCaptureLive_(ExplicitUserEvent|DurableArtifactEvent|TerminalBarrier)$" ./internal/runner | grep -c "^Test")" -gt 0; go test -race -tags=arcadedb_integration -count=1 ./internal/runner -run "^TestMemoryCaptureLive_(ExplicitUserEvent|DurableArtifactEvent|TerminalBarrier)$" -v; go vet ./...; go build ./...; go test ./internal/runner ./internal/arcadedb -count=1; go test -race ./internal/runner ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, production events are bypassed, terminal completion wins, immediate recall misses, provenance changes, or sentinel/generated/reasoning text persists.</fails_when>
  </verify>
  <done>AUTO-03 is proven live from exact structured production events through durability and immediate recall.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| production events -> capture queue | Structured evidence becomes accepted durable work. |
| capture queue -> ArcadeDB | Accepted direct evidence crosses into persistent identity-scoped memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4914-01 | Spoofing | actor/source | high | mitigate | Real host events and exact direct-ref assertions; no model identity fields. |
| T-4914-02 | Tampering | terminal barrier | high | mitigate | Watermark ordering and race/live proof that durability precedes success. |
| T-4914-03 | Information Disclosure | generated/reasoning capture | high | mitigate | Adversarial unique sentinels with complete absence assertions. |
| T-4914-04 | Denial of Service | sink/close | medium | mitigate | Bounded failure/close plus race and goleak tests. |
</threat_model>

<verification>
Run composition unit/race gates and the exact live production-event barrier test; neither may skip on missing evidence.
</verification>

<success_criteria>
- One production queue/sink drains and closes without leaks.
- Terminal success cannot precede durability or hide an exhausted sink.
- Real direct events persist; generated prose and reasoning never do.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-14-SUMMARY.md` when done.
</output>
