---
phase: 49-memory-tiers
plan: 04
type: tdd
wave: 6
depends_on:
  - 49-08
files_modified:
  - internal/arcadedb/memory.go
  - internal/arcadedb/memory_test.go
  - internal/arcadedb/memory_reasoning.go
  - internal/arcadedb/memory_reasoning_test.go
  - cmd/arcadedb-mcp/tool_memory_recall.go
  - cmd/arcadedb-mcp/tool_memory_recall_test.go
  - internal/config/config_retention.go
  - internal/config/config_retention_test.go
autonomous: true
requirements:
  - MEM-03
  - CTX-05
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "EnsureMemorySchema installs identity-isolated ReasoningTrace/ReasoningStep/ReasoningToolCall schema before any reasoning write or recall can run (MEM-03, D-11, D-12)."
    - "Only explicit memory_recall mode reasoning queries reasoning; automatic recall and context assembly cannot (CTX-05, D-13)."
    - "Successful reasoning traces retain for exactly 30 days while failed/cancelled traces retain for exactly 7 days, subject to earlier source deletion (D-14)."
    - "The Amendment #201 commit changes only `prd.md`; for each protected reasoning path already committed when this task executes, its earliest Phase 49 commit descends from the amendment, while untouched future-plan paths are permitted."
  artifacts:
    - path: internal/arcadedb/memory.go
      provides: "EnsureMemorySchema aggregation of reasoningSchemaStatements"
    - path: internal/arcadedb/memory_reasoning.go
      provides: "reasoning schema, bounded explicit recall, and status-aware expiry fields"
    - path: cmd/arcadedb-mcp/tool_memory_recall.go
      provides: "explicit reasoning dispatch behind the unified tool"
    - path: internal/config/config_retention.go
      provides: "typed exact 30-day success and 7-day failed/cancelled retention policy"
  key_links:
    - from: internal/arcadedb/memory.go
      to: internal/arcadedb/memory_reasoning.go
      via: "EnsureMemorySchema appends reasoningSchemaStatements before writers/readers start"
      pattern: "reasoningSchemaStatements"
    - from: cmd/arcadedb-mcp/tool_memory_recall.go
      to: internal/arcadedb/memory_reasoning.go
      via: "only mode=reasoning reaches SearchReasoningTraces/GetReasoningTrace"
      pattern: "SearchReasoning"
    - from: internal/config/config_retention.go
      to: internal/arcadedb/memory_reasoning.go
      via: "validated 30d/7d status policy sets terminal expires_at"
      pattern: "ReasoningRetention"
---

<objective>
Install the reasoning graph schema, make it explicitly retrievable, and freeze its exact retention contract.

Purpose: Give the production builder in Plan 49-12 a schema-initialized, bounded, identity-safe destination while satisfying MEM-03 and CTX-05.
Output: EnsureMemorySchema registration, explicit reasoning recall, exact 30-day/7-day retention, ancestry gates, and focused tests.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-08-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@.planning/phases/49-memory-tiers/49-PATTERNS.md
@internal/arcadedb/memory.go
@internal/arcadedb/memory_conversation.go
@cmd/arcadedb-mcp/tool_memory_recall.go
</context>

## Artifacts this phase produces

- `internal/arcadedb/memory_reasoning.go`: `reasoningSchemaStatements`, `ReasoningTrace`, `ReasoningStep`, `ReasoningToolCall`, `UpsertReasoningTrace`, `SearchReasoningTraces`, `GetReasoningTrace`, and status-aware expiry.
- `internal/arcadedb/memory.go`: `EnsureMemorySchema` registration of `reasoningSchemaStatements`, following the existing document/fact schema aggregation analog and the conversation registration established in Plan 49-07.
- ArcadeDB vertices `ReasoningTrace`, `ReasoningStep`, `ReasoningToolCall`; edges `INITIATED_BY`, `HAS_STEP`, `NEXT`, `INVOKED`, `TOUCHED`; indexed identity/source/status/expiry fields.
- `cmd/arcadedb-mcp/tool_memory_recall.go`: live `reasoning` mode dispatch on the existing unified `memory_recall` schema.
- `internal/config/config_retention.go`: `ReasoningSuccessTTL` (exactly 30 days) and `ReasoningFailedTTL` (exactly 7 days), following the existing typed retention analog. Environment catalog wiring follows in Plan 49-09.

<tasks>

<task type="tracer" tdd="true">
  <name>49-04-T1: Register the reasoning schema and prove amendment ancestry</name>
  <files>internal/arcadedb/memory.go, internal/arcadedb/memory_test.go, internal/arcadedb/memory_reasoning.go, internal/arcadedb/memory_reasoning_test.go</files>
  <read_first>
    @internal/arcadedb/memory.go
    @internal/arcadedb/memory_conversation.go
    @.planning/phases/49-memory-tiers/49-PATTERNS.md
    @.planning/phases/49-memory-tiers/49-VALIDATION.md
  </read_first>
  <behavior>
    - EnsureMemorySchema executes every statement returned by reasoningSchemaStatements before reasoning store/search is usable.
    - The fragment declares all three vertex types, all five edges, source/identity/status/expiry fields, and required indexes idempotently.
    - Amendment #201 changes only `prd.md`; every protected reasoning path already present in Phase 49 history has an earliest Phase 49 commit descended from it, while untouched paths assigned to later plans remain valid.
  </behavior>
  <action>
Start RED with TestEnsureMemorySchemaRegistersReasoningSchema using the schema recorder analog from memory_test.go, plus focused fragment assertions for vertices, edges, fields, indexes, and repeat-safe statements. In GREEN, create reasoningSchemaStatements beside its store and append it in EnsureMemorySchema; no reasoning operation may own a separate schema-init path. Model identity and source references as required fields, keep status/expiry indexed, and follow the existing idempotent DDL statement style. Per D-11/D-12, schema accepts provider-visible bounded evidence and entity links but no hidden-chain field. Add the intermediate Amendment #201 guard from VALIDATION: prove its diff-tree contains only `prd.md`, then independently inspect each enumerated protected path. If a path already has a Phase 49 commit, prove its earliest such commit descends from the amendment; if a later plan has not touched it yet, permit the empty result. Do not require protected paths to share a commit hash; Plan 49-11-T3 owns the final non-empty gate after all producers land.
  </action>
  <acceptance_criteria>
    - A recorder test proves EnsureMemorySchema invokes the complete reasoning fragment exactly through the shared initializer.
    - Re-running every reasoning schema statement is safe and leaves the expected vertices, edges, fields, and indexes available.
    - The intermediate gate proves amendment isolation and ancestry for each already-committed protected path, permits untouched future-plan paths, and imposes no cross-path hash equality.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; amend=$(git log --format=%H --grep="Amendment #201" -n 1 -- prd.md); test -n "$amend"; test "$(git diff-tree --no-commit-id --name-only -r "$amend" | sed "/^$/d")" = "prd.md"; for p in internal/runner/runner_reasoning_graph.go internal/arcadedb/memory_reasoning.go cmd/arcadedb-mcp/tool_memory_recall.go internal/config/config_retention.go internal/runner/runner_delete_reconcile.go cmd/aura/chat_boot_memory.go; do first=$(git log --reverse --format=%H "$amend^..HEAD" -- "$p" | head -n 1); test -z "$first" || git merge-base --is-ancestor "$amend" "$first"; done; test "$(go test -list "Test(EnsureMemorySchemaRegistersReasoningSchema|ReasoningSchemaStatements)" ./internal/arcadedb | grep -c "^Test")" -gt 0; go test ./internal/arcadedb -run "Test(EnsureMemorySchemaRegistersReasoningSchema|ReasoningSchemaStatements)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/arcadedb -count=1; go test -race ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, a schema element is absent/non-idempotent, EnsureMemorySchema omits the fragment, Amendment #201 is absent or changes a file besides `prd.md`, or an already-committed protected path has an earliest Phase 49 commit that is not descended from the amendment.</fails_when>
  </verify>
  <done>The shared memory initializer installs the full reasoning schema, amendment isolation is exact, and every protected path committed so far proves amendment-before-implementation ancestry without constraining future paths.</done>
</task>

<task type="auto" tdd="true">
  <name>49-04-T2: Enforce explicit-only reasoning retrieval and bounded metadata</name>
  <files>internal/arcadedb/memory_reasoning.go, internal/arcadedb/memory_reasoning_test.go, cmd/arcadedb-mcp/tool_memory_recall.go, cmd/arcadedb-mcp/tool_memory_recall_test.go</files>
  <read_first>
    @internal/arcadedb/memory_reasoning.go
    @cmd/arcadedb-mcp/tool_memory_recall.go
    @internal/conversations/compaction.go
    @.planning/phases/49-memory-tiers/49-CONTEXT.md
  </read_first>
  <behavior>
    - Only the explicit public reasoning mode dispatches to reasoning search/traversal and requires actor ownership.
    - Semantic/recent/open/scroll recall and ordinary context/compaction paths make zero reasoning-store calls.
    - Results expose bounded provider-visible summaries, tool status/duration and allowlisted reference metadata, never hidden chain-of-thought, secrets, blobs, or raw unbounded output.
  </behavior>
  <action>
Start RED with spies around ordinary recall modes and explicit-owner/foreign-owner reasoning calls, plus boundary tests for every stored/result field. In GREEN, connect the reserved reasoning enum from Plan 49-03 to SearchReasoningTraces/GetReasoningTrace only in the explicit branch, with mandatory actor/source predicates and bounded result counts. Per D-11/D-13/D-14, store and return only provider-visible summaries, step order, tool name/status/duration, allowed argument digest, bounded redacted observation, and validated artifact/entity references. Do not add reasoning to fact search, deterministic context preload, compaction, summarization, or fact extraction. REFACTOR only after the ordinary-mode spy count remains zero.
  </action>
  <acceptance_criteria>
    - Owner explicit reasoning recall returns a bounded ordered graph; foreign/missing identity fails closed.
    - Every ordinary recall mode and automatic context consumer performs zero reasoning queries.
    - Tests reject hidden reasoning, secrets, blobs, unrestricted arguments, and observations above the configured cap.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(ReasoningRecallExplicitOnly|ReasoningToolMetadataBounded|ReasoningRecallIdentity)" ./internal/arcadedb ./cmd/arcadedb-mcp | grep -c "^Test")" -gt 0; go test ./internal/arcadedb ./cmd/arcadedb-mcp -run "Test(ReasoningRecallExplicitOnly|ReasoningToolMetadataBounded|ReasoningRecallIdentity)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/arcadedb ./cmd/arcadedb-mcp -count=1; go test -race ./internal/arcadedb ./cmd/arcadedb-mcp -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, an ordinary consumer invokes reasoning, a foreign identity sees a trace, or sensitive/unbounded metadata persists or returns.</fails_when>
  </verify>
  <done>Reasoning is reachable only by an explicit owner request and remains bounded, redacted, and absent from automatic context.</done>
</task>

<task type="auto" tdd="true">
  <name>49-04-T3: Freeze exact 30-day success and 7-day failed/cancelled retention</name>
  <files>internal/config/config_retention.go, internal/config/config_retention_test.go, internal/arcadedb/memory_reasoning.go, internal/arcadedb/memory_reasoning_test.go</files>
  <read_first>
    @internal/config/config_retention.go
    @internal/arcadedb/memory_reasoning.go
    @.planning/phases/49-memory-tiers/49-RESEARCH.md
  </read_first>
  <behavior>
    - A successful trace receives exactly 30 days from terminal status; failed and cancelled traces receive exactly 7 days.
    - Source/conversation/identity deletion can shorten retention immediately; no status or override can lengthen the configured class.
    - Invalid, zero, or negative values fail typed validation instead of silently widening retention.
  </behavior>
  <action>
Start RED with a fake clock and tables for success, failure, cancellation, invalid values, and earlier source expiry. In GREEN, extend the existing typed RetentionConfig analog with ReasoningSuccessTTL=30*24h and ReasoningFailedTTL=7*24h; failed and cancelled share the shorter class. Set terminal expires_at once from the persisted status and clamp it to any earlier authoritative source expiry. Per D-14, no automatic access refresh or indefinite retention is allowed. Plan 49-09 catalogs validated operator values and owns the worker. REFACTOR status-to-TTL selection into one tested helper.
  </action>
  <acceptance_criteria>
    - Exact-duration tests prove 30 days for success and 7 days for failed/cancelled terminal states.
    - Earlier source deletion/expiry remains authoritative and no read extends expiry.
    - Invalid duration inputs fail typed validation.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(ReasoningRetentionPolicy|ReasoningTerminalExpiry)" ./internal/config ./internal/arcadedb | grep -c "^Test")" -gt 0; go test ./internal/config ./internal/arcadedb -run "Test(ReasoningRetentionPolicy|ReasoningTerminalExpiry)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/config ./internal/arcadedb -count=1; go test -race ./internal/config ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, a duration differs from success=30d or failed/cancelled=7d, a read extends expiry, or invalid configuration passes.</fails_when>
  </verify>
  <done>Reasoning status selects exact 30-day success or 7-day failed/cancelled retention with earlier source deletion authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shared initializer -> ArcadeDB | Idempotent DDL establishes the schema trusted by every reasoning reader/writer. |
| explicit recall -> reasoning graph | Untrusted mode/query input can reach sensitive provider-visible evidence only after identity checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4904-01 | Information Disclosure | reasoning graph | high | mitigate | Bounded provider-visible allowlist, redaction, identity predicates, and zero-query automatic-path spies. |
| T-4904-02 | Spoofing | actor/source links | high | mitigate | Host identity plus required source IDs; foreign/missing identity tests fail closed. |
| T-4904-03 | Tampering | schema/expiry | medium | mitigate | Shared idempotent registration, exact fake-clock duration tests, immutable source/created cap. |
| T-4904-04 | Elevation of Privilege | automatic reinjection | high | mitigate | Only explicit mode dispatch can reach the store; every other mode is spy-tested for zero calls. |
| T-4904-05 | Denial of Service | query/payload volume | medium | mitigate | Bounded traversal depth, result counts, field sizes, and retention. |
</threat_model>

<verification>
Run the ancestry/schema gate, explicit-isolation suite, exact retention tables, then Go vet/build/unit/race gates named by each task.
</verification>

<success_criteria>
- EnsureMemorySchema installs the complete reasoning schema before use.
- Explicit owner reasoning recall works while every automatic context path stays reasoning-free.
- Retention is exactly 30 days for success and 7 days for failed/cancelled traces.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-04-SUMMARY.md` when done.
</output>
