---
phase: 49-memory-tiers
plan: 04
type: tdd
wave: 4
depends_on:
  - 49-03
files_modified:
  - internal/runner/runner.go
  - internal/runner/runner_deps.go
  - internal/runner/runner_persist.go
  - internal/runner/runner_reasoning_graph.go
  - internal/runner/runner_reasoning_graph_test.go
  - internal/arcadedb/memory_reasoning.go
  - internal/arcadedb/memory_reasoning_test.go
  - internal/arcadedb/memory_reasoning_live_test.go
  - cmd/arcadedb-mcp/tool_memory_recall.go
  - cmd/arcadedb-mcp/tool_memory_recall_test.go
  - internal/conversations/history_reasoning_free_test.go
  - internal/runner/runner_delete_reconcile.go
  - cmd/aura/chat_boot_memory.go
  - cmd/aura/chat_boot.go
autonomous: true
requirements:
  - MEM-03
  - CTX-05
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Provider-exposed reasoning and bounded tool events persist as identity-isolated ArcadeDB Trace/Step/ToolCall graphs with ordered steps and entity edges (MEM-03, D-11, D-12)."
    - "Only an explicit `memory_recall` reasoning mode can query reasoning; ordinary semantic recall, context preload, compaction, summarization, and fact extraction structurally cannot (CTX-05, D-13)."
    - "Hidden chain-of-thought is never reconstructed, and stored tool metadata is bounded/redacted with no secrets, blobs, or unbounded raw output (D-11, D-14)."
    - "Deleting the PostgreSQL source turn/conversation removes its derived reasoning graph; it has no independent authority or retention (D-02, D-03)."
  artifacts:
    - path: internal/runner/runner_reasoning_graph.go
      provides: "provider-visible trace builder and post-commit graph sink"
    - path: internal/arcadedb/memory_reasoning.go
      provides: "Trace/Step/ToolCall persistence, entity edges, explicit search/traversal, and deletion"
    - path: cmd/arcadedb-mcp/tool_memory_recall.go
      provides: "explicit reasoning mode behind the unified recall tool"
    - path: internal/conversations/history_reasoning_free_test.go
      provides: "non-vacuous CTX-05 structural exclusion contract"
  key_links:
    - from: internal/runner/runner_persist.go
      to: internal/runner/runner_reasoning_graph.go
      via: "accepted provider-visible events assembled then offered only after source turn commit"
      pattern: "reasoningGraph"
    - from: cmd/arcadedb-mcp/tool_memory_recall.go
      to: internal/arcadedb/memory_reasoning.go
      via: "only `mode=reasoning` reaches reasoning search/traversal"
      pattern: "SearchReasoning"
    - from: internal/conversations/history_reasoning_free_test.go
      to: internal/conversations/compaction.go
      via: "history/compaction requests contain no graph reasoning evidence"
      pattern: "ReasoningFree"
---

<objective>
Persist an extended reasoning graph in ArcadeDB and make it retrievable only by explicit request through the unified recall surface.

Purpose: Complete MEM-03 and turn CTX-05 from a structurally pinned but vacuous contract into a graph-resident isolation proof.
Output: Trace graph builder/store, explicit reasoning recall, deletion propagation, live isolation and redaction tests.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-01-SUMMARY.md
@.planning/phases/49-memory-tiers/49-03-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@.planning/phases/49-memory-tiers/49-PATTERNS.md
@internal/agent/event.go
@internal/runner/runner_reasoning_persist.go
@internal/runner/runner_persist.go
@internal/toolinvocations/store.go
@internal/conversations/store_reasoning.go
</context>

## Artifacts this phase produces

- `internal/runner/runner_reasoning_graph.go`: `ReasoningGraphSink`, `ReasoningTraceBuilder`, `ObserveReasoning`, `ObserveToolInvocation`, `CommitSourceTurn`, and bounded redaction helpers.
- `internal/runner/runner_reasoning_graph_test.go`: ordering, retry-discard, post-commit, provider-visible-only, and redaction tests.
- `internal/arcadedb/memory_reasoning.go`: `ReasoningTrace`, `ReasoningStep`, `ReasoningToolCall`, `UpsertReasoningTrace`, `SearchReasoningTraces`, `GetReasoningTrace`, and `DeleteReasoningBySource`.
- New ArcadeDB vertex types: `ReasoningTrace`, `ReasoningStep`, `ReasoningToolCall`; edges: `INITIATED_BY`, `HAS_STEP`, `NEXT`, `INVOKED`, and `TOUCHED`.
- New graph fields: identity/source turn IDs, provider-visible text/provider summary, ordinal, timestamps, tool name/status/duration, allowed-argument digest, bounded redacted observation, referenced artifact/entity IDs, content hash, and deletion source.
- `cmd/aura/chat_boot_memory.go`: one composition helper shared by conversation projection, reasoning graph, and later capture wiring.
- Recall mode `reasoning` becomes live. New environment flags: none; honor existing reasoning-display/persistence cap and memory backend configuration.

<tasks>

<task type="tracer" tdd="true">
  <name>49-04-T1: Persist one provider-visible reasoning trace with a touched entity</name>
  <files>internal/runner/runner.go, internal/runner/runner_persist.go, internal/runner/runner_reasoning_graph.go, internal/runner/runner_reasoning_graph_test.go, internal/arcadedb/memory_reasoning.go</files>
  <precondition>The Phase 49 Amendment #181 commit is an ancestor, changes only `prd.md`, and predates this task; stop before writing code if the gate fails.</precondition>
  <read_first>
    @prd.md
    @.planning/phases/49-memory-tiers/49-01-SUMMARY.md
    @internal/agent/event.go
    @internal/runner/runner.go
    @internal/runner/runner_persist.go
    @internal/runner/runner_reasoning_persist.go
    @internal/arcadedb/memory.go
    @internal/toolinvocations/store.go
  </read_first>
  <behavior>
    - Provider-exposed reasoning deltas and one allowed tool event produce one ordered Trace -> Step -> ToolCall graph linked to the initiating turn and one touched entity.
    - A mid-stream retry discards repudiated reasoning before persistence.
    - Hidden/internal chain-of-thought and post-task generated summaries have no input path.
  </behavior>
  <action>
First prove the amendment gate from the recorded 49-01 hash and current git ancestry. Start RED with a runner tracer for provider-visible reasoning, an allowed tool invocation, a touched entity reference, retry-discard, and post-commit ordering. In GREEN, add a bounded `ReasoningTraceBuilder` beside the existing display accumulator and a narrow `ReasoningGraphSink`; feed only provider-exposed reasoning events or provider-supplied summaries per D-11 plus observed tool events. Materialize the D-12 graph shape with ordered Step nodes, ToolCall nodes, initiation/source links, and `TOUCHED` edges to existing entities. Offer graph persistence only after the authoritative PostgreSQL assistant turn commits; failures are observable/reconcilable and do not rewrite the answer. Do not derive reasoning from the final answer, tool output, a new summarizer, or any hidden provider field. REFACTOR after the tracer passes.
  </action>
  <acceptance_criteria>
    - The first reasoning implementation commit is a descendant of the isolated Amendment #181 commit.
    - One production event stream produces an ordered graph with source-turn and entity edges.
    - Retry-discard removes repudiated spans, and no synthetic reasoning source exists.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'amend=$(git log --format=%H --grep="Amendment #181" -n 1); test -n "$amend" &amp;&amp; git merge-base --is-ancestor "$amend" HEAD &amp;&amp; go test ./internal/runner -run "^TestReasoningGraphTracer$" -count=1 -v'</automated>
    <fails_when>The command exits non-zero, the amendment hash is absent/not an ancestor, Go reports `no tests to run`, ordering/source/entity edges are missing, or repudiated/synthetic reasoning is persisted.</fails_when>
  </verify>
  <done>The amendment-guarded production path persists one legitimate reasoning trace graph after its source turn commits.</done>
</task>

<task type="auto" tdd="true">
  <name>49-04-T2: Enforce explicit-only retrieval, bounded metadata, and context isolation</name>
  <files>internal/arcadedb/memory_reasoning.go, internal/arcadedb/memory_reasoning_test.go, cmd/arcadedb-mcp/tool_memory_recall.go, cmd/arcadedb-mcp/tool_memory_recall_test.go, internal/conversations/history_reasoning_free_test.go</files>
  <read_first>
    @internal/arcadedb/memory_reasoning.go
    @cmd/arcadedb-mcp/tool_memory_recall.go
    @internal/conversations/history_reasoning_free_test.go
    @internal/conversations/context.go
    @internal/conversations/compaction.go
    @internal/runner/runner_context.go
  </read_first>
  <behavior>
    - Only `memory_recall` with explicit `mode=reasoning` can invoke reasoning similarity/traversal.
    - Semantic/recent/open/scroll recall, preload, context build, compaction, summarization, and fact extraction never call the reasoning store.
    - Tool metadata stores an allowlisted bounded argument summary and redacted bounded observation; secret-like values, blobs, sidecar bodies, and raw unbounded output never persist.
  </behavior>
  <action>
Start RED with a spy reasoning store across every recall mode and non-recall consumer, plus secret/oversize/tool-argument fixtures. In GREEN, implement explicit reasoning similarity and entity traversal behind only the D-13 `mode=reasoning` branch. Keep reasoning evidence typed and distinct in the unified result; ordinary mixed semantic recall remains fact+conversation only. Apply D-14 allowlists, byte/rune caps, repository redaction helpers, artifact/reference extraction, and `TOUCHED` links before the graph boundary. Strengthen the existing history exclusion test so graph-resident reasoning makes CTX-05 non-vacuous: history, compaction, summarization, and fact extraction must remain structurally incapable of loading it. Do not add a preload knob for reasoning. REFACTOR after every negative-path spy stays untouched.
  </action>
  <acceptance_criteria>
    - Explicit reasoning recall returns trace evidence; all ordinary consumers make zero reasoning-store calls.
    - Stored tool events contain allowed bounded metadata and references, with secret/blob/raw payload fixtures absent.
    - CTX-05 remains true with actual graph-resident reasoning present.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/arcadedb ./cmd/arcadedb-mcp ./internal/conversations -run 'Test(ReasoningRecallExplicitOnly|ReasoningToolMetadataBounded|HistoryReasoningFree)' -count=1 -v</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, an ordinary consumer invokes reasoning, a sensitive/unbounded payload persists, or graph reasoning appears in rebuilt model context.</fails_when>
  </verify>
  <done>Reasoning is useful on explicit request and unreachable from every automatic context/fact path.</done>
</task>

<task type="auto" tdd="true">
  <name>49-04-T3: Wire graph lifecycle, deletion, and live explicit-isolation proof</name>
  <files>internal/runner/runner_deps.go, internal/runner/runner_delete_reconcile.go, internal/arcadedb/memory_reasoning_live_test.go, cmd/aura/chat_boot_memory.go, cmd/aura/chat_boot.go</files>
  <read_first>
    @internal/runner/runner_deps.go
    @internal/runner/runner_delete_reconcile.go
    @internal/runner/runner_delete.go
    @internal/arcadedb/memory_vector_live_test.go
    @cmd/aura/chat_boot.go
    @cmd/arcadedb-mcp/memory_live_integration_test.go
  </read_first>
  <behavior>
    - Production boot injects one reasoning graph sink and drains it with the runner lifecycle.
    - Deleting a source turn/conversation/identity removes its traces, steps, tool calls, and orphanable edges idempotently.
    - A live ordinary recall/context rebuild excludes a seeded reasoning trace, while an explicit reasoning recall returns it to the owner only.
  </behavior>
  <action>
Start RED with boot wiring, source deletion, repeated deletion, cross-identity, and live ordinary-vs-explicit retrieval tests. In GREEN, add the reasoning sink to `runner.Deps`, construct it through `chat_boot_memory.go`, and join existing close/reconciliation lifecycles. Per D-02/D-03, source deletion propagates to every derived reasoning vertex/edge and missing graph records are idempotent success. Keep graph failure fail-soft for the committed source turn but visible for reconciliation. Prove the explicit-only boundary with a live trace: ordinary recall and rebuilt context exclude it; the owner's explicit reasoning mode retrieves it; a second identity cannot. REFACTOR composition helpers after live/race tests pass.
  </action>
  <acceptance_criteria>
    - Production creates/injects/closes one sink without goroutine leaks.
    - Source deletion removes the complete trace subgraph and repeated delete converges.
    - Live ordinary consumers exclude reasoning and explicit owner recall includes it.
  </acceptance_criteria>
  <verify>
    <automated>go test -race -tags=arcadedb_integration -count=1 ./internal/arcadedb/ -run '^TestReasoningGraphLive_' -v</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, required live dependencies are absent, any trace survives source deletion, ordinary context contains reasoning, or a foreign identity retrieves it.</fails_when>
  </verify>
  <done>Graph reasoning is production-wired, source-bound, live-retrievable only explicitly, and absent from later injected context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| provider/agent events -> trace builder | Provider-visible reasoning and tool telemetry enter durable processing. |
| trace builder -> ArcadeDB | Potentially sensitive text/arguments/observations cross into long-lived graph storage. |
| explicit recall -> model context | Stored reasoning can re-enter the model only through an explicit user/tool request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4904-01 | Information Disclosure | reasoning/tool graph | high | mitigate | Provider-visible allowlist, pre-storage redaction/caps, no blobs/raw sidecars, identity predicates, and live foreign-identity tests. |
| T-4904-02 | Spoofing | reasoning source/entity links | high | mitigate | Host event/source-turn IDs and validated existing entity refs; never model-supplied identity or hidden-source claims. |
| T-4904-03 | Tampering | trace order/retry | medium | mitigate | Ordinal/content hashes, retry-discard reset, idempotent source keys, and post-commit offer. |
| T-4904-04 | Elevation of Privilege | automatic reasoning reinjection | high | mitigate | Single explicit mode dispatch; spy tests across every automatic context/summarization/fact path. |
| T-4904-05 | Repudiation | tool event provenance | medium | mitigate | Store name/status/duration/source IDs and bounded evidence refs with trace ordering. |
| T-4904-06 | Denial of Service | trace/tool payload volume | medium | mitigate | Per-turn/per-step/tool caps, bounded observation/reference lists, asynchronous fail-soft sink, and lifecycle flush bounds. |
</threat_model>

<verification>
Before implementation, assert Amendment #181 ancestry. Then run unit/TDD isolation suites and live graph persistence/deletion/explicit-recall tests.
</verification>

<success_criteria>
- Git history proves the amendment predates reasoning implementation.
- Extended reasoning persists as an identity-isolated entity-linked graph.
- Later automatic context remains reasoning-free; explicit owner retrieval works.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-04-SUMMARY.md` when done.
</output>
