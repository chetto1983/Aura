---
phase: 49-memory-tiers
plan: 07
type: tdd
wave: 3
depends_on:
  - 49-02
  - 49-06
files_modified:
  - internal/arcadedb/memory.go
  - internal/arcadedb/memory_test.go
  - internal/runner/runner.go
  - internal/runner/runner_deps.go
  - internal/runner/runner_persist.go
  - internal/runner/runner_memory_projection_test.go
  - internal/runner/runner_delete_reconcile.go
  - cmd/aura/chat_boot.go
autonomous: true
requirements:
  - MEM-01
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every eligible user/final-assistant turn is offered only after its PostgreSQL commit; projection failure never invalidates the source turn (MEM-01, D-02, D-04)."
    - "One ordered process-lifetime projector drains and closes without leaks, preserving per-conversation source order (D-04)."
    - "Boot-one-shot and periodic paged reconciliation repair crash-after-source-commit gaps from PostgreSQL without duplicates; no transactional outbox is introduced (D-02, D-04)."
    - "Every production `EnsureMemorySchema` call initializes the Conversation/Turn schema fragment before projection or recall can use it."
  artifacts:
    - path: internal/runner/runner_persist.go
      provides: "post-commit eligible-turn offer seam"
    - path: internal/runner/runner_delete_reconcile.go
      provides: "boot and periodic projection reconciliation lifecycle"
    - path: cmd/aura/chat_boot.go
      provides: "single production projector construction and shutdown wiring"
    - path: internal/arcadedb/memory.go
      provides: "shared EnsureMemorySchema aggregation of the conversation schema fragment"
  key_links:
    - from: internal/runner/runner_persist.go
      to: internal/runner/runner_memory_projection.go
      via: "offer after successful PostgreSQL append only"
      pattern: "offerConversationProjection"
    - from: internal/runner/runner_delete_reconcile.go
      to: internal/conversations/store_projection.go
      via: "bounded authoritative source paging at boot and interval"
      pattern: "ReconcileConversationProjection"
    - from: internal/arcadedb/memory.go
      to: internal/arcadedb/memory_conversation.go
      via: "EnsureMemorySchema executes conversationSchemaStatements after 49-06 completes its memory.go work"
      pattern: "conversationSchemaStatements"
---

<objective>
Wire the tested projection engine into the production commit and lifecycle seams, including deterministic crash recovery.

Decision coverage: D-02: source authority; D-04: post-commit ordered projection and reconciliation.

Purpose: Make MEM-01 operational without changing PostgreSQL authority or inventing an outbox.
Output: Conversation schema aggregation, post-commit runner wiring, one boot-owned projector, bounded boot/periodic reconciliation, and crash/goleak tests.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-02-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@internal/runner/runner.go
@internal/runner/runner_persist.go
@internal/runner/runner_delete_reconcile.go
@cmd/aura/chat_boot.go
</context>

## Artifacts this phase produces

- `internal/arcadedb/memory.go`: `EnsureMemorySchema` calls the `conversationSchemaStatements` fragment defined by `internal/arcadedb/memory_conversation.go`; analog: its existing aggregation of `memorySchemaStatements` and `vectorSchemaStatements` in `49-PATTERNS.md`.
- `internal/arcadedb/memory_test.go`: `TestEnsureMemorySchemaRegistersConversationSchema` proves the real boot entry emits every Conversation/Turn type, property, relation, full-text/vector index, and stays idempotent.
- `internal/runner/runner_persist.go`: `offerConversationProjection` runs only after the committed eligible source ref exists; analog: the post-commit persistence seam mapped in `49-PATTERNS.md`.
- `internal/runner/runner_delete_reconcile.go`: `ReconcileConversationProjection` owns boot-one-shot and periodic bounded replay; analog: `DeleteReconciler.Start`/`Stop`.
- `cmd/aura/chat_boot.go`: one `ConversationProjector` construction and joined shutdown owner.
- New symbols registered here: `EnsureMemorySchema -> conversationSchemaStatements`, `offerConversationProjection`, and `ReconcileConversationProjection`. New schema fields are those frozen in 49-02 (`identity_id`, `conversation_id`, `turn_seq`, `role`, `content`, `content_hash`, `occurred_at`, `source_ref`, `deleted_at`, `embedding`, high-water metadata); no new environment variable is introduced.

<tasks>

<task type="tracer" tdd="true">
  <name>49-07-T1: Initialize the conversation projection schema through the production aggregator</name>
  <files>internal/arcadedb/memory.go, internal/arcadedb/memory_test.go</files>
  <read_first>
    @internal/arcadedb/memory.go
    @internal/arcadedb/memory_test.go
    @internal/arcadedb/memory_conversation.go
    @.planning/phases/49-memory-tiers/49-06-SUMMARY.md
  </read_first>
  <behavior>
    - `EnsureMemorySchema` executes the complete `conversationSchemaStatements` fragment after the existing fact/vector fragments.
    - Two schema initializations emit only idempotent DDL and leave the same complete Conversation/Turn schema.
    - A missing Conversation/Turn type, property, relation, or retrieval index fails the aggregate schema test.
  </behavior>
  <action>Start RED with `TestEnsureMemorySchemaRegistersConversationSchema` against the real recording client used by existing schema tests. After confirming Plan 49-06 has completed, register Plan 49-02's domain-owned `conversationSchemaStatements()` inside `EnsureMemorySchema`; keep DDL definitions in `memory_conversation.go` and aggregation only in `memory.go`, matching the existing `vectorSchemaStatements` pattern. Assert all D-02 through D-05 source-key fields, `HAS_TURN`/`NEXT_TURN`, and full-text/vector retrieval indexes are emitted and every CREATE remains replay-safe. This task is sequenced after 49-06 specifically to avoid concurrent `memory.go` ownership.</action>
  <acceptance_criteria>
    - `EnsureMemorySchema` invokes `conversationSchemaStatements` exactly once per initialization.
    - The schema test enumerates every conversation vertex/edge/property/index required by 49-02 and fails if any registration disappears.
    - Repeated initialization remains idempotent and the existing fact/vector schema assertions stay green.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(EnsureMemorySchemaRegistersConversationSchema|SchemaIsIdempotentAndCarriesTheVectorIndex|SchemaAvoidsReservedWords)" ./internal/arcadedb | grep -c "^Test")" -gt 0; go test ./internal/arcadedb -run "Test(EnsureMemorySchemaRegistersConversationSchema|SchemaIsIdempotentAndCarriesTheVectorIndex|SchemaAvoidsReservedWords)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/arcadedb -count=1; go test -race ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, the conversation fragment is absent or incomplete, any DDL is non-idempotent, or existing fact/vector schema coverage regresses.</fails_when>
  </verify>
  <done>The production schema entry point initializes the complete conversation projection schema before any boot-owned projector starts.</done>
</task>

<task type="auto" tdd="true">
  <name>49-07-T2: Offer one committed production turn and survive a graph failure</name>
  <files>internal/runner/runner.go, internal/runner/runner_deps.go, internal/runner/runner_persist.go, internal/runner/runner_memory_projection_test.go, cmd/aura/chat_boot.go</files>
  <read_first>
    @internal/runner/runner.go
    @internal/runner/runner_deps.go
    @internal/runner/runner_persist.go
    @internal/runner/runner_stop_leak_test.go
    @cmd/aura/chat_boot.go
  </read_first>
  <behavior>
    - User and terminal assistant projection offers occur after the authoritative append succeeds and never on failed appends.
    - Tool/system/summary/reasoning events never offer projection work.
    - A graph failure is observable and retryable but the already committed turn still succeeds; shutdown joins the worker.
  </behavior>
  <action>Start RED with append-before-offer, failed-append, ineligible-event, graph-failure, per-conversation order, and close/goleak cases. In GREEN, inject one `ConversationProjector` through `runner.Deps`, offer only after successful user/final-assistant PostgreSQL persistence, and construct/close exactly one instance in the existing chat composition root. Per D-02/D-04, the graph is derived and fail-soft: record lag for reconciliation without converting a committed source turn into failure. Do not enqueue reasoning, tool turns, pause scaffolding, compaction output, or unsuccessful appends. Reuse existing lifecycle ownership; add no detached goroutine or package.</action>
  <acceptance_criteria>
    - Eligible offers occur after a successful PostgreSQL append and never before or after a failed append.
    - Ineligible events create zero projection work, while an ArcadeDB failure leaves the source turn successful and observable as lag.
    - Exactly one boot-owned projector preserves per-conversation order and joins without a goroutine leak.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(ConversationProjectionPostCommit|ConversationProjectionFailSoft|ChatBootMemoryProjection)" ./internal/runner ./cmd/aura | grep -c "^Test")" -gt 0; go test ./internal/runner ./cmd/aura -run "Test(ConversationProjectionPostCommit|ConversationProjectionFailSoft|ChatBootMemoryProjection)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner ./cmd/aura -count=1; go test -race ./internal/runner ./cmd/aura -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, offer ordering is wrong, an ineligible event is offered, a graph failure fails the source turn, or shutdown leaks.</fails_when>
  </verify>
  <done>One real committed turn traverses the production projector seam without weakening source durability.</done>
</task>

<task type="auto" tdd="true">
  <name>49-07-T3: Converge a crash-after-commit gap through boot and periodic reconciliation</name>
  <files>internal/runner/runner_delete_reconcile.go, internal/runner/runner_memory_projection_test.go, cmd/aura/chat_boot.go</files>
  <read_first>
    @internal/runner/runner_delete_reconcile.go
    @internal/runner/runner_delete.go
    @internal/conversations/store_projection.go
    @internal/runner/runner_memory_projection.go
    @cmd/aura/chat_boot.go
  </read_first>
  <behavior>
    - A turn committed to PostgreSQL immediately before process loss appears in ArcadeDB after the next boot reconciliation.
    - Replaying the same bounded page does not duplicate records and periodic reconciliation advances from authoritative source state.
    - Source paging/convergence failure is observable; it does not authorize an outbox or mark the turn invalid.
  </behavior>
  <action>Start RED with an injected crash after PostgreSQL commit but before projection offer, restart, repeated-page, edit/delete, and periodic-lag fixtures. In GREEN, extend the existing `DeleteReconciler` ownership pattern with a boot-one-shot plus interval paged projection reconciliation using Plan 49-02's source cursor and idempotent apply methods. PostgreSQL is the complete replay source per D-02; store only derived progress. Keep the worker bounded, context-owned, and joined. The research decision is closed: no transactional outbox. If the authoritative store cannot page or the live crash fixture cannot converge, fail this task and report evidence rather than adding a new persistence protocol.</action>
  <acceptance_criteria>
    - Boot reconciliation repairs the crash-window source row and repeated pages remain duplicate-free.
    - Periodic replay applies edits/deletes from bounded PostgreSQL pages and advances only derived progress.
    - Paging/convergence failures are observable and the worker closes within its bound without introducing an outbox.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(ConversationProjectionCrashRecovery|ConversationProjectionBootReconcile|ConversationProjectionPeriodicReconcile)" ./internal/runner ./cmd/aura | grep -c "^Test")" -gt 0; go test ./internal/runner ./cmd/aura -run "Test(ConversationProjectionCrashRecovery|ConversationProjectionBootReconcile|ConversationProjectionPeriodicReconcile)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner ./cmd/aura -count=1; go test -race ./internal/runner ./cmd/aura -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, restart leaves a committed source turn missing, replay duplicates, source paging is unbounded, or reconciliation leaks.</fails_when>
  </verify>
  <done>Production projection converges after crash/restart and periodic lag using only the PostgreSQL authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PostgreSQL commit -> projector | Authoritative source turns cross into a derived asynchronous system. |
| reconciler -> ArcadeDB | Bounded identity-scoped source pages create/update/delete derived graph records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4907-01 | Tampering | post-commit ordering | high | mitigate | Offer only after successful append; tests cover every eligible/ineligible path. |
| T-4907-02 | Denial of Service | projector/reconciler lifecycle | medium | mitigate | Bounded queue/pages/retry/close with goleak and race tests. |
| T-4907-03 | Information Disclosure | cross-identity replay | high | mitigate | Identity-scoped source paging and graph apply predicates. |
| T-4907-04 | Tampering | schema aggregation | high | mitigate | `EnsureMemorySchema` registration test enumerates the conversation fragment and preserves idempotent boot initialization. |
</threat_model>

<verification>
Run both task commands. At the wave gate run the live crash-after-source-commit projection fixture; missing services are a failure, not a skip-green result.
</verification>

<success_criteria>
- Production commit paths offer only eligible turns after PostgreSQL success.
- Crash/restart and periodic reconciliation converge idempotently without an outbox.
- Mandatory vet/build/unit/race gates pass for runner and Aura packages.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-07-SUMMARY.md` when done.
</output>
