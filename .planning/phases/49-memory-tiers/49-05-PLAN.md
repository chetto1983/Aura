---
phase: 49-memory-tiers
plan: 05
type: tdd
wave: 9
depends_on:
  - 49-09
files_modified:
  - internal/runner/runner.go
  - internal/runner/runner_persist.go
  - internal/runner/runner_memory_capture.go
  - internal/runner/runner_memory_capture_test.go
  - internal/arcadedb/memory.go
  - internal/agent/tools/write_file.go
  - internal/agent/tools/patch.go
autonomous: true
requirements:
  - AUTO-03
  - CTX-05
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A durable fact explicitly revealed by the user or a reliable allowlisted tool during shell/file work is accepted with direct provenance and becomes durable before terminal completion (AUTO-03, D-15, D-16)."
    - "Accepted captures drain through one ordered serial queue; terminal completion waits through a bounded durability barrier and surfaces failure rather than claiming success early (D-16)."
    - "Duplicates enrich provenance, contradictions create temporal state, and only the principal may supersede; workers cannot overwrite the principal (D-17, D-18)."
    - "Hypotheses, temporary task state, secrets, assistant-generated conclusions, and any reasoning text are structurally ineligible capture sources (D-15, CTX-05)."
  artifacts:
    - path: internal/runner/runner_memory_capture.go
      provides: "accepted-capture classifier, ordered queue, and terminal durability barrier"
    - path: internal/arcadedb/memory_capture.go
      provides: "direct-provenance duplicate/contradiction/authority write semantics"
    - path: internal/runner/runner_memory_capture_live_test.go
      provides: "mid-task durable-fact-before-completion proof"
  key_links:
    - from: internal/runner/runner_persist.go
      to: internal/runner/runner_memory_capture.go
      via: "user/tool evidence accepted at event time, never derived from final reasoning"
      pattern: "AcceptMemoryCapture"
    - from: internal/runner/runner_memory_capture.go
      to: internal/arcadedb/memory_capture.go
      via: "ordered serial sink with direct source refs and host actor"
      pattern: "ApplyAcceptedCapture"
    - from: terminal runner completion
      to: internal/runner/runner_memory_capture.go
      via: "FlushThrough accepted sequence before success is returned"
      pattern: "FlushThrough"
---

<objective>
Capture durable facts revealed during work through a direct-evidence ordered queue whose accepted writes are durable before completion.

Purpose: Close the genuinely open AUTO-03 queue/barrier requirement without laundering facts through reasoning or assistant summarization.
Output: Production AcceptedCapture producers, closed source allowlist, ordered queue, and terminal durability barrier.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-04-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@.planning/phases/49-memory-tiers/49-PATTERNS.md
@internal/runner/runner_persist.go
@internal/agent/event.go
@internal/agent/tools/result.go
@internal/agent/mcptools/bridge_actor.go
@internal/arcadedb/memory.go
@internal/arcadedb/fact_lock.go
</context>

## Artifacts this phase produces

- `internal/runner/runner_memory_capture.go`: `CaptureSourceKind`, `AcceptedCapture`, `MemoryCaptureSink`, `MemoryCaptureQueue`, `Accept`, `FlushThrough`, `Close`, and eligibility/redaction functions.
- Capture fields: stable idempotency key, identity, host-derived actor run/role, source kind, conversation/tool-call/source refs, fact subject/predicate/object, confidence, observed time, and accepted sequence.
- `internal/agent/tools/write_file.go` and `patch.go`: successful `ToolResult.Meta.durable_artifact` producers; analog: existing structured tool-result metadata, never preview/prose parsing.
- Existing `memory__memory_upsert_fact` success becomes the canonical explicit-fact producer; `runner_persist.go` consumes the typed result via its existing post-event seam.
- `internal/runner/runner_memory_capture_test.go`: exact-producer positives, closed-source negatives, ordering, retry-discard, terminal barrier, and close tests.
- New environment variables and graph schema fields: none. Graph application follows in Plan 49-10 and production composition/live proof in Plan 49-14.

<tasks>

<task type="tracer" tdd="true">
  <name>49-05-T1: Emit AcceptedCapture from explicit facts and durable tool artifacts</name>
  <files>internal/runner/runner_memory_capture.go, internal/runner/runner_memory_capture_test.go, internal/agent/tools/write_file.go, internal/agent/tools/patch.go</files>
  <read_first>
    @internal/runner/runner_memory_capture.go
    @internal/agent/event.go
    @internal/agent/tools/result.go
    @internal/agent/tools/write_file.go
    @internal/agent/tools/patch.go
  </read_first>
  <behavior>
    - A successful memory__memory_upsert_fact result emits one AcceptedCapture with the canonical accepted fact and direct user/tool-call provenance.
    - Successful write_file and patch calls emit AcceptedCapture only when ToolResult.Meta marks a durable artifact, preserving tool-call and artifact references.
    - Shell/read/document/prose/reasoning outputs and failed or temporary tool results emit no AcceptedCapture.
  </behavior>
  <action>
Start RED with producer tests around the actual successful memory__memory_upsert_fact, write_file, and patch completion paths plus negative tables for failed calls, shell/read/document results, assistant prose, reasoning, secrets, and temporary artifacts. In GREEN, define AcceptedCapture at the runner boundary and emit it from the canonical accepted-fact result and from ToolResult.Meta durable_artifact on the two allowlisted filesystem tools. Per D-15/D-18, copy host actor/run/parent-worker identity and direct source references; never derive a capture by parsing assistant text or reasoning. Keep the allowlist closed and attach artifact identity/confidence without copying unrestricted tool output. REFACTOR common producer construction only after each named production path has a positive assertion and every negative source has zero emissions.
  </action>
  <acceptance_criteria>
    - Each of the three named production paths emits exactly one typed AcceptedCapture on success.
    - Direct source, artifact, actor, run, and worker-role provenance survives unchanged.
    - Every non-allowlisted, failed, temporary, prose, or reasoning source emits zero captures.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(AcceptedCaptureProducer|MemoryUpsertAcceptedCapture|DurableArtifactAcceptedCapture)" ./internal/runner ./internal/agent/tools | grep -c "^Test")" -gt 0; go test ./internal/runner ./internal/agent/tools -run "Test(AcceptedCaptureProducer|MemoryUpsertAcceptedCapture|DurableArtifactAcceptedCapture)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner ./internal/agent/tools -count=1; go test -race ./internal/runner ./internal/agent/tools -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, a named producer emits zero/multiple events, provenance changes, or an excluded source emits an event.</fails_when>
  </verify>
  <done>AcceptedCapture has concrete positive producers on all three allowlisted production result paths and none on excluded sources.</done>
</task>

<task type="auto" tdd="true">
  <name>49-05-T2: Queue accepted captures in order and block terminal success through durability</name>
  <files>internal/runner/runner.go, internal/runner/runner_persist.go, internal/runner/runner_memory_capture.go, internal/runner/runner_memory_capture_test.go, internal/arcadedb/memory.go</files>
  <read_first>
    @internal/runner/runner.go
    @internal/runner/runner_persist.go
    @internal/runner/runner_memory_capture.go
    @internal/arcadedb/memory.go
  </read_first>
  <behavior>
    - Accepted captures receive monotonic sequence numbers and reach one bounded serial queue in accepted order.
    - Terminal success waits for FlushThrough(lastAcceptedSequence); exhausted persistence failure returns failure rather than false success.
    - Retry/discard and shutdown cannot leak a goroutine or transform an excluded event into a capture.
  </behavior>
  <action>
Begin RED with ordered multi-event, terminal race, bounded failure, retry-discard, and stop tests. In GREEN, add one MemoryCaptureQueue owned by Runner, assign sequence at acceptance, and make the existing success path call FlushThrough(lastAcceptedSequence) per D-16 before reporting completion. The queue invokes the injected graph sink serially and carries the original AcceptedCapture unchanged. An exhausted sink error or timeout must fail the turn; normal stop drains through the accepted watermark, while discarded retry attempts cannot publish. Do not add background best-effort completion or a post-task summarizer. REFACTOR lifecycle joins only after race tests remain deterministic.
  </action>
  <acceptance_criteria>
    - Durable writes occur in accepted sequence order and before terminal success.
    - Sink failure/timeout blocks success with a bounded error and normal stop drains without leaks.
    - Retry-discard publishes no event from the abandoned attempt.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(MemoryCaptureQueueOrder|MemoryCaptureTerminalBarrier|MemoryCaptureRetryDiscard|MemoryCaptureStop)" ./internal/runner | grep -c "^Test")" -gt 0; go test ./internal/runner -run "Test(MemoryCaptureQueueOrder|MemoryCaptureTerminalBarrier|MemoryCaptureRetryDiscard|MemoryCaptureStop)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner -count=1; go test -race ./internal/runner -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, completion precedes durability, accepted order changes, a discarded attempt publishes, or shutdown leaks.</fails_when>
  </verify>
  <done>The runner durably drains every accepted capture through its watermark before terminal success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user/tool events -> capture classifier | Untrusted or transient observations are considered for durable fact promotion. |
| capture queue -> ArcadeDB facts | Accepted evidence crosses into durable identity-scoped memory. |
| worker actor -> principal memory | Delegated workers may supply evidence but must not acquire principal supersession authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4905-01 | Spoofing | capture actor/source | high | mitigate | Host-derived actor and direct source refs; model cannot set identity/run/role/source kind. |
| T-4905-02 | Elevation of Privilege | worker supersession | high | mitigate | Graph-boundary principal-only transition check with negative worker tests. |
| T-4905-03 | Information Disclosure | secret/transient capture | high | mitigate | Source allowlist, secret redaction/rejection before enqueue and at sink, no reasoning/generated sources. |
| T-4905-04 | Tampering | duplicate/contradiction state | high | mitigate | Stable idempotency keys, provenance sets, temporal validity transitions, fact locks, and conflict retries. |
| T-4905-05 | Repudiation | capture provenance | medium | mitigate | Persist conversation/tool/artifact refs, actor run/role, confidence, accepted sequence, and timestamps. |
| T-4905-06 | Denial of Service | durability barrier | medium | mitigate | Ordered bounded queue, bounded retries/flush, explicit terminal failure, and joined shutdown. |
</threat_model>

<verification>
Run classifier/queue and graph authority tests, then the live mid-task terminal-barrier scenario under race detection.
</verification>

<success_criteria>
- A durable fact revealed mid shell/file task is captured before completion.
- Provenance points directly to the user/tool source and never to reasoning summarization.
- Replays, contradictions, worker activity, failures, and shutdown preserve the locked semantics.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-05-SUMMARY.md` when done.
</output>
