---
phase: 49-memory-tiers
plan: 05
type: tdd
wave: 5
depends_on:
  - 49-04
files_modified:
  - internal/runner/runner.go
  - internal/runner/runner_deps.go
  - internal/runner/runner_persist.go
  - internal/runner/runner_memory_capture.go
  - internal/runner/runner_memory_capture_test.go
  - internal/runner/runner_memory_capture_live_test.go
  - internal/arcadedb/memory_capture.go
  - internal/arcadedb/memory_capture_test.go
  - internal/arcadedb/memory.go
  - internal/arcadedb/fact_lock.go
  - internal/arcadedb/write_retry.go
  - cmd/aura/chat_boot_memory.go
  - cmd/aura/chat_boot.go
  - internal/runner/runner_stop_leak_test.go
autonomous: true
requirements:
  - AUTO-03
  - CTX-05
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A durable fact explicitly revealed by the user or a reliable allowlisted tool during shell/file work is accepted with direct provenance and becomes durable before terminal completion (AUTO-03, D-15, D-16)."
    - "Accepted captures drain through one ordered serial queue; terminal completion waits through a bounded durability barrier and surfaces failure rather than claiming success early (D-16)."
    - "Duplicates enrich provenance, contradictions create temporal state, and only the principal may supersede; workers cannot overwrite the principal (D-17, D-18)."
    - "Hypotheses, temporary task state, secrets, assistant-generated conclusions, and any reasoning text are structurally ineligible capture sources (D-15, CTX-05)."
  artifacts:
    - path: internal/runner/runner_memory_capture.go
      provides: "accepted-capture classifier, ordered queue, and terminal durability barrier"
    - path: internal/arcadedb/memory_capture.go
      provides: "direct-provenance duplicate/contradiction/authority write semantics"
    - path: internal/runner/runner_memory_capture_live_test.go
      provides: "mid-task durable-fact-before-completion proof"
  key_links:
    - from: internal/runner/runner_persist.go
      to: internal/runner/runner_memory_capture.go
      via: "user/tool evidence accepted at event time, never derived from final reasoning"
      pattern: "AcceptMemoryCapture"
    - from: internal/runner/runner_memory_capture.go
      to: internal/arcadedb/memory_capture.go
      via: "ordered serial sink with direct source refs and host actor"
      pattern: "ApplyAcceptedCapture"
    - from: terminal runner completion
      to: internal/runner/runner_memory_capture.go
      via: "FlushThrough accepted sequence before success is returned"
      pattern: "FlushThrough"
---

<objective>
Capture durable facts revealed during work through a direct-evidence ordered queue whose accepted writes are durable before completion.

Purpose: Close the genuinely open AUTO-03 queue/barrier requirement without laundering facts through reasoning or assistant summarization.
Output: Capture eligibility boundary, serial queue, authority-aware graph writes, composition wiring, and live terminal durability proof.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-04-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@.planning/phases/49-memory-tiers/49-PATTERNS.md
@internal/runner/runner_persist.go
@internal/agent/event.go
@internal/agent/tools/result.go
@internal/agent/mcptools/bridge_actor.go
@internal/arcadedb/memory.go
@internal/arcadedb/fact_lock.go
</context>

## Artifacts this phase produces

- `internal/runner/runner_memory_capture.go`: `CaptureSourceKind`, `AcceptedCapture`, `MemoryCaptureSink`, `MemoryCaptureQueue`, `Accept`, `FlushThrough`, `Close`, and eligibility/redaction functions.
- Capture fields: stable idempotency key, identity, host-derived actor run/role, source kind, conversation/tool-call/source refs, fact subject/predicate/object, confidence, observed time, and accepted sequence.
- `internal/arcadedb/memory_capture.go`: `ApplyAcceptedCapture`, duplicate provenance enrichment, contradiction temporal transitions, and principal-only supersession enforcement.
- `internal/runner/runner_memory_capture_test.go`: eligibility, ordering, retry, barrier, duplicate, contradiction, and worker-authority tests.
- `internal/runner/runner_memory_capture_live_test.go`: shell/file-task evidence capture and terminal barrier test.
- New environment flags: none. The queue/barrier is mandatory when the existing memory graph sink is configured; no reasoning-derived fallback exists.

<tasks>

<task type="tracer" tdd="true">
  <name>49-05-T1: Capture one explicit mid-task fact before the terminal answer returns</name>
  <files>internal/runner/runner.go, internal/runner/runner_persist.go, internal/runner/runner_memory_capture.go, internal/runner/runner_memory_capture_test.go, internal/arcadedb/memory.go</files>
  <read_first>
    @internal/runner/runner.go
    @internal/runner/runner_persist.go
    @internal/agent/event.go
    @internal/agent/tools/result.go
    @internal/arcadedb/memory.go
    @internal/agent/mcptools/bridge_actor.go
  </read_first>
  <behavior>
    - An explicit user fact accepted during a turn is queued with direct user-turn provenance and durable before the terminal result is returned.
    - An allowlisted reliable tool fact is queued with tool call/artifact provenance, not rewritten through assistant prose.
    - Reasoning, assistant-generated conclusions, hypotheses, secrets, and temporary task observations are rejected before enqueue.
  </behavior>
  <action>
Start RED with a runner tracer that reveals one explicit durable fact during a shell/file task, reaches a terminal answer, and asserts sink durability occurred first. Add negative source cases for reasoning, generated answer text, hypotheses, temporary state, and secrets. In GREEN, implement `AcceptedCapture` and one ordered `MemoryCaptureQueue`; accept only D-15 explicit user statements or reliable allowlisted tool evidence carrying direct source refs/confidence. Derive actor run/parent-worker role from the existing host context per D-18, never from model fields. Insert `FlushThrough(lastAcceptedSequence)` into the terminal success path so D-16 guarantees every accepted item is durable before completion; a bounded exhausted sink failure must surface instead of returning success. Do not run a post-task summarizer or inspect persisted reasoning. REFACTOR only after ordering and negative-source tests pass.
  </action>
  <acceptance_criteria>
    - The tracer's durable-write event precedes terminal completion.
    - User/tool source refs survive unchanged; no reasoning-summary provenance can be constructed.
    - Queue ordering is deterministic for multiple accepted captures in one turn.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/runner -run 'Test(MemoryCaptureTracer|MemoryCaptureRejectsIneligibleSources|MemoryCaptureTerminalBarrier)' -count=1 -v</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, completion precedes durability, accepted order changes, or any reasoning/generated/secret fixture reaches the sink.</fails_when>
  </verify>
  <done>One directly evidenced mid-task fact is durably captured before the runner reports terminal success.</done>
</task>

<task type="auto" tdd="true">
  <name>49-05-T2: Enforce duplicate, contradiction, retry, and principal authority semantics</name>
  <files>internal/arcadedb/memory_capture.go, internal/arcadedb/memory_capture_test.go, internal/arcadedb/memory.go, internal/arcadedb/fact_lock.go, internal/arcadedb/write_retry.go</files>
  <read_first>
    @internal/arcadedb/memory.go
    @internal/arcadedb/fact_lock.go
    @internal/arcadedb/write_retry.go
    @internal/arcadedb/merge.go
    @internal/agent/mcptools/bridge_actor.go
  </read_first>
  <behavior>
    - Replaying one accepted capture is idempotent and enriches provenance without duplicating the fact.
    - Contradictory evidence creates temporal state rather than overwriting history.
    - A worker may append provenance/evidence but cannot supersede a principal fact; the principal can supersede explicitly.
  </behavior>
  <action>
Begin RED with same-key replay, same-fact/different-source, contradiction, worker-supersede denial, principal-supersede success, and optimistic-conflict tests. In GREEN, add `ApplyAcceptedCapture` over existing canonical fact/lock/write-retry primitives. Per D-17, duplicates append/dedupe provenance; contradictions close/open temporal validity rather than erase; only a parent/principal actor may supersede. Per D-18, persist host-derived run/role and forbid reasoning-summarizer source kinds at the graph boundary as defense in depth. Generate stable idempotency from identity plus direct source ref plus normalized fact, and keep retries side-effect free outside the ArcadeDB transaction. REFACTOR common normalization/authority helpers after the tests pass.
  </action>
  <acceptance_criteria>
    - Replays converge, provenance is enriched once, and contradictory history remains inspectable.
    - Worker supersession fails without mutation; principal supersession records the temporal transition.
    - Retry after conflict restarts from committed graph state.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/arcadedb -run 'TestAcceptedCapture_(Idempotent|Contradiction|WorkerAuthority|Retry)' -count=1 -v</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, a duplicate fact is created, history is overwritten, a worker supersedes, or retry applies an effect twice.</fails_when>
  </verify>
  <done>Accepted captures converge under retry while preserving provenance, temporal contradictions, and principal authority.</done>
</task>

<task type="auto" tdd="true">
  <name>49-05-T3: Wire the capture sink and prove the live durability barrier</name>
  <files>internal/runner/runner_deps.go, internal/runner/runner_stop_leak_test.go, internal/runner/runner_memory_capture_live_test.go, cmd/aura/chat_boot_memory.go, cmd/aura/chat_boot.go</files>
  <read_first>
    @internal/runner/runner_deps.go
    @internal/runner/runner_stop_leak_test.go
    @cmd/aura/chat_boot_memory.go
    @cmd/aura/chat_boot.go
    @cmd/arcadedb-mcp/memory_live_integration_test.go
  </read_first>
  <behavior>
    - Production boot creates one capture queue/sink and injects it into the runner.
    - Normal stop drains accepted work; a stuck backend hits a bounded failure rather than leaking or falsely completing.
    - A live shell/file task reveals a fact, completes, and immediate recall returns that fact with direct provenance and no reasoning source.
  </behavior>
  <action>
Start RED with composition, bounded-stop, backend-failure, and live immediate-recall tests. In GREEN, add the capture sink to `runner.Deps`, build it through `chat_boot_memory.go`, and join it to existing runner stop lifecycle. The live fixture must reveal a durable fact mid shell/file task from an explicit user or reliable tool source, reach the terminal barrier, then recall it immediately with the exact direct source ref; inspect persisted provenance to prove no reasoning or assistant-summary source exists. Missing ArcadeDB, a timed-out unflushed accepted capture, or zero live assertions is a failure. REFACTOR only after leak/race tests pass.
  </action>
  <acceptance_criteria>
    - One process-lifetime queue is created, injected, flushed, and closed without leaks.
    - A live accepted fact is immediately durable/recallable when terminal completion returns.
    - Direct provenance is preserved and reasoning provenance is impossible.
  </acceptance_criteria>
  <verify>
    <automated>go test -race -tags=arcadedb_integration -count=1 ./internal/runner -run '^TestMemoryCaptureLive_MidTaskDurableBeforeCompletion$' -v</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, required live dependencies are absent, terminal completion wins the race, immediate recall misses, provenance changes, or a goroutine leaks.</fails_when>
  </verify>
  <done>The production runner proves AUTO-03's mid-task direct capture and terminal durability guarantee live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user/tool events -> capture classifier | Untrusted or transient observations are considered for durable fact promotion. |
| capture queue -> ArcadeDB facts | Accepted evidence crosses into durable identity-scoped memory. |
| worker actor -> principal memory | Delegated workers may supply evidence but must not acquire principal supersession authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4905-01 | Spoofing | capture actor/source | high | mitigate | Host-derived actor and direct source refs; model cannot set identity/run/role/source kind. |
| T-4905-02 | Elevation of Privilege | worker supersession | high | mitigate | Graph-boundary principal-only transition check with negative worker tests. |
| T-4905-03 | Information Disclosure | secret/transient capture | high | mitigate | Source allowlist, secret redaction/rejection before enqueue and at sink, no reasoning/generated sources. |
| T-4905-04 | Tampering | duplicate/contradiction state | high | mitigate | Stable idempotency keys, provenance sets, temporal validity transitions, fact locks, and conflict retries. |
| T-4905-05 | Repudiation | capture provenance | medium | mitigate | Persist conversation/tool/artifact refs, actor run/role, confidence, accepted sequence, and timestamps. |
| T-4905-06 | Denial of Service | durability barrier | medium | mitigate | Ordered bounded queue, bounded retries/flush, explicit terminal failure, and joined shutdown. |
</threat_model>

<verification>
Run classifier/queue and graph authority tests, then the live mid-task terminal-barrier scenario under race detection.
</verification>

<success_criteria>
- A durable fact revealed mid shell/file task is captured before completion.
- Provenance points directly to the user/tool source and never to reasoning summarization.
- Replays, contradictions, worker activity, failures, and shutdown preserve the locked semantics.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-05-SUMMARY.md` when done.
</output>
