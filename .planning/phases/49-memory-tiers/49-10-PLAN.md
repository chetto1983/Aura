---
phase: 49-memory-tiers
plan: 10
type: tdd
wave: 10
depends_on:
  - 49-05
files_modified:
  - internal/arcadedb/memory_capture.go
  - internal/arcadedb/memory_capture_test.go
  - internal/arcadedb/memory.go
  - internal/arcadedb/fact_lock.go
  - internal/arcadedb/write_retry.go
autonomous: true
requirements:
  - AUTO-03
  - CTX-05
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Accepted captures converge under retry while preserving direct user-turn/tool-artifact provenance, temporal contradictions, and principal-only supersession (AUTO-03, D-17, D-18)."
    - "The graph boundary rejects every source kind outside the Plan 49-05 AcceptedCapture enum, including reasoning/generated prose (CTX-05, D-15)."
  artifacts:
    - path: internal/arcadedb/memory_capture.go
      provides: "idempotent duplicate/contradiction/authority sink for AcceptedCapture"
  key_links:
    - from: internal/runner/runner_memory_capture.go
      to: internal/arcadedb/memory_capture.go
      via: "ordered serial sink carrying unchanged structured direct refs"
      pattern: "ApplyAcceptedCapture"
---

<objective>
Persist AcceptedCapture through the authority-aware graph sink with idempotent, temporal, and principal-safe semantics.

Decision coverage: D-15: eligible direct evidence; D-16: durability barrier; D-17: duplicates/contradictions/principal authority; D-18: host provenance.

Purpose: Finish AUTO-03 after Plan 49-05 grounds capture in exact structured production events.
Output: Idempotent graph sink, contradiction history, principal authority, retry, and source-defense tests. Production composition/live proof follows in Plan 49-14.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-05-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@internal/arcadedb/memory.go
@internal/arcadedb/fact_lock.go
@internal/arcadedb/write_retry.go
@cmd/aura/chat_boot.go
</context>

## Artifacts this phase produces

- `internal/arcadedb/memory_capture.go`: `ApplyAcceptedCapture`, accepted-source validation, stable capture idempotency key, contradiction transition, and principal-authority checks.
- `internal/arcadedb/memory_capture_test.go`: replay, provenance enrichment, temporal contradiction, worker/principal, conflict retry, and source-defense tables.
- `internal/arcadedb/memory.go`: graph write transaction seam following the canonical fact mutation analog.
- `internal/arcadedb/fact_lock.go` and `internal/arcadedb/write_retry.go`: reused lock/version and whole-decision retry primitives; no parallel primitive.
- New schema fields: capture idempotency/source ref/run/worker role and temporal validity/provenance links on existing fact records. New environment variables: none.

<tasks>

<task type="tracer" tdd="true">
  <name>49-10-T1: Persist one accepted explicit-user fact idempotently</name>
  <files>internal/arcadedb/memory_capture.go, internal/arcadedb/memory_capture_test.go, internal/arcadedb/memory.go, internal/arcadedb/fact_lock.go, internal/arcadedb/write_retry.go</files>
  <read_first>
    @internal/arcadedb/memory.go
    @internal/arcadedb/fact_lock.go
    @internal/arcadedb/write_retry.go
    @internal/arcadedb/merge.go
    @internal/agent/mcptools/bridge_actor.go
  </read_first>
  <behavior>
    - Replaying one AcceptedCapture is idempotent and enriches direct provenance without duplicating a fact.
    - Contradictory evidence creates temporal state rather than erasing history.
    - Worker actor may add evidence but cannot supersede principal state; principal actor can supersede explicitly.
  </behavior>
  <action>Begin RED with real T1 output shapes for same-key replay, same-fact/different-source, contradiction, worker-supersede denial, principal-supersede success, reasoning-source defense, and optimistic conflict. In GREEN, add `ApplyAcceptedCapture` over existing canonical fact/lock/write-retry primitives. Per D-17, duplicates dedupe/enrich provenance and contradictions close/open temporal validity without overwriting history. Per D-18, persist the host-derived run/role and allow supersession only for the principal. Recompute retry attempts from committed state and perform no external effect inside the transaction. Reject every source kind outside the Plan 49-05 enum, especially reasoning/summary/generated text, as defense in depth.</action>
  <acceptance_criteria>
    - Replaying the same AcceptedCapture creates one effect and enriches each direct provenance edge once.
    - Every non-allowlisted source enum fails before graph mutation.
    - Conflict retry recomputes from committed state and applies no external effect twice.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "TestAcceptedCapture_(Tracer|Idempotent|Retry|SourceDefense)" ./internal/arcadedb | grep -c "^Test")" -gt 0; go test ./internal/arcadedb -run "TestAcceptedCapture_(Tracer|Idempotent|Retry|SourceDefense)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/arcadedb -count=1; go test -race ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, replay duplicates, history is overwritten, worker supersedes, retry applies twice, or an ineligible source reaches storage.</fails_when>
  </verify>
  <done>The graph sink preserves idempotency, temporal contradictions, authority, and direct provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>49-10-T2: Preserve contradiction history and principal authority</name>
  <files>internal/arcadedb/memory_capture.go, internal/arcadedb/memory_capture_test.go, internal/arcadedb/memory.go, internal/arcadedb/fact_lock.go, internal/arcadedb/write_retry.go</files>
  <read_first>
    @internal/arcadedb/memory_capture.go
    @internal/arcadedb/fact_lock.go
    @internal/arcadedb/write_retry.go
    @internal/arcadedb/merge.go
    @internal/agent/mcptools/bridge_actor.go
  </read_first>
  <behavior>
    - Contradictory evidence closes/opens temporal validity while preserving inspectable history.
    - A worker actor may append direct evidence but cannot supersede principal state; the principal may supersede explicitly.
    - Same-fact different-source captures dedupe content and add each provenance edge once.
  </behavior>
  <action>Begin RED with same-fact/different-source, contradiction, worker-supersede denial, principal-supersede success, and optimistic-conflict cases. In GREEN, per D-17 store contradictions as temporal state transitions rather than overwrite/delete, dedupe direct provenance by stable source identity, and per D-18 derive worker/principal authority only from the host actor context carried by AcceptedCapture. A worker can enrich evidence but not close principal state. Retry the whole decision from committed graph state under the existing lock/version seam. Preserve every historical validity interval and direct source reference.</action>
  <acceptance_criteria>
    - Contradictions preserve prior temporal intervals and create an inspectable current transition.
    - Worker supersession fails without mutation; principal supersession succeeds with host-derived authority.
    - Same content from distinct direct sources produces one fact and one edge per source.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "TestAcceptedCapture_(Contradiction|WorkerAuthority|PrincipalAuthority|ProvenanceEnrichment)" ./internal/arcadedb | grep -c "^Test")" -gt 0; go test ./internal/arcadedb -run "TestAcceptedCapture_(Contradiction|WorkerAuthority|PrincipalAuthority|ProvenanceEnrichment)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/arcadedb -count=1; go test -race ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports no tests to run, history is overwritten, worker supersedes, principal authority is model-derived, or provenance duplicates.</fails_when>
  </verify>
  <done>The capture sink preserves temporal contradictions, direct provenance, and principal-only supersession.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| capture queue -> ArcadeDB | Accepted structured evidence becomes durable identity-scoped memory. |
| worker actor -> principal facts | Delegated evidence crosses a principal authority boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4910-01 | Spoofing | capture actor/source | high | mitigate | Host actor/direct refs plus source enum defense at sink. |
| T-4910-02 | Tampering | duplicate/contradiction/retry | high | mitigate | Identity-bound idempotency, temporal state, whole-decision retry. |
| T-4910-03 | Elevation of Privilege | worker supersession | high | mitigate | Principal-only supersession tests. |
| T-4910-04 | Information Disclosure | reasoning/generated capture | high | mitigate | Sentinel live negatives and source enum rejection. |
</threat_model>

<verification>
Run all three task commands, including goleak/race and real live ToolInvocationEnd scenarios.
</verification>

<success_criteria>
- Sink semantics converge under replay/conflict while preserving temporal history and authority.
- One production queue is drained before terminal success and closes without leaks.
- Real explicit-user and durable-artifact events persist with direct provenance and no prose/reasoning source.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-10-SUMMARY.md` when done.
</output>
