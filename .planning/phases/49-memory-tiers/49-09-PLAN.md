---
phase: 49-memory-tiers
plan: 09
type: tdd
wave: 8
depends_on:
  - 49-12
files_modified:
  - internal/runner/runner_deps.go
  - internal/runner/runner_delete_reconcile.go
  - internal/arcadedb/memory_reasoning.go
  - internal/arcadedb/memory_reasoning_live_test.go
  - internal/conversations/history_reasoning_free_test.go
  - cmd/aura/chat_boot_memory.go
  - cmd/aura/chat_boot.go
  - .env.example
autonomous: true
requirements:
  - MEM-03
  - MEM-06
  - CTX-05
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Production boot owns one reasoning sink and one bounded expiry worker using the typed 30-day success / 7-day failed-or-cancelled policy (MEM-03, D-14)."
    - "Identity, conversation, source-turn, and explicit operator deletion remove the complete trace subgraph immediately and take precedence over TTL; repeated deletion converges (D-02, D-03, D-14)."
    - "Graph-resident reasoning remains absent from ordinary recall, history, compaction, summarization, preload, and capture, but owner-only explicit recall returns it (CTX-05, D-13)."
  artifacts:
    - path: internal/runner/runner_delete_reconcile.go
      provides: "bounded reasoning expiry and source-deletion lifecycle worker"
    - path: cmd/aura/chat_boot_memory.go
      provides: "single reasoning sink/worker composition and close ownership"
    - path: internal/arcadedb/memory_reasoning_live_test.go
      provides: "status TTL, deletion precedence, identity, and explicit-isolation live tests"
    - path: .env.example
      provides: "operator catalog for the two reasoning retention overrides and defaults"
  key_links:
    - from: cmd/aura/chat_boot_memory.go
      to: internal/config/config_retention.go
      via: "validated typed reasoning TTLs injected at boot"
      pattern: "ReasoningSuccessTTL|ReasoningFailedTTL"
    - from: internal/runner/runner_delete_reconcile.go
      to: internal/arcadedb/memory_reasoning.go
      via: "immediate source deletion paths plus bounded expiry sweep"
      pattern: "DeleteReasoningBySource|DeleteExpiredReasoning"
---

<objective>
Wire the reasoning graph lifecycle into production and prove bounded retention, deletion precedence, and explicit-only isolation live.

Decision coverage: D-02: derived authority; D-03: source deletion; D-13: explicit-only access; D-14: bounded metadata and retention.

Purpose: Complete MEM-03/CTX-05 with a concrete privacy lifecycle rather than indefinitely retained derived reasoning.
Output: Boot-owned sink/worker, environment catalog entries, immediate deletion propagation, expiry sweep, and live isolation/lifecycle evidence.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-memory-tiers/49-04-SUMMARY.md
@.planning/phases/49-memory-tiers/49-01-SUMMARY.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@internal/config/config_retention.go
@internal/runner/runner_delete_reconcile.go
@cmd/aura/chat_boot.go
</context>

## Artifacts this phase produces

- `internal/runner/runner_delete_reconcile.go`: reasoning expiry/source-deletion worker using the existing bounded reconcile-loop analog.
- `cmd/aura/chat_boot_memory.go`: one process-lifetime reasoning sink/worker composed through the established boot/close ownership pattern.
- `internal/arcadedb/memory_reasoning.go`: `DeleteExpiredReasoning` and `DeleteReasoningBySource`, including complete Trace/Step/ToolCall/edge cleanup.
- `internal/arcadedb/memory_reasoning_live_test.go`: live deletion-precedence, expiry-race, explicit-isolation, and failed/cancelled-retention tests.
- `.env.example`: `AURA_RETENTION_REASONING_SUCCESS_DAYS=30` and `AURA_RETENTION_REASONING_FAILED_DAYS=7`, following typed retention parsing. New schema fields: none.

<tasks>

<task type="tracer" tdd="true">
  <name>49-09-T1: Expire one successful and one failed trace through production lifecycle wiring</name>
  <files>internal/runner/runner_deps.go, internal/runner/runner_delete_reconcile.go, internal/arcadedb/memory_reasoning.go, cmd/aura/chat_boot_memory.go, .env.example</files>
  <read_first>
    @internal/config/config_retention.go
    @internal/config/config_retention_test.go
    @internal/runner/runner_delete_reconcile.go
    @internal/arcadedb/memory_reasoning.go
    @cmd/aura/chat_boot_memory.go
    @.env.example
  </read_first>
  <behavior>
    - Production boot injects one sink and starts one bounded expiry worker; close joins both.
    - Successful traces are eligible at 30 days; failed/cancelled traces are eligible at 7 days; valid overrides alter those cutoffs.
    - Sweeps are identity-safe, page/batch bounded, idempotent, and remove trace children/edges with the root.
  </behavior>
  <action>Start RED with composition, success/failed/cancelled cutoff, override, bounded page, repeated sweep, and close/goleak cases. In GREEN, inject the Plan 49-04 sink through `runner.Deps`, construct it in `chat_boot_memory.go`, and extend the existing reconciliation lifecycle with a bounded `DeleteExpiredReasoning(now,limit)` worker. Read only the validated `RetentionConfig` fields selected in Plan 49-04. Add `.env.example` entries documenting `AURA_RETENTION_REASONING_SUCCESS_DAYS=30` and `AURA_RETENTION_REASONING_FAILED_DAYS=7` as operator policy defaults, not measured capacity. Use terminal `expires_at`; failed and cancelled share the shorter TTL. Delete each complete trace subgraph idempotently and join the worker on close. Do not establish a second retention authority.</action>
  <acceptance_criteria>
    - Successful traces receive exactly 30 days and failed/cancelled traces exactly 7 days after typed validation.
    - The bounded worker has one boot/close owner and drains without leaks.
    - Invalid overrides fail validation rather than widening retention.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -list "Test(ReasoningRetentionWorker|ReasoningRetentionBoot|ReasoningRetentionClose)" ./internal/runner ./internal/arcadedb ./cmd/aura | grep -c "^Test")" -gt 0; go test ./internal/runner ./internal/arcadedb ./cmd/aura -run "Test(ReasoningRetentionWorker|ReasoningRetentionBoot|ReasoningRetentionClose)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/runner ./internal/arcadedb ./cmd/aura -count=1; go test -race ./internal/runner ./internal/arcadedb ./cmd/aura -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, either cutoff/default is wrong, overrides are ignored, a sweep is unbounded, or lifecycle goroutines leak.</fails_when>
  </verify>
  <done>Production reasoning lifecycle applies concrete status-aware TTLs through the existing typed retention system.</done>
</task>

<task type="auto" tdd="true">
  <name>49-09-T2: Make explicit and source deletion dominate TTL</name>
  <files>internal/runner/runner_delete_reconcile.go, internal/arcadedb/memory_reasoning.go, internal/arcadedb/memory_reasoning_live_test.go</files>
  <read_first>
    @internal/runner/runner_delete.go
    @internal/runner/runner_delete_reconcile.go
    @internal/arcadedb/memory_reasoning.go
    @internal/arcadedb/forget.go
  </read_first>
  <behavior>
    - Identity, conversation, source-turn, and operator trace deletion remove an unexpired trace immediately.
    - Immediate deletion removes trace, steps, tool calls, and orphanable edges atomically/idempotently.
    - A concurrent expiry sweep and explicit delete converge to absent state without resurrecting or crossing identities.
  </behavior>
  <action>Start RED with four unexpired deletion triggers, repeated deletion, expiry/delete races, partial graph, and foreign-identity fixtures. In GREEN, connect existing identity/conversation/source deletion reservations to `DeleteReasoningBySource` and expose a bounded operator trace delete under the existing memory authorization boundary. Per D-02/D-03/D-14, immediate deletion always takes precedence; the expiry worker only acts on still-present derived roots and cannot defer or resurrect source-driven deletion. Delete the complete subgraph in one identity-scoped transaction and treat already-absent derived nodes as success.</action>
  <acceptance_criteria>
    - Source, conversation, identity, and explicit deletion remove the complete derived subgraph immediately.
    - Delete/expiry races and repeated calls converge without resurrection.
    - Foreign identity state is unaffected.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -tags=arcadedb_integration -list "^TestReasoningGraphLive_(DeletionPrecedence|ExpiryDeleteRace)$" ./internal/arcadedb | grep -c "^Test")" -gt 0; go test -race -tags=arcadedb_integration -count=1 ./internal/arcadedb -run "^TestReasoningGraphLive_(DeletionPrecedence|ExpiryDeleteRace)$" -v; go vet ./...; go build ./...; go test ./internal/runner ./internal/arcadedb -count=1; go test -race ./internal/runner ./internal/arcadedb -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, an unexpired trace survives an immediate trigger, a child/edge remains, a race resurrects state, or foreign identity is affected.</fails_when>
  </verify>
  <done>Immediate deletion has explicit precedence and complete idempotent graph cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>49-09-T3: Prove graph-resident reasoning stays explicit-only live</name>
  <files>internal/conversations/history_reasoning_free_test.go, internal/arcadedb/memory_reasoning_live_test.go, cmd/aura/chat_boot.go</files>
  <read_first>
    @internal/conversations/history_reasoning_free_test.go
    @internal/conversations/context.go
    @internal/conversations/compaction.go
    @internal/runner/runner_context.go
    @cmd/aura/chat_boot.go
  </read_first>
  <behavior>
    - With a real stored trace, ordinary semantic/recent/open/scroll recall, history rebuild, compaction, preload, and capture make zero reasoning-store reads.
    - Owner explicit `mode=reasoning` returns bounded trace evidence; a foreign identity cannot.
    - Failed/cancelled reasoning is not injected before or during its shorter retention period.
  </behavior>
  <action>Strengthen `HistoryReasoningFree` with a graph-resident fixture so CTX-05 is non-vacuous, then add live ordinary-vs-explicit and cross-identity cases. Keep every automatic context, compaction, summary, preload, and capture interface structurally reasoning-free; no preload knob or implicit fallback may invoke the graph. Explicit owner recall alone can retrieve bounded reasoning evidence per D-13. Also prove failed/cancelled traces remain excluded until their 7-day expiry. Missing live dependencies or zero ordinary-path assertions fail.</action>
  <acceptance_criteria>
    - Explicit owner recall returns bounded graph evidence while automatic consumers make zero reasoning reads.
    - Foreign identity cannot retrieve it and failed/cancelled traces never enter context.
    - Missing live dependencies or zero assertions fail rather than skip-green.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; test "$(go test -tags=arcadedb_integration -list "^TestReasoningGraphLive_(ExplicitIsolation|FailedCancelledRetention)$" ./internal/arcadedb | grep -c "^Test")" -gt 0; test "$(go test -list "Test(HistoryReasoningFree|ChatBootReasoningIsolation)" ./internal/conversations ./cmd/aura | grep -c "^Test")" -gt 0; go test -race -tags=arcadedb_integration -count=1 ./internal/arcadedb -run "^TestReasoningGraphLive_(ExplicitIsolation|FailedCancelledRetention)$" -v; go test ./internal/conversations ./cmd/aura -run "Test(HistoryReasoningFree|ChatBootReasoningIsolation)" -count=1 -v; go vet ./...; go build ./...; go test ./internal/conversations ./internal/arcadedb ./cmd/aura -count=1; go test -race ./internal/conversations ./internal/arcadedb ./cmd/aura -count=1'</automated>
    <fails_when>The command exits non-zero, reports `no tests to run`, an ordinary consumer reads reasoning, owner explicit recall misses, foreign identity reads it, or failed/cancelled trace enters context.</fails_when>
  </verify>
  <done>Reasoning is useful only explicitly and remains absent from every automatic context/capture path for its entire lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| retention config -> lifecycle worker | Operator policy controls deletion timing for sensitive derived data. |
| source deletion -> reasoning graph | Authoritative PostgreSQL lifecycle triggers derived graph deletion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4909-01 | Information Disclosure | over-retained reasoning | high | mitigate | Concrete status TTLs, validated overrides, bounded live expiry tests. |
| T-4909-02 | Tampering | deletion/expiry race | high | mitigate | Identity-scoped atomic idempotent delete and concurrency tests. |
| T-4909-03 | Elevation of Privilege | ordinary reasoning reads | high | mitigate | Explicit mode-only store interface plus graph-resident negative tests. |
</threat_model>

<verification>
Run all three task commands. Record measured trace byte/vertex/edge/index growth separately from the fixed policy defaults, and retain live deletion/expiry evidence.
</verification>

<success_criteria>
- Successful traces are bounded to 30 days and failed/cancelled traces to 7 days by default with validated overrides.
- Explicit/source deletion is immediate, complete, idempotent, and dominates TTL.
- Graph-resident reasoning remains absent from every automatic consumer and foreign identity.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-09-SUMMARY.md` when done.
</output>
