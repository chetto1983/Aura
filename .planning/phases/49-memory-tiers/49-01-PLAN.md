---
phase: 49-memory-tiers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - scripts/agent_memory_eval.py
  - scripts/agent_memory_eval_test.py
  - internal/agent/mcptools/bridge_memory_surface_test.go
autonomous: true
requirements:
  - MEM-06
  - TOOL-05
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Git history contains an Amendment #181 commit extending amendment #91, and that commit predates every Phase 49 reasoning-tier implementation commit (MEM-06, D-01)."
    - "The amendment records the measured baseline and explicitly distinguishes what the measurement does not prove."
    - "Aura retains one model-facing retrieval contract, memory_recall; conversation, fact, and explicitly requested reasoning remain evidence tiers behind that contract (TOOL-05)."
  artifacts:
    - path: prd.md
      provides: "Amendment #181 extending #91 after the Phase 49 baseline measurement"
    - path: scripts/agent_memory_eval.py
      provides: "Phase 49 baseline and final mixed-tier evaluation scenarios"
    - path: internal/agent/mcptools/bridge_memory_surface_test.go
      provides: "Model-facing unified-retrieval-surface contract"
  key_links:
    - from: prd.md
      to: .planning/phases/49-memory-tiers/49-02-PLAN.md
      via: "required ancestor commit before any reasoning-tier implementation plan may execute"
      pattern: "Amendment #181"
    - from: internal/agent/mcptools/bridge_memory_surface_test.go
      to: internal/agent/mcptools/bridge_policy.go
      via: "memory surface curation contract"
      pattern: "memory_recall"
  prohibitions:
    - statement: "Memory persistence MUST NOT reconstruct hidden chain-of-thought or introduce a post-task reasoning summarizer."
      status: resolved
      verification: judgment
    - statement: "Durable fact capture MUST NOT promote hypotheses, assistant-generated prose, or reasoning content into fact evidence."
      status: resolved
      verification: judgment
    - statement: "Derived conversation or reasoning records MUST NOT become independent authority or survive deletion of their PostgreSQL source."
      status: resolved
      verification: judgment
---

<objective>
Measure the live pre-Phase-49 memory path, amend amendment #91 in an isolated first commit, and freeze the unified retrieval surface before implementation begins.

Purpose: Satisfy MEM-06 and CLAUDE.md's measure -> amend -> implement order without letting tracer-first planning put reasoning code ahead of the governance gate.
Output: Amendment #181, reproducible evaluator fixtures, and a model-surface contract test.
</objective>

<execution_context>
@C:/Users/chett/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/chett/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@prd.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-memory-tiers/49-CONTEXT.md
@.planning/phases/49-memory-tiers/49-RESEARCH.md
@.planning/phases/49-memory-tiers/49-VALIDATION.md
@scripts/agent_memory_eval.py
@internal/agent/mcptools/bridge_policy.go
@internal/agent/mcptools/bridge_memory_surface_test.go
</context>

<assumption_delta_decision>
  <primary_noun>memory_recall</primary_noun>
  <decision>no-change</decision>
  <rationale>The unified model-facing retrieval tool remains primary; conversation, fact, and explicitly requested reasoning are internal evidence tiers, not sibling model retrieval tools.</rationale>
</assumption_delta_decision>

<probe_edge_resolutions>
  <resolution requirement="HARN-05" category="idempotency">Resolved by D-21: retries use idempotency keys and no external side effects occur inside a retrying transaction.</resolution>
  <resolution requirement="HARN-05" category="concurrency">Resolved by D-19 through D-21: isolated working state, final-state validation, single ArcadeDB transaction, rollback on conflict, and restart from committed state.</resolution>
  <resolution requirement="TOOL-05" category="unclassified">Resolved by D-09 and D-10: memory_recall is the sole retrieval tool with typed evidence and an effective-path report.</resolution>
  <resolution requirement="AUTO-03" category="unclassified">Resolved by D-15 through D-18: accepted evidence is direct, ordered, principal-aware, and durable before terminal completion.</resolution>
  <resolution requirement="CTX-05" category="unclassified">Resolved by D-13: ordinary context, compaction, summarization, and fact extraction never query reasoning.</resolution>
  <resolution requirement="MEM-01" category="unclassified">Resolved by D-02 through D-06: PostgreSQL is authoritative and eligible user/final-assistant turns project asynchronously with deletion and active-conversation suppression.</resolution>
  <resolution requirement="MEM-02" category="unclassified">Resolved by D-07 through D-10: independent tier retrieval, deterministic RRF, anchor windows, typed evidence, and abstention.</resolution>
  <resolution requirement="MEM-03" category="unclassified">Resolved by D-11 through D-14: only provider-visible reasoning and bounded tool events form an explicit-only Trace graph.</resolution>
  <resolution requirement="MEM-06" category="unclassified">Resolved by D-01 plus the measured-baseline task below: an isolated amendment states both the observed baseline and its non-proofs before reasoning implementation.</resolution>
</probe_edge_resolutions>

<source_coverage_audit>

| Source | Items | Coverage |
|---|---|---|
| GOAL | searchable short-term memory; explicit-only reasoning memory; PRD amendment gate | 49-02/03; 49-04; 49-01 |
| REQ | MEM-01, MEM-02, MEM-03, MEM-06, TOOL-05, AUTO-03, CTX-05, HARN-05 | 49-02, 49-03, 49-04, 49-01, 49-03, 49-05, 49-04, 49-06 |
| RESEARCH | baseline/non-proofs; PG-authoritative projection; RRF mixed recall; graph reasoning isolation; ordered capture barrier; atomic final-state batch | 49-01 through 49-06 respectively |
| CONTEXT | D-01; D-02..D-06; D-07..D-10; D-11..D-14; D-15..D-18; D-19..D-21 | 49-01; 49-02; 49-03; 49-04; 49-05; 49-06 |

All goal, requirement, research, and locked-context items are covered. CONTEXT.md contains no deferred ideas. TOOL-05 and CTX-05 are treated as shipped structural contracts that receive non-vacuous regression coverage, not as blank-slate rewrites.
</source_coverage_audit>

## Artifacts this phase produces

- `prd.md`: Amendment #181, dated 2026-08-31, explicitly extending amendment #91.
- `scripts/agent_memory_eval.py`: `mixed_tier_recall` and `reasoning_isolation` scenario definitions plus baseline/final evidence reporting.
- `scripts/agent_memory_eval_test.py`: deterministic evaluator-output assertions.
- `internal/agent/mcptools/bridge_memory_surface_test.go`: contract asserting that `memory_recall` is the only model-facing retrieval tool while existing mutation tools retain their separately classified surface.
- New public production symbols/fields/flags in this plan: none. No new environment variable is authorized by this plan.

<tasks>

<task type="tracer">
  <name>49-01-T1: Measure the published recall path and commit Amendment #181 alone</name>
  <files>prd.md</files>
  <read_first>
    @CLAUDE.md
    @prd.md
    @.planning/phases/49-memory-tiers/49-CONTEXT.md
    @.planning/phases/49-memory-tiers/49-RESEARCH.md
    @scripts/agent_memory_eval.py
    @cmd/arcadedb-mcp/memory_live_integration_test.go
  </read_first>
  <action>
Run the Phase 49 baseline probes against the current published `memory_recall` and record the exact commit, tool-list visibility, actual retrieval path, weak-query behavior, latency, eligible PostgreSQL turn counts/bytes/spill incidence, active-context ladder behavior, provider-exposed reasoning/tool-event sizes, and pre-RRF fact/conversation ranks. Then append Amendment #181 dated 2026-08-31 to `prd.md`, explicitly extending amendment #91 per D-01. Ratify D-02 through D-18 at contract level, including PostgreSQL authority, derived ArcadeDB projections, explicit-only reasoning, direct-evidence capture, and the unified `memory_recall` surface. Include a section titled `What this measurement does NOT prove` stating that the sample does not establish production semantic quality, retention sufficiency, absence of every future reasoning leak, crash recovery, or concurrent batch correctness. Do not touch reasoning-tier implementation files in this task. Commit only `prd.md` as the first Phase 49 commit; record its hash in the task summary so later plans can assert ancestry.
  </action>
  <acceptance_criteria>
    - Amendment #181 names the measured baseline, the extension of #91, the new persistence/isolation contract, and every stated non-proof.
    - The task's commit changes `prd.md` and no implementation file.
    - No Phase 49 reasoning-tier implementation commit precedes the amendment commit.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'grep -qE "^## §.*\(Amendment #181, 2026-08-31\)" prd.md &amp;&amp; grep -q "extends amendment #91" prd.md &amp;&amp; grep -q "What this measurement does NOT prove" prd.md &amp;&amp; grep -q "production semantic quality" prd.md'</automated>
    <fails_when>The command exits non-zero, the amendment heading/date is absent, the link to #91 is absent, or any required non-proof is missing.</fails_when>
  </verify>
  <done>The measured #91 extension exists as the isolated first Phase 49 commit and names what the measurement does not prove.</done>
</task>

<task type="auto" tdd="true">
  <name>49-01-T2: Freeze the evaluation evidence and unified retrieval contract</name>
  <files>scripts/agent_memory_eval.py, scripts/agent_memory_eval_test.py, internal/agent/mcptools/bridge_memory_surface_test.go</files>
  <read_first>
    @scripts/agent_memory_eval.py
    @scripts/agent_memory_eval_test.py
    @internal/agent/mcptools/bridge_policy.go
    @internal/agent/mcptools/bridge_memory_surface_test.go
    @cmd/arcadedb-mcp/tool_memory.go
  </read_first>
  <behavior>
    - Baseline and final reports distinguish `facts`, `conversations`, `mixed`, and explicit `reasoning` paths without inventing a pass for an unobserved tier.
    - A memory mount exposes `memory_recall` as its only model-facing retrieval operation; raw search/browse primitives and tier names are not sibling retrieval tools.
    - Existing memory mutation operations remain separately classified and are not mislabeled as retrieval siblings.
  </behavior>
  <action>
Start RED with evaluator tests for structured baseline/final evidence and a bridge contract that enumerates the retrieval surface, then run them and retain the observed failure. In GREEN, add deterministic `mixed_tier_recall` and `reasoning_isolation` scenario/report fields to the evaluator and strengthen `bridge_memory_surface_test.go` so the assumption-delta decision is executable: `memory_recall` stays the single model-facing retrieval surface, while conversation, fact, and explicit reasoning are internal evidence tiers. Do not hide or rename existing mutation tools; TOOL-05 governs retrieval, not mutation. REFACTOR only after both suites pass, and keep the evaluator honest about skipped live dependencies instead of converting absence into green.
  </action>
  <acceptance_criteria>
    - The evaluator emits machine-readable actual path, tier counts/ranks, latency, abstention, and OTel-evidence fields for later live gates.
    - The bridge test fails if a second model-facing memory retrieval primitive is introduced.
    - Tests do not treat a missing live backend, missing credentials, or zero executed scenarios as success.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest scripts.agent_memory_eval_test &amp;&amp; go test ./internal/agent/mcptools -run '^TestMemorySurfacePolicy_' -count=1 -v</automated>
    <fails_when>Either command exits non-zero, Go reports `no tests to run`, the named surface test is absent, or the evaluator silently passes with no executed scenario.</fails_when>
  </verify>
  <done>The evaluator and bridge tests freeze honest evidence reporting and one model-facing retrieval surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live services -> amendment | Measured operational output becomes normative PRD evidence. |
| MCP advertisement -> model manifest | Server-advertised tool names cross into the model-facing contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-4901-01 | Tampering | `prd.md` amendment ordering | high | mitigate | Isolated `prd.md` commit, recorded hash, and ancestor gate in every reasoning implementation plan. |
| T-4901-02 | Repudiation | baseline evidence | medium | mitigate | Record commit, commands, counts, paths, latency, and explicit non-proofs in Amendment #181 and evaluator output. |
| T-4901-03 | Information Disclosure | measurement logs | medium | mitigate | Persist aggregate sizes/ranks and redacted bounded evidence only; never copy bearer tokens, raw secrets, or hidden provider reasoning into the PRD. |
| T-4901-04 | Elevation of Privilege | MCP tool surface | medium | mitigate | Contract-test curated retrieval visibility; identity authorization remains at the existing bearer/session boundary. |
</threat_model>

<verification>
The first Phase 49 commit is an isolated Amendment #181 commit; evaluator and bridge tests then freeze the measurable contracts that later plans must satisfy.
</verification>

<success_criteria>
- `git log --reverse` proves the Amendment #181 hash predates every commit touching `memory_reasoning` or `runner_reasoning_graph`.
- The amendment includes measured facts and an explicit non-proof section.
- The retrieval surface contract preserves one `memory_recall` tool without treating evidence tiers as tools.
</success_criteria>

<output>
Create `.planning/phases/49-memory-tiers/49-01-SUMMARY.md` when done.
</output>
