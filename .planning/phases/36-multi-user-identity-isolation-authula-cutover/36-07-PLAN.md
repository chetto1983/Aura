---
phase: 36-multi-user-identity-isolation-authula-cutover
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/profile/store.go
  - internal/mcp/managed_config.go
  - internal/mcp/managed_config_identity.go
  - internal/skills/identity_root.go
  - internal/mcp/managed_config_identity_test.go
  - internal/skills/identity_root_test.go
autonomous: true
requirements: [MUSR-01]
must_haves:
  truths:
    - "Per-identity roots (~/.aura/mcp/{id}, $AURA_SKILLS_DIR/{id}, ~/.aura/pyscripts/{id}) are resolved through a path-traversal-safe rooting helper"
    - "Per-identity servers.json holds per-identity enable/trust for class-(a) stdio servers; the shared catalog stays read-only"
    - "The class-(b) shared agent-memory server (:8091) is admin-governed only — users cannot toggle it"
    - "Built-in skills are shared read-only; per-identity user skills live under the rooted dir"
  artifacts:
    - path: "internal/mcp/managed_config_identity.go"
      provides: "per-identity MCP config (MountForIdentity / *ForIdentity)"
    - path: "internal/skills/identity_root.go"
      provides: "newSkillToolForIdentity storage rooting"
  key_links:
    - from: "internal/mcp/managed_config_identity.go"
      to: "internal/profile/store.go profileDir guard"
      via: "traversal-safe filepath.Join(root, identity)"
      pattern: "identityPattern"
  prohibitions:
    - "Never filepath.Join an untrusted identity without the containment guard"
    - "No per-identity toggle of the shared class-(b) agent-memory server"
    - "Snippet/skill EXECUTION isolation is Phase 37 — this plan does STORAGE rooting only"
---

<objective>
Root per-identity MCP config and skills/pyscripts storage under identity-scoped directories (D-20/D-21),
reusing the established traversal-safe rooting convention from `internal/profile/store.go` (`profileDir`).
This is STORAGE rooting only — snippet/skill EXECUTION isolation is Phase 37 (the box), explicitly out of
scope (D-21 note / phase §domain).

Per-identity `~/.aura/mcp/{id}/servers.json` holds each identity's enable/trust for class-(a) stdio servers;
the shared catalog stays read-only. The class-(b) shared agent-memory server (:8091) is admin-governed only —
users can't toggle shared infra (D-19). Skills: `$AURA_SKILLS_DIR/{id}/` + `~/.aura/pyscripts/{id}/` with
built-ins shared read-only; additive `newSkillToolForIdentity(ctx)` with the `local` fallback (D-21).

Purpose: MUSR-01 filesystem-plane isolation; the provisioning saga (plan 08) creates these roots per identity.
Output: traversal-safe per-identity roots, per-identity MCP config, `newSkillToolForIdentity`, guard tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-PATTERNS.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Traversal-safe per-identity root helper + per-identity MCP config</name>
  <read_first>
    - internal/profile/store.go (`profileDir` ~201-222: identityPattern charset check + `..`/slash reject + filepath.Rel containment — the EXACT traversal-safe rooting analog, PATTERNS Cluster E; per-identity root `~/.aura/agents` ~85; AURA_PROFILE_DIR config ~206)
    - internal/mcp/managed_config.go (ManagedConfig durable registry mcpServers + enabled/trust/source ~32-70; trust classes ~21-25 — PATTERNS Cluster E)
    - 36-CONTEXT.md D-19/D-20 (class-a per-identity enable/trust; class-b shared admin-governed; per-identity config path)
  </read_first>
  <behavior>
    - Test 1: an identity containing `..` or `/` is REJECTED by the rooting helper (no path escape)
    - Test 2: a valid identity resolves `~/.aura/mcp/{id}/servers.json` under the mcp root (containment asserted)
    - Test 3: per-identity enable/trust changes a class-(a) server's state without touching the shared catalog
    - Test 4: attempting to toggle the class-(b) shared agent-memory server via the per-identity path is refused
  </behavior>
  <action>
    Add a per-identity root helper mirroring `profileDir`'s containment guard (identityPattern charset +
    `..`/slash reject + `filepath.Rel` "escapes root" assertion) — either extend `internal/profile/store.go`
    or a small helper reused by mcp+skills. Create `internal/mcp/managed_config_identity.go`: a
    `MountForIdentity`/`*ForIdentity` path that reads the shared catalog read-only and layers a per-identity
    `~/.aura/mcp/{id}/servers.json` for class-(a) enable/trust. The class-(b) shared agent-memory server
    (:8091) is admin-governed — the per-identity path must NOT expose a toggle for it (D-19). `local` fallback
    for the CLI path. Never `filepath.Join` an untrusted identity without the guard.
  </action>
  <verify>
    <automated>go vet ./internal/mcp/ ./internal/profile/ && go build ./... && go test -race ./internal/mcp/ ./internal/profile/ 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - source: managed_config_identity.go resolves per-identity servers.json through the containment guard; shared catalog read-only
    - behavior: `..`/slash identity rejected; class-(b) server not user-toggleable
    - test: `go test -race ./internal/mcp/` passes including the traversal-reject test
  </acceptance_criteria>
  <done>Per-identity MCP config is traversal-safe; class-(a) enable/trust is per-identity; class-(b) stays admin-governed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-identity skills + pyscripts storage rooting (newSkillToolForIdentity)</name>
  <read_first>
    - internal/skills/builtin.go (built-in materialization — built-ins shared read-only)
    - internal/config/config.go (SkillsDir default `~/.aura/skills` ~389, 545-546 — the base to root per identity)
    - internal/profile/store.go (the rooting guard from Task 1)
    - 36-CONTEXT.md D-21 (skills-dir + pyscripts rooting; identity-keyed skill_audit/approval; newSkillToolForIdentity; additive *ForIdentity + local fallback; execution isolation is Phase 37)
  </read_first>
  <behavior>
    - Test 1: `newSkillToolForIdentity(ctx)` roots user skills under `$AURA_SKILLS_DIR/{id}/` (containment guarded)
    - Test 2: built-in skills remain shared read-only (not copied per identity)
    - Test 3: the `local`/no-principal path resolves the existing default skills dir (backward compatible)
  </behavior>
  <action>
    Create `internal/skills/identity_root.go`: a `newSkillToolForIdentity(ctx)` that roots user skills under
    `$AURA_SKILLS_DIR/{id}/` and pyscripts under `~/.aura/pyscripts/{id}/` using the Task-1 containment guard,
    with built-ins shared read-only. Additive `*ForIdentity` methods alongside the existing base methods;
    `local` fallback to the current default dir (D-21). Do NOT implement snippet/skill execution isolation —
    storage rooting only (Phase 37 owns execution). Identity-keyed `skill_audit`/approval writes are handled in
    plan 10 (audit); this plan only establishes the per-identity storage roots.
  </action>
  <verify>
    <automated>go vet ./internal/skills/ && go build ./... && go test -race ./internal/skills/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - source: identity_root.go roots user skills+pyscripts per identity through the guard; built-ins shared read-only
    - behavior: local path resolves the existing default dir; per-identity path is isolated
    - test: `go test -race ./internal/skills/` passes
  </acceptance_criteria>
  <done>Per-identity skills+pyscripts storage is rooted and traversal-safe; built-ins shared; local backward-compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| identity string → filesystem path | An untrusted identity must never escape its rooted directory |
| user → shared class-(b) MCP server | Users cannot govern shared infra; admin only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07-T | Tampering | path traversal via crafted identity | mitigate | profileDir-style containment guard on every join (Task 1/2, PATTERNS Cluster E) |
| T-36-07-E | Elevation of Privilege | user toggles shared class-(b) server | mitigate | class-(b) admin-governed, no per-identity toggle (Task 1, D-19) |
| T-36-07-I | Information Disclosure | cross-identity skills/config read | mitigate | per-identity rooted dirs, built-ins shared read-only (Task 1/2, D-20/D-21) |
| T-36-07-SC | Tampering | package installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test -race ./internal/mcp/ ./internal/skills/ ./internal/profile/` green.
- Traversal-reject tests pass; class-(b) toggle refused.
</verification>

<success_criteria>
- MUSR-01 filesystem plane: per-identity MCP config + skills/pyscripts storage rooting, traversal-safe, class-(b) admin-only.
</success_criteria>

<artifacts_produced>
- Files: `internal/mcp/managed_config_identity.go`, `internal/skills/identity_root.go`
- Function: `newSkillToolForIdentity(ctx)`; per-identity `MountForIdentity`/`*ForIdentity`
- Roots: `~/.aura/mcp/{id}/servers.json`, `$AURA_SKILLS_DIR/{id}/`, `~/.aura/pyscripts/{id}/`
</artifacts_produced>

<output>
Create `.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-07-SUMMARY.md` when done.
</output>
