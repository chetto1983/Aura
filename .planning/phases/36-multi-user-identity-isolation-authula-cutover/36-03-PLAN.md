---
phase: 36-multi-user-identity-isolation-authula-cutover
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/tools/shell_bg.go
  - internal/agent/tools/shell_bg_owner.go
  - internal/agent/tools/shell_bg_ttl.go
  - internal/agent/tools/shell_bg_owner_test.go
  - internal/agent/tools/shell_bg_ttl_test.go
autonomous: true
requirements: [MUSR-03, MUSR-04]
must_haves:
  truths:
    - "Background shell job IDs are crypto/rand 128-bit hex, not sequential sh_N"
    - "Each background shell is bound to (identity, session) at start"
    - "shell_poll/shell_kill on a foreign job are denied unless the caller holds the admin capability"
    - "Jobs have a default 1h TTL (AURA_SHELL_BG_TTL) whose expiry records status and terminates the process group"
  artifacts:
    - path: "internal/agent/tools/shell_bg_owner.go"
      provides: "crypto/rand IDs + (identity,session) owner + authority check"
    - path: "internal/agent/tools/shell_bg_ttl.go"
      provides: "1h TTL reaper (killProcessGroup + record status on expiry)"
  key_links:
    - from: "internal/agent/tools/shell_bg_owner.go"
      to: "identityctx.IdentityID(ctx)"
      via: "owner binding at start"
      pattern: "identityctx"
  prohibitions:
    - "No sequential sh_%d IDs"
    - "No poll/kill of a foreign job without the admin capability"
---

<objective>
Close the MUSR-03/04 background-job gap in `internal/agent/tools/shell_bg.go`: sequential guessable IDs
(`sh_%d`), no owner binding, and no TTL. Retrofit crypto-random IDs, `(identity, session)` ownership with
an authority check (owner OR admin cap), and a default 1h TTL reaper that terminates the process group and
records status on expiry.

`shell_bg.go` is already 499 LOC (near the 600 cap) — split-on-touch into `shell_bg_owner.go` (IDs +
owner + authority) and `shell_bg_ttl.go` (reaper) per CLAUDE.md refactor-on-touch (RESEARCH Pitfall 7).

Purpose: MUSR-03 (unguessable, owner-bound, deny-cross-session) + MUSR-04 (TTL + process-group terminate
+ age metrics). No persistence needed — jobs are in-memory/process-scoped (RESEARCH Runtime State
Inventory: a daemon restart legitimately drops in-flight jobs).
Output: owner-bound crypto-random IDs, authority-gated poll/kill, 1h TTL reaper, Wave-0 tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-PATTERNS.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crypto-random IDs + (identity,session) owner + authority-gated poll/kill</name>
  <read_first>
    - internal/agent/tools/shell_bg.go (BackgroundShells registry ~lines 30-41; `start` sets process group + cancel ~147-188; sequential `sh_%d` ~169-170; `shell_poll`/`shell_kill` lookup by shell_id ~346, ~458 — PATTERNS Cluster F)
    - internal/identityctx/identityctx.go (IdentityID(ctx) — the owner principal, PATTERNS Shared Patterns)
    - internal/agui/auth.go (HasCapability interface ~35-38 — the admin-cap authority path for D-18)
    - internal/agui/onboarding_provision.go (uuid.NewString() usage ~line 221 — random-ID precedent)
    - 36-CONTEXT.md D-17/D-18/D-23 + 36-RESEARCH.md Pitfall 6
  </read_first>
  <behavior>
    - Test 1: two starts mint distinct 128-bit hex IDs; neither is of the form `sh_<int>` (unguessable)
    - Test 2: `shell_poll`/`shell_kill` by (identityA, sessionA) on a job owned by (identityB, sessionB) is DENIED (404-shaped miss for read, refusal for kill)
    - Test 3: a caller holding the admin capability may poll/kill a foreign job (D-18 cross-session recovery)
    - Test 4: same-owner (identity+session match) poll/kill succeeds
  </behavior>
  <action>
    In `shell_bg_owner.go`: replace the `sh_%d` minting with a `crypto/rand` 128-bit hex ID generator. Add
    `ownerID string` (`identityctx.IdentityID(ctx)` at start) and `sessionID string` (the conversation/
    session key, D-23) fields to `bgShell`. Add an `authorize(callerIdentity, callerSession string, hasAdmin
    bool) bool` helper: allow iff `(callerIdentity == ownerID && callerSession == sessionID) || hasAdmin`.
    Wire `shell_poll`/`shell_kill` to read the caller's `(identity, session)` from ctx and consult
    `HasCapability` for the admin path; a foreign non-admin lookup returns the not-found shape (D-06 read=404),
    a foreign non-admin kill is refused (D-06 mutate=403). Keep the existing `setProcessGroup`/`cmd.Cancel`
    behavior untouched. `local` (`00000000-0000-0000-0000-000000000001`) is the CLI/no-principal fallback owner.
  </action>
  <verify>
    <automated>go vet ./internal/agent/tools/ && go build ./... && go test -race -run 'TestBackgroundJobOwnerDeny|TestBackgroundJobID' ./internal/agent/tools/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - source: shell_bg_owner.go mints IDs via crypto/rand and stores ownerID+sessionID; no `sh_%d` remains
    - behavior: session B poll/kill of session A's job denied; admin-cap caller allowed
    - test: `go test -race -run TestBackgroundJobOwnerDeny ./internal/agent/tools/` passes
  </acceptance_criteria>
  <done>IDs unguessable, jobs owner-bound, poll/kill authority = owner OR admin cap; shell_bg.go ≤600 LOC after the split.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default 1h TTL reaper + age metric</name>
  <read_first>
    - internal/agent/tools/shell_bg.go (`Shutdown` group-terminate + drain ~259-306; `Evict` reclaim ~218-225; killProcessGroup/cmd.Cancel; env pattern `shellBackgroundBufCap`/`Max` ~428-455 — PATTERNS Cluster F)
    - internal/config/config.go (env-default helper for AURA_SHELL_BG_TTL)
    - 36-CONTEXT.md D-17 (default TTL = 1h, env-overridable; on expiry record status + terminate the process group)
  </read_first>
  <behavior>
    - Test 1: a job whose startedAt+ttl is in the past is reaped: killProcessGroup called, status recorded "expired"
    - Test 2: default TTL is 1h; AURA_SHELL_BG_TTL overrides it
    - Test 3: a poll on a running job exposes an age metric (now - startedAt)
  </behavior>
  <action>
    In `shell_bg_ttl.go`: add `startedAt time.Time` + `ttl time.Duration` to `bgShell` (default 1h, read
    `AURA_SHELL_BG_TTL` via the existing env pattern). Add a reaper (interval sweep or per-registry tick)
    that, for any job past `startedAt+ttl`, calls the existing `killProcessGroup`/cancel path and records
    status `"expired"` — reuse the `Shutdown` terminate path, do NOT re-invent process-group kill. Expose the
    job age (`now - startedAt`) on the `shell_poll` result payload. Keep the reaper goleak-clean (bounded,
    `once`-guarded stop mirroring the existing `Shutdown` drain).
  </action>
  <verify>
    <automated>go vet ./internal/agent/tools/ && go build ./... && go test -race -run 'TestBackgroundJobTTLExpiry|TestBackgroundJobAge' ./internal/agent/tools/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - source: shell_bg_ttl.go reads AURA_SHELL_BG_TTL (default 1h) and reaps expired jobs via killProcessGroup
    - behavior: TTL expiry records status "expired" + terminates the process group; poll shows age
    - test: `go test -race -run TestBackgroundJobTTLExpiry ./internal/agent/tools/` passes; reaper is goleak-clean
  </acceptance_criteria>
  <done>Jobs expire by TTL (default 1h, env-overridable), terminating the process group and recording status; age metric present; reaper leak-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| tool caller (session) → BackgroundShells registry | A caller may only reach its own (identity,session) jobs unless admin |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-E | Elevation of Privilege | guessable job ID → cross-session kill | mitigate | crypto/rand 128-bit IDs + (identity,session) owner check (Task 1, MUSR-03) |
| T-36-03-T | Tampering | foreign job kill | mitigate | authority = owner OR admin cap; foreign non-admin refused (Task 1, D-18) |
| T-36-03-D | Denial of Service | runaway job never reaped | mitigate | default 1h TTL reaper terminates the process group (Task 2, MUSR-04) |
| T-36-03-SC | Tampering | package installs | mitigate | stdlib crypto/rand only; no new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `go test -race ./internal/agent/tools/` green (owner-deny + TTL + goleak).
- `wc -l internal/agent/tools/shell_bg.go` ≤600 after the split.
- No `sh_%d` minting remains (`grep -c 'sh_%d' internal/agent/tools/*.go` == 0 after filtering comments).
</verification>

<success_criteria>
- MUSR-03: unguessable, owner-bound job IDs; cross-session poll/kill denied (admin-cap exempt).
- MUSR-04: default 1h TTL + process-group terminate + status record + age metric.
</success_criteria>

<artifacts_produced>
- Files: `internal/agent/tools/shell_bg_owner.go`, `shell_bg_ttl.go` (split from shell_bg.go)
- Fields: `bgShell.ownerID`, `.sessionID`, `.startedAt`, `.ttl`
- Env var: `AURA_SHELL_BG_TTL` (default 1h)
- Tests: `shell_bg_owner_test.go`, `shell_bg_ttl_test.go` (Wave-0 MUSR-03/04)
</artifacts_produced>

<output>
Create `.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-03-SUMMARY.md` when done.
</output>
