---
phase: 36-multi-user-identity-isolation-authula-cutover
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/migrate_0026_integration_test.go
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements: [MUSR-06]
must_haves:
  truths:
    - "TestMigration0026LocalAdminCapsRoundTrip asserts 0026's OWN reversibility (down removes governance.write/identity.create/agent.run, up restores them) regardless of how many migrations sit above 0026"
    - "The migration-0026 round-trip test passes on live db_integration CI (Integration + Knowledge-integration jobs) at HEAD with 32+ migrations present"
    - "scripts/check-no-url-tokens.sh runs as an enforced CI step so a future long-lived-token-in-URL regression fails the pipeline"
  prohibitions:
    - "The test MUST NOT use a bare relative MigrateSteps(-1)/(+1) from HEAD (it would reverse 0032 owner-RLS, not 0026)"
    - "The check-no-url-tokens.sh CI step MUST NOT be allowed to no-op silently (non-zero exit must fail the job)"
  artifacts:
    - path: "internal/db/migrate_0026_integration_test.go"
      provides: "version-anchored 0026 reversibility assertion"
    - path: ".github/workflows/ci.yml"
      provides: "the enforced no-URL-token static gate step"
---

<objective>
Close the two CI-correctness gaps that block Phase 36 from ever going green on live CI:

1. **VERIF-1** — `TestMigration0026LocalAdminCapsRoundTrip` uses `MigrateSteps(ctx, url, -1)/(+1)`, which are RELATIVE single steps from the CURRENT head. With 32 migrations present, `-1` reverses `0032_owner_rls`, not `0026`'s admin-cap seed, so the test asserts the wrong thing and is CONFIRMED failing on GitHub Actions run 28753262579 ("capability \"governance.write\" present=true, want present=false"). Rewrite it to isolate migration 0026's OWN down/up regardless of migrations above it.
2. **VERIF-6** — `scripts/check-no-url-tokens.sh` is a working, self-tested static gate (MUSR-06 "no long-lived token in a URL") but is wired into ZERO CI jobs, despite the explicit 36-11→36-12 handoff commitment. Add a CI step that runs it.

Purpose: The whole phase cannot pass verification until CI is green on the pushed HEAD (see 36-18). This plan removes the one confirmed-broken test and adds the missing regression gate so that when 36-18 pushes, these two do not block.
Output: A version-anchored migration-0026 test + an enforced `check-no-url-tokens.sh` CI step.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version-anchor the migration-0026 reversibility test</name>
  <files>internal/db/migrate_0026_integration_test.go</files>
  <read_first>
    - internal/db/migrate_0026_integration_test.go (the file being fixed — the broken lines are 94 `MigrateSteps(ctx, migrateURL, -1)` and 104 `MigrateSteps(ctx, migrateURL, 1)`, plus the head-version read at line 73 `currentMigrationVersion(t, ctx, freshAdmin)` and the final no-op assertion at 114-120)
    - internal/db/migrate_steps.go (`func MigrateSteps(ctx, migrateURL string, n int) error` — RELATIVE stepping; swallows ErrNoChange)
    - internal/db/migrate.go (`func Migrate(ctx, migrateURL string) (int, error)` — full up; returns pending-applied count)
    - internal/db/migrate_0017_integration_test.go (`func currentMigrationVersion(t, ctx, db) int` — the head-version helper already imported)
    - internal/db/migrations/0026_local_admin_caps.up.sql + .down.sql (the seed under test: three grants governance.write/identity.create/agent.run for local UUID …001; down deletes exactly those, `*` wildcard survives)
  </read_first>
  <action>
    Replace the relative `MigrateSteps(-1)`/`MigrateSteps(+1)` straddle (which straddles whatever the latest migration is) with a straddle that is POSITIONED at version 26 first, so the ±1 reverses/re-applies migration 0026 SPECIFICALLY:

    1. After the existing full `Migrate` up + the `head := currentMigrationVersion(t, ctx, freshAdmin)` read (rename/capture the head version into a local int), assert head ≥ 26 (keep the existing guard).
    2. BEFORE the down assertion, step the fresh DB DOWN from head to exactly version 26 by calling `MigrateSteps(ctx, migrateURL, 26 - head)` (a negative delta that reverses 0027..0032 but NOT 0026). At this point 0026's caps are still present — the caps assertion for "present" (all four) may stay here or before the positioning.
    3. Down-step 0026 itself: `MigrateSteps(ctx, migrateURL, -1)` now reverses 26→25 (0026's OWN down). Assert the three explicit caps are GONE and `*` survives (the existing `requireCap0026(..., false)` block).
    4. Re-up 0026: `MigrateSteps(ctx, migrateURL, 1)` now re-applies 25→26 (0026's OWN up, idempotent). Assert the three explicit caps are RESTORED and `*` survives.
    5. Restore to HEAD + prove no lingering pending: call `Migrate(ctx, migrateURL)` to re-apply 0027..0032 (expect it applies `head-26` migrations), then a SECOND `Migrate(ctx, migrateURL)` and assert it returns 0 (the "0026 reversible, no pending" invariant). Rewrite the current single `n != 0` block (lines 114-120) accordingly — do NOT assert the FIRST post-round-trip Migrate returns 0 (it now applies head-26), assert the trailing one does.

    Keep every other line (fresh-DB drill, EnsureRoles, pools, `localCapabilitySet0026`, `requireCap0026`) byte-identical. Do NOT add `AND deactivated_at IS NULL` or touch the migrations themselves — this is a test-only fix. The `//go:build db_integration` tag + `envOrSkip(t, "POSTGRES_PASSWORD")` (t.Fatal under $CI) stay so it runs live, never skips-as-green.
  </action>
  <acceptance_criteria>
    - Source assertion: the file contains NO bare `MigrateSteps(ctx, migrateURL, -1)` or `MigrateSteps(ctx, migrateURL, 1)` that is reached BEFORE a positioning `MigrateSteps(ctx, migrateURL, 26 - head)` call (grep confirms the positioning step precedes the ±1 straddle).
    - Behavior assertion: after the down straddle the three explicit caps read present=false and `*` present=true; after the re-up they read present=true.
    - Build: `go build -tags db_integration ./internal/db/` succeeds; `go vet -tags db_integration ./internal/db/` clean.
    - Test command (live tier, run in WSL/CI per 36-18): `go test -tags db_integration -run TestMigration0026LocalAdminCapsRoundTrip ./internal/db/ -count=1` passes against a live migrated Postgres with head ≥ 32.
  </acceptance_criteria>
  <verify>
    <automated>go build -tags db_integration ./internal/db/ && go vet -tags db_integration ./internal/db/</automated>
  </verify>
  <done>The test isolates 0026's own down/up (caps removed then restored) regardless of migrations above it; it compiles under db_integration and is structured to pass on the live tier.</done>
</task>

<task type="auto">
  <name>Task 2: Wire scripts/check-no-url-tokens.sh into CI as an enforced gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the file being edited — the "File-size cap (600 LOC)" step at ~line 66-67 runs `bash scripts/check-file-size.sh` inside the Build+vet+lint+deadcode+file-size job; the sibling static-gate steps "CI go-packages lint" and "AG-UI boundary gate" show the exact `- name:` / `run: bash scripts/...` step shape to mirror)
    - scripts/check-no-url-tokens.sh (the gate to invoke — exit 0 clean, exit 1 on a violation, exit 2 usage; supports `--self-test`; already executable, self-tests clean per VERIFICATION)
  </read_first>
  <action>
    Add ONE new step to the same job that runs `check-file-size.sh` (the Build+vet+lint+deadcode+file-size job), placed immediately AFTER the existing "File-size cap (600 LOC)" step, named "No long-lived token in URLs (MUSR-06)" with `run: bash scripts/check-no-url-tokens.sh`. Do NOT wrap it in `continue-on-error` and do NOT add `|| true` — a non-zero exit MUST fail the job (the whole point of the gate). Do not modify any other job, the musr-e2e job, or the file-size step itself. Keep YAML indentation identical to the neighbouring steps (two-space list item under `steps:`).
  </action>
  <acceptance_criteria>
    - Source assertion: `.github/workflows/ci.yml` contains a step whose `run:` is `bash scripts/check-no-url-tokens.sh`, positioned after the `bash scripts/check-file-size.sh` step, with no `continue-on-error`/`|| true` on that step.
    - Behavior assertion (local): `bash scripts/check-no-url-tokens.sh` exits 0 on the current tree; `bash scripts/check-no-url-tokens.sh --self-test` exits 0 (proves the gate still catches a planted `?session_token=`).
    - YAML validity: the workflow parses (e.g. `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` succeeds, or `gh workflow view` accepts it).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-no-url-tokens.sh && bash scripts/check-no-url-tokens.sh --self-test</automated>
  </verify>
  <done>CI runs `check-no-url-tokens.sh` as a blocking step beside the file-size gate; a future long-lived-token-in-URL regression fails the pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → merge/deploy | A green CI run is the gate that lets Phase 36 close; a falsely-green job (broken test masquerading as coverage, or a missing gate) lets an isolation defect ship |
| any URL/query string → session store | A long-lived session/auth token placed in a URL leaks via logs/referrer/history (MUSR-06 boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-13-01 | Repudiation/Tampering | migration-0026 reversibility test | mitigate | Version-anchor the ±1 straddle at v26 so the test proves 0026's own down/up, not 0032's — closing a falsely-red (and structurally-wrong) job that would otherwise mask real migration regressions |
| T-36-13-02 | Information Disclosure | long-lived token in URL | mitigate | Enforce `check-no-url-tokens.sh` in CI (non-zero exit fails the job) so a regression that emits a session/auth token into a query string is caught before merge |
| T-36-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (test edit + one CI shell step); go.mod/go.sum + package.json byte-unchanged. RESEARCH Package Legitimacy Audit: zero new packages this phase |
</threat_model>

<verification>
- `go build -tags db_integration ./internal/db/` + `go vet -tags db_integration ./internal/db/` clean.
- `bash scripts/check-no-url-tokens.sh` exit 0 + `--self-test` exit 0.
- `.github/workflows/ci.yml` parses as valid YAML.
- The live `db_integration TestMigration0026LocalAdminCapsRoundTrip` tier is run green in WSL/CI as part of 36-18 (no live PG on the Windows host).
</verification>

<success_criteria>
- The migration-0026 test isolates 0026's own reversibility and is structured to pass with 32+ migrations present.
- `check-no-url-tokens.sh` is an enforced (non-bypassable) CI step.
- No production code changed; no new dependencies.
</success_criteria>

<artifacts_produced>
- Rewritten `internal/db/migrate_0026_integration_test.go` (version-anchored straddle: position at v26 → -1 → +1 → full Migrate → trailing no-op assertion).
- New CI step "No long-lived token in URLs (MUSR-06)" in `.github/workflows/ci.yml` running `bash scripts/check-no-url-tokens.sh`.
</artifacts_produced>

<output>
Create `.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-13-SUMMARY.md` when done.
</output>
