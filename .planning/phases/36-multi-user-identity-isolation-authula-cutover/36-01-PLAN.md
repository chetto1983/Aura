---
phase: 36-multi-user-identity-isolation-authula-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/aura/identity.go
  - cmd/aura/recovery.go
  - internal/db/migrations/0026_local_admin_caps.up.sql
  - internal/db/migrations/0026_local_admin_caps.down.sql
autonomous: true
requirements: [MUSR-06]
must_haves:
  truths:
    - "A host operator can mint a short-lived password-reset / credential-reset token via `aura identity recover <name>` without a running server"
    - "The `local` identity holds explicit admin capability grants (governance.write + identity.create + agent.run) seeded at bootstrap"
    - "`aura identity grant/revoke <name> <cap>` writes an audited capability change"
  artifacts:
    - path: "cmd/aura/recovery.go"
      provides: "break-glass CLI recover subcommand reusing aura.password_reset_tokens (0023)"
    - path: "internal/db/migrations/0026_local_admin_caps.up.sql"
      provides: "idempotent seed of local's explicit admin caps"
  key_links:
    - from: "cmd/aura/recovery.go"
      to: "aura.password_reset_tokens"
      via: "short-lived hashed token insert"
      pattern: "password_reset_tokens"
  prohibitions:
    - "No standing bypass credential in the running server — host access is the only proof of ownership"
    - "No Casbin / role table imported or wired"
---

<objective>
Ship the MUSR-06 "break-glass first" deliverable: a host-only CLI recovery path plus the
explicit `local` admin-capability seed that the rest of Phase 36's capability gating depends on.

Break-glass reuses the already-shipped short-lived hashed-token infra (`aura.password_reset_tokens`,
migration 0023) — no new token scheme (D-16). Capability grant/revoke already exists in
`cmd/aura/identity.go`; this plan adds the `recover` branch and seeds `local`'s explicit caps (D-25).

Purpose: MUSR-06 requires provisioning + break-glass shipped FIRST, before the Authula cutover legs
build on capability gating. `local` must hold real admin caps so admin-gated routes resolve.
Output: `aura identity recover`, seed migration 0026, audited grant/revoke confirmation.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-PATTERNS.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed local's explicit admin capabilities (migration 0026)</name>
  <read_first>
    - internal/db/migrations/0004_identity.up.sql (the `INSERT INTO aura.capability_grants ... ON CONFLICT DO NOTHING` seed of local's `*` grant — the exact analog, PATTERNS Cluster C)
    - internal/identity/store.go (HasCapability wildcard `*` handling, ~lines 125-138)
    - 36-CONTEXT.md D-25 (local seeded with admin caps) and 36-RESEARCH.md OQ3 (reuse `governance.write` rather than a net-new `settings.model.write`)
  </read_first>
  <action>
    Add migration pair `0026_local_admin_caps.{up,down}.sql`. The up migration idempotently inserts
    explicit capability grants for the `local` identity (UUID `00000000-0000-0000-0000-000000000001`):
    `governance.write`, `identity.create`, `agent.run`. Mirror the `0004_identity.up.sql` seed shape
    exactly: `INSERT INTO aura.capability_grants (identity_id, capability) VALUES (...) ON CONFLICT DO NOTHING`.
    Per RESEARCH OQ3 we resolve the D-02/D-03 capability name to the EXISTING `governance.write`
    (already wired on the settings write-routes) — do NOT introduce `settings.model.write`. `local`
    already holds `*`; these explicit rows make the admin contract legible and survive any future
    narrowing of the wildcard. The down migration deletes exactly those three seeded rows for `local`.
    Add the standard `GRANT`-unchanged comment; no schema change, seed only.
  </action>
  <verify>
    <automated>go build ./... && go test -tags db_integration -run TestMigrations ./internal/db/ 2>&1 | tail -5 || echo "db_integration requires live stack"</automated>
  </verify>
  <acceptance_criteria>
    - source: internal/db/migrations/0026_local_admin_caps.up.sql contains `governance.write`, `identity.create`, `agent.run` and `ON CONFLICT DO NOTHING`
    - behavior: applying 0026 then querying `capability_grants` for local returns the three explicit rows plus the pre-existing `*`
    - test: `grep -c 'ON CONFLICT DO NOTHING' internal/db/migrations/0026_local_admin_caps.up.sql` >= 1
  </acceptance_criteria>
  <done>Migration 0026 seeds local's explicit admin caps idempotently; down migration reverses only those rows.</done>
</task>

<task type="auto">
  <name>Task 2: Break-glass `aura identity recover` subcommand (host-only)</name>
  <read_first>
    - cmd/aura/identity.go (hand-rolled switch dispatcher — NOT cobra; existing `grant`/`revoke` cases ~lines 98-124, PATTERNS Cluster C)
    - internal/db/migrations/0023_identity_recovery.up.sql (aura.password_reset_tokens: token_hash PK, expires_at, max_attempts — the infra to reuse)
    - cmd/aura/serve_password_reset.go (the ONLINE self-service reset — the counterpart to diff the token-minting shape against)
    - internal/identity/store.go (GetIdentityByName, GrantCapability, RevokeCapability signatures)
    - 36-CONTEXT.md D-16 (break-glass = CLI-minted recovery on the host; host access = proof of ownership; no standing bypass)
  </read_first>
  <action>
    Create `cmd/aura/recovery.go` and add a `case "recover"` branch to the `identity.go` switch tree
    (keep identity.go under 600 LOC — put the implementation in recovery.go, only the dispatch case in
    identity.go). `aura identity recover <name>` resolves the identity via `GetIdentityByName`, mints a
    short-lived recovery token by INSERTing a hashed token into `aura.password_reset_tokens` (reuse the
    exact hashing + `expires_at` + `max_attempts` shape from `serve_password_reset.go`; do NOT invent a
    new token scheme), and prints the one-time plaintext token to stdout for the operator to hand to the
    user. Default expiry short-lived (reuse the existing reset-token TTL const, ~1h). The token is minted
    only on the host CLI (host access == ownership); there is NO server route that mints an admin bypass.
    Never log the plaintext token via slog — stdout print only. Audited via the existing recovery audit
    trail if `serve_password_reset.go` writes one.
  </action>
  <verify>
    <automated>go vet ./cmd/aura/ && go build ./... && go test ./cmd/aura/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - source: cmd/aura/recovery.go inserts into `aura.password_reset_tokens` reusing the serve_password_reset hashing (no new token table)
    - behavior: `aura identity recover <name>` on an existing identity prints a one-time token and exits 0; unknown name exits non-zero
    - test: `go build ./...` succeeds and cmd/aura remains buildable; `wc -l cmd/aura/identity.go` <= 600
  </acceptance_criteria>
  <done>`aura identity recover` mints a working short-lived reset token from the host reusing 0023 infra; identity.go stays under the LOC cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host shell → aura CLI | The operator on the host is trusted; host access is the ownership proof for break-glass |
| CLI → aura.password_reset_tokens | Minted token is a bearer secret until consumed/expired |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-E | Elevation of Privilege | standing server bypass | mitigate | No server route mints an admin bypass; recovery is host-CLI only (D-16), Task 2 |
| T-36-01-I | Information Disclosure | plaintext recovery token | mitigate | Token printed to stdout only, never slog-logged; hashed at rest (0023 reuse), Task 2 |
| T-36-01-T | Tampering | forged capability grant | accept | Only host CLI + admin-gated routes grant caps; host trust boundary, `local` seed idempotent |
| T-36-01-SC | Tampering | package installs | mitigate | No new packages this plan (RESEARCH Package Legitimacy Audit: zero installs) |
</threat_model>

<verification>
- `go build ./...` clean; `go vet ./cmd/aura/` clean.
- Migration 0026 up/down apply cleanly on the live stack (`db_integration`).
- `aura identity recover <name>` prints a one-time token; `aura identity grant/revoke` still work.
- cmd/aura/identity.go remains ≤600 LOC.
</verification>

<success_criteria>
- Break-glass recovery CLI ships (MUSR-06 "shipped first").
- `local` holds explicit `governance.write` + `identity.create` + `agent.run` grants.
- No standing server bypass; no new token scheme; no Casbin wiring.
</success_criteria>

<artifacts_produced>
- CLI: `aura identity recover <name>` (new subcommand, cmd/aura/recovery.go)
- Migration: `0026_local_admin_caps` (seed local admin caps)
- Capability resolution: D-02/D-03 `settings.model.write` realized as EXISTING `governance.write` (RESEARCH OQ3)
</artifacts_produced>

<output>
Create `.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-01-SUMMARY.md` when done.
</output>
