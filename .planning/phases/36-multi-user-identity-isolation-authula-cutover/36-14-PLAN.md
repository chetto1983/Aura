---
phase: 36-multi-user-identity-isolation-authula-cutover
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/migrations/0033_scheduler_identity_purge_kind.up.sql
  - internal/db/migrations/0033_scheduler_identity_purge_kind.down.sql
  - internal/cron/store.go
  - internal/db/migrate_0033_integration_test.go
  - cmd/aura/serve_provisioning.go
  - cmd/aura/serve_onboarding.go
  - cmd/aura/serve_dispatch.go
  - cmd/aura/serve.go
  - cmd/aura/serve_provisioning_test.go
  - internal/identity/store.go
  - internal/agui/auth.go
  - cmd/aura/serve_auth.go
  - internal/agui/auth_deactivation_test.go
autonomous: true
gap_closure: true
requirements: [MUSR-01, MUSR-06]
must_haves:
  truths:
    - "A real HTTP admin-create through the running `aura serve` binary provisions the per-identity Garage bucket+key (aura.identity_object_store row exists) AND the per-identity mcp/skills/pyscripts/agents dirs"
    - "The grace-window de-provisioning purge is schedulable: aura.scheduler_tasks.kind CHECK admits 'identity_purge', handlers.IdentityPurgeHandler is registered in the live dispatch map with a non-nil Purger, and a purge task is seeded at boot"
    - "A soft-deleted (deactivated_at IS NOT NULL) principal is denied at the auth boundary and cannot re-authenticate during the grace window"
  prohibitions:
    - "buildOnboardingService MUST NOT ship with nil ObjectStore/Filesystem/Journal when the pool + Garage admin config are present (that leaves provisioning inert)"
    - "A deactivated identity MUST NOT pass RequireAuth (no re-login while soft-deleted), even though its row still exists for the deprovision saga + admin roster"
    - "GetIdentityByID/GetIdentityByName MUST NOT gain an `AND deactivated_at IS NULL` filter (ResolveTarget + admin roster need to see deactivated rows) — the deny happens at the auth boundary, not in the query"
    - "The purge saga MUST NOT tear down a plane for an identity other than its own (owner-keyed saga_id; each leg is identity-scoped)"
  artifacts:
    - path: "internal/db/migrations/0033_scheduler_identity_purge_kind.up.sql"
      provides: "widened scheduler_tasks.kind CHECK admitting 'identity_purge'"
    - path: "cmd/aura/serve_provisioning.go"
      provides: "ObjectStoreProvisioner / FilesystemProvisioner / SagaJournal / IdentityDeactivator composition-root adapters + Deprovisioner builder + purge-sweep seeder"
    - path: "internal/agui/auth.go"
      provides: "Identity.Deactivated projection + RequireAuth deactivation deny"
---

<objective>
Close VERIF-3/HI-01 (provisioning + de-provisioning are wired ONLY in tests; the daemon enforces neither) AND HI-02 (deactivation does NOT block re-login) — these MUST ship together (gap #3): wiring de-provisioning while the auth path still fails open would ship a reachable authorization bypass.

Three concrete failures, all confirmed against HEAD:
1. `buildOnboardingService` (`cmd/aura/serve_onboarding.go`) builds `OnboardingDeps{}` with `.ObjectStore`/`.Filesystem`/`.Journal` nil → `provisionResourceLegs` nil-guards skip the Garage-bucket + per-identity-dir + journal legs → a real admin-create creates the identity + Authula user but NO bucket/key and NO per-identity dirs.
2. `handlers.IdentityPurgeHandler` is absent from `cmd/aura/serve_dispatch.go`'s `real` map; `Deprovisioner` is never constructed; and `aura.scheduler_tasks.kind`'s CHECK (0009→0010) never admitted `'identity_purge'` → a purge task cannot be inserted, so the grace-window purge cannot run by ANY mechanism.
3. `internal/agui/auth.go` `RequireAuth` (line 223) treats any existing identity row as valid; the `agui.Identity` projection carries no deactivation flag; `GetIdentityByID`/`GetIdentityByName` select but do not surface `deactivated_at` → a soft-deleted principal re-authenticates (email+password still valid) and regains full access + capabilities for the whole grace window.

Purpose: make the object-store + filesystem isolation planes and the D-27 de-provisioning lifecycle actually enforced by the shipped daemon, with the deactivation auth-gate closing the fail-open the moment de-provisioning becomes reachable.
Output: migration 0033, composition-root provisioning/de-provisioning adapters wired live, a scheduled purge sweep, and a deactivation deny at the auth boundary.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-VERIFICATION.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-REVIEW.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-CONTEXT.md
@.claude/skills/golang-database/SKILL.md
@.claude/skills/golang-security/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0033 — widen scheduler_tasks.kind CHECK for 'identity_purge' + cron kind constant + round-trip test</name>
  <files>internal/db/migrations/0033_scheduler_identity_purge_kind.up.sql, internal/db/migrations/0033_scheduler_identity_purge_kind.down.sql, internal/cron/store.go, internal/db/migrate_0033_integration_test.go</files>
  <read_first>
    - internal/db/migrations/0010_skill_audit.up.sql (lines 96-99 — the EXACT DROP CONSTRAINT scheduler_tasks_kind_check + ADD CONSTRAINT ... CHECK (kind IN (...)) widen pattern to mirror; it added 'skill_ttl_sweep' to the 0009 list)
    - internal/db/migrations/0009_scheduler.up.sql (line 16 — the original CHECK: kind IN ('reminder','agent_job','backup_postgres','backup_neo4j'); GRANTs at 60-61 show aura_app already has DML on scheduler_tasks — no grant change needed)
    - internal/cron/store.go (the cron package file that defines the TaskKind constants incl. KindSkillTTLSweep/KindReminder — grep `KindSkillTTLSweep` under internal/cron to confirm the exact file; add the new constant beside it)
    - internal/cron/handlers/identity_purge.go (KindIdentityPurge TaskKind = "identity_purge" already defined in the handlers package — the cron-package constant must equal this string literal)
    - internal/db/migrate_0026_integration_test.go (the fresh-DB drill harness shape: EnsureRoles, CREATE DATABASE aura_migrate0026_drill, Migrate up, currentMigrationVersion — copy this skeleton for the 0033 round-trip test)
  </read_first>
  <action>
    Migration 0033 (next free slot after 0032_owner_rls). up.sql: mirror 0010's widen verbatim — `ALTER TABLE aura.scheduler_tasks DROP CONSTRAINT scheduler_tasks_kind_check;` then `ALTER TABLE aura.scheduler_tasks ADD CONSTRAINT scheduler_tasks_kind_check CHECK (kind IN ('reminder','agent_job','backup_postgres','backup_neo4j','skill_ttl_sweep','identity_purge'));`. down.sql: reverse to the 0010 list (DROP + ADD the same constraint WITHOUT 'identity_purge'); add a leading `DELETE FROM aura.scheduler_tasks WHERE kind='identity_purge';` in the down so the re-added narrower CHECK cannot fail on a seeded purge row. Add the GRANT ALL ... TO aura_migrate comment convention consistent with 0010. Every up has a matching down.

    In internal/cron/store.go add `KindIdentityPurge TaskKind = "identity_purge"` beside KindSkillTTLSweep, with a one-line comment noting it is the D-27 grace-window purge kind and MUST equal handlers.KindIdentityPurge's literal. Do NOT change any other cron constant.

    Confirm sqlc is unaffected: a pure CHECK-constraint widen touches no `internal/db/queries/*.sql`, so `sqlc generate` yields ZERO diff (the sqlc-generate-is-in-sync CI gate stays green). Run sqlc and confirm no diff; do NOT hand-edit generated files.

    Round-trip test internal/db/migrate_0033_integration_test.go (`//go:build db_integration`): on a fresh migrated DB (reuse the 0026 test's disposable-DB drill), assert (a) an INSERT into aura.scheduler_tasks with kind='identity_purge' SUCCEEDS after HEAD (the widened CHECK admits it), (b) the same INSERT would FAIL before 0033 (position at version 32 via MigrateSteps and assert the insert errors with a check_violation), and (c) 0033's down + re-up is clean (MigrateSteps straddle at v33, mirroring the version-anchored pattern from 36-13). Use envOrSkip(t,"POSTGRES_PASSWORD") (t.Fatal under $CI, no-skip-as-green).
  </action>
  <acceptance_criteria>
    - Source assertion: 0033_...up.sql contains `CHECK (kind IN ('reminder','agent_job','backup_postgres','backup_neo4j','skill_ttl_sweep','identity_purge'))`; down.sql omits 'identity_purge' and pre-deletes identity_purge rows.
    - Source assertion: internal/cron/store.go defines KindIdentityPurge with value "identity_purge".
    - sqlc: `go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate` then `git diff --stat internal/db/sqlc/` is empty.
    - Build: `go build ./... && go build -tags db_integration ./internal/db/` clean.
    - Test command (live, WSL/CI): `go test -tags db_integration -run TestMigration0033 ./internal/db/ -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet -tags db_integration ./internal/db/ ./internal/cron/</automated>
  </verify>
  <done>The scheduler kind CHECK admits 'identity_purge', a cron constant exists for it, and a live round-trip test proves the widen + reversibility.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the provisioning + de-provisioning saga into the serve composition root</name>
  <files>cmd/aura/serve_provisioning.go, cmd/aura/serve_onboarding.go, cmd/aura/serve_dispatch.go, cmd/aura/serve.go, cmd/aura/serve_provisioning_test.go</files>
  <read_first>
    - internal/agui/provisioning_saga_resumable_test.go (THE TEMPLATE — liveJournal (Record/CompletedSteps raw-SQL over aura.provisioning_saga), liveObjectStore (garageadmin.Client + objectstore.IdentityStore: ProvisionObjectStore = Resolve-idempotent-skip → CreateBucket → CreateKey → AllowBucketKey → store.Put; DeprovisionObjectStore = DeleteKey/DeleteBucket/store.Delete), liveFilesystem (profile.RootIdentityDir subRoots MkdirAll + write-if-absent Agent.md; DeprovisionIdentityDirs = RemoveAll). Promote these test adapters to production code in serve_provisioning.go)
    - internal/agui/onboarding_provision_resources.go (the ports being satisfied: ObjectStoreProvisioner{ProvisionObjectStore,DeprovisionObjectStore}, FilesystemProvisioner{ProvisionIdentityDirs,DeprovisionIdentityDirs}; provisionResourceLegs nil-guards each leg)
    - internal/agui/onboarding_provision.go (Provision calls provisionResourceLegs; OnboardingDeps is consumed here — the fields to set are ObjectStore/Filesystem/Journal per the test's newOnboardingService(OnboardingDeps{Journal,ObjectStore,Filesystem}))
    - internal/agui/deprovision.go (DeprovisionDeps{Journal,Deactivator,Sessions,Jobs,Conversations,Graph,ObjectStore,Filesystem,IdentityDelete,AuthulaDelete,GraceWindow}; NewDeprovisioner; PurgeExpired scans Deactivator.ListPurgeable; IdentityDeactivator{MarkDeactivated,ListPurgeable,ResolveTarget}; each port optional/nil-skips)
    - internal/agui/saga_journal.go (SagaJournal port: Record(ctx,sagaID,identityID,kind,step,status), CompletedSteps(ctx,sagaID))
    - internal/cron/handlers/identity_purge.go (IdentityPurgeHandler{Purger IdentityPurger}; IdentityPurger.PurgeExpired(ctx,now)(int,error) — the *agui.Deprovisioner satisfies it; KindIdentityPurge)
    - cmd/aura/serve_onboarding.go (buildOnboardingService — the deps literal at lines 247-263 to extend; authulaCoreAdapter already provides DeleteUser for AuthulaDelete; auraLegAdapter.DeleteIdentity for IdentityDelete)
    - cmd/aura/serve_dispatch.go (buildDispatch `real` map at lines 45-54 — add the KindIdentityPurge entry; handlerAdapter bridges handlers.Handler onto cron.Handler)
    - cmd/aura/serve.go (lines 271-296 — objectStore built at 272, chat.assets at 277, buildDispatch at 284, seedSkillTTLSweep at 294; and lines 534-564 seedSkillTTLSweep is the seed template; buildOnboardingService called at 450)
    - internal/objectstore/identity_store.go (NewIdentityStore(pool, authulaSecretHex, shared Credentials, localIdentity) — build the resolver from cfg.AuthulaSecret + shared bucket/creds)
    - internal/objectstore/garageadmin/client.go (garageadmin.New(endpoint, token); BucketForIdentity; CreateBucket/CreateKey/AllowBucketKey/DeleteBucket/DeleteKey; ReadWrite/Permissions)
    - internal/profile/store.go (RootIdentityDir(root, identity) traversal-safe rooting)
    - internal/config/config.go (GarageAdminEndpoint/GarageAdminToken, ObjectStoreBucket/AccessKey/SecretKey, AuthulaSecret, SkillsDir/ProfileDir fields for the FS roots)
    - internal/agui/audit_store.go (PgAuditStore — the raw-pgx composition-root adapter precedent for the new IdentityDeactivator)
  </read_first>
  <action>
    NEW cmd/aura/serve_provisioning.go (keep ≤600 LOC). Promote the shipped live test adapters to production:
    1. `objectStoreProvisionAdapter` satisfying agui.ObjectStoreProvisioner over a *garageadmin.Client + *objectstore.IdentityStore — bodies copied from liveObjectStore.ProvisionObjectStore/DeprovisionObjectStore (Resolve→idempotent skip; CreateBucket/CreateKey/AllowBucketKey(ReadWrite); store.Put; and the symmetric DeleteKey/DeleteBucket/store.Delete). Remember the bucket internal id per identity for the delete (a small mutex-guarded map, as in the template).
    2. `filesystemProvisionAdapter` satisfying agui.FilesystemProvisioner over profile.RootIdentityDir, rooted at the REAL config dirs (the ~/.aura/mcp, $AURA_SKILLS_DIR, ~/.aura/pyscripts, ~/.aura/agents bases the mcp/skills packages use — resolve from cfg, NOT a temp dir), MkdirAll(0o700) + write-if-absent empty Agent.md; DeprovisionIdentityDirs = RemoveAll each root.
    3. `sagaJournalAdapter` satisfying agui.SagaJournal over chat.pool — bodies copied from liveJournal (INSERT ... ON CONFLICT (saga_id, step) DO UPDATE; SELECT step WHERE status='done').
    4. `identityDeactivatorAdapter` satisfying agui.IdentityDeactivator over chat.pool (raw-pgx, PgAuditStore precedent): MarkDeactivated = UPDATE aura.identities SET deactivated_at=now(), purge_after=$2 WHERE id=$1; ListPurgeable = SELECT i.id, i.name, l.authula_user_id FROM aura.identities i LEFT JOIN aura.identity_auth_links l ON l.identity_id=i.id WHERE i.deactivated_at IS NOT NULL AND i.purge_after <= $1 → []DeprovisionTarget; ResolveTarget = the same by i.id.
    5. Constructor helpers `buildProvisioningPorts(chat) (agui.ObjectStoreProvisioner, agui.FilesystemProvisioner, agui.SagaJournal)` returning NIL adapters when the pool OR the Garage admin config (cfg.GarageAdminEndpoint/Token) is absent (backward compatible — an interview-only / pre-cutover deploy provisions nothing), and `buildDeprovisioner(chat) *agui.Deprovisioner` assembling DeprovisionDeps with the four new adapters + reuse IdentityDelete=identity.New(chat.pool).DeleteIdentity-backed port + AuthulaDelete=the authula core DeleteUser; wire Conversations/Graph/Sessions/Jobs where a one-line reuse exists, otherwise leave nil and record the nil planes in the SUMMARY as a data-retention follow-up (fail-closed-secure: the deactivated identity cannot log in per Task 3, so any retained plane is inert, not a leak). GraceWindow left default (7 days).
    6. `seedIdentityPurgeSweep(ctx, store *cron.Store) error` mirroring seedSkillTTLSweep verbatim: scan ListActiveTasks for an existing KindIdentityPurge; if absent, CreateTask with Kind=cron.KindIdentityPurge on an 'every' schedule (e.g. every 15 minutes) — the 0033-widened CHECK admits the row.

    serve_onboarding.go: in buildOnboardingService, after the existing `if chat.pool != nil` block, call `os, fs, jr := buildProvisioningPorts(chat)` and set deps.ObjectStore=os, deps.Filesystem=fs, deps.Journal=jr. Preserve the nil-safe behavior (unwired ports skip their leg).

    serve_dispatch.go: in buildDispatch's `real` map add `cron.KindIdentityPurge: handlers.IdentityPurgeHandler{Purger: buildDeprovisioner(chat)}`.

    serve.go: beside the existing `seedSkillTTLSweep(ctx, store)` call (~line 294) add `if err := seedIdentityPurgeSweep(ctx, store); err != nil { slog.Warn("aura serve: seed identity purge sweep", "err", err) }`.

    serve_provisioning_test.go: unit-assert (no live stores) that (a) buildProvisioningPorts returns non-nil ObjectStore/Filesystem/Journal when a pool + Garage admin config are present and nil when absent; (b) buildDeprovisioner returns a non-nil *Deprovisioner whose Purger seam is wired; (c) the buildDispatch real map contains cron.KindIdentityPurge (construct with a fake chatEnv/config as the existing serve_test.go fakes do). The live end-to-end (bucket+dirs created by a real admin-create) is covered by the 36-18 musr-e2e run.
  </action>
  <acceptance_criteria>
    - Source assertion: grep confirms `deps.ObjectStore`, `deps.Filesystem`, `deps.Journal` are assigned in buildOnboardingService (non-empty right-hand sides).
    - Source assertion: cmd/aura/serve_dispatch.go `real` map contains a `cron.KindIdentityPurge:` entry with `Purger:` set; grep `handlers.IdentityPurgeHandler` in cmd/aura returns a non-test hit.
    - Source assertion: `objectstore.IdentityStore.Resolve` / `NewIdentityStore` now have a NON-test consumer in cmd/aura (grep confirms serve_provisioning.go references them).
    - Build: `go build ./... && go vet ./...` clean; `go test ./cmd/aura/` (untagged) green including the new serve_provisioning_test.go.
    - Behavior (live, WSL/CI at 36-18): a real admin-create yields an aura.identity_object_store row + the per-identity dirs; the seeded identity_purge task exists in aura.scheduler_tasks.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./cmd/aura/ -run 'Provision|Onboard|Dispatch'</automated>
  </verify>
  <done>The running daemon provisions per-identity Garage bucket/key + dirs on admin-create, constructs the Deprovisioner, registers the purge handler, and seeds the grace-window purge sweep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deny deactivated identities at the auth boundary (HI-02)</name>
  <files>internal/identity/store.go, internal/agui/auth.go, cmd/aura/serve_auth.go, internal/agui/auth_deactivation_test.go</files>
  <behavior>
    - Test 1: RequireAuth with a wired identityChecker returning agui.Identity{Deactivated:true} for a valid session → the request is DENIED (redirectToLogin → 302 for a browser GET / 401 for XHR), next.ServeHTTP is NOT called.
    - Test 2: RequireAuth with agui.Identity{Deactivated:false} (a live identity) + a valid session → the request PASSES (existing behavior unchanged; the principal is stamped).
    - Test 3: identity.Store.fromRow maps a row with deactivated_at Valid=true → Identity.Deactivated=true, and Valid=false (NULL) → Deactivated=false.
  </behavior>
  <read_first>
    - internal/agui/auth.go (Identity struct lines 43-47 {ID,Name,Kind}; identityChecker seam lines 35-38; RequireAuth existence re-check line 223 `if _, err := deps.Identities.GetIdentityByID(...)`)
    - internal/identity/store.go (Identity struct lines 61-65 {ID,Name,Kind}; fromRow line 68-70; GetIdentityByID/GetIdentityByName return fromRow(r); the sqlc AuraIdentities row already carries DeactivatedAt pgtype.Timestamptz — 0029 columns are already SELECTed per internal/db/queries/identity.sql)
    - internal/db/queries/identity.sql (GetIdentityByID/GetIdentityByName already select deactivated_at, purge_after — do NOT add a WHERE filter; the row already exposes the column)
    - cmd/aura/serve_auth.go (identityCheckerAdapter.GetIdentityByID lines 41-47 maps identity.Identity → agui.Identity{ID,Name,Kind} — the one place to also copy Deactivated)
    - cmd/aura/serve_webui_auth_test.go (wiringIdentities/uncapableIdentities fakes return agui.Identity{...} — they compile unchanged with a new zero-value bool field; confirm no fake needs updating)
    - internal/identity/store_fake_test.go (idRow already supplies deactivated_at NULL at position 5 — fromRow's new mapping reads it; a live identity stays Deactivated=false)
  </read_first>
  <action>
    Write the tests FIRST (auth_deactivation_test.go, package agui), then:
    1. internal/identity/store.go: add `Deactivated bool` to Identity; in fromRow set `Deactivated: r.DeactivatedAt.Valid` (the sqlc row's DeactivatedAt is a pgtype.Timestamptz; Valid=true means deactivated_at IS NOT NULL). Leave GetIdentityByID/GetIdentityByName queries untouched.
    2. internal/agui/auth.go: add `Deactivated bool` to the Identity projection (lines 43-47). In RequireAuth (line 223) change the existence re-check to capture the identity and deny on deactivation: `id, err := deps.Identities.GetIdentityByID(r.Context(), identityID); if err != nil || id.Deactivated { deps.redirectToLogin(w, r); return }`. Update the comment to note the deactivation deny (a soft-deleted principal invalidates the session even with a valid Authula token).
    3. cmd/aura/serve_auth.go: in identityCheckerAdapter.GetIdentityByID map `Deactivated: idn.Deactivated` into the returned agui.Identity.
    Do NOT add `AND deactivated_at IS NULL` to any query (the deprovision saga's ResolveTarget + the admin roster legitimately need to see deactivated rows). The deny is at the boundary only.
  </action>
  <acceptance_criteria>
    - Source assertion: agui.Identity and identity.Identity both have a `Deactivated bool` field; RequireAuth's re-check denies when `id.Deactivated`; identityCheckerAdapter copies Deactivated; NO query gained `deactivated_at IS NULL`.
    - Behavior assertion: the three TDD tests above pass (`go test ./internal/agui/ -run Deactivat`).
    - Build: `go build ./... && go vet ./...` clean; `go test ./internal/identity/ ./internal/agui/ ./cmd/aura/` (untagged) green; existing auth fakes compile unchanged.
    - `go test -race ./internal/agui/` clean (run in WSL/CI — no CGO on the Windows host).
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go test ./internal/agui/ -run Deactivat && go test ./internal/identity/</automated>
  </verify>
  <done>A soft-deleted principal is denied at RequireAuth and cannot re-authenticate during the grace window, while deactivated rows stay visible to the deprovision saga + admin roster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated web request → identity store | The session cookie asserts a principal; a soft-deleted principal must be refused here (HI-02) |
| admin-create HTTP flow → per-identity Garage bucket + filesystem roots | Provisioning must create the storage-enforced isolation boundary, else a new identity shares the operator's bucket/dirs |
| scheduled purge → every store plane | The de-provisioning saga tears down data; it must key strictly on the target identity id (no cross-identity teardown) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14-01 | Elevation of Privilege | RequireAuth deactivation re-check | mitigate | Surface deactivated_at on the identity projection and deny in RequireAuth — closes the fail-open where a soft-deleted principal re-authenticates and regains all capabilities during the grace window (HIGH — becomes reachable the instant provisioning wiring lands) |
| T-36-14-02 | Information Disclosure | per-identity Garage bucket / filesystem roots | mitigate | Wire the ObjectStore/Filesystem provisioners into OnboardingDeps so admin-create actually creates the per-identity bucket+key+dirs (the storage-enforced isolation the phase built); nil-safe when unconfigured (pre-cutover) |
| T-36-14-03 | Tampering/DoS | de-provisioning purge saga | mitigate | The saga is owner-keyed (saga_id = UUIDv5(kind,identityID)); each leg is identity-scoped; ListPurgeable only returns rows past their own purge_after — a purge can never tear down a foreign identity's plane |
| T-36-14-04 | Denial of Service | grace-window purge scheduler | mitigate | Migration 0033 admits 'identity_purge' + the handler is registered + a task is seeded, so purge runs on schedule (5-min budget, idempotent, no-reschedule-on-recovery) instead of being structurally unschedulable |
| T-36-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages (stdlib + existing pgx/garageadmin/objectstore/profile); go.mod/go.sum byte-unchanged. RESEARCH Package Legitimacy Audit: zero installs this phase |
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean (untagged) and `go vet -tags db_integration ./internal/db/` clean.
- `go test ./cmd/aura/ ./internal/agui/ ./internal/identity/ ./internal/cron/` (untagged) green.
- `sqlc generate` yields zero diff (sqlc-generate-is-in-sync gate).
- Live tiers (run in WSL/CI at 36-18): `db_integration TestMigration0033*`, the `db_integration garage_integration TestProvisioningSagaResumable` (still green with production adapters), and the musr-e2e admin-create-provisions-resources leg; `-race ./internal/agui/`.
- COVERAGE FLOOR 85%: new adapters + the deactivation gate are exercised by the new unit tests + the live saga test; report the combined figure at phase close.
</verification>

<success_criteria>
- Admin-create through `aura serve` provisions the per-identity Garage bucket/key + dirs (aura.identity_object_store row + dirs exist).
- The grace-window purge is schedulable + registered + seeded; the Deprovisioner is constructed with live ports.
- A deactivated principal is denied at RequireAuth; deactivated rows stay visible to the saga + admin roster.
- No new dependencies; sqlc in sync; file-size cap respected (new adapters in serve_provisioning.go, ≤600 LOC).
</success_criteria>

<artifacts_produced>
- Migration `0033_scheduler_identity_purge_kind.{up,down}.sql` (scheduler kind CHECK admits 'identity_purge').
- `cron.KindIdentityPurge` TaskKind constant.
- `cmd/aura/serve_provisioning.go` — objectStoreProvisionAdapter, filesystemProvisionAdapter, sagaJournalAdapter, identityDeactivatorAdapter, buildProvisioningPorts, buildDeprovisioner, seedIdentityPurgeSweep.
- `OnboardingDeps.ObjectStore/Filesystem/Journal` wired live in buildOnboardingService.
- `cron.KindIdentityPurge → handlers.IdentityPurgeHandler{Purger}` entry in the live dispatch map + a seeded purge task.
- `identity.Identity.Deactivated` + `agui.Identity.Deactivated` fields; RequireAuth deactivation deny; identityCheckerAdapter mapping.
</artifacts_produced>

<output>
Create `.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-14-SUMMARY.md` when done. Record any Deprovisioner reverse-leg port left nil (Conversations/Graph/Sessions/Jobs) as an explicit data-retention follow-up.
</output>
