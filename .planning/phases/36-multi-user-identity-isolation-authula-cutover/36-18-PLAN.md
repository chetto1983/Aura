---
phase: 36-multi-user-identity-isolation-authula-cutover
plan: 18
type: execute
wave: 3
depends_on: [36-13, 36-14, 36-15, 36-16, 36-17]
files_modified:
  - .planning/phases/36-multi-user-identity-isolation-authula-cutover/36-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [MUSR-01, MUSR-02, MUSR-03, MUSR-04, MUSR-05, MUSR-06]
must_haves:
  truths:
    - "The whole Phase-36 branch is pushed and the FULL GitHub Actions matrix is GREEN on the actually-pushed HEAD — Unit, CodeQL, Integration (db_integration), Knowledge integration + smoke (neo4j), sqlc-generate-is-in-sync, Build+vet+lint+deadcode+file-size, AND musr-e2e (the five-tag two-identity E2E)"
    - "The live-stack + -race matrix (the two-identity cross-deny E2E, the neo4j backfill, the db/garage/authula integration tiers) runs green in WSL/CI — not inferred from a Windows compile"
    - "The AURA_MUSR_ISOLATION rollout timing for this deployment is decided and RECORDED (backfill-then-flip executed, OR a VERIFICATION.md override explaining why off-for-now is acceptable)"
  prohibitions:
    - "The phase MUST NOT be marked complete while any CI job is red or unrun on the pushed HEAD"
    - "AURA_MUSR_ISOLATION MUST NOT be flipped on before `aura documents backfill` runs (D-12: the flip must not orphan the operator's existing documents)"
    - "A green LOCAL Windows compile MUST NOT be accepted as proof of the live tiers (no-skip-as-green: the tagged tiers t.Fatal under $CI, so a skipped tier fails the gate — verify execution, not just PASS)"
  artifacts:
    - path: ".planning/phases/36-multi-user-identity-isolation-authula-cutover/36-VERIFICATION.md"
      provides: "the recorded rollout decision / override for human_verification #2"
---

<objective>
Terminal acceptance gate for Phase 36. Close VERIF-2 (the back half of the phase was never pushed or CI-validated) + human_verification #1 (run the full live-stack + -race matrix) + human_verification #2 (record the AURA_MUSR_ISOLATION rollout decision).

`origin/master` (f44dde45) contains only 36-01/02/03/04/07; the local branch is 27+ commits ahead — 36-05, 36-06, 36-08..36-12 (the object-store plane, provisioning saga, delete lifecycle, admin/audit UI, Telegram routing, the two-identity acceptance E2E + its musr-e2e CI job) plus the gap-closure work in 36-13..36-17 have NEVER run through any GitHub Actions job. After all code gaps land (this plan depends on 36-13..36-17), push the branch and confirm the FULL CI matrix is green on the actually-pushed HEAD, including the five-tag `musr-e2e` two-identity E2E that gates MUSR-01. This subsumes the human_verification #1 live-matrix run (the Windows host cannot run CGO/-race/live-stack; WSL/CI can, per CLAUDE.md "Where to run what").

Purpose: the phase cannot close on inference — it closes when CI proves it on the pushed HEAD and the operator records the isolation-activation timing.
Output: a green CI matrix on the pushed HEAD + a recorded rollout decision. Marked `autonomous: false` — it needs the operator's push, a live WSL/CI stack, and a business decision.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-VERIFICATION.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local pre-push gate (Windows-runnable) before the CI run</name>
  <files>(no source changes — verification commands only)</files>
  <read_first>
    - .planning/STATE.md (the 12 SUMMARY entries — confirm all gap-closure plans 36-13..36-17 have landed with their SUMMARYs before pushing)
    - .github/workflows/ci.yml (the job matrix to expect green: the Build+vet+lint+deadcode+file-size job now includes the no-URL-token step from 36-13; the musr-e2e job from 36-12)
    - scripts/check-file-size.sh, scripts/check-no-url-tokens.sh (the static gates)
  </read_first>
  <action>
    Run the full Windows-runnable pre-push gate and confirm each is clean BEFORE the push checkpoint (fix-forward into the relevant gap plan if any fails — do NOT push red):
    - `go build ./...` (clean)
    - `go vet ./...` (clean) and `go vet -tags 'db_integration neo4j_integration garage_integration authula_integration musr_e2e' ./...` (all five tags compile)
    - `go test ./...` untagged (green — the whole tree; the tagged tiers skip locally but MUST compile)
    - `go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate` then `git diff --stat internal/db/sqlc/` is EMPTY (sqlc-generate-is-in-sync)
    - `bash scripts/check-file-size.sh` (all ≤600 LOC)
    - `bash scripts/check-no-url-tokens.sh` + `--self-test` (no long-lived token in a URL)
    Record the pass/fail of each in the SUMMARY. This is the Windows-verifiable floor; the live tiers run in CI (Task 2).
  </action>
  <acceptance_criteria>
    - `go build ./...` + both `go vet` invocations clean; untagged `go test ./...` green.
    - sqlc diff empty; check-file-size + check-no-url-tokens (+self-test) exit 0.
    - Any failure is fixed in the owning gap plan (36-13..36-17) before proceeding — the push is NOT done on a red local gate.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./... && bash scripts/check-file-size.sh && bash scripts/check-no-url-tokens.sh</automated>
  </verify>
  <done>The full Windows-runnable gate is green; the tree is ready to push for the live CI matrix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All Phase-36 gap-closure code (36-13 CI gates + migration-0026 fix; 36-14 provisioning/de-provisioning wiring + migration 0033 + deactivation gate; 36-15 per-identity object-store consumption; 36-16 documents default-closed + config-validate + local-fallback; 36-17 Telegram fail-closed + shell admin caps) on top of the never-pushed 36-05/06/08..12 back half. The local pre-push gate (Task 1) is green.</what-built>
  <how-to-verify>
    1. Push the branch to the remote (per CLAUDE.md "git push at the end of a phase"; if on `master` the operator confirms pushing the 27+ ahead commits, else push the working branch): `git push`.
    2. Watch the full CI matrix on the pushed HEAD (gh CLI is authed — `gh run watch` / `gh run list --branch <branch>` / `gh run view <id>`). Confirm ALL of these jobs are GREEN on the ACTUALLY-PUSHED HEAD:
       - Unit
       - CodeQL
       - Integration tests (db_integration tag) — incl. TestMigration0026LocalAdminCapsRoundTrip (36-13 fix) + TestMigration0033* + the RLS backstop tier
       - Knowledge integration + smoke (neo4j + sidecar) — incl. TestDocumentsFailClosed + TestDocumentsBackfill
       - sqlc generate is in sync
       - Build + vet + lint + deadcode + file-size cap — incl. the new no-URL-token step (36-13)
       - musr-e2e — the five-tag live two-identity E2E (TestTwoIdentityCrossDeny + TestProvisionLoginIsolatedRun) with -race on the phase-36 packages
    3. If a live tier reveals a regression the Windows compile could not (a -race data race, a live-stack behavior), fix it in the owning gap plan and re-push. A skipped tagged tier is a FAILURE (no-skip-as-green: the tiers t.Fatal under $CI) — a sub-second "integration" runtime is a skip tell.
    This checkpoint subsumes human_verification #1 (the full live-stack + -race matrix runs on Linux CI, which the Windows host cannot provide).
  </how-to-verify>
  <resume-signal>Type "approved" once every listed CI job is green on the pushed HEAD, or describe the failing job(s) so the owning gap plan is revised.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>The AURA_MUSR_ISOLATION rollout timing for THIS deployment (human_verification #2).</decision>
  <context>
    The documents-plane (Neo4j) fail-closed scoping is gated by AURA_MUSR_ISOLATION (default off). The mechanism is verified sound; only the ACTIVATION timing for this deployment's real data is a human call. D-12/D-13 require the strict order: deploy(off) → `aura documents backfill` → verify the operator still sees own docs → flip AURA_MUSR_ISOLATION=true → restart (docs/runbooks/musr-rollout.md). NOTE: 36-16 now REFUSES provisioning a new user while the flag is off and FAILS server_production validation when off, so activation is coupled in code — but the operator must still decide when to run the backfill + flip for THIS deployment. Record the decision in 36-VERIFICATION.md.
  </context>
  <options>
    <option id="activate-now">
      <name>Backfill + flip now</name>
      <pros>Documents-plane isolation is live for real users immediately; the phase-goal "no cross-identity leak across documents" holds out-of-the-box; unblocks adding additional users (36-16 refuses provisioning while off)</pros>
      <cons>Requires running `aura documents backfill` against the live Neo4j + a restart with AURA_MUSR_ISOLATION=true; a brief operational window</cons>
    </option>
    <option id="defer-with-override">
      <name>Leave off for now + record a VERIFICATION.md override</name>
      <pros>No live-data operation yet; acceptable if the deployment is still single-operator (only `local` exists) — 36-16's boot WARN + provisioning refusal keep the leak un-armable</pros>
      <cons>The documents plane stays unscoped until the flip; a 2nd user cannot be provisioned until the flag is turned on (36-16 refusal); the phase closes with a recorded, time-bounded exception rather than full activation</cons>
    </option>
  </options>
  <resume-signal>Select: activate-now (run backfill+flip, record execution in 36-VERIFICATION.md) OR defer-with-override (record the override + rationale + the single-operator precondition in 36-VERIFICATION.md). Then the plan writes the decision into 36-VERIFICATION.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch → origin (CI) | Un-pushed, CI-unvalidated code cannot be trusted to enforce isolation; CI on the pushed HEAD is the acceptance authority |
| documents-plane flag → live user data | Flipping AURA_MUSR_ISOLATION before the backfill orphans the operator's docs; the order is load-bearing (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-18-01 | Repudiation/Tampering | unpushed, CI-unvalidated back half | mitigate | Push + confirm the FULL CI matrix (incl. musr-e2e + -race live tiers) green on the pushed HEAD — the phase closes on proof, not inference; no-skip-as-green enforced |
| T-36-18-02 | Information Disclosure | documents-plane rollout timing | mitigate | Record the D-12 backfill-then-flip decision; 36-16 already couples activation in code (provisioning refusal + server_production Fatal + boot WARN) so a 2nd user cannot be added into an unscoped plane |
| T-36-18-03 | Availability | flip-before-backfill | mitigate | The recorded runbook order (backfill → verify → flip) prevents orphaning the operator's existing docs |
| T-36-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages; this plan pushes + verifies + records a decision. RESEARCH audit: zero installs this phase |
</threat_model>

<verification>
- The full GitHub Actions matrix is green on the pushed HEAD (all seven job groups listed in Task 2, incl. musr-e2e).
- The live -race + integration + smoke tiers executed (not skipped) — verified via `gh run view` job logs (a sub-second tier runtime is a skip tell and fails the gate).
- The AURA_MUSR_ISOLATION rollout decision is recorded in 36-VERIFICATION.md.
- COVERAGE FLOOR 85% across the full tag matrix (unit+integration+smoke) reported at phase close — the combined figure, not a unit-only number.
</verification>

<success_criteria>
- Phase-36 branch pushed; full CI matrix green on the pushed HEAD; musr-e2e green.
- The live-stack + -race matrix ran green in CI/WSL.
- The rollout decision (activate-now OR defer-with-override) is recorded in 36-VERIFICATION.md.
- No CI job red or unrun; no isolation axis left inferred.
</success_criteria>

<artifacts_produced>
- A green CI matrix on the pushed Phase-36 HEAD (Unit, CodeQL, db_integration, neo4j integration+smoke, sqlc-in-sync, Build+lint+file-size+no-URL-token, musr-e2e).
- The recorded AURA_MUSR_ISOLATION rollout decision in 36-VERIFICATION.md (human_verification #2).
</artifacts_produced>

<output>
Create `.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-18-SUMMARY.md` when done, recording the final CI run URL + the rollout decision. After this plan, re-run phase verification (`/gsd-verify-work 36`) to confirm gaps_found → complete.
</output>
