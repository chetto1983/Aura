---
phase: 36-multi-user-identity-isolation-authula-cutover
plan: 10
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - internal/agui/audit_api.go
  - internal/agui/audit_store.go
  - cmd/aura/serve_webui.go
  - cmd/aura/serve_webui_musr.go
  - web/src/settings/SettingsPage.tsx
  - web/src/audit/AdminAuditView.tsx
  - internal/webui/dist
  - internal/agui/audit_api_test.go
autonomous: true
requirements: [MUSR-01]
must_haves:
  truths:
    - "The Settings page + admin controls are hidden in the SPA when the capability is absent (governance.write)"
    - "An admin-gated Settings control grants/revokes capabilities via the existing GrantCapability/RevokeCapability (audited)"
    - "An admin-gated web view shows per-user activity from the identity-keyed audit tables"
    - "The settings write-routes remain gated by governance.write (already wired — capability reused, not net-new)"
  artifacts:
    - path: "internal/agui/audit_api.go"
      provides: "admin-gated per-user audit read API (D-28)"
    - path: "web/src/audit/AdminAuditView.tsx"
      provides: "admin audit UI (D-28)"
  key_links:
    - from: "cmd/aura/serve_webui.go"
      to: "internal/agui.RequireCapability"
      via: "audit + grant/revoke routes mounted behind governance.write"
      pattern: "RequireCapability"
    - from: "internal/agui/audit_api.go"
      to: "aura.mcp_audit / skill_audit / tool_invocations"
      via: "per-user (actor_identity_id, created_at) reads"
      pattern: "actor_identity_id"
  prohibitions:
    - "GET /api/settings/telegram/link stays self-scoped USER — MUST NOT get the admin gate (D-02)"
    - "No net-new settings.model.write capability — reuse governance.write (RESEARCH OQ3)"
    - "SanitizeString any user text before it crosses the wire"
---

<objective>
Ship the admin/user distinction UI surface (D-03/D-26/D-28): hide the Settings page + admin controls in the
SPA when the capability is absent, add an admin-gated grant/revoke control, and ship the admin audit UI (a
per-user activity view over the identity-keyed audit tables). This is the single frontend plan — it owns the
`internal/webui/dist` rebuild.

Capability decision (RESEARCH OQ3, resolved): the settings write-routes are ALREADY gated by `governance.write`
in `serve_webui.go` — reuse it, do NOT introduce `settings.model.write`. So the settings write-gate needs no
route change; the deltas are (1) frontend capability-gating, (2) an admin grant/revoke control calling the
existing `GrantCapability`/`RevokeCapability`, (3) the audit read API + UI. `GET /api/settings/telegram/link`
stays a self-scoped USER action (D-02) — never admin-gated.

Purpose: D-28 admin audit UI + D-03/D-26 admin controls; completes the capability-gating cluster (D-01/D-04
seam untouched — Casbin-swap-ready). serve_webui.go is 538 LOC — put new mounts in serve_webui_musr.go.
Output: audit API+store, admin audit UI, frontend cap-gating, grant/revoke control, rebuilt dist.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-PATTERNS.md
@.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin audit read API + per-user query methods + route mount</name>
  <read_first>
    - internal/agui/settings_api.go (handleListSettings read → DTO projection → writeJSON ~88-139; principalIdentityID; SanitizeString precedent approvals_api.go:86 — the backing-read controller analog, PATTERNS Cluster H)
    - internal/db/migrations/0022_mcp_audit.up.sql + 0021_identity_audit + 0010 skill_audit + 0011 tool_invocations (identity-keyed audit tables; actor_identity_id) + plan-02 0031 audit identity indexes
    - cmd/aura/serve_webui.go (RequireCapability mounts ~358,382-394; governance.write const ~114; longest-pattern precedence — PATTERNS Cluster C)
    - internal/agui/auth.go (RequireCapability/HasCapability — do NOT touch the contract; D-04)
    - 36-CONTEXT.md D-28 (identity-key audit tables + full admin audit UI now)
  </read_first>
  <action>
    Create `internal/agui/audit_store.go` with per-user read methods over the identity-keyed audit tables
    (`mcp_audit`, `skill_audit`, `tool_invocations`) filtering on `actor_identity_id` (using the plan-02 0031
    indexes) with pagination + `created_at DESC`. Create `internal/agui/audit_api.go`: an admin-gated read handler
    projecting to a DTO and `writeJSON` (mirror `handleListSettings`), SanitizeString on any user text. Create
    `cmd/aura/serve_webui_musr.go` and mount the audit route + a grant/revoke route behind
    `RequireCapability(..., governance.write)` (keep serve_webui.go ≤600 by putting the new mounts here). The
    grant/revoke route calls the existing `GrantCapability`/`RevokeCapability` (audited) — same shape as the CLI
    (D-26). Do NOT gate `GET /api/settings/telegram/link` (D-02 self-scoped USER).
  </action>
  <verify>
    <automated>go vet ./internal/agui/ ./cmd/aura/ && go build ./... && go test ./internal/agui/ 2>&1 | tail -10 && wc -l cmd/aura/serve_webui.go</automated>
  </verify>
  <acceptance_criteria>
    - source: audit_api.go is mounted behind RequireCapability(governance.write); audit_store.go reads by actor_identity_id
    - source: telegram/link route is NOT admin-gated; no settings.model.write capability introduced
    - behavior: a non-admin identity gets 403 on the audit + grant/revoke routes
    - test: `go test ./internal/agui/` passes; `wc -l cmd/aura/serve_webui.go` <= 600
  </acceptance_criteria>
  <done>Admin-gated audit read API + grant/revoke control mounted behind governance.write; serve_webui.go under cap; telegram-link stays self-scoped.</done>
</task>

<task type="auto">
  <name>Task 2: SPA capability-gating + admin audit view + grant/revoke control + dist rebuild</name>
  <read_first>
    - web/src/settings/* (the Settings page + panels to hide when the capability is absent — PATTERNS canonical_refs)
    - web/src (the capability/principal the SPA already reads post-auth; the getJSON helper from Phase 32)
    - internal/webui/dist (committed Vite build — must be rebuilt after SPA changes; RESEARCH Runtime State Inventory)
    - CLAUDE.md Frontend_aesthetics (distinctive typography/color/motion — avoid generic AI-slop; the audit view should feel designed)
    - 36-CONTEXT.md D-03/D-26/D-28
  </read_first>
  <action>
    In the SPA: hide the Settings page + admin controls when the capability (`governance.write`) is absent (D-03) —
    read the capability from the post-auth principal payload. Add `web/src/settings/SettingsPage.tsx` grant/revoke
    control (admin-gated) calling the plan Task-1 route. Add `web/src/audit/AdminAuditView.tsx` — an admin-gated
    per-user activity view consuming the audit API, using the shared getJSON helper. Honor CLAUDE.md
    Frontend_aesthetics (distinctive, not generic). Rebuild `internal/webui/dist` from `web/` and commit the bundle
    (this plan is the sole dist owner this phase) so `web-dist-freshness` stays green.
  </action>
  <verify>
    <automated>cd web && npm run test -- --run 2>&1 | tail -15 && npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - source: SettingsPage hides when governance.write absent; AdminAuditView consumes the audit API; dist rebuilt
    - behavior: a non-admin SPA session does not render the Settings page / admin audit view
    - test: `cd web && npm run test -- --run` meets the ≥85% coverage thresholds; `npm run build` succeeds and dist is committed
  </acceptance_criteria>
  <done>SPA gates admin surfaces by capability; admin audit view + grant/revoke control ship; dist rebuilt + committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SPA → admin routes | Admin routes gated server-side by RequireCapability; the SPA hide is UX only, not the security boundary |
| audit read → user text | User-supplied audit text must be sanitized before the wire |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10-E | Elevation of Privilege | non-admin reaches settings/audit | mitigate | RequireCapability(governance.write) server-side; SPA hide is cosmetic (Task 1/2, D-03) |
| T-36-10-E2 | Elevation of Privilege | telegram-link wrongly admin-gated (breaks self-service) or grant route ungated | mitigate | telegram-link stays self-scoped; grant/revoke behind governance.write (Task 1, D-02/D-26) |
| T-36-10-I | Information Disclosure | XSS via unsanitized audit text | mitigate | SanitizeString before the wire (Task 1) |
| T-36-10-SC | Tampering | npm package installs | mitigate | No new npm deps expected; if any, run slopcheck + a blocking human-verify checkpoint before install (RESEARCH: zero installs planned) |
</threat_model>

<verification>
- `go test ./internal/agui/` green; non-admin gets 403 on audit/grant routes.
- `cd web && npm run test -- --run` meets the four ≥85% thresholds; `npm run build` green; dist committed.
- `web-dist-freshness` green (dist matches source).
- `wc -l cmd/aura/serve_webui.go` ≤600.
</verification>

<success_criteria>
- D-28 admin audit UI + D-03/D-26 admin controls ship; settings write-gate reuses governance.write; D-01/D-04 seam untouched.
</success_criteria>

<artifacts_produced>
- Files: `internal/agui/audit_api.go`, `audit_store.go`, `cmd/aura/serve_webui_musr.go`, `web/src/audit/AdminAuditView.tsx`
- Routes: admin-gated audit read + grant/revoke (behind governance.write)
- Capability resolution: D-03 settings gate = existing `governance.write` (no net-new cap)
- Rebuilt `internal/webui/dist`
</artifacts_produced>

<output>
Create `.planning/phases/36-multi-user-identity-isolation-authula-cutover/36-10-SUMMARY.md` when done.
</output>
