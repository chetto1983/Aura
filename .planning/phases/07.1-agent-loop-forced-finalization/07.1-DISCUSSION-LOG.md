# Phase 07.1: Agent-Loop Forced Finalization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-03
**Phase:** 07.1-agent-loop-forced-finalization
**Areas discussed:** Nudge & synthesis wording, `<budget>` block threading + format, finalize()/recovery placement + counter, Deterministic stub digest

---

SPEC.md (6 requirements, ambiguity 0.11) was loaded — requirements are LOCKED.
Discussion was confined to HOW-to-implement decisions only. The user selected all
four offered gray areas and supplied two external references to study.

## Area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Nudge & synthesis wording | Text + language of recovery (Req#3) and finalize (Req#2) nudges | ✓ |
| `<budget>` block: threading + format | How budget reaches `Build`; literal format; placement outside `messages[0]` (Req#6) | ✓ |
| finalize()/recovery placement + counter | New file? counter location (LlmAgent vs Budget vs InvocationContext) | ✓ |
| Deterministic stub digest | Worst-case Italian fallback wording + digest content (Req#5) | ✓ |

**User's choice:** All four, plus two references to read:
`AlessandroAnnini/agent-loop/loop_control.py` and the AWS dev.to article
"How to Prevent AI Agent Reasoning Loops from Wasting Tokens".

---

## `<budget>` block — message role

| Option | Description | Selected |
|--------|-------------|----------|
| Tail user message | Block as the last user-role message; safest cross-provider (system only at pos 0) | ✓ |
| Tail system message | Block as trailing system message; mild risk of non-leading-system inconsistency | |

**User's choice:** Tail user message.
**Notes:** Confirmed the cache-prefix-stability rationale — `messages[0]` must stay byte-identical; a trailing user message never risks prefix re-ordering.

---

## Areas ①③④ — bundled confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| Confirm all as proposed | Lock nudge wording (English, no IT directive — `SystemPrompt` already governs), finalize in new `llm_agent_finalize.go`, `LlmAgent.recoveryAttempts` counter, Italian stub + per-tool digest | ✓ |
| Adjust some | Walk through changes before writing CONTEXT.md | |

**User's choice:** Confirm all as proposed.
**Notes:** Verified during discussion that `SystemPrompt` (`internal/agent/prompt.go`) already ends with `Always respond in Italian.`, so the English-authored nudges need no per-nudge language directive. Counter placed on `LlmAgent` (not the pointer-shared `Budget`) to avoid leaking recovery state across swarm branches.

---

## Reference findings (folded into CONTEXT.md canonical_refs)

- **`loop_control.py`** — dedup signature (name + hashed args) CONFIRMS Aura's `canonicalArgs` + dedup ring; its NLP phrase-matching completion detection is the regex-on-prose anti-pattern Aura forbids (`feedback_no_regex_for_nlp`) — REJECTED. Its interactive y/N soft-stop also rejected (Aura's contract is always-return-an-answer).
- **AWS dev.to article** — its "debounce hook" (window=3, fingerprint, reset-per-invocation) CONFIRMS Aura's dedup ring 1:1; the "847 steps, never delivers" failure motivates the always-return-an-answer + budget design. Adjacent SUCCESS/FAILED tool-terminal-state idea is OUT of 07.1 scope (tools untouched) → Deferred.

## Claude's Discretion

- Exact stub digest byte caps (~500B/bullet + overall) and final nudge phrasing — tunable within locked intent.
- Whether the `Build` budget parameter is two ints / a struct / a pointer — implementation detail, provided `messages[0]` stays byte-stable.

## Deferred Ideas

- Anthropic-native `tool_choice` translation (Slice-13 LLMRouter).
- AG-UI / Phase-12 surfacing of new `finalEvent` termination-reason fields.
- Swarm finalization semantics (Phase 9).
- Interactive y/N completion soft-stop (rejected; `ask_user` is the HITL primitive).
- Explicit SUCCESS/FAILED tool terminal states — future tool-design / Skills (Phase 11) pass.
