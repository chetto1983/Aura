---
phase: 07.1
slug: agent-loop-forced-finalization
status: passed
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-03
last_audited: 2026-06-04
auditor: Codex gsd-validate-phase
input_state: A
---

# Phase 07.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `07.1-RESEARCH.md` section 4 Validation Architecture and the completed
> plan summaries for `07.1-01` through `07.1-05`.
> Nyquist principle: sample the artifact, never the reply prose alone.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| Framework | Go standard `testing` plus package `goleak` guards |
| Config file | `go.mod`; Go-native tests; coverage gate `scripts/coverage_gate.sh` |
| Quick run command | `go test ./internal/agent/... ./internal/llm/... -count=1` |
| Race command | `go test -race ./internal/agent/... ./internal/llm/... -count=1` |
| Tagged live compile command | `go test -tags live_finalize -run 'TestLive' ./internal/agent/ -count=1 -v` |
| Live paid command | `go test -tags live_finalize -run 'TestLiveToolChoiceNone|TestLiveMeteoCaraglio' -timeout 900s -v ./internal/agent/` with `OPENROUTER_API_KEY` and `SEARXNG_URL` set |
| Coverage and mutation | `scripts/coverage_gate.sh`; WSL `go-mutesting ./internal/agent/llm_agent_finalize.go` |

---

## Sampling Rate

- After every task commit: run the requirement-specific command in the verification map.
- After every plan wave: run `go test ./internal/agent/... ./internal/llm/... -count=1`.
- Before phase sign-off: run the race sweep, live paid gates, coverage gate, and mutation spot-check.
- Max feedback latency for the deterministic lane: under 15 seconds for the owned `agent` and `llm` surface on this audit host.

---

## Per-Task Verification Map

| Req | Plan/Wave | Signal (observable proof) | Test Files | Automated Command | Status |
|-----|-----------|---------------------------|------------|-------------------|--------|
| #1 ToolChoice | 07.1-01 / W1 | Wire body carries `tool_choice`; empty `ToolChoice` maps to `"auto"` byte-identically; `"none"` omits the `tools` key | `internal/llm/openai_compat/client_test.go` (`TestRequestBody`, `TestRequestBody_ToolChoice`) | `go test -run TestRequestBody ./internal/llm/openai_compat/ -count=1` | COVERED |
| #6 `<budget>` | 07.1-02 / W1 | `messages[0]` remains byte-stable; trailing `user` budget block tracks live used/remaining counts; caller history is not mutated | `internal/agent/prompt/budget_block_test.go`; `internal/agent/llm_agent_test.go` | `go test -run TestBudgetBlock ./internal/agent/prompt/ -count=1` | COVERED |
| #2 finalize | 07.1-03 / W2 | Dedup, max_steps, and wallclock termination paths end with non-empty terminal content; finalize sends `ToolChoice="none"` and preserves trip reason in `StateDelta` | `internal/agent/llm_agent_finalize_test.go`; `internal/agent/llm_agent_finalize_internal_test.go` | `go test -run 'TestFinalize|TestSynthesize' ./internal/agent/ -count=1` | COVERED |
| #3 recovery | 07.1-04 / W3 | Two trips inject exactly one `user` recovery nudge; recovery gate is an integer counter, not a boolean latch | `internal/agent/llm_agent_finalize_test.go`; `internal/agent/llm_agent_finalize_internal_test.go` | `go test -run 'TestRecovery|TestMaybeRecover' ./internal/agent/ -count=1` | COVERED |
| #4 outside-budget | 07.1-04 / W3 | Recovery and finalize do not decrement the step budget; normal ceiling stays `max_steps+2`; stalled finalize stream reaches stub through per-call timeout | `internal/agent/llm_agent_finalize_test.go` | `go test -run 'TestFinalizeOutsideBudget|TestFinalizeHangingStreamReachesStub' ./internal/agent/ -count=1` | COVERED |
| #5 retry+stub | 07.1-04 / W3 | Empty/error synthesis retries once; retry success returns retry prose; double failure emits deterministic Italian stub with `RoleTool` digest | `internal/agent/llm_agent_finalize_test.go`; `internal/agent/llm_agent_finalize_internal_test.go` | `go test -run 'TestFinalizeFallback|TestStubDigest|TestTruncateBytes' ./internal/agent/ -count=1` | COVERED |
| AC4 live `tool_choice=none` | 07.1-05 / W4 | Real DeepSeek-V4/OpenRouter `ToolChoice="none"` finalize-shaped request returns prose in `content`, with no tool-call-shaped JSON leak | `internal/agent/live_finalize_test.go` (`TestLiveToolChoiceNone`) | Local compile/skip: `go test -tags live_finalize -run 'TestLive' ./internal/agent/ -count=1 -v`; paid evidence in `07.1-VERIFICATION.md` | COVERED (env-gated) |
| AC9 live meteo-Caraglio | 07.1-05 / W4 | Exact prompt `dammi le previsioni meteo a Caraglio` yields non-empty terminal weather prose across 6 live runs; observed failure rate is 0 | `internal/agent/live_finalize_test.go` (`TestLiveMeteoCaraglio`) | Local compile/skip: `go test -tags live_finalize -run 'TestLive' ./internal/agent/ -count=1 -v`; paid evidence in `07.1-VERIFICATION.md` | COVERED (env-gated) |

---

## Generated Test Files

These test files were generated during phase execution and re-audited here:

| File | Covers |
|------|--------|
| `internal/agent/prompt/budget_block_test.go` | Req #6 budget block byte-stability and formatting |
| `internal/agent/llm_agent_finalize_test.go` | Req #2/#3/#4/#5 finalize, recovery, outside-budget, fallback, stalled-stream timeout |
| `internal/agent/llm_agent_finalize_internal_test.go` | White-box finalize invariants: `ToolChoice`, `SessionID`, deadline, nudge, stub helpers |
| `internal/agent/live_finalize_test.go` | AC4 and AC9 live paid gates behind the `live_finalize` build tag |
| `internal/llm/openai_compat/client_test.go` | Req #1 wire serialization for `ToolChoice` |

No new test file was required by the 2026-06-04 audit; the missing Wave 0 tests already exist and passed the deterministic commands.

---

## Manual And Environment-Gated Verifications

| Behavior | Requirement | Why Gated | Evidence |
|----------|-------------|-----------|----------|
| Real `ToolChoice="none"` returns prose in `content` | AC4 / Req #2 | Requires paid `OPENROUTER_API_KEY` | `07.1-VERIFICATION.md` records PASS: 394 bytes of prose and no tool-call-shaped leak |
| Meteo-Caraglio live E2E | AC9 / Req #5 | Requires paid endpoint plus live SearXNG backend | `07.1-VERIFICATION.md` records PASS: 6/6 non-empty answers, observed failure rate 0 |
| Race plus goleak | SC6 | Best run on WSL/Linux for parity | `07.1-VERIFICATION.md` records PASS; current audit reran the Windows race sweep successfully |
| Mutation spot-check | SC6 | Requires WSL `go-mutesting` setup | `07.1-VERIFICATION.md` records 82.9% killed (29/35), above the 70% gate |
| Owned-surface coverage | SC6 | Full gate may require integration services | `07.1-VERIFICATION.md` records 96.6% owned-surface aggregate, above the 85% gate |

---

## Gap Analysis

| Metric | Count |
|--------|-------|
| Requirement rows audited | 8 |
| COVERED | 8 |
| PARTIAL | 0 |
| MISSING | 0 |
| Escalated to manual-only | 0 |

Nyquist result: compliant. Every phase requirement has a concrete automated or environment-gated proof, and all deterministic proofs passed in this audit.

Non-Nyquist residual: `07.1-REVIEW.md` still notes WR-02, where the stub-fallback path drops retry-attempt usage from terminal cost accounting. This is an observability warning, not a missing validation test for the phase's always-non-empty answer contract.

---

## Validation Audit 2026-06-04

| Check | Command | Result |
|-------|---------|--------|
| ToolChoice wire tests | `go test -run TestRequestBody ./internal/llm/openai_compat/ -count=1` | PASS |
| Budget block tests | `go test -run TestBudgetBlock ./internal/agent/prompt/ -count=1` | PASS |
| Finalize/recovery/fallback tests | `go test -run 'TestFinalize|TestRecovery|TestSynthesize|TestMaybeRecover|TestStubDigest|TestTruncateBytes' ./internal/agent/ -count=1` | PASS |
| Vet owned surface | `go vet ./internal/agent/... ./internal/llm/...` | PASS |
| Live-tag compile/skip | `OPENROUTER_API_KEY='' SEARXNG_URL='' go test -tags live_finalize -run 'TestLive' ./internal/agent/ -count=1 -v` | PASS with 2 expected local skips |
| Unit owned surface | `go test ./internal/agent/... ./internal/llm/... -count=1` | PASS |
| Race owned surface | `go test -race ./internal/agent/... ./internal/llm/... -count=1` | PASS |
| Build whole tree | `go build ./...` | PASS |

No implementation files were modified by this audit. The only artifact update is this validation document.

---

## Validation Sign-Off

- [x] Nyquist config checked; validation is enabled.
- [x] Input state detected as State A: existing `07.1-VALIDATION.md` audited and updated.
- [x] PLAN and SUMMARY files read; requirement map reconstructed.
- [x] Test infrastructure detected from Go test files, `go.mod`, Makefile gates, and live build tag.
- [x] Requirement-to-test cross-reference completed.
- [x] No missing or partial requirement coverage found.
- [x] Deterministic unit, vet, build, and race commands passed in this audit.
- [x] Live, mutation, and coverage gates are documented with recorded evidence in `07.1-VERIFICATION.md`.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** passed
