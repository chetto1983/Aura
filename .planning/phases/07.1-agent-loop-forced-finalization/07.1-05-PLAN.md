---
phase: 07.1-agent-loop-forced-finalization
plan: 05
type: execute
wave: 4
depends_on: ["07.1-04"]
files_modified:
  - internal/agent/live_finalize_test.go
autonomous: true
requirements: [INFRA-03]
user_setup:
  - service: openrouter
    why: "Live smoke + E2E run against the real DeepSeek-V4 endpoint (paid, operator-authorized)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard → Keys"
must_haves:
  truths:
    - "A live finalize call with ToolChoice=none returns prose in content from real DeepSeek-V4/OpenRouter (no phantom tool-call-in-text)"
    - "The meteo-Caraglio prompt yields a non-empty weather answer across N consecutive live runs (observed failure rate = 0)"
    - "The live tests t.Skip locally without OPENROUTER_API_KEY and t.Fatal under $CI when the key is unset (no-skip-as-green)"
  artifacts:
    - path: "internal/agent/live_finalize_test.go"
      provides: "live tool_choice=none smoke (AC4) + meteo-Caraglio E2E (AC9), build-tag + key gated"
      contains: "OPENROUTER_API_KEY"
  key_links:
    - from: "internal/agent/live_finalize_test.go"
      to: "internal/llm/openai_compat real client"
      via: "real endpoint call with ToolChoice=none"
      pattern: "ToolChoice"
---

<objective>
Add the two LIVE, env-gated, operator-authorized tests that close ACs 4 and 9 against the real DeepSeek-V4/OpenRouter endpoint: (AC4) a smoke test proving a `ToolChoice="none"` finalize call returns prose in `content` (no phantom tool-call-in-text); (AC9) the meteo-Caraglio E2E proving "dammi le previsioni meteo a Caraglio" yields a non-empty weather answer across N consecutive runs (observed Phase-7 failure rate → 0).

Purpose: The deterministic lane (plans 01-04) proves the mechanism with FakeClient; these prove it holds against the real provider's non-deterministic behavior — the actual repro the phase exists to fix. They follow the project's live-eval gating convention (build tag + `OPENROUTER_API_KEY`, `t.Skip` locally / `t.Fatal` under `$CI`). Implements SPEC ACs 4 + 9, INFRA-03.
Output: One build-tagged live test file with the smoke + E2E cases. PAID/manual gate — never in default CI.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-SPEC.md
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-RESEARCH.md
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-VALIDATION.md
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- New build-tagged test file `internal/agent/live_finalize_test.go` under a NEW `live_finalize` build tag (RESEARCH A1: `internal/agent` has no live tag today; a new tag is cleanest and co-locates the finalize live tests with the code).
- New live test functions `TestLiveToolChoiceNone` (AC4 smoke) and `TestLiveMeteoCaraglio` (AC9 E2E) — names match VALIDATION.md Manual-Only Verifications.
No production code is created in this plan (the loop is complete after plan 04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live tool_choice=none smoke + meteo-Caraglio E2E (build-tag + key gated)</name>
  <behavior>
    - Given `OPENROUTER_API_KEY` is unset locally, When `go test -tags live_finalize -run 'TestLive' ./internal/agent/` runs, Then both tests `t.Skip` cleanly (the one legitimate local skip) and the default lane (no tag) excludes the file entirely.
    - Given `OPENROUTER_API_KEY` is unset AND `$CI` is set, When the tagged tests run, Then they `t.Fatal` (no-skip-as-green) rather than skipping.
    - Given `OPENROUTER_API_KEY` is set (operator, paid), When `TestLiveToolChoiceNone` runs, Then a `ToolChoice="none"` request returns non-empty `content` with no phantom tool-call-in-text.
    - Given `OPENROUTER_API_KEY` is set, When `TestLiveMeteoCaraglio` runs the meteo-Caraglio prompt N consecutive times, Then every run's terminal Event has non-empty `LLMResponse.Content` and the observed failure rate == 0.
  </behavior>
  <files>internal/agent/live_finalize_test.go</files>
  <read_first>
    - internal/eval/harness_cot_eval_test.go:1-19 (the EXACT header/gating pattern to copy: `//go:build <tag>` line, the doc-comment "MANUAL, OPERATOR-AUTHORIZED, PAID GATE" framing, the `OPENROUTER_API_KEY` gate with `t.Skip` locally; it drives the real `agent.LlmAgent` over the real `openai_compat` client against DeepSeek-V4 — mirror its client construction)
    - internal/runner/live_e2e_test.go:1-32 (the no-skip-as-green discipline: `t.Skip` locally when the key is unset, `t.Fatal` under `$CI`; the `-timeout 600s -v` reproduce recipe; the real-client + real-tools wiring for an E2E loop)
    - internal/agent/llm_agent.go:117-192 (the `Run` loop the E2E drives end to end) + internal/agent/llm_agent_finalize.go (the finalize path under test)
    - internal/llm/openai_compat/client.go:80-148 (the real `Stream` + `buildWireRequest` — for the AC4 smoke, construct a real client and a request with `ToolChoice="none"`, assert prose in `content`)
    - 07.1-SPEC.md AC4 (tool_choice=none returns prose live, parsed from content) + AC9 (meteo-Caraglio failure rate = 0 over N runs) + Constraint "Live smoke + E2E require OPENROUTER_API_KEY"
    - 07.1-RESEARCH.md §3 AC4(b)/AC9 + "Live-test convention (verified)" + A1 (new tag for internal/agent is cleanest) + Environment Availability (SearXNG + web tools are Phase-7 runtime; E2E is manual/paid)
    - 07.1-VALIDATION.md Manual-Only Verifications (the two test instructions + names `TestLiveToolChoiceNone` / `TestLiveMeteoCaraglio`)
  </read_first>
  <action>
    Create `internal/agent/live_finalize_test.go` with a NEW `//go:build live_finalize` tag on the first line (RESEARCH A1 — `internal/agent` has no live tag today). Copy the header doc-comment framing from `harness_cot_eval_test.go` (MANUAL, OPERATOR-AUTHORIZED, PAID; NOT a CI job) and a `REPRODUCE` recipe block. Gate BOTH tests on `OPENROUTER_API_KEY`: with the key unset, `t.Skip` locally BUT `t.Fatal` when `os.Getenv("CI") != ""` (no-skip-as-green — copy the exact gate helper shape from `live_e2e_test.go`). Write `TestLiveToolChoiceNone` (AC4): construct the real `openai_compat` client (mirror `harness_cot_eval_test.go`'s construction from env config) and issue ONE request with `ToolChoice="none"` and a simple synthesis prompt over a short fake tool-result history; drain the stream and assert the accumulated `content` is non-empty AND contains no phantom tool-call-in-text (assert the response text does not parse as a tool_calls JSON object — a structured check, NOT NLP phrase-matching per the project's no-regex-on-prose rule). Write `TestLiveMeteoCaraglio` (AC9): build a real `LlmAgent` with the real web tools registry (Phase-7 `web_search`/`web_fetch`, runtime SearXNG) and run the prompt "dammi le previsioni meteo a Caraglio" N consecutive times (N a small const, e.g. 6 — the observed 1-in-6 repro denominator); assert EVERY run's terminal Event has non-empty `LLMResponse.Content` (the always-return-an-answer contract); count and assert observed failure rate == 0. Reports go to `docs/`, never `/tmp` (CLAUDE.md). Ensure goleak cleanliness (the package runs under `goleak.VerifyTestMain`; shut down any TracerProvider via `t.Cleanup`). Verify it COMPILES under the tag without the key: `go test -tags live_finalize -run 'TestLive' ./internal/agent/` (skips locally, compiles clean).
  </action>
  <verify>
    <automated>go vet -tags live_finalize ./internal/agent/ && go test -tags live_finalize -run 'TestLive' ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the file's first line is `//go:build live_finalize` and both tests gate on `OPENROUTER_API_KEY` with `t.Fatal` under `$CI` when unset (`grep -n 'OPENROUTER_API_KEY\|"CI"' internal/agent/live_finalize_test.go`).
    - `go vet -tags live_finalize ./internal/agent/` succeeds (the live file compiles under the tag).
    - `go test -tags live_finalize -run 'TestLive' ./internal/agent/` skips cleanly when `OPENROUTER_API_KEY` is unset (the one legitimate local skip) and does NOT run in the default lane (no tag → file excluded).
    - LIVE (manual, paid, operator-run with the key set): `TestLiveToolChoiceNone` asserts non-empty `content` with no phantom tool-call-in-text; `TestLiveMeteoCaraglio` asserts non-empty weather prose across N runs with observed failure rate == 0 (VALIDATION.md Manual-Only Verifications).
  </acceptance_criteria>
  <done>Live `tool_choice=none` smoke + meteo-Caraglio E2E exist under the `live_finalize` tag, key-gated with no-skip-as-green; compile clean and skip locally without the key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real OpenRouter endpoint | The live tests cross to a real paid external endpoint with `OPENROUTER_API_KEY`. This is the only external surface in the phase, and it is test-only, operator-authorized, and excluded from default CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-09 | Information Disclosure | `OPENROUTER_API_KEY` in tests | mitigate | The key is read from env only (never hardcoded); the real client sets `Authorization` in exactly one place (`openai_compat/client.go:93`, D-28). Reports go to `docs/`, never logged with the key. |
| T-07.1-10 | Denial of Service | paid live calls in CI | mitigate | The `live_finalize` build tag excludes both tests from the default CI/Makefile lane; they run only when the operator passes the tag AND the key — no accidental paid runs. |
| T-07.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Go stdlib + goleak already vendored (RESEARCH Environment Availability). |

No high-severity threats: a test-only, build-tag-isolated, env-gated live lane with the key read from env and excluded from default CI.
</threat_model>

<verification>
- `go vet -tags live_finalize ./internal/agent/` clean (live file compiles under the tag).
- `go test -tags live_finalize -run 'TestLive' ./internal/agent/` skips cleanly without the key; not in the default lane.
- Manual/paid (operator, key set): both live tests assert their contracts; meteo-Caraglio failure rate == 0 over N runs.
</verification>

<success_criteria>
- `internal/agent/live_finalize_test.go` under the `live_finalize` tag, `OPENROUTER_API_KEY`-gated with `t.Fatal` under `$CI` (no-skip-as-green).
- `TestLiveToolChoiceNone` proves `tool_choice=none` returns prose in `content` live (AC4).
- `TestLiveMeteoCaraglio` proves the meteo-Caraglio prompt yields a non-empty weather answer across N runs, failure rate == 0 (AC9).
- File compiles under the tag, skips locally without the key, excluded from default CI.
</success_criteria>

<output>
Create `.planning/phases/07.1-agent-loop-forced-finalization/07.1-05-SUMMARY.md` when done.
</output>
