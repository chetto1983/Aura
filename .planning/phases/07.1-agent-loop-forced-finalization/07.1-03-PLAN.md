---
phase: 07.1-agent-loop-forced-finalization
plan: 03
type: execute
wave: 2
depends_on: ["07.1-01", "07.1-02"]
files_modified:
  - internal/agent/llm_agent_finalize.go
  - internal/agent/llm_agent.go
  - internal/agent/llm_agent_finalize_test.go
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "A run that trips the dedup veto ends with a non-empty finalEvent (not a bare terminalBudgetEvent)"
    - "A run that exhausts max_steps ends with a non-empty finalEvent synthesized from gathered tool results"
    - "A run that trips the wallclock budget ends with a non-empty finalEvent"
    - "The finalize request carries ToolChoice=none and the answer is parsed from content, not tool_calls"
    - "The finalize event preserves observability: it carries termination_reason/limit_hit in StateDelta"
  artifacts:
    - path: "internal/agent/llm_agent_finalize.go"
      provides: "finalize() tool-free synthesis method + finalize-event augmentation"
      contains: "func (a *LlmAgent) finalize"
    - path: "internal/agent/llm_agent.go"
      provides: "both trip sites re-routed to finalize() instead of returning empty"
      contains: "finalize"
    - path: "internal/agent/llm_agent_finalize_test.go"
      provides: "dedup/max_steps/wallclock trip → non-empty finalEvent tests"
  key_links:
    - from: "internal/agent/llm_agent.go:124"
      to: "finalize()"
      via: "budget trip routes to finalize instead of terminalBudgetEvent+return"
      pattern: "finalize"
    - from: "internal/agent/llm_agent.go:224"
      to: "finalize()"
      via: "dedup trip routes to finalize instead of terminalBudgetEvent+return"
      pattern: "finalize"
    - from: "internal/agent/llm_agent_finalize.go"
      to: "llm.Request.ToolChoice"
      via: "finalize sets ToolChoice=none on the synthesis request"
      pattern: "ToolChoice"
---

<objective>
Add the forced-finalization spine (Req#2): a `finalize()` method in a NEW file `internal/agent/llm_agent_finalize.go` (D-07 — `llm_agent.go` is 329 LOC, no-god-class) that builds a tool-free request (full in-memory history + the D-03 synthesis nudge, `ToolChoice="none"`), drains the stream, parses the answer from `content` (NOT `tool_calls`), and emits a non-empty `finalEvent` augmented with `termination_reason`/`limit_hit` for observability. Re-route BOTH early-termination sites in `llm_agent.go` (budget trip `:124-127`, dedup trip `:224-227`) through `finalize()` instead of returning a prose-less `terminalBudgetEvent`.

Purpose: Today both trip paths emit a signal-only Event and die silent (the ~1-in-6 empty meteo answer). This is the SPINE (RESEARCH §2) for the recovery/fallback work in plan 04. Implements SPEC Req#2 + ACs 1/2/3, INFRA-03 (finalize is an additive grant on the existing `Budget` contract).
Output: The new finalize file, the two re-routed call sites, and the three trip→non-empty-finalEvent tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-SPEC.md
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-PATTERNS.md
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-CONTEXT.md
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-RESEARCH.md
@.claude/skills/golang-concurrency/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
@.planning/phases/07.1-agent-loop-forced-finalization/07.1-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- New file `internal/agent/llm_agent_finalize.go`.
- New method `func (a *LlmAgent) finalize(...)` — builds the tool-free `ToolChoice="none"` request, drains the stream to a `content` string, emits a non-empty `finalEvent` augmented with `termination_reason`/`limit_hit`.
- New nudge constant for the D-03 finalize synthesis nudge (English, no IT directive — `SystemPrompt` governs language).
- A helper that augments `finalEvent`'s `Actions.StateDelta` with the `termination_reason`/`limit_hit` keys (landmine #10 — `finalEvent` does not carry these today).
- The re-routing edits at the two trip sites in `llm_agent.go` (`:124-127`, `:224-227`).
- New test file `internal/agent/llm_agent_finalize_test.go` (the dedup / max_steps / wallclock trip tests).
NOTE: the recovery counter (`recoveryAttempts`), the retry-once + Italian stub fallback, and the budget-bypass invariant are created in plan 04 (which depends on this plan). This plan wires the trip sites STRAIGHT to `finalize()` (no recovery turn yet); plan 04 inserts the counter-gated recovery turn before the finalize call.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement finalize() in the new llm_agent_finalize.go</name>
  <files>internal/agent/llm_agent_finalize.go</files>
  <read_first>
    - internal/agent/llm_agent.go:117-192 (the `Run` loop; the `Build` call at :137; the content-stop `finalEvent` shape at :160-169 — the EXACT pattern finalize reproduces: build a finalEvent with non-empty Content, append the answer to history, yield)
    - internal/agent/llm_agent.go:266-298 (`consume` — the stream-drain + after-false drain-to-close contract; landmine #12, L2)
    - internal/agent/llm_agent_events.go:62-70 (`finalEvent` constructor signature) and :88-101 (`terminalBudgetEvent` — copy its `StateDelta` map shape for the termination_reason/limit_hit keys; landmine #10)
    - internal/llm/client.go (the `ToolChoice` field added in plan 01; set `req.ToolChoice = "none"`)
    - 07.1-CONTEXT.md D-01/D-03/D-07 (English nudge, no IT directive; finalize nudge wording; new-file placement)
    - 07.1-PATTERNS.md §`internal/agent/llm_agent_finalize.go` (Patterns A/B/C — finalEvent emit, termination_reason preservation, stream-drain after-false; the "drain fully into strings.Builder WITHOUT yielding intermediate chunks then emit one terminal finalEvent" simplest-safe approach)
    - 07.1-SPEC.md Req#2 + constraint "parse content NOT tool_calls" (landmine #4 — DeepSeek-V4 emits tool-call syntax as text; with tool_choice=none the answer is in content)
    - .claude/skills/golang-concurrency/SKILL.md (iter.Seq2 / channel drain discipline) and golang-error-handling (%w wrapping on the transport error path)
  </read_first>
  <behavior>
    - `finalize()` builds a request from `a.history` + a trailing `user`-role synthesis nudge, with `ToolChoice="none"`, and calls `a.client.Stream` directly (NEVER `ConsumeStep` — Req#4 invariant; the counting is verified in plan 04).
    - It drains the stream fully into a content string (accumulating `c.Text`, capturing `c.Usage`) and parses the answer from `content`, never `tool_calls`.
    - It emits exactly ONE `finalEvent` whose `LLMResponse.Content` is the non-empty synthesized answer, with `Actions.StateDelta` carrying both the usage delta AND `termination_reason`/`limit_hit` (the trip reason passed in).
    - The synthesized answer is appended to `a.history` (mirrors the content-stop path).
    - The terminal user-facing Event is always a `finalEvent` — never the `iter.Seq2` error slot.
  </behavior>
  <action>
    Create `internal/agent/llm_agent_finalize.go` (package `agent`). Define a `finalize` method on `*LlmAgent` taking the trip reason plus the trace identity / `InvocationContext` / `yield` it needs to emit (mirror the parameter shape of the existing emit helpers in `llm_agent_events.go` / the dispatch signature in `llm_agent.go`). Build the synthesis request: `req := a.builder.Build(a.history, a.registry, a.cfg.Provider, a.cfg)` then `req.ToolChoice = "none"` and `req.SessionID = a.sessionID` (mirror `:137-138`); append the D-03 finalize nudge as a trailing `user` message to a COPY of history passed into `Build` (NOT `a.history` — the nudge for the synthesis turn rides the request; do not permanently mutate history with the synthesis instruction). Define the nudge as a load-bearing constant (English, NO Italian directive — `SystemPrompt` governs language, D-01): `"Stop calling tools. Using only the tool results already gathered above, write the final answer to the user's original question now."` Call `a.client.Stream(ctx, req)` DIRECTLY (never `ConsumeStep`). Drain the channel fully into a `strings.Builder` WITHOUT yielding intermediate chunk Events (Pattern C simplest-safe — sidesteps the after-false hazard; capture `c.Usage` from the trailing usage chunk), then parse the answer from the accumulated `content`. Append the answer to `a.history` (`RoleAssistant`) and emit ONE `finalEvent(...)` (reuse the `:62` constructor) whose `Actions.StateDelta` is AUGMENTED with `"termination_reason"` and `"limit_hit": reason` keys (copy the map shape from `terminalBudgetEvent` :91-101 and merge onto the finalEvent StateDelta — landmine #10) so observability is preserved. On a real Stream TRANSPORT error, wrap with `%w` per golang-error-handling; the retry/stub fallback that consumes this error is added in plan 04, so for THIS plan return/expose the error in a way plan 04 can branch on (e.g. return `(answer string, usage llm.Usage, err error)` from an inner synthesis helper and have `finalize` emit the finalEvent) — keep the public `finalize` always emitting a non-empty finalEvent for the happy path. Respect the after-false contract if any yield is added (landmine #12, L2). Run `go build ./internal/agent/...`.
  </action>
  <verify>
    <automated>go build ./internal/agent/... && go vet ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "func (a \*LlmAgent) finalize" internal/agent/llm_agent_finalize.go` returns the method.
    - Source assertion: the finalize synthesis request sets `ToolChoice = "none"` (`grep -n 'ToolChoice = "none"' internal/agent/llm_agent_finalize.go`).
    - Source assertion: finalize calls `a.client.Stream` directly and does NOT call `ConsumeStep` (`grep -c "ConsumeStep" internal/agent/llm_agent_finalize.go` returns 0).
    - Source assertion: the emitted event's StateDelta includes `termination_reason` and `limit_hit` keys (grep the constant key strings).
    - `go build ./internal/agent/...` + `go vet ./internal/agent/...` succeed; file ≤600 LOC.
  </acceptance_criteria>
  <done>`finalize()` exists, sends `ToolChoice="none"`, parses content, emits a non-empty finalEvent with termination_reason; never calls ConsumeStep; build+vet green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-route both trip sites in llm_agent.go to finalize()</name>
  <files>internal/agent/llm_agent.go</files>
  <read_first>
    - internal/agent/llm_agent.go:122-127 (the budget trip site — `if ok, reason := ic.Budget.ConsumeStep(); !ok { yield(terminalBudgetEvent...); return }`)
    - internal/agent/llm_agent.go:222-227 (the dedup trip site in `dispatch` — `if dedup, reason := ic.Budget.BeforeToolCall(...); dedup { yield(terminalBudgetEvent...); return true, nil }`; `call.Function.Name` is in scope at :204 for plan 04's nudge, not needed here)
    - internal/agent/llm_agent_finalize.go (the `finalize` method from Task 1 — its exact parameter list)
    - 07.1-PATTERNS.md §`internal/agent/llm_agent.go` (re-route both trip sites; Event-only never the error slot, L2/D-04; the recovery budget-bypass is plan 04, NOT here)
    - 07.1-SPEC.md Req#2 ("called at the budget trip and the dedup trip instead of returning empty")
  </read_first>
  <behavior>
    - The budget trip (`:124`) calls `finalize(reason=...)` and returns, instead of yielding `terminalBudgetEvent` + bare return — covers both `max_steps` and `wallclock` (same ConsumeStep call, different reason string).
    - The dedup trip (`:224`) calls `finalize(reason="dedup")` and returns `true`, instead of yielding `terminalBudgetEvent` + return.
    - No run ends on a bare `terminalBudgetEvent` as its last Event.
  </behavior>
  <action>
    In `internal/agent/llm_agent.go`, replace the budget-trip body at `:124-127`: instead of `yield(a.terminalBudgetEvent(...)); return`, call `a.finalize(ic, spanID, parentSpanID, requestID, reason, yield)` (match the actual Task-1 signature) and `return`. Replace the dedup-trip body at `:224-227` similarly, calling `finalize` with `reason` (the `"dedup"` value already in scope) and `return true, nil`. Keep these Event-only (never `yield(nil, err)` — L2/D-04). Do NOT add the recovery turn or budget-bypass here (that is plan 04). The `terminalBudgetEvent` helper stays in the file (its StateDelta map shape is reused by finalize) but is no longer the terminal Event at these two sites. Run `go build ./internal/agent/...`.
  </action>
  <verify>
    <automated>go build ./internal/agent/... && go vet ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: both former `yield(a.terminalBudgetEvent(...))` trip sites now route through `finalize` (`grep -n "a.finalize(" internal/agent/llm_agent.go` returns the two trip sites).
    - Source assertion: neither trip site does a bare `yield(a.terminalBudgetEvent(...)); return` anymore.
    - `go build ./internal/agent/...` + `go vet ./internal/agent/...` succeed; `llm_agent.go` ≤600 LOC.
  </acceptance_criteria>
  <done>Budget and dedup trips route through `finalize()`; no bare terminalBudgetEvent terminal at either site; build+vet green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Trip → non-empty finalEvent tests (dedup, max_steps, wallclock)</name>
  <files>internal/agent/llm_agent_finalize_test.go</files>
  <read_first>
    - internal/agent/llm_agent_test.go (existing helpers: `newAgent`, `newIC`, `collect`, `textResponseCall`, `recordingProvider` — REUSE them; do not build a new harness)
    - internal/agent/agenttest/fakeclient.go (`NewFakeClient`, `FakeTurn`, `TextChunks(finish, parts...)`, `ToolCallTurn`, `MakeToolCall`, `WithUsage`, `CallCount`, `Requests`, `LastRequest`)
    - internal/agent/budget.go:50-93 (`BudgetOptions`/`Now` injectable clock seam for the wallclock case; `MaxSteps` setter) — mirror `budget_test.go` clock injection
    - 07.1-RESEARCH.md §3 AC1/AC2/AC3 (exact scripting: dedup via repeated identical `echo` args ×3 then a finalize `TextChunks`; max_steps via `MaxSteps:1`; wallclock via injected clock past the deadline)
    - 07.1-VALIDATION.md Per-Task Verification Map Req#2 + #3/#5-wallclock rows (signal: last Event has non-empty `LLMResponse.Content` at all 3 trip reasons; commands `TestFinalize` / `TestFinalizeWallclock`)
    - .claude/skills/golang-testing/SKILL.md (table-driven, goleak via main_test.go, -race)
  </read_first>
  <behavior>
    - A scripted run that trips the dedup ring ends with a `finalEvent` whose `LLMResponse.Content` is the non-empty synthesized answer (NOT an Escalate-only `terminalBudgetEvent`).
    - A run with `MaxSteps=1` that exhausts the step budget ends with a non-empty `finalEvent`.
    - A run with an injected clock past the wallclock deadline ends with a non-empty `finalEvent`.
    - In each case the last Event's StateDelta carries the matching `termination_reason`/`limit_hit`.
  </behavior>
  <action>
    Create `internal/agent/llm_agent_finalize_test.go` (package `agent_test` black-box, reusing `llm_agent_test.go` helpers). Name tests `TestFinalize_DedupTrip`, `TestFinalize_MaxStepsTrip`, and `TestFinalizeWallclock` (the wallclock name MUST match VALIDATION.md's `go test -run 'TestFinalizeWallclock'`). DEDUP: script `ToolCallTurn` repeating the same `MakeToolCall("id","echo","{...}")` args enough times to trip the window-3 dedup ring (RESEARCH §3 — echo returns a stable result so the progress veto does not suppress dedup), then a finalize `TextChunks("stop","...synthesis answer...")` turn; assert `collect()`'s last Event has non-empty `Content` and is not Escalate-only. MAX_STEPS: `newIC` with `MaxSteps=1`, one `ToolCallTurn(echo)` then a finalize `TextChunks`; assert non-empty finalEvent. WALLCLOCK: `newIC` with an injected clock past the deadline (mirror `budget_test.go`'s `Now`/`MaxWallclockSec=0` seam) so the first `ConsumeStep` at `:124` returns `(false,"wallclock")`; assert non-empty finalEvent. Assert each last Event's StateDelta `termination_reason`/`limit_hit` matches. Table-driven where natural. Run `go test -run 'TestFinalize' ./internal/agent/`.
  </action>
  <verify>
    <automated>go test -run 'TestFinalize' ./internal/agent/ && go test -race ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -run 'TestFinalize' ./internal/agent/` passes (VALIDATION.md Req#2 signal: last Event has non-empty `LLMResponse.Content` at all 3 trip reasons).
    - `go test -run 'TestFinalizeWallclock' ./internal/agent/` passes (the wallclock-specific signal).
    - Behavior assertion: in each test the last collected Event has non-empty `LLMResponse.Content` and is NOT an Escalate-only event; its StateDelta `limit_hit` equals `dedup` / `max_steps` / `wallclock` respectively.
    - `go test -race ./internal/agent/` is green and goleak-clean.
  </acceptance_criteria>
  <done>Dedup, max_steps, and wallclock trips each end with a non-empty finalEvent carrying the right reason; -race + goleak clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop → LLM (finalize turn) | `finalize()` issues ONE extra tool-free LLM call on a trip. No new external/network/auth surface — same client, same endpoint. |
| trip reason → observability | The trip reason flows into the finalEvent StateDelta (internal telemetry), not into user-facing prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-04 | Denial of Service | extra LLM call on every trip | mitigate | `finalize()` is a SINGLE extra call per trip and never calls `ConsumeStep`; the bounded ceiling (`max_steps+2`, with the recovery turn) is enforced and tested in plan 04. This plan routes straight to a single finalize call — no loop. |
| T-07.1-05 | Tampering | finalize request cache prefix | mitigate | finalize reuses the byte-stable `messages[0]` via `PromptBuilder.Build`; the synthesis nudge is a trailing `user` message on a COPY, never the prefix (landmines #1/#2). |
| T-07.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure in-tree Go (RESEARCH State of the Art). |

No high-severity threats: one bounded extra internal LLM call with cache-prefix isolation preserved.
</threat_model>

<verification>
- `go build ./internal/agent/...` + `go vet ./internal/agent/...` clean; all touched files ≤600 LOC.
- `go test -run 'TestFinalize' ./internal/agent/` + `go test -run 'TestFinalizeWallclock' ./internal/agent/` green.
- `go test -race ./internal/agent/` green + goleak-clean.
</verification>

<success_criteria>
- `finalize()` in the new `llm_agent_finalize.go`: tool-free `ToolChoice="none"` request, content-parsed answer, non-empty finalEvent with `termination_reason`/`limit_hit`, never `ConsumeStep`.
- Both trip sites (budget `:124`, dedup `:224`) route through `finalize()`; no bare `terminalBudgetEvent` terminal.
- Dedup / max_steps / wallclock trips each end with a non-empty finalEvent (ACs 1/2/3).
- `-race` + goleak clean; touched files ≤600 LOC.
</success_criteria>

<output>
Create `.planning/phases/07.1-agent-loop-forced-finalization/07.1-03-SUMMARY.md` when done.
</output>
