---
phase: 05-sandbox-2a-stateless
plan: 04
type: execute
wave: 4
depends_on: [05-02, 05-03]
files_modified:
  - scripts/sandbox_escape_bench.sh
  - docs/aura-quality-snapshot.md
  - .github/workflows/sandbox.yml
autonomous: false
requirements: [CAP-01]
must_haves:
  truths:
    - "scripts/sandbox_escape_bench.sh runs the deterministic 18-scenario port, emits a per-scenario table with explicit N/A lines for inapplicable Kubernetes scenarios, and writes the live escape-rate to docs/aura-quality-snapshot.md (CAP-01 SC#4)"
    - "config-regression assertions (docker socket / privileged / writable host mount / excess caps structurally forbidden) stay at 0 (CAP-01 SC#5)"
    - "the bench asserts the compose hardening flags AND the daemon userns-remap setting are present (CAP-01 SC#5)"
    - "the CI DinD job installs runsc, writes the inner daemon.json (userns-remap + runsc), exports the composed sandbox env, runs the sandbox_integration tier + the bench + the QEMU arm64 leg, and GATES the merge (no-skip-as-green)"
    - "the escape-rate < 5% is recorded in docs/aura-quality-snapshot.md with the QEMU-arm64-divergence caveat as a tracked obligation"
  artifacts:
    - path: "scripts/sandbox_escape_bench.sh"
      provides: "deterministic 18-scenario escape bench (config-regression + live denominator), emits escape-rate"
      min_lines: 80
      contains: "escape-rate"
    - path: ".github/workflows/sandbox.yml"
      provides: "gating CI DinD job (runsc + userns-remap + QEMU arm64 + bench + integration tier)"
      contains: "runsc"
    - path: "docs/aura-quality-snapshot.md"
      provides: "Sandbox escape rate row populated with the measured value + QEMU caveat"
      contains: "escape rate"
  key_links:
    - from: "scripts/sandbox_escape_bench.sh"
      to: "docs/aura-quality-snapshot.md"
      via: "writes the escape-rate into the Sandbox escape rate row"
      pattern: "escape"
    - from: ".github/workflows/sandbox.yml"
      to: "scripts/sandbox_escape_bench.sh"
      via: "CI step runs the bench and gates on escape-rate < 5%"
      pattern: "sandbox_escape_bench"
---

<objective>
Build the Gate-3 evidence layer for CAP-01: the deterministic 18-scenario SandboxEscapeBench port (`scripts/sandbox_escape_bench.sh`) that probes each escape technique, asserts the structurally-forbidden misconfigs stay at 0 (config-regression — SC#5) and the runtime/kernel scenarios form the live escape-rate denominator (SC#4), emits an auditable per-scenario table (with explicit N/A lines for inapplicable Kubernetes scenarios per RESEARCH OQ1), and writes the escape-rate (< 5%) to `docs/aura-quality-snapshot.md`. Plus the gating CI DinD workflow that installs `runsc`, writes the inner `daemon.json` (userns-remap + runsc), exports the composed sandbox env (no-skip-as-green), runs the `sandbox_integration` tier + the bench + the QEMU arm64 validation leg, and gates the merge. Closes with a human-verify checkpoint to eyeball the live escape-rate, confirm userns-remap is actually live in the CI DinD daemon (Pitfall 3 — the single most likely CI-setup failure), and sign off the Gate-3 numbers.

Purpose: Prove the isolation composition (escape-rate < 5%, config-regressions = 0, hardening flags + daemon userns-remap present) and gate every future merge on it.
Output: `scripts/sandbox_escape_bench.sh`, `.github/workflows/sandbox.yml`, the updated `docs/aura-quality-snapshot.md` Sandbox row.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md
@.planning/phases/05-sandbox-2a-stateless/05-RESEARCH.md
@.planning/phases/05-sandbox-2a-stateless/05-VALIDATION.md
@docs/aura-quality-snapshot.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic 18-scenario escape bench + quality-snapshot write</name>
  <files>scripts/sandbox_escape_bench.sh, docs/aura-quality-snapshot.md</files>
  <read_first>
    - scripts/llm_smoke.sh lines 1-15 (the header discipline to copy: `#!/usr/bin/env bash`, `set -euo pipefail`, git-root cd, the WHY/no-skip-as-green comment block, explicit skip notice when a prerequisite is absent) — but note this bench is CI-GATING, not manual.
    - .planning/phases/05-sandbox-2a-stateless/05-RESEARCH.md § Common Pitfalls #6 (the 18-vs-20 scenario discrepancy + the mapping: Aura-structurally-forbidden misconfigs -> config-regression-must-stay-0; runtime/kernel CVE scenarios -> live escape-rate denominator; Kubernetes-only scenarios -> explicit N/A lines) + Open Question 1 (reconcile against the paper; emit a per-scenario table so the denominator is auditable) + § Security Domain STRIDE table (the escape patterns to probe).
    - .planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md <decisions> D-01 (all 18 scenarios as deterministic probes; escape-rate < 5% recorded) + D-02 (bench runs against the gVisor-primary x86 profile, container+seccomp floor as the arm64 fallback).
    - docs/aura-quality-snapshot.md (the existing seeded "Sandbox escape rate" row to populate — replace the YYYY-MM-DD placeholder + TBD with the measured date + value; the CI gate path glob is already `internal/sandbox/**`, `sandbox/Dockerfile`, `sandbox/seccomp.json`).
  </read_first>
  <action>
    Write scripts/sandbox_escape_bench.sh as a deterministic CI-gating bench (copy the llm_smoke.sh header discipline: shebang, set -euo pipefail, cd git-root, a WHY-block explaining the no-skip-as-green stance). Port the SandboxEscapeBench 18 conceptual scenarios as DIRECT probe steps (each attempts the escape technique against the running aura-sandbox container and asserts it FAILS). Partition the scenarios two ways per RESEARCH Pitfall 6 / OQ1: (a) CONFIG-REGRESSION scenarios — the misconfigs Aura structurally forbids (docker socket mounted, --privileged, writable host bind mount, excess capabilities) -> assert the compose service does NOT have them (grep compose.yaml + inspect the running container) and that these stay at 0; (b) LIVE-DENOMINATOR scenarios — the runtime/kernel CVE/syscall escapes (ptrace, unshare, mount, socket, /proc/self/root host-file read, etc.) -> attempt each inside the sidecar and count an "escape" only if the technique SUCCEEDS. Print an auditable per-scenario table: each line is `<id> <name> <category> <PASS-contained|ESCAPE|N/A>`, with an explicit `N/A (kubernetes — no orchestrator in Aura's deployment)` line for each inapplicable K8s scenario so the count and the escape-rate denominator are auditable (do NOT silently drop them). Compute escape-rate = escapes / (applicable live-denominator scenarios) and FAIL the script (non-zero exit) if escape-rate >= 5% OR any config-regression > 0. ALSO assert the daemon userns-remap setting is live (inspect `docker info` for the userns-remap field / the remapped uid range — D-14/D-15, CAP-01 SC#5). On success, WRITE the result into docs/aura-quality-snapshot.md: replace the Sandbox-escape-rate row's placeholder date with today's date and the value with the measured escape-rate, and append a one-line note that QEMU-arm64 confirmation is necessary-not-sufficient (real-DGX arm64 confirmation is a tracked obligation per D-12 / Pitfall 4). Skip-discipline: if the sidecar / docker is absent locally, print a clear skip notice — BUT under $CI, t.Fatal-equivalent (exit non-zero) so a skipped bench fails the gate, never passes it (no-skip-as-green).
  </action>
  <verify>
    <automated>cd /home/user/Aura && bash -n scripts/sandbox_escape_bench.sh && grep -q 'set -euo pipefail' scripts/sandbox_escape_bench.sh && grep -q 'escape-rate' scripts/sandbox_escape_bench.sh && grep -Eq 'N/A|kubernetes' scripts/sandbox_escape_bench.sh && grep -q 'config-regression' scripts/sandbox_escape_bench.sh && grep -Eiq 'userns-remap' scripts/sandbox_escape_bench.sh && grep -Eiq 'escape rate' docs/aura-quality-snapshot.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/sandbox_escape_bench.sh` parses; the script contains `set -euo pipefail`, computes an `escape-rate`, and prints a per-scenario table with explicit `N/A` lines for Kubernetes scenarios (auditable denominator — OQ1).
    - The script FAILS (non-zero exit) when escape-rate >= 5% OR any config-regression > 0, and asserts the daemon userns-remap is live (SC#5).
    - docs/aura-quality-snapshot.md "Sandbox escape rate" row carries a real date + measured value (no longer the YYYY-MM-DD/TBD placeholder) plus the QEMU-arm64 tracked-obligation note.
  </acceptance_criteria>
  <done>The deterministic 18-scenario bench emits an auditable escape-rate < 5% with config-regressions at 0 and a live userns-remap assertion, and records the result + QEMU caveat in the quality-snapshot (CAP-01 SC#4/SC#5).</done>
</task>

<task type="auto">
  <name>Task 2: Gating CI DinD workflow (runsc + userns-remap + QEMU arm64 + integration tier + bench)</name>
  <files>.github/workflows/sandbox.yml</files>
  <read_first>
    - .planning/phases/05-sandbox-2a-stateless/05-RESEARCH.md § Standard Stack install block (the exact runsc apt + `runsc install` + daemon.json commands) + § Common Pitfalls #2 (--privileged DinD disables seccomp -> the profile must apply to the INNER sidecar, not be relaxed) + #3 (userns-remap goes on the INNER DinD daemon; outer DinD runs --userns=host) + #4 (QEMU arm64 necessary-not-sufficient) + § Environment Availability (runsc + qemu-user-static must be installed in the CI runner; the research sandbox cannot run them).
    - .planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md <decisions> D-13 (Docker-in-Docker gating CI: real sidecar with runsc, runs //go:build sandbox_integration + the 18-scenario bench + QEMU arm64 validation, gates merge, no-skip-as-green, folds into the 85% floor) + D-11/D-12 (multi-arch seccomp; arm64 under QEMU `docker buildx --platform linux/arm64`).
    - CLAUDE.md "No-skip-as-green in CI" + "Quality tooling & gates" (the composed DSNs / env-export discipline; the gate must export the exact env the tests read so a skipped tier fails, never silently passes) + the AURA_SANDBOX_URL/AURA_SANDBOX_TIMEOUT_SEC/AURA_SANDBOX_RUNTIME env names from 05-03.
    - Any existing `.github/workflows/*.yml` in the repo (match the runner image / Go setup / caching conventions; if none exists, create the directory + a standalone workflow).
  </read_first>
  <action>
    Write .github/workflows/sandbox.yml: a gating job on a Linux runner that (1) installs runsc from the gVisor apt repo (GPG-signed) and runs `runsc install`; (2) writes the inner DinD daemon.json with {userns-remap:default, no-new-privileges:true, runtimes.runsc:{path:/usr/bin/runsc}} and restarts the daemon — per Pitfall 3 the userns-remap goes on the INNER daemon and the outer DinD runs --privileged --userns=host; (3) sets up qemu-user-static binfmt + docker buildx for the arm64 leg (Pitfall 4); (4) builds + starts the aura-sandbox sidecar via `docker compose -f compose.yaml -f compose.gvisor.yaml up -d aura-sandbox` (x86 runsc profile, D-04) and waits for /healthz; (5) EXPORTS the exact sandbox env the integration tier reads (AURA_SANDBOX_URL=http://127.0.0.1:18901, AURA_SANDBOX_TIMEOUT_SEC, AURA_SANDBOX_RUNTIME) so the no-skip-as-green skip-helper does not t.Fatal under $CI; (6) runs `go test -tags sandbox_integration -race ./internal/sandbox/...` (the negative-test tier — ptrace/socket/unshare/proc-root EPERM/ENOENT) and FAILS the job on any red; (7) runs `scripts/sandbox_escape_bench.sh` and FAILS on escape-rate >= 5% or any config-regression; (8) runs the arm64 leg: `docker buildx build --platform linux/arm64` of the sidecar + the negative tests under QEMU (necessary-not-sufficient — record the divergence caveat, do NOT claim arm64 production sign-off); (9) folds sandbox coverage into the 85% floor (run the coverage gate over the sandbox surface). The job is REQUIRED (gates the merge). CRITICAL: the seccomp profile must apply to the inner sidecar even though the outer DinD is --privileged (Pitfall 2) — assert in the job that a negative test (ptrace -> EPERM) actually triggers the boundary (a sub-second PASS is a skip tell). Document at the top of the workflow WHY each step exists (the two CI-setup traps from RESEARCH).
  </action>
  <verify>
    <automated>cd /home/user/Aura && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/sandbox.yml')); print('yaml-ok')" && grep -q 'runsc' .github/workflows/sandbox.yml && grep -Eiq 'userns-remap' .github/workflows/sandbox.yml && grep -q 'sandbox_integration' .github/workflows/sandbox.yml && grep -q 'sandbox_escape_bench.sh' .github/workflows/sandbox.yml && grep -Eq 'linux/arm64|qemu' .github/workflows/sandbox.yml && grep -q 'AURA_SANDBOX_URL' .github/workflows/sandbox.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/sandbox.yml` is valid YAML and installs runsc, writes the inner daemon.json (userns-remap + runsc), sets up QEMU/buildx for the arm64 leg, and exports AURA_SANDBOX_URL (no-skip-as-green).
    - The job runs `go test -tags sandbox_integration -race ./internal/sandbox/...` AND `scripts/sandbox_escape_bench.sh` AND the `--platform linux/arm64` QEMU leg, and gates the merge on all three.
    - The workflow documents Pitfall 2 (inner sidecar keeps seccomp despite --privileged outer DinD) and Pitfall 3 (userns-remap on the inner daemon, outer --userns=host).
  </acceptance_criteria>
  <done>A required gating CI DinD workflow installs runsc + userns-remap, runs the sandbox_integration negative tier + the deterministic bench + the QEMU arm64 leg, exports the sandbox env (no-skip-as-green), and folds coverage into the 85% floor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate-3 sign-off — live escape-rate + userns-remap-live + Gate-3 numbers</name>
  <what-built>
    The full Phase-5 sandbox: the hardened sidecar (05-02), the Go runner + execute tool + aura exec CLI (05-03), and the deterministic 18-scenario escape bench + gating CI DinD workflow (this plan). The bench emits the escape-rate to docs/aura-quality-snapshot.md; the CI job runs runsc + userns-remap + the negative tier + the bench + the QEMU arm64 leg.
  </what-built>
  <how-to-verify>
    1. Bring the stack up locally (WSL, per CLAUDE.md — `make neo4j-migrate` brings infra; build the sidecar): `docker compose -f compose.yaml -f compose.gvisor.yaml up -d aura-sandbox` then confirm `/healthz` is 200.
    2. Run `aura exec python "print(2+2)"` and confirm it prints `4` and exits 0 (CAP-01 SC#1).
    3. Run `aura exec python "import ctypes; ctypes.CDLL(None).ptrace(0,0,0,0)"` and confirm EPERM (SC#2); run `aura exec python "__import__('socket').socket().connect(('1.1.1.1',80))"` and confirm EPERM (SC#3).
    4. Run `scripts/sandbox_escape_bench.sh` and confirm: escape-rate < 5% printed, config-regressions = 0, the per-scenario table shows explicit N/A lines for Kubernetes scenarios, and docs/aura-quality-snapshot.md's Sandbox row now carries today's date + the measured value.
    5. Confirm userns-remap is LIVE in the daemon the sidecar ran under: `docker info | grep -i userns` (or inspect the remapped uid range) — Pitfall 3 is the single most likely CI-setup failure; eyeball it here.
    6. Run `make quality-full` and confirm the combined coverage is >= 85% with sandbox folded in, and the docker.go mutation spot-check is >= 70% killed (CLAUDE.md Gate-3).
    7. Confirm the QEMU-arm64 caveat is recorded in the quality-snapshot as a tracked obligation (real-DGX confirmation pending — D-12).
  </how-to-verify>
  <resume-signal>Type "approved" with the measured escape-rate + coverage number, or describe what failed (e.g. userns-remap not live, a negative test that did not trigger the boundary, escape-rate >= 5%).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench probe -> sidecar container | The bench deliberately attempts escapes from inside/against the sidecar; the container + seccomp + gVisor boundary must contain every applicable scenario. |
| CI outer DinD (--privileged) -> inner DinD daemon -> sidecar | --privileged on the OUTER DinD disables seccomp at that layer; the seccomp profile + userns-remap apply at the INNER daemon's sidecar (Pitfall 2/3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-FALSEGREEN | Repudiation | bench / CI skip | mitigate | No-skip-as-green: the bench exits non-zero under $CI when the sidecar is absent; the CI job exports the exact sandbox env so the integration skip-helper t.Fatals; a sub-second negative-test PASS is treated as a skip tell (Pitfall 2). |
| T-05-04-USERNS | Elevation of Privilege | CI DinD userns-remap | mitigate | userns-remap on the INNER daemon (outer DinD --userns=host, Pitfall 3); the bench asserts it is live; the human-verify checkpoint eyeballs `docker info`. |
| T-05-04-ARM64 | Elevation of Privilege | QEMU arm64 validation | accept | QEMU syscall emulation can diverge from a real arm64 kernel (Pitfall 4); accepted for 2a with a TRACKED OBLIGATION recorded in the quality-snapshot — real-DGX confirmation required before any production arm64 deployment (D-12). |
| T-05-04-DENOM | Repudiation | escape-rate denominator | mitigate | The per-scenario table prints explicit N/A lines for inapplicable Kubernetes scenarios so the denominator is auditable (OQ1) — no silent scenario drops that would inflate or deflate the rate. |
| T-05-04-SC | Tampering | runsc/qemu/image installs | mitigate | All CI installs are OS/container-level from approved sources (RESEARCH § Package Legitimacy Audit, all Approved): runsc (Google apt, GPG-signed), qemu-user-static (Debian apt), python:3.12-slim (Docker Official). No npm/pip/crates registry package -> no [ASSUMED]/[SUS]/[SLOP] -> no blocking legitimacy checkpoint. |
</threat_model>

<verification>
- `bash -n scripts/sandbox_escape_bench.sh` parses; the bench emits an auditable escape-rate < 5%, config-regressions = 0, and a live userns-remap assertion.
- `.github/workflows/sandbox.yml` is valid YAML, installs runsc + userns-remap, runs the integration tier + bench + QEMU arm64 leg, exports the sandbox env, and gates the merge.
- docs/aura-quality-snapshot.md Sandbox row is populated (real date + value) with the QEMU caveat.
- Human-verify checkpoint confirms the live escape-rate, userns-remap-live, SC#1/2/3 CLI behaviors, and the >= 85% combined coverage + >= 70% docker.go mutation (Gate-3 DoD).
</verification>

<success_criteria>
- CAP-01 SC#4: escape-rate < 5% recorded in docs/aura-quality-snapshot.md.
- CAP-01 SC#5: config-regressions = 0 + the hardening flags + daemon userns-remap asserted by the bench.
- The gating CI DinD workflow runs the negative tier + bench + QEMU arm64 leg with no-skip-as-green and folds into the 85% floor.
- Gate-3 DoD numbers (>=85% combined coverage, >=70% docker.go mutation, escape-rate < 5%) signed off at the human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/05-sandbox-2a-stateless/05-04-SUMMARY.md` when done.
</output>
