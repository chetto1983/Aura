---
phase: 05-sandbox-2a-stateless
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .planning/DECISIONS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CAP-01]
must_haves:
  truths:
    - "prd.md §Slice 2 D12 records gVisor runsc as the PRIMARY x86 boundary (not a >5% escalation seam)"
    - "DECISIONS.md D12 row re-decided to gVisor-primary x86 / hardened-container+seccomp+userns-remap floor / arm64 runc fallback / microVM still rejected"
    - "ROADMAP Phase 5 success-criterion #5 notes gVisor default-on x86 + the escape-bench profile is the gVisor-primary x86 profile"
    - "PRD Slice 2a acceptance #4 reads 'sidecar down AND auto-start fails → clear error' (the one-shot docker compose up -d auto-start helper is in docker.go scope)"
  artifacts:
    - path: "prd.md"
      provides: "Amended §Slice 2 D12 + Slice 2a acceptance #4"
      contains: "gVisor"
    - path: ".planning/DECISIONS.md"
      provides: "Re-decided D12 row (gVisor-primary x86)"
      contains: "gVisor"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 SC#5 gVisor-default-on-x86 note"
      contains: "gVisor"
  key_links:
    - from: "prd.md §Slice 2 D12"
      to: ".planning/DECISIONS.md D12"
      via: "consistent gVisor-primary re-decision (no contradiction between the two)"
      pattern: "gVisor"
---

<objective>
Land the TWO PRD-amendment commits that are HARD prerequisites for every Phase-5 code commit. CLAUDE.md's PRD-first principle is absolute: two CONTEXT decisions (D-05/06/07 and D-09) OVERRIDE locked PRD/DECISIONS.md content, so the truth-source documents MUST be amended BEFORE any sandbox code lands. This plan is doc-only and gates all code waves.

Purpose: Reconcile the truth-source (prd.md + DECISIONS.md + ROADMAP.md) with the gVisor-primary re-decision (D-05/06/07) and the auto-start lifecycle deviation (D-09) so no downstream code wave contradicts locked content.
Output: Two atomic PRD-amendment commits touching prd.md, .planning/DECISIONS.md, .planning/ROADMAP.md.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md
@.planning/phases/05-sandbox-2a-stateless/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend D12 to gVisor-primary (D-05/06/07) across prd.md + DECISIONS.md + ROADMAP.md</name>
  <files>prd.md, .planning/DECISIONS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/DECISIONS.md D12 row (the §1 TIER-1 table line currently reading "Primario = container+seccomp INDURITO ... gVisor = escalation x86-first") + the §5 follow-up note line 115 referencing amendment #32 — these are the verbatim locked text being re-decided.
    - prd.md §Slice 2 (lines ~1200–1330): the D12 isolation decision paragraph + Slice 2a acceptance list.
    - .planning/ROADMAP.md §"Phase 5: Sandbox 2a Stateless" success-criterion #5 (the `cap_drop:ALL ... userns-remap` hardening list line).
    - .planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md § Required PRD Amendments item 1 (source of truth for the re-decision wording).
  </read_first>
  <action>
    Re-decide D12 to gVisor-primary in all three documents, keeping them mutually consistent. Per D-05/D-06/D-07: gVisor `runsc` is the PRIMARY sandbox boundary on x86 (was: gVisor = >5%-escalation seam only); hardened-container + seccomp positive-allowlist + userns-remap is the PORTABLE FLOOR (defense-in-depth inside gVisor on x86, standalone fallback boundary on arm64/DGX until gVisor-arm64 is GA); the Go runner is runtime-agnostic, selected per-arch via `AURA_SANDBOX_RUNTIME` (resolves `runsc` on amd64, `runc` on arm64). microVM (Firecracker/Kata) stays REJECTED (needs `/dev/kvm`, violates the D00 portability invariant — keep this rejection intact). In prd.md §Slice 2 rewrite the D12 paragraph; in DECISIONS.md rewrite the D12 row and update the §5 follow-up line so it no longer claims "container+seccomp primary, gVisor escalation-only"; in ROADMAP Phase 5 annotate success-criterion #5 that gVisor is default-on x86 and the escape-bench (D-01) runs against the gVisor-primary x86 profile. Cite D-05/D-06/D-07 in the amendment prose. Record this as the amendment number after the existing #32 (use #36 or the next free number; preserve the QEMU-arm64-divergence caveat as a tracked obligation). Do NOT touch any code file — this is doc-only. Commit atomically with an imperative subject explaining WHY (re-decision driven by the KVM-less "full sandbox like Claude Code's" user directive → gVisor is the achievable maximum).
  </action>
  <verify>
    <automated>grep -q "gVisor" prd.md && grep -q "gVisor" .planning/DECISIONS.md && grep -iq "gVisor.*primar\|primar.*gVisor\|default-on x86\|default on x86" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gVisor" .planning/DECISIONS.md` returns ≥1 and the D12 row no longer contains the phrase "gVisor = escalation x86-first" as the operative primitive.
    - prd.md §Slice 2 D12 prose states gVisor runsc is the primary x86 boundary and cites D-05/D-06/D-07.
    - .planning/ROADMAP.md Phase 5 success-criterion section notes gVisor default-on x86 + the gVisor-primary escape-bench profile.
    - microVM rejection text is still present in all three documents (not deleted).
  </acceptance_criteria>
  <done>D12 re-decided to gVisor-primary consistently across prd.md, DECISIONS.md, and ROADMAP.md; microVM still rejected; amendment number recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Amend Slice 2a acceptance #4 for the D-09 auto-start lifecycle</name>
  <files>prd.md</files>
  <read_first>
    - prd.md §Slice 2 Slice-2a acceptance list — the acceptance item currently reading "sidecar down → clear error" (the exact wording to rewrite).
    - prd.md §Slice 2 file-targets line describing `docker.go ~220 LOC` (auto-start helper note attaches to this scope).
    - .planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md § Required PRD Amendments item 2 + <decisions> D-09 (source wording).
    - .planning/phases/05-sandbox-2a-stateless/05-RESEARCH.md Open Question 3 (auto-start must be best-effort + docker-CLI-gated; NEVER mount the docker socket).
  </read_first>
  <action>
    Rewrite the Slice 2a acceptance item "sidecar down → clear error" to "sidecar down AND auto-start fails → clear error" per D-09. In the prd.md `docker.go` scope note, add that the runner performs ONE best-effort auto-start (`docker compose up -d aura-sandbox`) on connect failure, health-checks, retries the call once, and only then returns the typed `ErrSandboxUnreachable`. CRITICAL constraint to record verbatim in the PRD note: the execution path stays HTTP-only; auto-start is docker-CLI-gated (skip straight to `ErrSandboxUnreachable` when no `docker` binary is on PATH) and MUST NEVER mount the docker socket into Aura (that re-introduces the #1 escape vector the SandboxEscapeBench tests). Cite D-09. Do NOT touch any code file. Commit atomically (separate from Task 1's commit if practical, or note both amendments — one slice can carry multiple PRD-amendment commits) with an imperative subject explaining the contained deviation (HTTP-only path preserved; acceptance rewritten).
  </action>
  <verify>
    <automated>grep -q "auto-start fails" prd.md && grep -iq "never mount the docker socket\|MUST NEVER mount" prd.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - prd.md Slice 2a acceptance contains the exact phrase "auto-start fails" in the rewritten acceptance #4.
    - prd.md `docker.go` scope note records the docker-CLI-gated one-shot auto-start AND the "never mount the docker socket" constraint.
    - The PRD still describes the execution path as HTTP-only (no docker-runtime-driving from Go).
  </acceptance_criteria>
  <done>Slice 2a acceptance #4 reads "sidecar down AND auto-start fails → clear error"; the docker-CLI-gated, socket-free auto-start helper is in docker.go's documented scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (doc-only plan) | No runtime trust boundary is crossed; this plan only edits truth-source markdown. The threat it mitigates is a process threat: code landing against stale/contradictory locked decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-DOC | Repudiation | PRD/DECISIONS drift | mitigate | Amendments cite D-05/06/07/09 + an amendment number so the re-decision is auditable and non-repudiable; microVM rejection preserved to prevent silent scope creep. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this doc-only plan; the package-legitimacy gate applies to plan 05-02/05-03 instead. |
</threat_model>

<verification>
- Both PRD amendments landed BEFORE any Phase-5 code commit (this is Wave 1; all code waves depend on it).
- `git log` shows the amendment commit(s) precede every code commit in the phase.
- The three documents are mutually consistent on gVisor-primary; microVM rejection intact.
</verification>

<success_criteria>
- prd.md §Slice 2 D12 = gVisor-primary x86; acceptance #4 = "sidecar down AND auto-start fails → clear error".
- .planning/DECISIONS.md D12 row re-decided; §5 follow-up line corrected.
- .planning/ROADMAP.md Phase 5 SC#5 notes gVisor default-on x86.
- No code file touched.
</success_criteria>

<output>
Create `.planning/phases/05-sandbox-2a-stateless/05-01-SUMMARY.md` when done.
</output>
