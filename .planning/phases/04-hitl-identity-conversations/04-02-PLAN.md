---
phase: 04-hitl-identity-conversations
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - internal/identity/store.go
  - internal/identity/store_test.go
  - internal/identity/main_test.go
  - cmd/aura/identity.go
  - cmd/aura/main.go
autonomous: true
requirements: [CORE-03]

must_haves:
  truths:
    - "HasCapability('local','any_tool') returns true on a fresh boot via the '*' wildcard"
    - "grant then revoke of an ordinary capability is idempotent (repeat is a no-op, never an error)"
    - "grant/revoke of '*' is rejected with a clear system-managed message and non-zero exit"
    - "deleting an identity cascades its capability_grants away"
    - "aura identity {list|get|grant|revoke} works as hand-rolled switch subcommands (NOT cobra)"
  artifacts:
    - path: "internal/identity/store.go"
      provides: "identity.Store over sqlc; HasCapability wildcard-or-exact; grant/revoke with SQLSTATE classification"
      contains: "func (s *Store) HasCapability"
    - path: "cmd/aura/identity.go"
      provides: "aura identity CLI (switch dispatcher mirroring runDB)"
      contains: "func runIdentity"
  key_links:
    - from: "internal/identity/store.go"
      to: "internal/db/sqlc HasCapability/GrantCapability/RevokeCapability"
      via: "s.q query calls"
      pattern: "s\\.q\\."
    - from: "cmd/aura/main.go"
      to: "runIdentity"
      via: "top-level switch case identity"
      pattern: "case \"identity\""
---

<objective>
Build the identity slice (PRD 1.7) FIRST to derisk and prove the canonical Store pattern (D-A4-01): per-domain `Store{pool,q}`, SQLSTATE error classification, `db_integration` test discipline, and a hand-rolled `switch` CLI — all on the lowest-risk domain (2 tables, no Runner coupling, no pause logic, no context management). 04-03/04/05 copy this exact pattern.

Purpose: Identity is the "hello world" of the Store pattern; proving it here means the Runner-coupled slices reuse a known-good shape. CORE-03.
Output: `internal/identity/Store` with `HasCapability` wildcard semantics + grant/revoke idempotency + `'*'` rejection + FK cascade; `aura identity {list|get|grant|revoke}` CLI wired into `main.go`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-hitl-identity-conversations/04-SPEC.md
@.planning/phases/04-hitl-identity-conversations/04-PATTERNS.md
@.planning/phases/04-hitl-identity-conversations/04-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: identity.Store — HasCapability wildcard + grant/revoke idempotency + '*' rejection + FK cascade (CORE-03 Req#5,#6)</name>
  <read_first>
    - internal/db/db.go:35 (error-wrap `fmt.Errorf("...: %w", ...)`), :72 (redactDSN)
    - internal/db/db_test.go:1-45 (`//go:build db_integration`, goleak.VerifyTestMain, envOrSkip that t.Fatals under $CI) and :206-209 (SQLSTATE `errors.As(err,&pgErr)` discipline — the analog, PATTERNS L85-92)
    - internal/db/sqlc/db.go:20 (`sqlc.New(pool)` construction)
    - internal/db/sqlc/querier.go + identity.sql.go + capability_grants.sql.go (the generated surface from 04-01; the exact method names + pgtype mappings)
    - 04-SPEC.md Req#5 (seed contract), Req#6 (HasCapability wildcard-or-exact, name regex `^[a-z][a-z0-9._-]{0,63}$`, '*' system-managed)
    - 04-PATTERNS.md L72-101 (identity/store.go assignment), 04-RESEARCH.md Pitfall 2 (SQLSTATE 23505 unique / 23503 FK / 42501 privilege)
  </read_first>
  <behavior>
    - HasCapability(ctx, identityID, cap): true when the identity has '*' OR the exact cap; false otherwise; error only on DB failure
    - GrantCapability is idempotent: granting an existing (identity,cap) is a no-op (swallow SQLSTATE 23505), never an error
    - RevokeCapability is idempotent: revoking an absent cap is a no-op
    - GrantCapability/RevokeCapability of '*' return a clear "wildcard is system-managed" error (rejected before touching the DB)
    - capability name failing `^[a-z][a-z0-9._-]{0,63}$` returns a validation error
    - DeleteIdentity cascades: capability_grants for that identity are gone afterward (FK ON DELETE CASCADE)
  </behavior>
  <action>
    Create `internal/identity/store.go`: `type Store struct{ pool *pgxpool.Pool; q *sqlc.Queries }`, `func New(pool *pgxpool.Pool) *Store { return &Store{pool, sqlc.New(pool)} }` (PATTERNS L77-82). Expose `HasCapability(ctx, identityID, cap string) (bool, error)` (wildcard-or-exact via the generated lookup query), `GrantCapability`, `RevokeCapability`, `ListIdentities`, `GetIdentityByName`, `DeleteIdentity`. Validate capability names against `^[a-z][a-z0-9._-]{0,63}$` (compiled once at package init). Reject grant/revoke of `'*'` BEFORE any DB call with a sentinel/clear error ("wildcard is system-managed"). Classify DB errors with `var pgErr *pgconn.PgError; errors.As(err,&pgErr)` switching on `pgErr.Code` (23505 → idempotent grant no-op; 23503 → FK; 42501 → privilege) — NEVER string-match. Wrap every returned error with `%w` + context prefix. No interface declared in this package (the consumer declares it, D-A2-02).
    Create `internal/identity/main_test.go` (goleak.VerifyTestMain) and `internal/identity/store_test.go` with `//go:build db_integration` covering: fresh-boot seed assertion (`HasCapability("local","any_tool")==true` via wildcard; exactly one `(...001,'*')` row), grant/revoke idempotency, `'*'` grant + revoke rejection, capability-name regex rejection, and FK cascade on DeleteIdentity. Use the envOrSkip helper shape so the tier `t.Fatal`s under `$CI` when DSN is unset (no-skip-as-green).
  </action>
  <verify>
    <automated>go build ./internal/identity/ && go vet ./internal/identity/ && go test ./internal/identity/ && go test -race ./internal/identity/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/identity/` and `-race` exit 0 (unit tier)
    - db_integration (Wave-2 merge): `go test -tags db_integration -race ./internal/identity/` exits 0
    - `SELECT count(*) FROM aura.capability_grants WHERE identity_id='00000000-0000-0000-0000-000000000001' AND capability='*'` == 1 (asserted in test)
    - `HasCapability("local","any_tool")` returns `(true, nil)` on a freshly migrated DB
    - granting an existing cap twice leaves exactly one row and returns no error; revoking an absent cap returns no error
    - `GrantCapability(...,"*")` and `RevokeCapability(...,"*")` both return an error containing the system-managed message
    - after `DeleteIdentity`, that identity's capability_grants count is 0
  </acceptance_criteria>
  <done>identity.Store proves the canonical Store pattern with wildcard HasCapability, idempotent grant/revoke, '*' rejection, name validation, and FK cascade — all db_integration-verified.</done>
</task>

<task type="auto">
  <name>Task 2: aura identity CLI as hand-rolled switch (resolve OPEN QUESTION 1 — NOT cobra) + wire into main.go (CORE-03 Req#6)</name>
  <read_first>
    - cmd/aura/db.go:19-44 (`runDB` switch dispatcher — the exact analog, PATTERNS L493-507) and :110-115 (tabwriter list output), :118-122 (--confirm gating)
    - cmd/aura/main.go:29-49 (top-level switch + usage line)
    - internal/identity/store.go (from Task 1 — the methods the CLI calls)
    - internal/config/config.go (config.Load) and internal/db/db.go (db.Open) — the composition shape per branch
    - 04-RESEARCH.md OPEN QUESTION 1 (L438-441) + Assumption A2 — cobra-vs-switch resolution
  </read_first>
  <action>
    Create `cmd/aura/identity.go` with `func runIdentity(args []string)` mirroring `runDB` exactly: usage line on missing/unknown subcommand, `config.Load()` → `db.Open(ctx,cfg)` → `identity.New(pool)`, then `switch args[0]` over `list|get <name>|grant <name> <cap>|revoke <name> <cap>`. Print human-readable output; use `tabwriter` for `list`/`get`. `os.Exit(1)` on error (per-branch shape from db.go). Grant/revoke print idempotent confirmation; `grant local '*'` / `revoke local '*'` surface the store's system-managed error to stderr with non-zero exit. Add `case "identity": runIdentity(os.Args[2:])` to `cmd/aura/main.go` and update the `usage()` line.
    DEVIATION NOTE (record in the SUMMARY): CONTEXT D-A3-05 says "cobra command group" but go.mod has no spf13/cobra and the codebase uses nested `switch` dispatchers (`runDB`); CLAUDE.md mandates following existing patterns and SPEC never requires cobra. Implemented as a `switch` subcommand tree (RESEARCH OPEN QUESTION 1 / Assumption A2). This is a HOW deviation from CONTEXT wording, not a SPEC requirement change — no PRD amendment needed beyond noting it.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && grep -q "case \"identity\"" cmd/aura/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` and `go vet ./...` exit 0
    - `grep -q "case \"identity\"" cmd/aura/main.go` returns 0; `grep -ri cobra go.mod` returns empty (no new dep)
    - manual/db_integration CLI smoke: `aura identity list` prints the `local` identity; `aura identity grant local foo` then again is idempotent; `aura identity revoke local foo` is idempotent; `aura identity grant local '*'` exits non-zero with the system-managed message
    - `cmd/aura/identity.go` ≤ 600 LOC
  </acceptance_criteria>
  <done>aura identity CLI works as a hand-rolled switch (no cobra), wired into main.go; the cobra→switch deviation is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → identity.Store | capability names + identity names cross here (untrusted-ish operator input) |
| identity.Store → Postgres | grant/revoke/lookup as bound params |
| (NOT crossed) LLM → identity | identity/capability tools are CLI/infra-only, never LLM-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | self-elevation via an LLM-facing identity tool | mitigate | identity/capability surface is CLI-only (`runIdentity`); NO LLM-facing grant tool is registered (SPEC out-of-scope; deferred-tool partition) |
| T-04-05 | Tampering | malformed capability name / injection | mitigate | name regex `^[a-z][a-z0-9._-]{0,63}$` validated before DB; sqlc parameterized queries (Task 1) |
| T-04-06 | Elevation of Privilege | granting/revoking the '*' wildcard out of band | mitigate | grant/revoke of '*' rejected with system-managed error before any DB call (Task 1) |
| T-04-07 | Tampering | SQLSTATE misclassification (string-match) breaking idempotency | mitigate | classify via `errors.As(&pgErr)` + `pgErr.Code` only; never message-match (Pitfall 2, Task 1) |
</threat_model>

<verification>
- Unit: `go test -race ./internal/identity/` green; goleak clean
- db_integration (Wave-2 merge, WSL stack up): `go test -tags db_integration -race ./internal/identity/ -count=1` green; seed + idempotency + '*' rejection + FK cascade assertions pass
- `golangci-lint run ./internal/identity/ ./cmd/aura/` == 0; files ≤ 600 LOC
- No cobra dependency introduced
</verification>

<success_criteria>
- HasCapability wildcard works on fresh boot; grant/revoke idempotent; '*' rejected; FK cascade verified
- aura identity CLI is a hand-rolled switch wired into main.go
- Canonical Store pattern proven for 04-03/04/05 to copy
- cobra→switch deviation documented (OPEN QUESTION 1 resolved)
</success_criteria>

<output>
Create `.planning/phases/04-hitl-identity-conversations/04-02-SUMMARY.md` when done
</output>
