---
phase: 04-hitl-identity-conversations
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - internal/conversations/store.go
  - internal/conversations/store_test.go
  - internal/conversations/context.go
  - internal/conversations/context_test.go
  - internal/conversations/orphan_scan.go
  - internal/conversations/orphan_scan_test.go
  - internal/conversations/title.go
  - internal/conversations/title_test.go
  - internal/conversations/main_test.go
autonomous: true
requirements: [CORE-04, CORE-05]

must_haves:
  truths:
    - "AppendTurn writes INSERT turn + UPDATE conversation aggregates in one db.WithTx tx; a mid-tx failure leaves no partial turn (SC-2)"
    - "LoadHistory reconstructs []llm.Message from conversation_turns ORDER BY seq, byte-identical across two calls (Req#8)"
    - "content > AURA_CONVERSATION_TURN_CAP_BYTES spills to a sidecar file with content=NULL + content_sidecar_path set"
    - "L1 microcompact rewrites only role='tool' turns older than the evict window to a read_tool_output pointer, NEVER seq=1; SC-1: L1-alone writes zero context_rot_events rows"
    - "L2.5 hard buffer drops the oldest user/assistant pair preserving len(history)%2==0 and writes a context_rot_events row"
    - "auto-title fires after seq>=3 via WithoutCancel+WithTimeout, WaitGroup-tracked, goleak-clean; LLM failure leaves title NULL without crashing"
    - "SearchConversationTurns returns matches ordered by similarity DESC from the pg_trgm GIN index (locked cross-slice query)"
    - "ScanOrphans removes conversations/* dirs with no DB row under an O_NOFOLLOW/Lstat symlink guard; size WARN is audit-only"
  artifacts:
    - path: "internal/conversations/store.go"
      provides: "conversations.Store: Create/LoadHistory/AppendTurn(WithTx)/List/Search/Delete/SetTitleIfNull/status"
      contains: "func (s *Store) AppendTurn"
    - path: "internal/conversations/context.go"
      provides: "L1/L2/L2.5 deterministic context ladder + cached tiktoken-go encoder"
      contains: "context_rot_events"
    - path: "internal/conversations/orphan_scan.go"
      provides: "ScanOrphans reconciliation GC with symlink guard + tmp TTL + audit-only size WARN"
      contains: "func ScanOrphans"
    - path: "internal/conversations/title.go"
      provides: "best-effort auto-title generation (the worker body; WaitGroup owned by Runner)"
      contains: "generateTitle"
  key_links:
    - from: "internal/conversations/store.go"
      to: "internal/db.WithTx"
      via: "AppendTurn wraps WithTx for atomic INSERT+UPDATE"
      pattern: "WithTx"
    - from: "internal/conversations/context.go"
      to: "internal/llm.Config ContextWindow/MaxOutputTokens"
      via: "L2 hard_cap = ContextWindow - max(MaxOutputTokens,20000) - 13000"
      pattern: "ContextWindow"
    - from: "internal/conversations/store.go"
      to: "pg_trgm similarity"
      via: "SearchConversationTurns content % $1 ORDER BY similarity DESC"
      pattern: "Search"
---

<objective>
Build the conversation persistence + deterministic context-management slice (PRD 1.8 + the 1.8.5 FTS query layer): `conversations.Store` (multi-thread persistence, atomic per-turn `AppendTurn` via `db.WithTx`, byte-identical `LoadHistory`, per-conversation token+USD aggregation, status/rename/delete, `SearchConversationTurns`), the L1/L2/L2.5 context ladder in `context.go`, the boot orphan-scan reconciliation GC in `orphan_scan.go`, and the best-effort auto-title worker body in `title.go`. The Runner (04-05) owns the WaitGroup and the composition root; this plan delivers the Store + helpers it consumes.

Purpose: This is the durable conversation core — crash atomicity (SC-2), deterministic resume (Req#8), bounded context growth (L1/L2/L2.5), and the locked FTS query. CORE-04 (Req#7-12) + CORE-05 (Req#13 query layer).
Output: `conversations.Store` + `context.go` (L1/L2/L2.5 + cached tiktoken encoder) + `orphan_scan.go` (symlink-guarded GC) + `title.go` (auto-title body).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-hitl-identity-conversations/04-SPEC.md
@.planning/phases/04-hitl-identity-conversations/04-CONTEXT.md
@.planning/phases/04-hitl-identity-conversations/04-PATTERNS.md
@.planning/phases/04-hitl-identity-conversations/04-RESEARCH.md
@.planning/phases/04-hitl-identity-conversations/04-01-SUMMARY.md
@.planning/phases/04-hitl-identity-conversations/04-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: conversations.Store — atomic AppendTurn (SC-2), byte-identical LoadHistory (Req#8), token/USD aggregation, sidecar spill, status/rename/delete, FTS Search (CORE-04 Req#7,#8,#9-agg,#12; CORE-05 Req#13)</name>
  <read_first>
    - internal/identity/store.go (proven Store{pool,q} + SQLSTATE pattern — copy)
    - internal/db/tx.go (WithTx — AppendTurn wraps it, from 04-01)
    - internal/db/sqlc/conversations.sql.go + conversation_turns.sql.go + querier.go (generated surface from 04-01; pgtype.Numeric/Text mappings — Pitfall 5)
    - internal/llm/client.go:24-30 (llm.Message/RoleTool shapes for LoadHistory) + :61 (Usage{PromptTokens,CompletionTokens,CachedTokens,Cost}) ; internal/llm/prices.go:32 (CostUSD source, PATTERNS L134-139)
    - internal/agent/tools/result.go:45-71 (validateID + sidecarPath path-traversal guard) + :75-84 (truncatePreview rune-boundary) — sidecar reuse, PATTERNS L155-203
    - internal/db/db_test.go:1-45 (db_integration tag + goleak + envOrSkip)
    - 04-SPEC.md Req#7 (tables + sidecar > cap), Req#8 (byte-identical LoadHistory + atomic tx), Req#9 (aggregation), Req#12 (delete cascade + RemoveAll), Req#13 (locked FTS query)
    - 04-PATTERNS.md L124-150 (store.go) + L431-439 (locked FTS query verbatim) + Pitfall 5 (pgtype handling)
  </read_first>
  <behavior>
    - AppendTurn(ctx, turn) wraps db.WithTx: INSERT conversation_turns + UPDATE conversations SET last_active_at, total_input/output/cached_tokens += , total_cost_usd += $delta — all in ONE tx; an injected failure between INSERT and UPDATE rolls back, leaving no partial turn (SC-2)
    - LoadHistory(convID) reconstructs []llm.Message from conversation_turns ORDER BY seq; two consecutive calls return byte-identical slices (Req#8)
    - turn content > AURA_CONVERSATION_TURN_CAP_BYTES spills to $AURA_RUN_DIR/conversations/<id>/<seq>.content via the validateID/sidecarPath guard; row stores content=NULL + content_sidecar_path
    - after a multi-turn run, conversations.total_cost_usd > 0 and equals the sum of turn-level token columns (aggregation correct)
    - SearchConversationTurns(q,limit) returns rows ordered by similarity(content,q) DESC from the GIN index
    - Delete(convID) DB-deletes the conversation (turns + paused_states cascade via FK) then os.RemoveAll the sidecar dir after commit
  </behavior>
  <action>
    Create `internal/conversations/store.go`: `Store{pool,q}` (copy identity shape). `Create`, `Get`, `List(includeArchived bool)`, `UpdateStatus` (active/archived/deleted for archive/unarchive/delete), `Rename`, `SetTitleIfNull` (idempotent `UPDATE ... WHERE title IS NULL`), `AppendTurn` (wrap db.WithTx: INSERT turn + UPDATE aggregates, SC-2 atomicity — aggregate USD in SQL as `total_cost_usd = total_cost_usd + $delta` per RESEARCH OQ4; convert pgtype.Numeric at the read boundary), `LoadHistory(convID) ([]llm.Message, error)` (ORDER BY seq, byte-identical; rehydrate sidecar-spilled content by reading content_sidecar_path; reuse llm.Message/RoleTool shapes), `SearchConversationTurns` (the LOCKED query — query layer is the cross-slice contract for future Telegram /search; excerpt is app-side in the CLI, 04-05), `Delete(convID)` (DB delete then os.RemoveAll the sidecar dir after commit; rm failure → WARN, recovered by ScanOrphans). Sidecar spill on write: content > `cfg.TurnCapBytes` (65536) → write `<seq>.content` via validateID/sidecarPath (reuse tools/result.go scheme; session_id==conversation_id, D-26), store content=NULL + path. Classify errors by SQLSTATE; wrap `%w`; render NULL title as `(untitled <created_at>)` at the boundary.
    Create `internal/conversations/main_test.go` (goleak) + `store_test.go` (`//go:build db_integration`): persist 3 turns, restart (fresh Store), LoadHistory returns 3 turns byte-identical across two calls; inject a failure between INSERT and UPDATE → rollback, no orphan turn (SC-2); content >65536B spills (content=NULL + file on disk); total_cost_usd > 0 aggregated == sum of turn columns; SearchConversationTurns ordered by similarity DESC; Delete cascades turns + paused_states + removes the sidecar dir. envOrSkip → t.Fatal under $CI.
  </action>
  <verify>
    <automated>go build ./internal/conversations/ && go vet ./internal/conversations/ && go test ./internal/conversations/ && go test -race ./internal/conversations/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/conversations/` and `-race` exit 0; goleak clean
    - db_integration (Wave-3 merge): two `LoadHistory(id)` calls return byte-equal slices after a process restart
    - SC-2: a failure injected between the turn INSERT and the aggregates UPDATE leaves `SELECT count(*) FROM aura.conversation_turns WHERE conversation_id=:id` unchanged (rollback, no partial turn)
    - turn content >65536B → row `content IS NULL` + `content_sidecar_path` set + file exists at `$AURA_RUN_DIR/conversations/<id>/<seq>.content`
    - `total_cost_usd > 0` and equals the per-turn sum after a multi-turn run
    - `SearchConversationTurns("phrase",N)` rows ordered by `similarity DESC`
    - `Delete(id)` → conversation_turns + paused_states for that id are 0 (FK cascade) and the sidecar dir is gone
    - file ≤ 600 LOC (split `store_<concern>.go` if it crosses)
  </acceptance_criteria>
  <done>conversations.Store delivers atomic per-turn writes, byte-identical LoadHistory, sidecar spill, token/USD aggregation, the locked FTS query, and delete cascade — all db_integration-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: context.go L1/L2/L2.5 ladder + cached tiktoken-go encoder (CORE-04 Req#10, SC-1)</name>
  <read_first>
    - internal/agent/tools/result.go:75-84 (rune-safe truncate analog) + read_tool_output.go (the pointer target L1 evicts to)
    - internal/llm/config.go (ContextWindow + MaxOutputTokens from 04-01) ; internal/config/config.go (AURA_CONTEXT_TOOL_EVICT_AFTER_TURNS=10, AURA_HISTORY_HARD_CAP_TURNS=50 from 04-01)
    - internal/db/sqlc/context_rot_events.sql.go (InsertContextRotEvent from 04-01)
    - internal/agent/llm_agent.go:69,133 (system prompt byte-stable — NEVER touch seq=1)
    - 04-SPEC.md Req#10 (L1/L2/L2.5 exact formulas + L3 NOT implemented), 04-CONTEXT.md D-A2-06 (tiktoken cached encoder init once), SC-1 (L1-alone → zero rot rows)
    - 04-PATTERNS.md L153-180 (context.go) + Pitfall 1 (KV-cache poisoning — only role='tool', never seq=1) + Assumption A6 (tiktoken offline vocab)
    - feedback_minipc_cpu_budget (encoder init once, no busy-loop)
  </read_first>
  <behavior>
    - L1: role='tool' turns with seq < (max_seq - evict_after) have content replaced by a read_tool_output(<id>) pointer; seq=1 (system) is NEVER touched; sidecar still fetchable via read_tool_output
    - SC-1: a history bloated only by large tool outputs is brought under budget by L1 alone — zero context_rot_events rows written
    - L2: hard_cap = ContextWindow - max(MaxOutputTokens,20000) - 13000; warn_cap = 0.75*hard_cap; over warn → log WARN; over hard → return an explicit error suggesting `aura chat new` (surfaces to the REPL, not the iter error slot)
    - L2.5: if still over 100% window after L1+L2, drop the oldest user/assistant PAIR (preserve system L0 + keep len(history)%2==0) until it fits, writing a context_rot_events row {action:'hard_drop_pairs', pairs_dropped, tokens_before, tokens_after}
    - token estimation uses a cached cl100k_base tiktoken-go encoder initialized once
  </behavior>
  <action>
    Create `internal/conversations/context.go`: a cached `tiktoken-go` cl100k_base encoder initialized once (package-level lazy init, goleak-safe, offline-vocab configured per Assumption A6 — bundle/embed the vocab or configure tiktoken_loader so no network fetch at runtime; verify offline behavior). Implement the ladder applied in/around LoadHistory in SC-1 order: **L1** rewrite only `role='tool'` turns older than `AURA_CONTEXT_TOOL_EVICT_AFTER_TURNS` (10) to a `read_tool_output(<id>)` pointer — NEVER seq=1 (Pitfall 1, KV-cache poisoning); reuse the rune-safe truncate from result.go. **L2** compute `hard_cap = ContextWindow - max(MaxOutputTokens,20000) - 13000`, `warn_cap = 0.75*hard_cap`; over warn → slog WARN; over hard → return an explicit error (suggest `aura chat new`). **L2.5** if still over window, drop the oldest user/assistant pair preserving `len(history)%2==0`, write a `context_rot_events` row via InsertContextRotEvent. L3 is explicitly NOT implemented.
    Create `context_test.go` (unit, no DB needed for L1/L2 logic; db_integration for the context_rot_events row): SC-1 — a tool-output-bloated history fits after L1 alone with ZERO context_rot_events rows; L1 never rewrites seq=1; over-hard-cap returns the explicit error; L2.5 drops the oldest pair, leaves len even, writes one context_rot_events row {action:'hard_drop_pairs'}. Mutation-target file (≥70% killed at phase gate).
  </action>
  <verify>
    <automated>go build ./internal/conversations/ && go vet ./internal/conversations/ && go test ./internal/conversations/ && go test -race ./internal/conversations/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/conversations/` and `-race` exit 0
    - SC-1: tool-output-bloated history brought under budget by L1 alone → `SELECT count(*) FROM aura.context_rot_events WHERE conversation_id=:id` == 0
    - L1 never mutates the seq=1 system turn (asserted: system content unchanged after L1)
    - over hard_cap → an explicit error mentioning `aura chat new` (not the iter error slot)
    - L2.5: history past the window drops the oldest pair, `len(history)%2==0` afterward, and exactly one `context_rot_events` row with `action='hard_drop_pairs'` is written
    - tiktoken encoder initialized once (no per-call init); no network fetch at runtime (offline vocab)
    - `grep -r "L3\|chat_compact" internal/conversations/context.go` shows no L3 implementation
  </acceptance_criteria>
  <done>The L1/L2/L2.5 ladder bounds history deterministically (L1-first per SC-1), never poisons the cached prefix, and the tiktoken encoder is cached + offline.</done>
</task>

<task type="auto">
  <name>Task 3: orphan_scan.go (symlink-guarded boot GC) + title.go (auto-title body) (CORE-04 Req#9, Req#12)</name>
  <read_first>
    - internal/agent/tools/result.go:45-71 (validateID + sidecarPath — the path-traversal guard analog, PATTERNS L187-203)
    - internal/db/sqlc/conversations.sql.go (the "does this conversation row exist" lookup)
    - cmd/aura/chat.go runOneTurn (LLM call shape for generateTitle) + signalTurnCtx (ctx lifecycle)
    - internal/conversations/store.go (SetTitleIfNull from Task 1)
    - 04-SPEC.md Req#9 (auto-title after seq>=3, idempotent, never blocks chat, NULL renders untitled), Req#12 (delete cascade + boot orphan scan + tmp TTL + size WARN)
    - 04-CONTEXT.md D-A5-01 (auto-title WithoutCancel+WithTimeout+WaitGroup), D-A5-02 (ScanOrphans O_NOFOLLOW/Lstat guard, audit-only size WARN)
    - 04-PATTERNS.md L183-211 (orphan_scan.go) + L215-237 (title.go) + Pattern-5 worker (RESEARCH L267-278) + Pitfall 3 (goleak)
  </read_first>
  <action>
    Create `internal/conversations/orphan_scan.go`: `ScanOrphans(ctx, pool, runDir) error` run at boot after db.Open, before serving (called from the 04-05 composition root). Walk `$AURA_RUN_DIR/conversations/*`; for each dir whose name (== conversation_id, D-26) has no matching `conversations` row, `os.RemoveAll` it — but guard the walk with `O_NOFOLLOW`/`Lstat` so a malicious symlink cannot redirect RemoveAll outside runDir (D-A5-02; beyond validateID's string check). Sweep `$AURA_RUN_DIR/tmp/*` older than 24h. Compute dir size and slog WARN if `> AURA_RUN_DIR_WARN_THRESHOLD_BYTES` (default 1 GiB) — audit-only, NEVER auto-purge. rm failures → WARN (recovered next boot). Wrap errors `%w`.
    Create `internal/conversations/title.go`: `generateTitle(ctx, client, history) (string, error)` — the best-effort LLM call producing a 4-6 word title. The WaitGroup is owned by the Runner (04-05), not here; this file exposes the body the worker calls. Errors NEVER block chat. (The fire/WithoutCancel/WithTimeout/WaitGroup wiring lives in the Runner per D-A5-01.)
    Create `orphan_scan_test.go` (db_integration): a stray `conversations/<orphan>/` dir with no DB row is removed by ScanOrphans; a dir WITH a matching row is preserved; a symlink pointing outside runDir is NOT followed (no external deletion); size over threshold logs WARN but does not purge. Create `title_test.go` (unit, agenttest.FakeClient): generateTitle returns a title on success; a fake-error client returns an error (caller leaves title NULL, no crash).
  </action>
  <verify>
    <automated>go build ./internal/conversations/ && go vet ./internal/conversations/ && go test ./internal/conversations/ && go test -race ./internal/conversations/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/conversations/` and `-race` exit 0; goleak clean
    - db_integration: a `conversations/<orphan>/` dir with no DB row is gone after ScanOrphans; a dir with a live row survives
    - symlink-escape test: a symlink under conversations/ pointing at an external dir is NOT followed; the external target still exists after ScanOrphans
    - size-WARN test: a dir exceeding the threshold logs a WARN and is NOT purged (audit-only)
    - title test: success returns a non-empty title; fake-error returns an error and the caller does not panic (title stays NULL → renders `(untitled ...)`)
    - both files ≤ 600 LOC
  </acceptance_criteria>
  <done>ScanOrphans reconciles orphan sidecar dirs under a symlink-escape guard with audit-only size WARN; generateTitle is a best-effort body the Runner drives, never blocking chat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| turn content → sidecar filesystem | conversation_id/seq derived path crosses to disk |
| boot scan → filesystem RemoveAll | a symlink in the run dir could redirect deletion outside it |
| FTS query string → Postgres | user search input crosses as a bound param |
| L1 rewrite → cached prompt prefix | mutating the wrong turn poisons the provider KV cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | path traversal via conversation_id/seq in the sidecar path | mitigate | reuse validateID (rejects `..` + separators) before filepath.Join (Task 1, T-03-07 prior art) |
| T-04-14 | Tampering/EoP | symlink escape in boot orphan RemoveAll | mitigate | O_NOFOLLOW/Lstat guard on the walk so a malicious symlink cannot redirect RemoveAll outside runDir (D-A5-02, Task 3) |
| T-04-15 | Tampering | SQL injection in FTS | mitigate | sqlc parameterized `content % $1`; never string-concat (Task 1) |
| T-04-16 | DoS / cost | KV-cache poisoning via L1 mutating cached prefix | mitigate | L1 touches only role='tool' turns older than the window, NEVER seq=1 (Pitfall 1, Task 2) |
| T-04-17 | DoS | auto-title goroutine leak | mitigate | worker body here; WaitGroup join owned by Runner (04-05); goleak sync point at Runner.Stop (Pitfall 3) |
| T-04-18 | DoS | unbounded run-dir growth | accept (audit-only) | size WARN logs but never auto-purges (D-A5-02); operator-driven cleanup is out of scope this phase |
</threat_model>

<verification>
- Unit: `go test -race ./internal/conversations/` green; goleak clean
- db_integration (Wave-3 merge, WSL stack up): `go test -tags db_integration -race ./internal/conversations/ -count=1` green (SC-2 atomicity, byte-identical LoadHistory, sidecar spill, FTS order, delete cascade, orphan scan, SC-1)
- `golangci-lint run ./internal/conversations/` == 0; every file ≤ 600 LOC
- Mutation (WSL, phase gate): ≥70% killed on context.go
</verification>

<success_criteria>
- AppendTurn atomic (SC-2); LoadHistory byte-identical (Req#8); token/USD aggregated; sidecar spill works
- L1/L2/L2.5 ladder bounds history (L1-first per SC-1); never poisons seq=1; tiktoken cached+offline
- ScanOrphans symlink-guarded GC + audit-only size WARN; generateTitle best-effort, never blocks chat
- SearchConversationTurns is the locked cross-slice query
</success_criteria>

<output>
Create `.planning/phases/04-hitl-identity-conversations/04-04-SUMMARY.md` when done
</output>
