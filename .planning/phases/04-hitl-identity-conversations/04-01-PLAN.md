---
phase: 04-hitl-identity-conversations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - go.mod
  - go.sum
  - internal/config/config.go
  - internal/llm/config.go
  - internal/llm/prices.go
  - internal/db/tx.go
  - internal/db/migrations/0003_paused_states.up.sql
  - internal/db/migrations/0003_paused_states.down.sql
  - internal/db/migrations/0004_identity.up.sql
  - internal/db/migrations/0004_identity.down.sql
  - internal/db/migrations/0005_conversations.up.sql
  - internal/db/migrations/0005_conversations.down.sql
  - internal/db/migrations/0006_conversation_turns_fts.up.sql
  - internal/db/migrations/0006_conversation_turns_fts.down.sql
  - internal/db/queries/paused_states.sql
  - internal/db/queries/identity.sql
  - internal/db/queries/capability_grants.sql
  - internal/db/queries/conversations.sql
  - internal/db/queries/conversation_turns.sql
  - internal/db/queries/context_rot_events.sql
  - internal/db/sqlc/db.go
  - internal/db/sqlc/models.go
  - internal/db/sqlc/querier.go
  - internal/db/sqlc/paused_states.sql.go
  - internal/db/sqlc/identity.sql.go
  - internal/db/sqlc/capability_grants.sql.go
  - internal/db/sqlc/conversations.sql.go
  - internal/db/sqlc/conversation_turns.sql.go
  - internal/db/sqlc/context_rot_events.sql.go
  - internal/db/migrate_test.go
autonomous: false
requirements: [CORE-02, CORE-03, CORE-04, CORE-05]
user_setup: []

must_haves:
  truths:
    - "aura db migrate applies 0003 through 0006 cleanly on a fresh DB; re-run reports no pending migrations"
    - "Fresh DB seeds identity 'local' (fixed UUID 00000000-0000-0000-0000-000000000001) + one (local,'*') capability grant"
    - "aura_app is denied DDL; only aura_migrate runs migrations"
    - "sqlc-generated Querier exposes the Phase-4 query surface (paused_states/identity/capability_grants/conversations/conversation_turns/context_rot_events)"
    - "db.WithTx commits on success, rolls back on error, re-panics on panic"
    - "L2 budget formula has real config inputs: ContextWindow + MaxOutputTokens"
  artifacts:
    - path: "internal/db/tx.go"
      provides: "Shared WithTx(ctx, pool, fn) atomic-write helper"
      contains: "func WithTx"
    - path: "internal/db/migrations/0006_conversation_turns_fts.up.sql"
      provides: "pg_trgm extension + GIN CONCURRENTLY index (isolated per golang-migrate tx rule)"
      contains: "CREATE INDEX CONCURRENTLY"
    - path: "internal/db/sqlc/querier.go"
      provides: "regenerated Querier interface with Phase-4 queries"
      contains: "SearchConversationTurns"
    - path: "internal/llm/config.go"
      provides: "ContextWindow + MaxOutputTokens fields for L2 budget"
      contains: "ContextWindow"
  key_links:
    - from: "internal/db/migrations/0005_conversations.up.sql"
      to: "aura.paused_states"
      via: "ALTER conversation_id to uuid + FK conversations(id) ON DELETE CASCADE"
      pattern: "ALTER TABLE aura.paused_states"
    - from: "internal/db/queries/conversation_turns.sql"
      to: "pg_trgm similarity"
      via: "content % $1 ORDER BY similarity(content,$1) DESC"
      pattern: "similarity\\(content"
---

<objective>
Lay the shared Phase-4 substrate every later plan depends on: the PRD-amendment commit (AM-01/AM-02/AM-03), the one new dependency (tiktoken-go, gated by a human-verify checkpoint), the config inputs the L2 budget formula needs (resolving OPEN QUESTION 2), the four migrations `0003`–`0006` (resolving the FTS migration landmine and the `conversation_spillover` non-table question, OPEN QUESTION 3), the six per-table sqlc query files, the regenerated sqlc package, and the `db.WithTx` atomic-write helper.

Purpose: Nothing in 04-02..04-05 can compile or run integration tests until the migrations, generated query surface, `db.WithTx`, and config fields exist. This is the foundation wave.
Output: PRD amendments committed; `tiktoken-go` added; `ContextWindow`/`MaxOutputTokens` + four `AURA_*` env vars wired; migrations `0003`–`0006` (up/down) applying cleanly; `internal/db/tx.go`; six query files; regenerated `internal/db/sqlc/*`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hitl-identity-conversations/04-SPEC.md
@.planning/phases/04-hitl-identity-conversations/04-CONTEXT.md
@.planning/phases/04-hitl-identity-conversations/04-PATTERNS.md
@.planning/phases/04-hitl-identity-conversations/04-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify tiktoken-go package legitimacy before go get (T-04-SC)</name>
  <what-built>Nothing yet — this gates the single new external dependency. `github.com/pkoukk/tiktoken-go@v0.1.8` is tagged [ASSUMED] in the RESEARCH Package Legitimacy Audit (slopcheck could not be run this session). Per the package-legitimacy gate, the `go get` must be human-verified before it runs.</what-built>
  <how-to-verify>
    1. Open https://pkg.go.dev/github.com/pkoukk/tiktoken-go and confirm the module exists, has 8 tagged releases through v0.1.8, and the source repo is github.com/pkoukk/tiktoken-go (real, not a slopsquat).
    2. Confirm the repo is a legitimate cl100k_base BPE port (README references OpenAI tiktoken).
    3. Note the offline-vocab concern (RESEARCH Assumption A6): at first use it may try to download the cl100k_base `.tiktoken` vocab over the network. Confirm you accept adding the dep; the encoder-offline configuration is handled in 04-04.
  </how-to-verify>
  <action>Blocking package-legitimacy gate (never auto-approvable — workflow.auto_advance is ignored). Do NOT run `go get github.com/pkoukk/tiktoken-go@v0.1.8` until the operator types "approved". Present the verification steps above, pause, and on approval allow Task 2 to run the `go get`. If the operator rejects, fall back only to the char/4 heuristic (a CONTEXT D-A2-06 deviation) and record it in the SUMMARY.</action>
  <verify>Operator response captured: "approved" -> proceed to Task 2; otherwise halt and record the decision.</verify>
  <done>Operator has explicitly approved (or rejected) adding tiktoken-go; the decision is recorded before any `go get` runs.</done>
  <resume-signal>Type "approved" to allow `go get github.com/pkoukk/tiktoken-go@v0.1.8`, or describe an alternative (the only fallback is a char/4 heuristic, which deviates from CONTEXT D-A2-06).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: PRD amendments AM-01/AM-02/AM-03 + add tiktoken-go + config inputs (resolve OPEN QUESTION 2)</name>
  <read_first>
    - prd.md §Slice 1.5 / 1.7 / 1.8 / 1.8.5 (the sections being amended)
    - .planning/phases/04-hitl-identity-conversations/04-CONTEXT.md (AM-01, AM-02, AM-03 amendment text; OPEN QUESTION 2)
    - internal/llm/config.go:54-65 (Config struct) and :196-254 (4-tier env-override pattern — the exact analog, PATTERNS L367-391)
    - internal/config/config.go:8 (header "No new fields land here without an owning slice plan") and :135-145 (envIntDefault analog, PATTERNS L394-414)
    - go.mod (confirm no cobra/tiktoken present)
  </read_first>
  <action>
    Amend prd.md in one edit pass for the three CONTEXT amendments, then add the dependency and config fields:
    - AM-01: note that only the pause-DETECTION half (`llm_agent_pause.go`) lives in the agent; `LoadHistory` is `conversations.Store.LoadHistory`; the Runner seeds the agent via the existing `LlmAgentConfig.UserTurns`. The agent stays DB-free.
    - AM-02: `aura.paused_states.resumed_answer` stores `{action: accept|decline|cancel, content}` (MCP three-action), not plain text.
    - AM-03: SPEC's `Loop.Turn`/`Loop.Stop` are `Runner.Turn`/`Runner.Stop` (avoid collision with `internal/agent/workflow.LoopAgent`).
    Run `go get github.com/pkoukk/tiktoken-go@v0.1.8` (only after Task 1 approval) so it lands in go.mod/go.sum.
    Resolve OPEN QUESTION 2: add `ContextWindow int` and `MaxOutputTokens int` to `internal/llm.Config`, wired through the same 4-tier load order (fileConfig + overlayFile + applyEnvOverrides) reading `AURA_MODEL_CONTEXT_WINDOW` / `AURA_MODEL_MAX_OUTPUT_TOKENS` via the fail-fast `envInt` helper; default `ContextWindow` to the ~1M DeepSeek-V4 window and `MaxOutputTokens` to a sensible cap (document the chosen defaults). Add the four conversation env vars to `internal/config.Config` + `Load()` via `envIntDefault` (non-fatal fallback): `AURA_CONVERSATION_TURN_CAP_BYTES`=65536, `AURA_CONTEXT_TOOL_EVICT_AFTER_TURNS`=10, `AURA_HISTORY_HARD_CAP_TURNS`=50, `AURA_RUN_DIR_WARN_THRESHOLD_BYTES`=1073741824. All names follow `AURA_<DOMAIN>_<UNIT>`.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && grep -q "ContextWindow" internal/llm/config.go && grep -q "AURA_CONVERSATION_TURN_CAP_BYTES" internal/config/config.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` and `go vet ./...` exit 0
    - `grep AM-01 prd.md`, `grep AM-02 prd.md`, `grep AM-03 prd.md` (or the amendment prose) each return a match
    - `grep -q "github.com/pkoukk/tiktoken-go" go.mod` returns 0
    - `internal/llm.Config` has `ContextWindow` and `MaxOutputTokens`; setting `AURA_MODEL_CONTEXT_WINDOW=12345` and reloading yields `ContextWindow==12345`; malformed value fails fast (returns an error, mirroring `envInt`)
    - `internal/config.Load()` returns the four new fields with the documented defaults when env is unset
  </acceptance_criteria>
  <done>PRD carries the three amendments; tiktoken-go is in go.mod; L2 budget inputs and the four conversation env vars exist with documented defaults.</done>
</task>

<task type="auto">
  <name>Task 3: db.WithTx helper + migrations 0003–0006 + 6 query files + sqlc regen (resolve OPEN QUESTION 3, FTS landmine, FIFO tiebreaker)</name>
  <read_first>
    - internal/db/sqlc/db.go:14-32 (DBTX interface; `New(DBTX)`; `(*Queries).WithTx(pgx.Tx)` — pgx.Tx satisfies DBTX, PATTERNS L52-61)
    - internal/db/migrations/0001_init.up.sql:22-33 (ALTER DEFAULT PRIVILEGES granting aura_app DML on future tables; the NEVER-grant-TRUNCATE/DROP/CREATE rule)
    - internal/db/migrations/0002_knowledge_migrations.up.sql:4-17 + .down.sql (table+grant+index+DROP pattern, PATTERNS L454-463)
    - internal/db/queries/knowledge_migrations.sql:1-8 (sqlc annotation pattern: :exec / :many / :one, PATTERNS L422-429)
    - internal/db/migrate_test.go (TestMigrate_Idempotent at ~71-94 — the analog to extend for 0003-0006)
    - sqlc.yaml (emit_interface: true, sql_package pgx/v5, schema = migrations dir)
    - 04-SPEC.md Req#3,#5,#7,#10,#13,#14 (exact column lists for every table) and Constraints L115 (paused_states.conversation_id is text in 0003, uuid+FK only in 0005)
    - 04-RESEARCH.md Pitfall 4 (FIFO tiebreaker), Pitfall 5 (pgtype.Numeric/Text), Pitfall 6 (CONCURRENTLY tx landmine), Pitfall 7 (default privileges), Code Examples L385-411 (FTS query + identity seed)
    - 04-CONTEXT.md OPEN QUESTION 3 (no conversation_spillover table — sidecar file + content_sidecar_path column only)
  </read_first>
  <action>
    Create `internal/db/tx.go` with `WithTx(ctx context.Context, pool *pgxpool.Pool, fn func(*sqlc.Queries) error) (err error)` — Begin, `defer` that rolls back on error, rolls back + re-panics on panic, commits on success; pass `sqlc.New(tx)` to `fn` (RESEARCH Pattern-2 verbatim). This is the single DRY seam reused by `conversations.Store.AppendTurn`, `askuser.Store.MarkResumedBatch`, and Stop auto-resolve.
    Write the four migration up/down pairs as `aura_migrate`-run DDL (explicit `GRANT SELECT,INSERT,UPDATE,DELETE ... TO aura_app` forensic belt-and-suspenders; NEVER grant TRUNCATE/DROP/CREATE):
    - `0003_paused_states`: `aura.paused_states (token uuid pk, conversation_id text [NO FK here], kind text CHECK IN (clarification,approval,choice), question text, options jsonb, priority int CHECK 0-100, resume_context jsonb, tool_call_id text, proxied_from_child_id uuid null, proxied_tool_call_id text null, created_at timestamptz default now(), resumed_at timestamptz null, resumed_answer jsonb null)`; partial index `(conversation_id, resumed_at) WHERE resumed_at IS NULL`. Resolve FIFO determinism (Pitfall 4 / Assumption A4): rely on `token ASC` as a total-order tiebreaker for `ORDER BY priority DESC, created_at ASC, token ASC` in the query layer (no schema change needed beyond the uuid pk).
    - `0004_identity`: `aura.identities (id uuid pk, name text unique not null, kind text CHECK IN (system,user,channel,service), created_at)` + `aura.capability_grants (identity_id uuid REFERENCES aura.identities(id) ON DELETE CASCADE, capability text, granted_at, pk(identity_id,capability))`; seed identity `local` fixed UUID `00000000-0000-0000-0000-000000000001` kind `system` + `(...001,'*')` grant, both `ON CONFLICT DO NOTHING`.
    - `0005_conversations`: `aura.conversations (id uuid pk, title text null, identity_id uuid FK, created_at, last_active_at, status text CHECK IN (active,archived,deleted), model text, total_input_tokens/total_output_tokens/total_cached_tokens bigint default 0, total_cost_usd numeric(10,4) default 0, metadata jsonb)` + `aura.conversation_turns (conversation_id uuid REFERENCES conversations(id) ON DELETE CASCADE, seq int, role text CHECK IN (system,user,assistant,tool), content text null, content_sidecar_path text null, tool_call_id text null, tool_calls jsonb null, created_at, input_tokens/output_tokens/cached_tokens int, pk(conversation_id,seq))`; in the SAME migration ALTER `aura.paused_states.conversation_id` to `uuid` + add FK `conversations(id) ON DELETE CASCADE`; create `aura.context_rot_events (ts timestamptz default now(), conversation_id uuid, action text, pairs_dropped int, tokens_before int, tokens_after int)`. NO `conversation_spillover` table (OPEN QUESTION 3: spillover is a sidecar FILE via `content_sidecar_path`; follow SPEC over CORE-04 prose).
    - `0006_conversation_turns_fts`: isolate `CREATE INDEX CONCURRENTLY conversation_turns_content_trgm ON aura.conversation_turns USING GIN (content gin_trgm_ops)` as the SOLE statement in its file (golang-migrate v4.19.1 tx-wraps each file; CONCURRENTLY cannot run in a tx — Pitfall 6). Put `CREATE EXTENSION IF NOT EXISTS pg_trgm` as a separate concern: either a preceding migration file (e.g. fold the EXTENSION into `0005`, or a dedicated single-statement file before the index) so the CONCURRENTLY index file stays single-statement. Verify the exact golang-migrate single-statement heuristic at build time; down migration drops the index (and extension if you created it standalone), same tx constraint applies.
    Create the six query files in `internal/db/queries/` with sqlc annotations: `paused_states.sql` (InsertPausedState/GetByToken/ListPending[ORDER BY priority DESC, created_at ASC, token ASC]/MarkResumed/MarkResumedBatch/CleanupResumedOlderThan/AutoResolveForConversation), `identity.sql` + `capability_grants.sql` (one or two files — discretion; CreateIdentity/GetIdentityByName/ListIdentities/DeleteIdentity + GrantCapability/RevokeCapability/ListCapabilities/HasCapability lookup), `conversations.sql` (Create/Get/List[--archived]/UpdateStatus/Rename/SetTitleIfNull/UpdateAggregates/Delete), `conversation_turns.sql` (InsertTurn/ListTurnsBySeq/SearchConversationTurns with the LOCKED `content % $1 ORDER BY similarity(content,$1) DESC LIMIT $2`), `context_rot_events.sql` (InsertContextRotEvent). Run `sqlc generate`; do NOT hand-edit generated files. Inspect generated `models.go` for `pgtype.Numeric`/`pgtype.Text`/`pgtype.Timestamptz` on numeric/nullable columns (Pitfall 5) — downstream Stores convert at the boundary.
    Extend `internal/db/migrate_test.go` so the idempotent-apply test covers 0003→0006 (4 applied on fresh, 0 on re-run).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && grep -c "CREATE INDEX CONCURRENTLY" internal/db/migrations/0006_conversation_turns_fts.up.sql && grep -rL "conversation_spillover" internal/db/migrations/ >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` and `go vet ./...` exit 0 (proves sqlc regen compiles)
    - `internal/db/sqlc/querier.go` contains `SearchConversationTurns` and the paused_states/identity/conversations method set
    - 0006 up file: `CREATE INDEX CONCURRENTLY` appears exactly once and is the only DDL statement in that file (extension lives elsewhere)
    - `grep -r conversation_spillover internal/db/` returns empty (no table; SPEC sidecar approach)
    - `grep -r timed_out internal/db/migrations/` returns empty (Req#4)
    - 0003 `paused_states.conversation_id` is `text`; 0005 ALTERs it to `uuid` + FK `ON DELETE CASCADE`
    - db_integration (run in Wave-1 merge): `aura db migrate` applies 4 migrations on a fresh DB, re-run applies 0; `aura.capability_grants` has exactly one `(00000000-0000-0000-0000-000000000001,'*')` row; DDL attempted as `aura_app` is denied
    - `internal/db/migrate_test.go` idempotent test extended through 0006 and passes
  </acceptance_criteria>
  <done>db.WithTx exists; migrations 0003-0006 apply cleanly and idempotently with role separation; six query files generate a compiling sqlc surface; FTS CONCURRENTLY index isolated; no spillover table; FIFO total-order tiebreaker locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → Postgres | DDL crosses here; must run only as aura_migrate |
| runtime (aura_app) → Postgres | DML only; aura_app must be denied DDL/TRUNCATE |
| sqlc query params → Postgres | user-derived values (FTS query, capability names) cross as bound params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | aura_app performing DDL | mitigate | Migrations run as aura_migrate only; aura_app gets DML grants via 0001 default privileges; NEVER grant TRUNCATE/DROP/CREATE (Task 3); db_integration role-separation assertion (DDL as aura_app → denied) |
| T-04-02 | Tampering | SQL injection in FTS / capability queries | mitigate | All queries are sqlc-generated parameterized `$1` (FTS `content % $1`); no string-concat of user input (Task 3) |
| T-04-03 | Tampering | DSN/password leak in migration logs | mitigate | Existing redactDSN discipline; migrations never log credentials (reuse internal/db patterns) |
| T-04-SC | Tampering | tiktoken-go supply chain ([ASSUMED]) | mitigate | Blocking human-verify checkpoint (Task 1) before go get; verify origin on pkg.go.dev; never auto-approved |
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean
- Wave-1 merge (WSL, stack up): `go test -tags db_integration -race ./internal/db/... -count=1` green; migrate 0003→0006 applies 4 / re-run 0; aura_app DDL denied; seed row present
- `golangci-lint run ./internal/db/...` == 0; every touched file ≤ 600 LOC
- `grep -r timed_out internal/db/` empty; `grep -r conversation_spillover internal/db/` empty
</verification>

<success_criteria>
- PRD carries AM-01/AM-02/AM-03; tiktoken-go added behind an approved checkpoint
- `ContextWindow`/`MaxOutputTokens` + four `AURA_*` conversation env vars exist with documented defaults
- `db.WithTx` atomic helper present and reused-ready
- Migrations 0003-0006 apply cleanly + idempotently as aura_migrate, denied as aura_app, seed local/'*'
- sqlc regenerated; Querier exposes the Phase-4 surface including the locked FTS query
- No `conversation_spillover` table; FTS CONCURRENTLY index isolated; FIFO `token ASC` tiebreaker
</success_criteria>

<output>
Create `.planning/phases/04-hitl-identity-conversations/04-01-SUMMARY.md` when done
</output>
