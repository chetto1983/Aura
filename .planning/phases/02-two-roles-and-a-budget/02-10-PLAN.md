---
phase: 02-two-roles-and-a-budget
plan: 10
type: execute
wave: 7
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06", "02-07", "02-08", "02-09"]
files_modified:
  - scripts/critical_mutation_gate.py
  - scripts/critical_mutation_gate_test.py
  - cmd/aura/two_role_credit_live_test.go
  - .planning/phases/02-two-roles-and-a-budget/02-LIVE-RUN-EVIDENCE.md
  - docs/aura-quality-snapshot.md
autonomous: false
requirements: [REL-06]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low
# Inside the smart zone. Most of the cost is the live run itself and the honest recording of what
# it did and did not show — Phase 1's equivalent (01-06) measured ~4h across 3 tasks and 17 files.
# calibration: factor 1, sample_count 0 -> tokens == raw_tokens and confidence: low is derived
# from the sample count.

must_haves:
  truths:
    - "`mutation-report.json` shows ≥70% killed for `gateway`, `identity_isolation`, `profile_validation`, `sandbox` and `frontend` — the five REL-06 names — AND for the two scopes this phase adds, `capability_policy` and `credit_policy`, so criterion 8's actual wording (\"the refusal branches this phase adds are provably killed\") is measured rather than assumed."
    - "The `GO_SCOPES` extension is the recorded resolution of the blocking decision `02-VALIDATION.md` raised: option 1, taken in plan `02-02` and implemented here. `make critical-mutation` passing is not evidence for criterion 8 without it, because none of the four pre-existing scope files is touched by this phase."
    - "Every refusal test asserts the refusal REASON, not merely that an error is non-nil. A mutant that swaps one refusal sentinel for another survives an `err != nil` assertion, and those are exactly the mutants a ≥70% score on a refusal file has to kill."
    - "The closing live run is driven as two real identities against a live `aura serve`, with A administrative and B an ordinary user, and is scored ≥9.8 — not a unit suite, per E2E-05 and the ROADMAP's own closing paragraph."
    - "B installs an MCP server, writes a skill, runs a shell command and approves a destructive tool call, and all four succeed — proving RBAC-03's uniform grant is real and not a claim about a table."
    - "B attempts to create an identity and to remove one; both are refused, and both refusals are read back out of the audit trail by B's own query — the RBAC-10 read-back, not an inspection of the database."
    - "A creates a third identity and removes it, both through the cockpit rather than by curl, and the reverse saga is observed to land on every plane: Postgres rows, ArcadeDB database, Garage bucket, Authula account, sandbox container and OpenRouter key."
    - "B is set to a zero cap and its next turn is refused before the model is called; A gives B credit and B's next turn runs; B's spend appears in the cockpit against B and not against A."
    - "The run records what it does NOT demonstrate, in its own section, as Phase 1's `01-LIVE-RUN-EVIDENCE.md` does — including the two claims no tier can settle: real provider-side cap enforcement beyond the 2026-09-08 measurement, and the provider's retention of a deleted key's consumption."
    - "The phase adds no package-manager install and no new dependency in any manifest: `git diff --exit-code -- go.mod go.sum web/package.json web/package-lock.json` against the phase's base commit is clean, which is why `02-RESEARCH.md` carries no Package Legitimacy Audit table and why threat `T-02-SC` is dispositioned accept across every plan."
  artifacts:
    - .planning/phases/02-two-roles-and-a-budget/02-LIVE-RUN-EVIDENCE.md
    - cmd/aura/two_role_credit_live_test.go
    - scripts/critical_mutation_gate.py
  key_links:
    - "internal/identity/capability_policy.go -> GO_SCOPES capability_policy -> mutation-report.json"
    - "internal/identitykey/policy.go -> GO_SCOPES credit_policy -> mutation-report.json"
    - "cmd/aura/two_role_credit_live_test.go -> a live aura serve with two provisioned identities -> 02-LIVE-RUN-EVIDENCE.md"
  prohibitions:
    - "A phase must never be reported as closed on the strength of a green unit suite."
    - "A measurement's perimeter must be stated alongside its number; a figure without what it does not cover is a supposition wearing a decimal point."
    - "Removing an identity must never be described to a human as erasing everything about them when the provider keeps its own record of what they spent."
---

<objective>
Close the phase the way this milestone closes every phase: measure the refusal branches this phase
added with mutation testing that actually points at them, then drive the whole permission-and-credit
matrix as two real identities against a live stack and write down what happened — including what
it did not show.

Purpose: `02-VALIDATION.md` raised a blocking finding — `scripts/critical_mutation_gate.py` scores
four fixed files, none of which this phase touches, so `make critical-mutation` would pass
unchanged while criterion 8 went entirely unmeasured. Plan `02-02` recorded the decision (option 1,
extend `GO_SCOPES`) and shaped two pure policy files to be single-file-mutable for it. This plan
implements the extension and then runs the phase's own closing evidence.

Output: two new mutation scopes with a passing report, a committed live-run harness, and
`02-LIVE-RUN-EVIDENCE.md` in the shape Phase 1 established.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-two-roles-and-a-budget/02-CONTEXT.md
@.planning/phases/02-two-roles-and-a-budget/02-VALIDATION.md
@.planning/phases/01-two-identities-live-and-separated/01-LIVE-RUN-EVIDENCE.md
@.planning/phases/02-two-roles-and-a-budget/02-09-SUMMARY.md
@CLAUDE.md
</context>

<planner_assumptions>
Flagged, unresolved — surfaced rather than auto-resolved:

- **REL-06 / `unclassified` edge probe.** The deterministic edge probe classified REL-06 as
  `unclassified` ("review manually") and it stays `unresolved` here rather than being
  auto-resolved with a backstop. The planner's reading is that the unprobed edge is the gap
  between REL-06's literal wording — "≥70% killed for gateway, identity, profile, sandbox and
  frontend", a fixed five-name list — and criterion 8's wording, "the refusal branches *this
  phase adds* are provably killed". Extending `GO_SCOPES` satisfies the second and leaves the
  first's five names measured as before. Whether a seventh and eighth scope should also appear in
  `REQUIREMENTS.md`'s REL-06 text, so a future phase inherits the widened contract rather than
  the original five, is a requirements question this plan does not decide. Recorded for
  `/gsd-verify-work`.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Point the mutation gate at the branches this phase actually added</name>
  <files>scripts/critical_mutation_gate.py, scripts/critical_mutation_gate_test.py</files>
  <read_first>
    - scripts/critical_mutation_gate.py — the whole file: `GO_SCOPES` at :17-22, `SUMMARY`'s regex over go-mutesting's output, `parse_go_mutation_output`'s killed/survived arithmetic and its cross-check against the reported score, `parse_frontend_report`'s Stryker path and its max-age rule, and how the report is assembled and bound to `candidate_commit`
    - scripts/critical_mutation_gate_test.py — the existing unit tests; the two new scopes need coverage there, and `Makefile:213` runs this file before the gate itself
    - Makefile lines 212-215 — the `critical-mutation` target's recipe
    - .github/workflows/ci.yml around :1585-1595 — the CI job that runs the test then the gate, and the artifact it uploads
    - internal/identity/capability_policy.go — plan `02-02`'s pure file, shaped for exactly this
    - internal/identitykey/policy.go — plan `02-05`'s pure file, same
    - .planning/phases/02-two-roles-and-a-budget/02-VALIDATION.md §Blocking Decision — the finding and the two options, so the SUMMARY can record which was taken against what it was weighed
    - CLAUDE.md §Quality tooling — go-mutesting runs in WSL only (the only fork supporting go1.26), `~/go/bin` is not on the login-shell PATH, and PASS means killed while FAIL means survived
  </read_first>
  <acceptance_criteria>
    - Source: `scripts/critical_mutation_gate.py`'s `GO_SCOPES` contains `"capability_policy": "internal/identity/capability_policy.go"` and `"credit_policy": "internal/identitykey/policy.go"`, alongside the four pre-existing entries, none removed.
    - Source: `scripts/critical_mutation_gate_test.py` covers the two new scopes — a scope whose file is missing must fail the gate rather than be skipped, and there is a test asserting that.
    - Behavior: `PYTHONPATH=scripts python3 -m unittest scripts/critical_mutation_gate_test.py` exits 0.
    - Behavior: `make critical-mutation` produces a `mutation-report.json` containing seven scopes, each with its own killed/survived counts and its own score; no scope's score is an average of others.
    - Behavior: `capability_policy` and `credit_policy` each score ≥70% killed. If either does not, add the assertions that kill the surviving mutants and re-run — do not lower the floor and do not remove the scope.
    - Behavior: the four pre-existing scopes still score ≥70%; adding scopes must not have been achieved by loosening the parser.
  </acceptance_criteria>
  <action>
    Add the two scopes to `GO_SCOPES` and cover them in `critical_mutation_gate_test.py`.

    Run the gate in **WSL**, not on the Windows host: CLAUDE.md records that go-mutesting's only
    fork supporting go1.26 lives there, and that the login shell does not put `~/go/bin` on PATH —
    prepend `$HOME/.local/bin:$HOME/go/bin` or invoke by full path. Reading `PASS` as killed and
    `FAIL` as survived is also recorded there and is the inverse of what the words suggest.

    Expect surviving mutants on first run and expect them to be instructive. `02-RESEARCH.md` Q6
    says exactly which kind: "the refusal branches (zero cap, ungrantable admin capability,
    last-admin protection) are exactly the kind of branch that is covered but not killed, so they
    need assertions on the refusal *reason*, not merely on the error being non-nil." A test that
    asserts `err != nil` leaves alive every mutant that swaps one refusal for another. Both policy
    files were written with distinct sentinels per refusal precisely so those mutants are killable
    — if a mutant survives, the fix is an assertion in the test, never a change to the policy and
    never a lowered floor.

    Do not remove or repoint any of the four existing scopes. `identity_isolation` in particular
    stays on `internal/identityctx/operator.go`: it measures no-principal attribution, a different
    boundary from capability grants, and `02-VALIDATION.md` flags the name similarity as a trap
    for anyone who assumes rather than reads.

    Record in the SUMMARY: the two option-1-versus-option-2 alternatives from
    `02-VALIDATION.md`, which was taken, and the seven measured scores. `02-VALIDATION.md`'s own
    sign-off list has an unticked box for this decision; the SUMMARY is what lets
    `/gsd-validate-phase` tick it.

    If either new scope's score cannot reach 70% because the file is genuinely too small to
    generate a meaningful mutant population, say so with the mutant count rather than declaring
    victory — a score computed over three mutants is not a measurement, and go-mutesting's own
    summary reports the total.
  </action>
  <verify>
    <automated>PYTHONPATH=scripts python3 -m unittest scripts/critical_mutation_gate_test.py</automated>
    <fails_when>non-zero exit; or the output reports "Ran 0 tests" — an empty unittest run exits 0 and proves nothing</fails_when>
    <automated>python3 -c "import sys; src=open('scripts/critical_mutation_gate.py').read(); need=['capability_policy','credit_policy','gateway','identity_isolation','profile_validation','sandbox']; missing=[n for n in need if n not in src]; print('missing:',missing); sys.exit(1 if missing else 0)"</automated>
    <fails_when>non-zero exit — a scope name is absent; a missing pre-existing name means the extension removed one, a missing new name means it was never added</fails_when>
    <automated>make critical-mutation</automated>
    <fails_when>non-zero exit; or `mutation-report.json` reports fewer than 7 scopes; or any scope's `score_percent` is below 70; or a scope's killed+survived total is under 10, which makes its percentage a coincidence rather than a measurement</fails_when>
    <automated>git diff --exit-code -- go.mod go.sum web/package.json web/package-lock.json</automated>
    <fails_when>non-zero exit — a dependency was added somewhere in this phase; the phase-wide invariant is that no package-manager install happens, which is why no Package Legitimacy Audit was required</fails_when>
  </verify>
  <reversibility rating="reversible">Two entries in a gate script; removing them removes a measurement.</reversibility>
  <done>The mutation gate scores the two files where this phase's refusal branches actually live, both at ≥70% killed with a mutant population big enough to mean something, the four pre-existing scopes are untouched and still passing, and the decision `02-VALIDATION.md` blocked on is recorded with its alternatives.</done>
</task>

<task type="auto">
  <name>Task 2: A committed harness that drives the whole matrix as two real identities</name>
  <files>cmd/aura/two_role_credit_live_test.go</files>
  <read_first>
    - .planning/phases/01-two-identities-live-and-separated/01-06-SUMMARY.md and 01-LIVE-RUN-EVIDENCE.md — the shape Phase 1 established: a committed harness, blocking machine checks, a recorded rubric, and a section for what the run does not demonstrate
    - cmd/aura/two_identity_e2e_test.go and cmd/aura/two_role_tracer_e2e_test.go — the existing live-path tests; this harness extends that lineage rather than starting a third
    - cmd/aura/two_identity_provision_harness_test.go — the fake `TelegramMint` and live-pool composition helper; reuse it
    - internal/dbtest/live_target_guard.go — the guard refusing a `db_integration` DSN named `aura` outside CI; do not weaken it
    - .planning/ROADMAP.md §Phase 2 "Closes on (live run)" — the exact sequence this harness drives, read verbatim; it is the acceptance definition, not a summary of one
    - .planning/phases/02-two-roles-and-a-budget/02-VALIDATION.md §Manual-Only Verifications — the four things this run confirms by observation rather than by assertion, and why each is manual
    - Makefile around the `musr-e2e` target — the tag set and the bring-up this harness's tags must be a subset of
  </read_first>
  <behavior>
    - `TestTwoRolesAndABudgetLiveMatrix` (tags matching the `musr-e2e` set), with subtests in the ROADMAP's own order:
      - `member_can_do_everything_else`: B installs an MCP server, authors a skill, runs a sandbox shell command and approves a destructive tool call. All four succeed.
      - `member_cannot_administer`: B attempts identity creation and identity removal; both are refused with 403.
      - `refusals_are_readable_by_the_refused`: B queries its own audit trail and both refusals are present, with capability, route and time — B's own query, not an admin's and not a database inspection.
      - `admin_creates_and_removes_through_the_cockpit`: A creates a third identity and removes it. The harness asserts the machine-checkable half (every plane torn down, including the OpenRouter key verified 404); the "through the cockpit rather than by curl" half is the human's observation, recorded in the evidence file.
      - `zero_cap_refuses_before_the_model`: B at a zero cap has its next turn refused with the `credit_exhausted` code, and no request reaches the provider.
      - `credit_restores_the_turn`: A gives B credit; B's next turn runs. The measured ~25s propagation (M-06) is expected and is waited for explicitly rather than flaked around.
      - `spend_lands_against_the_right_identity`: B's spend appears against B and not against A, read from the cockpit's own endpoint.
  </behavior>
  <action>
    Write the harness as a committed test, not a session transcript — the same standard Phase 3's
    adversarial suite will be held to and the same one Phase 1 met. It must be re-runnable by an
    operator on demand.

    Reuse Phase 1's provisioning harness and its fake `TelegramMint`. Do not weaken
    `internal/dbtest`'s live-target guard: the run uses a disposable database, and the guard
    exists because a test that migrates the operator's live `aura` database is a worse failure
    than a test that refuses to run.

    Per CLAUDE.md's NO-SKIP-AS-GREEN rule, every environment variable this harness needs must
    `t.Fatal` when unset and `$CI` is set, and skip only locally. Use the existing helper.

    The 25-second top-up propagation is real (M-06, measured 2026-09-08) and must be handled
    honestly: wait for it with a bounded poll and assert the transition, rather than sleeping a
    fixed interval and hoping, and rather than retrying until it passes. A test that retries until
    green cannot distinguish a slow provider from a broken one.

    Two subtests assert an absence and need a positive control, or they are vacuous:
    `zero_cap_refuses_before_the_model` asserts no request reached the provider — count requests
    against a transport, and assert the count is non-zero in the `credit_restores_the_turn` case,
    so "zero requests" is proven to be a real observation rather than a harness that was not
    wired. `refusals_are_readable_by_the_refused` asserts two rows are present — assert also that
    an unrelated identity's query does not return them, so a query that returns everything cannot
    pass.

    Do not add a new `make` target if `make musr-e2e`'s tag set already selects this file. Read
    the target and confirm; if it does not, say so in the SUMMARY rather than adding a parallel
    target that CI does not run.

    **Do not fix product bugs inside this task without a commit of their own.** The run is
    expected to break — that is what a closing run is for. Each break gets its own atomic commit
    with a message explaining why, per CLAUDE.md's commit discipline, and the evidence file
    records the sequence.
  </action>
  <verify>
    <automated>go vet -tags 'db_integration garage_integration authula_integration musr_e2e arcadedb_integration' ./cmd/aura/</automated>
    <fails_when>non-zero exit — the harness does not compile under its own tag set, which a plain `go vet ./...` would not have caught</fails_when>
    <automated>go test -race -count=1 -p 1 -tags 'db_integration garage_integration authula_integration musr_e2e arcadedb_integration' -run 'TestTwoRolesAndABudgetLiveMatrix' -v ./cmd/aura/</automated>
    <fails_when>non-zero exit; or "no tests to run" appears (the tag set did not select the file); or fewer than 7 "--- PASS: TestTwoRolesAndABudgetLiveMatrix/" subtest lines appear; or the whole run completes in under thirty seconds, which given the measured ~25s top-up propagation alone is proof it did not reach the provider</fails_when>
    <automated>grep -cE 't\.Skip\(' cmd/aura/two_role_credit_live_test.go</automated>
    <fails_when>the count is above 0 — a bare skip was written instead of the shared helper that t.Fatals under $CI, which is how a tier becomes falsely green in the pipeline</fails_when>
    <automated>bash scripts/check-file-size.sh &amp;&amp; go vet ./... &amp;&amp; go build ./...</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Source: `cmd/aura/two_role_credit_live_test.go`'s build-tag line is a subset of the tag set `make musr-e2e` selects, and the SUMMARY says which.
    - Source: the file contains no bare `t.Skip(` outside a helper that `t.Fatal`s when `os.Getenv("CI")` is set.
    - Source: the zero-cap subtest counts provider requests against a transport and asserts zero; the credit-restored subtest asserts the same counter is non-zero — the positive control that makes the absence meaningful.
    - Source: the audit read-back subtest asserts both that B's two refusals are present and that a different identity's query does not return them.
    - Behavior: the tagged run prints seven subtest PASS lines and takes longer than thirty seconds.
    - Behavior: any product bug the run exposes is fixed in its own atomic commit with a body explaining why, and the commits are listed in the evidence file.
  </acceptance_criteria>
  <reversibility rating="reversible">A committed test.</reversibility>
  <done>The whole permission-and-credit matrix runs as two real identities against a live stack, from a committed harness an operator can re-run, with the two absence-assertions carrying positive controls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: the scored live run, and what it did not show</name>
  <what-built>
    Everything this phase produces, wired and green: the retired wildcard and the single
    capability declaration; the ungrantable administrative pair and the last-admin protection;
    the capability-denial ledger and its audit-feed leg; the per-identity encrypted OpenRouter
    key, minted at a zero cap at provisioning and revoked-and-verified on removal; the seam-A
    resolver with no deployment-key fallback anywhere in the tree; the credit-exhausted refusal
    that fires before the network; the admin removal and credit routes; the cockpit's roster,
    removal dialog, credit panel and spend overview; and the widened money columns the ledger
    needed. Task 2's committed harness (`TestTwoRolesAndABudgetLiveMatrix`) is green against the
    live stack, and Task 1's mutation gate is green across seven scopes.

    What is NOT automated, and is why this checkpoint exists: the ROADMAP's closing run is scored
    by a human against a rubric, is driven through the cockpit rather than by curl for the
    creation-and-removal leg, and requires the reverse saga to be *observed* landing on every
    plane. `.planning/config.json` sets `human_verify_mode: end-of-phase`, and this is that batch.
  </what-built>
  <how-to-verify>
    Drive the run as a human, with the harness green, and score it. Phase 1's
    `01-LIVE-RUN-EVIDENCE.md` is the shape to fill; read it first.

    **What to do, in the ROADMAP's own order:**
    1. Bring the stack up with two identities live from Phase 1 — A administrative, B an ordinary
       user.
    2. As B, through the real agent: install an MCP server, write a skill, run a shell command in
       the sandbox, approve a destructive tool call. All four should succeed.
    3. As B: try to create an identity, and try to remove one. Both should be refused. Then, as B,
       query the audit trail and find both refusals — B's own query, not an admin's.
    4. As A, **through the cockpit and not by curl**: create a third identity, then remove it with
       the typed-email confirmation. Watch the removal land on every plane — Postgres rows,
       ArcadeDB database, Garage bucket, Authula account, sandbox container, OpenRouter key.
    5. As A, set B's cap to zero. As B, take a turn: it should be refused before the model is
       called, with the credit-exhausted copy and not a raw provider 403.
    6. As A, give B credit. Watch the cockpit say it takes about 25 seconds. Then, as B, take a
       turn: it should run.
    7. In the cockpit, check B's spend appears against B and not against A.
    8. Score the run ≥9.8 against a written rubric, as Phase 1 did.

    **What to write down, in `02-LIVE-RUN-EVIDENCE.md`:** the run, the per-assertion verdict for
    the blocking half, the rubric rows, any accumulated test debris (disclosed, not hidden), and —
    the section that matters most — **what this run does NOT demonstrate.** At minimum:
    - Real provider-side cap enforcement beyond the 2026-09-08 measurement (M-04: `limit: 0` →
      HTTP 403 `Key limit exceeded`). CI has no real key by design (`ci.yml` sets
      `OPENROUTER_API_KEY: ci-degraded-no-network`).
    - That the provider retains a deleted key's consumption in its analytics. This is a negative
      claim about a third party's data retention; `02-OPENROUTER-API.md` records it under "Not
      measured — do not assume" and it stays `[ASSUMED]`, stated in the removal dialog's copy,
      never `[VERIFIED]`.
    - What the agent loop does when a provider 403 arrives mid-turn — plan `02-05` recorded
      today's behaviour by reading the code, and did not change it.
    - That the embedding, multimodal, asset and voice clients were not converted to per-identity
      credentials by this phase (plan `02-05`'s stated open item).

    **Then run the full gate:** `make quality-full`, `make critical-mutation`, `make web-quality`,
    `make musr-e2e`. Record each result. A red gate is the phase's work, not a reason to write the
    evidence file anyway.
  </how-to-verify>
  <resume-signal>Reply "approved" with the rubric score, or describe which leg failed and what it did — a failing leg resumes at step 2 of the sequence, not at the leg that failed</resume-signal>
  <recovery>
    If a leg fails, fix it in its own atomic commit and re-run the whole sequence from step 2 —
    not just the failing leg. A matrix is only meaningful end to end, and Phase 1 recorded its
    first non-scored run honestly rather than discarding it; do the same here.

    If a leg cannot be completed at all — a plane that will not tear down, a refusal that cannot
    be read back — stop and record it as a blocker rather than narrowing the run's definition to
    fit what worked.
  </recovery>
  <done>`02-LIVE-RUN-EVIDENCE.md` exists, carries a rubric score ≥9.8, records every gate's result, and has a "What this run does NOT demonstrate" section naming at least the four items above.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measurements, and only the measurements</name>
  <files>docs/aura-quality-snapshot.md</files>
  <read_first>
    - docs/aura-quality-snapshot.md — the whole file: it is a historical measurement ledger, and CLAUDE.md is explicit that a row is updated only when its metric is actually measured, invalidated, superseded or retired. Read the existing rows' shape and their dating convention before adding one
    - CLAUDE.md §Behavioral rules "QUALITY SNAPSHOT IS MEASUREMENT-DRIVEN, NOT A DIFF GATE" — the rule that forbids bumping a date or prepending a metric-neutral re-attestation just because a path glob matched
    - .planning/phases/02-two-roles-and-a-budget/02-LIVE-RUN-EVIDENCE.md — the checkpoint's output; the numbers recorded here come from there, not from a second measurement
  </read_first>
  <acceptance_criteria>
    - Source: `docs/aura-quality-snapshot.md` carries a row for each metric this phase actually measured — the seven mutation scores, the coverage figure from `make coverage`, and the frontend coverage figure — each with the date it was measured.
    - Source: no existing row's date is bumped and no metric-neutral re-attestation is prepended. A row untouched by this phase's measurements is left exactly as it was.
    - Source: every number added here appears identically in `02-LIVE-RUN-EVIDENCE.md` or in a plan SUMMARY — the snapshot restates measurements, it does not produce them.
    - Behavior: `git diff docs/aura-quality-snapshot.md` shows only added rows and rows whose metric genuinely changed.
  </acceptance_criteria>
  <action>
    Add the measured rows and nothing else.

    The temptation this task exists to resist is documented in CLAUDE.md as an explicit rule: the
    snapshot is a measurement ledger, not a changelog, and a date bumped because a phase touched a
    matching path is a false attestation. If a metric was not measured in this phase, its row is
    not touched.

    Numbers come from where they were measured: the seven mutation scores from Task 1's
    `mutation-report.json`, the aggregate and package-local coverage from `make coverage` (which
    now includes two new `mode: target` packages, `internal/identitykey` and
    `internal/openrouterprovision`, both at the full 85% floor), and the frontend coverage from
    the wave runs. Copy them; do not re-derive them, because a second derivation that disagrees
    with the first is a third measurement nobody asked for.

    Note in the SUMMARY whether the aggregate coverage moved and in which direction, and whether
    any package on the `baseline` list — `internal/approvalgrants` at 4/57 and `internal/webauth`
    at 187/360 — was touched by this phase. Neither was expected to be: RBAC-07's last-admin
    refusal landed in `internal/identity` (`mode: target`, full 85%) rather than in
    `approvalgrants`, which `.planning/STATE.md`'s Blockers section predicted this phase would meet.
    If that prediction turned out wrong, say so — it is a standing concern the next phase inherits.
  </action>
  <verify>
    <automated>git diff --stat -- docs/aura-quality-snapshot.md</automated>
    <fails_when>the diff shows modifications to rows whose metric this phase did not measure — a date bumped or a row re-attested without a new measurement behind it, which CLAUDE.md names as a prohibited pattern</fails_when>
    <automated>make coverage</automated>
    <fails_when>non-zero exit — the owned-surface aggregate fell below 85%, or a package-local policy entry regressed, or the two new packages are unclassified in `scripts/coverage_package_policy.json`</fails_when>
    <automated>make quality</automated>
    <fails_when>non-zero exit — the pre-push gate (deadcode, vet, file-size, lint, test-race, vuln, build) is red, so the phase is not closable regardless of what the live run showed</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation rows.</reversibility>
  <done>The ledger carries this phase's measurements with their dates, no row it did not measure was touched, and every number in it can be traced to the artifact that produced it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the live run → the operator's real deployment | The harness must use a disposable database; `internal/dbtest`'s live-target guard is the boundary |
| the phase's own claims → the closing evidence | What gets asserted versus what gets observed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-46 | Repudiation | a phase closed on unit evidence | high | mitigate | The closing evidence is a live run driven by the real agent, per E2E-05 and the ROADMAP's own closing paragraph; the harness is committed and re-runnable, and the "what this run does NOT demonstrate" section names the four claims it cannot settle (Task 2, checkpoint) |
| T-02-47 | Tampering | a vacuous absence assertion | high | mitigate | The zero-cap subtest's "no request reached the provider" carries a positive control — the same counter is asserted non-zero in the credit-restored case — so a harness that was never wired cannot pass by observing nothing (Task 2) |
| T-02-48 | Tampering | a mutation gate that measures the wrong files | high | mitigate | `GO_SCOPES` extended to the two files where this phase's refusal branches land, with a mutant-population floor so a percentage over three mutants is not accepted as a measurement (Task 1) |
| T-02-49 | Repudiation | a snapshot row attested without a measurement | medium | mitigate | Only measured rows are touched; `git diff --stat` is a verify command and CLAUDE.md names the prohibited pattern explicitly (Task 3) |
| T-02-50 | Tampering | the live run against the operator's real database | high | mitigate | `internal/dbtest`'s live-target guard refuses a `db_integration` DSN named `aura` outside CI; the harness reuses it rather than bypassing it, as Phase 1's plan 01-01 established |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | `git diff --exit-code -- go.mod go.sum web/package.json web/package-lock.json` is a verify command in Task 1, closing the phase-wide invariant every other plan dispositioned as accept. No package-manager install occurs anywhere in this phase, which is why `02-RESEARCH.md` carries no Package Legitimacy Audit table |
</threat_model>

## Artifacts this phase produces

<artifacts_this_phase_produces>
- `scripts/critical_mutation_gate.py` — `GO_SCOPES` entries `capability_policy` and `credit_policy`
- `scripts/critical_mutation_gate_test.py` — coverage for the two new scopes
- File `cmd/aura/two_role_credit_live_test.go` (new) — `TestTwoRolesAndABudgetLiveMatrix` and its seven subtests
- File `.planning/phases/02-two-roles-and-a-budget/02-LIVE-RUN-EVIDENCE.md` (new)
- `docs/aura-quality-snapshot.md` — measured rows for the seven mutation scores and the coverage figures
</artifacts_this_phase_produces>

<edge_coverage_accounting>
This plan authors **0** of the 54 probe items and **surfaces 1 as a flagged assumption**: REL-06,
classified `unclassified` by the probe and therefore left `unresolved` rather than auto-resolved
with a backstop. Phase total: 48 authored + 6 flagged = 54.
</edge_coverage_accounting>

<verification>
- `PYTHONPATH=scripts python3 -m unittest scripts/critical_mutation_gate_test.py` green
- `make critical-mutation` green with seven scopes, each ≥70%
- `make quality-full` green
- `make web-quality` green
- `make musr-e2e` green
- The tagged live-matrix run green with seven subtests, taking longer than thirty seconds
- `02-LIVE-RUN-EVIDENCE.md` scored ≥9.8 with its "does NOT demonstrate" section filled
- `git diff --exit-code -- go.mod go.sum web/package.json web/package-lock.json` clean
</verification>

<success_criteria>
The refusal branches this phase added are provably killed rather than merely covered; the whole
permission-and-credit matrix has been driven as two real identities against a live stack and
scored; and the record says what the run showed, what it broke, and what it could not settle.
</success_criteria>

<output>
Create `.planning/phases/02-two-roles-and-a-budget/02-10-SUMMARY.md` when done. Record the seven
mutation scores with their mutant populations, the REL-06 option taken against the alternative,
every gate's result, the atomic commits any live-run breakage required, and whether
`internal/approvalgrants` or `internal/webauth` were touched after all — `.planning/STATE.md`
predicted this phase would meet both.
</output>
