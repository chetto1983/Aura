---
phase: 15-memory-subsystem
plan: 05
type: execute
wave: 2
depends_on: ["15-02", "15-03", "15-04"]
files_modified:
  - internal/agent/mcptools/memory_integration_test.go
  - cmd/aura/memory_integration_test.go
  - internal/agent/memory_recall_integration_test.go
  - docs/aura-quality-snapshot.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [UX-08, UX-09]
must_haves:
  truths:
    - "A live `memory_integration` tier mounts the running sidecar and asserts all 16 tools mount Deferred + `memory__*` (D-06/D-07)"
    - "A live test drives the agent recall path tool_search → memory__memory_search → text_response (spike 035, D-03 / re-scoped UX-09)"
    - "A live test seeds via `aura memory` and reads it back, and round-trips a reasoning trace (D-06 reasoning tools / re-scoped UX-09)"
    - "A genuinely new entity writes with dedup action=none (the provenance-safe-dedup fix survived the rebuild, D-10)"
    - "`scripts/cache_invariant_audit.sh` still passes UNCHANGED — no messages[2] stream was added (D-04)"
    - "Recall@5 / p95 are appended to docs/aura-quality-snapshot.md as an advisory snapshot (re-scoped UX-08, amendment #20 gate)"
  artifacts:
    - path: "internal/agent/mcptools/memory_integration_test.go"
      provides: "live 16-tool Deferred + memory__* mount assertion (port of spike 032)"
      contains: "memory_integration"
    - path: "cmd/aura/memory_integration_test.go"
      provides: "live `aura memory` seed/read + reasoning-trace round-trip (port of spikes 033/035)"
      contains: "memory_integration"
    - path: "internal/agent/memory_recall_integration_test.go"
      provides: "live agent loop recall path tool_search → memory__memory_search → text_response"
      contains: "memory_integration"
    - path: "docs/aura-quality-snapshot.md"
      provides: "advisory recall@5/p95 memory snapshot (amendment #20 gate)"
      contains: "memory"
  key_links:
    - from: "memory_integration tests"
      to: "AURA_AGENT_MEMORY_MCP_URL (live sidecar)"
      via: "mcp.OpenServer against the running rebuilt image"
      pattern: "AURA_AGENT_MEMORY_MCP_URL"
    - from: ".github/workflows/ci.yml"
      to: "memory_integration tier"
      via: "export AURA_AGENT_MEMORY_MCP_URL + stack up so the tier runs (no-skip-as-green)"
      pattern: "memory_integration"
---

<objective>
Prove the wiring against the live, rebuilt sidecar and close the validation surface: a
`memory_integration` build-tag tier (16-tool mount, `aura memory` seed/read, agent recall
loop, reasoning-trace round-trip, dedup `action=none`), the KV-cache invariant confirmation
(D-04 — run the existing audit, assert unchanged), and the UX-08 advisory recall@5/p95 snapshot.
This plan satisfies D-03 (pull-on-demand recall, proven in the loop), D-04 (cache invariant
holds trivially — confirmed, no new code), D-06/D-07 (16-tool Deferred + namespaced — asserted
live), D-10 (single-user scope + dedup `action=none`), re-scoped UX-08 (advisory snapshot), and
re-scoped UX-09 (agent-written reasoning/insight recalled on demand — the reasoning-trace tools
round-trip).

Purpose: The wiring plans (15-02/15-03) assert structure with unit tests; this plan asserts the
real recall path end-to-end against the running image (the spike-032/033/035 paths, re-run under
project gates), confirms the KV-cache invariant did NOT regress (D-04 — the load-bearing claim
that pull-on-demand never touches the cacheable prefix), and records the advisory quality
snapshot (amendment #20 gate: the snapshot file MUST be updated on a P15 PR). Tests do NOT
re-measure the package's internal memory behavior — they assert Aura's WIRING + the recall path
(the package's 2443 tests + TCK own the engine).

Output: three `memory_integration`-tagged live tests, a CI job export + stack-up so the tier
RUNS (never skip-greens), an asserted `cache_invariant_audit.sh` pass, and the advisory snapshot.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-memory-subsystem/15-CONTEXT.md
@.planning/phases/15-memory-subsystem/15-RESEARCH.md
@.planning/phases/15-memory-subsystem/15-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `memory_integration` tier — live 16-tool mount + `aura memory` seed/read + reasoning-trace + dedup, with CI wiring</name>
  <files>internal/agent/mcptools/memory_integration_test.go, cmd/aura/memory_integration_test.go, .github/workflows/ci.yml</files>
  <read_first>
    - internal/mcp/calculator_integration_test.go (the exact build-tag-gated live-MCP tier shape: `//go:build calculator_integration`, env-gated, Open → ListTools → CallTool)
    - .planning/spikes/032-agent-memory-mcp-live-mount/README.md (the 16-tool ground-truth list + the all-Deferred + `memory__*` assertion; expects len(mounted)==len(rawNames))
    - .planning/spikes/033-agent-memory-write-read-ground-truth/README.md (the seed/read ground-truth + dedup action=none for a new entity)
    - internal/agent/mcptools/mount.go (MountManagedServer HTTP branch — what mount returns; bridge.go Deferred + namespacedName for the `memory__*` + Deferred assertions)
    - cmd/aura/memory.go (the verbs to seed/read with, from plan 15-03)
    - .github/workflows/ci.yml (the existing integration-tier jobs — how db/neo4j/calculator tiers export their env + `t.Fatal`-under-$CI pattern; the no-skip-as-green gate per CLAUDE.md)
    - .planning/phases/15-memory-subsystem/15-RESEARCH.md "Common Pitfalls" 2/3/5 + "Open Questions" 3/4 (provenance scope key decision; memory_get_facts absence)
    - .planning/phases/15-memory-subsystem/15-PATTERNS.md "Integration tier memory_integration" section
  </read_first>
  <action>
    Add `//go:build memory_integration` tests (a NEW tag in the per-tier family — do NOT overload
    db_integration/neo4j_integration). Gate every test on `os.Getenv("AURA_AGENT_MEMORY_MCP_URL")`:
    when empty AND `$CI` is set → `t.Fatal` (no-skip-as-green, CLAUDE.md); when empty locally →
    `t.Skip`. Tests:
    (a) `internal/agent/mcptools/memory_integration_test.go` — `TestMemoryLiveMount`: build a
    `tools.Registry`, `MountManagedServer(ctx, reg, "memory", <streamable_http ManagedServer at
    AURA_AGENT_MEMORY_MCP_URL>)`, assert the mounted count equals the live `tools/list` count
    (the 16 from spike 032), every mounted spec is `Deferred`, and every registered name is
    `memory__*`. Do NOT apply any DenyRisk filter (Pitfall 2 — full 16-tool surface, D-06).
    (b) `cmd/aura/memory_integration_test.go` — `TestMemoryCLI`: drive `runMemoryCommand` to
    `add-entity` a uniquely-tagged entity (e.g. `AURA-P15-IT-<rand>`), then `search` for it and
    assert the tag comes back (seed+read, spike 033); `TestMemoryReasoningTrace`: round-trip
    `trace start` → `trace step` → `trace complete` then recall via `search`/`trace observations`
    and assert the trace content is retrievable (Aura asserts WIRING + recall, not the package's
    trace semantics — D-06/re-scoped UX-09); `TestMemoryDedupNewEntityActionNone`: write a
    genuinely-new uniquely-named entity and assert the result reports dedup `action=none` /
    `similarity_score=0.0` (the provenance-safe-dedup fix survived the rebuild, D-10 / Pitfall 5
    re-validation). Decide explicitly (Open Q3) that operator writes are unscoped / carry
    `user_id=aura-local` for cross-conversation same-user merge (D-10 intent) and note that choice
    in a test comment. Use `graph_query` or `memory_search` for fact-reads (no standalone
    `memory_get_facts`, Open Q4).
    (c) `.github/workflows/ci.yml` — add `memory_integration` to the tag matrix of the integration
    job: bring the stack up (the `aura-agent-memory-mcp` service built by plan 15-04 + neo4j +
    aura-llama-embed), export `AURA_AGENT_MEMORY_MCP_URL=http://127.0.0.1:8091/mcp/`, and run
    `go test -tags memory_integration ./internal/agent/mcptools/ ./cmd/aura/`. The job MUST export
    the URL so the tier RUNS under $CI (a skipped tier = a failing gate, never a pass — CLAUDE.md
    no-skip-as-green). Keep `OPENROUTER_API_KEY` optional via the existing placeholder default.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet -tags memory_integration ./internal/agent/mcptools/ ./cmd/aura/ && go test -tags memory_integration -run 'MemoryLiveMount|MemoryCLI|MemoryReasoningTrace|MemoryDedup' ./internal/agent/mcptools/ ./cmd/aura/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags memory_integration` (stack up, AURA_AGENT_MEMORY_MCP_URL set) passes all four tests
    - `TestMemoryLiveMount`: mounted count == live tools/list count; all Deferred; all names `memory__*`; NO DenyRisk filter
    - `TestMemoryCLI`: a uniquely-tagged entity seeded via `aura memory add-entity` is returned by `aura memory search`
    - `TestMemoryReasoningTrace`: a trace round-trips and is recallable
    - `TestMemoryDedupNewEntityActionNone`: a new entity reports dedup `action=none`
    - With `AURA_AGENT_MEMORY_MCP_URL` unset under `$CI`, the tests `t.Fatal` (not skip)
    - ci.yml exports `AURA_AGENT_MEMORY_MCP_URL` + brings the stack up so the tier runs
  </acceptance_criteria>
  <done>The live tier proves the 16-tool surface, the `aura memory` seed/read, the reasoning-trace round-trip, and dedup action=none — and CI runs it for real.</done>
</task>

<task type="auto">
  <name>Task 2: Agent loop recall test + KV-cache invariant confirmation (D-04) + UX-08 advisory snapshot</name>
  <files>internal/agent/memory_recall_integration_test.go, docs/aura-quality-snapshot.md</files>
  <read_first>
    - .planning/spikes/035-agent-memory-loop-recall/README.md (the real LlmAgent loop path: tool_search → memory__memory_search → text_response; uses agenttest.FakeClient)
    - internal/agent/agenttest/fakeclient.go (FakeClient/NewFakeClient/FakeTurn — how to script the model turns that call tool_search then memory__memory_search then text_response)
    - scripts/cache_invariant_audit.sh (the existing 22-hash messages[0]/messages[1]/skillman audit — RUN it, do NOT edit it; D-04 anti-pattern: do NOT add a messages[2] stream)
    - docs/aura-quality-snapshot.md (the existing snapshot format/sections to append a memory advisory block to; amendment #20 gate requires this file updated on a P15 PR)
    - .planning/phases/15-memory-subsystem/15-RESEARCH.md Validation Architecture rows for "Agent recall path", "KV-cache invariant unchanged", "Recall@5 / p95"
    - .planning/phases/15-memory-subsystem/15-CONTEXT.md D-03/D-04 + Claude's Discretion (cache audit, no user input)
  </read_first>
  <action>
    (a) Create `internal/agent/memory_recall_integration_test.go` (`//go:build memory_integration`,
    same env-gate + `t.Fatal`-under-$CI rule): `TestMemoryLoopRecall` builds the real registry
    with the memory recipe mounted (default-on path), seeds a uniquely-tagged memory, then runs a
    `LlmAgent` driven by `agenttest.FakeClient` scripted to call `tool_search` (finding the
    deferred `memory__memory_search`), then call `memory__memory_search`, then `text_response`;
    assert the final text contains the seeded tag — the spike-035 path under project gates (D-03 /
    re-scoped UX-09). This asserts the recall WIRING, not the model's judgment.
    (b) KV-cache invariant (D-04, Claude's Discretion — no user input): run
    `scripts/cache_invariant_audit.sh` (Postgres-free) and assert it still prints the 22 identical
    `messages[0]`/`messages[1]`/`skillman` hashes UNCHANGED. Because pull-on-demand never touches
    the cacheable prefix, the invariant holds trivially; the gate is that NO `messages[2]` insight
    stream was added (anti-pattern — there is no messages[2] by design). Record the audit result
    in the SUMMARY (run-only confirmation; do not edit the script or add a messages[2] block).
    (c) UX-08 advisory snapshot (amendment #20 gate): append a "Memory (Phase 15, agent-memory MCP)"
    section to `docs/aura-quality-snapshot.md` recording recall@5 and p95 measured against the
    PACKAGE's retrieval via `aura memory search` over a small seeded set (the package owns
    retrieval; this is an ADVISORY snapshot, NOT a hard pass/fail gate — re-scoped UX-08). Note the
    measurement method, the seed set, the date, and that it is advisory. The amendment-#20 CI gate
    only requires the file be UPDATED on the P15 PR — do not turn recall@5/p95 into a blocking
    threshold.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet -tags memory_integration ./internal/agent/ && bash scripts/cache_invariant_audit.sh && grep -qi "memory" docs/aura-quality-snapshot.md</automated>
  </verify>
  <acceptance_criteria>
    - `TestMemoryLoopRecall` (tags memory_integration, stack up) returns the seeded tag via the tool_search → memory__memory_search → text_response path
    - `scripts/cache_invariant_audit.sh` exits 0 with 22 unchanged hashes; no messages[2] stream added (D-04)
    - `docs/aura-quality-snapshot.md` has a memory advisory section with recall@5/p95 + method + date, marked advisory
    - With `AURA_AGENT_MEMORY_MCP_URL` unset under `$CI`, the recall test `t.Fatal`s (not skip)
  </acceptance_criteria>
  <done>The agent recall path is proven live, the KV-cache invariant is confirmed unchanged (D-04), and the advisory recall@5/p95 snapshot is recorded (re-scoped UX-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop → memory sidecar (MCP) | The recall test exercises untrusted memory output flowing into the agent's context (the same boundary the phase's mount widens). |
| live sidecar → Neo4j (bolt, loopback) + embedder (loopback) | The integration tier runs against the live `127.0.0.1:8091` sidecar talking to `neo4j:7687` + the embed sidecar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Integrity (HIGH) | Cross-run memory over-merge (provenance) | mitigate | `TestMemoryDedupNewEntityActionNone` asserts `action=none` / `similarity_score=0.0` for a genuinely new entity — re-validating the fork's provenance-safe-dedup fix survived the 15-04 rebuild (Pitfall 5, spike 033/034). Severity HIGH because a regression silently corrupts user memory; mitigation is the live dedup assertion in the gated tier. |
| T-15-05-02 | Tampering | Untrusted recall output reaches the agent | mitigate | The recall test runs through the existing bridge that tags results `TrustUntrusted, Source: "mcp:memory"`; the test asserts the recall path works, and the trust tagging is REUSE-as-is from 15-02. |
| T-15-05-03 | DoS / false-green | A skipped integration tier passes CI doing nothing | mitigate | The tier `t.Fatal`s under `$CI` when `AURA_AGENT_MEMORY_MCP_URL` is unset, and ci.yml exports it + brings the stack up (CLAUDE.md no-skip-as-green). A sub-second tier runtime is a skip tell — verify execution. |
| T-15-05-04 | Info disclosure | KV-cache prefix leaking memory into the cacheable prefix | mitigate | D-04: `cache_invariant_audit.sh` confirms `messages[0]`/`messages[1]`/`skillman` hashes are unchanged — pull-on-demand never injects memory into the cacheable prefix; no `messages[2]` stream exists. |
| T-15-05-05 | Info disclosure | PII in the advisory snapshot / seeded test data | accept | The snapshot records aggregate recall@5/p95 numbers + method, not user content; test seed data uses synthetic `AURA-P15-IT-*` tags, not real PII (single-user `local`, D-10). |
| T-15-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this test/doc plan; the install surface is plan 15-04 (vendored pinned fork). RESEARCH §Package Legitimacy Audit: zero new Go modules this phase. |
</threat_model>

<verification>
- `go test -tags memory_integration ./internal/agent/mcptools/ ./cmd/aura/ ./internal/agent/` green with the stack up + `AURA_AGENT_MEMORY_MCP_URL` set.
- `scripts/cache_invariant_audit.sh` exits 0 with 22 unchanged hashes (D-04).
- `docs/aura-quality-snapshot.md` updated with the advisory memory section (amendment #20 gate).
- CI runs the `memory_integration` tier for real (URL exported, stack up) — no skip-as-green.
- Owned-surface coverage floor (85%) applies to the Go wiring (catalog/config/CLI from 15-02/15-03);
  the vendored agent-memory package is NOT measured (CLAUDE.md / amendment #61) — state this in
  verify so execute/verify do not try to measure the package.
</verification>

<success_criteria>
- The live `memory_integration` tier proves: 16-tool Deferred + `memory__*` mount (D-06/D-07),
  `aura memory` seed/read, reasoning-trace round-trip (re-scoped UX-09), agent recall path
  (D-03), and dedup `action=none` (D-10).
- The KV-cache invariant is confirmed unchanged (D-04) — no `messages[2]` machinery built.
- The UX-08 advisory recall@5/p95 snapshot is appended to `docs/aura-quality-snapshot.md`.
- CI runs the tier for real; the vendored package is excluded from the coverage floor.
</success_criteria>

<artifacts_this_phase_produces>
- `internal/agent/mcptools/memory_integration_test.go` (new): `TestMemoryLiveMount` (build tag `memory_integration`).
- `cmd/aura/memory_integration_test.go` (new): `TestMemoryCLI`, `TestMemoryReasoningTrace`, `TestMemoryDedupNewEntityActionNone`.
- `internal/agent/memory_recall_integration_test.go` (new): `TestMemoryLoopRecall`.
- `.github/workflows/ci.yml`: `memory_integration` tier added to the integration job (exports `AURA_AGENT_MEMORY_MCP_URL`, stack up).
- `docs/aura-quality-snapshot.md`: new "Memory (Phase 15, agent-memory MCP)" advisory recall@5/p95 section.
- New build tag: `memory_integration`. Test-tier env var (cataloged in 15-01): `AURA_AGENT_MEMORY_MCP_URL`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/15-memory-subsystem/15-05-SUMMARY.md` when done.
</output>
