---
phase: 15-memory-subsystem
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - prd.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [UX-06, UX-07, UX-08, UX-09]
must_haves:
  truths:
    - "PRD amendment #62 records the four UX-06..09 re-scopes (D-12) and lands before any Go code"
    - "REQUIREMENTS.md UX-06..09 checkboxes are re-stated to the adopted-package scope"
    - "ROADMAP Phase 15 Success Criteria are re-derived against the agent-memory MCP surface"
    - "PRD env index catalogs AURA_AGENT_MEMORY_MCP_* (D-11 records 384d is already the live state — no migration drop)"
  artifacts:
    - path: "prd.md"
      provides: "Amendment #62 block under §Slice 11, re-scoping UX-06..09 and recording the env catalog + 384d-already-live note"
      contains: "Amendment #62"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UX-06..09 lines re-stated to the adopted scope"
      contains: "UX-06"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 Success Criteria re-derived against the MCP surface"
      contains: "Phase 15"
  key_links:
    - from: "prd.md amendment #62"
      to: ".planning/REQUIREMENTS.md UX-06..09"
      via: "matching re-scope text"
      pattern: "UX-0[6789]"
---

<objective>
Land the doc-only PRD amendment #62 that re-scopes requirements UX-06..09 to the
adopted `neo4j-labs/agent-memory` MCP surface, **before any Go code** (D-12, PRD-first
Wave-0 gate; the established P8 #44 / P9 D-23 pattern for superseding phases). This plan
satisfies D-12 and re-scopes UX-06, UX-07, UX-08, UX-09.

Purpose: The PRD is the truth-source (CLAUDE.md PRD-first, absolute). Phase 15 is an
adoption, not a build — the bespoke 11a/11b/11d/11e engine is superseded by the vendored
package (amendment #61). The four UX requirements were written for the bespoke build and
MUST be re-stated to the adopted scope before any wiring code, so executors and verifiers
plan against the real, re-scoped contract. The amendment commit MUST precede every Go
commit in this phase — `git log` ordering is the gate.

Output: `prd.md` amendment #62 block + re-stated `REQUIREMENTS.md` UX-06..09 + re-derived
`ROADMAP.md` Phase 15 Success Criteria + an `AURA_AGENT_MEMORY_MCP_*` entry in the PRD env
index. No Go files touched.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-memory-subsystem/15-CONTEXT.md
@.planning/phases/15-memory-subsystem/15-RESEARCH.md
@.planning/phases/15-memory-subsystem/15-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PRD amendment #62 (re-scope UX-06..09 + env catalog + 384d note)</name>
  <files>prd.md</files>
  <read_first>
    - prd.md §Slice 11 (the §Slice 11 header is at ~line 3660; Amendment #61 block at ~line 3662 is the exact analog format to mirror) — read ~3660-3705
    - prd.md P8 amendment #44 block (~line 2442, the sandbox→sandbox-agent supersede precedent) for the "kill bespoke, adopt off-the-shelf" amendment shape
    - .planning/phases/15-memory-subsystem/15-PATTERNS.md "PRD amendment #62" section (analog + exact D-12 contents)
    - .planning/phases/15-memory-subsystem/15-CONTEXT.md D-12 (verbatim re-scope text) and D-11 (384d)
  </read_first>
  <action>
    Add a new block-quote amendment immediately below Amendment #61 in prd.md §Slice 11,
    in the SAME `> **▶ Amendment #NN (title — context, date) — ...**` format as #61.
    Title it "Amendment #62 (Phase 15 plan — UX-06..09 re-scope to the adopted agent-memory
    MCP surface, 2026-06-11)". Record verbatim per D-12: (a) **UX-06** (document-RAG
    ingestion: file/URL → chunk → embed → entity pipeline) → DEFERRED to a future phase
    (`[DEFERRED]`); the package is conversation/entity memory, not a chunked doc-RAG engine.
    (b) **UX-07** (Leiden community detection) → already deferred (amendment #27); unchanged;
    entity resolution is now the package's responsibility (spike 034). (c) **UX-08** (hybrid
    retrieval recall@5 ≥ 0.8 / p95 ≤ 30ms) → becomes an ADVISORY snapshot measured against
    the package's retrieval and appended to `docs/aura-quality-snapshot.md`; the amendment-#20
    snapshot-update gate still applies; it is NOT an Aura-owned hard WRRF/p95 pass-fail gate.
    (d) **UX-09** (agent journal + cached `messages[2]` insight injection) → becomes
    agent-written reasoning/insight memories recalled on demand (the package's reasoning-trace
    tools); NO cached `messages[2]` injection (D-04), NO background journal cron. Tag each
    superseded bespoke item `[SUPERSEDED #61]` where it overlaps #61.
    Then add: an `AURA_AGENT_MEMORY_MCP_*` entry to the PRD §Caps & Limits env-var index
    (catalog `AURA_AGENT_MEMORY_MCP_PORT` default 8091, `AURA_AGENT_MEMORY_MCP_IMAGE` default
    `aura-agent-memory-mcp:local`, and the test-tier `AURA_AGENT_MEMORY_MCP_URL`); and an
    explicit note that 384d is ALREADY the live state (per D-11: `0001_init.cypher` is 384d,
    `DefaultEmbedDimensions=384`, granite-97m emits 384d natively) so NO 768d→384d migration
    drop is needed — the "768d legacy migrations" framing in #61 node (2) is stale and this
    amendment supersedes it. NO fenced code in the amendment prose. Cite D-11 and D-12 in the
    amendment text for traceability.
  </action>
  <verify>
    <automated>grep -q "Amendment #62" prd.md && grep -q "AURA_AGENT_MEMORY_MCP_PORT" prd.md && grep -Eq "UX-0[6789]" prd.md</automated>
  </verify>
  <acceptance_criteria>
    - `prd.md` contains a block-quote starting "▶ Amendment #62" under §Slice 11
    - The amendment text names all four UX-06, UX-07, UX-08, UX-09 with their re-scope
    - `AURA_AGENT_MEMORY_MCP_PORT`, `AURA_AGENT_MEMORY_MCP_IMAGE`, `AURA_AGENT_MEMORY_MCP_URL`
      appear in the PRD env index
    - The amendment states 384d is already the live state and no migration drop is needed (D-11)
    - The amendment cites D-11 and D-12
  </acceptance_criteria>
  <done>Amendment #62 records the four re-scopes (D-12), the env catalog, and the 384d-already-live note; cites D-11/D-12.</done>
</task>

<task type="auto">
  <name>Task 2: Re-state REQUIREMENTS.md UX-06..09 + re-derive ROADMAP Phase 15 Success Criteria</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md UX-06..09 (lines ~51-54) — the current bespoke wording to replace
    - .planning/ROADMAP.md Phase 15 (lines ~510-526) — the bespoke Success Criteria to re-derive
    - prd.md amendment #62 (just written in Task 1) — the source of the re-scope wording
    - .planning/phases/15-memory-subsystem/15-CONTEXT.md D-12
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, re-state the four checkboxes UX-06..09 (lines ~51-54)
    to the adopted scope, keeping the `UX-0N:` IDs and the `[ ]` checkbox markers intact
    (do NOT check them — re-scoped, not yet delivered): UX-06 → "DEFERRED to a future phase
    (agent-memory is conversation/entity memory, not doc-RAG); see PRD amendment #62"; UX-07 →
    "Entity resolution owned by the adopted package (spike 034); Leiden community detection
    deferred (#27)"; UX-08 → "Hybrid retrieval recall@5/p95 are ADVISORY snapshots vs the
    package, appended to docs/aura-quality-snapshot.md (amendment #20 gate)"; UX-09 →
    "Agent-written reasoning/insight memories recalled on demand via the package's
    reasoning-trace tools; no cached messages[2] injection, no journal cron". Reference
    "amendment #62" in each. In `.planning/ROADMAP.md` Phase 15, replace the bespoke
    Success Criteria list (the 5 `aura memory ingest/...` bullets) with re-derived criteria
    that match this phase's actual deliverables: (1) memory recipe is a trusted, default-on
    managed recipe — fresh machine mounts the 16 `memory__*` tools with no `aura mcp install`;
    (2) `aura memory <verb>` operator CLI round-trips against the live sidecar; (3) agent
    recall path `tool_search → memory__memory_search → text_response` returns seeded content;
    (4) `scripts/cache_invariant_audit.sh` still passes unchanged (no messages[2] stream
    added, D-04); (5) reproducible `docker compose build aura-agent-memory-mcp` from git +
    advisory recall@5/p95 appended to `docs/aura-quality-snapshot.md`. Keep the bespoke text
    as a struck-through/"superseded — design reference" note rather than deleting, matching
    how #61 retained the bespoke goal. Cite D-12.
  </action>
  <verify>
    <automated>grep -q "amendment #62" .planning/REQUIREMENTS.md && grep -Eq "default-on|advisory|reasoning/insight|DEFERRED" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md UX-06..09 lines re-state the adopted scope and reference amendment #62
    - The four `UX-0N:` IDs and `[ ]` checkbox markers are preserved (not checked)
    - ROADMAP Phase 15 Success Criteria are re-derived to match this phase's deliverables
      (default-on mount, CLI round-trip, agent recall path, cache audit, reproducible build + snapshot)
    - The bespoke ROADMAP text is retained as a superseded design reference, not deleted
  </acceptance_criteria>
  <done>UX-06..09 and the ROADMAP Phase 15 criteria are re-stated to the adopted scope, citing amendment #62 / D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → downstream executors | The amendment text becomes the contract executors plan against; a wrong re-scope mis-directs the whole phase (this is a doc-correctness boundary, not a runtime trust boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | PRD re-scope text | mitigate | Re-scope copied verbatim from D-12; Task 1 verify greps for each UX-0N + amendment marker so a dropped re-scope fails the gate. |
| T-15-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this doc-only plan; no supply-chain surface. RESEARCH §Package Legitimacy Audit confirms zero new modules this phase. |

This plan is doc-only — it introduces no runtime trust boundary, no network surface, no
secret, and no executable code. The HIGH-severity runtime threats (MCP sidecar boundary,
loopback exposure, PII/retention, default-on tool-surface widening) are owned by the wiring
plans 15-02/15-03/15-04, where their mitigations live.
</threat_model>

<verification>
- `grep "Amendment #62" prd.md` returns the amendment block.
- `git log --oneline` shows the amendment #62 commit precedes any Go-file commit in this phase
  (PRD-first ordering gate, D-12).
- The four UX-06..09 IDs are present and re-scoped in both `prd.md` and `.planning/REQUIREMENTS.md`.
</verification>

<success_criteria>
- PRD amendment #62 exists and re-scopes UX-06..09 (D-12) with the env catalog + 384d note (D-11).
- REQUIREMENTS.md UX-06..09 and ROADMAP Phase 15 Success Criteria are re-stated to the adopted scope.
- This commit lands before any Go code in the phase.
</success_criteria>

<artifacts_this_phase_produces>
- `prd.md`: new "Amendment #62" block-quote under §Slice 11; new `AURA_AGENT_MEMORY_MCP_PORT`
  / `AURA_AGENT_MEMORY_MCP_IMAGE` / `AURA_AGENT_MEMORY_MCP_URL` entries in the env index.
- `.planning/REQUIREMENTS.md`: re-stated UX-06, UX-07, UX-08, UX-09 lines.
- `.planning/ROADMAP.md`: re-derived Phase 15 Success Criteria (bespoke retained as superseded reference).
- No new Go symbols (doc-only plan).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/15-memory-subsystem/15-01-SUMMARY.md` when done.
</output>
