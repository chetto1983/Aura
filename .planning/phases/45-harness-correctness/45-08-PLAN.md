---
phase: 45-harness-correctness
plan: 08
type: execute
wave: 5
depends_on: ["45-02", "45-03", "45-04", "45-05", "45-06", "45-07"]
files_modified:
  - docker/aura/Dockerfile
  - compose.yaml
  - .planning/phases/45-harness-correctness/45-VALIDATION.md
  - docs/aura-quality-snapshot.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [ACC-01, ACC-02, HARN-06, HARN-07, HARN-08, HARN-09, MEM-04, MEM-05]

must_haves:
  truths:
    - "Every one of the twelve phase requirements is verified by a real conversation with the running Aura, scored on the answer she gave and the state she produced — a green suite is not evidence and closes no box (ACC-01). ONE requirement carries a named, bounded exception, spelled out in the third truth below: HARN-08's repair fires only on a provider-generated malformed batch and cannot be induced by asking. Every other requirement, HARN-09 included, is driven live."
    - "The live scenario exercises MEM-04 (two differently-named operator writes converging on one `Entity` vertex) and MEM-05 (a sentence-shaped object rejected with an error naming `statement`) DIRECTLY, as scenario steps f and g — not as a by-product of SC#1-#5, which cover only HARN-01/02/03/04/06/07 and ACC-02."
    - "HARN-09's same-message half IS driven live (step h1): the dedup keys on identical `(name, arguments)` in one assistant message, which is promptable in ordinary operator language, and the persisted `aura.conversation_turns.tool_calls` jsonb shows exactly one surviving call. HARN-08 is NOT drivable — its repair fires only on a provider-generated duplicate or blank `tool_call_id`, which cannot be requested and must not be hand-crafted; if no repaired id appears in the run, that is RECORDED and HARN-08 closes as unit-proven plus live-invariant-proven rather than being rounded up to 'live-verified'. The concession is HARN-08's alone and is not extended to HARN-09."
    - "The live invariant behind both is CODE-SENSITIVE, not schema-guaranteed: every `tool_call` in a persisted assistant message has exactly one matching `end` row in `aura.tool_invocations`, and every `end` row has a matching `tool_call` — the D-12 provider invariant, which fails on an orphan call or a collapsed reservation. The plan MUST NOT assert absence of duplicate `tool_call_id` within `(conversation_id, request_id, event_kind)`: `tool_invocations_once_per_phase_idx` (`0011_tool_invocations.up.sql:45-46`) guarantees that in every world, so it is unfalsifiable and evidences nothing."
    - "ACC-01's closing condition is explicit and checkable: the live scenario is driven end to end through the running stack by the real agent, scored above 9.8, and the score plus its justification are written into `45-VALIDATION.md`. A phase-close with an unscored or unrun scenario is not a close."
    - "ACC-02's evidence comes from OpenTelemetry traces (including the new `aura.tool.replayed` and `aura.tool.replay_layer` attributes), `aura.tool_invocations`, `aura.conversation_turns`, and the ArcadeDB graph itself — and no new eval harness is built; `internal/eval/` stays deleted."
    - "ACC-02's verbatim text (`REQUIREMENTS.md:183`) names a FOURTH surface, `aura.context_rot_events`, which CONTEXT.md D-22 does not. The discrepancy is resolved in writing, not silently: CONTEXT.md is authoritative on method, and `aura.context_rot_events` carries no evidence for SC#1-#5 because its columns are `action`, `pairs_dropped`, `tokens_before`, `tokens_after` (`internal/db/migrations/0005_conversations.up.sql:40-47`) — an L2.5 rolling-buffer audit, with nothing about tool execution, replay, turn honesty or memory validity. It is read once during the live run as a CONFOUND check (a drop mid-scenario means history was truncated and a model failure may not be this phase's), and that reading is recorded."
    - "Success Criterion 4 is proven by correcting the real F-1-caused misdiagnosis fact in live long-term memory — the ArcadeDB-orphan-nodes theory Aura wrote as fact off a stale replay — not by a synthetic fixture (D-23). Scope is that ONE fact; the nine compensating `learned_lesson` facts and the `always-deliver-files` skill remain Phase 54's."
    - "Success Criteria 1 and 2 are re-proved LIVE with SQL against `aura.tool_invocations`, not only at the `db_integration` tier: the cross-round re-issue shows two `event_kind='end'` rows and the retried dispatch shows one."
    - "Success Criterion 5 — operator-language replies, no leaked deliberation, no unkept stated intention — is judged on the real transcript, which is the only tier that can sample it (HARN-06, HARN-07, D-21)."
    - "The full gate is green before the phase-close push: `make quality-full` in WSL, plus the `arcadedb_integration` leg via `make agent-memory-eval`, plus a mutation spot-check at or above 70% killed on the changed key-composition and supersede files."
    - "`docs/aura-quality-snapshot.md` is re-attested for every row whose CI-gate-path glob matches a file this phase changed, and `scripts/quality_snapshot_gate.sh` prints `ok: … checked N row(s)` locally BEFORE the push."
    - "The scheduler is drained before the new key shape is deployed to the environment the live scenario runs against (D-06) — a reclaim of a pre-deploy `tool-child-v1` operation would otherwise execute once more."
    - "The container image the scenario runs against was built from the current HEAD, and that is CHECKABLE rather than asserted: `aura:local` bakes the commit SHA, so `docker run --rm aura:local aura version` prints it and it can be compared to `git rev-parse HEAD`. MEASURED 2026-08-13 that it does NOT today — `docker/aura/Dockerfile:36` builds with `-ldflags=\"-s -w\"` and no `-X main.commit=`; `.dockerignore:1` excludes `.git` so `debug.ReadBuildInfo()` yields no `vcs.revision`; `cmd/aura/version.go:16` defaults `commit` to `\"\"` and renders `unknown`; the Dockerfile has no `LABEL` and no `ARG`. `.goreleaser.yaml:31` stamps it, but that path does not build `aura:local` (`compose.yaml:13-16` builds `docker/aura/Dockerfile`). Task 1 closes that gap before Task 2 can consume it."
  artifacts:
    - path: ".planning/phases/45-harness-correctness/45-VALIDATION.md"
      provides: "The closed-out Per-Task Verification Map (23 rows, Status set), the completed Probe-Edge Ledger Verified column, the live scenario transcript reference, the score, and the mutation figures"
      contains: "nyquist_compliant"
    - path: "docs/aura-quality-snapshot.md"
      provides: "Re-attested rows for every CI-gate-path glob this phase's changed files match"
  key_links:
    - from: "the live scenario"
      to: "aura.tool_invocations"
      via: "SQL COUNT over event_kind='end' scoped by conversation_id and request_id"
      pattern: "tool_invocations"
    - from: "the live scenario"
      to: "OTel span attributes"
      via: "aura.tool.replayed / aura.tool.replay_layer read from the trace"
      pattern: "aura\\.tool\\.replay"
    - from: "the live scenario"
      to: "the ArcadeDB graph"
      via: "post-correction read proving exactly one validity window closed"
      pattern: "valid_to"
  prohibitions:
    - statement: "This phase MUST NOT be closed on a green suite. Unit, integration, race and smoke results keep the code honest; they say nothing about whether the agent behaves. Any requirement whose only evidence is a green suite is not done (ACC-01, verbatim)."
    - statement: "No new eval harness may be built to produce this evidence. `internal/eval/` was deleted because it was broken; OTel, `aura.tool_invocations` and `aura.conversation_turns` already carry what is needed (ACC-02)."
    - statement: "The live verification MUST NOT be a smoke test or a curl driven with hand-picked headers. It is a real scenario driven through the running stack by the real agent — self-authored probes test the author's assumptions, not the system."
    - statement: "No requirement may be closed on an assertion that is true by construction. Specifically, absence of duplicate `tool_call_id` within `(conversation_id, request_id, event_kind)` is enforced by a UNIQUE index and holds with or without this phase's code; asserting it and calling the result evidence would close HARN-08 and HARN-09 on a tautology, which is precisely what ACC-01 forbids."
    - statement: "The coverage gate MUST NOT be pointed at the live `aura` database. `scripts/coverage_docker.sh` provisions a disposable `aura_cov` and refuses `db_integration` against a database named `aura` when run locally; do not defeat that."
---

<objective>
Prove the phase, and close it honestly. Drive one real conversation through the running stack that
exercises all five success criteria — including correcting the specific false fact this phase's own
bug wrote into long-term memory — read the evidence from the three surfaces ACC-02 names, score it,
and only then run the phase-close bookkeeping.

Purpose: this is the requirement, not the ceremony. The audit that produced this milestone found a
replay defect that made Aura act on a stale result, report the wrong thing, and then write the
resulting misdiagnosis into her own memory as a fact. Every one of those steps happened while the
test suite was green.

Output: a scored live transcript, SQL and trace evidence for SC#1-#4, a completed validation record,
a re-attested quality snapshot, and the phase marked closed only if the score clears 9.8.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-harness-correctness/45-CONTEXT.md
@.planning/phases/45-harness-correctness/45-VALIDATION.md
@.planning/phases/45-harness-correctness/45-02-SUMMARY.md
@.planning/phases/45-harness-correctness/45-03-SUMMARY.md
@.planning/phases/45-harness-correctness/45-04-SUMMARY.md
@.planning/phases/45-harness-correctness/45-05-SUMMARY.md
@.planning/phases/45-harness-correctness/45-06-SUMMARY.md
@.planning/phases/45-harness-correctness/45-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate matrix and the mutation spot-check, before anything is driven live</name>
  <read_first>
    - Makefile (the `quality`, `quality-full`, `coverage`, `vuln`, `agent-memory-eval` targets and what each actually runs)
    - scripts/coverage_gate.sh (the `AURA_COVERAGE_TAGS` default and the owned-surface floor)
    - scripts/coverage_docker.sh (the disposable `aura_cov` provisioning and the refusal to run `db_integration` against a database named `aura`)
    - scripts/agent_memory_eval.py lines 40-80 (the `arcadedb_live` suite row and `PASS_THRESHOLD`)
    - .planning/phases/45-harness-correctness/45-VALIDATION.md (Sampling Rate, Manual-Only Verifications, Validation Sign-Off)
    - every 45-0N-SUMMARY.md from this phase (the per-plan measurements this task aggregates)
  </read_first>
  <action>
    Run the full matrix in WSL with the stack up, in this order, and record each result verbatim in
    `45-VALIDATION.md` rather than paraphrasing it:

    1. `make quality` — vet, file-size, lint (+dupl), deadcode, test-race, vuln, build.
    2. `make quality-full` — the above plus the coverage gate. Verify locally with
       `bash scripts/coverage_docker.sh` rather than pushing and waiting ~20 minutes for CI; the
       gate provisions a disposable `aura_cov` and drops it on exit. Record the owned-surface number
       and confirm it is at or above the 85% floor.
    3. `make agent-memory-eval` — the `arcadedb_live` leg, which runs
       `go test -race -tags=arcadedb_integration -coverprofile=… ./internal/arcadedb/`. Record the
       score against its `PASS_THRESHOLD`. Check the most recent `arcadedb-integration-test` CI run
       first so a pre-existing red baseline is not misread as this phase's regression.
    4. `go test -race -tags=db_integration -count=1 ./internal/gateway/` with the composed DSNs
       (`AURA_DB_URL`, `AURA_DB_MIGRATE_URL`) exported. A sub-second runtime is a skip tell — record
       the measured duration.
    5. Mutation spot-check with `go-mutesting` (WSL, `PATH=~/go/bin:$PATH`) over this phase's
       critical files: `internal/agent/idempotency_operation.go`,
       `internal/agent/llm_agent_call_dedup.go`, `internal/arcadedb/memory_supersede.go`. `PASS` is
       killed, `FAIL` is survived; score is killed/total and the floor is 70%. For files behind a
       build tag, add `GOFLAGS=-tags=…` plus the DSN env. Record killed/total per file in
       `45-VALIDATION.md`'s Manual-Only table.

    If any tier is red, STOP and report which one with its output. Do not proceed to the live
    scenario on a red gate — a live run cannot distinguish a code defect from a harness defect, and
    the whole point of ordering the gate first is to remove that ambiguity.

    6. **Make the image's provenance checkable (fix-on-touch, blocking Task 2).** Task 2's first
    precondition needs to compare the running image's build SHA against `git rev-parse HEAD`. That
    comparison is impossible today — MEASURED 2026-08-13:

    - `docker/aura/Dockerfile:36` — `go build -trimpath -ldflags="-s -w"`, no `-X main.commit=`.
    - `.dockerignore:1` excludes `.git`, so the build stage is not a VCS checkout and
      `debug.ReadBuildInfo()` returns no `vcs.revision` for `cmd/aura/version.go:48-52` to pick up.
    - `cmd/aura/version.go:16` defaults `commit` to `""`; `orUnknown` renders it as `unknown`. So
      `docker run --rm aura:local aura version` prints `commit: unknown`.
    - The Dockerfile carries no `LABEL` and no `ARG` at all.
    - `.goreleaser.yaml:28-32` DOES stamp `-X main.commit={{ .Commit }}` — but that is the release
      path; `compose.yaml:13-16` builds `aura:local` from `docker/aura/Dockerfile`.

    Close it with the smallest change that makes the fact exist. In `docker/aura/Dockerfile`, add
    `ARG VCS_REF=""` in the `build` stage and extend line 36's ldflags to
    `-ldflags="-s -w -X main.commit=${VCS_REF}"`. In `compose.yaml`, add
    `args: {VCS_REF: "${VCS_REF:-}"}` under `services.aura.build` so the value reaches the stage
    through the normal `docker compose build` path.

    This is a stamp, not a behaviour change: the binary is byte-identical apart from the embedded
    string, no runtime surface moves, and an unset `VCS_REF` leaves `commit: unknown` — honestly
    unstamped rather than wrong. Do NOT reach for `-X main.version=` or `-X main.date=`; goreleaser
    owns those on the release path and duplicating them here would create two sources of truth for
    the version. Do NOT delete `.git` from `.dockerignore` to get `vcs.revision` instead — that
    would push the whole git history into the build context on every build.

    Rebuild and confirm the stamp lands:
    `VCS_REF=$(git rev-parse HEAD) docker compose build aura` then
    `docker run --rm aura:local aura version`.

    The Per-Task Verification Map in `45-VALIDATION.md` is already POPULATED by the planner: 23
    rows, one per task across plans 45-01..45-08, each carrying its requirement, threat ref, test
    type and automated command. Do NOT rewrite it. Close it out instead:

    - Set every `Status` cell from the real run (green / red / flaky) — plan 45-02's Task 3 already
      set its own three; set the remaining rows.
    - Set every `File Exists` cell from the real tree.
    - Where a row's automated command was not the one actually run, correct that row and name the
      substitution in the SUMMARY.
    - Complete the Probe-Edge Ledger's `Verified` column in the same file: each edge is either
      asserted by a named test (give the test function name) or, for a `backstop` or
      `flagged-assumption` row, carries the written statement of why it is not asserted. A ledger row
      left blank is an unverified must-have, not a formatting gap.
  </action>
  <acceptance_criteria>
    - `make quality` and `make quality-full` both exit 0; the outputs are pasted into
      `45-VALIDATION.md`, not summarized.
    - `bash scripts/coverage_docker.sh` reports the owned-surface figure at or above 85%; the number
      is recorded with the date and the tag set it was measured on.
    - `make agent-memory-eval` meets its `PASS_THRESHOLD`; the score and the threshold are both
      recorded.
    - The `db_integration` gateway run's measured duration is recorded and is above one second.
    - Mutation figures for all three named files are recorded as killed/total with a percentage, each
      at or above 70%; any file below 70% is named with what survived and why.
    - `docker run --rm aura:local aura version` prints `commit: <40 hex>` equal to
      `git rev-parse HEAD` — not `unknown`. Paste both. This is what makes Task 2's precondition 1
      satisfiable; if this criterion is not met, Task 2 cannot start.
    - `git diff docker/aura/Dockerfile compose.yaml` shows ONLY the `ARG VCS_REF` / ldflags stamp and
      the compose `args` passthrough — no other build change rode along.
    - `45-VALIDATION.md`'s Per-Task Verification Map has no row left at `pending` Status and no
      empty `File Exists` cell across all 23 rows.
    - The Probe-Edge Ledger's `Verified` column is complete: every `explicit` row names the test
      function that asserts it; every `backstop` and `flagged-assumption` row carries its written
      justification.
  </acceptance_criteria>
  <verify>
    <automated>go vet ./... && go build ./... && go test -race ./internal/agent/ ./internal/gateway/ ./internal/arcadedb/ ./cmd/arcadedb-mcp/</automated>
  </verify>
  <done>Every automated tier is green and recorded with real numbers, and the mutation floor is met on the three files this phase's correctness rests on.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The live scenario — drive it, read the evidence, score it</name>
  <read_first>
    - .planning/phases/45-harness-correctness/45-VALIDATION.md (Success-Criterion → Evidence Map — the tier each criterion must be proved at, and the Manual-Only table)
    - .planning/ROADMAP.md §Phase 45 Success Criteria (all five, verbatim — the wording being judged)
    - .planning/REQUIREMENTS.md ACC-01 and ACC-02 verbatim (the closing rule and the permitted evidence surfaces)
    - .planning/phases/45-harness-correctness/45-CONTEXT.md §D-22, §D-23 (the evidence methodology and the specific real fact to correct)
    - every 45-0N-SUMMARY.md from plans 45-02..45-07 (what actually shipped, including the exact `replayedMarker` string and the frozen MCP field names — the scenario asserts on those literals)
    - internal/db/queries/tool_invocations.sql (the existing sqlc queries and the real column names for the SQL assertions)
    - internal/db/migrations/0011_tool_invocations.up.sql (the `event_kind` CHECK, and `tool_invocations_once_per_phase_idx` at lines 45-46 — `UNIQUE (conversation_id, request_id, tool_call_id, event_kind)`. Read it to understand why step h must NOT assert on duplicate ids: the index makes that query vacuous)
    - internal/db/migrations/0005_conversations.up.sql lines 23-36 (`aura.conversation_turns`, especially the `tool_calls` jsonb column — the surface step h1 and h2 read the persisted assistant batch from)
    - .planning/REQUIREMENTS.md MEM-04 and MEM-05 verbatim (the two requirements steps f and g exist to evidence)
    - 45-06-SUMMARY.md and 45-07-SUMMARY.md (the chosen canonical operator form and the chosen prose-rejection error text — steps f and g assert on those exact strings)
    - 45-04-SUMMARY.md (the `_d<n>` suffix shape and the `call_<12 hex>` blank-id derivation — step h matches against those literals)
  </read_first>
  <what-built>
    Round-discriminated idempotency keys (45-02), replay markers and span attributes on both replay
    layers plus a boot-time metadata guard (45-03), deterministic tool-call-id repair and
    same-message dedup (45-04), a completion critic that judges every voluntary termination and a
    system prompt that forbids leaked deliberation (45-05), exact-fact-key memory correction with an
    ambiguity refusal and a prose guard (45-06), and the model-facing surface for all of it plus
    operator-subject canonicalization (45-07). Every automated tier is green (Task 1).
  </what-built>
  <how-to-verify>
    **Preconditions — these are BLOCKING and each produces a recorded, quotable value in
    `45-VALIDATION.md` BEFORE the first message is sent.** They are acceptance criteria, not advice:
    every one of them has silently invalidated a live verification on this project before, and prose
    preconditions did not stop it. If any of the four cannot be satisfied, STOP — do not gather
    evidence against an environment you cannot name.

    1. **The image is built from current HEAD.** Task 1 step 6 made this checkable by baking the
       SHA into the binary; before it, `aura version` printed `commit: unknown` and no label existed,
       so this precondition was unsatisfiable. Record all four, verbatim:
       `git rev-parse HEAD`; `git status --porcelain` (must be EMPTY — a dirty tree means the image
       and the SHA describe different code); `docker run --rm aura:local aura version` (its
       `commit:` line MUST equal `git rev-parse HEAD`); and
       `docker image inspect aura:local --format '{{.Id}} {{.Created}}'`. If the SHA does not match,
       rebuild with `VCS_REF=$(git rev-parse HEAD) docker compose build aura` and re-record. A
       matching `Created` timestamp is NOT a substitute and must not be accepted as one — that
       fallback is exactly what T-45-36 exists to prevent.
    2. **The scheduler is drained** before the new key shape goes live (D-06). Record the drain
       command and, after it, the literal output of a query showing ZERO in-progress scheduler runs
       (`SELECT count(*) ... WHERE status IN ('in_progress', ...)` — read the scheduler's schema for
       the exact column and states rather than guessing). A pre-deploy `tool-child-v1` operation
       reclaimed after the deploy derives a new key and executes once more.
    3. **The live model is read, not assumed.** Quote the `aura.settings` row naming the model
       actually in use. Routing is DB-driven; the model in `.env`, in the compose file and in the
       last session's memory are all guesses.
    4. **`psql` on the `aura` schema and the OTel trace view are open before the first message**, so
       the evidence is read from the run rather than reconstructed after it. Record three artifacts
       proving it: the `psql` session's `SELECT now();` output taken BEFORE the first message (its
       timestamp must precede the scenario's first turn), the database and role the session is
       connected to (`SELECT current_database(), current_user;`), and the trace-view URL or the trace
       query used. "I had it open" is not recordable; a timestamp that precedes the run is.

    **Drive ONE real conversation with the real agent on the running stack.** Not a smoke test, not
    a curl with hand-picked headers. The scenario must exercise, in order:

    a. **SC#1** — ask Aura to run the same mutating command twice in one turn, changing the world in
       between (the shape of audit turns `[058]`/`[062]`: a write, then a re-read of the same target).
       Then assert with SQL: `SELECT count(*) FROM aura.tool_invocations WHERE conversation_id = …
       AND request_id = … AND event_kind = 'end' AND tool_name = …` returns **2**, and the two rows
       have distinct `tool_call_id` values and distinct `result_preview` content.
    b. **SC#2** — trigger a genuine retry or a scheduler reclaim of one operation. Assert exactly
       **one** `event_kind='end'` row for it. No duplicated side effect on disk or in the graph.
    c. **SC#3** — cause a legitimate replay. Read the tool result Aura received and confirm it
       carries the replay marker; read the `tool.execute` span and confirm `aura.tool.replayed=true`
       with `aura.tool.replay_layer` naming the correct layer.
    d. **SC#4 (D-23)** — correct the REAL misdiagnosis fact this phase's own bug produced: the
       ArcadeDB-orphan-nodes theory Aura wrote into long-term memory as fact off a stale replay. Ask
       her to recall it, confirm the recall now returns a `fact_key`, ask her to correct that one
       fact, and then read the graph directly: exactly one validity window closed, every sibling
       fact sharing the subject and predicate still open. Scope is that one fact — do not touch the
       nine compensating `learned_lesson` facts or the `always-deliver-files` skill (Phase 54).
    e. **SC#5** — across the whole scenario, judge the transcript: replies in the language the
       operator wrote in, no planning or self-critique or tool reasoning leaking into user-facing
       text, and every stated intention either ran or the turn says plainly that it did not and why.
       Include at least one turn where Aura states an intention she cannot complete, to exercise the
       widened completion critic (45-05) rather than assuming it fires.

    Steps a-e cover SC#1-#5, which between them evidence HARN-01/02/03/04/06/07 and ACC-02 — and
    NOTHING else. Three phase requirements have no evidence in that set, and ACC-01 says a
    requirement without live evidence is not done. Steps f, g and h close them, in the same
    conversation:

    f. **MEM-04** — ask Aura to record a fact about the operator using the operator's DISPLAY NAME as
       the subject, then, in a later turn, ask her to record a different fact about the operator using
       the identity UUID as the subject. Then read the graph directly: there is exactly ONE `Entity`
       vertex for the operator, carrying the canonical form plan 45-07 chose, and BOTH facts hang off
       it. Quote the vertex read and both FACT edges.
    g. **MEM-05** — ask Aura to remember something whose natural object is a whole sentence (a
       preference expressed as prose, e.g. a multi-clause statement about how the operator wants
       something done). The write must be REJECTED, and the rejection must name the `statement` field
       as where the detail belongs. Quote the error text verbatim and confirm no new `Entity` vertex
       was created by that attempt (count vertices before and after). Then confirm Aura RECOVERS —
       she re-issues the write with a short entity object and the detail moved into `statement` — so
       the error is proved actionable and not merely loud.
    h. **HARN-09's same-message half (driven), and the D-12 provider invariant (asserted).** Two
       parts, because the two requirements have genuinely different live reachability.

       **h1 — HARN-09, driven like any other step.** The same-message dedup keys on identical
       `(name, arguments)` in ONE assistant message, and that shape IS promptable through the real
       agent — ask, in ordinary operator language, for a naturally repeated action in a single turn
       ("check X, and check it again to be sure"). No hand-crafted request, no header fiddling: the
       same class of driving as steps a-g. Then read the PERSISTED assistant row for that turn from
       `aura.conversation_turns` (`role='assistant'`, the `tool_calls` jsonb column,
       `internal/db/migrations/0005_conversations.up.sql:30`):
       - If the model emitted two calls with identical `(name, arguments)`: assert the persisted
         `tool_calls` array holds exactly ONE of them — the repeat was dropped BEFORE the history
         append, so it never reached the provider — and that `aura.tool_invocations` holds exactly
         one `end` row for it. That is HARN-09's same-message half, proved live.
       - If the model emitted one call, or two with differing arguments: the induction did not
         reproduce the shape. Record that plainly and fall back; HARN-09's same-message half stays
         unit-proven, and its cross-round half is already proved live by SC#1.

       **h2 — the D-12 provider invariant, over the whole run.** Join the persisted assistant
       `tool_calls` against `aura.tool_invocations` for the run's `conversation_id` and assert BOTH
       directions: every `tool_call` in a persisted assistant message has exactly ONE matching `end`
       row, and every `end` row has a matching persisted `tool_call`. No orphan call, no execution
       without a call. This invariant is sensitive to both repairs and is the one D-12 and D-13 exist
       to protect: a duplicate dropped from the batch but left in history shows up as an orphan
       `tool_call`; a blank or colliding id collapsing two calls onto one `ReservationKey` shows up as
       a `tool_call` with no `end` row. Also record the shape check: every `tool_call_id` matching a
       repair shape conforms (`_d<n>` with n >= 2, or `call_` + exactly 12 lowercase hex), with a
       count of each.

       **Do NOT assert "no duplicate `tool_call_id` within `(conversation_id, request_id,
       event_kind)`".** MEASURED: `internal/db/migrations/0011_tool_invocations.up.sql:45-46`
       declares `CREATE UNIQUE INDEX tool_invocations_once_per_phase_idx ON
       aura.tool_invocations (conversation_id, request_id, tool_call_id, event_kind)`. That query
       returns zero in every possible world — with the repair, without it, and before this phase
       existed — so it is unfalsifiable and evidences nothing. The unrepaired symptom of a
       blank/duplicate id is a **missing** row, never a duplicate one: two such calls collapse onto
       one `ReservationKey`, which is the latent bug named in plan 45-04's own objective.

       **HARN-08 remains not live-inducible, and that is a real limitation rather than a shared
       one.** Its repair fires only on a provider-generated malformed batch — duplicate or blank
       `tool_call_id` — which cannot be requested of the model in operator language and must not be
       manufactured by hand-crafting a request. Its live evidence is h2's invariant plus the shape
       check on any repaired ids that do appear. If none appear, record that and close HARN-08 as
       unit-proven plus live-invariant-proven, naming the limitation. Do NOT extend that concession
       to HARN-09: h1 is drivable, so a missing h1 result is an unattempted step, not an inherent
       limit.

    **Score the whole scenario out of 10** on the answers she actually gave and the state she
    actually produced. The project Definition of Done is **>9.8**. Below that, the phase does not
    close: report which criterion failed, with the transcript excerpt and the query output that
    shows it.

    Paste into `45-VALIDATION.md`: every SQL query and its output, the span attribute values, the
    pre- and post-correction graph reads, the transcript excerpts backing the SC#5 judgement, and
    the score with its justification.
  </how-to-verify>
  <acceptance_criteria>
    - SC#1: a SQL query and its literal output showing 2 `end` rows for the cross-round re-issue.
    - SC#2: a SQL query and its literal output showing exactly 1 `end` row for the retried/reclaimed
      operation.
    - SC#3: the replayed tool result text containing the marker, AND the two span attribute values,
      both quoted.
    - SC#4: the pre-correction recall showing the `fact_key`, and the post-correction graph read
      showing one closed window and N-1 still-open siblings, both quoted.
    - SC#5: transcript excerpts, including the turn where an unkept intention was named plainly.
    - **Precondition 1 (blocking):** `docker image inspect aura:local --format '{{.Id}} {{.Created}}'`,
      `git rev-parse HEAD`, and the image's baked build SHA are all quoted verbatim, and the image's
      build SHA equals `git rev-parse HEAD`. A mismatch fails this task; it does not become a caveat.
    - **Precondition 2 (blocking):** the scheduler-drain command and the literal output of the
      in-progress-run count query are quoted, and the count is 0 (D-06).
    - **Precondition 3 (blocking):** the `aura.settings` row naming the live model is quoted verbatim.
    - **Precondition 4 (blocking):** the pre-first-turn `SELECT now();` and
      `SELECT current_database(), current_user;` outputs and the trace-view URL are quoted. These
      pin WHEN the run started, WHICH database the evidence queries read, and WHERE the spans were
      read from — without them a later reader cannot tell whether the SQL below was run against the
      same database and the same window as the conversation.
    - **MEM-04 (step f):** the graph read showing exactly one operator `Entity` vertex in the
      canonical form, with both the display-name-written and UUID-written facts attached — quoted.
    - **MEM-05 (step g):** the verbatim rejection text, showing it names `statement`; the
      before/after `Entity` vertex count proving none was created; and the successful re-issue.
    - **HARN-09 (step h1):** the prompt used, the persisted assistant `tool_calls` jsonb for that
      turn quoted verbatim, and either (a) the two-identical-calls case with exactly one surviving
      `tool_call` and exactly one `end` row, or (b) an explicit statement that the model did not emit
      the shape, with the jsonb showing what it did emit.
    - **D-12 invariant (step h2):** the join query and its literal output showing zero orphan
      `tool_call`s and zero `end` rows without a matching `tool_call`, plus the per-shape counts of
      repaired ids. A non-zero result on either direction FAILS this task, with ONE accepted
      exception: a `tool_call` still awaiting approval or in flight when the query ran is a correct
      state, not a defect. To claim it, scope the join to turns whose calls have all resolved, or
      quote the offending row AND the evidence that it was approval-withheld or in-flight (its
      pending-approval record, or a re-run of the same query after the call resolved showing zero).
      An unexplained non-zero result is a failure — the exception is a quoted proof, not an excuse.
    - **HARN-08 (step h2):** if zero repaired ids appeared, an explicit written statement that the
      repair did not fire live, that HARN-08 closes as unit-proven plus live-invariant-proven, and
      that this limitation is HARN-08's alone and does NOT extend to HARN-09.
    - No acceptance criterion asserts absence of duplicate `tool_call_id` within
      `(conversation_id, request_id, event_kind)` — that is guaranteed by
      `tool_invocations_once_per_phase_idx` and evidences nothing.
    - A numeric score with a written justification, and an explicit pass/fail against the >9.8 bar.
    - No new file under `internal/eval/` was created (ACC-02) — verify with `ls internal/eval` and
      record the result.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the score, or describe which success criterion failed and paste the evidence that shows it</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Quality snapshot re-attestation and phase-close bookkeeping</name>
  <read_first>
    - docs/aura-quality-snapshot.md (every row and its CI-gate-path glob)
    - scripts/quality_snapshot_gate.sh (what it checks and what `ok: … checked N row(s)` requires)
    - .planning/REQUIREMENTS.md (the twelve requirement checkboxes and the traceability table rows for Phase 45)
    - .planning/STATE.md (Current Position, progress counters, Session Continuity, Pending Todos)
    - .planning/phases/45-harness-correctness/45-VALIDATION.md (Tasks 1-2's recorded evidence, and the Validation Sign-Off checklist)
  </read_first>
  <action>
    Only after Task 2 has cleared the >9.8 bar.

    (a) Quality snapshot. Compute the changed-file set for this phase and re-attest every matching
    row, verifying LOCALLY before the push:
    `AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)"
    AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh`
    — it must print `ok: … checked N row(s)`. For each matching row, bump `Last measured` to today
    and PREPEND a re-attestation note: a fresh measurement if the metric moved, otherwise a
    metric-neutral justification naming exactly what changed and why the number cannot move. Keep
    the prior notes as `Prior …`. Do not delete history.

    (b) Requirements. Tick the twelve Phase 45 checkboxes in `.planning/REQUIREMENTS.md` and move
    their traceability rows from `Pending` to done, but ONLY for requirements whose evidence is
    actually in `45-VALIDATION.md`. A requirement with no live evidence stays open and is named in
    the SUMMARY — ACC-01 makes that the rule, not a judgement call. Task 2's steps a-h are what makes
    all twelve reachable: a-e for HARN-01/02/03/04/06/07 and ACC-02, f for MEM-04, g for MEM-05, h
    for the D-12 invariant plus HARN-09's driven same-message case. HARN-09 is ticked against step
    h1's live result when the induction reproduced the shape; if it did not, HARN-09's same-message
    half is ticked against unit proof plus h2's invariant and the SUMMARY says exactly that. HARN-08
    is ticked against unit proof plus h2's invariant whenever no repaired id appeared, with its
    limitation named — never as "live-verified".

    ACC-02 is ticked against REQUIREMENTS.md's verbatim text, which names four surfaces. Write ONE
    line into `45-VALIDATION.md` recording why the fourth, `aura.context_rot_events`, carries no
    evidence for these five criteria: it stores `action`, `pairs_dropped`, `tokens_before`,
    `tokens_after` for L2.5 rolling-buffer drops (`internal/db/migrations/0005_conversations.up.sql:40-47`)
    and holds nothing about tool execution, replay, turn honesty or memory validity. Record what the
    run's reading of it showed — ideally no rows, meaning history was not truncated mid-scenario and
    cannot be confounding the SC#5 judgement.

    (c) Validation sign-off. Complete `45-VALIDATION.md`'s Validation Sign-Off checklist and set
    `nyquist_compliant` and `status` in its frontmatter according to what was actually measured. If
    any success criterion was proved at a LOWER tier than the evidence map names, it is not proved —
    say so and leave the box unticked rather than rounding up.

    (d) STATE.md. Update Current Position, the progress counters, Session Continuity and the
    Performance Metrics table for Phase 45. Record in Blockers/Concerns the two hazards this phase
    deliberately carried forward: the concurrent-swarm-worker key collision (D-05 → Phase 51,
    SWARM-07) and the deferred `Entity`-junk sweep plus the general alias mechanism (→ Phase 49).
    Add any Pending Todo that Task 1 of plan 45-07 created.

    Then push the phase and confirm every CI job is green — including
    `arcadedb-integration-test`, which this phase's memory work lands in.
  </action>
  <acceptance_criteria>
    - `AURA_QUALITY_CHANGED_FILES=… AURA_QUALITY_BASE_DATE=… bash scripts/quality_snapshot_gate.sh`
      prints `ok: … checked N row(s)` locally, with N > 0 — paste the literal output.
    - Every re-attested row in `docs/aura-quality-snapshot.md` has today's `Last measured` and a
      prepended note; the prior notes survive as `Prior …`.
    - `.planning/REQUIREMENTS.md` shows the twelve Phase 45 requirements ticked, each traceable to a
      specific evidence block in `45-VALIDATION.md` — including MEM-04 → step f, MEM-05 → step g,
      HARN-08/HARN-09 → step h; any requirement left open is named in the SUMMARY with the reason.
    - `45-VALIDATION.md` contains the one-line disposition of `aura.context_rot_events` as an ACC-02
      surface carrying no evidence for SC#1-#5, with the run's reading of it recorded.
    - `45-VALIDATION.md` frontmatter carries `status: validated` and a `nyquist_compliant` value that
      matches the measured reality.
    - `.planning/STATE.md` records the two carried-forward hazards with their owning phases.
    - `gh run list --limit 5` (or the equivalent) shows the phase-close push with all jobs green,
      including `arcadedb-integration-test`.
  </acceptance_criteria>
  <verify>
    <automated>AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh</automated>
  </verify>
  <done>The snapshot is re-attested and locally verified, the requirements are ticked only where live evidence exists, the carried-forward hazards are recorded against their owning phases, and CI is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running stack → evidence surfaces | OTel, `aura.tool_invocations`, `aura.conversation_turns` and the ArcadeDB graph are what "done" is judged against; if the run was driven against a stale image or the wrong model, the evidence describes something other than this phase's code |
| live long-term memory → the correction under test | D-23's correction operates on the operator's real graph; a mis-targeted close here is real data loss, not a test failure |
| coverage/verification tooling → live databases | The coverage gate has previously truncated a live deployment's auth tables; the disposable-database guards are load-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-34 | Repudiation | a phase closed on a green suite | mitigate | ACC-01 as an explicit closing predicate: the score, the SQL output and the transcript excerpts must be in `45-VALIDATION.md` before Task 3 runs, and Task 3 is gated on Task 2 clearing 9.8 |
| T-45-40 | Repudiation | a requirement closed on an assertion that is true by construction | mitigate | Step h's original duplicate-id assertion was guaranteed by `tool_invocations_once_per_phase_idx` and would have closed HARN-08/HARN-09 on a tautology. Replaced with the code-sensitive D-12 join invariant plus a driven HARN-09 induction; the tautology is now prohibition-level |
| T-45-35 | Tampering | D-23's live correction closing more than the one named fact | mitigate | The correction is driven through the exact machinery plans 45-06/45-07 built (recall → `fact_key` → targeted close), and the post-state of EVERY sibling is read and quoted. Scope is one fact; the nine compensating facts are explicitly out of bounds |
| T-45-36 | Tampering | evidence gathered against a stale image or the wrong model | mitigate | Task 1 step 6 bakes the commit SHA into `aura:local` (`ARG VCS_REF` + `-X main.commit`), so Task 2's precondition 1 can compare `aura version` against `git rev-parse HEAD` mechanically instead of trusting a `Created` timestamp. Preconditions 1 and 3 are BLOCKING acceptance criteria producing quoted values. Measured 2026-08-13: before this change the image baked NO SHA (`Dockerfile:36` ldflags `-s -w` only, `.git` in `.dockerignore`, no `LABEL`/`ARG`), so the criterion was unsatisfiable and would have been rationalized back onto the timestamp |
| T-45-37 | Denial of Service | a scheduler reclaim of a pre-deploy `tool-child-v1` operation double-executing after the key shape changes | mitigate | Precondition 2, now a blocking acceptance criterion requiring the drain command AND a quoted zero-count query (D-06). Residual risk is the drain window itself, which is operational and accepted |
| T-45-38 | Tampering | the coverage gate truncating the live `aura` database | mitigate | Task 1 mandates `scripts/coverage_docker.sh`, which provisions a disposable `aura_cov` and refuses `db_integration` against a database named `aura` locally. Prohibition-level: that guard must not be defeated |
| T-45-39 | Information Disclosure | a live transcript pasted into the validation record carrying operator personal data from the memory graph | accept | `45-VALIDATION.md` is committed to the repository. Quote only the fact under correction and its siblings' identifying fields — subject, predicate, `valid_to` — not full statement bodies, when the content is personal. Redact before pasting; note any redaction |
| T-45-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase; the Package Legitimacy Gate is not applicable (RESEARCH.md). `go-mutesting` in Task 1 is an already-installed WSL toolchain binary, not a new dependency |
</threat_model>

<reversibility>
| Decision | Rating | Why |
|----------|--------|-----|
| Correct the real F-1-caused fact in live memory (D-23) | **one-way in effect** | Closing a validity window is bitemporal, so the old fact stays queryable in the past rather than being deleted — recoverable, but the correction is a real write to the operator's live graph and is not undone by a code revert. It is nevertheless the right proof: a synthetic fixture would not exercise the machinery end to end. Gated behind the human checkpoint in Task 2 |
| Tick the requirement checkboxes (Task 3b) | reversible | Bookkeeping; the rule that an unevidenced requirement stays open is what protects it |
| Re-attest the quality snapshot rows | reversible | Prior notes are kept as `Prior …`, so history survives every re-attestation |
| Bake `VCS_REF` into `aura:local` (Task 1 step 6) | reversible | Two additive lines (`ARG` + ldflags, and the compose passthrough). An unset `VCS_REF` yields `commit: unknown` exactly as today, so the change is inert until used; reverting restores the current behaviour with no runtime or data consequence |
</reversibility>

<artifacts_this_phase_produces>
This plan produces no new Go symbols. Build + record artifacts:
- `docker/aura/Dockerfile` — `ARG VCS_REF=""` in the `build` stage and `-X main.commit=${VCS_REF}` appended to line 36's ldflags. No runtime surface change; the existing `commit` variable at `cmd/aura/version.go:16` is the sink.
- `compose.yaml` — `args: {VCS_REF: "${VCS_REF:-}"}` under `services.aura.build`, so the stamp reaches the build stage through the normal `docker compose build` path.
- `.planning/phases/45-harness-correctness/45-VALIDATION.md` — completed Per-Task Verification Map (plans 45-02..45-07), the full gate outputs, the mutation figures per file, the live scenario evidence for SC#1-#5, the score, and the Validation Sign-Off with `status` and `nyquist_compliant` set.
- `docs/aura-quality-snapshot.md` — re-attested rows for every CI-gate-path glob matching this phase's changed files, with prepended notes and preserved `Prior …` history.
- `.planning/REQUIREMENTS.md` — the twelve Phase 45 checkboxes and traceability rows, ticked only where live evidence exists.
- `.planning/STATE.md` — position, counters, Session Continuity, and the two carried-forward hazards (D-05 → Phase 51/SWARM-07; the `Entity` sweep and general alias mechanism → Phase 49).
</artifacts_this_phase_produces>

<verification>
- `make quality` and `make quality-full` exit 0 in WSL with the stack up.
- `bash scripts/coverage_docker.sh` reports the owned-surface figure at or above 85%.
- `make agent-memory-eval` meets its `PASS_THRESHOLD`.
- Mutation spot-check at or above 70% killed on `idempotency_operation.go`,
  `llm_agent_call_dedup.go` and `memory_supersede.go`.
- `bash scripts/quality_snapshot_gate.sh` prints `ok: … checked N row(s)` locally before the push.
- CI green on the phase-close push, including `arcadedb-integration-test`.
- `ls internal/eval` confirms no eval harness was created.
- `docker run --rm aura:local aura version` prints a `commit:` equal to `git rev-parse HEAD`, with
  `git status --porcelain` empty.
- The image build SHA, the drained-scheduler count, the live model row and the pre-run `psql`
  `SELECT now()` are quoted in `45-VALIDATION.md` and dated before the scenario's first turn.
</verification>

<success_criteria>
- All five ROADMAP success criteria proved at the tier the evidence map names — SC#1/#2 with live
  SQL against `aura.tool_invocations`, SC#3 with the marker and the span attributes, SC#4 with a
  real graph read after correcting the real fact, SC#5 on the real transcript.
- The three requirements SC#1-#5 do not reach are evidenced by their own live steps: MEM-04 by the
  single-vertex convergence read, MEM-05 by the quoted rejection naming `statement`, HARN-09 by the
  driven same-message induction (h1), and both HARN-08 and HARN-09 by the code-sensitive D-12 join
  invariant over `aura.conversation_turns.tool_calls` × `aura.tool_invocations` (h2).
- No requirement is closed on a schema-guaranteed assertion.
- All four live-run preconditions are recorded as quoted values before the scenario's first turn,
  each with an artifact that could have come out otherwise.
- The scenario scored above 9.8, with the score and its justification written down.
- The quality snapshot is re-attested and locally verified before the push.
- Every requirement ticked has live evidence behind it; any that does not stays open and is named.
</success_criteria>

<output>
Create `.planning/phases/45-harness-correctness/45-08-SUMMARY.md` when done. Record: every gate
result with its number, the mutation figures per file, the four quoted preconditions (image build
SHA vs HEAD with `git status --porcelain` empty, drained-scheduler count, live model row, pre-run
`psql` timestamp), the `aura version` output proving the stamp landed, the score with its
justification, the step-h1 outcome (did the induction reproduce two identical same-message calls?)
and the step-h2 join-invariant output, whether HARN-08's id repair fired live at all, any requirement
left open and why, the two hazards carried forward with their owning phases, and the literal
`quality_snapshot_gate.sh` output.
</output>
