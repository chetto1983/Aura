---
phase: 02-agent-cornerstone
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - prd.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/02-agent-cornerstone/02-SPEC.md
  - .planning/phases/02-agent-cornerstone/02-VALIDATION.md
  - .planning/codebase/STRUCTURE.md
  - THIRD_PARTY_NOTICES.md
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "All Phase 2 truth sources are converged before code work begins: PRD, ROADMAP, REQUIREMENTS, SPEC, CONTEXT, RESEARCH, VALIDATION"
    - "A1-A7 amendments are applied up-front, not deferred to the final CLI cleanup plan"
    - "No plan uses failure-filtering verification commands (`|| true`, `grep -v` around compiler failures) as a pass condition"
    - "Nyquist validation maps every Phase 2 requirement to an automated command before implementation starts"
    - "Apache-2.0 attribution/notice obligations for adapted google/adk-go workflow code are explicit"
  artifacts:
    - path: ".planning/phases/02-agent-cornerstone/02-SPEC.md"
      provides: "Spec aligned with A1-A7 and the >95 artifact gate"
      contains: "SpanID [8]byte"
    - path: ".planning/phases/02-agent-cornerstone/02-VALIDATION.md"
      provides: "Complete Nyquist map with no placeholder 2-XX rows"
      contains: "TestParallelAgent_DepthChainBudgetShared_NotFresh"
    - path: "THIRD_PARTY_NOTICES.md"
      provides: "Apache-2.0 attribution for adapted adk-go workflow patterns"
      contains: "google/adk-go"
---

<objective>
Raise the Phase 2 artifact package above the 95% industrial-readiness gate before any runtime code is written. This is a pre-code convergence plan: it removes truth-source drift, regenerates the validation map, makes unsafe verification commands fail-closed, and adds third-party attribution hygiene for the adk-go patterns.

Purpose: Phase 2 is the cornerstone for every later agent phase. A correct implementation built from contradictory artifacts is still a governance failure. This wave makes the artifacts executable, converged, and audit-ready.
Output: updated PRD/planning artifacts + `THIRD_PARTY_NOTICES.md`; no Go runtime implementation in this plan.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-agent-cornerstone/02-SPEC.md
@.planning/phases/02-agent-cornerstone/02-CONTEXT.md
@.planning/phases/02-agent-cornerstone/02-RESEARCH.md
@.planning/phases/02-agent-cornerstone/02-VALIDATION.md
@.planning/phases/02-agent-cornerstone/02-01-PLAN.md
@.planning/phases/02-agent-cornerstone/02-02-PLAN.md
@.planning/phases/02-agent-cornerstone/02-03-PLAN.md
@.planning/phases/02-agent-cornerstone/02-07-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply A1-A7 to truth sources before code</name>
  <files>prd.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-agent-cornerstone/02-SPEC.md</files>
  <read_first>
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md section "PRD-Amendments Required"
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md sections "Trace IDs", "Validation Architecture", "Open Questions"
    - CLAUDE.md section "PRD-first principle"
  </read_first>
  <action>
    Apply A1-A7 up front: exported workflow structs, two-tier dedup with result-as-veto and period-2 detection, `internal/canonicaljson` rescope, 8-byte SpanID/ParentSpanID plus MessageID/ThreadID, passive branch soft cap with hard cap authority, google/uuid as a new direct dep, and all A7 env vars. Update ROADMAP/REQUIREMENTS so they no longer restate stale UUIDv7 SpanID or three-env-only budget language. This task must be completed before Plans 01-07 execute.
  </action>
  <verify>
    <automated>rg -n 'SpanID uuid.UUID|ParentSpanID \\*uuid.UUID|already in chain via|type sequentialAgent|type loopAgent|type parallelAgent|RFC 8785-like|≥ 0.75|2-XX-XX' .planning/phases/02-agent-cornerstone/02-SPEC.md prd.md .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The verify command returns no stale truth-source matches except historical discussion-log entries explicitly marked as reversed
    - ROADMAP lists this Gate 0 plan before the dependency/canonicaljson plan
    - REQUIREMENTS describes RequestID as UUIDv7 and SpanID as 8-byte OTel-compatible shape
    - SPEC acceptance requires 85% coverage, not 75%
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Regenerate Nyquist validation map from RESEARCH</name>
  <files>.planning/phases/02-agent-cornerstone/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md section "Phase Requirements -> Test Map"
    - .planning/phases/02-agent-cornerstone/02-01-PLAN.md through 02-07-PLAN.md task verify blocks
  </read_first>
  <action>
    Replace the placeholder validation table with the full requirement-to-test map: SC#1-SC#4, budget invariants, canonicaljson fuzz/property checks, Event JSON/trace shape, Sequential/Loop/Parallel workflow behavior, CLI smoke, coverage, mutation spot-check, lint, build, vet, file-size cap. SC#4 must be automated via unit + CLI smoke; it must not be manual-only.
  </action>
  <verify>
    <automated>rg -n '2-XX|Manual-Only.*request-id auto|nyquist_compliant: true' .planning/phases/02-agent-cornerstone/02-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - No `2-XX` placeholder remains
    - Every acceptance criterion in 02-SPEC has at least one automated command in VALIDATION
    - Manual-only section is empty or limited to mutation-score evidence that is documented but not CI-enforced
    - `nyquist_compliant` stays false until implementation exists, but validation coverage is complete
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fail-closed verification and dependency ordering</name>
  <files>.planning/phases/02-agent-cornerstone/02-01-PLAN.md, .planning/phases/02-agent-cornerstone/02-02-PLAN.md, .planning/phases/02-agent-cornerstone/02-03-PLAN.md, .planning/phases/02-agent-cornerstone/02-07-PLAN.md</files>
  <action>
    Make Plan 01 depend on this Gate 0 plan, Plan 02 depend on Plan 01, and remove failure-hiding verification (`grep -v 'Budget'`, `|| true`) from Plan 02. If a type is forward-referenced, provide a minimal compile-safe shell or reorder work; never hide compiler errors. Move PRD-amendment work out of Plan 07 and make Plan 07 verify that the already-applied A1-A7 amendments remain present.
  </action>
  <verify>
    <automated>rg -n '(2>&1 \\| grep -v|\\|\\| true|PRD amendments A1)' .planning/phases/02-agent-cornerstone -g '02-0*-PLAN.md' | rg -v '02-00-PLAN.md'</automated>
  </verify>
  <acceptance_criteria>
    - No fail-open verification commands remain
    - Plan 07 no longer first applies PRD amendments late
    - Plan dependencies form a strict 00 -> 01 -> 02 -> 03/04 -> 05 -> 06 -> 07 chain
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Third-party attribution hygiene</name>
  <files>THIRD_PARTY_NOTICES.md, .planning/phases/02-agent-cornerstone/02-SPEC.md, .planning/phases/02-agent-cornerstone/02-PATTERNS.md</files>
  <action>
    Add or update `THIRD_PARTY_NOTICES.md` with `google/adk-go` as an Apache-2.0 source for adapted workflow-agent patterns. Keep in-source attribution comments in adapted files, but do not rely on comments alone for license hygiene.
  </action>
  <verify>
    <automated>rg -n 'google/adk-go|Apache-2.0|Apache 2.0' THIRD_PARTY_NOTICES.md .planning/phases/02-agent-cornerstone/02-SPEC.md .planning/phases/02-agent-cornerstone/02-PATTERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - `THIRD_PARTY_NOTICES.md` exists and names google/adk-go, Apache-2.0, source URL, and adapted files
    - SPEC/PATTERNS instruct executors to preserve attribution comments in adapted workflow code
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `go test ./...` green before implementation begins
- `go vet ./...` green before implementation begins
- `bash scripts/check-file-size.sh` green before implementation begins
- Artifact audit score: each Phase 2 planning artifact >=95/100; no artifact below the gate may feed execution
</verification>

<success_criteria>
- Phase 2 artifacts are internally converged and execution-safe before code work
- Validation coverage is complete even though `nyquist_compliant` remains false until tests/code exist
- License/notice hygiene is explicit for adk-go adapted patterns
- No failure-hiding verification command remains in the Phase 2 plans
</success_criteria>

<output>
Create `.planning/phases/02-agent-cornerstone/02-00-SUMMARY.md` when done.
</output>
