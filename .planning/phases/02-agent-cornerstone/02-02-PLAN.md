---
phase: 02-agent-cornerstone
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/agent.go
  - internal/agent/event.go
  - internal/agent/errors.go
  - internal/agent/event_test.go
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "type Agent interface exposes exactly Name/Description/Run(InvocationContext) iter.Seq2[*Event, error]/SubAgents/FindAgent — no unexported seal (D-01)"
    - "Event marshals to JSON and round-trips byte-identical; LLMResponse is nil when the Event is not an LLM turn (Req#2)"
    - "RequestID/TraceID are 16-byte uuid.UUID (UUIDv7); SpanID/ParentSpanID are 8 bytes from crypto/rand; ParentSpanID nil at root (D-16/A4)"
    - "ErrBudgetExhausted is an exported sentinel error for errors.Is consumers (D-04)"
  artifacts:
    - path: "internal/agent/agent.go"
      provides: "Agent interface + InvocationContext + WithContext/WithSubAgent copy-semantics"
      contains: "type Agent interface"
      exports: ["Agent", "InvocationContext"]
    - path: "internal/agent/event.go"
      provides: "Event + Actions + LLMResponse full shape (AG-UI forward-compat)"
      contains: "type Event struct"
      exports: ["Event", "Actions", "LLMResponse"]
    - path: "internal/agent/errors.go"
      provides: "ErrBudgetExhausted sentinel (D-04)"
      contains: "ErrBudgetExhausted"
  key_links:
    - from: "internal/agent/event.go"
      to: "internal/llm.ToolCall"
      via: "Event.LLMResponse.ToolCalls reuses llm.ToolCall (D-17, no redefinition)"
      pattern: "llm\\.ToolCall"
    - from: "internal/agent/event.go"
      to: "github.com/google/uuid"
      via: "RequestID uuid.UUID UUIDv7"
      pattern: "uuid\\.UUID"
---

<objective>
Define the cornerstone contract types every later phase implements/consumes: the OPEN `Agent` interface (D-01), the single-Run-scoped `InvocationContext` (D-24), the full-shape forward-compat `Event`/`Actions`/`LLMResponse` (Req#2, AG-UI-ready via D-17), the OTel-correct trace IDs (D-16/A4), and the `ErrBudgetExhausted` sentinel (D-04).

Purpose: This is the substrate's public API. Interface-first ordering — these contracts must exist before Budget (Plan 03), mocks (Plan 04), and workflow agents (Plan 05/06) can compile against them. Getting the trace-ID widths right NOW (16B TraceID / 8B SpanID) is the single biggest correctness lever (D-16, the 78→95 scout-5 fix) — a future OTel slice becomes drop-in, not lossy truncation.
Output: `internal/agent/{agent,event,errors}.go` + `event_test.go` (JSON round-trip + trace-ID shape + property test).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-cornerstone/02-SPEC.md
@.planning/phases/02-agent-cornerstone/02-CONTEXT.md
@.planning/phases/02-agent-cornerstone/02-RESEARCH.md
@.planning/phases/02-agent-cornerstone/02-PATTERNS.md
@internal/llm/client.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agent interface + InvocationContext (agent.go) + ErrBudgetExhausted (errors.go)</name>
  <files>internal/agent/agent.go, internal/agent/errors.go</files>
  <read_first>
    - internal/agent/tools/spec.go (package-doc + interface doc-comment house style to replicate — PATTERNS § agent.go)
    - internal/db/config.go:17-25 (Config struct: per-field doc with in-line default — the field-not-embed idiom for InvocationContext, D-24)
    - internal/db/migrate.go:28-29 (sentinel error idiom — errors.go mirrors this but EXPORTS an error var per D-04)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Architecture Patterns › Pattern 1 (open interface, 5 methods, seal removed) + Pattern 5 (WithSubAgent copy semantics `c := ic; c.Agent = sub; return c`)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § agent.go (the exact interface block + the D-24 doc comment verbatim) + § errors.go
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-01, D-02 (typed-nil guard), D-09 (two derivations — WithSubAgent shares budget+ring as-is), D-24
    - D:/tmp/adk-go-study/agent/agent.go L43-52 (interface; note the internal() seal at L51 Aura REMOVES) + agent/context.go (InvocationContext shape — adk uses interface, Aura uses struct)
  </read_first>
  <behavior>
    - var _ agent.Agent = (*workflow.SequentialAgent)(nil) compiles (asserted in Plan 05/06 test files; here the interface must be shaped to allow it)
    - WithSubAgent(sub) returns a COPY with .Agent=sub, same *atomic.Int32 budget pointer, same dedup ring (D-09) — never mutates the receiver
    - errors.Is(err, ErrBudgetExhausted) works for programmatic consumers
  </behavior>
  <action>
    Create package `agent` (replacing the old loop.go package doc — old loop.go is deleted in Plan 07). Define `type Agent interface { Name() string; Description() string; Run(InvocationContext) iter.Seq2[*Event, error]; SubAgents() []Agent; FindAgent(name string) Agent }` — exactly five methods, NO unexported `internal()` seal (D-01). Package doc explains WHY the interface is open (Phase 3 LlmAgent + Phase 9 swarm implement it directly) and carries the adk attribution comment. Define `InvocationContext` as a plain struct (D-24): `Ctx context.Context` as a NAMED field (never embedded), `Agent Agent` (self-ref), `RequestID uuid.UUID`, `SpanID [8]byte`, `ParentSpanID *[8]byte` (nil at root), `Branch string`, `Budget *Budget` (forward-declared type from Plan 03 — same package, so the compile order resolves once budget.go lands). Add the D-24 doc comment verbatim: "InvocationContext is single-Run-scoped; never store on a long-lived struct, never cache, never share across invocations." Implement `WithContext(ctx)` and `WithSubAgent(sub)` returning a COPY (`c := ic; ...; return c`) — never mutate (D-24). `FindAgent` checks self then recurses into SubAgents (collapse adk's FindSubAgent split). In errors.go, export `var ErrBudgetExhausted = errors.New(...)` (D-04) for `errors.Is` consumers. Use the repo `fmt.Errorf("<verb> <noun>: %w", err)` wrap convention. Do NOT implement Budget here (Plan 03) — reference the `*Budget` type only.
  </action>
  <verify>
    <automated>go build ./internal/agent/ 2>&1 | grep -v 'Budget' ; go vet ./internal/agent/ 2>&1 | grep -v 'Budget' || true</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/agent.go` contains `type Agent interface` with exactly the 5 methods and NO method named `internal`
    - `grep -c 'func.*internal()' internal/agent/agent.go` returns 0 (seal removed, D-01)
    - InvocationContext has `Ctx context.Context` as a named field (not embedded): `grep -q 'Ctx context.Context' internal/agent/agent.go`
    - The literal D-24 doc string `single-Run-scoped` appears in agent.go
    - `internal/agent/errors.go` contains `ErrBudgetExhausted` declared via `errors.New`
    - (Build will be fully green only after Plan 03 lands `Budget`; this task's build-check tolerates the missing `Budget` symbol — agent.go + errors.go must otherwise be syntactically/vet clean)
  </acceptance_criteria>
  <done>Open Agent interface + single-Run-scoped InvocationContext with copy-semantics + exported sentinel; compiles modulo the forward-referenced Budget type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Event/Actions/LLMResponse full shape + trace IDs (event.go) + round-trip tests</name>
  <files>internal/agent/event.go, internal/agent/event_test.go</files>
  <read_first>
    - internal/llm/client.go:24-61 (struct + json-tag DTO idiom: omitempty, pointer-for-nullable, per-field doc explaining WHEN each field populates — the canonical shape file to copy exactly; also `ToolCall.ID` at L35 to surface as ToolCallID per D-17)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Architecture Patterns › Pattern 8 (trace IDs: uuid.NewV7 16B TraceID, crypto/rand 8B SpanID, WHY 8B matters) + § Code Examples › Event JSON marshal discipline (UseNumber, SetEscapeHTML(false), RFC3339Nano UTC)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § event.go (reuse llm.ToolCall directly — do NOT redefine; nullable `*LLMResponse`; trace-ID widths)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-16 (16B TraceID / 8B SpanID — the critical fix), D-17 (add MessageID/ToolCallID/ThreadID for AG-UI forward-compat), D-21 (Event JSON round-trip property test)
    - .planning/phases/02-agent-cornerstone/02-SPEC.md Req#2 (Event/Actions/LLMResponse target shape + Author pattern: workflow_agent_name, "user", or agent name)
    - internal/config/config_test.go:10-50 (table-driven + clearEnv/t.Setenv assertion style)
  </read_first>
  <behavior>
    - Event → JSON → Event round-trips byte-identical (D-21 property, Aura-internal symmetric encode)
    - Event with nil LLMResponse marshals without an LLMResponse object (omitempty / pointer-nil)
    - len(RequestID) == 16; SpanID serializes to 16 hex chars (8 bytes); ParentSpanID nil at root (TestEvent_TraceID16Bytes_SpanID8Bytes)
    - Actions.StateDelta accepts nested map[string]any
  </behavior>
  <action>
    Create event.go: `type Event struct { RequestID uuid.UUID; SpanID [8]byte; ParentSpanID *[8]byte; Author string; Branch string; ThreadID string; MessageID uuid.UUID; LLMResponse *LLMResponse; Actions Actions; Timestamp time.Time }`. `LLMResponse struct { Content string; ToolCalls []llm.ToolCall; FinishReason string }` — reuse `internal/llm.ToolCall` (D-17), do NOT redefine a ToolCall type; surface its `ID` as the AG-UI ToolCallID. `Actions struct { Escalate bool; StateDelta map[string]any; ArtifactDelta map[string]any }`. Apply the llm/client.go json-tag discipline: `omitempty` on optional fields, pointer-for-nullable (`*LLMResponse` nil when not an LLM turn, `*[8]byte` ParentSpanID nil at root). Author is set explicitly per workflow agent (D-14) — no global constant. Add a `SetAuthorIfEmpty(name string)` helper (D-14) for LLM-emitted events. For JSON marshal byte-identity (D-21): use `json.Encoder` with `SetEscapeHTML(false)`, decode with `UseNumber()`, format `time.Time` as RFC3339Nano UTC. Add MessageID (uuid.UUIDv7) + ThreadID for AG-UI forward-compat (D-17). Write event_test.go: `TestEvent_FullShapeMarshalsToJSON_RoundTrips`, `TestEvent_TraceID16Bytes_SpanID8Bytes`, and the rapid property `TestEvent_Property_JSONRoundTripByteIdentical`. SpanID generation uses `crypto/rand` 8 bytes (NOT uuid) per D-16.
  </action>
  <verify>
    <automated>go test ./internal/agent/ -run 'TestEvent' 2>&1 | grep -v 'Budget' ; go test ./internal/agent/ -run 'TestEvent'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/event.go` contains `type Event struct` and `LLMResponse *LLMResponse` (pointer/nullable)
    - `grep -q 'llm.ToolCall' internal/agent/event.go` (reuses llm.ToolCall, D-17 — does NOT define a new ToolCall type)
    - `SpanID` field type is `[8]byte` and `ParentSpanID` is `*[8]byte` (D-16): `grep -q '\[8\]byte' internal/agent/event.go`
    - `TestEvent_FullShapeMarshalsToJSON_RoundTrips` passes (byte-identical round-trip)
    - `TestEvent_TraceID16Bytes_SpanID8Bytes` passes (RequestID 16 bytes, SpanID 8 bytes, ParentSpanID nil at root)
    - `TestEvent_Property_JSONRoundTripByteIdentical` (rapid) passes
  </acceptance_criteria>
  <done>Event full shape round-trips byte-identical, trace IDs are OTel-correct widths, llm.ToolCall reused not redefined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process Run scope | InvocationContext is single-Run-scoped (D-24) — not a user session, no cross-invocation sharing |
| future trace export | SpanID/TraceID widths must match W3C/OTel exactly or future correlation silently breaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | SpanID/ParentSpanID (future trace forgery once exposed) | mitigate | 8 bytes from `crypto/rand` (NOT math/rand), exactly OTel RandomIDGenerator shape (D-16); proven by `TestEvent_TraceID16Bytes_SpanID8Bytes` |
| T-02-06 | Tampering | InvocationContext stored on a long-lived struct (cross-invocation leakage) | mitigate | D-24 invariant: `Ctx` is a named field (never embedded), `WithContext`/`WithSubAgent` always return a copy; doc comment `single-Run-scoped` enforced |
| T-02-07 | Information disclosure | future OTel mapping truncates a 16-byte SpanID → silent correlation break | accept→mitigate | A4 fix: SpanID is 8 bytes now, so the future OTel slice is drop-in, no lossy truncation |
</threat_model>

<verification>
- `go test ./internal/agent/ -run TestEvent` green (round-trip + trace-ID shape + property)
- `go vet ./internal/agent/` clean for event.go/agent.go/errors.go (Budget symbol resolves after Plan 03)
- `bash scripts/check-file-size.sh` clean (agent.go ~90, event.go ~110 — both ≤600)
- Compile-asserts deferred to Plan 05/06 test files (they reference the workflow structs that don't exist yet)
</verification>

<success_criteria>
- Open `Agent` interface (5 methods, no seal) + single-Run-scoped `InvocationContext` with copy-semantics (D-01/D-24)
- `Event` full shape round-trips byte-identical; `LLMResponse` nullable; trace IDs 16B/8B correct (Req#2, D-16)
- `llm.ToolCall` reused (D-17), not redefined; `ErrBudgetExhausted` exported (D-04)
</success_criteria>

<output>
Create `.planning/phases/02-agent-cornerstone/02-02-SUMMARY.md` when done
</output>
