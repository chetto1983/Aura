---
phase: 02-agent-cornerstone
plan: 01
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - go.mod
  - go.sum
  - internal/canonicaljson/canonicaljson.go
  - internal/canonicaljson/canonicaljson_test.go
  - .golangci.yml
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "go build ./... succeeds with github.com/google/uuid as a direct require and pgregory.net/rapid present (A6)"
    - "canonicaljson.Marshal produces byte-identical output regardless of input map key order"
    - "canonicaljson.Marshal treats 1 and 1.0 as DISTINCT (UseNumber literal preservation, D-08)"
    - "canonicaljson.Marshal strict-rejects NaN/Inf/func/chan with an error, never silently coercing (D-08)"
  artifacts:
    - path: "internal/canonicaljson/canonicaljson.go"
      provides: "Deterministic internal serializer Marshal(any) ([]byte, error)"
      contains: "func Marshal"
      min_lines: 50
    - path: "internal/canonicaljson/canonicaljson_test.go"
      provides: "Fuzz round-trip + 1!=1.0 + strict-reject coverage"
      contains: "func Fuzz"
  key_links:
    - from: "go.mod"
      to: "github.com/google/uuid"
      via: "direct require block (promoted from indirect)"
      pattern: "google/uuid"
---

<objective>
Land the two-dep + shared-serializer foundation that every later Phase-2 plan builds on, with zero dependency on `internal/agent` (so it can run first, fully parallel-safe). Promote `github.com/google/uuid` to a direct dependency and add `pgregory.net/rapid` (test-only) per A6. Hand-roll the `internal/canonicaljson` deterministic serializer (D-08 / A3 — NOT RFC-8785) that the Budget dedup fingerprint (Plan 03) consumes and Phase 4/11 reuse.

Purpose: `canonicaljson` is the determinism primitive for `sha256(name + canonical_json(args))` dedup hashing. Getting it wrong (silent float coercion, unstable key order) produces silent hash collisions that merge distinct tool calls — a Tampering threat (T-02-04). `uuid.NewV7` is the OTel-compatible TraceID source for SC#4.
Output: `internal/canonicaljson/` package + `go.mod`/`go.sum` with uuid direct + rapid added + `.golangci.yml` exclusion for the new test dep if needed.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-cornerstone/02-CONTEXT.md
@.planning/phases/02-agent-cornerstone/02-RESEARCH.md
@.planning/phases/02-agent-cornerstone/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote uuid to direct dep + add pgregory.net/rapid</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - go.mod (current: `golang.org/x/sync v0.18.0 // indirect`, `go.uber.org/goleak v1.3.0`; `github.com/google/uuid` present in go.sum transitively but NOT in require as direct — A6 confirmed)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Standard Stack (versions: uuid v1.6.0, rapid v1.3.0) + § Package Legitimacy Audit (both Approved, registry-verified, not slop)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md A6 (uuid is a genuine NEW direct dep; x/sync errgroup already present indirect — no new dep for ParallelAgent)
  </read_first>
  <action>
    Run `go get github.com/google/uuid@v1.6.0` and `go get pgregory.net/rapid@v1.3.0`, then `go mod tidy`. This promotes `google/uuid` into the direct require block (A6) and adds `pgregory.net/rapid` as a test-only dep (D-21). Do NOT add errgroup via go get — `golang.org/x/sync` is already in go.sum; it gets promoted to direct automatically once `parallel.go` (Plan 06) imports `errgroup`. Verify the version strings match exactly v1.6.0 / v1.3.0 (the legitimacy audit pinned these). Do not introduce any other dependency.
  </action>
  <verify>
    <automated>go build ./... && go list -m github.com/google/uuid pgregory.net/rapid</automated>
  </verify>
  <acceptance_criteria>
    - `go list -m github.com/google/uuid` prints `github.com/google/uuid v1.6.0`
    - `go list -m pgregory.net/rapid` prints `pgregory.net/rapid v1.3.0`
    - `go build ./...` exits 0
    - `grep -v '^//' go.mod | grep -q 'github.com/google/uuid v1.6.0'` succeeds (uuid in a require block, not only go.sum)
  </acceptance_criteria>
  <done>Both deps resolvable at the pinned versions; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-roll internal/canonicaljson deterministic serializer (D-08 / A3)</name>
  <files>internal/canonicaljson/canonicaljson.go, internal/canonicaljson/canonicaljson_test.go, .golangci.yml</files>
  <read_first>
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Architecture Patterns › Pattern 6 (the full algorithm + the documented rule: sort map keys by Go byte order; json.Decoder.UseNumber so numbers stay literal text; strict-reject un-canonicalizable input)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md › `internal/canonicaljson/canonicaljson.go` section (package-doc must state the A3 rescope verbatim) + § Shared Patterns › "Determinism rationale comment" (manifest.go:19-21 idiom — document WHY, citing the consequence of getting it wrong)
    - internal/agent/tools/manifest.go:19-21 (the sort-for-determinism rationale-comment house style to mirror)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-08 (the three collapsed requirements: (a) one documented key order, (b) UseNumber, (c) strict-reject)
  </read_first>
  <behavior>
    - canon({"a":1,"b":2}) == canon({"b":2,"a":1}) (key-order independent)
    - canon("1") != canon("1.0") (UseNumber literal preservation per documented rule, D-08)
    - canon(NaN/Inf/func/chan) returns a non-nil error (strict-reject, never silent coerce)
    - Fuzz property: canon(x) == canon(decode(encode(x))) — idempotent round-trip
  </behavior>
  <action>
    Create package `canonicaljson` exporting `Marshal(v any) ([]byte, error)`. Sort map keys by Go byte order (one documented order — there is NO cross-impl consumer, so do NOT chase RFC-8785 float canonicalization per A3). Decode numbers with `json.Decoder.UseNumber()` so `1` and `1.0` stay distinct literal text and never round-trip through `float64`. Strict-reject un-canonicalizable input (NaN, Inf, func, chan) by returning a wrapped error using the repo `fmt.Errorf("<verb> <noun>: %w", err)` convention — never silently coerce. Package doc MUST state the A3 rescope verbatim: "Deterministic serializer for Aura-internal hashing. NOT RFC-8785 (no cross-system crypto-signature consumer → float-canonicalization minefield avoided)." Add a `FuzzCanonical_RoundTripAndDistinctNumbers` fuzz target asserting the round-trip property + `canon("1") != canon("1.0")` + strict-reject. This package MUST NOT import `internal/agent` (it is consumed by, not consuming, the agent package). If `golangci-lint` flags the new `pgregory.net/rapid` import in tests, the existing `.golangci.yml` `_test.go` exclusion already covers gosec/errcheck — only touch `.golangci.yml` if a NEW path exclusion is genuinely required (do not weaken existing rules).
  </action>
  <verify>
    <automated>go test ./internal/canonicaljson/... && go test -run x -fuzz FuzzCanonical_RoundTripAndDistinctNumbers -fuzztime 10s ./internal/canonicaljson/ && go vet ./internal/canonicaljson/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/canonicaljson/canonicaljson.go` contains `func Marshal(v any) ([]byte, error)`
    - Package doc literally contains the string `NOT RFC-8785`
    - A test asserts `canon("1")` bytes != `canon("1.0")` bytes and passes
    - A test asserts `Marshal` returns a non-nil error for `math.NaN()` and `math.Inf(1)`
    - `go test -fuzz FuzzCanonical_RoundTripAndDistinctNumbers -fuzztime 10s ./internal/canonicaljson/` exits 0 (no failing corpus entry)
    - `grep -q 'internal/agent' internal/canonicaljson/canonicaljson.go` returns NOTHING (no agent import — one-direction dependency)
  </acceptance_criteria>
  <done>canonicaljson serializes deterministically, distinguishes 1 from 1.0, strict-rejects bad input, fuzz-clean, no agent import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | `AURA_LOOP_*` env vars parsed at boot (validated in Plan 03, not here) |
| tool args → dedup hash | tool-call arguments flow into `canonicaljson.Marshal` → `sha256` fingerprint (this plan owns the serializer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | `canonicaljson.Marshal` (dedup fingerprint input) | mitigate | strict-reject NaN/Inf/func/chan + `UseNumber` so `1`≠`1.0` (D-08) → no silent hash collision merging distinct tool calls; proven by `FuzzCanonical_RoundTripAndDistinctNumbers` |
| T-02-SC | Tampering | `go get` of uuid + rapid | accept | Both registry-verified canonical packages (RESEARCH § Package Legitimacy Audit: not [SLOP]/[SUS]; google/uuid since 2016, rapid stable line). slopcheck unavailable on Windows; risk minimal, no blocking checkpoint required for these two well-known modules. Versions pinned exactly (v1.6.0 / v1.3.0). |
</threat_model>

<verification>
- `go build ./...` clean (uuid direct, rapid present)
- `go test ./internal/canonicaljson/...` green incl. fuzz target
- `go vet ./internal/canonicaljson/...` clean
- `bash scripts/check-file-size.sh` clean (canonicaljson.go ≤600 LOC — target ~90)
</verification>

<success_criteria>
- `github.com/google/uuid v1.6.0` is a direct require; `pgregory.net/rapid v1.3.0` present (A6)
- `internal/canonicaljson.Marshal` is deterministic, distinguishes `1`/`1.0`, strict-rejects un-canonicalizable input (D-08), fuzz-clean
- No `internal/agent` import in canonicaljson (one-direction dependency confirmed)
</success_criteria>

<output>
Create `.planning/phases/02-agent-cornerstone/02-01-SUMMARY.md` when done
</output>
