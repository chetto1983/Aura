---
phase: 02-agent-cornerstone
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - cmd/aura/agent.go
  - cmd/aura/agent_test.go
  - cmd/aura/main.go
  - internal/agent/loop.go
  - scripts/loop_budget_smoke.sh
  - .env.example
  - .planning/codebase/STRUCTURE.md
  - prd.md
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "aura agent dry-run --request-id auto emits N Event JSON lines, every line carries a UUIDv7 request_id matching the v7 regex, all lines share the same value (SC#4)"
    - "aura agent dry-run --request-id <uuid> reproduces that request_id verbatim across runs (SC#4)"
    - "CLI flag precedence is CLI flag (non -1) > env > builtin default 25/300/3 (D-06 sentinel -1 fallthrough)"
    - "scripts/loop_budget_smoke.sh exits 0, stdout has exactly 26 Event JSON lines, and greps limit_hit:max_steps (SC#2)"
    - "internal/agent/loop.go is deleted; cmd/aura/main.go no longer dispatches case chat / chatOnce / stubClient and now dispatches case agent (SPEC boundary)"
  artifacts:
    - path: "cmd/aura/agent.go"
      provides: "aura agent dry-run subcommand (SC#4)"
      contains: "dry-run"
    - path: "scripts/loop_budget_smoke.sh"
      provides: "SC#2 fixture asserting 26 lines + grep limit_hit:max_steps"
      contains: "loop_budget_smoke"
  key_links:
    - from: "cmd/aura/agent.go"
      to: "internal/agent/workflow.NewLoop + internal/agent/agenttest.InfiniteToolCallAgent"
      via: "builds a mock LoopAgent, iterates Run, prints one Event per JSON line"
      pattern: "NewLoop|InfiniteToolCallAgent"
    - from: "cmd/aura/main.go"
      to: "runAgent"
      via: "switch case \"agent\""
      pattern: "case \"agent\""
---

<objective>
Wire the user-visible entry point and prove the phase end-to-end: the `aura agent dry-run` subcommand (SC#4 — UUIDv7 request_id on every Event, CLI>env>default precedence D-06), the SC#2 smoke script, and the destructive cleanup the SPEC mandates — delete `internal/agent/loop.go`, strip `case "chat"`/`chatOnce`/`stubClient` from `main.go`, add `case "agent"`, append the env vars to `.env.example`, fix the dangling README/STRUCTURE reference (RESEARCH finding), and land the A1–A7 PRD amendments.

Purpose: This is the demonstrable cornerstone. `aura agent dry-run` is the operator-facing proof that the Budget tree, the workflow agents, and the OTel-compatible trace IDs all work together. The smoke script is the CI-grep contract for SC#2. The cleanup makes the substitution atomic per the SPEC (Phase 1 `Loop` skeleton is replaced, not augmented). The PRD amendment commit (A1–A7) precedes/accompanies the code per CLAUDE.md PRD-first discipline.
Output: `cmd/aura/{agent,agent_test}.go` + `main.go` diff + `loop.go` deletion + `scripts/loop_budget_smoke.sh` + `.env.example` + STRUCTURE.md fix + `prd.md` amendments.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-cornerstone/02-SPEC.md
@.planning/phases/02-agent-cornerstone/02-CONTEXT.md
@.planning/phases/02-agent-cornerstone/02-RESEARCH.md
@.planning/phases/02-agent-cornerstone/02-PATTERNS.md
@.planning/phases/02-agent-cornerstone/02-05-SUMMARY.md
@.planning/phases/02-agent-cornerstone/02-06-SUMMARY.md
@cmd/aura/main.go
@cmd/aura/db.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: aura agent dry-run subcommand (SC#4) + agent_test.go + main.go dispatch wiring</name>
  <files>cmd/aura/agent.go, cmd/aura/agent_test.go, cmd/aura/main.go</files>
  <read_first>
    - cmd/aura/db.go:19-44 (the `runX(args []string)` dispatcher idiom: sub-subcommand switch, usage-to-stderr + os.Exit(1), exit non-zero on error — copy this shape for runAgent)
    - cmd/aura/main.go:22-50 (the switch to modify: remove `case "chat"`+chatOnce+stubClient L30-35,64-94; add `case "agent": runAgent(os.Args[2:])`; update package doc-comment subcommand list + usage())
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Code Examples › CLI flag sentinel precedence (flags default -1 → fall through to NewBudgetFromEnv; non--1 overrides via SetMaxSteps) + § Coverage Strategy (keep logic out of main() so agent_test.go can capture stdout)
    - .planning/phases/02-agent-cornerstone/02-SPEC.md Req#7 (the dry-run flag set + behavior) + § Acceptance SC#4 (the exact UUIDv7 regex `^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md D-06 (CLI flag > env > default; -1 sentinel; NewBudgetFromEnv fail-fast)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § cmd/aura/agent.go (db.go handler shape; one Event = one JSON line via canonicaljson.Marshal) + § cmd/aura/main.go (self-analog dispatcher edit; drop now-unused imports)
    - internal/agent/workflow/loop.go (NewLoop) + internal/agent/agenttest/mocks.go (InfiniteToolCallAgent) + internal/canonicaljson (Marshal for the JSON lines)
  </read_first>
  <behavior>
    - `aura agent dry-run --request-id auto` → every stdout line is a JSON Event with a `request_id` matching the v7 regex, all identical
    - `aura agent dry-run --request-id 0192f000-0000-7000-8000-000000000001` → that exact request_id on every line, reproducible
    - `aura agent dry-run --max-steps 25` → terminates after exactly 25 step Events + 1 budget-exhausted Event (26 lines)
    - flag -1 (unset) falls through to NewBudgetFromEnv; non--1 overrides (CLI>env>default)
  </behavior>
  <action>
    Create cmd/aura/agent.go: `runAgent(args []string)` mirroring db.go's dispatcher; the `dry-run` sub-subcommand parses `--request-id` (string, "auto" or a UUID), `--max-steps`/`--max-wallclock-sec`/`--dedup-window` (int, default -1 sentinel per D-06). Build `Budget` via `agent.NewBudgetFromEnv()` (fail-fast); for each flag != -1, override via the setter (CLI>env>default). `--request-id auto` → `uuid.NewV7()` at boot; a literal UUID → parse verbatim (reproducibility). Build a mock `LoopAgent` over `agenttest.InfiniteToolCallAgent` (the same-tool-call-forever fixture), construct the root `InvocationContext` (RequestID = the chosen uuid, fresh SpanID, nil ParentSpanID, the Budget), iterate `agent.Run(ic)` consuming `iter.Seq2[*Event,error]` with the two-var range form (D-22 footgun 4), and print each Event as ONE JSON line to stdout via `canonicaljson.Marshal` (so request_id is stable/correlatable). Exit 0 on natural termination, 1 on error. KEEP the iterate-and-print logic in a testable function (not inline in a `main`-only path) so agent_test.go can capture stdout. Edit main.go: remove `case "chat"`, `chatOnce`, `stubClient`; add `case "agent": runAgent(os.Args[2:])`; update the package doc subcommand list + `usage()` string; drop the now-unused `internal/llm` import (and `internal/agent` if chatOnce was its only consumer — verify with `go build`). Create agent_test.go: flag-parsing + request-id auto/verbatim tests capturing stdout, asserting the v7 regex and all-lines-equal.
  </action>
  <verify>
    <automated>go build ./... && go test ./cmd/aura/ -run 'TestDryRun|TestAgent' && go run ./cmd/aura agent dry-run --request-id 0192f000-0000-7000-8000-000000000001 --max-steps 5 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/aura/agent.go` builds a `LoopAgent` over `agenttest.InfiniteToolCallAgent` and prints one JSON Event per line
    - `cmd/aura/main.go` has `case "agent"` and NO `case "chat"`, NO `chatOnce`, NO `stubClient`: `grep -q 'case "agent"' cmd/aura/main.go` and `grep -c 'stubClient' cmd/aura/main.go` returns 0
    - `TestDryRun_RequestIDAuto_IsValidUUIDv7_AndStable` passes (every line matches the v7 regex, all identical)
    - a test asserts `--request-id <fixed>` reproduces verbatim
    - `go build ./...` clean (no unused-import error after dropping llm/agent)
  </acceptance_criteria>
  <done>aura agent dry-run emits UUIDv7-correlated Event JSON lines with CLI>env>default precedence (SC#4); chat/stubClient removed, agent dispatch wired; stdout-capturable; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: SC#2 smoke script + loop.go deletion + .env.example + dangling-doc fix</name>
  <files>scripts/loop_budget_smoke.sh, internal/agent/loop.go, .env.example, .planning/codebase/STRUCTURE.md</files>
  <read_first>
    - scripts/neo4j_smoke.sh (the `#!/usr/bin/env bash` + `set -euo pipefail` + delegate-to-Go idiom to mirror; Git-Bash compatible) + scripts/check-file-size.sh
    - .planning/phases/02-agent-cornerstone/02-SPEC.md § Acceptance SC#2 (smoke: 26 lines, grep `"limit_hit":"max_steps"`, final Event shape) + § Boundaries (loop.go deletion + main.go cleanup are explicit deliverables) + § Acceptance (`.env.example` must document AURA_LOOP_MAX_STEPS=25 / MAX_WALLCLOCK_SEC=300 / DEDUP_WINDOW=3)
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md `<specifics>` smoke (exactly 26 lines = 25 step + 1 budget-exhausted; grep limit_hit:max_steps; final Event author + StateDelta) + A7 (the extra env vars: AURA_LOOP_DEDUP_EXEMPT_TOOLS, AURA_LOOP_BRANCH_SOFT_FRACTION, AURA_LOOP_NODE_TIMEOUT_SEC, AURA_LOOP_DEDUP_RESULT_CAP)
    - .planning/phases/02-agent-cornerstone/02-RESEARCH.md § Runtime State Inventory (README.md / STRUCTURE.md L24,L335 reference internal/agent/loop.go — will dangle after deletion; fix the reference) + § Common Pitfalls (none new here)
    - .planning/phases/02-agent-cornerstone/02-PATTERNS.md § Shared Patterns › Smoke-script idiom (wc -l == 26, grep)
    - internal/agent/loop.go (the 132-LOC file to delete)
  </read_first>
  <action>
    Create scripts/loop_budget_smoke.sh: `#!/usr/bin/env bash` + `set -euo pipefail`; run `go run ./cmd/aura agent dry-run --max-steps 25`, capture stdout, assert line count `== 26` via `wc -l` (25 step Events + 1 budget-exhausted), and `grep '"limit_hit":"max_steps"'` must succeed; exit non-zero with a clear message on mismatch. Must actually RUN (no t.Skip / no skip-as-green — CLAUDE.md). Delete `internal/agent/loop.go` (the Phase-1 `Loop` struct; `git rm`). Append to `.env.example` the SPEC three vars `AURA_LOOP_MAX_STEPS=25`, `AURA_LOOP_MAX_WALLCLOCK_SEC=300`, `AURA_LOOP_DEDUP_WINDOW=3` plus the A7 vars `AURA_LOOP_DEDUP_EXEMPT_TOOLS=`, `AURA_LOOP_BRANCH_SOFT_FRACTION=`, `AURA_LOOP_NODE_TIMEOUT_SEC=`, `AURA_LOOP_DEDUP_RESULT_CAP=2048`, each with a one-line `AURA_<DOMAIN>_<UNIT>`-convention comment. Fix the dangling `internal/agent/loop.go` reference in `.planning/codebase/STRUCTURE.md` (point it at the new agent.go/workflow layout or remove the stale line). Also remove the `internal/agent/loop.go` exclusion line from `.golangci.yml` if present (the file no longer exists — stale exclusion).
  </action>
  <verify>
    <automated>bash scripts/loop_budget_smoke.sh && test ! -f internal/agent/loop.go && grep -q 'AURA_LOOP_MAX_STEPS' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/loop_budget_smoke.sh` exits 0; its stdout assertion confirms exactly 26 lines and `grep '"limit_hit":"max_steps"'` present (SC#2)
    - `internal/agent/loop.go` no longer exists: `test ! -f internal/agent/loop.go`
    - `.env.example` contains `AURA_LOOP_MAX_STEPS=25`, `AURA_LOOP_MAX_WALLCLOCK_SEC=300`, `AURA_LOOP_DEDUP_WINDOW=3` plus the four A7 vars
    - `.planning/codebase/STRUCTURE.md` no longer references `internal/agent/loop.go` as a live file
    - the smoke script contains `set -euo pipefail` and actually invokes `aura agent dry-run` (no skip)
  </acceptance_criteria>
  <done>SC#2 smoke green (26 lines + grep), loop.go deleted, env vars documented (SPEC 3 + A7), dangling doc/lint references fixed.</done>
</task>

<task type="auto">
  <name>Task 3: PRD amendments A1–A7 (prd.md) — PRD-first discipline</name>
  <files>prd.md</files>
  <read_first>
    - .planning/phases/02-agent-cornerstone/02-CONTEXT.md § PRD-Amendments Required (A1–A7, the exact corrections: A1 exported structs; A2 two-tier dedup + period-2 + exempt; A3 canonicaljson rescope drop RFC-8785; A4 8-byte SpanID + MessageID/ToolCallID/ThreadID; A5 soft cap + decrement-then-check + wallclock-via-deadline + AURA_LOOP_NODE_TIMEOUT_SEC; A6 uuid is a new direct dep + errgroup already present; A7 the four new env vars)
    - CLAUDE.md § PRD-first principle + § Commit discipline (PRD-amendment commit before/with the code commit)
    - prd.md § Slice 0.9 (the section to amend) + § Caps & Limits env catalog (where A7 env vars are catalogued)
  </read_first>
  <action>
    Apply the A1–A7 amendments to `prd.md` so the truth-source reflects the research-backed corrections discovered during discuss-phase (CLAUDE.md PRD-first: deviations require a PRD-amendment before/with the code commit). For each: A1 — note workflow structs are EXPORTED (SequentialAgent etc.), not unexported. A2 — the dedup is two-tier (sha256(name+canonical_args) + result-as-veto) + period-2 ping-pong + AURA_LOOP_DEDUP_EXEMPT_TOOLS, NOT (name,args,result)-in-hash. A3 — canonical JSON lives in internal/canonicaljson as an internal deterministic serializer; drop the RFC-8785 framing. A4 — SpanID/ParentSpanID are 8 bytes (crypto/rand), not UUIDv7; add MessageID/ToolCallID/ThreadID for AG-UI forward-compat. A5 — Budget gains per-branch soft cap, decrement-then-check-then-restore ConsumeStep, wallclock via context.WithDeadline + optional AURA_LOOP_NODE_TIMEOUT_SEC. A6 — github.com/google/uuid is a NEW direct dep (the prior "already in chain via migrate v4" claim was wrong); golang.org/x/sync errgroup is already present. A7 — add AURA_LOOP_DEDUP_EXEMPT_TOOLS, AURA_LOOP_BRANCH_SOFT_FRACTION, AURA_LOOP_NODE_TIMEOUT_SEC, AURA_LOOP_DEDUP_RESULT_CAP to the env catalog. Keep edits surgical (amend the relevant Slice 0.9 + env-catalog lines; do not rewrite unrelated sections). This task touches ONLY prd.md.
  </action>
  <verify>
    <automated>grep -q 'AURA_LOOP_DEDUP_EXEMPT_TOOLS' prd.md && grep -q 'AURA_LOOP_BRANCH_SOFT_FRACTION' prd.md</automated>
  </verify>
  <acceptance_criteria>
    - `prd.md` Slice 0.9 section reflects exported workflow structs (A1), two-tier dedup (A2), internal/canonicaljson rescope without RFC-8785 (A3), 8-byte SpanID + MessageID/ToolCallID/ThreadID (A4), soft cap + decrement-then-check + wallclock-deadline (A5)
    - `prd.md` notes `github.com/google/uuid` as a new direct dep (A6)
    - `prd.md` env catalog contains all four A7 vars: `grep -q 'AURA_LOOP_DEDUP_EXEMPT_TOOLS' prd.md`, `AURA_LOOP_BRANCH_SOFT_FRACTION`, `AURA_LOOP_NODE_TIMEOUT_SEC`, `AURA_LOOP_DEDUP_RESULT_CAP`
    - no `.go` files changed by this task (prd.md only)
  </acceptance_criteria>
  <done>A1–A7 amendments landed in prd.md (truth-source synced), env catalog updated, surgical edits only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags/env → Budget | `--request-id` and `--max-*` flags + `AURA_LOOP_*` env parsed at the entry point |
| dry-run output → CI grep | the smoke script's 26-line + grep contract is the SC#2 machine-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | malformed `AURA_LOOP_*` or `--max-steps` crashing the entry point | mitigate | `NewBudgetFromEnv` fail-fast (D-06) returns an error → `runAgent` exits 1 cleanly; CLI flags validated (-1 sentinel) |
| T-02-16 | Spoofing | predictable/forgeable request_id breaking trace correlation | mitigate | `uuid.NewV7()` from `crypto/rand` (via google/uuid) for `auto`; literal UUID parsed/validated for reproducibility (SC#4) |
| T-02-17 | Tampering | skip-as-green smoke test exercising nothing | mitigate | `scripts/loop_budget_smoke.sh` actually runs `aura agent dry-run` and asserts line count + grep (CLAUDE.md NO-SKIP-AS-GREEN); a sub-second/empty run fails the assertion |
</threat_model>

<verification>
- `bash scripts/loop_budget_smoke.sh` exits 0 (SC#2: 26 lines + grep limit_hit:max_steps)
- `go run ./cmd/aura agent dry-run --request-id auto` every line UUIDv7, all equal (SC#4)
- `go test ./cmd/aura/` green; `go build ./...` clean; `go vet ./...` clean
- `bash scripts/check-file-size.sh` clean (agent.go ~90, agent_test.go ≤250)
- `golangci-lint run ./internal/agent/... ./cmd/aura/...` clean (config exists; loop.go exclusion removed)
- `test ! -f internal/agent/loop.go` (deletion confirmed)
</verification>

<success_criteria>
- SC#4: `aura agent dry-run` emits UUIDv7-correlated Event lines, CLI>env>default precedence (D-06)
- SC#2: smoke script green (26 lines + grep), actually runs (no skip)
- loop.go deleted, chat/stubClient removed, agent dispatch wired, env vars documented; A1–A7 PRD amendments landed (PRD-first)
</success_criteria>

<output>
Create `.planning/phases/02-agent-cornerstone/02-07-SUMMARY.md` when done
</output>
