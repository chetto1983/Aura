---
phase: 08-sandbox-2b-session-bound
plan: 07
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - sandbox/sidecar.py
  - internal/sandbox/sandbox.go
  - internal/sandbox/docker.go
  - internal/sandbox/docker_test.go
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "The sidecar holds a long-lived per-session Python namespace dict: x=42 set in call 1 is readable as x in call 2 via /session/{id}/exec/python (in-memory state survives)"
    - "Shell exec stays subprocess.run per call but re-applies the per-session API-managed cwd; cd/export do NOT persist (asymmetric persistence, D-02)"
    - "The Runner interface + Result are extended additively — the 2a stateless RunPython/RunShell signatures and callers keep compiling"
    - "DockerRunner gains a per-session exec path (POST /session/{id}/exec/{lang}) alongside the existing /exec/{lang}; sidecar stays Python-stdlib-only"
  artifacts:
    - path: "sandbox/sidecar.py"
      provides: "per-session interpreter (exec into namespace dict) + /session/{id}/exec/{lang} router + threading.Lock guard; stays stdlib-only"
      contains: "/session/"
    - path: "internal/sandbox/docker.go"
      provides: "session-bound exec HTTP path (POST /session/{id}/exec/{lang})"
      contains: "/session/"
    - path: "internal/sandbox/sandbox.go"
      provides: "additively-extended Runner/Result for session-bound exec"
  key_links:
    - from: "internal/sandbox/docker.go session exec"
      to: "sandbox/sidecar.py /session/{id}/exec/{lang}"
      via: "HTTP POST (execution stays HTTP, D-05)"
      pattern: "/session/"
---

<objective>
Give the sidecar a long-lived per-session Python interpreter (D-01/D-02/D-03) and the Go runner a session-bound exec HTTP path. The sidecar keeps one namespace `dict` per `session_id`; `exec(compile(code,...), ns)` so `x=42` (call 1) lives in `ns["x"]` (call 2). Shell stays `subprocess.run` per call but re-applies a per-session API-managed cwd — asymmetric persistence (python vars persist; shell `cd`/`export` do NOT), documented to the model. Execution stays HTTP-only (D-05 — Go drives lifecycle via docker CLI in 08-05, but NEVER execution). Sidecar stays Python-stdlib-only (NO IPython/Jupyter — D-03 invariant).

Maps ROADMAP criterion 1 (session persistence — the interpreter half; the container-reuse half is 08-05). Live persistence assertions are 08-09; this plan ships the sidecar + runner LOGIC + the stdlib-only unit coverage. Shares no file with the parallel Wave-3 plans (08-05, 08-06).

Purpose: stateful per-session execution. Output: extended sidecar.py + docker.go + sandbox.go.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/08-sandbox-2b-session-bound/08-PATTERNS.md
@.planning/phases/05-sandbox-2a-stateless/05-CONTEXT.md
@.claude/skills/golang-error-handling/SKILL.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-session interpreter in sidecar.py (stdlib-only)</name>
  <read_first>
    - sandbox/sidecar.py — `INTERPRETERS` dispatch (lines 48-51), `run_code` (88-136, the `subprocess.run` + 1 MiB truncation + timeout/oom/pids `limit_hit`), `Handler.do_POST` (158+), `_send_json`/`do_GET /healthz`/`MAX_BODY_BYTES`/`MAX_STREAM` machinery (139-169). Read in FULL — keep all this intact.
    - 08-RESEARCH.md Pattern 2 (persistent stdlib interpreter: `exec(compile(code,"<session>","exec"), ns)`; redirect `sys.stdout`/`sys.stderr` to `io.StringIO` around the exec; shell stays subprocess.run with per-session `cwd=session_cwd`; guard the per-session namespace map with a `threading.Lock` for a 2nd concurrent Aura process — A6).
    - 08-PATTERNS.md `sandbox/sidecar.py` section (the router: static `INTERPRETERS.get(self.path)` → prefix parse for `/session/{id}/exec/{lang}`).
    - 05-CONTEXT.md D-20/D-20b — sidecar stays stdlib-only; the baked pip is net-blocked in 2a, net-allowed via the proxy in 2b.
  </read_first>
  <action>
    Extend `sandbox/sidecar.py` (stays Python-stdlib-only — NO IPython/Jupyter, D-03). Add a module-level `SESSIONS` dict: `session_id -> {"ns": {}, "cwd": <workspace path>}` guarded by a `threading.Lock` (defense against a 2nd concurrent Aura process — the Go container lock is Aura-local, A6). Add a small router in `do_POST` that recognizes `POST /session/{id}/exec/{lang}` (prefix parse) alongside the existing static `/exec/{lang}`. For `lang==python`: get-or-create the session `ns` dict; redirect `sys.stdout`/`sys.stderr` to `io.StringIO` around `exec(compile(code, "<session-"+id+">", "exec"), ns)` so `x=42` set in call 1 lives in `ns["x"]` for call 2 (in-memory state survives — D-01); capture stdout/stderr/exception → the SAME JSON contract as `run_code` (stdout/stderr/exit_code/elapsed_ms/truncated/limit_hit; reuse the 1 MiB `MAX_STREAM` truncation + `_send_json`). For `lang==shell`: keep `subprocess.run` per call BUT pass `cwd=session["cwd"]` (the per-session API-managed working dir) — `cd`/`export` inside the snippet do NOT persist; only the tracked cwd is re-applied (D-02 asymmetric). The stateless `/exec/{lang}` path stays UNCHANGED (2a callers). Keep `MAX_BODY_BYTES`/`do_GET /healthz`/timeout handling. Add a Python stdlib `unittest` covering: python session persistence (`x=42` then `print(x)`==42 across two calls to the same session), shell cwd re-application, an exception in python exec captured (not crashing the server), unknown path → 404.
  </action>
  <verify>
    <automated>cd /d/Aura && python -m py_compile sandbox/sidecar.py && grep -c "/session/" sandbox/sidecar.py && (grep -qE "IPython|ipykernel|jupyter|import zmq" sandbox/sidecar.py && echo "STDLIB-VIOLATION" || echo "stdlib-only OK")</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile sandbox/sidecar.py` exits 0.
    - sidecar.py has a `/session/{id}/exec/{lang}` route + a `SESSIONS` map guarded by `threading.Lock`; the stateless `/exec/{lang}` path is unchanged.
    - The sidecar imports NO IPython/ipykernel/jupyter/zmq (grep clean — stdlib-only, D-03).
    - The stdlib unittest asserts python `x=42` persists across two same-session calls; shell cwd re-applied; python exception captured into the JSON result (server stays up); unknown path → 404.
  </acceptance_criteria>
  <done>The sidecar serves session-bound exec with a persistent Python namespace + asymmetric shell cwd, stdlib-only; stateless path intact.</done>
</task>

<task type="auto">
  <name>Task 2: DockerRunner session exec path + additive Runner/Result extension</name>
  <read_first>
    - internal/sandbox/sandbox.go (lines 10-30) — the `Runner` interface (RunPython/RunShell + timeoutSec) + `Result` struct (the four original fields the comment says to keep; Truncated/LimitHit) — extend ADDITIVELY.
    - internal/sandbox/docker.go (lines 67-71 RunPython/RunShell, 99-135 `exec` HTTP path + ErrSandboxUnreachable/ErrSandboxProtocol wrapping, 140-165 effectiveTimeout/post, 153-154 the `r.url+"/exec/"+lang` URL build) — the analog to extend for `/session/{id}/exec/{lang}`.
    - internal/sandbox/errors.go — the sentinels (incl. the new ones from 08-02) for the session exec path's error taxonomy.
    - internal/agent/tools/execute.go + cmd/aura/exec.go — the 2a callers of RunPython/RunShell that MUST keep compiling (additive only).
    - 08-PATTERNS.md `internal/sandbox/sandbox.go` + `internal/sandbox/docker.go` sections (extend additively, do not break the stateless path).
  </read_first>
  <action>
    Extend `internal/sandbox/sandbox.go` ADDITIVELY: add session-aware methods to the `Runner` interface WITHOUT altering the existing `RunPython(ctx,code,timeoutSec)`/`RunShell(ctx,cmd,timeoutSec)` signatures — e.g. add `RunPythonSession(ctx, sessionID, code string, timeoutSec int) (Result, error)` + `RunShellSession(...)` (or a single session-scoped method taking lang). `Result` MAY gain optional advisory fields (the `{risk_tier, gate_recommended}` carrier the execute tool reads in 08-08) but the four original fields stay (comment "keep the agent-loop binding intact"). In `internal/sandbox/docker.go`, add the session exec HTTP path: a `sessionExec(ctx, sessionID, lang, code, timeoutSec)` mirroring the existing `exec` but POSTing to `r.url+"/session/"+sessionID+"/exec/"+lang` (execution stays HTTP — D-05; lifecycle is the SessionManager's docker CLI from 08-05, NEVER here). Reuse `effectiveTimeout`/`post`/`_send_json` decoding + the SAME `ErrSandboxUnreachable`/`ErrSandboxProtocol` taxonomy. Keep all 2a callers compiling. Update `docker_test.go` with a unit test using `httptest.Server` asserting the session exec path POSTs to `/session/{id}/exec/{lang}` and decodes the Result 1:1 (no live container needed — that's 08-09).
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test -run 'TestRunner' ./internal/sandbox/ && grep -c "/session/" internal/sandbox/docker.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 with the existing 2a callers (execute.go, exec.go) unchanged-and-compiling (additive extension).
    - `docker.go` POSTs session exec to `/session/{id}/exec/{lang}` (grep) reusing the existing timeout/decode/error-taxonomy machinery.
    - A `docker_test.go` `httptest` test asserts the session path URL + 1:1 Result decode.
    - The four original `Result` fields are unchanged; any added fields are optional/additive.
  </acceptance_criteria>
  <done>DockerRunner can drive session-bound exec over HTTP; Runner/Result extended additively; 2a path intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md)

| Boundary | Description |
|----------|-------------|
| Go runner ↔ sidecar (session path) | `docker.go` POSTs `{code,timeout_sec}` to `/session/{id}/exec/{lang}` over loopback; execution stays HTTP (D-05). Untrusted code runs in the persistent interpreter inside the hardened gVisor container (boundary from 08-05). |
| sidecar session map ↔ concurrent Aura processes | per-session namespace guarded by a `threading.Lock` (the Go container lock is Aura-local; a 2nd Aura process is defended sidecar-side, A6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-EOP-EXEC | Elevation of Privilege | persistent interpreter | mitigate | the interpreter runs inside the same hardened gVisor container (seccomp + cap_drop ALL + non-root 65532 + read-only rootfs from 08-05 docker run flags); the sidecar stays stdlib-only (no IPython/ZMQ attack surface, D-03). |
| T-08-07-INFO-XSESSION | Information Disclosure | cross-session namespace leak | mitigate | one namespace dict per `session_id`, keyed map; one container per conversation (08-05) so even map mixups cannot cross conversations. |
| T-08-07-TAMPER-WIRE | Tampering | sidecar JSON response | mitigate | session path reuses the 2a `ErrSandboxProtocol` non-2xx/undecodable taxonomy + loopback-only port (extends T-05-03-V13-WIRE). |
| T-08-07-DOS | Denial of Service | runaway session code | mitigate | reuses the 2a per-call timeout + 1 MiB stream truncation + container 512m/1cpu/64pids (from 08-05 docker run); idle interpreter bounded by the reaper (08-05). |
| T-08-07-SC | Tampering | sidecar package / go module installs | accept | sidecar stays Python-stdlib-only (no new pip — grep-enforced); docker.go uses stdlib net/http; no new go module. |
</threat_model>

<verification>
- `python -m py_compile sandbox/sidecar.py` + stdlib unittest green; no IPython/zmq import.
- `go build ./... && go test ./internal/sandbox/` green; 2a callers compile.
</verification>

<success_criteria>
The sidecar serves stateful per-session exec (persistent python ns + asymmetric shell cwd) stdlib-only; the runner drives it over a session HTTP path additively; live persistence assertions deferred to 08-09.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-07-SUMMARY.md` when done
</output>
