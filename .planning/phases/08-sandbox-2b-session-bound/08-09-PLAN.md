---
phase: 08-sandbox-2b-session-bound
plan: 09
type: execute
wave: 5
depends_on: ["08-08"]
autonomous: false
files_modified:
  - internal/sandbox/sessions_integration_test.go
  - internal/sandbox/workspace_integration_test.go
  - internal/sandbox/network_integration_test.go
  - .github/workflows/sandbox.yml
  - .planning/phases/08-sandbox-2b-session-bound/08-SECURITY.md
  - docs/aura-quality-snapshot.md
requirements: [CAP-02]
must_haves:
  truths:
    - "Criterion 1: x=42 set in execute call 1 is readable in call 2 in the same conversation; a workspace file written in call 1 is visible in call 2 (live container, sandbox_integration tier)"
    - "Criterion 2: ln -s /etc /workspace/escape then aura chat delete leaves host /etc intact + the symlink removed (live cascade)"
    - "Criterion 3: after TTL with no activity, aura sandbox sessions list shows the session reaped, container removed, workspace cleaned"
    - "Criterion 4: network_allow pypi.org makes pip install to pypi succeed; same call curl 1.1.1.1 is refused (live egress through the proxy via the bridge gateway)"
    - "08-SECURITY.md exists with the extended threat register (all block-on-high threats mitigated, AR-05-01 extended); coverage >=85% owned-surface; mutation >=70% on network.go + scoring.go + sessions.go"
  artifacts:
    - path: "internal/sandbox/sessions_integration_test.go"
      provides: "live session persistence + boot recovery + TTL live reap (sandbox_integration / db_integration tiers, no-skip-as-green)"
      contains: "sandbox_integration"
    - path: ".planning/phases/08-sandbox-2b-session-bound/08-SECURITY.md"
      provides: "Phase 8 threat register extending 05-SECURITY (T-08-* dispositions, AR-05-01 extension, Claude-Code-bypass caveat)"
      contains: "AR-05-01"
    - path: ".github/workflows/sandbox.yml"
      provides: "gating DinD job running the 2b session tiers + live egress + mutation + coverage fold"
      contains: "sandbox_integration"
  key_links:
    - from: "internal/sandbox/network_integration_test.go"
      to: "live pypi.org via the proxy + bridge gateway"
      via: "TestNetwork_PyPIAllowed / TestNetwork_NonAllowlistRefused"
      pattern: "pypi|1.1.1.1"
---

<objective>
Gate-3 evidence + the live integration tier + 08-SECURITY. Author the live sandbox_integration/db_integration tests that prove the 4 ROADMAP success criteria against real containers; resolve the HIGHEST landmine #3 with a LIVE egress-bridge reachability spike (the session container reaching the host proxy via the bridge gateway, criterion 4); author 08-SECURITY.md extending the 2a register (block-on-high mitigations, AR-05-01 extension, the documented Claude-Code allowlist-bypass caveat); wire the gating CI DinD job; fold sandbox+scoring into the 85% coverage floor; run mutation >=70% on the security/decision-critical files; and hold the human-verify checkpoint for live sign-off (CAP-02 closes on the human's call).

Purpose: prove CAP-02 holds live, with no skip-as-green, and a complete threat register. Output: integration tests + CI job + 08-SECURITY + quality-snapshot rows.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/08-sandbox-2b-session-bound/08-VALIDATION.md
@.planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md
@.planning/phases/05-sandbox-2a-stateless/05-SECURITY.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-security/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live sandbox_integration tier for the 4 ROADMAP criteria + egress-bridge spike</name>
  <read_first>
    - internal/sandbox/docker_integration_test.go (lines 1-43: the sandbox_integration build tag + the sidecarURL no-skip-as-green helper that t.Fatal under CI + goleak TestMain) — the exact tier scaffolding to copy for the session/workspace/network tiers + the db_integration env helper.
    - 08-RESEARCH.md Validation Architecture Phase Requirements to Test Map (the named test commands per criterion: TestSessions_PythonStatePersists / WorkspacePersists / ConcurrentSerialized, TestWorkspace_SymlinkEscapeCascade, TestReaper_LiveContainerRemoved, TestNetwork_PyPIAllowed / NonAllowlistRefused, TestSessions_BootRecovery) + Pitfall 1 (the live egress-bridge reachability spike — A2 HIGH) + Pitfall 7 (live pypi DNS rotation validation).
    - 08-VALIDATION.md Wave 0 Requirements (the live spike + the migration round-trip) + Per-Task Verification Map (populate it).
    - internal/sandbox/sessions.go, workspace.go, network.go, docker.go (the Wave-3/4 code under test).
  </read_first>
  <action>
    Author the live integration tier (copy the docker_integration_test.go sandbox_integration + db_integration build tags + the no-skip-as-green helper that calls t.Fatal under CI when env is unset). sessions_integration_test.go: TestSessions_PythonStatePersists (exec x=42 then print x equals 42 in the same conversation via the live SessionManager+sidecar — criterion 1a), TestSessions_WorkspacePersists (write a file in call 1, read it in call 2 — criterion 1b), TestSessions_ConcurrentSerialized (with -race, two goroutines same session serialize — criterion 1c), TestReaper_LiveContainerRemoved (force TTL, assert docker rm + workspace cleaned + sessions list empty — criterion 3 live), TestSessions_BootRecovery (db tier: active rows become terminated, lazy recreate — boot). workspace_integration_test.go: TestWorkspace_SymlinkEscapeCascade live (exec ln -s /etc /workspace/escape in the container, then run the host cascade, assert host /etc intact + the symlink removed — criterion 2). network_integration_test.go: TestNetwork_PyPIAllowed (network_allow pypi.org, live pip install to pypi succeeds through the proxy reached at the bridge gateway — criterion 4a + the LANDMINE 3 live reachability spike: if the container cannot reach the proxy this is the failing test that proves the bridge/seccomp posture wrong, fix the posture not the test), TestNetwork_NonAllowlistRefused (same session, curl 1.1.1.1 / urlopen example.com refused — criterion 4b). Add a db-tier migration round-trip test for 0008 up/down. Populate 08-VALIDATION.md Per-Task Verification Map (Task IDs, threat refs, commands, status) + set nyquist_compliant true + wave_0_complete true.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet -tags 'sandbox_integration db_integration' ./internal/sandbox/ && go build -tags 'sandbox_integration db_integration' ./internal/sandbox/</automated>
  </verify>
  <acceptance_criteria>
    - All three integration test files carry the sandbox_integration build tag + the no-skip-as-green helper (t.Fatal under CI when env unset).
    - The 8 named tests exist (criterion 1a/1b/1c, 2, 3, 4a/4b, boot) and compile under the tags.
    - The migration round-trip test for 0008 up/down exists (db tier).
    - 08-VALIDATION.md Per-Task Verification Map is populated; frontmatter nyquist_compliant + wave_0_complete are true.
    - The TestNetwork_PyPIAllowed test embodies the landmine 3 live reachability spike (proxy reached at the bridge gateway).
  </acceptance_criteria>
  <done>The live tier proves all 4 criteria; the egress-bridge reachability spike is a real failing-or-passing test, not an assumption.</done>
</task>

<task type="auto">
  <name>Task 2: 08-SECURITY.md + gating CI DinD job + coverage fold + mutation</name>
  <read_first>
    - .planning/phases/05-sandbox-2a-stateless/05-SECURITY.md (the full register format + AR-05-01 + the tracked-obligation section to EXTEND) + the T-08-* threat blocks authored across 08-01..08-08 PLAN threat_models.
    - .github/workflows/sandbox.yml (the 2a gating DinD job: runsc install + daemon.json userns-remap + QEMU arm64 + live sidecar + sandbox_integration tier + bench + mutation + 85% coverage fold + CI=true no-skip env) — the analog to extend for the 2b session/workspace/network tiers + the live egress test.
    - docs/aura-quality-snapshot.md (the living-doc rows; the 2a escape-rate/mutation/QEMU caveat format to append the 2b rows to).
    - CLAUDE.md quality gates (make quality-full, coverage_gate.sh 85% owned-surface floor, go-mutesting on WSL, no-skip-as-green) + 08-RESEARCH Sampling Rate (mutation >=70% on network.go + scoring.go; add sessions.go).
    - 08-DECISIONS-WAVE0.md (the egress posture + privacy-mode decisions to record in 08-SECURITY).
  </read_first>
  <action>
    Author 08-SECURITY.md extending 05-SECURITY: import the T-08-* threats from the 8 PLAN threat_models into a single register with dispositions (mitigate/accept/transfer); EVERY block-on-high threat must have a concrete mitigation citing the implementing file (workspace symlink to os.Root cascade test; docker socket to never-mount; egress exfil/rebinding to CONNECT validation + resolve-then-pin + deny-wins; gVisor inheritance to docker run --runtime; the connect-re-enable to host-proxy containment); EXTEND AR-05-01 with the 2b session-container network/seccomp posture (connect allowed because egress is host-proxy-contained, empty allowlist keeps egressless) and record the live-spike result; add the documented Claude-Code allowlist-bypass / data-exfil caveat (D-10) as an accepted-with-mitigation note; set threats_open 0 only after the live tier is green. Extend .github/workflows/sandbox.yml: add the 2b session/workspace/network sandbox_integration tier + the live egress (pypi allowed, 1.1.1.1 refused) leg + the 0008 db migration round-trip + go-mutesting on network.go/scoring.go/sessions.go (hard-fail under 70% under CI) + fold the new packages into the 85% coverage gate; export the new AURA_SANDBOX_* env + CI=true (no-skip-as-green). Append the 2b rows to docs/aura-quality-snapshot.md (coverage percent, mutation scores, egress-spike result, the AR-05-01 extension note).
  </action>
  <verify>
    <automated>cd /d/Aura && test -f .planning/phases/08-sandbox-2b-session-bound/08-SECURITY.md && grep -c AR-05-01 .planning/phases/08-sandbox-2b-session-bound/08-SECURITY.md && grep -c sandbox_integration .github/workflows/sandbox.yml</automated>
  </verify>
  <acceptance_criteria>
    - 08-SECURITY.md exists with a T-08-* register; every block-on-high threat (workspace symlink, docker socket, egress exfil/rebinding, gVisor inheritance, connect re-enable) has a concrete mitigation citing an implementing file.
    - AR-05-01 is EXTENDED with the 2b session-container network/seccomp posture + the live-spike result; the Claude-Code allowlist-bypass caveat is recorded.
    - .github/workflows/sandbox.yml runs the 2b session/workspace/network tiers + live egress + 0008 migration round-trip + mutation + folds the new packages into the 85% coverage gate; CI=true exported (no-skip-as-green).
    - docs/aura-quality-snapshot.md has the 2b rows (coverage, mutation, egress-spike).
  </acceptance_criteria>
  <done>08-SECURITY closes all block-on-high threats; the CI job gates the 2b live tiers + mutation + coverage; quality-snapshot updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live Gate-3 sign-off (CAP-02 close)</name>
  <action>This is a human-verify checkpoint: run the live Gate-3 confirmation steps in <how-to-verify> in WSL with the stack up. The executor performs no code changes here; it presents the steps, waits for the operator to run them and report results, and on "approved" marks CAP-02 closed (or routes a gap-closure plan on failure).</action>
  <what-built>Session-bound sandbox: per-conv gVisor containers, persistent interpreter, workspace mount, host-proxy egress allowlist, os.Root symlink-safe cascade, TTL reaper, scoring advisory, CLI. The live tier + CI job + 08-SECURITY are authored; this checkpoint is the human-run live confirmation.</what-built>
  <how-to-verify>
    In WSL with the stack up (export PATH ~/.local/bin:~/go/bin, composed DSNs, CI=true):
    1. `go test -race -tags 'sandbox_integration db_integration' ./internal/sandbox/ ./internal/scoring/ ./internal/conversations/` — all green, no sub-second skip tells (verify execution, not just PASS).
    2. Criterion 1: `aura exec --session <conv> python "x=42"` then `aura exec --session <conv> python "print(x)"` prints 42; a file written in call 1 is read in call 2.
    3. Criterion 2: in-session `ln -s /etc /workspace/escape` then `aura chat delete <conv>` — confirm host /etc intact + workspace cleared.
    4. Criterion 3: idle past AURA_SANDBOX_SESSION_TTL_SEC, then `aura sandbox sessions list` shows it reaped (container gone via docker ps, workspace dir gone).
    5. Criterion 4: a session with network_allow pypi.org does `pip install` successfully; `curl 1.1.1.1` in the same session is refused.
    6. `make quality-full` green: combined coverage >=85% with sandbox+scoring folded; golangci-lint 0; mutation >=70% on network.go/scoring.go/sessions.go.
    7. Confirm 08-SECURITY threats_open 0 and the AR-05-01 extension matches the live posture.
  </how-to-verify>
  <resume-signal>Type "approved" to close CAP-02, or describe failures (which criterion, which test) to route a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md / AR-05-01)

| Boundary | Description |
|----------|-------------|
| CI live runtime ↔ 2b boundary | the gating DinD job runs the real session containers + live egress + mutation; no-skip-as-green hard-fails under CI when env is unset (verify execution, not PASS). |
| session container ↔ host proxy (live) | the egress-bridge reachability spike is a live test (criterion 4); a failure proves the seccomp/bridge posture wrong (fix posture, not test). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-FALSEGREEN | Repudiation | integration / CI skip | mitigate | the sandbox_integration + db_integration helpers t.Fatal under CI when env unset; sub-second runtime treated as a skip tell; the CI job exports the exact env the tests read (composed DSNs + AURA_SANDBOX_*). |
| T-08-09-INFO-EGRESS-LIVE | Information Disclosure | live egress allowlist bypass | mitigate | TestNetwork_PyPIAllowed/NonAllowlistRefused run the real proxy + bridge; the Claude-Code-bypass caveat documented in 08-SECURITY; deny-wins + resolve-then-pin exercised live. |
| T-08-09-EOP-SYMLINK-LIVE | Elevation/Tampering | live cascade symlink escape | mitigate | TestWorkspace_SymlinkEscapeCascade plants a real symlink in a live container then runs the host cascade, asserting /etc survives (ROADMAP crit 2 live). |
| T-08-09-MUTSCORE | Repudiation | test adequacy on critical files | mitigate | go-mutesting hard-fails under 70% killed on network.go/scoring.go/sessions.go (security/decision-critical) in the CI job; snapshot records the score. |
| T-08-09-AR0501 | Repudiation | egress posture documentation gap | mitigate | AR-05-01 is EXTENDED for 2b (not silently shipped — the 08-01 amendment landed the egress pivot in the PRD first; 08-SECURITY records the live posture), closing the non-repudiation gap the 2a audit flagged. |
| T-08-09-SC | Tampering | runsc/qemu/image/go-module installs | accept | all CI installs from the approved 2a sources (runsc Google GPG apt, qemu-user-static Debian apt, python:3.12-slim Docker Official, go-mutesting Makefile-pinned); no new app dependency. |
</threat_model>

<verification>
- `go test -race -tags 'sandbox_integration db_integration' ./internal/sandbox/ ./internal/scoring/ ./internal/conversations/` green live; no skip tells.
- `make quality-full` green: coverage >=85%, lint 0, mutation >=70% on the 3 critical files.
- 08-SECURITY threats_open 0 (after live green); CI job gates the 2b tiers.
</verification>

<success_criteria>
All 4 ROADMAP criteria are proven live with no skip-as-green; 08-SECURITY closes every block-on-high threat and extends AR-05-01; the CI job + coverage + mutation gates are green; CAP-02 closes on the human's sign-off.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-09-SUMMARY.md` when done
</output>
