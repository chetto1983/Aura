---
phase: 08-sandbox-2b-session-bound
plan: 11
type: execute
wave: 7
gap_closure: true
depends_on: ["08-10"]
autonomous: false
requirements: [CAP-02]
files_modified:
  - internal/sandbox/sessions_live_integration_test.go
  - internal/sandbox/sessions.go
  - internal/sandbox/sessions_test.go
  - .github/workflows/sandbox.yml
  - .planning/phases/08-sandbox-2b-session-bound/08-SECURITY.md
  - .planning/ROADMAP.md
  - docs/aura-quality-snapshot.md
user_setup:
  - service: native-linux-docker-daemon
    why: "CAP-02 criteria 1b/2/4a require a REAL host workspace bind-mount. sessions.go workspaceMountSpec uses the local-volume `o=bind,nosuid,nodev,noexec` form whose `device=<hostPath>` is resolved DAEMON-SIDE; on Docker Desktop (daemon in a separate WSL2 VM) BOTH /home and /mnt/d device paths fail with `flags: 0x100e: no such file or directory`. The Gate-3 live tier therefore MUST run on a native-Linux daemon where host-path==daemon-path: the existing CI `sandbox.yml` DinD job (ubuntu-latest), or a deliberately stood-up native dockerd. This is an operator/CI environment contract, not code Claude can synthesize on the Docker-Desktop dev host."
    env_vars:
      - name: AURA_RUN_DIR
        source: "host run dir whose conversations/<id>/workspace subtree the per-conv container bind-mounts (CI: /tmp/aura-run); MUST equal the dir the workspace cascade test reads via runDirOrSkip"
      - name: AURA_SANDBOX_SESSION_IMAGE
        source: "freshly built session image baking the 08-10 Dockerfile /workspace + sidecar (CI: aura-sandbox:ci)"
      - name: AURA_SANDBOX_EGRESS_NETWORK
        source: "non-masquerading egress bridge name for criterion 4 (CI: aura-sandbox-egress)"
      - name: AURA_SANDBOX_PROXY_ENV
        source: "HTTP_PROXY/HTTPS_PROXY at <bridge-gateway-ip>:<proxy-port>, set after the host proxy is bound"
      - name: AURA_SANDBOX_SESSION_SECCOMP
        source: "path to sandbox/seccomp-session.json (connect-allowing variant)"
      - name: AURA_SANDBOX_NETWORK_ALLOW_HOSTS
        source: "CSV allowlist for criterion 4, e.g. pypi.org,files.pythonhosted.org"

must_haves:
  truths:
    - "The live `liveManager` helper (sessions_live_integration_test.go) injects a real `Workspace` ensurer (NewWorkspaceManager rooted at AURA_RUN_DIR, resolved by a new `sessionRunDir(t)` env-helper mirroring the existing sessionImage/sessionSeccomp/runDirOrSkip pattern), so `create()` calls EnsureDir and `runArgv` adds the `--mount` workspace bind. On a native-Linux daemon this makes /workspace WRITABLE, closing criterion 1b (TestSessions_WorkspacePersists) and unblocking criterion 2 (TestWorkspace_SymlinkEscapeCascade_Live) and 4a (pip --target /workspace). Production already wires this (cmd/aura/chat.go:129 sandboxSessionDeps Workspace) — the gap was ONLY the test helper. This REVISES 08-10's do-not-modify-liveManager constraint, which was scoped to forcing the SHARED endpoint-resolver design (BLOCKER-1), NOT the workspace wiring: injecting the Workspace ensurer makes the manager-under-test production-faithful and touches NO assertion and NO endpoint-resolver wiring."
    - "The mount source the test injects (NewWorkspaceManager(sessionRunDir(t),...)) is the SAME AURA_RUN_DIR the cascade test reads via runDirOrSkip, so the host PurgeConversationDir cascade (criterion 2) walks the very dir the container wrote into. sessionRunDir(t) is CI-fatal / local-skip per no-skip-as-green (an unset AURA_RUN_DIR t.Fatals under $CI, skips locally), so a skipped workspace tier never reports falsely green."
    - "SessionManager.Close gracefully TERMINATES its live session containers (stop+rm+MarkTerminated+Unregister) AFTER the reaper goroutine has stopped, so a finished/failed test never leaks an `aura-sandbox-sess-*` container that contaminates later runs (the observed 1a/1c spurious-FAIL-in-dirty-suite). This is production-correct graceful teardown (the prior model left orphans for the next boot's RecoverOnBoot sweep). It must NOT break goleak, the reaper eviction test (count already 0 by Close time), or the db-tier boot-recovery test (recoveryDocker no-op stop/remove). If Close-teardown proves to regress an existing test after 3 attempts, fall back to a test-only t.Cleanup in liveManager that force-removes the per-conv containers, documented in the SUMMARY."
    - "The existing CI `sandbox.yml` 2b DinD job (ubuntu-latest, native daemon, builds aura-sandbox:ci from ./sandbox, exports AURA_RUN_DIR + the egress env, runs `go test -race -tags 'sandbox_integration db_integration' ./internal/sandbox/`) is confirmed to run the FULL criteria set with the liveManager fix in place — no sub-second skip tells; if the job needs the workspace fix to be exercised it already will be once liveManager injects the ensurer. Any gap between the job's env and what the tests read is closed in this plan (the job is the Gate-3 host of record)."
    - "The full live Gate-3 on a native-Linux daemon is green: criteria 1a/1c/3/boot (already green on Docker Desktop) STILL green, AND 1b/2/4a/4b NOW green; no integration-criteria ASSERTION changed (only liveManager's manager-construction gained the Workspace dep + the new env helper). 08-SECURITY threats_open flips 0 + status verified; CAP-02 marked complete — on the human Gate-3 sign-off."
  artifacts:
    - path: "internal/sandbox/sessions_live_integration_test.go"
      provides: "liveManager injects Workspace: NewWorkspaceManager(sessionRunDir(t), 0); new sessionRunDir(t) env helper (AURA_RUN_DIR, CI-fatal/local-skip)"
      contains: "Workspace:"
    - path: "internal/sandbox/sessions.go"
      provides: "Close terminates live session containers after the reaper stops (graceful teardown, no orphan leak)"
      contains: "terminate"
  key_links:
    - from: "internal/sandbox/sessions_live_integration_test.go liveManager"
      to: "internal/sandbox/sessions.go create -> EnsureDir -> runArgv workspaceMountSpec (--mount nosuid,nodev,noexec)"
      via: "SessionDeps.Workspace = NewWorkspaceManager(sessionRunDir(t),0) — the missing live-path wiring (prod has it at chat.go:129)"
      pattern: "Workspace:|EnsureDir|--mount"
    - from: "internal/sandbox/sessions.go Close"
      to: "m.docker.stop/remove + store.MarkTerminated + endpoint().Unregister for each live session"
      via: "graceful teardown after reaper stop (no leftover-container contamination)"
      pattern: "terminate|stop|remove"
---

<objective>
Close the LAST CAP-02 gap a live Gate-3 run (08-10 post-merge) exposed: the do-not-modify live test helper `liveManager` builds the SessionManager with NO `Workspace` ensurer, so `create()` skips `EnsureDir`, `runArgv` adds no `--mount`, and the live session container's `/workspace` is the read-only baked image dir under `--read-only`. Result on EVERY daemon (Docker Desktop AND CI native Linux): criterion 1b (TestSessions_WorkspacePersists) and 2 (TestWorkspace_SymlinkEscapeCascade_Live) fail with `OSError [Errno 30] Read-only file system: '/workspace'` / `ln: ... Read-only file system`, and 4a (pip install --target /workspace) hits the same wall. PRODUCTION is correct — cmd/aura/chat.go:129 builds NewWorkspaceManager(cfg.RunDir,...) and sandboxSessionDeps injects it. The gap is ONLY the live test path.

This plan injects the Workspace ensurer into `liveManager` (matching the existing sessionImage/sessionSeccomp/runDirOrSkip env-helper pattern in the same package + build tags), making the manager-under-test production-faithful, then runs the true Gate-3 on a native-Linux daemon (the existing CI `sandbox.yml` DinD job, since Docker Desktop's daemon cannot resolve the local-volume `device=` host path — proven: `flags: 0x100e: no such file or directory` for both /home and /mnt/d). It also makes `SessionManager.Close` gracefully terminate live containers so a finished/failed test leaves no orphan that contaminates later runs.

It does NOT change any integration-criteria ASSERTION (the write/read /workspace, the ln -s plant, the host cascade, the pip install, the urlopen-refused checks are all byte-identical). It only adds the production dependency (Workspace) the test helper omitted — the legitimate, necessary fix.

Q&A REVISION (documented): 08-10 listed `liveManager` as do-not-modify. That constraint was scoped to forcing the SHARED `defaultSessionEndpoint` resolver design (BLOCKER-1 — preventing a workaround that edited the helper to inject a resolver). Injecting the Workspace ensurer is a DIFFERENT, orthogonal change: it touches neither the endpoint resolver nor any assertion; it restores parity with the production composition root. This revision is recorded here and in the SUMMARY; it is NOT a PRD amendment (D-04 workspace-mount semantics are unchanged).
</objective>

<execution_context>
@D:\Aura\.claude\get-shit-done\workflows\execute-plan.md
@D:\Aura\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md
@.planning/phases/08-sandbox-2b-session-bound/08-SECURITY.md
@.planning/phases/08-sandbox-2b-session-bound/08-10-SUMMARY.md
@.claude/skills/golang-testing/SKILL.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inject the Workspace ensurer into liveManager (close the read-only /workspace gap on the live path)</name>
  <read_first>
    - internal/sandbox/sessions_live_integration_test.go (lines 38-58 liveManager — builds SessionDeps WITHOUT Workspace; THIS is the gap. It already calls sessionRuntime(t)/sessionImage(t)/sessionSeccomp(t) — env helpers from docker_integration_test.go — so adding a Workspace from a sibling env helper matches the established pattern).
    - internal/sandbox/workspace_integration_test.go (lines 31-42 runDirOrSkip reads AURA_RUN_DIR, CI-fatal/local-skip; line 88 NewWorkspaceManager(runDir, 0) — the cascade test's mount-root contract the injected ensurer MUST agree with).
    - internal/sandbox/sessions.go (lines 40-47 WorkspaceEnsurer; lines 240-248 create's EnsureDir gate; lines 316-318 runArgv mount gate; 331-350 workspaceMountSpec — the mount only appears when create got a non-nil m.workspace).
    - internal/sandbox/workspace.go (NewWorkspaceManager(runDir, maxBytes); EnsureDir(convID) (string, error) — *WorkspaceManager satisfies WorkspaceEnsurer).
    - cmd/aura/chat.go (lines 129-130 + 190-194 — PROOF prod already wires Workspace: NewWorkspaceManager(cfg.RunDir,...) into sandboxSessionDeps; the test path is the only omission).
  </read_first>
  <behavior>
    - The live persistence/workspace/egress tests, when run on a native-Linux daemon with AURA_RUN_DIR set, now mount the host workspace at /workspace RW: a write in call 1 reads back in call 2 (1b); ln -s /etc /workspace/escape succeeds (exit 0, not 127/read-only) so the host cascade has a real planted symlink to purge (2); pip install --target /workspace/site succeeds (4a).
    - 1a/1c/3/boot (no /workspace dependency) STILL pass — the added mount is inert for them.
    - Unset AURA_RUN_DIR under $CI t.Fatals (no-skip-as-green); locally it skips. The injected mount-root equals the cascade test's runDirOrSkip root (same env var) so criterion 2 walks the container's actual writes.
  </behavior>
  <action>
    Add a `sessionRunDir(t *testing.T) string` helper (in sessions_live_integration_test.go OR reuse runDirOrSkip from workspace_integration_test.go — same package, same `sandbox_integration && db_integration` tags, so a direct call is fine; prefer reusing runDirOrSkip to avoid a duplicate env helper — DRY, CLAUDE.md REUSABLE CODE). In liveManager's SessionDeps literal, add `Workspace: NewWorkspaceManager(runDirOrSkip(t), 0),` (quota 0 = unbounded, matching the cascade test). Do NOT change liveManager's signature or any other field, and do NOT touch liveSessionExec or any test assertion. Confirm sessions_test.go's UNIT fakes still compile (the unit tier does not build the integration files; no change needed there unless a shared helper moves).

    NOTE the Q&A revision in a one-line comment at liveManager: the Workspace injection restores parity with cmd/aura/chat.go:129 (prod wiring); it is the 08-11 gap-closure for the read-only-/workspace live failure, orthogonal to the 08-10 shared-resolver design.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test -race ./internal/sandbox/ && grep -c "Workspace:" internal/sandbox/sessions_live_integration_test.go && grep -c "runDirOrSkip\|sessionRunDir" internal/sandbox/sessions_live_integration_test.go</automated>
  </verify>
  <acceptance_criteria>
    - liveManager's SessionDeps now sets Workspace to a real *WorkspaceManager rooted at the AURA_RUN_DIR the cascade test uses (runDirOrSkip), quota 0.
    - No liveManager signature change; no assertion change; liveSessionExec untouched; the endpoint-resolver wiring (08-10 BLOCKER-1) untouched.
    - Unit tier `go test -race ./internal/sandbox/` green (the integration files don't compile into the unit build; the change is build-tag-gated).
    - The Q&A revision (workspace injection restores prod parity, orthogonal to the do-not-modify-for-resolver constraint) is noted at the call site.
  </acceptance_criteria>
  <done>The live session manager mounts a writable host /workspace exactly as production does, so 1b/2/4a can pass on a native daemon; no criterion assertion changed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Graceful container teardown on SessionManager.Close (kill leftover-container contamination)</name>
  <read_first>
    - internal/sandbox/sessions.go (lines 181-189 Close — cancels the reaper ctx + waits on done, but does NOT stop/rm the live containers it owns; 354-377 Release/endpoint/liveCount; 60-67 session struct holds containerID + id).
    - internal/sandbox/sessions_reaper.go (evict — the canonical stop+rm+MarkTerminated+LoadAndDelete+count--+Unregister; Close teardown mirrors it but runs AFTER the reaper goroutine has exited, so no concurrent eviction).
    - internal/sandbox/sessions_recovery.go (RecoverOnBoot — the boot-side mass-clear pattern; Close is the shutdown-side analog but DOES stop/rm because the containers are this process's live ones, not crash orphans).
    - internal/sandbox/sessions_test.go (fakeDocker stop/remove are no-ops; the unit reaper/cap tests build managers and Close them — teardown must be a no-op-safe pass over an empty or fake-backed map).
    - internal/sandbox/sessions_integration_test.go (recoveryDocker no-op stop/remove; TestSessions_BootRecoveryMarksTerminated Closes its manager — teardown must not error on the no-op fake).
  </read_first>
  <action>
    In Close, AFTER `<-m.done` (the reaper has fully stopped — no concurrent evict), terminate every live session this manager owns. Add an unexported `terminateLiveSessions()` that, under capMu, Ranges m.sessions: for each *session, best-effort `m.docker.stop(ctx, s.containerID)` then `m.docker.remove(ctx, s.containerID)` then `m.store.MarkTerminated(ctx, s.id)` then `m.endpoint().Unregister(s.convID)` then `m.sessions.Delete(convID)` + clamp count toward 0. Use a FRESH `context.Background()` with a short timeout (the manager ctx is already cancelled by m.cancel()); do NOT take the per-session s.mu (teardown operates by container id, and an in-flight exec at shutdown is best-effort). WARN-log per-container failures (mirror RecoverOnBoot discipline); never panic. Keep Close idempotent (the m.started guard already gates the <-done; run teardown once inside that guard).

    This makes graceful shutdown leave no orphan `aura-sandbox-sess-*` container, eliminating the observed dirty-suite contamination (leftover containers from a finished/failed test making sibling 1a/1c spuriously FAIL). 3-STRIKE ESCAPE: if Close-teardown regresses goleak / the reaper test / the boot-recovery test and cannot be reconciled in 3 attempts, REVERT the Close change and instead add a t.Cleanup in liveManager that force-removes the created per-conv containers (pure test hygiene), documenting the choice in the SUMMARY.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test -race ./internal/sandbox/ && grep -c "terminateLiveSessions\|MarkTerminated" internal/sandbox/sessions.go</automated>
  </verify>
  <acceptance_criteria>
    - Close, after the reaper stops, stop+rm+MarkTerminated+Unregister's every live session under capMu using a fresh context; count clamps at 0; idempotent; goleak-clean.
    - The unit reaper/cap tests + the db-tier boot-recovery test still pass (no-op fakes tolerate the teardown).
    - go test -race ./internal/sandbox/ green; no leftover container after a live run (verified at Task 3).
    - If reverted per the 3-strike escape, the test-only t.Cleanup fallback + rationale are in the SUMMARY.
  </acceptance_criteria>
  <done>A finished or failed session test leaves no orphan container; the live suite is deterministic across repeated runs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Confirm the CI sandbox.yml DinD job is the Gate-3 host of record (native-Linux daemon)</name>
  <read_first>
    - .github/workflows/sandbox.yml (the 2b job ~lines 247-392: runs-on ubuntu-latest; env AURA_SANDBOX_SESSION_IMAGE=aura-sandbox:ci, AURA_RUN_DIR=/tmp/aura-run, AURA_SANDBOX_EGRESS_NETWORK, AURA_SANDBOX_SESSION_SECCOMP; builds aura-sandbox:ci from ./sandbox; the proxy-env export step; the `go test -race -tags 'sandbox_integration db_integration' ./internal/sandbox/` step). Confirm: it mkdir's AURA_RUN_DIR; the egress network is created non-masquerading; the host proxy is started + AURA_SANDBOX_PROXY_ENV exported BEFORE the tier; CI=true so the skip-helpers t.Fatal on missing env (no-skip-as-green).
    - internal/sandbox/network_integration_test.go (the egress env contract the job must satisfy for 4a/4b).
  </read_first>
  <action>
    Verify (read-only) that the DinD job's exported env exactly matches what the tests read after the Task-1 fix (AURA_RUN_DIR now ALSO drives the live mount source via liveManager, not just the cascade — confirm the job sets it before the 2b tier, which it does). If any gap exists (e.g. the job did not previously need AURA_RUN_DIR to be a real writable dir because the mount was never exercised, or the egress proxy/seccomp wiring is incomplete for 4a/4b), close it IN sandbox.yml: ensure mkdir -p "$AURA_RUN_DIR"; ensure the egress bridge + host proxy + AURA_SANDBOX_PROXY_ENV are wired before the tier; ensure the built image bakes the 08-10 Dockerfile /workspace. Do NOT weaken any gate; do NOT add t.Skip. If the job is already complete, record that (no-op) with the line references in the SUMMARY. This task writes ONLY sandbox.yml (CI), no Go.
  </action>
  <verify>
    <automated>cd /d/Aura && grep -c "AURA_RUN_DIR\|aura-sandbox:ci\|sandbox_integration && db_integration\|mkdir -p" .github/workflows/sandbox.yml</automated>
  </verify>
  <acceptance_criteria>
    - The 2b DinD job exports every var the post-fix tests read (AURA_RUN_DIR as a real writable dir, the session image, the egress bridge/proxy/seccomp), runs the full `sandbox_integration && db_integration` race tier under CI=true, with no t.Skip-as-green.
    - Any closed gap is a sandbox.yml-only edit; if already complete, the SUMMARY records the no-op with line refs.
  </acceptance_criteria>
  <done>The CI DinD job is confirmed to exercise all 4 ROADMAP criteria on a native-Linux daemon with the liveManager fix in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Live Gate-3 re-run on a native-Linux daemon + close CAP-02 (human sign-off)</name>
  <what-built>The live test path now mounts a writable host /workspace (liveManager injects the Workspace ensurer prod already had), Close terminates live containers (no orphan contamination), and the CI sandbox.yml DinD job is confirmed as the Gate-3 host. This checkpoint is the human-run LIVE confirmation that 1b/2/4a/4b now pass on a native daemon (Docker Desktop CANNOT — the local-volume device= path is daemon-side-unresolvable there: `flags: 0x100e: no such file or directory`).</what-built>
  <action>Human-verify checkpoint. The executor performs NO code changes here. It presents the two execution paths and waits for the operator to run ONE of them, then on "approved" performs the doc closeout: (1) flip 08-SECURITY threats_open to 0 + status verified; (2) confirm the 08-10 carve-outs (T-08-05-EOP-SOCKET 5th `port` verb, AR-08-02 DNS-pin scope, nosuid/nodev/noexec implemented) are recorded; (3) mark CAP-02 complete in ROADMAP.md + REQUIREMENTS.md; (4) update docs/aura-quality-snapshot.md with the green 2b row. The closeout doc edits are the ONLY writes after sign-off; NOT a PRD amendment. On failure, route a follow-up gap-closure plan naming the still-failing criterion.</action>
  <how-to-verify>
    Pick ONE native-Linux daemon path (Docker Desktop will NOT work for 1b/2/4a):

    PATH A — CI DinD (recommended, zero local infra risk): push the branch and let the `sandbox.yml` 2b job run on ubuntu-latest, OR trigger it via `gh workflow run sandbox.yml`. The job builds aura-sandbox:ci, sets AURA_RUN_DIR=/tmp/aura-run + the egress env, and runs `go test -race -tags 'sandbox_integration db_integration' ./internal/sandbox/` on a native daemon. Confirm the run is GREEN with multi-second (non-skip) timings. (Push is operator-initiated — Claude does not push without an explicit request.)

    PATH B — local native dockerd in WSL (invasive; only if you want a local green): install docker-ce + iptables in Ubuntu (via `wsl -u root`), start a native dockerd on an isolated socket/data-root, `DOCKER_HOST` it, `docker load`/build aura-sandbox:gate3 there, create the egress bridge, bind the host proxy at its gateway, then run the tier with the gate3 env (/mnt/d/tmp/gate3_env.sh + AURA_RUN_DIR on the Ubuntu fs). host-path==daemon-path holds, so the device= bind resolves.

    Either path must show, with CI=true (no skip tells):
    1. `go test -race -tags 'sandbox_integration db_integration' ./internal/sandbox/ ./internal/scoring/ ./internal/conversations/` — ALL green.
    2. 1b TestSessions_WorkspacePersists green (marker persists call→call on the bind-mount).
    3. 2 TestWorkspace_SymlinkEscapeCascade_Live green (ln -s exit 0 in-container; host /etc/hostname intact via os.SameFile; symlink+conv dir purged).
    4. 4a TestNetwork_PyPIAllowed + 4b TestNetwork_NonAllowlistRefused green (pip via the proxy; example.com refused).
    5. 1a/1c/3/boot STILL green (no regression from the Workspace injection or the Close teardown); no leftover aura-sandbox-sess-* container after the run.
    6. `make quality-full` green: coverage ≥85% owned-surface (sandbox+scoring folded), golangci-lint 0, mutation ≥70% on network.go + scoring.go + sessions.go + sessions_endpoint.go.
  </how-to-verify>
  <resume-signal>Type "approved" to close CAP-02 (flip 08-SECURITY threats_open 0 + status verified, confirm the 08-10 carve-outs recorded, mark CAP-02 complete in ROADMAP.md + REQUIREMENTS.md, update the quality snapshot), or name the failing criterion/test to route a follow-up gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (unchanged from 08-10; this plan adds no new boundary)
This plan is a test-wiring + graceful-teardown fix. It introduces NO new trust boundary, NO new dependency, NO new lifecycle verb. The host runner ↔ per-conv container (exec, HTTP loopback), per-conv container ↔ host workspace (os.Root bind, nosuid,nodev,noexec), and per-conv container ↔ host egress proxy boundaries are exactly as 08-10 established.

## STRIDE delta
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-11-DOS-ORPHAN | Denial of Service | live container accumulation on shutdown | mitigate | SessionManager.Close now stop+rm's every live session container after the reaper stops, so graceful shutdown leaks no `aura-sandbox-sess-*` container (previously left for the next boot's RecoverOnBoot sweep). docker rm releases the published loopback port; endpoint Unregister drops the URL. |
| T-08-11-SC | Tampering | no new package | accept | test-helper wiring + a teardown loop using EXISTING dockerClient verbs (stop/remove) + store.MarkTerminated. No new apt/pip/go dependency. No Package Legitimacy Gate trigger. |
</threat_model>

<verification>
- Unit tier: `go vet ./... && go build ./... && go test -race ./internal/sandbox/` green after each task (the integration files are build-tag-gated, so the unit build is unaffected by the liveManager change).
- grep gates: `Workspace:` + `runDirOrSkip` in sessions_live_integration_test.go; `terminateLiveSessions`/`MarkTerminated` in sessions.go; AURA_RUN_DIR + the 2b tier in sandbox.yml.
- NO integration-criteria ASSERTION changed (only liveManager's SessionDeps gained Workspace + a comment; liveSessionExec + every Test* assertion byte-identical).
- LIVE Gate-3 (Task 4, human, native-Linux daemon): the full `sandbox_integration && db_integration` race tier green — 1b/2/4a/4b now pass, 1a/1c/3/boot no-regress, no leftover container, no skip tells under CI.
- `make quality-full`: coverage ≥85% owned-surface, lint 0, mutation ≥70% on the security/decision-critical files.
</verification>

<success_criteria>
CAP-02's 4 ROADMAP criteria all prove green against REAL per-conversation containers on a native-Linux daemon: 1a/1b/1c workspace+state persistence, 2 host symlink-escape cascade safety, 3 idle reaper, 4a/4b egress allowlist — because the live test manager now mounts the writable host workspace exactly as production does (the 08-10 gap was the omitted Workspace dep in the do-not-modify liveManager, NOT a missing feature; prod wires it at chat.go:129). Close leaves no orphan container, so the suite is deterministic. The Docker-Desktop dev host cannot run the workspace-bind criteria (daemon-side device= path unresolvable); the CI sandbox.yml DinD job (or a deliberately stood-up native dockerd) is the Gate-3 host of record. 08-SECURITY threats_open flips 0 and CAP-02 closes on the human sign-off. No PRD amendment (D-04 workspace semantics unchanged); the do-not-modify-liveManager constraint is revised with documented justification (workspace injection is orthogonal to the 08-10 endpoint-resolver design).
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-11-SUMMARY.md` when done.
</output>
