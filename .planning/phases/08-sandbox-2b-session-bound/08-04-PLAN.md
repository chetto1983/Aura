---
phase: 08-sandbox-2b-session-bound
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - internal/web/ssrf.go
  - internal/web/dnspin.go
  - internal/web/transport.go
  - internal/web/export.go
  - internal/web/export_test.go
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "internal/sandbox can call a minimal exported SSRF surface from internal/web (ClassifyIP + a dial-guard / validateAndPin constructor) without copy-pasting classify"
    - "The Slice-5 web tier still passes after the export (no regression: ssrf_test + web_integration green)"
    - "No duplication: classify lives in exactly one place; dupl/golangci-lint stay clean"
  artifacts:
    - path: "internal/web/export.go"
      provides: "exported reuse surface: web.ClassifyIP(netip.Addr)(string,bool) + a constructor the proxy uses for resolve-then-pin"
      contains: "func ClassifyIP"
    - path: "internal/web/export_test.go"
      provides: "tests that the exported surface matches the internal classify behavior (no drift)"
  key_links:
    - from: "internal/web/export.go ClassifyIP"
      to: "internal/web/ssrf.go classify"
      via: "thin exported wrapper (no copy)"
      pattern: "classify\\("
---

<objective>
Resolve landmine #5: the Slice-5 SSRF guard (`classify`, `guard`, `validateAndPin`, `dnsPin`) is package-private to `internal/web`, so `internal/sandbox/network.go` (08-06) cannot reuse it. Per 08-DECISIONS-WAVE0, choose Option (a): export a MINIMAL reusable surface from `internal/web` (smaller than extracting `internal/netguard`, for scope control). This plan touches ONLY `internal/web` so it shares no file with the parallel Wave-2 plans, and it MUST be done before 08-06 (network.go) — that is why it is its own plan in this wave.

Hard rule: do NOT copy-paste `classify` into `network.go` (CLAUDE.md NO-DUPLICATE; `dupl` + audit flag it). The proxy reuses the export. Re-test the Slice-5 web tier after the export (no regression).

Purpose: a single source of truth for IP classification + resolve-then-pin, reused by both web and sandbox. Output: `internal/web/export.go` + its test; the network proxy consumes it in 08-06.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/08-sandbox-2b-session-bound/08-PATTERNS.md
@.planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md
@.claude/skills/golang-security/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export a minimal SSRF reuse surface from internal/web</name>
  <read_first>
    - internal/web/ssrf.go (lines ~35-107) — `classify(netip.Addr) (string,bool)` (the full SSRF class table: loopback/link-local/private/multicast/cgnat/this-network/metadata-v6, IPv4-mapped unmap) + `validateAndPin` (lines ~96-106, fail-closed on ANY blocked record).
    - internal/web/dnspin.go (lines ~28-44) — `dnsPin` struct + `now func() time.Time` injectable clock + the per-conv TTL semantics.
    - internal/web/transport.go (lines ~80-90) — `hardenedTransport.dialContext` resolve-then-pin gate (the proxy's CONNECT handler mirrors this).
    - 08-PATTERNS.md "SSRF resolve-then-pin reuse" Shared Pattern + landmine #5 note.
    - 08-DECISIONS-WAVE0.md — the export-Option-(a) decision (minimal surface, re-test mandate, no-copy rule).
    - internal/web/ssrf_test.go — the existing tests that must keep passing.
  </read_first>
  <action>
    Create `internal/web/export.go` declaring a MINIMAL exported surface that thin-wraps the existing unexported internals (NO behavior copy — the exported funcs CALL `classify`/`validateAndPin`/`dnsPin`): (1) `func ClassifyIP(ip netip.Addr) (reason string, blocked bool)` → returns `classify(ip)` directly; (2) a resolve-then-pin entry point the sandbox proxy needs — prefer exposing a small constructor/struct (e.g. `type DialGuard struct{...}` + `func NewDialGuard(dnsPinTTLSec int) *DialGuard` + a method `ResolveAndPin(ctx, convID, host string) (netip.Addr, string)`) that internally builds a `dnsPin` + calls `validateAndPin`, so the proxy gets resolve-then-pin + DNS-rebinding + classify-every-fail-closed without touching web internals. Keep the surface as small as possible — only what `network.go` (08-06) actually consumes; do NOT export `guard`/`hardenedTransport` wholesale. Add `export_test.go` asserting `ClassifyIP` returns the same `(reason,blocked)` as the internal `classify` for a representative table (loopback v4/v6, link-local, private, metadata, IPv4-mapped, a public IP) — this is the no-drift guard. After the export, run the FULL existing web tier (unit + the `web_integration` tier where available) to confirm no regression. Deep-refactor-on-touch applies (CLAUDE.md): if editing ssrf.go to add an export hook, remove any dead code + keep <600 LOC in the SAME commit.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./... && go test ./internal/web/ && grep -c "func ClassifyIP" internal/web/export.go && ~/go/bin/golangci-lint run ./internal/web/ 2>/dev/null || golangci-lint run ./internal/web/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/web/export.go` exports `ClassifyIP` + a resolve-then-pin entry (DialGuard or equivalent) that the sandbox proxy can call; the exported funcs delegate to the existing internals (grep shows `classify(` / `validateAndPin` called from export.go, NOT reimplemented).
    - `go test ./internal/web/` exits 0 (Slice-5 unit tier green — no regression).
    - `golangci-lint run ./internal/web/` reports 0 issues incl. `dupl` (no duplicated classify).
    - `export_test.go` asserts exported ClassifyIP == internal classify for the representative IP table.
  </acceptance_criteria>
  <done>A minimal, tested, non-duplicating SSRF reuse surface is exported from internal/web; web tier green; ready for 08-06 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md)

| Boundary | Description |
|----------|-------------|
| internal/web internals ↔ internal/sandbox proxy | the export draws a controlled seam: sandbox reuses the SSRF class table + resolve-then-pin via a minimal exported surface instead of a copy (single source of truth for the SSRF V10/V12 control). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-INFO-SSRF | Information Disclosure | duplicated/divergent SSRF logic | mitigate | export delegates to the existing `classify`/`validateAndPin` (no copy) so the sandbox proxy inherits the FULL class table + classify-every-fail-closed; `export_test.go` is the no-drift guard; dupl gate enforces single-source. |
| T-08-04-REGRESS | Tampering | export breaks shipped Slice-5 web tier | mitigate | full `internal/web` test tier re-run after export; deep-refactor-on-touch keeps the edit minimal; CI web tier gates the merge. |
| T-08-04-SC | Tampering | go module installs | accept | stdlib + existing deps only; no new module. |
</threat_model>

<verification>
- `go test ./internal/web/` green (no regression).
- `golangci-lint run ./internal/web/` 0 issues (dupl clean).
- export delegates, does not reimplement (grep classify( in export.go).
</verification>

<success_criteria>
internal/sandbox can reuse the SSRF guard via a minimal export; the Slice-5 web tier is regression-free; no duplication exists for the audit to flag.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-04-SUMMARY.md` when done
</output>
