---
phase: 08-sandbox-2b-session-bound
plan: 03
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - internal/scoring/scoring.go
  - internal/scoring/scoring_test.go
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "ComputeSandboxTier returns Safe for empty allowlist, Safe for pypi.org-only, Risky for arbitrary domains"
    - "Modifiers are monotone — they only ever raise a tier, never lower it (property-tested)"
    - "ComputeTaskTier + ComputeSkillTier + RequiresImmediateAlert + GateRecommended exist and are unit-tested but have NO runtime consumers in Phase 8 (D-12 scope guard)"
    - "scoring is a pure module: no DB, no IO, no env read (threshold passed in by the caller)"
  artifacts:
    - path: "internal/scoring/scoring.go"
      provides: "RiskTier enum + ComputeTaskTier/ComputeSkillTier/ComputeSandboxTier + GateRecommended + RequiresImmediateAlert + UP-only modifier table"
      contains: "func ComputeSandboxTier"
      min_lines: 60
    - path: "internal/scoring/scoring_test.go"
      provides: "exhaustive table tests per Compute* + property-based modifier monotonicity"
      contains: "func TestScoring"
  key_links:
    - from: "internal/scoring/scoring.go ComputeSandboxTier"
      to: "GateRecommended"
      via: "tier → advisory gate"
      pattern: "GateRecommended"
---

<objective>
Ship the FULL shared `internal/scoring/` module pulled forward from Slice 6 (D-11): the `RiskTier` enum, all three `Compute*Tier` functions, `GateRecommended`, `RequiresImmediateAlert`, the UP-only modifier table, exhaustively tested. A pure module — no DB, no IO, no env read (the alert threshold is a caller-supplied arg, mirroring how `dnspin.go` takes the TTL as a constructor arg, NOT an internal env read).

SCOPE GUARD (D-12): build the MODULE, NOT the per-slice application pipelines. `ComputeTaskTier`/`ComputeSkillTier`/`RequiresImmediateAlert` are built + unit-tested but have ZERO runtime consumers in Phase 8 — the Scheduler (P10) and Skills (P11) wire them later. Phase 8 wires ONLY the sandbox advisory path (`ComputeSandboxTier`, consumed by the execute tool in 08-08). Do NOT build the scheduler `pending_approval` flow, `agent_job_runs` audit columns, or the skills pending dir here.

TDD plan: the tier rules are pure `expect(fn(input))==output` — write the exhaustive table + property test FIRST (RED), then implement (GREEN). Fully independent package; shares no file with any other plan.

Purpose: ship the reusable scoring seam once, properly (not three inline copies). Output: `scoring.go` + `scoring_test.go`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/08-sandbox-2b-session-bound/08-PATTERNS.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<feature>
  <name>internal/scoring pure risk-tier module</name>
  <files>internal/scoring/scoring.go, internal/scoring/scoring_test.go</files>
  <behavior>
    From prd.md §Risk-Based Governance (~4459-4548) + 08-RESEARCH `## Code Examples` "scoring module sandbox advisory path" + D-11/D-12:
    - `RiskTier` is a string enum: Safe="safe", Normal="normal", Risky="risky", Destructive="destructive".
    - Test: `ComputeSandboxTier(SandboxArgs{NetworkAllow:[]})` → Safe (empty = no egress, D-12).
    - Test: `ComputeSandboxTier(SandboxArgs{NetworkAllow:["pypi.org"]})` → Safe (pypi-only legit install, D-12); also `["pypi.org","files.pythonhosted.org"]` → Safe (the canonical pip pair — match the onlyPyPI helper to BOTH pypi hosts).
    - Test: `ComputeSandboxTier(SandboxArgs{NetworkAllow:["evil.example.com"]})` → Risky (arbitrary domains, D-12).
    - Test: `GateRecommended(Risky)` == true, `GateRecommended(Destructive)` == true, `GateRecommended(Safe)` == false, `GateRecommended(Normal)` == false.
    - Test: `ComputeTaskTier` + `ComputeSkillTier` over their spec inputs (per prd.md table) — exhaustive rows; these have no Phase-8 consumer but MUST be correct (P10/P11 depend on them).
    - Test: `RequiresImmediateAlert(tier, threshold)` true when tier >= threshold ordering (Safe<Normal<Risky<Destructive); threshold is the caller-supplied arg (NOT an env read).
    - Property test (rapid, test-only dep already in repo): applying any modifier to a tier yields a tier >= the input tier — modifiers are UP-only / monotone, never lower a tier (RESEARCH Validation map "modifiers monotone (never down)").
  </behavior>
  <action>
    RED: write `scoring_test.go` first — exhaustive `t.Parallel()` table (mirror `internal/web/ssrf_test.go` `TestBlocked_Classification` shape) one block per `Compute*Tier` + a `GateRecommended`/`RequiresImmediateAlert` table + a rapid property test for modifier monotonicity. Run; confirm RED (compile-fail / not-implemented). GREEN: implement `scoring.go` — `RiskTier` enum with an internal ordering (an unexported `rank(RiskTier) int` so monotonicity + threshold comparison are total), `SandboxArgs{NetworkAllow []string}`, `ComputeSandboxTier` (empty→Safe, onlyPyPI→Safe matching pypi.org + files.pythonhosted.org, else→Risky), `ComputeTaskTier`/`ComputeSkillTier` per the prd.md spec table, the UP-only modifier table (a function that takes a base tier + a modifier and returns `max(rank)` so it can never lower), `GateRecommended(t)==Risky||Destructive`, `RequiresImmediateAlert(t, threshold)` via rank comparison. NO env read inside scoring (threshold is an arg — caller in 08-08 passes `cfg.RiskAlertThreshold`). NO DB/IO. Keep the file <600 LOC (it is ~100). Run; confirm GREEN. REFACTOR if needed. Commit RED then GREEN as separate atomic commits.
  </action>
  <implementation>Pure functions over the prd.md §Risk-Based Governance spec; ordering via an unexported rank() so both monotonicity and threshold comparison are total and testable.</implementation>
</feature>

<verification>
- `go test ./internal/scoring/` green (table + property).
- `go vet ./... && go build ./...` green.
- `grep -L "database/sql\|pgx\|os.Getenv\|net/http" internal/scoring/scoring.go` confirms purity (no DB/IO/env import).
</verification>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md)

| Boundary | Description |
|----------|-------------|
| tool args ↔ risk advisory | `ComputeSandboxTier` reads the model-supplied `network_allow` list and emits an advisory tier; it makes NO enforcement decision (advisory per PRD "gate consigliato") — enforcement is the egress proxy's (08-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-INFO-TIER | Information Disclosure | risk underclassification | mitigate | onlyPyPI helper matches ONLY pypi.org + files.pythonhosted.org; ANY other host → Risky → GateRecommended true; modifier table is monotone (property-tested) so a tier can never be silently lowered. |
| T-08-03-SCOPE | Repudiation | scope creep into P10/P11 | mitigate | D-12 scope guard: ComputeTask/SkillTier built + unit-tested but UNWIRED; no scheduler/skills pipeline code in this plan (verified by absence — only the sandbox advisory path is consumed, in 08-08). |
| T-08-03-SC | Tampering | go module installs | accept | uses stdlib + existing rapid test dep only; no new module. |
</threat_model>

<success_criteria>
The full pure scoring module ships with exhaustive + property-based tests; only the sandbox advisory path will be wired (in 08-08); Task/Skill tiers are correct-but-unwired per D-12.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-03-SUMMARY.md` when done
</output>
