---
phase: 08-sandbox-2b-session-bound
plan: 08
type: execute
wave: 4
depends_on: ["08-03", "08-05", "08-06", "08-07"]
files_modified:
  - internal/agent/tools/execute.go
  - cmd/aura/sandbox.go
  - cmd/aura/exec.go
  - cmd/aura/main.go
  - internal/conversations/store.go
  - compose.yaml
  - sandbox/seccomp-session.json
autonomous: true
requirements: [CAP-02]
must_haves:
  truths:
    - "The execute tool's session_id arg is ACTIVE: empty defaults to conversation_id (via WithToolCallContext), routed through the session-bound runner; the inert-reject branch is gone; the tool description documents the D-02 asymmetric persistence contract"
    - "A session call with a non-empty network_allow appends {risk_tier, gate_recommended} (from ComputeSandboxTier) to the lean preview — advisory only, no pending-state persistence (D-12)"
    - "aura exec --session <conv_id> runs through the session runner; aura sandbox sessions {list|terminate|prune} dispatches over LoadDB + ListActive/MarkTerminated"
    - "Conversations.Delete calls WorkspaceManager.PurgeConversationDir via an interface (no sandbox->conversations import cycle); replaces the os.RemoveAll at store.go:449"
    - "Session containers run with a seccomp variant that ALLOWS connect(2) (egress contained host-side); the proxy is reachable at the bridge gateway; empty allowlist keeps the 2a egressless posture (extends AR-05-01)"
  artifacts:
    - path: "internal/agent/tools/execute.go"
      provides: "active session_id + scoring advisory in the lean preview"
      contains: "ComputeSandboxTier"
    - path: "cmd/aura/sandbox.go"
      provides: "aura sandbox sessions {list|terminate|prune} dispatcher"
      contains: "func runSandbox"
    - path: "internal/conversations/store.go"
      provides: "cascade via ConversationCleaner interface (no import cycle)"
      contains: "PurgeConversationDir"
    - path: "sandbox/seccomp-session.json"
      provides: "session-container seccomp variant allowing connect(2)"
      contains: "connect"
  key_links:
    - from: "internal/agent/tools/execute.go"
      to: "scoring.ComputeSandboxTier + sandbox session runner"
      via: "advisory tier + session-bound exec"
      pattern: "ComputeSandboxTier|RunPythonSession"
    - from: "internal/conversations/store.go Delete"
      to: "WorkspaceManager.PurgeConversationDir"
      via: "ConversationCleaner interface (main.go wires the impl)"
      pattern: "PurgeConversationDir|Cleaner"
---

<objective>
Wire the Wave-3 building blocks into the live surface: activate the `execute` tool's `session_id` arg, route it through the session-bound runner, attach the scoring advisory; add the `aura sandbox sessions {list|terminate|prune}` CLI + activate `aura exec --session`; replace `Conversations.Delete`'s `os.RemoveAll` with the os.Root cascade via an interface (no import cycle); and lock down the session-container network+seccomp posture (the HIGHEST landmine #3 — a connect-allowing seccomp variant + bridge-gateway-reachable proxy, extending AR-05-01).

Maps all 4 ROADMAP criteria's wiring (the live assertions run in 08-09). Depends on scoring (03), sessions/workspace (05), proxy (06), sidecar/runner (07).

Purpose: the model + operator can actually use session-bound sandbox execution. Output: active execute tool + CLI + cascade wiring + session network/seccomp posture.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sandbox-2b-session-bound/08-CONTEXT.md
@.planning/phases/08-sandbox-2b-session-bound/08-RESEARCH.md
@.planning/phases/08-sandbox-2b-session-bound/08-PATTERNS.md
@.planning/phases/08-sandbox-2b-session-bound/08-DECISIONS-WAVE0.md
@.planning/phases/05-sandbox-2a-stateless/05-SECURITY.md
@.claude/skills/golang-error-handling/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate execute session_id + scoring advisory + Conversations.Delete cascade</name>
  <read_first>
    - internal/agent/tools/execute.go (lines 32 SessionID arg, 42/54 the "RESERVED/Do NOT set" description, 56 Deferred:true, 70-73 the inert-reject branch to remove) + internal/agent/tools/result.go (lines 26-33 WithToolCallContext — how session_id defaults to conversation_id; lines 45-58 validateID traversal guard).
    - internal/scoring/scoring.go (from 08-03) — ComputeSandboxTier(SandboxArgs{NetworkAllow}) + GateRecommended.
    - internal/sandbox/sandbox.go + docker.go (from 08-07) — the session-bound runner methods to call; internal/sandbox/sessions.go (08-05) — SessionManager.Acquire/Release.
    - internal/conversations/store.go (lines 431-454 Delete, 449 os.RemoveAll(dir)) + internal/sandbox/cleanup.go (08-05) — the ConversationCleaner interface to define in conversations + wire in main.go (Task 2).
    - 08-PATTERNS.md execute.go section (remove inert-reject, default session_id, document D-02) + landmine #4 (cascade ownership, import direction).
  </read_first>
  <action>
    In execute.go: remove the inert-reject branch (lines 70-73); when a.SessionID is empty, default it to the conversation id from toolCallCtx(ctx) (already wired via WithToolCallContext — D-25, no InvocationContext change); validate it with the existing validateID traversal guard. Route the call through the session-bound runner (Acquire the session, RunPythonSession/RunShellSession from 08-07, Release). Compute the advisory: parse network_allow (from the tool arg / AURA_SANDBOX_NETWORK_ALLOW_HOSTS), tier := scoring.ComputeSandboxTier(...); if the allowlist is non-empty, append {risk_tier, gate_recommended: scoring.GateRecommended(tier)} to the lean preview (advisory ONLY — NO pending-state persistence for sandbox in v1, D-12). Keep execute Deferred:true (no new deferred tool). Update the Spec() description: delete "Do NOT set session_id ..."; document the D-02 ASYMMETRIC persistence contract (python vars persist across calls in a session; shell cd/export do NOT). In conversations/store.go: define a ConversationCleaner interface with method PurgeConversationDir(convID string) error IN the conversations package (consumer-declares-interface, no sandbox->conversations import); add a nil-safe cleaner field to Store; in Delete, replace the os.RemoveAll(dir) at line 449 with cleaner.PurgeConversationDir(conversationID) when set (the os.Root no-follow cascade covering BOTH workspace/ and .result — landmine #4), falling back to the existing behavior only if cleaner is nil. The wiring of the concrete sandbox impl into the Store is main.go's job (Task 2).
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test ./internal/agent/tools/ ./internal/conversations/ && grep -c ComputeSandboxTier internal/agent/tools/execute.go && grep -c PurgeConversationDir internal/conversations/store.go</automated>
  </verify>
  <acceptance_criteria>
    - execute.go no longer contains the "is reserved for Phase 8" inert-reject (grep clean); empty session_id defaults to the conversation id; validateID applied.
    - A non-empty network_allow appends {risk_tier, gate_recommended} to the lean preview; NO pending-state persistence (no new DB write for the sandbox advisory).
    - execute stays Deferred:true; the Spec() description documents the D-02 asymmetric persistence contract.
    - conversations/store.go defines ConversationCleaner locally and calls PurgeConversationDir in Delete; go list -deps ./internal/conversations shows NO dependency on internal/sandbox.
    - go test ./internal/agent/tools/ ./internal/conversations/ green.
  </acceptance_criteria>
  <done>session_id is active + advisory-scored; the cascade goes through the os.Root cleaner via an interface; no import cycle.</done>
</task>

<task type="auto">
  <name>Task 2: aura sandbox sessions CLI + exec --session + main.go composition + session network/seccomp posture</name>
  <read_first>
    - cmd/aura/db.go (lines 19-42 runDB nested switch + text/tabwriter import) — the dispatcher analog for runSandbox.
    - cmd/aura/exec.go (lines 24-62 parseExecArgs --session latch, 83-93 the inert-reject to replace, 118-126 the errors.Is exit-code mapping + tools.FormatLean reuse).
    - cmd/aura/main.go (line ~37 the top-level switch) + how db.go/agent.go/identity.go register their cases + how the Store/runner are composed at boot.
    - internal/db/queries/sandbox_sessions.sql (ListActive/MarkTerminated from 08-02) + internal/sandbox/sessions.go (08-05 SessionManager) + internal/sandbox/workspace.go (08-05 WorkspaceManager — the concrete ConversationCleaner impl to wire into the Store).
    - compose.yaml (lines ~105-143 the 2a hardening + the aura-sandbox-egressless non-masquerading bridge + connect-denied seccomp) + sandbox/seccomp.json (the 2a positive allowlist that DENIES connect).
    - 08-RESEARCH.md Pitfall 1 (HIGHEST landmine #3: session containers need a connect-allowing seccomp variant; proxy reachable at the BRIDGE GATEWAY IP not container 127.0.0.1; empty allowlist keeps egressless) + 08-DECISIONS-WAVE0 (the posture decision) + 05-SECURITY AR-05-01 (the deviation to extend).
  </read_first>
  <action>
    Create cmd/aura/sandbox.go with func runSandbox(args []string) mirroring runDB: dispatch sessions {list|terminate|prune} over config.LoadDB() + the sqlc querier — list (ListActive, render with text/tabwriter: id, conversation_id, container_id, started_at, last_used_at, status), terminate <conv_id> (find + docker stop/rm + MarkTerminated), prune (reap all idle/terminated). Output formatting is Claude's discretion (tabwriter like db.go). Add case "sandbox": runSandbox(os.Args[2:]) to main.go's switch. In exec.go: replace the inert-reject (83-93) with the real session path — route RunPython/RunShell through the session-bound runner with ea.session as the session id; reuse tools.FormatLean + the errors.Is exit-code mapping (70/71/64; map the new ErrSessionCapReached to a distinct exit). In main.go boot composition: construct the SessionManager (with cfg + the sqlc querier + the proxy from 08-06) and the WorkspaceManager, and wire the WorkspaceManager as the conversations.Store ConversationCleaner (closes the import-cycle-free seam from Task 1); start the forward proxy + the reaper; ensure SessionManager.RecoverOnBoot runs at startup. Create sandbox/seccomp-session.json: COPY sandbox/seccomp.json and ADD connect (and any socket syscalls the proxy route needs) to the allowlist — egress is now contained host-side by the proxy, NOT by connect-denial (landmine #3). In compose.yaml, document the session-container network posture: the SessionManager's docker run (08-05) attaches the session container to a bridge that can reach the host proxy at the BRIDGE GATEWAY IP and passes --security-opt seccomp=sandbox/seccomp-session.json for sessions with a non-empty allowlist; an EMPTY allowlist keeps the 2a egressless posture (no proxy env, connect dead-ends). This deviation EXTENDS AR-05-01 — note it in the compose comment and flag it for the 08-09 08-SECURITY authoring + the live reachability spike.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./... && go build ./... && go test ./cmd/aura/ ./internal/sandbox/ && grep -c "func runSandbox" cmd/aura/sandbox.go && grep -c connect sandbox/seccomp-session.json</automated>
  </verify>
  <acceptance_criteria>
    - cmd/aura/sandbox.go defines runSandbox dispatching sessions {list|terminate|prune}; main.go switch has a case "sandbox".
    - aura exec --session no longer prints the "is reserved for Phase 8" inert message (grep clean) and routes through the session runner; ErrSessionCapReached maps to a distinct CLI exit code via errors.Is.
    - main.go boot composes the SessionManager + WorkspaceManager, wires WorkspaceManager as the conversations.Store ConversationCleaner, starts the proxy + reaper, and runs RecoverOnBoot.
    - sandbox/seccomp-session.json exists and ADDS connect to the 2a allowlist (landmine #3); a compose comment documents the bridge-gateway-reachable proxy + empty-allowlist-keeps-egressless posture extending AR-05-01.
    - go build ./... && go test ./cmd/aura/ ./internal/sandbox/ green.
  </acceptance_criteria>
  <done>The CLI + exec --session are live; boot composition wires the control plane + cleaner + proxy; the session network/seccomp posture is set (connect allowed, egress host-contained, extends AR-05-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (extends 05-SECURITY.md / AR-05-01)

| Boundary | Description |
|----------|-------------|
| model tool args ↔ execute | session_id validated by validateID (traversal guard); network_allow scored advisory-only (no enforcement here — the proxy enforces). |
| session container ↔ host proxy (network posture) | session containers with a non-empty allowlist run the connect-allowing seccomp variant + reach the proxy at the bridge gateway; empty allowlist keeps the 2a egressless posture. EXTENDS AR-05-01. |
| operator CLI ↔ control plane | aura sandbox sessions reads/mutates sandbox_sessions over LoadDB (aura_app DML); terminate/prune drive docker stop/rm via the SessionManager (never the socket). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-INFO-NET | Information Disclosure | session container egress posture (the connect re-enable) | mitigate | session seccomp variant allows connect ONLY because egress is host-proxy-contained (08-06); proxy reachable at the bridge gateway IP (not container 127.0.0.1); empty allowlist keeps the 2a egressless posture (connect dead-ends); documented as a deviation EXTENDING AR-05-01, re-audited live in 08-09. |
| T-08-08-V5-INPUT | Tampering | session_id / network_allow tool args | mitigate | validateID traversal guard on session_id (existing); network_allow CSV parsed + glob-validated (08-06); a global `*` deny rejected. |
| T-08-08-EOP-SYMLINK | Elevation/Tampering | cascade-on-delete | mitigate | Conversations.Delete now routes through the os.Root no-follow PurgeConversationDir (08-05) instead of os.RemoveAll — closes the workspace symlink-escape on the host cascade path (ROADMAP crit 2). |
| T-08-08-SCOPE | Repudiation | scope creep (D-12) | mitigate | only the sandbox advisory path is wired (ComputeSandboxTier → lean preview); NO scheduler pending_approval / agent_job_runs audit columns / skills pending dir (P10/P11). |
| T-08-08-EOP-SOCKET | Elevation of Privilege | CLI terminate/prune lifecycle | mitigate | terminate/prune drive docker stop/rm via the SessionManager (LookPath-gated, fixed argv, never the socket — inherited T-05-03-EOP-SOCKET / D-05). |
| T-08-08-SC | Tampering | go module installs | accept | stdlib + existing deps; no new module; seccomp-session.json derived from the shipped 2a profile (by-name syscalls, no new package). |
</threat_model>

<verification>
- go build ./... && go vet ./... green; go test ./cmd/aura/ ./internal/agent/tools/ ./internal/conversations/ ./internal/sandbox/ green.
- grep: execute + exec inert-reject gone; ComputeSandboxTier wired; seccomp-session.json adds connect.
- go list -deps ./internal/conversations shows no internal/sandbox dependency.
</verification>

<success_criteria>
session-bound sandbox execution is live end-to-end (tool + CLI), the cascade uses the os.Root cleaner via an interface, and the session network/seccomp posture is set (connect allowed, egress host-contained) extending AR-05-01 — with the live reachability spike + 08-SECURITY authoring queued for 08-09.
</success_criteria>

<output>
Create `.planning/phases/08-sandbox-2b-session-bound/08-08-SUMMARY.md` when done
</output>
