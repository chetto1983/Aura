---
phase: 19-audit-bug-resolution-e2e-live-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/tools/shell_exec.go
  - internal/agent/tools/shell_exec_unix.go
  - internal/agent/tools/shell_exec_windows.go
  - internal/agent/tools/shell_exec_test.go
  - internal/agent/tools/result.go
  - internal/agent/llm_agent_completion.go
autonomous: true
requirements: [H4, H5, L3, M-f]
must_haves:
  truths:
    - "A shell command spawning an orphan grandchild does NOT hang the call past timeout+WaitDelay; the grandchild PID is dead after Execute returns"
    - "On stdout larger than the preview cap, the model's RoleTool result still contains the [exit code N] / [aura_shell {...}] footer and a stderr tail"
    - "A ctx-cancelled (not deadline) shell run reports a distinct [command cancelled] status, not opaque signal:killed"
    - "stdout and stderr preserve temporal interleave (single synchronized writer)"
  artifacts:
    - path: "internal/agent/tools/shell_exec_unix.go"
      provides: "POSIX process-group setup + group-kill (Setpgid + Kill(-pgid))"
      contains: "setProcessGroup"
    - path: "internal/agent/tools/shell_exec_windows.go"
      provides: "Windows process-group setup + taskkill /T group-kill"
      contains: "setProcessGroup"
    - path: "internal/agent/tools/result.go"
      provides: "tail-reserving truncation variant for shell footer survival"
  key_links:
    - from: "internal/agent/tools/shell_exec.go"
      to: "setProcessGroup / killProcessGroup"
      via: "cmd.Cancel closure + cmd.WaitDelay"
      pattern: "cmd\\.Cancel|cmd\\.WaitDelay|setProcessGroup"
    - from: "internal/agent/llm_agent_completion.go"
      to: "tail-reserving digest"
      via: "sideEffectDigest must not head-truncate away the footer"
      pattern: "criticResultCap|sideEffectDigest"
---

<objective>
Close the "shell never answers" root-cause cluster on the shell tool surface (the single
highest-leverage cluster per the audit). H4 (orphan-child hang past timeout, no process
group / no WaitDelay), H5 (exit-code/stderr footer head-truncated away on large output),
L3 (opaque cancel status), and M-f (de-interleaved stderr) all live in `shell_exec.go` /
`result.go` and are fixed in ONE edit pass per refactor-on-touch (D-01b — L3/M-f ride the
H4/H5 edit, no double-touch).

Purpose: shell_exec is THE primary host execution surface (Phase 18, full-host-terminal
posture). An orphan grandchild keeping the stdout pipe open hangs `cmd.Wait()` forever; a
large failed run's exit-code/stderr footer is truncated head-first so the model "lies about
success". These are the mechanical causes of the named open UX bug.

Output: the first `//go:build` OS-split in `internal/` (`shell_exec_{unix,windows}.go`), a
tail-reserving truncation seam in `result.go`, the rewritten `TestShellExecTimesOut`
(D-04 false-green) asserting the grandchild PID is dead, and the critic digest no longer
head-truncating the footer.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-CONTEXT.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-RESEARCH.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-PATTERNS.md
@docs/audit/deep-correctness-audit-2026-06-10.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-platform process-group teardown (H4 + L3 + M-f) + rewrite TestShellExecTimesOut</name>
  <read_first>
    - internal/agent/tools/shell_exec.go (CURRENT state — Execute at :95-160, the two-builder
      stdout/stderr at :123-125, exitCodePtr at ~:324, renderShellOutput status switch ~:301-310;
      the existing shellIsCmdFallback OS switch and errors.AsType[T] usage at :318)
    - internal/agent/tools/shell_exec_test.go (CURRENT TestShellExecTimesOut — today only asserts
      the [command timed out] marker, never that the child died; the shellIsCmd() OS switch precedent ~:109)
    - .planning/spikes/038-profile-store-atomic-contract/replace_posix.go AND replace_windows.go
      (the ONLY existing _posix/_windows build-tag pair in the repo — the OS-split shape to copy)
    - 19-RESEARCH.md §"Pattern 1" (filename-suffix OS split), §"Pattern 2" (cmd.Cancel+WaitDelay
      verified Go semantics), §"Pitfall 1" (test must assert grandchild PID death), §"Pitfall 2"
      (exec.ErrWaitDelay is a NEW error class the render path must classify)
    - docs/audit/deep-correctness-audit-2026-06-10.md §H4, §L3, §M-f
    - .claude/skills/golang-concurrency/SKILL.md (process lifecycle), .claude/skills/golang-security/SKILL.md (orphan-process DoS)
  </read_first>
  <action>
    Create `internal/agent/tools/shell_exec_unix.go` (`//go:build !windows`) and
    `internal/agent/tools/shell_exec_windows.go` (`//go:build windows`), each in package `tools`,
    exporting two unexported helpers `setProcessGroup(cmd *exec.Cmd)` and
    `killProcessGroup(cmd *exec.Cmd) error`.
    - Unix `setProcessGroup`: `cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}`.
      Unix `killProcessGroup`: if `cmd.Process == nil` return nil, else
      `syscall.Kill(-cmd.Process.Pid, syscall.SIGKILL)` (negative pid kills the whole group).
    - Windows `setProcessGroup`: `cmd.SysProcAttr = &syscall.SysProcAttr{CreationFlags: syscall.CREATE_NEW_PROCESS_GROUP}`.
      Windows `killProcessGroup`: run `taskkill /F /T /PID <cmd.Process.Pid>` (A1 minimal-real
      choice, no Job Object) — exec it directly, ignore "process not found" exit codes.
    In `shell_exec.go` Execute, after `exec.CommandContext(runCtx, name, args...)` and before
    `cmd.Run()`: call `setProcessGroup(cmd)`, set `cmd.Cancel = func() error { return killProcessGroup(cmd) }`,
    and set `cmd.WaitDelay = 5 * time.Second`.
    M-f: replace the two separate `strings.Builder` (`stdout`, `stderr`) with ONE synchronized
    writer that preserves interleave order — point both `cmd.Stdout` and `cmd.Stdout`... point
    `cmd.Stdout` and `cmd.Stderr` at a single mutex-guarded writer (mirror the `safeBuffer` mutex
    pattern at `internal/mcp/client.go:317-334`), while still separately tracking the stderr-only
    bytes needed for the stderr block. Keep stdout-only available for the cwd-marker extraction.
    Pitfall 2: add an `errors.Is(runErr, exec.ErrWaitDelay)` branch to BOTH `exitCodePtr` (~:324)
    and `renderShellOutput` (~:301-310) — render it as a timeout/kill (set `timed_out: true`), NOT
    `[command failed: %v]`, so the internal Go error string never leaks to the model.
    L3: add a `errors.Is(runCtx.Err(), context.Canceled)` branch to `renderShellOutput` emitting a
    distinct `[command cancelled]` status (today the switch only branches DeadlineExceeded /
    *exec.ExitError / failed), and set `timed_out:false` for cancel.
    Rewrite `TestShellExecTimesOut` (D-04 — cite the false-green justification in the commit msg per
    CLAUDE.md "rewrite broken tests with justification"): the fixture spawns a backgrounded
    grandchild that records its PID (`sh -c 'sleep 30 & echo $! > <pidfile>; sleep 30'` POSIX;
    cmd.exe equivalent gated behind the existing `shellIsCmd()` switch), runs with a ~200ms timeout,
    and AFTER Execute returns asserts (a) Execute returned within timeout+WaitDelay margin and
    (b) the grandchild PID is DEAD (`syscall.Kill(pid, 0)` → ESRCH on POSIX; `tasklist`/signal-0 on
    Windows). The assertion MUST hold on BOTH Linux (WSL/CI) and Windows (w64devkit).
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/agent/tools/ && go vet ./internal/agent/tools/ && go test -run 'TestShellExec' ./internal/agent/tools/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): `go test -run TestShellExecTimesOut ./internal/agent/tools/`
      asserts the backgrounded grandchild PID is NOT alive after Execute returns (`syscall.Kill(pid,0)`→ESRCH
      POSIX / tasklist-absent Windows) AND Execute returned within timeout+WaitDelay. BEFORE the fix the
      grandchild PID survives past the timeout (orphan) and a pipe-holding grandchild can hang Wait() forever;
      AFTER the fix the group kill + WaitDelay reaps it.
    - The assertion holds on BOTH Linux (WSL/CI) and Windows (w64devkit) — both OS-split files compile and
      the test's POSIX/cmd.exe branches both pass (`GOOS=windows go build ./internal/agent/tools/` succeeds).
    - `go vet ./internal/agent/tools/` and `go build ./internal/agent/tools/` clean; no file exceeds 600 LOC
      (split shell_exec.go on touch if it does).
    - A ctx-cancelled run renders `[command cancelled]` (distinct from `[command timed out]`); an
      exec.ErrWaitDelay run renders as a timeout/kill, never `[command failed: exec: WaitDelay…]`.
  </acceptance_criteria>
  <done>
    H4 process group + cmd.Cancel + cmd.WaitDelay wired; the grandchild-PID-death regression passes on
    both OSes; L3 cancel status + M-f single-writer + exec.ErrWaitDelay classification all landed in the
    same edit pass; TestShellExecTimesOut is no longer false-green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tail-reserving truncation so the shell footer + exit code survive (H5)</name>
  <read_first>
    - internal/agent/tools/result.go (CURRENT NewResult + truncatePreview at :75-104 — the head-first
      `content[:cut]` cut; the toolCallCtx cap seam at :21)
    - internal/agent/tools/shell_exec.go (the footer append at :139-146 — renderShellOutput + appendShellFooter
      append status/stderr/[exit code N]/[aura_shell {...}] at the TAIL; Meta["exit_code"] at ~:154)
    - internal/agent/llm_agent_completion.go (sideEffectDigest at ~:157 — truncateBytes(results[id], criticResultCap=400)
      head-first on the SAME tool result; the critic grades DONE on a truncated failed run)
    - 19-RESEARCH.md §"Pattern 3" (reserve-tail-before-truncate, seam (b) NewResultReservingTail), §H5 row in Validation Architecture
    - docs/audit/deep-correctness-audit-2026-06-10.md §H5
  </read_first>
  <action>
    Add a `NewResultReservingTail(ctx context.Context, body, footer string) (ToolResult, error)`
    helper in `result.go` (RESEARCH §Pattern 3 seam (b)): it truncates `body` to `cap - len(footer)`
    (cap from the existing `toolCallCtx(ctx).cap` / `AURA_CONTEXT_PREVIEW_CAP_BYTES`, default 2048),
    then appends the always-keep `footer`, so the `[exit code N]` / `[aura_shell {...}]` footer and a
    stderr tail are NEVER sliced off. Leave the existing `NewResult` unchanged for non-shell tools.
    In `shell_exec.go` Execute, change the result construction so the rendered body (stdout) is the
    truncatable part and the status+stderr-tail+footer is the reserved footer passed to
    `NewResultReservingTail` — restructure `renderShellOutput`/`appendShellFooter` so the footer string
    (status marker + a bounded stderr tail + `[exit code N]` + `[aura_shell {...}]`) is assembled
    separately from the truncatable stdout body.
    Fix the SAME defect on the critic path: in `sideEffectDigest` (completion.go:~157) the
    `truncateBytes(results[id], criticResultCap)` head-truncation slices off the footer before the
    critic sees it — change the digest to keep the tail (footer) too (truncate the body, keep the
    footer), so a large FAILED run cannot grade DONE.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/agent/tools/ ./internal/agent/ && go test -run 'TestShellExec|TestNewResult|TestSideEffectDigest|TestCompletion' ./internal/agent/tools/ ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): a new test in `shell_exec_test.go` runs a command
      whose stdout exceeds 2048 bytes and exits non-zero, then asserts the resulting RoleTool content
      CONTAINS the `[exit code N]` footer / `[aura_shell {...exit_code...}]` marker. BEFORE the fix the
      footer is absent (head-first truncation dropped it); AFTER the fix it survives within the cap.
    - A completion-critic-path test asserts the critic digest of a large FAILED run still contains the
      exit-code/stderr footer (so it cannot grade a failed run DONE). Fails before, passes after.
    - `go build ./internal/agent/tools/ ./internal/agent/` clean; existing NewResult callers unchanged
      (new helper is additive); no file exceeds 600 LOC.
  </acceptance_criteria>
  <done>
    The shell footer + exit code + stderr tail survive truncation on large output for both the model's
    RoleTool view and the completion critic digest; the model can see a failed run failed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → host shell | The LLM-authored command crosses into host process execution (full-host-terminal posture, Phase 18 D-01) |
| child → grandchild process | bash spawns grandchildren (go run / python / npm) that can outlive the parent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Denial of Service | shell_exec orphan grandchild (H4, ASVS V12) | mitigate | Process group (`Setpgid`/`CREATE_NEW_PROCESS_GROUP`) + `cmd.Cancel` group-kill + `cmd.WaitDelay`(5s) force-closes inherited fds so `Wait()` cannot block forever; regression asserts grandchild PID death |
| T-19-02 | Elevation of Privilege | privileged orphan left running after kill (H4) | mitigate | Group-kill targets the whole process group (negative-pid SIGKILL POSIX / `taskkill /T` Windows) so no privileged grandchild survives the kill; the 120s model-path timeout enforcement is preserved (WaitDelay rides ON TOP of the existing WithTimeout, never replaces it) |
| T-19-03 | Information Disclosure | exec.ErrWaitDelay internal Go error leaked to model (Pitfall 2) | mitigate | Classify `exec.ErrWaitDelay` as a timeout/kill in renderShellOutput, never emit the raw Go error string to the model |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO new packages (RESEARCH §Package Legitimacy Audit: N/A); go.mod unchanged. No supply-chain surface. |

Threats NOT introduced by this remediation: no new auth/session/crypto surface (N/A — this is a robustness fix on an existing tool).
</threat_model>

<artifacts_this_phase_produces>
New files: `internal/agent/tools/shell_exec_unix.go`, `internal/agent/tools/shell_exec_windows.go`.
New exported/unexported symbols: `setProcessGroup(cmd *exec.Cmd)` (per-OS), `killProcessGroup(cmd *exec.Cmd) error` (per-OS), `NewResultReservingTail(ctx, body, footer string) (ToolResult, error)` in `result.go`.
Modified behavior: `shell_exec.go` Execute (cmd.Cancel/cmd.WaitDelay wiring, single synchronized writer, exec.ErrWaitDelay + context.Canceled classification); `renderShellOutput`/`exitCodePtr` new branches; `sideEffectDigest` tail-preserving digest in `llm_agent_completion.go`.
No new migration objects, sqlc symbols, or CLI flags in this plan.
</artifacts_this_phase_produces>

<verification>
- `go vet ./internal/agent/tools/ ./internal/agent/` clean.
- `go build ./...` AND `GOOS=windows go build ./internal/agent/tools/` both succeed (both OS-split halves compile).
- `go test -race ./internal/agent/tools/` green (process-lifecycle package — race tier required).
- TestShellExecTimesOut and the H5 footer-survival test both pass (and fail on a pre-fix tree).
</verification>

<success_criteria>
- H4: orphan grandchild reaped, no hang past timeout+WaitDelay, PID-death asserted on both OSes.
- H5: exit-code/stderr footer survives truncation for both model view and critic digest.
- L3: distinct `[command cancelled]` status. M-f: stderr interleave preserved.
- All four findings landed in one shell-cluster commit; both OS-split files compile; ≤600 LOC each.
</success_criteria>

<output>
Create `.planning/phases/19-audit-bug-resolution-e2e-live-test/19-01-SUMMARY.md` when done.
</output>
