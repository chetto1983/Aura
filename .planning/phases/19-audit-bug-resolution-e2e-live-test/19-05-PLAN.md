---
phase: 19-audit-bug-resolution-e2e-live-test
plan: 05
type: execute
wave: 2
depends_on: ["19-04", "19-09"]
files_modified:
  - internal/channels/telegram/renderer.go
  - internal/channels/telegram/bot_dispatch.go
  - internal/channels/telegram/commands.go
  - internal/channels/telegram/bot_dispatch_hitl.go
  - cmd/aura/serve_channels.go
autonomous: true
requirements: [H2, H3, M-e]
must_haves:
  truths:
    - "A turn that errors on Telegram shows the user a sanitized failure reason, not a bare 'Stato: errore' with the reason swallowed"
    - "An async (5-50MB) document conversion failure notifies the user via convertFailMessage instead of leaving 'elaborando…' then silence forever"
    - "/cancel during a pending ask_user pause cancels the pause (routes through SubmitAnswer ActionCancel): the paused_states row resolves and the inline keyboard clears"
  artifacts:
    - path: "internal/channels/telegram/renderer.go"
      provides: "RunErrorEvent case rendering a sanitized failure message"
      contains: "RunErrorEvent"
    - path: "internal/channels/telegram/bot_dispatch.go"
      provides: "async convert-failure notice via the captured sender"
      contains: "convertFailMessage"
  key_links:
    - from: "internal/channels/telegram/renderer.go"
      to: "agui.SanitizeString"
      via: "RunErrorEvent.Message sanitized before the user-facing send"
      pattern: "SanitizeString"
    - from: "internal/channels/telegram/commands.go"
      to: "SubmitAnswer(...ActionCancel)"
      via: "/cancel routes through the HITL cancel path when PendingFor is non-empty"
      pattern: "ActionCancel|PendingFor"
---

<objective>
Close the Telegram user-facing surface cluster: H2 (turn errors are swallowed — the renderer's
consume switch has NO `RunErrorEvent` case, so the error reason is dropped and the user sees a bare
"Stato: errore"), H3 (an async 5-50MB document-conversion failure logs `convErr` and returns silently
after replying "elaborando…", leaving the user waiting forever, while the sync ≤5MB path correctly
surfaces `convertFailMessage`), and M-e (`/cancel` during a pending ask_user pause does NOT cancel the
pause — it is intercepted as a command before the HITL path and only cancels an in-flight turn ctx,
which does not exist during a pause, so the `paused_states` row is orphaned and the inline keyboard
stays live). All in `internal/channels/telegram/`.

This plan DEPENDS ON 19-04 (wave 1) for the exported `agui.SanitizeString` — H2 routes the
`RunErrorEvent.Message` through that one redaction contract (the same chokepoint the HTTP path uses).

Purpose: these are the Telegram error-swallowing + orphaned-pause defects; the user must see why a
turn failed, learn a doc conversion failed, and be able to cancel a pause.

Output: the `RunErrorEvent` render case, the async convert-failure notice, the `/cancel`→pause-cancel
routing, and the corrected stale comment at `serve_channels.go:142` (D-04).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-CONTEXT.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-RESEARCH.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-PATTERNS.md
@docs/audit/deep-correctness-audit-2026-06-10.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render turn errors (H2) + correct the stale serve_channels comment + async doc-convert notice (H3)</name>
  <read_first>
    - internal/channels/telegram/renderer.go (CURRENT consume switch at :81-97 — cases for
      TextMessageContent/End + RunFinished ONLY; NO `*events.RunErrorEvent` case; how `r.flush`/send work;
      whether the renderer has a sendError helper)
    - internal/agui/server.go (the now-EXPORTED `SanitizeString` from 19-04, :403; sanitizeErr at :392 —
      H2 carries a `Message string`, so call SanitizeString, not sanitizeErr which takes an error)
    - internal/channels/telegram/bot_dispatch.go (CURRENT async `asyncResult` callback at :388-395 — logs
      convErr + returns; the captured `sender`/`notifier` at :386-387; the sync sibling at :400
      `t.reply(c, convertFailMessage)`; the convertFailMessage constant)
    - cmd/aura/serve_channels.go (CURRENT stale comment at :142 — "the user sees a generic ❌ Errore",
      which renders nowhere; RESEARCH corrected the audit's :145 → :142)
    - internal/channels/telegram/testdata/statuspane_run_error.golden (the golden = the full user-visible
      output for an errored turn — H2's regression compares against it)
    - 19-RESEARCH.md §"Code Examples → H2", §"H2"/"H3" rows in Validation Architecture, §"Shared Patterns → Error/secret sanitization"
    - 19-PATTERNS.md §"H2", §"H3"
    - docs/audit/deep-correctness-audit-2026-06-10.md §H2, §H3
    - .claude/skills/golang-error-handling/SKILL.md, .claude/skills/golang-security/SKILL.md
  </read_first>
  <action>
    H2: add `case *events.RunErrorEvent:` to the renderer's `consume` switch (renderer.go:86-96). It sends
    a sanitized user-facing failure message — `agui.SanitizeString(e.Message)` (the renderer carries a
    `Message string`, not an `error`, so use the exported string-level helper from 19-04, NOT `sanitizeErr`).
    Send it via the renderer's existing user-facing send path (the same path `r.flush`/`sendError` uses) so
    the golden `testdata/statuspane_run_error.golden` reflects a real failure message, not a bare status flip.
    Correct the stale comment at `serve_channels.go:142` (D-04 — the "the user sees a generic ❌ Errore"
    string renders nowhere): update or remove it to describe the ACTUAL behavior (the create-failure now
    surfaces through the RunErrorEvent render added above). Cite the false-green/stale-comment justification
    in the commit message.
    H3: in the async `asyncResult` callback (bot_dispatch.go:388-395), on `convErr != nil` send
    `convertFailMessage` via the captured `sender`/`notifier` (both already in the closure at :386-387) —
    mirror the sync sibling at :400 — BEFORE the `return`. Keep the existing slog.Warn.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/channels/... ./cmd/aura/ && go vet ./internal/channels/telegram/ && go test -run 'TestRenderer|TestRunError|TestStatusPane|TestAsyncConvert|TestDocConvert' ./internal/channels/telegram/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after, H2): a test puts a `*events.RunErrorEvent` (Message =
      a sanitizable error, e.g. carrying a DSN) on the consume channel and asserts the user-facing send
      contains the SANITIZED reason (not a bare "Stato: errore"; no DSN/token). The golden
      `testdata/statuspane_run_error.golden` matches the new rendered output. BEFORE the fix the switch has
      no RunErrorEvent case and the reason is dropped; AFTER it renders.
    - Layer-1 regression (fails-before / passes-after, H3): a test invokes the async callback with
      `convErr != nil` and asserts a FAKE sender received `convertFailMessage`. BEFORE the fix the callback
      logs + returns silently; AFTER, the user is notified.
    - The `serve_channels.go:142` comment no longer claims a string that renders nowhere.
    - `go build ./internal/channels/... ./cmd/aura/` clean; `go vet` clean; ≤600 LOC each file.
  </acceptance_criteria>
  <done>
    Telegram turn errors render a sanitized reason (golden updated), the stale comment is corrected, and
    async doc-conversion failures notify the user.
  </done>
</task>

<task type="auto">
  <name>Task 2: /cancel during a pending pause cancels the pause (M-e)</name>
  <read_first>
    - internal/channels/telegram/bot_dispatch.go (CURRENT dispatch ordering at :114-126 — step 1 command
      intercept `t.cmds.dispatchRich`, step 2 `t.hitlHandlesText`, step 3 ordinary turn; `/cancel` is a
      command so it never reaches the HITL path during a pause)
    - internal/channels/telegram/commands.go (CURRENT `cancel` at :167-177 — only cancels `c.cancels[chatID]`
      turn ctx; returns "Nessun turno in corso" during a pause)
    - internal/channels/telegram/bot_dispatch_hitl.go (CURRENT `PendingFor` seam at :24 —
      `t.deps.Resume.PendingFor(daemonCtx, convID(chatID))`; `hitlFor`/`handleTextReply` resume path)
    - internal/agui/server.go (the "Annulla" button precedent: resumeAnswers maps cancelled → ActionCancel,
      :285-293 — the SubmitAnswer(…ActionCancel) shape /cancel must mirror)
    - 19-RESEARCH.md §"M-e" row, 19-PATTERNS.md §"M-e"
    - docs/audit/deep-correctness-audit-2026-06-10.md §M-e
    - .claude/skills/golang-context/SKILL.md
  </read_first>
  <action>
    Route `/cancel` through the HITL cancel path when a pause is pending. Before (or inside) the `cancel`
    command handling, check `PendingFor(convID(chatID))` (the seam at bot_dispatch_hitl.go:24): when it is
    NON-empty, resolve the pending pause via the Runner's `SubmitAnswer(…ActionCancel)` (mirror the "Annulla"
    button → `askuser.ActionCancel` path used by `resumeAnswers` at agui/server.go:285-293) so the
    `paused_states` row resolves, then clear the inline keyboard (retire the pause prompt's reply markup).
    When no pause is pending, keep the existing in-flight-turn ctx-cancel behavior (commands.go:167-177).
    The change can live at the dispatch seam (intercept `/cancel` for a pending-pause chat and route to the
    HITL cancel) and/or in the `cancel` command handler given a `Resume` dependency — pick the seam that
    keeps the ordering correct (a pause-cancel must NOT fall through to the no-op turn-cancel). Confirm the
    keyboard is cleared (divergent-from-Annulla bug in the audit).
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/channels/... && go vet ./internal/channels/telegram/ && go test -run 'TestCancel|TestHitl|TestPause|TestCommand' ./internal/channels/telegram/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): a test sets up a pending pause (`PendingFor` returns
      a non-empty set) and dispatches `/cancel`, asserting it routes through `SubmitAnswer(…ActionCancel)`
      (the paused_states row is resolved via the Resume dep) AND the inline keyboard is cleared. BEFORE the
      fix `/cancel` returns "Nessun turno in corso" and the pause stays orphaned + keyboard live; AFTER, the
      pause is cancelled.
    - With NO pending pause, `/cancel` still cancels an in-flight turn ctx (existing behavior preserved) —
      assert the no-pause path is unchanged.
    - `go vet ./internal/channels/telegram/` clean; ≤600 LOC each file.
  </acceptance_criteria>
  <done>
    `/cancel` cancels a pending ask_user pause (paused_states resolved, keyboard cleared), matching the
    "Annulla" button; the no-pause turn-cancel is preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop → Telegram user surface | Error messages cross to the end user; an error string may embed secrets |
| Telegram user → HITL pause state | A `/cancel` command crosses into the paused_states lifecycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Information Disclosure | user-facing error leaks internal detail / secrets (H2, ASVS V7) | mitigate | The RunErrorEvent render routes the Message through the shared `agui.SanitizeString` (DSN/token/bearer redaction); the user sees the sanitized reason, not raw internals |
| T-19-12 | Repudiation / Information loss | async doc-convert failure swallowed → user waits forever (H3) | mitigate | The async callback sends `convertFailMessage` on `convErr != nil`, matching the sync path |
| T-19-13 | Denial of Service (state) | orphaned paused_states row + live keyboard after /cancel (M-e) | mitigate | `/cancel` during a pause routes through `SubmitAnswer(…ActionCancel)` so the pause resolves and the keyboard clears |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit: N/A); go.mod unchanged |

HIGH-severity information-disclosure on the user surface (H2) is MITIGATED via the shared sanitize chokepoint.
</threat_model>

<artifacts_this_phase_produces>
No new files. Modified behavior:
- `renderer.go`: new `case *events.RunErrorEvent:` in `consume` (sanitized via `agui.SanitizeString`).
- `bot_dispatch.go`: async `asyncResult` sends `convertFailMessage` on `convErr != nil`.
- `commands.go` / `bot_dispatch_hitl.go`: `/cancel` routes through `SubmitAnswer(…ActionCancel)` when
  `PendingFor` is non-empty + clears the inline keyboard.
- `cmd/aura/serve_channels.go:142`: stale comment corrected.
Consumes (does not create) the exported `agui.SanitizeString` (from 19-04) and the existing
`testdata/statuspane_run_error.golden`. No new migration/sqlc/CLI symbols.
</artifacts_this_phase_produces>

<verification>
- `go vet ./internal/channels/telegram/` clean; `go build ./internal/channels/... ./cmd/aura/` clean.
- `go test -race ./internal/channels/telegram/` green.
- H2 RunErrorEvent render test (golden match), H3 async-notice test, M-e /cancel-pause test all pass
  (and fail on a pre-fix tree).
</verification>

<success_criteria>
- H2: errored turns render a sanitized reason (golden updated); stale comment corrected.
- H3: async doc-convert failure notifies the user.
- M-e: /cancel cancels a pending pause + clears keyboard.
- All three findings + the comment fix in one Telegram-surface commit; depends on 19-04's SanitizeString.
</success_criteria>

<output>
Create `.planning/phases/19-audit-bug-resolution-e2e-live-test/19-05-SUMMARY.md` when done.
</output>
