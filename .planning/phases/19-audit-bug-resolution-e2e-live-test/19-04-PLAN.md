---
phase: 19-audit-bug-resolution-e2e-live-test
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agui/server.go
  - internal/agui/fanout.go
  - internal/agui/server_test.go
  - internal/agui/fanout_test.go
autonomous: true
requirements: [H1, M-c, M-d]
must_haves:
  truths:
    - "A slow AG-UI subscriber that overflows the buffer keeps a protocol-VALID surviving sub-sequence: a START frame is never dropped while its CONTENT/END is delivered; events.ValidateSequence accepts the survivors"
    - "The in-process Fanout path sanitizes RUN_ERROR Message AND the reasoning-trace event JSON (no DSN/token/bearer leaks), matching the HTTP path's redactEvent"
    - "A []InputContent (multimodal) last user message is NOT silently skipped — it is rejected explicitly or its text parts are projected"
  artifacts:
    - path: "internal/agui/server.go"
      provides: "widened isLifecycleFrame (all boundary frames non-droppable) + exported SanitizeString"
      contains: "func SanitizeString"
    - path: "internal/agui/fanout.go"
      provides: "sanitized RUN_ERROR Message + sanitized trace JSON on the in-process path"
      contains: "SanitizeString"
  key_links:
    - from: "internal/agui/fanout.go"
      to: "isLifecycleFrame (widened)"
      via: "send() treats all START/END/RESULT/CUSTOM/SNAPSHOT as non-droppable"
      pattern: "isLifecycleFrame"
    - from: "internal/agui/fanout.go"
      to: "SanitizeString"
      via: "sanitize err.Error() before NewRunErrorEvent + sanitize the traced event JSON"
      pattern: "SanitizeString|sanitize"
---

<objective>
Close the AG-UI transport cluster: H1 (SSE backpressure drops protocol BOUNDARY frames, so a
conformant client rejects the whole stream), M-c (a LIVE data leak today — the in-process Fanout
path forwards `err.Error()` verbatim into RUN_ERROR AND writes raw event JSON (DSN/token) into a
reasoning trace, while the HTTP path redacts via `redactEvent`), and M-d (`lastUserMessage` silently
drops `[]InputContent` multimodal content, driving the turn over rehydrated history with no new input
and no 400). All three live in `server.go` / `fanout.go`.

This plan ALSO exports the shared `SanitizeString` symbol (RESEARCH OQ3) that 19-05 (Telegram H2)
depends on — coordinate the export here so both the Fanout (M-c) and the Telegram renderer (H2) route
through ONE redaction contract.

Purpose: H1 is protocol corruption (Tampering); M-c is an active secret-disclosure leak (ASVS V7,
live today in the reasoning trace); M-d is silent input loss.

Output: a widened `isLifecycleFrame` (shared by the SSE pump and the in-process fanout), the exported
`SanitizeString`, the sanitized Fanout path, explicit multimodal handling in `lastUserMessage`, and the
rewritten `TestFanoutSlowSubscriberDropped` (D-04) calling `events.ValidateSequence` on the survivors.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-CONTEXT.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-RESEARCH.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-PATTERNS.md
@docs/audit/deep-correctness-audit-2026-06-10.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen isLifecycleFrame to all boundary frames + rewrite TestFanoutSlowSubscriberDropped (H1)</name>
  <read_first>
    - internal/agui/server.go (CURRENT isLifecycleFrame at :264-271 — today returns true ONLY for
      RunStarted/RunFinished/RunError; the SSE pump at ~:241 that calls it)
    - internal/agui/fanout.go (CURRENT send() at :112-143 — calls isLifecycleFrame; the cap-64 buffer;
      the `default:` drop arm at :134-141; the source loop at :80-101)
    - internal/agui/fanout_test.go (CURRENT TestFanoutSlowSubscriberDropped — today only asserts
      `len <= want` + first/last lifecycle; never re-validates the surviving sub-sequence)
    - The AG-UI SDK events.go (module cache) — events.ValidateSequence signature (:229) and the
      EventType constants (:14-71); 19-RESEARCH.md §"H1 frame classifier" enumerates the exact 13
      boundary frame types the translator emits and the ONLY droppable deltas
    - 19-RESEARCH.md §"Code Examples → H1 frame classifier" + §"H1 test — call ValidateSequence",
      §"Don't Hand-Roll → ValidateSequence"
    - docs/audit/deep-correctness-audit-2026-06-10.md §H1
    - .claude/skills/golang-concurrency/SKILL.md (goleak — fanout_test already uses it)
  </read_first>
  <action>
    Widen `isLifecycleFrame` (server.go:264) to return true for ALL boundary frames, not just the 3 run
    frames. Per RESEARCH's verified enumeration, return true for: `EventTypeRunStarted`,
    `EventTypeRunFinished`, `EventTypeRunError`, `EventTypeTextMessageStart`, `EventTypeTextMessageEnd`,
    `EventTypeToolCallStart`, `EventTypeToolCallEnd`, `EventTypeToolCallResult`, `EventTypeReasoningStart`,
    `EventTypeReasoningMessageStart`, `EventTypeReasoningMessageEnd`, `EventTypeReasoningEnd`,
    `EventTypeCustom`, `EventTypeStateSnapshot`. The `default:` (droppable) arm covers ONLY the repeatable
    deltas: `EventTypeTextMessageContent`, `EventTypeToolCallArgs`, `EventTypeReasoningMessageContent`,
    `EventTypeStateDelta`. Update the doc comment to state the new contract (dropping a START orphans its
    CONTENT/END and a conformant client rejects the whole turn). The classifier is shared by the SSE pump
    (server.go:241) and the in-process fanout (fanout.go:113) — fix it ONCE, both consume it.
    Rewrite `TestFanoutSlowSubscriberDropped` (D-04 — cite the false-green justification in the commit msg):
    overflow a slow subscriber past the cap-64 buffer, drain what it received, and assert
    `events.ValidateSequence(survivors) == nil` (the public SDK conformance check) AND that no START frame
    appears among the dropped frames. Keep goleak.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/agui/ && go vet ./internal/agui/ && go test -race -run 'TestFanout|TestIsLifecycle|TestSlowSubscriber' ./internal/agui/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): `go test -run TestFanoutSlowSubscriberDropped ./internal/agui/`
      overflows a slow subscriber and asserts `events.ValidateSequence(survivors) == nil` AND no START frame
      was dropped. BEFORE the fix a START frame is dropped while its CONTENT/END is delivered, so
      ValidateSequence rejects the survivors ("cannot add content to message that was not started"); AFTER,
      only repeatable deltas drop and the sub-sequence stays valid.
    - `go test -race ./internal/agui/` green (goleak clean — no producer goroutine leak under overflow).
    - `go vet ./internal/agui/` clean; ≤600 LOC.
  </acceptance_criteria>
  <done>
    Only repeatable delta frames may drop under backpressure; all boundary frames survive; the rewritten
    fanout test validates the surviving sub-sequence via the SDK conformance check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Export SanitizeString + sanitize the Fanout RUN_ERROR path (M-c) + explicit multimodal handling (M-d)</name>
  <read_first>
    - internal/agui/server.go (CURRENT sanitizeString at :403-419 — the redaction chokepoint;
      sanitizeErr at :392; redactEvent at :426-433 — the HTTP path the Fanout bypasses; lastUserMessage at :318-328)
    - internal/agui/fanout.go (CURRENT source loop at :80-101 — line :86 `ev = events.NewRunErrorEvent(err.Error())`
      forwards verbatim; line :91-95 `reasoningtrace.Record(... "event_json": eventJSONString(ev))` traces RAW JSON;
      the same raw-JSON trace in the drop arm at :138)
    - 19-RESEARCH.md §"Shared Patterns → Error/secret sanitization" (export SanitizeString — OQ3),
      §"M-c" + §"M-d" rows in Validation Architecture, §"Anti-Patterns → Don't widen the translator for M-c"
    - 19-PATTERNS.md §"M-c", §"M-d", §"Shared Patterns → Error/secret sanitization"
    - docs/audit/deep-correctness-audit-2026-06-10.md §M-c, §M-d
    - .claude/skills/golang-security/SKILL.md (secret-in-error-string leak, ASVS V7)
  </read_first>
  <action>
    Export the existing `sanitizeString` as `SanitizeString` (server.go:403) — rename and update the
    internal callers (`sanitizeErr`, `redactEvent`) to call the exported name. This is the one shared
    redaction contract 19-05 (Telegram H2) and this plan's M-c both reuse. (Coordinate: 19-05 depends on
    this export landing — it is in wave 2 after this plan.)
    M-c: on the in-process Fanout path (fanout.go), sanitize at the translator boundary — when wrapping an
    error into a RUN_ERROR at :86, use `events.NewRunErrorEvent(SanitizeString(err.Error()))`; and pass the
    traced event JSON through redaction before recording it (sanitize the RUN_ERROR Message via `redactEvent`
    BEFORE `eventJSONString`, at both :91-95 and the drop-arm trace :138). Do NOT widen the boundary-tested
    translator — sanitize ONLY at the Fanout boundary (RESEARCH anti-pattern).
    M-d: in `lastUserMessage` (server.go:318-328), when the last user message's `Content` is a
    `[]InputContent` (multimodal) rather than a `string`, do NOT silently skip it. Either reject explicitly
    (so the caller gets a 400 instead of an empty-input turn over rehydrated history) OR project the text
    parts (mirror the `payloadString` type-switch over `any` at server.go:300-313). Pick the explicit
    rejection unless text-part projection is trivially available; document the choice in the code comment.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/agui/ ./internal/channels/... && go vet ./internal/agui/ && go test -run 'TestSanitize|TestFanout|TestRunError|TestLastUserMessage|TestMultimodal' ./internal/agui/</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after): an M-c test drives a Fanout RUN_ERROR carrying a DSN
      / bearer token and asserts BOTH the emitted RunErrorEvent.Message AND the recorded reasoning-trace JSON
      contain NO DSN/token/bearer (SanitizeString applied). BEFORE the fix the verbatim secret leaks into
      both; AFTER, both are redacted.
    - Layer-1 regression (fails-before / passes-after): an M-d test passes a `[]InputContent` last user
      message and asserts it is rejected explicitly (or text-projected), NOT silently skipped to drive over
      rehydrated history. Fails before, passes after.
    - `SanitizeString` is exported and all prior internal callers (`sanitizeErr`, `redactEvent`) compile
      against it; `go build ./internal/agui/ ./internal/channels/...` clean.
    - `go vet ./internal/agui/` clean; ≤600 LOC.
  </acceptance_criteria>
  <done>
    The Fanout path redacts secrets in RUN_ERROR + traces (matching the HTTP path), multimodal last-user
    messages are handled explicitly, and `SanitizeString` is the shared exported redaction seam for 19-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent loop → in-process Fanout → Telegram/AG-UI consumers | Error strings + event JSON cross out of the agent; they may embed DSNs/tokens |
| AG-UI client → server (RunAgentInput) | Untrusted client input (multimodal content) crosses into the turn driver |
| server → conformant AG-UI client (SSE stream) | Protocol frames cross to a client that runs ValidateSequence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Information Disclosure | Fanout RUN_ERROR + trace JSON leak DSN/token (M-c, ASVS V7 — LIVE today) | mitigate | Apply the existing `SanitizeString`/`redactEvent` chokepoint at the Fanout boundary for both the emitted Message and the traced JSON; matches the HTTP path |
| T-19-09 | Tampering (protocol integrity) | dropped boundary frame corrupts the stream (H1) | mitigate | Widen `isLifecycleFrame` so only repeatable deltas drop; rewritten test runs `ValidateSequence` (the conformant-client check) on the survivors |
| T-19-10 | Spoofing / Input validation | silently-dropped multimodal input drives over rehydrated history (M-d, ASVS V5) | mitigate | `lastUserMessage` rejects `[]InputContent` explicitly (or projects text parts) instead of a silent skip |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH §Package Legitimacy Audit: N/A); go.mod unchanged |

HIGH-severity threat T-19-08 (live secret disclosure) is MITIGATED, not accepted — block gate satisfied.
</threat_model>

<artifacts_this_phase_produces>
New exported symbol: `SanitizeString(string) string` (renamed from the internal `sanitizeString` in
`internal/agui/server.go`) — the shared redaction contract reused by 19-05 (Telegram H2) and the M-c
Fanout path.
Modified behavior: `isLifecycleFrame` widened (server.go); Fanout RUN_ERROR Message + trace JSON
sanitized (fanout.go); `lastUserMessage` explicit multimodal handling (server.go); rewritten
`TestFanoutSlowSubscriberDropped`.
No new files, migration objects, sqlc symbols, or CLI flags.
</artifacts_this_phase_produces>

<verification>
- `go vet ./internal/agui/` clean; `go build ./internal/agui/ ./internal/channels/...` clean.
- `go test -race ./internal/agui/` green (goleak clean).
- TestFanoutSlowSubscriberDropped (ValidateSequence), the M-c redaction test, and the M-d multimodal
  test all pass (and fail on a pre-fix tree).
</verification>

<success_criteria>
- H1: only repeatable deltas drop; survivors pass ValidateSequence.
- M-c: Fanout RUN_ERROR + trace redacted (HIGH leak closed). M-d: multimodal handled explicitly.
- SanitizeString exported for 19-05. All three findings + the false-green rewrite in one AG-UI commit.
</success_criteria>

<output>
Create `.planning/phases/19-audit-bug-resolution-e2e-live-test/19-04-SUMMARY.md` when done.
</output>
