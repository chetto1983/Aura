---
phase: 19-audit-bug-resolution-e2e-live-test
plan: 11
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03", "19-04", "19-05", "19-06", "19-07", "19-08", "19-09", "19-10"]
files_modified:
  - docs/audit/19-LIVE-SIGNOFF-2026-06-10.md
autonomous: false
requirements: [H1, H2, H3, H4, H5, H6, H7, H9, M-b, M-e]
must_haves:
  truths:
    - "Every user-observable finding has a live before/after repro driven by the REAL paid agent with a REAL user prompt (no scripted fixtures), recorded in the sign-off doc with ground-truth evidence"
    - "The shell-never-answer cluster (H4/H5/M-b) answers a real terminal task live on the aura chat host loop"
    - "The Telegram surfaces (H2 error render, H3 doc-conversion notice, M-e /cancel-during-pause) confirm live via the CDP harness with DB as ground truth"
    - "The scheduler notify (H6/H7) and AG-UI slow-client (H1) and SSE truncation (H9) confirm live on the running stack"
  artifacts:
    - path: "docs/audit/19-LIVE-SIGNOFF-2026-06-10.md"
      provides: "the Layer-2 live operator sign-off evidence doc (mirror Phase 13-10 / Phase 8)"
      contains: "before"
  key_links:
    - from: "docs/audit/19-LIVE-SIGNOFF-2026-06-10.md"
      to: "DB / filesystem ground-truth"
      via: "each repro records a non-r.Reply assertion (DB row / tool trace / rendered body)"
      pattern: "ground.truth|DB|trace"
---

<objective>
Layer 2 of the two-layer proof (D-03): a live before/after repro of EVERY user-observable finding,
driven by the REAL PAID agent with a REAL user prompt — no scripted fixtures, no babysitting (user
directive: "we must debug with live agent (paid) with user prompt no baby sisttin"). This is a MANUAL
operator sign-off gate, NOT CI-automated (the live stack is paid + manually driven; CI runs only the
Layer-1 regressions in 19-01..19-10). This plan runs LAST (wave 3), after every fix has landed and the
full Layer-1 matrix is green.

Mirror the Phase 13-10 / Phase 8 live sign-off pattern: record before/after evidence in a doc under
`docs/` (NOT /tmp — [[feedback_no_docs_in_tmp]]), with ≥1 visual body print + a mojibake/structure scan,
and a ground-truth assertion that does NOT look at `r.Reply` (DB row / `· <toolname>` tool trace /
rendered Telegram body / filesystem) per [[feedback_probe_must_verify_artifact_not_reply]].

Batch the paid runs + get explicit operator go before any paid call ([[feedback_no_unsolicited_paid_runs_batch_calls]]).

Purpose: prove the fixes hold for the real user, not just in unit tests.

Output: `docs/audit/19-LIVE-SIGNOFF-2026-06-10.md` with the recorded before/after evidence per finding.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-CONTEXT.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-RESEARCH.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-VALIDATION.md
@docs/audit/deep-correctness-audit-2026-06-10.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live repro — shell-never-answer (H4/H5/M-b) + SSE truncation (H9) on the aura chat host loop</name>
  <files>docs/audit/19-LIVE-SIGNOFF-2026-06-10.md</files>
  <what-built>
    19-01 (H4 process-group kill + WaitDelay, H5 footer survival, L3/M-f), 19-02 (M-b veto-no-resurface),
    19-03 (H9 stream-error contract) all landed with green Layer-1 regressions. This task confirms the
    user-observable behavior LIVE on the `aura chat` host loop with the real paid agent.
  </what-built>
  <read_first>
    - Memory: [[reference_run_aura_binary_live_env_loading]] (binary doesn't auto-load .env; strip single
      quotes from POSTGRES_PASSWORD/NEO4J_PASSWORD; pipe prompts + /exit to `aura chat new`, cwd=d:/Aura)
    - Memory: [[reference_live_tool_selection_trace]] (`· <toolname>` trace = ground truth, never r.Reply;
      build in an isolated worktree at HEAD to dodge parallel-Codex compile breaks; `set -a; source <(tr -d '\r' < .env); set +a`)
    - 19-RESEARCH.md §"Validation Architecture → Layer 2" (the H4/H5/M-b + H9 live repro shapes)
    - 19-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <action>
    OPERATOR (manual, paid — NOT CI): build the fresh binary, run the real paid agent over a real user
    prompt on the `aura chat` host loop, reproduce the H4/H5/M-b shell-never-answer behavior and the H9
    SSE-truncation behavior LIVE, and record before/after evidence (the `· shell_exec` tool trace + the
    reply body, visual print + mojibake scan) in docs/audit/19-LIVE-SIGNOFF-2026-06-10.md. Follow the
    <how-to-verify> steps. This is the Layer-2 sign-off gate; do NOT mark it CI-automated.
  </action>
  <how-to-verify>
    1. Build the current binary in an isolated worktree at HEAD (load .env via `set -a; source <(tr -d '\r' < .env); set +a`).
    2. H4/H5/M-b: with the FRESH build, prompt the real agent (real user prompt, e.g. "run a build that
       spawns a backgrounded process and tell me the exit code") so it issues a shell command that spawns an
       orphan-prone grandchild and produces large output. Confirm via the `· shell_exec` trace + the final
       reply: the turn ANSWERS (not silence, not "run it yourself", not a false success) and the agent is
       aware of the real exit code. Record the `· <toolname>` trace + the reply body (visual print + mojibake scan).
    3. H9: induce / observe a mid-stream condition (a long answer; if a deterministic mid-stream cut is not
       reproducible live, note the Layer-1 premature_close.sse proof as the binding evidence and record the
       live attempt). Confirm a truncated stream surfaces an error/retry, not a silently-truncated "answer".
    4. Record BEFORE (cite the pre-fix audit behavior from docs/audit/deep-correctness-audit-2026-06-10.md)
       and AFTER (the observed live behavior + ground-truth trace) in docs/audit/19-LIVE-SIGNOFF-2026-06-10.md.
  </how-to-verify>
  <verify>
    <human-check>The sign-off doc records the H4/H5/M-b live repro (real prompt + `· shell_exec` tool trace + reply body, before/after) and the H9 live observation, with ≥1 non-r.Reply ground-truth assertion.</human-check>
  </verify>
  <done>
    The sign-off doc records the H4/H5/M-b + H9 live before/after evidence with the tool-trace ground truth.
  </done>
  <acceptance_criteria>
    - The sign-off doc records, for H4/H5/M-b: the real prompt used, the `· shell_exec` tool trace
      (ground-truth, not r.Reply), the final reply body (printed + mojibake-scanned), and a before/after
      contrast. The turn answers with correct exit-code awareness.
    - The sign-off doc records the H9 live observation (or the explicit note that the Layer-1
      premature_close.sse regression is the binding proof + the live attempt result).
    - Evidence includes ≥1 non-r.Reply ground-truth assertion (the tool trace).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded evidence, or describe what failed live.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live repro — Telegram surfaces (H2 error render, H3 doc-conversion notice, M-e /cancel-during-pause) via the CDP harness</name>
  <files>docs/audit/19-LIVE-SIGNOFF-2026-06-10.md</files>
  <what-built>
    19-05 (H2 RunErrorEvent render via the shared SanitizeString, H3 async convert-failure notice, M-e
    /cancel-during-pause) landed with green Layer-1 regressions. This task confirms the user-observable
    behavior LIVE on Telegram via the CDP harness with DB as ground truth.
  </what-built>
  <read_first>
    - Memory: [[reference_cdp_telegram_live_test_harness]] (drive web.telegram.org via Chrome
      --remote-debugging-port=9222 + py -3 playwright connect_over_cdp; D:/tmp/tg_cdp.py driver; QR login
      once; py -3 not .venv; slash-cmds via PowerShell (MSYS mangles /cost); DB = ground truth)
    - Memory: [[project_phase13_10_live_signoff_resume]] (the prior Telegram live sign-off pattern + the
      "tested-but-not-wired" class to watch for)
    - 19-RESEARCH.md §"Validation Architecture → Layer 2" (H2/H3/M-e live repro shapes)
    - 19-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <action>
    OPERATOR (manual, paid — NOT CI): start the real bot against the fresh build, connect the CDP harness,
    reproduce H2 (errored turn → sanitized reason), H3 (async doc-convert failure → convertFailMessage), and
    M-e (/cancel during a pause → paused_states resolved + keyboard cleared) LIVE, and record before/after
    evidence (rendered body printed + scanned + the DB ground-truth assertion) in
    docs/audit/19-LIVE-SIGNOFF-2026-06-10.md. Follow the <how-to-verify> steps. Layer-2 sign-off gate; not CI.
  </action>
  <how-to-verify>
    1. Start the real bot against the fresh build; connect the CDP harness (D:/tmp/tg_cdp.py, Chrome :9222).
    2. H2: induce a turn error (e.g. force a model-unavailable / DB error condition); confirm the Telegram
       message shows a SANITIZED reason (not a bare "Stato: errore", no DSN/token). DB = ground truth for the turn state.
    3. H3: send a 5-50MB document that fails async conversion; confirm `convertFailMessage` arrives (not an
       eternal "elaborando…" then silence).
    4. M-e: trigger an ask_user pause, send `/cancel`; confirm the paused_states row is RESOLVED (DB ground
       truth) and the inline keyboard is retired/cleared.
    5. Record BEFORE/AFTER for each in docs/audit/19-LIVE-SIGNOFF-2026-06-10.md with the rendered body
       (visual print + mojibake scan) and the DB ground-truth assertion.
  </how-to-verify>
  <verify>
    <human-check>The sign-off doc records H2 (sanitized reason rendered), H3 (convertFailMessage arrived), and M-e (paused_states resolved per DB + keyboard cleared), each before/after with a DB/rendered-body ground-truth assertion.</human-check>
  </verify>
  <done>
    The sign-off doc records the H2/H3/M-e Telegram live before/after evidence with DB ground truth.
  </done>
  <acceptance_criteria>
    - The sign-off doc records H2 (sanitized reason rendered — printed + scanned), H3 (convertFailMessage
      arrived), M-e (paused_states resolved per DB + keyboard cleared), each with a before/after contrast.
    - Each repro includes a DB / rendered-body ground-truth assertion (not r.Reply).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded evidence, or describe what failed live.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live repro — scheduler notify (H6/H7) on a real cron tick + AG-UI slow client (H1)</name>
  <files>docs/audit/19-LIVE-SIGNOFF-2026-06-10.md</files>
  <what-built>
    19-07 (H6 quiet-hours defer + window-end flush, H7 failed-self-send bounded retry, migration 0013) and
    19-04 (H1 boundary-frame classification) landed with green Layer-1 regressions. This task confirms the
    user-observable behavior LIVE on the running stack (real cron tick + a deliberately-slow SSE client).
  </what-built>
  <read_first>
    - Memory: [[reference_e2e_full_matrix_invocation]] (full live tier invocation + DSN/env gotchas; DB ground truth)
    - 19-RESEARCH.md §"Pitfall 5" (the H6/H7 live test needs a real cron tick + real quiet-hours window —
      configure AURA_SCHEDULER_QUIET_HOURS to cover now, schedule a job, let the tick fire, advance past the
      window, confirm delivery), §"Validation Architecture → Layer 2" (H6/H7 + H1 live shapes)
    - 19-VALIDATION.md §"Manual-Only Verifications"
  </read_first>
  <action>
    OPERATOR (manual, paid — NOT CI): apply migration 0013 to the live DB, then reproduce H6 (quiet-hours
    defer → window-end flush), H7 (failed self-send → bounded re-attempt) on a real cron tick, and H1 (slow
    SSE client keeps a ValidateSequence-valid stream) LIVE, recording before/after evidence with DB ground
    truth in docs/audit/19-LIVE-SIGNOFF-2026-06-10.md. Follow the <how-to-verify> steps. Layer-2 sign-off gate; not CI.
  </action>
  <how-to-verify>
    1. Apply migration 0013 (`aura db migrate`) against the live DB FIRST (the H6/H7 test runs against a
       freshly-migrated DB — Runtime State Inventory requirement).
    2. H6: configure AURA_SCHEDULER_QUIET_HOURS to cover now; schedule a job; let the tick fire (run
       completes, notification deferred). Confirm a pending_notifications row with notify_after = window end
       (DB ground truth). Advance past the window; confirm the sweep delivers it (notify_status='delivered'
       + the route/stdout sink received it).
    3. H7: induce a self-send failure (no MCP route mounted); confirm notify_status='failed' + attempts
       increments; a later tick re-attempts within the bound.
    4. H1: drive AG-UI SSE against `aura serve` with a deliberately-slow subscriber; confirm a conformant
       client (running events.ValidateSequence) ACCEPTS the surviving turn (before the fix it rejected the
       whole turn: "cannot add content to message that was not started").
    5. Record BEFORE/AFTER for each in docs/audit/19-LIVE-SIGNOFF-2026-06-10.md with DB ground truth.
  </how-to-verify>
  <verify>
    <human-check>The sign-off doc records H6 (deferred → delivered, DB pending_notifications rows), H7 (failed → bounded re-attempt, DB attempts), and H1 (slow client keeps a ValidateSequence-valid stream), each before/after with a DB / protocol-conformance ground-truth assertion. Migration 0013 applied to the live DB.</human-check>
  </verify>
  <done>
    The sign-off doc records the H6/H7/H1 live before/after evidence with DB ground truth; 0013 applied live.
  </done>
  <acceptance_criteria>
    - The sign-off doc records H6 (deferred → delivered at window end, DB pending_notifications rows),
      H7 (failed → bounded re-attempt, DB attempts increment), and H1 (slow client keeps a ValidateSequence-valid
      stream), each with a before/after contrast and a DB / protocol-conformance ground-truth assertion.
    - Migration 0013 was applied to the live DB before the H6/H7 repro.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded evidence, or describe what failed live.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live paid agent → real user surfaces | This plan EXERCISES the boundaries the fixes hardened; it introduces none |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-30 | (validation only) | live operator sign-off | accept | This plan adds NO code (only a sign-off doc); it validates the threats mitigated in 19-01..19-10. The paid-run discipline ([[feedback_no_unsolicited_paid_runs_batch_calls]]) governs cost, not security. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; doc-only plan. RESEARCH §Package Legitimacy Audit: N/A |

This plan introduces no new attack surface — it is the live confirmation of the prior plans' mitigations.
</threat_model>

<artifacts_this_phase_produces>
New file: `docs/audit/19-LIVE-SIGNOFF-2026-06-10.md` (the Layer-2 live operator sign-off evidence doc).
No code changes, exported symbols, migration objects, sqlc symbols, or CLI flags. Consumes the fixes from
19-01..19-10 and the live harnesses (`D:/tmp/tg_cdp.py`, `aura chat` host loop) that already exist.
</artifacts_this_phase_produces>

<verification>
- This plan is `autonomous: false` (live operator sign-off, NOT CI). Its "verification" is the recorded
  before/after evidence in docs/audit/19-LIVE-SIGNOFF-2026-06-10.md with DB / tool-trace / rendered-body
  ground-truth assertions (not r.Reply) for every user-observable finding.
- Precondition: the full Layer-1 matrix (19-01..19-10) is green in CI and migration 0013 is applied to the live DB.
</verification>

<success_criteria>
- Every user-observable finding (H1, H2, H3, H4, H5, H6, H7, H9, M-b, M-e) has a recorded live before/after
  repro driven by the real paid agent with a real user prompt, with ground-truth evidence.
- The sign-off doc mirrors the Phase 13-10 / Phase 8 pattern (visual body print + mojibake scan + DB/trace assertion).
- Operator sign-off recorded; this plan ran LAST after all fixes + the green Layer-1 matrix.
</success_criteria>

<output>
Create `.planning/phases/19-audit-bug-resolution-e2e-live-test/19-11-SUMMARY.md` when done.
</output>
