---
phase: 19-audit-bug-resolution-e2e-live-test
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/migrations/0013_pending_notifications.up.sql
  - internal/db/migrations/0013_pending_notifications.down.sql
  - internal/db/queries/pending_notifications.sql
  - internal/db/sqlc/pending_notifications.sql.go
  - internal/db/sqlc/models.go
  - internal/db/sqlc/querier.go
  - internal/cron/store_runs.go
  - internal/cron/dispatch.go
  - internal/cron/notify.go
autonomous: true
requirements: [H6, H7]
must_haves:
  truths:
    - "A successful non-reminder run deferred during quiet hours persists a pending notification with notify_after = window end; a tick sweep flushes it after the window so the user actually learns the job ran"
    - "A failed MCP self-send persists an undelivered (failed) pending notification; a later tick re-attempts it bounded by attempts < N, then stops re-selecting once the bound is exhausted"
    - "The sweep uses a REAL FOR UPDATE SKIP LOCKED inside a tx (unlike the inert autocommit one) and honors pgx lazy-error discipline (rows.Err after loop, SQLSTATE reclassification)"
  artifacts:
    - path: "internal/db/migrations/0013_pending_notifications.up.sql"
      provides: "aura.pending_notifications table + partial index on status='pending' + role grants"
      contains: "pending_notifications"
    - path: "internal/db/queries/pending_notifications.sql"
      provides: "Insert/Sweep/MarkDelivered/MarkFailed sqlc queries"
      contains: "SweepDueNotifications"
    - path: "internal/cron/store_runs.go"
      provides: "store methods wrapping the generated client (sweep + status writers)"
      contains: "PendingNotification"
  key_links:
    - from: "internal/cron/dispatch.go"
      to: "store.InsertPendingNotification"
      via: "H6 quiet-hours defer + H7 self-send failure persist a pending row instead of logging+returning"
      pattern: "PendingNotification|notify_after"
    - from: "internal/cron/dispatch.go"
      to: "store.SweepDueNotifications"
      via: "a new dispatcher tick pass flushes due/failed notifications via Notifier.Notify"
      pattern: "SweepDueNotifications|sweepNotifications"
---

<objective>
Close H6 + H7 (the dropped scheduler-notification contracts) with REAL durable machinery (D-02 —
NO doc-downgrade). Today `notify` (dispatch.go:143-161) returns after a WARN with NO persistence on
two paths: a quiet-hours-deferred successful run (line 154-156, "deferred to window end" — but the run
already completed terminally and `next_run_at` advanced, so the notification is LOST and the user never
learns the job ran), and a failed MCP self-send (line 158-160, logged as "bound-retry on a later tick"
— but the completed run is terminal and never re-selected, so the "bound-retry" is fiction). No
notification-state column or table exists (grep-confirmed).

This plan ships the net-new persistence: a new Postgres migration **`0013`** (head is `0012_telegram`,
RESEARCH correction — NOT 0011 as the audit said) creating `aura.pending_notifications`, the sqlc
queries, the regenerated client, the store methods, and a tick sweep that flushes deferred
notifications at quiet-hours window end (H6) and re-attempts failed self-sends bounded (H7).

Purpose: the user must actually learn a deferred job ran, and a transiently-failed notification must
actually be retried within a bound (the D-22 contract becomes real).

Output: migration 0013 + queries + regenerated sqlc + store methods + the dispatcher sweep wiring.
This plan MUST land BEFORE 19-08 (the rest of the scheduler cluster shares these cron files + needs
0013 applied).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-CONTEXT.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-RESEARCH.md
@.planning/phases/19-audit-bug-resolution-e2e-live-test/19-PATTERNS.md
@docs/audit/deep-correctness-audit-2026-06-10.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0013 pending_notifications + sqlc queries + regenerated client</name>
  <read_first>
    - internal/db/migrations/0009_scheduler.up.sql (the template — role-separated grants `aura_app`
      SELECT/INSERT/UPDATE + `aura_migrate` ALL; partial index on the poll predicate; the implicit-tx
      CONCURRENTLY hazard note at :48-52; agent_job_runs FK to scheduler_tasks)
    - internal/db/migrations/0009_scheduler.down.sql (drop child-first / FK-order discipline)
    - internal/db/migrations/0012_telegram.up.sql (CONFIRM head is 0012 → new is 0013)
    - internal/db/queries/agent_job_runs.sql (the sqlc query style: `-- name: X :one|:many|:exec`,
      full-column RETURNING, `make_interval(secs => $1)` interval idiom)
    - internal/db/queries/scheduler_tasks.sql (DueTasks at ~:26-34 — the FOR UPDATE SKIP LOCKED shape to
      model the sweep on; L5 notes this one is inert on autocommit, but the NEW sweep runs in a tx so its
      SKIP LOCKED is REAL)
    - internal/db/sqlc.yaml (pgx/v5, emit_interface, out=internal/db/sqlc, db.go/models.go/querier.go names)
    - 19-RESEARCH.md §"Pattern 6" (durable notification state — schema shape (b) recommended), §"Open
      Questions Q1" (new table recommended over agent_job_runs columns), §"Runtime State Inventory"
      (run `sqlc generate` + commit the regeneration; no data backfill — no prior state to migrate)
    - 19-PATTERNS.md §"NET-NEW 2" (the exact 0009/agent_job_runs/store_runs analogs + the 4 queries needed)
    - docs/audit/deep-correctness-audit-2026-06-10.md §H6, §H7
    - .claude/skills/golang-database/SKILL.md (pgx, migrations), .claude/skills/neo4j-* are NOT relevant here
  </read_first>
  <action>
    Create migration `internal/db/migrations/0013_pending_notifications.up.sql` and `.down.sql` following
    the 0009 template. Schema (RESEARCH-recommended, A2 — the new-table option):
    `CREATE TABLE aura.pending_notifications` with columns: `id uuid PRIMARY KEY`,
    `run_id uuid NOT NULL REFERENCES aura.agent_job_runs (id) ON DELETE CASCADE`,
    `notify_route text`, `body text NOT NULL`, `notify_after timestamptz NOT NULL`,
    `attempts int NOT NULL DEFAULT 0`, `last_error text`,
    `status text NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','failed','delivered'))`,
    `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()`.
    Add a partial index `CREATE INDEX pending_notifications_due_idx ON aura.pending_notifications (notify_after) WHERE status = 'pending';`
    Grants: `GRANT SELECT, INSERT, UPDATE ON aura.pending_notifications TO aura_app; GRANT ALL ON aura.pending_notifications TO aura_migrate;`
    (audit-leaning, no DELETE for aura_app — cascade handles cleanup). down.sql: `DROP TABLE IF EXISTS aura.pending_notifications;`
    Create `internal/db/queries/pending_notifications.sql` with sqlc queries copying the agent_job_runs.sql
    style: `InsertPendingNotification :one` (full-column RETURNING), `SweepDueNotifications :many`
    (`WHERE status='pending' AND notify_after <= now()` plus a `failed` AND `attempts < $bound`
    re-attempt branch, with `FOR UPDATE SKIP LOCKED` — REAL because it runs in a tx),
    `MarkNotificationDelivered :exec`, `MarkNotificationFailed :exec` (increment `attempts`, set
    `last_error`, `updated_at=now()`).
    Run `sqlc generate` (WSL `~/go/bin/sqlc` or full path) and COMMIT the regenerated
    `internal/db/sqlc/pending_notifications.sql.go` + the `models.go`/`querier.go` deltas.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/db/... && go vet ./internal/db/... && go test -tags db_integration -run 'TestMigrat|TestPendingNotif|Test0013' ./internal/db/ ./internal/cron/ 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 (migration applies + regenerates): `aura db migrate` (or the migration test) applies 0013
      idempotently; `internal/db/sqlc/pending_notifications.sql.go` exists with the 4 generated query
      methods; `go build ./internal/db/...` clean. BEFORE this task there is no pending_notifications table
      or generated symbol; AFTER, both exist and compile.
    - The migration follows 0009 role separation (aura_app DML-only, no DELETE; DDL to aura_migrate) and
      the partial index on `status='pending'`. No CONCURRENTLY inside the implicit tx.
    - sqlc regeneration committed (Runtime State Inventory requirement); `go vet ./internal/db/...` clean.
  </acceptance_criteria>
  <done>
    Migration 0013 creates aura.pending_notifications with the durable notification-state schema; the sqlc
    queries + regenerated client compile and are committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Persist deferred/failed notifications + tick sweep (H6 + H7)</name>
  <read_first>
    - internal/cron/dispatch.go (CURRENT notify at :143-161 — H6 quiet-hours defer logs+returns at :154-156;
      H7 self-send failure logs "bound-retry on a later tick" at :158-160; the deferred predicate at :163;
      complete at :120-135; the DispatchDeps + Notifier + QuietHours at :62-69; where the tick loop calls
      catch-up/orphan recovery — wire the sweep into that same tick pass)
    - internal/cron/notify.go (CURRENT Notifier interface at :65; compositeNotifier.Notify at :92 — returns
      the undelivered error the sweep re-calls; the stdout fallback at :166)
    - internal/cron/store_runs.go (CURRENT DueTasks at :81-94 — the clamp-at-boundary + rows→[]T loop +
      rows.Err discipline the new SweepDueNotifications store method copies; insertRunOnConn at :23-38 —
      the sqlc.New(conn) + RETURNING→struct template for InsertPendingNotification; int4OrNull clamp idiom)
    - 19-RESEARCH.md §"Pattern 6" (tick-sweep shape + bound), §"Pitfall 5" (the H6/H7 LIVE test needs a real
      cron tick + real quiet-hours window — Layer 2, plan 19-11), §"H6"/"H7" rows in Validation Architecture
    - 19-PATTERNS.md §"NET-NEW 2" (the dispatcher edit sites at dispatch.go:154-160 + the sweep wiring)
    - docs/audit/deep-correctness-audit-2026-06-10.md §H6, §H7
    - .claude/skills/golang-database/SKILL.md, .claude/skills/golang-error-handling/SKILL.md, .claude/skills/golang-context/SKILL.md
  </read_first>
  <action>
    Add store methods in `store_runs.go` wrapping the generated client: `InsertPendingNotification`
    (copy insertRunOnConn shape — sqlc.New + RETURNING→struct), `SweepDueNotifications(ctx, bound, limit)`
    (copy DueTasks clamp + rows-loop + `rows.Err()` after loop + SQLSTATE reclassification; run the SELECT
    inside `db.WithTx` so the FOR UPDATE SKIP LOCKED is real), `MarkNotificationDelivered`,
    `MarkNotificationFailed`.
    H6: in `notify` (dispatch.go:154-156), replace the log+return with
    `store.InsertPendingNotification(...)` carrying `notify_after` = the quiet-hours window END (compute
    from the QuietHours predicate / scheduler window), the route, and the body. The notification is now
    durable, not lost.
    H7: in `notify` (dispatch.go:158-160), when `Notifier.Notify` returns an undelivered error, persist a
    `failed` pending_notifications row (attempts=0, last_error) instead of just logging. The "bound-retry"
    is now backed.
    Add a new dispatcher sweep pass `sweepNotifications(ctx)` that: selects due pending notifications
    (`status='pending' AND notify_after<=now()` for H6 window-end flush; `status='failed' AND attempts<N`
    for H7 bounded re-attempt) via SweepDueNotifications, re-invokes `Notifier.Notify` for each, and updates
    status (MarkNotificationDelivered on success / MarkNotificationFailed incrementing attempts on failure).
    Wire `sweepNotifications` into the SAME tick pass where catch-up/orphan recovery runs (one extra pass
    per tick — NO new ticker, NO busy-poll; mini-PC budget). Bound the re-attempt count via a const
    (D-22 "bound-retry"); once `attempts >= N` the row is no longer re-selected.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/cron/ && go vet ./internal/cron/ && go test -tags db_integration -run 'TestNotify|TestSweep|TestPendingNotif|TestQuietHours|TestDispatch' ./internal/cron/ 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Layer-1 regression (fails-before / passes-after, H6, db_integration tier): a test schedules a job with
      a quiet-hours window covering now, runs the dispatch, and asserts a `pending_notifications` row exists
      with `status='pending'` and `notify_after` = window end; then advances past the window and runs the
      sweep, asserting the row flips to `delivered` and the Notifier received it. BEFORE the fix no row is
      written (notification lost); AFTER, it is deferred-then-delivered.
    - Layer-1 regression (fails-before / passes-after, H7, db_integration tier): a test induces a self-send
      failure (Notifier returns undelivered), asserts a `status='failed'` row with `attempts` incrementing;
      a later sweep re-attempts within the bound; once `attempts >= N` the row is no longer re-selected.
      BEFORE the fix nothing is persisted/retried; AFTER, bounded retry works.
    - No-skip-as-green: the db_integration tier `t.Fatal`s under `$CI` when `AURA_DB_URL` is unset (it must
      actually run against a migrated DB, not skip).
    - The sweep adds NO new ticker / busy-poll (it rides the existing tick pass); `rows.Err()` after the
      sweep loop; `go vet ./internal/cron/` clean; ≤600 LOC each file (split store_runs.go on touch if needed).
  </acceptance_criteria>
  <done>
    Quiet-hours-deferred notifications are persisted and flushed at window end (H6); failed self-sends are
    persisted and bounded-retried on a later tick (H7); the sweep rides the existing tick with no busy-poll.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → notification sink (MCP route / stdout) | The run-outcome notification crosses to the user's channel; a failed send must not be silently lost |
| daemon → Postgres (aura_app role) | The new durable state is written under the DML-only app role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-15 | Repudiation / Information loss | quiet-hours-deferred + failed notifications silently dropped (H6/H7) | mitigate | Persist a durable `pending_notifications` row + a bounded tick sweep that flushes/re-attempts; the user learns the job ran |
| T-19-16 | Elevation of Privilege | aura_app gaining DDL/DELETE on the new table | mitigate | Migration grants aura_app SELECT/INSERT/UPDATE only (no DELETE, no DDL); DDL reserved to aura_migrate (0009 role-separation precedent) |
| T-19-17 | Denial of Service | unbounded retry storm on a permanently-failing route (H7) | mitigate | The re-attempt is bounded (`attempts < N`); once exhausted the row is no longer re-selected (D-22 bound-retry) |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | Zero new external packages (sqlc/pgx/golang-migrate already in go.mod; sqlc generate is a build step). RESEARCH §Package Legitimacy Audit: N/A |

Threats NOT introduced: no secret/credential surface in the notification body beyond the existing route
(the notification text is the run summary, already user-bound); no new auth/crypto.
</threat_model>

<artifacts_this_phase_produces>
New files: `internal/db/migrations/0013_pending_notifications.up.sql` + `.down.sql`;
`internal/db/queries/pending_notifications.sql`; regenerated `internal/db/sqlc/pending_notifications.sql.go`.
New migration objects: `aura.pending_notifications` table, `pending_notifications_due_idx` partial index,
its role grants.
New sqlc-generated symbols: `InsertPendingNotification`, `SweepDueNotifications`,
`MarkNotificationDelivered`, `MarkNotificationFailed` (on the generated `Querier` + `Queries`), the
`PendingNotification` model struct, and their param structs — plus the `models.go`/`querier.go` deltas.
New store methods in `internal/cron/store_runs.go`: `InsertPendingNotification`, `SweepDueNotifications`,
`MarkNotificationDelivered`, `MarkNotificationFailed`. New dispatcher method `sweepNotifications`.
Modified behavior: `dispatch.go` notify H6/H7 paths persist instead of log+return; the tick pass calls the sweep.
No new CLI flags. (These NET-NEW symbols are excluded from drift verification per this section.)
</artifacts_this_phase_produces>

<verification>
- `go vet ./internal/db/... ./internal/cron/` clean; `go build ./...` clean (sqlc regeneration compiles).
- `go test -tags db_integration -race ./internal/cron/ ./internal/db/` green (stack up; DSNs composed) —
  the H6/H7 fixtures fail on a pre-fix tree and the tier `t.Fatal`s under $CI when env unset.
- Migration 0013 applies idempotently; sqlc regeneration committed.
</verification>

<success_criteria>
- H6: deferred notifications persisted + flushed at window end (db_integration proof). H7: failed
  self-sends persisted + bounded-retried.
- Migration 0013 + sqlc + store methods + sweep all landed; no busy-poll; role-separated grants.
- This plan lands BEFORE 19-08 (shared cron files + 0013 applied).
</success_criteria>

<output>
Create `.planning/phases/19-audit-bug-resolution-e2e-live-test/19-07-SUMMARY.md` when done.
</output>
