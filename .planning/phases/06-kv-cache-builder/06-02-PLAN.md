---
phase: 06-kv-cache-builder
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/agent/prompt/builder.go
  - internal/agent/prompt/hash.go
  - internal/agent/prompt/cache_anthropic.go
  - internal/agent/prompt/builder_test.go
  - internal/agent/prompt/hash_test.go
  - internal/agent/llm_agent.go
  - internal/llm/client.go
autonomous: true
requirements: [CAP-04]

must_haves:
  truths:
    - "PromptBuilder.Build assembles the wire llm.Request from history + registry + provider, reproducing today's byte-stable messages[0] exactly (D-01)"
    - "messages[0] is byte-identical across N successive Build() calls as history grows (monotonic growth, no mutation of [0])"
    - "PrefixHash(msgs, indices) returns a stable SHA-256 hex over the index-set, skipping absent indices so {0,1,2} works once Slices 10/11e ship (D-06a)"
    - "Under provider=openrouter the built Request carries NO cache_control; the anthropic branch is a dormant no-op seam (D-03)"
    - "internal/llm/client.go Request gains ToolsCacheControl and its 'wire layer is unaware' comment is corrected in the SAME commit (D-03a)"
  artifacts:
    - path: "internal/agent/prompt/builder.go"
      provides: "PromptBuilder.Build chokepoint extracted from llm_agent.go:131-137"
      contains: "func (b *PromptBuilder) Build"
    - path: "internal/agent/prompt/hash.go"
      provides: "PrefixHash index-set fingerprint via internal/canonicaljson"
      contains: "func PrefixHash"
    - path: "internal/agent/prompt/cache_anthropic.go"
      provides: "injectCacheControl provider branch (no-op unless anthropic)"
      contains: "injectCacheControl"
    - path: "internal/llm/client.go"
      provides: "Request.ToolsCacheControl field + corrected caching-aware comment"
      contains: "ToolsCacheControl"
  key_links:
    - from: "internal/agent/llm_agent.go:131-137"
      to: "internal/agent/prompt.PromptBuilder.Build"
      via: "swap inline llm.Request{} for Build(history, registry, provider, cfg)"
      pattern: "prompt\\.(NewPromptBuilder|.*Build)"
    - from: "internal/agent/prompt/hash.go"
      to: "internal/canonicaljson.Marshal"
      via: "deterministic serialization before sha256"
      pattern: "canonicaljson\\.Marshal"
---

<objective>
Extract a named `PromptBuilder` chokepoint (D-01) into a new `internal/agent/prompt` subpackage, build the index-set invariant hash (D-06a), and add the dormant provider-aware `cache_control` seam (D-03/D-03a). Swap the inline `llm.Request{}` construction in `LlmAgent.Run` for `PromptBuilder.Build(...)` while PRESERVING the existing byte-identity of `messages[0]` — the invariant already holds by construction; extraction gives the single hook the CI gate (06-05) and the future `messages[1]`/`messages[2]` tiers attach to.

Purpose: A single assembly chokepoint is where the runtime-faithful gate reads, where Slice 10 (Agent.md → messages[1]) and Slice 11e (AgentInsight → messages[2]) plug in, and where the provider branch lives. The hash accepts an index set so amendment #11 extends it to `{0,1,2}` with no rewrite.
Output: `internal/agent/prompt/` (builder.go + hash.go + cache_anthropic.go + tests), `llm_agent.go` swapped to the builder, `client.go` Request extended with `ToolsCacheControl` + corrected comment.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-kv-cache-builder/06-CONTEXT.md
@.planning/phases/06-kv-cache-builder/06-RESEARCH.md
@.planning/phases/06-kv-cache-builder/06-PATTERNS.md
@.planning/phases/06-kv-cache-builder/06-VALIDATION.md
@internal/agent/llm_agent.go
@internal/agent/prompt.go
@internal/agent/tools/manifest.go
@internal/llm/client.go
@internal/canonicaljson/canonicaljson.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PrefixHash index-set fingerprint (hash.go) + tests</name>
  <read_first>
    - 06-PATTERNS.md §"internal/agent/prompt/hash.go" — the analog `canonicalArgs` at `internal/agent/llm_agent.go:318-328` (the canonicaljson→fingerprint idiom) + the D-06a index-set requirement.
    - internal/canonicaljson/canonicaljson.go:5-7 (package doc) + :32 (`func Marshal`) — the deterministic serializer to reuse (sorted keys, json.Number, strict-reject). Do NOT hand-roll json.Marshal + key-sort.
    - internal/llm/client.go:7-22 (the `Message` struct shape PrefixHash serializes).
    - 06-RESEARCH.md §"Pattern 4" index-set hash code block + §"Don't Hand-Roll" row 1.
    - golang-testing skill: table-driven + deterministic-fixture discipline.
  </read_first>
  <behavior>
    - PrefixHash(msgs, {0}) over the same messages[0] returns identical hex on repeated calls (deterministic).
    - PrefixHash(msgs, {0}) is unchanged when later messages (history growth) are appended — only index 0 is hashed.
    - PrefixHash(msgs, {0,1,2}) when only index 0 exists hashes only [0] (skips absent indices >= len(msgs)) and equals PrefixHash(msgs, {0}).
    - Two messages with the SAME logical content but different key order serialize identically (canonicaljson sorted keys) → same hash.
    - A change to messages[0].Content changes the hex (sensitivity).
  </behavior>
  <action>
    Create `internal/agent/prompt/hash.go` with `func PrefixHash(msgs []llm.Message, indices []int) (string, error)`. Iterate `indices`, skip any `i >= len(msgs)` (so `{0,1,2}` is forward-compatible while only `[0]` is present today — D-06a), `canonicaljson.Marshal(msgs[i])` each present index, feed one `sha256.New()`, return `hex.EncodeToString(h.Sum(nil))`. Reuse `internal/canonicaljson.Marshal` (do not hand-roll). Wrap marshal errors with `%w` (golang-error-handling). Package doc comment: this is a content fingerprint, NOT a security primitive (ASVS V6 note). Write `hash_test.go` covering the five behaviors above (table-driven). NO god class; file stays well under 600 LOC.
  </action>
  <verify>
    <automated>go test ./internal/agent/prompt/ -run TestPrefixHash && go vet ./internal/agent/prompt/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agent/prompt/ -run TestPrefixHash` passes.
    - `hash.go` imports `internal/canonicaljson` (verifiable: `grep canonicaljson internal/agent/prompt/hash.go`), not a hand-rolled `json.Marshal` + sort.
    - PrefixHash skips indices `>= len(msgs)` (test asserts `{0,1,2}` == `{0}` when only [0] present).
    - File < 600 LOC.
  </acceptance_criteria>
  <done>PrefixHash is a deterministic, index-set, canonicaljson-backed fingerprint with table-driven tests proving stability + forward-compat + sensitivity (D-06a).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PromptBuilder.Build (builder.go) + cache_anthropic.go seam + llm.Request.ToolsCacheControl + tests</name>
  <read_first>
    - internal/agent/llm_agent.go:131-137 — the EXACT inline `llm.Request{}` the builder extracts (Model/Messages/Tools/Temperature/MaxTokens); reproduce byte-for-byte (D-01).
    - internal/agent/llm_agent.go:64-91 — `NewLlmAgent` prepends the byte-stable system message ONCE (history[0] = systemMessage()); the builder must NOT re-prepend or re-template it.
    - internal/agent/prompt.go:14-25 — `SystemPrompt` const + `systemMessage()` accessor (no clock/id).
    - internal/agent/tools/manifest.go:39,51 — `Render()`/`RenderToolDefs()` alphabetical sort (cache-load-bearing); the builder calls RenderToolDefs() as-is, does NOT re-sort.
    - internal/agent/tracing.go:129-138 — `setSpanAttrs` reads `provider` off the same llm.Config (analog for the provider-keyed branch); `a.cfg.Provider` is already on the struct (llm_agent.go:41 field; read at tracing.go:132).
    - internal/llm/config.go:19 — `defaultProvider = "openrouter"` (day-1 default; never hardcode).
    - internal/llm/client.go:88-94 — the `Request` struct + its "the wire layer is unaware" comment that D-03a rewrites; client.go:1-5 package doc ("KV-cache discipline lives in the prompt builder").
    - 06-RESEARCH.md §"Pattern 1" + §"Pattern 2" (Anthropic wire shape CITED: cache_control on system block + LAST tool def only, NEVER history) + §"Anti-Patterns" (no cache_control on history).
    - 06-PATTERNS.md §"internal/agent/prompt/builder.go" + §"internal/agent/prompt/cache_anthropic.go" + §"internal/llm/client.go".
    - claude-api skill (.claude/skills/claude-api/) — the `cache_control: {"type":"ephemeral"}` wire shape for the dormant seam.
  </read_first>
  <behavior>
    - Build(history, registry, "openrouter", cfg) returns a Request equal to today's inline construction (same Model/Messages/Tools/Temperature/MaxTokens), and carries NO cache_control (ToolsCacheControl == "").
    - Build called over a growing history keeps messages[0] byte-identical (PrefixHash({0}) equal across calls).
    - Build does NOT re-prepend or mutate history[0] (the system message is supplied already-prepended by the caller).
    - Build(history, registry, "anthropic", cfg) sets ToolsCacheControl to the ephemeral marker (dormant; serialized but ignored by the OpenAI-compat wire client) and does NOT add cache_control to any history message.
    - injectCacheControl(req, "openrouter") is a pure no-op (req unchanged); only "anthropic" mutates.
  </behavior>
  <action>
    (a) Add `ToolsCacheControl string` as a sibling field to `internal/llm/client.go` `Request` (D-03). In the SAME edit, rewrite the struct's "the wire layer is unaware" comment — it is now false; state the field is wire-shape but the injection *decision* lives in the PromptBuilder/provider layer; keep the client.go:1-5 package doc consistent (DEEP REFACTOR ON TOUCH, D-03a). Do not change the `Stream` wire path semantics.
    (b) Create `internal/agent/prompt/builder.go`: `type PromptBuilder struct{}` + `func NewPromptBuilder() *PromptBuilder` + `func (b *PromptBuilder) Build(history []llm.Message, reg *tools.Registry, provider string, cfg llm.Config) llm.Request`. Reproduce the inline construction exactly (Messages = history; Tools = reg.RenderToolDefs(); Model/Temperature/MaxTokens from cfg). Call `injectCacheControl(&req, provider)` last. Read provider config-driven (never a literal; Seam D00 / amendment #30).
    (c) Create `internal/agent/prompt/cache_anthropic.go`: `func injectCacheControl(req *llm.Request, provider string)` — pure no-op unless `provider == "anthropic"`, in which case set `req.ToolsCacheControl` to the ephemeral marker. NEVER attach cache_control to history messages. Document that the actual Anthropic-native block-array wire translation is Slice 13's job; this is the dormant branch only.
    (d) Swap `internal/agent/llm_agent.go:131-137`: replace the inline `llm.Request{...}` with `prompt.NewPromptBuilder().Build(a.history, a.registry, a.cfg.Provider, a.cfg)` (or hold the builder on the LlmAgent struct if cleaner). MUST preserve byte-identity of the emitted request (D-01). `internal/agent` already imports both `tools` and `llm` (llm_agent.go:21-23) — no cycle; importing the new `internal/agent/prompt` subpackage is also cycle-free.
    (e) Extend `builder_test.go` covering the five behaviors: byte-identity over N turns, monotonic history growth, no in-place mutation of [0], anthropic-sets-marker, openrouter-no-cache_control. Reuse `agenttest`-style messages where helpful but keep deterministic content (no clock/UUID).
    Watch the 600-LOC ceiling — split if builder.go + seam approach it (it will not; ~120+60 LOC).
  </action>
  <verify>
    <automated>go test -race ./internal/agent/prompt/ ./internal/agent/ && go vet ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/agent/prompt/ -run TestBuildPrefixStable` passes (SC#1u: byte-identical [0] over N turns + monotonic growth + no-mutation).
    - `go test ./internal/agent/prompt/ -run TestCacheControlSeam` passes (SC#3: anthropic build sets the marker, openrouter build carries none, NEVER on history).
    - `grep ToolsCacheControl internal/llm/client.go` matches; the "wire layer is unaware" comment no longer reads as an absolute (D-03a — verifiable by reading the diff).
    - `llm_agent.go` calls `prompt.…Build` (verifiable: `grep -n "prompt\\." internal/agent/llm_agent.go`); the inline `llm.Request{` at the old call site is gone.
    - `go build ./...` succeeds (no import cycle — confirms D-01a's relocation works).
    - All new/edited files < 600 LOC.
  </acceptance_criteria>
  <done>The single PromptBuilder chokepoint produces the byte-identical messages[0] today, the anthropic cache_control seam is present and dormant under OpenRouter, llm.Request carries ToolsCacheControl with a corrected comment, and LlmAgent.Run routes through Build — invariant preserved (D-01/D-03/D-03a).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PromptBuilder → wire Request | The assembled prompt prefix (`messages[0]`) is the cache breakpoint; any drift here is the poisoning this phase prevents. |
| provider string → cache_control injection | A misrouted provider branch could attach `cache_control` to history (the breakpoint-shifting bug). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | PromptBuilder.Build messages[0] assembly | mitigate | Build reproduces the byte-stable construction exactly (D-01); builder_test asserts byte-identity over N turns + no-mutation; the runtime gate in 06-05 enforces it on the real loop. |
| T-06-03 | Information Disclosure | cache_anthropic.go seam / ToolsCacheControl | mitigate | The seam carries only a literal `{"type":"ephemeral"}` marker, never message content or keys; injectCacheControl is a no-op under OpenRouter; SC#3 test asserts OpenRouter requests carry no cache_control. `llm.Config` never logged (D-28). |
| T-06-02 | Tampering | — | n/a | No SQL/input parsing in this plan (06-04). |
| T-06-04 | Elevation | — | n/a | No migration/role in this plan (06-03). |

No high-severity unmitigated threats. The prefix-poisoning surface (T-06-01) is the core of this plan and is unit-asserted here, runtime-asserted in 06-05.
</threat_model>

<verification>
- `go test -race ./internal/agent/prompt/ ./internal/agent/` green (byte-identity + cache_control seam + PrefixHash).
- `go build ./...` succeeds with `internal/agent/prompt` importing `tools` + `llm` (no cycle — proves D-01a).
- `golangci-lint run ./internal/agent/...` 0 issues; all touched files < 600 LOC.
- Diff of `client.go` shows the corrected comment (D-03a).
</verification>

<success_criteria>
A single `PromptBuilder.Build` chokepoint reproduces today's byte-identical `messages[0]` (D-01), exposes the index-set `PrefixHash` (D-06a), and carries the dormant provider-aware `cache_control` seam (D-03) with `llm.Request.ToolsCacheControl` + corrected comment (D-03a). `LlmAgent.Run` routes every call through `Build`. Unit tests cover SC#1u + SC#3.
</success_criteria>

<output>
Create `.planning/phases/06-kv-cache-builder/06-02-SUMMARY.md` when done.
</output>
