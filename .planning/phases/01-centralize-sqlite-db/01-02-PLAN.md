---
phase: 01-centralize-sqlite-db
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/auth/store.go
  - internal/auth/store_test.go
  - internal/settings/store.go
  - internal/settings/store_test.go
autonomous: true
requirements:
  - FIX-02

must_haves:
  truths:
    - "auth.NewStoreWithDB(db) constructs a valid auth.Store without calling sql.Open"
    - "settings.NewStoreWithDB(db) constructs a valid settings.Store without calling sql.Open"
    - "auth.Store has no Close() method and no owned field"
    - "settings.Store has no Close() method and no owned field"
  artifacts:
    - path: "internal/auth/store.go"
      provides: "Auth store constructor — only NewStoreWithDB; no OpenStore, Close, or owned"
    - path: "internal/settings/store.go"
      provides: "Settings store constructor — only NewStoreWithDB; no OpenStore, Close, or owned"
  key_links:
    - from: "internal/auth/store.go"
      to: "database/sql"
      via: "import"
      pattern: '"database/sql"'
      not: '_ "modernc.org/sqlite"'
    - from: "internal/settings/store.go"
      to: "database/sql"
      via: "import"
      pattern: '"database/sql"'
      not: '_ "modernc.org/sqlite"'
---

<objective>
Refactor the auth and settings stores to remove `OpenStore(path)`, the `Close()` method, the `owned bool` field, and the blank `modernc.org/sqlite` driver import. Both stores retain only `NewStoreWithDB(db *sql.DB)` as their constructor. Update all test functions in their test files to use `db.Open(tmpPath)` + `NewStoreWithDB(db)` instead of `OpenStore(path)`.
</objective>

<context>
@internal/auth/store.go — current state (OpenStore at line 74, NewStoreWithDB at line 93, Close at line 102, owned at line 69)
@internal/settings/store.go — current state (OpenStore at line 53, NewStoreWithDB at line 76, Close at line 85, owned at line 48)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor auth store (store.go + store_test.go)</name>
  <files>internal/auth/store.go, internal/auth/store_test.go</files>
  <read_first>@internal/auth/store.go — read entire file to understand existing structure before editing</read_first>
  <action>
**internal/auth/store.go changes:**

1. Remove the blank driver import: delete line 28 (`_ "modernc.org/sqlite"`).

2. In the `Store` struct (lines 66-70), remove the `owned bool` field. Keep `db *sql.DB` and `now func() time.Time`. The struct becomes:
```go
type Store struct {
    db  *sql.DB
    now func() time.Time
}
```

3. Delete the entire `OpenStore` function (lines 72-89). This removes one of the 6 sql.Open calls.

4. In `NewStoreWithDB` (lines 93-99), remove `owned: false` from the struct literal. The new body:
```go
func NewStoreWithDB(db *sql.DB) (*Store, error) {
    s := &Store{db: db, now: time.Now}
    if err := s.migrate(); err != nil {
        return nil, err
    }
    return s, nil
}
```

5. Delete the entire `Close` method (lines 102-107).

6. Update the package doc comment (lines 1-13): remove mentions of `OpenStore` owning lifecycle. Change to describe that the store receives a shared `*sql.DB` and never closes it.

**internal/auth/store_test.go changes:**

For EVERY test function in the file that calls `OpenStore(...)`:
- Replace `s, err := OpenStore(filepath.Join(t.TempDir(), "test.db"))` with:
```go
db, err := db.Open(filepath.Join(t.TempDir(), "test.db"))
if err != nil {
    t.Fatalf("db.Open: %v", err)
}
defer db.Close()
s, err := NewStoreWithDB(db)
```
- Remove all `defer s.Close()` calls (the `defer db.Close()` handles cleanup)
- Add `import "github.com/aura/aura/internal/db"` to the import block
- Remove `_ "modernc.org/sqlite"` blank import if present

After all edits, verify: `go test ./internal/auth/ -count=1` passes.
  </action>
  <verify>
    <automated>go test ./internal/auth/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func OpenStore" internal/auth/store.go` returns 0
    - `grep -c "func (s \*Store) Close()" internal/auth/store.go` returns 0
    - `grep -c "owned" internal/auth/store.go` returns 0
    - `grep -c '_ "modernc.org/sqlite"' internal/auth/store.go` returns 0
    - `grep -c "func NewStoreWithDB" internal/auth/store.go` returns 1
    - `grep -c "OpenStore" internal/auth/store_test.go` returns 0
    - `grep -c "defer s.Close()" internal/auth/store_test.go` returns 0
    - `grep -c '"github.com/aura/aura/internal/db"' internal/auth/store_test.go` is at least 1
    - `go test ./internal/auth/ -count=1` exits 0
  </acceptance_criteria>
  <done>Auth store uses only NewStoreWithDB constructor injection; all auth tests pass with db.Open</done>
</task>

<task type="auto">
  <name>Task 2: Refactor settings store (store.go + store_test.go)</name>
  <files>internal/settings/store.go, internal/settings/store_test.go</files>
  <read_first>@internal/settings/store.go — read entire file to understand existing structure before editing</read_first>
  <action>
**internal/settings/store.go changes:**

1. Remove the blank driver import: delete line 29 (`_ "modernc.org/sqlite"`).

2. In the `Store` struct (lines 45-49), remove the `owned bool` field. The struct becomes:
```go
type Store struct {
    db  *sql.DB
    now func() time.Time
}
```

3. Delete the entire `OpenStore` function (lines 51-71). This removes one of the 6 sql.Open calls.

4. In `NewStoreWithDB` (lines 76-82), remove `owned: false` from the struct literal. The new body:
```go
func NewStoreWithDB(db *sql.DB) (*Store, error) {
    s := &Store{db: db, now: time.Now}
    if err := s.migrate(); err != nil {
        return nil, err
    }
    return s, nil
}
```

5. Delete the entire `Close` method (lines 85-90). Note: the existing Close handles `s == nil` gracefully — after removal, callers that relied on `defer settingsStore.Close()` (like main.go) must use `defer pool.Close()` instead. This wiring change happens in Plan 05.

6. Update the package doc comment (lines 1-17): remove references to per-store lifecycle ownership.

**internal/settings/store_test.go changes:**

For EVERY test function in the file that calls `OpenStore(...)`:
- Replace `s, err := OpenStore(filepath.Join(t.TempDir(), "settings.db"))` with:
```go
db, err := db.Open(filepath.Join(t.TempDir(), "settings.db"))
if err != nil {
    t.Fatalf("db.Open: %v", err)
}
defer db.Close()
s, err := NewStoreWithDB(db)
```
- Remove all `defer s.Close()` calls
- Add `import "github.com/aura/aura/internal/db"` to the import block
- Remove `_ "modernc.org/sqlite"` blank import if present

After all edits, verify: `go test ./internal/settings/ -count=1` passes.
  </action>
  <verify>
    <automated>go test ./internal/settings/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func OpenStore" internal/settings/store.go` returns 0
    - `grep -c "func (s \*Store) Close()" internal/settings/store.go` returns 0
    - `grep -c "owned" internal/settings/store.go` returns 0
    - `grep -c '_ "modernc.org/sqlite"' internal/settings/store.go` returns 0
    - `grep -c "func NewStoreWithDB" internal/settings/store.go` returns 1
    - `grep -c "OpenStore" internal/settings/store_test.go` returns 0
    - `grep -c "defer s.Close()" internal/settings/store_test.go` returns 0
    - `grep -c '"github.com/aura/aura/internal/db"' internal/settings/store_test.go` is at least 1
    - `go test ./internal/settings/ -count=1` exits 0
  </acceptance_criteria>
  <done>Settings store uses only NewStoreWithDB constructor injection; all settings tests pass with db.Open</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan removes code only; introduces no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Denial of Service | Shared pool double-close | mitigate (this plan) | Remove Close() from auth.Store (line 102-107) and settings.Store (line 85-90). The `owned` guard in auth/settings was already correct for non-owned stores, but removing Close entirely eliminates the possibility of a future code path accidentally closing the shared pool. |
</threat_model>

<verification>
After both tasks: `go test ./internal/auth/... ./internal/settings/... -count=1` passes
After both tasks: `go vet ./internal/auth/ ./internal/settings/` — no warnings
</verification>

<success_criteria>
1. Auth store: no OpenStore, no Close, no owned field, no blank driver import; NewStoreWithDB is the only constructor
2. Settings store: same cleanup as auth
3. All existing auth and settings tests pass after migration to db.Open + NewStoreWithDB pattern
</success_criteria>

<output>
After completion, create `.planning/phases/01-centralize-sqlite-db/01-02-SUMMARY.md`
</output>
