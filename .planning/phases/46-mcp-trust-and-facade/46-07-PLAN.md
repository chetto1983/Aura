---
phase: 46-mcp-trust-and-facade
plan: 07
type: execute
wave: 4
depends_on: ["46-06"]
files_modified:
  - .planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md
autonomous: true
requirements: [MCP-04, MCP-05, MCP-02]

must_haves:
  truths:
    - "In one driven conversation on the running stack, the rendered tool manifest contains the curated calendar tool and contains none of the raw calendar__* tool names."
    - "In that same conversation a calendar read action completed with no approval step, and a calendar destructive action produced the approval gate — both quoted from aura.tool_invocations rows, not inferred from test output."
    - "The dispatched arguments of a live calendar detail call, read from aura.tool_invocations.args_raw, contain no accountId key."
    - "The detail call's reference value is byte-identical to the value the listing result issued — Aura passed it back unchanged, with no re-casing, trimming, normalization or re-encoding."
    - "The curated calendar tool is present in the manifest as an always-loaded tool, not reached through tool_search."
    - "The scenario is scored and the score is >= 9.8, with the scoring rationale written down; a green unit suite is explicitly not accepted as the closing evidence."
    - "46-VALIDATION.md's Per-Task Verification Map has a row for every task in plans 46-04 through 46-06 with its automated command and status."
    - statement: "A detail call built from a listing that returned zero events has no reference to pass; the fork rejects it as a missing required argument rather than resolving it against a default account. If the live account has events, this boundary is exercised against a deliberately empty filter; if it cannot be reached live, the schema-level assertion from 46-06 stands in and the gap is stated."
      verification: backstop
    - statement: "The opaque reference is treated as bytes end to end — no length, equality or normalization semantics are imposed by Aura on a value it did not mint."
      verification: backstop
  artifacts:
    - path: ".planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md"
      provides: "The tracer's live evidence, quoted rows, and the score"
      contains: "tool_invocations"
  key_links:
    - from: "the driven conversation"
      to: "aura.tool_invocations"
      via: "quoted rows as the evidence surface, never test output"
      pattern: "args_raw"
  prohibitions:
    - "Must NOT reintroduce Success Criterion 5 or design a scenario that tries to prove instruction-shaped MCP result text is not acted on — the operator struck that criterion and there is no envelope to carry it."
    - "Must NOT accept a green test suite, a curl issued with hand-written headers, or a smoke check as the closing evidence; the agent has to be driven and the operator-visible outcome read from persisted rows."
---

<objective>
**The tracer gate.** Prove the calendar slice live, end to end, on the running stack — before any
expansion work begins.

Purpose: this phase's Definition of Done is a real E2E scenario driven through the running stack and
scored >9.8 (CLAUDE.md). D-37 makes it concrete: one driven conversation that reads a calendar, sends
something that trips the approval gate, and follows an event from listing through to detail, with the
`aura.tool_invocations` rows quoted. Everything after this plan (the WhatsApp fork, its wiring, SC#6,
the fixture repair) is expansion on a pattern this plan either validates or falsifies.

Output: the live evidence for SC#1, SC#2 and SC#4 (calendar half), written into 46-VALIDATION.md.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md
@.planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md
@.planning/phases/46-mcp-trust-and-facade/46-06-SUMMARY.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| Accepting the tracer as validated and proceeding to expansion | reversible — a falsified tracer means fixing 46-06, not re-planning the phase | The gate is here precisely so a wrong pattern is not repeated twice. |
| The scenario's shape (read + destructive + list->detail in one conversation) | reversible | D-37 fixed it; a wider scenario is allowed, a narrower one is not. |
</reversibility>

## Artifacts this phase produces

Created by THIS plan: the filled Per-Task Verification Map rows and the live-evidence section in
`46-VALIDATION.md`. No code symbols.

<tasks>

<task type="auto">
  <name>Task 1: Drive one real conversation and capture the evidence rows</name>
  <read_first>
    - `.planning/phases/46-mcp-trust-and-facade/46-06-SUMMARY.md` — the pinned image tag, the curated tool's model-facing name, and its action enum.
    - `.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md` D-37 (the scenario and the evidence discipline) and D-04 (the surviving guardrails that must be seen working, since they are now the only ones).
    - `internal/db/migrations/0011_tool_invocations.up.sql` — the `aura.tool_invocations` columns available as evidence: `tool_name`, `args_raw`, `status`, `meta`.
    - `compose.yaml` — the services needed for a full-stack bring-up, including the `aura-pim-mcp` service now pinned by `:<sha>`.
    - `internal/agent/mcptools/bridge_deferral.go` — the INFO log emitted when a slot is granted; that log line is the manifest-composition evidence for SC#1 alongside the rendered manifest.
    - The user-memory recipe for driving the cockpit end to end: `/api/conversations` and `/agent/run` both require an `Idempotency-Key` header (they return 400 without one), and SSE tokens can split mid-word.
  </read_first>
  <action>
Bring the stack up with the new pin. **Verify image freshness first** — confirm the running
`aura-pim-mcp` container was started from the `:<sha>` tag now in `compose.yaml` (`docker inspect`
the image ID against the pulled tag), and confirm the `aura` image itself was built from current
HEAD. An E2E against a stale image proves nothing about the commit under test.

Drive the REAL agent through one conversation (not a smoke test, not a hand-crafted `curl` against
an endpoint you chose — the agent has to choose the tool):

1. Ask for something that requires reading the calendar (e.g. what is on the calendar this week).
   Expect the curated tool with a read action; expect NO approval prompt.
2. Ask for something that requires sending (an email). Expect the curated tool with the destructive
   action and expect the approval gate to fire.
3. Ask to follow one event from the listing through to its detail. Expect a detail call whose
   arguments carry the reference the listing issued and no `accountId`.

Capture, and keep verbatim:
- The rendered tool manifest for a turn in that conversation (from the OTel span or the
  prompt-render log). Confirm the curated tool is present and that no `calendar__<raw name>` appears.
  Confirm it was in the manifest, not activated through `tool_search`.
- The `aura.tool_invocations` rows for all three calls:
  `SELECT tool_name, args_raw, status, meta, created_at FROM aura.tool_invocations
   WHERE created_at > <conversation start> ORDER BY created_at;`
- The listing result's reference value and the detail call's `args_raw` reference value, compared
  byte-for-byte.
- The INFO log line recording which servers hold the two always-loaded slots.

If any expectation fails, STOP and fix 46-06 rather than proceeding — the whole point of a tracer
gate is that the pattern is corrected once, before it is repeated for the second sidecar.
  </action>
  <acceptance_criteria>
    - A captured manifest render contains the curated calendar tool's name and zero occurrences of any raw `calendar__` tool name other than the curated one.
    - An `aura.tool_invocations` row exists for the read action with a completed status and no approval step in its `meta`.
    - An `aura.tool_invocations` row exists for the destructive action showing the approval gate fired (approval-pending or the equivalent recorded state).
    - Both rows name the SAME tool and differ only in the `action` value inside `args_raw` — this is the per-action-survived-the-merge proof.
    - The detail call's `args_raw` contains no `accountId` key, verified by an exact JSON key check, not a substring search.
    - The reference value in the detail call's `args_raw` is byte-identical to the value in the listing result; the comparison is shown.
    - The container image IDs for `aura` and `aura-pim-mcp` are recorded and match current HEAD and the pinned `:<sha>` tag respectively.
  </acceptance_criteria>
  <verify>
    <automated>psql "$AURA_DB_URL" -c "SELECT tool_name, status, args_raw FROM aura.tool_invocations ORDER BY created_at DESC LIMIT 10;"</automated>
    <human-check>The transcript of the driven conversation reads as a real interaction, the approval prompt appeared for the send and not for the read, and the manifest excerpt shows the curated tool.</human-check>
  </verify>
  <done>The tracer is observed working (or falsified) against the running stack, with rows to quote.</done>
</task>

<task type="auto">
  <name>Task 2: Score the scenario and fill 46-VALIDATION.md</name>
  <read_first>
    - `.planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md` in full — the empty Per-Task Verification Map (line 53), the Wave 0 Requirements checklist (lines 77-93), the Manual-Only Verifications table (103-111) and the Sign-Off checklist (116-124).
    - `.planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md` — the shape a filled validation document takes in this project, including how live evidence was quoted for the previous phase.
    - The evidence captured in task 1.
  </read_first>
  <action>
Fill `46-VALIDATION.md`:

- **Per-Task Verification Map** — one row per task in plans 46-04, 46-05 and 46-06 (plans 46-01/02/03
  are documentation and get rows stating that, with their doc-level acceptance command). Each row
  carries: task ID, plan, wave, requirement, threat ref (the `T-46-NN` id from that plan's threat
  model), the secure behaviour it proves, test type, the automated command, whether the test file
  exists, and status.
- **Wave 0 Requirements** — tick the four gaps closed so far (the `multiplexedClassifiers` unit test,
  the deferral count test including the 2-slot-cap case, the `guard_test.go` D-34 case, the
  re-keyed `bridge_risk_test.go` cases) plus the mount-time reconciliation test, and the calendar half
  of the integration gap. Leave the WhatsApp half unticked — plan 46-08 closes it.
- **A new "Live evidence — tracer (calendar)" section** quoting: the manifest excerpt, the three
  `aura.tool_invocations` rows verbatim (tool_name, action from args_raw, status), the reference
  byte-comparison, and the slot-grant log line. Quote them; do not paraphrase.
- **Score the scenario** against CLAUDE.md's Definition of Done (>9.8) and write the rationale: what
  worked, what was observed rather than assumed, and what the run does NOT prove. State explicitly
  that SC#1/SC#2/SC#4 are proven for the CALENDAR half only, and that the WhatsApp half and SC#6 are
  still owed.
- **State the gaps honestly.** If the empty-listing boundary (MCP-05's zero-events case) could not be
  reached against the live account, say so and record that the schema-level assertion from 46-06
  stands in for it. An unconfirmed backstop abstains; it never silently passes.

Do NOT set `status: validated` or `nyquist_compliant: true` in the frontmatter — those are the phase
close's to set (plan 46-09), after the whole surface is proven.
  </action>
  <acceptance_criteria>
    - `46-VALIDATION.md`'s Per-Task Verification Map has no `_filled by planner_` placeholder row and has at least one row per task in plans 46-04/05/06.
    - The document contains a section quoting at least three `aura.tool_invocations` rows verbatim, including the `args_raw` of the detail call.
    - The document states a numeric score and its rationale, and names what the run does not prove.
    - The Wave 0 checklist has five of six items ticked, with the WhatsApp integration half explicitly left open and attributed to plan 46-08.
    - Frontmatter still reads `status: draft` and `nyquist_compliant: false`.
    - No line in the document asserts anything about instruction-shaped result text or result fencing.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=.planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md; ! grep -q "_filled by planner_" "$f" && grep -q "tool_invocations" "$f" && grep -q "status: draft" "$f" && echo OK'</automated>
  </verify>
  <done>The tracer's evidence is written down, scored, and its limits are stated — expansion may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test output -> claimed truth | A green suite is not evidence that a live turn behaved; the boundary is where "it compiles" gets mistaken for "it works". |
| Live model turn -> approval ledger | The gate that stops a destructive action is only real if it fires in a turn the model drove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-26 | Repudiation | Claiming the phase works on unit-test evidence | mitigate | Evidence is read from `aura.tool_invocations` rows and the rendered manifest, quoted verbatim; test output is explicitly not accepted. The image-freshness check closes the "green against a stale container" failure mode. |
| T-46-27 | Elevation of Privilege | The approval gate not actually firing live for a destructive action | mitigate | The scenario requires an approval-gated row and a non-gated row for the SAME tool in the SAME conversation; one without the other falsifies the tracer and stops expansion. |
| T-46-28 | Spoofing | Aura substituting or mangling the fork's opaque reference | mitigate | Byte-for-byte comparison of the reference as issued and as dispatched; any normalization shows up as a diff. |
| T-46-29 | Information Disclosure | Quoting live rows into a planning document | mitigate | Quote the structural fields (`tool_name`, action, status) and redact personal content from `args_raw`/`result_preview` before it enters the repository — the evidence needed is the shape, not the message body. |
| T-46-SC | Tampering | package installs | mitigate | No package installs in this phase; nothing to gate. |
</threat_model>

<verification>
- The stack is up on the pinned images and the image IDs are recorded.
- The three `aura.tool_invocations` rows exist and are quoted.
- `46-VALIDATION.md` has no placeholder rows and still reads `status: draft`.
</verification>

<success_criteria>
The calendar half of SC#1, SC#2 and SC#4 is proven live and scored, and the pattern is safe to
repeat for the second sidecar.
</success_criteria>

<output>
Create `.planning/phases/46-mcp-trust-and-facade/46-07-SUMMARY.md` when done, with the score and the
explicit go/no-go for expansion.
</output>
