---
phase: 46-mcp-trust-and-facade
plan: 09
type: execute
wave: 6
depends_on: ["46-08"]
files_modified:
  - internal/mcp/calculator_integration_test.go
  - internal/agent/mcptools/bridge_trust_test.go
  - internal/agent/tools/testdata/deferred_manifest.json
  - internal/agent/tools/search_gate_test.go
  - docs/aura-quality-snapshot.md
  - .planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04, TOOL-14]

must_haves:
  truths:
    - "A named unit test asserts that neither frameMCPSummary nor frameMCPDescription introduces distrust framing, for a normal description, an empty description, and an over-cap description."
    - "frameMCPDescription of an empty or absent server description yields an empty description — no placeholder, no framing, no marker text."
    - "Description and summary capping is defined in BYTES with UTF-8 rune-boundary safety, not in code points or grapheme clusters: a multi-byte description truncated at the cap never produces an invalid rune and never exceeds the byte cap."
    - "A named unit test asserts newResult stamps TrustTrusted for a bridged tool from any mounted server, not only a trusted recipe."
    - "Mounting the calculator MCP server live produces usable tools with no Aura code change and no catalog entry, and its unannotated mutating tool classifies fail-closed at Mutating+Destructive."
    - "The calculator server lands deferred, not always-loaded, because the 2-slot budget is already spent by the two curated servers."
    - "internal/agent/tools/testdata/deferred_manifest.json contains zero occurrences of the stale untrusted-MCP-server framing strings."
    - "The tool-search retrieval gate's corpus, its minimum-size floor and its gate cases all reflect the post-curation deferred surface, and the measured top-1 and recall@3 figures are recorded rather than assumed."
    - "docs/aura-quality-snapshot.md has a re-attestation for every row whose CI-gate-path glob matches a file this phase changed, dated today, with the prior notes preserved."
    - "bash scripts/coverage_docker.sh passes the owned-surface 85% floor with every branch this phase added covered by daemon-free unit tests."
    - "One real driven conversation exercises the curated WhatsApp tool with a read action AND send_message, and two aura.tool_invocations rows name the SAME model-facing tool with different action values landing on different tiers — the WhatsApp half of the per-action proof 46-07 produced for calendar."
    - "46-VALIDATION.md's Wave 0 checklist is fully ticked, its live evidence covers both sidecars plus SC#6, and its frontmatter is set to status: validated."
  artifacts:
    - path: "internal/mcp/calculator_integration_test.go"
      provides: "SC#6's proof: an unlisted server mounts with no code change and classifies fail-closed"
      contains: "TestCalculatorServerLive"
    - path: "internal/agent/tools/testdata/deferred_manifest.json"
      provides: "The post-curation deferred corpus the retrieval gate scores against"
    - path: "docs/aura-quality-snapshot.md"
      provides: "The phase-close quality re-attestation the push gate requires"
  key_links:
    - from: "internal/agent/tools/search_gate_test.go"
      to: "internal/agent/tools/testdata/deferred_manifest.json"
      via: "the gate replays real dumped specs; a stale corpus makes the gate score tools that no longer exist"
      pattern: "deferred_manifest.json"
  prohibitions:
    - "Must NOT reintroduce Success Criterion 5 in the validation document, in a test name, or as a re-interpretation of the trust tripwire — the tripwire proves descriptions and results are plain, never that instruction-shaped text was resisted."
    - "Must NOT lower a retrieval-gate floor to make the gate green; a floor that moves because the corpus shrank is a finding to record and explain, not a number to edit."
---

<objective>
Close the phase: prove SC#6 with a genuinely unlisted server, pin the trust posture with named
tripwire tests, repair the tool-search fixture the curation invalidated, and run the phase-close
gates.

Purpose: MCP-01 and MCP-03 shipped in `34b892512` without a test named for what they assert — the
only current coverage is an incidental `strings.Contains(..., "untrusted")` check inside a truncation
test. A ratified posture with no named tripwire is one refactor away from silently regressing. And
the curated surface removes 28 tool names from the deferred corpus, which makes the retrieval gate's
static fixture describe a system that no longer exists.

Output: SC#6's live proof, two named tripwire tests, a regenerated retrieval-gate corpus with
re-measured figures, and the phase-close gates green.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md
@.planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md
@.planning/phases/46-mcp-trust-and-facade/46-07-SUMMARY.md
@.planning/phases/46-mcp-trust-and-facade/46-08-SUMMARY.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| Regenerating the retrieval-gate corpus and dropping its now-impossible gate cases | costly — the gate's historical figures stop being comparable | Recorded with the before/after corpus size and both measured figures, so the discontinuity is explained rather than discovered later. |
| Setting `status: validated` on the phase | reversible | Only after every gate is green and the live evidence covers both sidecars. |
</reversibility>

## Artifacts this phase produces

Created by THIS plan (new symbols — not drift): test names
`TestMCPFramingCarriesNoDistrustMarkers`, `TestMCPFramingEmptyDescriptionStaysEmpty`,
`TestBridgedResultIsTrustedForAnyMountedServer`, and the extended
`TestCalculatorServerLive` risk-tier assertion; the regenerated
`internal/agent/tools/testdata/deferred_manifest.json`; the re-measured `gateTop1Floor` /
`gateRecall3Floor` attestation.

## Edge-coverage ledger for Phase 46 (18 probes, 18 authored, 0 flagged)

| Probe | Authored in | Verification |
|---|---|---|
| MCP-01:empty | 46-09 truths | explicit |
| MCP-01:encoding | 46-09 truths | explicit |
| MCP-02:idempotency | 46-06 truths | explicit |
| MCP-02:concurrency | 46-06 truths | explicit |
| MCP-03:adjacency | 46-06 truths | explicit |
| MCP-03:empty | 46-04 truths | explicit |
| MCP-03:ordering | 46-04 truths | explicit |
| MCP-04:adjacency | 46-08 truths | explicit |
| MCP-04:empty | 46-06 truths | explicit |
| MCP-04:ordering | 46-06 truths | explicit |
| MCP-05:boundary | 46-07 truths | backstop |
| MCP-05:empty | 46-05 truths | explicit |
| MCP-05:encoding | 46-07 truths | backstop |
| MCP-05:precision | 46-05 truths | backstop |
| TOOL-14:boundary | 46-04 truths | explicit |
| TOOL-14:empty | 46-04 truths | explicit |
| TOOL-14:encoding | 46-04 truths | explicit |
| TOOL-14:precision | 46-04 truths | backstop |

14 explicit, 4 backstop, 0 unresolved, 0 flagged assumptions. An unconfirmed backstop abstains to
`human_needed`; it never silently passes.

<tasks>

<task type="auto">
  <name>Task 1: Drive one real WhatsApp conversation and capture the per-action evidence rows</name>
  <files>.planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/46-mcp-trust-and-facade/46-07-PLAN.md` Task 1 IN FULL — the calendar drive this task mirrors for WhatsApp: how the conversation is driven, which rows are captured, and its acceptance criteria (same tool name, different `action` in `args_raw`, different tier). Do not invent a second methodology; copy this one.
    - `.planning/phases/46-mcp-trust-and-facade/46-07-SUMMARY.md` — how the calendar drive actually went, including anything about the approval gate's recorded state worth pre-empting here.
    - `.planning/phases/46-mcp-trust-and-facade/46-08-SUMMARY.md` — the WhatsApp curated tool's model-facing name, its 12-action enum, the exempted tool names, and the `sha-<7hex>` pin the stack must be running.
    - `internal/gateway/decide.go` — which tier actually stops the turn (only Destructive), so the expected operator-visible outcome is stated before the drive rather than rationalised after it.
    - `.planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md` — the Wave 0 checklist and the live-evidence section this task fills; the calendar half is already there from 46-07.
  </read_first>
  <action>
Drive the REAL agent on the running stack, through the cockpit, in ONE conversation. Not a curl with
hand-written headers, not a tagged test, not a smoke check — CLAUDE.md's DEFINITION OF DONE and
46-07's own prohibition both rule those out as closing evidence, and 46-08 proves WhatsApp's
per-action classification only in unit tests, which is exactly the gap this task closes.

The conversation must exercise, through the CURATED tool, in this order:
1. A read action (e.g. `get_chat` or `get_last_interaction`) — expected to complete with no approval
   step, because `classifyWhatsAppAction` maps a read to `scoring.Safe`.
2. `send_message` — expected to trip the approval gate, because the SAME tool with a different
   `action` value classifies `scoring.Destructive`, and Destructive is the only tier `decide.go`
   stops the turn for. Send to a chat the operator controls; do not message a third party.
Additionally, exercise one EXEMPTED tool (`list_chats` or `list_messages`) in the same conversation,
so the evidence shows all three model-facing WhatsApp tools reachable by the model, not just the
curated one.

Capture from `aura.tool_invocations` the rows for the read and for `send_message`, and show that both
name the SAME model-facing tool and differ only in the `action` value inside `args_raw`. That pair IS
the per-action-survived-the-merge proof for WhatsApp; a green unit test is not.

Record the evidence in `46-VALIDATION.md` beside 46-07's calendar drive, under its own heading, with
the same scoring treatment. Record the `aura` and `aura-whatsapp` container image IDs so a reader
knows which bytes produced the rows.

If the read action lands anything other than a completed no-approval row, or `send_message` lands
anything other than the approval gate firing, STOP and report — that is a classification regression in
the merged tool, not a scenario to re-word.
  </action>
  <acceptance_criteria>
    - An `aura.tool_invocations` row exists for the read action with a completed status and no approval step in its `meta`.
    - An `aura.tool_invocations` row exists for `send_message` showing the approval gate fired (approval-pending or the equivalent recorded state).
    - Both rows name the SAME model-facing tool and differ only in the `action` value inside `args_raw` — quoted in `46-VALIDATION.md`, not merely asserted.
    - A third row (or a captured manifest render) shows an exempted tool — `list_chats` or `list_messages` — invoked under its own name, proving all three WhatsApp model-facing tools are reachable. **Under `views-drop` there are no exempted tools:** assert instead that the curated tool is the sole WhatsApp model-facing name in the manifest render.
    - The scenario was driven through the agent, and the SUMMARY says by what means; a curl, a tagged test or a smoke check is not accepted as the closing evidence.
    - The container image IDs for `aura` and `aura-whatsapp` are recorded and match current HEAD and the pinned `sha-<7hex>` tag respectively.
  </acceptance_criteria>
  <verify>
    <automated>psql "$AURA_DB_URL" -Atc "SELECT tool_name, args_raw FROM aura.tool_invocations WHERE tool_name LIKE 'whatsapp%' ORDER BY created_at DESC LIMIT 10" | tee /dev/stderr | grep -q send_message</automated>
    <human-check>The captured rows read as one real driven conversation, and the read/destructive pair names the same tool with two different actions on two different tiers.</human-check>
  </verify>
  <done>WhatsApp's per-action classification is proven on the running stack by persisted rows, the way the calendar half was in 46-07 — so 46-09's "both sidecars" sign-off is backed by evidence this phase actually produced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#6 with the calculator server, and named tripwires for the trust posture</name>
  <read_first>
    - `internal/mcp/calculator_integration_test.go` in full — the `//go:build calculator_integration` tag, `calculatorConfigOrGate` (line 23, with its `t.Fatal` under `$CI` and its `AURA_MCP_CALCULATOR_SERVER_JSON` source), `TestCalculatorServerLive` (39-68) and `hasCalculatorTool` (70).
    - `internal/agent/mcptools/bridge_risk.go:98-149` — `mcpToolRisk` and `classifyToolRisk`, specifically the nil-`Annotations` fail-closed `return true, true` branch; and `bridge_risk_test.go:172` `TestMCPToolRisk_NilAnnotationsFailsClosed`, the unit-level proof this task promotes to a live one.
    - `internal/agent/mcptools/bridge.go:309-346` — `frameMCPSummary` and `frameMCPDescription`, `maxMCPSummaryBytes`/`maxMCPDescriptionBytes`, and `truncateUTF8Bytes`.
    - `internal/agent/mcptools/bridge_edges_test.go:290-330` — `TestFrameMCPSummary_HugeRequiredHintTruncatesHint`, whose incidental `strings.Contains(strings.ToLower(spec.Summary), "untrusted")` check is currently the only assertion covering MCP-01; and `:348` `TestTruncateUTF8Bytes`.
    - `internal/agent/mcptools/bridge_test.go:152-171` — `TestBridgedTool_Execute_MarksResultTrusted`, which proves `TrustTrusted` for one fixture server; MCP-03's claim is broader (unconditional across every mounted server).
    - `internal/agent/mcptools/bridge_deferral.go` (from 46-04) — the 2-slot budget, which by now is spent by the two curated servers, so the calculator must land deferred.
    - `.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md` D-38 (SC#6's proof and its caveat: the calculator IS referenced in Aura's tree via the test fixture, so the evidence must show the MOUNT needed nothing new, not that the server was unknown).
  </read_first>
  <behavior>
    - `frameMCPDescription("")` returns `""` — no placeholder, no marker, no framing.
    - `frameMCPDescription` and `frameMCPSummary` of a normal description contain no case-insensitive occurrence of `untrusted`, `do not trust`, or any distrust marker.
    - A description of exactly `maxMCPDescriptionBytes` bytes is returned uncapped; one byte more is capped, the result is <= the cap in BYTES, and `utf8.ValidString` holds on the result.
    - A description whose byte cap falls mid-rune truncates at the rune boundary below the cap, never mid-rune.
    - A bridged tool from a mount with NO recipe source (a plain, unlisted server) still returns `Provenance.Trust == tools.TrustTrusted`.
    - Live: mounting the calculator server registers its tools with no change to `BuiltInCatalog` and no new Aura code.
    - Live: an unannotated mutating calculator tool's spec reads `Mutating: true, Destructive: true`.
    - Live: the calculator's tools are `Deferred: true` — the 2-slot budget is spent.
  </behavior>
  <action>
Write the two tripwire tests first, in `internal/agent/mcptools/bridge_trust_test.go` (the file that
already owns the framing and cap tests):

- `TestMCPFramingCarriesNoDistrustMarkers` — table-driven over a normal description, an empty one, a
  whitespace-only one, and an over-cap one; asserts no distrust marker appears in either the summary
  or the description, checking a list of markers case-insensitively rather than the single literal
  `untrusted` the existing incidental check uses. Comment that this is MCP-01/MCP-03's tripwire for
  a posture ratified in prd.md by plan 46-01's amendment, and that it exists because a ratified
  posture with no named test is one refactor from silent regression.
- `TestMCPFramingEmptyDescriptionStaysEmpty` and the byte/rune-boundary cases (reuse
  `truncateUTF8Bytes`'s existing coverage rather than duplicating it; assert the CAP contract at the
  framing level: <= cap in bytes and `utf8.ValidString` true).
- `TestBridgedResultIsTrustedForAnyMountedServer` — mount a fixture server with an EMPTY
  `recipeSource` (not a trusted recipe) and assert `TrustTrusted`. This is MCP-03's unconditional
  claim, which the existing single-fixture test does not make.

Then extend `TestCalculatorServerLive` (SC#6):
- Assert the mount required no `BuiltInCatalog` entry: the server comes from
  `AURA_MCP_CALCULATOR_SERVER_JSON`, and the test states in a comment that this is D-38's caveat —
  the server IS referenced in Aura's tree via this fixture, so the evidence is that the MOUNT needed
  nothing new, not that the server was unknown.
- Assert the risk tier of an unannotated mutating tool reads `Mutating && Destructive` — the
  fail-closed default, proven end to end through a real mount rather than only at unit level.
- Assert the mounted tools are `Deferred: true`, and say why in a comment: the 2-slot budget is spent
  by the two curated servers, so a further qualifying server stays deferred and
  `tool_search`-discoverable — which still satisfies SC#6's promise (usable with no code change,
  fail-closed).

Run the tier live: `AURA_MCP_CALCULATOR_SERVER_JSON=... go test -tags calculator_integration
./internal/mcp/ -run TestCalculatorServerLive -v`, with the stack up. Record the mounted tool names
and the classified tiers in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `go test ./internal/agent/mcptools/ -run 'TestMCPFraming|TestBridgedResultIsTrusted' -v` passes with at least three named tests.
    - The distrust-marker test checks more than the single literal `untrusted` and fails if a framing prefix is reintroduced in either summary or description.
    - `go test -tags calculator_integration ./internal/mcp/ -run TestCalculatorServerLive -v` passes with runtime > 0.5s and asserts `Mutating && Destructive` on an unannotated tool and `Deferred: true`.
    - `git diff internal/mcp/manager/catalog.go` is empty — SC#6's whole claim is that no catalog entry was needed.
    - `go test -race ./internal/agent/mcptools/...` green.
    - No test added by this task references result fencing, instruction-shaped text, or Success Criterion 5.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'go test -race ./internal/agent/mcptools/ -run "TestMCPFraming|TestBridgedResultIsTrusted" -v && git diff --quiet internal/mcp/manager/catalog.go && echo OK'</automated>
  </verify>
  <done>The trust posture has named tests, and a server with no catalog entry mounts, works, and fails closed.</done>
</task>

<task type="auto">
  <name>Task 3: Repair the tool-search retrieval gate for the post-curation deferred surface</name>
  <read_first>
    - `internal/agent/tools/search_gate_test.go` in full — `gateTop1Floor = 0.90` and `gateRecall3Floor = 1.00` (lines 25-28), `loadManifestFixture` with its `if len(dumped) < 40 { t.Fatalf }` corpus-size floor (39-67), `newGateSearch` (70-79), `gateCases` (109-141) and both gate tests.
    - `internal/agent/tools/testdata/deferred_manifest.json` — 55 dumped specs today: 23 native, 14 `calendar__*`, 14 `whatsapp__*`, 4 `memory__*`; 64 occurrences of the stale `untrusted MCP server description` / `untrusted MCP server summary data` strings, which predate `34b892512`.
    - The fixture's provenance comment at `search_gate_test.go:13-15`: it is dumped from the deployed registry with `aura toolpipe --manifest-json`.
    - `.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md` §"Claude's Discretion" — the fix-on-touch note about the 64 stale occurrences.
  </read_first>
  <action>
This is the consequence the curation creates that no source document anticipated, so handle it
explicitly rather than leaving a gate that scores tools which no longer exist.

Measure first, then act. With the stack up on both new pins, re-dump the corpus:
`aura toolpipe --manifest-json` into `internal/agent/tools/testdata/deferred_manifest.json`. Report
the before/after spec counts. Expect roughly 55 -> 27: the 28 raw `calendar__*`/`whatsapp__*` specs
are gone, and the four always-loaded curated tools (calendar 1 + WhatsApp 3) do NOT appear
because they are always-loaded, not deferred.

Then:
- **The corpus-size floor.** `loadManifestFixture`'s `< 40` guard exists to catch a truncated dump,
  not to assert a specific surface size. Re-base it to a value just below the newly measured count,
  and update its message to say what it is guarding against. Do not delete the guard.
- **The four now-impossible gate cases.** `{"create a calendar event" -> calendar__create_event}`,
  `{"send an email" -> calendar__send_email}`, `{"send a whatsapp message" -> whatsapp__send_message}`
  and `{"search my whatsapp chat history" -> whatsapp__list_messages|whatsapp__get_chat}` name gold
  tools that no longer exist AND capabilities that are no longer deferred at all. Remove them, and
  add a comment recording WHY: those capabilities moved into the always-loaded manifest, so
  `tool_search` is no longer the path to them and scoring them here would assert the opposite of what
  the phase built. This is a scope reduction of the GATE, not of the capability — say so.
- **The stale framing strings.** They disappear as a side effect of the re-dump. Verify with a grep
  rather than assuming.
- **Re-measure, do not re-baseline blindly.** Run the gate and record the measured top-1 and recall@3
  over the reduced case set. If either now sits below its floor, that is a FINDING: report the
  missing queries and their rankings and explain the cause, then decide with evidence whether the
  floor or the corpus is wrong. Do not lower a floor to make the gate green.

If the stack cannot be brought up to produce a real dump, do NOT hand-edit the fixture into shape —
say so, leave the fixture and the gate untouched, and record the debt explicitly in the SUMMARY and
in 46-VALIDATION.md as an open item for the phase close.
  </action>
  <acceptance_criteria>
    - `grep -c "untrusted MCP server" internal/agent/tools/testdata/deferred_manifest.json` returns 0.
    - The fixture contains zero specs whose name starts with `calendar__` or `whatsapp__` other than any that genuinely remain deferred, and the before/after counts are reported in the SUMMARY.
    - `loadManifestFixture`'s size floor is below the newly measured count and its failure message explains what it guards.
    - `gateCases` contains no case whose gold names a tool absent from the fixture; a test or the gate itself fails if one does.
    - `go test ./internal/agent/tools/ -run TestToolSearchGate -v` passes, and its logged top-1 and recall@3 figures are quoted in the SUMMARY.
    - `gateTop1Floor` and `gateRecall3Floor` are unchanged, or their change is accompanied by the measured evidence and an explanation in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>bash -c '! grep -q "untrusted MCP server" internal/agent/tools/testdata/deferred_manifest.json && go test ./internal/agent/tools/ -run TestToolSearchGate -v'</automated>
  </verify>
  <done>The retrieval gate scores the surface that exists, and the four queries it can no longer answer are removed with their reason written down.</done>
</task>

<task type="auto">
  <name>Task 4: Phase-close gates — coverage, quality snapshot, validation sign-off, handoff</name>
  <read_first>
    - `scripts/coverage_docker.sh` and `scripts/coverage_gate.sh` — the owned-surface 85% floor, `AURA_COVERAGE_TAGS` defaulting to `db_integration` only, the disposable `aura_cov` database and the refusal to run `db_integration` against a database named `aura` outside CI.
    - `scripts/quality_snapshot_gate.sh` and `docs/aura-quality-snapshot.md` — the CI-gate-path globs, the `Last measured` column, and the re-attestation format (a fresh measurement if the metric moved, else a metric-neutral justification naming exactly what changed and why the number cannot move, keeping the prior notes as `Prior ...`).
    - `.planning/phases/46-mcp-trust-and-facade/46-VALIDATION.md` as filled by plan 46-07 — the Per-Task Verification Map, the Wave 0 checklist and the Sign-Off checklist.
    - `.planning/phases/46-mcp-trust-and-facade/46-06-SUMMARY.md` and `46-08-SUMMARY.md` — the two COMPAT blast-radius row counts to carry into the handoff.
    - `.planning/STATE.md` "Blockers/Concerns" — the still-open Nyquist per-task validation for Phase 45.1, which this phase does not close but must not silently absorb.
    - `.claude/skills/mutation-testing/SKILL.md` — for the mutation spot-check.
  </read_first>
  <action>
Run the phase-close gates, in this order, fixing rather than excusing anything red:

1. `go vet ./...`, `go build ./...`, `make quality`.
2. `bash scripts/coverage_docker.sh` — the full owned-surface floor with the stack up. Every branch
   this phase added lives in `internal/agent/mcptools` and `internal/gateway`, both daemon-free and
   both inside the `db_integration` tag set, so there is no excuse for an uncovered new branch.
   Report the measured aggregate.
3. A mutation spot-check (>= 70% killed) on this phase's critical file: `bridge_multiplex.go` (the
   `Multiplexed` gate and the per-action class lookup are where a silent under-gate would live).
   Run it in WSL with `GOFLAGS` set for the tags the file needs. Record killed/total.
4. `docs/aura-quality-snapshot.md`: for EVERY row whose CI-gate-path glob matches a file this phase
   changed, set `Last measured` to today and PREPEND a re-attestation — a fresh measurement if the
   metric moved, otherwise a metric-neutral justification naming exactly what changed and why the
   number cannot move — keeping the prior notes as `Prior ...`. Verify locally BEFORE any push, with
   the gate's own command, and require it to print `ok: ... checked N row(s)`.
5. Finish `46-VALIDATION.md`: tick every Wave 0 item (all six), add the WhatsApp and SC#6 live
   evidence sections beside the tracer's, complete the Sign-Off checklist, and set
   `status: validated`. Set `nyquist_compliant` only if `/gsd-validate-phase 46` has actually run;
   otherwise leave it `false` and say so — an unrun validation is not a passed one.

Then write the phase handoff into the SUMMARY:
- The two COMPAT blast-radius counts (`calendar__%` and `whatsapp__%` rows in
  `aura.tool_invocations`, plus any paused approval or scheduled `agent_job` referencing them), named
  as input to COMPAT-01/02/03 in Phases 47/48. This phase records the blast radius and does not
  absorb those requirements.
- The two fork SHAs and both pinned image tags (D-25).
- Which two servers hold the always-loaded slots and what the budget arithmetic decided, as the input
  Phase 48 needs for its 14-slot sizing.
- Any open item: the retrieval-gate debt if task 2 could not run live, the still-owed
  `/gsd-validate-phase 45.1`, and whether the MCP-05 empty-listing boundary was exercised.

Finally, per CLAUDE.md's merge and push discipline: merge to `master`, re-run fresh post-merge
verification, and push only once CI is green.
  </action>
  <acceptance_criteria>
    - `bash scripts/coverage_docker.sh` exits 0 and the measured owned-surface aggregate is >= 85%; the figure is quoted in the SUMMARY.
    - The mutation spot-check on `bridge_multiplex.go` reports >= 70% killed; killed/total is quoted.
    - `AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh` prints `ok: ... checked N row(s)` with N >= 1.
    - `46-VALIDATION.md` frontmatter reads `status: validated`, its Wave 0 checklist has six ticked items, and its Sign-Off checklist has no unaddressed line.
    - `make quality` green; `go test -race ./internal/agent/mcptools/... ./internal/gateway/... ./internal/agent/tools/...` green.
    - The SUMMARY quotes both COMPAT row counts, both fork SHAs, both image tags, and the slot-grant outcome.
    - CI is green on the pushed commit (`gh run list` for the branch shows success).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'bash scripts/coverage_docker.sh && AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh'</automated>
    <human-check>The phase's live evidence reads as a real driven scenario across both sidecars, and the handoff names what Phases 47/48 inherit rather than leaving them to rediscover it.</human-check>
  </verify>
  <done>Every gate is green, the evidence is written down, and the next phase starts from numbers rather than a rediscovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| An unlisted third-party server -> Aura's tool registry | SC#6's whole promise: a server with no entry anywhere must work, and must fail closed while doing it. |
| A stale test fixture -> a green gate | A gate scoring tools that no longer exist is a passing test that measures nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-35 | Elevation of Privilege | An unlisted server's unannotated mutating tool being under-gated | mitigate | `TestCalculatorServerLive` asserts `Mutating && Destructive` for an unannotated tool through a REAL mount, promoting the unit-level `TestMCPToolRisk_NilAnnotationsFailsClosed` to end-to-end evidence. |
| T-46-36 | Elevation of Privilege | An unlisted server self-promoting into the always-loaded manifest | mitigate | The calculator lands `Deferred: true` because the 2-slot budget is spent; asserted live. Overflow fails closed to deferred-but-discoverable. |
| T-46-37 | Repudiation | The trust posture regressing silently after ratification | mitigate | Named tripwire tests for both halves (descriptions plain, results trusted for ANY mounted server), checking a marker list rather than one literal, so a reintroduced framing prefix goes red. |
| T-46-38 | Tampering | A retrieval gate that passes against a corpus describing a system that is gone | mitigate | The corpus is re-dumped from the live registry, the size floor is re-based to a measured number, impossible gate cases are removed with their reason, and the floors may not be lowered to force green. |
| T-46-39 | Denial of Service | Coverage regression from new daemon-free branches | mitigate | Every branch this phase adds is in `internal/agent/mcptools`/`internal/gateway`, both in the `db_integration` tag set; `scripts/coverage_docker.sh` is run locally before the push rather than discovered red in CI 20 minutes later. |
| T-46-40 | Information Disclosure | Live conversation content entering the repository as evidence | mitigate | Evidence quotes structural fields (`tool_name`, action, status) with personal content redacted from `args_raw`/`result_preview`. |
| T-46-SC | Tampering | package installs | mitigate | No package installs in this phase; nothing to gate. |
</threat_model>

<verification>
- `make quality` green.
- `bash scripts/coverage_docker.sh` >= 85% owned surface.
- Mutation spot-check >= 70% killed on `bridge_multiplex.go`.
- `scripts/quality_snapshot_gate.sh` prints `ok: ... checked N row(s)`.
- All three tagged tiers (`calendar_integration`, `whatsapp_integration`, `calculator_integration`) pass live.
- CI green on the pushed commit.
</verification>

<success_criteria>
Every success criterion this phase still owns (SC#1, SC#2, SC#3, SC#4, SC#6 — SC#5 is deleted) is
proven live and written down; every gate is green; and Phases 47/48 inherit measured numbers instead
of a surprise.
</success_criteria>

<output>
Create `.planning/phases/46-mcp-trust-and-facade/46-09-SUMMARY.md` when done, with the phase handoff.
</output>
