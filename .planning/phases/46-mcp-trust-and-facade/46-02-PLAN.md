---
phase: 46-mcp-trust-and-facade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md
autonomous: false
requirements: [MCP-04, MCP-05]

must_haves:
  truths:
    - "docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md exists and specifies both curated surfaces: the calendar fork's ONE curated tool (14 actions), and the WhatsApp fork's ONE curated tool (12 actions) PLUS the two view-bound tools kept raw — three model-facing WhatsApp tools in total, stated as a number, not left to be inferred."
    - "The design doc names list_chats and list_messages as EXEMPT from the merge, records the operator's Task 1 decision verbatim, and states why the exemption exists: their MCP Apps views are live and in use, and a merged Mutating tool lands on the 403 side of CallReadOnlyTool's read-only gate."
    - "For every action in both curated lists, and for the two exempted raw tools, the design doc states its risk class (read / mutate / destructive), so bridge_risk.go's re-keyed table and each fork's action enum are written against one list, not two."
    - "The design doc states that trustedRecipeActions ends the phase with THREE key spaces, not two: calendar = action-keyed; whatsapp = MIXED within one source (12 action-keyed + 2 raw-tool-name-keyed); memory = raw-tool-name-keyed — and that no name is shared between whatsapp's two key spaces."
    - "The design doc states the flat-union argument shape: one flat object, `action` plus every field any action needs, all optional at the schema root except `action`, per-action requirements stated in each field's description, and no root oneOf/anyOf."
    - "The design doc forbids a bare `id` argument and names the typed identifiers instead (eventId, chatId, messageId, emailId)."
    - "The design doc specifies MCP-05's fix concretely: the calendar detail action accepts the same opaque reference the listing action returns, and requires no accountId."
    - "The design doc states the merged-description byte budget as ~1.5-2KB per curated tool with 4096 bytes as the hard cap, AND states the measured always-loaded cost of the two exempted WhatsApp tools on top of it, so the standing per-turn cost is a number a reviewer can check rather than an inference."
    - "The design doc records the model-facing namespaced names both curated tools will register under, derived from the mount namespaces in internal/mcp/manager/catalog.go, not guessed."
    - "The design doc states that mount-time reconciliation must treat a table entry as accounted-for when it is EITHER in the curated tool's action enum OR the name of a tool the server still advertises — otherwise the two exempted entries WARN spuriously on every mount."
    - statement: "The design doc's action lists are what the fork commits and the Go table are both written against; a divergence between them is caught by the mount-time reconciliation rather than by this document."
      verification: backstop
  artifacts:
    - path: "docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md"
      provides: "The in-repo contract the two fork commits implement (D-24)"
      min_lines: 140
      contains: "list_chats"
  key_links:
    - from: "docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md"
      to: "internal/agent/mcptools/bridge_risk.go trustedRecipeActions"
      via: "one action->risk-class list shared by fork and host, plus the two exempted raw names"
      pattern: "mcpActionRead|mcpActionMutate|mcpActionDestructive|MCPActionRead"
  prohibitions:
    - "Must NOT specify an Aura-side facade tool, hide-list, curation config or per-integration namespace table — the whole point of the design is that the host stays generic and the next mounted server needs no Aura code (D-17). The views exemption is a FORK-side decision about what the fork registers, not an Aura-side allow-list."
    - "Must NOT leave the curated raw handlers callable-but-unadvertised in either fork; the design must say DELETE (D-26), because an unadvertised reachable side-effect is a capability the operator cannot see. The two exempted WhatsApp handlers are a different case — they stay ADVERTISED, which is the opposite of dark."
    - "Must NOT re-open the mount target. Both sidecars were pinned by commit 73764ea11; the branch question the 2026-08-17 research asked is settled and must not be re-litigated in this document."
---

<objective>
Write the in-repo design doc that is the contract both fork commits implement (D-24), and take the
one genuinely-open operator decision this phase still carries: whether WhatsApp's two view-bound
tools are curated away or exempted from the merge.

Purpose: the curation happens in two other repositories. Without a contract in Aura's tree, a
reviewer cannot tell what the mounted surface is supposed to be, and the Go risk table and each
fork's action enum get written against two different lists.

**What changed since this plan was first written (2026-08-17).** The decision this plan used to ask
for — "12 actions as on `aura/cockpit-connect`, or 14 after merging from `main`" — is MOOT. That
branch is retired (fused into `main` on 2026-07-01) and nothing mounts it; commit `73764ea11` pinned
BOTH sidecars to the bytes they were built from (`whatsapp-mcp:sha-e0b8345` off `main`,
`aura-pim-mcp:10383276961828bc19f34a9372ba2c64a14e2b62`). The live server serves 14 tools and the Go
table was never stale. What replaced that question is a real design conflict the 2026-08-17 scope
never saw: **two of WhatsApp's 14 tools carry live MCP Apps views**, and collapsing all 14 into one
Mutating multiplexed tool takes a working, in-use operator capability to a hard 403.

Output: `docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md`, mirroring the 2026-06-16
fork-design precedent's ten-section shape. The filename keeps its 2026-08-17 date because plans
46-05, 46-06 and 46-08 reference that exact path; the document carries a dated "Revised 2026-08-22"
banner instead of being renamed.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md
@.planning/phases/46-mcp-trust-and-facade/46-RESEARCH.md
@.planning/phases/46-mcp-trust-and-facade/46-HALT-2026-08-22.md
@.planning/phases/46-mcp-trust-and-facade/46-PATTERNS.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| Curating the two view-bound tools away vs. exempting them | **one-way** | `checkpoint:decision` in Task 1. D-26 DELETES what is curated; the fork image is then pinned; the views' own embedded JS calls `list_chats`/`list_messages` BY NAME, so once those names are gone the documents are inert and only a second fork commit can bring them back. |
| The curated action set (a published `tools/list` other things depend on) | **one-way** | Same checkpoint — the exemption decision IS the action-set decision for WhatsApp (12 vs 14). |
| The model-facing curated tool names | **one-way** | Same checkpoint, sub-decision (B): the name is the `multiplexedClassifiers` key and appears in the boot-guard's panic message. |
| The flat-union argument shape | reversible (D-19, re-confirmed against SEP-2106) | Recorded, no checkpoint. |
| The ten-section doc structure | reversible | Mirrors the 2026-06-16 precedent. |
| The mount target / image pin | **already taken** | `73764ea11`, both sidecars. Not re-opened here. |
</reversibility>

## Artifacts this phase produces

Created by THIS plan:
- `docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md` (revised 2026-08-22).
- The vocabulary every later plan cites: the calendar curated tool name + its 14 action names; the
  WhatsApp curated tool name + its 12 action names; the named 2-tool exemption list
  (`list_chats`, `list_messages`); the model-facing namespaced forms of both curated tools; and the
  three-key-space description of `trustedRecipeActions` that plans 46-06 and 46-08 implement.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the WhatsApp views question, and confirm the two model-facing tool names</name>
  <files>docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md (Decisions log — written in Task 2)</files>
  <read_first>
    - `.planning/phases/46-mcp-trust-and-facade/46-RESEARCH.md` §2 "The views question — recommendation and cost" IN FULL — the recommendation, the two rejected candidates with their evidence, and the four stated costs of the recommendation.
    - `.planning/phases/46-mcp-trust-and-facade/46-HALT-2026-08-22.md` §4 (the two independent ways 14->1 breaks the views) and its later section "The views RENDER today — measured, not inferred": `GET /api/mcp/view?...thread.html` -> 200 with 27,733 bytes of armed HTML; `POST /api/mcp/view/call {tool:list_chats}` -> 200 with real rows; `POST /api/mcp/view/call {tool:send_message}` -> **403 `tool call refused`**. That 403 is the measurement that settles the question.
    - `internal/agent/mcptools/bridge_supervisor.go:337-344` — `CallReadOnlyTool` -> `toolIsReadOnly` -> `s.bridged[name]`, keyed by the RAW wire name (`trackBridgedTools`, `:363-374`, says so in its own comment) and gated on `!bt.Spec().Mutating`. This is the gate the 403 came from, and the reason a curated name would not merely be refused but not found at all.
    - `internal/agent/mcptools/bridge_views.go:35-60` — `viewRefFor` reads `_meta.ui` off the RAW mounted tool once, at construction, and is deliberately never refreshed.
    - `internal/mcp/manager/catalog.go:115-146` — the mount entries: PIM under namespace `calendar` (`Source: "recipe:calendar"`), WhatsApp under namespace `whatsapp` (`Source: "recipe:whatsapp"`).
    - `internal/agent/mcptools/name.go:43-64` — `namespacedName` builds `<namespace>__<tool>`; this is what makes the model-facing name and therefore the `multiplexedClassifiers` key.
    - `.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md` D-18 (names the curated tools `calendar` and `messages`; its "1 tool each" rationale is what option `views-exempt` amends), D-26 (raw handlers DELETED, one-way), D-27 (<= 3 model-facing tools earns a slot, global cap 2).
  </read_first>
  <decision>
Two choices, taken together before either fork is touched. (A) is one-way and is the one the phase
halted for; (B) is the naming sub-decision that was already open and is unaffected by the halt.

**(A) WhatsApp's two view-bound tools: exempt them from the merge, or curate them away.**

Measured live 2026-08-22 against the pinned `whatsapp-mcp:sha-e0b8345`: the sidecar serves 14 tools;
exactly two carry `_meta.ui` bindings (`list_messages` -> `ui://whatsapp/thread.html`,
`list_chats` -> `ui://whatsapp/chats.html`); both documents are catalogued sealed and render in the
cockpit today; a view-initiated `list_chats` call returns 200 with real rows and a view-initiated
`send_message` returns 403. A merged `messages(action=...)` tool must be Mutating (it covers
`send_message`), so every view callback through it lands on that same 403 — and worse, the views'
own JS calls `list_chats`/`list_messages` by name, which `toolIsReadOnly` resolves against the RAW
advertised names, so after D-26 deletes them there is no tool by that name at all.

A third candidate — a per-RESULT view binding, so one curated tool could point at two documents —
was investigated and **does not exist**: `bridgedTool` carries exactly one `mcp.ViewRef`, set once
from the raw tool at construction, and SEP-1865 (status Final) binds the view on the TOOL via
`Tool._meta.ui`, listing *"Support multiple UI resources in a tool response"* under Future
Considerations. Building it would mean inventing a protocol extension both Aura and its forks speak
alone. It is recorded as investigated-and-absent, not offered as an option.

**(B) The model-facing names.** D-18 names the curated tools `calendar` and `messages`. The mount
namespaces are `calendar` and `whatsapp`, and `namespacedName` produces `<namespace>__<tool>` — so
D-18 taken literally yields **`calendar__calendar`** (redundant) and **`whatsapp__messages`** (reads
well). The alternative is to name the calendar fork's curated tool something else (e.g. `pim`,
giving `calendar__pim`).
  </decision>
  <context>
(A) is one-way because D-26 DELETES the raw handlers rather than unadvertising them, the fork image
is then pinned by immutable tag, and the view documents ship inside that image with tool names
hard-coded in their JS. Choosing `views-drop` and reversing it later costs a second fork commit, a
second publish, a second pin, and an operator-visible outage of a capability they use now.

(A) also spends WhatsApp's entire headroom under D-27: exempting leaves WhatsApp at exactly 3
model-facing tools against a `<= 3` ceiling. One further standalone tool added in the fork flips the
whole server to deferred with no Aura code change. That is the cost of the recommendation, and it is
why this is put to the operator rather than decided by the planner.

(B) is one-way because the model-facing name is the `multiplexedClassifiers` map key and appears in
`ValidateClassifiable`'s boot-guard panic message, and because persisted `aura.tool_invocations`
rows, paused approvals and scheduled `agent_job`s referencing the old names have nothing to resolve
against afterwards (COMPAT-01/02/03 belong to Phases 47/48, i.e. AFTER this phase).
  </context>
  <options>
    <option id="views-exempt">
      <name>(A) RECOMMENDED — Exempt `list_chats` and `list_messages`: curate 12 actions, keep 2 raw tools. WhatsApp ends at 3 model-facing tools.</name>
      <pros>The views keep working exactly as measured today — both documents render and their read callbacks keep returning 200, because the two exempted tools stay raw-name-addressable and stay non-Mutating. Nothing is dropped: all 14 capabilities remain reachable. Still inside D-27's `<= 3` ceiling, so WhatsApp still earns its always-loaded slot with no code change. The merged description is computed over 12 actions instead of 14, so it is smaller, not larger.</pros>
      <cons>D-18's rationale text ("the two curated forks expose 1 tool each") becomes wrong for WhatsApp and must be corrected in 46-01's TOOL-14 prose, in ROADMAP §46 and in D-27's commentary. `trustedRecipeActions[whatsAppRecipeSource]` becomes a mixed key space WITHIN one source, which is subtler than D-35 anticipated. Zero headroom left under the `<= 3` ceiling. Two extra always-loaded descriptions are paid every turn (measured: 997 + 399 = 1,396 bytes).</cons>
    </option>
    <option id="views-drop">
      <name>(A) Curate all 14 actions into one tool and let the views die.</name>
      <pros>Honours D-18 literally: exactly 1 model-facing tool per curated fork, and the slot arithmetic stays the simple story the phase started with. Two tools of headroom under the `<= 3` ceiling. Smallest always-loaded footprint.</pros>
      <cons>Destroys a working, in-use capability, measured live on an authenticated session this week — and buys nothing the exemption does not also buy, since 3 <= 3 either way. The view documents ship inside the fork image and keep being served by `resources/list`, so the operator sees a UI that renders and then refuses every interaction: a 403 on the callback path, and a not-found once the raw names are deleted. Reversal costs a second fork commit + publish + pin.</cons>
    </option>
    <option id="name-keep-d18">
      <name>(B) Keep D-18's names: model-facing `calendar__calendar` and `whatsapp__messages`.</name>
      <pros>Honours the locked decision literally; no re-opening.</pros>
      <cons>`calendar__calendar` is redundant in the manifest the model reads every turn.</cons>
    </option>
    <option id="name-pim">
      <name>(B) Rename the calendar fork's curated tool to `pim`: model-facing `calendar__pim` and `whatsapp__messages`.</name>
      <pros>Reads better and describes what the sidecar actually serves (mail + calendar + contacts).</pros>
      <cons>Diverges from D-18's literal wording; `pim` inside a `calendar` namespace is its own small confusion.</cons>
    </option>
  </options>
  <resume-signal>Select one of `views-exempt` | `views-drop`, AND one of `name-keep-d18` | `name-pim`. Both choices are recorded verbatim in the design doc's Decisions log.</resume-signal>
  <action>
Do not write any file before the operator answers. Once both selections are in, carry them forward
UNCHANGED: quote each verbatim into the design doc's Decisions log in Task 2, together with the
rejected option and its stated cost, and use the selected names byte-for-byte everywhere later plans
read them (`multiplexedClassifiers` keys, name constants, integration-test expectations). If the
operator selects `views-drop`, say so explicitly in the SUMMARY — plan 46-08 is written against
`views-exempt` and its Tasks 1-3 change shape under the other answer.
  </action>
  <acceptance_criteria>
    - The selected (A) option is recorded verbatim in the design doc's Decisions log, together with the rejected option's stated cost, and with the per-result-view candidate recorded as investigated-and-absent (citing SEP-1865's Future Considerations and `bridgedTool`'s single `ViewRef`).
    - If `views-exempt` is selected: the design doc names `list_chats` and `list_messages` as the exemption list, states WhatsApp's model-facing tool count as **3**, and states that D-18's "1 tool each" rationale is amended for WhatsApp only.
    - If `views-drop` is selected: the design doc states that the two `ui://whatsapp/*.html` documents become unreachable, names the 403 and the not-found as the two mechanisms, and records that a follow-on item is owed to stop the fork serving documents nothing can drive.
    - The chosen model-facing names are recorded, and every later plan's `multiplexedClassifiers` key and name constant must match them byte-for-byte.
    - The Decisions log records the 2026-08-17 "12 vs 14 / `aura/cockpit-connect` vs `main`" question as SETTLED by measurement and commit `73764ea11` — not silently dropped.
  </acceptance_criteria>
  <verify>
    <human-check>Operator selects one option from each pair; the selections are quoted verbatim in the design doc's Decisions log, with the rejected alternative and its cost beside them.</human-check>
  </verify>
  <done>Both choices are recorded, with the alternative and its cost written down beside them, and the moot 2026-08-17 branch question is explicitly marked settled rather than silently dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Write the curated-surface design doc</name>
  <files>docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md</files>
  <read_first>
    - `docs/superpowers/specs/2026-06-16-calendar-pim-mcp-fork-design.md` — the precedent, in full: ten numbered sections (Background & motivation; Goals + Non-goals; Architecture; Thin-fork changes; Trimmed tool surface; Aura-side integration; Security & policy; Validation plan; Risks & open items; Decisions log), the "no C# vendored into the Aura repo — reproduction recipe + image pin live in-repo" rule, branch `aura/pim-sidecar`, and the admin-REST-is-not-MCP distinction.
    - `internal/agent/mcptools/bridge_risk.go:26-83` — the 14 calendar and 14 WhatsApp entries with their current risk classes; these classes carry over UNCHANGED to the action names. Re-read them from the file rather than copying the list out of this plan.
    - `internal/agent/mcptools/bridge_risk.go:108-149` — `classifyToolRisk`'s single lookup site, keyed on `t.Name` (the RAW wire name). Under `views-exempt` this branch stays LIVE for the two exempted tools; it is not, as the 2026-08-17 scope assumed, dead for everything except memory.
    - `internal/agent/tools/task.go:104-125` and `:150-181` — the flat-union `action` schema shape (root object, only `action` required, property-level enum, per-action requirements in field descriptions, no root oneOf) and the dispatch-by-action-string shape, which each fork's handler mirrors in its own language.
    - `internal/agent/tools/testdata/deferred_manifest.json` — the current per-tool descriptions for `calendar__*` and `whatsapp__*` (4,504 and 7,578 bytes as Aura frames them) and the `accountId` argument descriptions in `create_event`, `get_calendar_events` and `get_calendar_event_details`, which are the evidence for MCP-05's fix.
    - `internal/agent/mcptools/bridge.go:203-253` and `:309-346` — `capSchemaDescriptions` (16KB schema / 4096 description / 512 per-arg / 128 properties) and `frameMCPDescription`'s 4,096-byte cap; the budget these force.
    - `internal/agui/connect_pim_api.go` — the cockpit's PIM admin proxy; the admin REST surface is NOT MCP and this curation must not touch it.
    - `.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md` D-17..D-20, D-23, D-24, D-26, D-27, D-32, D-33, D-35, D-36.
    - `.planning/phases/46-mcp-trust-and-facade/46-RESEARCH.md` §1 (the 12/2 split), §2 (costs), §3 (calendar has no views and declares no `resources` capability at all), §4 (`sha-<7hex>` vs 40-hex).
  </read_first>
  <action>
Create `docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md` with the SAME ten numbered
sections as the 2026-06-16 precedent, covering BOTH forks in one document. Open it with a dated
**Revised 2026-08-22** banner naming the halt and the replan, so a reader who finds the 2026-08-17
filename knows the content is newer than its name.

Section content that is load-bearing:

**2. Goals / Non-goals** — Non-goals must state explicitly: no Aura-side facade tool, no curation
config, no hide-list, no `bridgePolicy` namespace table; the admin REST surface
(`internal/agui/connect_pim_api.go`) is untouched; the provider-call implementation layer in both
forks is untouched — only the registration/dispatch layer collapses. State that the views exemption
is a decision about what the FORK registers, not an Aura-side allow-list, so D-17 still holds.

**4. Thin-fork changes** — name the exact edit site and target branch per fork, and get the branches
right, because the 2026-08-17 edition of this phase got one of them wrong:
- `aura-pim-mcp`: `src/CalendarMcp.HttpServer/Program.cs` (the `.WithTools<...>()` registration
  block; `app.MapMcp()` serves whatever is registered), branch **`aura/pim-sidecar`**, whose
  `aura-publish-image.yml` tags the raw `${{ github.sha }}` — a full 40-hex tag.
- `whatsapp-mcp`: `whatsapp-mcp-server/main.py` (the `@mcp.tool()` decorators; `whatsapp.py`
  unchanged), branch **`main`**. `aura/cockpit-connect` is RETIRED — fused into `main` on
  2026-07-01, 143 commits behind, nothing mounts it. `main`'s `publish-image.yml` uses
  `docker/metadata-action`'s `type=sha`, which mints **`sha-<7hex>`**, never a 40-hex tag.
State that the curated raw handlers are DELETED, not unadvertised (D-26), and why: an unadvertised
but callable handler is dark code and stays reachable by anything holding the sidecar's HTTP port,
which Aura's namespacing does not protect. State that an EXEMPTED tool is the opposite case — it
stays advertised, so it is visible to the operator, to `tools/list` and to the risk table.

**5. The two curated surfaces** — one table per fork with columns `action` | `risk class` |
`required arguments` | `underlying provider call`.
- Calendar: the 14 actions from `trustedRecipeActions[calendarRecipeSource]` with their existing
  classes preserved exactly (`list_accounts`/`get_emails`/`get_email_details`/`search_emails`/
  `list_calendars`/`get_calendar_events`/`get_calendar_event_details`/`get_contacts`/
  `search_contacts`/`get_contact_details` = read; `create_event`/`update_event` = mutate;
  `respond_to_event`/`send_email` = destructive). One curated tool, 14 actions, no exemptions —
  calendar declares no `resources` capability at all and has zero views (measured 2026-08-22).
- WhatsApp under `views-exempt`: a 12-row curated table (`search_contacts`, `get_contact`,
  `get_chat`, `get_direct_chat_by_contact`, `get_contact_chats`, `get_last_interaction`,
  `get_message_context`, `download_media`, `send_message`, `send_reaction`, `send_file`,
  `send_audio_message`), PLUS a separate, clearly-headed **exemption table** for `list_chats` and
  `list_messages` giving their risk class (both read), their `ui://` binding, and the sentence that
  they remain registered as their own `@mcp.tool()` handlers with their `_meta.ui` intact.
  Preserve every existing class exactly (7 read, `download_media` = mutate, 4 destructive across the
  curated 12; both exempted = read). Do not invent new classes and do not re-tier an existing action.
  Under `views-drop`, write a single 14-row table and no exemption table.

Also in section 5: the flat-union schema shape (one flat object; `required: ["action"]` ONLY;
`action` carries a property-level `enum` of exactly the action names in the curated table — the
exempted names are NOT enum members; every other field optional at the root with its per-action
requirement stated in that field's `description`; no root `oneOf`/`anyOf`). And the ID discipline:
never a bare `id` — `eventId`, `chatId`, `messageId`, `emailId`.

Also in section 5: the DESCRIPTION BUDGET, with the always-loaded arithmetic stated as numbers
rather than left to be inferred:
- `frameMCPDescription` caps ANY single description at 4,096 bytes; `capSchemaDescriptions` empties
  a schema above 16KB or 128 properties.
- Target ~1.5-2KB for each curated tool's top-level description: a short preamble plus ONE line per
  action naming its required fields. Per-field detail belongs in the schema's argument descriptions
  (512 bytes each, read only when the model inspects arguments).
- Under `views-exempt`, the curated tool is no longer the only always-loaded WhatsApp description.
  **Measured 2026-08-22** by `tools/list` against the pinned `whatsapp-mcp:sha-e0b8345` on
  `127.0.0.1:8092/mcp` (protocolVersion `2025-06-18`, 14 tools, `annotations` empty on all 14):
  `list_messages` description = **997 bytes** (input schema 1,018 bytes);
  `list_chats` description = **399 bytes** (input schema 456 bytes); all 14 served descriptions
  together = 6,346 bytes. So WhatsApp's standing always-loaded description cost becomes
  ~1.5-2KB + 1,396 bytes = **~2.9-3.4KB**, against calendar's ~1.5-2KB: both curated slots together
  land near 5KB, comfortably inside D-36's "roughly 2k tokens/turn for both slots" target and far
  under the 6,346 bytes the raw WhatsApp surface would cost if it were loaded. Re-measure both
  numbers if the WhatsApp pin has moved since 2026-08-22, and state the pin they were taken from.

**6. Aura-side integration** — the model-facing names chosen in Task 1, derived from the catalog's
mount namespaces (`calendar`, `whatsapp`) through `namespacedName`'s `<namespace>__<tool>`; that
these names are the `multiplexedClassifiers` keys; and that the immutable image pin in `compose.yaml`
is the join point that makes each fork's new surface reach Aura only when Aura says so.

Also in section 6, and load-bearing for plan 46-08 — **the three key spaces of
`trustedRecipeActions`**. State them as a short table and say plainly that this is subtler than
D-35's original two-way framing:

| source | key space | read by |
|---|---|---|
| `recipe:calendar` | action names | the gateway classifier, from `rawArgs` |
| `recipe:whatsapp` | MIXED: 12 action names + 2 raw tool names (`list_chats`, `list_messages`) | the gateway classifier for the 12; `classifyToolRisk`'s existing `t.Name` lookup for the 2 |
| `recipe:memory` | raw tool names | `classifyToolRisk`'s `t.Name` lookup |

State that the two WhatsApp key spaces are disjoint (no curated action name equals either exempted
tool name) and that a test must assert it, because a collision would make one entry answer two
different questions.

Also in section 6: **the reconciliation rule**. D-33 WARNs at mount in both directions. Under
`views-exempt`, direction (b) — "a table entry the server no longer advertises" — would fire on
`list_chats` and `list_messages` at every single mount, because they are in the table and NOT in the
curated enum. State the correct predicate: a table entry is accounted-for when it is EITHER a member
of the curated tool's `action` enum OR the name of a tool the server currently advertises. This is
derived from what the server serves, so it introduces no Aura-side list and D-17 still holds.

**MCP-05's fix, stated concretely (section 5 or 6):** today `accountId` is two things under one
name — a defaultable routing hint in `create_event` and `get_calendar_events`, and a REQUIRED opaque
handle in `get_calendar_event_details` documented as "Account ID from get_calendar_events". The
curated detail action requires NO `accountId`: it accepts the same single opaque reference the
listing action returns (the shape `prd.md:4579` already uses for documents). Name the reference
argument and state that its value is opaque to Aura — Aura passes back byte-for-byte what the fork
issued and never re-cases, normalizes or re-encodes it.

**7. Security & policy** — state the view-callback grant explicitly, because the exemption is what
keeps it alive: a rendered view may call back only into the server that served it
(`ViewCallers.CallForView`), only tools that are advertised and non-Mutating
(`CallReadOnlyTool` -> `toolIsReadOnly`, fail-closed on an unknown name), with no agent turn and no
human around it. Under `views-exempt` that grant covers exactly `list_chats` and `list_messages`,
both read, unchanged from today — the exemption preserves an existing grant, it does not widen one.

**8. Validation plan** — per-fork: the fork's own CI green before push; the published immutable tag
as the artifact (**40-hex for `aura-pim-mcp`, `sha-<7hex>` for `whatsapp-mcp`** — say this
explicitly, it is the difference between two workflow files, not a formatting preference); and from
Aura's side, the `calendar_integration` / `whatsapp_integration` tag tests asserting the expected
model-facing tool count per sidecar — **1 for calendar, 3 for WhatsApp under `views-exempt`** — with
the expected action enum. State explicitly that no CI job can check across the repo boundary, so
"fork CI green" is a precondition recorded in the phase SUMMARY, not something Aura's pipeline
asserts.

**9. Risks & open items** — (i) the COMPAT blast radius: deleting the raw tool names leaves
persisted `aura.tool_invocations` rows, any paused approval, and any scheduled `agent_job`
referencing `calendar__*` / `whatsapp__*` with nothing to resolve against; COMPAT-01/02/03 are
assigned to Phases 47/48, i.e. after this. (ii) Under `views-exempt`, WhatsApp sits at exactly 3
model-facing tools with ZERO headroom under D-27's `<= 3` ceiling: one further standalone tool in
the fork silently flips the whole server to deferred with no Aura code change. Record it as a named
risk, with a mount-time WARN as an optional mitigation plan 46-08 may add. (iii) Aura never declares
the `io.modelcontextprotocol/ui` client capability (`internal/mcp/sdkclient.go:71` sends empty
`ClientCapabilities`; `AppsClientSettings()` has no production caller) — the views render anyway
because the server advertises `_meta.ui` unconditionally, so this is a missed negotiation promise,
recorded here and explicitly OUT of Phase 46's scope.

**10. Decisions log** — Task 1's two selections verbatim, plus D-17/D-18/D-19/D-20/D-23/D-26/D-27/
D-32/D-35/D-36 one line each with their rejected alternative. Include one line recording that the
2026-08-17 "12 vs 14 / `aura/cockpit-connect` vs `main`" question was SETTLED by measurement and
commit `73764ea11`, not decided here — so a later reader does not think it was forgotten.
  </action>
  <acceptance_criteria>
    - The file `docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md` exists with ten numbered `##` sections whose titles match the 2026-06-16 precedent's roles, and carries a dated "Revised 2026-08-22" banner.
    - Section 5 contains the calendar action table and the WhatsApp curated table; under `views-exempt` it also contains a separately-headed exemption table naming `list_chats` and `list_messages` with their `ui://whatsapp/*.html` bindings.
    - The union of the curated tables' `action` values plus the exemption list equals exactly the key set later written into `trustedRecipeActions[calendarRecipeSource]` and `[whatsAppRecipeSource]` by plans 46-06 and 46-08, and each entry's risk class matches the class it holds in `bridge_risk.go` today (7 read + 1 mutate + 4 destructive across WhatsApp's curated 12; both exempted read).
    - The document contains the literal strings `required: ["action"]`, `eventId`, `chatId`, `messageId`, `emailId`, and states no root `oneOf`.
    - The document states the ~1.5-2KB per-tool description target, cites 4,096 as the hard cap, and states the measured always-loaded cost of the exempted pair (997 + 399 = 1,396 bytes) naming the pin those bytes were measured from.
    - The document contains the three-key-space table for `trustedRecipeActions` and the sentence that the two WhatsApp key spaces are disjoint.
    - The document states the reconciliation predicate (enum member OR currently-advertised tool).
    - The document states that `get_calendar_event_details`' replacement requires no `accountId`.
    - The document names both fork repos, the branch `aura/pim-sidecar` for PIM and `main` for WhatsApp, both edit-site file paths, and both tag formats (40-hex vs `sha-<7hex>`).
    - The document contains no instruction to push to `aura/cockpit-connect`, and says that branch is retired.
    - The document's Non-goals section contains the words `no hide-list` and `no curation config` (or equivalents naming both).
    - The document's Risks section names COMPAT-01, COMPAT-02 and COMPAT-03, and names the zero-headroom risk.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'f=docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md; test -f "$f" || exit 1; [ "$(grep -c "^## " "$f")" -ge 10 ] || exit 1; for s in COMPAT-03 eventId 4096 list_chats "sha-" aura/pim-sidecar; do grep -qF -- "$s" "$f" || { echo "missing: $s"; exit 1; }; done; if grep -qF "aura/cockpit-connect" "$f" && ! grep -qiF "retired" "$f"; then echo "names the retired branch without marking it retired"; exit 1; fi; echo OK'</automated>
  </verify>
  <done>A reviewer can read one file in Aura's tree and know exactly what both curated surfaces are, how many model-facing tools each sidecar ends up with, what each action costs in risk terms, which tools were deliberately left out of the merge and why, and what breaks when the raw names disappear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aura repo -> fork repos | The design doc is the only artifact crossing the boundary; a divergence between it and a fork commit is invisible to CI. |
| Curated tool schema + description -> model | Read by the model every turn once always-loaded; server-authored text the host frames but does not semantically sanitise. |
| Server-authored view document -> mounted server | A rendered `ui://` document calls back into its own server with no agent turn and no human in the loop; the read-only gate is the whole grant. |

## STRIDE Threat Register (ASVS L1; block on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Tampering | The curated action enum vs. what the fork actually serves | mitigate | The doc is ONE action list that each fork's enum and the Go table are both written from; residual drift is caught at mount by the WARN reconciliation added in 46-06 (D-33), never silently. |
| T-46-05 | Denial of Service (context budget) | The always-loaded descriptions | mitigate | ~1.5-2KB target per curated tool against a 4,096-byte hard cap, per-field detail pushed into 512-byte argument descriptions, and — new under the exemption — the two exempted descriptions counted explicitly (997 + 399 measured) rather than assumed free. `capSchemaDescriptions` remains the backstop. |
| T-46-06 | Elevation of Privilege | Risk-class assignment during the merge | mitigate | The doc reproduces each action's EXISTING class from `bridge_risk.go`; re-tiering is out of scope and the acceptance criteria assert class equality, including for the two exempted reads. |
| T-46-07 | Spoofing | The opaque account/event reference (MCP-05) | mitigate | The reference is issued by the fork and passed back byte-for-byte; Aura never synthesizes or defaults it, which is exactly why host-side injection was rejected (it would pass the wrong account). |
| T-46-08 | Elevation of Privilege | The view-callback grant preserved by the exemption | accept (documented, unchanged from today) | The exemption preserves an EXISTING grant rather than widening one: a view may call back only into its own server, only advertised non-Mutating tools, fail-closed on an unknown name (`toolIsReadOnly`). Section 7 states the grant explicitly so a later reader need not re-derive it, and the measured 403 on `send_message` is quoted as the enforcement evidence. |
| T-46-09 | Repudiation | A moot decision silently dropped from the record | mitigate | The Decisions log must record the 2026-08-17 branch question as SETTLED by measurement and commit `73764ea11`, so its disappearance is documented rather than looking like an oversight. |
| T-46-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to gate. |
</threat_model>

<verification>
- `git diff --stat` shows only the new design doc.
- The action tables' risk classes are diffed against `bridge_risk.go`'s current classes and reported as identical in the SUMMARY.
- The two measured description byte counts are re-confirmed against the pin named in the doc (one `tools/list` against `127.0.0.1:8092/mcp`), or the doc records newer numbers and the newer pin.
</verification>

<success_criteria>
The fork work has a written contract in Aura's tree; the one-way views/action-set decision and the
naming decision were taken by the operator with their costs stated; and the settled mount-target
question is on the record as settled rather than missing.
</success_criteria>

<output>
Create `.planning/phases/46-mcp-trust-and-facade/46-02-SUMMARY.md` when done, recording the two
selected options verbatim, the final action lists, the exemption list, and the measured description
byte counts with the pin they were measured from.
</output>
