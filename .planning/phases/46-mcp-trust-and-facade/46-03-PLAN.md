---
phase: 46-mcp-trust-and-facade
plan: 03
type: execute
wave: 2
depends_on: ["46-01", "46-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MCP-02, MCP-04, MCP-05]

must_haves:
  truths:
    - "REQUIREMENTS.md's MCP-02, MCP-04 and MCP-05 rows read as current-state prose with no inline strikethrough, in the same clean shape MCPC-01..05 already use."
    - "Every wording removed from an MCP-02/04/05 row survives verbatim in a dated footnote in the same file — the history is relocated, never deleted."
    - "ROADMAP.md's Phase 46 section states the current goal, mechanism and dependencies once, with no SUPERSEDED paragraph left inline."
    - "Every SUPERSEDED paragraph removed from the ROADMAP's Phase 46 section survives verbatim in a dated footnote in the same file."
    - "ROADMAP Success Criterion 5 remains struck with its DELETED 2026-08-16 (D-07) marker and is not rewritten into a live criterion."
    - "The rewritten rows and section cite the prd.md amendment numbers assigned by plan 46-01."
    - statement: "The count of distinct superseded passages removed from the two files equals the count of footnotes added, so no falsification trail is lost in the relocation."
      verification: backstop
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Clean MCP-02/04/05 rows plus their dated footnotes"
      contains: "MCP-04"
    - path: ".planning/ROADMAP.md"
      provides: "Clean Phase 46 section plus its dated footnotes"
      contains: "Phase 46: MCP trust and facade"
  key_links:
    - from: ".planning/REQUIREMENTS.md MCP-04 row"
      to: "prd.md amendment assigned by 46-01"
      via: "amendment number citation"
      pattern: "Amendment #[0-9]+"
  prohibitions:
    - "Must NOT reintroduce Success Criterion 5 or reinterpret it as a live criterion — the operator struck it (D-07), and a clean rewrite is exactly the moment a struck criterion silently comes back."
    - "Must NOT delete a superseded passage instead of relocating it — D-31 was taken against the recommendation to leave the trail inline, and the footnote is the entire compensation for that choice."
---

<objective>
Rewrite REQUIREMENTS.md's MCP-02/04/05 rows and ROADMAP.md's Phase 46 section clean, moving every
superseded wording into dated footnotes (D-31).

Purpose: both documents currently read as struck-through prose with amendment notes appended — the
ROADMAP's Phase 46 section carries roughly six SUPERSEDED paragraphs. A reader cannot tell what the
phase IS without reconstructing it from what it is not. The operator chose relocation over inline
strikethrough, against the recommendation that the falsification trail is the point; the mandatory
footnote is what makes that choice safe.

Output: two rewritten planning documents whose current text stands alone and whose history is one
hop away, dated.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md
@.planning/phases/46-mcp-trust-and-facade/46-PATTERNS.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| Relocating the falsification trail into footnotes | costly (reversible only by re-inlining, which loses the clean read) | Operator's call, recorded in D-31 with the counter-argument. Footnotes are mandatory, which is the mitigation. |
| Citing prd.md amendment numbers in planning docs | reversible | Numbers come from 46-01's SUMMARY. |
</reversibility>

## Artifacts this phase produces

Created by THIS plan: the footnote anchors in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`
(new markdown anchors, not code symbols). No Go symbols.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite REQUIREMENTS.md's MCP-02/04/05 rows clean with dated footnotes</name>
  <read_first>
    - `.planning/REQUIREMENTS.md:113-123` — the current MCP block: the `▶ Amended 2026-08-16` header note and the MCP-01..MCP-05 rows, MCP-02/04/05 of which carry `~~struck~~` text inline.
    - `.planning/REQUIREMENTS.md:131-135` — MCPC-01..05, the CLEAN row shape to imitate: `[x]`/`[ ]` checkbox, bold requirement name, plain current-state prose first, historical corrections folded in as **bolded inline parentheticals** (`**Amended YYYY-MM-DD (...)**`), never strikethrough-and-append.
    - `.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md` D-31 (the decision, its counter-argument, and the mandatory-footnote condition), D-06 and D-07 (already done — do not redo).
    - `.planning/phases/46-mcp-trust-and-facade/46-01-SUMMARY.md` — the three prd.md amendment numbers assigned in wave 1.
    - `.planning/phases/46-mcp-trust-and-facade/46-02-SUMMARY.md` — the operator's `views-exempt` / `views-drop` selection and the chosen model-facing tool names. This is the authority for how many model-facing tools each curated fork ends up with; do not restate a count from any other document.
    - `.planning/phases/46-mcp-trust-and-facade/46-PATTERNS.md` §"REQUIREMENTS.md clean-row rewrite shape (D-31)".
  </read_first>
  <action>
Before editing, extract every `~~...~~` span and every superseded clause from the MCP-02, MCP-04 and
MCP-05 rows into a scratch list. That list is the footnote content and its length is the audit
number reported in the SUMMARY.

Rewrite the three rows in MCPC-01..05's shape:

- **MCP-02** — current text first: fail-closed risk classification for unknown tools remains in force
  and is proven live; the guardrails it asserts are `mcpToolRisk`'s fail-closed default (nil
  `Annotations` -> `true, true`, escalate-only), `unsafeToRepeatBeyondAura`'s escalation, the
  model-blind approval gate, bridge namespacing plus `Registry.Register`'s panic-on-duplicate, and
  `capSchemaDescriptions`' byte caps. Then one bolded `**Amended 2026-08-16 (operator decision;
  ratified in prd.md Amendment #N)**` clause recording that result fencing was removed by
  `34b892512` and that the removal is the ratified posture, that non-MCP sources keep the nonce
  envelope, and that the counter-recommendation was put and declined. The struck `~~Per-call result
  fencing and~~` wording goes to the footnote.
- **MCP-04** — current text first: calendar, mail, contacts and WhatsApp are reachable through TWO
  curated always-loaded slots — one multiplexed tool per sidecar, plus whatever a fork deliberately
  exempts from its own merge (WhatsApp keeps its two view-bound reads, so its slot holds 3 tools) —
  never deferred, replacing the raw third-party tools, with the curation living in the forks.
  Write "slots", not "tools": "two curated tools" is no longer true of the WhatsApp side. Then the amended clause citing the
  prd.md amendment number and D-17/D-18. The struck `~~one curated comms surface — a single loaded
  slot~~` wording goes to the footnote.
- **MCP-05** — current text first: the fork stops requiring a caller-supplied account identity; its
  detail tools accept the same opaque reference its listing tools return. Then the amended clause
  citing the amendment number and D-20, keeping the measured evidence (routing hint vs. required
  opaque handle) and the RFC 8707 spec basis. The struck `~~accountId is resolved host-side...~~`
  wording goes to the footnote.

Add a `### Footnotes — superseded MCP requirement wording` subsection at the end of the
`### MCP trust and facade` block. One entry per relocated passage: `<a name="fn-mcp-02-1"></a>
**MCP-02, superseded 2026-08-16:** "<verbatim original>"`. Link each row to its footnotes with
inline markdown anchors so the trail is one click, not a search.

Leave MCP-01 and MCP-03 as they are — they already read clean and are already `[x]`.
  </action>
  <acceptance_criteria>
    - `grep -c '~~' .planning/REQUIREMENTS.md` counted over lines 113-125 only returns 0 (no strikethrough remains inside the MCP-01..MCP-05 block).
    - A `Footnotes` subsection exists inside the `### MCP trust and facade` block, and the number of footnote entries equals the number of superseded passages extracted in the scratch list; both numbers are reported in the SUMMARY.
    - Each of MCP-02, MCP-04 and MCP-05 contains at least one footnote anchor reference.
    - Each of MCP-02, MCP-04 and MCP-05 cites a prd.md amendment number matching one assigned by 46-01.
    - Every verbatim string extracted to the scratch list is findable in the file by exact substring search.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'awk "NR>=113 && NR<=126" .planning/REQUIREMENTS.md | grep -c "~~" | grep -qx 0 && grep -q "Footnotes" .planning/REQUIREMENTS.md && echo OK'</automated>
  </verify>
  <done>MCP-02/04/05 read as current state; every removed word is recoverable from a dated footnote in the same file.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ROADMAP.md's Phase 46 section clean with dated footnotes</name>
  <read_first>
    - `.planning/ROADMAP.md:283-398` — the whole Phase 46 section: the `▶ Revised 2026-08-16` header, the "What changed, measured" block, the Blocking paragraph, Depends on, Requirements, the Rationale with its two `~~SUPERSEDED~~` spans, the `~~Verify explicitly...~~` paragraph, the D-21 consequence paragraph, the TOOL-14 paragraph, the MCP-trust-question paragraph, and the six Success Criteria (SC#4 reworded, SC#5 struck).
    - `.planning/ROADMAP.md` §Phase 47 (`:400-425`) — an already-clean phase section: Goal / Depends on / Requirements / Rationale / Success Criteria / Plans, no SUPERSEDED prose. This is the target shape.
    - `.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md` D-31, D-07 (SC#5 stays struck), D-17 (the `bridgePolicy` namespace-table rationale is superseded), D-21 (the load-bearing consequence that must SURVIVE in the clean text, not be footnoted away).
    - `.planning/phases/46-mcp-trust-and-facade/46-01-SUMMARY.md` — the amendment numbers to cite.
    - `.planning/phases/46-mcp-trust-and-facade/46-02-SUMMARY.md` — the operator's `views-exempt` / `views-drop` selection. TOOL-14's worked example in this section is written FROM this file (46-01 deliberately leaves it out), so read the selection rather than assuming it.
  </read_first>
  <action>
Extract every `~~...~~` span and every paragraph marked SUPERSEDED in the Phase 46 section into a
scratch list first; that count is the audit number.

Rewrite the section in Phase 47's shape:

- **Goal** — one paragraph, current: the shipped trust posture is ratified in the PRD rather than
  re-implemented; every mounted MCP server works with zero Aura-side code and zero declaration; and
  calendar/WhatsApp collapse from raw tools into two always-loaded curated slots — one multiplexed
  tool per sidecar — curated inside the forks.
- **Depends on** — Phase 45.1 (complete, `63b456f8e`/`02a291530`) and Phase 45's
  `ValidateClassifiable` boot guard, which the two-tool merge must satisfy per D-21. Drop the
  narrowing history into a footnote.
- **Requirements** — unchanged: MCP-01, MCP-02, MCP-03, MCP-04, MCP-05, TOOL-14.
- **TOOL-14's worked example — write it HERE, and only here.** Plan 46-01's PRD amendment states the
  `<= 3` ceiling and the cap of 2 WITHOUT a per-fork tool count, because 46-01 runs in wave 1 in
  parallel with 46-02's blocking decision. This plan runs in wave 2, after that decision exists, so
  it is the first document that may state the arithmetic concretely. Read the selection from
  `46-02-SUMMARY.md` and write what it actually says — under `views-exempt`: **calendar 1 curated
  tool, WhatsApp 3 (1 curated + 2 exempted view-bound reads, `list_chats` and `list_messages`), so
  both qualify under `<= 3` and WhatsApp has zero headroom**; under `views-drop`: 1 each. Do not
  copy either form from this plan — copy it from the SUMMARY.
- **Rationale** — current only: the trust half already shipped (`34b892512`) and needs an amendment;
  the facade half moved into the forks (D-17); wrapper removal is unconditional across every mounted
  server, present and future; the residual prompt-injection risk is carried by the surviving
  guardrails and by operator control over what gets mounted. The two `~~SUPERSEDED~~` spans (the
  `bridgePolicy` namespace-table claim, the `MCP-02's two independent guardrails` phrasing) and the
  `~~Verify explicitly that trust.go's nonce-wrapped result-fencing...~~` paragraph all go to
  footnotes.
- **KEEP INLINE, do not footnote:** the D-21 load-bearing-consequence paragraph (a bridged tool never
  sets `Multiplexed`, so the guard skips it and `classify` assigns one flat tier), the TOOL-14
  paragraph, and the MCP-trust-question paragraph's live conclusion (Amendment #110 is amended, not
  restored; `prd.md:4579` untouched). These are current, not superseded.
- **Success Criteria** — renumber nothing. SC#1, #2, #3, #6 stand as written. SC#4 keeps its
  reworded current text; move its `**Reworded 2026-08-16 (D-20)**` note to a footnote. **SC#5 stays
  exactly as it is** — struck, with its `DELETED 2026-08-16 (D-07)` marker inline. Do NOT relocate
  SC#5 to a footnote and do NOT rewrite it: an inline deletion marker is what stops a future reader
  reintroducing it.
- **Plans** — replace `TBD` with the plan list produced by this phase (nine entries,
  `46-01-PLAN.md` .. `46-09-PLAN.md`, each with a one-line objective).

Add a `#### Footnotes — superseded Phase 46 wording` subsection at the end of the section, one dated
entry per relocated passage, anchored and referenced from the paragraph it came from.
  </action>
  <acceptance_criteria>
    - Within the Phase 46 section, the only remaining `~~` strikethrough is Success Criterion 5's, and it still carries the literal text `DELETED 2026-08-16 (D-07)`.
    - The word `SUPERSEDED` no longer appears in the section body; every occurrence has moved into the Footnotes subsection.
    - A `Footnotes` subsection exists in the Phase 46 section with one entry per extracted passage; the extracted count and the footnote count are equal and both reported in the SUMMARY.
    - The D-21 consequence paragraph is still present inline (grep for `Multiplexed` inside the section returns >= 1 hit outside the footnotes).
    - The `**Plans**:` line lists nine plan files, not `TBD`.
    - Success Criteria still number 1 through 6 with SC#5 present-but-struck.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'sed -n "/^### Phase 46:/,/^### Phase 47:/p" .planning/ROADMAP.md > /tmp/p46.md; grep -q "DELETED 2026-08-16 (D-07)" /tmp/p46.md && grep -q "Footnotes" /tmp/p46.md && grep -q "46-09-PLAN.md" /tmp/p46.md && echo OK'</automated>
  </verify>
  <done>ROADMAP §46 reads as one current description of the phase; the six SUPERSEDED paragraphs are dated footnotes; SC#5 is still visibly struck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical record -> future reader | A clean rewrite is the moment a falsified claim can silently return, or a struck criterion can be quietly reinterpreted as live. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-08 | Repudiation | The relocated falsification trail | mitigate | Extract-then-count: the number of passages removed must equal the number of footnotes added, verified by an explicit substring re-find of every extracted string, and reported in the SUMMARY. |
| T-46-09 | Tampering | ROADMAP Success Criterion 5 | mitigate | SC#5 is explicitly excluded from relocation and must remain inline-struck with its D-07 marker; the acceptance criteria grep for that exact marker. |
| T-46-10 | Spoofing | Amendment numbers cited in planning docs | mitigate | Numbers are read from 46-01's SUMMARY, which recorded what was actually written to prd.md, not from any planning document's prediction. |
| T-46-SC | Tampering | package installs | mitigate | No package installs in this phase; nothing to gate. |
</threat_model>

<verification>
- `git diff --stat` shows only `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
- Extracted-passage count equals footnote count in both files.
</verification>

<success_criteria>
Both planning documents describe Phase 46 as it is, and every superseded word is still recoverable,
dated, in the same file.
</success_criteria>

<output>
Create `.planning/phases/46-mcp-trust-and-facade/46-03-SUMMARY.md` when done, reporting the
extracted/footnote counts for both files.
</output>
