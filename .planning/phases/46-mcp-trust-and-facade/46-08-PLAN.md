---
phase: 46-mcp-trust-and-facade
plan: 08
type: execute
wave: 5
depends_on: ["46-07"]
files_modified:
  - "EXTERNAL chetto1983/whatsapp-mcp@aura/cockpit-connect: whatsapp-mcp-server/main.py"
  - internal/agent/mcptools/bridge_multiplex.go
  - internal/agent/mcptools/bridge_multiplex_test.go
  - internal/agent/mcptools/bridge_risk.go
  - internal/agent/mcptools/bridge_risk_test.go
  - internal/gateway/classify.go
  - internal/gateway/classify_multiplexed_comms_test.go
  - internal/mcp/whatsapp_integration_test.go
  - compose.yaml
autonomous: true
requirements: [MCP-04]

must_haves:
  truths:
    - "The whatsapp-mcp fork's aura/cockpit-connect branch advertises exactly ONE model-facing MCP tool whose action enum equals the design doc's WhatsApp action list."
    - "The raw @mcp.tool() handlers are deleted from main.py, not merely undecorated, and whatsapp.py's implementation layer is unchanged."
    - "trustedRecipeActions[whatsAppRecipeSource] is keyed by ACTION name and its key set equals the served action enum exactly — no entry describing an action the mounted branch cannot serve."
    - "classify of the curated WhatsApp tool returns Safe for a read action, Normal for download_media, Destructive for send_message, and Risky for an unrecognised action."
    - "The two curated model-facing names are distinct and neither shadows a built-in; a duplicate registration is a boot panic from Registry.Register, never a silent shadow."
    - "MultiplexedMCPTools() now returns two names and every one still has exactly one multiplexedClassifiers entry — the invariant test passes unchanged, without being widened by hand."
    - "compose.yaml's AURA_WHATSAPP_MCP_IMAGE default is the immutable ghcr.io/chetto1983/whatsapp-mcp:<40-hex-sha> tag, and it lands in the same commit as the WhatsApp table re-key and classifier entry."
    - "Both curated servers now hold the two always-loaded slots: each exposes exactly 1 model-facing tool, so both qualify under the <= 3 ceiling and the 2-slot budget is exactly spent."
    - "The curated WhatsApp tool's description is under 2,048 bytes, measured, against a 4,096-byte host cap."
  artifacts:
    - path: "internal/agent/mcptools/bridge_multiplex.go"
      provides: "The second entry in multiplexedMCPTools and the WhatsApp curated name constant"
      contains: "WhatsApp"
    - path: "compose.yaml"
      provides: "The immutable WhatsApp sidecar pin"
      contains: "ghcr.io/chetto1983/whatsapp-mcp"
  key_links:
    - from: "internal/gateway/classify.go multiplexedClassifiers"
      to: "mcptools.WhatsAppMultiplexedToolName"
      via: "second MCP entry added to the same map, keyed by the namespaced name"
      pattern: "WhatsAppMultiplexedToolName"
  prohibitions:
    - "Must NOT leave the raw @mcp.tool() handlers callable-but-unadvertised — they stay reachable on the sidecar's HTTP port, which Aura's namespacing does not protect."
    - "Must NOT leave a risk-table entry for an action the mounted branch cannot serve; a table describing a server that is gone is exactly the drift this phase measured and must not re-create."
---

<objective>
Repeat the proven pattern for the second sidecar: curate `whatsapp-mcp` into one multiplexed
`messages`-shaped tool, re-key its half of the risk table, register its classifier, and pin its
image — in one atomic commit per D-32's per-source rule.

Purpose: MCP-04 needs BOTH sidecars collapsed. This runs after the tracer (46-07) deliberately: the
mechanism is now validated live, so this plan is an application of a known-good pattern rather than a
second experiment.

**Carries a resolved open question.** 46-RESEARCH.md measured that
`trustedRecipeActions[whatsAppRecipeSource]` lists 14 actions while the mounted branch
`aura/cockpit-connect` registers only 12 `@mcp.tool()` functions — `get_contact` and `send_reaction`
exist on the fork's `main` and were never merged onto the Aura branch. The operator resolved this in
plan 46-02's checkpoint. This plan implements that selection; it does not re-decide it.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-mcp-trust-and-facade/46-CONTEXT.md
@.planning/phases/46-mcp-trust-and-facade/46-RESEARCH.md
@docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md
@.planning/phases/46-mcp-trust-and-facade/46-06-SUMMARY.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| The WhatsApp action set (12 vs 14) | **one-way** | Already taken by the operator in 46-02's `checkpoint:decision`, with the alternative and its cost recorded. |
| Deleting the raw `@mcp.tool()` names | **one-way** | D-26; blast radius recorded in this plan's SUMMARY for Phases 47/48. |
| The `:<sha>` pin | reversible | D-23. |
</reversibility>

## Artifacts this phase produces

Created by THIS plan (new symbols — not drift): the curated WhatsApp tool in the fork and its action
enum; `ghcr.io/chetto1983/whatsapp-mcp:<40-hex-sha>`; Aura-side `WhatsAppMultiplexedToolName`, the
second `multiplexedMCPTools` entry, `classifyWhatsAppAction`, and the second
`multiplexedClassifiers` entry.

<tasks>

<task type="auto">
  <name>Task 1: Curate the WhatsApp fork per the operator's selected scope, and publish</name>
  <read_first>
    - `docs/superpowers/specs/2026-08-17-mcp-curated-surface-design.md` §5 (the WhatsApp action table and its risk classes), §4 (edit site), §6 (the model-facing name), §10 (the operator's 12-vs-14 selection, verbatim).
    - `.planning/phases/46-mcp-trust-and-facade/46-RESEARCH.md` §"The finding CONTEXT.md did not have" (the per-branch `@mcp.tool()` counts: `aura/cockpit-connect` 12, `main` 14) and §"Pitfall 2" (building the action set from the Go table produces a tool advertising actions the branch cannot serve, which fails at call time with a method error).
    - In the freshly cloned fork: `whatsapp-mcp-server/main.py` (the `@mcp.tool()` decorators — the registration layer) and `whatsapp-mcp-server/whatsapp.py` (the implementations each wrapper calls — NOT to be changed).
    - `internal/agent/tools/task.go:104-125` (Aura tree) — the flat-union schema shape to mirror in Python.
    - `internal/agent/tools/testdata/deferred_manifest.json` — the current `whatsapp__*` descriptions (7,578 bytes total), the number this plan's merged description must come in far under.
    - `.planning/phases/46-mcp-trust-and-facade/46-05-SUMMARY.md` — how the calendar fork's publish went, including any branch-protection or CI surprise worth pre-empting here.
  </read_first>
  <action>
`git clone https://github.com/chetto1983/whatsapp-mcp` outside this repo; check out
`aura/cockpit-connect`. Re-verify the current `@mcp.tool()` count with a fresh read before editing —
research is days old and this branch already drifted once.

Execute the operator's selection from 46-02:
- If **merge-14** was chosen: cherry-pick or merge `get_contact` and `send_reaction` from `main`
  onto `aura/cockpit-connect` FIRST, as a separate commit, and confirm the fork's tests still pass
  before curating. If the merge proves non-trivial, STOP and report — do not silently fall back to
  12, because the Go table's shape depends on which was chosen.
- If **scope-12** was chosen: curate exactly the 12 actions the branch serves, and note that task 2
  must prune `get_contact` and `send_reaction` from the Go table in the same Aura-side commit.

Then collapse the registration layer: replace the `@mcp.tool()` decorated wrappers with ONE decorated
dispatcher carrying the `action` argument, switching to the same `whatsapp.py` functions the wrappers
called. `whatsapp.py` does not change. Schema: flat object, `required: ["action"]` only, `action`
with a property-level enum of exactly the selected action names, every other field optional with its
per-action requirement in that field's description, no root `oneOf`. Typed identifiers only —
`chatId`, `messageId`, `contactId` — never a bare `id`.

DELETE the raw handlers (D-26), do not merely undecorate them: an undecorated-but-present function
that anything can still reach through the sidecar's port is dark code.

Description budget: preamble plus one line per action naming its required fields, target ~1.5-2KB.
Measure the byte length before committing — the existing per-tool text totals 7,578 bytes and the
host caps at 4,096, and this tool is always-loaded so every byte is paid every turn.

Commit, push to `aura/cockpit-connect`, watch `ci.yml` to green, confirm `aura-publish-image.yml`
produced both tags, then PULL the `:<sha>` image and issue a real `tools/list` against a container
started from it. Record the fork SHA, the image tag, both workflow run URLs, the measured description
bytes, and the served action enum.
  </action>
  <acceptance_criteria>
    - `grep -c "@mcp.tool()" whatsapp-mcp-server/main.py` in the fork returns 1.
    - `git diff` in the fork shows no hunk in `whatsapp-mcp-server/whatsapp.py` (implementation layer untouched) other than any required by the merge-14 option, if selected.
    - `gh run list --repo chetto1983/whatsapp-mcp --branch aura/cockpit-connect` shows both workflows `success` for the pushed SHA.
    - A `tools/list` against a container from the pulled `:<sha>` image returns exactly one tool whose action enum equals the selected list exactly.
    - The measured description byte count is < 2048 and is reported in the SUMMARY.
    - No schema argument is named `id`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'gh run list --repo chetto1983/whatsapp-mcp --branch aura/cockpit-connect --limit 5 --json name,conclusion,headSha'</automated>
  </verify>
  <done>The WhatsApp sidecar serves one curated tool, the raw handlers are gone, and an immutable tag exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-key the WhatsApp risk table, register the second classifier, and prove the invariant still holds</name>
  <read_first>
    - `internal/agent/mcptools/bridge_multiplex.go` as written by plan 46-06 — the `multiplexedMCPTools` map, `MCPActionClassFor`, `MultiplexedMCPTools`, `isKnownMultiplexedMCPTool`, `reconcileCuratedActions`, and the calendar name constants whose shape the WhatsApp ones mirror.
    - `internal/agent/mcptools/bridge_risk.go:42-58` — `trustedRecipeActions[whatsAppRecipeSource]`, still raw-tool-name keyed after 46-06, plus the mixed-key comment 46-06 added (which this task extends to name WhatsApp as action-keyed too).
    - `internal/gateway/classify.go` as written by plan 46-06 — `classifyCalendarAction` and the map entry, the exact shape `classifyWhatsAppAction` mirrors.
    - `internal/gateway/classify_multiplexed_comms_test.go` — the table-driven cases and `TestEveryMultiplexedMCPToolHasAClassifier`, which must pass with two entries WITHOUT being edited to accommodate the second one.
    - `internal/agent/mcptools/bridge_risk_test.go` — the WhatsApp cases in `TestTrustedRecipeRiskPolicyIsGraduated` and `TestMCPToolRiskRecipeTablesUnchanged`, whose hard-coded expectations must be re-keyed to action names with the same tiers.
    - `internal/mcp/manager/catalog.go:137-146` — the WhatsApp mount entry, `Name: "whatsapp"`, the namespace the model-facing name is built from.
    - `.planning/phases/46-mcp-trust-and-facade/46-08` task 1's SUMMARY output — the served action enum, which is the authority for the table's key set.
  </read_first>
  <behavior>
    - `MCPActionClassFor(<whatsapp curated name>, "list_messages")` -> `MCPActionRead`.
    - `MCPActionClassFor(<whatsapp curated name>, "download_media")` -> `MCPActionMutate`.
    - `MCPActionClassFor(<whatsapp curated name>, "send_message")` -> `MCPActionDestructive`.
    - `MCPActionClassFor(<whatsapp curated name>, "nope")` -> `MCPActionUnknown`.
    - `classify(curatedWhatsAppSpec, {"action":"list_chats"})` -> `scoring.Safe`; `{"action":"send_message"}` -> `scoring.Destructive`; `{"action":"nope"}` / `{}` / malformed -> `scoring.Risky`.
    - `MultiplexedMCPTools()` returns exactly two names, sorted, both containing `__`, and they are not equal to each other.
    - `TestEveryMultiplexedMCPToolHasAClassifier` passes with two entries and fails if either the map entry or the classifier is removed.
    - `trustedRecipeActions[whatsAppRecipeSource]`'s key set equals the served enum exactly — a key with no served action, or a served action with no key, fails.
    - Registering two tools that would produce the same model-facing name panics via `Registry.Register`; the curated names are distinct so the live registry boots.
  </behavior>
  <action>
Tests first, red.

In `bridge_multiplex.go`: add `const whatsAppCuratedTool = "<the fork tool name>"`,
`const whatsAppNamespace = "whatsapp"`, `var WhatsAppMultiplexedToolName = namespacedName(
whatsAppNamespace, whatsAppCuratedTool)`, and the second entry in `multiplexedMCPTools` mapping it to
`whatsAppRecipeSource`. Extend the catalog-agreement test to cover the WhatsApp entry's `Name`.

In `bridge_risk.go`: re-key `trustedRecipeActions[whatsAppRecipeSource]` to action names, preserving
each action's existing class exactly. If the operator chose scope-12, DELETE the `get_contact` and
`send_reaction` entries in this same commit — a table describing a server that is gone is the drift
this phase measured. Extend the mixed-key comment so it now names calendar AND whatsapp as
action-keyed and memory as raw-tool-name keyed.

In `classify.go`: add `classifyWhatsAppAction` mirroring `classifyCalendarAction` exactly (same
unmarshal-or-Risky, same class->tier switch, same `default: scoring.Risky`), and register it under
`mcptools.WhatsAppMultiplexedToolName`. Do not generalize the two classifiers into one parameterized
helper unless it stays equally readable — two five-line functions that each name their tool are
clearer than one that takes the tool as a parameter, and the map already provides the indirection.
If they ARE folded, the folded form must still be registered under two distinct keys.

Add a test asserting the two curated model-facing names are distinct and that a deliberate duplicate
registration panics through `Registry.Register` — the adjacency case where two curated tools would
collide.
  </action>
  <acceptance_criteria>
    - `go test ./internal/gateway/ -run 'TestClassifyWhatsApp|TestEveryMultiplexed' -v` passes and `TestEveryMultiplexedMCPToolHasAClassifier` was NOT edited to accommodate the second entry (`git diff` shows no change to that function body).
    - `trustedRecipeActions[whatsAppRecipeSource]`'s key set equals the served enum from task 1, asserted by a test that fails on either an extra or a missing key.
    - If scope-12 was chosen, `grep -c "get_contact\|send_reaction" internal/agent/mcptools/bridge_risk.go` returns 0.
    - `MultiplexedMCPTools()` returns 2 elements, asserted.
    - `go test -race ./internal/agent/mcptools/... ./internal/gateway/...` green.
    - `go vet ./...`, `go build ./...` green; both touched files <= 600 lines.
  </acceptance_criteria>
  <verify>
    <automated>go test -race ./internal/agent/mcptools/... ./internal/gateway/...</automated>
  </verify>
  <done>Both curated tools are known, classified per action from the one table, and the plural invariant holds without hand-widening.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the WhatsApp image, extend its integration tier, and land the second atomic commit</name>
  <read_first>
    - `compose.yaml:814-822` — the `whatsapp` service block, `image: ${AURA_WHATSAPP_MCP_IMAGE:-ghcr.io/chetto1983/whatsapp-mcp:sidecar}` and `pull_policy: ${AURA_WHATSAPP_MCP_PULL_POLICY:-missing}`.
    - `internal/mcp/whatsapp_integration_test.go` in full — the `//go:build whatsapp_integration` tag, `whatsappConfigOrGate` (line 23, including its no-skip-as-green `t.Fatal` under `$CI`), `TestWhatsAppServerLive` (39) and `hasWhatsAppTool` (68), the helper whose per-name checks the curated surface makes obsolete.
    - `internal/mcp/calendar_integration_test.go` as rewritten by plan 46-06 — the exactly-one-tool assertion shape to mirror.
    - `.planning/phases/46-mcp-trust-and-facade/46-08` task 1's SUMMARY — the `:<sha>` tag.
    - `internal/agent/mcptools/bridge_deferral.go` — the 2-slot budget; with both sidecars now at 1 model-facing tool each, the budget is exactly spent, and the INFO log line records which two servers hold it.
  </read_first>
  <action>
Set `compose.yaml`'s `AURA_WHATSAPP_MCP_IMAGE` default to the exact
`ghcr.io/chetto1983/whatsapp-mcp:<40-hex-sha>` tag from task 1, and update the adjacent comment the
way 46-06 did for the PIM sidecar.

Rewrite `TestWhatsAppServerLive`'s surface assertion: exactly ONE advertised model-facing tool, its
name equal to the curated name, and its `action` enum equal to the selected list exactly. Replace or
delete `hasWhatsAppTool` if the per-name helper no longer has a caller — an unused helper is dead
code. Keep `whatsappConfigOrGate` verbatim.

**Land ONE commit** containing task 2's table re-key + classifier + tests and this task's pin and
integration-test rewrite. Same reasoning as 46-06: a commit with the WhatsApp table re-keyed but the
pin unflipped makes every WhatsApp read demand approval; the reverse classifies the merged tool flat.

Run, in order: `go vet ./...`, `go build ./...`,
`go test ./internal/agent/mcptools/... ./internal/gateway/...`, the `-race` variant, then with the
stack up on both new pins:
`go test -tags whatsapp_integration ./internal/mcp/ -run TestWhatsAppServerLive -v` and
`go test -tags calendar_integration ./internal/mcp/ -run TestCalendarServerLive -v` (the calendar
tier must still pass — both sidecars now share the 2-slot budget).

Confirm from the slot-grant INFO log that both curated servers hold a loaded slot and that the budget
is exactly spent; record the log lines.

Record in the SUMMARY the COMPAT blast radius for the WhatsApp half:
`SELECT count(*) FROM aura.tool_invocations WHERE tool_name LIKE 'whatsapp\_\_%'`, plus any paused
approval or scheduled `agent_job` referencing those names, for the Phase 47/48 handoff.
  </action>
  <acceptance_criteria>
    - `grep -n "AURA_WHATSAPP_MCP_IMAGE" compose.yaml` shows a default matching `ghcr.io/chetto1983/whatsapp-mcp:[0-9a-f]{40}` equal to task 1's SHA.
    - `go test -tags whatsapp_integration ./internal/mcp/ -run TestWhatsAppServerLive -v` passes against the pinned image with runtime > 0.5s and asserts exactly one advertised tool.
    - `go test -tags calendar_integration ./internal/mcp/ -run TestCalendarServerLive -v` still passes.
    - `grep -c "hasWhatsAppTool" internal/mcp/whatsapp_integration_test.go` returns 0 if the helper lost its last caller.
    - `git log -1 --stat` shows ONE commit containing `bridge_multiplex.go`, `bridge_risk.go`, `classify.go`, `compose.yaml` and `whatsapp_integration_test.go`.
    - The slot-grant log lines are quoted in the SUMMARY and show two grants and a spent budget.
    - The SUMMARY quotes the measured `whatsapp__%` row count.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -Eq "ghcr.io/chetto1983/whatsapp-mcp:[0-9a-f]{40}" compose.yaml && go test ./internal/agent/mcptools/... ./internal/gateway/... && echo OK'</automated>
  </verify>
  <done>Both sidecars are curated, pinned, classified per action, and hold the two always-loaded slots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fork branch state -> curated surface | The Go table was written against a 14-action surface the mounted branch never had; the branch is the authority, not the table. |
| Two curated tools -> one namespace-flat registry | Two servers now contribute always-loaded tools; a name collision would silently shadow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-30 | Denial of Service | A curated action the mounted branch cannot serve (the measured 12-vs-14 drift) | mitigate | The operator's selection is executed explicitly; the table's key set is asserted equal to the SERVED enum, so a phantom action fails the test rather than the live call. |
| T-46-31 | Elevation of Privilege | Per-action tiers flattening for the second tool | mitigate | The same `Multiplexed` + classifier mechanism the tracer proved, plus `TestEveryMultiplexedMCPToolHasAClassifier` which must pass UNEDITED with two entries — a second server cannot be added while forgetting its classifier. |
| T-46-32 | Spoofing | Two mounted servers producing the same model-facing name | mitigate | Namespacing plus `Registry.Register`'s panic-on-duplicate; a test asserts the two curated names are distinct and that a deliberate collision panics. Fail-closed on wiring is the established rule. |
| T-46-33 | Tampering | Cross-repo drift in the second fork | mitigate | Immutable `:<sha>` pin plus `reconcileCuratedActions`' two-directional mount-time WARN; unknown action saturates to Risky at call time. |
| T-46-34 | Elevation of Privilege | Both always-loaded slots consumed, crowding out a future server | **accept** | By design (D-27): the budget is a hard constant of 2 and overflow fails closed to deferred-but-discoverable, which still satisfies the promise made to an unknown server. Phase 48 owns the total manifest budget. |
| T-46-SC | Tampering | package installs | mitigate | No package installs in this phase; nothing to gate. |
</threat_model>

<verification>
- `go test ./internal/agent/mcptools/... ./internal/gateway/...` and the `-race` variant.
- Both tagged integration tiers pass against both pinned images, each with a non-trivial runtime.
- `bash scripts/coverage_docker.sh` after this wave.
</verification>

<success_criteria>
Calendar and WhatsApp are each one curated, always-loaded, per-action-classified tool behind an
immutable pin, and the plural invariant is enforced by a test nobody had to widen.
</success_criteria>

<output>
Create `.planning/phases/46-mcp-trust-and-facade/46-08-SUMMARY.md` when done, recording the fork SHA,
the image tag, the selected action list, and the `whatsapp__%` blast-radius count.
</output>
