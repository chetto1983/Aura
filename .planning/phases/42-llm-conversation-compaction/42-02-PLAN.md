---
phase: 42-llm-conversation-compaction
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified: [internal/db/migrations/0036_compaction_checkpoints.up.sql, internal/db/migrations/0036_compaction_checkpoints.down.sql, internal/db/queries/compaction.sql, internal/conversations/compaction_claims.go, internal/conversations/store_compaction.go, internal/conversations/compaction_claims_multiprocess_test.go, cmd/compaction-test-worker/main.go, internal/conversations/compaction_manifest.go, internal/conversations/compaction_reconstruct.go]
autonomous: true
requirements: [IC-04, IC-05, IC-11, IC-14]
must_haves:
  truths: ["Claims are durable and inference never holds a transaction", "Only CAS-selected branch checkpoints reconstruct", "Recovery is bounded and rollback-compatible"]
  artifacts:
    - {path: internal/db/migrations/0036_compaction_checkpoints.up.sql, provides: "additive claim/checkpoint/pointer/restore/quarantine schema"}
    - {path: internal/conversations/store_compaction.go, provides: "claim and serializable CAS finalize store"}
    - {path: internal/conversations/compaction_reconstruct.go, provides: "pure versioned reconstruction and recovery"}
  key_links:
    - {from: internal/conversations/compaction_claims.go, to: internal/conversations/store_compaction.go, via: "stable operation ID and lease"}
    - {from: internal/conversations/compaction_reconstruct.go, to: internal/conversations/compaction_manifest.go, via: "RFC8785 SHA-256 v1 verification"}
---
<objective>Complete slice 1 persistence: additive branch-aware schema, distributed claim protocol, immutable manifests, CAS activation, bounded recovery, and rollback proof.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@internal/conversations/store_append.go
@internal/canonicaljson
@.planning/phases/42-llm-conversation-compaction/42-01-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Add additive checkpoint schema and durable claim/CAS protocol</name>
  <files>internal/db/migrations/0036_compaction_checkpoints.up.sql, internal/db/migrations/0036_compaction_checkpoints.down.sql, internal/db/queries/compaction.sql, internal/conversations/compaction_claims.go, internal/conversations/store_compaction.go, internal/conversations/compaction_claims_multiprocess_test.go, cmd/compaction-test-worker/main.go</files>
  <action>Create additive tables for claims, immutable branch generations, ordered manifests/digests, one active pointer, restore audit, and quarantine. Enforce unique conversation/branch/generation and idempotency keys, parent FK/adjacency, states, pointer identity, and retention metadata. Generate one durable operation ID before claim; short claim transaction, inference outside locks, short serializable finalize checking pending lease, base pointer, and governance watermark before atomic checkpoint insert/claim completion/pointer CAS. Uniqueness returns existing outcome; stale completion supersedes. Restore contends on the same pointer. Down migration removes only new objects; migration golden fixtures prove old rows remain readable. Follow the PATTERNS durable-claim analogs `store_append.go`, `askuser/store.go`, `saga_journal.go`, and scheduler lease stores for short tx/sqlc/error shapes, while implementing the RESEARCH Pattern 4 and Explicit No-Analog distributed claim/CAS contract rather than adding a process-local lock. Add a compiled `cmd/compaction-test-worker` subprocess fixture: the parent test allocates independent DB pools, starts workers with a shared operation/branch and barrier/phase arguments, synchronizes claim-acquired/inference-ready/finalize/restore phases over stdin/stdout JSON lines, advances an injectable database clock for lease expiry, and kills one worker with `os.Process.Kill` after durable claim but before finalize. Named tests must cover duplicate operation across processes, expired-lease takeover, killed-owner recovery, stale completion after takeover, and restore-vs-finalize CAS, asserting one active pointer and auditable loser outcomes.</action>
  <verify><automated>go test -tags=db_integration ./internal/conversations -run 'TestCompactionMultiProcessDuplicate|TestCompactionMultiProcessLeaseExpiry|TestCompactionMultiProcessProcessDeath|TestCompactionMultiProcessStaleCompletion|TestCompactionMultiProcessRestoreRace|TestCompactionMultiProcessGovernanceAfterCaptureInvalidatesFinalize|TestCompactionMultiProcessManualOutranksUnstartedAuto|TestCompactionMultiProcessManualDoesNotPreemptActiveInference|TestMigration0036' -count=1</automated></verify>
  <done>Independent connections prove duplicate/retry/lease/process-death/stale/governance/restore/manual-priority outcomes with no inference inside transaction.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement canonical manifests, reconstruction, and bounded recovery</name>
  <files>internal/conversations/compaction_manifest.go, internal/conversations/compaction_reconstruct.go</files>
  <behavior>RFC8785+SHA256 byte-stable fixtures classify each captured turn once and reconstruct L0, always-block, internal summary, tail order, and post-watermark; recovery follows active, compatible LKG, bounded rebuild, context_unavailable.</behavior>
  <action>Persist digest algorithm/version and immutable fields. Dispatch readers by stored schema/projection version; reject mismatches and quarantine corrupt checkpoint/artifact records. Reconstruct solely through active pointer, never timestamp. Add budget/version restore preview and transactional activation audit. Bounded canonical rebuild reuses the selector and never injects the full oversized transcript. Keep prior pointer active on all failures. Follow PATTERNS analogs `internal/canonicaljson/*`, `internal/skills/manifest.go`, and `internal/agui/recovery_hash.go` for versioned parse/validate/hash boundaries, and implement the RESEARCH Patterns 1-2 no-analog version-dispatch and exactly-once manifest contract explicitly.</action>
  <verify><automated>go test ./internal/conversations -run 'Manifest|Reconstruct|Recovery|RestorePreview|Quarantine' -count=1</automated></verify>
  <done>Repeated reconstruction is byte-stable, branch-isolated, exactly-once, and every recovery failure is non-destructive.</done>
</task>
</tasks>
<verification>go test -race -tags=db_integration ./internal/conversations -run 'Compaction|Reconstruct|Recovery|Restore' -count=1</verification>
<success_criteria>IC-04/05/11 schema, concurrency, recovery, migration, and rollback contracts pass with activation disabled.</success_criteria>
<output>Create `42-02-SUMMARY.md`; one atomic migration/store commit and one reconstruction/recovery commit.</output>
