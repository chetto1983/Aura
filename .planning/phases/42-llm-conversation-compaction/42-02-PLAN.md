---
phase: 42-llm-conversation-compaction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/conversations/compact.go
  - internal/conversations/title.go
  - internal/conversations/compact_test.go
  - docs/compact_prompt.md
  - .planning/milestones/v0.0.0-phases/04-hitl-identity-conversations/04-SPEC.md
  - prd.md
autonomous: true
requirements: [COMPACT-01, COMPACT-02]
must_haves:
  truths:
    - "COMPACT-01: CompactConversation with a stub llm.Client returning a canned summary populates CompactResult.Summary; TokensBefore == tiktoken count of the rendered input history; TokensAfter == tiktoken count of the summary; the trailing chunk's *llm.Usage is captured in CompactResult.Usage"
    - "COMPACT-01 empty guard: a stub returning empty/whitespace text → error, and CompactConversation performs NO persistence side effect (it never touches the store — persistence is the caller's job)"
    - "COMPACT-01 focus hook: opts.Focus=\"focus on the failing test\" appears verbatim in the outbound Request.Messages[0].Content (appended as the Compact Instructions hook)"
    - "COMPACT-01 stream drain: CompactConversation drains the client.Stream channel fully (interface contract — a partial drain leaks); MaxTokens == opts.MaxOutputTokens (not 32)"
    - "COMPACT-02 (D-06): compactSystemPrompt is a package-level const containing ALL 9 section headers (Primary Request/Intent, Key Technical Concepts, Files & Code Sections, Problem Solving, Pending Tasks, Current Work, Optional Next Step, All user messages, Errors and fixes)"
    - "COMPACT-02 governance (D-06): compactSystemPrompt contains the verbatim-preservation-of-user-stated-security/safety-constraints instruction AND the 'reply in TEXT ONLY, call no tools' guard AND the 'Reply in English only' clause"
    - "COMPACT-02 provenance: docs/compact_prompt.md gains a header note pointing at the compactSystemPrompt constant (it is retained as the OLDER-7-section provenance reference, not the source of truth)"
    - "D-07 doc activation: 04-SPEC.md's L3 deferral entry and prd.md §1.8 OQ#3 each gain a one-line 'activated in Phase 42' note (documentation only — NOT a PRD-amendment)"
  artifacts:
    - path: "internal/conversations/compact.go"
      provides: "CompactConversation, CompactOptions{MaxOutputTokens,Focus}, CompactResult{Summary,TokensBefore,TokensAfter,Usage}, the 9-section compactSystemPrompt const, render/sanitize helpers"
      contains: "func CompactConversation"
    - path: "internal/conversations/compact.go"
      provides: "compactSystemPrompt package const — 9-section governance-hardened schema (D-06)"
      contains: "compactSystemPrompt"
    - path: "docs/compact_prompt.md"
      provides: "header note pointing at the compactSystemPrompt constant (provenance reference)"
      contains: "compactSystemPrompt"
  key_links:
    - from: "internal/conversations/compact.go CompactConversation"
      to: "llm.Client.Stream"
      via: "2-message llm.Request (compactSystemPrompt system + rendered history), drained fully"
      pattern: "client.Stream"
    - from: "internal/conversations/compact.go"
      to: "internal/conversations/tiktoken.go countTokens"
      via: "TokensBefore/TokensAfter gating-grade counts via encoder()"
      pattern: "countTokens"
  prohibitions:
    - "MUST NOT template compactSystemPrompt per call — it is a package const; the ONLY per-call variation is the appended opts.Focus (SPEC Req#2 acceptance). Verify: source shows a single `const compactSystemPrompt`, no per-call fmt.Sprintf of the schema."
    - "MUST NOT persist anything from compact.go — CompactConversation returns a CompactResult; RecordCompaction (Plan 42-01) is the caller's persistence. Verify: compact.go imports no db/sqlc, calls no store method."
    - "MUST NOT discard the Usage — unlike generateTitle (which drops it), CompactConversation accumulates the trailing chunk's *llm.Usage into CompactResult (a compaction is a real billable call, COMPACT-10). Verify: unit asserts CompactResult.Usage is non-nil for a stub emitting a Usage chunk."
    - "MUST NOT edit 04-SPEC.md / prd.md beyond the one-line activation note — D-07 is documentation, not a PRD-amendment (no architectural change). Verify: the diff on those two files is additive one-liners only."
    - "MUST NOT drop the English-only clause — compactSystemPrompt keeps the 'Reply in English only' rule (matches titlePrompt, project convention). Verify: COMPACT-02 test asserts the clause is present."
---

<objective>
Build the single bounded LLM summarization call and its governance-hardened prompt — the semantic heart of compaction. `CompactConversation` mirrors the proven `generateTitle` shaping (2-message request, full stream drain, explicit `model string`) but keeps the `Usage`, bounds `MaxTokens` to the summary budget, and threads an optional `opts.Focus` hook (COMPACT-01). The `compactSystemPrompt` constant adopts the newer 9-section Claude Code schema with verbatim security/safety-constraint preservation and a no-tools guard (COMPACT-02 / D-06) — the phase's headline defense against Governance Decay. Plus the D-07 documentation housekeeping so the truth-source stops asserting "L3 NOT implemented" after ship.

Purpose: COMPACT-01 (bounded summarization call) and COMPACT-02 (the adapted 9-section prompt) are pure, daemon-free, floor-critical surface — a stub `llm.Client` covers every path, so this whole plan contributes to the ≥85% owned-surface floor with zero DB. D-06's verbatim-safety-constraint clause + the "TEXT ONLY, call no tools" guard are load-bearing security controls: the summary is re-injected every subsequent turn, so a poisoned or tool-calling summary is a real attack surface (RESEARCH Security Domain / Governance Decay).
Output: `internal/conversations/compact.go` (+ the possible title.go dupl-fold), `compact_test.go`, the `docs/compact_prompt.md` header note, and the two D-07 one-line doc notes.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-VALIDATION.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
@.claude/skills/golang-security/SKILL.md
@.claude/skills/golang-concurrency/SKILL.md
</context>

<artifacts_produced>
This plan adds these NEW symbols (exclude from source-grounding drift verification — net-new):
- `compact.go` — `CompactConversation(ctx context.Context, client llm.Client, model string, history []llm.Message, opts CompactOptions) (CompactResult, error)`; `CompactOptions{MaxOutputTokens int; Focus string}`; `CompactResult{Summary string; TokensBefore int; TokensAfter int; Usage *llm.Usage}`; `compactSystemPrompt` const; a `renderHistoryForCompaction` helper (or a shared `renderHistory` if the dupl-fold applies).
- `docs/compact_prompt.md` — a header note.
- `04-SPEC.md` + `prd.md` — one-line D-07 activation notes.
Consumes: nothing from other Phase-42 plans (Wave 1). Note: Plan 42-04 (Runner.Compact) is the caller that persists the CompactResult via Plan 42-01's RecordCompaction — this plan produces the result only.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compact.go — CompactConversation + 9-section compactSystemPrompt (COMPACT-01/02, D-06)</name>
  <files>internal/conversations/compact.go, internal/conversations/title.go, internal/conversations/compact_test.go</files>
  <read_first>
    - internal/conversations/title.go:14 (titlePrompt — the English-only package-const precedent), :26 (GenerateTitle exported wrapper), :39-65 (generateTitle — the 2-message llm.Request, `for chunk := range ch` full drain, explicit model, empty→error), :70-95 (renderHistoryForTitle — first-6-turns/500-cap render), :99-107 (sanitizeTitle). This is the exact structural template (42-PATTERNS.md § compact.go).
    - internal/conversations/tiktoken.go:90 (countTokens) + encoder() — the L2/L2.5 gating-grade cl100k_base encoder for TokensBefore/TokensAfter.
    - internal/llm — the llm.Request (Model, Messages, Temperature, MaxTokens), llm.Message (Role, Content), llm.Chunk (Text + trailing *Usage), llm.Usage (InputTokens/OutputTokens/CachedTokens/CostUSD) shapes; RoleSystem/RoleUser.
    - the title.go stub-client test (title_unit_test.go — TestGenerateTitle_StreamError etc.) — the canned-Stream stub llm.Client shape to reuse for compact_test.go.
    - docs/compact_prompt.md — the OLDER 7-section provenance prompt; ref D:\tmp\system_prompts_leaks\Anthropic\Claude Code\bundled-skills\compact.md (the D-06 9-section adaptation target, cited in 42-CONTEXT.md canonical_refs) if reachable — otherwise adapt the 7 sections + add the 2 new ones per D-06.
    - 42-CONTEXT.md D-06 (the exact 9-section + governance additions) + 42-RESEARCH.md § Security Domain (Governance Decay mitigation is the phase's headline control) + § No Analog Found (the prompt is an ADAPTATION target, no existing assertion to extend — the 9-header test is net-new).
  </read_first>
  <behavior>
    - happy: a stub client streaming "SUMMARY TEXT" + a trailing Usage → CompactResult{Summary:"SUMMARY TEXT", TokensBefore:countTokens(rendered input), TokensAfter:countTokens("SUMMARY TEXT"), Usage:non-nil}, nil error.
    - empty: a stub streaming "" or whitespace → error; CompactResult zero; no store interaction (compact.go has none).
    - nil client: → error (mirror generateTitle nil-client guard).
    - stream error: client.Stream returns err → wrapped error, no partial result.
    - focus: opts.Focus non-empty → it is appended to Request.Messages[0].Content (the system prompt) as the Compact Instructions hook; empty Focus → the bare compactSystemPrompt.
    - budget: Request.MaxTokens == opts.MaxOutputTokens.
    - prompt const: contains the 9 section headers + the English-only clause + the no-tools guard + the verbatim-safety-preservation instruction.
  </behavior>
  <action>
    Create `internal/conversations/compact.go` (`package conversations`). Declare `const compactSystemPrompt` = the adapted 9-section governance-hardened schema (D-06): the 7 original headers (Primary Request/Intent, Key Technical Concepts, Files & Code Sections, Problem Solving, Pending Tasks, Current Work, Optional Next Step) PLUS "All user messages" and "Errors and fixes"; a clause instructing verbatim preservation of any user-stated security/safety constraints so they keep applying after compaction; a "reply in TEXT ONLY, call no tools" guard; Aura-neutral framing (a general agent, not "development work"); a "Reply in English only" clause (match titlePrompt); and an explicit output-length instruction bounded to the summary budget. Declare `CompactOptions{MaxOutputTokens int; Focus string}` and `CompactResult{Summary string; TokensBefore int; TokensAfter int; Usage *llm.Usage}`. Implement `CompactConversation(ctx, client llm.Client, model string, history []llm.Message, opts CompactOptions) (CompactResult, error)` mirroring generateTitle: nil-client guard; build the system content = `compactSystemPrompt` + (if opts.Focus != "" a delimited "Compact Instructions:" + opts.Focus block); `req := llm.Request{Model: model, Messages: {system, {RoleUser, renderHistoryForCompaction(history)}}, Temperature: 0.3, MaxTokens: opts.MaxOutputTokens}`; `client.Stream`; drain `for chunk := range ch` accumulating chunk.Text into a strings.Builder AND capturing the last non-nil `chunk.Usage`; `summary := sanitize(builder)`; empty/whitespace → error (never a CompactResult); compute `enc,_ := encoder()`, `TokensBefore = countTokens(enc, renderHistoryForCompaction(history))`, `TokensAfter = countTokens(enc, summary)`; return `CompactResult{summary, TokensBefore, TokensAfter, usage}`. Add `renderHistoryForCompaction` (render user+assistant turns with a larger cap + more turns than title's 6/500 — the summary needs the whole conversation, not just the opening). Refactor-on-touch (CLAUDE.md dupl gate, threshold 100): if `dupl` flags renderHistoryForCompaction against title.go:renderHistoryForTitle as duplication, extract a shared parameterized `renderHistory(history, perTurnCap, maxTurns)` in title.go used by both (that is why title.go is in files_modified); if they diverge by more than a cap, keep them separate and leave title.go untouched. Write `compact_test.go` (UNTAGGED unit) reusing the title stub-client shape: the happy/empty/nil/stream-error/focus/budget behaviors above, plus COMPACT-02 substring assertions — all 9 headers present, the English-only clause present, the "TEXT ONLY, call no tools" guard present, the verbatim-safety-preservation clause present. This is the mutation-≥70% target for compact.go.
  </action>
  <acceptance_criteria>
    - `compact.go` declares `CompactConversation`, `CompactOptions`, `CompactResult`, and a single `const compactSystemPrompt`; it imports no `internal/db`/`sqlc` and calls no store method.
    - `compactSystemPrompt` contains the 9 section-header strings, "Reply in English only", the "TEXT ONLY"/"call no tools" guard, and the verbatim-safety-preservation instruction (COMPACT-02 test asserts each).
    - CompactConversation sets `MaxTokens: opts.MaxOutputTokens`, drains the stream fully, captures `Usage`, and appends `opts.Focus` into `Request.Messages[0].Content`.
    - `cd D:/Repo/Aura && go test ./internal/conversations/ -run 'CompactConversation|CompactPrompt'` passes (all COMPACT-01/02 behaviors, stub client, no DB).
    - `golangci-lint` clean on compact.go (incl. dupl — either folded or genuinely distinct).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/conversations/ && go test ./internal/conversations/ -run 'CompactConversation|CompactPrompt'</automated>
  </verify>
  <done>CompactConversation produces a structured summary + token counts + Usage from a single bounded, fully-drained LLM call, threads the focus hook, errors on empty, and never persists; compactSystemPrompt is the 9-section governance-hardened const (verbatim safety preservation + no-tools guard + English-only) proven by substring assertions.</done>
</task>

<task type="auto">
  <name>Task 2: doc housekeeping — compact_prompt.md provenance note + D-07 L3-activation notes (COMPACT-02 provenance, D-07)</name>
  <files>docs/compact_prompt.md, .planning/milestones/v0.0.0-phases/04-hitl-identity-conversations/04-SPEC.md, prd.md</files>
  <read_first>
    - docs/compact_prompt.md — the retained OLDER-7-section Claude Code /compact prompt; it gets a header note (SPEC Req#2: "docs/compact_prompt.md references the constant").
    - .planning/milestones/v0.0.0-phases/04-hitl-identity-conversations/04-SPEC.md — find the L3 deferral entry (Req#10 "L3 LLM-driven compaction is explicitly NOT implemented" + the Out-of-scope `chat_compact` line); those get the one-line activation note.
    - prd.md §1.8 OQ#3 — the open-question this phase closes; grep `prd.md` for `OQ#3` / `chat_compact` to locate it; it gets the activation note.
    - 42-CONTEXT.md D-07 — "documentation only, ~2-line touch; No PRD-amendment commit needed."
  </read_first>
  <action>
    Add a short header note at the top of `docs/compact_prompt.md` stating it is the OLDER 7-section provenance reference and that the shipping prompt is the 9-section `compactSystemPrompt` constant in `internal/conversations/compact.go` (adapted per D-06). In `04-SPEC.md`, next to the L3 "explicitly NOT implemented" Req#10 entry and the Out-of-scope `chat_compact` line, append a one-line note: "activated in Phase 42 (42-llm-conversation-compaction)". In `prd.md` §1.8 OQ#3, append the same one-line "activated in Phase 42" note. These are additive one-liners — do NOT restructure or re-open either document (D-07: not a PRD-amendment).
  </action>
  <acceptance_criteria>
    - `docs/compact_prompt.md` head references `compactSystemPrompt` (grep confirms).
    - `04-SPEC.md` and `prd.md §1.8 OQ#3` each contain an "activated in Phase 42" one-line note adjacent to the L3 deferral / OQ#3.
    - `git diff --stat` on those three files shows additive line changes only (no large rewrites).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && grep -n "compactSystemPrompt" docs/compact_prompt.md && grep -rn "activated in Phase 42" ".planning/milestones/v0.0.0-phases/04-hitl-identity-conversations/04-SPEC.md" prd.md</automated>
  </verify>
  <done>The provenance doc points at the shipping constant, and the Phase-4 L3 deferral + PRD §1.8 OQ#3 record that L3 is activated in Phase 42 — the truth-source no longer asserts "L3 NOT implemented" (D-07, documentation only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation history → summary → every future turn | The summary is load-bearing: it is re-injected each subsequent turn, so its content integrity is a governance/safety control. |
| model output → summary text | An adversarial/long-horizon conversation could try to make the summarizer drop safety constraints or emit tool calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering (integrity of safety context — Governance Decay) | compact.go compactSystemPrompt | mitigate | D-06 prompt MANDATES verbatim preservation of user-stated security/safety constraints + a "reply in TEXT ONLY, call no tools" guard; COMPACT-02 unit-asserts both are present in the const. This is the phase's headline security control (RESEARCH § Governance Decay). |
| T-42-09 | Tampering (prompt drift per call) | compact.go | mitigate | compactSystemPrompt is a single package const; only opts.Focus varies per call; a unit asserts the const shape so a refactor cannot silently alter the schema. |
| T-42-10 | Denial of Service (unbounded summary) | compact.go CompactConversation | mitigate | MaxTokens == opts.MaxOutputTokens (the summary budget); the prompt carries an explicit output-length instruction; the stream is fully drained (no leak). |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new packages (RESEARCH § Package Legitimacy Audit) — reuses vendored tiktoken-go + the in-repo llm.Client. |
</threat_model>

<verification>
- `cd D:/Repo/Aura && go vet ./internal/conversations/ && go test -race ./internal/conversations/ -run 'CompactConversation|CompactPrompt'` green (stub client, all COMPACT-01/02 paths — no DB).
- `golangci-lint run ./internal/conversations/` clean (dupl included).
- Manual-Only (VALIDATION): `go-mutesting` ≥70% killed on compact.go (WSL).
- Doc notes present (grep in the verify command).
</verification>

<success_criteria>
- CompactConversation is a bounded, fully-drained, Usage-preserving summarization call with a focus hook and an empty-summary guard (COMPACT-01), proven entirely daemon-free.
- compactSystemPrompt is the 9-section governance-hardened const — verbatim safety-constraint preservation + no-tools guard + English-only — mitigating Governance Decay (COMPACT-02 / D-06).
- docs/compact_prompt.md references the constant; the Phase-4 L3 deferral + PRD §1.8 OQ#3 note the Phase-42 activation (D-07, documentation only).
</success_criteria>

<output>
Create `.planning/phases/42-llm-conversation-compaction/42-02-SUMMARY.md` when done.
</output>
