---
phase: 42-llm-conversation-compaction
plan: 09
type: execute
wave: 8
depends_on: [42-08]
files_modified: [internal/conversations/compaction_eval/evaluator.go, internal/conversations/compaction_rollout.go, internal/conversations/compaction_rollout_test.go, internal/conversations/compaction_rollout_chain_integration_test.go, internal/config/config_compaction.go, internal/runner/interfaces.go, internal/runner/runner_compact.go, cmd/aura/chat_boot.go, cmd/aura/serve.go, cmd/aura/chat_boot_test.go]
autonomous: true
requirements: [IC-13, IC-14]
must_haves:
  truths: ["Evaluator decisions flow through durable CAS state before affecting runtime", "Runtime config and coordinator consume the same persisted effective snapshot", "Any rollback trigger disables new claims and restores LKG atomically across replicas"]
  artifacts:
    - {path: internal/conversations/compaction_eval/evaluator.go, provides: "immutable versioned cohort decision"}
    - {path: internal/conversations/compaction_rollout.go, provides: "durable rollout controller"}
    - {path: internal/config/config_compaction.go, provides: "validated effective runtime snapshot"}
  key_links:
    - {from: internal/conversations/compaction_eval/evaluator.go, to: internal/conversations/compaction_rollout_store.go, via: "immutable evaluator evidence and expected-version transition"}
    - {from: internal/conversations/compaction_rollout_store.go, to: internal/config/config_compaction.go, via: "persisted effective snapshot"}
    - {from: internal/config/config_compaction.go, to: internal/runner/runner_compact.go, via: "claim/finalize coordinator fence"}
---
<objective>Wire evaluator to durable controller to effective runtime config to coordinator, with automatic evidence-gated promotion and rollback.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-08-SUMMARY.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Produce immutable evaluator decisions and drive durable controller transitions</name>
  <files>internal/conversations/compaction_eval/evaluator.go, internal/conversations/compaction_rollout.go, internal/conversations/compaction_rollout_test.go</files>
  <behavior>Promotion requires every numerical gate plus 24 hours and 1000 eligible attempts; stale evaluator/config/scorer versions cannot transition; safety/continuation/failure/latency/restore/corruption triggers atomically rollback.</behavior>
  <action>Make evaluator output a canonical immutable cohort decision containing eligibility/censor/stratum results, exact numerical trigger snapshot, evidence digest, and evaluator/scorer/config/corpus versions. The controller loads the durable row, rejects stale/incompatible decisions, and attempts an expected-version CAS transition; it never holds authoritative stage in memory. Promote only after both duration and attempts plus all gates. Roll back for any safety/authority/continuation gate failure, failure over 2% in 15m, latency breach sustained 30m, restore over 1%, corrupt/incompatible evidence, or stale evaluator version. For `internal/conversations/compaction_eval/evaluator.go`, follow bounded metric construction in `internal/agent/metrics.go` for labels but use the explicit statistical/no-analog cohort contract in `42-RESEARCH.md` "Evaluation Architecture". For `internal/conversations/compaction_rollout.go`, the exact analog is the CAS/lease controller pattern catalogued from `internal/documents/jobs_store.go` in `42-PATTERNS.md`; stage/duration/rollback semantics are explicitly no-analog. For `internal/conversations/compaction_rollout_test.go`, follow independent-clock/table fixture style in `internal/config/config_knobs_test.go` and add fake-store conflict plus DB-backed behavior delegated to Plan 08.</action>
  <verify><automated>go test -race ./internal/conversations ./internal/conversations/compaction_eval -run 'RolloutController|PromotionAfter24HoursAnd1000Attempts|EvaluatorToDurableController|RollbackSafety|RollbackContinuation|RollbackFailureWindow|RollbackLatencyWindow|RollbackRestoreRate|StaleEvaluator' -count=1</automated></verify>
  <done>Tests prove immutable evaluator evidence drives only durable CAS transitions and every rollback condition restores LKG before a transition is observable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expose persisted effective config and fence coordinator claims/finalization</name>
  <files>internal/config/config_compaction.go, internal/runner/interfaces.go, internal/runner/runner_compact.go, cmd/aura/chat_boot.go, cmd/aura/serve.go, cmd/aura/chat_boot_test.go</files>
  <behavior>Runner reads durable effective snapshot before claim and finalize; concurrent rollback prevents activation; cohort assignment stays deterministic; restart and replicas converge on the same version.</behavior>
  <action>Define the persisted effective-state reader in `internal/config/config_compaction.go` and add it to `internal/runner/interfaces.go`; static config is disabled bootstrap only. In `runner_compact.go`, load it before claim and finalize and carry scope/version/cohort fences. In the exact composition root `cmd/aura/chat_boot.go:205 assembleChatEnv`, construct rollout store/controller and evaluator from the open pool, inject the persisted reader into Runner deps, and register cleanup. In daemon startup `cmd/aura/serve.go:210-228`, schedule evaluator invocation beside the existing sweeper/worker/reconciler/scheduler lifecycle and stop it with the signal context. These are exact composition/lifecycle analogs. `cmd/aura/chat_boot_test.go` adds named `TestCompactionRolloutComposition`, `TestCompactionRolloutPersistedReaderInjection`, `TestCompactionRolloutEvaluatorLifecycle`, `TestCompactionRolloutCancellation`, and `TestCompactionRolloutStartupFailsClosed`, proving construction, injection, invocation, cancellation, and fail-closed startup for incomplete adapter preflight or durable state. Scope note: Task 2 is one live composition seam spanning the narrow Runner interface, its implementation, and the two existing composition/lifecycle roots; separating them would leave a buildable but unwired controller.</action>
  <verify><automated>go test -race ./internal/conversations ./internal/config ./internal/runner ./cmd/aura -run 'ControllerToRuntimeConfig|CoordinatorHonorsRollback|EffectiveSnapshot|FinalizeFence|ReplicaConvergence|Restart|CompactionRolloutComposition|CompactionRolloutPersistedReaderInjection|CompactionRolloutEvaluatorLifecycle|CompactionRolloutCancellation|CompactionRolloutStartupFailsClosed' -count=1</automated></verify>
  <done>The complete evaluator-to-coordinator chain is explicit and stale/in-memory configuration cannot bypass durable rollback.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove the complete durable rollout-to-runner chain in PostgreSQL</name>
  <files>internal/conversations/compaction_rollout_chain_integration_test.go</files>
  <behavior>Independent handles and close/reopen prove evaluator evidence to CAS state to effective snapshot to runner claim/finalize fencing, stale rejection, and atomic LKG rollback across replicas.</behavior>
  <action>Create named `TestCompactionRolloutFullChainPostgres`. Use two independent pools, persist evaluator evidence, win one controller CAS, observe converged effective snapshots, claim through one Runner, rollback from the other before finalize, reject stale finalize, verify decision ledger and LKG snapshot changed atomically, close both, reopen, and prove disabled LKG state/version remain authoritative. `internal/documents/jobs_store_test.go` is an analog only for independent-pool mechanics; this full chain follows the explicit no-analog RESEARCH contract.</action>
  <verify><automated>go test -race -tags=db_integration ./internal/conversations -run '^TestCompactionRolloutFullChainPostgres$' -count=1</automated></verify>
  <done>One PostgreSQL test proves the entire live chain and restart/replica rollback fence.</done>
</task>
</tasks>
<verification>go test -race ./internal/conversations/... ./internal/config ./internal/runner ./cmd/aura -run 'Rollout|EvaluatorToDurableController|ControllerToRuntimeConfig|CoordinatorHonorsRollback|ReplicaConvergence|CompactionRolloutComposition|CompactionRolloutPersistedReaderInjection|CompactionRolloutEvaluatorLifecycle|CompactionRolloutCancellation|CompactionRolloutStartupFailsClosed' -count=1</verification>
<success_criteria>Promotions and rollbacks become effective only via the durable store, and all replicas/coordinator paths honor the persisted version fence.</success_criteria>
<output>Create `42-09-SUMMARY.md`; atomic evaluator/controller and runtime/coordinator commits.</output>
