---
phase: 42-llm-conversation-compaction
plan: 10
type: execute
wave: 9
depends_on: [42-07, 42-09]
files_modified: [testdata/compaction/golden.jsonl, testdata/compaction/adversarial.jsonl, internal/conversations/compaction_eval/corpus_test.go, docs/conversation-compaction.md, .github/workflows/ci.yml]
autonomous: true
requirements: [IC-12, IC-13, IC-14]
must_haves:
  truths: ["The 500+200 corpus satisfies every numerical promotion gate", "CI cannot count skipped migration rollback privacy or surface tiers as green", "Acceptance records exact versions commands results stage evidence compatibility and rollback"]
  artifacts:
    - {path: testdata/compaction/golden.jsonl, provides: "500+ stratified versioned cases"}
    - {path: testdata/compaction/adversarial.jsonl, provides: "200+ adversarial versioned cases"}
    - {path: docs/conversation-compaction.md, provides: "operations migration rollback and acceptance record"}
  key_links:
    - {from: .github/workflows/ci.yml, to: testdata/compaction/golden.jsonl, via: "blocking evaluator tier"}
    - {from: docs/conversation-compaction.md, to: internal/conversations/compaction_rollout_store.go, via: "durable migration/rollback procedure and evidence schema"}
---
<objective>Complete the numerical corpus, blocking CI matrix, operational documentation, and terminal acceptance evidence.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-VALIDATION.md
@.planning/phases/42-llm-conversation-compaction/42-09-SUMMARY.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md</context>
<tasks>
<task type="auto">
  <name>Task 1: Finalize versioned golden and adversarial evaluation corpora</name>
  <files>testdata/compaction/golden.jsonl, testdata/compaction/adversarial.jsonl, internal/conversations/compaction_eval/corpus_test.go</files>
  <action>Populate at least 500 stratified golden and 200 adversarial cases covering every eligibility/censor/stratum and authority/ledger/tool/pending/artifact/poison boundary. Create `corpus_test.go` with named `TestCompactionCorpusCensus`, `TestCompactionCorpusUniqueIDs`, `TestCompactionCorpusRequiredStrata`, `TestCompactionCorpusSchemaVersionAndProvenance`, `TestCompactionCorpusExpectedOutcomes`, and `TestCompactionCorpusNumericalGates`. Follow Aura's strict checked-in fixture loaders in `internal/eval/chat50_prompts_test.go` (scanner, duplicate-ID rejection, exact census, required category floors) and `internal/eval/retrieval_eval_test.go` (explicit threshold assertions and no-skip-as-green); use `internal/agent/tools/search_corpus_test.go` for deterministic metric aggregation. The curated `D:/tmp/Backboard-Locomo-Benchmark/locomo_dataset.json` is research input for long-conversation single-hop/multi-hop/temporal taxonomy only and is not copied or made a runtime dependency. The Phase 42 JSONL schema, provenance, censoring, strata, and adversarial taxonomy remain the no-analog contracts in RESEARCH/PATTERNS. Every row has deterministic ID, corpus/schema version, provenance/license field, stratum/taxonomy tags, source/expected projection, and expected outcomes; tests fail on duplicates, missing fields/categories, fewer than 500+200 distinct cases, or any unmet Section 17.13 gate.</action>
  <verify><automated>go test ./internal/conversations/compaction_eval -run 'CompactionCorpus|Threshold|Cohort' -count=1</automated></verify>
  <done>Census and evaluation prove all numerical gates over distinct, versioned, stratified cases.</done>
</task>
<task type="auto">
  <name>Task 2: Make CI and operator acceptance enforce the complete rollout contract</name>
  <files>.github/workflows/ci.yml, docs/conversation-compaction.md</files>
  <action>In `.github/workflows/ci.yml`, follow its existing named Go race, DB-service, web, and quality jobs, adding blocking compaction evaluator, migration up/down/up, distributed restart/multi-replica/CAS rollback, leak, mutation, API/FE, privacy/security, and E2E tiers with no skip-as-green; the combined matrix is an explicit no-analog RESEARCH contract. No generic operations document exists: use `docs/runbooks/musr-rollout.md` only for repository runbook tone/headings and the RESEARCH/PATTERNS no-analog evidence contract for compaction. Document migration, durable scope/evidence/snapshots, the complete live flow, stale decisions, restart/replicas, rollback, compatibility, disabled bootstrap, thresholds, and manual evidence. Record exact commands, commits, versions, environment, timestamps, results, and keep unobserved stages disabled.</action>
  <verify><automated>make quality &amp;&amp; bash scripts/coverage_docker.sh &amp;&amp; go test -race -tags=db_integration ./internal/conversations/... ./internal/assets/... ./internal/memory/... ./internal/runner/... ./internal/agui/... ./cmd/aura/... &amp;&amp; cd web &amp;&amp; npm test -- --run</automated></verify>
  <done>CI blocks on every automated gate and the runbook contains reproducible durable rollout, rollback, migration, and acceptance evidence without claiming unobserved canaries.</done>
</task>
</tasks>
<verification>make quality &amp;&amp; bash scripts/coverage_docker.sh &amp;&amp; go test -race -tags=db_integration ./internal/conversations/... ./internal/assets/... ./internal/memory/... ./internal/runner/... ./internal/agui/... ./cmd/aura/... &amp;&amp; cd web &amp;&amp; npm test -- --run</verification>
<success_criteria>IC-12/13/14 corpus, CI, docs, compatibility, privacy, security, migration, durable rollout, rollback and terminal evidence gates are explicit and enforceable.</success_criteria>
<output>Create `42-10-SUMMARY.md`; atomic corpus and CI/docs commits.</output>
