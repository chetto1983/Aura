---
phase: 42-llm-conversation-compaction
plan: 07
type: execute
wave: 4
depends_on: ["42-06"]
files_modified:
  - web/src/conversations/useConversations.ts
  - web/src/chat/footerMetrics.ts
  - web/src/chat/ContextBudgetGauge.tsx
  - web/src/chat/composer/skillPickerModel.ts
  - web/src/chat/composer/SkillPicker.tsx
  - web/src/chat/Composer.tsx
  - web/src/chat/ExternalStoreChat.tsx
  - web/src/i18n/en.json
  - web/src/chat/composer/skillPickerModel.test.ts
  - web/src/chat/__tests__/ContextBudgetGauge.test.tsx
  - web/src/chat/footerMetrics.test.ts
autonomous: true
requirements: [COMPACT-05, COMPACT-06, COMPACT-07, COMPACT-10]
must_haves:
  truths:
    - "D-08 composer trigger: '/compact' is a QuickCommand in the composer palette (added to the QuickCommand union + QUICK_COMMANDS catalog + a distinct Lucide glyph in COMMAND_ICON); selecting it fires useCompactConversation() (POST /api/conversations/{id}/compact) for the active conversation and renders the returned token delta as a system/toast line; a below-floor response surfaces as a non-error notice"
    - "D-09 gauge marker: ContextBudgetGauge consumes useConversationCompactions(id) (GET /compactions) and renders a SECOND marker line (via a totalSummarized/compactionCount aggregation in footerMetrics) when there is ≥1 compaction, sibling to the existing pairs_dropped rot-events marker"
    - "D-10 visual distinction: the compaction marker uses a NEW i18n key (e.g. footer.summarized — NOT the taken footer.compacted, which labels L2.5 hard-drops) AND a visually distinct glyph/color (not just a tooltip); new/edited React follows CLAUDE.md §Frontend_aesthetics (distinctive, not AI-slop)"
    - "wire shape (PATTERNS correction): the Compaction hook interface uses PascalCase Go field names (TS, Trigger, Model, CheckpointSeq, SummaryTurnSeq, TokensBefore, TokensAfter) matching the domain struct serialized directly by the GET route; the POST mutation response uses snake_case (tokens_before/tokens_after/compaction_id — a hand-written handler struct)"
    - "useCompactConversation onSuccess invalidates the compactions query key so the gauge marker refreshes after a manual /compact; credentials:'same-origin' + retry:false per the useConversations file contract"
    - "web test gate (D-10): a NEW web/src/chat/__tests__/ContextBudgetGauge.test.tsx is CREATED (the gauge has NO existing test — RESEARCH landmine) asserting the compaction marker renders distinct from the rot-events marker; skillPickerModel.test.ts is EXTENDED for the '/compact' QuickCommand; governed by the web vitest gate, NOT scripts/coverage_gate.sh"
  artifacts:
    - path: "web/src/conversations/useConversations.ts"
      provides: "Compaction interface (PascalCase), CONVERSATION_COMPACTIONS_KEY, useConversationCompactions(id) query, useCompactConversation() mutation"
      contains: "useConversationCompactions"
    - path: "web/src/chat/ContextBudgetGauge.tsx"
      provides: "second marker line for compactions, visually distinct from the pairs_dropped marker"
      contains: "useConversationCompactions"
    - path: "web/src/chat/composer/skillPickerModel.ts"
      provides: "'compact' in the QuickCommand union + QUICK_COMMANDS catalog row"
      contains: "compact"
    - path: "web/src/chat/__tests__/ContextBudgetGauge.test.tsx"
      provides: "NEW gauge test — stubs /rot-events + /compactions, asserts two distinct markers coexist"
      contains: "compactions"
  key_links:
    - from: "web/src/chat/composer/SkillPicker.tsx + Composer.tsx"
      to: "web/src/conversations/useConversations.ts useCompactConversation"
      via: "the 'compact' QuickCommand handleSelect branch fires the POST mutation for the active conversation"
      pattern: "useCompactConversation"
    - from: "web/src/chat/ContextBudgetGauge.tsx"
      to: "web/src/conversations/useConversations.ts useConversationCompactions"
      via: "GET /compactions → totalSummarized aggregation → second marker line"
      pattern: "useConversationCompactions"
  prohibitions:
    - "MUST NOT overload the i18n key footer.compacted — it already labels L2.5 hard-drops ('Compacted N older turns'); the LLM-compaction marker needs a NEW distinct key + glyph/color (D-09/D-10, Pitfall 8). Verify: the gauge test asserts the new key renders and differs from footer.compacted."
    - "MUST NOT use PascalCase for the POST mutation response or snake_case for the GET read shape — the GET read serializes the domain struct (PascalCase Go fields, no json tags), the POST response is a hand-written snake_case handler struct (PATTERNS correction). Verify: the Compaction interface is PascalCase; useCompactConversation reads tokens_before/tokens_after/compaction_id."
    - "MUST NOT reuse the clear/Eraser glyph for /compact — pick a distinct Lucide glyph (e.g. Combine/FoldVertical) so the command reads as 'summarize', not 'erase'. Verify: COMMAND_ICON['compact'] is not Eraser."
    - "MUST NOT 'extend' a non-existent gauge test — ContextBudgetGauge has NO test; ContextBudgetGauge.test.tsx must be CREATED (RESEARCH Pitfall 8). Verify: the file exists and runs under vitest."
    - "MUST NOT let the /compact QuickCommand skip the loading/notice UX — the delta (or below-floor notice) renders as a system/toast line; it is the first QuickCommand that hits a server mutation (model it on the mutation pattern, not the pure-client resets). Verify: Composer test mocks the POST and asserts the notice renders."
---

<objective>
Ship the two web cockpit surfaces that make compaction visible and triggerable in the browser: a `/compact` QuickCommand in the composer command palette wired to the POST route (D-08 — the 4th manual trigger, extends COMPACT-05/06/07), and visually-distinct compaction markers on the `ContextBudgetGauge` fed by the GET `/compactions` route (D-09 — extends COMPACT-10's ListCompactions projection). New/edited React follows CLAUDE.md §Frontend_aesthetics (D-10) and the existing read-chain patterns exactly.

Purpose: operator parity with CLI/REPL/Telegram in the web cockpit, plus at-a-glance visibility of where the conversation was summarized. This is a purely additive read-path + one new mutation reusing shipped patterns (rot-events read chain + composer QuickCommand) — no new frontend architecture. It is governed by the web vitest gate (web coverage ≥85%), NOT the Go coverage gate (D-10). Two RESEARCH landmines are pre-baked in: the `ContextBudgetGauge` has NO existing test (must be CREATED, not extended), and the i18n key `footer.compacted` is already taken by L2.5 hard-drops (a NEW distinct key + glyph is required).
Output: the `useConversations.ts` Compaction hook + mutation, the gauge marker + `footerMetrics` aggregation, the composer QuickCommand + icon + dispatch, a new i18n key, and the CREATED gauge test + extended skill-picker/footerMetrics tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-VALIDATION.md
@.claude/skills/frontend-design/SKILL.md
@.claude/skills/vercel-react-best-practices/SKILL.md
@.claude/skills/accessibility/SKILL.md
</context>

<artifacts_produced>
This plan adds these NEW symbols (exclude from source-grounding drift verification — net-new):
- `useConversations.ts` — `Compaction` interface (PascalCase), `CONVERSATION_COMPACTIONS_KEY`, `useConversationCompactions(id)`, `useCompactConversation()`.
- `footerMetrics.ts` — `totalSummarized` (or `compactionCount`) aggregation.
- `ContextBudgetGauge.tsx` — the second (compaction) marker line.
- `skillPickerModel.ts` — `'compact'` in the QuickCommand union + catalog; `SkillPicker.tsx` — the COMMAND_ICON glyph; `Composer.tsx`/`ExternalStoreChat.tsx` — the handleSelect '/compact' dispatch branch.
- `en.json` — a NEW `footer.summarized` (or similar) i18n key + the composer command label/subtitle keys.
- `ContextBudgetGauge.test.tsx` (CREATED), extended `skillPickerModel.test.ts` + `footerMetrics.test.ts`.
Consumes from Plan 42-06: the GET /api/conversations/{id}/compactions route (domain-struct wire) + the POST /api/conversations/{id}/compact route ({tokens_before,tokens_after,compaction_id}).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useConversations.ts — Compaction hook + useCompactConversation mutation (D-08/D-09 data layer)</name>
  <files>web/src/conversations/useConversations.ts</files>
  <read_first>
    - web/src/conversations/useConversations.ts:42 (RotEvent interface — PascalCase, no json tags), :106 (CONVERSATION_ROT_EVENTS_KEY), :160 (useConversationRotEvents — useQuery shape, getJSON, enabled, retry:false), :176 (useRenameConversation onSuccess → queryClient.invalidateQueries) + the file header contract (credentials:'same-origin', wire shapes are Go PascalCase projections). The verbatim template (42-PATTERNS.md § useConversations.ts).
    - internal/agui/conversations_api.go (Plan 42-06) — the GET returns the domain conversations.Compaction (PascalCase: ID, TS, Trigger, Model, CheckpointSeq, SummaryTurnSeq, TokensBefore, TokensAfter); the POST returns snake_case {tokens_before, tokens_after, compaction_id}.
    - 42-PATTERNS.md top-of-file load-bearing correction (domain-struct-as-wire; PascalCase read, snake_case POST response) + § useConversations.ts "What differs".
  </read_first>
  <behavior>
    - Compaction interface: readonly PascalCase fields (TS:string, Trigger:string, Model:string, CheckpointSeq:number, SummaryTurnSeq:number, TokensBefore:number, TokensAfter:number).
    - useConversationCompactions(id): useQuery on [CONVERSATION_COMPACTIONS_KEY, id], getJSON<Compaction[]>(`/api/conversations/${id}/compactions`), enabled when id non-empty, retry:false.
    - useCompactConversation(): useMutation POST /api/conversations/${id}/compact, response {tokens_before, tokens_after, compaction_id} (snake_case); onSuccess invalidates [CONVERSATION_COMPACTIONS_KEY, id] so the gauge refreshes.
  </behavior>
  <action>
    In `web/src/conversations/useConversations.ts`, add a `Compaction` interface with readonly PascalCase fields matching the domain struct (TS, Trigger, Model, CheckpointSeq, SummaryTurnSeq, TokensBefore, TokensAfter), a `CONVERSATION_COMPACTIONS_KEY` const, a `useConversationCompactions(conversationId)` query (verbatim shape of useConversationRotEvents with the `/compactions` URL, retry:false, enabled on non-empty id), and a `useCompactConversation()` mutation (POST `/api/conversations/${encodeURIComponent(id)}/compact` with an optional `{ focus }` body) whose response type is snake_case `{ tokens_before: number; tokens_after: number; compaction_id: string }` and whose onSuccess invalidates the compactions key (mirror useRenameConversation). Keep `credentials: 'same-origin'` + `retry: false` per the file header contract.
  </action>
  <acceptance_criteria>
    - `useConversations.ts` exports `Compaction` (PascalCase), `CONVERSATION_COMPACTIONS_KEY`, `useConversationCompactions`, `useCompactConversation`; the query hits `/compactions`, the mutation POSTs `/compact` and reads snake_case fields; onSuccess invalidates the compactions key.
    - `cd D:/Repo/Aura/web && npx tsc --noEmit` (or the project typecheck) passes.
    - `cd D:/Repo/Aura/web && npm test -- useConversations` passes (if a hook test exists/added) — otherwise covered by the gauge test in Task 3.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura/web && npm test -- --run useConversations skillPicker</automated>
  </verify>
  <done>The data layer exposes the compaction read query (PascalCase, from the domain-struct GET) and the compact mutation (snake_case POST response) with cache-invalidation on success — the exact sibling of the shipped rot-events read chain + rename mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: composer /compact QuickCommand — union + catalog + glyph + dispatch (D-08, COMPACT-05/06/07)</name>
  <files>web/src/chat/composer/skillPickerModel.ts, web/src/chat/composer/SkillPicker.tsx, web/src/chat/Composer.tsx, web/src/chat/ExternalStoreChat.tsx, web/src/i18n/en.json, web/src/chat/composer/skillPickerModel.test.ts</files>
  <read_first>
    - web/src/chat/composer/skillPickerModel.ts:12 (the QuickCommand union = 'add-files'|'new-chat'|'clear'), :52 (QUICK_COMMANDS catalog rows with labelKey/subtitleKey) — add 'compact' (42-PATTERNS.md § composer palette Analog A).
    - web/src/chat/composer/SkillPicker.tsx:24 (COMMAND_ICON Record<QuickCommand, LucideIcon>: add-files→Paperclip, new-chat→MessageSquarePlus, clear→Eraser) — add a DISTINCT glyph for 'compact' (e.g. Combine/FoldVertical, NOT Eraser).
    - web/src/chat/Composer.tsx:137 (handleSelect command switch: add-files/new-chat/clear branches) — add an `else if (item.command === 'compact')` branch; ExternalStoreChat.tsx owns onNewChat + the active conversation id, the pattern for wiring a command that needs the conversation id + a mutation (model on useCreateConversation, not the pure-client resets).
    - web/src/conversations/useConversations.ts (Task 1) — useCompactConversation.
    - web/src/i18n/en.json — the composer command label/subtitle key convention (KEY_PREFIX.cmdX / cmdXSubtitle) to add cmdCompact/cmdCompactSubtitle.
    - CLAUDE.md §Frontend_aesthetics (distinctive, not AI-slop) + 42-CONTEXT.md D-10.
  </read_first>
  <behavior>
    - skillPickerModel: 'compact' is in the QuickCommand union + QUICK_COMMANDS; filterPickerItems / commandIds include 'compact' (the identifier doubles as the filter corpus).
    - SkillPicker: COMMAND_ICON['compact'] is a distinct non-Eraser Lucide glyph.
    - Composer.handleSelect: selecting the 'compact' command fires useCompactConversation() for the active conversation id and renders the returned token delta (or below-floor notice) as a system/toast line; a mutation error renders a non-fatal notice.
    - en.json: cmdCompact/cmdCompactSubtitle present.
  </behavior>
  <action>
    Add `'compact'` to the `QuickCommand` union and a `{ command: 'compact', labelKey: KEY_PREFIX+'.cmdCompact', subtitleKey: KEY_PREFIX+'.cmdCompactSubtitle' }` row to `QUICK_COMMANDS` in `skillPickerModel.ts`. Add a distinct Lucide glyph for `'compact'` in `SkillPicker.tsx` COMMAND_ICON (e.g. `Combine` or `FoldVertical` — NOT Eraser). Add the `cmdCompact` / `cmdCompactSubtitle` strings to `en.json`. In `Composer.tsx` handleSelect, add an `else if (item.command === 'compact')` branch that calls the `useCompactConversation()` mutation for the active conversation id (threaded from `ExternalStoreChat.tsx`, which owns the conversation id + onNewChat) and renders the returned delta (or the below-floor "nothing to compact" notice, or a mutation error) as a system/toast line — this is the first QuickCommand hitting a server mutation, so model it on the useCreateConversation mutation pattern, not the pure-client add-files/new-chat/clear resets. Follow CLAUDE.md §Frontend_aesthetics for any new visual affordance. Extend `skillPickerModel.test.ts`: the filterPickerItems / commandIds assertions include `'compact'`.
  </action>
  <acceptance_criteria>
    - `skillPickerModel.ts` QuickCommand union + QUICK_COMMANDS include `'compact'`; `SkillPicker.tsx` COMMAND_ICON['compact'] is a distinct non-Eraser glyph; `en.json` has cmdCompact/cmdCompactSubtitle.
    - `Composer.tsx` handleSelect fires `useCompactConversation` for the active conversation and renders the delta/notice; it does not treat compact as a pure-client reset.
    - `cd D:/Repo/Aura/web && npm test -- --run skillPickerModel` passes with `'compact'` asserted in the palette.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura/web && npm test -- --run skillPickerModel</automated>
  </verify>
  <done>Typing "/" in the composer surfaces a distinct-glyph /compact command that POSTs to the shared compaction route for the active conversation and renders the token delta (or below-floor notice) as a system line — parity with CLI/REPL/Telegram, extended in the pure skillPickerModel test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ContextBudgetGauge compaction markers + footerMetrics aggregation + NEW gauge test (D-09/D-10)</name>
  <files>web/src/chat/footerMetrics.ts, web/src/chat/ContextBudgetGauge.tsx, web/src/i18n/en.json, web/src/chat/footerMetrics.test.ts, web/src/chat/__tests__/ContextBudgetGauge.test.tsx</files>
  <read_first>
    - web/src/chat/ContextBudgetGauge.tsx:38 (const { data: rotEvents } = useConversationRotEvents(conversationId)) + :78 (the `dropped > 0` marker block rendering t('footer.compacted', {count})) — the marker block to add a SIBLING for (42-PATTERNS.md § ContextBudgetGauge Analog A).
    - web/src/chat/footerMetrics.ts:147 (totalPairsDropped — the reduce-over-events aggregation) — the sibling totalSummarized/compactionCount to add.
    - web/src/chat/__tests__/RuntimeFooter.test.tsx — the render+hook-mock analog (QueryClientProvider wrapper + import '../../i18n/i18n' + a stubFetch branching on the URL returning Go-PascalCase JSON). ContextBudgetGauge has NO test — CREATE ContextBudgetGauge.test.tsx (RESEARCH Pitfall 8, 42-PATTERNS.md § ContextBudgetGauge.test.tsx).
    - web/src/conversations/useConversations.ts (Task 1) — useConversationCompactions + the Compaction shape.
    - web/src/i18n/en.json — CONFIRM footer.compacted is taken (L2.5); add a NEW footer.summarized (or similar) key.
    - CLAUDE.md §Frontend_aesthetics + 42-CONTEXT.md D-10 (visually distinct glyph/color, not just a tooltip) + 42-RESEARCH.md Pitfall 8.
  </read_first>
  <behavior>
    - footerMetrics.totalSummarized(compactions): count (or sum) of compaction events; 0 for empty; finite-guarded like totalPairsDropped.
    - ContextBudgetGauge: consumes useConversationCompactions(conversationId); when totalSummarized>0 renders a SECOND marker line with the NEW i18n key + a distinct glyph/color; the existing pairs_dropped marker is unchanged and both can coexist.
    - gauge test: stubFetch branches on /rot-events (PairsDropped fixtures) AND /compactions (Compaction fixtures); asserts the compaction marker renders with the new key, is DISTINCT from the pairs_dropped marker, and both coexist; a zero-compaction case renders no compaction marker.
  </behavior>
  <action>
    Add `totalSummarized` (or `compactionCount`) to `footerMetrics.ts` — a reduce/count over the Compaction[] mirroring totalPairsDropped (finite-guarded). Add a NEW i18n key (e.g. `footer.summarized`, "Summarized N checkpoint(s)") to `en.json` — do NOT reuse `footer.compacted` (taken by L2.5, Pitfall 8). In `ContextBudgetGauge.tsx`, consume `useConversationCompactions(conversationId)`, compute `totalSummarized(compactions ?? [])`, and render a SECOND marker line when it is > 0 with a visually distinct glyph/color (D-10 — different from the pairs_dropped marker, not just a tooltip; follow CLAUDE.md §Frontend_aesthetics). CREATE `web/src/chat/__tests__/ContextBudgetGauge.test.tsx` (the gauge has NO test): wrap in QueryClientProvider (retry:false) + `import '../../i18n/i18n'`; a `stubFetch` branching on the URL to return `/rot-events` (PairsDropped) and `/compactions` (Compaction with PascalCase fields) JSON Responses; assert the compaction marker renders with the new key + distinct glyph, is DISTINCT from the pairs_dropped marker, both coexist, and a zero-compaction fixture renders no compaction marker. Extend `footerMetrics.test.ts` with the totalSummarized cases (empty, one, many).
  </action>
  <acceptance_criteria>
    - `footerMetrics.ts` exports `totalSummarized`; `en.json` has a NEW compaction key that is NOT `footer.compacted`.
    - `ContextBudgetGauge.tsx` consumes `useConversationCompactions` and renders a distinct second marker when totalSummarized>0 (distinct glyph/color per D-10).
    - `web/src/chat/__tests__/ContextBudgetGauge.test.tsx` EXISTS and asserts the two markers coexist + are visually distinct; `footerMetrics.test.ts` has the totalSummarized cases.
    - `cd D:/Repo/Aura/web && npm test -- --run ContextBudgetGauge footerMetrics` passes; web coverage ≥85% for the touched modules (web gate, not the Go gate).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura/web && npm test -- --run ContextBudgetGauge footerMetrics</automated>
  </verify>
  <done>The context-budget gauge shows LLM-compaction checkpoints as a visually-distinct second marker (new i18n key + glyph/color, not overloading footer.compacted), fed by the GET /compactions read chain and aggregated in footerMetrics — proven by the CREATED gauge test asserting both markers coexist and differ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → AG-UI compaction routes | The React layer calls the owner-gated POST/GET routes (Plan 42-06); the frontend adds no new trust surface — authz is server-side. |
| i18n key namespace | Reusing footer.compacted would silently mislabel LLM summaries as L2.5 hard-drops (a UX integrity defect). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | Elevation / Info Disclosure (cross-identity) | useConversations hooks | mitigate (server-side) | Authz is enforced by the owner-gated routes in Plan 42-06 (GetForIdentity); the hooks send credentials:'same-origin' and never take a cross-identity id — no client-side authz is claimed or bypassable. |
| T-42-18 | Tampering (UX integrity / mislabeled marker) | ContextBudgetGauge + i18n | mitigate | A NEW footer.summarized key + distinct glyph/color distinguishes an LLM summary-checkpoint from an L2.5 hard-drop; the gauge test asserts the two markers are distinct (D-09/D-10, Pitfall 8). |
| T-42-19 | Denial of Service (accidental compaction spam) | Composer /compact dispatch | accept | The POST is user-initiated per selection; the server bounds the work (one attempt, MaxBytesReader — Plan 42-06); the mutation onSuccess/onError render a notice rather than retry-looping (retry:false). |
| T-42-SC | Tampering | npm installs | accept | Zero net-new npm packages — reuses react-i18next, @tanstack/react-query, lucide-react, vitest, @testing-library/react already in web/package.json (RESEARCH § Standard Stack Frontend). |
</threat_model>

<verification>
- `cd D:/Repo/Aura/web && npm test -- --run ContextBudgetGauge footerMetrics skillPickerModel useConversations` green (the CREATED gauge test + extended picker/footer tests).
- `cd D:/Repo/Aura/web && npm test` (full `vitest run --coverage`) green with web coverage ≥85% for the touched modules — the WEB gate (web_quality_hook / vitest), NOT scripts/coverage_gate.sh (D-10).
- Typecheck/lint: the project web typecheck + eslint clean on the touched files.
- Manual-Only (DoD E2E): in the running cockpit, typing "/" surfaces /compact, selecting it POSTs and renders the delta; the gauge shows a distinct compaction marker after a manual compaction; a two-identity check confirms the routes deny foreign ids (server-side, Plan 42-06).
</verification>

<success_criteria>
- The composer exposes a distinct-glyph /compact QuickCommand that triggers the shared server compaction and renders the token delta / below-floor notice (D-08, 4th manual surface).
- The ContextBudgetGauge renders visually-distinct compaction markers fed by GET /compactions, using a NEW i18n key (not footer.compacted) and a distinct glyph/color (D-09/D-10).
- A NEW ContextBudgetGauge.test.tsx is created and the skill-picker/footerMetrics tests extended; the web vitest gate (coverage ≥85%) is green — the Go floor is unaffected (Go-owned-surface only).
</success_criteria>

<output>
Create `.planning/phases/42-llm-conversation-compaction/42-07-SUMMARY.md` when done.
</output>
