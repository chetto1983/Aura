---
phase: 42-llm-conversation-compaction
plan: 07
type: execute
wave: 7
depends_on: [42-06]
files_modified: [cmd/aura/chat_compact.go, internal/channels/telegram/compact.go, internal/agui/conversations_api.go, web/src/conversations/CompactionHistory.tsx, web/src/conversations/useCompactions.ts]
autonomous: true
requirements: [IC-12, IC-14]
must_haves:
  truths: ["Every surface exposes identical authorized coordinator outcomes", "Operators can preview diff restore and distinguish compaction from loss", "No surface can activate compaction during an in-flight stream"]
  artifacts:
    - {path: internal/agui/conversations_api.go, provides: "owner-gated compaction operations API"}
    - {path: web/src/conversations/CompactionHistory.tsx, provides: "accessible history preview diff restore and degradation UX"}
  key_links:
    - {from: cmd/aura/chat_compact.go, to: internal/runner/runner_compact.go, via: "shared coordinator"}
    - {from: web/src/conversations/useCompactions.ts, to: internal/agui/conversations_api.go, via: "bounded authorized operations API"}
---
<objective>Deliver slice 7 operator surfaces and accessible recovery UX through the already-tested common coordinator.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-06-SUMMARY.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expose common coordinator operations on CLI, REPL, Telegram, and AG-UI</name>
  <files>cmd/aura/chat_compact.go, internal/channels/telegram/compact.go, internal/agui/conversations_api.go</files>
  <behavior>All surfaces expose manual compact/history/preview/diff/restore with identical outcomes; cross-owner requests fail without metadata; streaming never changes active projection mid-response.</behavior>
  <action>Intercept commands before model dispatch and route all operations through the common coordinator with owner/capability/IDOR checks, bounded strict bodies, sanitized localized output, and consistent trigger/reason/token delta/model/quality/manifests. For `cmd/aura/chat_compact.go`, follow the exact command registration/dispatch analog in `cmd/aura/chat.go` catalogued in `42-PATTERNS.md` "Operations surfaces". For `internal/channels/telegram/compact.go`, follow the pre-model command interception and localization analog in `internal/channels/telegram/commands.go` cited in that same PATTERNS section. For `internal/agui/conversations_api.go`, extend its existing owner-scoped conversation controller and bounded-body/error mapping in place; checkpoint diff/restore/quarantine response shapes are the explicit no-analog cockpit contract in `42-RESEARCH.md`.</action>
  <verify><automated>go test -race ./cmd/aura ./internal/channels/telegram ./internal/agui -run 'Compact|Compaction|Restore|IDOR' -count=1</automated></verify>
  <done>Behavioral tests prove parity, authorization, sanitization, localization, body bounds, and no mid-stream activation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add accessible compaction history and recovery controls</name>
  <files>web/src/conversations/CompactionHistory.tsx, web/src/conversations/useCompactions.ts</files>
  <behavior>History distinguishes compaction, L1 offload, and L2.5 loss; preview/diff/restore work by keyboard and screen reader; degraded/corrupt states have actionable recovery.</behavior>
  <action>Implement typed fetching/mutations and the accessible history/recovery panel. For `web/src/conversations/useCompactions.ts`, follow the exact query/mutation/error-state analog in `web/src/conversations/useConversations.ts` cited by `42-PATTERNS.md`. For `web/src/conversations/CompactionHistory.tsx`, follow the keyboard/focus/list composition analog in `web/src/conversations/ConversationSidebar.tsx`; the compaction-specific preview/diff/quarantine visual vocabulary has no existing component analog and must follow `42-RESEARCH.md` "Operations Cockpit" including screen-reader names and reduced-motion behavior.</action>
  <verify><automated>cd web &amp;&amp; npm test -- --run</automated></verify>
  <done>Frontend tests prove semantic state distinctions, keyboard/focus/screen-reader behavior, reduced motion, and recovery flows.</done>
</task>
</tasks>
<verification>go test -race ./cmd/aura ./internal/channels/telegram ./internal/agui -run 'Compact|Compaction|Restore|IDOR' -count=1 &amp;&amp; cd web &amp;&amp; npm test -- --run</verification>
<success_criteria>IC-12 surface parity and accessible recovery pass without duplicating coordinator policy.</success_criteria>
<output>Create `42-07-SUMMARY.md`; atomic backend-surface and web-surface commits.</output>
