---
phase: 42-llm-conversation-compaction
plan: 04
type: execute
wave: 4
depends_on: [42-03]
files_modified: [internal/assets/content_parts.go, internal/assets/content_parts_store.go, internal/assets/content_parts_test.go, internal/db/migrations/0037_content_parts.up.sql, internal/db/migrations/0037_content_parts.down.sql, internal/llm/content_projection.go, internal/conversations/context.go]
autonomous: true
requirements: [IC-02, IC-08, IC-14]
must_haves:
  truths: ["Typed immutable content parts reload only after authorization", "Provider requests contain verified media or explicit reference-only fallback", "L1 offloads only reconstructable content and preserves typed references"]
  artifacts:
    - {path: internal/assets/content_parts.go, provides: "typed part and immutable turn-link model"}
    - {path: internal/llm/content_projection.go, provides: "capability-negotiated outbound projection"}
  key_links:
    - {from: internal/conversations/context.go, to: internal/assets/content_parts_store.go, via: "authorized typed L1 reference"}
---
<objective>Deliver slice 3 typed content parts, artifact durability, reachability retention, L1 editing, and real provider projection.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-03-SUMMARY.md
@internal/assets/types.go
@internal/assets/store.go</context>
<tasks>
<task type="auto">
  <name>Task 1: Add typed part schema, immutable links, authorization, and reachability lifecycle</name>
  <files>internal/assets/content_parts.go, internal/assets/content_parts_store.go, internal/assets/content_parts_test.go, internal/db/migrations/0037_content_parts.up.sql, internal/db/migrations/0037_content_parts.down.sql</files>
  <action>Add storage ID, MIME, digest, byte length, tenant/owner, encryption/retention classes, provider requirements, fallback, immutable turn links, checkpoint/memory reachability edges, quarantine states, and additive rollback. Reauthorize every reload; validate digest; distinguish missing, corrupt, unauthorized, and migrated. GC only unreachable expired parts and retain reachable bytes across backup/migration. Do not describe host-local files as durable. Extend the PATTERNS role-match analogs `internal/assets/types.go`, `store.go`, and `context.go`; implement the RESEARCH typed-part reachability-GC no-analog contract rather than compaction-only blobs.</action>
  <verify><automated>go test -tags=db_integration ./internal/assets -run 'ContentPart|Authorization|Reachability|Migration0037|GC' -count=1</automated></verify>
  <done>Cross-identity denial and missing/corrupt/migrated/GC cases pass without leaking metadata.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Project real media by capability and make L1 typed-reference safe</name>
  <files>internal/llm/content_projection.go, internal/conversations/context.go</files>
  <behavior>Supported media emits verified original bytes; unsupported media emits an explicit reference-only typed part; L1 clears only reconstructable payload and retains authorized reference.</behavior>
  <action>Negotiate against provider capability declarations, validate part digest/authz immediately before request construction, and refuse unavailable mandatory modality. Integrate L1 edits into BudgetDecision without mutating canonical turns or splitting semantic units. Ensure fallback text is stored data, never invented media description. For `internal/llm/content_projection.go`, follow the exact provider request-part construction analog in `internal/llm/client.go` and the typed attachment/capability role match catalogued in `42-PATTERNS.md` under "Provider projection"; where those paths have no authorization-at-projection or reference-only equivalent, implement the explicit no-analog contract in `42-RESEARCH.md` under "Typed Content-Part Prerequisite". For `internal/conversations/context.go`, follow its existing pure projection/selection helper seam identified in `42-PATTERNS.md`, adding L1 edits to BudgetDecision without canonical mutation.</action>
  <verify><automated>go test ./internal/llm ./internal/conversations -run 'ContentProjection|ReferenceOnly|L1Typed' -count=1</automated></verify>
  <done>Actual outbound request fixtures and L1 round trips prove typed, authorized, recoverable behavior.</done>
</task>
</tasks>
<verification>go test -race -tags=db_integration ./internal/assets ./internal/llm ./internal/conversations -run 'Content|Artifact|L1' -count=1</verification>
<success_criteria>IC-08 and typed L1 pass durability, authz, provider-request, migration, and rollback gates.</success_criteria>
<output>Create `42-04-SUMMARY.md`; atomic schema/store and projection/L1 commits.</output>
