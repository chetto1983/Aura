---
phase: 42-llm-conversation-compaction
plan: 04
type: execute
wave: 2
depends_on: ["42-01", "42-02"]
files_modified:
  - internal/runner/runner_compact.go
  - internal/runner/runner.go
  - internal/runner/interfaces.go
  - internal/runner/fakes_test.go
  - internal/runner/runner_compact_test.go
autonomous: true
requirements: [COMPACT-08, COMPACT-10, COMPACT-11]
must_haves:
  truths:
    - "Shared seam (D-08): ONE Runner.Compact(ctx, convID, CompactRequest) (CompactOutcome, error) is the single server-side compaction path — it loads raw history, applies the D-04 floor, calls conversations.CompactConversation, captures checkpoint_seq from the snapshot, and calls Conv.RecordCompaction; all 5 triggers reach it (no duplicate compaction impl)"
    - "D-04 derived floor: Runner.Compact returns CompactOutcome{NoOp:true} (no LLM call, no row) when the loaded body turns ≤ ~3 OR estimated input tokens < 2× effective MaxOutputTokens — derived from existing values, no new knob"
    - "COMPACT-08 auto-fallback: when loadTurnHistory gets ErrContextWindowExceeded AND r.compactAutoEnabled, it runs Runner.Compact once with trigger='auto', re-loads managed history ONCE, and proceeds; a still-over-cap reload surfaces the ORIGINAL ErrContextWindowExceeded exactly once (no retry loop)"
    - "COMPACT-08 toggle: AURA_COMPACT_AUTO_ENABLED=false → loadTurnHistory early-returns the old ErrContextWindowExceeded dead-end unchanged (L1 + L2.5 untouched)"
    - "COMPACT-11 lifecycle: the auto path wraps the compaction in context.WithTimeout(context.WithoutCancel(ctx), r.compactTimeout) and is INLINE-BOUNDED (the turn waits) — NOT a background goroutine (no new r.wg entry, no new goleak surface)"
    - "COMPACT-11 never-corrupting: a compaction LLM error/timeout on the auto path surfaces the original ErrContextWindowExceeded and leaves no partial state (persistence atomicity is Plan 42-01's tx); -race + goleak clean"
    - "COMPACT-10 cost: Runner.Compact passes the CompactResult.Usage into RecordCompactionParams so the compaction call's tokens+cost bill to the conversation aggregates (the summary turn carries the Usage, Plan 42-01)"
    - "D-05 model: the effective compaction model is r.compactModel when non-empty, else the conversation model (r.cfg.Model); the effective MaxOutputTokens falls back to a safe default (4096) when the runner field is unset/zero"
  artifacts:
    - path: "internal/runner/runner_compact.go"
      provides: "Runner.Compact shared method, CompactRequest{Trigger,Focus}, CompactOutcome{TokensBefore,TokensAfter,CompactionID,NoOp}, the D-04 floor check, the inline autoCompact ctx-shaping"
      contains: "func (r *Runner) Compact"
    - path: "internal/runner/runner.go"
      provides: "loadTurnHistory auto-fallback branch + Deps fields (CompactAutoEnabled/CompactTimeout/CompactMaxOutput/CompactModel) + unexported Runner fields"
      contains: "compactAutoEnabled"
    - path: "internal/runner/interfaces.go"
      provides: "ConversationStore widened with RecordCompaction + LatestTurnSeq (LoadHistory already present)"
      contains: "RecordCompaction"
  key_links:
    - from: "internal/runner/runner_compact.go Runner.Compact"
      to: "conversations.CompactConversation + conversations.Store.RecordCompaction"
      via: "load raw history → floor → summarize → capture checkpoint_seq (LatestTurnSeq snapshot) → RecordCompaction(Usage)"
      pattern: "CompactConversation"
    - from: "internal/runner/runner.go loadTurnHistory"
      to: "internal/runner/runner_compact.go Runner.Compact"
      via: "on errors.Is(err, conversations.ErrContextWindowExceeded) && r.compactAutoEnabled → one auto attempt, one re-load"
      pattern: "ErrContextWindowExceeded"
  prohibitions:
    - "MUST NOT spawn a goroutine for auto-compaction — it is INLINE-BOUNDED (RESEARCH Pattern 3, the headline distinction from maybeAutoTitle). Do NOT copy the r.wg.Add(1)/go func() scaffold. Verify: runner_compact.go has no `go func`/`r.wg` for the compaction; goleak clean."
    - "MUST NOT retry-loop the auto path — at most ONE auto-compaction per loadTurnHistory and exactly ONE re-load; a still-over-cap reload surfaces the ORIGINAL error once. Verify: unit forces a second over-cap and asserts exactly one RecordCompaction call + one surfaced error."
    - "MUST NOT surface a worse state than the original — a compaction failure on the auto path returns the ORIGINAL ErrContextWindowExceeded, never a compaction error. Verify: unit with a failing compaction asserts errors.Is(returned, ErrContextWindowExceeded)."
    - "MUST NOT recompute checkpoint_seq inside RecordCompaction — Runner.Compact captures it from the summarized snapshot (LatestTurnSeq at load) and passes it (RESEARCH anti-pattern). Verify: source shows CheckpointSeq set from the load-time seq, not a post-summary re-read."
    - "MUST NOT change L1 (tool-eviction) or L2.5 (hard-drop) behavior — auto-compaction sits strictly AFTER the L2.5 dead-end. Verify: the loadTurnHistory diff only adds the post-error branch; the pre-existing load path is byte-unchanged when no error."
---

<objective>
Build the single shared server-side compaction seam — `Runner.Compact` — that all five trigger surfaces (auto-fallback + CLI + REPL + Telegram + web POST) invoke, and wire the auto-fallback branch into `loadTurnHistory` at the `ErrContextWindowExceeded` dead-end (COMPACT-08/10/11, D-04/D-05/D-08). This is the orchestration heart: it composes Plan 42-02's `CompactConversation` with Plan 42-01's `RecordCompaction`, applies the derived D-04 floor, and owns the bounded/never-corrupting lifecycle.

Purpose: COMPACT-08 (auto-fallback replaces the dead-end, one bounded attempt, toggleable), COMPACT-11 (bounded/best-effort/never-corrupting), and the D-08 "shared, no duplicate implementation" mandate all land here. The auto path is INLINE-BOUNDED — the turn waits for the compacted history — so, unlike `maybeAutoTitle`, it spawns no goroutine and adds no goleak surface (RESEARCH Pattern 3, the load-bearing distinction). Every path is daemon-free unit-testable via `fakeConvStore` (an injectable `LoadManagedHistory` error + a `RecordCompaction` spy), keeping this floor-critical runner surface fully covered without a DB.
Output: `runner_compact.go` (the shared method + types + inline auto path), edited `runner.go` (loadTurnHistory branch + Deps/Runner fields) and `interfaces.go` (ConversationStore widening), plus the extended `fakes_test.go` and `runner_compact_test.go`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-VALIDATION.md
@.claude/skills/golang-concurrency/SKILL.md
@.claude/skills/golang-context/SKILL.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<artifacts_produced>
This plan adds these NEW symbols (exclude from source-grounding drift verification — net-new):
- `runner_compact.go` — `(r *Runner) Compact(ctx, convID string, req CompactRequest) (CompactOutcome, error)`; `CompactRequest{Trigger string; Focus string}`; `CompactOutcome{TokensBefore int; TokensAfter int; CompactionID string; NoOp bool}`; the D-04 floor helper; the inline auto ctx-shaping.
- `runner.go` — Deps fields `CompactAutoEnabled bool`, `CompactTimeout time.Duration`, `CompactMaxOutput int`, `CompactModel string`; unexported Runner fields `compactAutoEnabled`, `compactTimeout`, `compactMaxOutput`, `compactModel`; the loadTurnHistory auto-fallback branch.
- `interfaces.go` — ConversationStore += `RecordCompaction(ctx, conversations.RecordCompactionParams) (conversations.Compaction, error)` and `LatestTurnSeq(ctx, conversationID) (int, error)`.
Consumes from Plan 42-01: `conversations.RecordCompactionParams`, `conversations.Compaction`, `(*Store) RecordCompaction`, `(*Store) LatestTurnSeq`, `conversations.ErrContextWindowExceeded`. From Plan 42-02: `conversations.CompactConversation`, `CompactOptions`, `CompactResult`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runner_compact.go — Runner.Compact shared method + D-04 floor + CompactRequest/Outcome (COMPACT-10/11, D-04/D-05/D-08)</name>
  <files>internal/runner/runner_compact.go, internal/runner/interfaces.go, internal/runner/runner.go, internal/runner/fakes_test.go</files>
  <read_first>
    - internal/runner/runner_resume.go:19 (maybeAutoTitle) — the context.WithoutCancel + context.WithTimeout ctx-shaping to REUSE; but note it is a goroutine on r.wg — the compaction path drops the goroutine (RESEARCH Pattern 3, 42-PATTERNS.md § runner_compact.go "What CRITICALLY differs").
    - internal/runner/runner.go — the Runner struct fields (client, cfg with cfg.Model, Conv, wg, evictAfter at :161), Deps at :66/:86 (EvictAfter is the field-threading precedent), runner.New.
    - internal/runner/interfaces.go:34 (ConversationStore — LoadHistory already at :44; the narrow-interface widening convention) — add RecordCompaction + LatestTurnSeq.
    - internal/conversations/compact.go (Plan 42-02) — CompactConversation signature, CompactOptions{MaxOutputTokens,Focus}, CompactResult{Summary,TokensBefore,TokensAfter,Usage}.
    - internal/conversations/store_compact.go (Plan 42-01) — RecordCompactionParams (ConversationID, Summary, Trigger, Model, CheckpointSeq, TokensBefore, TokensAfter, Usage), Compaction.ID, LatestTurnSeq.
    - internal/conversations/tiktoken.go:90 (countTokens) + encoder() — the D-04 floor's input-token estimate (gating-grade).
    - internal/runner/fakes_test.go:54 (fakeConvStore.manErr — the injectable LoadManagedHistory error) — extend with a RecordCompaction spy + a LatestTurnSeq stub + LoadHistory turns fixture.
    - 42-CONTEXT.md D-04 (derived floor shape + tunable ~3 / 2× constants), D-05 (model default), D-08 (shared path) + 42-RESEARCH.md Pattern 1 (shared seam), § Anti-Patterns (checkpoint_seq from snapshot), OQ1 (manual-path concurrency — pass snapshot seq; optionally hold the per-conversation lock), OQ3 (apply WithTimeout to the manual path too; reserve WithoutCancel for auto).
  </read_first>
  <behavior>
    - Runner.Compact happy: LoadHistory(convID) → floor passes → CompactConversation(effectiveModel, history, {MaxOutputTokens:effective, Focus:req.Focus}) → capture checkpointSeq = LatestTurnSeq(convID) snapshot → RecordCompaction({..., Usage}) → CompactOutcome{TokensBefore, TokensAfter, CompactionID:rec.ID, NoOp:false}.
    - Runner.Compact below-floor (D-04): body turns ≤ ~3 OR estimated input tokens < 2× effective MaxOutputTokens → CompactOutcome{NoOp:true}, nil, no LLM call, no RecordCompaction.
    - Runner.Compact compaction error: CompactConversation returns err → Runner.Compact returns the err (the manual callers surface it; the auto caller maps it to the original window error).
    - effective model/budget: effectiveModel = r.compactModel!="" ? r.compactModel : r.cfg.Model (D-05); effectiveMax = r.compactMaxOutput>0 ? r.compactMaxOutput : 4096.
    - ctx bound (OQ3): Runner.Compact bounds the LLM call with WithTimeout(r.compactTimeout) regardless of trigger; the WithoutCancel wrap is applied by the AUTO caller (loadTurnHistory), not the manual callers.
    - interface: fakeConvStore records the RecordCompaction params (spy) and returns a canned Compaction; LatestTurnSeq returns a stubbed seq.
  </behavior>
  <action>
    Create `internal/runner/runner_compact.go` (`package runner`). Define `CompactRequest{Trigger string; Focus string}` and `CompactOutcome{TokensBefore int; TokensAfter int; CompactionID string; NoOp bool}`. Implement `(r *Runner) Compact(ctx context.Context, convID string, req CompactRequest) (CompactOutcome, error)`: (1) `hist, err := r.Conv.LoadHistory(ctx, convID)` (raw byte-identical); (2) D-04 floor: count body turns (exclude the system L0 message) and estimate input tokens via `countTokens(encoder(), renderedHist)` — if `bodyTurns <= 3 || inputTokens < 2*effectiveMax` return `CompactOutcome{NoOp:true}, nil` (constants tunable within D-04); (3) bound the call: `cctx, cancel := context.WithTimeout(ctx, r.compactTimeout); defer cancel()` (OQ3 — manual path bounded too); (4) `res, err := conversations.CompactConversation(cctx, r.client, effectiveModel, hist, conversations.CompactOptions{MaxOutputTokens: effectiveMax, Focus: req.Focus})` — propagate err; (5) `checkpointSeq, err := r.Conv.LatestTurnSeq(ctx, convID)` captured as the snapshot watermark (NOT recomputed in the persist tx — RESEARCH anti-pattern; OQ1: optionally take the per-conversation runner lock around load→persist for full manual-path serialization); (6) `rec, err := r.Conv.RecordCompaction(ctx, conversations.RecordCompactionParams{ConversationID: convID, Summary: res.Summary, Trigger: req.Trigger, Model: effectiveModel, CheckpointSeq: checkpointSeq, TokensBefore: res.TokensBefore, TokensAfter: res.TokensAfter, Usage: res.Usage})` — this folds the Usage into the conversation aggregates (COMPACT-10); (7) return `CompactOutcome{res.TokensBefore, res.TokensAfter, rec.ID, false}`. Widen `internal/runner/interfaces.go` ConversationStore with `RecordCompaction(ctx, conversations.RecordCompactionParams) (conversations.Compaction, error)` and `LatestTurnSeq(ctx, conversationID) (int, error)` (LoadHistory already present). Add the Deps + unexported Runner fields in `runner.go` (mirror EvictAfter→evictAfter): `CompactAutoEnabled bool`/`compactAutoEnabled`, `CompactTimeout time.Duration`/`compactTimeout`, `CompactMaxOutput int`/`compactMaxOutput`, `CompactModel string`/`compactModel`; set them in runner.New from Deps (default compactTimeout to 60s and compactMaxOutput to 4096 if the Deps value is zero, so non-chat_boot constructors are safe). Extend `fakes_test.go` fakeConvStore with a RecordCompaction spy (records params, returns a canned Compaction), a LatestTurnSeq stub, and a LoadHistory turns fixture. Accept-interfaces/return-structs (golang-structs-interfaces): no premature interface beyond the ConversationStore widening.
  </action>
  <acceptance_criteria>
    - `runner_compact.go` declares `Runner.Compact`, `CompactRequest`, `CompactOutcome`; it calls `conversations.CompactConversation` then `r.Conv.RecordCompaction` with the Usage; the D-04 floor short-circuits to `NoOp:true` before any LLM call.
    - `checkpointSeq` is captured from `LatestTurnSeq` at load time and passed as `RecordCompactionParams.CheckpointSeq` (grep confirms no post-summary re-read of max seq for the checkpoint).
    - `interfaces.go` ConversationStore has `RecordCompaction` + `LatestTurnSeq`; `runner.go` has the four Deps + four unexported fields.
    - `cd D:/Repo/Aura && go test ./internal/runner/ -run 'Compact'` passes (happy, below-floor NoOp, compaction-error propagation, effective-model/budget, Usage-in-RecordCompaction).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/runner/ && go build ./... && go test ./internal/runner/ -run 'Compact'</automated>
  </verify>
  <done>One shared Runner.Compact loads raw history, applies the derived D-04 no-op floor, summarizes with the D-05 effective model, captures the snapshot checkpoint, and persists with the Usage billed to the conversation — the single seam every trigger reaches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: loadTurnHistory auto-fallback — inline-bounded, one attempt, no loop, toggleable (COMPACT-08/11)</name>
  <files>internal/runner/runner.go, internal/runner/runner_compact_test.go, internal/runner/fakes_test.go</files>
  <read_first>
    - internal/runner/runner.go:485 (loadTurnHistory — the exact ErrContextWindowExceeded auto-fallback seam; the branchLeaf>0 vs LoadManagedHistory load closure) — 42-RESEARCH.md Pattern 3 gives the target rewrite verbatim.
    - internal/conversations/context.go:62 (ErrContextWindowExceeded) — the sentinel to match with errors.Is.
    - internal/runner/fakes_test.go:54 (fakeConvStore.manErr) — the injectable LoadManagedHistory error; extend to a "manErr-once" mode (fire on load #1, clear for load #2) for the one-attempt assertion.
    - internal/runner/main_test.go / the goleak TestMain (if present) — the -race + goleak harness the new test joins.
    - 42-RESEARCH.md Pattern 3 (inline-bounded, WithoutCancel+WithTimeout, no goroutine, one re-load) + Pitfall 3 (lifecycle) + § Anti-Patterns (no retry loop).
  </read_first>
  <behavior>
    - toggle off: r.compactAutoEnabled=false → loadTurnHistory returns the load result/err unchanged (ErrContextWindowExceeded still a dead-end; L1/L2.5 unchanged).
    - happy auto: load #1 → ErrContextWindowExceeded; r.compactAutoEnabled=true → exactly one Runner.Compact(trigger="auto") (one RecordCompaction spy hit); load #2 succeeds → returns the compacted history, nil.
    - still-over-cap: load #1 err; compaction succeeds; load #2 STILL ErrContextWindowExceeded → returns the ORIGINAL error exactly once (no third load, no loop).
    - compaction failure: load #1 err; Runner.Compact returns an error → loadTurnHistory returns the ORIGINAL ErrContextWindowExceeded (never the compaction error).
    - non-window error: load #1 returns some other error → returned as-is, no compaction attempt.
    - lifecycle: a stub client that blocks past r.compactTimeout → the compaction times out, the original window error surfaces, goleak clean (no dangling goroutine — the path is inline).
  </behavior>
  <action>
    Rewrite `loadTurnHistory` in `internal/runner/runner.go` per RESEARCH Pattern 3: keep the existing `load()` closure (branchLeaf>0 → LoadManagedHistoryForBranch, else LoadManagedHistory) BYTE-UNCHANGED for the no-error path; after `history, err := load()`, `if err == nil || !errors.Is(err, conversations.ErrContextWindowExceeded) || !r.compactAutoEnabled { return history, err }`; else `cctx, cancel := context.WithTimeout(context.WithoutCancel(ctx), r.compactTimeout); defer cancel()` and `if _, cErr := r.Compact(cctx, convID, CompactRequest{Trigger: "auto"}); cErr != nil { return nil, err }` (surface the ORIGINAL err); then re-load ONCE — `if history2, rErr := load(); rErr == nil { return history2, nil }`; `return nil, err` (still over cap → original error, once, no loop). Create `internal/runner/runner_compact_test.go` (UNTAGGED unit, `-race`): drive loadTurnHistory (or the exported Turn path that calls it) with the extended fakeConvStore manErr-once + RecordCompaction spy, asserting each <behavior> case; add the blocking-client timeout case and run under goleak (no dangling goroutine — the auto path is inline). Extend `fakes_test.go` with the manErr-once toggle if not added in Task 1.
  </action>
  <acceptance_criteria>
    - `loadTurnHistory` returns the load result unchanged when there is no error, a non-window error, or `compactAutoEnabled=false`; the compaction branch fires ONLY on `errors.Is(err, ErrContextWindowExceeded) && compactAutoEnabled`.
    - The auto branch wraps the call in `context.WithTimeout(context.WithoutCancel(ctx), r.compactTimeout)`, runs `r.Compact` at most once, re-loads at most once, and returns the ORIGINAL error on any failure/still-over-cap.
    - `runner_compact.go`/`runner.go` contain no `go func`/`r.wg` for compaction (grep confirms) — the auto path is inline.
    - `cd D:/Repo/Aura && go test -race ./internal/runner/ -run 'AutoCompact|Compact'` passes with goleak clean (one-attempt, no-loop, original-error, toggle-off, timeout).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/runner/ && go test -race ./internal/runner/ -run 'AutoCompact|Compact'</automated>
  </verify>
  <done>Auto-compaction replaces the ErrContextWindowExceeded dead-end with a single inline-bounded attempt + one re-load behind AURA_COMPACT_AUTO_ENABLED; it never loops, never surfaces a worse state than the original window error, adds no goroutine/goleak surface, and leaves L1/L2.5 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request ctx → auto compaction | A client disconnect mid-turn must not abort a half-written checkpoint or hang the turn. |
| turn loop → shared Compact | All five triggers funnel through one method; a duplicated impl would drift the safety/atomicity guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering / data corruption (half-written checkpoint under cancellation) | runner.go auto path | mitigate | context.WithoutCancel(ctx) + context.WithTimeout(r.compactTimeout) so a client disconnect can't corrupt the checkpoint; the persist itself is Plan 42-01's single db.WithTx. |
| T-42-07 | Denial of Service (hung turn / leaked goroutine / retry storm) | runner.go loadTurnHistory | mitigate | INLINE-BOUNDED (no goroutine → no new goleak surface); at most ONE auto attempt + ONE re-load (no retry loop); a timeout surfaces the original error. goleak + -race asserted. |
| T-42-13 | Repudiation / audit gap | runner_compact.go trigger tagging | mitigate | Every compaction writes a conversation_compactions row with trigger ('auto' here, 'manual' from the trigger surfaces) via RecordCompaction — the audit is inseparable from the persist. |
| T-42-14 | Data loss (racing turn folded under checkpoint) | runner_compact.go checkpoint capture | mitigate | checkpoint_seq captured from the summarized snapshot (LatestTurnSeq at load), passed to RecordCompaction; a turn racing in lands at seq>checkpoint and is preserved (RESEARCH anti-pattern + OQ1). |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new packages (RESEARCH § Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd D:/Repo/Aura && go vet ./internal/runner/ && go build ./... && go test -race ./internal/runner/ -run 'Compact|AutoCompact'` green with goleak clean.
- Manual-Only (VALIDATION): `go-mutesting` ≥70% killed on runner_compact.go + the loadTurnHistory change (WSL); an end-to-end auto-fallback against a live over-cap conversation (final DoD E2E, requires a provider key).
- Per-wave: `go test -race ./internal/conversations/ ./internal/runner/` (Plan 42-01 + this plan compile + pass together).
</verification>

<success_criteria>
- One shared Runner.Compact serves every trigger, applies the derived D-04 floor, uses the D-05 effective model, and bills the compaction Usage to the conversation (COMPACT-10, D-08).
- Auto-fallback replaces the dead-end with a single inline-bounded attempt behind AURA_COMPACT_AUTO_ENABLED, no loop, no worse-than-original state, no goleak surface, L1/L2.5 unchanged (COMPACT-08/COMPACT-11).
- Every path is proven daemon-free (-race + goleak) via fakeConvStore, contributing to the ≥85% owned-surface floor.
</success_criteria>

<output>
Create `.planning/phases/42-llm-conversation-compaction/42-04-SUMMARY.md` when done.
</output>
