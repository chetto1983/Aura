---
phase: 42-llm-conversation-compaction
plan: 05
type: execute
wave: 5
depends_on: [42-04]
files_modified: [internal/conversations/context.go, internal/conversations/compaction_budget.go, internal/runner/runner_compact.go, internal/conversations/compaction_overflow_test.go, internal/conversations/compaction_rebase.go, internal/conversations/compaction_rebase_test.go, internal/config/config_compaction.go]
autonomous: true
requirements: [IC-03, IC-07, IC-09, IC-11, IC-13, IC-14]
must_haves:
  truths: ["L2.4 attempt or allowed waiver always precedes L2.5", "Overflow is one bounded attempt and reconstruction", "Generation five rebases canonical units and failed rebase preserves LKG"]
  artifacts:
    - {path: internal/conversations/context.go, provides: "atomic BudgetDecision and L2.5 gate"}
    - {path: internal/conversations/compaction_rebase.go, provides: "hierarchical canonical rebase"}
  key_links:
    - {from: internal/conversations/context.go, to: internal/runner/runner_compact.go, via: "semantic candidate attempt/waiver before emergency candidate"}
---
<objective>Deliver slices 4 and 5: atomic proactive ladder, bounded overflow, recursive generations, hierarchical rebase, corruption recovery, and canary control primitives.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-04-SUMMARY.md
@internal/conversations/context.go</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Ship pure L2.4 decision seam atomically with L2.5 and overflow gates</name>
  <files>internal/conversations/context.go, internal/conversations/compaction_budget.go, internal/runner/runner_compact.go, internal/conversations/compaction_overflow_test.go</files>
  <behavior>At utilization >=0.80 or inadequate forecast headroom compact toward <=0.55; accept only max(4096,10% capacity) savings and 20% reduction; hysteresis prevents repetition; L2.5 requires success or disabled/no_eligible_prefix/provider_unsupported/quality_rejected/timeout waiver; overflow attempts once.</behavior>
  <action>Make LoadManagedHistory return pure BudgetDecision with projection, L1 edits, semantic and emergency candidates and reason; mutation belongs only in coordinator execution. Enforce exact allowed waiver enum and record it before any rot event. Invalid/fixed-exhausted capacity returns the locked error without persistence or emergency retry. Overflow performs one bounded compaction and one reconstruction, never loops, never activates mid-stream, and returns original/recoverable state on timeout or failure. Add shadow outcome evaluation and redacted metrics. Preserve the PATTERNS exact `context.go` pure-helper seam and runner one-shot retry conventions, while implementing the RESEARCH pure-ladder/no-analog L2.4-before-L2.5 waiver contract.</action>
  <verify><automated>go test -race ./internal/conversations ./internal/runner -run 'BudgetDecision|L24BeforeL25|Overflow|Hysteresis|Waiver' -count=1</automated></verify>
  <done>Tests prove semantic compaction precedes every eligible hard drop and failures are bounded/non-destructive.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add recursive generations, canonical hierarchical rebase, and deterministic canary controls</name>
  <files>internal/conversations/compaction_rebase.go, internal/conversations/compaction_rebase_test.go, internal/config/config_compaction.go</files>
  <behavior>Four incremental generations succeed; fifth rebases; coverage below 100%, entailment below .98, similarity below .90, or depth four forces rebase; chunks <=60% summarizer capacity; failure keeps LKG and disables auto generations.</behavior>
  <action>Implement frozen/versioned probe/scorer/model/prompt contract and hierarchical reduction of canonical semantic units carrying complete manifests, ledgers and artifact references. Store every interpretation version. Add deterministic tenant/conversation sampling controls for disabled/shadow/1/5/20/50 percent modes, default disabled; configuration rejects illegal ratios and incompatible activation. Quarantine corruption and require successful recovery drills before enabled mode. For `internal/conversations/compaction_rebase.go`, reuse the bounded reconstruction/LKG recovery flow in `internal/conversations/context.go` identified by `42-PATTERNS.md` under "Recovery and rebase", and implement the explicit no-analog hierarchical reduction/drift scorer contract in `42-RESEARCH.md` Pattern 6. For `internal/config/config_compaction.go`, mirror the layered validation/defaulting shape in `internal/config/config_knobs.go` and `internal/llm/config.go` cited by `42-PATTERNS.md`; sampling-stage compatibility rules are the no-analog contract in `42-RESEARCH.md` "Rollout Architecture".</action>
  <verify><automated>go test ./internal/conversations ./internal/config -run 'Generation|Rebase|Drift|Canary|CompactionConfig' -count=1</automated></verify>
  <done>Recursive/rebase fixtures have no duplicate-tail facts and failed rebase cannot change active pointer.</done>
</task>
</tasks>
<verification>go test -race ./internal/conversations ./internal/runner ./internal/config -run 'Compaction|Rebase|Overflow|Canary' -count=1</verification>
<success_criteria>IC-03/07/09/11 atomic ladder, overflow, rebase, and canary primitives pass while enabled activation remains gated.</success_criteria>
<output>Create `42-05-SUMMARY.md`; atomic ladder and recursive/canary commits.</output>
