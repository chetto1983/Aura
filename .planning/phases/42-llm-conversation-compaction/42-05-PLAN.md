---
phase: 42-llm-conversation-compaction
plan: 05
type: execute
wave: 3
depends_on: ["42-03", "42-04"]
files_modified:
  - cmd/aura/chat_boot.go
  - cmd/aura/chat.go
  - cmd/aura/chat_compact.go
  - cmd/aura/chat_repl.go
  - internal/channels/telegram/commands.go
  - internal/channels/telegram/commands_test.go
autonomous: true
requirements: [COMPACT-05, COMPACT-06, COMPACT-07, COMPACT-09]
must_haves:
  truths:
    - "COMPACT-09 wiring: assembleChatEnv threads all four AURA_COMPACT_* Config fields into runner.Deps (CompactAutoEnabled, CompactTimeout=Duration(CompactTimeoutSec)s, CompactMaxOutput, CompactModel) exactly like EvictAfter — so both the manual and auto paths are configured from one composition root"
    - "COMPACT-05 CLI: `aura chat compact <id> [--focus \"...\"]` boots via bootChat, calls env.run.Compact, prints `tokens_before → tokens_after (Δ%)` + the compaction id, and writes the watermark + summary turn; --focus threads into CompactRequest.Focus"
    - "COMPACT-05 no-op (D-04): a below-floor conversation prints a clear 'nothing to compact' and exits 0 (no row written) — the CompactOutcome.NoOp branch"
    - "COMPACT-06 REPL: dispatchSlash(line) intercepts /compact [focus] in chatLoop AFTER the /exit check and BEFORE the turn dispatch — it runs the compaction call only (no runner.Turn) and prints the delta; /bogus returns 'unknown command' and never reaches the LLM; a normal message runs a turn unchanged"
    - "COMPACT-07 Telegram: /compact is added to dispatchRich, intercepted BEFORE any LLM turn (like /clear — never forwarded to handleTurn), compacts convID(chatID), and replies with the token delta; helpText advertises /compact"
    - "shared path (D-08): all three manual surfaces call the SAME Runner.Compact (Plan 42-04) — no duplicate compaction implementation in cmd/aura or telegram"
  artifacts:
    - path: "cmd/aura/chat_compact.go"
      provides: "chatCompact(args) — boot, --focus parse, env.run.Compact, token-delta print, NoOp 'nothing to compact' exit 0"
      contains: "func chatCompact"
    - path: "cmd/aura/chat.go"
      provides: "case \"compact\": chatCompact(args[1:]) in runChat + chatUsage update"
      contains: "compact"
    - path: "cmd/aura/chat_repl.go"
      provides: "dispatchSlash(line) (handled bool, out string) slash router + /compact handling in chatLoop"
      contains: "dispatchSlash"
    - path: "internal/channels/telegram/commands.go"
      provides: "/compact case in dispatchRich + compactBackend seam in commandDeps + helpText update"
      contains: "/compact"
    - path: "cmd/aura/chat_boot.go"
      provides: "runner.Deps compact-field wiring in assembleChatEnv"
      contains: "CompactAutoEnabled"
  key_links:
    - from: "cmd/aura/chat_boot.go assembleChatEnv"
      to: "runner.Deps compact fields"
      via: "cfg.CompactAutoEnabled/CompactMaxOutputTokens/CompactTimeoutSec/CompactModel → Deps (mirror EvictAfter)"
      pattern: "CompactAutoEnabled"
    - from: "cmd/aura/chat_compact.go + chat_repl.go + telegram/commands.go"
      to: "runner Runner.Compact"
      via: "env.run.Compact / d.run.Compact / compactBackend.Compact — the single shared seam"
      pattern: "Compact"
  prohibitions:
    - "MUST NOT duplicate the compaction implementation — CLI/REPL/Telegram are thin dispatch into Runner.Compact (Plan 42-04). Verify: no CompactConversation / RecordCompaction call in cmd/aura or telegram; only Runner.Compact."
    - "MUST NOT forward a handled /compact to the LLM — in the REPL and Telegram a handled command runs the compaction call only, NEVER runner.Turn/handleTurn (the T-13-06 command-interception invariant). Verify: REPL test asserts /compact and /bogus never call runner.Turn; telegram test asserts dispatch(handled=true) is not forwarded."
    - "MUST NOT error on a below-floor manual compaction — the CLI prints 'nothing to compact' and exits 0; the REPL/Telegram surface it as a non-error notice (D-04). Verify: CLI NoOp test asserts exit 0 + no row."
    - "MUST NOT reach the LLM for /bogus — an unknown slash returns the hint 'unknown command; type your message or /exit' and is not sent as a turn (SPEC Req#6). Verify: REPL test."
---

<objective>
Wire the three manual `/compact` trigger surfaces — CLI (`aura chat compact <id> [--focus]`), the interactive REPL (`/compact`, via the REPL's first real slash router), and Telegram (`/compact` + `/help`) — plus thread the four `AURA_COMPACT_*` config values into `runner.Deps` at the composition root (COMPACT-05/06/07/09). Every surface is thin dispatch into the shared `Runner.Compact` from Plan 42-04 (D-08: no duplicate implementation).

Purpose: literal Claude Code `/compact` parity across all three channels, and the composition wiring that configures BOTH the manual and auto paths from one place. `cmd/aura` is excluded from the ≥85% Go floor (behaviorally covered), so the floor-critical logic stays in `Runner.Compact`/`conversations`; `internal/channels/telegram` IS owned surface, so its dispatcher is unit-tested here with a `compactBackend` double.
Output: `chat_boot.go` Deps wiring, `chat.go` switch + `chat_compact.go` (new), `chat_repl.go` `dispatchSlash`, `telegram/commands.go` `/compact` + `/help` + the `compactBackend` seam, and the telegram dispatch test.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-VALIDATION.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<artifacts_produced>
This plan adds these NEW symbols (exclude from source-grounding drift verification — net-new):
- `cmd/aura/chat_compact.go` — `chatCompact(args []string)`.
- `cmd/aura/chat.go` — `case "compact": chatCompact(args[1:])` + `chatUsage` line.
- `cmd/aura/chat_repl.go` — `dispatchSlash(line string) (handled bool, out string)` + the chatLoop hook.
- `internal/channels/telegram/commands.go` — `case "/compact":` + a `compactBackend` consumer-side interface in commandDeps + helpText line.
- `cmd/aura/chat_boot.go` — the four runner.Deps compact-field assignments.
Consumes from Plan 42-04: `Runner.Compact`, `CompactRequest`, `CompactOutcome`, the runner.Deps compact fields. From Plan 42-03: the Config compact fields.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: chat_boot.go Deps wiring + CLI `aura chat compact <id> [--focus]` (COMPACT-05/09, D-04)</name>
  <files>cmd/aura/chat_boot.go, cmd/aura/chat.go, cmd/aura/chat_compact.go</files>
  <read_first>
    - cmd/aura/chat_boot.go:205 (assembleChatEnv), :323/:337 (the Deps{… EvictAfter: cfg.ContextToolEvictAfterTurns …} thread) — the exact field-wiring precedent (42-PATTERNS.md § chat_boot.go Analog C). Note bootChat(ctx) returns env{run, conv, close}.
    - cmd/aura/chat.go:43 (runChat switch list|new|resume|rename|search) + :129 (chatRename — the sibling handler shape: bootChat, defer env.close(), call, print, os.Exit on error) + the flagValue/--limit parse helper chatSearch uses.
    - internal/runner/runner_compact.go (Plan 42-04) — Runner.Compact signature, CompactRequest{Trigger,Focus}, CompactOutcome{TokensBefore,TokensAfter,CompactionID,NoOp}.
    - internal/config/config.go (Plan 42-03) — CompactAutoEnabled/CompactMaxOutputTokens/CompactTimeoutSec/CompactModel.
    - 42-CONTEXT.md D-04 (below-floor "nothing to compact") + 42-PATTERNS.md § chat.go/chat_compact.go.
  </read_first>
  <action>
    In `cmd/aura/chat_boot.go assembleChatEnv`, add the four compact fields to the `runner.Deps` literal (mirror EvictAfter): `CompactAutoEnabled: cfg.CompactAutoEnabled`, `CompactTimeout: time.Duration(cfg.CompactTimeoutSec) * time.Second`, `CompactMaxOutput: cfg.CompactMaxOutputTokens`, `CompactModel: cfg.CompactModel`. In `cmd/aura/chat.go`, add `case "compact": chatCompact(args[1:])` to the runChat switch and add a `compact <id> [--focus "..."]` line to `chatUsage`. Create `cmd/aura/chat_compact.go`: `chatCompact(args []string)` — require an `<id>` (else print usage + os.Exit(1)); parse an optional `--focus "<instruction>"` via the existing flagValue helper; `ctx := context.Background(); env := bootChat(ctx); defer env.close()`; `out, err := env.run.Compact(ctx, id, runner.CompactRequest{Trigger: "manual", Focus: focus})`; on error print to stderr + os.Exit(1); if `out.NoOp` print "nothing to compact" and return (exit 0, no row); else print `tokens_before → tokens_after (Δ%)` and the compaction id (`out.CompactionID`). Keep chat_compact.go a thin controller — the floor logic is in Runner.Compact.
  </action>
  <acceptance_criteria>
    - `chat_boot.go` sets all four runner.Deps compact fields from cfg (grep shows CompactAutoEnabled + CompactTimeout + CompactMaxOutput + CompactModel).
    - `chat.go` runChat has `case "compact"`; `chatUsage` documents `compact <id> [--focus]`.
    - `chat_compact.go` calls `env.run.Compact` with Trigger "manual", threads --focus into CompactRequest.Focus, prints the delta + id on success and "nothing to compact" (exit 0) on NoOp; it does NOT call CompactConversation/RecordCompaction directly.
    - `cd D:/Repo/Aura && go build ./cmd/aura/ && go vet ./cmd/aura/` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./cmd/aura/ && go build ./cmd/aura/ && grep -n "CompactAutoEnabled" cmd/aura/chat_boot.go</automated>
  </verify>
  <done>`aura chat compact <id> [--focus]` compacts a conversation via the shared Runner.Compact, prints the token delta + compaction id, no-ops cleanly below the floor, and the four AURA_COMPACT_* knobs are wired into runner.Deps from the composition root.</done>
</task>

<task type="auto">
  <name>Task 2: chat_repl.go dispatchSlash — the REPL's first slash router + /compact (COMPACT-06)</name>
  <files>cmd/aura/chat_repl.go</files>
  <read_first>
    - cmd/aura/chat_repl.go:51 (chatLoop), :68 (the /exit special-case), :72 (the runUserTurn turn dispatch) — dispatchSlash slots between :68 and :72 (42-PATTERNS.md § chat_repl.go). The REPL deps struct d carries d.run (the Runner), d.out, d.convID.
    - internal/channels/telegram/commands.go dispatchRich (see Task 3) — the command-interception-before-turn pattern the REPL mirrors (the REPL gains its FIRST slash router).
    - internal/runner/runner_compact.go (Plan 42-04) — Runner.Compact.
    - 42-SPEC.md Req#6 (unknown slash → hint, never LLM; /compact runs the compaction call only; non-slash unchanged) + 42-PATTERNS.md § "Command-interception-before-LLM-turn".
  </read_first>
  <action>
    In `cmd/aura/chat_repl.go`, add `dispatchSlash(line string) (handled bool, out string)` (structured as a switch/table so future slash commands slot in). For a line starting with "/": `/compact` (optionally `/compact <focus text>`) → call `d.run.Compact(ctx, d.convID, runner.CompactRequest{Trigger: "manual", Focus: <rest>})`, format the token delta (or "nothing to compact" on NoOp, or the error text) into `out`, return handled=true — NO runner.Turn; any other `/`-prefixed input → return handled=true with `out = "unknown command; type your message or /exit"` (never sent to the LLM). A non-slash line → return handled=false (unchanged path). Invoke `dispatchSlash` in `chatLoop` immediately AFTER the `/exit` check and BEFORE `runUserTurn`: if handled, write `out` to `d.out` and continue the loop; else fall through to the existing turn dispatch. Keep it a thin controller into the shared Runner.Compact.
  </action>
  <acceptance_criteria>
    - `chat_repl.go` declares `dispatchSlash`; chatLoop invokes it after the /exit check and before runUserTurn.
    - `/compact` calls `d.run.Compact` (Trigger "manual") and prints the delta without a turn; `/bogus` returns the "unknown command" hint and does not reach runUserTurn; a normal line falls through to the existing turn dispatch unchanged.
    - `chat_repl.go` calls no CompactConversation/RecordCompaction directly.
    - `cd D:/Repo/Aura && go build ./cmd/aura/ && go vet ./cmd/aura/` succeeds; the existing REPL tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./cmd/aura/ && go build ./cmd/aura/ && go test ./cmd/aura/ -run 'Repl|Chat|Slash'</automated>
  </verify>
  <done>The REPL gains a real slash router: /compact compacts the active conversation via Runner.Compact and prints the delta without an LLM turn, /bogus returns a hint and never reaches the LLM, and normal messages run a turn unchanged — structured so future slash commands slot in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Telegram /compact + /help + compactBackend seam (COMPACT-07)</name>
  <files>internal/channels/telegram/commands.go, internal/channels/telegram/commands_test.go</files>
  <read_first>
    - internal/channels/telegram/commands.go:128 (dispatchRich switch), :153/:216 (the /clear interception — handled BEFORE any LLM turn; fireCancel-then-act), :169 (helpText), :50 (clearBackend — the consumer-side backend seam declared in commandDeps that keeps the dispatcher unit-testable with a double), :123 (the dispatch returns handled=true so a command NEVER reaches handleTurn — the T-13-06 invariant). All excerpted in 42-PATTERNS.md § telegram/commands.go.
    - internal/channels/telegram/commands_test.go — the existing dispatch test harness (scripted deps + a backend double) to extend for /compact.
    - internal/runner/runner_compact.go (Plan 42-04) — Runner.Compact (the concrete the compactBackend interface abstracts).
    - 42-SPEC.md Req#7 (/compact bot-intercepted, never forwarded; /help lists it) + 42-RESEARCH.md OQ1 (concurrent live turn — pass snapshot seq, handled inside Runner.Compact).
  </read_first>
  <behavior>
    - dispatch(ctx, chatID, "/compact"): handled=true, NOT forwarded to handleTurn; calls compactBackend.Compact(convID(chatID)); replies with the token delta (or a below-floor "nothing to compact" notice, or a user-facing error).
    - dispatch(ctx, chatID, "/compact focus text"): the focus text rides CompactRequest.Focus.
    - /help: the reply text lists /compact.
    - a normal message: unchanged (still reaches handleTurn).
  </behavior>
  <action>
    In `internal/channels/telegram/commands.go`, add a `compactBackend` consumer-side interface to `commandDeps` (mirror clearBackend at :50 — a narrow `Compact(ctx, convID string, req runner.CompactRequest) (runner.CompactOutcome, error)`-shaped method, declared here so the dispatcher stays unit-testable with a double; the concrete *runner.Runner satisfies it). Add `case "/compact":` to `dispatchRich`, intercepted like `/clear` (handled=true, never reaching handleTurn): parse an optional focus suffix, call `c.deps.Compact.Compact(ctx, convID(chatID), runner.CompactRequest{Trigger: "manual", Focus: <rest>})`, and return `textReply(<token-delta-or-nothing-to-compact-or-error>)`. Unlike /clear, /compact does NOT delete and need not fireCancel. Update `helpText` (:169) to advertise `/compact`. Extend `commands_test.go`: assert `/compact` is handled and never forwarded, that the compactBackend double is called with convID(chatID) + the focus, that the reply carries the delta, that a below-floor NoOp yields a non-error notice, and that `/help` lists `/compact`.
  </action>
  <acceptance_criteria>
    - `commands.go` dispatchRich has `case "/compact":`, returns handled=true, and calls the `compactBackend` seam (grep shows the commandDeps field + the interface); helpText contains "/compact".
    - The dispatcher calls no CompactConversation/RecordCompaction directly — only the compactBackend.Compact seam (→ Runner.Compact).
    - `cd D:/Repo/Aura && go test ./internal/channels/telegram/ -run 'Compact|Dispatch|Help'` passes (handled-not-forwarded, focus threading, delta reply, NoOp notice, /help lists it).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/channels/telegram/ && go test ./internal/channels/telegram/ -run 'Compact|Dispatch|Help'</automated>
  </verify>
  <done>Telegram /compact is bot-intercepted (never forwarded to the LLM), compacts the chat's conversation via the shared Runner.Compact behind a testable compactBackend seam, replies with the token delta (or a below-floor notice), and /help advertises it — proven by the extended dispatch test on this owned surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user command (CLI/REPL/Telegram) → shared compaction | A handled /compact must run the compaction call ONLY, never leak into an LLM turn. |
| operator env → runner.Deps | The compaction knobs must reach the runner via one composition root, not per-surface re-reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-15 | Elevation / unexpected LLM invocation | REPL dispatchSlash + telegram /compact | mitigate | A handled command returns handled=true and runs the compaction call only — it NEVER calls runner.Turn/handleTurn (T-13-06 invariant); unit tests assert /compact and /bogus never reach the turn path. |
| T-42-16 | Tampering (drifted duplicate impl) | cmd/aura + telegram | mitigate | All three surfaces call the single Runner.Compact (D-08); grep asserts no CompactConversation/RecordCompaction in cmd/aura or telegram. |
| T-42-06 | Information disclosure (error text) | chat_compact.go / telegram reply | mitigate | Surfaced errors are the user-facing compaction error string (the manual path surfaces errors per SPEC Req#11); no DSN/internal detail is interpolated (the store errors are already wrapped in conversations/runner). |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new packages (RESEARCH § Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd D:/Repo/Aura && go vet ./cmd/aura/ ./internal/channels/telegram/ && go build ./... && go test ./cmd/aura/ ./internal/channels/telegram/ -run 'Compact|Repl|Dispatch|Help|Slash'` green.
- Manual-Only (VALIDATION / DoD E2E): `aura chat compact <id>` on a real multi-turn conversation prints the delta + writes the row; in-REPL /compact + /bogus behavior; Telegram /compact intercepted + /help lists it (live, requires a provider key).
</verification>

<success_criteria>
- Manual /compact works on all three channels via the single shared Runner.Compact, reporting the token delta, no-op'ing cleanly below the floor, never forwarding a handled command to the LLM (COMPACT-05/06/07).
- The four AURA_COMPACT_* knobs are threaded into runner.Deps from assembleChatEnv (COMPACT-09 wiring).
- The Telegram dispatcher (owned surface) is unit-tested; cmd/aura stays thin (floor logic in Runner.Compact/conversations).
</success_criteria>

<output>
Create `.planning/phases/42-llm-conversation-compaction/42-05-SUMMARY.md` when done.
</output>
