---
phase: 42-llm-conversation-compaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/llm/capabilities.go, internal/llm/capabilities_test.go, internal/llm/openai_compat/client.go, internal/llm/openai_compat/client_test.go, internal/agent/prompt/builder.go, internal/agent/prompt/builder_test.go, internal/conversations/compaction_budget.go, internal/conversations/compaction_budget_test.go, internal/conversations/semantic_units.go, internal/conversations/semantic_units_test.go, internal/conversations/compaction_metrics.go]
autonomous: true
requirements: [IC-01, IC-02, IC-13, IC-14]
must_haves:
  truths: ["Provider/model budgets are exact and fail closed before division", "Semantic units and recent tail are atomic and pairwise disjoint", "Slice 1 emits redacted shadow telemetry while activation remains disabled"]
  artifacts:
    - {path: internal/llm/capabilities.go, provides: "versioned provider capability contract"}
    - {path: internal/conversations/compaction_budget.go, provides: "pure exact budget calculation"}
    - {path: internal/conversations/semantic_units.go, provides: "semantic-unit selector and manifests"}
  key_links:
    - {from: internal/llm/capabilities.go, to: internal/conversations/compaction_budget.go, via: "validated window/output/estimator contract"}
    - {from: internal/conversations/semantic_units.go, to: internal/conversations/compaction_budget.go, via: "tail capacity and savings constraints"}
---

<objective>
Build delivery slice 1's deterministic provider, budget, semantic-selection, and telemetry foundation under the locked Section 17 contract. Purpose: make every later mutation depend on tested pure decisions. Output: capability registry, exact budget model, semantic-unit selector, and bounded redacted metrics.
</objective>

<execution_context>
@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@docs/superpowers/specs/2026-07-13-industrial-conversation-compaction-design.md
@internal/llm/config.go
@internal/conversations/context.go
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define and validate provider capabilities and exact budgets</name>
  <files>internal/llm/capabilities.go, internal/llm/capabilities_test.go, internal/llm/openai_compat/client.go, internal/llm/openai_compat/client_test.go, internal/agent/prompt/builder.go, internal/agent/prompt/builder_test.go, internal/conversations/compaction_budget.go, internal/conversations/compaction_budget_test.go</files>
  <behavior>Table cases cover valid, unknown/invalid/zero/negative/fixed-exhausted capacity, provider tokenization, fallback ceil(estimate*1.15)+256, 200-sample forecast, 1000-sample calibrated p99 error, ratio validation, and both savings gates.</behavior>
  <action>First add failing tables, then implement the versioned Section 17.12 contract (window/output, estimator/tokenizer, schemas, internal role, modalities, tool ordering, usage/cost, native metadata, retention/ZDR) and pure integer Section 17.1 calculator. For `internal/llm/capabilities.go`, follow the capability-table and explicit-default conventions in `internal/llm/model_reasoning_caps.go` and `internal/config/config.go`; for `internal/conversations/compaction_budget.go`, follow the pure-transform/error-sentinel conventions in `internal/conversations/context.go` and `internal/agent/budget.go`. The exhaustive current adapter table has rows for `openrouter`, `llamacpp`, and generic OpenAI-compatible endpoints (including configured `vllm`): declarations live in `internal/llm/capabilities.go`; envelope/request rendering lives in `internal/agent/prompt/builder.go` and `internal/llm/openai_compat/client.go`; row tests live in the three paired test files. Every row asserts tokenizer/estimator and calibration, schemas, non-authoritative internal envelope, modalities, tool-call/result ordering, usage/cost, provider-native metadata, retention, and ZDR. Unknown names and any incomplete row fail activation preflight with a typed reason; arbitrary compatibility config remains usable only with compaction disabled. `cmd/aura/llm_client.go` proves all current providers share this adapter; `internal/agent/prompt/cache_anthropic.go` is a projection rule, not another transport adapter. Return insufficient_input_capacity before ratio math and context_unavailable without persistence eligibility when fixed plus pending cannot fit. Record estimator/calibration versions and prevent double counting. Add bounded-label metric events with no content, identity, artifact name, or secret values. Keep activation default false and source files under 600 LOC. Scope note: the eight Task-1 files are one atomic adapter contract—declaration, two renderers, and their paired tests—while Task 2 is an independent three-file pure selector; splitting the declaration from either renderer would permit an unvalidated adapter state.</action>
  <verify><automated>go test ./internal/llm ./internal/llm/openai_compat ./internal/agent/prompt ./internal/conversations -run 'Capability|Budget|Estimator|Forecast|InternalContext|Envelope' -count=1</automated></verify>
  <done>All exact quantities/defaults and fail-closed branches are pinned; unsupported adapters cannot activate.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build semantic-unit selection and bounded recent-tail manifests</name>
  <files>internal/conversations/semantic_units.go, internal/conversations/semantic_units_test.go, internal/conversations/compaction_metrics.go</files>
  <behavior>Fixtures cover parallel calls/results, approvals, ask-user, retry, cancellation, missing/duplicate results, streams, malformed legacy rows, tail expansion, 20-percent cap, disjoint summarized/tail/protected/excluded sets, and reasoned no-ops.</behavior>
  <action>Model units keyed by parent turn, invocation, tool-call, approval, and response/stream IDs. Normalize only explicitly closable legacy rows; treat open/malformed/partial cycles as ineligible. Select a token tail and expand backward only to its complete unit while enforcing the capacity cap and minimum-saving prefix. Classify every captured turn exactly once and expose typed L1 edits only for reconstructable content, retaining authorized typed references. Emit structural shadow telemetry only. Follow the PATTERNS lifecycle analogs `internal/toolinvocations/store.go` and `internal/askuser/store.go` for typed states, while implementing RESEARCH Pattern 3's no-analog unified semantic-unit machine rather than the old message-pair selector.</action>
  <verify><automated>go test ./internal/conversations -run 'SemanticUnit|RecentTail|ManifestSelection|L1Edit' -count=1</automated></verify>
  <done>Selection never splits a unit, manifests are disjoint and complete, and no-op reasons are deterministic.</done>
</task>
</tasks>

<verification>go test -race ./internal/llm ./internal/llm/openai_compat ./internal/agent/prompt ./internal/conversations -run 'Capability|Budget|Estimator|Forecast|InternalContext|Envelope|SemanticUnit|RecentTail' -count=1</verification>
<success_criteria>IC-01/02 pure foundations pass unit/property/fuzz fixtures; activation stays disabled; telemetry is structurally redacted.</success_criteria>
<output>Create `.planning/phases/42-llm-conversation-compaction/42-01-SUMMARY.md`; commit each task atomically as `test(42-01): ...` then `feat(42-01): ...`.</output>
