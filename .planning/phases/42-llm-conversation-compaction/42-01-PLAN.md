---
phase: 42-llm-conversation-compaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/migrations/0036_conversation_compactions.up.sql
  - internal/db/migrations/0036_conversation_compactions.down.sql
  - internal/db/queries/conversation_compactions.sql
  - internal/db/sqlc/conversation_compactions.sql.go
  - internal/db/sqlc/querier.go
  - internal/db/sqlc/models.go
  - internal/conversations/context_compaction.go
  - internal/conversations/store_compact.go
  - internal/conversations/context.go
  - internal/conversations/context_compaction_test.go
  - internal/conversations/store_compact_test.go
  - internal/conversations/store_compact_fakedbtx_test.go
autonomous: true
requirements: [COMPACT-03, COMPACT-04, COMPACT-10]
must_haves:
  truths:
    - "COMPACT-03 atomic persist (D-01): RecordCompaction on an N-turn conversation writes EXACTLY one aura.conversation_compactions row + one new summary turn carrying __aura_compaction_summary__ in tool_call_id; all N original turns remain in conversation_turns and SearchConversationTurns still matches their content"
    - "COMPACT-03 atomicity: an injected mid-tx DB failure (fakeDBTX) leaves NEITHER the summary turn NOR the watermark row — both roll back inside one db.WithTx"
    - "COMPACT-04 reconstruction (D-02): given a turn list + a LatestCompaction at checkpoint_seq=K, managedFromTurns yields [system(seq=1), always-block, summary, turns with seq>K] and excludes every body turn with seq≤K; two consecutive LoadManagedHistory calls are byte-identical"
    - "COMPACT-04 KV-cache (D-02): messages[0] is byte-identical before and after compaction (the summary lands at messages[2]; the agent.SystemPrompt system turn at messages[0] is untouched)"
    - "COMPACT-04 protection (D-03): the summary turn survives a dropOldestPairs (L2.5) pass that drops other user/assistant pairs (isCompactionSummary guards it like isAlwaysBlock)"
    - "COMPACT-04 wire cleanliness (D-03): toMessages emits the summary as a clean role=user llm.Message with an EMPTY ToolCallID (the marker is stripped before the wire)"
    - "COMPACT-10 projection: ListCompactions(id) returns rows ordered created_at DESC with the correct fields; LatestCompaction returns the newest deterministically (ORDER BY created_at DESC, summary_turn_seq DESC LIMIT 1)"
    - "COMPACT-10 cost path: the summary turn's Usage (input/output/cached tokens + cost) placed in AppendTurnParams folds into conversations.total_input_tokens/total_output_tokens/total_cost_usd via the existing appendTurnWrites aggregate path — no separate aggregate call"
    - "migration 0036 applies as aura_migrate and is denied as aura_app; .down.sql reverses cleanly; a re-run is a no-op"
  artifacts:
    - path: "internal/db/migrations/0036_conversation_compactions.up.sql"
      provides: "aura.conversation_compactions table (id, conversation_id FK ON DELETE CASCADE, checkpoint_seq, summary_turn_seq, trigger CHECK IN (manual,auto), model, tokens_before, tokens_after, created_at) + index (conversation_id, created_at DESC) + aura_app/aura_migrate grants"
      contains: "conversation_compactions"
    - path: "internal/db/queries/conversation_compactions.sql"
      provides: "InsertConversationCompaction :one, GetLatestCompaction :one, ListCompactions :many, GetMaxTurnSeq :one"
      contains: "InsertConversationCompaction"
    - path: "internal/conversations/context_compaction.go"
      provides: "compactionSummaryMarker const, isCompactionSummary predicate, pure truncateAtCheckpoint(turns, checkpointSeq)"
      contains: "__aura_compaction_summary__"
    - path: "internal/conversations/store_compact.go"
      provides: "Compaction struct (plain Go, no json tags), RecordCompactionParams, RecordCompaction (one db.WithTx), LatestCompaction, ListCompactions, LatestTurnSeq"
      contains: "func (s *Store) RecordCompaction"
    - path: "internal/conversations/context.go"
      provides: "managedFromTurns checkpoint truncation, dropOldestPairs summary protection, toMessages marker strip"
      contains: "isCompactionSummary"
  key_links:
    - from: "internal/conversations/context.go managedFromTurns"
      to: "conversations.Store.LatestCompaction"
      via: "one lookup before applyContextLadder, then truncateAtCheckpoint"
      pattern: "LatestCompaction"
    - from: "internal/conversations/store_compact.go RecordCompaction"
      to: "aura.conversation_turns + aura.conversation_compactions"
      via: "one db.WithTx: allocateTurnSeq -> AppendTurnTx(marker+Usage) -> InsertConversationCompaction"
      pattern: "db.WithTx"
    - from: "internal/conversations/context.go toMessages"
      to: "clean role=user llm.Message"
      via: "isCompactionSummary branch stripping the marker (mirror the isAlwaysBlock branch)"
      pattern: "isCompactionSummary"
  prohibitions:
    - "MUST NOT delete or rewrite pre-checkpoint turns from the DB — truncateAtCheckpoint reduces ONLY the in-context list; originals stay in conversation_turns (FTS + audit see them). Verify: db_integration counts N originals present after compaction and SearchConversationTurns still matches."
    - "MUST NOT let the summary turn reach the wire carrying its marker — toMessages emits role=user with empty ToolCallID (a user message must never carry a tool_call_id, Pitfall 4). Verify: unit asserts the emitted message ToolCallID == \"\"."
    - "MUST NOT recompute checkpoint_seq inside the persist tx — RecordCompaction takes CheckpointSeq as a param from the summarized snapshot's max(seq); a turn racing in between load and persist lands at seq>checkpoint and is PRESERVED (RESEARCH anti-pattern: in-tx max(seq) silently drops it). Verify: source shows CheckpointSeq is a param, never a max(seq) SELECT inside WithTx."
    - "MUST NOT insert the summary into the cached system prefix — messages[0] stays the agent.SystemPrompt constant byte-identical; the summary is messages[2] (D-02, Phase-6 KV-cache invariant). Verify: the cache-stable-prefix assertion on a compacted fixture."
    - "MUST NOT push context.go over 600 LOC — the new pure symbols (marker const, isCompactionSummary, truncateAtCheckpoint) live in context_compaction.go (D-B2 discretion, Pitfall 7); context.go gets only the three surgical edits. Verify: file-size gate (make quality) / wc -l context.go < 600."
---

<objective>
Build the non-destructive checkpoint-watermark persistence and the checkpoint-aware history reconstruction — the DB + reconstruction backbone every trigger surface later reuses. Migration `0036_conversation_compactions` + sqlc queries; the pure marker/transform primitives in a NEW `internal/conversations/context_compaction.go` (D-01/D-03); the atomic `store_compact.go` (`RecordCompaction`/`LatestCompaction`/`ListCompactions`, D-01/COMPACT-03/COMPACT-10); and the three surgical `context.go` edits that make loaded history start from the summary (D-02/COMPACT-04).

Purpose: This is the load-bearing slab. COMPACT-03 (atomic non-destructive persist), COMPACT-04 (checkpoint reconstruction + KV-cache preservation + marker protection), and COMPACT-10's `ListCompactions` projection + the cost-attribution mechanism (Usage rides `AppendTurnParams`) all land here. The daemon-free pure functions (`truncateAtCheckpoint`, `isCompactionSummary`, `toMessages` strip) are the highest-value coverage surface — they run in every test pass and keep the ≥85% owned-surface floor reachable even if a live DB tier is misconfigured (RESEARCH Validation Architecture).
Output: migration `0036` + `internal/db/queries/conversation_compactions.sql` (+ regenerated sqlc), `internal/conversations/{context_compaction.go, store_compact.go}` (new), edited `context.go`, and the unit + db_integration + fakeDBTX tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-VALIDATION.md
@.claude/skills/golang-database/SKILL.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<artifacts_produced>
This plan adds these NEW symbols (exclude from source-grounding drift verification — net-new):
- migration `0036_conversation_compactions.{up,down}.sql`; table `aura.conversation_compactions`.
- `internal/db/queries/conversation_compactions.sql` → sqlc `InsertConversationCompaction`, `GetLatestCompaction`, `ListCompactions`, `GetMaxTurnSeq` on the `Querier` (emit_interface:true).
- `context_compaction.go` — `compactionSummaryMarker` const, `isCompactionSummary(t Turn) bool`, `truncateAtCheckpoint(turns []Turn, checkpointSeq int) []Turn`.
- `store_compact.go` — `Compaction` struct, `RecordCompactionParams`, `(*Store) RecordCompaction`, `(*Store) LatestCompaction`, `(*Store) ListCompactions`, `(*Store) LatestTurnSeq`.
- `context.go` edits — checkpoint truncation in `managedFromTurns`, summary protection in `dropOldestPairs`, marker strip in `toMessages`.
Consumes: nothing from other Phase-42 plans (this is Wave 1, the foundation).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: migration 0036 + sqlc queries — the conversation_compactions DB layer (COMPACT-03/10)</name>
  <files>internal/db/migrations/0036_conversation_compactions.up.sql, internal/db/migrations/0036_conversation_compactions.down.sql, internal/db/queries/conversation_compactions.sql, internal/db/sqlc/conversation_compactions.sql.go, internal/db/sqlc/querier.go, internal/db/sqlc/models.go</files>
  <read_first>
    - internal/db/migrations/0005_conversations.up.sql (the context_rot_events table + the two-line GRANT pair aura_app/aura_migrate + COMMENT ON TABLE) — the DDL analog per 42-PATTERNS.md § migration.
    - internal/db/migrations/0035_assets_source_kind_agent.{up,down}.sql — CONFIRM this is the current head (0036 is the next free slot, RESEARCH Pitfall 5).
    - internal/db/queries/context_rot_events.sql — the `-- name: X :kind` sqlc annotation style, positional $N binds, explicit column lists (42-PATTERNS.md § conversation_compactions.sql).
    - sqlc.yaml — confirms sqlc reads the schema from internal/db/migrations, queries from internal/db/queries, generates into internal/db/sqlc; emit_interface:true.
    - 42-RESEARCH.md Pitfall 5 (migration + sqlc regen workflow) + Pitfall 6 (GetLatestCompaction determinism: ORDER BY created_at DESC, summary_turn_seq DESC LIMIT 1).
    - CLAUDE.md § Persistence + § migration numbering — 0036 applies as aura_migrate, denied as aura_app; .down.sql reverses; re-run no-op.
  </read_first>
  <action>
    Create `internal/db/migrations/0036_conversation_compactions.up.sql`: `CREATE TABLE aura.conversation_compactions` with columns `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `conversation_id uuid NOT NULL REFERENCES aura.conversations(id) ON DELETE CASCADE` (rot-events lack this FK; compactions need it, 42-PATTERNS.md), `checkpoint_seq integer NOT NULL`, `summary_turn_seq integer NOT NULL`, `trigger text NOT NULL CHECK (trigger IN ('manual','auto'))`, `model text`, `tokens_before integer NOT NULL DEFAULT 0`, `tokens_after integer NOT NULL DEFAULT 0`, `created_at timestamptz NOT NULL DEFAULT now()`; add `CREATE INDEX ... ON aura.conversation_compactions (conversation_id, created_at DESC)`; add the two GRANT lines (`GRANT SELECT, INSERT, UPDATE, DELETE ... TO aura_app;` and `GRANT ALL ... TO aura_migrate;`) mirroring 0005; add a `COMMENT ON TABLE`. Create the `.down.sql` = `DROP TABLE IF EXISTS aura.conversation_compactions;`. Create `internal/db/queries/conversation_compactions.sql` with four queries: `InsertConversationCompaction :one` (INSERT the 7 caller-supplied columns, `RETURNING *` so RecordCompaction gets id+created_at), `GetLatestCompaction :one` (`WHERE conversation_id=$1 ORDER BY created_at DESC, summary_turn_seq DESC LIMIT 1` — Pitfall 6 determinism), `ListCompactions :many` (`WHERE conversation_id=$1 ORDER BY created_at DESC` — COMPACT-10), and `GetMaxTurnSeq :one` (`SELECT COALESCE(max(seq),0)::int FROM aura.conversation_turns WHERE conversation_id=$1` — the snapshot checkpoint read Plan 42-04 consumes; NOT recomputed in-tx). Then run `sqlc generate` from the repo root and commit the regenerated `internal/db/sqlc/*` diff (new `conversation_compactions.sql.go`, widened `Querier`, new model struct).
  </action>
  <acceptance_criteria>
    - `internal/db/migrations/0036_conversation_compactions.up.sql` declares `aura.conversation_compactions` with the FK `REFERENCES aura.conversations(id) ON DELETE CASCADE`, the `trigger` CHECK constraint, the `(conversation_id, created_at DESC)` index, and both aura_app/aura_migrate grants; `.down.sql` is `DROP TABLE IF EXISTS aura.conversation_compactions;`.
    - `internal/db/queries/conversation_compactions.sql` contains `InsertConversationCompaction`, `GetLatestCompaction` (with `ORDER BY created_at DESC, summary_turn_seq DESC LIMIT 1`), `ListCompactions`, `GetMaxTurnSeq`.
    - After `sqlc generate`, `grep -n "InsertConversationCompaction\|GetLatestCompaction\|ListCompactions\|GetMaxTurnSeq" internal/db/sqlc/querier.go` shows all four on the interface.
    - `cd D:/Repo/Aura && go build ./internal/db/...` succeeds (generated code compiles).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go build ./internal/db/... && grep -n "InsertConversationCompaction" internal/db/sqlc/querier.go</automated>
  </verify>
  <done>Migration 0036 creates the watermark table (FK, CHECK, index, grants) with a clean reversible down; the four sqlc queries are generated onto the Querier; the DB layer compiles and is ready for store_compact.go.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: context_compaction.go (pure marker + truncation) + store_compact.go (atomic persist + projections) (D-01/COMPACT-03/COMPACT-10)</name>
  <files>internal/conversations/context_compaction.go, internal/conversations/store_compact.go, internal/conversations/context_compaction_test.go, internal/conversations/store_compact_test.go, internal/conversations/store_compact_fakedbtx_test.go</files>
  <read_first>
    - internal/conversations/context.go:48 (alwaysBlockMarker const) + :303 (isAlwaysBlock predicate) — copy the marker-in-ToolCallID trick VERBATIM, re-keyed (42-PATTERNS.md § context_compaction.go). Marker lives in ToolCallID, a field a real user turn never sets → collision-proof.
    - internal/conversations/store_append.go:34 (AppendTurnParams — ToolCallID + InputTokens/OutputTokens/CachedTokens/CostUSD fields), :64 (the db.WithTx wrapper shape), :116 (AppendTurnTx — exported for cross-write tx composition; requires Seq>0; rejects >turnCapBytes with ErrContentSpillUnsupported), :195 (allocateTurnSeq — row-locked monotonic seq in the caller's tx), :213 (appendTurnWrites folds Usage into total_* aggregates automatically), :251 (insertTurnAndAggregates).
    - internal/conversations/context.go:139 (RotEvent domain struct — plain Go types, NO json tags) + :151 (ListContextRotEvents — parseUUID guard → one sqlc call → map at the pgtype boundary) — the projection template for Compaction + ListCompactions/LatestCompaction (42-PATTERNS.md § store_compact.go Analog B).
    - internal/conversations/store.go — the Store struct (s.pool, s.q *sqlc.Queries), parseUUID helper, Turn struct (Seq/Role/Content/ToolCallID).
    - internal/conversations/store_fakedbtx_test.go + store_search_fakedbtx_test.go — the fake-DBTX rollback-injection pattern (daemon-free COMPACT-03 rollback coverage, Pitfall 2).
    - internal/conversations/main_test.go — the //go:build db_integration + goleak TestMain the store_compact_test.go joins.
    - 42-RESEARCH.md Pattern 2 (atomic checkpoint persist), Pattern 4 (truncateAtCheckpoint keeps seq==1 OR seq>checkpointSeq), § Anti-Patterns (checkpoint_seq from snapshot, not in-tx), OQ2 (4096-token summary ≈16KB << 65536 turnCapBytes — safe; clamp/document only above ~16k tokens).
  </read_first>
  <behavior>
    - truncateAtCheckpoint: keeps every turn with Seq==1 (system L0) OR Seq>checkpointSeq; drops all body turns with 1<Seq<=checkpointSeq; the summary turn (summary_turn_seq>checkpointSeq) survives for free; preserves input order; empty input → empty output; checkpointSeq=0 → returns all turns unchanged.
    - isCompactionSummary: true iff ToolCallID==__aura_compaction_summary__ AND Role==RoleUser; false for a normal user turn, an always-block turn, or a tool turn.
    - RecordCompaction happy: one db.WithTx allocates the summary seq, appends the summary turn (Role=user, ToolCallID=marker, Usage in the token/cost fields), inserts the watermark row (checkpoint_seq from params, summary_turn_seq=allocated seq) → returns the Compaction; all prior turns untouched.
    - RecordCompaction rollback: a fakeDBTX that fails on the watermark INSERT leaves NEITHER the summary turn NOR the watermark (the whole WithTx rolls back).
    - LatestCompaction: returns the newest row deterministically or nil when none exists (pgx.ErrNoRows → nil, not an error).
    - ListCompactions: rows created_at DESC; empty slice when none.
    - LatestTurnSeq: COALESCE(max(seq),0) for the conversation.
  </behavior>
  <action>
    Create `internal/conversations/context_compaction.go` (`package conversations`): declare `const compactionSummaryMarker = "__aura_compaction_summary__"`; `func isCompactionSummary(t Turn) bool` (mirror isAlwaysBlock — `t.ToolCallID == compactionSummaryMarker && t.Role == llm.RoleUser`); and the PURE `func truncateAtCheckpoint(turns []Turn, checkpointSeq int) []Turn` (keep `t.Seq == 1 || t.Seq > checkpointSeq`). Create `internal/conversations/store_compact.go` (`package conversations`): declare the domain `Compaction` struct with plain Go types and NO json tags (`ID, TS string; Trigger, Model string; CheckpointSeq, SummaryTurnSeq, TokensBefore, TokensAfter int`) and `RecordCompactionParams{ConversationID, Summary, Trigger, Model string; CheckpointSeq, TokensBefore, TokensAfter int; Usage *llm.Usage}`. Implement `(*Store) RecordCompaction(ctx, RecordCompactionParams) (Compaction, error)` as ONE `db.WithTx(ctx, s.pool, func(q *sqlc.Queries) error { ... })`: `seq, err := s.allocateTurnSeq(ctx, q, p.ConversationID)`; then `s.AppendTurnTx(ctx, q, AppendTurnParams{ConversationID, Seq:seq, Role:llm.RoleUser, Content:p.Summary, ToolCallID:compactionSummaryMarker, InputTokens/OutputTokens/CachedTokens/CostUSD from p.Usage})` (empty p.Summary → return an error BEFORE any write); then `q.InsertConversationCompaction(ctx, ...)` with `checkpoint_seq=p.CheckpointSeq` (the param — NEVER a recomputed max(seq), the RESEARCH anti-pattern) and `summary_turn_seq=int32(seq)`; map the RETURNING row into `Compaction` via a small `compactionFromRow`. Implement `(*Store) LatestCompaction(ctx, conversationID) (*Compaction, error)` (parseUUID → GetLatestCompaction; pgx.ErrNoRows → `nil, nil`), `(*Store) ListCompactions(ctx, conversationID) ([]Compaction, error)` (parseUUID → ListCompactions → map at the pgtype boundary, `.Valid`→RFC3339, int32→int, mirror ListContextRotEvents), and `(*Store) LatestTurnSeq(ctx, conversationID) (int, error)` (parseUUID → GetMaxTurnSeq). Write `context_compaction_test.go` (UNTAGGED unit): table-driven truncateAtCheckpoint (the [system, summary, turns>K] cases + seq≤K exclusion + empty + checkpoint=0) and isCompactionSummary (true/false matrix) — this is the mutation-≥70% target. Write `store_compact_fakedbtx_test.go` (UNTAGGED): inject a mid-tx failure on the watermark INSERT via the fake-DBTX pattern → assert neither row exists (COMPACT-03/11 rollback branch, daemon-free). Write `store_compact_test.go` (`//go:build db_integration`): seed an N-turn conversation, RecordCompaction → exactly one compaction row + one marked summary turn, all N originals still present, SearchConversationTurns still matches, the conversation token/cost aggregates rose by the Usage (COMPACT-10), ListCompactions order + LatestCompaction determinism; use the envOrSkip→t.Fatal-under-$CI discipline (Pitfall 9, no-skip-as-green).
  </action>
  <acceptance_criteria>
    - `context_compaction.go` declares `compactionSummaryMarker = "__aura_compaction_summary__"`, `isCompactionSummary`, and the pure `truncateAtCheckpoint`; no DB access in this file.
    - `store_compact.go` declares `Compaction` (no json tags), `RecordCompactionParams`, `RecordCompaction`, `LatestCompaction`, `ListCompactions`, `LatestTurnSeq`; RecordCompaction is one `db.WithTx`; the summary turn carries `ToolCallID: compactionSummaryMarker` and the Usage fields; `checkpoint_seq` is `p.CheckpointSeq` (grep confirms no `max(` / `GetMaxTurnSeq` call inside the WithTx body).
    - `cd D:/Repo/Aura && go test ./internal/conversations/ -run 'TruncateAtCheckpoint|IsCompactionSummary|RecordCompactionRollback'` passes (untagged unit + fakeDBTX rollback).
    - `cd D:/Repo/Aura && go test -tags db_integration -race -p1 ./internal/conversations/ -run 'RecordCompaction|ListCompactions'` passes with the stack up; a sub-second runtime is a skip tell (it must actually execute).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/conversations/ && go test ./internal/conversations/ -run 'TruncateAtCheckpoint|IsCompactionSummary|RecordCompactionRollback'</automated>
  </verify>
  <done>The marker/protection primitives and pure truncation live in context_compaction.go (context.go stays lean); RecordCompaction persists the summary turn + watermark atomically with the Usage folding into aggregates for free; LatestCompaction/ListCompactions/LatestTurnSeq expose the projections; rollback + originals-retained + FTS-still-matches are proven daemon-free and db_integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: context.go reconstruction — checkpoint truncation + summary protection + marker strip (COMPACT-04, D-02/D-03)</name>
  <files>internal/conversations/context.go, internal/conversations/context_compaction_test.go</files>
  <read_first>
    - internal/conversations/context.go:217 (managedFromTurns — a *Store method with ctx+conversationID → it can call s.LatestCompaction before applyContextLadder), :283/:287 (injectAlwaysBlock synthetic-turn shape — the summary lands at messages[2] after L0+always-block), :381/:386-392 (dropOldestPairs protected-head split — add the summary guard after the always-block guard), :446/:452 (toMessages — the isAlwaysBlock branch to mirror for the marker strip). All excerpted in 42-PATTERNS.md § context.go (three surgical touches).
    - internal/conversations/context.go:62 (ErrContextWindowExceeded — unchanged; the overflow path is Plan 42-04's concern), :228 (applyContextLadder), :151 (ListContextRotEvents — same *Store shape).
    - internal/conversations/context_compaction.go (Task 2) — the truncateAtCheckpoint + isCompactionSummary this task wires in.
    - the existing cache-stable-prefix / always-block reconstruction test (context_alwaysblock_test.go or equivalent) — extend it to a compacted conversation for the messages[0] byte-identity assertion (Pitfall 1, Phase-6 invariant).
    - 42-RESEARCH.md Pattern 4 (reconstruction is a pure transform gated by one DB lookup; the summary survives seq>checkpoint for free) + Pitfall 1 (KV-cache) + Pitfall 4 (marker consistent in 3 places).
  </read_first>
  <behavior>
    - managedFromTurns with a LatestCompaction at checkpoint_seq=K: reconstructs [system(seq=1), always-block, summary, turns with seq>K], excludes every body turn 1<seq<=K; with no compaction it is byte-identical to today's output (pure additive gate).
    - two consecutive LoadManagedHistory on a compacted conversation are byte-identical.
    - messages[0] is byte-identical before vs after compaction (the summary never enters the cached system prefix).
    - dropOldestPairs on a compacted history drops other user/assistant pairs but NEVER the summary turn.
    - toMessages emits the summary as role=user with ToolCallID=="" (marker stripped); a normal user turn is unchanged.
  </behavior>
  <action>
    Edit `internal/conversations/context.go` with three surgical touches (mirror the shipped always-block handling, 42-PATTERNS.md). (1) In `managedFromTurns`, BEFORE `applyContextLadder`, add `latest, err := s.LatestCompaction(ctx, conversationID)` (propagate a real error; nil = no compaction) then `if latest != nil { turns = truncateAtCheckpoint(turns, latest.CheckpointSeq) }` — the summary survives the seq>checkpoint filter and injectAlwaysBlock places it at messages[2]. (2) In `dropOldestPairs`, after the existing always-block protected-head bump (`if len(turns) > start && isAlwaysBlock(turns[start]) { start++ }`), add `if len(turns) > start && isCompactionSummary(turns[start]) { start++ }` so L2.5 never drops the summary. (3) In `toMessages`, add an `isCompactionSummary(t)` branch mirroring the isAlwaysBlock branch — emit `llm.Message{Role: llm.RoleUser, Content: t.Content}` with the marker stripped (a user message must not carry a tool_call_id on the wire, Pitfall 4). Keep context.go under 600 LOC (the pure symbols already live in context_compaction.go; these edits are ~8-12 LOC total). Refactor-on-touch: no dead code, comments updated. Extend `context_compaction_test.go` (UNTAGGED unit) with the reconstruction cases driven through managedFromTurns/toMessages against an in-memory turn list + a fake LatestCompaction (inject via a store test seam or a package-internal helper): the [system, always-block, summary, turns>K] shape, the byte-equality of two loads, the messages[0] byte-identity before/after (KV-cache), the summary-survives-dropOldestPairs case, and the toMessages marker-strip (emitted ToolCallID=="").
  </action>
  <acceptance_criteria>
    - `context.go` managedFromTurns calls `s.LatestCompaction` then `truncateAtCheckpoint`; dropOldestPairs has the `isCompactionSummary` protected-head bump; toMessages has the `isCompactionSummary` strip branch emitting empty ToolCallID.
    - `wc -l internal/conversations/context.go` < 600.
    - `cd D:/Repo/Aura && go test ./internal/conversations/ -run 'Reconstruct|Compaction|KVCacheStable|DropOldest'` passes (reconstruction shape, byte-equality x2, messages[0] identity, summary-protection, marker-strip).
    - The pre-existing always-block / cache-stable-prefix suite still passes (no regression to L0/L1/L2.5).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/conversations/ && go test -race ./internal/conversations/ -run 'Reconstruct|Compaction|KVCacheStable|DropOldest|AlwaysBlock'</automated>
  </verify>
  <done>Loaded history starts from the summary (messages[2]) after a checkpoint, is deterministic across two loads, keeps messages[0] byte-identical (KV-cache preserved), the summary is immune to L2.5, and it reaches the wire as a clean marker-free role=user message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner/store → Postgres | The summary turn + watermark row are written in one tx; a half-written checkpoint would corrupt reconstruction. |
| loaded summary text → LLM wire | The persisted summary is re-injected every subsequent turn; its marker must never reach the provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering / data corruption | store_compact.go RecordCompaction | mitigate | Summary turn + watermark row in ONE db.WithTx (allocateTurnSeq → AppendTurnTx → InsertConversationCompaction); a fakeDBTX mid-tx failure rolls back BOTH (daemon-free test) + a db_integration injected failure proves it live. checkpoint_seq is a param from the summarized snapshot (never recomputed in-tx) so a racing turn is preserved, never dropped. |
| T-42-04 | Tampering (wire malformation) | context.go toMessages | mitigate | toMessages strips the marker → the summary is a clean role=user message with empty ToolCallID; unit asserts ToolCallID=="" (Pitfall 4). |
| T-42-08 | Tampering (KV-cache / cost regression) | context.go managedFromTurns | mitigate | messages[0] stays the agent.SystemPrompt constant byte-identical; the summary is messages[2]; the cache-stable-prefix assertion runs on a compacted fixture (Phase-6 invariant, D-02). |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new packages (RESEARCH § Package Legitimacy Audit); `sqlc generate` regenerates in-repo code, not a dependency install. |
</threat_model>

<verification>
- `cd D:/Repo/Aura && go vet ./internal/conversations/ ./internal/db/... && go build ./... && go test -race ./internal/conversations/` green (untagged unit incl. truncate/isCompactionSummary/reconstruction/rollback).
- `cd D:/Repo/Aura && go test -tags db_integration -race -p1 ./internal/conversations/ -run 'RecordCompaction|ListCompactions|Reconstruct'` green with the stack up (atomic persist, originals-retained, FTS-still-matches, aggregate attribution).
- `bash scripts/coverage_docker.sh` (WSL, stack up) reports the owned-surface aggregate ≥85 with internal/conversations covered.
- Manual-Only (VALIDATION): migration 0036 up/down + aura_app deny as aura_migrate; KV-cache stable-prefix test on a compacted fixture; `go-mutesting` ≥70% killed on context_compaction.go (WSL).
- `make quality` file-size gate: context.go < 600 LOC.
</verification>

<success_criteria>
- Migration 0036 + sqlc queries ship; the DB layer compiles and applies/reverses cleanly (COMPACT-03).
- RecordCompaction persists the summary turn + watermark atomically and non-destructively (originals retained, FTS still matches), with the compaction Usage folded into conversation aggregates (COMPACT-03/COMPACT-10).
- Loaded history reconstructs from the checkpoint deterministically, preserves messages[0] (KV-cache), protects the summary from L2.5, and strips the marker on the wire (COMPACT-04).
- The pure primitives are exhaustively unit-tested (mutation ≥70% target); context.go stays < 600 LOC.
</success_criteria>

<output>
Create `.planning/phases/42-llm-conversation-compaction/42-01-SUMMARY.md` when done.
</output>
