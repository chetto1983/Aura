---
phase: 42-llm-conversation-compaction
plan: 03
type: execute
wave: 3
depends_on: [42-02]
files_modified: [internal/conversations/compaction_schema.go, internal/conversations/compaction_authority.go, internal/conversations/compaction_summarize.go, internal/llm/internal_context.go, testdata/compaction/adversarial.jsonl, internal/runner/runner_compact.go, internal/agui/conversations_api.go]
autonomous: true
requirements: [IC-06, IC-07, IC-11, IC-13, IC-14]
must_haves:
  truths: ["L0 and governance never enter summaries", "Structured summaries cannot become user authority", "Manual preview and restore share one coordinator and activation stays disabled"]
  artifacts:
    - {path: internal/conversations/compaction_schema.go, provides: "versioned summary schema and safety validator"}
    - {path: internal/conversations/compaction_authority.go, provides: "typed unresolved-instruction ledger"}
    - {path: internal/runner/runner_compact.go, provides: "unified bounded coordinator"}
  key_links:
    - {from: internal/conversations/compaction_summarize.go, to: internal/llm/internal_context.go, via: "validated non-authoritative envelope"}
    - {from: internal/runner/runner_compact.go, to: internal/conversations/store_compaction.go, via: "claim/infer/finalize and restore preview"}
---
<objective>Deliver slice 2: safe structured summarization, authority ledger, adversarial validation, and manual preview/restore through the common coordinator, still shadow-only.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-02-SUMMARY.md
@internal/conversations/title.go</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement structured summarizer, authority ledger, and injection validator</name>
  <files>internal/conversations/compaction_schema.go, internal/conversations/compaction_authority.go, internal/conversations/compaction_summarize.go, internal/llm/internal_context.go, testdata/compaction/adversarial.jsonl</files>
  <behavior>Reject malformed, empty, oversized, insufficiently reductive, policy-dropping, poisoned, authority-confusing, role/delimiter/encoded injection, fake summary, revoked instruction, and malicious quotation cases; preserve 100% L0 hash and typed unresolved ledger.</behavior>
  <action>Define versioned structured JSON, prompt/projection versions, typed ledger events with source sequence, authority, quoted-data encoding, state and revocation. Summarize without tools outside transactions; parse and deterministically validate schema, manifests, safety predicate, reduction, ledger and L0 hash before persistence. Render accepted data through adapter internal role or fixed developer-level non-authoritative instruction plus escaped data block; never interpolate quotations as instructions. Grow versioned adversarial fixtures toward the terminal 200-case corpus. Follow PATTERNS analogs `internal/conversations/title.go` and `internal/skills/manifest.go` for bounded LLM lifecycle and typed validation; implement RESEARCH Pattern 5's no-analog authority-ledger/internal-role safety contract explicitly.</action>
  <verify><automated>go test ./internal/conversations ./internal/llm -run 'SummarySchema|AuthorityLedger|Injection|InternalContext' -count=1</automated></verify>
  <done>No poisoned or authority-escalating summary activates; L0 and ledger retention are exact.</done>
</task>
<task type="auto">
  <name>Task 2: Wire manual coordinator, preview, restore, and safe trigger semantics</name>
  <files>internal/runner/runner_compact.go, internal/agui/conversations_api.go</files>
  <action>Expose one coordinator for manual/proactive/boundary/idle/model-safe-point/overflow triggers with stable operation ID and common checkpoint semantics. Add owner/capability-gated bounded preview and restore API; sanitize responses and distinguish disabled, unsupported, rejected, timeout, compacting, recovered, and unavailable. Never activate mid-stream; failure keeps prior projection. Keep semantic activation disabled in config and include rollback to prior pointer. Follow PATTERNS runner lifecycle helpers and the exact owner-gated `internal/agui/conversations_api.go` controller shape; implement the RESEARCH no-analog restore-preview and common durable-operation semantics.</action>
  <verify><automated>go test -race ./internal/runner ./internal/agui -run 'CompactCoordinator|Preview|Restore|Owner|SafePoint' -count=1</automated></verify>
  <done>All manual flows use the same durable protocol, authorization is fail-closed, and no partial write survives failure.</done>
</task>
</tasks>
<verification>go test -race ./internal/conversations ./internal/llm ./internal/runner ./internal/agui -run 'Compact|Summary|Authority|Restore' -count=1</verification>
<success_criteria>IC-06/07/11 safety and manual recovery pass adversarial and authorization tests without enabling live activation.</success_criteria>
<output>Create `42-03-SUMMARY.md`; atomic commits for validator and coordinator/API.</output>
