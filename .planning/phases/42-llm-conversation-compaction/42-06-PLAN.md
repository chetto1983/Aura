---
phase: 42-llm-conversation-compaction
plan: 06
type: execute
wave: 6
depends_on: [42-05]
files_modified: [internal/memory/compaction_candidates.go, internal/memory/compaction_policy.go, internal/memory/compaction_candidates_test.go, internal/db/migrations/0038_compaction_memory.up.sql, internal/db/migrations/0038_compaction_memory.down.sql, docs/security/compaction-memory-review.md]
autonomous: true
requirements: [IC-10, IC-13, IC-14]
must_haves:
  truths: ["Continuation summaries and durable memories remain separate", "Automatic promotion defaults off", "Retrieval/deletion obey tenant identity purpose region sensitivity and consent"]
  artifacts:
    - {path: internal/memory/compaction_candidates.go, provides: "typed idempotent candidate lifecycle"}
    - {path: docs/security/compaction-memory-review.md, provides: "separate privacy/security gate evidence"}
  key_links:
    - {from: internal/memory/compaction_policy.go, to: internal/memory/compaction_candidates.go, via: "promotion/retrieval/deletion policy"}
---
<objective>Deliver slice 6 as an independently reviewed durable-memory privacy lifecycle, separate from working context.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-05-SUMMARY.md
@internal/profile/store_fact.go</context>
<tasks>
<task type="auto">
  <name>Task 1: Persist typed candidates with additive privacy lifecycle</name>
  <files>internal/memory/compaction_candidates.go, internal/memory/compaction_candidates_test.go, internal/db/migrations/0038_compaction_memory.up.sql, internal/db/migrations/0038_compaction_memory.down.sql</files>
  <action>Add tenant/identity owner, purpose/consent basis, source manifest, minimized evidence digest, confidence, authority, sensitivity, region, encryption, retention/expiry, supersession and revocation. Idempotency spans restore/rebuild. Reject secrets and evidence over-collection. Keep automatic promotion false absent class policy. Add immutable source/reachability links and rollback-compatible migration; never store summary prose as promoted memory. Follow the PATTERNS partial analog `internal/profile/store_fact.go` and identity-scoped store conventions for typed persistence, while implementing RESEARCH Pattern 7's no-analog consent/region/source-manifest lifecycle.</action>
  <verify><automated>go test -tags=db_integration ./internal/memory -run 'CompactionCandidate|Idempotent|Secret|Migration0038' -count=1</automated></verify>
  <done>Duplicate rebuilds dedupe and source provenance remains minimized, encrypted, typed, and deletable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce retrieval, promotion, deletion, consent, and regional gates</name>
  <files>internal/memory/compaction_policy.go, internal/memory/compaction_candidates_test.go, docs/security/compaction-memory-review.md</files>
  <behavior>Cross-identity/region/purpose/capability/sensitivity denial; expiry, supersession, deletion, forget-me, consent withdrawal propagation; explicit class policy required for promotion.</behavior>
  <action>Apply relevance and recency only after hard tenant/identity/capability/purpose/region/sensitivity filters. Propagate source deletion, consent withdrawal and forget-me transactionally to candidates/memories and reachability. For `internal/memory/compaction_policy.go`, follow the identity-owner checks and explicit store-error mapping in `internal/profile/store_fact.go` plus the transactional deletion propagation shape in `internal/conversations/store.go`, as indexed by `42-PATTERNS.md` under "Durable memory"; consent/region/sensitivity ordering remains the explicit no-analog policy in `42-RESEARCH.md` Pattern 7. For `docs/security/compaction-memory-review.md`, follow the section/evidence/disposition format of `docs/security/identity-recovery-review.md` if present; if that file is absent, use the required threat-boundary, abuse-case, exact-test, residual-risk, and reviewer-gate schema stated in `42-RESEARCH.md` "Durable Memory Lifecycle" as an explicit documentation no-analog. Keep promotion and retrieval disabled until that review gate passes.</action>
  <verify><automated>go test -race -tags=db_integration ./internal/memory -run 'RetrievalGate|Promotion|Consent|Forget|Region|Deletion' -count=1</automated></verify>
  <done>Privacy lifecycle tests and review evidence show no cross-boundary retrieval or orphaned consent.</done>
</task>
</tasks>
<verification>go test -race -tags=db_integration ./internal/memory -count=1</verification>
<success_criteria>IC-10 lifecycle and independent security/privacy review pass; automatic promotion remains disabled.</success_criteria>
<output>Create `42-06-SUMMARY.md`; atomic candidate-schema and policy/review commits.</output>
