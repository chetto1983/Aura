---
phase: 42-llm-conversation-compaction
plan: 06
type: execute
wave: 3
depends_on: ["42-01", "42-04"]
files_modified:
  - internal/agui/conversations_api.go
  - internal/agui/server.go
  - internal/agui/types.go
  - internal/agui/conversations_api_compact_test.go
autonomous: true
requirements: [COMPACT-05, COMPACT-06, COMPACT-07, COMPACT-10]
must_haves:
  truths:
    - "D-08 (web trigger = 4th manual surface, extends COMPACT-05/06/07): POST /api/conversations/{id}/compact owner-gates the caller, runs the SHARED Runner.Compact (the agui server already holds a Runner), and returns {tokens_before, tokens_after, compaction_id} (snake_case handler struct); below-floor (D-04) → a 200 'nothing to compact' non-error notice"
    - "D-09 (gauge read route, extends COMPACT-10's ListCompactions projection): GET /api/conversations/{id}/compactions is a verbatim sibling of handleConversationRotEvents — owner-gate → s.conv.ListCompactions → writeJSON serializing the DOMAIN conversations.Compaction struct directly (NO agui DTO)"
    - "V4 owner-gate (SECURITY): both routes call GetForIdentity(ctx, id, scopedIdentityID(ctx)) BEFORE any read/mutate; a foreign/absent id 404s before the store/runner touch (IDOR denied) — proven by a foreign-id unit test on each route"
    - "V5 DoS guard: the POST body is capped with http.MaxBytesReader(w, r.Body, maxRunBodyBytes) (mirror handleRenameConversation); parseConvID rejects malformed ids with 404, never a 500 parse leak"
    - "V7 error hygiene: store/runner errors on both routes go through sanitizeErr/writeStoreErr — no DSN/internal detail reaches the wire"
    - "interface widening (PATTERNS correction): server.go Runner gains Compact; types.go ConversationStore gains ListCompactions beside ListContextRotEvents — NewServer signature is UNCHANGED and there is NO new JSON DTO"
  artifacts:
    - path: "internal/agui/conversations_api.go"
      provides: "handleConversationCompactions (GET) + handleCompactConversation (POST) + their mux registrations"
      contains: "compactions"
    - path: "internal/agui/server.go"
      provides: "Runner interface widened with Compact + the scriptedRunner test double's Compact spy"
      contains: "Compact"
    - path: "internal/agui/types.go"
      provides: "ConversationStore interface widened with ListCompactions + the fakeConvStore double"
      contains: "ListCompactions"
  key_links:
    - from: "internal/agui/conversations_api.go handleCompactConversation (POST)"
      to: "runner Runner.Compact"
      via: "owner-gate → MaxBytesReader → s.run.Compact → writeJSONStatus {tokens_before,tokens_after,compaction_id}"
      pattern: "s.run.Compact"
    - from: "internal/agui/conversations_api.go handleConversationCompactions (GET)"
      to: "conversations.Store.ListCompactions"
      via: "owner-gate → s.conv.ListCompactions → writeJSON (domain struct, no DTO)"
      pattern: "s.conv.ListCompactions"
  prohibitions:
    - "MUST NOT add an agui JSON DTO for the read path — handleConversationRotEvents serializes the DOMAIN conversations.RotEvent directly; the compaction read route serializes the DOMAIN conversations.Compaction directly. The types.go edit is an INTERFACE widening (ListCompactions), not a DTO (PATTERNS load-bearing correction). Verify: no new struct with json tags in types.go for compactions."
    - "MUST NOT skip the owner gate on either route — GetForIdentity(ctx, id, scopedIdentityID(ctx)) runs before the store/runner touch (V4 IDOR). Verify: a foreign-id unit test asserts 404 before any ListCompactions/Compact call on the fake."
    - "MUST NOT route the mutating POST through a raw store call — it goes through the Runner (the shared Compact seam, D-08); the store has no compaction-trigger method. Verify: the POST handler calls s.run.Compact, not s.conv.*."
    - "MUST NOT leak internal detail — route errors go through sanitizeErr/writeStoreErr (a runner error can embed a DSN, V7). Verify: a DSN-bearing error stub does not put the DSN on the wire."
    - "MUST NOT change NewServer's signature — the new methods ride the existing narrow Runner/ConversationStore interfaces. Verify: NewServer(run, conv, cfg) unchanged."
---

<objective>
Add the two AG-UI routes that make the web cockpit the 4th manual `/compact` trigger surface and feed the context-budget-gauge compaction markers: `POST /api/conversations/{id}/compact` (runs the shared server-side compaction path, D-08) and `GET /api/conversations/{id}/compactions` (a thin `ListCompactions` read, D-09). Both are exact siblings of the shipped rot-events read-path + rename POST-path; both owner-gate before any store/runner touch.

Purpose: D-08's web trigger (extends COMPACT-05/06/07 as the 4th manual surface — same shared Runner.Compact, no duplicate impl) and D-09's gauge read route (extends COMPACT-10's ListCompactions projection). These Go handlers are owned surface (≥85% floor) and are unit-tested all-fake (scriptedRunner + fakeConvStore, no DB) — the frontend that consumes them is Plan 42-07. Load-bearing PATTERNS correction: the rot-events path serializes the DOMAIN struct directly with NO agui DTO, so the "compaction wire type" is the domain `conversations.Compaction`, and the `types.go` edit is widening `ConversationStore` with `ListCompactions`, not adding a JSON DTO.
Output: two handlers + mux registrations in `conversations_api.go`, the `server.go` Runner-interface + `types.go` ConversationStore-interface widenings (+ the test doubles), and the httptest route tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-llm-conversation-compaction/42-SPEC.md
@.planning/phases/42-llm-conversation-compaction/42-CONTEXT.md
@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@.planning/phases/42-llm-conversation-compaction/42-VALIDATION.md
@.claude/skills/golang-security/SKILL.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
</context>

<artifacts_produced>
This plan adds these NEW symbols (exclude from source-grounding drift verification — net-new):
- `conversations_api.go` — `(s *Server) handleConversationCompactions` (GET), `(s *Server) handleCompactConversation` (POST) + the two mux registrations + the small snake_case POST-response struct ({tokens_before, tokens_after, compaction_id}).
- `server.go` — Runner interface += `Compact(ctx, convID string, req runner.CompactRequest) (runner.CompactOutcome, error)`; scriptedRunner.Compact spy.
- `types.go` — ConversationStore interface += `ListCompactions(ctx, conversationID) ([]conversations.Compaction, error)`; fakeConvStore.ListCompactions.
Consumes from Plan 42-04: `runner.Runner.Compact`, `runner.CompactRequest`, `runner.CompactOutcome`. From Plan 42-01: `conversations.Compaction`, `(*Store) ListCompactions`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /compactions read route + ListCompactions interface widening (D-09, COMPACT-10)</name>
  <files>internal/agui/conversations_api.go, internal/agui/types.go, internal/agui/conversations_api_compact_test.go</files>
  <read_first>
    - internal/agui/conversations_api.go:52 (the mux.HandleFunc("GET /api/conversations/{id}/rot-events", …) registration) + :219 (handleConversationRotEvents — parseConvID → GetForIdentity owner gate → s.conv.ListContextRotEvents → writeJSON of the DOMAIN struct, NO DTO), :224 (the owner gate), :229 (the ListContextRotEvents wrapper call). The verbatim template (42-PATTERNS.md § conversations_api.go GET).
    - internal/agui/types.go (the ConversationStore interface, ListContextRotEvents at :38, GetForIdentity) — add ListCompactions beside ListContextRotEvents (PATTERNS load-bearing correction: interface widening, NOT a DTO).
    - internal/conversations/store_compact.go (Plan 42-01) — the domain conversations.Compaction struct (plain Go, no json tags) + (*Store) ListCompactions.
    - internal/agui/*_test.go — the existing rot-events route test + the agui.fakeConvStore double to extend with ListCompactions.
    - 42-PATTERNS.md § "Frontend read chain" + top-of-file correction (domain-struct-as-wire, PascalCase on the React side) + 42-RESEARCH.md § Security Domain (V4 owner-gate).
  </read_first>
  <behavior>
    - GET /compactions owned: owner-gate passes → returns 200 with the JSON array of conversations.Compaction (PascalCase Go field names, created_at DESC order from ListCompactions).
    - GET /compactions foreign/absent id: GetForIdentity errors → writeStoreErr → 404, and s.conv.ListCompactions is NEVER called.
    - GET /compactions malformed id: parseConvID → 404 (no 500).
    - empty: no compactions → 200 with `[]`.
  </behavior>
  <action>
    Widen the `internal/agui/types.go` ConversationStore interface with `ListCompactions(ctx context.Context, conversationID string) ([]conversations.Compaction, error)` (beside ListContextRotEvents; *conversations.Store already satisfies it via Plan 42-01). In `internal/agui/conversations_api.go`, add `(s *Server) handleConversationCompactions(w, r)` as a verbatim copy of handleConversationRotEvents with `s.conv.ListCompactions` swapped in: `id, ok := parseConvID(w, r)` (404 on malformed); owner gate `if _, err := s.conv.GetForIdentity(r.Context(), id, scopedIdentityID(r.Context())); err != nil { writeStoreErr(w, err); return }`; `rows, err := s.conv.ListCompactions(r.Context(), id)`; `if err != nil { writeStoreErr(w, err); return }`; `writeJSON(w, rows)` (serialize the DOMAIN struct directly — NO agui DTO). Register `mux.HandleFunc("GET /api/conversations/{id}/compactions", s.handleConversationCompactions)` next to the rot-events line (:52). Extend the agui fakeConvStore with ListCompactions + a spy flag (to assert it is NOT called on the foreign-id path). Create `conversations_api_compact_test.go` (UNTAGGED, httptest): the owned 200 (array shape + order), the foreign-id 404 (ListCompactions not called), and the malformed-id 404.
  </action>
  <acceptance_criteria>
    - `types.go` ConversationStore has `ListCompactions`; there is NO new json-tagged DTO struct for compactions in types.go.
    - `conversations_api.go` has `handleConversationCompactions` (owner-gate → ListCompactions → writeJSON) + the GET mux registration.
    - `cd D:/Repo/Aura && go test ./internal/agui/ -run 'Compaction'` passes (owned 200, foreign-id 404 with ListCompactions un-called, malformed 404).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/agui/ && go test ./internal/agui/ -run 'Compaction'</automated>
  </verify>
  <done>GET /api/conversations/{id}/compactions is an owner-gated verbatim sibling of the rot-events route, serializing the domain conversations.Compaction directly (no DTO); a foreign id 404s before the store touch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /compact trigger route + Runner.Compact interface widening (D-08, COMPACT-05/06/07)</name>
  <files>internal/agui/conversations_api.go, internal/agui/server.go, internal/agui/conversations_api_compact_test.go</files>
  <read_first>
    - internal/agui/conversations_api.go:244 (handleRenameConversation — the POST template: r.Body = http.MaxBytesReader(w, r.Body, maxRunBodyBytes) T-12-12 DoS guard, json.NewDecoder decode → 400 on bad body, :261 owner gate), and the writeJSON/writeJSONStatus + sanitizeErr/writeStoreErr helpers.
    - internal/agui/server.go:53 (the Runner interface — Turn/SubmitAnswers/NewConversation/DeleteConversationLifecycle) + :160 (NewServer(run Runner, conv ConversationStore, cfg) — signature MUST stay unchanged) + the scriptedRunner test double to extend with a Compact spy.
    - internal/runner/runner_compact.go (Plan 42-04) — Runner.Compact(ctx, convID, CompactRequest) (CompactOutcome, error); CompactOutcome{TokensBefore,TokensAfter,CompactionID,NoOp}.
    - 42-RESEARCH.md § Security Domain (V4 owner-gate on the mutating route; V5 MaxBytesReader; V7 sanitizeErr — a runner error can embed a DSN, mirror the approvals_api DSN-leak test) + 42-PATTERNS.md § conversations_api.go POST.
  </read_first>
  <behavior>
    - POST /compact owned: owner-gate passes → s.run.Compact(id, {Trigger:"manual", Focus: body.focus}) → 200 with {tokens_before, tokens_after, compaction_id}.
    - POST /compact below-floor (NoOp): 200 with a 'nothing to compact' non-error notice (not an HTTP error).
    - POST /compact foreign/absent id: owner-gate 404 before s.run.Compact is called.
    - POST /compact oversized body: MaxBytesReader trips → 400/413, no unbounded read.
    - POST /compact runner error (e.g. DSN-bearing): sanitizeErr/writeStoreErr → the DSN is NOT on the wire.
    - malformed id: parseConvID → 404.
  </behavior>
  <action>
    Widen the `internal/agui/server.go` Runner interface with `Compact(ctx context.Context, convID string, req runner.CompactRequest) (runner.CompactOutcome, error)` (the concrete *runner.Runner satisfies it via Plan 42-04). In `internal/agui/conversations_api.go`, add `(s *Server) handleCompactConversation(w, r)` (POST): `id, ok := parseConvID(w, r)` (404 malformed); `r.Body = http.MaxBytesReader(w, r.Body, maxRunBodyBytes)`; decode an optional small body `{ focus string }` (empty body allowed → no focus; a bad body → 400); owner gate `GetForIdentity(r.Context(), id, scopedIdentityID(r.Context()))` → writeStoreErr on error (404 IDOR) BEFORE the runner touch; `out, err := s.run.Compact(r.Context(), id, runner.CompactRequest{Trigger: "manual", Focus: body.Focus})`; on error `writeStoreErr(w, sanitizeErr(err))` (V7 no DSN); on success write a small snake_case response struct `{tokens_before: out.TokensBefore, tokens_after: out.TokensAfter, compaction_id: out.CompactionID}` (or, when out.NoOp, a 200 body with a "nothing to compact" notice field) via writeJSONStatus. Register `mux.HandleFunc("POST /api/conversations/{id}/compact", s.handleCompactConversation)`. Extend the scriptedRunner double with a Compact spy (records the convID + req, returns a canned CompactOutcome or an injected error). Add to `conversations_api_compact_test.go` (httptest): the owned 200 (delta shape + Compact spy called with the id + focus), the NoOp notice, the foreign-id 404 (Compact NOT called), the oversized-body cap, the malformed-id 404, and a DSN-bearing runner error → assert the DSN is absent from the response body.
  </action>
  <acceptance_criteria>
    - `server.go` Runner interface has `Compact`; `NewServer(run, conv, cfg)` signature is unchanged.
    - `conversations_api.go` has `handleCompactConversation` (parseConvID → MaxBytesReader → owner-gate → s.run.Compact → snake_case response) + the POST mux registration; it calls the Runner, not the store, for the trigger.
    - `cd D:/Repo/Aura && go test ./internal/agui/ -run 'Compact'` passes (owned 200 delta, NoOp notice, foreign-id 404 with Compact un-called, oversized-body cap, malformed 404, DSN-not-leaked).
    - `golangci-lint run ./internal/agui/` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/agui/ && go test ./internal/agui/ -run 'Compact'</automated>
  </verify>
  <done>POST /api/conversations/{id}/compact makes the web cockpit the 4th manual trigger via the shared Runner.Compact — owner-gated (IDOR-denied), body-capped (DoS), DSN-sanitized (V7), returning {tokens_before, tokens_after, compaction_id} or a below-floor 'nothing to compact' notice — with NewServer's signature untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web client → AG-UI routes | Untrusted principal crosses here; both routes are per-identity and one is mutating. |
| runner/store error → HTTP wire | A runner error can embed a DSN/internal detail that must not reach the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | Elevation of Privilege / Information Disclosure (cross-identity IDOR) | both new routes | mitigate | GetForIdentity(ctx, id, scopedIdentityID(ctx)) before any read/mutate → foreign/absent id 404s before the store/runner touch; a foreign-id unit test asserts the store/runner method is never called (V4). |
| T-42-05 | Denial of Service (oversized POST body / focus) | handleCompactConversation | mitigate | http.MaxBytesReader(w, r.Body, maxRunBodyBytes) on the POST body (mirror rename); the summary itself is bounded by AURA_COMPACT_MAX_OUTPUT_TOKENS; one bounded attempt (Plan 42-04) — no retry storm (V5). |
| T-42-06 | Information Disclosure (DSN/internal in error) | both routes error paths | mitigate | sanitizeErr/writeStoreErr on every error path; a DSN-bearing runner error stub is asserted absent from the wire (V7, mirror the approvals_api DSN-leak test). |
| T-42-17 | Tampering (parse-leak 500) | parseConvID | mitigate | Malformed ids → 404 via parseConvID, never a 500 parse leak. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new packages (RESEARCH § Package Legitimacy Audit); no new Go module, no new npm dep (the React consumers in Plan 42-07 add none either). |
</threat_model>

<verification>
- `cd D:/Repo/Aura && go vet ./internal/agui/ && go build ./... && go test ./internal/agui/ -run 'Compact'` green (all-fake httptest: owner-gate, DoS cap, DSN-sanitize, NoOp notice).
- `golangci-lint run ./internal/agui/` clean; every touched file ≤600 LOC.
- Manual-Only (DoD E2E): a two-identity live check — identity B cannot POST /compact or GET /compactions on identity A's conversation (404); a real POST returns the delta and the gauge (Plan 42-07) shows the marker.
</verification>

<success_criteria>
- POST /compact (web trigger, 4th manual surface, D-08) and GET /compactions (gauge read, D-09) ship as owner-gated siblings of the shipped rot-events + rename routes.
- Both routes deny cross-identity access (V4), cap the POST body (V5), and sanitize errors (V7); the read path serializes the domain struct with no DTO; NewServer is unchanged.
- The Go handlers are unit-tested all-fake (owned surface → ≥85% floor); the React consumers are Plan 42-07.
</success_criteria>

<output>
Create `.planning/phases/42-llm-conversation-compaction/42-06-SUMMARY.md` when done.
</output>
