---
phase: 42-llm-conversation-compaction
plan: 08
type: execute
wave: 7
depends_on: [42-06]
files_modified: [internal/db/migrations/0039_compaction_rollout.up.sql, internal/db/migrations/0039_compaction_rollout.down.sql, internal/db/queries/compaction_rollout.sql, internal/db/sqlc/compaction_rollout.sql.go, internal/conversations/compaction_rollout_store.go, internal/conversations/compaction_rollout_store_test.go]
autonomous: true
requirements: [IC-13, IC-14]
must_haves:
  truths: ["Rollout state survives restarts and is shared by replicas", "Transitions are transactional version-CAS operations", "Evidence and decisions are immutable while rollback restores a last-known-good snapshot atomically"]
  artifacts:
    - {path: internal/db/migrations/0039_compaction_rollout.up.sql, provides: "versioned rollout state and immutable evidence schema"}
    - {path: internal/conversations/compaction_rollout_store.go, provides: "transactional CAS store and atomic rollback"}
  key_links:
    - {from: internal/conversations/compaction_rollout_store.go, to: internal/db/queries/compaction_rollout.sql, via: "sqlc transaction and expected-version CAS"}
---
<objective>Persist the rollout control plane durably before any evaluator or runtime activation can consume it.</objective>
<execution_context>@C:/Users/Davide/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Davide/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/42-llm-conversation-compaction/42-RESEARCH.md
@.planning/phases/42-llm-conversation-compaction/42-PATTERNS.md
@internal/db/migrations/0025_document_control_plane.up.sql
@internal/db/queries/document_control_plane.sql
@internal/documents/jobs_store.go</context>
<tasks>
<task type="auto">
  <name>Task 1: Add rollout-state migration and sqlc CAS queries</name>
  <files>internal/db/migrations/0039_compaction_rollout.up.sql, internal/db/migrations/0039_compaction_rollout.down.sql, internal/db/queries/compaction_rollout.sql, internal/db/sqlc/compaction_rollout.sql.go</files>
  <action>Create one versioned effective-state row per deployment/control-plane scope (not per process), immutable append-only evaluator evidence and transition decisions, stage start/eligible attempts, evaluator/scorer/config/corpus versions, per-stratum snapshots, rolling failure/latency/restore windows, active config, and complete last-known-good config/pointer-policy snapshot. The down migration removes only these additive objects. For both migration files, follow naming, constraints, indexes, timestamps, and reversible ordering from `internal/db/migrations/0025_document_control_plane.up.sql` and `.down.sql`. For `internal/db/queries/compaction_rollout.sql`, follow the claim/update patterns in `internal/db/queries/document_control_plane.sql`, but implement the `42-RESEARCH.md` explicit no-analog immutable-decision plus `WHERE scope_id = ... AND version = expected_version` CAS contract. Regenerate `internal/db/sqlc/compaction_rollout.sql.go` using the repository sqlc command; its analog is the generated `internal/db/sqlc/document_control_plane.sql.go`, so do not hand-edit generated conventions.</action>
  <verify><automated>go test -tags=db_integration ./internal/db -run 'Migration0039|CompactionRolloutSchema' -count=1 &amp;&amp; sqlc diff</automated></verify>
  <done>Up/down/up succeeds; generated queries expose scoped load, append-evidence, CAS transition, and atomic rollback primitives.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement transactional rollout store and distributed durability tests</name>
  <files>internal/conversations/compaction_rollout_store.go, internal/conversations/compaction_rollout_store_test.go</files>
  <behavior>Restart sees prior state; replicas race and exactly one expected-version transition wins; stale decisions cannot overwrite newer state; rollback persists decision and restores LKG in one transaction.</behavior>
  <action>Wrap generated queries in a narrow store that validates immutable evidence digests/versions, performs transition and evidence append in one `db.WithTx`, classifies zero-row CAS as stale, and makes rollback append its reason/evidence then restore the complete LKG effective snapshot before commit. Follow the transaction ownership and `%w` error mapping analog in `internal/documents/jobs_store.go` and `internal/askuser/store.go`, indexed in `42-PATTERNS.md` "Distributed persistence". The multi-replica/restart/stale-decision semantics and immutable decision ledger are explicit no-analog requirements from `42-RESEARCH.md` "Evaluation and Rollout Architecture". Use independent DB handles in integration subtests for two replicas, close/reopen for restart, and prove a failed transaction changes neither state nor decision ledger.</action>
  <verify><automated>go test -race -tags=db_integration ./internal/conversations -run 'RolloutStore|MultiReplica|Restart|StaleDecision|AtomicRollback|LastKnownGood' -count=1</automated></verify>
  <done>Durable state, immutable evidence, CAS conflicts, restart, multi-replica races, stale decisions, and atomic LKG rollback are proven against Postgres.</done>
</task>
</tasks>
<verification>go test -race -tags=db_integration ./internal/db ./internal/conversations -run 'Migration0039|RolloutStore|MultiReplica|Restart|StaleDecision|AtomicRollback|LastKnownGood' -count=1 &amp;&amp; sqlc diff</verification>
<success_criteria>No rollout decision depends on process memory; every transition and rollback is durable, scoped, immutable-evidenced, and concurrency-safe.</success_criteria>
<output>Create `42-08-SUMMARY.md`; atomic schema/query and store/test commits.</output>
