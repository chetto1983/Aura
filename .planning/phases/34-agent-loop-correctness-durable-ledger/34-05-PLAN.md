---
phase: 34-agent-loop-correctness-durable-ledger
plan: 05
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - internal/askuser/store.go
  - internal/askuser/store_unit_test.go
  - internal/conversations/store_append.go
  - internal/conversations/store_append_tx_test.go
autonomous: true
requirements: [LOOP-02, LOOP-03, QUAL-04]
must_haves:
  truths:
    - "askuser exposes tx-accepting MarkResumedTx(q,...), MarkResumedBatchTx(q,...), InsertTx(q,...) that operate on a caller-supplied *sqlc.Queries; the existing MarkResumed/MarkResumedBatch/Insert become thin db.WithTx wrappers over them (DRY, no behavior change for current callers)."
    - "MarkResumedTx classifies a rows-affected==0 claim as ErrPauseNotFound using the regenerated MarkPausedStateResumed (:execrows) — the raw markResumedSQL pool.Exec const is deleted."
    - "MarkResumedBatchTx claims pauses in SORTED token order so two concurrent overlapping batches lock rows in the same order (no Postgres 40P01 deadlock) — the loser blocks, re-checks under READ COMMITTED, and gets a clean ErrPauseNotFound."
    - "conversations exposes AppendTurnTx(ctx, q, params) — the tx-inner turn+aggregate insert body extracted from AppendTurn's WithTx closure; AppendTurn becomes a wrapper over it. It assumes NO spill (resume/pause turns are always <64 KiB)."
    - "ListRecent guards its int32(limit) narrowing (QUAL-04a) mirroring ListPendingAll's math.MaxInt32 pattern."
  artifacts:
    - path: "internal/askuser/store.go"
      provides: "MarkResumedTx / MarkResumedBatchTx (sorted-token) / InsertTx + thin WithTx wrappers; raw markResumedSQL removed; ListRecent int32 guard"
      contains: "MarkResumedBatchTx"
    - path: "internal/conversations/store_append.go"
      provides: "AppendTurnTx(ctx, q, params) tx-inner insert body; AppendTurn rewired to it"
      contains: "AppendTurnTx"
    - path: "internal/askuser/store_unit_test.go"
      provides: "int32 guard table + sorted-token ordering assertion"
      contains: "func Test"
  key_links:
    - from: "internal/askuser/store.go MarkResumedTx"
      to: "q.MarkPausedStateResumed(ctx, arg) → rowsAffected==0 → ErrPauseNotFound"
      via: "regenerated :execrows generated fn"
      pattern: "MarkPausedStateResumed"
  prohibitions:
    - "MUST NOT open a transaction inside the *Tx methods — they RECEIVE a *sqlc.Queries bound to the caller's tx; the pool-owning ResumeCommitter (34-06) provides the tx."
    - "MUST NOT drag cleanupSidecarOnTxError / spill handling into AppendTurnTx — it is the no-spill tx-inner body; the general spill path stays in AppendTurn (resume/pause turns never spill, assumption A3)."
    - "MUST NOT reach for SERIALIZABLE — READ COMMITTED + sorted-token ordering + the WHERE resumed_at IS NULL predicate recheck is the correct, non-flaky design."
    - "MUST NOT change the on-wire behavior of the existing MarkResumed/MarkResumedBatch/Insert/AppendTurn public methods (they stay thin wrappers)."
---

<objective>
Expose the cross-store transaction the HITL-durability committer (34-06) needs, as the DRY `…Tx(q,…)` variant + thin wrapper shape — plus the QUAL-04a int32 guard (deep-refactor-on-touch, same file).

**askuser (D-03):** add `InsertTx(ctx, q, params)`, `MarkResumedTx(ctx, q, token, ans)`, `MarkResumedBatchTx(ctx, q, answers)` operating on a caller-supplied `*sqlc.Queries`. `MarkResumedTx` uses the regenerated `MarkPausedStateResumed` (`:execrows`) and maps rows-affected==0 → `ErrPauseNotFound`, deleting the raw `markResumedSQL` `pool.Exec` const (the only reason the store bypassed sqlc). `MarkResumedBatchTx` sorts tokens before the per-row UPDATE loop (landmine #2 — deadlock-free concurrent batches). The existing `Insert`/`MarkResumed`/`MarkResumedBatch` become thin `db.WithTx` wrappers over the `…Tx` variants (behavior-preserving).

**conversations (D-03):** extract `AppendTurnTx(ctx, q, params)` — the turn+aggregate insert body currently inside `AppendTurn`'s `db.WithTx` closure — as an EXPORTED method so the runner-package committer can call it. It assumes no spill (resume/pause turns are always <64 KiB, A3); the general spill path stays in `AppendTurn`, which becomes a wrapper for the seq-provided case.

**QUAL-04a (D-15 / D-15a):** `ListRecent` (`askuser/store.go:231`) does an unguarded `int32(limit)`; add the `math.MaxInt32` guard mirroring `ListPendingAll` (:196-199).

Purpose: interface-first — define the tx-accepting store seams BEFORE the committer that composes them (34-06), so the runner rewire builds against fixed signatures. Wave 2 — depends on 34-01's `:execrows`. Output: the three askuser tx methods + wrappers, the conversations `AppendTurnTx`, the int32 guard, and their unit tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-agent-loop-correctness-durable-ledger/34-CONTEXT.md
@.planning/phases/34-agent-loop-correctness-durable-ledger/34-RESEARCH.md
@.planning/phases/34-agent-loop-correctness-durable-ledger/34-VALIDATION.md
@internal/askuser/store.go
@internal/conversations/store_append.go
@internal/db/tx.go
@internal/db/queries/paused_states.sql
</context>

## Artifacts this plan produces (★ new / △ modified)

- ★ `askuser.Store.InsertTx(ctx, q *sqlc.Queries, p InsertParams) error` — `internal/askuser/store.go`
- ★ `askuser.Store.MarkResumedTx(ctx, q *sqlc.Queries, token string, ans ResumeAnswer) error` (uses regenerated `:execrows`, rows==0→ErrPauseNotFound) — same file
- ★ `askuser.Store.MarkResumedBatchTx(ctx, q *sqlc.Queries, answers map[string]ResumeAnswer) error` (SORTED token order) — same file
- △ `Insert`/`MarkResumed`/`MarkResumedBatch` → thin `db.WithTx` wrappers; `markResumedSQL` const deleted — same file
- △ `ListRecent` int32(limit) guarded (QUAL-04a) — same file
- ★ `conversations.Store.AppendTurnTx(ctx, q *sqlc.Queries, p AppendTurnParams) error` (no-spill tx-inner body); `AppendTurn` rewired — `internal/conversations/store_append.go`
- (consumers, plan 34-06) `runner.PoolResumeCommitter` composes these three under one `db.WithTx`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: askuser tx-accepting methods + sorted-token batch + int32 guard</name>
  <files>internal/askuser/store.go, internal/askuser/store_unit_test.go</files>
  <read_first>
    - internal/askuser/store.go (`Insert` :116-145; `MarkResumed` :258-275 — the raw `markResumedSQL` `s.pool.Exec` + RowsAffected==0→ErrPauseNotFound; `markResumedSQL` const :280-282; `MarkResumedBatch` :287-326 — the map-iteration UPDATE loop; `AutoResolveForConversation` :331-349 as the db.WithTx wrapper template; `ListRecent` :227-253 unguarded int32; `ListPendingAll` :192-209 the math.MaxInt32 guard to mirror; `encodeAnswer`/`parseUUID` helpers)
    - internal/db/tx.go (`WithTx(ctx, pool, fn func(*sqlc.Queries) error)` — the seam the wrappers use)
    - internal/db/sqlc (regenerated `MarkPausedStateResumed(ctx, arg) (int64, error)` from 34-01; `InsertPausedState` params struct)
    - 34-RESEARCH.md §Gotchas #2 (sorted-token deadlock avoidance), #3 (READ COMMITTED, no SERIALIZABLE), #9 (:execrows regen mechanics); 34-CONTEXT.md D-03/D-04/D-06
  </read_first>
  <behavior>
    - MarkResumedTx on a pending token → rows affected 1, nil err; the row's resumed_at is set.
    - MarkResumedTx on an unknown/already-resumed token → ErrPauseNotFound (rows==0), no panic.
    - MarkResumedBatchTx iterates tokens in sorted order (assert the UPDATE call order is sorted, e.g. via a recording fake *sqlc.Queries or a deterministic-order assertion) so concurrent overlapping batches cannot deadlock.
    - InsertTx writes one pause via the supplied queries (no pool.Begin inside).
    - Insert/MarkResumed/MarkResumedBatch (public) behave exactly as today (thin WithTx wrappers).
    - ListRecent(limit) with limit unset/0/50/MaxInt32/overflow → clamps to a safe int32 (fallback 50 on <=0), never wraps.
  </behavior>
  <action>
    Add `InsertTx(ctx, q *sqlc.Queries, p InsertParams) error` (the body of `Insert` minus the `s.q` reference — call `q.InsertPausedState`), `MarkResumedTx(ctx, q, token, ans)` (parse+encode, `n, err := q.MarkPausedStateResumed(ctx, arg)`; on `n == 0` return `ErrPauseNotFound`), and `MarkResumedBatchTx(ctx, q, answers)` (collect the answer tokens into a slice, `sort.Strings` them, then iterate in that sorted order issuing `q.MarkPausedStateResumed` per token, mapping rows==0→ErrPauseNotFound). Rewrite `Insert`/`MarkResumed`/`MarkResumedBatch` as thin `db.WithTx(ctx, s.pool, func(q){ return s.XTx(ctx, q, ...) })` wrappers. DELETE the `markResumedSQL` const (no longer used). Add the int32 guard to `ListRecent` (mirror `ListPendingAll` — clamp `limit` to `math.MaxInt32`, keep the `<=0 → 50` fallback). Extend `store_unit_test.go` with the int32 table and the sorted-token ordering assertion.
  </action>
  <acceptance_criteria>
    - `grep -n 'markResumedSQL' internal/askuser/store.go` returns 0 (raw const removed).
    - `MarkResumedTx`/`MarkResumedBatchTx`/`InsertTx` exist and take a `*sqlc.Queries` (no `pool.Begin` inside any of them).
    - The batch method sorts tokens before the UPDATE loop (asserted by a test).
    - `ListRecent` clamps int32 (table test: unset/0/50/MaxInt32/overflow).
    - `go test -race ./internal/askuser/` green; existing askuser tests still pass (wrappers behavior-preserving).
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/d/Aura && ! grep -q 'markResumedSQL' internal/askuser/store.go && go test -race ./internal/askuser/</automated>
  </verify>
  <done>askuser exposes the three tx methods (sorted batch), public methods are thin wrappers, the raw const is gone, ListRecent int32 is guarded; race-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: conversations AppendTurnTx (no-spill tx-inner body)</name>
  <files>internal/conversations/store_append.go, internal/conversations/store_append_tx_test.go</files>
  <read_first>
    - internal/conversations/store_append.go (`AppendTurn` :41-84 — the Seq>0 branch's `db.WithTx` closure calling `insertTurnAndAggregates`; `appendTurnWrites` :166-202 building the turn+agg params via `maybeSpill`; `insertTurnAndAggregates` :204-212; `cleanupSidecarOnTxError` :222-230)
    - internal/db/tx.go (`WithTx` seam)
    - 34-RESEARCH.md §Gotchas #10 (resume/pause turns never spill — do NOT drag cleanupSidecarOnTxError into the tx-inner body); 34-CONTEXT.md D-03 step 3 (extract appendTurnTx, sidecar spill stays outside the tx)
  </read_first>
  <behavior>
    - AppendTurnTx(ctx, q, params) with a small (<64 KiB) resume/pause turn → inserts the turn row + folds the aggregate, using the caller-supplied q; no pool.Begin, no spill.
    - AppendTurn (public) behaves exactly as today for both the Seq>0 and seq-allocated branches (spill + cleanupSidecarOnTxError preserved).
  </behavior>
  <action>
    Extract `func (s *Store) AppendTurnTx(ctx context.Context, q *sqlc.Queries, p AppendTurnParams) error` (EXPORTED — the runner-package committer calls it) that builds the turn+aggregate params via `appendTurnWrites(p)` (which for a <cap turn returns an inline content, no sidecar path) and calls `insertTurnAndAggregates(ctx, q, turn, agg)`. Require `p.Seq > 0` (the committer supplies the seq) — return a clear error if `Seq <= 0` (the tx-inner path does not allocate a seq; that stays in `AppendTurn`). Rewire the `AppendTurn` Seq>0 branch to call `AppendTurnTx` inside its existing `db.WithTx` + `cleanupSidecarOnTxError` composition, so the public method's spill semantics are unchanged. Do NOT add spill handling to `AppendTurnTx`. Add `store_append_tx_test.go` (unit where possible; note the real tx path is exercised in 34-06's db_integration) asserting `AppendTurnTx` errors on `Seq<=0` and that `AppendTurn` still round-trips a normal and a spilled turn.
  </action>
  <acceptance_criteria>
    - `AppendTurnTx` is exported, takes a `*sqlc.Queries`, requires Seq>0, and contains no spill/cleanupSidecar logic.
    - `AppendTurn` (public) still passes existing conversations append tests (spill + rollback cleanup intact).
    - `go test -race ./internal/conversations/ -run 'Append'` green.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/conversations/ -run 'Append'</automated>
  </verify>
  <done>AppendTurnTx exposes the no-spill tx-inner insert body; AppendTurn is a wrapper with unchanged spill/rollback behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent resume batches → paused_states | Two overlapping batches locking the same rows in different orders can deadlock; this seam's sorted-token order is the mitigation the 34-06 tx relies on. |
| tx composition | The *Tx methods run under a caller-owned transaction — correctness depends on them NOT opening their own tx. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-B | Tampering / Repudiation (F-004) | MarkResumedBatchTx concurrent deadlock | mitigate | Sorted-token claim order → no 40P01; loser gets clean ErrPauseNotFound under READ COMMITTED |
| T-34-C | Tampering (F-029) | tx-inner claim+append seams | mitigate | Tx-accepting methods let 34-06 span claim+append in ONE tx (all-or-nothing) |
| T-34-QA | DoS (int32 wrap) | ListRecent narrowing | mitigate | math.MaxInt32 clamp (QUAL-04a), mirrors ListPendingAll |
| T-34-SC | Tampering (supply chain) | package installs | accept | N/A — stdlib sort + vendored pgx/sqlc; no installs |
</threat_model>

<verification>
- `go test -race ./internal/askuser/ ./internal/conversations/` green (unit tier).
- `go vet` clean; touched files stay ≤600 LOC (askuser/store.go — verify it does not cross 600 after the additions; split into `store_tx.go` if it does, per deep-refactor-on-touch).
- The atomic cross-store behavior these seams enable is proven in 34-06's db_integration tests (this plan proves the unit-level contracts: rows==0→ErrPauseNotFound, sorted order, Seq>0 guard).
- Mutation spot-check target for the phase gate: `askuser/store.go` batch tx ≥70% killed.
</verification>

<success_criteria>
- Three tx-accepting askuser methods (sorted batch) + thin wrappers; raw markResumedSQL removed.
- AppendTurnTx exported no-spill body; AppendTurn unchanged externally.
- ListRecent int32 guarded.
</success_criteria>

<output>
Create `.planning/phases/34-agent-loop-correctness-durable-ledger/34-05-SUMMARY.md` when done.
</output>
