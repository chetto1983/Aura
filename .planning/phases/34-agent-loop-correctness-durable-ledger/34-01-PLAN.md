---
phase: 34-agent-loop-correctness-durable-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/queries/paused_states.sql
  - internal/db/queries/conversation_turns.sql
  - internal/db/sqlc/paused_states.sql.go
  - internal/db/sqlc/conversation_turns.sql.go
  - internal/db/sqlc/querier.go
  - .planning/ROADMAP.md
autonomous: true
requirements: [LOOP-02, LOOP-03, LOOP-09]
must_haves:
  truths:
    - "The generated MarkPausedStateResumed returns (int64, error) so a rows-affected==0 claim can drive ErrPauseNotFound INSIDE a shared tx (the cross-store resume tx in 34-06 depends on this)."
    - "A read-only ListSpilledSeqsForConversation query returns every seq whose content_sidecar_path IS NOT NULL for a conversation, so the crash-orphan GC (34-04) can reconcile live .content files against committed rows."
    - "ROADMAP Phase-34 goal no longer claims a durable ledger state machine / migration 0025 (D-07); all four success criteria remain intact."
  artifacts:
    - path: "internal/db/queries/paused_states.sql"
      provides: "MarkPausedStateResumed annotated :execrows (per D-03)"
      contains: "MarkPausedStateResumed :execrows"
    - path: "internal/db/queries/conversation_turns.sql"
      provides: "ListSpilledSeqsForConversation :many read-only referenced-seq lookup (per D-09)"
      contains: "ListSpilledSeqsForConversation"
    - path: "internal/db/sqlc/querier.go"
      provides: "Regenerated Querier interface with the new (int64,error) signature + new query method"
      contains: "MarkPausedStateResumed"
  key_links:
    - from: "internal/db/queries/paused_states.sql"
      to: "internal/db/sqlc/paused_states.sql.go"
      via: "make sqlc regeneration"
      pattern: "MarkPausedStateResumed\\(ctx"
  prohibitions:
    - "MUST NOT add or edit any file under internal/db/migrations/ — this phase ships NO new migration (D-02/D-07). Both changes are query-only (an annotation change + a new read-only SELECT)."
    - "MUST NOT hand-edit the generated *.sql.go / querier.go — regenerate them via `make sqlc` (sqlc v1.31.1) and commit the tool output verbatim."
    - "MUST NOT weaken any of the four ROADMAP Phase-34 success criteria when editing the goal prose."
---

<objective>
Land the two query-layer prerequisites the HITL-durability and sidecar-GC plans depend on, and reconcile the ROADMAP goal to the ledger-free reality (D-07).

1. **sqlc (D-03):** flip `MarkPausedStateResumed` from `:exec` to `:execrows` so the generated function returns `(int64, error)` — this is what lets `askuser.MarkResumedTx` classify a rows-affected==0 claim as `ErrPauseNotFound` from INSIDE the shared cross-store transaction (today `askuser.MarkResumed` bypasses sqlc with a raw `pool.Exec` precisely because `:exec` discards the CommandTag).
2. **sqlc (D-09):** add a read-only `ListSpilledSeqsForConversation` query returning every `seq` with a non-null `content_sidecar_path`, so the crash-orphan reconcile (34-04) knows which `<seq>.content` files are referenced. Read-only, no schema change → D-07 holds.
3. **ROADMAP (D-07):** drop the "durable ledger state machine (migration 0025)" clause from the Phase-34 goal (two sites) — this phase spans every affected write with ONE `db.WithTx`, no ledger, no migration.

Purpose: Establish the fixed generated signatures FIRST so waves 2–3 build against them without re-running sqlc mid-stream (a regen that touches `querier.go` from two plans in the same wave would conflict). Both query edits regenerate `querier.go`, so they MUST land in one `make sqlc` run — hence one plan.

Output: annotated `paused_states.sql`, new query in `conversation_turns.sql`, the regenerated sqlc Go files, and a ledger-free ROADMAP goal.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-agent-loop-correctness-durable-ledger/34-CONTEXT.md
@.planning/phases/34-agent-loop-correctness-durable-ledger/34-RESEARCH.md
@.planning/phases/34-agent-loop-correctness-durable-ledger/34-VALIDATION.md
@internal/db/queries/paused_states.sql
@internal/db/queries/conversation_turns.sql
@internal/db/tx.go
@sqlc.yaml
@Makefile
</context>

## Artifacts this phase produces (phase-wide — for source-grounding; this plan creates the ones marked ★)

- ★ `MarkPausedStateResumed(ctx, arg) (int64, error)` — regenerated `internal/db/sqlc/paused_states.sql.go` + `querier.go` (from `:execrows`)
- ★ `ListSpilledSeqsForConversation(ctx, conversationID) ([]int32, error)` — regenerated `internal/db/sqlc/conversation_turns.sql.go` + `querier.go`
- `ResumeCommitter` interface + `PoolResumeCommitter`/`splitResumeCommitter` + `CommitResume`/`CommitResumeBatch`/`CommitPause` — `internal/runner` (plan 34-06)
- `askuser.Store.MarkResumedTx`/`MarkResumedBatchTx`/`InsertTx` — `internal/askuser/store.go` (plan 34-05)
- `conversations.Store.AppendTurnTx` — `internal/conversations/store_append.go` (plan 34-05)
- shared `conversations` sidecar reader `readTurnSidecar` — `internal/conversations/store.go` (plan 34-04)

<tasks>

<task type="auto">
  <name>Task 1: Annotate MarkPausedStateResumed :execrows, add ListSpilledSeqsForConversation, regenerate</name>
  <files>internal/db/queries/paused_states.sql, internal/db/queries/conversation_turns.sql, internal/db/sqlc/paused_states.sql.go, internal/db/sqlc/conversation_turns.sql.go, internal/db/sqlc/querier.go</files>
  <read_first>
    - internal/db/queries/paused_states.sql (the `MarkPausedStateResumed :exec` block at lines 48-53 — the conditional `WHERE token=$1 AND resumed_at IS NULL` update)
    - internal/db/queries/conversation_turns.sql (place the new read-only SELECT alongside the existing turn queries; do NOT touch the locked trigram search block)
    - internal/askuser/store.go (`markResumedSQL` const + `MarkResumed`/`MarkResumedBatch` — confirms the generated fn currently has ZERO hand-written callers, so the signature change breaks no build)
    - sqlc.yaml (`emit_interface: true`, engine postgresql, pgx/v5) and Makefile (the `sqlc` target → `sqlc generate`, CLI pinned v1.31.1)
  </read_first>
  <action>
    In `internal/db/queries/paused_states.sql`, change the `MarkPausedStateResumed` annotation from `:exec` to `:execrows` (leave the SQL body byte-identical). In `internal/db/queries/conversation_turns.sql`, add a new query named `ListSpilledSeqsForConversation :many` that selects `seq` from `aura.conversation_turns` where `conversation_id = $1 AND content_sidecar_path IS NOT NULL` (parameterized, read-only — NO ordering guarantees needed by the caller; the GC sorts/uses a set). Then run `make sqlc` (WSL, sqlc v1.31.1) to regenerate `internal/db/sqlc/paused_states.sql.go`, `internal/db/sqlc/conversation_turns.sql.go`, and `internal/db/sqlc/querier.go`. Commit the regenerated files verbatim — do NOT hand-edit them. Confirm `MarkPausedStateResumed` now returns `(int64, error)` and `ListSpilledSeqsForConversation` returns `([]int32, error)`.
  </action>
  <acceptance_criteria>
    - `grep -n ':execrows' internal/db/queries/paused_states.sql` shows the MarkPausedStateResumed annotation.
    - `grep -n 'ListSpilledSeqsForConversation' internal/db/sqlc/querier.go` returns a method whose signature ends `(int64` free — i.e. returns a seq slice + error.
    - `grep -n 'func (q \*Queries) MarkPausedStateResumed' internal/db/sqlc/paused_states.sql.go` shows a `(int64, error)` return.
    - `go build ./...` is green (no caller of the generated fn existed, so nothing else needs updating in this plan).
    - No file added or modified under `internal/db/migrations/`.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/d/Aura && go build ./internal/db/... && grep -q ':execrows' internal/db/queries/paused_states.sql && grep -q 'ListSpilledSeqsForConversation' internal/db/sqlc/querier.go</automated>
  </verify>
  <done>The regenerated `MarkPausedStateResumed` returns rows-affected, `ListSpilledSeqsForConversation` exists, `go build ./...` is green, and no migration file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP Phase-34 goal (drop ledger/migration clause, D-07)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the two Phase-34 goal sites: the phase-list summary near line 85 `Goal: ... durable ledger state machine (migration 0025).` and the detailed section near line 211 `**Goal:** ... durable ledger state machine (migration 0025).`)
    - .planning/phases/34-agent-loop-correctness-durable-ledger/34-CONTEXT.md (D-07 rationale + the exact replacement wording)
  </read_first>
  <action>
    Edit BOTH Phase-34 goal occurrences in `.planning/ROADMAP.md` to drop the "durable ledger state machine (migration 0025)" clause, per D-07. Replace with the ledger-free wording: "Terminal-response exclusivity, atomic HITL resume/pause (single cross-store transaction), fenced sidecars, crash-orphan reconciliation." Leave the four Success Criteria under the detailed Phase-34 section UNCHANGED (they remain satisfied). Do not alter any other phase's entry. Reference D-07 in the summary so the change is traceable.
  </action>
  <acceptance_criteria>
    - `grep -c 'durable ledger state machine' .planning/ROADMAP.md` returns 0 for the Phase-34 goal lines (other phases' goals may legitimately differ — confirm no Phase-34 goal line still mentions the ledger/migration 0025).
    - The four Phase-34 Success Criteria bullets are byte-unchanged.
    - `grep -n 'single cross-store transaction' .planning/ROADMAP.md` shows the new goal wording at both sites.
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/d/Aura && ! grep -n 'migration 0025' .planning/ROADMAP.md | grep -qi 'phase 34\|Agent-Loop' && grep -q 'single cross-store transaction' .planning/ROADMAP.md</automated>
  </verify>
  <done>Both Phase-34 goal sites drop the ledger/migration clause and state the single-cross-store-transaction reality; success criteria unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/agent → paused_states | The only agent-supplied inputs to these queries are UUIDs (parameterized); no free-form SQL crosses the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-A | Tampering (SQL injection) | new ListSpilledSeqsForConversation query | mitigate | sqlc-generated parameterized query ($1 bind), no string concatenation; conversation_id is a pgtype.UUID at the boundary |
| T-34-01-B | Information Disclosure | referenced-seq lookup exposes only seq integers | accept | returns seq ints for a single conversation id; no content, no cross-conversation leakage (owner-scoping is Phase 36) |
| T-34-SC | Tampering (supply chain) | package installs | accept | N/A — NO npm/pip/cargo/`go get` this phase; only the vendored `sqlc` CLI regenerates from local schema. No Package Legitimacy checkpoint required. |
</threat_model>

<verification>
- `make sqlc` produces a clean diff limited to the two generated query files + `querier.go` (no schema/migration change).
- `go build ./...` and `go vet ./internal/db/...` green.
- `git status` shows no file under `internal/db/migrations/` changed.
</verification>

<success_criteria>
- `MarkPausedStateResumed` returns `(int64, error)`; `ListSpilledSeqsForConversation` exists and is read-only.
- ROADMAP Phase-34 goal is ledger-free at both sites; four success criteria intact.
- Zero new migrations; build + vet green.
</success_criteria>

<output>
Create `.planning/phases/34-agent-loop-correctness-durable-ledger/34-01-SUMMARY.md` when done.
</output>
