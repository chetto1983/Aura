---
phase: 17-packaging-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .planning/phases/17-packaging-distribution/17-SPEC.md
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "A PRD/SPEC amendment commit lands BEFORE any Phase-17 code (PRD-first principle, D-04)"
    - "The amendment ADDS the optional gVisor appliance tier framed as a transparent isolation boundary, not capability-stripping"
    - "The amendment RECORDS the ec7fe2f6 audit-jail revert (D-01) and corrects the stale SPEC Background + the SPEC In-scope hardening line"
    - "The amendment NOTES Docker Sandboxes (sbx) as evaluated-and-deferred (D-05)"
  artifacts:
    - path: "prd.md"
      provides: "PRD amendment entry registering the gVisor tier + the de-harden revert + sbx deferral"
      contains: "gVisor"
    - path: ".planning/phases/17-packaging-distribution/17-SPEC.md"
      provides: "Corrected Background + In-scope line + an amendment note recording the box-model settlement + the new Req 11b gVisor sub-requirement"
      contains: "compose.gvisor.yaml"
  key_links:
    - from: "prd.md amendment"
      to: ".planning/phases/17-packaging-distribution/17-SPEC.md"
      via: "amendment number cross-reference"
      pattern: "amendment"
---

<objective>
Land the D-04 PRD/SPEC amendment commit that is the BLOCKING pre-execution gate for Phase 17. Per the absolute PRD-first principle in CLAUDE.md, no Phase-17 code may be written until this documentation commit exists: it (a) ADDS the optional gVisor `runsc` appliance tier (`compose.gvisor.yaml`) as a new SPEC sub-requirement "Req 11b", (b) RECORDS the `ec7fe2f6` audit-jail revert and corrects the now-stale SPEC Background + the contradictory In-scope hardening line, and (c) NOTES Docker Sandboxes (sbx) as evaluated-and-deferred plus the docker-in-box-without-host-socket future capability.

Purpose: gVisor is NOT in the locked SPEC — adding it without a PRD amendment violates the PRD-first principle. The SPEC also carries an internal contradiction (Req 1 / In-scope line list `cap_drop: ALL`/read-only-rootfs as in-scope, while the Requirement body, Constraints, Out-of-scope, and Revision R6 all DECLINE them). This commit resolves both before any executor touches a Dockerfile or compose file.
Output: a single docs-only commit amending `prd.md` (new amendment entry) and `17-SPEC.md` (new Req 11b + corrected Background + In-scope line + an amendment note). Every downstream Phase-17 plan depends_on this.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-packaging-distribution/17-SPEC.md
@.planning/phases/17-packaging-distribution/17-CONTEXT.md
@.planning/phases/17-packaging-distribution/17-RESEARCH.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (17-01) produces ONLY documentation artifacts (the pre-execution gate):
- A new amendment entry in `prd.md` registering: the gVisor `runsc` optional appliance tier (transparent isolation boundary); the `ec7fe2f6` audit-jail revert; Docker Sandboxes (sbx) evaluated-and-deferred.
- A new sub-requirement "Req 11b — Optional gVisor strong-isolation appliance tier (D-03)" added to `.planning/phases/17-packaging-distribution/17-SPEC.md`, framed as transparent isolation (not capability-stripping).
- Corrections in `.planning/phases/17-packaging-distribution/17-SPEC.md`: the stale Background (SPEC.md:16 — the audit ADDED a root `Dockerfile` + a compose `aura` service; the Background still claims neither exists); the contradictory In-scope hardening line (SPEC.md:137 lists `cap_drop: ALL`/read-only-rootfs as in-scope, contradicting the de-harden); an amendment note recording the box-model settlement (spikes 059-062).
No code, no Dockerfile, no compose change in this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the PRD amendment entry (gVisor tier + de-harden revert + sbx deferral)</name>
  <read_first>
    - prd.md (find the amendment log / §Q&A revision protocol section where prior amendments #47, #50, #59, #60, #62 are recorded — match that exact entry shape and numbering)
    - .planning/phases/17-packaging-distribution/17-CONTEXT.md (D-01..D-06; the box-model settlement and the amendment #50 dev-full-trust / prod-hardening-tier menu this extends)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (the "D-04 Pre-Execution Amendment Gate" section + the "Deep-Dive Resolutions (2026-06-14)" appendix item 4 on gVisor — the three required amendment contents, verbatim)
  </read_first>
  <action>
    Append a new PRD amendment entry (next sequential amendment number after the latest in prd.md) to the prd.md amendment log, matching the established amendment-entry shape (number, date 2026-06-14, title, body explaining WHY). The entry MUST cover three points per D-04: (1) ADD the optional gVisor `runsc` strong-isolation appliance tier delivered as `compose.gvisor.yaml` (`runtime: runsc` on the `aura` service), native-Linux/arm64 only, OFF on Docker-Desktop dev — framed explicitly as a TRANSPARENT isolation boundary, NOT capability-stripping: gVisor (`runsc`) interposes a userspace kernel so Aura keeps full parity inside (full Linux, container root, writable rootfs, `shell_exec`, self-extension, `mcp-neo4j-cypher` — spike 010 proved the python/subprocess workload survives `runsc`); only the HOST gets thicker walls. Opt-in = `docker compose -f compose.yaml -f compose.gvisor.yaml up -d`. State the provisioning prerequisite: `runsc` must be registered in the host `/etc/docker/daemon.json` (gvisor apt repo or `runsc install`, then `systemctl reload docker`) — compose cannot install a runtime; gVisor needs Linux 4.14.77+. State the accepted residual: gVisor adds latency on syscall-/I-O-heavy paths (the reason it is OPTIONAL on a CPU-budgeted mini-PC). This is consistent with the SPEC "no internal lockdown" model and extends the amendment #50 dev-full-trust / prod-hardening-tier menu; (2) RECORD the `ec7fe2f6` audit-jail revert per D-01 (the audit added a distroless root Dockerfile + `cap_drop: ALL` + `read_only: true` + forced non-root `65532` two days after the SPEC locked "the container is Aura's computer, not a jail"; spike 059 proved that box structurally cannot run `shell_exec`/self-extend/spawn `mcp-neo4j-cypher`, exit 127) — the SPEC pre-authorizes this revert as "the accepted residual, operator's informed choice 2026-06-10"; (3) NOTE Docker Sandboxes (`sbx`) as evaluated-and-deferred per D-05 (Firecracker-microVM gold standard but arm64 = macOS-only so the DGX appliance is unsupported; wraps coding-agent CLIs not a persistent compose stack; perf crippling on a CPU-budgeted mini-PC; needs `sbx login`) AND the docker-in-box-without-host-socket future capability that could lift SPEC Req 3. Cross-reference the Phase 17 SPEC and spikes 059-062/010 in the entry body. Do NOT inline any Dockerfile/compose code — this is a PRD documentation entry describing the decision.
  </action>
  <verify>
    <automated>grep -q "gVisor" prd.md && grep -q "compose.gvisor.yaml" prd.md && grep -q "ec7fe2f6" prd.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `prd.md` contains a new amendment entry dated 2026-06-14 whose body names `compose.gvisor.yaml`, `runtime: runsc`, the `ec7fe2f6` revert, and Docker Sandboxes (sbx) as deferred.
    - The entry frames gVisor as a transparent isolation boundary (NOT capability-stripping): the entry text explicitly states "full parity inside" or "not capability-stripping" or "transparent".
    - The entry names the provisioning prerequisite (`runsc install` / `/etc/docker/daemon.json` / `systemctl reload docker`) and the opt-in invocation (`-f compose.yaml -f compose.gvisor.yaml`).
    - The amendment number is sequential (one greater than the previously-latest amendment in prd.md).
    - No fenced Dockerfile/compose code block was added to prd.md: `grep -c '^FROM ' prd.md` is unchanged from before the edit.
  </acceptance_criteria>
  <done>prd.md carries a sequential amendment entry registering the gVisor tier (transparent boundary, provisioning prereq named), the ec7fe2f6 revert, and the sbx deferral, cross-referencing the Phase 17 SPEC + spikes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Req 11b (gVisor tier), correct the stale SPEC Background + In-scope hardening line, and add a SPEC amendment note</name>
  <read_first>
    - .planning/phases/17-packaging-distribution/17-SPEC.md (the Background section ~lines 14-25 / SPEC.md:16 claiming "no Dockerfile for the `aura` app" + "compose.yaml has no `aura` service"; the Boundaries In-scope first bullet / SPEC.md:137 listing `cap_drop: ALL`/read-only-rootfs/non-root as in-scope; the Requirements body Req 1, Constraints SPEC.md:166, Out-of-scope SPEC.md:152, Revision R6 SPEC.md:229 which all DECLINE hardening — these four are authoritative, de-harden wins)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Landmine #8 "SPEC internal contradiction on hardening" + the "D-04 Pre-Execution Amendment Gate" item 2 + the "Deep-Dive Resolutions (2026-06-14)" gVisor item — the exact stale lines to correct)
  </read_first>
  <action>
    Edit `17-SPEC.md` in four places, leaving the 16 locked requirement IDs and acceptance criteria intact: (1) ADD a new sub-requirement "Req 11b — Optional gVisor strong-isolation appliance tier (D-03)", framed as TRANSPARENT isolation NOT capability-stripping: gVisor (`runsc`) interposes a userspace kernel; Aura keeps full parity inside (full Linux, container root, writable rootfs, `shell_exec`, self-extension, `mcp-neo4j-cypher` — spike 010 proved the python/subprocess workload survives `runsc`); only the HOST gets thicker walls. Native-Linux / amd64+arm64 appliance ONLY; OFF on Docker-Desktop dev (no `runsc` slot — spikes 010/059). Opt-in = `docker compose -f compose.yaml -f compose.gvisor.yaml up -d`. Provisioning prereq: `runsc` registered in host `/etc/docker/daemon.json` (gvisor apt repo or `runsc install` + `systemctl reload docker`); compose cannot install a runtime; gVisor needs Linux 4.14.77+. Accepted residual: gVisor adds latency on syscall-/I-O-heavy paths (why it is optional on a CPU-budgeted mini-PC). Add Req 11b as a sub-requirement (do NOT change the locked count of 16 top-level requirements — it is a clearly-marked sub-item under the boundary requirement, mirroring the SPEC's existing sub-requirement convention). (2) Correct the Background (SPEC.md:16) — it still claims "no Dockerfile for the `aura` app" and "compose.yaml has no `aura` service"; both became FALSE with the audit `ec7fe2f6` (2026-06-12) which added a distroless root Dockerfile and a hardened `aura` compose service. Update the Background to state that an audit jail (root distroless Dockerfile + hardened `aura` compose service) was added post-SPEC and is REVERTED by this phase (D-01), with the fat full-power box replacing it. (3) Correct the contradictory Boundaries In-scope line (SPEC.md:137) that lists `cap_drop: ALL` / read-only-rootfs / forced non-root as in-scope hardening — replace it to state the `aura` container is a boundary by PACKAGING + MOUNTS + the no-host-Docker-socket invariant + `cpus`/`mem`/`pids` stability limits, with NO capability stripping (consistent with the Requirement body, Constraints SPEC.md:166, Out-of-scope SPEC.md:152, and Revision R6 SPEC.md:229, which are authoritative). (4) Add a short "Amendment 2026-06-14 (box-model settlement)" note near the Revision Log recording the gVisor tier (Req 11b), the `ec7fe2f6` revert, and the sbx deferral, cross-referencing the prd.md amendment number from Task 1 and spikes 059-062/010. Do NOT alter the acceptance-criteria block.
  </action>
  <verify>
    <automated>grep -q "ec7fe2f6" .planning/phases/17-packaging-distribution/17-SPEC.md && grep -q "gVisor\|runsc" .planning/phases/17-packaging-distribution/17-SPEC.md && grep -q "compose.gvisor.yaml" .planning/phases/17-packaging-distribution/17-SPEC.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "gVisor\|runsc" 17-SPEC.md` succeeds (Req 11b present) and `grep -q "ec7fe2f6" 17-SPEC.md` succeeds (revert recorded).
    - `17-SPEC.md` Background no longer asserts "no Dockerfile for the `aura` app" / "compose.yaml has no `aura` service" as a current fact; it records the audit-jail addition + revert.
    - The Boundaries In-scope line (SPEC.md:137) that previously listed `cap_drop: ALL` + read-only-rootfs as an in-scope hardening item no longer presents capability stripping as in-scope — the line is corrected to the no-socket invariant + stability limits + no capability stripping.
    - A dated "box-model settlement" amendment note appears near the Revision Log naming `compose.gvisor.yaml`, the `ec7fe2f6` revert, and the sbx deferral.
    - The acceptance-criteria block still contains its original checkboxes (`grep -c '^- \[ \]' .planning/phases/17-packaging-distribution/17-SPEC.md` unchanged for the acceptance section).
  </acceptance_criteria>
  <done>17-SPEC.md gains Req 11b (transparent gVisor tier); its Background + In-scope line are corrected for the audit-jail revert; and a box-model-settlement amendment note records the gVisor tier + sbx deferral; the acceptance criteria are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is documentation-only (no code, no runtime surface). No new trust boundary is crossed. The only "threat" relevant here is a documentation-integrity one: an inaccurate amendment that misframes the box model.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-R | Repudiation | The PRD/SPEC amendment record | mitigate | The amendment cross-references the `ec7fe2f6` git SHA + spikes 059-062/010 so the box-model decision is auditable and non-repudiable. |
| T-17-01-I | Tampering (doc integrity) | gVisor tier framing | mitigate | The amendment text explicitly frames gVisor as a transparent isolation boundary (not capability-stripping), preventing a later plan-checker from "restoring" the audit hardening by misreading the In-scope line (Landmine #8). |

No `high`-severity security threats are in scope for a docs-only gate commit. The substantive security model (no host Docker socket, no secrets in layers, data loopback, keyless fail-closed) is threat-modeled in the code/compose plans (17-02..17-08).
</threat_model>

<verification>
- `grep -q "compose.gvisor.yaml" prd.md` and `grep -q "compose.gvisor.yaml" .planning/phases/17-packaging-distribution/17-SPEC.md` both succeed.
- `grep -q "ec7fe2f6" prd.md` and `grep -q "ec7fe2f6" .planning/phases/17-packaging-distribution/17-SPEC.md` both succeed.
- `grep -q "gVisor\|runsc" .planning/phases/17-packaging-distribution/17-SPEC.md` succeeds (Req 11b present).
- The prd.md amendment number is sequential; the 17-SPEC.md acceptance block is unchanged.
- No code/Dockerfile/compose files are in this plan's diff (docs-only).
</verification>

<success_criteria>
- A single docs-only commit amends `prd.md` (new sequential amendment entry) and `17-SPEC.md` (new Req 11b + corrected Background + In-scope line + box-model-settlement note).
- The amendment ADDS the gVisor tier (transparent boundary, provisioning prereq named), RECORDS the `ec7fe2f6` revert, and NOTES sbx as deferred — satisfying all three D-04 requirements.
- This commit lands BEFORE any Phase-17 code commit (it gates Waves 2-3).
</success_criteria>

<output>
Create `.planning/phases/17-packaging-distribution/17-01-SUMMARY.md` when done.
</output>
