---
phase: 17-packaging-distribution
plan: 08
type: execute
wave: 3
depends_on: ["17-04", "17-06"]
files_modified:
  - internal/cron/handlers/backup.go
  - internal/cron/handlers/backup_test.go
  - scripts/restore_drill.sh
  - README.md
autonomous: false
requirements: [OPS-01]
must_haves:
  truths:
    - "A scheduled backup artifact appears in the host-visible AURA_BACKUP_DIR on schedule (from inside the socket-less box)"
    - "The backup no longer requires a host Docker socket (the docker-exec paradox is resolved via in-box network pg_dump + APOC bolt export)"
    - "The documented one-command restore drill rebuilds BOTH the Postgres and the Neo4j databases"
    - "The README documents docker compose pull && docker compose up -d (migrations via aura-migrate, volumes persist) and the end-user quick start (no host pip install of mcp-neo4j-cypher)"
  artifacts:
    - path: "internal/cron/handlers/backup.go"
      provides: "Backup execution model that runs from inside the socket-less aura box (network pg_dump over the DSN + APOC apoc.export.cypher.all over bolt), writing to the host-visible AURA_BACKUP_DIR; no docker exec / no socket / no resolveDocker"
      contains: "AURA_BACKUP_DIR"
    - path: "scripts/restore_drill.sh"
      provides: "Postgres pg_restore leg + a new Neo4j cypher-shell restore leg"
      contains: "cypher-shell"
    - path: "README.md"
      provides: "End-user quick start (curl|sh / Docker-Desktop Windows door / appliance), restore drill, compose pull update path; removes the host pip install of mcp-neo4j-cypher; whatsapp ToS/QR + Caddy self-signed-trust residuals"
      contains: "docker compose pull"
  key_links:
    - from: "internal/cron/handlers/backup.go"
      to: "the host-visible AURA_BACKUP_DIR bind-mount (plan 17-06)"
      via: "dump written to AURA_BACKUP_DIR from inside the box, no socket"
      pattern: "AURA_BACKUP_DIR"
    - from: "README.md update path"
      to: "aura-migrate one-shot (plan 17-06)"
      via: "docker compose pull && up -d runs migrations via aura-migrate"
      pattern: "docker compose pull"
---

<objective>
Resolve the highest-priority surfaced conflict (Req 16) and finish the lifecycle docs (D-14). `internal/cron/handlers/backup.go` `BackupHandler.Run` shells out to `docker exec` (backup.go:102, 150-155) — but the socket-less `aura` box (Req 1/3) has NO Docker socket, so the scheduled backup running inside the box would FAIL. RESEARCH + PATTERNS recommend the in-box-safe option-b hybrid: dump Postgres over the DSN with a network `pg_dump` (no socket) and dump Neo4j via `apoc.export.cypher.all` over bolt, both writing to the host-visible `AURA_BACKUP_DIR` (bind-mounted into pg/neo4j by plan 17-06). Then rewrite the README to the end-user quick start, the documented restore drill (now with a Neo4j leg), and the `docker compose pull && up -d` update path.

Purpose: data durability must actually work from inside the box, and the operator needs a documented restore + update path. The README must stop telling the user to `pip install mcp-neo4j-cypher` on the host (it now lives in the image).
Output: the backup execution-model fix (network dump + APOC bolt export, no socket), its tests, the restore-drill Neo4j leg, and the README rewrite.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-packaging-distribution/17-SPEC.md
@.planning/phases/17-packaging-distribution/17-CONTEXT.md
@.planning/phases/17-packaging-distribution/17-RESEARCH.md
@.planning/phases/17-packaging-distribution/17-PATTERNS.md
@internal/cron/handlers/backup.go
@scripts/restore_drill.sh
@README.md
@.claude/skills/golang-database/SKILL.md
@.claude/skills/golang-testing/SKILL.md
</context>

<artifacts_this_phase_produces>
- `internal/cron/handlers/backup.go`: the backup execution model changed from `docker exec` (which needs a socket) to an in-box-safe path — network `pg_dump` over the Postgres DSN for the Postgres variant (no socket) + `apoc.export.cypher.all(null,{stream:true,...})` over bolt for Neo4j — writing the dump to the host-visible `AURA_BACKUP_DIR` (the bind-mount from plan 17-06). The `dockerCLI` seam is replaced by `pgDumper`/`neo4jDumper` seams; `resolveDocker()` + the docker `LookPath` + the WR-04 docker-exec host-stat block are removed; `backupDir()`/the rolling retention sweep/`MissedBackupAlert` (SC#3) are preserved; file stays ≤600 LOC.
- `internal/cron/handlers/backup_test.go`: updated/added tests for the new execution model (seamed so unit tests do not need a live DB; a `db_integration`-tagged live leg for the real dump; assertions that the pg_dump argv targets the network host and contains no `docker`/`exec`, and that a neo4j dump produces a `.cypher` artifact).
- `scripts/restore_drill.sh`: a new Neo4j restore leg (`cypher-shell` against `bolt://neo4j:7687`) alongside the existing Postgres `pg_restore` leg.
- `README.md`: end-user quick start (curl|sh / Docker-Desktop Windows door with the PowerShell secret-gen one-liner / appliance), the documented Postgres + Neo4j restore commands, the `docker compose pull && docker compose up -d` update path (migrations via `aura-migrate`, volumes persist), removal of the host `pip install mcp-neo4j-cypher` step (now in-image), and the whatsapp ToS/ban + headless-QR + Caddy self-signed-trust residual notes.
- New/clarified env wiring: `AURA_BACKUP_DIR` consumed from inside the box.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the Req 16 backup execution model (the docker-exec paradox)</name>
  <files>internal/cron/handlers/backup.go (decision affects this file's rewrite in Task 2)</files>
  <decision>How should the scheduled backup run, given the aura box has NO Docker socket?</decision>
  <action>Pause for the operator to select the backup execution model before Task 2 implements it. Present the three options below; the recommended default is option-b-network-dump (in-box network pg_dump + APOC bolt export, no socket), now VALIDATED by deep research with a concrete implementation (network `pg_dump -h postgres` + `apoc.export.cypher.all` over bolt). Record the selection so Task 2 implements the chosen model. If the operator does not respond, proceed with option-b-network-dump (the recommended default).</action>
  <context>
    `BackupHandler.Run` (internal/cron/handlers/backup.go:102, 150-155) shells out to `docker exec aura-postgres pg_dump ...` / `docker exec aura-neo4j neo4j-admin dump ...`. The current design assumes a HOST-run Aura that can reach the Docker daemon — the backup.go comment at lines 43-53 says exactly that. The socket-less in-box Aura (the lone load-bearing invariant, Req 1/3) cannot `docker exec`. This is a genuine cross-requirement conflict; a box with a backup that silently no-ops = data-loss risk. RESEARCH (Landmine #1, Open Question 1) + the "Deep-Dive Resolutions (2026-06-14)" appendix recommend option (b) and supply the concrete impl. So the operator's choice is now "confirm option-b (recommended)" vs the two alternatives.
  </context>
  <options>
    <option id="option-b-network-dump">
      <name>(RECOMMENDED, deep-research-validated) In-box network pg_dump + APOC bolt export (option b)</name>
      <pros>Cleanest fit for "the box stays a box" — no socket, no exception service. Postgres dumps over the DSN from inside the box (`pg_dump -h postgres -U aura_migrate -Fc`, needs the bundled postgresql-client-17 from plan 17-02); Neo4j dumps via `apoc.export.cypher.all(null,{stream:true,...})` over bolt (APOC already enabled, no Neo4j config change). The dump lands on the host-visible AURA_BACKUP_DIR bind-mount (plan 17-06). Preserves the existing backupDir()/retention/MissedBackupAlert logic.</pros>
      <cons>The streamed APOC payload must survive the line-buffered MCP channel — if `mcp-neo4j-cypher`'s read tool cannot carry it, a small direct-bolt (or `cypher-shell`) seam is needed for the Neo4j leg (slightly more work than the Postgres leg).</cons>
    </option>
    <option id="option-a-socketed-service">
      <name>(b) Separate socketed backup compose service (option a)</name>
      <pros>Keeps the existing docker-exec argv verbatim; the backup runs as a deliberate, scoped compose service that DOES mount the socket — the aura box itself never gets one.</pros>
      <cons>Re-introduces a Docker socket somewhere in the deployment (a scoped exception); more compose surface; the SPEC's "box stays a box" spirit is best served by keeping any socket out of the default deployment.</cons>
    </option>
    <option id="option-c-host-timer">
      <name>(c) Host-side cron/systemd timer invoking docker compose exec</name>
      <pros>Mirrors what scripts/restore_drill.sh already does from the host (restore_drill.sh:41); no socket inside any container.</pros>
      <cons>Moves the backup OUT of the Phase-10 in-app scheduler (the agent_job/cron handlers) onto the host — diverges from the "Aura schedules its own backups" model; harder to keep in sync with the in-app retention logic.</cons>
    </option>
  </options>
  <verify><human-check>Operator selected a backup execution model (or accepted the option-b-network-dump default).</human-check></verify>
  <done>The backup execution model is selected and recorded for Task 2 (default: option-b-network-dump, deep-research-validated).</done>
  <resume-signal>Select: option-b-network-dump (recommended), option-a-socketed-service, or option-c-host-timer. Default if unspecified: option-b-network-dump.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement the chosen backup execution model (default: in-box network pg_dump + APOC bolt export, no socket)</name>
  <files>internal/cron/handlers/backup.go, internal/cron/handlers/backup_test.go</files>
  <read_first>
    - internal/cron/handlers/backup.go (the full handler: `Run` at lines 75-119 with the `docker exec` at line 102; `dumpArgv` at 150-155 building the `exec ... pg_dump` / `exec ... neo4j-admin dump` argv; `resolveDocker` at 133-142 LookPath-gating docker; `backupDir()`/`AURA_BACKUP_DIR` at 180-193; the `dockerCLI` test seam at 56-58; the WR-04 bind-mount precondition + docker-exec host-stat comment at 47-53; the retention sweep `sweepRetention` at 117; the `MissedBackupAlert` SC#3 path)
    - scripts/restore_drill.sh (the host-side `docker compose exec` dump/restore reference — the DSN derivation; the new Neo4j leg is added in Task 3)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the `internal/cron/handlers/backup.go` assignment: the docker-exec paradox + the option-b network-pg_dump recommendation; the AURA_BACKUP_DIR resolution already present)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Landmine #1 HIGHEST PRIORITY; Open Question 1; the "Deep-Dive Resolutions (2026-06-14)" backup item — the concrete `pg_dump -h postgres -U aura_migrate -Fc` + `apoc.export.cypher.all(null,{stream:true,streamStatements:true,batchSize:1000})` impl + the line-buffered-MCP caveat)
    - internal/config/config.go (the Postgres DSN composition — the DSN the network pg_dump connects over; the Neo4j config/bolt URI for the APOC export path)
  </read_first>
  <action>
    Implement the model selected in Task 1 (default option-b-network-dump). For option-b, encode the concrete in-box impl (replacing the `docker exec` design): Postgres = run `pg_dump -h postgres -U aura_migrate -Fc -f <dest> aura` over the network (PGPASSWORD env, the bundled postgresql-client-17 from plan 17-02; preserve the existing fixed-argv / no-injection discipline) — no `docker exec`, no socket; Neo4j = `CALL apoc.export.cypher.all(null,{stream:true,streamStatements:true,batchSize:1000,format:'cypher-shell'})` over bolt, writing the concatenated `cypherStatements` to `<dest>.cypher`. Reshape `backup.go`: REPLACE the `dockerCLI` seam with `pgDumper`/`neo4jDumper` seams (injectable command runners for tests); DELETE `resolveDocker()` and the docker `LookPath`; REMOVE the WR-04 docker-exec host-stat block (Postgres now writes directly to the box's own `AURA_BACKUP_DIR` mount). KEEP `backupDir()`/`AURA_BACKUP_DIR`, the timestamped filename, the rolling retention sweep, and the `MissedBackupAlert` SC#3 behavior intact; preserve the loud-fail-on-missing-artifact behavior (a dump that did not materialize on the host is a terminal error, not a misleading "ok"). Update the misleading WR-04/T-10-15 comments (which described the host-run socket model) to reflect the in-box network dump. Deep-refactor-on-touch: keep the file ≤600 LOC, fold any dup, update comments. CAVEAT: if `mcp-neo4j-cypher`'s read tool cannot carry the streamed APOC export over the line-buffered MCP channel, use a small direct-bolt (or `cypher-shell`) seam for the Neo4j leg instead — pick whichever lands a restorable `.cypher` artifact on `AURA_BACKUP_DIR`; document the choice. If Task 1 selected option-a or option-c instead, implement that model accordingly and adjust the compose/docs — but the default and recommended path is option-b.
  </action>
  <verify>
    <automated>go test ./internal/cron/handlers/ -run TestBackup -v && go vet ./internal/cron/handlers/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cron/handlers/backup.go` no longer requires a Docker socket for the in-box scheduled backup: the Postgres dump runs `pg_dump` over the network (the argv contains `-h postgres` and contains NO `docker`/`exec`, excluding comments); `resolveDocker()`/the docker `LookPath`/the WR-04 docker-exec host-stat block are removed.
    - The Neo4j leg produces a restorable `.cypher` artifact (via `apoc.export.cypher.all` over bolt, or the direct-bolt/`cypher-shell` fallback) under `AURA_BACKUP_DIR`.
    - `backup_test` asserts the pg_dump argv targets the network host (contains `-h postgres`, contains NO `docker`/`exec`) and that a neo4j dump produces a `.cypher` artifact (via the `pgDumper`/`neo4jDumper` seams).
    - The dump is written under `AURA_BACKUP_DIR`; the loud-fail-on-missing-host-artifact behavior + the retention sweep + `MissedBackupAlert` (SC#3) are preserved.
    - `go test ./internal/cron/handlers/` passes (updated unit tests via the dump seams) + a `db_integration`-tagged live leg dumps a real artifact; `t.Fatal` under `$CI` when env unset (no-skip-as-green).
    - `go vet ./internal/cron/handlers/`, `go build ./...`, and `go test -race ./internal/cron/handlers/` are clean; owned-surface coverage ≥85%; the file stays ≤600 LOC; the stale WR-04/T-10-15 host-socket comments are corrected.
  </acceptance_criteria>
  <done>The scheduled backup runs from inside the socket-less box (network `pg_dump -h postgres` for Postgres + `apoc.export.cypher.all` over bolt for Neo4j) to the host-visible AURA_BACKUP_DIR, via pgDumper/neo4jDumper seams (resolveDocker + the WR-04 docker-exec host-stat block deleted), with artifact-verification + retention + MissedBackupAlert preserved and the comments corrected; race-clean, ≥85% coverage, ≤600 LOC.</done>
</task>

<task type="auto">
  <name>Task 3: restore_drill.sh Neo4j leg + README rewrite — end-user quick start + restore drill + compose pull update + residuals</name>
  <files>scripts/restore_drill.sh, README.md</files>
  <read_first>
    - scripts/restore_drill.sh (the existing Postgres `pg_restore` leg + the host-side DSN derivation — currently Postgres-only; the new Neo4j leg is added here)
    - README.md (the current developer-flow quick start — find any `pip install`/host `mcp-neo4j-cypher` setup step + the `make tools`/`go run` developer dance to replace; line 19 already describes the mcp-neo4j-cypher MCP — keep the architecture note, replace the install step)
    - scripts/install.sh (the curl|sh entry from plan 17-07 — the Linux/macOS quick-start command)
    - compose.yaml (the Windows Docker-Desktop path: the PowerShell secret-gen one-liner producing a `.env` with generated DB secrets + AURA_ACCESS_TOKEN; the `aura-migrate` one-shot that runs migrations on `compose pull && up -d`; the whatsapp headless-QR + Caddy self-signed-trust residuals from plan 17-06)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 13 Windows door HOW; Req 16 restore + update HOW; the "Deep-Dive Resolutions (2026-06-14)" appendix — the Neo4j restore via `cypher-shell`, the whatsapp ToS/QR residual, the Caddy self-signed-trust residual; the Runtime State Inventory note — document that the host can drop the orphaned pip mcp-neo4j-cypher + the WSL whatsapp install)
  </read_first>
  <action>
    First, add a Neo4j restore leg to `scripts/restore_drill.sh` (currently Postgres-only): restore the `.cypher` dump via `cypher-shell -a bolt://neo4j:7687 -u neo4j -p <pw> < neo4j-<ts>.cypher` alongside the existing Postgres `pg_restore` leg. Then rewrite the README quick start to the end-user flow: (1) Linux/macOS = `curl -fsSL <raw-url>/install.sh | sh` (host needs only Docker); (2) Windows = the documented Docker-Desktop path: install Docker Desktop, run a documented PowerShell secret-gen one-liner that produces a `.env` with generated DB secrets + AURA_ACCESS_TOKEN (user never hand-types them), then `docker compose up` against the shipped `compose.yaml` — NO bespoke Windows installer; (3) appliance = same compose + pre-seeded image. Document BOTH restore commands (Postgres `pg_restore` + Neo4j `cypher-shell`) and the update path `docker compose pull && docker compose up -d` (migrations via `aura-migrate`, volumes persist). REMOVE the undocumented host `pip install mcp-neo4j-cypher==0.6.0` setup step (it now lives in the image — Req 6) and add a short "retired" note that the host can drop the orphaned host pip mcp-neo4j-cypher + the retired WSL whatsapp-mcp install (Runtime State Inventory). Add the residual notes: (a) the whatsapp sibling uses an UNOFFICIAL whatsmeow client -> WhatsApp ToS/ban risk + the headless first-run QR pairing step (scan from `docker compose logs -f whatsapp`); (b) the Caddy `tls internal` self-signed-trust note (other LAN hosts get a browser warning unless they import the CA root from `/data/caddy/pki/authorities/local/root.crt`). Keep the developer section if present, but the end-user quick start is the lede.
  </action>
  <verify>
    <automated>grep -q 'cypher-shell' scripts/restore_drill.sh && grep -q 'docker compose pull' README.md && grep -q 'install.sh' README.md && grep -q 'restore' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/restore_drill.sh` contains a `cypher-shell` Neo4j restore leg (against `bolt://neo4j:7687`) alongside the Postgres `pg_restore` leg.
    - README quick start leads with the `curl -fsSL <url>/install.sh | sh` end-user flow (Linux/macOS), documents the Windows Docker-Desktop + PowerShell secret-gen door, and the appliance path.
    - README documents BOTH restore commands (Postgres + Neo4j) and the `docker compose pull && docker compose up -d` update path (migrations via `aura-migrate`, volumes persist).
    - The undocumented host `pip install mcp-neo4j-cypher` setup step is removed; a "retired" note explains the host no longer needs it (now in-image) and can drop the orphaned host pip + WSL whatsapp install.
    - The README documents the whatsapp ToS/ban + headless-QR pairing residual and the Caddy `tls internal` self-signed-trust residual.
    - `grep -q 'docker compose pull' README.md`, `grep -q 'install.sh' README.md`, and `grep -q 'cypher-shell' scripts/restore_drill.sh` all succeed.
  </acceptance_criteria>
  <done>scripts/restore_drill.sh gains a Neo4j cypher-shell restore leg; the README leads with the end-user quick start (curl|sh / Windows Docker-Desktop / appliance), documents the Postgres + Neo4j restore + the compose-pull update, removes the host pip install of mcp-neo4j-cypher, and documents the whatsapp ToS/QR + Caddy self-signed-trust residuals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aura box -> Postgres/Neo4j (network dump) | The backup connects over the in-box network (DSN/bolt), NOT the Docker socket — the box stays a box. |
| dump -> host-visible AURA_BACKUP_DIR | The dump is written to a host bind-mount so it survives container recreation (data durability). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08-E | Elevation of Privilege | a backup design that re-introduces a Docker socket to dump | mitigate | Option-b runs `pg_dump` over the DSN + `apoc.export.cypher.all` over bolt from inside the box — no socket, no host-root surface; `resolveDocker()` is deleted. The box keeps its lone invariant (no Docker socket). |
| T-17-08-D | Denial of Service / data loss | a backup that silently no-ops on the socket-less box | mitigate | The handler preserves the WR-04 loud-fail-on-missing-host-artifact behavior + `MissedBackupAlert` — a dump that did not land on the host is a terminal error, never a misleading "ok" (no silent data-loss). |
| T-17-08-I | Information Disclosure | DB credentials in the dump command / logs | accept | The DSN/credentials are operator-config env (already in the process); the dump argv carries no model/untrusted input (the existing fixed-argv discipline is preserved). The dump file lives on the operator-controlled `AURA_BACKUP_DIR`. |
| T-17-08-T | Tampering (docs) | a README still telling the user to `pip install` on the host | mitigate | The README removes the host pip step (now in-image, Req 6) and documents the host can drop the orphan — preventing host accretion. |

No `high` residuals. The backup runs in-box over the network with the data-durability artifact-verification preserved.
</threat_model>

<verification>
- Task 1 decision resolved (default option-b-network-dump).
- `go test ./internal/cron/handlers/` (incl. updated tests + the `db_integration` live leg) passes; `-race` clean; `go vet ./...` + `go build ./...` clean.
- The backup runs without a Docker socket (`pg_dump -h postgres` + APOC bolt export) and writes to `AURA_BACKUP_DIR`; the loud-fail-on-missing-artifact + retention + MissedBackupAlert are preserved; `resolveDocker()` is gone.
- `scripts/restore_drill.sh` has a Neo4j `cypher-shell` leg; README leads with the end-user quick start, documents both restores + `compose pull` update, removes the host pip install, and documents the whatsapp/Caddy residuals.
- Live (Manual-Only): a scheduled backup artifact appears in the host-visible dir (a `.dump` + a `.cypher`); the documented restore commands rebuild both DBs; `compose pull && up -d` to a newer tag runs migrations + preserves data.
</verification>

<success_criteria>
- The Req 16 docker-exec paradox is resolved: the scheduled backup runs from inside the socket-less box (network `pg_dump -h postgres` + `apoc.export.cypher.all` over bolt) to the host-visible `AURA_BACKUP_DIR`, via pgDumper/neo4jDumper seams, with artifact-verification + retention + MissedBackupAlert preserved.
- `scripts/restore_drill.sh` gains a Neo4j `cypher-shell` restore leg; the README documents the end-user quick start, both restore drills, the `compose pull` update path, removes the host pip install of mcp-neo4j-cypher, and documents the whatsapp ToS/QR + Caddy self-signed-trust residuals.
- All new code is race-clean, vet/build-clean, ≤600 LOC, and ≥85% owned-surface coverage.
</success_criteria>

<output>
Create `.planning/phases/17-packaging-distribution/17-08-SUMMARY.md` when done.
</output>
