---
phase: 17-packaging-distribution
plan: 06
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - compose.yaml
  - compose.gvisor.yaml
  - caddy/Caddyfile
  - docker/whatsapp/Dockerfile
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "docker compose up brings the stack healthy only after aura-migrate exits 0"
    - "Writing config then docker compose down && up preserves it via the aura-home named volume"
    - "From a LAN host the wizard is HTTPS-reachable only with the access token; Postgres/Neo4j/embed/agent-memory/sidecars/whatsapp are loopback-only and refused"
    - "The whatsapp sibling resolves to the catalog endpoint when up and degrades fail-soft when down"
    - "compose.gvisor.yaml adds runtime: runsc to the aura service for the native-Linux/arm64 appliance tier; OFF on Docker-Desktop dev"
  artifacts:
    - path: "compose.yaml"
      provides: "aura-migrate one-shot (service_completed_successfully gate), aura-home named volume -> AURA_CONFIG_DIR, whatsapp sibling (loopback), caddy front (only LAN-exposed), AURA_BACKUP_DIR bind-mounts into pg/neo4j"
      contains: "service_completed_successfully"
    - path: "compose.gvisor.yaml"
      provides: "Minimal override: aura service runtime: runsc (transparent isolation tier, native-Linux/arm64 only)"
      contains: "runsc"
    - path: "caddy/Caddyfile"
      provides: "TLS internal CA + shared-token enforcement fronting only wizard + AG-UI"
      contains: "tls internal"
    - path: "docker/whatsapp/Dockerfile"
      provides: "whatsmeow bridge sibling build context"
      min_lines: 5
  key_links:
    - from: "compose.yaml aura service"
      to: "aura-migrate one-shot"
      via: "depends_on condition: service_completed_successfully"
      pattern: "service_completed_successfully"
    - from: "caddy service"
      to: "wizard (9081) + AG-UI (9080)"
      via: "reverse_proxy fronting only the user-facing surface; data/sidecars stay loopback"
      pattern: "reverse_proxy"
    - from: "whatsapp sibling loopback port"
      to: "whatsappRecipeURL() default in catalog.go (plan 17-03)"
      via: "matching AURA_WHATSAPP_MCP_PORT loopback publish"
      pattern: "127.0.0.1:.*whatsapp"
---

<objective>
Build the compose topology and lifecycle (Req 8/11, D-13/D-11/D-03) on top of the de-hardened `aura` service (17-02) and the streamable-HTTP whatsapp recipe (17-03): a one-shot `aura-migrate` (gated `service_completed_successfully`), the `aura-home` named volume (`AURA_CONFIG_DIR`), the whatsapp whatsmeow bridge sibling (loopback), a bundled Caddy reverse proxy (TLS internal-CA + shared token, the ONLY LAN-exposed service), the optional `compose.gvisor.yaml` override (`runtime: runsc`), and the `AURA_BACKUP_DIR` bind-mounts into the pg/neo4j containers (the host-visible backup precondition consumed by plan 17-08). Every data + sidecar service stays loopback-only; only Caddy is LAN-reachable.

Purpose: Aura becomes a first-class ordered durable service — migrations run once before boot, config survives `down`/`up`, the user-facing surface is TLS+token-gated while data/sidecars never touch the LAN, and the appliance can opt into transparent gVisor isolation.
Output: the `compose.yaml` topology additions, `compose.gvisor.yaml`, `caddy/Caddyfile` + caddy service, and the `docker/whatsapp/` sibling build context.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-packaging-distribution/17-SPEC.md
@.planning/phases/17-packaging-distribution/17-CONTEXT.md
@.planning/phases/17-packaging-distribution/17-RESEARCH.md
@.planning/phases/17-packaging-distribution/17-PATTERNS.md
@compose.yaml
@docker/agent-memory/Dockerfile
@.claude/skills/spike-findings-Aura/references/sandbox-runtime.md
</context>

<artifacts_this_phase_produces>
- `compose.yaml`: a one-shot `aura-migrate` service running `aura db migrate && aura neo4j migrate` (exit 0) with the `aura` service gated by `depends_on: { aura-migrate: { condition: service_completed_successfully } }`; the `aura` `depends_on` extended to neo4j + aura-llama-embed healthchecks; the `aura-home` named volume mounted at `AURA_CONFIG_DIR` (replacing the `aura-runs`/`aura-skills`/`aura-exported-skills` set); the whatsapp sibling service (loopback `127.0.0.1:<port>` matching `whatsappRecipeURL()` default); the Caddy service (the only non-loopback publish); the `AURA_BACKUP_DIR` host bind-mount into pg + neo4j containers (Req 16 precondition).
- `compose.gvisor.yaml` (NEW): minimal override adding `runtime: runsc` to the `aura` service (native-Linux/arm64 only; OFF on Docker-Desktop dev) with a header comment giving the exact `-f compose.yaml -f compose.gvisor.yaml up` invocation + the `runsc install` daemon.json prerequisite note.
- `caddy/Caddyfile` (NEW) + the caddy compose service: `tls internal` + shared-token enforcement fronting ONLY wizard (`:9081`) + AG-UI (`:9080`).
- `docker/whatsapp/` build context (NEW): the whatsmeow bridge sibling Dockerfile.
- New env vars: `AURA_CONFIG_DIR`, `AURA_WHATSAPP_MCP_PORT` (default matching plan 17-03), the Caddy access-token var, `AURA_PIDS_LIMIT` (if not already added in 17-02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: aura-migrate one-shot + aura-home volume + extended depends_on</name>
  <read_first>
    - compose.yaml (the de-hardened `aura` service from 17-02; the current `depends_on: postgres` block ~lines 25-27; the `aura-runs`/`aura-skills`/`aura-exported-skills` volumes ~lines 40-43 + the top-level `volumes:` map ~lines 340-348; the `AURA_RUN_DIR`/`AURA_SKILLS_DIR`/`AURA_SKILL_EXPORT_DIR` env ~lines 33-35; the neo4j + aura-llama-embed healthchecks already present)
    - compose.yaml (the `aura-agent-memory-mcp` sibling block ~lines 143-206 — the canonical `depends_on: { <dep>: { condition: service_healthy } }` topology; `service_completed_successfully` is the new condition for the one-shot)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the aura-migrate one-shot + aura-home volume assignments; `db migrate` uses `LoadDB()` so the one-shot boots keyless independent of D-10)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 8 HOW; Landmine #11 — `aura db migrate && aura neo4j migrate` subcommands both exist and migrate is keyless)
  </read_first>
  <action>
    Add an `aura-migrate` one-shot service to `compose.yaml` (mirror the sibling block shape) that runs `aura db migrate && aura neo4j migrate`, uses the same image/env as `aura`, `depends_on` the pg/neo4j healthchecks, and exits 0 (no `restart`). Gate the `aura` service with `depends_on: { aura-migrate: { condition: service_completed_successfully } }` AND extend its existing `depends_on` to wait on `neo4j` + `aura-llama-embed` healthchecks (currently only `postgres`). Replace the three volumes `aura-runs`/`aura-skills`/`aura-exported-skills` (and their `AURA_RUN_DIR`/`AURA_SKILLS_DIR`/`AURA_SKILL_EXPORT_DIR` mounts) with a single `aura-home` named volume mounted at `AURA_CONFIG_DIR` (holding `llm.json`, `mcp/servers.json`, `Agent.md`, skills, journals); set `AURA_CONFIG_DIR` in the `aura` + `aura-migrate` env; keep the run-artifact dir as a volume or tmpfs. Declare `aura-home` in the top-level `volumes:` map. Do NOT touch the postgres/neo4j named volumes (data unchanged — no migration). `aura-migrate` boots keyless (db migrate uses `LoadDB()`), independent of the 17-04 keyless-boot relaxation.
  </action>
  <verify>
    <automated>grep -q 'service_completed_successfully' compose.yaml && grep -q 'aura-migrate' compose.yaml && grep -q 'aura-home' compose.yaml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `compose.yaml` has an `aura-migrate` one-shot running `aura db migrate && aura neo4j migrate`; the `aura` service `depends_on` includes `aura-migrate: { condition: service_completed_successfully }` and the neo4j + aura-llama-embed healthchecks.
    - The `aura-home` named volume is declared in the top-level `volumes:` map and mounted at `AURA_CONFIG_DIR` on `aura` + `aura-migrate`; the old `aura-runs`/`aura-skills`/`aura-exported-skills` skills/config volumes are consolidated.
    - `docker compose config` parses without error (Manual-Only if `.env` interpolation is needed).
    - Live (Manual-Only): `docker compose up` reaches healthy only after `aura-migrate` exits 0; write `llm.json`/`mcp/servers.json`/`Agent.md`, `down && up` preserves them; removing the mount loses them.
  </acceptance_criteria>
  <done>compose.yaml gains an aura-migrate one-shot gating the aura service via service_completed_successfully, an aura-home volume at AURA_CONFIG_DIR, and extended depends_on healthchecks.</done>
</task>

<task type="auto">
  <name>Task 2: whatsapp whatsmeow bridge sibling (loopback, fail-soft) [PLANNER-CHOICE: bridge image]</name>
  <read_first>
    - compose.yaml (the `aura-agent-memory-mcp` sibling block ~lines 143-206 — build-context + `pull_policy: never` + `container_name` + `restart` + loopback `127.0.0.1:<port>` publish + healthcheck — the exact template to mirror)
    - docker/agent-memory/Dockerfile (the in-repo sidecar Dockerfile template for the whatsapp build context)
    - internal/mcp/manager/catalog.go (the `whatsappRecipeURL()` default loopback port chosen in plan 17-03 — the sibling MUST publish the matching `127.0.0.1:<port>`)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the whatsapp sibling assignment; the fail-soft-when-down shared pattern)
    - .planning/spikes/002-whatsapp-mcp-pairing/README.md + bridge-patch.diff (the chetto1983 whatsapp-mcp fork + spike-002 bridge patch basis — D-07)
  </read_first>
  <action>
    Add a `whatsapp` compose sibling mirroring `aura-agent-memory-mcp` exactly: a build context `docker/whatsapp/` (or a pinned image), `pull_policy: never` for dev, `container_name`, `restart: unless-stopped`, a loopback publish `127.0.0.1:${AURA_WHATSAPP_MCP_PORT:-<default>}:<int>` matching the `whatsappRecipeURL()` default from plan 17-03, and a healthcheck. Create `docker/whatsapp/Dockerfile` building the whatsmeow bridge over streamable-HTTP. PLANNER-CHOICE FLAG (D-07/A4 — surface for a quick user confirm): the concrete bridge image basis is the existing chetto1983 whatsapp-mcp fork + the spike-002 `bridge-patch.diff`; default to building from that fork unless the user prefers an upstream image. Fail-soft is automatic — the streamable-HTTP recipe (17-03) admits an unreachable URL at boot without dialing, so the `aura` service must NOT `depends_on` the whatsapp sibling being healthy (keep whatsapp optional, never a boot dependency). Keep whatsapp loopback-only (never LAN — Caddy does not front it).
  </action>
  <verify>
    <automated>test -f docker/whatsapp/Dockerfile && grep -q 'whatsapp' compose.yaml && grep -Eq '127.0.0.1:.*whatsapp|whatsapp.*127.0.0.1|AURA_WHATSAPP_MCP_PORT' compose.yaml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `compose.yaml` has a `whatsapp` sibling publishing on `127.0.0.1:${AURA_WHATSAPP_MCP_PORT:-<default>}` matching the `whatsappRecipeURL()` default port from plan 17-03.
    - The `aura` service does NOT list the whatsapp sibling as a `depends_on` healthy condition (whatsapp stays optional / fail-soft).
    - `docker/whatsapp/Dockerfile` exists and builds the whatsmeow bridge.
    - The bridge-image basis is flagged for user confirm (PLANNER-CHOICE D-07).
    - Live (Manual-Only): sibling up -> the whatsapp recipe resolves + lists tools; sibling down -> `aura` boots without crash; `command -v wsl.exe` in the `aura` image is non-zero.
  </acceptance_criteria>
  <done>A loopback whatsapp whatsmeow-bridge sibling (matching the catalog port) is added with its build context; it is optional/fail-soft and never a boot dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Caddy TLS + shared-token front (only user-facing) + AURA_BACKUP_DIR bind-mounts [PLANNER-CHOICE: token mechanism]</name>
  <read_first>
    - compose.yaml (every sidecar's loopback `127.0.0.1:<port>` publish — postgres 5432, neo4j 7687/7474, embed 8081, agent-memory-mcp 8091, the multimodal sidecars — these stay loopback; the `aura` AG-UI `127.0.0.1:9080` and the wizard `:9081`; the postgres + neo4j named volumes for the backup bind-mount)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the `caddy/Caddyfile` + Caddy compose service assignment: the only non-loopback publish; `tls internal`; Pitfall #10 — do NOT publish aura :9080 on 0.0.0.0, Caddy fronts it; the backup.go AURA_BACKUP_DIR bind-mount precondition)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 11 HOW; the token-mechanism planner-choice flag; Landmine #10 loopback-vs-LAN split is already 90% done — only Caddy is new LAN; the backup bind-mount precondition at backup.go:47-53)
    - internal/cron/handlers/backup.go (the `backupDir()` AURA_BACKUP_DIR resolution + the WR-04 bind-mount precondition comment — the host bind-mount this task wires into pg/neo4j)
  </read_first>
  <action>
    Add a `caddy` compose service (official `caddy:2` image, `restart: unless-stopped`, a healthcheck) that is the ONLY service publishing on a non-loopback interface (e.g. `0.0.0.0:443`); every data/sidecar port stays `127.0.0.1` (Pitfall #10 — confirm the `aura` AG-UI `:9080` and wizard `:9081` remain loopback in compose; Caddy fronts them for the LAN, they are NOT republished on `0.0.0.0`). Create `caddy/Caddyfile` using `tls internal` (local-CA cert, zero-config) enforcing a generated shared access token, fronting ONLY the wizard (`:9081`) + AG-UI (`:9080`) via `reverse_proxy`. PLANNER-CHOICE FLAG (D-11/A1 — surface for confirm): the token enforcement mechanism — default to a header/`@token` matcher returning 401/403 without the token (simpler than `forward_auth`; verify with the 401-without-token live probe); flag the choice. The access token value is generated by `install.sh` (plan 17-07) and injected via env; the Caddyfile references it, never hard-codes it. Separately (Req 16 precondition for plan 17-08): add the host-visible `AURA_BACKUP_DIR` bind-mount into the postgres + neo4j containers at the SAME in-container path (per the backup.go WR-04 precondition) so the network/exec dump lands on the host; set `AURA_BACKUP_DIR` in the relevant env. Do NOT publish any sidecar on the LAN.
  </action>
  <verify>
    <automated>test -f caddy/Caddyfile && grep -q 'tls internal' caddy/Caddyfile && grep -q 'reverse_proxy' caddy/Caddyfile && grep -q 'caddy' compose.yaml && grep -q 'AURA_BACKUP_DIR' compose.yaml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `caddy/Caddyfile` contains `tls internal` + a `reverse_proxy` to the wizard + AG-UI and enforces a shared token (no token -> 401/403); the token is referenced via env, not hard-coded.
    - `compose.yaml` has a `caddy` service that is the ONLY service publishing on a non-`127.0.0.1` interface; postgres/neo4j/embed/agent-memory/multimodal/whatsapp all remain `127.0.0.1`-bound (grep confirms no `0.0.0.0:` publish on any of them).
    - `compose.yaml` bind-mounts `AURA_BACKUP_DIR` into the postgres + neo4j containers at the same in-container path (the Req 16 host-visible-dir precondition).
    - The token mechanism choice is flagged for user confirm (PLANNER-CHOICE D-11).
    - Live (Manual-Only): from a 2nd LAN host the wizard is HTTPS-reachable only with the token (no token -> 401/403); a LAN connection to PG/Neo4j/embed/agent-memory/any sidecar is refused/unreachable.
  </acceptance_criteria>
  <done>Caddy fronts only the wizard + AG-UI with TLS internal-CA + a shared token; all data/sidecars stay loopback; AURA_BACKUP_DIR is bind-mounted into pg/neo4j for plan 17-08.</done>
</task>

<task type="auto">
  <name>Task 4: compose.gvisor.yaml optional transparent-isolation tier (native-Linux/arm64)</name>
  <read_first>
    - .planning/spikes/008-sandbox-token-auth/compose.token.yaml (the compose-override-per-tier pattern: a header comment with the exact `-f compose.yaml -f <override>.yaml up` invocation, then `services:` naming only the changed keys)
    - .claude/skills/spike-findings-Aura/references/sandbox-runtime.md (the established hardening-tiers menu + compose-override-per-tier pattern)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Landmine #3 — `runtime: runsc` requires host `/etc/docker/daemon.json` registration via `runsc install`; native-Linux/arm64 only; OFF on Docker-Desktop dev; D-03/D-04)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the `compose.gvisor.yaml` assignment — keep the override tiny, just the `runtime:` field on the `aura` service)
  </read_first>
  <action>
    Create `compose.gvisor.yaml` as a minimal compose override (mirror the spike-008 override shape) that names ONLY the `aura` service and adds `runtime: runsc` — the transparent strong-isolation appliance tier (D-03, registered in the 17-01 amendment). Header comment MUST give: (1) the exact invocation `docker compose -f compose.yaml -f compose.gvisor.yaml up -d`; (2) the prerequisite that `runsc` be pre-registered in the host `/etc/docker/daemon.json` via `runsc install` (compose cannot install it — Landmine #3); (3) that this tier is native-Linux/arm64 ONLY and is OFF on Docker-Desktop dev (no runtime slot — spikes 010/059). Frame it (in the comment) as a TRANSPARENT isolation boundary keeping full in-box parity, NOT capability-stripping (consistent with the de-harden). Keep the override to the single `runtime:` field — do NOT re-introduce any `cap_drop`/`read_only`/non-root.
  </action>
  <verify>
    <automated>test -f compose.gvisor.yaml && grep -q 'runsc' compose.gvisor.yaml && ! grep -q 'cap_drop' compose.gvisor.yaml && ! grep -q 'read_only' compose.gvisor.yaml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `compose.gvisor.yaml` exists, names only the `aura` service, and sets `runtime: runsc` (no `cap_drop`/`read_only`/non-root anywhere in it).
    - The header comment gives the exact `-f compose.yaml -f compose.gvisor.yaml up -d` invocation + the `runsc install`/daemon.json prerequisite + the native-Linux/arm64-only / OFF-on-Docker-Desktop note.
    - Live (Manual-Only, native-Linux only): with the override applied, `aura` runs under `runsc` with full parity inside; on Docker Desktop the override is NOT applied.
  </acceptance_criteria>
  <done>compose.gvisor.yaml is a minimal transparent-isolation override (runtime: runsc on aura only) with the daemon.json prerequisite + native-Linux/arm64-only note, no capability stripping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN -> Caddy | The ONLY LAN-exposed surface; TLS-terminated + shared-token-gated; fronts only the wizard + AG-UI. |
| Caddy -> data/sidecar services | Everything except Caddy stays loopback (`127.0.0.1`); never LAN-reachable. |
| host -> aura (optional gVisor tier) | `runtime: runsc` interposes a user-space kernel (transparent; full in-box parity) on the native-Linux/arm64 appliance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-I | Information Disclosure | a compose port-publish exposing Postgres/Neo4j/embed/agent-memory/sidecars/whatsapp on the LAN | mitigate | Every data/sidecar service keeps its `127.0.0.1:` publish; Caddy is the ONLY service on a non-loopback interface (grep-verified). A LAN connection to any data/sidecar port is refused (Req 11 acceptance). |
| T-17-06-S/I | Spoofing / Information Disclosure | unauthenticated LAN access to the wizard | mitigate | Caddy `tls internal` (TLS) + a generated shared access token (401/403 without it) front the user-facing surface (D-11). Real multi-user auth is OUT (future milestone, documented). |
| T-17-06-E | Elevation of Privilege | a malicious skill/MCP attempting host escape | mitigate (optional thicker walls) | Baseline: no host Docker socket + host fs invisible. Optional `compose.gvisor.yaml` `runtime: runsc` adds transparent syscall interception on the native-Linux/arm64 appliance (D-03) — full parity inside, thicker host walls. |
| T-17-06-T | Tampering | the gVisor override accidentally re-introducing capability stripping | mitigate | The override is constrained to the single `runtime:` field; the plan forbids any `cap_drop`/`read_only`/non-root in it (verified) — gVisor is transparent isolation, not a jail. |

Accepted residuals (per SPEC): outbound exfiltration (open NAT egress, allowlist declined); home-volume/database confidentiality (the agent legitimately reads/writes). Caddy uses an internal CA + shared token — real public TLS (ACME) + RBAC are explicitly OUT (future milestone).
</threat_model>

<verification>
- `docker compose config` parses (Manual-Only with `.env`); `aura-migrate` gates `aura` via `service_completed_successfully`; `aura-home` persists config; whatsapp sibling is loopback + optional; Caddy is the only LAN publish; `AURA_BACKUP_DIR` is bind-mounted into pg/neo4j; `compose.gvisor.yaml` adds only `runtime: runsc`.
- Live (Manual-Only): migrate-gating + restart; config persistence across down/up; LAN token gate + sidecar loopback refusal; whatsapp up/down fail-soft; gVisor tier on native-Linux.
</verification>

<success_criteria>
- The stack comes up healthy only after `aura-migrate` exits 0; config survives `down`/`up` via `aura-home` (Req 8).
- From the LAN only Caddy (TLS + token) is reachable; all data/sidecars are loopback-refused (Req 11).
- The whatsapp sibling resolves when up and degrades fail-soft when down (Req 4 topology side).
- The optional `compose.gvisor.yaml` adds transparent `runsc` isolation for the native-Linux/arm64 appliance with no capability stripping (D-03).
- `AURA_BACKUP_DIR` is bind-mounted into pg/neo4j (the plan-17-08 precondition).
</success_criteria>

<output>
Create `.planning/phases/17-packaging-distribution/17-06-SUMMARY.md` when done.
</output>
