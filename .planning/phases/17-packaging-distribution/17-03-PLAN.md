---
phase: 17-packaging-distribution
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - internal/mcp/manager/runtime.go
  - internal/mcp/manager/catalog.go
  - internal/mcp/manager/runtime_guard_test.go
  - internal/mcp/manager/catalog_test.go
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "Launching a runtime.kind=docker MCP from inside the aura container returns a clear 'deploy as a compose sibling' error, not a confusing exec: docker not found"
    - "The whatsapp recipe resolves to a streamable-HTTP sibling endpoint (no wsl.exe), fail-soft when the sibling is down"
    - "The aura image carries no wsl.exe / Windows dependency"
  artifacts:
    - path: "internal/mcp/manager/runtime.go"
      provides: "In-container docker-runtime guard gated on AURA_IN_CONTAINER=1 for RuntimeDocker/RuntimeDockerGateway, returning the literal actionable error before building a docker run command line"
      contains: "AURA_IN_CONTAINER"
    - path: "internal/mcp/manager/catalog.go"
      provides: "whatsapp recipe rewritten to ServerTypeStreamableHTTP + whatsappRecipeURL() (no Command/Args); Summary updated to drop the WSL/wsl.exe string"
      contains: "whatsappRecipeURL"
  key_links:
    - from: "internal/mcp/manager/runtime.go guard"
      to: "AURA_IN_CONTAINER marker baked in docker/aura/Dockerfile (plan 17-02)"
      via: "os.Getenv check in RuntimeLaunchConfig dispatch"
      pattern: "os.Getenv..AURA_IN_CONTAINER"
    - from: "internal/mcp/manager/catalog.go whatsapp recipe"
      to: "the whatsapp compose sibling endpoint (plan 17-06)"
      via: "whatsappRecipeURL() loopback streamable-HTTP URL"
      pattern: "ServerTypeStreamableHTTP"
---

<objective>
Implement the two MCP-boundary code changes (same package `internal/mcp/manager`): the D-08 in-container docker-runtime guard (Req 3) and the D-07 whatsapp sibling recipe rewrite (Req 4). Inside the socket-less `aura` box a `runtime.kind=docker` MCP cannot launch — the guard must fail with a clear actionable error instead of a confusing `exec: "docker": not found`. The whatsapp recipe currently spawns `wsl.exe` (impossible in a Linux container) — it becomes a streamable-HTTP recipe pointing at a compose sibling, fail-soft when down.

Purpose: the box never punctures itself to launch more containers (no host Docker socket); container-needing MCP become compose siblings over the existing streamable-HTTP transport. Every MCP runs in the Linux container world — no Windows/`wsl.exe` coupling.
Output: `internal/mcp/manager/runtime.go` (guard added to the `RuntimeDocker`/`RuntimeDockerGateway` dispatch), `internal/mcp/manager/catalog.go` (whatsapp recipe -> streamable-HTTP sibling), plus their table-driven unit tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-packaging-distribution/17-SPEC.md
@.planning/phases/17-packaging-distribution/17-CONTEXT.md
@.planning/phases/17-packaging-distribution/17-RESEARCH.md
@.planning/phases/17-packaging-distribution/17-PATTERNS.md
@internal/mcp/manager/runtime.go
@internal/mcp/manager/catalog.go
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
</context>

<artifacts_this_phase_produces>
- `internal/mcp/manager/runtime.go`: a new package-level sentinel error (e.g. `errDockerRuntimeInContainer`) + a guard in `RuntimeLaunchConfig` that, when `os.Getenv("AURA_IN_CONTAINER") == "1"` AND the runtime kind is `RuntimeDocker`/`RuntimeDockerGateway`, returns the literal error "docker runtime unavailable inside the container — deploy as a compose sibling and mount via URL" BEFORE building a `docker run` command line. `os` import added.
- `internal/mcp/manager/catalog.go`: the whatsapp recipe rewritten to `Type: mcp.ServerTypeStreamableHTTP` + `URL: whatsappRecipeURL()` (no `Command`/`Args`); a new `whatsappRecipeURL()` helper cloning `memoryRecipeURL()` (validates `AURA_WHATSAPP_MCP_PORT` 1-65535, falls back to the sibling's loopback port); the `Summary` updated to drop "in WSL, stdio via wsl.exe". New env var `AURA_WHATSAPP_MCP_PORT`.
- `internal/mcp/manager/runtime_guard_test.go` (NEW) + `internal/mcp/manager/catalog_test.go` (NEW or extended): table-driven unit tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-container docker-runtime guard in runtime.go (D-08, Req 3)</name>
  <read_first>
    - internal/mcp/manager/runtime.go (the `RuntimeLaunchConfig` switch at lines 83-99: `case RuntimeDocker: return dockerRuntimeConfig(server)` and `case RuntimeDockerGateway: return dockerGatewayRuntimeConfig(name, server)`; the `errMCPServerBlocked` sentinel at line 19 — the idiom to mirror; note `os` is NOT yet imported)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the `internal/mcp/manager/runtime.go — in-container docker guard` assignment: the exact dispatch to extend + the literal error string + the sentinel-error idiom)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 3 HOW; Landmine #5 — the AURA_IN_CONTAINER marker is load-bearing; `/.dockerenv` is unreliable, use the explicit marker)
    - .claude/skills/golang-error-handling/SKILL.md (sentinel error + `%w` wrapping conventions)
  </read_first>
  <behavior>
    - When `AURA_IN_CONTAINER=1` and the server's runtime kind is `RuntimeDocker`: `RuntimeLaunchConfig` returns an error whose message contains "deploy as a compose sibling" (and not a `docker run` config).
    - When `AURA_IN_CONTAINER=1` and the runtime kind is `RuntimeDockerGateway`: same actionable error.
    - When `AURA_IN_CONTAINER` is unset (host/dev): `RuntimeDocker` still produces the normal docker command line via `dockerRuntimeConfig` (behavior unchanged off-box).
    - The guard fires BEFORE `dockerRuntimeConfig`/`dockerGatewayRuntimeConfig` build the command line (no confusing `exec: "docker": not found`).
    - The trust-blocked check (errMCPServerBlocked) still runs first and is unchanged.
  </behavior>
  <action>
    Add a package-level sentinel `var errDockerRuntimeInContainer = errors.New("docker runtime unavailable inside the container — deploy as a compose sibling and mount via URL")` (mirror the `errMCPServerBlocked` idiom at runtime.go:19). In `RuntimeLaunchConfig` (runtime.go:83-99), in the `case RuntimeDocker:` and `case RuntimeDockerGateway:` arms, add a guard: if `os.Getenv("AURA_IN_CONTAINER") == "1"` return `mcp.ServerConfig{}, fmt.Errorf("%w (server %q)", errDockerRuntimeInContainer, name)` BEFORE calling `dockerRuntimeConfig`/`dockerGatewayRuntimeConfig`. Add `"os"` to the import block (catalog.go in the same package already imports `os`, so it is package-consistent). The error string must be a clear constant (non-injectable, V5 ASVS). Do NOT change the `default`/`RuntimeLocal` path or the trust-blocked early return. `RunnableManagedServers` already treats a `RuntimeLaunchConfig` error that is NOT `errMCPServerBlocked` as fatal at boot — confirm a docker MCP configured in-box surfaces this guard error at the call site, not as a fail-soft skip (the guard is a hard, actionable error per the SPEC acceptance, distinct from the fail-soft whatsapp path).
  </action>
  <verify>
    <automated>go test ./internal/mcp/manager/ -run TestRuntimeGuard -v && go vet ./internal/mcp/manager/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/mcp/manager/` passes with a new table-driven test asserting: marker set + `RuntimeDocker` -> error containing "deploy as a compose sibling"; marker set + `RuntimeDockerGateway` -> same; marker unset + `RuntimeDocker` -> normal docker command line (no guard error). Use `t.Setenv("AURA_IN_CONTAINER","1")`.
    - The error is reachable via `errors.Is(err, errDockerRuntimeInContainer)` (sentinel idiom).
    - `go vet ./internal/mcp/manager/` and `go build ./...` are clean; `go test -race ./internal/mcp/manager/` passes.
    - The new test file pushes/holds `internal/mcp/manager` owned-surface coverage ≥85%.
  </acceptance_criteria>
  <done>RuntimeLaunchConfig returns the literal "deploy as a compose sibling" actionable error for docker-kind MCP when AURA_IN_CONTAINER=1, and the normal docker command line when unset; covered by a race-clean table-driven test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the whatsapp recipe to a streamable-HTTP sibling (D-07, Req 4)</name>
  <read_first>
    - internal/mcp/manager/catalog.go (the whatsapp recipe at lines 113-129 using `Command: "wsl.exe"` + the WSL Args; the `memory` recipe at lines 130-146 — the streamable-HTTP template: `Type: mcp.ServerTypeStreamableHTTP`, `URL: memoryRecipeURL()`, no Command; the `memoryRecipeURL()` helper at lines 19-25 — the port-validated loopback URL builder to clone)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the `catalog.go — whatsapp recipe -> sibling` assignment: mirror the memory recipe + clone memoryRecipeURL as whatsappRecipeURL)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 4 HOW; the fail-soft degrade pattern — an unreachable HTTP recipe URL must NOT crash boot; the PLANNER-CHOICE FLAG on the concrete bridge image)
  </read_first>
  <behavior>
    - The whatsapp catalog entry resolves to a `ServerTypeStreamableHTTP` server with a loopback URL `http://127.0.0.1:<port>/mcp/` and NO `Command`/`Args` (no `wsl.exe`).
    - `whatsappRecipeURL()` reads `AURA_WHATSAPP_MCP_PORT`; a valid 1-65535 port is used; garbage or empty falls back to the sibling default port (the loopback port the whatsapp sibling publishes in plan 17-06).
    - The whatsapp entry `Summary` no longer contains "wsl.exe" or "in WSL".
    - When the sibling URL is unreachable, mounting degrades fail-soft (the recipe is admitted at boot without dialing — the existing streamable-HTTP path), so boot does not crash.
  </behavior>
  <action>
    Rewrite the whatsapp `CatalogEntry.Server` (catalog.go:119-128) to mirror the `memory` recipe (catalog.go:140-145): `Type: mcp.ServerTypeStreamableHTTP`, `URL: whatsappRecipeURL()`, `Source: "recipe:whatsapp"`, `Trust: mcp.ManagedTrust{Class: mcp.TrustTrustedRecipe}` — remove `Command: "wsl.exe"` and the WSL `Args` entirely. Add a `whatsappRecipeURL()` helper cloning `memoryRecipeURL()` (catalog.go:19-25): read `AURA_WHATSAPP_MCP_PORT`, validate 1-65535, fall back to the whatsapp sibling's default loopback port (pick a free default consistent with the sibling chosen in plan 17-06 — e.g. `8092` or the next free loopback port; document the chosen default in the helper comment so plan 17-06 publishes the matching port). Update the `Summary` (catalog.go:115) to drop "(whatsmeow bridge in WSL, stdio via wsl.exe)" — e.g. "chetto1983/whatsapp-mcp whatsmeow bridge sibling over streamable-HTTP". Keep `TrustClass: mcp.TrustTrustedRecipe` and `Runtime: "local"`. PLANNER-CHOICE FLAG (D-07): the concrete whatsmeow bridge IMAGE/build-context is chosen in plan 17-06 (compose sibling) — this task only points the recipe at the sibling's loopback endpoint; flag the bridge-image choice (basis: chetto1983 whatsapp-mcp fork + spike 002 `bridge-patch.diff`) for a quick user confirm.
  </action>
  <verify>
    <automated>go test ./internal/mcp/manager/ -run TestWhatsapp -v && go vet ./internal/mcp/manager/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - The whatsapp catalog entry has `Type == mcp.ServerTypeStreamableHTTP`, a non-empty loopback `URL`, and an empty `Command` (asserted by a new test iterating `BuiltinCatalog()`/the catalog accessor for `Name == "whatsapp"`).
    - `grep -c "wsl.exe" internal/mcp/manager/catalog.go` returns 0 (excluding comments: `grep -v '^[[:space:]]*//' internal/mcp/manager/catalog.go | grep -c "wsl.exe"` == 0).
    - `whatsappRecipeURL()` returns the default loopback port for empty/invalid `AURA_WHATSAPP_MCP_PORT` and the configured port for a valid one (table-driven test mirroring any existing `memoryRecipeURL` test, using `t.Setenv`).
    - `go vet`, `go build ./...`, and `go test -race ./internal/mcp/manager/` are clean; owned-surface coverage ≥85%.
  </acceptance_criteria>
  <done>The whatsapp recipe is a streamable-HTTP sibling recipe (no wsl.exe), port-validated like memory, fail-soft when down, with the Summary corrected; covered by a race-clean table-driven test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aura container -> host Docker daemon | The box has no Docker socket; a docker-runtime MCP must NOT be launchable in-box (the guard enforces a clear error instead of a punctured boundary). |
| aura container -> MCP sibling (whatsapp) | Container-needing MCP cross via streamable-HTTP on loopback, never by spawning a sub-container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-E | Elevation of Privilege | a docker-runtime MCP attempting to launch in-box (which would require the host socket) | mitigate | The D-08 guard returns a hard actionable error for `RuntimeDocker`/`RuntimeDockerGateway` when `AURA_IN_CONTAINER=1`, before any `docker run` line is built — the box is never punctured to launch more containers. |
| T-17-03-T | Tampering (injection) | the in-container guard error string | accept | The error is a compile-time constant (non-injectable, V5 ASVS); no model/untrusted input flows into it. |
| T-17-03-I | Information Disclosure | `whatsappRecipeURL()` port interpolation (the `8091@evil.example` userinfo-retarget trick) | mitigate | The helper validates `AURA_WHATSAPP_MCP_PORT` as a 1-65535 integer before interpolation (cloned from `memoryRecipeURL` WR-01), so a malformed value cannot retarget the loopback-by-construction recipe off-host. |

No `high` residuals. The whatsapp-down case is a fail-soft availability concern (not a security threat) — bounded by the existing streamable-HTTP boot path.
</threat_model>

<verification>
- `go test ./internal/mcp/manager/` (incl. the two new test files) passes; `go test -race ./internal/mcp/manager/` clean; `go vet ./...` + `go build ./...` clean.
- The docker-runtime guard fires for `RuntimeDocker`/`RuntimeDockerGateway` only when `AURA_IN_CONTAINER=1`; off-box behavior unchanged.
- The whatsapp recipe is streamable-HTTP with no `wsl.exe`; port-validated; fail-soft when the sibling is down.
- Live (Manual-Only / Wave-3 acceptance): in-container, configure a `runtime.kind=docker` MCP -> assert the "deploy as a compose sibling" error; bring the streamable-HTTP whatsapp sibling up -> recipe resolves + lists tools; sibling down -> `aura` boots without crash; `command -v wsl.exe` in the image is non-zero.
</verification>

<success_criteria>
- The in-container docker-runtime guard returns the literal actionable error for docker-kind MCP when AURA_IN_CONTAINER=1 (Req 3 acceptance).
- The whatsapp recipe resolves to a fail-soft streamable-HTTP sibling endpoint with no Windows/wsl.exe coupling (Req 4 acceptance).
- Both changes are race-clean, vet/build-clean, and hold ≥85% owned-surface coverage on `internal/mcp/manager`.
</success_criteria>

<output>
Create `.planning/phases/17-packaging-distribution/17-03-SUMMARY.md` when done.
</output>
