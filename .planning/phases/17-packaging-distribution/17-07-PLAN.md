---
phase: 17-packaging-distribution
plan: 07
type: execute
wave: 3
depends_on: ["17-02", "17-06"]
files_modified:
  - scripts/install.sh
  - .goreleaser.yaml
  - deploy/aura.service
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "curl -fsSL <url>/install.sh | sh on a clean Linux host yields a healthy stack + a printed summary (wizard URL with token pre-filled, next steps) with zero host Python/Node/pip"
    - "Re-running install.sh leaves existing .env secrets byte-identical (idempotent secret-gen)"
    - "Under-spec hardware warns/aborts before bringing the stack up (abort <16GB RAM / <4 cores / <20GB disk)"
    - "Missing Docker auto-installs via the OS package path; on forced failure prints guided instructions + non-zero exit (no hang)"
    - "A tagged release publishes a public multi-arch ghcr.io/<org>/aura:<tag> AND host-binary archives; compose references the pinned tag (never latest)"
    - "After an appliance reboot the full stack is healthy with no human action (systemd autostart); the optional gVisor tier is host-provisioned arch-aware on native-Linux only"
  artifacts:
    - path: "scripts/install.sh"
      provides: "HW preflight + Docker check/auto-install/guided-fallback + idempotent openssl secret-gen (AURA_ACCESS_TOKEN + DB pw, chmod 600, no regen) + compose up + post-install summary with token-prefilled wizard URL"
      contains: "openssl rand"
      min_lines: 40
    - path: ".goreleaser.yaml"
      provides: "buildx multi-arch image build + ghcr.io push pinned per tag (never latest); host binaries retained"
      contains: "ghcr.io"
    - path: "deploy/aura.service"
      provides: "systemd unit running docker compose up -d on power-on for the appliance"
      contains: "docker compose"
  key_links:
    - from: "scripts/install.sh secret-gen"
      to: "the Caddy AURA_ACCESS_TOKEN + POSTGRES_PASSWORD/NEO4J_PASSWORD in .env"
      via: "openssl rand -hex 32, chmod 600, idempotent guard"
      pattern: "openssl rand"
    - from: ".goreleaser.yaml ghcr image"
      to: "compose.yaml ${AURA_IMAGE} pinned tag"
      via: "pinned-per-tag image reference"
      pattern: "ghcr.io"
---

<objective>
Build the distribution surface (Req 12/13/14/15, D-12/D-13): the `scripts/install.sh` curl|sh self-host door (HW preflight, Docker check + best-effort auto-install + guided fallback, idempotent secret-gen, compose up, post-install summary), the `.goreleaser.yaml` extension that builds + pushes the multi-arch image to `ghcr.io` pinned per release tag (host binaries retained), and the `deploy/aura.service` systemd autostart unit + the optional appliance-only gVisor provisioning. Depends on the fat image (17-02) and a working compose topology (17-06).

Purpose: collapse the install from today's developer dance (git clone -> make tools -> go run + undocumented host pip install) to a single `curl|sh` (Linux/macOS) / documented Docker-Desktop compose path (Windows) / pre-imaged appliance — host needs only Docker.
Output: `scripts/install.sh`, the goreleaser ghcr multi-arch publish block, and the systemd autostart unit + the appliance gVisor-provisioning path.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-packaging-distribution/17-SPEC.md
@.planning/phases/17-packaging-distribution/17-CONTEXT.md
@.planning/phases/17-packaging-distribution/17-RESEARCH.md
@.planning/phases/17-packaging-distribution/17-PATTERNS.md
@scripts/restore_drill.sh
@scripts/coverage_gate.sh
@deploy/aura-scheduler.service
@.goreleaser.yaml
@.claude/skills/golang-security/SKILL.md
</context>

<artifacts_this_phase_produces>
- `scripts/install.sh` (NEW, Linux + macOS): `set -euo pipefail`; HW preflight (RAM/disk/CPU — abort below 16 GB RAM / 4 cores / 20 GB free disk; warn below 32 GB RAM or ~50 GB free disk; non-zero exit, no hang); Docker check + `command -v` detection + best-effort auto-install (`get.docker.com` on Linux, `brew --cask docker` on macOS) + guided fallback + non-zero exit; idempotent secret-gen (`openssl rand -hex 32` for `POSTGRES_PASSWORD`/`NEO4J_PASSWORD` + the `AURA_ACCESS_TOKEN`, `.env` chmod 600, guarded so no regen if `.env` exists); fetch compose + `.env`; `docker compose up`; print the post-install summary with the token-prefilled wizard URL `https://<host>/setup/?token=<TOKEN>` + next steps.
- `.goreleaser.yaml`: a buildx multi-arch block pushing `ghcr.io/chetto1983/aura:<tag>` pinned per release tag (never `latest`); the existing host-binary `builds`/`archives` retained.
- `deploy/aura.service` (NEW): a systemd unit `ExecStart=/usr/bin/docker compose -f /opt/aura/compose.yaml up -d`, `After=network-online.target docker.service`, `WantedBy=multi-user.target`; plus the optional appliance-only, native-Linux, arch-aware gVisor `runsc` provisioning recipe (gvisor apt repo or `runsc install` + `systemctl reload docker`) documented for the `--appliance` path.
- New env vars referenced: `AURA_ACCESS_TOKEN` (generated here), `AURA_IMAGE` pinned-tag (appliance).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: scripts/install.sh — HW preflight + Docker handling + idempotent secret-gen + compose up + token-prefilled summary</name>
  <read_first>
    - scripts/restore_drill.sh (the shell-guard header at lines 18-38: `set -euo pipefail`, `export MSYS_NO_PATHCONV=1`, `${VAR:?msg}` fail-fast env, the `docker`/`docker.exe` `command -v` detection idiom)
    - scripts/coverage_gate.sh (the fail-loud parse-or-die idiom at lines 19-21, 59-62: `set -euo pipefail`, `cd "$(git rev-parse --show-toplevel)"`, fail-loud + exit non-zero)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the `scripts/install.sh` assignment: the full HOW + Pitfall #9 idempotent .env guard)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 12/13 HOW; the "Deep-Dive Resolutions (2026-06-14)" install item — HW thresholds (abort <16GB/<4cores/<20GB, warn <32GB/~50GB), `openssl rand -hex 32` -> `AURA_ACCESS_TOKEN`, token-prefilled summary URL; Landmine #9 idempotent secret-gen)
    - compose.yaml (the secrets it expects: `POSTGRES_PASSWORD`/`NEO4J_PASSWORD`; the `AURA_ACCESS_TOKEN` Caddy token from plan 17-06; the wizard URL/port for the summary)
  </read_first>
  <action>
    Create `scripts/install.sh` (Linux + macOS) replicating the `restore_drill.sh`/`coverage_gate.sh` shell-guard style: `set -euo pipefail`. Steps: (1) HW preflight — read total RAM/free disk/CPU count; ABORT (non-zero exit, no hang) below the hard floor: < 16 GB RAM, < 4 cores, or < 20 GB free disk; WARN (continue) below the comfortable floor: < 32 GB RAM or ~< 50 GB free disk. (The deploy target is a mini-PC 16-core/32 GB; the DGX appliance arm64 is larger.) (2) Docker check — `command -v docker`; if absent, best-effort auto-install (`curl -fsSL https://get.docker.com | sh` on Linux, `brew install --cask docker` on macOS); on failure print clear instructions + the official link and `exit` non-zero (NEVER hang). (3) Idempotent secret-gen — if `.env` does NOT exist, generate `POSTGRES_PASSWORD`/`NEO4J_PASSWORD` + the Caddy `AURA_ACCESS_TOKEN` each via `openssl rand -hex 32`, write `.env`, `chmod 600 .env`; if `.env` EXISTS, do NOT regenerate (re-run leaves it byte-identical — Pitfall #9); `OPENROUTER_API_KEY` is opt-in (a flag or left empty for the keyless-boot path). (4) Fetch/ensure `compose.yaml` + `.env` are present. (5) `docker compose up -d`. (6) Print the post-install summary: the token-prefilled wizard URL `https://<host>/setup/?token=<TOKEN>` (Caddy HTTPS, the `AURA_ACCESS_TOKEN` value substituted in), and next steps. Optionally enable the systemd unit + the gVisor provisioning (Task 3) on an `--appliance` flag. Windows is NOT scripted here (documented Docker-Desktop + PowerShell door lives in the README, plan 17-08).
  </action>
  <verify>
    <automated>bash -n scripts/install.sh && grep -q 'openssl rand' scripts/install.sh && grep -q 'AURA_ACCESS_TOKEN' scripts/install.sh && grep -q 'set -euo pipefail' scripts/install.sh && grep -q 'get.docker.com' scripts/install.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install.sh` (syntax check) passes; the script begins `set -euo pipefail`.
    - The secret-gen block uses `openssl rand -hex 32` for `POSTGRES_PASSWORD`/`NEO4J_PASSWORD` + `AURA_ACCESS_TOKEN`, is guarded so it does NOT regenerate when `.env` exists (idempotent), and applies `chmod 600 .env` (Pitfall #9).
    - The Docker-absent path tries `get.docker.com` (Linux) / `brew --cask docker` (macOS) and on failure prints guided instructions + exits non-zero (no hang).
    - The HW preflight ABORTS (non-zero) below 16 GB RAM / 4 cores / 20 GB free disk and WARNS below 32 GB RAM / ~50 GB free disk, BEFORE `docker compose up`.
    - The post-install summary prints the token-prefilled wizard URL `https://<host>/setup/?token=<TOKEN>` with the generated `AURA_ACCESS_TOKEN` substituted.
    - Live (Manual-Only, native-Linux): `curl -fsSL <url>/install.sh | sh` on a clean Linux host yields a healthy stack + the printed summary; `which python3 pip node` shows none; re-run leaves `.env` byte-identical; under-spec HW warns/aborts first.
  </acceptance_criteria>
  <done>scripts/install.sh does HW preflight (abort <16GB/<4cores/<20GB, warn <32GB/~50GB), Docker auto-install+guided-fallback, idempotent `openssl rand -hex 32` secret-gen for the DB passwords + AURA_ACCESS_TOKEN (chmod 600, no regen), compose up, and prints the token-prefilled `https://<host>/setup/?token=<TOKEN>` wizard URL + next steps.</done>
</task>

<task type="auto">
  <name>Task 2: .goreleaser.yaml — buildx multi-arch + ghcr.io push pinned per tag (host binaries retained) [PLANNER-CHOICE: directive]</name>
  <read_first>
    - .goreleaser.yaml (the existing `builds`/`archives`/`release` blocks at lines 12-69; `release: { github: { owner: chetto1983, name: Aura } }` — the org is `chetto1983`; the local-validate command in the header comment `goreleaser check && goreleaser build --snapshot --clean`)
    - docker/aura/Dockerfile (the fat multi-arch image from plan 17-02 — the image goreleaser builds + pushes)
    - compose.yaml (`image: ${AURA_IMAGE:-aura:local}` + `pull_policy: never` — the appliance pins a real `ghcr.io/...:<tag>`; dev keeps the local build)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 14 HOW; Assumption A3 — verify the exact goreleaser v2 multi-arch directive with a snapshot build)
  </read_first>
  <action>
    Extend `.goreleaser.yaml` to build + push the multi-arch `aura` image to `ghcr.io/chetto1983/aura:<tag>` pinned per release tag, NEVER `latest`, using `docker/aura/Dockerfile`. Keep the existing host-binary `builds`/`archives` (dev retains them). PLANNER-CHOICE FLAG (A3 — verify, not user-confirm): use the current goreleaser v2 multi-arch image directive (`dockers_v2` with buildx for `linux/amd64`+`linux/arm64`, or `docker_manifests` over per-arch `dockers` if the unified v2 directive is unavailable in the pinned goreleaser version) — verify with `goreleaser check` + `goreleaser build --snapshot --clean`. Reference `compose.yaml`'s `${AURA_IMAGE}` so the appliance pins the real ghcr tag while dev keeps `aura:local` + `pull_policy: never`. Tag the image with `{{ .Version }}` (per release tag), never a `latest` tag.
  </action>
  <verify>
    <automated>grep -q 'ghcr.io' .goreleaser.yaml && (command -v goreleaser >/dev/null 2>&1 && goreleaser check || echo "goreleaser not on PATH — Manual-Only check") && echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `.goreleaser.yaml` builds + pushes `ghcr.io/chetto1983/aura:<tag>` for `linux/amd64`+`linux/arm64`, pinned per release tag, with NO `latest` tag emitted.
    - The existing host-binary `builds`/`archives` are retained (dev keeps them).
    - `compose.yaml`'s `${AURA_IMAGE}` resolves to the pinned ghcr tag on the appliance and `aura:local` for dev.
    - `goreleaser check` passes (Manual-Only if goreleaser is not on PATH); a `goreleaser build --snapshot --clean` verifies the directive (Manual-Only).
    - Live (Manual-Only): a tagged release publishes a public multi-arch `ghcr.io/chetto1983/aura:<tag>` (amd64+arm64) AND the host-binary archives; compose references the pinned tag.
  </acceptance_criteria>
  <done>.goreleaser.yaml publishes a pinned-per-tag multi-arch ghcr image (never latest) from docker/aura/Dockerfile while retaining host binaries; compose references the pinned tag.</done>
</task>

<task type="auto">
  <name>Task 3: deploy/aura.service — systemd appliance autostart + optional arch-aware gVisor provisioning</name>
  <read_first>
    - deploy/aura-scheduler.service (the in-repo systemd unit template: the `[Unit]/[Service]/[Install]` skeleton, `After=network-online.target docker.service`, `Wants=network-online.target`, `KillSignal=SIGTERM`/`TimeoutStopSec`, the install-recipe header comment)
    - .planning/phases/17-packaging-distribution/17-PATTERNS.md (the `scripts/aura.service` assignment: for the appliance use `ExecStart=/usr/bin/docker compose -f /opt/aura/compose.yaml up -d`; `WantedBy=multi-user.target`; the file-location choice — `deploy/aura.service` is the consistent placement with the existing analog)
    - .planning/phases/17-packaging-distribution/17-RESEARCH.md (Req 15 HOW; `restart: unless-stopped` already covers crashes, systemd covers reboots; the "Deep-Dive Resolutions (2026-06-14)" gVisor item — appliance-only, native-Linux, arch-aware `runsc install` provisioning, Linux 4.14.77+)
    - compose.gvisor.yaml (plan 17-06 — the override this provisioning enables; the `up -d` invocation appends `-f compose.gvisor.yaml`)
  </read_first>
  <action>
    Create `deploy/aura.service` (placed next to the existing `deploy/aura-scheduler.service` analog) mirroring its `[Unit]/[Service]/[Install]` skeleton but for the appliance: `ExecStart=/usr/bin/docker compose -f /opt/aura/compose.yaml up -d` (compose-up at boot, not `aura serve`), `After=network-online.target docker.service`, `Wants=network-online.target`, `WantedBy=multi-user.target` (system-wide appliance, vs the analog's per-user `default.target`). Keep the install-recipe header-comment convention (the `systemctl enable` recipe). `restart: unless-stopped` in compose already covers crashes (Req 8); this unit covers reboots. The installer (Task 1) optionally enables it on the `--appliance` path. ALSO document (in the unit header comment and/or a sibling note the installer references) the OPTIONAL gVisor provisioning for the strong-isolation tier — appliance-only, native-Linux, ARCH-AWARE: register `runsc` via the gvisor apt repo (`echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/gvisor-archive-keyring.gpg] https://storage.googleapis.com/gvisor/releases release main"` + the key) OR `runsc install`, then `systemctl reload docker`; gVisor needs Linux 4.14.77+ (supports amd64+arm64); this is NEVER attempted on Docker Desktop. After provisioning, the appliance boots the gVisor tier by appending `-f compose.gvisor.yaml` to the compose up (plan 17-06). The provisioning is OPTIONAL — the base unit works without it.
  </action>
  <verify>
    <automated>test -f deploy/aura.service && grep -q 'docker compose' deploy/aura.service && grep -q 'WantedBy=multi-user.target' deploy/aura.service && (grep -q 'runsc' deploy/aura.service || echo "gVisor provisioning may live in a sibling note") && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/aura.service` exists with `ExecStart=/usr/bin/docker compose ... up -d`, `After=network-online.target docker.service`, and `WantedBy=multi-user.target`.
    - The header comment gives the `systemctl enable` install recipe (mirroring the analog).
    - The optional gVisor provisioning is documented as appliance-only, native-Linux, arch-aware (`dpkg --print-architecture` in the apt repo line, or `runsc install`), followed by `systemctl reload docker`, needs Linux 4.14.77+, and is NEVER attempted on Docker Desktop; it appends `-f compose.gvisor.yaml` to the compose up.
    - Live (Manual-Only, appliance): after a reboot the full stack is healthy with no human action (the unit fired + `restart: unless-stopped`).
  </acceptance_criteria>
  <done>deploy/aura.service runs docker compose up -d at boot for the appliance, mirroring the existing scheduler unit, with a documented enable recipe AND the optional appliance-only/native-Linux/arch-aware gVisor `runsc` provisioning recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote install URL -> host | `curl|sh` runs an in-repo script that installs Docker + generates secrets + brings up the stack; the host gains only Docker. |
| install.sh -> .env secrets | Generated DB passwords + the `AURA_ACCESS_TOKEN` land in a `chmod 600` `.env`, never echoed to logs. |
| release -> public ghcr image | The image is public + versioned (pinned per tag); no secret may be in any layer (enforced in plan 17-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07-I | Information Disclosure | generated secrets in `.env` world-readable or regenerated over an existing install | mitigate | `install.sh` `chmod 600 .env` and the idempotent guard never regenerates an existing `.env` (re-run byte-identical) — secrets are not clobbered and not world-readable (Pitfall #9). The token is `openssl rand -hex 32` (256-bit). |
| T-17-07-T | Tampering (supply-chain) | the ghcr image pulling a mutable `latest` tag | mitigate | goreleaser pins the image per release tag and emits NO `latest`; `compose.yaml` references the pinned tag (Constraint). |
| T-17-07-D | Denial of Service | the installer hanging on a missing Docker | mitigate | The Docker-absent path tries the OS package install and, on failure, prints guided instructions + exits non-zero — never a silent hang (Req 13 acceptance). |
| T-17-07-DoomedBoot | Availability | bringing the stack up on under-spec hardware | mitigate | HW preflight aborts (non-zero) below 16 GB RAM / 4 cores / 20 GB free disk and warns below 32 GB / ~50 GB before `docker compose up` (Req 12). |

No `high` residuals. The image-layer secret-leak threat is owned + mitigated in plan 17-02 (`docker history` clean); this plan consumes that guarantee. The gVisor provisioning is host-root apt/`runsc install` on the operator's own appliance (deliberate, optional, native-Linux only).
</threat_model>

<verification>
- `bash -n scripts/install.sh` passes; `set -euo pipefail`; idempotent `openssl rand -hex 32` secret-gen (DB pw + AURA_ACCESS_TOKEN); Docker auto-install + guided fallback; HW preflight abort<16GB/<4cores/<20GB + warn<32GB/~50GB; token-prefilled post-install summary.
- `.goreleaser.yaml` pushes a pinned-per-tag multi-arch ghcr image (no `latest`), host binaries retained; `goreleaser check` (Manual-Only if not on PATH).
- `deploy/aura.service` runs `docker compose up -d` at boot, `WantedBy=multi-user.target`; the optional gVisor provisioning is appliance-only/native-Linux/arch-aware.
- Live (Manual-Only): clean-host `curl|sh` -> healthy + token-prefilled summary + zero host runtimes + idempotent re-run; tagged release -> public multi-arch ghcr + host archives; appliance reboot -> healthy no-touch.
</verification>

<success_criteria>
- `curl|sh` installs the whole stack on a clean Linux host with idempotent `openssl rand -hex 32` secret-gen (DB pw + AURA_ACCESS_TOKEN), HW preflight (abort <16GB/<4cores/<20GB, warn <32GB/~50GB), Docker auto-install/guided-fallback, and a token-prefilled `https://<host>/setup/?token=<TOKEN>` summary (Req 12/13).
- A tagged release publishes a pinned-per-tag public multi-arch ghcr image (never latest) alongside the retained host binaries; compose references the pinned tag (Req 14).
- The systemd unit brings the stack up turnkey after an appliance reboot; the optional gVisor tier is host-provisioned arch-aware on native-Linux only (Req 15).
</success_criteria>

<output>
Create `.planning/phases/17-packaging-distribution/17-07-SUMMARY.md` when done.
</output>
