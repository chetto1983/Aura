---
phase: 52-mid-turn-steering
plan: 06
type: execute
wave: 4
depends_on: ["52-04"]
files_modified:
  - internal/channels/telegram/bot_dispatch_steer.go
  - internal/channels/telegram/bot_dispatch_steer_test.go
  - internal/channels/telegram/bot_dispatch_queue.go
  - internal/channels/telegram/bot_dispatch_queue_test.go
  - internal/channels/telegram/bot_dispatch_turn.go
  - internal/channels/telegram/bot.go
  - internal/channels/telegram/bot_dispatch.go
  - cmd/aura/serve_channels.go
  - cmd/aura/steer_wiring_test.go
autonomous: true
requirements: [STEER-05]

must_haves:
  truths:
    - "A plain-text message sent to Telegram while a turn is live for that chat becomes a steer on that turn, not a busy reply."
    - "The bot always answers that it redirected the turn, so the operator never has to infer where their message went."
    - "A NON-text message during a live turn (photo, voice, document) is QUEUED for the next turn and delivered when the live turn ends — it is not a steer, and it is not dropped. Today it is dropped: startTurn calls onBusy() and returns, and there is no queue anywhere on that path. D-05 requires the queue and this plan builds it."
    - "The operator is told which of the two happened. A queued media message gets a reply naming that it will run after the current request, distinct from the redirect echo and distinct from today's busy copy."
    - "The queued turn is delivered under the SAME per-chat registration and the SAME identity-scoped ctx the live turn ran on, exactly once, with a chain cap of one — a second media message arriving during the delivered turn queues again rather than recursing."
    - "If the live turn was cancelled, the queued message is NOT delivered silently: the operator is told it did not run, so a /cancel never swallows a message the bot said it had accepted."
    - "The steer carries the operator's RAW text — the per-turn context composition (attachment blocks, the thread's knowledge catalog) is NOT prepended to a steer; only a fresh turn gets that treatment."
    - "The queued media turn carries the text that was ALREADY composed at arrival time, because composeTurnContext must run on the handler goroutine while the tele.Context is live — so composition happens on arrival and delivery replays it."
    - "The Telegram steer path is keyed on the SAME conversation identity the cockpit path uses: convID(chatID) is the value the runner persists as the conversation, and the phase's one inbox is keyed on exactly that."
    - "The steer and queue branches run only AFTER the fail-closed identity scoping, so an unlinked or unresolvable chat can no more steer or queue than it can start a turn."
    - "A HITL resume continuation is NOT a steer and is NOT queued: the ask_user busy path is untouched, because an ask_user-paused run is terminal and not steerable."
    - "With the inbox unwired (the AURA_AGUI_RUN_STEER=false rollback), Telegram degrades to exactly today's busy reply for BOTH text and media — no panic, no half-live branch, one switch for the whole D-03/D-04/D-05 cluster."
    - "The pointer agui receives via SetSteerInbox and the pointer telegram.Deps.Steer carries are the SAME inbox instance, pinned by a composition-root test."
  artifacts:
    - path: "internal/channels/telegram/bot_dispatch_steer.go"
      provides: "The text-vs-non-text routing decision and the redirect echo"
      contains: "turnSteeredMessage"
    - path: "internal/channels/telegram/bot_dispatch_queue.go"
      provides: "The per-chat pending-turn slot D-05 requires, and its bounded delivery at turn end"
      contains: "pendingTurns"
    - path: "cmd/aura/serve_channels.go"
      provides: "The shared inbox injected into telegram.Deps"
      contains: "Steer:"
    - path: "cmd/aura/steer_wiring_test.go"
      provides: "The one-inbox-two-consumers pointer-identity proof and the rollback proof"
      contains: "func Test"
  key_links:
    - from: "internal/channels/telegram/bot_dispatch_turn.go startTurn busy branch"
      to: "internal/steer Inbox.Push"
      via: "registerTurn returning false IS the live-turn signal; the steer replaces onBusy for plain text"
      pattern: "Push\\("
    - from: "internal/channels/telegram/bot_dispatch_turn.go startTurn goroutine"
      to: "internal/channels/telegram/bot_dispatch_queue.go pending-turn delivery"
      via: "one call after handleTurn returns, under the same registration and the same scoped ctx"
      pattern: "pendingTurns"
    - from: "cmd/aura/serve_channels.go buildTelegramDeps"
      to: "cmd/aura chatEnv.steer"
      via: "the one process-wide inbox, the same singleton agui receives"
      pattern: "chat.steer"
  prohibitions:
    - "Must NOT let a message the operator meant as a NEW task be silently absorbed into a running turn. Making every plain text a steer buys reach at the cost of a second failure mode — a request that gets no turn of its own and no acknowledgement that this is what happened — and the echo is the only thing standing between the operator and that."
    - "Must NOT ship D-05 as 'today's behaviour'. Today a photo sent during a live turn is DROPPED, not queued. Shipping the drop and calling it the queue would deliver the opposite of the ratified half of the text/non-text split, while every criterion phrased as 'the busy reply is sent' still passes."
    - "Must NOT grow a second implementation of steering inside the channel. A channel that re-validates, re-caps, re-keys or re-shapes what the shared seam already owns will drift from it, and the drift will be invisible until the two surfaces disagree about the same message — the channel is a wrapper, and the wrapper adds a predicate and a rendering, nothing else."
---

<!-- planner-discipline-allow: chat.steer -->
<!-- chat.steer is the Go composition-root expression `Steer: chat.steer` this plan must write; the criterion that mentions it asserts a count of 1 in serve_channels.go, not 0. -->

<objective>
**STEER-05 and the roadmap's SC#5: the same steer works from a channel, not only the cockpit.** On
Telegram, a plain-text message arriving during a live turn becomes a steer on that turn and the bot
says so, replacing today's busy copy. A non-text message is queued for the next turn and delivered
when the live turn ends — which requires building a queue, because today there is none.

Purpose: this is the requirement that decided the inbox's key. `RunRegistry` is wired only on the
AG-UI composition path and a Telegram turn calls `runner.Turn` directly with no run identity in
scope, so a run-id-keyed inbox would have put channel steering out of reach by construction (D-01).
Two ways in, one queue.
</objective>

<no_stale_inputs>
| Fact | Obtain it this way — never from this plan |
|---|---|
| The inbox's `Push` signature and sentinels | `grep -n 'func (i \*Inbox)' internal/steer/inbox.go` |
| The marker/echo decisions from the tracer | `.planning/phases/52-mid-turn-steering/52-02-SUMMARY.md` and `.planning/phases/52-mid-turn-steering/52-04-SUMMARY.md` |
| The current busy-copy constant and its call sites | `grep -rn 'turnBusyMessage' internal/channels/telegram/` |
| What actually happens today on a busy chat | Read `startTurn`'s `!registerTurn` branch yourself. Do not take this plan's word for it: the whole of Task 2 exists because the branch calls `onBusy()` and RETURNS. |
| Where any symbol lives | `grep -n '<symbol>' <file>` — symbols only, deliberately |
| Every file's headroom | `wc -l` before editing |
</no_stale_inputs>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-mid-turn-steering/52-CONTEXT.md
@.planning/phases/52-mid-turn-steering/52-PATTERNS.md
@.planning/phases/52-mid-turn-steering/52-04-SUMMARY.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| Every plain-text message during a live turn is a steer (D-03) | reversible — one branch, and the busy path it replaces stays in the file for the rollback case | Confirmed against hermes' ACP adapter AFTER the decision, not before it. |
| Non-text is queued for the next turn rather than steering (D-05) | reversible in policy, **costly in code** — the queue is new machinery with a lifecycle, not a branch | hermes gates its redirect on a text-only prompt for the same reason: a steer is text that redirects, media is new material. The queue is what makes "queued" true; ratified as correction 5 of 52-01's amendment. |
| The media queue is gated on the same `Steer` seam as the steer branch | reversible — one nil check | Chosen so `AURA_AGUI_RUN_STEER=false` restores today's Telegram behaviour exactly, keeping one switch for one decision cluster rather than leaving half the change live under a rollback. |
| The inbox is conversation-keyed, which is what makes this plan possible at all (D-01) | **costly** | Already implemented by 52-02; this plan consumes it. |
</reversibility>

<stated_non_goals>
**`internal/channels/telegram/commands.go` is NOT modified.** An earlier pattern note reserved room in
it for a "busy-peek helper"; measured, none is needed. `registerTurn` already returns false when a
turn is in flight for the chat, and that return value IS the live-turn signal both branches key on.
The pending-turn slot lives in `bot_dispatch_queue.go` with its own mutex rather than being bolted
onto the `cancels` map, because the two have different lifetimes: `cancels` is cleared when the turn
ends, and the pending slot must survive exactly that moment. If a task finds itself needing a peek
method after all, that is a plan deviation — stop and say so rather than widening `commands.go`
silently.

**`internal/channels/telegram/bot_dispatch_hitl.go` is NOT modified**, and a `git diff --quiet` on it
is an acceptance criterion.

The flagged-assumption register (4 authored + 5 flagged = 9) lives in `52-02-PLAN.md`
§`<flagged_assumptions>` and is not restated here. **FA-4 is owned by this plan** and is closed by
`TestTelegramConvIDIsTheInboxKey`.
</stated_non_goals>

<no_redundancy>
**Telegram is a WRAPPER over the shared seam. It re-implements nothing.** Operator directive,
binding on this plan.

The cockpit route and the Telegram branch are two callers of ONE decision path. Concretely, the
following must exist exactly once in the tree and be called, never copied:

| Concern | The one place it lives | What Telegram does |
|---|---|---|
| Emptiness / whitespace rejection | `steer.Inbox.Push` | calls it, checks the sentinel; writes NO own check |
| Byte-cap enforcement | `steer.Inbox.Push` | same; no second `len()` guard, no second cap constant |
| Queue-full / closed detection | `steer.Inbox.Push` | same; no peek-then-push race of its own |
| The conversation key | `convID(chatID)` | calls it; never re-derives a uuid, never formats one inline |
| Marker construction, the scrub, the drain | `internal/agent/llm_agent_steer.go` | touches none of it — Telegram pushes raw text and stops |
| Persistence of the steer turn | `internal/runner/runner_steer.go` | touches none of it |
| The echo payload shape | the `SteerDelta` keys from 52-02/52-04 | reuses the keys; no parallel payload |

The ONLY Telegram-specific code this plan may add is: (a) the text-vs-attachment predicate, (b) the
rendering of a `Push` sentinel into a chat reply, (c) the `Deps` field and its injection, and (d) the
per-chat pending-turn slot D-05 requires, which has no shared-seam equivalent because it is a channel
lifecycle concern rather than a steer concern (the cockpit has no "arrived while busy" state — its
composer routes to the steer route instead). If a task finds itself writing a validation rule, a cap,
a key derivation or a payload shape that already exists, that is the signal to CALL the existing one
— or, if it is not callable from here, to extract it once and have BOTH callers use it. Never a
second copy.

Corollary for the sentinel→reply mapping: the HTTP route renders `Push` sentinels as status codes and
Telegram renders them as chat text. Those are two renderings of ONE classification. Keep the
classification in `internal/steer` (the typed sentinels) and let each surface render — do not let
either surface re-derive "is this too long" from the text itself.
</no_redundancy>

## Artifacts this phase produces

Created by THIS plan: `internal/channels/telegram/bot_dispatch_steer.go` with the routing predicate
and `turnSteeredMessage`; `internal/channels/telegram/bot_dispatch_queue.go` with the per-chat
`pendingTurns` slot, `turnQueuedForNextTurnMessage`, `turnQueuedNotDeliveredMessage` and
`mediaQueueMaxChain`; `telegram.Deps.Steer`; `Steer: chat.steer` in `buildTelegramDeps`;
`cmd/aura/steer_wiring_test.go`. New tests: `TestPlainTextDuringLiveTurnSteers`,
`TestNonTextDuringLiveTurnIsQueuedNotDropped`, `TestQueuedTurnDeliveredAfterLiveTurnEnds`,
`TestQueuedTurnNotDeliveredOnCancelIsAnnounced`, `TestQueueChainIsBounded`,
`TestSteerCarriesRawTextNotComposedContext`, `TestHitlResumeIsNotASteer`,
`TestNilSteerInboxDegradesToBusy`, `TestTelegramConvIDIsTheInboxKey`,
`TestOneInboxServesBothSurfaces`, `TestSteerRollbackDarkensBothSurfaces`.

(Full phase artifact roll-up lives in `52-01-PLAN.md` §"Artifacts this phase produces".)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route a plain-text message on a busy chat to the inbox and echo the redirect</name>
  <files>internal/channels/telegram/bot_dispatch_steer.go, internal/channels/telegram/bot_dispatch_steer_test.go, internal/channels/telegram/bot_dispatch_turn.go, internal/channels/telegram/bot.go, internal/channels/telegram/bot_dispatch.go, cmd/aura/serve_channels.go</files>
  <read_first>
    - `internal/channels/telegram/bot_dispatch_turn.go` in full — `runTurn`, `runTurnWithAssets`, `composeTurnContext`, `startTurn` (especially the ORDER: `scopeTurnToIdentity` fail-closed BEFORE `registerTurn`, and the `onBusy` call inside the `!registerTurn` branch, which is followed by a bare `return`), and `sendBusy`.
    - `internal/channels/telegram/commands.go` — `registerTurn` / `unregisterTurn` and the `cancels` map. **Read only; not modified** (see `<stated_non_goals>`). `registerTurn` returning false IS the "a turn is live for this chat" signal.
    - `internal/channels/telegram/bot_dispatch.go` — where `turnBusyMessage` is defined and every handler that reaches `runTurnWithAssets`.
    - `internal/channels/telegram/bot_dispatch_hitl.go` — its `sendBusy` call site. **This one does NOT change**: an `ask_user`-paused run is terminal and not steerable (#132 item 7), so the HITL continuation keeps the busy copy. Read it to be sure you can tell the two call sites apart.
    - `internal/channels/telegram/agui_subscriber.go` — `convID(chatID)` and `telegramChatNamespace`. This is the conversation identity; assert it equals what the runner persists rather than assuming it (FA-4).
    - `internal/channels/telegram/bot.go` — the `Deps` struct and its field-by-field convention where each field documents what a nil value degrades to. `Steer` follows it exactly.
    - `cmd/aura/serve_channels.go` — `buildTelegramDeps` and the shared-backend injection convention (`Assets: chat.assets`, `Search: chat.conv`).
    - `internal/steer/inbox.go` as 52-02 left it — `Push`'s signature and its sentinels, so the echo can distinguish "queued" from "queue full".
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` D-03, D-04, D-05, and the "Interaction with steering" note.
    - `D:/tmp/hermes-agent/acp_adapter/server.py` around the active-turn redirect and `_cmd_steer` (`grep -n "redirect\|_cmd_steer" D:/tmp/hermes-agent/acp_adapter/server.py`) — read what the reference actually replies and when it falls back to queueing.
    - `.claude/skills/golang-testing/SKILL.md`.
  </read_first>
  <behavior>
    - Text message, no attachments, a turn already live for the chat → the RAW text is pushed to the inbox under `convID(chatID)`, and the bot replies with the redirect echo. `turnBusyMessage` is NOT sent.
    - Text message with NO live turn → unchanged: a normal turn starts and nothing is pushed.
    - The pushed text is the operator's raw message, NOT the output of `composeTurnContext` — no attachment block, no knowledge catalog.
    - A push refused by the inbox (queue full / oversize / closed) replies with a message naming what happened; it never silently swallows and never claims a redirect that did not occur.
    - `t.deps.Steer == nil` → the busy reply, exactly as today, with no panic.
    - An unlinked / unresolvable chat cannot steer: `scopeTurnToIdentity` fails closed and the turn is dropped before the busy/steer decision is ever reached.
    - The HITL resume path's busy reply is byte-identical to before this plan.
  </behavior>
  <action>
Tests first, red, in `internal/channels/telegram/bot_dispatch_steer_test.go`.

Put the routing logic in a NEW sibling file `bot_dispatch_steer.go`; `bot_dispatch_turn.go` gets the
call, not the logic.

**The ordering trap, stated because it is the thing that will silently go wrong:**
`runTurnWithAssets` reassigns `text = t.composeTurnContext(...)` BEFORE calling `startTurn`. A steer
must carry the operator's raw words, so capture the raw text before that reassignment and close over
it. If you close over the composed text instead, every Telegram steer arrives at the model wrapped in
an attachment block and a document catalog, and nothing will fail — it will just quietly be wrong.
Assert the raw text in a test.

Build the `onBusy` closure at the `runTurnWithAssets` call site from three inputs: whether there are
attachments, the raw text, and whether `t.deps.Steer` is wired. Plain text + no attachments + wired
inbox ⇒ push and echo. (The attachments arm is Task 2's; until Task 2 lands, that arm keeps today's
`turnBusyMessage` — the two tasks are ONE commit each and Task 2 immediately follows.) Leave
`bot_dispatch_hitl.go`'s `sendBusy` call untouched and say why in a comment referencing #132 item 7 —
a future reader will otherwise "fix" the asymmetry.

The steer branch necessarily sits inside `startTurn`'s `!registerTurn` arm, which is AFTER
`scopeTurnToIdentity`. That is the right place and it is not accidental: it means a steer inherits
the same fail-closed identity scoping (HI-03) a turn does. Write that down.

`turnSteeredMessage` is a load-bearing constant asserted byte-for-byte, in the same language and tone
as `turnBusyMessage` (read that constant; match its register). It must tell the operator two things:
the message was applied to the running turn, and it will take effect at the next step — not
instantly, because the current tool is not killed.

Add `Steer` to `telegram.Deps` with the field-doc convention that file uses (state what nil degrades
to: neither the redirect branch nor Task 2's queue branch fires, and `sendBusy` stays the fallback —
the documented `AURA_AGUI_RUN_STEER=false` rollback). Pass `Steer: chat.steer` in `buildTelegramDeps`.

Write `TestTelegramConvIDIsTheInboxKey` to close FA-4: assert that the key this path pushes under is
the same conversation identity the runner persists for a Telegram turn. Derive both sides from the
real functions; do not hardcode a uuid.

Commit: `feat(52): steer the live turn from Telegram instead of replying busy`.
  </action>
  <acceptance_criteria>
    - `go test -race ./internal/channels/telegram/ -run 'TestPlainTextDuringLiveTurnSteers|TestSteerCarriesRawTextNotComposedContext|TestHitlResumeIsNotASteer|TestNilSteerInboxDegradesToBusy|TestTelegramConvIDIsTheInboxKey' -v` passes.
    - **no-redundancy gate:** `grep -Ec 'TrimSpace|len\(\[\]byte' internal/channels/telegram/bot_dispatch_steer.go` returns 0 — the channel performs no emptiness check and no byte-cap check of its own; both come from `Push`'s sentinels.
    - `grep -rn "AURA_AGUI_RUN_STEER" internal/channels/telegram/` returns nothing — the channel reads no steer knob directly.
    - `grep -Ec 'uuid.NewSHA1|telegramChatNamespace' internal/channels/telegram/bot_dispatch_steer.go` returns 0 — the key comes from calling `convID`, not from re-deriving it.
    - The steer file stays a wrapper, with an exact bound rather than an approximate one: `[ "$(wc -l < internal/channels/telegram/bot_dispatch_steer.go)" -le 90 ]`. If it does not fit, something that already exists was duplicated — call it instead.
    - `git diff --quiet internal/channels/telegram/bot_dispatch_hitl.go` and `git diff --quiet internal/channels/telegram/commands.go`.
    - `grep -rc 'turnBusyMessage' internal/channels/telegram/` shows the constant still used; it was not deleted.
    - A test asserts the pushed text differs from `composeTurnContext`'s output when attachments or a catalog are present.
    - `grep -n 'Steer' internal/channels/telegram/bot.go` shows the `Deps` field with a doc comment naming what nil degrades to.
    - `grep -c 'chat.steer' cmd/aura/serve_channels.go` returns 1.
    - `wc -l` ≤ 600 for every touched file; `go vet ./...`, `go build ./...` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/channels/telegram/; git diff --quiet internal/channels/telegram/bot_dispatch_hitl.go; git diff --quiet internal/channels/telegram/commands.go; [ "$(grep -c "chat.steer" cmd/aura/serve_channels.go)" = 1 ]; [ "$(grep -Ec "TrimSpace|len\(\[\]byte" internal/channels/telegram/bot_dispatch_steer.go)" = 0 ]; [ "$(wc -l < internal/channels/telegram/bot_dispatch_steer.go)" -le 90 ]; for f in internal/channels/telegram/bot_dispatch_turn.go internal/channels/telegram/bot.go internal/channels/telegram/bot_dispatch.go; do [ "$(wc -l < $f)" -le 600 ]; done; go vet ./...; echo OK'</automated>
  </verify>
  <done>The operator corrects Aura from their phone mid-task and is told it landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the queue D-05 requires — a media message during a live turn is held and delivered, never dropped</name>
  <files>internal/channels/telegram/bot_dispatch_queue.go, internal/channels/telegram/bot_dispatch_queue_test.go, internal/channels/telegram/bot_dispatch_turn.go, internal/channels/telegram/bot.go</files>
  <reversibility rating="costly">The queue is new machinery with a lifecycle, not a branch. Its delivery point sits inside the turn goroutine and its failure mode (a message the bot said it accepted, never delivered) is exactly the one STEER-04 exists to forbid on the steer path.</reversibility>
  <read_first>
    - `internal/channels/telegram/bot_dispatch_turn.go` in full again, and this time follow the goroutine: `t.wg.Go(func(){ defer cancel(); defer t.cmds.unregisterTurn(chatID); stop := pulseChatAction(...); defer stop(); t.handleTurn(turnCtx, sender, chatID, userMsg, inboundWasVoice) })`. The delivery point is immediately after `handleTurn` returns, INSIDE this goroutine, while the chat is still registered and `turnCtx` is still live. Understand why that placement is forced: after `unregisterTurn` a re-entrant `startTurn` would be racing a fresh inbound message; before it, a second `registerTurn` would fail.
    - `internal/channels/telegram/bot_dispatch.go` — every handler that reaches `runTurnWithAssets`, especially the asset/document path, so you know what has ALREADY happened to a media message by the time it gets here (the asset is ingested; `composeTurnContext` has already built its attachment block).
    - `internal/channels/telegram/bot.go` — the `Telegram` struct (where the pending slot's mutex + map live) and `Stop`'s `t.wg` drain, which is what must not be left holding an undelivered slot.
    - `52-05-PLAN.md` Task 1 — its `<behavior>` and `<action>` for `deliverLeftoverSteer` / `steerAutoDeliverMaxChain`. **Read the PLAN, not the landed code: 52-05 is in the SAME wave as this plan and its file may not exist yet.** The contract you are matching is the shape 52-05 specifies — one bounded follow-on under the existing lock hold, cap expressed as a named constant rather than an inline literal, announced out loud — not whatever line-for-line form 52-05 happens to produce. If `internal/runner/runner_steer.go` does already carry `deliverLeftoverSteer` when you get here, read it too and say so in the SUMMARY; if it does not, the PLAN is sufficient and nothing is blocked. This queue is the channel-side analogue: match the shape, never import it.
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` D-05, and 52-01's amendment correction 5 as landed (`grep -n "queued for the next turn" prd.md`).
    - `D:/tmp/hermes-agent/acp_adapter/server.py` around `queued_prompts` (`grep -n "queued_prompts" D:/tmp/hermes-agent/acp_adapter/server.py`) — the reference's fallback queue, for what it stores and when it drains it.
    - `.claude/skills/golang-concurrency/SKILL.md`, `.claude/skills/golang-testing/SKILL.md` — the slot is shared state touched from the poller goroutine and the turn goroutine; it needs a mutex and it must be `-race` clean and goleak-clean.
  </read_first>
  <behavior>
    - Message with attachments (photo / voice / document) while a turn is live, inbox wired → the ALREADY-COMPOSED text is stored in the chat's pending slot, and the bot replies that it will run after the current request. `turnBusyMessage` is NOT sent and nothing is pushed to the steer inbox.
    - When the live turn's `handleTurn` returns normally, the pending slot is drained and ONE more turn is driven with the stored text, under the same registration and the same scoped ctx.
    - A second media message arriving DURING the delivered turn is queued again for after it, but the delivery chain within one `startTurn` call is capped at one (`mediaQueueMaxChain = 1`) — no unbounded recursion.
    - If the live turn's ctx was cancelled (a `/cancel`), the pending message is NOT delivered and the operator is told it did not run. The slot is cleared; nothing is left dangling.
    - N media messages queued during one live turn are held up to a small explicit cap; past the cap the operator is told the message was not accepted rather than it being dropped silently.
    - `t.deps.Steer == nil` → no queue at all; today's `turnBusyMessage` is sent for media, byte-identically.
    - The pending slot is per-chat: a message queued for chat A never runs on chat B.
    - `Stop` drains `t.wg` with no goroutine left behind and no pending slot resurrected.
  </behavior>
  <action>
Tests first, red, in `internal/channels/telegram/bot_dispatch_queue_test.go`.

**Start by proving the defect.** Write a test against today's behaviour that sends a media message on
a busy chat and asserts the turn never runs. That test is the reason this task exists, and it is the
one that must go from red to green.

New file `internal/channels/telegram/bot_dispatch_queue.go`. It owns:
- a per-chat pending slot on the `Telegram` struct (`pendingTurns map[int64]pendingTurn` plus its own
  mutex — NOT the `cancels` map, whose lifetime is exactly wrong, see `<stated_non_goals>`);
- `pendingTurn` holding what a turn needs and nothing more: the composed text, `inboundWasVoice`, and
  the arrival time. **It does NOT hold the `tele.Context`** — that value is recycled when the handler
  returns, which is precisely why `composeTurnContext` already ran on the handler goroutine. Store the
  composed text; the composition is not repeated at delivery. Say so in the code, because a reader
  will otherwise wonder why the raw text is not stored instead (a steer stores raw, a queued turn
  stores composed, and the asymmetry is the point: a steer joins a turn, a queued message IS a turn).
- an explicit small cap on how many messages one chat may hold, with a distinct reply past it;
- `mediaQueueMaxChain = 1` as a named constant.

Wire the enqueue into the `onBusy` closure's attachments arm from Task 1: attachments present +
wired inbox ⇒ enqueue + `turnQueuedForNextTurnMessage`; anything else ⇒ today's `turnBusyMessage`.

Wire the delivery into `startTurn`'s goroutine, immediately after `handleTurn` returns and BEFORE the
deferred `unregisterTurn` fires. Guard it: if `turnCtx.Err() != nil` the turn was cancelled, so do NOT
deliver — clear the slot and send `turnQueuedNotDeliveredMessage` instead. A `/cancel` that silently
ate a message the bot said it had accepted would be the same lie STEER-04 forbids on the steer path,
and it is the one failure mode a queue adds that a drop does not.

`turnQueuedForNextTurnMessage` and `turnQueuedNotDeliveredMessage` are load-bearing constants asserted
byte-for-byte, in `turnBusyMessage`'s register and language. The first must make clear the message
was accepted and will run next; the second must make clear it did NOT run and the operator should
resend.

Commit: `feat(52): queue a media message sent during a live turn instead of dropping it`.
  </action>
  <acceptance_criteria>
    - `go test -race ./internal/channels/telegram/ -run 'TestNonTextDuringLiveTurnIsQueuedNotDropped|TestQueuedTurnDeliveredAfterLiveTurnEnds|TestQueuedTurnNotDeliveredOnCancelIsAnnounced|TestQueueChainIsBounded' -v` passes.
    - `TestNonTextDuringLiveTurnIsQueuedNotDropped` asserts the turn DOES eventually run — it counts turn invocations, not replies. A test that only asserts "a reply was sent" would pass against today's drop and is not acceptable evidence.
    - `grep -n 'mediaQueueMaxChain' internal/channels/telegram/bot_dispatch_queue.go` shows the cap is a named constant.
    - `grep -c 'turnQueuedForNextTurnMessage\|turnQueuedNotDeliveredMessage' internal/channels/telegram/bot_dispatch_queue.go` is at least 2, and a test asserts each string byte-for-byte.
    - The pending slot is not the cancels map: `git diff --quiet internal/channels/telegram/commands.go`.
    - The queue does not survive the rollback: a test asserts that with `Deps.Steer == nil` a media message on a busy chat produces `turnBusyMessage` and no enqueue.
    - A goleak-clean `Stop` test covers a chat with a pending slot outstanding.
    - `wc -l internal/channels/telegram/bot_dispatch_queue.go internal/channels/telegram/bot_dispatch_turn.go internal/channels/telegram/bot.go` — all ≤ 600.
    - `go vet ./...`, `go build ./...` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/channels/telegram/; grep -q mediaQueueMaxChain internal/channels/telegram/bot_dispatch_queue.go; git diff --quiet internal/channels/telegram/commands.go; for f in internal/channels/telegram/bot_dispatch_queue.go internal/channels/telegram/bot_dispatch_turn.go internal/channels/telegram/bot.go; do [ "$(wc -l < $f)" -le 600 ]; done; go vet ./...; echo OK'</automated>
  </verify>
  <done>A photo sent while Aura is working waits its turn and then gets one — and if the turn was cancelled, the operator hears about it instead of wondering.</done>
</task>

<task type="auto">
  <name>Task 3: One inbox, two consumers — prove the composition root wires the same singleton to both surfaces</name>
  <files>cmd/aura/steer_wiring_test.go</files>
  <read_first>
    - `cmd/aura/chat_boot.go` — `chatEnv` and where its shared singletons are constructed, as 52-04 left it after adding `steer`.
    - `cmd/aura/serve_agui.go` — the `SetSteerInbox` wiring 52-04 added, and the `SetApprovalStore(chat.pause)` precedent for one backend serving two consumers.
    - `cmd/aura/serve_channels.go` — `bootChannelsAndSetup` and `buildTelegramDeps`.
    - `internal/config/config_agui_steer.go` — the `Enabled` gate, so the rollback path is a nil seam on BOTH surfaces, not one.
    - An existing `cmd/aura` test for the composition root (`cmd/aura/chat_boot_test.go`) — the package's fixture and cleanup conventions, and the known goleak sensitivity to an unclosed pgxpool. The new assertions land in a NEW file `cmd/aura/steer_wiring_test.go` rather than being appended to an existing one, so the file this plan owns is unambiguous.
  </read_first>
  <action>
No new behaviour; one structural proof and one rollback proof, because "two ways in, one queue" is an
invariant a future refactor can break silently.

Write `TestOneInboxServesBothSurfaces` in `cmd/aura/steer_wiring_test.go`: assert that the pointer
agui receives via `SetSteerInbox` and the pointer `telegram.Deps.Steer` carries are the SAME inbox
instance — not two inboxes of the same type. Two instances would mean a cockpit steer and a Telegram
steer for the same conversation land in different queues and only one is ever drained; nothing would
fail, the steer would just vanish. This is the invariant test the assumption-delta decision in 52-02
committed to.

Then `TestSteerRollbackDarkensBothSurfaces`: with `AGUISteer.Enabled=false` the composition root wires
nil to BOTH surfaces, the agui route answers as an unwired surface, and Telegram falls back to the
busy reply for text AND for media. One surface live and the other dark is the half-live state D-12
forbids.

Commit: `test(52): pin the one-inbox-two-consumers invariant and the steer rollback`.
  </action>
  <acceptance_criteria>
    - `cmd/aura/steer_wiring_test.go` exists and `go test -race ./cmd/aura/ -run 'TestOneInboxServesBothSurfaces|TestSteerRollbackDarkensBothSurfaces' -v` passes.
    - The identity assertion is on the POINTER, not on the type: `grep -Ec '==|Same' cmd/aura/steer_wiring_test.go` is at least 1 and the test fails when fed two separately-constructed inboxes (add that negative case as a subtest).
    - `go test -race ./cmd/aura/` passes (mind the known `cmd/aura` goleak sensitivity to an unclosed pgxpool — close what the test opens).
    - `go vet ./...`, `go build ./...` exit 0; `wc -l cmd/aura/steer_wiring_test.go cmd/aura/serve_channels.go` ≤ 600.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; test -f cmd/aura/steer_wiring_test.go; go test -race ./cmd/aura/; go vet ./...; go build ./...; echo OK'</automated>
  </verify>
  <done>There is one queue and a test that goes red the day someone makes it two.</done>
</task>

</tasks>

<threat_model>
**Tier: ASVS L1 — block on `high`.** Every threat rated HIGH below must be mitigated before the plan
closes; `accept` is available only for the non-high rows and each carries its stated residual.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram inbound message → live agent turn | A message from a chat now mutates an in-flight turn instead of being deferred. |
| Telegram chat identity → Aura identity | Which operator's run a chat may steer or queue against. |
| Held message → deferred turn | A message the bot has acknowledged but not yet run is a promise the process must keep across a turn boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-30 | **Elevation of Privilege (HIGH)** | An unlinked or divergent-key chat steering or queueing against the operator's live turn | mitigate | Both branches sit inside `startTurn`'s `!registerTurn` arm, which is reached only AFTER `scopeTurnToIdentity` fails closed (HI-03). An unresolvable chat has its turn dropped before the busy/steer/queue decision exists. Asserted by a test driving an unlinked chat. |
| T-52-31 | **Tampering (HIGH)** | A Telegram steer landing in a different queue than the cockpit's, so one of them is silently never drained | mitigate | One process-wide inbox injected into both surfaces from the same `chatEnv` field, pinned by `TestOneInboxServesBothSurfaces` (pointer identity, with a negative subtest), plus `TestTelegramConvIDIsTheInboxKey` closing the key-equality assumption (FA-4). |
| T-52-35 | **Repudiation (HIGH — new, from BLOCKER 2)** | A media message the bot said it had accepted, never delivered, and never mentioned again — the exact shape STEER-04 forbids on the steer path, arriving on the media path instead | mitigate | The delivery runs inside the turn goroutine before `unregisterTurn`; a cancelled ctx produces `turnQueuedNotDeliveredMessage` rather than silence; `Stop` drains `t.wg` with a goleak test covering an outstanding slot. `TestNonTextDuringLiveTurnIsQueuedNotDropped` counts turn invocations, so a test that only checks a reply cannot pass for the drop. |
| T-52-32 | Repudiation | A new task silently absorbed into a running turn, getting no turn of its own and no acknowledgement | mitigate | D-04's echo is mandatory and byte-asserted: the bot always replies that it redirected. Residual and accepted: an operator who wanted a new task still has their words applied to the old one — the echo makes that visible immediately so they can `/cancel` and resend. |
| T-52-33 | Information Disclosure | The composed per-turn context (attachment blocks, the thread's document catalog) being pushed as steer text and re-injected mid-run | mitigate | The steer captures the RAW text before `composeTurnContext` runs, asserted by `TestSteerCarriesRawTextNotComposedContext`. The QUEUED turn deliberately stores the composed text instead, because it is a turn rather than a steer; the asymmetry is documented in the code. |
| T-52-36 | Denial of Service | Unbounded per-chat pending messages, or unbounded delivery recursion | mitigate | An explicit small per-chat cap with a distinct reply past it, and `mediaQueueMaxChain = 1`. Both are named constants with tests. |
| T-52-34 | Elevation of Privilege | Steering a run paused on `ask_user`, bypassing an approval | **accept** — closed by construction | #132 item 7; the HITL busy path is deliberately untouched and `git diff --quiet` on that file is an acceptance criterion. |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `go test -race ./internal/channels/telegram/ ./cmd/aura/`
- `go vet ./...`, `go build ./...`
- `git diff --quiet internal/channels/telegram/bot_dispatch_hitl.go internal/channels/telegram/commands.go`
- `wc -l` ≤ 600 on every touched file.
</verification>

<success_criteria>
- A plain-text Telegram message during a live turn steers it and the bot says so.
- A media message during a live turn is HELD and then RUN — proven by counting turn invocations, not
  by observing a reply — and a cancelled turn announces that it did not run.
- The HITL path is byte-unchanged and `commands.go` is untouched.
- One inbox serves both surfaces, proven by pointer identity, and the rollback darkens both together.
</success_criteria>

<output>
Create `.planning/phases/52-mid-turn-steering/52-06-SUMMARY.md` when done. Record the exact
`turnSteeredMessage`, `turnQueuedForNextTurnMessage` and `turnQueuedNotDeliveredMessage` strings —
52-08's live Telegram E2E asserts all three — plus the per-chat queue cap chosen and why.
</output>
