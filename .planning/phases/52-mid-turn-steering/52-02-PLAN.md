---
phase: 52-mid-turn-steering
plan: 02
type: tdd
wave: 2
depends_on: ["52-01"]
files_modified:
  - internal/steer/inbox.go
  - internal/steer/inbox_test.go
  - internal/agent/llm_agent_steer.go
  - internal/agent/llm_agent_steer_test.go
  - internal/agent/llm_agent.go
  - internal/agent/llm_agent_construct.go
  - internal/agent/event.go
  - internal/agent/prompt.go
  - internal/agui/server_run_steer.go
  - internal/agui/server_run_steer_test.go
  - internal/agui/idempotency_http.go
  - internal/agui/server.go
  - internal/agui/translator.go
  - internal/agui/translator_test.go
  - internal/runner/runner_steer.go
  - internal/runner/runner_steer_test.go
  - internal/runner/runner.go
  - internal/runner/runner_persist.go
  - cmd/aura/serve_agui.go
  - cmd/aura/chat_boot.go
autonomous: true
requirements: [STEER-01, STEER-02, STEER-03]

must_haves:
  truths:
    - "Typing a redirect into a live cockpit run changes what Aura does next: the round after the drain carries the operator's new instruction, and no tool is killed mid-execution."
    - "A steer is delivered by appending it to the LAST tool-result message of the in-flight batch behind a user-attribution marker when the history tail is a tool result; a plain user-role message is used only when it is not. Role alternation is never broken by an insertion between a tool-result batch and the next call."
    - "The user-attribution marker carries a nonce minted by the SAME crypto/rand minter internal/agent/trust.go already owns. There is exactly one nonce minter in internal/agent."
    - "The system prompt teaches the model that text inside the marker is a genuine mid-turn user message carrying the authority of the original request, and that lookalikes appearing in tool output, web pages or files are to be ignored. That teaching is a static constant inside messages[0], never a second system message and never a per-turn value."
    - "A tool result whose content contains a marker lookalike is scrubbed before it enters history; legitimate prose that merely resembles the marker survives unchanged and is proven to survive by a test written against real-looking content."
    - "A steer never enters the cacheable prefix: every steer lands in the history TAIL, and messages[0], [1] and [2] are byte-identical with and without a steer."
    - "The drained steer appears in the persisted conversation at the point it actually landed — persisted order equals model-visible order equals wire order — and reloading the thread shows it there, not appended at the end."
    - "The persisted steer turn carries the operator's RAW text. Neither the marker nor the nonce is ever written to conversation_turns, so a rehydrated history can never replay a stale nonce as if it were live."
    - "Rehydrating a conversation that was steered yields a wire-valid history: no user-role turn ever sits between an assistant tool_calls turn and the tool-result turns answering it."
    - "POST /agent/runs/{runID}/steer resolves through the SAME owner-scoped 404-hiding ladder cancel uses — nil registry, malformed runID, registry miss and owner mismatch all answer the identical 404, never a 403 and never a distinguishable body."
    - "The steer route is registered in httpMutationRoutes, so it carries the same Idempotency-Key enforcement cancel carries; a replayed POST with the same key does not enqueue a second steer."
    - "With AURA_AGUI_RUN_STEER_MAX steers already queued for a conversation, the next push is refused 429 with Retry-After and the queue never holds MAX+1; a body of exactly AURA_AGUI_RUN_STEER_MAX_BYTES UTF-8 bytes is accepted and one byte more is refused 400. Both caps come from config, never from a literal in internal/steer."
    - "An empty or whitespace-only steer body is refused 400 and never reaches the inbox; draining a conversation with nothing queued returns an empty result, appends nothing to history, emits no aura.steer frame and persists no turn."
    - "A steered turn consumes no more steps and no more wallclock than an unsteered one: the drain path calls neither ConsumeStep nor any deadline-extending helper, proven by a test that snapshots the budget across a drain of zero, one, and MAX steers."
    - statement: "The user-attribution marker and its nonce reach the provider byte-identically — no NFKC normalisation, HTML escaping, whitespace collapsing or case folding rewrites them between the append into history and the bytes on the wire."
      verification: backstop
    - "Every entry path pushes into ONE conversation-id-keyed queue. There is exactly one key space and exactly one inbox type in the tree; no run-id-keyed twin exists."
  artifacts:
    - path: "internal/steer/inbox.go"
      provides: "The conversation-id-keyed bounded FIFO steer inbox with Push/Drain/Close"
      contains: "func (i *Inbox) Drain("
    - path: "internal/agent/llm_agent_steer.go"
      provides: "Both drain points, the nonce-marked delivery, the tool-result-append-or-user-fallback rule, and the lookalike scrub"
      contains: "drainSteer"
    - path: "internal/agui/server_run_steer.go"
      provides: "POST /agent/runs/{runID}/steer, resolving runID to a conversation and pushing into the shared inbox"
      contains: "handleRunSteer"
    - path: "internal/runner/runner_steer.go"
      provides: "Runner-side inbox threading and the drain-time AppendTurn persistence"
      contains: "persistSteerTurn"
    - path: "internal/agui/translator.go"
      provides: "The aura.steer CUSTOM frame beside aura.artifact / aura.display / aura.mcp_view"
      contains: "SteerEventName"
  key_links:
    - from: "internal/agui/server_run_steer.go handleRunSteer"
      to: "internal/steer Inbox.Push"
      via: "sess.ThreadID resolved from the runID IS the conversation id the inbox keys on"
      pattern: "Push\\("
    - from: "internal/agent/llm_agent.go round loop"
      to: "internal/agent/llm_agent_steer.go drainSteer"
      via: "two one-line call sites — before the request is built, and after a non-terminal dispatch returns"
      pattern: "drainSteer\\("
    - from: "internal/agent/llm_agent_steer.go"
      to: "internal/agent/trust.go toolOutputNonce"
      via: "the marker reuses the existing minter; no second minter is written"
      pattern: "toolOutputNonce\\(\\)"
    - from: "internal/runner/runner_persist.go persistEvent"
      to: "internal/runner/runner_steer.go persistSteerTurn"
      via: "a SteerDelta Event appends the raw operator text as a user turn at the drain seq"
      pattern: "SteerDelta"
  prohibitions:
    - "Must NOT let a steer enter history in a form a later reader cannot attribute. A redirect that lands indistinguishable from the model's own text — or from a tool's output — makes the transcript unable to answer who said what, which is the one question an audit of an autonomous agent exists to answer."
---

<objective>
**The tracer slice.** A steer typed into a live cockpit run reaches the model at the next round
boundary, is echoed on the wire, and is persisted where it landed — end to end, production quality,
in one commit at the end of this plan.

Purpose: this is STEER-01, STEER-02 and STEER-03 together, and they are only meaningful together. An
inbox nobody drains is dead code; a drain with no persistence loses the steer on reload; persistence
with no route is unreachable. The two component tasks build the halves the tracer needs, and the
tracer task is the one that makes the capability real.

Output: a new `internal/steer` package, a new sibling file carrying both drain points, the AG-UI
route, the `aura.steer` echo frame, and drain-time persistence — with a test that drives a real steer
through a real run and asserts the next round saw it.
</objective>

<no_stale_inputs>
**No number, line reference or default written in this plan may enter the code without being
re-measured first.** The tree moves; amendment #132's own correction (b) records the design study's
line citations drifting by ~150 lines between writing and reading.

| Fact | Obtain it this way — never from this plan |
|---|---|
| Where any symbol lives | `grep -n '<symbol>' <file>`. Every citation below names a **symbol**, deliberately. If a `<read_first>` entry names a line number anywhere, treat it as wrong and grep. |
| A file's remaining headroom under the 600-LOC cap | `wc -l <file>` immediately before editing. `internal/agent/llm_agent.go` and `internal/runner/runner.go` were the two tightest files when this plan was written; re-measure both before touching either, and if an edit takes a file past ~590, do the refactor-on-touch split in the SAME commit (CLAUDE.md deep-refactor rule). |
| The queue-depth and byte caps | Read them out of the amendment landed by 52-01 (`grep -n "AURA_AGUI_RUN_STEER_MAX" prd.md`) and out of `internal/config` (`grep -rn "AURA_AGUI_RUN_STEER" internal/config/`). `internal/steer` reads them from config and hardcodes neither. |
| The exact auto-delivery wording plans 52-03/52-06 assert against | `.planning/phases/52-mid-turn-steering/52-01-SUMMARY.md` |
| The current per-package and aggregate coverage | `bash scripts/coverage_docker.sh`. The figure in CLAUDE.md predates several phases and is explicitly not current state. |
</no_stale_inputs>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-mid-turn-steering/52-CONTEXT.md
@.planning/phases/52-mid-turn-steering/52-PATTERNS.md
@.planning/phases/52-mid-turn-steering/52-01-SUMMARY.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| The inbox is keyed on **conversation id**, not run id (D-01) | **costly** — the key appears in the inbox, every push site, the drain lookup and the persisted echo; changing it later touches all four | Rating carried from CONTEXT.md D-01, with its measured reason: `RunRegistry` is agui-only and a Telegram turn has no runID at all, so a runID-keyed inbox puts STEER-05 out of reach by construction. No checkpoint — decided. |
| `POST /agent/runs/{runID}/steer` stays runID-shaped and resolves the conversation internally (D-02) | **costly** — a published HTTP contract with an idempotency registration behind it | Carried from CONTEXT.md D-02; #132 item 2 ratifies it and it preserves alignment with the existing `agent_run_cancel` registration. |
| The marker shape (nonce envelope) written into model-visible history (D-07) | **costly** — the form is what the model is taught to trust; changing it later leaves old turns carrying the old form | Carried from CONTEXT.md D-07. Mitigated by never persisting the marker (see must_haves) — the durable record carries raw text, so a marker change touches only live turns. |
</reversibility>

<assumption_delta_decision>
The phase introduces a **second** entry path for operator input into a live turn (cockpit route and
Telegram dispatch), and moves the addressing key from something derived (the run) to something chosen
(the conversation). That is a pluralization plus a derived→chosen transition, so the identity model is
re-asked here rather than inherited.

- **Primary noun: the conversation.** The steer inbox is keyed on conversation id and nothing else.
- **Decision: `promote`.** The conversation is promoted to the primary identity of a steer; the run id
  is demoted to a *lookup hint* that the HTTP route resolves and then discards. It is explicitly NOT
  `add-alongside`: no run-id-keyed queue is built beside the conversation-keyed one. Two key spaces
  would mean a Telegram steer and a cockpit steer for the same thread could sit in different queues,
  and only one of them would ever be drained — the failure would be silent, and it is exactly the
  shape D-01 rejected.
- **Rationale:** `RunRegistry` is constructed only on the AG-UI composition path; `runner.Turn` is
  called directly by the Telegram channel with no run identity in scope at all. A run-id key is
  therefore not a narrower version of a conversation key — it is unreachable from half the callers.
- **Invariant test (accepted, and required by this plan):** every entry path round-trips through the
  one conversation-id-keyed inbox. Asserted structurally — the tree contains exactly one inbox type
  and one key space — so a future phase reintroducing a run-scoped queue goes red rather than landing
  silently.
</assumption_delta_decision>

<flagged_assumptions>
The deterministic edge probe surfaced **9** items across STEER-01..06. Four were classified against
STEER-01 and are authored into `must_haves.truths` above (boundary → explicit; empty → explicit;
precision → explicit; encoding → `verification: backstop`, because whether the marker survives the
provider path byte-identically is precisely what amendment #132 says was never measured). The
remaining **five** were returned `unclassified` and are surfaced here rather than auto-resolved or
dropped. 4 authored + 5 flagged = 9; nothing was silently absorbed.

| # | Requirement | Assumption being carried, and who closes it |
|---|---|---|
| FA-1 | STEER-02 | **Assumed:** the drain path is the only place a steer could touch the budget, so a code invariant plus a live A/B is sufficient proof that steering buys no budget. Not proven until the live A/B in 52-06 compares a steered and an unsteered run on `aura.conversation_turns` (D-13). A green invariant test alone closes nothing (ACC-01). |
| FA-2 | STEER-03 | **Assumed:** appending the steer at the drain point always lands at a `seq` where the rehydrated history stays wire-valid, because the previous round's tool-result turns are already persisted by the time either drain point fires. This plan asserts it with a test; it is an assumption about the runner's persistence ordering until that test exists and passes against a real multi-round turn. |
| FA-3 | STEER-04 | **Assumed:** the 410-on-terminal contract and the auto-delivery of an already-queued leftover together leave no third case in which a steer is neither delivered nor named. Owned by 52-03; if a third case exists it will surface there, not here. |
| FA-4 | STEER-05 | **Assumed:** `convID(chatID)` on the Telegram side and `ThreadID` on the AG-UI side denote the same conversation identity, so one key space genuinely reaches both channels. Owned by 52-04, which must assert it rather than assume it. |
| FA-5 | STEER-06 | **Assumed:** the four corrections landed by 52-01 are the complete set — that no FIFTH place in #132 contradicts the reference implementation. Nobody re-read hermes end to end for this phase; the four came from a targeted reading. Recorded so a later contradiction reads as a known limit of the reading, not as a surprise. |

Also carried from CONTEXT.md's Deferred Ideas and unchanged by this plan: whether Aura's real
provider path (OpenRouter, llama.cpp — not Anthropic direct) accepts a plain `user` message at the
steer position. The tool-result-append branch needs no such verification; the fallback branch does,
and 52-06's live E2E must exercise **both** branches or the fallback ships unproven.
</flagged_assumptions>

## Artifacts this phase produces

Created by THIS plan (new symbols — not drift):
- Package `internal/steer`: `Inbox`, `Message`, `Config`, `New`, `(*Inbox).Push`, `(*Inbox).Drain`,
  `(*Inbox).Close`, `ErrEmpty`, `ErrTooLarge`, `ErrQueueFull`, `ErrClosed`
- `internal/agent/llm_agent_steer.go`: `SteerInbox` (the narrow consumer-side interface),
  `(*LlmAgent).drainSteer`, `wrapUserSteer`, `scrubSteerLookalikes`, `steerMarkerTag`
- `internal/agent`: `LlmAgentConfig.Steer`, `LlmAgent.steer`, `Actions.SteerDelta` (json
  `steer_delta`), `SteerChannelNote` (the static teaching block inside `SystemPrompt`)
- `internal/agui`: `SteerEventName` = `"aura.steer"`, `(*Server).handleRunSteer`,
  `(*Server).SetSteerInbox`, `Server.steer`, route `POST /agent/runs/{runID}/steer`,
  `httpMutationRoutes` entry `agent_run_steer`
- `internal/runner`: `runner_steer.go`, `Runner.steer`, `Deps.Steer`, `(*Runner).persistSteerTurn`
- `cmd/aura`: `chatEnv.steer`
- New test names: `TestInboxPushDrainFIFO`, `TestInboxCapsAtMax`, `TestInboxRejectsEmptyAndOversize`,
  `TestInboxConcurrentPushDrain`, `TestDrainSteerAppendsToLastToolResult`,
  `TestDrainSteerFallsBackToUserMessage`, `TestDrainSteerDoesNotConsumeBudget`,
  `TestSteerNeverEntersCacheablePrefix`, `TestScrubKeepsLegitimateLookalikeProse`,
  `TestHandleRunSteerHides404Ladder`, `TestSteerRouteIsIdempotencyRegistered`,
  `TestSteerFrameIsCustomEvent`, `TestPersistedSteerCarriesRawText`,
  `TestRehydratedSteeredHistoryIsWireValid`, `TestSteerEndToEndRedirectsNextRound`

(Full phase artifact roll-up lives in `52-01-PLAN.md` §"Artifacts this phase produces".)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: internal/steer — the conversation-id-keyed bounded inbox</name>
  <reversibility rating="costly">D-01: the conversation-id key appears in the inbox, every push site, the drain lookup and the persisted echo.</reversibility>
  <read_first>
    - `internal/agui/runregistry.go` in full — the mutex + keyed-map + lazy-reaper shape this package composes from. Note what it does that the inbox must NOT: no SSE fan-out, no subscriber set, no replay-from-seq. The inbox is consumed, not replayed.
    - `internal/agui/runsession.go` in full — the fixed-cap ring and the append/drain mutex discipline, and the `defaultRunBufferEvents` naming convention for a cap constant.
    - `internal/config/config_agui_steer.go` (created by 52-01) — the three caps this package is CONFIGURED with. Read the actual defaults from the file; do not copy a number from this plan.
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` D-01 and its Integration Points paragraph — in particular the stated single-replica boundary carried from amendment #133.
    - `.planning/phases/52-mid-turn-steering/52-PATTERNS.md` §`internal/steer` — the two-analog composite and the explicit note that the D-08 scrub happens in the agent, not here.
    - `.claude/skills/golang-concurrency/SKILL.md` and `.claude/skills/golang-testing/SKILL.md` — the mutex discipline, goleak, and `-race` expectations. If `internal/steer` starts a goroutine, it must be joinable and goleak-clean.
  </read_first>
  <behavior>
    - `Push(conv, text)` then `Drain(conv)` returns exactly that one message; a second `Drain(conv)` returns empty (drain consumes).
    - Three pushes then one drain return all three in push order (FIFO), and the slot is empty afterwards.
    - `Drain` on a conversation that was never pushed to returns an empty (len 0) result and does not allocate a map entry that outlives the call.
    - `Push` with an empty body, or a body that is only whitespace, returns `ErrEmpty` and enqueues nothing.
    - `Push` with a body of exactly `MaxBytes` UTF-8 bytes succeeds; `MaxBytes+1` bytes returns `ErrTooLarge`. The boundary test uses a **multi-byte** string, so an implementation counting runes instead of bytes fails it.
    - With `Max` messages already queued for a conversation, the next `Push` returns `ErrQueueFull` and the queue still holds exactly `Max`.
    - Two conversations are independent: filling one to `Max` does not affect the other's capacity, and draining one does not drain the other.
    - `Close` makes subsequent `Push` return `ErrClosed`; a `Drain` after `Close` still returns whatever was queued (nothing is silently destroyed by shutdown).
    - Concurrent pushes and drains from many goroutines are race-free under `-race`, and no message is lost or duplicated: the union of everything drained equals the set of everything successfully pushed.
  </behavior>
  <action>
Tests first, red, in `internal/steer/inbox_test.go`.

Create the package with a header comment stating what it is and what it is NOT: an in-memory,
**single-replica-by-construction** queue (the boundary amendment #133 states explicitly — a
multi-replica Aura would need cross-replica signalling, not a bigger queue), keyed on conversation
id because the Telegram path has no run identity in scope at all.

`Inbox` holds one mutex and one `map[string][]Message`. `Message` carries the operator's raw text
plus whatever provenance the echo frame needs (at minimum: an id, the source channel, and the
arrival time) — keep it a small value type, not an interface.

`New(cfg Config)` takes the caps; `Config` mirrors the fields of `config.AGUISteerConfig` so the
composition root passes them through without the package importing `internal/config`. **Do not
hardcode the caps here** — a literal in this package is the drift D-11 exists to prevent, one level
down. A non-positive cap resolves to the package default using the same `<=0 → default` convention
`agui.NewRunRegistry` already uses (read it, match it).

`Push` validates in this order — empty/whitespace, oversize, closed, queue-full — and returns a
distinct sentinel for each so the HTTP route can map them to 400 / 400 / 410 / 429 without string
matching. `Drain` pops and clears the slot under the same mutex, deleting the map key so an idle
conversation leaves no residue. `Close` sets a flag; it does not discard queued messages.

**Byte semantics, stated in the code:** the size cap is `len([]byte(text))` — UTF-8 bytes, not runes
and not grapheme clusters — because the cap exists to bound memory and wire size, and a rune cap
would let a multi-byte body exceed the memory bound it was written to enforce. Say so in a comment
next to the check; the boundary test will otherwise look arbitrary to the next reader.

Commit: `feat(52): add the conversation-keyed bounded steer inbox`.
  </action>
  <acceptance_criteria>
    - `go test -race ./internal/steer/` passes, including the concurrent push/drain case.
    - `go test ./internal/steer/ -run 'TestInbox' -v` shows the boundary cases at `Max`/`Max+1` and `MaxBytes`/`MaxBytes+1` as distinct passing subtests.
    - The `MaxBytes` boundary test uses a string containing at least one multi-byte rune, provable by `grep -n 'MaxBytes' internal/steer/inbox_test.go` showing a non-ASCII literal or an explicit byte-length assertion.
    - `grep -c '8\b\|16384' internal/steer/inbox.go` shows no ratified cap value hardcoded in the package; caps arrive via `Config`.
    - `go list -deps ./internal/steer | grep -c 'chetto1983/aura/internal/config'` returns 0 — the package is configured, not coupled.
    - `go vet ./...` and `go build ./...` exit 0; `wc -l internal/steer/inbox.go` ≤ 600.
    - `grep -rn 'runID\|RunID' internal/steer/` returns nothing — there is one key space and it is the conversation.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/steer/; [ "$(go list -deps ./internal/steer | grep -c chetto1983/aura/internal/config)" = 0 ]; ! grep -rq "RunID" internal/steer/; go vet ./internal/steer/; echo OK'</automated>
  </verify>
  <done>One queue, one key space, bounded in both dimensions, safe under -race, and configured rather than hardcoded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: internal/agent/llm_agent_steer.go — both drain points, the nonce-marked delivery, the teaching note, and the lookalike scrub</name>
  <reversibility rating="costly">D-07: the marker form is what the model is taught to trust and it is written into model-visible history.</reversibility>
  <read_first>
    - `internal/agent/llm_agent.go` in full. You need the whole `Run` loop, not an excerpt: the budget gate and its `skipBudgetGate`/`recoveryTurn` interplay, the `transportRetry` path that REPLAYS an already-built request, `roundBudget`, `prepareReasoningRequest`, the content-stop branch, the `pauseCalls` branch, and the `dispatch` call whose non-`done` return re-enters the loop. Locate each by symbol.
    - `internal/agent/llm_agent_finalize.go` in full — the established sibling-file precedent, including its header comment stating WHY it exists (the LOC ceiling) and `maybeRecover`'s `a.history = append(a.history, llm.Message{Role: llm.RoleUser, ...})` mechanic. **Copy the mechanic, not the semantics:** a steer is not a one-shot-per-run gate.
    - `internal/agent/llm_agent_dispatch.go` — find where the `RoleTool` results are appended to `a.history` (`grep -n 'RoleTool' internal/agent/llm_agent_dispatch.go`). This is what determines whether the history tail is a tool result at each drain point, which is the whole basis of the append-vs-fallback rule.
    - `internal/agent/trust.go` in full — `wrapUntrustedToolOutput`, `toolOutputNonce`, `renderToolResultForPrompt`, `untrustedSource` and `trustedToolNames`. The steer marker reuses `toolOutputNonce()`; note also that `wrapUntrustedToolOutput` applies `html.EscapeString` and NFKC normalisation to tool content, which is directly relevant to the backstop truth about the marker surviving byte-identically.
    - `internal/agent/llm_agent_construct.go` in full — the `Gateway`/`Ledger` nil-safe field-threading pattern the `Steer` field copies exactly.
    - `internal/agent/event.go` — the `Actions` struct and the doc comments on `ArtifactDelta` / `ViewDelta` explaining WHY those are untyped `map[string]any` rather than typed pointers. `SteerDelta` follows the untyped-map rule, not `Display`'s typed-pointer rule.
    - `internal/agent/prompt.go` in full — `SystemPrompt` and `systemMessage()`, plus the constant's own doc comment about byte-stability and the prompt-cache discount, and its ONE RULE about naming only loaded tools.
    - `internal/agent/prompt/hash.go` and any test asserting the prompt prefix (`grep -rln 'SystemPrompt' internal/agent/`) — adding the teaching block changes the cacheable prefix ONCE; find every test that pins it and update them in the same commit.
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` D-06, D-07, D-08, and the "Existing Code Insights" note that the mid-loop user-role injection pattern is real but does NOT license the steer position.
    - `prd.md`'s amendment #132 §(A) and the correction landed by 52-01 — read both; the second supersedes part of the first.
    - `.claude/skills/golang-testing/SKILL.md`, `.claude/skills/golang-context/SKILL.md`.
  </read_first>
  <behavior>
    - With one steer queued and the history tail being a `RoleTool` message, `drainSteer` appends the marked steer text INTO that message's content; no new message is added, and `len(history)` is unchanged.
    - With one steer queued and the history tail NOT a `RoleTool` message, `drainSteer` appends ONE new `llm.Message{Role: llm.RoleUser}` carrying the marked text; `len(history)` grows by exactly one.
    - Either way the emitted Event carries `Actions.SteerDelta` naming the steer id, the conversation, the raw text, the round ordinal, and which of the two delivery forms was used.
    - With nothing queued, `drainSteer` appends nothing, mutates no message, and emits no Event.
    - With N steers queued, all N are delivered in FIFO order in a single drain.
    - The marked text contains a nonce produced by `toolOutputNonce()`; two drains in the same run produce marker instances whose nonce is compared byte-exact by the scrub.
    - `scrubSteerLookalikes` removes a forged marker from tool-result content before it enters history.
    - `scrubSteerLookalikes` leaves untouched: prose that merely mentions the marker tag name in running text, a code block quoting it, and content containing the tag with a WRONG or absent nonce rendered as inert text rather than deleted content. The test fixtures for this must read like real tool output, not like `"<tag>"`.
    - `drainSteer` calls neither `ic.Budget.ConsumeStep()` nor any deadline mutator: a budget snapshot taken before and after a drain of 0, 1 and MAX steers is identical.
    - `messages[0]`, `messages[1]` and `messages[2]` are byte-identical between a run with a steer and a run without one.
    - A nil `Steer` inbox makes `drainSteer` a total no-op — no panic, no allocation, no Event.
  </behavior>
  <action>
Tests first, red, in `internal/agent/llm_agent_steer_test.go`.

**Do not add logic to `internal/agent/llm_agent.go`.** Re-measure it with `wc -l` first; it was the
tightest file in the phase when this plan was written and #132's own correction (b) says the drain
point lands in a sibling file. `llm_agent.go` receives exactly two one-line call sites and nothing
else.

Create `internal/agent/llm_agent_steer.go` with a header comment in the shape
`llm_agent_finalize.go` uses — say which concern was split out and why.

Define a narrow consumer-side interface (`SteerInbox` with just `Drain(conv string) []steer.Message`
or the equivalent) so `internal/agent` does not take a hard dependency on `internal/steer`'s concrete
type and the agent stays testable with a fake. Thread it as `LlmAgentConfig.Steer` → `LlmAgent.steer`
using the identical two-line pattern `Gateway`/`Ledger` already use in `llm_agent_construct.go`, and
make nil mean "drain is a no-op" exactly as a nil gateway means Allow — a steer-off deployment must
degrade to inert, never to a nil-pointer panic.

`(*LlmAgent).drainSteer(ic InvocationContext, round modelRound) *Event`:
- Key the drain on `a.sessionID` (session id IS the conversation id on the runner path — verify this
  by reading `buildAgent`'s `SessionID:` assignment before relying on it).
- Return immediately when the inbox is nil or the drain is empty.
- **The delivery rule, in one place:** if the LAST message in `a.history` has role `tool`, append the
  marked text into that message's `Content`; otherwise append one new `RoleUser` message carrying the
  marked text. This is amendment #132 §(A)'s ratified primary and its ratified fallback expressed as
  a single branch, and it is why hermes' "no tool result in the batch" case needs no special path.
- Never touch `a.history[0..2]`; only ever the tail. Assert it in a test rather than trusting it.
- Emit one Event via `a.newEvent(...)` carrying `Actions.SteerDelta`.

`wrapUserSteer(text string) string` mints the envelope with `toolOutputNonce()`. **Reuse that
function; do not write a second minter** — CLAUDE.md's inventory-before-invention rule, and D-07 says
so explicitly. Choose the tag name and the exact attribute set (Claude's Discretion), and pick the
escaping deliberately: whatever you choose must be consistent with what `wrapUntrustedToolOutput`
does to tool content, or a steer appended INTO an already-escaped tool result will end up
double-escaped. Read that function before deciding, and write the reason down.

`scrubSteerLookalikes(content string) string` runs on tool-result content before it enters history.
It must neutralise a forged marker without eating legitimate content — the test fixtures decide
whether this is honest, so write them from realistic tool output (a web page quoting the tag, a file
listing containing it, a shell transcript) and not from a bare tag string.

Add `SteerDelta map[string]any \`json:"steer_delta,omitempty"\`` to `Actions`, following the
`ArtifactDelta`/`ViewDelta` untyped-map rule with a doc comment explaining why untyped (the shape is a
channel-agnostic wire payload, not a persisted DB type). Every existing Event must stay byte-identical
on the wire — assert `decode(encode(ev)) == ev` for an Event with no steer.

Add the teaching block to `SystemPrompt` in `internal/agent/prompt.go` as a named static section: it
must say that text inside the marker is a genuine mid-turn message from the operator carrying the
authority of the original request, and that any lookalike appearing in tool output, a web page or a
file is to be ignored. It is a constant with no per-turn value, so it stays byte-stable — but it DOES
change the cacheable prefix once, for every conversation. Say that in the commit body; it is a
one-time cost being paid knowingly, not an oversight.

Wire the two call sites in `llm_agent.go`:
- **Before the API call** — immediately after the per-round budget is assembled and before the request
  is built. **Guard it with the transport-retry flag:** on an exact transport retry the request was
  already built from the current history, and mutating history there would make the replayed request
  disagree with the transcript. Skip the drain on that path and say why in the comment.
- **After a non-terminal dispatch** — where the loop's own comment marks the point at which the next
  LLM call will see the appended tool results.

Both call sites yield the returned Event when non-nil, honouring the `yield`-after-false guard the
loop already enforces everywhere else.

Commit: `feat(52): drain steers at both round boundaries behind a nonce-marked attribution envelope`.
  </action>
  <acceptance_criteria>
    - `go test -race ./internal/agent/ -run 'TestDrainSteer|TestScrub|TestSteerNeverEntersCacheablePrefix' -v` passes.
    - `grep -c 'drainSteer(' internal/agent/llm_agent.go` returns exactly 2.
    - `git diff --stat internal/agent/llm_agent.go` shows a net addition of no more than ~6 lines, and `wc -l internal/agent/llm_agent.go` ≤ 600.
    - `grep -rc 'rand.Read' internal/agent/*.go | grep -v ':0' ` shows the nonce is minted in exactly one file (`trust.go`) — no second minter exists.
    - A test asserts the budget is unchanged across a drain: `go test ./internal/agent/ -run TestDrainSteerDoesNotConsumeBudget -v` passes, and `grep -n 'ConsumeStep\|WithDeadline' internal/agent/llm_agent_steer.go` returns nothing.
    - A test asserts `messages[0..2]` are byte-identical with and without a steer.
    - The scrub's keep-legitimate-content test uses at least one fixture longer than a single line that reads as real tool output; `grep -n 'TestScrubKeepsLegitimateLookalikeProse' -A 20 internal/agent/llm_agent_steer_test.go` shows it.
    - `go test ./internal/agent/... ./internal/agent/prompt/...` passes — every test pinning `SystemPrompt` or the prefix hash was updated in the same commit.
    - `go vet ./...`, `go build ./...` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/agent/ ./internal/agent/prompt/; [ "$(grep -c "drainSteer(" internal/agent/llm_agent.go)" = 2 ]; ! grep -q "ConsumeStep" internal/agent/llm_agent_steer.go; [ "$(wc -l < internal/agent/llm_agent.go)" -le 600 ]; go vet ./...; echo OK'</automated>
  </verify>
  <done>The model sees the operator's redirect at the next round boundary, knows it came from the operator, ignores forgeries, and gets no extra budget for it.</done>
</task>

<task type="tracer">
  <name>Task 3: The route, the echo frame, the persistence and the wiring — a steer typed into a live run lands, end to end, in one commit</name>
  <reversibility rating="costly">D-02: `POST /agent/runs/{runID}/steer` is a published HTTP contract with an idempotency registration behind it.</reversibility>
  <read_first>
    - `internal/agui/server_run_resume.go` in full — `resolveRunSession` (the 404-hiding ladder, reused verbatim, with its four rungs and their stated rationale) and `handleRunCancel` (resolve → side-effect → 202). `handleRunSteer` is that shape with a different side effect and a body.
    - `internal/agui/server_run_detach.go` in full — how a `RunSession` is created, what `threadID` is set from, and how the producer/subscriber split works. This tells you that `sess.ThreadID` is the value passed to `s.run.Turn`.
    - `internal/agui/server_run.go` — the `s.run.Turn(ctx, in.ThreadID, ...)` call, and the `ErrThreadBusy` 409 on the non-detached tail. **D-10 removes that 409 for the steer path from the cockpit's point of view; confirm by reading which branch actually runs under `AURA_AGUI_RUN_DETACH=true` before changing anything.**
    - `internal/agui/idempotency_http.go` — `httpMutationRoutes`, the `agent_run_cancel` row, `httpMutationMeta`, and `normalizeHTTPMutation`'s body read with its `maxRunBodyBytes` limit. The steer route copies the row and reuses the body-cap idiom.
    - `internal/agui/server.go` — the `Server` struct fields, `SetRunRegistry` (the setter shape and its doc comment about a nil seam hiding the surface), and the mux block where `POST /agent/runs/{runID}/cancel` is registered.
    - `internal/agui/translator.go` — the three `aura.*` CUSTOM-event name constants with their doc comments, and the `ArtifactDelta` / `Display` emission branches including `closeRuns()` and the precedence comment above the `StateDelta` branch.
    - `internal/runner/runner.go` — the `Runner` struct, the `Deps` struct, `New`, `Turn`, `runTurn`, `turnLocked` (specifically `appendUserTurn` and where the agent history is loaded), and `buildAgent`'s `LlmAgentConfig{...}` literal. **`wc -l` this file first**; it was the second-tightest file in the phase.
    - `internal/runner/runner_reasoning.go` in full — the file-per-concern sibling precedent `runner_steer.go` mirrors.
    - `internal/runner/runner_persist.go` — `persistEvent`'s branch chain, `turnTracker`, `persistToolTurnEvent` and `flushToolCalls`. You are adding one branch to `persistEvent` and the handler lives in `runner_steer.go`.
    - `internal/conversations/store_append.go` — `AppendTurn` / `AppendTurnTx` and `AppendTurnParams`.
    - `cmd/aura/serve_agui.go` — the `RunRegistry` construction block and the `SetApprovalStore(chat.pause)` / `SetApprovalGrantStore(...)` wiring, which is the established "one backend, two consumers" injection shape.
    - `cmd/aura/chat_boot.go` — the `chatEnv` struct and where its shared singletons are constructed.
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` D-02, D-10, and the Integration Points paragraph.
    - `.planning/phases/52-mid-turn-steering/52-PATTERNS.md` §`internal/agui/server_run_steer.go`, §`idempotency_http.go`, §`translator.go`, §`runner.go + runner_steer.go`, §`serve_agui.go`.
    - `.claude/skills/golang-testing/SKILL.md`.
  </read_first>
  <action>
This is the tracer: production quality, no stubs on the happy path, landed as ONE commit so no
intermediate state exists in which the route accepts a steer nothing drains.

**The route.** `internal/agui/server_run_steer.go`, `handleRunSteer`:
- Call `resolveRunSession` — zero new resolution logic. Every rung of that ladder already answers the
  identical 404 (hide existence, never 403); do not add a 403 and do not vary the body.
- Read the JSON body under the same size-limited read `normalizeHTTPMutation` uses; decode the steer
  text from a single named field.
- Map the inbox's sentinels to the ratified refusal ladder: empty/whitespace → 400, oversize → 400,
  queue-full → 429 with `Retry-After`, run already terminal → 410. Read the terminal-run definition
  off the `RunSession` (the same terminal state the reaper and the resume route already consult);
  do not invent a second notion of terminal.
- On success `Push(sess.ThreadID, text)` and answer 202, mirroring cancel's `{"status": ...}` body
  shape. **`sess.ThreadID` IS the conversation id** — verify that by reading the `Turn` call site and
  `buildAgent`'s `SessionID:` assignment before you rely on it; it is FA-4's cockpit half.
- Register `"POST /agent/runs/{runID}/steer": httpMutationMeta("agent_run_steer")` beside the cancel
  row, and `mux.HandleFunc` + `SetSteerInbox` beside their cancel/registry twins. Both files are
  one-or-two-line changes; resist putting any steer logic in `server.go`.

**The echo.** Add `SteerEventName = "aura.steer"` beside the other three constants with a doc comment
in their style, and one emission branch keyed on `ev.Actions.SteerDelta` slotted with the
artifact/display branches, before the generic `StateDelta` branch, using the same `closeRuns()` +
`yield(events.NewCustomEvent(...))` + `continue` shape. It is ring-buffered because every frame in
that ring is (#132 item 4) — nothing extra to do, but say so in the comment so a reader does not go
looking for special handling.

**The persistence.** `internal/runner/runner_steer.go`:
- `persistSteerTurn` appends the steer as a `RoleUser` turn via `Conv.AppendTurn` at the moment the
  Event arrives, so persisted order equals model-visible order equals wire order (#132 item 5).
- **Persist the RAW operator text, never the marked form.** The marker carries a per-run nonce; a
  nonce in durable history would be replayed on the next rehydration as though it were live, which is
  the precise forgery D-07 exists to prevent. Write that reason in the code.
- One branch added to `persistEvent` in `runner_persist.go`, calling into this file.
- Prove FA-2 rather than assuming it: a test that drives a multi-round turn with a steer drained
  between rounds, reloads the history through the real loader, and asserts no user-role turn sits
  between an assistant `tool_calls` turn and the tool-result turns answering it.

**The wiring.** One process-wide `*steer.Inbox`, constructed where the other shared singletons are
built in `cmd/aura`, hung on `chatEnv`, and injected into agui (`SetSteerInbox`) — gated on
`cfg.AGUISteer.Enabled` exactly as the `RunRegistry` is gated on `AGUIRun.Detach`, so the documented
rollback leaves a nil seam rather than a half-live feature. `runner.Deps.Steer` → `Runner.steer` →
one line in `buildAgent`'s `LlmAgentConfig` literal. **`runner.go` gets struct/Deps/literal lines and
nothing else**; any real logic goes in `runner_steer.go`. Re-`wc -l` it after editing and split
on-touch if it crosses ~590.

**The end-to-end proof.** `TestSteerEndToEndRedirectsNextRound`: start a real run through the agui
handler against a scripted LLM client that makes a tool call on round 1; POST a steer to the route
while the run is live; assert (a) 202, (b) the round-2 request the fake client receives contains the
operator's text behind the marker, (c) an `aura.steer` CUSTOM frame reached the stream, (d) a
`RoleUser` turn carrying the raw text was persisted at the drain point, and (e) the run's step count
matches an identical unsteered run. **Exercise BOTH delivery branches** — one case where the history
tail is a tool result and one where it is not — or the fallback branch ships unproven (see FA-1 and
the deferred provider-path item).

Commit: `feat(52): land mid-turn steering end to end from the cockpit`, body naming the route, the
frame, the persistence contract and the two delivery branches.
  </action>
  <acceptance_criteria>
    - `go test -race ./internal/agui/ ./internal/runner/ ./internal/agent/ ./internal/steer/` passes.
    - `go test ./internal/agui/ -run TestSteerEndToEndRedirectsNextRound -v` passes with subtests for both the tool-result-append branch and the user-message fallback branch.
    - `grep -c 'agent_run_steer' internal/agui/idempotency_http.go` returns 1, and the mutation-route coverage test in that package still passes (it is fail-closed and will flag an unregistered mutation route).
    - `grep -c 'resolveRunSession' internal/agui/server_run_steer.go` returns 1, and `grep -c 'StatusForbidden\|403' internal/agui/server_run_steer.go` returns 0.
    - `grep -n 'SteerEventName' internal/agui/translator.go` shows the constant and exactly one emission branch.
    - A test asserts the persisted steer turn's content contains neither the marker tag nor a nonce: `go test ./internal/runner/ -run TestPersistedSteerCarriesRawText -v`.
    - `go test ./internal/runner/ -run TestRehydratedSteeredHistoryIsWireValid -v` passes.
    - `wc -l` ≤ 600 for every file touched, checked explicitly for `internal/agent/llm_agent.go`, `internal/runner/runner.go`, `internal/agui/server.go` and `internal/runner/runner_persist.go`.
    - `git show --stat HEAD` shows the route, the frame, the persistence and the wiring in ONE commit — no intermediate commit accepts a steer that nothing drains.
    - `go vet ./...`, `go build ./...` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/agui/ ./internal/runner/ ./internal/agent/ ./internal/steer/; [ "$(grep -c agent_run_steer internal/agui/idempotency_http.go)" = 1 ]; ! grep -q "403\|StatusForbidden" internal/agui/server_run_steer.go; for f in internal/agent/llm_agent.go internal/runner/runner.go internal/agui/server.go internal/runner/runner_persist.go; do [ "$(wc -l < $f)" -le 600 ]; done; go vet ./...; echo OK'</automated>
  </verify>
  <done>An operator types into a running turn from the cockpit and the next round acts on it — echoed on the wire, persisted where it landed, with no tool killed and no budget bought.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cockpit HTTP → live run | An authenticated POST mutates the state of an in-flight agent turn belonging to an identity. |
| Tool output / web page / file content → model authority | Content Aura itself fetched is about to sit next to text the model is taught to obey. |
| Operator text → durable conversation history | What is written now is what a later rehydration replays to the model. |
| Steer body → in-process memory | Unbounded operator text on a long-running turn is an availability surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-10 | **Spoofing (HIGH — the threat the nonce exists to close)** | A forged user-attribution marker in tool output, a web page or a file, granting attacker text the authority of the operator | mitigate | The marker carries a per-run nonce from `toolOutputNonce()` (unpredictable, not derived from anything in the transcript), the system prompt teaches the model to ignore lookalikes, and `scrubSteerLookalikes` removes forged markers from tool content before it enters history. Three independent layers because any one of them alone fails silently. Proven by the scrub tests and by the `messages[0..2]` byte-identity test. |
| T-52-11 | **Elevation of Privilege (HIGH)** | Steering another identity's run | mitigate | `handleRunSteer` reuses `resolveRunSession` verbatim: nil registry / malformed runID / registry miss / owner mismatch all answer the identical 404. No 403 exists on this path, so a probe cannot distinguish "not yours" from "does not exist". Asserted by `TestHandleRunSteerHides404Ladder`. |
| T-52-12 | Tampering | A replayed POST enqueuing the same steer twice, so the model receives a doubled instruction | mitigate | The route is inventoried in `httpMutationRoutes` as `agent_run_steer`, inheriting the same Idempotency-Key enforcement `agent_run_cancel` carries. The package's fail-closed mutation-route coverage test flags any unregistered mutation route. |
| T-52-13 | Denial of Service | Unbounded queued text on a long-lived run exhausting process memory | mitigate | Per-conversation FIFO capped at `AURA_AGUI_RUN_STEER_MAX`, per-message body capped at `AURA_AGUI_RUN_STEER_MAX_BYTES` measured in UTF-8 bytes, both enforced in `Push` before anything is stored, both proven at the boundary and one step past it. Queue-full answers 429 + Retry-After rather than growing. |
| T-52-14 | Repudiation | A steer landing in history in a form no later audit can attribute to the operator | mitigate | Every steer is delivered inside a named attribution envelope in the model-visible history AND persisted as an explicit `RoleUser` turn at the drain seq, plus echoed as an `aura.steer` frame in the run's replay ring. Three records, one event. This is the plan's authored prohibition. |
| T-52-15 | Information Disclosure | A per-run nonce leaking into durable history and being replayed on a later rehydration as if live | mitigate | The persisted turn carries the RAW operator text; the marker and nonce exist only in the in-memory model-visible history for the life of the run. Asserted by `TestPersistedSteerCarriesRawText`. |
| T-52-16 | Tampering | A steer inserted between an assistant `tool_calls` message and its tool results, producing a wire-invalid history a strict provider rejects | mitigate | The delivery rule appends INTO the last tool-result message rather than inserting between messages, which is exactly why #132 §(A) chose it; the fallback fires only when the tail is not a tool result. Rehydration is separately asserted wire-valid by `TestRehydratedSteeredHistoryIsWireValid`. |
| T-52-17 | Elevation of Privilege | Steering a run that is paused on `ask_user`, bypassing the approval gate | **accept** — closed by construction | #132 item 7: `ask_user`-paused runs are terminal and not steerable, so the route's 410 branch covers it and the two surfaces never overlap at runtime. Recorded rather than mitigated because no new control is added; if a future change makes a paused run non-terminal, this line is where the hazard reopens. |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. Every symbol used is stdlib or already in `go.mod`; no RESEARCH.md Package Legitimacy Audit exists for this phase, so adding a dependency is a plan deviation that stops for the operator. |
</threat_model>

<verification>
- `go test -race ./internal/steer/ ./internal/agent/ ./internal/agent/prompt/ ./internal/agui/ ./internal/runner/`
- `go vet ./...`, `go build ./...`
- File-size gate on every touched Go file (`wc -l`, ≤ 600), re-measured after the last edit.
- `git show --stat HEAD` — the tracer landed as one commit.
</verification>

<success_criteria>
- A steer POSTed to a live cockpit run reaches the model at the next round boundary in the ratified
  form, with the fallback branch also exercised.
- The steer is echoed as an `aura.steer` CUSTOM frame and persisted as a raw-text user turn at the
  seq it landed at, leaving a wire-valid rehydrated history.
- A steered turn's step and wallclock consumption is identical to an unsteered one's.
- No identity can steer another identity's run, and no probe can tell the difference between
  "not yours" and "never existed".
</success_criteria>

<output>
Create `.planning/phases/52-mid-turn-steering/52-02-SUMMARY.md` when done. Record the chosen marker
tag and escaping decision (52-04 and 52-06 assert against them), whether the plain-`user` fallback
branch was exercised against a real provider or only a fake, and the post-edit `wc -l` of
`llm_agent.go`, `runner.go` and `server.go` so the next plan knows its remaining headroom.
</output>
