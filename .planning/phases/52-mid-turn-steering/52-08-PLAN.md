---
phase: 52-mid-turn-steering
plan: 08
type: execute
wave: 5
depends_on: ["52-03", "52-04", "52-05", "52-06", "52-07"]
files_modified:
  - .planning/phases/52-mid-turn-steering/52-VALIDATION.md
  - docs/aura-quality-snapshot.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [STEER-01, STEER-02, STEER-03, STEER-04, STEER-05, STEER-06, RESUME-01]

must_haves:
  truths:
    - "A real operator-driven scenario on the running stack shows Aura mid-task, receives a typed redirect FROM THE COCKPIT COMPOSER, and her NEXT round acts on the new instruction — observed live, not asserted by a unit test."
    - "No tool is killed mid-execution by a steer: the in-flight tool call completes and its result is in the transcript."
    - "The same steer works from Telegram on the live stack, not only from the cockpit."
    - "A photo sent to Telegram during a live turn is QUEUED and then RUN on the live stack — observed as a second turn actually executing, not as a reply saying it will."
    - "STEER-02 is judged on what it actually forbids: the step CEILING and the wallclock DEADLINE of the steered run are identical to the unsteered run's. Consumption delta is reported as corroboration, with an explicit tie-breaker so a legitimate extra round does not read as a violation."
    - "A steer that arrives after its run has ended is observed arriving as the next turn with the visible line, on the live stack."
    - "Reloading the steered thread in the cockpit shows the steer at the point it landed, not appended at the end."
    - "The scenario is scored against the phase's five Success Criteria and the score is >= 9.8; anything lower is recorded as such and the phase does not close."
    - "The full tag matrix is re-measured on this tree — the aggregate owned-surface coverage figure is a number produced by this run, not carried from any prior phase — and the frontend gates (vitest coverage, Stryker) are re-measured too, because this phase ships web source."
    - "docs/aura-quality-snapshot.md is re-attested for every row whose CI-gate-path glob matches a file this phase changed, and scripts/quality_snapshot_gate.sh prints ok locally before the push."
    - "Every claim in 52-VALIDATION.md that was NOT proven live is written down as not proven, with what would prove it — a green suite closes nothing."
  artifacts:
    - path: ".planning/phases/52-mid-turn-steering/52-VALIDATION.md"
      provides: "The live evidence, the A/B comparison, the score, and the honest list of what stayed unproven"
      contains: "Success Criteria"
    - path: "docs/aura-quality-snapshot.md"
      provides: "Re-attested rows for every CI-gate path this phase touched"
      contains: "Last measured"
  key_links:
    - from: ".planning/phases/52-mid-turn-steering/52-VALIDATION.md"
      to: "aura.conversation_turns and aura.tool_invocations"
      via: "the A/B budget comparison is read from the live tables, not from test output"
      pattern: "conversation_turns"
  prohibitions:
    - "Must NOT close this phase on a green test suite. Every requirement here is about what happens to a person mid-task; a suite that passes proves the code does what the code was written to do, and says nothing about whether the redirect landed."
    - "Must NOT report a coverage or mutation number this phase did not measure on this tree. A carried-forward figure presented as current state is the same failure as a stale PRD line, with more authority."
    - "Must NOT record a STEER-02 failure on a consumption delta alone. STEER-02 forbids EXTENDING the budget, and a redirect that legitimately needs one more round is not an extension — judging it as one invites the phase to be stalled by a false red, or worse, invites explaining away the very number it forbade explaining away."
---

<objective>
**Gate 3 — Definition of Done.** Drive the real agent on the running stack, steer it mid-task from
both surfaces, compare a steered run against an unsteered one on the live tables, score the scenario,
re-measure the gates, and write down what stayed unproven.

Purpose: CLAUDE.md's DEFINITION OF DONE is a real E2E scored above 9.8, and ACC-01's policy — set in
Phase 45 and governing every phase since — is that a green suite closes nothing. This phase's
requirements are all about what happens to a person who is watching Aura work and wants to change
her mind; the only instrument that measures that is a person changing their mind against a live
stack.
</objective>

<no_stale_inputs>
**This plan exists to produce fresh numbers. It may not consume a single stale one.**

| Fact | Obtain it this way — never from a document |
|---|---|
| Container freshness | Verify `aura:local` was built from current HEAD BEFORE any live run; rebuild if not (`docker compose build aura` is ~30s on a warm cache and builds the web bundle too — which matters this phase, because 52-07 changed the cockpit). A live result from a stale image measures the previous commit. |
| Which model is actually live | Read `aura.settings` — routing is DB-driven. Never assume the local model is serving. |
| Aggregate + per-package coverage | `bash scripts/coverage_docker.sh` on this tree. The figure in CLAUDE.md predates several phases and the graph-store removal, and is explicitly not current state. |
| Frontend coverage + mutation | `make web-test` and `make web-mutation`. This phase ships web source, so the frontend tiers are part of the close, not optional. |
| Go mutation score | Run `go-mutesting` **in a throwaway worktree** — it rewrites files in place in the live checkout and a concurrent `go test` will compile a mutant. |
| Snapshot gate result | `AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh` — must print `ok: … checked N row(s)` |
| The exact strings to assert live | `52-01-SUMMARY.md` (auto-delivery wording), `52-02-SUMMARY.md` (marker), `52-05-SUMMARY.md` (`steerAutoDeliveryNotice`), `52-06-SUMMARY.md` (`turnSteeredMessage`, `turnQueuedForNextTurnMessage`, `turnQueuedNotDeliveredMessage`), `52-07-SUMMARY.md` (the `chat.steer.*` strings) |
| The next migration slot, if any late fix needs one | `ls internal/db/migrations/ \| tail -1` then +1 |
</no_stale_inputs>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-mid-turn-steering/52-CONTEXT.md
@.planning/phases/52-mid-turn-steering/52-04-SUMMARY.md
@.planning/phases/52-mid-turn-steering/52-05-SUMMARY.md
@.planning/phases/52-mid-turn-steering/52-06-SUMMARY.md
@.planning/phases/52-mid-turn-steering/52-07-SUMMARY.md
@.planning/phases/52-mid-turn-steering/52-03-SUMMARY.md
</context>

## Artifacts this phase produces

Created by THIS plan: `.planning/phases/52-mid-turn-steering/52-VALIDATION.md`; re-attested rows in
`docs/aura-quality-snapshot.md`; the STEER-01..06 and RESUME-01 checkboxes and traceability rows in
`.planning/REQUIREMENTS.md`; the Phase 52 plan-completion rows in `.planning/ROADMAP.md`.

**Note on VALIDATION.md.** No RESEARCH.md exists for this phase, so no Nyquist validation document
preceded it; `52-VALIDATION.md` is created here and is the phase's first and only validation artifact.
Its Manual-Only / Not-Proven table is therefore load-bearing rather than decorative.

<tasks>

<task type="auto">
  <name>Task 1: Drive the real agent, steer it from both surfaces, and run the D-13 A/B on the live tables</name>
  <files>.planning/phases/52-mid-turn-steering/52-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` D-13 — the A/B is the evidence for STEER-02; the code invariant is only what stops a future change moving the budget unnoticed.
    - All six prior SUMMARYs in this phase — for the exact strings this task asserts live.
    - `.planning/ROADMAP.md` Phase 52's five Success Criteria, as 52-01 left them (SC#3 now says auto-delivery).
    - `CLAUDE.md` §DEFINITION OF DONE and §"NO TEST BABY SITTING" / no-skip-as-green.
    - The cockpit E2E driver recipe and its `Idempotency-Key` requirement — `/api/conversations` and `/agent/run` answer 400 without it, and the steer route carries the same enforcement. Also mind the SSE token-split behaviour when asserting on streamed text.
    - `.env` for the live stack credentials; `compose.yaml` for which services must be up (including `--profile sandbox` if the deployed profile is the hardened one).
  </read_first>
  <action>
**Bring the stack up and verify the image is current before anything else** (see `<no_stale_inputs>`).
A live number from a stale container is worse than no number — and this phase changed the web bundle,
so a stale image means the old cockpit.

Design ONE real scenario — a genuine multi-step task the operator would actually give Aura, long
enough to have a middle. Not a smoke test, not a trivial question. Then run it **three times**:

1. **Unsteered baseline.** Full scenario, no interruption. Record the conversation id, the step
   ceiling, the wallclock deadline, the steps consumed and the wallclock elapsed from
   `aura.conversation_turns` (and `aura.tool_invocations` for the tool sequence).
2. **Steered from the cockpit.** Same scenario, same opening prompt. Mid-task, while a tool is
   visibly running, **type a redirect into the composer and press send** — this is 52-07's affordance
   and it is the leg that makes ROADMAP SC#1's cockpit half real. Capture: whether the composer
   accepted the input at all, the HTTP status of the steer POST, the `aura.steer` frame on the SSE
   stream, the notice the cockpit rendered, whether the in-flight tool completed (it must), the
   content of the NEXT round's request, and the persisted position of the steer turn. Then reload the
   thread and confirm the steer renders where it landed rather than at the end.
3. **Steered from Telegram.** Same scenario from the phone. Send a plain-text redirect while the turn
   is live; confirm the redirect echo comes back with the exact `turnSteeredMessage` string, and that
   the next round acts on it.

Then these targeted live cases:
- **Telegram media queue (D-05).** Send a PHOTO during a live turn. Confirm (a) it does NOT steer,
  (b) the reply is `turnQueuedForNextTurnMessage`, and (c) **a second turn actually runs afterwards**
  carrying that photo. (c) is the case that distinguishes the queue this phase built from the drop it
  replaced; a reply alone proves nothing. Then repeat and `/cancel` the live turn instead, and confirm
  `turnQueuedNotDeliveredMessage` arrives rather than silence.
- **Leftover auto-delivery.** Steer a run late enough that it ends before the drain (steer during the
  final round, or against a run about to finish). Confirm the leftover arrives as the next turn with
  the `steerAutoDeliveryNotice` line, and that it was persisted as an ordinary user turn — **once.**
  Run the count, do not eyeball the thread: `SELECT count(*) FROM aura.conversation_turns WHERE
  conversation_id = <id> AND role = 'user' AND content LIKE '%<leftover text>%'` must return exactly
  1. Two rows is the double-write B-2 named (52-04's drain-time branch firing alongside the follow-on
  turn's own persistence) and it is invisible to every other observation in this list — the notice
  still renders, the turn still runs, the status is still 202. Record the number itself in the
  evidence table, not the word "persisted".
- **Terminal refusal.** POST a steer at a run that has already finished; confirm the 410 and a body
  that tells the operator what to do, and confirm the cockpit renders it as the `ended` refusal rather
  than swallowing it.
- **Resume, not only reload.** Reconnect the cockpit to the steered run mid-stream (or drive
  `GET /agent/runs/{runID}/events` with a `Last-Event-ID` preceding the steer) and confirm the
  `aura.steer` frame comes back in sequence. This is STEER-03's third leg; 52-04 proved it in a test,
  and this is the live corroboration.
- **RESUME-01.** Exercise the three folded defects against the live stack: a resume POST carrying an
  accept with an empty answer (expect 400), a resume whose decision the pause's policy does not permit
  (expect 403), and a pending approval left past the TTL (expect it to resolve as an expiry,
  observable in `resumed_answer`, and NOT as a yes).

**The D-13 A/B is the deliverable, not a footnote — and it is judged on the right quantity.**
STEER-02 says a steer does not EXTEND the step or wallclock budget. So:
- **The pass/fail gate is the ceiling and the deadline.** Compare run 1 and run 2 on the step ceiling
  and the wallclock deadline the run was given. If either moved by any amount, STEER-02 is NOT met and
  the phase does not close — say so rather than explaining it away.
- **Consumption is corroboration, reported with its own tie-breaker.** Report steps consumed and
  wallclock elapsed for both runs with the deltas. A steered run MAY legitimately consume more,
  because a redirect changes the work: that is the operator getting what they asked for, not a budget
  extension. Record such a delta together with the round count and what the extra round did, and mark
  STEER-02 met on the ceiling/deadline evidence. Only a moved ceiling or a moved deadline is a
  violation. State this rule in VALIDATION.md before the numbers, so the reading is fixed in advance
  rather than chosen after seeing them.

**Exercise both delivery branches or say you did not.** One steer must land while the history tail is
a tool result (the ratified primary) and one while it is not (the plain-`user` fallback, which
amendment #132 says was never verified against Aura's real provider path — OpenRouter/llama.cpp, not
Anthropic direct). If the live provider rejects the fallback shape, that is a finding worth the whole
phase; record it as a finding, not as a flaky run.

Record everything in `52-VALIDATION.md`: per-Success-Criterion evidence rows, the A/B table with its
stated reading rule, the score out of 10 against the five criteria, and a **Manual-Only / Not Proven**
table listing every claim that did not get live evidence with what would prove it. FA-1 through FA-5
from `52-02-PLAN.md`'s register are each either closed here with evidence or restated as still open —
none may quietly disappear.
  </action>
  <acceptance_criteria>
    - `52-VALIDATION.md` exists with one evidence row per Success Criterion, each naming an observable (a status code, a table row, a stream frame, a screenshot, a chat reply) rather than a test name.
    - The A/B section states its reading rule BEFORE its numbers: ceiling + deadline are the gate, consumption is corroboration.
    - The A/B table shows the step ceiling, the wallclock deadline, the steps consumed and the wallclock elapsed for the steered and unsteered runs with the deltas, sourced from `aura.conversation_turns`, and names the two conversation ids so the query can be re-run.
    - If the steered run consumed more, the document records the round count and what the extra round did, and states explicitly that the ceiling and deadline did not move — or, if either did, that STEER-02 is not met.
    - The leftover auto-delivery row is recorded as a COUNT: the `aura.conversation_turns` query above is quoted in `52-VALIDATION.md` with its result, and the result is `1`. If it is 2, STEER-04 is not met and the phase does not close.
    - The tool that was in flight when the steer landed appears COMPLETED in `aura.tool_invocations` for the steered run — proving nothing was killed mid-execution.
    - The cockpit section records that the composer accepted the typed redirect (no dead input, no 409), the steer POST's status, and the notice string rendered.
    - The Telegram section records the exact echo string received, and the media case records **that a second turn ran**, with its conversation-turn row as the evidence — not merely that a reply arrived.
    - The `/cancel` variant records `turnQueuedNotDeliveredMessage` arriving.
    - Both delivery branches are recorded as exercised, or the fallback is explicitly recorded as unexercised with the reason.
    - The RESUME-01 section records the 400, the 403 and the expiry's `resumed_answer` value.
    - The score is stated as a number; if < 9.8 the document says the phase does not close and why.
    - FA-1..FA-5 each appear with a closed/still-open disposition.
    - The container-freshness check and the live model read from `aura.settings` are both recorded with their values.
  </acceptance_criteria>
  <verify>
    <human-check>
Open the cockpit against the running stack, start the scenario, and while Aura is visibly mid-task type a redirect into the composer.

1. Does the composer accept the message — is the input live and the send reachable (no dead button, no 409)?
2. Does the running tool finish rather than getting killed?
3. Does her NEXT step do the new thing?
4. Does the cockpit say the turn was redirected?
5. Does the steer appear in the thread at the point it landed, after a page reload?
6. Repeat from Telegram with a plain-text message: does the bot reply that it redirected, and does the next step change?
7. Send a photo during a live turn: does it correctly NOT steer — and does that photo's turn then actually RUN once the live turn ends?

Reply "approved" if all seven hold, or describe exactly what you saw instead.
    </human-check>
    <automated>bash -c 'set -e; V=.planning/phases/52-mid-turn-steering/52-VALIDATION.md; test -f "$V"; grep -q "conversation_turns" "$V"; grep -qi "count(\*)" "$V"; grep -qi "not proven\|manual-only" "$V"; grep -qi "ceiling" "$V"; grep -q "FA-1" "$V"; grep -q "FA-5" "$V"; grep -q "RESUME-01" "$V"; echo OK'</automated>
  </verify>
  <done>The redirect landed in front of a person, from the composer and from the phone, the queued photo got its turn, and the budget ceiling did not move — measured on the live tables, with the unproven parts named.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure the gates on THIS tree, re-attest the quality snapshot, and close the requirement rows</name>
  <files>docs/aura-quality-snapshot.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/52-mid-turn-steering/52-VALIDATION.md</files>
  <read_first>
    - `CLAUDE.md` §"Quality tooling & gates", §"COVERAGE FLOOR 85%", §"COVERAGE GATE TAG SET", and §"QUALITY SNAPSHOT AT PHASE CLOSE" — read all four; they carry the exact commands and the exact failure modes.
    - `scripts/coverage_gate.sh` and `scripts/coverage_docker.sh` — in particular the disposable-DB provisioning and the refusal to run `db_integration` against a database named `aura` locally. That guard exists because this gate once truncated the live deployment's auth tables.
    - `scripts/quality_snapshot_gate.sh` — what it checks and what `ok:` output looks like.
    - `scripts/web_dist_freshness.sh` and the `Makefile` `web-*` targets — this phase ships web source, so the frontend tiers are part of the close.
    - `docs/aura-quality-snapshot.md` — the row format, the `Last measured` field, and the prepend-a-re-attestation-note convention (keep prior notes as `Prior …`).
    - `.planning/REQUIREMENTS.md` STEER-01..06 and RESUME-01, and the traceability table.
    - `.planning/ROADMAP.md` Phase 52's `**Plans**:` line, its plan list, and the phase table row.
  </read_first>
  <action>
**Run the full matrix in WSL or CI Linux, not on Windows.** Prepend `~/.local/bin:~/go/bin` to PATH
and export the composed DSNs; CGO is what makes `-race` real.

- `make quality` then `make quality-full` with the stack up.
- `bash scripts/coverage_docker.sh` — report the aggregate owned-surface figure THIS run produced,
  plus the per-package figures for `internal/steer`, `internal/agent`, `internal/agui`,
  `internal/runner`, `internal/askuser`, `internal/channels/telegram`, `internal/config`. If the
  aggregate is below the 85% floor, the phase does not close; the likely cause is daemon-gated code
  without daemon-free unit tests for its pure logic, and `internal/steer` is pure logic with no
  excuse.
- **Frontend gates**, because 52-07 shipped web source: `make web-lint`, `make web-test` (report the
  coverage figure), `make web-mutation` (report killed/total), and `make web-freshness` (must print
  `equals a fresh build.`). A phase that changes `web/src` and reports only Go numbers has measured
  half of itself.
- Go mutation spot-check on this phase's critical files — at minimum `internal/steer/inbox.go`,
  `internal/agent/llm_agent_steer.go` and `internal/channels/telegram/bot_dispatch_queue.go`, target
  >= 70% killed. **Run it in a throwaway worktree**; it rewrites files in place and leaves mutants
  behind.
- `govulncheck ./...`.

Then re-attest `docs/aura-quality-snapshot.md`: for EVERY row whose CI-gate-path glob matches a file
this phase changed, bump `Last measured` to today and PREPEND a re-attestation note — a fresh
measurement where the metric moved, or a metric-neutral justification naming exactly what changed and
why the number cannot move. Keep prior notes as `Prior …`. **Mind that this phase touched `web/**` and
`internal/webui/dist`**, so the frontend rows are in scope too. Verify locally with the
`AURA_QUALITY_CHANGED_FILES` invocation in `<no_stale_inputs>` and do not push until it prints `ok:`.

Close the paperwork honestly: tick STEER-01..06 and RESUME-01 in `.planning/REQUIREMENTS.md` and
update their traceability rows; update `.planning/ROADMAP.md`'s Phase 52 `**Plans**:` line, its plan
list and the phase table row with the real plan count and status. **Tick only what the live evidence
supports** — a requirement whose live proof is missing is recorded as partially met with the gap
named, exactly as Phase 46 recorded its half-proven MCP-05 round-trip rather than rounding it up.

Then merge the phase work to `master` and run fresh post-merge verification before pushing, per
CLAUDE.md's merge-on-task-close and git-push discipline. Confirm CI is green after the push — with
particular attention to `web-dist-freshness`, which is CI-only and will not have been caught by any
pre-push hook.
  </action>
  <acceptance_criteria>
    - `make quality-full` passes on this tree, with the stack up, run in WSL/CI Linux.
    - `bash scripts/coverage_docker.sh` reports an aggregate owned-surface figure >= 85%, and that figure is written into `52-VALIDATION.md` and the snapshot as a number measured on this tree today.
    - `make web-test` and `make web-mutation` pass their configured thresholds, with both figures recorded; `make web-freshness` prints `equals a fresh build.`
    - Go mutation spot-check >= 70% killed on `internal/steer/inbox.go`, `internal/agent/llm_agent_steer.go` and `internal/channels/telegram/bot_dispatch_queue.go`, recorded with the killed/total counts.
    - `go test -race` green on every package this phase touched.
    - `govulncheck ./...` clean.
    - `AURA_QUALITY_CHANGED_FILES=... bash scripts/quality_snapshot_gate.sh` prints `ok: … checked N row(s)` with N > 0.
    - Every `wc -l` on a file this phase touched is <= 600.
    - STEER-01..06 and RESUME-01 rows in `.planning/REQUIREMENTS.md` reflect the live evidence, with any partial marked partial.
    - `.planning/ROADMAP.md` Phase 52 shows the real plan count (8) and status, and its plan list names all eight.
    - The work is merged to `master`, post-merge verification is green, and CI is green after the push (`gh run list` confirms), including the `web-dist-freshness` job.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh | grep -q "^ok:"; go vet ./...; go build ./...; for f in $(git diff --name-only origin/master..HEAD -- "*.go"); do [ ! -f "$f" ] || [ "$(wc -l < $f)" -le 600 ]; done; grep -q "RESUME-01" .planning/REQUIREMENTS.md; echo OK'</automated>
  </verify>
  <done>Every gate has a number produced by this tree today — Go and frontend both — the snapshot says so, and the requirement rows claim exactly what was proven, no more.</done>
</task>

</tasks>

<threat_model>
**Tier: ASVS L1 — block on `high`.** Every threat rated HIGH below must be mitigated before the plan
closes; `accept` is available only for the non-high rows and each carries its stated residual.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live stack → recorded evidence | What gets written into VALIDATION.md becomes the phase's truth; an unverified claim here outlives every code comment. |
| Coverage/mutation tooling → the live database | The coverage gate has truncated a live deployment's auth tables before. |
| A committed build artifact → every operator's binary | `internal/webui/dist` ships embedded; a stale commit is caught only in CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-50 | **Repudiation (HIGH — the threat this plan exists to close)** | A phase sealed on a green suite, with the live behaviour never observed | mitigate | The Success-Criterion evidence rows must each name an observable, not a test name; the human-check gate is a real operator driving the real cockpit and the real bot; the Not-Proven table is a required section. |
| T-52-51 | **Tampering (HIGH)** | The coverage gate pointed at the live `aura` database and truncating it | mitigate | `scripts/coverage_docker.sh` provisions a disposable `aura_cov` DB and refuses `db_integration` against a database named `aura` when run locally. Use the script; never hand-roll the tagged run, and never run it as the superuser `aura` role. |
| T-52-56 | **Repudiation (HIGH — new, from WARNING 6)** | STEER-02 judged on consumption alone, producing a false red that stalls the phase — or inviting the number to be explained away, which is the exact failure the requirement forbids | mitigate | The pass/fail gate is the step ceiling and the wallclock deadline, which is what STEER-02 actually forbids extending. Consumption is corroboration with a stated tie-breaker, and the reading rule is written into VALIDATION.md BEFORE the numbers so it cannot be chosen after seeing them. |
| T-52-57 | **Repudiation (HIGH — new, from BLOCKER 2)** | The Telegram media case recorded as passing on the strength of a reply, when the queued turn never ran | mitigate | The acceptance criterion demands the second turn's `aura.conversation_turns` row as the evidence, and the human-check asks the operator whether the photo's turn actually ran. A reply-only observation is explicitly insufficient. |
| T-52-52 | Tampering | `go-mutesting` rewriting the live worktree and a concurrent `go test` compiling a mutant | mitigate | Run mutation in a throwaway worktree, explicitly required by the task. |
| T-52-53 | Repudiation | A carried-forward coverage or mutation figure presented as current state | mitigate | The plan's authored prohibition; the acceptance criteria require the figure to be produced by this run and written into both VALIDATION.md and the snapshot with today's date — Go AND frontend, because this phase ships web source. |
| T-52-58 | **Repudiation (HIGH — new, from BLOCKER B-2)** | The leftover recorded as "persisted" on an observation that cannot distinguish one row from two, sealing a duplicated operator message into the durable record | mitigate | The evidence for the auto-delivery case is a `count(*)` against `aura.conversation_turns` quoted with its result, and the result must be `1`. The notice string, the follow-on turn running, and the 202 all pass on a duplicate, so none of them is accepted as the evidence. |
| T-52-54 | Repudiation | A requirement ticked on partial evidence | mitigate | Partial evidence is recorded as partial, following the precedent Phase 46 set when it refused to round up a half-proven MCP round-trip. |
| T-52-55 | Denial of Service | A live E2E run against a stale container image, measuring the previous commit — acutely relevant this phase, because the image carries the web bundle | mitigate | Container-freshness verification is a recorded precondition of Task 1, with its result written into VALIDATION.md. |
| T-52-58 | Tampering | A stale committed `internal/webui/dist` reaching `master`, caught only by the CI-only freshness job ~20 minutes after the push | mitigate | `make web-freshness` runs locally on Linux as part of this task's gate list, before the merge; CI confirms afterwards. |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. Tooling (`go-mutesting`, `govulncheck`, `golangci-lint`, Stryker) is already installed per CLAUDE.md's quality-tooling section and `web/package.json`; if one is missing, install it from the pinned source named there, not from an ad-hoc fetch. |
</threat_model>

<verification>
- `make quality` and `make quality-full` (WSL/CI Linux, stack up).
- `bash scripts/coverage_docker.sh` — aggregate >= 85%, disposable DB only.
- `make web-lint`, `make web-test`, `make web-mutation`, `make web-freshness`.
- Go mutation spot-check in a throwaway worktree, >= 70% killed.
- `govulncheck ./...`.
- `scripts/quality_snapshot_gate.sh` prints `ok:`.
- The live scenario, driven by a person, scored >= 9.8.
</verification>

<success_criteria>
- All five ROADMAP Success Criteria have live evidence or an explicit not-proven entry, including
  SC#1's cockpit-composer leg and SC#5's channel leg.
- The D-13 A/B shows the steered run's step ceiling and wallclock deadline unmoved, with consumption
  reported under its stated reading rule.
- The Telegram media queue is proven by a turn that ran, not by a reply that promised one.
- Every gate figure was measured on this tree today — Go and frontend — the snapshot is re-attested
  and the gate is green.
- The requirement rows claim exactly what was proven, and the branch is merged and CI-green.
</success_criteria>

<output>
Create `.planning/phases/52-mid-turn-steering/52-08-SUMMARY.md` when done, and
`.planning/phases/52-mid-turn-steering/52-VALIDATION.md` as the phase's evidence record.
</output>
