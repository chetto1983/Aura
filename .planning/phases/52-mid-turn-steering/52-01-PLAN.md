---
phase: 52-mid-turn-steering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - internal/config/config_agui_steer.go
  - internal/config/config_agui_steer_test.go
  - internal/config/config_askuser.go
  - internal/config/config_askuser_test.go
  - internal/config/config.go
  - internal/config/config_knobs.go
autonomous: true
requirements: [STEER-06, STEER-01]

must_haves:
  truths:
    - "The PRD amendment carrying all five corrections to #132 is committed BEFORE any steering implementation commit exists in this phase — provable from git log order, not from prose."
    - "prd.md §Mid-turn steering no longer asserts a single drain point, an operator-retyped leftover steer, a marker whose construction is unspecified, or AURA_AGUI_RUN_STEER defaulting false; each superseded claim is struck through in place with the replacement beside it, the same convention amendments #123 and #132 already use."
    - "REQUIREMENTS.md STEER-04 and ROADMAP.md Phase 52 SC#3 both describe automatic delivery as the next user turn; neither still says the operator re-sends the message."
    - "ROADMAP.md Phase 52's `Depends on:` line no longer claims Phase 51, and says what the execution order actually is."
    - "The folded amendment-#133 approval-resume work has a requirement ID of its own (RESUME-01) in REQUIREMENTS.md and on ROADMAP Phase 52's Requirements line, so a downstream verifier looking for its evidence is sent to the plan that implements it rather than to a STEER plan that does not."
    - "config_knobs.go catalogues AURA_AGUI_RUN_DETACH with the SAME default the code loads; the two disagree nowhere in the tree."
    - "config.Load() with no steer env set yields AGUISteer.Enabled == true; setting AURA_AGUI_RUN_STEER=false yields false; setting it to a malformed value falls back to true and does not fail the boot."
    - "Every env var this phase introduces appears in BOTH config_knobs.go and prd.md's env catalogue; a knob catalogued in one place only is the exact drift D-11 exists to close."
    - "With AURA_AGUI_RUN_STEER_MAX queued steers already held for a conversation, pushing the Nth succeeds and pushing the (N+1)th is refused; a steer body of exactly AURA_AGUI_RUN_STEER_MAX_BYTES UTF-8 bytes is accepted and one byte more is refused. The caps are read from config, never from a literal in the steer package."
  artifacts:
    - path: "internal/config/config_agui_steer.go"
      provides: "The AURA_AGUI_RUN_STEER* knob bundle with non-fatal envutil fallbacks"
      contains: "AGUISteerConfig"
    - path: "internal/config/config_agui_steer_test.go"
      provides: "The default / explicit-override / malformed-falls-back triple for each new knob"
      contains: "func Test"
    - path: "internal/config/config_knobs.go"
      provides: "The catalogue rows for the new knobs and the corrected AURA_AGUI_RUN_DETACH default"
      contains: "AURA_AGUI_RUN_STEER"
    - path: "prd.md"
      provides: "The amendment carrying the five corrections plus the env-catalogue rows"
      contains: "Mid-turn steering"
  key_links:
    - from: "internal/config/config.go loadBase()"
      to: "internal/config/config_agui_steer.go loadAGUISteerConfig"
      via: "the same call-site convention AGUIRun: loadAGUIRunConfig() already uses"
      pattern: "loadAGUISteerConfig"
    - from: "internal/config/config_knobs.go catalogue"
      to: "internal/config/config_agui_steer.go envutil defaults"
      via: "every Name/Default row states the value the loader actually applies"
      pattern: "AURA_AGUI_RUN_STEER"
  prohibitions:
    - "Must NOT ship a flag defaulted off and call it a feature switch. A capability nobody can reach without editing the environment is dark code by the operator's own ruling (D-12); the knob exists as the rollback, and the rollback is the off position, not the shipping position."
    - "Must NOT resolve the AURA_AGUI_RUN_DETACH contradiction by changing the CODE to match the stale catalogue. The code default is the one with a passing live campaign behind it; the catalogue row is the side with no evidence, and correcting the evidenced side to match the unevidenced one would launder a documentation bug into a behaviour change."
---

<objective>
**The amendment gate.** Land the five corrections that reading hermes and the live tree produced
against PRD amendment #132, move the three planning documents that still assert the superseded
behaviour, give the folded #133 work a requirement id of its own, and reconcile the knob surface —
all before a single line of steering code exists.

Purpose: STEER-06 is not paperwork. Amendment #132 was ratified on 2026-08-24 from the design study
plus a first reading of the reference implementations; the 2026-08-25 discussion read further and
found four places where the ratified text is wrong, and the plan-check pass against the live tree
found a fifth (correction 5 below). CLAUDE.md's PRD-first rule is explicit that a measurement beats
the PRD and the PRD gets corrected — citing date and evidence — *before* the code that depends on the
correction. Every later plan in this phase implements the corrected contract, so if this plan does not
land first, seven plans implement a document that contradicts itself.

Output: one amendment commit (prd.md + REQUIREMENTS.md + ROADMAP.md), then one config commit
(`internal/config/*`). Nothing else in the phase may commit before the first of those two.
</objective>

<no_stale_inputs>
**Every number below is measured at execution time, never inherited from this plan.** The plan was
written on 2026-08-25 against a tree that keeps moving; a hardcoded number in a plan is a lie with a
timestamp on it.

| Fact | How to obtain it — run this, do not trust a number written anywhere |
|---|---|
| The next PRD amendment number | `grep -o 'Amendment #[0-9]*' prd.md \| grep -o '[0-9]*' \| sort -n \| tail -1` then +1 |
| The next migration slot (only if a migration turns out to be needed — it is not expected in this plan) | `ls internal/db/migrations/ \| tail -1` then +1 |
| Any file's LOC headroom against the 600 cap | `wc -l <file>` immediately before editing it |
| The line number of any symbol cited below | `grep -n '<symbol>' <file>` — the line numbers in this plan and in 52-PATTERNS.md are hints that WILL have drifted; amendment #132's own correction (b) records the design study drifting by 150 lines. Locate by symbol, never by line. |
| The ratified queue-depth and byte caps | `grep -n "AURA_AGUI_RUN_STEER_MAX" prd.md` — read the numbers OUT of the amendment at execution time; the values written in this plan are a plan-time reading and must be re-confirmed against prd.md before they enter code |
| The current coverage figure | `bash scripts/coverage_docker.sh` — CLAUDE.md states the last full-matrix figure (90.3%) predates phases 37A-37F, 42, 43 and the graph-store removal and is NOT current state |
</no_stale_inputs>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-mid-turn-steering/52-CONTEXT.md
@.planning/phases/52-mid-turn-steering/52-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| `AURA_AGUI_RUN_STEER` ships default `true` (D-12) | **costly** — flipping it later means an amendment line plus a re-run of the live E2E that justified the default | Carried from CONTEXT.md D-12, itself carried from D-11's identical shape. Operator ruling, already made; no checkpoint. The rollback is the explicit `=false`. |
| `AURA_AGUI_RUN_DETACH` catalogue corrected to `true` (D-11) | reversible — one map row | Decided by D-11; the code side already carries a passing live campaign recorded in its own source comment (read the comment, do not quote a score from this plan). |
| Amendment text (the ratified contract every later plan implements) | **costly** — CONTEXT.md rates the underlying decisions D-06/D-07/D-09 costly, and the amendment is where they become binding | No checkpoint: these are recorded operator decisions from `/gsd-discuss-phase`, not open questions. Re-asking would re-litigate a settled discussion. |
| Minting `RESUME-01` as a new requirement id | reversible — one bullet and one traceability row | The folded todo has no id today, which is exactly why the plan-check pass found the STEER-04 mis-tag. Minting it makes the fold traceable; deleting it later costs two lines. |
</reversibility>

## Artifacts this phase produces

Created by THIS plan (new symbols — not drift):
- `internal/config/config_agui_steer.go`: type `AGUISteerConfig` with fields `Enabled`, `Max`,
  `MaxBytes`; func `loadAGUISteerConfig`
- `internal/config/config_askuser.go`: type `AskUserConfig` with field `PauseTTLSec`; func
  `loadAskUserConfig`
- `internal/config/config.go`: `Config.AGUISteer`, `Config.AskUser`
- New env vars: `AURA_AGUI_RUN_STEER`, `AURA_AGUI_RUN_STEER_MAX`, `AURA_AGUI_RUN_STEER_MAX_BYTES`,
  `AURA_ASKUSER_PAUSE_TTL_SEC`
- Corrected catalogue default: `AURA_AGUI_RUN_DETACH` → `"true"`
- New requirement id: `RESUME-01` in `.planning/REQUIREMENTS.md` (+ its traceability row, + its
  addition to ROADMAP Phase 52's `**Requirements**:` line)
- New test names: `TestAGUISteerConfigDefaultsAndOverrides`, `TestAskUserConfigDefaultsAndOverrides`,
  `TestEveryNewKnobIsCatalogued`
- One PRD amendment (number resolved at execution time — see `<no_stale_inputs>`)

**Full phase artifact roll-up** (for the source-grounding pass — these are created by later plans in
this phase and are therefore new, not drift):

- `internal/steer` package with `Inbox`, `Message`, `Config`, `New`, `Push`, `Drain`, `Close`,
  `ErrEmpty`, `ErrTooLarge`, `ErrQueueFull`, `ErrClosed` (52-02)
- `internal/agent/llm_agent_steer.go` with `(*LlmAgent).drainSteer`, `steerMarkerOpen`,
  `steerMarkerClose`, `wrapUserSteer`, `scrubSteerLookalikes`, `SteerInbox` interface;
  `agent.LlmAgentConfig.Steer`; `agent.Actions.SteerDelta` (json `steer_delta`);
  `agent.SteerChannelNote` inside `prompt.go`'s `SystemPrompt` (52-02)
- `agui.SteerEventName` (`"aura.steer"`), `(*agui.Server).handleRunSteer`,
  `(*agui.Server).SetSteerInbox`, route `POST /agent/runs/{runID}/steer`, `httpMutationRoutes` entry
  `agent_run_steer`; `internal/runner/runner_steer.go` with `Runner.steer`, `Deps.Steer`,
  `(*Runner).persistSteerTurn`; `chatEnv.steer` (52-04)
- `(*Runner).deliverLeftoverSteer`, `steerAutoDeliverMaxChain`, `steerAutoDeliveryNotice` (52-05)
- `telegram.Deps.Steer`, `turnSteeredMessage`, `turnQueuedForNextTurnMessage`,
  `internal/channels/telegram/bot_dispatch_steer.go`,
  `internal/channels/telegram/bot_dispatch_queue.go` with the per-chat `pendingTurns` slot,
  `mediaQueueMaxChain` (52-06)
- `web/src/chat/steerRun.ts` with `steerRun`, `SteerRefusal`; `web/src/chat/ExternalStoreChat_steer.ts`
  with `useSteerSend`; `web/src/chat/SteerNotice.tsx`; `steerNoticeValue` + the `onSteer` pump seam in
  `sseAdapter.ts` / `sseResume.ts`; i18n keys under `chat.steer.*` (52-07)
- `internal/askuser/expire.go` with `(*Store).ExpirePendingOlderThan`, `ActionExpired`,
  `ErrPauseExpired`; `internal/agui/server_project.go`'s `validateResumeEntries`,
  `ErrResumeEmptyAnswer`, `ErrResumeDecisionNotAllowed`; the per-pause decision policy inside the
  existing `resume_context` jsonb (52-03)

<tasks>

<task type="auto">
  <name>Task 1: Land the five corrections to amendment #132, move the three documents that still assert the superseded behaviour, and mint RESUME-01 — ONE commit</name>
  <files>prd.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <reversibility rating="costly">The amendment is the contract every later plan in this phase implements; changing it after those plans land means re-opening seven plans.</reversibility>
  <read_first>
    - `prd.md` §Mid-turn steering (locate with `grep -n "Mid-turn steering" prd.md`) — **read the whole amendment #132 block end to end**, including its "What is ratified" enumeration of twelve items and its "What this measurement does NOT prove" closing paragraph. You are amending this text in place; you cannot amend text you have not read.
    - `prd.md` §The approval resume path (amendment #133, locate with `grep -n "approval resume path" prd.md`) — it says its gaps are for "Phase 47", a phase deleted on 2026-08-25. That pointer moves in this same commit.
    - `prd.md`'s env-var catalogue section (locate with `grep -n "Indice completo env vars\|Caps & Limits" prd.md`) — the shape the four new rows must match.
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` §"Specific Ideas" — the numbered corrections 1-4 and documents 5-7 are part of the literal work list of this task; and §"Implementation Decisions" D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12 for the evidence each correction cites; and §"Folded Todos" for what RESUME-01 names.
    - `.planning/REQUIREMENTS.md` STEER-01..06 (locate with `grep -n "STEER-0" .planning/REQUIREMENTS.md`) — both the requirement list and the traceability table. Read the surrounding requirement ids to copy the bullet + table-row format exactly.
    - `.planning/ROADMAP.md` `### Phase 52: Mid-turn steering` in full — the REVISED 2026-08-24 block, the `Depends on:` line, the `**Requirements**:` line, and the five Success Criteria.
    - `internal/channels/telegram/bot_dispatch_turn.go` — read `startTurn` and follow what happens on `!registerTurn`: `onBusy()` is called and the function **returns**. There is no queue anywhere on that path. This is the measurement correction 5 records; confirm it yourself before writing the amendment line.
    - `D:/tmp/hermes-agent/agent/turn_finalizer.py` around `pending_steer` (locate with `grep -n "pending_steer" D:/tmp/hermes-agent/agent/turn_finalizer.py`) and its two consumers in `gateway/run.py` and `cli.py` (`grep -n "pending_steer\|leftover" D:/tmp/hermes-agent/gateway/run.py D:/tmp/hermes-agent/cli.py`) — the evidence correction 1 cites. Cite what the code says, not what this plan says it says.
    - `D:/tmp/hermes-agent/agent/conversation_loop.py` around the pre-API-call steer drain (`grep -n "steer" D:/tmp/hermes-agent/agent/conversation_loop.py`) — the evidence correction 2 cites, including the in-code reason.
    - `D:/tmp/hermes-agent/agent/prompt_builder.py` around `STEER_MARKER_OPEN` / `STEER_CHANNEL_NOTE` (`grep -n "STEER_" D:/tmp/hermes-agent/agent/prompt_builder.py`) — the static marker correction 3 replaces.
    - `internal/agent/trust.go` in full — `wrapUntrustedToolOutput` and `toolOutputNonce`, the machinery correction 3 says Aura already owns, **and** the `html.EscapeString(norm.NFKC.String(...))` it applies, which is what makes correction 3's scrub-position clause non-obvious.
    - `internal/agent/llm_agent_dispatch.go` — `grep -n 'renderToolResultForPrompt\|RoleTool' internal/agent/llm_agent_dispatch.go`: the envelope is applied BEFORE the message enters history, not at render time. Correction 3's added clause depends on this fact; verify it.
    - `internal/config/config_agui_run.go` and `internal/config/config_knobs.go`'s `AURA_AGUI_RUN_DETACH` row (`grep -n "AURA_AGUI_RUN_DETACH" internal/config/*.go`) — the two contradicting sides D-11 decides.
  </read_first>
  <action>
Resolve the amendment number FIRST (see `<no_stale_inputs>`); do not write a number this plan
guessed. Append the new amendment as a sibling section to #132's, in prd.md's established
`## §<title> (Amendment #N, <date>)` shape, titled so a reader knows it corrects #132.

**Use the in-place strike-through convention #132 and #123 already use** — the superseded sentence
stays, struck through, with the replacement beside it. Do not delete the old text: the trail is the
point, and #132's own revision note says the correction cost nothing precisely because the trail
survived.

The amendment carries exactly these five corrections, each with its evidence citation and each
stating what it does NOT prove:

1. **#132 item (B) → automatic delivery.** "Drained and named in the run's terminal event" becomes:
   a steer still queued when the run ends is **delivered automatically as the next user turn**,
   preceded by a visible line saying that is what happened. Evidence: hermes hands the leftover back
   on `result["pending_steer"]` and three independent consumers deliver it (`turn_finalizer.py`,
   `gateway/run.py`, `cli.py`, plus the ACP path) — cite the file and the quoted in-code rationale,
   not a line number.
   **Resolve the 410 tension explicitly in the amendment text; do not leave it for the reader.**
   #132's ratified refusal ladder keeps `410 Gone` for a run that is ALREADY terminal at the moment
   the POST arrives — the operator is told plainly the message was not queued and re-sends it. The
   auto-delivery covers the different case the 410 structurally cannot: a steer accepted against a
   live run whose drain never came. Together they are what makes STEER-04's "never silently
   swallowed" true; either alone leaves a hole. Say so in the amendment.
2. **#132's single drain point → two.** At the end of a tool-call batch AND before every API call.
   Evidence: `conversation_loop.py`'s second drain, quoting the reason its own code states — a steer
   sent during an API call otherwise lands only after the NEXT tool batch, which may never come.
3. **#132's marker → per-run nonce + system-prompt note + lookalike scrub, and BOTH of their
   positions fixed.** #132 ratifies the tool-result append but leaves the marker's construction and
   its placement unspecified; hermes' is static and therefore forgeable by anything that can put text
   into a tool result. Aura reuses `toolOutputNonce()` from `internal/agent/trust.go` (D-07) — record
   explicitly that this is a SECOND CONSUMER of existing machinery, not a second nonce minter,
   because CLAUDE.md's inventory-before-invention rule is what caught it. Add the tool-output
   lookalike scrub (D-08) as the belt to the nonce's braces.
   **Two placement clauses, both measured on the live tree and both load-bearing:**
   (a) The untrusted-data envelope is applied **before** history, not at render: `llm_agent_dispatch.go`
   sets `run.Preview = renderToolResultForPrompt(...)` and appends THAT string as the `RoleTool`
   content, so every untrusted tool result already sits inside `<tool_output …>…</tool_output>`.
   The steer marker is therefore appended **after the closing `</tool_output>`**, never inside it —
   inside, the operator's redirect would sit in a block the system prompt teaches the model to
   distrust, which defeats D-07 while looking correct.
   (b) The scrub runs on the **raw** tool-result content **inside `renderToolResultForPrompt`, before
   either branch** — so it matches the LITERAL marker bytes `wrapUserSteer` produces, never an
   HTML-escaped form. Record the reason it is not redundant: `wrapUntrustedToolOutput` already
   escapes untrusted content, so the load-bearing case for the scrub is the TRUSTED passthrough
   branch (`trustedToolNames`, plus any tool carrying explicit `TrustTrusted` provenance), where the
   preview reaches history unescaped. Record also that the scrub must not touch `run.Result.Preview`
   itself, because the dedup/progress veto hashes the RAW preview by design.
4. **#132 items 10 and 12 → `AURA_AGUI_RUN_STEER` default `true`, composer contract in-phase.**
   Quote the operator ruling that a flag defaulted off is dark code, and tie it to CLAUDE.md's own
   DARK-CODE-IS-FORBIDDEN rule. Item 12's deferral of the composer flip to a later amendment is
   superseded: the composer contract lands inside Phase 52 (D-10), with the live cockpit E2E as its
   evidence rather than its precondition. **State the measured shape of that contract**, because it
   is not what #132 implied: the server's `ErrThreadBusy` 409 is NOT removed. The cockpit blocks the
   send client-side before any request is made (`ExternalStoreChat.tsx` passes `sendBlocked` from
   `live_run_id` into `Composer.tsx`'s `sendDisabled`), so the 409 was never what the operator hit.
   The contract is satisfied by the composer routing a submit to `POST /agent/runs/{runID}/steer`
   while a run is live, which means `/agent/run` is never POSTed during a live run and the busy 409
   is never reached — the thread lock keeps its guarantee untouched.
5. **#132 and D-05's non-text arm → a real queue, because today there is none.** D-05 says a
   non-text message during a live turn is "queued for the next turn". Measured: Telegram's
   `startTurn` calls `onBusy()` and returns — the media message is **dropped**, not queued. The
   amendment records that D-05 therefore requires a NEW per-chat pending-turn slot delivered when the
   live turn ends (52-06), and that "today's behaviour" is not an acceptable reading of D-05. State
   what it does NOT prove: the queue is in-process and single-replica by construction, exactly like
   the steer inbox.

**Also in this same commit**, because they assert the superseded behaviour and would otherwise
outlive it:

6. `.planning/REQUIREMENTS.md` STEER-04: replace "returned to the operator to re-send as a normal
   turn" with automatic delivery as the next user turn plus a visible line. Leave the "never
   silently swallowed" clause — it is the invariant, and it is now satisfied by two mechanisms.
7. `.planning/ROADMAP.md` Phase 52 SC#3: replace "returns the message to the operator to send
   normally" with the auto-delivery formulation. SC#5 (channel parity) is unaffected and stays
   verbatim, per D-01.
8. `.planning/ROADMAP.md` Phase 52 `Depends on:`: it still reads "Phase 51", which the document's own
   Progress section contradicts (execution order `45 → 45.1 → 46 → 52 → 50 → 49 → 51 → 54`, and the
   phase block's REVISED note says "promoted ahead of Phase 51"). Replace it with what is true, and
   say in one clause why the original reasoning inverted — the substrate does not exist, so building
   51 first is what would create the second run model.
9. `prd.md` amendment #133's "Phase 47" pointer: Phase 47 was deleted on 2026-08-25 and its three
   approval-resume defects were folded into Phase 52. Add a dated line INSIDE the #133 block
   re-pointing them to Phase 52 and to the new `RESUME-01` id, citing the folded todo. Do not rewrite
   #133's findings — only its destination.
10. **Mint `RESUME-01`** in `.planning/REQUIREMENTS.md`, in the same bullet format the STEER ids use,
   worded as one testable outcome covering all three folded defects — for example: *the approval
   resume path refuses an accept carrying no answer, refuses a decision the pause's policy does not
   permit, and expires a pending approval as an expiry rather than as a yes — without weakening the
   `WHERE resumed_at IS NULL` conditional update that IS the idempotency key.* Add its traceability
   row (`| RESUME-01 | Phase 52 | Pending |`) beside the STEER rows, and add `RESUME-01` to ROADMAP
   Phase 52's `**Requirements**:` line. Rationale to record in the commit body: the folded work had
   no id, so a verifier hunting its evidence was being sent to a STEER plan that does not implement
   it.

**Do not add the AURA_* env rows in this task.** They land in Task 2 alongside the loader they
describe, so a row can never be catalogued for a knob that does not load.

Commit alone: `docs(52): correct amendment #132 against hermes and the live tree before any steering code`,
body naming the five corrections, the three documents and the minted RESUME-01. This commit MUST be
the first commit of the phase — nothing from plans 52-02..52-08 may precede it.
  </action>
  <acceptance_criteria>
    - `git log --oneline -1 -- prd.md` is the phase's amendment commit, and `git log --oneline` shows NO commit touching `internal/steer/`, `internal/agent/llm_agent_steer.go`, `internal/agui/server_run_steer.go`, `internal/runner/runner_steer.go`, `internal/channels/telegram/bot_dispatch_steer.go` or `web/src/chat/steerRun.ts` before it.
    - `grep -c "Amendment #" prd.md` increased by exactly 1 relative to `git show HEAD~1:prd.md`, and the new number equals the previous maximum + 1.
    - `grep -n "delivered automatically as the next user turn" prd.md` returns at least one line, and the same phrase (or its exact agreed wording) appears in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
    - `grep -rn "returned to the operator to re-send\|returns the message to the operator to send normally" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns nothing.
    - `awk '/^### Phase 52/,/^### Phase 5[3-9]/' .planning/ROADMAP.md | grep -c "Depends on\*\*: Phase 51"` returns 0.
    - `grep -n "410" prd.md | grep -i steer` shows the terminal-run refusal and the auto-delivery are both described and their boundary is stated.
    - `grep -n "toolOutputNonce" prd.md` returns a line — the amendment names the existing minter it reuses.
    - `grep -c "</tool_output>" prd.md` is at least 1 and the surrounding text states the steer marker sits AFTER the close — correction 3(a) is written down, not implied.
    - The #133 block is re-pointed, checked by a range-scoped extraction rather than a chained grep: `awk '/Amendment #133/,/^## /' prd.md | grep -q "Phase 52"` exits 0, and `awk '/Amendment #133/,/^## /' prd.md | grep -q "RESUME-01"` exits 0.
    - `grep -c "RESUME-01" .planning/REQUIREMENTS.md` is at least 2 (the bullet and the traceability row) — measured 2026-08-25 it is **0**, so this is work, not a tautology.
    - The ROADMAP assertion is anchored on the `**Requirements**:` LINE, not on the phase block: `awk '/^### Phase 52/,/^### Phase 5[3-9]/' .planning/ROADMAP.md | grep '^\*\*Requirements\*\*' | grep -q 'RESUME-01'` exits 0. Measured 2026-08-25 the block-wide form ALREADY passes — `RESUME-01` appears twice in the plan list this planner wrote — so it proves nothing. The Requirements line today reads `STEER-01, STEER-02, STEER-03, STEER-04, STEER-05, STEER-06` and this criterion fails until action item 10 lands.
    - The amendment contains an explicit "what this does NOT prove" paragraph, matching #132's and #133's own convention: `grep -c "does NOT prove\|non prova" prd.md` increased.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; N=$(grep -o "Amendment #[0-9]*" prd.md | grep -o "[0-9]*" | sort -n | tail -1); P=$(git show HEAD~1:prd.md | grep -o "Amendment #[0-9]*" | grep -o "[0-9]*" | sort -n | tail -1); [ "$N" = "$((P+1))" ]; grep -q "delivered automatically as the next user turn" prd.md; ! grep -rq "returned to the operator to re-send" .planning/REQUIREMENTS.md; awk "/Amendment #133/,/^## /" prd.md | grep -q "Phase 52"; awk "/Amendment #133/,/^## /" prd.md | grep -q "RESUME-01"; [ "$(awk "/^### Phase 52/,/^### Phase 5[3-9]/" .planning/ROADMAP.md | grep -c "Depends on\*\*: Phase 51")" = 0 ]; awk "/^### Phase 52/,/^### Phase 5[3-9]/" .planning/ROADMAP.md | grep "^\*\*Requirements\*\*" | grep -q "RESUME-01"; [ "$(grep -c RESUME-01 .planning/REQUIREMENTS.md)" -ge 2 ]; echo OK'</automated>
  </verify>
  <done>The contract every later plan implements says what the reference implementation and the live tree actually do, the three planning documents agree with it, the folded work has an id, and git proves the correction preceded the code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The knob surface — three steer knobs, one askuser TTL knob, and the AURA_AGUI_RUN_DETACH reconcile</name>
  <files>internal/config/config_agui_steer.go, internal/config/config_agui_steer_test.go, internal/config/config_askuser.go, internal/config/config_askuser_test.go, internal/config/config.go, internal/config/config_knobs.go, prd.md</files>
  <reversibility rating="costly">`AURA_AGUI_RUN_STEER` default `true` is D-12; the value is an operator ruling, and flipping it later needs its own amendment line.</reversibility>
  <read_first>
    - `internal/config/config_agui_run.go` in full — it is the structural twin: the sub-struct, the `envutil.BoolDefault`/`IntDefault` non-fatal fallbacks, and its header comment explaining WHY the file was split out of `config.go` (the 600-LOC cap) and why every field is non-fatal.
    - `internal/config/config_agui_test.go` in full — `TestAGUIConfigDefaultsAndOverrides` proves the exact three-phase pattern to clone: default → explicit override wins → malformed value falls back to the default without a fatal. Read its `AURA_AGUI_RUN_DETACH` assertions verbatim; the messages state the rationale, and the new tests should state theirs the same way.
    - `internal/config/config.go` — locate `AGUIRun` with `grep -n "AGUIRun" internal/config/config.go`: both the struct field with its doc comment and the `loadBase()` call site. Two lines, one shape, copy it.
    - `internal/config/config_knobs.go` — locate the AGUI block with `grep -n "AURA_AGUI_RUN" internal/config/config_knobs.go`. Read the surrounding rows for the `{Name:, Kind:, Default:}` shape, AND read the file's header comment for what the catalogue is FOR (the per-profile re-parse pass that flags a malformed value under a strict tier) — that is why a mis-stated default is not cosmetic.
    - `internal/envutil` — the `BoolDefault` / `IntDefault` contract, specifically what they do with a malformed value. Do not assume; read it.
    - `internal/askuser/store.go` — `grep -n "CleanupResumedOlderThan\|AutoResolveForConversation" internal/askuser/store.go` and read both: they are the existing cutoff-sweep precedent the TTL knob will feed in plan 52-03, and they tell you the unit the store already thinks in.
    - `.planning/phases/52-mid-turn-steering/52-CONTEXT.md` D-11 and D-12, and its "Folded Todos" §3 (pending approvals never expire).
    - `.planning/phases/52-mid-turn-steering/52-PATTERNS.md` §`internal/config/config_agui_steer.go` and §`internal/config/config_knobs.go`.
    - `.claude/skills/golang-testing/SKILL.md` — the table-driven and `t.Setenv` discipline these tests use.
  </read_first>
  <behavior>
    - `config.Load()` with no steer env set: `AGUISteer.Enabled == true`, `AGUISteer.Max == 8`, `AGUISteer.MaxBytes == 16384`.
    - `AURA_AGUI_RUN_STEER=false` → `AGUISteer.Enabled == false` (the explicit rollback wins).
    - `AURA_AGUI_RUN_STEER=not-a-bool` → `AGUISteer.Enabled == true` (malformed falls back to the default; boot does not fail).
    - `AURA_AGUI_RUN_STEER_MAX=3` → `3`; `AURA_AGUI_RUN_STEER_MAX=banana` → `8`.
    - `AURA_AGUI_RUN_STEER_MAX_BYTES=1024` → `1024`; malformed → `16384`.
    - `AURA_ASKUSER_PAUSE_TTL_SEC` unset → the chosen default; malformed → the same default; explicit → the explicit value.
    - Every `AURA_AGUI_RUN_STEER*` and `AURA_ASKUSER_PAUSE_TTL_SEC` name appears exactly once in `config_knobs.go`, with a `Default` string equal to the value the loader applies.
    - The catalogued default for `AURA_AGUI_RUN_DETACH` equals the loader's default.
  </behavior>
  <action>
Tests first, red.

Create `internal/config/config_agui_steer.go` mirroring `config_agui_run.go` field for field:
type `AGUISteerConfig` with `Enabled bool`, `Max int`, `MaxBytes int`, each carrying an end-of-line
comment naming its env var and its default; func `loadAGUISteerConfig()` returning it through
`envutil.BoolDefault("AURA_AGUI_RUN_STEER", true)`, `envutil.IntDefault("AURA_AGUI_RUN_STEER_MAX", 8)`,
`envutil.IntDefault("AURA_AGUI_RUN_STEER_MAX_BYTES", 16384)`.

**The two int defaults are 8 and 16384 because amendment #132 item 10 ratifies exactly those
numbers** — and #132's own closing paragraph says they are untested defaults carried from the study.
Put that sentence in the file's header comment: a reader three phases from now must not mistake a
carried-forward number for a measured one. The only value D-12 changes is `Enabled`, from `false` to
`true`.

Create `internal/config/config_askuser.go` in the same shape: `AskUserConfig` with `PauseTTLSec int`,
`loadAskUserConfig()` reading `envutil.IntDefault("AURA_ASKUSER_PAUSE_TTL_SEC", <default>)`. Choose
the default deliberately and justify it in the comment — a pending approval is a human-facing
question, so the TTL must be long enough that a person who steps away for a working day does not
find their approval silently expired. `AURA_MCP_ELICITATION_TIMEOUT_SEC` (read its ACTUAL current
value out of internal/config at execution time; do not trust any number written in this plan) is NOT
the precedent: that bounds an in-flight protocol wait, this bounds an async cross-turn ledger entry
with nobody blocked on it. Say which one you picked and why.

Wire both into `internal/config/config.go`: one struct field each with a doc comment (copy `AGUIRun`'s
comment shape — it names the amendment that introduced the bundle), one `loadBase()` line each.

`internal/config/config_knobs.go`, in one edit:
- Correct the `AURA_AGUI_RUN_DETACH` row's `Default` from `"false"` to `"true"` (D-11). The code side
  is right and has a passing live campaign recorded in its own source comment (`config_agui_run.go`'s
  own comment and `config_agui_test.go`'s passing assertion); the catalogue row is the only place in
  the tree still saying otherwise. Add a short trailing comment naming D-11 so the next reader knows
  this was decided, not drifted back.
- Add four rows beside the existing `AURA_AGUI_RUN_*` block: `AURA_AGUI_RUN_STEER` (`KindBool`,
  `"true"`), `AURA_AGUI_RUN_STEER_MAX` (`KindInt`, `"8"`), `AURA_AGUI_RUN_STEER_MAX_BYTES`
  (`KindInt`, `"16384"`), `AURA_ASKUSER_PAUSE_TTL_SEC` (`KindInt`, the default you chose).

Add the same four rows to prd.md's env catalogue (the §Caps & Limits index CLAUDE.md names), in that
file's existing row format. The env-catalogue gap found in 45.1-07 is a standing STATE.md concern —
do not reproduce it here.

Tests: clone `config_agui_test.go`'s triple into `config_agui_steer_test.go` (new sibling file, per
that file's own split precedent) covering each of the four new knobs, plus a catalogue-consistency
test that fails if a knob is catalogued with a `Default` string that the loader does not actually
apply. Write that consistency test so it covers the four NEW knobs and `AURA_AGUI_RUN_DETACH` — it is
the tripwire that makes a second D-11-shaped drift impossible, and it is worth more than the row it
is fixing.

Commit: `feat(52): add the steer + pause-TTL knob surface and reconcile AURA_AGUI_RUN_DETACH`.
  </action>
  <acceptance_criteria>
    - `go test ./internal/config/ -run 'TestAGUISteerConfig|TestAskUserConfig|TestEveryNewKnobIsCatalogued|TestAGUIConfig' -v` passes.
    - `go test -race ./internal/config/` passes; `go vet ./...` and `go build ./...` exit 0.
    - `grep -c 'AURA_AGUI_RUN_DETACH' internal/config/config_knobs.go` is 1, and that row's `Default` is `"true"`.
    - `grep -rn 'Default: "false"' internal/config/config_knobs.go | grep AURA_AGUI_RUN_DETACH` returns nothing.
    - Each of `AURA_AGUI_RUN_STEER`, `AURA_AGUI_RUN_STEER_MAX`, `AURA_AGUI_RUN_STEER_MAX_BYTES`, `AURA_ASKUSER_PAUSE_TTL_SEC` appears in BOTH `internal/config/config_knobs.go` and `prd.md`: `for k in AURA_AGUI_RUN_STEER AURA_AGUI_RUN_STEER_MAX AURA_AGUI_RUN_STEER_MAX_BYTES AURA_ASKUSER_PAUSE_TTL_SEC; do grep -q "$k" internal/config/config_knobs.go && grep -q "$k" prd.md || exit 1; done`
    - `grep -n 'loadAGUISteerConfig\|loadAskUserConfig' internal/config/config.go` returns exactly two lines.
    - `wc -l internal/config/config.go internal/config/config_knobs.go internal/config/config_agui_steer.go internal/config/config_askuser.go` — every file ≤ 600.
    - The `AURA_AGUI_RUN_DETACH` reconcile is asserted on the SAME LINE as the knob, not on the next one: `grep AURA_AGUI_RUN_DETACH internal/config/config_knobs.go | grep -q 'Default: "true"'`. Measured 2026-08-25 that row reads `Default: "false"` while `config_agui_run.go` loads `envutil.BoolDefault("AURA_AGUI_RUN_DETACH", true)` — the contradiction D-11 resolves. A `grep -A1` form would also be satisfied by the word `true` drifting in from the following knob's row, which is not what is being proven.
    - A test exists whose failure message names D-11 by ID, so a future revert reads as a decision reversal rather than a typo.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/config/; for k in AURA_AGUI_RUN_STEER AURA_AGUI_RUN_STEER_MAX AURA_AGUI_RUN_STEER_MAX_BYTES AURA_ASKUSER_PAUSE_TTL_SEC; do grep -q "$k" internal/config/config_knobs.go && grep -q "$k" prd.md; done; [ "$(grep -c AURA_AGUI_RUN_DETACH internal/config/config_knobs.go)" = 1 ]; grep AURA_AGUI_RUN_DETACH internal/config/config_knobs.go | grep -q "Default: \"true\""; go vet ./...; echo OK'</automated>
  </verify>
  <done>Steering has a knob surface that ships on, an expiry TTL exists to be consumed, and the tree no longer states one env default two different ways.</done>
</task>

</tasks>

<threat_model>
**Tier: ASVS L1 — block on `high`.** Every threat rated HIGH below must be mitigated before the plan
closes; `accept` is available only for the non-high rows and each carries its stated residual.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator environment → config loader | An env value the operator sets (or mistypes) decides whether steering is reachable and how much text it accepts. |
| Documentation → implementation | Seven later plans read the amendment as their contract; a wrong sentence here becomes wrong code seven times. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | **Tampering (HIGH — the threat this plan exists to close)** | A ratified contract that contradicts the reference implementation it claims parity with, or the live tree it will be applied to | mitigate | The five corrections land BEFORE any implementation commit, each citing the hermes file (or the Aura symbol) and its in-code rationale rather than this plan's paraphrase. Enforced by the git-order acceptance criterion, not by intent. |
| T-52-02 | Denial of Service | `AURA_AGUI_RUN_STEER_MAX` / `_MAX_BYTES` set absurdly high, letting one operator queue unbounded text into a live run's memory | mitigate | Both are `IntDefault` non-fatal ints consumed by the inbox's cap check (plan 52-02). The caps are read from config, never hardcoded in `internal/steer`, so an operator can lower but the default is bounded. Accepted residual: an operator can raise their own ceiling — this is a single-operator appliance, and the boundary is the operator's own environment. |
| T-52-03 | Elevation of Privilege | A malformed env value failing OPEN (steering enabled when the operator meant it off, or a cap parsed as 0 = unlimited) | mitigate | `envutil.*Default` falls back to the DEFAULT on a malformed value, never to zero, and the malformed-falls-back test asserts it per knob. The `config_knobs.go` catalogue additionally feeds the per-profile strict re-parse pass that surfaces a malformed value instead of silently defaulting. |
| T-52-04 | Repudiation | A future revert of D-11 or D-12 reading as a typo fix rather than a decision reversal | mitigate | The catalogue row carries a D-11 comment and the test failure message names the decision ID; the amendment records D-12's operator ruling verbatim. |
| T-52-05 | Repudiation | The folded #133 work carrying no requirement id, so its evidence is looked for under a STEER id that does not implement it | mitigate | `RESUME-01` is minted here with its traceability row and its ROADMAP entry; 52-03 carries it in frontmatter and 52-08 ticks it. |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | **No package installs in this phase.** No `go get`, no new module requirement, no `npm install` (the web plan 52-07 adds no dependency — it uses `fetch`, React and i18next, all already in `web/package.json`): every symbol used is stdlib or already declared. If an executor finds itself about to add a dependency, that is a plan deviation and stops for the operator — RESEARCH.md does not exist for this phase, so no Package Legitimacy Audit table exists to clear one. |
</threat_model>

<verification>
- `go test -race ./internal/config/` (~5s).
- `go vet ./...`, `go build ./...`.
- `git log --oneline --name-only -5` — the amendment commit precedes every code commit of the phase.
- File-size gate: `wc -l` on every touched Go file, all ≤ 600.
</verification>

<success_criteria>
- The corrected contract is in prd.md, committed before any steering code, with evidence citations
  and an explicit "what this does not prove" paragraph.
- REQUIREMENTS.md, ROADMAP.md and prd.md agree on what happens to a leftover steer, on where the
  steer marker sits, on what the composer contract actually is, and on the fact that D-05 needs a
  queue that does not exist yet.
- The folded #133 work has a requirement id and a traceable home.
- `config.Load()` returns steering enabled by default and a pause TTL that later plans can consume.
- No env default is stated two different ways anywhere in the tree, and a test now enforces that.
</success_criteria>

<output>
Create `.planning/phases/52-mid-turn-steering/52-01-SUMMARY.md` when done. Record the amendment
number actually assigned, the `AURA_ASKUSER_PAUSE_TTL_SEC` default chosen and why, the exact wording
landed for the auto-delivery clause (plans 52-05 and 52-08 assert against that wording), and the
`RESUME-01` text as landed.
</output>
