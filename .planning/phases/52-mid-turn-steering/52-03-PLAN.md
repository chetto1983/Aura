---
phase: 52-mid-turn-steering
plan: 03
type: tdd
wave: 2
depends_on: ["52-01"]
files_modified:
  - internal/agui/server_project.go
  - internal/agui/server_project_test.go
  - internal/agui/server_run.go
  - internal/agui/server_run_detach.go
  - internal/askuser/expire.go
  - internal/askuser/expire_test.go
  - internal/askuser/store.go
  - internal/db/queries/paused_states.sql
  - internal/runner/runner_resume.go
  - internal/runner/runner_mint_approval.go
  - cmd/aura/serve.go
autonomous: true
requirements: [RESUME-01]

must_haves:
  truths:
    - "A resume carrying an ACCEPT with an empty answer is refused before it reaches the store; it can no longer resume the model with an empty tool answer."
    - "A resume whose decision the pause's policy does not permit is refused; a crafted POST cannot approve a pause that was minted as decline-only."
    - "Every new validation rejects BEFORE MarkResumedTx is reached, so an invalid resume never consumes the conditional-update slot a legitimate later retry needs."
    - "The idempotency invariant is intact and unweakened: the WHERE resumed_at IS NULL conditional update in internal/db/queries/paused_states.sql is still the idempotency key, RowsAffected==0 still yields ErrPauseNotFound for an unknown OR already-resumed token, and CommitResumeBatch still claims every pause under sorted-token ordering in ONE cross-store transaction."
    - "A pending approval older than the configured TTL is expired rather than left pending forever, and the expiry is visible — it never reads as an approval."
    - "An expired approval resolves through the SAME conditional-update front door a real resume uses, so an expiry and a concurrent genuine resume cannot both win."
    - "The expiry reaper is goroutine-clean: it is lazily started, joined on shutdown, and goleak-clean under -race."
    - "No validation was added as a second path around the transaction's front door; every new check lives inside or before it."
    - "The per-pause decision policy is recorded at MINT time in internal/runner/runner_mint_approval.go, because a policy that is not recorded when the pause is created cannot be enforced when it is resumed."
  artifacts:
    - path: "internal/askuser/expire.go"
      provides: "The pending-approval TTL sweep, as a sibling file rather than an append to store.go"
      contains: "ExpirePending"
    - path: "internal/agui/server_project.go"
      provides: "Resume-entry validation ahead of the resume mapping"
      contains: "validateResumeEntries"
    - path: "internal/runner/runner_mint_approval.go"
      provides: "The mint-time record of which decisions a given pause permits"
      contains: "resume_context"
  key_links:
    - from: "internal/agui/server_project.go validateResumeEntries"
      to: "internal/agui/server_run.go / server_run_detach.go SubmitAnswers call sites"
      via: "validation runs before the mapping, so an invalid entry never reaches the store"
      pattern: "validateResumeEntries"
    - from: "internal/runner/runner_mint_approval.go"
      to: "internal/agui/server_project.go validateResumeEntries"
      via: "the permitted-decision set written into resume_context at mint is the set the validator reads at resume"
      pattern: "resume_context"
    - from: "internal/askuser/expire.go"
      to: "internal/askuser/store.go MarkResumedTx"
      via: "expiry goes through the same conditional update, never a bespoke UPDATE"
      pattern: "MarkResumed"
  prohibitions:
    - "Must NOT let an expiry read as an approval. A pending decision that times out into 'yes' turns a safety gate into a delay, and the mutating tool behind it runs with nobody having said so — the expiry must resolve to a refusal and be labelled as an expiry, never as a human decision."
    - "Must NOT add a second validation path around the resume transaction. The WHERE resumed_at IS NULL conditional update IS the idempotency key; a check that runs outside it, or a repair that writes around it, silently trades a proven invariant for a convenient one."
---

<!-- planner-discipline-allow: UPDATE -->
<!-- The action forbids a bespoke SQL UPDATE and the criterion greps the expiry file for a count of 0. The literal must appear in the action because that is what is being forbidden. -->

<objective>
**The folded todo, now with an id of its own.** Amendment #133's three approval-resume defects,
scheduled inside the deleted Phase 47 and rehomed here because they are resume-path security gaps
living in the same mid-turn input path this phase opens: no per-tool decision policy, an empty answer
resuming silently, and pending approvals that never expire.

Purpose: LibreChat's threat model on this path is that **the human's POST is untrusted** — it returns
403 for a decision the policy did not permit and 400 for an empty answer. Aura covers the
model-facing half of that threat and not this one. The third defect is simpler and worse: nothing in
`internal/gateway` expires a pending approval, so a question nobody answers stays open forever.

These share a package with steering but not a code path: an `ask_user`-paused run is terminal and not
steerable (#132 item 7), so the two surfaces never overlap at runtime. This plan runs in wave 2
beside 52-02, touching a disjoint file set.

**Requirement tagging note.** This plan is tagged `RESUME-01`, minted by 52-01, and NOT `STEER-04`.
It implements nothing about leftover-steer delivery; STEER-04 is worked by 52-05 (auto-delivery + the
terminal 410) and validated by 52-08. The previous tagging sent a downstream verifier here looking
for STEER-04 evidence that does not exist in this plan.
</objective>

<no_stale_inputs>
| Fact | Obtain it this way — never from this plan |
|---|---|
| The next migration slot, **if and only if** a schema change proves necessary | `ls internal/db/migrations/ \| tail -1` then +1. Do NOT hardcode, do NOT infer from a slice number, do NOT copy one from CLAUDE.md or the PRD — the directory is the only source of truth, and CLAUDE.md says so in imperative terms. |
| Whether a migration is needed at all | Read `internal/db/migrations/*paused_states*` first. `created_at` and `resumed_at` already exist; prefer computing the TTL from what is there over adding a column. Add one only if the sweep genuinely cannot be expressed without it, and say why in the SUMMARY. |
| The `AURA_ASKUSER_PAUSE_TTL_SEC` default | `grep -rn AURA_ASKUSER_PAUSE_TTL_SEC internal/config/` — chosen and justified by 52-01, not by this plan |
| The exact action constants and their validation set | `grep -n 'Action' internal/askuser/store.go` |
| The real query file for the paused-state statements | `ls internal/db/queries/` and `grep -rn "resumed_at IS NULL" internal/db/queries/`. It is `paused_states.sql`. An earlier draft of this plan named `askuser.sql`, which does not exist; if any citation anywhere still says that, it is wrong. |
| Where any symbol lives | `grep -n '<symbol>' <file>` — symbols only |
| Every file's headroom | `wc -l` before editing |
</no_stale_inputs>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-mid-turn-steering/52-CONTEXT.md
@.planning/phases/52-mid-turn-steering/52-PATTERNS.md
@.planning/todos/pending/approval-resume-defects.md
</context>

<reversibility>
| Decision | Rating | Handling |
|---|---|---|
| Where the per-pause decision policy is stored | **costly** — if it lands in `resume_context` jsonb it is a shape every future pause writer must honour | Prefer the existing `resume_context` jsonb over a new column: it is already the pause's own extension point, and a column is a migration plus a sqlc regeneration for a field only this path reads. Record the choice and its rationale in the SUMMARY. |
| Expiry resolves to a refusal, not an approval | **costly, with a one-way consequence** — an expiry that ever resolved to accept would have run mutating tools nobody authorised, and that cannot be undone | Not a decision to re-open: it is the plan's authored prohibition, so the door is closed before the executor reaches it. No checkpoint, because there is no defensible alternative to weigh and `human_verify_mode` is end-of-phase. |
| Whether a migration is needed | reversible | Decided by reading the existing schema, at execution time. |
</reversibility>

## Artifacts this phase produces

Created by THIS plan: `internal/askuser/expire.go` with `(*Store).ExpirePendingOlderThan`, the reaper
lifecycle, `ActionExpired` and `ErrPauseExpired`; `internal/agui`: `validateResumeEntries`,
`ErrResumeEmptyAnswer`, `ErrResumeDecisionNotAllowed`; the per-pause permitted-decision set written
into the existing `resume_context` jsonb by `internal/runner/runner_mint_approval.go`. Possibly one
new sqlc query in `internal/db/queries/paused_states.sql`. New tests:
`TestResumeRejectsEmptyAcceptAnswer`, `TestResumeRejectsDisallowedDecision`,
`TestValidationRejectsBeforeMarkResumed`, `TestIdempotencyInvariantUnchanged`,
`TestExpirePendingOlderThan`, `TestExpiryIsNotAnApproval`, `TestExpiryRacesRealResumeSafely`,
`TestReaperIsGoleakClean`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refuse an empty accept and a decision the pause's policy does not permit — before the transaction's front door</name>
  <files>internal/agui/server_project.go, internal/agui/server_project_test.go, internal/agui/server_run.go, internal/agui/server_run_detach.go, internal/runner/runner_mint_approval.go, internal/runner/runner_resume.go</files>
  <read_first>
    - `internal/agui/server_project.go` in full — `resumeAnswers` and `payloadString`. Both defects live in this function pair: `payloadString(nil)` returns `""` and `resumeAnswers` maps `resolved→ActionAccept` unconditionally.
    - `internal/agui/server_run.go` and `internal/agui/server_run_detach.go` — BOTH `SubmitAnswers` call sites. Validation must cover both or the detached path keeps the defect. Note while you are here that both files carry a `TryLockThread` 409 `ErrThreadBusy` fast-path; **this plan does not touch that branch** and neither does any steer plan (see 52-04's stated non-goal).
    - `internal/runner/runner_resume.go` and `internal/runner/resume_committer.go` — `SubmitAnswers`, `CommitResumeBatch`, and the sorted-token deadlock-free ordering. Read the whole claim path before adding anything to it.
    - `internal/askuser/store.go` — `MarkResumedTx`, `MarkResumedBatchTx`, the action-validation switch, `InsertParams` (including `ResumeContext`), and `Pending`. The `n == 0` / `RowsAffected==0` check IS the idempotency key; read the comment that says so.
    - `internal/db/queries/paused_states.sql` and the generated `internal/db/sqlc` for `MarkPausedStateResumed` — specifically its `WHERE resumed_at IS NULL` clause. **This is the real file; `internal/db/queries/askuser.sql` does not exist.**
    - `internal/runner/runner_mint_approval.go` — where an approval pause is minted, i.e. the ONLY place that can record which decisions a given pause permits. If the policy is not recorded at mint time, it cannot be enforced at resume time; the mint side is implemented HERE, in this task, and the file is declared in `files_modified` for that reason.
    - `.planning/todos/pending/approval-resume-defects.md` in full, especially the "Constraint on any fix" paragraph.
    - `prd.md` amendment #133 — the LibreChat quotes and the 403/400 reference behaviour — plus the dated re-pointing line 52-01 added to it.
    - `.claude/skills/golang-error-handling/SKILL.md`, `.claude/skills/golang-database/SKILL.md`.
  </read_first>
  <behavior>
    - A resume entry with status resolved and a nil payload → refused; the store is never called.
    - A resume entry with status resolved and a whitespace-only payload → refused.
    - A resume entry with status cancelled and no payload → ACCEPTED (a cancel needs no answer; only an accept does).
    - A resume entry whose decision is not in the pause's permitted set → refused, with an error distinct from the empty-answer one.
    - A pause minted with no explicit policy permits the decisions it permits today — the change is additive and no existing pause becomes unresolvable.
    - A pause minted WITH a restricted policy carries that policy in `resume_context`, and the validator reads it from there.
    - A batch containing one invalid entry refuses the batch and leaves EVERY token in it still pending, so a retry with a corrected entry still works.
    - After a refusal, a subsequent valid resume of the same token succeeds — the refused attempt consumed no conditional-update slot.
    - The unchanged invariants still hold: resuming an unknown token yields `ErrPauseNotFound`; resuming an already-resumed token yields `ErrPauseNotFound`; a batch still commits in one cross-store transaction under sorted-token ordering.
  </behavior>
  <action>
Tests first, red. Write `TestIdempotencyInvariantUnchanged` FIRST and keep it green throughout — it
is the regression net for the constraint the todo carries.

Add `validateResumeEntries` in `server_project.go` (the file is a pure-mapping-helpers file and this
is a pure check; `wc -l` it first — it had ample headroom when this plan was written, but measure).
It runs BEFORE `resumeAnswers` at both `SubmitAnswers` call sites and returns typed errors the
handlers map to 400 (empty accept) and 403 (disallowed decision) — matching LibreChat's statuses,
which the amendment records as the reference behaviour.

**Do not weaken `payloadString`.** Its nil→`""` behaviour is correct for a cancel; the defect is that
an ACCEPT was allowed to carry it. Fix the caller's contract, not the helper.

For the per-pause policy, prefer recording the permitted decisions in the pause's existing
`resume_context` jsonb at mint time over adding a column — it is already the pause's extension point,
and a column costs a migration plus a sqlc regeneration for a field only this path reads. The write
side is `internal/runner/runner_mint_approval.go`: a policy that is not recorded when the pause is
created cannot be enforced when it is resumed, so the mint and the validation land in the SAME commit
or the feature is decorative. If you conclude a column is genuinely required, resolve the migration
number by `ls`-ing the directory (see `<no_stale_inputs>`) and say in the SUMMARY why jsonb was
insufficient.

Absent policy must mean "today's permitted set", so no pause already in the table becomes
unresolvable when this ships.

**The placement rule is the whole point:** every new check rejects before `MarkResumedTx` is reached.
A check that runs after it has already spent the `WHERE resumed_at IS NULL` slot, and a legitimate
retry then gets `ErrPauseNotFound` for a pause that was never really resumed. Assert that ordering
directly, with a test that refuses once and then resumes successfully.

Commit: `fix(52): refuse an empty accept and a policy-disallowed resume decision`.
  </action>
  <acceptance_criteria>
    - `go test -race ./internal/agui/ ./internal/runner/ ./internal/askuser/ -run 'TestResumeRejects|TestValidationRejectsBeforeMarkResumed|TestIdempotencyInvariantUnchanged' -v` passes.
    - `grep -c 'validateResumeEntries' internal/agui/server_run.go internal/agui/server_run_detach.go` shows it called on BOTH paths.
    - `git diff internal/askuser/store.go` shows no change to the `RowsAffected==0` / `ErrPauseNotFound` logic, and no change to `CommitResumeBatch`'s ordering.
    - The conditional update is still there, in the file that actually holds it: `grep -q 'resumed_at IS NULL' internal/db/queries/paused_states.sql` exits 0. (`internal/db/queries/askuser.sql` must not be created — `[ ! -f internal/db/queries/askuser.sql ]`.)
    - `grep -n 'resume_context' internal/runner/runner_mint_approval.go` shows the permitted-decision set is written at mint.
    - A test proves a refused resume leaves the token resumable.
    - A test proves a batch with one bad entry leaves all its tokens pending.
    - A test proves a pause minted with no explicit policy is still resolvable with today's decision set.
    - `go test -tags=db_integration -race ./internal/askuser/ ./internal/runner/` passes against the disposable DB (see `<verification>` — never point it at the live `aura` database, and never run it as the superuser `aura` role).
    - `go vet ./...`, `go build ./...` exit 0; `wc -l` ≤ 600 on every touched file.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/agui/ ./internal/runner/ ./internal/askuser/; grep -q validateResumeEntries internal/agui/server_run.go; grep -q validateResumeEntries internal/agui/server_run_detach.go; grep -q "resumed_at IS NULL" internal/db/queries/paused_states.sql; [ ! -f internal/db/queries/askuser.sql ]; grep -q resume_context internal/runner/runner_mint_approval.go; go vet ./...; echo OK'</automated>
  </verify>
  <done>A crafted POST can no longer approve what the policy restricted, an accept with nothing in it is refused instead of resuming the model with silence, and the idempotency key is exactly as strong as it was.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expire a pending approval on a TTL, through the same front door, resolving to a refusal</name>
  <files>internal/askuser/expire.go, internal/askuser/expire_test.go, internal/askuser/store.go, internal/db/queries/paused_states.sql, cmd/aura/serve.go</files>
  <reversibility rating="costly">The CONSEQUENCE of getting this wrong is one-way — an expiry that resolved to accept would have run mutating tools nobody authorised, and that cannot be undone. The DECISION, however, is not an open door the executor walks through: it is closed in advance by this plan's authored prohibition, so there is nothing for a checkpoint to weigh. Rated costly rather than one-way for exactly that reason.</reversibility>
  <read_first>
    - `internal/askuser/store.go` — `CleanupResumedOlderThan` and `AutoResolveForConversation` in full. These are the existing cutoff-sweep and auto-resolve precedents; the TTL sweep is their sibling, not a new idiom. Note in particular what `AutoResolveForConversation` writes as its answer and how it labels it.
    - `internal/agui/runregistry.go` — the lazy-start reaper (`startReaperLocked`, the `stop`/`reaperDone` channel pair, `evictExpired`) and how `Close` joins it. This is the proven goleak-clean ticker shape in this codebase; copy it rather than inventing a goroutine lifecycle. **Read it, do not edit it** — it is not in this plan's `files_modified`.
    - `internal/db/migrations/*paused_states*` — the actual columns. `created_at` and `resumed_at` exist; decide from the real schema whether a migration is needed at all.
    - `internal/db/queries/paused_states.sql` — the existing statement set, so a new sweep query (if one is needed) lands beside its siblings in the right file.
    - `internal/config/config_askuser.go` (created by 52-01) — the TTL knob and its justified default.
    - `cmd/aura/serve.go` — where long-lived background workers are started and drained on shutdown (`grep -n 'drainShutdown\|Close()' cmd/aura/serve.go`), so the reaper is joined like its peers rather than leaked. `wc -l` it first; it was at 483 when this plan was written and CLAUDE.md's split-on-touch rule applies.
    - `prd.md` amendment #133's third defect and its LibreChat reference (`APPROVAL_EXPIRED_ERROR`, `expireApproval()`, checkpoint pruning).
    - `.claude/skills/golang-concurrency/SKILL.md` and `.claude/skills/golang-testing/SKILL.md` — goleak is mandatory here.
  </read_first>
  <behavior>
    - A pending pause older than the TTL is resolved by the sweep; a pause younger than the TTL is untouched.
    - The resolution is recorded as an EXPIRY, distinguishable from both a human accept and a human cancel by inspecting the stored answer.
    - The expired pause behaves downstream as a refusal: whatever the pause was gating does not proceed.
    - An already-resumed pause is never touched by the sweep.
    - A sweep racing a genuine human resume on the same token: exactly one wins, the other observes the conditional update finding nothing, and neither corrupts state. Both orderings are tested.
    - The reaper starts lazily, ticks on an interval derived from the TTL, stops on `Close`, and leaves no goroutine behind under goleak.
    - With the TTL knob set to a non-positive value the sweep is disabled entirely and no goroutine starts.
  </behavior>
  <action>
Tests first, red.

Land the sweep in a NEW sibling file `internal/askuser/expire.go` — `wc -l internal/askuser/store.go`
first; it was tight when this plan was written, and appending a ticker plus a reaper to it would trip
the file-size rule.

Reuse `runregistry.go`'s reaper shape verbatim in structure: lazy start, `stop`/`reaperDone` channel
pair, ticker, join on `Close`. Do not invent a second goroutine-lifecycle idiom in this codebase.

Resolve an expired pause **through the same conditional update a real resume uses**
(`MarkResumedTx`'s `WHERE resumed_at IS NULL` path), with a distinct `ActionExpired` answer. That is
what makes the expiry-versus-resume race safe by construction rather than by luck: exactly one
conditional update can affect the row. Do NOT write a bespoke `UPDATE` for expiry — that is precisely
the "second path around the transaction's front door" the folded todo forbids.

`ActionExpired` must be added to the store's action-validation set, and it must be visibly not an
approval: a reader of `resumed_answer` must be able to tell an expiry from a human decision without
consulting a timestamp.

Wire the reaper's start and its shutdown join in `cmd/aura/serve.go` beside the peers already drained
there. A non-positive TTL starts nothing.

If — and only if — the schema genuinely cannot express the sweep, resolve the next migration number
by `ls internal/db/migrations/ | tail -1` + 1 at that moment, and record in the SUMMARY why
`created_at` was insufficient.

Commit: `fix(52): expire pending approvals on a TTL through the resume front door`.
  </action>
  <acceptance_criteria>
    - `go test -race ./internal/askuser/ -run 'TestExpirePending|TestExpiryIsNotAnApproval|TestExpiryRacesRealResumeSafely|TestReaperIsGoleakClean' -v` passes.
    - `go test -tags=db_integration -race ./internal/askuser/` passes against the disposable coverage DB, exercising the real conditional update.
    - `grep -c 'UPDATE' internal/askuser/expire.go` returns 0 — the file issues no bespoke update; it calls the existing resume path.
    - `grep -n 'ActionExpired' internal/askuser/store.go` shows it in the action-validation set.
    - A goleak test covers the reaper's full start/stop cycle.
    - A test asserts a non-positive TTL starts no goroutine.
    - `wc -l internal/askuser/store.go internal/askuser/expire.go cmd/aura/serve.go` — all ≤ 600.
    - `go vet ./...`, `go build ./...` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; go test -race ./internal/askuser/; [ "$(grep -c UPDATE internal/askuser/expire.go)" = 0 ]; grep -q ActionExpired internal/askuser/store.go; for f in internal/askuser/store.go internal/askuser/expire.go cmd/aura/serve.go; do [ "$(wc -l < $f)" -le 600 ]; done; go vet ./...; echo OK'</automated>
  </verify>
  <done>A question nobody answered stops being open forever, and it closes as a "nobody answered" — never as a yes.</done>
</task>

</tasks>

<threat_model>
**Tier: ASVS L1 — block on `high`.** Every threat rated HIGH below must be mitigated before the plan
closes; `accept` is available only for the non-high rows and each carries its stated residual.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The human's resume POST → the pause store | LibreChat's stated threat model, and the one Aura does not currently cover: the POST is untrusted. |
| Time → an unanswered approval | A gate nobody closes is a gate that stays open. |
| Expiry sweep → the resume conditional update | Two writers for one row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-40 | **Elevation of Privilege (HIGH)** | A crafted resume POST approving a pause the policy restricted to decline/respond | mitigate | `validateResumeEntries` enforces the per-pause permitted-decision set on BOTH `SubmitAnswers` call sites and returns 403, matching the reference behaviour amendment #133 records. The set is written at mint in `runner_mint_approval.go` in the same commit, because an unrecorded policy is unenforceable. Absent policy defaults to today's set, so nothing already pending becomes unresolvable. |
| T-52-41 | **Elevation of Privilege (HIGH)** | An accept carrying no payload resuming the model with an empty tool answer | mitigate | Refused with 400 before the store is reached. `payloadString`'s nil→`""` behaviour is left intact because it is correct for a cancel; the caller's contract is what changes. |
| T-52-42 | **Elevation of Privilege (HIGH)** | A pending approval timing out into an implicit yes | mitigate | The plan's authored prohibition, rated one-way. Expiry writes a distinct `ActionExpired` answer and behaves downstream as a refusal, asserted by `TestExpiryIsNotAnApproval`. |
| T-52-43 | Tampering | An expiry and a genuine human resume both landing on the same pause | mitigate | Both go through the SAME `WHERE resumed_at IS NULL` conditional update in `paused_states.sql`, so exactly one can affect the row; the loser observes zero rows affected. Both orderings are tested. `grep -c UPDATE` on the expiry file being 0 is the structural guard against a bespoke second writer. |
| T-52-44 | Denial of Service | A leaked reaper goroutine per boot, or a ticker running when the feature is disabled | mitigate | Lazy start, `stop`/`reaperDone` join on `Close`, goleak test over the full cycle, and a non-positive TTL starting nothing. |
| T-52-45 | Tampering | A new validation path that weakens the idempotency key it was supposed to protect | mitigate | `TestIdempotencyInvariantUnchanged` is written first and kept green throughout; `git diff` on the store's `RowsAffected==0` logic and on `CommitResumeBatch`'s ordering must be empty. |
| T-52-46 | Information Disclosure | A 403 on a disallowed decision revealing the existence of a pause to a caller who should not know | **accept** | The resume routes already sit behind the parent `RequireAuth` whole-origin gate and are owner-scoped; the caller is an authenticated operator by construction. The 403-vs-404 distinction that matters on the run routes (T-52-11) does not apply here, because reaching this code already proves ownership. |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
- `go test -race ./internal/agui/ ./internal/runner/ ./internal/askuser/`
- `go test -tags=db_integration -race ./internal/askuser/ ./internal/runner/` — **run it as `aura_app`/`aura_migrate` against the disposable coverage DB, never as the superuser `aura` role and never against the live `aura` database.** A hand-run as `aura` is BYPASSRLS and gives a false green on every identity-scoping assertion.
- goleak on the reaper.
- `go vet ./...`, `go build ./...`; `wc -l` ≤ 600 on every touched file.
</verification>

<success_criteria>
- The three defects of amendment #133 are closed, each with a test that fails without the fix.
- Aura's idempotency story is provably no weaker than before, and the conditional update still lives
  where it always did.
- No pending approval can stay open forever, and no expiry can be mistaken for a human yes.
- RESUME-01 has an implementation a verifier can find.
</success_criteria>

<output>
Create `.planning/phases/52-mid-turn-steering/52-03-SUMMARY.md` when done. Record where the per-pause
decision policy is stored and why, whether a migration was needed (and if so, the number the
directory actually yielded), and the TTL sweep interval.
</output>
