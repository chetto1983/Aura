---
phase: 27-neo4j-graph-explorer
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/src/graph/types.ts
  - web/src/graph/graphIntent.ts
  - web/src/graph/graphApi.ts
  - web/src/graph/__tests__/graphIntent.test.ts
autonomous: false
requirements: [GRAPH-02, GRAPH-04]
must_haves:
  truths:
    - "The four MIT graph packages (sigma 3.0.3, graphology 0.26.0, @react-sigma/core 5.0.6, graphology-layout-forceatlas2 0.10.1) install at their pinned versions and are re-verified before the lockfile commits (D-01)"
    - "graphIntent.ts is a PURE module (no sigma/@react-sigma import) holding the intent reducer, label/edge filter predicates, and the schema-driven label-family color mapper (D-02, D-04)"
    - "The label-family color mapper maps known Frame-06 families to brand tokens and assigns unmapped live labels a DETERMINISTIC stable color via hash(label)→ramp; Entity.type is a second color dimension (D-02)"
    - "graphApi.ts provides typed fetch wrappers for GET /api/graph/schema and POST /api/graph/query mirroring the plan-01/02 contract; it follows the SAME error convention as web/src/conversations/useConversations.ts getJSON — credentials: 'same-origin', and a non-200 (incl. 401) THROWS `new Error(`HTTP ${status}`)` (no discriminated-union return), so consumers drive it via a TanStack Query error path / try-catch (GRAPH-01 consumer, W3)"
    - "postGraphQuery and fetchGraphSchema return a REJECTED promise (throw) on a mocked non-200 response — explicitly including a 401 (expired/absent session) — so the plan-04 workspace can surface a visible auth-error state rather than a silent blank canvas (B3, threat T-27-03)"
    - "Vitest unit tests cover color/filter/intent/normalization logic with NO real WebGL and reach ≥85% line coverage on the pure modules"
  artifacts:
    - path: "web/src/graph/types.ts"
      provides: "TS mirror of the {nodes,edges,paths,schema,query} contract + GraphIntent"
      contains: "GraphResult"
    - path: "web/src/graph/graphIntent.ts"
      provides: "pure intent reducer + filter predicates + labelFamilyColor mapper + rows→client-graph mapping"
      contains: "labelFamilyColor"
    - path: "web/src/graph/graphApi.ts"
      provides: "fetchGraphSchema + postGraphQuery typed fetch wrappers (same-origin credentialed; non-200 incl. 401 throws `Error(`HTTP ${status}`)`, mirroring useConversations.ts getJSON)"
      contains: "postGraphQuery"
    - path: "web/src/graph/__tests__/graphIntent.test.ts"
      provides: "Vitest coverage of color/filter/intent (jsdom, no WebGL)"
      contains: "describe"
  key_links:
    - from: "web/src/graph/graphApi.ts"
      to: "POST /api/graph/query + GET /api/graph/schema"
      via: "fetch({credentials:'same-origin'}); mirrors web/src/conversations/useConversations.ts getJSON — non-200 (incl. 401) throws `Error(`HTTP ${status}`)`, consumed via a TanStack Query error path in plan 04"
      pattern: "/api/graph/"
    - from: "graphIntent.ts labelFamilyColor"
      to: "the locked brand tokens + a deterministic ramp"
      via: "known family → var(--color-*); unmapped → hash(label) → ramp index"
      pattern: "hash"
  prohibitions:
    - "NO sigma / @react-sigma / graphology import in graphIntent.ts, graphApi.ts, or types.ts (jsdom has no WebGL — Pitfall 4; the renderer is imported only by SigmaCanvas in plan 04)"
    - "NO raw-Cypher input field or Cypher string built on the client — the client sends a structured GraphIntent only (D-04/D-05)"
    - "NO hard-coded per-label color table and NO rainbow — color is schema-driven from the brand ramp (D-02)"
    - "NO unpinned/caret install that drifts the four versions; NO package with a non-empty scripts.postinstall accepted without inspection (T-27-SC)"
    - "NO dangerouslySetInnerHTML / HTML-from-property anywhere; node/edge property strings are data rendered as text (HARDEN-08)"
---

<objective>
Install the locked Sigma/graphology stack (gated by a legitimacy checkpoint) and build the PURE, jsdom-testable frontend core — the TS contract types, the intent/filter/color logic, and the typed fetch wrappers — that carries the Vitest ≥85% + Stryker ≥70% gate without ever touching a real WebGL context. This is the testable seam the plan-04 renderer mounts on.

Purpose: Pitfall 4 — WebGL cannot run in jsdom. All the logic that must be unit-/mutation-tested (label-family color, label/edge filters, intent state, row→client-graph mapping) lives here with NO `sigma` import; `SigmaCanvas` (plan 04) is the only module that imports the renderer and is exercised only in Playwright.
Output: `web/package.json`/lockfile (4 deps) + `web/src/graph/{types,graphIntent,graphApi}.ts` + Vitest tests.

Artifacts this phase produces (this plan's slice):
- npm deps: `sigma@3.0.3`, `graphology@0.26.0`, `@react-sigma/core@5.0.6`, `graphology-layout-forceatlas2@0.10.1` (optional `graphology-communities-louvain@2.0.2`)
- `web/src/graph/types.ts` — `GraphResult`, `GraphNode`, `GraphEdge`, `GraphPath`, `GraphSchema`, `GraphIntent` (TS mirror of the Go contract)
- `web/src/graph/graphIntent.ts` — `intentReducer`, `applyFilters(nodes, edges, intent)`, `labelFamilyColor(label, entityType, schema)`, `rowsToClientGraph`
- `web/src/graph/graphApi.ts` — `fetchGraphSchema()`, `postGraphQuery(intent)` (typed, credentialed fetch)
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-neo4j-graph-explorer/27-CONTEXT.md
@.planning/phases/27-neo4j-graph-explorer/27-RESEARCH.md
@.planning/phases/27-neo4j-graph-explorer/27-PATTERNS.md
@.planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md
@.planning/phases/27-neo4j-graph-explorer/27-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install + legitimacy-gate the four MIT graph packages</name>
  <what-built>
    The locked renderer stack will be added to web/package.json + package-lock.json. Before this runs, re-verify each package's current version + empty postinstall (slopcheck dry-run equivalent), per the RESEARCH §Package Legitimacy Audit recommendation.
  </what-built>
  <read_first>
    - .planning/phases/27-neo4j-graph-explorer/27-RESEARCH.md §"Package Legitimacy Audit" (the approved table + the planner recommendation to gate the install) + §"Standard Stack" (versions)
    - .planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md §"Registry Safety" (these are npm libs, not shadcn blocks)
    - web/package.json (current deps + the existing test/build/mutation scripts)
  </read_first>
  <action>
    Run, from `web/`, `npm view sigma version`, `npm view graphology version`, `npm view @react-sigma/core version`, `npm view graphology-layout-forceatlas2 version`, and `npm view sigma scripts.postinstall` (expect empty). Confirm the four versions still match the RESEARCH-pinned set (3.0.3 / 0.26.0 / 5.0.6 / 0.10.1) — if any drifted, pin to the RESEARCH-verified version explicitly. Then `npm install --save-exact sigma@3.0.3 graphology@0.26.0 @react-sigma/core@5.0.6 graphology-layout-forceatlas2@0.10.1`. Present the resolved versions + the postinstall check result for sign-off BEFORE the lockfile is committed.
  </action>
  <how-to-verify>
    1. Review the printed `npm view <pkg> version` outputs — all four match the pinned set (or are explicitly re-pinned to a verified version).
    2. Confirm `npm view sigma scripts.postinstall` is empty (no install-time script of concern).
    3. Confirm only the four (+ optional louvain) packages + their transitive deps changed in `package-lock.json`.
  </how-to-verify>
  <resume-signal>Type "approved" once the versions + postinstall check look right, or name the package to re-pin/reject.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: types.ts + graphApi.ts + pure graphIntent.ts (intent/filter/color) + Vitest</name>
  <files>web/src/graph/types.ts, web/src/graph/graphApi.ts, web/src/graph/graphIntent.ts, web/src/graph/__tests__/graphIntent.test.ts</files>
  <read_first>
    - web/src/chat/displays/sourceExplorerData.ts + web/src/chat/displays/__tests__/sourceExplorerData.test.ts:1-13 — the "pure logic off the .tsx, directly mutation-tested" idiom (the exact testable-seam pattern to copy) including the test-header discipline
    - internal/knowledge/graphview.go + graphview_intent.go from plan 01 — the EXACT contract field names (GraphResult/GraphNode/GraphEdge/GraphPath/GraphSchema/GraphIntent) to mirror in TS (json tags → TS keys)
    - .planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md §"Color → Label-family palette" (the family→token table + the unmapped hash→ramp rule + Entity.type as 2nd dimension + the ≥3:1 / ≥4.5:1 contrast rules)
    - .planning/phases/27-neo4j-graph-explorer/27-RESEARCH.md §"Dense-graph strategy" (caps 75/300, 200/800 — the client default intent) + §Accessibility (color-never-the-only-encoding)
    - web/src/conversations/useConversations.ts:61-70 (`getJSON<T>`) + :108-120 (`mutate`) — the EXACT credentialed-fetch convention graphApi.ts must mirror: `credentials: 'same-origin'`, `headers: { Accept: 'application/json' }`, and `if (!res.ok) throw new Error(`HTTP ${String(res.status)}`)` (non-200 THROWS — no discriminated-union return), consumed via TanStack Query `queryFn` with `retry: false` so a 401/transient failure surfaces immediately on the error path. (web/src/chat/attachments/api.ts `readJSON`/`errorDetail` is a richer variant; prefer the simpler useConversations `getJSON` shape for parity.)
  </read_first>
  <behavior>
    - labelFamilyColor('Document', undefined, schema) returns the source/evidence brand token (deterministic, stable across calls)
    - labelFamilyColor('Entity', 'PERSON', schema) and labelFamilyColor('Entity', 'ORGANIZATION', schema) return DIFFERENT shades (Entity.type is a 2nd dimension) but both in the entity family hue
    - labelFamilyColor('SomeUnmappedLabel', undefined, schema) === labelFamilyColor('SomeUnmappedLabel', undefined, schema) (deterministic hash→ramp), and a different unmapped label generally maps to a different ramp index
    - applyFilters dims/excludes nodes whose label is not in the active label filter and edges whose relType is not in the active relType filter; an empty filter set means "show all"
    - intentReducer transitions: setSeed(session) → op 'seed'; expand(nodeId) → op 'expand' with seed_id set; toggling a label updates intent.labels; defaults are node_cap 75 / edge_cap 200
    - rowsToClientGraph maps a contract GraphResult into the graphology-ready {nodes,edges} (id/caption/color/size + source/target/label) with no duplicate node ids
    - postGraphQuery/fetchGraphSchema send same-origin credentialed requests to the correct paths and parse the typed contract (test via a mocked fetch)
    - postGraphQuery and fetchGraphSchema each REJECT (throw `Error(`HTTP 401`)`) when `fetch` is mocked to resolve a 401 response — a NAMED Vitest case (e.g. `it('postGraphQuery rejects on 401')` / `it('fetchGraphSchema rejects on 401')`) so the consumer (plan 04) can render an auth-error state, never swallow it (B3)
  </behavior>
  <action>
    Create `types.ts` mirroring the plan-01 Go contract field-for-field (use the json tag names as TS keys; `GraphIntent` op union `'seed' | 'expand' | 'schema_overview'`). Create `graphApi.ts` with `fetchGraphSchema(): Promise<GraphSchema>` and `postGraphQuery(intent: GraphIntent): Promise<GraphResult>` using same-origin credentialed `fetch` (the RequireAuth cookie rides automatically), mirroring the `useConversations.ts` `getJSON`/`mutate` convention EXACTLY: `credentials: 'same-origin'`, `Accept: 'application/json'` (+ `Content-Type: application/json` on the POST), and `if (!res.ok) throw new Error(`HTTP ${String(res.status)}`)` — a non-200, **including a 401**, THROWS (it does NOT return a discriminated union), so plan 04 wires the consumer via a TanStack Query error path / try-catch. NO Cypher is ever sent — only the structured intent. Create `graphIntent.ts` (PURE — no sigma/graphology/@react-sigma import) with: `intentReducer` (the seed/expand/filter state machine, default caps 75/200), `applyFilters(nodes, edges, intent)`, `labelFamilyColor(label, entityType, schema)` (known Frame-06 families → `var(--color-*)` brand tokens per the UI-SPEC table; unmapped → a small deterministic `hash(label) % rampLen` index into a curated blue-anchored ramp; `entityType` shifts lightness/hue within the entity family), and `rowsToClientGraph`. Size encodes degree (a non-color channel); color is never the only encoding (callers also render caption + legend). Create `__tests__/graphIntent.test.ts` (Vitest, the sourceExplorerData.test.ts shape) covering every Behavior case; mock `fetch` for the graphApi cases INCLUDING a mocked-401 case per wrapper with a NAMED `it(...)` asserting the returned promise rejects (`await expect(postGraphQuery(...)).rejects.toThrow('HTTP 401')` and the same for `fetchGraphSchema`). NO `sigma` import anywhere in this task's files.
    Keep each file ≤600 LOC.
  </action>
  <verify>
    <automated>cd web && npm run test -- src/graph --run</automated>
  </verify>
  <done>graphIntent.ts/graphApi.ts/types.ts compile and pass Vitest with ≥85% line coverage on the pure modules; no sigma/@react-sigma import in any of the three; label-family color is deterministic and schema-driven; the client never builds Cypher; **graphApi.ts mirrors useConversations.ts `getJSON` (credentials 'same-origin'; non-200 incl. 401 throws `Error(`HTTP ${status}`)`), and a named Vitest case asserts both wrappers reject on a mocked 401**.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/graph/* | `graphApi.ts` sends a structured `GraphIntent` (never raw Cypher) over a same-origin credentialed fetch; the server enforces RequireAuth + validation |
| contract JSON → render model | Node/edge property strings (untrusted, partly model/web-derived) flow into the client graph model as data |
| npm registry → build | The four graph packages enter the supply chain |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-SC | Tampering (supply chain) | the npm install | mitigate | Legitimacy-gate checkpoint re-runs `npm view <pkg> version` + checks empty `scripts.postinstall` for the four MIT packages before the lockfile commits; versions pinned `--save-exact`; the RESEARCH audit table records age/downloads/canonical repos |
| T-27-01 | Tampering (Cypher injection) | graphApi.ts | mitigate | The client sends ONLY a structured `GraphIntent`; there is no raw-Cypher field and no client-side Cypher construction (D-04/D-05); the server compiles + parameterizes |
| T-27-04 | Tampering / untrusted output | rowsToClientGraph + color/caption | mitigate | Node/edge property strings are carried as data and rendered as text by the plan-04 components; this plan introduces no HTML-from-property and no `dangerouslySetInnerHTML` (HARDEN-08) |
</threat_model>

<verification>
- `cd web && npm run test -- src/graph --run` passes with ≥85% line coverage on `graphIntent.ts`/`graphApi.ts`
- `cd web && npx tsc --noEmit` (or the project typecheck) clean for the new files
- No `import ... from 'sigma'` / `@react-sigma` / `graphology` in `graphIntent.ts`, `graphApi.ts`, or `types.ts`
- The four packages appear in `web/package.json` at the pinned versions; the lockfile changed only for them + transitive deps
</verification>

<success_criteria>
- GRAPH-02 (foundation): the locked Sigma/graphology stack is installed (legitimacy-gated) and the schema-driven label-family color system is implemented + tested
- GRAPH-04 (client contract): the client speaks the structured-intent contract only (no raw Cypher), with dense-graph caps as the default intent
- The Vitest ≥85% / Stryker ≥70% gate is reachable because the testable logic is in pure modules with no WebGL dependency
</success_criteria>

<output>
Create `.planning/phases/27-neo4j-graph-explorer/27-03-SUMMARY.md` when done
</output>
