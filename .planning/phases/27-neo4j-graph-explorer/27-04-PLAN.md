---
phase: 27-neo4j-graph-explorer
plan: 04
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified:
  - web/src/graph/GraphExplorer.tsx
  - web/src/graph/SigmaCanvas.tsx
  - web/src/graph/SeedFilterPanel.tsx
  - web/src/graph/NodeInspector.tsx
  - web/src/graph/PathStrip.tsx
  - web/src/AppShell.tsx
  - web/src/i18n/resources.graph.ts
  - web/src/i18n/resources.ts
  - web/scripts/contrast-check.mjs
  - web/stryker.config.json
  - web/e2e/graph.spec.ts
  - web/e2e/graph-a11y.spec.ts
  - internal/webui/dist
autonomous: true
requirements: [GRAPH-02, GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - "When surface==='graph' the AppShell center swaps ExternalStoreChat for a LAZY <Suspense> GraphExplorer (its own Vite chunk), seeded from the active conversation (D-07, D-11)"
    - "The Sigma WebGL canvas renders nodes (fill=label-family color, size=degree, caption) + edges (label=rel type), with the selected/pinned path highlighted and non-path elements dimmed (D-02, D-08, D-10)"
    - "Tap/click on a canvas node OR a node-list item opens the inspector; keyboard Tab into the canvas then Enter/Space opens the focused node — hover is NEVER the only access path (GRAPH-03, D-03)"
    - "A semantic DOM node/edge list + the path strip (ordered node —REL→ node steps) are the non-WebGL fallback + screen-reader surface; the canvas has role=img + aria-label (D-03, D-10)"
    - "The inspector shows label/properties/degree/neighbors/citations + pin-path (client-side) + open-source (Phase-26 openSources()) + show-Cypher (read-only); add-note is NOT rendered (GRAPH-03, D-09)"
    - "The inspector RENDERS the node's citations list (the GraphNode.Citations field from plan 01) as distinct read-only text — separate from the open-source ACTION; a named Vitest case asserts a node with citations shows them and a node without renders none (GRAPH-03, SC3, D-09)"
    - "Show Cypher reveals the compiled parameterized query read-only (never editable) (D-04, D-09)"
    - "Dense graphs default to filtered evidence paths with caps + a cap notice; Expand neighbors grows the view intentionally; empty/no-footprint renders the schema overview, never a blank canvas (GRAPH-04, D-08)"
    - "The graph workspace propagates a 401 (expired/absent session) from a graph fetch as a VISIBLE auth-error state (or redirect to login) — never a silent blank canvas; a Playwright e2e asserts this (B3, threat T-27-03)"
    - "A distinct Sigma/GraphExplorer lazy chunk is emitted by the Vite build (separate from index-[hash].js) — Sigma never lands in the main bundle (Pitfall 7)"
    - "graph.* i18n keys exist in en+it; internal/webui/dist is rebuilt + committed; Stryker mutate includes src/graph"
  artifacts:
    - path: "web/src/graph/GraphExplorer.tsx"
      provides: "three-pane workspace shell + intent/selection/path state, lazy-mounted"
      contains: "GraphExplorer"
    - path: "web/src/graph/SigmaCanvas.tsx"
      provides: "SigmaContainer + loader/events/highlight + ForceAtlas2 position-cache + resize-remount + ErrorBoundary"
      contains: "SigmaContainer"
    - path: "web/src/graph/NodeInspector.tsx"
      provides: "node detail incl. citations list + pin-path/open-source/show-Cypher (no add-note)"
      contains: "openSource"
    - path: "web/src/graph/PathStrip.tsx"
      provides: "D-10 path strip + a11y parallel node/edge list (role=list, keyboard)"
      contains: "role=\"list\""
    - path: "web/src/i18n/resources.graph.ts"
      provides: "graphEn/graphIt bundles"
      contains: "graphEn"
    - path: "web/e2e/graph-a11y.spec.ts"
      provides: "axe + keyboard/tap inspector e2e"
      contains: "axe"
  key_links:
    - from: "web/src/AppShell.tsx"
      to: "web/src/graph/GraphExplorer.tsx"
      via: "surface==='graph' ? lazy <Suspense><GraphExplorer/> : <ExternalStoreChat/>"
      pattern: "surface === 'graph'"
    - from: "web/src/graph/NodeInspector.tsx"
      to: "web/src/chat/displays Source Explorer"
      via: "useSourceExplorer().openSources(sourcesForNode(node), node.refId)"
      pattern: "openSources"
    - from: "web/src/graph/SigmaCanvas.tsx"
      to: "graphIntent.ts (plan 03)"
      via: "labelFamilyColor + rowsToClientGraph drive node fills + the graphology Graph"
      pattern: "labelFamilyColor"
  prohibitions:
    - "NO add-note / write / annotation action in the inspector or anywhere (deferred to Phase 29)"
    - "NO editable Cypher input — Show Cypher is display-only (D-04/D-09)"
    - "NO hover-only inspector trigger (WCAG fail — click/tap/keyboard MUST also open it, D-03)"
    - "NO re-layout on every render — cache ForceAtlas2 positions (Pitfall 1/llm_wiki position cache)"
    - "NO sigma import in graphIntent.ts/graphApi.ts/types.ts (already pure from plan 03) — only SigmaCanvas imports the renderer"
    - "NO inline graph_chunk chat mini-preview added (D-11) — the Phase-26 router default: stays untouched"
    - "NO dockable-window / icon-rail / command-palette (ui_control deferred) — this is a mode-swap workspace only (D-11)"
    - "NO dangerouslySetInnerHTML for node/edge property strings — render as text (HARDEN-08)"
    - "NO Sigma in the MAIN bundle — it must be a distinct lazy chunk (Pitfall 7)"
    - "NO file >600 LOC — split components by concern"
---

<objective>
Build the Frame-06 three-pane Graph Explorer workspace — the Sigma WebGL canvas (with the resize-remount crash workaround + position cache), the seed/filter panel with the read-only Cypher preview, the node inspector with the read-only action set (incl. the citations list), and the path strip / a11y parallel-DOM surface — mount it as a lazy chunk when `surface==='graph'`, add en+it copy, extend the mutation scope, and rebuild `dist`. Completes GRAPH-02, GRAPH-03, and the visible half of GRAPH-04.

Purpose: This is the operator-facing evidence-graph workspace. It must be accessible (hover is never the only path), read-only (no add-note, no editable Cypher), legible (evidence paths, not a hairball), resilient (a 401 surfaces as a visible error, never a blank canvas), and a lazy chunk (no main-bundle weight). It consumes the plan-03 pure core + the plan-02 REST routes.
Output: `web/src/graph/{GraphExplorer,SigmaCanvas,SeedFilterPanel,NodeInspector,PathStrip}.tsx` + the AppShell swap + `resources.graph.ts`/`resources.ts` + `contrast-check.mjs` + `stryker.config.json` + `web/e2e/graph*.spec.ts` + rebuilt `internal/webui/dist`.

Artifacts this phase produces (this plan's slice):
- `web/src/graph/SigmaCanvas.tsx` (greenfield WebGL renderer — the ONLY module importing the Sigma stack)
- `web/src/graph/GraphExplorer.tsx` (lazy default export), `SeedFilterPanel.tsx`, `NodeInspector.tsx` (renders `GraphNode.Citations`), `PathStrip.tsx`
- `web/src/i18n/resources.graph.ts` (`graphEn`/`graphIt`) + spreads in `resources.ts`
- the `surface==='graph'` center swap + `const GraphExplorer = React.lazy(...)` in `AppShell.tsx`
- the label-family ramp added to `web/scripts/contrast-check.mjs`
- `src/graph/**` added to `web/stryker.config.json` `mutate`
- `web/e2e/graph.spec.ts` + `web/e2e/graph-a11y.spec.ts` (incl. the session-expiry 401 → visible-error e2e)
- regenerated `internal/webui/dist` (with the distinct Sigma lazy chunk)
- new i18n keys: `graph.title`, `graph.loading`, `graph.cta.seedConversation`, `graph.cta.expand`, `graph.empty.heading`, `graph.empty.body`, `graph.inspector.emptyHeading`, `graph.inspector.emptyBody`, `graph.inspector.pinPath`, `graph.inspector.openSource`, `graph.inspector.showCypher`, `graph.inspector.citations`, `graph.error.query`, `graph.error.schema`, `graph.error.auth`, `graph.cypher.show`, `graph.cypher.hide`, `graph.cypher.note`, `graph.path.label`, `graph.path.empty`, `graph.a11y.nodeList`, `graph.a11y.edgeList`, `graph.a11y.canvasName`, `graph.filter.labels`, `graph.filter.relTypes`, `graph.cap.notice`
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-neo4j-graph-explorer/27-CONTEXT.md
@.planning/phases/27-neo4j-graph-explorer/27-RESEARCH.md
@.planning/phases/27-neo4j-graph-explorer/27-PATTERNS.md
@.planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md
@.planning/phases/27-neo4j-graph-explorer/27-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SigmaCanvas — the greenfield WebGL renderer (ported from llm_wiki)</name>
  <files>web/src/graph/SigmaCanvas.tsx, web/src/graph/__tests__/SigmaCanvas.test.tsx</files>
  <read_first>
    - **First confirm `D:/tmp/llm_wiki/src/components/graph/graph-view.tsx` is readable** (the external load-bearing template). If it is present, port from it. If it is NOT readable, fall back to RESEARCH §"Pattern 2" (the Sigma + graphology wiring excerpt) + Pitfall 1 (resize-remount) as the spec — do NOT block; the RESEARCH excerpt is sufficient to build the canvas.
    - D:/tmp/llm_wiki/src/components/graph/graph-view.tsx — the EXTERNAL wiring reference (SigmaContainer + useLoadGraph + useRegisterEvents + useSigma; nodeReducer/edgeReducer highlight-dim; the `key={sigmaKey}` resize-remount crash workaround at ~:485-524; the ForceAtlas2 inferSettings+assign with barnesHutOptimize + the positionCache). This is greenfield-ported, NOT from this repo.
    - web/src/graph/graphIntent.ts + types.ts from plan 03 (labelFamilyColor, applyFilters, rowsToClientGraph, GraphResult/GraphNode shapes)
    - .planning/phases/27-neo4j-graph-explorer/27-RESEARCH.md §"Pattern 2" (the Sigma wiring) + Pitfall 1 (resize-remount crash) + Pitfall 4 (jsdom has no WebGL — mock the renderer in unit) + Pitfall 7 (lazy chunk)
    - .planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md §"Canvas + visual encoding" + §Color + §Typography (canvas labels)
  </read_first>
  <action>
    Create `SigmaCanvas.tsx` (the ONLY module importing `sigma`/`@react-sigma/core`/`graphology`/`graphology-layout-forceatlas2`): a `SigmaContainer` with inner hook components — a `GraphLoader` (`useLoadGraph` builds the graphology `Graph` from `rowsToClientGraph`, runs `forceAtlas2.assign` with `inferSettings` + `barnesHutOptimize: nodes.length > 50` + iterations ~150, and caches positions so re-renders do not re-layout), an `EventHandler` (`useRegisterEvents` binding `clickNode`/`enterNode`/`leaveNode`, exposed via props so the parent shell opens the inspector), and a `HighlightManager` (`useSigma` + `nodeReducer`/`edgeReducer` to highlight the pinned path and dim the rest — D-10/D-08). Apply the Pitfall-1 workaround: a `key={sigmaKey}` prop that the parent increments on mode-swap + inspector open/close (debounced) and wrap the container in an `ErrorBoundary`. The wrapper gets `role="img"` + `aria-label={t('graph.a11y.canvasName', {nodeCount, edgeCount})}`. Respect `prefers-reduced-motion` (skip the animated settle). Node fill = `labelFamilyColor`, size = degree, caption = name; edge label = rel type above a zoom threshold. SigmaCanvas takes `{nodes, edges, pinnedPath, sigmaKey, onNodeClick, ...}` as props — it owns rendering ONLY; the intent/seed/fetch state lives in GraphExplorer (Task 2).
    Create `__tests__/SigmaCanvas.test.tsx` mocking `@react-sigma/core` (jsdom has no WebGL — Pitfall 4) to assert the canvas wrapper gets `role="img"` + the accessible name and that node/edge props flow in; the real WebGL render is asserted in Task 4's Playwright e2e.
    Split any file approaching 600 LOC by concern (e.g. the reducers into a sibling `SigmaCanvas_reducers.ts`).
  </action>
  <verify>
    <automated>cd web && npm run test -- src/graph/__tests__/SigmaCanvas --run && npx tsc --noEmit</automated>
  </verify>
  <done>SigmaCanvas wires the llm_wiki pattern (or the RESEARCH §Pattern 2 fallback if llm_wiki is unreadable) with position-cache + `key={sigmaKey}` resize-remount + ErrorBoundary + `role="img"` accessible name; node/edge props flow in; the component test passes with a mocked renderer; tsc clean; only this file imports the Sigma stack.</done>
</task>

<task type="auto">
  <name>Task 2: GraphExplorer shell + SeedFilterPanel (no WebGL) + lazy AppShell swap</name>
  <files>web/src/graph/GraphExplorer.tsx, web/src/graph/SeedFilterPanel.tsx, web/src/AppShell.tsx, web/src/graph/__tests__/GraphExplorer.test.tsx</files>
  <read_first>
    - web/src/AppShell.tsx:75-89 (useSurfaceIntent → surface) + :212-236 (the center <section> that today always mounts ExternalStoreChat inside a Suspense) — the exact swap seam
    - web/src/shell/modes.ts:1 (MODES includes 'graph' already)
    - web/src/graph/graphIntent.ts + graphApi.ts + types.ts from plan 03 (intentReducer, applyFilters, postGraphQuery, fetchGraphSchema — note: non-200 incl. 401 THROWS `Error(`HTTP ${status}`)`, so the shell must catch and render an error state)
    - web/src/graph/SigmaCanvas.tsx from Task 1 (the renderer props contract)
    - .planning/phases/27-neo4j-graph-explorer/27-RESEARCH.md §"Dense-graph strategy" (caps + cap notice) + §"Default seed" (op:'seed' from threadId, schema-overview fallback)
    - .planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md §"Layout — Frame-06 three-pane" + §"States" (loading/empty/error/populated) + §"Read-only transparency" (the Cypher preview is display-only)
  </read_first>
  <behavior>
    - On mount with a threadId, GraphExplorer calls `postGraphQuery({op:'seed', session: threadId})`; on an empty result it calls `fetchGraphSchema()` and renders the schema overview (never a blank canvas)
    - When a graph fetch REJECTS with `HTTP 401`, GraphExplorer renders a visible auth-error state (using `graph.error.auth`) — NOT a blank canvas and NOT a swallowed error (B3)
    - When a graph fetch rejects with a non-401 error, it renders the `graph.error.query`/`graph.error.schema` state
    - On result overflow (caps hit), the cap notice (`graph.cap.notice`) is shown
    - SeedFilterPanel's "Show Cypher" toggle reveals `result.query` as read-only text (never an input)
  </behavior>
  <action>
    Create `GraphExplorer.tsx` (default export for `React.lazy`): the three-CSS-grid-column workspace shell holding `SeedFilterPanel | SigmaCanvas | NodeInspector` with the path strip below the canvas; owns the intent/selection/path state (via the plan-03 `intentReducer`) and the `sigmaKey` counter (bumped on mode-swap + inspector open/close for Pitfall-1). On mount, seed from `threadId` (`postGraphQuery({op:'seed', session: threadId})`) wrapped in try/catch (or a TanStack Query error path): on an empty result fall back to the schema overview (`fetchGraphSchema`); **on a rejected promise whose message is `HTTP 401`, render a visible auth-error state (`graph.error.auth`) — never a blank canvas (B3)**; on other rejections render `graph.error.query`/`graph.error.schema`. Render loading/empty/error/populated states (UI-SPEC §States). Wire `Expand neighbors` (`op:'expand'`) and the cap notice (`graph.cap.notice`) on overflow. (NodeInspector + PathStrip are created in Task 3 and slotted into this shell there; Task 2 may stub the right pane + below-canvas slot.)
    Create `SeedFilterPanel.tsx` (left pane): the seed control (primary CTA `graph.cta.seedConversation`), label/edge-type filter toggles (44px touch targets, accent only when active, `aria-invalid` omitted when valid), and the read-only Cypher preview (Commit Mono, `graph.cypher.show`/`hide` + `graph.cypher.note`) showing `result.query` from the contract — display-only, NEVER an input.
    Edit `AppShell.tsx`: add `const GraphExplorer = React.lazy(() => import('./graph/GraphExplorer'));` and in the center `<section>` swap `ExternalStoreChat` for `<Suspense fallback={…graph.loading…}><GraphExplorer threadId={activeThreadId} /></Suspense>` when `surface === 'graph'` (keep ExternalStoreChat for all other surfaces; do NOT remove the ThreadApprovalCards for chat).
    Create `__tests__/GraphExplorer.test.tsx` mocking `./SigmaCanvas` (no WebGL) + `../graphApi`: assert the seed→schema-overview fallback on an empty result, AND a NAMED case `it('renders an auth-error state on a 401')` asserting that when `postGraphQuery` is mocked to reject with `Error('HTTP 401')` the shell shows the `graph.error.auth` copy (NOT a blank canvas) — this is the unit half of B3 (the full browser run is Task 4 Playwright).
  </action>
  <verify>
    <automated>cd web && npm run test -- src/graph/__tests__/GraphExplorer --run && npx tsc --noEmit</automated>
  </verify>
  <done>The three-pane GraphExplorer lazy-mounts on surface==='graph'; it seeds from threadId with a schema-overview fallback; a 401 fetch rejection renders a visible auth-error state (named unit test) rather than a blank canvas; the SeedFilterPanel Cypher preview is display-only; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: NodeInspector (read-only action set + citations list + Source Explorer cross-link) + PathStrip + a11y parallel DOM</name>
  <files>web/src/graph/NodeInspector.tsx, web/src/graph/PathStrip.tsx, web/src/graph/GraphExplorer.tsx, web/src/graph/__tests__/NodeInspector.test.tsx</files>
  <read_first>
    - web/src/chat/displays/SourceExplorerContext.tsx:30-48 (openSources signature + the single SourceExplorerSheet the provider renders) + web/src/chat/displays/sourceExplorerControls.ts (the useSourceExplorer/openSources export — the actual cross-link seam, per the PATTERNS naming-drift note) + SourceExplorerSheet.tsx (panel + focus-mgmt layout)
    - web/src/graph/types.ts from plan 03 — the GraphNode shape INCLUDING the `citations` field (the plan-01 `GraphNode.Citations` mirror) the inspector renders
    - .planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md §"Access-path contract (D-03 — HARD a11y gate)" (every row is a testable obligation) + §"Read-only transparency (D-04, D-09)" (inspector action set = pin path / open source / show Cypher; add-note NOT rendered; citations is a DISPLAYED field, not an action) + §"Copywriting Contract" (the exact i18n keys)
    - .planning/phases/27-neo4j-graph-explorer/27-RESEARCH.md §"Accessibility" (parallel-DOM node/edge list, keyboard traversal, focus-return, reduced motion) + the [[feedback_aria_invalid_omit_when_valid]] rule
    - GraphExplorer.tsx + the intent/selection/path state from Task 2
  </read_first>
  <behavior>
    - The inspector renders the selected node's label / properties (as TEXT) / degree / neighbors / **citations** (the `node.citations` list from the contract) — citations shown as read-only text, DISTINCT from the "open source" action
    - A node WITH a non-empty `citations` list shows them; a node with an empty/absent list renders no citations (or an empty-citations affordance) — never crashes
    - The action set is EXACTLY pin-path / open-source / show-Cypher — NO add-note element is present in the DOM
    - "open source" calls a mocked `openSources(sourcesForNode(node), node.refId)` for a `:Document`/`:Source` node
    - Selecting a node via the node-list `Enter` key (the non-hover path) opens the inspector
  </behavior>
  <action>
    Create `NodeInspector.tsx` (right pane): renders the selected node's label / properties (as TEXT, never HTML) / degree (or confidence) / neighbors / **citations** (render `node.citations` from the contract as a read-only list under `graph.inspector.citations` — this is the SC3/D-09 "connected evidence/citations" DISPLAY, distinct from the open-source action), plus the read-only action set — `pin path` (client-side highlight, drives the SigmaCanvas reducer + the path strip), `open source` (calls `useSourceExplorer().openSources(sourcesForNode(node), node.refId)` ONLY when a `:Document`/`:Source` node's URL matches a Phase-26 citation, or for any document node — reuse the existing read-only sheet, do NOT build a new one), and `show Cypher` (reveals `result.query`, display-only). **`add note` is NOT rendered.** Empty state = `graph.inspector.emptyHeading`/`emptyBody`. Opening the inspector moves focus into it; closing returns focus to the originating node-list item (dialog focus-return). On mobile the inspector is a focus-trapped sheet (reuse the Phase-25/26 drawer pattern).
    Create `PathStrip.tsx`: the D-10 path strip below the canvas (the selected path as an ordered list of `node —REL→ node` steps, `graph.path.label`/`empty`) AND the a11y parallel surface — a focusable, ordered DOM **node list** (`role="list"`, `graph.a11y.nodeList` with count, arrow-key/Tab navigation, `Enter` selects = same effect as a canvas click, the focused node mirrored to the canvas) + an **edge list** (`graph.a11y.edgeList`). This DOM mirror is the non-WebGL fallback + the screen-reader surface. Touch targets ≥44px; `aria-invalid` omitted when valid.
    Wire both into `GraphExplorer.tsx` (replacing the Task-2 stubs): selecting a node (canvas click OR node-list Enter/tap) opens the inspector; pinning a path highlights it on the canvas AND in the path strip.
    Create `__tests__/NodeInspector.test.tsx` asserting: the action set is exactly pin/open-source/show-Cypher (no add-note element present); open-source calls a mocked `openSources` with the node's sources + refId; properties render as text; selection-via-node-list-Enter opens the inspector (the non-hover path); AND a NAMED case `it('inspector renders citations')` asserting a node with a non-empty `citations` list shows those citations while a node with none renders no citations — these are the GRAPH-03/D-03/D-09 + SC3 obligations as unit assertions (the full axe/keyboard run is Task 4 Playwright). (The named citations case also satisfies W4 — the `-t inspector` / citations filter matches a real assertion, no 0-test false-green.)
  </action>
  <verify>
    <automated>cd web && npm run test -- src/graph -t 'inspector|path' --run</automated>
  </verify>
  <done>The inspector shows the read-only set (no add-note), renders the node's citations list (named Vitest case) distinct from the open-source action, cross-links via openSources, and renders properties as text; the path strip mirrors the selected path and provides the keyboard-navigable node/edge list; node-list Enter opens the inspector (non-hover path); focus returns on close; unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: i18n (en+it) + contrast-check ramp + Stryker scope + Playwright graph + graph-a11y e2e (incl. 401) + dist rebuild</name>
  <files>web/src/i18n/resources.graph.ts, web/src/i18n/resources.ts, web/scripts/contrast-check.mjs, web/stryker.config.json, web/e2e/graph.spec.ts, web/e2e/graph-a11y.spec.ts, internal/webui/dist</files>
  <read_first>
    - web/src/i18n/resources.display.ts:1-9 (the feature-bundle-split header + the displayEn/displayIt export shape) + web/src/i18n/resources.ts:1,142,415 (the import + the `...displayEn`/`...displayIt` spread sites in en.translation and it.translation)
    - .planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md §"Copywriting Contract" (the FULL key→copy table — every graph.* key incl. graph.inspector.citations + graph.error.auth + its en string; the it bundle is the Italian translation)
    - web/scripts/contrast-check.mjs (the existing 15/15 required-pairs check — add the label-family ramp pairs vs --color-bg) + .planning/phases/27-neo4j-graph-explorer/27-UI-SPEC.md §Color contrast gates (≥3:1 UI graphics, ≥4.5:1 label text)
    - web/stryker.config.json (the current `mutate` scoping only src/chat/displays — extend to src/graph) + web/e2e/displays.spec.ts (the Playwright analog: how the cockpit boots + auth + a surface is exercised) + web/e2e/auth.ts (the e2e auth helper — and how a session is cleared/expired) + web/playwright.config.ts (device projects — desktop + mobile)
    - the CLAUDE.md single-binary //go:embed rule + [[reference_quality_snapshot_freshness_gate_gotcha]] (rebuild dist + the CI freshness gate)
  </read_first>
  <action>
    Create `resources.graph.ts` exporting `graphEn` and `graphIt` with EVERY `graph.*` key from the UI-SPEC Copywriting Contract (en string verbatim; it = the Italian translation) — including `graph.inspector.citations` and `graph.error.auth`. Spread `graphEn`/`graphIt` into `resources.ts` exactly like `displayEn`/`displayIt` (import at top, spread into both `en.translation` and `it.translation`). Add keys to BOTH languages — a missing key in either is a defect.
    Extend `web/scripts/contrast-check.mjs` to include the label-family ramp colors (each node-fill family vs `--color-bg` at the ≥3:1 UI-graphics threshold; legend/label text at ≥4.5:1) so the ramp is gated alongside the existing 15 pairs.
    Edit `web/stryker.config.json`: add `"src/graph/**/*.{ts,tsx}"` to `mutate` (and the `!src/graph/**/__tests__/**` + `!src/graph/types.ts` excludes mirroring the displays entries). The ≥70% break threshold already applies.
    Create `web/e2e/graph.spec.ts` (Playwright, the displays.spec.ts shape, across the desktop + mobile device projects): boot the cockpit authenticated, switch to the graph surface, assert the WebGL canvas renders (a real browser — the only place WebGL runs), the legend shows label families, and the path strip lists a selected path.
    Create `web/e2e/graph-a11y.spec.ts`: run axe and assert 0 WCAG-AA violations on the graph route; assert tap/focus opens the inspector (NOT hover) and keyboard Tab→Enter on a node-list item opens it; assert the canvas wrapper has an accessible name; AND a NAMED case `test('session expires mid-workspace surfaces a visible auth error, not a blank canvas')` — open the graph workspace authenticated, then expire/clear the session (via the auth.ts helper or by clearing the cookie) and trigger the next graph fetch (e.g. Expand neighbors or re-seed); assert the UI shows a visible auth-error state (`graph.error.auth`) or redirects to login, and NEVER a silent blank canvas (B3, threat T-27-03).
    Rebuild the embedded SPA (Node-24 `npm run build`) and commit the regenerated `internal/webui/dist` so the CI freshness gate stays green; confirm the Vite build output shows a DISTINCT Sigma/GraphExplorer lazy chunk (not folded into the main bundle — Pitfall 7).
  </action>
  <verify>
    <automated>cd web && npm run build && node scripts/contrast-check.mjs && npm run test:e2e -- graph</automated>
  </verify>
  <done>graph.* keys (incl. graph.inspector.citations + graph.error.auth) exist in en+it and render; the label-family ramp passes contrast-check; Stryker mutate includes src/graph (≥70% killed); the Playwright graph + graph-a11y specs pass (canvas renders, axe 0 WCAG-AA, tap/keyboard opens the inspector, AND the session-expiry 401 surfaces a visible auth error not a blank canvas); the Vite build emits a **DISTINCT Sigma/GraphExplorer lazy chunk (e.g. `GraphExplorer-[hash].js` separate from `index-[hash].js`)**; `internal/webui/dist` is rebuilt and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract JSON → DOM/canvas | Node/edge label + property strings (untrusted, partly model/web-derived) are rendered in the inspector, path strip, node list, and as canvas labels |
| operator gesture → query | A tap/click/keyboard gesture triggers a structured-intent fetch (plan 03 graphApi) — never raw Cypher |
| "open source" deep-link | The inspector hands node sources to the Phase-26 Source Explorer (read-only) / image-proxy (SSRF-hardened) |
| expired session → graph fetch | A 401 from a graph fetch must surface as a visible error/redirect, never a silent blank canvas |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Tampering / untrusted output (stored-XSS-style) | NodeInspector / PathStrip / SigmaCanvas labels | mitigate | All node/edge label + property strings (and citations) render as TEXT (React escapes by default); NO `dangerouslySetInnerHTML`; carry forward the Phase-26 HARDEN-08 untrusted-output posture in the inspector/path-strip/canvas labels; an e2e/unit assertion confirms a script-like property renders inert |
| T-27-02 | Tampering (write via UI) | inspector action set | mitigate | The action set is exactly pin-path/open-source/show-Cypher (read-only); add-note is NOT rendered; Show Cypher is display-only (no editable input) — a unit test asserts no add-note/editable-Cypher element exists (D-09); citations is a DISPLAY field, not a write |
| T-27-05 | DoS (dense-graph hairball) | GraphExplorer default view | mitigate | Default = filtered evidence paths with caps (75/200) + a cap notice; Expand neighbors grows intentionally; the schema overview is the no-footprint fallback — never an unbounded hairball dump (D-08); the server also clamps caps (plan 01) |
| T-27-03 | Spoofing (auth) | graphApi fetch | mitigate | The fetch is same-origin credentialed; RequireAuth is enforced server-side (plan 02). A 401 (expired session) is propagated by graphApi (plan 03 — non-200 throws) and rendered by GraphExplorer as a visible auth-error state (named unit test) + asserted in a Playwright e2e (session-expiry → visible error, never a blank canvas). The frontend adds no new auth surface |
</threat_model>

<verification>
- `cd web && npm run test -- src/graph --run` passes; `npx tsc --noEmit` clean
- `cd web && npm run test:e2e -- graph` passes both `graph.spec.ts` (canvas renders, legend, path strip) and `graph-a11y.spec.ts` (axe 0 WCAG-AA, tap/keyboard opens inspector, canvas accessible name, session-expiry → visible auth error) across desktop + mobile projects
- `node web/scripts/contrast-check.mjs` passes with the label-family ramp added
- `cd web && npx stryker run` reports ≥70% killed on `src/graph`
- The Vite build emits a DISTINCT Sigma/GraphExplorer chunk (Pitfall 7); `internal/webui/dist` is rebuilt + committed (CI freshness gate green)
- No add-note element, no editable Cypher input, no `dangerouslySetInnerHTML`, no inline graph_chunk preview, no dockable-window machinery
</verification>

<success_criteria>
- GRAPH-02: the operator opens the Graph Explorer (WebGL canvas) and sees evidence paths with label-family color encoding + a readable path strip
- GRAPH-03: the operator selects a node and inspects label/properties/degree/neighbors/citations (the citations list is rendered, asserted by a named Vitest case); on mobile + keyboard, tap/focus opens the inspector (hover is never the only access path)
- GRAPH-04 (visible): dense graphs default to filtered evidence paths (not hairballs) with a read-only Cypher preview; the operator expands neighbors intentionally; a 401 surfaces as a visible error, never a blank canvas
</success_criteria>

<output>
Create `.planning/phases/27-neo4j-graph-explorer/27-04-SUMMARY.md` when done
</output>
