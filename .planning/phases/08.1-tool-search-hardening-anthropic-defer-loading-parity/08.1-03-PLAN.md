---
phase: 08.1-tool-search-hardening-anthropic-defer-loading-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/mcptools/bridge.go
  - internal/agent/mcptools/name.go
  - internal/agent/mcptools/name_test.go
  - internal/agent/mcptools/bridge_test.go
autonomous: true
requirements: [D-06, D-07, D-08]
must_haves:
  truths:
    - "D-06: a bridged MCP tool's model-facing name is namespaced <server>__<tool> (double-underscore), e.g. github__create_issue, applied by Bridge/Mount which take the server/namespace name as input"
    - "D-07: the namespaced name is sanitized to [a-zA-Z0-9_-], capped at 64 bytes, and on collision gets a short deterministic hash suffix (namespace-first then tool-name)"
    - "D-07/D-08: the existing all-or-nothing Mount collision refusal stays as the outer guard, applied AFTER namespacing; namespacing also mitigates malicious tool-name shadowing of built-ins"
    - "the bridgedTool wire name (used by CallTool against the MCP server) stays RAW; only spec.Name (model-facing) is namespaced"
  artifacts:
    - path: "internal/agent/mcptools/name.go"
      provides: "sanitizeName, namespacedName, hashSuffix helpers + nsDelimiter/maxToolNameLen/hashLen consts"
      contains: "func namespacedName"
    - path: "internal/agent/mcptools/name_test.go"
      provides: "table-driven sanitize/64B-cap/collision-hash tests"
    - path: "internal/agent/mcptools/bridge.go"
      provides: "Bridge/Mount take a namespace arg; spec.Name namespaced; bridgedTool.name stays raw"
      contains: "__"
  key_links:
    - from: "internal/agent/mcptools/bridge.go Bridge/Mount"
      to: "internal/agent/mcptools/name.go namespacedName"
      via: "apply <ns>__<tool> sanitize+cap+collision-hash to spec.Name only"
      pattern: "namespacedName"
    - from: "internal/agent/mcptools/bridge.go bridgedTool.Execute"
      to: "srv.CallTool"
      via: "routes by the RAW server-side tool name (bridgedTool.name), not the namespaced spec.Name"
      pattern: "srv.CallTool"
---

<objective>
Namespace bridged MCP tool names as `<server>__<tool>` with sanitize + 64-byte cap + collision hash, mirroring Codex `codex-mcp/src/tools.rs`. Implements D-06, D-07, D-08.

Purpose: When the retained `mcptools` seam is eventually mounted (Phase 11 skills / future stdio MCP servers), bridged tool names must not silently shadow built-ins (a malicious-shadowing mitigation) and must be valid LLM tool-names. `/` and `.` are invalid in tool-names and single-underscore is ambiguous; `__` is the convergent standard (MCP SEP-993, Claude Code SDK, MetaMCP).
Output: A new `name.go` with the string-shaping helpers, `Bridge`/`Mount` taking a namespace argument and namespacing only the model-facing `spec.Name` (the wire name stays raw), and updated tests. Low blast radius: no production caller mounts MCP yet (D-08), so only test assertions change.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-CONTEXT.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-RESEARCH.md
@.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols THIS PHASE creates (not drift — newly introduced by 08.1; this plan creates the mcptools namespacing subset):

This plan (08.1-03):
- `internal/agent/mcptools/name.go` (NEW file) — `func sanitizeName(string) string`; `func namespacedName(namespace, tool string) string` (or a collision-aware variant returning the chosen name); `func hashSuffix(rawIdentity string) string`; consts `nsDelimiter = "__"`, `maxToolNameLen = 64`, `hashLen = 12`.
- `internal/agent/mcptools/name_test.go` (NEW file) — table-driven sanitize/cap/collision tests.
- `internal/agent/mcptools/bridge.go` — `Bridge` and `Mount` gain a `namespace string` parameter (exact signature shape is executor's discretion per CONTEXT D-07 discretion note); `spec.Name` becomes the namespaced name; `bridgedTool.name` stays raw.

Other plans in this phase (for cross-plan source-grounding — do NOT implement here):
- 08.1-01: bm25.go + search.go BM25 keyword ranking + max_results.
- 08.1-02: Registry.Validate() (≥1-non-deferred guard), registry-derived tool_search Description (groups by <ns>__ prefix produced HERE), cmd/aura boot hooks, manifest namespaced-ordering test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create name.go — sanitizeName / namespacedName / hashSuffix string-shaping helpers</name>
  <files>internal/agent/mcptools/name.go, internal/agent/mcptools/name_test.go</files>
  <read_first>
    - internal/agent/mcptools/name.go (does not exist — confirm absence; create new)
    - internal/agent/mcptools/bridge.go (FULL — the firstLine helper lines 126-133 is the in-package pure-string-helper style to mirror; the bridgedTool struct lines 34-39; the current Bridge spec construction lines 79-89)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-RESEARCH.md §"Architecture Patterns" Pattern 3 (lines 199-212: sanitize, __ delimiter, MAX_TOOL_NAME_LENGTH=64, SHA-1 hex, CALLABLE_NAME_HASH_LEN=12, rawIdentity = namespace+"\0"+toolName) + §"Code Examples" MCP namespacing (lines 361-391)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-PATTERNS.md §"internal/agent/mcptools/bridge.go (MODIFY)" (lines 149-187) + §"MCP test double" (lines 253-255)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-RESEARCH.md §"Security Domain" (V6: SHA-1 is collision-disambiguation only, NOT a security control — document that)
  </read_first>
  <behavior>
    - TestSanitizeName: chars outside [a-zA-Z0-9_-] are replaced with '_'; '-' is KEPT (D-07, where Codex maps '-'→'_' — Aura is more permissive); an all-invalid string sanitizes to a non-empty result ("_").
    - TestNamespacedName: namespacedName("github","create_issue") == "github__create_issue" (double-underscore delimiter); a namespace/tool containing odd chars is sanitized on both sides before joining.
    - TestNameLengthCap: a namespace+tool whose joined sanitized name exceeds 64 bytes is truncated and given a deterministic 12-hex hash suffix so the final name is ≤64 bytes; the suffix is stable across calls for the same inputs.
    - TestHashSuffixDeterministic: hashSuffix(same rawIdentity) is byte-identical across calls and is 1+12 chars ("_"+12 hex).
  </behavior>
  <action>
    Create internal/agent/mcptools/name.go, stdlib-only (strings, crypto/sha1, encoding/hex).
    Add consts: `nsDelimiter = "__"`, `maxToolNameLen = 64`, `hashLen = 12`.
    Implement (transcribe RESEARCH §Code Examples MCP namespacing, adapted):
    (a) `sanitizeName(s string) string`: keep runes in [a-zA-Z0-9_-] (NOTE D-07 keeps '-', unlike Codex which maps it to '_'); replace every other rune with '_'; if the result is empty return "_".
    (b) `hashSuffix(rawIdentity string) string`: SHA-1 the rawIdentity bytes, hex-encode, return "_" + first hashLen hex chars. Document in a one-line comment that SHA-1 here is a non-cryptographic collision-disambiguation hash (Codex parity), NOT a security control (RESEARCH §Security Domain V6).
    (c) `namespacedName(namespace, tool string) string`: base = sanitizeName(namespace) + nsDelimiter + sanitizeName(tool); if len(base) > maxToolNameLen, truncate the tool-name part and append hashSuffix(namespace + "\0" + tool) so the total fits in 64 bytes (fit-with-hash). Return base. (Cross-name collision disambiguation across a Mount batch is handled in Task 2, where the registry/seen set is visible — namespacedName here only does prefix+sanitize+length-cap.)
    Write name_test.go: table-driven, mirror the firstLine/bridge_test.go idiom; continue mutant-kill comment convention; cover the sanitize char-class boundary, the '-'-kept case, the 64B boundary (a name exactly 64 vs 65 bytes), and hashSuffix determinism + length.
    Do NOT touch bridge.go in this task.
  </action>
  <verify>
    <automated>go build ./internal/agent/mcptools/ && go test ./internal/agent/mcptools/ -run 'TestSanitizeName|TestNamespacedName|TestNameLengthCap|TestHashSuffixDeterministic' -x</automated>
  </verify>
  <acceptance_criteria>
    - internal/agent/mcptools/name.go exists and contains `func namespacedName(` and `func sanitizeName(` and `func hashSuffix(` and `nsDelimiter = "__"` and `maxToolNameLen = 64` and `hashLen = 12`.
    - namespacedName("github","create_issue") returns "github__create_issue" (asserted in TestNamespacedName).
    - sanitizeName keeps '-' (a case with a hyphen in TestSanitizeName proves the hyphen survives, not mapped to '_').
    - A joined name longer than 64 bytes is ≤64 bytes after namespacedName and ends in a 12-hex suffix (TestNameLengthCap).
    - `go test ./internal/agent/mcptools/ -run 'TestSanitizeName|TestNamespacedName|TestNameLengthCap|TestHashSuffixDeterministic'` exits 0.
  </acceptance_criteria>
  <done>name.go provides deterministic sanitize/namespace/hash helpers matching the Codex __-delimiter/64B/12-hex contract; helper tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply namespacing in Bridge/Mount (model-facing name only; wire name stays raw)</name>
  <files>internal/agent/mcptools/bridge.go, internal/agent/mcptools/bridge_test.go</files>
  <read_first>
    - internal/agent/mcptools/bridge.go (FULL — bridgedTool struct lines 34-39, Execute routes via b.name line 55, Bridge spec construction lines 79-89, Mount all-or-nothing collision guard lines 104-121)
    - internal/agent/mcptools/name.go (the sanitizeName/namespacedName/hashSuffix from Task 1 — the API you wire against)
    - internal/agent/mcptools/bridge_test.go (FULL — fakeServer + sandboxDefs lines 15-39; existing TestBridge_TranslatesTools, TestBridgedTool_Execute_RoutesAndWraps asserts srv.lastName=="sandbox_exec", TestMount_RegistersAndRefusesCollision, TestMount_RefusesDuplicateWithinServer — all legitimately change per D-08)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-PATTERNS.md §"internal/agent/mcptools/bridge.go (MODIFY)" (lines 149-187: the bridgedTool.name=raw vs spec.Name=namespaced distinction is load-bearing; the sequence prefix→sanitize→cap-with-hash→residual-collision-hash→all-or-nothing-check)
    - .planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-CONTEXT.md (D-08: only bridge_test.go assertions change; native built-in names NOT renamed; D-06: Mount/Bridge take the namespace as input)
  </read_first>
  <behavior>
    - TestBridge_Namespaced: Bridge(ctx, "github", srv) over a server advertising "create_issue" yields a tool whose Spec().Name == "github__create_issue" (model-facing) while routing stays raw.
    - TestBridgedTool_RoutesRawName: after namespacing, Execute still calls srv.CallTool with the RAW server-side name ("sandbox_exec"), not the namespaced one (assert fakeServer.lastName == "sandbox_exec").
    - TestMount_Namespaced: Mount(ctx, reg, "sb", srv) registers tools under their namespaced names (reg.Get("sb__sandbox_exec") ok); re-mounting the same namespace collides (all-or-nothing outer guard preserved); a duplicate within the batch is still refused.
    - TestMount_CollisionHash: if two namespaced names would collide (or collide with an existing registry name), the loser gets a deterministic hash suffix before the all-or-nothing check (or the existing refusal fires — match the sequence in PATTERNS: residual collision → append hashSuffix(ns+"\0"+tool), THEN the all-or-nothing check).
  </behavior>
  <action>
    Modify bridge.go:
    (a) Change Bridge and Mount signatures to take a `namespace string` parameter (D-06). Exact placement/shape is your discretion (e.g. `Bridge(ctx, namespace, srv)`, `Mount(ctx, reg, namespace, srv)`).
    (b) In Bridge's spec construction (lines 79-89): set `spec.Name = namespacedName(namespace, d.Name)` (MODEL-facing), but keep `bridgedTool.name = d.Name` RAW (the wire name CallTool uses — Execute at line 55 must keep routing by the raw name). This wire-name/model-name separation is the load-bearing distinction (PATTERNS lines 169).
    (c) In Mount: namespacing slots in BEFORE the collision check (CONTEXT code_context line 94). Sequence per D-07: each bridged tool already has a namespaced+sanitized+length-capped spec.Name from Bridge; in the Mount loop, if a chosen namespaced name collides with an already-chosen name in this batch OR with an existing registry name, append hashSuffix(namespace + "\0" + rawToolName) to disambiguate; THEN keep the existing all-or-nothing refusal (lines 104-114) as the outer guard for any residual collision. Keep firstLine for the Summary unchanged.
    (d) Do NOT rename native built-in tools (D-08) — this only touches the mcptools bridge.
    Update bridge_test.go: pass a namespace to every Bridge/Mount call; assert namespaced spec.Name AND raw-name routing (fakeServer.lastName); update TestMount_RegistersAndRefusesCollision / TestMount_RefusesDuplicateWithinServer to the namespaced names (these assertions legitimately change — D-08); add TestBridge_Namespaced / TestBridgedTool_RoutesRawName / TestMount_CollisionHash. Continue the mutant-kill comment convention.
    Keep bridge.go ≤600 LOC (it is small; helpers live in name.go). Deep-refactor-on-touch.
  </action>
  <verify>
    <automated>go vet ./internal/agent/mcptools/ && go build ./internal/agent/mcptools/ && go test -race ./internal/agent/mcptools/ -x</automated>
  </verify>
  <acceptance_criteria>
    - bridge.go Bridge and Mount take a namespace string argument (grep shows the new param).
    - In bridge.go the bridgedTool literal still sets `name: d.Name` (raw) while the spec uses `namespacedName(...)` for Name — the two are distinct (source assertion).
    - TestBridge_Namespaced asserts Spec().Name == "github__create_issue" (or the sb__sandbox_exec equivalent).
    - TestBridgedTool_RoutesRawName asserts fakeServer.lastName == "sandbox_exec" after Execute despite the namespaced model-facing name.
    - TestMount_Namespaced asserts reg.Get("sb__sandbox_exec") is ok and a re-mount of the same namespace errors.
    - `go test -race ./internal/agent/mcptools/` exits 0.
    - No native built-in tool name is changed (no edits to cmd/aura built-in Register calls in this plan).
  </acceptance_criteria>
  <done>Bridged MCP tools are namespaced <ns>__<tool> (sanitized, 64B-capped, collision-hashed) on the model-facing name; CallTool still routes by the raw wire name; the all-or-nothing Mount guard is preserved; -race clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server → Aura registry | a (future-mounted) MCP server's advertised tool names cross into Aura's model-facing tool namespace |
| MCP server tool name → LLM tool-call wire | a server-controlled name string is placed into the LLM tool manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-04 | Spoofing / Tampering | bridged tool name shadowing a built-in (e.g. server advertises `web_fetch`) | mitigate | `<server>__<tool>` namespacing (D-06) guarantees a server tool can never collide with an un-namespaced built-in; the existing all-or-nothing Mount refusal (bridge.go:104-114) stays as the outer guard (vulnerablemcp.info tool-name-collisions) |
| T-08.1-05 | Tampering / DoS | control chars / oversized name breaks the LLM tool-call parser or wire protocol | mitigate | sanitizeName restricts to [a-zA-Z0-9_-] and namespacedName caps at 64 bytes with a hash suffix (D-07) |
| T-08.1-06 | Tampering | predictable collision hash lets an attacker craft a colliding suffix | accept | the hash is a non-cryptographic disambiguator over namespace+"\0"+tool; collisions still hit the all-or-nothing Mount refusal, so a forced collision fails closed (no silent shadow); documented as non-security SHA-1 use (RESEARCH V6) |
| T-08.1-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs ZERO packages (stdlib crypto/sha1, encoding/hex, strings only); no install task exists |
</threat_model>

<verification>
- `go vet ./internal/agent/mcptools/` clean.
- `go build ./internal/agent/mcptools/` succeeds.
- `go test -race ./internal/agent/mcptools/` exits 0.
- `name.go` and `bridge.go` each ≤600 LOC.
- No new entry in go.mod/go.sum (stdlib only).
</verification>

<success_criteria>
- Bridged MCP tools carry namespaced model-facing names (`<ns>__<tool>`, sanitized to [a-zA-Z0-9_-], ≤64 bytes, collision-hashed) while routing via the raw wire name; the all-or-nothing Mount collision guard is preserved as the outer check (D-06, D-07, D-08).
- Zero new dependencies; native built-in names unchanged.
</success_criteria>

<output>
Create `.planning/phases/08.1-tool-search-hardening-anthropic-defer-loading-parity/08.1-03-SUMMARY.md` when done.
</output>
