---
phase: 20-scheduler-hardening-full-implementation
plan: 04
type: execute
wave: 3
depends_on: ["20-03"]
files_modified:
  - internal/db/migrations/0014_pending_notifications_identity.up.sql
  - internal/db/migrations/0014_pending_notifications_identity.down.sql
  - internal/db/queries/pending_notifications.sql
  - internal/db/sqlc/pending_notifications.sql.go
  - internal/cron/store_runs.go
  - internal/cron/dispatch.go
  - internal/cron/deliver.go
  - internal/cron/dispatch_integration_test.go
autonomous: false
requirements: [R6]
must_haves:
  truths:
    - "Migration 0014 adds pending_notifications.identity_id text (NO FK) and applies (up) + reverts (down) cleanly"
    - "InsertPendingNotification callers thread task.IdentityID; the value round-trips through SweepDueNotifications + the projection"
    - "sweepNotifications delivers a swept row via deliverToOrigin keyed on the row's identity_id (subject to the same AURA_SCHEDULER_PREFER_ORIGIN_CHANNEL gate)"
    - "Legacy/CLI rows with identity_id=NULL fall back to notify_route — byte-identical to today (no backfill, no regression)"
    - "LIVE: a quiet-hours-deferred agent_job notification, swept after the forced window, lands in the origin Telegram chat"
  artifacts:
    - path: "internal/db/migrations/0014_pending_notifications_identity.up.sql"
      provides: "ALTER TABLE aura.pending_notifications ADD COLUMN identity_id text (no FK)"
      contains: "ADD COLUMN identity_id text"
    - path: "internal/db/migrations/0014_pending_notifications_identity.down.sql"
      provides: "clean revert"
      contains: "DROP COLUMN IF EXISTS identity_id"
    - path: "internal/db/queries/pending_notifications.sql"
      provides: "identity_id threaded into InsertPendingNotification + SweepDueNotifications"
      contains: "identity_id"
    - path: "internal/cron/store_runs.go"
      provides: "PendingNotification/InsertPendingNotificationParams/projection carry IdentityID"
      contains: "IdentityID"
    - path: "internal/cron/dispatch.go"
      provides: "sweepNotifications routes swept rows through deliverToOrigin keyed on row identity_id"
      contains: "deliverToOrigin"
    - path: "internal/cron/dispatch_integration_test.go"
      provides: "migration-0014 up/down + identity round-trip db_integration test"
      contains: "identity_id"
  key_links:
    - from: "internal/cron/dispatch.go"
      to: "internal/cron/store_runs.go"
      via: "insertPendingNotification threads task.IdentityID into InsertPendingNotificationParams"
      pattern: "IdentityID:\\s*task\\.IdentityID"
    - from: "internal/cron/dispatch.go"
      to: "internal/cron/deliver.go"
      via: "sweepNotifications calls deliverToOrigin keyed on row.IdentityID"
      pattern: "deliverToOrigin"
    - from: "internal/cron/store_runs.go"
      to: "internal/db/sqlc/pending_notifications.sql.go"
      via: "pendingNotificationFromRow reads r.IdentityID.String"
      pattern: "IdentityID"
---

<objective>
Complete the deferred + failed sweep route-back (R6, Step 2). Migration **0014** adds `identity_id text` (NO FK — the stable snapshot delivery key, mirroring `scheduler_tasks.identity_id`; survives a deleted origin conversation, Fork 1) to `pending_notifications`. The Insert/Sweep sqlc queries + the `store_runs.go` projection thread the column; `insertPendingNotification` callers pass `task.IdentityID`; and `sweepNotifications` delivers swept rows via the `deliverToOrigin` helper (from 20-03) keyed on the row's `identity_id`, under the same default-on kill-switch. Legacy rows (NULL identity) fall back to the route — byte-identical to today, no backfill. A db_integration test proves the migration up/down + the identity round-trip; the live gate proves the quiet-hours-deferred sweep lands in the origin Telegram chat.

Purpose: reminders bypass quiet-hours so Step 1 covers the headline; Step 2 closes the agent_job/advisory deferred + transiently-failed paths so EVERY scheduled-notification class routes back to origin.
Output: migration 0014 (up/down), the sqlc query edits + regen, `store_runs.go` threading, the `sweepNotifications` route-back, the db_integration migration+round-trip test, and the Step-2 LIVE sign-off.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-scheduler-hardening-full-implementation/20-SPEC.md
@.planning/phases/20-scheduler-hardening-full-implementation/20-RESEARCH.md
@.planning/phases/20-scheduler-hardening-full-implementation/20-PATTERNS.md
@.planning/phases/20-scheduler-hardening-full-implementation/20-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates (excluded from drift verification):
- migration `0014_pending_notifications_identity.up.sql` / `.down.sql` (new files)
- `pending_notifications.identity_id` column (`text`, no FK)
- `identity_id` threading in `InsertPendingNotification` + `SweepDueNotifications` (queries/pending_notifications.sql) + regenerated `pending_notifications.sql.go` (`AuraPendingNotifications.IdentityID pgtype.Text`)
- `IdentityID` field on `cron.PendingNotification` + `cron.InsertPendingNotificationParams` + `pendingNotificationFromRow` projection (store_runs.go)
- `sweepNotifications` route-back via `deliverToOrigin` keyed on the row's identity (dispatch.go)
- db_integration test symbols (migration-0014 up/down + identity round-trip)

Consumed-from-earlier-waves (already exist): `deliverToOrigin` + `DispatchDeps.ChannelDeliverer`/`.PreferOriginChannel` (20-03), `task.IdentityID` snapshot (20-02).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: migration 0014 + sqlc query edits + store_runs.go identity threading (R6 schema half)</name>
  <files>internal/db/migrations/0014_pending_notifications_identity.up.sql, internal/db/migrations/0014_pending_notifications_identity.down.sql, internal/db/queries/pending_notifications.sql, internal/db/sqlc/pending_notifications.sql.go, internal/cron/store_runs.go, internal/cron/dispatch.go</files>
  <read_first>
    - internal/db/migrations/0013_pending_notifications.up.sql + .down.sql (the ANALOG — same table; the up relies on the existing 0013 aura_app DML grant, the down is a single DROP)
    - internal/db/queries/pending_notifications.sql (RE-READ CURRENT — not opened in the pattern map; locate `InsertPendingNotification` (column list + `$N` values + RETURNING) and `SweepDueNotifications` (both SELECT lists); `MarkNotification*` unchanged) and internal/db/queries/telegram_accounts.sql:11-14 (the mechanical sqlc column-list edit shape)
    - internal/cron/store_runs.go:106-129 (`PendingNotification` + `InsertPendingNotificationParams`), store_runs.go:149-178 (`InsertPendingNotification` — thread `text(p.IdentityID)` alongside `text(p.NotifyRoute)`), store_runs.go:248-261 (`pendingNotificationFromRow` — add `IdentityID: r.IdentityID.String`)
    - internal/db/sqlc/pending_notifications.sql.go (the generated file regen target — `AuraPendingNotifications` gains `IdentityID pgtype.Text`)
    - 20-PATTERNS.md §"NEW migration 0014" + §"MOD pending_notifications.sql" + §"MOD store_runs.go" (copy-the-shape, verbatim) + 20-RESEARCH.md §"Migration up/down + sqlc header style" + drift #21/#22 (store_runs.go is the threading site; column is text NO FK — NOT the spike's uuid REFERENCES)
    - MEMORY `reference_db_knowledge_integration_test_invocation` (derive AURA_DB_URL/AURA_DB_MIGRATE_URL from POSTGRES_PASSWORD for sqlc/migration work)
  </read_first>
  <action>
    Create `internal/db/migrations/0014_pending_notifications_identity.up.sql`: `ALTER TABLE aura.pending_notifications ADD COLUMN identity_id text;` + a `COMMENT ON COLUMN` documenting it as the stable owning-identity snapshot (Phase 20 / Fork 1), plain text NO FK, NULL for legacy/CLI rows → falls back to notify_route. NO new GRANT (the existing 0013 aura_app DML grant covers the column). Create `0014_..._.down.sql`: `ALTER TABLE aura.pending_notifications DROP COLUMN IF EXISTS identity_id;`. **DO NOT** use the spike's `origin_conversation_id uuid REFERENCES … ON DELETE SET NULL` — it predates Fork 1 and is SPEC-superseded (drift #22). 0014 is the next free slot (0013 is latest, confirmed).

    Edit `internal/db/queries/pending_notifications.sql`: add `identity_id` to `InsertPendingNotification` (column list + the next `$N` value with EXACT placeholder renumbering — read the CURRENT query first + RETURNING list) and to BOTH SELECT lists in `SweepDueNotifications`; leave `MarkNotification*` unchanged. Run `sqlc generate` and commit the regenerated `pending_notifications.sql.go` (the `AuraPendingNotifications` row struct gains `IdentityID pgtype.Text`).

    In `internal/cron/store_runs.go`: add `IdentityID string` to `PendingNotification` (projection) and `InsertPendingNotificationParams`; in `InsertPendingNotification` thread the value alongside `NotifyRoute` using the existing `text(...)` boundary helper (`text(p.IdentityID)` — empty→NULL via pgtype; this column is `text`, NOT a `pgtype.UUID` conversion — Pitfall 6); in `pendingNotificationFromRow` read `IdentityID: r.IdentityID.String`. The `PendingNotificationStore` interface method signature is UNCHANGED (only the params struct gains a field).

    Then thread `IdentityID: task.IdentityID` into the `insertPendingNotification` helper's `InsertPendingNotificationParams` (dispatch.go:237-259) so deferred AND failed rows carry the route-back key (this completes the failed-row threading 20-03 may have deferred).

    Post-edit: `go build ./...`; `go vet ./internal/cron/... ./internal/db/...`.
  </action>
  <verify>
    <automated>cd D:/Aura ; go build ./... ; go vet ./internal/cron/... ./internal/db/...</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE assertion: `0014_pending_notifications_identity.up.sql` contains `ADD COLUMN identity_id text` (and NO `REFERENCES`/`uuid`); the `.down.sql` contains `DROP COLUMN IF EXISTS identity_id`. `internal/db/queries/pending_notifications.sql` has `identity_id` in `InsertPendingNotification` + both `SweepDueNotifications` SELECT lists. `internal/cron/store_runs.go` has `IdentityID` on `PendingNotification` + `InsertPendingNotificationParams` + threaded via `text(p.IdentityID)` + read in `pendingNotificationFromRow`.
    - `go build ./...` succeeds with the regenerated `pending_notifications.sql.go` (the `IdentityID pgtype.Text` field compiles against `store_runs.go`).
    - `dispatch.go`'s `insertPendingNotification` threads `IdentityID: task.IdentityID`.
    - `golangci-lint run ./internal/cron/... ./internal/db/...` 0 issues; all touched files ≤600 LOC.
  </acceptance_criteria>
  <done>Migration 0014 (text, no FK) + the sqlc Insert/Sweep edits + the store_runs.go projection threading ship; the failed/deferred pending rows carry `task.IdentityID`; module builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: sweepNotifications route-back + migration-0014 db_integration round-trip test (R6 behavior half)</name>
  <files>internal/cron/dispatch.go, internal/cron/deliver.go, internal/cron/dispatch_integration_test.go</files>
  <read_first>
    - internal/cron/dispatch.go:278-298 (`sweepNotifications` — the `Notifier.Notify` call at :287 to route swept rows through `deliverToOrigin` keyed on the row's identity)
    - internal/cron/deliver.go (the `deliverToOrigin` helper from 20-03 — RE-READ; for the sweep, key on the swept row's `IdentityID`, not `task.IdentityID`; the gate semantics are the same)
    - internal/cron/dispatch_integration_test.go (the `db_integration`-tagged harness — `migratedPool(t)` applies migrations) + internal/conversations/store_test.go:49 (`migratedPool` shape) + dispatch_test.go `TestDispatchSweepNotifications*` (the sweep test ANALOG)
    - 20-PATTERNS.md Test Analog Map row 5 (migration-0014 db_integration) + 20-VALIDATION.md R6 rows
    - MEMORY `reference_db_knowledge_integration_test_invocation` (derive AURA_DB_URL/AURA_DB_MIGRATE_URL from POSTGRES_PASSWORD; `db_integration` tag) + the no-skip-as-green rule (the tier `t.Fatal`s under $CI when env unset)
  </read_first>
  <behavior>
    - integration: `migratedPool(t)` applies through 0014; insert a `pending_notifications` row WITH `identity_id="id-I"`; `SweepDueNotifications` → the projected `PendingNotification.IdentityID == "id-I"` (round-trips through the new column).
    - migration up/down: a down-migration test reverts 0014 cleanly (the column is dropped, re-up re-adds it).
    - sweep route-back (unit or integration with a fakeChannelDeliverer): a swept row with a real `identity_id` → `deliverToOrigin` keyed on the row identity delivers via the channel; a NULL-identity (legacy) row → falls back to `Notifier.Notify` with the route (byte-identical to today).
  </behavior>
  <action>
    In `sweepNotifications` (dispatch.go:278-298), route each swept row through `deliverToOrigin` BEFORE the existing `Notifier.Notify` at :287, keyed on the SWEPT ROW's `IdentityID` (not `task.IdentityID` — at sweep time there is no live task; the row carries the snapshot). Build a `Task`-shaped argument or extend `deliverToOrigin` to accept an explicit `(identityID, notifyRoute)` pair so it works for both the live-task `notify` path and the swept-row path; reuse the SAME gate (PreferOriginChannel + empty-route + non-'local'/non-empty identity). On not-handled, fall through to the existing route; on owns-but-failed during the sweep, keep the row for the next sweep (do not cross-channel fall back — Pitfall 3). Keep the gate logic single-sourced in `deliver.go` (no duplicated precedence).

    Add the db_integration test in `internal/cron/dispatch_integration_test.go` (build tag `db_integration`): `migratedPool(t)` applies 0014; insert a pending row with `identity_id`; sweep; assert the projection round-trips the identity; add a down-migration revert assertion. Derive `AURA_DB_URL`/`AURA_DB_MIGRATE_URL` from `POSTGRES_PASSWORD` (MEMORY). The tier must ACTUALLY run (no-skip-as-green): the skip-helper `t.Fatal`s under `$CI` when env is unset; a sub-second runtime is a skip tell — verify execution.
  </action>
  <verify>
    <automated>cd D:/Aura ; go test -race -tags db_integration -run TestDispatch ./internal/cron -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -tags db_integration -run TestDispatch ./internal/cron -count=1` (with `AURA_DB_URL`/`AURA_DB_MIGRATE_URL` exported) passes: migration 0014 applies up, the inserted `identity_id` round-trips through `SweepDueNotifications` + the projection, and the down-migration reverts cleanly. Runtime is NOT sub-second (proves the DB tier actually ran, not skipped).
    - SOURCE assertion: `sweepNotifications` calls `deliverToOrigin` (keyed on the swept row's `IdentityID`) before `Notifier.Notify`; the precedence gate is single-sourced in `deliver.go` (no duplicated `PreferOriginChannel`/route-precedence logic).
    - BEHAVIOR assertion: a NULL-identity legacy row still routes via `Notifier.Notify` (regression guard — verified by an integration/unit case, not the agent reply).
    - `go test -race ./internal/cron/ -count=1` (unit tier) still green; `golangci-lint run ./internal/cron/...` 0 issues.
  </acceptance_criteria>
  <done>`sweepNotifications` routes swept rows back to origin keyed on the row identity under the shared gate; the db_integration migration+round-trip test runs (not skipped) and is green; legacy NULL rows regress to the route.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: LIVE Step 2 — quiet-hours-deferred sweep routes back to the origin Telegram chat (R6 hard gate)</name>
  <files>(none — live verification of the wired Step-2 sweep path; no code change)</files>
  <read_first>
    - 20-VALIDATION.md §"Manual-Only Verifications" (Step 2 row — force quiet-hours to cover now, schedule an agent_job, sweep after window)
    - 20-RESEARCH.md §"Ground-Truth Assertions for the LIVE steps" (Step 2 — assert the DESTINATION + the pending_notifications row's identity_id set + status→delivered; never r.Reply)
    - MEMORY `reference_cdp_telegram_live_test_harness` + `reference_run_aura_binary_live_env_loading` + the 19-11 quiet-hours forcing pattern (19-11-PLAN.md:178-191)
    - internal/config (the `AURA_SCHEDULER_QUIET_HOURS` env to force the window)
  </read_first>
  <what-built>
    The Step-2 deferred/failed path: migration 0014 stores the `identity_id` snapshot on `pending_notifications` (Task 1), the Insert/Sweep queries + projection thread it (Task 1), and `sweepNotifications` routes a swept row back to the origin channel via `deliverToOrigin` keyed on the row identity (Task 2). Unlike reminders (which bypass quiet-hours), an agent_job notification deferred by quiet-hours is queued and later swept — this gate proves it routes back to Telegram, not the default route.
  </what-built>
  <how-to-verify>
    1. Bring up the stack + `aura serve` with Telegram enabled and an onboarded account (same setup as 20-03 Task 3). Leave `AURA_SCHEDULER_PREFER_ORIGIN_CHANNEL` UNSET (default on).
    2. FORCE the quiet-hours window to cover "now" via `AURA_SCHEDULER_QUIET_HOURS` (the 19-11 forcing pattern) so a non-reminder notification is DEFERRED into `pending_notifications` rather than sent immediately.
    3. From the Telegram DM, schedule an agent_job whose completion emits a notification (or trigger the agent_job dispatch so it produces a deferred notification). Confirm a `pending_notifications` row is created.
    4. DB ground truth (during deferral): `SELECT identity_id, status FROM aura.pending_notifications ORDER BY id DESC LIMIT 1;` → assert `identity_id` is set to the chat's identity (the snapshot), `status` is the deferred/pending state.
    5. Let the sweep run after the (forced) window ends. Assert (CDP-observed) the notification text lands in the SAME origin Telegram chat — NOT stdout/whatsapp. NEVER assert on the agent reply.
    6. DB ground truth (after sweep): the same `pending_notifications` row's `status` transitions to `delivered`.
  </how-to-verify>
  <action>HUMAN-VERIFY GATE (no autonomous code change): execute the `how-to-verify` recipe against the live stack + CDP harness with the quiet-hours window forced to cover "now", then assert the DESTINATION + the pending_notifications DB ground truth in `acceptance_criteria`. This is a D-04 hard phase-close gate — it cannot be auto-approved (no-skip-as-green); the sweep must actually fire. Tasks 1 + 2 must be green first.</action>
  <acceptance_criteria>
    - DESTINATION assertion (hard gate, D-04): the deferred agent_job notification is observed (CDP) landing in the SAME origin Telegram chat after the forced quiet-hours window — not stdout/whatsapp.
    - DB assertion: the `pending_notifications` row carries `identity_id` = the chat's identity and `status` transitions to `delivered` after the sweep.
    - NO assertion on `r.Reply` / agent confirmation text (ground-truth discipline). No-skip-as-green: the sweep actually fires (not a skipped/forced-pass path).
  </acceptance_criteria>
  <verify>
    <human-check>Deferred agent_job notification observed in the origin Telegram chat (CDP) after the forced quiet-hours window + `SELECT identity_id, status FROM aura.pending_notifications ORDER BY id DESC LIMIT 1` shows identity_id = chat identity and status=delivered.</human-check>
  </verify>
  <done>The quiet-hours-deferred agent_job notification is confirmed routing back to the origin Telegram chat after the sweep and the pending_notifications row shows identity_id set + status=delivered (R6 hard gate signed off).</done>
  <resume-signal>Type "approved" once the deferred notification is observed in the origin Telegram chat after the sweep AND the pending_notifications row shows identity_id set + status=delivered; otherwise describe the route taken + DB values.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pending_notifications row → sweep delivery | the durable row's `identity_id` snapshot decides who receives the swept notification; a stale/orphaned identity must NOT leak to another user |
| migration 0014 → existing data | the new column on a live table; legacy rows must degrade safely (NULL → route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | Information Disclosure / misroute | swept-row delivery keyed on `pending_notifications.identity_id` | mitigate | The sweep routes via the SAME `deliverToOrigin` gate keyed on the row's snapshot identity; an un-owned/NULL identity → not-my-user → route fallback (never a stray channel). The db_integration round-trip pins the identity carried, and the live gate asserts the DESTINATION chat. |
| T-20-12 | Tampering / orphaned identity | `identity_id text` with NO FK (Fork 1) | mitigate | Deliberately FK-free so a deleted origin conversation does not NULL the delivery key; an orphaned/stale identity that no longer maps to an account resolves to `pgx.ErrNoRows` at `GetAccountByIdentity` (20-01) → not-my-user → route fallback. It cannot silently re-target another user (the lookup is exact-match on the uuid). |
| T-20-13 | Availability / data regression | migration 0014 on a live `pending_notifications` table | mitigate | Additive nullable column, no backfill, existing aura_app DML grant covers it; legacy rows (NULL) route exactly as today. Up/down migration is proven clean by the db_integration test (no-skip-as-green) before phase close. |
| T-20-14 | Tampering / double-delivery on sweep | owns-but-failed during a sweep | mitigate | A failed channel send during the sweep keeps the row for the next sweep (same-channel retry) and does NOT cross-channel fall back (Pitfall 3) — the single-sourced `deliverToOrigin` gate enforces it for both the live-task and swept-row paths. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero new deps; `sqlc generate` regenerates an existing-tool artifact (20-RESEARCH §Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `go test -race -tags db_integration -run TestDispatch ./internal/cron -count=1` (env exported) — migration 0014 up/down + identity round-trip green, NOT sub-second (actually ran).
- `go test -race ./internal/cron/ -count=1` — unit tier (incl. sweep route-back) still green.
- `go build ./...` — module compiles with the regenerated sqlc.
- `golangci-lint run ./internal/cron/... ./internal/db/...` — 0 issues.
- LIVE Step 2 hard gate (Task 3) signed off — deferred notification observed in the origin Telegram chat + DB row (identity_id set, status=delivered).
- All touched files ≤600 LOC.
</verification>

<success_criteria>
- Migration 0014 (`identity_id text`, no FK) applies up + reverts down cleanly; `pending_notifications` carries `identity_id` (R6 acceptance).
- Insert/Sweep + projection thread the snapshot; `sweepNotifications` routes swept rows back to origin via the shared `deliverToOrigin` gate; legacy NULL rows regress to the route.
- db_integration migration+round-trip test runs (not skipped) and is green.
- R6 LIVE Step 2 hard gate signed off: a quiet-hours-deferred agent_job notification, swept after the forced window, lands in the origin Telegram chat + DB shows identity_id set + status=delivered.
</success_criteria>

<output>
Create `.planning/phases/20-scheduler-hardening-full-implementation/20-04-SUMMARY.md` when done. Record: the exact `$N` placeholder renumbering done on InsertPendingNotification, the regenerated sqlc field name, how `deliverToOrigin` was made to serve both the live-task and swept-row paths (single-sourced gate), and the Step-2 live sign-off evidence. Note the deferred PRD env-catalog housekeeping (`AURA_SCHEDULER_PREFER_ORIGIN_CHANNEL`) for phase close.
</output>
