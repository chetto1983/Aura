---
phase: 18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .planning/REQUIREMENTS.md
  - internal/toolinvocations/store.go
  - internal/toolinvocations/store_test.go
  - internal/db/migrations/0011_tool_invocations.up.sql
  - internal/db/migrations/0011_tool_invocations.down.sql
  - internal/db/queries/tool_invocations.sql
  - internal/db/sqlc/tool_invocations.sql.go
  - internal/runner/runner_persist.go
  - internal/runner/runner_toolinvocations_test.go
  - cmd/aura/shell.go
  - cmd/aura/shell_test.go
  - docs/phase-18-xlsx-call-breakdown.md
autonomous: false
requirements: [CAP-08.1]
user_setup:
  - service: openrouter
    why: "D-03 characterization probe runs ONE live xlsx E2E against DeepSeek-V4 to ground the call-budget acceptance before code lands"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard -> Keys (already in .env per project convention)"

must_haves:
  truths:
    - "PRD §Slice 7e-core, §Slice 7 amendment #52/D-41 (~2190), and the commit template are amended so snippet execution posture is HOST-PRIMARY (D-01): the line ~2190 follow-up is marked RESOLVED, not 'INVARIATA per ora'"
    - "REQUIREMENTS.md carries a new CAP-08.1 row (D-04); CAP-08 is reconciled as completed-by-Phase-11 (no longer bare Pending)"
    - "The aura.tool_invocations ledger (migration 0011 + store + sqlc + runner wiring + cmd/aura/shell.go) is committed; go build/test green for internal/toolinvocations and internal/runner, including the db_integration ledger round-trip under the composed DSN"
    - "A live xlsx E2E run's per-call breakdown is dumped from the ledger to docs/phase-18-xlsx-call-breakdown.md, grounding the provisional ~5-call/<40s target (D-03)"
  artifacts:
    - path: "prd.md"
      provides: "Host-primary snippet posture amendment + ~2190 follow-up RESOLVED + CAP-08.1 wording"
      contains: "RESOLVED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CAP-08.1 requirement row + CAP-08 reconciliation"
      contains: "CAP-08.1"
    - path: "internal/toolinvocations/store.go"
      provides: "Append-only tool ledger store (ListByConversation is the gate's read seam)"
      contains: "ListByConversation"
    - path: "internal/db/migrations/0011_tool_invocations.up.sql"
      provides: "Append-only aura.tool_invocations table with BEFORE UPDATE/DELETE/TRUNCATE triggers + aura_app SELECT/INSERT grant"
      contains: "tool_invocations"
    - path: "docs/phase-18-xlsx-call-breakdown.md"
      provides: "Empirical per-call breakdown of one live xlsx run from the ledger (D-03 grounding)"
  key_links:
    - from: "cmd/aura/chat.go"
      to: "internal/toolinvocations"
      via: "toolinvocations.New(pool) injected into runner.Deps.ToolInvocations"
      pattern: "toolinvocations\\.New"
    - from: "internal/runner/runner_persist.go"
      to: "internal/toolinvocations"
      via: "persistToolInvocation writes one Event per ToolInvocation start/end"
      pattern: "toolInvocations\\.Insert"
---

<objective>
Wave-0 gate for Phase 18: do the PRD-first paperwork and lock the measurement substrate BEFORE any posture/wiring code lands.

This plan (1) amends prd.md to flip snippet execution posture to HOST-PRIMARY per D-01 (superseding the "INVARIATA per ora" follow-up at line ~2190 and the §Slice 7e-core "eseguiti by-path via sandbox_exec" wording), (2) adds the new CAP-08.1 requirement and reconciles CAP-08 (D-04), (3) commits the in-flight `internal/toolinvocations` ledger + migration 0011 + runner wiring + `cmd/aura/shell.go` exactly as already authored on disk, and (4) runs ONE live xlsx E2E and dumps the per-call ledger breakdown to ground the provisional ~5-call/<40s target (D-03).

Purpose: PRD-first principle (CLAUDE.md) — the amendment commit comes first; and the call-budget acceptance the downstream E2E gate (18-04) asserts must be grounded in a real ledger run, not a registration-commit estimate.
Output: amended prd.md + REQUIREMENTS.md; committed ledger substrate; docs/phase-18-xlsx-call-breakdown.md with the real per-call sequence.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-CONTEXT.md
@.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-RESEARCH.md
@.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-PATTERNS.md
@.claude/skills/spike-findings-Aura/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PRD amendment (host-primary snippet posture, D-01) + CAP-08.1 in REQUIREMENTS.md (D-04)</name>
  <files>prd.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - prd.md lines ~2116 (amendment #48), ~2125 (7e-core split), ~2153 (snippet lifecycle action=use/restore/archive), ~2186-2195 (amendment #52/D-41 host pivot + line ~2190 follow-up), ~2389-2397 (§Slice 7e-core Idea/Scope) — these are the exact passages this amendment supersedes
    - .planning/REQUIREMENTS.md lines 39 (CAP-08), 122-123 (CAP-07/CAP-08 traceability rows), 136-141 (Coverage counts)
    - .planning/phases/18-.../18-CONTEXT.md (D-01, D-04 verbatim)
    - prd.md amendment header style (the "▶ Amendment #N (D-XX, ...)" blockquote convention used at lines 2168, 2186) — the new amendment MUST follow that exact format with a new sequential number
  </read_first>
  <action>
    Add a new PRD amendment blockquote (next sequential number after #53 — use #54) in the §Slice 7 / §Slice 7e-core amendment region (near ~2186-2195), following the existing "▶ Amendment #N (D-XX, driver, date)" format. Content per D-01:
    (a) Snippet execution posture is now HOST-PRIMARY: approved (saved-then-activated) snippets run via host `shell_exec` by-path (`<interpreter> <host-export-path>`), mirroring instruction skills which already run on the host (#50/D-15c). They are vetted artifacts at save time, not fresh model code. `sandbox_exec` REMAINS shipped as the deliberate per-run escalation for untrusted/model-generated code (the §Slice 7 amendment #52/D-41 point 2 already says this for the loop).
    (b) Explicitly mark the line ~2190 follow-up RESOLVED: change/annotate the "Gli snippet Slice 7e mantengono INVARIATA la loro esecuzione by-path nel sandbox per ora; la loro postura è ri-valutata in una slice successiva (follow-up aperto, NON toccare il codice snippet in questo giro)" so it reads RESOLVED-by-Phase-18-host-primary (D-01). Use the literal token "RESOLVED" so the must-have grep passes.
    (c) Supersede the §Slice 7e-core "Idea" wording at ~2389 ("eseguiti by-path via il shipped sandbox_exec ... python3 /skills/<name>.py") and the ~2125 7e-core bullet ("eseguibili by-path via the shipped sandbox_exec") so they describe host shell_exec by-path against AURA_SKILL_EXPORT_DIR as the primary path, sandbox as escalation. Mark the superseded text inline (the project convention uses `[SUPERATO #54]` markers, mirroring the `[SUPERATO #52]` markers already in the file).
    (d) Confirm the Neo4j `:UserSnippet` mirror (~3538) stays DEFERRED with 7f — note it, do not change it.
    (e) Update the §Slice 7e-core commit template (in the Componenti/commit-template region ~2389-2600) to reference host-primary by-path execution.
    Then in REQUIREMENTS.md: add a new `- [ ] **CAP-08.1**:` row under the Capabilities section immediately after CAP-08 (line 39), wording: "Snippet reuse steady-state — model-facing in-loop snippet save (UNGATED, D-02) + execution-posture resolution (host shell_exec by-path, D-01) + restore/archive lifecycle handlers + measured steady-state artifact reuse <40s / ~5 LLM calls from the aura.tool_invocations ledger. [Slice 7e-core follow-up — Phase 18, amendment #54]". Reconcile CAP-08: change its traceability row (line 123) from bare "Pending" to "Complete (7e-core sandbox-posture shipped 11-07; host-posture follow-up = CAP-08.1 / Phase 18)" and add a new CAP-08.1 traceability row "| CAP-08.1 | Phase 18 — Slice 7e snippet reuse steady-state | In Progress |". Bump the Coverage counts (line 138: v1 requirements 28 -> 29; CAP count 9 -> 10) and update the "Last updated" footer line (145).
    Do NOT write any .go code in this task — PRD-first, doc-only, this is the amendment commit content.
  </action>
  <verify>
    <automated>grep -q "RESOLVED" prd.md && grep -q "CAP-08.1" .planning/REQUIREMENTS.md && grep -q "host" prd.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - prd.md contains a new amendment blockquote (#54) stating host-primary snippet posture per D-01, in the existing amendment format
    - The literal token "RESOLVED" appears in prd.md marking the ~2190 follow-up closed
    - The §Slice 7e-core Idea wording and the ~2125 bullet are annotated as superseded (`[SUPERATO #54]`) and describe host shell_exec by-path as primary, sandbox as escalation
    - REQUIREMENTS.md contains a `**CAP-08.1**` requirement row + a CAP-08.1 traceability row + a reconciled CAP-08 row; Coverage counts bumped to 29 total / 10 CAP
    - No .go files changed by this task (verified by `git diff --name-only` showing only prd.md + REQUIREMENTS.md)
  </acceptance_criteria>
  <done>prd.md amended host-primary with the ~2190 follow-up RESOLVED; REQUIREMENTS.md carries CAP-08.1 + reconciled CAP-08 + bumped coverage; no code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the in-flight tool_invocations ledger substrate as-authored (no re-authoring)</name>
  <files>internal/toolinvocations/store.go, internal/toolinvocations/store_test.go, internal/db/migrations/0011_tool_invocations.up.sql, internal/db/migrations/0011_tool_invocations.down.sql, internal/db/queries/tool_invocations.sql, internal/db/sqlc/tool_invocations.sql.go, internal/runner/runner_persist.go, internal/runner/runner_toolinvocations_test.go, cmd/aura/shell.go, cmd/aura/shell_test.go</files>
  <read_first>
    - internal/toolinvocations/store.go (the authored store — Insert + ListByConversation + Event shape, already on disk)
    - internal/toolinvocations/store_test.go (the authored unit + any db_integration test — check the build-tag header)
    - internal/db/migrations/0011_tool_invocations.up.sql + .down.sql (the authored migration — append-only triggers + aura_app SELECT/INSERT grant)
    - internal/runner/runner_persist.go (persistToolInvocation already wired; runner.go injects ToolInvocationStore at line 117)
    - internal/runner/runner_toolinvocations_test.go (the authored runner persist test — check whether it carries a db_integration tag + a ledger round-trip)
    - internal/runner/interfaces.go (ToolInvocationStore narrow seam, lines 72-77)
    - cmd/aura/chat.go lines 144,175 (toolinvocations.New(pool) already injected — confirms the substrate is integrated, just uncommitted)
    - cmd/aura/shell.go + cmd/aura/shell_test.go (the host-shell entry point, uncommitted ?? in git status)
    - The git status snapshot: these files are `M` (runner_persist.go) and `??` (the rest) — they are in-flight uncommitted work, NOT to be re-authored
  </read_first>
  <action>
    This task is a VERIFY-AND-COMMIT, not an author task. The measurement substrate is already authored on disk (the migration, store.go, the generated sqlc file, the queries file, the runner wiring, the runner test, and cmd/aura/shell.go + shell_test.go). Per RESEARCH §Wave 0 Gaps and the brief: do NOT re-author it. Steps:
    (1) Run `go build ./internal/toolinvocations/ ./internal/runner/ ./cmd/aura/` and `go vet ./...`.
        SCOPE BOUNDARY (load-bearing — resolves the "fix only compile breaks" vs "not re-author" tension): the ONLY in-task edits permitted are (i) a stale-sqlc regeneration via the project's sqlc generate target (check the Makefile for the sqlc target — never hand-edit the generated file) and (ii) trivial mechanical compile fixes that are NOT re-authoring (an import line, a renamed-symbol reference that drifted against a sibling package committed in parallel). ESCALATION RULE: if the in-flight code does NOT compile clean beyond a sqlc regen + those trivial mechanical fixes — i.e. it needs real logic/structure changes to build — the executor MUST STOP and report (do NOT re-author the package). Task 3 does NOT run until Task 2's build is green.
    (2) Determine whether store_test.go / runner_toolinvocations_test.go carry a db_integration ledger round-trip. If a db_integration variant exists, run it under the composed DSN per CLAUDE.md (db_integration tag) and confirm the append-only ledger round-trips: Insert a start fact + end fact, ListByConversation returns both correlated by conversation_id/request_id/tool_call_id. If NO db_integration round-trip test exists yet in the in-flight code, the executor MUST author the minimal one (store_test.go or runner_toolinvocations_test.go, build-tag db_integration, goleak per package discipline) BEFORE the Task-2 commit — this is the no-skip-as-green floor, and VALIDATION.md row 18-01-T2 maps to "unit+integration" (a unit-only pass under-asserts the ledger and is not acceptable). The minimal test = Insert(start) + Insert(end) for one (conversation_id, request_id, tool_call_id), then ListByConversation returns both correlated facts.
    (3) Stage EXACTLY these files (explicit `git add` of each path — never `git add -A`; per memory gsd-commit-files-sweeps-staged-work the tree is dirty with parallel work). Confirm via `git diff --cached --name-only` that ONLY the ledger substrate + shell.go/shell_test.go are staged.
    The 0011 slot is the tool_invocations ledger (NOT 0011_snippet_runs — that table was deliberately skipped, D-19/A4). No new migration for snippets in this phase.
  </action>
  <verify>
    <automated>go build ./internal/toolinvocations/ ./internal/runner/ ./cmd/aura/ && go test ./internal/toolinvocations/ && go test -tags db_integration ./internal/toolinvocations/ ./internal/runner/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/toolinvocations/ ./internal/runner/ ./cmd/aura/` exits 0
    - `go test ./internal/toolinvocations/` (unit) passes
    - `go test -tags db_integration ./internal/toolinvocations/ ./internal/runner/` passes under the composed DSN — the append-only ledger round-trip (Insert start+end -> ListByConversation returns both correlated by conversation_id/request_id/tool_call_id) actually RUNS (not skipped-as-green); if the round-trip test was missing in the in-flight code it is authored in this task before the commit
    - Build did not require re-authoring the package: only a sqlc regen and/or trivial mechanical compile fixes were applied; if more was needed the executor stopped and reported (the SUMMARY records which)
    - `git diff --cached --name-only` shows ONLY the ledger substrate + cmd/aura/shell.go + cmd/aura/shell_test.go (no parallel/unrelated files swept in)
    - No new migration numbered 0011_snippet_runs exists (forbidden — D-19/A4)
  </acceptance_criteria>
  <done>The authored tool_invocations ledger + runner wiring + cmd/aura/shell.go compile, the unit AND db_integration ledger round-trip are green, and the files are staged exactly (no sweep), with no re-authoring beyond a sqlc regen / trivial compile fixes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The PRD amendment + CAP-08.1 are written and the tool_invocations ledger is committed. This checkpoint runs the D-03 characterization probe: ONE live xlsx E2E (operator-gated, PAID — OPENROUTER_API_KEY) and dumps the per-call ledger breakdown to ground the ~5-call/<40s target the 18-04 gate will assert.</what-built>
  <read_first>
    - internal/eval/skills_cot_eval_test.go header (the operator invocation block, lines 46-66) — the exact env + command to run the live xlsx North-Star E2E
    - reference_e2e_full_matrix_invocation + reference_live_finalize_e2e_socat_bridge (auto-memory) — SearXNG bridge + .env shell-sourcing gotchas
    - internal/toolinvocations/store.go ListByConversation — the read seam the breakdown dump uses
  </read_first>
  <how-to-verify>
    1. Bring up SearXNG (web tools backend): `docker compose up -d searxng` (or the socat bridge per memory).
    2. Export the live env per the eval header: `set -a; . ./.env; set +a; export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"; export AURA_SKILLS_DIR=<active skills root>; export AURA_RUN_DIR=<inspectable scratch>; export SEARXNG_URL=http://127.0.0.1:18080/search`.
    3. Run the live xlsx E2E ONCE: `go test -tags cot_eval -run TestSkillsE2E -timeout 900s -v ./internal/eval/`. (This drives the real DeepSeek-V4 loop and lands a .xlsx in the workspace.)
    4. Query the ledger for that conversation and write the per-call sequence (tool_name, event_kind, duration_ms, distinct request_id count) to docs/phase-18-xlsx-call-breakdown.md. Use `internal/toolinvocations` ListByConversation OR a direct read-only SQL SELECT against aura.tool_invocations ordered by seq. The doc MUST record: total distinct request_ids (the LLM-roundtrip proxy), total wall-clock (max ended_at - min started_at), and the per-tool call counts — empirically confirming or revising the ~5-call/<40s target. Per CLAUDE.md docs live in docs/, never /tmp.
    5. OPEN the produced .xlsx visually (feedback_inspect_artifact_visually_not_just_pass_status) and confirm it carries today's data.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed distinct-request-id count + wall-clock from docs/phase-18-xlsx-call-breakdown.md, or describe what the probe revealed (the ~5-call target may need revision per D-03).</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates/commits the following NEW symbols and files (the plan-review source-grounding pass must treat these as newly-introduced, not drift):
- New PRD amendment #54 (host-primary snippet posture) in prd.md
- New requirement `CAP-08.1` in .planning/REQUIREMENTS.md (+ traceability rows)
- Committed (previously uncommitted) package `internal/toolinvocations` (Store, Event, Insert, ListByConversation, eventFromRow, toParams + the pgtype helpers)
- Committed migration `internal/db/migrations/0011_tool_invocations.{up,down}.sql` (table aura.tool_invocations, function aura.reject_tool_invocations_mutation, triggers tool_invocations_no_update_delete / tool_invocations_no_truncate)
- Committed `internal/db/queries/tool_invocations.sql` + generated `internal/db/sqlc/tool_invocations.sql.go` (InsertToolInvocation, ListToolInvocationsByConversation, AuraToolInvocations, InsertToolInvocationParams)
- Committed `cmd/aura/shell.go` + `cmd/aura/shell_test.go` (the host-shell entry)
- New doc `docs/phase-18-xlsx-call-breakdown.md` (the D-03 ledger characterization)
- `runner.ToolInvocationStore` seam + `runner_persist.go` persistToolInvocation (already authored; committed here)
- A db_integration ledger round-trip test in store_test.go or runner_toolinvocations_test.go (authored here only if absent in the in-flight code — the no-skip-as-green floor for 18-01-T2)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model/agent → aura.tool_invocations | Tool-dispatch facts cross into a durable Postgres ledger; the agent never writes it directly (the Runner does, from Events) |
| operator → live LLM (OpenRouter) | The D-03 probe sends a paid request; OPENROUTER_API_KEY is the secret that must never leak into committed artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-R | Repudiation | aura.tool_invocations | mitigate | Migration 0011 BEFORE UPDATE/DELETE/TRUNCATE triggers + aura_app SELECT/INSERT-only grant make the ledger append-only — forensics cannot be hidden (the gate's ground truth is tamper-evident) |
| T-18-02-I | Information Disclosure | docs/phase-18-xlsx-call-breakdown.md, eval report | mitigate | The probe dump records tool_name/duration/request_id counts ONLY — never args_raw containing secrets; the eval harness already asserts no OPENROUTER_API_KEY value appears in output (skills_cot_eval_test.go SECRET LEAK check) |
| T-18-03-T | Tampering | generated sqlc file | accept | internal/db/sqlc/tool_invocations.sql.go is generated; hand-edits are forbidden — regenerate via the sqlc target, never patch (low risk: single-developer, generated-from-query) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — internal-substrate only (RESEARCH Package Legitimacy Audit: none). No slopcheck needed |
</threat_model>

<verification>
- `grep -q RESOLVED prd.md && grep -q CAP-08.1 .planning/REQUIREMENTS.md` — amendment + requirement present
- `go build ./... && go vet ./...` — the committed ledger substrate compiles module-wide
- `go test ./internal/toolinvocations/ ./internal/runner/` — ledger + runner persistence green (unit)
- `go test -tags db_integration ./internal/toolinvocations/ ./internal/runner/` — the append-only ledger round-trip runs under the composed DSN (no-skip-as-green)
- docs/phase-18-xlsx-call-breakdown.md exists with the live per-call breakdown (D-03 grounding)
- `git diff --cached --name-only` after staging shows only the intended files (no parallel-work sweep)
</verification>

<success_criteria>
- PRD-first: prd.md amended host-primary (D-01), ~2190 follow-up RESOLVED, BEFORE any posture code
- CAP-08.1 added + CAP-08 reconciled (D-04); coverage counts updated
- The tool_invocations ledger is committed exactly as authored, compiling and testing green (unit + db_integration round-trip)
- The ~5-call/<40s target is grounded by one live ledger run (D-03), documented in docs/
</success_criteria>

<output>
Create `.planning/phases/18-slice-7e-executable-snippet-reuse-steady-state-artifact-runs/18-01-SUMMARY.md` when done
</output>
</content>
