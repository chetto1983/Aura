---
phase: 14-onboarding-agent-md
plan: 5
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04]
files_modified:
  - scripts/cache_invariant_audit.sh
  - docs/aura-quality-snapshot.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/14-onboarding-agent-md/14-VALIDATION.md
  - internal/channels/telegram/profile_onboarding_test.go
  - cmd/aura/profile_test.go
autonomous: false
requirements: [UX-05]

must_haves:
  truths:
    - "Phase 14 is not complete until the real operator flow proves Telegram onboarding, profile CLI, and cache audit together"
    - "scripts/cache_invariant_audit.sh is the no-skip-as-green cache gate for messages[0]/messages[1]"
    - "docs/aura-quality-snapshot.md records live Phase 14 evidence after sign-off"
    - "ROADMAP and REQUIREMENTS stay Pending until live/manual evidence is recorded"
    - "Operator can update profile with aura profile add-fact and see updated content injected next turn"
  artifacts:
    - path: "docs/aura-quality-snapshot.md"
      provides: "Phase 14 live/operator evidence"
      contains: "Phase 14"
    - path: ".planning/phases/14-onboarding-agent-md/14-VALIDATION.md"
      provides: "Nyquist validation status for UX-05"
      contains: "nyquist_compliant"
  key_links:
    - from: "scripts/cache_invariant_audit.sh"
      to: "cmd/aura/cache_audit.go"
      via: "runtime cache invariant gate"
      pattern: "messages1"
---

<objective>
Close Phase 14 with integrated verification: profile store, CLI update, prompt
injection, cache audit, and real Telegram onboarding evidence. This plan is the
blocking sign-off layer that converts the built pieces into a validated UX-05
deliverable.
</objective>

<context>
@.planning/phases/14-onboarding-agent-md/14-VALIDATION.md
@.planning/phases/14-onboarding-agent-md/14-RESEARCH.md
@docs/aura-quality-snapshot.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run integrated automated verification and plug any test gaps</name>
  <files>cmd/aura/profile_test.go, internal/channels/telegram/profile_onboarding_test.go, scripts/cache_invariant_audit.sh</files>
  <read_first>
    - .planning/phases/14-onboarding-agent-md/14-VALIDATION.md
    - scripts/cache_invariant_audit.sh
    - cmd/aura/cache_audit.go
  </read_first>
  <behavior>
    - Run the Phase 14 quick command and add missing tests rather than accepting gaps.
    - Cache audit must assert messages[0] stable and messages[1] profile/skills stable.
    - Profile CLI tests must use temp dirs and avoid operator profile files.
  </behavior>
  <action>Execute the automated suite, fill narrow gaps found by the run, and update 14-VALIDATION.md statuses for automated rows that pass.</action>
  <verify>
    <automated>go test ./internal/profile/ ./internal/onboarding/ ./internal/conversations/ ./internal/runner/ ./internal/channels/telegram/ ./cmd/aura/ -count=1 && scripts/cache_invariant_audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - Automated rows in 14-VALIDATION.md are PASS or have explicit remaining manual-only status.
    - `scripts/cache_invariant_audit.sh` passes.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: live Telegram profile onboarding sign-off</name>
  <read_first>
    - internal/channels/telegram/profile_onboarding.go
    - docs/aura-quality-snapshot.md
  </read_first>
  <action>Run `aura serve` with Telegram enabled and a clean or reset `local` profile. The operator uses the real Telegram bot: activate setup if needed, send the first normal message or `/onboard`, answer the interview, test edit, confirm, then send a normal chat message. Verify Agent.md exists and normal chat resumes.</action>
  <how-to-verify>
    1. Set `AURA_PROFILE_DIR` to a controlled temp or operator-approved profile root.
    2. Start `aura serve` with Telegram configured.
    3. In Telegram, send `/onboard` or first normal message after setup.
    4. Answer prompts, use edit once, then confirm.
    5. Check `~/.aura/agents/local/Agent.md`, `preferences.json`, `metadata.json`, and `changelog.md`.
    6. Send a normal message and confirm it reaches the LLM turn.
  </how-to-verify>
  <resume-signal>Type "approved" when the real Telegram profile onboarding flow writes Agent.md and normal chat resumes, or describe the failure.</resume-signal>
  <acceptance_criteria>
    - Real Telegram profile onboarding is observed by the operator.
    - Confirm writes profile files.
    - Edit and skip have at least automated coverage; confirm path has live evidence.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: verify CLI profile update reaches the next prompt</name>
  <files>cmd/aura/profile_test.go, docs/aura-quality-snapshot.md</files>
  <read_first>
    - cmd/aura/profile.go
    - cmd/aura/cache_audit.go
  </read_first>
  <behavior>
    - Run `aura profile show --identity local` and `aura profile add-fact --identity local "I prefer Italian responses"`.
    - Inspect the next captured prompt or cache-audit fixture to confirm updated content is at messages[1].
    - Record evidence in docs and validation.
  </behavior>
  <action>Perform the CLI update check against a controlled profile dir; add a test if any part of the path is not already covered.</action>
  <verify>
    <automated>go test ./cmd/aura ./internal/runner -run 'Profile|CacheAudit' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `show` renders the updated fact.
    - Captured prompt has updated fact at `messages[1]`.
    - `messages[0]` hash remains unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: close documentation and requirement status</name>
  <files>docs/aura-quality-snapshot.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/14-onboarding-agent-md/14-VALIDATION.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 14
    - .planning/REQUIREMENTS.md UX-05
    - docs/aura-quality-snapshot.md
  </read_first>
  <behavior>
    - Update 14-VALIDATION.md to `nyquist_compliant: true` only after automated + live evidence.
    - Mark UX-05 complete only after all ROADMAP success criteria are true.
    - Add a concise docs/aura-quality-snapshot.md Phase 14 evidence section.
    - Add 14-05-SUMMARY.md and leave no stale "TBD" plan wording in ROADMAP.
  </behavior>
  <action>Update the docs and planning status after sign-off. Do not mark complete before live evidence exists.</action>
  <verify>
    <automated>git diff --check && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP Phase 14 success criteria are reflected as complete.
    - REQUIREMENTS UX-05 status is complete.
    - docs snapshot has Phase 14 evidence.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-14-05-SkipAsGreen | Process | validation | mitigate | cache audit and live Telegram sign-off are blocking |
| T-14-05-ProfileRootDamage | Reliability | live test | mitigate | use controlled `AURA_PROFILE_DIR` for live proof unless operator chooses real root |
| T-14-05-StaleDocs | Process | ROADMAP/REQ/docs | mitigate | docs/status updated only after evidence and checked with git diff |
</threat_model>

<verification>
- `go test ./internal/profile/ ./internal/onboarding/ ./internal/conversations/ ./internal/runner/ ./internal/channels/telegram/ ./cmd/aura/ -count=1`
- `scripts/cache_invariant_audit.sh`
- `go test -race ./internal/profile/ ./internal/onboarding/ ./internal/channels/telegram/ ./internal/runner/ ./cmd/aura/`
- `go test ./... -count=1`
- Live Telegram profile onboarding operator sign-off
</verification>

<success_criteria>
- All ROADMAP Phase 14 success criteria are true.
- UX-05 is marked complete only after live evidence.
- The profile/cache invariant remains enforced by automated tests and script.
</success_criteria>

<output>
Create `.planning/phases/14-onboarding-agent-md/14-05-SUMMARY.md` when done.
</output>
