---
phase: 14-onboarding-agent-md
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/profile/store.go
  - internal/profile/render.go
  - internal/profile/parser.go
  - internal/profile/atomic_windows.go
  - internal/profile/atomic_posix.go
  - internal/profile/store_test.go
  - internal/profile/render_test.go
  - internal/config/config.go
  - cmd/aura/profile.go
  - cmd/aura/profile_test.go
  - cmd/aura/main.go
autonomous: true
requirements: [UX-05]

must_haves:
  truths:
    - "Agent.md lives on the filesystem under AURA_PROFILE_DIR/<identity>/Agent.md; it is NOT stored in Neo4j or Postgres"
    - "Profile writes use same-directory temp files, file fsync, and platform-aware replace; Windows uses MoveFileEx with replace/write-through"
    - "Identity names are validated before path join; traversal and path separators are rejected"
    - "aura profile show --identity local renders Agent.md sections as a tree"
    - "aura profile add-fact rewrites Agent.md atomically and appends changelog.md"
  artifacts:
    - path: "internal/profile/store.go"
      provides: "Profile Store over Agent.md, preferences.json, metadata.json, changelog.md"
      contains: "type Store"
    - path: "cmd/aura/profile.go"
      provides: "aura profile show/add-fact CLI"
      contains: "runProfile"
  key_links:
    - from: ".planning/spikes/038-profile-store-atomic-contract/README.md"
      to: "internal/profile/atomic_windows.go"
      via: "Windows replace contract"
      pattern: "MoveFileEx"
---

<objective>
Build the Phase 14 filesystem profile substrate: per-identity Agent.md storage,
preferences/metadata/changelog files, atomic replacement, parsing/render helpers,
and the required `aura profile show` + `aura profile add-fact` CLI. This plan
creates the durable surface that later prompt injection and Telegram onboarding use.
</objective>

<context>
@.planning/phases/14-onboarding-agent-md/14-RESEARCH.md
@.planning/spikes/038-profile-store-atomic-contract/README.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: create internal/profile Store with atomic cross-platform writes</name>
  <files>internal/profile/store.go, internal/profile/atomic_windows.go, internal/profile/atomic_posix.go, internal/profile/store_test.go</files>
  <read_first>
    - .planning/spikes/038-profile-store-atomic-contract/main.go
    - internal/skills/writer.go
    - internal/skills/snippet.go
    - internal/identity/store.go
  </read_first>
  <behavior>
    - Store root defaults to `~/.aura/agents`, overridable by config in Task 3.
    - One identity directory contains exactly `Agent.md`, `preferences.json`, `metadata.json`, and `changelog.md`.
    - Writes are atomic per file: temp in same directory, write, file Sync, close, platform replace, best-effort dir Sync.
    - Windows replacement uses `MoveFileEx(..., MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH)`.
    - Identity validation rejects empty, traversal, `/`, `\`, and names outside `[A-Za-z0-9][A-Za-z0-9_.-]{0,63}` with no `..`.
  </behavior>
  <action>Implement the profile package Store and tests. Keep it DB-free and Neo4j-free. Return typed sentinel errors for missing profile and invalid identity so CLI/Telegram can render clean messages.</action>
  <verify>
    <automated>go test ./internal/profile/ -run 'Store|Atomic|Path' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Initial write + update + read round-trip passes.
    - Changelog preserves two entries across an update.
    - Path traversal cases fail before any file write.
    - No temp files remain after success or simulated failure.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: render and parse Agent.md sections</name>
  <files>internal/profile/render.go, internal/profile/parser.go, internal/profile/render_test.go</files>
  <read_first>
    - .planning/spikes/036-phase14-agentmd-source-truth/README.md
    - .planning/spikes/037-agentmd-messages1-cache-invariant/README.md
  </read_first>
  <behavior>
    - Render Agent.md with stable section order: Facts, Preferences, Context, Custom Instructions.
    - Preserve unknown markdown sections when adding facts where feasible.
    - Parse a simple section tree for CLI display.
    - Cap rendered profile bytes to a conservative limit used again by prompt injection.
  </behavior>
  <action>Add render/parse helpers and tests for empty profile, multi-section profile, add-fact idempotency, and bounded output.</action>
  <verify>
    <automated>go test ./internal/profile/ -run 'Render|Parse|Fact' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `AddFact` adds a bullet under Facts without duplicating an existing fact.
    - Parser returns a display tree with Preferences and Custom Instructions when present.
    - Rendered markdown remains byte-stable for identical input.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: add profile config knobs</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - prd.md lines around AURA_PROFILE_DIR and AURA_PROFILE_CERTAINTY_N
    - internal/config/config.go
  </read_first>
  <behavior>
    - Add `AURA_PROFILE_DIR` with default `~/.aura/agents`.
    - Add `AURA_PROFILE_CERTAINTY_N` with default `3` for later profile updater use.
    - Config parsing follows existing env fallback style; malformed integer should fail if consistent with nearby config knobs, otherwise preserve local config style.
  </behavior>
  <action>Extend config.Config and tests. Do not wire prompt injection yet; that is Plan 14-02.</action>
  <verify>
    <automated>go test ./internal/config/ -run 'Profile|Config' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Defaults match PRD.
    - Env overrides are test-covered.
    - Existing config tests remain green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: implement aura profile show/add-fact CLI</name>
  <files>cmd/aura/main.go, cmd/aura/profile.go, cmd/aura/profile_test.go</files>
  <read_first>
    - cmd/aura/identity.go
    - cmd/aura/config.go
    - cmd/aura/main.go
  </read_first>
  <behavior>
    - `aura profile show --identity local` reads the profile and prints a section tree.
    - Missing profile prints a clear non-stack-trace message and exits non-zero unless `--json` or explicit empty rendering is chosen.
    - `aura profile add-fact --identity local "I prefer Italian responses"` atomically rewrites Agent.md and appends changelog.md.
    - The implementation uses internal/profile.Store only; command code does no direct file writes.
  </behavior>
  <action>Add the `profile` top-level command with show/add-fact. Tests use a temp profile root and do not touch the operator profile.</action>
  <verify>
    <automated>go test ./cmd/aura/ -run Profile -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Show renders parsed sections.
    - Add-fact updates Agent.md and changelog.
    - Unsafe identity fails.
    - `go test ./internal/profile/ ./cmd/aura/ -count=1` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-14-01-PathTraversal | EoP | profile identity path | mitigate | strict identity validation before path join plus filepath.Rel containment check |
| T-14-01-CrashCorrupt | Integrity | atomic profile writes | mitigate | temp+fsync+platform replace; no partial direct writes |
| T-14-01-ProfileLeak | Privacy | CLI output | accept | user explicitly requests local profile display; no secrets expected in profile files |
| T-14-01-Neo4jDrift | Architecture | storage choice | mitigate | profile package is filesystem-only and imports no db/knowledge packages |
</threat_model>

<verification>
- `go test ./internal/profile/ ./internal/config/ ./cmd/aura/ -count=1`
- `go test -race ./internal/profile/ ./cmd/aura/ -run Profile`
- `go build ./...`
</verification>

<success_criteria>
- `~/.aura/agents/local/Agent.md` style profile files can be written, read, updated, and audited.
- `aura profile show --identity local` and `aura profile add-fact ...` exist and are tested.
- The profile store is ready for prompt injection and Telegram onboarding.
</success_criteria>

<output>
Create `.planning/phases/14-onboarding-agent-md/14-01-SUMMARY.md` when done.
</output>
