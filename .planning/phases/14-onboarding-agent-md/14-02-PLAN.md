---
phase: 14-onboarding-agent-md
plan: 2
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - internal/runner/runner.go
  - internal/runner/interfaces.go
  - internal/runner/profile_context_test.go
  - internal/conversations/context.go
  - internal/conversations/context_profile_test.go
  - cmd/aura/chat.go
  - cmd/aura/serve_channels.go
  - cmd/aura/cache_audit.go
  - cmd/aura/cache_test.go
  - cmd/aura/serve_adapters.go
autonomous: true
requirements: [UX-05]

must_haves:
  truths:
    - "Profile context is injected at messages[1] as RoleUser; Agent.md never appears in messages[0]"
    - "messages[1] composition is profile first, then always-on skills"
    - "A profile update changes messages[1] and leaves messages[0] byte-identical"
    - "The context ladder protects the combined profile/skills block through L1/L2/L2.5"
    - "cache-audit prints and validates profile-aware messages[1] hashes in the 20-turn replay"
  artifacts:
    - path: "internal/runner/runner.go"
      provides: "identity-aware context block provider wiring"
      contains: "ContextBlock"
    - path: "cmd/aura/cache_audit.go"
      provides: "runtime-faithful messages[0]/messages[1] cache audit"
      contains: "messages1"
  key_links:
    - from: ".planning/spikes/037-agentmd-messages1-cache-invariant/main.go"
      to: "internal/runner/profile_context_test.go"
      via: "20-turn cache invariant assertions"
      pattern: "PrefixHash"
---

<objective>
Wire the profile store into Aura's prompt prefix without mutating the system
prompt. The Runner must load the conversation identity, render a profile-first
context block, inject it through the existing protected `messages[1]` seam, and
extend cache audit coverage so profile drift cannot silently poison `messages[0]`.
</objective>

<context>
@.planning/phases/14-onboarding-agent-md/14-RESEARCH.md
@.planning/spikes/036-phase14-agentmd-source-truth/README.md
@.planning/spikes/037-agentmd-messages1-cache-invariant/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: make Runner context block identity-aware</name>
  <files>internal/runner/runner.go, internal/runner/interfaces.go, internal/runner/profile_context_test.go</files>
  <read_first>
    - internal/runner/runner.go
    - internal/runner/interfaces.go
    - internal/conversations/store.go
  </read_first>
  <behavior>
    - Add a context block provider that receives the conversation identity id or identity name.
    - Before `LoadManagedHistory`, Runner reads the conversation via `Conv.Get` and renders the block for that identity.
    - Preserve backwards compatibility for always-on skills by composing them after profile content.
    - Missing profile is not an error; it emits no profile section and still includes always-on skills when present.
  </behavior>
  <action>Replace or supplement the zero-arg `AlwaysBlock` with an identity-aware provider. Keep the context ladder field name if a rename would cause churn; a local helper can render profile+skills into the existing `AlwaysBlock` string.</action>
  <verify>
    <automated>go test ./internal/runner/ -run Profile -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Runner tests prove non-empty profile is at `messages[1]`.
    - Missing profile falls back to skills-only or empty block.
    - `messages[0]` hash is unchanged between no-profile and profile-present runs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: compose profile-first messages[1] block</name>
  <files>internal/profile/render.go, cmd/aura/chat.go, cmd/aura/serve_channels.go, cmd/aura/serve_adapters.go</files>
  <read_first>
    - cmd/aura/serve_adapters.go
    - cmd/aura/chat.go
    - internal/skills/messages.go
  </read_first>
  <behavior>
    - Composition order is profile block first, then always-on skill block.
    - Profile block has explicit stable markers, e.g. `<profile:Agent.md>` and `</profile:Agent.md>`.
    - Always-on skill block remains byte-stable and keeps its existing render helper.
    - The combined block stays bounded; over-limit profile renders a safe warning/truncated profile rather than breaking context.
  </behavior>
  <action>Add profile block renderer and wire chat/serve composition roots to pass profile store + skills provider into Runner. Keep `aura tools` and registry-only paths independent of profile IO.</action>
  <verify>
    <automated>go test ./internal/profile/ ./cmd/aura/ -run 'Profile|AlwaysBlock|Serve|Chat' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Profile marker appears before always-on skills marker.
    - No profile content is written to system prompt or tool descriptions.
    - Serve/chat boot paths can construct a Runner even when profile dir is absent.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: protect profile/skills block in context ladder tests</name>
  <files>internal/conversations/context.go, internal/conversations/context_profile_test.go</files>
  <read_first>
    - internal/conversations/context.go
    - internal/conversations/context_alwaysblock_test.go
  </read_first>
  <behavior>
    - Existing protected synthetic user-role turn remains the single `messages[1]` block.
    - L1 does not rewrite it.
    - L2.5 never drops it.
    - Tests name the block as profile/skills context even if the code field remains `AlwaysBlock`.
  </behavior>
  <action>Add focused regression tests around a profile-containing block under long-history trimming. Avoid broad refactors unless needed.</action>
  <verify>
    <automated>go test ./internal/conversations/ -run 'AlwaysBlock|Profile' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A 20+ turn reduced history still has system at `messages[0]` and profile block at `messages[1]`.
    - The profile marker is absent when the provider returns empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: extend cache-audit with profile-aware messages[1] assertions</name>
  <files>cmd/aura/cache_audit.go, cmd/aura/cache_test.go, scripts/cache_invariant_audit.sh</files>
  <read_first>
    - cmd/aura/cache_audit.go
    - scripts/cache_invariant_audit.sh
    - .planning/spikes/037-agentmd-messages1-cache-invariant/main.go
  </read_first>
  <behavior>
    - The audit fixture creates a deterministic profile for `local`.
    - Audit output asserts `messages[0]` stable and `messages[1]` stable for the fixed profile/skill set.
    - Negative test mutating profile mid-replay fails with a clear `messages[1]` drift message while `messages[0]` remains stable.
    - Script count assertions include the profile-aware messages1 stream; no skip-as-green.
  </behavior>
  <action>Adapt the hidden cache audit fixture setup and tests. Keep the public output format compatible: `request NN`, `messages1 NN`, `skillman NN` lines.</action>
  <verify>
    <automated>go test ./cmd/aura -run CacheAudit -count=1 && scripts/cache_invariant_audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/cache_invariant_audit.sh` passes with profile present.
    - Test proves Agent.md content is absent from `messages[0]`.
    - Test proves profile update changes `messages[1]` only.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-14-02-CachePoison | Integrity/cost | prompt prefix | mitigate | profile injected only at user-role messages[1]; cache audit asserts messages[0] hash stability |
| T-14-02-ProfileOversize | DoS | Agent.md render | mitigate | byte cap and explicit truncation/warning; tests cover bound |
| T-14-02-SkillDrift | Integrity | combined messages[1] | mitigate | profile-first deterministic composition and messages1 hash stream |
| T-14-02-IdentityMixup | Privacy | profile provider | mitigate | profile load by conversation identity, not global process state |
</threat_model>

<verification>
- `go test ./internal/profile/ ./internal/conversations/ ./internal/runner/ ./cmd/aura/ -run 'Profile|AlwaysBlock|CacheAudit' -count=1`
- `scripts/cache_invariant_audit.sh`
- `go build ./...`
</verification>

<success_criteria>
- Operator can inspect a prompt with Agent.md at `messages[1]`, not `messages[0]`.
- Cache audit passes across a 20-turn replay.
- Profile updates invalidate only the profile context segment.
</success_criteria>

<output>
Create `.planning/phases/14-onboarding-agent-md/14-02-SUMMARY.md` when done.
</output>
