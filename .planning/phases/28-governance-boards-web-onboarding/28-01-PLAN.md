---
phase: 28-governance-boards-web-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/queries/agent_job_runs.sql
  - internal/db/sqlc/agent_job_runs.sql.go
  - internal/cron/store_runs.go
  - internal/identity/store.go
  - internal/identity/audit_store.go
  - internal/db/queries/identity_audit.sql
  - internal/db/sqlc/identity_audit.sql.go
  - internal/db/migrations/0021_identity_audit.up.sql
  - internal/db/migrations/0021_identity_audit.down.sql
  - internal/mcp/probe.go
  - cmd/aura/mcp_status.go
  - internal/skills/stage_reader.go
  - internal/agui/server.go
  - internal/cron/store_runs_test.go
  - internal/identity/audit_store_test.go
  - internal/skills/stage_reader_test.go
  - internal/mcp/probe_test.go
autonomous: true
requirements: [GOV-01, GOV-02, GOV-03, ONBD-01]
must_haves:
  truths:
    - "internal/cron.Store exposes ListRunsForTask(ctx, taskID, limit, offset) returning run rows newest-first (GOV-03 paginated run history)"
    - "internal/identity.Store exposes ListCapabilities(ctx, identityID) returning the identity's granted capability names (D-06 picker source)"
    - "A structured probeServer(ctx, name, server) returning {Name, OK, ToolCount, Detail, Err} exists, extracted from the mcpDoctorAll text-writer path (GOV-01 live probe)"
    - "A per-stage skills reader lists pending/ and archived/ skill metadata via os.ReadDir + parseFrontmatter, never mounting bodies into LLM context (GOV-02 tabs)"
    - "Migration 0021_identity_audit creates an append-only aura.identity_audit table (role grant + row trigger + statement trigger), modeled on 0010_skill_audit"
    - "internal/identity.InsertIdentityAuditTx appends exactly one immutable audit row inside a db.WithTx, and UPDATE/DELETE/TRUNCATE on the table are rejected at the DB"
    - "agui.Server has SetGovernanceProviders and SetOnboardingService injection seams kept OFF the NewServer constructor; an unwired Server answers the new routes 503"
    - "Migration 0021 applies cleanly via the project golang-migrate flow against a live DB (build/type checks passing without the migration applied is NOT sufficient)"
  artifacts:
    - path: "internal/db/migrations/0021_identity_audit.up.sql"
      provides: "Immutable aura.identity_audit table"
      contains: "reject_identity_audit_mutation"
    - path: "internal/identity/audit_store.go"
      provides: "Append-only identity-audit store with InsertIdentityAuditTx + ErrAuditImmutable"
      exports: ["InsertIdentityAuditTx", "ErrAuditImmutable"]
    - path: "internal/cron/store_runs.go"
      provides: "ListRunsForTask paginated wrapper"
      contains: "ListRunsForTask"
    - path: "internal/mcp/probe.go"
      provides: "Structured per-server live probe"
      contains: "func probeServer"
    - path: "internal/skills/stage_reader.go"
      provides: "Per-stage (pending/archived) skills metadata reader"
  key_links:
    - from: "internal/identity/audit_store.go"
      to: "internal/db/sqlc (InsertIdentityAudit)"
      via: "tx-bound Queries inside db.WithTx"
      pattern: "InsertIdentityAudit"
    - from: "internal/cron/store_runs.go"
      to: "aura.agent_job_runs"
      via: "ListRunsForTask sqlc query"
      pattern: "ListRunsForTask"
  prohibitions:
    - statement: "MUST NOT make the per-stage skills reader mount, run, or inject any pending-skill body into an LLM context"
      status: resolved
      verification: test
    - statement: "MUST NOT create a loginable identity audit path that permits UPDATE/DELETE/TRUNCATE on aura.identity_audit (append-only enforced at the DB)"
      status: resolved
      verification: test
---

<objective>
Wave-0 backend gap closures + DI seams that every downstream board/provisioning plan consumes. No UI, no new endpoints — pure store/query/migration/seam work so Waves 2-4 are thin adapters.

Purpose: RESEARCH identified four concrete backend gaps (no paginated run-history query, no ListCapabilities store wrapper, no structured MCP probe, no skills pending/archived partition) plus a new immutable identity-audit table (migration 0021) and the two agui injection seams. Closing them first keeps the board/wizard handlers thin and prevents a scavenger hunt in later waves.
Output: ListRunsForTask + ListCapabilities wrappers, the structured probeServer, the skills per-stage reader, migration 0021 + the identity audit store, and the SetGovernanceProviders/SetOnboardingService seams on *agui.Server.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-governance-boards-web-onboarding/28-RESEARCH.md
@.planning/phases/28-governance-boards-web-onboarding/28-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 0) creates the following NEW symbols (the source-grounding pass excludes these from drift verification):
- `internal/cron.Store.ListRunsForTask(ctx, taskID string, limit, offset int) ([]Run, error)` + sqlc query `ListRunsForTask :many`
- `internal/identity.Store.ListCapabilities(ctx, identityID string) ([]string, error)`
- `internal/identity` append-only audit store: `InsertIdentityAuditTx`, `NewIdentityAuditStore`, `IdentityAuditInsert`, `IdentityAuditRow`, `ErrAuditImmutable`, `ListIdentityAudit`
- migration `0021_identity_audit.{up,down}.sql` → table `aura.identity_audit` + function `aura.reject_identity_audit_mutation` + sqlc query `InsertIdentityAudit`/`ListIdentityAudit`
- `internal/mcp.probeServer(ctx, name string, server <serverType>) ProbeResult` + type `ProbeResult{Name, OK, ToolCount, Detail, Err}`
- `internal/skills` per-stage reader: `ListStage(stage)` / `StageSkill` (pending/archived metadata)
- `agui.Server.SetGovernanceProviders(...)` + `agui.Server.SetOnboardingService(...)` + the backing nil-checked struct fields
(Downstream consumers: Plan 02 governance handlers, Plan 05 onboarding/provision saga.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scheduler run-history query + identity capability/audit stores + migration 0021</name>
  <files>internal/db/queries/agent_job_runs.sql, internal/db/sqlc/agent_job_runs.sql.go, internal/cron/store_runs.go, internal/identity/store.go, internal/db/queries/identity_audit.sql, internal/db/sqlc/identity_audit.sql.go, internal/db/migrations/0021_identity_audit.up.sql, internal/db/migrations/0021_identity_audit.down.sql, internal/identity/audit_store.go, internal/cron/store_runs_test.go, internal/identity/audit_store_test.go</files>
  <read_first>
    - internal/db/queries/agent_job_runs.sql (existing :many queries — copy the projection + naming convention)
    - internal/cron/store.go (ListActiveTasks store.go:162 + the Run struct store.go:77-90 — mirror the projection/error-wrap shape)
    - internal/cron/store_runs.go (GetRun store_runs.go:60 — the row scan pattern)
    - internal/db/queries/capability_grants.sql (ListCapabilities :many query at line 11 — already exists; map to a wrapper)
    - internal/db/sqlc/capability_grants.sql.go (the generated ListCapabilities at line 58 returns []AuraCapabilityGrants over pgtype.UUID)
    - internal/identity/store.go (HasCapability store.go:128-138 + GrantCapability store.go:145-185 — the parse-UUID + wrap-error self-precedent)
    - internal/db/migrations/0010_skill_audit.up.sql (the append-only triple: function + row trigger + statement trigger + role grants — copy verbatim, rename)
    - internal/skills/audit_store.go (Store{pool,q} INSERT+SELECT-only shape, InsertAuditTx audit_store.go:146-158, ErrAuditImmutable + classifyAuditErr audit_store.go:55-63 — mirror for identity)
    - internal/db/tx.go (WithTx signature tx.go:22: func WithTx(ctx, pool, fn func(*sqlc.Queries) error) error)
  </read_first>
  <behavior>
    - ListRunsForTask: given N runs for a task, returns them ORDER BY started_at DESC, honoring limit/offset; limit<=0 falls back to a default (25) in the wrapper; mutates nothing.
    - ListCapabilities: returns the identity's capability strings (including `*` if present — the picker filtering of `*` is the handler's job, not the store's); empty slice for an identity with no grants; invalid UUID → wrapped error.
    - InsertIdentityAuditTx: appends exactly one row inside a db.WithTx; the row carries actor_identity_id, new_identity_id, new_identity_name, granted_capabilities[], authula_user_id.
    - Append-only: a direct UPDATE, DELETE, or TRUNCATE against aura.identity_audit raises insufficient_privilege (SQLSTATE 42501) → surfaced as ErrAuditImmutable.
  </behavior>
  <action>
    Add sqlc query `ListRunsForTask :many` to agent_job_runs.sql: `SELECT <same columns as GetRun> FROM aura.agent_job_runs WHERE task_id = $1 ORDER BY started_at DESC LIMIT $2 OFFSET $3;` Regenerate sqlc (run the project's sqlc generate step that produces internal/db/sqlc/*.sql.go). Add `func (s *Store) ListRunsForTask(ctx context.Context, taskID string, limit, offset int) ([]Run, error)` to internal/cron/store_runs.go mirroring GetRun's scan into the existing `Run` struct (store.go:77-90); default limit 25 when limit<=0; clamp offset>=0.
    Add `func (s *Store) ListCapabilities(ctx context.Context, identityID string) ([]string, error)` to internal/identity/store.go: parse the UUID (reuse the existing parse helper used by HasCapability), call the generated `q.ListCapabilities`, project `[]AuraCapabilityGrants` → `[]string` of the `Capability` field, wrap errors `fmt.Errorf("list capabilities %q: %w", ...)`. Do NOT filter `*` here.
    Create migration 0021_identity_audit.{up,down}.sql modeled verbatim on 0010_skill_audit (rename table → aura.identity_audit, function → aura.reject_identity_audit_mutation, triggers → identity_audit_no_update_delete / identity_audit_no_truncate). Columns per RESEARCH §Hard Problem 1: `id uuid PRIMARY KEY DEFAULT gen_random_uuid(), created_at timestamptz NOT NULL DEFAULT now(), actor_identity_id text NOT NULL, new_identity_id uuid NOT NULL, new_identity_name text NOT NULL, granted_capabilities text[] NOT NULL, authula_user_id text NOT NULL`. Grant `SELECT, INSERT ON aura.identity_audit TO aura_app` + `ALL TO aura_migrate`. Plain (non-CONCURRENT) index on (new_identity_id) and (created_at). NO coherence CHECK (that is skill-specific). The down migration drops triggers, function, table.
    Add sqlc query `InsertIdentityAudit :one` + `ListIdentityAudit :many` to a new internal/db/queries/identity_audit.sql; regenerate. Create internal/identity/audit_store.go mirroring internal/skills/audit_store.go: `IdentityAuditStore{pool,q}` + `NewIdentityAuditStore(pool)`, `InsertIdentityAuditTx(ctx, q *sqlc.Queries, in IdentityAuditInsert) error` (tx-bound so it commits inside the saga's db.WithTx), `ListIdentityAudit(ctx, filter)`, sentinel `ErrAuditImmutable` for SQLSTATE 42501 via a classify helper copied from skills/audit_store.go:55-63.
    Apply migration 0021 against the live DB via the project golang-migrate flow (the same path `make neo4j-migrate` / the db migrate command uses with AURA_DB_MIGRATE_URL) and confirm the schema exists — a build/type pass WITHOUT the applied migration is a false-green.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -tags db_integration ./internal/cron/ ./internal/identity/ -run 'TestListRunsForTask|TestIdentityAuditImmutable|TestListCapabilities'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cron/store_runs.go` contains `func (s *Store) ListRunsForTask` and returns rows ordered started_at DESC under a default limit
    - `internal/identity/store.go` contains `func (s *Store) ListCapabilities`
    - `internal/db/migrations/0021_identity_audit.up.sql` contains `reject_identity_audit_mutation`, a BEFORE UPDATE OR DELETE FOR EACH ROW trigger, and a BEFORE TRUNCATE FOR EACH STATEMENT trigger
    - `go test -tags db_integration ./internal/identity/ -run TestIdentityAuditImmutable` proves a row INSERTs and a subsequent UPDATE/DELETE returns ErrAuditImmutable (SQLSTATE 42501)
    - A live `db migrate` (golang-migrate) brings the schema to 0021 and `aura.identity_audit` is present (not just a compile pass)
  </acceptance_criteria>
  <done>ListRunsForTask + ListCapabilities + the immutable identity audit store + migration 0021 exist, are integration-tested green, and 0021 is applied live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured MCP probe extraction + skills per-stage reader + agui DI seams</name>
  <files>internal/mcp/probe.go, cmd/aura/mcp_status.go, internal/skills/stage_reader.go, internal/agui/server.go, internal/mcp/probe_test.go, internal/skills/stage_reader_test.go</files>
  <read_first>
    - cmd/aura/mcp_status.go (mcpDoctorAll mcp_status.go:53-123; writeRuntimeCheck mcp_status.go:93 exec.LookPath; writeRecipeChecks mcp_status.go:106; loadManagedMCPConfig mcp_status.go:21 — the refactor target)
    - internal/mcp/manager/status.go (SnapshotStatus status.go:40 + StatusSnapshot fields status.go:28-36 — the static row source the board uses unchanged)
    - internal/mcp/client.go and internal/mcp/http_client.go (the dial + tools/list path — a real mounted-tool-count needs Client.Open + a tools/list call, NOT just LookPath; L1 landmine)
    - internal/secret/envkey.go (IsSecretEnvKey envkey.go:69 — render env KEYS with a redacted chip; import path internal/secret)
    - internal/mcp/redact.go (RedactSecrets redact.go:6-15 — mask values before they reach a caller)
    - internal/skills/loader.go (scan loader.go:132, parseFrontmatter, the goroutine-free 1s TTL cache loader.go:17,113-127; Skill struct loader.go:26-34 — the per-stage reader reuses parseFrontmatter, NEVER the mount path loader.go:207-220)
    - internal/skills/writer.go (WriterConfig PendingDir/ActiveDir/ArchiveDir writer.go:33-54 — the default sibling layout <ActiveDir>/pending + /archived is the stage-dir contract)
    - internal/agui/server.go (Server struct fields server.go:88-97; NewServer server.go:104-106; SetGraphView server.go:122-127; the Mux() register block server.go:148-158 — add fields + setters, NOT register calls here; registration lands in Plan 02/05)
    - internal/agui/graph_api.go (the consumer-side narrow GraphView interface graph_api.go:57-60 — the seam shape to mirror for governance/onboarding provider interfaces)
  </read_first>
  <behavior>
    - probeServer(healthy stdio server): returns {OK:true, ToolCount:n} where n is the real tools/list count under a bounded context.
    - probeServer(hung/dead server, context times out): returns {OK:false, Err:<timeout>} for THAT server only; it never panics and never blocks beyond the supplied context deadline.
    - probeServer targets ONLY the server passed in (which the caller looks up in the loaded config) — there is no body-supplied URL/command path (prohibition #5 substrate).
    - ListStage("pending"): returns metadata for each skill under PendingDir (name, description, type, language, content hash where derivable) parsed via parseFrontmatter; NEVER returns or loads the body into any mount/manifest path.
    - ListStage("archived"): same over ArchiveDir.
  </behavior>
  <action>
    Extract `func probeServer(ctx context.Context, name string, server <the per-server config type from ManagedConfig>) ProbeResult` into a new internal/mcp/probe.go, returning `type ProbeResult struct { Name string; OK bool; ToolCount int; Detail string; Err error }`. Move the per-server reachability logic out of mcpDoctorAll's text-writer (writeRuntimeCheck/writeRecipeChecks) into this structured function; for the tool count, dial via internal/mcp Client.Open + a tools/list call (reuse the existing client timeout semantics from HARDEN-03; do NOT invent a new transport). The function takes the caller-supplied context so the handler can bound it with context.WithTimeout(r.Context(), 3*time.Second). Refactor mcpDoctorAll to call probeServer per server and render the existing text output from the ProbeResult (no behavior change to `aura mcp status`). Apply RedactSecrets to Detail/Err strings before returning.
    Create internal/skills/stage_reader.go: `func ListStage(pendingDir, archiveDir, stage string) ([]StageSkill, error)` that os.ReadDir the stage dir, parses each <name>/SKILL.md via the existing parseFrontmatter, and returns `StageSkill{Name, Description, Type, Language, ContentHash}` — reusing internal/skills/contenthash.go for the hash. It MUST NOT call the loader mount path (loader.go:207-220) — pending/archived bodies never enter LLM context by construction.
    Add to internal/agui/server.go: struct fields for the governance providers and onboarding service (nil by default), plus `func (s *Server) SetGovernanceProviders(...)` and `func (s *Server) SetOnboardingService(...)` mirroring SetGraphView's doc + off-constructor discipline (server.go:122-127). Declare the consumer-side narrow provider interfaces (e.g. governance MCP/Skills/Scheduler readers + the onboarding service) here or beside the future handlers; keep them off NewServer so existing callers/tests are unchanged. Do NOT add register calls to Mux() in this plan.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/mcp/ ./internal/skills/ ./internal/agui/ -run 'TestMCPProbe|TestStageReader|TestSetGovernance'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/mcp/probe.go` contains `func probeServer` returning a `ProbeResult` with Name/OK/ToolCount/Detail/Err
    - `go test ./internal/mcp/ -run TestMCPProbe` proves a hung server (context deadline) yields {OK:false} without panic and without exceeding the deadline
    - `internal/skills/stage_reader.go` lists pending/archived metadata and a test asserts NO loader-mount call path is reached for pending bodies
    - `internal/agui/server.go` contains `func (s *Server) SetGovernanceProviders` and `func (s *Server) SetOnboardingService`, both absent from the NewServer constructor signature
    - `aura mcp status` CLI output is unchanged (mcpDoctorAll renders from ProbeResult) — `go build ./cmd/aura` passes and the text path still compiles
  </acceptance_criteria>
  <done>The structured probe, the skills per-stage reader, and the two agui DI seams exist with tests; the CLI status path is refactored without behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server process ↔ aura (probe) | The probe dials/launches configured MCP servers — a privileged server-side action; never client-driven |
| aura_app DB role ↔ aura.identity_audit | The audit ledger must be append-only at the DB, not just in app code |
| skills pending/archived dir ↔ LLM context | Pending bodies must never cross into a mounted/injected context |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering / Repudiation | aura.identity_audit | mitigate | DB-enforced append-only (role grant SELECT/INSERT only + BEFORE UPDATE/DELETE row trigger + BEFORE TRUNCATE statement trigger), copied verbatim from the proven 0010_skill_audit pattern; integration test asserts ErrAuditImmutable |
| T-28-01-02 | Information disclosure | internal/mcp/probe.go | mitigate | RedactSecrets applied to Detail/Err before return; env values never placed in ProbeResult (only IsSecretEnvKey-flagged KEY names downstream) |
| T-28-01-03 | Elevation of privilege | internal/skills/stage_reader.go | mitigate | The per-stage reader uses os.ReadDir + parseFrontmatter only; it never invokes the loader mount path, so a pending skill body cannot enter an LLM context |
| T-28-01-04 | Tampering | internal/mcp/probe.go (probe target) | mitigate | probeServer takes a server value the caller looked up in the loaded config; there is no URL/command-from-request path (configured-servers-only substrate for prohibition #5) |
| T-28-01-SC | Tampering | sqlc regen / migration apply | mitigate | No new external package installs (RESEARCH §Environment: zero net-new deps); regen + live migrate are local-only; the migration is applied + asserted live (no false-green) |
</threat_model>

<verification>
- `go build ./...` + `go vet ./...` clean.
- `go test -tags db_integration ./internal/cron/ ./internal/identity/ ./internal/mcp/ ./internal/skills/ ./internal/agui/` green (stack up; composed DSNs per CLAUDE.md integration env).
- Migration 0021 applied live via golang-migrate; `aura.identity_audit` present in the schema.
- `go test -race ./internal/mcp/ ./internal/skills/` clean (goleak via TestMain).
</verification>

<success_criteria>
- The four RESEARCH-named backend gaps are closed (ListRunsForTask, ListCapabilities, structured probeServer, skills per-stage reader).
- Migration 0021 + the immutable identity audit store exist, are integration-tested, and the migration is applied live.
- SetGovernanceProviders + SetOnboardingService seams exist on *agui.Server, off the constructor, 503-until-wired.
- No same-wave file conflicts introduced; no new external dependency added.
</success_criteria>

<output>
Create `.planning/phases/28-governance-boards-web-onboarding/28-01-SUMMARY.md` when done.
</output>
