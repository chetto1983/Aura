---
phase: 28-governance-boards-web-onboarding
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - prd.md
  - .planning/ROADMAP.md
  - .planning/phases/30-telegram-onboarding-link-qr/30-SPEC.md
  - docs/cockpit-overhaul/05-authula-auth-SPEC.md
  - internal/webauth/authula.go
  - cmd/aura/serve_auth.go
  - internal/webauth/authula_multiuser_test.go
autonomous: true
requirements: [ONBD-01]
must_haves:
  truths:
    - "A PRD-amendment commit lands BEFORE any provisioning implementation (Plan 05), relaxing the single-operator boundary: Phase 28 introduces a 2nd web-loginable identity via the cockpit onboarding wizard (CLAUDE.md PRD-first; D-07)"
    - "The amendment states authz STAYS capability_grants-based — NO new RBAC, NO route-scoping beyond RequireCapability, NO OAuth; identity resolution generalizes 1:N via aura.identity_auth_links (already 1:N-ready, migration 0019)"
    - ".planning/PROJECT.md §Out of Scope single-operator bullet (line ~90) is relaxed to the multi-loginable-identity / capability_grants-only posture"
    - "prd.md carries a PRD-amendment block (#NN) recording the Phase-28 single-operator relaxation and that OperatorUserID's >1-user error is superseded by 1:N resolution through identity_auth_links"
    - "ROADMAP Phase-30 is marked absorbed-into-28 with a one-line pointer to 28-SPEC §ONBD-01b (both the line-56 bullet and the §Phase 30 detail section + the Progress-table row) via targeted edits — NOT a full-file ROADMAP rewrite (anti-pattern #15)"
    - ".planning/phases/30-*/30-SPEC.md is converted to a tombstone referencing 28-SPEC §ONBD-01b (traceability preserved)"
    - "webauth.OperatorUserID's >1-user case no longer breaks daemon boot or the live login path: enrollment of a 2nd user is permitted; the live session-validate path resolves identity exclusively via ResolveIdentityID (verified: no live request path depends on OperatorUserID)"
    - "An existing single-operator deployment is unaffected — the seeded local identity remains linked and loginable after the relaxation (no regression)"
  artifacts:
    - path: "prd.md"
      provides: "PRD-amendment block for the Phase-28 single-operator relaxation"
      contains: "Phase 28"
    - path: "internal/webauth/authula.go"
      provides: "Relaxed OperatorUserID multi-user handling (enrollment-time only; no live-path dependency)"
      contains: "OperatorUserID"
    - path: ".planning/phases/30-telegram-onboarding-link-qr/30-SPEC.md"
      provides: "Phase-30 tombstone → 28-SPEC §ONBD-01b"
      contains: "absorbed"
  key_links:
    - from: "cmd/aura/serve_auth.go"
      to: "webauth.IdentityLinker.ResolveIdentityID"
      via: "live validate path resolves identity 1:N, not via OperatorUserID"
      pattern: "ResolveIdentityID"
  prohibitions:
    - statement: "MUST NOT introduce new RBAC, route-scoping beyond RequireCapability, OAuth, or per-identity session isolation — authz stays capability_grants-only"
      status: resolved
      verification: judgment
---

<objective>
The BLOCKING PRD-amendment (D-07) + the Phase-30 absorption (D-09), plus the small enrollment-time relaxation of the OperatorUserID single-user guard. This commit MUST land before the provisioning implementation (Plan 05) — CLAUDE.md PRD-first principle: deviations require a PRD-amendment commit first.

Purpose: Phase 28 intentionally creates a 2nd web-loginable identity, which collides with PROJECT.md's single-operator mandate, the OperatorUserID >1-user guard, and the local-identity pin. The amendment records the relaxation (authz stays capability_grants-only), absorbs Phase 30, and makes the >1-user case non-fatal — all before any saga code exists.
Output: the amended PROJECT.md + prd.md + ROADMAP (targeted edits) + the 30-SPEC tombstone + the cockpit-overhaul OQ note, and the guarded OperatorUserID change, in one PRD-amendment commit.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-governance-boards-web-onboarding/28-CONTEXT.md
@.planning/phases/28-governance-boards-web-onboarding/28-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan modifies governance/PRD docs + one small code relaxation; the NEW/CHANGED artifacts:
- A PRD-amendment block in `prd.md` (numbered #NN) + the relaxed PROJECT.md §Out of Scope bullet
- ROADMAP Phase-30 absorption markers (the line-56 bullet + the §Phase 30 detail section + the Progress-table row) + the `30-SPEC.md` tombstone
- A behavioral relaxation in `internal/webauth/authula.go` (`OperatorUserID` >1-user case) + the matching comment update in `cmd/aura/serve_auth.go`
- The new capability name `identity.create` is INTRODUCED conceptually here (the gate name Plan 05 mounts); document it in the amendment as the create-mutation capability (parity with `agent.run`)
(No new endpoints or stores; this is the PRD-first gate for Plan 05.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: PRD-amendment — relax single-operator (D-07) + absorb Phase 30 (D-09)</name>
  <files>.planning/PROJECT.md, prd.md, .planning/ROADMAP.md, .planning/phases/30-telegram-onboarding-link-qr/30-SPEC.md, docs/cockpit-overhaul/05-authula-auth-SPEC.md</files>
  <read_first>
    - .planning/PROJECT.md (the §Out of Scope "Multi-user con auth/RBAC reale" bullet at line 90 — the exact text RESEARCH located; the §Requirements ONBD row)
    - prd.md (grep `single-user`/`multi-user` — the §Slice 1.7 + identity section around prd.md:806,875,877; the §Q&A revision protocol / amendment-block convention to follow for amendment #NN)
    - .planning/ROADMAP.md (the Phase-30 bullet at line 56; the `### Phase 30:` detail section at ~line 254; the Progress table Phase-30 row ~line 278 — the three Phase-30 touch points)
    - .planning/phases/30-telegram-onboarding-link-qr/30-SPEC.md (the spec to tombstone)
    - docs/cockpit-overhaul/05-authula-auth-SPEC.md (the OQ-4 / OQ-8 single-operator notes to mark partially-resolved)
    - .planning/phases/28-governance-boards-web-onboarding/28-RESEARCH.md §Hard Problem 2 (the EXACT amendment-target table — copy the prescribed wording)
  </read_first>
  <action>
    Edit .planning/PROJECT.md §Out of Scope line 90: relax the single-operator bullet to RESEARCH §Hard Problem 2's prescribed text — "v1.0.0 Phase 28 introduces a 2nd web-loginable identity via the cockpit onboarding wizard. Authz STAYS capability_grants-based — NO RBAC, NO route-scoping beyond RequireCapability, NO OAuth. Identity resolution generalizes 1:N via aura.identity_auth_links (already 1:N-ready, migration 0019)." Note the ONBD row now includes full provisioning of a loginable identity.
    Add a PRD-amendment block to prd.md (following the existing amendment-block convention, numbered as the next #NN): record that the single-operator boundary is relaxed for web-loginable identities created via the onboarding wizard; capability_grants remains the only authz model; OperatorUserID's >1-user error is superseded by 1:N resolution through identity_auth_links; the create mutation is gated by the new capability_grants name identity.create (parity with agent.run).
    ROADMAP edits PREFER gsd tooling for the structural changes (never a full-file ROADMAP Write — D-09 / anti-pattern #15): use the gsd phase/roadmap-status tooling (`/gsd-phase` / the roadmap-status mechanism `/gsd-transition` uses) for (a) the Phase-30 bullet at line 56 → an absorbed-into-28 marker + a one-line pointer "(Telegram link/QR delivered as ONBD-01b; see 28-SPEC §ONBD-01b)" and (c) the Progress-table Phase-30 row → status "Absorbed into 28", WHERE the tooling can express them. Fall back to a TARGETED `Edit` (never a full-file Write) ONLY for edits the gsd tooling cannot make — notably (b) the `### Phase 30:` detail section → replace the `[To be planned]` Goal with a one-line tombstone pointer to 28-SPEC §ONBD-01b and mark the section absorbed — and note in the commit/SUMMARY that this fell back to a targeted Edit because the roadmap tooling does not express a free-form section tombstone. Do NOT rewrite unrelated ROADMAP content; the rest of the file stays byte-unchanged outside the Phase-30 touch points.
    Convert .planning/phases/30-telegram-onboarding-link-qr/30-SPEC.md to a tombstone: a short note that Phase 30 is absorbed into Phase 28 (ONBD-01b) with a pointer to 28-SPEC §ONBD-01b, preserving the original requirement traceability line.
    In docs/cockpit-overhaul/05-authula-auth-SPEC.md, mark OQ-8 (multi-user) partially resolved for the capability_grants-only path (a one-line note pointing at this amendment).
  </action>
  <verify>
    <automated>grep -q "absorbed-into-28" .planning/ROADMAP.md && grep -q "Phase 28" prd.md && grep -riq "absorbed" .planning/phases/30-telegram-onboarding-link-qr/30-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` §Out of Scope bullet (line ~90) now states the 2nd-web-loginable-identity / capability_grants-only posture (no longer "niente login multi-tenant")
    - `prd.md` contains a numbered PRD-amendment block naming Phase 28, capability_grants-only, and the identity.create gate
    - `.planning/ROADMAP.md` contains an absorbed-into-28 marker on the Phase-30 bullet and the §Phase 30 detail section points to 28-SPEC §ONBD-01b (verified by grep; the rest of the ROADMAP is byte-unchanged outside the Phase-30 touch points)
    - `.planning/phases/30-telegram-onboarding-link-qr/30-SPEC.md` is a tombstone referencing 28-SPEC §ONBD-01b
    - No RBAC/route-scoping/OAuth language is introduced (authz stays capability_grants-only — judgment check)
  </acceptance_criteria>
  <done>The PRD-amendment (PROJECT.md + prd.md), the Phase-30 absorption (ROADMAP targeted edits + 30-SPEC tombstone), and the cockpit-overhaul OQ note are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Relax the OperatorUserID >1-user guard (enrollment-time only, no live-path dependency)</name>
  <files>internal/webauth/authula.go, cmd/aura/serve_auth.go, internal/webauth/authula_multiuser_test.go</files>
  <read_first>
    - internal/webauth/authula.go (OperatorUserID authula.go:205-230 — the switch returning an error for len(users) > 1; the comment at 205-209)
    - cmd/aura/serve_auth.go (the only production caller serve_auth.go:127-141 — it already handles uerr != nil gracefully by deferring the pin; the local link from first enrollment persists)
    - internal/webauth/identity_link.go (ResolveIdentityID identity_link.go:41-54 + LinkOperator identity_link.go:60-72 — the live validate path resolves identity 1:N from the link table, NOT via OperatorUserID)
    - internal/webauth/authula_integration_test.go (the existing UserService.Create/Delete cleanup + OperatorUserID test at lines 76-87 — confirm the multi-user expectation and extend, do not break)
    - internal/webauth/webauth_test.go (the nil-provider OperatorUserID test webauth_test.go:220-221 — keep passing)
  </read_first>
  <behavior>
    - With exactly one Authula user: OperatorUserID returns that id (unchanged).
    - With zero users: returns "" (unchanged — not yet enrolled).
    - With >1 user: the live login path is UNAFFECTED (it resolves via ResolveIdentityID over the link table); daemon boot does NOT fatal. The enrollment-time re-pin is skipped for the already-linked local identity rather than aborting; a created 2nd identity authenticates via its own identity_auth_links row.
    - A pre-existing single-operator deployment behaves identically (no regression).
  </behavior>
  <action>
    Make the >1-user case non-fatal for the live path. Per RESEARCH §Hard Problem 2 recommendation (a): keep OperatorUserID as an enrollment-only helper used solely to pin the FIRST user; ensure the live session-validate path uses ResolveIdentityID exclusively (verify by grepping that no live request handler calls OperatorUserID — only the serve_auth.go enrollment block does). The minimal change: confirm serve_auth.go:133's `else if uerr != nil` branch already tolerates the >1 case (it prints a deferred-lookup notice and proceeds — the existing local link persists), and update the OperatorUserID doc-comment (authula.go:205-209) + the serve_auth.go comment (serve_auth.go:127-132) to state the new posture (2nd loginable identity is intentional; multi-user resolution is via identity_auth_links). If the existing >1-user error path is too noisy or could mask a real misconfig, change the >1 case to return the seeded-operator id (the first/oldest user) with no error instead of erroring — but do NOT add RBAC or route-scoping. Add internal/webauth/authula_multiuser_test.go asserting: (1) with 2 enrolled users, boot/enrollment does not fatal and the local identity stays resolvable via ResolveIdentityID; (2) a single-operator deployment is unchanged.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -tags db_integration ./internal/webauth/ -run 'TestOperatorUserID|TestAuthulaMultiUser|TestIdentityLink'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/webauth/authula.go` OperatorUserID's >1-user case no longer aborts the live login path; the doc-comment reflects the multi-user-via-identity_auth_links posture
    - A grep confirms no live request handler (outside the serve_auth.go enrollment block) calls OperatorUserID
    - `go test -tags db_integration ./internal/webauth/ -run TestAuthulaMultiUser` proves 2 enrolled users do not break boot and the local identity resolves via ResolveIdentityID
    - The existing single-operator tests (webauth_test.go, authula_integration_test.go) still pass (no regression)
  </acceptance_criteria>
  <done>The OperatorUserID guard tolerates >1 user without breaking the live login path; multi-user resolution rides on identity_auth_links; tests prove no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authula user set ↔ identity resolution | Going from 1 to N users must not de-pin the existing operator nor allow ambiguous identity resolution |
| PRD/ROADMAP docs ↔ implementation gate | The amendment is the PRD-first gate; provisioning code (Plan 05) must not precede it |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Elevation of privilege | the relaxation scope | mitigate | The amendment explicitly KEEPS authz at capability_grants — no RBAC, no route-scoping, no OAuth; the create mutation is gated by identity.create (Plan 05). The relaxation is identity-resolution only, not an authz model change |
| T-28-04-02 | Spoofing / auth bypass | OperatorUserID >1-user | mitigate | Live login resolves via ResolveIdentityID over identity_auth_links (UNIQUE on authula_user_id, 1:N-ready); a 2nd user gets its own link row; the existing operator stays pinned (no de-pin). Tested for no-regression |
| T-28-04-03 | Repudiation | ROADMAP edit mechanism | mitigate | ROADMAP changed via targeted Edit on the located Phase-30 lines only (never a full-file Write — anti-pattern #15); the rest of the ROADMAP is byte-unchanged |
| T-28-04-SC | Tampering | dependency surface | accept | No package installs; docs + a guarded code relaxation only; zero net-new deps |
</threat_model>

<verification>
- `go build ./...` + `go vet ./...` clean.
- `go test -tags db_integration ./internal/webauth/` green (incl. the new multi-user test + the unchanged single-operator tests).
- grep confirms the ROADMAP Phase-30 absorption markers, the prd.md amendment block, and the 30-SPEC tombstone.
- Commit message is a PRD-amendment (imperative subject + why-body + Co-Authored-By), landing on master before Plan 05 begins.
</verification>

<success_criteria>
- The PRD-first gate is satisfied: PROJECT.md + prd.md relaxed, Phase 30 absorbed, before any provisioning code.
- The single-operator boundary relaxation is documented as capability_grants-only (no RBAC).
- OperatorUserID tolerates >1 user without breaking the live login path; no single-operator regression.
</success_criteria>

<output>
Create `.planning/phases/28-governance-boards-web-onboarding/28-04-SUMMARY.md` when done.
</output>
