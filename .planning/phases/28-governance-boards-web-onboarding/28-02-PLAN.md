---
phase: 28-governance-boards-web-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - internal/agui/governance_api.go
  - internal/agui/governance_api_test.go
  - internal/agui/server.go
  - cmd/aura/serve_webui.go
  - cmd/aura/serve.go
autonomous: true
requirements: [GOV-01, GOV-02, GOV-03]
must_haves:
  truths:
    - "GET /api/governance/mcp returns every configured MCP server (name, trust, runtime, startupState, authStatus, redacted env KEY chips, lastError), always rendering even with zero servers, with NO raw secret value in the response (GOV-01)"
    - "GET /api/governance/mcp/{name}/probe runs a bounded (3s) per-server live doctor + tool-count probe; a hung/dead server resolves to a timed-out/error result for ITS row only and never stalls the static list (GOV-01, R1 boundary)"
    - "The MCP probe targets ONLY a server found by {name} in the loaded config (404 if absent); it never accepts a URL/command from the request body (Prohibition #5)"
    - "MCP server rows render in a deterministic by-name order (R1 ordering)"
    - "GET /api/governance/skills?stage=active|pending|archived lists the correct lifecycle set with per-skill metadata, and pending rows carry NO run/activate field; a skill exactly at TTL appears in whichever tab the backend reports (no client-side TTL recompute) (GOV-02, R2 boundary)"
    - "GET /api/governance/skills/audit returns append-only ledger rows newest-first via AuditStore.List (GOV-02, R2)"
    - "A pending skill's body is never injected into any LLM context — the board reads pending metadata via the per-stage reader, never the loader mount path (GOV-02, Prohibition #1)"
    - "GET /api/governance/scheduler lists active tasks ordered by next fire; GET /api/governance/scheduler/{id}/runs returns paginated run history (default limit, a stable last page) and mutates nothing (GOV-03, R3)"
    - "A non-UUID scheduler {id} is a clean 404 before any store call (R3)"
    - "Every governance read endpoint returns 401 unauthenticated (inherited RequireAuth); read GETs are side-effect-free; an unwired provider returns 503; a backend failure mid-request returns a sanitized 502 with no stack/secret leak (cross-cutting R7)"
    - "An empty dataset renders a safe empty payload (200 + empty array), never a crash (R1/R3/R7 empty)"
  artifacts:
    - path: "internal/agui/governance_api.go"
      provides: "Six read-only governance REST handlers + narrow provider interfaces + register func"
      contains: "registerGovernanceRoutes"
      exports: ["registerGovernanceRoutes"]
    - path: "cmd/aura/serve_webui.go"
      provides: "Parent-mux mount of the 6 governance reads behind RequireAuth"
      contains: "/api/governance/mcp"
    - path: "cmd/aura/serve.go"
      provides: "Composition-root build of the governance providers + SetGovernanceProviders"
      contains: "SetGovernanceProviders"
  key_links:
    - from: "internal/agui/governance_api.go"
      to: "internal/mcp.probeServer + manager.SnapshotStatus"
      via: "per-row bounded probe + static snapshot"
      pattern: "probeServer|SnapshotStatus"
    - from: "internal/agui/governance_api.go"
      to: "internal/skills (Loader.List + ListStage + AuditStore.List)"
      via: "stage-partitioned reads"
      pattern: "ListStage|AuditStore"
    - from: "internal/agui/governance_api.go"
      to: "internal/cron.Store (ListActiveTasks + ListRunsForTask)"
      via: "task list + paginated run history"
      pattern: "ListRunsForTask"
  prohibitions:
    - statement: "MUST NOT render or return any raw secret (MCP env value) in any board/API response or write it to logs"
      status: resolved
      verification: test
    - statement: "MUST NOT make a pending skill runnable, activatable, or prompt-injectable from the skills board (no run/activate control; body never injected into an LLM context)"
      status: resolved
      verification: test
    - statement: "MUST NOT let the live MCP probe target anything beyond the already-configured servers (no operator-supplied or arbitrary outbound targets)"
      status: resolved
      verification: judgment
---

<objective>
The three read-only governance boards' backend: a single thin REST adapter file exposing six GET routes over the Wave-0 seams, plus the parent-mux mount and composition-root wiring. No business logic in the handlers — parse, validate, one provider call, project to JSON.

Purpose: GOV-01/02/03 need authenticated `/api/governance/*` reads following the proven graph_api.go template. The static MCP list always renders; the live probe is a separate per-row bounded request so a hung server isolates to its row.
Output: internal/agui/governance_api.go (6 handlers + narrow provider interfaces), the registerGovernanceRoutes hook in server.go Mux(), the serve_webui.go mounts, and the serve.go provider build + SetGovernanceProviders call.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-governance-boards-web-onboarding/28-RESEARCH.md
@.planning/phases/28-governance-boards-web-onboarding/28-PATTERNS.md
@.planning/phases/28-governance-boards-web-onboarding/28-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols created by this plan (excluded from drift verification):
- REST endpoints: `GET /api/governance/mcp`, `GET /api/governance/mcp/{name}/probe`, `GET /api/governance/skills`, `GET /api/governance/skills/audit`, `GET /api/governance/scheduler`, `GET /api/governance/scheduler/{id}/runs`
- `internal/agui/governance_api.go`: `registerGovernanceRoutes`, handler funcs `handleMCPList`/`handleMCPProbe`/`handleSkillsList`/`handleSkillsAudit`/`handleSchedulerList`/`handleSchedulerRuns`, and the consumer-side narrow provider interfaces (`MCPBoard`/`SkillsBoard`/`SchedulerBoard` or a `GovernanceProviders` aggregate)
- `internal/agui/server.go`: the `s.registerGovernanceRoutes(mux)` call added to Mux()
- `cmd/aura/serve_webui.go`: the six governance route consts (mounted behind RequireAuth)
- `cmd/aura/serve.go`: the governance-provider build block + `aguiServer.SetGovernanceProviders(...)`
(Consumed by Plan 03 frontend governance workspace.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: governance_api.go — six read handlers over the Wave-0 seams</name>
  <files>internal/agui/governance_api.go, internal/agui/server.go, internal/agui/governance_api_test.go</files>
  <read_first>
    - internal/agui/graph_api.go (the canonical thin read handler: GraphView narrow interface graph_api.go:57-60; registerGraphRoutes graph_api.go:66-69; the 503-when-unwired + sanitized-502 shape graph_api.go:75-90; the body-cap/validate/dispatch shape graph_api.go:95-116)
    - internal/agui/conversations_api.go (writeJSON/writeJSONStatus conversations_api.go:64-74; parseConvID uuid-guard conversations_api.go:79-86; parseLimit conversations_api.go:24-30)
    - internal/agui/server_redact.go (sanitizeErr server_redact.go:41-46 — every wire error passes through it)
    - internal/agui/server.go (the SetGovernanceProviders fields/setter added in Plan 01; the Mux() register block server.go:148-158 — add s.registerGovernanceRoutes(mux) here, mirroring the existing register calls)
    - internal/mcp/manager/status.go (SnapshotStatus status.go:40; StatusSnapshot status.go:28-36 — the static row source, deterministic by-name)
    - internal/mcp/probe.go (the Wave-0 probeServer + ProbeResult — call it per {name} under context.WithTimeout 3s)
    - internal/mcp/redact.go (RedactSecrets redact.go:6-15) + internal/secret/envkey.go (IsSecretEnvKey envkey.go:69 — render env KEYS as redacted chips, values NEVER sent)
    - internal/skills/loader.go (Loader.List loader.go:91 — active tab) + internal/skills/stage_reader.go (Wave-0 ListStage — pending/archived) + internal/skills/audit_store.go (AuditStore.List audit_store.go:169, newest-first; AuditRow audit_store.go:81-94)
    - internal/skills/contenthash.go + internal/skills/snippet_usage.go (per-skill metadata sources: content hash, use count, last used; map each field — A2: a field with no source renders as "—", never fabricated)
    - internal/cron/store.go (ListActiveTasks store.go:162; Task struct store.go:57-74) + internal/cron/store_runs.go (the Wave-0 ListRunsForTask)
  </read_first>
  <behavior>
    - handleMCPList (zero servers): 200 with {servers: []} — board still renders (empty edge).
    - handleMCPList (N servers): rows by-name; each env value is absent; only env KEY names appear flagged redacted. No raw secret string anywhere in the body.
    - handleMCPProbe (healthy {name}): {name, ok:true, toolCount:n}. handleMCPProbe (hung {name}): resolves within ~3s to {ok:false, error:"timed out"} (sanitized) — sibling probes unaffected (separate requests). handleMCPProbe (unknown {name}): 404, no probe dialed.
    - handleSkillsList(stage=pending): rows with NO action/run field. handleSkillsAudit: newest-first rows. handleSchedulerRuns({id} non-UUID): 404 before the store call; valid id with no runs: 200 {runs: []}.
    - Any provider nil (unwired): 503. Any backend error: sanitized 502.
  </behavior>
  <action>
    Create internal/agui/governance_api.go in package agui. Declare consumer-side narrow interfaces for exactly the methods the handlers call (e.g. an MCP board provider exposing the config snapshot + a probe func; a skills provider exposing active List + ListStage + audit List; a scheduler provider exposing ListActiveTasks + ListRunsForTask) — mirror graph_api.go:57-60. Add `func (s *Server) registerGovernanceRoutes(mux *http.ServeMux)` registering the six routes from RESEARCH §REST Endpoint Shapes (GET /api/governance/mcp, GET /api/governance/mcp/{name}/probe, GET /api/governance/skills, GET /api/governance/skills/audit, GET /api/governance/scheduler, GET /api/governance/scheduler/{id}/runs) and wire `s.registerGovernanceRoutes(mux)` into Mux() (server.go:148-158, beside the existing register calls).
    Each handler: nil-provider → 503 (graph_api.go:76-79 shape); success → writeJSON; backend-fail → writeJSONStatus(502, sanitizeErr(err)). For MCP list, build rows from SnapshotStatus(doc); for each env entry emit {key, redacted:true} using IsSecretEnvKey on the KEY, NEVER the value; apply RedactSecrets to lastError. For the MCP probe, look up {name} in the loaded config (404 if absent — Prohibition #5), then call probeServer under context.WithTimeout(r.Context(), 3*time.Second); a timeout/error becomes {ok:false, error: sanitizeErr(...)} (200, isolated). For skills, branch on ?stage (active → Loader.List; pending/archived → ListStage), aggregate per-skill metadata from contenthash + snippet_usage + audit-derived risk tier (a missing source → "—"), and OMIT any action field for pending rows. For audit, AuditStore.List with default limit 100 (the store default) → newest-first. For scheduler list, ListActiveTasks ordered by next fire; for runs, parse {id} via the uuid-guard (conversations_api.go:79-86 → 404 on non-UUID) + ?limit (default 25)/?offset (default 0) via parseLimit, then ListRunsForTask. NO mutation anywhere.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/agui/ -run TestGovernance</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agui/governance_api.go` contains `func (s *Server) registerGovernanceRoutes` registering all six routes, wired into Mux()
    - `go test ./internal/agui/ -run TestGovernanceMCP` asserts no raw secret value appears in the MCP list response body and rows are by-name ordered
    - `go test ./internal/agui/ -run TestMCPProbeIsolation` asserts a hung server's probe resolves to a timed-out result without blocking siblings, under the 3s deadline
    - `go test ./internal/agui/ -run TestMCPProbeConfiguredOnly` asserts an unknown {name} is 404 and no dial occurs (Prohibition #5)
    - `go test ./internal/agui/ -run TestGovernanceSkills` asserts pending rows carry no action field and the three stages list the correct sets
    - `go test ./internal/agui/ -run TestGovernanceScheduler` asserts a non-UUID {id} is 404 pre-store and run history paginates with default limit 25
    - `go test ./internal/agui/ -run TestGovernanceEmptyAndError` asserts empty datasets → 200 empty payload, unwired provider → 503, backend error → sanitized 502
    - `go test ./internal/agui/ -run TestGovernanceNilProvider503` constructs a Server via `NewServer` then `SetGovernanceProviders(nil)` and asserts a governance route returns 503 (the unwired-seam path; referenced by Task 2)
  </acceptance_criteria>
  <done>All six governance read handlers exist, registered in Mux(), with the redaction/isolation/pagination/empty/error behaviors integration-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Parent-mux mount + composition-root wiring for the governance boards</name>
  <files>cmd/aura/serve_webui.go, cmd/aura/serve.go</files>
  <read_first>
    - cmd/aura/serve_webui.go (graphSchemaRoute/graphQueryRoute consts serve_webui.go:147-150; the read-GET mount serve_webui.go:266-267; the /api/ exclusion carve-out serve_webui.go:90; the whole-mux RequireAuth wrap serve_webui.go:289 — all six governance reads mount as read-GETs inheriting RequireAuth, NEVER a bare /api/)
    - cmd/aura/serve.go (the SetGraphView/SetApprovalStore/SetImageProxy composition block serve.go:285-312; chat.pool exposes the pgxpool; the MCP config loader + skills loader + cron.Store construction sites — build the governance providers best-effort, then SetGovernanceProviders; a missing backend leaves routes at 503 and MUST NOT abort boot)
    - internal/agui/server.go (SetGovernanceProviders signature added in Plan 01)
    - internal/mcp/manager/status.go + cmd/aura/mcp_status.go (loadManagedMCPConfig mcp_status.go:21 — the config source to lift/share for the provider)
  </read_first>
  <action>
    In serve_webui.go, add the six governance route consts beside graphSchemaRoute (serve_webui.go:147-150) and mount each as a read-GET on the parent mux exactly like the graph reads (serve_webui.go:266-267) — they are SPECIFIC method+path siblings under the /api/ carve-out, never a bare /api/ (would shadow /api/integrations/). They inherit the whole-mux RequireAuth (serve_webui.go:289) — no second auth check.
    In serve.go, after agui.NewServer (mirror serve.go:285-312), build the governance providers: the MCP config loader (reuse loadManagedMCPConfig / mcp.LoadManagedConfig) + the structured probeServer, the skills active Loader + the Wave-0 ListStage reader + the AuditStore, and the existing cron.Store (chat.pool / the scheduler store wired in serve). Call aguiServer.SetGovernanceProviders(...) with them. Build best-effort: a provider that fails to construct logs a slog.Warn and leaves its routes at 503 — it must NOT abort daemon boot (the SetGraphView precedent serve.go:307-312).
  </action>
  <verify>
    <automated>go build ./cmd/aura && go vet ./... && go test ./internal/agui/ -run 'TestGovernance|TestGovernanceNilProvider503'</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/aura/serve_webui.go` contains the six `/api/governance/*` route consts and mounts them as read-GETs (inheriting RequireAuth)
    - `cmd/aura/serve.go` contains an `aguiServer.SetGovernanceProviders(` call in the composition block
    - `go build ./cmd/aura` passes; a daemon built with a deliberately-unavailable backend still boots (provider nil → routes 503, no fatal)
    - `go test ./internal/agui/ -run TestGovernanceNilProvider503` constructs an `agui.Server` via `NewServer` then `SetGovernanceProviders(nil)` (never wired) and asserts a governance route (e.g. `GET /api/governance/mcp`) returns **503** — proving the unwired-provider path is exercised and that `NewServer` boots without the providers (the seam stays off the constructor). This may live in Task 1's `governance_api_test.go` and be referenced here
    - `GET /api/governance/mcp` returns 401 when unauthenticated (inherited RequireAuth) — covered by an agui auth-gate test
  </acceptance_criteria>
  <done>The six governance reads are mounted behind RequireAuth and wired from the composition root, with best-effort provider construction that never aborts boot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /api/governance/* | Authenticated read surface; untrusted query/path params cross here |
| Handler ↔ MCP server (probe) | Server-side privileged dial; target restricted to loaded config |
| Backend strings ↔ wire | MCP env values, lastError, skill names, task summaries must be redacted/escaped |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Information disclosure | handleMCPList | mitigate | Env VALUES never serialized; only IsSecretEnvKey-flagged KEY names + {redacted:true}; RedactSecrets on lastError; a test asserts no secret substring in the body |
| T-28-02-02 | Tampering (SSRF pivot) | handleMCPProbe | mitigate | {name} looked up in loaded config (404 if absent); never a body-supplied URL/command; configured-servers-only (Prohibition #5). Generic SSRF on configured endpoints is canon (/gsd-secure-phase) |
| T-28-02-03 | Denial of service | handleMCPProbe | mitigate | Per-server context.WithTimeout(3s) + per-row isolation (separate request) so a hung server cannot stall the board; static list renders independently |
| T-28-02-04 | Elevation of privilege | handleSkillsList(pending) | mitigate | Pending rows carry no action field; the board reads via the per-stage reader, never the loader mount path; loader never injects pending bodies into context |
| T-28-02-05 | Spoofing / auth bypass | all six routes | mitigate | Mounted under the whole-mux RequireAuth (401 unauthenticated); read-only GETs (no RequireCapability needed — no mutation) |
| T-28-02-06 | Information disclosure | every handler error | mitigate | sanitizeErr on every wire error (502); no stack/DSN/host leak (HARDEN-08) |
| T-28-02-SC | Tampering | dependency surface | accept | No package installs in this plan (pure handler + wiring over Wave-0 seams); zero net-new deps |
</threat_model>

<verification>
- `go build ./...` + `go vet ./...` clean.
- `go test ./internal/agui/` green (handler unit tests with mocked providers + probe).
- A no-secret assertion over the MCP list response body passes.
- A daemon boot smoke (provider unavailable → 503, not fatal) passes.
</verification>

<success_criteria>
- Six authenticated read endpoints serve GOV-01/02/03 data following the graph_api.go template.
- MCP probe is bounded, per-row-isolated, configured-servers-only.
- Skills pending rows are non-runnable by construction; audit is newest-first.
- Scheduler run history paginates; nothing mutates.
- Empty/error/unwired/unauth states are all safe.
</success_criteria>

<output>
Create `.planning/phases/28-governance-boards-web-onboarding/28-02-SUMMARY.md` when done.
</output>
