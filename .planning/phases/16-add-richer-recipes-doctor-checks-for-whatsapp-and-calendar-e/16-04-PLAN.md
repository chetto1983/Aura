---
phase: 16-mcp-sidecar-manager-third-party-trust
plan: 04
type: execute
wave: 1
depends_on: ["16-02", "16-03"]
files_modified:
  - cmd/aura/mcp.go
  - cmd/aura/mcp_test.go
  - internal/mcp/manager/status.go
  - internal/mcp/manager/status_test.go
autonomous: true
requirements: [CAP-09]
---

<objective>
Build the MCP status, doctor, and log surface. This is the control-plane UX that catches WhatsApp bridge drift and third-party trust/runtime problems before chat boot.
</objective>

<context>
@cmd/aura/mcp.go
@D:/tmp/codex/codex-rs/app-server-protocol/schema/json/v2/ListMcpServerStatusResponse.json
@D:/tmp/codex/codex-rs/app-server-protocol/schema/json/v2/McpServerStatusUpdatedNotification.json
@.planning/phases/16-add-richer-recipes-doctor-checks-for-whatsapp-and-calendar-e/16-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add status snapshots and JSON output</name>
  <files>internal/mcp/manager/status.go, internal/mcp/manager/status_test.go, cmd/aura/mcp.go, cmd/aura/mcp_test.go</files>
  <behavior>
    - `aura mcp status` shows server, profile, trust, runtime, startup state, auth status, tool count, and last error.
    - `aura mcp status --json` emits stable machine-readable JSON.
    - Blocked servers are visible as blocked and are not launched just to show status.
  </behavior>
  <action>
    Add status structs inspired by Codex's status response but shaped for Aura CLI. Include `starting|ready|failed|blocked|disabled|unknown`, auth status, server info when available, and policy summary. Add tests with temp config and fake status collectors.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./cmd/aura/ ./internal/mcp/manager/ -run 'TestMCP.*Status|TestStatus' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand doctor to all servers and recipe-specific checks</name>
  <files>cmd/aura/mcp.go, cmd/aura/mcp_test.go, internal/mcp/manager/status.go</files>
  <behavior>
    - `aura mcp doctor --all` runs non-secret config/runtime checks for every configured server.
    - WhatsApp checks WSL plus REST `:8080`.
    - Mail checks required env placeholders without printing secret values.
    - Calendar checks fixture mode and live-auth readiness separately.
    - Docker/npm/npx/uv/wsl command checks are fakeable in tests.
  </behavior>
  <action>
    Refactor doctor into layered checks: config, runtime, transport, recipe, policy. Keep existing `doctor <name>` behavior compatible. Add command-probe seams for tests. Redact values in every diagnostic line.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./cmd/aura/ -run 'TestMCP.*Doctor|TestProbe|TestRedact' -count=1</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add stderr/log tail capture to status/doctor</name>
  <files>internal/mcp/client.go, internal/mcp/manager/status.go, cmd/aura/mcp.go</files>
  <behavior>
    - Failed stdio startup reports a redacted stderr tail.
    - Log output does not include env secret values.
  </behavior>
  <action>
    Expose a safe stderr tail from MCP startup failures or collect it in doctor/status code. Redact likely secrets before printing. Keep full logs out of git and out of exported profiles.
  </action>
  <verify>
    <automated>cd D:/Aura && go test ./internal/mcp/ ./cmd/aura/ -run 'Test.*Stderr|Test.*Log|Test.*Redact' -count=1</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- `aura mcp doctor --all` is useful before live evals.
- Status is stable for both humans and scripts.
- Diagnostics are actionable and redacted.
</success_criteria>

<output>Create `16-04-SUMMARY.md` when done.</output>
