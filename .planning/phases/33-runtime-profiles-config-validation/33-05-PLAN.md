---
phase: 33-runtime-profiles-config-validation
plan: 05
type: execute
wave: 4
depends_on: ["33-04"]
files_modified:
  - cmd/aura/config.go
  - cmd/aura/config_validate.go
  - cmd/aura/config_validate_test.go
autonomous: true
requirements: [PROF-01]
must_haves:
  truths:
    - "aura config validate --profile server_production exits non-zero and lists EVERY unmet requirement, each line naming its AURA_* knob (success criterion #1, PROF-01)."
    - "--profile overrides AURA_PROFILE for the validation run so an operator can lint any profile without changing their env (D-02)."
  artifacts:
    - path: "cmd/aura/config_validate.go"
      provides: "config validate subcommand: flag parse (--profile/--json), LoadServe, ValidateProfile, render, exit code"
      contains: "func configValidate"
    - path: "cmd/aura/config_validate_test.go"
      provides: "exit-code + knob-name output test for server_production (PROF-01 e2e)"
      contains: "TestConfigValidate_ServerProduction"
  key_links:
    - from: "cmd/aura/config.go runConfig"
      to: "configValidate (new case)"
      via: "subcommand dispatch beside show|get|set"
      pattern: "case \"validate\""
    - from: "cmd/aura/config_validate.go"
      to: "(*config.Config).ValidateProfile"
      via: "thin presenter over the internal/config core"
      pattern: "ValidateProfile\\("
  prohibitions:
    - "MUST NOT re-implement validation logic in cmd/aura — the CLI is a THIN presenter; all rules live in internal/config (ValidateProfile). The only new gating call site in cmd/aura is this validate subcommand."
    - "MUST NOT print secret knob VALUES in table or --json output — redact via KnobSpec.Secret (mirror redactedAPIKey)."
---

<objective>
Expose the validation core as the operator-facing `aura config validate [--profile <p>] [--json]` subcommand (PROF-01), slotting beside `show|get|set` in the `runConfig` dispatcher. It loads the full config, runs `(*Config).ValidateProfile` (plan 04), renders a human table (default) or JSON (CI), and exits non-zero when any Fatal violation exists — fail-closed, listing every unmet requirement at once.

Purpose: This is the acceptance command from the audit action-plan — `aura config validate --profile server_production reports all unmet requirements`. It is a thin presenter; all logic lives in `internal/config`.
Output: `cmd/aura/config_validate.go` (the subcommand), the `validate` dispatch case, and an exit-code + knob-name e2e test.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-runtime-profiles-config-validation/33-CONTEXT.md
@.planning/phases/33-runtime-profiles-config-validation/33-RESEARCH.md
@.planning/phases/33-runtime-profiles-config-validation/33-PATTERNS.md
@.planning/phases/33-runtime-profiles-config-validation/33-VALIDATION.md
@cmd/aura/config.go
</context>

## Artifacts this phase produces (phase-wide — for source-grounding; this plan creates the ones marked ★)

- `RuntimeProfile`/`ParseProfile`, `Config.Profile`/`ObjectStoreReplicationFactor`/`GarageRPCSecret`, `Violation`/`Severity` — `internal/config` (plan 01, CONSUMED)
- `KnobSpec`/`knobRegistry`/`reparsePass` (incl. `Secret` flag for redaction) — `internal/config/config_knobs.go` (plan 03, CONSUMED)
- `(*Config).ValidateProfile` + bespoke gates + profile-aware `Validate()` — `internal/config/config_validate.go` (plan 04, CONSUMED)
- D-12 destructive flip — `internal/agent/tools/shell_exec_env.go` (plan 02)
- ★ `configValidate(args []string)` + inner `runConfigValidate(args, io.Writer) int`, `--profile`/`--json` flags, `validate` dispatch case, secret-redacting renderer — `cmd/aura/config_validate.go` + `cmd/aura/config.go`
- ★ `TestConfigValidate_ServerProduction` (exit-code + knob-name output, PROF-01 e2e) — `cmd/aura/config_validate_test.go`

<tasks>

<task type="auto">
  <name>Task 1: Implement the config validate subcommand (flags, LoadServe, ValidateProfile, render, exit code)</name>
  <files>cmd/aura/config.go, cmd/aura/config_validate.go</files>
  <read_first>
    - `cmd/aura/config.go` lines 26-47 (`runConfig` dispatch switch + `configUsage`) and lines 49-72 (`configShow` — the load→render→`os.Exit(1)` analog) and line 24 (`const redactedAPIKey = "REDACTED"` — the redaction convention to mirror).
    - `internal/config/config.go` lines 248-258 (`LoadServe()` — permits empty LLM key; the loader the CLI uses) and the `ValidateProfile`/`Violation`/`Severity` + `KnobSpec.Secret` surface (plans 01/03/04).
    - PATTERNS.md §"cmd/aura/config_validate.go (NEW) + cmd/aura/config.go (MODIFIED)" (the dispatch + load→render→exit analog + `flag.NewFlagSet` for `--profile`/`--json`) and §"No Analog Found" (factor logic into a testable inner `func() int`; keep the presenter thin).
    - RESEARCH.md §"Code Examples → CLI subcommand wiring" + §"State of the Art → --json recommendation (~15 LOC)" + §Security Pitfall 6 (redact secret knobs).
  </read_first>
  <action>
    Create `cmd/aura/config_validate.go` (package `main`). Implement `func configValidate(args []string) { os.Exit(runConfigValidate(args, os.Stdout)) }` and a testable inner `func runConfigValidate(args []string, out io.Writer) int`. In the inner func: build a `flag.NewFlagSet("config validate", flag.ContinueOnError)` with `--profile string` (overrides `AURA_PROFILE` for this run, D-02) and `--json bool`; on flag parse error return 2 with usage to stderr. Load `config.LoadServe()` (tolerant of empty LLM key); on load error print to stderr + return 1. Resolve the profile: if `--profile` set, `config.ParseProfile(flagValue)`; else `cfg.Profile`. Call `cfg.ValidateProfile(p)`. Render: default = a human table (knob, severity, message — one row per violation, plus a clean "ok" line when none); `--json` = `json.NewEncoder(out).Encode(violations)`. REDACT secret knobs: never print any knob VALUE; for knobs whose `KnobSpec.Secret` is true, ensure only the name + message (which is value-free) appear (mirror `redactedAPIKey`). Return 1 if ANY violation has `Sev == config.Fatal` (fail-closed), else 0. In `cmd/aura/config.go` add `case "validate": configValidate(args[1:])` to the `runConfig` switch and update `configUsage` to document `validate [--profile <p>] [--json]`.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./cmd/aura/ && go vet ./cmd/aura/</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/aura/config.go` `runConfig` switch contains `case "validate"` (`grep -c 'case "validate"' cmd/aura/config.go` == 1) and `configUsage` mentions `validate`.
    - `cmd/aura/config_validate.go` defines `func configValidate(args []string)` and a testable `func runConfigValidate(args []string, out io.Writer) int` calling `config.LoadServe()` and `cfg.ValidateProfile(...)` (`grep -c "ValidateProfile" cmd/aura/config_validate.go` == 1).
    - `--profile` parsing uses `config.ParseProfile` to honor D-02 override; `--json` path uses `json.NewEncoder`.
    - No validation rule is reimplemented in cmd/aura (`grep -c "defaultObjectStore\|replication_factor\|ParseBool" cmd/aura/config_validate.go` == 0 — logic stays in internal/config).
    - `go build ./cmd/aura/` + `go vet ./cmd/aura/` clean.
  </acceptance_criteria>
  <done>The validate subcommand is wired as a thin presenter over ValidateProfile with --profile/--json + secret redaction + fail-closed exit code; build/vet green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e test — config validate --profile server_production exits non-zero listing every unmet knob</name>
  <files>cmd/aura/config_validate_test.go</files>
  <read_first>
    - `cmd/aura/config_validate.go` (the inner `runConfigValidate(args, out) int` from Task 1 — the test target).
    - PATTERNS.md §"No Analog Found" (assert exit-code via the inner `func() int` core capturing `out`, not a subprocess) and §"Test Patterns → Table-driven pure-function gate test" (the `strings.Contains(out, "AURA_...")` assertion style).
    - `internal/config/config_test.go` `clearPostgresEnv` (extended in plan 01) — the env-baseline approach to mirror with `t.Setenv`.
    - VALIDATION.md row PROF-01 e2e (`TestConfigValidate_ServerProduction`).
  </read_first>
  <behavior>
    - With an env composing an unsafe server_production posture (sample object-store keys, empty GARAGE_RPC_SECRET, AURA_AGUI_CORS_PERMISSIVE=true, AURA_OBJECTSTORE_REPLICATION_FACTOR=1, AURA_SHELL_DESTRUCTIVE_PATTERNS=off, empty AURA_AUTHULA_SECRET, required DB/Neo4j secrets present so the run reaches the profile gates), `runConfigValidate(["--profile","server_production"], &buf)` returns 1.
    - The captured output contains each offending knob name (`AURA_OBJECTSTORE_ACCESS_KEY`, `AURA_AGUI_CORS_PERMISSIVE`, `AURA_OBJECTSTORE_REPLICATION_FACTOR`, `GARAGE_RPC_SECRET`, `AURA_AUTHULA_SECRET`, `AURA_SHELL_DESTRUCTIVE_PATTERNS`).
    - A `--profile dev` run of a benign env returns 0.
    - `--json` output is valid JSON decodable into a `[]config.Violation`.
    - No secret knob VALUE appears verbatim in the output (redaction).
  </behavior>
  <action>
    Create `cmd/aura/config_validate_test.go` (package `main`). Add `TestConfigValidate_ServerProduction`: set the unsafe env via `t.Setenv` (sample keys, permissive CORS, replication 1, destructive off, empty Authula/RPC, but DB/Neo4j secrets present), capture a `bytes.Buffer`, call `runConfigValidate([]string{"--profile", "server_production"}, &buf)`, assert the return is 1 and `buf.String()` contains each offending knob name (mirror the `strings.Contains(msg, "AURA_...")` assertion). Add a `--profile dev` benign-env sub-test asserting return 0. Add a `--json` sub-test asserting `json.Unmarshal(buf.Bytes(), &[]config.Violation{})` succeeds and lists the violations. Add a redaction assertion: pick a secret knob given an obviously-fake secret value and assert that value does NOT appear in `buf.String()`.
  </action>
  <verify>
    <automated>cd /d/Aura && go test ./cmd/aura/ -run TestConfigValidate_ServerProduction -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func TestConfigValidate_ServerProduction" cmd/aura/config_validate_test.go` == 1.
    - The test asserts `runConfigValidate(["--profile","server_production"], buf) == 1` AND `buf` contains each of the ≥6 offending knob names.
    - A `--profile dev` benign sub-test asserts return 0 (criterion #4 parity).
    - The `--json` sub-test decodes the output into `[]config.Violation` without error.
    - A redaction sub-assertion proves no secret VALUE is echoed.
    - `go test ./cmd/aura/ -run TestConfigValidate_ServerProduction -race` passes.
  </acceptance_criteria>
  <done>PROF-01 e2e proven: validate --profile server_production exits 1 listing every unmet knob; dev benign exits 0; --json valid; secrets redacted; green under -race.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv (`--profile`/`--json`) + env → `config validate` | Operator-driven lint of any profile (D-02). Output may land in CI logs. |
| validate output → stdout / CI logs | Secret-bearing knob values must never be echoed (V8). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Information Disclosure | secret knob value printed to stdout/CI logs | mitigate | Renderer never prints knob VALUES; `KnobSpec.Secret`-driven redaction (mirror `redactedAPIKey`); Violation messages are value-free. Test asserts no secret value echoed. |
| T-33-11 | Denial of Service / fail-open | `config validate` returning 0 for an unsafe prod config | mitigate | Fail-closed: any Fatal ⇒ exit 1; aggregate output lists EVERY unmet requirement (criterion #1). e2e test asserts exit 1 + all knob names. |
| T-33-12 | Tampering | validation logic duplicated/diverging in the CLI | mitigate | CLI is a THIN presenter — all rules live in `internal/config.ValidateProfile`; test asserts no rule constants in cmd/aura. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No installs (pure-Go). Legitimacy gate N/A. |

ASVS: V14 Configuration (operator-facing contract check), V7 Logging (knob-naming, value-free messages), V8 Data Protection (secret redaction). No crypto.
</threat_model>

<verification>
- `go build ./cmd/aura/ && go vet ./cmd/aura/` clean.
- `go test ./cmd/aura/ -run TestConfigValidate_ServerProduction -race -count=1` green.
- CLI contains no reimplemented validation constants (thin presenter).
- Manual smoke (optional, host): `aura config validate --profile server_production` on a sample env exits non-zero listing knobs (build+run in WSL/container per MEMORY — never run the .exe natively).
</verification>

<success_criteria>
- `aura config validate --profile server_production` exits non-zero listing every unmet requirement, each line naming its knob (criterion #1, PROF-01).
- `--profile` overrides AURA_PROFILE (D-02); `--json` emits CI-parseable output; secrets redacted.
- A benign dev run exits 0 (criterion #4 parity).
</success_criteria>

<output>
Create `.planning/phases/33-runtime-profiles-config-validation/33-05-SUMMARY.md` when done.
</output>
