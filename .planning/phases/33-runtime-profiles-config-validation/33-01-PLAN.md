---
phase: 33-runtime-profiles-config-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_validate.go
  - internal/config/config_runtimeprofile.go
  - internal/config/config_runtimeprofile_test.go
  - internal/config/config_test.go
autonomous: true
requirements: [PROF-01, PROF-05]
must_haves:
  truths:
    - "AURA_PROFILE unset resolves to dev (D-03) — preserves today's full-host behavior unchanged (success criterion #4)."
    - "Config exposes a runtime-readable Profile, ObjectStoreReplicationFactor, and GarageRPCSecret populated at load."
  artifacts:
    - path: "internal/config/config_runtimeprofile.go"
      provides: "RuntimeProfile enum + ParseProfile + Tier helper (per D-01/D-03)"
      contains: "func ParseProfile"
    - path: "internal/config/config_validate.go"
      provides: "Validate() moved here (frees config.go LOC) + Violation/Severity contract types"
      contains: "type Violation"
    - path: "internal/config/config.go"
      provides: "Config.Profile / Config.ObjectStoreReplicationFactor / Config.GarageRPCSecret fields populated in loadBase"
      contains: "ParseProfile"
  key_links:
    - from: "internal/config/config.go loadBase()"
      to: "ParseProfile(os.Getenv(\"AURA_PROFILE\"))"
      via: "field population"
      pattern: "ParseProfile\\("
  prohibitions:
    - "MUST NOT add per-profile runtime enforcement of tool capabilities, file-path fences, sandbox selection, or network egress (Tool Gateway, Phase 35+). This plan only adds config-load fields + types."
---

<objective>
Unblock the phase by splitting `Validate()` out of `internal/config/config.go` (it is 557/600 LOC — the whole-tree file-size git hook BLOCKS ALL COMMITS the moment any file exceeds 600), then add the runtime-profile primitives the rest of the phase builds on: the `RuntimeProfile` typed enum (D-01/D-03) and three new `Config` fields (`Profile`, `ObjectStoreReplicationFactor`, `GarageRPCSecret`) populated at load.

Purpose: Establish the contract types (`Violation`/`Severity`) and the profile/field read-path FIRST so plans 02–05 build against fixed interfaces without re-reading the codebase. The split must land before any new validation code or every subsequent commit is blocked (RESEARCH Pitfall 3).

Output: `config_validate.go` (moved Validate + contract types), `config_runtimeprofile.go` (enum + parser), three new `Config` fields wired in `loadBase`, an extended `clearPostgresEnv` test baseline, and `TestParseProfile`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-runtime-profiles-config-validation/33-CONTEXT.md
@.planning/phases/33-runtime-profiles-config-validation/33-RESEARCH.md
@.planning/phases/33-runtime-profiles-config-validation/33-PATTERNS.md
@.planning/phases/33-runtime-profiles-config-validation/33-VALIDATION.md
@internal/config/config.go
@internal/config/config_test.go
@internal/config/config_validate_test.go
</context>

## Artifacts this phase produces (phase-wide — for source-grounding; this plan creates the ones marked ★)

- ★ `RuntimeProfile` type (string) + constants `ProfileDev`, `ProfileLocalTrusted`, `ProfileSingleUserHardened`, `ProfileServerProduction` — `internal/config/config_runtimeprofile.go`
- ★ `ParseProfile(s string) RuntimeProfile` (total; unknown/empty → `ProfileDev`, D-03) + `(RuntimeProfile).Tier()`/`Strict()` helper (lenient = {dev,local_trusted}; strict = {single_user_hardened,server_production}) — same file
- ★ `Config.Profile RuntimeProfile`, `Config.ObjectStoreReplicationFactor int`, `Config.GarageRPCSecret string` fields + `AURA_PROFILE` / `AURA_OBJECTSTORE_REPLICATION_FACTOR` env reads + upstream `GARAGE_RPC_SECRET` read — `internal/config/config.go`
- ★ `Severity` type + `Warn`/`Fatal` constants, `Violation` struct {Knob, Sev, Msg} — `internal/config/config_validate.go`
- `KnobKind` type + `KindString`/`KindInt`/`KindBool`/`KindEnum`, `KnobSpec` struct, `knobRegistry() []KnobSpec`, `reparsePass(RuntimeProfile) []Violation` — `internal/config/config_knobs.go` (plan 03)
- `(*Config).ValidateProfile(RuntimeProfile) []Violation`, bespoke gates `gateObjectStoreCreds`/`gateGarageRPCSecret`/`gateReplication`/`gateCORS`/`gateDestructiveShell`/`gateWebAuth`/`gateObjectStoreEndpoint`, refactored `(*Config).Validate()` — `internal/config/config_validate.go` (plan 04)
- `configValidate(args []string)` + `--profile`/`--json` flags, `validate` dispatch case — `cmd/aura/config_validate.go` + `cmd/aura/config.go` (plan 05)
- D-12 `destructiveShellPatterns()` empty→defaults semantics flip — `internal/agent/tools/shell_exec_env.go` (plan 02)
- New test files: `config_runtimeprofile_test.go`, `config_knobs_test.go`, `cmd/aura/config_validate_test.go`; extended: `config_validate_test.go`, `config_rundir_test.go`, `shell_exec_destructive_default_test.go`, `config_test.go`

<tasks>

<task type="auto">
  <name>Task 1: Split Validate() out of config.go into config_validate.go (LOC unblock) + define the Violation/Severity contract</name>
  <files>internal/config/config.go, internal/config/config_validate.go</files>
  <read_first>
    - `internal/config/config.go` lines 260-280 (`Validate()` — the exact body to MOVE verbatim) and the package header/imports.
    - PATTERNS.md §"config_validate.go (NEW) — THE LOAD-BEARING ANALOG" (the `Validate` multi-error idiom) and §"config.go (MODIFIED)" item 4 (MOVE Validate FIRST).
    - RESEARCH.md Pitfall 3 (config.go 557/600 LOC; file-size hook blocks ALL commits when any file >600).
    - MEMORY: `gsd commit wrapper times out on file-size hook` — use direct `git commit`, not the SDK commit wrapper, for this phase.
  </read_first>
  <action>
    Move the `Validate()` method (config.go:265-280, including its doc comment at 260-264) VERBATIM into a new file `internal/config/config_validate.go` (package `config`, same imports it needs: `fmt`, `strings`). Do NOT change its behavior in this task — this is a pure LOC-relief move so config.go drops below 600 before any field is added. In the SAME file, define the contract types the later plans consume: `type Severity int` with `const ( Warn Severity = iota; Fatal )`, and `type Violation struct { Knob string; Sev Severity; Msg string }`. Add a file-header comment citing CLAUDE.md ≤600-LOC refactor-on-touch (mirror the `config_env.go` header precedent). Remove the now-dead `Validate` body from config.go. Per CLAUDE.md deep-refactor-on-touch: verify no imports become unused in config.go after the move and drop any that do.
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/config/ && go test ./internal/config/ -run TestConfigValidate -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/config/config_validate.go` exists, declares `package config`, and contains `func (c *Config) Validate() error`, `type Violation struct`, and `type Severity`.
    - `config.go` no longer contains `func (c *Config) Validate()` (grep: `grep -c "func (c \*Config) Validate" internal/config/config.go` returns 0).
    - `internal/config/config.go` line count ≤ 600 after the move (`grep -c '' internal/config/config.go` < 600 — confirm headroom for plan 01 Tasks 2-3 additions).
    - `go test ./internal/config/ -run TestConfigValidate` passes unchanged (the move is behavior-preserving).
  </acceptance_criteria>
  <done>Validate() lives in config_validate.go; config.go is under the 600-LOC cap; Violation/Severity contract types exist; existing TestConfigValidate is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the RuntimeProfile typed enum + ParseProfile + Tier helper</name>
  <files>internal/config/config_runtimeprofile.go, internal/config/config_runtimeprofile_test.go</files>
  <read_first>
    - `internal/config/config.go` lines 282-308 (`GuardWebBind` — the pure/total function discipline to mirror) and lines 24, 199-200, 468-469 (the Agent.md `profile` import + `ProfileDir`/`ProfileCertaintyN` — the names you must NOT shadow).
    - PATTERNS.md §"config_runtimeprofile.go (NEW)" (Pattern 3 + the CRITICAL naming-collision section) and §"Test Patterns → Table-driven pure-function gate test".
    - RESEARCH.md Pitfall 1 (Agent.md-profile naming collision) and Architecture Pattern 3.
  </read_first>
  <behavior>
    - `ParseProfile("")` → `ProfileDev` (D-03 default-when-unset).
    - `ParseProfile("dev"|"local_trusted"|"single_user_hardened"|"server_production")` → the matching constant.
    - `ParseProfile("garbage")` → `ProfileDev` (total: unknown never panics, never errors; defaults to the most permissive/loudest tier).
    - `ProfileDev.Strict()` == false; `ProfileLocalTrusted.Strict()` == false; `ProfileSingleUserHardened.Strict()` == true; `ProfileServerProduction.Strict()` == true.
  </behavior>
  <action>
    Create `internal/config/config_runtimeprofile.go` (package `config`). Define `type RuntimeProfile string` with the four constants `ProfileDev = "dev"`, `ProfileLocalTrusted = "local_trusted"`, `ProfileSingleUserHardened = "single_user_hardened"`, `ProfileServerProduction = "server_production"`. Implement `func ParseProfile(s string) RuntimeProfile` as a total switch over `strings.TrimSpace(s)` defaulting unknown/empty to `ProfileDev` (D-03). Implement `func (p RuntimeProfile) Strict() bool` returning true only for `ProfileSingleUserHardened`/`ProfileServerProduction` (this single helper is the severity decision the plan-03 re-parse pass keys on: lenient = {dev, local_trusted} per D-14; strict = {hardened, prod} per D-07). Use the DISTINCT names per RESEARCH Pitfall 1 — do NOT touch `internal/profile`, `Config.ProfileDir`, `AURA_PROFILE_DIR`, or name anything `config_profile_test.go`. Create `internal/config/config_runtimeprofile_test.go` with `TestParseProfile` as an anonymous-struct table mirroring `TestGuardWebBind`'s style, covering all four named profiles + empty + a garbage string, asserting both the parsed constant and `.Strict()`.
  </action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/config/ -run TestParseProfile -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `config_runtimeprofile.go` contains `func ParseProfile(s string) RuntimeProfile` and `func (p RuntimeProfile) Strict() bool`.
    - No symbol named `Profile` collides with the Agent.md identity profile: `grep -rn "AURA_PROFILE_DIR\|ProfileDir" internal/config/config_runtimeprofile.go` returns 0 matches.
    - `go test ./internal/config/ -run TestParseProfile` passes, including the empty-string → dev and garbage → dev cases.
    - No file named `internal/config/config_profile_test.go` is created or modified by this task (the existing one belongs to the Agent.md profile).
  </acceptance_criteria>
  <done>RuntimeProfile enum + total ParseProfile + Strict() exist with a passing table test; zero name collision with the Agent.md profile surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Config.Profile / ObjectStoreReplicationFactor / GarageRPCSecret fields + loadBase reads + extend clearPostgresEnv</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - `internal/config/config.go` lines 120-134 (ObjectStore struct field block — where to declare the two new object-store-adjacent fields) and lines 410-478 (`loadBase()` object-store/Authula read block — the `envDefault`/`envutil.IntDefault`/`os.Getenv` idiom to mirror) and line 384 area (the `SEARXNG_URL` upstream-name `os.Getenv` precedent for `GARAGE_RPC_SECRET`).
    - PATTERNS.md §"config.go (MODIFIED)" items 1-3 (the exact field additions + read idioms) and §"Test Patterns → Env-defaults/override loader test" (the `clearPostgresEnv` gap note).
    - `internal/config/config_test.go` lines 15-55 (`clearPostgresEnv` keys slice — DOES NOT yet clear the new knobs).
    - CLAUDE.md env convention `AURA_<DOMAIN>_<UNIT>` (so `AURA_OBJECTSTORE_REPLICATION_FACTOR` is correct) + sidecar exception (`GARAGE_RPC_SECRET` keeps upstream name).
  </read_first>
  <behavior>
    - With `AURA_PROFILE` unset, `loadBase()` yields `Config.Profile == ProfileDev`.
    - With `AURA_PROFILE=server_production`, `Config.Profile == ProfileServerProduction`.
    - With `AURA_OBJECTSTORE_REPLICATION_FACTOR` unset, `Config.ObjectStoreReplicationFactor == 1` (matches docker/garage/garage.toml:5 default); with `=3`, it is 3.
    - With `GARAGE_RPC_SECRET=abc`, `Config.GarageRPCSecret == "abc"`; unset → "".
  </behavior>
  <action>
    Per D-13 (the ONE genuinely net-new config read this phase needs — validate Garage durability via a new env knob, NOT by parsing garage.toml), in `internal/config/config.go`: (1) add `Profile RuntimeProfile // AURA_PROFILE — runtime deployment profile, default dev (D-01/D-03)` to the `Config` struct (place near the top-level fields, NOT near `ProfileDir` to avoid reader confusion). (2) In the ObjectStore field block (~line 130) add `ObjectStoreReplicationFactor int // AURA_OBJECTSTORE_REPLICATION_FACTOR — declared Garage durability intent, default 1 (D-13/PROF-06)` and `GarageRPCSecret string // GARAGE_RPC_SECRET — Garage inter-node RPC secret (upstream name; D-13/PROF-03)`. (3) In `loadBase()` populate them: `Profile: ParseProfile(os.Getenv("AURA_PROFILE"))`, `ObjectStoreReplicationFactor: envutil.IntDefault("AURA_OBJECTSTORE_REPLICATION_FACTOR", 1)`, `GarageRPCSecret: os.Getenv("GARAGE_RPC_SECRET")` — mirroring the object-store read block at 417-432. These are config-contract READS only (declared intent per D-13), NOT runtime enforcement (scope fence); keeping garage.toml in sync is a deployment follow-on, NOT this phase's acceptance. In `internal/config/config_test.go`, append `"AURA_PROFILE", "AURA_OBJECTSTORE_REPLICATION_FACTOR", "GARAGE_RPC_SECRET", "AURA_SHELL_DESTRUCTIVE_PATTERNS"` to the `clearPostgresEnv` keys slice so downstream profile/gate tests run from a known baseline (PATTERNS test-env-hygiene correction). Add a small `TestRuntimeProfileFieldsLoad` to `config_runtimeprofile_test.go` exercising the four behaviors above via `clearPostgresEnv` + `t.Setenv` + `LoadDB()` (mirror `TestWebAuthConfigLoad`).
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./... && go test ./internal/config/ -run 'TestRuntimeProfileFieldsLoad|TestParseProfile|TestConfigValidate|TestWebAuthConfigLoad' -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ObjectStoreReplicationFactor\|GarageRPCSecret\|Profile RuntimeProfile" internal/config/config.go` ≥ 1 for each (struct fields present).
    - `grep -c 'ParseProfile(os.Getenv("AURA_PROFILE"))' internal/config/config.go` == 1 (loadBase populates Profile).
    - `clearPostgresEnv` keys slice contains `AURA_PROFILE`, `AURA_OBJECTSTORE_REPLICATION_FACTOR`, `GARAGE_RPC_SECRET`, `AURA_SHELL_DESTRUCTIVE_PATTERNS` (`grep -c "AURA_OBJECTSTORE_REPLICATION_FACTOR" internal/config/config_test.go` == 1).
    - `go test ./internal/config/ -run TestRuntimeProfileFieldsLoad` passes (default replication 1, default profile dev, GarageRPCSecret round-trips).
    - `go build ./...` is green (no unused import / no >600-LOC file: `grep -c '' internal/config/config.go` < 600).
  </acceptance_criteria>
  <done>Config carries the three new fields populated at load with correct defaults; clearPostgresEnv clears the new knobs; whole-tree build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env / `.env` → `loadBase()` | Operator-controlled env crosses into the `Config` struct. Operator is trusted (single-tenant, identity-isolation, no RBAC); the threat is ACCIDENTAL misconfiguration, not a malicious operator (RESEARCH §Security operator-trust note). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `AURA_PROFILE` value | mitigate | `ParseProfile` is total: an unknown/garbage profile defaults to `dev` (loudest, most permissive) — never silently selects a stricter tier the operator did not intend, and never panics at load (V5 validation). |
| T-33-02 | Information Disclosure | `GarageRPCSecret` field added to `Config` | accept (this plan) | Field merely stored from env this plan; redaction in operator-facing output is enforced by the `KnobSpec.Secret` flag in plan 03/05. No new log/print of the secret is added here (verify: no `fmt.Print*`/`slog` of `GarageRPCSecret` in this plan's diff). |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure-Go, all deps pre-present in go.mod (RESEARCH §Package Legitimacy Audit). Legitimacy gate N/A. |

ASVS: V14 Configuration (secure-by-default profile resolution), V5 Validation (total parser). No crypto performed — secret is only stored, not compared, in this plan.
</threat_model>

<verification>
- `go build ./...` green; `internal/config/config.go` ≤ 600 LOC.
- `go test ./internal/config/ -race -count=1` green (existing + new tests).
- No edit to `internal/profile`, `config_profile_test.go`, `Config.ProfileDir`, or `AURA_PROFILE_DIR` (naming-collision guard).
</verification>

<success_criteria>
- Validate() relocated to config_validate.go; config.go under the LOC cap (commits unblocked for the rest of the phase).
- RuntimeProfile enum + total ParseProfile + Strict() helper land with a passing table test; AURA_PROFILE unset → dev (criterion #4 foundation).
- Config carries Profile / ObjectStoreReplicationFactor / GarageRPCSecret with correct defaults; clearPostgresEnv extended.
- Violation/Severity contract types exist for plans 02-05.
</success_criteria>

<output>
Create `.planning/phases/33-runtime-profiles-config-validation/33-01-SUMMARY.md` when done.
</output>
