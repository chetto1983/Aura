---
phase: 33-runtime-profiles-config-validation
plan: 03
type: tdd
wave: 2
depends_on: ["33-01"]
files_modified:
  - internal/config/config_knobs.go
  - internal/config/config_knobs_test.go
autonomous: true
requirements: [PROF-04, QUAL-04]
must_haves:
  truths:
    - "Invalid int/bool/enum env values fail-fast (Fatal) under hardened/production and warn (never Fatal) under dev/local_trusted — generic, zero per-knob code (success criterion #3, PROF-04 / D-07)."
    - "Every hot-path AURA_* knob in the Tier A+B cut has exactly one registry row — the registry is the single source of truth (QUAL-04 / D-08)."
  artifacts:
    - path: "internal/config/config_knobs.go"
      provides: "KnobSpec registry (single source of truth) + kind-driven reparsePass"
      contains: "func knobRegistry"
    - path: "internal/config/config_knobs_test.go"
      provides: "registry round-trip test + reparse table test + 3 rapid PBT invariants"
      contains: "rapid.Check"
  key_links:
    - from: "internal/config/config_knobs.go reparsePass()"
      to: "RuntimeProfile.Strict()"
      via: "severity selection (Fatal under strict, Warn under lenient)"
      pattern: "\\.Strict\\(\\)"
  prohibitions:
    - "MUST NOT modify internal/envutil (it stays the dumb silent-fallback leaf, D-06). The re-parse pass re-reads raw env independently for diagnostics — it does not couple the leaf to profile state."
    - "MUST NOT catalogue Tier C (agent-tools/loop/llm AURA_* knobs) — Tier A+B only (D-16); Tier C is a documented follow-on."
    - "MUST NOT add per-profile runtime enforcement of tool capabilities/path fences/sandbox/egress (Tool Gateway, Phase 35+)."
---

<objective>
Build the QUAL-04 deliverable — a `[]KnobSpec` Go registry as the single source of truth for hot-path `AURA_*` knobs (D-08) — and the generic, kind-driven re-parse pass that satisfies PROF-04/F-016 for EVERY cataloged int/bool/enum knob with zero per-knob code. Under strict tiers (hardened/production) any invalid cataloged value is a Fatal `Violation`; under lenient tiers (dev/local_trusted) it is at most a Warn (the runtime read still silently falls back via the untouched `envutil` leaf).

Purpose: This is the validation ENGINE (D-08 "the registry IS the engine") — a data registry plus ~20 LOC of stdlib `strconv`, not a framework. It is the PBT centerpiece of the phase.
Output: `config_knobs.go` (KnobSpec + registry + reparsePass) and `config_knobs_test.go` (round-trip + table + 3 rapid invariants).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-runtime-profiles-config-validation/33-CONTEXT.md
@.planning/phases/33-runtime-profiles-config-validation/33-RESEARCH.md
@.planning/phases/33-runtime-profiles-config-validation/33-PATTERNS.md
@.planning/phases/33-runtime-profiles-config-validation/33-VALIDATION.md
@internal/envutil/envutil.go
@internal/config/config_runtimeprofile.go
@internal/config/config_validate.go
@.claude/skills/property-based-testing/SKILL.md
</context>

## Artifacts this phase produces (phase-wide — for source-grounding; this plan creates the ones marked ★)

- `RuntimeProfile`/`ParseProfile`/`Strict()`, `Config.Profile`/`ObjectStoreReplicationFactor`/`GarageRPCSecret`, `Violation`/`Severity` — `internal/config` (plan 01, CONSUMED here)
- ★ `KnobKind` type + `KindString`/`KindInt`/`KindBool`/`KindEnum`, `KnobSpec` struct {Name, Kind, Default, Enum, Secret}, `knobRegistry() []KnobSpec`, `reparsePass(RuntimeProfile) []Violation` — `internal/config/config_knobs.go`
- ★ `TestKnobRegistry`, `TestReparsePass`, rapid invariants (strictness / no-false-positive / aggregation-monotonicity) — `internal/config/config_knobs_test.go`
- `ValidateProfile` + bespoke gates + refactored `Validate()` — `internal/config/config_validate.go` (plan 04, CONSUMES reparsePass)
- D-12 destructive flip — `internal/agent/tools/shell_exec_env.go` (plan 02)
- `configValidate` CLI — `cmd/aura/config_validate.go` (plan 05)

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: KnobSpec type + knobRegistry() (Tier A+B single source of truth) + TestKnobRegistry</name>
  <files>internal/config/config_knobs.go, internal/config/config_knobs_test.go</files>
  <read_first>
    - RESEARCH.md §"Knob Registry Catalogue" — the FULL grounded Tier A + Tier B knob list (the exact knob names, kinds, defaults, secret flags, and the AURA_PROFILE enum row). §"Code Examples → KnobSpec shape (recommended hybrid)".
    - `internal/config/config_runtimeprofile.go` (the `RuntimeProfile` enum the AURA_PROFILE KindEnum row references; plan 01) and `internal/config/config_validate.go` (`Violation`/`Severity` types; plan 01).
    - PATTERNS.md §"config_knobs.go (NEW)" (the `setConfigKey` kind-switch + `IntDefault/BoolDefault` mechanics analog; the `KnobSpec.Secret` redaction note).
    - CLAUDE.md env convention; D-16 (Tier A+B only — Tier C OUT).
  </read_first>
  <behavior>
    - `knobRegistry()` returns one `KnobSpec` per Tier A + Tier B knob from RESEARCH §"Knob Registry Catalogue"; no duplicate `Name`.
    - The `AURA_PROFILE` row is `KindEnum` with `Enum = {dev, local_trusted, single_user_hardened, server_production}`.
    - Secret knobs (`AURA_OBJECTSTORE_ACCESS_KEY`, `AURA_OBJECTSTORE_SECRET_KEY`, `GARAGE_RPC_SECRET`, `AURA_AUTHULA_SECRET`) carry `Secret: true`.
    - `AURA_AGUI_CORS_PERMISSIVE` / `AURA_OBJECTSTORE_PATH_STYLE` / `AURA_VISION_CLOUD` etc. are `KindBool`; `AURA_AGUI_BUFFER_CAP` / `AURA_OBJECTSTORE_REPLICATION_FACTOR` / the cap/timeout knobs are `KindInt`.
  </behavior>
  <action>
    Create `internal/config/config_knobs.go` (package `config`). Define `type KnobKind int` with `const ( KindString KnobKind = iota; KindInt; KindBool; KindEnum )` and `type KnobSpec struct { Name string; Kind KnobKind; Default string; Enum []string; Secret bool }`. Implement `func knobRegistry() []KnobSpec` as ONE slice literal containing every Tier A + Tier B knob enumerated in RESEARCH §"Knob Registry Catalogue" (the security-gated string/secret knobs + the `internal/config` int/bool reliability surface), each with its kind, documented default, and `Secret` flag. Add `AURA_PROFILE` as the `KindEnum` row. Do NOT add Tier C (agent-tools/loop/llm) knobs (D-16). Add `TestKnobRegistry` to `config_knobs_test.go` asserting: (a) no duplicate `Name`; (b) every `KindEnum` row has a non-empty `Enum`; (c) the four secret knobs have `Secret: true`; (d) `AURA_OBJECTSTORE_REPLICATION_FACTOR` and `AURA_AGUI_CORS_PERMISSIVE` are present with the right kinds (round-trips the QUAL-04 "every cataloged knob has a row" promise).
  </action>
  <verify>
    <automated>cd /d/Aura && go build ./internal/config/ && go test ./internal/config/ -run TestKnobRegistry -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `config_knobs.go` contains `func knobRegistry() []KnobSpec` and the `KnobKind`/`KnobSpec` type defs (`grep -c "func knobRegistry" internal/config/config_knobs.go` == 1).
    - `TestKnobRegistry` passes and asserts no-duplicate-Name + enum-non-empty + secret-flag coverage.
    - No Tier C knob name appears: `grep -E "AURA_LOOP_|AURA_LLM_|AURA_SWARM_MAX_DEPTH|AURA_FS_" internal/config/config_knobs.go` returns nothing.
    - `internal/envutil/envutil.go` is unmodified by this plan (D-06 leaf untouched).
  </acceptance_criteria>
  <done>The KnobSpec registry exists as a single source-of-truth slice (Tier A+B), with a passing structural round-trip test; envutil untouched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Generic kind-driven reparsePass + table test + 3 rapid PBT invariants</name>
  <files>internal/config/config_knobs.go, internal/config/config_knobs_test.go</files>
  <read_first>
    - RESEARCH.md §"Code Examples → Generic F-016 re-parse pass" (the exact loop: unset/whitespace → skip; severity = Fatal when `p.Strict()`, else Warn; `strconv.Atoi`/`ParseBool` + enum `slices.Contains`).
    - `internal/envutil/envutil.go` lines 22-47 (the `strconv` mechanics to mirror — same parse, opposite reaction: emit a Violation instead of silent fallback).
    - PATTERNS.md §"Test Patterns → Property-based test (PBT)" + `internal/mcp/manager/envedit_property_test.go` lines 50-85 (the in-tree `pgregory.net/rapid` template — rapid is a DIRECT dep, no install).
    - VALIDATION.md §"Property-based invariants (rapid)" (the three invariants) + §"Sampling Rate".
    - `.claude/skills/property-based-testing/SKILL.md` (rapid generator + invariant patterns); `.claude/skills/golang-testing/SKILL.md` (table-driven + -race).
  </read_first>
  <behavior>
    - For a cataloged `KindInt` knob set to a non-integer string: `reparsePass(ProfileServerProduction)` returns a Violation naming that knob with `Sev == Fatal`; `reparsePass(ProfileDev)` returns one with `Sev == Warn` (never Fatal).
    - For a cataloged `KindBool` knob set to garbage: same Fatal-under-strict / Warn-under-lenient split.
    - For a `KindEnum` knob set to a value not in `Enum`: a Violation naming it (Fatal under strict).
    - Unset OR whitespace-only value for any knob → NO violation (uses the default).
    - A valid value of the knob's kind → NO violation (no-false-positive invariant).
    - Adding a second bad knob never removes the first's violation (aggregation monotonicity).
  </behavior>
  <action>
    Add `func reparsePass(p RuntimeProfile) []Violation` to `config_knobs.go`. Choose `sev := Warn; if p.Strict() { sev = Fatal }` (D-07 — keyed on plan-01 `RuntimeProfile.Strict()`). Loop `knobRegistry()`; for each knob `os.LookupEnv(k.Name)`; if unset or `strings.TrimSpace(raw) == ""` → continue (uses default, no violation). Switch on `k.Kind`: `KindInt` → `strconv.Atoi(strings.TrimSpace(raw))`; `KindBool` → `strconv.ParseBool(strings.TrimSpace(raw))`; `KindEnum` → `slices.Contains(k.Enum, strings.TrimSpace(raw))`; `KindString` → no parse check. On parse failure append `Violation{Knob: k.Name, Sev: sev, Msg: "not a valid <kind>"}` (the message NAMES the knob — criterion #1). Do NOT touch `envutil`. In `config_knobs_test.go` add `TestReparsePass` (table over profile × knob × value asserting severity + knob name) and three `rapid.Check` tests encoding the VALIDATION invariants: (1) strictness (strict ⇒ Fatal naming the knob; lenient ⇒ at most Warn) over `rapid.SampledFrom(profiles)` × a sampled int/bool knob × `rapid.String()` garbage; (2) no-false-positive (a valid generated value of the kind ⇒ no violation for that knob); (3) aggregation-monotonicity (a second bad knob never drops the first's violation). Use `clearPostgresEnv(t)` as the baseline and `t.Setenv` to inject values.
  </action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/config/ -run 'TestReparsePass|TestRapidEnv|TestReparse' -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `config_knobs.go` contains `func reparsePass(p RuntimeProfile) []Violation` and selects severity via `p.Strict()` (`grep -c "p.Strict()" internal/config/config_knobs.go` ≥ 1).
    - `config_knobs_test.go` contains at least one `rapid.Check(` call (`grep -c "rapid.Check" internal/config/config_knobs_test.go` ≥ 1) covering the strictness invariant.
    - `go test ./internal/config/ -run 'TestReparsePass|TestRapidEnv' -race` passes — invalid int/bool under server_production yields Fatal naming the knob; under dev yields at most Warn.
    - A valid value produces zero violations (no-false-positive assertion present and green).
    - `internal/envutil/envutil.go` remains unmodified (`git diff --stat internal/envutil/` empty for this plan).
  </acceptance_criteria>
  <done>The kind-driven reparsePass exists and is proven by a table test + three rapid invariants under -race; strict ⇒ Fatal, lenient ⇒ Warn; envutil untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → re-parse validation pass | A misconfigured int/bool/enum knob (typo) is the threat. Under strict tiers it must fail fast, not silently degrade a security/reliability knob (F-016). Operator trusted; threat = accidental misconfiguration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering / Repudiation | silent int/bool fallback hiding a misconfigured security knob (F-016) | mitigate | Strict re-parse under hardened/production emits Fatal `Violation` per bad knob (PROF-04 / D-07). PBT strictness invariant proves it for every cataloged knob × garbage value. |
| T-33-03b | Information Disclosure | secret knob values surfaced by later rendering | mitigate (data here) | `KnobSpec.Secret: true` on the four secret knobs; redaction enforced by the renderer in plan 05. This plan only flags the data; it never prints knob VALUES (Violation.Msg is "not a valid <kind>", no value echoed). |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No installs (pure-Go; rapid is a pre-existing direct dep). Legitimacy gate N/A. |

ASVS: V5 Validation (stdlib `strconv`, no hand-rolled parser), V14 Configuration (fail-fast in strict tiers), V8 Data Protection (Secret flag; no value echoed in messages).
</threat_model>

<verification>
- `go test ./internal/config/ -race -count=1` green (registry + reparse + rapid).
- `internal/envutil` unmodified; no Tier C knobs catalogued.
- Each rapid invariant green over default rapid iterations; no flaky seeds (run twice if needed per VALIDATION).
</verification>

<success_criteria>
- KnobSpec registry is the single source of truth (Tier A+B), structurally validated (QUAL-04).
- Generic reparsePass: invalid int/bool/enum ⇒ Fatal under strict, Warn under lenient, naming the knob (PROF-04 / criterion #3 engine).
- Three rapid invariants (strictness, no-false-positive, aggregation-monotonicity) green under -race.
</success_criteria>

<output>
Create `.planning/phases/33-runtime-profiles-config-validation/33-03-SUMMARY.md` when done.
</output>
