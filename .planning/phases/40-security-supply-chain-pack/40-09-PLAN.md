---
phase: 40-security-supply-chain-pack
plan: 09
type: tdd
wave: 3
depends_on: ["40-01", "40-07"]
files_modified:
  - internal/tracesink/sink.go
  - internal/tracesink/sink_test.go
  - internal/reasoningtrace/reasoningtrace.go
  - internal/reasoningtrace/reasoningtrace_test.go
  - internal/config/config_validate.go
  - internal/config/config_validate_test.go
  - internal/config/config_knobs.go
autonomous: true
requirements: [SEC-01]
user_setup: []

must_haves:
  truths:
    - "internal/reasoningtrace redactString no longer uses the inline 4-marker (KEY/TOKEN/PASSWORD/SECRET) check; it calls the canonical secret.IsSecretEnvKey/IsSecretEnvVar, so AURA_DB_URL's value (whose key lacks those markers) is redacted in full-trace mode."
    - "reasoningtrace.Record folds in a redact.String pattern pass (the OUTBOUND regex redactor) so a literal postgres://user:pass@host DSN typed into any free-text/error trace field is redacted — exactly one redaction pass per surface (no double-redaction of existing [REDACTED_*]/[capped] markers)."
    - "config_validate.go gains gateReasoningTraceFull(p) mirroring gateDestructiveShell: under RuntimeProfile.Strict(), AURA_REASONING_TRACE=full is Fatal unless AURA_TRACE_FULL_ACK=1; it is a no-op under dev/local_trusted; it is registered in ValidateProfile's aggregation."
    - "config_knobs.go registers the three net-new knobs: AURA_REASONING_TRACE (enum off/summary/full or string), AURA_TRACE_FULL_ACK (bool, default false), AURA_TRACE_ENCRYPT_KEY (string, Secret:true)."
    - "internal/tracesink is a new package providing an encrypted full-trace file sink: AES-256-GCM with a fresh random nonce per record (nonce-prefixed framing), key = HKDF(AURA_TRACE_ENCRYPT_KEY, aura-reasoning-trace-sink-v1); when full-trace is acked but the key is absent/malformed the sink REFUSES to write (fail-closed), never a plaintext fallback."
    - "The encrypted sink is wired into the reasoningtrace full-mode write path so an acked full trace is written encrypted (and refused when the key is missing)."
    - statement: "gateReasoningTraceFull is a no-op under dev/local_trusted (returns nil) and Fatal under Strict() when AURA_REASONING_TRACE=full without AURA_TRACE_FULL_ACK=1 — confirm the exact profile matrix matches gateDestructiveShell/gateWebAuth precedent."
      verification: backstop
    - statement: "Two encrypts of the same plaintext produce different ciphertext (fresh nonce per record); a decrypt round-trip recovers the plaintext; a truncated ciphertext (shorter than NonceSize) returns an error, not a panic — confirm the framing boundary."
      verification: backstop
    - statement: "When AURA_REASONING_TRACE=full is acked but AURA_TRACE_ENCRYPT_KEY is absent/malformed, the sink drops the record (logs a warning once) rather than writing plaintext — confirm there is exactly one write path and it is unreachable without a valid key."
      verification: backstop
  artifacts:
    - path: "internal/tracesink/sink.go"
      provides: "AES-256-GCM fresh-nonce-per-record encrypted trace sink, HKDF key, fail-closed on missing key"
      contains: "NonceSize"
    - path: "internal/reasoningtrace/reasoningtrace.go"
      provides: "redactString using the canonical secret predicate + redact.String folded into Record"
      contains: "secret.IsSecret"
  key_links:
    - from: "internal/reasoningtrace/reasoningtrace.go"
      to: "secret.IsSecretEnvVar + redact.String"
      via: "canonical predicate replaces the 4-marker check; pattern pass folded into Record"
      pattern: "secret\\.IsSecret|redact\\.String"
    - from: "internal/config/config_validate.go"
      to: "gateReasoningTraceFull"
      via: "registered in ValidateProfile; Fatal unless AURA_TRACE_FULL_ACK=1 under Strict()"
      pattern: "gateReasoningTraceFull"
  prohibitions:
    - "Do NOT fall back to plaintext when AURA_TRACE_ENCRYPT_KEY is missing/malformed — the sink MUST fail closed (refuse to write), or it reopens the F-021 cleartext-on-disk exposure (D-13)."
    - "Do NOT reuse one nonce across records — GCM fails catastrophically on nonce reuse; call rand.Read(nonce) inside EVERY encrypt, never once at sink construction (D-13)."
    - "Do NOT derive the sink key from the raw AURA_TRACE_ENCRYPT_KEY without HKDF — use hkdf.Key with the domain-separated label aura-reasoning-trace-sink-v1 (D-13)."
    - "Do NOT double-redact — keep exactly ONE redaction pass per surface; do not re-run the exact-match over already-[REDACTED_*]/[capped] output (D-11)."
    - "Do NOT invent a fourth secret-marker list in reasoningtrace — call the canonical secret.IsSecretEnvKey/IsSecretEnvVar (D-11)."
    - "Do NOT add filippo.io/age — golang.org/x/crypto + stdlib crypto/aes+cipher+hkdf are already available (RESEARCH: zero new deps)."
    - "Do NOT import the unexported IdentityStore encrypt/decrypt methods — write a fresh ~20-line implementation in internal/tracesink (D-13)."
---

<objective>
Close the trace half of SEC-01 (D-11/D-12/D-13, F-021): (1) FIX the confirmed reasoningtrace redaction bug —
redactString only exact-matches env values whose KEY contains KEY/TOKEN/PASSWORD/SECRET, so AURA_DB_URL's value
and a literal DSN in a free-text field both LEAK in full-trace mode; align it to the canonical
secret.IsSecretEnvKey/IsSecretEnvVar and fold a redact.String pattern pass into Record (one pass, no double-
redaction). (2) Add a prod posture gate gateReasoningTraceFull (Fatal unless AURA_TRACE_FULL_ACK=1 under Strict())
plus three new KnobSpec rows. (3) Build a REAL encrypted full-trace file sink (internal/tracesink): AES-256-GCM,
fresh random nonce per record, HKDF-derived key from a net-new AURA_TRACE_ENCRYPT_KEY, FAIL-CLOSED when the key is
absent (never a plaintext fallback), and wire it into the acked full-trace write path.

Purpose: full reasoning-trace mode requires an explicit prod ack, redacts Aura's configured secrets + credential
DSNs outbound, and persists only encrypted — no cleartext reasoning trace at rest.
Output: internal/tracesink/sink.go(+test) + reasoningtrace.go(+test) + config_validate.go + config_knobs.go.

Depends on 40-01 (SEC-01 amendment) and 40-07 (config_validate.go + config_knobs.go file conflict — 40-07 removes
the CORS gate/knob; this plan adds the trace gate/knobs; sequence after it). TDD: the sink is a pure crypto
feature — RED round-trip + fail-closed test first. Run tests in WSL; the sink + gate tests are daemon-free unit.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-security-supply-chain-pack/40-CONTEXT.md
@.planning/phases/40-security-supply-chain-pack/40-PATTERNS.md
@internal/reasoningtrace/reasoningtrace.go
@internal/objectstore/identity_store.go
@internal/config/config_validate.go
</context>

## Artifacts this phase produces (this plan)

- `internal/tracesink/sink.go` — NEW package: AES-256-GCM encrypted sink (fresh nonce/record), HKDF key from `AURA_TRACE_ENCRYPT_KEY`, fail-closed on missing key.
- `internal/tracesink/sink_test.go` — NEW: `TestSink` (round-trip, fresh-nonce, fail-closed-on-bad-key).
- `internal/reasoningtrace/reasoningtrace.go` — `redactString` uses `secret.IsSecretEnvKey/IsSecretEnvVar`; `Record` folds `redact.String`; wires the sink for acked full mode.
- `internal/reasoningtrace/reasoningtrace_test.go` — NEW/extended: `TestRecordRedaction` (AURA_DB_URL value + literal DSN redacted).
- `internal/config/config_validate.go` — NEW `gateReasoningTraceFull(p)` + its ValidateProfile registration.
- `internal/config/config_validate_test.go` — NEW `TestGateReasoningTraceFull`.
- `internal/config/config_knobs.go` — three NEW KnobSpec rows (`AURA_REASONING_TRACE`, `AURA_TRACE_FULL_ACK`, `AURA_TRACE_ENCRYPT_KEY`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: internal/tracesink encrypted sink — test-first (round-trip + fresh-nonce + fail-closed)</name>
  <files>internal/tracesink/sink.go, internal/tracesink/sink_test.go</files>
  <read_first>
    - internal/objectstore/identity_store.go:155-176 (encrypt/decrypt fresh-nonce framing to mirror — UNEXPORTED, so rewrite ~20 lines) + :192-208 (deriveObjectStoreKey HKDF + fail-closed 64-hex check)
    - 40-CONTEXT.md D-13; 40-RESEARCH.md Pattern 2 (AES-GCM fresh-nonce) + Pitfall 4 (plaintext fallback reopens F-021); 40-PATTERNS.md "SEC-01 tracesink"
  </read_first>
  <behavior>
    - encrypt then decrypt round-trips a plaintext record back to the original.
    - Two encrypts of the same plaintext produce DIFFERENT ciphertext (fresh random nonce per record).
    - A ciphertext shorter than NonceSize returns an error (not a panic).
    - Key derivation: HKDF(AURA_TRACE_ENCRYPT_KEY 64-hex, label aura-reasoning-trace-sink-v1) succeeds; a non-64-hex key returns an error (fail closed).
    - The sink Write refuses (returns an error / drops with one warning) when the key is absent/malformed — never writes plaintext.
  </behavior>
  <action>
    FIRST write internal/tracesink/sink_test.go (TestSink) asserting the behavior rows against the not-yet-existing
    package; confirm RED. THEN create internal/tracesink/sink.go: a key-derivation function mirroring
    deriveObjectStoreKey (TrimSpace, len==64, hex.DecodeString, hkdf.Key(sha256.New, raw, nil,
    "aura-reasoning-trace-sink-v1", 32), fail closed); a sink constructor that builds an AES-256-GCM cipher.AEAD
    from the derived key and returns an error when the key is absent/malformed (so the sink is unconstructable
    without a valid key); an append/Write method that seals each record with a FRESH rand nonce (nonce :=
    make([]byte, aead.NonceSize()); rand.Read(nonce); aead.Seal(nonce, nonce, plaintext, nil)) — one write path,
    nonce-prefixed; a decrypt helper for the test (nonce, ct := ct[:ns], ct[ns:]; aead.Open). NO plaintext branch
    anywhere. Confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test ./internal/tracesink/ -run TestSink -count=1 -v && go test -race ./internal/tracesink/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/tracesink/sink.go builds an AES-256-GCM AEAD from an HKDF-derived key (label aura-reasoning-trace-sink-v1) and returns an error on an absent/malformed key.
    - TestSink proves: round-trip, fresh-nonce (two encrypts differ), short-ciphertext error, fail-closed on bad key.
    - There is exactly one write path and no plaintext-write branch (rg finds no unencrypted os.WriteFile of the record).
    - go test -race ./internal/tracesink/ green; package coverage >=85%.
  </acceptance_criteria>
  <done>The encrypted sink round-trips with a fresh nonce per record, derives its key via HKDF, and fails closed on a missing key — no plaintext path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix reasoningtrace redaction (D-11) + wire the sink for acked full mode</name>
  <files>internal/reasoningtrace/reasoningtrace.go, internal/reasoningtrace/reasoningtrace_test.go</files>
  <read_first>
    - internal/reasoningtrace/reasoningtrace.go:232-248 (redactString — the 4-marker inline check to replace; secret package NOT imported) + :90-125 (Record — the write path; fold redact.String here, wire the sink for acked full mode)
    - internal/reasoningtrace/reasoningtrace_test.go (EXISTING — 3 pre-existing tests: TestRecord_RotatesAtCap plus the redaction-mode precedents TestRecord_DefaultOmitsVerbatimHistoryAndUser / TestRecord_FullModeAllowsVerbatimHistory; mirror their t.Setenv + trace-mode fixture style and PRESERVE all three when adding TestRecordRedaction)
    - internal/secret/envkey.go:65-99 (IsSecretEnvKey/IsSecretEnvVar — the canonical predicate) ; internal/redact/string.go (String — the outbound pattern pass to fold in)
    - internal/tracesink/sink.go (Task 1 — the sink to wire for acked full mode)
    - 40-CONTEXT.md D-11/D-12/D-13; 40-PATTERNS.md "SEC-01 reasoningtrace.go" section
  </read_first>
  <behavior>
    - A trace row containing AURA_DB_URL's value (key lacks KEY/TOKEN/PASSWORD/SECRET markers) is redacted (was leaking).
    - A literal postgres://user:pass@host DSN in a free-text/error field is redacted by the folded-in redact.String pass.
    - Exactly one redaction pass per surface — an already-[REDACTED_*] / [capped] marker is not re-redacted / double-wrapped.
    - When full-trace is acked, the record is written via the encrypted sink; when the key is absent, the record is dropped (fail-closed), not written plaintext.
  </behavior>
  <action>
    In reasoningtrace.go: replace redactString's inline 4-marker check with the canonical
    secret.IsSecretEnvKey(name)/secret.IsSecretEnvVar(name, value) predicate (import internal/secret) so keys like
    AURA_DB_URL are caught; fold a redact.String(...) pattern pass into the Record write path so literal
    credential URLs in free-text fields are also redacted — keep it to ONE pass per surface (do not re-run the
    exact-match over already-redacted output; guard against double-wrapping [REDACTED_*]/[capped]). Wire the
    internal/tracesink sink into the full-mode write path: when AURA_REASONING_TRACE=full is active (and acked per
    the Task 3 gate), write the (already-redacted) record through the encrypted sink instead of plaintext
    os.OpenFile; if AURA_TRACE_ENCRYPT_KEY is absent/malformed, drop the record with a single warning (fail-closed),
    never plaintext. Add reasoningtrace_test.go TestRecordRedaction asserting the AURA_DB_URL-value and literal-DSN
    cases are redacted and that markers are not double-redacted.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go build ./internal/reasoningtrace/ && go test ./internal/reasoningtrace/ -run TestRecordRedaction -count=1 -v && go test -race ./internal/reasoningtrace/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - reasoningtrace.go imports internal/secret and calls IsSecretEnvKey/IsSecretEnvVar (rg confirms); the 4-marker inline check is gone.
    - Record folds in redact.String (rg confirms) and writes acked full traces via internal/tracesink (fail-closed on missing key).
    - TestRecordRedaction proves AURA_DB_URL's value and a literal DSN are redacted, and no double-redaction of existing markers.
    - go test -race ./internal/reasoningtrace/ green.
  </acceptance_criteria>
  <done>The reasoningtrace leak is fixed (canonical predicate + folded pattern pass, one pass per surface) and acked full traces persist only through the encrypted, fail-closed sink.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gateReasoningTraceFull prod gate + three new KnobSpec rows</name>
  <files>internal/config/config_validate.go, internal/config/config_validate_test.go, internal/config/config_knobs.go</files>
  <read_first>
    - internal/config/config_validate.go:232-247 (gateDestructiveShell — the exact template) + :249-260 (gateWebAuth — the Strict() early-return shape) + :88-105 (ValidateProfile aggregation to register the new gate)
    - internal/config/config_validate_test.go:157-182 (TestGateDestructiveShell — the t.Setenv table-test template)
    - internal/config/config_knobs.go:44-50 (KnobSpec struct) + :72 (AURA_AUTHULA_SECRET Secret:true row shape)
    - internal/reasoningtrace/reasoningtrace.go:21-23 (Env const AURA_REASONING_TRACE / file / max-bytes knobs already read there)
    - 40-CONTEXT.md D-12; 40-PATTERNS.md "SEC-01 config_validate.go + config_knobs.go" + "Profile-Gated Fatal-Unless-Acked Validation"
  </read_first>
  <behavior>
    - Under dev/local_trusted: gateReasoningTraceFull returns nil (no-op) regardless of AURA_REASONING_TRACE.
    - Under Strict() (single_user_hardened / server_production): AURA_REASONING_TRACE=full without AURA_TRACE_FULL_ACK=1 is Fatal; with AURA_TRACE_FULL_ACK=1 it is allowed; a non-full value is fine.
    - The gate is registered in ValidateProfile (surfaces in the one-pass aggregation, never first-fails).
    - The three knobs appear in the KnobSpec registry (AURA_TRACE_ENCRYPT_KEY marked Secret:true).
  </behavior>
  <action>
    Add gateReasoningTraceFull(p RuntimeProfile) []Violation to config_validate.go mirroring gateDestructiveShell:
    early-return nil unless p.Strict(); read AURA_REASONING_TRACE raw (os.Getenv); if it equals "full" and
    strings.TrimSpace(os.Getenv("AURA_TRACE_FULL_ACK")) != "1", return a Fatal Violation naming AURA_REASONING_TRACE
    (message: full trace requires AURA_TRACE_FULL_ACK=1 under the profile); else nil. Register it in ValidateProfile's
    append list. Add three KnobSpec rows to config_knobs.go: AURA_REASONING_TRACE (KindEnum with off/summary/full if
    a fixed set, else KindString), AURA_TRACE_FULL_ACK (KindBool, Default "false"), AURA_TRACE_ENCRYPT_KEY
    (KindString, Default "", Secret: true — matching the AURA_AUTHULA_SECRET row shape). Add
    TestGateReasoningTraceFull to config_validate_test.go (t.Setenv table: dev+full no-op, strict+full-no-ack Fatal,
    strict+full+ack ok, strict+summary ok).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go build ./internal/config/ && go test ./internal/config/ -run TestGateReasoningTraceFull -count=1 -v && rg -n "AURA_REASONING_TRACE|AURA_TRACE_FULL_ACK|AURA_TRACE_ENCRYPT_KEY" internal/config/config_knobs.go</automated>
  </verify>
  <acceptance_criteria>
    - config_validate.go has gateReasoningTraceFull registered in ValidateProfile; it is a no-op under non-strict profiles and Fatal for full-without-ack under Strict().
    - config_knobs.go has the three new rows; AURA_TRACE_ENCRYPT_KEY is Secret:true.
    - TestGateReasoningTraceFull covers dev-no-op, strict-full-no-ack-Fatal, strict-full-ack-ok, strict-summary-ok; green.
    - go build ./internal/config/ clean; go test ./internal/config/ green.
  </acceptance_criteria>
  <done>Full-trace mode fails closed under prod unless explicitly acked; the three net-new knobs are registered (encrypt key marked Secret); gate proven by the profile table test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reasoning trace (may carry prompts/credentials/PII) → disk at rest | Full-trace mode persists sensitive content; the ack gate + encrypted sink + redaction are the boundary controls. |
| operator env (AURA_TRACE_ENCRYPT_KEY) → sink key | The sink key is derived from operator-provided key material; absence must fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-09-LEAK | Information Disclosure | AURA_DB_URL value / literal DSN leaking in full-trace | mitigate | redactString aligned to canonical secret predicate + redact.String folded into Record (D-11) — the confirmed bug fixed. |
| T-40-09-CLEARTEXT | Information Disclosure | cleartext reasoning trace at rest (F-021) | mitigate | Encrypted sink (AES-256-GCM, HKDF key) is the only write path; fail-closed on missing key — no plaintext fallback (D-13). |
| T-40-09-NONCE | Information Disclosure (crypto) | GCM nonce reuse across records | mitigate | Fresh rand nonce per encrypt call (never once per file); unit-proven two-encrypts-differ (D-13). |
| T-40-09-PRODON | Information Disclosure | full-trace silently enabled under prod | mitigate | gateReasoningTraceFull Fatal unless AURA_TRACE_FULL_ACK=1 under Strict(); no-op under dev (D-12). |
| T-40-09-KEYREUSE | Tampering (crypto) | sink key = raw AURA_TRACE_ENCRYPT_KEY | mitigate | HKDF domain-separated label aura-reasoning-trace-sink-v1; never the raw key (D-13). |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib crypto/aes+cipher+hkdf + existing internal/redact+secret only. |
</threat_model>

<verification>
- go test -race ./internal/tracesink/ (round-trip + fresh-nonce + fail-closed) and ./internal/reasoningtrace/ (redaction) green (WSL).
- go test ./internal/config/ -run TestGateReasoningTraceFull green; rg confirms the three knobs registered (encrypt key Secret:true).
- rg confirms reasoningtrace imports internal/secret + folds redact.String; the 4-marker inline check is gone; no plaintext write branch in tracesink.
- Owned-surface coverage for internal/tracesink >=85%; go build ./... clean after the config edits.
</verification>

<success_criteria>
The trace half of SEC-01 is closed: the reasoningtrace leak is fixed (canonical secret predicate + folded
outbound pattern pass, one pass per surface), full-trace mode fails closed under prod unless explicitly acked
(gateReasoningTraceFull + three knobs), and acked full traces persist only through a real AES-256-GCM encrypted
sink with a fresh nonce per record and a fail-closed missing-key policy — no cleartext reasoning trace at rest.
</success_criteria>

<output>
Create `.planning/phases/40-security-supply-chain-pack/40-09-SUMMARY.md` when done.
</output>
