---
phase: 40-security-supply-chain-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - prd.md
autonomous: true
requirements: [SEC-01, SEC-02, SEC-09]
user_setup: []

must_haves:
  truths:
    - "REQUIREMENTS.md SEC-09 no longer says 'salted KDF'; it reads keyed-hash-or-documented-FP (HMAC-SHA-256 with a server-side pepper where CodeQL accepts it, else a documented 256-bit-CSPRNG false-positive dismissal), per D-08."
    - "REQUIREMENTS.md SEC-01 is scoped: Aura's CONFIGURED secrets are redacted at every at-rest surface (turn body, spill, tool-result sidecar); unknown secret-like values are the agent's working data and are pattern-redacted only OUTBOUND (display/export/ledger/trace), per D-10b."
    - "REQUIREMENTS.md SEC-02 reads same-origin-only (no CORS headers); the allowlist/Vary/methods-sync wording is removed, per D-14b."
    - "ROADMAP.md Phase 40 success criterion 5 uses the keyed-hash-or-documented-FP wording (no 'strong salted KDF')."
    - "ROADMAP.md Phase 40 success criterion 2 reflects configured-secrets-at-rest redaction and same-origin CORS removal (no 'permissive CORS refused when auth is disabled' allowlist framing)."
    - "prd.md carries a dated amendment note superseding the permissive-CORS text (§~2791 and the AURA_AGUI_CORS_PERMISSIVE knob row §~5297) with same-origin-only, and recording the SEC-09 keyed-hash and SEC-01 configured-secret-scope decisions."
    - "Each of the three amendments is its OWN atomic commit landed BEFORE any SEC-01/SEC-02/SEC-09 code commit (PRD-first §Q&A revision protocol)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended SEC-01/SEC-02/SEC-09 requirement wording"
      contains: "keyed hash"
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 40 success criteria 2 and 5"
      contains: "same-origin"
    - path: "prd.md"
      provides: "Dated amendment note (CORS same-origin supersede + SEC-09/SEC-01 decisions)"
      contains: "same-origin"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "40-CONTEXT.md D-08/D-10b/D-14b"
      via: "requirement text matches the locked amendment wording"
      pattern: "keyed hash|same-origin|configured secrets"
  prohibitions:
    - "Do NOT amend SEC-09 toward 'salted KDF'/'random-salt argon2id' — a randomly-salted KDF cannot serve a lookup-by-hash PRIMARY KEY (rejected in D-08)."
    - "Do NOT reintroduce the CORS allowlist / Vary / methods-sync wording for SEC-02 — same-origin-only is the locked amendment (D-14b)."
    - "Do NOT land any SEC-01/SEC-02/SEC-09 CODE in this plan — this plan is docs-only; the code plans (40-06/40-07/40-08/40-09) depend on it."
    - "Do NOT touch SEC-03/04/05/06 requirement text — only SEC-01/02/09 are amended this phase."
---

<objective>
Land the three mandatory REQUIREMENTS/ROADMAP/PRD amendments that the discuss+adversarial pass produced,
BEFORE any of their code ships (PRD-first §Q&A revision protocol). Each amendment is its own atomic commit
so the SEC-01/SEC-02/SEC-09 code plans (40-06/40-07/40-08/40-09) build on already-corrected requirements:

1. **SEC-09 (D-08):** "remediated with a strong salted KDF and the alert resolves" is architecturally
   impossible (a random-salt KDF cannot back a `token_hash` PRIMARY-KEY lookup). Amend to keyed-hash
   (HMAC-SHA-256 + server-side pepper) where CodeQL accepts it, OR a documented 256-bit-CSPRNG false-positive
   dismissal — either resolves the alert.
2. **SEC-01 (D-10b):** scope "redact secret-like values before persistence" to *Aura's configured secrets at
   every at-rest surface (turn body, spill, tool-result sidecar)*; unknown secret-like values are the agent's
   working data and are pattern-redacted only on the OUTBOUND surface (display/export/ledger/trace).
3. **SEC-02 (D-14b):** "explicit origin allowlists + Vary/methods-sync" → *same-origin only, no CORS headers*
   (no production cross-origin consumer exists; SameSite=Strict already blocks cross-site cookies).

Purpose: honor the project's absolute PRD-first rule so no code contradicts a stale requirement.
Output: 3 atomic amendment commits across REQUIREMENTS.md, ROADMAP.md, and prd.md.

Execute sequentially, no git worktrees (memory: execute-phase runs serial on master). Use direct `git commit`
(the gsd commit wrapper times out on the file-size hook). This plan runs before all amended-requirement code.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-security-supply-chain-pack/40-CONTEXT.md
@.planning/phases/40-security-supply-chain-pack/40-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- `.planning/REQUIREMENTS.md` — amended SEC-01, SEC-02, SEC-09 bullet text (no new symbols; docs).
- `.planning/ROADMAP.md` — amended Phase 40 success criteria 2 and 5 (docs).
- `prd.md` — new dated amendment note block (CORS same-origin supersede + SEC-09/SEC-01 decisions) (docs).

<tasks>

<task type="auto">
  <name>Task 1: Amend SEC-09 wording (D-08) — keyed-hash-or-documented-FP</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, prd.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:139 (current SEC-09 bullet — "cryptographically strong, salted KDF/hash")
    - .planning/ROADMAP.md:737 (success criterion 5 — "strong salted KDF and the alert resolves")
    - .planning/phases/40-security-supply-chain-pack/40-CONTEXT.md:42,76 (D-08 exact target wording + "Rejected" list)
    - .planning/phases/40-security-supply-chain-pack/40-PATTERNS.md:1038-1051 (the current-wording-to-amend excerpts)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md SEC-09 (line ~139): replace "a cryptographically strong, salted KDF/hash
    rather than a weak/fast hash" with the D-08 target: "remediated with a keyed hash (HMAC-SHA-256 with a
    server-side pepper) where CodeQL accepts it, OR dismissed as a documented false positive (256-bit CSPRNG
    token, not a password / no limited input space), such that the SEC-09 alert resolves." In
    .planning/ROADMAP.md success criterion 5 (line ~737): replace "remediated with a strong salted KDF and the
    alert resolves" with the same keyed-hash-or-documented-FP wording. In prd.md: add a dated amendment note
    (append to the amendment log / near the persistence section) recording that a lookup-by-hash PRIMARY KEY
    (`token_hash`) precludes a randomly-salted KDF, so the SEC-09 remediation is HMAC-SHA-256 + HKDF-derived
    pepper (or documented FP). Commit as its own atomic commit: `docs(40): amend SEC-09 to keyed-hash-or-FP (D-08)`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && grep -q "keyed hash" .planning/REQUIREMENTS.md && grep -qi "salted KDF" .planning/ROADMAP.md && test $? -ne 0 || ! grep -qi "strong salted KDF" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` SEC-09 line contains "keyed hash" and "256-bit CSPRNG"; no longer contains "salted KDF".
    - `.planning/ROADMAP.md` success criterion 5 no longer contains "strong salted KDF".
    - `prd.md` contains a dated SEC-09 amendment note referencing the `token_hash` PRIMARY-KEY constraint.
    - `git log --oneline -1` shows a commit whose subject cites D-08 / SEC-09, touching only these 3 docs.
  </acceptance_criteria>
  <done>SEC-09 requirement + roadmap criterion + PRD reflect keyed-hash-or-documented-FP; landed as an atomic commit before any SEC-09 code.</done>
</task>

<task type="auto">
  <name>Task 2: Amend SEC-01 scope (D-10b) — configured secrets at every at-rest surface</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, prd.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:131 (current SEC-01 bullet)
    - .planning/ROADMAP.md:734 (success criterion 2)
    - .planning/phases/40-security-supply-chain-pack/40-CONTEXT.md:50,77 (D-10b exact target wording)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md SEC-01 (line ~131): keep the full-trace-mode + retention + encrypted-sink
    clause, and scope the redaction clause to the D-10b wording: "Aura's CONFIGURED secrets are redacted before
    persistence at every at-rest surface (turn body, spill, tool-result sidecar); unknown secret-like values in
    tool output are the agent's working data and are pattern-redacted only on the OUTBOUND surface
    (display/export/ledger/trace)." In .planning/ROADMAP.md success criterion 2 (line ~734): adjust the redaction
    half to "Aura's configured secrets are redacted before persistence at every at-rest surface" (leave the CORS
    half for Task 3). In prd.md: add the SEC-01 configured-secret-scope note to the same dated amendment block,
    citing the exact-match-inbound vs pattern-outbound split (consistent with LibreChat store-faithful/redact-at-
    export). Commit atomically: `docs(40): scope SEC-01 to configured-secrets-at-rest (D-10b)`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && grep -qi "configured secrets" .planning/REQUIREMENTS.md && grep -qi "tool-result sidecar" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` SEC-01 contains "configured secrets", "tool-result sidecar", and "OUTBOUND".
    - `.planning/ROADMAP.md` criterion 2 redaction half references configured secrets at rest.
    - `prd.md` contains the SEC-01 exact-match-inbound / pattern-outbound scope note.
    - `git log --oneline -1` shows an atomic commit citing D-10b / SEC-01, touching only these 3 docs.
  </acceptance_criteria>
  <done>SEC-01 requirement + roadmap + PRD scoped to configured-secrets-at-rest; atomic commit landed before SEC-01 code.</done>
</task>

<task type="auto">
  <name>Task 3: Amend SEC-02 (D-14b) — same-origin only + PRD CORS supersede</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, prd.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:132 (current SEC-02 bullet — allowlist/Vary/methods-sync)
    - .planning/ROADMAP.md:734 (success criterion 2, CORS half)
    - .planning/phases/40-security-supply-chain-pack/40-CONTEXT.md:57,78 (D-14b exact target wording)
    - prd.md:2791 (permissive-CORS dev text), prd.md:5297 (AURA_AGUI_CORS_PERMISSIVE knob row)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md SEC-02 (line ~132): replace the allowlist/Vary/methods-sync text with the
    D-14b wording: "Wildcard/permissive CORS is removed; the cockpit is served same-origin only (no CORS
    headers). Introducing any cross-origin consumer later requires an explicit credentialed allowlist AND the
    auth.go SameSite/CSRF re-evaluation." In .planning/ROADMAP.md criterion 2 (CORS half): "permissive CORS is
    removed; the cockpit is served same-origin only." In prd.md: add a dated amendment note that SUPERSEDES the
    §~2791 permissive-CORS-for-dev text and marks the `AURA_AGUI_CORS_PERMISSIVE` knob row (§~5297) as removed,
    stating the same-origin-only rationale (go:embed same-origin dist, no prod cross-origin consumer,
    SameSite=Strict+Secure already blocks cross-site cookies; a dev-only Vite :5173→:9080 workflow is a
    documented dev-profile escape, never a prod path). Commit atomically:
    `docs(40): amend SEC-02 to same-origin-only, supersede PRD CORS (D-14b)`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && grep -qi "same-origin only" .planning/REQUIREMENTS.md && ! grep -qi "sets \`Vary: Origin\`" .planning/REQUIREMENTS.md && grep -qi "same-origin" prd.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` SEC-02 contains "same-origin only" and "no CORS headers"; no longer contains "allowlists" or "Vary: Origin".
    - `.planning/ROADMAP.md` criterion 2 CORS half says permissive CORS is removed / same-origin only.
    - `prd.md` contains a dated note superseding the §2791 CORS text and marking `AURA_AGUI_CORS_PERMISSIVE` removed.
    - `git log --oneline -1` shows an atomic commit citing D-14b / SEC-02, touching only these 3 docs.
  </acceptance_criteria>
  <done>SEC-02 requirement + roadmap + PRD reflect same-origin-only; atomic commit landed before SEC-02 code (40-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only amendment. No runtime trust boundary changes; this plan gates code that changes them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-STALE | Tampering (of the spec) | requirement drift between docs and code | mitigate | Amendment commits land BEFORE code (PRD-first); each code plan (40-06..09) depends_on this plan. |
| T-40-01-OVERCLAIM | Repudiation | over-claiming SEC-01/02/09 acceptance | mitigate | Scope wording (D-08/D-10b/D-14b) prevents claiming resistance/allowlist/salted-KDF that the code does not deliver. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs — docs-only plan, nothing to audit. |
</threat_model>

<verification>
- All three amendment strings present in REQUIREMENTS.md / ROADMAP.md / prd.md (grep assertions above).
- Three atomic commits in `git log` (one per D-08/D-10b/D-14b), each touching only the 3 docs.
- No code file modified by this plan (`git show --stat` on each commit lists only .planning/*.md + prd.md).
</verification>

<success_criteria>
The three mandatory REQUIREMENTS/ROADMAP/PRD amendments (SEC-09 keyed-hash-or-FP, SEC-01 configured-secrets-at-rest,
SEC-02 same-origin-only) are landed as atomic commits before any SEC-01/SEC-02/SEC-09 code, so downstream plans
build on corrected requirements and cannot over-claim.
</success_criteria>

<output>
Create `.planning/phases/40-security-supply-chain-pack/40-01-SUMMARY.md` when done.
</output>
