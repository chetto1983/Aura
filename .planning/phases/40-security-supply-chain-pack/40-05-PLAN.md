---
phase: 40-security-supply-chain-pack
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/codeql.yml
  - .github/workflows/release.yml
  - .github/workflows/skills.yml
  - .goreleaser.yaml
  - scripts/workflow_pin_gate.sh
  - scripts/workflow_pin_gate_test.sh
  - go.mod
  - go.sum
autonomous: true
requirements: [SEC-05]
user_setup: []

must_haves:
  truths:
    - "Every uses: reference across ci.yml, codeql.yml, release.yml, skills.yml is pinned to a 40-hex commit SHA with a trailing # vX.Y.Z comment (Dependabot-updatable); no floating @vN tags remain."
    - "github/codeql-action/init and github/codeql-action/analyze are pinned to the IDENTICAL SHA."
    - "The three @latest tool installs are pinned to exact versions: deadcode (ci.yml:80), govulncheck (ci.yml:160), go-mutesting (skills.yml:133); no `go install ...@latest` remains anywhere (including run: | block bodies)."
    - ".goreleaser.yaml has a sboms: block emitting BOTH a binary/archive SBOM (artifacts: archive, Go-modules) AND a source SBOM (artifacts: source, covering web/package-lock.json), SPDX-JSON, per release; release.yml installs syft via a SHA-pinned anchore/sbom-action/download-syft step BEFORE the goreleaser step."
    - "scripts/workflow_pin_gate.sh is a blocking CI gate that FAILS when any uses: is not @<40-hex> # vX.Y.Z (allowing multi-segment paths like github/codeql-action/init, local ./..., and docker://...@sha256:...) OR when any go install ...@latest appears (scanning indented run: | bodies), while NOT false-flagging goreleaser-action's version: input or the pinned @v2.12.2/@v1.31.1/pipx== installs."
    - "scripts/workflow_pin_gate_test.sh is a fixture-based self-test (tmpdir + rc assertions, mirroring quality_snapshot_gate_test.sh) that proves the gate accepts a correctly-pinned file and rejects (a) a multi-segment unpinned ref, (b) a go install @latest inside a run: | block, and (c) confirms the version: input is NOT flagged."
    - "govulncheck stays reachable-any = block (the vulncheck job runs govulncheck over scripts/go_packages.sh with no flags), and the govulncheck tool is pinned to an exact version (v1.6.0 or the current resolved version)."
    - "A ref pinned to a 40-hex SHA with a trailing # vX.Y.Z passes; a bare SHA without the comment fails; a floating @vN fails; a local ./action and docker://img@sha256:... are allowed (SEC-05 adjacency edge — resolved covered)."
    - "The gate scans all four workflow files; a file with zero uses: lines passes; run: | block bodies are scanned for go install @latest (SEC-05 empty edge — resolved covered)."
    - statement: "codeql init and analyze must be pinned to the identical SHA — grep cannot enforce cross-line equality, so this is a manual review-checklist item captured in the SUMMARY, not an automated gate assertion."
      verification: backstop
  artifacts:
    - path: "scripts/workflow_pin_gate.sh"
      provides: "Blocking SHA-pin + no-@latest workflow lint gate"
      contains: "uses:"
    - path: ".goreleaser.yaml"
      provides: "sboms: block (archive + source) for binary + web/ coverage"
      contains: "sboms"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "anchore/sbom-action/download-syft"
      via: "SHA-pinned syft install step before goreleaser (goreleaser-action does not install syft)"
      pattern: "download-syft@[0-9a-f]{40}"
    - from: ".github/workflows/ci.yml"
      to: "scripts/workflow_pin_gate.sh"
      via: "a blocking CI job invoking the gate"
      pattern: "workflow_pin_gate"
  prohibitions:
    - "Do NOT pin without the trailing # vX.Y.Z comment — Dependabot only updates SHA pins that carry the version comment; a bare SHA rots blind (D-20)."
    - "Do NOT false-flag goreleaser-action's `version: \"~> v2\"` INPUT (release.yml:55) — the gate's ref-pattern must be scoped to uses: lines only (D-19)."
    - "Do NOT use a naive owner/repo@ regex — it misses multi-segment paths (github/codeql-action/init); match the ref before @ as one opaque non-whitespace token (D-19 / Pitfall 5)."
    - "Do NOT anchor the @latest check on step-level `run:` only — scan indented run: | body lines, where every real tool-install lives (D-19 / Pitfall 5)."
    - "Do NOT add osv-scanner or a full lockfile/container vuln scanner — beyond the Go call-graph, alert fatigue, out of scope (Deferred)."
    - "Do NOT add image-level SBOM attestation (dockers_v2 bypasses sboms:) — deferred; ship binary + source archive SBOM only (D-17)."
    - "Do NOT claim 'blocks merges' in acceptance without confirming vulncheck is a required branch-protection status check (D-18 caveat)."
---

<objective>
Close F-051 (SEC-05): make the CI/release supply chain reproducible and attested. Four moves:
1. SHA-pin every uses: across ci/codeql/release/skills.yml with a mandatory # vX.Y.Z comment (re-resolve each
   SHA via gh api at execute time — the research table is a spot-check, not a source of truth), codeql init +
   analyze on the SAME SHA, and pin the 3 @latest tool installs (deadcode, govulncheck, go-mutesting) to exact
   versions (optionally via go.mod tool directives for deadcode + govulncheck so Dependabot's gomod ecosystem
   tracks them).
2. Emit an SBOM: a goreleaser sboms: block covering BOTH the binary/archive (Go modules) AND the source tarball
   (web/package-lock.json), SPDX-JSON per release, with a SHA-pinned syft install step before goreleaser.
3. Ship scripts/workflow_pin_gate.sh + a fixture self-test + a blocking CI job (a custom grep gate, dodging the
   pin-the-linter bootstrap paradox; regex hardened for multi-segment paths, run: | bodies, and the goreleaser
   version: false-positive).
4. Keep govulncheck reachable-any = block, pinned to an exact version.

Purpose: a compromised/re-tagged Action or a floating @latest tool can no longer silently change what CI runs,
and every release ships a checksummed SBOM covering the Go binary AND the embedded web/ graph.
Output: 4 workflow files pinned, .goreleaser.yaml sboms:, the pin-gate script + self-test + CI job, go.mod/go.sum tool pins.

No amendment dependency (SEC-05 unchanged). Execute sequentially on master. Re-resolve every SHA with
`gh api repos/<owner>/<repo>/commits/<tag> --jq '.sha'` immediately before committing (tags are mutable).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-security-supply-chain-pack/40-CONTEXT.md
@.planning/phases/40-security-supply-chain-pack/40-RESEARCH.md
@.planning/phases/40-security-supply-chain-pack/40-PATTERNS.md
@scripts/quality_snapshot_gate.sh
@scripts/quality_snapshot_gate_test.sh
</context>

## Artifacts this phase produces (this plan)

- `scripts/workflow_pin_gate.sh` — NEW: the SHA-pin + no-@latest blocking gate.
- `scripts/workflow_pin_gate_test.sh` — NEW: fixture-based self-test.
- `.goreleaser.yaml` — NEW `sboms:` block (`artifacts: archive` + `artifacts: source`).
- `.github/workflows/release.yml` — NEW SHA-pinned `anchore/sbom-action/download-syft` step; all `uses:` SHA-pinned.
- `.github/workflows/ci.yml` — all `uses:` SHA-pinned; deadcode + govulncheck exact-pinned; NEW `workflow_pin_gate` job.
- `.github/workflows/codeql.yml` — all `uses:` SHA-pinned (init + analyze identical SHA).
- `.github/workflows/skills.yml` — all `uses:` SHA-pinned; go-mutesting exact-pinned.
- `go.mod` / `go.sum` — optional `tool` directives for `golang.org/x/tools/cmd/deadcode` + `golang.org/x/vuln/cmd/govulncheck`.

<tasks>

<task type="auto">
  <name>Task 1: SHA-pin all Action refs + exact-pin the 3 @latest tool installs</name>
  <files>.github/workflows/ci.yml, .github/workflows/codeql.yml, .github/workflows/release.yml, .github/workflows/skills.yml, go.mod, go.sum</files>
  <read_first>
    - .github/workflows/ci.yml (55 uses:, deadcode@latest :78-81, golangci-lint@v2.12.2 :85 precedent, govulncheck@latest :158-161, sqlc@v1.31.1 :465 precedent)
    - .github/workflows/codeql.yml (init+analyze :53-58,64-67 — must share one SHA), release.yml (7 uses:), skills.yml (go-mutesting@latest :132-134)
    - 40-RESEARCH.md "Resolving the 14 GitHub Action Refs" table (SHAs + version comments; RE-RESOLVE at execute time) + go.mod tool-directive verdict
    - 40-CONTEXT.md D-20 (pin scope, comment mandatory), D-18 (govulncheck exact-pin)
  </read_first>
  <action>
    For each uses: line in all four workflow files, resolve the current tag to a 40-hex SHA with
    `gh api repos/<owner>/<repo>/commits/<tag> --jq '.sha'` and rewrite as `uses: <ref>@<sha> # <vX.Y.Z>` (the
    version comment is MANDATORY). Pin github/codeql-action/init AND analyze to the identical SHA. Replace the 3
    `go install ...@latest` lines with exact versions: deadcode -> golang.org/x/tools/cmd/deadcode@v0.48.0 (the
    version already in go.mod for x/tools), govulncheck -> golang.org/x/vuln/cmd/govulncheck@v1.6.0 (re-resolve
    `go list -m golang.org/x/vuln@latest`), go-mutesting -> the current pinned pseudo-version (re-resolve
    `go list -m github.com/avito-tech/go-mutesting@latest`). RECOMMENDED (D-20 discretion): move deadcode +
    govulncheck to go.mod `tool` directives via `go get -tool <module>@<version>` and invoke via `go tool` in CI,
    so Dependabot's gomod ecosystem tracks them; run `go mod tidy` and commit go.mod/go.sum. Do NOT migrate
    go-mutesting to a tool directive unless go.sum churn is acceptable (a plain exact-pinned go install satisfies
    D-20). Keep the vulncheck job's flag-free `govulncheck $(scripts/go_packages.sh)` invocation (reachable-any = block).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && ! rg -n "uses:\\s*\\S+@v[0-9]" .github/workflows/ && ! rg -n "go install .*@latest" .github/workflows/ && rg -n "codeql-action/(init|analyze)@[0-9a-f]{40}" .github/workflows/codeql.yml && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "uses:\\s*\\S+@v[0-9]" .github/workflows/` returns nothing (no floating tag refs remain).
    - `rg -n "go install .*@latest" .github/workflows/` returns nothing.
    - codeql init and analyze both match `@<40-hex>` and are the SAME SHA (verified by eye + noted in SUMMARY).
    - Every pinned uses: carries a trailing `# vX.Y.Z` comment.
    - If tool directives adopted: `go tool deadcode` / `go tool govulncheck` resolve; `go mod tidy` leaves a clean tree; `go build ./...` passes.
  </acceptance_criteria>
  <done>All Action refs SHA-pinned with version comments (init+analyze identical), the 3 @latest tool installs exact-pinned, govulncheck stays reachable-any-block.</done>
</task>

<task type="auto">
  <name>Task 2: SBOM — goreleaser sboms: (archive + source) + pinned syft install step</name>
  <files>.goreleaser.yaml, .github/workflows/release.yml</files>
  <read_first>
    - .goreleaser.yaml (version: 2, archives: present, NO sboms: block yet; dockers_v2: is the deferred image path)
    - .github/workflows/release.yml (the goreleaser-action step; add syft install BEFORE it)
    - 40-RESEARCH.md D-17 resolution (sboms: [{artifacts: archive},{artifacts: source}]; anchore/sbom-action/download-syft@<sha> v0.24.0) + Standard Stack (syft v1.49.0)
    - 40-CONTEXT.md D-17 (web/ coverage requires the source SBOM; goreleaser-action does NOT install syft)
  </read_first>
  <action>
    Add to .goreleaser.yaml a top-level `sboms:` block with TWO entries: `{artifacts: archive}` (Go-modules SBOM
    from the binary/archive buildinfo) and `{artifacts: source}` (source-tarball SBOM, the only way to capture
    web/package-lock.json), both SPDX-JSON. In release.yml, add a syft install step BEFORE the goreleaser step:
    `uses: anchore/sbom-action/download-syft@<sha> # v0.24.0` (re-resolve the SHA via gh api). Ensure the SBOM
    documents attach to the GitHub Release and are checksummed (goreleaser's checksum step already covers extra
    artifacts). Do NOT add image-level attestation (dockers_v2 bypasses sboms: — deferred). Validate locally with
    `goreleaser check` (and a `goreleaser build --snapshot --clean` if syft is available locally; if not, note it
    as CI-validated).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && rg -n "sboms:" .goreleaser.yaml && rg -n "artifacts: source" .goreleaser.yaml && rg -n "download-syft@[0-9a-f]{40}" .github/workflows/release.yml && (command -v goreleaser >/dev/null && goreleaser check || echo "goreleaser not local — CI validates")</automated>
  </verify>
  <acceptance_criteria>
    - .goreleaser.yaml contains a `sboms:` block with BOTH `artifacts: archive` and `artifacts: source`.
    - release.yml contains a SHA-pinned `anchore/sbom-action/download-syft@<40-hex> # v0.24.0` step placed before the goreleaser step.
    - `goreleaser check` passes locally (or the SUMMARY notes it is CI-validated because syft/goreleaser is not on the dev box).
    - No `dockers_v2` image-SBOM attestation is added (deferred).
  </acceptance_criteria>
  <done>Each release ships a binary (Go-modules) SBOM AND a source SBOM covering web/, SPDX-JSON, checksummed; syft is installed via a pinned action before goreleaser.</done>
</task>

<task type="auto">
  <name>Task 3: workflow_pin_gate.sh + fixture self-test + blocking CI job</name>
  <files>scripts/workflow_pin_gate.sh, scripts/workflow_pin_gate_test.sh, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/quality_snapshot_gate.sh:1-11 (header/setup shape: set -euo pipefail, repo-root cd), quality_snapshot_gate_test.sh (tmpdir + fixture + rc-assertion self-test shape)
    - .github/workflows/release.yml:51-56 (goreleaser-action version: input — the false-positive the gate must NOT flag)
    - .github/workflows/ci.yml:78-81,158-161 + skills.yml:132-134 (indented run: | tool-install bodies the @latest check must scan)
    - 40-RESEARCH.md Pitfall 5 (multi-segment paths + run: | bodies); 40-CONTEXT.md D-19; 40-PATTERNS.md "Shell-gate + self-test precedent"
  </read_first>
  <action>
    Create scripts/workflow_pin_gate.sh (set -euo pipefail, cd repo root) with TWO independent checks over
    .github/workflows/*.yml: (a) every `uses:` line must match `uses:\s*(\S+)@([0-9a-f]{40})\s*#\s*v\S+` where the
    ref before @ is one opaque non-whitespace token (handles github/codeql-action/init multi-segment); ALLOW
    local `./...` refs and `docker://...@sha256:...`; scope this pattern to `uses:` lines ONLY so it never flags
    goreleaser-action's `version: "~> v2"` input; fail (rc=1) listing any offending line. (b) no `go install
    ...@latest` anywhere — scan EVERY non-blank line (including indented run: | body lines), NOT just step-level
    run:; do NOT flag pinned `@v2.12.2`/`@v1.31.1` or pipx `==` installs; fail on any `go install` + `@latest`.
    Create scripts/workflow_pin_gate_test.sh mirroring quality_snapshot_gate_test.sh: a tmpdir with fixture YAML
    files reproducing (1) a correctly-pinned multi-segment ref (expect pass), (2) an unpinned `github/codeql-action/
    init@v4` (expect fail), (3) a `go install ...@latest` inside a `run: |` block (expect fail), (4) a
    `version: "~> v2"` input line (expect NOT flagged), asserting the gate's rc for each. Add a blocking job to
    ci.yml (`workflow_pin_gate`) that runs `bash scripts/workflow_pin_gate.sh`. Add the codeql-init+analyze-same-SHA
    manual review-checklist note to the SUMMARY (grep can't enforce cross-line equality).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && bash scripts/workflow_pin_gate_test.sh && bash scripts/workflow_pin_gate.sh && rg -n "workflow_pin_gate" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - scripts/workflow_pin_gate.sh exits 0 against the now-pinned repo workflows and exits 1 on a synthetic unpinned/@latest fixture.
    - scripts/workflow_pin_gate_test.sh passes: it asserts pass on the correctly-pinned fixture, fail on the multi-segment-unpinned and run:|-@latest fixtures, and NOT-flagged on the goreleaser version: input.
    - ci.yml contains a blocking `workflow_pin_gate` job invoking the gate.
    - The SUMMARY records the codeql init/analyze same-SHA manual-check and the D-18 branch-protection-required-check caveat.
  </acceptance_criteria>
  <done>A blocking, self-tested pin gate rejects unpinned uses: and @latest installs (multi-segment + run:| aware) without false-flagging the goreleaser version: input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub Action / tool registry → CI runner | A re-tagged Action or a floating @latest tool install can change what CI executes with repo credentials — the build-pipeline trust boundary. |
| release build → published artifacts | The SBOM attests the dependency graph of what is shipped (binary + embedded web/). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05-RETAG | Tampering (of the build pipeline) | mutable Action tag / floating @latest tool | mitigate | SHA-pin every uses: with a Dependabot-updatable # vX.Y.Z comment; exact-pin the 3 tool installs; the blocking workflow_pin_gate enforces it (F-051). |
| T-40-05-CODEQLSPLIT | Tampering | codeql init/analyze pinned to different SHAs | mitigate | Both pinned to one SHA; manual review-checklist item (grep cannot enforce cross-line equality). |
| T-40-05-SBOMGAP | Information Disclosure (provenance gap) | web/ npm graph invisible in a binary-only SBOM | mitigate | Emit a source-artifact SBOM covering web/package-lock.json in addition to the archive SBOM (D-17). |
| T-40-05-VULN | Tampering | reachable known-vuln dependency ships | mitigate | govulncheck reachable-any = block, pinned to an exact version; branch-protection-required-check is a manual acceptance item (D-18). |
| T-40-05-SC | Tampering | the one NEW action (anchore/sbom-action/download-syft) | mitigate | Legitimacy-audited (org-owned Anchore, 252 stars, non-archived — 40-RESEARCH Package Legitimacy Audit); SHA-pinned. |
</threat_model>

<verification>
- rg confirms no floating @vN uses: and no go install @latest across the four workflow files.
- bash scripts/workflow_pin_gate.sh exits 0 on the pinned repo; bash scripts/workflow_pin_gate_test.sh passes.
- .goreleaser.yaml sboms: archive + source present; release.yml has the pinned download-syft step before goreleaser.
- goreleaser check passes (or CI-validated note); go build ./... clean after any go.mod tool-directive changes.
- SUMMARY records: codeql init/analyze same-SHA manual check; vulncheck branch-protection-required-check caveat (D-18).
</verification>

<success_criteria>
F-051 (SEC-05) closed: every Action ref + CI tool install is SHA/exact-pinned with a Dependabot-updatable comment,
a blocking self-tested workflow-pin gate rejects floating refs and @latest, each release publishes a checksummed
SBOM covering the Go binary AND the embedded web/ graph, and govulncheck stays a reachable-any blocking gate.
</success_criteria>

<output>
Create `.planning/phases/40-security-supply-chain-pack/40-05-SUMMARY.md` when done.
</output>
