---
phase: 40-security-supply-chain-pack
plan: 06
type: tdd
wave: 2
depends_on: ["40-01"]
files_modified:
  - internal/secret/redact_exact.go
  - internal/secret/redact_exact_test.go
  - internal/conversations/store_append.go
  - internal/agent/tools/result.go
  - internal/agent/tools/result_test.go
  - internal/conversations/store_append_redaction_test.go
autonomous: true
requirements: [SEC-01]
user_setup: []

must_haves:
  truths:
    - "internal/secret/redact_exact.go builds a boot-time set of Aura's CONFIGURED secret VALUES by iterating os.Environ(), keeping entries where secret.IsSecretEnvVar(name,value) is true AND len(value) >= a documented length floor, and exposes a Redact(s) that strings.ReplaceAll-es each configured secret value with a placeholder (exact-match, zero false positives)."
    - "A secret the AGENT discovered at runtime (not in the boot-harvested configured set) survives Redact unchanged — it is the agent's working data, not Aura's configured secret (D-10 inbound scope)."
    - "The inbound detector is applied at store_append.go:218 (wrapping p.Content before maybeSpill) so it covers BOTH the inline turn body AND the conversations spill sidecar in one place."
    - "The independent spill-guard length check at store_append.go:123 uses the SAME redaction so the guard and maybeSpill agree at the turnCapBytes boundary (D-11 fix-on-touch coupling)."
    - "internal/agent/tools/result.go NewResult applies the SAME inbound detector to content before writeSidecar, so the full-output .result sidecar (mode 0o600) never persists Aura's configured secrets verbatim (D-09 second at-rest surface)."
    - "A db_integration round-trip proves a tool output containing a configured secret value (e.g. AURA_DB_URL's value) leaves NO verbatim copy in the turn body, the spill sidecar, or the .result sidecar."
    - "An empty or below-floor configured secret value is NOT harvested, so it never over-redacts short legitimate substrings (SEC-01 unclassified edge — resolved covered)."
    - statement: "When a configured secret value happens to be a substring of unrelated legitimate content, exact-match ReplaceAll will still replace that substring — accept this as the deliberate exact-match tradeoff (zero false-negatives for Aura's own secrets at rest), and confirm the length floor keeps collisions negligibly rare."
      verification: backstop
    - statement: "The boot-harvested set is captured once at process start; a secret rotated into the env after boot is not reflected until restart — confirm this is the intended at-rest posture (matches the process-scoped config model)."
      verification: backstop
  artifacts:
    - path: "internal/secret/redact_exact.go"
      provides: "Inbound exact-match configured-secret detector (boot-harvested set)"
      contains: "IsSecretEnvVar"
    - path: "internal/secret/redact_exact_test.go"
      provides: "Unit test: configured secret redacted, agent-discovered secret survives, length floor"
      contains: "length floor"
  key_links:
    - from: "internal/conversations/store_append.go"
      to: "secret.Redact (redact_exact)"
      via: "wrap p.Content at :218 before maybeSpill (covers inline + spill)"
      pattern: "secret\\.Redact|redactExact"
    - from: "internal/agent/tools/result.go"
      to: "secret.Redact (redact_exact)"
      via: "redact content before writeSidecar in NewResult"
      pattern: "secret\\.Redact|redactExact"
  prohibitions:
    - "Do NOT pattern-redact (regex / redact.String) the INBOUND turn/spill/.result content — it corrupts the agent's re-fed working data. Inbound is exact-match against the boot-harvested configured-secret set ONLY (D-10)."
    - "Do NOT redact only maybeSpill's inline branch — the redactor must sit at :218 before maybeSpill so it covers inline AND the spill sidecar (D-09)."
    - "Do NOT invent a fourth secret-marker list — reuse secret.IsSecretEnvKey/IsSecretEnvVar (the canonical predicate)."
    - "Do NOT skip the store_append.go:123 spill-guard mirror — an un-mirrored guard disagrees with maybeSpill at the cap boundary (D-11 coupling)."
    - "Do NOT scatter per-writer regex across the two at-rest seams — reuse the ONE detector at both (store_append + result.go)."
    - "Do NOT harvest env values below the length floor — short values cause over-redaction of legitimate content (documented floor, Claude's discretion)."
---

<objective>
Close the inbound half of SEC-01 (D-09/D-10, F-021): Aura's CONFIGURED secrets must never persist verbatim at
ANY at-rest surface. There are TWO such surfaces: the conversation_turns choke-point (store_append.go:218, which
also feeds the spill sidecar) AND the tool-result .result sidecar (result.go NewResult, which writes the FULL
tool output, not just the capped preview). Both get the SAME inbound exact-match detector — a boot-harvested set
of configured secret VALUES (via the canonical secret.IsSecretEnvVar predicate + a length floor) replaced by a
placeholder. Exact-match INBOUND is deliberate: a connstring the agent DISCOVERED survives (it is the agent's
working data), while Aura's own DSN/keys never land at rest. The store_append.go:123 spill-guard is mirrored so
the guard and maybeSpill agree at the cap boundary (D-11 coupling).

Purpose: Aura's configured secrets are redacted before persistence at every at-rest surface (the amended SEC-01
scope), without corrupting the agent's re-fed working data.
Output: internal/secret/redact_exact.go(+test) + store_append.go + result.go(+test) + a db_integration round-trip.

Depends on 40-01 (SEC-01 amendment D-10b lands the configured-secrets-at-rest scope BEFORE this code). TDD: the
detector is a pure function — RED table test first, then wire the two at-rest seams. Run -race and db_integration
tiers in WSL; the db_integration round-trip must actually execute (no skip-as-green).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-security-supply-chain-pack/40-CONTEXT.md
@.planning/phases/40-security-supply-chain-pack/40-PATTERNS.md
@internal/secret/envkey.go
@internal/conversations/store_append.go
@internal/agent/tools/result.go
</context>

## Artifacts this phase produces (this plan)

- `internal/secret/redact_exact.go` — NEW: boot-harvested configured-secret set + `Redact(string) string` (exact-match, length-floored).
- `internal/secret/redact_exact_test.go` — NEW: `TestRedactExact` (configured redacted / discovered survives / length floor).
- `internal/conversations/store_append.go` — the `:218` redactor wrap + the `:123` spill-guard mirror.
- `internal/agent/tools/result.go` — redact `content` before `writeSidecar` in `NewResult`.
- `internal/agent/tools/result_test.go` — NEW sibling: `TestNewResult_RedactsConfiguredSecrets`.
- `internal/conversations/store_append_redaction_test.go` — NEW `db_integration` round-trip (turn + spill + .result no verbatim secret).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inbound exact-match detector internal/secret/redact_exact.go — test-first</name>
  <files>internal/secret/redact_exact.go, internal/secret/redact_exact_test.go</files>
  <read_first>
    - internal/secret/envkey.go:65-99 (IsSecretEnvKey / IsSecretEnvVar / ContainsCredentialURL — the canonical predicate to reuse)
    - internal/secret/envkey_test.go (the package's test style)
    - 40-CONTEXT.md D-10 (inbound exact-match, boot-harvested, length floor — floor is Claude's discretion, document it)
    - 40-PATTERNS.md "SEC-01 redact_exact.go" section + "Canonical Secret-Env Predicate" shared pattern
    - 40-RESEARCH.md Pattern 1 note (this is the INBOUND detector, distinct from outbound internal/redact.String — never merge)
  </read_first>
  <behavior>
    - Redact replaces a configured secret value (harvested from a test-set env via IsSecretEnvVar, len>=floor) with a placeholder.
    - A value NOT in the harvested set (an agent-discovered postgres://... connstring) survives unchanged.
    - A configured env value shorter than the length floor is NOT harvested (no over-redaction of short legit text).
    - Multiple distinct configured secrets in one string are all replaced.
    - Empty input returns empty; a string with no configured secret is returned unchanged.
  </behavior>
  <action>
    FIRST write internal/secret/redact_exact_test.go (TestRedactExact) that seeds a controlled env (t.Setenv for
    a couple of secret-shaped keys with long values + one short value + one non-secret key), constructs the
    detector, and asserts the behavior rows; confirm RED. THEN create internal/secret/redact_exact.go: a
    constructor that iterates os.Environ(), splits name=value, keeps entries where IsSecretEnvVar(name,value) AND
    len(value) >= a documented length floor (choose and document a sane floor, e.g. 8-12), storing the VALUES in
    a set; a Redact(s string) string that strings.ReplaceAll-es each stored value with a fixed placeholder (e.g.
    "[REDACTED_CONFIGURED_SECRET]"). Expose a package-level boot-initialized detector (sync.Once or a var
    initialized from os.Environ) AND a testable constructor that accepts an explicit environ slice so the test is
    deterministic. Document the exact-match-inbound vs pattern-outbound distinction in the package/file doc.
    Confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test ./internal/secret/ -run TestRedactExact -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/secret/redact_exact.go calls secret.IsSecretEnvVar and applies a documented length floor.
    - TestRedactExact asserts: configured secret redacted, agent-discovered connstring survives, below-floor value not harvested, multiple secrets replaced.
    - go test ./internal/secret/ -run TestRedactExact green in <30s, no skip.
    - The file doc states inbound=exact-match / outbound=pattern (internal/redact) and names the length floor.
  </acceptance_criteria>
  <done>The inbound exact-match detector redacts Aura's configured secrets while leaving agent-discovered data intact; length-floored; unit-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the detector at both at-rest seams (store_append + result sidecar) + db_integration round-trip</name>
  <files>internal/conversations/store_append.go, internal/agent/tools/result.go, internal/agent/tools/result_test.go, internal/conversations/store_append_redaction_test.go</files>
  <read_first>
    - internal/conversations/store_append.go:213-222 (appendTurnWrites — wrap p.Content at :218 before maybeSpill) + :120-124 (the independent spill-guard length check to mirror)
    - internal/conversations/store_helpers.go:123-142 (maybeSpill / postgresTextSafe — what the redactor sits in front of)
    - internal/agent/tools/result.go:158-165,234-244 (NewResult writeSidecar call + writeSidecar body, 0o600)
    - internal/agent/tools/result_test.go (TestNewResult_LargeSpills — the sidecar-content assertion style to mirror)
    - internal/conversations/store_test.go + store_append_tx_test.go (db_integration harness style, //go:build db_integration)
    - 40-CONTEXT.md D-09/D-11; 40-PATTERNS.md "SEC-01 store_append.go" + "SEC-01 result.go" sections
  </read_first>
  <behavior>
    - A turn whose Content contains a configured secret value persists with the secret replaced in BOTH the inline turn body AND (when spilled) the spill sidecar file.
    - The store_append.go:123 spill-guard computes length on the SAME redacted content as maybeSpill (guard and spill agree at the cap boundary).
    - NewResult writes a .result sidecar with the configured secret replaced (full-output surface covered).
    - An agent-discovered secret in tool output survives at rest (inbound exact-match, not pattern).
  </behavior>
  <action>
    In store_append.go appendTurnWrites (:218): wrap p.Content with the internal/secret inbound detector BEFORE
    postgresTextSafe/maybeSpill so ONE call covers both the inline content branch and the maybeSpill sidecar branch.
    Mirror the SAME redaction in the AppendTurnTx spill-guard length check (:123) so len() is computed on redacted
    content (or compute the redacted content once and thread it). In result.go NewResult: apply the same detector
    to content immediately before the writeSidecar(path, content) call, so the .result file never holds a configured
    secret verbatim. Add internal/agent/tools/result_test.go sibling TestNewResult_RedactsConfiguredSecrets
    (t.Setenv a secret-shaped env, run NewResult with content containing that value, read the sidecar, assert the
    value is absent and the placeholder present, 0600 preserved). Add internal/conversations/
    store_append_redaction_test.go (//go:build db_integration) that appends a turn (both small=inline and
    large=spill) with a configured secret in Content and asserts NO verbatim secret in the turn row nor the spill
    sidecar; extend it (or a sibling) to assert the .result surface too. Keep the exact-match detector the ONLY
    inbound redaction — do NOT add redact.String here.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go build ./internal/conversations/ ./internal/agent/tools/ && go test ./internal/agent/tools/ -run TestNewResult -count=1 && go test -tags db_integration -race -count=1 -p 1 ./internal/conversations/ -run TestAppendTurnRedaction</automated>
  </verify>
  <acceptance_criteria>
    - store_append.go wraps p.Content with the secret detector at :218 (rg finds secret.Redact/redactExact in appendTurnWrites) AND the :123 spill-guard uses the same redaction.
    - result.go redacts content before writeSidecar; TestNewResult_RedactsConfiguredSecrets asserts absence of the verbatim secret + 0600.
    - The db_integration round-trip (WSL) proves no verbatim configured secret in turn body, spill sidecar, or .result sidecar; an agent-discovered secret survives.
    - No inbound pattern-redaction (redact.String) is added; go test -race green for both packages.
  </acceptance_criteria>
  <done>Both at-rest surfaces (turns table inline+spill, and the .result sidecar) redact Aura's configured secrets via the one exact-match detector; the spill-guard is mirrored; agent-discovered data survives; db_integration round-trip green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool output containing Aura's own env/config → at-rest persistence | A tool that dumps env/config (or a DSN in an error) writes to the turns table, the spill sidecar, and the .result sidecar — the at-rest boundary this plan redacts. |
| agent-discovered secret (working data) → persistence | The agent legitimately persists secrets it discovered; these must survive (exact-match inbound, not pattern). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06-EXFIL | Information Disclosure | Aura's configured DSN/keys persisted verbatim at rest (turn/spill/.result), later displayed/exported | mitigate | Exact-match inbound detector at both at-rest seams redacts configured secret values before write (F-021, SEC-01 inbound). |
| T-40-06-SPILLBYPASS | Information Disclosure | redacting only the inline branch re-opens the spill bypass | mitigate | Redactor at store_append.go:218 (before maybeSpill) covers inline AND spill in one place (D-09). |
| T-40-06-SIDECAR | Information Disclosure | the FULL-output .result sidecar (0o600) holds secrets the capped preview hid | mitigate | Same detector applied in result.go NewResult before writeSidecar (D-09 second surface). |
| T-40-06-CORRUPT | Tampering (data integrity) | pattern-redacting inbound corrupts the agent's re-fed working data | mitigate | Inbound is exact-match against the configured-secret set only; unknown secrets untouched (D-10). |
| T-40-06-CAPDRIFT | Tampering | guard/spill disagree at the cap boundary after redaction | mitigate | Mirror the redaction in the :123 spill-guard length check (D-11 coupling). |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib strings/os + existing internal/secret only. |
</threat_model>

<verification>
- go test ./internal/secret/ -run TestRedactExact green; go test ./internal/agent/tools/ -run TestNewResult green (WSL).
- go test -tags db_integration -race -count=1 -p 1 ./internal/conversations/ -run TestAppendTurnRedaction green (WSL, stack up) — actually executes, no skip.
- rg confirms the detector is wired at store_append.go:218 and :123 and in result.go NewResult; no inbound redact.String.
- go build ./internal/conversations/ ./internal/agent/tools/ clean; owned-surface coverage for internal/secret stays >=85%.
</verification>

<success_criteria>
The inbound half of SEC-01 is closed: Aura's configured secrets are redacted before persistence at every at-rest
surface (turn body, spill sidecar, .result sidecar) via one exact-match detector, agent-discovered data survives,
the spill-guard is mirrored, and a real db_integration round-trip proves no verbatim configured secret at rest.
</success_criteria>

<output>
Create `.planning/phases/40-security-supply-chain-pack/40-06-SUMMARY.md` when done.
</output>
