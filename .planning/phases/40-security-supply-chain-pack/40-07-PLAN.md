---
phase: 40-security-supply-chain-pack
plan: 07
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - internal/agui/server_cors.go
  - internal/agui/server.go
  - internal/agui/server_test.go
  - internal/config/config.go
  - internal/config/config_knobs.go
  - internal/config/config_validate.go
  - internal/config/config_validate_test.go
  - cmd/aura/serve.go
autonomous: true
requirements: [SEC-02]
user_setup: []

must_haves:
  truths:
    - "internal/agui/server_cors.go is deleted; the withCORS middleware no longer exists and neither Mux() call site wraps the mux (server.go returns mux / returns guarded directly)."
    - "No Access-Control-* header is emitted on ANY response for ANY Origin; an OPTIONS request to /agent/run returns the mux's default (method-not-allowed), never 204 with Access-Control-Allow-Origin."
    - "ServerConfig.CORSPermissive, Config.AGUICORSPermissive (field + env wiring at config.go:438), the AURA_AGUI_CORS_PERMISSIVE KnobSpec row (config_knobs.go:77), and cmd/aura/serve.go's CORSPermissive construction line are all removed."
    - "config_validate.go's gateCORS function and its ValidateProfile call are removed (dead code once the knob is gone)."
    - "TestServer_CORSPermissive is replaced by TestNoCORSHeaders (asserting no ACAO header and OPTIONS not 204); TestGateCORS is deleted; the package builds and tests pass."
    - "The session cookie's SameSite=Strict + Secure posture (auth_cookie.go:129-130) is untouched — same-origin-only preserves the sole CSRF control the auth.go note depends on."
    - "No CORS headers are emitted on any response regardless of whether an Origin header is present or absent (SEC-02 empty edge — resolved covered)."
    - "The AURA_AGUI_CORS_PERMISSIVE knob and its gateCORS validation are FULLY removed — there is no residual permissive path to toggle (SEC-02 adjacency edge, per the CONTEXT reversal note — resolved covered)."
    - "An OPTIONS preflight to a registered route returns the mux's default (method-not-allowed), never 204 with ACAO (SEC-02 ordering edge — resolved covered)."
  artifacts:
    - path: "internal/agui/server.go"
      provides: "Mux() with the withCORS wrap removed (same-origin serving)"
      contains: "func (s *Server) Mux"
    - path: "internal/agui/server_test.go"
      provides: "TestNoCORSHeaders (no ACAO header, OPTIONS not 204)"
      contains: "Access-Control-Allow-Origin"
  key_links:
    - from: "internal/agui/server.go"
      to: "http mux"
      via: "withCORS wrap removed at both Mux() return sites"
      pattern: "return (mux|guarded)"
    - from: "cmd/aura/serve.go"
      to: "agui.ServerConfig"
      via: "CORSPermissive construction line removed"
      pattern: "ServerConfig\\{"
  prohibitions:
    - "Do NOT emit any CORS header (not even a restrictive allowlist) — the decision is same-origin ONLY, no headers (D-14)."
    - "Do NOT try to sync Access-Control-Allow-Methods to the route table — the routes are scattered across ~15 files with no registry; same-origin removes the need entirely (D-14)."
    - "Do NOT weaken auth_cookie.go's SameSite=Strict + Secure — it is the sole CSRF control the auth.go note relies on (D-14)."
    - "Do NOT leave gateCORS or AGUICORSPermissive as dead code — remove the field, the knob row, the gate, and its ValidateProfile call together."
    - "Do NOT reintroduce a cross-origin path — a dev-only Vite :5173->:9080 workflow, if kept, is a documented dev-profile-only escape, never a prod path (D-14b)."
---

<objective>
Close F-022 (SEC-02) via the adversarially-validated reversal (D-14/D-14b): serve the cockpit SAME-ORIGIN ONLY and
emit NO CORS headers at all. The credentialed allowlist first considered has no production cross-origin consumer
(the dist is go:embed-served same-origin), SameSite=Strict+Secure already blocks cross-site cookies, and the
allowlist would trip the auth.go CSRF re-eval and the unsolvable methods-sync problem. So delete withCORS
(server_cors.go, 23 lines), un-wrap both Mux() call sites, and remove the entire AURA_AGUI_CORS_PERMISSIVE config
surface (field + env wiring + KnobSpec row + the now-dead gateCORS gate). Simpler and strictly stronger than the
allowlist given the evidence.

Purpose: the CORS wildcard is eliminated with zero new CSRF surface and no methods-sync maintenance trap.
Output: server_cors.go deleted; server.go/serve.go un-wrapped; config field/knob/gate removed; tests updated.

Depends on 40-01 (SEC-02 amendment D-14b lands same-origin-only wording BEFORE this code). This plan owns
internal/config/config_knobs.go + config_validate.go + cmd/aura/serve.go, so 40-08 (SEC-09) and 40-09 (SEC-01b)
sequence AFTER it (they edit the same files). type: execute — this is subtraction + a test replacement, not a
new-logic feature. Run tests in WSL.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-security-supply-chain-pack/40-CONTEXT.md
@.planning/phases/40-security-supply-chain-pack/40-PATTERNS.md
@internal/agui/server_cors.go
@internal/agui/server.go
@internal/config/config_validate.go
</context>

## Artifacts this phase produces (this plan)

This plan REMOVES symbols (subtraction); it produces no net-new runtime symbols. Removed:
- `internal/agui/server_cors.go` (the whole file / `withCORS`).
- `ServerConfig.CORSPermissive` field (server.go), `Config.AGUICORSPermissive` field + env wiring (config.go:122,438).
- `AURA_AGUI_CORS_PERMISSIVE` KnobSpec row (config_knobs.go:77), `gateCORS` func + its ValidateProfile call (config_validate.go).
Added (tests): `TestNoCORSHeaders` (server_test.go, replacing `TestServer_CORSPermissive`).

<tasks>

<task type="auto">
  <name>Task 1: Delete withCORS + un-wrap Mux() + remove ServerConfig.CORSPermissive + serve.go line</name>
  <files>internal/agui/server_cors.go, internal/agui/server.go, cmd/aura/serve.go, internal/agui/server_test.go</files>
  <read_first>
    - internal/agui/server_cors.go (whole 23-line file — the withCORS to delete; note the hardcoded method list already missing PATCH/PUT, the drift proof)
    - internal/agui/server.go:41-43 (ServerConfig.CORSPermissive field) + :356-364 (the two Mux() withCORS call sites)
    - cmd/aura/serve.go:358-360 (the CORSPermissive construction line to remove)
    - internal/agui/server_test.go:462-519 (TestServer_CORSPermissive — delete; the off: sub-case becomes TestNoCORSHeaders)
    - internal/agui/auth.go:14-17 + auth_cookie.go:123-131 (CSRF/SameSite posture — DO NOT touch, confirm preserved)
    - 40-CONTEXT.md D-14/D-14b; 40-PATTERNS.md "SEC-02 server_cors.go" section
  </read_first>
  <action>
    Delete internal/agui/server_cors.go entirely. In server.go Mux(): change the two return sites from the
    withCORS-wrapped forms to returning the bare mux / guarded handlers; remove the CORSPermissive bool field from
    ServerConfig. In cmd/aura/serve.go bootServe: remove the CORSPermissive construction line from the
    agui.ServerConfig literal (leave BufferCap/ReadinessState/etc. intact). In server_test.go: delete
    TestServer_CORSPermissive and add TestNoCORSHeaders — construct the test server WITHOUT any CORSPermissive
    field, send an OPTIONS to /agent/run, assert the Access-Control-Allow-Origin header is empty and the status is
    not http.StatusNoContent. Do NOT touch auth.go / auth_cookie.go.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && test ! -f internal/agui/server_cors.go && go build ./internal/agui/ ./cmd/aura/ && go test ./internal/agui/ -run TestNoCORSHeaders -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/agui/server_cors.go no longer exists; rg -n "withCORS|CORSPermissive" internal/agui/server.go cmd/aura/serve.go returns nothing.
    - server.go Mux() returns mux / guarded directly (no withCORS wrap); ServerConfig has no CORSPermissive field.
    - TestNoCORSHeaders asserts no Access-Control-Allow-Origin header and OPTIONS not 204; go test ./internal/agui/ -run TestNoCORSHeaders green.
    - auth.go / auth_cookie.go unchanged (SameSite=Strict + Secure preserved).
  </acceptance_criteria>
  <done>withCORS deleted, both Mux() sites un-wrapped, ServerConfig/serve.go CORSPermissive removed, no CORS header emitted; TestNoCORSHeaders green; CSRF cookie posture intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the AURA_AGUI_CORS_PERMISSIVE config surface + dead gateCORS</name>
  <files>internal/config/config.go, internal/config/config_knobs.go, internal/config/config_validate.go, internal/config/config_validate_test.go</files>
  <read_first>
    - internal/config/config.go:118-122 (AGUICORSPermissive field + comment) + :438 (envutil.BoolDefault("AURA_AGUI_CORS_PERMISSIVE", false) wiring)
    - internal/config/config_knobs.go:77 (the AURA_AGUI_CORS_PERMISSIVE KnobSpec row to remove)
    - internal/config/config_validate.go:222-230 (gateCORS func) + :97 (the gateCORS call in ValidateProfile)
    - internal/config/config_validate_test.go:136-152 (TestGateCORS — delete)
    - 40-PATTERNS.md "SEC-02" config-surface removal excerpts
  </read_first>
  <action>
    Remove the AGUICORSPermissive field + its doc comment from config.go (:118-122) and the
    envutil.BoolDefault("AURA_AGUI_CORS_PERMISSIVE", false) wiring line (:438). Remove the AURA_AGUI_CORS_PERMISSIVE
    row from config_knobs.go (:77). Remove the gateCORS function (config_validate.go:222-230) AND its call in
    ValidateProfile (:97). Delete TestGateCORS from config_validate_test.go. Confirm no other reference to
    AGUICORSPermissive / gateCORS / AURA_AGUI_CORS_PERMISSIVE remains anywhere. Run go vet + the config tests
    (unset any AURA_WEB_AUTH_SECRET env leak first per the make-coverage .env gotcha if running the coverage gate).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && rg -n "AGUICORSPermissive|gateCORS|AURA_AGUI_CORS_PERMISSIVE" internal/ cmd/ ; go build ./... && go test ./internal/config/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - rg -n "AGUICORSPermissive|gateCORS|AURA_AGUI_CORS_PERMISSIVE" internal/ cmd/ returns nothing (field, wiring, knob row, gate, and gate call all removed).
    - config_validate_test.go no longer contains TestGateCORS.
    - go build ./... succeeds; go test ./internal/config/ green.
    - ValidateProfile no longer calls gateCORS (rg on config_validate.go confirms).
  </acceptance_criteria>
  <done>The entire AURA_AGUI_CORS_PERMISSIVE surface (field, env wiring, KnobSpec row, gateCORS gate + call, TestGateCORS) is removed as dead code; build + config tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-origin browser → agui server | With CORS removed, the browser's same-origin policy blocks cross-origin credentialed reads/writes; the wildcard that previously relaxed this is gone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07-CORS | Tampering / Information Disclosure | wildcard/permissive CORS on a credentialed API | mitigate | Remove withCORS and the AURA_AGUI_CORS_PERMISSIVE surface entirely; emit no CORS headers (same-origin only) — closes F-022. |
| T-40-07-CSRF | Tampering | introducing new cross-origin write surface | mitigate | Same-origin-only preserves SameSite=Strict+Secure as the sole CSRF control; no allowlist added, so the auth.go re-eval trigger is not tripped (D-14). |
| T-40-07-DEADCODE | (maintainability) | orphaned gateCORS/knob rotting | mitigate | Remove field, env wiring, KnobSpec row, gate, and its call together; rg confirms zero residue. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs — deletion + config removal only. |
</threat_model>

<verification>
- internal/agui/server_cors.go deleted; rg finds no withCORS/CORSPermissive/AGUICORSPermissive/gateCORS/AURA_AGUI_CORS_PERMISSIVE anywhere.
- go build ./... clean; go test ./internal/agui/ -run TestNoCORSHeaders and go test ./internal/config/ green (WSL).
- auth.go / auth_cookie.go unchanged (git diff shows no edits) — SameSite=Strict + Secure preserved.
- No CORS header emitted; OPTIONS returns the mux default, not 204.
</verification>

<success_criteria>
F-022 (SEC-02) closed via the same-origin-only reversal: the CORS wildcard and its entire config surface are
removed, no CORS header is emitted on any response, the SameSite=Strict CSRF control is preserved untouched, and
the methods-sync maintenance trap is eliminated — simpler and stronger than the allowlist.
</success_criteria>

<output>
Create `.planning/phases/40-security-supply-chain-pack/40-07-SUMMARY.md` when done.
</output>
