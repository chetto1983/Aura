---
phase: 40-security-supply-chain-pack
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/agui/strict_decode.go
  - internal/agui/strict_decode_test.go
  - internal/agui/server_run_request.go
  - internal/agui/approvals_api.go
  - internal/agui/onboarding_api.go
  - internal/agui/assets_api.go
  - internal/agui/governance_write_api.go
  - internal/agui/governance_write_skills_api.go
  - internal/agui/strict_decode_routes_test.go
autonomous: true
requirements: [SEC-06]
user_setup: []

must_haves:
  truths:
    - "internal/agui/strict_decode.go exports a single strictDecodeJSON(w, r, dst, opts) helper that rejects a present non-application/json Content-Type, size-caps the body via http.MaxBytesReader (default maxRunBodyBytes = 1<<20), calls dec.DisallowUnknownFields(), tolerates an empty body only when opts.allowEmpty, and requires a SECOND dec.Decode(&struct{}{}) to return io.EOF (rejects trailing JSON)."
    - "A body of {class:trusted_local}{ignored:true} (the F-052 audit repro) is REJECTED by the helper (trailing-data error), where the pre-existing single-decode idiom accepted it."
    - "All six audit-named privileged routes use the helper: /agent/run (server_run_request.go), approvals-resolve (approvals_api.go), onboarding (onboarding_api.go), assets (assets_api.go), governance MCP (governance_write_api.go), governance skills (governance_write_skills_api.go)."
    - "Per-route allowEmpty is correct: false for /agent/run, approvals, onboarding, assets; true for governance MCP and governance skills (their verb-only enable/disable/delete bodies)."
    - "An unknown field on any of the six routes yields 400; an oversized (>1 MiB) body yields 400."
    - "The scheduler-edit route (governance_write_scheduler.go) is CONSCIOUSLY left OUT of the gated set (D-16c refactor-on-touch) and this deferral is flagged in the SUMMARY acceptance evidence."
    - "A body exactly at maxRunBodyBytes (1 MiB) is accepted; one byte over is rejected by MaxBytesReader (SEC-06 boundary edge — resolved covered)."
    - "Trailing whitespace/newline after the JSON value is tolerated (json.Decoder), but a second JSON value is rejected via the second-decode-must-EOF check (SEC-06 precision edge — resolved covered)."
    - statement: "A missing Content-Type header is tolerated (decodes when the body is valid JSON); a present non-application/json Content-Type is rejected — confirm the exact contract for missing vs mismatched."
      verification: backstop
  artifacts:
    - path: "internal/agui/strict_decode.go"
      provides: "Centralized strict JSON decode with trailing-data + unknown-field rejection"
      contains: "DisallowUnknownFields"
    - path: "internal/agui/strict_decode_test.go"
      provides: "Table-driven unit test for the helper (trailing/unknown/empty/content-type/oversize)"
      contains: "trailing"
  key_links:
    - from: "internal/agui/governance_write_api.go"
      to: "strictDecodeJSON"
      via: "decodeMCPBody replaced by the helper with allowEmpty:true"
      pattern: "strictDecodeJSON"
    - from: "internal/agui/server_run_request.go"
      to: "strictDecodeJSON"
      via: "the /agent/run body decode routed through the helper (allowEmpty:false)"
      pattern: "strictDecodeJSON"
  prohibitions:
    - "Do NOT copy the single-decode Decode-then-not-io.EOF idiom as the fix — it IS the F-052 trailing-JSON bug. The helper MUST add the second decode-must-be-EOF step (D-16b)."
    - "Do NOT sweep governance_write_scheduler.go:131 into the gated set — it is consciously deferred to refactor-on-touch (D-16c); flag it in the acceptance evidence instead."
    - "Do NOT add connect_api.go to the acceptance set — its io.LimitReader reads an UPSTREAM sidecar response, not an inbound privileged route (size-cap pattern only)."
    - "Do NOT introduce a new size cap — all six routes already cap at maxRunBodyBytes (1<<20); the helper defaults to it."
    - "Do NOT big-bang the ~20+ other agui decoders — D-16 is the acceptance set + refactor-on-touch only."
    - "Do NOT create a file >600 LOC — strict_decode.go is a small helper; refactor-on-touch any route file that would exceed the cap."
---

<objective>
Close F-052 (SEC-06): introduce ONE centralized strict-JSON-decode helper and apply it to the six audit-named
privileged routes. The helper adds what every current in-repo decoder lacks — DisallowUnknownFields() and the
mandatory SECOND Decode(&struct{}{})-must-be-io.EOF step (the Alex-Edwards idiom) that rejects trailing-JSON
smuggling (the audit's exact repro) — while preserving the existing MaxBytesReader size cap and per-route
empty-body tolerance.

Purpose: privileged JSON routes reject trailing / unknown-field / empty (per route) / wrong-content-type /
oversized bodies via a single tested helper, not six divergent idioms.
Output: internal/agui/strict_decode.go(+test) + the six routes wired + a per-route regression test.

TDD: the helper is the feature — write the failing table test first (RED), implement the two-decode idiom
(GREEN), refactor if needed. No amendment dependency (SEC-06 unchanged). Execute sequentially on master; run
Go tests in WSL. governance_write_scheduler.go stays OUT (D-16c) and MUST be flagged in the SUMMARY.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-security-supply-chain-pack/40-CONTEXT.md
@.planning/phases/40-security-supply-chain-pack/40-PATTERNS.md
@internal/agui/governance_write_api.go
@internal/agui/server_run_request.go
@internal/agui/onboarding_api.go
</context>

## Artifacts this phase produces (this plan)

- `internal/agui/strict_decode.go` — NEW: `strictDecodeJSON(w, r, dst, opts)` + `decodeOpts{maxBytes int64, allowEmpty bool}`.
- `internal/agui/strict_decode_test.go` — NEW: `TestStrictDecodeJSON` (table-driven).
- `internal/agui/strict_decode_routes_test.go` — NEW: per-route httptest regression proving trailing JSON now 400s.
- Modified callers: `server_run_request.go`, `approvals_api.go`, `onboarding_api.go`, `assets_api.go`, `governance_write_api.go` (`decodeMCPBody`), `governance_write_skills_api.go` (`decodeSkillsBody`).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: strictDecodeJSON helper — test-first (RED then GREEN)</name>
  <files>internal/agui/strict_decode.go, internal/agui/strict_decode_test.go</files>
  <read_first>
    - internal/agui/governance_write_api.go:190-201 (decodeMCPBody — the VULNERABLE single-decode idiom; keep MaxBytesReader+allowEmpty, FIX trailing JSON)
    - internal/agui/server.go:28 (const maxRunBodyBytes = 1 << 20 — the shared default cap)
    - 40-CONTEXT.md D-16/D-16b/D-16c; 40-RESEARCH.md Pattern 3 (the two-decode helper) + Pitfall 1
    - 40-PATTERNS.md "SEC-06 strict_decode.go" section (the correct two-decode helper + the 3 anti-pattern sites)
  </read_first>
  <behavior>
    - Valid single JSON object, correct/absent Content-Type, under cap: decodes into dst, no error.
    - Trailing JSON (two concatenated values): error "unexpected trailing data" (the F-052 repro).
    - Unknown field with DisallowUnknownFields: error.
    - Empty body: allowEmpty=true returns nil; allowEmpty=false returns error.
    - Content-Type text/plain (present, non-json): error before decode; empty Content-Type: tolerated.
    - Body exactly maxRunBodyBytes: ok; body over maxRunBodyBytes: error (MaxBytesReader).
    - Trailing whitespace/newline after the value: ok; a second value: error.
  </behavior>
  <action>
    FIRST write internal/agui/strict_decode_test.go (TestStrictDecodeJSON, table-driven with httptest.NewRequest +
    httptest.NewRecorder) covering every row in the behavior block against the not-yet-existing helper; confirm RED.
    THEN create internal/agui/strict_decode.go defining decodeOpts{ maxBytes int64; allowEmpty bool } and
    strictDecodeJSON(w http.ResponseWriter, r *http.Request, dst any, opts decodeOpts) error: reject a present
    non-application/json Content-Type; default opts.maxBytes when <=0 to maxRunBodyBytes; set r.Body =
    http.MaxBytesReader(w, r.Body, cap); dec := json.NewDecoder(r.Body); dec.DisallowUnknownFields(); first
    dec.Decode(dst) where errors.Is(err, io.EOF) and opts.allowEmpty returns nil, any other error wraps to a
    400-suitable error; THEN the mandatory second dec.Decode(and a throwaway empty struct) which MUST return io.EOF,
    else return an "unexpected trailing data after JSON body" error. Return the error to the caller (callers map it
    to 400). Confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test ./internal/agui/ -run TestStrictDecodeJSON -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/agui/strict_decode.go contains DisallowUnknownFields() and a second Decode requiring io.EOF.
    - TestStrictDecodeJSON covers trailing-JSON, unknown-field, empty (both allowEmpty values), content-type, and oversize rows; exits 0.
    - The trailing-JSON case asserts a non-nil error (rejected).
    - go test ./internal/agui/ -run TestStrictDecodeJSON green in <30s, no skip.
  </acceptance_criteria>
  <done>The centralized helper implements the full two-decode idiom (unknown-field + trailing-data rejection + size cap + per-route allowEmpty), proven by the table test; the F-052 repro is rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the six privileged routes + per-route trailing-JSON regression</name>
  <files>internal/agui/server_run_request.go, internal/agui/approvals_api.go, internal/agui/onboarding_api.go, internal/agui/assets_api.go, internal/agui/governance_write_api.go, internal/agui/governance_write_skills_api.go, internal/agui/strict_decode_routes_test.go</files>
  <read_first>
    - internal/agui/server_run_request.go:26 (decodeRunAgentRequest takes a *json.Decoder; caller applies MaxBytesReader at server.go:393)
    - internal/agui/approvals_api.go:122-127 (MaxBytesReader + plain Decode), internal/agui/assets_api.go:80-85 (identical shape)
    - internal/agui/onboarding_api.go:270-304 (handleOnboardingMutation / prepareOnboardingMutation; decode at :279)
    - internal/agui/governance_write_api.go:190-201 (decodeMCPBody, allowEmpty:true), governance_write_skills_api.go:216-226 (decodeSkillsBody, allowEmpty:true)
    - internal/agui/governance_write_scheduler.go:130-135 (the D-16c DEFERRED route — do NOT touch, just note)
    - 40-RESEARCH.md Pattern 3 per-route table (allowEmpty values); 40-PATTERNS.md per-route allowEmpty table
  </read_first>
  <behavior>
    - Each of the 6 routes, given two concatenated JSON values, returns HTTP 400 (previously accepted).
    - Governance MCP and governance skills still accept an EMPTY body (verb-only) — not 400 on empty.
    - /agent/run, approvals, onboarding, assets reject an empty body (allowEmpty:false).
    - An unknown JSON field on any route returns 400.
  </behavior>
  <action>
    Route each of the six decoders through strictDecodeJSON with the correct decodeOpts:
    /agent/run: refactor handleRun/decodeRunAgentRequest so the body passes through strictDecodeJSON(w, r, and the
    request struct, decodeOpts{allowEmpty:false}), keeping ValidateRunInput afterward.
    approvals-resolve (approvals_api.go:124), onboarding (onboarding_api.go:279 inside the prepareOnboardingMutation
    decode closure), assets (assets_api.go:82): replace the plain Decode with strictDecodeJSON(..., decodeOpts{allowEmpty:false}),
    mapping a returned error to http.Error(w, ..., 400).
    governance MCP (decodeMCPBody) and governance skills (decodeSkillsBody): replace the vulnerable single-decode
    body with strictDecodeJSON(..., decodeOpts{allowEmpty:true}), preserving the existing bool return contract
    (true on success). Leave governance_write_scheduler.go UNCHANGED.
    Then add internal/agui/strict_decode_routes_test.go sending a two-value trailing-JSON body to each of the six
    routes (httptest) asserting 400, plus an empty-body case for the two governance routes asserting NOT-400.
    Record the D-16c scheduler-edit deferral in the SUMMARY acceptance evidence.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go build ./internal/agui/ && go test ./internal/agui/ -run 'TestStrictDecode' -count=1 && rg -c "strictDecodeJSON" internal/agui/server_run_request.go internal/agui/approvals_api.go internal/agui/onboarding_api.go internal/agui/assets_api.go internal/agui/governance_write_api.go internal/agui/governance_write_skills_api.go</automated>
  </verify>
  <acceptance_criteria>
    - rg -c "strictDecodeJSON" reports >=1 in each of the six route files (all six wired).
    - internal/agui/strict_decode_routes_test.go asserts 400 for a trailing-JSON body on all six routes and NOT-400 for an empty body on the two governance routes.
    - go build ./internal/agui/ succeeds; the route regression tests pass in WSL.
    - governance_write_scheduler.go is unchanged (rg -c strictDecodeJSON governance_write_scheduler.go returns 0) and the deferral is noted in the SUMMARY.
  </acceptance_criteria>
  <done>All six privileged routes reject trailing/unknown-field bodies via the shared helper with correct per-route allowEmpty; scheduler-edit is consciously deferred and flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted HTTP client → privileged agui route body | Attacker-controlled JSON crosses into /agent/run, approvals, onboarding, assets, governance writes — the strict-decode boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-SMUGGLE | Tampering | trailing-JSON body smuggling past a single-decode | mitigate | The mandatory second Decode-must-be-io.EOF step (D-16b) on all six routes; per-route regression asserts 400. |
| T-40-03-UNKNOWN | Tampering | unknown-field injection (mass-assignment shape) | mitigate | dec.DisallowUnknownFields() in the shared helper (F-052). |
| T-40-03-DOS | Denial of Service | oversized body allocation | mitigate | http.MaxBytesReader default maxRunBodyBytes (1 MiB) enforced by the helper. |
| T-40-03-CTYPE | Spoofing | wrong Content-Type carrying JSON | mitigate | Present non-application/json Content-Type is rejected before decode. |
| T-40-03-SCHED | Tampering | scheduler-edit left un-gated | accept | D-16c: consciously deferred to refactor-on-touch; flagged in the SUMMARY so a reviewer sees the intentional gap. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib encoding/json only. |
</threat_model>

<verification>
- go test ./internal/agui/ -run TestStrictDecodeJSON and -run TestStrictDecode... green (WSL), no skip.
- rg -c strictDecodeJSON shows all six route files wired; scheduler-edit shows 0 (deferred).
- go vet + go build ./internal/agui/ clean; go test -race ./internal/agui/ green for the touched routes.
- The two-decode idiom (DisallowUnknownFields + second Decode io.EOF) present in strict_decode.go.
</verification>

<success_criteria>
F-052 (SEC-06) closed: a single tested strict-decode helper (size cap + content-type check + DisallowUnknownFields
+ trailing-JSON rejection + per-route allowEmpty) governs the six audit-named privileged routes, the trailing-JSON
repro is rejected, and the scheduler-edit deferral is documented (D-16c).
</success_criteria>

<output>
Create `.planning/phases/40-security-supply-chain-pack/40-03-SUMMARY.md` when done.
</output>
