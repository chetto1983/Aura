---
phase: 12-ag-ui-gateway
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - internal/agui/fanout.go
  - internal/agui/client.go
  - internal/agui/fanout_test.go
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "Fanout distributes a translated AG-UI event stream to N concurrent subscriber channels without blocking the producer"
    - "A slow subscriber is dropped (with a WARN) instead of back-pressuring the Loop"
    - "Every fanout/client goroutine exits on ctx-cancel or producer end (goleak-clean)"
    - "client.go exposes an in-process subscriber helper over the translated stream (the Phase-13 Telegram consumer seam)"
  artifacts:
    - path: "internal/agui/fanout.go"
      provides: "Fanout struct: wrap iter.Seq2 → N subscriber chans with cap-64 buffered drop-on-full"
      min_lines: 50
    - path: "internal/agui/client.go"
      provides: "In-process subscriber helper + SDK type aliases to keep the external package out of call sites"
      min_lines: 40
    - path: "internal/agui/fanout_test.go"
      provides: "goleak + slow-subscriber-drop + disconnect-leak property"
      min_lines: 60
  key_links:
    - from: "internal/agui/fanout.go"
      to: "internal/agui/translator.go"
      via: "wraps the Translate output iter.Seq2[events.Event,error]"
      pattern: "iter\\.Seq2\\[.*events\\.Event"
    - from: "internal/agui/fanout.go"
      to: "buffered channel + select ctx.Done + default-drop"
      via: "non-blocking pump (openai_compat Stream analog)"
      pattern: "select|ctx\\.Done\\(\\)|default:"
---

<objective>
Build the Slice 8a in-process fan-out: `fanout.go` (the `Fanout` struct that wraps a translated `iter.Seq2[events.Event, error]` and distributes to N concurrent subscriber channels with cap-64 buffered, drop-on-full, never-block-the-Loop semantics) and `client.go` (the in-process subscriber helper + SDK type aliases). This is the seam Phase 13 (Telegram) consumes — `internal/agui/fanout.go` centralizes the Slice-9b→Slice-8 dependency that was retro-added in an earlier audit (PRD file-targets note).

Purpose: UX-01's fanout is explicitly NOT on the HTTP path (RESEARCH diagram line 134; amendment #35) — Telegram consumes in-process via fanout, not HTTP/SSE. Landing it here centralizes the dependency and gives the Phase-13 channel a leak-clean, backpressure-bounded subscriber API.

Output: `internal/agui/{fanout,client}.go` + `fanout_test.go` (goleak + slow-subscriber-drop + disconnect-leak).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ag-ui-gateway/12-RESEARCH.md
@.planning/phases/12-ag-ui-gateway/12-PATTERNS.md
@.claude/skills/golang-concurrency/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fanout.go — cap-64 buffered, drop-on-full, never-block Fanout over the translated stream</name>
  <read_first>
    - internal/agui/translator.go (the `Translate` output type `iter.Seq2[events.Event, error]` Fanout wraps — from Plan 01)
    - internal/llm/openai_compat/client.go lines 108-130 (THE backpressure-pump analog: `out := make(chan ..., chunkBuffer)`, `go func(){ defer close(out); emit := func(ch){ select { case out<-ch: case <-ctx.Done(): return false } } }`; the named-cap const convention `chunkBuffer`)
    - .planning/phases/12-ag-ui-gateway/12-PATTERNS.md §fanout.go (the third `default:` drop arm with `slog.Warn("agui fanout: subscriber slow, dropping event", "type", ev.Type())`; the `fanoutBuffer = 64` named const)
    - .planning/phases/12-ag-ui-gateway/12-RESEARCH.md Pattern 4 (backpressure) + Pitfall 4 (pump leak on disconnect)
    - .claude/skills/golang-concurrency/SKILL.md (core principle: sole sender closes; every send is a select with ctx.Done; goleak)
    - internal/swarm/swarm_property_test.go lines 1-45 (rapid + goleak shape for fanout_test.go)
  </read_first>
  <behavior>
    - A `Fanout` with K subscribers fed an N-event stream delivers each event to every subscriber whose channel has room; the producer never blocks waiting on any subscriber
    - A subscriber that never reads, once its cap-64 buffer fills, gets subsequent events DROPPED (one WARN per drop) — other subscribers keep receiving
    - On ctx-cancel, the producer goroutine and all subscriber-feeder goroutines exit; all subscriber channels close (sole sender closes)
    - On producer end (the wrapped iter.Seq2 finishes), all subscriber channels close cleanly
    - `runtime.NumGoroutine()` returns to baseline after Fanout completes or ctx is cancelled (goleak)
  </behavior>
  <action>
    Create `internal/agui/fanout.go` (~80 LOC). Declare `const fanoutBuffer = 64` (the AURA_AGUI_* env-tunable cap is exposed via config in Plan 03; here use the named const default, mirroring `chunkBuffer`). Define a `Fanout` struct that wraps the translated `iter.Seq2[events.Event, error]`. Provide `Subscribe() <-chan events.Event` returning a fresh cap-64 buffered channel, and a `Run(ctx)` (or constructor that spawns) which ranges the source iter.Seq2 once and, for each event, sends to every subscriber via the three-arm select copied from the openai_compat pump PLUS the `default:` drop arm (PATTERNS §fanout.go): `case sub <- ev:` / `case <-ctx.Done(): return` / `default: slog.Warn("agui fanout: subscriber slow, dropping event", "type", ev.Type())`. The single producer goroutine is the SOLE sender on every subscriber channel and `defer close`s them all on exit (ctx-cancel OR source end). Errors from the source iter.Seq2 are surfaced to subscribers as a translated RUN_ERROR event already (the Translate output is the events.Event stream — do not re-handle the error slot; the translator already mapped it). Keep Fanout standalone — it is NOT routed through by server.go (RESEARCH diagram line 134); document that in a one-line WHY comment.
  </action>
  <verify>
    <automated>go vet ./internal/agui/ && go build ./... && go test -race ./internal/agui/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/agui/` passes the fanout tests with `goleak.VerifyTestMain`/`VerifyNone` clean
    - A test with one never-reading subscriber + one reading subscriber shows the reading subscriber receives all events and the producer completes (no deadlock) — the slow one's overflow is dropped with a WARN
    - A ctx-cancel mid-stream test shows all subscriber channels close and `runtime.NumGoroutine()` returns to baseline
    - `grep -c 'ctx.Done()' internal/agui/fanout.go` ≥ 1 and the `default:` drop arm is present (every channel send has both a ctx.Done arm and a drop arm)
    - `bash scripts/agui_boundary_check.sh` still exits 0; `bash scripts/check-file-size.sh` passes
  </acceptance_criteria>
  <done>fanout.go distributes the translated stream to N subscribers with drop-on-full backpressure, never blocks the producer, and is goleak-clean on cancel and on source end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: client.go — in-process subscriber helper + SDK type aliases</name>
  <read_first>
    - internal/agui/fanout.go (the Fanout the client subscribes to — Task 1)
    - internal/agui/translator.go + types.go (the Translate signature + IDGenerator the client drives — Plan 01)
    - internal/llm/openai_compat/client.go lines 80-130 (the emit-closure / streaming shape; client.go here is the read side)
    - .planning/phases/12-ag-ui-gateway/12-PATTERNS.md §client.go (role-match: in-proc subscriber over the buffered-chan pump)
    - prd.md §Slice 8 File targets — client.go "~80 LOC. Wrapper sul Go SDK community core/events. Type aliases per evitare leak del package esterno nei call sites."
    - .planning/phases/12-ag-ui-gateway/12-RESEARCH.md §"Reasoning dual-field" (REASONING_* mapping note — accept-both reasoning/reasoning_content; do NOT add semantic parsing, this phase only needs the alias surface)
  </read_first>
  <behavior>
    - An in-process consumer can obtain a typed channel of AG-UI events from a translated agent turn (threadID + runID + the agent iter.Seq2) without importing the external SDK package directly at the call site
    - The client helper drives `Translate` and feeds a `Fanout` (or returns a single subscriber channel) so a caller like a future Telegram channel ranges AG-UI events directly
    - SDK type aliases (`type Event = events.Event` etc.) keep `github.com/ag-ui-protocol/...` out of consumer call sites (PRD: avoid leaking the external package)
  </behavior>
  <action>
    Create `internal/agui/client.go` (~80 LOC). Declare the minimal SDK type aliases the PRD calls for so downstream call sites reference `agui.Event` not `events.Event` (e.g. `type Event = events.Event`; only alias what the in-process subscriber surface exposes — do not blanket-alias the whole SDK). Provide an in-process subscriber helper that composes Plan 01's `Translate` with Task 1's `Fanout`: a function/struct that, given `threadID, runID string`, an `IDGenerator`, and the agent `iter.Seq2[*agent.Event, error]`, returns a subscriber `<-chan Event` (single-consumer) or wires a `Fanout` for N consumers, with the same ctx-cancel discipline. This is the seam Phase 13 Telegram consumes (`agui_subscriber.go` per amendment #35) — keep it transport-free (no HTTP, no SSE; that is Plan 03's server.go). Document the dual-field REASONING_* note as a forward-compat comment only (no parsing added this phase). Reuse, do NOT duplicate, the Fanout pump.
  </action>
  <verify>
    <automated>go vet ./internal/agui/ && go build ./... && go test -race ./internal/agui/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/agui/` passes; a client-subscriber test ranges a translated agent stream end-to-end (RUN_STARTED…RUN_FINISHED) via the in-process channel
    - `grep -c 'github.com/ag-ui-protocol' internal/agui/client.go` is minimal (aliases only); downstream tests reference `agui.Event` (or the chosen alias), not `events.Event`
    - No HTTP/SSE imports in client.go (`grep -c 'net/http\|encoding/sse' internal/agui/client.go` == 0) — transport-free subscriber seam
    - `bash scripts/agui_boundary_check.sh` exits 0; `golangci-lint run ./internal/agui/` 0 issues; `bash scripts/check-file-size.sh` passes
  </acceptance_criteria>
  <done>client.go exposes a transport-free in-process AG-UI subscriber over Translate+Fanout with SDK type aliases shielding call sites; leak-clean; the Phase-13 Telegram consumer seam is ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer (agent turn) → subscriber channels | A slow/dead subscriber must not back-pressure the agent Loop. Drop-on-full decouples them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Denial of Service | Fanout producer | mitigate | cap-64 buffered subscriber channels + `default:` drop-with-WARN arm; the producer never blocks on a slow subscriber (RESEARCH Pattern 4). |
| T-12-06 | Denial of Service | goroutine leak | mitigate | sole sender closes; every send is a select with a `<-ctx.Done()` arm; goleak TestMain proves exit on cancel + on source end (Pitfall 4). |
| T-12-07 | Information Disclosure | aliased SDK types | accept | client.go only re-exports event TYPE aliases; no new data crosses a trust boundary versus the translator. The translated stream's RUN_ERROR sanitization is owned by Plan 01/03. |
</threat_model>

<verification>
- `go vet ./... && go build ./... && go test -race ./internal/agui/` green (incl. fanout + client tests)
- goleak clean on ctx-cancel and on source-end; slow-subscriber drop verified
- `bash scripts/agui_boundary_check.sh` exits 0; `golangci-lint run ./internal/agui/` 0 issues; file-size cap holds
</verification>

<success_criteria>
- `fanout.go` distributes the translated AG-UI stream to N subscribers with drop-on-full backpressure, never blocking the Loop, goleak-clean (UX-01 fanout obligation)
- `client.go` exposes a transport-free in-process subscriber seam + SDK type aliases (the Phase-13 Telegram consumer dependency, centralized here)
</success_criteria>

<output>
Create `.planning/phases/12-ag-ui-gateway/12-02-SUMMARY.md` when done.
</output>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Types / functions (new symbols — exclude from drift verification):
- `agui.Fanout` struct + `Subscribe() <-chan events.Event` + `Run(ctx)` (cap-64 drop-on-full pump)
- `agui.fanoutBuffer` const (= 64)
- in-process subscriber helper in `client.go` (Translate + Fanout composition)
- SDK type aliases (`agui.Event = events.Event`, minimal alias surface)

New file paths:
- `internal/agui/fanout.go`
- `internal/agui/client.go`
- `internal/agui/fanout_test.go`
</artifacts_produced>
