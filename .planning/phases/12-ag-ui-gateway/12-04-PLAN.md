---
phase: 12-ag-ui-gateway
plan: 04
type: execute
wave: 4
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - scripts/agui_smoke.sh
  - .github/workflows/ci.yml
  - docs/aura-quality-snapshot.md
  - .planning/phases/12-ag-ui-gateway/12-VALIDATION.md
autonomous: false
requirements: [UX-01]
must_haves:
  truths:
    - "scripts/agui_smoke.sh runs aura serve, curls POST /agent/run, and asserts RUN_STARTED…RUN_FINISHED SSE frames + GET /threads/<id>/messages JSON"
    - "CI runs the agui db_integration tier (SC1/SC3) with the existing AURA_DB_URL/AURA_DB_MIGRATE_URL/CI=true env (no skip-as-green)"
    - "Owned-surface coverage for internal/agui is ≥85% across the tag matrix"
    - "translator.go mutation spot-check ≥70% killed"
    - "The operator confirms a live curl SSE round-trip against a real (or FakeClient) aura serve"
  artifacts:
    - path: "scripts/agui_smoke.sh"
      provides: "Live curl SSE round-trip + GET snapshot smoke (SC1/SC3 operator/CI gate)"
    - path: "docs/aura-quality-snapshot.md"
      provides: "Phase 12 coverage + mutation + smoke rows"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "./internal/agui/... db_integration tier"
      via: "integration-test job go test -tags db_integration"
      pattern: "internal/agui"
    - from: "scripts/agui_smoke.sh"
      to: "POST /agent/run + GET /threads/<id>/messages"
      via: "curl assertions on the live daemon"
      pattern: "agent/run|threads/"
---

<objective>
Close Phase 12 Gate-3: wire the `internal/agui` db_integration tier into CI (SC1/SC3, no skip-as-green), author the live `scripts/agui_smoke.sh` curl round-trip, run the translator.go mutation spot-check + the owned-surface coverage gate, record the Phase-12 quality-snapshot rows, flip the (planner-pre-filled) 12-VALIDATION.md map to green + set `wave_0_complete: true`, and get the operator's live human-verify sign-off on a real `aura serve` + `curl` SSE stream.

Purpose: UX-01's success criteria 1 and 3 are operator-observable HTTP behaviors that must be proven LIVE (curl against a running daemon), not just compile-checked. The phase closes only after the full tag matrix is green, coverage ≥85%, translator mutation ≥70%, and the operator confirms the live stream (the prior-phase Gate-3 pattern).

Wave note: this plan is Wave 4 — it runs after Plan 03 (Wave 3) lands the server + server_test.go that the db_integration CI tier and the smoke exercise. The Per-Task Verification Map in 12-VALIDATION.md is ALREADY filled by the planner (one row per task, with Plan/Wave/Requirement/Threat-Ref/Automated-Command/File-Exists locked pre-execution); this plan only flips the Status cells to green as artifacts land and sets `wave_0_complete: true`.

Output: `scripts/agui_smoke.sh`, the CI integration-tier wiring, the quality-snapshot rows, the VALIDATION.md status flip, and the operator checkpoint.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ag-ui-gateway/12-RESEARCH.md
@.planning/phases/12-ag-ui-gateway/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: agui_smoke.sh live round-trip + CI integration-tier wiring + coverage/mutation + VALIDATION status flip</name>
  <files>scripts/agui_smoke.sh, .github/workflows/ci.yml, docs/aura-quality-snapshot.md, .planning/phases/12-ag-ui-gateway/12-VALIDATION.md</files>
  <read_first>
    - scripts/check-file-size.sh (the shell-gate shape: shebang + `set -euo pipefail` + exit codes + remediation line — model agui_smoke.sh on this)
    - .github/workflows/ci.yml lines 114-192 (the integration-test job: it exports `AURA_DB_URL`/`AURA_DB_MIGRATE_URL`/`CI=true` at lines 122-142, runs `go test -tags db_integration -race -count=1 -p 1 ./internal/db/... ./internal/cron/...` at line 165 — ADD `./internal/agui/...` to that step's package list; keep `-p 1` for the shared-Postgres advisory-lock rule)
    - cmd/aura/serve.go + cmd/aura/chat.go (the `aura serve` daemon + how a conversation id is minted — the smoke needs an existing threadId; reuse `aura chat new` / the runner.NewConversation path or seed a conversation row before the curl)
    - docs/aura-quality-snapshot.md (the living-doc row format to append Phase-12 rows)
    - scripts/coverage_gate.sh (the owned-surface ≥85% gate; `internal/agui` is owned surface — confirm it is in scope, NOT excluded as generated/skeleton)
    - .planning/phases/12-ag-ui-gateway/12-VALIDATION.md (the Per-Task Verification Map — ALREADY filled by the planner; this task only flips Status cells + sets wave_0_complete: true)
    - .planning/phases/12-ag-ui-gateway/12-RESEARCH.md §"Sampling Rate" + §Environment Availability (OpenRouter key operator-gated → FakeClient covers the automated tier; live curl is the operator checkpoint)
  </read_first>
  <action>
    Create `scripts/agui_smoke.sh` modeled on the repo shell-gate shape (`#!/usr/bin/env bash`, `set -euo pipefail`, documented exit codes, remediation line). It must: (1) build `aura`, (2) ensure a conversation row exists and capture its threadId (e.g. `aura chat new` or a seed step against the same DSN the integration tier uses), (3) start `aura serve` in the background and wait for `127.0.0.1:9080` to accept connections (poll, no fixed sleep), (4) `curl -N -X POST http://127.0.0.1:9080/agent/run -H 'Content-Type: application/json' -d` a RunAgentInput with that threadId + a user message, and ASSERT the SSE frames contain `event: RUN_STARTED` … `event: RUN_FINISHED` in order (grep the frame types; do NOT assert on the model's reply text — assert the FRAME ground truth, not the prose), (5) `curl http://127.0.0.1:9080/threads/<threadId>/messages` and assert a `MESSAGES_SNAPSHOT` JSON body with ≥1 message, (6) tear down the daemon (kill + wait). Under `CI=true` the script must hard-fail when the DB env is unset — never skip-as-green. Gate the LIVE-LLM leg behind `OPENROUTER_API_KEY` presence; without it, drive a FakeClient path (the automated CI gate uses FakeClient; the live OpenRouter curl is the operator checkpoint in Task 2). Print ≥1 SSE frame body so the operator inspects it visually (memory: inspect artifact, not just PASS).

    Wire CI in `.github/workflows/ci.yml`: add `./internal/agui/...` to the integration-test job's `go test -tags db_integration -race -count=1 -p 1` package list (it already exports the DSNs + CI=true — reuse them; do NOT duplicate the env block). Add an `agui smoke (FakeClient)` step that runs `bash scripts/agui_smoke.sh` in a job that has Postgres up (the integration-test job or a sibling), CI=true armed.

    Run the owned-surface coverage gate (`scripts/coverage_gate.sh` / `make coverage`, AURA_COVERAGE_MIN default) and the translator.go mutation spot-check on WSL (`go-mutesting ./internal/agui/translator.go`, PASS=killed, score=killed/total). Autopsy survivors before chasing the score (memory: classify near-equivalent survivors, kill the meaningful ones via seam/message-contract, advisory-accept the rest with documentation).

    Update `.planning/phases/12-ag-ui-gateway/12-VALIDATION.md`: the Per-Task Verification Map is ALREADY filled (planner pre-locked Plan/Wave/Requirement/Threat-Ref/Automated-Command/File-Exists). This task ONLY: (a) flips each row's Status cell to ✅ green as the matching artifact lands and its command passes, (b) flips the `File Exists` markers from ❌/⬜ to ✅, (c) sets `wave_0_complete: true` in the frontmatter once Wave-0 artifacts (pin, golden, boundary, smoke) all exist on disk. Do NOT re-author the map rows — `nyquist_compliant: true` is already set by the planner.
  </action>
  <verify>
    <automated>go test -tags db_integration -race -count=1 -p 1 ./internal/agui/...</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags db_integration -race -count=1 -p 1 ./internal/agui/...` passes with the stack up; the tier RUNS (sub-second runtime would be a skip tell — verify execution)
    - `bash scripts/agui_smoke.sh` exits 0 with the stack up: the POST stream contains `event: RUN_STARTED` and `event: RUN_FINISHED` (printed), and the GET returns a MESSAGES_SNAPSHOT JSON with ≥1 message (this is the operator/Gate-3 reference command, run in Task 2 — not the autonomous `<verify>` here)
    - `.github/workflows/ci.yml` integration-test step package list contains `./internal/agui/...`; a CI step invokes `bash scripts/agui_smoke.sh`
    - Owned-surface coverage for `internal/agui` ≥ 85% (combined unit + db_integration matrix, reported as a number)
    - `translator.go` mutation score ≥ 70% killed (or documented advisory-accept of near-equivalent survivors)
    - `docs/aura-quality-snapshot.md` has Phase-12 rows (coverage %, translator mutation %, smoke status)
    - `12-VALIDATION.md` Per-Task Verification Map Status cells flipped to ✅ (rows themselves already filled by the planner); `wave_0_complete: true` set
  </acceptance_criteria>
  <done>The agui db_integration tier runs in CI; agui_smoke.sh proves the live SSE round-trip + GET snapshot; coverage ≥85% and translator mutation ≥70% recorded in the quality snapshot; VALIDATION map Status flipped green + wave_0_complete: true.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator live Gate-3 sign-off — aura serve + curl SSE round-trip (OpenRouter) via agui_smoke.sh</name>
  <action>This is a human-verify checkpoint: no autonomous code change. The executor presents the live-verification steps below and pauses for the operator to confirm the real OpenRouter SSE round-trip, the GET snapshot, the 404, and the graceful shutdown. `scripts/agui_smoke.sh` is the canonical Gate-3 reference command here (live daemon, stack-dependent, ~120s) — it is intentionally NOT the autonomous CI gate (the fast db_integration tier in Task 1 is); this is the operator-observed live leg.</action>
  <what-built>
    A loopback AG-UI gateway on the `aura serve` daemon: `POST /agent/run` streams translated AG-UI SSE events from a live agent turn, and `GET /threads/<id>/messages` returns the persisted history as a MESSAGES_SNAPSHOT. The automated CI gate proves SC1/SC3 with a FakeClient (fast db_integration tier); this checkpoint confirms the LIVE OpenRouter round-trip (operator-gated key) per the prior-phase Gate-3 pattern.
  </what-built>
  <how-to-verify>
    1. Bring the stack up (Postgres + the sidecars) and set `OPENROUTER_API_KEY` (source `.env` per the run-binary memory: strip single quotes from the password vars).
    2. Build + start the daemon: `go build -o aura ./cmd/aura && ./aura serve` (observe the log line `aura serve: agui http server listening addr=127.0.0.1:9080`).
    3. In another shell, mint a conversation: `./aura chat new` (or note an existing threadId from `./aura chat list`).
    4. Run: `curl -N -X POST http://127.0.0.1:9080/agent/run -H 'Content-Type: application/json' -d '{"threadId":"<conv_uuid>","messages":[{"role":"user","content":"ciao dimmi 2+2"}]}'`
       Expected: an SSE stream with `event: RUN_STARTED`, `event: TEXT_MESSAGE_START`/`CONTENT` (token-per-token deltas), `event: TEXT_MESSAGE_END`, and a final `event: RUN_FINISHED` with `data` outcome `{"type":"success"}`. Confirm the answer text reconstructs from the CONTENT deltas (e.g. "4") and is NOT double-streamed.
    5. Run: `curl http://127.0.0.1:9080/threads/<conv_uuid>/messages` — expected: an `application/json` MESSAGES_SNAPSHOT whose messages include the user turn + the assistant answer just streamed (matches the SSE content).
    6. Confirm `curl http://127.0.0.1:9080/threads/does-not-exist/messages` returns HTTP 404.
    7. Send SIGTERM to the daemon (Ctrl+C) and confirm a graceful shutdown log line (no panic, no leaked goroutine warning).
    (Equivalently: `bash scripts/agui_smoke.sh` with `OPENROUTER_API_KEY` set drives steps 2-6 and prints ≥1 SSE frame body for visual inspection.)
  </how-to-verify>
  <verify>
    <human-check>Operator runs steps 1-7 (or `bash scripts/agui_smoke.sh` live) and observes the live SSE round-trip + GET snapshot + 404 + graceful shutdown.</human-check>
  </verify>
  <done>Operator approves the live OpenRouter SSE round-trip, GET snapshot, 404, and graceful shutdown.</done>
  <resume-signal>Type "approved" once the live SSE round-trip + GET snapshot + 404 + graceful shutdown all check out, or describe the deviation (e.g. double-streamed final answer, tool-result leaking as assistant text, frame ordering).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → live `aura serve` smoke | The smoke drives the real HTTP surface; it exercises the same loopback bind + input-validation controls Plan 03 ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Denial of Service | CI no-skip-as-green | mitigate | The agui db_integration tier exports the exact DSN env it reads and `t.Fatal`s under `CI=true` when unset; a skipped tier fails the gate (a sub-second runtime is a skip tell — verify execution). |
| T-12-15 | Information Disclosure | live smoke output | mitigate | agui_smoke.sh asserts SSE FRAME ground truth (event types), not raw secrets; the printed body is the public AG-UI wire payload, never a DSN/key. |
| T-12-SC | Tampering | (carried) | mitigate | No new package installs in this plan; the AG-UI SDK pin gate (Plan 01) remains the supply-chain control. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. |
</threat_model>

<verification>
- `go test -tags db_integration -race -count=1 -p 1 ./internal/agui/...` runs and passes (SC1/SC3) — the autonomous Gate
- `bash scripts/agui_smoke.sh` exits 0 (live SSE round-trip + GET snapshot) — the operator/Gate-3 reference (Task 2)
- Coverage ≥85% owned-surface; translator.go mutation ≥70%; quality-snapshot rows recorded
- Operator human-verify approval on the live OpenRouter round-trip + graceful shutdown
</verification>

<success_criteria>
- The agui db_integration tier + smoke run in CI (no skip-as-green); SC1/SC3 proven live
- Coverage ≥85% owned-surface; translator mutation ≥70%; snapshot updated
- 12-VALIDATION.md Per-Task Verification Map Status flipped green; wave_0_complete: true (nyquist_compliant already true)
- Operator confirms the live curl SSE round-trip + GET snapshot + 404 + graceful shutdown
</success_criteria>

<output>
Create `.planning/phases/12-ag-ui-gateway/12-04-SUMMARY.md` when done.
</output>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New file paths:
- `scripts/agui_smoke.sh` (live SSE round-trip + GET snapshot smoke)

CI additions:
- integration-test job: `./internal/agui/...` added to the `go test -tags db_integration` package list (SC1/SC3)
- a `bash scripts/agui_smoke.sh` step (FakeClient, Postgres up, CI=true)

Docs:
- `docs/aura-quality-snapshot.md` Phase-12 rows (coverage %, translator mutation %, smoke status)
- `12-VALIDATION.md` Per-Task Verification Map Status flipped to ✅; `wave_0_complete: true`
</artifacts_produced>
</output>
