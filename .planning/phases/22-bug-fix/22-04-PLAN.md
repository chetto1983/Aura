---
phase: 22-bug-fix
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - internal/agent/hooks.go
  - internal/agent/hooks_command.go
  - internal/agent/hooks_command_test.go
  - internal/agent/llm_agent_hooks_test.go
  - internal/agent/trust.go
  - internal/agent/llm_agent_provenance_test.go
  - internal/swarm/runner_adapter.go
  - internal/swarm/runner_adapter_test.go
  - internal/agent/tools/fs_read.go
  - internal/agent/tools/fs_write.go
  - internal/agent/tools/fs_edit.go
  - internal/agent/tools/fs_grep.go
  - internal/agent/tools/fs_glob.go
  - internal/agent/tools/fs_test.go
  - internal/agent/tools/fs_fence_test.go
  - internal/agent/tools/shell_bg.go
  - internal/agent/tools/shell_bg_test.go
  - internal/agent/tools/task.go
  - internal/agent/tools/task_test.go
  - internal/agent/tools/send_file.go
  - internal/agent/tools/send_file_test.go
  - internal/agent/tools/search.go
  - internal/agent/tools/search_mount_test.go
  - internal/agent/tools/read_tool_output.go
  - internal/agent/tools/read_tool_output_test.go
  - internal/agent/workflow/workflow.go
  - internal/agent/workflow/workflow_contract_test.go
  - internal/agent/workflow/parallel.go
  - internal/agent/workflow/parallel_test.go
  - internal/agent/workflow/sequential.go
  - internal/agent/workflow/sequential_test.go
  - internal/agent/prompt/hash.go
  - internal/agent/prompt/hash_test.go
autonomous: true
requirements: [HARDEN-07, HARDEN-08, HARDEN-09, HARDEN-10, HARDEN-12]
must_haves:
  truths:
    - "D-03: the later live pass must exercise the full tool surface including GLM OCR; this plan adds the tool-surface guards needed for that pass."
    - "D-05: AURA_FS_MAX_READ_BYTES starts at 10 MiB, stat-then-rejects over cap, and returns a paging hint."
    - "D-09: stay inside internal/agent/internal/swarm for these findings; unrelated OPS and multi-tenant work is ledgered/routed."
    - "Hook fail_open is default for non-security hooks; fail_closed remains available and explicit."
    - "Unknown tool and swarm child output default to untrusted unless explicitly allowlisted safe."
  artifacts:
    - path: "internal/agent/hooks_command.go"
      provides: "fail_open/fail_closed policy, absolute hook path validation, rewrite exit-zero requirement"
      contains: "fail_open"
    - path: "internal/agent/trust.go"
      provides: "default-untrusted provenance for unknown tools"
      contains: "untrustedSource"
    - path: "internal/swarm/runner_adapter.go"
      provides: "untrusted provenance propagation for swarm child reports"
      contains: "TrustUntrusted"
    - path: "internal/agent/tools/fs_read.go"
      provides: "AURA_FS_MAX_READ_BYTES cap and paging hint"
      contains: "AURA_FS_MAX_READ_BYTES"
    - path: "internal/agent/workflow/workflow.go"
      provides: "cycle-safe findInTree"
      contains: "visited"
---

<objective>
Close the broad P2/P3 operational hardening wave after the crash, secret, observability, MCP, and budget base is in place: hook reliability, default-untrusted provenance, tool memory/session/file consistency, workflow cycle/leak proofing, and runtime cache-prefix drift detection.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bug-fix/22-SPEC.md
@.planning/phases/22-bug-fix/22-CONTEXT.md
@.planning/phases/22-bug-fix/22-RESEARCH.md
@docs/audit/bug-report.md
@docs/audit/proposed-patches.md
@docs/audit/testing-strategy.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hook reliability for AG-004, AG-030, AG-054, AG-058 and remaining AG-003 rewrite bounds</name>
  <files>internal/agent/hooks.go, internal/agent/hooks_command.go, hook tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-003, AG-004, AG-030, AG-054, AG-058 and `proposed-patches.md` PP-4.
    - `22-CONTEXT.md` out-of-scope note: do not take full exec-by-fd TOCTOU close; land minimal-env/fail-soft/single-operator slice.
    - `internal/agent/hooks.go`, `hooks_command.go`, `hooks_command_test.go`, `llm_agent_hooks_test.go`.
  </read_first>
  <action>
    Add per-hook failure policy with explicit `fail_open` and `fail_closed`; default non-security hooks to `fail_open`. Under `fail_open`, timeout/error/panic/trust mismatch should log, metric, and allow; under `fail_closed`, abort with a clear reason.

    Wrap in-process hook calls in recover. Command-hook child env minimal was handled in Plan 22-02; preserve it.

    Require command hook paths to be absolute. Reject bare command names resolved via runtime PATH.

    Require exit code 0 for `rewrite`; allow `deny` on non-zero only if the design intentionally treats denial as security-safe. A crashed-after-emitting rewrite must not be applied as success.

    Validate hook-supplied request/tool rewrites for bounded size, message/tool counts, and allowed fields before replacing live request state. Record rewrite/deny in reasoning trace/audit without raw secrets.
  </action>
  <verify>
    <automated>go test ./internal/agent -run 'TestCommandHook|TestHookManager|TestLlmAgentHooks' -count=1</automated>
    <automated>go test -race ./internal/agent -run 'Test.*Hook.*Panic|Test.*Hook.*Fail' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Failing, timed-out, and panicking hooks complete the turn under `fail_open`.
    - The same failures abort with a clear reason under `fail_closed`.
    - Non-zero-exit rewrite is rejected.
    - Bare hook command names are rejected.
    - Oversized/invalid rewrites are rejected and audited.
  </acceptance_criteria>
  <done>Hook faults are contained by policy, not turn-fatal by default, and rewrite behavior is bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default-untrusted provenance for AG-052</name>
  <files>internal/agent/trust.go, internal/swarm/runner_adapter.go, provenance tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-052 and `proposed-patches.md` PP-8.
    - `internal/agent/trust.go` and provenance tests.
    - `internal/swarm/runner_adapter.go`, `internal/swarm/report.go`, and swarm report tests.
    - Existing nonce untrusted-output envelope tests.
  </read_first>
  <action>
    Invert trust default: unknown tool output is untrusted unless the tool is explicitly allowlisted safe. Keep existing known untrusted tools untrusted and add a small explicit trusted allowlist only for genuinely safe built-ins.

    Stamp swarm child reports that may embed child web/fs/tool content with `TrustUntrusted` so parent prompts wrap them in the nonce `<tool_output trust="untrusted">` envelope.
  </action>
  <verify>
    <automated>go test ./internal/agent -run 'Test.*Provenance|Test.*Trust|Test.*Untrusted' -count=1</automated>
    <automated>go test ./internal/swarm -run 'Test.*RunnerAdapter|Test.*Report|Test.*Trust' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Unknown tool output is wrapped as untrusted in the prompt.
    - A swarm child's malicious web/file content is enveloped in the parent prompt.
    - Explicit safe built-ins remain trusted by test, not by broad default.
  </acceptance_criteria>
  <done>Unknown and swarm-child output cannot launder prompt injection through trusted provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tool hardening for AG-014..020, AG-045, AG-046, AG-050</name>
  <files>fs tools, shell_bg, task, send_file, search, read_tool_output, tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-014, AG-015, AG-016, AG-017, AG-018, AG-019, AG-020, AG-045, AG-046, AG-050 and `proposed-patches.md` PP-10.
    - `22-CONTEXT.md` D-05 fs cap decision.
    - `internal/agent/tools/fs_read.go`, `fs_write.go`, `fs_edit.go`, `fs_grep.go`, `fs_glob.go`, and tests.
    - `internal/agent/tools/shell_bg.go` and `shell_bg_test.go`.
    - `internal/agent/tools/task.go`, `send_file.go`, `search.go`, `read_tool_output.go`, and tests.
  </read_first>
  <action>
    Add `AURA_FS_MAX_READ_BYTES` with 10 MiB default. For `fs_read`, `fs_write`, and `fs_edit`, stat/size-check before loading or writing large content. Over-cap errors must be clean, no OOM, and for reads must suggest `offset`/`limit` paging.

    Implement `BackgroundShells` as a session evictor and prune finished shell buffers on poll/kill/session end, not only on next start. Simplify fragile snapshot bookkeeping with byte-exact trim/paging tests.

    Validate model-supplied `cwd` and normalize approval digests for `shell_exec` session/approval paths if not already closed.

    Gate all `agent_job` schedules or surface full goal to operator at fire time; do not rely on destructive keyword checks.

    Make `send_file` fail closed in non-CLI contexts when workspace root is empty.

    Add per-tool description hashes so MCP reconnect description changes invalidate/rebuild semantic tool embeddings.

    Make fs writes/edits atomic with temp-file plus rename where practical; document last-writer-wins if concurrent edits remain possible.

    Unify `fs_grep` and `fs_glob` glob semantics or document the difference in tool descriptions and tests.

    Assert `read_tool_output` sidecar runDir invariants: absolute and within run root.
  </action>
  <verify>
    <automated>go test ./internal/agent/tools -run 'TestFS|TestShellBg|TestTask|TestSendFile|TestSearch|TestReadToolOutput' -count=1</automated>
    <automated>go test -race ./internal/agent/tools -run 'TestBackgroundShell|TestFS|TestSearchMount' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File over cap returns a clean error with paging hint and no OOM.
    - Finished background shell buffers are reclaimed on poll/kill/session eviction.
    - `agent_job` cannot silently schedule arbitrary future agent runs without gate/surface.
    - Empty workspace root fails closed for non-CLI `send_file`.
    - MCP description hash change invalidates search/ranker state.
    - Atomic write/edit tests cover crash/truncate or temp+rename behavior.
    - Glob behavior is unified or clearly documented and tested.
    - Sidecar path invariant is asserted.
  </acceptance_criteria>
  <done>Tool execution is memory-safe, evictable, and behaviorally consistent enough for the live tool-surface pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Workflow correctness edges for AG-031, AG-037, AG-038, AG-043, AG-059..064</name>
  <files>workflow files, prompt/hash.go, swarm_context.go, related tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-031, AG-037, AG-038, AG-043, AG-059, AG-060, AG-061, AG-062, AG-063, AG-064.
    - `internal/agent/workflow/workflow.go`, `parallel.go`, `sequential.go`, `loop.go`, and tests.
    - `internal/agent/prompt/hash.go` and hash tests.
    - `internal/agent/swarm_context.go` and swarm tests.
    - `internal/swarm/swarm.go` budget-reserve behavior.
  </read_first>
  <action>
    Add visited/depth guard to `findInTree` keyed by agent identity where possible; cyclic/diamond agent graphs must not recurse forever or stack overflow.

    Prove or fix the parallel result-closer leak edge with a `goleak` stress test that breaks the consumer at every event index under multiple children.

    Add runtime cache-prefix drift detection after `BeforeModel` hook rewrites using `prompt.PrefixHash`, emitting a `prefix_drift` metric without changing `messages[0]` invariants.

    For swarm budget reserve, either atomically reserve/restore the parent synthesis reserve or document/ledger it as best-effort with a test proving the current guarantee. Prefer real reservation if local code shape permits.

    Add `chain_aborted_at` or equivalent StateDelta marker when sequential workflow aborts on a sub error.

    Document and race-test the `SwarmContextValue` shared registry/client concurrent-read contract.

    Document empty-pass/non-returning child and checkpoint cancellation semantics where behavior is intentionally cooperative rather than preemptive.

    If extremely wide tool batches are practical to test, add a worker-pool guard for `executeBatch`; otherwise ledger AG-064 as accepted/improvement with rationale.
  </action>
  <verify>
    <automated>go test -race ./internal/agent/workflow -count=1</automated>
    <automated>go test ./internal/agent/prompt -run 'TestPrefixHash|Test.*Prefix.*Drift' -count=1</automated>
    <automated>go test -race ./internal/agent ./internal/swarm -run 'Test.*Swarm|Test.*Context|Test.*Budget' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Cyclic/diamond tree does not stack overflow and returns deterministic lookup behavior.
    - Parallel consumer-break stress test is goleak-clean.
    - Cache-prefix drift after hook rewrite emits a metric.
    - Swarm reserve behavior is either enforced or explicitly ledgered with rationale.
    - Sequential sub-error StateDelta identifies the aborting sub-agent.
    - Shared swarm context race test is clean.
  </acceptance_criteria>
  <done>Workflow hardening edges are fixed, proven safe, or ledgered with explicit rationale.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-22-12 | Denial of Service | hooks | mitigate | fail-open default, fail-closed option, recover, absolute path validation |
| T-22-13 | Tampering / Info Disclosure | provenance | mitigate | unknown/swarm child output defaults untrusted and nonce-enveloped |
| T-22-14 | Denial of Service | tool memory/session state | mitigate | fs cap, session eviction, atomic writes, path invariants |
| T-22-15 | Denial of Service | workflow recursion/leaks | mitigate | visited guard and goleak stress tests |
</threat_model>

<verification>
- `go test -race ./internal/agent/tools -count=1`
- `go test -race ./internal/agent/workflow -count=1`
- `go test -race ./internal/agent ./internal/swarm -run 'Test.*Hook|Test.*Provenance|Test.*Trust|Test.*Swarm|Test.*Budget|Test.*Prefix' -count=1`
- `go test ./internal/agent/... ./internal/swarm/... -count=1`
</verification>

<success_criteria>
- HARDEN-07: hook failures are contained under `fail_open` and abort clearly under `fail_closed`.
- HARDEN-08: unknown-tool and swarm-child output default untrusted.
- HARDEN-09: workflow cycles/leak edges and runtime prefix drift are bounded or explicitly ledgered.
- HARDEN-10: tools are memory-safe, evictable, and consistent.
- HARDEN-12: AG-014..020, AG-030, AG-031, AG-037, AG-038, AG-043, AG-045, AG-046, AG-050, AG-052, AG-054, AG-058, AG-059..064 have fixed+test or accepted+rationale ledger entries.
</success_criteria>

<output>
Create `.planning/phases/22-bug-fix/22-04-SUMMARY.md` with hook policy defaults, fs cap behavior, provenance allowlist, workflow ledger decisions, and tests added.
</output>
