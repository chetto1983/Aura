---
phase: 22-bug-fix
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - internal/agent/mcptools/bridge_reconnect.go
  - internal/agent/mcptools/bridge.go
  - internal/agent/mcptools/timeout.go
  - internal/agent/mcptools/name.go
  - internal/agent/mcptools/mount.go
  - internal/agent/mcptools/bridge_reconnect_branches_test.go
  - internal/agent/mcptools/bridge_edges_test.go
  - internal/agent/mcptools/bridge_trust_test.go
  - internal/agent/mcptools/name_test.go
  - internal/agent/llm_agent_reasoning.go
  - internal/agent/llm_agent_reasoning_test.go
  - internal/agent/prompt/reasoning_classifier.go
  - internal/agent/prompt/reasoning_classifier_test.go
  - internal/agent/budget.go
  - internal/agent/budget_test.go
  - internal/agent/workflow/loop.go
  - internal/agent/workflow/loop_test.go
  - cmd/aura/agent.go
  - cmd/aura/agent_test.go
autonomous: true
requirements: [HARDEN-03, HARDEN-06, HARDEN-09, HARDEN-12]
must_haves:
  truths:
    - "D-06: AURA_MCP_CALL_TIMEOUT_SEC semantic flip is required: 0 means default, -1 means explicit infinite, with a migration note in close-out."
    - "D-07: reasoning router stays ON by default; bound it and add breaker behavior, do not implement default-off."
    - "D-08: MCP breaker defaults are 3 failures, 30s cooldown, 500ms to 30s exponential backoff, and 10s reconnect timeout unless config names force adjustment."
    - "D-09: Budget.WithDeadline wiring in cmd/aura/agent.go lands here; unrelated other-package fixes are routed, not pulled in."
    - "MCP reconnect never holds s.mu across subprocess spawn, handshake, ListTools, backoff sleep, or network I/O."
  artifacts:
    - path: "internal/agent/mcptools/bridge_reconnect.go"
      provides: "single-flight off-lock reconnect with breaker/backoff and mutating-flip warning"
      contains: "WithoutCancel"
    - path: "internal/agent/mcptools/timeout.go"
      provides: "0->default and -1->infinite timeout semantics"
      contains: "defaultMCPCallTimeout"
    - path: "internal/agent/llm_agent_reasoning.go"
      provides: "bounded router fallback with embed-sidecar breaker"
      contains: "ReasoningTierLow"
    - path: "internal/agent/budget.go"
      provides: "positive budget validation and active deadline helper"
      contains: "WithDeadline"
    - path: "cmd/aura/agent.go"
      provides: "composition-root active wallclock context"
      contains: "WithDeadline"
---

<objective>
Make external-dependency and loop bounds production-safe: MCP reconnect/hang resilience (AG-005/006/024/025/027 plus the AG-007 mutating-flip warning slice), reasoning-router fallback bounds (AG-008), classifier cold-start lock narrowing (AG-032), and active budget/wallclock validation/wiring (AG-035/036/041).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-bug-fix/22-SPEC.md
@.planning/phases/22-bug-fix/22-CONTEXT.md
@.planning/phases/22-bug-fix/22-RESEARCH.md
@docs/audit/bug-report.md
@docs/audit/proposed-patches.md
@docs/audit/testing-strategy.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP reconnect, timeout, namespace, and schema hardening for AG-005, AG-006, AG-022, AG-023, AG-024, AG-025, AG-026, AG-027, AG-029, AG-007-flip-warn</name>
  <files>internal/agent/mcptools/{bridge_reconnect.go,bridge.go,timeout.go,name.go,mount.go}, mcptools tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-005, AG-006, AG-007, AG-022, AG-023, AG-024, AG-025, AG-026, AG-027, AG-029 and `proposed-patches.md` PP-3.
    - `internal/agent/mcptools/bridge_reconnect.go` and `bridge_reconnect_branches_test.go`.
    - `internal/agent/mcptools/bridge.go`, `bridge_edges_test.go`, and `bridge_trust_test.go`.
    - `internal/agent/mcptools/timeout.go`.
    - `internal/agent/mcptools/name.go`, `name_test.go`, and `mount.go`.
    - `.claude/skills/golang-concurrency/SKILL.md` and `.claude/skills/golang-context/SKILL.md`.
  </read_first>
  <action>
    Refactor reconnect so the expensive work is outside `s.mu`: decide reconnect ownership under lock, release lock, use a single-flight or equivalent in-package guard, run `openMCPClient` and `ListTools` under `context.WithoutCancel(parent)` plus a dedicated reconnect timeout, then publish the new client/spec under lock.

    Add per-server backoff and circuit breaker with D-08 defaults. Breaker-open calls should degrade to a model-visible MCP transport error quickly, without respawning on every call.

    Change timeout semantics: `AURA_MCP_CALL_TIMEOUT_SEC=0` returns the default timeout; `-1` means explicit infinite. Resolve and validate timeout once at mount/boot, not per call, and fail loud on malformed values.

    Mark after-send transport failures non-retryable so mutating MCP tools are not encouraged to double-execute. Cap total marshalled schema bytes, property counts, and inlined MCP error text, falling back to a safe empty-object schema or capped preview when a server advertises or returns too much.

    Emit `slog.Warn` when reconnect changes `Mutating` or required args for an existing tool. Do not implement the full AG-007 capability_grants gate in this phase; ledger that multi-tenant slice as deferred per `22-CONTEXT.md`.

    Validate namespace collisions at boot, including post-sanitization collisions. Document and test the registry atomic-swap contract for reconnect-during-dispatch.
  </action>
  <verify>
    <automated>go test -race ./internal/agent/mcptools -run 'Test.*Reconnect|Test.*Breaker|Test.*Timeout|Test.*Mutating|Test.*Schema|Test.*Name' -count=1</automated>
    <automated>go test -race ./internal/agent/mcptools -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Concurrent calls during slow reconnect do not head-of-line block behind `s.mu` beyond the configured bound.
    - Crash-loop fake server trips breaker after the configured failure count and respects cooldown/backoff.
    - Hung server with timeout env `0` is cancelled by default deadline and leaks no goroutine.
    - Timeout env `-1` is the only explicit infinite mode.
    - Malformed timeout config fails at mount/boot, not mid-turn per call.
    - Reconnect mutating flip and required-arg changes emit warnings.
    - Over-large schema/property manifests and MCP error previews are capped and tested.
    - Namespace and sanitized-name collisions are boot validation failures or deterministic warnings with no silent server drop.
    - Reconnect-during-dispatch is race-clean and documents the atomic-swap contract.
  </acceptance_criteria>
  <done>MCP flapping/hung behavior is bounded, reconnect is off-lock/single-flight, timeout semantics are safe, and schema/name hazards are tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded reasoning-router fallback for AG-008 and classifier lock scope for AG-032</name>
  <files>internal/agent/llm_agent_reasoning.go, internal/agent/prompt/reasoning_classifier.go, related tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-008 and AG-032.
    - `22-CONTEXT.md` D-07: router stays on by default; bound it, do not disable it.
    - `internal/agent/llm_agent_reasoning.go` and `llm_agent_reasoning_test.go`.
    - `internal/agent/prompt/reasoning_classifier.go` and classifier tests.
    - `internal/agent/prompt/reasoning_router.go` and router tests.
  </read_first>
  <action>
    Implement D-07 precisely: keep the reasoning router available/on by default, but prevent an embed-sidecar outage from adding a per-turn latency cliff. A classifier error/abstain should either issue at most one bounded router attempt before breaker-open, or immediately use static `ReasoningTierLow` according to the final local design. The acceptance must prove repeated outage turns are unaffected.

    Cap router timeout well below the old approximate 8s value and make env/config names follow the existing `AURA_<DOMAIN>_<UNIT>` convention.

    Narrow `ensureAnchors` lock scope by building/loading anchor data outside `c.mu` or using `singleflight`, then publishing under lock.
  </action>
  <verify>
    <automated>go test ./internal/agent -run 'TestAdaptiveReasoning|TestReasoningRouter|TestReasoningFallback' -count=1</automated>
    <automated>go test ./internal/agent/prompt -run 'TestReasoningClassifier|TestEnsureAnchors|TestReasoningRouter' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - With embedder forced to error, repeated turns do not issue an LLM router round-trip every turn.
    - If one fallback router call is allowed, the next calls are breaker-open/static until cooldown.
    - Static fallback is `ReasoningTierLow` when classifier is wired but unavailable/abstaining.
    - Existing classifier accuracy/path tests still pass.
    - Concurrent cold-start calls do not serialize all turns behind network work while holding `c.mu`.
  </acceptance_criteria>
  <done>Router quality remains available, but embed outages no longer impose a recurring latency cliff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Active budget/wallclock and loop bounds for AG-035, AG-036, AG-041</name>
  <files>internal/agent/budget.go, internal/agent/workflow/loop.go, cmd/aura/agent.go, tests</files>
  <read_first>
    - `docs/audit/bug-report.md` AG-035, AG-036, AG-041 and `proposed-patches.md` PP-9.
    - `internal/agent/budget.go` `NewBudget`, env parsing, and `WithDeadline`.
    - `internal/agent/budget_test.go` existing validation/deadline tests.
    - `internal/agent/workflow/loop.go` and loop tests for `maxIterations==0`.
    - `cmd/aura/agent.go` run-root context setup and `cmd/aura/agent_test.go`.
  </read_first>
  <action>
    Reject `maxSteps < 1` and `wallclock < 1` at `NewBudget` / env construction with clear errors. Preserve defaults for unset env and existing valid override precedence.

    Thread `Budget.WithDeadline(parent)` into the root agent invocation context in `cmd/aura/agent.go`, with `defer cancel()` on every path. This is the explicitly allowed one-line composition-root wiring from D-09.

    Add a default iteration ceiling when `maxIterations==0` so loop agents cannot spend nearly all `max_steps` on one-step-per-iteration loops without an iteration cap. Keep the existing budget/no-progress guards as additional protection.
  </action>
  <verify>
    <automated>go test ./internal/agent -run 'TestNewBudget|TestBudget_WithDeadline|TestBudget_Wallclock' -count=1</automated>
    <automated>go test ./internal/agent/workflow -run 'TestLoopAgent.*MaxIter|TestLoopAgent.*Budget' -count=1</automated>
    <automated>go test ./cmd/aura -run 'Test.*Budget|Test.*Agent.*Deadline|TestTurnBudget' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `NewBudget(0)` and negative max steps/wallclock are construction errors.
    - A malformed MCP timeout/budget env is surfaced at boot/mount where relevant.
    - A root run context carries the budget deadline and actively cancels long operations.
    - `maxIterations==0` has a documented default ceiling and tests prove bounded behavior.
  </acceptance_criteria>
  <done>Loop and wallclock bounds are active, validated, and composition-root wired.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-22-08 | Denial of Service | MCP reconnect | mitigate | off-lock single-flight reconnect, breaker, backoff, bounded timeout |
| T-22-09 | Denial of Service | reasoning router | mitigate | bounded router fallback and embed-sidecar breaker/static fallback |
| T-22-10 | Denial of Service | loop/wallclock | mitigate | positive config validation and active context deadline |
| T-22-11 | Tampering | MCP mutating flip | partial | warn and ledger single-operator slice; defer full multi-tenant grants |
</threat_model>

<verification>
- `go test -race ./internal/agent/mcptools -count=1`
- `go test ./internal/agent ./internal/agent/prompt ./internal/agent/workflow -count=1`
- `go test ./cmd/aura -run 'Test.*Budget|Test.*Agent' -count=1`
- `go test -race ./internal/agent/... -count=1`
</verification>

<success_criteria>
- HARDEN-03: flapping/hung MCP server degrades gracefully with single-flight off-lock reconnect, backoff, breaker, and safe timeout semantics.
- HARDEN-06: embed-sidecar outage adds no recurring router latency cliff; router remains bounded and available by design.
- HARDEN-09: budget/wallclock and loop correctness are bounded and validated.
- HARDEN-12: AG-005, AG-006, AG-007 flip-warn slice, AG-008, AG-022, AG-023, AG-024, AG-025, AG-026, AG-027, AG-029, AG-032, AG-035, AG-036, and AG-041 have fixed+test or routed ledger entries.
</success_criteria>

<output>
Create `.planning/phases/22-bug-fix/22-03-SUMMARY.md` with MCP defaults/env names, router fallback policy, budget validation changes, and migration note for `AURA_MCP_CALL_TIMEOUT_SEC`.
</output>
