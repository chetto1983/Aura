---
phase: 00-prd-amendments
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md  # Slice 0.9 + Slice 1 + Slice 1.8 (new sub-slice 1.8.5) + Slice 9a + Slice 9b + Caps&Limits env table — non-overlapping with plans 01/03/04/05
autonomous: true
commit_per_plan: false  # phase emits ONE aggregated commit via 00-06-PLAN.md
phase_commit_aggregator: false
requirements: [PRD-01]

must_haves:
  truths:
    - "Operator greps prd.md for 'Slice 1.8.5' and observes the new sub-slice header present (Amendment #7 — conversation FTS via pg_trgm)"
    - "Operator greps prd.md for '/cost' and observes the new Telegram command in Slice 9b commands list (Amendment #8)"
    - "Operator greps prd.md for 'request_id' AND 'UUIDv7' and observes both present in InvocationContext (Amendment #9 — OTel hooks)"
    - "Operator greps prd.md for 'AURA_SETUP_TOKEN' and observes the env var present in Slice 9a + Caps&Limits table (Amendment #10)"
  artifacts:
    - path: "prd.md"
      provides: "Feature gaps cluster — conversation FTS, /cost commands, OTel request_id hooks, setup wizard one-time token"
      contains: "Slice 1.8.5"
  key_links:
    - from: "prd.md Slice 1.8.5 (new sub-slice)"
      to: "Slice 9b /search command + Slice 9b /cost command + CLI aura chat search/cost"
      via: "pg_trgm GIN index on conversation_turns.content"
      pattern: "pg_trgm"
    - from: "prd.md Slice 0.9 InvocationContext struct"
      to: "OTel-compatible request_id UUIDv7 propagation"
      via: "field addition + lifecycle docs"
      pattern: "request_id.*UUIDv7"
    - from: "prd.md Slice 9a Setup wizard"
      to: "first-boot one-time token gate"
      via: "AURA_SETUP_TOKEN env var + stdout print"
      pattern: "AURA_SETUP_TOKEN"
---

<objective>
Apply Feature gaps cluster — PRD amendments #7 through #10 from `.planning/research/SUMMARY.md` "PRD Amendments Required" table (lines 64-89). Four small additive edits: new sub-slice 1.8.5 (conversation FTS), `/cost` command + CLI subcommand, OTel `request_id` UUIDv7 in `InvocationContext`, and `AURA_SETUP_TOKEN` one-time setup wizard token.

Purpose: Close 4 feature gaps identified by independent research convergence as small-enough-for-v1. Each lands inside an existing slice (no new slices created at the integer level); 1.8.5 is a decimal sub-slice extension of 1.8 per the PRD numbering convention.

Output: prd.md updated with Amendments #7-10 applied. NO commit (deferred to 00-06).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aura/.planning/PROJECT.md
@D:/Aura/.planning/ROADMAP.md
@D:/Aura/.planning/STATE.md
@D:/Aura/.planning/research/SUMMARY.md
@D:/Aura/.planning/REQUIREMENTS.md
@D:/Aura/prd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Amendment #7 (new Slice 1.8.5 conversation FTS sub-slice)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 76: `| 7 | Add conversation FTS sub-slice 1.8.5 (pg_trgm GIN + aura chat search) | Features gap | Slice 1.8 extension |`
    - D:/Aura/.planning/research/SUMMARY.md line 42 ("Add to v1: Conversation FTS (~80 LOC + GIN index, Slice 1.8.5)")
    - D:/Aura/.planning/REQUIREMENTS.md line 28 (REQ CORE-05 definition: "Conversation full-text search via Postgres `pg_trgm` GIN index su `conversation_turns.content` + `aura chat search "<query>"` CLI + Telegram `/search` command. ~80 LOC + 1 migration. [Slice 1.8.5 — amendment #7]")
    - D:/Aura/prd.md line 858 (Slice 1.8 header) and the surrounding section to find the section's logical end (before Slice 2 at line 1051 — use `grep -n "^## Slice " prd.md` output already in context)
    - D:/Aura/prd.md line 990 (Slice 1.8 OQ about system message) — locate the end of Slice 1.8 (search for "Commit message template" inside Slice 1.8 or the last `---` divider before Slice 2 header at 1051)
  </read_first>
  <action>
    Insert a new sub-section `## Slice 1.8.5 — Conversation full-text search (pg_trgm GIN + aura chat search)` between Slice 1.8 (ends before line 1051) and Slice 2 (starts at line 1051). Insertion strategy: locate the last line before `## Slice 2 — Sandbox runner` at line 1051; insert the new sub-slice immediately before it, after a clean `---` divider.

    The new sub-slice MUST contain (no fenced code blocks for implementation — directive prose only, per CLAUDE.md "NO COMMENTS UNLESS WHY IS NON-OBVIOUS"):

    Section content outline (write in same prose register as the surrounding PRD):
    1. **Goal sentence**: "Aggiunge conversation full-text search a Slice 1.8 — index Postgres `pg_trgm` GIN su `aura.conversation_turns.content`, CLI subcommand `aura chat search "<query>"`, e Telegram command `/search` (Slice 9b reuse). Amendment #7 (SUMMARY.md PRD Amendments table, 2026-05-29)."
    2. **Atomicity note**: "Sub-slice atomico ~80 LOC + 1 migration `0005_conversation_turns_fts.up.sql` (CREATE EXTENSION pg_trgm IF NOT EXISTS + CREATE INDEX CONCURRENTLY conversation_turns_content_trgm ON aura.conversation_turns USING GIN (content gin_trgm_ops))."
    3. **Pre-requisiti**: Slice 0.5 Postgres infra, Slice 1.8 conversation persistence.
    4. **Smoke**: `aura chat search "specific phrase"` returns matching turn excerpts ordered by similarity, p95 ≤ 50ms su 10K turns.
    5. **Acceptance** (bulleted list, each item machine-checkable):
       - Migration `0005_conversation_turns_fts.up.sql` creates `pg_trgm` extension + GIN index; reverse in `0005_conversation_turns_fts.down.sql` drops the index + extension.
       - sqlc query `SearchConversationTurns(query text, limit int) returns []ConversationTurn` using `content % $1 ORDER BY similarity(content, $1) DESC LIMIT $2`.
       - CLI subcommand `aura chat search "<query>" [--conversation <id>] [--limit N]` prints `<conv_id> | <turn_seq> | <similarity_score> | <excerpt>` per row.
       - Telegram `/search <query>` (added to Slice 9b commands MVP list — referenced cross-slice; the binding itself lands in Slice 9b).
       - Slice 9b `/search` returns identical results to CLI `aura chat search` for the same query (cross-slice invariant test).
    6. **File targets** (table — keep concise, no LOC inflation):
       - `internal/db/migrations/0005_conversation_turns_fts.up.sql` ~15 LOC
       - `internal/db/migrations/0005_conversation_turns_fts.down.sql` ~5 LOC
       - `internal/db/queries/conversation_search.sql` ~10 LOC (1 query)
       - `internal/conversations/search.go` ~40 LOC (`Search(ctx, query, opts) ([]TurnHit, error)`)
       - `cmd/aura/chat.go` (diff) ~+20 LOC (new `search` sub-command wiring)
       - Total: ~80 LOC + ~30 test
    7. **Open questions**: none (closed at amendment time).
    8. **Commit message template**: `slice 1.8.5: conversation FTS via pg_trgm GIN + aura chat search\n\nAmendment #7 — adds full-text search across conversation_turns.content...`
    9. **Migration numbering note**: "NOTA migration: 0005 in questa sub-slice è after 0004 (paused_states con resume_context, Slice 1.5) e before 0006 (scheduler, Slice 6). Verificare prima del commit di Phase 4 che il numero sia ancora disponibile (Slice 1.7 identity migration potrebbe occupare uno slot — re-numerare on conflict)."

    After the new section, ensure a `---` divider appears before `## Slice 2 — Sandbox runner` (preserve existing divider if any).
  </action>
  <acceptance_criteria>
    - `grep -c "^## Slice 1\.8\.5" D:/Aura/prd.md` returns exactly 1
    - `grep -c "pg_trgm" D:/Aura/prd.md` returns at least 3 (header reference + Acceptance + at least one file target / Goal mention)
    - `grep -c "aura chat search" D:/Aura/prd.md` returns at least 2
    - `grep -c "amendment #7" D:/Aura/prd.md` returns at least 1
    - `grep -E "0005_conversation_turns_fts" D:/Aura/prd.md` returns at least 2 matches (up + down migration)
    - The new section appears BEFORE `## Slice 2 — Sandbox runner`: `awk '/^## Slice 1\.8\.5/{a=NR} /^## Slice 2 — Sandbox/{b=NR} END{exit !(a&&b&&a&lt;b)}' D:/Aura/prd.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "^## Slice 1\.8\.5" prd.md) -eq 1 &amp;&amp; test $(grep -c "pg_trgm" prd.md) -ge 3 &amp;&amp; test $(grep -c "aura chat search" prd.md) -ge 2 &amp;&amp; test $(grep -c "amendment #7" prd.md) -ge 1 &amp;&amp; test $(grep -c "0005_conversation_turns_fts" prd.md) -ge 2 &amp;&amp; awk "/^## Slice 1\.8\.5/{a=NR} /^## Slice 2 — Sandbox/{b=NR} END{exit !(a\&\&b\&\&a<b)}" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; new Slice 1.8.5 section landed between 1.8 and 2; references CORE-05 requirement traceability.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Amendments #8, #10 (/cost Telegram command + CLI + AURA_SETUP_TOKEN)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 77: `| 8 | Add /cost Telegram + aura chat cost CLI | Features gap | Slice 9b |`
    - D:/Aura/.planning/research/SUMMARY.md line 79: `| 10 | Add setup wizard one-time token AURA_SETUP_TOKEN | Features gap | Slice 9a |`
    - D:/Aura/prd.md line 2482 (Slice 9 decision table row 1 about setup wizard)
    - D:/Aura/prd.md line 2498 (Slice 9 decision table row 8: "8 commands MVP: `/start /help /whoami /cancel /new /conversations /resume /reset`")
    - D:/Aura/prd.md line 2535 (Slice 9b commands.go file target row: lists the same 8 commands)
    - D:/Aura/prd.md lines 2540-2543 (commands.go inline command list)
    - D:/Aura/prd.md line 2689 (Slice 9b acceptance about commands.go)
    - D:/Aura/prd.md lines 2675-2680 (Slice 9a Setup wizard acceptance bullets)
    - D:/Aura/prd.md line 4278 (env table row `AURA_SETUP_BIND`)
    - D:/Aura/prd.md line 4305 (env table row `TELEGRAM_BOT_TOKEN`)
  </read_first>
  <action>
    Apply Amendment #8 (/cost Telegram command + CLI):
    - prd.md line 2498 (decision table row 8): replace "8 commands: `/start /help /whoami /cancel /new /conversations /resume /reset`" with "10 commands: `/start /help /whoami /cancel /new /conversations /resume /reset /cost /search` (amendment #8 adds `/cost`; amendment #7 adds `/search`)"
    - prd.md line 2535 (`commands.go` file target row): replace the `~170` LOC estimate parenthetical "8 commands MVP" with "10 commands MVP (incl. `/cost` per amendment #8, `/search` per amendment #7)"; bump LOC to `~210`.
    - prd.md lines 2540-2543 (commands.go inline command listing inside the architecture component): immediately after the line "#       /reset - conv.Delete + new" insert two new lines:
      "#       /cost - aura.cost_aggregator.TodayUSD() + per-conv breakdown (amendment #8)"
      "#       /search <query> - SearchConversationTurns via pg_trgm (Slice 1.8.5, amendment #7)"
    - prd.md line 2689 (Slice 9b acceptance about commands): immediately after this bullet insert a new acceptance bullet:
      "- [ ] **`/cost` command (amendment #8)**: returns today's cumulative USD spend (`aura.cost_aggregator.TodayUSD()`) + per-conversation breakdown top 5. Uses `aura chat cost` shared logic in `cmd/aura/cost.go` (~30 LOC, single source). Telegram-side: bot-intercept, no LLM call."
      "- [ ] **`/search` command (amendments #7+#8 cross-ref)**: bot-intercept calling `internal/conversations/search.Search(ctx, query)` from Slice 1.8.5. Result: top 5 turn excerpts with conversation links."

    Apply Amendment #10 (AURA_SETUP_TOKEN one-time token):
    - prd.md line 2482 (decision table row 1, setup wizard): replace "Sempre on `http://<bind>:9081/setup`, paste bot token + valida via getMe + mostra QR del deep link `t.me/<bot>?start=<onboarding_token>`." with "Sempre on `http://<bind>:9081/setup?token=<AURA_SETUP_TOKEN>`, one-time token `AURA_SETUP_TOKEN` (random UUIDv4) generato + stampato a stdout primo boot (`aura serve`), gated middleware su tutti i `/setup/*` endpoint. Paste bot token + valida via getMe + mostra QR del deep link `t.me/<bot>?start=<onboarding_token>`. Amendment #10 — previene unauthorized access se `AURA_SETUP_BIND=0.0.0.0:9081`."
    - prd.md lines 2675-2680 (Slice 9a setup wizard acceptance section): immediately after the `GET /setup` bullet, insert a new acceptance bullet:
      "- [ ] **One-time token gate (amendment #10)**: `AURA_SETUP_TOKEN` env var. If unset at boot, `aura serve` generates a random UUIDv4 and prints `AURA_SETUP_TOKEN=<value>` to stdout (single line, parseable). Token persists in memory only (no disk). Middleware `requireSetupToken` on `/setup/*` returns 401 if `?token=` query param or `X-Aura-Setup-Token` header does not match. After successful Telegram onboarding (POST /setup/onboard-link completion event), the token is invalidated and a second navigation returns 401. Acceptance test: 401 without token, 200 with token, 401 after onboarding-complete."
    - prd.md line 4278 (env table row `AURA_SETUP_BIND`): immediately after this row, insert new row:
      "| `AURA_SETUP_TOKEN` | (auto-generated UUIDv4 if unset) | secret | 9a | One-time setup wizard auth token (amendment #10). Printed to stdout on first `aura serve` boot; invalidated after first successful Telegram onboarding. Required when `AURA_SETUP_BIND=0.0.0.0:9081` (remote setup). |"

    Per CLAUDE.md "NO TEST ASILO NIDO": acceptance bullets must enable a real test, not a vibes check.
  </action>
  <acceptance_criteria>
    - `grep -c "/cost" D:/Aura/prd.md` returns at least 3 (decision table + commands.go listing + acceptance bullet)
    - `grep -c "AURA_SETUP_TOKEN" D:/Aura/prd.md` returns at least 3 (decision table + acceptance bullet + env table row)
    - `grep -c "amendment #8" D:/Aura/prd.md` returns at least 3
    - `grep -c "amendment #10" D:/Aura/prd.md` returns at least 3
    - `grep -E "^\| \`AURA_SETUP_TOKEN\`" D:/Aura/prd.md` returns exactly 1 (env table row)
    - `grep -E "^\| 1 \| Setup wizard" D:/Aura/prd.md | grep -c "AURA_SETUP_TOKEN"` returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "/cost" prd.md) -ge 3 &amp;&amp; test $(grep -c "AURA_SETUP_TOKEN" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #8" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #10" prd.md) -ge 3 &amp;&amp; test $(grep -cE "^\| \`AURA_SETUP_TOKEN\`" prd.md) -eq 1 &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; commands list includes /cost + /search; env table includes AURA_SETUP_TOKEN row; Slice 9a acceptance includes one-time token gate.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Amendment #9 (OTel hooks — request_id UUIDv7 in InvocationContext)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 78: `| 9 | Add OTel hooks (request_id UUIDv7 in InvocationContext) | Features gap + Pitfall #23 | Slice 0.9 + 1 cross-cutting |`
    - D:/Aura/.planning/research/SUMMARY.md line 42 ("OTel hooks (~150 LOC, 1 + 0.9)")
    - D:/Aura/.planning/REQUIREMENTS.md line 20 (INFRA-03: "InvocationContext include `request_id` UUIDv7 per OTel correlation")
    - D:/Aura/.planning/REQUIREMENTS.md line 24 (CORE-01: "OTel span per LLM call")
    - D:/Aura/prd.md lines 286-298 (Slice 0.9 `InvocationContext` struct definition)
    - D:/Aura/prd.md line 461 (Slice 0.9 acceptance: "`internal/agent/agent.go` definisce `Agent` interface + `InvocationContext` + builder helpers")
    - D:/Aura/prd.md line 475 (Slice 0.9 OQ #1 about `Branch` field — context for how OQs are written)
    - D:/Aura/prd.md line 571 (Slice 1 acceptance about cancel context + goleak — context for adding OTel span acceptance)
  </read_first>
  <action>
    Apply Amendment #9 — OTel `request_id` UUIDv7 hook in `InvocationContext` (Slice 0.9) + OTel span per LLM call (Slice 1):

    Edit 1 (Slice 0.9 — InvocationContext struct fence at lines 288-298):
    The struct in prd.md is inside a code fence (lines 287-298). Per CLAUDE.md "READ BEFORE EDIT", re-read those lines first to confirm the exact field list. Inject a new field line BEFORE the `// ... extension points` comment, with line content:
    `    RequestID     string             // UUIDv7 per Run invocation — OTel-compatible correlation id; root of trace span tree (amendment #9)`

    Edit 2 (Slice 0.9 acceptance — line 461 cluster): immediately after the existing bullet about `InvocationContext`, insert a new acceptance bullet:
    "- [ ] **`InvocationContext.RequestID` UUIDv7 (amendment #9)**: every top-level `agent.Run(ctx)` invocation populates `RequestID` with a fresh UUIDv7 (time-ordered, 128-bit) via `github.com/google/uuid` v1.6.0+ `uuid.NewV7()`. Child sub-agents (Sequential/Loop/Parallel) inherit the parent's `RequestID` unchanged. Every emitted `*Event` carries the `RequestID` in `Event.Branch` prefix `req:<uuid7>::<branch-path>` for OTel correlation. Acceptance test: spawn nested 3-level agent tree, assert all emitted events share the same `req:<uuid7>::` prefix."

    Edit 3 (Slice 0.9 — Pre-requisiti list at line 248): immediately after the existing 3 bullets (Go 1.25+, Slice 0 Postgres, Slice 0.7 Neo4j), insert:
    "- `github.com/google/uuid` v1.6.0+ (UUIDv7 — amendment #9 OTel correlation)"

    Edit 4 (Slice 1 acceptance — add OTel span bullet near line 571 cluster, before the cancel/goleak bullet):
    Insert a new acceptance bullet:
    "- [ ] **OTel span per LLM call (amendment #9)**: every `client.Request(req)` emits an OTel span via `go.opentelemetry.io/otel/trace` (no SDK initialization in Slice 1 — uses the global `otel.GetTracerProvider()`; in CI the provider is a no-op recorder). Span name `llm.request`, attributes: `llm.model`, `llm.provider`, `llm.prompt_tokens`, `llm.completion_tokens`, `llm.cache_hit_tokens`, `aura.request_id` (from `InvocationContext.RequestID`). Span linked to parent trace via `RequestID`. Acceptance test: with in-memory recorder, assert 1 span emitted per call, attributes populated, span_id stable across SSE chunks of the same call."

    Edit 5 (Slice 0.9 Open questions section at line 475 area — close as not-needed): no new OQ; record decision in acceptance only.

    Edit 6 (Caps & Limits env vars section at line 4233 — add operational env if needed): NO new env var required (request_id is per-Run, not config). Skip.

    Per CLAUDE.md "READ BEFORE EDIT" — confirm InvocationContext struct lines 287-298 are unchanged before insertion; per the no-fenced-code rule of this PRD-edit phase, the addition stays inside an EXISTING code fence (already present at line 287) so no new fence is created.
  </action>
  <acceptance_criteria>
    - `grep -c "RequestID" D:/Aura/prd.md` returns at least 3 (struct field, Slice 0.9 acceptance, Slice 1 acceptance)
    - `grep -c "UUIDv7" D:/Aura/prd.md` returns at least 3 (Pre-requisiti, Slice 0.9 acceptance, Slice 1 acceptance)
    - `grep -c "amendment #9" D:/Aura/prd.md` returns at least 4 (Pre-requisiti bullet + struct field + Slice 0.9 acceptance + Slice 1 acceptance)
    - `grep -E "google/uuid" D:/Aura/prd.md` returns at least 1 match (the Pre-requisiti bullet)
    - `grep -E "llm\.request" D:/Aura/prd.md` returns at least 1 match (Slice 1 OTel span acceptance)
    - `grep -E "aura\.request_id" D:/Aura/prd.md` returns at least 1 match (OTel span attribute)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "RequestID" prd.md) -ge 3 &amp;&amp; test $(grep -c "UUIDv7" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #9" prd.md) -ge 4 &amp;&amp; grep -q "google/uuid" prd.md &amp;&amp; grep -q "llm\.request" prd.md &amp;&amp; grep -q "aura\.request_id" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; RequestID UUIDv7 field added to InvocationContext struct; Slice 0.9 + Slice 1 acceptance bullets cite OTel span requirements; google/uuid added to Slice 0.9 prerequisites.</done>
</task>

</tasks>

<verification>
Phase-level grep fingerprints for feature-gaps cluster (post Wave 1):
- `Slice 1.8.5` ≥ 1 hit (new sub-slice)
- `pg_trgm` ≥ 3 hits
- `AURA_SETUP_TOKEN` ≥ 3 hits
- `/cost` ≥ 3 hits
- `RequestID` ≥ 3 hits
- `UUIDv7` ≥ 3 hits
- `amendment #7` ≥ 1, `amendment #8` ≥ 3, `amendment #9` ≥ 4, `amendment #10` ≥ 3
</verification>

<success_criteria>
- 3 tasks complete
- 4 amendments (#7, #8, #9, #10) reflected in prd.md
- No `.go`, `.sql`, or `.yaml` files modified by this plan
- No git commit created (commit is aggregated in 00-06)
- File state ready for 00-06 aggregator
</success_criteria>

<output>
On completion, create `.planning/phases/00-prd-amendments/00-02-SUMMARY.md` documenting:
- New Slice 1.8.5 section: starting line + ending line in prd.md
- Lines edited in Slice 9 decision table + commands.go architecture block + Slice 9a acceptance + env table
- Lines edited in Slice 0.9 InvocationContext struct + acceptance, Slice 1 acceptance
- Acceptance grep counts at final state
- Confirmation: no commit created, no code files touched
</output>
