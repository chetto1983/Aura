---
phase: 00-prd-amendments
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md  # Slice 0.5 + Slice 0.7 + Slice 0.9 + Slice 7c + new §Cross-cutting cache_invariant section + Caps&Limits env table — non-overlapping with plans 01/02/03/05
autonomous: true
commit_per_plan: false
phase_commit_aggregator: false
requirements: [PRD-01]

must_haves:
  truths:
    - "Operator greps prd.md for 'goleak.VerifyNone' AND 'all goroutine packages' and observes the extended mandate (Amendment #15)"
    - "Operator greps prd.md for 'cache_invariant_audit.sh' and observes the new cross-slice CI job documented (Amendment #16)"
    - "Operator greps prd.md for 'aura_app' AND 'aura_migrate' and observes the role separation specified (Amendment #17)"
    - "Operator greps prd.md for 'AURA_EMBED_DIMENSIONS' and observes the env contract + boot assertion (Amendment #18)"
    - "Operator greps prd.md for 'AURA_LOOP_MAX_STEPS' AND 'AURA_LOOP_MAX_WALLCLOCK_SEC' AND 'AURA_LOOP_DEDUP_WINDOW' and observes the budget contract (Amendment #19)"
  artifacts:
    - path: "prd.md"
      provides: "Cross-cutting pitfall mitigations — goleak extension, cache invariant CI, audit role separation, embedding dim env contract, loop budget contract"
      contains: "cache_invariant_audit.sh"
  key_links:
    - from: "prd.md new §Cross-cutting Cache invariant CI section"
      to: "scripts/cache_invariant_audit.sh (to be authored Phase 6)"
      via: "20-turn replay SHA-256 assertion"
      pattern: "cache_invariant_audit\\.sh"
    - from: "prd.md Slice 0.5 + 7c"
      to: "aura_app/aura_migrate role separation + BEFORE TRUNCATE trigger"
      via: "Postgres role grants + raise_audit_immutable() extension"
      pattern: "aura_app|aura_migrate"
    - from: "prd.md Slice 0.7 embedding sidecar boot"
      to: "AURA_EMBED_DIMENSIONS=768 assertion"
      via: "GET /health returning {dim:768}"
      pattern: "AURA_EMBED_DIMENSIONS"
    - from: "prd.md Slice 0.9 budget envs"
      to: "child budget inheritance from parent's remaining"
      via: "AURA_LOOP_* 3-cap contract"
      pattern: "AURA_LOOP_MAX_STEPS"
---

<objective>
Apply Cross-cutting pitfalls cluster — PRD amendments #15 through #19 from `.planning/research/SUMMARY.md` "PRD Amendments Required" table (lines 64-89). Five cross-cutting hardening items mapped to Pitfalls #10 (goleak), #3 (KV cache invariant CI), #6 (audit TRUNCATE bypass), #7 (embedding dim silent corruption), #9 (infinite tool-call loop budget).

Purpose: Cross-cutting means MULTIPLE slices touch the same invariant. Without a single PRD source of truth on these, every slice will re-derive (or worse, drift). Lock the cross-slice contracts here so later phases consume them as inputs.

Output: prd.md updated with Amendments #15-19 applied; new §Cross-cutting section added between Slice 4 and §Test discipline if appropriate placement allows, else inline within affected slices. NO commit.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aura/.planning/PROJECT.md
@D:/Aura/.planning/ROADMAP.md
@D:/Aura/.planning/STATE.md
@D:/Aura/.planning/research/SUMMARY.md
@D:/Aura/.planning/REQUIREMENTS.md
@D:/Aura/prd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Amendments #15, #16 (goleak mandate extension + cache_invariant_audit.sh CI section)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md lines 84-85: rows #15 (goleak extension to all goroutine packages) + #16 (cache_invariant_audit.sh cross-slice CI job — new §Cross-cutting section).
    - D:/Aura/.planning/research/SUMMARY.md line 57 (Pitfall #2 description: "Slice 4 owns invariant but Slices 1.8, 5, 7e, 10, 11e all mutate prefix. CI job `scripts/cache_invariant_audit.sh` 20-turn replay asserts SHA-256(messages[0]) constant. Architectural rule: two system messages")
    - D:/Aura/.planning/research/SUMMARY.md line 62 (Pitfall #10: goroutine leak)
    - D:/Aura/prd.md line 1393 (header "## §Test discipline (vale per tutte le slice)" — the cross-cutting test conventions live here; NEW §Cross-cutting cache invariant section will be inserted as a peer §)
    - D:/Aura/prd.md line 1455 (existing goleak bullet: "Slice 1/3/6/8/9/11/13 lo richiedono esplicitamente in acceptance" — this is the line to extend per amendment #15)
    - D:/Aura/prd.md line 1490 (header "## §Slice Q&A discipline" — context for §-prefixed section convention)
  </read_first>
  <action>
    Apply Amendment #15 — extend goleak mandate to ALL goroutine-spawning packages:

    Edit 1 (prd.md line 1455 — existing goleak bullet "3. Goroutine leak check..."): replace the trailing sentence "Slice 1/3/6/8/9/11/13 lo richiedono esplicitamente in acceptance." with "**Amendment #15 (2026-05-29): mandate extended to ALL packages che spawn goroutine** — non solo le slice 1/3/6/8/9/11/13 originali. Lista completa pre-Phase 0 closure: Slice 0.9 (workflow agents Parallel + Loop), 1 (HTTP client SSE reader), 1.5 (ask_user resume goroutine), 1.8 (microcompact background), 2a/2b (sandbox reaper goroutine), 3 (swarm ParallelAgent), 4 (no goroutine — skipped), 5 (web_fetch HTTP), 6 (scheduler dispatcher + heartbeat + cron tick), 7c (TTL sweeper if implemented), 7e-core (TTL archive sweeper), 8 (AG-UI SSE emitter), 9a (setup wizard HTTP + SSE pump), 9b (telegram bot polling), 9c (multimodal sidecar HTTP), 10 (interview LoopAgent — no extra goroutine), 11b/c/d/e (ingest pipeline + community + memify background workers), 13 (offline detector + cost tracker — v2). Every `*_test.go` for these packages MUST call `goleak.VerifyNone(t)` in TestMain OR `defer goleak.VerifyNone(t)` per test."

    Apply Amendment #16 — new §Cross-cutting cache invariant CI section + script reference:

    Edit 2 (insert new section header BEFORE line 1393 `## §Test discipline`): insert a new top-level `##` section between the end of Slice 4 (around line 1391 — verify the `---` divider) and line 1393. Section content:

    "## §Cross-cutting — KV cache invariant CI (amendment #16, Pitfall #3 P0)

    Cache poisoning is the highest-risk cross-slice failure mode in Aura. Slice 4 owns the invariant (`messages[0]` byte-identical turn-su-turn) but Slices 1.8, 5, 7e-core, 10, 11e all mutate the message-construction pipeline. Without a cross-slice gate, every capability silently risks planting its own poisoning site. Reference: `reference_aura_cache_poisoning_sites_2026-05-27` (6 sites mapped pre-rewrite — historical record kept as warning).

    ### Architectural rule: TWO system messages

    - `messages[0]`: byte-identical turn-su-turn. Contains: system prompt + tool manifest (alphabetically sorted by name, per Slice 4). NO per-turn data, NO timestamps, NO conversation_id, NO identity_id strings.
    - `messages[1]`: mutable. Contains: `Agent.md` per-identity profile (Slice 10 injection) + cached `:AgentInsight` payload (Slice 11e injection, cached per amendment #11 via `AURA_AGENT_INSIGHT_CACHE_TTL_SEC`).
    - All conversation turns: `messages[2..N]`.

    Any future slice that wants to inject context MUST land in `messages[1]` (cached/stable-per-identity) — NEVER mutate `messages[0]`. Violation = automatic Slice 4 invariant break.

    ### CI gate `scripts/cache_invariant_audit.sh`

    - Authored in Phase 6 (Slice 4 implementation).
    - 20-turn replay against a fixture conversation. Computes `SHA-256(json.Marshal(messages[0]))` per turn. Asserts all 20 hashes identical.
    - On failure: explicit error `messages[0] mutated at turn N — diff: <previous-hash> vs <current-hash>; offending diff hint: <first 200 bytes of unified diff>`.
    - Runs in CI on every PR from Phase 6 onward. PRs that break the script fail with the explicit error above.
    - Exit codes: `0` (pass), `1` (mutation detected — gate failure), `2` (fixture corrupt — re-run).
    - File targets: `scripts/cache_invariant_audit.sh` (~80 LOC bash) + `scripts/fixtures/cache_invariant/turn-{01..20}.json` (replay turns) + CI workflow integration `.github/workflows/ci.yml` step `name: cache invariant gate`.

    ### Cross-slice acceptance bullets (already present per-slice — this section enumerates them for audit)

    - Slice 0.9 InvocationContext composition: confirm SessionStore / GraphStore / LLMClient injection is by-pointer (no per-call alloc).
    - Slice 1 LLM client: `client.Request(req)` does NOT mutate `req.Messages` (read-only contract).
    - Slice 1.8 LoadHistory: deterministic byte-identical reads.
    - Slice 4 PromptBuilder: SHA-256(`messages[0]`) constant — the anchor invariant.
    - Slice 5 web_fetch: no system-prompt mutation when results returned.
    - Slice 7e-core skill snippet execution: skill body sourced from disk-cached SKILL.md, NOT inlined per call.
    - Slice 10 Agent.md injection: lands at `messages[1]`, NOT `messages[0]`.
    - Slice 11e :AgentInsight injection: cached per amendment #11, lands at `messages[2]` continuation (or `messages[1]` append after Agent.md depending on builder order — Phase 15 decides).

    ### Pitfall #3 mitigation summary

    Pitfall #3 (Audit-table TRUNCATE bypass) is covered by amendment #17 (this plan, Task 2). Pitfall #2 (cross-slice KV cache poisoning) is covered by this section + the CI script.

    ---"

    Per CLAUDE.md "READ BEFORE EDIT": confirm line 1391 is the `---` divider closing Slice 4 commit message template, and line 1393 is `## §Test discipline`. Insert the new section between them.
  </action>
  <acceptance_criteria>
    - `grep -c "cache_invariant_audit\.sh" D:/Aura/prd.md` returns at least 3 (new section header + CI gate paragraph + file targets reference)
    - `grep -c "amendment #15" D:/Aura/prd.md` returns at least 1
    - `grep -c "amendment #16" D:/Aura/prd.md` returns at least 2 (section header + at least one inline cite)
    - `grep -c "^## §Cross-cutting" D:/Aura/prd.md` returns at least 1 (new section header)
    - `grep -E "messages\[0\] mutated" D:/Aura/prd.md` returns at least 1 (the CI failure error string)
    - `grep -E "Slice 0\.9 \(workflow agents Parallel" D:/Aura/prd.md` returns at least 1 (extended goleak list)
    - New section appears AFTER Slice 4 commit template and BEFORE §Test discipline: `awk '/^## Slice 4 — KV cache/{a=NR} /^## §Cross-cutting — KV cache/{b=NR} /^## §Test discipline/{c=NR} END{exit !(a&&b&&c&&a<b&&b<c)}' D:/Aura/prd.md` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "cache_invariant_audit\.sh" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #15" prd.md) -ge 1 &amp;&amp; test $(grep -c "amendment #16" prd.md) -ge 2 &amp;&amp; test $(grep -c "^## §Cross-cutting" prd.md) -ge 1 &amp;&amp; grep -q "messages\[0\] mutated" prd.md &amp;&amp; awk "/^## Slice 4 — KV cache/{a=NR} /^## §Cross-cutting — KV cache/{b=NR} /^## §Test discipline/{c=NR} END{exit !(a\&\&b\&\&c\&\&a<b\&\&b<c)}" prd.md &amp;&amp; echo OK'</automated>
  </verify>
  <done>New §Cross-cutting section landed between Slice 4 and §Test discipline; goleak mandate extended bullet rewritten; all grep checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Amendment #17 (audit role separation aura_app vs aura_migrate + TRUNCATE trigger)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 86: `| 17 | Add audit-table role separation aura_app vs aura_migrate + TRUNCATE trigger | Pitfall #6 | Slice 0.5 + 7c |`
    - D:/Aura/.planning/research/SUMMARY.md line 58 (Pitfall #6 description: "Postgres `BEFORE UPDATE OR DELETE` does NOT fire for TRUNCATE/DROP. Role separation: `aura_app` (INSERT, SELECT only) + `aura_migrate` gated by `AURA_DB_MIGRATE_URL` + `BEFORE TRUNCATE` trigger.")
    - D:/Aura/.planning/REQUIREMENTS.md line 18 (INFRA-01: "Include role separation `aura_app` (INSERT/SELECT only on audit tables) vs `aura_migrate` (DDL gated)")
    - D:/Aura/prd.md line 21 (Slice 0.5 header) and surrounding lines down to ~line 127 (Slice 0.5 body)
    - D:/Aura/prd.md line 49 (Slice 0.5 acceptance about empty initial schema)
    - D:/Aura/prd.md line 1896 (Slice 7c acceptance about audit trigger BEFORE UPDATE/DELETE)
    - D:/Aura/prd.md line 1931 (Slice 7c migration `0007_skill_audit.up.sql` description — references "Function `raise_audit_immutable()` + trigger `skill_audit_append_only BEFORE UPDATE OR DELETE`")
    - D:/Aura/prd.md line 1994 (Slice 7c commit body about audit immutability)
  </read_first>
  <action>
    Apply Amendment #17 — Postgres role separation + BEFORE TRUNCATE trigger:

    Edit 1 (Slice 0.5 — add new acceptance bullet near line 49 cluster, search for the existing acceptance section start under Slice 0.5; insert after the "Schema iniziale **vuoto**" bullet):
    "- [ ] **Postgres role separation (amendment #17, Pitfall #6 P0)**: migration `0001_init.sql` creates TWO roles: `aura_app` (granted `INSERT, SELECT` on `aura.*` audit tables, granted `INSERT, SELECT, UPDATE, DELETE` on `aura.*` mutable tables — NO TRUNCATE, NO DROP) and `aura_migrate` (granted ALL on `aura.*` including DDL, TRUNCATE). The runtime binary `aura serve` connects with `AURA_DB_URL` (role `aura_app`). Migrations run via `aura db migrate` connecting with `AURA_DB_MIGRATE_URL` (role `aura_migrate`). If `AURA_DB_MIGRATE_URL` is unset and `aura serve` boots: continue, but `aura db migrate` subcommand fails fast with explicit error \"AURA_DB_MIGRATE_URL required for DDL operations — see prd.md amendment #17\". Acceptance test (`db_integration`): connect as `aura_app`, attempt `TRUNCATE aura.skill_audit;` → must return permission denied. Connect as `aura_migrate`, same TRUNCATE → succeeds (consumed by migration only)."

    Edit 2 (Slice 0.5 — extend env section / Caps & Limits cross-ref): note in the Slice 0.5 body that two env vars are now required (`AURA_DB_URL` already exists; add `AURA_DB_MIGRATE_URL` to the env table). Specifically in the env table at line 4239 cluster, insert immediately after the `AURA_DB_URL` row:
    "| `AURA_DB_MIGRATE_URL` | (optional, defaults to `AURA_DB_URL`) | secret | 0.5 | Migrations-only DSN connecting as `aura_migrate` role (amendment #17, Pitfall #6 P0). If unset, `aura db migrate` fails fast. Production runtime never uses this URL — only `aura db migrate` subcommand reads it. |"

    Edit 3 (Slice 7c — extend audit trigger description at line 1931 area, the `0007_skill_audit.up.sql` row in file targets): the existing description mentions "Function `raise_audit_immutable()` + trigger `skill_audit_append_only BEFORE UPDATE OR DELETE`". REPLACE this trigger description to extend coverage to TRUNCATE:
    Locate the exact phrase "trigger `skill_audit_append_only BEFORE UPDATE OR DELETE ON aura.skill_audit FOR EACH ROW EXECUTE FUNCTION raise_audit_immutable()`" and replace with "trigger `skill_audit_append_only BEFORE UPDATE OR DELETE ON aura.skill_audit FOR EACH ROW EXECUTE FUNCTION raise_audit_immutable()` + trigger `skill_audit_truncate_block BEFORE TRUNCATE ON aura.skill_audit FOR EACH STATEMENT EXECUTE FUNCTION raise_audit_immutable_truncate()` (amendment #17 — Pitfall #6 P0: `BEFORE UPDATE OR DELETE` does NOT fire on TRUNCATE/DROP per PG docs; a second statement-level trigger closes the bypass. Combined with role separation `aura_app` lacking TRUNCATE privilege, this provides defense-in-depth.)"

    Edit 4 (Slice 7c acceptance at line 1896 area): immediately after the existing "Postgres trigger `BEFORE UPDATE/DELETE`" bullet, insert:
    "- [ ] **BEFORE TRUNCATE trigger (amendment #17, Pitfall #6 P0)**: function `raise_audit_immutable_truncate()` + trigger `skill_audit_truncate_block BEFORE TRUNCATE ON aura.skill_audit FOR EACH STATEMENT`. Acceptance test (`db_integration`): connect as `aura_migrate`, `TRUNCATE aura.skill_audit;` → returns explicit error `audit table is append-only`. Cross-link role separation: connect as `aura_app`, same TRUNCATE → returns permission denied (role lacks TRUNCATE privilege). Both gates active = belt-and-suspenders defense per Pitfall #6 P0."

    Edit 5 (Slice 7c commit body at line 1994 area): replace "audit log on Postgres via sqlc (migration 0007_skill_audit with CREATE TABLE aura.skill_audit + index ts DESC + trigger raise_audit_immutable BEFORE UPDATE/DELETE → RAISE EXCEPTION)" with "audit log on Postgres via sqlc (migration 0007_skill_audit with CREATE TABLE aura.skill_audit + index ts DESC + trigger raise_audit_immutable BEFORE UPDATE/DELETE → RAISE EXCEPTION + trigger raise_audit_immutable_truncate BEFORE TRUNCATE → RAISE EXCEPTION + role-separation grants from migration 0001 ensuring aura_app role lacks TRUNCATE — amendment #17 Pitfall #6 P0)"

    Per CLAUDE.md "NEVER SUPPOSE": after editing, re-grep to confirm both triggers + both roles are mentioned in Slice 7c migration row.
  </action>
  <acceptance_criteria>
    - `grep -c "aura_app" D:/Aura/prd.md` returns at least 4 (Slice 0.5 acceptance + env table + Slice 7c acceptance + commit body)
    - `grep -c "aura_migrate" D:/Aura/prd.md` returns at least 4
    - `grep -c "AURA_DB_MIGRATE_URL" D:/Aura/prd.md` returns at least 3 (Slice 0.5 acceptance + env table row + at least one cross-ref)
    - `grep -E "^\| \`AURA_DB_MIGRATE_URL\`" D:/Aura/prd.md` returns exactly 1 (env table row)
    - `grep -c "BEFORE TRUNCATE" D:/Aura/prd.md` returns at least 2 (Slice 7c migration description + acceptance bullet)
    - `grep -c "raise_audit_immutable_truncate" D:/Aura/prd.md` returns at least 2
    - `grep -c "amendment #17" D:/Aura/prd.md` returns at least 5
    - `grep -E "Pitfall #6" D:/Aura/prd.md` returns at least 3 matches (cross-section coverage)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "aura_app" prd.md) -ge 4 &amp;&amp; test $(grep -c "aura_migrate" prd.md) -ge 4 &amp;&amp; test $(grep -c "AURA_DB_MIGRATE_URL" prd.md) -ge 3 &amp;&amp; test $(grep -cE "^\| \`AURA_DB_MIGRATE_URL\`" prd.md) -eq 1 &amp;&amp; test $(grep -c "BEFORE TRUNCATE" prd.md) -ge 2 &amp;&amp; test $(grep -c "raise_audit_immutable_truncate" prd.md) -ge 2 &amp;&amp; test $(grep -c "amendment #17" prd.md) -ge 5 &amp;&amp; test $(grep -c "Pitfall #6" prd.md) -ge 3 &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; role separation + AURA_DB_MIGRATE_URL env + dual-trigger (UPDATE/DELETE + TRUNCATE) all specified in Slice 0.5 and Slice 7c.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Amendments #18, #19 (embedding dim env contract + loop budget contract)</name>
  <files>D:/Aura/prd.md</files>
  <read_first>
    - D:/Aura/.planning/research/SUMMARY.md line 87: `| 18 | Add embedding dim env contract + boot assertion + runbook | Pitfall #7 | Slice 0.7 + 11a + 11b |`
    - D:/Aura/.planning/research/SUMMARY.md line 88: `| 19 | Add agent loop budget contract (3 env vars + child budget inheritance) | Pitfall #9 | Slice 0.9 + 1 + 3 + 6 |`
    - D:/Aura/.planning/research/SUMMARY.md line 59 (Pitfall #7: "neo4j#13387, langchain#16336. Env contract AURA_EMBED_DIMENSIONS=768; sidecar boot asserts model.output_dim == env")
    - D:/Aura/.planning/research/SUMMARY.md line 60 (Pitfall #9: "3 orthogonal caps: MAX_STEPS=25, MAX_WALLCLOCK_SEC=300, DEDUP_WINDOW=3; per-child budget inherits parent's remaining budget (not fresh — otherwise swarm depth=3 × 25 each = 15625 steps)")
    - D:/Aura/.planning/REQUIREMENTS.md line 19 (INFRA-02: "Embedding dim env contract `AURA_EMBED_DIMENSIONS=768` con boot assertion sidecar")
    - D:/Aura/.planning/REQUIREMENTS.md line 20 (INFRA-03: budget contract 3 env vars + child inheritance)
    - D:/Aura/prd.md line 128 (Slice 0.7 header)
    - D:/Aura/prd.md lines 160-182 (Slice 0.7 acceptance section — find the embed sidecar bullets)
    - D:/Aura/prd.md line 247 (Slice 0.9 Pre-requisiti — already edited in plan 01 to mention Go 1.25; new env entries land in acceptance + env table)
    - D:/Aura/prd.md line 461 (Slice 0.9 acceptance about `Agent` interface)
    - D:/Aura/prd.md line 4239 (env table head — `AURA_DB_URL` row, before which `AURA_EMBED_DIMENSIONS` should land for Slice 0.7 logical grouping; budget envs land before `AURA_LLM_BASE_URL` row at line 4240 since they're Slice 0.9)
    - D:/Aura/prd.md line 4260 (env table row `AURA_SWARM_MAX_DEPTH` — context for where Loop budget envs land alphabetically/by slice)
  </read_first>
  <action>
    Apply Amendment #18 — embedding dim env contract + boot assertion + runbook (Slice 0.7 + Slice 11a/b):

    Edit 1 (Slice 0.7 acceptance section, lines 160-182 area — search for the existing bullet about embedding sidecar `/health`): immediately after the existing sidecar bullet, insert:
    "- [ ] **Embedding dim env contract (amendment #18, Pitfall #7 P0)**: env `AURA_EMBED_DIMENSIONS=768` (default 768 for `embeddinggemma-300m`). On boot, embed sidecar `aura-llama-embed` performs self-test: load one dummy embedding, assert `len(vector) == AURA_EMBED_DIMENSIONS`. Mismatch → exit code 78 (`EX_CONFIG`) with explicit error `embedding model output_dim=N != AURA_EMBED_DIMENSIONS=M — refuse to start (Pitfall #7 silent corruption)`. Aura `aura knowledge ping` validates sidecar `/health` returns `{\"dim\": <int>}` matching env. Mismatch → boot fail. Reference industry incidents: `neo4j#13387`, `langchain#16336`."
    "- [ ] **Embedding model swap runbook (amendment #18)**: docstring section in Slice 0.7 PRD body documents the rule: \"NO in-place embedding model upgrades. To change embed model: (1) stop ingest, (2) snapshot Neo4j via `neo4j-admin database dump`, (3) drop vector index, (4) re-create with new `vector.dimensions`, (5) re-embed all `:Chunk.embedding` from `:Chunk.text`, (6) re-create index. Half-state = silent retrieval corruption.\""

    Edit 2 (Slice 11a — acceptance section around line 3419 ingest preconditions): immediately after the existing acceptance bullets for 11a schema, insert (find the "#### 11a — Schema" bullet block at line 3418 area):
    "- [ ] **Embedding dim consistency check (amendment #18 cross-ref Slice 0.7)**: Cypher migration `0002_memory_schema.cql` CREATE VECTOR INDEX statements MUST reference dimensions via env-templated value (not hardcoded). Migration loader substitutes `${AURA_EMBED_DIMENSIONS}` (default 768) at apply time. Re-running migration with different env value DENIED unless preceded by `aura memory wipe-vectors --confirm` (idempotency safeguard). Acceptance test: change env to 1024, attempt re-apply → explicit error `vector dimension conflict: existing 768 vs requested 1024 — see Slice 0.7 runbook (amendment #18)`."

    Edit 3 (Slice 11b — ingest pipeline acceptance section at line 3425 area): immediately after the existing 11b ingest bullets, insert:
    "- [ ] **Embed batch dim consistency (amendment #18 P0)**: `embedder.go` Batch() asserts every returned vector has `len(v) == AURA_EMBED_DIMENSIONS`. Mismatch on any returned vector → abort ingest, log `embedding sidecar returned wrong dim — refusing to corrupt Neo4j vector index`. Acceptance test: mock sidecar returning 384d vector → ingest aborts before Neo4j upsert."

    Edit 4 (env table — insert new row before line 4239 `AURA_DB_URL`): note that all `AURA_*` envs in the table are NOT alphabetical (they're grouped by slice). Insert `AURA_EMBED_DIMENSIONS` adjacent to other Slice 0.7 / 11 envs. The simplest location: immediately BEFORE the row `AURA_MEMORY_CHUNK_SIZE_TOKENS` at line 4286 (first Slice 11 env), insert:
    "| `AURA_EMBED_DIMENSIONS` | `768` | cap | 0.7 | Embedding vector dimensionality (amendment #18, Pitfall #7 P0). Sidecar `aura-llama-embed` boot-asserts `model.output_dim == this`. Neo4j `CREATE VECTOR INDEX` substitutes at migration time. **NEVER change in-place** on a populated DB — see Slice 0.7 runbook. |"

    Apply Amendment #19 — agent loop budget contract (3 env vars + child inheritance):

    Edit 5 (Slice 0.9 acceptance at line 461 area — locate the existing acceptance bullet about `Agent` interface): insert new bullets:
    "- [ ] **Agent loop budget contract (amendment #19, Pitfall #9 P0)**: three orthogonal caps enforced per `Run(InvocationContext)`:
      - `AURA_LOOP_MAX_STEPS=25` (default; hard cap on tool-call iterations within a single Run)
      - `AURA_LOOP_MAX_WALLCLOCK_SEC=300` (default; hard cap on wall-clock seconds before returning `Event{FinishReason:'max_wallclock', Actions.Escalate=true}`)
      - `AURA_LOOP_DEDUP_WINDOW=3` (default; sliding window — if the last N=3 tool calls had identical `tool_name + args_hash`, return `Event{FinishReason:'dedup_loop', Actions.Escalate=true}`)
      All three caps are orthogonal: ANY one tripping terminates the Run. The triggering cap is reported in `Event.FinishReason`."
    "- [ ] **Child budget inheritance (amendment #19, Pitfall #9 P0)**: workflow agents Sequential/Loop/Parallel propagate the parent `InvocationContext`'s **remaining** step + wallclock budgets to children (NOT fresh per child). Otherwise swarm depth=3 × 25 = 15625 total steps (compounding explosion). Implementation: `InvocationContext.RemainingSteps int` + `RemainingWallclockDeadline time.Time` fields, decremented + checked by each agent's Run before spawning sub-agents. Acceptance test: depth-3 spawn chain with parent budget=20 steps → total steps across the tree ≤ 20, NOT 20×3=60. Test: TestBudgetInheritance_3Deep_ParallelSpawn covers Parallel propagation; TestBudgetInheritance_LoopWithChildSwarm covers Loop→Parallel composition."

    Edit 6 (Slice 0.9 InvocationContext struct — extend with new fields). DO NOT use absolute line numbers: plan 00-02 task 3 runs in the same wave and inserts `RequestID` into this same struct, shifting line numbers underneath us. Locate the insertion point dynamically: grep `prd.md` for `RequestID.*UUIDv7.*amendment #9` inside the InvocationContext struct fence; if that line exists, insert the two new fields on the line immediately after it. If plan 00-02 has not yet landed (RequestID line absent), insert at the position just BEFORE the `// ... extension points` comment in the InvocationContext struct fence — plan 00-02 will then insert RequestID above your additions, which is also acceptable. Either ordering satisfies the aggregator's `grep -c "RemainingSteps" prd.md >= 3` and `grep -c "RequestID" prd.md >= 3` post-checks. Re-grep after editing to confirm:
    `    RemainingSteps           int           // budget contract amendment #19: steps remaining for THIS Run before parent-budget exhaustion. Workflow agents decrement + propagate.`
    `    RemainingWallclockDeadline time.Time   // budget contract amendment #19: absolute deadline (parent-anchored). Children inherit this instant unchanged.`

    Edit 7 (env table — insert 3 new rows for budget envs near Slice 0.9 grouping; safest location is immediately BEFORE `AURA_SWARM_MAX_DEPTH` at line 4260): insert three new rows in order:
    "| `AURA_LOOP_MAX_STEPS` | `25` | cap | 0.9 | Tool-call iteration cap per `Run` (amendment #19, Pitfall #9 P0). Child agents inherit parent's REMAINING (not fresh). Trip → `Event.FinishReason='max_steps'`. |"
    "| `AURA_LOOP_MAX_WALLCLOCK_SEC` | `300` | cap | 0.9 | Wallclock budget per `Run` in seconds (amendment #19, Pitfall #9 P0). Children inherit parent's absolute deadline. Trip → `Event.FinishReason='max_wallclock'`. |"
    "| `AURA_LOOP_DEDUP_WINDOW` | `3` | cap | 0.9 | Sliding window size for tool-call dedup (amendment #19, Pitfall #9 P0). If last N tool calls have identical `tool_name + args_hash`, trip → `Event.FinishReason='dedup_loop'`. |"

    Edit 8 (Slice 1 acceptance — cross-reference budget contract): immediately after the OTel span bullet added by plan 02 task 3 (Slice 1 acceptance), insert:
    "- [ ] **Budget contract enforcement (amendment #19 cross-ref)**: `LlmAgent.Run` checks `InvocationContext.RemainingSteps` and `RemainingWallclockDeadline` before each LLM call. Mid-Run trip → emit terminal Event with appropriate `FinishReason` ('max_steps' | 'max_wallclock' | 'dedup_loop'). Test: TestLlmAgent_StepCap_Trips, TestLlmAgent_WallclockCap_Trips, TestLlmAgent_DedupWindow_Trips."

    Edit 9 (Slice 3 acceptance — already cross-referenced via plan 03 task 2's amendment #12 edits; add an explicit pointer): immediately after the existing Slice 3 acceptance bullet about MaxSpawnDepth, insert:
    "- [ ] **Swarm budget inheritance (amendment #19 cross-ref Slice 0.9)**: `Coordinator.Spawn` propagates parent's REMAINING budget to spawned children via `InvocationContext.RemainingSteps` + `RemainingWallclockDeadline`. Acceptance test: parent has 20 steps remaining, spawns 3 children — total step count across the spawn tree ≤ 20 (NOT 20*3=60). Cross-link Pitfall #9."

    Edit 10 (Slice 6 acceptance — scheduler-spawned jobs inherit budget): locate Slice 6 acceptance section (around line 1669-1810); insert a new bullet (find the existing acceptance section's tail and insert):
    "- [ ] **Scheduler budget contract (amendment #19 cross-ref Slice 0.9)**: scheduler-spawned `agent_job` runs read budget from `aura.agent_job_runs.step_budget` (NEW column, ALTER TABLE in same migration) — defaults to `AURA_LOOP_MAX_STEPS` if NULL. The scheduler sets `InvocationContext.RemainingSteps = step_budget` at spawn. Per-job override via `aura task schedule --max-steps N`. Acceptance test: schedule task with `--max-steps 10`, agent loops → terminates at step 10 (not fresh 25)."
  </action>
  <acceptance_criteria>
    - `grep -c "AURA_EMBED_DIMENSIONS" D:/Aura/prd.md` returns at least 4 (Slice 0.7 acceptance + Slice 11a + Slice 11b + env table row)
    - `grep -c "amendment #18" D:/Aura/prd.md` returns at least 5
    - `grep -c "AURA_LOOP_MAX_STEPS" D:/Aura/prd.md` returns at least 3 (Slice 0.9 acceptance + env table row + Slice 1 or Slice 6 cross-ref)
    - `grep -c "AURA_LOOP_MAX_WALLCLOCK_SEC" D:/Aura/prd.md` returns at least 2
    - `grep -c "AURA_LOOP_DEDUP_WINDOW" D:/Aura/prd.md` returns at least 2
    - `grep -c "RemainingSteps" D:/Aura/prd.md` returns at least 3 (struct field + Slice 0.9 acceptance + Slice 3 cross-ref)
    - `grep -c "amendment #19" D:/Aura/prd.md` returns at least 7
    - `grep -E "^\| \`AURA_EMBED_DIMENSIONS\`" D:/Aura/prd.md` returns exactly 1
    - `grep -E "^\| \`AURA_LOOP_MAX_STEPS\`" D:/Aura/prd.md` returns exactly 1
    - `grep -E "^\| \`AURA_LOOP_MAX_WALLCLOCK_SEC\`" D:/Aura/prd.md` returns exactly 1
    - `grep -E "^\| \`AURA_LOOP_DEDUP_WINDOW\`" D:/Aura/prd.md` returns exactly 1
    - `grep -c "Pitfall #7" D:/Aura/prd.md` returns at least 3
    - `grep -c "Pitfall #9" D:/Aura/prd.md` returns at least 4
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Aura &amp;&amp; bash -c 'set -e; test $(grep -c "AURA_EMBED_DIMENSIONS" prd.md) -ge 4 &amp;&amp; test $(grep -c "amendment #18" prd.md) -ge 5 &amp;&amp; test $(grep -c "AURA_LOOP_MAX_STEPS" prd.md) -ge 3 &amp;&amp; test $(grep -c "AURA_LOOP_MAX_WALLCLOCK_SEC" prd.md) -ge 2 &amp;&amp; test $(grep -c "AURA_LOOP_DEDUP_WINDOW" prd.md) -ge 2 &amp;&amp; test $(grep -c "RemainingSteps" prd.md) -ge 3 &amp;&amp; test $(grep -c "amendment #19" prd.md) -ge 7 &amp;&amp; test $(grep -cE "^\| \`AURA_EMBED_DIMENSIONS\`" prd.md) -eq 1 &amp;&amp; test $(grep -cE "^\| \`AURA_LOOP_MAX_STEPS\`" prd.md) -eq 1 &amp;&amp; test $(grep -c "Pitfall #7" prd.md) -ge 3 &amp;&amp; test $(grep -c "Pitfall #9" prd.md) -ge 4 &amp;&amp; echo OK'</automated>
  </verify>
  <done>All grep checks succeed; embed dim contract + 3 budget envs + InvocationContext struct extension + cross-slice acceptance bullets present in Slice 0.7, 0.9, 1, 3, 6, 11a, 11b.</done>
</task>

</tasks>

<verification>
Phase-level grep fingerprints for cross-cutting pitfalls cluster (post Wave 1):
- `cache_invariant_audit.sh` ≥ 3 hits
- `aura_app`, `aura_migrate` ≥ 4 hits each
- `AURA_DB_MIGRATE_URL` ≥ 3 hits
- `AURA_EMBED_DIMENSIONS` ≥ 4 hits
- `AURA_LOOP_MAX_STEPS` ≥ 3 hits, `AURA_LOOP_MAX_WALLCLOCK_SEC` ≥ 2 hits, `AURA_LOOP_DEDUP_WINDOW` ≥ 2 hits
- `RemainingSteps` ≥ 3 hits
- New `## §Cross-cutting` section header present
- `amendment #15` ≥ 1, `amendment #16` ≥ 2, `amendment #17` ≥ 5, `amendment #18` ≥ 5, `amendment #19` ≥ 7
- `Pitfall #6` ≥ 3, `Pitfall #7` ≥ 3, `Pitfall #9` ≥ 4
</verification>

<success_criteria>
- 3 tasks complete
- 5 amendments (#15, #16, #17, #18, #19) reflected in prd.md
- New §Cross-cutting cache invariant section added
- 4 new env vars added to Caps & Limits table: AURA_DB_MIGRATE_URL, AURA_EMBED_DIMENSIONS, AURA_LOOP_MAX_STEPS, AURA_LOOP_MAX_WALLCLOCK_SEC, AURA_LOOP_DEDUP_WINDOW
- InvocationContext struct extended with RemainingSteps + RemainingWallclockDeadline
- No `.go`, `.sql`, `.yaml`, or `.sh` files created (the `scripts/cache_invariant_audit.sh` file authored in Phase 6, not Phase 0)
- No git commit created
</success_criteria>

<output>
On completion, create `.planning/phases/00-prd-amendments/00-04-SUMMARY.md` documenting:
- New §Cross-cutting section: line range
- Lines edited in Slice 0.5 + Slice 0.7 + Slice 0.9 (struct, acceptance) + Slice 1 + Slice 3 + Slice 6 + Slice 7c + Slice 11a/b
- New env table rows added (5 total: AURA_DB_MIGRATE_URL, AURA_EMBED_DIMENSIONS, AURA_LOOP_MAX_STEPS, AURA_LOOP_MAX_WALLCLOCK_SEC, AURA_LOOP_DEDUP_WINDOW)
- Acceptance grep counts at final state
- Confirmation: no commit, no code/sql/yaml/sh files created
</output>
