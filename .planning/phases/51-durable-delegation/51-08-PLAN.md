---
phase: 51-durable-delegation
plan: 08
type: execute
wave: 9
depends_on: ["51-01", "51-02", "51-03", "51-04", "51-05", "51-06a", "51-06b", "51-07", "51-09", "51-10", "51-11", "51-12a", "51-12b"]
files_modified:
  - .planning/phases/51-durable-delegation/drive-sc.sh
  - .planning/phases/51-durable-delegation/51-VALIDATION.md
  - docs/aura-quality-snapshot.md
  - prd.md
autonomous: false
requirements: [SWARM-01, SWARM-02, SWARM-03, SWARM-04, SWARM-05, SWARM-06, SWARM-07, SWARM-09, SWARM-10]
must_haves:
  truths:
    - "SC#1 proven live: delegating returns Aura's turn immediately, the operator keeps talking, and the consolidated result lands in aura.conversation_turns when the work finishes — asserted with psql on a conversation the operator has NOT reopened, because a report the operator has to go fetch by speaking first is not SC#1 (plan 51-10)"
    - "SC#1 absent-operator leg proven live: with the cockpit closed, the report reaches the owning channel exactly once and the conversation row exists anyway; with the cockpit open and a turn running, the steer rail delivers it and NO channel push follows"
    - "SC#2 proven live: a worker that delegates receives its own workers' results within its turn"
    - "SC#3 proven live: the worker brief carries context separately from goal, and the rendered schema shows the operator's real caps"
    - "SC#4 proven live: a worker's question surfaces in the operator's channel naming the worker, and answering makes THAT worker continue — the verdict is work the worker did AFTER the answer, never merely that a sibling stayed still (plan 51-06b)"
    - "SC#4 deferred-tool leg proven live: a worker that used tool_search before pausing can still call that tool after the resume — LibreChat's measured 'unknown tool' failure does not reproduce"
    - "D-03 proven live: a delegation that runs longer than the retired ~240s wall clock COMPLETES, and a stalled worker is still reaped at AURA_SWARM_CHILD_IDLE_SEC without a second worker picking up its goal (plan 51-09)"
    - "SC#5 proven live: after a fan-out the graph holds one correctly-attributed fact per worker — no duplicates, no lost writes, none attributed to the parent"
    - "Every verdict is read from the reassembled final message and the graph/DB state, never from a grep on the raw SSE stream"
    - "docs/aura-quality-snapshot.md carries a re-attestation for every row whose CI-gate glob matches a file this phase changed, and scripts/quality_snapshot_gate.sh prints ok"
    - statement: "The crash-after-partial-side-effects case (a worker that ran shell_exec then the daemon died before the ledger write) is either exercised live or recorded as documented residual risk with an explicit verdict — never silently assumed covered by spike 100"
      verification: explicit
  prohibitions:
    - "MUST NOT derive a pass/fail verdict from a single grep line on the SSE stream — spike 098 measured two false OBEYED verdicts that way"
    - "MUST NOT close the phase on a green unit suite — DoD is a live E2E scored >9.8"
    - "MUST NOT claim the deleted internal/eval harness's uncovered items (mail/WhatsApp read-back, <1.5x timing ratio, >=90% judge score, no-over-spawn) as closed unless they were actually run"
    - "MUST NOT run the live verification against a stale container image"
  artifacts:
    - path: ".planning/phases/51-durable-delegation/drive-sc.sh"
      provides: "phase-specific live driver for SC#1..SC#5"
      min_lines: 80
  key_links:
    - from: "drive-sc.sh"
      to: "the running stack"
      via: "authenticated /api/conversations + /agent/run with Idempotency-Key"
      pattern: "Idempotency-Key"
---

<objective>
Close the phase the only way CLAUDE.md accepts: **a green unit suite closes nothing.** Drive
the real agent on the running stack, score a real scenario per success criterion, and record
both what was proven and what was not.

The old live-judge harness (`internal/eval/harness_swarm_e2e_test.go`) stays deleted, so this
phase supplies its own driver — modelled on `.planning/spikes/098-steer-carries-worker-result/
drive.sh` and `.planning/spikes/099-worker-duration-and-progress/drive.sh`, which are the two
scripts that actually found this phase's three shipped defects by driving the agent rather than
reading code.

Purpose: Definition of Done. Output: a driver script, a filled VALIDATION.md, a re-attested
quality snapshot, and a PRD paragraph recording what was measured — including its perimeter.
</objective>

## Artifacts this phase produces

- File: `.planning/phases/51-durable-delegation/drive-sc.sh`
- Updated: `51-VALIDATION.md` (status → validated, per-task map filled with real results),
  `docs/aura-quality-snapshot.md` (re-attested rows), `prd.md` (post-implementation measurement
  paragraph under Amendment #154)

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-durable-delegation/51-RESEARCH.md
@.planning/phases/51-durable-delegation/51-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the phase's live driver for SC#1..SC#5</name>
  <files>.planning/phases/51-durable-delegation/drive-sc.sh</files>
  <read_first>
    - `.planning/spikes/098-steer-carries-worker-result/drive.sh` and its `README.md` (the delta-reassembly method and the false-verdict finding)
    - `.planning/spikes/099-worker-duration-and-progress/drive.sh` and its `README.md` (the fan-out timing method)
    - `.planning/spikes/101-message-channel-necessity/README.md` (the cross-channel delivery check)
    - `51-RESEARCH.md` §`Common Pitfalls` Pitfall 4 (raw SSE grepping produces false verdicts)
    - `51-VALIDATION.md` §`Manual-Only Verifications`
  </read_first>
  <action>
Write `drive-sc.sh` (LF line endings — a CRLF script looks fine in Git Bash and dies in WSL with
`set: pipefail: invalid option name`). It must: authenticate, create a conversation, and drive
`/agent/run` with an `Idempotency-Key` header on both `/api/conversations` and `/agent/run`
(both return 400 without it).

Five scenarios, each writing its raw stream AND its reassembled final message to a per-scenario
artifact directory:
- **SC#1:** ask for a 2-goal delegation; assert the run returns before the workers finish;
  immediately send a second operator message on the same thread; then poll
  `aura.conversation_turns` until the consolidated report appears. Capture the elapsed time
  between the turn returning and the report landing.
- **SC#2:** ask for a nested delegation (a worker that must itself delegate); assert the
  worker's own report contains its children's results and that the worker's turn did not return
  early.
- **SC#3:** dump the rendered `swarm_spawn` schema from a live manifest and assert the three
  cap values match the deployment's env; inspect one worker's brief for the separate context
  section.
- **SC#4:** drive a delegation whose worker must ask; assert the question reaches the operator
  channel NAMING the worker; answer it; assert that worker resumed and no sibling pause moved.
- **SC#5:** fan out several workers that each learn something durable; read the graph back and
  assert one correctly-attributed fact per worker, zero duplicates, zero facts attributed to
  the parent.

Verdicts are computed from the REASSEMBLED `TEXT_MESSAGE_CONTENT` deltas and from DB/graph
state — never from a grep on the raw SSE stream. Spike 098 reported "OBEYED" twice on a
`grep -qi` when the model had actually REFUSED and was quoting the word while explaining the
refusal.

Before any run: `docker compose build aura && docker compose up -d`, then verify the image was
built from current HEAD. A stale image has produced false verdicts here twice.
  </action>
  <verify>
    <automated>bash -n .planning/phases/51-durable-delegation/drive-sc.sh &amp;&amp; file .planning/phases/51-durable-delegation/drive-sc.sh | grep -vq CRLF</automated>
  </verify>
  <acceptance_criteria>
    - `drive-sc.sh` contains `Idempotency-Key` for both `/api/conversations` and `/agent/run`
    - `grep -c 'TEXT_MESSAGE_CONTENT' .planning/phases/51-durable-delegation/drive-sc.sh` returns ≥1 (delta reassembly is present)
    - The script has no `grep -qi` used as a compliance verdict
    - `bash -n` parses clean and the file has LF line endings
    - The script writes one artifact directory per scenario containing the raw stream, the reassembled message, and the DB/graph read-back
  </acceptance_criteria>
  <done>A repeatable live driver exists for all five success criteria.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Score SC#1-SC#5 live on the running stack</name>
  <what-built>The full Phase 51 stack: durable background delegation, Postgres steer queue with per-kind TTLs, goal/context brief split, live-rendered caps, depth-bounded nesting, fenced per-worker pauses, host-derived fact provenance, and a transcript read surface.</what-built>
  <how-to-verify>
    1. `docker compose build aura && docker compose up -d`; confirm the image is from current HEAD.
    2. Run `bash .planning/phases/51-durable-delegation/drive-sc.sh` end to end.
    3. For EACH of SC#1..SC#5, read the reassembled final message and the DB/graph read-back,
       and score the scenario. CLAUDE.md's bar is **>9.8**; anything below is a phase that is
       not done, not a phase with a caveat.
    4. Confirm SC#1's report actually influenced the model's answer (spike 098's failure mode is
       a mechanically successful delivery the model discounts as spoofing).
    5. Confirm SC#5 in the graph directly: one fact per worker, correct `writer_role`/run id,
       zero attributed to the parent.
    6. State a verdict on the crash-after-partial-side-effects case: exercised live, or recorded
       as documented residual risk. Do not leave it implied.
  </how-to-verify>
  <resume-signal>Report the five scores and the residual-risk verdict, or describe what failed</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the measurement — VALIDATION, quality snapshot, PRD</name>
  <files>.planning/phases/51-durable-delegation/51-VALIDATION.md, docs/aura-quality-snapshot.md, prd.md</files>
  <read_first>
    - `docs/aura-quality-snapshot.md` (every row's CI-gate-path glob and its `Last measured` field)
    - `scripts/quality_snapshot_gate.sh`
    - `prd.md` lines 8504-8585 (Amendment #154 — the paragraph this measurement extends)
    - `51-VALIDATION.md` (the Per-Task Verification Map to complete with real results)
  </read_first>
  <action>
Fill `51-VALIDATION.md`: replace every remaining `⬜ pending` with the real outcome, set
`status: validated`, and set `nyquist_compliant` truthfully — `false` with named gaps is
acceptable and honest; `true` with an unrun tier is not.

Update `docs/aura-quality-snapshot.md`: for EVERY row whose CI-gate-path glob matches a file
changed in this phase, bump `Last measured` to today and PREPEND a re-attestation — a fresh
measurement if the metric moved, else a metric-neutral justification naming exactly what changed
and why the number cannot move, keeping the prior notes as `Prior …`. The "Swarm E2E" row gets
this phase's real numbers, and its four still-uncovered items (mail/WhatsApp MCP read-back,
`<1.5×` timing ratio, `≥90%` judge score, no-over-spawn) stay listed as uncovered unless the
driver actually ran them.

Verify locally BEFORE the phase-close push — it must print `ok: … checked N row(s)`:
`AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)"
AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash
scripts/quality_snapshot_gate.sh`

Append a post-implementation measurement paragraph to `prd.md` under Amendment #154: what was
measured on the running stack, with the date and the evidence path, AND — per CLAUDE.md — what
the measurement does NOT prove (one deployment, one profile, one routed model; the
crash-after-partial-side-effects verdict; whichever E2E items remain unrun). A number without
its perimeter is another assumption in disguise.

Then run the full gate: `make quality-full` with the stack up, plus the `db_integration` and
`arcadedb_integration` matrices as `aura_app`, and confirm the aggregate owned-source floor
stays ≥85% with `bash scripts/coverage_docker.sh` (disposable `aura_cov`, never the live `aura`
database).
  </action>
  <verify>
    <automated>AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh</automated>
    <automated>bash scripts/coverage_docker.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/quality_snapshot_gate.sh` prints `ok:` and a non-zero checked-row count
    - `scripts/coverage_docker.sh` reports the aggregate owned-source ratio ≥ 85% and no package-policy regression
    - `51-VALIDATION.md` has `status: validated` and zero remaining `TBD` task ids
    - `prd.md` gains a dated measurement paragraph that includes an explicit "what this does NOT prove" list
    - The Swarm E2E row in `docs/aura-quality-snapshot.md` carries today's date and keeps its still-uncovered items listed
    - `make quality-full` exits 0 with the stack up
  </acceptance_criteria>
  <done>The phase's measurement is recorded with its perimeter, the snapshot gate passes, and the coverage floor holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver script → live stack | the driver holds real credentials against a running deployment |
| live scenario output → PRD/documentation | a measured number becomes a project-wide fact |

## STRIDE Threat Register (ASVS L1, block on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-32 | Information Disclosure | credentials in the driver script | high | mitigate | Read auth from `.env`/environment; the script contains no literal secret and its artifacts directory is git-ignored |
| T-51-33 | Repudiation | a claimed measurement nobody can reproduce | high | mitigate | Every verdict is written to a per-scenario artifact directory with the raw stream, the reassembled message and the DB/graph read-back |
| T-51-34 | Tampering | verification run against a stale image or the live `aura` database | high | mitigate | Rebuild-and-verify-HEAD precondition; `coverage_docker.sh`'s disposable `aura_cov` refusal guard |
| T-51-35 | Spoofing | a false green from an SSE grep | high | mitigate | Delta reassembly mandated; grep-as-verdict prohibited (Pitfall 4) |
</threat_model>

<verification>
- Full tag matrix: unit + `-race` + `db_integration` (as `aura_app`) + `arcadedb_integration`
- `make quality-full` with the stack up
- Five live scenarios scored >9.8
</verification>

<success_criteria>
- SC#1..SC#5 each proven live with reproducible artifacts
- Quality snapshot re-attested and the gate green
- The PRD records what was measured AND what it does not prove
</success_criteria>

<output>
Create `.planning/phases/51-durable-delegation/51-08-SUMMARY.md` when done
</output>
