---
phase: 51-durable-delegation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/swarm/delegation_queue.go
  - internal/swarm/delegation_queue_test.go
  - internal/swarm/swarm.go
  - internal/config/config.go
  - cmd/aura/serve_delegation.go
  - .env.example
autonomous: true
requirements: [SWARM-03, SWARM-09]
must_haves:
  truths:
    - "A top-level swarm_spawn call returns a ToolResult before any worker has finished, and the parent turn continues"
    - "One aura.ingestion_jobs row per goal exists with job_type='swarm_delegation' and the caller's identity_id"
    - "A claim loop running in the daemon claims that row through the real ClaimIngestionJobs Go path, runs a worker, and marks the row completed"
    - "The consolidated worker report re-enters the conversation through steer with Source=steer.SourceWorker, observable in aura.conversation_turns"
    - "A worker whose lease expires while its owner is dead is reclaimed by the next claim pass; a live worker's lease is never robbed (D-01)"
    - "A delegation row that has failed max_attempts times stops being claimed instead of looping (D-01)"
    - statement: "SWARM-03 edge (empty): a swarm_spawn call with zero goals enqueues zero rows and returns a model-readable rejection, not a Go error"
      verification: backstop
    - statement: "SWARM-09 edge (unclassified/durability): a delegation row claimed by a process that dies before writing any terminal status is reclaimed exactly once, not duplicated"
      verification: backstop
    - statement: "SWARM-03 edge (concurrency): two claim-loop passes running concurrently against the same identity never claim the same row twice (FOR UPDATE SKIP LOCKED)"
      verification: backstop
  prohibitions:
    - "MUST NOT create a new delegation table — D-01 measured aura.ingestion_jobs as sufficient (spike 100)"
    - "MUST NOT build agent_message_send or any messaging-schema table — closed by D-02 / spike 101"
    - "MUST NOT push a delegation result into the steer inbox with any Source other than steer.SourceWorker"
    - "MUST NOT size the delegation lease against the nominal AURA_SWARM_CHILD_TIMEOUT_SEC=120 — the measured effective bound is 240s (D-03)"
    - "MUST NOT build the worker LlmAgent without gateway.WithDelegatedDispatch(ctx)"
    - "MUST NOT add the enqueue/claim-loop concern inline to internal/swarm/swarm.go"
  artifacts:
    - path: "internal/swarm/delegation_queue.go"
      provides: "Enqueue + claim-loop worker for job_type='swarm_delegation'"
      min_lines: 120
    - path: "cmd/aura/serve_delegation.go"
      provides: "Composition-root wiring of the delegation claim loop"
    - path: "internal/swarm/delegation_queue_test.go"
      provides: "db_integration coverage of the real claim/reclaim Go path"
  key_links:
    - from: "internal/swarm/delegation_queue.go"
      to: "internal/documents (ClaimIngestionJobs)"
      via: "reuse of the shipped lease queue, no new table"
      pattern: "Claim\\(ctx, .*ClaimIngestionJobsRequest"
    - from: "internal/swarm/delegation_queue.go"
      to: "internal/steer"
      via: "Push of the consolidated report under SourceWorker"
      pattern: "steer\\.SourceWorker"
    - from: "internal/swarm/delegation_queue.go"
      to: "internal/gateway"
      via: "delegated dispatch reservation marker"
      pattern: "gateway\\.WithDelegatedDispatch"
---

<objective>
TRACER SLICE. Prove SC#1 end to end at production quality on the thinnest possible path: one
top-level delegation enqueues a durable row into the EXISTING `aura.ingestion_jobs` queue
(D-01, measured by spike 100), a daemon-resident claim loop picks it up through the REAL
`ClaimIngestionJobs` Go path, runs a worker exactly as `runChild` does today, and its
consolidated report re-enters the conversation through the shipped steer rail under
`steer.SourceWorker` (D-04, envelope already shipped in `f60d109f4`).

Purpose: every later plan in this phase expands a path this slice has already proven. Nothing
here is a stub — the queue row, the lease, the worker, and the delivery are all real.
Output: `internal/swarm/delegation_queue.go`, its `db_integration` test, and the `cmd/aura`
wiring that makes the loop resident in the daemon.

Decisions carried: **D-00** (LibreChat/hermes read before designing — the claim loop copies
`internal/documents/jobs_worker.go`, itself the in-tree answer to LibreChat's
`GenerationJobManager`), **D-01** (generalize `aura.ingestion_jobs`, no new table),
**D-02** (no message channel: steer rail present-operator + existing outbox absent-operator),
**D-03** (lease sized against the measured 240s effective bound, never the nominal 120s),
**D-04** (steer rail is the delivery mechanism; worker envelope already exists),
**D-05** (no channel is excluded — delivery is server-side), **D-14** (one single Phase 51).
</objective>

## Artifacts this phase produces

New symbols introduced by THIS plan (exclude from drift verification — they do not exist yet):

- Files: `internal/swarm/delegation_queue.go`, `internal/swarm/delegation_queue_test.go`,
  `cmd/aura/serve_delegation.go`
- Types: `swarm.DelegationEnqueuer`, `swarm.DelegationClaimLoop`, `swarm.DelegationPayload`,
  `swarm.SteerPublisher` (interface, defined in `internal/swarm`, wired at `cmd/aura`)
- Functions/methods: `swarm.NewDelegationClaimLoop`, `(*DelegationClaimLoop).ProcessOnce`,
  `(*DelegationClaimLoop).Run`, `swarm.EnqueueDelegation`, `swarm.delegationIdempotencyKey`
- Constants: `swarm.JobTypeSwarmDelegation = "swarm_delegation"`
- Config fields: `Config.SwarmDelegationLeaseSec`, `Config.SwarmDelegationPollSec`
- Env vars: `AURA_SWARM_DELEGATION_LEASE_SEC`, `AURA_SWARM_DELEGATION_POLL_SEC`

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-durable-delegation/51-RESEARCH.md
@.planning/phases/51-durable-delegation/51-PATTERNS.md
@.planning/phases/51-durable-delegation/51-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Durable delegation enqueue + claim loop, end to end</name>
  <files>internal/swarm/delegation_queue.go, internal/swarm/delegation_queue_test.go, internal/swarm/swarm.go, internal/config/config.go, .env.example</files>
  <read_first>
    - `internal/documents/jobs_worker.go` (272 LOC, THE analog — `ProcessOnce`, `handleWithHeartbeat`, `maintainLease`, `recordHandlerFailure`)
    - `internal/documents/jobs_store.go` (408 LOC — `ClaimIngestionJobsRequest`, `withIdentity`, `UpdateStatus`, `Retry`, `Heartbeat`)
    - `internal/db/queries/ingestion_jobs.sql` (the claim predicate to reuse verbatim)
    - `internal/swarm/swarm.go` (245 LOC — `Run`, `preflight`, `runWave`, `runChild`; the worker construction to copy)
    - `internal/gateway/delegated_reservation.go` (119 LOC — read the header comment in full)
    - `internal/steer/inbox.go` (`SourceWorker`, `Push` signature)
    - `internal/agent/llm_agent_steer.go` (`markSteer` envelope selection — do not modify)
    - `51-PATTERNS.md` §`internal/swarm/delegation_queue.go` and §`Delegated-dispatch reservation marker`
  </read_first>
  <behavior>
    - `delegationIdempotencyKey(identityID, convID, parentRunID, goalIndex, goal)` is deterministic: the same inputs produce the same key, a different goal index produces a different key.
    - `EnqueueDelegation` with an empty goal slice enqueues zero rows and returns a model-readable rejection string, not a Go error.
    - `EnqueueDelegation` writes exactly one row per goal with `job_type="swarm_delegation"`, the caller's `identity_id`, and a payload carrying `{goal, context, conversation_id, parent_run_id, depth}`.
    - Re-running the same enqueue with the same idempotency key adds no second row.
    - `ProcessOnce` claims only rows whose `job_type` is `swarm_delegation`, never an ingestion row.
    - A claimed row whose `locked_until` has passed while `status='running'` is reclaimed by the next `ProcessOnce`; a row with a live lease is not.
    - A row at `attempt_count >= max_attempts` is not claimed again and lands in `dead_letter`.
    - On worker success, the loop pushes one `steer.Message` with `Source: steer.SourceWorker` and transitions the row to `completed` — in that order, with the transition gated on the push having been attempted.
  </behavior>
  <action>
Create `internal/swarm/delegation_queue.go` as a NEW file (never inline in `swarm.go` — that
file is 245/600 LOC and CLAUDE.md's NO GOD CLASS rule plus the package's own
`brief.go`/`swarm_depth.go`/`report.go` split precedent govern).

Define in it: `const JobTypeSwarmDelegation = "swarm_delegation"`; `type DelegationPayload
struct { Goal, Context, ConversationID, ParentRunID string; Depth int }` marshalled into the
existing `payload` jsonb column; `type SteerPublisher interface { Push(conv string, m
steer.Message) error }` declared HERE in `internal/swarm` (consuming-package interface seam —
the same shape as `swarmRunner` in `internal/agent/tools/swarm_spawn.go:9-18`, `SteerInbox` in
`internal/agent/llm_agent_steer.go:19-21`, and `ChannelDeliverer` in
`internal/cron/deliver.go:22-27`), never a new import edge into a concrete steer type;
`type DelegationEnqueuer` holding the ingestion-job store seam; `func EnqueueDelegation(ctx,
identityID string, goals []string, brief DelegationPayload) (string, error)` returning the
model-readable summary string; `type DelegationClaimLoop` with `ProcessOnce(ctx) (int, error)`
and `Run(ctx) error`.

`ProcessOnce` copies `IngestionJobWorker.ProcessOnce` verbatim in shape: claim via
`ClaimIngestionJobsRequest{IdentityID, WorkerID, LeaseDuration, BatchSize}`, assert
`job.IdentityID == w.IdentityID` and fail loudly on mismatch, then process each claimed job.
Filter on `job_type = JobTypeSwarmDelegation` at claim time; do NOT add swarm-specific columns
to `aura.ingestion_jobs` (D-01 keeps the generic queue generic — swarm shape lives in `payload`).

The per-job handler builds an `agent.LlmAgent` exactly as `runChild` does and MUST set
`ic := agent.InvocationContext{Ctx: gateway.WithDelegatedDispatch(ctx), RequestID:
uuid.Must(uuid.NewV7()), Budget: budget}` — omitting the marker reproduces the orphaned
reservation defect fixed in `791dcd7e0` (a false `crash-orphaned … indeterminate` stamp 30
minutes later, per RESEARCH.md Pitfall 2).

Lease/heartbeat: copy the `handleWithHeartbeat` + `maintainLease` shape (goroutine racing the
handler, `context.WithCancel`, buffered result channel) but move the tick SOURCE into the
worker's own `for ev, err := range worker.Run(ic)` event loop per D-03 — heartbeat on worker
events, not on a fixed ticker. Default lease `AURA_SWARM_DELEGATION_LEASE_SEC=300`, chosen
because the measured effective worst case is `SwarmChildTimeoutSec + LLMTotalTimeoutSec` =
240s, NOT the nominal 120s (D-03, spike 099). Add `SwarmDelegationLeaseSec` and
`SwarmDelegationPollSec` (default 2) to `internal/config/config.go` beside the three existing
swarm caps at lines 95-97, and add both names to `.env.example`.

On terminal success: marshal the child reports with the existing `marshalReports` and push ONE
`steer.Message{Source: steer.SourceWorker, Text: <consolidated report>}` for the row's
`conversation_id`, then `UpdateStatus` to `completed`. Any other `Source` value routes the
report through `wrapUserSteer` and the model discounts it as a spoofing attempt — spike 098
measured this in three live runs (RESEARCH.md Pitfall 5). On handler failure reuse
`recordHandlerFailure`'s branch verbatim: `Retry` with backoff below `max_attempts`,
`dead_letter` at the cap. Never write a second retry/backoff implementation.

In `internal/swarm/swarm.go`, add ONLY the branch: at `depth == 0` (top level) call
`EnqueueDelegation` and return the queued summary immediately; at `depth > 0` keep the current
fully synchronous `runWave` path untouched (SWARM-04's requirement, expanded in plan 51-05).
Everything else stays in `delegation_queue.go`. Apply DEEP REFACTOR ON TOUCH to `swarm.go` in
the same commit: dead code removed, comments updated, still under 600 LOC.

Write `internal/swarm/delegation_queue_test.go` with the `db_integration` build tag FIRST
(RED before GREEN), copying `internal/documents/integration_pool_helper_test.go`'s three-role
DSN bootstrap VERBATIM so the test runs as `aura_app`. Tests: `TestBackgroundDelegationEnqueues`,
`TestDelegationClaimReclaim` (real Go claim path under an expired lease — this closes spike
100's own stated gap that only a hand-built SQL SELECT was ever exercised),
`TestDelegationDeadLettersAtMaxAttempts`, and a table-free unit test for
`delegationIdempotencyKey` determinism plus the empty-goals rejection (both daemon-free, so
the 85% package floor holds without Postgres).
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/swarm/... &amp;&amp; go test ./internal/swarm/ -run 'TestDelegationIdempotencyKey|TestEnqueueDelegationEmptyGoals' &amp;&amp; go test -race ./internal/swarm/...</automated>
    <automated>go test -tags=db_integration ./internal/swarm/ -run 'TestBackgroundDelegationEnqueues|TestDelegationClaimReclaim|TestDelegationDeadLettersAtMaxAttempts' -v</automated>
  </verify>
  <acceptance_criteria>
    - `internal/swarm/delegation_queue.go` exists and contains `const JobTypeSwarmDelegation = "swarm_delegation"`
    - `internal/swarm/delegation_queue.go` contains `gateway.WithDelegatedDispatch(` — verify with `grep -v '^\s*//' internal/swarm/delegation_queue.go | grep -c 'gateway.WithDelegatedDispatch'` returning ≥1
    - `internal/swarm/delegation_queue.go` contains `steer.SourceWorker` and does NOT contain a literal `"swarm"` string used as a Source
    - No new SQL table is created: `ls internal/db/migrations/ | wc -l` is unchanged by this task
    - `grep -rn 'agent_message_send' internal/ cmd/` returns no matches
    - `wc -l internal/swarm/swarm.go` reports ≤ 600 and `wc -l internal/swarm/delegation_queue.go` reports ≤ 600
    - `go test -tags=db_integration ./internal/swarm/ -run TestDelegationClaimReclaim` exits 0 **executed with the `aura_app` DSN**, never the superuser `aura` role (`aura` is superuser+BYPASSRLS and yields a FALSE GREEN on identity scoping); the test asserts a row belonging to another `identity_id` is invisible to the claim
    - The `db_integration` tests take >1s of real runtime (a sub-second run is a skip tell); the skip helper calls `t.Fatal` when the DSN env is unset and `$CI` is set
    - `.env.example` contains `AURA_SWARM_DELEGATION_LEASE_SEC=` and `AURA_SWARM_DELEGATION_POLL_SEC=`
    - The configured default lease is ≥ 240 seconds
  </acceptance_criteria>
  <done>A top-level delegation writes durable rows, the real Go claim path claims and reclaims them correctly as `aura_app`, and the loop pushes the report under `steer.SourceWorker`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the claim loop into the daemon composition root</name>
  <files>cmd/aura/serve_delegation.go, cmd/aura/serve.go</files>
  <read_first>
    - `cmd/aura/serve_dispatch.go` (the `268580e23` wiring — the `ConversationRecorder` seam and how a background loop is started in this composition root)
    - `internal/cron` (`claim.go`, `heartbeat.go`, `recover.go`, `dispatch.go` — the resident-loop lifecycle idiom)
    - `internal/swarm/delegation_queue.go` (written in Task 1)
    - `51-RESEARCH.md` §`Risk Register` → "Not one-way, but worth flagging: the new claim-loop worker's placement"
  </read_first>
  <action>
Create `cmd/aura/serve_delegation.go` (new file, keeps `serve.go` off the 600-LOC ceiling) that
constructs the `swarm.DelegationClaimLoop` with the ingestion-job store already built for
documents, the identity id, `cfg.SwarmDelegationLeaseSec`, `cfg.SwarmDelegationPollSec`, and a
`swarm.SteerPublisher` adapter over the process's existing steer inbox instance — the SAME
instance the AG-UI steer route and the Telegram dispatch already push into, so a worker report
and an operator steer land in one queue (D-04/D-05: delivery is server-side, not session-bound).

Start the loop with the same lifecycle discipline the cron loop uses: a context cancelled at
daemon shutdown, an error path that logs and continues rather than killing the daemon, and
`slog` fields naming `job_type=swarm_delegation`. Per RESEARCH.md's Risk Register, the wiring
site is what determines whether the loop inherits `gateway.WithDelegatedDispatch` — assert in
review that the loop's own dispatch path carries it (Task 1 sets it inside the loop, this task
must not build a second construction path that bypasses it).

Do not add a cockpit-side polling loop or any client-tier delegation status surface: per
RESEARCH.md's Architectural Responsibility Map every capability in this phase is
daemon-process or Postgres tier, and the cockpit is a passive SSE consumer.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./cmd/aura/... &amp;&amp; go test ./cmd/aura/...</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/aura/serve_delegation.go` exists and contains `swarm.NewDelegationClaimLoop(`
    - `grep -v '^\s*//' cmd/aura/serve_delegation.go | grep -c 'SteerPublisher\|steerInbox'` returns ≥1
    - `wc -l cmd/aura/serve.go` reports ≤ 600
    - `go build ./...` exits 0 and `go vet ./...` reports nothing
    - The loop is started exactly once per daemon: `grep -c 'NewDelegationClaimLoop' cmd/aura/*.go` returns 1
  </acceptance_criteria>
  <done>The daemon starts with a resident delegation claim loop bound to the same steer inbox the cockpit and Telegram already use.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The tracer slice: a top-level delegation enqueues durable rows, a daemon-resident claim loop runs the workers, and the consolidated report re-enters the conversation under the worker envelope.</what-built>
  <how-to-verify>
    1. `docker compose build aura &amp;&amp; docker compose up -d` (the build is ~30s warm and includes the web bundle; never hot-swap the binary).
    2. Confirm the image is from current HEAD before trusting anything.
    3. Drive the REAL agent on the running stack with a driver script modelled on
       `.planning/spikes/098-steer-carries-worker-result/drive.sh` (login + `Idempotency-Key`
       header on `/api/conversations` and `/agent/run`). Ask for a delegation of 2 goals.
    4. Assert Aura's turn returns BEFORE the workers finish, then send a second operator
       message immediately and confirm it is accepted on the same thread.
    5. `psql` as `aura_app`: `SELECT job_type, status, attempt_count FROM aura.ingestion_jobs
       WHERE job_type='swarm_delegation' ORDER BY created_at DESC LIMIT 5;` — expect the rows
       to reach `completed`.
    6. `SELECT seq, role, left(content,120) FROM aura.conversation_turns WHERE conversation_id=
       '<conv>' ORDER BY seq;` — the consolidated worker report must be present.
    7. Reassemble the `TEXT_MESSAGE_CONTENT` deltas into the final message before judging
       whether the model USED the report — a raw `grep` on the SSE stream produced two false
       verdicts in spike 098 (RESEARCH.md Pitfall 4).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what the model did with the report</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker LLM output → parent turn context | attacker-influencable text (a worker summarising fetched/executed content) crosses into the parent's prompt |
| claim loop → Postgres (`aura.ingestion_jobs`) | rows carry `identity_id`; the loop runs daemon-side with a pooled role |

## STRIDE Threat Register (ASVS L1, block on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Spoofing / Elevation of Privilege | steer push in `delegation_queue.go` | high | mitigate | Push with `steer.SourceWorker` unconditionally so `markSteer` selects `wrapUntrustedToolOutput`; asserted in acceptance criteria (V5) |
| T-51-02 | Tampering / Information Disclosure | delegation rows in `aura.ingestion_jobs` | high | mitigate | Reuse `db.WithIdentityTx` scoping; `db_integration` test runs as `aura_app` and asserts a foreign `identity_id` row is invisible (V4) |
| T-51-03 | Repudiation | gateway reservation ledger | medium | mitigate | `gateway.WithDelegatedDispatch(ctx)` on every claim-loop `InvocationContext` so the opener closes its own row (V4) |
| T-51-04 | Denial of Service | claim loop retry storm | medium | mitigate | Reuse the shipped `attempt_count`/`max_attempts` + `next_attempt_at` backoff; no second retry implementation |
| T-51-05 | Tampering | a live worker reclaimed mid-flight | medium | mitigate | Lease ≥ 240s (D-03 measured effective bound) plus event-loop heartbeat |
| T-51-SC | Tampering | package installs | low | accept | This phase installs no external package (RESEARCH.md §Package Legitimacy Audit); any new dependency proposed at execution time triggers the full legitimacy gate |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./...`
- `go test -race ./internal/swarm/... ./internal/gateway/... ./internal/agent/...`
- `go test -tags=db_integration ./internal/swarm/...` executed as `aura_app`
- Live tracer verification (Task 3 checkpoint) on the running stack
</verification>

<success_criteria>
- SC#1's mechanical half is proven live: the turn returns immediately and the report lands in `aura.conversation_turns`
- Zero new tables, zero new messaging schema, zero second delivery mechanism
- Every file touched is ≤600 LOC with dead code removed in the same commit
</success_criteria>

<output>
Create `.planning/phases/51-durable-delegation/51-01-SUMMARY.md` when done
</output>
