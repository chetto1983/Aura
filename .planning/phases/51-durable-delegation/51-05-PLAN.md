---
phase: 51-durable-delegation
plan: 05
type: execute
wave: 3
depends_on: ["51-01", "51-03"]
files_modified:
  - internal/swarm/swarm.go
  - internal/swarm/swarm_depth.go
  - internal/swarm/swarm_test.go
  - internal/agent/swarm_context.go
  - internal/agent/idempotency_operation_test.go
autonomous: true
requirements: [SWARM-04, SWARM-05, SWARM-08]
must_haves:
  truths:
    - "A worker's registry contains swarm_spawn when its depth is below the configured cap, so a worker can orchestrate workers of its own"
    - "A delegation issued BY a worker runs synchronously — the worker's turn does not return until its own workers have reported"
    - "At the depth cap the behaviour is explicit and model-readable — the worker is told it cannot delegate further, never silently handed a tool that fails"
    - "A nested worker dispatches every tool in its flattened surface with zero operation-fingerprint denials (SWARM-08, guarding the 67d24aee4 fix under a NEW nesting path)"
    - statement: "SWARM-04 edge (unclassified/synchrony): a nested delegation that errors still returns inside the worker's own turn — the worker reports the failure, it does not leak into the parent as a missing result"
      verification: explicit
    - statement: "SWARM-05 edge (unclassified/boundary): depth is counted from the top-level call, so a worker at depth == cap-1 may delegate and its children at depth == cap may not"
      verification: explicit
    - statement: "SWARM-08 edge (unclassified/dispatch): the regression guard dispatches a tool through the gateway rather than inspecting registry membership — a membership-only assertion would have passed throughout the entire period the shipped defect existed"
      verification: explicit
  prohibitions:
    - "MUST NOT background a depth>0 delegation — an orchestrating worker needs its workers inside its own turn (SWARM-04)"
    - "MUST NOT key any new re-entry or passthrough guard on OperationScope alone — compare scope AND fingerprint"
    - "MUST NOT re-implement the depth guard: swarm_depth.go's checkDepth/maxDepth already exist and are tested"
    - "MUST NOT create a new test file for the SWARM-08 guard — TestDeriveToolOperationContextDerivesForNestedToolCall already exists and is extended"
  artifacts:
    - path: "internal/swarm/swarm_depth.go"
      provides: "depth-aware registry decision at the cap"
      contains: "checkDepth"
  key_links:
    - from: "internal/swarm/swarm.go (runChild)"
      to: "internal/agent/tools registry"
      via: "depth-conditional swarm_spawn availability"
      pattern: "tools.Without|swarmSpawnTool"
    - from: "internal/swarm/swarm.go"
      to: "internal/swarm delegation branch"
      via: "depth>0 stays on the synchronous runWave path"
      pattern: "Depth"
---

<objective>
Open the nesting the PRD designed and the implementation forecloses (**SWARM-05**), and keep a
worker-issued delegation synchronous (**SWARM-04**) — an orchestrating worker needs its own
workers' results inside its own turn.

The foreclosure is one line: `runChild` builds every worker with
`Registry: tools.Without(rc.ParentRegistry, swarmSpawnTool)` and the comment "flat v1 forbids
nested swarms". The depth machinery to replace it with ALREADY EXISTS and is already tested
(`swarm_depth.go`: `defaultMaxDepth`, `maxDepth()` reading `AURA_SWARM_MAX_DEPTH`, `checkDepth`;
`swarm_test.go:464` `TestSwarmDepthGuard`) — it has simply been unreachable because depth never
mattered beyond 1.

**SWARM-08 rides here, not as new build work.** RESEARCH.md marks it MOSTLY DONE: commit
`67d24aee4` fixed `deriveToolOperationContext` to key its re-entry guard on scope AND
fingerprint, re-measured at 0 denials, and the regression guard
`TestDeriveToolOperationContextDerivesForNestedToolCall` already exists
(`internal/agent/idempotency_operation_test.go:209`). What this plan owes SWARM-08 is proof
that the NEW nesting path this plan opens does not reintroduce the defect class — RESEARCH.md
is explicit that lifting the registry restriction reopens exactly it.

Purpose: SC#2. Output: depth-bounded nesting, a synchronous nested leg, and an extended
regression guard.
</objective>

## Artifacts this phase produces

- Functions: `swarm.workerRegistry(parent tools.Registry, depth int) tools.Registry`
- Constant: `swarm.nestingClosedNotice` (the model-readable at-cap message)
- Test names: `TestNestedDelegationSynchronous`, `TestSwarmDepthGuardAtCapIsModelReadable`,
  `TestDeriveToolOperationContextDerivesForDelegatedDispatch` (extension of the existing file)

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-durable-delegation/51-RESEARCH.md
@.planning/phases/51-durable-delegation/51-PATTERNS.md
@.planning/phases/51-durable-delegation/51-01-SUMMARY.md
@.planning/phases/51-durable-delegation/51-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Depth-bounded nesting — a worker may orchestrate (SWARM-05/SWARM-04)</name>
  <files>internal/swarm/swarm.go, internal/swarm/swarm_depth.go, internal/agent/swarm_context.go, internal/swarm/swarm_test.go</files>
  <read_first>
    - `internal/swarm/swarm.go` (245 LOC — `Run`, `preflight`, `runWave`, `runChild`; the `Registry: tools.Without(rc.ParentRegistry, swarmSpawnTool)` line and its "flat v1" comment)
    - `internal/swarm/swarm_depth.go` (38 LOC — `defaultMaxDepth`, `maxDepth()`, `checkDepth`)
    - `internal/swarm/swarm_test.go:464` (`TestSwarmDepthGuard` — extend, do not rewrite) and `:205-236` (`TestSwarmParallelTiming` for the concurrency test shape + `goleak` convention)
    - `internal/agent/swarm_context.go:20` (where the worker registry is derived)
    - `internal/swarm/delegation_queue.go` (from plan 51-01 — the depth==0 background branch this task must NOT extend to depth>0)
    - `D:/tmp/hermes-agent/tools/delegate_tool.py` — `role: leaf|orchestrator` with `max_spawn_depth`, degrading `orchestrator` to `leaf` past the cap (D-00 requires reading the reference before choosing)
    - `51-PATTERNS.md` §`internal/swarm/swarm.go` (the depth-guard degrade-vs-reject discussion)
  </read_first>
  <behavior>
    - `workerRegistry(parent, depth)` returns a registry CONTAINING `swarm_spawn` when `depth+1 < maxDepth()`, and one WITHOUT it at the cap.
    - At the cap the worker's brief states plainly that it cannot delegate further (hermes' degrade-to-leaf framing), rather than the worker discovering it by calling a tool that is absent.
    - A depth>0 `swarm.Run` executes the shipped `runWave` path and returns only after every child has reported — it never enqueues a durable row, never returns a "queued" summary.
    - A depth>0 delegation whose child fails returns the failure inside the worker's own report; the parent sees a completed worker, not a missing one.
    - `checkDepth` still rejects beyond the cap with its existing model-readable message; the existing `TestSwarmDepthGuard` continues to pass unmodified in its original assertions.
  </behavior>
  <action>
Write the failing tests first: `TestNestedDelegationSynchronous` (a depth-1 worker's `Run`
returns only after its children report — assert ordering with a fake child runner that records
completion times, run under `-race` with `goleak.VerifyNone`) and
`TestSwarmDepthGuardAtCapIsModelReadable`. Commit `test(51-05): add failing nested delegation
tests`.

Then replace the unconditional `tools.Without(rc.ParentRegistry, swarmSpawnTool)` with a
`workerRegistry(parent, depth)` helper living in `swarm_depth.go` (the depth concern's own
file — do NOT grow `swarm.go`, which is at 245/600 and already carries preflight, wave
concurrency and child lifecycle). Delete the now-false "flat v1 forbids nested swarms" comment
in the same commit (DEEP REFACTOR ON TOUCH: stale comments are a defect, not decoration).

Adopt hermes' degrade framing rather than a bare removal: at the cap the worker gets the
registry minus `swarm_spawn` AND a brief line saying so, so the model reads its own limit
instead of discovering it. `checkDepth`'s existing hard rejection stays as the second line of
defence for a call that arrives anyway.

In `swarm.Run`, gate the plan-51-01 background branch on `depth == 0` explicitly (it already is
— assert it with a test, because SWARM-04 is exactly the requirement that a nested delegation
must NOT be backgrounded). `AURA_SWARM_MAX_DEPTH` stays out of the Tier A/B operator knob
registry (`config_knobs_test.go:105` bans it there) while plan 51-03 renders it into the tool's
own model-facing schema — two different catalogs, per RESEARCH.md Open Question 1.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./internal/swarm/... &amp;&amp; go test -race ./internal/swarm/ -run 'TestNestedDelegationSynchronous|TestSwarmDepthGuard|TestSwarmParallelTiming' -v</automated>
    <automated>go test -race ./internal/swarm/... ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' internal/swarm/swarm.go | grep -c 'tools.Without(rc.ParentRegistry'` returns 0 (the unconditional restriction is gone)
    - `internal/swarm/swarm_depth.go` contains `func workerRegistry(`
    - `grep -rn 'flat v1 forbids nested swarms' internal/` returns no matches
    - `TestSwarmDepthGuard`'s original assertions are unchanged (`git diff internal/swarm/swarm_test.go` shows additions, not rewrites, in that test)
    - A test asserts a depth>0 `Run` writes ZERO rows with `job_type='swarm_delegation'` (fake enqueuer records calls)
    - `go test -race ./internal/swarm/` exits 0 with `goleak.VerifyNone` in every new test
    - `wc -l internal/swarm/swarm.go internal/swarm/swarm_depth.go` each report ≤ 600
  </acceptance_criteria>
  <done>A worker can orchestrate workers, bounded by the configured depth, and its delegation blocks inside its own turn.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the new nesting path does not reintroduce the fingerprint-collapse defect (SWARM-08)</name>
  <files>internal/agent/idempotency_operation_test.go</files>
  <read_first>
    - `internal/agent/idempotency_operation.go:41-60` (`deriveToolOperationContext` — the scope AND fingerprint guard fixed by `67d24aee4`)
    - `internal/agent/idempotency_operation_test.go:209` (`TestDeriveToolOperationContextDerivesForNestedToolCall` — the EXISTING guard whose shape to copy; do NOT create a new file for it)
    - `.planning/spikes/099-worker-duration-and-progress/README.md` (the 41/41-denials measurement and the 0-denial re-measurement)
    - `51-RESEARCH.md` §`Common Pitfalls` Pitfall 1 and §`Already In The Tree` SWARM-08 row
    - `51-PATTERNS.md` §`Already Covered — Do Not Re-plan`
  </read_first>
  <action>
**This task builds no production code.** RESEARCH.md marks SWARM-08 MOSTLY DONE and PATTERNS.md
confirms its regression guard already exists — re-implementing it would be exactly the
"planning work already in the tree" failure this phase has suffered once. What is genuinely new
is the code path plan 51-01 and Task 1 above added: a claim-loop-driven worker and a nested
worker, neither of which existed when `67d24aee4` was measured.

Extend `internal/agent/idempotency_operation_test.go` with
`TestDeriveToolOperationContextDerivesForDelegatedDispatch`, copying the existing test's shape
(build two operations sharing `OperationScopeAgent`, assert the derived child key differs).
Cover: a nested worker's own tool call inside a parent operation of the same scope, and a
claim-loop-dispatched worker's call. Per RESEARCH.md's Sampling Rate note, the assertion must
exercise DISPATCH, not registry membership — a membership-only test would have passed
throughout the entire period the shipped defect existed.

Record in the SUMMARY that SWARM-08 required zero production changes this phase, naming
`67d24aee4` as the fix and the extended test as the guard for the new paths.
  </action>
  <verify>
    <automated>go test ./internal/agent/ -run 'TestDeriveToolOperationContext' -v &amp;&amp; go test -race ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func TestDeriveToolOperationContext' internal/agent/idempotency_operation_test.go` returns ≥2 (the original plus the new one)
    - No new file named `*idempotency*_test.go` was created: `ls internal/agent/ | grep -c 'idempotency_operation_test.go'` returns 1
    - `git diff --stat internal/agent/idempotency_operation.go` shows zero lines changed (this task adds no production code)
    - The new test's assertion path calls the dispatch derivation, not a registry membership check
  </acceptance_criteria>
  <done>The nesting and claim-loop dispatch paths are pinned against the fingerprint-collapse defect class without re-implementing the shipped fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → tool surface | a nested worker now holds a delegation tool the flat design withheld |
| worker → budget | a nested fan-out multiplies spend against the shared `agent.Budget` |

## STRIDE Threat Register (ASVS L1, block on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-18 | Denial of Service | unbounded recursive spawning | high | mitigate | `AURA_SWARM_MAX_DEPTH` enforced by `checkDepth` AND by registry composition at the cap; the shipped `budgetReserve`/`TryReserve` reservation semantics are preserved on the nested leg |
| T-51-19 | Elevation of Privilege | a nested worker inheriting a wider surface than its parent | high | mitigate | `workerRegistry` derives from the PARENT's registry only, never from the root manifest — a child can never hold a tool its parent lacked |
| T-51-20 | Tampering | re-entry guard collapse on the new nesting path | high | mitigate | Extended `deriveToolOperationContext` guard test on both new dispatch paths (Task 2) |
| T-51-21 | Repudiation | nested worker tool calls orphaning gateway reservations | medium | mitigate | `gateway.WithDelegatedDispatch` already set by `runChild`; the nested leg reuses `runChild` unchanged |
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./... && go test -race ./internal/swarm/... ./internal/agent/... ./internal/gateway/...`
- Live nested run is verified in plan 51-08's SC#2 driver
</verification>

<success_criteria>
- SC#2's mechanical half proven by test: a nested delegation returns inside the worker's turn
- The registry restriction is depth-conditional, not removed wholesale
- SWARM-08 gained a guard for the new paths and zero re-implementation
</success_criteria>

<output>
Create `.planning/phases/51-durable-delegation/51-05-SUMMARY.md` when done
</output>
