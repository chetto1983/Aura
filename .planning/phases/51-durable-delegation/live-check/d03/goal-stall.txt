Usa swarm_spawn (caricalo con tool_search se serve) per delegare IN BACKGROUND un singolo goal a UN worker e rispondimi subito che il worker e' in coda, senza aspettare il risultato. Passa al worker ESATTAMENTE questo goal, parola per parola: "Come PRIMA e unica azione esegui shell_exec con il comando `sleep 480 && echo STALL-PROBE-DONE` passando ESPLICITAMENTE il parametro timeout_ms=590000 (e' obbligatorio: senza timeout_ms il comando verrebbe interrotto prima). Non fare nessun altro comando prima. Quando il comando termina riporta il suo output."
