---
phase: 23-frontend-infrastructure-industrial-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/tsconfig.json
  - web/tsconfig.node.json
  - web/eslint.config.js
  - web/.prettierrc.json
  - web/vitest.config.ts
  - web/playwright.config.ts
  - web/src/__tests__/AppShell.test.tsx
  - web/e2e/shell.spec.ts
  - web/.gitignore
  - internal/webui/embed.go
  - internal/webui/embed_test.go
  - internal/webui/doc.go
  - .gitattributes
autonomous: false
requirements: [FND-01, FND-02, FND-03]
must_haves:
  truths:
    - "The FND-01 decision record (23-RESEARCH.md) is reviewer-approved and locks linter/formatter/tokens/layout/pipeline/harness"
    - "The npm dependency tree installs from a committed package-lock.json with a clean slopcheck on the youngest package"
    - "The zero-warning gate configs (eslint flat config, prettier, tsconfig strict) exist and run"
    - "The component + E2E test stubs exist and FAIL red against the not-yet-built shell (Wave 0 honesty)"
    - "internal/webui exposes Sub() + Handler() over //go:embed all:dist and is a leaf package (imports neither agent nor agui)"
  artifacts:
    - path: "web/eslint.config.js"
      provides: "ESLint 10 flat config — typescript-eslint strict + react-hooks recommended-latest + jsx-a11y, prettier LAST"
      contains: "eslint-config-prettier"
    - path: "web/package.json"
      provides: "Scripts: dev/build/lint/format/format:check/typecheck/test/test:e2e; lint carries --max-warnings=0"
      contains: "--max-warnings=0"
    - path: "internal/webui/embed.go"
      provides: "//go:embed all:dist + Sub() + Handler() static placeholder host"
      contains: "all:dist"
    - path: "internal/webui/embed_test.go"
      provides: "httptest coverage of Handler() — index.html 200 text/html, asset resolves, missing 404"
      contains: "httptest"
    - path: "web/e2e/shell.spec.ts"
      provides: "Playwright stub asserting brand + theme-before-paint + no-hero-text (red until 23-02/23-03)"
      contains: "data-theme"
  key_links:
    - from: "internal/webui/embed.go"
      to: "web/dist"
      via: "//go:embed all:dist + fs.Sub"
      pattern: "go:embed all:dist"
    - from: "web/eslint.config.js"
      to: "eslint-config-prettier"
      via: "prettier layered LAST in the flat config array"
      pattern: "prettier"
---

<objective>
Establish the Phase-23 Wave-0 foundation: confirm the FND-01 locked decision record is approved, install the research-pinned npm toolchain behind a supply-chain checkpoint, lay down the zero-warning gate configs (ESLint flat config + Prettier + strict tsconfig), write the validating test stubs (Vitest/RTL + Playwright + Go embed httptest) so they exist RED before the scaffold builds them green, and create the `internal/webui` Go embed package + its test against `//go:embed all:dist`.

Purpose: Per Nyquist validation, the gate configs and failing tests must exist before scaffold tasks assert against them. This plan is the "Wave 0 Requirements" from 23-VALIDATION.md — configs and tests first, implementation in 23-02/23-03.
Output: `web/` config + test stubs, `internal/webui/*.go` embed package + test, committed `package-lock.json`, `.gitignore`/`.gitattributes` wiring. The decision record is signed off.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-PATTERNS.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-VALIDATION.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: FND-01 decision-record sign-off + supply-chain legitimacy gate</name>
  <read_first>
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md (the FND-01 deliverable; §Decision Corrections, §Standard Stack, §Package Legitimacy Audit)
  </read_first>
  <what-built>
    Nothing is built yet — this is the SC1 doc-gate (FND-01) plus the package-legitimacy gate that RESEARCH.md flagged as required before the first npm install. RESEARCH.md ran slopcheck 0.6.1 (24 OK / 1 SUS); the single SUS is `@rolldown/plugin-babel` (youngest package, age heuristic only, individually [OK], official Rolldown org). `eslint-plugin-import-x` is [ASSUMED] — not version-pinned/slopchecked yet.
  </what-built>
  <how-to-verify>
    1. Read `23-RESEARCH.md` and confirm it locks ALL six FND-01 dimensions: linter ruleset (typescript-eslint strict flat config, Correction #1), formatter (Prettier, layered last), design-token architecture (tokens.json + tiny generator + Tailwind 4 @theme, D-08), `web/` layout (single npm package), build+release pipeline (committed dist + freshness gate + Node-24 Docker), and the test harness (Vitest+RTL+Playwright). Confirm the three Decision Corrections (D-02 dead-airbnb, D-12 @rolldown/plugin-babel, D-02 react-compiler-lint→react-hooks) read as intent-preserving.
    2. Verify `@rolldown/plugin-babel` on https://www.npmjs.com/package/@rolldown/plugin-babel (official Rolldown org, react.dev-recommended path). Approve or flag.
    3. Run `slopcheck scan --pkg npm eslint-plugin-import-x` and `npm view eslint-plugin-import-x version` (the only non-version-verified package). If it does not cleanly resolve, the implementer drops it (A-IMPORTX says import-order lint is non-critical to the gate).
    4. Re-verify the fast-moving versions per RESEARCH "Valid until ~2026-07-16": `npm view vite version && npm view @vitejs/plugin-react version && npm view babel-plugin-react-compiler version && npm view tailwindcss version`. Note any drift; the implementer pins the verified current versions.
  </how-to-verify>
  <acceptance_criteria>
    Operator confirms the decision record is approved AND the package legitimacy gate passed (`@rolldown/plugin-babel` verified on npmjs.com, `eslint-plugin-import-x` slopchecked-or-dropped). No [SLOP] package proceeds. This checkpoint is never auto-approvable.
  </acceptance_criteria>
  <action>Gate task (no code). Read 23-RESEARCH.md and confirm it is the locked FND-01 decision record covering all six dimensions (linter/formatter/tokens/layout/pipeline/harness) with the three intent-preserving Decision Corrections. Run the package-legitimacy gate before any npm install: verify `@rolldown/plugin-babel` on npmjs.com (the youngest/SUS package), `slopcheck scan --pkg npm eslint-plugin-import-x` + `npm view eslint-plugin-import-x version` (verify-or-drop), and re-check the fast-moving versions (`npm view vite|@vitejs/plugin-react|babel-plugin-react-compiler|tailwindcss version`). Record approval + any version pins/drops so Task 2 installs the vetted set. This is a blocking-human checkpoint — never auto-approved (legitimacy gates ignore auto_advance).</action>
  <verify>Operator types "approved" after confirming the decision record is locked and the legitimacy gate passed (no [SLOP], `@rolldown/plugin-babel` verified, `eslint-plugin-import-x` verified-or-dropped, versions re-checked).</verify>
  <done>The FND-01 decision record is signed off and the npm dependency set is supply-chain-vetted with any version drift pinned/recorded; Task 2 may install.</done>
  <resume-signal>Type "approved" (record locked + packages verified) or list packages to drop/swap.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install pinned npm toolchain + zero-warning gate configs</name>
  <files>web/package.json, web/package-lock.json, web/tsconfig.json, web/tsconfig.node.json, web/eslint.config.js, web/.prettierrc.json, web/vitest.config.ts, web/playwright.config.ts, web/.gitignore, .gitattributes</files>
  <read_first>
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md §Scaffold Blueprint (package.json scripts lines 230-247; tsconfig strictness lines 249-266; ESLint flat-config shape lines 302-329; Prettier config lines 331-334; Playwright config lines 491-514)
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-PATTERNS.md §"No Analog Found" (greenfield rows — follow RESEARCH verbatim)
    - .claude/skills/vite/SKILL.md (Vite 8 Rolldown/Oxc; ESM-only, vite.config.ts)
    - .claude/skills/vercel-react-best-practices/SKILL.md
  </read_first>
  <action>
    Create the `web/` single npm package and install the RESEARCH §Standard Stack dependencies (the `npm install -D …` block, RESEARCH lines 144-154) at the versions verified in Task 1; commit the generated `package-lock.json` (the reproducibility anchor, D-05/D-06). Author `package.json` with EXACTLY the script names from RESEARCH lines 230-247 ("type":"module"; dev/build/preview/lint/format/format:check/typecheck/test/test:e2e); `lint` MUST carry `--max-warnings=0` (D-03 golangci-lint parity); `build` runs `node tokens/generate-theme.mjs && tsc -b && vite build` (token gen before bundle). Write `tsconfig.json` with the strict block from RESEARCH lines 249-266 (strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, verbatimModuleSyntax, noEmit) + `tsconfig.node.json` for config/scripts. Write `eslint.config.js` as the Correction #1 FLAT config (RESEARCH lines 302-329): `js.configs.recommended` + `tseslint.configs.strictTypeChecked` + `stylisticTypeChecked` + `eslint-plugin-react-hooks` recommended-latest (this carries the React-Compiler rules per Correction #3) + `jsx-a11y` + `react-refresh`, with `eslint-config-prettier` as the LAST array element (D-02 integration constraint). Do NOT install or reference `eslint-config-airbnb-typescript`, `@kesills/...`, `FlatCompat`, `@eslint/eslintrc`, or the standalone `eslint-plugin-react-compiler` (RESEARCH Corrections #1/#3, Pitfall 2). Add `eslint-plugin-import-x` flat preset only if Task 1 verified it. Write `.prettierrc.json` (RESEARCH lines 331-334). Write `vitest.config.ts` (jsdom env + setup, RESEARCH §Validation). Write `playwright.config.ts` Chromium-only with `webServer` booting `../aura serve --only=cli`, `baseURL` http://127.0.0.1:9080, health-gate on `/healthz`, `reuseExistingServer: !process.env.CI`, and an explicit `env` block (Pitfall 6: webServer does NOT inherit CI env) — leave the real DB env wiring to the 23-03 CI job, but include the `env` key so it is obvious where serve's required vars go. Add `web/.gitignore` for `node_modules/`, `coverage/`, `playwright-report/`, `test-results/` (NOT `dist/` — dist is committed, D-05). Add a root `.gitattributes` enforcing `web/** text eol=lf` (Pitfall 9: cross-host CRLF drift breaks the freshness gate).
  </action>
  <verify>
    <automated>cd web && npx eslint --version && npx prettier --version && npx tsc --noEmit -p tsconfig.json 2>&1 | head -5 && test -f package-lock.json && grep -q -- '--max-warnings=0' package.json && grep -q 'prettier' eslint.config.js</automated>
  </verify>
  <done>`web/package.json` has all SC3 script names with `--max-warnings=0` on `lint`; `package-lock.json` is committed; `eslint.config.js` is a flat config with prettier LAST and NO airbnb/FlatCompat/standalone-react-compiler imports; `tsconfig.json` is strict; `playwright.config.ts` boots `../aura serve` Chromium-only with an explicit `env` block; `.gitattributes` enforces LF for `web/**`.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 RED test stubs (Vitest/RTL + Playwright + Go embed package & httptest)</name>
  <files>web/src/__tests__/AppShell.test.tsx, web/e2e/shell.spec.ts, internal/webui/embed.go, internal/webui/doc.go, internal/webui/embed_test.go</files>
  <read_first>
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md §Go Embed Integration (lines 336-367 — the embed.go shape) + §Validation §Test Map (SC2/SC4 rows lines 562-564)
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-PATTERNS.md §"internal/webui/embed.go" (Analog 1 embed.FS from internal/skills/builtin.go; Analog 2 Mux from internal/agui/server.go) + §"internal/webui/embed_test.go" (the readiness_test.go subtest/httptest idiom)
    - internal/agui/readiness_test.go (the t.Run + http.Get + status/body-assert idiom — copy stdlib testing, NO testify)
    - internal/skills/builtin.go (the //go:embed + var X embed.FS + named-root-const convention)
    - .claude/skills/golang-testing/SKILL.md, .claude/skills/golang-structs-interfaces/SKILL.md
  </read_first>
  <action>
    Write `internal/webui/embed.go` per RESEARCH lines 336-367: package `webui`, `//go:embed all:dist var distFS embed.FS` (the `all:` prefix is load-bearing — bare `dist` drops Workbox dotfiles/SW, Pitfall 7), `Sub() (fs.FS, error)` returning `fs.Sub(distFS, "dist")`, and `Handler() (http.Handler, error)` returning `http.FileServerFS(sub)`. Add `internal/webui/doc.go` (or a package doc atop embed.go) stating it is the Phase-23 static placeholder host and that the SPA catch-all / index.html-fallback / API-route-exclusion is DELIBERATELY Phase 24 (WEB-01) — do NOT add fallback logic here (RESEARCH lines 357/366; PATTERNS §package-doc convention). Keep the package LEAF-level: import only stdlib (`embed`, `io/fs`, `net/http`) — neither `internal/agent` nor `internal/agui` — so `scripts/agui_boundary_check.sh` stays green (RESEARCH line 367). To let `//go:embed all:dist` compile before 23-02 produces real bytes, create a placeholder `web/dist/index.html` (a minimal `<!doctype html>` with the dark `theme-color` + a `data-theme="dark"` root so the embed test passes; 23-02 overwrites it with the real Vite build). Write `internal/webui/embed_test.go` mirroring `internal/agui/readiness_test.go`: `t.Run` subtests with `httptest.NewServer(h)` (or `httptest.NewRecorder` + `h.ServeHTTP`), `defer resp.Body.Close()`, `io.ReadAll`+`strings.Contains` — assert GET `/` returns 200 + `Content-Type: text/html` and a body containing the brand/`data-theme`, and GET a nonexistent path returns 404. NO testify (the agui/webui HTTP surface uses stdlib `testing`). Write the two FRONTEND stubs as RED-until-scaffold: `web/src/__tests__/AppShell.test.tsx` (RTL — imports `../AppShell`, asserts `getByRole('img', { name: /aura/i })` present AND asserts NO text matching a marketing-hero blocklist; will fail to import until 23-02 creates `AppShell.tsx`) and `web/e2e/shell.spec.ts` (Playwright — asserts `data-theme`/`data-density` are set on `<html>` of the first response HTML, brand visible, Copy-Contract: no marketing-hero text; will fail until 23-03 wires serve+embed). Do not weaken these assertions later — fix the code, never the test (CLAUDE.md).
  </action>
  <verify>
    <automated>go vet ./internal/webui/ && go build ./internal/webui/ && go test ./internal/webui/ -count=1 && bash scripts/agui_boundary_check.sh && bash scripts/check-file-size.sh</automated>
  </verify>
  <done>`internal/webui` builds, `go test ./internal/webui/` is GREEN (embed handler serves the placeholder index.html 200 text/html + 404 on missing), the package imports no `agent`/`agui` (boundary gate green), all files ≤600 LOC. The two frontend test stubs exist and are RED (AppShell.tsx + the real served shell do not exist yet) — that redness is the Wave-0 contract 23-02/23-03 turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → web/ dependency tree | Untrusted third-party package code enters the build toolchain (supply-chain) |
| committed web/dist bytes → //go:embed → aura serve | Static asset bytes baked into the binary, served to the browser |
| build host (WSL/Windows) ↔ CI/Docker → committed dist | Reproducibility boundary: divergent output forges the freshness gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-SC | Tampering | npm install of the new dependency tree | mitigate | Task 1 supply-chain checkpoint: slopcheck pass + npmjs.com verify on `@rolldown/plugin-babel` (the SUS/youngest) + verify-or-drop `eslint-plugin-import-x`; committed `package-lock.json` pins exact resolved versions; `npm ci` (not install) in CI/Docker (23-03) |
| T-23-01 | Tampering | committed `web/dist` vs a fresh rebuild | mitigate | The freshness gate (`git diff --exit-code -- web/dist/`, wired in 23-03) is the tamper-evidence control; this plan lays the LF `.gitattributes` + committed lockfile that make the rebuild byte-stable (Pitfall 3/9) |
| T-23-02 | Information Disclosure | `http.FileServerFS` serving from `embed.FS` | accept | Path traversal is precluded by `http.FileServerFS` over `embed.FS` (no `..` escape from the embedded tree); noted, no extra control needed |
| T-23-DEFER-AUTH | Spoofing/Elevation | the embedded shell mounts at `/` on loopback | accept (deferred → Phase 24) | Web AUTH (GAP-2), the non-loopback boot guard, and SPA route-exclusion are OUT OF SCOPE here (WEB-02/WEB-03). The placeholder mounts additively on loopback only; the existing `aura serve` bind is hardcoded loopback (serve.go:221). HIGH-severity web-exposure threats are explicitly Phase-24 deferred, not introduced by this static placeholder. |
</threat_model>

<verification>
- SC1 (FND-01): Task 1 checkpoint — reviewer signs off `23-RESEARCH.md` as the locked decision record.
- SC3 (FND-03 configs): `cd web && npx tsc --noEmit` runs; `eslint.config.js` flat config present with `--max-warnings=0` in `package.json`; prettier last.
- SC2 (FND-02 embed): `go test ./internal/webui/` green — `//go:embed all:dist` → `Handler()` serves the placeholder index.html (200, text/html); 404 on missing.
- Wave-0 honesty: `web/src/__tests__/AppShell.test.tsx` + `web/e2e/shell.spec.ts` exist RED (assert the not-yet-built shell).
- `bash scripts/agui_boundary_check.sh` green (webui is leaf); `bash scripts/check-file-size.sh` green.
</verification>

<success_criteria>
- The FND-01 decision record is reviewer-approved (SC1).
- The npm toolchain is installed from a committed `package-lock.json` behind a passed supply-chain checkpoint.
- The zero-warning gate configs exist and run (`eslint.config.js`, `.prettierrc.json`, strict `tsconfig.json`).
- `internal/webui` exposes `Sub()`+`Handler()` over `//go:embed all:dist`, is leaf-level, and its httptest is green.
- The frontend Vitest + Playwright stubs exist RED, pinned to the SC2/SC4 contract.
</success_criteria>

<output>
Create `.planning/phases/23-frontend-infrastructure-industrial-foundation/23-01-SUMMARY.md` when done.
</output>
</invoke>
