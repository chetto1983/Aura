---
phase: 23-frontend-infrastructure-industrial-foundation
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - cmd/aura/serve.go
  - cmd/aura/serve_webui.go
  - cmd/aura/serve_webui_test.go
  - .github/workflows/ci.yml
  - docker/aura/Dockerfile
  - Makefile
  - scripts/web_dist_freshness.sh
autonomous: true
requirements: [FND-02, FND-03, FND-06]
must_haves:
  truths:
    - "aura serve mounts the embedded webui.Handler at / additively, AG-UI routes keep priority, the placeholder shell renders from the single binary"
    - "Four path-filtered web CI jobs (web-lint, web-test, web-dist-freshness, web-e2e) run blocking on every PR in the existing ci.yml"
    - "The Node-24 webbuild Docker stage rebuilds dist reproducibly; no Node-for-SPA reaches the runtime image"
    - "The Playwright E2E boots aura serve over a docker-compose Postgres and asserts theme-before-paint + brand + no-hero-text"
    - "The web-dist-freshness gate fails on a non-empty git diff -- web/dist/"
  artifacts:
    - path: "cmd/aura/serve_webui.go"
      provides: "Parent-mux wiring delegating non-AG-UI paths to webui.Handler() (additive mount, no SPA-fallback)"
      contains: "webui"
    - path: ".github/workflows/ci.yml"
      provides: "web-lint, web-test, web-dist-freshness, web-e2e jobs (path-filtered, Node 24)"
      contains: "web-e2e"
    - path: "docker/aura/Dockerfile"
      provides: "node:24-bookworm-slim AS webbuild stage feeding COPY --from=webbuild into the go build stage"
      contains: "webbuild"
    - path: "scripts/web_dist_freshness.sh"
      provides: "npm ci + npm run build + git diff --exit-code -- dist/ stale-artifact guard"
      contains: "git diff --exit-code"
  key_links:
    - from: "cmd/aura/serve.go"
      to: "internal/webui.Handler"
      via: "parent mux delegating / to the embedded static handler"
      pattern: "webui\\."
    - from: ".github/workflows/ci.yml web-e2e"
      to: "aura serve + docker-compose postgres"
      via: "make db-up + composed DSN env passed to Playwright webServer"
      pattern: "AURA_DB_URL"
    - from: "docker/aura/Dockerfile webbuild"
      to: "go build stage web/dist"
      via: "COPY --from=webbuild /web/dist ./web/dist"
      pattern: "COPY --from=webbuild"
---

<objective>
Wire the embedded SPA into the running daemon and the quality pipeline: mount `webui.Handler()` additively at `/` in `aura serve` (AG-UI routes keep priority; NO SPA-fallback — that is Phase 24), add the four path-filtered web CI jobs to the existing `ci.yml` (lint/test/freshness/e2e, blocking, every PR), add the Node-24 `webbuild` Docker stage (no Node-for-SPA in runtime), and add the `web-dist-freshness` gate script. This turns the 23-01 Playwright E2E stub GREEN (boots `aura serve`, asserts the branded dark-operator shell with theme-before-paint and no hero text).

Purpose: FND-02 (serve renders the embedded shell), FND-03 (zero-warning blocking CI gate, golangci-lint parity), FND-06 (Node-24 Docker build + E2E harness green in CI).
Output: the additive serve mount + its httptest, four web CI jobs, the webbuild Docker stage, the freshness gate script + Makefile target.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-PATTERNS.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-CONTEXT.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-01-SUMMARY.md
@.planning/phases/23-frontend-infrastructure-industrial-foundation/23-02-SUMMARY.md
@cmd/aura/serve.go
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additively mount webui.Handler() into aura serve (no SPA-fallback) + httptest</name>
  <files>cmd/aura/serve.go, cmd/aura/serve_webui.go, cmd/aura/serve_webui_test.go</files>
  <read_first>
    - cmd/aura/serve.go (bootServe lines 191-272 — where httpSrv is built with aguiServer.Mux() as Handler; this is the single mount surface, lines 245-249)
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md §Go Embed Integration (mount point lines 366; Open Question 2 lines 665-668 — minimal additive parent mux, NOT a Server.Mux() refactor)
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-PATTERNS.md §"internal/webui/embed.go" Mount point (serve.go:222-249) + the Mux() route list (AG-UI routes keep priority)
    - internal/agui/server.go (Mux() — the exact AG-UI route set: /healthz /readyz /debug/vars /metrics /agent/run /threads/{id}/messages)
    - internal/webui/embed.go (the Handler() from 23-01)
  </read_first>
  <action>
    Add `cmd/aura/serve_webui.go`: a small parent-mux constructor that returns an `http.Handler` delegating the AG-UI route set to `aguiServer.Mux()` and EVERYTHING ELSE (`/`, `/assets/*`, `/manifest.webmanifest`, `/sw.js`, icons) to `webui.Handler()` (RESEARCH Open Question 2 recommendation — minimal additive mount). The cleanest shape: a `http.NewServeMux` that registers the explicit AG-UI prefixes (`/healthz`, `/readyz`, `/debug/vars`, `/metrics`, `/agent/run`, `/threads/`) to the AG-UI mux and registers `/` (the catch-all) to the embedded static handler — so AG-UI routes keep priority (exact/longer patterns win under Go 1.22 ServeMux precedence) and the static handler serves the placeholder at `/`. Do NOT implement index.html-fallback for unknown client routes and do NOT exclude API 404s from the SPA — that real SPA-fallback/route-exclusion logic is DELIBERATELY Phase 24 (WEB-01); a missing asset under `/` simply 404s from `http.FileServerFS`, which is correct for a static placeholder. In `cmd/aura/serve.go` bootServe, change `httpSrv.Handler` from `aguiServer.Mux()` to the new parent handler (e.g. `newServeHandler(aguiServer)` calling into `webui.Handler()`). If `webui.Handler()` errors (embed sub failure), fail boot with a named error (it should never error with a committed dist). Keep `internal/webui` leaf-level (cmd/aura imports it; webui imports neither agent nor agui — boundary gate stays green). Write `cmd/aura/serve_webui_test.go` (stdlib testing + httptest, NO testify): assert GET `/` returns 200 + `text/html` + the brand/`data-theme` body (the embedded shell), GET `/healthz` still routes to the AG-UI handler (priority preserved), and a GET to a bogus asset path 404s. Use a constructed parent handler with a real or fake AG-UI mux so the precedence assertion is genuine.
  </action>
  <verify>
    <automated>go vet ./cmd/aura/ && go build ./cmd/aura/ && go test ./cmd/aura/ -run TestServeWebui -count=1 && bash scripts/agui_boundary_check.sh && bash scripts/check-file-size.sh</automated>
  </verify>
  <done>`aura serve`'s `httpSrv.Handler` is the parent mux that serves the embedded placeholder shell at `/` while AG-UI routes (`/healthz`, `/readyz`, `/agent/run`, `/threads/...`, `/metrics`, `/debug/vars`) keep priority. `cmd/aura/serve_webui_test.go` proves `/` → 200 text/html shell, `/healthz` → AG-UI handler, bogus path → 404. No SPA-fallback logic added (Phase 24). Boundary + file-size gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Four path-filtered web CI jobs + web-dist-freshness gate script + Makefile target</name>
  <files>.github/workflows/ci.yml, scripts/web_dist_freshness.sh, Makefile</files>
  <read_first>
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md §CI + Docker (the 4 jobs lines 472-489; path-filter lines 482-489; Playwright CI-env caveat lines 513) + §Validation Wave-0 gaps
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-PATTERNS.md §"ci.yml — 4 new web jobs" (job-shape skeleton, job-with-service-stack idiom, path-filter, zero-warning teeth)
    - .github/workflows/ci.yml (the existing job shape lines 26-45; the integration-test job lines 166-228 — the docker-compose Postgres + composed DSN env + `make db-up` + `aura db migrate` + the AG-UI gateway live smoke that boots `aura serve` over the same stack, lines 230-243; the web-integration-test service-stack idiom lines 289-341)
    - scripts/agui_boundary_check.sh (the `set -euo pipefail` + exit-code-contract script header to mirror)
    - web/playwright.config.ts (the webServer env block from 23-01 — this job fills its required DB env)
    - Makefile (the coverage: → scripts/*.sh target idiom, lines 62-91)
  </read_first>
  <action>
    Add `scripts/web_dist_freshness.sh` mirroring `scripts/agui_boundary_check.sh`'s header (`#!/usr/bin/env bash`, `set -euo pipefail`, a human-readable failure line + explicit exit codes): `cd web && npm ci && npm run build && git diff --exit-code -- dist/ || { echo "web/dist is stale — run 'npm run build' in web/ and commit" >&2; exit 1; }`. Add a `Makefile` `web-freshness:` target shelling to it. Add FOUR jobs to `.github/workflows/ci.yml` (all `runs-on: ubuntu-latest`, `actions/checkout@v6`, `actions/setup-node@v4` with `node-version: 24` + `cache: npm`), each guarded by a `dorny/paths-filter@v3` step (filters `web/**` + `.github/workflows/ci.yml`) so Go-only PRs no-op them green (D-17, one unified workflow — NOT a separate web.yml):
      1. `web-lint` — `cd web && npm ci && npm run lint && npm run format:check && npm run typecheck` (the D-03 zero-warning gate; `--max-warnings=0` is in the `lint` script).
      2. `web-test` — `cd web && npm ci && npm run test` (Vitest + coverage, FND-06).
      3. `web-dist-freshness` — checkout with `fetch-depth: 0`, then `bash scripts/web_dist_freshness.sh` (D-05 stale guard; pins committed dist to a fresh Node-24 build).
      4. `web-e2e` — RESOLVES A-SERVE-NOSTACK: `aura serve` hard-requires a Postgres pool (serve.go bootServe → bootServeChatEnv → bootChatEnvWithConfig → db.Open, confirmed during planning). So this job MUST provision the stack exactly like the existing AG-UI live smoke (ci.yml lines 166-243): export the integration-test env block (`POSTGRES_*`, `CI: "true"`, the composed `AURA_DB_URL`/`AURA_DB_MIGRATE_URL`, `NEO4J_PASSWORD`, and a DUMMY non-empty `OPENROUTER_API_KEY` so config.LoadServe boots — the placeholder shell needs no real key), `make db-up`, `go run ./cmd/aura db migrate`, then build dist (`cd web && npm ci && npm run build`), `go build -o aura ./cmd/aura` (binary with the freshly-embedded dist), `npx playwright install --with-deps chromium`, and `cd web && npm run test:e2e`. CRITICAL (Pitfall 6): Playwright's `webServer` does NOT inherit the runner env — pass the DB env (`AURA_DB_URL`/`AURA_DB_MIGRATE_URL`/`OPENROUTER_API_KEY`) explicitly into `webServer.env` in `web/playwright.config.ts` (update it here to read from `process.env`) so `aura serve` boots inside the Playwright lifecycle. Tear down with `if: always(): docker compose down`. All four jobs are blocking on every PR (D-16).
  </action>
  <verify>
    <automated>bash -n scripts/web_dist_freshness.sh && grep -q 'git diff --exit-code' scripts/web_dist_freshness.sh && grep -q 'web-e2e' .github/workflows/ci.yml && grep -q 'web-dist-freshness' .github/workflows/ci.yml && grep -q 'node-version' .github/workflows/ci.yml && grep -q 'AURA_DB_URL' .github/workflows/ci.yml && grep -q 'web-freshness' Makefile && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <done>`.github/workflows/ci.yml` has the four path-filtered Node-24 web jobs (`web-lint`, `web-test`, `web-dist-freshness`, `web-e2e`); `web-e2e` brings up a docker-compose Postgres + migrates + boots `aura serve` with the composed DSN env passed explicitly into Playwright's `webServer.env` (A-SERVE-NOSTACK resolved via the AG-UI-live-smoke pattern); `scripts/web_dist_freshness.sh` is the `git diff --exit-code` stale guard with a `Makefile web-freshness` target; the YAML parses.</done>
</task>

<task type="auto">
  <name>Task 3: Node-24 webbuild Docker stage (no Node-for-SPA in runtime) + Docker smoke</name>
  <files>docker/aura/Dockerfile</files>
  <read_first>
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-RESEARCH.md §"Node-24 multi-stage Docker" (the exact stage lines 516-541; the runtime-Node-is-for-recipe-MCP distinction line 540)
    - .planning/phases/23-frontend-infrastructure-industrial-foundation/23-PATTERNS.md §"Dockerfile — webbuild stage" (the existing COPY --from idiom)
    - docker/aura/Dockerfile (the existing 2-stage build: golang-alpine `build` → debian-slim runtime; runtime installs Node 24 for recipe MCP servers, lines 25-30)
  </read_first>
  <action>
    Add a `webbuild` stage to `docker/aura/Dockerfile` BEFORE the existing `build` stage (RESEARCH lines 518-525): `FROM node:24-bookworm-slim AS webbuild`, `WORKDIR /web`, `COPY web/package.json web/package-lock.json ./`, `RUN npm ci`, `COPY web/ ./`, `RUN npm run build` (produces `/web/dist`). In the existing `build` stage, after `COPY . .`, add `COPY --from=webbuild /web/dist ./web/dist` to overwrite the committed dist with a fresh reproducible build (ties D-05↔D-06; the `web-dist-freshness` CI job proves the committed bytes equal a fresh build). The `webbuild` Node never reaches the runtime image (multi-stage discards it) — satisfies D-06 "no Node in the runtime layer for the SPA". Add a Dockerfile comment documenting that the runtime stage's existing Node-24 (lines 25-30) is ORTHOGONAL — it exists only for recipe MCP servers (mail-mcp), NOT the SPA, which is fully baked into the Go binary before runtime (RESEARCH line 540 — so a reviewer does not read runtime-Node as a D-06 violation). Do not otherwise change the runtime stage.
  </action>
  <verify>
    <automated>grep -q 'FROM node:24-bookworm-slim AS webbuild' docker/aura/Dockerfile && grep -q 'COPY --from=webbuild /web/dist' docker/aura/Dockerfile && grep -q 'RUN npm run build' docker/aura/Dockerfile</automated>
  </verify>
  <done>`docker/aura/Dockerfile` has the `node:24-bookworm-slim AS webbuild` stage building `/web/dist`, fed into the go `build` stage via `COPY --from=webbuild /web/dist ./web/dist`; the webbuild Node never lands in runtime; a comment documents the runtime Node-24 is for recipe MCP servers, not the SPA (D-06). (Full `docker build` smoke runs in CI / operator Gate-3 — SC5.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → aura serve / (loopback) | The embedded static handler serves bytes from embed.FS over the loopback HTTP server |
| committed web/dist ↔ CI/Docker fresh rebuild | Reproducibility / tamper-evidence boundary (freshness gate) |
| CI runner → Playwright webServer (aura serve boot) | The E2E boots the real binary over a provisioned DB stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Information Disclosure | `http.FileServerFS` over `embed.FS` at `/` | accept | Path traversal is precluded by `http.FileServerFS` over `embed.FS` (no `..` escape from the embedded subtree); the static handler exposes only the dist tree. Noted, no extra control. |
| T-23-01 | Tampering | committed `web/dist` vs the CI/Docker fresh rebuild | mitigate | `web-dist-freshness` (`git diff --exit-code -- dist/`) is the tamper-evidence control — a hand-edited or stale dist reds CI; the Docker `COPY --from=webbuild` rebuilds reproducibly so the released image can never diverge from a fresh build (D-05↔D-06). |
| T-23-06 | Denial of Service | unauthenticated loopback HTTP server | accept (existing posture) | The serve bind is hardcoded loopback (serve.go:221, amendment #35); `aguiReadHeaderTimeout` already defangs slow-loris (serve.go:49, T-12-09). The embed mount is additive and adds no new listener. |
| T-23-DEFER-AUTH | Spoofing/Elevation | web exposure beyond loopback (auth, non-loopback boot guard, SPA route-exclusion) | accept (deferred → Phase 24, BLOCK-ON-HIGH flagged) | These HIGH-severity web-exposure controls (WEB-02 non-loopback boot guard, WEB-03 web auth + signed session cookie, WEB-01 SPA route-exclusion) are OUT OF SCOPE for this foundation phase and are explicitly Phase-24 deliverables. Phase 23 mounts the placeholder additively on loopback ONLY; it does not open a non-loopback bind or any mutating route. Flagged as a deferred high-severity item, not a regression introduced here. |
| T-23-SC | Tampering | `npm ci` in CI/Docker (supply chain) | mitigate | `npm ci` (not `npm install`) installs exactly the committed `package-lock.json`; the 23-01 legitimacy checkpoint already vetted the tree. |
</threat_model>

<verification>
- SC2 (FND-02): the 23-01 Playwright E2E (`web/e2e/shell.spec.ts`) turns GREEN — `aura serve` (booted by `webServer` over the provisioned Postgres) serves the embedded shell; `data-theme`/`data-density` set on `<html>` of the first response (theme-before-paint), brand visible, no hero text.
- SC3 (FND-03): `web-lint` job = `npm run lint` (`--max-warnings=0`) + `format:check` + `typecheck`, blocking on every PR (golangci-lint parity).
- SC5 (FND-06): the `webbuild` Docker stage builds dist; the runtime image has no Node-for-SPA; all four web jobs run green in CI; `web-dist-freshness` ties committed↔fresh dist.
- A-SERVE-NOSTACK resolved: `web-e2e` provisions Postgres (`make db-up` + migrate) and passes the composed DSN env into Playwright's `webServer.env` — confirmed against the serve.go boot path (db.Open is unconditional).
- `bash scripts/agui_boundary_check.sh` green (webui stays leaf); `bash scripts/check-file-size.sh` green.
</verification>

<success_criteria>
- `aura serve` serves the branded placeholder shell embedded via `//go:embed all:dist` at `/`, AG-UI routes keep priority, no SPA-fallback added (SC2).
- Four path-filtered, Node-24, blocking web CI jobs are in the existing `ci.yml` (SC3/SC5).
- The Node-24 `webbuild` Docker stage rebuilds dist reproducibly with no Node-for-SPA in the runtime image (SC5).
- The Playwright E2E boots `aura serve` over a provisioned Postgres stack and asserts the dark-operator shell (theme-before-paint, brand, no hero text) — A-SERVE-NOSTACK resolved.
- The `web-dist-freshness` gate fails on a non-empty `git diff -- web/dist/`.
</success_criteria>

<output>
Create `.planning/phases/23-frontend-infrastructure-industrial-foundation/23-03-SUMMARY.md` when done.
</output>
