---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 05
type: tdd
wave: 2
depends_on: ["32-02"]
files_modified:
  - internal/neostore/neostore.go
  - internal/neostore/neostore_test.go
  - internal/reasoningstore/store.go
  - internal/toolselectstore/store.go
  - internal/activelearn/learner.go
  - internal/db/numeric.go
  - internal/db/db_unit_test.go
  - internal/conversations/store_helpers.go
  - internal/cachemetrics/store_helpers.go
  - internal/envutil/envutil.go
  - internal/envutil/envutil_test.go
  - internal/config/config_env.go
  - internal/config/config_test.go
  - internal/channels/registry.go
  - internal/channels/telegram/config.go
  - scripts/coverage_gate.sh
autonomous: true
requirements: [QUAL-03]
user_setup: []

must_haves:
  truths:
    - "internal/neostore exports HashText, AsString, AsFloats, and the GraphClient interface; reasoningstore/toolselectstore/activelearn use neostore.* and their local copies are deleted."
    - "internal/db exports NumericFromFloat/FloatFromNumeric (+ DefaultNumericMaxCost); conversations and cachemetrics use them and their local numericFromFloat/floatFromNumeric copies are deleted."
    - "internal/envutil exports IntDefault/BoolDefault; config/channels/telegram use them and the 3 self-documented copies are deleted."
    - "Each extraction has a characterization/parity table test that was GREEN against the OLD duplicated code before the move, and is GREEN against the extracted helper after (union of inputs both copies handled, D-09/D-10)."
    - "internal/neostore and internal/envutil each independently clear the ≥85% coverage floor; scripts/coverage_gate.sh includes the 3 new packages (D-13)."
  artifacts:
    - path: "internal/neostore/neostore.go"
      provides: "HashText/AsString/AsFloats/GraphClient (canonical Neo4j store-helper leaf)"
      exports: ["HashText", "AsString", "AsFloats", "GraphClient"]
    - path: "internal/db/numeric.go"
      provides: "NumericFromFloat/FloatFromNumeric (pgtype.Numeric ↔ float, numeric(10,4))"
      exports: ["NumericFromFloat", "FloatFromNumeric"]
    - path: "internal/envutil/envutil.go"
      provides: "IntDefault/BoolDefault env-parse-with-fallback leaf"
      exports: ["IntDefault", "BoolDefault"]
  key_links:
    - from: "internal/reasoningstore/store.go"
      to: "internal/neostore"
      via: "neostore.HashText/AsString/AsFloats/GraphClient"
      pattern: "neostore\\."
    - from: "internal/conversations/store_helpers.go"
      to: "internal/db.NumericFromFloat"
      via: "shared numeric helper"
      pattern: "db\\.NumericFromFloat"
  prohibitions:
    - "Do NOT assert numeric parity on the error STRING — the two copies differ ('+/-%.4f' vs '±%v'); assert the pgtype.Numeric value (Int+Exp) + err presence (Pitfall 5)."
    - "Do NOT expand envutil beyond the 3 named copies — moving agent-tool knob reads to construction time is QUAL-04 (Phase 33/34, OUT). Mechanical os.Getenv→envutil swap only."
    - "Do NOT introduce an import cycle or any agui back-edge — all three are stdlib/pg leaves."
    - "Do NOT lose the AsFloats nil-vs-empty distinction ('[]' string and []any{} cases)."
---

<objective>
Extract the three pure-leaf duplicated helper families to canonical homes (QUAL-03), each test-first
per D-09/D-10 (parity table GREEN against the OLD copies, then move, then GREEN again):
(1) `internal/neostore` (canonical, D-06) ← `HashText`/`AsString`/`AsFloats`/`GraphClient`, with 3
byte-identical copies across `reasoningstore`/`toolselectstore`/`activelearn`;
(2) `internal/db.NumericFromFloat`/`FloatFromNumeric` (Open Question #1 default — Postgres home, NOT
neostore) ← the near-identical `numericFromFloat`/`floatFromNumeric` in `conversations`/`cachemetrics`
(only the error *string* differs);
(3) `internal/envutil` (minimal, D-06) ← `IntDefault`/`BoolDefault`, with 3 self-documented copies in
`config`/`channels`/`telegram`.

Purpose: ROADMAP C2 — kill cross-package duplication so phases 33+ reuse clean shared packages.
Output: 2 new leaf packages + 1 new file in `internal/db`, all call sites migrated, parity tests, and
D-13 coverage-gate confirmation (each new package ≥85%).

D-14: execute sequentially, no git worktrees. D-11: one commit per extraction (neostore; db numeric;
envutil) following the test-first cycle. The `gsd` commit wrapper times out on the file-size hook — use
direct `git commit`. All verify commands run in WSL (never run .exe on the Windows host). Follow the
`internal/secret/envkey.go` + `internal/canonicaljson` leaf-package template (32-PATTERNS § Shared
Pattern A) and the table-test + goleak idiom (Shared Pattern B).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@internal/canonicaljson/canonicaljson.go
@internal/secret/envkey.go
@internal/reasoningstore/store.go
@internal/config/config_env.go
</context>

## Artifacts this phase produces (this plan)

- `internal/neostore/neostore.go` (+ `_test.go`) — NEW: `HashText`, `AsString`, `AsFloats`, `GraphClient`.
- `internal/db/numeric.go` — NEW file in existing pkg: `NumericFromFloat`, `FloatFromNumeric`, `DefaultNumericMaxCost`.
- `internal/envutil/envutil.go` (+ `_test.go`) — NEW: `IntDefault`, `BoolDefault` (optionally `StringDefault`/`SliceDefault`).
- `scripts/coverage_gate.sh` — confirmed/edited so neostore/envutil/agentrender are included (D-13).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract internal/neostore (HashText/AsString/AsFloats/GraphClient) test-first</name>
  <files>internal/neostore/neostore.go, internal/neostore/neostore_test.go, internal/reasoningstore/store.go, internal/toolselectstore/store.go, internal/activelearn/learner.go</files>
  <read_first>
    - internal/reasoningstore/store.go:19-22 (GraphClient) + :80-121 (hashText/asString/asFloats)
    - internal/toolselectstore/store.go:22-25,115-156 (the verbatim twins; hash named hashQuery)
    - internal/activelearn/learner.go:113-116 (3rd HashText copy)
    - internal/canonicaljson/canonicaljson.go (leaf shape) + internal/secret/envkey.go (doc style)
    - 32-RESEARCH.md § Validation Architecture (neostore union tables: HashText/AsString/AsFloats incl. nil-vs-empty)
  </read_first>
  <behavior>
    - HashText: ""/"a"/unicode/long → deterministic sha256→hex; all 3 old copies == new for every input.
    - AsString: string→itself; int/nil/[]any/map→"" ; old==new.
    - AsFloats: APOC JSON string "[-0.02,0.5]"→[]float64; malformed string→nil; []any{float64}→ok; []any{int64,int}→converted; []any{string}→nil; nil→nil; "[]"→empty; []any{}→empty (capture exact nil-vs-empty); old==new for all.
  </behavior>
  <action>
    FIRST (D-10): write `neostore_test.go` table tests feeding the union-of-inputs to BOTH the old copies
    (reasoningstore + toolselectstore + activelearn helpers) AND the about-to-be-created neostore funcs,
    asserting `oldA(x)==oldB(x)==New(x)`; confirm GREEN against the old code. THEN create
    `internal/neostore/neostore.go` (stdlib-only leaf: crypto/sha256, encoding/hex, encoding/json,
    context) exporting `HashText`, `AsString`, `AsFloats`, and the `GraphClient` interface (Read/Write),
    with the Shared-Pattern-A package doc explaining the 3-copy origin. Migrate `reasoningstore` +
    `toolselectstore` to delete their local `hashText`/`hashQuery`/`asString`/`asFloats`/`GraphClient` and
    reference `neostore.*`; `activelearn` imports `neostore.HashText`. `*knowledge.Client` already satisfies
    `neostore.GraphClient` structurally — no change at the satisfying type. Confirm GREEN again.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race -cover ./internal/neostore/ && go build ./internal/reasoningstore/ ./internal/toolselectstore/ ./internal/activelearn/ && deadcode -test ./internal/reasoningstore/ ./internal/toolselectstore/ ./internal/activelearn/ 2>/dev/null | grep -E 'hashText|hashQuery|asString|asFloats' ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `internal/neostore/neostore.go` contains `func HashText(`, `func AsString(`, `func AsFloats(`, `type GraphClient interface`.
    - `go test -race -cover ./internal/neostore/` exits 0 and reports ≥85% coverage.
    - `rg -n 'func (hashText|hashQuery|asString|asFloats)' internal/reasoningstore/ internal/toolselectstore/` returns nothing (copies deleted).
    - The AsFloats test covers the nil-vs-empty distinction ("[]" and []any{}).
  </acceptance_criteria>
  <done>neostore is the canonical home; all 3 call-site packages use it; parity test green pre- and post-move; package ≥85%.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract internal/db.NumericFromFloat/FloatFromNumeric test-first (Open Question #1 home)</name>
  <files>internal/db/numeric.go, internal/db/db_unit_test.go, internal/conversations/store_helpers.go, internal/cachemetrics/store_helpers.go</files>
  <read_first>
    - internal/conversations/store_helpers.go:148-177 (numericFromFloat err "+/-%.4f"; numericMaxCost @:21) + floatFromNumeric
    - internal/cachemetrics/store_helpers.go:53-97 (numericFromFloat err "±%v"; numericMaxCost @:65) + floatFromNumeric (byte-identical)
    - 32-PATTERNS.md § "internal/db — ADD NumericFromFloat/FloatFromNumeric"; 32-RESEARCH.md numeric union table + Pitfall 5
  </read_first>
  <behavior>
    - NumericFromFloat: 0; ±1.2345; rounding 0.12345→0.1235; boundary ±999999.9999; over-range 1e9→err; NaN/±Inf→err; tiny 0.00001.
    - Round-trip: FloatFromNumeric(NumericFromFloat(f)) ≈ f within ±1e-4 for in-range f.
    - Parity: assert the returned pgtype.Numeric (Int + Exp) AND err-presence equal across both old copies and the new helper — NOT the error string (it differs).
  </behavior>
  <action>
    FIRST: write the parity table in `internal/db/db_unit_test.go` exercising both old copies + the new
    helper, asserting pgtype.Numeric(Int,Exp) + err-presence equality (Pitfall 5: never the message);
    confirm GREEN. THEN create `internal/db/numeric.go` exporting `NumericFromFloat(f float64) (pgtype.Numeric, error)`,
    `FloatFromNumeric(n pgtype.Numeric) float64`, and `DefaultNumericMaxCost` (999999.9999), preserving the
    identical numeric(10,4) logic (NaN/Inf/range guard → ×1e4 → round half-away-from-zero → big.Int
    mantissa, Exp:-4). Migrate `conversations` + `cachemetrics` to `db.NumericFromFloat`/`db.FloatFromNumeric`
    and delete their local copies + the duplicated `numericMaxCost`. Confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/db/ && go build ./internal/conversations/ ./internal/cachemetrics/ && rg -n 'func numericFromFloat|func floatFromNumeric' internal/conversations/ internal/cachemetrics/ ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `internal/db/numeric.go` contains `func NumericFromFloat(` and `func FloatFromNumeric(`.
    - `go test -race ./internal/db/` exits 0 with the numeric parity table (asserting value+err-presence, not string).
    - `rg -n 'func numericFromFloat|func floatFromNumeric' internal/conversations/ internal/cachemetrics/` returns nothing.
  </acceptance_criteria>
  <done>The numeric helpers live in internal/db (the Postgres home), both callers migrated, copies deleted, parity table green; Open Question #1 default applied and noted in the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract internal/envutil (IntDefault/BoolDefault) test-first + confirm D-13 coverage gate</name>
  <files>internal/envutil/envutil.go, internal/envutil/envutil_test.go, internal/config/config_env.go, internal/config/config_test.go, internal/channels/registry.go, internal/channels/telegram/config.go, scripts/coverage_gate.sh</files>
  <read_first>
    - internal/config/config_env.go:28-54 (canonical envIntDefault/envBoolDefault; also StringDefault :17-22, SliceDefault :60-73)
    - internal/channels/telegram/config.go:56-66 (envIntDefault copy) + internal/channels/registry.go:162-173 (envChannelEnabled bool-default wrapper)
    - internal/config/config_test.go:536-578 (the OLD-copy t.Setenv tests — feed their union to both old + new)
    - scripts/coverage_gate.sh (exclude patterns; confirm internal/neostore|envutil|agentrender are NOT excluded)
    - 32-RESEARCH.md § envutil extraction + scope boundary; 32-PATTERNS § envutil
  </read_first>
  <behavior>
    - IntDefault: "42"→42; ""→fallback; "not-a-number"→fallback (never fatal).
    - BoolDefault: "true"/"1"→true; ""→fallback; garbage→fallback.
    - Parity: old config.envIntDefault/envBoolDefault == new envutil.IntDefault/BoolDefault for the union of inputs.
  </behavior>
  <action>
    FIRST: write `internal/envutil/envutil_test.go` with the `t.Setenv` table (valid/empty/garbage) and a
    parity assertion against the OLD `config.envIntDefault`/`envBoolDefault`; confirm GREEN. THEN create
    `internal/envutil/envutil.go` (stdlib leaf: os, strconv, strings) exporting `IntDefault(key string,
    fallback int) int` and `BoolDefault(key string, fallback bool) bool` (Shared-Pattern-A doc). Migrate:
    `config_env.go` helpers become thin `envutil.*` calls (or delete + call directly); `telegram/config.go`
    drops its copy → `envutil.IntDefault`; `channels/registry.go` `envChannelEnabled` keeps its key-building
    but delegates the parse → `envutil.BoolDefault(key, true)`. KEEP envutil minimal — do NOT pull agent-tool
    knob reads to construction time (that is QUAL-04 → Phase 33/34, OUT). Optionally mirror `StringDefault`/
    `SliceDefault` only if it removes a copy. Then CONFIRM D-13: verify `scripts/coverage_gate.sh` does NOT
    exclude `internal/neostore`/`internal/envutil`/`internal/agentrender` (they auto-include under
    `internal/`); if any exclude pattern catches them, edit the script to include them. Confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race -cover ./internal/envutil/ && go build ./internal/config/ ./internal/channels/ ./internal/channels/telegram/ && rg -n 'func envIntDefault' internal/channels/telegram/config.go ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `internal/envutil/envutil.go` contains `func IntDefault(` and `func BoolDefault(`.
    - `go test -race -cover ./internal/envutil/` exits 0 and reports ≥85% coverage.
    - `rg -n 'func envIntDefault' internal/channels/telegram/config.go` returns nothing (copy deleted).
    - `scripts/coverage_gate.sh` includes (does not exclude) neostore/envutil/agentrender — confirmed or edited.
  </acceptance_criteria>
  <done>envutil is the canonical env-parse leaf, the 3 copies are gone, parity test green, scope kept minimal, and the D-13 coverage-gate inclusion is confirmed for all 3 new packages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Behavior-preserving extraction of internal helpers to leaf packages — no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05-PAR | Tampering | helper behavior drift on merge | mitigate | Test-first parity tables (union of inputs both copies handled) GREEN against old before the move (D-09/D-10). |
| T-32-05-NUM | Tampering | numeric(10,4) scale/round | mitigate | Assert pgtype.Numeric(Int,Exp)+err-presence, not the error string (Pitfall 5); round-trip ±1e-4. |
| T-32-05-CYC | Tampering | import cycle / agui back-edge | mitigate | neostore/envutil are stdlib leaves; db is existing; no agui import (Shared Pattern C). |
| T-32-05-HASH | (crypto note) | neostore.HashText sha256 | accept | sha256 content-MERGE key (not a password) — leave as-is (V6 note). |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go test -race -cover ./internal/neostore/ ./internal/envutil/` each ≥85%; `go test -race ./internal/db/` green (WSL).
- All migrated callers build; no local copies remain (rg checks empty).
- `scripts/coverage_gate.sh` confirmed to include the 3 new packages (D-13).
- `go vet $(bash scripts/go_packages.sh)` + `deadcode -test` clean for touched packages.
</verification>

<success_criteria>
ROADMAP C2 (partial): neostore, db numeric, and envutil are extracted with a parity test per extraction
(GREEN against the old copies first), all call sites migrated, copies deleted, no import cycle, and each
new package independently ≥85% with coverage-gate registration confirmed.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-05-SUMMARY.md` when done.
</output>
