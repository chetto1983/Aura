---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/settings/settings.go
  - internal/settings/settings_test.go
  - web/src/settings/ModelSettingsPanel.tsx
  - web/src/i18n/resources.ts
  - compose.yaml
  - .env.example
  - internal/webui/dist
autonomous: true
requirements: [QUAL-02]
user_setup: []

must_haves:
  truths:
    - "AURA_MEMORY_EMBED_BASE_URL and AURA_MEMORY_EMBED_API_KEY are removed from internal/settings AllowedKeys; OverlayEnv silently ignores any stale aura.settings DB rows (no migration)."
    - "The two keys are removed from the frontend SettingsKey union, the BACKEND_SETTINGS array, and BOTH en and it i18n resources (symmetric — i18n parity test stays green)."
    - "compose.yaml and .env.example document that AURA_MEMORY_EMBED_* are sidecar-owned compose/.env vars (consumed by the agent-memory sidecar at container start, NOT runtime-overridable via the cockpit)."
    - "internal/webui/dist is rebuilt and committed so web-dist-freshness stays green."
  artifacts:
    - path: "internal/settings/settings.go"
      provides: "AllowedKeys without the 2 sidecar-owned memory-embed keys"
    - path: "web/src/settings/ModelSettingsPanel.tsx"
      provides: "SettingsKey union + BACKEND_SETTINGS without the 2 keys"
  key_links:
    - from: "compose.yaml:405-406"
      to: "agent-memory sidecar OPENAI_API_KEY/OPENAI_BASE_URL"
      via: "container-start env mapping (the daemon's os.Setenv cannot reach a running sidecar)"
      pattern: "AURA_MEMORY_EMBED_"
  prohibitions:
    - "Do NOT log the value of AURA_MEMORY_EMBED_API_KEY on removal (it is Secret:true); secret.IsSecretEnvKey redaction must remain unaffected."
    - "Do NOT add a DB migration — OverlayEnv's allowlist guard ignores unlisted rows (Runtime State Inventory)."
    - "Do NOT remove the compose/.env variable names — they remain valid sidecar inputs; only the cockpit overlay control is removed."
    - "Do NOT remove the i18n keys from en only — remove from en AND it symmetrically (Pitfall 6)."
---

<objective>
Remove the two `AURA_MEMORY_EMBED_*` cockpit settings keys that are silent no-ops: nothing in the
daemon reads `os.Getenv("AURA_MEMORY_EMBED_*")`; `compose.yaml:405-406` maps them to the agent-memory
sidecar's `OPENAI_API_KEY`/`OPENAI_BASE_URL` at container start, which the daemon's `OverlayEnv`
`os.Setenv` cannot reach (D-05: remove-from-Go + document). RESEARCH proved this is a FULL-STACK change
(Go AllowedKeys ×2, frontend type union + BACKEND_SETTINGS array, i18n en+it labelKeys, compose/.env
doc, and a `internal/webui/dist` rebuild) — not the audit's "one-liner".

Purpose: ROADMAP C1 — remove the phantom in-process knobs while preserving the sidecar's real
compose/.env variables and the Secret-redaction guard.
Output: keys removed across Go + web + i18n, the sidecar ownership documented, dist rebuilt+committed.

D-14: execute sequentially, no git worktrees. D-11: split into the Go commit and the web+docs+dist
commit (the dist rebuild rides with the web commit). The `gsd` commit wrapper times out on the file-size
hook — fall back to direct `git commit`. All Go/web verify commands run in WSL (never run .exe on the
Windows host). Before `make coverage`/web tests, unset `.env`'s `AURA_WEB_AUTH_SECRET` (MEMORY).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@internal/settings/settings.go
</context>

## Artifacts this phase produces (this plan)

- (modified, no new symbol) `internal/settings/settings.go` — AllowedKeys minus 2 entries.
- (modified, no new symbol) `web/src/settings/ModelSettingsPanel.tsx`, `web/src/i18n/resources.ts` — frontend type/array/i18n trimmed.
- (modified) `compose.yaml`, `.env.example` — sidecar-ownership documentation.
- (rebuilt artifact) `internal/webui/dist` — committed bundle reflecting the web change.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove AURA_MEMORY_EMBED_* from Go settings AllowedKeys (no migration, no value logging)</name>
  <files>internal/settings/settings.go, internal/settings/settings_test.go</files>
  <read_first>
    - internal/settings/settings.go:46-126 (AllowedKeys map; the 2 entries @:61-62; OverlayEnv allowlist guard `if _, ok := AllowedKeys[r.Key]; !ok { continue }`)
    - 32-RESEARCH.md § "AURA_MEMORY_EMBED_* Removal Map" steps 1 + security note; Runtime State Inventory
  </read_first>
  <behavior>
    - Test: AllowedKeys no longer contains "AURA_MEMORY_EMBED_BASE_URL" or "AURA_MEMORY_EMBED_API_KEY".
    - Test: OverlayEnv given a stale `aura.settings` row for a now-unlisted key is a no-op (the row is silently skipped) — no os.Setenv for it, no error.
    - Test (guard): secret.IsSecretEnvKey behavior is unchanged for other *_KEY/*_SECRET keys.
  </behavior>
  <action>
    Delete the two `AURA_MEMORY_EMBED_BASE_URL` and `AURA_MEMORY_EMBED_API_KEY` entries from
    `settings.AllowedKeys` (`:61-62`). Do NOT log the removed values (the API key is `Secret:true`). Confirm
    `secret.IsSecretEnvKey` redaction is unaffected (no change to that predicate). Extend
    `settings_test.go` to assert (a) both keys are absent from AllowedKeys and (b) OverlayEnv silently
    ignores a stale row for an unlisted key. No DB migration is added.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/settings/ && rg -n 'AURA_MEMORY_EMBED' internal/settings/settings.go ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'AURA_MEMORY_EMBED' internal/settings/settings.go` returns nothing.
    - `go test -race ./internal/settings/` exits 0 with the new AllowedKeys + OverlayEnv-skip assertions.
    - No migration file was added; no value is logged on removal.
  </acceptance_criteria>
  <done>The two memory-embed keys are gone from Go settings with tests proving the allowlist guard ignores stale rows and the Secret guard is intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the keys from frontend (union + BACKEND_SETTINGS + i18n en/it), document sidecar ownership, rebuild dist</name>
  <files>web/src/settings/ModelSettingsPanel.tsx, web/src/i18n/resources.ts, compose.yaml, .env.example, internal/webui/dist</files>
  <read_first>
    - web/src/settings/ModelSettingsPanel.tsx:39-40 (SettingsKey union) + :102-112 (BACKEND_SETTINGS entries)
    - web/src/i18n/resources.ts (settings.fields.memoryEmbedBaseUrl + settings.fields.memoryEmbedKey in BOTH en and it)
    - compose.yaml:402-406 (the sidecar env mapping)
    - 32-RESEARCH.md § Removal Map steps 2-6 + Pitfall 3 (dist) + Pitfall 6 (i18n symmetry)
  </read_first>
  <behavior>
    - Test (web): the rendered settings panel no longer offers a control for either memory-embed key.
    - Test (web, i18n parity): the en and it resource trees have identical key sets after removal (no orphaned key in one locale).
  </behavior>
  <action>
    Remove `'AURA_MEMORY_EMBED_BASE_URL' | 'AURA_MEMORY_EMBED_API_KEY'` from the `SettingsKey` union
    (`ModelSettingsPanel.tsx:39-40`); remove the 2 `BACKEND_SETTINGS` entries (`:102-112`); remove
    `settings.fields.memoryEmbedBaseUrl` and `settings.fields.memoryEmbedKey` from BOTH `en` and `it` in
    `resources.ts` (symmetric — Pitfall 6). Add a comment near `compose.yaml:402-406` and an entry/comment
    in `.env.example` stating `AURA_MEMORY_EMBED_BASE_URL`/`_API_KEY` remain valid compose/.env variables
    consumed by the agent-memory sidecar at container start and are NOT runtime-overridable via the cockpit.
    Then rebuild the frontend (`cd web && npm run build`) and commit the updated `internal/webui/dist`
    (Pitfall 3 — the `web-dist-freshness` CI job diffs it).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura/web && npm test && npm run lint && npm run build && cd /mnt/d/Aura && git status --porcelain internal/webui/dist | head</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'AURA_MEMORY_EMBED|memoryEmbed' web/src/` returns nothing.
    - `cd web && npm test && npm run lint && npm run build` all exit 0 (i18n parity test green).
    - `compose.yaml` and `.env.example` document the sidecar ownership of the keys.
    - `internal/webui/dist` is rebuilt and staged for commit (no drift after build).
  </acceptance_criteria>
  <done>The keys are removed full-stack (web + i18n) with parity test green, sidecar ownership documented, and dist rebuilt + committed so web-dist-freshness stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cockpit → daemon settings overlay | Operator-supplied settings keys cross into the daemon; the removed keys were a silent no-op overlay. |
| daemon → agent-memory sidecar | The sidecar reads AURA_MEMORY_EMBED_* from compose/.env at container start (unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04-SEC | Information Disclosure | AURA_MEMORY_EMBED_API_KEY (Secret:true) removal | mitigate | Do not log the value on removal; leave secret.IsSecretEnvKey redaction untouched (V6). |
| T-32-04-CFG | Tampering | sidecar config continuity | mitigate | Keep the compose/.env variable names; document ownership so operators don't think the knob was deleted (only the no-op cockpit control is). |
| T-32-04-DIST | Tampering | internal/webui/dist freshness | mitigate | Rebuild + commit dist in the same commit (Pitfall 3) so the served bundle matches source. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go test -race ./internal/settings/` exits 0; `rg AURA_MEMORY_EMBED internal/settings/settings.go` empty.
- `cd web && npm test && npm run lint && npm run build` exit 0 (i18n parity green); `rg memoryEmbed web/src/` empty.
- `internal/webui/dist` rebuilt + committed; compose/.env documented.
</verification>

<success_criteria>
ROADMAP C1 (partial): the sidecar-owned `AURA_MEMORY_EMBED_*` cockpit keys are removed full-stack
(Go + web + i18n), the real compose/.env sidecar variables are preserved + documented, the Secret guard
is intact, and the dist bundle is fresh.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-04-SUMMARY.md` when done.
</output>
