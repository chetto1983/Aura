---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/channels/deps.go
  - internal/channels/doc.go
  - internal/agent/llm_agent.go
  - internal/agent/llm_agent_test.go
  - go.mod
autonomous: true
requirements: [QUAL-02]
user_setup: []

must_haves:
  truths:
    - "The redundant telebot blank import in internal/channels/deps.go is removed; go mod tidy keeps gopkg.in/telebot.v4 in the DIRECT require block (via the genuine consumer telegram/bot.go); go build stays green."
    - "internal/agent/llm_agent.go:235 no longer runs both Build() and BuildWithReasoningTier() — exactly one request builder runs per branch, and the chosen request is byte-identical to the old chosen request."
  artifacts:
    - path: "internal/channels/deps.go"
      provides: "telebot anchor removed (file deleted or reduced to package doc), executor's call per Open Question #4"
    - path: "internal/agent/llm_agent.go"
      provides: "Branch-selected request builder (no discarded Build())"
      contains: "BuildWithReasoningTier"
  key_links:
    - from: "internal/channels/telegram/bot.go:18"
      to: "go.mod direct require gopkg.in/telebot.v4"
      via: "genuine tele import keeps the pin DIRECT after the anchor is removed"
      pattern: "gopkg.in/telebot.v4"
  prohibitions:
    - "Do NOT remove the genuine telebot import in internal/channels/telegram/bot.go."
    - "Do NOT change request output: the branch-selected req must be byte-identical to the old chosen req per branch (no behavior change)."
---

<objective>
Resolve the two remaining QUAL-02 redundant-code items: (1) the telebot blank import in
`internal/channels/deps.go:37` — its in-code "amendment-#58 CI pin gate" justification is FALSE
(RESEARCH grepped `.github/`, `scripts/`, `Makefile`: zero telebot references); the genuine consumer
`telegram/bot.go:18` keeps `go.mod:38` DIRECT regardless, so the anchor is safe to remove after a
`go mod tidy` check. (2) the discarded `Build()` at `internal/agent/llm_agent.go:235` — when
`adaptiveTierOK`, both `Build(...)` and `BuildWithReasoningTier(...)` run and the first is thrown away
(a wasted `RenderToolDefs()` per turn); restructure to an if/else so only one builder runs.

Purpose: ROADMAP C1 — kill redundant code without changing behavior.
Output: a removed telebot anchor (build + tidy green) and a restructured request-builder with a
branch-parity test.

D-14: execute sequentially, no git worktrees. D-11: one commit per item (telebot removal; Build
restructure). The `gsd` commit wrapper times out on the file-size hook — fall back to direct
`git commit`. All verify commands run in WSL (never run .exe on the Windows host).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@internal/channels/deps.go
@internal/agent/llm_agent.go
</context>

## Artifacts this phase produces (this plan)

- `internal/channels/doc.go` — (only if the executor moves the package doc here per Open Question #4) holds the `package channels` doc comment after `deps.go` is deleted.
- (modified, no new symbol) `internal/agent/llm_agent.go` — branch-selected request builder.
- `internal/agent/llm_agent_test.go` — extended with the branch-parity assertion.

<tasks>

<task type="auto">
  <name>Task 1: Remove the redundant telebot blank import (go mod tidy must keep it DIRECT)</name>
  <files>internal/channels/deps.go, internal/channels/doc.go, go.mod</files>
  <read_first>
    - internal/channels/deps.go (the package-doc comment + the lone `_ "gopkg.in/telebot.v4"` blank import @:37; note x/image and qrterminal anchors were already removed)
    - internal/channels/telegram/bot.go:18 (genuine `tele "gopkg.in/telebot.v4"` import)
    - 32-RESEARCH.md § Triage row T5 + Assumption A2 + Open Question #4
  </read_first>
  <action>
    First confirm the false-pin-gate claim: `rg -n 'telebot' .github/ scripts/ Makefile` returns nothing.
    Remove the `_ "gopkg.in/telebot.v4"` blank import from `deps.go`. The import block becomes empty — per
    Open Question #4 (executor's call): EITHER delete `deps.go` and move the valuable `package channels`
    doc comment into a new `internal/channels/doc.go` (preferred, matches the project doc idiom), OR keep
    `deps.go` as doc-only. Then run `go mod tidy` and confirm `gopkg.in/telebot.v4 v4.0.0-beta.9` stays in
    the DIRECT require block of `go.mod` (kept by `telegram/bot.go:18`). If tidy demotes/drops it, STOP and
    surface (Assumption A2). Run `go build $(bash scripts/go_packages.sh)`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go mod tidy && grep -q 'gopkg.in/telebot.v4' go.mod && go build $(bash scripts/go_packages.sh)</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'gopkg.in/telebot.v4' internal/channels/deps.go` returns nothing (anchor gone); `rg -n 'telebot' .github/ scripts/ Makefile` returns nothing (false-gate confirmed).
    - After `go mod tidy`, `go.mod` still lists `gopkg.in/telebot.v4` in the DIRECT (non-indirect) require block.
    - `go build $(bash scripts/go_packages.sh)` exits 0; the `package channels` doc comment survives (in `doc.go` or `deps.go`).
  </acceptance_criteria>
  <done>The redundant telebot anchor is removed, the pin stays DIRECT after tidy, build is green, and the package doc is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restructure the discarded Build() at llm_agent.go:235 (branch parity)</name>
  <files>internal/agent/llm_agent.go, internal/agent/llm_agent_test.go</files>
  <read_first>
    - internal/agent/llm_agent.go around :235 (the adaptiveTierOK path that runs both Build(...) and BuildWithReasoningTier(...) and discards the first)
    - 32-RESEARCH.md § Triage row T7; 32-PATTERNS.md § "internal/agent/llm_agent.go — restructure discarded Build()"
  </read_first>
  <behavior>
    - Test: with `adaptiveTierOK == true`, the produced request equals the OLD chosen request (the BuildWithReasoningTier output) — byte/struct-identical.
    - Test: with `adaptiveTierOK == false`, the produced request equals the OLD chosen request (the Build output) — byte/struct-identical.
    - Test (implicit): `Build()` is NOT called when `adaptiveTierOK == true` (no wasted RenderToolDefs()).
  </behavior>
  <action>
    Restructure to `if adaptiveTierOK { req = BuildWithReasoningTier(...) } else { req = Build(...) }` so
    exactly one builder runs per turn. The selected `req` must be byte-identical to the old code's chosen
    `req` in BOTH branches (no behavior change — only the discarded call is eliminated). Add a parity test
    in `llm_agent_test.go` asserting the chosen request per branch matches the pre-refactor output. Run
    refactor-on-touch checks on the file.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/agent/ && go vet ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/agent/` exits 0 with the branch-parity test present.
    - `rg -n 'BuildWithReasoningTier' internal/agent/llm_agent.go` shows it inside the `adaptiveTierOK` branch only; `Build(` is in the else branch only.
    - No code path runs both builders (verified by the test / reading the if/else).
  </acceptance_criteria>
  <done>The discarded Build() is eliminated via if/else; a parity test pins byte-identical request output per branch; agent tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Behavior-preserving removal/restructure — no trust boundary crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-PIN | Tampering | go.mod telebot pin | mitigate | `go mod tidy` + grep assertion confirm the pin stays DIRECT before commit (Assumption A2); STOP-and-surface if demoted. |
| T-32-03-REQ | Tampering | LLM request shape (Build restructure) | mitigate | Branch-parity test asserts byte-identical chosen request per branch (no behavior change). |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go mod tidy` keeps telebot DIRECT; `go build $(bash scripts/go_packages.sh)` exits 0 (WSL).
- `go test -race ./internal/agent/` exits 0 with the branch-parity test.
- `rg -n 'telebot' .github/ scripts/ Makefile` returns nothing (false-gate confirmed).
</verification>

<success_criteria>
ROADMAP C1 (partial): the telebot anchor and the discarded Build() are removed with build + tidy green
and a request-output parity test — each confirmed redundant, neither changing behavior.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-03-SUMMARY.md` when done.
</output>
