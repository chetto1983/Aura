---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 10
type: tdd
wave: 3
depends_on: ["32-05", "32-06", "32-07"]
files_modified:
  - internal/channels/telegram/profile_onboarding_test.go
  - internal/agent/llm_agent_completion_test.go
  - .planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-10-SUMMARY.md
autonomous: true
requirements: [QUAL-05]
user_setup: []

must_haves:
  truths:
    - "internal/channels/telegram has a table test for answersFromText's Italian-keyword fallback path (previously only the LLM-extractor path was tested)."
    - "internal/agent has a UTF-8 boundary table test for truncateTailBytes (mirroring TestTruncateBytes): n<=0, len(s)<=n, multi-byte mid-rune walk-back, head+marker+tail."
    - "The memory_integration CI leg is verified-and-documented as already-live (ci.yml:606-719) with no-skip-as-green discipline — NO redundant leg is added (D-12)."
  artifacts:
    - path: "internal/channels/telegram/profile_onboarding_test.go"
      provides: "answersFromText Italian-keyword fallback table"
      contains: "answersFromText"
    - path: "internal/agent/llm_agent_completion_test.go"
      provides: "truncateTailBytes UTF-8 boundary table"
      contains: "truncateTailBytes"
  key_links: []
  prohibitions:
    - "Do NOT add a new memory_integration CI matrix entry — the leg already runs live (D-12); verify + document only."
    - "Do NOT modify production code in internal/channels/telegram or internal/agent — these are test-only gap closures."
---

<objective>
Close the remaining QUAL-05 items:
(1) Telegram `answersFromText` keyword fallback (`internal/channels/telegram/profile_onboarding.go:362`)
— only the LLM-extractor path is tested today; add an Italian-keyword-fallback table.
(2) `truncateTailBytes` (`internal/agent/llm_agent_completion.go:209-221`, with
`truncateBytesKeepingTail:196-206`) — add a UTF-8 boundary table mirroring the sibling `TestTruncateBytes`.
(3) `memory_integration` CI leg (D-12 / QA-A-09) — RESEARCH verified the leg ALREADY runs live
(`ci.yml:606-719`: sets `CI:"true"`, exports `AURA_AGENT_MEMORY_MCP_URL`, compiles + runs
`-tags memory_integration` against the live sidecar; `memory_recall_integration_test.go:1,52` carries the
tag + `t.Fatal`-under-`$CI`). QA-A-09 is stale → VERIFY one last time and DOCUMENT; do NOT add a
redundant matrix entry.

Purpose: ROADMAP C3 — close the last targeted test gaps and confirm the memory tier truly runs (no
skip-as-green), without redundant CI.
Output: two test extensions (race-clean) and a documented memory_integration verdict.

D-14: execute sequentially, no git worktrees. D-11: one commit per test gap + one doc commit. The `gsd`
commit wrapper times out on the file-size hook — use direct `git commit`. All verify commands run in WSL
(never run .exe on the Windows host). Use the golang-testing table-test idiom (32-PATTERNS § Shared
Pattern B).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@internal/agent/llm_agent_completion.go
@internal/channels/telegram/profile_onboarding.go
</context>

## Artifacts this phase produces (this plan)

- (extended, no new symbol) `internal/channels/telegram/profile_onboarding_test.go` — `answersFromText` keyword-fallback table.
- (extended, no new symbol) `internal/agent/llm_agent_completion_test.go` — `truncateTailBytes` UTF-8 table.
- (doc) the 32-10-SUMMARY records the memory_integration already-live verdict + the one-last-time verification evidence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Table-test Telegram answersFromText Italian-keyword fallback</name>
  <files>internal/channels/telegram/profile_onboarding_test.go</files>
  <read_first>
    - internal/channels/telegram/profile_onboarding.go:362 (answersFromText(text string) profileflow.Answers — the keyword-fallback path)
    - existing telegram tests (the LLM-extractor path harness)
    - 32-PATTERNS.md § Test-Gap Targets Telegram row
  </read_first>
  <behavior>
    - answersFromText maps representative Italian keyword phrases to the expected profileflow.Answers (the fallback when the LLM extractor is not used).
    - non-matching text → the defined empty/default Answers.
  </behavior>
  <action>
    Add a table test in `internal/channels/telegram/profile_onboarding_test.go` exercising the Italian
    keyword-fallback branch of `answersFromText` with representative phrases and the no-match case. Do NOT
    modify the production function. Run under the race detector.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/channels/telegram/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/channels/telegram/profile_onboarding_test.go` covers the keyword-fallback path of `answersFromText` (incl. no-match).
    - `go test -race ./internal/channels/telegram/` exits 0.
    - production `profile_onboarding.go` is unchanged.
  </acceptance_criteria>
  <done>The answersFromText keyword fallback is pinned by a table test; production untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UTF-8 boundary table for truncateTailBytes (mirror TestTruncateBytes)</name>
  <files>internal/agent/llm_agent_completion_test.go</files>
  <read_first>
    - internal/agent/llm_agent_completion.go:196-221 (truncateBytesKeepingTail :196-206 + truncateTailBytes :209-221)
    - the sibling TestTruncateBytes in the same package (the analog shape)
    - 32-PATTERNS.md § Test-Gap Targets truncateTailBytes row + 32-RESEARCH.md test-gap table
  </read_first>
  <behavior>
    - n<=0 → defined behavior per the function contract.
    - len(s)<=n → unchanged.
    - multi-byte string truncated mid-rune → walks back to a rune boundary (no broken UTF-8).
    - head+marker+tail composition matches the documented format.
  </behavior>
  <action>
    Add a UTF-8 boundary table test in `internal/agent/llm_agent_completion_test.go` for `truncateTailBytes`
    mirroring the existing `TestTruncateBytes`: n<=0, len(s)<=n, multi-byte mid-rune walk-back, and the
    head+marker+tail case. Assert valid UTF-8 output. Do NOT modify the production function. Run under the
    race detector.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race -run Truncate ./internal/agent/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/llm_agent_completion_test.go` covers the 4 truncateTailBytes cases incl. mid-rune walk-back producing valid UTF-8.
    - `go test -race -run Truncate ./internal/agent/` exits 0.
    - production `llm_agent_completion.go` is unchanged.
  </acceptance_criteria>
  <done>truncateTailBytes is pinned by a UTF-8 boundary table mirroring its sibling; production untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Verify + document the already-live memory_integration CI leg (D-12, no new leg)</name>
  <files>.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-10-SUMMARY.md</files>
  <read_first>
    - .github/workflows/ci.yml:606-719 (the "Memory MCP (memory_integration tier ...)" job — CI:"true", AURA_AGENT_MEMORY_MCP_URL @:642, compile @:653-656, run @:711-715)
    - internal/agent/memory_recall_integration_test.go:1 (//go:build memory_integration) + :52 (t.Fatal under $CI when URL unset)
    - internal/agent/mcptools/memory_integration_test.go (in the run set)
    - 32-RESEARCH.md § "D-12 Verdict" + 32-VALIDATION.md Manual-Only memory_integration row
  </read_first>
  <action>
    Re-verify (read) that the `memory_integration` job at `ci.yml:606-719` sets `CI:"true"`, exports
    `AURA_AGENT_MEMORY_MCP_URL`, compiles `-tags memory_integration`, and runs the tier against the live
    sidecar; and that `memory_recall_integration_test.go` carries the build tag (:1) and `t.Fatal`s under
    `$CI` when the URL is unset (:52). Record the verdict (already-live, no-skip-as-green) + the line
    evidence in the 32-10 SUMMARY. Do NOT add a redundant CI matrix entry (D-12). If — contrary to RESEARCH
    — the leg is found NOT to run, STOP and surface to the operator before adding anything.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && grep -n 'memory_integration' .github/workflows/ci.yml && grep -n 'go:build memory_integration' internal/agent/memory_recall_integration_test.go && grep -n 't.Fatal' internal/agent/memory_recall_integration_test.go</automated>
  </verify>
  <acceptance_criteria>
    - The grep confirms the `memory_integration` job + tag + `t.Fatal`-under-$CI exist (no-skip-as-green).
    - The 32-10 SUMMARY documents the already-live verdict with `ci.yml:606-719` + `memory_recall_integration_test.go:1,52` evidence.
    - No new CI matrix entry was added.
  </acceptance_criteria>
  <done>The memory_integration leg is verified already-live and documented; QA-A-09 is recorded as stale; no redundant CI added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI green-signal integrity | The memory_integration leg must actually run (no skip-as-green) for the CI signal to be honest — verified, not changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10-SKIP | Repudiation | memory_integration CI leg (false-green risk) | mitigate | Verify CI:"true" + t.Fatal-under-$CI + the live run; document the evidence (D-12). |
| T-32-10-UTF | Tampering | truncateTailBytes UTF-8 safety | mitigate | Boundary table asserts mid-rune walk-back yields valid UTF-8. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go test -race ./internal/channels/telegram/ ./internal/agent/` exits 0 (WSL).
- The memory_integration grep evidence confirms the live leg + no-skip-as-green; SUMMARY documents it.
- No production code changed in telegram/agent; no new CI leg added.
</verification>

<success_criteria>
ROADMAP C3 (complete): the Telegram keyword fallback and truncateTailBytes UTF-8 gaps are closed with
race-clean tests, and the memory_integration CI leg is verified already-live and documented (D-12) with
no redundant CI.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-10-SUMMARY.md` when done.
</output>
