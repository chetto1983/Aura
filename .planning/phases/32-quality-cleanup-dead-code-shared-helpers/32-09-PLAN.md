---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 09
type: tdd
wave: 3
depends_on: ["32-05", "32-06"]
files_modified:
  - internal/web/throttle_test.go
  - internal/setup/handlers_test.go
  - internal/webauth/authula_test.go
autonomous: true
requirements: [QUAL-05]
user_setup: []

must_haves:
  truths:
    - "internal/web/throttle.go has a concurrency test (acquire blocks at perHostLimit=2, release frees a token, ctx-cancel → ok=false with a no-op release, per-host isolation, concurrent race) running under goleak with the race detector."
    - "internal/setup has a regression test asserting InvalidateToken is called before the first SSE write (the race the handlers.go:146 comment fixes)."
    - "internal/webauth has a table test of ensureAuthulaSearchPath covering empty / malformed / already-has-search_path / no-search_path (kept IN Phase 32 per the source-conflict resolution)."
  artifacts:
    - path: "internal/web/throttle_test.go"
      provides: "hostThrottle acquire/release/ctx-cancel/per-host/race coverage with goleak"
      contains: "perHostLimit"
    - path: "internal/webauth/authula_test.go"
      provides: "ensureAuthulaSearchPath DSN edge-case table"
      contains: "ensureAuthulaSearchPath"
  key_links: []
  prohibitions:
    - "Do NOT modify internal/web/throttle.go, internal/setup/handlers.go, or internal/webauth/authula.go production code — these are test-only gap closures (the functions already exist)."
    - "Do NOT add Authula-cutover infrastructure — ensureAuthulaSearchPath is a pure string function; the DSN test needs no cutover infra (source-conflict resolution keeps it IN Phase 32)."
---

<objective>
Close three QUAL-05 test gaps (test-only — the production functions already exist):
(1) `internal/web/throttle.go` (`hostThrottle.acquire/sem`, `perHostLimit=2`) — a concurrency primitive
with NO test; add acquire-blocks-at-limit, release-frees-a-token, ctx-cancel→ok=false+no-op-release,
per-host isolation, and a concurrent race test, under `goleak` (the package already has a goleak
`TestMain` in `main_test.go`).
(2) setup ordering — a regression test asserting `InvalidateToken` is called before the first SSE write
(the race the `internal/setup/handlers.go:146` comment fixes).
(3) Authula DSN — a table test of `ensureAuthulaSearchPath` (`internal/webauth/authula.go:292`):
empty / malformed / already-has-`search_path` / no-`search_path`. SOURCE CONFLICT NOTE: ROADMAP/REQUIREMENTS
place this IN Phase 32; audit §E routes it to Phase 34. Resolution: KEEP IN Phase 32 — it's a pure
existing string function needing no cutover infra (RESEARCH HIGH confidence). Defer to 34 ONLY if it
turns out to need infra not yet present.

Purpose: ROADMAP C3 — close targeted test gaps so these surfaces are pinned before phases 33+ build on them.
Output: three test files/extensions, all green under the race detector.

D-14: execute sequentially, no git worktrees. D-11: one commit per test gap. The `gsd` commit wrapper
times out on the file-size hook — use direct `git commit`. All verify commands run in WSL (never run
.exe on the Windows host). Use the golang-testing table-test + goleak idiom (32-PATTERNS § Shared
Pattern B).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@internal/web/throttle.go
@internal/webauth/authula.go
</context>

## Artifacts this phase produces (this plan)

- `internal/web/throttle_test.go` — NEW test file (the package goleak TestMain already exists in main_test.go).
- (extended, no new symbol) `internal/setup/handlers_test.go` — ordering regression.
- (extended, no new symbol) `internal/webauth/authula_test.go` — `ensureAuthulaSearchPath` table.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test internal/web/throttle.go (acquire/release/ctx-cancel/per-host/race) under goleak</name>
  <files>internal/web/throttle_test.go</files>
  <read_first>
    - internal/web/throttle.go:1-49 (hostThrottle.acquire/sem; perHostLimit=2; release semantics; ctx-cancel path)
    - internal/web/main_test.go (existing goleak TestMain — do NOT duplicate it)
    - 32-PATTERNS.md § Test-Gap Targets web/throttle row + Shared Pattern B
  </read_first>
  <behavior>
    - acquire blocks once perHostLimit (2) tokens are held; a release unblocks a waiter.
    - ctx-cancel while waiting → acquire returns ok=false AND the returned release is a no-op (does NOT free a token it never took).
    - per-host isolation: host A at its limit does not block host B.
    - concurrent race: many goroutines acquire/release without a data race (run under -race) and with no goroutine leak (goleak).
  </behavior>
  <action>
    Create `internal/web/throttle_test.go` with table/subtests covering the behavior cases above. Do NOT
    add a second goleak TestMain (the package already has one). Assert the ctx-cancel release is a no-op
    against an untaken token (the subtle correctness point from RESEARCH). Run under the race detector.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race -run Throttle ./internal/web/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/web/throttle_test.go` exists and references `perHostLimit` / acquire / release / ctx-cancel cases.
    - `go test -race -run Throttle ./internal/web/` exits 0 with no data race and no goroutine leak.
    - The ctx-cancel-no-op-release case is asserted.
  </acceptance_criteria>
  <done>throttle.go is pinned by a race+goleak concurrency test covering acquire/release/ctx-cancel/per-host isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression test setup InvalidateToken-before-SSE ordering</name>
  <files>internal/setup/handlers_test.go</files>
  <read_first>
    - internal/setup/handlers.go:146 (the documented InvalidateToken-before-writeSSE fix)
    - existing internal/setup tests (for the harness/fixtures)
    - 32-PATTERNS.md § Test-Gap Targets setup ordering row
  </read_first>
  <behavior>
    - On the SSE handler path, InvalidateToken is invoked BEFORE the first SSE write (the race the comment fixes).
  </behavior>
  <action>
    Add a regression test in `internal/setup/handlers_test.go` that observes the ordering (e.g. via a
    seam/spy or recorded call order) and asserts `InvalidateToken` is called before the first `writeSSE`.
    Do NOT modify the production handler. Run under the race detector.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/setup/</automated>
  </verify>
  <acceptance_criteria>
    - The new test fails if the InvalidateToken/writeSSE order is swapped (it pins the fix).
    - `go test -race ./internal/setup/` exits 0.
    - `internal/setup/handlers.go` is unchanged (test-only).
  </acceptance_criteria>
  <done>The setup SSE-ordering fix is pinned by a regression test; production handler untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Table-test ensureAuthulaSearchPath DSN parsing (kept IN Phase 32)</name>
  <files>internal/webauth/authula_test.go</files>
  <read_first>
    - internal/webauth/authula.go:292 (ensureAuthulaSearchPath(dsn string) (string, error))
    - existing internal/webauth tests (harness)
    - 32-RESEARCH.md § Source Conflict resolution (keep IN 32; pure fn, no cutover infra) + Assumption A5
  </read_first>
  <behavior>
    - empty DSN → defined error/handling per the function contract.
    - malformed DSN → error.
    - DSN already containing search_path → returned unchanged (idempotent).
    - DSN without search_path → search_path appended correctly.
  </behavior>
  <action>
    Add a table test in `internal/webauth/authula_test.go` covering empty / malformed / already-has-
    search_path / no-search_path. Do NOT add Authula-cutover infrastructure (the function is a pure string
    helper). Record in the SUMMARY that the source conflict was resolved by keeping the test IN Phase 32
    (defer to 34 only if it had needed infra not yet present — it does not).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/webauth/</automated>
  </verify>
  <acceptance_criteria>
    - `internal/webauth/authula_test.go` covers the 4 DSN cases for `ensureAuthulaSearchPath`.
    - `go test -race ./internal/webauth/` exits 0.
    - `internal/webauth/authula.go` is unchanged (test-only); SUMMARY records the source-conflict resolution.
  </acceptance_criteria>
  <done>ensureAuthulaSearchPath is pinned by a DSN edge-case table; the source conflict is resolved (kept IN Phase 32) and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → web throttle / setup SSE | The throttle bounds per-host concurrency and the setup token-invalidation guards a session race — both are correctness surfaces being pinned by tests (no production change). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-09-DOS | Denial of Service | web/throttle per-host limit | mitigate | Test asserts acquire blocks at perHostLimit and ctx-cancel frees no extra token — pins the rate-limit correctness. |
| T-32-09-RACE | (race correctness) | setup InvalidateToken/SSE ordering | mitigate | Regression test pins the invalidate-before-write order (the documented fix). |
| T-32-09-DSN | Tampering | Authula search_path DSN | mitigate | Table test covers malformed/idempotent/append cases of the pure parser. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go test -race ./internal/web/ ./internal/setup/ ./internal/webauth/` exits 0 (WSL).
- throttle test runs under goleak + race; setup ordering test fails on a swapped order; Authula DSN table covers 4 cases.
- No production code changed in these three packages.
</verification>

<success_criteria>
ROADMAP C3 (partial): web/throttle, setup InvalidateToken-before-SSE ordering, and Authula
ensureAuthulaSearchPath DSN parsing are each covered by a race-clean test, with no production change and
the source conflict resolved.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-09-SUMMARY.md` when done.
</output>
