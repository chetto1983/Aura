---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 07
type: tdd
wave: 2
depends_on: ["32-02"]
files_modified:
  - internal/agentrender/agentrender.go
  - internal/agentrender/agentrender_test.go
  - cmd/aura/chat_render.go
  - internal/eval/capture_cot_eval.go
autonomous: true
requirements: [QUAL-03]
user_setup: []

must_haves:
  truths:
    - "internal/agentrender exports the shared render primitives (FlushRemainder, IsToolResultPreview, IsTerminalToolCall, UsageFromStateDelta, AnyInt, AnyFloat); cmd/aura chat_render and internal/eval use them and their local copies are deleted."
    - "AnyInt adopts the chat_render superset (handles json.Number); the eval path now counts json.Number tokens correctly (a documented behavior FIX for the previously-lossy eval copy)."
    - "A parity table test was GREEN against the OLD copies before the move; the AnyInt table documents the eval json.Number fix."
    - "internal/agentrender independently clears the ≥85% coverage floor; no agui back-edge is introduced."
  artifacts:
    - path: "internal/agentrender/agentrender.go"
      provides: "shared REPL/eval event-render primitives"
      exports: ["FlushRemainder", "IsToolResultPreview", "IsTerminalToolCall", "UsageFromStateDelta", "AnyInt", "AnyFloat"]
  key_links:
    - from: "cmd/aura/chat_render.go"
      to: "internal/agentrender"
      via: "agentrender.UsageFromStateDelta / AnyInt / FlushRemainder"
      pattern: "agentrender\\."
  prohibitions:
    - "Do NOT import internal/agui from agentrender (boundary guard) — agentrender → agent + llm only."
    - "Do NOT preserve the eval copy's lossy json.Number→0 behavior — adopt the superset (the fix is intended) and document it in the parity test + SUMMARY."
---

<objective>
Extract the ~80-LOC render-primitive set duplicated between `cmd/aura/chat_render.go:111-229` and
`internal/eval/capture_cot_eval.go:153-229` into a new `internal/agentrender` leaf (QUAL-03, minimal
per D-06), test-first per D-09/D-10. RESEARCH found a real DRIFT: `chat_render`'s `anyInt` handles
`json.Number` while the eval copy does NOT (it silently zeroes `json.Number` token counts). Adopt the
`chat_render` superset — this is a documented behavior FIX for the eval path, not a regression.

Purpose: ROADMAP C2 — centralize the drift-prone event-shaping primitives shared by the REPL renderer
and the eval scorer, fixing the eval token-count bug in the process.
Output: `internal/agentrender` with the superset primitives, both call sites migrated, a parity test
that documents the eval fix, and ≥85% coverage.

D-14: execute sequentially, no git worktrees. D-11: one commit for the extraction (the eval fix rides
with it, documented). The `gsd` commit wrapper times out on the file-size hook — use direct
`git commit`. All verify commands run in WSL (never run .exe on the Windows host).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@cmd/aura/chat_render.go
@internal/eval/capture_cot_eval.go
@internal/canonicaljson/canonicaljson_test.go
</context>

## Artifacts this phase produces (this plan)

- `internal/agentrender/agentrender.go` (+ `_test.go`) — NEW: `FlushRemainder`, `IsToolResultPreview`, `IsTerminalToolCall`, `UsageFromStateDelta`, `AnyInt`, `AnyFloat`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create internal/agentrender with the superset primitives (parity test GREEN against old copies)</name>
  <files>internal/agentrender/agentrender.go, internal/agentrender/agentrender_test.go</files>
  <read_first>
    - cmd/aura/chat_render.go:111-229 (flushRemainder, isToolResultPreview, isTerminalToolCall, usageFromStateDelta, anyInt @:222-228 with json.Number, anyFloat)
    - internal/eval/capture_cot_eval.go:153-229 (the twin; anyInt @:202-213 WITHOUT json.Number → lossy)
    - 32-PATTERNS.md § agentrender (the AnyInt superset excerpt) + 32-RESEARCH.md § Validation Architecture AnyInt/AnyFloat/UsageFromStateDelta rows
  </read_first>
  <behavior>
    - AnyInt: int→n; int64→int(n); float64→int(n); json.Number(valid)→int; json.Number(invalid)→0; nil→0; other→0. Result == chat_render's superset for all; DOCUMENT eval's old json.Number→0 as fixed.
    - AnyFloat: float64/int/json.Number → (value, ok); old==new.
    - UsageFromStateDelta: full StateDelta map → llm.Usage (PromptTokens/CompletionTokens/CachedTokens via AnyInt, Cost via AnyFloat); nil→zero Usage; old==new (eval now counts json.Number tokens).
    - FlushRemainder / IsToolResultPreview / IsTerminalToolCall: prefix/divergent/empty remainder; event with/without tool_call_id; calls with/without text_response; old==new (byte/struct-equal).
  </behavior>
  <action>
    FIRST (D-10): write `agentrender_test.go` table tests feeding the union of inputs to BOTH old copies
    (chat_render + eval) AND the new agentrender funcs; for AnyInt assert the NEW == chat_render's superset
    and add an explicit case documenting that eval's OLD copy returned 0 for json.Number (the fix); confirm
    GREEN. THEN create `internal/agentrender/agentrender.go` (imports `internal/agent` for *agent.Event +
    `internal/llm` for llm.ToolCall/llm.Usage; NO agui) exporting `FlushRemainder`, `IsToolResultPreview`,
    `IsTerminalToolCall`, `UsageFromStateDelta`, `AnyInt` (superset), `AnyFloat`, with the Shared-Pattern-A
    doc noting the json.Number drift. (Optional boundary simplification: `IsToolResultPreview` may take
    `map[string]any` to drop the agent import — minor signature change at 2 call sites, still boundary-safe.)
    Confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race -cover ./internal/agentrender/ && ! go list -deps ./internal/agentrender/ | grep -q 'internal/agui'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agentrender/agentrender.go` contains `func AnyInt(`, `func UsageFromStateDelta(`, `func FlushRemainder(`, `func IsToolResultPreview(`, `func IsTerminalToolCall(`, `func AnyFloat(`.
    - `go test -race -cover ./internal/agentrender/` exits 0 and reports >=85% coverage.
    - `go list -deps ./internal/agentrender/` does NOT include `internal/agui` (boundary guard).
    - The AnyInt test documents the eval json.Number fix.
  </acceptance_criteria>
  <done>agentrender exists with the superset primitives, parity test green (incl. the documented eval fix), >=85%, no agui import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate cmd/aura chat_render and internal/eval to agentrender; delete the local copies</name>
  <files>cmd/aura/chat_render.go, internal/eval/capture_cot_eval.go</files>
  <read_first>
    - cmd/aura/chat_render.go:111-229 (the local helpers to delete + their call sites)
    - internal/eval/capture_cot_eval.go:153-229 (the local helpers to delete + their call sites)
    - internal/agentrender/agentrender.go (the new exported API from Task 1)
  </read_first>
  <behavior>
    - chat_render output is unchanged after migration (the REPL renders identically).
    - eval scoring now counts json.Number token fields (the fix takes effect); other eval behavior unchanged.
  </behavior>
  <action>
    Replace the local `flushRemainder`/`isToolResultPreview`/`isTerminalToolCall`/`usageFromStateDelta`/
    `anyInt`/`anyFloat` calls in `chat_render.go` and `capture_cot_eval.go` with `agentrender.*`, and DELETE
    the local copies from both files. Run refactor-on-touch dead-code/dup checks. Confirm chat_render output
    parity and eval json.Number counting via the existing/extended tests for those packages.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./cmd/aura/ ./internal/eval/ && rg -n 'func (flushRemainder|usageFromStateDelta|anyInt|anyFloat|isToolResultPreview|isTerminalToolCall)' cmd/aura/chat_render.go internal/eval/capture_cot_eval.go ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'agentrender\.' cmd/aura/chat_render.go internal/eval/capture_cot_eval.go` shows both call sites use the package.
    - `rg -n 'func (flushRemainder|usageFromStateDelta|anyInt|anyFloat|isToolResultPreview|isTerminalToolCall)' cmd/aura/chat_render.go internal/eval/capture_cot_eval.go` returns nothing (copies deleted).
    - `go test -race ./cmd/aura/ ./internal/eval/` exits 0.
  </acceptance_criteria>
  <done>Both call sites use agentrender; the local copies are gone; REPL output unchanged; eval json.Number fix in effect; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Behavior-preserving extraction (with one documented eval-path fix) — no new trust boundary; the agent ⇸ agui boundary must not gain a back-edge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07-BND | Tampering | agent ⇸ agui import boundary | mitigate | `go list -deps ./internal/agentrender/` asserts no `internal/agui`; agentrender → agent + llm only (Shared Pattern C). |
| T-32-07-FIX | Tampering | eval token counting (json.Number) | mitigate | Adopt the chat_render superset; the parity test documents the OLD eval json.Number→0 as fixed (not a silent change). |
| T-32-07-PAR | Tampering | chat_render output drift | mitigate | Parity table GREEN against old copies before the move; chat_render package tests green after. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go test -race -cover ./internal/agentrender/` >=85%; `go test -race ./cmd/aura/ ./internal/eval/` green (WSL).
- `go list -deps ./internal/agentrender/` has no `internal/agui`.
- No local render-primitive copies remain in chat_render/capture_cot_eval; both use `agentrender.*`.
</verification>

<success_criteria>
ROADMAP C2 (partial): the shared REPL/eval render primitives live in internal/agentrender (>=85%), both
call sites migrated, the eval json.Number bug fixed-and-documented, with a parity test green against the
old copies first and no agui back-edge.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-07-SUMMARY.md` when done.
</output>
