---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/assets/types.go
autonomous: false
requirements: [QUAL-02]
user_setup: []

must_haves:
  truths:
    - "An operator keep/kill decision (D-04) is recorded for assets.Status{Created,Embedding,Canceled} before any code change."
    - "If KEEP: internal/assets/types.go documents the deferred multimodal-asset lifecycle and the 3 named constants remain exported."
    - "If DELETE: only the 3 named constants are removed, each confirmed dead by deadcode + knip + repo-wide rg; go build stays green."
  artifacts:
    - path: "internal/assets/types.go"
      provides: "Asset lifecycle Status constants, annotated (KEEP) or trimmed to the 3 named (DELETE)"
      contains: "Status"
  key_links: []
  prohibitions:
    - "Do NOT wire the status state machine — wiring is new feature behavior (D-02 guardrail); this plan only keeps+annotates or deletes-3-named."
    - "Do NOT touch the ~7 other unused siblings (accepted/processing/searchable/complete/failed/refused/deleted) — D-03 no sweep; log as follow-up only."
    - "Do NOT confuse internal/assets.StatusCanceled with onboarding.StatusCanceled (a different package's constant that IS used)."
---

<objective>
Resolve the single QUAL-02 item that triage flipped to "intended-but-unwired": the exported
`assets.Status{Created,Embedding,Canceled}` constants (`internal/assets/types.go:14,20,25`). RESEARCH
verified these are part of a designed-but-unbuilt 12-state lifecycle
(`docs/superpowers/plans/2026-06-18-industrial-multimodal-asset-pipeline.md`); production emits only
`StatusPresigned` + `StatusUploaded`. Per D-02 (wiring = new feature → escalate) and D-04 (exported
symbol → operator keep/kill sign-off), this is an operator decision, not an autonomous delete.

Purpose: Honor the operator's framing — "check if it's really dead or needs wired" — without
destroying designed work or silently expanding scope.
Output: A recorded keep/kill decision and the corresponding minimal code change (annotate or
delete-3-named).

D-14: execute sequentially, no git worktrees (config.use_worktrees overridden for this phase).
Commit discipline (D-11): one commit for the resulting code change. NOTE: the `gsd` commit wrapper
times out on the ~34s file-size pre-commit hook — fall back to a direct `git commit` if it stalls.
Verification commands run in WSL/container (NEVER run .exe on the Windows host).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@internal/assets/types.go
@internal/assets/store.go
</context>

## Artifacts this phase produces (this plan)

- (no new symbol) — modifies `internal/assets/types.go` only (annotate, or delete the 3 named constants).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator keep/kill sign-off on assets.Status{Created,Embedding,Canceled} (D-04)</name>
  <files>internal/assets/types.go</files>
  <read_first>
    - internal/assets/types.go (the 12-constant Status block at :14-25; production emits only StatusPresigned/StatusUploaded)
    - internal/assets/store.go (confirm only StatusPresigned @:41 + MarkUploaded path emit a status)
    - docs/superpowers/plans/2026-06-18-industrial-multimodal-asset-pipeline.md (the designed lifecycle these constants belong to)
    - 32-RESEARCH.md § Dead-vs-Unwired Triage row T2
  </read_first>
  <action>
    Before deciding, re-run the D-04 confirmation set in WSL and paste the results into the decision
    context: `deadcode -test $(bash scripts/go_packages.sh)`, `rg -n 'StatusCreated|StatusEmbedding|StatusCanceled' --glob '!**/dist/**'`,
    and confirm no JSON-string/DB consumer reads the literal values "created"/"embedding"/"canceled"
    (Assumption A1). Present the decision to the operator.
  </action>
  <decision>Keep the 3 exported assets.Status constants (annotate the deferred lifecycle) or delete them?</decision>
  <context>
    These are exported constants of a designed-but-unwired 12-state asset lifecycle (created→presigned→
    uploaded→accepted→processing→searchable→embedding→complete→failed/refused/deleted/canceled).
    Production currently emits only `StatusPresigned` and `StatusUploaded`. The 3 audit-named constants
    are unused — but so are ~7 siblings. Wiring the machine is new feature behavior (D-02 guardrail →
    out of scope here). RESEARCH recommends KEEP + annotate over delete-and-re-add churn.
  </context>
  <options>
    <option id="keep-annotate">
      <name>KEEP + annotate (RECOMMENDED)</name>
      <pros>Preserves the designed lifecycle; zero churn when phases 33+ wire it; documents intent so the next deadcode run stops re-flagging.</pros>
      <cons>The deadcode flag persists (mitigated by an explicit "deferred lifecycle" comment).</cons>
    </option>
    <option id="delete-3-named">
      <name>DELETE the 3 named only</name>
      <pros>Clears the exact QUAL-02 flag.</pros>
      <cons>Deletes designed work; likely re-added in a later phase; D-03 forbids also removing the ~7 unused siblings, so the lifecycle is left half-present and inconsistent.</cons>
    </option>
  </options>
  <verify>
    <automated>rg -n 'StatusCreated|StatusEmbedding|StatusCanceled' internal/assets/types.go</automated>
  </verify>
  <acceptance_criteria>
    - The decision (keep-annotate | delete-3-named) is recorded in the plan SUMMARY with the deadcode/rg evidence attached.
    - No code is changed in this task (decision only).
  </acceptance_criteria>
  <resume-signal>Reply "keep-annotate" or "delete-3-named".</resume-signal>
  <done>The operator has selected keep-annotate or delete-3-named, and the choice + deadcode/rg evidence is recorded in the SUMMARY (no code changed in this task).</done>
</task>

<task type="auto">
  <name>Task 2: Apply the recorded decision to internal/assets/types.go</name>
  <files>internal/assets/types.go</files>
  <read_first>
    - internal/assets/types.go (Status block :9-25)
    - Task 1 recorded decision
  </read_first>
  <action>
    If the decision is KEEP: add a concise doc comment above the `const (` Status block (near :13)
    stating that the full 12-state set is a deferred lifecycle from
    `docs/superpowers/plans/2026-06-18-industrial-multimodal-asset-pipeline.md`, that production
    currently emits only StatusPresigned/StatusUploaded, and that the remaining states are intentionally
    retained for the unbuilt pipeline (so future deadcode runs treat them as known-deferred). Do NOT
    delete any constant.
    If the decision is DELETE: remove ONLY `StatusCreated` (:14), `StatusEmbedding` (:20), and
    `StatusCanceled` (:25). Leave the other ~7 unused siblings untouched (D-03 no sweep) and log them as
    a follow-up in the SUMMARY. Re-run the D-04 confirmation set to prove the 3 are dead before removal.
    Either branch: run `go build $(bash scripts/go_packages.sh)` + `go vet` for `internal/assets`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go build ./internal/assets/ && go vet ./internal/assets/ && go test ./internal/assets/</automated>
  </verify>
  <acceptance_criteria>
    - KEEP branch: `rg -n 'deferred|lifecycle' internal/assets/types.go` shows the annotation; all 12 constants still present.
    - DELETE branch: `rg -n 'StatusCreated|StatusEmbedding|StatusCanceled' internal/assets/types.go` returns nothing; the other 7 siblings still present; `deadcode -test` no longer flags the 3.
    - `go build ./internal/assets/` exits 0 in WSL.
  </acceptance_criteria>
  <done>The operator's keep/kill decision is implemented in types.go with build green; the SUMMARY records the decision, the evidence, and (if DELETE) the deferred follow-up for the ~7 untouched siblings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Behavior-preserving triage of unused constants — no trust boundary crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-A1 | Tampering | DELETE branch removing a constant a wire/DB consumer expects | mitigate | D-04 confirmation set (deadcode + knip + repo-wide rg incl. _test.go) + Assumption A1 grep for literal "created"/"embedding"/"canceled" before any removal. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH § Environment Availability) — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go build ./internal/assets/` and `go vet ./internal/assets/` exit 0 (WSL).
- The decision and its deadcode/rg evidence are recorded in the SUMMARY.
- No status-machine wiring was added (scope guardrail held).
</verification>

<success_criteria>
ROADMAP C1 (partial): the assets.Status QUAL-02 item is resolved by an operator-signed-off decision,
not a blind deletion; the resulting types.go state is build-green and evidence-backed.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-01-SUMMARY.md` when done.
</output>
