---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 06
type: tdd
wave: 2
depends_on: ["32-03"]
files_modified:
  - internal/canonicaljson/canonicaljson.go
  - internal/canonicaljson/canonicaljson_test.go
  - internal/agent/llm_agent_args.go
  - internal/agent/workflow/loop.go
  - internal/agent/llm_agent_retry.go
  - internal/agent/llm_agent_stream_retry.go
  - internal/agent/llm_agent_retry_test.go
  - internal/agent/llm_agent_stream_retry_test.go
autonomous: true
requirements: [QUAL-03]
user_setup: []

must_haves:
  truths:
    - "canonicaljson.CanonicalArgs is the single canonical tool-arg canonicalizer; agent's canonicalArgs and workflow's canonArgs are deleted and both call sites use canonicaljson.CanonicalArgs."
    - "A shared isTransientNetworkErr (typed-network-sentinel subset) exists in package agent; isTransientToolErr delegates to it AND keeps context.DeadlineExceeded→true (intentional WIDENING, documented); retryableStreamOpenError keeps its context.*→false guard FIRST and produces output byte-identical to before (STRICT parity)."
    - "Each extraction has a parity/golden table test GREEN against the OLD code before the change."
  artifacts:
    - path: "internal/canonicaljson/canonicaljson.go"
      provides: "CanonicalArgs(rawArgs string) []byte"
      exports: ["CanonicalArgs"]
    - path: "internal/agent/llm_agent_retry.go"
      provides: "shared isTransientNetworkErr + widened isTransientToolErr"
      contains: "isTransientNetworkErr"
  key_links:
    - from: "internal/agent/llm_agent_args.go"
      to: "internal/canonicaljson.CanonicalArgs"
      via: "tool-arg canonicalization"
      pattern: "canonicaljson\\.CanonicalArgs"
  prohibitions:
    - "Do NOT make both classifiers delegate symmetrically — retryableStreamOpenError must return false for context.DeadlineExceeded (opposite of the tool path); a naive merge breaks the stream path (Pitfall 2)."
    - "Do NOT change retryableStreamOpenError's output for ANY input — capture a golden table BEFORE and assert identical after (STRICT parity)."
    - "The shared subset excludes context.*, HTTP status, url.Error, retryableNetworkText, and ErrStreamIdleTimeout — those stay domain-specific."
---

<objective>
Two same-package agent extractions (QUAL-03), each test-first per D-09/D-10:
(1) `CanonicalArgs` (QA-A-01) — the byte-identical `canonicalArgs` (`agent/llm_agent_args.go:66-76`)
and `canonArgs` (`agent/workflow/loop.go:345-355`) move into their existing home
`internal/canonicaljson` (both call sites already import it — zero new edges).
(2) `isTransientNetworkErr` (QA-A-02) — the ASYMMETRIC transient-error classifiers. Extract ONLY the
typed-network-sentinel subset both share; then `isTransientToolErr` becomes an INTENTIONAL WIDENING
(`context.DeadlineExceeded` OR the shared subset — now also retries ECONNRESET/EOF) while
`retryableStreamOpenError` stays STRICT (context.*→false guard FIRST, output byte-identical). A naive
symmetric merge breaks the stream path's deliberate `context.DeadlineExceeded → false` (Pitfall 2).

Purpose: ROADMAP C2 — dedupe agent helpers without changing the stream retry contract and with a
documented, tested widening on the tool path.
Output: CanonicalArgs in canonicaljson, a shared isTransientNetworkErr, parity tests (strict for stream,
characterized widening for tool), call sites repointed.

D-14: execute sequentially, no git worktrees. D-11: one commit per extraction. The `gsd` commit wrapper
times out on the file-size hook — use direct `git commit`. All verify commands run in WSL (never run
.exe on the Windows host). Follow 32-PATTERNS § "isTransientNetworkErr ⚠ ASYMMETRIC" exactly.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@internal/canonicaljson/canonicaljson.go
@internal/agent/llm_agent_retry.go
@internal/agent/llm_agent_stream_retry.go
</context>

## Artifacts this phase produces (this plan)

- `internal/canonicaljson/canonicaljson.go` — NEW exported symbol `CanonicalArgs`.
- (modified, no new exported symbol) `internal/agent/llm_agent_retry.go` — NEW unexported `isTransientNetworkErr` + widened `isTransientToolErr`.
- (modified) `internal/agent/llm_agent_stream_retry.go` — `retryableStreamOpenError` delegating to the shared subset (strict).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move CanonicalArgs into internal/canonicaljson test-first; delete the two agent copies</name>
  <files>internal/canonicaljson/canonicaljson.go, internal/canonicaljson/canonicaljson_test.go, internal/agent/llm_agent_args.go, internal/agent/workflow/loop.go</files>
  <read_first>
    - internal/agent/llm_agent_args.go:66-76 (canonicalArgs) + the import of canonicaljson @:8
    - internal/agent/workflow/loop.go:345-355 (canonArgs — byte-identical except name)
    - internal/canonicaljson/canonicaljson.go + _test.go (home + table-test shape)
    - 32-RESEARCH.md § Validation Architecture CanonicalArgs union row
  </read_first>
  <behavior>
    - CanonicalArgs: valid object (unsorted keys→sorted), array, nested, number forms (1e3/1.0/large int), string/bool/null, malformed JSON→raw bytes, empty string→raw "", whitespace-only.
    - Parity: canonArgs(x)==canonicalArgs(x)==CanonicalArgs(x) byte-equal for all x.
  </behavior>
  <action>
    FIRST: add a `CanonicalArgs` table test to `canonicaljson_test.go` feeding the union of inputs and
    asserting the old `canonicalArgs`/`canonArgs` agree with the new `CanonicalArgs`; confirm GREEN. THEN
    add `CanonicalArgs(rawArgs string) []byte` to `canonicaljson.go` (non-JSON → raw bytes;
    un-canonicalizable → raw bytes; else `canonicaljson.Marshal`). Delete `canonicalArgs` from
    `llm_agent_args.go` and `canonArgs` from `workflow/loop.go`; repoint both call sites to
    `canonicaljson.CanonicalArgs`. Confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/canonicaljson/ ./internal/agent/ ./internal/agent/workflow/ && rg -n 'func canonicalArgs|func canonArgs' internal/agent/ ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `internal/canonicaljson/canonicaljson.go` contains `func CanonicalArgs(`.
    - `rg -n 'func canonicalArgs|func canonArgs' internal/agent/` returns nothing.
    - `go test -race ./internal/canonicaljson/ ./internal/agent/ ./internal/agent/workflow/` exits 0 with the parity table.
  </acceptance_criteria>
  <done>CanonicalArgs is the single canonicalizer in canonicaljson; both agent copies deleted; call sites repointed; parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shared isTransientNetworkErr; WIDEN tool path, keep stream path STRICT (golden-before)</name>
  <files>internal/agent/llm_agent_retry.go, internal/agent/llm_agent_stream_retry.go, internal/agent/llm_agent_retry_test.go, internal/agent/llm_agent_stream_retry_test.go</files>
  <read_first>
    - internal/agent/llm_agent_retry.go:56-68 (isTransientToolErr — DeadlineExceeded→true)
    - internal/agent/llm_agent_stream_retry.go:77-113 (retryableStreamOpenError — context.*→false guard FIRST, then HTTPError 429/5xx, url.Error, ErrStreamIdleTimeout, typed syscall sentinels, retryableNetworkText)
    - 32-PATTERNS.md § "isTransientNetworkErr ⚠ ASYMMETRIC"; 32-RESEARCH.md § Validation Architecture (the 3 classifier rows) + Pitfall 2
  </read_first>
  <behavior>
    - isTransientNetworkErr (NEW subset): net.Error{timeout}→T; io.EOF/io.ErrUnexpectedEOF→T; syscall.ECONNRESET/ECONNREFUSED/ETIMEDOUT→T; nil→F; plain error→F. (Excludes context.*, HTTP status, url.Error, retryableNetworkText, ErrStreamIdleTimeout.)
    - isTransientToolErr (WIDENED): context.DeadlineExceeded→T (kept); net timeout→T; ECONNRESET→T (was F); io.ErrUnexpectedEOF→T (was F); validation error→F. Characterize OLD first, then assert the NEW widened set; DOCUMENT the change.
    - retryableStreamOpenError (STRICT): context.Canceled→F; context.DeadlineExceeded→F; HTTPError{429/500/503}→T; HTTPError{400}→F; net timeout→T; url.Error{timeout}→T; url.Error{retryable text}→T; ErrStreamIdleTimeout→T; io.EOF/ErrUnexpectedEOF→T; syscall sentinels→T; retryableNetworkText markers + bare "eof"→T; plain error→F. Output IDENTICAL to before for every input.
  </behavior>
  <action>
    FIRST: capture a golden table for `retryableStreamOpenError` (every input row above) asserting the
    CURRENT output, and characterize the OLD `isTransientToolErr`; confirm GREEN against the current code.
    THEN add the shared `isTransientNetworkErr(err) bool` (typed-network sentinels only) to
    `llm_agent_retry.go`. Rewrite `isTransientToolErr` = `errors.Is(err, context.DeadlineExceeded) ||
    isTransientNetworkErr(err)` (INTENTIONAL WIDENING — document in a comment + the SUMMARY that it now
    retries ECONNRESET/EOF). Rewrite `retryableStreamOpenError` to keep the leading `context.Canceled`/
    `context.DeadlineExceeded → false` guard FIRST, then HTTPError, url.Error, ErrStreamIdleTimeout,
    `isTransientNetworkErr(err)`, and the `retryableNetworkText` fallback LAST — its golden table output
    must be byte-identical to the pre-refactor capture. Confirm GREEN (the stream golden table unchanged;
    the tool table now reflects the widened set).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/agent/ && rg -n 'func isTransientNetworkErr' internal/agent/llm_agent_retry.go</automated>
  </verify>
  <acceptance_criteria>
    - `internal/agent/llm_agent_retry.go` contains `func isTransientNetworkErr(` and `isTransientToolErr` delegates to it while keeping `context.DeadlineExceeded → true`.
    - The `retryableStreamOpenError` golden table asserts byte-identical output before/after; its context.*→false guard remains FIRST.
    - The tool-path test asserts the WIDENED set (ECONNRESET/EOF now true) and the SUMMARY documents the widening.
    - `go test -race ./internal/agent/` exits 0.
  </acceptance_criteria>
  <done>The shared subset is extracted; the tool path is widened (tested + documented); the stream path is byte-identical (golden table); no symmetric-merge regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → LLM provider (retry classification) | Misclassifying a non-retryable error as retryable could change request behavior; the stream path's deadline semantics are deliberate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06-STR | Tampering | retryableStreamOpenError (stream retry contract) | mitigate | Golden table captured BEFORE refactor; STRICT byte-identical output asserted; context.*→false guard kept FIRST (Pitfall 2). |
| T-32-06-TOOL | Tampering | isTransientToolErr widening | mitigate | Characterize OLD then assert NEW widened set; document the intentional behavior change (retries ECONNRESET/EOF). |
| T-32-06-CYC | Tampering | canonicaljson edge | mitigate | Both call sites already import canonicaljson — no new edge, no cycle. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go test -race ./internal/canonicaljson/ ./internal/agent/ ./internal/agent/workflow/` exits 0 (WSL).
- The stream golden table is byte-identical before/after; the tool table reflects the documented widening.
- No `canonicalArgs`/`canonArgs` copies remain; `isTransientNetworkErr` present.
</verification>

<success_criteria>
ROADMAP C2 (partial): CanonicalArgs unified into canonicaljson and the transient-error classifiers
share a typed-network subset — the stream path unchanged (strict golden), the tool path intentionally
widened (tested + documented), each with a parity test that was green against the old code first.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-06-SUMMARY.md` when done.
</output>
