---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/aura/agent.go
  - cmd/aura/agent_test.go
  - internal/agui/governance_api.go
  - internal/agui/governance_api_test.go
  - internal/assets/context.go
  - internal/rerank/client.go
autonomous: true
requirements: [QUAL-02]
user_setup: []

must_haves:
  truths:
    - "Every dry-run agent event carries a non-zero requestID (the cmd/aura/agent.go:127 re-stamp is proven load-bearing by a regression test, not removed)."
    - "internal/agui/governance_api.go uses strings.IndexByte (the local indexByte helper is deleted) and the NetworkAllowlist still marshals as [] (never null) for an empty allowlist."
    - "The truncateRunes 5-line duplication has a recorded resolution (default: accept-with-cross-reference-comment) — no new package created without justification."
  artifacts:
    - path: "cmd/aura/agent_test.go"
      provides: "Regression test proving the RequestID re-stamp is load-bearing on the dry-run path"
      contains: "requestID"
    - path: "internal/agui/governance_api.go"
      provides: "stdlib-based env-chip parsing + inlined non-nil allowlist copy"
      contains: "strings.IndexByte"
  key_links:
    - from: "cmd/aura/agent.go:127"
      to: "agenttest.InfiniteToolCallAgent (mocks.go:61)"
      via: "dry-run path that omits RequestID on the fake agent's events"
      pattern: "ev.RequestID = requestID"
  prohibitions:
    - "Do NOT remove cmd/aura/agent.go:127 — triage verdict is KEEP (load-bearing on the dry-run path)."
    - "Do NOT change the agui.stringList inline to a nil-based copy — must stay append([]string{}, ...) so JSON stays [] not null (Pitfall 4)."
    - "Do NOT touch MCP trust-normalization (QA-C-03 → Phase 38) or decode*Body strict-decode (QA-C-01 → Phase 38/40) even if encountered in agui."
---

<objective>
Resolve the QUAL-02 dead-code items that are KEEPs or in-place stdlib swaps (no extraction):
(1) the `RequestID` re-stamp at `cmd/aura/agent.go:127` — triage flipped this to **load-bearing**
(the dry-run drives `agenttest.InfiniteToolCallAgent`, whose `Run` builds events with no RequestID,
`agenttest/mocks.go:61`); prove it with a regression test rather than removing it. (2) `agui.indexByte`
and `agui.stringList` (`internal/agui/governance_api.go:226,199`) — reinvented stdlib; swap to
`strings.IndexByte` and inline the slice copy while preserving the non-nil-empty allowlist. (3) the
`truncateRunes` 5-line dup (`assets/context.go:133`, `rerank/client.go:167`) — record the
accept-vs-fold decision (default: accept-with-cross-reference-comment per Open Question #2).

Purpose: ROADMAP C1 — remove reinvented-stdlib/placeholders while protecting the two named items that
triage proved are NOT dead.
Output: a load-bearing-RequestID test, stdlib-based agui parsing with a preserved allowlist contract,
and a recorded truncateRunes resolution.

D-14: execute sequentially, no git worktrees. D-11: one commit per item (RequestID test; agui swap;
truncateRunes resolution). The `gsd` commit wrapper times out on the ~34s file-size hook — fall back
to direct `git commit`. All verify commands run in WSL (never run .exe on the Windows host).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@cmd/aura/agent.go
@internal/agent/agenttest/mocks.go
@internal/agui/governance_api.go
</context>

## Artifacts this phase produces (this plan)

- `cmd/aura/agent_test.go` — new/extended regression test asserting every dry-run event carries `requestID`.
- (modified, no new symbol) `internal/agui/governance_api.go` — `indexByte` deleted, `stringList` inlined.
- (modified, no new symbol) `internal/assets/context.go`, `internal/rerank/client.go` — cross-reference comments on the `truncateRunes` dup (accept branch).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove cmd/aura/agent.go:127 RequestID re-stamp is load-bearing (KEEP + test)</name>
  <files>cmd/aura/agent_test.go, cmd/aura/agent.go</files>
  <read_first>
    - cmd/aura/agent.go:85-133 (the dry-run path; line 127 `ev.RequestID = requestID`)
    - internal/agent/agenttest/mocks.go:54-71 (InfiniteToolCallAgent.Run builds &agent.Event{Author,Branch,LLMResponse} with NO RequestID)
    - 32-RESEARCH.md § Triage row T1; 32-VALIDATION.md "RequestID stamp load-bearing on dry-run"
  </read_first>
  <behavior>
    - Test: running the dry-run agent path, EVERY emitted event has `RequestID == requestID` (non-zero, the uniform stamp) — GREEN with line 127 present.
    - Test (documenting): the fake `InfiniteToolCallAgent` emits events with a zero/empty RequestID, so line 127 is the only uniform stamp on the dry-run path (this is why removal would emit "00000000-..." on every line and break SC#4 reproducibility).
  </behavior>
  <action>
    Add a regression test in `cmd/aura/agent_test.go` (use `-run Dry` selectable name) that exercises the
    dry-run path and asserts every produced event's `RequestID` equals the supplied requestID. Do NOT
    remove line 127. Clarify the in-code comment at `cmd/aura/agent.go:127` to state that the FAKE agent
    (`agenttest.InfiniteToolCallAgent`) does not stamp RequestID, so this re-stamp is the uniform source
    on the dry-run path (the real `LlmAgent.newEvent` copies it, the fake does not). Record in the
    SUMMARY that this named QUAL-02 item resolved to KEEP.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race -run Dry ./cmd/aura/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -run Dry ./cmd/aura/` exits 0 with the new assertion present.
    - The test fails if line 127 is removed (load-bearing proof) — note this in the SUMMARY (do not commit the removal).
    - `rg -n 'ev.RequestID = requestID' cmd/aura/agent.go` still returns the line.
  </acceptance_criteria>
  <done>A dry-run regression test pins the RequestID stamp; the comment is clarified; the SUMMARY records the KEEP verdict for this named QUAL-02 item.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap agui.indexByte → strings.IndexByte and inline stringList (preserve non-nil allowlist)</name>
  <files>internal/agui/governance_api.go, internal/agui/governance_api_test.go</files>
  <read_first>
    - internal/agui/governance_api.go:170-231 (envChips @:209-222 uses indexByte @:226; NetworkAllowlist @:68/:177 uses stringList @:199)
    - 32-PATTERNS.md § "internal/agui/governance_api.go — stdlib swaps"
    - 32-RESEARCH.md § Triage rows T3/T4 + Pitfall 4
  </read_first>
  <behavior>
    - Test: env-chip parsing for "k=v"→key "k", "="→key "", "novalue"→whole entry (no split), ""→no split — byte-identical to the pre-swap envChips output.
    - Test: marshalled NetworkAllowlist for an empty Runtime.Network is `[]` (non-nil), NOT `null`; for N elements it is the same list.
  </behavior>
  <action>
    Replace the `indexByte(entry, '=')` call at `:213` with `strings.IndexByte(entry, '=')` (or
    `strings.Cut`) and delete the local `indexByte` helper (:224-231). Inline `stringList(server.Runtime.Network)`
    at the `:177` NetworkAllowlist assignment as `append([]string{}, server.Runtime.Network...)` and delete
    the `stringList` helper (:199-203). CRITICAL: keep the `[]string{}` literal (non-nil) so an empty
    allowlist marshals `[]` not `null`. Add/extend a test in `governance_api_test.go` asserting (a) envChips
    output unchanged across the union cases and (b) empty allowlist → `[]` in the marshalled JSON. Run
    refactor-on-touch dead-code/dup checks on the file.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go test -race ./internal/agui/ && deadcode -test ./internal/agui/ 2>/dev/null | grep -E 'indexByte|stringList' ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/agui/` exits 0.
    - `rg -n 'func indexByte|func stringList' internal/agui/governance_api.go` returns nothing.
    - A test asserts empty `Runtime.Network` → marshalled `NetworkAllowlist` is `[]` (never `null`).
    - `rg -n 'strings.IndexByte|strings.Cut' internal/agui/governance_api.go` returns the swap.
  </acceptance_criteria>
  <done>The two reinvented-stdlib helpers are gone; envChips output and the non-nil allowlist contract are preserved by tests; the file passes refactor-on-touch checks.</done>
</task>

<task type="auto">
  <name>Task 3: Record the truncateRunes dup resolution (default: accept-with-cross-reference comment)</name>
  <files>internal/assets/context.go, internal/rerank/client.go</files>
  <read_first>
    - internal/assets/context.go:133 (truncateRunes, param `value`)
    - internal/rerank/client.go:167 (truncateRunes, param `s`)
    - 32-RESEARCH.md § Triage row T8 + Open Question #2 (no internal/strutil home exists; a new util needs coverage registration)
  </read_first>
  <action>
    Apply the planner default (Open Question #2): ACCEPT the 5-line duplication with a one-line
    cross-reference comment on each copy noting the sibling (e.g. "duplicated 5-liner; see
    internal/rerank/client.go truncateRunes — folding deferred, audit accepts at this scale"). Do NOT
    create `internal/strutil` (it would itself need coverage-gate registration for a 5-liner). Record the
    decision and its rationale in the SUMMARY so the QUAL-02 named item is explicitly resolved, not dropped.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go build ./internal/assets/ ./internal/rerank/ && rg -n 'truncateRunes' internal/assets/context.go internal/rerank/client.go</automated>
  </verify>
  <acceptance_criteria>
    - Both `truncateRunes` copies carry a cross-reference comment; no `internal/strutil` package was created.
    - `go build ./internal/assets/ ./internal/rerank/` exits 0.
    - The SUMMARY records the accept-vs-fold decision and rationale.
  </acceptance_criteria>
  <done>The truncateRunes QUAL-02 item is resolved by an explicit, documented decision (accept) with build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → agui governance API | The NetworkAllowlist field is serialized to the cockpit; its marshalled shape must not silently change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-AL | Tampering / Information | agui NetworkAllowlist marshaling | mitigate | Inline preserves `append([]string{},...)` (non-nil); a JSON test asserts empty → `[]` not `null` (Pitfall 4). |
| T-32-02-DEF | (deferred security work) | MCP trust-norm / decode*Body in agui | accept | Out of scope (QA-C-03→38, QA-C-01→38/40); do NOT merge — flagged as prohibition. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `go test -race ./cmd/aura/ ./internal/agui/` exits 0 (WSL).
- `deadcode -test ./internal/agui/` no longer reports `indexByte`/`stringList`.
- The empty-allowlist-`[]` JSON test passes; RequestID dry-run test passes.
- truncateRunes resolution recorded.
</verification>

<success_criteria>
ROADMAP C1 (partial): reinvented-stdlib `agui.indexByte`/`stringList` removed (stdlib + inline) with the
allowlist contract preserved; the RequestID item resolved to KEEP with a load-bearing regression test;
truncateRunes resolved by documented decision.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-02-SUMMARY.md` when done.
</output>
