---
phase: 32-quality-cleanup-dead-code-shared-helpers
plan: 08
type: execute
wave: 2
depends_on: ["32-04"]
files_modified:
  - web/src/conversations/useConversations.ts
  - web/src/governance/governanceApi.ts
  - web/src/board/BoardLayout.tsx
  - web/src/governance/McpLifecycleCluster.tsx
  - web/src/conversations/ConversationSidebar.tsx
  - web/src/search/SearchPanel.tsx
  - web/src/governance/governanceView.tsx
  - web/src/components/ui/skeleton.tsx
  - internal/webui/dist
autonomous: true
requirements: [QUAL-03]
user_setup: []

must_haves:
  truths:
    - "getJSON has a single source (web/src/api/json.ts); the two byte-identical copies in useConversations.ts and governanceApi.ts are deleted and import the canonical one; anything importing getJSON from governanceApi is repointed."
    - "BoardLayout and McpLifecycleCluster adopt the canonical web/src/a11y/focusTrap.ts (fixing the buggy inline copies — disabled-element skip + non-button focusables); their consumer tests stay green."
    - "One skeleton system remains (the rich components/skeleton/Skeleton.tsx); the 3 shadcn consumers are migrated and components/ui/skeleton.tsx is retired."
    - "internal/webui/dist is rebuilt and committed; web-dist-freshness stays green; vitest per-file coverage and Stryker thresholds hold."
  artifacts:
    - path: "web/src/conversations/useConversations.ts"
      provides: "imports canonical getJSON (local copy removed)"
    - path: "web/src/board/BoardLayout.tsx"
      provides: "uses canonical focusTrap (inline buggy copy removed)"
  key_links:
    - from: "web/src/governance/governanceApi.ts"
      to: "web/src/api/json.ts"
      via: "import { getJSON }"
      pattern: "from '\\.\\./api/json'"
  prohibitions:
    - "Do NOT preserve the buggy inline focus-trap behavior — adopt the canonical util (it fixes a11y: BoardLayout omits the disabled filter, McpLifecycleCluster queries only button)."
    - "Do NOT skip the dist rebuild — any web/src change without a rebuilt internal/webui/dist fails web-dist-freshness (Pitfall 3, bit Phase 31)."
---

<objective>
Collapse the frontend duplication (QUAL-03 + D-08), the highest-visual-regression item of the phase:
(1) `getJSON` (QA-D-01) — two byte-identical copies (`useConversations.ts:61-70`,
`governanceApi.ts:113-122`) collapse to the canonical `web/src/api/json.ts`;
(2) `focusTrap` (QA-D-02) — the inline copies in `BoardLayout.tsx:56-91` and `McpLifecycleCluster.tsx:208-238`
are BUGGY (missing disabled-filter / button-only query); adopt the canonical `web/src/a11y/focusTrap.ts`
(this FIXES latent a11y defects, not strict byte-parity);
(3) skeleton unification (QA-D-08 / D-08) — keep the rich `components/skeleton/Skeleton.tsx`, migrate the
3 shadcn consumers (`ConversationSidebar`, `SearchPanel`, `governanceView`), retire
`components/ui/skeleton.tsx`.

Purpose: ROADMAP C2 — one shared frontend helper per concern, fixing the a11y bugs the inline copies
carried.
Output: single getJSON import, canonical focusTrap adopted, one skeleton system, dist rebuilt+committed,
all web gates green.

D-14: execute sequentially, no git worktrees (this depends on 32-04, which last rebuilt dist). D-11:
one commit per concept (getJSON+focusTrap dedup; skeleton unification) — each rides with a dist rebuild.
The `gsd` commit wrapper times out on the file-size hook — use direct `git commit`. Web tests/build run
in WSL/Node (never run .exe on the Windows host); before web tests unset `.env`'s
`AURA_WEB_AUTH_SECRET` if it leaks (MEMORY).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-CONTEXT.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-RESEARCH.md
@.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-PATTERNS.md
@web/src/api/json.ts
@web/src/a11y/focusTrap.ts
</context>

## Artifacts this phase produces (this plan)

- (modified, no new symbol) `web/src/conversations/useConversations.ts`, `web/src/governance/governanceApi.ts` — import canonical `getJSON` (copies removed).
- (modified, no new symbol) `web/src/board/BoardLayout.tsx`, `web/src/governance/McpLifecycleCluster.tsx` — adopt canonical `focusTrap`.
- (modified) skeleton consumers migrated; `web/src/components/ui/skeleton.tsx` retired.
- (rebuilt artifact) `internal/webui/dist`.

<tasks>

<task type="auto">
  <name>Task 1: Collapse getJSON to the canonical web/src/api/json.ts and adopt canonical focusTrap</name>
  <files>web/src/conversations/useConversations.ts, web/src/governance/governanceApi.ts, web/src/board/BoardLayout.tsx, web/src/governance/McpLifecycleCluster.tsx, internal/webui/dist</files>
  <read_first>
    - web/src/api/json.ts:1-23 (canonical getJSON + postJSON)
    - web/src/conversations/useConversations.ts:55-110 (local getJSON :61-70 + hand-rolled createConversation POST :84-104)
    - web/src/governance/governanceApi.ts:105-139 (exported getJSON :113-122 — check external importers)
    - web/src/a11y/focusTrap.ts (canonical focusFirstDescendant/trapTabKey with isFocusable disabled filter)
    - web/src/board/BoardLayout.tsx:56-91 (inline copy omits disabled filter) + web/src/governance/McpLifecycleCluster.tsx:208-238 (RemoveDialog queries only button)
    - 32-PATTERNS.md § Web Dedup Targets + 32-RESEARCH.md § Web getJSON/focusTrap rows + Pitfall 3
  </read_first>
  <action>
    Delete the local `getJSON` from `useConversations.ts` (:61-70) and `governanceApi.ts` (:113-122);
    import `{ getJSON }` from `../api/json` in both. Repoint anything that imported `getJSON` FROM
    `governanceApi` (it was exported) to `../api/json`. Opportunistic refactor-on-touch: fold
    `useConversations.createConversation`'s hand-rolled POST (:84-104) to `postJSON` (QA-D-04). Replace the
    inline focus-trap logic in `BoardLayout.tsx` and `McpLifecycleCluster.tsx` (RemoveDialog) with the
    canonical `focusFirstDescendant`/`trapTabKey` from `web/src/a11y/focusTrap.ts` (this fixes the
    disabled-filter and button-only-query bugs). Keep the consumer tests green. Rebuild + commit
    `internal/webui/dist`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura/web && npm test && npm run lint && npm run deadcode && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'function getJSON|const getJSON' web/src/conversations/useConversations.ts web/src/governance/governanceApi.ts` returns nothing.
    - `rg -n "from '\.\./api/json'" web/src/conversations/useConversations.ts web/src/governance/governanceApi.ts` shows the canonical import in both.
    - BoardLayout and McpLifecycleCluster reference the canonical focusTrap util; `npm test` (incl. BoardLayout.test.tsx, McpLifecycleCluster) exits 0.
    - `npm run deadcode` (knip) reports no orphaned getJSON/focusTrap; `npm run build` exits 0; `internal/webui/dist` rebuilt.
  </acceptance_criteria>
  <done>getJSON has one source, the buggy inline focus traps are replaced by the canonical a11y util, consumer tests pass, knip clean, dist fresh.</done>
</task>

<task type="auto">
  <name>Task 2: Unify the skeleton system (keep rich Skeleton.tsx, retire shadcn ui/skeleton.tsx)</name>
  <files>web/src/conversations/ConversationSidebar.tsx, web/src/search/SearchPanel.tsx, web/src/governance/governanceView.tsx, web/src/components/ui/skeleton.tsx, internal/webui/dist</files>
  <read_first>
    - web/src/components/skeleton/Skeleton.tsx (rich, 349 LOC, CSS-wave, --skeleton-* tokens — KEEP)
    - web/src/components/ui/skeleton.tsx (13 LOC shadcn animate-pulse — RETIRE)
    - the 3 shadcn consumers: ConversationSidebar, SearchPanel, governanceView
    - 32-PATTERNS.md § Web Dedup skeleton row + Assumption A4 (reverse choice defensible) + 32-RESEARCH.md § Skeleton unification
  </read_first>
  <action>
    Migrate the 3 shadcn skeleton consumers (`ConversationSidebar`, `SearchPanel`, `governanceView`) to the
    rich `components/skeleton/Skeleton.tsx` system (per Assumption A4 — KEEP the rich one). Delete
    `web/src/components/ui/skeleton.tsx`. If a barrel export (QA-D-13/D-18) now becomes real, fold that in
    (refactor-on-touch). Run Playwright E2E on the affected loading states to assert no visual regression,
    then rebuild + commit `internal/webui/dist`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura/web && npm test && npm run lint && npm run deadcode && npm run build && npx playwright test --grep skeleton 2>/dev/null || npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/components/ui/skeleton.tsx` is deleted; `rg -n "components/ui/skeleton" web/src/` returns nothing.
    - The 3 consumers import from `components/skeleton/Skeleton`; `npm test` + `npm run deadcode` (knip) exit 0.
    - Playwright E2E on the loading states passes (no visual regression); `npm run build` exits 0; `internal/webui/dist` rebuilt.
  </acceptance_criteria>
  <done>One skeleton system remains, the 3 consumers migrated, the shadcn copy retired, Playwright confirms visual parity, dist fresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cockpit API (getJSON) | The shared HTTP read helper crosses the client→API boundary; its error/parse behavior must be the canonical one. |
| user → focus management (a11y) | The focus trap governs keyboard navigation; the buggy copies were an a11y defect being fixed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08-A11Y | Repudiation/Usability | inline focus traps | mitigate | Adopt the canonical focusTrap (fixes disabled-filter + button-only bugs); consumer tests + Playwright assert behavior. |
| T-32-08-DIST | Tampering | internal/webui/dist freshness | mitigate | Rebuild + commit dist with each web commit (Pitfall 3). |
| T-32-08-VIS | Tampering | skeleton visual regression | mitigate | Playwright E2E on loading states + the rich-system migration (Assumption A4). |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — cleanup-only, nothing to audit. |
</threat_model>

<verification>
- `cd web && npm test && npm run lint && npm run deadcode && npm run build` all exit 0; vitest per-file >=85% + Stryker >=70% hold.
- Playwright E2E on the skeleton loading states passes.
- `internal/webui/dist` rebuilt + committed (web-dist-freshness green); no getJSON/focusTrap/ui-skeleton copies remain.
</verification>

<success_criteria>
ROADMAP C2 (partial): the frontend has one getJSON, one (canonical, bug-fixed) focusTrap, and one
skeleton system, with consumer tests + Playwright green and a fresh dist bundle.
</success_criteria>

<output>
Create `.planning/phases/32-quality-cleanup-dead-code-shared-helpers/32-08-SUMMARY.md` when done.
</output>
