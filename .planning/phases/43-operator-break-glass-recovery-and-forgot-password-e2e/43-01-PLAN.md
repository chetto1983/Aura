---
phase: 43-operator-break-glass-recovery-and-forgot-password-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/breakglass/guard.go
  - internal/breakglass/guard_test.go
  - internal/breakglass/source.go
  - internal/breakglass/source_test.go
autonomous: true
requirements: [R2, R3, R6]
must_haves:
  truths:
    - "cardinality(zero) R2: selectSoleOperator over 0 kind='user' identities returns an error and NO identity (the orchestrator refuses → ZERO writes downstream)"
    - "cardinality(many) R2: selectSoleOperator over >1 ACTIVE kind='user' identities returns an error (the > 1 ambiguity guard counts ACTIVE operators per D-11 — a deactivated stale identity does NOT trip refusal against the lone active operator)"
    - "D-11 deactivated rule (LOCKED): exactly one active operator → return it (even alongside deactivated stragglers); zero active + exactly one total user → return that lone deactivated operator (a plausible recoverable lockout); zero active + >1 total → error; zero total → error"
    - "empty/whitespace input R3: Sourcer.Source rejects an empty/whitespace password (and, when re-seeding, an empty/whitespace answer) with an error BEFORE returning any Secrets — no DB call is ever reached"
    - "source conflict R3: AURA_RECOVERY_PASSWORD env set together with --generate → Source returns an error (never silently picks one)"
    - "no-input environment R3: non-TTY (IsTTY()==false) with no AURA_RECOVERY_PASSWORD and no --generate → Source returns an error (cannot prompt)"
    - "--generate R3: Source emits exactly one secret line to the injected Stdout, the emitted password is ≥20 chars over the [A-Za-z0-9_-] charset, and the SAME value never appears in the injected Stderr"
  artifacts:
    - path: "internal/breakglass/guard.go"
      provides: "selectSoleOperator([]identity.Identity) (identity.Identity, error) — pure operator-resolution guard with the D-11 active/deactivated rule"
      contains: "selectSoleOperator"
    - path: "internal/breakglass/source.go"
      provides: "Sourcer + Secrets: password/question/answer sourcing (env / --generate / injected hidden prompt) with the conflict/non-TTY/empty decision tree"
      contains: "func (s Sourcer) Source"
    - path: "internal/breakglass/guard_test.go"
      provides: "table test covering 0 / 1-active / >1-active / active+deactivated / lone-deactivated / >1-deactivated / non-user-kind excluded"
      contains: "selectSoleOperator"
    - path: "internal/breakglass/source_test.go"
      provides: "table test covering env / --generate length+charset+single-emission / env+generate conflict / non-TTY / empty-whitespace / no-leak-in-errors-or-stderr"
      contains: "Source"
  key_links:
    - from: "internal/breakglass/guard.go"
      to: "internal/identity.Identity"
      via: "filter Kind=='user' + Deactivated"
      pattern: "identity\\.Identity"
    - from: "internal/breakglass/source.go"
      to: "crypto/rand"
      via: "generateSecret → base64.RawURLEncoding(rand.Read)"
      pattern: "crypto/rand"
  prohibitions:
    - "MUST NOT interpolate the sourced/generated password or answer into any returned error string or into the injected Stderr — only the single --generate Stdout line may carry a secret (verify: source_test captures Stderr + every error and asserts the sentinel is absent)"
    - "MUST NOT reach a DB call on any invalid-input path — every conflict/non-TTY/empty case returns an error from Source before the orchestrator would touch Postgres (this plan's functions take NO pool)"
---

<objective>
Create the new `internal/breakglass` package with its two pure, unit-testable primitives — the operator-resolution guard (`selectSoleOperator`, R2/D-11) and the password + recovery-Q&A sourcing engine (`Sourcer.Source`, R3/D-03) — plus their table tests. This is the Wave-0 package skeleton whose existence makes `internal/breakglass` a coverage-gate target (`scripts/coverage_gate.sh ./internal/...`, D-09): every symbol here is counted toward the 85% owned-surface floor and needs no DB or TTY to test.

Purpose: Land the two highest-branch-density, DB-free decision trees as pure functions so R2 (0/1/>1 guard, deactivated-aware) and R3 (source conflict / non-TTY / empty / generate) are fully proven in fast unit tests before any orchestration or Authula wiring exists (interface-first: Waves 2–3 consume these symbols).
Output: `internal/breakglass/{guard,source}.go` + `{guard,source}_test.go`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-SPEC.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-CONTEXT.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-PATTERNS.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-VALIDATION.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-structs-interfaces/SKILL.md
@.claude/skills/golang-error-handling/SKILL.md
</context>

<artifacts_produced>
This plan creates the NEW package `internal/breakglass` and these NEW symbols (source-grounding may exclude them from drift verification — they do not exist upstream):
- `internal/breakglass/guard.go` — package-private `selectSoleOperator(ids []identity.Identity) (identity.Identity, error)`.
- `internal/breakglass/source.go` — exported `Secrets` struct (`Password`, `Question`, `Answer`, `Generated bool`); exported `Sourcer` struct with injectable seams (`Getenv func(string) string`, `IsTTY func() bool`, `ReadHidden func(prompt string) (string, error)`, `Stdout io.Writer`, `Stderr io.Writer`); exported method `func (s Sourcer) Source(generate, noRecovery bool) (Secrets, error)`; package-private `generateSecret(nBytes int) (string, error)`; the three env-var name constants `AURA_RECOVERY_PASSWORD` / `AURA_RECOVERY_QUESTION` / `AURA_RECOVERY_ANSWER`; a fixed default question constant used by `--generate`.
- `internal/breakglass/{guard,source}_test.go` — white-box (`package breakglass`) table tests.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: guard.go — selectSoleOperator with the D-11 active/deactivated rule (R2)</name>
  <files>internal/breakglass/guard.go, internal/breakglass/guard_test.go</files>
  <read_first>
    - internal/identity/store.go:59-94 — the `Identity` struct (`ID, Name, Kind, Deactivated bool`; DC-2/0029) and `ListIdentities`; the guard consumes `[]identity.Identity`, filters `Kind=="user"`, and reads `Deactivated`.
    - internal/db/migrations/0004_identity.up.sql — the `kind IN ('system','user','channel','service')` CHECK and the seeded `local` row (kind='system'), which the `kind=='user'` filter correctly excludes.
    - 43-PATTERNS.md § "guard.go" + § "Drift Corrections DC-2" — the net-new pure filter and the mandate to add a `Deactivated:true` row to the test matrix.
    - 43-RESEARCH.md "Operator-count guard as a pure function" (Code Examples) + Pitfall 6 — the base shape and why >1 is a deliberate hard stop.
    - .claude/skills/golang-testing/SKILL.md § Table-Driven Tests — named subtests, one case per row.
  </read_first>
  <behavior>
    - 0 identities → error, zero-value Identity.
    - 1 kind='user' active → returns that identity.
    - 2 kind='user' active → error (ambiguous active operators).
    - 1 active + 1 deactivated (both kind='user') → returns the ACTIVE one (deactivated does not trip the >1 refusal).
    - 1 kind='user' deactivated, 0 active → returns that lone deactivated identity (recoverable lockout).
    - 2 kind='user' deactivated, 0 active → error (ambiguous, nothing active to prefer).
    - a kind='system'/'channel'/'service' identity present is never selected or counted (e.g. seeded `local` is kind='system').
  </behavior>
  <action>
    Create `internal/breakglass/guard.go` (`package breakglass`, importing `internal/identity`) with package-private `selectSoleOperator(ids []identity.Identity) (identity.Identity, error)`. LOCK this exact rule (D-11): partition `ids` filtered to `Kind=="user"` into `active` (`!Deactivated`) and the full `users` set. Then: if `len(active)==1` return `active[0]`; if `len(active)>1` return an error like `"N active operator identities found; refusing (break-glass targets a single active operator)"` (use `fmt.Errorf` with the count); if `len(active)==0` then — `len(users)==1` return `users[0]` (the lone deactivated operator is recoverable), `len(users)==0` return `errors.New("no operator (kind='user') identity found")`, `len(users)>1` return an error naming the deactivated-ambiguous case. Error strings lowercase, no trailing punctuation, no secret (golang-error-handling). Then create `guard_test.go` (white-box `package breakglass`) as a table test with one named subtest per <behavior> row, asserting both the returned identity (by ID) and whether an error is present.
  </action>
  <acceptance_criteria>
    - `internal/breakglass/guard.go` declares `package breakglass` and `selectSoleOperator(ids []identity.Identity) (identity.Identity, error)`.
    - The active/deactivated rule matches the <behavior> table exactly (a single active operator is chosen even when deactivated stragglers exist; a lone deactivated operator is returned; >1 active OR >1 deactivated-with-zero-active errors).
    - `guard_test.go` is a table test with a named subtest for every <behavior> row, including a `Deactivated:true` row and a non-'user' kind row.
    - `cd D:/Repo/Aura && go test ./internal/breakglass/ -run TestSelectSoleOperator -v` passes; no error string contains a password/answer.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/breakglass/ && go build ./internal/breakglass/ && go test ./internal/breakglass/ -run TestSelectSoleOperator</automated>
  </verify>
  <done>selectSoleOperator resolves exactly one operator under the locked D-11 active/deactivated rule and refuses 0 / ambiguous states; the table test covers every cardinality × deactivated combination and excludes non-'user' kinds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: source.go — password + recovery Q&A sourcing (env / --generate / hidden prompt) with the conflict decision tree (R3, D-03)</name>
  <files>internal/breakglass/source.go, internal/breakglass/source_test.go</files>
  <read_first>
    - cmd/aura/serve_password_reset.go:531-537 — `newPasswordResetToken()` (`crypto/rand.Read` + `base64.RawURLEncoding`), the generator template; use 24 bytes → 32 chars (≥20).
    - 43-RESEARCH.md "Hidden password prompt with non-TTY detection" + "Strong generated password" (Code Examples) + Pitfall 5 — the x/term seam shape and the stdout-only / never-slog secret discipline. NOTE: this package stays x/term-free — the REAL term.IsTerminal/term.ReadPassword are injected by the Wave-3 glue; source.go only holds function-field seams.
    - cmd/aura/recovery.go:53-55 — the "stdout only — NEVER slog" single-emission discipline to mirror for --generate.
    - 43-PATTERNS.md § "source.go" + § "No Analog Found" — generator has a template; the hidden-prompt + conflict tree is net-new pure logic (highest branch density → the mutation spot-check target).
    - .claude/skills/golang-structs-interfaces/SKILL.md § "Make the Zero Value Useful" / function-field seams — inject Getenv/IsTTY/ReadHidden/Stdout/Stderr rather than premature interfaces.
    - .claude/skills/golang-testing/SKILL.md § Table-Driven Tests + capturing output into a bytes.Buffer.
  </read_first>
  <behavior>
    - env only: AURA_RECOVERY_PASSWORD set, no --generate, IsTTY irrelevant → Secrets.Password == env value; Generated==false; nothing written to Stdout/Stderr.
    - env Q&A: AURA_RECOVERY_QUESTION + AURA_RECOVERY_ANSWER set → Secrets.Question/Answer from env (when noRecovery==false).
    - --generate: no env password → Secrets.Password is a fresh ≥20-char [A-Za-z0-9_-] value, Secrets.Answer is a fresh random value paired with the fixed default question, Generated==true, and EXACTLY ONE secret line is written to Stdout carrying both; the same values are NOT in Stderr.
    - conflict: AURA_RECOVERY_PASSWORD env AND --generate both set → error, no Secrets.
    - non-TTY no-source: IsTTY()==false, no env password, no --generate → error (cannot prompt).
    - empty/whitespace: env password is "" or all-whitespace → error before returning; likewise an empty/whitespace answer when noRecovery==false → error.
    - noRecovery==true: the Q&A is NOT sourced (no answer prompt/env read) and no answer-empty error is raised.
  </behavior>
  <action>
    Create `internal/breakglass/source.go` (`package breakglass`). Define exported `Secrets{Password, Question, Answer string; Generated bool}` and exported `Sourcer{Getenv func(string) string; IsTTY func() bool; ReadHidden func(prompt string)(string,error); Stdout io.Writer; Stderr io.Writer}`. Define the three env-name consts (`AURA_RECOVERY_PASSWORD`, `AURA_RECOVERY_QUESTION`, `AURA_RECOVERY_ANSWER`) and a fixed default-question const for --generate. Implement package-private `generateSecret(nBytes int) (string, error)` mirroring `newPasswordResetToken` (crypto/rand + base64.RawURLEncoding; call with 24). Implement `func (s Sourcer) Source(generate, noRecovery bool) (Secrets, error)` as the decision tree in <behavior>: resolve the password first (env vs generate vs `ReadHidden` prompt-with-confirm via Stderr, using IsTTY to reject the non-TTY-no-source case), reject the env+generate conflict and any empty/whitespace value up front; then, unless noRecovery, resolve the Q&A symmetrically (env pair vs generated random answer + default question vs hidden prompt). When Generated, write the single secret line to `s.Stdout` (the ONLY place a secret is emitted); never write a secret to `s.Stderr` and never interpolate one into an error (golang-error-handling `%w`, lowercase). Then create `source_test.go` (white-box) as a table test: inject a fake `Getenv` map, `IsTTY` bool, a `ReadHidden` stub, and `bytes.Buffer` Stdout/Stderr; cover every <behavior> row. For --generate assert exactly one Stdout line, the emitted password matches `^[A-Za-z0-9_-]{20,}$`, and neither the emitted password nor answer appears in Stderr. For the env path use a fixed sentinel and assert it never appears in Stderr nor in any returned error (`!strings.Contains`).
  </action>
  <acceptance_criteria>
    - `source.go` declares `Secrets`, `Sourcer` (with the five injectable seams), `Source(generate, noRecovery bool)`, `generateSecret`, and the `AURA_RECOVERY_PASSWORD`/`AURA_RECOVERY_QUESTION`/`AURA_RECOVERY_ANSWER` consts + a default-question const; it imports NO `golang.org/x/term` (seams only).
    - env+generate → error; non-TTY+no-source → error; empty/whitespace password (and answer when !noRecovery) → error; each before returning Secrets.
    - `--generate` writes exactly one Stdout line; the emitted password is ≥20 chars over `[A-Za-z0-9_-]`; the generated values are absent from Stderr.
    - The no-leak assertion holds: a sentinel env password never appears in the captured Stderr buffer nor in any returned error string.
    - `cd D:/Repo/Aura && go test ./internal/breakglass/ -run TestSource` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./internal/breakglass/ && go build ./internal/breakglass/ && go test ./internal/breakglass/ -run TestSource</automated>
  </verify>
  <done>Sourcer.Source resolves the password + recovery Q&A across env/--generate/hidden-prompt, rejects the conflict/non-TTY/empty cases before any write, emits a generated secret exactly once to Stdout, and never leaks a secret to Stderr or an error — all proven by the injected-seam table test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin/env → sourced secret | The operator-supplied password/answer enters via env or the (Wave-3-injected) hidden prompt; from here it must reach only the argon2 hasher, never a log/stderr/error. |
| identity roster → operator selection | `[]identity.Identity` read from `aura.identities` is the input the guard trusts to pick exactly one operator; ambiguity is refused, not guessed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information disclosure | source.go secret handling | mitigate | The only secret emission is the single `--generate` Stdout line; source_test captures Stderr + every returned error and asserts the sentinel is absent (no `%s`-into-error, no slog) |
| T-43-07 | Elevation of privilege | selectSoleOperator resolution | mitigate | Guard resolves exactly one active operator and refuses 0/ambiguous state (D-11 rule); the caller performs ZERO writes when the guard errors |
| T-43-08 | Denial of service (weak generated secret) | generateSecret | mitigate | `crypto/rand` (never math/rand) + base64.RawURLEncoding, 24 bytes → 32 chars; test asserts ≥20-char `[A-Za-z0-9_-]` |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO package (RESEARCH § Package Legitimacy Audit: zero net-new deps; x/term is promoted later in Wave 3, already in go.sum) |
</threat_model>

<verification>
- `cd D:/Repo/Aura && go vet ./internal/breakglass/ && go build ./internal/breakglass/ && go test -race ./internal/breakglass/` green (unit, <5s; `-race` per post-edit validation).
- Guard covers 0 / 1-active / >1-active / active+deactivated / lone-deactivated / >1-deactivated / non-'user'-kind.
- Source covers env / --generate(length+charset+single-emission) / env+generate conflict / non-TTY / empty-whitespace / no-leak-in-Stderr-or-errors.
</verification>

<success_criteria>
- `internal/breakglass` exists and compiles; the coverage gate will now discover it under `./internal/...` (D-09).
- `selectSoleOperator` implements the locked D-11 rule; `Sourcer.Source` implements the R3 decision tree with injectable seams; both are DB-free and TTY-free.
- Every <behavior> row is a named subtest; no secret appears in any error or Stderr; `-race` clean.
</success_criteria>

<output>
Create `.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-01-SUMMARY.md` when done.
</output>
