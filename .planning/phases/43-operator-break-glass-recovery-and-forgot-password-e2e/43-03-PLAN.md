---
phase: 43-operator-break-glass-recovery-and-forgot-password-e2e
plan: 03
type: execute
wave: 3
depends_on: ["43-02"]
files_modified:
  - cmd/aura/recover_operator.go
  - cmd/aura/identity.go
  - go.mod
  - go.sum
autonomous: true
requirements: [R1, R3, R4]
must_haves:
  truths:
    - "R1 exit path: `aura identity recover-operator` on a healthy single-operator deployment resolves the operator, resets the password, kills sessions, re-seeds recovery, prints an `ok:` success line (no secret) and exits 0"
    - "R3 sourcing wiring: the glue builds the Plan-43-01 Sourcer with the REAL seams — os.Getenv, term.IsTerminal(os.Stdin.Fd()), term.ReadPassword, os.Stdout, os.Stderr — so env / --generate / hidden-prompt all work end-to-end; a source error (env+--generate conflict, non-TTY-no-source, empty) prints to stderr (no secret) and exits non-zero"
    - "R4 --no-recovery wiring: passing --no-recovery sets Deps.NoRecovery=true (skips the re-seed) and prints a warning that online forgot-password stays unavailable until reconfigured"
    - "D-05 sibling dispatch: a new `case \"recover-operator\":` in runIdentity's switch calls identityRecoverOperator(ctx, pool, cfg, args[1:]); the existing `recover <name>` branch is untouched; identityUsage lists both with a one-line disambiguation"
    - "the command loads config via the existing LLM-free config.LoadDB() (already populates AuthulaSecret + AuthulaDatabaseURL) — no OPENROUTER_API_KEY required; an empty AURA_AUTHULA_SECRET yields a clear stderr error (webauth.New requires it)"
  artifacts:
    - path: "cmd/aura/recover_operator.go"
      provides: "identityRecoverOperator CLI glue: hand-rolled --generate/--no-recovery parse → Sourcer → NewAuthula+CoreServices → breakglass.RecoverOperator → single --generate stdout emission + ok/exit"
      contains: "identityRecoverOperator"
    - path: "cmd/aura/identity.go"
      provides: "runIdentity switch gains `recover-operator` (D-05) + identityUsage disambiguation; passes cfg into the branch"
      contains: "recover-operator"
    - path: "go.mod"
      provides: "golang.org/x/term promoted from indirect to a direct require (used by the glue's hidden-prompt seams)"
      contains: "golang.org/x/term"
  key_links:
    - from: "cmd/aura/recover_operator.go"
      to: "internal/breakglass.RecoverOperator"
      via: "Deps{Pool, Core, NoRecovery} + Secrets"
      pattern: "breakglass\\.RecoverOperator"
    - from: "cmd/aura/identity.go"
      to: "identityRecoverOperator"
      via: "case \"recover-operator\" in runIdentity"
      pattern: "recover-operator"
  prohibitions:
    - "MUST NOT emit the password/answer anywhere except the single --generate stdout line — the ok/success line names no secret, errors are printed with no secret, argv carries no secret (the password is never a CLI argument); mirror recovery.go:53-55."
    - "MUST NOT use cobra — parse --generate/--no-recovery hand-rolled (a manual arg loop or std flag.FlagSet), mirroring the runIdentity/runDB switch-tree deviation (CLAUDE.md; identity.go:1-16)."
    - "MUST NOT add a live-`aura` name refusal to the command — it MUST run against live `aura` in production (D-08 scope note); the refusal lives only in the db_integration test."
    - "MUST NOT touch the existing `recover <name>` token-mint path (recovery.go) — recover-operator is a sibling, not a replacement (D-05)."
---

<objective>
Wire the break-glass logic into the CLI: a new `cmd/aura/recover_operator.go` (`identityRecoverOperator`) that parses `--generate`/`--no-recovery`, builds the Plan-43-01 `Sourcer` with the real `golang.org/x/term` seams, constructs the offline Authula `CoreServices` (Plan 43-02 `NewAuthula`), calls `breakglass.RecoverOperator`, prints a generated secret exactly once, and exits; plus a sibling `case "recover-operator"` in `runIdentity` with the D-05 usage disambiguation, and the `golang.org/x/term` indirect→direct promotion.

Purpose: Deliver the operator-facing `aura identity recover-operator` command (R1 exit path, R3 sourcing, R4 --no-recovery) as thin, behaviourally-covered glue over the owned `internal/breakglass` logic — the last wave that turns the tested primitives into a runnable command.
Output: `cmd/aura/recover_operator.go`, an edited `cmd/aura/identity.go`, and `go.mod`/`go.sum` with `x/term` promoted.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-SPEC.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-CONTEXT.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-PATTERNS.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-RESEARCH.md
@cmd/aura/identity.go
@cmd/aura/recovery.go
</context>

<artifacts_produced>
This plan adds these NEW symbols (exclude from drift verification — net-new):
- `cmd/aura/recover_operator.go` — `identityRecoverOperator(ctx context.Context, pool *pgxpool.Pool, cfg *config.Config, args []string)`; the `recover-operator` subcommand; the `--generate` and `--no-recovery` flags; the single `--generate` stdout emission.
- `cmd/aura/identity.go` — a new `case "recover-operator":` branch + an extended `identityUsage` string.
- `go.mod`/`go.sum` — `golang.org/x/term v0.44.0` moved to the direct require block.
Consumes from Plan 43-01/43-02: `breakglass.Sourcer`, `breakglass.Secrets`, `breakglass.NewAuthula`, `breakglass.Deps`, `breakglass.RecoverOperator`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: recover_operator.go — identityRecoverOperator glue (flags → Sourcer → CoreServices → RecoverOperator → exit) (R1/R3/R4, D-05)</name>
  <files>cmd/aura/recover_operator.go</files>
  <read_first>
    - cmd/aura/recovery.go:37-56 — `identityRecover` (the thin resolve→call→print→exit shape) and :53-55 — the "stdout only — NEVER slog" one-time-secret discipline to mirror for `--generate`.
    - cmd/aura/identity.go:31-63 — `runIdentity` loads `cfg := config.LoadDB()` and opens the aura pool; the branch handlers' `(ctx, store/pool, args)` signature convention; `os.Exit(1)` + stderr-usage on error.
    - internal/breakglass/source.go (Plan 43-01) — `Sourcer{Getenv, IsTTY, ReadHidden, Stdout, Stderr}` + `Secrets`; the glue supplies os.Getenv, `term.IsTerminal(int(os.Stdin.Fd()))`, a `term.ReadPassword(int(os.Stdin.Fd()))`-backed ReadHidden (prompt to os.Stderr, confirm), os.Stdout, os.Stderr.
    - internal/breakglass/setter.go + breakglass.go (Plan 43-02) — `NewAuthula(dsn, secret)`, `Deps{Pool, Core, NoRecovery}`, `RecoverOperator`.
    - internal/config/config.go:157-158,329-331,461-465 — `config.LoadDB()` populates `AuthulaDatabaseURL` + `AuthulaSecret`; derive the Authula DSN as `cfg.AuthulaDatabaseURL` if set, else the aura DB URL (webauth forces `?search_path=authula`).
    - scripts/authula_seed_e2e.go:41-59 — the `webauth.New` → `CoreServices()` → `defer provider.Close()` construction the glue reuses via `breakglass.NewAuthula`.
    - 43-RESEARCH.md "Hidden password prompt" (Code Examples) + Assumption A3 — document the `AURA_AUTHULA_SECRET` runtime requirement in the command's error/usage.
  </read_first>
  <action>
    Create `cmd/aura/recover_operator.go` (`package main`) with `identityRecoverOperator(ctx context.Context, pool *pgxpool.Pool, cfg *config.Config, args []string)`. Parse `--generate` and `--no-recovery` as booleans hand-rolled (a manual `for _, a := range args` switch, or `flag.NewFlagSet("recover-operator", flag.ContinueOnError)` with `BoolVar` — std lib, NOT cobra); on an unknown flag print usage to stderr and `os.Exit(1)`. Build a `breakglass.Sourcer{Getenv: os.Getenv, IsTTY: func() bool { return term.IsTerminal(int(os.Stdin.Fd())) }, ReadHidden: <prompt to os.Stderr + term.ReadPassword(int(os.Stdin.Fd())) with a confirm read>, Stdout: os.Stdout, Stderr: os.Stderr}`; call `secrets, err := sourcer.Source(generate, noRecovery)` — on error print it to stderr (it carries no secret) and `os.Exit(1)`. Derive the Authula DSN (cfg.AuthulaDatabaseURL or the aura DB URL); if `cfg.AuthulaSecret == ""` print a clear stderr error naming `AURA_AUTHULA_SECRET` and `os.Exit(1)`. Construct `provider, err := breakglass.NewAuthula(authulaDSN, cfg.AuthulaSecret)` (`os.Exit(1)` on error, no secret in the message); `defer provider.Close()`; `core := provider.CoreServices()`. Call `id, err := breakglass.RecoverOperator(ctx, breakglass.Deps{Pool: pool, Core: core, NoRecovery: noRecovery}, secrets)`; on error print to stderr + `os.Exit(1)`. On success print an `ok:`-prefixed line to stdout that names NO secret (mirror recovery.go:54, e.g. "ok: recovered operator <id>; sessions invalidated; recovery re-seeded"); if `noRecovery` print a warning to stderr that online forgot-password stays unavailable until reconfigured. The generated secret, when `--generate`, is already emitted exactly once to os.Stdout by `Sourcer.Source` — do NOT re-print it. Keep this file thin (behaviourally covered; `cmd/aura` is excluded from the coverage floor). Split into `recover_operator_password.go` only if needed to stay ≤600 LOC.
  </action>
  <acceptance_criteria>
    - `cmd/aura/recover_operator.go` declares `identityRecoverOperator(ctx, pool, cfg, args)`; flags are parsed without cobra; the password never appears as an argv element or in the success/error lines (only the Sourcer's single --generate stdout line carries a secret).
    - The Sourcer is wired with the real seams (os.Getenv, term.IsTerminal, term.ReadPassword, os.Stdout, os.Stderr); a source error exits non-zero with a secret-free stderr message.
    - An empty `cfg.AuthulaSecret` produces a clear stderr error naming `AURA_AUTHULA_SECRET` and a non-zero exit; the provider is always `Close()`d (defer).
    - `--no-recovery` sets `Deps.NoRecovery=true` and prints the reconfigure warning; a success prints an `ok:` line and returns exit 0.
    - `cd D:/Repo/Aura && go vet ./cmd/aura/ && go build ./cmd/aura/` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go vet ./cmd/aura/ && go build ./cmd/aura/</automated>
  </verify>
  <done>`identityRecoverOperator` turns flags + sourced secrets + the offline CoreServices into a `breakglass.RecoverOperator` call, emits a generated secret exactly once, warns on --no-recovery, requires AURA_AUTHULA_SECRET, and never leaks a secret into argv/stdout/stderr/errors.</done>
</task>

<task type="auto">
  <name>Task 2: identity.go dispatch + usage (D-05) + go.mod x/term promotion</name>
  <files>cmd/aura/identity.go, go.mod, go.sum</files>
  <read_first>
    - cmd/aura/identity.go:29 (`identityUsage`), :49-63 (the switch; `cfg := config.LoadDB()` at :38 is in scope) — add the sibling case and extend the usage string; do NOT touch cmd/aura/main.go (it already routes `identity` → runIdentity).
    - cmd/aura/recovery.go:34-56 — the existing `recover <name>` branch that must stay untouched (D-05).
    - 43-PATTERNS.md § "identity.go (EDIT)" + § "go.mod (EDIT)" — the in-file sibling-case pattern and the `go get golang.org/x/term@v0.44.0` promotion.
    - go.mod:36-40 (the direct golang.org/x/* block) and :203 (the `golang.org/x/term v0.44.0 // indirect` line to promote).
  </read_first>
  <action>
    Edit `cmd/aura/identity.go`: add `case "recover-operator":` to the `runIdentity` switch (after the existing `case "recover":`), calling `identityRecoverOperator(ctx, pool, cfg, args[1:])` — `cfg` is already in scope from :38. Extend `identityUsage` (:29) to include `recover-operator` and append a one-line disambiguation per D-05 (e.g. "recover <name> = mint a reset token to hand a user · recover-operator = offline operator password reset + recovery re-seed"). Leave the `recover` branch and recovery.go untouched. Then promote `golang.org/x/term` from indirect to direct: run `go get golang.org/x/term@v0.44.0` (version already pinned in go.sum — no new supply-chain surface) so the glue's `term.IsTerminal`/`term.ReadPassword` import is a direct require; run `go mod tidy` if needed and confirm `go build ./...` is clean.
  </action>
  <acceptance_criteria>
    - `runIdentity` has a `recover-operator` case calling `identityRecoverOperator(ctx, pool, cfg, args[1:])`; the `recover` case is unchanged.
    - `identityUsage` lists `recover-operator` alongside `recover <name>` with the D-05 disambiguation line.
    - `golang.org/x/term v0.44.0` appears in the direct `require` block of go.mod (no longer `// indirect`); `go.sum` is unchanged in content (already pinned).
    - `cmd/aura/main.go` is NOT modified.
    - `cd D:/Repo/Aura && go vet ./... && go build ./...` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura && go build ./... && go vet ./cmd/aura/</automated>
  </verify>
  <done>`aura identity recover-operator` dispatches to the new glue as a sibling of `recover <name>`, the usage line disambiguates the two, and `golang.org/x/term` is a direct dependency — the command is now runnable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → aura CLI | Host access is the admin proof; the operator supplies the new password via env/prompt/--generate and the command mutates auth state offline. |
| CLI process → stdout/stderr/argv | The only sanctioned secret emission is the single `--generate` stdout line; argv, the ok-line, and errors must stay secret-free. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Information disclosure (secret in argv/stdout/stderr) | recover_operator.go emission discipline | mitigate | The password is never a CLI argument (sourced from env/prompt/--generate); the success `ok:` line and all error prints name no secret; the only secret line is the Sourcer's single `--generate` stdout write (mirror recovery.go:53-55). |
| T-43-09 | Denial of service (goroutine leak on the offline provider) | provider lifecycle | mitigate | `breakglass.NewAuthula` provider is always `defer provider.Close()`d so Authula's expiry workers stop (goleak-clean), even on the error paths. |
| T-43-10 | Spoofing (missing secret → silent wrong behavior) | cfg.AuthulaSecret guard | mitigate | An empty `AURA_AUTHULA_SECRET` fails fast with a clear stderr error (documented runtime requirement, A3) rather than constructing a broken provider. |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No net-new package; `golang.org/x/term` is merely promoted indirect→direct (already pinned in go.sum, gated by govulncheck). |
</threat_model>

<verification>
- `cd D:/Repo/Aura && go build ./... && go vet ./cmd/aura/` green (the command compiles and dispatches).
- `go.mod` shows `golang.org/x/term v0.44.0` in the direct require block; `cmd/aura/main.go` untouched.
- Manual (Manual-Only, deploy host): on a single-operator deployment, `aura identity recover-operator` with `AURA_RECOVERY_PASSWORD`/`AURA_RECOVERY_QUESTION`/`AURA_RECOVERY_ANSWER` set resets login and restores forgot-password (Telegram code delivery); `--generate` prints one secret line; `--no-recovery` warns.
</verification>

<success_criteria>
- `aura identity recover-operator` exists as a sibling subcommand (D-05), parses `--generate`/`--no-recovery` without cobra, sources the secret three ways (R3), calls the owned `breakglass.RecoverOperator`, and exits 0 on success / non-zero on refusal or bad input.
- No secret appears in argv, the success line, stderr, or errors — only the single `--generate` stdout line; the offline provider is always closed.
- `golang.org/x/term` is a direct require; the existing `recover <name>` path is untouched.
</success_criteria>

<output>
Create `.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-03-SUMMARY.md` when done.
</output>
