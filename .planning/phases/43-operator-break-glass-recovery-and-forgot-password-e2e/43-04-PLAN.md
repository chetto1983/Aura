---
phase: 43-operator-break-glass-recovery-and-forgot-password-e2e
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/e2e/password-reset.spec.ts
autonomous: true
requirements: [R5]
must_haves:
  truths:
    - "R5 happy path: driving the mocked forgot-password flow (unauthenticated login → 'Forgot password?' → /start → /question → /verify → /complete, all page.route-mocked, Telegram delivery implied by the /start mock) lands on the done panel showing 'Password updated' + a 'Back to sign in' control"
    - "R5 deny path: /start returns the SAME neutral notice 'If that account has recovery enabled, check Telegram now.' as the happy path (no enumeration), and a denied /question surfaces exactly 'Couldn't reset the password. Check the details and try again.' (login.reset.errors.generic) — the DOM names NO recovery factor (never 'identity_recovery', never 'telegram')"
    - "D-10 mock discipline: both paths are driven entirely by page.route().fulfill() (mirror web/e2e/onboarding.spec.ts); the spec has ZERO dependency on the Go break-glass command and reaches the panel on an UNAUTHENTICATED context (NOT gotoAuthenticated)"
    - "every assertion is a counted DOM/text fact guarded so a no-op run FAILS (no-skip-as-green); the web-e2e CI job is path-filtered on web/** so this new spec auto-triggers it"
  artifacts:
    - path: "web/e2e/password-reset.spec.ts"
      provides: "Playwright happy + deny specs for /api/auth/password-reset/* driven by page.route mocks; asserts done-title on happy and generic-no-factor denial on deny"
      contains: "password-reset"
  key_links:
    - from: "web/e2e/password-reset.spec.ts"
      to: "/api/auth/password-reset/{start,question,verify,complete}"
      via: "page.route().fulfill()"
      pattern: "password-reset"
  prohibitions:
    - "MUST NOT reveal which recovery factor is missing on the deny path — the deny assertion checks the rendered text equals the generic login.reset.errors.generic constant and page.locator('body').innerHTML() contains neither 'identity_recovery' nor 'telegram'; the /start notice is byte-identical to the happy path's."
    - "MUST NOT render the typed new password in the DOM — assert page.locator('body').innerHTML() does not contain NEW_PASSWORD (mirror onboarding.spec.ts:248-251; RESEARCH Open Q3)."
    - "MUST NOT drive a real backend leg or seed DB state — every /api/auth/password-reset/* response is a page.route mock (D-10); the real backend deny branch is covered by the Plan 43-02 db_integration tests, not the browser."
    - "MUST NOT use gotoAuthenticated — the forgot-password panel is reached from the LoginPage on an unauthenticated context."
---

<objective>
Add `web/e2e/password-reset.spec.ts` — Playwright coverage of the online forgot-password flow (R5): a happy path (recovery configured → all four `/api/auth/password-reset/*` steps mocked → lands on "Password updated") and a deny path (recovery missing → neutral `/start` notice + a generic `/question` denial that names no recovery factor). Both are fully `page.route()`-mocked (D-10), reached from the unauthenticated LoginPage, mirroring `web/e2e/onboarding.spec.ts`.

Purpose: Prove the React `PasswordResetPanel` renders the happy flow to completion and the deny flow as a generic, non-enumerating denial — closing the R5 gap (no E2E existed for `/api/auth/password-reset/*`). Fully parallel with the backend (Wave 1): the mocked spec has ZERO dependency on the Go command.
Output: `web/e2e/password-reset.spec.ts`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-SPEC.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-CONTEXT.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-PATTERNS.md
@.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-RESEARCH.md
@web/e2e/onboarding.spec.ts
@web/src/auth/PasswordResetPanel.tsx
</context>

<artifacts_produced>
This plan creates the NEW file `web/e2e/password-reset.spec.ts` (exclude from drift verification — net-new). It drives the ALREADY-SHIPPED `PasswordResetPanel` (Phase 36) served by the Playwright `webServer` (`aura serve`); no `web/src` change and no `internal/webui/dist` rebuild is required.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: password-reset.spec.ts — happy + deny (page.route mocks, generic-no-factor denial) (R5, D-10)</name>
  <files>web/e2e/password-reset.spec.ts</files>
  <read_first>
    - web/e2e/onboarding.spec.ts:1-70 (mock-install shape: `page.route('**/api/…', route => route.fulfill({status,contentType,body}))`) + :248-251 (the `page.locator('body').innerHTML()` no-leak DOM assertion to copy) — the structural analog to mirror.
    - web/src/auth/PasswordResetPanel.tsx — the 4-step state machine (`start→code→answer→complete→done`): `/start` sets the neutral notice + advances to `code`; `/question` reveals the security question (catch → `login.reset.errors.generic`); `/verify` returns a resetToken; `/complete` → `done` panel ("Password updated" + "Back to sign in"). The deny surfaces via the `/question` catch at :100-101.
    - web/src/auth/passwordResetApi.ts — the exact endpoints (`/api/auth/password-reset/start|question|verify|complete`) and request/response shapes (`{status}`, `{question}`, `{resetToken}`).
    - web/src/routes/LoginPage.tsx:398-409 — the "Forgot password?" button (`login.authula.forgotPassword`) that opens the panel on the unauthenticated login view (`setResetOpen(true)`).
    - web/src/i18n/resources.login.ts:40-59 — the EXACT English strings to assert: `noticeCodeSent` = "If that account has recovery enabled, check Telegram now.", `doneTitle` = "Password updated", `backToSignIn` = "Back to sign in", `errors.generic` = "Couldn't reset the password. Check the details and try again.", `forgotPassword` = "Forgot password?".
    - web/playwright.config.ts — the `webServer` (`aura serve`), `baseURL http://127.0.0.1:9080`, `serviceWorkers:'block'` (so page.route sees every request) and the chromium + mobile-chrome projects.
    - 43-PATTERNS.md § "password-reset.spec.ts" + 43-RESEARCH.md Q7 + Open Q3 — the mock-both-paths approach, the neutral-`/start` fact, and the "also assert the DOM never renders NEW_PASSWORD" recommendation.
  </read_first>
  <action>
    Create `web/e2e/password-reset.spec.ts` mirroring onboarding.spec.ts (import `{ expect, test }` from `@playwright/test`; run on the same chromium + mobile-chrome projects). Define constants: `NEW_PASSWORD`, `SECURITY_QUESTION = 'First school?'`, `SECURITY_ANSWER`, a `RESET_TOKEN`. Write two tests, each navigating to the login route on an UNAUTHENTICATED context (do NOT use gotoAuthenticated) and clicking the "Forgot password?" button (getByRole button name 'Forgot password?').

    Happy test: `page.route()` all four endpoints — `**/api/auth/password-reset/start` → 200 `{status:'ok'}`; `**/api/auth/password-reset/question` → 200 `{question: SECURITY_QUESTION}`; `**/api/auth/password-reset/verify` → 200 `{resetToken: RESET_TOKEN}`; `**/api/auth/password-reset/complete` → 200 `{status:'ok'}`. Drive: fill the email → submit → assert the neutral notice text "If that account has recovery enabled, check Telegram now." is visible → fill the Telegram code → submit → assert the security question `First school?` is rendered → fill the answer → submit → fill new password + confirm (both `NEW_PASSWORD`) → submit → assert the done panel shows "Password updated" and a "Back to sign in" control. Finally assert `await page.locator('body').innerHTML()` does NOT contain `NEW_PASSWORD` (no-leak).

    Deny test: mock `**/api/auth/password-reset/start` → 200 `{status:'ok'}` (ALWAYS neutral — this is the anti-enumeration fact) and `**/api/auth/password-reset/question` → a denial (e.g. `route.fulfill({status:403, contentType:'application/json', body: JSON.stringify({error:'denied'})})`, which makes `fetchPasswordResetQuestion` throw → the panel's catch sets `login.reset.errors.generic`). Drive: fill email → submit → assert the notice is the SAME neutral text as the happy path (character-for-character) → fill the code → submit → assert the visible error text equals "Couldn't reset the password. Check the details and try again." Then capture `const html = await page.locator('body').innerHTML()` and assert `expect(html).not.toContain('identity_recovery')` and `expect(html.toLowerCase()).not.toContain('telegram_accounts')` and that no factor name is revealed (the only allowed 'Telegram' occurrence is the neutral notice/label copy, never a "which factor is missing" disclosure). Keep every assertion a counted `expect(...).toBeVisible()`/`toContainText`/exact-equality so a no-op run FAILS (no-skip-as-green).
  </action>
  <acceptance_criteria>
    - `web/e2e/password-reset.spec.ts` exists, reaches the panel via the unauthenticated LoginPage "Forgot password?" button, and mocks all `/api/auth/password-reset/*` via `page.route().fulfill()` (no gotoAuthenticated, no real backend leg, no DB seeding).
    - Happy: the flow reaches the "Password updated" done panel with a "Back to sign in" control; `body.innerHTML()` does not contain `NEW_PASSWORD`.
    - Deny: the `/start` notice equals the happy-path neutral string; the denied `/question` shows exactly "Couldn't reset the password. Check the details and try again."; the DOM contains no factor-name disclosure (`identity_recovery` absent; no "which factor is missing" text).
    - Every assertion is counted (visible/exact) so a no-op FAILS.
    - `cd D:/Repo/Aura/web && npx playwright test password-reset.spec.ts` passes (chromium; and mobile-chrome if the project runs it).
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Repo/Aura/web && npx playwright test password-reset.spec.ts</automated>
  </verify>
  <done>The E2E proves the forgot-password happy path completes to "Password updated" and the deny path renders a generic, non-enumerating denial (identical neutral /start notice + generic /question error, no factor named, no typed password in the DOM) — fully page.route-mocked with counted assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser → forgot-password API | An anonymous visitor drives `/api/auth/password-reset/*`; the responses must never disclose whether an account exists or which recovery factor is configured. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Information disclosure (account/factor enumeration) | deny-path assertions | mitigate | The deny spec asserts the `/start` notice is byte-identical to the happy path (neutral), the `/question` denial is the generic constant, and `body.innerHTML()` names no factor (`identity_recovery` absent) — proving the panel never reveals which factor is missing (R5). |
| T-43-11 | Information disclosure (typed password in DOM) | no-leak DOM assertion | mitigate | `expect(body.innerHTML()).not.toContain(NEW_PASSWORD)` (mirror onboarding.spec.ts:248-251). |
| T-43-12 | Repudiation (falsely-green e2e) | counted assertions | mitigate | Every assertion is a counted visible/exact check so a no-op run FAILS (no-skip-as-green); the web-e2e job auto-triggers on the new `web/**` file. |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No new package — Playwright is already installed under web/node_modules. |
</threat_model>

<verification>
- `cd D:/Repo/Aura/web && npx playwright test password-reset.spec.ts` green on chromium (and mobile-chrome if configured) — happy reaches "Password updated"; deny shows the generic error with no factor named.
- The spec uses only `page.route()` mocks (no gotoAuthenticated, no live backend), so it runs deterministically in the path-filtered web-e2e CI job.
</verification>

<success_criteria>
- `web/e2e/password-reset.spec.ts` covers the happy path (to "Password updated") and the deny path (neutral `/start` + generic `/question` denial, no factor leak, no typed password in the DOM), fully mocked (D-10), reached unauthenticated.
- Every assertion is counted (no-skip-as-green); the new spec auto-triggers the web-e2e CI job.
</success_criteria>

<output>
Create `.planning/phases/43-operator-break-glass-recovery-and-forgot-password-e2e/43-04-SUMMARY.md` when done.
</output>
