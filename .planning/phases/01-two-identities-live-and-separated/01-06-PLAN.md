---
phase: 01-two-identities-live-and-separated
plan: 06
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - scripts/musr_live_run.sh
  - scripts/musr_live_run_assert.go
  - docs/runbooks/two-identity-live-run.md
  - .planning/phases/01-two-identities-live-and-separated/01-LIVE-RUN-EVIDENCE.md
autonomous: true
requirements: [E2E-01, E2E-02]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A committed harness — not a session transcript — authenticates as each identity and drives two concurrent `POST /agent/run` conversations through the real AG-UI gateway against a live `aura serve`, capturing both transcripts and their timing. Anyone can re-run it (D-16)."
    - "Both identities perform the SAME three tasks on their own data, deliberately raced so the calls overlap in time: a document search, a memory write and a sandbox command (D-17)."
    - "The two conversations genuinely overlap: the captured timings show each identity's tool calls beginning before the other's have ended, so the run exercises the shared singletons rather than taking turns."
    - "When the two identities issue the SAME task with equivalent arguments at the same moment, the two runs separate rather than merge or collide: each answer carries its own identity's seeded token, each transcript records its own tool executions, and neither result is served to both."
    - "The machine-checkable half BLOCKS the phase: both conversations complete, the required tools fire, each identity's expected tokens appear in its own answer, and neither answer carries the other identity's token (D-18)."
    - "The ≥9.8 rubric is written down with its dimensions, weights and what 9.8 means, and it is scored and RECORDED as phase evidence — it does not gate. `internal/agenteval/case.go`'s position that a gate needing a model to decide whether it passed cannot be trusted to gate the model stands unamended (D-18)."
    - "Identity B — provisioned by `aura identity create` in this same run — reaches a useful conversation from zero: memory database, object bucket, sandbox and skills root, with no manual step outside the documented path (E2E-02)."
    - "Identity B's forced first login is driven end to end: the password change and the mandatory TOTP enrollment `EnforceFirstLogin` requires, not merely the TOTP verify an already-enrolled identity needs."
    - "The real Telegram bot is used exactly once, here, and the captured deep link is the evidence it works (D-08)."
    - "Every provisioned identity, bootstrap included, reaches a useful conversation through the same documented path: identity A is the bootstrap `local` operator and identity B is the saga-provisioned one, and both drive the identical authenticated `POST /agent/run` route through the same three tasks."
    - "A conversation that produced zero tool calls, or an identity that failed to authenticate, is a FAILED run and not a low-scoring one — the machine-checkable half requires both transcripts non-empty with their required tools fired before any score is discussed."
    - "The order in which the two conversations finish is not part of the pass criteria; either may complete first, and both transcripts are captured and scored independently."
  artifacts:
    - scripts/musr_live_run.sh
    - scripts/musr_live_run_assert.go
    - docs/runbooks/two-identity-live-run.md
    - .planning/phases/01-two-identities-live-and-separated/01-LIVE-RUN-EVIDENCE.md
  key_links:
    - "scripts/musr_live_run.sh -> go build ./cmd/aura -> a backgrounded `aura serve` under a strict profile with AURA_MUSR_ISOLATION=true -> poll /healthz"
    - "scripts/musr_live_run.sh -> `aura identity create` (plan 01-01) -> the real provisioning saga -> identity B's four resources + a real Telegram deep link"
    - "scripts/musr_live_run.sh -> Authula CSRF login + forced password change + TOTP enrollment -> two authenticated cookie jars"
    - "scripts/musr_live_run.sh -> two concurrent POST /agent/run SSE streams -> two captured transcripts + timings -> scripts/musr_live_run_assert.go"
  prohibitions:
    - "The scoring rubric must not gate the phase — a check that needs a model to decide whether it passed cannot be trusted to gate the model, and that position must not be silently reversed."
    - "A missing case, an unauthenticated identity or an empty transcript must never be converted into a passing score."
    - "The run must not use the operator's real correspondents: no message reaches a third party beyond the single Telegram deep-link mint the provisioning saga performs, and no email is sent."
    - "The run must not plant test facts in, or delete documents from, the operator's own data — each identity works on data it owns and the run names what it created."
    - "The phase must not be reported as closed on the strength of a green unit suite."
---

<objective>
Close the phase the only way this milestone allows: a real run against the live stack, driven
by the real agent.

Two identities hold real conversations at the same time against one running `aura serve`, each
doing useful work on its own data, deliberately raced so the collision is real rather than
sequential — and the pair is scored against a written rubric while a machine-checkable half
decides whether the phase actually closes.

Purpose: E2E-01 and E2E-02 are the requirements every other plan in this phase exists to make
reachable. The existing `two_identity_e2e_*` files never spawn an `aura serve` process and
never perform a real Authula login; `scripts/agui_smoke.sh` does both but drives the
bootstrap-seeded `local` identity, which never passes through `EnforceFirstLogin`'s forced
password change and mandatory TOTP enrollment. Nothing in this repository currently drives a
freshly provisioned identity through first login end to end. This plan builds that.

Output: a committed, re-runnable harness; a blocking machine-checked assertion set; a written
≥9.8 rubric that records rather than gates; and the recorded evidence of one scored run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-two-identities-live-and-separated/01-CONTEXT.md
@.planning/phases/01-two-identities-live-and-separated/01-RESEARCH.md
@.planning/phases/01-two-identities-live-and-separated/01-PATTERNS.md
@.planning/phases/01-two-identities-live-and-separated/01-01-SUMMARY.md
@.planning/phases/01-two-identities-live-and-separated/01-02-SUMMARY.md
@.planning/phases/01-two-identities-live-and-separated/01-04-SUMMARY.md
@CLAUDE.md
</context>

<read_the_documentation_first>
Before a line of the login step is written, read the installed TOTP plugin's exported API.
RESEARCH.md flags this as Assumption A2 and Open Question 2: `EnforceFirstLogin`
(`internal/agui/onboarding_provision.go:204-210`) requires TOTP *enrollment*, and
`scripts/agui_smoke.sh:267-304` only handles *verify* against an already-enrolled identity. It
is not known from source read so far whether the enrollment endpoint returns a raw secret a
headless harness can feed into an RFC 6238 generator, or only a QR image.

Run, and record what you find in the SUMMARY:

- `go doc github.com/Authula/authula/plugins/totp`
- read the installed package under `$(go env GOMODCACHE)/github.com/!authula/...` — the module
  cache, not a docs site and not memory
- `grep -rn "totp" internal/webauth/authula.go` for how Aura already wires it

CLAUDE.md's rule is explicit and this is exactly the case it was written for: probing a live
system to discover an API is guessing with extra steps. If enrollment turns out not to be
headlessly automatable, STOP and report it rather than working around it — that finding
changes what E2E-02's "no manual step outside the documented path" can mean, and it belongs in
front of the user, not inside a workaround.
</read_the_documentation_first>

<tasks>

<task type="auto">
  <name>Task 1: The committed harness — two identities, one live daemon, three raced tasks</name>
  <files>scripts/musr_live_run.sh</files>
  <precondition>A live stack the harness can drive: Docker Engine running, the compose services up and healthy, a `.env` carrying `AURA_PROFILE=single_user_hardened`, `AURA_MUSR_ISOLATION=true`, a reachable `AURA_SANDBOX_IMAGE`, `AURA_ARCADEDB_TENANT_SECRET`, `AURA_AUTHULA_SECRET`, the Garage credentials, a real `TELEGRAM_BOT_TOKEN` (the provisioning saga requires the Telegram port, D-06), and `OPENROUTER_API_KEY` or a local model endpoint the agent can actually answer from. Assert each before starting the daemon and halt naming the missing one — a partial stack produces a transcript that looks like a failure of isolation and is not.</precondition>
  <reversibility rating="reversible">A new script; deleting it restores the previous state. It modifies no production code.</reversibility>
  <read_first>
    - scripts/agui_smoke.sh — ALL 448 lines, and lines 129-330 with particular care. This is the model, ported rather than extended: `go build -o "${BIN}" ./cmd/aura`, the backgrounded `serve` with its PID captured, the poll loop that checks the process is still alive AND polls `/healthz` with no fixed sleep, the Authula `/api/auth/config` CSRF fetch, the `X-AUTHULA-CSRF-TOKEN` login with a cookie jar, the `totp_redirect` branch at lines 267-304, and the Windows-vs-POSIX process cleanup. Reuse its shapes; do not re-derive them.
    - internal/agui/onboarding_provision.go lines 200-215 — `EnforceFirstLogin`: what it marks and therefore what identity B's FIRST login must satisfy that the bootstrap `local` identity never does.
    - cmd/aura/identity_create.go — the verb plan 01-01 shipped: its flags, where it reads the password and security answer from, and exactly what it prints on success (the identity UUID and the Telegram deep link, each on its own line). The harness parses that output; read it rather than assuming its shape.
    - cmd/aura/serve_sandbox_preflight.go — the boot refusal plan 01-02 shipped, so the harness can distinguish "the daemon refused because the box image is unreachable" from "the daemon crashed", and report the first as a precondition failure rather than as a run failure.
    - .planning/phases/01-two-identities-live-and-separated/01-02-SUMMARY.md — what the shipped profile now sets, so the harness does not re-set it.
    - internal/agui — the `POST /agent/run` route, its request body shape and its SSE frame shape. Find the handler and read it; the harness must parse real frames, not a guessed schema.
    - scripts/musr_e2e.sh — the script plan 01-04 shipped, for the readiness-polling and teardown conventions this one follows. Note the difference: THAT one runs against a disposable stack; THIS one runs against the real one and must not tear the real one down.
  </read_first>
  <action>
    Write `scripts/musr_live_run.sh`, porting `scripts/agui_smoke.sh`'s process-lifecycle and
    authentication shapes, doing in order:

    1. Assert every precondition above and name the first missing one. Refuse to start.
    2. Build the binary and start `aura serve` in the background with its PID captured and its
       log redirected to a file the harness prints on any failure. Poll `/healthz`, checking
       the process is still alive on each iteration, with no fixed sleep. If the process exits
       during boot, print the log and distinguish the sandbox-image preflight refusal from any
       other cause.
    3. Provision identity B by running `aura identity create` — the documented path, not a raw
       insert and not an HTTP call the docs do not describe. Capture the identity UUID and the
       Telegram deep link from its stdout. This is the ONE use of the real bot in this phase
       (D-08); record the deep link in the evidence file as proof it was minted.
    4. Authenticate identity A — the bootstrap `local` operator — through the Authula CSRF +
       email/password + TOTP-verify flow `agui_smoke.sh` already exercises, into cookie jar A.
    5. Authenticate identity B through the FORCED first-login flow: password change and TOTP
       *enrollment*, using whatever contract the module-cache read established, into cookie jar
       B. Fail with a named message if the enrollment response does not carry what a headless
       code generator needs, rather than looping on a verify that cannot succeed.
    6. Drive two concurrent `POST /agent/run` conversations — one per identity, each with its
       own cookie jar — as background jobs released together, capturing each SSE stream to its
       own transcript file and recording per-tool-call start and end timestamps to a timing
       file. Both identities perform the SAME three tasks on their OWN data (D-17): a document
       search, a memory write, and a sandbox command. Symmetry is the point: a leak shows up
       as B's answer carrying A's token, which needs no interpretation.
    7. Time the three tasks to overlap deliberately. Emit the timing file in a form the
       assertion step can check for actual overlap, so "concurrent" is measured rather than
       intended.
    8. Stop the daemon it started and leave the compose stack exactly as it found it. This
       harness runs against the REAL deployment; it must tear down only its own `aura serve`
       process and the artifacts it wrote.

    Seed each identity's own data as part of the run and name what it created, so the run is
    re-runnable and so nothing it writes is mistaken for the operator's own material. Do not
    touch the operator's documents or plant facts in the operator's memory graph.

    Print the transcript paths, the timing path and the daemon log path at the end regardless
    of outcome — the operator's role in D-16 is to read and score, and they cannot read what
    the harness did not tell them where to find.
  </action>
  <acceptance_criteria>
    - Behavior: `bash -n scripts/musr_live_run.sh` exits 0.
    - Behavior: `bash scripts/musr_live_run.sh` with a deliberately missing required variable exits non-zero before building the binary, naming that variable.
    - Behavior: a full run produces two transcript files, a timing file and a daemon log, and prints all four paths.
    - Behavior: the run provisions identity B through `aura identity create` — `grep -c 'identity create' scripts/musr_live_run.sh` is at least 1 and the script contains no `INSERT INTO aura.identities`.
    - Source: the readiness wait polls and checks the daemon PID is alive each iteration; there is no fixed sleep preceding a health check.
    - Source: the two `POST /agent/run` invocations are backgrounded and joined, not sequential — verified by reading the two `&` job launches and the `wait`.
    - Source: the script contains `TELEGRAM` only in a precondition assertion and in the deep-link capture; it sends no message itself.
    - Behavior: after the run, `docker compose ps` shows the same services in the same state as before it, and no `aura serve` process started by the harness remains.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/musr_live_run.sh</automated>
    <fails_when>non-zero exit, or a `syntax error near unexpected token` line</fails_when>
    <automated>bash scripts/musr_live_run.sh</automated>
    <fails_when>non-zero exit; a `FAIL:` line; the daemon log containing the sandbox-image preflight refusal (a precondition failure, not a run result); either transcript file being empty or absent; the timing file showing no overlap between the two identities' tool calls; or the run completing in under 10 seconds, which for two real model-driven conversations with three tool calls each means no turn actually ran</fails_when>
  </verify>
  <done>One command starts a live strict-profile daemon, provisions a second identity through the documented path including its forced first login, drives two concurrent authenticated conversations doing the same three tasks on their own data with overlapping timings, captures both transcripts, and leaves the deployment as it found it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The half that blocks — machine checks over the captured transcripts</name>
  <files>scripts/musr_live_run_assert.go</files>
  <reversibility rating="reversible">A new `//go:build ignore` script; deleting it restores the previous state and it enters no package's coverage denominator.</reversibility>
  <read_first>
    - internal/agenteval/case.go — ALL 205 lines. `Case` and every field: `AnswerContains` (exact tokens that cannot be produced by paraphrase — a customer code, a row count, never a phrase), `AnswerMustNotContain` (the deletion primitive, and the measured 2026-08-03 case where a catalog row was correct and the answer still came from a copy left in the sandbox), `RequiredTools` (at least one, stating the capability without pinning the route), `RequiredToolSequence`. And lines 14-17's position: there is no LLM judge and no rubric, because a gate that needs a model to decide whether it passed cannot be trusted to gate the model. That comment stands unamended and this task must not contradict it.
    - internal/agenteval/cases.go — how cases are declared, for the shape this task's two symmetric case sets follow.
    - scripts/authula_seed_e2e.go lines 1-12 — the `//go:build ignore` + `package main` convention every `scripts/*.go` follows, so this file stays out of `go build ./...` and out of every coverage denominator.
    - scripts/musr_live_run.sh — the transcript and timing file formats Task 1 emits. Read them; do not assume a schema.
    - internal/agenteval/live_test.go — the existing live tier, for how a real run's durable record is read back and asserted.
  </read_first>
  <behavior>
    Write the assertions first against a hand-written fixture pair of transcripts, so each one
    is shown to fail before any real run is scored:

    - Completion: both transcripts carry a terminal answer frame. An empty transcript, or one
      that ends without a terminal frame, FAILS — it is not a low score.
    - Authentication: both identities authenticated. An identity that never got a session
      FAILS the run rather than scoring zero.
    - Required tools: each identity's transcript shows a document-search tool, a memory-write
      tool and a sandbox command actually executed. At least one tool per capability, named
      the way `RequiredTools` names them — the capability, not the route.
    - Expected tokens: each identity's answer contains ITS OWN seeded token — an exact value
      that cannot be produced by paraphrase.
    - Cross-read: each identity's answer does NOT contain the other identity's token. This is
      the assertion the symmetry exists for; a leak is self-evident and needs no judgement.
    - Overlap: the timing file shows at least one interval where both identities have a tool
      call in flight simultaneously. A run that took turns FAILS as not-concurrent, because
      the whole point is exercising the shared singletons.
    - Order independence: the assertions do not depend on which conversation finished first,
      and a fixture with the completion order swapped produces the same verdict.
    - Exit status: any failure exits non-zero with the failing assertion named and the
      offending transcript path printed.
  </behavior>
  <action>
    Write `scripts/musr_live_run_assert.go` with `//go:build ignore` and `package main`,
    following `scripts/authula_seed_e2e.go`'s convention exactly. It reads the two transcripts
    and the timing file Task 1 emits, and applies two symmetric case sets — one per identity —
    declared in the same structural style `internal/agenteval`'s `Case` uses: exact tokens,
    tool names and counts, never natural-language matching on an answer.

    Import `internal/agenteval` and reuse its `Case` type and its evaluation semantics if the
    package exports what this needs; if it does not, mirror the field semantics and say so in
    the SUMMARY rather than quietly inventing a looser contract. Do NOT add a production file
    to `internal/agenteval` — it is a `target` package at an 85% floor and a new declarative
    file there moves a denominator the fail-closed policy checks.

    Include a fixture mode that runs the assertions against committed hand-written transcript
    pairs — one clean pair and one leaking pair where B's answer carries A's token — so the
    assertions are proven able to fail without needing a live run to break. This is the same
    reason `internal/agenteval`'s own header gives for existing at all.

    Wire it as the final step of `scripts/musr_live_run.sh`: the harness runs it and propagates
    its exit status, so the machine-checkable half is what decides whether the run passed, and
    the rubric that follows is recorded rather than consulted.
  </action>
  <acceptance_criteria>
    - Source: `head -1 scripts/musr_live_run_assert.go` is `//go:build ignore` and the package is `main`.
    - Source: `go build ./...` does not compile the file — confirmed because the tree still builds with the file present and `go list ./scripts/...` does not report it as a package.
    - Behavior: `go run ./scripts/musr_live_run_assert.go --fixture clean` exits 0.
    - Behavior: `go run ./scripts/musr_live_run_assert.go --fixture leaking` exits non-zero and names the cross-read assertion and the offending transcript path.
    - Behavior: a fixture with an empty transcript exits non-zero naming the completion assertion, not with a low score.
    - Behavior: a fixture whose timings do not overlap exits non-zero naming the overlap assertion.
    - Behavior: swapping which conversation completes first in the clean fixture still exits 0.
    - Source: the file contains no LLM call and no scoring arithmetic — the rubric lives in Task 3's document, not here.
    - Source: `git diff --exit-code -- internal/agenteval/ scripts/coverage_package_policy.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go vet ./... &amp;&amp; go build ./...</automated>
    <fails_when>non-zero exit, or any `file.go:line:col:` diagnostic — a `//go:build ignore` file that accidentally entered the build would show up here</fails_when>
    <automated>go run ./scripts/musr_live_run_assert.go --fixture clean</automated>
    <fails_when>non-zero exit, or any `FAIL` line — the clean fixture must pass or the assertions are miscalibrated and would fail a correct run</fails_when>
    <automated>go run ./scripts/musr_live_run_assert.go --fixture leaking; test $? -ne 0</automated>
    <fails_when>the wrapper exiting non-zero, which happens when the assert script exited 0 on the leaking fixture — an assertion set that cannot detect a planted cross-read is a gate that would pass a real leak</fails_when>
    <automated>git diff --exit-code -- internal/agenteval/ scripts/coverage_package_policy.json</automated>
    <fails_when>non-zero exit — a production file entered a target-floor package or the fail-closed coverage inventory changed</fails_when>
  </verify>
  <done>A leaking transcript pair fails the run non-zero naming the assertion, a clean pair passes, an empty transcript or a non-overlapping timing fails rather than scoring low, and the harness propagates that verdict as the thing that decides whether the phase closes.</done>
</task>

<task type="auto">
  <name>Task 3: Write the rubric, run it for real, and score the transcripts</name>
  <files>docs/runbooks/two-identity-live-run.md, .planning/phases/01-two-identities-live-and-separated/01-LIVE-RUN-EVIDENCE.md</files>
  <reversibility rating="costly">D-18 records this: the rubric becomes the cross-phase scoring instrument every later phase's closing run is measured with. Changing its dimensions later makes earlier scores incomparable, which is a real cost even though nothing about it is technically irreversible.</reversibility>
  <read_first>
    - internal/agenteval/case.go lines 1-20 — the position the rubric must not contradict: no LLM judge, no rubric IN THE GATE, because a gate that needs a model to decide whether it passed cannot be trusted to gate the model. The rubric written here scores and records; it does not gate. Say so in the document itself so a later reader cannot mistake it.
    - CLAUDE.md § DEFINITION OF DONE — the >9.8 bar on a real scenario, and § PRD-first principle's requirement that an amendment records a measurement and states what the measurement does NOT show.
    - .planning/ROADMAP.md § Phase 1 — Success Criterion 2 and the "Closes on (live run)" line, which is what this run has to satisfy.
    - docs/runbooks/musr-rollout.md — the sibling runbook's voice and structure, as updated by plan 01-04.
    - scripts/musr_live_run.sh and scripts/musr_live_run_assert.go — as Tasks 1 and 2 left them, so the runbook describes what they actually do.
  </read_first>
  <action>
    (a) Write `docs/runbooks/two-identity-live-run.md`: how to run the harness, what it needs,
    what it produces, and the ≥9.8 rubric. The rubric names its dimensions, their weights, and
    what a 9.8 means for each — concretely enough that two readers scoring the same transcripts
    land within a tenth of each other. Suggested dimensions, adjust with reasons recorded:
    task completion, answer correctness against the seeded ground truth, tool-route sanity (did
    she reach for the right capability, not merely any capability), isolation legibility (is it
    evident from the transcript alone that each identity worked on its own data), and
    degradation honesty (when something failed, did the transcript say so rather than paper
    over it).

    State plainly, in the document, the split D-18 requires: the machine-checkable half
    (`scripts/musr_live_run_assert.go`) is what blocks; the rubric score is recorded as phase
    evidence and gates nothing. Cite `internal/agenteval/case.go`'s reasoning rather than
    restating it loosely, and do not amend that comment — under this split nothing contradicts
    it and it stands.

    (b) Run `bash scripts/musr_live_run.sh` for real, against the live stack, with the real
    Telegram bot. Capture both transcripts, the timings, the daemon log and the assert script's
    exit status.

    (c) Assemble what the operator needs to score, and hand it over: the two transcript paths,
    the timing overlap the run measured, the assert script's verdict per assertion, and the
    rubric. The operator's role is exactly this — read and score — because D-16 built the
    harness so the run itself needs no human. This project runs `human_verify_mode` at
    `end-of-phase`, so do NOT halt mid-plan waiting for the score: leave the scoring
    instruction in this task's `<verify><human-check>` block, which the verifier harvests into
    the phase UAT batch, and leave the per-dimension score rows of the evidence file empty and
    clearly marked as awaiting that batch.

    (d) Write `.planning/phases/01-two-identities-live-and-separated/01-LIVE-RUN-EVIDENCE.md`
    recording: the commit SHA the run was made on, the date, the two identity UUIDs, the
    Telegram deep link that proves the mint worked, the three tasks each identity performed,
    the measured timing overlap, the machine-check verdict per assertion, one empty score row
    per rubric dimension marked as awaiting the end-of-phase scoring batch, and — per
    CLAUDE.md's PRD-first principle — what this run does NOT demonstrate. Name at least: it
    samples one collision shape; it runs one model on one host; and it says nothing about
    behaviour under load, under attack or across a restart, which are phases 3, 4 and 5.

    (e) When the score arrives and is below 9.8, that is a recorded finding and a follow-up,
    not a silent pass and not a rewritten rubric. Fix what the transcripts show is wrong and
    re-run the harness. Never edit a dimension's definition to make a transcript clear it.
  </action>
  <acceptance_criteria>
    - Docs: `docs/runbooks/two-identity-live-run.md` exists, names every rubric dimension with a weight and a description of what 9.8 means for it, and states in its own text that the rubric does not gate and that the machine-checkable half does.
    - Docs: the runbook cites `internal/agenteval/case.go` and `git diff --exit-code -- internal/agenteval/case.go` exits 0 — that comment was not amended.
    - Behavior: `bash scripts/musr_live_run.sh` completed against the live stack with a non-zero-length transcript for each identity and an assert-script exit status of 0.
    - Evidence: `01-LIVE-RUN-EVIDENCE.md` exists and carries the commit SHA, the date, both identity UUIDs, the Telegram deep link, the measured overlap interval, a per-assertion verdict, one score row per rubric dimension awaiting the end-of-phase batch, and a "what this does not show" section naming at least three limits.
    - Evidence: the score rows are the operator's to fill and are recorded whatever their value — a score below 9.8 lands in the file as a finding with its follow-up, never as a rewritten dimension.
  </acceptance_criteria>
  <verify>
    <human-check>
      Read both captured transcripts end to end. Confirm, without needing to interpret:
      (1) each identity completed all three tasks on its own data;
      (2) neither answer carries the other identity's seeded token;
      (3) where something failed, the transcript says so rather than papering over it;
      (4) the tool routes are sane — she reached for the right capability, not merely any one.
      Then score both transcripts against every rubric dimension and record the score with
      your reasoning. Confirm the assert script's verdict was 0 before treating the score as
      meaningful; a failing machine check is a failed run whatever the prose reads like.
    </human-check>
    <automated>go run ./scripts/musr_live_run_assert.go --transcripts .planning/phases/01-two-identities-live-and-separated/</automated>
    <fails_when>non-zero exit, or any `FAIL` line naming an assertion — the machine-checkable half is what blocks the phase, and it must be green before the recorded score means anything</fails_when>
    <automated>git diff --exit-code -- internal/agenteval/case.go</automated>
    <fails_when>non-zero exit — `case.go`'s no-rubric-gates position was amended, which D-18 explicitly forbids: under this split nothing contradicts it and it stands unamended</fails_when>
  </verify>
  <done>The rubric is written down, one real run is complete with both transcripts captured and machine-checked green, the operator has scored both transcripts against every dimension, and the score plus its limits are recorded as phase evidence.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and paths this plan creates:

- Files: `scripts/musr_live_run.sh`, `scripts/musr_live_run_assert.go` (`//go:build ignore`,
  `package main`), `docs/runbooks/two-identity-live-run.md`,
  `.planning/phases/01-two-identities-live-and-separated/01-LIVE-RUN-EVIDENCE.md`
- Committed test fixtures for the assert script: a clean transcript pair and a leaking
  transcript pair, under a fixtures path the assert script names
- Script flags: `--fixture clean`, `--fixture leaking`, `--transcripts <dir>`
- Runtime artifacts the harness emits (not committed): two transcript files, one timing file,
  one daemon log
- No new package, no new Go build tag, no new migration, no new production symbol. Every env
  var the harness reads already exists (`AURA_PROFILE`, `AURA_MUSR_ISOLATION`,
  `AURA_SANDBOX_IMAGE`, `AURA_ARCADEDB_TENANT_SECRET`, `AURA_AUTHULA_SECRET`,
  `TELEGRAM_BOT_TOKEN`, `OPENROUTER_API_KEY`, `AURA_E2E_AUTHULA_TOTP_CODE`).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → the real deployment | This run drives the operator's actual stack, with real data and a real bot, unlike every other tier in this phase. |
| a freshly provisioned identity → its first login | Forced password change plus mandatory TOTP enrollment, a path nothing in this repository has driven end to end. |
| the provisioning saga → Telegram | A real deep link is minted against a real bot token. |
| identity B's conversation → identity A's data | Two authenticated principals, one daemon, three concurrent tool calls each. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | one identity's answer carrying the other's data | high | mitigate | THIS PHASE BUILDS — the symmetric raced tasks make a leak self-evident, and `AnswerMustNotContain`-shaped assertions on each identity's seeded token block the run non-zero. The leaking fixture proves the assertion can fail. |
| T-06-02 | Authentication bypass | a harness that authenticates as B by a shortcut the docs do not describe | high | mitigate | THIS PHASE BUILDS — identity B authenticates through the real Authula CSRF login and the real forced first-login flow, and the harness fails loudly rather than working around an enrollment contract it cannot satisfy. A shortcut here would prove nothing about E2E-02. |
| T-06-03 | Information Disclosure | a secret reaching a transcript, a log or the evidence file | high | mitigate | THIS PHASE BUILDS — the harness prints paths, identity UUIDs and the deep link; the password, the security answer and the TOTP secret are never echoed. Reviewer check before committing the evidence file: no `set -x` in the harness and no credential in any captured artifact. |
| T-06-04 | Tampering | a run mutating the operator's own documents or memory | high | mitigate | THIS PHASE BUILDS — each identity works on data the run seeded and names, and no case deletes an operator document. The spike's own discipline (no operator documents deleted, no test facts planted in the operator's memory, no third-party message sent) is carried over verbatim as a prohibition. |
| T-06-05 | Repudiation | a rubric score converting a missing or failed case into a pass | high | mitigate | THIS PHASE BUILDS — the machine-checkable half runs first and blocks; the rubric is recorded after and gates nothing. `internal/agenteval/case.go`'s position stands unamended and a `git diff --exit-code` on it is a verify command. |
| T-06-06 | Spoofing | the Telegram deep link reaching an unintended recipient | medium | accept | The saga mints one pending link and the harness records it; nothing sends it anywhere. The link's own expiry and single-consumption are pre-existing properties of the Telegram leg, not built here. |
| T-06-07 | Denial of Service | the harness leaving the operator's deployment in a changed state | medium | mitigate | THIS PHASE BUILDS — the harness tears down only the `aura serve` process it started, leaves the compose stack as it found it, and the acceptance criteria check `docker compose ps` before and after. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this plan. RESEARCH.md § Package Legitimacy Audit: "Not applicable — this phase adds no new external package." The sandbox command each identity runs executes inside its own per-identity box against the shipped image. |
</threat_model>

<verification>
- `bash -n scripts/musr_live_run.sh` exits 0; `go vet ./... && go build ./...` clean with the `//go:build ignore` script present.
- The clean fixture passes and the leaking fixture fails non-zero naming the cross-read assertion.
- One real run completed against the live stack: two non-empty transcripts, a measured timing overlap, and an assert-script exit status of 0.
- `git diff --exit-code -- internal/agenteval/ scripts/coverage_package_policy.json` exits 0.
- The evidence file records the SHA, the date, both identities, the deep link, the per-assertion verdict, the per-dimension score and what the run does not show.
</verification>

<success_criteria>
Two identities held real conversations at the same time against one running `aura serve`, each
doing useful work on its own data with their tool calls overlapping in time; the second
identity got there from zero through `aura identity create` and its own forced first login,
with no manual step outside the documented path; the machine-checkable half passed; and the
pair is scored against a written rubric whose result is recorded as phase evidence.
</success_criteria>

<output>
Create `.planning/phases/01-two-identities-live-and-separated/01-06-SUMMARY.md` when done.
</output>
