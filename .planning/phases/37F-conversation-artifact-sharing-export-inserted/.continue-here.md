---
context: phase
phase: 37F-conversation-artifact-sharing-export-inserted
plan: 09
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-07-17T16:37:07.795Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **gopls "undefined symbol" diagnostics are stale, not real** — WSL `go build ./...` is the only ground truth. Every plan this session triggered alarming `undefined: X` IDE diagnostics for symbols that were correctly defined; the compiler disproved each one. Do NOT act on IDE diagnostics — verify via `wsl bash -lc 'cd /mnt/d/Aura && go build ./...'`.
- [ ] CONSTRAINT: **This phase's locked plan artifacts contain STALE line references** — parallel commits moved the code after 37F's research was mapped. 37F-14 found `AppShell.tsx:514-517` (a floating cluster that commit `3f687e456` deleted, replacing it with `web/src/shell/ChatWorkspaceControls.tsx`); 37F-16 found `useBlobPreview.ts` was never re-pointed through 37F-05's R-05 seam. When a `read_first` line reference does not match what is on disk, TRUST THE CODE, adapt, and document the deviation — never force a stale instruction into a broken edit.
- [ ] CONSTRAINT: **NEVER run the coverage gate against the live `aura` DB** — `make coverage`/`scripts/coverage_gate.sh` reset schemas and have previously wiped live auth data. The gate provisions a disposable `aura_cov` DB; do not override that. Destructive drills go against a throwaway DB (37F-02 precedent: `aura_migrate0040_drill`).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trust gopls over compiler | IDE showed `undefined: BuildSnapshot`, `db.WithIdentityTxRaw`, `dropSnapshotBlobs` etc. — all real, index just lagged | blocking | Verify every "undefined" via `wsl ... go build ./...` before treating it as a bug |
| Force stale plan line-refs | Plans point at `AppShell.tsx:514-517` / seam wiring that later commits changed | blocking | Diff `read_first` refs against disk; adapt + document deviation, do not force |
| Coverage gate on live DB | `coverage_gate.sh` truncated live auth tables in a prior incident | blocking | Only the disposable `aura_cov` DB; refuse `db_integration` against a DB named `aura` locally |
| `git add -A` sweeps parallel work | User edits hooks/docs/codebase concurrently on master | advisory | Executors stage ONLY their own files by explicit path; never `-A` / `commit -a` |
| Secret leak via shell-quoting | 37F-02 leaked POSTGRES_PASSWORD into transcript via awk field-split through Bash→WSL | advisory | Compose DSNs inside a script that never echoes; redact before any log |
| Report skip as green | `db_integration` sub-second runtime = compiled-and-skipped, not passed | advisory | Confirm real per-test runtime (0.15–0.35s = real DB round trips) before claiming green |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Executing `/gsd-execute-phase 37F` wave-by-wave, sequentially on `master` (worktrees disabled). 10 of 19 plans complete; one gap-closure plan (37F-20) authored and committed but NOT executed. Paused cleanly at the wave-4 boundary — 37F-08 fully closed (code + SUMMARY + STATE + ROADMAP committed at `cdee8a9e1`). Working tree is clean except the two perpetually-dirty background-tool graph files. Next up is 37F-09.
</current_state>

<completed_work>

Wave 1: 37F-01 (PRD amendment + ADR-0039) ✓
Wave 2: 37F-02 (migration 0040) ✓ · 37F-03 (redaction core) ✓ · 37F-04 (token/expiry/key primitives) ✓ · 37F-05 (web foundations) ✓
Wave 3: 37F-06 (format adapters) ✓ · 37F-07 (store + audit ledger) ✓ · 37F-14 (ShareToggle) ✓ · 37F-16 (SharePage) ✓
Wave 4: 37F-08 (share lifecycle service) ✓

Post-merge gates for waves 2 and 3 both green: 68 packages, 0 failures.
Every relayed PASS was independently re-verified against WSL `go build`/tests; security-critical claims against source.
</completed_work>

<remaining_work>

- Wave 4 (finish): 37F-09 (WEBSHARE-01 export endpoint), 37F-15 (ShareModal + shareApi — wires 37F-14's `hasInternalShare`/`hasPublicShare` hardcoded-`false` stubs in `ChatWorkspaceControls.tsx`)
- Wave 5: 37F-10 (HTTP surface, 3 trust tiers), 37F-11 (expiry sweep + revoke-on-delete), 37F-17 (management surfaces)
- Wave 6: 37F-12 (mount on parent mux), **37F-20 (RLS backstop — gap closure, run before 37F-13)**
- Wave 7: 37F-13 (cross-identity deny E2E, WEBSHARE-04)
- Wave 8: 37F-18 (full local green + bundle rebuild + quality snapshot — will hit the objectstore 63.9% coverage debt)
- Wave 9: 37F-19 (manual verification + ship — CHECKPOINT, `autonomous: false`, requires human interaction)
</remaining_work>

<decisions_made>

- Sequential on master (worktrees disabled): concurrent commits race on `index.lock`; a worktree path is not `/mnt/d/Aura` so WSL tests would hit the wrong tree.
- Skills scoped per plan in each executor prompt (user-approved): Go plans get `golang-*`, web plans get react/web, doc plans get none. Saves ~500k tokens phase-wide vs. injecting all 20, zero safety loss.
- 37F-20 authored to add RLS on `shared_links` (user chose "Add migration 0041 + RLS policy"): mirror 0032's permissive-on-unset policy keyed on `owner_identity_id`; the unset-identity escape hatch keeps the plain-pool token/id resolvers working.
- `share_audit` append-only via grants only (no trigger) — consistent with `skill_audit`/`mcp_audit` precedent; documented, not a defect.
</decisions_made>

<blockers>
- RLS gap on `aura.shared_links`: mitigated by 37F-20 (committed `b2d4cb321`), not yet executed. Run it in wave 6 before 37F-13's E2E so the E2E can assert DB-layer denial.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full machine-readable state (commit SHAs per plan, next_action).
2. `docs/adr/0039-conversation-sharing-vs-identity-isolation.md` — the seven fail-closed mitigations every share-lifecycle/HTTP plan must implement; the governing security contract.
3. `.planning/phases/37F-conversation-artifact-sharing-export-inserted/37F-07-SUMMARY.md` — the store API + its documented RLS caveat that 37F-20 closes.
4. `.planning/phases/37F-conversation-artifact-sharing-export-inserted/37F-08-SUMMARY.md` — the share lifecycle service the remaining HTTP plans (37F-09/10) compose.
5. `.planning/phases/37F-conversation-artifact-sharing-export-inserted/deferred-items.md` — pre-existing debts (objectstore coverage 63.9%) that 37F-18 must face.

## Infrastructure State
- Docker stack (Postgres `aura-postgres` on `127.0.0.1:5432`, Neo4j) is UP — `db_integration` tests ran live this session.
- Post-merge gate script at `/mnt/d/tmp/w2_gate.sh` (WSL): build + untagged test, expects `68 pkgs, 0 fail`. Invoke `MSYS_NO_PATHCONV=1 wsl bash /mnt/d/tmp/w2_gate.sh`.
- Parallel session (the human) is actively committing on master: hooks (`lefthook.yml`, `scripts/check-file-size.sh` — now much faster), `CLAUDE.md`, `README.md`, `docs/*`, `.planning/codebase/*`, skill-search work. Executors must stage only their own files by explicit path.
- No background agents/servers left running by this workflow. No async external jobs.

<context>
The phase is healthy and ahead of the risk curve — the hard security primitives (redaction allowlist, CSPRNG token, identity-scoped store, lifecycle service) are done and verified, and the remaining plans are mostly HTTP wiring + UI + the E2E proof. The one architectural gap found (missing RLS) is already planned. Pace is ~30–55 min/plan dominated by real work (live DB drills, TDD cycles, per-edit WSL validation), not token waste. Resume by dispatching 37F-09 with the same scoped-skills executor prompt shape used all session.
</context>

<next_action>
Start with: re-run `/gsd-execute-phase 37F` (or resume manually) — dispatch `Agent(subagent_type=gsd-executor, model=sonnet)` for **37F-09** (WEBSHARE-01 export endpoint `GET /api/conversations/{id}/export?format=md|json`), giving it the Go-scoped skills set and the concurrency/toolchain/security preamble established this session. Then 37F-15 to finish wave 4, then the post-merge gate.
</next_action>
