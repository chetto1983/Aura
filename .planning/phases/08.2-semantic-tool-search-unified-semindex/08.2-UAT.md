---
status: testing
phase: 08.2-semantic-tool-search-unified-semindex
source: [08.2-VERIFICATION.md]
started: 2026-06-12T00:00:00Z
updated: 2026-06-12T00:00:00Z
---

## Current Test

number: 1
name: Live reasoning-classifier accuracy floor (Req-4 operator gate)
expected: |
  With the granite embedding sidecar up on :8081, the migrated reasoning
  classifier scores accPct >= 90 AND nvrPct (none-vs-rest) >= 92 on the
  24-prompt Italian corpus — proving the semindex migration did not regress
  the reasoning tier and the raised floors are real.
awaiting: user response

## Tests

### 1. Live reasoning-classifier accuracy (Req-4)
command: |
  # granite sidecar on :8081 must be up (WSL/Docker)
  go test -tags reasoning_live -run TestReasoningClassifierLive ./internal/agent/prompt/
expected: PASS with accPct >= 90 AND nvrPct >= 92 on the 24-prompt corpus
why_human: Requires live granite :8081 sidecar; the tier compiles (`go vet -tags reasoning_live` exits 0) and the automated equivalence + unit-corpus proxy stays green, but the live number must be confirmed once with the stack up. Must also re-validate the Plan-02 normalization-semantics nuance (centroid = mean of L2-normalized exemplars) against real granite embeddings.
result: [pending]

### 2. :ToolSelectionExample Neo4j round-trip (Req-5 persistence)
command: |
  # Neo4j + granite up (make neo4j-migrate); mcp/driver env set
  go test -tags reasoning_live -run TestToolSelectStoreE2E ./internal/toolselectstore/
expected: Save -> LoadExamples recovers the embedding byte-for-byte through both mandatory Cypher list-param workarounds
why_human: Requires a live Neo4j instance; no-skip-as-green — the e2e helper t.Fatal under $CI when its env is unset, so it cannot falsely pass. Local execution needs the stack up.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
