---
phase: 08.2-semantic-tool-search-unified-semindex
plan: 04
type: execute
wave: 3
depends_on: ["08.2-01", "08.2-02"]
files_modified:
  - internal/activelearn/activelearn.go
  - internal/activelearn/learner.go
  - internal/activelearn/learner_test.go
  - internal/reasoninglearn/learner.go
  - internal/agent/prompt/reasoning_classifier.go
  - internal/agent/prompt/reasoning_classifier_live_test.go
files_modified_note: "runner.go is NOT edited here (reasoninglearn keeps its New(cfg) signature; the refactor is internal). Disjoint from Plan 03 (tools/ + runner.go) and Plan 05 (which owns runner.go tool-select wiring). reasoning_classifier_live_test.go is edited here ONLY to RAISE the 80/88 floors to 90/92 — the stale one-arg COMPILE repair already landed in Plan 02 (Wave 2), this plan depends on 08.2-02 so the file already compiles under -tags reasoning_live when this runs."
autonomous: true
requirements: [Req-4, Req-5, D-05, D-04]
must_haves:
  truths:
    - "internal/activelearn is a label-agnostic async mechanism core: bounded queue + sha256 content-hash dedup + sync.Map seen-set + margin gate + non-blocking drop-on-full + refresh hook + goleak-clean Close — importing NEITHER prompt NOR a tool type"
    - "reasoninglearn.Learner is refactored onto internal/activelearn, behavior-preserving (same Observe/process/Close semantics, same prompt.ReasoningTier Oracle/Saver signatures, reasoning E2E stays green)"
    - "after the structural migration is proven (Plan 02) AND the reasoning_live tier already compiles (Plan 02 repaired the stale one-arg call), seed/anchor/oracle-example tuning raises TestReasoningClassifierLive (tag reasoning_live) from the shipped 80/88 floor to ≥ 90% accuracy / ≥ 92% none-vs-rest"
  artifacts:
    - path: "internal/activelearn/learner.go"
      provides: "the extracted label-agnostic async skeleton (queue/dedup/margin/refresh/Close)"
      contains: "func (l *Learner) Observe"
    - path: "internal/activelearn/activelearn.go"
      provides: "Oracle/Saver/Detector seams + Config, label opaque (no generics)"
      contains: "type Config struct"
    - path: "internal/reasoninglearn/learner.go"
      provides: "reasoning learner refactored onto activelearn, behavior-preserving"
      contains: "activelearn"
  key_links:
    - from: "internal/reasoninglearn/learner.go"
      to: "internal/activelearn"
      via: "delegates the queue/dedup/margin/refresh mechanism; keeps the prompt.ReasoningTier I/O"
      pattern: "activelearn"
    - from: "internal/agent/prompt/reasoning_classifier_live_test.go"
      to: "internal/agent/prompt.NewReasoningClassifier"
      via: "raised 90/92 floor (the two-arg compile repair already landed in Plan 02)"
      pattern: "accPct < 90"
---

<objective>
Extract the shipped `reasoninglearn.Learner` async skeleton into a label-agnostic
`internal/activelearn` core (Req-5 mechanism, D-05): bounded channel queue + sha256 content-hash
dedup + `sync.Map` seen-set + margin gate + non-blocking drop-on-full + refresh hook + one bounded
goleak-clean goroutine + `Close` (`once.Do(cancel)` + `wg.Wait`). The core imports NEITHER `prompt`
NOR a tool type — the observation is opaque; the consumer supplies `Oracle`/`Saver`/`Detector`. Then
refactor `reasoninglearn.Learner` ONTO this core, behavior-preserving (D-05; the reasoning E2E stays
green). FINALLY — sequenced AFTER the behavior-equivalence proof from Plan 02 — do the deliberate
accuracy RAISE (Req-4 part b, D-04 amendment): tune seeds/anchors/oracle examples to raise
`TestReasoningClassifierLive` from the shipped 80%/88% floor to ≥ 90% accuracy / ≥ 92% none-vs-rest.

NOTE the COMPILE repair moved: the stale one-arg `NewReasoningClassifier(embedder)` call in the live
test is repaired in **Plan 02** (Wave 2, the earliest plan touching `internal/agent/prompt/`), NOT
here. This plan depends on `08.2-02`, so by the time it runs the `reasoning_live` tier already
compiles; this plan ONLY raises the floors and tunes seeds to MEET the raised floor. Touching the live
test here is the floor-raise edit alone.

Purpose: D-05 honors REUSABLE-CODE (the mechanism is shared; only the divergent stores/oracles stay
specialized) and provides the async skeleton Plan 05's tool-selection loop reuses. The accuracy raise
is a DELIBERATE additive scope item (operator decision, 2026-06-12 amendment) — NOT a change to the
migration math. The named `TestAdaptiveReasoningItalianCorpusE2E` does NOT exist; the real gate is
`TestReasoningClassifierLive` (tag `reasoning_live`, floor 80/88 → 90/92).
Output: `internal/activelearn` core, a behavior-preserving `reasoninglearn` refactor, and a raised
live reasoning gate.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-SPEC.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-CONTEXT.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-RESEARCH.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-PATTERNS.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-VALIDATION.md
@internal/reasoninglearn/learner.go
@internal/agent/prompt/reasoning_classifier.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract internal/activelearn label-agnostic core + refactor reasoninglearn onto it (behavior-preserving)</name>
  <files>internal/activelearn/activelearn.go, internal/activelearn/learner.go, internal/activelearn/learner_test.go, internal/reasoninglearn/learner.go</files>
  <read_first>
    - internal/reasoninglearn/learner.go (FULL — the EXACT skeleton to extract: `Oracle`/`Saver`/`Config` :18-40, the bounded-queue/seen-set/margin-floor :44-90, `Observe` non-blocking + dedup + drop-on-full :95-108, `run`/`process` :110-139, goleak `Close` :143-149, `hashText` :151-154, the `//nolint:gosec // G118` retained-cancel :77)
    - 08.2-PATTERNS.md §`internal/activelearn/learner.go` (the full skeleton excerpt + the constants to carry + the label-seam options) + §`internal/activelearn/activelearn.go` (the Oracle/Saver/Config analog) + §"goleak-clean async lifecycle" + §"Content-hash dedup"
    - 08.2-CONTEXT.md D-05 (label-agnostic core; REJECTS Learner[L] generics — keep the observation opaque; refactor reasoninglearn behavior-preserving) ; 08.2-RESEARCH.md §D-05 + Open-Q #4 (the two viable label seams — pick one; the constraint is the core imports neither prompt nor a tool type, and the reasoning E2E stays green)
    - .claude/skills/golang-concurrency/SKILL.md + .claude/skills/golang-context/SKILL.md (the bounded-queue / drop-on-full / cancel+wg.Wait patterns)
  </read_first>
  <action>
    Create `internal/activelearn/activelearn.go`: the label-agnostic seams + `Config`. Pick the Open-Q #4
    seam — make the observation OPAQUE: `Oracle.Label(ctx, text) (label any, ok bool)` (or a typed callback
    the consumer owns) + `Saver.Save(ctx, text, vec []float64, label any) error` + an optional `Detector`
    hook; `Config{Oracle, Saver, Refresh func(), MarginFloor, Queue}`. D-05 REJECTS `Learner[L]` type params —
    do NOT genericize; the label stays opaque (`any`/`string`), the core imports NEITHER `prompt` NOR a tool
    type. Create `internal/activelearn/learner.go`: LIFT the `reasoninglearn` skeleton VERBATIM (queue/dedup/
    margin/refresh/drop-on-full/`run`/`process`/`Close`/`hashText`), carrying the constants
    (`defaultMarginFloor=0.05`, `defaultQueue=64`) and the `//nolint:gosec // G118` retained-cancel comment.
    `New(cfg)` returns nil if Oracle or Saver is missing (copy the guard). Create `internal/activelearn/learner_test.go`:
    mechanism unit tests with fake Oracle/Saver — assert Observe is non-blocking + margin-gated + dedup-gated +
    drop-on-full, refresh fires after a save, transient-failure retry (`seen.Delete` on oracle/save error), and
    goleak-clean `Close` (use `goleak.VerifyTestMain` or `goleak.VerifyNone`). THEN refactor
    `internal/reasoninglearn/learner.go` to DELEGATE the mechanism to `activelearn` while keeping its
    `prompt.ReasoningTier` `Oracle`/`Saver` signatures and its `prompt.Learner`-satisfying `Observe` — same
    external behavior, the queue/dedup/goroutine now live in `activelearn`. DEEP-REFACTOR-ON-TOUCH: remove the
    now-duplicated skeleton from `reasoninglearn`. Each file ≤ 600 LOC (~140 each).
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./internal/activelearn/ ./internal/reasoninglearn/ && go build ./... && go test -race ./internal/activelearn/ ./internal/reasoninglearn/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go test -race ./internal/activelearn/ ./internal/reasoninglearn/` exits 0
    - `internal/activelearn` imports NEITHER `prompt` NOR a tool type (`grep -c -E 'internal/agent/prompt|internal/agent/tools' internal/activelearn/*.go` returns 0 — D-05 / Open-Q #4)
    - the activelearn mechanism test is goleak-clean and asserts: drop-on-full (queue=1, flood → no block), margin-gated (above-floor margin dropped), dedup (same text labeled once), refresh-fires-after-save, transient-failure retry (VALIDATION.md Req-5 mechanism)
    - `reasoninglearn` no longer defines its own queue/goroutine skeleton (`grep -c -E 'make\(chan observation|go l.run\(\)' internal/reasoninglearn/learner.go` returns 0 — mechanism delegated); existing `reasoninglearn` behavior preserved (its package tests still pass)
    - `internal/reasoninglearn/learner.go` and `internal/activelearn/learner.go` are each ≤ 600 LOC
  </acceptance_criteria>
  <done>internal/activelearn holds the label-agnostic async skeleton (goleak-clean, drop-on-full, dedup, margin-gated), reasoninglearn delegates the mechanism behavior-preserving keeping its ReasoningTier I/O, and the duplicated skeleton is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Raise TestReasoningClassifierLive to 90/92 via seed/anchor tuning (the compile repair already landed in Plan 02)</name>
  <files>internal/agent/prompt/reasoning_classifier_live_test.go, internal/agent/prompt/reasoning_classifier.go</files>
  <read_first>
    - internal/agent/prompt/reasoning_classifier_live_test.go (FULL — the build tag `reasoning_live` :1, the 24-prompt `liveCorpus` :39-51, the constructor call at :61 — CONFIRM it is ALREADY the two-arg `NewReasoningClassifier(embedder, nil)` repaired by Plan 02; this plan does NOT re-repair it — and the floor asserts `accPct < 80` / `nvrPct < 88` :92-97 which THIS plan raises)
    - internal/agent/prompt/reasoning_classifier.go (the migrated classifier from Plan 02 — seeds `reasoningTierSeeds` :51-73, defs `reasoningTierDefs` :45-49, the anchors fold) — this is where seed/anchor tuning lands
    - internal/agent/prompt/reasoning_classifier_test.go (Plan 00 unit corpus — the existing unit tests that MUST stay green after tuning; the non-regression proxy)
    - internal/agent/prompt/reasoning_classifier_equivalence_test.go (Plan 02 — the behavior-equivalence proof that MUST stay green AFTER tuning; the raise is sequenced AFTER it)
    - 08.2-PATTERNS.md §MODIFIED `reasoning_classifier_live_test.go` (the floor-raise edit: 80/88 → 90/92 — the compile repair is Plan 02's, not this plan's)
    - 08.2-RESEARCH.md §3 (the gate is `TestReasoningClassifierLive` tag `reasoning_live` — NOT `TestAdaptiveReasoningItalianCorpusE2E`; the shipped floor is 80/88) + risk #1 + risk #2 ; 08.2-CONTEXT.md D-04 amendment (deliberate raise to 90/92, sequenced AFTER equivalence)
    - reference_cot_eval_harness (memory — the live eval harness context) ; project_adaptive_reasoning_router_latency (the seeds were spike-052 validated at 90/92 over 60 prompts)
  </read_first>
  <action>
    Edit `reasoning_classifier_live_test.go` (the REAL gate — `TestReasoningClassifierLive`, tag `reasoning_live`;
    do NOT reference the never-existent `TestAdaptiveReasoningItalianCorpusE2E`): RAISE the floors :92-97 from
    `accPct < 80`/`nvrPct < 88` to `accPct < 90`/`nvrPct < 92`. (The stale one-arg constructor call was ALREADY
    repaired to two-arg `NewReasoningClassifier(embedder, nil)` in Plan 02 — this plan depends on `08.2-02` so
    the file already compiles under `-tags reasoning_live`; do NOT re-repair it, only raise the floors.) Then,
    to MEET the raised floor on the 24-prompt Italian `liveCorpus`, tune `reasoningTierSeeds`/`reasoningTierDefs`
    (and/or the oracle-example fold path) in `reasoning_classifier.go` — additive seed/anchor refinement ONLY.
    This tuning is sequenced AFTER and MUST NOT break the Plan 02 behavior-equivalence test or any existing
    `reasoning_classifier_test.go` unit test (if a seed change moves a golden verdict, that is a regression —
    re-tune, do not weaken the equivalence test). Keep seeds authoritative; this is a deliberate accuracy
    improvement, not a math change. The live 90/92 confirmation requires granite `:8081` up (operator-gated
    Manual-Only tier); note this in the SUMMARY. Because the live 90/92 run is operator-gated, ALSO leave behind
    an automated non-regression PROXY (see acceptance) so a CI reviewer cannot bump the floor constants without
    actually re-tuning: after the seed/anchor tuning the full `reasoning_classifier_test.go` unit corpus AND the
    Plan 02 behavior-equivalence test must BOTH still pass green — the tuning may not regress any committed
    deterministic verdict.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet -tags reasoning_live ./internal/agent/prompt/ && go build -tags reasoning_live ./internal/agent/prompt/ && go test -race ./internal/agent/prompt/</automated>
  </verify>
  <acceptance_criteria>
    - the `reasoning_live` tier still COMPILES (the two-arg call repaired by Plan 02 is intact): `go vet -tags reasoning_live ./internal/agent/prompt/ && go build -tags reasoning_live ./internal/agent/prompt/` exits 0 (`grep -c 'NewReasoningClassifier(embedder, nil)' internal/agent/prompt/reasoning_classifier_live_test.go` returns 1 — NOT re-repaired here, just preserved)
    - the floors are raised: `grep -c 'accPct < 90' internal/agent/prompt/reasoning_classifier_live_test.go` returns 1 AND `grep -c 'nvrPct < 92' ...` returns 1
    - AUTOMATED non-regression proxy (proves the tuning did NOT just bump constants — W-4): after the seed/anchor tuning, `go test -race ./internal/agent/prompt/` exits 0 — i.e. the ENTIRE existing `reasoning_classifier_test.go` unit corpus AND the Plan 02 behavior-equivalence test BOTH still pass green; NO committed deterministic verdict regressed (VALIDATION.md Req-4 unit + equivalence rows)
    - MANUAL-ONLY (operator-gated, granite :8081 up): `go test -tags reasoning_live -run TestReasoningClassifierLive ./internal/agent/prompt/` reports accuracy ≥ 90% AND none-vs-rest ≥ 92% (VALIDATION.md Req-4 live row + Manual-Only table)
  </acceptance_criteria>
  <done>The live floor is raised to 90/92 (the stale-call compile repair already landed in Plan 02, preserved here), seed/anchor tuning meets it without breaking the behavior-equivalence proof or any existing unit test (the automated unit+equivalence corpus is the non-regression proxy that the floor was actually earned, not bumped); the live confirmation is the operator-gated reasoning_live run with granite up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| turn observation → activelearn queue | untrusted text/vectors enter the async bounded queue; must never block the hot path |
| activelearn → Oracle (LLM router) | the labeling teacher is a (paid) LLM call — must be margin-gated + async + bounded |
| activelearn → Saver (Neo4j) | persistence; idempotent by content hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-11 | Denial of Service | unbounded queue / hot-path block under a flood of uncertain turns | mitigate | bounded channel (default 64) + non-blocking `Observe` (drop-on-full) — carried verbatim from `reasoninglearn`; a drop-on-full test (queue=1, flood → no block) is an acceptance gate |
| T-08.2-12 | Tampering | duplicate/replayed observations amplifying a single text's influence | mitigate | sha256 content-hash dedup + `sync.Map` seen-set (one label per unique text, ever); transient-failure `seen.Delete` only on oracle/save error — covered by the mechanism test |
| T-08.2-13 | Denial of Service | goroutine leak on shutdown | mitigate | `Close` = `once.Do(cancel)` + `wg.Wait`; goleak assertion in the mechanism test |
| T-08.2-14 | Elevation of Privilege | the accuracy-raise tuning silently weakening the structural guarantee (bumping the floor constant without earning it) | mitigate | the raise is sequenced AFTER the Plan 02 behavior-equivalence test, which MUST stay green — re-tune rather than weaken; the AUTOMATED non-regression proxy (full unit corpus + equivalence stay green after tuning) is an acceptance gate so a constant-bump-without-tuning fails CI |
| T-08.2-SC | Tampering | npm/pip/cargo installs | accept | no new dependency — pure in-process refactor + test edits (Req-8) |
</threat_model>

<verification>
- `go build ./... && go test -race ./internal/activelearn/ ./internal/reasoninglearn/ ./internal/agent/prompt/` green
- `go build -tags reasoning_live ./internal/agent/prompt/` compiles (the stale call was repaired in Plan 02; this plan preserves it and raises the floors)
- `golangci-lint run ./internal/activelearn/ ./internal/reasoninglearn/` = 0; no duplicated skeleton
- activelearn imports neither prompt nor a tool type (D-05); reasoning E2E behavior preserved
- AUTOMATED non-regression proxy: after tuning, the full unit corpus + the Plan 02 equivalence test stay green (the floor was earned, not bumped)
- MANUAL-ONLY: `go test -tags reasoning_live -run TestReasoningClassifierLive ./internal/agent/prompt/` ≥ 90/92 with granite up
</verification>

<success_criteria>
- internal/activelearn is the shared label-agnostic mechanism (queue/dedup/margin/refresh/Close), goleak-clean (Req-5 mechanism, D-05)
- reasoninglearn refactored onto it, behavior-preserving (D-05)
- the reasoning_live tier compiles (the stale one-arg call was repaired in Plan 02; preserved here)
- TestReasoningClassifierLive raised to ≥90%/≥92% via seed/anchor tuning, sequenced after the equivalence proof, with an automated unit+equivalence non-regression proxy proving the floor was earned (Req-4 part b, D-04 amendment)
</success_criteria>

<output>
Create `.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-04-SUMMARY.md` when done
</output>
</output>
