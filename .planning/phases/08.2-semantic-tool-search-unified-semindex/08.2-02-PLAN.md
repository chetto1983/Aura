---
phase: 08.2-semantic-tool-search-unified-semindex
plan: 02
type: execute
wave: 2
depends_on: ["08.2-01"]
files_modified:
  - internal/agent/prompt/reasoning_classifier.go
  - internal/agent/prompt/reasoning_classifier_equivalence_test.go
  - internal/agent/prompt/reasoning_classifier_live_test.go
autonomous: true
requirements: [Req-4, D-04, D-01]
must_haves:
  truths:
    - "reasoning_classifier.go delegates its centroid/cosine/margin math to semindex.Classifier (Centroid mode) — the private helpers no longer live in package prompt"
    - "prompt.Embedder becomes a type alias of semindex.Embedder (documents.EmbeddingClient still satisfies it with no adapter)"
    - "for the SAME seed/anchor set the tier verdicts, centroids, and margins are unchanged (structural behavior-preserving)"
    - "the classifier KEEPS its soft LLM-router fallback: Classify returns ('', false) on any embed failure — the hard-dep is tool_search-only, NOT the classifier"
    - "all existing reasoning_classifier_test.go tests pass UNCHANGED"
    - "the reasoning_live tier COMPILES: the stale one-arg NewReasoningClassifier(embedder) call in reasoning_classifier_live_test.go is repaired to two-arg NewReasoningClassifier(embedder, nil) — the compile repair lands HERE (the earliest Wave-2 plan touching package prompt) so -tags reasoning_live builds through Waves 2-4 (the 80/88 → 90/92 floor RAISE is Plan 04)"
  artifacts:
    - path: "internal/agent/prompt/reasoning_classifier.go"
      provides: "the tier classifier re-expressed on semindex.Classifier; soft fallback preserved"
      contains: "semindex"
    - path: "internal/agent/prompt/reasoning_classifier_equivalence_test.go"
      provides: "behavior-equivalence test: identical tier verdicts at the same seeds, semindex-backed vs the shipped corpus"
      contains: "func Test"
    - path: "internal/agent/prompt/reasoning_classifier_live_test.go"
      provides: "the reasoning_live live-corpus test with its stale one-arg constructor call repaired to two-arg (compile-unblocks the operator-gated reasoning_live tier through Waves 2-4)"
      contains: "NewReasoningClassifier(embedder, nil)"
  key_links:
    - from: "internal/agent/prompt/reasoning_classifier.go"
      to: "internal/semindex"
      via: "semindex.Classifier{Centroid} for centroid/cosine/margin"
      pattern: "semindex\\."
    - from: "internal/agent/prompt"
      to: "internal/semindex"
      via: "type Embedder = semindex.Embedder alias"
      pattern: "type Embedder = semindex.Embedder"
    - from: "internal/agent/prompt/reasoning_classifier_live_test.go"
      to: "internal/agent/prompt.NewReasoningClassifier"
      via: "two-arg call (embedder, nil) — compile-unblocks the reasoning_live tier"
      pattern: "NewReasoningClassifier\\(embedder, nil\\)"
---

<objective>
Structurally migrate `reasoning_classifier.go` onto `semindex.Classifier` (Centroid mode),
behavior-preserving (Req-4 part a, D-04 structural). The private centroid/cosine/margin math
(`l2normalize`/`cosineSimilarity`/`meanNormalize`, the argmax-with-second loop, the `anchors`
build) is removed from package `prompt` and delegated to the shared core extracted in Plan 01.
`prompt.Embedder` becomes a `type Embedder = semindex.Embedder` alias so `documents.EmbeddingClient`
satisfies both with no adapter. The classifier's externally-observable behavior is UNCHANGED: the
greeting pre-filter, the soft LLM-router fallback (`("", false)` on embed failure), the seeds-
authoritative + oracle-examples-additive fold, the tier outputs/centroids/margins all identical at
the same seeds.

This plan ALSO repairs the stale one-arg `NewReasoningClassifier(embedder)` call in
`reasoning_classifier_live_test.go` (a compile blocker under `-tags reasoning_live`). The repair
lands HERE — this is the earliest Wave-2 plan that touches `internal/agent/prompt/` and the migration
renames/re-homes the classifier internals, so the `reasoning_live` tier MUST keep compiling from this
plan onward (it would otherwise stay broken through Waves 2-4). Only the COMPILE repair moves here;
the deliberate 80/88 → 90/92 accuracy floor RAISE stays in Plan 04 (sequenced AFTER this
behavior-equivalence proof).

Purpose: Prove the unified-core thesis on the lowest-risk consumer (its tests already exist), and
free `package prompt` of the duplicated math. This is the STRUCTURAL migration ONLY — the deliberate
accuracy RAISE to ≥90%/≥92% is Plan 04 (sequenced AFTER this behavior-equivalence proof). Do NOT
touch seeds/anchors for accuracy here, and do NOT raise the live floor here (Plan 04 owns that).
Output: a behavior-preserving migration + a behavior-equivalence test + a compiling `reasoning_live`
tier, all existing unit tests green.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-SPEC.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-CONTEXT.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-RESEARCH.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-PATTERNS.md
@.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-VALIDATION.md
@internal/semindex/semindex.go
@internal/semindex/classifier.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delegate the classifier math to semindex.Classifier; alias Embedder; preserve soft fallback; repair the stale reasoning_live call</name>
  <files>internal/agent/prompt/reasoning_classifier.go, internal/agent/prompt/reasoning_classifier_live_test.go</files>
  <read_first>
    - internal/agent/prompt/reasoning_classifier.go (FULL — the seams `Embedder`/`ExampleStore`/`Learner` :14-38, the two-arg constructor `func NewReasoningClassifier(embed Embedder, store ExampleStore)` :115, the struct :95-102, `Classify` :137-171 with the soft-fallback returns at :147,:151-153,:164-166, `ensureAnchors` :176-209, the math :218-264 now moved to semindex) — CONFIRM the shipped constructor is two-arg BEFORE editing the live-test call (it is: `NewReasoningClassifier(embed Embedder, store ExampleStore)`)
    - internal/agent/prompt/reasoning_classifier_live_test.go:1 (the `//go:build reasoning_live` tag) AND :55-64 (the `embedder` construction + the stale ONE-ARG `prompt.NewReasoningClassifier(embedder)` at :61 — repair to two-arg `(embedder, nil)`)
    - internal/semindex/classifier.go + internal/semindex/semindex.go (Plan 01: the Classifier API + the owned Embedder seam to alias)
    - internal/reasoningstore/store.go:14,46,61 AND internal/reasoninglearn/learner.go:15 (these import `prompt.LabeledVec`/`ExampleStore`/`Learner` — CASCADE-BREAK WARNING: alias ONLY `Embedder`; KEEP LabeledVec/ExampleStore/Learner in `prompt` or you break two shipped packages)
    - 08.2-PATTERNS.md §MODIFIED `internal/agent/prompt/reasoning_classifier.go` (the KEEP-the-soft-fallback excerpt + the alias rule)
    - 08.2-RESEARCH.md §D-04 mismatch #2 (the stale one-arg call, shipped signature two-arg since `31d3de34`) + risk #2 + risk #9 (the embed-hard-dep must NOT leak into the classifier) ; 08.2-CONTEXT.md D-04 structural-migration block
  </read_first>
  <action>
    Replace `prompt.Embedder` (the interface definition at :14-16) with `type Embedder = semindex.Embedder`
    (a type alias — interchangeable, preserves no-adapter). KEEP `LabeledVec`, `ExampleStore`, `Learner`
    EXACTLY where they are (reasoning-specific; `reasoningstore`/`reasoninglearn` import them — relocating
    cascade-breaks them). Re-express `ReasoningClassifier` so the per-tier centroid bank, the cosine argmax,
    and the top-2 margin are computed by a `semindex.Classifier{Centroid}` held by the struct. The
    `ensureAnchors` build (def+seeds per tier → `semindex.Classifier.AddVecs`/`Add` per label) replaces the
    private `meanNormalize` fold; the `Classify` argmax (:155-169) delegates to `semindex.Classifier.Rank`
    returning best label + Margin. DELETE the now-dead private `l2normalize`/`cosineSimilarity`/`meanNormalize`
    from this file (they live in `semindex/core.go` — DEEP-REFACTOR-ON-TOUCH, no dup). PRESERVE verbatim: the
    `normalizeForGreeting` pre-filter + `trivialGreetings` fast-path (:79-89,:141-145), the soft fallback —
    `Classify` MUST still return `("", false)` on any embed/build failure (:147,:151-153,:164-166) so the caller
    falls back to the LLM router (risk #9: the hard-dep is tool_search-only), the seeds-authoritative +
    oracle-examples-additive fold (:194-202), the `Refresh()`/`SetLearner()`/`NewReasoningClassifier(embed, store)`
    two-arg signatures, and `c.learner.Observe(userText, q, margin)` at :167-169. Keep the file ≤ 600 LOC.
    THEN, in the SAME commit (DEEP-REFACTOR-ON-TOUCH), repair the stale one-arg live-test call:
    `reasoning_classifier_live_test.go:61` `prompt.NewReasoningClassifier(embedder)` → `prompt.NewReasoningClassifier(embedder, nil)`
    (the shipped two-arg signature confirmed in read_first). This is a ONE-LINE compile repair — do NOT touch
    the `accPct < 80`/`nvrPct < 88` floors here (Plan 04 raises them to 90/92); do NOT change the corpus or any
    other assertion. The `reasoning_live` tier is an OPERATOR-GATED tier (needs granite :8081 up) — it is NOT
    the standard CI quality gate; the point of this repair is that `go build -tags reasoning_live` succeeds so
    the tier is runnable by an operator, not silently broken.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./internal/agent/prompt/ ./internal/reasoningstore/ ./internal/reasoninglearn/ && go build ./... && go build -tags reasoning_live ./internal/agent/prompt/ && go test -race ./internal/agent/prompt/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 (the Embedder alias does not cascade-break `reasoningstore`/`reasoninglearn`/`runner`)
    - the `reasoning_live` tier COMPILES: `go build -tags reasoning_live ./internal/agent/prompt/` exits 0 (the stale one-arg call is repaired — `grep -c 'NewReasoningClassifier(embedder, nil)' internal/agent/prompt/reasoning_classifier_live_test.go` returns 1 AND `grep -c 'NewReasoningClassifier(embedder)$' internal/agent/prompt/reasoning_classifier_live_test.go` returns 0)
    - all existing `reasoning_classifier_test.go` tests pass UNCHANGED: `go test -race ./internal/agent/prompt/` exits 0 (VALIDATION.md Req-4 unit row)
    - `reasoning_classifier.go` no longer defines `cosineSimilarity`/`meanNormalize` (dead-math removed — `grep -c -E 'func (cosineSimilarity|meanNormalize)' internal/agent/prompt/reasoning_classifier.go` returns 0); it imports `internal/semindex`
    - `prompt.Embedder` is a type alias (`grep -c 'type Embedder = semindex.Embedder' internal/agent/prompt/reasoning_classifier.go` returns 1); `LabeledVec`/`ExampleStore`/`Learner` are still defined in `prompt`
    - `Classify` still returns `("", false)` on embed failure (soft-fallback preserved — a unit test with a failing embedder asserts the false verdict)
    - the live floors are UNCHANGED in this plan (Plan 04 raises them): `grep -c 'accPct < 80' internal/agent/prompt/reasoning_classifier_live_test.go` returns 1 (still the shipped 80 floor — the 90 raise is Plan 04)
  </acceptance_criteria>
  <done>The classifier computes centroids/cosine/margin via semindex.Classifier; the private math is gone from package prompt; Embedder is an alias; the soft fallback + greeting pre-filter + seeds-authoritative fold are byte-for-byte preserved; the stale one-arg reasoning_live call is repaired to two-arg so the operator-gated tier compiles; all existing unit tests pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behavior-equivalence test — identical tier verdicts at the same seeds, pre/post migration</name>
  <files>internal/agent/prompt/reasoning_classifier_equivalence_test.go</files>
  <read_first>
    - internal/agent/prompt/reasoning_classifier_test.go:15-57 (`fakeEmbedder` + `vecFor` one-hot synthetic embedder) AND :129-160 (the corpus/verdict assertion shape to mirror) — the equivalence test reuses these doubles
    - internal/agent/prompt/reasoning_classifier.go (the migrated classifier from Task 1)
    - internal/semindex/classifier.go (the shared core the equivalence test exercises through the classifier)
    - 08.2-VALIDATION.md Req-4 row + Wave 0 "NEW behavior-equivalence test" gap
    - 08.2-RESEARCH.md §D-04 (equivalence = identical tier verdicts at the same seeds) ; 08.2-CONTEXT.md D-04
  </read_first>
  <action>
    Add `reasoning_classifier_equivalence_test.go` (package `prompt`, no build tag — runs free in CI).
    Using the synthetic `vecFor`/`fakeEmbedder` doubles (deterministic, no sidecar), build the classifier
    over the EXISTING seeds/anchors and assert, over a fixed corpus of inputs spanning all three tiers
    (none/low/high), that the tier verdict AND the margin sign/ordering are exactly what the shipped
    seed+centroid math produces — i.e. the semindex-backed path is verdict-identical to the pre-migration
    behavior at the same seeds. Pin the expected `(input → tier)` table as golden so any future drift in
    the centroid math is caught. This is the STRUCTURAL behavior-preservation proof, not an accuracy gate
    (the live accuracy raise is Plan 04). Per `golang-testing`: table-driven, parallel, deterministic,
    no external dependency.
  </action>
  <verify>
    <automated>cd /d/Aura && go test -race -run Equivalence ./internal/agent/prompt/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -run Equivalence ./internal/agent/prompt/` exits 0 (VALIDATION.md Req-4 behavior-equivalence row)
    - the test asserts a golden `(input → ReasoningTier)` table over ≥1 input per tier (none/low/high) using the synthetic embedder (no sidecar, deterministic, free in CI)
    - the test exercises the migrated classifier through `NewReasoningClassifier(embed, store)` (the two-arg shipped signature) — no `reasoning_live` build tag
  </acceptance_criteria>
  <done>A CI-free behavior-equivalence test pins the migrated classifier's tier verdicts to golden expectations at the same seeds, proving the structural migration changed no observable classification behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user turn → classifier | untrusted free text classified into a tier; the embed sidecar call is the only I/O |
| classifier → semindex | in-process; vectors crossing into the shared centroid math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-04 | Denial of Service | embed sidecar down during classification | mitigate | the classifier KEEPS its SOFT fallback — `Classify` returns `("", false)` and the caller falls back to the LLM router; the embed-hard-dep is `tool_search`-ONLY (risk #9). A unit test with a failing embedder asserts the false verdict |
| T-08.2-05 | Tampering | the Embedder type-alias cascade-breaking reasoning-specific types | mitigate | alias ONLY `Embedder`; KEEP `LabeledVec`/`ExampleStore`/`Learner` in `prompt`; `go build ./...` over `reasoningstore`+`reasoninglearn`+`runner` is an acceptance gate |
| T-08.2-06 | Repudiation | silent centroid-math drift after the migration | mitigate | the behavior-equivalence test pins golden tier verdicts at the same seeds — any drift fails CI |
| T-08.2-06b | Denial of Service | the `reasoning_live` tier silently broken (stale one-arg call) through Waves 2-4 | mitigate | the compile repair lands in THIS Wave-2 plan (earliest plan touching `package prompt`); `go build -tags reasoning_live ./internal/agent/prompt/` is an acceptance gate so the operator-gated tier stays runnable |
| T-08.2-SC | Tampering | npm/pip/cargo installs | accept | no new dependency added — this plan only re-wires existing in-process packages (Req-8) |
</threat_model>

<verification>
- `go build ./...` green (no cascade break)
- `go build -tags reasoning_live ./internal/agent/prompt/` compiles (the stale one-arg call is repaired here, NOT in Plan 04)
- `go test -race ./internal/agent/prompt/` green incl. all existing tests UNCHANGED + the new equivalence test
- `golangci-lint run ./internal/agent/prompt/` = 0; no dead math left in `reasoning_classifier.go`
- the classifier's soft LLM-router fallback is preserved (Req-4 / risk #9)
- the live floors stay at the shipped 80/88 in THIS plan (Plan 04 raises them to 90/92)
</verification>

<success_criteria>
- reasoning_classifier.go delegates centroid/cosine/margin to semindex.Classifier; private math deleted (Req-4 structural)
- Embedder is an alias; no cascade break; soft fallback preserved (D-01 / risk #9)
- the stale one-arg `reasoning_live` call is repaired to two-arg so the operator-gated tier compiles (risk #2 — repaired in the earliest Wave-2 plan, not Wave-3 Plan 04)
- behavior-equivalence test proves identical tier verdicts at the same seeds (Req-4 acceptance)
- all existing unit tests pass unchanged
</success_criteria>

<output>
Create `.planning/phases/08.2-semantic-tool-search-unified-semindex/08.2-02-SUMMARY.md` when done
</output>
</output>
