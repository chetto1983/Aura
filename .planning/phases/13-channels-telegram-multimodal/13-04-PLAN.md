---
phase: 13-channels-telegram-multimodal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/channels/channel.go
  - internal/channels/registry.go
  - internal/channels/registry_test.go
  - internal/channels/telegram/config.go
  - internal/channels/telegram/config_test.go
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "A Channel is a narrow interface (Name/Start/Stop/IsHealthy) — Start takes NO fanout subscriber (fanout is per-turn, not per-channel)"
    - "Registry.StartAll reads AURA_CHANNEL_<NAME>_ENABLED (default true) per channel, aggregates errors with errors.Join, and one failed channel never aborts the daemon (fail-soft)"
    - "The new AURA_* env knobs (channel enable, setup bind/token, vision cloud, telegram throttles) load with silent-fallback defaults"
  artifacts:
    - path: "internal/channels/channel.go"
      provides: "Channel interface (lifecycle contract)"
      contains: "type Channel interface"
    - path: "internal/channels/registry.go"
      provides: "Registry StartAll/StopAll with per-channel enable gate + errors.Join"
      min_lines: 60
    - path: "internal/channels/telegram/config.go"
      provides: "telegram channel config (bot token env, bind, throttles)"
      min_lines: 40
  key_links:
    - from: "internal/channels/registry.go"
      to: "AURA_CHANNEL_<NAME>_ENABLED"
      via: "envBoolDefault gate before Start"
      pattern: "AURA_CHANNEL"
    - from: "internal/config/config.go"
      to: "AURA_SETUP_BIND / AURA_VISION_CLOUD / AURA_TELEGRAM_*"
      via: "env-helper loads into Config"
      pattern: "AURA_VISION_CLOUD|AURA_SETUP_BIND"
---

<objective>
Build the Channels framework (UX-02 deliverable, ~170 LOC): the narrow `Channel` interface, the `Registry` that aggregates lifecycle fail-soft with a per-channel `AURA_CHANNEL_<NAME>_ENABLED` gate, the Telegram channel's `config.go` (bot-token env + bind + throttles), and the new Phase-13 `AURA_*` env knobs added to the central `config.Config`. No fanout subscriber on `Start` — fanout is built per-turn inside the channel (research §1 critical correction; the PRD's `Start(ctx, sub)` sketch is wrong for this codebase).

Purpose: This is the framework every later channel mounts into and the env surface the serve.go wiring (plan 13-09) reads. Decoupling it to Wave 1 lets the Telegram-core plans (13-05/06) depend on a stable interface + config rather than racing them.
Output: `internal/channels/channel.go`, `internal/channels/registry.go` + test, `internal/channels/telegram/config.go` + test, the `config.Config` additions.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md
@.planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Channel interface + Registry (fail-soft, enable-gated)</name>
  <files>internal/channels/channel.go, internal/channels/registry.go, internal/channels/registry_test.go</files>
  <read_first>
    - internal/agent/tools/spec.go (lines 68-99 — the Tool interface narrowness + Registry{map}/NewRegistry/Register idiom to mirror)
    - cmd/aura/serve.go (lines 84-102 — the fail-soft "log but never exit" daemon-subsystem posture + graceful shutdown the registry replicates)
    - internal/config/config.go (lines 343-369 — envBoolDefault silent-fallback helper shape, for the enable gate)
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§Pattern 1 + Pattern 2 — the interface shape + StartAll/StopAll error-aggregation contract)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§channel.go + §registry.go — role-match, replicate the shape)
  </read_first>
  <behavior>
    - `Channel` interface: `Name() string`, `Start(ctx context.Context) error`, `Stop(ctx context.Context) error`, `IsHealthy() bool`. Start takes NO subscriber (per-turn fanout is internal to the channel).
    - `Registry`: map-backed (`NewRegistry`/`Register(Channel)`); `StartAll(ctx)` reads `AURA_CHANNEL_<upper(Name)>_ENABLED` (default true) per registered channel, skips disabled ones, calls Start, and aggregates failures with `errors.Join` — one failed channel returns an error but does NOT abort the others (fail-soft).
    - `StopAll(ctx)` calls Stop on every started channel, aggregating errors with errors.Join; idempotent.
    - A disabled channel is never Started (gate respected) and its Stop is a no-op.
  </behavior>
  <action>Create `internal/channels/channel.go` with the 4-method `Channel` interface (doc-comment stating Start takes no subscriber — fanout is per-turn). Create `internal/channels/registry.go` with `Registry{channels map[string]Channel}`, `NewRegistry`, `Register`, `StartAll(ctx) error`, `StopAll(ctx) error`. StartAll uppercases the channel name into `AURA_CHANNEL_<NAME>_ENABLED`, checks it via an envBoolDefault-shaped helper (default true), starts enabled channels, joins errors. Track started channels so StopAll only stops what it started. Test with a fake Channel double: enable-gate respected, one-failing-Start-does-not-abort-others (errors.Join contains the failure), StopAll joins, goleak-clean (the package main_test.go from plan 13-01 provides the leak gate). Honor the `--no-telegram`/`--only=cli` override wiring as a registry input field (the flags are parsed in serve.go plan 13-09; here accept an override map/predicate).</action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/channels/ -run 'Registry|Channel' 2>&1 | tail -15</automated>
  </verify>
  <done>StartAll honors the per-channel enable env, fail-softly aggregates Start errors via errors.Join, never aborts siblings on one failure; StopAll joins and is idempotent; the fake-double tests pass under goleak.</done>
  <acceptance_criteria>
    - `go test ./internal/channels/ -run Registry` proves: enable-gate respected, one-failing-channel-does-not-abort-others, errors.Join aggregation (VALIDATION UX-02 "Channel interface + registry StartAll/StopAll + AURA_CHANNEL_*_ENABLED + error aggregation").
    - `grep 'type Channel interface' internal/channels/channel.go` matches; the interface has exactly Name/Start/Stop/IsHealthy.
    - `Start` signature takes `context.Context` only — NO fanout/subscriber param (research §1 correction).
    - `grep 'errors.Join' internal/channels/registry.go` matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: telegram config.go + central AURA_* env additions</name>
  <files>internal/channels/telegram/config.go, internal/channels/telegram/config_test.go, internal/config/config.go, internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go (lines 222-234 — the AGUIBind/envBoolDefault/envIntDefault block to extend; line 230 `envDefault("AURA_AGUI_BIND", "127.0.0.1:9080")` is the loopback-bind precedent)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§config.go — the env-helper idiom + the env catalog)
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§Multimodal sidecar wiring env catalog — exact var names + defaults + the AURA_ vs upstream-naming split)
    - CLAUDE.md (env var convention: AURA_<DOMAIN>_<UNIT>; third-party/sidecar keep upstream naming)
  </read_first>
  <behavior>
    - telegram `config.go` reads `TELEGRAM_BOT_TOKEN` (upstream naming, third-party), the throttles `AURA_TELEGRAM_STATUS_THROTTLE_MS` (default 1500), `_CONTENT_THROTTLE_MS` (default 500), `_CHAT_RATE_LIMIT_MS` (default 1000), via silent-fallback helpers.
    - Central `config.Config` gains: `AURA_SETUP_BIND` (default `127.0.0.1:9081`, loopback, distinct from `:9080` AGUI), `AURA_SETUP_TOKEN` (empty default → generated at boot in plan 13-07), `AURA_VISION_CLOUD` (bool, default false), and the sidecar URLs `MULTIMODAL_BASE_URL`/`MULTIMODAL_MODEL`, `STT_BASE_URL`/`STT_MODEL`, `TTS_BASE_URL`/`TTS_VOICE`/`TTS_FORMAT`, plus `MULTIMODAL_FALLBACK_MODEL` (default `minimax/minimax-m3`) (upstream naming for sidecars per CLAUDE.md exception).
    - Malformed values fall back silently (never fatal) — the envBoolDefault/envIntDefault contract.
  </behavior>
  <action>Create `internal/channels/telegram/config.go` with a `Config` struct + loader reading the bot token + the three throttle ints via the config envIntDefault-shaped helpers (or expose them through the central config; pick one and be consistent). Extend `internal/config/config.go`'s Load near line 222: add `AURA_SETUP_BIND` (default `127.0.0.1:9081`), `AURA_SETUP_TOKEN` (default ""), `AURA_VISION_CLOUD` (envBoolDefault false), and the sidecar URL/model knobs with upstream naming. Update `config_test.go` to assert the new defaults + an override. Document each new var with a comment citing the env convention. Do NOT add `skip2/go-qrcode`-related config (qr_svg deferred).</action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/config/ ./internal/channels/telegram/ -run 'Config|Env' 2>&1 | tail -15</automated>
  </verify>
  <done>All new AURA_* + sidecar env vars load with the documented defaults and honor overrides; malformed values fall back silently; AURA_SETUP_BIND defaults to a loopback port distinct from AGUI's :9080.</done>
  <acceptance_criteria>
    - `go test ./internal/config/` proves the new defaults (AURA_SETUP_BIND=127.0.0.1:9081, AURA_VISION_CLOUD=false) + override behaviour.
    - `grep 'AURA_VISION_CLOUD' internal/config/config.go` and `grep 'AURA_SETUP_BIND' internal/config/config.go` both match.
    - `AURA_SETUP_BIND` default port differs from the AGUI `:9080` bind (separate-port requirement).
    - telegram `config.go` reads `TELEGRAM_BOT_TOKEN` (upstream naming) — NOT an `AURA_`-prefixed token (CLAUDE.md third-party exception).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → daemon config | env vars control channel enablement + bind addresses + the vision route |
| one channel failure → daemon | a misbehaving channel must not crash the long-lived serve daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-DaemonAbort | DoS | Registry.StartAll | mitigate | fail-soft errors.Join: one channel's Start failure never aborts the others or the daemon (Task 1) |
| T-13-04-SetupExposure | Spoofing/Elevation | AURA_SETUP_BIND | mitigate | default `127.0.0.1:9081` loopback; the override exists only for deliberate remote-QR scan (operator choice), token gate is plan 13-07 |
| T-13-04-EnableBypass | Tampering | AURA_CHANNEL_*_ENABLED | accept | default-true is intentional (a registered channel runs unless disabled); operator owns the env |
</threat_model>

<verification>
- `go test ./internal/channels/ -run Registry` green (fail-soft + enable gate).
- `go test ./internal/config/` green (new env defaults + overrides).
- `go test ./internal/channels/telegram/ -run Config` green.
</verification>

<success_criteria>
- Channel interface is the narrow 4-method contract with subscriber-free Start.
- Registry is fail-soft + enable-gated + errors.Join-aggregating.
- The Phase-13 AURA_* + sidecar env surface loads with documented loopback defaults.
</success_criteria>

<output>
Create `.planning/phases/13-channels-telegram-multimodal/13-04-SUMMARY.md` when done.
</output>
