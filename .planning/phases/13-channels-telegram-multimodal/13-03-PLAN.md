---
phase: 13-channels-telegram-multimodal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/llm/models.go
  - internal/llm/models_test.go
  - internal/channels/telegram/mdv2.go
  - internal/channels/telegram/mdv2_test.go
  - internal/channels/telegram/tables.go
  - internal/channels/telegram/tables_test.go
  - internal/channels/telegram/testdata
autonomous: true
requirements: [UX-02, UX-04]

must_haves:
  truths:
    - "A model→capability table reports SupportsVision/SupportsAudio (deepseek-v4-flash=false, minimax-m3=true) — net-new, no Model struct existed"
    - "The MarkdownV2 escaper escapes reserved chars OUTSIDE entities only and survives a 10K-Unicode fuzz with zero `400 can't parse entities`"
    - "A markdown table renders to a deterministic gridded PNG (4-col and 6-col) via x/image + embedded gofonts; pre-block monospace is the ≤56-char fallback"
  artifacts:
    - path: "internal/llm/models.go"
      provides: "model capability lookup (SupportsVision/SupportsAudio)"
      contains: "SupportsVision"
    - path: "internal/channels/telegram/mdv2.go"
      provides: "entity-aware MarkdownV2 escaper + plain-text fallback signal"
      min_lines: 50
    - path: "internal/channels/telegram/tables.go"
      provides: "markdown table → gridded PNG (x/image) + pre-block fallback"
      min_lines: 100
  key_links:
    - from: "internal/channels/telegram/mdv2.go"
      to: "Telegram MarkdownV2 reserved set"
      via: "escape outside entities only, never whole-string"
      pattern: "MarkdownV2|escape"
    - from: "internal/channels/telegram/tables.go"
      to: "golang.org/x/image/font/gofont"
      via: "opentype face + draw grid + png.Encode"
      pattern: "gofont|opentype"
---

<objective>
Build the three pure-transform / data-declaration pieces the Telegram renderer (plan 13-05) and photo client (plan 13-08) consume, all with no cross-file conflicts: (1) the net-new model-capability table (`SupportsVision`/`SupportsAudio` — `models.go` does NOT exist today, `Model` is a bare string at `config.go:77`); (2) the entity-aware MarkdownV2 escaper (~80 LOC, locked in-tree by amendment #4, proven by a 10K-Unicode fuzz); (3) the markdown-table→PNG renderer (x/image + embedded gofonts, spike 018b winner).

Purpose: These are the leaf transforms the renderer/photo wire together. Decoupling them into one Wave-1 plan keeps the heavy Telegram-core plans (13-05/06) focused on event flow, and gets the fuzz + golden gates green early. The escaper is the SC#2 "no 400 can't parse entities" guarantee; the table PNG is the operator's on-device head-to-head verdict; the capability flags are the `AURA_VISION_CLOUD=true` precondition.
Output: `internal/llm/models.go` + test, `internal/channels/telegram/mdv2.go` + fuzz, `internal/channels/telegram/tables.go` + golden test.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md
@.planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md
@.planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md
@.claude/skills/spike-findings-Aura/references/telegram-channel.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: net-new model capability table (SupportsVision/SupportsAudio)</name>
  <files>internal/llm/models.go, internal/llm/models_test.go</files>
  <read_first>
    - internal/llm/config.go (lines 60-96 — Model is a bare `string` field; there is NO Model struct and NO openai_compat/models.go; the research's cited path is stale per PATTERNS)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§"No Analog Found" — this is genuinely novel; the planner decides where the map lives)
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§7 vision branch — deepseek-v4-flash=false, minimax-m3=true; default AURA_VISION_CLOUD=false)
  </read_first>
  <behavior>
    - `SupportsVision(model string) bool`: returns true for vision-capable models (`minimax/minimax-m3` and its `:` variants), false for `deepseek/deepseek-v4-flash` and unknown models (conservative default false).
    - `SupportsAudio(model string) bool`: false for both current models (audio is sidecar-routed this phase); the function exists for forward-compat symmetry.
    - Matching is prefix/normalize-tolerant of the OpenRouter `:exacto`/`:flash` suffixes (the same model id may carry a routing suffix).
  </behavior>
  <action>Create `internal/llm/models.go` in package `llm` with a small `model→{vision,audio}` table and the two lookup functions `SupportsVision(model string) bool` / `SupportsAudio(model string) bool`. Normalize the model id (strip an OpenRouter `:`-suffix before lookup) so `minimax/minimax-m3:something` still resolves. Seed: `deepseek/deepseek-v4-flash` = {vision:false,audio:false}, `minimax/minimax-m3` = {vision:true,audio:false}; unknown → both false (conservative). Do NOT change the `Config.Model` bare-string field — this is a pure additive lookup the photo client (13-08) calls. Keep it ~30 LOC.</action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/llm/ -run 'Supports|Models|Capab' 2>&1 | tail -12</automated>
  </verify>
  <done>SupportsVision returns true for minimax-m3 (incl. a suffixed id), false for deepseek-v4-flash and unknown; SupportsAudio false for both; Config.Model untouched.</done>
  <acceptance_criteria>
    - `go test ./internal/llm/ -run Supports` passes the vision/audio truth table (VALIDATION UX-04 photo.go env-branch precondition).
    - `grep 'func SupportsVision' internal/llm/models.go` matches.
    - `SupportsVision("minimax/minimax-m3:thinking")` and `SupportsVision("minimax/minimax-m3")` both return true; `SupportsVision("deepseek/deepseek-v4-flash:exacto")` returns false.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entity-aware MarkdownV2 escaper + 10K fuzz</name>
  <files>internal/channels/telegram/mdv2.go, internal/channels/telegram/mdv2_test.go</files>
  <read_first>
    - .claude/skills/spike-findings-Aura/references/telegram-channel.md (§MarkdownV2 discipline — the reserved set, the inside-fence rule, the all-or-nothing 400 behaviour, the plain-text fallback mandate — this is the BINDING spec, there is no code analog)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§mdv2.go — partially-novel, spec from spike, proof = 10K fuzz)
    - .planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md (the FuzzMdv2 row + 30s fuzztime command)
  </read_first>
  <behavior>
    - Reserved OUTSIDE entities: `_ * [ ] ( ) ~ ` `> # + - = | { } . ! \` — each escaped with a leading `\` when it appears outside an intended bold/italic/code/pre span.
    - Inside ```pre```/`code` fences: only `` ` `` and `\` are reserved; pipes, dashes, dots flow through unescaped (verified live with table payloads).
    - NEVER whole-string escape (that neutralizes the bot's own formatting — the 017 throwaway escaper is the negative example).
    - The escaper exposes a signal/helper so the renderer can do the plain-text fallback: if a MarkdownV2 send 400s, resend WITHOUT ParseMode (the fallback lives in the renderer, plan 13-05; this file provides the escape + a documented contract).
    - A 10K-Unicode fuzz (`FuzzMdv2`) plus a seeded corpus produces output that, when wrapped in a MarkdownV2 message, never yields `400 can't parse entities` (asserted against a parser/validator model of the reserved grammar, since live sends are the telegram_integration tier).
  </behavior>
  <action>Create `internal/channels/telegram/mdv2.go` implementing the entity-aware escaper per the spike's `telegram-channel.md` MarkdownV2 discipline. Track fence state (inside ```/` vs outside) and apply the correct reserved set per region; escape outside-entity reserved chars only. Write `FuzzMdv2` (`testing.F`) with a ≥10K-entry seeded corpus of Unicode + prose-punctuation strings; the oracle asserts the escaped output is a well-formed MarkdownV2 entity stream (no unescaped reserved char outside a fence, no broken fence) — i.e. it would not 400. Add the seed corpus as a testdata table. Live send-assertion is deferred to the telegram_integration tier (plan 13-09).</action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/channels/telegram/ -run Mdv2 && go test ./internal/channels/telegram/ -run FuzzMdv2 -fuzz FuzzMdv2 -fuzztime 30s 2>&1 | tail -15</automated>
  </verify>
  <done>The escaper escapes outside-entity reserved chars, leaves in-fence pipes/dashes/dots intact, never whole-string escapes; FuzzMdv2 runs 30s with zero crashes / zero would-400 outputs; the seeded 10K corpus passes.</done>
  <acceptance_criteria>
    - `go test -run FuzzMdv2 -fuzz FuzzMdv2 -fuzztime 30s ./internal/channels/telegram/` reports no failing input (VALIDATION UX-02 "MarkdownV2 escaper: 10K Unicode fuzz, no 400 can't parse entities").
    - A unit case proves pre-fence pipes/dashes are NOT escaped (VALIDATION "pre-fence pipes/dashes unescaped").
    - A unit case proves a naked `-`/`.`/`(` outside a fence IS escaped.
    - `grep -i 'whole.string\|EscapeAll' internal/channels/telegram/mdv2.go` returns nothing (no whole-string escape — the anti-pattern).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: markdown table → gridded PNG (x/image) + pre-block fallback</name>
  <files>internal/channels/telegram/tables.go, internal/channels/telegram/tables_test.go, internal/channels/telegram/testdata</files>
  <read_first>
    - .claude/skills/spike-findings-Aura/references/telegram-channel.md (§Table rendering — the proven ~150 LOC x/image pipeline: opentype face Size 28 DPI 72, gomono cells + gomonobold header, MeasureString per column, pad 16px, draw grid, png.Encode; pre-block ≤56-char fallback)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§tables.go — partially-novel, body from spike source 018b)
    - .planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md (tables.go golden 4-col/6-col + pre-block fallback row)
  </read_first>
  <behavior>
    - Detect a markdown table (a `|`-delimited header row + a `|---|` separator row + N data rows) in a string and return the parsed cell grid.
    - Render the grid to a deterministic PNG: gomonobold header row, gomono data cells, per-column width from MeasureString + 16px pad, drawn grid lines, png.Encode; the same input yields byte-stable dims+grid (golden test for a 4-col and a 6-col table).
    - A pre-block monospace fallback pads cells to per-column rune widths inside a ``` fence; documented as readable only ≤56 char/row (the operator's on-device ceiling).
    - The renderer (plan 13-05) decides PNG-vs-fallback and calls sendPhoto; this file is the pure transform (markdown → PNG bytes / pre-block string).
  </behavior>
  <action>Create `internal/channels/telegram/tables.go` porting the spike 018b pipeline: parse the markdown table to a `[][]string` grid, render via `golang.org/x/image/font/opentype` with `font/gofont/gomono` (cells) + `gomonobold` (header), measure columns with `font.Drawer.MeasureString`, pad 16px, draw the grid, `png.Encode` to bytes. Add the pre-block fallback function (pad to per-column rune width inside a ``` fence). Golden-test the 4-col and 6-col PNGs for deterministic dims+grid (store golden bytes/dims in testdata). Promote `golang.org/x/image` is already done in plan 13-01 — just import it. Keep the file ≤600 LOC (it is ~150).</action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/channels/telegram/ -run 'Table|tables' 2>&1 | tail -15</automated>
  </verify>
  <done>A 4-col and 6-col markdown table each render to a deterministic PNG matching the golden dims+grid; the pre-block fallback pads cells correctly; rendering is pure (no network, no Telegram call).</done>
  <acceptance_criteria>
    - `go test ./internal/channels/telegram/ -run Table` passes the 4-col + 6-col golden (VALIDATION UX-02 "tables.go 4-col/6-col → deterministic PNG (dims+grid)").
    - The pre-block fallback produces a `` ``` ``-fenced monospace table (VALIDATION "pre-block fallback ≤56 char/row").
    - `grep 'gomono' internal/channels/telegram/tables.go` matches (embedded Go fonts, no CGO/fontconfig).
    - No `fogleman/gg` or headless-browser import (spike "What to Avoid").
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM prose → Telegram MarkdownV2 | model-generated text crosses into a strict parser; one bad char 400s the whole send |
| markdown table → rendered image | model-controlled table content drives the PNG render (size/contents) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-MdV2 | Tampering / DoS | mdv2.go escaper | mitigate | entity-aware escape + the renderer's plain-text fallback; 10K fuzz acceptance proves no would-400 output (Task 2) |
| T-13-03-PNGoom | DoS | tables.go render of a huge table | accept | tables are bounded by the conversation turn cap upstream; render cost is 5-21ms/22-67KB measured (spike); a pathological table degrades to the pre-block fallback, not an OOM |
| T-13-03-UnknownModelVision | Spoofing of capability | models.go | mitigate | unknown model → SupportsVision false (conservative), so a typo never routes an image to a non-vision model (Task 1) |
</threat_model>

<verification>
- `go test ./internal/llm/ -run Supports` green (capability table).
- `go test -run FuzzMdv2 -fuzz FuzzMdv2 -fuzztime 30s ./internal/channels/telegram/` no failing input.
- `go test ./internal/channels/telegram/ -run Table` green (4-col + 6-col golden).
</verification>

<success_criteria>
- models.go provides SupportsVision/SupportsAudio with conservative unknown-default false.
- mdv2.go is entity-aware, fuzz-clean, never whole-string-escapes.
- tables.go renders deterministic PNGs + a pre-block fallback using embedded gofonts.
</success_criteria>

<output>
Create `.planning/phases/13-channels-telegram-multimodal/13-03-SUMMARY.md` when done.
</output>
