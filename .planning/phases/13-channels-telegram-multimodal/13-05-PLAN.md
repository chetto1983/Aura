---
phase: 13-channels-telegram-multimodal
plan: 05
type: execute
wave: 2
depends_on: [13-01, 13-03, 13-04]
files_modified:
  - internal/channels/telegram/bot.go
  - internal/channels/telegram/agui_subscriber.go
  - internal/channels/telegram/renderer.go
  - internal/channels/telegram/status_pane.go
  - internal/channels/telegram/bot_test.go
  - internal/channels/telegram/renderer_test.go
  - internal/channels/telegram/status_pane_test.go
  - internal/channels/telegram/testdata
autonomous: true
requirements: [UX-02]

must_haves:
  truths:
    - "The Telegram channel implements Channel: Start launches a telebot polling goroutine; Stop drains it goleak-clean"
    - "Each turn builds NewFanout(Translate(...)) → Subscribe×2 (status+content) BEFORE Run → consumes both subscriber channels (per-turn, never one fanout at channel start)"
    - "AG-UI events map to status-pane-B: msg#1 edited in place (tool list 🟡→✅/❌, 💭 reasoning, cost footer); msg#2 streamed content escaped via mdv2 with a plain-text fallback; tables→PNG sendPhoto"
    - "A MarkdownV2 send that 400s is resent WITHOUT ParseMode (plain-text fallback) — SC#2 'no can't parse entities'"
  artifacts:
    - path: "internal/channels/telegram/bot.go"
      provides: "telebot.Bot wrapper implementing Channel; polling Start/Stop"
      min_lines: 80
    - path: "internal/channels/telegram/agui_subscriber.go"
      provides: "per-turn NewFanout/Subscribe×2/Run wiring (the seam consumer)"
      min_lines: 40
    - path: "internal/channels/telegram/renderer.go"
      provides: "content-event consumer → escaped send + tables PNG + throttle"
      min_lines: 80
    - path: "internal/channels/telegram/status_pane.go"
      provides: "status-event consumer → msg#1 edited tool list + reasoning + cost"
      min_lines: 80
  key_links:
    - from: "internal/channels/telegram/agui_subscriber.go"
      to: "internal/agui/fanout.go"
      via: "Subscribe×2 before Run (per turn)"
      pattern: "NewFanout|Subscribe"
    - from: "internal/channels/telegram/renderer.go"
      to: "internal/channels/telegram/mdv2.go"
      via: "escape + plain-text fallback on 400"
      pattern: "mdv2|ParseMode"
---

<objective>
Build the Telegram channel core: the `bot.go` telebot wrapper that implements `Channel` (polling Start/Stop, goleak-clean), the `agui_subscriber.go` per-turn fanout consumer (Subscribe-before-Run, the Phase-12 seam), and the two render consumers — `status_pane.go` (msg#1 edited: tool list 🟡→✅/❌, 💭 reasoning, running cost footer) and `renderer.go` (msg#2 streamed content, mdv2-escaped with the plain-text fallback, tables→PNG sendPhoto). This is the SC#2 "streamed reply with status pane + content, no 400 can't parse entities" surface.

Purpose: The heart of UX-02. The channel drives `runner.Turn` through `agui.Translate` → `NewFanout` → `Run`, fanning status + content to two stateful renderers with the locked status-pane-B throttles. The biggest risk (research §Don't Hand-Roll key insight) is re-implementing event distribution — this plan MUST consume `internal/agui/fanout.go`, not rebuild it.
Output: bot.go, agui_subscriber.go, renderer.go, status_pane.go + their golden/unit tests.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md
@.planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md
@.planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md
@.claude/skills/spike-findings-Aura/references/telegram-channel.md
@internal/agui/fanout.go
@internal/agui/translator.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bot.go (Channel impl) + agui_subscriber.go (per-turn fanout)</name>
  <files>internal/channels/telegram/bot.go, internal/channels/telegram/agui_subscriber.go, internal/channels/telegram/bot_test.go</files>
  <read_first>
    - internal/agui/fanout.go (NewFanout/Subscribe/Run — Subscribe-before-Run PANICS at :51, double-Run PANICS at :67; the producer closes both channels on ctx-cancel/source-end)
    - internal/agui/translator.go (line 23 — `Translate(threadID, runID, idgen, seq) iter.Seq2[events.Event, error]` signature)
    - internal/runner/runner.go (line 177 — `Turn(ctx, convID, *userMsg) iter.Seq2[*agent.Event, error]`, the source iterator)
    - cmd/aura/serve.go (lines 84-102 — the fail-soft start-goroutine + graceful-shutdown posture bot.go's Start/Stop replicate)
    - cmd/aura/chat.go (lines 48-58 — chatEnv exposes run/conv/pause/cfg/reg the telegram.Deps consumes)
    - .claude/skills/spike-findings-Aura/references/telegram-channel.md (§Transport — tele.NewBot{Token}, Offline:true for unit tests, Send response is the read-back ground truth)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§bot.go + §agui_subscriber.go)
  </read_first>
  <behavior>
    - `telegram.New(Deps{Runner *runner.Runner, Conv, Pause *askuser.Store, Token string, Store *telegram.Store, StatusThrottle/ContentThrottle/ChatRateLimit time.Duration, ...})` returns a `*Telegram` implementing `channels.Channel`.
    - `Start(ctx)`: constructs `tele.NewBot(tele.Settings{Token})`, registers the text-message handler, launches the polling goroutine; returns once started (not per turn). Bot construction failure (bad token / getMe) returns an error the registry fail-softs.
    - `Stop(ctx)`: stops the bot (telebot Bot.Stop) and joins the polling goroutine — goleak-clean.
    - `IsHealthy()`: true when the bot is constructed and polling.
    - `handleTurn(ctx, convID, userMsg)`: `Translate(convID, runID, idgen, runner.Turn(ctx, convID, &userMsg))` → `NewFanout(translated)` → `statusCh := fo.Subscribe()`; `contentCh := fo.Subscribe()` (BOTH before Run) → `fo.Run(ctx)` → dispatch `statusCh` to status_pane.consume, `contentCh` to renderer.consume.
    - Unit tests use `tele.Settings{Offline:true}` (no network); fanout consumption is tested with a synthetic `*agent.Event` stream through the real Translate→Fanout path.
  </behavior>
  <action>Create `bot.go` with the `Deps`/`Telegram` types implementing `channels.Channel`. Start launches the telebot poller in a goroutine the channel tracks for Stop (WaitGroup or a done-chan join); mirror the serve.go fail-soft goroutine + graceful join. Create `agui_subscriber.go` with `handleTurn` doing the exact per-turn wiring from research §2: Translate → NewFanout → Subscribe(status) → Subscribe(content) → Run → consume. Subscribe BOTH consumers before Run (fanout.go:51 panics otherwise); one Fanout per turn (fanout.go:67 panics on double-Run). The text handler reads the incoming message, calls handleTurn. Use Offline:true bots in tests; drive a synthetic event stream to prove the fanout consumption maps to the two consumers. goleak (the package TestMain from 13-01) gates the polling-goroutine join.</action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/channels/telegram/ -run 'Bot|Fanout|Subscribe|Turn' 2>&1 | tail -15</automated>
  </verify>
  <done>Telegram implements Channel; Start polls + Stop joins goleak-clean (Offline bot in tests); handleTurn subscribes status+content BEFORE Run over a real Translate→Fanout drive of a synthetic event stream; no fanout re-implementation.</done>
  <acceptance_criteria>
    - `go test ./internal/channels/telegram/ -run Fanout` proves per-turn Subscribe×2-before-Run consumption of a synthetic event stream (VALIDATION UX-02 "Per-turn fanout consumption maps AG-UI events → status-pane-B render").
    - The polling goroutine is joined by Stop — `go test ./internal/channels/telegram/` stays goleak-clean (VALIDATION "bot polling goroutine goleak-clean").
    - `grep 'agui.NewFanout' internal/channels/telegram/agui_subscriber.go` matches AND `grep 'Subscribe' ...` shows two subscribes before `Run` (research Pitfall 1).
    - bot.go imports `internal/agui` (consumes the seam) and does NOT redeclare a fanout/distribution type.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: status_pane.go + renderer.go (status-pane-B render + mdv2 fallback + tables PNG)</name>
  <files>internal/channels/telegram/status_pane.go, internal/channels/telegram/renderer.go, internal/channels/telegram/renderer_test.go, internal/channels/telegram/status_pane_test.go, internal/channels/telegram/testdata</files>
  <read_first>
    - internal/agui/translator.go (the event set the consumers receive: RUN_STARTED, TOOL_CALL_START/ARGS/END/RESULT, REASONING_*, TEXT_MESSAGE_*, STATE_DELTA, RUN_FINISHED success/interrupt, RUN_ERROR — the switch arms map 1:1)
    - internal/channels/telegram/mdv2.go (the escaper from plan 13-03 — escape + the plain-text-fallback contract)
    - internal/channels/telegram/tables.go (the table→PNG transform from plan 13-03 — renderer detects tables and sendPhoto)
    - .claude/skills/spike-findings-Aura/references/telegram-channel.md (§MarkdownV2 discipline — all-or-nothing 400 + resend-without-ParseMode fallback; §Constraints — 4096 text cap, 1024 caption cap)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§renderer.go + §status_pane.go — the event-family switch + stateful throttle)
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§2 event→render map; throttles 1500/500/1000ms)
  </read_first>
  <behavior>
    - status_pane.consume(statusCh): maintains msg#1, edited in place — RUN_STARTED opens the pane; TOOL_CALL_START/ARGS/END/RESULT update a tool list (🟡 in-flight → ✅/❌ on result); REASONING_* appends a 💭 line; STATE_DELTA (usage/cost) updates a running-cost footer; throttled to AURA_TELEGRAM_STATUS_THROTTLE_MS (1500ms default) via a coalescing editor.
    - renderer.consume(contentCh): TEXT_MESSAGE_* streams into msg#2, mdv2-escaped, sent with ModeMarkdownV2; on a 400 `can't parse entities` it RESENDS the same text WITHOUT ParseMode (plain-text fallback) — SC#2 guarantee. A detected markdown table in the content is rendered via tables.go → sendPhoto (caption ≤1024). Content throttled to AURA_TELEGRAM_CONTENT_THROTTLE_MS (500ms); a per-chat_id queue rate-limits to AURA_TELEGRAM_CHAT_RATE_LIMIT_MS (1000ms). Text cap 4096 (split/truncate per the spike constraint).
    - RUN_FINISHED(success) finalizes; RUN_FINISHED(interrupt) hands off to HITL (plan 13-06); RUN_ERROR marks ❌.
    - A microcompact read_tool_output pointer in a tool result preview renders as a pointer (PRD acceptance — golden fixture).
    - Throttle timing is tested with `testing/synctest` (deterministic virtual clock).
    - A Telegram Send is asserted on the RESPONSE (msg.Text/msg.Entities/msg.Photo), never getUpdates (spike 017/019).
  </behavior>
  <action>Create `status_pane.go` (a stateful pane manager keyed on a chat/turn, editing msg#1 with a coalescing throttle) and `renderer.go` (the content consumer with the mdv2 escape + plain-text fallback + table-detect→sendPhoto + per-chat rate limit). Both consume the AG-UI `events.Event` set the translator produces — switch on the event type 1:1 with translator.go's emit. For the fallback: attempt ModeMarkdownV2; on a Bot-API 400 whose message contains `can't parse entities`, resend the unescaped text without ParseMode. Use the throttle durations from the Deps/config. Golden-fixture one render per event type into testdata (incl. the microcompact-pointer case). Test throttle coalescing with `testing/synctest`. In tests, inject a bot double / Offline bot and assert on the Send response payload (never getUpdates). Keep each file ≤600 LOC (split renderer into renderer.go + a small helper file if it grows).</action>
  <verify>
    <automated>cd /d/Aura && go test ./internal/channels/telegram/ -run 'Render|StatusPane|Fallback|Throttle' 2>&1 | tail -15</automated>
  </verify>
  <done>status_pane edits msg#1 with the tool-list/reasoning/cost render under the 1500ms throttle; renderer streams msg#2 escaped with a working plain-text fallback on a simulated 400, renders a table to a PNG sendPhoto, and rate-limits per chat; goldens cover every event type incl. the microcompact pointer; synctest proves the throttle timing.</done>
  <acceptance_criteria>
    - `go test ./internal/channels/telegram/ -run Render` passes the per-event golden incl. the microcompact-pointer fixture (VALIDATION UX-02 "Per-turn fanout consumption maps AG-UI events → status-pane-B render ... incl. microcompact pointer").
    - A test simulating a `400 can't parse entities` proves the content is resent WITHOUT ParseMode (SC#2 plain-text fallback) and asserts on the Send RESPONSE, not getUpdates.
    - A `testing/synctest` test proves the status throttle coalesces edits to the 1500ms window (VALIDATION "throttle via synctest").
    - A markdown-table content renders via sendPhoto (msg.Photo non-nil in the bot double) — table→PNG path wired.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM event stream → Telegram API | model-generated content + tool results cross into Bot-API sends (escape, size, MIME) |
| per-turn fanout producer → consumers | the single producer goroutine must close cleanly so the polling daemon stays leak-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-MdV2Send | Tampering / DoS | renderer.go MarkdownV2 send | mitigate | mdv2 entity-aware escape + plain-text fallback on a 400 (Task 2 acceptance) — SC#2 |
| T-13-05-PollLeak | DoS (resource) | bot.go polling goroutine | mitigate | Stop joins the goroutine; goleak TestMain gates it (Task 1) |
| T-13-05-FanoutMisuse | Integrity | agui_subscriber.go | mitigate | Subscribe×2-before-Run + one-Fanout-per-turn honored (fanout.go panic guards), proven by the synthetic-stream test |
| T-13-05-FloodSend | DoS | renderer per-chat sends | mitigate | per-chat_id rate limit (AURA_TELEGRAM_CHAT_RATE_LIMIT_MS) + content throttle |
</threat_model>

<verification>
- `go test ./internal/channels/telegram/ -run 'Bot|Fanout'` green (Channel impl + per-turn consumption).
- `go test ./internal/channels/telegram/ -run 'Render|StatusPane|Fallback|Throttle'` green (status-pane-B + fallback + synctest throttle).
- `go test ./internal/channels/telegram/` goleak-clean (polling join).
</verification>

<success_criteria>
- Telegram implements Channel with a goleak-clean polling lifecycle.
- Per-turn fanout consumption uses the Phase-12 seam (no re-implementation).
- Status-pane-B render maps every AG-UI event; content has a working mdv2 + plain-text fallback and tables→PNG; throttles enforced.
</success_criteria>

<output>
Create `.planning/phases/13-channels-telegram-multimodal/13-05-SUMMARY.md` when done.
</output>
