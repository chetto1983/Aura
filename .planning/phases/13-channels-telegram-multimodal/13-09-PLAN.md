---
phase: 13-channels-telegram-multimodal
plan: 09
type: execute
wave: 4
depends_on: [13-05, 13-06, 13-07, 13-08]
files_modified:
  - cmd/aura/serve.go
  - cmd/aura/serve_test.go
  - internal/channels/telegram/integration_test.go
  - internal/channels/telegram/multimodal_integration_test.go
  - .github/workflows/ci.yml
  - compose.yaml
  - docs/aura-quality-snapshot.md
autonomous: false
requirements: [UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "bootServe mounts the channels Registry (Telegram) + the setup :9081 server as fail-soft siblings of the AG-UI gateway; runServe StartAll/StopAll them around the scheduler lifecycle, StopAll before env.close()"
    - "On first boot the setup token prints to stdout; the operator can configure the bot token via /setup/token or TELEGRAM_BOT_TOKEN and onboard via the ASCII QR deep-link"
    - "The telegram_integration tier asserts on the Bot-API response (msg.Photo/Document/Voice non-nil), never getUpdates; the multimodal_integration tier round-trips STT/OCR/TTS; both t.Fatal under $CI when their env is set"
    - "The full tag matrix runs live, owned-surface coverage ≥85%, mutation ≥70% on the escaper + renderer, and the operator signs off the live Telegram turn / /cancel / voice / image"
  artifacts:
    - path: "cmd/aura/serve.go"
      provides: "channels Registry + setup server mounted in bootServe/runServe"
      contains: "channels"
    - path: "internal/channels/telegram/integration_test.go"
      provides: "telegram_integration tier (live bot, response-asserted)"
      contains: "telegram_integration"
    - path: ".github/workflows/ci.yml"
      provides: "telegram_integration + multimodal_integration CI tiers (no-skip-as-green)"
  key_links:
    - from: "cmd/aura/serve.go"
      to: "internal/channels.Registry + internal/setup.Server"
      via: "bootServe builds + runServe StartAll/StopAll, fail-soft"
      pattern: "StartAll|setup"
    - from: ".github/workflows/ci.yml"
      to: "telegram_integration / multimodal_integration"
      via: "tagged tiers t.Fatal under $CI when env set"
      pattern: "telegram_integration|multimodal_integration"
---

<objective>
Wire the phase together and prove it: mount the channels `Registry` (Telegram) + the setup `:9081` server into `bootServe`/`runServe` as fail-soft daemon siblings of the AG-UI gateway, add the two live integration tiers (`telegram_integration`, `multimodal_integration`) + the compose sidecar services, wire the CI no-skip-as-green tiers, run the full tag matrix + coverage + mutation, and take the Gate-3 operator sign-off on the live Telegram surface.

Purpose: This closes UX-02/03/04. The mount is the last seam (depends on the whole channel + setup + multimodal). The live tiers assert on the Bot-API response (spike 017/019 ground truth), and the operator verifies the manual-only behaviours (live turn render, /cancel, voice note, image). Owned-surface coverage ≥85% (CLAUDE.md floor) across the matrix.
Output: serve.go mount + test, the two integration_test tiers, compose services, CI wiring, the quality-snapshot rows, and the live human-verify checkpoint.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md
@.planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md
@.planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md
@cmd/aura/serve.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mount channels Registry + setup server in bootServe/runServe</name>
  <files>cmd/aura/serve.go, cmd/aura/serve_test.go</files>
  <read_first>
    - cmd/aura/serve.go (the WHOLE file — serveEnv struct line 50, bootServe lines 115-145 AG-UI mount, runServe lines 61-109 start/shutdown lifecycle; the fail-soft goroutine 84-88)
    - cmd/aura/chat.go (lines 48-58 — chatEnv exposes run/conv/pause/cfg/reg/pool the telegram.Deps + setup.Deps consume)
    - internal/channels/registry.go (StartAll/StopAll from plan 13-04)
    - internal/channels/telegram/bot.go (telegram.New(Deps) from plan 13-05)
    - internal/setup/server.go (setup.NewServer(Deps) from plan 13-07)
    - .planning/phases/13-channels-telegram-multimodal/13-PATTERNS.md (§cmd/aura/serve.go MODIFY — add a channels field + a setup http.Server sibling; StopAll before env.close())
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§1 / Pattern 2 — registry mount + --no-telegram/--only=cli flags)
  </read_first>
  <behavior>
    - `serveEnv` gains a `channels *channels.Registry` field and a `setupSrv *http.Server` (the :9081 sibling).
    - bootServe: after the AG-UI mount, build `telegram.New(telegram.Deps{Runner: chat.run, Conv: chat.conv, Pause: chat.pause, Token: chat.cfg.TelegramBotToken, Store: telegramStore, throttles...})`; register it in a new `channels.NewRegistry()`; build the setup server `setup.NewServer(setup.Deps{Store: telegramStore, BotProbe: <telebot getMe>, Bind: cfg.SetupBind, Token: cfg.SetupToken})`; build the telegramStore from `chat.pool`.
    - runServe: call `reg.StartAll(ctx)` alongside `scheduler.Start`, launch the setup server in a fail-soft goroutine (mirror the AG-UI 84-88 pattern), and on shutdown `reg.StopAll(ctx)` + `setupSrv.Shutdown` BEFORE `env.close()`. One failed subsystem never aborts the daemon.
    - `--no-telegram` / `--only=cli` flags override the env enable gate (PRD Punto 1).
    - The whole daemon stays goleak-clean (the existing serve shutdown path joins everything).
  </behavior>
  <action>Modify `serve.go`: add the `channels` + `setupSrv` fields to serveEnv; in bootServe build the telegramStore (telegram.New from chat.pool), the channels Registry with the registered Telegram channel, and the setup server (BotProbe = a telebot getMe closure); in runServe StartAll the registry + run the setup server fail-soft, StopAll + Shutdown both before env.close(). Add the --no-telegram/--only=cli flag parsing feeding the registry override. Keep serve.go ≤600 LOC — if it grows past, split the channel/setup wiring into a `serve_channels.go` sibling (refactor-on-touch). Test boot+shutdown with a fake registry/setup (no live bot): StartAll/StopAll called, fail-soft on a failing channel, goleak-clean.</action>
  <verify>
    <automated>cd /d/Aura && go build ./... && go test ./cmd/aura/ -run 'Serve|Boot|Channel|Setup' 2>&1 | tail -15</automated>
  </verify>
  <done>bootServe mounts the channels Registry + setup :9081 server; runServe StartAll/StopAll + Shutdown them fail-soft around the scheduler, StopAll before env.close(); --no-telegram/--only=cli honored; daemon goleak-clean.</done>
  <acceptance_criteria>
    - `go test ./cmd/aura/ -run Serve` proves StartAll/StopAll are invoked and a failing channel does NOT abort the daemon (fail-soft).
    - `grep 'channels' cmd/aura/serve.go` and `grep 'setup' cmd/aura/serve.go` both match (both subsystems mounted).
    - StopAll/Shutdown run BEFORE `env.close()` in runServe (ordering asserted by the test or code review).
    - `go build ./...` green; no file >600 LOC.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: live integration tiers + compose sidecars + CI no-skip-as-green wiring</name>
  <files>internal/channels/telegram/integration_test.go, internal/channels/telegram/multimodal_integration_test.go, .github/workflows/ci.yml, compose.yaml</files>
  <read_first>
    - .planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md (§Test tiers — telegram_integration + multimodal_integration: build tags, response-not-getUpdates ground truth, t.Fatal under $CI when env set; §Manual-Only Verifications)
    - .planning/phases/13-channels-telegram-multimodal/13-RESEARCH.md (§Multimodal sidecar wiring — the compose sidecar services + endpoints; §Environment Availability — the fallback/skip discipline)
    - .github/workflows/ci.yml (the existing db_integration / neo4j_integration job pattern — env export, CI=true, the no-skip-as-green precedent from Phase 8/11)
    - .claude/skills/spike-findings-Aura/references/telegram-channel.md (§Artifact + Transport — assert on the Send response, never getUpdates; the operator's own chat)
    - compose.yaml (the existing service shape to add the 3 sidecars + markitdown)
  </read_first>
  <action>Create `integration_test.go` (`//go:build telegram_integration`): live sends of a table-PNG (sendPhoto), an artifact (sendDocument), and a TTS voice (sendVoice) to the operator's own chat (TELEGRAM_BOT_TOKEN + AURA_E2E_CHAT_ID env), asserting on the Bot-API RESPONSE (msg.Photo / msg.Document.{FileName,MIME,FileSize} / msg.Voice non-nil) — NEVER getUpdates. Create `multimodal_integration_test.go` (`//go:build multimodal_integration`): a voice-note IT round-trip (STT transcript non-empty), a photo OCR recall, a TTS opus round-trip — requires the 3 sidecars up. Both tiers `t.Fatal` under `$CI` when their env is SET but unusable (no-skip-as-green); they skip locally when env is unset. Add the 3 CPU sidecars (`aura-ocr-vl`, `aura-stt`, `aura-tts`) + markitdown to `compose.yaml` (config only — endpoints/ports per research §7; with AURA_VISION_CLOUD=true the operator simply doesn't start aura-ocr-vl). Wire `ci.yml`: add the unit tier package to the default job; add a sidecar-gated `multimodal_integration` job and an operator-token-gated optional `telegram_integration` step, both exporting the exact env the tests read + CI=true.</action>
  <verify>
    <automated>cd /d/Aura && go vet -tags 'telegram_integration multimodal_integration' ./internal/channels/telegram/ && go test ./internal/channels/... ./internal/setup/... 2>&1 | tail -15</automated>
  </verify>
  <done>Both tagged tiers compile and follow the response-not-getUpdates + t.Fatal-under-$CI discipline; compose has the 3 sidecars + markitdown; ci.yml runs the unit tier in the default job and adds the two gated integration jobs (no-skip-as-green).</done>
  <acceptance_criteria>
    - `go vet -tags 'telegram_integration multimodal_integration' ./internal/channels/telegram/` compiles both tiers.
    - The telegram_integration assertions read `msg.Photo`/`msg.Document.FileName`/`msg.Voice` — `grep -c getUpdates internal/channels/telegram/integration_test.go` is 0 (ground truth = response, spike 017/019).
    - Both tiers contain a `t.Fatal`-under-`$CI`-when-env-set guard (no-skip-as-green — VALIDATION).
    - `compose.yaml` has services `aura-ocr-vl`, `aura-stt`, `aura-tts`, and a markitdown service.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate-3 — full matrix + coverage + mutation + live operator sign-off</name>
  <read_first>
    - .planning/phases/13-channels-telegram-multimodal/13-VALIDATION.md (§Manual-Only Verifications — the 5 live behaviours; §Sampling Rate phase gate)
    - CLAUDE.md (the Gate-3 DoD: coverage ≥85% owned-surface across the full tag matrix, mutation ≥70%, goleak, no-skip-as-green)
    - docs/aura-quality-snapshot.md (the living-doc schema to append the Phase-13 rows)
  </read_first>
  <action>Before pausing for the operator, the implementer runs the FULL tag matrix live (`go test -race -tags 'db_integration telegram_integration multimodal_integration' ./...` with PG + the 3 sidecars + the operator bot token up), `make coverage` (owned-surface ≥85%), and a go-mutesting spot-check ≥70% on mdv2.go + renderer.go (WSL go-mutesting, PASS=killed), then appends the full-matrix/coverage/mutation rows to `docs/aura-quality-snapshot.md`. THEN pause at this blocking checkpoint for the operator to confirm the manual-only live behaviours (setup token + 401/200, live render no-400, /cancel, voice + image, onboarding) per how-to-verify. Do NOT mark UX-02/03/04 complete until the operator signs off.</action>
  <what-built>
    The full Phase-13 Telegram channel + setup API + multimodal stack is wired into `aura serve`. Before this checkpoint the implementer runs the FULL tag matrix live, `make coverage` (owned-surface ≥85%), and a mutation spot-check ≥70% on the escaper (mdv2.go) + renderer.go critical files, then appends the numbers to `docs/aura-quality-snapshot.md`. This checkpoint is the operator's live confirmation of the manual-only behaviours that a test harness cannot judge.
  </what-built>
  <how-to-verify>
    1. Bring up the 9c sidecars: `aura-ocr-vl` + `aura-stt` + `aura-tts` + markitdown (or set `AURA_VISION_CLOUD=true` to skip aura-ocr-vl), Postgres, and run `aura serve` with `TELEGRAM_BOT_TOKEN` set.
    2. Observe the stdout `AURA_SETUP_TOKEN=<value>` line on first boot; `curl -s 'http://127.0.0.1:9081/setup/status?token=<value>'` returns 200; the same call without the token returns 401.
    3. In Telegram, send a message → observe the streamed reply with the status pane (tool list / reasoning / cost) + content render correctly, NO `400 can't parse entities`.
    4. Send `/cost` (today's cumulative USD) and `/search "<phrase>"` (matching excerpts == the CLI). Send `/cancel` during a long tool call → observe immediate abort + Aura confirmation.
    5. Send a voice note → observe transcription (+ optional voice reply); send an image with caption "cosa c'è in questa foto?" → observe a description (local GLM-OCR or, with AURA_VISION_CLOUD=true, OpenRouter).
    6. Scan the ASCII QR / click `t.me/<bot>?start=<token>` → observe onboarding completion (a telegram_accounts row + the SSE onboarding_completed event).
    7. Confirm the snapshot rows: full-matrix PASS, owned-surface coverage ≥85%, mdv2/renderer mutation ≥70%.
  </how-to-verify>
  <resume-signal>Type "approved" once the live Telegram turn, /cancel, voice, image, and onboarding all behave and the snapshot numbers are recorded — or describe the failure.</resume-signal>
  <acceptance_criteria>
    - Full tag matrix runs live (db + telegram + multimodal) with the no-skip-as-green guards firing — not a sub-second skip.
    - Owned-surface coverage ≥85% recorded in docs/aura-quality-snapshot.md (CLAUDE.md floor).
    - Mutation spot-check ≥70% on mdv2.go + renderer.go documented in the snapshot.
    - Operator confirms SC#1 (setup token + 401/200), SC#2 (live render no 400), SC#3 (/cancel), SC#4 (voice + image), SC#5 (/cost + /search) all live.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon composition root | bootServe mounts three loopback subsystems (AG-UI :9080, setup :9081, Telegram polling) — each must fail-soft |
| CI env → integration tiers | the tagged tiers must actually run (env-exported) under CI, never skip-as-green |
| live bot + sidecars | the Gate-3 live surface exercises the real Bot-API + the 3 CPU sidecars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09-DaemonAbort | DoS | serve.go subsystem mount | mitigate | each subsystem fail-soft (errors.Join / log-but-never-exit); a failed channel/setup never crashes the daemon (Task 1) |
| T-13-09-SkipAsGreen | Auditing gap | CI integration tiers | mitigate | both tiers t.Fatal under $CI when env set; CI exports the exact env the tests read (Task 2) — Phase 8/11 precedent |
| T-13-09-SC | Tampering | compose sidecar images | accept | the 3 sidecars are operator-started compose services (pull/run is the operator's posture, mirrors sandbox-up); no Aura-side provision at boot |
| T-13-09-CoverageFloor | Auditing | owned-surface coverage | mitigate | Gate-3 records ≥85% across the full matrix + mutation ≥70% on the critical files (Task 3) |
</threat_model>

<verification>
- `go build ./...` + `go test ./cmd/aura/ -run Serve` green (mount + fail-soft).
- `go vet -tags 'telegram_integration multimodal_integration' ./internal/channels/telegram/` compiles both tiers.
- Full tag matrix live + `make coverage` ≥85% + mutation ≥70% (Gate-3).
- Operator sign-off on the 5 live success criteria.
</verification>

<success_criteria>
- The channels Registry + setup :9081 server mount fail-soft in aura serve; StopAll/Shutdown before env.close().
- The two live integration tiers compile + assert on the Bot-API response with no-skip-as-green CI wiring; compose has the 4 sidecars.
- Full matrix live, coverage ≥85% owned-surface, mutation ≥70%, and the operator confirms all 5 ROADMAP success criteria.
</success_criteria>

<output>
Create `.planning/phases/13-channels-telegram-multimodal/13-09-SUMMARY.md` when done.
</output>
