---
phase: 45.1-native-mcp-client
plan: 08
type: execute
wave: 6
depends_on: ["45.1-07"]
files_modified:
  - .planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/aura-quality-snapshot.md
  - prd.md
autonomous: false
requirements: [MCPC-01, MCPC-02, MCPC-03, MCPC-04, MCPC-05]

must_haves:
  truths:
    - "A live conversation drives calendar, WhatsApp and memory tools through the SDK client, and aura.tool_invocations shows the same tool surface and results as before the swap."
    - "internal/mcp/client.go, http_client.go, bridge_reconnect.go and bridge_ping.go are gone from the tree, and no hand-rolled JSON-RPC framing or reconnect loop replaces them elsewhere."
    - "Killing a sidecar mid-conversation and restarting it recovers on the SDK's own mechanism — for a 2026-07-28 peer, because the next call is a fresh POST, not because a reconnect loop ran."
    - "An SSRF/egress probe against a mounted HTTP server is still refused after the swap."
    - "A live turn shows the calling identity reaching cmd/arcadedb-mcp through _meta and no longer as a tool argument."
    - "The negotiated protocol version of all three mounted sidecars is measured and recorded, not assumed."
    - "The full gate matrix is green in WSL or CI: vet, build, race, tagged tiers, coverage >=85% owned surface, mutation >=70% killed on the phase's critical files, govulncheck."
    - "docs/aura-quality-snapshot.md carries a re-attestation for every row whose CI-gate-path glob matches a file this phase changed."
  artifacts:
    - path: ".planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md"
      provides: "the phase's measured evidence — protocol versions, coverage number, mutation scores, SC#1-5 outcomes"
      contains: "nyquist_compliant"
    - path: "docs/aura-quality-snapshot.md"
      provides: "the per-row re-attestation the phase-close push gate requires"
  key_links:
    - from: ".planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md"
      to: "the three sidecars' negotiated ProtocolVersion"
      via: "the permanent 'mcp session open' log line"
      pattern: "protocol_version"
  prohibitions:
    - "A green unit suite must not be mistaken for a closed phase: the Definition of Done is a real end-to-end scenario driven through the running stack and scored above 9.8, and no amount of passing tests substitutes for it."
    - "A quality-snapshot row must not be re-dated without a measurement or a metric-neutral justification naming exactly what changed and why the number cannot move."
    - "A falsified premise must not be left standing in the requirements: every correction this phase measured has to land as an amendment with its citation, or the next phase inherits the same wrong fact."
---

<objective>
Close the phase on measured evidence: the live scenario, the full gate matrix, the protocol
versions nobody has actually read yet, and the amendments the phase's own measurements forced.

Purpose: CLAUDE.md's Definition of Done is explicit — *"una suite unitaria verde non chiude
niente"*. The five ROADMAP Success Criteria are the scenario, and this plan is where they are
driven for real.
Output: `45.1-VALIDATION.md` with measured evidence, a re-attested quality snapshot, and
amendments to REQUIREMENTS.md / ROADMAP.md / prd.md for the four premises this phase falsified.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md
@.planning/phases/45.1-native-mcp-client/COVERAGE.md
@.planning/phases/45.1-native-mcp-client/45.1-01-SUMMARY.md
@.planning/phases/45.1-native-mcp-client/45.1-02-SUMMARY.md
@.planning/phases/45.1-native-mcp-client/45.1-03-SUMMARY.md
@.planning/phases/45.1-native-mcp-client/45.1-04-SUMMARY.md
@.planning/phases/45.1-native-mcp-client/45.1-05-SUMMARY.md
@.planning/phases/45.1-native-mcp-client/45.1-06-SUMMARY.md
@.planning/phases/45.1-native-mcp-client/45.1-07-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate matrix, measured in WSL against the live stack</name>
  <files>.planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md</files>
  <read_first>
    - CLAUDE.md § "Quality tooling & gates (industrial)" (the WSL PATH prerequisite, `make quality` vs `make quality-full`, and which gate runs where)
    - CLAUDE.md § "COVERAGE FLOOR 85%" and § "COVERAGE GATE TAG SET = db_integration ONLY" (which tiers feed the gate and which run-but-feed-nothing)
    - CLAUDE.md § "NO SKIP-AS-GREEN IN CI"
    - scripts/coverage_docker.sh and scripts/coverage_gate.sh (whole files)
    - Makefile lines 80-180 (`quality`, `quality-full`, `coverage`, `vuln`, `agent-memory-eval`)
    - .planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md (the Per-Task Verification Map and Manual-Only table to fill)
    - .github/workflows/ci.yml (the tagged jobs, so a locally-green matrix matches what CI will run)
  </read_first>
  <action>
Run the whole matrix in WSL with `export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"` and the
composed DSNs exported (`AURA_DB_URL`, `AURA_DB_MIGRATE_URL` — the composed forms the tests read,
not the `POSTGRES_*` primitives `config.Load` composes for the CLI). Bring the stack up first
(`make db-migrate memory-up`, plus `--profile sandbox` if the deploy needs it).

1. `go vet ./...` and `go build ./...`.
2. `go test -race ./... -count=1` — `goleak` green, no data race.
3. Tagged compile + run for every tier this phase touched:
   `go vet -tags='db_integration arcadedb_integration docker_integration calendar_integration whatsapp_integration calculator_integration' ./...`,
   then run `db_integration` and `arcadedb_integration` live. Verify a sub-second "integration"
   runtime is not a skip tell — check the tests actually executed, not just that the job says PASS.
4. `make quality` (vet + file-size + lint + dupl + deadcode + test-race + vuln + build).
5. `bash scripts/coverage_docker.sh` — the owned-surface floor. Record the exact percentage.
6. Mutation spot-check under WSL on this phase's critical files, ≥70% killed each:
   `go-mutesting internal/agent/mcptools/bridge_supervisor.go`,
   `go-mutesting internal/agent/mcptools/bridge_risk.go`,
   `go-mutesting cmd/arcadedb-mcp/identity.go`. `PASS` = killed, `FAIL` = survived; score is
   killed/total. `bridge_supervisor.go` and `identity.go` are the right targets because they carry
   the no-replay, drift and cross-tenant invariants — the guarantees a surviving mutant would prove
   untested.
7. `govulncheck ./...`.

Fill `45.1-VALIDATION.md`'s Per-Task Verification Map from the eight plans' task IDs, and its
Manual-Only table with the measured mutation scores. Set `wave_0_complete: true` and
`nyquist_compliant: true` in the frontmatter only if every row is green; a red or unrun row leaves
both false and the phase does not seal.

Do **not** bypass `coverage_docker.sh`'s guards: it provisions a disposable `aura_cov` owned by
`aura_migrate`, drops it on exit, and refuses `db_integration` against a database named `aura` when
`GITHUB_ACTIONS` is unset. That refusal exists because a 2026-07-10 run truncated the live
deployment's auth tables.
  </action>
  <verify>
    <automated>make quality-full && bash scripts/coverage_docker.sh && govulncheck ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./...`, `go build ./...`, `go test -race ./... -count=1` all exit 0.
    - `go vet -tags='db_integration arcadedb_integration docker_integration calendar_integration whatsapp_integration calculator_integration' ./...` exits 0.
    - `make quality` exits 0 including `golangci-lint`, `dupl` and `deadcode`.
    - `bash scripts/coverage_docker.sh` prints a pass and the recorded percentage is ≥85%.
    - `go-mutesting` reports ≥70% killed for each of `bridge_supervisor.go`, `bridge_risk.go`, `identity.go`, each recorded in `45.1-VALIDATION.md`.
    - `govulncheck ./...` reports no vulnerabilities.
    - `45.1-VALIDATION.md`'s Per-Task Verification Map has no `⬜ pending` row.
    - `psql -l` shows no leftover `aura_cov`, and the live `aura` database row count is unchanged.
    - No non-test Go file in the repository exceeds 600 LOC (`make quality`'s file-size check).
  </acceptance_criteria>
  <done>The full gate matrix is green on measured runs, with coverage, mutation and vulnerability numbers written into VALIDATION.md rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the five Success Criteria live, and read the protocol versions</name>
  <files>.planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md</files>
  <read_first>
    - .planning/ROADMAP.md § "Phase 45.1" → "Success Criteria" (the five, verbatim — this task's script)
    - .planning/phases/45.1-native-mcp-client/45.1-RESEARCH.md § "Correction 3" (**what recovery actually looks like** for a 2026-07-28 peer: no reconnect log line, just the next call succeeding)
    - .planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md § "Manual-Only Verifications" (the five rows this task fills)
    - compose.yaml lines 800-1000 (`whatsapp-mcp` and `aura-pim-mcp` sidecar definitions — the containers to kill and restart)
    - internal/mcp/manager/catalog.go lines 120-175 (the three built-in recipes, all streamable-HTTP over loopback)
    - CLAUDE.md § "DEFINITION OF DONE" and the memory note `e2e-real-not-smoke` (a live smoke is forbidden as a green signal)
    - CLAUDE.md § "Check container image freshness first" — **before driving anything, confirm `aura:local` was built from current HEAD.** An E2E against a stale image proves nothing and has cost two corrections already.
  </read_first>
  <action>
Rebuild the container from current HEAD first. Then drive a **real** scenario through the running
stack as the actual agent — not curl with hand-written headers, not a smoke test. Mine the agent's
own memory graph and the cockpit as an operator would.

**SC#1 — tool surface parity.** Capture the pre-swap tool surface and a sample of
`aura.tool_invocations` rows from before this phase (git history plus the DB). Drive one
conversation that exercises calendar, WhatsApp and memory in the same turn sequence. Diff the tool
names, argument shapes and results against the pre-swap capture. Record the diff — an empty diff is
the evidence, "it looked the same" is not.

**SC#2 — the files are gone and nothing replaced them.** Record the output of
`ls internal/mcp/ internal/agent/mcptools/` and of `go test ./internal/mcp/ -run TestNoHandRolledJSONRPCFraming -v`.

**SC#3 — kill and restart.** `docker kill` a sidecar mid-conversation, restart it, then issue a
**new** tool call and confirm it succeeds. **Do not expect a "reconnecting" log line for a
2026-07-28-negotiated server — there will not be one, by design.** Under that protocol the
standalone SSE stream is removed (`streamable.go:2095-2098`) and every call is a fresh HTTP POST
(`streamable.go:2244-2269`), so recovery is per-call. Write the narrative as "the next call simply
succeeded", and record which mechanism actually fired per sidecar. If a sidecar negotiated **below**
2026-07-28 its behaviour may differ and still be correct — record the difference rather than
forcing one story onto all three. Also kill an **ad hoc stdio** server mid-run and confirm the
accepted regression from D-105: it does not self-heal, and the call returns a clear transport error
naming the server.

**SC#4 — SSRF/egress refusal.** Mount an HTTP MCP server, probe a disallowed host, and confirm the
refusal fires at the `http.RoundTripper` layer with the policy enforced. Record the error text.

**SC#5 — identity through `_meta`.** Drive one memory write and one memory read, and capture the
wire evidence that `user_identifier` arrives in `_meta.aura` and appears in **no** tool argument.
Then drive the negative: issue a memory call with the `_meta` identity stripped and confirm the
server refuses it with a model-visible tool error and resolves no tenant.

**The protocol-version reading.** Capture the `"mcp session open"` log line for all three mounted
sidecars and record `protocol_version` and `session_id` per server in VALIDATION.md's Manual-Only
table. This closes RESEARCH Assumption A1 and Open Question #4 with a measurement instead of the
`[ASSUMED]` it has carried since 2026-08-16. If any sidecar negotiates below 2026-07-28, record it
and note in COVERAGE.md that `KeepAlive` would be live for that peer — still OPT-OUT, but for a
narrower reason than "inert everywhere".

**If `45.1-06` chose `mint-and-wait`**, additionally drive threat T-45.1-32: an elicitation
minted on a conversation whose Turn is still blocked, resolved by the operator, and confirm the
resume Turn and the blocked Turn do not corrupt the conversation's message sequence.

Score the whole scenario. The phase does not close below **9.8**.
  </action>
  <verify>
    <human-check>
Bring the stack up from a container built at current HEAD, then:
1. Confirm image freshness: `docker inspect aura:local --format '{{.Created}}'` and `git log -1 --format=%cI` — the image must be newer than HEAD's commit.
2. Drive one conversation touching calendar, WhatsApp and memory. Compare the tool names and results against the pre-swap capture. Expected: identical surface, identical results.
3. `ls internal/mcp/ internal/agent/mcptools/` — expected: no `client.go`, `http_client.go`, `bridge_reconnect.go`, `bridge_ping.go`.
4. `docker kill aura-whatsapp-mcp` mid-conversation, `docker start` it, then ask Aura something that needs WhatsApp. Expected: the next call simply succeeds, with **no** reconnect log line for a 2026-07-28 peer.
5. Mount an HTTP MCP server pointing at a disallowed host and run `aura mcp doctor`. Expected: refused, with the policy named in the error.
6. Drive one memory write. Expected: `_meta.aura.user_identifier` on the wire; no `user_identifier` argument anywhere; the fact lands in the calling identity's database and nowhere else.
7. Read the three `"mcp session open"` log lines and record each `protocol_version`.
Score the run out of 10. Report the score and any deviation.
    </human-check>
  </verify>
  <acceptance_criteria>
    - All five ROADMAP Success Criteria are recorded as met in `45.1-VALIDATION.md`, each with its evidence (a diff, a log line, an error text, a wire capture), not a checkmark.
    - The negotiated `protocol_version` for all three sidecars is recorded — no `[ASSUMED]` remains in the Manual-Only table.
    - SC#3's narrative describes the mechanism that actually fired per sidecar, and explicitly does not claim a reconnect loop ran for a 2026-07-28 peer.
    - The D-105 accepted regression (ad hoc stdio server does not self-heal mid-run) is demonstrated and recorded as expected behaviour.
    - The negative identity case is demonstrated: a stripped `_meta` is refused and no tenant is resolved.
    - The live scenario is scored and the score is above 9.8.
    - The container driven was built from current HEAD.
  </acceptance_criteria>
  <done>All five Success Criteria are proven on the running stack by a real scenario scored above 9.8, and the fleet's protocol versions are measured rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Land the amendments this phase's measurements forced, and re-attest the quality snapshot</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, prd.md, docs/aura-quality-snapshot.md, .planning/phases/45.1-native-mcp-client/COVERAGE.md</files>
  <read_first>
    - CLAUDE.md § "QUALITY SNAPSHOT AT PHASE CLOSE" (the exact re-attestation rule and the local pre-check command)
    - scripts/quality_snapshot_gate.sh (whole file — what it matches and what makes it pass)
    - docs/aura-quality-snapshot.md (every row, and each row's CI-gate-path glob)
    - .planning/REQUIREMENTS.md lines 125-136 (MCPC-01..05 and their existing amendment blocks)
    - .planning/ROADMAP.md lines 167-251 (the Phase 45.1 section, its amendment blocks, and its `**Plans**: TBD` line)
    - prd.md § "Caps & Limits → Indice completo env vars" (where `AURA_MCP_ELICITATION_TIMEOUT_SEC` is added and `AURA_MCP_PING_INTERVAL_SEC` is removed)
    - .planning/phases/45.1-native-mcp-client/45.1-05-SUMMARY.md (the `<falsified_premise>` correction with its SDK citations)
    - .planning/phases/45.1-native-mcp-client/45.1-03-SUMMARY.md (the measured deleted-LOC totals)
  </read_first>
  <action>
**Amendments, each citing its measurement.** CLAUDE.md's PRD-first principle is that the PRD
records what was measured; when a measure contradicts it, the measure wins and the document is
corrected with date and evidence.

1. **REQUIREMENTS.md MCPC-04.** Its 2026-08-16 extension states that returning a Go `error` from a
   `cmd/arcadedb-mcp` handler "would surface as a protocol error". Measured false against
   `go-sdk@v1.7.0`: `server.go:385-392` returns a protocol error **only** for a `*jsonrpc.Error`,
   and otherwise calls `CallToolResult.SetError` (`protocol.go:347`), producing exactly the
   `IsError: true` + `TextContent` the requirement asks for. Amend with the citation and note that
   the *literal* instruction — returning a non-nil `*CallToolResult` with a zero `Out` — falls
   through to output-schema validation (`server.go:405-424`) and can produce the protocol error it
   was trying to avoid. The requirement's intent is unchanged and was delivered.
2. **ROADMAP.md and REQUIREMENTS.md MCPC-03: the test-LOC figure.** Both say "≈900 LOC of tests".
   Measured at HEAD: **≈3,750** (2,503 in `internal/mcp`, 1,244 in `internal/agent/mcptools`).
   Correct both, and note that the 85% floor was therefore under four times the assumed pressure.
3. **ROADMAP.md Phase 45.1 `**Plans**: TBD`.** Replace with the eight plans and their one-line
   objectives, and tick each. (The plan list itself was written at planning time; this step ticks
   it and records the wave structure that actually ran.)
4. **prd.md env catalogue.** Add `AURA_MCP_ELICITATION_TIMEOUT_SEC` with its default and its
   `<= 0 disables` semantics. **Remove `AURA_MCP_PING_INTERVAL_SEC`** — it died with
   `bridge_ping.go`. A catalogue that lists a knob nobody reads is the same class of error the
   `AURA_DOCUMENT_CHUNK_MAX_TOKENS` incident recorded in CLAUDE.md.
5. **COVERAGE.md.** Update the `KeepAlive` and `Ping` rows if Task 2's measurement found any sidecar
   negotiating below 2026-07-28 — the OPT-OUT stands either way, but its reason narrows from "inert
   against every peer" to "inert against the peers we run".
6. **prd.md, D-106.** Record the deletion of the stdio launch-command validator with its rationale
   **and** the counter-argument that was put and declined, so a later reader does not mistake the
   removal for an oversight. Then flag the phase for `/gsd-secure-phase 45.1` so it enters the
   security record.

**Quality snapshot re-attestation.** For **every** row in `docs/aura-quality-snapshot.md` whose
CI-gate-path glob matches a file this phase changed — expect matches on `internal/mcp/**`,
`internal/agent/mcptools/**`, `cmd/arcadedb-mcp/**`, `cmd/aura/**`, `internal/gateway/**`,
`internal/obs/**` — bump `Last measured` to today and **prepend** a re-attestation note: a fresh
measurement if the metric moved, otherwise a metric-neutral justification naming exactly what
changed and why the number cannot move. Keep the prior notes as `Prior …`. Verify locally FIRST;
the CI job fails the push otherwise (PRD amendment #20):

`AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh`

It must print `ok: … checked N row(s)`.

Finally set `45.1-VALIDATION.md`'s frontmatter `status: validated` and `nyquist_compliant: true`,
and update `.planning/STATE.md`'s current position, decisions and metrics for the phase close.
  </action>
  <verify>
    <automated>AURA_QUALITY_CHANGED_FILES="$(git diff --name-only origin/master..HEAD)" AURA_QUALITY_BASE_DATE="$(git log -1 --format=%cs origin/master)" bash scripts/quality_snapshot_gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/quality_snapshot_gate.sh` prints `ok: … checked N row(s)` with N > 0.
    - `grep -n 'AURA_MCP_ELICITATION_TIMEOUT_SEC' prd.md` matches and `! grep -n 'AURA_MCP_PING_INTERVAL_SEC' prd.md`.
    - REQUIREMENTS.md MCPC-04 carries the `server.go:385-392` / `protocol.go:347` citation for the Go-error correction.
    - REQUIREMENTS.md MCPC-03 and ROADMAP.md both carry the corrected ≈3,750 test-LOC figure.
    - ROADMAP.md Phase 45.1 lists all eight plans, each ticked, with the wave structure.
    - `45.1-VALIDATION.md` frontmatter has `status: validated` and `nyquist_compliant: true`.
    - `.planning/STATE.md` records the phase close, the measured coverage number, and the live-scenario score.
    - COVERAGE.md's `KeepAlive` row reflects the measured fleet.
    - `git push` and every CI job green.
  </acceptance_criteria>
  <done>Every premise this phase falsified is corrected in the documents that carry it, with date and citation; the quality snapshot passes its gate; and the phase's evidence is recorded rather than remembered.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**Master inventory for the whole phase.** The plan-review-convergence source-grounding pass reads
this section to exclude newly-created symbols from drift verification.

**`internal/mcp`**
`SessionOptions` (fields `Logger`, `Sending`, `ToolListChanged`, `Elicitation`), `SDKClientOptions`,
`newSDKClient`, `OpenSDKSession`, `OpenSDKSessionForConfig`, `logNegotiatedProtocol`,
`mcpClientName`, `mcpClientVersion`, `TransportErrorf`, `DecodeToolResult`, `DecodeToolCallError`,
`MetaNamespaceAura`, `MetaFieldOperationKey`, `MetaFieldOperationScope`,
`MetaFieldOperationFingerprint`, `MetaFieldUserIdentifier`, `auraMeta`, `SetAuraMetaField`,
`OperationMetaMiddleware`, `sdkStdioConnectBoundary`, `sdkHTTPConnectBoundary`,
`sdkStdioListBoundary`, `sdkHTTPListBoundary`, `sdkStdioCallBoundary`, `sdkHTTPCallBoundary`.
New files: `sdkclient.go`, `errors.go`, `server_config.go`, `result.go`, `middleware.go`,
`darkcode_test.go`, `sdkclient_test.go`, `result_test.go`, `middleware_test.go`.
Relocated (not new behaviour): `ErrTransport`, `IsTransportError`, `ServerConfig`,
`withMCPRedirectGuard`, `httpAuthFromEnv`, `ssrfEnforceFromEnv`, `processEnvForMCP`.

**`internal/agent/mcptools`**
`MountedServer`, `NewMountedServer`, `openSessionFunc`, `Attach`, `watch`, `currentSession`,
`ListTools`, `CallToolText`, `Close`, `onToolListChanged`, `trackBridgedTools`,
`trackAcceptedTools`, `validateToolSetLocked`, `refreshSpecsLocked`, `setRefreshHook`,
`toolIsReadOnly`, `isSessionTransportFailure`, `redialBackoff`, `sleepContext`,
`defaultMCPRedialTimeout`, `mcpRedialBreakerAfter`, `mcpRedialCooldown`, `mcpRedialBackoffMin`,
`mcpRedialBackoffMax`, `schemaJSON`, `annotationIdempotent`, `annotationOpenWorld`,
`unsafeToRepeatBeyondAura`, `IdentityMetaMiddleware`, `ElicitationRequest`, `ElicitationField`,
`ElicitationConsent`, `NewElicitationHandler`, `configuredElicitationTimeout`,
`summariseElicitationSchema`, `envMCPElicitationTimeoutSec`, `defaultMCPElicitationTimeoutSec`,
`MountOptions`, `MountManagedServerWithOptions`, `newInMemoryMounted`, `mustTool`.
New files: `bridge_call.go`, `bridge_supervisor.go`, `elicitation.go`, `helpers_test.go`,
`bridge_supervisor_test.go`, `bridge_call_test.go`, `elicitation_test.go`.

**`cmd/arcadedb-mcp`**
`auraMetaKey`, `metaFieldUserIdentifier`, `errMissingIdentityMeta`, `identityFromMeta`.
New files: `identity.go`, `identity_test.go`.

**`cmd/aura`**
`openManagedMCPSession`, `callSessionText`, `renderElicitationPrompt`, and the chosen
`ElicitationConsent` implementation. New files: `elicitation_consent.go`,
`elicitation_consent_test.go`.

**`internal/gateway`**
`PendingCountForTool`, `gatewayApprovalKeyPrefix`.

**`internal/obs`**
`MCPElicitationsID` (`aura.mcp.elicitation` / `aura_mcp_elicitation_total`) and its finite-set
action attribute.

**New `_meta` keys:** `_meta.aura.user_identifier` (plus the pre-existing
`_meta.aura.operation_key` / `operation_scope` / `operation_fingerprint`, preserved).

**New env vars:** `AURA_MCP_ELICITATION_TIMEOUT_SEC`.
**Removed env vars:** `AURA_MCP_PING_INTERVAL_SEC`.

**New log records:** `"mcp session open"` (INFO: `server`, `transport`, `protocol_version`,
`session_id`), `"mcp session closed"` (WARN: `server`, `error`), `"mcp tool annotations"` (DEBUG),
`"gateway approval ledger miss"` (INFO: `conversation_id`, `tool`, `args_fingerprint`,
`pending_count`, `reason`).

**New CLI flags:** none.
</artifacts_this_phase_produces>

<flagged_assumptions_closure>
The three `unclassified` edge-probe rows carried since planning are closed or re-stated here:

- **MCPC-01** — closed by Task 2's protocol-version reading. Until that log is read, RESEARCH
  Assumption A1 (`aura-pim-mcp` and `whatsapp-mcp` are Python/FastMCP and may negotiate below
  2026-07-28) rests on one code comment (`tool_error.go:96-98` naming "FastMCP"/"Pydantic") and two
  GHCR image builds — **LOW confidence, never verified by reading either repository**. Task 2
  replaces it with a measurement.
- **MCPC-02** — partially closed. `newHardenedHTTPClient` is proven honoured for the POST path by
  reading `streamable.go`'s `Write` and by SC#4's live probe. It is **not** verified for the
  standalone SSE GET path, because that path only exists for a peer negotiating below 2026-07-28
  and no such peer is confirmed to exist. If Task 2 finds one, add a live SSE-path egress probe;
  otherwise record the assumption as bounded by the measured fleet.
- **MCPC-03** — remains a bounded assumption by construction. `TestNoHandRolledJSONRPCFraming`
  covers eleven deleted-symbol tokens; a future framing helper under an unpredicted name would pass
  it. The durable guarantee is the prohibition in every plan's `must_haves`, enforced at review.
  Stated, not eliminated.
</flagged_assumptions_closure>

<reversibility>
| Decision | Rating | Note |
|---|---|---|
| Seal the phase on the live scenario | reversible | A failing SC re-opens the phase via `/gsd-plan-phase 45.1 --gaps`. |
| Land the four amendments | one-way in practice | They correct documents other phases read. Each carries its date and citation so a later measurement can correct them in turn — which is the point of the PRD-first principle, not an exception to it. |
</reversibility>

<threat_model>
ASVS level: **1**. Block on: **high**.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local coverage gate → live database | The gate provisions and drops databases on the developer's own stack. |
| Phase-close push → CI and `master` | The push is what makes the deletion and the schema change real for everyone. |
| Live E2E → the operator's real memory graph | The scenario writes real facts under a real identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45.1-35 | Denial of service (self-inflicted) | `scripts/coverage_docker.sh` | mitigate | Disposable `aura_cov`, dropped on exit, plus a refusal to run `db_integration` against a database named `aura` locally. Both guards are load-bearing after the 2026-07-10 incident that wiped the live deployment's operator identity with no backup. Never bypassed. |
| T-45.1-36 | Tampering | live E2E writing to the operator's real memory | accept | The scenario is driven under the operator's own identity by design — that is what makes it evidence rather than a smoke test. Writes are facts the operator can inspect and forget; the identity-isolation guarantee is itself one of the things under test (SC#5). |
| T-45.1-37 | Elevation of privilege | sealing the phase with plan 45.1-05 unlanded | mitigate | Threat T-45.1-15's identity-guard window (opened when `HTTPConfig.ToolIdentityArgument` died in plan 45.1-03) is closed only by plan 45.1-05. `depends_on` chains 08 → 07 → 05, so the phase cannot seal without it. SC#5's negative case is the proof. |
| T-45.1-06 | Tampering (command injection) | deleted stdio launch-command validator | accept, recorded | Carried forward from plan 45.1-01. Task 3 records it in prd.md with its rationale and its declined counter-argument, and flags `/gsd-secure-phase 45.1` so it enters the security record rather than disappearing into a diff. |
| T-45.1-SC | Tampering | npm/pip/cargo installs | n/a | This phase installed zero external packages; `go.mod` and `go.sum` are unchanged. Verify with `git diff --stat origin/master..HEAD -- go.mod go.sum` showing no changes. |
</threat_model>

<verification>
- `make quality-full` green in WSL against the live stack.
- `bash scripts/coverage_docker.sh` ≥85% owned surface.
- `go-mutesting` ≥70% killed on `bridge_supervisor.go`, `bridge_risk.go`, `identity.go`.
- `govulncheck ./...` clean.
- `bash scripts/quality_snapshot_gate.sh` prints `ok: … checked N row(s)`.
- `git diff --stat origin/master..HEAD -- go.mod go.sum` shows no change.
- All five ROADMAP Success Criteria met live, scenario scored >9.8.
- `git push` and every CI job green.
</verification>

<success_criteria>
- The five ROADMAP Success Criteria are proven on the running stack with recorded evidence.
- The full gate matrix is green with measured coverage, mutation and vulnerability numbers.
- The three sidecars' negotiated protocol versions are measured, not assumed.
- Every premise this phase falsified is amended in the document that carries it, with citation.
- The quality snapshot passes its phase-close gate and CI is green after the push.
</success_criteria>

<output>
Create `.planning/phases/45.1-native-mcp-client/45.1-08-SUMMARY.md` when done, carrying the live
scenario's score, the measured coverage percentage, the three mutation scores, and the three
negotiated protocol versions.
</output>
