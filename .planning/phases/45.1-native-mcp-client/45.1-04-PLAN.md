---
phase: 45.1-native-mcp-client
plan: 04
type: execute
wave: 3
depends_on: ["45.1-02"]
files_modified:
  - internal/agent/mcptools/bridge_risk.go
  - internal/agent/mcptools/bridge_risk_test.go
autonomous: true
requirements: [MCPC-01]

must_haves:
  truths:
    - "All four SDK tool annotation hints are read; none is truncated away as ReadOnlyHint and DestructiveHint were."
    - "An adopted annotation can raise a tool's risk tier and can never lower one, proven exhaustively over every hint combination rather than by example."
    - "An unannotated tool still classifies as mutating and destructive — fail closed."
    - "No tool in the calendar, WhatsApp or memory recipe tables changes its risk tier as a result of adopting the two new hints."
    - "A nil Annotations pointer takes the same fail-closed branch an unannotated ToolDef took before the swap."
  artifacts:
    - path: "internal/agent/mcptools/bridge_risk.go"
      provides: "mcpToolRisk over all four SDK hints, escalate-only"
      contains: "IdempotentHint"
    - path: "internal/agent/mcptools/bridge_risk_test.go"
      provides: "the exhaustive escalate-only invariant proof plus a recipe-table no-drift table"
      contains: "func TestMCPToolRiskEscalatesOnly"
  key_links:
    - from: "internal/agent/mcptools/bridge_risk.go"
      to: "sdkmcp.ToolAnnotations"
      via: "all four hint fields"
      pattern: "OpenWorldHint|IdempotentHint"
  prohibitions:
    - "A server-declared annotation must never be able to lower a tool's risk tier: a mounted server must not be able to talk itself out of an approval gate by advertising readOnlyHint or idempotentHint."
    - "Operator curation must not silently become advisory: the trusted-recipe action tables are the operator's explicit classification and a newly-adopted hint must not re-tier a tool the operator already tiered, except through an explicit destructiveHint claim, which already escalates today."
---

<objective>
Adopt all four SDK tool annotations and prove, exhaustively, that adoption can only raise a
tool's risk tier.

Purpose: `internal/mcp/client.go:88-91`'s hand-rolled `ToolAnnotations` carried only
`ReadOnlyHint` and `DestructiveHint`. Deleting that type removes the truncation for free — but
"for free" is exactly when a risk classifier quietly changes behaviour, so the adoption ships with
its invariant proof rather than after it.
Output: a four-hint `mcpToolRisk`, an exhaustive escalate-only proof, and a recipe-table no-drift
table.

D-107 closes 46-CONTEXT's deferred item *"Adopting the SDK's `IdempotentHint`/`OpenWorldHint` → the
client-swap phase, or Phase 48"* in favour of this phase.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45.1-native-mcp-client/45.1-CONTEXT.md
@.planning/phases/45.1-native-mcp-client/45.1-RESEARCH.md
@.planning/phases/45.1-native-mcp-client/45.1-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read all four annotation hints with their documented defaults</name>
  <files>internal/agent/mcptools/bridge_risk.go, internal/agent/mcptools/bridge_risk_test.go</files>
  <read_first>
    - internal/agent/mcptools/bridge_risk.go (whole file as it stands after plan 45.1-02's retype — `mcpActionClass`, `trustedRecipeActions` for all three recipes, `managedBridgePolicy`, `mcpToolRisk`, `explicitDestructive`)
    - internal/agent/mcptools/bridge_risk_test.go (whole file — the existing table-driven shape to extend, not replace)
    - internal/agent/mcptools/bridge.go (`specFromToolDefWithPolicy` and `applyMCPOperationMetadata` — the consumers of `mcpToolRisk`'s two return values; `applyMCPOperationMetadata` is deliberately out of scope)
    - internal/agent/tools/spec.go (the `Spec.Mutating` / `Spec.Destructive` fields and what the gateway does with them)
    - `git show <phase-start-sha>:internal/mcp/client.go | sed -n '86,100p'` — the two-field `ToolAnnotations` this adoption replaces, read from history because the file is deleted by plan 45.1-03
    - $(go env GOMODCACHE)/github.com/modelcontextprotocol/go-sdk@v1.7.0/mcp/protocol.go — the `ToolAnnotations` struct and, critically, its four doc-comment defaults: `DestructiveHint` **default true**, `IdempotentHint` **default false**, `OpenWorldHint` **default true**, `ReadOnlyHint` **default false**
  </read_first>
  <behavior>
    - `annotationIdempotent(nil)` is false; `annotationIdempotent(&ToolAnnotations{})` is false;
      `annotationIdempotent(&ToolAnnotations{IdempotentHint: true})` is true.
    - `annotationOpenWorld(nil)` is **true**; `annotationOpenWorld(&ToolAnnotations{})` is **true**
      (nil `*bool` means the documented default, which is true); `OpenWorldHint: &false` is false;
      `OpenWorldHint: &true` is true.
    - `explicitDestructive(nil)` is false; a nil `DestructiveHint` is false; `&false` is false;
      `&true` is true. Unchanged semantics, pointer-safe receiver.
    - `unsafeToRepeatBeyondAura(a)` is true exactly when `!a.ReadOnlyHint && !annotationIdempotent(a) && annotationOpenWorld(a)`.
  </behavior>
  <action>
Add three readers to `bridge_risk.go`, each honouring the SDK's documented default and each
carrying a one-line comment citing `protocol.go`'s doc comment. The defaults are counter-intuitive
— a nil pointer means *more* dangerous, not less — and that is exactly what CLAUDE.md requires a
comment for.

- `func annotationIdempotent(a *sdkmcp.ToolAnnotations) bool` returning `a != nil && a.IdempotentHint`.
- `func annotationOpenWorld(a *sdkmcp.ToolAnnotations) bool` returning true when `a == nil`, true
  when `a.OpenWorldHint == nil`, else `*a.OpenWorldHint`.
- `func unsafeToRepeatBeyondAura(a *sdkmcp.ToolAnnotations) bool` returning
  `a != nil && !a.ReadOnlyHint && !annotationIdempotent(a) && annotationOpenWorld(a)` — one named
  predicate so the escalation rule added in Task 2 reads as a sentence rather than a boolean pile.
  Comment the reasoning: non-idempotent means a redial-and-reissue can never be safe; open-world
  means the effect lands outside anything Aura can undo.

Make `explicitDestructive` take `*sdkmcp.ToolAnnotations` and be nil-safe. `Tool.Annotations` is a
**pointer** in the SDK where `mcp.ToolDef.Annotations` was a value, so every dereference in this
file must go through one of these readers — a raw `def.Annotations.X` is a panic waiting for the
first server that ships a tool with no annotations block.

Add the unit tests for all four readers covering every case in `<behavior>`.
  </action>
  <verify>
    <automated>go test -race ./internal/agent/mcptools/ -run 'TestAnnotation|TestExplicitDestructive|TestUnsafeToRepeat' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go vet ./...` exit 0.
    - `grep -n 'func annotationIdempotent' internal/agent/mcptools/bridge_risk.go` matches.
    - `grep -n 'func annotationOpenWorld' internal/agent/mcptools/bridge_risk.go` matches.
    - `grep -n 'func unsafeToRepeatBeyondAura' internal/agent/mcptools/bridge_risk.go` matches.
    - A test asserts `annotationOpenWorld(nil) == true` and `annotationOpenWorld(&sdkmcp.ToolAnnotations{}) == true` — the documented default, not the Go zero value.
    - `! grep -n 'Annotations\.\(ReadOnlyHint\|IdempotentHint\|OpenWorldHint\)' internal/agent/mcptools/bridge_risk.go` — no raw dereference of the pointer.
  </acceptance_criteria>
  <done>All four hints are readable with the SDK's documented defaults, nil-safe, with the counter-intuitive default-true pointers commented and cited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One new escalation in the fallback path, proven escalate-only over all 36 hint combinations</name>
  <files>internal/agent/mcptools/bridge_risk.go, internal/agent/mcptools/bridge_risk_test.go</files>
  <read_first>
    - internal/agent/mcptools/bridge_risk.go (as left by Task 1)
    - internal/agent/mcptools/bridge_risk_test.go (as left by Task 1)
    - .planning/phases/45.1-native-mcp-client/45.1-CONTEXT.md § D-107 (the escalate-only invariant, stated as the thing that MUST be proven by test)
    - .claude/skills/property-based-testing/SKILL.md (the exhaustive-combination shape this invariant proof wants)
  </read_first>
  <behavior>
    Tier ordering, used by every assertion below:
    `readOnly (false,false)` &lt; `mutating (true,false)` &lt; `destructive (true,true)`.

    - `Annotations == nil` classifies `(true, true)`.
    - `DestructiveHint == &true` classifies `(true, true)` regardless of every other hint and
      regardless of the recipe table — unchanged, highest precedence.
    - A tool present in a trusted-recipe action table classifies exactly as the table says, unless
      `DestructiveHint == &true` escalates it. `IdempotentHint` and `OpenWorldHint` in any
      combination change nothing for a table-listed tool.
    - Fallback path, `ReadOnlyHint == true` with no explicit destructive claim: `(false, false)`,
      whatever the two new hints say. Reading an open world is still reading.
    - Fallback path, `DestructiveHint == nil`: `(true, true)` — nil means destructive per the SDK's
      documented default.
    - Fallback path, `DestructiveHint == &false`, `ReadOnlyHint == false`, tool non-idempotent and
      open-world: `(true, true)`. **This is the one new escalation.**
    - Fallback path, `DestructiveHint == &false`, `ReadOnlyHint == false`, `IdempotentHint == true`:
      `(true, false)` — an idempotent mutation is neither escalated nor de-escalated.
    - **The invariant:** for each of the 36 combinations of `ReadOnlyHint ∈ {true,false}` ×
      `IdempotentHint ∈ {true,false}` × `OpenWorldHint ∈ {nil,&true,&false}` ×
      `DestructiveHint ∈ {nil,&true,&false}`, and for both the table and fallback paths, the new
      classification's tier is **greater than or equal to** the tier a frozen two-hint baseline
      produces. Never lower. Not one case.
    - Every tool name in all three trusted-recipe tables classifies to the same
      `(mutating, destructive)` pair before and after this change, both for a nil `Annotations` and
      for an all-hints-set `Annotations` with `DestructiveHint == &false`.
  </behavior>
  <action>
Leave the trusted-recipe branch of `mcpToolRisk` **exactly as it is**. The operator's action tables
are explicit curation and outrank a server hint; only an explicit `destructiveHint:true` claim
escalates a table-listed tool, which it already does today. Adding the new hints there would
silently re-tier `create_event`, `download_media` and `memory_upsert_fact` and put an approval
prompt in front of ordinary work — a behaviour change nobody asked for.

In the fallback branch, after the existing `ReadOnlyHint` case and the `DestructiveHint == nil`
case, and before the final `return true, *a.DestructiveHint`, insert the single new escalation:
`if unsafeToRepeatBeyondAura(a) { return true, true }`.

Emit one `slog.Debug("mcp tool annotations", "tool", name, "read_only", …, "destructive", …,
"idempotent", …, "open_world", …, "mutating", …, "tier_destructive", …)` at classification time so
the adoption is observable in a boot log — that is how an operator sees the truncation is gone
without reading code.

**Out of scope, deliberately:** do not feed `IdempotentHint` into `applyMCPOperationMetadata`'s
`ReplayPolicy`. Phase 45's ratified finding (prd.md Amendment #121) is that the uniform
`tools.ReplayToolResult` default is already correct for every mutating bridged tool and there is no
vocabulary to wait for. Re-deriving it from a server-declared hint would let a mounted server
influence Aura's replay policy — strictly worse than a fixed default, and outside D-107's mandate.

Extend `bridge_risk_test.go`, do not replace it. Add:
1. `TestMCPToolRiskEscalatesOnly` — build the 36-combination cartesian product; evaluate both the
   new `mcpToolRisk` and a **frozen local baseline** `frozenTwoHintBaseline` reproducing today's
   two-hint semantics verbatim (read-only-and-not-explicitly-destructive → `(false,false)`; nil
   destructive hint → `(true,true)`; else `(true, *hint)`); assert `riskTier(new) >= riskTier(baseline)`
   for every combination in both the table and fallback paths. Name the failing combination in the
   assertion message — a bare `false != true` on case 23 of 36 is worthless. Assert the executed
   case count equals 36 per path inside the test, so a silently-shrunk table fails rather than
   passing vacuously.
2. `TestMCPToolRiskRecipeTablesUnchanged` — iterate every tool name in all three
   `trustedRecipeActions` maps and assert the classification for a nil `Annotations` and for
   `&sdkmcp.ToolAnnotations{ReadOnlyHint: false, IdempotentHint: false, OpenWorldHint: ptr(true), DestructiveHint: ptr(false)}`
   matches a **hard-coded** expected pair per tool. Hard-code the expectations; a test that derives
   its expectation from the code under test proves nothing. Assert the covered-name count equals
   `len(trustedRecipeActions[src])` for each source.
3. `TestMCPToolRiskNilAnnotations` — `(true, true)`.
  </action>
  <verify>
    <automated>go test -race ./internal/agent/mcptools/ -run 'TestMCPToolRisk' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go vet ./...` exit 0.
    - `go test -race ./internal/agent/mcptools/ -count=1` exits 0.
    - `TestMCPToolRiskEscalatesOnly` asserts its own case count equals 36 per path.
    - `TestMCPToolRiskRecipeTablesUnchanged` asserts its covered-name count equals `len(trustedRecipeActions[src])` for all three sources.
    - `grep -n 'unsafeToRepeatBeyondAura' internal/agent/mcptools/bridge_risk.go` matches inside `mcpToolRisk`'s fallback branch only — `grep -c 'unsafeToRepeatBeyondAura' internal/agent/mcptools/bridge_risk.go` equals 2 (definition plus one call site).
    - `! grep -n 'IdempotentHint\|OpenWorldHint' internal/agent/mcptools/bridge.go` — `applyMCPOperationMetadata` is untouched.
    - `wc -l internal/agent/mcptools/bridge_risk.go` reports under 600.
    - `go-mutesting internal/agent/mcptools/bridge_risk.go` reports ≥70% killed (WSL), recorded in `45.1-VALIDATION.md`.
  </acceptance_criteria>
  <done>One new escalation lands in the fallback path only, escalate-only is proven over every hint combination against a frozen baseline, and no recipe-table tool moves tier.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by **this plan**:

- `internal/agent/mcptools/bridge_risk.go`: `annotationIdempotent`, `annotationOpenWorld`, `unsafeToRepeatBeyondAura`.
- `internal/agent/mcptools/bridge_risk_test.go`: `TestMCPToolRiskEscalatesOnly`, `TestMCPToolRiskRecipeTablesUnchanged`, `TestMCPToolRiskNilAnnotations`, `TestAnnotationOpenWorldDefaults`, `frozenTwoHintBaseline`, `riskTier`, `ptr`.
- New log record: `"mcp tool annotations"` at DEBUG with keys `tool`, `read_only`, `destructive`, `idempotent`, `open_world`, `mutating`, `tier_destructive`.
</artifacts_this_phase_produces>

<reversibility>
| Decision | Rating | Note |
|---|---|---|
| Adopt all four hints now rather than in Phase 48 | reversible | D-107, an operator decision; ~20 LOC on a pure function. Reverting is local. |
| Add one new escalation (non-idempotent + open-world + `destructiveHint:false` → destructive) | reversible, but user-visible | It can put an approval prompt in front of a tool that previously ran unprompted. Bounded to the **fallback** path — no built-in recipe tool is affected, asserted by test — so in practice it can only fire for an ad hoc or self-minted server. Recorded so a later "why did this start asking me?" has an answer. |
| Do **not** derive `ReplayPolicy` from `IdempotentHint` | reversible | Deliberate. Letting a mounted server influence Aura's replay policy is worse than the fixed default prd.md Amendment #121 already ratified. |
</reversibility>

<threat_model>
ASVS level: **1**. Block on: **high**.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mounted server's declared annotations → Aura's risk tiering | A server chooses its own hint values; Aura decides what they are allowed to do. |
| Risk tier → gateway approval decision | `Spec.Mutating` / `Spec.Destructive` are what put a human in the loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45.1-18 | Elevation of privilege | `mcpToolRisk` hint adoption | mitigate | Escalate-only, proven exhaustively over all 36 hint combinations against a frozen two-hint baseline. A server advertising `readOnlyHint:true` or `idempotentHint:true` cannot move any tool to a lower tier than today's classification. |
| T-45.1-19 | Elevation of privilege | nil `*ToolAnnotations` | mitigate | Nil takes the fail-closed `(true, true)` branch, matching what an unannotated `mcp.ToolDef` produced. The SDK's pointer-valued `OpenWorldHint`/`DestructiveHint` both default to the *more dangerous* reading, and `annotationOpenWorld` honours that. |
| T-45.1-20 | Tampering | recipe-table re-tiering | mitigate | The trusted-recipe branch is untouched; a per-tool no-drift table asserts every name in all three recipes classifies identically before and after. |
| T-45.1-21 | Spoofing | server influencing replay policy | mitigate | `IdempotentHint` is deliberately not wired into `applyMCPOperationMetadata`; replay policy stays the fixed Aura default. |
| T-45.1-SC | Tampering | npm/pip/cargo installs | n/a | Zero external packages installed. |
</threat_model>

<verification>
- `go build ./... && go vet ./...` green.
- `go test -race ./internal/agent/mcptools/ -count=1` green.
- `golangci-lint run` clean.
- `go-mutesting internal/agent/mcptools/bridge_risk.go` ≥70% killed (WSL), recorded in `45.1-VALIDATION.md`.
</verification>

<success_criteria>
- All four SDK annotation hints are read, with the SDK's documented defaults honoured.
- Escalate-only is proven over every hint combination, not sampled.
- No tool in any trusted-recipe table changes tier.
- Fail-closed on a nil annotation is preserved.
</success_criteria>

<output>
Create `.planning/phases/45.1-native-mcp-client/45.1-04-SUMMARY.md` when done.
</output>
