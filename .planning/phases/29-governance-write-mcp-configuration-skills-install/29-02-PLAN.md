---
phase: 29-governance-write-mcp-configuration-skills-install
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - internal/mcp/manager/envedit.go
  - internal/mcp/manager/envedit_test.go
  - internal/mcp/manager/configwrite.go
  - internal/mcp/manager/configwrite_test.go
  - internal/agui/governance_write_seam.go
  - internal/agui/governance_write_api.go
  - internal/agui/governance_write_api_test.go
  - cmd/aura/serve_webui.go
autonomous: true
requirements: [MCPW-01, MCPW-02, MCPW-03]
must_haves:
  truths:
    - "Installing an MCP recipe and a custom-stdio server each write the expected entry to the managed servers.json; the response previews the CLI-equivalent + the ManagedConfigPath() destination before the write"
    - "A server name that already exists is rejected (no duplicate servers.json entry); re-saving identical config is a no-op (idempotency edge MCPW-01)"
    - "An interrupted install/mutation leaves the prior config intact — the write is temp-file → db.WithTx(audit INSERT) → os.Rename on tx success / discard temp on failure (concurrency edge MCPW-01; D-04 atomicity)"
    - "Editing one MCP env value preserves the other env values and any unchanged secret submitted as its redacted ${KEY} placeholder (mergeEnvPreserveCredentials); the value is never overwritten with the placeholder text"
    - "No /api/governance/mcp/* response body contains a raw env value — only key-only chips (envChips) with a redacted flag cross the wire"
    - "A still-placeholder required recipe env var raises a soft warning in the response but the save still succeeds and the server stays blocked/unhealthy until filled"
    - "Each of install / env-edit / enable / disable / remove / trust appends EXACTLY ONE aura.mcp_audit row (one named action = one audit row); a double-submitted remove removes once then 404s with at most one audit row"
    - "Trust-approve (operator-direct, not a model gate) populates the today-empty Trust.ApprovedBy/ApprovedAt/Reason, writes one mcp_audit row with action=trust + reason, flips the server runnable, and the board then shows its live tool count via ProbeServer"
    - "Server rows render in a deterministic by-name order after any edit/disable/remove (ordering edge MCPW-02)"
    - "With an allowlist present, a denied/destructive advertised tool is shown explicitly and is absent from the runtime registry while allowed tools still mount; a failing/hung server surfaces a fail-soft per-row warning without stalling the board (MCPW-03)"
    - "Every new mutating endpoint returns 401 unauthenticated and 403 without governance.write; a forced backend failure renders a sanitized error (no stack/secret); an empty config renders a valid empty state"
  artifacts:
    - path: "internal/mcp/manager/envedit.go"
      provides: "SetServerEnv — load→merge one server's Env via mergeEnvPreserveCredentials→re-set whole entry (D-05)"
      exports: ["SetServerEnv"]
    - path: "internal/mcp/manager/configwrite.go"
      provides: "Atomic temp-write → caller-tx → rename wrapper for the six MCP write verbs (D-04 / MCPW-01 concurrency edge)"
    - path: "internal/agui/governance_write_seam.go"
      provides: "MCPWriteProvider interface + GovernanceWriteProviders bundle + SetGovernanceWriteProviders setter"
      exports: ["MCPWriteProvider", "GovernanceWriteProviders", "SetGovernanceWriteProviders"]
    - path: "internal/agui/governance_write_api.go"
      provides: "POST/PATCH/DELETE /api/governance/mcp[/{name}/...] thin handlers (mirror governance_api.go)"
  key_links:
    - from: "internal/agui/governance_write_api.go"
      to: "internal/mcp/manager (SetServerEnv, the configwrite atomic wrapper, InsertMCPAuditTx)"
      via: "the MCPWriteProvider methods each run db.WithTx(temp config write + InsertMCPAuditTx)→rename"
      pattern: "InsertMCPAuditTx"
    - from: "internal/agui/governance_write_api.go"
      to: "internal/agui/governance_api.go (envChips)"
      via: "every env-bearing response reuses envChips (key-only, no value)"
      pattern: "envChips"
    - from: "cmd/aura/serve_webui.go"
      to: "internal/agui/auth.go (RequireCapability)"
      via: "each new MCP write route mounted behind RequireCapability(governanceWriteCapability)"
      pattern: "governanceWriteCapability"
    - from: "internal/agui/governance_write_api.go"
      to: "internal/mcp/probe.go (ProbeServer)"
      via: "post-install/post-trust live tool-count reuses the MCPBoardProvider.Probe (3s bounded, per-row)"
      pattern: "Probe\\("
  prohibitions:
    - statement: "MUST NOT render, return, or log any raw MCP env value in any API response, DOM, or log — redacted chips / key-only rows only"
      status: resolved
      verification: "test — a handler test asserts no /api/governance/mcp/* response carries an env value; the log-scan backstop runs in plan 05"
    - statement: "MUST NOT overwrite a stored secret when the operator submits its redacted placeholder unchanged"
      status: resolved
      verification: "test — property: for any stored secret S and submitted ${KEY}, after SetServerEnv the stored value is exactly S, never \"${KEY}\""
    - statement: "MUST NOT let a model tool call approve, trust-approve, or self-activate an MCP config mutation — trust-approve is operator-only via the cockpit action behind governance.write"
      status: resolved
      verification: "test — the trust route requires governance.write (403 without); there is no model-facing trust path"
    - statement: "MUST NOT silently mount a destructive or denied MCP tool when an allowlist exists — denied tools are shown explicitly and excluded from the runtime registry before model reach"
      status: resolved
      verification: "test — mount-policy unit test: a denied tool is absent from the runtime registry and explicitly surfaced (Phase-16 policy unchanged)"
    - statement: "MUST NOT mutate MCP config without appending an immutable mcp_audit row, nor remove a server without an explicit confirmation"
      status: resolved
      verification: "test — each of install/edit/enable/disable/remove/trust appends exactly one mcp_audit row; remove is confirmation-gated client-side (plan 04) and audited here"
---

<objective>
Build the MCP write backend + the authenticated mutating endpoints over the EXISTING Phase-16
manager — the in-place env-edit path (gap-filler #2), the atomic config-write+audit wrapper, and
the six named-action handlers. Reuse `mergeEnvPreserveCredentials`/`isPlaceholderValue` (the D-05
substrate), `SaveManagedConfig`, `ProbeServer`, `envChips`, and the `InsertMCPAuditTx`/`governance.write`
from plan 01. No new MCP transport, no new trust/risk logic — thin write adapters only.

Purpose: deliver MCPW-01/02/03 — recipe/custom install with pre-save preview, redacted four-state
env editing, reversible enable/disable, confirmed remove, operator-direct trust-approval — every
mutation atomic with its audit row, no secret ever on the wire.
Output: `internal/mcp/manager/envedit.go` + `configwrite.go`, `internal/agui/governance_write_seam.go`
+ `governance_write_api.go`, and the route+capability mounts in `cmd/aura/serve_webui.go`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: In-place env-edit path (SetServerEnv) + atomic temp→tx→rename config-write wrapper (D-05 / D-04 / MCPW-01 concurrency)</name>
  <files>internal/mcp/manager/envedit.go, internal/mcp/manager/envedit_test.go, internal/mcp/manager/configwrite.go, internal/mcp/manager/configwrite_test.go</files>
  <read_first>
    - internal/mcp/manager/config.go (mergeEnvPreserveCredentials:95, isPlaceholderValue:145, ImportProfile:47, RedactEnv:78, cutEnv:137 — ALL unexported in package manager; the env-edit path lives in this package so it calls them directly — the D-05 substrate)
    - internal/mcp/managed_config.go (SaveManagedConfig:122 — a DIRECT os.WriteFile with 0o600, NOT temp+rename; ManagedConfigPath:87; ManagedServer/ManagedTrust:51/65; LoadManagedConfig:101; validateManagedServers:249)
    - internal/skills/writer.go:179-225 (WriteInstallPending — the proven "reconcilable FS write then audit INSERT inside db.WithTx" ordering to mirror)
    - internal/db/tx.go (db.WithTx — signature `func WithTx(ctx context.Context, pool *pgxpool.Pool, fn func(*sqlc.Queries) error) error`; copy the parameter types verbatim into WriteConfigWithAudit)
    - internal/mcp/manager/audit.go (InsertMCPAuditTx from plan 01 — `func InsertMCPAuditTx(ctx context.Context, q *sqlc.Queries, in MCPAuditInsert) error`)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"Pattern: atomic mutation + audit (D-04)" + §"Common Pitfalls" Pitfall 1
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md §3 + §"Shared Patterns: Atomic mutation + audit"
  </read_first>
  <behavior>
    - SetServerEnv(doc, name, submitted): the resulting server.Env preserves any existing real secret whose submitted value is the redacted ${KEY} placeholder; a non-placeholder submitted value overwrites; other servers' env untouched
    - Property: for any stored secret S under key K and submitted "K=${K}", after SetServerEnv doc.MCPServers[name].Env holds "K=S" exactly (never "K=${K}")
    - The atomic write wrapper: writes config to a temp file in the same dir, runs the caller's db.WithTx(audit INSERT), os.Rename(temp, path) on tx success, os.Remove(temp) on tx failure → an induced tx error leaves the prior servers.json byte-identical
    - A non-secret env var edits in place; clearing it is allowed
  </behavior>
  <action>
    Create `internal/mcp/manager/envedit.go`: `func SetServerEnv(doc *mcp.ManagedConfig, name string,
    submitted []string) error` — load the existing entry from `doc.MCPServers[name]` (404-style error if
    absent), compute `next.Env = mergeEnvPreserveCredentials(existing.Env, submitted)` (a submitted
    `${KEY}` placeholder is treated as unchanged → the stored secret is preserved), re-set the whole entry
    `doc.MCPServers[name] = next`. (The simplest equivalent reuse is `ImportProfile(doc,
    singleServerIncomingDoc, ImportOptions{OverwriteCredentials:false})` which already runs this merge per
    server — either path is acceptable; prefer the direct `mergeEnvPreserveCredentials` call for clarity
    since this code is in `package manager`.)
    Create `internal/mcp/manager/configwrite.go`: `func WriteConfigWithAudit(ctx context.Context, pool
    *pgxpool.Pool, path string, next mcp.ManagedConfig, in MCPAuditInsert) error` — mirror the `db.WithTx`
    signature EXACTLY: `ctx` is `context.Context` and `pool` is `*pgxpool.Pool` (NOT strings); only `path`
    is a `string`. Implement the locked ordering (RESEARCH Pitfall 1):
    (1) validate+normalize then write `next` to a temp file beside `path` (reuse SaveManagedConfig's
    marshal+validate; write to a sibling temp, 0o600); (2) `db.WithTx(ctx, pool, func(q *sqlc.Queries)
    error { return InsertMCPAuditTx(ctx, q, in) })`; (3) on tx error `os.Remove(temp)` and return; (4) on
    success `os.Rename(temp, path)`. This single wrapper gives BOTH the D-04 atomicity AND the MCPW-01
    concurrency-edge temp+rename. Write the env-edit property test (`${KEY}` preserves the stored secret;
    other values untouched) and the atomic-write test (induced tx failure → prior file intact).
  </action>
  <acceptance_criteria>
    - `internal/mcp/manager/envedit.go` exports `SetServerEnv`; a unit test submits `TOKEN=${TOKEN}` over a stored `TOKEN=real-secret` and asserts the stored value remains `real-secret`
    - `internal/mcp/manager/configwrite.go` declares `WriteConfigWithAudit(ctx context.Context, pool *pgxpool.Pool, path string, next mcp.ManagedConfig, in MCPAuditInsert) error` (ctx/pool correctly typed, NOT strings), writes a temp file, runs the caller tx, and `os.Rename`s only on tx success; an induced tx failure test asserts the original servers.json bytes are unchanged
    - `go test ./internal/mcp/manager/ -run 'TestSetServerEnv|TestWriteConfigWithAudit'` exits 0
    - `go test -tags db_integration ./internal/mcp/manager/ -run TestWriteConfigWithAuditAtomic` exits 0 (the tx-failure-leaves-prior-config assertion against live PG)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'func SetServerEnv' internal/mcp/manager/envedit.go && grep -q 'pool \*pgxpool.Pool' internal/mcp/manager/configwrite.go && grep -q 'os.Rename' internal/mcp/manager/configwrite.go && go test ./internal/mcp/manager/ -run 'TestSetServerEnv|TestWriteConfigWithAudit'</automated>
  </verify>
  <done>SetServerEnv preserves untouched secrets via mergeEnvPreserveCredentials; WriteConfigWithAudit has the correct `ctx context.Context, pool *pgxpool.Pool` signature; the atomic temp→tx→rename wrapper makes every MCP mutation+audit all-or-nothing and gives the concurrency-edge its temp+rename; both proven by unit + live-PG tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCPWriteProvider seam + the six named-action handlers (install/env/trust/enable/disable/remove)</name>
  <files>internal/agui/governance_write_seam.go, internal/agui/governance_write_api.go, internal/agui/governance_write_api_test.go</files>
  <read_first>
    - internal/agui/governance_seam.go (MCPBoardProvider:28 + GovernanceProviders:53 + SetGovernanceProviders:101 — the consumer-side narrow seam + off-constructor setter to mirror)
    - internal/agui/governance_api.go (handleMCPList:158 thin-handler shape; envChips:209 the key-only no-leak projection to REUSE; registerGovernanceRoutes:144 the specific method+path registration; mcp.RedactSecrets on errors)
    - internal/agui/onboarding_api.go (the mutation body-decode + MaxBytesReader size-cap + validate + principalIdentityID + one-service-call skeleton — the generic handleOnboardingMutation[Req,Resp])
    - internal/agui/approvals_api.go:114-143 (handleResolveApproval — the path-token guard + closed verb-set + sanitizeErr shape)
    - internal/agui/auth.go:296 (principalFrom — the actor_identity_id for the audit row, D-03)
    - internal/mcp/managed_config.go (ManagedTrust:65 fields ApprovedBy/ApprovedAt/Reason — never populated today; trust classes:21-25; NormalizedTrust:220) + internal/mcp/probe.go (ProbeServer:43 for the post-write tool-count) + internal/mcp/manager/catalog.go (BuiltInCatalog/LookupCatalog + CatalogEntry.RequiredEnv:93 — surface as the guided-form source)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md §4 + §5 (the handler + seam adaptation, the exact route list) + §"Shared Patterns: Trust-approve populates the today-empty fields"
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md §1/§2/§3 (the response contract: CLI-equiv + `Will write to:` destination preview, four-state env, soft warning, denied-tool danger marker, fail-soft probe warning)
  </read_first>
  <behavior>
    - POST /api/governance/mcp installs a recipe or custom-stdio server: duplicate name → 409 with the duplicate-name error; success writes the servers.json entry (atomic wrapper) + one install audit row; the response echoes the CLI-equivalent + ManagedConfigPath() destination
    - PATCH /api/governance/mcp/{name}/env applies SetServerEnv (preserve untouched secrets) → atomic write + one edit audit row; response carries key-only env chips + a soft-warning list when a required recipe var is still placeholder/missing; no raw value
    - POST /api/governance/mcp/{name}/trust sets Trust.{Class,ApprovedBy:principal,ApprovedAt:now,Reason:body.reason} → atomic write + one trust audit row → flips runnable → re-probe tool count (D-12: operator-direct trust-approve, not a model gate)
    - POST /api/governance/mcp/{name}/enable|disable toggles Enabled (idempotent) → atomic write + one audit row per real transition
    - DELETE /api/governance/mcp/{name} removes the entry → atomic write + one remove audit row; a second delete 404s (≤1 audit row)
    - every handler: 503 when the write provider is nil; 401 unauth / 403 without governance.write (from the mount); sanitized error on failure
  </behavior>
  <action>
    In `internal/agui/governance_write_seam.go` declare `MCPWriteProvider` with methods `InstallServer(ctx,
    req) (…, error)`, `SetServerEnv(ctx, name, submitted) (…, error)`, `TrustApprove(ctx, name, reason)
    (…, error)`, `SetEnabled(ctx, name, enabled bool) error`, `RemoveServer(ctx, name) error` — each
    concrete impl runs `WriteConfigWithAudit` (the plan-01/task-1 atomic wrapper) with the right
    `MCPAuditInsert{ActorIdentityID: principal, Action: …, ServerName: name, Reason: …}`. Add a
    `GovernanceWriteProviders` bundle + `SetGovernanceWriteProviders` setter (off-constructor, narrow seam
    — mirror governance_seam.go). A nil field → its routes answer 503.
    Create `internal/agui/governance_write_api.go` with `registerGovernanceWriteRoutes(mux)` registering
    the SPECIFIC method+path siblings (D-01): `POST /api/governance/mcp`, `PATCH
    /api/governance/mcp/{name}/env`, `POST /api/governance/mcp/{name}/trust`, `POST
    /api/governance/mcp/{name}/enable`, `POST /api/governance/mcp/{name}/disable`, `DELETE
    /api/governance/mcp/{name}`. Each handler mirrors the read-handler + onboarding-mutation shape:
    nil-check the write provider (503), `MaxBytesReader` + decode + sanitized-validate (400), read
    `principalFrom`/`principalIdentityID` (401 if absent — but the mount already gates), ONE provider call,
    project any env echo through `envChips` (key-only — no value, Pitfall 4), build the CLI-equivalent +
    `ManagedConfigPath()` destination preview in the install response, surface the soft-warning list (a
    required recipe var still placeholder/missing), `sanitizeErr` on failure. The denied/destructive-tool
    surfacing + fail-soft probe warning REUSE the existing read board's `ProbeServer`/SnapshotStatus path
    (Phase-16 mount policy UNCHANGED) — the install/trust response re-probes via `MCPBoardProvider.Probe`
    for the live tool count. Write `governance_write_api_test.go` (httptest) covering: duplicate-name 409,
    env-edit preserves secret (no value in body), trust populates the fields + audit, enable/disable
    idempotent, double-remove 404, 503-when-unwired, 403-without-cap, sanitized error, deterministic
    by-name ordering.
  </action>
  <acceptance_criteria>
    - `internal/agui/governance_write_seam.go` exports `MCPWriteProvider`, `GovernanceWriteProviders`, `SetGovernanceWriteProviders`
    - `internal/agui/governance_write_api.go` registers exactly the six MCP write routes as method+path-specific patterns (never a bare `/api/`)
    - a handler test asserts a PATCH .../env response body contains NO env value (only `{key, redacted}` chips) and that submitting `${KEY}` leaves the stored secret intact
    - a handler test asserts a duplicate install returns 409 and writes no second servers.json entry
    - `go test ./internal/agui/ -run TestGovernanceWrite` exits 0; `go build ./... && go vet ./...` pass
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'registerGovernanceWriteRoutes' internal/agui/governance_write_api.go && grep -q 'MCPWriteProvider' internal/agui/governance_write_seam.go && go test ./internal/agui/ -run TestGovernanceWrite</automated>
  </verify>
  <done>The MCPWriteProvider seam + six named-action handlers exist, mirroring the read-board thin-handler shape; env echoes are key-only; install previews the CLI-equiv + destination; trust populates the today-empty fields + audit; all mutations atomic with one audit row each; handler tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the MCP write routes behind RequireCapability(governance.write) + wire the concrete provider at the composition root</name>
  <files>cmd/aura/serve_webui.go</files>
  <read_first>
    - cmd/aura/serve_webui.go:165-172 (the governance read route consts) + :317-322 (the read mounts behind RequireCapability(governanceReadCapability)) + :331-334 (the onboarding create mounts behind identity.create — the EXACT mount idiom) + the governanceWriteCapability const added in plan 01
    - internal/agui/governance_write_api.go (the route consts/strings from plan-02 task 2)
    - internal/agui/governance_write_seam.go (SetGovernanceWriteProviders — wire the concrete MCP write provider where the read GovernanceProviders are wired)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"Parent-mux mount discipline" + Pitfall 5 (method+path-specific only; never a bare /api/ — shadows /api/integrations/, T-24-07)
  </read_first>
  <action>
    Add the `governance*WriteRoute` method+path consts in `cmd/aura/serve_webui.go` (mirroring the
    `governanceMCPListRoute` const block). Mount EACH new MCP write route `mux.Handle(<METHOD PATH>,
    agui.RequireCapability(aguiHandler, auth, governanceWriteCapability))` next to the read mounts — Go 1.22
    longest-pattern precedence keeps these method+path siblings authoritative over the bare `/api/` carve-out
    and the `/` embed catch-all (Pitfall 5: NEVER register a bare `/api/`). At the composition root where
    `SetGovernanceProviders` wires the read board, also construct + wire the concrete MCP write provider via
    `SetGovernanceWriteProviders` (it needs the pool, the managed-config path, and the MCPAuditStore from
    plan 01; the live tool-count reuses the same `ProbeServer` the read board already wires). Re-affirm the
    CSRF posture per the auth.go:18 note: the SPA writes are same-origin `credentials:'same-origin'` under
    SameSite=Strict — no cross-origin write path is introduced (document this in a comment at the mount block).
  </action>
  <acceptance_criteria>
    - `grep -n 'RequireCapability(aguiHandler, auth, governanceWriteCapability)' cmd/aura/serve_webui.go` matches once per MCP write route (install/env/trust/enable/disable/remove)
    - no bare `mux.Handle("/api/"` is introduced (grep returns nothing new)
    - `go build ./... && go vet ./...` pass
    - a serve-level test (or the existing serve smoke) confirms `POST /api/governance/mcp` returns 401 unauthenticated and 403 for an identity without governance.write, and 200/expected for the `*`-holding local identity
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'governanceWriteCapability' cmd/aura/serve_webui.go | grep -qv '^0$' && go build ./... && go vet ./...</automated>
  </verify>
  <done>All six MCP write routes are mounted behind RequireCapability(governance.write) as method+path-specific siblings; the concrete MCP write provider is wired at the composition root; build+vet pass; the auth gate (401/403) is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cockpit browser → SSR mux → agui write handler | untrusted operator request crosses RequireAuth → RequireCapability(governance.write) → the MCP write provider |
| write handler → managed servers.json | the FS write the atomic temp→tx→rename wrapper protects |
| write provider → aura.mcp_audit | the audit row that makes every mutation non-repudiable |
| manager → MCP server process (probe) | the live tool-count probe (configured-servers-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Information Disclosure | env values in responses/logs | mitigate | envChips (key-only) on every env-bearing response (governance_api.go:209 reused); SetServerEnv preserves untouched secrets; mcp.RedactSecrets on errors; log-scan backstop in plan 05 |
| T-29-02-02 | Tampering | unchanged-secret overwrite on edit | mitigate | mergeEnvPreserveCredentials + isPlaceholderValue (config.go:95/145) — a submitted ${KEY} preserves the stored value; property test |
| T-29-02-03 | Elevation of Privilege | model self-trusts a server | mitigate | trust-approve is operator-only behind RequireCapability(governance.write); no model-facing trust path; custom defaults to TrustBlocked until explicit approval |
| T-29-02-04 | Repudiation | unaudited config mutation | mitigate | WriteConfigWithAudit makes the config write + InsertMCPAuditTx all-or-nothing (temp→tx→rename); one audit row per named action |
| T-29-02-05 | Tampering | denied/destructive tool silently mounted | mitigate | Phase-16 mount-time risk policy UNCHANGED — denied tools excluded from the registry before model reach + surfaced explicitly with a danger marker (MCPW-03) |
| T-29-02-06 | Tampering | CSRF on the new write surface | mitigate | same-origin SameSite=Strict cookie; no cross-origin write path (re-affirmed at the mount per auth.go:18 Phase-29 note); TanStack useMutation is same-origin |
| T-29-02-07 | Denial of Service | a hung MCP server stalls the board | mitigate | the post-write probe reuses the Phase-28 bounded 3s timeout + per-row isolation (ProbeServer); a dead server fails only its own row |
| T-29-02-SC | Tampering | npm/pip/cargo installs | mitigate | NO package installs in this plan (Go-only MCP write code); the `skills` npx CLI legitimacy gate lives in plan 03 |

*Canon-referred to `/gsd-secure-phase` (do NOT design here):* SSRF / arbitrary outbound on the MCP
probe is canon web-safety (the DISP-04 error classes + `/api/image-proxy` SSRF guard + Phase-28's
"configured-servers-only" judgment). The probe targets only operator-configured servers; the SSRF
mitigation itself is owned by `/gsd-secure-phase`.
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean.
- `go test ./internal/mcp/manager/ -run 'TestSetServerEnv|TestWriteConfigWithAudit'` (unit) green.
- `go test -tags db_integration ./internal/mcp/manager/ -run 'TestWriteConfigWithAuditAtomic|TestMCPAuditOnePerAction'` (live PG) green — one audit row per action; tx-failure leaves prior config intact.
- `go test ./internal/agui/ -run TestGovernanceWrite` (httptest) green — duplicate 409, env-edit no-value + secret-preserved, trust populates fields, idempotent enable/disable, double-remove 404, 503/401/403, sanitized error, by-name order.
- `go test -race ./internal/mcp/... ./internal/agui/` clean.
- Serve auth gate: `POST /api/governance/mcp` 401 unauth / 403 without governance.write.
</verification>

<success_criteria>
- MCPW-01: recipe + custom install from the cockpit write the expected servers.json entry; CLI-equiv + destination previewed; duplicate rejected; interrupted install leaves prior config intact.
- MCPW-02: in-place env edit preserves other values + unchanged secrets; four-state + soft warning surfaced; no raw secret on the wire; reversible disable; confirmed remove; one mcp_audit row per action (append-only).
- MCPW-03: operator trust-approve populates ApprovedBy/At/Reason + audit + flips runnable + tool count; denied/destructive tool excluded + surfaced; fail-soft per-row warning.
- Every new endpoint 401/403-gated; sanitized errors; empty state valid.
</success_criteria>

<output>
Create `.planning/phases/29-governance-write-mcp-configuration-skills-install/29-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
