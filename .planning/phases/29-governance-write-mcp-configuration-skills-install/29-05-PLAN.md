---
phase: 29-governance-write-mcp-configuration-skills-install
plan: 05
type: execute
wave: 5
depends_on: ["29-01", "29-02", "29-03", "29-04"]
files_modified:
  - internal/agui/governance_write_secret_scan_test.go
  - internal/agui/no_model_approve_test.go
  - internal/mcp/manager/audit_store_integration_test.go
  - internal/skills/installer_integration_test.go
  - web/e2e/governance-write.spec.ts
  - web/src/governance/__tests__/governanceWrite.coverage.test.tsx
  - web/stryker.conf.json
  - internal/webui/dist
autonomous: true
requirements: [MCPW-01, MCPW-02, MCPW-03, SKW-01, SKW-02, SKW-03]
must_haves:
  truths:
    - "A held-out log-scan + DOM-grep over a FULL run (MCP install + env-edit + skill install) finds zero secret values — for ALL env values, the /api/governance/* response and the DOM contain only KEY + redacted, never the value (the non-inferable prohibition backstop)"
    - "A property test proves: for any stored secret S and a submitted ${KEY}, after env-edit the stored value is exactly S (never the placeholder text) — for every secret key, not one example (the redacted-placeholder-preserved backstop)"
    - "(a) A CONCRETE static/callgraph check over internal/agent/tools/ (and any model-facing tool registry) proves COUNT==0 call sites of skills.Writer.Activate / ResumeHandler.Resume reachable from a model-tool path; AND (b) the active loader's List()/manifest excludes pending/ — a pending skill body never appears in any LLM-context-building call. (a) and (b) each pass independently (the no-model-approve / pending-non-injectable backstop)"
    - "Live-PG integration: UPDATE / DELETE / TRUNCATE against aura.mcp_audit AND aura.skill_audit each raise insufficient_privilege (append-only proven on both ledgers)"
    - "A mount-policy test proves a denied/destructive tool is absent from the runtime registry and explicitly surfaced when an allowlist exists (no silent destructive mount)"
    - "Go owned-surface coverage is >=85% across the full tag matrix (db_integration; unit + integration) via make coverage on the live stack"
    - "Vitest >=85% (statements/branches/functions/lines) on the touched web/src/governance + web/src/approvals dirs; Stryker >=70% killed on the new modules"
    - "Playwright e2e covers the full cockpit flow (install -> trust-approve -> mounted with tool count; env-edit preserving a secret; skill install -> approval queue -> resume-activate; restore/archive) + axe a11y on the new surfaces, on chromium + mobile-chrome"
    - "node web/scripts/contrast-check.mjs passes AA for every new pair; the embedded internal/webui/dist is rebuilt from web/ so the single-binary //go:embed reflects the new UI"
    - "Every new mutating endpoint returns 401 unauthenticated and 403 without governance.write (a final cross-surface auth sweep); all new copy has both en and it keys"
  artifacts:
    - path: "internal/agui/governance_write_secret_scan_test.go"
      provides: "The held-out log-scan + response/DOM secret-grep over a full install+edit run (zero secret values)"
    - path: "internal/agui/no_model_approve_test.go"
      provides: "The concrete static/callgraph check that no model-facing tool path reaches Writer.Activate / ResumeHandler.Resume (COUNT==0) + the active-loader-excludes-pending assertion (SPEC Prohibition #3 backstop)"
    - path: "web/e2e/governance-write.spec.ts"
      provides: "The full cockpit install->approve->mounted + env-edit + restore/archive Playwright flow + axe"
    - path: "internal/webui/dist"
      provides: "The rebuilt embedded SPA dist (single-binary //go:embed invariant)"
  key_links:
    - from: "internal/agui/governance_write_secret_scan_test.go"
      to: "the full MCPW + SKW run path"
      via: "captures logs + response bodies + (e2e) DOM and greps token/DSN/password patterns -> expects 0"
      pattern: "secret|token|password"
    - from: "internal/agui/no_model_approve_test.go"
      to: "internal/agent/tools/ (the model-facing tool registry) + internal/skills (Activate/Resume)"
      via: "go/packages (or golang.org/x/tools callgraph) loads internal/agent/tools/... and asserts zero call edges into skills.Writer.Activate / ResumeHandler.Resume"
      pattern: "Activate|Resume"
    - from: "web/e2e/governance-write.spec.ts"
      to: "the live cockpit + backend"
      via: "Playwright drives the real install/trust/env-edit/skill-install/restore flow"
      pattern: "governance"
  prohibitions:
    - statement: "MUST NOT render, return, or log any raw secret anywhere — proven by a held-out log-scan + response/DOM grep over a full install+edit run (zero secret values)"
      status: resolved
      verification: "test — the secret-scan test greps token/DSN/password patterns across captured logs + responses + DOM and asserts zero"
    - statement: "MUST NOT overwrite a stored secret submitted as its redacted placeholder — proven for every secret key by a property test (not a single example)"
      status: resolved
      verification: "test — property: for all S, submit ${KEY} -> stored value == S, never the placeholder"
    - statement: "MUST NOT let a pending skill run / be injected, nor a model approve — proven by (a) a concrete static/callgraph check that COUNT==0 model-tool paths reach Writer.Activate/ResumeHandler.Resume AND (b) the loader excludes pending/; both independently"
      status: resolved
      verification: "test — (a) go/packages callgraph over internal/agent/tools/ asserts zero edges into Activate/Resume; (b) a pending body never appears in any context-building call"
    - statement: "MUST NOT leave either audit ledger mutable — UPDATE/DELETE/TRUNCATE on mcp_audit AND skill_audit each raise insufficient_privilege"
      status: resolved
      verification: "test — live-PG integration on both ledgers"
    - statement: "MUST NOT silently mount a destructive/denied MCP tool when an allowlist exists — proven by the mount-policy test"
      status: resolved
      verification: "test — the denied tool is absent from the runtime registry and explicitly surfaced"
---

<objective>
Close the phase to Gate-3: prove the non-inferable prohibition backstops (the held-out secret
log-scan + DOM-grep, the redacted-placeholder property, the no-model-approve / pending-non-injectable
held-out check, the append-only invariant on BOTH ledgers, the no-silent-destructive-mount), run the
full quality matrix (Go >=85% owned-surface on the live stack; Vitest >=85% + Stryker >=70% on the
touched dirs; Playwright e2e + axe + contrast), and rebuild the embedded `internal/webui/dist` so the
single-binary `//go:embed` reflects the new UI.

This wave depends on all four prior plans (the backend + frontend must exist before the e2e + gates).

Purpose: the SPEC's 14 acceptance criteria + 20/20 edges + 8 prohibitions are all observed by an
automated signal; the owned-surface + frontend floors hold; the dist is fresh for handoff.
Output: the backstop tests, the Playwright flow + axe, the Stryker scope, and the rebuilt dist.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-01-SUMMARY.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-02-SUMMARY.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-03-SUMMARY.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The non-inferable prohibition backstops — secret log-scan + DOM-grep, redacted-placeholder property, no-model-approve (concrete static/callgraph) / pending-non-injectable, append-only on both ledgers, no-silent-destructive-mount</name>
  <files>internal/agui/governance_write_secret_scan_test.go, internal/agui/no_model_approve_test.go, internal/mcp/manager/audit_store_integration_test.go, internal/skills/installer_integration_test.go</files>
  <read_first>
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"Prohibition -> backstop signal (the non-inferable, property/held-out)" + §"Edge Coverage -> validation signal" (the exact backstop assertions to implement)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md §"Prohibitions (must-NOT)" (the 8 prohibitions, esp. Prohibition #3 "no model-facing approve") + §"Acceptance Criteria" (the 14 pass/fail criteria, esp. the log-scan + append-only ones)
    - internal/skills/writer_activate.go:24 (Writer.Activate — the ONLY method that promotes pending→active; the static check asserts no model-tool path reaches it) + internal/skills/resume.go:51 (ResumeHandler.Resume — calls Writer.Activate; the legitimate operator-resume path) + internal/skills/writer.go:139 (the ApprovalAuto in-Writer call — NOT a model-tool path; do not flag)
    - internal/agent/tools/ (the model-facing tool registry: skill.go, skill_write.go, skill_read.go, registry.go, manifest.go, action.go — the package set the callgraph/AST scan loads; assert ZERO call edges into skills.Writer.Activate / ResumeHandler.Resume from any of these)
    - internal/mcp/manager/audit.go + internal/mcp/manager/audit_store_integration_test.go (the plan-01 append-only test to extend to cover the full one-row-per-action set) + internal/skills/audit_store.go (the existing skill_audit append-only test to mirror for the both-ledgers assertion)
    - internal/skills/stage_reader.go (ListStage scans active/ only — the pending-non-injectable held-out; List()/manifest excludes pending/)
    - internal/mcp/manager/runtime.go (RunnableManagedServers + the mount-time risk policy — the denied-tool exclusion test) + internal/secret/envkey.go (IsSecretEnvKey — the secret-pattern set the scan greps)
  </read_first>
  <action>
    Create `internal/agui/governance_write_secret_scan_test.go`: drive a FULL run (an MCP recipe+custom
    install, an env-edit submitting a real secret then re-submitting the redacted ${KEY}, a skill install)
    against the httptest server with a captured slog handler; grep the captured logs + every
    `/api/governance/*` response body for the secret patterns (token/DSN/password/the seeded secret values)
    and assert ZERO matches. Add the redacted-placeholder PROPERTY test (table or gopter/rapid over many
    secret keys/values): for all S under key K, after `SetServerEnv` with `K=${K}` the stored value is
    exactly S, never `${K}`.
    Create `internal/agui/no_model_approve_test.go` — the SPEC-Prohibition-#3 backstop, proving BOTH parts
    INDEPENDENTLY (two separate test functions / sub-tests, each fails on its own):
    (a) `TestNoModelFacingActivatePath` — a CONCRETE static check (NOT a generic unit test): load the
        model-facing tool packages with `golang.org/x/tools/go/packages` (LoadAllSyntax over
        `./internal/agent/tools/...` and any other model-tool registry package), build a call graph
        (`golang.org/x/tools/go/callgraph/cha` or a syntax-level AST walk of every CallExpr resolving the
        selector via `types.Info`), and assert COUNT==0 call sites/edges whose callee is
        `(*skills.Writer).Activate` OR `(*skills.ResumeHandler).Resume` reachable from any function in those
        packages. The legitimate callers (`internal/skills/resume.go`, the `ApprovalAuto` path in
        `internal/skills/writer.go`, the CLI command, the operator HTTP resume handler) are OUTSIDE the
        scanned model-tool package set, so they do not count. (A simpler-but-still-concrete fallback if
        x/tools callgraph proves heavy: an AST scan of `internal/agent/tools/*.go` that resolves each
        `CallExpr` selector to its `types.Object` and asserts none is `Activate`/`Resume` on the skills
        types — but it MUST resolve via the type-checker, not a string grep, so it cannot be fooled by a
        local method of the same name.)
    (b) `TestActiveLoaderExcludesPending` — assert the active loader's `List()`/manifest scans `active/`
        only and excludes `pending/`: stage a skill to `pending/`, call the loader's List/manifest path,
        and assert the pending skill body never appears in the returned set nor in any context-building call.
    Extend `internal/mcp/manager/audit_store_integration_test.go` (db_integration) to assert UPDATE/DELETE/
    TRUNCATE on `aura.mcp_audit` each raise insufficient_privilege AND that each of
    install/edit/enable/disable/remove/trust appends EXACTLY ONE row (and a double-remove leaves at most
    one). Add `internal/skills/installer_integration_test.go` (db_integration) asserting the skill install/
    restore/archive each append one `skill_audit` row newest-first AND that UPDATE/DELETE/TRUNCATE on
    `aura.skill_audit` raise insufficient_privilege (the both-ledgers append-only proof). Add the
    no-silent-destructive-mount unit test: with an allowlist present, a denied/destructive advertised tool
    is absent from the runtime registry and explicitly surfaced (Phase-16 mount policy unchanged).
  </action>
  <acceptance_criteria>
    - `go test ./internal/agui/ -run TestGovernanceWriteSecretScan` exits 0 — the captured logs + responses contain zero secret values over a full install+edit+skill-install run
    - `go test ./internal/mcp/manager/ -run TestSetServerEnvPreservesAllSecrets` exits 0 — the property holds for every secret key in the table
    - `go test ./internal/agui/ -run TestNoModelFacingActivatePath` exits 0 — the type-resolved callgraph/AST scan over internal/agent/tools/... finds ZERO call sites of skills.Writer.Activate / ResumeHandler.Resume (passes independently of part b)
    - `go test ./internal/agui/ -run TestActiveLoaderExcludesPending` exits 0 — the active loader List()/manifest excludes pending/ (passes independently of part a)
    - `go test -tags db_integration ./internal/mcp/manager/ ./internal/skills/ -run 'TestMCPAuditAppendOnly|TestSkillAuditAppendOnly|TestAuditOnePerAction'` exits 0 — both ledgers reject UPDATE/DELETE/TRUNCATE; one row per action
    - `go test ./internal/mcp/... -run TestDeniedToolNotMounted` exits 0 — a denied tool is excluded + surfaced
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/agui/ -run 'TestGovernanceWriteSecretScan|TestNoModelFacingActivatePath|TestActiveLoaderExcludesPending' && go test ./internal/mcp/manager/ -run 'TestSetServerEnvPreservesAllSecrets|TestDeniedToolNotMounted'</automated>
  </verify>
  <done>Every non-inferable prohibition has a held-out/property backstop: the secret scan finds zero values over a full run; the redacted-placeholder property holds for all keys; the no-model-approve check is a CONCRETE type-resolved callgraph/AST scan asserting COUNT==0 model-tool paths into Writer.Activate/ResumeHandler.Resume (part a) AND the loader excludes pending/ (part b), each passing independently; both audit ledgers are append-only; no denied tool mounts silently.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend gates — Playwright e2e (full cockpit flow) + axe + Vitest >=85% + Stryker >=70% + contrast AA</name>
  <files>web/e2e/governance-write.spec.ts, web/src/governance/__tests__/governanceWrite.coverage.test.tsx, web/stryker.conf.json</files>
  <read_first>
    - web/e2e/ (the existing Playwright specs — the harness/config, the chromium + mobile-chrome projects, the auth/login helper, the BoardLayout selectors to drive)
    - web/stryker.conf.json (the existing Stryker scope — add the new web/src/governance/* + web/src/approvals/* modules to the mutate glob; the >=70% killed floor)
    - web/scripts/contrast-check.mjs (confirm the plan-04 pairs are present and AA-passing)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md §"Acceptance Criteria" (the cockpit-completable flows the e2e must cover end-to-end: install->trust->mounted+tool-count; env-edit preserve-secret; skill install->approval->resume-activate; restore/archive) + §"Edge Coverage" (the empty-state + four-state + colliding-restore + expired-token edges the e2e/Vitest assert)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"Sampling Rate" + §"Wave 0 Gaps" (the test files to add) + the frontend gate thresholds (Vitest >=85%, Stryker >=70%)
    - MEMORY directive: frontend quality gates coverage >85% + mutation >70% (feedback_frontend_quality_gates_coverage_mutation)
  </read_first>
  <action>
    Create `web/e2e/governance-write.spec.ts` driving the FULL cockpit flow on chromium + mobile-chrome
    (reuse the existing auth/login helper): (1) MCP recipe + custom install -> assert the CLI-equiv +
    `Will write to:` destination preview, then trust-approve -> assert the row shows mounted with a live tool
    count; (2) env-edit -> submit a secret, re-open and submit the redacted placeholder -> assert the secret
    persists (and DOM-grep asserts no value is in the DOM); (3) the four env states render distinctly + a
    placeholder-required save shows the soft warning but succeeds; (4) skill install (source field) -> assert
    the RISKY badge + five checklist items + container note + `Stage for approval`, then resolve via the
    approval queue -> assert it activates (and an expired-token path shows the terminal chip); (5)
    restore/archive across tabs, with a colliding restore showing the safe inline error; (6) the remove
    confirmation dialog (action-specific labels, not default-focused). Run axe on each new surface. Add the
    Vitest coverage test(s) needed to lift the touched dirs to >=85% (state coverage: empty/loading/error/
    error-auth/submitting/success/field-error). Extend `web/stryker.conf.json` to include the new
    governance + approvals modules in the mutate scope and confirm >=70% killed. Confirm
    `node web/scripts/contrast-check.mjs` passes AA with the plan-04 pairs.
  </action>
  <acceptance_criteria>
    - `cd web && npm run e2e` exits 0 on chromium + mobile-chrome — the full install->trust->mounted + env-edit-preserve-secret + skill-install->approval->resume + restore/archive flow passes, axe finds no violations on the new surfaces
    - `cd web && npm run test:coverage` reports >=85% statements/branches/functions/lines on web/src/governance + web/src/approvals
    - `cd web && npm run test:mutation` (Stryker) reports >=70% killed on the new governance + approvals modules
    - `node web/scripts/contrast-check.mjs` exits 0 (all pairs AA-passing)
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run test:coverage && node scripts/contrast-check.mjs</automated>
  </verify>
  <done>The Playwright e2e covers the full cockpit write flow + axe on chromium + mobile-chrome; Vitest is >=85% and Stryker >=70% on the touched dirs; contrast passes AA.</done>
</task>

<task type="auto">
  <name>Task 3: Full-matrix Go coverage gate + cross-surface auth sweep + i18n parity + dist rebuild</name>
  <files>internal/webui/dist</files>
  <read_first>
    - Makefile (the `make coverage` target -> scripts/coverage_gate.sh — the owned-surface >=85% floor across the full tag matrix; the live-stack requirement; `make quality-full`)
    - scripts/coverage_gate.sh (the owned-surface definition — internal/* minus generated sqlc + pre-rewrite skeletons + test-support; the new internal/mcp/manager + internal/agui + internal/skills code must clear 85%)
    - cmd/aura/serve_webui.go (confirm every new MCP + skills write route is mounted behind RequireCapability(governance.write) — the cross-surface auth sweep) + internal/agui/auth_test.go (the 401/403 assertion shape to extend across all new routes)
    - web/ build (the `npm run build` -> dist pipeline that feeds internal/webui/dist via //go:embed; the dist-freshness gate)
    - CLAUDE.md §"Quality tooling & gates" (the NO-SKIP-AS-GREEN + the >=85% floor + the full integration matrix on the live stack; WSL is the primary env for make quality-full)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"Runtime State Inventory" (rebuild internal/webui/dist before close — the single-binary invariant) + §"Validation Architecture" §"Phase gate"
  </read_first>
  <action>
    Run the full Go quality matrix on the live stack (WSL primary): `make coverage` (>=85% owned-surface
    across `db_integration`; bring the stack up via `make neo4j-migrate` first) — if any new package
    (`internal/mcp/manager`, `internal/agui`, `internal/skills`) is below 85%, add the targeted tests to
    lift it (do NOT lower the floor). Run `go test -tags db_integration -race ./...` clean. Do the
    cross-surface AUTH sweep: a table test asserting EVERY new mutating endpoint (the six MCP + the seven
    skills write routes) returns 401 unauthenticated and 403 without governance.write (mirror
    auth_test.go:494). Confirm the i18n parity (zero keys missing in either en or it across the new bundle).
    Rebuild the embedded SPA: `cd web && npm run build` then ensure `internal/webui/dist` is updated (the
    `//go:embed` source) so the single binary serves the new UI; confirm the dist-freshness gate is green.
    Run the final `make quality-full` (vet + build + file-size <=600 LOC + lint(+dupl) + test-race + vuln +
    the coverage gate) and confirm green.
  </action>
  <acceptance_criteria>
    - `make coverage` exits 0 with owned-surface >=85% across the full tag matrix on the live stack (the new internal/mcp/manager + internal/agui + internal/skills code clears 85%)
    - `go test -tags db_integration -race ./...` exits 0 (no skip-as-green: the integration tiers actually run under the composed DSNs)
    - the cross-surface auth sweep asserts 401/403 on all 13 new mutating routes (six MCP + seven skills)
    - `internal/webui/dist` is rebuilt from web/ (the dist-freshness gate is green); no source file added this phase exceeds 600 LOC
    - `make quality-full` exits 0
  </acceptance_criteria>
  <verify>
    <automated>make coverage && go build ./...</automated>
  </verify>
  <done>The Go owned-surface coverage is >=85% across the full tag matrix on the live stack; the integration tiers actually run (no skip-as-green); every new mutating route is 401/403-gated; en+it parity holds; internal/webui/dist is rebuilt; make quality-full is green; no file exceeds 600 LOC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the full MCPW+SKW run -> logs/responses/DOM | the boundary the secret-scan held-out proves no secret crosses |
| internal/agent/tools/ (model-tool packages) -> skills.Writer.Activate/Resume | the call boundary the callgraph/AST scan proves no model path crosses (SPEC Prohibition #3) |
| every new mutating route -> the auth gate | the cross-surface sweep confirms 401/403 on all 13 routes |
| web/ build -> internal/webui/dist | the single-binary //go:embed boundary the dist rebuild keeps fresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Information Disclosure | a secret leaking anywhere (response/DOM/log) | mitigate | the held-out log-scan + response/DOM grep over a full run asserts zero secret values (the non-inferable backstop) |
| T-29-05-02 | Tampering | a regression making an audit ledger mutable | mitigate | live-PG append-only integration on BOTH mcp_audit + skill_audit (UPDATE/DELETE/TRUNCATE -> insufficient_privilege) |
| T-29-05-03 | Elevation of Privilege | a model-tool path gains a skill-activation/approve edge | mitigate | the concrete type-resolved callgraph/AST scan over internal/agent/tools/... asserts COUNT==0 edges into Writer.Activate/ResumeHandler.Resume (SPEC Prohibition #3 backstop) |
| T-29-05-04 | Elevation of Privilege | an un-gated mutating route slips through | mitigate | the cross-surface auth sweep asserts 401/403 on all 13 new routes (no route reachable without governance.write) |
| T-29-05-05 | Tampering | a stale dist serving the old UI / a skipped integration tier | mitigate | the dist-freshness gate + NO-SKIP-AS-GREEN (the integration tiers t.Fatal under $CI when their env is unset; make coverage runs them live) |
| T-29-05-SC | Tampering | npm/pip/cargo installs | mitigate | NO new installs in this plan (test + gate + dist rebuild only); the `skills` npx CLI was audited in plan 03 (slopcheck clean, no postinstall) |

*Canon-referred to `/gsd-secure-phase`:* the SSRF (MCP probe + skill-source fetch) + skill name/path
traversal mitigations are retro-verified by `/gsd-secure-phase` after this phase; this plan proves the
phase-owned backstops (secret no-leak, append-only, no-model-approve, no-silent-destructive-mount).
</threat_model>

<verification>
- `make coverage` >=85% owned-surface across the full tag matrix on the live stack; `make quality-full` green.
- `go test -tags db_integration -race ./...` clean (no skip-as-green).
- `go test ./internal/agui/ -run 'TestGovernanceWriteSecretScan|TestNoModelFacingActivatePath|TestActiveLoaderExcludesPending|TestGovernanceWriteAuthSweep'` green.
- `go test -tags db_integration ./internal/mcp/manager/ ./internal/skills/ -run 'AppendOnly|OnePerAction'` green (both ledgers).
- `cd web && npm run test:coverage` >=85% + `npm run test:mutation` >=70% + `npm run e2e` (chromium + mobile-chrome) + `node scripts/contrast-check.mjs` all green.
- `internal/webui/dist` rebuilt; the dist-freshness gate green.
</verification>

<success_criteria>
- All 8 SPEC prohibitions observed by an automated signal (the held-out backstops + the concrete no-model-approve callgraph/AST scan + the append-only + no-silent-mount tests).
- All 14 SPEC acceptance criteria + 20/20 edges have a green automated signal (Go + Vitest + Playwright).
- Go owned-surface >=85% across the full tag matrix on the live stack; Vitest >=85% + Stryker >=70% on the touched dirs; Playwright e2e + axe + contrast AA green.
- internal/webui/dist rebuilt (single-binary //go:embed fresh); no file >600 LOC; make quality-full green; en+it parity holds.
</success_criteria>

<output>
Create `.planning/phases/29-governance-write-mcp-configuration-skills-install/29-05-SUMMARY.md` when done.
</output>
</content>
