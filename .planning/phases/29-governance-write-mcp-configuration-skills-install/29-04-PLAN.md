---
phase: 29-governance-write-mcp-configuration-skills-install
plan: 04
type: execute
wave: 4
depends_on: ["29-02", "29-03"]
files_modified:
  - web/src/governance/governanceApi.ts
  - web/src/governance/McpInstallPanel.tsx
  - web/src/governance/McpEnvEditForm.tsx
  - web/src/governance/McpServerDetail.tsx
  - web/src/governance/McpBoard.tsx
  - web/src/governance/SkillInstallPanel.tsx
  - web/src/governance/SkillsBoard.tsx
  - web/src/approvals/InlineApprovalCard.tsx
  - web/src/i18n/resources.governance.ts
  - web/scripts/contrast-check.mjs
  - web/src/governance/__tests__/McpEnvEditForm.test.tsx
  - web/src/governance/__tests__/McpInstallPanel.test.tsx
  - web/src/governance/__tests__/SkillInstallPanel.test.tsx
autonomous: true
requirements: [MCPW-01, MCPW-02, MCPW-03, SKW-01, SKW-02, SKW-03]
must_haves:
  truths:
    - "The Phase-28 governance MCP/Skills read boards are EXTENDED in place (D-10) with write controls — one governance workspace, no new design system/workspace/transport/approval surface"
    - "The MCP install panel (detail-pane slot) offers a Recipe|Custom 2-segment toggle; Recipe renders BuiltInCatalog RequiredEnv as a guided form; a live preview shows the CLI-equivalent + the `Will write to:` ManagedConfigPath destination before save; a duplicate name shows an inline aria-invalid error and Install stays disabled"
    - "The MCP env-edit form renders the FOUR env states distinctly (required/optional/missing/placeholder — dot + label, never color-alone); a stored secret shows a redacted chip in read mode and the redacted ${KEY} placeholder in edit mode with NO eye-reveal (the value is never sent); leaving it untouched preserves the secret"
    - "A still-placeholder required env var shows a soft-warning card (border-warning bg-warning/10 + bulleted offending keys) above submit; Save changes is still allowed (informational, not a blocker)"
    - "MCP lifecycle controls are an inline cluster (NOT a kebab): enable/disable toggle (success/muted dot + label, idempotent), Trust & approve inline form requiring a reason, Remove behind a confirmation dialog with action-specific labels (Remove server / Keep server, destructive button not default-focused), a denied/destructive tool shown explicitly with a danger marker, a fail-soft per-row probe warning"
    - "The skill install panel (RISKY-honest) offers a source field OR a skills.sh search behind the External discovery toggle (reflecting AURA_SKILLS_EXTERNAL_DISCOVERY, off by default); it surfaces source/content-hash/preview/destination + a RISKY badge + the FIVE checklist items (NO --ignore-scripts) + the container-isolation note; submit is `Stage for approval` (never Install/Activate)"
    - "The RISKY/DESTRUCTIVE skill action is rendered by the EXISTING /api/approvals InlineApprovalCard (extended with source/hash/preview/risk/resume-token above the existing verbs) — no second badge/queue; pending carries no run/activate affordance; an expired/consumed token renders a warning TerminalChip"
    - "The SkillsBoard four tabs gain in-place controls: active->Archive skill, archived->Restore skill (collision-reject inline error), pending->no control, audit->no mutate; per-row metadata shows capability scope/last used/use count/TTL-archive/risk/hash; audit newest-first"
    - "Every new UI string has BOTH en and it i18n keys in resources.governance.ts; every new fg/bg pair is added to web/scripts/contrast-check.mjs and passes WCAG AA"
    - "Every write surface renders all states: empty/loading/populated/error/error-auth (BoardStateView) + submitting/success/field-validation-error; a forced backend failure renders a sanitized error; no secret value is ever in the DOM on any state"
  artifacts:
    - path: "web/src/governance/governanceApi.ts"
      provides: "postJSON/patchJSON/deleteJSON (same-origin, retry:false, encodeURIComponent) + the write DTOs (install/env-edit/trust/skill-install requests, catalog-search response)"
    - path: "web/src/governance/McpEnvEditForm.tsx"
      provides: "four-state env editor + redacted-secret-chip (no eye-reveal) + soft-warning card"
    - path: "web/src/governance/McpInstallPanel.tsx"
      provides: "Recipe|Custom install form + CLI-equiv + destination preview + duplicate-name guard"
    - path: "web/src/governance/SkillInstallPanel.tsx"
      provides: "RISKY-honest install panel (source/catalog toggle, hash/preview, five-item checklist, container note, Stage for approval)"
  key_links:
    - from: "web/src/governance/governanceApi.ts"
      to: "the Phase-29 backend (POST/PATCH/DELETE /api/governance/mcp|skills/*)"
      via: "TanStack useMutation + invalidateQueries over the new write fns"
      pattern: "postJSON|patchJSON|deleteJSON"
    - from: "web/src/approvals/InlineApprovalCard.tsx"
      to: "the existing /api/approvals queue + ApprovalBadge"
      via: "the skill-install entry extends the card; no second badge/queue (D-11)"
      pattern: "InlineApprovalCard"
    - from: "web/src/governance/McpEnvEditForm.tsx"
      to: "the McpEnvChip key-only DTO (no value)"
      via: "the edit form holds the redacted ${KEY} placeholder, never the value"
      pattern: "redacted"
  prohibitions:
    - statement: "MUST NOT render any raw secret (MCP env value) in the DOM on any state (read/edit/error/preview) — only redacted chips / key-only rows"
      status: resolved
      verification: "test — Vitest asserts no env-value appears in the rendered DOM; Playwright DOM scan in plan 05"
    - statement: "MUST NOT present a skill install as 'safe' — the panel always renders the RISKY badge + the five-item checklist + the container-isolation note; submit is Stage for approval, never Install/Activate"
      status: resolved
      verification: "judgment + test — the panel renders the RISKY badge and exactly five checklist items; the submit label is `Stage for approval`"
    - statement: "MUST NOT offer a run/activate affordance on a pending skill or mint a second approval badge/queue — activation is the resume only (D-11)"
      status: resolved
      verification: "test — the approval card has no run/activate button; the existing ApprovalBadge/queue is reused (no second one)"
    - statement: "MUST NOT enable external skills.sh discovery without the explicit operator-visible toggle reflecting AURA_SKILLS_EXTERNAL_DISCOVERY"
      status: resolved
      verification: "test — the search box is disabled with a note when the toggle is off"
    - statement: "MUST NOT use a bare Cancel/Submit/OK label, nor a kebab menu for lifecycle, nor type-to-confirm for Remove — action-specific verb+noun labels, an inline control cluster, a single confirm dialog"
      status: resolved
      verification: "judgment — every secondary action carries an action-specific label; Remove is a single confirm with Remove server/Keep server"
---

<objective>
Extend the Phase-28 governance MCP/Skills read boards IN PLACE (D-10) with the write controls,
held to the LOCKED blue design system + the approved 29-UI-SPEC layouts. One `governance`
workspace — no new design system, workspace, transport, or approval surface. Reuse `BoardLayout`,
`BoardStateView`/`boardStatus`, the `McpServerDetail`/`SkillDetail` `<dl>` idiom, the Phase-25
`InlineApprovalCard`/`ApprovalList`/`ApprovalBadge`, and the existing token + i18n + contrast
pipelines.

Purpose: deliver the operator-facing write UI for MCPW-01/02/03 + SKW-01/02/03 — the install
panels, the four-state redacted env editor, the trust/lifecycle cluster, the RISKY-honest skill
install panel, the reused approval card, and the lifecycle tabs — with en+it copy and AA contrast.
Output: the new `web/src/governance/*` panels + the extended boards/approval card + `governanceApi.ts`
write fns + `resources.governance.ts` keys + new contrast pairs.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-02-SUMMARY.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: governanceApi write layer (postJSON/patchJSON/deleteJSON + DTOs) + the MCP install panel + four-state env-edit form</name>
  <files>web/src/governance/governanceApi.ts, web/src/governance/McpInstallPanel.tsx, web/src/governance/McpEnvEditForm.tsx, web/src/governance/McpServerDetail.tsx, web/src/governance/McpBoard.tsx, web/src/governance/__tests__/McpEnvEditForm.test.tsx, web/src/governance/__tests__/McpInstallPanel.test.tsx</files>
  <read_first>
    - web/src/governance/governanceApi.ts (getJSON:1-12 + the six read fns + the explicit comment "all writes are Phase 29 — there is no postJSON here"; the McpEnvChip/McpServerRow/McpProbeResult/SkillRow DTOs to extend with write request/response shapes)
    - web/src/governance/McpServerDetail.tsx:48-87 (the read-only env-keys `<section>` the env-edit form REPLACES in edit mode; the `<dl>`/Field idiom; the redacted-chip render at :71-82)
    - web/src/governance/McpBoard.tsx (the per-row probe isolation to keep; where the `+ Add MCP server` CTA + the inline lifecycle cluster attach)
    - web/src/governance/governanceView.tsx (BoardStateView/boardStatus — the five-state machine to reuse) + web/src/governance/BoardLayout.tsx (master+detail desktop / bottom-sheet mobile slot)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md §1 (install panel: Recipe|Custom, RequiredEnv guided form, `Will write to:` preview, duplicate-name aria-invalid) + §2 (four env states table + secret masking minus eye-reveal + soft-warning card) + §Copywriting Contract (the exact en/it labels) + §Color/§Typography/§Spacing (the locked tokens)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-02-SUMMARY.md (the actual install/env response shapes the DTOs mirror)
  </read_first>
  <action>
    Extend `governanceApi.ts`: add `postJSON`/`patchJSON`/`deleteJSON` mirroring `getJSON` exactly
    (`credentials:'same-origin'`, `Accept: application/json`, `retry:false`, a non-200-incl-401 THROWS
    `Error("HTTP <n>")`, `encodeURIComponent` on every `{name}` path segment) + the write DTOs (install
    request: recipe-name|custom command/args/env; env-edit request: name + the submitted env rows; trust
    request: name + reason; the install/env responses carrying key-only chips + the CLI-equiv + destination
    + soft-warning list). Create `McpInstallPanel.tsx` in the BoardLayout detail slot: a 2-segment
    Recipe|Custom control (Recipe default); Recipe -> a dropdown of `BuiltInCatalog` recipes whose
    `RequiredEnv` renders as a guided form (secret-typed vars use the masked field of the env form); Custom
    -> command (mono) + repeatable args (mono) + env key+value rows; a live read-only PREVIEW block showing
    the CLI-equivalent (`aura mcp install …` / `aura mcp add … -- …`, font-mono on surface-3) + the
    `Will write to:` ManagedConfigPath destination (font-mono); submit `Install server` (accent CTA, 44px);
    a duplicate name -> inline `aria-invalid` field error (text-danger) + Install disabled, no request fires;
    abandon -> `Discard install` (secondary). Create `McpEnvEditForm.tsx` replacing the McpServerDetail env
    `<section>` in edit mode: render the FOUR states (required/optional/missing/placeholder — dot + label,
    never color-alone); a stored secret shows a redacted chip in read mode and the redacted `${KEY}`
    placeholder in edit mode with NO eye-reveal (the value is never sent — leave-untouched = preserved);
    non-secret vars are plain mono inputs; a still-placeholder required var -> a soft-warning card
    (`border-warning bg-warning/10` + bulleted offending keys) above submit, Save still allowed; a brief
    `Unchanged secrets were preserved.` affirmation on save; submit `Save changes` / abandon `Discard
    changes`. Wire both via TanStack `useMutation` + `invalidateQueries`. Write the Vitest tests
    (McpEnvEditForm: four-state render, redacted-placeholder-no-value-in-DOM, soft-warning-save-allowed;
    McpInstallPanel: preview text, duplicate-name disabled).
  </action>
  <acceptance_criteria>
    - `web/src/governance/governanceApi.ts` exports `postJSON`, `patchJSON`, `deleteJSON` (the "there is no postJSON here" comment is replaced); each uses `credentials:'same-origin'` and `encodeURIComponent` on `{name}`
    - `McpEnvEditForm.test.tsx` asserts the four state chips render distinctly AND that no env VALUE appears in the rendered DOM (only key + redacted) AND that a placeholder-required save still succeeds with the soft warning shown
    - `McpInstallPanel.test.tsx` asserts the live preview shows the CLI-equivalent + `Will write to:` destination, and a duplicate name disables Install with an `aria-invalid` error
    - `cd web && npm run test -- governance` exits 0; `cd web && npm run lint` is zero-warning
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q 'postJSON' src/governance/governanceApi.ts && npm run test -- governance</automated>
  </verify>
  <done>The governanceApi write layer + the MCP install panel (CLI-equiv + destination preview + duplicate guard) + the four-state redacted env-edit form (no value in DOM, soft-warning save-allowed) exist and pass Vitest + lint; the read boards are extended in place.</done>
</task>

<task type="auto">
  <name>Task 2: MCP lifecycle cluster (trust/enable/disable/remove + denied-tool) + the RISKY-honest skill install panel + the SkillsBoard tab controls</name>
  <files>web/src/governance/McpServerDetail.tsx, web/src/governance/McpBoard.tsx, web/src/governance/SkillInstallPanel.tsx, web/src/governance/SkillsBoard.tsx, web/src/governance/__tests__/SkillInstallPanel.test.tsx</files>
  <read_first>
    - web/src/governance/SkillsBoard.tsx (the four-tab role="tablist" active/pending/archived/audit; where the `+ Install skill` CTA + the per-tab Archive/Restore controls attach; the AuditList newest-first to keep)
    - web/src/governance/McpServerDetail.tsx (the detail header where the inline lifecycle cluster attaches; the tool list where the denied-tool danger marker renders)
    - web/src/approvals/InlineApprovalCard.tsx (the Phase-25 card — read in task 3; here just confirm the SkillsBoard does NOT mint a second approval surface)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md §3 (lifecycle cluster: enable/disable toggle, Trust & approve inline reason form, Remove confirm dialog action-specific labels not-default-focused, denied-tool danger marker, fail-soft probe warning) + §4 (skill install panel: source/catalog toggle reflecting AURA_SKILLS_EXTERNAL_DISCOVERY, source/hash/preview/destination, RISKY badge, FIVE checklist items, container-isolation note, `Stage for approval`) + §6 (lifecycle tabs: active->Archive, archived->Restore collision-reject, pending no control, audit no mutate, per-row metadata) + §Copywriting Contract (exact en/it labels incl. the Remove dialog + container note + risk banner)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-03-SUMMARY.md (the install InstallInfo response shape + the catalog/restore-409 contract)
  </read_first>
  <action>
    Extend `McpServerDetail.tsx`/`McpBoard.tsx` with the inline lifecycle cluster (NOT a kebab): an
    enable/disable toggle (`Enable server`/`Disable server`; enabled = success dot + `Enabled` label,
    disabled = muted dot + `Disabled`; idempotent); a `Trust & approve` inline action opening a small form
    requiring a reason then confirming (on success shows the read-back `Approved by … · {ts} · "{reason}"`);
    a `Remove server` destructive confirmation dialog restating the name with action-specific buttons
    (`Remove server`/`Keep server`, the destructive button text-danger outline, NOT default-focused,
    Escape-dismissable, focus-trapped); the tool list renders a denied/destructive tool explicitly with a
    `danger` marker (`Excluded — destructive/denied`) and a fail-soft per-row probe warning (text-warning,
    this-row-only). Create `SkillInstallPanel.tsx` in the Skills detail slot: a source field (owner/repo,
    URL, path — mono) OR a skills.sh search box behind the `External discovery (skills.sh)` toggle (OFF by
    default, reflecting AURA_SKILLS_EXTERNAL_DISCOVERY; disabled with a note when off); a pre-activation
    `<dl>` of Source/Content-hash/Preview/Destination + a prominent RISKY badge (bg-warning/15 + text-warning
    + dot — install ALWAYS RISKY, never "safe") + the FIVE checklist items (each pass text-success ✓ / fail
    text-danger ✗ + matched position) + a neutral container-isolation note; submit `Stage for approval`
    (accent CTA — NOT Install/Activate); abandon `Discard install`; an empty/invalid source -> safe inline
    error. Extend `SkillsBoard.tsx`: active rows -> `Archive skill` (neutral, reversible, no confirm);
    archived rows -> `Restore skill` (a colliding restore -> safe inline error from the 409); pending rows
    keep NO run/activate control; audit stays no-mutate; surface the per-row metadata (capability scope /
    last used / use count / TTL-archive / risk / content hash, mono for hash/counts); keep the audit tab
    newest-first. Wire writes via TanStack `useMutation` + `invalidateQueries`. Write
    `SkillInstallPanel.test.tsx` (RISKY badge present, exactly five checklist items, `Stage for approval`
    label, search disabled when the toggle is off, empty-source safe error).
  </action>
  <acceptance_criteria>
    - `SkillInstallPanel.test.tsx` asserts the panel renders the RISKY badge, EXACTLY five checklist items (no `--ignore-scripts`), a `Stage for approval` submit (never Install/Activate), and a disabled search box when the External-discovery toggle is off
    - the Remove control opens a confirmation dialog with `Remove server`/`Keep server` labels (no bare Cancel/OK), the destructive button not default-focused
    - the SkillsBoard active tab shows `Archive skill`, the archived tab shows `Restore skill`, pending shows no run/activate control, audit shows no mutate control
    - a denied/destructive MCP tool renders an explicit danger marker and is shown as not-mounted
    - `cd web && npm run test -- governance` exits 0; `cd web && npm run lint` is zero-warning
  </acceptance_criteria>
  <verify>
    <automated>cd web && grep -q 'Stage for approval' src/i18n/resources.governance.ts && npm run test -- governance</automated>
  </verify>
  <done>The MCP lifecycle cluster (trust/enable/disable/remove + denied-tool + fail-soft warning) + the RISKY-honest skill install panel (five-item checklist, container note, Stage for approval, flag-gated search) + the SkillsBoard tab controls (archive/restore-409, per-row metadata) exist and pass Vitest + lint.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the approval card for skill installs + en/it i18n keys + new contrast pairs</name>
  <files>web/src/approvals/InlineApprovalCard.tsx, web/src/i18n/resources.governance.ts, web/scripts/contrast-check.mjs</files>
  <read_first>
    - web/src/approvals/InlineApprovalCard.tsx:24-40 (the Phase-25 HITL card + its terminal-state render + the ariaInvalid helper + the SECURITY note T-25-20 "always React-escaped text, never dangerouslySetInnerHTML"; the existing three verbs Answer/Decline/Cancel; the ApprovalBadge it feeds)
    - web/src/i18n/resources.governance.ts (the 201-line governanceEn/governanceIt bundle — add EVERY new key to BOTH; the existing key idiom)
    - web/scripts/contrast-check.mjs (the WCAG-AA pair list — add the new fg/bg pairs: text-warning on surface-2, text-danger on surface-2, on-accent on accent, etc.)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md §5 (approval-queue entry: source·hash·preview·risk·resume-token above the existing verbs; NO run/activate; expired/consumed -> warning TerminalChip) + §Copywriting Contract (the FULL en/it table — every label/empty-state/error/warning/note) + §Color (the contrast-gate note + the shipped passing pairs)
  </read_first>
  <action>
    Extend `InlineApprovalCard.tsx` to render the skill-install entry: above the EXISTING three verbs,
    show source · content-hash · preview-excerpt · RISK TIER · resume-token (mono for the data fields, all
    React-escaped text nodes — never dangerouslySetInnerHTML, T-25-20); carry NO run/activate affordance
    (activation is the resume only — prohibition #3/#4); reuse the existing `TerminalChip` for an
    expired/already-consumed resume token (a warning chip with `approval.terminal.expired` copy, verbs gone)
    — never a silent activation or disappearance. Do NOT mint a second badge or queue (the existing
    ApprovalBadge already counts it — D-11). Add EVERY new UI string to BOTH `governanceEn` and
    `governanceIt` in `resources.governance.ts` per the UI-SPEC Copywriting Contract (the primary CTAs,
    secondary CTAs, empty states — including the REPLACED MCP/skills empty-state copy that now points to the
    write path, the duplicate-name/empty-source/colliding-restore errors, the soft-warning heading+body, the
    secret-preserved affirmation, the RISKY banner, the container-isolation note, the denied-tool marker,
    the fail-soft probe warning, the External-discovery toggle label+off-note, the Remove confirmation
    dialog). Add the new fg/bg pairs to `web/scripts/contrast-check.mjs` (text-warning on surface-2,
    text-danger on surface-2, on-accent on accent for the primary CTA, and any other new pair) and confirm
    they pass AA.
  </action>
  <acceptance_criteria>
    - `InlineApprovalCard.tsx` renders the skill-install metadata (source/hash/preview/risk/resume-token) above the existing verbs, with NO run/activate button, and a warning TerminalChip for an expired/consumed token
    - every new key present in `governanceEn` is also present in `governanceIt` (an i18n-parity test or a key-diff over the bundle returns zero missing keys in either locale)
    - `node web/scripts/contrast-check.mjs` exits 0 with the new pairs included (all AA-passing)
    - `cd web && npm run test` and `cd web && npm run lint` (zero-warning) pass
  </acceptance_criteria>
  <verify>
    <automated>cd web && node scripts/contrast-check.mjs && npm run lint</automated>
  </verify>
  <done>The approval card surfaces the skill-install metadata above the existing verbs (no run/activate, reused badge/queue, warning terminal chip); every new string has en+it keys; the new contrast pairs pass WCAG AA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> React write controls | the UI gathers operator input; the backend (plans 02/03) is the authority for validation + the no-leak belt |
| backend env DTO -> DOM | the boundary a raw secret must NEVER cross (key-only chips; the edit form holds the redacted placeholder) |
| backend strings -> React render | untrusted backend text is always React-escaped (no dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Information Disclosure | secret value in the DOM | mitigate | the edit form holds the redacted ${KEY} placeholder, never the value (no eye-reveal); read mode shows a redacted chip; Vitest asserts no value in the DOM; Playwright DOM scan in plan 05 |
| T-29-04-02 | Tampering | XSS via a backend string (skill preview / server name / error) | mitigate | all backend strings rendered as React-escaped text nodes, never dangerouslySetInnerHTML (T-25-20 carried from InlineApprovalCard) |
| T-29-04-03 | Elevation of Privilege | a run/activate affordance on a pending skill | mitigate | the approval card carries NO run/activate button; activation is the resume only; the reused ApprovalBadge/queue (no second one) — D-11 |
| T-29-04-04 | Spoofing | silent external discovery | mitigate | the External-discovery toggle reflects AURA_SKILLS_EXTERNAL_DISCOVERY and is OFF by default; the search box is disabled with a note when off |
| T-29-04-05 | Tampering | accidental destructive remove | mitigate | a single confirmation dialog with action-specific labels (Remove server/Keep server), the destructive button not default-focused (NN/g); no type-to-confirm (recoverable by re-install) |
| T-29-04-SC | Tampering | npm installs | mitigate | NO new npm deps (hand-rolled components over the locked token system; the elysia references are read-only inspiration, NOT imported); no component registry — UI-SPEC §Registry Safety confirms zero third-party blocks |

*Canon-referred to `/gsd-secure-phase`:* none new at the UI layer beyond the no-leak belt (the SSRF /
traversal canon lives in the backend plans 02/03 + `/gsd-secure-phase`).
</threat_model>

<verification>
- `cd web && npm run lint` zero-warning + `tsc --noEmit` clean.
- `cd web && npm run test -- governance` green (the new panel/form/board tests).
- `cd web && npm run test` green (the approval card extension + i18n parity).
- `node web/scripts/contrast-check.mjs` exits 0 with the new fg/bg pairs (AA-passing).
- An i18n key-diff confirms zero keys missing in either en or it.
- (Coverage + Stryker + Playwright e2e + axe land in plan 05.)
</verification>

<success_criteria>
- The Phase-28 boards are extended in place (one governance workspace) with: the MCP install panel (CLI-equiv + destination preview + duplicate guard), the four-state redacted env editor (no value in DOM, soft-warning save-allowed), the trust/enable/disable/remove cluster + denied-tool marker + fail-soft warning, the RISKY-honest skill install panel (five-item checklist + container note + Stage for approval + flag-gated search), the reused approval card (no run/activate, no second badge), and the lifecycle tab controls (archive/restore-409 + per-row metadata).
- Every new string has en+it keys; every new contrast pair passes AA.
- Every surface renders all states (empty/loading/populated/error/error-auth + submitting/success/field-error); no secret in the DOM on any state.
</success_criteria>

<output>
Create `.planning/phases/29-governance-write-mcp-configuration-skills-install/29-04-SUMMARY.md` when done.
</output>
