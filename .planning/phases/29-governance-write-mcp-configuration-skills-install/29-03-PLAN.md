---
phase: 29-governance-write-mcp-configuration-skills-install
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - internal/skills/installer.go
  - internal/skills/installer_test.go
  - internal/skills/installer_ansi.go
  - internal/agui/governance_write_seam.go
  - internal/agui/governance_write_skills_api.go
  - internal/agui/governance_write_skills_api_test.go
  - cmd/aura/serve_webui.go
  - internal/skills/validator_fuzz_test.go
autonomous: true
requirements: [SKW-01, SKW-02, SKW-03]
must_haves:
  truths:
    - "Installing a skill from a source field (owner/repo, URL, or path) and from a skills.sh catalog search each stage the fetched body through the existing Writer gate showing source + content hash/preview + risk tier + the FIVE validation-checklist items (sanitized env, SKILL.md parse, body cap, injection-literal blocklist, sanitized name/path) + destination, labelled RISKY (never 'safe')"
    - "The npx skills transport runs `npx skills find <q>` / `npx skills add <src> -y` via exec.CommandContext inside Aura's container WITH install scripts permitted (NO --ignore-scripts), strips ANSI, stages the fetched tree to a temp dir, parses SKILL.md frontmatter, computes the canonical ContentHash, runs ValidateForWrite, then hands off to Writer.WriteInstallPending"
    - "A skill body one byte over the body cap fails the validation checklist (install rejected); at-cap passes (boundary edge SKW-01)"
    - "An injection literal expressed via Unicode-compatibility/equivalent forms is still caught (NFKC-normalized, case-folded scan); a non-ASCII skill name is rejected by SanitizeName (encoding edge SKW-01)"
    - "An empty/invalid source field is rejected with a safe error; a skills.sh search with no matches renders an empty state (empty edge SKW-01)"
    - "External skills.sh discovery is gated by the AURA_SKILLS_EXTERNAL_DISCOVERY flag the cockpit toggle reflects; with the flag off the catalog search is disabled (no silent discovery escalation, prohibition #8)"
    - "A RISKY/DESTRUCTIVE skill action enters the existing /api/approvals cross-thread queue with source/preview/risk-tier/resume-token via the HITL Interrupt/Resume[] protocol; a pending skill is absent from the active loader (cannot run, not injected)"
    - "Activation happens ONLY on the approval resume (no model-facing approve) via ResumeHandler.Resume; an expired or already-consumed resume token cannot activate and renders a terminal state"
    - "Archive moves an active skill to archived and restore moves it back (reflected on next fetch); a restore whose name collides with an active skill is REJECTED with a 409 BEFORE Writer.Restore runs (no silent overwrite — the restore-collision landmine)"
    - "The skills audit ledger records install/restore/archive as append-only rows, newest-first (created_at desc, id desc); no endpoint mutates an existing skill_audit row"
    - "Every new skills mutating endpoint returns 401 unauthenticated and 403 without governance.write; a forced backend failure renders a sanitized error; an empty dataset renders an empty state"
  artifacts:
    - path: "internal/skills/installer.go"
      provides: "Installer.Install (source/catalog -> npx skills -> stage -> ValidateForWrite -> WriteInstallPending) + Installer.Search (npx skills find behind the discovery flag) — the largest net-new piece"
      exports: ["Installer", "NewInstaller"]
    - path: "internal/agui/governance_write_skills_api.go"
      provides: "POST /api/governance/skills/install + .../{name}/restore|archive + create/update/delete + GET .../catalog handlers"
    - path: "internal/skills/installer_ansi.go"
      provides: "ANSI strip + provenance parse for the npx skills output (spike-proven transport)"
  key_links:
    - from: "internal/skills/installer.go"
      to: "internal/skills/writer.go (WriteInstallPending) + validator.go (ValidateForWrite) + contenthash.go (HashSkillDir) + scoring.go (ComputeSkillTier)"
      via: "the transport stages then hands off to the existing pending+audit sink; tier/validation/hash reused, not re-derived"
      pattern: "WriteInstallPending"
    - from: "internal/agui/governance_write_skills_api.go"
      to: "internal/skills/resume.go (ResumeHandler.Resume) via /api/approvals"
      via: "the install mints an ask_user pause that surfaces in the cross-thread queue; activation is the resume bridge only — no second queue (D-11)"
      pattern: "ResumeHandler|approvals"
    - from: "internal/agui/governance_write_skills_api.go"
      to: "internal/skills/writer_activate.go (Restore)"
      via: "the restore handler stats active/{name} and 409s BEFORE Writer.Restore (the os.RemoveAll collision landmine)"
      pattern: "Restore"
    - from: "internal/skills/installer.go"
      to: "AURA_SKILLS_EXTERNAL_DISCOVERY flag"
      via: "Search is disabled when the flag is off (D-08; no silent discovery)"
      pattern: "AURA_SKILLS_EXTERNAL_DISCOVERY"
  prohibitions:
    - statement: "MUST NOT let a model tool call approve or self-activate a skill — activation is operator-only via the approval resume; there is no model-facing approve path"
      status: resolved
      verification: "test — no code path activates a skill outside ResumeHandler.Resume/CLI; the install endpoint stages to pending only"
    - statement: "MUST NOT let a pending skill run, be materialized, or have its body injected into any LLM context while pending"
      status: resolved
      verification: "test — the active loader (List/ListStage) scans active/ only; a pending skill body never appears in any context-building call (held-out backstop)"
    - statement: "MUST NOT present a skill install as 'safe' — the cockpit always renders install as RISKY supply-chain input with the FIVE-item validation checklist (post-D-09; install scripts permitted, container-isolated)"
      status: resolved
      verification: "judgment — the install pipeline always labels RISKY; the checklist has five items, no --ignore-scripts"
    - statement: "MUST NOT enable external skills.sh discovery without an explicit operator-visible toggle (AURA_SKILLS_EXTERNAL_DISCOVERY) — no silent discovery escalation"
      status: resolved
      verification: "judgment — Search returns disabled/empty when the flag is off; the toggle state is explicit in the response"
    - statement: "MUST NOT silently overwrite an active skill on restore — a name-colliding restore is rejected with a safe error"
      status: resolved
      verification: "test — the restore handler 409s on an existing active/{name} BEFORE Writer.Restore (which does os.RemoveAll); the active skill is untouched"
---

<objective>
Build the skills write backend over the EXISTING Phase-11 Writer/gate/resume/audit — the net-new
`npx skills` install transport (gap-filler #3, the largest single new piece), the HTTP write
endpoints (install / restore / archive / create / update / delete / catalog), and the wiring that
surfaces a gated install in the EXISTING `/api/approvals` cross-thread queue. Reuse `ValidateForWrite`,
`HashSkillDir`, `ComputeSkillTier`, `WriteInstallPending`, `Writer.{Archive,Restore}`,
`WriteMutationByName`, and `ResumeHandler.Resume` — no new gate/scoring/activation logic.

This wave depends on plan 01 (governance.write capability) AND the D-09 SPEC-amendment commit from
plan 01 (PRD-first: no skills-install code lands before that commit). It extends the plan-02
governance_write seam + handlers in place.

Purpose: deliver SKW-01/02/03 — RISKY-honest install with the full checklist, the resume-only
approval queue (pending inert, no model approve), restore(409)/archive, append-only audit.
Output: `internal/skills/installer.go` (+ `installer_ansi.go`), the skills write handlers in
`internal/agui/governance_write_skills_api.go`, the `SkillsWriteProvider` seam, and the route mounts.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-01-SUMMARY.md
@.planning/phases/29-governance-write-mcp-configuration-skills-install/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The npx skills install transport (Installer.Install / Search) — stage -> validate -> WriteInstallPending</name>
  <files>internal/skills/installer.go, internal/skills/installer_ansi.go, internal/skills/installer_test.go, internal/skills/validator_fuzz_test.go</files>
  <read_first>
    - internal/skills/writer.go:179-225 (WriteInstallPending — the pending+audit sink the transport hands off to: stages the whole tree to pending/<name>/, records the D-29 install tuple in db.WithTx, NEVER self-activates, returns StatusPendingApproval)
    - internal/skills/validator.go (ValidateForWrite:82 — the FIVE-item checklist: SanitizeName ^[a-z0-9-]{1,64}$, description len, body cap, type enum, NFKC+casefold injection blocklist with matched-byte-position; violatesBlocklist:58)
    - internal/skills/contenthash.go (HashSkillFiles/HashSkillDir — the canonical sha256: byte-sorted hash the UI shows)
    - internal/scoring/scoring.go:126-140 (ComputeSkillTier: install->Risky; GateRecommended)
    - internal/skills/writer.go:232 (WriteMutationByName(ctx, action, name, description, body, always, actor) — the create/update/delete wrap reused by the create/update/delete endpoints in task 2)
    - .claude/skills/spike-findings-Aura/SKILL.md (the spike-proven `npx skills find`/`add -y` transport: `-y` non-interactive, strip ANSI, provenance/installs in the body; MCP/skill backend constraints)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md §6 (the transport adaptation — exec.CommandContext, scripts PERMITTED per D-06/D-07, NO --ignore-scripts) + §"No Analog Found" (the exec/ANSI glue is the only genuinely net-new piece)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"Common Pitfalls" Pitfall 6 (ANSI + non-interactivity) + Open Question 3 (RequiredEnv population)
  </read_first>
  <behavior>
    - Install(ctx, source) with a body at the cap passes ValidateForWrite; one byte over fails with ErrInvalidStructure; a blocklist hit fails with ErrBlocklisted + matched byte position
    - Install stages the fetched tree to a temp dir, parses SKILL.md frontmatter, computes the canonical hash, validates, then calls WriteInstallPending -> StatusPendingApproval (never activates)
    - The npx invocation uses `-y` (non-interactive) and the output is ANSI-stripped before the body is staged; a fake `npx` in the test exercises the strip+stage+parse path WITHOUT a real network call
    - Search(ctx, q) returns catalog results ONLY when AURA_SKILLS_EXTERNAL_DISCOVERY is set; otherwise it returns a disabled/empty result with the toggle state explicit
    - Install runs WITH install scripts permitted (NO --ignore-scripts flag is passed) — the container is the isolation boundary (D-06/D-07)
  </behavior>
  <action>
    Create `internal/skills/installer.go`: an `Installer` struct over the live `Writer` + an injectable
    command runner (so tests substitute a fake `npx`), `NewInstaller(writer *Writer, ...)`, `func (i
    *Installer) Install(ctx, source string, actor AuditActor) (status string, info InstallInfo, err error)`
    and `func (i *Installer) Search(ctx, q string) (CatalogResult, error)`. Install: reject an empty/blank
    source with a safe error; run `npx skills add <source> -y` via exec.CommandContext inside Aura's
    container — scripts PERMITTED, NO `--ignore-scripts` (D-06/D-07; the container IS the blast boundary);
    capture stdout/stderr, strip ANSI (the helper in installer_ansi.go), stage the fetched tree to a temp
    dir, parse the staged `SKILL.md` frontmatter (reuse the existing frontmatter parser), compute
    `HashSkillDir`, run `ValidateForWrite(fm, body, blocklist, bodyCap, false)` (the FIVE-item checklist),
    then `writer.WriteInstallPending(ctx, fm, body, stagedDir, hash, actor)` -> StatusPendingApproval.
    Return an `InstallInfo{Source, ContentHash, Preview, Destination, RiskTier, Checklist []CheckItem}` so
    the handler/UI can surface source/hash/preview/risk/checklist/destination. Search: run `npx skills find
    <q>` ONLY when `os.Getenv("AURA_SKILLS_EXTERNAL_DISCOVERY")` is truthy (D-08); else return a disabled
    CatalogResult carrying the toggle state. Create `internal/skills/installer_ansi.go` with the ANSI-strip
    + provenance parse (spike-proven). Write `installer_test.go` using a FAKE command runner to exercise:
    ANSI strip, stage, frontmatter parse, hash, body-cap +/-1 (ValidateForWrite), blocklist hit,
    empty-source rejection, Search-disabled-when-flag-off. Extend `validator_fuzz_test.go` if the five-item
    checklist surfacing needs new NFKC/non-ASCII-name coverage.
  </action>
  <acceptance_criteria>
    - `internal/skills/installer.go` exports `Installer` + `NewInstaller`; `grep -n 'ignore-scripts' internal/skills/installer.go` returns NOTHING (scripts permitted per D-09)
    - the install path calls `WriteInstallPending` (not a re-implemented pending writer) and never `Activate`
    - a unit test with a fake `npx` asserts: a body one byte over the cap fails ValidateForWrite; at-cap passes; a fullwidth/NFKC injection literal is caught; a non-ASCII name is rejected; an empty source is a safe error
    - a unit test asserts `Search` returns a disabled result when `AURA_SKILLS_EXTERNAL_DISCOVERY` is unset and runs `npx skills find` only when set
    - `go test ./internal/skills/ -run 'TestInstaller|TestValidatorFuzz'` exits 0; `go build ./... && go vet ./...` pass
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'func NewInstaller' internal/skills/installer.go && grep -q 'WriteInstallPending' internal/skills/installer.go && grep -q 'AURA_SKILLS_EXTERNAL_DISCOVERY' internal/skills/installer.go && go test ./internal/skills/ -run TestInstaller</automated>
  </verify>
  <done>The npx skills transport stages, ANSI-strips, validates (five-item checklist), hashes, and hands off to the existing WriteInstallPending sink (never self-activates); install scripts are permitted (no --ignore-scripts); external discovery is flag-gated; unit tests with a fake npx are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skills write handlers + SkillsWriteProvider seam (install -> approval queue, restore-409, archive, create/update/delete, catalog)</name>
  <files>internal/agui/governance_write_skills_api.go, internal/agui/governance_write_seam.go, internal/agui/governance_write_skills_api_test.go</files>
  <read_first>
    - internal/skills/writer_activate.go:112-138 (Restore -> promoteDir -> os.RemoveAll(dst): SILENTLY overwrites an active skill of the same name — the 409 guard MUST be HTTP-handler-side; Archive:68 de-materialize+move+audit to wrap directly; Activate:24)
    - internal/skills/resume.go:48 (ResumeHandler.Resume — accept->Activate, decline/cancel->DiscardPending: the ONLY activation path, reuse verbatim, no second one)
    - internal/agui/approvals_api.go:69-143 (handleListApprovals/handleResolveApproval -> Runner.SubmitAnswers — the cross-thread queue + resume token the install approval reuses; the queue is pause-source-agnostic, A3)
    - internal/agent/llm_agent_pause.go (tools.ErrAwaitingUserInput — how a skill-approval pause is minted; confirm the cockpit-origin install pause surfaces in the same /api/approvals queue and the resume dispatch routes a name+token skill pause to ResumeHandler — A3 / RESEARCH Open Q2)
    - internal/agui/governance_write_api.go + governance_write_seam.go (the plan-02 MCPWriteProvider seam + handlers to EXTEND in place — add SkillsWriteProvider to the same GovernanceWriteProviders bundle)
    - internal/agui/governance_api.go:144 (registerGovernanceRoutes + the thin-handler-over-provider + envChips shape) + onboarding_api.go (the generic mutation decode->validate->call skeleton) + approvals_api.go:114 (the path-token guard + closed verb-set)
    - internal/skills/installer.go (Installer.Install/Search from task 1) + internal/skills/stage_reader.go:41 (ListStage — pending/archived metadata; the loader scans active/ only, so pending is non-runnable by construction)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-PATTERNS.md §7 (the lifecycle endpoints reuse + the restore-collision 409) + §"Frontend" approval-card note
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-UI-SPEC.md §4/§5/§6 (the install response: source/hash/preview/destination/RISKY/5-checklist + container-isolation note; the approval-queue entry reuses /api/approvals; restore-collision error; audit append-only)
  </read_first>
  <behavior>
    - POST /api/governance/skills/install routes source/catalog -> Installer.Install -> pending + mints the ask_user pause that surfaces in /api/approvals with source/preview/risk-tier/resume-token; the response is the InstallInfo (RISKY label, five checklist items); the skill is NOT in the active loader
    - Activation happens only when the operator resolves the approval (Runner.SubmitAnswers -> ResumeHandler.Resume -> Activate); an expired/consumed token cannot activate (404/terminal); no model-facing approve
    - POST /api/governance/skills/{name}/restore stats active/{name}: if present -> 409 (no Writer.Restore call, active untouched); else -> Writer.Restore (-> active) + audit
    - POST /api/governance/skills/{name}/archive -> Writer.Archive (-> archived) + audit
    - POST/PATCH/DELETE create/update/delete -> Writer.WriteMutationByName (gated -> pending or audited delete)
    - GET /api/governance/skills/catalog?q= -> Installer.Search (disabled/empty when AURA_SKILLS_EXTERNAL_DISCOVERY off)
    - every handler: 503 when the provider is nil; sanitized error; the audit-read tabs are unchanged from Phase-28 (no mutate control)
  </behavior>
  <action>
    Extend `internal/agui/governance_write_seam.go`: declare `SkillsWriteProvider` with `Install(ctx,
    source) (InstallInfo, error)`, `Search(ctx, q) (CatalogResult, error)`, `Restore(ctx, name) error`
    (the impl stats active/{name} and returns a sentinel collision error BEFORE Writer.Restore), `Archive(ctx,
    name) error`, and `Mutate(ctx, action, name, description, body, always) (string, error)` wrapping
    `Writer.WriteMutationByName`. Add the `SkillsWriteProvider` field to the existing
    `GovernanceWriteProviders` bundle (do NOT mint a second bundle/setter). Create
    `internal/agui/governance_write_skills_api.go` registering the SPECIFIC method+path routes via the
    existing `registerGovernanceWriteRoutes`: `POST /api/governance/skills/install`, `POST
    /api/governance/skills/{name}/restore`, `POST /api/governance/skills/{name}/archive`, `POST
    /api/governance/skills` (create), `PATCH /api/governance/skills/{name}` (update), `DELETE
    /api/governance/skills/{name}` (delete), `GET /api/governance/skills/catalog`. Each handler: nil-check
    the provider (503), MaxBytesReader + decode + sanitized-validate, read principalFrom (the actor), ONE
    provider call, sanitizeErr on failure. The install handler returns the InstallInfo (RISKY label + the
    five checklist items + source/hash/preview/destination + the container-isolation note) and ensures the
    gated install surfaces in the EXISTING `/api/approvals` queue (A3: confirm/wire the ask_user pause origin
    so a cockpit-initiated install appears identically to a model-proposed mutation — the resume dispatch is
    name+token based, transport-agnostic; do NOT mint a second queue or badge — D-11). The restore handler
    maps the collision sentinel to 409 with the safe "active skill already exists" error. Write
    `governance_write_skills_api_test.go` (httptest + a fake Installer/Writer) covering: install -> pending
    (absent from active loader) + RISKY + five checklist items; restore-collision 409 (active untouched);
    archive->archived; create/update/delete via WriteMutationByName; catalog disabled when the flag is off;
    503/401/403; sanitized error; an expired/consumed resume token shows terminal (no activation).
  </action>
  <acceptance_criteria>
    - `internal/agui/governance_write_seam.go` adds `SkillsWriteProvider` to the existing `GovernanceWriteProviders` bundle (one bundle/setter total)
    - a handler test asserts the install response carries exactly FIVE checklist items (no `--ignore-scripts`), the RISKY label, and that the staged skill is absent from the active loader
    - a handler test asserts a restore whose name collides with an active skill returns 409 and that `Writer.Restore` was NOT invoked (the active skill bytes are unchanged)
    - a handler test asserts the install surfaces in the `/api/approvals` queue and that there is no model-facing approve path (activation only via the resume)
    - `go test ./internal/agui/ -run TestGovernanceWriteSkills` exits 0; `go build ./... && go vet ./...` pass
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'SkillsWriteProvider' internal/agui/governance_write_seam.go && grep -q 'StatusConflict' internal/agui/governance_write_skills_api.go && go test ./internal/agui/ -run TestGovernanceWriteSkills</automated>
  </verify>
  <done>The skills write handlers + SkillsWriteProvider exist; install routes through the gate to pending and surfaces in the existing /api/approvals queue (resume-only activation, no model approve); restore is 409-collision-guarded before the os.RemoveAll; archive/create/update/delete wrap the existing Writer; handler tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the skills write routes behind RequireCapability(governance.write) + wire the concrete SkillsWriteProvider</name>
  <files>cmd/aura/serve_webui.go</files>
  <read_first>
    - cmd/aura/serve_webui.go (the plan-02 MCP write route consts + mounts to mirror; the governanceWriteCapability const; the SetGovernanceWriteProviders wiring at the composition root)
    - internal/agui/governance_write_skills_api.go (the skills write route consts/strings from task 2)
    - internal/skills/installer.go + internal/skills/writer.go (construct the concrete SkillsWriteProvider: the live Writer + the new Installer + the resume bridge)
    - .planning/phases/29-governance-write-mcp-configuration-skills-install/29-RESEARCH.md §"Parent-mux mount discipline" + Pitfall 5 (method+path-specific only; never a bare /api/)
  </read_first>
  <action>
    Add the `governanceSkills*WriteRoute` method+path consts in `cmd/aura/serve_webui.go` (mirroring the
    plan-02 MCP write route const block). Mount EACH new skills write route `mux.Handle(<METHOD PATH>,
    agui.RequireCapability(aguiHandler, auth, governanceWriteCapability))` next to the MCP write mounts —
    method+path-specific siblings (Pitfall 5: never a bare `/api/`). At the composition root where
    `SetGovernanceWriteProviders` was wired in plan 02, construct + add the concrete SkillsWriteProvider (the
    live Writer + the new Installer + the resume bridge) to the bundle so both MCP and skills write providers
    are wired in one call. The catalog GET route inherits RequireAuth + governance.write like the mutations
    (it triggers an external network fetch when the discovery flag is on, so it is privileged, not a bare read).
  </action>
  <acceptance_criteria>
    - `grep -c 'governanceWriteCapability' cmd/aura/serve_webui.go` increases (one mount per skills write route: install/restore/archive/create/update/delete/catalog)
    - no bare `mux.Handle("/api/"` is introduced
    - `go build ./... && go vet ./...` pass
    - a serve-level test confirms `POST /api/governance/skills/install` returns 401 unauthenticated and 403 for an identity without governance.write
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'governanceSkills' cmd/aura/serve_webui.go && grep -q 'SetGovernanceWriteProviders' cmd/aura/serve_webui.go && go build ./... && go vet ./...</automated>
  </verify>
  <done>All skills write routes are mounted behind RequireCapability(governance.write) as method+path-specific siblings; the concrete SkillsWriteProvider (Writer + Installer + resume bridge) is wired at the composition root; build+vet pass; the auth gate is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cockpit browser -> SSR mux -> skills write handler | untrusted operator request crosses RequireAuth -> RequireCapability(governance.write) -> the skills write provider |
| skills.sh / source field -> npx skills -> staged tree | untrusted supply-chain content crosses into staging (Writer gate validates before pending) |
| installer -> Aura container | the install runs with scripts permitted; the container IS the isolation boundary (D-06/D-07) |
| pending/ skill -> LLM context | the boundary pending must NEVER cross (loader scans active/ only) |
| operator approval -> ResumeHandler -> Activate | the ONLY activation path (no model-facing approve) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Tampering | prompt injection via a fetched skill body | mitigate | ValidateForWrite NFKC+casefold injection blocklist (matched-position) at the write boundary, allowBlocklisted=false; body cap; staged to pending/ (non-runnable) BEFORE any approval |
| T-29-03-02 | Tampering | a pending skill body injected into LLM context | mitigate | the loader/manifest scans active/ ONLY (ListStage returns metadata, never a mounted body); pending is non-runnable by construction; held-out backstop in plan 05 |
| T-29-03-03 | Elevation of Privilege | model self-approves / self-activates a skill | mitigate | activation is operator-only via ResumeHandler.Resume from the /api/approvals resolve; no model-facing approve path; the install endpoint stages to pending only |
| T-29-03-04 | Tampering | restore silently overwrites an active skill | mitigate | the restore HTTP handler stats active/{name} and 409s BEFORE Writer.Restore (which does os.RemoveAll); the active skill is untouched |
| T-29-03-05 | Repudiation | unaudited skill lifecycle action | mitigate | install/restore/archive/create/delete each append one skill_audit row inside db.WithTx (reused Writer paths); append-only via the existing 0010 triggers |
| T-29-03-06 | Spoofing/Information Disclosure | silent external discovery escalation | mitigate | Search is gated by AURA_SKILLS_EXTERNAL_DISCOVERY (D-08); the toggle state is explicit in the response; off by default |
| T-29-03-07 | Denial of Service | a hung/looping npx invocation | mitigate | exec.CommandContext is ctx-bounded (the handler applies a deadline); a fake runner in tests; the container caps blast radius |
| T-29-03-SC | Tampering | the `skills` npx CLI supply chain | mitigate | RESEARCH Package Legitimacy Audit: `skills` v1.5.12 (vercel-labs), slopcheck clean, no postinstall — APPROVED (the spike-proven model-path transport). No NEW Go/npm deps added. The fetched-skill install scripts are deliberately permitted (container = boundary, D-06/D-07). |

*Canon-referred to `/gsd-secure-phase` (do NOT design here):* SSRF / arbitrary outbound on the
skill-source fetch is canon web-safety (the DISP-04 error classes + `/api/image-proxy` SSRF guard
precedent). Skill name/path traversal is canon — the `SanitizeName` `^[a-z0-9-]{1,64}$` grammar is
the chokepoint (reused at every Writer entry); the mitigation design is owned by `/gsd-secure-phase`.
The arbitrary-code-execution risk of `npx`/post-install scripts is the REASON install is RISKY +
container-isolated + approval-gated (post-D-09); the sandbox isolation mechanism is owned by the
Phase-8 sandbox posture / the container boundary.
</threat_model>

<verification>
- `go build ./... && go vet ./...` clean.
- `go test ./internal/skills/ -run 'TestInstaller|TestValidatorFuzz'` (unit, fake npx) green — body-cap +/-1, NFKC injection caught, non-ASCII name rejected, empty source safe error, Search flag-gated.
- `go test ./internal/agui/ -run TestGovernanceWriteSkills` (httptest) green — install->pending (absent from active loader) + RISKY + five checklist items; restore-collision 409 (active untouched); archive; create/update/delete; catalog flag-gated; 503/401/403; expired-token terminal.
- `go test -tags db_integration ./internal/skills/ ./internal/agui/` (live PG) green — install/restore/archive each append one skill_audit row; audit newest-first.
- `go test -race ./internal/skills/ ./internal/agui/` clean.
- The plan-01 D-09 SPEC-amendment commit is already in (no skills-install code lands before it).
</verification>

<success_criteria>
- SKW-01: cockpit install from a source field + a skills.sh catalog search each surface source/hash/preview/RISKY/five-checklist/destination before activation; over-cap/blocklist fails; empty source rejected; discovery toggle explicit.
- SKW-02: a gated action enters the existing /api/approvals queue with source/preview/risk/token; pending is inert (not runnable/injectable); activation only on the resume (no model approve); expired/consumed token terminal.
- SKW-03: restore/archive move skills (reflected next fetch); a colliding restore 409s (no overwrite); the audit ledger records each action append-only newest-first; no UI/endpoint mutates a skill_audit row.
- Every new endpoint 401/403-gated; sanitized errors; empty states valid.
</success_criteria>

<output>
Create `.planning/phases/29-governance-write-mcp-configuration-skills-install/29-03-SUMMARY.md` when done.
</output>
