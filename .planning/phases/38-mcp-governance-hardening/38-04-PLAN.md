---
phase: 38-mcp-governance-hardening
plan: 04
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - internal/mcp/manager/runtime.go
  - internal/mcp/manager/status.go
  - internal/mcp/managed_config_identity.go
  - internal/mcp/managed_config_identity_test.go
autonomous: true
requirements: [MCPH-01, MCPH-02]

must_haves:
  truths:
    - "manager/runtime.go's normalizedTrustForServer and isStreamableHTTPServer are migrated onto mcp.Classify — the DUPLICATE F-013 auto-promote-to-TrustRemoteHTTP branch in normalizedTrustForServer is deleted (the same fix as 38-01's managed_config.go, applied to its independent copy)."
    - "manager/status.go's runtimeName is migrated onto mcp.Classify (no residual inline Type==streamable_http || URL!=\"\" check)."
    - "MountForIdentity classifies each shared-catalog server and, for a REMOTE (streamable_http) server, does NOT apply a per-identity override's Trust.Class — a remote can only be made runnable via the admin shared catalog (D-04 fail-closed)."
    - "SetTrustForIdentity (via mutateIdentityPref) refuses/ignores a remote-server trust elevation with a sentinel error following the errors.New(\"mcp: ...\") convention, before persisting any overlay."
    - "A per-identity override that toggles enable or adjusts LOCAL (stdio) trust still works unchanged (the guard only fires for remote transports)."
    - "Empty, blank, and whitespace-only trust on a remote continue to resolve BLOCKED through the migrated manager path (F-013 closed at the manager copy too)."
  prohibitions:
    - "A blocked remote must never become runnable via a per-identity override."
  artifacts:
    - path: internal/mcp/managed_config_identity.go
      provides: "remote-trust-elevation guard in MountForIdentity + mutateIdentityPref/SetTrustForIdentity"
      contains: "Classify"
    - path: internal/mcp/managed_config_identity_test.go
      provides: "class-(a) REMOTE server fixture proving a per-identity override cannot elevate it"
    - path: internal/mcp/manager/runtime.go
      provides: "normalizedTrustForServer/isStreamableHTTPServer via Classify (duplicate F-013 branch deleted)"
  key_links:
    - from: internal/mcp/manager/runtime.go
      to: internal/mcp/classify.go
      via: "eligibility (RunnableManagedServers) resolves trust/type through Classify"
      pattern: "Classify"
    - from: internal/mcp/managed_config_identity.go
      to: internal/mcp/classify.go
      via: "MountForIdentity/mutateIdentityPref classify a server to detect a remote transport before applying an override"
      pattern: "Classify"
---

<objective>
Complete the classifier collapse in the `internal/mcp/manager` package (call sites #5, #6, #7 — including the
INDEPENDENTLY-DUPLICATED F-013 auto-promote bug in `normalizedTrustForServer`) and add the D-04 remote-trust
elevation guard: a per-identity overlay may toggle `enable` and adjust local trust, but it may NOT elevate a
REMOTE (`streamable_http`) server to a runnable trust class — that requires the admin shared catalog.

Purpose: MCPH-01 (the manager's eligibility resolver must route through the ONE classifier, not its own copy)
and MCPH-02 (D-04 — the network-facing transport is fail-closed against per-identity elevation). RESEARCH
correction #3: the F-013 fallback exists in TWO files; 38-01 fixed managed_config.go, this plan fixes the
manager's duplicate. D-04 is an ASVS V4 access-control boundary between the per-identity overlay and the
admin-governed shared catalog.
Output: manager runtime/status migrated onto Classify; the D-04 guard in the per-identity overlay + write path.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-mcp-governance-hardening/38-CONTEXT.md
@.planning/phases/38-mcp-governance-hardening/38-RESEARCH.md
@.planning/phases/38-mcp-governance-hardening/38-PATTERNS.md
@internal/mcp/managed_config_identity.go
@internal/mcp/manager/runtime.go
@internal/mcp/manager/status.go
@.planning/phases/38-mcp-governance-hardening/38-01-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Migrate manager/runtime.go + manager/status.go onto Classify (delete the duplicate F-013 branch)</name>
  <read_first>
    - internal/mcp/classify.go (from 38-01 — the exact Classify signature; 38-01-SUMMARY.md for the returned constants)
    - internal/mcp/manager/runtime.go (`normalizedTrustForServer` ~166-181 with the DUPLICATE `TrustRemoteHTTP` auto-promote branch; `isStreamableHTTPServer` ~... ; `RunnableManagedServers`/`RuntimeLaunchConfig` at ~60,63,88 that consume them)
    - internal/mcp/manager/status.go (`runtimeName` ~90-98 inline `URL!="" || Type==streamable_http`)
    - 38-PATTERNS.md §Cluster 1 (the duplicate-bug excerpt) and 38-RESEARCH.md Pitfall #1 + Call-Site Inventory (#5,#6,#7)
  </read_first>
  <action>
    Replace `normalizedTrustForServer`'s body with a call to `mcp.Classify` (taking the trust return), DELETING its
    independent `server.Type == mcp.ServerTypeStreamableHTTP || URL != "" → mcp.TrustRemoteHTTP` fallback (the duplicated F-013
    bug) so an untrusted remote resolves to TrustBlocked here too. Replace `isStreamableHTTPServer` with a Classify-derived
    serverType check. Migrate `runtimeName` in status.go to derive the transport label from Classify. Keep RunnableManagedServers'
    "exclude TrustBlocked" gating semantics intact — the point is that untrusted remotes now ARE TrustBlocked, so they are correctly
    excluded. Delete the now-dead helper bodies (deep-refactor-on-touch; do not leave unreachable duplicates). If a manager test
    (runtime_test.go/status_test.go) asserted the old auto-promote behavior, rewrite it to the fixed behavior with a commit-message
    justification (it encoded the bug). Handle a Classify error conservatively (treat as TrustBlocked / non-runnable) since the
    manager path is eligibility gating.
  </action>
  <verify>
    <automated>go vet ./internal/mcp/manager/ && go test ./internal/mcp/manager/ -race</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/mcp/manager/runtime.go` `normalizedTrustForServer` references `mcp.Classify` and no longer returns `mcp.TrustRemoteHTTP` as an inferred fallback (grep: the auto-promote branch is gone).
    - Source: `isStreamableHTTPServer` and `runtimeName` no longer contain the inline `URL != "" || Type == ...` reimplementation (they call Classify).
    - Behavior: a manager test asserts a bare-URL remote with empty trust is excluded from RunnableManagedServers (resolves Blocked).
    - Test: `go test ./internal/mcp/manager/ -race` exits 0.
  </acceptance_criteria>
  <done>The manager package's three classification sites route through Classify; the duplicate F-013 auto-promote branch is deleted; the package is green under -race.</done>
</task>

<task type="execute" tdd="true">
  <name>Task 2: D-04 remote-trust elevation guard in the per-identity overlay + write path</name>
  <read_first>
    - internal/mcp/managed_config_identity.go (`MountForIdentity` ~150-183 — the `if !IsSharedAdminGoverned(srv) { ... pref.Trust ... }` overlay block; `SetTrustForIdentity` ~194-201; `mutateIdentityPref` ~206-236 that looks up `srv` from the shared catalog before mutating; the `ErrSharedAdminGoverned`/`ErrUnknownServer` sentinels ~27-34)
    - internal/mcp/classify.go (from 38-01 — to detect ServerTypeStreamableHTTP)
    - internal/mcp/managed_config_identity_test.go (`TestSetTrustForIdentityOverlaysClassA` ~116 — today's fixture has a class-(a) LOCAL stdio server + a class-(b) admin-governed remote; it needs a NEW class-(a) REMOTE fixture to prove D-04)
    - 38-PATTERNS.md §Cluster 2 (the exact guard slot points + sentinel convention) and 38-RESEARCH.md §Security Domain V4 + Assumptions/Open Q on ignore-vs-error
  </read_first>
  <behavior>
    - a class-(a) LOCAL (stdio) server + a per-identity override setting trust → override applied (unchanged).
    - a class-(a) REMOTE (streamable_http) server + a per-identity override setting trust → override's Trust NOT applied by MountForIdentity (effective config keeps the shared-catalog trust; the remote is not elevated).
    - SetTrustForIdentity(identity, name, class) where name is a remote server → returns a non-nil sentinel error, and mutateIdentityPref persists NOTHING (no overlay written).
    - enable toggle on a remote still works (the guard is scoped to trust elevation, not enable).
  </behavior>
  <action>
    In MountForIdentity, inside the existing `if !IsSharedAdminGoverned(srv) { ... }` block, before applying
    `srv.Trust = pref.Trust`, classify `srv` via mcp.Classify; if the serverType is ServerTypeStreamableHTTP, SKIP applying the
    override's Trust (silently ignore, mirroring how IsSharedAdminGoverned is silently ignored today) so the remote keeps its
    shared-catalog trust — the enable toggle is still honored. In the write path, add a new sentinel (e.g.
    `ErrRemoteElevationForbidden = errors.New("mcp: remote (streamable_http) trust elevation requires the admin shared catalog")`)
    following the existing `errors.New("mcp: ...")` convention, and in mutateIdentityPref (right after the IsSharedAdminGoverned
    check, where `srv` is already looked up) return that sentinel if the classified serverType is streamable_http and the mutation
    is a trust change — so SetTrustForIdentity fails closed for a remote before persisting. Add a class-(a) REMOTE server fixture to
    managed_config_identity_test.go and tests covering the <behavior> rows (write test first for the guard — RED via compile/behavior,
    then GREEN). Keep managed_config_identity.go ≤600 LOC.
  </action>
  <verify>
    <automated>go test ./internal/mcp/ -run 'TestSetTrustForIdentity|TestMountForIdentity' -race</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/mcp/managed_config_identity.go` references `mcp.Classify`/ServerTypeStreamableHTTP inside the MountForIdentity overlay block and in mutateIdentityPref; a new `ErrRemoteElevationForbidden`-style sentinel exists following the `errors.New("mcp: ...")` convention.
    - Behavior: `SetTrustForIdentity` on a class-(a) REMOTE server returns the remote-elevation sentinel error and writes no overlay (assert via a follow-up read).
    - Behavior: `MountForIdentity` for an identity with a remote-trust override yields an effective config where the remote's trust is the shared-catalog value, NOT the override (the remote is not runnable-via-override).
    - Behavior: a LOCAL stdio override still applies; an enable toggle on a remote still applies.
    - Test: `go test ./internal/mcp/ -run 'TestSetTrustForIdentity|TestMountForIdentity' -race` exits 0; goleak clean.
  </acceptance_criteria>
  <done>A per-identity override cannot elevate a remote server to runnable (fail-closed in both the merge and the write path); a class-(a) remote fixture proves it; local overlays are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-identity overlay → admin shared catalog | A per-identity preference must not cross the admin-governance boundary to make a network-facing server runnable. |
| operator config → manager eligibility | The manager's own trust resolver must agree with the canonical classifier (no divergent copy re-opening F-013). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Elevation of Privilege (V4) | per-identity overlay elevates a remote (D-04) | mitigate | MountForIdentity + mutateIdentityPref classify the server and refuse remote trust elevation (skip in merge, sentinel error in write); verified by the class-(a) remote fixture test. |
| T-38-01b | Elevation of Privilege | duplicated F-013 auto-promote in manager/runtime.go | mitigate | normalizedTrustForServer migrated onto Classify; the duplicate auto-promote branch deleted; verified by the manager eligibility test. |
| T-38-SC | Tampering (supply chain) | package installs | accept | No new external packages; reuses `errors`/`strings` stdlib + the 38-01 Classify function; no install, no [ASSUMED]/[SUS]/[SLOP]. |
</threat_model>

<artifacts_this_phase_produces>
- Symbol: `mcp.ErrRemoteElevationForbidden` sentinel (naming Claude's Discretion, follows `errors.New("mcp: ...")`)
- Behavior change: `MountForIdentity`, `mutateIdentityPref`, `SetTrustForIdentity`, `normalizedTrustForServer`, `isStreamableHTTPServer`, `runtimeName` now route through `Classify`
- Test fixture: a class-(a) REMOTE (streamable_http) server in managed_config_identity_test.go
No new env vars, CLI flags, or migrations.
</artifacts_this_phase_produces>

<planner_assumptions>
- Probe #4 ([MCPH-02] ordering — per-identity override vs shared catalog precedence): authored as a covered truth (D-04 makes the shared catalog authoritative for remote runnability).
- Ignore-vs-error split (RESEARCH Open Q on D-04 shape): MountForIdentity SILENTLY IGNORES the override's remote trust (matching how it already silently ignores IsSharedAdminGoverned), while the explicit write path (SetTrustForIdentity) returns a sentinel error so a deliberate CLI/web remote-elevation attempt fails loudly rather than silently no-op'ing. This dual shape is the executor's spec.
</planner_assumptions>

<verification>
- `go test ./internal/mcp/ ./internal/mcp/manager/ -race` green; goleak clean.
- `go vet ./internal/mcp/ ./internal/mcp/manager/` + `go build ./...` clean.
- `bash scripts/check-file-size.sh` exit 0.
- grep: no residual `TrustRemoteHTTP` auto-promote fallback in manager/runtime.go; no inline `URL != "" ||` classification left in manager/*.
</verification>

<success_criteria>
Contributes to phase SC1 (empty-remote-trust blocked, never reaches stdio open) and completes MCPH-02's D-04
fail-closed guard. Together with 38-01 it closes F-013 at BOTH its independent copies and closes the per-identity
remote-elevation hole.
</success_criteria>

<output>
Create `.planning/phases/38-mcp-governance-hardening/38-04-SUMMARY.md` when done, noting the new sentinel name
and the D-04 guard behavior for the verifier.
</output>
