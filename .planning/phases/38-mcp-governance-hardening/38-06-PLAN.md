---
phase: 38-mcp-governance-hardening
plan: 06
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - cmd/aura/mcp_status.go
  - cmd/aura/doctor.go
  - cmd/aura/mcp_status_test.go
  - cmd/aura/doctor_test.go
autonomous: true
requirements: [MCPH-01, MCPH-09]

must_haves:
  truths:
    - "cmd/aura/mcp_status.go writeRuntimeCheck calls mcp.ProbeServer for HTTP (streamable_http/URL) servers too — the F-046 early-return that printed \"http endpoint configured\" without dialing is deleted; a dead/typoed HTTP endpoint reports OK=false / runtime-missing (D-16)."
    - "The probe is bounded by context.WithTimeout(ctx, AURA_MCP_PROBE_TIMEOUT) (default ~5s) — today neither writeRuntimeCheck branch imposes any deadline on the background ctx it receives."
    - "aura mcp status (mcpStatus) reflects the live probe in addition to the config-derived SnapshotStatus columns (D-17 — one probe surfaced on this surface too, additive, SnapshotStatus not replaced)."
    - "aura doctor gains a NEW 6th check that live-probes only ENABLED + RUNNABLE HTTP MCP servers via mcp.ProbeServer, skipping disabled/blocked servers deterministically (D-18); a per-server dial failure fails only its own result, never blocks siblings."
    - "doctorProbeMCPBinary (the Neo4j-Cypher sidecar binary check) is NOT touched — RESEARCH correction: it is unrelated to the managed MCP registry (Pitfall #11)."
    - "mcp_status.go's inline Type==streamable_http || URL!=\"\" classification (call-site #9) is migrated onto mcp.Classify while the probe-routing is rewritten."
    - "A reachable HTTP MCP server with zero tools probes OK=true (ToolCount 0); an unreachable/typoed endpoint probes OK=false."
    - statement: "A tools/list returning exactly as the probe deadline fires yields a deterministic OK verdict (not a race between OK and timeout)."
      verification: backstop
    - "A probe that times out leaves no leaked dialing goroutine (goleak-clean)."
    - "aura doctor live-probes only enabled+runnable HTTP servers; disabled and blocked servers are skipped deterministically."
  prohibitions: []
  artifacts:
    - path: cmd/aura/mcp_status.go
      provides: "writeRuntimeCheck + mcpStatus wired to live mcp.ProbeServer for HTTP, bounded by AURA_MCP_PROBE_TIMEOUT"
    - path: cmd/aura/doctor.go
      provides: "6th doctor check live-probing enabled+runnable HTTP MCP servers (D-18)"
    - path: cmd/aura/mcp_status_test.go
      provides: "dead-HTTP-endpoint OK=false test (httptest, no real network)"
  key_links:
    - from: cmd/aura/mcp_status.go
      to: internal/mcp/probe.go
      via: "writeRuntimeCheck/mcpStatus call mcp.ProbeServer for HTTP servers (reuse verbatim, already correct+tested)"
      pattern: "ProbeServer"
    - from: cmd/aura/doctor.go
      to: internal/mcp/probe.go
      via: "6th check calls mcp.ProbeServer per enabled+runnable HTTP server bounded by AURA_MCP_PROBE_TIMEOUT"
      pattern: "ProbeServer"
---

<objective>
Make the MCP HTTP health check LIVE everywhere it is surfaced, reusing the already-correct-and-tested
`mcp.ProbeServer` (RESEARCH: it already dials HTTP + calls tools/list and is already tested — this is a WIRING
task, not a new probe). Fix the REAL F-046 bug (RESEARCH correction #2: `cmd/aura/mcp_status.go:writeRuntimeCheck`,
NOT `doctor.go:doctorProbeMCPBinary`): delete the `streamable_http` early-return that printed "http endpoint
configured" without dialing, call `ProbeServer` bounded by `AURA_MCP_PROBE_TIMEOUT`, reflect the live probe in
`aura mcp status`, and add a NEW 6th `aura doctor` check that live-probes only enabled+runnable HTTP servers.

Purpose: MCPH-09 / F-046 — a dead/typoed HTTP MCP endpoint reports OK=false instead of healthy-by-config (ASVS
V7 false-confidence). D-17's "one probe, three surfaces" already holds for the governance board (handleMCPProbe);
this plan converges `aura doctor` + `aura mcp status` onto the same probe. Also completes classifier call-site #9.
Depends on 38-01 only (ProbeServer→OpenServer→Classify; #9 migration). No overlap with 38-04/38-05 files.
Output: live-probing `mcp_status.go` + a 6th `doctor.go` check.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-mcp-governance-hardening/38-CONTEXT.md
@.planning/phases/38-mcp-governance-hardening/38-RESEARCH.md
@.planning/phases/38-mcp-governance-hardening/38-PATTERNS.md
@cmd/aura/mcp_status.go
@cmd/aura/doctor.go
@internal/mcp/probe.go
@.planning/phases/38-mcp-governance-hardening/38-01-SUMMARY.md
</context>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: Fix the real F-046 — live-probe HTTP in writeRuntimeCheck + reflect it in mcp status (D-16/D-17)</name>
  <read_first>
    - cmd/aura/mcp_status.go (`writeRuntimeCheck` ~90-109 — the F-046 early-return `"%s: http endpoint configured\n"` that skips ProbeServer for HTTP; the stdio branch that ALREADY calls mcp.ProbeServer correctly; `mcpStatus` ~14-48 using `mcpmanager.SnapshotStatus`; the inline `Type==streamable_http || URL!=""` at ~98 = call-site #9; the ctx flow — `runMCP`→`runMCPCommand(context.Background(), ...)`, no deadline anywhere today)
    - internal/mcp/probe.go (`ProbeServer` ~47-76 — already dials both stdio+HTTP via OpenServer and calls ListTools; ProbeResult shape {OK, ToolCount, Detail, Err}; REUSE VERBATIM, no change)
    - internal/mcp/classify.go + 38-01-SUMMARY.md (for the #9 migration)
    - internal/agui/governance_api.go (`handleMCPProbe` ~228-249 + `probeDeadline()` — the already-correct third surface's `context.WithTimeout(r.Context(), deadline)` + per-row isolation shape to mirror)
    - 38-RESEARCH.md Pitfall #11 (the exact bug + why doctorProbeMCPBinary is OUT of scope) + D-16/D-17; 38-PATTERNS.md §Cluster 7
  </read_first>
  <behavior>
    - writeRuntimeCheck for a reachable HTTP MCP server (httptest serving tools/list) → OK / runtime-ok line.
    - writeRuntimeCheck for a dead/typoed HTTP endpoint → OK=false / runtime-missing line (not "http endpoint configured").
    - the probe is bounded: a hung endpoint returns within ~AURA_MCP_PROBE_TIMEOUT, not indefinitely.
    - a reachable server with zero tools → OK=true, ToolCount 0.
    - mcpStatus output reflects the live probe result alongside the SnapshotStatus config columns.
  </behavior>
  <action>
    In writeRuntimeCheck, DELETE the `if server.Type == streamable_http || server.URL != "" { return "...http endpoint
    configured..." }` early-return; route BOTH stdio and HTTP through mcp.ProbeServer(ctx, name, server) bounded by
    `context.WithTimeout(ctx, probeTimeout)` where probeTimeout comes from a resolver reading AURA_MCP_PROBE_TIMEOUT seconds
    (default 5, via envutil.IntDefault) — mirroring handleMCPProbe's per-row `context.WithTimeout` + isolation (a dead server fails
    only its own line). Replace the inline #9 classification with mcp.Classify to decide the transport label. In mcpStatus, keep
    mcpmanager.SnapshotStatus for the config-derived columns (trust/runtime/profiles) but ADD a live-probe column/row per server
    (bounded by the same AURA_MCP_PROBE_TIMEOUT, per-server isolated) so the surface reflects reachability — additive, do NOT replace
    SnapshotStatus. Add mcp_status_test.go tests using httptest (no real network) covering the <behavior> rows, including the
    dead-endpoint OK=false case and the zero-tools OK=true case. Do NOT touch doctorProbeMCPBinary. Keep mcp_status.go ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./cmd/aura/ && go test ./cmd/aura/ -run 'TestWriteRuntimeCheck|TestMcpStatus|TestProbe' -race</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cmd/aura/mcp_status.go` `writeRuntimeCheck` no longer contains the `"http endpoint configured"` string (grep: absent); it calls `mcp.ProbeServer` for HTTP servers under a `context.WithTimeout(..., AURA_MCP_PROBE_TIMEOUT)`.
    - Source: the inline `Type == ... || URL != ""` at the probe-routing site is replaced by `mcp.Classify`.
    - Source: `doctorProbeMCPBinary` is unchanged (grep diff shows no edit to that function).
    - Behavior: a dead httptest HTTP endpoint yields a non-OK runtime line (test asserts OK=false / "runtime missing"); a reachable zero-tools endpoint yields OK=true.
    - Test: `go test ./cmd/aura/ -race` exits 0.
  </acceptance_criteria>
  <done>writeRuntimeCheck + mcpStatus live-probe HTTP servers under a bounded deadline; a dead endpoint reports OK=false; #9 is on Classify; doctorProbeMCPBinary untouched.</done>
</task>

<task type="execute">
  <name>Task 2: New 6th aura doctor check — live-probe enabled+runnable HTTP MCP servers (D-18)</name>
  <read_first>
    - cmd/aura/doctor.go (`doctorChecks()` ~89-97 — the 5-check registry; `doctorProbe` signature `func(context.Context, *config.Config) (string, error)` ~18; `defaultDoctorProbeNeo4j` ~113-127 — the one-string-detail/one-error probe shape to mirror; the `exitUnreachable`/`exitInfra` failure codes)
    - internal/mcp/probe.go (`ProbeServer`) + internal/mcp/manager (RunnableManagedServers — the enabled+runnable set to filter) + internal/mcp/classify.go (to filter to HTTP-only)
    - 38-PATTERNS.md §Cluster 7 (the 6th-check shape + D-18 scope: only enabled+runnable HTTP, skip disabled/blocked, and skip stdio since D-18 is scoped to the dead-HTTP-endpoint problem) + 38-RESEARCH.md D-18
  </read_first>
  <action>
    Add a new `doctorProbeMCPServers(ctx, cfg) (string, error)` following the defaultDoctorProbeNeo4j shape (return one detail
    string + one error). It loads the managed MCP config, iterates RunnableManagedServers, filters to enabled + runnable + HTTP
    (streamable_http via mcp.Classify — skip disabled, skip blocked, skip stdio per D-18), and calls mcp.ProbeServer per server
    bounded by `context.WithTimeout(ctx, AURA_MCP_PROBE_TIMEOUT)` with per-server isolation (one dead server fails only its own
    sub-result, never blocks siblings). Aggregate to a single pass/fail + detail (e.g. "3/3 HTTP MCP servers reachable" or names the
    unreachable ones). Register it as a 6th `doctorCheck{name: "mcp", probe: doctorProbeMCPServers, failureCode: exitUnreachable}` in
    doctorChecks(). Add doctor_test.go coverage (httptest) proving: a reachable HTTP server → pass; a dead one → fail with a naming
    detail; disabled/blocked/stdio servers are skipped (not dialed). Keep doctor.go ≤600 LOC.
  </action>
  <verify>
    <automated>go vet ./cmd/aura/ && go test ./cmd/aura/ -run 'TestDoctor|TestProbeMCPServers' -race</automated>
  </verify>
  <acceptance_criteria>
    - Source: `cmd/aura/doctor.go` `doctorChecks()` includes a 6th entry `{name: "mcp"` whose probe live-dials HTTP MCP servers; a new `doctorProbeMCPServers` exists with the `func(context.Context, *config.Config) (string, error)` signature.
    - Behavior: the check passes for a reachable httptest HTTP server, fails (naming it) for a dead one, and does NOT dial disabled/blocked/stdio servers (assert via a spy or by observing no dial attempt).
    - Behavior: the probe is bounded — a hung endpoint returns within ~AURA_MCP_PROBE_TIMEOUT; goleak clean (no leaked dialing goroutine).
    - Test: `go test ./cmd/aura/ -race` exits 0.
  </acceptance_criteria>
  <done>aura doctor has a 6th check that live-probes only enabled+runnable HTTP MCP servers, bounded + isolated, skipping disabled/blocked/stdio deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured HTTP MCP endpoint → operator health view | A dead/typoed endpoint must not be reported healthy purely because it is configured (false confidence). |
| probe dial → daemon liveness | A hung endpoint must not block the doctor/status command or leak a dialing goroutine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-10 | Information Disclosure (false confidence) | writeRuntimeCheck skip-probe for HTTP (F-046) | mitigate | Delete the early-return; call mcp.ProbeServer for HTTP bounded by AURA_MCP_PROBE_TIMEOUT; dead endpoint → OK=false; verified by the dead-endpoint httptest. |
| T-38-10b | Denial of Service (probe) | unbounded / goroutine-leaking probe | mitigate | Per-server context.WithTimeout + isolation (a dead server fails only its own row); goleak-clean; verified by the timeout + goleak tests. |
| T-38-SC | Tampering (supply chain) | package installs | accept | No new external packages; reuses `net/http/httptest`, `context`, existing `mcp.ProbeServer`; no install, no [ASSUMED]/[SUS]/[SLOP]. |
</threat_model>

<artifacts_this_phase_produces>
- Function: `doctorProbeMCPServers(context.Context, *config.Config) (string, error)` + the 6th `doctorCheck{name: "mcp"}` registry row
- Env var: `AURA_MCP_PROBE_TIMEOUT` (seconds, default 5) read via envutil.IntDefault (Tier C, not registered in config_knobs.go)
- Behavior change: `writeRuntimeCheck` live-probes HTTP; `mcpStatus` adds a live-probe column; call-site #9 on Classify
- Test files: `cmd/aura/mcp_status_test.go`, `cmd/aura/doctor_test.go` (dead-endpoint OK=false via httptest)
No new migrations, CLI flags, DB. `internal/mcp/probe.go` is reused verbatim (unchanged).
</artifacts_this_phase_produces>

<planner_assumptions>
- Probe #19 ([MCPH-09] adjacency — deadline-exact) authored as a `backstop` truth (verifier abstains to human_needed if the exact-deadline race is not explicitly evidenced).
- Probes #20/#21/#22 (zero-tools OK=true / unreachable OK=false; doctor skips disabled+blocked deterministically; no leaked goroutine) authored as covered truths.
- D-17 note: the governance health board (handleMCPProbe) is ALREADY the live surface; this plan only converges doctor + mcp status onto the same ProbeServer. AURA_MCP_PROBE_TIMEOUT may reconcile with the board's hardcoded defaultProbeTimeout (3s) or stay independent — Claude's Discretion; the two surfaces this plan owns use AURA_MCP_PROBE_TIMEOUT.
- If a goleak TestMain is absent in cmd/aura, verify before asserting no-leaks there (RESEARCH: goleak is wired in internal/mcp, not automatically in cmd/aura).
</planner_assumptions>

<verification>
- `go test ./cmd/aura/ -race` green (mcp_status_test.go + doctor_test.go, httptest-only, no real network).
- `go vet ./cmd/aura/` + `go build ./...` clean.
- `bash scripts/check-file-size.sh` exit 0 (mcp_status.go, doctor.go ≤600).
- grep: no `"http endpoint configured"` in mcp_status.go; doctorProbeMCPBinary unchanged; ProbeServer called from both surfaces.
</verification>

<success_criteria>
Delivers phase SC4 ("a dead HTTP MCP endpoint reports OK=false"): MCPH-09 across the two surfaces this plan owns
(aura doctor's new 6th check + aura mcp status/writeRuntimeCheck), converged onto the single already-tested
ProbeServer with the governance board (the third surface, already live). Completes classifier call-site #9 (MCPH-01).
</success_criteria>

<output>
Create `.planning/phases/38-mcp-governance-hardening/38-06-SUMMARY.md` when done, recording AURA_MCP_PROBE_TIMEOUT
default + the 6th doctor check name for the verifier.
</output>
