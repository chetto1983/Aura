---
phase: 25-chat-approval-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agui/conversations_api.go
  - internal/agui/conversations_api_test.go
  - internal/agui/server.go
  - cmd/aura/serve_webui.go
  - cmd/aura/serve_webui_test.go
autonomous: true
requirements: [CHAT-02, CHAT-03]
must_haves:
  truths:
    - "GET /api/conversations returns the conversation list (incl. archived when ?archived=true) as JSON, behind RequireAuth"
    - "GET /api/conversations/search?q= returns FTS snippet hits projected from SearchConversationTurns"
    - "POST /api/conversations/{id}/rename, /archive, /unarchive, and DELETE /api/conversations/{id} call exactly the matching Store method"
    - "A malformed conversation id is a clean 404, never a 500"
    - "The cockpit SSE stream emits real REASONING_* delta text (Translate showReasoning=true) for the web cockpit path"
    - "/api/integrations/ proxy still routes correctly after the new /api/conversations/ subtree is mounted"
  artifacts:
    - path: "internal/agui/conversations_api.go"
      provides: "Thin /api/conversations… REST handlers over conversations.Store"
      exports: ["handleListConversations", "handleSearchConversations", "handleRenameConversation", "handleArchiveConversation", "handleDeleteConversation"]
    - path: "internal/agui/conversations_api_test.go"
      provides: "db_integration coverage of every conversation route → Store method mapping"
    - path: "cmd/aura/serve_webui.go"
      provides: "Mount of /api/conversations/ subtree behind RequireAuth (no bare /api/)"
  key_links:
    - from: "internal/agui/conversations_api.go"
      to: "conversations.Store"
      via: "widened ConversationStore consumer interface in internal/agui"
      pattern: "s\\.conv\\.(List|SearchConversationTurns|Rename|UpdateStatus|Delete)"
    - from: "cmd/aura/serve_webui.go"
      to: "/api/conversations/ handler"
      via: "mux.Handle specific subtree (NOT bare /api/)"
      pattern: "mux\\.Handle\\(\"/api/conversations/\""
---

<objective>
Build the thin HTTP adapter surface for conversation management (CHAT-02) and flip
the cockpit reasoning stream on (D-01, the CHAT-03 backend half). All new `/api/`
reads/writes are thin shells over the already-complete `conversations.Store`, mounted
behind the Phase-24 `RequireAuth` whole-origin gate.

Purpose: CHAT-02 (browse/search/rename/archive/delete conversations) needs a REST
surface the React sidebar consumes; CHAT-03 needs the cockpit SSE path to actually
stream reasoning deltas (today redacted). Both are minimal-mechanism wiring over
shipped seams.
Output: `internal/agui/conversations_api.go` (+ test), a widened `ConversationStore`
consumer interface in `internal/agui/server.go`, the `Translate(…, true)` cockpit
flip, and the `/api/conversations/` mount in `cmd/aura/serve_webui.go`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-chat-approval-center/25-CONTEXT.md
@.planning/phases/25-chat-approval-center/25-RESEARCH.md
@.planning/phases/25-chat-approval-center/25-PATTERNS.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan (executors and later plans depend on these):
- Route `GET /api/conversations` — list (`?archived=true` includes archived)
- Route `GET /api/conversations/search?q={query}&limit={n}` — FTS snippet hits
- Route `POST /api/conversations/{id}/rename` (body `{title}`)
- Route `POST /api/conversations/{id}/archive` and `POST /api/conversations/{id}/unarchive`
- Route `DELETE /api/conversations/{id}` — hard delete (D-07 confirm lives in the UI)
- Widened `ConversationStore` interface in `internal/agui` adding `List`,
  `SearchConversationTurns`, `UpdateStatus`, `Rename`, `Delete`, `SetTitleIfNull`, `Get`
- The cockpit-scoped `showReasoning=true` Translate flip (D-01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conversation REST adapter + widened consumer interface (CHAT-02)</name>
  <files>internal/agui/conversations_api.go, internal/agui/conversations_api_test.go, internal/agui/server.go</files>
  <read_first>
    - internal/agui/conversations_api.go (does not exist yet — create)
    - internal/agui/server.go:57 (Runner interface), the `ConversationStore` interface decl, server.go:220 (`handleMessages` thin-read analog), server.go:475 (`sanitizeErr`), server.go:486 (`SanitizeString`), server.go:31 (`maxRunBodyBytes`)
    - internal/conversations/store.go:97 (Conversation struct), :156 (Get), :173 (List), :187 (UpdateStatus), :204 (Rename), :221 (SetTitleIfNull), :299 (SearchResult), :310 (SearchConversationTurns), :364 (Delete)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"internal/agui/conversations_api.go" (thin-read pattern + consumer-interface-widen guidance) and §"uuid.Parse-before-store-call guard"
  </read_first>
  <behavior>
    - Test: GET /api/conversations returns `Store.List(ctx, false)`; `?archived=true` → `List(ctx, true)`
    - Test: GET /api/conversations/search?q=foo returns `SearchConversationTurns(ctx, "foo", limit)` projected to JSON; empty q → 400
    - Test: POST /api/conversations/{id}/rename with `{title}` calls `Store.Rename(ctx, id, title)` → 204; empty title → 400
    - Test: POST /api/conversations/{id}/archive calls `UpdateStatus(ctx, id, "archived")`; /unarchive → `UpdateStatus(ctx, id, "active")`
    - Test: DELETE /api/conversations/{id} calls `Store.Delete(ctx, id)` → 204
    - Test (edge): a non-UUID id on any route → 404 (never 500); a missing conversation → 404 mapping `conversations.ErrConversationNotFound`
  </behavior>
  <action>
    Create `internal/agui/conversations_api.go` with five thin handlers
    (`handleListConversations`, `handleSearchConversations`, `handleRenameConversation`,
    `handleArchiveConversation` covering both archive+unarchive via the path verb, and
    `handleDeleteConversation`). Each parses the request, `uuid.Parse`-guards any `{id}`
    path param to a clean 404 BEFORE the store round-trip (Shared Pattern
    "uuid.Parse-before-store-call guard"), calls EXACTLY ONE store method, and projects to
    JSON — NO business logic in the handler (PATTERNS thin-adapter discipline). Cap mutating
    request bodies with the existing `maxRunBodyBytes` ceiling; redact every error response
    with `sanitizeErr(err)`. Widen the existing `ConversationStore` consumer interface in
    `server.go` (accept interfaces, return structs — D-A2-02) to add `List`,
    `SearchConversationTurns`, `UpdateStatus`, `Rename`, `Delete`, `SetTitleIfNull`, `Get`;
    `*conversations.Store` satisfies it implicitly — do NOT change the concrete store.
    Map `conversations.ErrConversationNotFound` → 404. Per D-08 the search response carries
    the `SearchResult` projection as-is (snippet + conversationID + title) so the UI can open
    the thread at the match. Do NOT register routes here — mounting is Task 3.
    Write `conversations_api_test.go` as a `db_integration`-tagged test (the env-gated tier
    `t.Fatal`s under `$CI` when unset — no skip-as-green) exercising each route→method mapping
    plus the malformed-id and not-found edge cases against a live `aura.*` schema.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./internal/agui/ && go build ./internal/agui/ && go test -tags db_integration ./internal/agui/ -run TestConversationsAPI -race</automated>
  </verify>
  <acceptance_criteria>
    - `go test -tags db_integration ./internal/agui/ -run TestConversationsAPI` PASSES with a non-trivial (>1s, real DB) runtime — a sub-second run is a skip tell and FAILS the gate
    - Source assertion: `grep -c "uuid.Parse" internal/agui/conversations_api.go` ≥ 1 (every id route guards)
    - Source assertion: `grep -c "sanitizeErr" internal/agui/conversations_api.go` ≥ 1 (errors redacted on the wire)
    - Behavior: a non-UUID id returns HTTP 404 (asserted in test), never 500
    - No business logic: each handler body calls exactly one `s.conv.*` method (reviewer-checkable against the thin-adapter rule)
  </acceptance_criteria>
  <done>All five conversation routes map to the correct Store method, malformed ids 404, errors are redacted, and the db_integration test passes on the live stack.</done>
</task>

<task type="auto">
  <name>Task 2: Cockpit reasoning-on flip (D-01 / CHAT-03 backend half)</name>
  <files>internal/agui/server.go, internal/agui/conversations_api_test.go</files>
  <read_first>
    - internal/agui/server.go:151 (`handleRun` — VERIFY the call graph: this handler serves BOTH the cockpit and Telegram today), server.go:214 (`Translate(in.ThreadID, runID, s.idgen, turn, false)` call site)
    - internal/agui/translator.go:53 (`showReasoning bool` last param threaded into `reasoningRunState`), translator.go:228 (`reasoningRunState`)
    - internal/agui/agui_subscriber.go (the Telegram live-CoT opt-in path — confirm whether it shares `handleRun`)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"internal/agui/server.go (controller, streaming) — D-01 reasoning-on flip"
    - .planning/phases/25-chat-approval-center/25-CONTEXT.md D-01 (whole-origin-private justification; HARDEN-05 trace-persistence is SEPARATE)
  </read_first>
  <action>
    Flip the cockpit SSE path so REASONING_* delta text streams (D-01). FIRST verify the
    `handleRun` call graph (read_first): the same handler may serve both the cockpit and the
    Telegram channel. If it serves only the web cockpit, change the final `false` → `true` at
    server.go:214. If it is shared, do NOT hard-code a blanket global `true` — thread the
    `showReasoning` value from config or a per-route/per-principal flag so Telegram keeps its
    existing opt-in posture and ONLY the whole-origin-private cockpit gets live CoT. Document
    the choice inline: update the existing redaction comment to record the whole-origin-private
    single-operator justification (Phase 24 D-03) and that this is the live cockpit STREAM,
    distinct from trace persistence (HARDEN-05, which is unchanged). Keep `redactEvent` /
    `sanitizeErr` on the stream untouched — only the reasoning delta gating changes. Add a
    Go golden/unit assertion (extend the agui reasoning test, ❌ Wave-0 if no analog exists:
    `internal/agui/translator_reasoning_test.go`) proving `Translate(…, true)` emits non-empty
    REASONING_* delta content where `false` redacts it.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./internal/agui/ && go build ./internal/agui/ && go test ./internal/agui/ -run 'TestTranslate|Reasoning' -race</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the cockpit call site no longer passes a hard-coded `false` for the cockpit path (grep confirms `true` or a config/per-route variable at the cockpit Translate call)
    - Behavior: `go test ./internal/agui/ -run TestTranslate` (or the reasoning golden test) PASSES and asserts non-empty REASONING_* delta text under showReasoning=true vs redacted under false
    - If `handleRun` is shared with Telegram: the Telegram path's `showReasoning` value is provably unchanged (test or code-review assertion) — the flip is cockpit-scoped, not global
    - Comment at the call site records the whole-origin-private justification + the trace-persistence-is-separate note
  </acceptance_criteria>
  <done>The cockpit SSE stream emits real reasoning deltas; the Telegram posture is unchanged; the golden test proves the gating; the justification is documented inline.</done>
</task>

<task type="auto">
  <name>Task 3: Mount /api/conversations/ subtree behind RequireAuth (Pitfall 6)</name>
  <files>cmd/aura/serve_webui.go, cmd/aura/serve_webui_test.go</files>
  <read_first>
    - cmd/aura/serve_webui.go:16-25 (the bare-/api/ mux-collision warning), :45 (`aguiRoutePrefixes`), :62-70 (`fallbackExcludedPrefixes` — already returns `/api/`), :100-132 (`newServeHandler` mux composition + RequireAuth wrap), :109 (`POST /agent/run` RequireCapability), :122 (`integrationsRoutePrefix` mount)
    - internal/agui/conversations_api.go (Task 1 output — the handlers to mount)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"cmd/aura/serve_webui.go (route-mount …) — Pitfall 6 footgun"
  </read_first>
  <action>
    Register the SPECIFIC new subtree `mux.Handle("/api/conversations/", aguiHandler)` (and
    method-specific patterns where Go 1.22 routing helps, e.g. `DELETE /api/conversations/{id}`)
    in `newServeHandler` — NEVER a bare `mux.Handle("/api/", …)` (it would shadow
    `integrationsRoutePrefix` = `/api/integrations/`, the documented T-24-07 footgun). The
    `/api/` SPA-fallback exclusion already returns these as backend routes
    (`fallbackExcludedPrefixes()` returns `/api/`) so NO fallback change is needed. The whole
    mux is wrapped in `agui.RequireAuth` at the bottom — the new reads inherit the whole-origin
    gate for free; do NOT add a second auth check. Wire the conversation handlers (Task 1) into
    the AG-UI `Server` so the prefix dispatches to them (mirror how the gateway routes its own
    prefixes). Extend `serve_webui_test.go` to assert (a) `/api/conversations/` is reachable
    and gated by RequireAuth, and (b) `/api/integrations/...` still routes to the integrations
    proxy after the new mount (no shadowing).
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./cmd/aura/ && go build ./cmd/aura/ && go test ./cmd/aura/ -run 'ServeWebui|Route|Mount' -race</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '"/api/conversations/"' cmd/aura/serve_webui.go` ≥ 1 AND `grep -c 'mux.Handle("/api/",' cmd/aura/serve_webui.go` == 0 (no bare /api/ — use `grep -v '^//'` to exclude the warning comment)
    - Behavior test: a request to `/api/integrations/...` still hits the integrations proxy after the new mount (asserted in serve_webui_test.go)
    - Behavior test: `/api/conversations` is unauthenticated-rejected when a secret is configured (RequireAuth inherited)
    - `go test ./cmd/aura/` PASSES
  </acceptance_criteria>
  <done>The conversation subtree is mounted specifically (no bare /api/), the integrations proxy is unshadowed, and every new route is behind RequireAuth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/conversations… | Operator-supplied conversation ids, FTS query, rename title cross into the store |
| Cockpit SSE consumer → reasoning stream | Live CoT delta text crosses to the authenticated operator's browser (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | FTS `?q=` on /api/conversations/search | mitigate | Bind via the LOCKED sqlc `content % $1` parameterized contract (`conversation_turns.sql:33`) — never interpolate/rewrite the query string |
| T-25-02 | Information Disclosure | malformed conversation id → 500 stack leak | mitigate | `uuid.Parse` the id BEFORE the store call → clean 404 (server.go:167 precedent); errors wrapped in `sanitizeErr` |
| T-25-03 | Information Disclosure | reasoning CoT live in the drawer (D-01 flips redaction on) | accept | Justified by the whole-origin-private single-operator cockpit (Phase 24 D-03); the only viewer is the authenticated operator; trace still does NOT persist verbatim (HARDEN-05). Telegram posture provably unchanged (Task 2). |
| T-25-04 | Spoofing/Elevation | unauthenticated caller hits /api/conversations… | mitigate | Mounted behind the Phase-24 `RequireAuth` whole-origin gate (inherited by the wrapped mux); verified in serve_webui_test.go |
| T-25-05 | Tampering | `/api/integrations/` shadowed by a bare `/api/` registration | mitigate | Register the SPECIFIC `/api/conversations/` subtree only (Pitfall 6); test asserts the integrations proxy still routes |
| T-25-SC | Tampering | npm/go installs | mitigate | This plan adds NO new dependency (pure wiring over shipped stores) |
</threat_model>

<verification>
- `go test -tags db_integration ./internal/agui/ -run TestConversationsAPI -race` green on the live stack (derive `AURA_DB_URL`/`AURA_DB_MIGRATE_URL` from `POSTGRES_PASSWORD`)
- `go test ./internal/agui/ -run TestTranslate` proves the reasoning gating
- `go test ./cmd/aura/` proves the mount + no-shadow
- `make quality` (vet+build+lint+race+vuln) clean for the touched packages
</verification>

<success_criteria>
- CHAT-02: all five conversation routes map 1:1 to the correct `conversations.Store` method, malformed ids 404, errors redacted, behind RequireAuth
- CHAT-03 (backend half): the cockpit SSE stream emits real REASONING_* deltas (D-01), cockpit-scoped, with Telegram unchanged
- `/api/integrations/` proxy unshadowed after the new mount
- Coverage on `internal/agui` stays ≥85% across the tag matrix
</success_criteria>

<output>
Create `.planning/phases/25-chat-approval-center/25-01-SUMMARY.md` when done
</output>
