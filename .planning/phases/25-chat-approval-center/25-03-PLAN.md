---
phase: 25-chat-approval-center
plan: 03
type: execute
wave: 3
depends_on: ["25-01"]
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/src/chat/sseAdapter.ts
  - web/src/chat/__tests__/sseAdapter.test.ts
  - web/src/chat/ExternalStoreChat.tsx
  - web/src/chat/Composer.tsx
  - web/src/chat/ReasoningDrawer.tsx
  - web/src/chat/ToolActivityCard.tsx
  - web/src/chat/__tests__/ReasoningDrawer.test.tsx
  - web/src/chat/__tests__/ToolActivityCard.test.tsx
  - web/src/AppShell.tsx
  - web/src/i18n/resources.ts
autonomous: false
requirements: [CHAT-01, CHAT-03]
user_setup: []
must_haves:
  truths:
    - "Operator types a prompt and watches the assistant response stream token-by-token over POST /agent/run (SSE) in the assistant-ui chat lane"
    - "Stop/interrupt aborts the active turn (ctx-cancel via the aborted fetch)"
    - "REASONING_* deltas render in a collapsible reasoning drawer with a persisted show/hide preference (D-01)"
    - "Each tool call shows name + status + an expandable raw text/JSON result blob (D-02, raw view only — no typed rendering)"
    - "A STATE_DELTA carrying tool_call_id is treated as a tool-result marker, never assistant prose (Pitfall 2)"
  artifacts:
    - path: "web/src/chat/sseAdapter.ts"
      provides: "POST-SSE fetch+ReadableStream → ThreadMessage[] reducer (frame→part mapping + usageFromStateDelta)"
      min_lines: 40
    - path: "web/src/chat/ExternalStoreChat.tsx"
      provides: "AssistantRuntimeProvider + useExternalStoreRuntime wiring over /agent/run"
    - path: "web/src/chat/ReasoningDrawer.tsx"
      provides: "Collapsible reasoning part with persisted preference (D-01)"
    - path: "web/src/chat/ToolActivityCard.tsx"
      provides: "Name + status dot + expandable mono raw blob (D-02)"
  key_links:
    - from: "web/src/chat/ExternalStoreChat.tsx"
      to: "POST /agent/run"
      via: "useExternalStoreRuntime onNew → fetch streaming + sseAdapter reducer"
      pattern: "useExternalStoreRuntime"
    - from: "web/src/AppShell.tsx"
      to: "ExternalStoreChat"
      via: "chatRegion section mount"
      pattern: "ExternalStoreChat"
---

<objective>
Build the Core-Value chat lane (CHAT-01) on assistant-ui's `useExternalStoreRuntime`,
plus the reasoning drawer (D-01) and the raw tool-activity card (D-02 — the CHAT-03
frontend half). This installs the net-new assistant-ui deps and the one genuinely new
frontend pattern: a POST-SSE `fetch` + `ReadableStream` reducer mapping the AG-UI event
stream onto assistant-ui's `ThreadMessage[]` parts.

Purpose: CHAT-01 is the milestone's Core-Value moment — the operator types a prompt and
watches the streamed answer. CHAT-03's drawer + tool card surface the reasoning + tool
activity the backend (plan 25-01 Task 2) now streams. The runtime adapter is
`useExternalStoreRuntime` (UI-SPEC RESOLVED, research-confirmed) because Aura's SSE is a
custom AG-UI shape and D-09 (plan 25-05) needs branch/edit/stop first-class.
Output: assistant-ui deps in `web/package.json`, `sseAdapter.ts` (+ test against golden
frames), `ExternalStoreChat.tsx`, `Composer.tsx`, `ReasoningDrawer.tsx`,
`ToolActivityCard.tsx`, the `AppShell` chat mount, and en+it copy.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-chat-approval-center/25-CONTEXT.md
@.planning/phases/25-chat-approval-center/25-RESEARCH.md
@.planning/phases/25-chat-approval-center/25-PATTERNS.md
@.planning/phases/25-chat-approval-center/25-UI-SPEC.md
@.planning/phases/25-chat-approval-center/25-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan:
- npm deps `@assistant-ui/react@0.14.22`, `@assistant-ui/react-markdown@0.14.4`, `assistant-stream@0.3.23` (exact-pinned)
- `web/src/chat/sseAdapter.ts` — `usageFromStateDelta(ops)` + the frame→part reducer
- `web/src/chat/ExternalStoreChat.tsx` — the runtime provider mounted into AppShell chatRegion
- `web/src/chat/Composer.tsx`, `ReasoningDrawer.tsx`, `ToolActivityCard.tsx`
- i18n keys `chat.*` (composer placeholder `Ask Aura`, Send/Stop, reasoning toggle, tool expander, running status, empty/error states) in en+it
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] assistant-ui package legitimacy verification before install</name>
  <what-built>Nothing yet — this gate verifies the three net-new npm packages BEFORE install per the package-legitimacy gate (RESEARCH ## Package Legitimacy Audit, all [OK] on npm but flagged here as a blocking pre-install human checkpoint per the security threat T-25-SC).</what-built>
  <read_first>
    - .planning/phases/25-chat-approval-center/25-RESEARCH.md ## Package Legitimacy Audit (the slopcheck `--ecosystem npm` footgun: run from `web/` or pass `-e npm`, NOT from the go.mod root which false-flags [SLOP])
  </read_first>
  <how-to-verify>
    1. Confirm each package on npmjs.com: https://www.npmjs.com/package/@assistant-ui/react (1.2M wk dl, repo github.com/assistant-ui/assistant-ui, MIT, ~2yr old), https://www.npmjs.com/package/@assistant-ui/react-markdown (509k wk), https://www.npmjs.com/package/assistant-stream (1.15M wk).
    2. Confirm none declare a network/filesystem `postinstall` (RESEARCH: empty `scripts.postinstall`).
    3. Optionally re-run `cd web && npx slopcheck@latest @assistant-ui/react @assistant-ui/react-markdown assistant-stream --ecosystem npm` → expect all [OK].
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the pinned install, or describe a concern.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install assistant-ui + SSE→ThreadMessage reducer (CHAT-01, the new pattern)</name>
  <files>web/package.json, web/package-lock.json, web/src/chat/sseAdapter.ts, web/src/chat/__tests__/sseAdapter.test.ts</files>
  <read_first>
    - web/src/health/useRuntimeHealth.ts:26-69 (same-origin fetch + React Query precedent), web/src/routes/LoginPage.tsx:32-37 (POST body + credentials)
    - internal/agui/translator.go (the SSE event sequence: TEXT_MESSAGE_*, REASONING_*, TOOL_CALL_START/ARGS/END/RESULT, STATE_DELTA usage + the tool_call_id marker at :325-336 `toolResultCallID`, :341 sorted-key ops), internal/agent/llm_agent_events.go:229 (`usageStateDelta`: prompt_tokens/completion_tokens/cache_hit_tokens/cost_usd)
    - internal/agui/testdata/golden-events.json (the captured real translator output — the test fixture; if absent, capture it from a real `Translate` run, NOT synthetic shapes)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"web/src/chat/sseAdapter.ts (NEW-PATTERN…)" + RESEARCH ## Architecture Patterns Pattern 1 (frame→part mapping) and the Code Examples `usageFromStateDelta`
    - assistant-ui.com/llms.txt (consult for the exact 0.14.22 ThreadMessage/MessagePart shape — RESEARCH A1, SKILL.md mandate)
  </read_first>
  <behavior>
    - Test (against golden-events.json): TEXT_MESSAGE_START/CONTENT/END → a single `{type:"text"}` part; double-streaming the final Event's content is NOT double-printed (translator treats final as END-only)
    - Test: REASONING_START/MESSAGE_*/END → a `{type:"reasoning"}` part (drawer-bound)
    - Test: TOOL_CALL_START(name)/ARGS/END/RESULT → a tool-call part with the raw result preview; a STATE_DELTA carrying `tool_call_id` is routed to the tool part, NEVER an assistant text delta (Pitfall 2)
    - Test: STATE_DELTA with usage keys → `usageFromStateDelta` returns {promptTokens, completionTokens, cacheHitTokens, costUsd}; missing cost_usd → costUsd undefined; cache% guards /0
    - Test: RUN_ERROR (sanitized) → an error part / incomplete status
  </behavior>
  <action>
    `cd web && npm install --save-exact @assistant-ui/react@0.14.22 @assistant-ui/react-markdown@0.14.4 assistant-stream@0.3.23` (exact-pinned; the Phase-23 supply-chain gate + `allowScripts` allow-list governs). Create `web/src/chat/sseAdapter.ts`: a pure reducer mapping the AG-UI SSE frame sequence onto assistant-ui `MessagePart[]` per RESEARCH Pattern 1 (frame→part table) + `usageFromStateDelta(ops)` reading the JSONPatch ops at `/prompt_tokens` etc. Use a `fetch` + `ReadableStream` reader loop (NOT `EventSource` — it cannot POST a body) with an `AbortController` (the stop affordance). CRITICAL: trust the event TYPE not the content — a `STATE_DELTA` with `tool_call_id` is a tool-result marker, never assistant prose (Pitfall 2). Write `sseAdapter.test.ts` (Vitest) driven by the REAL captured `internal/agui/testdata/golden-events.json` frames (no synthetic shapes — no-skip-as-green discipline). Do NOT render UI in this task — pure reducer + parse.
  </action>
  <verify>
    <automated>cd /d/Aura/web && npm run lint && npm run typecheck && npm run test -- sseAdapter</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `web/package.json` pins `@assistant-ui/react` at exactly `0.14.22` (no `^`/`~`) — `grep '"@assistant-ui/react": "0.14.22"' web/package.json`
    - Behavior: `npm run test -- sseAdapter` PASSES and the test imports the golden fixture (asserted: the test references `golden-events.json`, not inline synthetic frames)
    - Behavior: the Pitfall-2 test asserts a `tool_call_id` STATE_DELTA produces a tool part, NOT a text delta
    - Behavior: `usageFromStateDelta` /0 cache-% guard test passes (promptTokens=0 → no NaN)
    - `npm run typecheck` clean
  </acceptance_criteria>
  <done>The deps are exact-pinned, the SSE reducer maps every frame type correctly against real golden frames, the tool-result marker is disambiguated, and usage parsing guards /0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chat lane — runtime provider, composer (Send↔Stop), reasoning drawer, tool card (CHAT-01/03 + D-01/D-02)</name>
  <files>web/src/chat/ExternalStoreChat.tsx, web/src/chat/Composer.tsx, web/src/chat/ReasoningDrawer.tsx, web/src/chat/ToolActivityCard.tsx, web/src/chat/__tests__/ReasoningDrawer.test.tsx, web/src/chat/__tests__/ToolActivityCard.test.tsx, web/src/AppShell.tsx, web/src/i18n/resources.ts</files>
  <read_first>
    - web/src/AppShell.tsx:48 (the empty center `<section aria-label={t('shell.chatRegion')}>` to fill), :2 + :54 (how RuntimeHealthPanel is composed)
    - web/src/health/RuntimeHealthPanel.tsx:23-42 (StatusDot + StatusRow — the tool-card status-dot + mono pattern), :128 (status cluster)
    - web/src/routes/LoginPage.tsx:102-144 (`aria-pressed` show/hide toggle for the reasoning drawer), :157 (send button), :98 (`aria-invalid` omit-when-valid)
    - web/src/chat/sseAdapter.ts (Task 2 output)
    - web/src/i18n/resources.ts:1-43 (en+it bundle shape)
    - .planning/phases/25-chat-approval-center/25-UI-SPEC.md §Component Inventory + §Copywriting Contract (`Ask Aura`, Send/Stop, reasoning toggle, tool expander) + §Color (accent reserved list; tool card stays neutral) + §Interaction & Accessibility Contract
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"ExternalStoreChat.tsx + BranchPicker.tsx" + §"RuntimeFooter.tsx + ToolActivityCard.tsx" + §"a11y component idioms"
    - assistant-ui.com/llms.txt (exact 0.14.22 useExternalStoreRuntime / ThreadPrimitive / ComposerPrimitive / MessagePrimitive.Parts API)
  </read_first>
  <behavior>
    - Test (ReasoningDrawer): collapsed by default unless the persisted preference says shown; toggling updates the persisted preference; `aria-pressed`/`aria-expanded` reflect state; reasoning text renders only when expanded
    - Test (ToolActivityCard): renders tool name + a status dot (icon+text, never color alone) + a collapsed raw blob; the expander (`aria-label="Show raw result"`) reveals the mono raw text; the raw blob renders as text/mono, NEVER dangerouslySetInnerHTML (XSS guard)
    - Test (ToolActivityCard): renders the RAW result only — no typed `switch(payload.type)` branch (Phase 26 is out of scope)
  </behavior>
  <action>
    Build `ExternalStoreChat.tsx`: an `AssistantRuntimeProvider` over `useExternalStoreRuntime`,
    feeding the Task-2 reducer's `ThreadMessage[]`; wire `onNew` to POST `/agent/run`
    (`credentials: 'same-origin'`) streaming via the sseAdapter, and `onCancel` to the
    AbortController (stop = `ComposerPrimitive.Cancel` → `api.thread().cancelRun()` → abort the
    fetch; the server `streamSSE` unwinds cleanly on ctx.Done — server.go:274). Mount it into the
    AppShell center `chatRegion` `<section>` (currently empty). `Composer.tsx`: `ComposerPrimitive`
    with the `Ask Aura` placeholder, Enter sends / Shift+Enter newlines / Esc cancels, and the
    Send↔Stop swap on `isRunning` (44×44 touch target). `ReasoningDrawer.tsx`: a collapsible
    (`<details>`/`aria-expanded`) reasoning part fed by the `MessagePrimitive.Parts` reasoning
    render-fn (D-01), with a persisted show/hide preference (localStorage, builder default = shown).
    `ToolActivityCard.tsx`: reuse the `StatusDot`/mono pattern — name + status dot + an expandable
    mono raw text/JSON blob (D-02), rendered as TEXT/mono only, never `dangerouslySetInnerHTML`
    (untrusted tool output, HARDEN-08); NO typed rendering (Phase 26). Assistant text parts render
    through `@assistant-ui/react-markdown` `MarkdownTextPrimitive` (sanitized); the raw tool blob
    stays plain mono. Add ALL copy to BOTH en+it in `resources.ts` under `chat.*` (UI-SPEC en
    contract is the source of truth). Streaming text lives in an `aria-live="polite"` region; the
    running-status row is `role="status"` with the active tool name. Do NOT build the branch picker
    or footer here (plans 25-05 / 25-04). Leave a documented integration seam for both.
  </action>
  <verify>
    <automated>cd /d/Aura/web && npm run lint && npm run typecheck && npm run test -- ReasoningDrawer ToolActivityCard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `AppShell.tsx` mounts `ExternalStoreChat` in the chatRegion section (`grep -c "ExternalStoreChat" web/src/AppShell.tsx` ≥ 1)
    - Source assertion: NO `dangerouslySetInnerHTML` in `ToolActivityCard.tsx` (`grep -c "dangerouslySetInnerHTML" web/src/chat/ToolActivityCard.tsx` == 0) — XSS guard
    - Source assertion: every new user-facing string has BOTH an `en` and `it` entry in `resources.ts` (reviewer-checkable: each new `chat.*` key appears under both bundles)
    - Behavior: `npm run test -- ReasoningDrawer ToolActivityCard` PASSES; the drawer toggle persists; the tool card expander reveals mono raw text
    - `npm run build` succeeds (dist rebuilt after copy changes)
    - Frontend coverage on the touched chat files ≥85% (vitest --coverage gate)
  </acceptance_criteria>
  <done>The chat lane streams over /agent/run with Send↔Stop, the reasoning drawer persists its preference, the tool card shows the raw blob safely (no XSS, no typed rendering), copy is en+it, and dist is rebuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → React render | Streamed assistant text + raw tool-result blobs (untrusted tool/swarm output) cross into the DOM |
| npm registry → web build | Three net-new assistant-ui packages enter the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-11 | Tampering/Injection | XSS via streamed assistant text or the raw tool blob | mitigate | Raw tool blob renders as text/mono, NEVER `dangerouslySetInnerHTML` (asserted by grep test); markdown goes through `MarkdownTextPrimitive` (sanitized); tool output is default-untrusted (HARDEN-08) |
| T-25-12 | Tampering | tool-result preview mis-rendered as assistant prose (double-print) | mitigate | Trust the event TYPE — `STATE_DELTA` with `tool_call_id` is a tool marker (Pitfall 2), asserted in the sseAdapter test |
| T-25-13 | Denial of Service | giant SSE / raw blob floods the renderer | accept | Existing `maxRunBodyBytes` body cap + the SSE pump drop-on-full backpressure (server.go:301); tool-result spillover already capped (sidecar) — backend-side, unchanged |
| T-25-SC | Tampering | the 3 net-new npm installs (slopsquat / malicious postinstall) | mitigate | [BLOCKING] human verification (Task 1) on npmjs.com + slopcheck `-e npm` [OK]; exact-pinned versions; no network postinstall; Phase-23 supply-chain gate + `allowScripts` allow-list |
</threat_model>

<verification>
- `npm run test -- sseAdapter ReasoningDrawer ToolActivityCard` green (golden-frame reducer + drawer + tool card)
- `npm run lint && npm run typecheck && npm run build` clean
- `npm run test` coverage ≥85% on the new chat files
- Manual: a real `aura serve` turn streams token-by-token; Stop aborts mid-stream (covered by plan 25-06 E2E)
</verification>

<success_criteria>
- CHAT-01: the operator types a prompt and watches the streamed answer over /agent/run; Stop aborts the active turn
- CHAT-03 (frontend half): reasoning drawer (collapsible, persisted) + raw tool-activity card render the backend stream
- D-02: tool output is name + collapsed/expandable RAW result only (no typed rendering)
- assistant-ui deps exact-pinned + legitimacy-verified; coverage ≥85%
</success_criteria>

<output>
Create `.planning/phases/25-chat-approval-center/25-03-SUMMARY.md` when done
</output>
