---
phase: 25-chat-approval-center
plan: 06
type: execute
wave: 6
depends_on: ["25-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - internal/db/migrations/0017_conversation_turn_branches.up.sql
  - internal/db/migrations/0017_conversation_turn_branches.down.sql
  - internal/db/migrations/migrate_test.go
  - internal/db/queries/conversation_turns.sql
  - internal/conversations/store_branch.go
  - internal/conversations/store_branch_test.go
  - internal/conversations/context.go
  - internal/conversations/context_branch_test.go
autonomous: true
requirements: [CHAT-05]
must_haves:
  truths:
    - "ROADMAP + REQUIREMENTS record CHAT-05 (the conversation branch-tree requirement) BEFORE any branch code (PRD-first)"
    - "Migration 0017 adds parent/branch pointer columns to aura.conversation_turns and defaults existing rows into ONE canonical linear branch"
    - "Path-aware LoadManagedHistory reconstructs a deterministic root→leaf turn list for the selected branch"
    - "For a non-branched conversation, LoadHistory / LoadManagedHistory stay byte-identical to the pre-0017 linear case"
    - "The KV-cache stable-prefix invariant holds: messages[0] and the always-block are byte-identical across branches (cache_invariant_audit.sh green)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CHAT-05 branch-tree requirement + the requirement-coverage table row"
      contains: "CHAT-05"
    - path: "internal/db/migrations/0017_conversation_turn_branches.up.sql"
      provides: "additive parent_seq/branch_id columns with default backfill (tx-safe ALTER)"
      contains: "ALTER TABLE aura.conversation_turns"
    - path: "internal/conversations/store_branch.go"
      provides: "branch-pointer writes + the deterministic leaf→root path walk"
      contains: "func"
    - path: "internal/conversations/context.go"
      provides: "LoadManagedHistory extended to walk a selected branch path"
  key_links:
    - from: "internal/conversations/context.go"
      to: "store_branch path walk"
      via: "LoadManagedHistory selects the branch leaf then reconstructs root→leaf"
      pattern: "branch|parent_seq"
    - from: "internal/conversations/store_branch.go"
      to: "scripts/cache_invariant_audit.sh"
      via: "byte-identical protected head preserved across branches"
      pattern: "LoadHistory|protected"
---

<objective>
Lay the D-09 conversation branch-tree FOUNDATION: record the PRD-first ROADMAP/REQUIREMENTS
amendment (CHAT-05) BEFORE any branch code, ship migration 0017 (additive branch pointers on
`aura.conversation_turns` defaulting existing rows into one canonical branch), and extend the
history loaders to walk a *selected* branch path deterministically — WITHOUT breaking the
`messages[0]` KV-cache stable-prefix invariant (CAP-04, CI-gated).

Purpose: D-09 is the ONE genuinely-new backend mechanism in Phase 25 and the highest risk.
Per CLAUDE.md PRD-first discipline and the CONTEXT `## Deferred Ideas` scope note, the
amendment is recorded first. The path walk reuses the shipped byte-identity + protected-head
invariants; the new code is the parent/branch-pointer topology + the deterministic walk. The
cache-invariant audit is a [BLOCKING] gate — branching must not poison the OpenRouter prompt
cache.
Output: the CHAT-05 amendment, migration 0017 (up/down + test), `store_branch.go`, the
extended `LoadManagedHistory`, and a green `scripts/cache_invariant_audit.sh`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-chat-approval-center/25-CONTEXT.md
@.planning/phases/25-chat-approval-center/25-RESEARCH.md
@.planning/phases/25-chat-approval-center/25-PATTERNS.md
@prd.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan:
- Requirement `CHAT-05` (conversation branch trees) in ROADMAP.md Phase 25 + REQUIREMENTS.md
  (description, scope note, coverage-table row → Phase 25)
- Migration `0017_conversation_turn_branches.{up,down}.sql` — additive `parent_seq` + `branch_id`
  (or equivalent N-sibling pointer model) columns with a tx-safe default backfill into one canonical branch
- sqlc queries on `conversation_turns.sql` for the branch-pointer writes + the path walk
- `internal/conversations/store_branch.go` — branch-pointer write + the deterministic leaf→root walk
- `LoadManagedHistory` extended with an optional selected-branch parameter (default = canonical branch)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: PRD-first amendment — record CHAT-05 branch-tree requirement BEFORE any code</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md:121-135 (Phase 25 block — Requirements + Success Criteria to extend)
    - .planning/REQUIREMENTS.md:48-57 (CHAT-01..04 + APRV-01..03 shape), :167-173 (the requirement-coverage table), :207 (the per-phase requirement roll-up)
    - .planning/phases/25-chat-approval-center/25-CONTEXT.md D-09 + `## Deferred Ideas` "Scope note (NOT deferred)" (the explicit mandate to amend first)
    - CLAUDE.md §PRD-first principle (no code without PRD/amendment)
  </read_first>
  <action>
    Record the branch-tree scope expansion as `CHAT-05` BEFORE writing any branch code (PRD-first).
    In REQUIREMENTS.md add `CHAT-05: Operator can branch/version messages (edit a user turn or
    regenerate an assistant turn produces a navigable branch tree) over a path-aware conversation
    history, preserving the messages[0] KV-cache invariant` to the CHAT requirement list, add its
    coverage-table row (`CHAT-05 | Phase 25 | Pending`), and bump the Phase-25 requirement count in the
    roll-up (line ~207, now 8). In ROADMAP.md Phase 25, add `CHAT-05` to the Requirements line and add a
    Success Criterion (e.g. "Operator can edit/regenerate a message producing a navigable branch tree
    over path-aware history, with the messages[0] KV-cache invariant preserved (cache-invariant gate
    green)"). Note in both that D-09 is a deliberate operator-chosen scope addition (CONTEXT D-09). Do NOT
    write any migration/code in this task — the amendment lands first as its own change.
  </action>
  <verify>
    <automated>cd /d/Aura && grep -c "CHAT-05" .planning/REQUIREMENTS.md && grep -c "CHAT-05" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "CHAT-05" .planning/REQUIREMENTS.md` ≥ 2 (description + coverage row)
    - Source assertion: `grep -c "CHAT-05" .planning/ROADMAP.md` ≥ 1 (Requirements line)
    - The Phase-25 requirement roll-up count is updated to 8
    - The amendment is a standalone change recorded BEFORE the migration/code tasks (PRD-first — reviewer-checkable commit ordering)
  </acceptance_criteria>
  <done>CHAT-05 is recorded in ROADMAP + REQUIREMENTS with a coverage row and an updated count, before any branch code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0017 — additive branch pointers + default backfill (D-09)</name>
  <files>internal/db/migrations/0017_conversation_turn_branches.up.sql, internal/db/migrations/0017_conversation_turn_branches.down.sql, internal/db/migrations/migrate_test.go, internal/db/queries/conversation_turns.sql</files>
  <read_first>
    - internal/db/migrations/0005_conversations.up.sql:23-36 (the `conversation_turns` table + PK `(conversation_id, seq)`), :60-65 (GRANT style)
    - internal/db/migrations/0006_conversation_turns_fts.up.sql:1-8 (the CONCURRENTLY-must-be-its-own-file rule — Pitfall 5)
    - internal/db/migrations/0003_paused_states.up.sql:26-27 (GRANT to aura_app/aura_migrate)
    - internal/db/queries/conversation_turns.sql:13 (`NextConversationTurnSeq`), :18 (`ListTurnsBySeq`)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"0017_conversation_turn_branches" + RESEARCH Pitfall 5 (CONCURRENTLY split) + Runtime State note + A3 (existing-row default backfill keeps LoadHistory byte-identical)
  </read_first>
  <behavior>
    - Test: `migrate up` to 0017 then `migrate down` round-trips cleanly (no `CREATE INDEX CONCURRENTLY` in a tx block)
    - Test: existing `conversation_turns` rows are defaulted into ONE canonical linear branch (parent = previous seq, a default branch id) so a pre-0017 conversation loads byte-identically
    - Test: the new columns are NOT NULL with a default (or nullable-with-default) such that `ListTurnsBySeq` + `LoadHistory` for a non-branched conversation are unchanged
  </behavior>
  <action>
    Write `0017_conversation_turn_branches.up.sql` as a tx-safe `ALTER TABLE aura.conversation_turns
    ADD COLUMN … DEFAULT …` adding the branch/parent pointers (e.g. `parent_seq integer` + `branch_id`
    with a default, supporting N siblings under one parent — RESEARCH OQ3 full tree). DEFAULT existing
    rows into one canonical linear branch (parent_seq = previous seq, a default branch id) so non-branched
    conversations keep byte-identical `LoadHistory` (RESEARCH A3). Mirror the `GRANT SELECT,INSERT,UPDATE,
    DELETE … TO aura_app` / `GRANT ALL … TO aura_migrate` style (0005:60-65 / 0003:26-27). If a non-trivial
    index on the new column is needed on a large table, split the `CREATE INDEX CONCURRENTLY` into its OWN
    single-statement migration file (the 0006 precedent — Pitfall 5); a plain ALTER backfill stays in 0017.
    Write the `.down.sql` to DROP the columns. Add the branch-pointer write + path-walk queries to
    `conversation_turns.sql` (sqlc style mirroring `ListTurnsBySeq`); run `make sqlc`. Extend
    `migrate_test.go` (db_integration) to assert the up/down round-trip + the existing-row default backfill.
  </action>
  <verify>
    <automated>cd /d/Aura && make sqlc && go build ./internal/db/... && go test -tags db_integration ./internal/db/ -run TestMigrate -race</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "ALTER TABLE aura.conversation_turns" internal/db/migrations/0017_conversation_turn_branches.up.sql` ≥ 1 (tx-safe additive)
    - Source assertion: no `CREATE INDEX CONCURRENTLY` inside the multi-statement 0017 file (`grep -c "CONCURRENTLY" 0017…up.sql` == 0 unless it is the ONLY statement in its own file — Pitfall 5)
    - Behavior: `go test -tags db_integration ./internal/db/ -run TestMigrate` PASSES with up/down round-trip + the default-backfill assertion (real DB, >1s)
    - Behavior: a pre-0017 (default-backfilled) conversation's `LoadHistory` is byte-identical to the linear case (asserted here or in Task 3)
  </acceptance_criteria>
  <done>Migration 0017 adds branch pointers tx-safely, defaults existing rows into one canonical branch, round-trips up/down, and keeps non-branched LoadHistory byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Path-aware history walk + [BLOCKING] cache-invariant audit (D-09 / Pitfall 3)</name>
  <files>internal/conversations/store_branch.go, internal/conversations/store_branch_test.go, internal/conversations/context.go, internal/conversations/context_branch_test.go</files>
  <read_first>
    - internal/conversations/store.go:250-269 (`LoadHistory` byte-identity contract), :255 (LoadHistory), :262 (`turnToMessage`), :268 (`repairToolMessagePairs`), :273 (`loadTurns`), :278 (`ListTurnsBySeq`)
    - internal/conversations/context.go:37-47 (messages[0] is branch-independent BY CONSTRUCTION — rebuilt per turn, preserve this), :137 (`LoadManagedHistory`), :305-318 (`dropOldestPairs` protected-head: seq=1 system + the always-block — never dropped)
    - internal/db/queries/conversation_turns.sql (the Task-2 branch-pointer + path-walk queries)
    - scripts/cache_invariant_audit.sh (the CAP-04 messages[0]/[1] stable-prefix gate — 22 byte-stable hashes, Postgres-free, CI-blocking)
    - .planning/phases/25-chat-approval-center/25-PATTERNS.md §"store_branch.go + context.go (D-09 — NEW topology)" + RESEARCH Pitfall 3 (the four how-to-avoid rules)
  </read_first>
  <behavior>
    - Test: a deterministic leaf→root walk produces the root→leaf ordered turn list for the SELECTED branch; two consecutive calls are byte-identical (pure function of the persisted rows — the LoadHistory contract extended)
    - Test (Pitfall 3, the load-bearing invariant): for ANY branch selection, the protected head (system seq=1 + the always-block) is byte-identical to the linear case — only the body turns differ per branch
    - Test: a non-branched conversation (default canonical branch) loads byte-identically to the pre-0017 path (regression guard)
    - Test: `repairToolMessagePairs` still pairs tool turns correctly along a branch path
  </behavior>
  <action>
    Create `store_branch.go` with the deterministic path walk: given a selected leaf (or the default
    canonical branch leaf), walk parent pointers leaf→root and reverse to produce a root→leaf ordered
    `[]Turn` whose head (system + always-block) is byte-identical to the linear case — only the body turns
    differ per branch (Pitfall 3 rule 1). Reuse `loadTurns`/`turnToMessage`/`repairToolMessagePairs`
    UNCHANGED — the new code is ONLY the parent/branch-pointer walk. Extend `LoadManagedHistory`
    (context.go:137) to accept the selected branch (default = canonical) and feed the walked turn list into
    the existing microcompact ladder; PRESERVE the messages[0] rebuilt-per-turn / branch-independent
    construction (context.go:37-47, Pitfall 3 rule 2) and the protected-head split (context.go:305-318) —
    do NOT re-order or rebuild the head from a persisted branch turn. After wiring, run `scripts/
    cache_invariant_audit.sh` ([BLOCKING] — it must stay green; it is Postgres-free, 22 byte-stable
    `request NN:` hashes, CI-blocking). Write `store_branch_test.go` + `context_branch_test.go`
    (db_integration) asserting the deterministic walk + the byte-identical protected head across branches +
    the non-branched regression.
  </action>
  <verify>
    <automated>cd /d/Aura && go vet ./internal/conversations/ && go build ./internal/conversations/ && go test -tags db_integration ./internal/conversations/ -run 'TestBranchPath|TestBranch' -race && bash scripts/cache_invariant_audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - [BLOCKING] `bash scripts/cache_invariant_audit.sh` EXITS 0 (22 byte-stable hashes; reports NO "messages[0] mutated at request N") — this is the CAP-04 gate and is non-negotiable
    - Behavior: the protected-head byte-identity test PASSES — for any branch, system seq=1 + the always-block are byte-identical to the linear case (Pitfall 3)
    - Behavior: a non-branched conversation loads byte-identically to the pre-0017 path (regression guard asserted)
    - Behavior: two consecutive path-walk calls return byte-identical slices (the extended LoadHistory contract)
    - `go test -tags db_integration ./internal/conversations/ -run TestBranch` PASSES with real-DB runtime
    - Go mutation spot-check ≥70% on `store_branch.go` (the new path loader) — documented in 25-VALIDATION.md
  </acceptance_criteria>
  <done>The path-aware walk reconstructs the selected branch deterministically, the protected head stays byte-identical across branches, the non-branched case is unchanged, and the cache-invariant audit is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Branch selection → history reconstruction | A selected branch leaf drives which turns form messages[1..N] |
| Migration 0017 → existing durable rows | The migration touches a table with live rows in any deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-21 | Tampering | branch reconstruction mutates messages[0] → poisons the KV-cache stable prefix | mitigate | [BLOCKING] `cache_invariant_audit.sh` green; messages[0] rebuilt-per-turn/branch-independent (Pitfall 3 rules 1-2); the path walk only varies the body, never the protected head |
| T-25-22 | Denial of Service (data) | migration 0017 orphans existing rows (no default branch) → path walk sees orphans | mitigate | The ALTER defaults existing rows into ONE canonical linear branch (RESEARCH A3 / Runtime State note); the non-branched byte-identity regression test guards it |
| T-25-23 | Tampering | `CREATE INDEX CONCURRENTLY` in the multi-statement migration aborts the tx | mitigate | A plain `ALTER … ADD COLUMN … DEFAULT` is tx-safe; any concurrent index is split into its OWN single-statement file (Pitfall 5 / 0006 precedent) |
| T-25-SC | Tampering | go/sqlc tooling | mitigate | This plan adds NO new third-party dependency (sqlc + golang-migrate already in the toolchain) |
</threat_model>

<verification>
- `go test -tags db_integration ./internal/db/ -run TestMigrate -race` (0017 up/down + backfill)
- `go test -tags db_integration ./internal/conversations/ -run TestBranch -race` (deterministic walk + protected head)
- `bash scripts/cache_invariant_audit.sh` exits 0 ([BLOCKING] CAP-04 gate)
- `grep -c CHAT-05 .planning/REQUIREMENTS.md .planning/ROADMAP.md` (PRD-first amendment recorded)
- `make quality` clean for touched packages; Go mutation ≥70% on store_branch.go
</verification>

<success_criteria>
- CHAT-05 amendment recorded in ROADMAP + REQUIREMENTS BEFORE any branch code (PRD-first)
- Migration 0017 adds branch pointers tx-safely + defaults existing rows into one canonical branch
- Path-aware LoadManagedHistory reconstructs the selected branch deterministically
- Non-branched conversations stay byte-identical; the cache-invariant audit is green (the highest-risk gate)
</success_criteria>

<output>
Create `.planning/phases/25-chat-approval-center/25-06-SUMMARY.md` when done
</output>
