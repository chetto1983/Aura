---
phase: 37-per-user-full-capability-sandbox
plan: 04
type: execute
wave: 2
depends_on: ["37-01", "37-02"]
files_modified:
  - internal/sandbox/usersandbox/docker_backend.go
  - internal/sandbox/usersandbox/docker_backend_lifecycle.go
  - internal/sandbox/usersandbox/docker_backend_exec.go
  - internal/sandbox/usersandbox/materialize.go
  - internal/sandbox/usersandbox/docker_backend_integration_test.go
  - internal/sandbox/usersandbox/lifecycle_integration_test.go
autonomous: true
requirements: [SBX-03, SBX-01]
must_haves:
  truths:
    - "A full moby v0.4.1 SDK round-trip works against a live dockerd: image pull → named volume → create (AutoRemove=false) → exec → cp in → cp out → suspend → resume → stop."
    - "Suspend stops the box but RETAINS the named volume; Resume restarts the same container against the same volume; Stop(Delete) removes container + volume (D-02 lifecycle: Resolve=direct create, idle=Suspended) — proven by an integration test."
    - "Two identities get two named volumes aura-box-<id>; identity B cannot read A's /workspace file (storage-enforced cross-deny) — proven by an integration test."
    - "The box exec env is scrubbed of secret-like vars exactly as the host mergeEnv path does (secret.IsSecretEnvVar)."
    - "Resolve CALLS MaterializeIn at both create AND resume (docker cp tar stream), so per-identity skills / Agent.md / pyscripts are present in the box volume every time it is (re)opened — never host-bound; a routed-written /workspace artifact is retrievable via CopyArtifactsOut — proven by an integration test."
  artifacts:
    - path: "internal/sandbox/usersandbox/docker_backend.go"
      provides: "DockerBackend implementing Backend over moby/moby/client (Resolve/Suspend/Resume/Stop)"
      exports: ["DockerBackend", "NewDockerBackend"]
    - path: "internal/sandbox/usersandbox/docker_backend_exec.go"
      provides: "Exec via ExecCreate/ExecAttach/stdcopy demux/ExecInspect + secret env scrub"
      contains: "IsSecretEnvVar"
    - path: "internal/sandbox/usersandbox/materialize.go"
      provides: "docker cp in (skills/Agent.md/pyscripts tar stream) + copy artifacts out (D-10)"
      exports: ["MaterializeIn", "CopyArtifactsOut"]
  key_links:
    - from: "internal/sandbox/usersandbox/docker_backend.go"
      to: "internal/sandbox/usersandbox/translate.go"
      via: "Resolve builds the container via toHostConfig (SBX-02 pins)"
      pattern: "toHostConfig"
    - from: "internal/sandbox/usersandbox/docker_backend.go"
      to: "aura-box-<identity> named volume"
      via: "VolumeCreate idempotent-by-name per identity"
      pattern: "aura-box-"
    - from: "internal/sandbox/usersandbox/docker_backend_lifecycle.go"
      to: "internal/sandbox/usersandbox/materialize.go"
      via: "Resolve invokes MaterializeIn at create and at resume (D-10)"
      pattern: "MaterializeIn\\("
  prohibitions:
    - "MUST NOT create boxes with AutoRemove/`--rm` — that destroys a suspendable box (Pitfall 5)."
    - "MUST NOT drive docker cp/exec by shelling a `docker` CLI command — use the Go SDK tar stream (Pitfall 6 MSYS mangling)."
    - "MUST NOT use windowsShell()/`pwd -W` in the box-exec branch — POSIX `/bin/sh -c` only (Pitfall 6)."
    - "MUST NOT leak host secret-like env into the box exec (reuse secret.IsSecretEnvVar scrub)."
    - "MUST NOT use app-prefix scoping for /workspace isolation — named volume per identity (spike 078 non-negotiable)."
    - "MUST NOT define MaterializeIn/CopyArtifactsOut as dead code — Resolve MUST call MaterializeIn (create+resume); CopyArtifactsOut is called by send_file in 37-07."
---

<objective>
Build the genuinely-new work: `DockerBackend` — the first `moby/moby/client` v0.4.1 caller in
the repo — implementing the `Backend` E2B verbs over the Docker Engine API (the SDK binding the
spikes never exercised; they proved the *model* via the CLI, RESEARCH Pitfall 7). Deliver the
per-identity named-volume lifecycle (create → suspend-retain → resume → delete), the secret-
scrubbed exec, and `materialize.go` (docker cp skills/Agent.md/pyscripts in as tar streams, copy
artifacts out — D-10, since host bind-mounts are unrepresentable), and **wire MaterializeIn into
Resolve at create and resume** so materialization is not dead code. Prove it end-to-end against a
live dockerd, including the cross-identity volume cross-deny (SBX-03) and the materialize-at-resolve
call site.

Purpose: this is the box runtime everything routes into. Correctness of the v0.4.1 options-struct
API (Pitfall 1), the suspend-retain lifecycle (Pitfall 5), storage-enforced isolation
(spike 078), and the materialize call site (D-10 — without it a routed fs_write artifact never
reaches the box and send_file can't copy it out) are the load-bearing risks — retire them here
with real-dockerd tests before any tool routes into a box.
Output: `docker_backend*.go` + `materialize.go` (called from Resolve) + the docker_integration lifecycle/cross-deny/materialize suite.
</objective>

## Artifacts This Phase Produces (this plan's slice)

- `DockerBackend` (implements `Backend`), `NewDockerBackend(cli, imageRef, limits)`.
- `docker_backend_lifecycle.go` (Resolve/Suspend/Resume/Stop over VolumeCreate/ContainerCreate/Stop/Start/Remove; Resolve calls MaterializeIn), `docker_backend_exec.go` (Exec).
- `MaterializeIn`, `CopyArtifactsOut` (tar-stream docker cp helpers) — `MaterializeIn` wired into Resolve; `CopyArtifactsOut` consumed by send_file in 37-07.
- Named volumes `aura-box-<identityID>` + the shared `aura-uv-cache` volume.

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-per-user-full-capability-sandbox/37-CONTEXT.md
@.planning/phases/37-per-user-full-capability-sandbox/37-RESEARCH.md
@.planning/phases/37-per-user-full-capability-sandbox/37-PATTERNS.md
@.planning/phases/37-per-user-full-capability-sandbox/37-VALIDATION.md
@.planning/phases/37-per-user-full-capability-sandbox/37-01-SUMMARY.md
@.planning/phases/37-per-user-full-capability-sandbox/37-02-SUMMARY.md
@internal/agent/tools/shell_exec.go
@.claude/skills/spike-findings-Aura/references/multiuser-per-identity-isolation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DockerBackend lifecycle over moby v0.4.1 + the SDK round-trip smoke (Pitfall 1/7)</name>
  <files>internal/sandbox/usersandbox/docker_backend.go, internal/sandbox/usersandbox/docker_backend_lifecycle.go, internal/sandbox/usersandbox/docker_backend_integration_test.go</files>
  <read_first>
    - 37-01-SUMMARY.md "Moby v0.4.1 API surface" — the EXACT captured options-struct fields (ContainerCreateOptions/ExecCreateOptions/ExecAttachOptions/CopyToContainerOptions + HostConfig/Resources/NetworkMode). Use these, not the classic docker/docker/client signature.
    - RESEARCH Pattern 4 (37-RESEARCH.md lines 273-275) — Resolve=VolumeCreate(idempotent by name)+ContainerCreate; Suspend=ContainerStop; Resume=ContainerStart; Stop(Delete)=ContainerRemove+VolumeRemove; NEVER --rm.
    - RESEARCH Pitfall 5 (37-RESEARCH.md lines 357-361) — AutoRemove:false; Suspend=stop-but-retain; Resume=same container.
    - RESEARCH Pitfall 7 (37-RESEARCH.md lines 369-372) — the Wave-0 full SDK round-trip smoke against live dockerd (the earliest real proof).
    - 37-02-SUMMARY.md — the Backend verb contract + toHostConfig this implements against.
    - PATTERNS.md docker_backend.go section (lines 151-157) — no in-repo analog; source shape is RESEARCH Code Example.
  </read_first>
  <action>
    Create docker_backend.go: `DockerBackend{cli *client.Client; image string; limits Resources}` + `NewDockerBackend`. Implement Resolve in docker_backend_lifecycle.go: idempotent `VolumeCreate` for `aura-box-<IdentityID>` and the shared `aura-uv-cache`, then get-or-create the container from `toHostConfig(spec)` (37-02) with the fat image — if a Suspended container already exists for the identity, transparently `ContainerStart` (Resume) instead of re-creating (D-08). Implement Suspend=`ContainerStop` (volume untouched), Resume=`ContainerStart` (same container/volume), Stop=`ContainerRemove`+`VolumeRemove` (only the per-identity volume, never the shared uv-cache). Set AutoRemove:false in the create path (via toHostConfig). Add a `//go:build docker_integration` smoke test that does the full round-trip (pull → volume → create → exec `echo` → cp a file in → cp it out → suspend → resume → exec again → stop) against a live dockerd using skipUnlessDockerd. Split by verb if docker_backend.go approaches 600 LOC. Leave a clearly-marked MaterializeIn call site in Resolve for Task 2 to fill (create + resume), so the materialize helper is not dead code.
  </action>
  <verify>
    <automated>go build ./internal/sandbox/usersandbox/ && go test -tags docker_integration ./internal/sandbox/usersandbox/ -run "TestDockerBackend_RoundTrip" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The SDK round-trip smoke passes against a live dockerd (skips locally when dockerd is down, `t.Fatal`s under `$CI`).
    - Resolve is idempotent (second call returns the existing box, not a duplicate); AutoRemove is never set.
    - `go build ./internal/sandbox/usersandbox/` green; no file exceeds 600 LOC.
  </acceptance_criteria>
  <done>DockerBackend lifecycle over moby v0.4.1 + the SDK round-trip smoke (Pitfall 1/7) — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

<task type="auto">
  <name>Task 2: Secret-scrubbed Exec + materialize in/out WIRED INTO Resolve + lifecycle & cross-identity-deny tests (SBX-03)</name>
  <files>internal/sandbox/usersandbox/docker_backend_exec.go, internal/sandbox/usersandbox/materialize.go, internal/sandbox/usersandbox/docker_backend_lifecycle.go, internal/sandbox/usersandbox/lifecycle_integration_test.go</files>
  <read_first>
    - RESEARCH Code Example "Exec into the box" (37-RESEARCH.md lines 377-390) — ExecCreate/ExecAttach + stdcopy.StdCopy demux + ExecInspect for exit code, honoring ctx cancellation.
    - PATTERNS.md "Secret env scrub on the box exec path" (lines 248-254) — reuse shell_exec.go:433-454 mergeEnv → secret.IsSecretEnvVar verbatim on the box exec env.
    - PATTERNS.md docker_backend Windows→Linux caveat (line 157) — POSIX `/bin/sh -c`, tar-stream cp, AutoRemove:false.
    - CONTEXT D-10 (37-CONTEXT.md lines 84-86) — skills/Agent.md/pyscripts materialized into the volume AT create/resume; /workspace = named volume + tmpfs scratch; artifacts copied out for Telegram sendDocument.
    - RESEARCH Pattern 4 + Security Domain cross-vol row (37-RESEARCH.md line 561) — bucket/volume-per-identity storage-enforced; spike 078 A-writes/B-can't-read.
    - VALIDATION.md test map SBX-03 rows (37-VALIDATION.md lines 52-54) — TestVolume_CrossIdentityDeny, TestLifecycle_SuspendResumeDelete.
    - 37-04 Task 1's Resolve (the MaterializeIn call site marked in docker_backend_lifecycle.go) — this task fills it.
  </read_first>
  <action>
    Create docker_backend_exec.go: `Exec` via ExecCreate (Cmd `/bin/sh -c <command>`, WorkingDir, Env scrubbed via secret.IsSecretEnvVar exactly as shell_exec.go mergeEnv), ExecAttach, stdcopy.StdCopy demux into stdout/stderr buffers, ExecInspect for the exit code, honoring ctx cancellation (WaitDelay analog). Create materialize.go: `MaterializeIn(ctx, box, srcDirs)` tar-streams the identity's skills / Agent.md / pyscripts host dirs into the box volume via CopyToContainer (strip/reject symlinks per sandbox-runtime.md); `CopyArtifactsOut(ctx, box, /workspace/path)` via CopyFromContainer for send_file delivery. **Then WIRE MaterializeIn into Resolve** (docker_backend_lifecycle.go): call it after the container is created AND after a transparent resume of a Suspended box (D-10 — materialize at both create and resume), sourcing the identity's skills/Agent.md/pyscripts dirs; a materialize error fails Resolve closed (never silently skips). Add the lifecycle_integration_test.go (docker_integration): TestLifecycle_SuspendResumeDelete (volume retained across suspend/resume, gone after Stop(Delete)), TestVolume_CrossIdentityDeny (A writes /workspace/secret.txt, B's box cannot read it — separate named volumes), and TestResolve_MaterializesInputs (after Resolve the seeded skill/Agent.md/pyscript files are present at their in-box paths at create; after suspend→resume they are present again; a file written to /workspace is retrievable via CopyArtifactsOut). Add a `goleak` check in the package TestMain for the integration tier.
  </action>
  <verify>
    <automated>go test ./internal/sandbox/usersandbox/ -count=1 && go test -tags docker_integration -race ./internal/sandbox/usersandbox/ -run "TestLifecycle_SuspendResumeDelete|TestVolume_CrossIdentityDeny|TestExec_ScrubsSecretEnv|TestResolve_MaterializesInputs" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestVolume_CrossIdentityDeny proves B cannot read A's /workspace file (storage-enforced).
    - TestLifecycle_SuspendResumeDelete proves suspend retains the volume, resume reuses the same container, Stop(Delete) removes container+per-identity-volume (uv-cache survives).
    - TestResolve_MaterializesInputs proves Resolve materializes skills/Agent.md/pyscripts at create AND resume, and a /workspace artifact round-trips via CopyArtifactsOut (MaterializeIn/CopyArtifactsOut are NOT dead code).
    - The box exec env drops secret-like vars (unit-provable TestExec_ScrubsSecretEnv builds the env and asserts secrets absent).
    - `-race` clean; goleak green on the integration tier.
  </acceptance_criteria>
  <done>Secret-scrubbed Exec + materialize in/out WIRED INTO Resolve + lifecycle & cross-identity-deny tests (SBX-03) — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aura daemon → dockerd | the daemon holds the Docker client and creates per-identity boxes/volumes |
| box A volume ↔ box B volume | storage isolation must hold at the kernel/volume layer |
| host env → box exec env | secrets must not cross into the untrusted box |
| host source dirs → box volume | skills/Agent.md/pyscripts are copied IN (tar stream) at create/resume — never host-bound |

## STRIDE Threat Register (ASVS L1, block: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-CROSSVOL | Information Disclosure | shared-volume cross-identity read | mitigate | Named volume `aura-box-<id>` per identity (storage-enforced); TestVolume_CrossIdentityDeny proves cross-deny; never app-prefix scoping. |
| T-37-04-SECRETENV | Information Disclosure | host secret exfil via box env | mitigate | Scrub the box exec env with secret.IsSecretEnvVar (reuse mergeEnv); TestExec_ScrubsSecretEnv. |
| T-37-04-STARVE | Denial of Service | co-tenant resource exhaustion | mitigate | cgroup v2 caps (NanoCPUs/Memory/PidsLimit via toHostConfig, 2CPU/2GB/512pids start) — proven bounded spike 059; tuned by the D-14 benchmark (37-08). |
| T-37-04-RM | Tampering | `--rm` destroys a suspendable box | mitigate | AutoRemove:false pinned in toHostConfig; Suspend=stop-retain (Pitfall 5). |
| T-37-04-CPMSYS | Tampering | shelled docker cp path mangling / symlink escape | mitigate | Go SDK tar streams only (Pitfall 6); materialize strips/rejects symlinks (sandbox-runtime.md). |
| T-37-04-DEADCP | (integrity) | materialize defined but never invoked (D-10 undelivered) | mitigate | Resolve calls MaterializeIn at create+resume; TestResolve_MaterializesInputs proves the call site (not dead code); CopyArtifactsOut consumed by send_file (37-07). |
</threat_model>

<verification>
- `go test ./internal/sandbox/usersandbox/` green (unit).
- `go test -tags docker_integration -race ./internal/sandbox/usersandbox/` green on a live dockerd (round-trip + lifecycle + cross-deny + materialize-at-resolve).
- No file >600 LOC; `go vet` clean.
</verification>

<success_criteria>
- The per-identity box runtime works end-to-end against real dockerd with storage-enforced cross-identity isolation, the suspend-retain lifecycle, and materialize-at-resolve (skills/Agent.md/pyscripts present in the box, artifacts retrievable out) — SBX-03's volume + lifecycle legs are proven and D-10 is delivered (not dead code).
</success_criteria>

<output>
Create `.planning/phases/37-per-user-full-capability-sandbox/37-04-SUMMARY.md` when done — record the DockerBackend method signatures, the Resolve→MaterializeIn call site, and the CopyArtifactsOut seam for 37-05/37-07.
</output>
