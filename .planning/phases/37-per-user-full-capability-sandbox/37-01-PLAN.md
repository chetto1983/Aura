---
phase: 37-per-user-full-capability-sandbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - prd.md
  - .planning/ROADMAP.md
  - docker/aura-sandbox/Dockerfile
  - go.mod
  - go.sum
  - internal/config/config.go
  - internal/config/config_sandbox.go
  - internal/config/config_knobs.go
  - internal/config/config_sandbox_test.go
autonomous: true
requirements: [SBX-01, SBX-03, SBX-04]
must_haves:
  truths:
    - "REQUIREMENTS.md SBX-04 (and ROADMAP.md Phase 37 SC#4) record the D-06 amendment (default egress = full-internet-minus-internal, not --network none) AND the gVisor⊥nat mutual-exclusion, committed BEFORE any egress code lands."
    - "A digest-pinned fat sandbox image builds with a full shell, root user, writable rootfs, and python3/node/uv/git/gh/jq all resolving via `command -v`."
    - "moby/moby/client and moby/moby/api are direct go.mod requires; the exact v0.4.1 options-struct API surface is captured in the SUMMARY for the DockerBackend plan."
    - "The AURA_SANDBOX_* operator config surface (idle-TTL, CPU/memory/pids caps, egress allowlist, image ref) is defined as typed config fields + cataloged knobs (PROF env-catalog discipline) and parses cleanly; 37-05/37-06 read these values into specFor/buildEgressSidecar."
  artifacts:
    - path: "docker/aura-sandbox/Dockerfile"
      provides: "The shared fat full-capability box image (D-12/D-13)"
      contains: "FROM debian:bookworm-slim"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SBX-04 amendment (D-06 Gate-1 discipline)"
      contains: "full-internet-minus-internal"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 37 Success Criterion #4 reconciled to the D-06 default (full public internet minus the tenancy boundary, not --network none)"
      contains: "full public internet"
    - path: "go.mod"
      provides: "moby SDK promoted indirect→direct"
      contains: "github.com/moby/moby/client"
    - path: "internal/config/config_sandbox.go"
      provides: "SandboxConfig (AURA_SANDBOX_* operator knobs: idle-TTL, cgroup caps, egress allowlist, image) — the config surface 37-05/37-06 consume"
      contains: "AURA_SANDBOX_"
    - path: "internal/config/config_knobs.go"
      provides: "AURA_SANDBOX_* knob-registry rows (PROF env-catalog re-parse validation)"
      contains: "AURA_SANDBOX_IDLE_TTL_SEC"
  key_links:
    - from: "docker/aura-sandbox/Dockerfile"
      to: "docker/aura/Dockerfile"
      via: "reuses the proven fat runtime base (packaging-box.md)"
      pattern: "astral-sh/uv"
    - from: "internal/config/config_sandbox.go"
      to: "internal/config/config_knobs.go"
      via: "every AURA_SANDBOX_* field has a matching KnobSpec row (registry-is-the-engine)"
      pattern: "AURA_SANDBOX_"
  prohibitions:
    - "The sandbox image MUST NOT be distroless, non-root (USER directive), or read-only-rootfs — that breaks shell_exec parity (D-12, REVERT ec7fe2f6)."
    - "No egress-enforcement code is written in this plan — only the SBX-04 doc amendment (Gate-1: amendment precedes egress code)."
    - "Do not add docker/docker/client (classic SDK) — D-01 locks moby/moby/client."
    - "The AURA_SANDBOX_* knobs are DEFINED + cataloged + parse-tested here only — they are NOT wired into the router/spec in this plan (that is 37-05/37-06); do not touch internal/sandbox/."
    - "The idle-TTL / CPU / memory / pids knobs MUST be registered as KindInt (AURA_SANDBOX_IDLE_TTL_SEC is int seconds, matching the AURA_RUN_DIR_SWEEP_INTERVAL_SEC precedent) — a KindString registration gets NO reparsePass check and would silently fall back on a malformed value under server_production (the exact bug W-cfg fixes)."
---

<objective>
Lay the phase foundation and satisfy the mandatory PRD-first Gate-1 discipline: (1) amend
REQUIREMENTS.md/PRD to record the SBX-04 egress-default change (D-06) and the gVisor⊥nat
mutual-exclusion **before any egress code exists**; (2) build the shared fat full-capability
box image (D-12/D-13); (3) promote the two already-vendored moby modules to direct requires
and capture the exact v0.4.1 options-struct API surface so the DockerBackend plan (37-04) is
not writing against a guessed signature (RESEARCH Pitfall 1); (4) define + catalog the
`AURA_SANDBOX_*` operator config surface (idle-TTL, cgroup caps, egress allowlist, image ref)
following the PROF env-catalog discipline so the router/egress plans have a configured source
for `specFor`/`buildEgressSidecar`.

Purpose: honor CLAUDE.md PRD-first (no egress line of code before the amendment commit),
give every downstream plan a digest-pinned box image + a verified SDK + a configured operator
surface, and keep the box fat (isolation is the boundary, never crippling the inside).
Output: the REQUIREMENTS/PRD amendment, `docker/aura-sandbox/Dockerfile`, direct moby deps,
the `AURA_SANDBOX_*` config surface, and a captured moby API reference in the SUMMARY.
</objective>

## Artifacts This Phase Produces (this plan's slice)

- `docker/aura-sandbox/Dockerfile` — the shared fat box image (digest-pinned).
- `go.mod` direct requires: `github.com/moby/moby/client` v0.4.1, `github.com/moby/moby/api` v1.54.2.
- REQUIREMENTS.md SBX-04 amended text + a PRD amendment note + ROADMAP.md Phase 37 SC#4 reconciled to the D-06 default (no stale `--network none`).
- `internal/config` `SandboxConfig` (the `AURA_SANDBOX_*` operator knobs) + its `knobRegistry()` catalog rows + a parse test.
- SUMMARY-captured moby API surface: `client.ContainerCreateOptions`, `client.ExecCreateOptions`, `client.ExecAttachOptions`, `client.CopyToContainerOptions`, `container.HostConfig`, `container.Resources`, `network.NetworkMode` field lists (for 37-04); and the resolved `SandboxConfig` field list (for 37-05/37-06).

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-per-user-full-capability-sandbox/37-CONTEXT.md
@.planning/phases/37-per-user-full-capability-sandbox/37-RESEARCH.md
@.planning/phases/37-per-user-full-capability-sandbox/37-PATTERNS.md
@.planning/REQUIREMENTS.md
@.claude/skills/spike-findings-Aura/references/packaging-box.md
@docker/aura/Dockerfile
@go.mod
@internal/config/config.go
@internal/config/config_knobs.go
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SBX-04 amendment (D-06 Gate-1 discipline) — REQUIREMENTS + PRD, committed before any egress code</name>
  <files>.planning/REQUIREMENTS.md, prd.md, .planning/ROADMAP.md</files>
  <read_first>
    - CONTEXT D-04/D-05/D-06 (37-CONTEXT.md lines 46-61) — the "full public internet minus the tenancy boundary" posture and the explicit "PRD/REQUIREMENTS-amendment commit before implementation" instruction.
    - RESEARCH State-of-the-Art row (37-RESEARCH.md line 440) — `--network none` default → full-internet-minus-internal; "REQUIREMENTS-amendment required".
    - RESEARCH Pitfall 2 (37-RESEARCH.md lines 339-343) — gVisor⊥nat (issue #934): FQDN-allowlist mode (nat-redirect) is runc-only, mutually exclusive with `runtime: runsc`.
    - REQUIREMENTS.md SBX-04 (line 58) — the literal text being amended.
    - ROADMAP.md Phase 37 Success Criterion #4 (line ~342) + the milestone-index SC#4 (line ~95) — both still read the stale "default `--network none`"; they must be reconciled to the D-06 posture in the SAME Gate-1 doc commit.
  </read_first>
  <action>
    Edit the SBX-04 bullet in REQUIREMENTS.md: replace the literal "Sandbox egress defaults to `--network none`" with the D-06 amended default — **full public internet, DROP only the tenancy boundary (RFC1918 private ranges + 169.254.169.254 cloud-metadata + the shared-services Docker bridge)** — while PRESERVING SBX-04's enforced core: a tightened allowlist, when an operator sets one, is enforced via nftables (not advisory), and `runtime: runsc` stays selectable under server_production. Add a parenthetical AMENDED-2026-07-06 note citing D-04/D-05/D-06 and a one-line record that the OpenSandbox FQDN-allowlist (nat-redirect) mode is **runc-only / mutually exclusive with gVisor `runsc`** (issue #934) — the always-on filter-table floor is gVisor-compatible. Add the same amendment note to prd.md wherever the sandbox egress default is stated (search for `--network none` / sandbox egress); if prd.md has no such statement, add a short "Phase 37 SBX-04 egress amendment" note under the sandbox/persistence section. Also reconcile ROADMAP.md Phase 37 Success Criterion #4 (and the milestone-index SC#4 summary near line 95) — replace the stale "default `--network none`" with the D-06 posture (full public internet minus the tenancy boundary: DROP RFC1918 + 169.254.169.254 + the shared-services bridge), in the SAME Gate-1 doc commit so ROADMAP, REQUIREMENTS, and PRD state one consistent default before any egress code lands. Do NOT touch any egress code — none exists yet; this is the Gate-1 doc commit that must precede it.
  </action>
  <verify>
    <automated>grep -n "full-internet-minus-internal\|full public internet" .planning/REQUIREMENTS.md && grep -n "runc-only\|#934\|gVisor" .planning/REQUIREMENTS.md && grep -n "full public internet\|full-internet-minus-internal" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md SBX-04 states the amended default (full-internet-minus-internal) and preserves the enforced-allowlist + runsc-selectable core.
    - Both REQUIREMENTS.md and prd.md carry a dated D-06 amendment note recording the gVisor⊥nat mutual-exclusion.
    - ROADMAP.md Phase 37 SC#4 (and the milestone-index SC#4) no longer state `--network none` as the default — they read the D-06 posture (full public internet minus the tenancy boundary), reconciled in the same Gate-1 commit.
    - No file under `internal/sandbox/` or any egress rule file is created or edited in this task.
  </acceptance_criteria>
  <done>SBX-04 amendment (D-06 Gate-1 discipline) — REQUIREMENTS + PRD, committed before any egress code — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

<task type="auto">
  <name>Task 2: Fat full-capability sandbox image (D-12/D-13) — docker/aura-sandbox/Dockerfile</name>
  <files>docker/aura-sandbox/Dockerfile</files>
  <read_first>
    - PATTERNS.md "docker/aura-sandbox/Dockerfile — EXACT TEMPLATE" (lines 161-165) — reuse `docker/aura/Dockerfile`'s fat runtime base verbatim; the anti-pattern guard (line 165).
    - packaging-box.md "Fat runtime Dockerfile base" (lines 49-77) — the proven layer order, NO USER directive, uv COPYed from `ghcr.io/astral-sh/uv`, `mcp-neo4j-cypher==0.6.0` pin, PEP-668 `--break-system-packages`.
    - CONTEXT D-13 (37-CONTEXT.md lines 106-107) — hybrid: bake python3/node/go/uv/git/gh/jq + Phase-5 heavy set; uv long-tail via shared warm-cache; digest-pin.
    - docker/aura/Dockerfile (the analog to copy the runtime stage from).
  </read_first>
  <action>
    Create `docker/aura-sandbox/Dockerfile` deriving from the proven fat runtime stage in `docker/aura/Dockerfile` (debian:bookworm-slim, root, writable rootfs, no USER line). Bake the D-13 runtime set: python3 + python3-pip (with `--break-system-packages`), Node, `uv`/`uvx` COPYed from a **digest-pinned** `ghcr.io/astral-sh/uv` image, `git`, `gh` (GitHub CLI), `jq`, `curl`, ca-certificates, and the pinned `mcp-neo4j-cypher==0.6.0`. Order the heavy apt/uv/pip layers first (cache-stable). Pin the base image by digest (`debian:bookworm-slim@sha256:...`) per D-13. Declare `/root/.cache/uv` as the shared warm-cache mount target and `/workspace` as the per-identity volume mount point (documented via a comment; the actual mount is set by the translator, not the image). Do NOT add a distroless/non-root/read-only final stage and do NOT add an entrypoint that drops privileges — this is the fat box (D-12). The image does not COPY the aura binary (the box runs the model's shell, not aura).
  </action>
  <verify>
    <automated>docker build -f docker/aura-sandbox/Dockerfile -t aura-sandbox:build . && docker run --rm aura-sandbox:build sh -c 'command -v python3 && command -v node && command -v uvx && command -v git && command -v gh && command -v jq && id -u'</automated>
  </verify>
  <acceptance_criteria>
    - `docker build` succeeds; the probe resolves python3/node/uvx/git/gh/jq and `id -u` prints `0` (root).
    - The Dockerfile has NO `USER` directive, NO distroless final stage, NO `read_only`.
    - The base image is digest-pinned and `uv`/`uvx` come from a digest-pinned astral image.
    - `docker run --rm aura-sandbox:build sh -c 'echo x > /probe && cat /probe'` round-trips (writable rootfs).
  </acceptance_criteria>
  <done>Fat full-capability sandbox image (D-12/D-13) — docker/aura-sandbox/Dockerfile — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

<task type="auto">
  <name>Task 3: Promote moby modules to direct + capture the v0.4.1 API surface (Pitfall 1)</name>
  <files>go.mod, go.sum</files>
  <read_first>
    - RESEARCH Standard Stack + Installation (37-RESEARCH.md lines 80-116) — the two modules, the `go get`+`go mod tidy` promotion, and the `go doc` commands to run.
    - RESEARCH Pitfall 1 (37-RESEARCH.md lines 333-337) — the options-struct API differs from every `docker/docker/client` example; `go doc` the vendored types before writing the backend.
    - PATTERNS.md metadata (line 281) — `go.mod:143-144` both `// indirect`.
  </read_first>
  <action>
    Promote `github.com/moby/moby/client` (v0.4.1) and `github.com/moby/moby/api` (v1.54.2) from indirect to direct: `go get github.com/moby/moby/client@v0.4.1 && go get github.com/moby/moby/api@v1.54.2 && go mod tidy`. Then capture the EXACT vendored API surface (RESEARCH Pitfall 1 / Assumption A2) by running `go doc github.com/moby/moby/client.ContainerCreateOptions`, `go doc github.com/moby/moby/client.ExecCreateOptions`, `go doc github.com/moby/moby/client.ExecAttachOptions`, `go doc github.com/moby/moby/client.CopyToContainerOptions`, `go doc github.com/moby/moby/api/types/container.HostConfig`, `go doc github.com/moby/moby/api/types/container.Resources`, and `go doc github.com/moby/moby/api/types/network.NetworkMode`. Record the field/signature output verbatim in the plan SUMMARY under a "Moby v0.4.1 API surface" heading — this is the interface-first handoff the DockerBackend plan (37-04) reads instead of guessing. Do NOT add `docker/docker/client` (D-01). Do NOT write any backend code in this plan.
  </action>
  <verify>
    <automated>go mod verify && grep -E "moby/moby/(client|api) v" go.mod && ! grep -E "moby/moby/(client|api) v[0-9].* // indirect" go.mod && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - go.mod lists both moby modules as direct requires (no `// indirect` on them).
    - `go mod verify` passes; `go build ./...` is green.
    - The SUMMARY contains the captured `go doc` field lists for the create/exec/attach/cp options structs and HostConfig/Resources/NetworkMode.
    - No `docker/docker/client` import is introduced.
  </acceptance_criteria>
  <done>Promote moby modules to direct + capture the v0.4.1 API surface (Pitfall 1) — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

<task type="auto">
  <name>Task 4: AURA_SANDBOX_* operator config surface (PROF env-catalog discipline)</name>
  <files>internal/config/config.go, internal/config/config_sandbox.go, internal/config/config_knobs.go, internal/config/config_sandbox_test.go</files>
  <read_first>
    - RESEARCH Runtime State Inventory "Secrets / env vars" row (37-RESEARCH.md line 326) — the explicit mandate: "catalog the new AURA_SANDBOX_* env (PROF env-catalog discipline)".
    - CONTEXT D-08 (idle-TTL is "a config knob", ~30 min default) + D-14 (cgroup caps 2 CPU / 2GB / 512 pids are tunable "starting points") + D-06 (tightened egress allowlist is an operator opt-in) + Claude's Discretion (37-CONTEXT.md lines 121-124).
    - config.go Config struct + loadBase() (in context) — the AURA_<DOMAIN>_<UNIT> field+load pattern: `Neo4j knowledge.Config` sub-struct composition, `SwarmChildTimeoutSec` via `envutil.IntDefault`, `SkillInjectionBlocklist` via `envSliceDefault`, `MUSRIsolation` as a dedicated field (the closest precedent).
    - config_knobs.go knobRegistry() (in context) — the Tier-A/Tier-B `KnobSpec` rows the per-profile re-parse validator reads; the `AURA_MUSR_ISOLATION` row (lines 116-120) is the exact Phase-36 precedent to mirror.
    - config_knobs.go `AURA_RUN_DIR_SWEEP_INTERVAL_SEC` row (KindInt, Default "3600") — the exact cadence-knob precedent: an int-seconds knob registered KindInt so the reparse pass catches a malformed value. `AURA_SANDBOX_IDLE_TTL_SEC` mirrors it. (The old `AURA_SANDBOX_AGENT_*` remote-agent surface was removed with `internal/sandboxagent`, so there is no longer any AURA_SANDBOX_ collision to avoid.)
  </read_first>
  <action>
    Define a `SandboxConfig` sub-struct in a new `internal/config/config_sandbox.go` (mirroring the `db.Config`/`knowledge.Config` sub-struct composition to keep config.go ≤600 LOC) holding the primitive operator knobs — no `internal/sandbox` import (config is a leaf; 37-05's specFor maps these primitives into usersandbox.Resources/EgressPolicy). Fields + env, all AURA_<DOMAIN>_<UNIT>, non-fatal envutil-style fallbacks: `IdleTTLSec int` (AURA_SANDBOX_IDLE_TTL_SEC, int seconds, default 1800 = 30m — D-08; a plain int-seconds knob, NOT a time.Duration, so it registers KindInt like AURA_RUN_DIR_SWEEP_INTERVAL_SEC and a malformed value is caught by the reparse pass instead of silently defaulting — 37-05's reaper converts via `time.Duration(IdleTTLSec)*time.Second`), `CPULimit` (AURA_SANDBOX_CPU_LIMIT, NanoCPUs or CPU count, default 2 — D-14), `MemoryLimit int64` (AURA_SANDBOX_MEMORY_LIMIT, bytes, default 2GiB — D-14), `PidsLimit int64` (AURA_SANDBOX_PIDS_LIMIT, default 512 — D-14), `EgressAllowlist []string` (AURA_SANDBOX_EGRESS_ALLOWLIST via `envSliceDefault`, empty default = floor-only, D-06), `Image string` (AURA_SANDBOX_IMAGE, default the digest-pinned ref built in Task 2). Add `loadSandboxConfig()` in the same file and wire it in loadBase() as `Sandbox: loadSandboxConfig()` (add the single `Sandbox SandboxConfig` field to the Config struct in config.go). Add the six matching `KnobSpec` rows to `knobRegistry()` in config_knobs.go (KindInt for IdleTTLSec/CPULimit/MemoryLimit/PidsLimit, KindString only for Image/EgressAllowlist — the numeric caps + TTL must be KindInt so the reparse pass flags a malformed value) so `aura config validate`'s re-parse pass flags a malformed value under a strict tier (PROF env-catalog). Add config_sandbox_test.go: `t.Setenv` each of the six vars, assert `config.Load()` parses each into the right `cfg.Sandbox.*` field, and assert an unset var falls back to the documented default. This task DEFINES + catalogs + parse-tests the surface ONLY — do NOT touch `internal/sandbox/` or wire these into any router/spec (that is 37-05/37-06).
  </action>
  <verify>
    <automated>go test ./internal/config/ -run "Sandbox" -count=1 && go vet ./internal/config/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `config.Load()` parses AURA_SANDBOX_IDLE_TTL_SEC / _CPU_LIMIT / _MEMORY_LIMIT / _PIDS_LIMIT / _EGRESS_ALLOWLIST / _IMAGE into typed `cfg.Sandbox.*` fields, with documented defaults on unset.
    - `knobRegistry()` catalogs all six AURA_SANDBOX_* knobs; AURA_SANDBOX_IDLE_TTL_SEC/_CPU_LIMIT/_MEMORY_LIMIT/_PIDS_LIMIT are KindInt so the re-parse validator FLAGS a malformed value under a strict tier (never a silent fallback) — a parse test asserts AURA_SANDBOX_IDLE_TTL_SEC="abc" yields a Fatal violation under server_production.
    - AURA_SANDBOX_IDLE_TTL_SEC is int seconds (name + KindInt mirror AURA_RUN_DIR_SWEEP_INTERVAL_SEC); 37-05's reaper reads it as `time.Duration(IdleTTLSec)*time.Second`.
    - `go build ./...` green; config.go stays ≤600 LOC; nothing under `internal/sandbox/` is touched.
  </acceptance_criteria>
  <done>AURA_SANDBOX_* operator config surface (PROF env-catalog discipline) — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain → build | new direct deps + a new container base image enter the build (moby modules, debian base, uv image) |
| doc → implementation | the SBX-04 amendment is the policy the egress plan (37-06) must implement |
| operator env → runtime | the AURA_SANDBOX_* knobs are the operator's tuning surface — a malformed value must be caught, not silently defaulted, under a strict tier |

## STRIDE Threat Register (ASVS L1, block: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-SC | Tampering | moby module promotion + astral/debian base images | mitigate | go.sum hash-pins the modules (RESEARCH Package Legitimacy Audit — Approved); digest-pin the debian base + astral uv image; `go mod verify` in the gate. slopcheck N/A (Go modules / source-built image). |
| T-37-01-DISTRO | Elevation of Privilege | box image posture | mitigate | Image is fat but the ESCAPE surface is closed by the boundary in later plans; this plan's guard: no USER-drop/distroless/read-only regression (D-12), verified by the root+writable probe. |
| T-37-01-DOCDRIFT | Repudiation | SBX-04 requirement | accept→mitigate | The Gate-1 amendment lands BEFORE egress code (wave 1 < egress wave 3), so the shipped default cannot silently diverge from the recorded requirement. |
| T-37-01-CFG | Tampering | malformed AURA_SANDBOX_* operator knob | mitigate | The numeric caps + TTL (AURA_SANDBOX_IDLE_TTL_SEC/_CPU_LIMIT/_MEMORY_LIMIT/_PIDS_LIMIT) are KindInt rows → the per-profile re-parse pass emits a Fatal violation under a strict tier on a malformed value (never a silent fallback into an unsafe cap/TTL). Registering IDLE_TTL as KindString would DEFEAT this (KindString gets no reparse check) — it is int-seconds/KindInt for that reason. Parse test proves the Fatal. |
</threat_model>

<verification>
- `docker build -f docker/aura-sandbox/Dockerfile` green; root + writable + full-runtime probe passes.
- `go build ./...` + `go mod verify` green with moby direct.
- REQUIREMENTS.md + prd.md carry the dated D-06 amendment + gVisor⊥nat note.
- `go test ./internal/config/ -run Sandbox` green; the six AURA_SANDBOX_* knobs parse + are cataloged.
- No file under `internal/sandbox/` exists yet (this plan writes zero Go source in that package).
</verification>

<success_criteria>
- The Gate-1 SBX-04 amendment is committed (precedes all egress code).
- The digest-pinned fat box image builds and proves full-capability (root, writable, full runtime).
- The moby SDK is a direct dep with its exact API surface captured for 37-04.
- The AURA_SANDBOX_* operator config surface is defined, cataloged, and parse-tested — the configured source 37-05/37-06 read into specFor/buildEgressSidecar.
</success_criteria>

<output>
Create `.planning/phases/37-per-user-full-capability-sandbox/37-01-SUMMARY.md` when done — include the captured "Moby v0.4.1 API surface" section, the resolved image digest, and the final `SandboxConfig` field list (for 37-05/37-06).
</output>
