---
phase: 37-per-user-full-capability-sandbox
plan: 08
type: execute
wave: 4
depends_on: ["37-05", "37-06"]
files_modified:
  - docs/adr/0037-per-identity-docker-sandbox.md
  - compose.yaml
  - internal/sandbox/usersandbox/bench_soak_test.go
  - .planning/phases/37-per-user-full-capability-sandbox/37-VALIDATION.md
autonomous: true
requirements: [SBX-05]
must_haves:
  truths:
    - "An ADR records: container-per-identity over Docker for the mini-PC; K8s + agent-sandbox + gVisor-as-default reserved for the DGX-Spark multi-node tier; the sandbox config mirrors the E2B / agent-sandbox template/claim shape for forward compatibility."
    - "The ADR records the accepted residuals: the aura daemon holds the Docker socket (never a box), optionally narrowed by a socket-proxy (Open Q5); the gVisor⊥nat FQDN-allowlist mutual-exclusion (Pitfall 2)."
    - "The aura service in compose gets Docker API access to spawn boxes (socket into the daemon only, never a box); the egress + sandbox images are referenced."
    - "A D-14 concurrency-soak harness exists (10-20 concurrent per-identity boxes: aggregate RAM within 32GB + headroom, Resolve p95 <~2s, Resume p95 <~1s, cgroup caps prevent starvation) and its real-32GB-host run is recorded in VALIDATION.md Manual-Only as the Gate-3 evidence."
  artifacts:
    - path: "docs/adr/0037-per-identity-docker-sandbox.md"
      provides: "SBX-05 ADR (D-15 decision + residuals)"
      contains: "container-per-identity"
    - path: "internal/sandbox/usersandbox/bench_soak_test.go"
      provides: "D-14 concurrency-soak harness (real-32GB-host)"
      contains: "Resolve"
  key_links:
    - from: "compose.yaml"
      to: "the aura service Docker API access"
      via: "socket mount into the daemon only (Pitfall 4 / Open Q5)"
      pattern: "docker.sock|docker-socket-proxy"
  prohibitions:
    - "The compose change MUST NOT mount the Docker socket into any per-identity box — only into the aura daemon (SBX-02)."
    - "The benchmark harness MUST NOT be trusted on dev WSL (15.47 GiB cap) — the pass/fail measurement is real-32GB-host only (D-14), recorded as Manual-Only."
    - "The ADR MUST NOT record K8s/gVisor-default as the mini-PC choice — those are DGX-tier-reserved (D-15)."
---

<objective>
Close SBX-05: write the ADR recording the container-per-identity-over-Docker decision for the
mini-PC (K8s + agent-sandbox + gVisor-as-default reserved for the DGX-Spark tier; the sandbox
config mirrors the E2B/agent-sandbox template/claim shape for forward compat — D-15), including
the accepted residuals surfaced by research (the daemon-holds-the-Docker-socket surface and its
socket-proxy narrowing — Pitfall 4/Open Q5; the gVisor⊥nat FQDN mutual-exclusion — Pitfall 2).
Give the aura service the Docker API access it needs to spawn boxes under production (socket into
the daemon only, never a box). Build the D-14 concurrency-soak harness and record its real-32GB-
host run as the Gate-3 Manual-Only evidence.

Purpose: the ADR is the SBX-05 deliverable and the forward-compat contract; the benchmark is
success-criterion 5's concurrency proof (dev WSL's 15.47 GiB cap cannot validate the 10-20-box
envelope — it must run on the real 32GB host).
Output: `docs/adr/0037-*.md`, the compose daemon-Docker access, `bench_soak_test.go`, and the
VALIDATION.md Manual-Only fill.
</objective>

## Artifacts This Phase Produces (this plan's slice)

- `docs/adr/0037-per-identity-docker-sandbox.md` — the SBX-05 ADR.
- `compose.yaml` aura-service Docker API access (+ optional socket-proxy) + egress/sandbox image references.
- `internal/sandbox/usersandbox/bench_soak_test.go` — the D-14 soak harness.
- The filled 37-VALIDATION.md Manual-Only D-14 table (Gate-3 evidence, populated on the 32GB host).

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-per-user-full-capability-sandbox/37-CONTEXT.md
@.planning/phases/37-per-user-full-capability-sandbox/37-RESEARCH.md
@.planning/phases/37-per-user-full-capability-sandbox/37-VALIDATION.md
@.planning/phases/37-per-user-full-capability-sandbox/37-06-SUMMARY.md
@.claude/skills/spike-findings-Aura/references/multiuser-per-identity-isolation.md
@.claude/skills/spike-findings-Aura/references/packaging-box.md
@compose.gvisor.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: SBX-05 ADR + compose daemon-Docker access (Open Q5 residual)</name>
  <files>docs/adr/0037-per-identity-docker-sandbox.md, compose.yaml</files>
  <read_first>
    - CONTEXT D-15 (37-CONTEXT.md lines 116-119) — the exact decision to record: container-per-identity over Docker for the mini-PC; K8s + agent-sandbox + gVisor-default for DGX-Spark; config mirrors the E2B/agent-sandbox template/claim shape.
    - RESEARCH Pitfall 4 + Open Q5 (37-RESEARCH.md lines 351-355, 477) — the daemon needs dockerd access (host socket vs socket-proxy vs TCP-TLS); recommend host socket into the aura service only, narrowed via tecnativa/docker-socket-proxy; record as the ADR's accepted residual.
    - RESEARCH Pitfall 2 / Open Q1 (37-RESEARCH.md lines 339-343, 460-463) + 37-06-SUMMARY.md — the gVisor⊥nat FQDN mutual-exclusion the ADR records.
    - multiuser-per-identity-isolation.md "What to Avoid" (K8s on the appliance) + packaging-box.md (fat box, gVisor optional tier).
    - REQUIREMENTS OPS-06 note — ADRs exist for deployment profiles + the sandbox decision (this ADR is that entry).
  </read_first>
  <action>
    Write docs/adr/0037-per-identity-docker-sandbox.md in the repo's ADR style (Context / Decision / Consequences / Alternatives / Residuals). Record D-15: container-per-identity over Docker on the mini-PC (Docker-direct, not K8s); K8s + agent-sandbox + gVisor-as-default reserved for the DGX-Spark multi-node tier; the SandboxSpec/Backend mirrors the E2B / agent-sandbox template/claim shape (the D-01 forward-bet). Record the accepted residuals: (a) the aura daemon holds Docker API access (socket into the daemon only, never a box — SBX-02), optionally narrowed by tecnativa/docker-socket-proxy to the container/exec/volume/image subset (Open Q5); (b) the gVisor⊥nat mutual-exclusion — the filter-floor is always-on (both runtimes), the FQDN allowlist is runc-only (Pitfall 2, #934); (c) the D-06 egress amendment (full-internet-minus-internal). Cross-reference SBX-04's amendment. Then edit compose.yaml: give the `aura` service Docker API access to spawn boxes (mount `/var/run/docker.sock` into the aura service only, or wire the socket-proxy) and reference the digest-pinned sandbox + egress images; do NOT touch any box definition to mount the socket. Keep the change a no-op under dev/local_trusted (documented — routing is Strict()-gated).
  </action>
  <verify>
    <automated>test -f docs/adr/0037-per-identity-docker-sandbox.md && grep -qi "container-per-identity" docs/adr/0037-per-identity-docker-sandbox.md && grep -qi "DGX" docs/adr/0037-per-identity-docker-sandbox.md && grep -qi "socket" docs/adr/0037-per-identity-docker-sandbox.md && docker compose -f compose.yaml config >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - The ADR records the container-per-identity decision, the DGX-tier reservation, the E2B forward-compat shape, and all three residuals (socket, gVisor⊥nat, egress amendment).
    - `docker compose -f compose.yaml config` validates; the socket is granted to the aura service only, never a box.
    - No box service definition gains a Docker socket mount.
  </acceptance_criteria>
  <done>SBX-05 ADR + compose daemon-Docker access (Open Q5 residual) — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

<task type="auto">
  <name>Task 2: D-14 concurrency-soak harness + Manual-Only Gate-3 evidence table</name>
  <files>internal/sandbox/usersandbox/bench_soak_test.go, .planning/phases/37-per-user-full-capability-sandbox/37-VALIDATION.md</files>
  <read_first>
    - CONTEXT D-14 (37-CONTEXT.md lines 110-114) — 10-20 concurrent per-identity boxes; aggregate RAM within 32GB + headroom; Resolve p95 <~2s; Resume-from-suspend p95 <~1s; cgroup caps (2 CPU / 2GB / 512 pids start) prevent starvation; pass = fits 32GB with headroom + no starvation (not a scale SLA).
    - VALIDATION Manual-Only D-14 row (37-VALIDATION.md lines 82) + Environment Availability real-32GB-host row (37-RESEARCH.md lines 486, 491) — dev WSL 15.47 GiB is insufficient; real-host-only, no fallback.
    - 37-05-SUMMARY.md (Route/Resolve) + 37-04-SUMMARY.md (Suspend/Resume) — the verbs the harness drives.
  </read_first>
  <action>
    Create bench_soak_test.go under `//go:build docker_integration` (or a `cmd/`-tool equivalent): resolve N (10-20, configurable via env) per-identity boxes concurrently, measure Resolve p95 and Resume-from-suspend p95, sample aggregate RSS/RAM against the 32GB envelope, and run a starvation probe (a CPU/mem-hungry box must not starve co-tenants under the cgroup caps). It records the metrics to stdout/a report and asserts the pass criteria ONLY when an explicit `AURA_SANDBOX_SOAK_REALHOST=1` env is set (so CI/dev do not falsely pass/fail on the 15.47 GiB cap); without it, it skips with a clear "real-32GB-host only" message. Update 37-VALIDATION.md Manual-Only: replace the D-14 row's placeholder with the concrete run instructions and a results table (N, aggregate RAM, headroom, Resolve p95, Resume p95, starvation verdict) to be filled on the 32GB host as the Gate-3 evidence.
  </action>
  <verify>
    <automated>go vet -tags docker_integration ./internal/sandbox/usersandbox/ && go test -tags docker_integration ./internal/sandbox/usersandbox/ -run "TestSoak" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - The soak harness compiles and runs; without AURA_SANDBOX_SOAK_REALHOST it skips with the real-host-only message (no false pass on the WSL cap).
    - The harness measures Resolve p95, Resume p95, aggregate RAM, and a starvation verdict.
    - 37-VALIDATION.md Manual-Only carries the D-14 run instructions + a results table for the 32GB-host Gate-3 fill.
  </acceptance_criteria>
  <done>D-14 concurrency-soak harness + Manual-Only Gate-3 evidence table — implemented and all acceptance criteria above verified green (go vet/build + the listed tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aura daemon → dockerd | the daemon's Docker API access is a privileged surface (Pitfall 4) |
| decision → future ops | the ADR binds the deployment/isolation posture (OPS-06) |

## STRIDE Threat Register (ASVS L1, block: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08-SOCKET | Elevation of Privilege | daemon Docker socket surface | mitigate | Socket into the aura service ONLY (never a box, SBX-02); optionally narrow via docker-socket-proxy to the lifecycle subset; recorded as the ADR accepted residual (Open Q5). |
| T-37-08-STARVE | Denial of Service | co-tenant starvation at concurrency | mitigate | The D-14 soak proves cgroup caps bound co-tenant impact on the real 32GB host (Gate-3 evidence). |
| T-37-08-FALSEBENCH | (test integrity) | benchmark passing on the 15.47 GiB dev cap | mitigate | Pass/fail gated on AURA_SANDBOX_SOAK_REALHOST; dev/CI skip with a real-host-only message (no false green). |
</threat_model>

<verification>
- `docs/adr/0037-*.md` exists with the D-15 decision + all three residuals.
- `docker compose -f compose.yaml config` validates; socket granted to the daemon only.
- The soak harness compiles + runs; VALIDATION.md Manual-Only carries the D-14 Gate-3 table.
- `go build ./...` green.
</verification>

<success_criteria>
- SBX-05 closed: the ADR records the decision + forward-compat shape + residuals; the aura daemon can spawn boxes; the D-14 concurrency benchmark exists and its real-32GB-host run is the recorded Gate-3 evidence.
</success_criteria>

<output>
Create `.planning/phases/37-per-user-full-capability-sandbox/37-08-SUMMARY.md` when done — note the ADR path + the pending real-32GB-host soak as the Gate-3 Manual-Only item.
</output>
