---
phase: 31-stabilization-ci-unblock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
# Verify-only baseline. Expected modification set is EMPTY (C1/C2/C4 are already
# green per 31-RESEARCH, measured on the CI-matching Node 24.16.0 / npm 11.17.0).
# Contingency remediation files (only if a gate is unexpectedly RED) — none conflict
# with any Wave-2 plan, so listing them here is informational only:
#   - internal/webui/dist/**   (C2: rebuild+commit ONLY if web-dist-freshness drifts)
#   - web/src/__tests__/**      (C4: add branch tests ONLY if vitest branches < 85)
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "scripts/check-file-size.sh exits 0 whole-tree (no tracked production/test source >600 LOC; only generated internal/db/sqlc/* is exempt) — C1"
    - "A fresh Node-24 'npm ci && npm run build' leaves internal/webui/dist byte-identical: git diff --exit-code -- internal/webui/dist/ is clean — C2"
    - "vitest run --coverage passes all four global thresholds >=85 (statements/branches/functions/lines); branches ~85.45% is the binding constraint — C4"
  artifacts:
    - path: "scripts/check-file-size.sh"
      provides: "whole-tree 600-LOC cap gate (already present; asserted exit 0)"
      contains: "600"
    - path: "internal/webui/dist/index.html"
      provides: "committed Vite bundle embedded via //go:embed (must equal a fresh Node-24 build)"
      contains: "<!doctype html"
    - path: "web/vitest.config.ts"
      provides: "four 85% coverage thresholds (statements/branches/functions/lines)"
      contains: "branches: 85"
  key_links:
    - from: "web/ (vite build)"
      to: "internal/webui/dist"
      via: "vite outDir ../internal/webui/dist"
      pattern: "internal/webui/dist"
  prohibitions:
    - "No tracked production/test source file exceeds 600 LOC (do NOT raise the cap to pass)"
    - "Do NOT modify any non-test web/src or tokens/ file — that re-stales the committed dist bundle and reds web-dist-freshness (keep web edits to test files only)"
    - "Frontend branch coverage MUST NOT regress below 85% (do NOT lower vitest thresholds to pass)"
    - "Do NOT re-split serve_webui.go / LoginPage.test.tsx — Codex already split them (525/592); this plan only verifies"

threat_model_required: true
---

<objective>
Confirm the QUAL-01 stabilization baseline is GREEN before the Wave-2 implementation
plans mutate CI and code: the 600-LOC file-size cap (C1), the committed dist↔source
freshness (C2), and the four ≥85% frontend coverage thresholds (C4). 31-RESEARCH
measured all three green on the CI-matching toolchain (Node 24.16.0 / npm 11.17.0);
this plan re-asserts each via its authoritative gate so any later regression is
attributable to Wave 2.

Purpose: A clean, attributable starting line. C1/C2/C4 are three of the five Phase-31
success criteria; they are already satisfied but must be proven (not assumed) and
guarded against regression by the implementation waves.

Output: No source changes expected. Three green gate runs (file-size, dist-freshness,
vitest coverage). If a gate is unexpectedly RED, minimal in-scope remediation only
(rebuild+commit dist for C2; add targeted branch tests for the binding files for C4) —
never a scope reduction or threshold lowering.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/31-stabilization-ci-unblock/31-RESEARCH.md
@.planning/phases/31-stabilization-ci-unblock/31-VALIDATION.md
@scripts/check-file-size.sh
@scripts/web_dist_freshness.sh
@web/vitest.config.ts
</context>

## Artifacts this phase produces (31-01)

This plan is VERIFY-ONLY and produces no new symbols or files. It asserts the
existing gates remain green:
- `scripts/check-file-size.sh` → exit 0 (C1)
- `internal/webui/dist/**` ↔ a fresh `npm run build` → zero drift (C2)
- `web/vitest.config.ts` four thresholds → all ≥85 (C4)

(Phase roll-up of NEW artifacts lives in 31-02 — the CI lint scripts — and 31-03 —
the MCP SSRF guard. This plan adds none.)

<tasks>

<task type="auto">
  <name>Task 1: Assert the 600-LOC file-size cap is green whole-tree (C1)</name>
  <read_first>
    - scripts/check-file-size.sh (the gate: globs git ls-files, 600-LOC cap, the `^internal/db/sqlc/` exemption for generated assets.sql.go)
    - cmd/aura/serve_webui.go (already split to 525 LOC; serve_webui_auth_config.go=109 holds the spillover — do NOT touch)
    - web/src/__tests__/LoginPage.test.tsx (already split to 592 LOC; LoginPage.authula-errors.test.tsx=128 holds the spillover — 8-line headroom, do NOT add tests here)
    - .planning/phases/31-stabilization-ci-unblock/31-RESEARCH.md (§C1 — measured GREEN: 525/592, only generated sqlc >600 and it is exempt)
  </read_first>
  <files>(none — verification only)</files>
  <action>
    Run scripts/check-file-size.sh from the repo root and confirm exit 0 with the
    "all source files within the 600-LOC cap" message. Do NOT re-split serve_webui.go
    or LoginPage.test.tsx — Codex already split them (525/592, both under the cap) and
    re-splitting would churn shipped source (and, for LoginPage, risk re-staling dist
    paths). The only tracked file over 600 is the generated internal/db/sqlc/assets.sql.go
    (722), which the script's `^internal/db/sqlc/` filter exempts — do not edit generated
    sqlc. If (unexpectedly) the gate reports a NON-generated file over 600, split it per
    the established sibling-split convention (a `<name>_<concern>.go` / `<Name>.<concern>.test.tsx`
    sibling, mirror serve_webui_auth_config.go and LoginPage.authula-errors.test.tsx) — never
    raise the cap.
  </action>
  <verify>
    <automated>cd d:/Aura && bash scripts/check-file-size.sh && echo C1_FILESIZE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-file-size.sh` exits 0 and prints the within-cap success line
    - No tracked non-generated source file is > 600 LOC
    - serve_webui.go and LoginPage.test.tsx are UNCHANGED by this task (git diff clean for them)
  </acceptance_criteria>
  <done>The whole-tree 600-LOC cap gate is green with no source modification.</done>
</task>

<task type="auto">
  <name>Task 2: Assert internal/webui/dist is fresh vs a Node-24 rebuild (C2)</name>
  <read_first>
    - scripts/web_dist_freshness.sh (the CI job body: `(cd web && npm ci && npm run build)` then `git diff --exit-code -- internal/webui/dist/`)
    - web/vite.config.ts (confirm outDir is ../internal/webui/dist, NOT web/dist — the bundle is committed source embedded into the single binary)
    - .planning/phases/31-stabilization-ci-unblock/31-RESEARCH.md (§C2 — measured GREEN with Node 24.16.0; the committed 63-file bundle equals a fresh build; landmine: always `npm ci` from package-lock so a drifted node_modules can't shift bytes)
  </read_first>
  <files>(none expected — internal/webui/dist/** is a contingency-only rebuild target)</files>
  <action>
    Reproduce the canonical CI build with Node 24.x / npm 11 (matching the CI
    AURA_CI_NODE_VERSION 24.x — do NOT use a different Node major, byte output can shift):
    `npm ci` (clean install from package-lock, never a bare `npm install`) then
    `npm run build` in web/, then assert `git diff --exit-code -- internal/webui/dist/`
    is clean. Per 31-RESEARCH this is byte-reproducible and currently drift-free. Touch
    NO non-test web/src or tokens/ files in this phase (they re-stale the bundle). ONLY
    if the diff is non-empty (a real, unexpected drift), commit the rebuilt
    internal/webui/dist in the SAME commit — do not hand-edit bundle files.
  </action>
  <verify>
    <automated>cd d:/Aura/web && npm ci && npm run build && git -C d:/Aura diff --exit-code -- internal/webui/dist/ && echo C2_DIST_FRESH</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm ci && npm run build` completes, then `git diff --exit-code -- internal/webui/dist/` exits 0 (empty diff)
    - The whole working tree stays clean after the rebuild (no incidental web/ churn)
    - No non-test web/src or tokens/ file was modified by this task
  </acceptance_criteria>
  <done>The committed dist bundle is provably equal to a fresh Node-24 build; web-dist-freshness is green.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the four vitest coverage thresholds pass at ≥85 (C4)</name>
  <read_first>
    - web/vitest.config.ts (the four global thresholds: statements/branches/functions/lines = 85; v8 provider; main.tsx excluded)
    - .planning/phases/31-stabilization-ci-unblock/31-RESEARCH.md (§C4 — measured 90.88/85.45/90.89/92.85; BRANCHES 85.45% is binding, ~0.45pp headroom; lowest-coverage drags: governance/McpLifecycleCluster.tsx, settings/settingsApi.ts, i18n.ts, theme/ThemeSwitcher.tsx, settings/ModelSettingsPanel.tsx, governance/McpInstallPanel.tsx)
    - CLAUDE.md (frontend quality gates: coverage ≥85% + mutation ≥70%; gate is GLOBAL not per-file)
  </read_first>
  <files>(none expected — web/src/__tests__/** is a contingency-only test-add target)</files>
  <action>
    Run the frontend coverage suite (`npm run test` = `vitest run --coverage`) in web/
    on Linux/WSL (CI parity) and confirm it exits 0 with all four global thresholds met.
    Treat BRANCHES (binding ~85.45%) as the don't-regress watch number. Add NO new tests
    unless the gate is actually red. ONLY if a threshold falls below 85 (e.g. a prior
    merge regressed branches), add targeted tests to the lowest-coverage binding files
    listed in 31-RESEARCH §C4 (start with governance/McpLifecycleCluster.tsx and
    settings/settingsApi.ts branches) — test files only (they are not bundled, so C2
    stays green for free). Never lower the thresholds in vitest.config.ts to pass.
  </action>
  <verify>
    <automated>cd d:/Aura/web && npm run test && echo C4_COVERAGE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run test` exits 0
    - The coverage summary reports statements/branches/functions/lines all ≥85 (branches the binding figure)
    - web/vitest.config.ts thresholds are UNCHANGED (still 85/85/85/85)
    - If any test was added, it lives under web/src/**/*.{test,spec}.{ts,tsx} (not a bundled source file)
  </acceptance_criteria>
  <done>The frontend coverage gate passes at the ≥85 floor on all four thresholds with no threshold lowering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | C1/C2/C4 are read-only CI assertions over already-committed artifacts — no untrusted runtime input crosses a boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-VERIFY-01 | Repudiation (assurance) | the three verify-only gates | mitigate | Each gate FAILS loud on regression: check-file-size.sh exits non-zero over the cap; `git diff --exit-code` reds on any dist drift; vitest exits non-zero below a threshold. No-skip-as-green: there is no `$CI` skip branch in these gates. |
| T-31-C1/C2/C4-SEC | — | file-size / dist / coverage | N/A | Per the phase security contribution, the verify-only criteria carry NO high-severity threat. No SSRF/injection/secret surface here. |
| T-31-SC (package installs) | Tampering | `npm ci` from package-lock | accept | `npm ci` installs from the committed lockfile (no new packages, no `go get`); 31-RESEARCH Package Legitimacy Audit = N/A. No `[ASSUMED]`/`[SUS]` package introduced, so no blocking legitimacy checkpoint is required. |
</threat_model>

<verification>
Per-task → 31-VALIDATION criterion map (1:1 with 31-VALIDATION §Per-Task Verification Map):

| Task | Criterion | Command | Pass |
|------|-----------|---------|------|
| Task 1 | C1 file-size (QUAL-01) | `bash scripts/check-file-size.sh` | exit 0, within-cap message |
| Task 2 | C2 dist freshness (QUAL-01) | `cd web && npm ci && npm run build` → `git diff --exit-code -- internal/webui/dist/` | exit 0, empty diff |
| Task 3 | C4 frontend coverage (QUAL-01) | `cd web && npm run test` | exit 0, four thresholds ≥85 |

Wave-merge sampling: re-run all three before promoting Wave 1.
</verification>

<success_criteria>
- C1: `scripts/check-file-size.sh` exits 0 whole-tree (no non-generated source >600 LOC).
- C2: `internal/webui/dist` equals a fresh Node-24 `npm ci && npm run build` (empty `git diff --exit-code`).
- C4: `vitest run --coverage` passes statements/branches/functions/lines all ≥85.
- No shipped web/src or tokens/ file modified; no threshold lowered; no file re-split.
</success_criteria>

<output>
Create `.planning/phases/31-stabilization-ci-unblock/31-01-SUMMARY.md` when done.
</output>
