---
phase: 03-llm-client-toolresult
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prd.md
  - .env.example
  - go.mod
  - go.sum
  - internal/llm/config.go
  - internal/llm/prices.go
  - internal/llm/config_test.go
  - internal/llm/prices_test.go
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "PRD-amendment commit (A1 REPL, A2 real exporter, A3 cost actual+table, A4 byte offset, A5 env catalog) lands before any implementation"
    - "go.mod pins the OTel v1.44.0 unified trace train (otel, sdk, stdouttrace, otlptracegrpc) with no -rc/-beta/-alpha suffix in go.sum"
    - "llm.Config loads with deterministic precedence: built-in default < .env (OPENROUTER_API_KEY) < ~/.aura/llm.json < AURA_LLM_*"
    - "Empty APIKey surfaces a clear non-panic error (not a panic, not a silent default)"
    - "A3 price table is seeded with deepseek-v4-flash and reports n/a (never $0) for an unknown model"
    - "config.Config composes LLM llm.Config + AURA_OTEL_* knobs without breaking existing DB/Neo4j wiring"
  artifacts:
    - path: "internal/llm/config.go"
      provides: "llm.Config struct + Load with default<.env<file<AURA_LLM_* precedence"
      contains: "func Load"
    - path: "internal/llm/prices.go"
      provides: "A3 static price table seeded with deepseek-v4-flash; n/a for unknown model"
      contains: "deepseek/deepseek-v4-flash"
    - path: "internal/config/config.go"
      provides: "root composite extended with LLM llm.Config + AURA_OTEL_* fields"
      contains: "LLM"
  key_links:
    - from: "internal/config/config.go"
      to: "internal/llm.Load"
      via: "config.Load composes llm.Load output into Config.LLM"
      pattern: "llm\\.(Load|Config)"
---

<objective>
Land the phase's first two D-01 commits: (1) the combined PRD-amendment commit (A1+A2+A3+A4+A5) at the head of the phase, and (2) the typed `llm.Config` + load-order chain + A3 price table + go.mod OTel v1.44.0 deps + `config.Config` composition. This is the foundation every later Phase-3 plan reads.

Purpose: SPEC Req#5 (LLM config + load order) + the locked D-01/D-02 commit discipline (PRD-amendment FIRST, no implementation before it). Pins the verified OTel module set so Plan 04 can wire the real TracerProvider. Seeds the cost-honesty surface (D-23 price table) consumed by Plan 02/05.
Output: PRD amended; `internal/llm/{config.go,prices.go}` with tests; go.mod/go.sum carrying the OTel train; `internal/config/config.go` extended with `LLM` + `AURA_OTEL_*`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-client-toolresult/03-SPEC.md
@.planning/phases/03-llm-client-toolresult/03-CONTEXT.md
@.planning/phases/03-llm-client-toolresult/03-RESEARCH.md
@.planning/phases/03-llm-client-toolresult/03-PATTERNS.md
@.planning/phases/03-llm-client-toolresult/03-AI-SPEC.md
@CLAUDE.md
@prd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PRD-amendment commit (A1+A2+A3+A4+A5) + go.mod OTel v1.44.0 deps</name>
  <read_first>
    - prd.md §Slice 1 (lines ~542-670: goal, smoke `AURA_LLM_*` env block ~572-574, file targets, the `read_tool_output` "limit=200 righe" line ~595 + ~627, the OpenRouter/deepseek-v4-flash default block ~635-640) — the surface A1-A5 amend
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-02 combined amendment commit; the A4 byte-offset + A5 env-catalog descriptions in `### PRD-Amendments Required`)
    - .planning/phases/03-llm-client-toolresult/03-SPEC.md (A1/A2/A3 scope-note lines 15-18; Constraints env naming)
    - .planning/phases/03-llm-client-toolresult/03-RESEARCH.md (Standard Stack OTel v1.44.0 pin + Installation block lines 96-104; "Version verification" line 104 — go.sum must carry no -rc/-beta/-alpha)
    - .env.example (current env catalog to extend with A5)
    - go.mod (current deps: uuid v1.6.0, goleak v1.3.0, rapid v1.3.0 present; OTel absent)
  </read_first>
  <action>
    Amend prd.md §Slice 1 in a single PRD-amendment commit covering all five amendments (per D-02 they land together, FIRST, before any implementation):
    - A1: `aura chat` is an interactive multi-turn REPL (in-memory history only — no DB persistence/auto-title/resume, those stay Phase 4). Update the PRD smoke from single-shot `aura chat "msg"` to the REPL form.
    - A2: OTel emits to a real configured exporter (stdout/OTLP), not the PRD's emit-only no-op provider.
    - A3: USD cost = OpenRouter actual `usage.cost` with a static price-table fallback; unknown model reports `n/a`, never `$0`.
    - A4: `read_tool_output` offset/limit are BYTES (default limit ~2048), overriding the PRD/SPEC "limit=200 righe" line — fix both the ~595 and ~627 occurrences to byte semantics with footer `showing bytes X-Y of Z, next offset Y`.
    - A5: env catalog additions — add `AURA_LLM_TEMPERATURE`, `AURA_LLM_MAX_TOKENS`, `AURA_LLM_TOTAL_TIMEOUT_SEC`, `AURA_LLM_CONNECT_TIMEOUT_SEC`, `AURA_OTEL_EXPORTER` (∈ {stdout,otlp,none}, default otlp), `AURA_OTEL_ENDPOINT` to the PRD §Caps & Limits env index AND to `.env.example`. Keep `OPENROUTER_API_KEY` canonical third-party (NOT renamed to AURA_*). Confirm the OpenRouter/`deepseek/deepseek-v4-flash:exacto` default block (PRD ~635-640) is already correct; align the `AURA_LLM_*` smoke env block (~572-574) to OpenRouter defaults (base `https://openrouter.ai/api/v1`, model `deepseek/deepseek-v4-flash:exacto`).
    Then add the OTel v1.44.0 unified trace train via `go get` exactly as RESEARCH Installation block: `go.opentelemetry.io/otel@v1.44.0`, `go.opentelemetry.io/otel/sdk@v1.44.0`, `go.opentelemetry.io/otel/exporters/stdout/stdouttrace@v1.44.0`, `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc@v1.44.0`, then `go mod tidy`. Do NOT add `usage:{include:true}` or `stream_options` anywhere — they are deprecated no-ops (RESEARCH State of the Art). Verify go.sum carries no `-rc`/`-beta`/`-alpha` on any `go.opentelemetry.io/otel*` line before completing.
    Commit message subject: `prd(03): A1-A5 amendments — REPL, real OTel exporter, actual+table cost, byte read_tool_output, env catalog` (imperative, Co-Authored-By trailer per project convention). This is its own atomic commit; no .go implementation in this commit.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && grep -v '^#' prd.md | grep -c -E 'AURA_OTEL_EXPORTER|AURA_LLM_TEMPERATURE' && grep -q 'v1.44.0' go.mod && go build ./... && ! grep -E 'go\.opentelemetry\.io/otel.*(-rc|-beta|-alpha)' go.sum</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'go.opentelemetry.io/otel' go.mod` ≥ 4 (the four pinned modules at v1.44.0); `go.sum` has zero `go.opentelemetry.io/otel*` lines with `-rc`/`-beta`/`-alpha` suffix
    - `.env.example` contains `AURA_OTEL_EXPORTER`, `AURA_OTEL_ENDPOINT`, `AURA_LLM_TEMPERATURE`, `AURA_LLM_MAX_TOKENS`, `AURA_LLM_TOTAL_TIMEOUT_SEC`, `AURA_LLM_CONNECT_TIMEOUT_SEC`; `OPENROUTER_API_KEY` retained (not renamed)
    - prd.md §Slice 1 `read_tool_output` describes BYTE offset/limit (no remaining "200 righe"/"200 lines" line semantics) — maps SPEC Req#7 / Amendment A4
    - `git log --oneline -1` shows the `prd(03):` amendment commit with zero `.go` files changed (the OTel `go get` updates go.mod/go.sum only, no source files)
    - `go build ./...` succeeds with the new deps resolved
  </acceptance_criteria>
  <done>PRD §Slice 1 amended for A1-A5 in a dedicated PRD-amendment commit; OTel v1.44.0 train pinned in go.mod with clean go.sum; .env.example carries the new env catalog.</done>
</task>

<task type="auto">
  <name>Task 2: llm.Config load-order chain + A3 price table + config.Config composition</name>
  <read_first>
    - internal/agent/budget.go:99-205 (canonical Aura env load-order + fail-fast `envIntFailFast`/`envFloatFailFast` + `errMalformed` const-literal at ~72-74) — the precedence machinery to replicate for fail-fast tiers
    - internal/config/config.go (full — `Load`, `envDefault`/`envIntDefault` silent-absorb helpers lines 103-124, `defaultRunDir` lines 128-133 using os.UserCacheDir, the Config struct lines 24-29, ToolPreviewCap/RunDir already present)
    - internal/llm/client.go (full — wire types; `Request.Temperature`/`MaxTokens` already exist; config must populate them)
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-22 Config shape + defaults; D-23 price table; D-24 aura config keys — config CLI itself is Plan 05, only the file read/write shape is needed here)
    - .planning/phases/03-llm-client-toolresult/03-PATTERNS.md (`internal/llm/config.go` + `prices.go` section lines 90-112: fail-fast for LLM config / silent-absorb for AURA_OTEL_*, defaultRunDir template for ~/.aura/llm.json home path)
    - .planning/phases/03-llm-client-toolresult/03-RESEARCH.md (Assumptions A1/A2/A3: seed the A3 table from the OpenRouter deepseek-v4-flash model page at commit time; usage.cost always preferred so the table is fallback-only)
  </read_first>
  <action>
    Create `internal/llm/config.go` with `Config{Provider, Model, BaseURL, APIKey, TotalTimeoutSec, ConnectTimeoutSec, Temperature, MaxTokens, Headers map[string]string, Prices map[string]Price}` (D-22). Implement load order (SPEC Req#5): built-in defaults (provider `openrouter`, model `deepseek/deepseek-v4-flash:exacto`, base `https://openrouter.ai/api/v1`, Temperature 0.7, MaxTokens 4096, TotalTimeoutSec 120, ConnectTimeoutSec 10, Headers `HTTP-Referer`=Aura repo URL + `X-Title`=`Aura`) < `.env` (`OPENROUTER_API_KEY` → APIKey) < `~/.aura/llm.json` (parse JSON if present; absent file is not an error) < `AURA_LLM_*` env (`AURA_LLM_MODEL`, `AURA_LLM_BASE_URL`, `AURA_LLM_TEMPERATURE`, `AURA_LLM_MAX_TOKENS`, `AURA_LLM_TOTAL_TIMEOUT_SEC`, `AURA_LLM_CONNECT_TIMEOUT_SEC`). Resolve the `~/.aura/` home path via the `defaultRunDir`/`os.UserCacheDir`-style helper pattern (use `os.UserHomeDir` for `~/.aura`). Empty APIKey after the full chain returns a clear non-panic error (fail-fast, budget.go style with a load-bearing literal) — never panic, never silent default. APIKey is NEVER placed in any logged/serialized representation (D-28: structural — keep it a plain field set only here, no String()/log of the struct).
    Create `internal/llm/prices.go` with `type Price struct{InputPer1M, OutputPer1M float64}` and a seeded `defaultPrices` map keyed by model id, seeded with `deepseek/deepseek-v4-flash:exacto` (seed from the OpenRouter model page numbers at commit time per RESEARCH A1 — document the source in a comment). Add a `CostUSD(model string, promptTokens, completionTokens int, providerCost *float64) (string, bool)` helper: if `providerCost` non-nil → return that exact USD (D-18 actual-cost-first); else if model in table → compute from tokens; else return `"n/a"` with ok=false (D-23 — NEVER "$0"). `~/.aura/llm.json` `prices` field overrides the seed.
    Edit `internal/config/config.go`: add `LLM llm.Config` to the Config struct and `AURA_OTEL_*` knobs (`OtelExporter` default `otlp`, `OtelEndpoint` default `localhost:4317` — silent-absorb via `envDefault`, D-06). In `Load`, call `llm.Load()` and assign to `Config.LLM`; surface its error (empty-key) through the existing `(*Config, error)` return. Update the package doc comment line ~8 which currently says "Phase 2 will add LLM llm.Config" — it lands now.
    Write `internal/llm/config_test.go`: table-driven load-order test proving default < file < env (set AURA_LLM_MODEL, write a temp llm.json, assert env wins); a missing-APIKey test asserting a clear non-panic error; assert OpenRouter attribution headers present. Write `internal/llm/prices_test.go`: provider-cost-present → exact USD; cost-absent known model → table USD; unknown model → `n/a` (not `$0`). Reuse `t.Setenv` + `t.TempDir` for the file tier. Add a `goleak.VerifyTestMain` in `internal/llm/main_test.go` if not inherited (no goroutines spawned here, but the package gains one in Plan 02 — establish the harness now). Reuse error-wrap `%w` + lowercase-phrase convention (PATTERNS Shared Patterns).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go vet ./internal/llm/... ./internal/config/... && go build ./... && go test ./internal/llm/ ./internal/config/ -run 'TestConfigLoadOrder|TestConfigMissingKey|TestCost|TestLoad' && go test -race ./internal/llm/</automated>
  </verify>
  <acceptance_criteria>
    - SPEC Req#5: `go test ./internal/llm/ -run TestConfigLoadOrder` proves default < `~/.aura/llm.json` < `AURA_LLM_*`; a missing-key test asserts a clear error string (non-panic) and the function returns `(_, error)` not a panic
    - SPEC Req#12 (config half): `go test ./internal/llm/ -run TestCost` — provider-cost present → exact USD; absent+known → table-computed; unknown model → literal `n/a` substring, and the result is NOT `$0`/`0`
    - `internal/llm/config.go` defaults match D-22 exactly: model `deepseek/deepseek-v4-flash:exacto`, base `https://openrouter.ai/api/v1`, Temperature 0.7, MaxTokens 4096, timeouts 120/10; Headers carry `HTTP-Referer` + `X-Title: Aura`
    - `config.Load()` returns a Config whose `LLM` field is populated and whose `AURA_OTEL_*` fields default to `otlp`/`localhost:4317`; existing DB/Neo4j fields unchanged (`go test ./internal/config/` green)
    - APIKey value never appears in any struct dump / log call in the package (grep for `slog`/`fmt.Print`/`%+v` of the config struct returns none); structural redaction per D-28
  </acceptance_criteria>
  <done>llm.Config loads with the locked 4-tier precedence and fail-fast empty-key; prices.go reports actual-or-table-or-n/a (never $0); config.Config composes LLM + AURA_OTEL_*; tests green under -race.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/.env/llm.json → process config | Untrusted-ish local config values (incl. the secret OPENROUTER_API_KEY) cross into the running process here |
| config struct → logs/errors | Any serialization of the config could leak the API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `llm.Config.APIKey` in logs/errors/struct dumps | mitigate | Structural redaction (D-28): APIKey is a plain field set only at load; no `String()`, no `%+v`/`slog` of the config struct; the Plan-04 anti-leak test (release-blocking) asserts the key value is absent from all observable output |
| T-03-02 | Tampering | malformed `AURA_LLM_*` / llm.json values | mitigate | Fail-fast on malformed-but-set numeric values (budget.go `errMalformed` pattern); empty APIKey is a clear non-panic error, never a silent default |
| T-03-03 | Tampering | OTel module supply chain (new deps) | mitigate | Pin the verified v1.44.0 unified train; `go.sum` cryptographic lock; assert no `-rc/-beta/-alpha` suffix. RESEARCH Package Legitimacy Audit marks all OTel modules Approved (official CNCF, Go-proxy verified) — no [ASSUMED]/[SUS] package, so no legitimacy checkpoint required |
| T-03-SC | Tampering | go get OTel installs | mitigate | Go modules resolved via go.sum (not an npm/PyPI hallucination surface); RESEARCH audit = Approved; slopcheck N/A for Go modules |
</threat_model>

<verification>
- `go build ./...` green with OTel v1.44.0 resolved
- `go vet ./internal/llm/... ./internal/config/...` clean
- `go test -race ./internal/llm/ ./internal/config/` green
- `golangci-lint run ./internal/llm/... ./internal/config/...` 0 issues (Gate 2)
- PRD-amendment commit is atomic (zero .go files), precedes all implementation commits
</verification>

<success_criteria>
- SPEC Req#5 (config + load order) satisfied and tested
- SPEC Req#12 cost-table half satisfied and tested (full cost path completes in Plan 02/05 with the usage chunk)
- A1-A5 PRD amendments committed first; OTel v1.44.0 pinned; env catalog extended
- All files ≤600 LOC; CGO-free build
</success_criteria>

<output>
Create `.planning/phases/03-llm-client-toolresult/03-01-SUMMARY.md` when done
</output>
