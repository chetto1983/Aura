---
phase: 03-llm-client-toolresult
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - internal/agent/llm_agent.go
  - internal/agent/prompt.go
  - internal/agent/tracing.go
  - internal/agent/tools/manifest.go
  - internal/agent/llm_agent_test.go
  - internal/agent/tracing_test.go
  - internal/agent/prompt_test.go
  - internal/agent/main_test.go
  - internal/agent/agenttest/fakeclient.go
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "LlmAgent implements the Agent interface; Run(InvocationContext) iter.Seq2[*Event,error] streams ordered Events (chunk -> tool_call -> tool_result -> final) and terminates via text_response"
    - "The loop checks budget step/wallclock/dedup BEFORE each LLM call; a trip emits a terminal Event with reason (max_steps | wallclock | dedup), never an iter.Seq2 error"
    - "Real infra failure (LLM wire dead) surfaces via the iter.Seq2 error slot; malformed args / unknown tool become an error tool-result the model self-corrects from"
    - "Multiple tool_calls in one assistant message dispatch sequentially, results appended in tool_call_id order, then one next LLM call"
    - "Each LLM call emits exactly one llm.request OTel span with llm.model/llm.provider/llm.prompt_tokens/llm.completion_tokens/llm.cache_hit_tokens/aura.request_id and a stable span_id; never an api_key attr"
    - "Root mints an 8-byte crypto/rand SpanID; children chain ParentSpanID = parent.SpanID (the agent.go:51-52 deferral is resolved)"
    - "The client never mutates req.Messages (byte-identical pre/post); the system prompt messages[0] carries no timestamp and is byte-stable across turns"
    - "Registry.RenderToolDefs() maps the alphabetical-sorted []ManifestEntry to []llm.ToolDef so LlmAgent builds a valid llm.Request"
    - "Emitted Events carry a non-zero UTC Timestamp; the OPENROUTER_API_KEY value appears in no Event/span/error/log"
  artifacts:
    - path: "internal/agent/llm_agent.go"
      provides: "LlmAgent: Run loop, budget gate, tool dispatch, ToolResult threading, terminal Events"
      exports: ["LlmAgent"]
    - path: "internal/agent/prompt.go"
      provides: "minimal EN tool-aware byte-stable system prompt constant (mechanism, not enumeration)"
      contains: "Always respond in Italian"
    - path: "internal/agent/tracing.go"
      provides: "TracerProvider bootstrap (otlp/stdout/none) + SpanID minting + llm.request span helper"
      contains: "newTracerProvider"
    - path: "internal/agent/tools/manifest.go"
      provides: "Registry.RenderToolDefs() []llm.ToolDef — alphabetical-sorted ManifestEntry -> llm.ToolDef mapping"
      contains: "RenderToolDefs"
  key_links:
    - from: "internal/agent/llm_agent.go"
      to: "github.com/chetto1983/aura/internal/llm.Client"
      via: "a.client.Stream(callCtx, req) drives each turn"
      pattern: "\\.Stream\\("
    - from: "internal/agent/llm_agent.go"
      to: "internal/agent/budget.go ConsumeStep"
      via: "budget gate before each LLM call -> terminal Event on trip"
      pattern: "ConsumeStep"
    - from: "internal/agent/llm_agent.go"
      to: "internal/agent/tools.NewResult"
      via: "runTool spills via the shared helper"
      pattern: "NewResult|Execute"
    - from: "internal/agent/llm_agent.go"
      to: "internal/agent/tools.Registry.RenderToolDefs"
      via: "req.Tools = registry.RenderToolDefs() builds the []llm.ToolDef wire shape"
      pattern: "RenderToolDefs"
---

<objective>
Implement `LlmAgent` — Aura's first real `Agent` implementation (D-01 commit 6): the budget-gated tool-dispatch run-loop driving the openai_compat client, threading `ToolResult` into in-memory history, streaming chunk/tool-call/tool-result/final Events, terminating via `text_response` with a content-stop fallback, plus the OTel `TracerProvider` bootstrap + full-tree SpanID minting + the per-call `llm.request` span, the minimal byte-stable system prompt, and the `Registry.RenderToolDefs()` ManifestEntry->llm.ToolDef mapping the loop needs to build a valid `llm.Request`.

Purpose: SPEC Req#9 (LlmAgent implements Agent), Req#10 (budget enforcement -> terminal Events), Req#13 (per-call span + message immutability), Req#14 prefix-stability half (byte-stable messages[0], no timestamp), and the agent-loop halves of Req#2/#12 (consume accumulated tool calls + usage). Resolves the Phase-2 deferred SpanID minting (agent.go:51-52).
Output: `internal/agent/{llm_agent.go,prompt.go,tracing.go}` + the `RenderToolDefs` mapping on `internal/agent/tools/manifest.go` + a reusable fake `llm.Client` in agenttest, tests green under `-race` + goleak.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-client-toolresult/03-SPEC.md
@.planning/phases/03-llm-client-toolresult/03-CONTEXT.md
@.planning/phases/03-llm-client-toolresult/03-RESEARCH.md
@.planning/phases/03-llm-client-toolresult/03-PATTERNS.md
@.planning/phases/03-llm-client-toolresult/03-AI-SPEC.md
@.planning/phases/03-llm-client-toolresult/03-01-SUMMARY.md
@.planning/phases/03-llm-client-toolresult/03-02-SUMMARY.md
@.planning/phases/03-llm-client-toolresult/03-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TracerProvider bootstrap + full-tree SpanID minting + minimal byte-stable system prompt</name>
  <read_first>
    - internal/agent/agent.go (full — InvocationContext.SpanID [8]byte line 51 + ParentSpanID *[8]byte line 52 currently zeroed/nil "DEFERRED to the OTel slice"; RequestID uuid.UUID line 50 already minted at root; WithContext/WithSubAgent copy semantics)
    - internal/agent/event.go:37-66 (Event shape: SpanID/ParentSpanID hex on wire lines 39-40, Timestamp RFC3339Nano UTC line 47 + MarshalJSON lines 105-116 already forces UTC — the agent must SET ev.Timestamp = time.Now().UTC())
    - cmd/aura/agent.go:135-148 (the existing uuid.NewV7() RequestID minting pattern to reuse)
    - .planning/phases/03-llm-client-toolresult/03-AI-SPEC.md lines 251-275 (AUTHORITATIVE newTracerProvider: none->no-op, stdout->stdouttrace.New, otlp->otlptracegrpc localhost:4317 silent-drop) + lines 392-399 (span attrs)
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-03/D-04/D-05/D-06 OTel stack, SpanID 8-byte crypto/rand full-tree, AURA_OTEL_EXPORTER in {stdout,otlp,none} default otlp, silent-drop; D-09 minimal EN mechanism-not-enumeration byte-stable prompt + "Always respond in Italian"; D-30 thin slog)
    - .planning/phases/03-llm-client-toolresult/03-PATTERNS.md lines 11-23 (Run takes InvocationContext; ic.Ctx; mint SpanID now; RequestID reuse) + lines 186-189 (tracing.go) + line 36 (prompt.go)
    - internal/config/config.go (from Plan 01 — OtelExporter/OtelEndpoint fields)
    - internal/agent/tools/text_response.go (const-string style for prompt.go)
  </read_first>
  <action>
    Create `internal/agent/tracing.go`: `newTracerProvider(ctx, mode, endpoint) (*sdktrace.TracerProvider, error)` exactly per AI-SPEC — `none` -> `sdktrace.NewTracerProvider()` (no exporter = zero-overhead no-op), `stdout` -> `stdouttrace.New()`, default `otlp` -> `otlptracegrpc.New(ctx, WithEndpoint(endpoint), WithInsecure())` (silent-drop without a collector, D-05 — never fail-fast, never auto-fallback-to-stdout). `otel.SetTracerProvider(tp)`; caller defers `tp.Shutdown(ctx)`. Add a `mintSpanID() [8]byte` using `crypto/rand` (D-04) and a helper to chain children (`ParentSpanID = &parent.SpanID`). Add a `startLLMSpan(ic, tracer)` helper that starts `"llm.request"` from `ic.Ctx` and a `setSpanAttrs(span, model, provider, usage, requestID)` setting `llm.model`, `llm.provider`, `llm.prompt_tokens`, `llm.completion_tokens`, `llm.cache_hit_tokens` (= usage.CachedTokens, the cached_tokens read NOT cache_write_tokens per RESEARCH delta), `aura.request_id` — and NEVER an api_key attr (D-28).
    Create `internal/agent/prompt.go`: the minimal system prompt as a package constant/function (D-09) — one-line Aura identity (domain-neutral agentic substrate), an explanation of the tool MECHANISM (you have a tool list; discover more via tool_search; terminate by calling text_response) WITHOUT enumerating tool names (enumeration cache-busts), and an explicit `Always respond in Italian` directive (EN prompt, IT output — feedback_all_prompts_in_english_only). NO timestamp, no per-turn value (KV byte-stability, D-08/D-09). Authored in English.
    Write `tracing_test.go`: use the OTel in-memory recorder (tracetest/InMemoryExporter from otel/sdk v1.44.0) to assert that, given a started+ended `llm.request` span, exactly one span is recorded with all six attrs populated and a stable span_id; assert no attribute key contains `api_key`/`key`/`authorization`. Assert `none` mode produces a no-op provider (no recorded spans / no error). Write `prompt_test.go`: assert the prompt contains `Always respond in Italian`, contains NO digit-timestamp / RFC3339 pattern, and is byte-identical when fetched twice (it is a constant — proves prefix stability seed for Req#14).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go vet ./internal/agent/ && go build ./... && go test ./internal/agent/ -run 'TestTracer|TestSpan|TestPrompt' && go test -race ./internal/agent/ -run 'TestTracer|TestPrompt'</automated>
  </verify>
  <acceptance_criteria>
    - Req#13 (span half): `go test ./internal/agent/ -run TestSpan` with the in-memory recorder asserts exactly 1 `llm.request` span with `llm.model`/`llm.provider`/`llm.prompt_tokens`/`llm.completion_tokens`/`llm.cache_hit_tokens`/`aura.request_id` populated and a stable span_id; a test asserts NO span attribute key matches api_key/authorization
    - D-04: `mintSpanID()` returns a non-zero `[8]byte` from crypto/rand; a child chains `ParentSpanID` to the parent's SpanID
    - Req#14 (prompt half): `go test ./internal/agent/ -run TestPrompt` asserts `Always respond in Italian` present, no timestamp/RFC3339 substring, byte-identical across two reads
    - D-05/D-06: `newTracerProvider(ctx, "none", "")` returns a working no-op provider with nil error; `otlp` mode does not panic / fail-fast when no collector is reachable
    - tracing.go + prompt.go each ≤600 LOC; `go test -race` green
  </acceptance_criteria>
  <done>Real OTel TracerProvider (otlp/stdout/none) wired with full-tree crypto/rand SpanID minting; the minimal byte-stable EN system prompt is in place; span + prompt halves of Req#13/#14 green.</done>
</task>

<task type="auto">
  <name>Task 2: Registry.RenderToolDefs mapping + LlmAgent.Run loop — budget gate, tool dispatch, ToolResult threading, terminal Events, immutability</name>
  <read_first>
    - internal/agent/agenttest/mocks.go:54-71 (InfiniteToolCallAgent tool-call Event emission) + :188-210 (CountingAgent.Run — THE canonical budget-gated iter.Seq2 emitter with ConsumeStep -> terminal Event-not-error + yield-after-false guard; LlmAgent.Run replicates this exact shape) + :27-32 (compile-time var _ agent.Agent)
    - internal/agent/budget.go:207-223 (ConsumeStep returns (ok bool, reason string), reason in {"max_steps","wallclock"} — NOTE "wallclock" not "max_wallclock"; AI-SPEC writes the wrong string) + budget_dedup.go (dedup consumed via the same *Budget)
    - internal/agent/event.go:37-66 (Event + LLMResponse{Content, ToolCalls, FinishReason} + Actions.StateDelta termination_reason/limit_hit; Timestamp must be set UTC)
    - internal/llm/client.go (Client.Stream + Chunk + Request + Message roles RoleAssistant/RoleTool/RoleSystem; ToolCall + the `ToolDef` shape at client.go:46-53 that RenderToolDefs produces) + the openai_compat Client + Usage from Plan 02
    - internal/agent/tools/spec.go + result.go (from Plan 03 — ToolResult, NewResult, the ctx id-injection accessors WithToolCallContext) + manifest.go:22-39 (Render returns []ManifestEntry — NOT []llm.ToolDef; this task ADDS Registry.RenderToolDefs() []llm.ToolDef mapping each ManifestEntry to llm.ToolDef, keeping the alphabetical sort at manifest.go:37 which is cache-stability-load-bearing)
    - internal/agent/main_test.go (check if a Phase-2 goleak harness `goleak.VerifyTestMain(m)` is already present; preserve it if so, add only if absent)
    - cmd/aura/agent.go:118-129 (the `for ev, runErr := range root.Run(ic)` consumption pattern + RequestID stamping)
    - .planning/phases/03-llm-client-toolresult/03-AI-SPEC.md lines 360-425 (AUTHORITATIVE LlmAgent.Run loop — but apply RESEARCH deltas: Run takes InvocationContext not ctx; reason "wallclock"; Render returns ManifestEntry) + lines 455-473 (parseTextResponse) + lines 478-505 (textExtractor.feed)
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-13 text_response sole terminal + content-stop fallback + incremental text extraction; D-14 multiple tool_calls sequential, RoleTool in tool_call_id order, one next call; D-15 tool errors -> error tool-result never terminal, iter.Seq2 error only for real infra fail; D-16 tool_choice auto; D-21 finish_reason length -> partial + `[risposta troncata: max_tokens]`; D-26 session_id=ThreadID; D-29 discard partial assistant msg on cancel)
    - .planning/phases/03-llm-client-toolresult/03-PATTERNS.md lines 129-182 (the exact CountingAgent.Run shape to replicate; Render->ToolDef mapping guidance line 182; fakeClient guidance; Event Timestamp UTC)
    - internal/knowledge/client_unit_test.go:84-100 (anti-leak test template)
  </read_first>
  <behavior>
    - Test 1 (Req#9): LlmAgent.Run against a fake llm.Client yields ordered Events (chunk -> tool_call -> tool_result -> final) and terminates on text_response; race + goleak clean
    - Test 2 (Req#10): step cap — a fake client that always returns the same tool call trips max_steps, emitting a terminal Event with reason "max_steps", total steps <= cap
    - Test 3 (Req#10): wallclock cap — trips with reason "wallclock" (injected clock); dedup window — repeated identical tool call trips with reason "dedup"; all three are terminal Events, NEVER iter.Seq2 errors
    - Test 4 (Req#13): exactly one llm.request span per LLM call with stable span_id; req.Messages is byte-identical pre/post the call (immutability)
    - Test 5 (D-15): a malformed-args / unknown-tool call -> a RoleTool error message appended (model self-corrects), the loop continues (bounded by budget); a real infra failure (fake client returns a Stream error) -> the iter.Seq2 error slot
    - Test 6 (D-14): a fake assistant message with 2 tool_calls -> both dispatched sequentially, two RoleTool results appended in tool_call_id order, then one next LLM call
    - Test 7 (Req#14): messages[0] (the system prompt) is byte-identical across two turns of a 2-turn run
    - Test 8 (D-28): a sentinel APIKey never appears in any emitted Event, recorded span, error, or captured log
    - Test 9 (D-21): finish_reason "length" -> final Event content carries the partial + `[risposta troncata: max_tokens]`, no auto-continue
    - Test 10 (RenderToolDefs): `Registry.RenderToolDefs()` returns `[]llm.ToolDef` in the same alphabetical order as `Render()`, one ToolDef per registered tool, with Name/Description/Parameters carried from each ManifestEntry/Spec
  </behavior>
  <action>
    Add `Registry.RenderToolDefs() []llm.ToolDef` to `internal/agent/tools/manifest.go` (BLOCKER-4 mapping): map each `ManifestEntry` produced by the existing alphabetical-sorted `Render()` to an `llm.ToolDef` (`client.go:46-53` shape — Name + Description + Parameters JSON schema). Keep the single alphabetical sort in `Render()` (manifest.go:37, cache-stability-load-bearing — `feedback_aura_cache_poisoning_sites`); `RenderToolDefs` reuses that ordering, it does NOT re-sort. This is the surface `LlmAgent.Run` calls to build `req.Tools` — without it the agent cannot build a valid `llm.Request`.
    Create `internal/agent/agenttest/fakeclient.go`: a `FakeClient` implementing `llm.Client` that returns a caller-supplied `<-chan llm.Chunk` script (with an optional Stream error) — the deterministic driver for LlmAgent tests (PATTERNS: write a new fakeClient; REUSE the existing mock AGENTS, do not duplicate them). Add `var _ llm.Client = (*FakeClient)(nil)`.
    Create `internal/agent/llm_agent.go`: `LlmAgent` implementing `agent.Agent` (Name/Description/Run/SubAgents/FindAgent; `var _ agent.Agent = (*LlmAgent)(nil)`). Fields: `client llm.Client`, `cfg llm.Config`, `registry *tools.Registry`, `history []llm.Message` (in-memory, D-26), `tracer`, plus the system prompt at history[0]. `Run(ic agent.InvocationContext) iter.Seq2[*Event, error]` replicates the CountingAgent.Run skeleton (mocks.go:188-210):
    - Wrap `ic.Ctx` with `context.WithTimeout(ic.Ctx, cfg.TotalTimeoutSec)` for each call; mint/propagate SpanID (Task-1 helpers).
    - Budget gate BEFORE each call: `ok, reason := ic.Budget.ConsumeStep()`; if `!ok` -> yield a terminal Event with `Actions.StateDelta{termination_reason, limit_hit:reason}` (reason "max_steps"/"wallclock") then return — NEVER the error slot (D-04). Dedup checked via the same *Budget.
    - Start the `llm.request` span; build `llm.Request{Model, Messages: a.history (read-only — never mutate), Tools: registry.RenderToolDefs(), Temperature, MaxTokens}` with tool_choice auto handled in the client; `ch, err := client.Stream(callCtx, req)`; on err -> span.End, yield(nil, err) (real infra fail, D-15), return.
    - Consume the channel: stream chunk Events as they arrive (set Event.Timestamp = time.Now().UTC()); accumulate text + finalized tool calls + usage; set span attrs (Task-1 helper) + span.End.
    - No tool calls -> terminal: if content non-empty accept it (content-stop fallback, D-13/D-16); finish_reason "length" -> append `[risposta troncata: max_tokens]` to the partial (D-21); append assistant message to history; yield final Event; return.
    - Tool calls present -> append the assistant tool_calls message; for each call sequentially (D-14): if name == "text_response" -> extract its `text` arg (parseTextResponse / the incremental extractor — structural, not regex), yield the final Event, return; else `runTool`: inject session_id+tool_call_id+run_dir into ctx (tools.WithToolCallContext), call `tool.Execute` -> ToolResult, append a RoleTool message with `res.Preview` (incl. footer if Truncated) keyed by call.ID; malformed args / unknown tool (registry miss or Execute error) -> append a RoleTool error message (D-15), NOT terminal, continue. Then loop (next call sees appended results).
    - The yield-after-false guard (`if !yield(...) { return }`) is mandatory.
    Write `llm_agent_test.go` per the behavior block using FakeClient + the existing agenttest mocks + an injectable clock for the wallclock cap + the in-memory OTel recorder; include the `TestRenderToolDefs` ordering/mapping test. Reuse the knowledge anti-leak test shape for Test 8 (release-blocking). Ensure `internal/agent/main_test.go` has `goleak.VerifyTestMain` (preserve the Phase-2 harness if present; add only if absent).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go vet ./internal/agent/... && go build ./... && go test ./internal/agent/ ./internal/agent/tools/ -run 'TestLlmAgent_EventOrder|TestLlmAgent_StepCap_Trips|TestLlmAgent_WallclockCap_Trips|TestLlmAgent_DedupWindow_Trips|TestMessagesImmutable|TestLlmAgent_ToolError|TestLlmAgent_SequentialToolCalls|TestPrefixStable|TestLlmAgent_SecretRedaction|TestLlmAgent_LengthTruncation|TestRenderToolDefs' && go test -race ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
    - Req#9: `go test -race ./internal/agent/ -run TestLlmAgent_EventOrder` yields ordered Events (chunk->tool_call->tool_result->final) and terminates; goleak clean
    - Req#10: `go test ./internal/agent/ -run 'TestLlmAgent_(StepCap|WallclockCap|DedupWindow)_Trips'` — each asserts a terminal Event with the correct reason ("max_steps"/"wallclock"/"dedup") and total steps <= cap; the trip is an Event, NOT an iter.Seq2 error
    - Req#13: `go test ./internal/agent/ -run 'TestSpan|TestMessagesImmutable'` — exactly 1 span/call with stable span_id; req.Messages byte-identical pre/post
    - BLOCKER-4 (RenderToolDefs): `go build ./...` compiles with `req.Tools: registry.RenderToolDefs()` in llm_agent.go (grep confirms `RenderToolDefs` is defined in manifest.go AND called in llm_agent.go); `go test ./internal/agent/tools/ -run TestRenderToolDefs` asserts the `[]llm.ToolDef` is alphabetical-ordered, one per tool, Name/Description/Parameters carried
    - D-15: `TestLlmAgent_ToolError` asserts a RoleTool error message is appended (loop continues) for malformed args/unknown tool; a real Stream error surfaces in the error slot
    - D-14: `TestLlmAgent_SequentialToolCalls` asserts 2 tool_calls -> 2 RoleTool results in tool_call_id order -> one next LLM call
    - Req#14: `TestPrefixStable` asserts messages[0] byte-identical across 2 turns; D-28 `TestLlmAgent_SecretRedaction` asserts the sentinel key absent from every Event/span/error/log (release-blocking)
    - D-21: `TestLlmAgent_LengthTruncation` asserts the final content carries `[risposta troncata: max_tokens]`, no auto-continue
    - `var _ agent.Agent = (*LlmAgent)(nil)` and `var _ llm.Client = (*FakeClient)(nil)` compile; all files ≤600 LOC; `go test -race ./internal/agent/...` green
  </acceptance_criteria>
  <done>LlmAgent drives the budget-gated tool-dispatch loop with sequential dispatch, ToolResult threading, content-stop fallback, terminal budget Events, per-call spans, message immutability, structural secret redaction, and the Registry.RenderToolDefs mapping that builds req.Tools — Req#9/#10/#13 + the loop halves of #2/#12/#14 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output (tool calls, args) -> tool dispatch | Untrusted model-supplied tool names + JSON args drive Execute |
| budget / loop -> spend | An unbounded loop burns real money; the budget gate is the control |
| API key -> Event/span/log | Any Event/span/error emission could leak the key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | API key in Event/span/error/log | mitigate | No api_key span attr (setSpanAttrs excludes it); slog request_id-correlated no-secrets (D-30); the LlmAgent anti-leak test asserts the sentinel key absent from every Event/span/error/log (release-blocking) |
| T-03-10 | Denial of Service (cost) | runaway tool loop / malformed-arg retry storm burning spend | mitigate | Budget gate BEFORE every call (ConsumeStep step/wallclock + dedup ring) -> terminal Event; malformed args -> bounded error tool-result not unbounded retry (Req#10, D-15) |
| T-03-11 | Tampering | malformed tool args crashing the loop | mitigate | parse error -> error tool-result the model self-corrects from; never a panic; the iter.Seq2 error slot reserved for real infra failure only (D-15) |
| T-03-12 | Repudiation / integrity | KV-cache prefix poisoning (clock/per-turn value in messages[0]) | mitigate | System prompt is a constant with no timestamp / no tool-name enumeration; TestPrefixStable asserts byte-stability across turns; time only via current_time tool (D-08/D-09) |
</threat_model>

<verification>
- `go test -race ./internal/agent/...` green with goleak (VerifyTestMain present)
- `go vet ./internal/agent/...` clean; `go build ./...` CGO-free
- `golangci-lint run ./internal/agent/...` 0 issues
- In-memory OTel recorder test proves exactly 1 span/call with all attrs + stable span_id
- ConsumeStep reason strings are the REAL "max_steps"/"wallclock" (not the AI-SPEC's "max_wallclock")
- `Registry.RenderToolDefs()` defined in manifest.go and consumed by llm_agent.go (grep confirms both)
</verification>

<success_criteria>
- SPEC Req#9, #10, #13 fully satisfied; Req#2/#12/#14 loop halves satisfied
- Phase-2 deferred SpanID minting resolved (full-tree crypto/rand)
- Terminal budget Events (never error slot); error tool-result self-correction (never panic)
- The ManifestEntry->llm.ToolDef mapping (RenderToolDefs) lets LlmAgent build a valid llm.Request
- Secret-redaction anti-leak test green (release-blocking); all files ≤600 LOC
</success_criteria>

<output>
Create `.planning/phases/03-llm-client-toolresult/03-04-SUMMARY.md` when done
</output>
