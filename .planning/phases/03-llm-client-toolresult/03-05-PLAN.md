---
phase: 03-llm-client-toolresult
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - cmd/aura/chat.go
  - cmd/aura/config.go
  - cmd/aura/main.go
  - cmd/aura/chat_test.go
  - cmd/aura/config_test.go
  - scripts/llm_smoke.sh
autonomous: false
requirements: [CORE-01]
user_setup:
  - service: openrouter
    why: "Live aura chat streaming + the manual real-OpenRouter smoke (SPEC Req#11 acceptance). Deterministic CI tier needs no key."
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter Dashboard -> Keys (https://openrouter.ai/keys)"
must_haves:
  truths:
    - "aura chat reads stdin turns in a loop, streams each reply token-by-token as clean prose (never raw tool-call JSON), keeps in-memory history across turns, prints a per-turn token+USD cost footer, exits cleanly on EOF/'/exit'"
    - "Two scripted stdin turns produce two streamed replies sharing one session_id; the second turn sees the first in in-memory history"
    - "A missing API key fails the first turn with a clear stderr message and no panic"
    - "First Ctrl+C aborts the in-flight turn (HTTP aborts ~100ms) and returns to the prompt; second Ctrl+C / EOF / '/exit' quits cleanly, goleak-clean"
    - "aura config show prints the effective config with APIKey REDACTED; get reads; set writes ~/.aura/llm.json (creating it if absent)"
    - "The OTel TracerProvider is wired from AURA_OTEL_EXPORTER and shut down (batch flushed) on REPL exit"
    - "scripts/llm_smoke.sh is gated on OPENROUTER_API_KEY and is NOT a CI job (manual acceptance only)"
  artifacts:
    - path: "cmd/aura/chat.go"
      provides: "aura chat REPL: mint session ThreadID, drive LlmAgent.Run, stream prose, cost footer, two-stage Ctrl+C"
      contains: "chat"
    - path: "cmd/aura/config.go"
      provides: "aura config show/get/set over ~/.aura/llm.json with redacted show"
      contains: "REDACTED"
    - path: "scripts/llm_smoke.sh"
      provides: "manual real-OpenRouter smoke, OPENROUTER_API_KEY-gated, NOT CI"
      contains: "OPENROUTER_API_KEY"
  key_links:
    - from: "cmd/aura/chat.go"
      to: "internal/agent.LlmAgent.Run"
      via: "the REPL ranges over Run(ic) and renders Events"
      pattern: "\\.Run\\("
    - from: "cmd/aura/main.go"
      to: "cmd/aura/chat.go + cmd/aura/config.go"
      via: "the main switch dispatches chat + config"
      pattern: "case \"(chat|config)\""
---

<objective>
Land the user-facing surface (D-01 commit 7): the `aura chat` interactive in-memory REPL driving `LlmAgent.Run` with token-by-token prose streaming (incremental text_response extraction), the per-turn token+USD cost footer, two-stage Ctrl+C, the OTel exporter wired from `AURA_OTEL_EXPORTER` and flushed on exit; the `aura config show/get/set` subcommand over `~/.aura/llm.json` with a redacted `show`; and the manual `scripts/llm_smoke.sh` real-OpenRouter acceptance gate (NOT CI). Ends with a human-verify checkpoint for the live SPEC Req#11 / ROADMAP SC#1 acceptance.

Purpose: SPEC Req#11 (aura chat REPL), the user-facing half of Req#12 (cost footer) and Req#13 (real exporter run), and D-10/D-11/D-12/D-24/D-31. This is the only plan with a checkpoint — the deterministic tier (Plans 01-04) already proves all logic without network; the checkpoint verifies live prose + a non-zero cost footer the product owner eyeballs.
Output: `cmd/aura/{chat.go,config.go}` + main.go wiring + `scripts/llm_smoke.sh`; deterministic scripted-stdin tests green; one human-verify checkpoint.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-client-toolresult/03-SPEC.md
@.planning/phases/03-llm-client-toolresult/03-CONTEXT.md
@.planning/phases/03-llm-client-toolresult/03-RESEARCH.md
@.planning/phases/03-llm-client-toolresult/03-PATTERNS.md
@.planning/phases/03-llm-client-toolresult/03-AI-SPEC.md
@.planning/phases/03-llm-client-toolresult/03-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: aura config show/get/set + aura chat REPL + cost footer + two-stage Ctrl+C + OTel wiring</name>
  <read_first>
    - cmd/aura/main.go (full — the switch lines 29-45 where `chat`/`shell`/`serve` print TODO at line 41; `buildRegistry()` lines 52-57 registers TextResponse + ToolSearch — extend to register read_tool_output + current_time; usage line 49)
    - cmd/aura/db.go:19-44 (the subcommand dispatch + config.Load + os.Exit(1) pattern that `aura config` mirrors)
    - cmd/aura/agent.go:46-66 + :89-148 (the dryRun loop driver: build InvocationContext, mint RequestID via uuid.NewV7() lines 135-148, `for ev, runErr := range root.Run(ic)` lines 118-129, Event rendering)
    - internal/agent/llm_agent.go + prompt.go + tracing.go (from Plan 04 — LlmAgent constructor, the system prompt, newTracerProvider)
    - internal/llm/config.go + prices.go (from Plan 01 — Config fields, CostUSD helper for the footer)
    - internal/config/config.go (Load, RunDir, ToolPreviewCap, OtelExporter/OtelEndpoint)
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-10 two-stage Ctrl+C via signal.NotifyContext; D-11 footer `· {tok} ({in}/{out}) · ${usd} · {lat}s` USD from CostUSD; D-12 dim tool-activity one-liner; D-24 aura config show redacted/get/set ~/.aura/llm.json; D-26 session_id=ThreadID minted once/session; D-29 discard partial assistant msg on cancel)
    - .planning/phases/03-llm-client-toolresult/03-AI-SPEC.md lines 478-505 (textExtractor.feed incremental JSON-string scanner for prose rendering — structural, not regex) + lines 251-275 (TracerProvider bootstrap usage at the REPL edge, defer tp.Shutdown)
    - .planning/phases/03-llm-client-toolresult/03-PATTERNS.md lines 223-245 (chat.go/config.go controller analogs; mint ThreadID; wire into main.go; buildRegistry extension; cost footer + dim feedback have no in-repo analog -> AI-SPEC §4b reference)
  </read_first>
  <action>
    Create `cmd/aura/config.go`: `runConfig(args)` mirroring `runDB` dispatch (db.go:19-44). `show` -> load the effective `llm.Config` and print it with `APIKey` shown as `REDACTED` (D-24/D-28 — never print the real key); `get <key>` -> read a dotted key (`llm.model`, `llm.base_url`, `llm.temperature`, etc.) from the effective config; `set <key> <value>` -> read-modify-write `~/.aura/llm.json` (create the file + dir if absent, via the os.UserHomeDir + filepath.Join helper), persisting only the file-tier keys. Unknown key / bad usage -> stderr + os.Exit(1).
    Create `cmd/aura/chat.go`: `runChat(args)`. Load config (config.Load); on empty APIKey error -> clear stderr message + os.Exit non-zero, NO panic (Req#11). Build the TracerProvider via `newTracerProvider(ctx, cfg.OtelExporter, cfg.OtelEndpoint)` and `defer tp.Shutdown(ctx)` (flush the batch on exit, Req#13). Mint one session `ThreadID` via `uuid.NewV7()` (D-26, agent.go:135-148 pattern), kept for the whole session. Build the `LlmAgent` over `buildRegistry()` (extended to register read_tool_output + current_time) + the openai_compat client + the system prompt. Two-stage Ctrl+C via `signal.NotifyContext` (D-10): first SIGINT cancels the in-flight turn's ctx (HTTP aborts ~100ms via the openai_compat ctx wiring) and returns to the prompt with the partial assistant message discarded (D-29); a second consecutive SIGINT, or EOF, or `/exit` quits cleanly. REPL loop: read a stdin line -> append RoleUser to history -> mint a fresh RequestID per turn -> range over `LlmAgent.Run(ic)`: render text-Event content token-by-token as clean prose using the incremental text extractor (AI-SPEC §4b — extract the text_response `text` value from the streaming tool-call args, never show raw JSON); on a tool_call Event print a dim one-liner (D-12); on the final Event print the cost footer `· {tok} ({in}/{out}) · ${usd} · {lat}s` where USD = `prices.CostUSD(...)` preferring the provider usage.cost, `n/a` for unknown (D-11/D-23). goleak-clean teardown.
    Wire `cmd/aura/main.go`: replace the `chat` TODO at line 41 with `case "chat": runChat(os.Args[2:])` and add `case "config": runConfig(os.Args[2:])`; update `buildRegistry()` to also `reg.Register(&tools.ReadToolOutput{...})` + `reg.Register(tools.CurrentTime{})`; update the usage string.
    Write `cmd/aura/chat_test.go`: scripted-stdin test (Req#11) driving `runChat` (or its testable inner loop) with a FakeClient (reuse agenttest.FakeClient from Plan 04) — 2 turns produce 2 streamed replies sharing one session_id, the second turn sees the first in-memory; a missing-API-key path asserts a clear stderr error and no panic; assert the rendered output is clean prose (no `{"text":` / raw JSON fragments) and a cost footer line is printed. Write `cmd/aura/config_test.go`: `set` writes ~/.aura/llm.json in a temp HOME (t.Setenv HOME / t.TempDir); `show` redacts the APIKey (assert the real key value is absent, `REDACTED` present); `get` reads back. Reuse the anti-leak assertion shape. Keep cmd/aura files ≤600 LOC (split chat rendering helpers into chat.go vs a small chat_render.go if needed — refactor-on-touch).
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go vet ./cmd/aura/ && go build ./... && go test ./cmd/aura/ -run 'TestChat|TestConfig' && go test -race ./cmd/aura/ && golangci-lint run ./cmd/aura/...</automated>
  </verify>
  <acceptance_criteria>
    - Req#11: `go test ./cmd/aura/ -run TestChat` — 2 scripted turns -> 2 streamed replies sharing one session_id, second turn sees the first; missing-key path -> clear stderr error, no panic; rendered output contains NO `{"text":` / raw-JSON fragment (clean prose)
    - D-24: `go test ./cmd/aura/ -run TestConfig` — `set` creates/updates ~/.aura/llm.json under a temp HOME; `show` output contains `REDACTED` and NOT the real APIKey value; `get llm.model` returns the effective model
    - `aura chat` without a key fails the first turn with a clear stderr message (the test captures stderr) and exit != 0, no panic
    - main.go dispatches `chat` + `config` (grep `case "chat"` + `case "config"`); buildRegistry registers read_tool_output + current_time (grep confirms both)
    - The cost footer renders `· N tok (... in / ... out) · $... · ...s` with USD from CostUSD (n/a for unknown model, never $0)
    - `go test -race ./cmd/aura/` goleak-clean; `golangci-lint run ./cmd/aura/...` 0 issues; all cmd/aura files ≤600 LOC
  </acceptance_criteria>
  <done>aura chat is a working in-memory REPL with prose streaming, cost footer, two-stage Ctrl+C and OTel-wired-on-exit; aura config show/get/set manages ~/.aura/llm.json with a redacted show; deterministic scripted-stdin tests green.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/llm_smoke.sh manual real-OpenRouter acceptance gate (NOT CI)</name>
  <read_first>
    - internal/knowledge/smoke_test.go:38-45 (the envOrSkipCI / OPENROUTER_API_KEY-gate pattern + the no-skip-as-green discipline)
    - scripts/loop_budget_smoke.sh (the Phase-2 smoke script style — grep-contract shape, exit codes)
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-31 manual real smoke gated on OPENROUTER_API_KEY NOT in CI; specifics line 208: two-turn scripted stdin asserting a streamed reply + a non-zero token+USD footer)
    - .planning/phases/03-llm-client-toolresult/03-AI-SPEC.md lines 605-610 (smoke scenarios: ping, tool-using turn, length turn, optional two-turn, optional cancel)
    - cmd/aura/chat.go (from Task 1 — the REPL the smoke drives)
    - CLAUDE.md §No-skip-as-green (the smoke is explicitly NOT a CI gate — document WHY in the script header)
  </read_first>
  <action>
    Create `scripts/llm_smoke.sh`: a manual acceptance script gated on `OPENROUTER_API_KEY` (if unset -> print a clear "smoke skipped: OPENROUTER_API_KEY unset (manual gate, not CI)" message and exit 0 — this is the ONLY legitimate skip because it is explicitly NOT a CI job; the header comment documents the no-skip-as-green rationale: nothing in CI pretends to hit a live LLM, the deterministic tier in Plans 01-04 genuinely exercises every parse/loop/redaction path). When the key is set: pipe a two-turn scripted stdin into `go run ./cmd/aura chat` (or the built binary), assert the output contains a streamed prose reply and a cost-footer line with a NON-ZERO token count and a USD figure (grep the footer pattern, fail if `0 tok` or absent). Optionally run a tool-provoking turn (e.g. ask for the current time -> current_time) and a length-likely turn. Exit non-zero on any failed assertion. Do NOT wire this into Makefile `quality`/`quality-full` or any CI workflow.
    Add a one-line note to the script header that this is the SPEC Req#11 / ROADMAP SC#1 live acceptance gate, run locally by the operator.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && bash -n scripts/llm_smoke.sh && OPENROUTER_API_KEY= bash scripts/llm_smoke.sh; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/llm_smoke.sh` parses clean (no syntax errors)
    - With `OPENROUTER_API_KEY` unset the script exits 0 with a "skipped: ... not CI" message (the legitimate manual-gate skip, documented)
    - The script is NOT referenced in any CI workflow / Makefile `quality*` target (grep the Makefile + .github confirms absence)
    - The script header documents the no-skip-as-green rationale + that it is the Req#11 live acceptance gate
  </acceptance_criteria>
  <done>scripts/llm_smoke.sh is a documented manual real-OpenRouter acceptance gate, OPENROUTER_API_KEY-gated, explicitly out of CI, asserting a streamed reply + non-zero token+USD footer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: live aura chat acceptance (Req#11 / ROADMAP SC#1)</name>
  <action>Human-verify only — confirm live prose + non-zero cost footer + two-stage Ctrl+C + redacted config show. No code change; follow how-to-verify below.</action>
  <what-built>
    The full Phase-3 user-facing surface: `aura chat` interactive REPL streaming clean prose from `deepseek/deepseek-v4-flash:exacto` via OpenRouter with a per-turn token+USD cost footer, two-stage Ctrl+C, `read_tool_output` + `current_time` builtins, ToolResult sidecar spillover, per-call OTel spans, and `aura config show/get/set`. All deterministic logic (parser, accumulator, cancel, budget, redaction, cost-table) is already proven by the CI tier in Plans 01-04. This checkpoint covers the one Human dimension: live prose quality + a real non-zero cost footer (SPEC Req#11 / ROADMAP SC#1).
  </what-built>
  <how-to-verify>
    1. Set your key: `export OPENROUTER_API_KEY=sk-or-...` (from https://openrouter.ai/keys).
    2. Run `bash scripts/llm_smoke.sh` and confirm it reports a streamed reply + a non-zero token+USD footer (no `0 tok`, no `$n/a` for the known default model).
    3. Run `aura chat` interactively (`go run ./cmd/aura chat`): type a message, confirm Aura streams a clean Italian prose reply (NO raw `{"text":` JSON), then a footer like `· 412 tok (180 in / 232 out) · $0.0003 · 1.4s`.
    4. Ask "che ore sono?" and confirm a dim `current_time` activity line appears, then a prose answer.
    5. During a long generation press Ctrl+C once: confirm the turn aborts within ~1s and you return to the prompt (no hang); press Ctrl+C again (or type `/exit`) to quit cleanly.
    6. Run `aura config show` and confirm the APIKey prints as `REDACTED` (not your real key).
    7. (Optional) If you run an OTLP collector on localhost:4317, confirm one `llm.request` span per turn appears with `llm.model`/`prompt_tokens`/`completion_tokens`/`cache_hit_tokens`; otherwise confirm the REPL runs normally with the collector absent (silent-drop).
  </how-to-verify>
  <resume-signal>Type "approved" once live prose + a non-zero cost footer + clean two-stage Ctrl+C + redacted config show are confirmed; otherwise describe what rendered wrong (raw JSON leak, $0/wrong cost, hang on Ctrl+C, key leak).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin (operator) -> REPL -> agent loop | Operator-supplied turns drive the loop |
| config CLI -> ~/.aura/llm.json | set writes a file holding config (the key normally comes from .env, not this file) |
| API key -> config show / footer / logs | show / footer rendering must never print the key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | API key in `aura config show` / footer / REPL output | mitigate | `show` renders APIKey as `REDACTED`; the footer prints only tokens+USD+latency (no key); config_test asserts the real key value is absent from `show` output (release-blocking discipline) |
| T-03-04 | Denial of Service | unbounded REPL / runaway turn | mitigate | Each turn drives the budget-gated LlmAgent.Run (Plan 04) — step/wallclock/dedup terminate the turn; two-stage Ctrl+C aborts an in-flight turn within ~100ms |
| T-03-13 | Information Disclosure | prompt content / key in OTel export | mitigate | Span attrs exclude api_key and prompt content (Plan 04 setSpanAttrs); OTLP default localhost:4317 (local), silent-drop without a collector |
| T-03-SC | Tampering | no package installs in this plan | accept | This plan adds no new dependencies (uses Plan-01 OTel + existing deps); no legitimacy checkpoint required |
</threat_model>

<verification>
- `go test -race ./cmd/aura/` green with goleak
- `go vet ./cmd/aura/` clean; `go build ./...` CGO-free
- `golangci-lint run ./cmd/aura/...` 0 issues (this plan-level block is the authoritative lint gate; the task-level `golangci-lint` in Task 1 is fast-feedback)
- `make quality-full` green: owned-surface coverage ≥85% across the full tag matrix (CLAUDE.md hard floor, overrides PRD 75/60) — report the combined figure
- `scripts/llm_smoke.sh` is NOT in CI; the human-verify checkpoint covers the live Req#11 acceptance
- Mutation spot-check ≥70% killed on the phase's critical file(s) (e.g. llm_agent.go / sse.go / result.go) documented in VALIDATION Manual-Only
</verification>

<success_criteria>
- SPEC Req#11 satisfied (deterministic scripted-stdin test + manual smoke + human-verify); the user-facing halves of Req#12/#13 satisfied
- aura chat streams clean prose + cost footer; aura config redacts the key; OTel wired+flushed on exit
- The 4 ROADMAP SC: SC#1 (streamed reply + token+USD) via the checkpoint; SC#2 (Ctrl+C ~100ms no leak) via Plan-02 + the checkpoint; SC#3 (50KB tool output -> preview + sidecar + read_tool_output) via Plan-03; SC#4 (OTel spans with attrs) via Plan-04 + the optional collector check
- Phase closes only after golangci-lint=0, coverage ≥85% combined, green CI (CLAUDE.md phase-end mandate)
</success_criteria>

<output>
Create `.planning/phases/03-llm-client-toolresult/03-05-SUMMARY.md` when done
</output>
