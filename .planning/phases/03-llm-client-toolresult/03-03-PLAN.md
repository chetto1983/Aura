---
phase: 03-llm-client-toolresult
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/agent/tools/spec.go
  - internal/agent/tools/result.go
  - internal/agent/tools/text_response.go
  - internal/agent/tools/search.go
  - internal/agent/tools/read_tool_output.go
  - internal/agent/tools/current_time.go
  - internal/agent/tools/result_test.go
  - internal/agent/tools/read_tool_output_test.go
  - internal/agent/tools/current_time_test.go
  - internal/agent/tools/text_response_test.go
  - internal/agent/tools/search_test.go
  - internal/agent/tools/main_test.go
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "Tool.Execute returns (ToolResult, error); all existing tools (text_response, tool_search) adapt in one coupled commit"
    - "A 100KB tool output leaves only a ≤2KiB preview+footer in history and writes the full bytes to a sidecar file"
    - "A ≤cap (2048B) tool output writes no sidecar file and the history content equals the raw preview"
    - "The preview is truncated on a UTF-8 rune boundary (never splits a multi-byte rune)"
    - "read_tool_output(tool_call_id, offset, limit) returns the correct BYTE slice; an unknown id hard-fails with an error (not empty/panic)"
    - "The sidecar lives at $AURA_RUN_DIR/conversations/<session_id>/<tool_call_id>.result"
    - "current_time returns parseable RFC-3339 UTC by default and the correct offset for an IANA timezone arg"
    - "A sidecar write failure returns preview + a 'full output unavailable' note and the turn continues"
    - "A session_id/tool_call_id bearing `..` or a path separator is rejected before filepath.Join — the sidecar never escapes <run_dir>/conversations/ (T-03-07)"
  artifacts:
    - path: "internal/agent/tools/spec.go"
      provides: "Tool.Execute (ToolResult, error) signature + ToolResult{Preview, FullPath, Bytes, Truncated}"
      contains: "ToolResult"
    - path: "internal/agent/tools/result.go"
      provides: "shared tools.NewResult(ctx, content) preview/sidecar spillover helper (ctx-injected ids) with id path-traversal rejection"
      contains: "func NewResult"
    - path: "internal/agent/tools/read_tool_output.go"
      provides: "non-deferred builtin, byte offset/limit, unknown id hard-fail"
      contains: "read_tool_output"
    - path: "internal/agent/tools/current_time.go"
      provides: "non-deferred builtin, RFC-3339 UTC + optional IANA tz"
      contains: "current_time"
  key_links:
    - from: "internal/agent/tools/result.go"
      to: "$AURA_RUN_DIR/conversations/<session_id>/<tool_call_id>.result"
      via: "filepath.Join + lazy os.MkdirAll from validated ctx-injected ids"
      pattern: "conversations"
    - from: "internal/agent/tools/text_response.go"
      to: "internal/agent/tools/spec.go ToolResult"
      via: "Execute returns ToolResult"
      pattern: "ToolResult"
---

<objective>
Land the ToolResult coupled migration + the two new non-deferred builtins (D-01 commits 4+5): migrate `Tool.Execute` from `(string, error)` to `(ToolResult, error)` across `spec.go` + `text_response.go` + `search.go` in ONE coupled commit (SPEC Constraint — avoid re-opening the ≤600-LOC files twice), add the shared `tools.NewResult` preview/sidecar spillover helper (D-25), the byte-ranged `read_tool_output` builtin (D-27/A4), and the cache-safe `current_time` builtin (Req#14).

Purpose: SPEC Req#6 (ToolResult preview/sidecar), Req#7 (read_tool_output byte ranges, unknown-id hard-fail), Req#8 (sidecar layout), Req#14 (current_time + UTC discipline). The ctx-injected-ids spillover keeps tools DRY (CLAUDE.md reusable-code). No llm.Config dependency — runs parallel with Plan 01 (disjoint files).
Output: migrated tools package with the spillover helper + two builtins + tests green under `-race`.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-client-toolresult/03-SPEC.md
@.planning/phases/03-llm-client-toolresult/03-CONTEXT.md
@.planning/phases/03-llm-client-toolresult/03-RESEARCH.md
@.planning/phases/03-llm-client-toolresult/03-PATTERNS.md
@.planning/phases/03-llm-client-toolresult/03-AI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coupled Tool.Execute → (ToolResult, error) migration + tools.NewResult spillover helper</name>
  <read_first>
    - internal/agent/tools/spec.go (full — `Tool` interface line 30-33 `Execute(ctx, args) (string, error)` is what migrates; `Spec`, `Registry`)
    - internal/agent/tools/text_response.go (full — `TextResponse.Execute` line 35-43 arg-parse pattern; the const-string style for prompt.go reference)
    - internal/agent/tools/search.go (full — `ToolSearch.Execute` line 46; adapt to ToolResult)
    - internal/config/config.go:81-82 (RunDir + ToolPreviewCap default 2048 already wired) + defaultRunDir lines 128-133 (filepath.Join template)
    - .planning/phases/03-llm-client-toolresult/03-AI-SPEC.md §3 Pattern 3 lines 193-205 (the UTF-8 rune-boundary `truncatePreview`, byte-based footer format, sidecar-fail → preview + note) — AUTHORITATIVE
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-25 shared helper ctx-injected session_id+tool_call_id+run_dir; D-26 sidecar dir $AURA_RUN_DIR/conversations/<ThreadID>/<tool_call_id>.result; D-29 sidecar-fail degradation)
    - .planning/phases/03-llm-client-toolresult/03-PATTERNS.md `result.go` section lines 192-196 + the "CRITICAL EDIT coupled D-01 commit 4" note lines 217
    - internal/agent/tools/manifest.go (Render — unaffected, but note alphabetical sort for cache stability)
  </read_first>
  <behavior>
    - Test 1 (Req#6 large): a fake tool returning 100KB → ToolResult.Truncated true, ToolResult.Bytes 100000, Preview ≤2048B; the history content (Preview + footer pointer) ≤ ~2KiB; the sidecar file holds the full 100KB
    - Test 2 (Req#6 small): a ≤2048B output → no sidecar file written, ToolResult.Truncated false, history content equals the raw preview, FullPath empty
    - Test 3 (Req#6 property, rapid): for any string + any cap, truncatePreview output is valid UTF-8, ≤ cap bytes, and a prefix of the input
    - Test 4 (Req#8): the sidecar file path is exactly `<run_dir>/conversations/<session_id>/<tool_call_id>.result`; the dir is created lazily on first persist
    - Test 5 (D-29): a sidecar write failure (e.g. unwritable run_dir) → ToolResult returns Preview + a `full output unavailable` note, no error propagated as terminal (turn continues)
    - Test 6 (migration): TextResponse + ToolSearch compile and pass under the new `(ToolResult, error)` signature; their existing behavior (text echo / search results) preserved
    - Test 7 (T-03-07 path traversal): `TestSidecarPathTraversal` — inject `session_id`/`tool_call_id` values containing `..`, `../..`, and path separators (`/`, `\`) into the ctx before NewResult spills a >cap output; assert NewResult returns an error (the id is rejected before filepath.Join) and NO file is written outside `<run_dir>/conversations/` — i.e. no `..` traversal escape, no separator-injected absolute path (the run_dir + `conversations/` prefix is fixed and not model-controlled)
  </behavior>
  <action>
    In ONE coupled commit (SPEC Constraint — D-01 commit 4):
    - `spec.go`: add `type ToolResult struct{Preview string; FullPath string; Bytes int; Truncated bool}` and migrate the `Tool` interface to `Execute(ctx context.Context, args json.RawMessage) (ToolResult, error)`.
    - `result.go` (NEW): `tools.NewResult(ctx context.Context, content string) (ToolResult, error)` (D-25). Read the cap, run_dir, session_id, tool_call_id from the ctx (define small unexported ctx-key types + exported setter helpers e.g. `WithToolCallContext(ctx, sessionID, toolCallID, runDir, cap)` the agent calls before Execute). VALIDATE session_id + tool_call_id BEFORE filepath.Join (T-03-07): reject any id containing `..` or a path separator (`/`, `\`, or `os.IsPathSeparator`) with a clear error — the sidecar path is `<run_dir>/conversations/<session_id>/<tool_call_id>.result` and the `<run_dir>/conversations/` prefix is fixed, never model-controlled. If `len(content) <= cap` → `ToolResult{Preview: content, Bytes: len(content), Truncated: false}`, no disk write. If `> cap` → `truncatePreview(content, cap)` (UTF-8 rune-boundary backoff per AI-SPEC Pattern 3), build the byte-based footer pointer `\n\n[output truncated: showing bytes 0-<cap> of <total>; read more via read_tool_output(tool_call_id="<id>", offset=<cap>, limit=2048)]`, write the FULL bytes to `filepath.Join(runDir, "conversations", sessionID, toolCallID+".result")` (lazy `os.MkdirAll`), return `ToolResult{Preview: preview+footer, FullPath: path, Bytes: total, Truncated: true}`. On write failure → return `ToolResult{Preview: preview + "\n\n[full output unavailable: <reason>]", Bytes: total, Truncated: true}` with NO error (D-29 — turn continues). The Preview field is what the agent puts in RoleTool.Content.
    - `text_response.go` + `search.go`: adapt `Execute` to return `ToolResult` (wrap their string output via `NewResult` or a direct small `ToolResult{Preview: s, Bytes: len(s)}` for text_response which is terminal and small). Preserve existing arg-parse + error behavior.
    - `main_test.go`: ensure `goleak.VerifyTestMain` if the package gains goroutines (it does not, but keep parity).
    Write the tests per the behavior block (`result_test.go`, plus updated `text_response_test.go`/`search_test.go`). Use `t.TempDir()` for run_dir; reuse `pgregory.net/rapid` for the truncatePreview property. Error-wrap `%w` + lowercase phrase. (Tests are co-located with the production code in this same coupled commit — co-located test discipline, no separate Wave-0 stub task.)
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go vet ./internal/agent/tools/ && go build ./... && go test ./internal/agent/tools/ -run 'TestNewResult|TestSidecarLayout|TestSidecarPathTraversal|TestTruncatePreview|TestTextResponse|TestToolSearch' && go test -race ./internal/agent/tools/</automated>
  </verify>
  <acceptance_criteria>
    - Req#6: `go test ./internal/agent/tools/ -run TestNewResult` — 100KB → history content ≤ ~2KiB (preview+footer) + sidecar file holds 100KB; ≤cap → no file + history == raw preview; the property test proves valid-UTF-8 ≤cap prefix
    - Req#8: `go test ./internal/agent/tools/ -run TestSidecarLayout` asserts path `conversations/<session_id>/<tool_call_id>.result` and lazy dir creation
    - T-03-07: `go test ./internal/agent/tools/ -run TestSidecarPathTraversal` — ids containing `..`/`../..`/path-separators return an error and write NO file outside `<run_dir>/conversations/` (no traversal escape, no separator-injected absolute path)
    - D-29: a write-failure test asserts Preview carries `full output unavailable` and NO error returned
    - Migration: `Tool.Execute` signature is `(ToolResult, error)` in spec.go (grep confirms); `go build ./...` compiles (proves text_response + search adapted in the same commit); no third tool left on the old signature
    - All three migrated files + 2 new tool files ≤600 LOC; CGO-free
  </acceptance_criteria>
  <done>Tool.Execute migrated to (ToolResult, error) coupled with text_response + search; tools.NewResult does cap→preview→sidecar with UTF-8-safe truncation, path-traversal-rejecting id validation, and degrade-clean write-failure; Req#6/#8 + T-03-07 green.</done>
</task>

<task type="auto">
  <name>Task 2: read_tool_output (byte ranges, unknown-id hard-fail) + current_time (UTC + IANA tz) builtins</name>
  <read_first>
    - internal/agent/tools/search.go (full — `ToolSearch` struct + `Spec()` + `Execute` non-deferred-tool shape, the exact template for both new tools)
    - internal/agent/tools/text_response.go (full — arg-parse + `Spec()` returning `{Name, Summary, Description, Parameters, Deferred:false}`)
    - internal/agent/tools/result.go (from Task 1 — sidecar path layout + the id-validation helper read_tool_output must reuse)
    - .planning/phases/03-llm-client-toolresult/03-CONTEXT.md (D-27 read_tool_output offset/limit in BYTES default ~2048, footer `showing bytes X-Y of Z, next offset Y`; D-08 current_time RFC-3339 UTC default + optional IANA tz, NEVER in messages[0]; D-15 unknown id → error tool-result the model sees, not a panic)
    - .planning/phases/03-llm-client-toolresult/03-SPEC.md Req#7 (unknown tool_call_id hard-fails; acceptance offset=50000 limit=100 on the 100KB fixture) + Req#14 (current_time RFC-3339 UTC + IANA offset)
    - .planning/phases/03-llm-client-toolresult/03-PATTERNS.md `read_tool_output.go + current_time.go` section lines 200-219 (both Deferred:false; current_time via time.LoadLocation)
  </read_first>
  <behavior>
    - Test 1 (Req#7): `read_tool_output{tool_call_id, offset=50000, limit=100}` against the 100KB sidecar fixture returns the correct 100-byte slice starting at byte 50000, with a footer `showing bytes 50000-50100 of 100000, next offset 50100`
    - Test 2 (Req#7): an unknown tool_call_id → Execute returns an error (becomes a RoleTool error the model sees — D-15), NOT empty content, NOT a panic
    - Test 3 (Req#7 defaults): offset omitted → 0; limit omitted → ~2048 bytes
    - Test 4 (Req#14): `current_time{}` returns a string parseable by `time.Parse(time.RFC3339, ...)` in UTC; `current_time{timezone:"Europe/Rome"}` returns the correct offset (assert the zone offset matches `time.LoadLocation("Europe/Rome")` for the same instant)
    - Test 5 (Req#14): an invalid IANA tz → error tool-result (not panic)
  </behavior>
  <action>
    Create `read_tool_output.go`: struct + `Spec()` (`Name: "read_tool_output"`, `Deferred: false`, Parameters JSON-schema `{tool_call_id: string, offset?: int (bytes, default 0), limit?: int (bytes, default 2048)}` — BYTES per A4/D-27, the schema text MUST say bytes not lines). `Execute(ctx, raw)`: parse args; resolve the sidecar path from the ctx-injected session_id + run_dir (reuse the result.go ctx accessors AND the result.go id-validation helper — reject `..`/path-separator ids, T-03-07) as `conversations/<session_id>/<tool_call_id>.result`; if the file does not exist (unknown/never-spilled id) → return an error (D-15 hard-fail, Req#7); else read the byte range `[offset, offset+limit)` clamped to file size, return a ToolResult whose Preview is the slice + the footer `showing bytes <offset>-<end> of <total>, next offset <end>`. read_tool_output's own output is small (≤ its limit) so it never re-spills.
    Create `current_time.go`: struct + `Spec()` (`Name: "current_time"`, `Deferred: false`, Parameters `{timezone?: string (IANA, default "UTC")}`). `Execute(ctx, raw)`: parse the optional timezone; `time.Now().UTC()` formatted RFC-3339 when default; for a non-empty tz `time.LoadLocation(tz)` then format in that zone (still an RFC-3339 string carrying the offset). Invalid tz → error (not panic). The clock is read ONLY here (tool path) — never written into a system prompt / messages[0] (KV discipline, D-08). Return via a small `ToolResult{Preview: s, Bytes: len(s)}` (the timestamp is small, no spill).
    Write `read_tool_output_test.go` + `current_time_test.go` per the behavior block. For read_tool_output, build a 100KB sidecar file in `t.TempDir()` via the result.go helper or directly, inject the ctx ids, assert the byte slice + unknown-id error. For current_time, assert RFC-3339 parseability + the Europe/Rome offset equals what LoadLocation yields for the same instant. Both tools `Deferred: false`.
  </action>
  <verify>
    <automated>cd /mnt/d/Aura && go vet ./internal/agent/tools/ && go build ./... && go test ./internal/agent/tools/ -run 'TestReadToolOutput|TestCurrentTime' && go test -race ./internal/agent/tools/</automated>
  </verify>
  <acceptance_criteria>
    - Req#7: `go test ./internal/agent/tools/ -run TestReadToolOutput` — offset=50000 limit=100 returns the correct slice of the 100KB fixture with the byte-based `next offset` footer; an unknown id returns an error (asserted non-nil, non-panic); defaults offset=0 limit~2048
    - Req#14: `go test ./internal/agent/tools/ -run TestCurrentTime` — `current_time{}` parses as RFC-3339 UTC; `{timezone:"Europe/Rome"}` offset matches `time.LoadLocation("Europe/Rome")`; invalid tz → error
    - Both tools' `Spec().Deferred == false`; read_tool_output schema text says BYTES (grep confirms "bytes", no "lines"/"righe")
    - `go test -race ./internal/agent/tools/` green; both files ≤600 LOC
  </acceptance_criteria>
  <done>read_tool_output pages byte ranges from the sidecar with an unknown-id hard-fail; current_time returns RFC-3339 UTC + IANA offsets without ever touching the prompt prefix; Req#7/#14 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool output (untrusted, possibly from a model/sandbox) → disk sidecar | Large/arbitrary bytes written to $AURA_RUN_DIR; the tool_call_id/session_id form the path |
| model-supplied read_tool_output args → filesystem read | offset/limit/tool_call_id come from the model and select a file + byte range |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering / Information Disclosure | path traversal / id injection in `conversations/<session_id>/<tool_call_id>.result` | mitigate | Validate session_id + tool_call_id are opaque-id shaped (no `/`, no `\`, no `..`, no path separators) before filepath.Join; reject ids containing `..` or path separators with a clear error; the run_dir + conversations/ prefix is fixed and not model-controlled. `TestSidecarPathTraversal` (Task 1) feeds `..`/`../..`/separator-bearing ids and asserts an error + no file written outside the prefix, not an escape; read_tool_output reuses the same validation |
| T-03-08 | Denial of Service | untrusted tool output written to disk (disk fill) | accept | Single-operator local REPL this phase; the budget/step cap bounds the number of tool calls; sidecar size is bounded by the tool's own output. Document; full quota enforcement deferred (no quota subsystem in scope) |
| T-03-09 | Denial of Service | read_tool_output huge limit / offset overflow | mitigate | Clamp offset/limit to file size; a negative or overflowing offset/limit → error, not a panic or unbounded read |
</threat_model>

<verification>
- `go test -race ./internal/agent/tools/` green with goleak parity
- `go vet ./internal/agent/tools/` clean; `go build ./...` CGO-free
- `golangci-lint run ./internal/agent/tools/...` 0 issues
- Path-traversal test (`..`, `../..`, separators in id) asserts an error not an escape (`TestSidecarPathTraversal`, T-03-07)
- The coupled migration commit changes spec.go + text_response.go + search.go together (one commit, SPEC Constraint)
</verification>

<success_criteria>
- SPEC Req#6, #7, #8, #14 fully satisfied with automated tests
- Tool.Execute migrated coupled (one commit); spillover DRY via tools.NewResult
- Property test for UTF-8 truncation passes (rapid)
- Path-traversal mitigation tested (T-03-07); all files ≤600 LOC
</success_criteria>

<output>
Create `.planning/phases/03-llm-client-toolresult/03-03-SUMMARY.md` when done
</output>
