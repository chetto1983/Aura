---
phase: 07-web-tools
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - internal/agent/tools/web_search.go
  - internal/agent/tools/web_fetch.go
  - internal/agent/tools/web_fetch_test.go
  - internal/agent/tools/web_search_test.go
  - cmd/aura/web.go
  - cmd/aura/main.go
  - internal/web/searxng_integration_test.go
  - internal/web/dnspin_integration_test.go
  - scripts/ssrf_smoke.sh
  - scripts/web_search_smoke.sh
  - .planning/phases/07-web-tools/07-VALIDATION.md
  - docs/aura-quality-snapshot.md
autonomous: false
requirements: [CAP-05]
user_setup: []

must_haves:
  truths:
    - "web_search and web_fetch are registered Deferred:true tools that appear in the alphabetically-sorted manifest by name only until tool_search loads their full spec"
    - "web_fetch markdown exceeding AURA_WEB_RESPONSE_CAP_BYTES returns a preview plus tool_result_id and the full body is readable via read_tool_output"
    - "SSRF blocks, unsupported schemes, and unavailable backends return inline sanitized structured objects via the tool result channel; only successful large content spills to ToolResult"
    - "aura tool web_search/web_fetch and aura web doctor run as hand-rolled CLI subcommands with human output and distinct exit codes, never using a public SearXNG fallback"
    - "The web_integration tier actually runs live against the SearXNG container in CI (t.Fatal under $CI when env unset, never skip-as-green)"
  artifacts:
    - path: "internal/agent/tools/web_search.go"
      provides: "Deferred:true adapter → web.Search → sanitized result / NewResult (D-01/10/41)"
      contains: "Deferred"
    - path: "internal/agent/tools/web_fetch.go"
      provides: "Deferred:true adapter → web.Fetch → NewResult spillover (D-21)"
      contains: "NewResult"
    - path: "cmd/aura/web.go"
      provides: "aura web doctor + aura tool web_search/web_fetch hand-rolled (D-43/D-44/D-45)"
      contains: "doctor"
    - path: "scripts/ssrf_smoke.sh"
      provides: "SC#3 SSRF block smoke (metadata, IPv4-mapped IPv6, IPv6 link-local, metadata hostname)"
      contains: "169.254.169.254"
    - path: "internal/web/dnspin_integration_test.go"
      provides: "SC#4 DNS-rebinding belt-and-suspenders (web_integration tier)"
      contains: "web_integration"
  key_links:
    - from: "cmd/aura/main.go buildRegistry"
      to: "internal/agent/tools/web_search.go + web_fetch.go"
      via: "reg.Register with a shared web.NewClient engine"
      pattern: "reg.Register\\(&tools.Web"
    - from: "internal/agent/tools/web_fetch.go"
      to: "internal/agent/tools/result.go NewResult"
      via: "large markdown spillover to sidecar"
      pattern: "NewResult\\(ctx"
    - from: "cmd/aura/main.go switch"
      to: "cmd/aura/web.go runWeb"
      via: "new web case"
      pattern: "case \"web\""
---

<objective>
Land the thin LLM-facing layer and the operator/CI surface: the two `Deferred:true` tool adapters (`web_search.go`, `web_fetch.go`) that marshal args, call `web.Client`, map sanitized errors inline, and route large markdown through `tools.NewResult`; the `aura web` CLI (doctor + smoke verbs); the registry wiring; and the live `web_integration` test tier plus the SSRF/search smoke scripts.

Purpose: Decisions D-01/D-10/D-21/D-38..D-45 plus the full ROADMAP smoke surface (SC#1 live, SC#2 live, SC#3 smoke, SC#4 integration belt-and-suspenders). This wave is non-autonomous because the live SC#1/SC#2 acceptance against a running SearXNG container is a human-verify checkpoint (needs the Compose stack + public internet).
Output: Registered web tools the agent loop can call, `aura web doctor` + `aura tool web_search/web_fetch`, the `web_integration` tier that runs live in CI, the SSRF smoke script, and the live acceptance sign-off.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-web-tools/07-CONTEXT.md
@.planning/phases/07-web-tools/07-RESEARCH.md
@.planning/phases/07-web-tools/07-PATTERNS.md
@CLAUDE.md
@internal/agent/tools/execute.go
@cmd/aura/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: web_search.go + web_fetch.go Deferred adapters + registry wiring</name>
  <read_first>
    - internal/agent/tools/execute.go (the full reference Deferred adapter: struct with injected engine, Spec() with Deferred:true + long description + JSON schema, Execute() unmarshal→validate-as-ToolResult→delegate→NewResult; enum/validation failures return NewResult(ctx,"error:...") so the model self-corrects, real infra faults return a Go error)
    - .planning/phases/07-web-tools/07-PATTERNS.md (§web_search.go+web_fetch.go — WebSearch{Engine *web.Client}/webSearchArgs{...D-09}, WebFetch{Engine *web.Client}/webFetchArgs{URL}; the adapter rule D-26/27/28/41: SSRF block/unsupported/unavailable is a model-visible sanitized JSON via NewResult, NOT a Go error; spillover is FREE via NewResult; §main.go MODIFY buildRegistry + switch)
    - cmd/aura/main.go (buildRegistry lines 72-81 — reg.Register after the Execute line; the switch lines 35-65; usage() line 68-69)
    - internal/web/errors.go + client.go (the WebError mapper + web.NewClient, from 07-02/07-03)
    - internal/agent/tools/main_test.go (goleak TestMain for the tools package)
  </read_first>
  <behavior>
    - `web_fetch` of a page whose `content_md` exceeds `AURA_WEB_RESPONSE_CAP_BYTES` (>24000B) returns a `ToolResult` with `Truncated==true` plus a `tool_result_id`, and `read_tool_output(tool_call_id)` returns the tail bytes (D-21 spillover via NewResult — TestWebFetch_Spillover).
    - `web_search.Spec().Deferred == true` AND `web_fetch.Spec().Deferred == true`; both appear in the manifest by Name + Summary only until tool_search loads the full spec (TestWebSearch_DeferredSpec).
    - An SSRF block / unavailable error returned by either tool is an inline `NewResult` whose JSON has ONLY `{error,reason,message}` keys and contains NO resolved IP / internal host / response header / body substring (D-26/27/28/41 — TestWeb_SanitizedInlineError leak-scan).
  </behavior>
  <action>
    Create `web_search.go`: `type WebSearch struct { Engine *web.Client }`; `Spec()` returns `Name:"web_search"`, `Summary:"Search the public web via the configured SearXNG instance."`, a long Description with example arg lines, a JSON schema exposing exactly the D-09 public controls (`query`,`max_results`,`category` enum general|news, `language`,`time_range`,`domains` []string,`include_metadata` bool) and NOTHING else (no raw engines/safesearch — D-10), `Deferred:true`. `Execute(ctx, raw)` unmarshals into `webSearchArgs`, validates (bad category/domain-with-scheme → `NewResult(ctx, sanitizedErrorJSON)` so the model self-corrects), calls `e.Engine.Search`, maps a `*web.WebError` (unavailable/etc) to the sanitized `{error,reason,message}` JSON via `NewResult` (D-41 inline small), and on success formats the result list and returns `NewResult(ctx, resultsJSON)`. Create `web_fetch.go`: `type WebFetch struct { Engine *web.Client }`, `webFetchArgs{ URL string }`, `Spec()` `Name:"web_fetch"`, `Summary:"Fetch a public web page and return it as readable markdown."`, `Deferred:true`; `Execute` calls `e.Engine.Fetch(ctx, convID, url)` where convID comes from the tool-call context, maps a `*web.WebError` (blocked_url/unsupported_scheme/unsupported_content_type/response_too_large/timeout/http_error/extraction_failed) to sanitized inline JSON via `NewResult`, and on success returns `NewResult(ctx, fetchPackageJSON)` so large `content_md` spills to the sidecar automatically (D-21 — ZERO new spillover code). NEVER put a resolved IP/host/header/redirect-chain into the returned string (D-27). In `cmd/aura/main.go` add `reg.Register(&tools.WebSearch{Engine: webEngine})` and `reg.Register(&tools.WebFetch{Engine: webEngine})` in `buildRegistry` (after Execute), where `webEngine := web.NewClient(config.LoadDB())`; do NOT hand-order (manifest auto-sorts by Name — web_fetch < web_search). Add the `case "web": runWeb(os.Args[2:])` to the switch and update `usage()`.
  </action>
  <verify>
    <automated>go build ./... && go test -race -run "TestWebFetch_Spillover|TestWebSearch_DeferredSpec|TestWeb_SanitizedInlineError" ./internal/agent/tools/ && go run ./cmd/aura tools | grep -E "web_search|web_fetch"</automated>
  </verify>
  <acceptance_criteria>
    - both tools' `Spec().Deferred == true` and appear in `aura tools` output by Name + Summary only
    - `TestWebFetch_Spillover`: a >24000-byte content_md returns a ToolResult with `Truncated==true` + a sidecar file, and `read_tool_output(tool_call_id)` returns the tail bytes
    - `TestWeb_SanitizedInlineError`: an SSRF block / unavailable returns an inline `NewResult` whose JSON has only `{error,reason,message}` keys and greps clean for any IP/host/header substring
    - `buildRegistry` registers both; the manifest stays alphabetically sorted (web_fetch before web_search) with no hand-ordering
    - `go vet ./...` and `go build ./...` pass
  </acceptance_criteria>
  <done>Both web tools are Deferred adapters that delegate to web.Client, map errors to sanitized inline objects, spill large fetch markdown via NewResult, and are registered for the agent loop.</done>
</task>

<task type="auto">
  <name>Task 2: aura web CLI (doctor + tool verbs) + ssrf/search smoke scripts + live web_integration tier</name>
  <read_first>
    - cmd/aura/exec.go (the hand-parsed subcommand: sysexits constants exitUnreachable=70/exitInfra=71/exitUsage=64 ~18-22, parseExecArgs range/switch ~43-79, config.LoadDB()+engine+run+os.Exit ~105-127, reuse the engine's own FormatLean formatter — no drift)
    - .planning/phases/07-web-tools/07-PATTERNS.md (§cmd/aura/web.go — aura web doctor checks SEARXNG_URL→reachable→JSON round-trip→tiny search, human output, NO public fallback; aura tool web_search/web_fetch '<json>' marshal the arg and call web.Client directly)
    - internal/sandbox/docker_test.go (the //go:build sandbox_integration split + envOrSkip that t.Fatal under $CI — copy this for the web_integration tier, no-skip-as-green)
    - .planning/phases/07-web-tools/07-RESEARCH.md (§Validation — SC#4 injectable resolver is the primary unit test in 07-02; the dnslib/live tier here is belt-and-suspenders; §Test Framework web_integration tag; §Common Pitfalls injectable resolver vs real DNS)
    - scripts/ (existing smoke script conventions, e.g. loop_budget_smoke.sh / microcompact_smoke.sh for the grep-contract style)
  </read_first>
  <action>
    Create `cmd/aura/web.go` with `runWeb(args []string)` dispatching `doctor`, `tool web_search '<json>'`, and `tool web_fetch '<json>'` (hand-parsed switch, no cobra — repo convention; confirm `aura tool` singular does not collide with the existing `aura tools` plural). `doctor` (D-44): `config.LoadDB()` (must not require OPENROUTER_API_KEY), check `SEARXNG_URL` set → reachable → JSON round-trip → a tiny search via `web.NewClient`; print human-readable status (D-45 — human CLI only, no JSON operator output) and exit with distinct sysexits codes; NEVER fall back to a public instance (D-05/D-44). `tool web_search`/`web_fetch` marshal the JSON arg and call the same `web.Client`, printing the formatted result. Create `scripts/ssrf_smoke.sh` exercising SC#3 against `aura tool web_fetch` for `http://169.254.169.254/latest/meta-data/`, `http://[::ffff:169.254.169.254]/`, `http://[fe80::1]/`, and `http://metadata.google.internal/`, asserting each returns `blocked_url` with the expected reason and that no internal IP/host leaks into the output (grep-clean). Create `scripts/web_search_smoke.sh` running the D-43 `aura tool web_search '{"query":"site:wikipedia.org SearXNG","max_results":3}'` against the live stack. Create `internal/web/searxng_integration_test.go` and `internal/web/dnspin_integration_test.go` behind `//go:build web_integration` with their own goleak TestMain: the searxng test runs `web.Search` live against the container (asserts ranked results, D-43); the dnspin test is the SC#4 belt-and-suspenders against the running fixture (the primary deterministic SC#4 test is the injectable-resolver unit test from 07-02). Use an `envOrSkip` helper that calls `t.Fatal` when `SEARXNG_URL`/the fixture env is unset AND `$CI` is set, skips locally (no-skip-as-green). Add the `web_integration` tier to CI exporting `SEARXNG_URL` with the SearXNG container up.
  </action>
  <verify>
    <automated>go build ./cmd/aura && go vet -tags web_integration ./internal/web/</automated>
  </verify>
  <acceptance_criteria>
    - `aura web doctor` with `SEARXNG_URL` unset prints a clear "not configured" status and a non-zero exit, with NO public fallback attempt
    - `scripts/ssrf_smoke.sh` asserts all four SC#3 targets return `blocked_url` and greps clean for resolved IPs / internal hostnames in the output — RUN this script AFTER it is written (it is an artifact this task creates, so it is an acceptance step, not the `<verify><automated>` self-verify): `bash scripts/ssrf_smoke.sh 2>&1 | grep -E "blocked_url"` (one matching line per SC#3 `blocked_url` target: `http://169.254.169.254/...`, `http://[::ffff:169.254.169.254]/`, `http://[fe80::1]/`, `http://metadata.google.internal/`)
    - `internal/web/*_integration_test.go` compile under `-tags web_integration` and their skip-helper `t.Fatal`s under `$CI` when env is unset (sub-second runtime is treated as a skip tell — the CI job exports SEARXNG_URL with the container up)
    - `go run ./cmd/aura web doctor` exit codes are the sysexits set (70/71/64), reusing the engine formatter
    - `aura tool` (singular) does not collide with `aura tools` (plural) in the switch
  </acceptance_criteria>
  <done>The operator CLI (doctor + tool verbs), the SSRF + search smoke scripts, and the live web_integration tier all exist; after `scripts/ssrf_smoke.sh` is written, running it returns `blocked_url` for all four SC#3 URLs (grep-clean of any IP/host leak) with no public fallback and no skip-as-green.</done>
</task>

<task type="auto">
  <name>Task 3: Flip 07-VALIDATION.md statuses green + record the quality snapshot once scaffolds pass</name>
  <files>.planning/phases/07-web-tools/07-VALIDATION.md, docs/aura-quality-snapshot.md</files>
  <read_first>
    - .planning/phases/07-web-tools/07-VALIDATION.md (the Per-Task Verification Map + the `wave_0_complete: false` frontmatter + the sign-off `wave_0_complete` checkbox — this task owns flipping them)
    - docs/aura-quality-snapshot.md (the living quality doc; add/refresh the `web` row per the Aura quality-matrix convention)
  </read_first>
  <action>
    After the Wave 0 TDD scaffolds (the `*_test.go` files + `internal/web/main_test.go` goleak harness from Waves 1-3) compile and the unit RED→GREEN tiers pass, update `.planning/phases/07-web-tools/07-VALIDATION.md`: flip the frontmatter `wave_0_complete: false` → `wave_0_complete: true`, set each Per-Task Verification Map row whose command is green from `⬜ pending` to `✅ green` (and its `File Exists` column from `❌ W0` to a checkmark), and tick the final sign-off `wave_0_complete: true` checkbox. Then refresh the `web` row in `docs/aura-quality-snapshot.md` with the unit-tier coverage number for `internal/web` (the live SC#1 latency, the `ssrf.go` mutation %, and the combined cross-tag coverage % are recorded at the Task 4 checkpoint, not here — leave those cells as `pending @ Gate-3` until the human-verify step fills them). This task owns the VALIDATION contract so the `wave_0_complete` flip is not orphaned; it does NOT mark CAP-05 complete (that is the Task 4 checkpoint after the live + Gate-3 numbers meet floor).
  </action>
  <verify>
    <automated>grep -E "wave_0_complete: true" .planning/phases/07-web-tools/07-VALIDATION.md && grep -qE "^\| .*web" docs/aura-quality-snapshot.md && echo "validation flipped + snapshot row present"</automated>
  </verify>
  <acceptance_criteria>
    - `07-VALIDATION.md` frontmatter shows `wave_0_complete: true`
    - every Per-Task Verification Map row with a passing command shows `✅ green` (no stale `⬜ pending` for a tier that is actually green)
    - the final sign-off `wave_0_complete: true` checkbox is ticked
    - `docs/aura-quality-snapshot.md` has a `web` row carrying the unit-tier `internal/web` coverage number (live latency / mutation / combined coverage cells may read `pending @ Gate-3`)
  </acceptance_criteria>
  <done>07-VALIDATION.md statuses are green and `wave_0_complete: true` is set once the scaffolds pass; the quality snapshot carries the web row — the contract is owned by this task, not orphaned.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Live SC#1/SC#2 acceptance against the running SearXNG stack + ssrf_smoke + mutation gate</name>
  <what-built>
    The full Phase 7 web surface is implemented: SearXNG Compose service + JSON settings, the SSRF-hardened engine, both Deferred tools, the CLI, and the live integration tier. This checkpoint verifies the four ROADMAP success criteria end-to-end against a live stack plus the Gate-3 numbers (mutation ≥70% on ssrf.go, coverage ≥85% across the full tag matrix) that cannot be fabricated without the running container.
    Automated before this checkpoint: unit tiers (SC#3 classification, SC#4 pin reuse, SC#1-parse, SC#2 extraction) all green; ssrf_smoke.sh asserts the SC#3 blocks; the web_integration tier compiled.
  </what-built>
  <how-to-verify>
    1. Bring the stack up: `docker compose up -d searxng` (and confirm `docker compose ps` shows it healthy with NO host port).
    2. SC#1: `aura tool web_search '{"query":"Neo4j HNSW vector index","max_results":5}'` returns ranked {title,url,snippet} results within ~2s.
    3. SC#2: `aura tool web_fetch '{"url":"https://en.wikipedia.org/wiki/Knowledge_graph"}'` returns clean markdown (no nav/footer chrome).
    4. SC#3: `bash scripts/ssrf_smoke.sh` — every metadata/IPv4-mapped-IPv6/link-local/metadata-hostname target returns `blocked: cloud metadata`/`blocked_url`.
    5. SC#4: run the DNS-rebinding test (`go test -tags web_integration -run TestDNSRebind ./internal/web/`) — the second fetch to the same host reuses the first pinned IP within 60s.
    6. In WSL: `go-mutesting ./internal/web/ssrf.go` reports ≥70% killed; `make coverage` (or the scoped gate) reports ≥85% on the internal/web owned surface across unit + web_integration tiers; `golangci-lint run ./...` = 0.
    7. `aura web doctor` against the running stack reports SEARXNG_URL reachable + JSON round-trip OK + a tiny search succeeding.
  </how-to-verify>
  <action>
    Operator brings up the SearXNG container and runs the seven verification steps above. Record the live SC#1 latency, the mutation score on ssrf.go, and the combined coverage number into `docs/aura-quality-snapshot.md` (web row). If any SC fails or a number is below floor, file the gap and return to the relevant wave; do NOT mark CAP-05 complete until all four SCs are live-green and the Gate-3 numbers meet floor.
  </action>
  <resume-signal>Type "approved" with the recorded SC#1 latency, ssrf.go mutation %, and combined coverage %, or describe the failing criterion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool args → adapter | the model-supplied search params / fetch URL are untrusted; the adapter validates and delegates to the hardened engine |
| engine error → model channel | sanitized WebError crosses back to the model; must carry no internal topology (D-27) |
| operator CLI → web engine | doctor/tool verbs run with operator trust but must not enable any public-instance fallback (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-30 | Information Disclosure | web_search/web_fetch adapter error mapping | mitigate | adapter maps the rich internal error → sanitized inline `{error,reason,message}` via NewResult; never the resolved IP/host/header/redirect-chain (D-26/27/28/41) — TestWeb_SanitizedInlineError leak-scan |
| T-07-31 | Tampering | web_search Spec schema | mitigate | JSON schema exposes ONLY the D-09 public controls + category enum general|news; no raw engines/safesearch/pass-through (D-10) — TestWebSearch_DeferredSpec |
| T-07-32 | Information Disclosure | KV cache stability | mitigate | both tools register Deferred:true and the manifest auto-sorts by Name; web tools return via ToolResult only, never mutate messages[0] (Phase 6 invariant) |
| T-07-33 | Spoofing | aura web doctor | mitigate | no public/random SearXNG fallback; SEARXNG_URL is the only backend (D-05/D-44) — doctor "not configured" acceptance |
| T-07-34 | Tampering | web_integration CI tier | mitigate | no-skip-as-green: envOrSkip t.Fatal under $CI; the CI job exports SEARXNG_URL with the container up so SC#1/SC#4 actually execute |
| T-07-SC | Tampering | ssrf.go regression | mitigate | go-mutesting ≥70% killed gate on ssrf.go enforced at the Gate-3 checkpoint before CAP-05 closes |
</threat_model>

<verification>
- `go build ./...` + `go vet ./...` (incl. `-tags web_integration`) green.
- `go test -race ./internal/agent/tools/...` green (spillover + sanitized-error + deferred-spec).
- `scripts/ssrf_smoke.sh` asserts SC#3 blocks; greps clean for leaks.
- Live SC#1/SC#2/SC#4 + mutation ≥70% on ssrf.go + coverage ≥85% + golangci-lint 0 at the Gate-3 checkpoint.
</verification>

<success_criteria>
All four ROADMAP success criteria are live-green; both Deferred tools are registered and behave (spillover + sanitized inline errors); the CLI + smoke + integration tier exist with no public fallback and no skip-as-green; CAP-05 closes only after the Gate-3 numbers meet floor.
</success_criteria>

<output>
Create `.planning/phases/07-web-tools/07-04-SUMMARY.md` when done.
</output>
