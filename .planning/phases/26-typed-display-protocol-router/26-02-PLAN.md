---
phase: 26-typed-display-protocol-router
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/chat/sseAdapter.ts
  - web/src/chat/ExternalStoreChat.tsx
  - web/src/chat/displays/types.ts
  - web/src/chat/displays/snapshotToMessages.ts
  - web/src/chat/displays/DisplayRouter.tsx
  - web/src/chat/displays/DisplayPagination.tsx
  - web/src/chat/__tests__/sseAdapter.test.ts
  - web/src/chat/displays/__tests__/snapshotToMessages.test.ts
  - web/src/chat/displays/__tests__/DisplayRouter.test.tsx
  - web/src/chat/displays/__tests__/DisplayPagination.test.tsx
  - web/src/i18n/resources.ts
autonomous: true
requirements: [DISP-01, DISP-02]
must_haves:
  truths:
    - "Reopening a thread fetches GET /threads/{id}/messages and rehydrates the chat lane (text + tool turns) — a reopened thread is no longer blank (D-06 prerequisite)"
    - "The sseAdapter reducer recognizes a CUSTOM/aura.display frame and attaches the typed DisplayPayload to the matching tool part by toolCallId"
    - "DisplayRouter switches on payload.type and its default case returns the raw ToolActivityCard (never null) — output is never lost (D-FALLBACK / HARDEN-08)"
    - "DisplayPagination renders 'X–Y of N' + prev/next + per-page select, default 3 items/page, all client-side"
  artifacts:
    - path: "web/src/chat/displays/types.ts"
      provides: "DisplayPayload TS wire interface mirroring the Go union (26-01)"
      contains: "interface DisplayPayload"
    - path: "web/src/chat/displays/snapshotToMessages.ts"
      provides: "MESSAGES_SNAPSHOT -> ThreadMessageLike[] mapper for replay"
    - path: "web/src/chat/displays/DisplayRouter.tsx"
      provides: "switch(payload.type) router shell; default -> ToolActivityCard"
      contains: "switch"
    - path: "web/src/chat/displays/DisplayPagination.tsx"
      provides: "in-card pagination (ported elysia logic, rewritten chrome)"
  key_links:
    - from: "web/src/chat/ExternalStoreChat.tsx"
      to: "/threads/{id}/messages"
      via: "history-rehydration fetch on mount/threadId change"
      pattern: "threads/.*messages"
    - from: "web/src/chat/sseAdapter.ts"
      to: "tool part .display"
      via: "case 'CUSTOM' aura.display attach by toolCallId"
      pattern: "aura\\.display"
---

<objective>
Wire the D-06 history-rehydration PREREQUISITE (the single highest-value finding: the cockpit chat lane does NOT rehydrate history on reopen today — `ExternalStoreChat.tsx:79` mounts `messages:[]` with no fetch), extend the kept-verbatim `sseAdapter.ts` reducer additively with a CUSTOM/`aura.display` frame that attaches the typed payload to the tool part by `toolCallId`, and stand up the `DisplayRouter` shell (default → raw card) + `DisplayPagination` so the Wave-2/3 per-type displays slot in.

Purpose: This is the FIRST wave and the foundation for display replay AND general text/tool replay. The backend `GET /threads/{id}/messages` endpoint already exists and works (`server.go:122`); only the frontend fetch + a snapshot→`ThreadMessageLike` mapper are missing. Without this, reopened threads render blank and displays can never replay (D-06).

Output: A reopened thread rehydrates; the reducer recognizes `aura.display`; the router shell + pagination exist; the `DisplayPayload` TS wire type mirrors the Go union from 26-01.
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-typed-display-protocol-router/26-CONTEXT.md
@.planning/phases/26-typed-display-protocol-router/26-RESEARCH.md
@.planning/phases/26-typed-display-protocol-router/26-UI-SPEC.md
@.claude/skills/streaming/SKILL.md
@.claude/skills/runtime/SKILL.md
@.claude/skills/vercel-react-best-practices/SKILL.md
@.claude/skills/accessibility/SKILL.md
</context>

<artifacts_produced>
NEW symbols this plan creates (excluded from downstream drift verification):
- `web/src/chat/displays/types.ts`: `DisplayPayload` interface + per-type interfaces + `DisplayKind` union (TS mirror of the Go wire shape from 26-01-SUMMARY).
- `web/src/chat/displays/snapshotToMessages.ts`: `snapshotToMessages(snapshot) : ThreadMessageLike[]` mapper.
- `web/src/chat/displays/DisplayRouter.tsx`: `DisplayRouter` component (switch shell, default → ToolActivityCard).
- `web/src/chat/displays/DisplayPagination.tsx`: `DisplayPagination` component.
- `web/src/chat/sseAdapter.ts`: `CustomFrame` added to the `AguiFrame` union + a `MESSAGES_SNAPSHOT`-handling path; `ToolPart.display?` field.
- `web/src/chat/ExternalStoreChat.tsx`: the rehydration `useEffect`/query.
- New i18n keys under `display.*` / `display.pagination.*` (en + it).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: History-rehydration fetch + snapshotToMessages mapper (D-06 prerequisite)</name>
  <files>web/src/chat/displays/snapshotToMessages.ts, web/src/chat/ExternalStoreChat.tsx, web/src/chat/displays/__tests__/snapshotToMessages.test.ts</files>
  <read_first>
    - web/src/chat/ExternalStoreChat.tsx (`messages` mount :79 `useState([])`; the `useQueryClient` :78; the existing query-key pattern `CONVERSATION_KEY` :83-89 — model the fetch on React Query consistent with `useConversations`)
    - web/src/chat/sseAdapter.ts (the `ThreadMessageLike` shape the reducer emits; `ChatPart`/`ToolPart` :177-194 — the mapper must produce the SAME part shapes)
    - internal/agui/server.go (`handleMessages` :261-288, route `GET /threads/{id}/messages` :122, `projectMessages`/`projectToolCalls` :455-494 — the MESSAGES_SNAPSHOT wire shape: `Content`=ResultPreview for tool turns + `ToolCalls`)
    - .planning/phases/26-typed-display-protocol-router/26-RESEARCH.md §"Resolved R9" (the fetch + mapper design) + §"Pitfall 1" (assuming rehydration exists) + §"Code anchor verification" (sseAdapter.ts:269-279 is TOOL_CALL_RESULT, NOT snapshot-aware)
    - .claude/skills/runtime/SKILL.md (ExternalStore runtime + setMessages)
  </read_first>
  <behavior>
    - Test: `snapshotToMessages(snapshot)` maps a user turn → a user `ThreadMessageLike`, an assistant text turn → an assistant message with a text part, and a tool turn (`Content`=ResultPreview + `ToolCalls`) → a tool-call part keyed by toolCallId, in arrival order.
    - Test: a snapshot tool turn carrying a re-derived `display` payload (added by 26-03) maps to a tool part with `.display` set (forward-compatible; tolerated when absent).
    - Behavior: on mount and on `threadId` change (when `threadId.length>0`), `ExternalStoreChat` fetches `GET /threads/{id}/messages`, maps via `snapshotToMessages`, and `setMessages(...)`; an empty thread id mounts `[]`.
  </behavior>
  <action>
    Create `snapshotToMessages.ts`: a pure `snapshotToMessages(messages: events.Message[]) : ThreadMessageLike[]` mapper that mirrors the reducer's part shapes (text part for prose, tool-call part keyed by `toolCallId` carrying `argsText`+`result`=Content, optional `.display`). In `ExternalStoreChat.tsx`, add a React Query (`useQuery`, key `[CONVERSATION_HISTORY_KEY, threadId]`, enabled when `threadId.length>0`) that GETs `/threads/{id}/messages`, and a `useEffect` that calls `setMessages(snapshotToMessages(data))` on success — replacing the blank `messages:[]` start. Keep the live SSE folding untouched (rehydration sets the baseline; live frames append). Reuse the existing fetch helper/auth (RequireAuth cookie inherited). Add the test.
  </action>
  <verify>
    <automated>cd web && npm run test -- snapshotToMessages && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n 'threads/.*messages' web/src/chat/ExternalStoreChat.tsx` returns the rehydration fetch.
    - SOURCE: `ExternalStoreChat.tsx` no longer relies on a permanently-empty `messages` — the rehydration effect sets it.
    - BEHAVIOR: `cd web && npm run test -- snapshotToMessages` passes (VALIDATION.md D-06 mapper row).
    - CLI: `cd web && npm run typecheck` clean.
  </acceptance_criteria>
  <done>Reopening a thread fetches the snapshot and rehydrates text + tool turns; the mapper produces reducer-identical part shapes and tolerates an optional `.display`; types pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DisplayPayload wire types + sseAdapter CUSTOM/aura.display frame</name>
  <files>web/src/chat/displays/types.ts, web/src/chat/sseAdapter.ts, web/src/chat/__tests__/sseAdapter.test.ts</files>
  <read_first>
    - web/src/chat/sseAdapter.ts (`AguiFrame` union :104-118 — CUSTOM is absent; add a `CustomFrame`; `reduceFrame` :245-311; `TOOL_CALL_RESULT` case :269-279; `ToolPart` interface :177-183 — add `display?`; the doc comment :98-103 that lists CUSTOM/aura.artifact as "not modelled")
    - .planning/phases/26-typed-display-protocol-router/26-01-SUMMARY.md (the authoritative Go `DisplayPayload` JSON wire shape to mirror exactly)
    - .planning/phases/26-typed-display-protocol-router/26-RESEARCH.md §"Code Examples" (the reducer CUSTOM frame snippet attaching by tool_call_id) + §"Reducer CUSTOM frame"
    - .claude/skills/streaming/SKILL.md (SSE frame folding)
  </read_first>
  <behavior>
    - Test (DISP-02): a `CUSTOM` frame with `name:'aura.display'` and `value.tool_call_id` attaches the payload to the existing tool part by `toolCallId` (`tools.get(id).display === payload`).
    - Test: a `CUSTOM` frame whose `tool_call_id` has no prior tool part creates/ensures the part (tolerant, like TOOL_CALL_RESULT) and attaches the display.
    - Test: a `CUSTOM` frame with an unrecognized name is a no-op (no corruption) — additive safety.
    - Test: `DisplayPayload` TS type is structurally assignable to a fixture mirroring the 26-01 Go JSON (compile-time + a runtime shape assertion).
  </behavior>
  <action>
    Create `types.ts`: the `DisplayPayload` interface + `DisplayKind` union + per-type interfaces, field-for-field mirroring the Go wire shape recorded in 26-01-SUMMARY (snake_case JSON keys: `type`, `tool_call_id`, `web_results`, `document`, `code`, `table`, `chart`, `system`, `swarm`, `sources`, etc.). In `sseAdapter.ts`: add `CustomFrame { type:'CUSTOM'; name:string; value:unknown }` to the `AguiFrame` union; add `display?: DisplayPayload` to `ToolPart`; add a `case 'CUSTOM':` to `reduceFrame` that, when `frame.name==='aura.display'` and `value.tool_call_id` is a string, `ensureTool(...)` and attaches `display: value as DisplayPayload`, else returns state unchanged. Do NOT rewrite the reducer (kept-verbatim Phase-25). Update the doc comment to reflect CUSTOM is now modelled. Extend `sseAdapter.test.ts`.
  </action>
  <verify>
    <automated>cd web && npm run test -- sseAdapter && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "aura.display" web/src/chat/sseAdapter.ts` returns the CUSTOM-frame attach branch.
    - SOURCE: `grep -n "interface DisplayPayload" web/src/chat/displays/types.ts` returns the wire type.
    - BEHAVIOR: `cd web && npm run test -- sseAdapter` passes the attach-by-toolCallId + unknown-name no-op cases (VALIDATION.md DISP-02 reducer row).
    - CLI: `cd web && npm run typecheck` clean (TS wire type matches the Go shape).
  </acceptance_criteria>
  <done>The reducer recognizes CUSTOM/aura.display and attaches the typed payload to the tool part by toolCallId; the TS wire type mirrors the Go union; unknown CUSTOM names are no-ops; tests + typecheck pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DisplayRouter shell (default → raw card) + DisplayPagination + Fallback branch wiring</name>
  <files>web/src/chat/displays/DisplayRouter.tsx, web/src/chat/displays/DisplayPagination.tsx, web/src/chat/ExternalStoreChat.tsx, web/src/chat/displays/__tests__/DisplayRouter.test.tsx, web/src/chat/displays/__tests__/DisplayPagination.test.tsx, web/src/i18n/resources.ts</files>
  <read_first>
    - web/src/chat/ExternalStoreChat.tsx (`tools.Fallback` :426-435 — branch `part.display ? <DisplayRouter/> : <ToolActivityCard/>`; the existing ToolActivityCard wiring is the default the router returns)
    - web/src/chat/ToolActivityCard.tsx (the raw card the router default returns — status dots :15-31, settle-collapse :85-95)
    - D:/tmp/elysia-frontend/...RenderDisplay.tsx (the `switch(payload.type)` shape :51; NOTE its `default: return null` at :125 — Aura OVERRIDES to the raw card, never null)
    - D:/tmp/elysia-frontend/...DisplayPagination.tsx (the pagination LOGIC :24-68 — `React.Children.toArray` + slice + page state; rewrite the chrome with native `<button>`/`<select>` + committed tokens, NO framer-motion/react-icons/shadcn Select)
    - .planning/phases/26-typed-display-protocol-router/26-UI-SPEC.md §"Component Inventory" (DisplayRouter, DisplayPagination rows) + §"Copywriting Contract" (pagination keys) + §"Spacing/Typography/Color" (the locked tokens; 44px touch targets; accent only on active page)
    - .planning/phases/26-typed-display-protocol-router/26-CONTEXT.md D-FALLBACK, D-PAGINATION
  </read_first>
  <behavior>
    - Test (DISP-02): `DisplayRouter` renders the raw `ToolActivityCard` for an unknown `payload.type` (default case, NEVER null).
    - Test (DISP-02/HARDEN-08): a malformed/unknown-type payload renders escaped (via the raw card), never markdown/HTML.
    - Test (DISP-03): `DisplayPagination` with 7 children + default 3/page shows "1–3 of 7", next advances to "4–6 of 7", per-page select changes the window; prev/next have aria-labels and 44px targets.
    - Behavior: `ExternalStoreChat` `tools.Fallback` branches `part.display ? <DisplayRouter payload={part.display}/> : <ToolActivityCard .../>`.
  </behavior>
  <action>
    Create `DisplayRouter.tsx`: a single `switch(payload.type)` with NO known-type cases yet (those land in 26-04/26-05) and a `default:` that returns the existing `<ToolActivityCard/>` (D-FALLBACK, never null — explicitly do NOT copy elysia's `return null`). Create `DisplayPagination.tsx`: port the elysia logic (children-to-array + slice + page/per-page state, default 3/page), rewrite the chrome with native `<button>`/`<select>` styled on the committed tokens, "X–Y of N" micro-label, prev/next/per-page with `aria-label`s + `min-h-11 min-w-11`, accent only on the active page number (UI-SPEC Color rule). In `ExternalStoreChat.tsx`, change the `tools.Fallback` to the `part.display ? DisplayRouter : ToolActivityCard` branch. Add `display.pagination.*` i18n keys (en + it). Tests for both.
  </action>
  <verify>
    <automated>cd web && npm run test -- DisplayRouter && npm run test -- DisplayPagination && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "ToolActivityCard" web/src/chat/displays/DisplayRouter.tsx` shows the default-case raw card; no `return null` in the default.
    - BEHAVIOR: `cd web && npm run test -- DisplayRouter` passes the default→raw-card + escaped-unknown cases (VALIDATION.md DISP-02 + HARDEN-08 rows).
    - BEHAVIOR: `cd web && npm run test -- DisplayPagination` passes the "X–Y of N" + prev/next + per-page assertions (VALIDATION.md DISP-03 pagination row).
    - SOURCE: `grep -n "part.display" web/src/chat/ExternalStoreChat.tsx` returns the Fallback branch.
    - CLI: `cd web && npm run lint` clean (jsx-a11y: aria-labels on icon-only controls).
  </acceptance_criteria>
  <done>The router shell defaults to the raw card (never null); pagination renders the locked chrome with a11y + 44px targets; the Fallback branch routes display payloads; tests + lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend snapshot/SSE → reducer | Untrusted payload enters the client reducer; only a trusted-normalizer `display` payload is attached, everything else folds as before |
| display payload → router | The router decides rich vs escaped render; the default must stay escaped (HARDEN-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering/Elevation | `DisplayRouter` default | mitigate | `default:` returns the escaped raw `ToolActivityCard`, never null/markdown; a Vitest XSS-shaped assertion covers unknown/malformed payloads (HARDEN-08) |
| T-26-06 | Tampering | `sseAdapter` CUSTOM frame | mitigate | An unrecognized CUSTOM name is a no-op (returns state unchanged); only `aura.display` with a string `tool_call_id` attaches — no reducer corruption |
| T-26-07 | Information Disclosure | rehydration fetch | mitigate | The `/threads/{id}/messages` GET inherits the Phase-24 `RequireAuth` whole-origin gate (no new unauthenticated route) |
| T-26-SC | Tampering | npm installs | mitigate | NO new dependency in this plan (router shell + pagination use native elements + committed tokens); the net-new deps (radix-hover-card, shiki) land in 26-05 behind the Phase-23 supply-chain gate |
</threat_model>

<verification>
- `cd web && npm run typecheck && npm run lint` clean.
- `cd web && npm run test -- snapshotToMessages sseAdapter DisplayRouter DisplayPagination` green.
- A reopened thread (manual or e2e in 26-06) rehydrates text + tool turns (no blank viewport).
- i18n keys present in both en + it; `web/dist` rebuild deferred to the final frontend plan.
</verification>

<success_criteria>
- Reopening a thread rehydrates history (D-06 prerequisite wired).
- The reducer attaches `aura.display` payloads by toolCallId; unknown CUSTOM names are no-ops.
- The DisplayPayload TS wire type mirrors the Go union.
- The router shell defaults to the raw card (never null); pagination renders the locked chrome.
- Targeted Vitest + typecheck + lint pass.
</success_criteria>

<output>
Create `.planning/phases/26-typed-display-protocol-router/26-02-SUMMARY.md` when done. Record the `DisplayRouter` extension point (how Wave-2/3 add `case` branches) and the `DisplayPayload` TS type location for downstream plans.
</output>
