---
phase: 26-typed-display-protocol-router
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - internal/agent/prompt/builder.go
  - internal/agent/prompt/builder_test.go
  - internal/agent/prompt/budget_sources_test.go
  - internal/agent/llm_agent.go
  - internal/agent/llm_agent_sources_test.go
  - internal/web/fetcher_image.go
  - internal/web/fetcher_image_test.go
  - internal/agui/image_proxy.go
  - internal/agui/image_proxy_test.go
  - internal/agui/server.go
  - internal/agui/server_display_test.go
  - scripts/cache_invariant_audit.sh
  - internal/agent/prompt/cache_audit_sources_test.go
autonomous: true
requirements: [DISP-01, DISP-03, DISP-05]
must_haves:
  truths:
    - "The volatile numbered source list rides the tail-inject append-to-copy path (Budget.Sources) — messages[0] stays byte-identical and the cache-invariant gate stays green (DISP-01/D-05)"
    - "A static citation-convention sentence lives in messages[0]; the volatile list never does"
    - "A backend image-proxy (/api/image-proxy, RequireAuth) fetches thumbnails/favicons via the existing web SSRF guard + DNS-pin with an image content-type allowlist + size cap (D-09)"
    - "Reopened-thread tool turns are re-derived through the SAME normalizer at projectMessages so replay displays are identical to live (D-06)"
  artifacts:
    - path: "internal/web/fetcher_image.go"
      provides: "Client.FetchImage reusing guard.validateAndPin + hardenedTransport"
      contains: "func (c *Client) FetchImage"
    - path: "internal/agui/image_proxy.go"
      provides: "/api/image-proxy handler behind RequireAuth"
    - path: "internal/agent/prompt/builder.go"
      provides: "Budget.Sources field + block() source-list line"
      contains: "Sources"
  key_links:
    - from: "internal/agent/llm_agent.go"
      to: "prompt.Budget.Sources"
      via: "tail-inject of the rendered numbered source list (copy, not messages[0])"
      pattern: "Sources"
    - from: "internal/agui/server.go"
      to: "display.Normalize"
      via: "re-derive per tool turn in projectMessages (D-06)"
      pattern: "display\\.Normalize|Normalize\\("
    - from: "internal/web/fetcher_image.go"
      to: "guard.validateAndPin"
      via: "image fetch reuses the SSRF defense"
      pattern: "validateAndPin|hardenedTransport"
---

<objective>
Complete the backend protocol: (1) the D-05 source-list tail-inject — thread the normalizer's numbered source list through `prompt.Budget.Sources` so it rides the append-to-copy path (KV-cache safe, `messages[0]` untouched) while a static convention sentence lives in `messages[0]`; (2) the D-09 image-proxy — `Client.FetchImage` reusing the existing SSRF guard + a `/api/image-proxy` route behind `RequireAuth`; (3) the D-06 re-derive — run the SAME `display.Normalize` over each persisted tool turn at `projectMessages` so reopened-thread displays are identical to live.

Purpose: These are the three architecturally non-trivial backend wirings (KV-cache invariant, SSRF-safe image fetch, live/replay parity). Each reuses a battle-tested seam rather than re-opening a closed cache or security hole.

Output: Source list rides the tail-inject (cache gate green), the image-proxy serves SSRF-safe images, reopened threads re-derive displays. Depends on 26-01 (`display.Normalize` + the source registry + `RenderSourceList`).
</objective>

<execution_context>
@/home/user/Aura/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Aura/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-typed-display-protocol-router/26-CONTEXT.md
@.planning/phases/26-typed-display-protocol-router/26-RESEARCH.md
@.planning/phases/26-typed-display-protocol-router/26-01-SUMMARY.md
@.claude/skills/golang-security/SKILL.md
@.claude/skills/golang-context/SKILL.md
@.claude/skills/golang-testing/SKILL.md
@.claude/skills/golang-database/SKILL.md
</context>

<artifacts_produced>
NEW symbols this plan creates (excluded from downstream drift verification):
- `internal/agent/prompt/builder.go`: `Budget.Sources` field + the `block()` source-list line.
- `internal/agent/llm_agent.go`: the source-registry → `Budget.Sources` threading + the static convention sentence added to `messages[0]` (system prompt).
- `internal/web/fetcher_image.go`: `func (c *Client) FetchImage(ctx, convID, rawURL string) ([]byte, string, error)`.
- `internal/agui/image_proxy.go`: the `/api/image-proxy` HTTP handler + route registration in `server.go`.
- `internal/agui/server.go`: the `projectMessages` re-derive call to `display.Normalize`.
- `scripts/cache_invariant_audit.sh` + the `aura cache-audit` fixtures: a turn that emits a source list (extends the existing 22-fixture stream).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source-list tail-inject (Budget.Sources) + static convention line + cache-invariant fixtures</name>
  <files>internal/agent/prompt/builder.go, internal/agent/prompt/builder_test.go, internal/agent/prompt/budget_sources_test.go, internal/agent/llm_agent.go, internal/agent/llm_agent_sources_test.go, scripts/cache_invariant_audit.sh, internal/agent/prompt/cache_audit_sources_test.go</files>
  <read_first>
    - internal/agent/prompt/builder.go (`Budget` struct :33-39, `present()` :43-45, `block()` :49-64, `buildBase` append-to-copy :91-104 — `messages[0]` is never mutated; add a `Sources` field + a block() line mirroring CurrentTime/Today)
    - internal/agent/llm_agent.go (the tail-inject comment + `prompt.Budget{…CurrentTime…}` build :268-287; the AG-031 KV-drift guard :303-304 — `prefixSnapshot(req.Messages)`)
    - internal/agent/display/sources.go (from 26-01: the source registry + `RenderSourceList()` — the input to Budget.Sources)
    - scripts/cache_invariant_audit.sh (the 22-fixture gate driving `aura cache-audit`; the THREE byte-stable streams `request NN`/`messages1 NN`/`skillman NN` — a new source-list turn must keep messages[0] byte-identical)
    - .planning/phases/26-typed-display-protocol-router/26-RESEARCH.md §"Resolved R7" (tail-inject mechanics) + §"Pitfall 2" (poisoning messages[0]) + §"Assumptions A7" (static sentence is invariant)
    - .planning/phases/26-typed-display-protocol-router/26-CONTEXT.md D-05 (CACHE-CRITICAL paragraph)
  </read_first>
  <behavior>
    - Test (DISP-01/05): `Budget{Sources: "[1] Title — url"}` causes `Build` to append a trailing user-role message containing the source list to a COPY of history; `messages[0]` is byte-identical to the no-Sources build.
    - Test (DISP-01/05): with a non-empty Sources, the budget hint is still tail-injected (present() true); empty Sources omits the line (backward-compatible).
    - Test (DISP-01): the cache-audit replay with a source-list-emitting turn keeps every `request NN` (messages[0]) hash identical across turns.
    - Test: the static citation-convention sentence is present in the system prompt (messages[0]) and is a fixed string (no per-turn templating).
  </behavior>
  <action>
    Add a `Sources string` field to `prompt.Budget` (:33-39), include it in `present()`, and add a `block()` line rendering the numbered source list (e.g. wrapped in a `<sources>…</sources>` tag, mirroring `<current_time>` :57-59). In `llm_agent.go`, render the per-turn source registry (accumulated across `web_search` calls) via `display.RenderSourceList()` and thread it into the `prompt.Budget` constructed at :275-282 — so it rides the existing append-to-copy tail-inject, NEVER `messages[0]`. Add the STATIC convention sentence ("emit `[n]` next to claims; sources are numbered in the provided list") to the system prompt builder that produces `messages[0]` (it is invariant — safe in the cached prefix per A7). Extend `scripts/cache_invariant_audit.sh` fixtures (and the `aura cache-audit` fixture set) with a turn that emits a source list, asserting messages[0] stays byte-identical. Add `budget_sources_test.go` + `cache_audit_sources_test.go`.
  </action>
  <verify>
    <automated>go test ./internal/agent/prompt/ -run TestBudgetSources && go test ./internal/agent/ -run TestSources && bash scripts/cache_invariant_audit.sh</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n 'Sources' internal/agent/prompt/builder.go` returns the field + block() line.
    - BEHAVIOR: `go test ./internal/agent/prompt/ -run TestBudgetSources` passes (VALIDATION.md "Volatile source list NOT in messages[0]" row).
    - BEHAVIOR: `bash scripts/cache_invariant_audit.sh` exits 0 with the new source-list fixture (VALIDATION.md "messages[0] byte-stable" row) — the wrapper's independent hash diff agrees.
    - BEHAVIOR: a test asserts the static convention sentence is in messages[0] and the volatile list is NOT.
    - CLI: `go vet ./internal/agent/prompt/ ./internal/agent/` clean.
  </acceptance_criteria>
  <done>The numbered source list rides Budget.Sources tail-inject (messages[0] byte-identical, cache gate green); the static convention sentence is in messages[0]; the volatile list is provably absent from the cached prefix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Image-proxy — Client.FetchImage + /api/image-proxy route behind RequireAuth</name>
  <files>internal/web/fetcher_image.go, internal/web/fetcher_image_test.go, internal/agui/image_proxy.go, internal/agui/image_proxy_test.go, internal/agui/server.go</files>
  <read_first>
    - internal/web/fetcher.go (`Client.Fetch` HTML-only :53, `allowedContentTypes` :37-40 — the shape to parallel for images)
    - internal/web/ssrf.go (`guard.validateAndPin` :85 — hostname blocklist → DNS resolve → classify → pin) and internal/web/transport.go (`hardenedTransport` :44 — dial-only-pinned-IP, no auto-redirect; `withConvID` :28) and internal/web/client.go (`Client` composition :14-37)
    - internal/agui/server.go (the mux + route registration pattern; the `/api/` carve-out + `RequireAuth` whole-origin gate the image-proxy inherits; the existing handler signatures)
    - .planning/phases/26-typed-display-protocol-router/26-RESEARCH.md §"Resolved R8" (image-proxy reuse seam) + §"Security Domain" (image-only allowlist EXCLUDE svg, size cap, no redirect follow) + §"Don't Hand-Roll" (SSRF-safe image fetch)
    - .planning/phases/26-typed-display-protocol-router/26-CONTEXT.md D-09 (mandatory safety constraint) + §security_threat_model (image-proxy as open SSRF relay)
  </read_first>
  <behavior>
    - Test (D-09): `FetchImage` against a private/metadata target (169.254.169.254, 127.0.0.1, 10.x) is REJECTED by the SSRF guard (reuses validateAndPin).
    - Test (D-09): `FetchImage` of a non-image content-type is rejected (image content-type allowlist: png/jpeg/webp/gif; EXCLUDE image/svg+xml per A5).
    - Test (D-09): a response exceeding the size cap is rejected; redirects are NOT auto-followed.
    - Test: the `/api/image-proxy` route is registered behind `RequireAuth` (an unauthenticated request beyond loopback is rejected); a valid image streams back with a strict `Content-Type` + `Cache-Control`.
  </behavior>
  <action>
    Add `func (c *Client) FetchImage(ctx context.Context, convID, rawURL string) ([]byte, string, error)` to `internal/web/fetcher_image.go`, reusing `c.transport.client` + `withConvID` + `guard.validateAndPin` (do NOT use a fresh `http.Get` — that re-opens the SSRF hole) with an image content-type allowlist (png/jpeg/webp/gif; exclude svg) + a size cap (io.LimitReader) + no auto-redirect-follow. Create `internal/agui/image_proxy.go`: a handler reading a `url` query param, calling `FetchImage`, streaming the bytes with `Content-Type` + `Cache-Control`; register it under `/api/image-proxy` in `server.go` behind the existing `RequireAuth` gate (inherited by all `/api/` routes). Tests for the SSRF rejections, the content-type allowlist, the size cap, and the auth gate.
  </action>
  <verify>
    <automated>go test ./internal/web/ -run TestFetchImage && go test ./internal/agui/ -run TestImageProxy && go test -race ./internal/web/ ./internal/agui/</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n 'func (c \*Client) FetchImage' internal/web/fetcher_image.go` returns the method.
    - SOURCE: `grep -nE 'validateAndPin|hardenedTransport' internal/web/fetcher_image.go` confirms SSRF reuse (no fresh http.Get).
    - SOURCE: `grep -n 'image-proxy' internal/agui/server.go` shows the route under the RequireAuth-gated `/api/` prefix.
    - BEHAVIOR: `go test ./internal/web/ -run TestFetchImage` passes the private/metadata-IP rejection + content-type allowlist + size-cap assertions (VALIDATION.md DISP-05/D-09 row; tag `web_integration` if the test dials).
    - BEHAVIOR: `go test ./internal/agui/ -run TestImageProxy` asserts the RequireAuth gate.
    - CLI: `go test -race ./internal/web/ ./internal/agui/` clean.
  </acceptance_criteria>
  <done>FetchImage reuses the SSRF guard + DNS-pin with an image allowlist + size cap + no redirect follow; the /api/image-proxy route is RequireAuth-gated; SSRF + auth tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-06 re-derive displays at projectMessages (live/replay parity)</name>
  <files>internal/agui/server.go, internal/agui/server_display_test.go</files>
  <read_first>
    - internal/agui/server.go (`handleMessages` :261-288, `projectMessages` :455-473, `projectToolCalls` :475-494 — tool turns project `Content=ResultPreview` + `ToolCalls`; attach the re-derived display per tool turn into the MESSAGES_SNAPSHOT)
    - internal/agent/display/normalize.go (from 26-01: `Normalize(toolName, result)` — run it over the persisted tool turn)
    - internal/runner/runner_persist.go (tool turn persist :118-123 — `Content=ResultPreview`, NOT full bytes; the value the replay normalizer must consume to match live, Pitfall 4)
    - .planning/phases/26-typed-display-protocol-router/26-RESEARCH.md §"Resolved R9" (re-derive at projectMessages) + §"Pitfall 4" (preview-vs-full-bytes fidelity) + §"Assumptions A4 / OQ2" (per-type preview sufficiency — confirm each in-scope type re-derives from the preview; if a type is lossy, use the D-06 store-fallback for THAT type only)
  </read_first>
  <behavior>
    - Test (D-06): `projectMessages` over a persisted `web_search` tool turn produces a snapshot message whose tool part carries a re-derived `DisplayPayload` matching what the live `aura.display` event carried for the same result.
    - Test (D-06): a tool turn for an unrecognized tool produces NO display (recognized==false) — the snapshot replays the raw card, identical to live D-FALLBACK.
    - Test: each in-scope display type re-derives identically from the persisted `ResultPreview` (per-type preview sufficiency audit, OQ2); any type whose preview is lossy is documented + uses the store-fallback.
  </behavior>
  <action>
    In `projectMessages`/`projectToolCalls`, for each tool turn run `display.Normalize(toolName, <the persisted preview/result shape>)`; on recognized==true, attach the resulting `DisplayPayload` to the projected tool message so the MESSAGES_SNAPSHOT carries the re-derived display (D-06 "one normalizer for live + replay"). Audit per-type preview sufficiency (A4/OQ2): confirm each in-scope type (web_result/document/code/local_artifact/table/chart/system_event/swarm_report) re-derives from `ResultPreview`; for any lossy type, record it in the SUMMARY and apply the D-06 store-fallback for that type ONLY (the in-scope types should all be derivable per RESEARCH R6). Add `server_display_test.go` with a snapshot golden asserting live/replay parity.
  </action>
  <verify>
    <automated>go test ./internal/agui/ -run TestProjectMessagesDisplay</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -nE 'display\.Normalize|Normalize\(' internal/agui/server.go` shows the re-derive call in projectMessages.
    - BEHAVIOR: `go test ./internal/agui/ -run TestProjectMessagesDisplay` passes the live/replay parity golden (VALIDATION.md D-06 snapshot row).
    - BEHAVIOR: a test asserts an unrecognized-tool turn produces no display (replay D-FALLBACK == live).
    - DOC: the SUMMARY records the per-type preview-sufficiency audit result (which types re-derive from preview; any store-fallback).
    - CLI: `go vet ./internal/agui/` clean.
  </acceptance_criteria>
  <done>Reopened-thread tool turns re-derive displays through the same normalizer at projectMessages; replay matches live by construction; the per-type preview audit is recorded; the snapshot golden passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citation data → prompt prefix | The volatile source list must not enter the cached `messages[0]` |
| cockpit → external image host | The image-proxy is the only path to an external host; arbitrary client-side `<img src>` is forbidden |
| persisted tool result → replay normalizer | The replay path must consume the same preview shape as live (parity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | DoS (cost) | `prompt.Budget.Sources` | mitigate | Volatile source list rides the append-to-copy tail-inject; `messages[0]` byte-identical; AG-031 guard (`llm_agent.go:303`) + `cache_invariant_audit.sh` fixtures assert it |
| T-26-09 | Information Disclosure / Elevation | `web.FetchImage` | mitigate | Reuses `guard.validateAndPin` + `hardenedTransport` (no fresh http.Get); image content-type allowlist (EXCLUDE svg); size cap; no auto-redirect-follow — blocks private/metadata-IP targets (D-09) |
| T-26-10 | Elevation | `/api/image-proxy` | mitigate | Mounted behind the Phase-24 `RequireAuth` whole-origin gate (inherited by `/api/`); not an open relay |
| T-26-11 | Spoofing | `projectMessages` re-derive | mitigate | The same trusted `display.Normalize` runs live + replay; an unrecognized tool produces no display (replay D-FALLBACK == live) |
| T-26-SC | Tampering | go installs | mitigate | NO new dependency (standard-library + existing `internal/web`); slopcheck N/A |
</threat_model>

<verification>
- `go build ./...` + `go vet ./internal/agent/prompt/ ./internal/agent/ ./internal/web/ ./internal/agui/` clean.
- `bash scripts/cache_invariant_audit.sh` exits 0 (messages[0] byte-stable with the new source-list turn).
- `go test ./internal/agent/prompt/ ./internal/web/ ./internal/agui/` + `-race` green.
- The per-type preview-sufficiency audit (OQ2) is recorded in the SUMMARY.
</verification>

<success_criteria>
- The source list rides Budget.Sources (messages[0] byte-identical; cache gate green); the static convention sentence is in messages[0].
- The image-proxy reuses the SSRF guard + DNS-pin with an image allowlist + size cap, behind RequireAuth.
- Reopened threads re-derive displays identically to live; the preview-sufficiency audit is documented.
- Targeted Go tests + race + the cache-invariant gate pass.
</success_criteria>

<output>
Create `.planning/phases/26-typed-display-protocol-router/26-03-SUMMARY.md` when done. Record the image-proxy route path + query param contract (for the WebResultDisplay `<img>` in 26-05) and the per-type preview-sufficiency audit result.
</output>
