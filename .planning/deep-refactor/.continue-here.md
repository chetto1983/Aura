---
context: deep-refactor
phase: Phase01B
status: phase01b-parent-closed-container-e2e-verified
last_updated: 2026-05-15T22:32:25+02:00
---

# Aura Deep Refactor Handoff

<current_state>
The previous deep-refactor phase folders were intentionally deleted because
they had become noisy. Clean phase scaffolds now exist under
`D:/Aura/.planning/deep-refactor/`, generated from root `prd.md` plus
`D:/Aura/.planning/aura-deep-refactor-decisions.json`.

Last completed commit observed:
`009639ae feat(tools): add MCP hints + VisibilityTier to ToolDefinition (US-I01)`.
Latest observed git HEAD is
`009639ae feat(tools): add MCP hints + VisibilityTier to ToolDefinition (US-I01)`.
</current_state>

<completed_work>

- Recreated `D:/Aura/.planning/deep-refactor/INDEX.md` as a clean phase map.
- Recreated `D:/Aura/.planning/deep-refactor/Phase01/` as the true parent
  phase folder.
- Recreated Phase01 children under `Phase01/subphases/`:
  `Phase01_Stabilize_Map`, `Phase01A_Run_Event_Foundation`,
  `Phase01B_Identity_Capability_Grants`, and `Phase01C_Question_Gate`.
- Recreated standalone Phase02 through Phase09 folders with
  `plan.md`, `source.md`, `benchmark.md`, and `progress.md`.
- Marked every recreated folder as self-audited scaffold, not verified.
- Preserved old `.planning/wave*` files as evidence only.
- Implemented Phase01A first storage foundation patch locally:
  migration v6 and `internal/storage/runs`.
- Implemented Phase01A second Hub lifecycle patch locally:
  optional `chat.Hub` lifecycle store seam, inbound dedupe, and redacted
  lifecycle event persistence.
- Ran a fresh Codex verifier pass for Phase01A and repaired the missing
  durable tool/usage/final-output metadata event path.
- Promoted Phase01B planning from scaffold to source-audited implementation
  plan. The first bounded code slice is migration v7 plus `internal/identity`.
- Implemented Phase01B1 locally: migration v7 identity/capability tables,
  non-breaking `runs.actor_id`, and `internal/identity` default-deny
  authorizer.
- Closed Phase01B1 after subagent loop: repaired delegated actor inheritance,
  channel-account/principal mismatch, disabled-principal denial,
  parented-session inheritance, and grant-subject validation; local full gates
  and subagent verification passed.
- Implemented Phase01B2 locally: migration v8 backfills persisted
  `allowed_users` rows into deterministic Telegram principals, channel
  accounts, session actors, and owner/user grants; auth bootstrap/approval
  creates the same identity rows while preserving `allowed_users`.
- Verified Phase01B2 with the Phase01B benchmark, targeted migration/identity
  /auth tests, `go build ./...`, `go vet ./...`, and `go test ./...`.
- Implemented Phase01B3 locally: dashboard bearer requests now carry the
  deterministic Telegram session actor ID, `/chat` rejects authenticated body
  `user_id` impersonation, and `api.chat` authorization is enforced before
  chat execution.
- Verified Phase01B3 with targeted API/auth tests, `go test ./internal/...`,
  `go build ./...`, `go vet ./...`, and `go test ./...`.
- Upgraded Phase01B3 to container E2E verified: rebuilt `aura:local`, recreated
  only `aura-aura-1`, confirmed health, probed `/api/health` and `/api/chat`
  bearer boundaries, received `AURA_E2E_PHASE01B3_OK` from live chat, and
  verified the live SQLite `api.chat` authorization decision.
- Implemented and verified Phase01B4: tool definitions can declare required
  capabilities, registry execution authorizes `tool.execute` or a tool-specific
  capability before calling `Tool.Execute`, dashboard chat carries the identity
  authorizer into the agent path, and local plus containerized registry tests
  prove visible-tool fail-closed behavior with disposable SQLite evidence.
- Upgraded Phase01B4 to container verified: rebuilt `aura:local`, recreated
  only `aura-aura-1`, confirmed health, probed `/health`, `/api/health`, and
  bearer `/api/chat`, received `AURA_E2E_PHASE01B4_OK`, and verified the latest
  live SQLite `api.chat` authorization decision.
- Registered `D:/tmp/cli-printing-press` as an Aura reference map in
  `D:/Aura/docs/cli-printing-press-study.md`.
- Implemented and verified Phase01B5: Telegram `/start` and `/login` now ensure
  deterministic identity/grants before minting dashboard tokens. Configured
  allowlist users remain config-only, not `allowed_users` rows, while persisted
  bootstrap/approved users repair missing identity from stored source.
- Upgraded Phase01B5 to container-updated: rebuilt `aura:local`, recreated only
  `aura-aura-1`, confirmed health `healthy`, `/health=200`, unauthenticated
  `/api/health=401`, and unauthenticated `/api/chat=401`. Live Telegram API
  fixture and bearer chat marker are unavailable in this shell because
  `AURA_E2E_TOKEN` is not set; deterministic auth/Telegram Go fixtures are the
  ground-truth B5 proof.
- Implemented and verified Phase01B6: `identity.DelegateActor` creates bounded
  delegated actors and direct child grants after parent authorization; cron
  `RunNow`, cron Hub agent jobs, and swarm worker assignments now carry
  delegated actor context when identity authority is present; swarm tools
  require `swarm.spawn`.
- Upgraded Phase01B6 to container-updated: rebuilt `aura:local`, recreated only
  `aura-aura-1`, confirmed health `healthy`, `/health=200`, and
  unauthenticated `/api/health=401`. B6 ground truth is the deterministic
  SQLite-backed identity/cron/cron-Hub/swarm fixtures.
- Implemented and verified Phase01B7: authorization denials now carry run/event
  provenance, `runs.Store` records metadata-only `authorization_denied` run and
  audit events, chat Hub attaches run provenance and the denial recorder, and
  production Telegram/Cron Hubs use the shared run store.
- Removed Phase01B fail-open authority paths: registry execution denies when no
  authorizer is present; cron manual jobs, cron Hub jobs, and swarm delegated
  assignments fail before runner execution when identity delegation is missing;
  app startup no longer falls back to a cron agent dispatch path if cron Hub
  construction fails.
- Re-verified the repo and updated the container after the fail-closed repair:
  Phase01B targeted gates passed, `go build ./...`, `go vet ./...`,
  `go test ./... -count=1`, `docker compose build aura`,
  `docker compose up -d --no-deps aura`, `aura-aura-1` health `healthy`,
  `/health=200`, unauthenticated `/api/health=401`, unauthenticated
  `/api/chat=401`, and startup logs showed no errors.
- Ran the Phase01B parent closure verifier. Local/code gates passed again:
  `go test ./internal/storage/runs ./internal/chat -count=1`, the Phase01B
  target package gate, `go build ./...`, `go vet ./...`, and
  `go test ./... -count=1`.
- Rebuilt/restarted the `aura` container after the parent verifier repair.
  Live auth-boundary probes passed: `/health=200`, unauthenticated
  `/api/health=401`, unauthenticated `/api/chat=401`, bearer
  `/api/health=200`, bearer mismatched `/api/chat=403`, and SQLite
  `authz_decisions` records `api.chat` allow decisions for
  `actor:telegram:session:1148481707`.
- Repaired a parent-verifier gap: Hub-backed `runs.actor_id` and lifecycle
  `run_events.actor_id` now inherit the actor from context.
- Closed the Phase01B parent provider/config blocker: secret-shaped dashboard
  keys now route to `secrets`, the live stale `settings.LLM_API_KEY` row was
  removed, runtime `D:/Aura/data/.env` plus its temporary retired copy were
  removed from `D:/Aura/data`, the container was rebuilt, and exact live
  markers `AURA_PIPE_OK` plus `PHASE01B_CLOSE_OK` passed.
</completed_work>

<remaining_work>

- Recommended next bounded action: select the next canonical phase slice from
  `D:/Aura/.planning/deep-refactor/INDEX.md`.
- A separate subagent verifier for Phase01A was not spawned in this turn.
- Phase01B1 subagent verification passed. Phase01B parent is closed for the
  identity/capability slice.
- Keep Phase 8 swarm/team-loop work separate from Phase01A unless the PRD
  explicitly requires run/event contract support for parent/child runs.
</remaining_work>

<decisions_made>

- ADR-036: old `.planning/` wave files are evidence, not executable queue.
- ADR-037: `internal/cron` package-name cleanup is complete.
- Recreated phase folders intentionally do not inherit stale verifier status.
- Cron behavior, missed-run semantics, workflow/RunGraph integration, and
  background-run policy remain Phase 8 work.
</decisions_made>

<blockers>

No remaining local Phase01A Go/test blocker. Phase01B1 local and subagent
verification passed. Phase01B2 local Go verification passed. Phase01B3
container E2E verification passed. Phase01B4 repo and container verification
passed. Phase01B5 local proof and container update passed. Phase01B6 delegated
actor proof and container update passed. Phase01B7 denial-event proof and the
fail-closed repair passed full Go gates and container update. Parent closure
local/code, live auth-boundary, DB/secrets, runtime `.env` retirement, and
exact live bearer chat marker gates passed. Phase01B parent is closed for the
identity/capability slice.
</blockers>

## Required Reading

1. `D:/Aura/CONTINUE-HERE.md` - fresh-session entrypoint.
2. `D:/Aura/AGENTS.md` - operating rules and planning-state discipline.
3. `D:/Aura/prd.md` - active route and phase definitions.
4. `D:/Aura/.planning/aura-deep-refactor-decisions.json` - ADR route.
5. `D:/Aura/.planning/deep-refactor/INDEX.md` - phase folder map.
6. `D:/Aura/.planning/deep-refactor/Phase01/subphase-summary.md` - parent Phase 1 state.
7. `D:/Aura/.planning/deep-refactor/Phase01/subphases/Phase01B_Identity_Capability_Grants/` - next implementation target.

## Critical Anti-Patterns

- Do not execute old wave plans directly.
- Do not mark a phase green without live gates and a fresh review pass.
- Do not implement from a self-audited scaffold; promote one selected phase
  first.

## Infrastructure State

- No dev server or watcher intentionally left running.
- Git head observed:
  `009639ae feat(tools): add MCP hints + VisibilityTier to ToolDefinition (US-I01)`.
- Worktree dirty state observed: Phase01B6 delegated actor implementation,
  Phase01B7 denial-event implementation, Phase01B fail-closed repair,
  planning handoff/progress records, provider-secret closure docs,
  `AGENTS.md` CI-after-push rule, and unrelated untracked Markdown files. Preserve the
  unrelated Markdown file unless a current slice explicitly owns it.

<next_action>
Select the next canonical phase slice from the deep-refactor index before
editing more code. Do not bundle Phase 8 cron RunGraph or swarm topology
redesign into unrelated slices.
</next_action>
