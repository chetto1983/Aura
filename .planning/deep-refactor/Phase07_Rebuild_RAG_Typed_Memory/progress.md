# Phase07 Progress

| Date | Actor | Change | Verification | Blockers | Deviations From Plan |
| --- | --- | --- | --- | --- | --- |
| 2026-05-17 | Codex | Repaired conversation archive recall after live DB audit found historical archive rows absent from compact recall and assistant tool-call rows still eligible for compact archive. `Rebuild` now scans the full archive via `ListAll(0)`, and compact archive eligibility excludes assistant rows that carry tool-call JSON while preserving final assistant rows and the raw lossless `conversations` table. | `go test ./internal/storage/memoryindex -run "Test(ArchiveEligibility\|ArchiveTurnEligibility\|ParityAppendAndRebuildProduceSameCompactSet\|RebuildIndexesAllArchiveTurns\|ArchiveTurns_ToolRowPersistsInConversationsButNotCompact)" -count=1`; `go test ./internal/agent/tools/registry -run "TestSearchMemoryTool_ToolRoleExcludedFromDefaultSearch" -count=1`; `go test ./internal/conversation -run "Test.*ListAll\|Test.*Archive" -count=1`; `go test ./internal/storage/memoryindex ./internal/conversation ./internal/agent/tools/registry -count=1`; `go test ./internal/db -run TestProductionSQLiteOpensGoThroughSharedDBPackage -count=1`; `go build ./...`; `go test ./internal/... -count=1` all passed. | Live DB compact archive was not rebuilt in this slice. Restarting Aura will run the compact-memory rebuild path with the repaired full archive scan. | Also adjusted untracked `cmd/debug_convdump/main.go` to use `internal/db.OpenReadOnly` so the repo SQLite-open policy test is not blocked by local debug tooling. |
| 2026-05-17 | Codex | Created canonical Phase07D planning folder for user/operational memory typed tiers and updated parent references to treat it as a self-audited closure candidate, not a closed phase. | `go test ./internal/storage/memoryindex ./internal/learning ./internal/agent/tools/registry -run "Test(CollectionConstants\|KindBackCompat\|ScaffoldedCollections\|WriteApprovedLesson\|WriteApprovedUserFact\|RecallUserMemoryTool\|RecallOperationalTool\|ExamplesParameterEval)" -count=1` passed. | Fresh independent verifier and live mixed-tier probe not run. | No production code edited; existing Phase-N/O/Q code evidence was translated into Phase07D planning artifacts. |
| 2026-05-17 | Codex | Hardened Phase07D local closure evidence by adding deterministic broad-scope and runtime recall/freshness wiring tests. | `go test ./... -count=1`, `go build ./...`, `go vet ./...`, and the Phase07D targeted gates passed. | Fresh independent verifier and full `cmd/probe_chat` mixed-tier golden fixture still not run. | This moves Phase07D from source-only self-audit to locally tested closure hardening, but not to independently verified/closed. |
| 2026-05-17 | Codex | Added a disposable mixed-tier direct tool golden for Phase07D. The runtime test seeds approved/pending user and operational rows plus source/archive/proposal trap rows and proves typed recall and broad search stay separated. | `go test ./cmd/aura -run TestRegisterMemoryRecallToolsWiresTypedTiersAndFreshness -count=1`; `go test ./internal/agent/tools/registry -run "Test(SearchMemoryTool(UsesOneCompactSearchForAllScope\|RejectsTypedTierScopes)\|RecallUserMemoryTool\|RecallOperationalTool\|ExamplesParameterEval)" -count=1`; `go test ./cmd/aura ./internal/agent/tools/registry -count=1`; combined Phase07D gate with `./cmd/aura` passed. | Fresh independent verifier and full live/chat `cmd/probe_chat` reply capture still not run. | No live user data was mutated; the fixture uses disposable SQLite. |
| 2026-05-17 | Codex | **Phase07D CLOSED** - added and ran the live/chat `phase07d-mixed-tier-recall` probe through `/api/chat`, validating typed recall tool use and no wrong-tier token leakage against live SQLite `tool_attempts`. | Live Compose probe passed with `pass=true`, `tool_calls=2`, `llm_calls=2`; `go test ./cmd/probe_chat ./cmd/aura ./internal/agent/tools/registry -count=1`; combined Phase07D gate with `./cmd/probe_chat`; `go vet ./...`; `go build ./...`; `go test ./... -count=1` all passed. | None for Phase07D. | Temporary fixture rows are cleaned by the probe; a temporary E2E bearer token was minted for the live request. |
| 2026-05-17 | Codex | Created Phase07E Source Span And Byte Offsets subphase folder and mapped the source/page-only gap to a bounded implementation plan. | Read-only inspection of PRD Phase 7 gates, Phase07 parent files, Phase07B audit G.2.3, `internal/db/migrations`, `internal/storage/memoryindex`, `internal/agent/tools/registry/{memory_search,source_read,source}`, and `cmd/probe_chat`. | No implementation benchmarks run yet. No independent verifier/subagent review in this turn. | Phase07E starts with page-row byte spans into the source text artifact; sub-page semantic chunking and PDF coordinate spans are deferred. |
| 2026-05-17 | Codex | **Phase07E CLOSED** - compact source span schema, rebuild stamping, span-aware source search output, production `source(action=read,...)` byte-range reads, and live `cmd/probe_chat` source-span golden are implemented. | `go test ./internal/db/migrations ./internal/storage/memoryindex ./internal/agent/tools/registry ./cmd/probe_chat -count=1`; `go vet ./...`; `go build ./...`; `go test ./... -count=1`; live Compose `phase07e-source-span-read` returned `pass=true`, `tool_calls=2`, `llm_calls=3`, exact `SPAN=<target>`. | No independent verifier/subagent review in this turn. | Production follow-up uses registered `source(action=read,...)`, not the historical `read_source` tool name. PDF coordinate spans and sub-page semantic chunking are deferred. |
| 2026-05-17 | Codex | **Phase07F CLOSED** - wiki frontmatter trust metadata now flows through wiki docs, graph node cards, SQLite/Qdrant results, and `search_memory(scope=wiki)`. The live wiki write path now direct-upserts changed Qdrant page docs from `ReindexWikiPage` instead of using the warm-cache full rebuild. | `go test ./internal/wiki ./internal/storage/search ./internal/agent/tools/registry ./cmd/probe_chat -count=1`; `go vet ./...`; `go build ./...`; `go test ./... -count=1`; `docker compose build aura`; `docker compose up -d --no-deps aura`; live `phase07f-wiki-frontmatter-metadata` returned `pass=true`, `tool_calls=2`, `llm_calls=3`, exact marker/source and metadata tokens. | No Phase07F blocker remains. No independent verifier/subagent review in this turn. | `tool_attempts` did not receive fresh web-chat rows during the live probe, so the Phase07F live proof used unprompted marker/source retrieval plus API/Qdrant ground truth. Run/tool-attempt observability should be audited in a later runtime/observability slice. |
| 2026-05-16 | Codex | Repaired parent Phase07 planning state to align with current PRD/code: Phase07C is Projection Freshness Registry, not the stale task-level recall scaffold. Created the canonical Phase07C subphase folder and translated Phase-M evidence into Aura phase files. | `go test ./internal/storage/freshness ./internal/db/migrations ./internal/storage/memoryindex ./internal/agent/tools/registry ./cmd/aura -count=1` passed. | No code blocker. Resume files still mention older HEAD `ecb4cf3e`; this repair follows current git/PRD/code authority. | No independent verifier/subagent run in this turn; Phase07C is marked self-audited rather than independently verified. |
| 2026-05-15 | Codex | Recreated clean standalone Phase07 scaffold after phase-folder reset. | Local file contract only. | Needs source map and verifier. | No old verification inherited. |
| 2026-05-15 | Codex | Source-merged the memory/compact-archive failure into a Phase 7 parent plan and defined Phase07A Compact Archive Hygiene as the first bounded sub-phase. Sources include PRD Phase 7/9, ADR-031, `docs/graphrag-local-first-reference-map.md`, `docs/aura-master-plan.md` as evidence, `D:/tmp/llm_wiki`, `D:/tmp/logseq`, `D:/tmp/mem0`, `D:/tmp/nanobot`, and current Aura memoryindex/search code. | Read-only file and code inspection plus three read-only subagent reports. Benchmarks not run; no production code edited. | Phase01B closure verifier is still open in durable handoff. Phase07A implementation must not mark Phase01B complete. | Promoted Phase07 from a thin scaffold to a sub-phase parent plan inside the existing standalone folder to avoid moving phase folders during a dirty worktree. |
| 2026-05-16 | Ralph | **Phase07A CLOSED** — Compact Archive Hygiene (role=tool exclusion from compact_memory_documents). US-K01..K04 shipped (commits 9d74809d, cfda6bee, 43504082, 7ebbf083, 770eed0a). All benchmark checks met; full test suite green. Phase07B (Typed Collection Registry) is the natural next sub-phase — scaffolded but not in scope. | go build/vet/test ./... all green across all 4 US-K stories. All 5 benchmark rows now show "met". | None. | Phase07B-F remain scaffolded; no implementation work started. |
| 2026-05-16 | Ralph | **Phase07B CLOSED** — Typed Collection Registry (typed enum + score components + follow-up handles + SourceID filter). US-L01..L05 shipped (commits 1a6a609a, 92e446fb, ca6a86e3, bb0ed864, 508b32a1). All G.1 audit checks met; full test suite green. Phase07C-F remain scaffolded. | go build/vet/test ./... all green across all 5 US-L stories. All 9 benchmark rows show "met". | None. | Phase07C-F remain scaffolded; G.2 deferrals (freshness, user/operational memory, span offsets, frontmatter) explicitly in scope of later sub-phases. |
