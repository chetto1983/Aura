# Phase07F Progress - Wiki Frontmatter Schema And Prompt-Version Promotion

| Date | Actor | Change | Verification | Blockers | Deviations |
| --- | --- | --- | --- | --- | --- |
| 2026-05-17 | Codex | Created the canonical Phase07F planning folder and mapped the deferred wiki frontmatter metadata gap to a bounded read-side implementation slice. | Read-only inspection of PRD Phase 7, Phase07 parent plan/source/benchmark, Phase07B current-types audit G.2.4, `internal/wiki`, `internal/storage/search`, and `internal/agent/tools/registry/memory_search.go`. | Baseline package tests, implementation, live probe, and independent verifier not run yet. | No production code edited in this planning step. The plan is self-audited, not independently verified. |
| 2026-05-17 | Codex | **Phase07F CLOSED** - promoted wiki frontmatter trust metadata through wiki indexing, SQLite/Qdrant search results, graph node cards, and `search_memory(scope=wiki)`. Fixed a live blocker where `ReindexWikiPage` used the warm-cache full rebuild path and skipped changed wiki pages; it now upserts the changed page directly and deletes stale page/graph-node points when the file is gone. Added the live `phase07f-wiki-frontmatter-metadata` probe. | `go test ./internal/storage/search ./internal/agent/tools/registry ./cmd/probe_chat -run "Test(LoadWikiDocumentsPromotesFrontmatterMetadata\|SQLiteSearchReturnsFrontmatterMetadata\|QdrantSearcherSearchQueriesPointsAndMapsPayload\|QdrantRepositoryReindexWikiPageUpsertsChangedPageAfterWarmCache\|RebuildQdrantWikiDocumentsCreatesCollectionAndUpsertsDocs\|SearchMemoryTool_WikiFrontmatterMetadata)" -count=1`; `go test ./internal/wiki ./internal/storage/search ./internal/agent/tools/registry ./cmd/probe_chat -count=1`; `go vet ./...`; `go build ./...`; `go test ./... -count=1`; `docker compose build aura`; `docker compose up -d --no-deps aura`; live `cmd/probe_chat -case phase07f-wiki-frontmatter-metadata` passed with `pass=true`, `tool_calls=2`, `llm_calls=3`, exact marker/source and metadata tokens. | No Phase07F blocker remains. | The slice added a narrow reindex fix and a disposable live wiki fixture even though the first plan said read-side only. This was required to make the live metadata benchmark truthful. Current web chat did not create fresh `tool_attempts` rows during this probe; Phase07F uses unprompted marker/source retrieval plus API/Qdrant ground truth instead and leaves that run-observability gap for a later slice. No independent verifier/subagent was run. |
