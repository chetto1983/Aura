# Phase07D Benchmark - User/Operational Memory Typed Tiers

Status: closed with local and live verification on 2026-05-17. Package-level
baseline, broad-scope exclusion, runtime registration, disposable direct
mixed-tier tool golden probes, live `cmd/probe_chat` mixed-tier recall, and
full repo gates passed.

## Benchmark Rows

| Row Type | Command / Probe | Fixture / Data Source | Expected Ground Truth | Pass / Fail Threshold | PRD Gate | Actual Result |
| --- | --- | --- | --- | --- | --- | --- |
| Baseline | `go test ./internal/storage/memoryindex ./internal/learning ./internal/agent/tools/registry -run "Test(CollectionConstants\|KindBackCompat\|ScaffoldedCollections\|WriteApprovedLesson\|WriteApprovedUserFact\|RecallUserMemoryTool\|RecallOperationalTool\|ExamplesParameterEval)" -count=1` | Existing in-memory SQLite and fake compact-search fixtures | Registry has user/operational entries; writers create typed rows; recall tools return active-only layer-labelled hits; examples validate schema | Command exits 0 | Phase 7 typed layers and task-level recall | passed 2026-05-17 by Codex |
| Static registry | Same baseline command, registry tests | `internal/storage/memoryindex/collections_test.go` | `CollectionUserMemory` and `CollectionOperational` exist, have non-empty `StorageBackend`, and are not scaffolded | Tests pass | Typed collection registry | passed within baseline |
| User writer | Same baseline command, writer tests | `internal/learning/user_memory_writer_test.go` | Approved `user_memory` proposal writes exactly one `KindUserMemory` row with stable handle, category tag, patch update behavior, and wrong-kind no-op | Tests pass; deny path writes no row | User facts do not enter wiki accidentally | passed within baseline |
| Operational writer | Same baseline command, writer tests | `internal/learning/writer_test.go` | Approved `operational_memory` proposal writes exactly one `KindOperational` row; wrong-kind no-op; duplicate approval remains idempotent | Tests pass | Tool failures become operational lessons only after promotion | passed within baseline |
| User recall | Same baseline command, recall tests | `internal/agent/tools/registry/recall_user_memory_test.go` | `recall_user_memory` returns active user facts only, sorts newest first, includes `user_memory:` handles, filters by category, hides pending rows | Tests pass | Task-level user recall with handles | passed within baseline |
| Operational recall | Same baseline command, recall tests | `internal/agent/tools/registry/recall_operational_test.go` | `recall_operational` returns active operational lessons only, sorts newest first, includes `operational:` handles, filters by tool/error, hides pending rows | Tests pass | Task-level operational recall with handles | passed within baseline |
| Tool manifest | Same baseline command, examples parameter eval | `tool_definitions.go`, `examples_parameter_eval_test.go` | Recall tools are read-only/idempotent active-turn tools with examples that match parameters | Tests pass | Safe task-level recall surface | passed within baseline |
| Broad-scope exclusion | `go test ./internal/agent/tools/registry -run "TestSearchMemoryTool(UsesOneCompactSearchForAllScope\|RejectsTypedTierScopes)" -count=1` | `memory_search.go` scope enum and compact kind mapping | `search_memory` scopes remain `all`, `wiki`, `sources`, `archive`, `proposals`; user/operational tiers require task-level tools | Tests confirm `scope=all` queries only source/archive/proposal compact kinds and `user_memory`/`operational` scopes are rejected | Retrieval stops being memory soup | passed 2026-05-17 by Codex |
| Runtime registration | `go test ./cmd/aura -run TestRegisterMemoryRecallToolsWiresTypedTiersAndFreshness -count=1` | Disposable migrated SQLite DB with compact memory, mixed-tier trap rows, and `projection_state` | `search_memory`, `recall_operational`, and `recall_user_memory` register after compact memory and receive freshness store | Test confirms all three tools register; typed recall direct execution returns layer handles plus `freshness=` and `degraded_read=true` from the shared compact projection row | Recall tools available in production runtime | passed 2026-05-17 by Codex |
| Disposable user-memory probe | `go test ./cmd/aura -run TestRegisterMemoryRecallToolsWiresTypedTiersAndFreshness -count=1` plus existing recall tests | Disposable SQLite DB seeded with approved and pending user facts plus operational/source/archive/proposal trap rows sharing the same query token | Tool output cites only the approved `[user_memory]` handle, includes freshness/degraded annotations, and excludes pending user facts plus all wrong-tier trap rows | Direct tool probe passes | User facts do not appear in wiki unless promoted | passed 2026-05-17 by Codex |
| Disposable operational probe | `go test ./cmd/aura -run TestRegisterMemoryRecallToolsWiresTypedTiersAndFreshness -count=1` plus existing recall tests | Disposable SQLite DB seeded with approved and pending operational lessons plus user/source/archive/proposal trap rows sharing the same query token | Tool output cites only the approved `[operational]` handle, includes freshness/degraded annotations, and excludes pending operational lessons plus all wrong-tier trap rows | Direct tool probe passes | Tool failures do not become memory without validation | passed 2026-05-17 by Codex |
| Disposable mixed-tier golden | `go test ./cmd/aura -run TestRegisterMemoryRecallToolsWiresTypedTiersAndFreshness -count=1` | Same disposable mixed-tier fixture with source/archive/proposal trap rows | `search_memory(scope=all)` returns ordinary source/archive/proposal compact hits but never returns `user_memory` or `operational` rows; typed recall tools never return source/archive/proposal rows | Test asserts approved typed tokens are absent from broad search and trap tokens are absent from typed recall outputs | Retrieval stops being memory soup | passed 2026-05-17 by Codex |
| Live/chat mixed-tier golden | `docker compose run --rm -v ${PWD}\data:/data test go run ./cmd/probe_chat -case phase07d-mixed-tier-recall -url http://aura:8080/api/chat -api http://aura:8080/api -db /data/aura.db -token <seeded-token> -timeout 240 -json` | Live Aura container, `/data/aura.db`, temporary `compact_memory_documents` fixture seeded and cleaned by `cmd/probe_chat` | Assistant must call `recall_user_memory` and `recall_operational`, cite only approved user/operational tokens, avoid wrong-tier tokens, and `tool_attempts` must contain ok rows for both typed recall tools with no `search_memory` call | Probe exits 0 with `pass=true`, `tool_calls=2`, and no mismatches | Live task-level typed recall | passed 2026-05-17 by Codex; reply contained only approved tokens |
| Phase07D targeted gate | `go test ./internal/storage/memoryindex ./internal/learning ./internal/agent/tools/registry ./cmd/aura ./cmd/probe_chat -run "Test(CollectionConstants\|KindBackCompat\|ScaffoldedCollections\|WriteApprovedLesson\|WriteApprovedUserFact\|RecallUserMemoryTool\|RecallOperationalTool\|ExamplesParameterEval\|SearchMemoryTool(UsesOneCompactSearchForAllScope\|RejectsTypedTierScopes)\|RegisterMemoryRecallToolsWiresTypedTiersAndFreshness)" -count=1` | Phase07D package tests plus probe command compilation | All Phase07D local behavior and probe command compile | Command exits 0 | Closure regression gate | passed 2026-05-17 by Codex |
| Full repo gate | `go vet ./...`; `go build ./...`; `go test ./... -count=1` | Whole repository after Phase07D probe addition | No vet, build, or test failures | All commands exit 0 | No regression outside Phase07D | passed 2026-05-17 by Codex |
| Residue gate | `git status --short --untracked-files=all` after planning and tests | Worktree after this planning run | Intended tracked planning files are modified; the new Phase07D folder exists on disk but is ignored by `.gitignore`; unrelated dirty files are preserved | No generated junk, no server left running, no user data mutation | Planning-state discipline | passed 2026-05-17 with unrelated `cmd/debug_ingest/main.go` and untracked encoded docs path left untouched |

## Metrics And Thresholds

| Metric | Target | Current |
| --- | --- | --- |
| Targeted package test wall time | Under 30s on local dev machine | 7.1s observed for the passing combined Phase07D command |
| User recall max returned rows | `limit` capped at 20 | enforced in `recall_user_memory.go` |
| Operational recall max returned rows | `limit` capped at 20 | enforced in `recall_operational.go` |
| Live E2E reply capture | Full assistant/tool output plus durable SQLite facts | passed via `cmd/probe_chat -case phase07d-mixed-tier-recall`: `pass=true`, `tool_calls=2`, `llm_calls=2`, approved tokens only |
| Cross-tier leakage | 0 pending, raw failure, archive, source, proposal, or wrong typed-tier rows through wrong recall surface | passed in disposable `cmd/aura` mixed-tier direct tool golden and live `cmd/probe_chat` chat-level fixture |

## Deferred

- Per-tier projection freshness rows are deferred; current tools use the
  Phase07C compact projection freshness row.
