# Phase07D Progress

| Date | Actor | Change | Verification | Blockers | Deviations from plan |
| --- | --- | --- | --- | --- | --- |
| 2026-05-17 | Codex | Created the canonical Phase07D planning folder and mapped current user/operational memory code evidence into Aura `source.md`, `plan.md`, and `benchmark.md`. | `go test ./internal/storage/memoryindex ./internal/learning ./internal/agent/tools/registry -run "Test(CollectionConstants\|KindBackCompat\|ScaffoldedCollections\|WriteApprovedLesson\|WriteApprovedUserFact\|RecallUserMemoryTool\|RecallOperationalTool\|ExamplesParameterEval)" -count=1` passed. | No code blocker found. Live mixed-tier `cmd/probe_chat` fixture and fresh independent verifier not run. | Existing Phase-N/O/Q code already covers much of Phase07D, so this is a planning/closure-candidate folder, not new implementation. |
| 2026-05-17 | Codex | Hardened Phase07D closure evidence with deterministic tests for broad-scope exclusion and runtime recall/freshness wiring. Extracted memory recall registration in `cmd/aura/app_wire.go`, added `cmd/aura/app_wire_test.go`, and added `TestSearchMemoryToolRejectsTypedTierScopes`. | Baseline Phase07D command passed before edits. Added tests passed: `go test ./internal/agent/tools/registry -run "Test(SearchMemoryTool(UsesOneCompactSearchForAllScope\|RejectsTypedTierScopes)\|RecallUserMemoryTool\|RecallOperationalTool\|ExamplesParameterEval)" -count=1`; `go test ./cmd/aura -run TestRegisterMemoryRecallToolsWiresTypedTiersAndFreshness -count=1`; combined Phase07D gate with `./cmd/aura` passed; `go test ./cmd/aura ./internal/agent/tools/registry -count=1` passed; `go vet ./...`, `go build ./...`, and `go test ./... -count=1` passed. | Fresh independent verifier and full `cmd/probe_chat` mixed-tier golden fixture still not run. | No live user data was mutated. Direct disposable SQLite tool probes now cover approved user/operational handles plus shared compact projection freshness/degraded annotations; full chat-level eval remains deferred. |
| 2026-05-17 | Codex | Strengthened the runtime wiring test into a disposable mixed-tier golden fixture: approved and pending user/operational rows plus source/archive/proposal trap rows share the same query token. The test asserts typed recall only returns its approved tier and broad `search_memory(scope=all)` excludes typed memory. | `go test ./cmd/aura -run TestRegisterMemoryRecallToolsWiresTypedTiersAndFreshness -count=1`; `go test ./internal/agent/tools/registry -run "Test(SearchMemoryTool(UsesOneCompactSearchForAllScope\|RejectsTypedTierScopes)\|RecallUserMemoryTool\|RecallOperationalTool\|ExamplesParameterEval)" -count=1`; `go test ./cmd/aura ./internal/agent/tools/registry -count=1`; combined Phase07D gate with `./cmd/aura` passed. | Fresh independent verifier and full live/chat `cmd/probe_chat` reply capture still not run. | Kept the golden at direct tool level to avoid mutating `data/aura.db` or live user/runtime data. |
| 2026-05-17 | Codex | Closed Phase07D with a live `cmd/probe_chat` mixed-tier recall probe and repo gates. Added `phase07d-mixed-tier-recall`, which seeds temporary live compact-memory rows, forces `/api/chat` through `recall_user_memory` and `recall_operational`, verifies approved tokens plus `tool_attempts`, and cleans up fixture rows. | Live probe passed in Compose with `pass=true`, `tool_calls=2`, `llm_calls=2`, no mismatches. Local gates passed: `go test ./cmd/probe_chat ./cmd/aura ./internal/agent/tools/registry -count=1`; combined Phase07D gate with `./cmd/probe_chat`; `go vet ./...`; `go build ./...`; `go test ./... -count=1`. | None for Phase07D. Per-tier projection freshness rows remain a future projection slice. | A temporary E2E bearer token was minted through `cmd/seed_e2e_env`; no raw token is recorded in planning docs. |
