# Phase01C Progress

| Date | Actor | Change | Verification | Blockers | Deviations From Plan |
| --- | --- | --- | --- | --- | --- |
| 2026-05-15 | Codex | Recreated clean Phase01C scaffold after phase-folder reset. | Local file contract only. | Needs targeted source audit and verifier. | Prior verification intentionally not inherited. |
| 2026-05-16 | Codex | Closed Phase01C durable question gate E2E: `chat_questions` lifecycle, `question_requested` / `question_answered` persistence, ask_user exclusive pause, restart-safe pending question resume for Telegram, duplicate/wrong-channel answer rejection, and phase docs promoted from scaffold to closure record. | Named migration/store/hub/agent/Telegram benchmarks passed; Phase01C package gate passed; `go build ./...`, `go vet ./...`, and final `go test ./... -count=1` passed; compose test-profile Phase01C package gate passed; production `aura:local` rebuilt/restarted healthy with `/health` alive, `/api/health` 401 unauthenticated, and clean fresh logs. See `benchmark.md`. | Superseded by follow-up falsification row below. | Live external Telegram API roundtrip was not used; deterministic adapter tests plus production bot startup are the channel evidence. Fine-grained per-tool approval policy for all destructive tools remains a later tool/runtime hardening layer over the closed question primitive. |
| 2026-05-16 | Codex | Re-ran Phase01C as a falsification instead of accepting "Aura responds": live web `ask_user` probe first failed the canonical `chat_questions` ground truth because `/api/chat` had an empty `ThreadID`; fixed web thread id derivation and ask_user sentinel logging. | `go test ./internal/channels/web -count=1`; `go test ./internal/agent/tools/registry -run TestRegistryExecuteAskUserSentinelIsNotWarn -count=1`; `go test ./internal/db/migrations ./internal/storage/runs ./internal/chat ./internal/agent ./internal/agent/tools/registry ./internal/channels/telegram ./internal/channels/web -count=1`; rebuilt/restarted `aura:local`; final live `cmd/probe_chat` ask_user probe produced `chat_questions` row `a21b8513|b71e2677b9683e41|web:1148481707|web|approval|waiting|...|waiting_for_user`; token revoked. Telegram packages and fixtures passed via `go test ./internal/telegram ./internal/channels/telegram ./internal/channels/telegram/fixture -count=1`. | No Phase01C blocker after fix. External Telegram chat roundtrip still lacks a scripted fixture; container env does not expose `TELEGRAM_BOT_TOKEN`, though recent Telegram runs exist in the live DB. Unrelated Qdrant tool-vector dimension warnings remain outside this phase. | Corrected the closure claim: prior pipe evidence only proved chat stack responsiveness. The new evidence proves the question gate's durable DB contract on the live web pipe and deterministic Telegram adapter/resume contract. |
| 2026-05-16 | Codex | Pushed Phase01C closure and verified CI for the pushed commit. | PASS: commit `ecb4cf3e fix(chat): close Phase01C question gate` is on `origin/master`; GitHub Actions run `25958870299` passed `Frontend build` and `Go test + Phase 2 guards`. | None. | CI verification is now part of the closure evidence; local tests do not stand alone. |
