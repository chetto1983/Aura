# Phase01B Progress

| Date | Actor | Change | Verification | Blockers | Deviations From Plan |
| --- | --- | --- | --- | --- | --- |
| 2026-05-15 | Codex | Recreated clean Phase01B scaffold after phase-folder reset. | Local file contract only. | Needs targeted source audit and verifier. | Prior verification intentionally not inherited. |
| 2026-05-15 | Codex | Promoted Phase01B from scaffold to source-audited implementation plan for Phase01B1 schema + identity package. | Local source audit and external primary-source audit; no code tests run because no Phase01B code edited. | Needs fresh implementation verifier before being marked complete; no subagent verifier spawned this turn. | Chose schema/identity foundation as first slice before Telegram/API/Cron/Swarm rewiring. |
| 2026-05-15 | Codex | Implemented Phase01B1: migration v7 identity/capability tables, non-breaking `runs.actor_id`, and `internal/identity` default-deny authorizer. | Passed migration/identity targeted tests, targeted build/vet, `go build ./...`, `go vet ./...`, and `go test ./...`. | Needs fresh verifier before Phase01B is called complete; no subagent verifier spawned this turn. | Did not rewire Telegram/API/cron/swarm; Phase01B2 remains separate. |
| 2026-05-15 | Codex + subagents | Closed Phase01B1 after subagent loop. Repaired delegated actor grant inheritance, channel-account/principal mismatch, disabled-principal denial, parented-session inheritance, and polymorphic grant-subject validation. | PASS: `go test ./internal/identity`; `go test ./internal/db/migrations ./internal/identity`; `go build ./...`; `go vet ./...`; `go test -count=1 ./...`; goal verifier PASS 10/10; code-risk recheck PASS 9.5/10 with no B1 blockers. | None for Phase01B1. | Phase01B parent remains open; Telegram/API/tool/cron/swarm/denial-event wiring stays in B2-B7. |
| 2026-05-15 | Codex | Implemented Phase01B2 allowlist backfill: migration v8 backfills persisted `allowed_users` into deterministic Telegram principals, channel accounts, session actors, and owner/user grants; auth bootstrap/approval now creates the same identity rows while preserving `allowed_users`. | PASS: baseline `go test ./internal/db/migrations ./internal/api/auth ./internal/api ./internal/telegram ./internal/agent/tools/registry ./internal/agent/tools/sets ./internal/agent/tools/swarm ./internal/cron`; `go test ./internal/db/migrations ./internal/identity ./internal/api/auth`; repeated Phase01B benchmark; `go build ./...`; `go vet ./...`; `go test ./...`. | None for Phase01B2 local scope. | Did not wire dashboard actor context, tool capability enforcement, cron delegation, swarm delegation, or denial-event persistence; Phase01B3-B7 remain open. |
| 2026-05-15 | Codex | Added Phase01B3 benchmark planning rows for dashboard bearer actor context, body-user impersonation prevention, missing-grant denial, SQL evidence, and idempotent repeated setup. | Documentation-only verification: planned rows added to `benchmark.md`; no Go code or tests executed. | Phase01B3 implementation still not started; missing authenticated `api.chat` grant benchmark now expects `403 Forbidden`, with `401 Unauthorized` reserved for missing/invalid bearer authentication. | Benchmark planning only. Cron delegation, swarm delegation, tool required-capability metadata, and denial-event persistence remain Phase01B4-B7. |
| 2026-05-15 | Codex | Implemented Phase01B3 dashboard bearer actor context for `/chat`: bearer middleware now attaches deterministic Telegram session actor ID, authenticated chat rejects body `user_id` impersonation, and `api.chat` authorization is enforced before chat execution. | PASS: `go test ./internal/api ./internal/api/auth`; `go test ./internal/...`; `go build ./...`; `go vet ./...`; `go test ./...`. | None for Phase01B3 local scope. | Did not implement tool capability checks, cron delegation, swarm delegation, or denial-event run/audit wiring; those remain Phase01B4-B7. |
| 2026-05-15 | Codex | Upgraded Phase01B3 from local verification to container E2E verification. Rebuilt `aura:local`, recreated only the `aura` container, and probed live `/api/chat` with the mounted E2E bearer token. | PASS: `docker compose build aura`; `docker compose up -d --no-deps aura`; container health `healthy`; `/health=200`; unauth `/api/health=401`; bearer `/api/health=200`; unauth `/api/chat=401`; bearer mismatched `user_id` `/api/chat=403`; bearer chat reply `AURA_E2E_PHASE01B3_OK`, client latency 2829 ms, API latency 2635 ms, `llm_calls=1`, `tool_calls=0`, `tokens=8654`; SQLite latest `api.chat` authz decision is `allow|api.chat|api|chat|active_grant`. | None for Phase01B3 container scope. | Did not revoke live grants to simulate missing-grant denial in production data; that failure mode remains covered by targeted Go tests to avoid destructive live DB mutation. |
| 2026-05-15 | Codex | Implemented Phase01B4 tool capability checks: tool definitions can declare `RequiredCapability`, registry execution authorizes `tool.execute` or a tool-specific capability before `Tool.Execute`, and dashboard chat now carries the identity authorizer into the agent path. This row is superseded by the Phase01B fail-closed repair that removes the former unauthenticated registry execution path. | PASS: baseline `go test ./internal/db/migrations ./internal/api/auth ./internal/api ./internal/telegram ./internal/agent/tools/registry ./internal/agent/tools/sets ./internal/agent/tools/swarm ./internal/cron`; `go test ./internal/identity`; `go test ./internal/agent/tools/registry`; `go test ./internal/api ./internal/api/auth`; `go test ./internal/agent ./internal/telegram ./internal/channels/telegram`; `go test ./internal/...`; `go build ./...`; `go vet ./...`; `go test ./...`; `docker compose build aura`; `docker compose up -d --no-deps aura`; health `healthy`; `/health=200`; unauth `/api/health=401`; bearer `/api/health=200`; bearer `/api/chat` reply `AURA_E2E_PHASE01B4_OK`, `llm_calls=1`, `tool_calls=0`; containerized registry fail-closed tests passed via `docker compose run --rm test go test ./internal/agent/tools/registry -run "TestRegistryExecuteRequiresIdentityGrant|TestRegistryExecuteUsesToolSpecificCapability" -count=1`. | None for Phase01B4. | Did not wire configured Telegram actor parity, cron delegation, swarm delegation, or run/audit denial events; those remain Phase01B5-B7. |
| 2026-05-15 | Codex | Implemented Phase01B5 Telegram identity grant parity: `/start` and `/login` ensure deterministic Telegram principal, channel account, session actor, and grants before token issuance; configured allowlist users remain config-only and are not inserted into `allowed_users`; persisted bootstrap/approved users repair identity from stored source. Registered `D:/tmp/cli-printing-press` as an Aura reference map. | PASS: `go test ./internal/api/auth -run "TestEnsureTelegramAllowlistedIdentity|TestBackfillAllowedUserIdentitiesMigratesExistingRows|TestBootstrapUser_ClaimsEmptyAllowlist" -count=1`; `go test ./internal/telegram -run "TestOnLogin|TestApproveAccessCreatesIdentityBeforeSendingToken|TestIsAllowlisted|TestCollectOwnerIDs" -count=1`; `go test ./internal/identity ./internal/api/auth ./internal/telegram -count=1`; baseline Phase01B packages; `go test ./internal/... -count=1`; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; `docker compose build aura`; `docker compose up -d --no-deps aura`; container health `healthy`; `/health=200`; unauthenticated `/api/health=401`; unauthenticated `/api/chat=401`. | Live Telegram fixture and bearer chat marker unavailable in this shell because `AURA_E2E_TOKEN` is not set. | Did not implement cron delegation, swarm delegation, or run/audit denial events; those remain Phase01B6-B7. |
| 2026-05-15 | Codex | Repaired Phase01B6 planning readiness: updated the execution index from B5 to B6, added a B6 slice contract, source rows for cron Hub/manual paths and swarm manager actor propagation, and concrete benchmark rows for delegated actor SQL evidence. | Planning repair only; no code or Go tests run yet. | Needs implementation and verification against the new Phase01B6 benchmark rows. | Kept Phase 8 schedule-fire/RunGraph redesign and Phase01B7 denial-event persistence out of B6. |
| 2026-05-15 | Codex + plan verifier | Repaired Phase01B6 verifier findings: added explicit benchmark coverage for `internal/channels/cron/loop.go`, added composition-root source/benchmark coverage for `cmd/aura/app.go`, and added B6-specific stop conditions. | Plan verifier reported no BLOCKER findings but two MAJOR coverage gaps before this repair; baseline `go test ./internal/db/migrations ./internal/api/auth ./internal/api ./internal/telegram ./internal/agent/tools/registry ./internal/agent/tools/sets ./internal/agent/tools/swarm ./internal/cron -count=1` passed. | Implementation still not started. | Accepted the verifier's MAJOR findings instead of narrowing B6 by dropping cron Hub or composition-root wiring. |
| 2026-05-15 | Codex | Implemented Phase01B6 delegated actors: `identity.DelegateActor` creates bounded child actors/grants after parent authorization; cron `RunNow` and cron Hub jobs run under delegated `cron` actors when authority is present; swarm tools require `swarm.spawn` and manager worker execution runs under delegated `swarm` actors; `cmd/aura` wires the shared auth store into cron delegation. | PASS: `go test ./internal/identity -run TestDelegateActor -count=1`; `go test ./internal/cron -run "TestRunNowDelegatesCronActor|TestRunNowDelegationRejectsMissingParentGrant" -count=1`; `go test ./internal/channels/cron -run TestCronAgentLoopDelegatesActorContext -count=1`; `go test ./internal/agent/tools/swarm ./internal/swarm -run "Test.*DelegatedActor|Test.*SwarmSpawnCapability" -count=1`; `go test ./cmd/aura -run TestAuthStoreImplementsDelegationInterfaces -count=1`; Phase01B baseline gate; `go test ./internal/identity ./internal/cron ./internal/channels/cron ./internal/agent/tools/swarm ./internal/swarm ./cmd/aura -count=1`; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; `docker compose build aura`; `docker compose up -d --no-deps aura`; container health `healthy`; `/health=200`; unauthenticated `/api/health=401`. | No B6 blocker. CI not checked because this slice has not been committed or pushed yet. | Did not implement Phase 8 schedule-fire/RunGraph semantics and did not wire Phase01B7 denial events. |
| 2026-05-15 | Codex | Repaired Phase01B7 planning readiness: added a B7 slice contract for metadata-only authorization denial run/audit events, named owned files, added local source rows for run store, chat Hub, registry, Telegram/Cron Hub wiring, and converted B7 benchmarks from generic planned checks into concrete SQL-backed gates. | Documentation-only verification: `HANDOFF.json` parses via `ConvertFrom-Json`; `git diff --check` reports line-ending warnings only. Fresh B7 verifier still needed before implementation is called ready. | B7 implementation not started. | Kept Phase 8 RunGraph/schedule-fire/topology work and full web-chat Hub migration out of B7. |
| 2026-05-15 | Codex | Implemented Phase01B7 authorization denial run/audit events: `identity.Authorize` now accepts run/event provenance from context, emits denial recorder callbacks only for denied decisions, `runs.Store` records metadata-only `authorization_denied` run and audit events, chat Hub attaches run provenance/denial recorder to agent-loop context, registry tool denials pass run provenance, and production Telegram/Cron Hubs use the shared run store. | PASS: `go test ./internal/storage/runs -run TestRecordAuthorizationDenial -count=1`; `go test ./internal/identity -run TestAuthorize_DenialRecorder -count=1`; `go test ./internal/chat -run TestReceiveMessage_WithLifecycleStoreRecordsAuthorizationDenial -count=1`; `go test ./internal/agent/tools/registry -run TestRegistryExecuteRecordsAuthorizationDenialEvent -count=1`; `go test ./cmd/aura -run TestChatHubLifecycleStoreWiring -count=1`; `rg -n "internal/storage/runs" internal/identity` returned no matches; Phase01B baseline gate; `go test ./internal/storage/runs ./internal/identity ./internal/chat ./internal/agent/tools/registry ./cmd/aura -count=1`; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; `docker compose build aura`; `docker compose up -d --no-deps aura`; container health `healthy`; `/health=200`; unauthenticated `/api/health=401`; unauthenticated `/api/chat=401`. | No B7 blocker. CI not checked because this slice has not been committed or pushed yet. | Did not implement Phase 8 RunGraph/schedule-fire/topology semantics and did not migrate web chat fully onto Hub. |
| 2026-05-15 | Codex | Removed Phase01B fail-open authority paths: registry execution now denies when no authorizer is present; cron manual jobs, cron Hub jobs, and swarm delegated assignments now fail before runner execution when identity delegation is missing; app startup no longer falls back to a cron agent dispatch path if cron Hub creation fails. | PASS: `go test ./internal/agent/tools/registry -run "TestRegistryDefinitionsAndExecute|TestRegistryExecuteWithoutAuthorizerFailsClosed|TestRegistryExecuteRequiresIdentityGrant|TestRegistryExecuteRecordsAuthorizationDenialEvent" -count=1`; `go test ./internal/cron -run "TestRunNowDelegatesCronActor|TestRunNowDelegationRejectsMissingParentGrant|TestRunNowRequiresIdentityDelegation" -count=1`; `go test ./internal/channels/cron -run "TestCronAgentLoopDelegatesActorContext|TestCronAgentLoopRequiresIdentityDelegation" -count=1`; `go test ./internal/swarm -run "TestManagerDelegatesAssignmentActorContext|TestManagerDelegationRejectsMissingParentGrant|TestManagerDelegationRequiresIdentity" -count=1`; Phase01B baseline gate; shared Phase01B gate; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; `docker compose build aura`; `docker compose up -d --no-deps aura`; `aura-aura-1` healthy; `/health=200`; unauthenticated `/api/health=401`; unauthenticated `/api/chat=401`. | CI not checked because this slice has not been committed or pushed yet. | Initial broad tests exposed Telegram, execute-code, swarm-tool, and agent fixtures that still invoked tools without identity context; those fixtures were repaired to pass explicit test authority instead of reviving runtime fallback. |
| 2026-05-15 | Codex | Ran the Phase01B parent closure verifier and repaired missing Chat Hub actor persistence: `runs.actor_id` and durable lifecycle `run_events.actor_id` now inherit the actor from context for Hub-backed runs/events. | PASS: `go test ./internal/storage/runs ./internal/chat -count=1`; Phase01B target package gate; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; `docker compose build aura`; `docker compose up -d --no-deps aura`; `aura-aura-1` healthy; `/health=200`; unauthenticated `/api/health=401`; unauthenticated `/api/chat=401`; bearer `/api/health=200`; bearer mismatched `/api/chat=403`; live SQLite records `api.chat` allow decisions for `actor:telegram:session:1148481707`. | Live bearer `/api/chat` marker is blocked: endpoint returns `500` because the configured LLM provider returns `401 Missing Authentication header`; `secrets` contains `llm_api_key`, but the provider credential path is not producing a valid provider request. Phase01B parent is not complete until this marker rerun passes. | This verifier exposed and fixed a run/event actor persistence gap in Chat Hub. The remaining blocker is provider/config, not identity authorization. CI not checked because this slice has not been committed or pushed yet. |
| 2026-05-15 | Codex | Closed the Phase01B parent blocker by eliminating runtime `.env` dependence and repairing the DB-backed provider secret path. Secret-shaped dashboard keys now route to `secrets`, the live stale `settings.LLM_API_KEY` row was removed, no `.env*` file remains in `D:/Aura/data`, `cmd/chat` no longer sends a synthetic `chat-cli` user under bearer auth, and the container was rebuilt/restarted from DB/secrets state. | PASS: `go test ./internal/api -run TestSettings -count=1`; `go test ./cmd/chat -count=1`; `go test ./cmd/aura -run TestChatHubLifecycleStoreWiring -count=1`; `go test ./internal/secrets ./internal/config -count=1`; `go test ./internal/api ./cmd/aura ./internal/secrets ./internal/config -count=1`; Phase01B baseline package gate; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; live DB shows `llm_api_key` present and `settings.LLM_API_KEY` row count `0`; `docker compose build aura`; `docker compose up -d --no-deps aura`; image manifest `sha256:ea46cdf1d5eafb534e5494c501d14227d4a3d556e0cdfe4fc0ed281ecf6cb9b0`; `/health=200`; unauthenticated `/api/health=401`; bearer `/api/health=200`; `cmd/chat` exact `AURA_PIPE_OK`; bearer `/api/chat` exact `PHASE01B_CLOSE_OK`, `llm_calls=1`, `tool_calls=0`, `tokens=8790`; latest authz decision remains `allow|api.chat|api|chat|active_grant`. | None for Phase01B parent closure. | `cmd/probe_chat` reused thread context and echoed an earlier marker, so the exact closure evidence is the direct bearer `/api/chat` request plus `cmd/chat` pipe marker. CI not checked because this turn did not push. |
| 2026-05-15 | Codex | Removed the legacy direct web `/api/chat` runner: `internal/api/web_chat.go` was deleted, `cmd/aura` now wires `/api/chat` through `chat.Hub` plus the shared `runs.Store`, and Telegram Hub entrypoints now carry actor/authority context instead of `context.Background()`. | PASS: `go test ./cmd/aura ./internal/api ./internal/channels/web ./internal/telegram -count=1`; `go test ./internal/storage/runs ./internal/chat ./internal/channels/web ./internal/telegram ./internal/api ./cmd/aura -count=1`; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; source scan for `NewWebChatService`, `webChatService`, `agent.RunTask(ctx, deps, task)`, and `api.NewWebChatService` returned no matches in `cmd` or `internal`; `docker compose build aura`; `docker compose up -d --no-deps aura`; image `sha256:0dcb1cb74ba687b5e921121beda58782e27aff6762e97a9d8b781f00ef741139`; container healthy; `/health=200`; unauthenticated `/api/health=401`; temporary bearer `/api/chat` exact `WEB_HUB_LIVE_OK`, `llm_calls=1`, `tool_calls=0`, `tokens=9820`; SQLite latest `runs` row is `6983e1e41855db95|actor:telegram:session:1148481707|web|completed|WEB_HUB_LIVE_OK`; `run_events` count `4` with min/max actor `actor:telegram:session:1148481707`; temporary bearer revoked. | None for this repair. | Public `/api/chat` JSON shape is preserved by the adapter. The fix closes the two P1 findings from `docs/aura-interactive-debug-report-2026-05-15.md`; CI not checked because this turn did not push. |
| 2026-05-15 | Codex | Stabilized the Hub-backed web chat path after the legacy removal: web tool execution now carries the model-visible tool allowlist in context, `D:/tmp/w64devkit/bin/gcc.exe` and matching binutils are installed for CGO/race verification, and the rebuilt container passed a three-turn live stability probe. | PASS: `go test ./cmd/aura -run "TestWebToolExecutorCarriesVisibleToolContext|TestHubBackedWebChatPersistsRunAndActor" -count=1`; `go test ./internal/telegram -run TestTelegramHubContextCarriesActorAndAuthority -count=1`; `go test ./cmd/aura ./internal/api ./internal/channels/web ./internal/telegram ./internal/chat ./internal/storage/runs ./internal/agent/tools/registry -count=1`; `go vet ./...`; `go build ./...`; `go test ./... -count=1`; `go test -race ./cmd/aura ./internal/api ./internal/channels/web ./internal/telegram ./internal/chat ./internal/storage/runs -count=1` with `D:/tmp/w64devkit/bin` first in `PATH`; `docker compose build aura`; `docker compose up -d --no-deps aura`; image `sha256:e5cc463996ec1abc67077c690d83aa8898a2548c7cd0621f6e3df042db78eb77`; container healthy; `/health=200`; unauthenticated `/api/health=401`; live exact markers `WEB_STABLE_A`, `WEB_STABLE_B`, `WEB_STABLE_C`; SQLite run rows `a632a3749f93e881`, `53f595d4ad841334`, `f17712e30bbe9f17` all `web|completed` with actor `actor:telegram:session:1148481707`; each run has 4 events with the same actor; temporary bearer revoked; recent container logs contain no `warn`, `error`, `fatal`, or `panic` levels. | None for this stability pass. | User-level `Path` now starts with `D:/tmp/w64devkit/bin` and `go env CC/CXX` point to w64devkit; existing long-lived shells may still need a fresh process or explicit `PATH` prefix. CI not checked because this turn did not push. |
| 2026-05-16 | Codex | Repaired the legacy `agent.RunTask` run-correlation seam for cron and swarm: `RunTaskDeps` carries `RunID`; `RunTask` installs it into executor/tool context; cron `JobRequest` carries the delegated run ID; cron/swarm app adapters populate `deps.RunID`; cron and swarm delegated actor contexts preserve the parent run ID. Also fixed the full-gate daily briefing time fixture and classified `connection refused`/reset/network unreachable as recoverable I/O. | PASS: `go test ./internal/agent -run TestRunTaskDepsRunIDPropagatesToToolContext -count=1`; `go test ./cmd/aura -run "TestAgentJobRunnerAdapterPassesRequestRunID|TestSwarmRunnerAdapterPassesContextRunID" -count=1`; `go test ./internal/cron -run TestRunNowDelegatesCronActor -count=1`; `go test ./internal/channels/cron -run TestCronAgentLoopDelegatesActorContext -count=1`; `go test ./internal/swarm -run TestManagerDelegatesAssignmentActorContext -count=1`; `go test ./internal/agent ./cmd/aura ./internal/cron ./internal/channels/cron ./internal/swarm ./internal/agent/tools/registry -count=1`; `go build ./...`; `go vet ./...`; `go test ./... -count=1`; `go test -race ./internal/agent ./cmd/aura ./internal/cron ./internal/channels/cron ./internal/swarm ./internal/agent/tools/registry -count=1` with `D:/tmp/w64devkit/bin` first in `PATH`; `docker compose build aura`; `docker compose up -d --no-deps aura`; image `sha256:5b2d7496c2a8fb56c4b06d1bba7dc200266ded99885830191853c18215b7026a`; container healthy; `/health=200`; unauthenticated `/api/health=401`; recent container logs contain no `warn`, `error`, `fatal`, or `panic` levels. | None for this repair. | Live container precheck is health/auth-boundary only; RunID ground truth is covered by disposable SQLite/adapter fixtures because production cron/swarm run injection should not be simulated by destructive live DB mutation. CI not checked because this turn did not push. |
