---
context: spike
phase: adaptive-intelligence
task: 2
total_tasks: 6
status: in_progress
last_updated: 2026-07-24T15:09:31.955Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were learned through actual failures. Acknowledge each one
> explicitly before proceeding.

- [ ] Read `D:\Aura\AGENTS.md` and `D:\Aura\CLAUDE.md` completely before any action. Re-read `CLAUDE.md` if its commit changes.
- [ ] Do not describe the current hook/outbox as a functional learning loop. It is shadow observation plumbing only.
- [ ] Do not use Qwen3.5 2B or synthetic canary results as real-model or production evidence.
- [ ] Use WSL with `/home/davide/.local/bin:/home/davide/go/bin` prepended for every substantive Go/quality/integration command.
- [ ] Commit PRD amendments before code, make commits atomic, run normal hooks, and never use `--no-verify`.
- [ ] Never recreate the deleted legacy packages, knobs, scheduler kind, or stored graph labels.
- [ ] Verify migration slots from disk; do not infer that PostgreSQL `0060` or Neo4j `0005` is still free.
- [ ] Preserve the unified fail-safe invariant: unavailable/off/rollback/unsupported state serves static Aura behavior.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Observation presented as learning | The first hook logs served actions but lacks typed assignments, support, propensity, challengers, and cohorts. | blocking | Require an OPE-complete assignment contract and executable cohort tests before using “learning loop.” |
| Spike presented as production proof | Qwen 2B improved point estimates, but the unseen-family interval crossed zero and the model is not Aura production. | blocking | Bind every gate artifact to the real configured model ID and reject synthetic/spike subjects. |
| Partial lint called full lint | Cleanup-touched lint was green while full lint still had adaptive findings. | blocking | Run `golangci-lint run ./...` in WSL and capture exit 0 before completion. |
| Authorization inferred | Stored legacy data deletion was briefly treated as unauthorized despite explicit operator direction. | blocking | Read the latest user instruction and PRD scope; never narrow or expand authorization by assumption. |
| CRLF `.env` source treated as a code failure | Two live-test attempts produced invalid-control-character DSNs before reaching PostgreSQL. | advisory | Parse the password with explicit UTF-8 on Windows and pass DSNs through `WSLENV`; never print credentials. |
| Push race interpreted as failure | A concurrent push moved the remote to the intended commit and caused a ref-lock rejection. | advisory | Compare `origin/master`, `HEAD`, and the remote SHA before retrying a rejected push. |
| Stale graph report treated as current | `GRAPH_REPORT.md` records source `8ebc0717`, earlier than the adaptive commits. | advisory | Regenerate graphify before using it as current architecture evidence. |

<current_state>

Master and `origin/master` are clean at `6042c113e`.

The shipped checkpoint contains:

- Amendments #93, #93.1, #93.2, and #93.3.
- A PostgreSQL-authoritative adaptive event/outbox, identity tombstones, policy
  read/gate, promotion-statistics proof primitives, and a Neo4j projection
  schema.
- Shadow observation and atomic turn/outbox persistence.
- Complete deletion of the superseded reasoning/tool learner packages,
  learned reranking, dynamic classifier examples, compaction scheduler,
  configuration, observability, and legacy stored data.
- Removal of the unreachable Go `PolicyService`; owned-tree
  `deadcode -test ./...` is clean.

This is **not** the finished adaptive implementation.

The latest independent cleanup reviews returned:

- `SPEC_COMPLIANT`
- `APPROVED` with no Critical or Important cleanup defect

Latest GitHub Actions for `6042c113e` were still running:

- CI: `30104006958`
- CodeQL: `30104007075`
- Skills: `30104006995`

</current_state>

<completed_work>

Completed:

- `90a908f65` — root `AGENTS.md` permanently points every agent to the full
  canonical `CLAUDE.md`.
- `76099e691`, `73e57db34`, `66eaab23b` — ratified adaptive design,
  adversarial scope correction, and complete legacy decommission.
- `a4d123b71` — shipped the adaptive shadow foundation and legacy removal.
- `ddeb0902f` — re-attested all matched quality-snapshot rows.
- `9fea6eff4`, `ffe4a5b84` — ratified and removed dormant policy-service dark
  code.
- `5cbdae144`, `e31d32f5f` — refreshed generated adaptive SQL models and added
  graph-purge adapter coverage.
- `6042c113e` — committed the requested graph report/status pair.

Live persistence proof:

- PostgreSQL `public.schema_migrations`: version `59`, dirty `false`.
- Neo4j migrations `0001` through `0004`: no pending migration.
- `learning_compaction` task/run counts: zero.
- Retired settings counts: zero.
- `ReasoningExample`, `ReasoningSeed`, `ToolSelectionExample`, and
  `ToolSelectionSeed` node count: zero.
- A unique-fixture live Neo4j test proved migration `0004` deletes all four
  retired labels while preserving one `User`, one `AdaptiveEpisode`, and one
  `AdaptiveEvent`, then leaks zero fixtures.

Verification captured in WSL:

- Full `golangci-lint run ./...`: `0 issues`.
- `go vet ./...`: exit 0.
- `go build ./...`: exit 0.
- Affected unit and race suites: exit 0.
- Live PostgreSQL/Neo4j integration suites for the adaptive cleanup: exit 0.
- Raw policy DML denial live test: exit 0.
- Owned `deadcode -test ./...`: zero-byte findings file.
- File-size gate: all tracked source files at or below 600 LOC.
- Pre-push quality snapshot/build/deadcode/web gates: green.

</completed_work>

<remaining_work>

1. Check the three outstanding GitHub Actions runs before editing.
2. Correct remaining target-language overclaims in the design as the next
   implementation contract lands.
3. Write PRD Amendment #93.4 before code. It must define an OPE-complete
   assignment/outcome envelope:
   - point-unique decision/assignment ID;
   - domain and decision point;
   - policy epoch/version and immutable snapshot ID;
   - champion, candidate set/support, selected candidate, and served action;
   - non-zero behavior propensity and explicit no-op arm;
   - model/provider identity and cohort identity;
   - outcome, evaluator provenance/calibration, censoring, harm, latency,
     tokens, and cost linkage.
4. Implement typed adapters for reasoning, tool, skill, knowledge, memory,
   model/provider, context, document, multimodal, and retry/recovery decisions.
5. Fix multi-round reasoning so one request cannot emit different assignments
   under a duplicated DecisionID.
6. Implement immutable cohort reconstruction with duplicate/poison,
   overlap/support, propensity, censoring, and provenance validation.
7. Implement the production projector lifecycle and private graph projection;
   current `Projector` has no production composition call site.
8. Implement frozen graph-kNN/champion snapshots and shadow disagreement.
9. Reintroduce policy mutation only with a real capability-gated CLI/API
   production caller and same-slice audit evidence; do not resurrect the
   deleted test-only service.
10. Implement safe canary serving and distributed rollback without granting
    capabilities or exploring destructive actions.
11. Benchmark Aura's actual configured model. Qwen3.5 2B remains spike-only.
12. Close full-matrix coverage >=85%, mutation >=70% on critical adaptive
    files, real scenario score >9.8, adversarial review, and explicit operator
    activation approval.

</remaining_work>

<decisions_made>

- PostgreSQL is authoritative; Neo4j and the deployed agent-memory
  infrastructure are a replayable private analytical projection.
- Adaptive payloads do not go through LLM-facing memory tools.
- Qwen3.5 2B Q4_K_M under llama.cpp proves portability and measurement only.
- The `+0.1679` held-out point gain did not pass the family-bootstrap gate:
  95% interval `[-0.0168, +0.3198]`.
- The seeded canary artifact is `VALIDATED_GATE_NOT_MODEL` with
  `subject_model_id=synthetic-model-not-production`.
- Production remains static/off-or-shadow until real-model evidence passes.
- Legacy stored data was explicitly authorized for deletion and is gone.
- Test-only reachability is dark code; mutation functionality returns only
  with a real production caller.

</decisions_made>

<blockers>

- Technical: generic events are not OPE-ready.
- Technical: no typed per-domain assignment adapters.
- Technical: no durable immutable cohort loader.
- Technical: no production-composed projector.
- Technical: no graph challenger/snapshot/disagreement implementation.
- Technical: no production policy administration or learned-action canary
  serving.
- External: final-head GitHub Actions were still running at pause.
- Deployment: the healthy `aura` container and `aura:local` image were created
  at `2026-07-24T10:02Z`, before `a4d123b71`; migrations are live, but the new
  binary is not deployed.
- Human gate: future activation requires explicit operator approval after real
  model proof.

</blockers>

## Required Reading (in order)

1. `D:\Aura\AGENTS.md` — forces canonical repository-rule loading.
2. `D:\Aura\CLAUDE.md` — binding PRD-first, WSL, lint, verification, quality,
   atomic-commit, dark-code, and push rules.
3. `D:\Aura\prd.md` — Amendments #93 through #93.3.
4. `D:\Aura\docs\superpowers\specs\2026-07-24-aura-adaptive-intelligence-design.md`
   — target design and explicit production blockers.
5. `D:\Aura\.planning\spikes\102-adaptive-generalization-ope-gate\README.md`
   — Qwen llama.cpp/OPE proof and failed unseen-family gate.
6. `D:\Aura\.planning\spikes\103-adaptive-runner-outbox-proof\README.md`
   — runner/outbox contract.
7. `D:\Aura\.planning\spikes\104-adaptive-identity-distributed-safety\README.md`
   — deletion, fencing, projection, and distributed safety.
8. `D:\Aura\.planning\spikes\105-adaptive-quality-canary-gate\README.md`
   — seeded statistical gate; not model proof.
9. `D:\Aura\internal\adaptive\event.go`,
   `D:\Aura\internal\adaptive\hook.go`,
   `D:\Aura\internal\adaptive\policy.go`,
   `D:\Aura\internal\adaptive\projector.go`,
   `D:\Aura\internal\runner\adaptive_persist.go` — current executable boundary.
10. `D:\Aura\docs\aura-quality-snapshot.md` — phase-close re-attestation
    contract.
11. `.planning/METHODOLOGY.md` if present — apply its analytical lenses before
    assumptions.

## Critical Anti-Patterns (do NOT repeat these)

- Never infer authorization or narrow explicit deletion scope.
- Never call a point estimate a win when its preregistered interval gate fails.
- Never equate OpenRouter proof, llama.cpp proof, or Qwen proof with real-model
  production proof.
- Never report a component-level projector test as a running projection
  service.
- Never count only cleanup-touched lint when full lint is required.
- Never source the CRLF `.env` into WSL without a proven sanitizer; pass
  credential-derived DSNs without printing them.
- Never commit generated graph output as current-head evidence when its header
  names an older commit.

## Infrastructure State

- Docker stack is running.
- `aura`, PostgreSQL, Neo4j, agent-memory MCP, llama embedding, reranker, PIM,
  WhatsApp, STT, TTS, and SearXNG were healthy/running at pause.
- PostgreSQL port: `127.0.0.1:5432`.
- Neo4j Bolt ports: `127.0.0.1:7687` and `127.0.0.1:17687`.
- Agent-memory MCP: `127.0.0.1:8091`.
- Current Aura container matches the current `aura:local` tag, but both image
  and container predate the adaptive commits. Rebuild is still required before
  deployed-code claims.
- No `.planning/async-jobs/*.json` manifests exist.

## Pre-Execution Critique Required

- Design artifact:
  `docs/superpowers/specs/2026-07-24-aura-adaptive-intelligence-design.md`
- Critique focus:
  - point-unique assignments and multi-round reasoning;
  - support/propensity/no-op correctness;
  - immutable cohort and evaluator provenance;
  - typed tool/skill/knowledge/memory actions;
  - production projector lifecycle;
  - graph snapshot immutability and privacy;
  - canary rollback and destructive-action exclusion.
- Gate: do not start the next code slice until Amendment #93.4 is committed
  PRD-first and an adversarial reviewer accepts its machine-checkable contract.

<context>

The cleanup checkpoint is proven and pushed, but the operator explicitly
corrected any implication that the adaptive implementation was finished.
Resume as an implementation continuation, not a cleanup close-out. Use small
dependency-ordered slices and keep static Aura behavior as the champion.

</context>

<next_action>

Start with:

1. `git status --short` and `git log -1 --oneline --decorate` — require clean
   `6042c113e`.
2. Inspect GitHub runs `30104006958`, `30104007075`, `30104006995`.
3. Read the required documents above.
4. Draft Amendment #93.4 for OPE-complete typed assignments and first-domain
   adapters.
5. Send that amendment to an adversarial agent before code, then commit it
   atomically with normal hooks.

</next_action>
