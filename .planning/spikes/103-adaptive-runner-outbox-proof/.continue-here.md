---
context: spike
phase: adaptive-intelligence
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-07-24T15:57:05.167Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These constraints were learned through actual failures. Acknowledge each one
> explicitly before proceeding.

- [ ] Read `D:\Aura\AGENTS.md` and `D:\Aura\CLAUDE.md` completely before
  planning, commands, edits, delegation, review, or commits. Re-read
  `CLAUDE.md` if it changes.
- [ ] Do not describe the current hook/outbox as a functional learning loop.
  It is shadow observation plumbing only.
- [ ] Do not use Qwen3.5 2B or synthetic canary results as real-model or
  production evidence.
- [ ] Use WSL with
  `/home/davide/.local/bin:/home/davide/go/bin` prepended for every
  substantive Go, quality, integration, and E2E command.
- [ ] Commit PRD amendments before code, keep every commit atomic, run normal
  hooks, and never use `--no-verify`.
- [ ] Never recreate the deleted legacy packages, knobs, scheduler kind, or
  stored graph labels. Their deletion was explicitly authorized.
- [ ] Verify migration slots from disk immediately before allocation. At
  pause, the disk tails are PostgreSQL `0059_decommission_legacy_learning` and
  Neo4j `0004_decommission_legacy_learning`; do not assume the next numbers
  remain free.
- [ ] Preserve the fail-safe invariant: unavailable, off, rollback, poisoned,
  unsupported, or unproven state serves static Aura behavior.
- [ ] Qwen llama.cpp Aura E2E may override the model through the database only.
  The override must be temporary, owner-scoped where applicable, and restored
  with before/after proof. Qwen is a spike model, not the real model.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention mechanism |
|---|---|---|---|
| Observation presented as learning | The hook logs served actions but lacks typed assignments, support, propensity, challengers, and cohorts. | blocking | Require an OPE-complete assignment contract and executable cohort tests before using "learning loop." |
| Spike presented as production proof | Qwen 2B improved point estimates, but its unseen-family interval crossed zero and it is not Aura production. | blocking | Bind every production gate artifact to the actual configured model ID and reject synthetic or spike subjects. |
| Reasoning-only scope | Earlier framing underrepresented tool, skill, memory, knowledge, model, context, document, multimodal, and recovery adaptation. | blocking | Make the PRD contract enumerate and type every supported adaptive decision domain. |
| Partial lint called full lint | Cleanup-touched lint was green while full lint still had adaptive findings. | blocking | Run `golangci-lint run ./...` in WSL and capture exit 0 before completion. |
| Authorization inferred | Stored legacy data deletion was briefly treated as unauthorized despite explicit operator direction. | blocking | Read the latest user instruction and PRD scope; never narrow or expand authorization by assumption. |
| Dormant API shipped for tests | The first policy mutation service had no production caller and was detected by deadcode. | blocking | Mutation returns only with a real capability-gated production caller and same-slice audit proof. |
| CRLF `.env` sourced in WSL | Two live-test attempts produced invalid-control-character DSNs before reaching PostgreSQL. | advisory | Pass credential-derived DSNs through `WSLENV` without printing secrets; do not source the Windows `.env` directly. |
| Superseded CI treated as failure | Runs for `6042c113e` were cancelled when the later WIP handoff push superseded them. | advisory | Judge the current head; `29ffcbbcb` has green CI, CodeQL, and Skills runs. |
| Stale graph report treated as current | `GRAPH_REPORT.md` records source `8ebc0717`, earlier than the adaptive commits. | advisory | Regenerate graphify before using it as current architecture evidence. |

<current_state>

The implementation checkpoint, `origin/master`, and `origin/HEAD` are at:

```text
29ffcbbcb wip: adaptive intelligence paused at shadow foundation
```

This handoff is committed locally as one additional atomic WIP commit and is
not pushed. On resume, `master` should therefore be clean and one commit ahead
of `origin/master`; use `git log -1` to identify the handoff commit hash.

The current-head workflows are complete and green:

- CI `30104349551`: success.
- CodeQL `30104349617`: success.
- Skills `30104349547`: success.

The older `6042c113e` CodeQL run succeeded; its CI and Skills runs were
cancelled only because the `29ffcbbcb` push superseded them.

The checkpoint contains:

- Amendments #93, #93.1, #93.2, and #93.3.
- A PostgreSQL-authoritative adaptive event/outbox, identity tombstones,
  policy read/gate, promotion-statistics proof primitives, and a Neo4j
  projection schema.
- Shadow observation and atomic turn/outbox persistence.
- Complete deletion of the superseded reasoning/tool learner packages,
  learned reranking, dynamic classifier examples, compaction scheduler,
  configuration, observability, and legacy stored data.
- Removal of the unreachable Go `PolicyService`; owned-tree
  `deadcode -test ./...` was clean.

This is **not** the finished adaptive implementation. The current event hook is
observation plumbing and static Aura behavior remains the champion.

Runtime state changed after the earlier handoff:

- The `aura` container is restarting with exit `71` and is unhealthy.
- The exact terminal error is:
  `migration tracker incompatible: version=59 dirty=false want=51`.
- Cause: the `aura:local` image was created at
  `2026-07-24T10:02:01Z`, before the adaptive migration commits. The live
  database is newer than the stale binary.
- PostgreSQL, Neo4j, agent-memory MCP, llama embedding, reranker, PIM,
  SearXNG, STT, TTS, Garage, and WhatsApp were running; health-bearing
  supporting services were healthy at pause.
- Do not rebuild merely to hide this handoff condition. Rebuild/recreate Aura
  after the next deployable slice passes its required local gates.

</current_state>

<completed_work>

Completed checkpoint commits:

- `90a908f65` - root `AGENTS.md` permanently points every agent to the complete
  canonical `CLAUDE.md`.
- `76099e691`, `73e57db34`, `66eaab23b` - ratified adaptive design,
  adversarial scope correction, and complete legacy decommission.
- `a4d123b71` - shipped the adaptive shadow foundation and legacy removal.
- `ddeb0902f` - re-attested all matched quality-snapshot rows.
- `9fea6eff4`, `ffe4a5b84` - ratified and removed dormant policy-service dark
  code.
- `5cbdae144`, `e31d32f5f` - refreshed generated adaptive SQL models and added
  graph-purge adapter coverage.
- `6042c113e` - committed the requested graph report/status pair.
- `29ffcbbcb` - committed the prior pause handoff and is the current green
  remote checkpoint.

Last live persistence proof:

- PostgreSQL `public.schema_migrations`: version `59`, dirty `false`.
- Neo4j migrations `0001` through `0004`: no pending migration.
- `learning_compaction` task/run counts: zero.
- Retired settings counts: zero.
- `ReasoningExample`, `ReasoningSeed`, `ToolSelectionExample`, and
  `ToolSelectionSeed` node count: zero.
- A unique-fixture Neo4j test proved migration `0004` deletes all four retired
  labels while preserving `User`, `AdaptiveEpisode`, and `AdaptiveEvent`, then
  leaks zero fixtures.

Verification captured at the checkpoint in WSL:

- Full `golangci-lint run ./...`: `0 issues`.
- `go vet ./...`: exit 0.
- `go build ./...`: exit 0.
- Affected unit and race suites: exit 0.
- Live PostgreSQL/Neo4j integration suites for adaptive cleanup: exit 0.
- Raw policy DML denial live test: exit 0.
- Owned `deadcode -test ./...`: no findings.
- File-size gate: all tracked source files at or below 600 LOC.
- Pre-push quality snapshot, build, deadcode, and web gates: green.
- Current-head GitHub CI, CodeQL, and Skills: green.

The phase-summary placeholder scan found no `To be filled` or `TBD` markers.
Matches for the word `placeholder` were descriptive historical text, not
incomplete summary templates.

</completed_work>

<remaining_work>

1. Ratify PRD Amendment #93.4 before code. It must define a typed,
   OPE-complete assignment/outcome envelope:
   - point-unique assignment/decision ID;
   - domain and decision point;
   - policy epoch/version and immutable snapshot ID;
   - champion, candidate set/support, selected candidate, and served action;
   - nonzero behavior propensity and explicit no-op arm;
   - model, provider, owner/cohort, and request/turn identity;
   - outcome, evaluator provenance/calibration, censoring, harm, latency,
     tokens, and cost linkage.
2. Send #93.4 to a dedicated adversarial agent before implementation. Every
   delegated prompt must first require full reading of `D:\Aura\CLAUDE.md`.
3. TDD typed adapters for all approved domains:
   - reasoning;
   - tool;
   - skill;
   - knowledge;
   - memory;
   - model/provider;
   - context;
   - document;
   - multimodal;
   - retry/recovery.
4. Fix multi-round reasoning so distinct assignment points cannot share a
   duplicated `DecisionID`.
5. Implement immutable cohort reconstruction with duplicate/poison,
   overlap/support, propensity, censoring, and provenance validation.
6. Compose the projector in the production lifecycle. The current `Projector`
   has no production call site.
7. Reuse Neo4j and the existing agent-memory infrastructure as a private,
   replayable projection; do not send adaptive payloads through LLM-facing
   memory tools.
8. Implement frozen graph-kNN/champion snapshots and shadow disagreement.
9. Reintroduce policy mutation only with a real capability-gated CLI/API
   production caller and same-slice authorization/audit evidence.
10. Implement safe canary serving and distributed rollback without granting
    capabilities or exploring destructive actions.
11. Run Aura E2E against Qwen3.5 2B Q4_K_M through llama.cpp using only a
    temporary database model override. Capture the original rows, the exact
    override, E2E evidence, restoration, and post-restore equality. This proves
    the non-OpenRouter path only.
12. Benchmark Aura's actual configured production model with preregistered
    cohorts and real-model-bound artifacts. Qwen 2B remains spike-only.
13. Rebuild/recreate Aura after a deployable slice, prove migration
    compatibility and health, then run the real scenario.
14. Close full-matrix coverage `>=85%`, mutation `>=70%` on critical adaptive
    files, real scenario score `>9.8`, adversarial review, quality-snapshot
    re-attestation, atomic commits, final push, and green CI.
15. Obtain explicit operator approval before any production adaptive
    activation. Until then, remain off/shadow with static serving.

</remaining_work>

<decisions_made>

- PostgreSQL is authoritative; Neo4j and the existing agent-memory
  infrastructure are replayable private analytical projections.
- Adaptive payloads do not go through LLM-facing memory tools.
- Adaptation is multi-domain, not reasoning-only.
- Qwen3.5 2B Q4_K_M under llama.cpp proves portability and E2E integration
  only.
- Qwen's `+0.1679` held-out point gain did not pass the unseen-family gate:
  95% family-bootstrap interval `[-0.0168, +0.3198]`.
- The seeded canary artifact is `VALIDATED_GATE_NOT_MODEL` with
  `subject_model_id=synthetic-model-not-production`.
- A temporary Qwen model override must be database-only and restored with
  evidence; production config and environment must not be rewritten for it.
- Production remains static/off-or-shadow until real-model evidence passes.
- Legacy stored data deletion was explicitly authorized and is complete.
- Test-only reachability is dark code; mutation functionality returns only
  with a real production caller.

</decisions_made>

<blockers>

- Technical: generic events are not OPE-ready.
- Technical: no typed per-domain assignment adapters.
- Technical: no durable immutable cohort loader.
- Technical: no production-composed projector.
- Technical: no graph challenger, snapshot, or disagreement implementation.
- Technical: no production policy administration or learned-action canary
  serving.
- Deployment: `aura` is restarting because the stale binary wants migration
  `51` while the live database is at `59`.
- Human gate: future activation requires explicit operator approval after real
  model proof.

</blockers>

## Required Reading (in order)

1. `D:\Aura\AGENTS.md` - session bootstrap rule.
2. `D:\Aura\CLAUDE.md` - complete binding repository rulebook.
3. `D:\Aura\.planning\HANDOFF.json` - machine-readable current state.
4. `D:\Aura\prd.md` - Amendments #93 through #93.3 and PRD amendment protocol.
5. `D:\Aura\docs\superpowers\specs\2026-07-24-aura-adaptive-intelligence-design.md`
   - target design and explicit production blockers.
6. `D:\Aura\.planning\spikes\102-adaptive-generalization-ope-gate\README.md`
   - Qwen llama.cpp/OPE proof and failed unseen-family gate.
7. `D:\Aura\.planning\spikes\103-adaptive-runner-outbox-proof\README.md`
   - runner/outbox contract.
8. `D:\Aura\.planning\spikes\104-adaptive-identity-distributed-safety\README.md`
   - deletion, fencing, projection, and distributed safety.
9. `D:\Aura\.planning\spikes\105-adaptive-quality-canary-gate\README.md`
   - seeded statistical gate; not model proof.
10. `D:\Aura\internal\adaptive\event.go`,
    `D:\Aura\internal\adaptive\hook.go`,
    `D:\Aura\internal\adaptive\policy.go`,
    `D:\Aura\internal\adaptive\projector.go`, and
    `D:\Aura\internal\runner\adaptive_persist.go` - current executable
    boundary.
11. `D:\Aura\docs\aura-quality-snapshot.md` - phase-close re-attestation
    contract.
12. `D:\Aura\.planning\METHODOLOGY.md` if it exists - apply its analytical
    lenses before assumptions.

## Infrastructure State

- Git: clean local handoff commit one commit ahead of
  `origin/master == 29ffcbbcb`; the handoff was not pushed.
- GitHub Actions at `29ffcbbcb`: CI, CodeQL, and Skills all successful.
- Docker support services: running; health-bearing services healthy at pause.
- `aura`: restarting/unhealthy, exit `71`, because its stale binary expects
  migration `51` and PostgreSQL reports `59`.
- PostgreSQL: `127.0.0.1:5432`.
- Neo4j Bolt: `127.0.0.1:7687` and `127.0.0.1:17687`.
- Agent-memory MCP: `127.0.0.1:8091`.
- Aura image creation: `2026-07-24T10:02:01.870465866Z`, before the adaptive
  commits.
- No `.planning/async-jobs/*.json` manifests exist.
- `git worktree list` reports the root worktree plus two prunable stale
  metadata entries for missing Codex/Superpowers paths. They are not active
  implementation worktrees.

## Pre-Execution Critique Required

- Design artifact:
  `docs/superpowers/specs/2026-07-24-aura-adaptive-intelligence-design.md`
- First contract artifact to create: PRD Amendment #93.4.
- Critique focus:
  - point-unique assignments and multi-round reasoning;
  - support, propensity, and explicit no-op correctness;
  - immutable cohort and evaluator provenance;
  - every adaptive decision domain, not reasoning alone;
  - production projector lifecycle;
  - graph snapshot immutability and privacy;
  - reachable capability-gated administration;
  - canary rollback and destructive-action exclusion;
  - DB-only temporary Qwen override and provable restoration;
  - separation of Qwen portability proof from real-model production proof.
- Gate: do not begin code until #93.4 is committed PRD-first and a dedicated
  adversarial reviewer accepts its machine-checkable contract.

<context>

The cleanup checkpoint is proven and pushed, but the operator explicitly
corrected any implication that the adaptive implementation was finished.
Resume as an implementation continuation. Work in small, dependency-ordered,
TDD slices. Keep static Aura behavior as champion. Use Qwen only to prove the
llama.cpp/Aura path through a temporary database override; use Aura's actual
configured model for scientific production evidence.

</context>

<next_action>

Start with:

1. Run `$gsd-resume-work`.
2. Read `AGENTS.md` and `CLAUDE.md` completely before any other action.
3. Acknowledge every blocking constraint above.
4. Verify `git status --short --branch`, current `HEAD`, `origin/master`, and
   current-head GitHub Actions.
5. Read the required documents and current code.
6. Draft PRD Amendment #93.4 for OPE-complete typed assignments/outcomes and
   all adaptive domains.
7. Send #93.4 to a dedicated adversarial agent before code.
8. Commit #93.4 atomically with normal hooks, then begin TDD implementation.

</next_action>
