---
context: default
phase: aura-calm-prism-chat-refinement
task: 9
total_tasks: 9
status: in_progress
last_updated: 2026-07-17T01:38:44.9963328+02:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are repository rules, not suggestions. Keep every checked constraint in force.

- [x] **Track progress in the written plan immediately.** Task 8's eight boxes were checked only after implementation, fresh browser/static verification, spec approval, and adversarial approval.
- [x] **Merge every task when it closes.** Task 9 may not start until this Task 8 checkpoint and implementation are merged into `master`, freshly verified there, and the worktree is synchronized.
- [x] **Keep the 600-LOC cap on every touched file.** Task 8 closes with every changed text source under 600 lines; Task 9 must preserve the cap.
- [x] **Use strict RED → GREEN and both review gates.** Every implementation task requires a spec review followed by an adversarial review; findings return to the same implementer and the same reviewers re-review.
- [x] **Use the approved browser only.** The user approved Playwright with installed Chrome. Task 8 must not substitute Chromium, Edge, Firefox, or WebKit.
- [x] **Run npm only from `web`.** Never create a root `node_modules`. Run the exact size gate with Windows Git Bash, never WSL.

## Current State

Tasks 1–8 of the Calm Prism plan are implemented, plan-checked, independently approved, merged, freshly verified on `master`, and synchronized back to the worktree. Task 9 is now in progress from the verified Task 8 merge.

- Worktree: `D:\Aura\.worktrees\calm-prism-chat-plan`
- Branch: `plan/calm-prism-chat`
- Verified Task 7 merge: `7f7c95353a98067b7543d122361e3c7a8924d324` (`merge: Calm Prism run telemetry task 7`)
- Task 7 final implementation: `1d023511a6cbd8c4883781c87ddf6cdea006dda1` (`fix(web): fail soft across usage outages`)
- Task 7 public contract: `RunUsageEvent` remains exactly `{runId, phase, usage}`.
- Task 7 full frontend verification: 174 files, 1422/1422 tests; coverage 92.76% statements, 86.85% branches, 92.99% functions, 94.62% lines.
- Task 7 focused implementer verification: 74/74. Spec reviewer: 77/77 and `✅ Spec compliant`. Adversarial reviewer: 74/74, no Critical/Important/Minor findings, `Ready to merge? Yes`.
- Typecheck, ESLint, Prettier, contrast 102/102, exact Git-Bash size, diff hygiene, direct hook, and commit-time hook all passed.
- The first full run had one unrelated `DocumentsWorkspace` five-second load timeout; it passed 4/4 in isolation and the authoritative full rerun passed 1422/1422. No unrelated timeout was patched.
- Fresh master verification: focused 77/77; full 174 files and 1422/1422 tests; typecheck, ESLint, Prettier, contrast 102/102, exact Git-Bash size, and diff hygiene all passed.
- Worktree and master matched exactly at the verified merge before this Task 8 start checkpoint.
- Task 8 implementation commits: `aea0e3cb4` (`test(web): lock Calm Prism Chrome visuals`) and `61ff8a90d` (`test(web): harden Calm Prism browser evidence`).
- Task 8 accepted browser evidence: installed Chrome desktop 13 passed / 5 expected skips; mobile Chrome 10 passed / 8 expected skips after the two review-driven hardening regressions.
- Task 8 spec re-review: `✅ Spec compliant`. Adversarial re-review: no Critical/Important/Minor findings; `Ready to merge: Yes`.
- Four accepted goldens and six same-input baseline/implementation/comparison images are committed; both combined comparisons were independently inspected at original detail.
- Verified Task 8 merge: `026d9b3236c82ece65d993810c556d63dccba1d7` (`merge: Calm Prism Chrome evidence task 8`).
- Fresh master frontend: 174/174 files and 1423/1423 tests; 92.76% statements, 86.83% branches, 92.99% functions, 94.63% lines.
- Fresh master static/build: typecheck, ESLint, Prettier, contrast 102/102, production preview build, exact Git-Bash size, and diff hygiene passed.
- Fresh master Playwright, run in the written sequential order: Chrome 13 passed / 5 expected skips; mobile Chrome 10 passed / 8 expected skips. A parallel-only desktop-golden mismatch did not reproduce in three isolated repeats or either authoritative sequential full project run; no code was changed for the runner-contention artifact.

## Task 7 Delivered

- Persistent AppShell-owned run IDs, stale event/baseline rejection, and exact start/update/finally-settle lifecycle for send, edit, reload, and approval resume.
- Cancel only aborts; unmount/thread changes cannot let old work overwrite a newer run.
- Visible telemetry stays live while the hidden status announces exactly once on settlement.
- Mobile Cost/Context disclosure, cue, gauge placement, bilingual resources, and unique accessibility relationships are preserved.
- Session totals handle post-inclusive aggregates, rapid next runs, late seeds, and repeated unavailable baselines without loss or double-counting.
- Baseline detail reads fail soft after 1500 ms, propagate the React Query abort signal, cancel the exact pending query on timeout/Stop, allow a clean retry, and suppress late publication.

## Task 7 Close Sequence — Completed

1. [x] Task 7 plan and handoff checkpoint committed at `480ac1613`.
2. [x] Feature branch merged into `master` with `--no-ff` at `7f7c95353` while preserving the external graph commit.
3. [x] Fresh master focused/full frontend, typecheck, lint, Prettier, contrast, exact size, and diff gates passed.
4. [x] Master confirmed clean at the verified Task 7 merge.
5. [x] Worktree branch fast-forwarded to the verified master merge.
6. [x] Task 8 advanced to `in_progress`; browser-use instructions and the written Task 8 contract were read before Playwright work.

## Remaining Work

### Task 8 — Explicit Chrome Playwright health and visual contracts

Implementation and both review gates are complete; all eight Task 8 plan boxes are checked. Required close sequence:

1. [x] Task 8 plan/handoff checkpoint committed at `4d93ba2a7`.
2. [x] Feature branch merged into `master` with `--no-ff` at `026d9b323` while preserving unrelated master history.
3. [x] Fresh master frontend, installed-Chrome desktop/mobile, typecheck, lint, Prettier, contrast, exact size, build, and diff gates passed.
4. [x] Master confirmed clean at the verified Task 8 merge.
5. [x] Feature worktree synchronized to the verified merge.
6. [x] Task 9 advanced to `in_progress` in this written handoff.

### Task 9 — Final repository verification and reviewed bundle

In progress from verified merge `026d9b323`. Written Steps 1-4 are checked after targeted/full frontend, installed-Chrome desktop/mobile, build inspection, Go, and file-size gates passed. The first adversarial audit blocked close; review-hardening commit `c461cf2f4` now closes stale draft resurrection, thread-change footer reset, overly broad aborted-request suppression, and visual-evidence attribution with focused RED/GREEN coverage. The same reviewer still requires a final post-fix bundle refresh, a Composer callback truthfulness test, corrected Task-9 provenance wording, and this committed exception ledger before approval. The opt-in real-agent Chrome 10/10 test exists locally and passes lint/typecheck; it remains uncommitted and unexecuted against a rebuilt current instance. Keep Steps 5-8 open until their evidence is complete.

#### Task 9 owned-range exception ledger

- `b6c8a504c`, `1a3e86d65`, and `4bd6ed203` are preserved Phase 37F planning commits from parallel `master` history, not Calm Prism changes.
- `b2c2c6471` is a preserved graph/intel refresh from parallel `master` history, not a Calm Prism change.
- `web/src/api/json.ts` is the sole frontend API-helper exception: it adds an optional internal `AbortSignal` only and changes no endpoint, payload, protocol, backend, dependency, manifest, or lockfile.
- `aea0e3cb4` includes Task 8 production corrections despite its `test(web)` subject; preserve history and record the subject/scope mismatch rather than rewriting it.
- `7133012bf` was the first generated-only refresh but predates `c461cf2f4`; a second generated-only refresh is mandatory after all review source fixes.

## Critical Anti-Patterns

| Pattern | Severity | Prevention |
|---|---:|---|
| Start Task 9 before Task 8 merge verification | blocking | Complete the six-step close sequence above first. |
| Mark a task from tests or an implementer report alone | blocking | Require both independent approvals before changing plan boxes. |
| Leave a closed task only in the worktree | blocking | Merge on every task close, then verify freshly on master. |
| Treat screenshots as visual QA | blocking | Compare reference and implementation at identical viewport/state, fix visible differences, and compare again. |
| Ignore console/page/request/response failures | blocking | Collect and fail on strict browser-health evidence. |
| Substitute a browser | blocking | Use installed Chrome, as approved by the user. |
| Run npm from the worktree root | advisory | Run all frontend commands from `web`. |
| Use WSL for the linked-worktree size gate | advisory | Run `& 'C:\Program Files\Git\bin\bash.exe' scripts/check-file-size.sh` from the Windows worktree/root. |
| Patch an unrelated timeout without reproducing it | advisory | Isolate first, then rerun the authoritative suite. |

## Required Reading for Task 9

1. `.planning/.continue-here.md`
2. `CLAUDE.md`
3. `docs/superpowers/plans/2026-07-15-aura-calm-prism-chat-refinement.md` — Task 9 source of truth
4. `docs/superpowers/specs/2026-07-15-aura-calm-prism-chat-refinement-design.md`
5. `prd.md`, Amendment #83
6. Browser/Playwright and verification skill instructions selected for Task 9
7. `C:\Users\Davide\.codex\plugins\cache\openai-curated-remote\product-design\0.1.50\references\critical-overrides.md`

## Blockers

- Technical blockers: none.
- Human/external blockers: none.
- Required checkpoint: none; Task 8 close sequence is complete.

## Resume Prompt

Resume Task 9 after review-hardening commit `c461cf2f4`. Finish the Composer callback/provenance minors with the same implementer, obtain the same adversarial reviewer’s zero-finding approval, commit the typed/linted `web/e2e/chat-real-agent.spec.ts`, rebuild and commit a second generated-only `internal/webui/dist` refresh, then run final gates. Merge Task 9 to `master`, rebuild/recreate the current Aura service, and run installed Chrome against the real Authula/composer/DOCX/unmocked-agent path until it explicitly reports `SCORE: 10/10 = 100%`. Do not claim the active goal complete before that proof.
