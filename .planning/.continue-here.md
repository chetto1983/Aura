---
context: default
phase: aura-calm-prism-chat-refinement
task: 7
total_tasks: 9
status: paused
last_updated: 2026-07-16T13:36:54.808Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Acknowledge each one before resuming.

- [ ] **Start Task 7 from RED.** Task 7 has not started. Add and run the lifecycle/footer tests before changing production code.
- [ ] **Keep the close gate intact.** A task closes only after implementation, spec review, adversarial quality review, immediate checkbox update, docs-only checkpoint, merge into `master`, and fresh post-merge verification.
- [ ] **Respect the 600-LOC cap on touch.** `AppShell.tsx` is 579 LOC and `ExternalStoreChat.tsx` is 590 LOC before Task 7; use the planned `runUsage.ts` extraction and split further rather than exceeding the cap.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Mark or merge before both reviews approve | Task 5 and Task 6 adversarial reviews found real concurrency/loading/focus defects after initial green suites. | blocking | Use the same implementer for fixes, then the same spec reviewer, then the same quality reviewer; do not check the plan early. |
| Leave an approved task only in a worktree | The user made merge-on-task-close a permanent repository rule. | blocking | After the docs checkpoint, merge locally into `master`, run fresh tests/typecheck/lint there, then fast-forward the worktree branch to the verified merge. |
| Treat an empty local array as loaded state | Task 6 briefly showed starters while history was loading or failed. | advisory | Model lifecycle/readiness explicitly and reject stale async completions; Task 7 usage state must likewise be run-scoped and phase-explicit. |
| Consume an event while its target is disabled | A locked composer consumed a draft nonce and could not restore focus. | advisory | Do not consume run/draft events until their target phase can apply them; test locked/cancelled/stale transitions. |
| Run npm from the worktree root | This created an unintended root `node_modules/` twice. | advisory | Run frontend commands from `web`; if cleanup is needed, resolve and verify the exact root path before native PowerShell removal, never touch `web/node_modules`. |
| Use WSL for the linked-worktree size gate | WSL could not resolve Windows worktree metadata and could falsely exit green. | advisory | From the worktree use `& 'C:\Program Files\Git\bin\bash.exe' scripts/check-file-size.sh` with at least a 240-second allowance. |
| Patch an unrelated timeout without reproducing it | A full-suite `DocumentsWorkspace` 5-second timeout passed 4/4 in isolation and the next full run passed. | advisory | Follow systematic debugging: isolate first, then rerun full; do not edit unrelated tests for load-only flakiness. |

<current_state>

Tasks 1–6 of the Calm Prism plan are fully approved, checked, merged, and freshly verified. Task 7 is the next untouched task.

- `master`: `25fca185c98ae89c2d4bc24af7602f2df209de58` (`merge: Calm Prism prompt starters task 6`).
- Worktree: `D:\Aura\.worktrees\calm-prism-chat-plan`.
- Worktree branch: `plan/calm-prism-chat`, fast-forwarded to the same `25fca185c`; clean.
- Source of truth: `docs/superpowers/plans/2026-07-15-aura-calm-prism-chat-refinement.md`.
- Task 6 post-merge verification on `master`: 169/169 files and 1394/1394 frontend tests; 92.63% statements, 94.49% lines; full TypeScript and ESLint passed.
- No Task 7 source or test edits exist. All seven Task 7 checkboxes remain open.
- No Vite, Playwright, backend, or watcher process is intentionally running.

</current_state>

<completed_work>

- Tasks 1–3: approved and merged at `6909e51d3`.
- Tasks 4–5: approved and merged at `6686b9cab`; Task 5 includes exact per-attempt approval recovery, focus ownership, real dictation cancellation, and capture-lock shutdown.
- Task 6 implementation: `ccb06a966` (`feat(web): add Calm Prism prompt starters`).
- Task 6 adversarial correction: `851e1b576` (`fix(web): harden prompt starter transitions`).
- Task 6 plan checkpoint: `cb5970cc6`.
- Task 6 merge: `25fca185c`.
- Task 6 delivers exact English/Italian starters, AppShell-owned nonce replacement, editable focused drafts, zero run/picker side effects, history-ready visibility, stale-request rejection, locked-to-unlocked draft deferral, and supported group semantics.

</completed_work>

<remaining_work>

## Task 7 — Make usage state run-scoped and announcements settlement-only

Not started. Follow the written seven steps exactly:

1. Add RED lifecycle tests for start/reset/update/settle-once/stale/cancel/clear across normal send, edit, reload, resume, failure, and AbortError.
2. Add RED footer tests separating live visible telemetry from a single settled-only hidden announcement, including mobile Cost/Context labels, disclosure cue, and gauge placement.
3. Create `web/src/chat/runUsage.ts` with explicit `idle | running | settled` events and exact run IDs.
4. Wire every stream path through `start → update → finally settle`; thread change clears state; cancel only aborts.
5. Store the whole usage event in AppShell and make RuntimeFooter announce once per settled run without reusing prior-turn values.
6. Run focused, expanded, full frontend, typecheck, full lint/Prettier, contrast/parity, exact Git-Bash size, diff/safety, hooks, spec review, and adversarial review.
7. After both reviewers approve: check Task 7 immediately, commit the docs checkpoint, merge into `master`, run fresh post-merge verification, and sync the worktree before Task 8.

## Task 8 — Chrome Playwright health and visual contracts

Not started. The user already approved Playwright. Use installed Chrome, current Vite source at `127.0.0.1:5173`, strict console/page/request health, deterministic fixtures, responsive/theme screenshots, and the visual-review record.

## Task 9 — Final repository verification and reviewed bundle

Not started. Execute the complete static/unit/browser/build/Go/file-size gates and final commit/path audit exactly as written.

</remaining_work>

<decisions_made>

- The written plan is the implementation-progress source of truth; tests or an implementer report alone never close a task.
- Every task uses strict test-first RED→GREEN, then spec review before adversarial quality review.
- Every task is merged and freshly verified on `master` before the next task begins.
- AppShell remains the sole nonce-backed composer draft owner.
- Async UI visibility and recovery use explicit phase/generation ownership rather than inferred empty arrays or late callbacks.
- Task 7 must keep live visible telemetry separate from settlement-only assistive announcements and must never reuse a prior run's usage.
- Existing design tokens/components remain authoritative; no new UI dependency or backend protocol is allowed.

</decisions_made>

<blockers>

- Technical blockers: none.
- Human/external blockers: none.
- Async external jobs: none.
- Pre-existing placeholder scan matches exist in unrelated `.planning/phases` summaries/verification files (phases 31–37F, 42, and 43). They were not created or validated by Calm Prism work and are not Task 7 scope; do not use them as completion evidence.

</blockers>

## Required Reading (in order)

1. `.planning/.continue-here.md` — this handoff.
2. `.planning/HANDOFF.json` — machine-readable resume state.
3. `CLAUDE.md` — global PRD, merge-on-close, validation, and 600-LOC rules.
4. `docs/superpowers/plans/2026-07-15-aura-calm-prism-chat-refinement.md` — Task 7 source of truth and commands.
5. `docs/superpowers/specs/2026-07-15-aura-calm-prism-chat-refinement-design.md` — approved design/test contract, especially usage settlement and footer semantics.
6. `prd.md`, Amendment #83 — blocking Calm Prism product contract.
7. `C:\Users\Davide\.codex\plugins\cache\openai-curated-remote\product-design\0.1.50\references\critical-overrides.md` — reread before UI work and every second user-facing message.

## Critical Anti-Patterns (do NOT repeat these)

- Do not start Task 7 on `master`; implement in the linked worktree.
- Do not change production before observing the exact Task 7 RED failures.
- Do not dispatch quality review before spec approval.
- Do not mark Task 7 or start Task 8 until review, plan checkpoint, merge, and post-merge verification all pass.
- Do not use `git add -A`, WSL for the worktree size gate, or destructive cleanup in the dirty master checkout.
- Do not weaken live-region, browser-health, console, request, focus, or accessibility assertions to make tests green.
- Do not add arbitrary long test timeouts; isolate and debug the actual async boundary.

## Infrastructure State

- `master` and `plan/calm-prism-chat`: `25fca185c`.
- Feature worktree is clean.
- Existing unrelated master changes must be preserved: `.planning/graphs/.last-build-status.json`, `.planning/graphs/GRAPH_REPORT.md`, and untracked `.planning/intel/`.
- No active async job manifests and no intentional Aura/Vite/Playwright processes.
- Frontend dependencies remain in `web/node_modules`; there must be no worktree-root `node_modules`.
- Use Windows Git Bash for `scripts/check-file-size.sh`; allow roughly 120–240 seconds.

<context>

Resume with a fresh Task 7 implementer. The main risk is lifecycle ownership: current telemetry can reuse prior-turn data and the visible metrics/live region are coupled. Build the isolated `runUsage.ts` seam test-first, then wire every stream entry/exit path through it. AppShell and ExternalStoreChat are near the file cap, so use the planned extraction and split on touch. Keep the same strict review loop used successfully for Tasks 5 and 6.

</context>

<next_action>

Start with:

```powershell
cd D:\Aura\.worktrees\calm-prism-chat-plan
git status --short
git rev-parse --short HEAD
cd web
npx vitest run src/chat/__tests__/ExternalStoreChat.test.tsx src/chat/__tests__/RuntimeFooter.test.tsx
```

Expected baseline: clean worktree at `25fca185c`, existing suites green. Then create `src/chat/__tests__/runUsage.test.tsx` and the remaining Task 7 lifecycle/footer tests, run the exact focused command from the plan, and capture the expected RED before editing production.

</next_action>
