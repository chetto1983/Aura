---
context: default
phase: aura-calm-prism-chat-refinement
task: 4
total_tasks: 9
status: in_progress
last_updated: 2026-07-15T15:33:14.375Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **Task 4 is not approved.** Do not merge it, mark its plan boxes, or begin Task 5 until its lifecycle fixes pass implementation, spec, and quality review.
- [ ] **Resume from RED.** At `10c8df592`, the focused Task 4 suite must fail 1/29 because the collapsed raw body is absent. Add the remaining requested failing lifecycle tests before changing production code.
- [ ] **Keep review ranges exact.** Docs-only progress commits are interleaved with implementation commits; always provide explicit base and head SHAs to reviewers.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern                                                   | Description                                                                                                                                              | Severity | Prevention Mechanism                                                                                                                 |
| --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Merge before both reviews approve                         | Task 4 passed spec but quality review found two Important lifecycle/accessibility defects.                                                               | blocking | Keep Task 4 on `plan/calm-prism-chat`; merge only after the same spec and quality reviewers approve the final range.                 |
| Treat timestamp presence as tool status                   | `finishedAt` can arrive before `result`, and `result` can settle without `finishedAt`; this produced mismatched visible/assistive state and timer leaks. | blocking | Derive one canonical status in the parent and test split END → RESULT plus result/error without `finishedAt`.                        |
| Use the stale embedded frontend for Playwright            | Port `9080` served old JavaScript and produced false reasoning results.                                                                                  | advisory | Run current source with Vite on `127.0.0.1:5173` and `AURA_E2E_ORIGIN=http://127.0.0.1:5173`; keep strict console/page-error checks. |
| Run the file-size script through WSL in a linked worktree | WSL could not resolve the Windows Git worktree metadata.                                                                                                 | advisory | Use Git Bash and allow about 120–160 seconds for `scripts/check-file-size.sh`.                                                       |

<current_state>

Paused after merging fully approved Tasks 1–3 into `master` at `6909e51d3`.

The active implementation remains in `D:\Aura\.worktrees\calm-prism-chat-plan` on `plan/calm-prism-chat` at `10c8df592`. That branch contains:

- `306e2720f` — Task 4 initial implementation. Spec review approved it; quality review returned **With fixes**.
- `10c8df592` — test-only WIP checkpoint. The focused suite is intentionally RED at 28/29 because the collapsed raw `<pre>` is not kept in the DOM.

The feature worktree is clean. Task 4 plan boxes remain open. Tasks 5–9 have not started.

</current_state>

<completed_work>

Completed and merged:

- Task 1: PRD-first Calm Prism contract — approved.
- Task 2: normal-flow workspace controls, measurable message geometry, fluid Markdown tables including nested tables, 44 px action targets — final spec and quality reviews approved.
- Task 3: reasoning hidden by default, safe storage precedence, unique disclosure IDs, installed-Chrome chronology coverage, truthful E2E fixtures, and Chrome project compatibility — final spec and quality reviews approved.

Stable merge verification at `6909e51d3`:

- Frontend: 159/159 files, 1298/1298 tests passed with coverage.
- TypeScript: `npm run typecheck` passed.
- Go: `go test -p=2 ./...` passed.

</completed_work>

<remaining_work>

## Task 4 — finish before anything else

Quality review findings to fix with strict TDD:

1. Render `<pre id={bodyId} hidden={!expanded}>` whenever raw content exists so collapsed `aria-controls` resolves. Update collapsed assertions to check a hidden target, not DOM removal; retain React-text XSS safety.
2. Derive one canonical parent tool status and use it for row state, assistive elapsed semantics, and ticker lifetime. Keep `finishedAt` authoritative when provided.
3. Add a split lifecycle test: `finishedAt` arrives while result is absent, so the row remains Running and elapsed stays `aria-hidden`; result then arrives, status becomes Done, duration stays frozen, and `aria-hidden` is removed.
4. Add result/error-without-`finishedAt` tests: the ticker stops on logical settlement, timer count drops, duration freezes, and the settled value is available to assistive technology.
5. Strengthen manual-intent delayed-raw coverage: collapse manually, rerender with no raw, then add late raw and verify it stays collapsed with a resolving hidden body.
6. Update the stale ToolActivityCard comment that still describes a tinted line and pill.
7. Run targeted tests, typecheck, owned lint, Prettier, raw-color/diff checks, Git-Bash file-size, and commit hooks.
8. Repeat spec review first, then quality review. Fix every finding with the same implementer/reviewer loop.
9. Only after both approve: mark all Task 4 plan boxes, commit a docs-only checkpoint, then begin Task 5 with a fresh implementer.

Tasks 5–9 remain as written in the implementation plan.

</remaining_work>

<decisions_made>

- Merged only through Task 3 because Task 4 has unresolved Important findings.
- Preserved the first Task 4 fix attempt as a test-only RED WIP commit instead of committing unreviewed production changes.
- Kept the durable plan rule: a task is checked only after implementation plus spec and quality reviews pass; the checkbox update is a separate docs-only commit.
- Kept Playwright’s desktop project name `chrome` with `channel: 'chrome'`; all seven legacy desktop predicates were updated from `chromium` to `chrome`.
- Kept Markdown tables fluid at top level and inside blockquotes/lists by omitting the prose cap on structural ancestors containing a table.

</decisions_made>

<blockers>

- **Technical:** Task 4 quality review is not approved. The exact unresolved findings are listed above.
- **Human/external:** None.

</blockers>

## Required Reading (in order)

1. `.planning/.continue-here.md` — this handoff.
2. `docs/superpowers/plans/2026-07-15-aura-calm-prism-chat-refinement.md` — source of truth and checkbox state.
3. `docs/superpowers/specs/2026-07-15-aura-calm-prism-chat-refinement-design.md` — approved design contract.
4. `prd.md`, Amendment #83 — blocking product contract.
5. `C:\Users\Davide\.codex\plugins\cache\openai-curated-remote\product-design\0.1.50\references\critical-overrides.md` — reread before continuing UI work.

## Critical Anti-Patterns (do NOT repeat these)

- Do not accept an implementer’s test report as completion; run spec review, then quality review.
- Do not cap the entire Markdown part; only prose blocks may receive the exact 48 rem cap.
- Do not run Playwright against stale `:9080` embedded assets when validating current frontend source.
- Do not weaken browser console/page/request health assertions to make E2E green; fix deterministic fixtures at their source.
- Do not use `git add -A`, WSL file-size checks, or destructive cleanup in the dirty main checkout.

## Infrastructure State

- `master`: `6909e51d3`, approved Tasks 1–3 merged and verified.
- Feature worktree: `D:\Aura\.worktrees\calm-prism-chat-plan`.
- Feature branch: `plan/calm-prism-chat`, HEAD `10c8df592`, clean.
- No Vite, Playwright, or backend server is intentionally left running.
- Existing unrelated `master` changes were preserved: `.planning/graphs/.last-build-status.json`, `.planning/graphs/GRAPH_REPORT.md`, and untracked `.planning/intel/`.

<context>

Resume cautiously from the Task 4 review failure, not from the stable merge. The initial Task 4 implementation is close: 29/29 original tests passed, locale formatting and seven-state disclosure behavior were added, and spec review approved. Quality review exposed lifecycle cases the original matrix did not cover. The first accessibility RED test is committed; the remaining lifecycle RED tests still need to be written before production changes.

</context>

<next_action>

Start with:

```powershell
cd D:\Aura\.worktrees\calm-prism-chat-plan
git status --short
git rev-parse --short HEAD
cd web
npx vitest run src/chat/__tests__/ToolActivityCard.test.tsx src/i18n/__tests__/resources.parity.test.ts
```

Expected: clean branch at `10c8df592`; 28/29 tests pass and the collapsed-body accessibility test fails. Then add the remaining split-lifecycle and timer RED tests before editing `ToolActivityCard.tsx`.

</next_action>
