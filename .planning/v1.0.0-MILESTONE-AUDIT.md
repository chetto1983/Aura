---
milestone: v1.0.0
milestone_name: Aura Deep Search Web Cockpit
audited: "2026-06-19"
status: gaps_found
scope_note: "Milestone is 6/9 phases complete (67%). Phases 28, 29, 30 are unbuilt by design — this audit reflects a mid-milestone state, not a pre-archival close. The gaps_found status is driven primarily by the unbuilt phases."
scores:
  requirements: 40/51 built+verified (11 unsatisfied = unbuilt P28/P29; of the 40, SWARM-01 partial + GRAPH-01..04 carry a verification-artifact gap)
  phases: 6/9 built (5/6 fully verified+closed; P27 operator-closed without a VERIFICATION.md)
  integration: pass-with-warning (cockpit chain 22→27 wires end-to-end; 0 broken flows, 1 WARNING, 2 INFO)
  flows: 6/6 wired (SWARM-01 + code typed-display degrade to raw card in the live SSE path)
gaps:
  requirements:
    - id: "GOV-01"
      status: "unsatisfied"
      phase: "Phase 28 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "No phase-28 directory, no plans, no code. REQUIREMENTS.md [ ] Pending."
    - id: "GOV-02"
      status: "unsatisfied"
      phase: "Phase 28 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as GOV-01."
    - id: "GOV-03"
      status: "unsatisfied"
      phase: "Phase 28 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as GOV-01."
    - id: "ONBD-01"
      status: "unsatisfied"
      phase: "Phase 28 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Web onboarding wizard not built. (Note: a CLI/loopback onboarding LoopAgent exists from substrate Phase 14; the WEB surface does not.)"
    - id: "ONBD-02"
      status: "unsatisfied"
      phase: "Phase 28 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as ONBD-01."
    - id: "MCPW-01"
      status: "unsatisfied"
      phase: "Phase 29 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "No phase-29 directory. Backend MCP manager exists (substrate Phase 16); web write surface does not. REQUIREMENTS.md [ ] Pending."
    - id: "MCPW-02"
      status: "unsatisfied"
      phase: "Phase 29 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as MCPW-01."
    - id: "MCPW-03"
      status: "unsatisfied"
      phase: "Phase 29 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as MCPW-01."
    - id: "SKW-01"
      status: "unsatisfied"
      phase: "Phase 29 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "No phase-29 directory. Backend skill install/approval/audit exists (substrate Phase 11); web surface does not."
    - id: "SKW-02"
      status: "unsatisfied"
      phase: "Phase 29 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as SKW-01."
    - id: "SKW-03"
      status: "unsatisfied"
      phase: "Phase 29 (not built)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Same as SKW-01."
    - id: "SWARM-01"
      status: "partial"
      phase: "Phase 26"
      claimed_by_plans: ["26-01-PLAN.md", "26-04-PLAN.md"]
      completed_by_plans: ["26-01-SUMMARY.md", "26-04-SUMMARY.md"]
      verification_status: "passed (per-phase, in isolation) — integration gap found"
      evidence: "Per-phase verification marked SATISFIED via SwarmReportTable + display.normalizeSwarm component tests. Integration check found the LIVE SSE path (llm_agent_events.go:152 → deriveDisplay → NormalizeToolPreview) only decodes web_search/web_fetch (preview.go default: return nil,false). swarm_spawn produces Actions.Display==nil → raw-card fallback. SwarmReportTable is unreachable in live + replay runs. Safe fallback (no crash), but the typed card never renders."
    - id: "GRAPH-01..04"
      status: "partial (verification-artifact gap)"
      phase: "Phase 27"
      claimed_by_plans: ["27-01..27-04-PLAN.md"]
      completed_by_plans: ["27-01..27-04-SUMMARY.md"]
      verification_status: "missing VERIFICATION.md (27-VALIDATION.md status=complete, operator-closed)"
      evidence: "Phase 27 was closed by operator directive 2026-06-19 with a complete 27-VALIDATION.md (nyquist_compliant, 599/599 Vitest, 75.88% Stryker, 12/12 live Playwright) but NO 27-VERIFICATION.md. SUMMARY frontmatter + REQUIREMENTS.md [x] confirm delivery; the formal verifier artifact is absent. Code is delivered; the gap is the missing verification record + uncommitted post-close graph WIP in the working tree."
  integration:
    - finding: "SWARM-01 / code typed-display not wired into the live SSE event path"
      severity: "WARNING"
      file: "internal/agent/display/preview.go:55 (default: return nil,false); internal/agent/llm_agent_events.go:152 (call site)"
      affected_reqs: ["SWARM-01", "DISP-01", "DISP-04"]
      detail: "NormalizeToolPreview bridges only web_search/web_fetch from JSON preview strings. swarm_spawn/shell_exec/sandbox_exec → Actions.Display nil → raw-card fallback in live + replay. Components + DisplayRouter cases are committed and correct; the missing link is a decodeToolPreview case for these tools' JSON preview shape."
    - finding: "Graph explorer best-effort boot — 503 for daemon lifetime if Neo4j/mcp-neo4j-cypher down at boot, no retry path; frontend shows generic error not 'graph not configured'"
      severity: "WARNING (by design)"
      file: "cmd/aura/serve.go:307-312; web/src/graph/GraphExplorer.tsx:200-210"
      affected_reqs: ["GRAPH-01", "GRAPH-02"]
      detail: "SetGraphView only called on successful knowledge.Open. Documented intentional (graph outage != daemon outage), but no in-flight reconnect — requires daemon restart."
    - finding: "Authula auth provider is in the committed path (flag-gated, defaults to passphrase, nil-guarded) — large overhaul + SPA login still uncommitted in working tree"
      severity: "INFO"
      file: "cmd/aura/serve_auth.go; internal/webauth"
      affected_reqs: ["WEB-03", "WEB-04"]
      detail: "Not a committed defect; default path safe. Working tree carries the cockpit-overhaul + Authula SPA login uncommitted (see docs/cockpit-overhaul/)."
    - finding: "Asset upload flow requires RequireAuth to have run; unauthenticated loopback dev returns 401"
      severity: "INFO"
      file: "internal/agui/assets_api.go:159 (principalIdentityID)"
      affected_reqs: ["CHAT-04"]
      detail: "Not a production gap (boot guard confines unconfigured secrets to loopback); local dev cannot use attachments without a passphrase configured."
  flows: []
tech_debt:
  - phase: "Phase 24"
    items:
      - "WEB-02 (non-loopback boot guard) is absent from all Phase-24 SUMMARY requirements_completed frontmatter (24-01 and 24-02 frontmatter are empty []). VERIFICATION.md verifies it SATISFIED with strong evidence (GuardWebBind + TestGuardWebBind + serve_smoke). Traceability-hygiene gap only — fix the SUMMARY/frontmatter, not the code."
  - phase: "Phase 22"
    items:
      - "toolOutputNonce() in trust.go:62 still panics on crypto/rand failure (not a recovered site, not an audited AG-### finding). WARNING; covered by the Runner per-turn backstop; crypto/rand failure extremely rare."
  - phase: "Phase 26"
    items:
      - "AppShell.conversation.test.tsx flakes (1/5) under parallel coverage CPU load; passes in isolation. Cosmetic, not a production bug."
  - phase: "Phase 23"
    items:
      - "Two live-CI items carried as human_needed: web-dist-freshness byte-canonical reconciliation (Windows Node-22 dist vs Linux Node-24) + Playwright web-e2e live boot. Documented expected reconciliation path, not a code gap."
  - phase: "Phase 25"
    items:
      - "6 live UAT items (SSE streaming feel, reasoning-drawer persistence, branch nav, cross-thread HITL flow, footer live update, live Playwright non-golden path) carried into the cockpit-overhaul live cutover. See 25-HUMAN-UAT.md."
  - phase: "Phase 27"
    items:
      - "No 27-VERIFICATION.md (operator-closed via 27-VALIDATION.md). Backend go-mutesting spot-check on graphview*.go not separately recorded (frontend Stryker 75.88% is). Uncommitted post-close graph WIP (web/src/graph/* + dist rebuild + untracked web/e2e/_uiux-shots.spec.ts) not covered by the closure."
  - phase: "cross-cutting (working tree)"
    items:
      - "A large cockpit-overhaul frontend layer + embedded Authula auth are UNCOMMITTED in the working tree (reworked the P23/24/25 surfaces in place). The committed/verified phase states differ from current on-disk code. The integration checker read the on-disk (overhauled) code. Commit + live cutover pending."
---

# Milestone v1.0.0 — Aura Deep Search Web Cockpit — Audit

**Audited:** 2026-06-19
**Status:** `gaps_found` (milestone still 6/9 phases — see Scope; the **completed-surface** findings below are now remediated)
**Auditor:** Claude (gsd-audit-milestone orchestrator)

## Remediation Update (2026-06-19, post-audit)

The three actionable completed-surface findings were fixed + validated E2E on the live Docker stack (real OpenRouter + Playwright/Chromium). The unbuilt-phase requirements (GOV/ONBD/MCPW/SKW, Phases 28–30) remain — status stays `gaps_found`.

- **SWARM-01 (+ DISP-01/02 `code`) — RESOLVED.** Root cause was deeper than the audit's "decode gap": (1) `NormalizeToolPreview` only decoded web tools (commit `9462d216` adds `swarm_spawn`→`[]ChildReport` and `shell_exec`/`sandbox_exec`→`CodeInput`, footer-stripped); (2) a **pre-existing translator branch-order bug** — the tool-result event carries `Actions.Display` on the SAME event as the `ToolInvocation`, and the translator `continue`d after `TOOL_CALL_RESULT`, so the `aura.display` CUSTOM event was **never emitted live for ANY typed display** (web_result/swarm_report/code), masked because Phase 26 tests fed standalone events / mocked the frame (commit `e51e4516` emits the CUSTOM payloads inline). **Live proof:** a real OpenRouter `swarm_spawn` turn now streams `aura.display {type:swarm_report}` and renders the "Workers" card (w1/w2 ● OK + haiku summaries) in the cockpit browser DOM; a `shell_exec` turn streams `aura.display {type:code}` (footer stripped). Playwright `displays.spec` 9/9 + `graph.spec` 3/3 + `graph-a11y.spec` 3/3 green against the live serve.
- **GRAPH-01..04 verification-artifact gap — RESOLVED.** `27-VERIFICATION.md` written (live-backed: graph REST contract via curl + graph Playwright DOM). The "uncommitted post-close graph WIP" is moot — the working tree is clean at HEAD (the WIP was committed).
- **WEB-02 SUMMARY-frontmatter hygiene — RESOLVED.** `requirements-completed: [WEB-02]` added to `24-01-SUMMARY.md` (commit `7b0558bf`); `summary-extract` now traces it.

Commits: `9462d216` (decode), `7b0558bf` (WEB-02 doc), `e51e4516` (translator live-emission). Not pushed.

## Scope & Framing

This is a **mid-milestone audit**, not a pre-archival close. The milestone has **9 phases (22–30)**; **6 are built (22–27)** and **3 are unbuilt (28, 29, 30)**. `init.milestone-op` reports `all_phases_complete: false` (6/9, 67%).

The `gaps_found` status is therefore **expected** — it is driven primarily by the 11 requirements that belong to the two unbuilt phases (28, 29), plus the unplanned Phase 30. The substantive value of this audit is in the **completed cockpit surface (22–27)**: its requirement coverage, its one verification-artifact gap, and the cross-phase integration check.

## Requirements Coverage (3-source cross-reference: VERIFICATION + SUMMARY frontmatter + REQUIREMENTS traceability)

| Category | Phase | Reqs | Built? | Status |
|---|---|---|---|---|
| HARDEN-01..12 | 22 | 12 | ✅ | **Satisfied** — VERIFICATION `passed` (signed off 06-16), all 3 sources agree |
| FND-01..06 | 23 | 6 | ✅ | **Satisfied** — VERIFICATION `human_needed` (2 live-CI items, no code gap), all 3 sources agree |
| WEB-01..04 | 24 | 4 | ✅ | **Satisfied** — VERIFICATION `passed`; ⚠ WEB-02 missing from SUMMARY frontmatter (hygiene only) |
| CHAT-01..05, APRV-01..03 | 25 | 8 | ✅ | **Satisfied** — VERIFICATION `closed-carried-forward` (8/8 automated; 6 live UAT carried fwd) |
| DISP-01..05, SWARM-01 | 26 | 6 | ✅/⚠ | **Satisfied** except **SWARM-01 partial** (live-wiring gap — see integration) |
| GRAPH-01..04 | 27 | 4 | ⚠ | **Partial** — operator-closed via VALIDATION.md; **no VERIFICATION.md** (verification-artifact gap) |
| GOV-01..03, ONBD-01..02 | 28 | 5 | ❌ | **Unsatisfied** — phase not built |
| MCPW-01..03, SKW-01..03 | 29 | 6 | ❌ | **Unsatisfied** — phase not built |
| (Telegram onboarding) | 30 | 0 (TBD) | ❌ | **Unplanned** — SPEC only, no requirements assigned |

**Tally:** 40/51 requirements built+verified · 11 unsatisfied (unbuilt P28/P29) · SWARM-01 partial · GRAPH-01..04 carry a verification-artifact gap.

**Orphan detection:** No orphaned requirements. Every REQ-ID in the traceability table maps to a phase; every built phase's requirements appear in its SUMMARY frontmatter (sole exception: WEB-02, a frontmatter omission — the code + VERIFICATION evidence exist).

## Phase Verification Roll-up

| Phase | VERIFICATION.md | Status | Score | Note |
|---|---|---|---|---|
| 22 Agent Perimeter Hardening | ✅ | `passed` (signed off) | 12/12 | B3 live sign-off 9/9 (06-16) |
| 23 Frontend Foundation | ✅ | `human_needed` | 5/5 auto | 2 live-CI items, no code gap |
| 24 Web Foundation (serve/auth/health) | ✅ | `passed` | 4/4 | both human items resolved |
| 25 Chat + Approval Center | ✅ | `closed-carried-forward` | 8/8 auto | 6 live UAT carried into overhaul cutover |
| 26 Typed-Display Protocol + Router | ✅ | `passed`/closed | 6/6 | live items cleared via Playwright matrix |
| 27 Neo4j Graph Explorer | ❌ **missing** | VALIDATION `complete`, operator-closed | per-task green | **No VERIFICATION.md** — verification-artifact gap |

## Cross-Phase Integration (gsd-integration-checker)

**Verdict:** the cockpit chain **22→27 wires end-to-end: yes**, with one functional caveat.

- **6/6 E2E flows wired:** boot→serve→SPA · auth boundary · chat turn (SSE→tokens→footer) · typed display in chat · HITL approval · graph explorer (REST) · cache-invariant preserved · replay parity.
- **0 unprotected routes:** all 12 `/api/*` routes inherit `RequireAuth`; the 4 mutating routes additionally pass `RequireCapability`; graph routes read-only behind `RequireAuth`-only (correct for the read-only milestone).
- **0 broken flows.**

**Findings:**
1. **WARNING — SWARM-01 / `code` typed-display not wired into the live path.** The live event pipeline (`llm_agent_events.go:152` → `deriveDisplay` → `NormalizeToolPreview`) only decodes `web_search`/`web_fetch` (`preview.go:55` `default: return nil, false`). `swarm_spawn`/`shell_exec`/`sandbox_exec` → `Actions.Display==nil` → raw-card fallback in **both live and replay**. `SwarmReportTable`/`CodeDisplay` + their `DisplayRouter` cases are committed and tested but unreachable in production. Safe (no crash), but the typed card never renders. Fix: add `decodeToolPreview` cases for these tools' JSON preview shape. Affects SWARM-01, DISP-01 (partial), DISP-04 (replay).
2. **WARNING (by design) — graph best-effort boot.** `SetGraphView` only fires on a successful boot-time `knowledge.Open`; if Neo4j/mcp-neo4j-cypher is down at boot, both graph routes 503 for the daemon lifetime with no retry; frontend shows a generic error, not "graph not configured". (`serve.go:307-312`)
3. **INFO — Authula in committed path** (flag-gated, defaults to passphrase, nil-guarded); the full overhaul + SPA login remain uncommitted.
4. **INFO — asset upload needs auth on loopback dev** (`assets_api.go:159`); not a production gap.

## Nyquist / Validation

The `validate-phase` `verify:post` step hook is **not active** in this configuration, so the formal Nyquist gate is skipped per the workflow. Informational: all six VALIDATION.md carry `nyquist_compliant: true`, but only **22** (`validated`) and **27** (`complete`) reached a terminal validation status; **23/24/25/26** VALIDATION.md remain at planning stage (`planned`/`draft`, `wave_0_complete: false`) — i.e., the validation contract was authored but not flipped to a post-execution validated record (those phases were instead carried by their VERIFICATION.md).

## Gate Decision

**FAIL gate fires** (`gaps_found`): 11 unsatisfied requirements (Phases 28, 29 unbuilt). Additionally surfaced for the completed surface: the Phase-27 verification-artifact gap and the SWARM-01 live-wiring integration gap.

## Recommended Next Steps

The milestone is mid-flight, so the dominant "gap" (3 unbuilt phases) is just remaining roadmap work, not remediation. The actionable items for the **completed** surface are:

1. **(Verification-artifact gap)** Produce a `27-VERIFICATION.md` for Phase 27 (or formally accept the operator-close + VALIDATION.md as the verification record) and reconcile/commit-or-discard the uncommitted post-close graph WIP.
2. **(Integration WARNING)** Decide SWARM-01: either wire `swarm_spawn`/`code` into `NormalizeToolPreview`'s live decode path (small, localized fix in `internal/agent/display/preview.go`), or formally re-scope SWARM-01's acceptance to the component level + raw-card live fallback (PRD-first). The same fix lights up the `code` typed display live.
3. **(Hygiene)** Add WEB-02 to the Phase-24 SUMMARY `requirements_completed` frontmatter.
4. **(Continue the milestone)** Plan the remaining phases: `/gsd-plan-phase 28`, then 29, then 30.
5. **(Working tree)** Commit the cockpit-overhaul + Authula layer and run the carried-forward P23/P25 live UAT once against the committed overhaul stack (folded into the overhaul cutover, not re-run against pre-overhaul surfaces).
