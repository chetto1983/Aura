// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: compaction.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getActiveCompactionPointer = `-- name: GetActiveCompactionPointer :one
SELECT conversation_id, branch_id, generation, checkpoint_id, updated_at FROM aura.compaction_active_pointers WHERE conversation_id = $1 AND branch_id = $2
`

type GetActiveCompactionPointerParams struct {
	ConversationID pgtype.UUID `json:"conversation_id"`
	BranchID       string      `json:"branch_id"`
}

func (q *Queries) GetActiveCompactionPointer(ctx context.Context, arg GetActiveCompactionPointerParams) (AuraCompactionActivePointers, error) {
	row := q.db.QueryRow(ctx, getActiveCompactionPointer, arg.ConversationID, arg.BranchID)
	var i AuraCompactionActivePointers
	err := row.Scan(
		&i.ConversationID,
		&i.BranchID,
		&i.Generation,
		&i.CheckpointID,
		&i.UpdatedAt,
	)
	return i, err
}

const getCompactionCheckpoint = `-- name: GetCompactionCheckpoint :one
SELECT id, conversation_id, branch_id, generation, parent_id, captured_watermark_seq, summarized_turn_seqs, tail_turn_seqs, protected_turn_seqs, excluded_turn_seqs, manifest_digest, complete_capture_digest, digest_algorithm, digest_version, structured_summary, schema_version, prompt_version, projection_version, metadata, quality_state, rollout_mode, retention_until, created_at FROM aura.compaction_checkpoints WHERE id = $1
`

func (q *Queries) GetCompactionCheckpoint(ctx context.Context, id pgtype.UUID) (AuraCompactionCheckpoints, error) {
	row := q.db.QueryRow(ctx, getCompactionCheckpoint, id)
	var i AuraCompactionCheckpoints
	err := row.Scan(
		&i.ID,
		&i.ConversationID,
		&i.BranchID,
		&i.Generation,
		&i.ParentID,
		&i.CapturedWatermarkSeq,
		&i.SummarizedTurnSeqs,
		&i.TailTurnSeqs,
		&i.ProtectedTurnSeqs,
		&i.ExcludedTurnSeqs,
		&i.ManifestDigest,
		&i.CompleteCaptureDigest,
		&i.DigestAlgorithm,
		&i.DigestVersion,
		&i.StructuredSummary,
		&i.SchemaVersion,
		&i.PromptVersion,
		&i.ProjectionVersion,
		&i.Metadata,
		&i.QualityState,
		&i.RolloutMode,
		&i.RetentionUntil,
		&i.CreatedAt,
	)
	return i, err
}

const getCompactionClaim = `-- name: GetCompactionClaim :one
SELECT operation_id, conversation_id, branch_id, idempotency_key, captured_watermark_seq, governance_watermark, base_active_generation, priority, state, inference_started, owner_id, lease_until, outcome_checkpoint_id, created_at, updated_at FROM aura.compaction_claims WHERE operation_id = $1
`

func (q *Queries) GetCompactionClaim(ctx context.Context, operationID pgtype.UUID) (AuraCompactionClaims, error) {
	row := q.db.QueryRow(ctx, getCompactionClaim, operationID)
	var i AuraCompactionClaims
	err := row.Scan(
		&i.OperationID,
		&i.ConversationID,
		&i.BranchID,
		&i.IdempotencyKey,
		&i.CapturedWatermarkSeq,
		&i.GovernanceWatermark,
		&i.BaseActiveGeneration,
		&i.Priority,
		&i.State,
		&i.InferenceStarted,
		&i.OwnerID,
		&i.LeaseUntil,
		&i.OutcomeCheckpointID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listCompatibleCompactionCheckpoints = `-- name: ListCompatibleCompactionCheckpoints :many
SELECT id, conversation_id, branch_id, generation, parent_id, captured_watermark_seq, summarized_turn_seqs, tail_turn_seqs, protected_turn_seqs, excluded_turn_seqs, manifest_digest, complete_capture_digest, digest_algorithm, digest_version, structured_summary, schema_version, prompt_version, projection_version, metadata, quality_state, rollout_mode, retention_until, created_at FROM aura.compaction_checkpoints
WHERE conversation_id = $1 AND branch_id = $2 AND schema_version = $3 AND projection_version = $4
ORDER BY generation DESC LIMIT $5
`

type ListCompatibleCompactionCheckpointsParams struct {
	ConversationID    pgtype.UUID `json:"conversation_id"`
	BranchID          string      `json:"branch_id"`
	SchemaVersion     int32       `json:"schema_version"`
	ProjectionVersion int32       `json:"projection_version"`
	Limit             int32       `json:"limit"`
}

func (q *Queries) ListCompatibleCompactionCheckpoints(ctx context.Context, arg ListCompatibleCompactionCheckpointsParams) ([]AuraCompactionCheckpoints, error) {
	rows, err := q.db.Query(ctx, listCompatibleCompactionCheckpoints,
		arg.ConversationID,
		arg.BranchID,
		arg.SchemaVersion,
		arg.ProjectionVersion,
		arg.Limit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuraCompactionCheckpoints{}
	for rows.Next() {
		var i AuraCompactionCheckpoints
		if err := rows.Scan(
			&i.ID,
			&i.ConversationID,
			&i.BranchID,
			&i.Generation,
			&i.ParentID,
			&i.CapturedWatermarkSeq,
			&i.SummarizedTurnSeqs,
			&i.TailTurnSeqs,
			&i.ProtectedTurnSeqs,
			&i.ExcludedTurnSeqs,
			&i.ManifestDigest,
			&i.CompleteCaptureDigest,
			&i.DigestAlgorithm,
			&i.DigestVersion,
			&i.StructuredSummary,
			&i.SchemaVersion,
			&i.PromptVersion,
			&i.ProjectionVersion,
			&i.Metadata,
			&i.QualityState,
			&i.RolloutMode,
			&i.RetentionUntil,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
