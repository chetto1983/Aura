[
  {
    "name": "calendar__calendar",
    "deferred": true,
    "summary": "Unified access to email, calendar, and contacts across Microsoft 365, Google Workspace/Gmail, Outlook.com, and IMAP/SMTP mailboxes. Exactly one action per call, selected via the required `action` argument: Required args: action.",
    "description": "Unified access to email, calendar, and contacts across Microsoft 365, Google Workspace/Gmail, Outlook.com, and IMAP/SMTP mailboxes. Exactly one action per call, selected via the required `action` argument:\n- list_accounts: list configured accounts. No arguments.\n- get_emails: recent emails, newest first. accountId optional; count, unreadOnly optional.\n- get_email_details: full email body and attachments. Requires accountId, emailId.\n- search_emails: full-text email search. Requires query. accountId, count, fromDate, toDate optional.\n- send_email: send an email, optional attachments. Requires to, subject. accountId, body, bodyFormat, cc, attachments, textBody, htmlBody optional.\n- list_calendars: list calendars. accountId optional.\n- get_calendar_events: events in a date range. Requires timeZone. accountId, calendarId, startDate, endDate, count optional. Each returned event's eventId is an opaque reference -- pass it unchanged to get_calendar_event_details.\n- get_calendar_event_details: full event detail. Requires timeZone, calendarId, and eventId from get_calendar_events. No accountId -- the account is resolved from eventId.\n- create_event: create an event. Requires subject, start, end. accountId, calendarId, location, attendees, body, timeZone optional.\n- update_event: update an event. Requires accountId, calendarId, eventId. subject, start, end, location, attendees, timeZone optional.\n- respond_to_event: accept, tentative, or decline an invite. Requires eventId, response. accountId, calendarId, comment optional.\n- get_contacts: list contacts. accountId, count optional.\n- search_contacts: search contacts. Requires query. accountId, count optional.\n- get_contact_details: full contact detail. Requires accountId, contactId.\n- delete_email: delete an email. Requires accountId, emailId. Google trashes it; Microsoft deletes outright.\n- mark_email_read: mark an email read or unread. Requires accountId, emailId, isRead.\n- move_email: move an email to a folder or label. Requires accountId, emailId, destination.\n- delete_event: delete a calendar event. Requires eventId; pass accountId (and calendarId) or the first account is used.\n- create_contact: create a contact. Requires displayName. accountId, givenName, surname, email, phone, jobTitle, companyName, notes optional.\n- update_contact: update a contact. Requires accountId, contactId. Any of displayName, givenName, surname, email, phone, jobTitle, companyName, notes.\n- delete_contact: delete a contact. Requires accountId, contactId.\n- get_email_attachment: fetch one attachment. Requires accountId, emailId, attachmentId. mode 'stash' (default) or 'inline'.\n- get_contextual_email_summary: cluster recent mail into topics. topics, countPerAccount, unreadOnly, includeBodyPreview, maxSamplesPerCluster optional.\n- get_guide: read an in-depth topical guide. Omit name (or pass 'index') for the list.\n- get_unsubscribe_info: report how a mailing can be unsubscribed from. Requires accountId, emailId.\n- unsubscribe_from_email: act on that unsubscribe. Requires accountId, emailId. method 'auto' (default), 'http' or 'mailto'.\n- bulk_delete_emails: delete several emails. Requires items, each with accountId and emailId.\n- bulk_mark_emails_read: mark several emails read or unread. Requires items and isRead.\n- bulk_move_emails: move several emails. Requires items and destination.",
    "parameters": {
      "properties": {
        "accountId": {
          "default": null,
          "description": "Account id. A defaultable routing hint on get_emails/search_emails/list_calendars/get_calendar_events/get_contacts/search_contacts/create_event/respond_to_event (omit to use all accounts or smart routing). Required (not a hint) on get_email_details, get_contact_details, update_event, delete_email, mark_email_read, move_email. NOT used by get_calendar_event_details -- pass its eventId instead. Obtain from list_accounts.",
          "type": [
            "string",
            "null"
          ]
        },
        "action": {
          "description": "Required. The operation to perform -- see the tool description for each action's required and optional fields.",
          "enum": [
            "list_accounts",
            "get_emails",
            "get_email_details",
            "search_emails",
            "list_calendars",
            "get_calendar_events",
            "get_calendar_event_details",
            "get_contacts",
            "search_contacts",
            "get_contact_details",
            "create_event",
            "update_event",
            "respond_to_event",
            "send_email",
            "delete_email",
            "mark_email_read",
            "move_email",
            "delete_event",
            "create_contact",
            "update_contact",
            "delete_contact",
            "get_email_attachment",
            "get_contextual_email_summary",
            "get_guide",
            "get_unsubscribe_info",
            "unsubscribe_from_email",
            "bulk_delete_emails",
            "bulk_mark_emails_read",
            "bulk_move_emails"
          ],
          "type": "string"
        },
        "attachmentId": {
          "default": null,
          "description": "get_email_attachment only. Required. Attachment id from get_email_details.",
          "type": [
            "string",
            "null"
          ]
        },
        "attachments": {
          "default": null,
          "description": "send_email only. Optional file attachments. Each item sets EITHER attachmentId (from a prior upload) OR base64Content (small files only); name is required with base64Content. Total decoded payload per message must stay under 25 MB.",
          "items": {
            "properties": {
              "attachmentId": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "base64Content": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "contentType": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "name": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "type": [
              "object",
              "null"
            ]
          },
          "type": [
            "array",
            "null"
          ]
        },
        "attendees": {
          "default": null,
          "description": "create_event/update_event. List of attendee email addresses.",
          "items": {
            "type": [
              "string",
              "null"
            ]
          },
          "type": [
            "array",
            "null"
          ]
        },
        "body": {
          "default": null,
          "description": "Body content. send_email: ignored when bodyFormat is 'multipart' (use textBody/htmlBody instead). create_event: optional description.",
          "type": [
            "string",
            "null"
          ]
        },
        "bodyFormat": {
          "default": null,
          "description": "send_email only. Body content format: 'html' (default), 'text', or 'multipart' (then set textBody and htmlBody instead of body).",
          "type": [
            "string",
            "null"
          ]
        },
        "calendarId": {
          "default": null,
          "description": "Calendar id. Required for get_calendar_event_details and update_event; optional scoping filter for get_calendar_events; optional target for create_event. Obtain from list_calendars, or pass 'primary' for the default calendar.",
          "type": [
            "string",
            "null"
          ]
        },
        "cc": {
          "default": null,
          "description": "send_email only. CC recipient email addresses.",
          "items": {
            "type": [
              "string",
              "null"
            ]
          },
          "type": [
            "array",
            "null"
          ]
        },
        "comment": {
          "default": null,
          "description": "respond_to_event only. Optional message to include with the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "companyName": {
          "default": null,
          "description": "create_contact/update_contact. Company name.",
          "type": [
            "string",
            "null"
          ]
        },
        "contactId": {
          "default": null,
          "description": "Contact id. Required for get_contact_details. Obtain from get_contacts or search_contacts.",
          "type": [
            "string",
            "null"
          ]
        },
        "count": {
          "default": null,
          "description": "Maximum number of results to return. Applies to get_emails (default 20), search_emails (default 20), get_calendar_events (default 50), get_contacts (default 50), search_contacts (default 50).",
          "type": [
            "integer",
            "null"
          ]
        },
        "countPerAccount": {
          "default": null,
          "description": "get_contextual_email_summary only. How many emails to scan per account. Default 50.",
          "type": [
            "integer",
            "null"
          ]
        },
        "destination": {
          "default": null,
          "description": "move_email only. Required. Destination: 'archive', 'inbox', 'trash', 'spam', 'drafts' (Microsoft only), 'sentitems' (Microsoft only), or a custom label/folder id (Google only). Aliases: 'deleteditems'='trash', 'junkemail'='spam'.",
          "type": [
            "string",
            "null"
          ]
        },
        "displayName": {
          "default": null,
          "description": "create_contact/update_contact. Contact display name. Required for create_contact.",
          "type": [
            "string",
            "null"
          ]
        },
        "email": {
          "default": null,
          "description": "create_contact/update_contact. Primary email address.",
          "type": [
            "string",
            "null"
          ]
        },
        "emailId": {
          "default": null,
          "description": "Email id. Required for get_email_details, delete_email, mark_email_read and move_email. Obtain from the id field returned by get_emails or search_emails.",
          "type": [
            "string",
            "null"
          ]
        },
        "end": {
          "default": null,
          "description": "create_event/update_event. Event end date and time (ISO 8601). Required for create_event.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "endDate": {
          "default": null,
          "description": "get_calendar_events only. End of the date range, inclusive (ISO 8601). Defaults to 7 days after startDate.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "eventId": {
          "default": null,
          "description": "Event id. For get_calendar_event_details this is the OPAQUE eventId returned per event by get_calendar_events -- pass it back unchanged; do not construct or guess one. For update_event/respond_to_event this is the plain event id from get_calendar_events or get_calendar_event_details.",
          "type": [
            "string",
            "null"
          ]
        },
        "fromDate": {
          "default": null,
          "description": "search_emails only. Only return emails received on or after this date (ISO 8601, e.g. '2026-02-01').",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "givenName": {
          "default": null,
          "description": "create_contact/update_contact. Given (first) name.",
          "type": [
            "string",
            "null"
          ]
        },
        "htmlBody": {
          "default": null,
          "description": "send_email only. HTML body for multipart/alternative messages. Required when bodyFormat is 'multipart'.",
          "type": [
            "string",
            "null"
          ]
        },
        "includeBodyPreview": {
          "default": null,
          "description": "get_contextual_email_summary only. Include a short body preview with each sample. Default false.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "isRead": {
          "default": null,
          "description": "mark_email_read only. Required. True to mark the email read, false to mark it unread.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "items": {
          "default": null,
          "description": "bulk_delete_emails/bulk_mark_emails_read/bulk_move_emails. Required. The emails to act on, each item carrying its accountId and emailId.",
          "items": {
            "properties": {
              "accountId": {
                "type": "string"
              },
              "emailId": {
                "type": "string"
              }
            },
            "type": [
              "object",
              "null"
            ]
          },
          "type": [
            "array",
            "null"
          ]
        },
        "jobTitle": {
          "default": null,
          "description": "create_contact/update_contact. Job title.",
          "type": [
            "string",
            "null"
          ]
        },
        "location": {
          "default": null,
          "description": "create_event/update_event. Event location.",
          "type": [
            "string",
            "null"
          ]
        },
        "maxSamplesPerCluster": {
          "default": null,
          "description": "get_contextual_email_summary only. Maximum sample emails shown per cluster. Default 5.",
          "type": [
            "integer",
            "null"
          ]
        },
        "method": {
          "default": null,
          "description": "unsubscribe_from_email only. Unsubscribe method: 'auto' (default), 'http', or 'mailto'.",
          "type": [
            "string",
            "null"
          ]
        },
        "mode": {
          "default": null,
          "description": "get_email_attachment only. 'stash' (default) writes the attachment to the attachment store and returns a handle; 'inline' returns base64 content.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "default": null,
          "description": "get_guide only. Guide name; omit (or pass 'index') for the list of available guides.",
          "type": [
            "string",
            "null"
          ]
        },
        "notes": {
          "default": null,
          "description": "create_contact/update_contact. Free-text notes.",
          "type": [
            "string",
            "null"
          ]
        },
        "phone": {
          "default": null,
          "description": "create_contact/update_contact. Primary phone number.",
          "type": [
            "string",
            "null"
          ]
        },
        "query": {
          "default": null,
          "description": "Search query. Required for search_emails and search_contacts.",
          "type": [
            "string",
            "null"
          ]
        },
        "response": {
          "default": null,
          "description": "respond_to_event only. Required. One of: 'accept', 'tentative', 'decline'.",
          "type": [
            "string",
            "null"
          ]
        },
        "start": {
          "default": null,
          "description": "create_event/update_event. Event start date and time (ISO 8601). Required for create_event.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "startDate": {
          "default": null,
          "description": "get_calendar_events only. Start of the date range (ISO 8601, e.g. '2026-02-20'). Defaults to today.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "subject": {
          "default": null,
          "description": "Subject line. Required for send_email and create_event; optional field to change on update_event.",
          "type": [
            "string",
            "null"
          ]
        },
        "surname": {
          "default": null,
          "description": "create_contact/update_contact. Surname (last name).",
          "type": [
            "string",
            "null"
          ]
        },
        "textBody": {
          "default": null,
          "description": "send_email only. Plain-text body for multipart/alternative messages. Required when bodyFormat is 'multipart'.",
          "type": [
            "string",
            "null"
          ]
        },
        "timeZone": {
          "default": null,
          "description": "IANA timezone name (e.g. 'America/Chicago', 'Europe/London'). Required for get_calendar_events and get_calendar_event_details; used to create/update events at the correct local time on create_event/update_event.",
          "type": [
            "string",
            "null"
          ]
        },
        "to": {
          "default": null,
          "description": "send_email only. Required. Recipient email address(es) as a JSON array, e.g. [\"alice@example.com\"].",
          "items": {
            "type": [
              "string",
              "null"
            ]
          },
          "type": [
            "array",
            "null"
          ]
        },
        "toDate": {
          "default": null,
          "description": "search_emails only. Only return emails received on or before this date (ISO 8601, e.g. '2026-02-28').",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "topics": {
          "default": null,
          "description": "get_contextual_email_summary only. Comma-separated topics to cluster around; omit to let the summary choose.",
          "type": [
            "string",
            "null"
          ]
        },
        "unreadOnly": {
          "default": null,
          "description": "get_emails only. If true, only return unread emails. Default false.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    }
  },
  {
    "name": "current_time",
    "deferred": true,
    "summary": "Get the current date and time as an RFC-3339 string.",
    "description": "Returns the current instant formatted as RFC-3339 IN THE OPERATOR'S TIMEZONE, with the UTC offset and the zone name (e.g. '2026-08-16T17:49:04+02:00 (Europe/Rome, CEST)'). The conversion is already done: report the time as given and never recompute an offset. Pass an IANA timezone name only to ask about a DIFFERENT zone.",
    "parameters": {
      "type": "object",
      "properties": {
        "timezone": {
          "type": "string",
          "description": "Optional IANA timezone name (e.g. 'Europe/Rome'). Defaults to the operator's own timezone, already converted -- do NOT convert the result yourself."
        }
      }
    }
  },
  {
    "name": "linear__create_attachment",
    "deferred": true,
    "summary": "Deprecated fallback for tiny files only. Accepts base64 file content, verifies SHA-256 checksum, and uploads it through the MCP worker. Required args: issue, base64Content, filename, contentType, sha256.",
    "description": "Deprecated fallback for tiny files only. Accepts base64 file content, verifies SHA-256 checksum, and uploads it through the MCP worker.\nPrefer `prepare_attachment_upload` plus direct PUT plus `create_attachment_from_upload`.\n\nCRITICAL: Do not print base64Content and then copy it into this tool call.\nOpaque base64 copied through model-visible text is easy to corrupt.\n\nGenerate base64Content mechanically from the source bytes and pass it through a programmatic argument construction path whenever available. Before calling this tool, verify the values are consistent:\n\nUnix-like shells:\nfile=\"/path/to/file\"\nbase64Content=$(base64 < \"$file\" | tr -d '\\n\\r')\nsha256=$(shasum -a 256 \"$file\" | awk '{print $1}')\ndecodedSha256=$(printf '%s' \"$base64Content\" | base64 -d | shasum -a 256 | awk '{print $1}')\nsize=$(wc -c < \"$file\" | tr -d ' ') # optional; stat can also get file byte size\ndecodedSize=$(printf '%s' \"$base64Content\" | base64 -d | wc -c | tr -d ' ')\n\nPowerShell:\n$file = 'C:\\path\\to\\file'\n$bytes = [IO.File]::ReadAllBytes($file)\n$base64Content = [Convert]::ToBase64String($bytes)\n$sha256 = (Get-FileHash -Algorithm SHA256 -Path $file).Hash.ToLower()\n$size = $bytes.Length # optional\n$decoded = [Convert]::FromBase64String($base64Content)\n$decodedStream = [IO.MemoryStream]::new($decoded)\n$decodedSha256 = (Get-FileHash -Algorithm SHA256 -InputStream $decodedStream).Hash.ToLower()\n$decodedSize = $decoded.Length\n\ndecodedSha256 must equal sha256. If you pass size, decodedSize must equal size.\nPass `sha256`. Passing `size` is optional, but recommended for clearer mismatch errors.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "base64Content": {
          "description": "Deprecated base64-encoded file content to upload",
          "type": "string"
        },
        "contentType": {
          "description": "MIME type for the upload (e.g., 'image/png', 'application/pdf')",
          "type": "string"
        },
        "filename": {
          "description": "Filename for the upload (e.g., 'screenshot.png')",
          "type": "string"
        },
        "issue": {
          "description": "Issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "sha256": {
          "description": "Expected SHA-256 hex digest of the decoded file bytes.",
          "pattern": "^[a-fA-F0-9]{64}$",
          "type": "string"
        },
        "size": {
          "description": "Optional expected decoded file size in bytes. Rejects the upload if it does not match.",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991,
          "type": "integer"
        },
        "subtitle": {
          "description": "Optional subtitle for the attachment",
          "type": "string"
        },
        "title": {
          "description": "Optional title for the attachment",
          "type": "string"
        }
      },
      "required": [
        "issue",
        "base64Content",
        "filename",
        "contentType",
        "sha256"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__create_attachment_from_upload",
    "deferred": true,
    "summary": "Link an already-uploaded Linear assetUrl to an existing issue as an attachment. Required args: issue, assetUrl.",
    "description": "Link an already-uploaded Linear assetUrl to an existing issue as an attachment.\n\nUse this only after:\n1. prepare_attachment_upload returned an assetUrl and uploadRequest.\n2. The client successfully PUT raw file bytes to uploadRequest.url.\n\nThis tool does not upload file content. It only creates the Linear attachment row.\nIf the direct upload failed or the signed URL expired, rerun prepare_attachment_upload and upload again.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "assetUrl": {
          "description": "Linear upload assetUrl returned by prepare_attachment_upload",
          "format": "uri",
          "type": "string"
        },
        "issue": {
          "description": "Issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "subtitle": {
          "description": "Optional attachment subtitle",
          "type": "string"
        },
        "title": {
          "description": "Attachment title. Defaults to filename or asset URL",
          "type": "string"
        }
      },
      "required": [
        "issue",
        "assetUrl"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__create_issue_label",
    "deferred": true,
    "summary": "Create a new Linear issue label Required args: name.",
    "description": "Create a new Linear issue label",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "color": {
          "description": "Hex color code",
          "type": "string"
        },
        "description": {
          "description": "Label description",
          "type": "string"
        },
        "isGroup": {
          "default": false,
          "description": "Is label group (not directly applicable)",
          "type": "boolean"
        },
        "name": {
          "description": "Label name",
          "type": "string"
        },
        "parent": {
          "description": "Parent label group name",
          "type": "string"
        },
        "teamId": {
          "description": "Team UUID (omit for workspace label)",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__delete_attachment",
    "deferred": true,
    "summary": "Delete an attachment by ID Required args: id.",
    "description": "Delete an attachment by ID",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Attachment ID",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__delete_comment",
    "deferred": true,
    "summary": "Delete a Linear comment. Inline description comments (those with non-null `quotedText`) anchor a mark in the editor, so their root cannot be deleted — delete the replies individually or resolve the thread instead. Required args: id.",
    "description": "Delete a Linear comment. Inline description comments (those with non-null `quotedText`) anchor a mark in the editor, so their root cannot be deleted — delete the replies individually or resolve the thread instead.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Comment ID",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__delete_diff_comment",
    "deferred": true,
    "summary": "Delete a comment or persisted draft from a Linear diff",
    "description": "Delete a comment or persisted draft from a Linear diff",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "commentId": {
          "description": "Submitted diff comment ID to delete",
          "minLength": 1,
          "type": "string"
        },
        "draftId": {
          "description": "Persisted diff comment draft ID to delete",
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__delete_status_update",
    "deferred": true,
    "summary": "Delete (archive) a project or initiative status update. Required args: type, id.",
    "description": "Delete (archive) a project or initiative status update.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Status update ID",
          "type": "string"
        },
        "type": {
          "description": "Type of status update",
          "enum": [
            "project",
            "initiative"
          ],
          "type": "string"
        }
      },
      "required": [
        "type",
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__extract_images",
    "deferred": true,
    "summary": "Extract and fetch images from markdown content. Use this to view screenshots, diagrams, or other images embedded in Linear issues, comments, or documents. Pass the markdown content (e.g., issue description) and receive the images as viewable data. Required args: markdown.",
    "description": "Extract and fetch images from markdown content. Use this to view screenshots, diagrams, or other images embedded in Linear issues, comments, or documents. Pass the markdown content (e.g., issue description) and receive the images as viewable data.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "markdown": {
          "description": "Markdown content containing image references (e.g., issue description, comment body)",
          "type": "string"
        }
      },
      "required": [
        "markdown"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_agent_skill",
    "deferred": true,
    "summary": "Retrieve a Linear Agent skill by ID, including its full markdown instructions. Required args: id.",
    "description": "Retrieve a Linear Agent skill by ID, including its full markdown instructions.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Agent skill ID",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_attachment",
    "deferred": true,
    "summary": "Retrieve an attachment's content by ID. Required args: id.",
    "description": "Retrieve an attachment's content by ID.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Attachment ID",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_diff",
    "deferred": true,
    "summary": "Exact lookup for a Linear diff. Use with review URLs, GitHub PR URLs, Linear full identifiers, UUIDs, or slugs. Required args: urlOrId.",
    "description": "Exact lookup for a Linear diff. Use with review URLs, GitHub PR URLs, Linear full identifiers, UUIDs, or slugs.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "urlOrId": {
          "description": "Linear review URL, diff slug, pull request ID, Linear full identifier, or GitHub PR URL",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "urlOrId"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_diff_threads",
    "deferred": true,
    "summary": "Exact lookup for diff threads and the authenticated user's drafts. Use with review URLs, GitHub PR URLs, Linear full identifiers, UUIDs, or slugs. Required args: urlOrId.",
    "description": "Exact lookup for diff threads and the authenticated user's drafts. Use with review URLs, GitHub PR URLs, Linear full identifiers, UUIDs, or slugs.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "resolved": {
          "description": "Filter returned threads by resolved state",
          "type": "boolean"
        },
        "threadId": {
          "description": "Optional Linear comment UUID of any comment in the thread (not its `externalThreadId`); a reply's UUID resolves to its thread root",
          "type": "string"
        },
        "urlOrId": {
          "description": "Linear review URL, diff slug, pull request ID, Linear full identifier, or GitHub PR URL",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "urlOrId"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_document",
    "deferred": true,
    "summary": "Retrieve a Linear document by ID or slug Required args: id.",
    "description": "Retrieve a Linear document by ID or slug",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Document ID or slug",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_issue",
    "deferred": true,
    "summary": "Retrieve detailed information about an issue by ID, including attachments, git branch name, and active Triage Intelligence suggestions when the issue is in triage Required args: id.",
    "description": "Retrieve detailed information about an issue by ID, including attachments, git branch name, and active Triage Intelligence suggestions when the issue is in triage",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "includeCustomerNeeds": {
          "default": false,
          "description": "Include associated customer needs",
          "type": "boolean"
        },
        "includeRelations": {
          "default": false,
          "description": "Include blocking/related/duplicate relations",
          "type": "boolean"
        },
        "includeReleases": {
          "default": false,
          "description": "Include associated releases",
          "type": "boolean"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_issue_status",
    "deferred": true,
    "summary": "Retrieve detailed information about an issue status in Linear by name or ID Required args: id, name, team.",
    "description": "Retrieve detailed information about an issue status in Linear by name or ID",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Status ID",
          "type": "string"
        },
        "name": {
          "description": "Status name",
          "type": "string"
        },
        "team": {
          "description": "Team name or ID",
          "type": "string"
        }
      },
      "required": [
        "id",
        "name",
        "team"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_milestone",
    "deferred": true,
    "summary": "Retrieve details of a specific milestone by ID or name Required args: project, query.",
    "description": "Retrieve details of a specific milestone by ID or name",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "project": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        },
        "query": {
          "description": "Milestone name or ID",
          "type": "string"
        }
      },
      "required": [
        "project",
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_project",
    "deferred": true,
    "summary": "Retrieve details of a specific project in Linear Required args: query.",
    "description": "Retrieve details of a specific project in Linear",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "includeMembers": {
          "default": false,
          "description": "Include project members",
          "type": "boolean"
        },
        "includeMilestones": {
          "default": false,
          "description": "Include milestones",
          "type": "boolean"
        },
        "includeResources": {
          "default": false,
          "description": "Include resources (documents, links, attachments)",
          "type": "boolean"
        },
        "query": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_release",
    "deferred": true,
    "summary": "Retrieve details of a release by ID or slug. Required args: id.",
    "description": "Retrieve details of a release by ID or slug.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Release ID or slug",
          "type": "string"
        },
        "includeReleaseNotes": {
          "default": false,
          "description": "Include associated release notes",
          "type": "boolean"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_release_note",
    "deferred": true,
    "summary": "Retrieve release notes by ID or slug, including markdown content. Required args: id.",
    "description": "Retrieve release notes by ID or slug, including markdown content.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Release notes ID or slug",
          "type": "string"
        },
        "includeReleases": {
          "default": false,
          "description": "Include associated releases",
          "type": "boolean"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_status_updates",
    "deferred": true,
    "summary": "List or get project/initiative status updates. Pass `id` to get a specific update, or filter to list. Required args: type.",
    "description": "List or get project/initiative status updates. Pass `id` to get a specific update, or filter to list.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "id": {
          "description": "Status update ID - if provided, returns this specific update",
          "type": "string"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "initiative": {
          "description": "Initiative name, ID, identifier (e.g., INIT-123), or slug",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "project": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        },
        "type": {
          "description": "Type of status update",
          "enum": [
            "project",
            "initiative"
          ],
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "user": {
          "description": "User ID, name, email, or \"me\"",
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_team",
    "deferred": true,
    "summary": "Retrieve details of a specific Linear team Required args: query.",
    "description": "Retrieve details of a specific Linear team",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "query": {
          "description": "Team UUID, key, or name",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_user",
    "deferred": true,
    "summary": "Retrieve details of a specific Linear user Required args: query.",
    "description": "Retrieve details of a specific Linear user",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "query": {
          "description": "User ID, name, email, or \"me\"",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__get_workspace",
    "deferred": true,
    "summary": "Retrieve the connected Linear workspace",
    "description": "Retrieve the connected Linear workspace",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "name": "linear__list_agent_skills",
    "deferred": true,
    "summary": "List Linear Agent skills available to the authenticated user.",
    "description": "List Linear Agent skills available to the authenticated user.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_comments",
    "deferred": true,
    "summary": "List comments on a Linear issue, project, initiative, document, project milestone, or project/initiative status update. Provide exactly one of `issueId`, `projectId`, `initiativeId`, `documentId`, `milestoneId`, or `statusUpdateId`. For issues, projects, and initiatives this returns both top-level discussion threads and inline description comments. Inline (anchored) comments carry a non-null `quotedText` set to the snippet of description text they reference. Returned comments include `author` and nullable `onBehalfOf`; when set, `onBehalfOf` identifies the user the agent author acted for.",
    "description": "List comments on a Linear issue, project, initiative, document, project milestone, or project/initiative status update. Provide exactly one of `issueId`, `projectId`, `initiativeId`, `documentId`, `milestoneId`, or `statusUpdateId`. For issues, projects, and initiatives this returns both top-level discussion threads and inline description comments. Inline (anchored) comments carry a non-null `quotedText` set to the snippet of description text they reference. Returned comments include `author` and nullable `onBehalfOf`; when set, `onBehalfOf` identifies the user the agent author acted for.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "documentId": {
          "description": "Document ID or slug (provide exactly one parent)",
          "type": "string"
        },
        "initiativeId": {
          "description": "Initiative name, ID, identifier (e.g., INIT-123), or slug (provide exactly one parent)",
          "type": "string"
        },
        "issueId": {
          "description": "Issue ID or identifier (e.g., LIN-123) (provide exactly one parent)",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "milestoneId": {
          "description": "Milestone UUID (provide exactly one parent). Resolve milestone names via `list_milestones` first.",
          "type": "string"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "projectId": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug (provide exactly one parent)",
          "type": "string"
        },
        "statusUpdateId": {
          "description": "Status update UUID (provide exactly one parent). Resolve status updates via `get_status_updates` first.",
          "type": "string"
        },
        "statusUpdateType": {
          "description": "Type of status update named by `statusUpdateId`, as returned by `get_status_updates`. Only valid together with `statusUpdateId`; omit to check both project and initiative status updates.",
          "enum": [
            "project",
            "initiative"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_cycles",
    "deferred": true,
    "summary": "Retrieve cycles for a specific Linear team Required args: teamId.",
    "description": "Retrieve cycles for a specific Linear team",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "teamId": {
          "description": "Team ID",
          "type": "string"
        },
        "type": {
          "description": "Filter: current, previous, next, or all",
          "enum": [
            "current",
            "previous",
            "next"
          ],
          "type": "string"
        }
      },
      "required": [
        "teamId"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__list_diffs",
    "deferred": true,
    "summary": "List Linear diff pull requests visible to the authenticated user",
    "description": "List Linear diff pull requests visible to the authenticated user",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "author": {
          "description": "Filter by author ID, name, email, or \"me\"",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "owner": {
          "description": "Filter returned diffs by repository owner",
          "type": "string"
        },
        "query": {
          "description": "Broad search by title, branch, PR number, or bare slug",
          "type": "string"
        },
        "repo": {
          "description": "Filter returned diffs by repository name",
          "type": "string"
        },
        "reviewState": {
          "description": "Reviewer state; requires reviewer and defaults to pending when reviewer is set",
          "enum": [
            "pending",
            "approved",
            "changesRequested",
            "commented",
            "dismissed"
          ],
          "type": "string"
        },
        "reviewer": {
          "description": "Filter by reviewer ID, name, email, or \"me\"",
          "type": "string"
        },
        "status": {
          "description": "Filter returned diffs by pull request status",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_documents",
    "deferred": true,
    "summary": "List documents in the user's Linear workspace",
    "description": "List documents in the user's Linear workspace",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "creatorId": {
          "description": "Filter by creator ID",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "fields": {
          "description": "Fields to include in each result. `id` is always included. Omit or pass an empty array for the default response.",
          "items": {
            "enum": [
              "id",
              "title",
              "content",
              "url",
              "createdAt",
              "updatedAt",
              "archivedAt",
              "creator",
              "updatedBy",
              "project",
              "initiative",
              "team",
              "issue"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "initiativeId": {
          "description": "Filter by initiative ID",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "projectId": {
          "description": "Filter by project ID",
          "type": "string"
        },
        "query": {
          "description": "Search query",
          "type": "string"
        },
        "teamId": {
          "description": "Filter by team ID",
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_issue_labels",
    "deferred": true,
    "summary": "List available issue labels in a Linear workspace or team",
    "description": "List available issue labels in a Linear workspace or team",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "name": {
          "description": "Filter by name",
          "type": "string"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "team": {
          "description": "Team name or ID",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_issue_statuses",
    "deferred": true,
    "summary": "List available issue statuses in a Linear team Required args: team.",
    "description": "List available issue statuses in a Linear team",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "team": {
          "description": "Team name or ID",
          "type": "string"
        }
      },
      "required": [
        "team"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__list_issues",
    "deferred": true,
    "summary": "List issues in the user's Linear workspace, including active Triage Intelligence suggestions for issues in triage. For my issues, use \"me\" as the assignee. Use \"null\" for no assignee.",
    "description": "List issues in the user's Linear workspace, including active Triage Intelligence suggestions for issues in triage. For my issues, use \"me\" as the assignee. Use \"null\" for no assignee.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "assignee": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "User ID, name, email, or \"me\""
        },
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "cycle": {
          "description": "Cycle name, number, or ID",
          "type": "string"
        },
        "delegate": {
          "description": "Agent name or ID. When the user asks to delegate to \"Linear\" or \"the Linear agent\", this refers to the \"Linear\" app user specifically",
          "type": "string"
        },
        "fields": {
          "description": "Fields to include in each result. `id` is always included. Omit or pass an empty array for the default response.",
          "items": {
            "enum": [
              "id",
              "title",
              "description",
              "projectMilestone",
              "priority",
              "estimate",
              "url",
              "gitBranchName",
              "createdAt",
              "updatedAt",
              "archivedAt",
              "completedAt",
              "startedAt",
              "canceledAt",
              "dueDate",
              "slaStartedAt",
              "slaMediumRiskAt",
              "slaHighRiskAt",
              "slaBreachesAt",
              "slaType",
              "status",
              "statusType",
              "labels",
              "triageIntel",
              "createdBy",
              "createdById",
              "assignee",
              "assigneeId",
              "delegate",
              "delegateId",
              "project",
              "projectId",
              "parentId",
              "team",
              "teamId",
              "cycleId"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "label": {
          "description": "Label name or ID",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "parentId": {
          "description": "Parent issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "priority": {
          "description": "0=None, 1=Urgent, 2=High, 3=Medium, 4=Low",
          "type": "number"
        },
        "project": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        },
        "query": {
          "description": "Search issue title or description",
          "type": "string"
        },
        "release": {
          "description": "Release ID or slug",
          "type": "string"
        },
        "state": {
          "description": "State type, name, or ID",
          "type": "string"
        },
        "team": {
          "description": "Team name or ID",
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_milestones",
    "deferred": true,
    "summary": "List all milestones in a Linear project Required args: project.",
    "description": "List all milestones in a Linear project",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "project": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__list_project_labels",
    "deferred": true,
    "summary": "List available project labels in the Linear workspace",
    "description": "List available project labels in the Linear workspace",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "name": {
          "description": "Filter by name",
          "type": "string"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_projects",
    "deferred": true,
    "summary": "List projects in the user's Linear workspace",
    "description": "List projects in the user's Linear workspace",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "fields": {
          "description": "Fields to include in each result. `id` is always included. Omit or pass an empty array for the default response.",
          "items": {
            "enum": [
              "id",
              "name",
              "summary",
              "description",
              "url",
              "trashed",
              "createdAt",
              "updatedAt",
              "startedAt",
              "completedAt",
              "canceledAt",
              "startDate",
              "startDateResolution",
              "targetDate",
              "targetDateResolution",
              "priority",
              "labels",
              "initiatives",
              "lead",
              "status",
              "teams",
              "members",
              "milestones"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "includeMembers": {
          "default": false,
          "description": "Include project members",
          "type": "boolean"
        },
        "includeMilestones": {
          "default": false,
          "description": "Include milestones",
          "type": "boolean"
        },
        "initiative": {
          "description": "Initiative name, ID, identifier (e.g., INIT-123), or slug",
          "type": "string"
        },
        "label": {
          "description": "Label name or ID",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 50)",
          "maximum": 50,
          "type": "number"
        },
        "member": {
          "description": "User ID, name, email, or \"me\"",
          "type": "string"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "query": {
          "description": "Search project name",
          "type": "string"
        },
        "state": {
          "description": "State type, name, or ID",
          "type": "string"
        },
        "team": {
          "description": "Team name or ID",
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_release_notes",
    "deferred": true,
    "summary": "List release notes in the workspace, optionally filtered by pipeline or covered release.",
    "description": "List release notes in the workspace, optionally filtered by pipeline or covered release.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "includeContent": {
          "default": false,
          "description": "Include markdown release notes content",
          "type": "boolean"
        },
        "includeReleases": {
          "default": false,
          "description": "Include associated releases",
          "type": "boolean"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "pipeline": {
          "description": "Release pipeline ID, slug, or exact name",
          "type": "string"
        },
        "query": {
          "description": "Search release notes title",
          "type": "string"
        },
        "release": {
          "description": "Release ID or slug",
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_release_pipelines",
    "deferred": true,
    "summary": "List release pipelines in the workspace.",
    "description": "List release pipelines in the workspace.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "includeStages": {
          "default": false,
          "description": "Include each pipeline's stages",
          "type": "boolean"
        },
        "includeTeams": {
          "default": false,
          "description": "Include each pipeline's teams",
          "type": "boolean"
        },
        "isProduction": {
          "description": "Filter by production pipeline flag",
          "type": "boolean"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "query": {
          "description": "Search pipeline name",
          "type": "string"
        },
        "team": {
          "description": "Team name or ID",
          "type": "string"
        },
        "type": {
          "description": "Pipeline type: continuous | scheduled",
          "enum": [
            "continuous",
            "scheduled"
          ],
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_releases",
    "deferred": true,
    "summary": "List releases in the workspace, with optional filtering by pipeline, stage, version, and text.",
    "description": "List releases in the workspace, with optional filtering by pipeline, stage, version, and text.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "hasReleaseNotes": {
          "description": "Filter to releases that do (true) or do not (false) have release notes",
          "type": "boolean"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "includeReleaseNotes": {
          "default": false,
          "description": "Include associated release notes",
          "type": "boolean"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "pipeline": {
          "description": "Release pipeline ID, slug, or exact name",
          "type": "string"
        },
        "query": {
          "description": "Search release name or version",
          "type": "string"
        },
        "stage": {
          "description": "Release stage ID or exact name",
          "type": "string"
        },
        "stageType": {
          "description": "Filter by stage lifecycle type",
          "enum": [
            "planned",
            "started",
            "completed",
            "canceled"
          ],
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "version": {
          "description": "Exact version match",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_teams",
    "deferred": true,
    "summary": "List teams in the user's Linear workspace",
    "description": "List teams in the user's Linear workspace",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "createdAt": {
          "description": "Created after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        },
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "includeArchived": {
          "default": false,
          "description": "Include archived items",
          "type": "boolean"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "query": {
          "description": "Search query",
          "type": "string"
        },
        "updatedAt": {
          "description": "Updated after: ISO-8601 date/duration (e.g., -P1D)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__list_users",
    "deferred": true,
    "summary": "Retrieve users in the Linear workspace",
    "description": "Retrieve users in the Linear workspace",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "cursor": {
          "description": "Next page cursor",
          "type": "string"
        },
        "limit": {
          "default": 50,
          "description": "Max results (default 50, max 250)",
          "maximum": 250,
          "type": "number"
        },
        "orderBy": {
          "default": "updatedAt",
          "description": "Sort: createdAt | updatedAt",
          "enum": [
            "createdAt",
            "updatedAt"
          ],
          "type": "string"
        },
        "query": {
          "description": "Filter by name or email",
          "type": "string"
        },
        "team": {
          "description": "Team name or ID",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__merge_diff",
    "deferred": true,
    "summary": "Merge a Linear diff or add it to the repository's merge queue Required args: urlOrId.",
    "description": "Merge a Linear diff or add it to the repository's merge queue",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "mergeMethod": {
          "description": "Merge method; omit to use the repository default",
          "enum": [
            "MERGE",
            "REBASE",
            "SQUASH"
          ],
          "type": "string"
        },
        "urlOrId": {
          "description": "Linear review URL, diff slug, pull request ID, Linear full identifier, or GitHub PR URL",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "urlOrId"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__prepare_attachment_upload",
    "deferred": true,
    "summary": "Prepare a direct Linear file upload for an existing issue. Required args: issue, filename, contentType, size.",
    "description": "Prepare a direct Linear file upload for an existing issue.\n\nWorkflow:\n1. Call this tool with issue, filename, contentType, and size.\n2. Upload raw bytes with PUT to uploadRequest.url outside MCP.\n3. All headers in uploadRequest.headers are part of the signed request, so send them verbatim.\n4. After PUT succeeds, call create_attachment_from_upload with assetUrl to link it to the issue.\n\nDo not base64-encode or transform the file. Use curl --data-binary @path or fetch(url, { method: 'PUT', body: blob }).\nOmitting or modifying any signed header, including casing, will return HTTP 403.\nThe signed URL must be used within 60 seconds or it will expire.\n\nUpload sequencing:\nPrepare, PUT, and finalize one file before calling this tool for another file.\nDo not batch multiple prepare_attachment_upload calls before starting the PUTs because earlier signed URLs can expire while later files are prepared.\n\nExample:\ncurl -X PUT --data-binary @file.png \\\n  -H \"content-type: image/png\" \\\n  -H \"x-goog-content-length-range: N,N\" \\\n  -H \"cache-control: public, max-age=31536000\" \\\n  -H 'Content-Disposition: attachment; filename=\"file.png\"' \\\n  \"<uploadRequest.url>\"",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "contentType": {
          "description": "MIME type, e.g. image/png or application/pdf",
          "type": "string"
        },
        "filename": {
          "description": "Filename for the upload, e.g. screenshot.png",
          "type": "string"
        },
        "issue": {
          "description": "Issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "size": {
          "description": "Exact file size in bytes. Must be smaller than 2 GB.",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991,
          "type": "integer"
        },
        "subtitle": {
          "description": "Suggested attachment subtitle for the finalize step",
          "type": "string"
        },
        "title": {
          "description": "Suggested attachment title for the finalize step",
          "type": "string"
        }
      },
      "required": [
        "issue",
        "filename",
        "contentType",
        "size"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__resolve_diff_thread",
    "deferred": true,
    "summary": "Resolve or reopen a top-level comment thread on a Linear diff Required args: threadId.",
    "description": "Resolve or reopen a top-level comment thread on a Linear diff",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "resolved": {
          "default": true,
          "description": "True to resolve the thread; false to reopen it",
          "type": "boolean"
        },
        "threadId": {
          "description": "Top-level diff thread/comment ID",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "threadId"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__save_comment",
    "deferred": true,
    "summary": "Create or update a comment on a Linear issue, project, initiative, document, project milestone, or project/initiative status update. If `id` is provided, updates the existing comment; otherwise creates a new one. To start a new thread, pass `body` and exactly one of `issueId`, `projectId`, `initiativeId`, `documentId`, `milestoneId`, or `statusUpdateId` — comments on issues/projects/initiatives become top-level discussion threads; comments on documents/milestones become description comments. A status update holds a single thread, so a comment on an update that already has one is added to it as a reply. To reply to an existing thread, pass `parentId` and `body`; the reply inherits the parent's thread type, so no enti [summary truncated] Required args: body.",
    "description": "Create or update a comment on a Linear issue, project, initiative, document, project milestone, or project/initiative status update. If `id` is provided, updates the existing comment; otherwise creates a new one. To start a new thread, pass `body` and exactly one of `issueId`, `projectId`, `initiativeId`, `documentId`, `milestoneId`, or `statusUpdateId` — comments on issues/projects/initiatives become top-level discussion threads; comments on documents/milestones become description comments. A status update holds a single thread, so a comment on an update that already has one is added to it as a reply. To reply to an existing thread, pass `parentId` and `body`; the reply inherits the parent's thread type, so no entity reference is needed. Parent reference fields are ignored when `id` or `parentId` is provided (`statusUpdateType` is rejected instead).",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences. To mention a user, use @displayName (e.g., @johndoe)",
          "type": "string"
        },
        "documentId": {
          "description": "Document ID or slug (provide exactly one parent)",
          "type": "string"
        },
        "id": {
          "description": "Comment ID. If provided, updates the existing comment",
          "type": "string"
        },
        "initiativeId": {
          "description": "Initiative name, ID, identifier (e.g., INIT-123), or slug (provide exactly one parent)",
          "type": "string"
        },
        "issueId": {
          "description": "Issue ID or identifier (e.g., LIN-123) (provide exactly one parent)",
          "type": "string"
        },
        "milestoneId": {
          "description": "Milestone UUID (provide exactly one parent). Resolve milestone names via `list_milestones` first.",
          "type": "string"
        },
        "parentId": {
          "description": "Parent comment ID (for replies, only when creating)",
          "type": "string"
        },
        "projectId": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug (provide exactly one parent)",
          "type": "string"
        },
        "statusUpdateId": {
          "description": "Status update UUID (provide exactly one parent). Resolve status updates via `get_status_updates` first.",
          "type": "string"
        },
        "statusUpdateType": {
          "description": "Type of status update named by `statusUpdateId`, as returned by `get_status_updates`. Only valid together with `statusUpdateId`; omit to check both project and initiative status updates.",
          "enum": [
            "project",
            "initiative"
          ],
          "type": "string"
        }
      },
      "required": [
        "body"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__save_diff_comment",
    "deferred": true,
    "summary": "Create, reply to, or edit a comment or persisted draft on a Linear diff. Set draft to true to save without submitting. Provide draftId to edit or submit a persisted draft, and commentId only to edit a submitted comment. Submitting an inline draft reuses its saved anchor; submitting a reply draft requires parentId again. Required args: body.",
    "description": "Create, reply to, or edit a comment or persisted draft on a Linear diff. Set draft to true to save without submitting. Provide draftId to edit or submit a persisted draft, and commentId only to edit a submitted comment. Submitting an inline draft reuses its saved anchor; submitting a reply draft requires parentId again.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "anchor": {
          "additionalProperties": {},
          "description": "Optional comment anchor copied from a diff thread or built for a file or diff hunk",
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "anchorContent": {
          "description": "Exact source text of the anchor's end line; stored with an inline draft so it can be re-anchored after the diff receives new revisions",
          "type": "string"
        },
        "body": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences",
          "minLength": 1,
          "type": "string"
        },
        "commentId": {
          "description": "Existing comment ID to edit",
          "type": "string"
        },
        "draft": {
          "default": false,
          "description": "Save as a private persisted draft without submitting",
          "type": "boolean"
        },
        "draftId": {
          "description": "Existing persisted draft ID to edit or submit",
          "type": "string"
        },
        "parentId": {
          "description": "Top-level comment ID to reply to; also required when submitting a persisted reply draft",
          "type": "string"
        },
        "urlOrId": {
          "description": "Linear review URL, diff slug, pull request ID, Linear full identifier, or GitHub PR URL",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "body"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__save_document",
    "deferred": true,
    "summary": "Create or update a Linear document. If `id` is provided, updates the existing document; otherwise creates a new one. When creating, `title` is required and exactly one parent (`project`, `issue`, `initiative`, `cycle`, or `team`) must be specified. On update, passing a parent reparents the document. To change parts of the content without resending all of it, pass `patch` instead of `content`.",
    "description": "Create or update a Linear document. If `id` is provided, updates the existing document; otherwise creates a new one. When creating, `title` is required and exactly one parent (`project`, `issue`, `initiative`, `cycle`, or `team`) must be specified. On update, passing a parent reparents the document. To change parts of the content without resending all of it, pass `patch` instead of `content`.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "color": {
          "description": "Hex color",
          "type": "string"
        },
        "content": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences. To mention a user, use @displayName (e.g., @johndoe)",
          "type": "string"
        },
        "cycle": {
          "description": "Cycle name, number, or ID. When passing a name or number, also pass `team` to disambiguate.",
          "type": "string"
        },
        "icon": {
          "description": "Icon name or emoji code (e.g. \"Rocket\" or \":eagle:\"), not a raw Unicode emoji",
          "type": "string"
        },
        "id": {
          "description": "Document ID or slug to update. Omit to create a new document.",
          "type": "string"
        },
        "initiative": {
          "description": "Initiative name, ID, identifier (e.g., INIT-123), or slug",
          "type": "string"
        },
        "issue": {
          "description": "Issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "patch": {
          "description": "Partial edits applied to the current content, in order and atomically (one failing operation aborts the whole save). Every anchor string must match the current content exactly once. Only valid on update, in place of the full content/description field",
          "items": {
            "oneOf": [
              {
                "properties": {
                  "new_string": {
                    "description": "Replacement text. Empty string deletes the match",
                    "type": "string"
                  },
                  "old_string": {
                    "description": "Exact text to replace. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "replace",
                    "type": "string"
                  },
                  "replace_all": {
                    "description": "Replace every occurrence instead of requiring a unique match",
                    "type": "boolean"
                  }
                },
                "required": [
                  "op",
                  "old_string",
                  "new_string"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert before. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_before",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly before the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert after. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_after",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly after the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "prepend",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very start of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "append",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very end of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "from": {
                    "description": "Exact text where the range starts (inclusive). Must match exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "new_string": {
                    "description": "Text replacing the range. Empty string deletes it",
                    "type": "string"
                  },
                  "op": {
                    "const": "replace_range",
                    "type": "string"
                  },
                  "to": {
                    "description": "Exact text where the range ends (exclusive, stays in place). Must match exactly once after `from`",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "from",
                  "to",
                  "new_string"
                ],
                "type": "object"
              }
            ]
          },
          "maxItems": 50,
          "minItems": 1,
          "type": "array"
        },
        "project": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        },
        "team": {
          "description": "Team name or ID. Attaches the document to the team, unless `cycle` is also passed, in which case it disambiguates the cycle.",
          "type": "string"
        },
        "title": {
          "description": "Document title (required when creating)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__save_issue",
    "deferred": true,
    "summary": "Create or update a Linear issue. If `id` is provided, updates the existing issue; otherwise creates a new one. When creating, `title` and `team` are required. Note: use `assignee` (not `assigneeId`) to set the assignee — it accepts a user ID, name, email, or \"me\".",
    "description": "Create or update a Linear issue. If `id` is provided, updates the existing issue; otherwise creates a new one. When creating, `title` and `team` are required. Note: use `assignee` (not `assigneeId`) to set the assignee — it accepts a user ID, name, email, or \"me\".",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "addReleases": {
          "description": "Release IDs or slugs to add. Append-only; existing releases are never removed",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "assignee": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "User ID, name, email, or \"me\". Null to remove"
        },
        "blockedBy": {
          "description": "Issue IDs/identifiers blocking this. Append-only; existing relations are never removed",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blocks": {
          "description": "Issue IDs/identifiers this blocks. Append-only; existing relations are never removed",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cycle": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Cycle name, number, or ID. Null to remove"
        },
        "delegate": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Agent name or ID. When the user asks to delegate to \"Linear\" or \"the Linear agent\", this refers to the \"Linear\" app user specifically. Null to remove"
        },
        "description": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences. To mention a user, use @displayName (e.g., @johndoe)",
          "type": "string"
        },
        "dueDate": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Due date (ISO format). On update, pass null to remove the due date"
        },
        "duplicateOf": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Duplicate of issue ID/identifier. Null to remove"
        },
        "estimate": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "description": "Issue estimate value. On create, pass null or omit for no estimate. On update, pass null to clear the estimate; omitting leaves it unchanged. 0 is a real estimate only on teams that allow zero estimates."
        },
        "id": {
          "description": "Only for updating an existing issue. Pass the issue ID or identifier (e.g., LIN-123). Do NOT pass this parameter when creating a new issue.",
          "type": "string"
        },
        "labels": {
          "description": "Label names or IDs as a JSON array of strings (e.g. [\"Bug\", \"Urgent\"]). Replaces the full label set; existing labels not included are removed. Omit to leave labels unchanged",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "links": {
          "description": "Link attachments to add [{url, title}]. Append-only; existing links are never removed",
          "items": {
            "properties": {
              "title": {
                "minLength": 1,
                "type": "string"
              },
              "url": {
                "format": "uri",
                "type": "string"
              }
            },
            "required": [
              "url",
              "title"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "milestone": {
          "description": "Milestone name or ID",
          "type": "string"
        },
        "parentId": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Parent issue ID or identifier (e.g., LIN-123). Null to remove"
        },
        "patch": {
          "description": "Partial edits applied to the current content, in order and atomically (one failing operation aborts the whole save). Every anchor string must match the current content exactly once. Only valid on update, in place of the full content/description field",
          "items": {
            "oneOf": [
              {
                "properties": {
                  "new_string": {
                    "description": "Replacement text. Empty string deletes the match",
                    "type": "string"
                  },
                  "old_string": {
                    "description": "Exact text to replace. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "replace",
                    "type": "string"
                  },
                  "replace_all": {
                    "description": "Replace every occurrence instead of requiring a unique match",
                    "type": "boolean"
                  }
                },
                "required": [
                  "op",
                  "old_string",
                  "new_string"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert before. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_before",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly before the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert after. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_after",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly after the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "prepend",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very start of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "append",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very end of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "from": {
                    "description": "Exact text where the range starts (inclusive). Must match exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "new_string": {
                    "description": "Text replacing the range. Empty string deletes it",
                    "type": "string"
                  },
                  "op": {
                    "const": "replace_range",
                    "type": "string"
                  },
                  "to": {
                    "description": "Exact text where the range ends (exclusive, stays in place). Must match exactly once after `from`",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "from",
                  "to",
                  "new_string"
                ],
                "type": "object"
              }
            ]
          },
          "maxItems": 50,
          "minItems": 1,
          "type": "array"
        },
        "priority": {
          "description": "0=None, 1=Urgent, 2=High, 3=Medium, 4=Low",
          "type": "number"
        },
        "project": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug. Null to remove"
        },
        "relatedTo": {
          "description": "Related issue IDs/identifiers. Append-only; existing relations are never removed",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "removeBlockedBy": {
          "description": "Issue IDs/identifiers to remove as blockers of this issue",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "removeBlocks": {
          "description": "Issue IDs/identifiers to stop blocking",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "removeRelatedTo": {
          "description": "Related issue IDs/identifiers to remove",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "removeReleases": {
          "description": "Release IDs or slugs to remove. Only valid when updating an existing issue",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "setReleases": {
          "description": "Replace all releases on the issue with these. Cannot be combined with addReleases/removeReleases",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "slaBreachesAt": {
          "anyOf": [
            {
              "format": "date-time",
              "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z|([+-](?:[01]\\d|2[0-3]):[0-5]\\d)))$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "ISO-8601 timestamp when the SLA will breach. On update, pass null to remove the SLA"
        },
        "slaType": {
          "anyOf": [
            {
              "enum": [
                "all",
                "onlyBusinessDays"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "SLA day counting type: \"all\" or \"onlyBusinessDays\". Only use with slaBreachesAt; null means unset"
        },
        "state": {
          "description": "State type, name, or ID",
          "type": "string"
        },
        "team": {
          "description": "Team name or ID (required when creating)",
          "type": "string"
        },
        "title": {
          "description": "Issue title (required when creating)",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__save_milestone",
    "deferred": true,
    "summary": "Create or update a milestone in a Linear project. If `id` is provided, updates the existing milestone; otherwise creates a new one. When creating, `name` is required. Required args: project.",
    "description": "Create or update a milestone in a Linear project. If `id` is provided, updates the existing milestone; otherwise creates a new one. When creating, `name` is required.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "description": {
          "description": "Milestone description",
          "type": "string"
        },
        "id": {
          "description": "Milestone name or ID",
          "type": "string"
        },
        "name": {
          "description": "Milestone name (required when creating)",
          "type": "string"
        },
        "project": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        },
        "targetDate": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Target completion date (ISO format, null to remove)"
        }
      },
      "required": [
        "project"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__save_project",
    "deferred": true,
    "summary": "Create or update a Linear project. If `id` is provided, updates the existing project; otherwise creates a new one. When creating, `name` and at least one team (via `addTeams` or `setTeams`) are required. To change parts of the description without resending all of it, pass `patch` instead of `description`.",
    "description": "Create or update a Linear project. If `id` is provided, updates the existing project; otherwise creates a new one. When creating, `name` and at least one team (via `addTeams` or `setTeams`) are required. To change parts of the description without resending all of it, pass `patch` instead of `description`.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "addInitiatives": {
          "description": "Initiatives to add. Initiative name, ID, identifier (e.g., INIT-123), or slug",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "addTeams": {
          "description": "Team name or ID to add",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "color": {
          "description": "Hex color",
          "type": "string"
        },
        "description": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences. To mention a user, use @displayName (e.g., @johndoe)",
          "type": "string"
        },
        "icon": {
          "description": "Icon name or emoji code (e.g. \"Rocket\" or \":eagle:\"), not a raw Unicode emoji",
          "type": "string"
        },
        "id": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug. If provided, updates the existing project",
          "type": "string"
        },
        "labels": {
          "description": "Label names or IDs as a JSON array of strings (e.g. [\"Bug\", \"Urgent\"]). Replaces the full label set; existing labels not included are removed. Omit to leave labels unchanged",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "lead": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "User ID, name, email, or \"me\". Null to remove"
        },
        "links": {
          "description": "External resource links to add [{url, title}]. Append-only; existing links are never removed",
          "items": {
            "properties": {
              "title": {
                "minLength": 1,
                "type": "string"
              },
              "url": {
                "format": "uri",
                "type": "string"
              }
            },
            "required": [
              "url",
              "title"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "name": {
          "description": "Project name (required when creating)",
          "type": "string"
        },
        "patch": {
          "description": "Partial edits applied to the current content, in order and atomically (one failing operation aborts the whole save). Every anchor string must match the current content exactly once. Only valid on update, in place of the full content/description field",
          "items": {
            "oneOf": [
              {
                "properties": {
                  "new_string": {
                    "description": "Replacement text. Empty string deletes the match",
                    "type": "string"
                  },
                  "old_string": {
                    "description": "Exact text to replace. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "replace",
                    "type": "string"
                  },
                  "replace_all": {
                    "description": "Replace every occurrence instead of requiring a unique match",
                    "type": "boolean"
                  }
                },
                "required": [
                  "op",
                  "old_string",
                  "new_string"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert before. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_before",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly before the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert after. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_after",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly after the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "prepend",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very start of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "append",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very end of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "from": {
                    "description": "Exact text where the range starts (inclusive). Must match exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "new_string": {
                    "description": "Text replacing the range. Empty string deletes it",
                    "type": "string"
                  },
                  "op": {
                    "const": "replace_range",
                    "type": "string"
                  },
                  "to": {
                    "description": "Exact text where the range ends (exclusive, stays in place). Must match exactly once after `from`",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "from",
                  "to",
                  "new_string"
                ],
                "type": "object"
              }
            ]
          },
          "maxItems": 50,
          "minItems": 1,
          "type": "array"
        },
        "priority": {
          "description": "0=None, 1=Urgent, 2=High, 3=Medium, 4=Low",
          "maximum": 4,
          "minimum": 0,
          "type": "integer"
        },
        "removeInitiatives": {
          "description": "Initiatives to remove. Initiative name, ID, identifier (e.g., INIT-123), or slug",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "removeTeams": {
          "description": "Team name or ID to remove",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "setInitiatives": {
          "description": "Replace all initiatives with these. Initiative name, ID, identifier (e.g., INIT-123), or slug. Cannot combine with addInitiatives/removeInitiatives",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "setTeams": {
          "description": "Replace all teams with these. Cannot combine with addTeams/removeTeams",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "startDate": {
          "description": "Start date (ISO format). Pair with startDateResolution to indicate precision (e.g. month, quarter)",
          "type": "string"
        },
        "startDateResolution": {
          "description": "Start date resolution",
          "enum": [
            "halfYear",
            "month",
            "quarter",
            "year"
          ],
          "type": "string"
        },
        "state": {
          "description": "Project state",
          "type": "string"
        },
        "summary": {
          "description": "Short summary (max 255 chars)",
          "type": "string"
        },
        "targetDate": {
          "description": "Target date (ISO format). Pair with targetDateResolution to indicate precision (e.g. month, quarter)",
          "type": "string"
        },
        "targetDateResolution": {
          "description": "Target date resolution",
          "enum": [
            "halfYear",
            "month",
            "quarter",
            "year"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__save_release",
    "deferred": true,
    "summary": "Create or update a release. If `id` is provided, updates the existing release; otherwise creates a new one. When creating, `name` and `pipeline` are required. Release status is modeled as the release pipeline stage.",
    "description": "Create or update a release. If `id` is provided, updates the existing release; otherwise creates a new one. When creating, `name` and `pipeline` are required. Release status is modeled as the release pipeline stage.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "commitSha": {
          "description": "Commit SHA associated with the release",
          "type": "string"
        },
        "completedAt": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Completed timestamp (ISO DateTime, null to remove)"
        },
        "createdAt": {
          "description": "Import/create timestamp (ISO DateTime)",
          "type": "string"
        },
        "description": {
          "description": "Release description",
          "type": "string"
        },
        "id": {
          "description": "Release ID or slug to update. Omit to create a new release.",
          "type": "string"
        },
        "name": {
          "description": "Release name (required when creating)",
          "type": "string"
        },
        "pipeline": {
          "description": "Release pipeline ID, slug, or exact name (required when creating)",
          "type": "string"
        },
        "stage": {
          "description": "Release stage ID, exact name, or lifecycle type within the release pipeline",
          "type": "string"
        },
        "startDate": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Estimated start date (ISO YYYY-MM-DD, null to remove)"
        },
        "startedAt": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Started timestamp (ISO DateTime, null to remove)"
        },
        "targetDate": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "description": "Estimated completion date (ISO YYYY-MM-DD, null to remove)"
        },
        "version": {
          "description": "Version identifier",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__save_release_note",
    "deferred": true,
    "summary": "Create or update release notes. If `id` is provided, updates the existing release notes; otherwise creates a new one. When creating, `pipeline` and either `releases` or a release range are required. To change parts of the content without resending all of it, pass `patch` instead of `content`.",
    "description": "Create or update release notes. If `id` is provided, updates the existing release notes; otherwise creates a new one. When creating, `pipeline` and either `releases` or a release range are required. To change parts of the content without resending all of it, pass `patch` instead of `content`.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "content": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences. To mention a user, use @displayName (e.g., @johndoe)",
          "type": "string"
        },
        "id": {
          "description": "Release notes ID or slug to update. Omit to create new release notes.",
          "type": "string"
        },
        "patch": {
          "description": "Partial edits applied to the current content, in order and atomically (one failing operation aborts the whole save). Every anchor string must match the current content exactly once. Only valid on update, in place of the full content/description field",
          "items": {
            "oneOf": [
              {
                "properties": {
                  "new_string": {
                    "description": "Replacement text. Empty string deletes the match",
                    "type": "string"
                  },
                  "old_string": {
                    "description": "Exact text to replace. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "replace",
                    "type": "string"
                  },
                  "replace_all": {
                    "description": "Replace every occurrence instead of requiring a unique match",
                    "type": "boolean"
                  }
                },
                "required": [
                  "op",
                  "old_string",
                  "new_string"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert before. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_before",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly before the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "anchor": {
                    "description": "Exact text to insert after. Must match the current content exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "op": {
                    "const": "insert_after",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert directly after the anchor. Include separators (e.g. \"\\n\\n\")",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "anchor",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "prepend",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very start of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "op": {
                    "const": "append",
                    "type": "string"
                  },
                  "text": {
                    "description": "Text to insert at the very end of the content",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "text"
                ],
                "type": "object"
              },
              {
                "properties": {
                  "from": {
                    "description": "Exact text where the range starts (inclusive). Must match exactly once",
                    "minLength": 1,
                    "type": "string"
                  },
                  "new_string": {
                    "description": "Text replacing the range. Empty string deletes it",
                    "type": "string"
                  },
                  "op": {
                    "const": "replace_range",
                    "type": "string"
                  },
                  "to": {
                    "description": "Exact text where the range ends (exclusive, stays in place). Must match exactly once after `from`",
                    "minLength": 1,
                    "type": "string"
                  }
                },
                "required": [
                  "op",
                  "from",
                  "to",
                  "new_string"
                ],
                "type": "object"
              }
            ]
          },
          "maxItems": 50,
          "minItems": 1,
          "type": "array"
        },
        "pipeline": {
          "description": "Release pipeline ID, slug, or exact name (required when creating)",
          "type": "string"
        },
        "rangeFromRelease": {
          "description": "Oldest release ID or slug in the note range",
          "type": "string"
        },
        "rangeToRelease": {
          "description": "Newest release ID or slug in the note range",
          "type": "string"
        },
        "releases": {
          "description": "Release IDs or slugs to include in the note",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "Release notes title",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "linear__save_status_update",
    "deferred": true,
    "summary": "Create or update a project/initiative status update. Omit `id` to create, provide `id` to update. Required args: type.",
    "description": "Create or update a project/initiative status update. Omit `id` to create, provide `id` to update.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences. To mention a user, use @displayName (e.g., @johndoe)",
          "type": "string"
        },
        "health": {
          "description": "onTrack | atRisk | offTrack",
          "enum": [
            "onTrack",
            "atRisk",
            "offTrack"
          ],
          "type": "string"
        },
        "id": {
          "description": "Status update ID - if provided, updates this existing update",
          "type": "string"
        },
        "initiative": {
          "description": "Initiative name, ID, identifier (e.g., INIT-123), or slug",
          "type": "string"
        },
        "isDiffHidden": {
          "description": "Deprecated. Hide diff with previous update (create only)",
          "type": "boolean"
        },
        "project": {
          "description": "Project name, ID, identifier (e.g., PROJ-123), or slug",
          "type": "string"
        },
        "type": {
          "description": "Type of status update",
          "enum": [
            "project",
            "initiative"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__search_documentation",
    "deferred": true,
    "summary": "Search Linear's documentation to learn about features and usage Required args: query.",
    "description": "Search Linear's documentation to learn about features and usage",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "page": {
          "default": 0,
          "description": "Page number",
          "type": "number"
        },
        "query": {
          "description": "Search query",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__share_issue",
    "deferred": true,
    "summary": "Share an issue with a workspace user who cannot otherwise access it. Use this tool only when the user explicitly asks to share the issue. Required args: issue, user.",
    "description": "Share an issue with a workspace user who cannot otherwise access it. Use this tool only when the user explicitly asks to share the issue.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "issue": {
          "description": "Issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "user": {
          "description": "User ID, name, email, or \"me\" Must be an email address or UUID.",
          "type": "string"
        }
      },
      "required": [
        "issue",
        "user"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__submit_diff_review",
    "deferred": true,
    "summary": "Approve a Linear diff, request changes, or submit a review comment Required args: urlOrId, decision.",
    "description": "Approve a Linear diff, request changes, or submit a review comment",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "body": {
          "description": "Content as Markdown. Do not escape the string — use literal newlines and special characters, not escape sequences. Required when requesting changes or commenting",
          "minLength": 1,
          "type": "string"
        },
        "decision": {
          "description": "Review decision: approved, changesRequested, or commented",
          "enum": [
            "approved",
            "changesRequested",
            "commented"
          ],
          "type": "string"
        },
        "urlOrId": {
          "description": "Linear review URL, diff slug, pull request ID, Linear full identifier, or GitHub PR URL",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "urlOrId",
        "decision"
      ],
      "type": "object"
    }
  },
  {
    "name": "linear__unshare_issue",
    "deferred": true,
    "summary": "Remove a workspace user's shared access to an issue. Use this tool only when the user explicitly asks to stop sharing the issue. Required args: issue, user.",
    "description": "Remove a workspace user's shared access to an issue. Use this tool only when the user explicitly asks to stop sharing the issue.",
    "parameters": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "properties": {
        "issue": {
          "description": "Issue ID or identifier (e.g., LIN-123)",
          "type": "string"
        },
        "user": {
          "description": "User ID, name, email, or \"me\" Must be an email address or UUID.",
          "type": "string"
        }
      },
      "required": [
        "issue",
        "user"
      ],
      "type": "object"
    }
  },
  {
    "name": "memory__memory_forget",
    "deferred": true,
    "summary": "Detach a source's support or remove matching facts and orphaned entities. A source-scoped call keeps facts that still have another source. Use this for what was never true -- a probe, a bad extraction, a run written under the wrong identity. For something that STOPPED being true, use memory_upsert_fact with supersedes instead: that closes the fact's window and keeps the past answerable, where this destroys it. Pass dry_run first to see the size of what you are about to remove.",
    "description": "Detach a source's support or remove matching facts and orphaned entities. A source-scoped call keeps facts that still have another source. Use this for what was never true -- a probe, a bad extraction, a run written under the wrong identity. For something that STOPPED being true, use memory_upsert_fact with supersedes instead: that closes the fact's window and keeps the past answerable, where this destroys it. Pass dry_run first to see the size of what you are about to remove.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "dry_run": {
          "description": "count what would be removed without removing it",
          "type": "boolean"
        },
        "entity": {
          "description": "remove every fact touching this entity, in either direction, and the entity itself",
          "type": "string"
        },
        "keep_orphan_entities": {
          "description": "keep entities this removal strips of their last fact",
          "type": "boolean"
        },
        "object": {
          "description": "narrow to one object; cannot be used alone",
          "type": "string"
        },
        "predicate": {
          "description": "narrow to one relation; cannot be used alone",
          "type": "string"
        },
        "source": {
          "additionalProperties": false,
          "description": "detach support from this source run; facts with other sources remain",
          "properties": {
            "memory_ids": {
              "description": "message or memory ids supporting this fact",
              "items": {
                "type": "string"
              },
              "type": [
                "null",
                "array"
              ]
            },
            "run_id": {
              "description": "the extraction or operator run supporting this fact",
              "type": "string"
            }
          },
          "required": [
            "run_id"
          ],
          "type": [
            "null",
            "object"
          ]
        },
        "subject": {
          "description": "remove facts with this subject",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "memory__memory_merge_entities",
    "deferred": true,
    "summary": "Fold one entity's facts onto another and remove it, for when the same thing was recorded under two names. Prefer this over memory_forget for a duplicate: forgetting destroys the facts only the duplicate knew. If the target does not exist yet this is a rename. Required args: source, target.",
    "description": "Fold one entity's facts onto another and remove it, for when the same thing was recorded under two names. Prefer this over memory_forget for a duplicate: forgetting destroys the facts only the duplicate knew. If the target does not exist yet this is a rename.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "source": {
          "description": "the duplicate name; it is removed",
          "type": "string"
        },
        "target": {
          "description": "the name that survives and receives the facts",
          "type": "string"
        }
      },
      "required": [
        "source",
        "target"
      ],
      "type": "object"
    }
  },
  {
    "name": "memory__memory_recall",
    "deferred": true,
    "summary": "The single deep-read operation for Aura memory. Current profile facts are already supplied in conversation context; call this only for a fact outside that bounded index or for a historical as_of question. Give entity when its exact name is known; otherwise give query. Returns explicit abstention instead of an approximate unrelated fact.",
    "description": "The single deep-read operation for Aura memory. Current profile facts are already supplied in conversation context; call this only for a fact outside that bounded index or for a historical as_of question. Give entity when its exact name is known; otherwise give query. Returns explicit abstention instead of an approximate unrelated fact.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "as_of": {
          "description": "RFC3339 instant; return facts valid then rather than now",
          "type": "string"
        },
        "entity": {
          "description": "exact entity name when known; selects graph traversal instead of semantic search",
          "type": "string"
        },
        "limit": {
          "description": "how many facts to return",
          "type": "integer"
        },
        "predicate": {
          "description": "optional relation filter used with entity",
          "type": "string"
        },
        "query": {
          "description": "what to recall in natural language; required when entity is empty",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "name": "memory__memory_upsert_fact",
    "deferred": true,
    "summary": "Store one fact as a bitemporal edge between two entities. A fact is never overwritten: when it is superseded its validity window is closed, so both what is true now and what was true then stay answerable. To correct a fact precisely, set supersedes_fact_key to the fact_key a prior recall returned. Without it, supersedes:true resolves the subject+predicate match itself: exactly one candidate closes; zero or more than one candidate REFUSES -- the call still succeeds, refused is true, and candidates carries the previews (each with its own fact_key) to retry with supersedes_fact_key. Required args: subject, predicate, object, statement, source.",
    "description": "Store one fact as a bitemporal edge between two entities. A fact is never overwritten: when it is superseded its validity window is closed, so both what is true now and what was true then stay answerable. To correct a fact precisely, set supersedes_fact_key to the fact_key a prior recall returned. Without it, supersedes:true resolves the subject+predicate match itself: exactly one candidate closes; zero or more than one candidate REFUSES -- the call still succeeds, refused is true, and candidates carries the previews (each with its own fact_key) to retry with supersedes_fact_key.",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "object": {
          "description": "the entity or value the subject relates to",
          "type": "string"
        },
        "object_kind": {
          "description": "optional entity kind, e.g. Location or Organization",
          "type": "string"
        },
        "predicate": {
          "description": "the relation, e.g. lives_in",
          "type": "string"
        },
        "source": {
          "additionalProperties": false,
          "description": "required provenance supporting this fact",
          "properties": {
            "memory_ids": {
              "description": "message or memory ids supporting this fact",
              "items": {
                "type": "string"
              },
              "type": [
                "null",
                "array"
              ]
            },
            "run_id": {
              "description": "the extraction or operator run supporting this fact",
              "type": "string"
            }
          },
          "required": [
            "run_id"
          ],
          "type": "object"
        },
        "statement": {
          "description": "the fact in natural language; this is what gets embedded and searched",
          "type": "string"
        },
        "subject": {
          "description": "the entity the fact is about",
          "type": "string"
        },
        "subject_kind": {
          "description": "optional entity kind, e.g. Person or Organization",
          "type": "string"
        },
        "supersedes": {
          "description": "close any still-valid fact with the same subject and predicate",
          "type": "boolean"
        },
        "supersedes_fact_key": {
          "description": "the fact_key of the exact fact to close, taken from a prior recall result; set this to disambiguate after a refused correction",
          "type": "string"
        },
        "valid_from": {
          "description": "RFC3339 instant when the fact became true; defaults to now",
          "type": "string"
        },
        "valid_to": {
          "description": "RFC3339 instant when the fact stopped being true; omit while it still holds",
          "type": "string"
        }
      },
      "required": [
        "subject",
        "predicate",
        "object",
        "statement",
        "source"
      ],
      "type": "object"
    }
  },
  {
    "name": "plugin_pack",
    "deferred": true,
    "summary": "Read a knowledge-work plugin pack — its skills, connectors and commands — as one unit.",
    "description": "Read a knowledge-work plugin PACK from its repository: its skills, its MCP connectors, and its commands, as one unit. The skills catalogue indexes a pack's skills individually and DROPS the grouping — `call-prep` is listed as `anthropics/knowledge-work-plugins/call-prep` with no sign that it belongs to `sales`, nor of the connectors that ship beside it. This puts the pack back together.\n\naction=list <owner/repo> summarizes every pack in a repository. action=show <owner/repo/plugin> gives one in full.\n\nThis tool READS. It installs nothing: use skill_manage to install a skill the pack names (the `owner/repo@skill` form is printed for you), and tell the operator about the connectors — each one needs their approval, and a connector marked as needing OAuth cannot be brought up without them.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "list",
            "show"
          ],
          "description": "list: every pack in the repository. show: one pack in full."
        },
        "ref": {
          "type": "string",
          "description": "owner/repo for list, owner/repo/plugin for show. Example: anthropics/knowledge-work-plugins/sales"
        }
      },
      "required": [
        "action",
        "ref"
      ]
    }
  },
  {
    "name": "shell_kill",
    "deferred": true,
    "summary": "Terminate a background shell_exec job.",
    "description": "Kill a running background shell started by shell_exec (Claude Code's KillBash). Takes the shell_id; returns success even if the job already finished (idempotent).",
    "parameters": {
      "type": "object",
      "properties": {
        "shell_id": {
          "type": "string",
          "description": "The id of the background shell to terminate."
        }
      },
      "required": [
        "shell_id"
      ]
    }
  },
  {
    "name": "shell_poll",
    "deferred": true,
    "summary": "Read new output from a background shell_exec job.",
    "description": "Retrieve output from a running or finished background shell started by shell_exec with \"background\": true. Returns ONLY the output produced since your last poll, plus a final [aura_shell_bg {...}] footer with the shell_id and status (running | exited:<code> | killed). Pass an optional regex 'filter' to keep only matching lines. Poll again to follow a long job; stop it with shell_kill.",
    "parameters": {
      "type": "object",
      "properties": {
        "shell_id": {
          "type": "string",
          "description": "The id returned by the background shell_exec call."
        },
        "filter": {
          "type": "string",
          "description": "Optional regular expression; only output lines matching it are returned."
        }
      },
      "required": [
        "shell_id"
      ]
    }
  },
  {
    "name": "skill_manage",
    "deferred": true,
    "summary": "Administer the deployment-wide skills library.",
    "description": "Write half of the skills library: author (create/update/delete), install from the open ecosystem, and manage runnable snippets (save_snippet/archive/restore). Requires the authenticated identity to hold `governance.write` because the library is shared deployment configuration. Use `skill` to list, read or apply an existing skill — this tool is for changing the library. Every write takes effect immediately: what you create, update, install or save is usable on this same turn.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "create",
            "update",
            "delete",
            "install",
            "save_snippet",
            "restore",
            "archive"
          ],
          "description": "create authors a new skill and update revises one; delete removes it; install fetches a skill from the open ecosystem into the deployment library; save_snippet stores runnable code you can re-run by path on a later turn instead of re-authoring it; archive de-activates a snippet and restore un-archives it. Every write takes effect immediately: what you create, update, install or save is usable on this same turn. Nothing is staged and nothing waits for approval. Never install by running the skills CLI in a terminal - it writes outside the library and the skill will not load."
        },
        "name": {
          "type": "string",
          "description": "Required for every action except install. The exact skill/snippet name (lowercase, [a-z0-9-], 1-64 chars)."
        },
        "description": {
          "type": "string",
          "description": "Required for create, update and save_snippet. A one-line summary, shown in the skill manifest - it is how you find it again. Refused without one."
        },
        "body": {
          "type": "string",
          "description": "Required for create and update. The markdown instructions that make up the skill."
        },
        "source": {
          "type": "string",
          "description": "Required for install: owner/repo (optionally owner/repo@skill-name), a URL, or a local path."
        },
        "language": {
          "type": "string",
          "enum": [
            "python",
            "shell",
            "js"
          ],
          "description": "Required for save_snippet. The language of the snippet code."
        },
        "code": {
          "type": "string",
          "description": "Required for save_snippet. The executable body, run later by path."
        },
        "always": {
          "type": "boolean",
          "description": "Optional for create and update. true injects the body into every future turn's always-on block instead of only when applied by name - standing instructions only, since it costs context every turn."
        }
      },
      "required": [
        "action"
      ]
    }
  },
  {
    "name": "swarm_spawn",
    "deferred": true,
    "summary": "Run 2 or more independent subtasks in parallel as worker agents and collect their reports. Whenever the user asks for multiple unrelated things in one request, call this instead of doing them one by one. Call shape: {\"goals\":[\"<complete brief for subtask 1>\",\"<complete brief for subtask 2>\"]}.",
    "description": "Run several independent subtasks in parallel as headless worker agents and get back one report per subtask. Use this ONLY when the work splits into 2 or more independent, self-contained subtasks that can run at the same time. A simple single task is NOT a reason to spawn — answer it directly yourself instead of spawning a swarm. Each goal must be a complete, standalone brief: state its objective, the output format you expect, and its boundaries, because the worker cannot see the conversation, the user, the other workers, or anything outside the goal text you give it. Each worker returns a single final report; you read those reports and synthesize the answer. Example: {\"goals\":[\"Summarize the Q1 revenue figures from the attached notes\",\"List the top 3 competitor product launches this quarter\"]}.",
    "parameters": {
      "type": "object",
      "properties": {
        "goals": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "Two or more independent, self-contained subtask briefs. Each entry is one worker's complete instructions (objective + output format + boundaries); the worker sees only this text."
        }
      },
      "required": [
        "goals"
      ]
    }
  },
  {
    "name": "task",
    "deferred": true,
    "summary": "Your scheduler and timer (scheduling): schedule future, periodic or recurring work — reminders and agent_job wake-ups later, tomorrow, at a time, every N minutes or on a cron; list, cancel, or run one now.",
    "description": "Aura's scheduler, timer and scheduling wake-up: manage scheduled work via a single action enum. action=schedule creates a one-shot (at), interval (every), or cron task of a kind (reminder|agent_job|backup_postgres); action=list shows active and awaiting-approval tasks; action=cancel/run_now operate on a task_id. When the operator asks for recurring or future work (a daily summary, a morning digest, a periodic check, a reminder, something later or tomorrow), or when you need to wake up later to check on something, schedule it here instead of trying to do it now: a reminder delivers its payload text; an agent_job runs a fresh agent turn AT FIRE TIME with the goal in its payload, so you do NOT need the job's tools available now — the job resolves its own tools when it runs. Put the operator's intent in the payload goal and schedule it. Destructive payloads (rm/drop/delete) are routed to pending_approval and require operator approval outside this model-facing tool before they fire.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "schedule",
            "list",
            "cancel",
            "run_now"
          ],
          "description": "The scheduler operation: schedule (create a task), list (show active + awaiting-approval tasks), cancel (stop a task by id), run_now (fire a task immediately by id)."
        },
        "schedule_kind": {
          "type": "string",
          "enum": [
            "at",
            "every",
            "cron"
          ],
          "description": "Required when action=schedule. at=one-shot at a fixed instant; every=fixed interval in minutes; cron=a cron expression evaluated in the task timezone."
        },
        "cron": {
          "type": "string",
          "description": "Required when action=schedule and schedule_kind=cron. A standard 5-field cron expression, e.g. '30 9 * * 1-5'."
        },
        "at": {
          "type": "string",
          "description": "Required when action=schedule and schedule_kind=at. An RFC-3339 instant, e.g. '2030-01-01T09:30:00Z'."
        },
        "every_minutes": {
          "type": "integer",
          "description": "Required when action=schedule and schedule_kind=every. Interval in minutes (minimum 5)."
        },
        "tz": {
          "type": "string",
          "description": "Optional IANA timezone for a cron schedule, e.g. 'Europe/Rome'. Defaults to the scheduler default timezone."
        },
        "kind": {
          "type": "string",
          "enum": [
            "reminder",
            "agent_job",
            "backup_postgres"
          ],
          "description": "Required when action=schedule. The task kind: reminder (deliver text), agent_job (run an agent turn), backup_postgres (database dump)."
        },
        "payload": {
          "type": "object",
          "description": "Optional when action=schedule. The task payload: for a reminder {\"text\": \"...\"}, for an agent_job {\"goal\": \"...\"}. Scanned for destructive intent (rm/drop/delete) which gates the task to pending_approval."
        },
        "step_budget": {
          "type": "integer",
          "description": "Optional when action=schedule and kind=agent_job. Maximum agent steps for the job run."
        },
        "notify": {
          "type": "string",
          "enum": [
            "whatsapp",
            "email",
            "stdout",
            "telegram"
          ],
          "description": "Optional when action=schedule. Where the task output is delivered. telegram delivers to the operator's bound Telegram chat; whatsapp and email are self-sends to an external address; stdout is the server-console fallback. Defaults to the scheduler default route."
        },
        "task_id": {
          "type": "string",
          "description": "Required when action=cancel or run_now. The id of the target task."
        }
      },
      "required": [
        "action"
      ]
    }
  },
  {
    "name": "todo_write",
    "deferred": true,
    "summary": "Track multi-step work as a todo list (replace the full list each call).",
    "description": "Maintain a structured todo list to plan and track multi-step work. Pass the FULL list each call — it replaces the previous one. Each item has content (imperative), status (pending | in_progress | completed), and an optional activeForm (present-continuous form shown while in_progress). Keep exactly one item in_progress at a time. Use it for non-trivial multi-step tasks; skip it for single-step work.",
    "parameters": {
      "type": "object",
      "properties": {
        "todos": {
          "type": "array",
          "description": "The full todo list; replaces the previous list each call.",
          "items": {
            "type": "object",
            "properties": {
              "content": {
                "type": "string",
                "description": "The task in imperative form."
              },
              "status": {
                "type": "string",
                "description": "One of: pending, in_progress, completed."
              },
              "activeForm": {
                "type": "string",
                "description": "Optional present-continuous form shown while in_progress, e.g. \"Running tests\"."
              }
            },
            "required": [
              "content",
              "status"
            ]
          }
        }
      },
      "required": [
        "todos"
      ]
    }
  },
  {
    "name": "web_fetch",
    "deferred": true,
    "summary": "Fetch a public web page and return it as readable markdown.",
    "description": "Fetch a single public web page over http/https and get back its main content as clean, readable markdown (navigation, ads, and footer chrome are stripped). Use it to read a page you found via web_search or a URL the user gave you. Only http and https URLs to public hosts are allowed — private, loopback, and cloud-metadata targets are blocked, and a redirect to a blocked target is rejected. A large page is truncated in the preview with a tool_call_id you can page through with read_tool_output. A block, an unsupported scheme, an oversized body, a timeout, or an extraction failure come back as a small {error,reason,message} object you should read and adapt to — they are not tool failures. Example: {\"url\":\"https://en.wikipedia.org/wiki/Knowledge_graph\"}.",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "The absolute http(s) URL of a public web page to fetch. Only http and https are supported."
        }
      },
      "required": [
        "url"
      ]
    }
  },
  {
    "name": "web_search",
    "deferred": true,
    "summary": "Search the public web via the configured SearXNG instance.",
    "description": "Search the public web through the configured private SearXNG meta-search instance and get back a ranked list of {title, url, snippet} results. Use it to find pages, recent news, or candidate URLs you can then read with web_fetch. Freshness matters: the general category returns broad web results (good for reference and current structured facts like a live price or a definition) but its snippets can be cached and undated. For the latest developments, breaking news, or anything time-sensitive ('today', 'latest', 'recent'), set category:news together with time_range:day or week, and pass include_metadata:true to read each result's published_at so you can prefer the freshest. You can also narrow with a language hint and a domains allowlist of bare hostnames. Example (reference): {\"query\":\"orbital period of Titan\",\"max_results\":5}. Example (latest news): {\"query\":\"EU AI Act\",\"category\":\"news\",\"time_range\":\"day\",\"include_metadata\":true}. Example (domain-scoped): {\"query\":\"site reliability\",\"domains\":[\"wikipedia.org\"]}.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The search query. Plain words; no engine-specific operators are needed."
        },
        "max_results": {
          "type": "integer",
          "minimum": 1,
          "maximum": 25,
          "description": "Optional cap on the number of results returned (default: the server cap). Omit to use the default."
        },
        "category": {
          "type": "string",
          "enum": [
            "general",
            "news"
          ],
          "description": "Result category. general (default): broad web results, reference pages, and current structured facts (a live price, a definition). news: recent reporting — use it WITH time_range:day|week for the latest developments, breaking events, or 'what happened recently' queries."
        },
        "language": {
          "type": "string",
          "description": "Optional BCP-47-ish language hint (e.g. en, it, de). Omit for the engine default."
        },
        "time_range": {
          "type": "string",
          "enum": [
            "day",
            "week",
            "month",
            "year"
          ],
          "description": "Optional recency window for engines that support it. Use day or week for breaking/recent topics. Omit for no time filter."
        },
        "domains": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional list of bare hostnames (e.g. wikipedia.org) to restrict results to. Hostnames only — no scheme or path."
        },
        "include_metadata": {
          "type": "boolean",
          "description": "When true, attach a metadata tier (engine, score, category, published_at, thumbnail) to each result. Default false keeps the lean {title,url,snippet} shape."
        }
      },
      "required": [
        "query"
      ]
    }
  },
  {
    "name": "whatsapp__download_media",
    "deferred": true,
    "summary": "Download media from a WhatsApp message and get the local file path. Required args: message_id, chat_jid.",
    "description": "Download media from a WhatsApp message and get the local file path.\n\n    Args:\n        message_id: The ID of the message containing the media\n        chat_jid: The JID of the chat containing the message\n\n    Returns:\n        A dictionary containing success status, a status message, and the file path if successful",
    "parameters": {
      "properties": {
        "chat_jid": {
          "title": "Chat Jid",
          "type": "string"
        },
        "message_id": {
          "title": "Message Id",
          "type": "string"
        }
      },
      "required": [
        "message_id",
        "chat_jid"
      ],
      "title": "download_mediaArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__get_chat",
    "deferred": true,
    "summary": "Get WhatsApp chat metadata by JID. Required args: chat_jid.",
    "description": "Get WhatsApp chat metadata by JID.\n\n    Args:\n        chat_jid: The JID of the chat to retrieve\n        include_last_message: Whether to include the last message (default True)",
    "parameters": {
      "properties": {
        "chat_jid": {
          "title": "Chat Jid",
          "type": "string"
        },
        "include_last_message": {
          "default": true,
          "title": "Include Last Message",
          "type": "boolean"
        }
      },
      "required": [
        "chat_jid"
      ],
      "title": "get_chatArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__get_contact",
    "deferred": true,
    "summary": "Look up a WhatsApp contact by phone number, LID, or full JID.",
    "description": "Look up a WhatsApp contact by phone number, LID, or full JID.\n\n    Automatically detects the identifier type and queries appropriately.\n\n    Args:\n        identifier: Phone number, LID, or full JID. Examples:\n                    - \"12025551234\" (phone number)\n                    - \"35047067385985\" (LID - numeric)\n                    - \"12025551234@s.whatsapp.net\" (phone JID)\n                    - \"184125298348272@lid\" (LID JID)\n        phone_number: Backward-compatible alias for `identifier`.\n        phone: Backward-compatible alias for `identifier` (matches README parameter name).\n\n    Returns:\n        Dictionary with jid, name, display_name, is_lid, and resolved status",
    "parameters": {
      "properties": {
        "identifier": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Identifier"
        },
        "phone": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Phone"
        },
        "phone_number": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Phone Number"
        }
      },
      "title": "get_contactArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__get_contact_chats",
    "deferred": true,
    "summary": "Get all WhatsApp chats involving the contact. Required args: jid.",
    "description": "Get all WhatsApp chats involving the contact.\n\n    Args:\n        jid: The contact's JID to search for\n        limit: Maximum number of chats to return (default 20)\n        page: Page number for pagination (default 0)",
    "parameters": {
      "properties": {
        "jid": {
          "title": "Jid",
          "type": "string"
        },
        "limit": {
          "default": 20,
          "title": "Limit",
          "type": "integer"
        },
        "page": {
          "default": 0,
          "title": "Page",
          "type": "integer"
        }
      },
      "required": [
        "jid"
      ],
      "title": "get_contact_chatsArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__get_direct_chat_by_contact",
    "deferred": true,
    "summary": "Get WhatsApp chat metadata by sender phone number. Required args: sender_phone_number.",
    "description": "Get WhatsApp chat metadata by sender phone number.\n\n    Args:\n        sender_phone_number: The phone number to search for",
    "parameters": {
      "properties": {
        "sender_phone_number": {
          "title": "Sender Phone Number",
          "type": "string"
        }
      },
      "required": [
        "sender_phone_number"
      ],
      "title": "get_direct_chat_by_contactArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__get_last_interaction",
    "deferred": true,
    "summary": "Get most recent WhatsApp message involving the contact. Required args: jid.",
    "description": "Get most recent WhatsApp message involving the contact.\n\n    Args:\n        jid: The JID of the contact to search for\n\n    Returns:\n        Message dictionary with id, timestamp, sender, content, etc. or empty dict if not found.",
    "parameters": {
      "properties": {
        "jid": {
          "title": "Jid",
          "type": "string"
        }
      },
      "required": [
        "jid"
      ],
      "title": "get_last_interactionArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__get_media_data",
    "deferred": true,
    "summary": "Return a message's image as a data: URL, for a rendered view to display. Required args: message_id, chat_jid.",
    "description": "Return a message's image as a data: URL, for a rendered view to display.\n\n    `download_media` gives back a path inside this container, which a browser can\n    neither open nor fetch, so a view could only ever show a placeholder where a\n    photo belongs. This carries the bytes in the result instead.\n\n    Read-only, and images only: nothing is sent, nothing is modified, and a file\n    that is not an image a view can paint -- or that is over the inline cap -- is\n    refused with the reason rather than silently returned as a blank.\n\n    Args:\n        message_id: The ID of the message containing the media\n        chat_jid: The JID of the chat containing the message\n\n    Returns:\n        On success the mime type, the byte count and a `data:` URL; otherwise\n        `success: False` and a `reason` of unavailable, not_inlinable, too_large or\n        unreadable.",
    "parameters": {
      "properties": {
        "chat_jid": {
          "title": "Chat Jid",
          "type": "string"
        },
        "message_id": {
          "title": "Message Id",
          "type": "string"
        }
      },
      "required": [
        "message_id",
        "chat_jid"
      ],
      "title": "get_media_dataArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__get_message_context",
    "deferred": true,
    "summary": "Get context around a specific WhatsApp message. Required args: message_id.",
    "description": "Get context around a specific WhatsApp message.\n\n    Args:\n        message_id: The ID of the message to get context for\n        before: Number of messages to include before the target message (default 5)\n        after: Number of messages to include after the target message (default 5)",
    "parameters": {
      "properties": {
        "after": {
          "default": 5,
          "title": "After",
          "type": "integer"
        },
        "before": {
          "default": 5,
          "title": "Before",
          "type": "integer"
        },
        "message_id": {
          "title": "Message Id",
          "type": "string"
        }
      },
      "required": [
        "message_id"
      ],
      "title": "get_message_contextArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__list_chats",
    "deferred": true,
    "summary": "Get WhatsApp chats matching specified criteria.",
    "description": "Get WhatsApp chats matching specified criteria.\n\n    Args:\n        query: Search term to filter chats by name or JID\n        limit: Max chats to return (default 50, max 200)\n        page: Page number for pagination (default 0)\n        include_last_message: Include the last message in each chat (default True)\n        sort_by: \"last_active\" (default, most recent first) or \"name\" (alphabetical)",
    "parameters": {
      "properties": {
        "include_last_message": {
          "default": true,
          "title": "Include Last Message",
          "type": "boolean"
        },
        "limit": {
          "default": 50,
          "title": "Limit",
          "type": "integer"
        },
        "page": {
          "default": 0,
          "title": "Page",
          "type": "integer"
        },
        "query": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Query"
        },
        "sort_by": {
          "default": "last_active",
          "title": "Sort By",
          "type": "string"
        }
      },
      "title": "list_chatsArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__list_messages",
    "deferred": true,
    "summary": "Get WhatsApp messages matching specified criteria with optional context.",
    "description": "Get WhatsApp messages matching specified criteria with optional context.\n\n    Each message includes sender_display showing \"Name (phone)\" for easy identification.\n\n    Args:\n        after: ISO-8601 date string (e.g., \"2026-01-01\" or \"2026-01-01T09:00:00\")\n        before: ISO-8601 date string (e.g., \"2026-01-09\" or \"2026-01-09T18:00:00\")\n        sender_phone_number: Phone number to filter by sender (e.g., \"12025551234\")\n        chat_jid: Chat JID to filter by (e.g., \"12025551234@s.whatsapp.net\" or group JID)\n        query: Search term to filter messages by content\n        limit: Max messages to return (default 50, max 500)\n        page: Page number for pagination (default 0)\n        include_context: Include surrounding messages for context (default True)\n        context_before: Messages to include before each match (default 1)\n        context_after: Messages to include after each match (default 1)\n        sort_by: \"newest\" (default, most recent first) or \"oldest\" (chronological)",
    "parameters": {
      "properties": {
        "after": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "After"
        },
        "before": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Before"
        },
        "chat_jid": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Chat Jid"
        },
        "context_after": {
          "default": 1,
          "title": "Context After",
          "type": "integer"
        },
        "context_before": {
          "default": 1,
          "title": "Context Before",
          "type": "integer"
        },
        "include_context": {
          "default": true,
          "title": "Include Context",
          "type": "boolean"
        },
        "limit": {
          "default": 50,
          "title": "Limit",
          "type": "integer"
        },
        "page": {
          "default": 0,
          "title": "Page",
          "type": "integer"
        },
        "query": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Query"
        },
        "sender_phone_number": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Sender Phone Number"
        },
        "sort_by": {
          "default": "newest",
          "title": "Sort By",
          "type": "string"
        }
      },
      "title": "list_messagesArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__search_contacts",
    "deferred": true,
    "summary": "Search WhatsApp contacts by name or phone number. Required args: query.",
    "description": "Search WhatsApp contacts by name or phone number.\n\n    Args:\n        query: Search term to match against contact names or phone numbers",
    "parameters": {
      "properties": {
        "query": {
          "title": "Query",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "title": "search_contactsArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__send_audio_message",
    "deferred": true,
    "summary": "Send any audio file as a WhatsApp audio message to the specified recipient. For group messages use the JID. If it errors due to ffmpeg not being installed, use send_file instead. Required args: recipient, media_path.",
    "description": "Send any audio file as a WhatsApp audio message to the specified recipient. For group messages use the JID. If it errors due to ffmpeg not being installed, use send_file instead.\n\n    Args:\n        recipient: The recipient - either a phone number with country code but no + or other symbols,\n                 or a JID (e.g., \"123456789@s.whatsapp.net\" or a group JID like \"123456789@g.us\")\n        media_path: The absolute path to the audio file to send (will be converted to Opus .ogg if it's not a .ogg file)\n\n    Returns:\n        A dictionary containing success status and a status message",
    "parameters": {
      "properties": {
        "media_path": {
          "title": "Media Path",
          "type": "string"
        },
        "recipient": {
          "title": "Recipient",
          "type": "string"
        }
      },
      "required": [
        "recipient",
        "media_path"
      ],
      "title": "send_audio_messageArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__send_file",
    "deferred": true,
    "summary": "Send a file such as a picture, raw audio, video or document via WhatsApp to the specified recipient. For group messages use the JID. Required args: recipient, media_path.",
    "description": "Send a file such as a picture, raw audio, video or document via WhatsApp to the specified recipient. For group messages use the JID.\n\n    Args:\n        recipient: The recipient - either a phone number with country code but no + or other symbols,\n                 or a JID (e.g., \"123456789@s.whatsapp.net\" or a group JID like \"123456789@g.us\")\n        media_path: The absolute path to the media file to send (image, video, document)\n\n    Returns:\n        A dictionary containing success status and a status message",
    "parameters": {
      "properties": {
        "media_path": {
          "title": "Media Path",
          "type": "string"
        },
        "recipient": {
          "title": "Recipient",
          "type": "string"
        }
      },
      "required": [
        "recipient",
        "media_path"
      ],
      "title": "send_fileArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__send_message",
    "deferred": true,
    "summary": "Send a WhatsApp message to a person or group. For group chats use the JID. Required args: recipient, message.",
    "description": "Send a WhatsApp message to a person or group. For group chats use the JID.\n\n    Args:\n        recipient: The recipient - either a phone number with country code but no + or other symbols,\n                 or a JID (e.g., \"123456789@s.whatsapp.net\" or a group JID like \"123456789@g.us\")\n        message: The message text to send\n        quoted_message_id: ID of the message to reply to (optional). When set, the sent\n                           message will appear as a quoted reply in WhatsApp.\n        quoted_sender_jid: Full JID of the author of the quoted message. Required for\n                           group replies so WhatsApp renders the correct attribution.\n        quoted_content: Text content of the quoted message, used for the reply preview.\n                        Only plain text is supported; media previews are not included.\n\n    Returns:\n        A dictionary containing success status and a status message",
    "parameters": {
      "properties": {
        "message": {
          "title": "Message",
          "type": "string"
        },
        "quoted_content": {
          "default": "",
          "title": "Quoted Content",
          "type": "string"
        },
        "quoted_message_id": {
          "default": "",
          "title": "Quoted Message Id",
          "type": "string"
        },
        "quoted_sender_jid": {
          "default": "",
          "title": "Quoted Sender Jid",
          "type": "string"
        },
        "recipient": {
          "title": "Recipient",
          "type": "string"
        }
      },
      "required": [
        "recipient",
        "message"
      ],
      "title": "send_messageArguments",
      "type": "object"
    }
  },
  {
    "name": "whatsapp__send_reaction",
    "deferred": true,
    "summary": "Send (or remove) a reaction to a WhatsApp message. Required args: recipient, message_id, emoji.",
    "description": "Send (or remove) a reaction to a WhatsApp message.\n\n    Args:\n        recipient: The chat JID the message belongs to (e.g., \"12025551234@s.whatsapp.net\"\n                   or a group JID like \"123456789@g.us\")\n        message_id: The ID of the message to react to\n        emoji: The reaction emoji (e.g., \"👍\"). Pass an empty string to remove the reaction.\n        from_me: Whether the original message was sent by the current user (default False)\n        sender_jid: JID of the original message sender — required for group messages when\n                    from_me is False so the bridge can build the correct WhatsApp key\n\n    Returns:\n        A dictionary containing success status and a status message",
    "parameters": {
      "properties": {
        "emoji": {
          "title": "Emoji",
          "type": "string"
        },
        "from_me": {
          "default": false,
          "title": "From Me",
          "type": "boolean"
        },
        "message_id": {
          "title": "Message Id",
          "type": "string"
        },
        "recipient": {
          "title": "Recipient",
          "type": "string"
        },
        "sender_jid": {
          "default": "",
          "title": "Sender Jid",
          "type": "string"
        }
      },
      "required": [
        "recipient",
        "message_id",
        "emoji"
      ],
      "title": "send_reactionArguments",
      "type": "object"
    }
  }
]
