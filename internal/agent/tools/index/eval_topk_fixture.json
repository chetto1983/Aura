[
  {
    "query": "search Aura memory wiki pages notes",
    "expected_tool": "search_memory",
    "rationale": "Both 'search' and 'memory' appear in the tool name; description mentions wiki, pages, and notes explicitly."
  },
  {
    "query": "generate document xlsx docx pdf from blocks",
    "expected_tool": "doc",
    "rationale": "'doc' is in the tool name; description covers xlsx, docx, and pdf generation from structured blocks."
  },
  {
    "query": "schedule cancel reminder task recurring",
    "expected_tool": "task",
    "rationale": "'task' is in the tool name; description explicitly covers scheduling, cancelling, and reminders."
  },
  {
    "query": "search web fetch page current information URL",
    "expected_tool": "web",
    "rationale": "'web' is in the tool name; description covers both web search and URL fetching."
  },
  {
    "query": "execute Python code sandbox computation",
    "expected_tool": "execute_code",
    "rationale": "Both 'execute' and 'code' are in the tool name; description mentions Python sandbox explicitly."
  },
  {
    "query": "ask user clarification question approval pause",
    "expected_tool": "ask_user",
    "rationale": "Both 'ask' and 'user' are in the tool name; description covers clarification and approval flows."
  },
  {
    "query": "read write list search workspace files patch",
    "expected_tool": "file",
    "rationale": "'file' is in the tool name; description explicitly covers read, write, list, search, and patch actions."
  },
  {
    "query": "store source PDF upload ingest extract",
    "expected_tool": "source",
    "rationale": "'source' is in the tool name; description covers storing, uploading PDFs, and ingesting pipeline."
  },
  {
    "query": "daily briefing morning tasks today attention",
    "expected_tool": "daily_briefing",
    "rationale": "Both 'daily' and 'briefing' are in the tool name; description is about the daily morning review."
  },
  {
    "query": "find tools catalog natural language query discover",
    "expected_tool": "tool_search",
    "rationale": "Both 'tool' and 'search' are in the tool name; description is about finding tools by natural-language query."
  },
  {
    "query": "dashboard token login bearer access",
    "expected_tool": "request_dashboard_token",
    "rationale": "Both 'dashboard' and 'token' appear in the tool name; description covers dashboard login and token minting."
  },
  {
    "query": "create edit replace append wiki page content",
    "expected_tool": "wiki_page",
    "rationale": "Both 'wiki' and 'page' are in the tool name; description explicitly covers create, edit, replace, and append actions."
  },
  {
    "query": "manage python script registry dev tool save",
    "expected_tool": "dev_tool",
    "rationale": "Both 'dev' and 'tool' are in the tool name; description covers Python script registry management."
  },
  {
    "query": "shell command diagnostics git runtime inspect",
    "expected_tool": "execute_shell",
    "rationale": "Both 'execute' and 'shell' are in the tool name; description covers shell commands, diagnostics, and git."
  }
]
