# Binaries
/aura
/cmd/aura/aura

# Environment
.env
.env.*
!.env.example

# MCP runtime config (template tracked as mcp.example.json)
/mcp.json

# skills.sh CLI lockfile (per-install metadata, not source)
/skills-lock.json

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Goversioninfo output
/cmd/aura/resource.syso
/internal/tray/icon_app.ico

# Wiki data (tracked via git inside wiki dir)*.exe*
*.db
.claude/*
*.pdf
wiki/*
.claude/settings.json

# Playwright (E2E artifacts and locally-resolved browsers)
/web/playwright-report/
/web/test-results/
/web/blob-report/
/web/playwright/.cache/
/web/.e2e-token

# Coverage artifacts (Q&A session output, not source of truth)
/cover.out
/cover.html
/$cover
.codex-*
*.log

# Local benchmark/history artifacts
/reports/memory-quality/
.agents/skills/*

# Local sandbox runtime bundle (release artifact, not source)
/runtime/pyodide/
/runtime/_downloads/
