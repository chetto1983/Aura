# Failure Modes — 2026-05-19 — git_head: 4e2eb06d3ce7c02044223b47d5e14a76d15ff4b3

Static analysis only. No live probes were run. User-visible impact is stated from the Telegram/web-chat execution paths where code is explicit; otherwise it is marked for live confirmation.

## External dependencies

| dep | transport/URL config | health check (file:line) | failure behavior | fallback (file:line) | user-visible impact |
|---|---|---|---|---|---|
| qdrant (vector store) | HTTP REST via `QDRANT_URL`, `QDRANT_COLLECTION`, `QDRANT_API_KEY` in `compose.yaml:66`, `compose.yaml:67`, `compose.yaml:68`; config load in `internal/config/config.go:250`, `internal/config/config.go:251`, `internal/config/config.go:252`; service in `compose.yaml:192`-`compose.yaml:199`. | Boot gate calls `GET /readyz` in `internal/storage/qdrant/client.go:58`-`internal/storage/qdrant/client.go:71`, waits in `internal/storage/qdrant/client.go:220`-`internal/storage/qdrant/client.go:246`, and fails app startup when configured but unhealthy in `cmd/aura/app.go:187`-`cmd/aura/app.go:191`. | Crash/fail-fast at startup when `QDRANT_URL` is set and readiness fails (`cmd/aura/app.go:187`-`cmd/aura/app.go:191`). Runtime query/upsert HTTP/network failures are returned from `internal/storage/qdrant/client.go:280` and `internal/storage/qdrant/client.go:294`. Wiki vector setup/indexing failures warn and continue in `cmd/aura/app.go:235`-`cmd/aura/app.go:245`. | Tool-vector readiness falls back to FTS in `internal/agent/tools/registry/registry.go:371`-`internal/agent/tools/registry/registry.go:390`; compact-memory vector mirror falls back to SQLite-only in `cmd/aura/app.go:373`-`cmd/aura/app.go:385`; `search_memory` reports warnings instead of failing the whole tool in `internal/agent/tools/registry/memory_search.go:270`-`internal/agent/tools/registry/memory_search.go:335`. | If boot readiness fails, Aura is unavailable in Telegram and web chat. If Qdrant degrades after boot, semantic memory/tool-vector recall degrades to FTS/SQLite where available, and the LLM may answer with less relevant context. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm exact Telegram/web wording. |
| embedding sidecar (`embeddinggemma-300m` via llama.cpp) | OpenAI-compatible embeddings over HTTP. `EMBEDDING_BASE_URL=http://aura-llama-embed:8080/v1`, `EMBEDDING_MODEL`, `EMBEDDING_API_KEY`, and output dim are configured in `compose.yaml:69`-`compose.yaml:72`; llama.cpp embedding sidecar and `embeddinggemma-300m-Q4_0.gguf` are declared in `compose.yaml:201`-`compose.yaml:229`; config load is in `internal/config/config.go:271`-`internal/config/config.go:275`. | No explicit boot health check detected. Health is call-time only: single embeddings POST to `/embeddings` in `internal/storage/search/embed_http.go:69`; batch embeddings POST to `/embeddings` in `internal/storage/search/embed_batch.go:65`; app creates embedding functions only when configured in `cmd/aura/app.go:219`-`cmd/aura/app.go:222`. | Error returned to caller on request/read/non-2xx/decode/empty-vector failures in `internal/storage/search/embed_http.go:76`-`internal/storage/search/embed_http.go:96` and `internal/storage/search/embed_batch.go:72`-`internal/storage/search/embed_batch.go:102`. Wiki indexing warns and continues when indexing fails in `cmd/aura/app.go:242`-`cmd/aura/app.go:243`. Tool-vector query embedding failures are returned in `internal/agent/tools/registry/registry_search_vector.go:150`-`internal/agent/tools/registry/registry_search_vector.go:177`. | Embedding cache creation failure falls back to uncached embeddings in `cmd/aura/app.go:225`-`cmd/aura/app.go:231`. Tool-vector readiness can fall back to FTS in `internal/agent/tools/registry/registry.go:371`-`internal/agent/tools/registry/registry.go:390`. `search_memory` can still return compact/wiki warnings and any other available source in `internal/agent/tools/registry/memory_search.go:270`-`internal/agent/tools/registry/memory_search.go:335`. | Semantic search/indexing becomes degraded or unavailable; the user may receive answers without expected memory/wiki recall. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm exact chat response. |
| searxng (web search) | HTTP JSON search via `WEB_SEARCH_PROVIDER=searxng` and `SEARXNG_BASE_URL=http://searxng:8080` in `compose.yaml:59`-`compose.yaml:60`; config load in `internal/config/config.go:242`; web tool registration in `cmd/aura/app.go:344`-`cmd/aura/app.go:349`. | No boot health check detected. Per-call search hits `GET /search?q=...&format=json` in `internal/agent/tools/registry/searxng.go:106`-`internal/agent/tools/registry/searxng.go:160`; client timeout is set in `internal/agent/tools/registry/searxng.go:26`-`internal/agent/tools/registry/searxng.go:29`. | Error returned to caller. `Execute` wraps search failures as `web_search: ...` in `internal/agent/tools/registry/searxng.go:74`-`internal/agent/tools/registry/searxng.go:88`; request/read/non-2xx/decode failures are returned in `internal/agent/tools/registry/searxng.go:142`-`internal/agent/tools/registry/searxng.go:160`; agent executor turns tool errors into `Error: ...` tool output in `internal/agent/executor.go:179`-`internal/agent/executor.go:187`. | No alternate search backend detected. Unified `web` still supports direct fetch when a URL is known in `internal/agent/tools/registry/web.go:119`-`internal/agent/tools/registry/web.go:121`; unknown web-search provider disables search registration in `cmd/aura/app.go:344`-`cmd/aura/app.go:349`. | Web search requests fail or time out; the assistant may continue without fresh search results or report the tool error. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm exact Telegram/web wording. |
| garage (S3-compatible artifact/backup store) | S3-compatible HTTP configured by `GARAGE_S3_ENDPOINT`, `GARAGE_S3_REGION`, `GARAGE_S3_BUCKET`, `GARAGE_S3_ACCESS_KEY_FILE`, `GARAGE_S3_SECRET_KEY_FILE` in `compose.yaml:61`-`compose.yaml:65`; app config load supports `GARAGE_S3_ACCESS_KEY`/`GARAGE_S3_SECRET_KEY` and their `_FILE` variants in `internal/config/config.go:245`-`internal/config/config.go:249` and `internal/config/env.go:18`-`internal/config/env.go:29`; path-style S3 client is built in `internal/backup/s3.go:130`-`internal/backup/s3.go:142`; compose waits for `garage-init` in `compose.yaml:110`-`compose.yaml:116`. | No in-process boot health check detected. On-demand backup endpoints construct the manager in `internal/api/backups.go:75`-`internal/api/backups.go:95`; S3 calls happen through `PutObject` and paginated `ListObjects` in `internal/backup/s3.go:64`-`internal/backup/s3.go:80`. | Error returned to caller. Missing config produces HTTP 503 in `internal/api/backups.go:84`-`internal/api/backups.go:89`; client construction/list/export failures produce HTTP 502 in `internal/api/backups.go:25`, `internal/api/backups.go:55`, and `internal/api/backups.go:94`; export aborts on archive/manifest upload errors in `internal/backup/export.go:101`, `internal/backup/export.go:123`-`internal/backup/export.go:125`, and `internal/backup/export.go:178`-`internal/backup/export.go:180`. | No alternate S3/artifact backup backend detected. Export archives are temporary local files before upload in `internal/backup/export.go:159`-`internal/backup/export.go:180`; failure leaves canonical local data untouched. | Web backup/list/export routes return 503/502. Telegram chat impact is not explicit unless a chat/tool path invokes backups. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm UI toast/body text. |
| mistral OCR (`/v1/ocr`) | HTTPS Mistral API. Default base URL is `https://api.mistral.ai/v1` in `internal/config/config.go:147`; config load for `MISTRAL_API_KEY`, `MISTRAL_OCR_MODEL`, `MISTRAL_OCR_BASE_URL`, `MISTRAL_OCR_TABLE_FORMAT`, `MISTRAL_OCR_EXTRACT_HEADER`, and `MISTRAL_OCR_EXTRACT_FOOTER` is in `internal/config/config.go:304`-`internal/config/config.go:309`; runtime client is enabled only with API key in `cmd/aura/app.go:268`-`cmd/aura/app.go:278`. The client posts to `baseURL + "/ocr"` in `internal/storage/sources/ocr/client.go:121`, so the default endpoint is `/v1/ocr`; the test asserts `/v1/ocr` in `internal/storage/sources/ocr/client_test.go:19`-`internal/storage/sources/ocr/client_test.go:20`. | No boot health check detected. Per-call OCR request/response validation is in `internal/storage/sources/ocr/client.go:97`-`internal/storage/sources/ocr/client.go:149`. | Graceful disabled state when no key is configured (`cmd/aura/app.go:268`-`cmd/aura/app.go:278`). When enabled, request/read/non-2xx/decode failures are returned from `internal/storage/sources/ocr/client.go:131`-`internal/storage/sources/ocr/client.go:149`. `ocr_source` records source status `failed` and returns `ocr_source: ...` on OCR error in `internal/agent/tools/registry/source_ocr.go:69`-`internal/agent/tools/registry/source_ocr.go:77`; unified source reprocess returns `ocr backend unavailable` or wrapped OCR errors in `internal/agent/tools/registry/source_unified.go:256`-`internal/agent/tools/registry/source_unified.go:270`. | No PDF OCR fallback detected. Markitdown handles other extraction paths when configured in `cmd/aura/app.go:282`-`cmd/aura/app.go:287`, but OCR-specific PDF processing fails when Mistral is unavailable. | PDF OCR/reprocess fails and the source can be marked failed; the LLM receives a tool error if OCR is invoked through chat. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm upload/reprocess wording in Telegram/web. |
| OpenAI-compatible LLM endpoint | OpenAI-compatible chat completions. `LLM_BASE_URL`, `LLM_API_KEY`, and `LLM_MODEL` load in `internal/config/config.go:237`-`internal/config/config.go:239`; app client creation uses `llm.NewOpenAIClient` and wraps `llm.NewRetryClient` in `cmd/aura/helpers.go:59`-`cmd/aura/helpers.go:65`; non-streaming and streaming POST to `/chat/completions` in `internal/llm/openai.go:231` and `internal/llm/openai.go:318`. | No boot health check detected. Health is per chat request; non-2xx responses become `APIError` in `internal/llm/openai.go:246` and `internal/llm/openai.go:333`. | Retry on transient pre-response failures. Retry defaults are in `internal/llm/retry.go:38`-`internal/llm/retry.go:45`; `Send` retries transients in `internal/llm/retry.go:73`-`internal/llm/retry.go:107`; `Stream` retries pre-stream transients in `internal/llm/retry.go:116`-`internal/llm/retry.go:149`. 429/5xx/deadline/timeouts are classified in `internal/llm/classify.go:80`-`internal/llm/classify.go:132`. Mid-stream token errors are not retried by policy in `internal/llm/retry.go:111`-`internal/llm/retry.go:115` and surface through `internal/llm/openai.go:604`. Agent loop returns interrupted content on deadline after tool activity or a generic apology/error otherwise in `internal/agent/loop.go:322`-`internal/agent/loop.go:336`. | No alternate LLM provider fallback detected. If no API key is configured, app runs echo mode instead of the LLM in `cmd/aura/app.go:172`-`cmd/aura/app.go:176`; after deadline with tool calls, interrupted-content fallback is built in `internal/agent/loop_dedup.go:43`-`internal/agent/loop_dedup.go:49`. | Chat turns can be delayed by backoff, end with partial/interrupted output, or return "Sorry, I couldn't process your message. Please try again." from `internal/agent/loop.go:336`. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm streaming/web/Telegram timing. |
| replicate (image generation, when configured) | Not wired in runtime at this SHA. Planning docs mention `REPLICATE_API_KEY` and `IMAGE_GEN_BACKEND=replicate` in `docs/phase-mm-image-plan-2026-05-17.md:317`-`docs/phase-mm-image-plan-2026-05-17.md:351`, but no Replicate config exists in the runtime config block `internal/config/config.go:63`-`internal/config/config.go:150` or overlay key list `internal/config/applier.go:28`-`internal/config/applier.go:85`. | Not applicable: no runtime Replicate client, tool, or health check detected at this SHA. | Not applicable in current runtime. No HTTP request, retry, or failure path detected for Replicate image generation. | Not applicable in current runtime. README/planning mention future image generation, but no registered `generate_image` tool was detected at this SHA (`README.md:224`; `docs/phase-mm-image-plan-2026-05-17.md:317`-`docs/phase-mm-image-plan-2026-05-17.md:351`). | Replicate outages have no current runtime impact because the dependency is not configured or called. Image-generation requests have no Replicate-backed tool path at this SHA. STATIC-ANALYSIS-INSUFFICIENT: needs live probe only if local runtime contains uncommitted image-tool wiring. |
| MCP servers (stdio and Streamable-HTTP) | Per-server config file from `MCP_SERVERS_PATH` in `internal/config/config.go:264`; schema supports either stdio command/args/env or Streamable-HTTP URL/headers in `internal/mcp/config.go:13`-`internal/mcp/config.go:24`; missing MCP config path/file returns no servers without error in `internal/mcp/config.go:35`-`internal/mcp/config.go:43`. | Per-server startup bootstrap is the health gate: stdio client starts command and bootstraps in `internal/mcp/client.go:72`-`internal/mcp/client.go:80`; HTTP client bootstraps in `internal/mcp/client.go:86`-`internal/mcp/client.go:91`; bootstrap calls `initialize` and `tools/list` in `internal/mcp/client.go:154`-`internal/mcp/client.go:212`. | Graceful skip at app startup. Config load errors warn and continue without MCP in `cmd/aura/app.go:473`-`cmd/aura/app.go:476`; client creation/bootstrap errors warn and skip that server in `cmd/aura/app.go:484`-`cmd/aura/app.go:489`; runtime `tools/call` errors are returned from `internal/mcp/client.go:110`-`internal/mcp/client.go:139`. Stdio context cancellation closes stdin and returns `ctx.Err()` in `internal/mcp/client.go:321`-`internal/mcp/client.go:327`; HTTP non-200 responses error in `internal/mcp/client.go:401`-`internal/mcp/client.go:406`. | Other MCP servers and built-in tools remain available. Config watcher detects changes, but runtime reconnect/reload is deferred in `internal/mcp/watcher.go:1`-`internal/mcp/watcher.go:10`; missing tools are reported as not found by registry execution in `internal/agent/tools/registry/registry.go:264`-`internal/agent/tools/registry/registry.go:273`. | MCP-backed tool calls fail with unavailable/not-found/tool-error output while the chat loop continues. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm exact Telegram/web wording for each server/tool. |

## Internal failure modes

| internal mode | trigger | code path | user-visible impact |
|---|---|---|---|
| `context.DeadlineExceeded` mid-turn (`MaxElapsed`) | Per-turn elapsed budget reaches zero or the LLM call returns deadline exceeded while a turn is in progress. Defaults and clamping are in `internal/agent/loop.go:207`-`internal/agent/loop.go:233`. | Remaining-budget check sets `MaxElapsedHit` and `StopReason=max_elapsed_hit`, then returns `finalAnswerOnBudget(lastToolResult)` in `internal/agent/loop.go:263`-`internal/agent/loop.go:274`; chat deadline after prior tool calls returns interrupted assistant content in `internal/agent/loop.go:322`-`internal/agent/loop.go:336` and `internal/agent/loop_dedup.go:43`-`internal/agent/loop_dedup.go:49`. | User may see a partial/fallback answer based on the last tool result, or the generic apology from `internal/agent/loop.go:336`. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm channel-specific rendering. |
| `MaxIterations` hit (per-turn cap, not `MaxToolCalls`) | Loop iterations reach configured cap after clamping to `MaxIterationsCeiling`. | Cap setup is in `internal/agent/loop.go:207`-`internal/agent/loop.go:229`; exhaustion path sets stop metadata and attempts one final tool-less answer in `internal/agent/loop.go:629`-`internal/agent/loop.go:646`; finalization call is in `internal/agent/loop.go:649`-`internal/agent/loop.go:679`. | User receives a best-effort final response or the last tool-result fallback from `internal/agent/loop_dedup.go:75`-`internal/agent/loop_dedup.go:80`; answer may be incomplete. |
| Empty LLM response after a tool call | LLM returns no assistant content and no tool calls after prior tool execution. | Empty response with no tool calls returns `finalAnswerOnBudget(lastToolResult)` in `internal/agent/loop.go:355`-`internal/agent/loop.go:356`; fallback formats the last tool result or a generic budget message in `internal/agent/loop_dedup.go:75`-`internal/agent/loop_dedup.go:80`. | User can see raw-ish last tool output or a terse fallback instead of a natural final answer. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm Telegram/web formatting. |
| LLM tool-call arg parse error | OpenAI-compatible response contains malformed tool-call arguments that JSON decode and repair cannot parse. | Non-streaming and streaming responses route through `parseToolCalls` in `internal/llm/openai.go:259` and `internal/llm/openai.go:525`; argument parsing and limited brace/bracket repair happen in `internal/llm/openai.go:404`-`internal/llm/openai.go:422`; stream token parse errors surface as `Token.Err` in `internal/llm/openai.go:604`. | Pre-stream parse errors may be retried as LLM errors by `internal/llm/retry.go:73`-`internal/llm/retry.go:149`; mid-stream parse errors are not retried by policy in `internal/llm/retry.go:111`-`internal/llm/retry.go:115`. User likely sees retry delay or the generic apology. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm. |
| Sandbox unavailable (Python exec) | Sandbox disabled by config, unavailable at startup, or execution tool is requested without a registered/allowed sandbox tool. | Sandbox setup returns nil when disabled in `cmd/aura/helpers.go:83`-`cmd/aura/helpers.go:88`, checks availability in `cmd/aura/helpers.go:104`-`cmd/aura/helpers.go:114`, and builds runtime in `cmd/aura/helpers.go:116`-`cmd/aura/helpers.go:129`; app wiring registers sandbox tools only when constructors return non-nil in `cmd/aura/app_wire.go:279`-`cmd/aura/app_wire.go:283`; execute-code/shell constructors return nil for nil or unsupported runtimes in `internal/agent/tools/registry/exec.go:31`-`internal/agent/tools/registry/exec.go:43`; missing/not-allowed tools fail in `internal/agent/tools/registry/registry.go:264`-`internal/agent/tools/registry/registry.go:273` and `internal/agent/executor.go:153`-`internal/agent/executor.go:157`. | User sees code execution unavailable, a tool-not-found/not-allowed error, or an apology after the LLM receives the tool error. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm exact wording. |
| Capability deny (identity authorizer says no) | Actor lacks required capability for a tool or there is no authorizer. | Registry authorization is enforced in `internal/agent/tools/registry/registry.go:275`-`internal/agent/tools/registry/registry.go:286` and `internal/agent/tools/registry/registry.go:342`-`internal/agent/tools/registry/registry.go:364`; identity-store decisions and deny audit recording are in `internal/identity/store_auth.go:180`-`internal/identity/store_auth.go:236` and `internal/identity/store_auth.go:254`-`internal/identity/store_auth.go:259`; executor converts the denial to a formatted tool error in `internal/agent/executor.go:179`-`internal/agent/executor.go:187`. | Chat continues with a permission/tool error visible to the LLM and possibly summarized to the user. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm Telegram/web phrasing. |
| Phantom tool detection | LLM text claims a tool action happened, but the response contains no actual tool call and the claimed tool was not called in the current turn. | Guard construction and matching are in `internal/agent/phantom_guard.go:38`-`internal/agent/phantom_guard.go:90`; performative proximity check is in `internal/agent/phantom_guard.go:131`-`internal/agent/phantom_guard.go:222`; loop injects correction and continues in `internal/agent/loop.go:358`-`internal/agent/loop.go:377`. | Ideally hidden from final output after correction. In streaming channels the premature phantom text may already have been sent unless UI buffering prevents it. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm. |
| Duplicate tool call (dedup) | Same tool name and canonical JSON arguments repeat in the current batch or a prior non-retryable result exists. | Dedupe keying and canonical JSON are in `internal/agent/dedupe.go:12`-`internal/agent/dedupe.go:35`; loop applies dedupe in `internal/agent/loop.go:430`-`internal/agent/loop.go:468`; duplicate synthetic result is built in `internal/agent/loop_dedup.go:24`-`internal/agent/loop_dedup.go:29`. | Duplicate work is suppressed; the LLM receives a synthetic duplicate-tool result and can continue. User normally sees no direct error unless the LLM summarizes the duplicate result. |
| LLM rate-limited (`429`) | LLM endpoint returns HTTP 429 or a rate-limit-shaped error message. | HTTP non-2xx becomes `APIError` in `internal/llm/openai.go:246` and `internal/llm/openai.go:333`; 429 and rate-limit text classify as transient in `internal/llm/classify.go:101`-`internal/llm/classify.go:102` and `internal/llm/classify.go:121`-`internal/llm/classify.go:122`; retry/backoff is in `internal/llm/retry.go:73`-`internal/llm/retry.go:149`. | User sees a delayed response if retry succeeds; if retries exhaust, the agent loop returns the generic apology/error path in `internal/agent/loop.go:322`-`internal/agent/loop.go:336`. |
| LLM transient network error (connection refused/reset) | Chat completion request cannot connect, connection resets, or another non-permanent network error is returned by the HTTP client. | HTTP request errors are returned from `internal/llm/openai.go:231`-`internal/llm/openai.go:240` and `internal/llm/openai.go:318`-`internal/llm/openai.go:327`; timeouts and unknown non-permanent errors classify as transient in `internal/llm/classify.go:113`-`internal/llm/classify.go:115` and `internal/llm/classify.go:132`; retry/backoff is in `internal/llm/retry.go:73`-`internal/llm/retry.go:149`. | User sees retry latency and then either a normal answer or the generic apology from `internal/agent/loop.go:336`. STATIC-ANALYSIS-INSUFFICIENT: needs live probe to confirm exact rendering for refused vs reset connections. |
