# Session handoff — 1.7 on-channel scheduled-task approval (E2E + ship)

**Date:** 2026-07-24
**Outcome:** ✅ **Shipped.** `master` @ `58060fe5e`, pushed, **CI green** (CI + Skills + CodeQL all success).
**Branch:** `fix/1.7-scheduled-approval-on-channel` (merged FF into master; still exists — delete when convenient).

## What happened

Task was: test E2E the 1.7 branch, update the container, close & merge. The live E2E (real stack: Telegram + DeepSeek-V4-Flash + cockpit) caught **five real Telegram-only bugs** that all unit tests and the full local gate had passed — then fixed, re-validated live, merged, CI-green.

### The five fixes (commit `104a284d0`)

| # | Bug (Telegram only; WebUI was clean) | Root cause | Fix | Live-validated |
|---|---|---|---|---|
| A | accept → agent re-schedules in a loop | resume-hook activation is a silent side-effect; the resumed model was never told the task went active | `answerTurn` injects an explicit "task ACTIVE/CANCELLED — do not re-schedule" RoleTool result (shared, both channels) | ✅ round 3 |
| B | `BUTTON_DATA_INVALID(400)` — sweep push never delivered | telebot frames callback_data `\f<Unique>\|<Data>`; 19-char `aura_sched_approval` + UUID + `\|approve` = 65 > 64 | shortened Unique → `aura_sappr` + on-wire length guard + a markup test that counts the framing | ✅ backstop |
| C | raw 36-char UUID in the prompt | — | masked to `kind + first-8` (`shortScheduledTaskID`); full id stays in `resume_context` | ✅ round 3 |
| D | duplicate prompt | sweep raced the in-turn relay, minting a 2nd pause between `task schedule` and the model's `ask_user` | sweep is now a true backstop: **grace delay** (`AURA_SCHEDULER_APPROVAL_GRACE_SEC`, default 60s) + **deliver-only-when-minted** (`EnsureApprovalPause` returns a `minted` flag) | ✅ (1 pause) |
| E | backstop resolve read as "nothing happened" | `resolveScheduled` drives no continuation turn → only a toast | `onScheduledApprovalCallback` rewrites the prompt to a visible outcome (`✅ approvato` / `❌ rifiutato`) | unit-tested |

Fixes A, D are in shared code (`runner`, `cron`) — survive the consolidation. B, C, E patch the Telegram-native path the next phase deletes.

### Verification
- WSL `go build ./...` + `go vet` + `-race` on all 5 touched packages: green.
- Live E2E round 3: schedule from Telegram → **one** masked prompt → tap Sì → task `active`, agent confirms, **no loop**, **no duplicate**, **no** `BUTTON_DATA_INVALID`. Backstop forced (deleted the model pause) → sweep re-minted + delivered its own button cleanly → resolved to `active`.
- WebUI confirmed clean by the operator (old container).
- Pre-push gates: quality-snapshot ✓ (3 rows, base 2026-07-23), build ✓, deadcode ✓.
- CI: CI ✓ / Skills (strict `db_integration` coverage) ✓ / CodeQL ✓.

### Also landed
- `AURA_SCHEDULER_APPROVAL_{REMINDER,GRACE}_SEC` plumbed into `compose.yaml` (were unreachable in the container).
- Design doc updated: `docs/superpowers/specs/2026-07-23-scheduled-task-approval-on-channel-design.md` → "Post-E2E hardening" section.
- Next-phase spec: `docs/superpowers/specs/2026-07-24-channel-consolidation-next-phase.md`.
- 5 docs-only spike commits (097-101, adaptive intelligence control plane) from a parallel session rode along in the merge — reviewed as docs-only, no code.

## Root-cause insight (operator directive)

The bugs were Telegram-only because Telegram carries a **duplicate** approval implementation (`internal/channels/telegram/hitl.go` + `scheduled_approval.go`) divergent from the cockpit's `web/src/approvals/InlineApprovalCard.tsx` + `/api/approvals`. The point-fixes make the native path correct enough to ship; **the next phase consolidates** — deleting the duplicate.

## Environment note
The running `aura` container was recreated with `AURA_SCHEDULER_APPROVAL_GRACE_SEC=30` (a **test** override, for a fast backstop demo). Production default is **60** (compose). A plain `docker compose up -d aura` restores 60.

## Follow-ups (memories written)
- `project_telegram_miniapp_richer_askuser` — Phase A: private channel consolidation (Mini App REJECTED — no cloud/public endpoint; shared orchestration + richer native Telegram).
- `project_cockpit_scheduler_board_detail_edit_gap` — Phase B: scheduler board can't show/modify a task.

---

## NEXT-SESSION PROMPT (paste to start)

```
Continue the Aura channel-consolidation work. 1.7 is SHIPPED (master @ 58060fe5e,
CI green) — the on-channel scheduled-task approval with a deterministic sweep
backstop. Read docs/superpowers/2026-07-24-1.7-session-handoff.md for context.

## Phase A — private native consolidation (priority)
The REAL fix for the 5 Telegram-only 1.7 bugs: Telegram has its own duplicate
approval implementation, divergent from the cockpit's. Full brainstorm → spec →
plan → execute. Read:
  docs/superpowers/specs/2026-07-24-channel-consolidation-next-phase.md
CONSTRAINT (operator, 2026-07-24): no cloud, no public endpoint — simple and
private. This RULES OUT the Telegram Mini App (a web_app button needs a reachable
HTTPS endpoint the client loads). Do it in-process, no new infra:
  1. Consolidate the shared resolve/continuation orchestration so BOTH channels
     (Telegram callback + WebUI /api/approvals) drive ONE code path in
     runner.SubmitAnswer(s)/answerTurn — structurally kills the loop/duplicate/
     divergence bug class. Collapse aura_hitl + aura_sappr to one path; channel =
     render + forward-action only.
  2. Enrich the NATIVE Telegram approval (no Mini App): formatted message (kind +
     bounded goal summary + schedule + risk, secret-safe) + multi-row keyboard
     (Approva/Rifiuta/Dettagli) + ForceReply for free-text/choice ask_user kinds.
     Keep the 1.7 guards (callback on-wire length, id mask, outcome edit).

## Phase B (independent) — scheduler board detail + edit
Cockpit scheduler board can't show a task's contents or modify it. Add a detail
panel (render aura.scheduler_tasks payload/goal/schedule/next_run, secret-safe) +
an owner-scoped `aura task update` tool + cockpit edit form (re-gate to
pending_approval if the edit raises risk). See memory
project_cockpit_scheduler_board_detail_edit_gap.

## State to know
- Running container was recreated with AURA_SCHEDULER_APPROVAL_GRACE_SEC=30 (TEST
  override); production default is 60. `docker compose up -d aura` restores 60.
- Delete the merged branch fix/1.7-scheduled-approval-on-channel when convenient.
- Relevant memories: project_telegram_miniapp_richer_askuser (= consolidation),
  project_cockpit_scheduler_board_detail_edit_gap.
```
