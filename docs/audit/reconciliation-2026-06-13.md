# Reconciliation — verified true status (2026-06-13)

The 2026-06-12 audit set (`risk-register.md`, `audit-index.json`, `re-audit-2026-06-12.md`) was written in the **morning**, listing all 40 findings as OPEN. Later that day commit **`ec7fe2f6 "fix(audit): close P1 items"`** (32 files, +803) and the 08.2 phase landed. Those audit docs never reflected the fixes, so this note **supersedes their Status columns** with a re-verification of every finding against the current `tabula-rasa` working tree.

## Method

Four read-only verification passes (reliability/loop · memory/context · ops/observability · security/tools/test) re-read the code for each finding against its **acceptance criterion** (not its presence), with `file:line` evidence and the asserting test where one exists. The headline durability fix **B-01** was additionally read down to its recovery-marker test. Every prior "CLOSED" was treated as a claim to falsify — consistent with the audit's own over-credit warning.

## Score

**6.5 → ~7.5.** All P1 go-live blockers are genuinely closed and tested (the audit predicted "closing the P1 set lands 7.5–8"). The score is held below 8 by the **22 OPEN + 6 PARTIAL P2/P3** items (no idle watchdog, per-turn breaker, microcompact answer-loss, no `/readyz`, no Windows CI, no fuzz/bench/mutation). Core loop ≈8.5; perimeter now ≈6.5.

## Tally (40 findings)

| Status | Count | IDs |
|---|---|---|
| **CLOSED** | 10 | B-01, B-02, B-03, B-04, O-01, O-02, D-01 (the P1 gate) · M-05, B-11, T-02 |
| **PARTIAL** | 6 | B-07, B-13, M-06, O-04, B-15, T-03 |
| **OPEN** | 22 | B-05, B-06, B-08, B-12, M-01, M-02, M-03, M-04, M-07, M-08, R-41, O-03, O-05, O-06, O-07, O-08, B-09, B-10, B-14, B-16, T-01, T-04 |
| **TRACKED** | 2 | R-26, R-40 |

## P1 gate — CLOSED & tested (`ec7fe2f6`, post-audit)

| ID | Verified evidence | Acceptance test (PASS) |
|---|---|---|
| B-01 | `conversations/store_helpers.go:302` repairs a dangling mutating call with a synthetic "previous result unknown… verify before re-running" marker instead of dropping it; write-ahead ordering enforced | `store_unit_test.go:154-188` (preserved+marked) · `runner_test.go:268` (`TestTurnPersistsAssistantToolCallsBeforeMutatingToolExecutes`) · `TestResume_NoSilentReRun_SC4` |
| B-02 | `swarm/runner_adapter.go:62` sets `Provenance{Source:"swarm",Trust:Untrusted}`; honored at `agent/trust.go:34` | `runner_adapter_test.go:61-65` |
| B-03 | per-thread in-flight guard in Runner, shared by AG-UI | `agui/server_p1_test.go` `TestServer_RunBusyThread409` |
| B-04 | `skill` tool schema states the true auto-activate policy; audit/alert fires on the ungated path | `TestSkillToolSchemaStatesActualAutoActivationPolicy`, `TestModelMutationBypassesGateExceptAlwaysOn`, `TestAuditActionFor` |
| O-01 | shared `obs.Init` (`internal/obs/init.go:47-79`) boots the tracer + JSON `slog` (service/version + secret redaction), called by `serve.go` and `chat_repl.go` | `TestInitInstallsJSONLoggerWithServiceAttrsAndTracerShutdown` |
| O-02 | `internal/agent/metrics.go` prom counters/histograms; `agui/server.go` mounts `GET /metrics` | `TestServerMetricsExposesPrometheus` |
| D-01 | non-root `Dockerfile` + hardened `aura` compose service (`read_only`, `cap_drop`, `mem_limit`, healthcheck) | `cmd/aura/container_artifacts_test.go` `TestProductionContainerArtifactsAreHardened` |

> The audit-index counted "8 P1"; the register carries **7** distinct P1 rows (B-04 folds the R-09 schema+alert pair). All 7 are closed.

## Other CLOSED / PARTIAL (P2/P3)

| ID | Status | Verified note |
|---|---|---|
| M-05 | CLOSED | `context.go` `dropOldestRound` preserves the tail; undroppable oversized → `ErrContextWindowExceeded` (`context_unit_test.go`). Original design, predates the audit. |
| B-11 | CLOSED | `shell_exec.go` = **599** LOC — at the wire, satisfies "no file >600". No real split; trivially re-breaches on next touch. |
| T-02 | CLOSED | `TestSendFileCaptionASCIISanitized` covers `foldToASCII`. |
| O-04 | PARTIAL | Secret-redacting `slog` handler done (`obs/init.go:81`); **no `Config.Validate()` fail-fast** — empty `NEO4J_PASSWORD` still boots. |
| M-06 | PARTIAL | Boot-time `tmp/*` 24h sweep exists; **no periodic sweep, no reasoningtrace rotation/cap**. |
| B-07 | PARTIAL | Escalate-on-exhaustion terminates a budget-owning branch; **no test for `maxIter=0` + budget-owning** termination. |
| B-13 | PARTIAL | Typed `errors.As` checks present **plus a retained substring fallback**; no `io.ErrUnexpectedEOF`/`ECONNRESET` sentinels. |
| B-15 | PARTIAL | Bridged MCP tool desc/summary framed; **arg-schema field descriptions still unframed/uncapped**. |
| T-03 | PARTIAL | Each deferred tool's `Spec()` tested individually; **no single golden sweep** over all deferred specs. |

## Genuinely OPEN — the real remaining roadmap (22)

- **Reliability:** B-05 (per-turn breaker, `llm_agent.go:131`), B-06 (breaker-open → error slot not finalize), B-08 (no stream idle watchdog), B-12 (live partial-chunk replay, cosmetic), M-08 (`EnsureConversation` masks 23505).
- **Memory/context:** M-01 (microcompact destroys `ask_user` answers — `context.go` `applyL1` has no sidecar guard), M-02 (`SubmitAnswer` inject + mark are **separate** txns — `runner_resume.go:77`), M-03 (`hardCap<=0` returns raw history; the bug is **test-locked** in `context_boundary_test.go`), M-04 (sidecar spill outside the tx), M-07 (`anyInt` lacks `json.Number` in **both** copies — `runner_persist.go` + `chat_render.go`), R-41 (per-session tool state never evicted).
- **Ops/obs:** O-03 (otel no-op error handler), O-05 (no `/readyz`, `/healthz` PG-only), O-06 (SIGTERM hard-cancels turns), O-07 (no Windows CI lane), O-08 (spans `llm.request`-only).
- **Security:** B-09 (divergent `secretEnvKey` — `PRIVATE_KEY` redacts in MCP, not shell), B-10 (destructive gate off by default, no conservative defaults), B-14 (`Registry.Register` silent overwrite), B-16 (`fs_grep`/`fs_glob` no node/deadline cap).
- **Test apex:** T-01 (no fuzz, no agent-core mutation score, bench targets the wrong path), T-04 (`agenttest` still in the coverage denominator).

## Sequencing for the close-out

Reliability + memory correctness first (B-05/06, B-08, M-01, M-02, M-03, M-04), then ops/security (O-03/04/05/06/07/08, B-09/10/14/16), then test apex (T-01/03/04) and cleanups (M-07, B-12, M-08). Two need a design decision before coding: **M-03** (what `hardCap≤0` should do for small-window models) and **B-11** (whether to actually split the 599-LOC file or leave it at the wire).

## Closures landed 2026-06-13 (the PARTIAL set, TDD-first, one atomic commit each)

| ID | Commit | What landed |
|---|---|---|
| B-13 | `b5767c2a` | typed network sentinels (`io.ErrUnexpectedEOF`/`io.EOF`/`syscall.ECONNRESET`/`ECONNREFUSED`/`ETIMEDOUT`) as the primary retry classifier; substring table demoted to last-resort fallback |
| B-15 | `66e014a5` | recursive 512-byte cap on every MCP arg-schema `description`; `bridge_spec_test.go` split to hold the 600-LOC line |
| O-04 | `a6373124` | `Config.Validate()` fail-fast on empty DB DSN / `NEO4J_PASSWORD`, wired into the shared chat/serve boot before any connection opens |
| B-07 | `99ccf919` | no-progress guard terminates a `maxIter=0` loop whose budget-owning sub neither spends nor escalates; `terminalEventKind` carries the reason |
| M-06 | `a822b1b5` | reasoningtrace rotates to a `.1` backup at a byte cap (`AURA_REASONING_TRACE_MAX_BYTES`, default 8 MiB) — **part 1 only** |
| T-03 | `298d04ea` | golden well-formed-spec sweep over the 18 built-in tools (unique names, present summary/description, JSON-object params, deferred-richness) |

**Result: 5 PARTIAL → CLOSED.** M-06 advanced to rotation-done; its **periodic sidecar TTL sweep + archived-sidecar reclaim** half remains a background-worker + reclaim-policy feature deferred to its own pass. Updated tally: **15 CLOSED / 1 PARTIAL (M-06) / 22 OPEN / 2 TRACKED.**

## Reliability cluster — OPEN → CLOSED (2026-06-13, TDD-first, one atomic commit each)

| ID | Commit | What landed |
|---|---|---|
| B-05 | `52c0565b` | breaker hoisted to a **process-lifetime Runner singleton** (`Deps.Breaker` → `Runner.breaker`, defaulted via `llm.NewDefaultBreaker`) and injected into every per-turn agent (`LlmAgentConfig.Breaker`) so a provider outage trips cross-turn protection — the per-turn breaker reset on each rebuild and never opened. Refactor-on-touch: `consume` split to `llm_agent_consume.go`. Tests: `TestInjectedBreakerIsSharedNotPerAgent` (agent mechanism), `TestRunnerInjectsSharedBreakerIntoEveryTurn` (Runner wiring), `TestNewDefaultBreakerUsesPolicyDefaults` (policy single-source). |
| B-06 | `c9515390` | breaker-open now routes to `finalize()` (a non-empty terminal Event via the deterministic stub digest) instead of the iter.Seq2 **error slot** — graceful degradation, not an infra failure. `finalize`'s own synthesis short-circuits the same open breaker and falls through to the stub, so the terminal is always non-empty. Test: `TestBreakerOpenRoutesToFinalize`. |
| B-08 | _(this commit)_ | per-read **stream idle watchdog** in the openai_compat client (`AURA_LLM_STREAM_IDLE_TIMEOUT_SEC`, default 60s, 0 disables). Resets on ANY bytes from the wire — data OR `: OPENROUTER PROCESSING` keep-alives — so a long reasoning phase never trips it; only a dead connection does. On a stall it cancels the read and emits a **retryable** `ErrStreamIdleTimeout`, which the agent's stream classifier retries once before surfacing. Tests: `TestStream_IdleTimeoutAbortsStall`, `TestStream_IdleTimeoutDisabledWhenZero`, `TestRetryableStreamOpenError_IdleTimeout` + config default/override. |

Running tally after this section: **18 CLOSED / 1 PARTIAL (M-06) / 19 OPEN / 2 TRACKED.**

## Memory-correctness cluster — OPEN → CLOSED (2026-06-13, TDD-first, one atomic commit each)

| ID | Commit | What landed |
|---|---|---|
| M-01 | `b4f09719` | L1 microcompact (`applyL1`) now rewrites a `RoleTool` turn to a `read_tool_output` pointer **only when it is sidecar-backed** (`isSidecarBacked`: a `[output truncated:` footer or a `ContentSidecarPath`). A non-spilled turn — an `ask_user` answer or a small inline result — has nowhere to page back from, so the old unconditional rewrite created a dead pointer that silently destroyed the content after ~evictAfter rounds (R-28). Test: `TestApplyL1_PreservesNonSidecarToolAnswers`; 5 pre-existing fixtures that evicted unrealistic large-non-footer turns were made sidecar-backed (production always spills large outputs with a footer). |
| M-02 | _(this commit)_ | **Gate-first reorder** in `SubmitAnswer`: `MarkResumed` (whose `RowsAffected==0` gate returns `ErrPauseNotFound`) runs BEFORE `injectAnswer`, so a duplicate/already-resumed token is rejected before a second answer turn is appended (the old order injected first → two tool results for one tool_call → wire-invalid round, R-27). Meets the AP-9 acceptance (retry → exactly one answer turn + `ErrPauseNotFound`). Per user decision the full single-tx was **downgraded to the reorder** (the cross-store tx would force re-wiring ~94 runner-fake touchpoints); the residual `MarkResumed`-then-`injectAnswer`-fail window is documented and deferred. Test: `TestSubmitAnswer_DuplicateResumeInjectsExactlyOneAnswer`. |
| M-03 | _(this commit)_ | **Nanobot-style small-window `hardCap` floor.** `ContextConfig.hardCap()` kept the SPEC Req#10 formula (`ContextWindow − max(MaxOutputTokens,20000) − 13000`) for normal/large windows but, when the formula is **non-positive** (a window below the ~33k fixed reservation — e.g. a Slice-13 local vLLM), now clamps to a positive `smallWindowHardCapFloor(ContextWindow)` = `ContextWindow/2` instead of `0`. Previously a `hardCap==0` made `applyContextLadder` return **raw history with L2/L2.5 protection entirely off** (R-29); the floor keeps L2.5 truncation active. Only a degenerate `ContextWindow<=0` still yields `0` (the sole remaining `hardCap==0` path; boot fail-fast is the seam to reject it). The change is a Req#10 semantics shift → PRD-amendment committed first (PRD-first). The pre-fix behavior was test-locked in `context_boundary_test.go` part (b) + `context_unit_test.go` `TestHardCap`, both repointed with justification to the window≤0 path. Tests: `TestLadder_SmallWindowFloor_ProtectsNotRaw`, `TestSmallWindowHardCapFloor` (new `context_smallwindow_test.go`). |
| M-07 | _(this commit)_ | **`json.Number` case added to BOTH `anyInt` copies** (`runner_persist.go` + `cmd/aura/chat_render.go`). The two helpers read token counts off the `StateDelta`, which is decoded with `UseNumber` (and a jsonb round-trip widens the same way), so a count arriving as a `json.Number` hit the `default` branch and returned `0` — silently zeroing the cost/usage row (R-42, dormant). Each copy now parses via `.Int64()` and falls through to `0` only on parse error, matching every existing case (the runner `anyFloat` already carried the parallel `json.Number` case under IN-03). The two copies stay separate per the finding (distinct packages — `runner` vs `cmd`); no de-dup. TDD-first: `TestAnyInt_JSONNumber` (runner) + a `json_number_tokens` subtest in `TestUsageFromStateDelta` (cmd) both went RED (returned 0) before the case, GREEN after. |
| R-41 | _(this commit)_ | **`SessionEvictor` evict-on-Stop closes the daemon leak.** A new `tools.SessionEvictor` interface (`Evict(sessionID)`, idempotent + self-locked) is implemented by the three genuinely session-keyed holders — `TodoTool.byID` (`todo.go`), `ShellExec.cwd` (extracted to `shell_exec_session.go` to keep `shell_exec.go` ≤600 after the touch), and `ShellApprovals.{approved,pending}` (`shell_approval.go`, composite-key prefix sweep). `Runner.Stop` now ranges the tool registry (`registry.All()`) and calls `Evict(convID)` (sessionID==conversationID per D-26) after the auto-resolve, so a long-running `serve` daemon reclaims a finished conversation's tool state instead of accumulating it forever (R-41). `shell_bg`'s `BackgroundShells` is keyed by generated `sh_N` ids — NOT sessionID — and already self-prunes + has `Shutdown`, so it is intentionally untouched (an `Evict(sessionID)` there would be a meaningless no-op). TDD-first: `TestSessionEvictor_{TodoTool,ShellExecCwd,ShellApprovals}` (tools) + `TestStop_EvictsSessionToolState` (runner) all went RED (interface undefined / no Evict calls) before, GREEN after. |
| M-04 | _(this commit)_ | **Cleanup-on-rollback for the turn-append sidecar spill.** `AppendTurn` + `AppendAssistantTurnWithCacheMetric` both spilled oversized turn content to a sidecar file (`maybeSpill` → `writeTurnSidecar`) and then ran the `db.WithTx` INSERT+aggregates inside the SAME flow; if the tx rolled back after the spill (e.g. `insertTurnAndAggregates` failed, or a PK collision on `(conversation_id, seq)`), the DB row was gone but the file remained an **orphan** — reclaimed only at the next boot scan, so a long-running daemon kept it alive for the whole live session (AP-16). Both methods now wrap their `db.WithTx` in a new `cleanupSidecarOnTxError(run, spilledPath)` helper: on a tx error it best-effort `os.Remove`s the just-spilled sidecar (guarded on a non-empty path; the seq-allocated-inside-tx branch captures the path via a closure resolved only on the error path), so a rolled-back turn never leaves an orphan. The success path is untouched (the file is the committed turn's durable content) and the boot orphan scan (`orphan_scan.go`) remains the backstop; the remove error is intentionally ignored. TDD-first **unit-seam** test (no live DB): `appendTurnWrites` does the real spill, the injectable tx step forces a post-spill rollback, `os.Stat` asserts removal-on-error + keep-on-success — `TestCleanupSidecarOnTxError_RemovesSpilledFileOnRollback` (3 subtests: rollback-removes-orphan, success-keeps-file, no-spill-noop) went RED (orphan survived) before, GREEN after. |

Running tally after this section: **24 CLOSED / 1 PARTIAL (M-06) / 13 OPEN / 2 TRACKED.**

## Ops/observability cluster — OPEN → CLOSED (2026-06-13, TDD-first, one atomic commit each)

| ID | Commit | What landed |
|---|---|---|
| O-03 | _(this commit)_ | **Rate-limited slog handler for OTel errors.** The process-global `otel.SetErrorHandler(func(error){})` no-op (`tracing.go` otlp branch) silently swallowed every OTel SDK error — export failures after a collector goes down mid-run, batch-queue-full drops, malformed-span errors — so with a real collector deployed, dropped traces were invisible (R-31). It was also set only on the otlp path, so behavior differed by exporter mode. Replaced by a `rateLimitedOTelHandler` (`internal/obs/otel_error_handler.go`) installed in `obs.Init`'s shared boot path right after the JSON `slog` logger is set as default, so it now covers **every** exporter mode (otlp/stdout/none). Each error logs `WARN "otel error" error=…` at most once per window (`defaultOTelErrorWindow`, 60s); errors inside the window increment a `suppressed` counter that the next emission carries (`suppressed=N`), so a persistently-failing exporter can't flood the logs while real trace loss is surfaced. The clock is injectable (`now func() time.Time`, defaults `time.Now`) so the window is unit-testable without sleeps; a nil logger falls back to `slog.Default()`. The no-op was deleted from `tracing.go` (the `otel` import stays — `SetTracerProvider`/`Tracer` still use it). TDD-first: `TestRateLimitedOTelHandler_{FirstErrorLogged,BurstWithinWindowSuppressed,NextWindowReportsSuppressedCount,FirstLogOmitsSuppressedWhenZero,NilLoggerDefaultsToSlogDefault}` went RED (`undefined: newRateLimitedOTelHandler`) before, GREEN after — asserting the first error logs, a burst within the window is suppressed to one line, the next window reports the suppressed count, and the error detail is included. |
| O-05 | _(this commit)_ | **`/readyz` readiness endpoint over an injectable probe seam.** The serve daemon's health endpoint was Postgres-only liveness with no Neo4j/provider readiness and no `/readyz` (R-14 residual), so a load balancer or orchestrator could route traffic to an instance whose required graph backend was down. Added `GET /readyz` in `internal/agui` (`readiness.go`) over a `ReadinessProbe` seam (`{Name string, Check func(ctx) error}` list on `ServerConfig`): each probe runs under a bounded 3s ctx, `/readyz` answers 200 only when **all** required deps pass, else 503 with a terse JSON body (`{"ready":false,"deps":{…}}`) naming each failed dep — the error string is sanitized through `SanitizeString`, so a bolt DSN never leaks. `serve.go` wires two real probes via `serveReadinessProbes(chat)`: `postgres` (the open pgx pool's `Ping`) and `neo4j` (`knowledge.VerifyConnectivity` — a native-driver dial + `VerifyConnectivity` + `Close`, no long-lived handle, since the daemon holds no persistent graph client; the mcp-neo4j-cypher subprocess is opened only behind the reasoning-learner gate). `/healthz` stays the cheap PG-only **liveness** check and is deliberately NOT flipped by a down readiness dep — Open-Q #2: an MCP-free `aura chat` is not hard-coupled to graph availability, only the SERVE daemon's readiness reflects its required backends. A probe whose handle is absent (nil pool) is omitted, not falsely failed (AP-14). TDD-first: `TestServerReadyz/{all probes healthy->200,a failing probe->503 naming the dep,no probes configured->200,healthz stays 200 when a readiness dep is down}` went RED (`unknown field ReadinessProbes` / `undefined: ReadinessProbe`) before, GREEN after. |
| O-08 | _(this commit)_ | **`agent.turn` + `tool.execute` spans (AP-20).** OTel span coverage was `llm.request`-only — `startLLMSpan` emitted one span per LLM call but there was NO span for the overall agent turn nor for individual tool executions, so a trace could not show per-turn or per-tool latency. Added an `agent.turn` span wrapping the whole `Run` loop: it is started before the `iter.Seq2` closure (`startTurnSpan(ic.Ctx, requestID, sessionID)`), the turn ctx is threaded back via `ic.WithContext(turnCtx)`, and it ends on **every** return path through a single `defer endTurnSpan(turnSpan, turnReason)` over the closure — `turnReason` is set at each terminal path (`content_stop`/`tool_terminal`/`empty_response`/`max_steps`/`wallclock`/`dedup`/`breaker_open`/`ask_user_pause`/stream-error/consumer-stop) and stamped as `aura.turn_outcome` on End. Added a `tool.execute` span per dispatch in `runTool` (attrs `tool.name`, `tool.mutating`, `tool.success`, and `tool.error` on failure; unknown-tool dispatches also get one). Because the tool dispatch ctx and `callCtx` both derive from the threaded turn ctx, the existing `llm.request` span and the new `tool.execute` span now **nest under** `agent.turn`. The four span helpers (`startTurnSpan`/`endTurnSpan`/`startToolSpan`/`endToolSpan`) live in `tracing.go`; span creation stays cheap + non-fatal (a no-op tracer mints a dropped span, never panics). Concurrent (`executeBatch`) dispatches each start their own `tool.execute` span off the shared turn ctx — otel spans are safe to start in parallel. `llm_agent.go` 537→567 LOC (no split needed). TDD-first: `TestSpan_TurnTreeParenting` (asserts `agent.turn` + `tool.execute[tool.name=echo]` + `llm.request` recorded, with `tool.execute`/`llm.request` parent SpanID == `agent.turn` SpanID) and `TestSpan_TurnEndsOnEveryPath` (exactly one ended `agent.turn` span on the content-stop terminal path) went RED before, GREEN after; the stale `TestSpan_PerCall` total-span==1 assertion was narrowed to count `llm.request` spans specifically (the per-call contract is preserved — the run now legitimately also records `agent.turn`). |
| O-06 | _(this commit)_ | **Bounded in-flight turn drain on SIGTERM (AP-17).** A SIGTERM hard-cancelled in-flight turns (asymmetric drain, R-34): the channel (Telegram) per-turn ctx in `startTurn` derived (`context.WithCancel(daemonCtx)`) from the daemon's `signal.NotifyContext`, so the instant a SIGTERM/SIGINT fired it cancelled the turn ctx and killed a turn **mid-stream** rather than letting it reach a terminal `agent.Event`/frame — while the HTTP/SSE path was already drained by `http.Server.Shutdown` (request ctx not signal-derived), hence the asymmetry. Fix: `runServe` now keeps the signal ctx as the stop-**trigger** only (it is what returns `scheduler.Start(signalCtx)`) and parents the actual per-turn work on a **separate** background-derived `workCtx` (`context.WithCancel(context.Background())`) passed to `bootServe` → `startChannelSubsystems` — the signal does NOT cancel it. On the signal the daemon stops accepting NEW work (`stopChannelSubsystems` stops the poller + `StopAll` joins each channel's in-flight-turn WaitGroup; `httpSrv.Shutdown` stops the listener) and JOINS the in-flight turns under a bounded grace window (`AURA_SERVE_SHUTDOWN_GRACE_SEC`, default 25s) via the small testable `drainWithGrace(drain, grace)` helper (`cmd/aura/serve_drain.go` — returns `drained`/`timed-out`, runs the drain in its own goroutine, never blocks past the grace on an overrunning turn); only AFTER the grace does `workCancel()` fire as the final hard-cancel backstop. A non-positive knob degrades to an immediate non-blocking poll (backstop never disabled). No change to `internal/runner` or `internal/agui` — the entire seam is at the `cmd/aura` composition root. TDD-first: `TestDrainWithGrace_{FinishesBeforeGrace->drained + the in-flight drain ran to completion not cut short, ExceedsGrace->timed-out + returned within ~grace not blocked on the straggler}` went RED (`undefined: drainWithGrace`/`drainResultDrained`/`drainResultTimedOut`) before, GREEN after. |

Running tally after this section: **28 CLOSED / 1 PARTIAL / 9 OPEN / 2 TRACKED.**

## Security cluster — OPEN → CLOSED (2026-06-13, TDD-first, one atomic commit each)

| ID | Commit | What landed |
|---|---|---|
| B-10 | _(this commit)_ | **Default-on advisory destructive-shell gate (AP-15).** The approval gate was OFF by default — an unset `AURA_SHELL_DESTRUCTIVE_PATTERNS` returned no patterns, so `rm -rf /` ran unattended (RED test executed it for 10.6s, only host OS perms saved the run). Now ON BY DEFAULT: an unset env var falls back to a conservative built-in `defaultDestructivePatterns` (`shell_exec_env.go`) — `rm -rf` on a root/top-level path, `mkfs.*`, `dd of=/dev/*`, the classic fork bomb, redirect-truncate into a raw block device — narrow on purpose so an ordinary `rm file.txt` / `go test` / `git status` is never gated. Documented as **advisory, not a security boundary** (regex over the raw command line is phrase-around-able; the real boundary is host/sandbox policy + the operator's own privileges) in a doc comment on the env const and on `defaultDestructivePatterns`. Stays overridable: an explicit value REPLACES the default (no merge), the empty string or `"off"` is the explicit opt-out. TDD-first: `TestDestructiveShellDefaultOnFlagsRmRf` went RED (`rm -rf /` actually executed) then GREEN (gated in 0.16s, never runs); `TestDestructiveShellDefaultPatternsCoverConservativeSet` (destructive set matches, benign does not) + `TestDestructiveShellDefaultIsOverridable` (`off` disables, custom replaces-not-merges). |
| B-09 | _(this commit)_ | **One shared `secret.IsSecretEnvKey` across all three sites (AP-13).** The secret-env-key denylist had **three divergent local copies** and they disagreed on the bare `*_KEY` case: shell child-env (`shell_exec_env.go` `secretEnvKey` — markers `token`/`secret`/`password`/`passwd`/`api_key`/`apikey`/`auth`/`bearer`/`credential`, **missing `key`**), MCP child-env (`mcp/client.go` `secretEnvKey` — same set **plus `key`**), and MCP managed-config export (`mcp/manager/config.go` `isSecretEnvKey` — `token`/`secret`/`pass`/`key`/`auth`/`bearer`/`credential`). So a bare `*_KEY` such as `PRIVATE_KEY` was redacted on the MCP child-env path but **leaked into the shell child env** (R-07 divergence). Unified onto one canonical predicate `secret.IsSecretEnvKey` in a new `internal/secret/envkey.go` — the safer **superset** of all three (markers `key`/`token`/`secret`/`pass`/`auth`/`bearer`/`credential` + audit-mandated `private`/`cert`; `key` subsumes `api_key`/`apikey`/`private_key`/`ssh_key`, `pass` subsumes `password`/`passwd`/`passphrase`), with a "never shrink this list" guard comment. All three call sites now call it and the three local copies are **deleted**; coverage is the union (nothing redacts less than before). TDD-first: a table test for `IsSecretEnvKey` (asserts the canonical secret set — incl. `PRIVATE_KEY`/`API_KEY`/`OPENROUTER_API_KEY`/`*_TOKEN`/`*_SECRET`/`*_PASSWORD` — matches AND clear non-secrets `PATH`/`HOME`/`LANG`/`AURA_LOOP_MAX_STEPS` are NOT over-redacted) plus `TestMergeEnvStripsPrivateKeyFromChild` (shell) went **RED** — `PRIVATE_KEY=…` was observed in the shell child env — before, GREEN after; `TestProcessEnvForMCPStripsPrivateKeyFromChild` (MCP) locks the cross-site equivalence (the acceptance: `PRIVATE_KEY` redacts identically in shell + MCP). |

| B-14 | _(this commit)_ | **Fail-loud on duplicate tool registration.** `Registry.Register` silently OVERWROTE a previously-registered tool sharing a name — a programming error swallowed, a name collision quietly shadowing a tool. Now it FAILS LOUD with a panic at registration time (the net/http duplicate-route idiom; `Register` keeps its no-error signature because no caller can recover from a static wiring collision — fix the wiring). The panic immediately surfaced a REAL latent double-registration the silent overwrite had been masking: `cmd/aura`'s `replayAudit` (`cache_audit.go`) called `buildRegistry()` — which already wires a default `skill` tool — then registered its own deterministic `skill`, silently replacing the default; fixed minimally with the existing `tools.Without(buildRegistry(), skillManifestName)` to drop the default before registering the audit's deterministic one (preserves intent). All other call sites use distinct names; the runtime never re-registers (registration is boot-time only — `ToolSearch` reads the registry, never mounts). TDD-first: `TestRegisterDuplicateNamePanics` went RED (silent overwrite, no panic) then GREEN (panics, naming the colliding tool); `TestRegisterDistinctNamesSucceeds` locks the normal path. |

| B-16 | _(this commit)_ | **Node + deadline cap on the `fs_grep`/`fs_glob` walks.** Both tools walked an arbitrary tree with NO node-count or time budget — the `maxResults` cap bounds only MATCHES, not nodes VISITED, so a `path:/` (or any huge tree) that matches nothing still touched every file and could wedge a turn (full-disk scan). Added a shared `walkBudget` seam in `fs.go` bounding the walk by a node-count cap (`AURA_FS_WALK_NODE_CAP`, default 50000) AND a deadline (`AURA_FS_WALK_TIMEOUT_MS`, default 5s), and it also honors an already-threaded ctx deadline/cancellation so a turn-level timeout aborts the walk too. Each tool calls `budget.step()` per visited entry and stops early via `filepath.SkipAll` on exhaustion, then appends `walkTruncatedMarker` (via `withWalkTruncation`) so the model treats the scan as partial, not exhaustive. Defaults are generous for ordinary project trees and operator-tunable for a deliberate large-tree grep; the existing `skipWalkDir` (.git/node_modules/vendor) and binary skip still apply. TDD-first: `TestFSGrep/GlobNodeCapTruncates` (over-cap, non-matching pattern → flagged), `TestFSGrep/GlobUnderCapNoTruncation` (under-cap → no flag, matches still returned), `TestFSGrepDeadlineCapTruncates` (1ms deadline trips), `TestWalkBudgetRespectsCtxDeadline` (cancelled ctx → exceeded) went RED (undefined symbols) then GREEN. |

Running tally after this section: **32 CLOSED / 1 PARTIAL / 5 OPEN / 2 TRACKED.**
