# Aura Quality Snapshot (living doc)

**Created:** 2026-05-29
**Current update:** 2026-07-31 (Phase 41 closeout: exact-candidate deterministic, race,
coverage, mutation, load/chaos, DR, rollback, and observability evidence is green. The paid
DeepSeek CoT and scheduler natural-prompt rows remain explicitly non-green and require operator
authorization; their dates were refreshed to record that honest status, not to claim a rerun.)
**Current graph renderer re-measurement:** 2026-08-03 — the identity-memory renderer now uses
Cytoscape + fCoSE as the sole graph stack. Full web: **92.76% statements / 87.26% branches /
92.65% functions / 94.59% lines**, 202 files and 1,747 tests. `src/graph`: **95.88% /
87.41% / 93.45% / 97.67%**; `ArcadeGraphCanvas_data.ts` mutation: **85.96%** (153/178
killed). Production chunks remain below 500 kB without raising the warning limit: Cytoscape core
435.41 kB and layout 122.56 kB.
**Current Agent Memory measurement:** 2026-08-03 — the production ArcadeDB MCP passed the
fail-closed `aura.agent-memory-eval/v1` gate at **100.00/100**. The combined live
`internal/arcadedb` profile is **943/1016 = 92.81%**; the operator-path CLI measured
**p50 70.820 ms / p95 79.763 ms / max 88.034 ms** over 25 sequential cold-retained
identity lookup + MCP initialize + search samples. A deliberately nonexistent claim returns
`facts: []` with `abstained: true` and `reason: no_qualified_candidates`. Runtime evidence is
ArcadeDB 26.7.3, MCP 0.1.0, and `embeddinggemma-300M-Q8_0.gguf` at 768 dimensions.
**Latest concern closure (2026-08-25):** Unanswered persisted approvals now expire through an
owner-scoped atomic refusal path after a configurable 48-hour default. Targeted WSL race, vet,
build, lint, sqlc, file-size, and dead-code gates are green. Disposable PostgreSQL proved due-row
selection, RLS isolation, atomic visibility, and an expiry-versus-human race with exactly one
winner and one `RoleTool` turn; migration 0102's up/down/up round trip remains green. The touched
ask-user/Runner database matrix measured **86.2%**, all changed critical expiry functions are at
least 85%, and `ExpirePendingApprovals` killed **14/14 mutants = 100%**. A fresh real
Runner/OpenRouter E2E advanced the durable conversation from 2 to 3 turns with **3752 prompt
tokens** and truthfully reported that the approval expired without execution: **10.0/10**. The
prior full-matrix owned-surface baseline remains **26827/31139 = 86.2%**; no repository-wide rerun
is claimed for this isolated concern.
**Production-document closure (2026-08-25):** Amendment #141 replaces the retired
PostgreSQL version/stage plane with the deployed Garage -> CocoIndex -> per-identity ArcadeDB
path. The WSL production gate passed all **10/10 hard assertions**: add, unchanged restart,
modify, delete, live refresh, exact 13-field Passage shape, real `.xls` extraction, two-identity
isolation, migration 0102 up/down/up, and a real OpenRouter agent turn that invoked
`document_search`. Targeted Go test/race/vet/build, the ingest integration tier, Compose parsing,
and the full-module dead-code graph are green. The earlier machine-card reference-corpus recall
number is not carried forward as evidence for this materially different retrieval path.
**Last updated:** 2026-08-25 (Approval-expiry closure: unanswered persisted approvals now expire through an owner-scoped atomic refusal path; targeted WSL race/vet/build/lint/deadcode and disposable-PostgreSQL selection/RLS/race gates are green; migration 0102 round-trip remains green; touched ask-user/Runner DB coverage is **86.2%**, changed critical expiry functions are >=85%, mutation is **14/14 = 100%**, and the fresh real-agent E2E scored **10.0/10**. The prior full-matrix baseline remains **26827/31139 = 86.2%**; no new repository-wide coverage run is claimed.) Prior same-day (Approval resume empty-answer closure: ONE concern closed with fail-closed validation at the AG-UI wire, Runner effective-answer, and transactional Store boundaries. The full disposable-PostgreSQL `db_integration` matrix measured **26827/31139 = 86.2%** owned coverage and `internal/agui` **4218/4928 = 85.6%**. Missing, empty, and whitespace-only accepted payload regressions are green at wire, Runner, Store, and rollback seams; `ValidateResumeAnswer` killed **4/5 mutants = 80%** (the reported survivor was byte-identical to the original); decline/cancel and scheduled server-authored outcomes remain valid. What this does NOT prove: per-pause decision policy and approval expiry remain open; no real-model turn, live SSE round-trip, or `translator.go` mutation campaign was run.) Prior 2026-08-24 (Per-identity MCP OAuth, end to end: TWO rows re-attested, both MEASURED rather than argued. A remote MCP connector can now be authorized by a person instead of by a token the operator pastes into config — migration 0100 stores one grant per (identity, server) with AES-256-GCM under an identity-derived key and both RLS layers, the SDK's authorization flow is wired to the transport, `aura mcp login` drives it from a terminal, and the cockpit drives it from a browser. Owned-surface aggregate **26820/31045 = 86.4%** on the WSL `db_integration` gate; `internal/agui` re-measured at **85.6%** (4200/4909), `internal/mcp` **93.3%**, `internal/mcp/manager` **96.0%**, new `internal/mcpoauth` **88.0%**. What the slice does NOT prove is stated in PRD amendment #129 and worth repeating here: no provider was contacted. It was measured against the CODE of three implementations — go-sdk v1.7.0, LibreChat and Hermes — read before anything was written, and that reading corrected four things a supposition would have got wrong (three artifacts must be persisted and not one; the SDK never spends a refresh token; the endpoint pin was decorative because the SDK builds its config from DISCOVERED endpoints; and no public URL is required, because the redirect is followed by the human's browser and never fetched by the authorization server). Four defects were found by tools rather than argued: two parked goroutines (an expired flow and shutdown), `omitempty` on a `time.Time` that would have rendered 0001-01-01 beside "Authorized", and `authStatus` reporting `unsupported` for exactly the servers that authorize by OAuth with no configuration. One flake banked, NOT fixed: `internal/runner`'s `TestVerifyOnStopFiresOnARealTurn` fails under the full parallel gate and passes 3/3 in isolation; nothing in `internal/runner` was touched.) Prior 2026-08-23 (Dead- and dark-code sweep: THREE rows re-attested. The range deletes the per-identity MCP/skills machinery that never had a production caller and moves every remaining test-only helper out of the production binary — `deadcode -test ./...` now reports nothing in Aura's own code. Every symbol removed had ZERO production callers, so no runtime path changed and the live legs cannot move; the one leg that CAN move, `internal/agui` coverage, was measured rather than argued at **85.9%** (up from 85.5%), with the owned-surface aggregate at **26094/30215 = 86.4%** on the WSL `db_integration` matrix. One correction banked: `ratioFromCapacity` is NOT dead — `disk_usage_windows.go` is its only caller and a Linux lint run cannot see it; trusting the tool over the build tags briefly broke the Windows build, and GOOS=linux/windows/darwin are now all verified. A Git-Bash run of the same gate reported four RED tests that are Windows artifacts, not regressions: POSIX file modes (`-rw-rw-rw-` vs `0600`) and `C:\...\Temp` paths in the verification classifier. WSL is the environment the gate is measured in.) Prior 2026-08-03 (Memory embedding backfill sweep: ONE row re-attested, metric-neutral. `memory_embed_backfill` is a SIXTH system-seeded sweep kind added alongside the existing five — a new `TaskKind`, a handler built from the already-shared `countingSweepHandler`, one entry in `isSilentSuccessKind`, one dispatch registration and one seed call — plus migration 0091 widening the `scheduler_tasks.kind` CHECK to admit it. The two `every`-cadence seed helpers were folded into one shared `seedEveryCronSweep` (byte-identical `ParseSchedule`/`NextRunAt`/`CreateTask` sequence, identical log lines) because a third copy is what `dupl` exists to catch. Nothing in schedule parsing, claim/lease, chaos-failover, once-per-window or the `task` tool changed, so the scheduler's measured numbers cannot move. Witnesses: `go build ./... && go vet ./...` and `go vet -tags db_integration ./...` clean; `go test ./internal/... ./cmd/...` green; golangci-lint 0 issues on `./internal/arcadedb/... ./internal/cron/... ./cmd/aura/... ./internal/db/...`; WSL `-race` green on `internal/arcadedb`, `internal/cron/...`, `internal/db`; live: migration applied on the running deployment and the sweep observed seeded and active at `every 5m` in `aura.scheduler_tasks`.) Prior 2026-08-03 (Pre-push inherited-range audit: ONE Web tools row re-attested. The sole production delta is a documentation-only example query in `web_search.Spec`; tests replace retired Neo4j fixture words with ArcadeDB. Live latency was freshly measured over **n=20** loopback JSON searches at **p50 0.354s / p95 0.993s / max 1.933s**, and the real `web_integration` race tier passed in 2.344s. SSRF policy, mutation target, coverage, request caps, ranking, and engine configuration are unchanged.) Prior 2026-08-03 (ArcadeDB memory-graph restoration: THREE rows matched. AG-UI's changed graph route is re-measured: full `db_integration` profile **3925/4588 = 85.5%**, while the identity-wide live browser view rendered **59 Entity nodes and 34 FACT edges** on desktop and mobile. Sandbox is metric-neutral because compose only rewires the independent ArcadeDB MCP health/runtime; Telegram is metric-neutral because `ci.yml` changes only the LOCOMO prerequisite comment. Full owned matrix is **23474/27152 = 86.4%**; `internal/arcadedb` is **94.0%** on its live matrix and **93.9%** unit; web is **92.65% statements, 87.14% branches, 92.55% functions, 94.55% lines**, with **202 files / 1755 tests**. The production image is Go-only and 7.7 MB; Vite's editor chunks are split below 500 kB.) Prior 2026-08-03 (a PDF's words reach the card: FOUR rows touched, ONE re-measured. **Document routing recall@1** moved — 18/22 = 81.8% → **20/22 = 90.9%**, MRR 0.841 → 0.932 — and a second, harder set of eight queries answerable only from INSIDE a PDF went **0/8 → 8/8**, because `internal/documents/filecard` now reads a PDF's text at ingest through poppler's `pdftotext`, which was already installed in the runtime image and therefore runs in the same container as ingest. Capped at the first 20 pages and 200 KB, both stated in the card when they bite; the whole-corpus card build goes 0.66-0.76 s → 1.40-1.43 s and the row says so. All five Normattiva decrees are routable by subject for the first time. The **Telegram**, **Skills** and **AG-UI** rows are metric-neutral: their globs match `.github/workflows/ci.yml` / `skills.yml`, where the only edit is an `apt-get install poppler-utils` step so the extractor exists on a hosted runner as it already does in `docker/aura/Dockerfile` — a binary installed before the same commands run on the same code. The sentence-query finding on the document row is UNCHANGED at 0/6: this work did not touch the query path. No number in this file was rewritten and every prior note is preserved verbatim. Witnesses: `go build ./... && go vet ./... && go test ./internal/... ./cmd/...` green; golangci-lint 0 issues on `./internal/documents/...`; `-race` green on `./internal/documents/...`; `internal/documents/filecard` untagged coverage **88.8%**; no file over 600 LOC.) Prior 2026-08-02 (adaptive-learning plane + eval oracle removal: FIVE rows re-attested and ONE RETIRED. `internal/adaptive` (40,225 LOC), the `cmd/aura` adaptive-benchmark machine and `internal/eval` (10,809 LOC) are deleted; the per-turn adaptive REASONING tier — classifier, policy, router, `internal/reasoningtrace`, `AURA_LLM_ADAPTIVE_REASONING` — is untouched and re-verified live at 10/10 against the real embedder. The Live CoT/tool-use eval row is RETIRED with an empty gate path: `TestCoTEval` no longer exists, so its metric is undefinable rather than merely stale, and the paid re-measurement it owed is void. Three surviving rows (Skills, Swarm E2E, Snippet-reuse) lost an `internal/eval/**` glob from their CI-gate paths and say plainly that the live paid E2E leg behind them can no longer be reproduced — the tiers those rows also cover are unaffected. The KV-cache and Scheduler rows are metric-neutral: the deleted `ReasoningControl` port and the `json:"-"` `DynamicTailID` field could not move a measured byte. No number in this file was rewritten and every prior note is preserved verbatim — a signed measurement is a record, not a draft. Witnesses: `go build ./... && go vet ./...` clean; `docker compose -f compose.yaml config` parses.) Prior 2026-08-02 (graph-store removal, documentation + infrastructure sweep: ONE row re-attested metric-neutral — the sandbox baseline, whose `compose.yaml` glob matches a comment-only edit — and TWO rows RETIRED rather than re-attested. The GraphRAG recall@5 and 100K vector-p95 rows measured a document-retrieval capability that no longer exists, and two of their three CI-gate globs pointed at deleted trees, so they were unreachable by the gate as well as unmeasurable; their prior notes are kept verbatim. No number in this file was rewritten — a signed measurement is a record, not a draft. Witness: `docker compose -f compose.yaml config` parses.) Prior 2026-07-31 (tool_search rewrite: re-attested all FOUR rows whose CI-gate globs match a changed path. ONE is re-measured — the skills unit/race tiers and coverage, on fresh runs; its live North-Star E2E leg was NOT re-run and says so. THREE are metric-neutral justifications naming exactly what changed: two are test-fixture-only touches of `internal/agent/mcptools/**`, one is the `rankSkills` field projection under `internal/agent/tools/skill*.go`. The GraphRAG recall@5 and snippet-reuse rows carry their INVALIDATED status forward UNCHANGED — this range does not clear them. Witnesses: `go build ./... && go vet ./...` clean; golangci-lint 0 issues on every touched package; `-race` green on `internal/agent/tools`, `internal/adaptive/...`, `internal/runner`, `internal/skills/...`, `internal/skilladapters`; tool_search retrieval gate 96% top-1 / 100% recall@3 over the live 54-tool corpus at 17us/query.) Prior 2026-07-30 (pre-push ownership re-attestation: Telegram, Web, and Swarm are green on fresh unit/race evidence; Web also passed its live tier. The repaired deferred-tool cache replay is green at 23/23 system hashes, 23/23 skills-block hashes, and 0/23 skill manifests by contract). Prior 2026-07-28 (memory-surface redesign + the skills-board range that preceded it: re-attested all EIGHT rows whose CI-gate globs match a changed path. Four are honest re-attestations or re-measurements - sandbox baseline metric-neutral, MCP manager tier re-run green, AG-UI coverage leg re-measured, skills unit tiers re-measured. FOUR ARE NOT GREEN AND SAY SO: the embedding model was swapped, which unbacks GraphRAG recall@5 and the @100K vector p95 outright, while the deferred-grant decay and the skills-stage deletion leave the live CoT eval and the snippet-reuse E2E unverified against this range. Those four carry INVALIDATED / RE-MEASUREMENT DUE and must be re-run before they are quoted again. Witnesses: `go build ./... && go vet ./...` clean; golangci-lint 0 issues on every touched package; untagged tiers green on `internal/agui` 80.7%, `internal/skills` 84.4%, `internal/agent/mcptools` 93.3%, `internal/knowledge` 79.7%; live embed sidecar p50 157.3ms / p95 238.5ms over 40 requests.) Prior 2026-07-27 (approval-gate narrowing + live-appliance boot fixes: re-attested the one row whose CI-gate glob matches a changed path — MCP manager mock E2E, metric-neutral, see the row for the UV_OFFLINE and bootCLIChat rationale plus its live witness. The gateway change itself — only Destructive stops the turn, swarm_spawn drops to Normal — lives in `internal/gateway/**`, which no snapshot row gates. Witnesses: `go build ./... && go vet ./...` clean; WSL `-race` green on `internal/gateway`, `internal/agent`, `internal/runner`, `internal/cron/...`, `internal/mcp/...`, `cmd/aura`; golangci-lint 0 issues on every touched package.) Prior 2026-07-26 (Amendment #95 interview-removal review-fix pass: re-attested the one row whose CI-gate glob matches a changed path — Scheduler North-Star, via a COMMENT-ONLY edit to `cmd/aura/serve.go` (metric-neutral, zero executable statements). The Amendment-#95 surfaces themselves — `internal/agui/**`, `internal/channels/telegram/**` — were last measured 2026-07-25 and are not stale against this base. Witnesses: `go build ./... && go vet ./...` clean; WSL `-race` green on `internal/agui`, `internal/onboarding`, `internal/channels/telegram`, `cmd/aura`; web vitest 198 files / 1713 tests green; `internal/webui/dist` rebuilt from `web/`.) Prior 2026-07-25 (memory-plumbing session: re-attested the two rows whose CI-gate globs match the changed paths — GraphRAG recall@5 (metric-neutral: agent-memory read surface, zero retrieval/embedding/HNSW change) and MCP manager mock E2E (RE-MEASURED: the Streamable-HTTP session-recovery branch is in that tier, so it was re-run, not argued). Fork suite 67 passed incl. 2 live-Neo4j tests; Go `-race` green on `internal/mcp` + `internal/agent/mcptools`; golangci-lint 0 issues module-wide.) Prior 2026-07-24 (Amendment #93.2 adaptive shadow-control replacement: re-attested sandbox, KV cache, GraphRAG recall, vector p95, live CoT, scheduler, and AG-UI rows as metric-neutral; full WSL lint/vet/build, affected unit/race, live PostgreSQL/Neo4j migrations, and isolated graph-preservation integration are green.) Prior 2026-07-23 (fix-plan Wave 1 session re-baseline for amendment #20: re-attested the seven quality rows the CI gate flagged stale — KV cache hit rate, GraphRAG retrieval recall@5, Telegram channel, MCP manager mock E2E, Live CoT/tool-use eval, Scheduler North-Star live E2E, and AG-UI gateway — after six commits `b0edba5d..14892823` touching `internal/llm/**` (llama.cpp-only usage streaming + opt-in OpenRouter middle-out transform), `internal/agent/mcptools/**` (HTTP reconnect wrapper + ping poll), `internal/cron/scheduler.go` (readiness staleness window), and `cmd/aura/serve.go`/`internal/agui/**` (AG-UI assembly split + SSE idle heartbeat); every match is metric-neutral, evidenced by targeted `go test` + WSL `-race` green plus live E2E witnesses — llamacpp usage flow, MCP ping failure-injection, scheduler readiness healthcheck, and a live SSE round-trip with heartbeats.) Prior 2026-07-22 (Phase 39 final production close-out, Amendment #88: re-attested all seven quality rows matched by the phase. Fresh final evidence includes a healthy/ready rebuilt production image with exact MCP runtime pins, real-agent Playwright **10/10**, paid live CoT **12 scenarios / all release-blocking and asserted dimensions green**, Linux race on swarm/tools, full observability smoke, and a fully uncached disposable Postgres+Neo4j coverage matrix at **85.4%**. The Swarm row is explicitly metric-neutral because only `swarm_spawn.Spec()` idempotency metadata changed; self-mail/self-WhatsApp recipients were not configured and no skipped run is presented as live evidence.) Prior same-day (Phase 39 observability durability close-out, Amendment #88: re-attested the three rows whose CI-gate globs match the composition-root change — **Telegram channel**, **Scheduler North-Star**, and **AG-UI gateway**. The shared `cmd/aura/serve.go` change only passes the already-open PostgreSQL pool into observability startup and owns the retention-backlog observer lifecycle; Telegram rendering/routing, scheduler dispatch/once-per-window/chaos, and AG-UI SSE/snapshot/reasoning behavior are unchanged. Fresh witnesses: full untagged Go test/build/vet, Linux race for the daemon and scheduler surfaces, the full disposable Postgres/Neo4j matrix at **85.4%**, and the complete observability runtime smoke.) Prior 2026-07-21 (Agent.md -> agent-memory supersession, Amendment #87: re-attested the six rows whose CI-gate globs match this phase — **Vector search p95** (`cmd/aura/memory_onboarding.go`, the new onboarding memory adapter — metric-neutral, future-benchmark stands), **Telegram channel** (onboarding storage swapped Agent.md->memory MCP; mdv2/renderer/send/routing untouched; `internal/channels/telegram` **86.1%**), **Skills system** + **Snippet-reuse** (`internal/skills/identity_root.go` now delegates the D-20/D-21 path guard to the relocated `internal/idroot`; validator/writer/snippet/xlsx untouched; `internal/skills` **88.1%**, `internal/idroot` 85.7%), **MCP manager** (same idroot relocation in `internal/mcp/managed_config_identity.go`; `internal/mcp` 91.6%), **AG-UI gateway** (web onboarding->memory MCP; SSE/agent-run/REASONING/translator untouched; `internal/agui` **86.1%**). Full `db_integration neo4j_integration` owned-surface matrix green at **85.5% aggregate** (>=85% floor) via `scripts/coverage_docker.sh` (disposable `aura_cov` Postgres + disposable `aura-neo4j-cov`). Prior 2026-07-20 (compaction-engine removal, Amendment #86: re-attested the four rows whose CI-gate globs match this phase — **KV cache hit rate** + **Live CoT eval** (`internal/llm/**` dark preflight deleted, default replay path byte-identical — metric-neutral), **Telegram channel** (`/compact` command + `cmd/aura/serve.go` wiring + `ci.yml` jobs removed, mdv2/renderer/send untouched — metric-neutral), **Scheduler North-Star** (`cmd/aura/serve.go` evaluator goroutine + `ci.yml` jobs removed, cron surface untouched — metric-neutral); the removal is the exact inverse of the 2026-07-14 Phase-42 additions each row already documented). Prior 2026-07-18 (Phase 37F-18 full-matrix verification close-out: re-attested the two rows whose CI-gate globs match this plan's changed paths — **Telegram channel** (`cmd/aura/serve.go`: share-service composition-root wiring only, zero telegram/channels/setup surface — metric-neutral) and **Scheduler North-Star** (`internal/cron/**` + `cmd/aura/serve.go`: a new, independent share_expiry_sweep TaskKind + its dispatch/seed wiring, zero reminder/agent_job/backup/once-per-window/chaos surface — metric-neutral). Also fixed a deadcode-surfaced composition-root gap (share.Service was never wired into the deployed binary — see 37F-18-SUMMARY.md) and two mutation-gate scoping gaps (Stryker's mutate/test lists never grew to cover web/src/chat/share/*). Full local matrix: Go owned-surface coverage 85.7% aggregate (5/6 owned packages >=85%, internal/objectstore's pre-existing 69.6% unchanged); SC3 core mutation 92.6% killed (25/27); web vitest 92.99% statements; Stryker project mutation 75.34% (>=70% break threshold). Prior 2026-07-11 (Phase 37E composer reasoning-effort selector close-out: re-attested the four rows whose CI-gate globs match the 37E range — **KV cache hit rate** (`internal/llm/**`: reasoning-effort detection + an optional `reasoning` fold; the default/`auto` replay path leaves the cache prefix byte-identical — metric-neutral), **Telegram channel** + **Scheduler North-Star** (`cmd/aura/serve.go`: only the `wireReasoningCapabilities` boot call; zero telegram/cron surface — metric-neutral), and **Live CoT/tool-use eval** (`internal/agent/llm_agent*.go` + `internal/llm/**`: an opt-in reasoning-override seam defaulting to today's adaptive policy, so the asserted dimensions are governed by unchanged code; graduated fidelity is manual-only per D-09 — metric-neutral). Web-only selector; the ≥85% frontend coverage floor is enforced by the web unit gate (`vitest run --coverage`, all-files 92.62% stmts). Prior 2026-07-10 (Phase 37D composer skill-picker close-out: re-attested the three rows whose CI-gate globs match the pushed range — **AG-UI gateway** (`internal/agui/**`: added the `GET /api/composer/skills` route + `aura.skill` Mechanism-A prepend via `composer_api.go`/`governance_seam.go`/`server_run_request.go`/`server.go`; `internal/agui` re-measured **86.8%** on the full db+neo4j matrix, isolated DB; SSE translator / `POST /agent/run` / REASONING_* mutation surface unchanged; live `composer-skills.spec.ts` **8/8** against the rebaked container), and **Skills system** + **Snippet-reuse** (`internal/agent/tools/skill*.go`: `skill_read.go` exports `useAuthorityFrame`→`UseAuthorityFrame`, a byte-identical const the composer reuses — behavior-identical, so validator/writer/xlsx-North-Star and the `TestSnippetReuseE2E` row of record stand — metric-neutral; witness `go test -count=1 ./internal/agent/tools/ ./internal/skills/ ./internal/toolinvocations/` green). Also closed the coverage-gate live-DB footgun after a 2026-07-10 data-loss incident: `scripts/coverage_docker.sh` now provisions/drops a disposable coverage DB, and `scripts/coverage_gate.sh` refuses `db_integration` against a live `aura` locally. Prior 2026-07-02 (industrial validation re-baseline: WSL2 Go vet/build/test/race, golangci-lint/deadcode/govulncheck, web format/lint/typecheck/dup/deadcode/unit/build/contrast, Docker rebuild, full Playwright desktop+mobile container E2E 90 passed/8 intentionally skipped, and live paid real-agent TestCoTEval PASS 37/37 asserted+critical. Prior 2026-06-29 (product-hardening re-baseline for onboarding/Authula, runtime settings, Postgres 18/container bootstrap, GPU compose, Garage key generation, and mobile governance UI fixes. This batch updates the seven rows whose gate globs match touched setup/auth/container/UI paths; these are metric-neutral re-attestations except for the local unit/build witnesses called out below. Local witnesses after the file-size split: `bash scripts/check-file-size.sh`, `go test -count=1 ./cmd/aura`, `npx vitest run src/__tests__/LoginPage.test.tsx src/__tests__/LoginPage.authula-errors.test.tsx --config vitest.config.ts`; full repo gates rerun before push: `go test -count=1 ./...`, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test`, and `bash scripts/quality_snapshot_gate.sh`.) Prior 2026-06-28 (Phase-30 retrieval-memory-hardening RET-05 re-baseline for amendment #20: this push adds a rerank/retrieval `go vet` compile-floor + `AURA_RERANK_BASE_URL` export to `.github/workflows/ci.yml` and a pure-metrics + gated `retrieval_eval` harness under `internal/eval/`; it re-baselines the Telegram, AG-UI, and Live CoT/tool-use eval rows whose globs match the touched `.github/workflows/ci.yml` / `internal/eval/**` paths, each metric-neutral — no Telegram-render, AG-UI SSE/REASONING, or CoT-harness/agent/llm production surface changed. Local witnesses: `go test -race ./internal/documents/ ./internal/eval/ -count=1` green, `bash scripts/quality_snapshot_gate.sh` passes.) Prior 2026-06-20 (Sigma parallel-relationship and GraphView fallback re-baseline for amendment #20: this patch touches `internal/knowledge/**` test expectations for the documented drawable overview fallback and web graph renderer/assets so Sigma uses a multigraph for parallel Neo4j relationships. No retrieval ranking, embedding/HNSW vector search, corpus-search, or vector-latency production surface changed; no 100K benchmark was run. Local witnesses: `go test -race -count=1 -tags 'db_integration neo4j_integration' ./internal/knowledge/ -run TestGraphViewLive_Footprint`, `npx vitest run src/graph/__tests__/SigmaCanvas.test.tsx`, `npm run lint`, `npm run typecheck`, `npm run build`, `go test ./...`, and `bash scripts/quality_snapshot_gate.sh`.) Prior 2026-06-19 (Phase 27 graph-explorer publish re-baseline for amendment #20: this push included the Phase 27 read-only graph contract/API/UI commits, touching `internal/knowledge/**` and `cmd/aura/serve.go` for GraphView/GraphReader and `/api/graph/*` wiring, with no retrieval ranking, embedding/HNSW vector search, or scheduler dispatch/once-per-window production logic changed. Local Docker witnesses: graph/web lint, format, typecheck, build, contrast, full Vitest, Playwright desktop/mobile graph E2E, targeted graph mutation, and containerized Go test/vet/build/race for `internal/knowledge` + `internal/agui` all passed.) Prior 2026-06-19 (CI web-e2e Garage objectstore revalidation: the workflow now boots and bootstraps Garage before Playwright so browser asset uploads exercise a real S3-compatible target; local web lint and Vitest coverage passed with branch coverage 85.12%, and local Phase 26 desktop/mobile E2E evidence remains 54/54. Final GitHub run pending for this follow-up.) Prior 2026-06-18 (CI Node 24 action/runtime revalidation: `.github/workflows/ci.yml` now uses `AURA_CI_NODE_VERSION=24.x` plus Node-24-native `setup-node@v6` and `paths-filter@v4`; current run 27745323083 passed the relevant AG-UI/Telegram live/compile jobs before the remaining generated-code/web-unit fixes in this commit.) Prior 2026-06-16 (CI revalidation for amendment #20 after Phase-23 frontend/serve wiring touched `.github/workflows/ci.yml`, `cmd/aura/serve.go`, and `scripts/agui_boundary_check.sh`: run 27617744760 passed go test -race, db_integration, AG-UI live smoke, scheduler chaos, Telegram compile, multimodal audio, Knowledge, Memory MCP, and coverage jobs; web lockfile was repaired and local web lint/format/typecheck/test/build passed.) Prior 2026-06-14 (CoT/Telegram rendering revalidation for amendment #20: the changed agent-owned path was the reasoning-stream privacy/passthrough event plumbing, covered by focused redaction/passthrough tests and the full untagged Go suite; the changed Telegram-owned paths were HTML normalization/status-pane rendering, with Markdown heading/hr and redacted-sentinel regressions covered locally. Prior same-day onboarding/profile revalidation: onboarding/profile dispatch plumbing and tests changed; `mdv2.go`, `renderer.go`, Bot-API send rendering, multimodal sidecars, and inbound command routing were untouched. Local `go test ./...` passed; the pushed CI Telegram live Bot-API compile leg passed.) Prior 2026-06-13 (audit-closure re-measure: MCP mock tier re-run green after the B-09 secret-unify; CoT-eval live re-run because the agent/llm core changed (B-05/06/08, O-08, B-12, ShowReasoning default); KV-cache/GraphRAG/Vector-p95/Telegram/Scheduler/AG-UI re-baselined with metric-neutrality justifications per amendment #20 — each owned-path touch was audit-hardening (a readiness probe, a SIGTERM drain, a sweeper, a secret-key unify) that does not change the measured behavior; plus 5 pre-existing CodeQL high alerts fixed). Prior 2026-06-12 (Phase-15 close-out E2E re-measure: KV 87.6%, smoke recall@5 5/5 p95 25ms + agent-memory advisory 1.000/44.55ms, CoT 36/37 post-memory-doctrine, Telegram CI live job green, AG-UI live smoke green, sandbox re-attested, Skills/Snippet shared CI gate refreshed at 85.1% — amendment #20 staleness sweep.)
**Owner:** rotating (per metric, see table) — root mandate per amendment #20

---

## Purpose

This file is the contract surface between Aura phases. Every phase that touches a measured quality dimension — sandbox escape rate, KV cache hit rate, retrieval recall/p95, snippet exec success, MarkdownV2 escape fuzz — MUST update the relevant row in this file as part of its Gate 3 Definition of Done. The CI gate `scripts/quality_snapshot_gate.sh` (implemented and wired in Phase 20) enforces this: any PR whose changed file paths match a row's "CI gate path" glob fails if the row's `Last measured` date is older than the PR's merge-base commit date.

The intent traces back to user memory `feedback_aura_as_product` — "Aura come prodotto, non playground, quality matrix obbligatorio, max 2 fasi staged avanti, ogni wave gate su numeri (Recall@5/nDCG@10/p95)" — and is locked in PRD amendment #20 (see `.planning/research/SUMMARY.md` "PRD Amendments Required" row 20, plus `prd.md` Slice 11d acceptance + §Slice Q&A discipline Gate 3 bullet).

This is a living document. A new row may start as unmeasured only while its owner phase is still open; it must be replaced with measured evidence, an explicit blocked status, or a future-benchmark classification before closure. Once a row carries a real value, every subsequent PR under its CI gate path glob MUST re-measure and bump the `Last measured` column — staleness blocks merge.

---

## Quality matrix

**Current-range re-attestation — MCP manager row (2026-07-31, metric-neutral):**
`internal/mcp/tool_error.go` now maps only the two live WhatsApp `None`/`DictModel`
misses to stable `not_found` outcomes. Transport, mount, trust, reconnect, manager,
and tool-policy behavior are unchanged, so the row's measured manager result cannot
move. Fresh witnesses: focused `internal/mcp` regressions, full WSL
`go test -race ./...`, `go build ./...`, and filtered repository `go vet` all pass;
the disposable Postgres/Neo4j owned-surface matrix is 85.4%, and the touched
`internal/mcp/tool_error.go` mutation score is 100% (35/35 killed).

| Metric | Target | Last measured | Last value | Owner phase | CI gate path |
|---|---|---|---|---|---|
| Sandbox execution baseline (sandbox-agent pivot) | live command + workspace persistence pass | 2026-08-25 | **RE-ATTESTED 2026-08-25 (metric-neutral; approval-expiry deployment plumbing only).** The matching compose.yaml change adds only the AURA_ASKUSER_PAUSE_TTL_SEC passthrough on the Aura service. No sandbox service, image, profile, capability, mount, egress rule, resource limit, internal/sandboxagent/**, docker/sandbox-agent/**, or sandbox_exec path changed, so live command execution and workspace persistence cannot move. The sandbox tier was not re-run. Witness: docker compose config --quiet green. Prior **RE-ATTESTED 2026-08-24 (metric-neutral; two dead env lines removed, neither of them the sandbox).** The matching path is `compose.yaml`, and this range's only edits to it are two deletions from the `aura` service environment: `AURA_MCP_SERVERS_JSON` and `AURA_MCP_CONFIG`. Both pointed at the file-backed MCP registry (`servers.json`), which this range replaces with Postgres (`aura.mcp_server`, migration 0101) — they were forwarding a path nothing opens any more, so removing them changes no running behaviour at all, let alone the sandbox's. No service, image, profile, capability, cgroup, seccomp rule, egress rule, mount or resource limit moved, and no byte under `internal/sandboxagent/**`, `internal/agent/tools/sandbox_exec.go` or `docker/sandbox-agent/**` changed — the range lives in `cmd/aura`, `internal/mcp*`, `internal/agent/tools/spec.go` + `manifest.go`, `internal/agui`, `internal/config`, `internal/db` and `web/`. Live command execution and workspace persistence have no input here, so this row's result cannot have moved. What these witnesses do NOT cover: the sandbox tier itself was not re-run — `docker_integration` is out of the range and out of the coverage gate, so this is an argument from an unchanged surface, not a fresh measurement of the box. Witnesses for the range: WSL `make quality` green end to end (deadcode, vet, build, file-size, embedding/LLM model contract, lint, `test-race`, govulncheck 0 vulnerabilities), and the rebuilt `aura:local` came up healthy with all five MCP servers mounted (calendar, linear, memory, notion, whatsapp) — the sandbox profile was untouched by that rebuild. Prior 2026-08-22 **RE-ATTESTED 2026-08-22 (metric-neutral; a sidecar image tag, not the sandbox).** The matching path is `compose.yaml`, and this change's only edit to it is the `aura-pim-mcp` service: the floating `:sidecar` tag replaced by the immutable commit tag `:10383276961828bc19f34a9372ba2c64a14e2b62` (digest `sha256:48c9119a...`), plus the comment explaining why. The calendar/PIM sidecar is a sibling MCP container; it has no sandbox-agent, no `sandbox_exec` route, and no capability, cgroup, seccomp or egress setting anywhere near it. `internal/sandboxagent/**`, `internal/agent/tools/sandbox_exec.go` and `docker/sandbox-agent/**` are untouched, so nothing this row measures - live command execution or workspace persistence - can have moved. Verified after the swap that the change is confined to the PIM service: `docker compose config aura-pim-mcp` resolves the pinned tag and the container came up healthy on that digest; no sandbox service was rebuilt or restarted. Prior 2026-08-17 **RE-ATTESTED 2026-08-17 (metric-neutral; a second published port and one env line, neither of them the sandbox).** The matching path is `compose.yaml`, and this phase's edits to it are two: `AURA_MCP_SANDBOX_ORIGIN: ${AURA_MCP_SANDBOX_ORIGIN:-}` on the `aura` service, and `0.0.0.0:${AURA_MCP_SANDBOX_PORT:-8444}:8444` published on `caddy`. Both belong to the MCP Apps view sandbox (SEP-1865), which is a BROWSER origin - an iframe boundary the user agent enforces so a mounted server's HTML can hold `allow-same-origin` without holding the cockpit's session - and shares nothing with the sandbox-agent execution box this row measures. Nothing under `internal/sandboxagent/**`, `internal/agent/tools/sandbox_exec.go` or `docker/sandbox-agent/**` is touched, and no sandbox service, image, profile, capability, egress rule, mount or resource limit changed. The exec baseline has no input in the range, so the live-command and workspace-persistence results cannot move. Prior 2026-08-15 **RE-ATTESTED 2026-08-15 (metric-neutral; a memory-sidecar env line, not the sandbox).** The matching path is `compose.yaml`, and the edit in this phase is ONE added line on the `arcadedb-mcp` service: `AURA_MEMORY_OPERATOR_DISPLAY_NAME: ${AURA_MEMORY_OPERATOR_DISPLAY_NAME:-}`, plus its comment. It passes the operator's display name to the memory sidecar so `canonicalSubject` has a canonical form to collapse onto (MEM-04); Phase 45 shipped the `os.Getenv` read without the compose declaration, which made the knob unreachable from the deployment however `.env` was set. Nothing under `internal/sandboxagent/**`, `internal/agent/tools/sandbox_exec.go` or `docker/sandbox-agent/**` is touched, and no sandbox service, image, profile, capability, egress rule, mount or resource limit changed - the `arcadedb-mcp` service is a different service entirely and the sandbox has no input in the range, so the live-command and workspace-persistence result cannot move. Fresh witness: `shell_exec` ran live in the box repeatedly during 45-08's scenario on this branch (`df -h` reporting `/workspace` on `/dev/sdd`, and four separate write-then-read commands against `/tmp/aura45.txt`), all exit_code 0. Prior 2026-08-13 **RE-ATTESTED 2026-08-13 (metric-neutral; the observability pack, not the sandbox).** The matching path is `compose.yaml`, and the edits in it are the prometheus/tempo/grafana services plus two env lines on aura: `network_mode: "service:aura"` is removed from the two observability sidecars, AURA_OTEL_ENDPOINT moves to the tempo service name, and AURA_METRICS_BIND is set. Nothing under `internal/sandboxagent/**`, `internal/agent/tools/sandbox_exec.go` or `docker/sandbox-agent/**` is touched, and no sandbox service, image, profile, capability, egress rule or resource limit in compose.yaml changed - so the exec baseline this row measures has no input in the range. Why compose.yaml was touched at all: `up{job="aura"}` sat at 0 for five and a half hours on 2026-08-13 because the sidecars sharing aura's network namespace stayed attached to a dead one after the owner was recreated (docker/compose#6626, still open). Fresh witness: the full stack including the sandbox profile came up healthy on this branch and is what production runs now. Prior 2026-08-09 **RE-ATTESTED 2026-08-09 (metric-neutral).** The matching path is `compose.yaml`, and the edit is a COMMENT on the `aura-ingest` service only: it recorded that catch-up mode fails closed on an extraction error, which was measured false, and now points at `app.py`'s `audit_pass()` instead. No `test:` line, image, capability, mount, command contract, box profile or `AURA_SANDBOX_*` limit moved, and no byte under `internal/sandboxagent/**`, `docker/sandbox-agent/**` or `internal/agent/tools/sandbox_exec.go` changed, so this row's live-command and workspace-persistence result has no input that could move. Prior **RE-ATTESTED 2026-08-08 (metric-neutral).** The matching path saw only the asset object-key change (`AssetKey` now returns `chat/<assetID>.<ext>`) and the file-manager seams. Nothing in the sandbox runtime, its exec path or workspace persistence was touched; no command, mount or capability changed, so the live-command and persistence result cannot move. Prior 2026-08-06 **RE-ATTESTED 2026-08-06 (metric-neutral).** The matching path is again `compose.yaml`, and the edit is confined to observability liveness: grafana's healthcheck now also probes `http://aura:3200/ready` and `http://aura:9090/-/ready`, because tempo and prometheus run with `network_mode: service:aura` and cannot detect their own orphaning when that namespace is replaced. Comments were added above the tempo and prometheus healthchecks; their `test:` lines are byte-unchanged (tempo's image is distroless and cannot run a shell test at all). No sandbox-agent image, capability, workspace-persistence path, command contract, box profile or `AURA_SANDBOX_*` limit was touched, and no byte under `internal/sandboxagent/**`, `docker/sandbox-agent/**` or `internal/agent/tools/sandbox_exec.go` changed, so this row's measured result has no input that could move. Prior **RE-ATTESTED 2026-08-03 (metric-neutral).** The matching `compose.yaml` edit changes only the independent ArcadeDB MCP runtime and health probe: the retired Python/spaCy worker layer becomes a static Go-only Alpine image, and its healthcheck uses BusyBox `wget` against the same `/health` endpoint. No sandbox-agent image, capability, workspace-persistence path, command contract, or `AURA_SANDBOX_*` limit changed, so this row's measured result cannot move. Witnesses: `docker compose config --quiet` green; rebuilt `arcadedb-mcp` healthy; image size 7,709,634 bytes with no Python or worker artifact. Prior **RE-ATTESTED 2026-08-02 (metric-neutral, third entry today).** The matching path is again `compose.yaml`: the `markitdown` service is deleted (with its `docker/markitdown/` build context, the `DOCUMENTS_BASE_URL` passthrough on the aura service and the `compose.minipc.yaml` override + `depends_on` entry) because the Telegram document client was its last caller and that client is gone. Nothing in this row's measured surface has an input that could move: no `AURA_SANDBOX_*` cap, profile, box image or workspace mount changed, and no byte under `internal/sandboxagent/**`, `docker/sandbox-agent/**` or `internal/agent/tools/sandbox_exec.go` was touched — the removed service was never part of the sandbox lifecycle. Witnesses: `docker compose -f compose.yaml config` and `docker compose -f compose.yaml -f compose.minipc.yaml config` both parse and render a service set identical except for the removed sidecar. Prior **RE-ATTESTED 2026-08-02 (metric-neutral, second entry today).** A later change in the same range also matches `compose.yaml`: the `AURA_CONTEXT_MEMORY_RECALL` and `AURA_CONTEXT_MEMORY_RECALL_MAX_ITEMS` passthroughs are removed with the adaptive plane's dynamic-recall leg (its only provider called a `memory_get_context` tool the ArcadeDB server does not have). Two env lines on the aura service, nothing else: no sandbox cap, profile, box image, workspace mount, `internal/sandboxagent/**`, `docker/sandbox-agent/**` or `sandbox_exec` byte moved, so live command execution and workspace persistence have no input that could shift. Witness: `docker compose -f compose.yaml config` parses. Prior **RE-ATTESTED 2026-08-02 (metric-neutral).** The matching path is `compose.yaml`, and the change there is comment text only — two header comments that narrated the retired graph service were replaced with a statement of what the stack is. No service, image, `mem_limit`, volume, healthcheck, `depends_on` condition or environment value moved, and `docker/aura-sandbox/Dockerfile` lost only a comment (not in this row's glob in any case). Live command execution and workspace persistence have no input that could have shifted. Witness: `docker compose -f compose.yaml config` parses and renders an unchanged service set. Prior **RE-ATTESTED 2026-07-30 (metric-neutral)** - the matching change is the embedding-sidecar posture: `compose.yaml` raises only its context/ubatch limits, while `.github/compose.ci-cache.yaml` selects the CPU llama.cpp image and clears the workstation NVIDIA reservation on hosted runners. No `AURA_SANDBOX_*` cap, sandbox profile, box image, workspace mount, `internal/sandboxagent/**`, `docker/sandbox-agent/**`, or `sandbox_exec` code changed, so live command execution and workspace persistence have no input that could move. WSL witnesses: both actual CI Compose chains (`compose.yaml` + `compose.ci-cache`, with and without `compose.ci-musr`) resolve `ghcr.io/ggml-org/llama.cpp:server` and contain no NVIDIA/deploy reservation; `docker compose config --quiet` passes for both. Prior **RE-ATTESTED 2026-07-28 (metric-neutral)** - the only path this range matches is `compose.yaml`, and every hunk in it is the embedding sidecar: `aura-llama-embed`'s `--hf-repo`/`--hf-file`/`--pooling` arguments and the memory sidecar's `NAM_EMBEDDING` default. No `AURA_SANDBOX_*` cap, no profile, no box image, and no file under `internal/sandboxagent/**` or `docker/sandbox-agent/**` is touched, so nothing in the measured lifecycle has an input that could have moved. Reported as context and NOT as a re-measurement of this tier: untagged `go test ./internal/sandbox/usersandbox/` green (neighbouring package, different glob). Witnesses: `go build ./... && go vet ./...` clean, `golangci-lint` 0 issues on every touched package. Prior **RE-MEASURED 2026-07-27 (first live run of the tier)** - the matching changed path is `compose.yaml`, but this is not the usual metric-neutral note: the edit forwards `AURA_PROFILE` and the `AURA_SANDBOX_*` caps to the aura service, which compose had never passed, so a deployment silently ran the `dev` default and the whole strict-profile box path was unreachable from a compose stack. Defaults are the in-code defaults, so no existing deployment changes behaviour; what changed is that the posture became selectable at all. The same range makes the `docker_integration` tier RUN in CI for the first time (job `sandbox-docker-integration`), and its first honest execution — on a native-Linux appliance, Docker 29.6.2 — found three real defects now fixed: the egress floor DROPped the box's own RFC1918 DNS resolver (name resolution dead, raw IPs fine); Suspend paid Docker's 10s grace twice per box for a SIGTERM nothing answers; and `AURA_SANDBOX_IDLE_TTL_SEC=0` suspended every box on every tick instead of disabling the reaper. Fresh live witnesses on that appliance: full tier green (`usersandbox` 31.1s, `agent/tools` 3.0s, `cmd/aura` 21.6s) where the pre-fix run failed `TestEgress_FloorDropsInternal` in 0.92s; measured lifecycle — warm Route (what every tool call pays) **22ms**, cold Route 290ms, auto-resume after suspend 347ms, Suspend 4.29s (was ~20s). Local: `go build ./... && go vet ./...` green, `golangci-lint` 0 issues, `go test ./internal/sandbox/usersandbox/` green. Prior **RE-ATTESTED 2026-07-26** - the only matching changed path is `compose.yaml`, and both edits are on the `aura-agent-memory-mcp` service: the `AURA_AGENT_MEMORY_VENDOR_REV` build arg (the Dockerfile now refuses a build that does not name the Aura revision carrying the vendored tree) and the `AURA_RERANK_BASE_URL` passthrough that stops the RET-02 recall reranker degrading to identity order. Zero `internal/sandboxagent/**`, `internal/agent/tools/sandbox_exec.go` or `docker/sandbox-agent/**` bytes changed, and the `sandbox-agent` service block is untouched, so live command execution and workspace persistence cannot have moved. Prior **RE-ATTESTED 2026-07-24** - Amendment #93.2 changes this row only through `compose.yaml`, removing retired learning env wiring; sandbox-agent, `sandbox_exec`, and workspace persistence are untouched, so the live sandbox baseline is metric-neutral. Fresh witnesses: WSL full lint 0 issues, vet/build green, affected unit/race green. Prior **RE-ATTESTED 2026-07-23** - the matching changed path is `compose.yaml` only: the fix-plan 1.3 Tier B / 1.12 batch adds the `AURA_AGUI_RUN_*` five-knob passthrough (amendment #90 point 9; `AURA_AGUI_RUN_DETACH` default flipped to `true` per amendment #90.1 after the live E2E scored 10/10) and the `AURA_REASONING_PERSIST_MAX_RUNES` passthrough (amendment #91) on the aura service - env-plumbing only. Zero `docker/sandbox-agent/**`, `internal/sandboxagent/**`, or `sandbox_exec` surface changed (the batch lives in `internal/agui`, `internal/runner`, `internal/conversations`, `internal/config`, `cmd/aura`, `web/` - none in this row's glob), so live command execution and workspace persistence are metric-neutral. Fresh witnesses: rebuilt `aura:local` deployed healthy (stale-image audit container==tag); the RS-08 live E2E drove real agent turns through the container (S1-S5 all pass incl. shell/tool-using turns persisting normally); `go build ./... && go vet ./...` + full repo pre-commit gates (gofmt/file-size/lint/vet) green on every one of the batch's commits. Prior **RE-ATTESTED 2026-07-22** - Phase 39 matches this row through `compose.yaml` only: optional observability/readiness wiring was added, and `garage-bootstrap` now waits for the successful migration job before its idempotent bootstrap. No `docker/sandbox-agent/**`, `internal/sandboxagent/**`, or `sandbox_exec` production behavior changed, so live command execution and workspace persistence remain metric-neutral. Fresh witnesses: the rebuilt production image is healthy and ready; the real-agent Playwright gate scored **10/10** with clean desktop/mobile browser health; full uncached disposable Postgres+Neo4j owned coverage is **85.4%**; untagged test/build/vet and affected Linux race tiers are green. Prior **RE-ATTESTED 2026-07-20** - the matching changed path is `compose.yaml`; two same-day pushes: (a) added the `AURA_CONTEXT_MEMORY_RECALL` / `AURA_CONTEXT_MEMORY_RECALL_MAX_ITEMS` env passthrough gating L4 archival-memory recall, then (b) flipped the aura-service **deployed default to `true`** after the feature went CI-green + live-E2E-validated. Both are env-plumbing on the aura service only — zero `docker/sandbox-agent/**`, `internal/sandboxagent/**`, or `sandbox_exec` surface changed (the recall path lives in `internal/runner` + `cmd/aura`, none in this row's glob). Metric-neutral: sandbox live-command + workspace-persistence behavior is governed by unchanged code. Local witness: `go build ./... && go vet ./...` green; full repo gates (gofmt/file-size/vet/lint) green pre-commit; owned-surface coverage 85.6% (db+neo4j matrix); `docker exec aura printenv AURA_CONTEXT_MEMORY_RECALL` → `true` post-recreate. Prior **RE-ATTESTED 2026-07-08** - the matching changed path is `compose.yaml`; the 37A follow-up mounts three retained package-cache volumes (`aura-{npm,pip,uv}-cache-host` → `/root/.npm`, `/root/.cache/pip`, `/root/.cache/uv`) on the aura service so the non-strict host-direct `shell_exec` path (which runs IN the aura container, not a box) stops re-downloading npm/pip/uv packages on every recreate — mount-only, zero `docker/sandbox-agent/**`, `internal/sandboxagent/**`, or `sandbox_exec` surface changed. Live witness: after `docker compose up -d --force-recreate aura`, `npm install docx --offline` succeeded (22 pkgs, no network) and `pip install` reported `Using cached`; the caches survived the recreate. Local: `go test ./internal/sandbox/usersandbox/` ok, `go build ./... && go vet ./...` green. Prior **RE-ATTESTED 2026-07-06** - the matching changed path is `compose.yaml`; the Phase 36 edit only plumbs the D-13 `AURA_MUSR_ISOLATION` documents-plane flag to the aura service (it was unreachable in the deployed container), defaulting off — zero `docker/sandbox-agent/**`, `internal/sandboxagent/**`, or `sandbox_exec` surface changed. Local witness: `go build ./... && go vet ./...` green; full repo gates green pre-push. Prior **RE-ATTESTED 2026-07-01** - the matching changed path is `compose.yaml`; the bundled change (commit `10829280`, carried into the Phase 33 push) only parameterizes the `llama-embed` embedding-sidecar model via `AURA_EMBED_HF_REPO`/`AURA_EMBED_HF_FILE` env defaults — the edit is in the llama-embed service, not the sandbox; zero `docker/sandbox-agent/**`, `internal/sandboxagent/**`, or `sandbox_exec` surface changed. Phase 33 itself touched only `internal/config`, `internal/agent/tools/shell_exec_env.go` (≠ `sandbox_exec.go`), and `cmd/aura` — none in this row's glob. Local witness: `go test -count=1 ./cmd/aura` green; full repo gates (build/vet/lint/deadcode/file-size) green pre-push. Prior Re-attested 2026-06-29: product-hardening batch touched compose/Postgres 18/GPU/Garage setup wiring and generated web assets; no sandbox-agent, sandbox_exec, or workspace-persistence behavior changed. Local witnesses: `bash scripts/check-file-size.sh`, `go test -count=1 ./cmd/aura`; full repo gates rerun before push. Prior Re-attested 2026-06-28: the matching changed path is `compose.yaml`; this push only adds `aura.depends_on.aura-agent-memory-mcp.condition=service_healthy` so Aura waits for the memory MCP sidecar before the boot-time mount. Local witness: `docker compose config` rendered the dependency as `service_healthy`; zero `docker/sandbox-agent/**`, `internal/sandboxagent/**`, or `sandbox_exec` surface changed. Phase 8/08.1 validation evidence stands; bespoke Phase 5 escape bench superseded and removed. Prior 2026-06-21 re-attestation covered the `aura-stt` loopback sidecar auth config; prior 2026-06-16 re-attestation covered `aura-pim-mcp` HTTP sidecar, GHCR default image/pull-policy wiring, and `aura-pim-data` volume for calendar/mail/contacts MCP wiring; prior 2026-06-15 re-attestation covered appliance env forwarding/setup orchestration compose wiring; prior 2026-06-12 re-attestation covered an aura-agent-memory-mcp-only compose touch. | Phase 8 sandbox-agent pivot | `internal/sandboxagent/**`, `internal/agent/tools/sandbox_exec.go`, `docker/sandbox-agent/**`, `compose.yaml` |
| KV cache hit rate (DeepSeek-V4 Flash, 20-turn replay) | >= 80% | 2026-08-22 | **RE-ATTESTED 2026-08-22 (metric-neutral, and the neutrality is pinned by a test rather than asserted here).** The matching paths are `internal/llm/client.go`, `internal/llm/config.go` and `internal/llm/openai_compat/client.go`, which add `llm.Sampling` — the model-card knobs beyond temperature (top_p, top_k, min_p, presence and repetition penalties) — as five POINTER fields carried from config to the wire. Unset is the default and unset is omitted: every new `wireRequest` field is an `omitempty` pointer and `Config.Sampling` stays nil unless the operator sets `AURA_LLM_TOP_P`/`TOP_K`/`MIN_P`/`PRESENCE_PENALTY`/`REPETITION_PENALTY`, none of which this deployment's `.env` sets, so the serialized OpenRouter request this row replays is byte-identical. That is not a claim in prose: `TestRequestBody_CardSamplingOmittedWhenUnset` fails if any of the five ever appears in the body when unconfigured, and `TestRequestBody_CardSampling` pins the shape when it is. `messages[0]` construction, `cache_control` placement and the provider-aware prefix are untouched — the prompt builder's only change is copying `cfg.Sampling` into the request, which is not inside `messages` at all. `scripts/cache_invariant_audit.sh` is untouched. Witnesses at this HEAD: `go build ./...` and `go vet ./...` clean; WSL `-race` green on `internal/llm`, `internal/llm/openai_compat` and `internal/agent/prompt`; golangci-lint 0 issues on those packages. The real guard remains the dedicated `KV-cache prefix invariant gate (Postgres-free)` CI job, which re-runs on this push. Prior 2026-08-17 **RE-ATTESTED 2026-08-17 (metric-neutral, and the neutrality is asserted by a new test rather than by this note).** The only path under this row's globs is `internal/llm/openai_compat/client.go`, where the `wireRequest.MaxTokens` JSON tag gains `,omitempty` so a caller that sets NO output cap sends no `max_tokens` key instead of `max_tokens: 0`. This row measures the `messages[0]` prefix, the `cache_control` placement and the shape of the wire request; `max_tokens` is not inside `messages` at all, and every existing caller sets it from config, so their serialized bytes are unchanged. That second half is not an assertion here: `TestWireKeepsAnExplicitMaxTokens` (`client_max_tokens_wire_test.go`, new) fails if an explicit cap ever stops reaching the wire, and `TestWireOmitsMaxTokensWhenTheCallerAsksForTheModelsOwnCeiling` pins the new behaviour across openrouter/llamacpp/vllm. The one caller that uses the new path is the conversation summarizer, which is NOT a chat request and does not participate in this row's replay. `scripts/cache_invariant_audit.sh` is untouched. Witnesses at this HEAD: `go build ./...`, `go vet ./...` clean; `go test ./internal/llm/...` green (openai_compat 97.6% stmts). The real guard remains the dedicated `KV-cache prefix invariant gate (Postgres-free)` CI job, which re-runs on this push. Prior 2026-08-16 **RE-ATTESTED 2026-08-16 (NOT claimed metric-neutral — the decider is the CI job).** The matching paths are `internal/llm/config.go` and `config_validation_test.go`, which add `defaultCompactionTriggerPercent = 50` and scale the token reserves to the window. That is a behaviour change, not a comment: if early compaction fires inside the 20-turn replay it rewrites history and therefore the `messages[0]` prefix this row measures, so neutrality is deliberately NOT asserted here. What is unchanged is the request-composition path itself — `cache_control` placement and the provider-aware prefix are byte-identical, and `scripts/cache_invariant_audit.sh` is untouched. The real guard is the dedicated `KV-cache prefix invariant gate (Postgres-free)` CI job, which re-runs on this push and goes red if the prefix moved. Witnesses: `go build ./...` and `go vet ./...` clean at this HEAD. Prior 2026-08-13 **RE-ATTESTED 2026-08-13 (metric-neutral; the matching edit is a comment).** The only path in this range under this row's globs is `internal/llm/client.go`, and the entire diff there is two comment lines added above the `ReasoningEffort` const block by a lint pass that wants the block documented. No constant value, no struct field, no struct tag and no serialisation changed, so the bytes this row actually measures - the `messages[0]` prefix, the `cache_control` placement and the shape of the wire request - are identical to the run of record. `scripts/cache_invariant_audit.sh`, this row's other glob, is untouched. Witnesses: `go build ./...` and `go vet ./...` clean at this HEAD. The real guard is not this note: the dedicated `KV-cache prefix invariant gate (Postgres-free)` CI job passed on this branch at 40bc74b85 and re-runs on this push. Prior 2026-08-02 **RE-ATTESTED 2026-08-02 (metric-neutral).** The matching path is `internal/llm/**`, and the only edit there is the deletion of `llm.Message.DynamicTailID` — a field tagged `json:"-"`, so it was never serialised onto the wire in the first place. It existed solely to mark the adaptive plane's dynamic recall tail, which is deleted with the plane; nothing that composes `messages[0]`, the cache_control seam, or the request prefix changed a byte. `scripts/cache_invariant_audit.sh` is untouched. Witness: `go build ./... && go vet ./...` clean. Prior **RE-ATTESTED 2026-07-30.** The cache audit now matches the deferred-tool contract: 23/23 identical `messages[0]` hashes, 23/23 identical `messages[1]` hashes, and skill absent from all 23 request manifests because it is deferred. A new negative proves a partial `skillman` stream fails, so the conditional witness re-arms if skill becomes always-active. Focused `cmd/aura` and `internal/agent/tools` unit/race, full `go vet ./...`, and `go build ./...` passed. The prior hit-rate value remains metric-neutral because pricing/spend accounting and invalid-budget rejection do not alter valid request bytes. Prior: **INVALIDATED / RE-MEASUREMENT DUE 2026-07-30.** The current push range changes pricing/spend accounting and rejects impossible token budgets without changing valid request serialization; however the runtime-faithful cache audit is red: 23/23 `messages[0]` and 23/23 `messages[1]` hashes are stable, but the deferred-skill change emits 0/23 `skillman` witnesses. The prior hit-rate number is not current until that deferred-manifest witness is repaired and the replay rerun. Fresh unit and race suites passed for `internal/llm/...`. Prior: **RE-ATTESTED 2026-07-26** - the only matching changed path is `internal/llm/client.go`, which adds one field to `Message`: `DynamicTailID string `json:"-"``. The `json:"-"` tag excludes it from serialization entirely, so the request body sent to the provider is byte-identical to before — and the cache hit rate is a property of the serialized prefix. A field that never reaches the wire cannot change what the provider caches. The surrounding lines in the diff are gofmt realignment of the existing struct tags. Prior **RE-ATTESTED 2026-07-24** - Amendment #93.2 removes legacy learned-serving configuration from `internal/llm/**`; it does not change request serialization, cache-control, model routing, or the llama.cpp/OpenRouter wire, so the cache metric is neutral. Fresh witnesses: WSL full lint 0 issues, vet/build and affected unit/race green. Prior **RE-ATTESTED 2026-07-23** - fix-plan Wave 1 touches `internal/llm/**` twice: (a) `openai_compat/client.go` now sends `stream_options:{include_usage:true}` ONLY on the llama.cpp reasoning target (cockpit usage/gauge fix, commit bd10a8f6) — the OpenRouter wire this row measures is asserted byte-unchanged by the openrouter-target wire tests; (b) opt-in `transforms:["middle-out"]` (commit 14892823) is double-gated on OpenRouter target AND `AURA_LLM_OPENROUTER_MIDDLE_OUT` (default OFF) so the default wire is byte-unchanged. messages[0] construction, cache_control, and the provider-aware prefix are untouched — metric-neutral for the measured replay. Local witnesses: `go test ./internal/llm/...` + WSL `-race` green; live llamacpp E2E showed cache_hit_tokens 14428 of prompt_tokens 14665 flowing end-to-end (usage now visible on the local path). Prior **RE-ATTESTED 2026-07-20** - The compaction-engine removal (Amendment #86) deletes from `internal/llm/**` the dark fail-closed provider compaction-capability preflight (`capabilities.go`: `CapabilityFor`/`ProviderCapability`/`ValidateForCompaction` + the trigger/modality/usage/native-compaction structs) and trims `capabilities_test.go` to the surviving `ConservativeTokenUpperBound` seed; `EstimatorCapability`, `internal_context.go`, and `openai_compat/client_compaction_test.go` are unchanged. None of the removed code ever ran on the default/`auto` replay path this row measures — `messages[0]` construction, `cache_control`, and the provider-aware prefix stay byte-identical (the exact inverse of the 2026-07-14 addition below). Local witness: `go build ./... && go vet ./...` green; `go test ./internal/llm/...` green. Prior **RE-ATTESTED 2026-07-14** - Phase 42 (LLM conversation compaction, plans 42-01..42-10) touches `internal/llm/**`: `capabilities.go`+`capabilities_test.go` (a NEW fail-closed provider compaction-capability preflight contract, consulted only when compaction activation is enabled), `content_projection.go`+`content_projection_test.go` (NEW typed multimodal content-part projection for the disabled compaction summarizer path), `internal_context.go` (a plain `InternalContext` struct definition, no wiring), and `openai_compat/client_compaction_test.go` (test-only). Compaction rollout stays fail-closed disabled by default through the whole phase (`cmd/aura/chat_boot.go`'s `EnsureDisabledDefault(ctx, "default")`, pinned by `TestCompactionRolloutDisabledBootstrapSeed`), so none of these additions execute on the default/`auto` replay path this row measures — `messages[0]` construction, `cache_control`, and the provider-aware prefix stay byte-identical. Local witness: `go build ./... && go vet ./...` green; `go test -race ./internal/conversations/compaction_eval/...` green (42-10). Prior **RE-ATTESTED 2026-07-11** - Phase 37E (composer reasoning-effort selector) touched `internal/llm/**` (`reasoning_target.go`/`model_reasoning_caps.go`/`llamacpp_caps.go`/`client.go`/`config.go` + `openai_compat/client.go`) to DETECT a model's advertised effort levels and fold an OPTIONAL `reasoning` effort symbol into the request; the DEFAULT (`auto`/absent) path — the KV replay's path — leaves `messages[0]` construction, `cache_control`, and the provider-aware prefix byte-identical, and a per-turn fixed effort lands in the `reasoning` field (never the stable prefix), so it cannot poison the prefix cache. The measured 80% floor cannot move for the default replay. Local witness: `go build ./... && go vet ./...` green. Prior **PASS 2026-07-08 (LIVE RE-MEASURED)** - `TestKVCacheWarmingE2E` re-run live against `deepseek/deepseek-v4-flash:nitro` (OpenRouter) AFTER the full-promotion deferred-tool change (`manifest.go` now hides deferred tools until `tool_search` loads their schema): peak hit rate **94.2%**, post-first-turn peak **94.2%** (target 80%) over the 6-turn replay (per-turn 92.5/94.2/86.5/80.1/69.3/89.7%) — PASS. This directly validates that hide-then-promote does NOT poison the provider-aware prefix cache: promotion grows the manifest only AFTER a `tool_search` turn, never mutating the stable prefix. The matching `internal/llm/**` changes are test-only `Build(activated)` signature updates. Local witness: `set -a; . ./.env; set +a; go test -tags live_e2e -run TestKVCacheWarmingE2E ./internal/eval/` PASS (33.6s). Prior **RE-ATTESTED 2026-06-29** - runtime model/token settings and OpenAI-compatible auth/config plumbing changed; no messages[0] construction, cache_control, or provider-aware prefix construction changed. Local witnesses: `go test -count=1 ./cmd/aura`, targeted LoginPage Vitest split; full repo gates rerun before push. Prior **PASS** — re-baselined 2026-06-16: this branch changes env/config coherence only for `internal/llm` (`:nitro` as canonical DeepSeek-V4 Flash routing suffix plus the matching seeded price key), with no messages[0] construction, `cache_control`, or provider-aware prefix logic changed. Local witnesses: `bash scripts/cache_invariant_audit.sh` passed (22 identical messages[0] hashes, 22 identical profile/skills hashes, 22 identical skill-manifest hashes), `go test ./...` passed, and pushed CI run 27636749299 passed the KV-cache prefix invariant gate. Prior 2026-06-13 live run: `TestKVCacheWarmingE2E` (post-<memory>-doctrine prompt f1062711): turn-2 **87.6%**, turn-3 86.7%, turn-4 79.4% (turns 5-6 OpenRouter instance-switch 0%); peak post-first **87.6%** >= 80 gate. | Phase 6 Slice 4 | `internal/llm/**`, `scripts/cache_invariant_audit.sh` |
| GraphRAG retrieval recall@5 @ 100K corpus (RETIRED) | n/a — capability removed | 2026-08-02 | **RETIRED 2026-08-02.** The metric is not merely unmeasured, it is undefinable: the capability it scored — cited passage retrieval over a 100K-chunk document graph — no longer exists. Documents are a Postgres catalog of one row per file now; `document_search` answers *which file* and `document_open` hands over the file itself, so there is no passage set to compute recall@5 over and no 100K corpus to compute it against. Memory recall (ArcadeDB, full-text ⊕ 768-d dense, fused by `vector.fuse`) is a **different** metric over a corpus of tens-to-thousands of facts, and inventing a number for it here would be worse than this row's absence; when it is measured it gets its own row with its own target. The CI gate path is emptied deliberately — two of its three globs (`internal/knowledge/**`, `docker/agent-memory/**`) point at deleted trees and could never fire again, which made the row unreachable rather than merely stale. Prior notes retained below for the audit trail. Prior **RE-ATTESTED METRIC-NEUTRAL 2026-07-31 (tool_search rewrite) — THE ROW REMAINS INVALIDATED FROM 2026-07-28 AND THIS DOES NOT CLEAR IT.** This range touches `internal/agent/mcptools/**` in test files only (an embedder fixture deleted with the `ToolSearch.Embed` field). Graph retrieval runs through the agent-memory MCP and shares no code with tool discovery, so recall@5 cannot move on this change — but it is still owed a re-measurement for the embedding-model swap recorded on 2026-07-28, and must not be quoted until that runs. Prior **RE-MEASURED LIVE 2026-07-30 (the 100K row remains INVALIDATED).** Recreated the committed local GPU embedder posture and asserted its real command contains `-np 1 -c 8192`; the endpoint and the active knowledge index use the clean-slate native 1024d contract. The deterministic Italian smoke then ran inside the current non-empty graph and measured actual recall@5 = **5/5**, warmed HNSW **p95 = 2 ms** (30 ms operator budget). That run exposed that the harness requested five candidates but discarded four with `LIMIT 1`; `a52eeda63` now consumes all five, so the gate measures recall@5 rather than mislabeled top-1. This is still the five-document smoke in a live corpus, not a 100K-corpus benchmark, so it does not restore the invalidated 100K claim. Prior **SMOKE-TIER WITNESS 2026-07-28, the 100K row stays INVALIDATED - RE-MEASUREMENT DUE.** The smoke harness was writing the sidecar's NATIVE 1024-wide vector into the 768 index instead of narrowing it the way the write path does; Neo4j keeps such a node and silently drops it from the index, so the tier could have gone green while retrieving nothing. Fixed (`knowledge.TruncateMRL` now serves both the boot check and the harness), and re-run live against the swapped Qwen sidecar: `recall@5 = 5/5`, warmed `p95 = 1 ms`, fixture chunks removed from the graph afterwards. That is a 5-document corpus - it says the post-swap retrieval path works end to end, it does NOT restore this row's 100K figure, which remains unbacked until the benchmark is re-run. Prior **INVALIDATED 2026-07-28 - RE-MEASUREMENT DUE; the prior number is not current and must not be quoted as such.** All three globs match, and the reason is the same in each: this range swaps the embedding model (prd.md amendment #18 runbook) and rewrites the memory sidecar's retrieval surface. recall@5 was measured inside the PREVIOUS embedder's vector space, and a different model is a different space - so the figure is not merely stale, it is unbacked, and it cannot be extrapolated from the old one. What WAS measured on the live graph, and is emphatically not a substitute: the cosine amplitude separating unrelated entities from genuine duplicates went from 0.16 (0.7312-0.8904, a band straddling the 0.80 resolver threshold, which predicted every duplicate collapse we saw) to 0.4857; and the 8 real onboarding nodes that had been written at the wrong width were re-embedded to the contract width and verified present in the HNSW index via `db.index.vector.queryNodes` (0.9999 self-match). The 100K-corpus benchmark must be re-run before this row carries a value again. Prior **RE-ATTESTED** - the matching changed path is `docker/agent-memory/**`: schema DDL (CREATE/DROP CONSTRAINT and INDEX) now commits through a new `execute_schema` transaction instead of `execute_write`, whose per-call corpus-epoch bump made Neo4j reject the sidecar's own bootstrap on any database whose constraints did not exist yet (`Tried to execute Write query after executing Schema modification`). The DDL statements are byte-identical; only the transaction they commit in differs. Nothing in document retrieval ranking, embeddings, the HNSW index or corpus search is touched, so this row is metric-neutral and its `NOT_E2E_FUTURE_BENCHMARK` status stands. Witnesses: the fork's suite 117 passed; a throwaway neo4j:5.26.28-community went from 0 constraints to 13 - including `conversation_id`, the exact one CI died on - plus 39 indexes. The same commit adds the CI step that runs those 117 tests for the first time: there was no `pytest` anywhere in .github/workflows, which is why a bootstrap that cannot create its own schema reached master. Prior **RE-ATTESTED 2026-07-25** - the matching changed paths are `internal/agent/mcptools/**` (the memory bridge now derives which tools get the caller's `user_identifier` from each tool's advertised schema instead of a hand-kept name list) and `docker/agent-memory/**` (the vendored fork: reads project the STORED entity name, `memory_get_entity` discloses same-name duplicates instead of returning only the top hit, `memory_forget` cuts every edge that kept a forgotten entity in the CALLER's read scope, and a `backfill-entity-ownership` maintenance verb). None of it touches document retrieval ranking, embeddings, the HNSW index or corpus search, so the 100K-corpus recall row is metric-neutral and its `NOT_E2E_FUTURE_BENCHMARK` status stands. What DID change is agent-memory read scope, deliberately and in the narrowing direction: an entity the caller forgot no longer comes back through `MENTIONS`. Witnesses: the fork's suite 67 passed (65 unit + 2 executing Cypher against a live Neo4j, each mutation-verified to fail on the exact defect); `go test -race ./internal/agent/mcptools/` green; live controprova on the deployed stack corrected the operator's memory in four tool calls with no `add_*` fallback. Prior **RE-ATTESTED 2026-07-24** - Amendment #93.2 removes four retired learning labels from graph-explorer treatment and migration 0004 deletes only those labels; document/chunk retrieval, ranking, embeddings, and the 100K corpus benchmark surface are unchanged, so recall is metric-neutral. A live unique-fixture test proves User/AdaptiveEpisode/AdaptiveEvent preservation. Prior **RE-ATTESTED 2026-07-23** - the matching changed path is `internal/agent/mcptools/**` (commit 81c8341d): streamable-HTTP managed mounts now get the same reconnect-on-use wrapper as stdio (pluggable opener) plus a bounded background ping poll (`AURA_MCP_PING_INTERVAL_SEC`, default 60). Zero retrieval ranking, embedding, HNSW, or corpus-search surface changed — the future-benchmark status stands. Witnesses: `go test ./internal/agent/mcptools/ -count=1` + WSL `-race` green; live failure-injection E2E — stopping the calendar HTTP sidecar produced proactive ping detection in 37s with breaker/backoff engaged, and post-heal calendar+memory tool calls completed green. Prior **RE-ATTESTED 2026-07-22** - Phase 39 touches `internal/knowledge/client.go` only at the MCP transport boundary: the client now ignores interleaved JSON-RPC notifications/blank separators until the matching response ID and keeps the existing bounded-read fail-closed policy. It also touches `internal/agent/mcptools/**` for durable operation metadata, not retrieval ranking. Embedding, HNSW, scoring, and corpus-search logic are unchanged, so the 100K row remains `NOT_E2E_FUTURE_BENCHMARK`. Fresh live witnesses: the 10,002-node Neo4j compaction integration passed twice plus an independent rerun; the full uncached tagged matrix passed at **85.4%** with zero cached packages; and the rebuilt production agent's real DOCX `document_search` was grounded and contributed to the explicit **10/10** browser score. Prior **RE-ATTESTED 2026-07-20** - the matching changed path is `internal/knowledge/client.go` + `client_hang_test.go` (Wave 0.3): `Cypher` now bounds the response read by a 120s hang ceiling via `readLineWithContext` (goroutine + ctx-select + `Process.Kill` on a genuine hang, `context.WithoutCancel` so a caller-cancel never kills the shared subprocess); every non-hanging call returns the response byte-identically — zero retrieval ranking, embedding, HNSW index, or corpus-search production surface changed, so the future-benchmark status stands. Local witnesses: `go test -race ./internal/knowledge/ -count=1` green (incl. `TestCypherHungSubprocessFailsFastNoLeak` under goleak); live container proof — the cockpit Graph rendered 15 nodes/19 edges through the patched client, zero graph errors. Prior **RE-ATTESTED 2026-07-18** - Phase 38 (MCP governance hardening) touches `internal/agent/mcptools/**` (bridge.go/bridge_reconnect.go/mount.go/mount_retry.go: two-context bounded mount handshake under `AURA_MCP_MOUNT_TIMEOUT`, aggregate shutdown under `AURA_MCP_SHUTDOWN_TIMEOUT`, bounded mount-retry, stdio frame-cap + process-group reap); zero retrieval ranking, embedding, HNSW index, or corpus-search production surface changed — the future-benchmark status stands. Local witnesses: `go test ./internal/agent/mcptools/ -count=1` green; `go build ./... && go vet ./...` green; live container proof (github MCP 44 tools + memory 17 mounted, hung server dropped at the exact 3s `AURA_MCP_MOUNT_TIMEOUT`, subprocess reaped). Prior **RE-ATTESTED 2026-07-08** - the pushed range touches `internal/knowledge/graphview_intent.go` (graph-overview intent read-path speedup) + `graphview_test.go` (scope-guard test) + `internal/agent/mcptools/bridge.go` (scoping no-principal memory-MCP calls to the operator); zero retrieval ranking, embedding, HNSW index, or corpus-search production surface changed; the future-benchmark status stands. Local witnesses: `go test ./internal/agent/mcptools/ ./internal/knowledge/ -count=1` green. Prior **RE-ATTESTED 2026-07-06** - the matching changed path is internal/knowledge/client.go; the Phase-36 gap-closure edit only guards Cypher against use-after-close (returns a clean error instead of dereferencing a nil stdin on a closed client) plus a two-identity E2E cleanup-ordering fix — zero retrieval ranking, embedding, HNSW index, or corpus-search production surface changed; the future-benchmark status stands. Prior **NOT_E2E_FUTURE_BENCHMARK** (re-baselined 2026-06-28: this push touches `internal/agent/mcptools/**` for bounded boot-mount retry and MCP probe/tool-count behavior, but no `internal/knowledge/**`, embedding, HNSW index, retrieval ranking, or corpus-search production behavior changed. Local witness: `go test ./cmd/aura ./internal/mcp ./internal/mcp/manager ./internal/agent/mcptools -count=1` passed. No 100K benchmark harness was executed, so the row remains a future benchmark.) Prior 2026-06-20: GraphView live fallback expectations were aligned with the documented drawable overview behavior and Sigma renders parallel Neo4j relationships via graphology multigraph mode, with zero retrieval ranking/embedding/HNSW/corpus-search production change. Prior 2026-06-19: Phase 27 touched `internal/knowledge/**` for read-only GraphView/GraphReader contracts, intent guardrails, and graph API integration; this was Neo4j graph-explorer read-path/UI evidence plumbing, not retrieval ranking, embedding, HNSW index, or corpus-search surface. Prior 2026-06-15: CI-only touch — the `internal/knowledge/client_unit_test.go` loopback compose-test fix (sanction the one caddy public ingress), zero retrieval/HNSW/embedding surface. Prior 2026-06-13: the `internal/knowledge` change was O-05's `probe.go` — a dial → VerifyConnectivity → close readiness probe for `/readyz`, with zero retrieval/HNSW/embedding surface; the future-benchmark status + smoke/advisory evidence stand. The Neo4j smoke passed on the operator host (`recall@5=5/5`, p95=3 ms after warm-up), but that 5-document smoke is not recorded as the 100K benchmark. Re-measured 2026-06-12: `make smoke` recall@5 **5/5** (p95 25 ms warm) + Phase-15 advisory snapshot vs the agent-memory package recall@5 **1.000** (15-05). Still smoke/advisory evidence, not the 100K benchmark. Follow-up: memory-scale benchmark phase. | Phase 15 Slice 11d | — (retired; the capability and its globs are gone) |
| Vector search p95 latency @ 100K corpus (RETIRED) | n/a — capability removed | 2026-08-02 | **RETIRED 2026-08-02.** Same reason as the recall row above: the 100K-vector document index it timed does not exist. The vector index that does exist is memory's `LSM_VECTOR` (HNSW) leg at 768 d over a personal-scale corpus, reached through `cmd/arcadedb-mcp`; its latency is a different measurement against a different index and is not carried forward here on the strength of a superseded number. The gate path is emptied because `internal/knowledge/**` is a deleted tree. Prior notes retained below. Prior **RE-MEASURED LIVE 2026-07-30 (the 100K row remains INVALIDATED).** The committed local embedder was recreated and its actual container command asserted `-np 1 -c 8192` with the native 1024d endpoint/index contract. On the current non-empty graph, the corrected five-result Italian smoke measured warmed server-side HNSW **p95 = 2 ms** against the strict 30 ms operator budget while recall@5 remained 5/5. This is a real warm-sidecar latency witness, but it is not a 100K-corpus run and therefore does not restore the invalidated 100K latency claim. Prior **SMOKE-TIER WITNESS 2026-07-28, the corpus-level figure stays INVALIDATED - RE-MEASUREMENT DUE.** With the harness corrected to narrow the 1024-wide sidecar response to the 768 contract width (see the GraphRAG row), the warmed HNSW `p95 = 1 ms` server-side over the 5-document fixture, read from `ResultAvailableAfter()`. First run of the same test measured 101 ms on a cold index while the host was also running the coverage gate - recorded because it is what an unwarmed, contended box looks like, not because it is the metric. Neither number is a 100K corpus, so this row's target stays unbacked. Prior **PARTIALLY RE-MEASURED 2026-07-28 - the corpus-level figure is INVALIDATED, RE-MEASUREMENT DUE.** The matched paths (`internal/knowledge/**` and the `aura-llama-embed` sidecar config) changed for one reason: a different embedding model, of a different size. A p95 measured against the previous model does not describe this one. The embed leg alone was measured live against the running sidecar today - 40 sequential single-string requests, mixed IT/EN, short inputs: p50 157.3ms, p95 238.5ms, max 1148.3ms, mean 187.5ms. That is the embed call ONLY: it excludes the Neo4j HNSW query and it is not a 100K corpus, so it is a component witness and not this row's metric. The same run confirms the sidecar returns 1024 components against a 768-wide index, which is why truncation to the contract width is done client-side rather than requested from the server. Prior **RE-ATTESTED** - the pushed range is exactly two files. `cmd/aura/memory*.go` matches on `memory_integration_test.go`, a TEST helper: it now resolves the operator identity through withOperatorIdentity before dispatching, which is what `aura memory` itself does in runMemory — the tier had been calling runMemoryCommand directly and so exercised a path no real invocation takes. No production file is in the range. The other file is `.github/workflows/ci.yml`, where the Memory MCP job now runs `make neo4j-migrate` instead of `neo4j-up` so the Cypher migrations actually apply (0005 MERGEs the corpus-epoch singleton every sidecar write depends on). Neither the HNSW index, the embedding sidecar config, nor any query path is touched, so vector search p95 cannot have moved and this row's `NOT_E2E_FUTURE_BENCHMARK` status stands. Live witnesses for what DID change: read-only `aura memory search` through the real runMemory path resolved the enrolled operator and returned the graph (exit 0, nothing written), then the tier itself passed against the live stack — `ok github.com/chetto1983/aura/cmd/aura 2.505s`, goleak included. Prior **RE-ATTESTED 2026-07-26** - the matching changed paths are `cmd/aura/memory*.go` (operator-identity resolution, the one-MCP-session seam for the onboarding seed, and the `facts`/`update`/`--about` verbs) and `internal/knowledge/**` (the cockpit graph-view query compiler, the Cypher migration statement splitter, and migrations 0005/0006). None of it touches the retrieval path: the embedder (`aura-llama-embed`, granite-311m 768d) is unchanged, no vector index is created, dropped or re-dimensioned, and no `db.index.vector.*` call is added or edited. Migration 0006 writes `(:Fact)-[:ABOUT_SUBJECT]->(:Entity)` relationships, which the HNSW index does not span. The two `embedding`/`vector index` occurrences in this diff are a single comment describing what `memory_update` already did. Corpus, index and query shape are identical, so p95 cannot have moved. Prior **RE-ATTESTED 2026-07-24** - Amendment #93.2 touches `internal/knowledge/**` only to remove retired learning-label graph views and add narrowly scoped migrations/tests; vector indexes, embedding dimensions, similarity queries, and corpus size are unchanged, so p95 is metric-neutral. WSL knowledge unit/race and live Neo4j integration are green. Prior **RE-ATTESTED 2026-07-21** - Amendment #87 (Agent.md -> agent-memory supersession) matches via `cmd/aura/memory_onboarding.go`: the new onboarding `ProfileMemoryStore` adapter deterministically maps `session.Answers` -> `memory_add_*` over the memory MCP. Zero `internal/knowledge/**`, vector-index, HNSW, embedding, or query-latency production surface changed — the future-benchmark status stands. Local witness: full `db_integration neo4j_integration` owned-surface matrix green at 85.5% aggregate via `scripts/coverage_docker.sh`. Prior **RE-ATTESTED 2026-07-20** - same Wave 0.3 `internal/knowledge/client.go` hang-ceiling change as the GraphRAG row (bounded Cypher response read via `readLineWithContext`; every non-hanging call is byte-identical); zero vector-index, HNSW, embedding, or query-latency production surface changed, so the future-benchmark status stands. Local witness: `go test -race ./internal/knowledge/ -count=1` green; live cockpit Graph read-path exercised through the patched client. Prior **RE-ATTESTED 2026-07-08** - same pushed range as the GraphRAG row: `internal/knowledge/graphview_intent.go` (graph-overview intent read-path speedup) + `graphview_test.go`; zero vector-index, HNSW, embedding, or query-latency production surface changed; the future-benchmark status stands. Local witness: `go test ./internal/knowledge/ -count=1` green. Prior **RE-ATTESTED 2026-07-06** - same internal/knowledge/client.go use-after-close guard as the GraphRAG row; zero vector-index, HNSW, embedding, or query-latency production surface changed; the future-benchmark status stands. Prior **NOT_E2E_FUTURE_BENCHMARK** (re-baselined 2026-06-20: same GraphView/Sigma parallel-relationship patch as the GraphRAG row, with zero vector-index, HNSW, embedding, or query-latency production surface changed. The 100K benchmark remains a future benchmark. Local witnesses: `go test -race -count=1 -tags 'db_integration neo4j_integration' ./internal/knowledge/ -run TestGraphViewLive_Footprint`, `npx vitest run src/graph/__tests__/SigmaCanvas.test.tsx`, `npm run lint`, `npm run typecheck`, `npm run build`, `go test ./...`, and `bash scripts/quality_snapshot_gate.sh`.) Prior 2026-06-19: same Phase 27 `internal/knowledge/**` touch as the GraphRAG row — read-only graph explorer contracts/API/UI and Cypher intent guardrails, with zero vector-index/HNSW/embedding/query-latency production surface changed. Local witnesses: containerized `go test -buildvcs=false ./internal/knowledge/... ./internal/agui/...`, `go vet`, `go build`, `go test -race`, Phase 27 Playwright desktop/mobile graph E2E, and targeted graph mutation passed. Prior 2026-06-15: CI-only touch — same `internal/knowledge` loopback compose-test fix, zero vector/latency surface. Prior 2026-06-13: same O-05 `internal/knowledge/probe.go` readiness-probe touch — zero vector/latency surface; future-benchmark status + smoke/advisory evidence stand. No 100K vector latency harness was executed in Phase 20. The warmed Neo4j smoke p95 was 3 ms and remains smoke evidence only, not the 100K row. Re-measured 2026-06-12: warmed smoke p95 **25 ms** (5-doc corpus) + Phase-15 advisory p95 **44.55 ms** vs the agent-memory package (15-05). Still smoke/advisory evidence, not the 100K row. Follow-up: memory-scale benchmark phase. | Phase 15 Slice 11d | — (retired; see the row above) |
| Telegram channel (UX-02/03/04) — MarkdownV2 escape fuzz + `mdv2.go`/`renderer.go` mutation + owned-surface coverage + live Bot-API sends + multimodal sidecars + inbound command route | 400 rate = 0% / mdv2+renderer mutation >=70% / owned-surface >=85% / live sends response-asserted / multimodal round-trip / inbound commands reply in Telegram Web | 2026-08-25 | **RE-ATTESTED 2026-08-25 (metric-neutral; independent approval-expiry lifecycle only).** The matching cmd/aura/serve.go change starts and stops an approval-expiry sweeper. No Telegram package, Markdown renderer, Bot API send, multimodal sidecar, setup flow, or inbound command route changed, so none of this row metrics can move. The Telegram live tier was not re-run; targeted WSL race, vet, build, lint, and deadcode gates are green on the touched packages. Prior **RE-ATTESTED 2026-08-24 (metric-neutral; ArcadeDB identity-memory wiring only).** The matching path is `cmd/aura/serve.go`: startup now builds the ArcadeDB memory provisioner, reconciles active user tenants before readiness, and passes that provisioner into onboarding/bootstrap. No Telegram package, renderer, routing, Bot API, or multimodal path changed, so this row's measured behavior cannot move. Fresh witness: targeted `cmd/aura` and `internal/agui` unit/race runs green. Prior **RE-ATTESTED 2026-08-23 (metric-neutral; one approval keyboard branch, no rendering or escaping path).** The matching path is `internal/channels/telegram/**`, and this phase's only edits under it are `hitl.go` gaining an `approvalScopeMarkup` branch for an approval pause that carries options (PRD amendment #127) plus its Italian button copy, and the new `hitl_scope_test.go` that covers them. `mdv2.go` and `renderer.go` are untouched, so the MarkdownV2 escape fuzz corpus, the 400-rate and the mutation targets this row records have no input in the range. The new branch is a keyboard builder: it composes `tele.InlineButton` values and never reaches the MarkdownV2 escaper, and the inbound command route is unchanged. Owned-surface coverage moves only upward — the branch ships with its own table test. Prior 2026-08-22 **RE-ATTESTED 2026-08-22 (metric-neutral; two CI env values in jobs that run no Telegram code).** The matching path is `.github/workflows/ci.yml`, and this change's only edits to it are the `AURA_PIM_MCP_IMAGE` values in the `calendar-integration-test` and `integrations-proxy-test` job env blocks, moved off the floating `:sidecar` onto the commit tag `:10383276961828bc19f34a9372ba2c64a14e2b62`, plus one comment. Those two jobs boot the calendar PIM and WhatsApp sidecars; neither compiles or executes `internal/channels/telegram/**`. No Telegram job, tag set, fuzz seed corpus or mutation target changed, and `mdv2.go`/`renderer.go` are untouched - so the escape fuzz, the mutation score and the owned-surface coverage this row records cannot have moved. Prior 2026-08-17 **RE-ATTESTED 2026-08-17 (metric-neutral; one `live_e2e`-tagged file added, which no gate compiles).** The matching path is `internal/channels/telegram/**`, and this phase's only edit under it is the addition of `artifact_live_e2e_test.go` - the EXT-005 real-Bot-API delivery receipt, carried uncommitted from an earlier session and landed here rather than dropped. It sits behind `//go:build live_e2e`, a tag that appears in no CI job and in no entry of `AURA_COVERAGE_TAGS` (`scripts/coverage_gate.sh` defaults to `db_integration`), so neither the coverage gate nor the race suite nor the mutation spot-check compiles it. No production file under the path changed: `mdv2.go`, `renderer.go` and the inbound command route are untouched, so the 400-rate, the mutation score and the owned-surface coverage all have no input in the range. Prior 2026-08-16 **RE-ATTESTED 2026-08-16 (metric-neutral; `mdv2.go` and `renderer.go` are untouched).** The matching paths are the operator-profile onboarding flow (`internal/channels/telegram/profile_onboarding.go` plus its two tests and `bot.go`'s wiring) and six lines in `cmd/aura/serve.go` binding the SEP-2322 elicitation consent surface to the channels Registry (plan 45.1-07). This row measures the MarkdownV2 escape fuzz and the `mdv2.go`/`renderer.go` mutation score; NEITHER file appears anywhere in the range, so no escaping rule, no renderer branch and no fuzz corpus changed. The new onboarding messages travel the same renderer, so they inherit the existing escape contract rather than bypassing it. Witnesses: `go build ./...` clean, `go test -race ./cmd/aura/` green (37.2s). Named as NOT re-run: live Bot-API sends and the multimodal sidecars — the measured legs of this row are code-level and are covered above. Prior 2026-08-13 **RE-ATTESTED 2026-08-13 (metric-neutral; one test assertion, no renderer).** The matching path is `internal/channels/telegram/renderer_error_test.go`, and the whole diff is one assertion comparing against `redact.Placeholder` instead of a hard-coded `[redacted]` literal. It went red because the credential redactors were unified onto one table and the marker standardised on the ledger's upper-case spelling; the redaction itself was CORRECT - the rendered error read `Errore: connect failed: postgres://[REDACTED]`, with no DSN credential in it. Nothing this row measures has an input here: no byte of `mdv2.go`, `renderer.go` or any non-test file under `internal/channels/**` changed, so the MarkdownV2 escape fuzz, the mutation legs, the owned-surface coverage, the live Bot-API sends and the inbound command route all keep their prior dates. Fresh witness: `go test -count=1 ./internal/channels/telegram/` green. Prior 2026-08-09 **RE-ATTESTED 2026-08-09 (metric-neutral).** The matching path is `.github/workflows/ci.yml`, and the edit ADDS one job, `ingest-sidecar-test`, which runs the `services/ingest` pytest suite against a live ArcadeDB and EmbeddingGemma. No existing job, step, env var, matrix entry or `needs:` edge was modified, and nothing under `internal/channels/**`, `internal/setup/**` or `cmd/aura/serve.go` changed, so the 400-rate, the mdv2/renderer mutation score, the owned-surface coverage and the live Bot-API sends have no input that could move. Prior **RE-ATTESTED 2026-08-08 (metric-neutral).** The channel's only delta is where an inbound attachment LANDS: `objectstore.AssetKey` moved it out of `identity/<id>/asset/<id>/original` into the identity's own bucket under `chat/<assetID>.<ext>`. The Bot-API send path, MarkdownV2 escaping, the renderer and the inbound command route are byte-identical, so the escape fuzz, mutation score and coverage figures cannot move. Prior 2026-08-06 **RE-ATTESTED 2026-08-06 (metric-neutral).** The sole matching path is `cmd/aura/serve.go`, changed by document-pipeline wiring: the asset-processing worker field is retyped from `runtimeIngestionWorker` to `runtimeProcessingWorkers`, the sandbox router is threaded into `newRuntimeAssetProcessingWorker`, and a `seedPostgresBackup` call is added. No byte under `internal/channels/**` or `internal/channels/telegram/**` changed; `mdv2.go` and `renderer.go` are untouched, as are the escape-fuzz corpus, the Bot-API send path, the multimodal sidecars and the inbound command route. Nothing this row measures has an input in the diff. Prior **RE-ATTESTED 2026-08-03 (metric-neutral).** The sole matching path is `.github/workflows/ci.yml`, where a comment now names the hand-extracted LOCOMO facts and embedding models actually used after deletion of the caller-less spaCy benchmark branch. No executable workflow step, Telegram renderer, MarkdownV2 escape path, Bot-API sender, multimodal sidecar, inbound command route, threshold, or test changed, so none of this row's metrics can move. Prior **RE-ATTESTED 2026-08-03 (metric-neutral).** The matching path is `.github/workflows/ci.yml`, and the only edit in it is a new `apt-get install -y poppler-utils` step added to the unit-test, db_integration and coverage jobs, so the document card's PDF text extractor exists on a hosted runner the way it already does in `docker/aura/Dockerfile`. It installs a binary before the same commands run. No Telegram code, no MarkdownV2 escape, no fuzz corpus, no mutation target, no live Bot-API leg and no threshold in this row is touched by it, so none of its numbers can move. Prior **RE-MEASURED 2026-08-02 (Telegram becomes a wrapper).** Not metric-neutral: this range deletes the channel's private media pipeline, so the row's surface really moved. `documents.go` (the markitdown `/convert` client whose own header admitted the request shape was ASSUMED), `photo.go`, `photo_resize.go` (a byte-for-byte duplicate of `assets.downscaleAssetForVision`, same 1024px/q85 constants) and `voice.go`'s STT client are gone, together with the legacy fallback bodies in `bot_dispatch.go` and the `Deps.DocumentIngest` seam. None of that was reachable: `serve.go` builds `chat.assets` at line 294 and `bootChannelsAndSetup` at 313, so `t.deps.Assets != nil` was always true and every handler returned on the asset branch before the fallback — it was dark code that made the channel look like it still owned an ingestion path. Inbound voice, photos and documents now have exactly ONE path: `assets.IngestTelegramFile` → the modality's shared processor → a turn carrying the attachment block, the same path a cockpit upload travels. **The metrics this row measures split cleanly.** `mdv2.go`, `renderer.go`, the Bot-API send transport and every inbound command ROUTE are untouched, so the 400-rate, the mdv2+renderer mutation score and the live-send assertions have no input that could shift. The **multimodal round-trip DID change** and is restated rather than carried over: the document leg is deleted outright (markitdown lost its last caller and the sidecar is removed from `compose.yaml`/`compose.minipc.yaml` with its `docker/markitdown/` build context and `DOCUMENTS_BASE_URL`), the vision and STT legs moved to `internal/assets`, and the tagged tier now round-trips the one sidecar the channel still owns — TTS→STT, the two test names CI already runs, so no workflow change. Owned-surface coverage was RE-MEASURED, not attested, because ~470 LOC of well-tested source left the package: `internal/channels/telegram` **88.1%** on the `db_integration` tier with **0 skipped tests** (verbose skip audit), against a DISPOSABLE `aura_tgcov` database created and dropped by the run — the live `aura` DB was never touched. The first measurement after the deletions was 84.7%, BELOW the floor, and the gap was closed with real tests rather than a lowered bar: the download-failure leg, the three handlers' malformed-update nil-guards, the unresolved-owner refusal, and the refusal-copy mapping. Behaviour preserved with a named replacement: the >50MB refuse tier is now `assets.Limits` (one ceiling for every channel) surfaced through the new `ErrAssetTooLarge`/`ErrAssetUnsupported` sentinels so the operator still learns WHICH refusal they hit; the 😵 hard-fail reaction stays (it is Bot-API, not STT); and the PRD 2-retry STT backoff moved into `assets.AudioProcessor` — Telegram ingest calls `processAsset` INLINE, so the job worker's minute-scale retry never covered a voice note and without the move the retry would have been silently lost. Witnesses: full untagged `go test ./internal/... ./cmd/...` exit 0; `go build`/`go vet ./...` clean in WSL; `go vet` green under each of the `telegram_integration`, `multimodal_integration`, `live_e2e` and `db_integration` tags; `-race` green on telegram/assets/multimodal/config/documents; `golangci-lint` 0 issues on the touched packages (it caught `downloadFile` going dead, now removed with its file); both compose chains parse. Prior **RE-ATTESTED 2026-08-02 (metric-neutral).** The only path this range matches is `.github/workflows/ci.yml`, and the change there is the removal of the deleted cross-encoder rerank leg: the `AURA_RERANK_BASE_URL` env export and the `go vet -tags rerank_integration ./internal/rerank/` step, both in the Knowledge job. No Telegram job, step, service or environment value moved, and nothing under `internal/channels/telegram/**`, `internal/channels/**`, `internal/setup/**` or `cmd/aura/serve.go` was touched, so MarkdownV2 escaping, Bot-API sends, the multimodal sidecars and the inbound command route have no input that could have shifted. The deleted step vetted a build tag no source file in the tree now carries. Witness: `go build ./... && go vet ./...` clean. Prior **RE-ATTESTED 2026-07-30 (metric-neutral for existing rendering/send metrics).** Matching changes are billed-spend reporting for `/cost`, tenant plumbing/CI environment alignment, and mechanical Go modernizations such as `SplitSeq`; they do not change MarkdownV2 escaping, Bot-API send behavior, multimodal sidecars, or the pre-existing command routes. Fresh unit and race suites passed for `internal/channels/...` and `internal/setup`; the new spend-route tests exercise `/cost`. Prior: **RE-ATTESTED** - the ONLY matching changed path is `.github/workflows/ci.yml`, and the edit is purely additive: one new step in the Memory MCP job that runs the vendored agent-memory pytest suite inside the image that job already builds. No existing job, step order, trigger, env or gate threshold changed, and no Telegram source or test file is in the pushed range (not `internal/channels/**`, not `internal/setup/**`, not `cmd/aura/serve.go`), so MarkdownV2 escape fuzz, `mdv2.go`/`renderer.go` mutation score, owned-surface coverage, the live Bot-API sends and the inbound command route cannot have moved. The push also carries `docker/agent-memory/**` (schema DDL routing) and two new `internal/adaptive/**` test files, neither of which this row's globs match. Prior **RE-MEASURED 2026-07-25** - Phase A Tasks 3-5 consolidate the approval surface inside this package: `scheduled_approval.go` + its test are DELETED (the `aura_sappr` duplicate endpoint), `deliver.go` renders the sweep push through the shared `approvalMarkup`, and `hitl.go`/`bot_dispatch.go` gained the framing-inclusive 64-byte guard, the directive-driven outcome switch, and the non-resolving Dettagli reveal. This is the largest change to this row's surface since the row was created, so it is re-MEASURED, not attested: `internal/channels/telegram` **86.0%** (>=85) on the `db_integration` tier against a disposable DB (`aura_phasea`, created and dropped; the live `aura` DB untouched). `mdv2.go`, `renderer.go`, the Bot-API send transport, multimodal sidecars, and every inbound command ROUTE are untouched — the 400-rate / mdv2+renderer mutation / live-send / multimodal metrics cannot move. The deleted duplicate's still-relevant test (`TestDeliverApprovalTriState`) was preserved into `deliver_test.go`, and a new markup test pins the on-wire budget WITH telebot's framing — the exact check whose absence shipped BUTTON_DATA_INVALID. Witnesses: `go test -race ./internal/channels/telegram/` green under `goleak.VerifyTestMain`; `go build ./...` + `go vet` green. NOT yet live-E2E'd: the appliance container was down (migration-head mismatch) at close. Prior **RE-ATTESTED 2026-07-24** - Phase A Task 2 (SubmitAnswer -> ResolveDirective, commit 58b3c678a) touched `internal/channels/telegram/hitl.go`: the 3 SubmitAnswer call sites now read `directive.Remaining` (behavior-preserving signature migration, callers unchanged); `mdv2.go`, `renderer.go`, the Bot-API send path, multimodal sidecars, and every inbound command route are untouched — metric-neutral, the 400-rate / mutation / live-send / multimodal metrics cannot move. Witness: `go build`/`go vet`/`go test -race ./internal/channels/telegram/` green in a clean HEAD worktree (spike-free verify). Prior **RE-ATTESTED 2026-07-23** - the matching changed path is `cmd/aura/serve.go` (commit a93648c5): a pure mechanical split moved the AG-UI server assembly into the new `cmd/aura/serve_agui.go` (serve.go 600→415 LOC) plus the one-line `SSEHeartbeatSec` wiring. Zero `internal/channels/telegram/**`, `internal/setup/**`, or `internal/channels/**` surface changed — metric-neutral. Witnesses: `go test ./cmd/aura -count=1` + WSL `-race` green. Prior **RE-ATTESTED 2026-07-22** - Phase 39 changes `cmd/aura/serve.go` only to pass the existing database pool into observability bootstrap so the durable retention-backlog gauge can be registered and stopped with the daemon. Telegram MarkdownV2 rendering, Bot-API send, multimodal sidecars, and inbound command routing are untouched, so the row of record stands metric-neutral. Fresh witnesses: `go test ./... -count=1`, `go build ./...`, `go vet ./...`, Linux `-race` for the affected daemon boundary, and the complete disposable Postgres/Neo4j matrix at **85.4%**. Prior **RE-ATTESTED 2026-07-21** - Amendment #87 rewires Telegram onboarding off Agent.md onto the memory MCP: `profile_onboarding.go` `writeCompleted`/`writeSkipped` now take ctx and call `StoreConfirmed`/`StoreSkipped`, `bot.go` `Deps.Profile` is the shared `onboarding.ProfileMemoryStore` port, and the onboarding-dispatch tests use an in-memory `fakeMemoryStore`. `mdv2.go`, `renderer.go`, the Bot-API send path, multimodal sidecar wiring, and every inbound command ROUTE are untouched — only the onboarding STORAGE backend swapped, so the 400-rate / mutation / live-send / multimodal metrics cannot move. `internal/channels/telegram` re-measured **86.1%** (owned-surface >=85%) on the full db+neo4j matrix (`scripts/coverage_docker.sh`). Prior **RE-ATTESTED 2026-07-20** - The compaction-engine removal (Amendment #86) deletes the `/compact` slash-command added on 2026-07-14: `compact.go`+`compact_test.go` gone; the `Compact compactService` dep + `case "/compact":` branch removed from `bot.go`/`bot_dispatch.go`/`commands.go` (plus the now-dead `turnInFlight` helper and the menu/help entries); `commands_test.go` back to 12 commands. `cmd/aura/serve.go` drops the compaction evaluator goroutine + `SetCompactService`/`SetCompactionMemoryStore`; `.github/workflows/ci.yml` drops the three Phase-42 jobs + re-scopes the race/leak DB tier. `mdv2.go`, `renderer.go`, the Bot-API send path, multimodal sidecar wiring, and every EXISTING command route are untouched — the exact inverse of the 2026-07-14 addition below; owned-surface coverage is re-gated by `scripts/coverage_gate.sh` on every run. Local witness: `go build ./... && go vet ./...` green; `go test ./internal/channels/telegram/...` green. Prior **RE-ATTESTED 2026-07-18** - 37F-18's matching changed path is `cmd/aura/serve.go`; its delta is share-service composition-root wiring only (`buildShareService` (share_service_wiring.go) + `aguiServer.SetShareService` + `chat.run.SetShareRevoker` + a `seedShareExpirySweep` call) — zero `internal/channels/telegram/**`, `internal/setup/**`, or `internal/channels/**` edits. `mdv2.go`/`renderer.go`, the Bot-API send path, multimodal sidecar wiring, and every existing command route are untouched (`git diff --stat` confirms no changes under any of the three channels globs). Local witness: `go build ./... && go vet ./...` clean; `go test -race ./internal/channels/telegram/...` unaffected (not re-run — no source in that tree changed). Prior **RE-ATTESTED 2026-07-14** - Phase 42 (LLM conversation compaction) added a new `/compact` slash-command: `bot.go`/`bot_dispatch.go`/`commands.go` wire a `Compact compactService` dep and one new `case "/compact":` branch into `dispatchRich`, and `compact.go`+`compact_test.go` implement the handler; `commands_test.go` gained matching coverage. `mdv2.go`, `renderer.go`, the Bot-API send path, multimodal sidecar wiring, and every EXISTING command's routing are untouched — purely additive (`git diff --stat`: 0 deletions in `bot.go`/`bot_dispatch.go`, dispatch-table only in `commands.go`). `cmd/aura/serve.go` gained the compaction rollout evaluator goroutine + `SetCompactService` AG-UI wiring, unrelated to Telegram. Local witness: `go build ./... && go vet ./...` green; `go test -race ./internal/conversations/compaction_eval/...` green (42-10). Prior **RE-ATTESTED 2026-07-11** - Phase 37E (composer reasoning-effort selector) touched `cmd/aura/serve.go` only to call `wireReasoningCapabilities` (boot-warm the reasoning-capability source + inject it into the agui Server); zero `mdv2.go`, `renderer.go`, Bot-API-send, multimodal-sidecar, or inbound-command production surface changed — no `internal/channels/telegram`, `internal/setup`, or `internal/channels` file is in the 37E range, so the Telegram metrics cannot move. Local witness: `go test ./cmd/aura -count=1` green. Prior **RE-ATTESTED 2026-07-09** - Phase 37C (web voice lane) touched `cmd/aura/serve.go` only for web-voice composition-root wiring (the `wireVoiceProviders` call + relocating `seedSkillTTLSweep` into `serve_provisioning.go` for the 600-LOC cap); zero `mdv2.go`, `renderer.go`, Bot-API-send, multimodal-sidecar, or inbound-command production surface changed — the web lane calls `multimodal.TTSClient`/`STTClient` but does NOT modify `internal/multimodal` or `internal/channels/telegram`, and Telegram's opus TTS path is untouched. No `internal/channels/telegram` file is in the pushed range. Prior **RE-ATTESTED 2026-07-08** - the matching changed paths are `internal/channels/telegram/artifact_test.go` + `cot_live_e2e_test.go` (tests) and `cmd/aura/serve.go` (37A `send_file` ingest composition-root wiring); zero `mdv2.go`, `renderer.go`, Bot-API-send, multimodal-sidecar, or inbound-command production surface changed. Local witness: `go test ./internal/channels/telegram/` green (6.7s). Prior **RE-ATTESTED 2026-07-06** - Phase 36 gap-closure (36-17) hardened Telegram turn-scoping fail-closed (bot_dispatch_turn.go/bot_dispatch_auth.go drop an unlinked/blank-principal turn instead of falling back to the seeded local admin) + added bot_dispatch_failclosed_test.go; mdv2.go, renderer.go, Bot-API send, multimodal sidecar, and inbound-command route behavior are unchanged (the measured mutation/400-rate/live-send surface). internal/channels/telegram coverage re-measured 86.1% (owned-surface gate 85.9%), >=85, via scripts/coverage_gate.sh (db+neo4j). Local witnesses: go test -race ./internal/channels/telegram/ green; golangci-lint 0. Prior **RE-ATTESTED 2026-07-03** - Phase 35 (35-05) touched `cmd/aura/serve.go` only to wire the gateway crash-orphan reconciler (construct + Start/Stop) and to relocate cron-dispatch assembly into `serve_dispatch.go`/`serve_drain.go` (behavior-preserving split for the 600-LOC cap); `internal/channels/telegram/**`, `internal/setup/**`, and `internal/channels/**` were NOT touched, so `mdv2.go`, `renderer.go`, Bot-API send, multimodal sidecar, and inbound-command route behavior are unchanged. Local witnesses (WSL2): `make quality` green (full `-race` unit suite `go test -race -count=1 $(bash scripts/go_packages.sh)` incl. `internal/channels/telegram`, plus build/vet/file-size/`golangci-lint` 0 issues/deadcode/govulncheck); owned-surface coverage 86.1% >= 85% via `scripts/coverage_gate.sh` (db+neo4j, mcp-neo4j-cypher containerized). Prior **RE-ATTESTED 2026-06-29** - onboarding/auth/bootstrap/channel setup touched `cmd/aura/serve.go` and frontend flows, including Telegram onboarding visibility; no `mdv2.go`, `renderer.go`, Bot-API send, multimodal sidecar, or inbound-command route behavior changed. Local witnesses: `go test -count=1 ./cmd/aura`, targeted onboarding/auth Vitest coverage; full repo gates rerun before push. Prior **PASS** — re-baselined 2026-06-28 (Phase-30 retrieval-hardening RET-05 touched `.github/workflows/ci.yml` to add a rerank/retrieval `go vet` compile-floor + `AURA_RERANK_BASE_URL` export; zero `mdv2.go`/`renderer.go`/Bot-API-send/multimodal/inbound-command production surface changed, so the measured Telegram metrics cannot move. Local witness: `go test -race ./internal/documents/ ./internal/eval/ -count=1` green; `go vet -tags telegram_integration ./internal/channels/telegram/` compiles). Prior 2026-06-21 (Calendar-connect patch touched `cmd/aura/serve.go` only to mount the authenticated PIM admin proxy; zero `mdv2.go`/`renderer.go`/Bot-API-send/multimodal/inbound-command production surface changed. Local witnesses: `go test ./internal/agui ./internal/config ./cmd/aura -count=1` and `npx vitest run src/governance/__tests__/CalendarConnect.test.tsx src/governance/__tests__/governanceApi.test.ts src/governance/__tests__/SkillsBoard.test.tsx --coverage.enabled=false`). Prior 2026-06-20 (Phase 28 governance/onboarding follow-up touched `cmd/aura/serve.go` for provisioning-only Authula wiring; zero `mdv2.go`/`renderer.go`/Bot-API-send/multimodal/inbound-command production surface changed. Local witnesses: `go test ./...`, `docker compose up -d --build aura`, and a live authenticated onboarding provision probe returned HTTP 200 with Telegram deep-link + QR after the Telegram channel started). Prior 2026-06-19 (CI workflow-only Garage bootstrap for browser asset-upload E2E; run 27812067983 already passed the Telegram compile/no-skip floor and live multimodal audio, and local 2026-06-19 live multimodal sidecars passed forced -count=1; zero mdv2.go/renderer.go/Bot-API-send/multimodal/inbound-command production surface changed). Prior 2026-06-18 (CI-only Node 24 workflow update: `setup-node@v6` + `paths-filter@v4` with `AURA_CI_NODE_VERSION=24.x`; current run 27745323083 passed the Telegram compile/no-skip floor and live multimodal audio; zero `mdv2.go`/`renderer.go`/Bot-API-send/multimodal/inbound-command production surface changed). Prior 2026-06-17 (test-only touch: rewrote the timing-dependent CoT-posture test `TestHandleTurnReasoningPostureFollowsConfig` to the deterministic throttle=0 direct-pane mechanism `cot_live_e2e_test` uses — synthetic events fire sub-1ms, so the 1ms-throttle live-CoT frame was coalesced away and RunFinished then reset the FIFO to "completato", making the assertion flaky/red on CI; zero production `mdv2.go`/`renderer.go`/Bot-API-send/multimodal/inbound-command surface changed; the full `internal/channels/telegram` package passes `-race`). Prior 2026-06-16: run 27617744760 passed the Telegram integration compile/no-skip floor and the live multimodal audio tier; the touched owned paths were CI wiring and `cmd/aura/serve.go` web-shell mounting, not `mdv2.go`, `renderer.go`, Bot-API send rendering, multimodal command logic, or inbound command routing. Prior 2026-06-15: CI-config touch only — an `AURA_ACCESS_TOKEN` CI placeholder added to `.github/workflows/ci.yml` (Phase-17 caddy interpolation), zero mdv2/renderer/Bot-API-send/multimodal/inbound-command surface. Prior 2026-06-14: Phase 14 onboarding enrichment shipped (5-step LLM-extraction interview + 8-section Agent.md extractor; `internal/onboarding`, `internal/profile`, `internal/channels/telegram` onboarding dispatch); live operator sign-off 2026-06-14 (Davide /onboard → Conferma → Agent.md written, onboarding_completed:true; internal/onboarding 90.4%, internal/profile 89.5%). Zero `mdv2.go`/`renderer.go`/Bot-API-send/multimodal/inbound-command rendering surface changed. Local `go test ./...` passed; full untagged suite green; golangci-lint 0 issues. Prior 2026-06-14: the changed Telegram-owned paths were onboarding/profile dispatch plumbing and tests (`bot.go`, `bot_dispatch*.go`, `profile_onboarding.go`, `profile_onboarding_test.go`, auth/onboarding dispatch tests), local `go test ./...` passed, CI run 27496009898 compiled the Telegram live Bot-API tier successfully. Historical mutation/coverage stay green: `mdv2.go` 81.8%, `renderer.go` 74.1%, owned-surface refreshed in Phase 20 at **85.9%**. | Phase 13 Slice 9 | `internal/channels/telegram/**`, `internal/setup/**`, `internal/channels/**`, `cmd/aura/serve.go`, `.github/workflows/ci.yml` |
| Web tools — `ssrf.go` mutation (go-mutesting, >=70% killed) + `internal/web` coverage (>=85% combined) + live `web_search` p95 (<=2s) | mut >=70% / cov >=85% / p95 <=2s | 2026-08-03 | **RE-ATTESTED 2026-08-03 (latency RE-MEASURED; remaining legs metric-neutral).** The inherited local range changes one `web_search.Spec` example string from a retired database query to `orbital period of Titan`; its unit and live fixtures likewise replace Neo4j words with ArcadeDB. No SSRF policy, `ssrf.go`, request path, retry cap, ranking, SearXNG setting, or result contract changed, so the 94.4% mutation and 85.9% combined-coverage records cannot move. The query fixture can affect upstream timing, so latency was re-measured rather than asserted: **n=20**, p50 **0.354s**, p95 **0.993s**, max **1.933s**, all within the 2s gate. Witness: real `web_integration` race tier green in 2.344s. Prior **RE-ATTESTED 2026-07-30.** The only matching production change is the exact Go modernization of bounded retry/redirect loops from three-clause integer loops to `range N`; SSRF policy, targets, caps, ranking, and request count are unchanged. Unit and race suites passed, and the live `web_integration` race tier passed against SearXNG in 3.216s. Historical mutation 94.4%, coverage 85.9%, and p95 0.55s cannot move from this syntactic equivalence; no new latency measurement is claimed. Prior: **RE-ATTESTED 2026-07-27** - searxng image bump 2026.7.19->2026.7.26 (compose pin + the `settings.yml` "Source:" header comment; `use_default_settings` inherits the new engine catalogue, so no key in this file changed). Zero `internal/web` or `internal/agent/tools/web_*.go` production code touched - the `ssrf.go` mutation score (94.4%) and `internal/web` coverage (85.9% combined) cannot move, by construction. Live latency RE-MEASURED against the NEW image on the running stack: n=20 distinct JSON searches over the loopback publish, p50 0.44s, p95 0.55s, max 0.55s, all <= the 2s gate; the image was also booted standalone against this exact `settings.yml` before the stack was converged (healthz + ranked JSON results OK). CI `web_integration` re-runs the live smoke on this push. Prior: **RE-ATTESTED 2026-07-22** - searxng image bump 2026.5.31->2026.7.19 (compose pin + settings.yml source comment only; use_default_settings inherits the new engine catalogue). Zero `internal/web` or `internal/agent/tools/web_*.go` production code changed - ssrf.go mutation (94.4%) and coverage (85.9%) unmoved by definition. Live latency re-measured against the NEW image: n=20 JSON searches, p50 0.48s, p95 1.0s <= 2s gate; CI web_integration tier re-runs the live smoke on this push. Prior: **RE-ATTESTED 2026-07-06** - Phase 36 gap-closure (36-18 CI hardening) wrapped the `TestSearch_Live` web_integration smoke set in a bounded 3-attempt retry with backoff + one high-recall insurance query, after CI run 28796539832 flaked "zero results for all smoke queries" while the immediately-prior run passed the byte-identical `internal/web` tree (transient SearXNG upstream throttle of the CI datacenter IP, not a regression). Zero `internal/web/ssrf.go`, `internal/agent/tools/web_*.go`, fetch policy, or result-ranking production code changed — the `ssrf.go` mutation (94.4%), `internal/web` coverage (85.9% combined), and live `web_search` p95 metrics are unaffected. This is NOT skip-as-green: every retry is a real live call, and a genuine json-format/deadline contract break still returns zero/err on every attempt and fails deterministically. Local witness: `go vet -tags web_integration ./internal/web/` green; the tier compiles under the tag. Prior **RE-ATTESTED 2026-06-29** - SearXNG/container setup changed for generated-key and limiter bootstrap; no `internal/web/ssrf.go`, `internal/agent/tools/web_*.go`, fetch policy, or result-ranking code changed. Local witnesses: `bash scripts/check-file-size.sh`, full repo Go/web gates rerun before push. Prior **PASS** - `go test -race -count=1 -tags web_integration ./internal/web/` green; `scripts/web_search_smoke.sh` green with 3 ranked SearXNG results; `scripts/ssrf_smoke.sh` PASS under WSL. Historical ssrf.go mutation 94.4% remains the mutation witness. Owned-surface coverage refreshed at **85.9%**. | Phase 7 Slice 5 | `internal/web/**`, `internal/agent/tools/web_*.go`, `searxng/settings.yml` |
| Skills system (CAP-07 / CAP-08 / D-35, post-#51 shape) — xlsx North-Star E2E + tiers + mutation + coverage | E2E judge >=0.90 over 2 dims (capability_gap_recognition + skill_output_quality) + artifact-not-reply (FRESH .xlsx exists/opens/today) + structured-arg self-install evidence (`npx skills add` against a concrete xlsx/excel/spreadsheet-capable source) / unit+db+sandbox tiers green / validator.go hard mutation >=70% and writer.go advisory / combined coverage >=85% / SC#1-SC#4 true (SC#5 superseded by #51) | 2026-08-23 | **RE-ATTESTED 2026-08-23 (metric-neutral; one PURE EXTRACTION in the skills package, one ADDITIVE tool elsewhere).** Two paths under this row's globs changed. (1) `internal/skills/frontmatter.go` exports `SplitFrontmatter`: the CRLF normalization that lived in `parseFrontmatter` moved INTO the splitter and `parseFrontmatter` now calls it. Same bytes in, same bytes out, one call deeper — a pack's `commands/*.md` opens with the identical fence a SKILL.md does, and re-implementing that rule would have been a second, drifting reading of the same convention. No loader rule, validator, writer, audit path, snippet runtime or xlsx flow is in the diff. (2) `internal/agent/tools/pack.go` is a NEW read-only deferred tool (`plugin_pack`); it adds a file to that directory but touches no `skill*.go`, and a tool that resolves a public git repository has no input to anything this row measures. Coverage re-measured rather than asserted, untagged `-count=1`: `internal/skills` **84.4%**, `internal/agent/tools` **86.3%** — the skills figure is the same untagged number recorded on 2026-07-31 (84.8%) and 2026-07-28 (84.4%); the gate's own `db_integration` tier, which is the stricter one the 85% floor reads, was not re-run here. Witnesses: `go build ./...` clean; `go test -count=1 -race ./internal/skills/ ./internal/agent/tools/ ./cmd/aura/` green in WSL (Linux, the environment Aura actually runs in); `golangci-lint run ./internal/packs/... ./internal/agent/tools/... ./cmd/aura/...` **0 issues**. The E2E, mutation and tagged-coverage legs keep their prior dates: none was re-run in this range. Prior**RE-ATTESTED 2026-08-16 (metric-neutral; the single matching path is a test file).** The only path in this range under this row's globs is `internal/skills/audit_store_integration_test.go`. No skill runtime file, no `internal/agent/tools/skill*.go`, no `.github/workflows/skills.yml` and no `scripts/cache_invariant_audit.sh` changed, so the xlsx North-Star E2E, the tier matrix, the mutation score and the owned-surface coverage this row measures cannot have moved — a build-tagged integration test does not alter the surface it measures. Witness: `go build ./...` clean at this HEAD. Prior 2026-08-13 **RE-ATTESTED 2026-08-13 (the catalogue MOVED position in the prompt; the skills runtime is byte-identical; the live E2E was NOT re-run).** `internal/agent/tools/skill.go` stops building its `Description` from the loader: the read verb now carries a constant frame and is `Deferred: false`, while the installed-skill catalogue is rendered - by the SAME `skilladapters` renderer `action=list` already uses - into the `messages[1]` always-block by `cmd/aura/serve_adapters.go`. Nothing about what a skill IS, or how it loads, validates, writes or executes changed: `internal/skills/**` has no byte in this diff, so the tiers, the `validator.go`/`writer.go` mutation legs and the coverage floor have no input here and keep their prior dates. What DID change is WHERE the catalogue sits, and that is the point: it leaves the `tools` array - which was re-serialised on every skill install, breaking the cache prefix for the whole turn - and enters a position Aura already rebuilds per turn, so the catalogue becomes byte-stable across turns and the read verb is callable without a `tool_search` round-trip. Measured against the live loader with 11 skills installed: the default manifest goes **4,237 -> 4,660 tokens (11 -> 12 tools)**, i.e. `skill` now costs a constant **~423 tokens** instead of the **~1,773** it cost when promoted. Stated plainly because it is easy to misread: this does NOT save tokens - the catalogue's weight moves to the always-block - it buys cache stability and always-visibility. Three tests changed deliberately, none weakened: the byte-stability test is INVERTED (it now asserts the catalogue is ABSENT from the tool description), `TestSkillDescriptionIgnoresTheLoader` is new, and `always_active_test.go`'s `want` is extended exactly as its own failure message invites ("if it is genuinely structural, extend want and say why"). Fresh witnesses: `go build ./...` and `go vet ./cmd/aura/ ./internal/agent/tools/` clean; `go test -count=1 ./cmd/aura/ ./internal/skilladapters/ ./internal/gateway/` green. The `internal/agent/tools` leg fails locally ONLY on `TestStageBoxArtifact_ExtractsRegularFile` (staged mode `-rw-rw-rw-` vs 0600) - a Windows-host POSIX-mode artifact in `send_file_sandbox_test.go`, a file this range does not touch, and the same file passes the Linux `Unit tests (race detector)` tier on PR #43. Prior same-day (PR #43, merged into master after this branch forked) **RE-ATTESTED 2026-08-13 (metric-neutral).** The only matching change in this range is the export rename `shellQuoteArg` -> `ShellQuoteArg` — one call site in `write_file.go`'s in-box sha256sum verification and one word in a `skill_read_test.go` comment — made so the verification ledger's box project probe shares ONE quoting rule instead of copying it. The function body, its output and every caller's behaviour are byte-identical; no skill loader, validator, writer, audit ledger, snippet runtime or xlsx path is in the diff. Witness: full untagged `go test ./...` green in WSL (Linux, the environment Aura actually runs in) on the merge commit. Prior 2026-08-12 **RE-ATTESTED 2026-08-12 (tiers verified; the TOOL SURFACE CHANGED and the live E2E was NOT re-run).** `internal/agent/tools/skill*.go` splits the ten-action `skill` verb into a read-only `skill` (list/info/use) and a Mutating+Multiplexed `skill_manage` (create/update/delete/install/save_snippet/restore/archive); the gateway classifier moves to the new name and the reads drop out of `skillFixedTiers` as dead entries. This is a behaviour change to what the model sees, NOT a cosmetic one: any flow that AUTHORS or INSTALLS a skill now names `skill_manage`, so the North-Star E2E's self-install leg would issue a different tool call — and it was not re-run (its harness was deleted with `internal/eval`, and no live stack was available here). What WAS verified, in code and by test: the risk tiers do not move — the reads were ALREADY pinned to `scoring.Safe`, and classify returns Safe for a non-Mutating tool, while delete/create/update/install keep Destructive/Risky through `classifySkill` on the new name, with `ValidateClassifiable` still demanding that classifier. ~40 tests failed on the split and were RETARGETED, not weakened, including `TestApproveIsHostSideOnly`, which correctly caught that `delete` was ungated while its fixture still said `"skill"`. Measured effect on the manifest: promoting the read verb now costs 590 tokens instead of 1081 (-45%); both halves together cost the same 1081 as before. Fresh witnesses: `go build ./... && go vet ./...` clean; full untagged `go test ./...` green; `go test -race ./internal/agent/... ./internal/gateway/ ./cmd/aura/` green; gofmt clean. The mutation and tagged-coverage legs keep their prior dates — neither was re-run in this range. Prior 2026-08-08 **RE-ATTESTED 2026-08-08 (metric-neutral).** The matching path is the shared `cmd/aura` composition root, where the change is object-store wiring (an ensuring resolver, the file-manager seams). No skill loader, validator, stage reader, audit ledger or snippet runtime was touched, so the E2E judge score, tiers, mutation and coverage cannot move. Prior 2026-08-06 **RE-ATTESTED 2026-08-06 (metric-neutral).** The matching path is `.github/workflows/skills.yml`, and the edit adds three environment variables (`AURA_DOCLING_API_KEY`, `AURA_EMBED_REVISION`, `AURA_EMBED_FINGERPRINT`) so that `docker compose up postgres` can interpolate the file at all. They were made `:?`-required on the aura and aura-docling services by the document-pipeline convergence commit, and compose interpolates the whole file before selecting a service, so this job was failing at bring-up before any tier executed. No test selection, tag matrix, mutation target, fuzz corpus, coverage threshold or byte under `internal/skills/**` changed; the job now reaches the tiers it always claimed to run. Nothing this row measures has an input in the diff. Prior **RE-ATTESTED 2026-08-03 (metric-neutral).** The matching path is `.github/workflows/skills.yml`, and the only edit in it is a new `apt-get install -y poppler-utils` step placed before the coverage gate, so `internal/documents/filecard`'s PDF extraction path is installed rather than absent when the gate measures it. It adds a binary to the runner; the skills tiers, the fuzz target, the `validator.go`/`writer.go` mutation spot-check and every threshold this row carries run the same commands on the same code, so none of its numbers can move. Prior **RE-ATTESTED 2026-08-02 for the tiers; THE LIVE xlsx NORTH-STAR E2E LEG NO LONGER HAS A HARNESS.** Two changes match. (1) `internal/agent/tools/skill*.go`: `SkillTool.Adaptive` and `orderSkillList` are gone and `actionList` now calls `rankSkills(t.Loader.List(), query)` directly. This is ordering-neutral by construction — the adaptive plane sat at policy `off`, where `OrderSkillRouting` fell through to the same `rankSkills` static branch, and the wrapper's only other outcome was the identical static fallback on an invalid ordering. (2) `.github/workflows/skills.yml`: comment text only, no step, tag, env or job changed. The honest loss is D-35: `internal/eval/skills_cot_eval_test.go` is DELETED with the rest of `internal/eval`, so the paid live xlsx North-Star E2E that backed part of this row can no longer be reproduced, and that glob is removed from the CI-gate path below. The unit/db_integration/mutation/coverage tiers are unaffected. Witness: `go build ./... && go vet ./...` clean. Prior **TIERS RE-MEASURED 2026-07-31; THE LIVE xlsx NORTH-STAR E2E WAS NOT RE-RUN.** `internal/agent/tools/**` changed substantially (tool_search resolves by name and ranks lexically; the embedding plane is deleted). The skills-facing part is one line in `skill_read.go`: `rankSkills` now projects a skill's description into the Spec field the shared ranker indexes, because the retrieval document moved from Description to Summary — same text, same BM25, same ordering contract. The shared tokenizer also now drops English function words and folds regular plurals, which applies to skill ranking as well and is asserted by the skills tests. Fresh witnesses: `go test -race -count=1 ./internal/skills/... ./internal/agent/tools/... ./internal/skilladapters/...` green; coverage `internal/agent/tools` 86.3%, `internal/skills` 84.8% (untagged, -count=1); golangci-lint 0 issues. The North-Star E2E and mutation legs keep their prior dates: neither was re-run in this range. Prior **RE-ATTESTED 2026-07-30 (metric-neutral; the prior E2E and mutation legs remain DUE).** The only matching change is `.github/workflows/skills.yml` quoting `AURA_AGENT_MEMORY_MCP_AUTH_SECRET`: YAML previously coerced the numeric-looking `...e5` placeholder to `0`. No Skills source, test command, tag, mutation threshold, coverage threshold, or xlsx flow changed. Fresh WSL witness: `go test -race -count=1 ./internal/skills/... ./internal/agent/tools/... ./internal/skilladapters/...` green. This attestation deliberately does not promote the already-recorded due xlsx/mutation legs to green. Prior **PARTIALLY RE-ATTESTED 2026-07-28 - the E2E and mutation legs are DUE.** This range deletes the skills pending stage, makes the delete route actually delete, moves installation through the tool, adds the cockpit skills board/editor, and embeds one new builtin (`memory-aura`). That is behaviour, not cosmetics, so the xlsx North-Star E2E, the mutation spot-check and the tagged coverage legs must be re-run and are NOT re-run here. Re-measured today, untagged: `go test ./internal/skills/` green at 84.4% of statements (UNTAGGED - the Skills gate adds `db_integration`, and that stricter number is what the 85% floor reads); `internal/agent/tools`, `internal/skilladapters` and the `cmd/aura` skills CLI surface green. The single failure in that `cmd/aura` run is `TestWriteAdaptiveBenchmarkReportCreatesPrivateNonReplacingArtifact` (artifact permissions=0666) - a Windows-host artifact on a file this range never touches, not a regression. Prior **RE-MEASURED 2026-07-27** - this range edits `validator.go`, which this row's threshold names explicitly, so the number was re-measured rather than argued: `go-mutesting internal/skills/validator.go` in WSL scored **1.000 (29 passed / 0 failed / 29 total)**, above the >=70% hard floor and unchanged from its prior perfect score — the new branch (an empty/whitespace description is refused at the write boundary, matching the loader's rule) arrives with a test that kills every mutant of it, including the trimmed-vs-exact comparison that the loader change exists to keep in sync. The other matching paths are `internal/agent/tools/skill_read.go` (the `use` frame drops the clause naming `sandbox_exec`, deleted 2026-06-10 — amendment #96) and `skill.go` (schema wording: description is required, not optional, for save_snippet). The xlsx North-Star E2E cannot move on these: the removed clause named a tool the registry does not serve, so it could only ever have cost the model a failed call, and the description rule rejects at save a shape the loader already skipped at load. Live witness of the runtime under the new frame (strict profile, in-box path): `save_snippet` -> approve -> `use` -> `shell_exec python3 '/skills/sommadue/sommadue.py'` -> `42`, exit_code 0, 86ms — see the snippet-reuse row below for the full ledger window. Prior **RE-ATTESTED 2026-07-26** - the matching changed paths are `internal/agent/tools/skill.go` and `skill_read.go`, which add a `SkillRoutingControl` seam: `action=list` ordering now goes through `orderSkillList` instead of calling `rankSkills` inline. The seam is inert in this deployment — `adaptive_skill.go:101` returns the static `rankSkills` order verbatim when `t.Adaptive == nil`, and grep over `internal/` and `cmd/` finds no production assignment to that field (test doubles only). Even when wired, an order failing `validOrderedIDs` falls back to static. So list ordering is byte-identical to the measured run, and skill execution, tiers, mutation score and coverage are untouched by these two files. Prior **RE-ATTESTED 2026-07-22** - dependabot bump actions/setup-go 6->7 (skills.yml): CI toolchain installer action only, Go version still resolved from go.mod, no test-surface or gate change - metric-neutral, numbers cannot move. Prior: **RE-ATTESTED 2026-07-21** - Amendment #87 relocates the per-identity path-traversal guard (D-20/D-21) into `internal/idroot`; `internal/skills/identity_root.go` now delegates to it (behavior-identical move; `internal/idroot` re-measured 85.7%). Zero `validator.go`, writer, audit-store, snippet-runtime, or xlsx-North-Star behavior changed — the E2E judge + tier + mutation evidence stand. `internal/skills` re-measured **88.1%** (combined >=85%) on the full db+neo4j matrix (`scripts/coverage_docker.sh`). Prior **RE-MEASURED 2026-07-17** - Fast catalog search added a bounded direct `skills.sh` API client plus timeout-bounded CLI fallback, singleflight coalescing, a 60-second/128-entry LRU cache, cancellation-safe waiters, and the governance UI debounce/cancellation path (`internal/skills/catalog_search.go`, `installer.go`, and focused tests). The full `db_integration neo4j_integration` matrix reached `internal/skills` **88.1%** coverage (PASS, >=85%); `go-mutesting internal/skills/catalog_search.go` scored **80.56%** (58/72 killed, >=70%); package unit, WSL `-race`, vet, build, and the full untagged WSL suite are green. The matrix runner's only overall failure was the unrelated pre-existing `TestMigration0039RoundTrip` asserting version 39 after migration 0040 had landed; the `internal/skills` leg itself passed. `validator.go`, writer/audit, snippet runtime, and the xlsx North-Star path are untouched, so their prior behavioral evidence stands. Prior **RE-ATTESTED 2026-07-14** - Phase 42 (LLM conversation compaction, 42-10 phase-close lint gate) added ONE `//nolint:staticcheck` comment pair to `internal/skills/smoke_test.go` (the pre-existing `if haiku == nil { t.Fatalf(...) }` guard, an SA5011 false positive unrelated to any Phase 42 change — staticcheck's SA5011 does not always recognize `t.Fatalf` as execution-terminating, so it flags the subsequent `haiku.Always` dereference; this file was last behaviorally touched in Phase 11 and carries zero diff across the 42-01..42-10 range before this lint-only edit). Zero skill validator (`validator.go`), writer, audit-store, snippet-runtime, or xlsx-North-Star behavior changed — the smoke assertions (haiku loaded, `Always=true`, `RenderAlwaysBlock`) are byte-identical. Local witness: `go build ./internal/skills/... && go vet ./internal/skills/... && go test -count=1 ./internal/skills/...` green; `golangci-lint run ./internal/skills/...` 0 issues. Prior **RE-ATTESTED 2026-07-10** - Phase 37D (composer skill-picker) touched `internal/agent/tools/skill_read.go` — EXPORTED the load-bearing `useAuthorityFrame` const to `UseAuthorityFrame` (byte-identical value) so the 37D composer Mechanism A reuses the EXACT same authority frame the `use` action emits (DRY — the string IS the contract), with the two internal call sites (`actionUse`/`renderSnippetUse`) updated to the exported name — plus `skill_test.go` (test); zero skill validator (`validator.go`), writer, audit-store, snippet-runtime, or xlsx-North-Star behavior changed, so the E2E judge + tier + mutation + coverage evidence stand — metric-neutral. Local witness: `go test -count=1 ./internal/agent/tools/ ./internal/skills/` green. Prior **RE-ATTESTED 2026-07-06** - Phase 36 gap-closure (36-18) added `AURA_GARAGE_ADMIN_TOKEN: ci-not-a-secret` to `.github/workflows/skills.yml` so the Skills CI workflow's compose bring-up carries the required Garage admin token (mirrors the main CI workflow env); zero skill runtime, validator, writer, audit store, snippet execution, or xlsx North-Star behavior changed — a CI-env-only touch. Local witness: `docker compose config` renders with the token set; the Skills (CAP-07/CAP-08) workflow stayed green. Prior **RE-ATTESTED 2026-06-29** - CI-only compose-env placeholder patch for generated Authula/PIM/SearXNG/Garage objectstore secrets after the Skills workflow failed before Postgres startup at compose interpolation time; no skill runtime, validator, writer, audit store, snippet execution, or xlsx North-Star behavior changed. Local witness: `docker compose --env-file <ci-placeholder-env> config --quiet` passes, plus prior same-day `go test -count=1 ./...` and web gates remain green. Prior **PASS** — re-measured 2026-06-21 after CodeQL hardening in `internal/skills`: direct writer path sinks now reject non-local/non-grammar names at the FS boundary and audit list limits convert through the int32 clamp helper; focused boundary tests plus full `go test ./internal/skills -count=1` PASS. Prior 2026-06-15: CI-config touch only — an `AURA_ACCESS_TOKEN` CI placeholder added to `.github/workflows/skills.yml` (Phase-17 caddy interpolation), zero skill-runtime/validator/writer surface. Prior 2026-06-13 (coverage campaign): the only owned-path touch was test-only fake/unit tests added to `internal/skills` (audit_store/writer/snippet pre-tx branches) — no skill-runtime/validator/writer behavior change, so the xlsx North-Star E2E judge + tier evidence stand; `internal/skills` package coverage rose 81.7→**87.8%** and overall owned-surface to **90.3%** (gate). Prior: **PASS** - CI run 27408061937 (commit c413b9ef): unit/race, 60s `FuzzSkillValidator`, db_integration, `TestSnippetExec`, go-mutesting, and coverage gate all green. Owned-surface coverage **85.1% >= 85%**; validator.go mutation **1.000** hard PASS; writer.go **0.547 advisory** accepted as db-subprocess artifact with DB tests as witness. Local unit package coverage raised to **62.5%** via host-invocation, TTL/staleness, content-hash, promote-dir, and audit-action tests. Prior chat-surface gate remains **PASS 6/6 = 100%**; structural guard remains **PASS** (`TestSkillsE2E`, judgeMean=1.00, selfInstall=true target=true selector=true, fresh/openable/today xlsx). | Phase 11 Slice 7 (7a-7g) | `internal/skills/**`, `internal/agent/tools/skill*.go`, `.github/workflows/skills.yml`, `scripts/cache_invariant_audit.sh` |
| Swarm E2E (CAP-03 / SC#5 / D-22) — autonomous parallelization (≥2 workers, natural prompt) + mail+WhatsApp MCP read-back + timing <1.5× + judge ≥90% | ≥2 workers / facts present / mail+WA read-back / <1.5× / judge ≥0.90 / no-over-spawn | 2026-08-07 | **RE-ATTESTED 2026-08-07 (comment-only; provably metric-neutral).** The only change under this row's globs is five comment lines in `internal/agent/tools/swarm_spawn.go`'s `Spec()`. `Deferred` was flipped to `false` while the always-active set was being aligned to the reference coding agent's own tools, then flipped back, so the emitted Spec is byte-identical and `internal/swarm/**` is untouched — `git diff origin/master..HEAD -- internal/agent/tools/swarm_spawn.go internal/swarm/` reports 6 insertions / 1 deletion, all comment. The comment records WHY the promotion was reverted, because the reason is a measurement worth keeping: the reference agent keeps its delegation tool always-active, but `swarmSpawnDescription` is 4,364 chars (~1,100 tokens) billed on every turn against a few lines for the equivalent, so matching the tool SET is not sufficient grounds to promote — the description bytes are the other half. Worker fan-out, budget, goal preflight, report assembly, mail+WhatsApp read-back, timing ratio and judge paths are all untouched. The live harness remains DELETED (see 2026-08-02), so no fresh number could have been produced either way; this is an attestation, not a measurement, and it claims nothing more. Witness: Linux `go build ./... && go vet ./...` clean, `go test ./internal/agent/tools/` and `go test -race ./internal/agent/tools/` green. Prior **RE-ATTESTED 2026-08-02 (metric-neutral for the runtime); THE E2E HARNESS IS DELETED.** The matching change in `internal/swarm/**` removes the `ReasoningControl` field from `RunConfig`/`SwarmContextValue` and stops threading it through `WithSwarmContext` — the adaptive plane's per-round port, which sat at policy `off` and always fell through to the static request builder. Worker fan-out, budget, goal preflight and report assembly are untouched, so parallelization behaviour cannot move; `reasoning_control_test.go` was split so its two non-adaptive tests survive verbatim in `preflight_test.go`. The honest loss is that `internal/eval/harness_swarm_e2e_test.go` is DELETED with the rest of `internal/eval`, so the live judge-scored E2E that produced this row's number can no longer be reproduced; that glob is removed below. Witness: `go build ./... && go vet ./...` clean; `go test ./internal/swarm/` green. Prior **RE-ATTESTED 2026-07-30.** The only matching production change replaces the concurrency floor and wave-end branches with equivalent `max`/`min` expressions; goals, fan-out, worker count, timing, MCP read-back, and judge paths are unchanged. Fresh unit/property and race suites passed for `internal/swarm`. The operator mail/WhatsApp harness was not rerun, so the 2026-06-04 live result remains the row of record. Prior: **RE-ATTESTED** - the matching paths are `internal/swarm/**` (a `ReasoningControl` field threaded onto the child config, plus gofmt realignment) and, transitively, the adaptive ordering seams. Unlike the skills seam these controls ARE constructed in production - `newAdaptiveControlSet` (cmd/aura/chat_adaptive_controls.go:32) returns live controls whenever pool, provider and model are set - so nil-ness is not the argument. The argument is the policy: `aura.adaptive_policy_state` on this deployment reads `policy_version=runtime-hardening-off, mode=off, rollout_bps=0`, and `aura.adaptive_randomization_receipts` holds **0 rows** - no ordering or reasoning point has ever drawn a randomized arm. Every point resolves to the `static` champion (Amendment #93.8), which is the pre-change behaviour, so worker fan-out, the MCP read-back and the timing ratio cannot have moved. This attestation is void the moment `mode` leaves `off` or a receipt appears: the owning phase must then re-run the live harness, which additionally needs the mail + WhatsApp MCP servers mounted (currently `aura mcp list` reports none) and `AURA_EVAL_SELF_MAIL`/`AURA_EVAL_SELF_PHONE`/`AURA_EVAL_WA_CHAT_SELF` set, because it sends real messages to the operator and reads them back. Prior **RE-ATTESTED 2026-07-22** - Phase 39 changes `internal/agent/tools/swarm_spawn.go` only in `Spec()`: it adds canonical agent-operation metadata and durable replay policy to the already-mutating multiplexed tool. Worker fan-out, runner concurrency, aggregation, mail/WhatsApp read-back, timing calculation, control prompt, and judge logic are unchanged, so the 2026-06-04 live row of record remains metric-neutral. Fresh witnesses: Linux `-race -count=1` passed for `internal/swarm` and `internal/agent/tools`; the full uncached disposable Postgres+Neo4j matrix passed at **85.4%**; operation reserve/complete telemetry is live in production. The operator-only self-mail/self-WhatsApp targets are not configured locally, so no recipient was invented and no skipped run is claimed as new live evidence. Prior **RE-ATTESTED 2026-07-18** - Phase 38's only change matching this row's globs is a mechanical two-context signature adaptation in the test helper `internal/eval/harness_swarm_e2e_test.go` (`mcptools.MountServer(ctx, reg, ...)` → `MountServer(ctx, ctx, ...)` for the new processCtx/handshakeCtx split); zero change to swarm autonomous parallelization, worker fan-out, mail+WhatsApp MCP read-back, timing, or judge logic — the 2026-06-04 live PASS stands and cannot move. Local witness: `go build ./... && go vet ./...` green. Prior 2026-06-04 (live, run 8 of 8) **PASS** (workers=2, fan-out 15.9s / baseline 12.2s = 1.30×, e2e 27.8s advisory, mail+WA read-back=found, judge=1.00, control 0 workers + 5/5) | Phase 9 Slice 3 | `internal/swarm/**`, `internal/agent/tools/swarm_spawn.go` |
| MCP manager mock E2E + policy gate (CAP-09 / MCP-V2-01) | mock stdio + Streamable HTTP + trust gate + policy gate pass; live tiers explicit | 2026-08-25 | **RE-ATTESTED 2026-08-25 (metric-neutral dead-code deletion in the composition root).** The only matching production change is removal of the unreachable `closeMCPDB` wrapper from `cmd/aura/mcp_registry.go`; `TestMain` now performs the same pool close directly during goleak cleanup. No MCP manager, transport, trust, policy, auth, or registry behavior changed, so the row's prior measurements cannot move. Targeted `cmd/aura` tests plus touched-package build/vet are green, and changed-scope production/test dead-code scans report zero findings. Prior **RE-ATTESTED 2026-08-24 (per-identity MCP OAuth: `authStatus` re-measured, package coverage 96.0% under the gate matrix).** Production code under this row's globs changed, so nothing is claimed metric-neutral. `manager.authStatus` no longer decides OAuth by sniffing env key names for "OAUTH": it asks `mcp.UsesOAuth`. The sniff was wrong in the direction that costs most - a remote server with NO oauth configuration is exactly the Linear/Atlassian shape (dynamic client registration needs none) and was reported `unsupported`, the one answer that stops an operator from trying to connect it. Our own `MCP_OAUTH_*` keys are now skipped in the legacy sniff below it, because `MCP_OAUTH_TOKEN_URL` contains "TOKEN" and would otherwise be read as a bearer credential the server does not have; any OTHER oauth-shaped key still means what it always did, a server running its own OAuth internally. Three table cases added for exactly those readings. The mock stdio / Streamable HTTP / trust-gate / policy-gate paths are untouched - `authStatus` is a display classifier with no gate semantics - so this row's PASS conditions cannot move. Measured on the WSL `db_integration` gate: `internal/mcp/manager` 316/329 = **96.0%**, `internal/mcp` 995/1066 = **93.3%**, new package `internal/mcpoauth` 125/142 = **88.0%**. Witness: `bash scripts/coverage_docker.sh` under WSL, `ok: owned coverage 26820/31045 (86.4%) >= 85%`. A Git-Bash run of the same gate again reported the four known Windows artifacts (POSIX file modes, Windows Temp paths) plus one `internal/runner` flake that passes 3/3 in isolation; WSL remains the environment this gate is measured in. Prior **RE-ATTESTED 2026-08-23 (the stdio branch acquires a mount-time refusal; coverage and mutation RE-MEASURED, mutation went 69.2% -> 100%; driven live on the built binary).** Production code under this row's globs changed, so nothing here is claimed as metric-neutral. `internal/mcp/stdio_shape.go` is new and `managed_config.go` / `sdkclient.go` each gain one call into it (PRD amendment #128): D-106's premise — that only the operator writes the MCP registry — was measured FALSE, because a skill install runs third-party npm lifecycle scripts as root in the aura process and `/var/lib/aura/mcp/servers.json` is writable from there. The refusal is NOT the allowlist D-106 rejected: `bash`, `npx`, `uv`, `docker` and a script in a home directory all still mount. It refuses three shapes no MCP server has — a launch declaration that names the registry itself (unconditional, the stdio analogue of `ssrf.go`'s IMDS block), an inline shell script writing to an OS persistence surface, and one that fetches-and-executes or posts data off the box — at TWO checkpoints, `SaveManagedConfig` and `OpenSDKSessionForConfig`, because an entry planted out of band never passes through the first. `LoadManagedConfig` deliberately does not run it, so one planted entry cannot make the whole registry unreadable. **Coverage re-measured** (untagged, `-count=1`): `internal/mcp` **93.5%** (from 91.8%). **Mutation measured, then RAISED**: the first `go-mutesting ./internal/mcp/stdio_shape.go` run scored **69.2% (36/52)**, below the 70% floor. Of the 16 survivors, three were equivalent `make` capacity hints and two more pointed at code that did nothing — `shellBasename`'s empty-token early return (`path.Base("")` is `.`, which is not a shell) and the `runtime.image` append (last in the slice, so adjacent to no inline-script flag, and an image reference cannot name a file). Both were DELETED rather than tested, and the remaining eleven were killed by real cases the tests had missed: the registry named in the command position and bind-mounted into a container, `gcc -c` not being an inline script, a decoy interpreter before the real one in the same argv, an interpreter+flag pair with an empty script, and the two legitimate halves of the egress clause (a pipe into an interpreter with no egress; an upload command sitting in an env var with no inline script). Second run: **100% (48/48 killed)**, 0 survivors. **Driven live on the real binary**, not asserted from tests: `go build ./cmd/aura` then `aura mcp doctor` against a planted registry — the SSH-persistence entry and the self-rewriting entry were both refused by shape, while the control entry PASSED the gate and was genuinely spawned (it failed later at the MCP handshake with a transport error, which is the distinction that matters: the gate is not refusing at large), and the registry file was byte-identical afterwards. Witnesses: `go build ./...` + `go vet ./...` clean, full `go test ./...` green in WSL, `go test -race ./internal/mcp/...` green, golangci-lint 0 issues. NOT re-run in this range: the tagged coverage aggregate and the live sidecar E2E, which keep their prior dates. Prior**RE-ATTESTED 2026-08-23 (coverage and mutation RE-MEASURED; the live sidecar E2E re-run; a mutation-silent gap in the view gate FOUND AND CLOSED).** Production code under this row's globs changed, so nothing here is claimed as metric-neutral. `bridge_views.go` now carries a view-bound result's TEXT alongside its structured half and `CallForView` returns the whole `mcp.ToolPayload` -- `structuredContent` is OPTIONAL in MCP, so a server answering with a plain string had been handing its view a null; `bridge_risk.go` gains one entry, whatsapp's `get_media_data` as `MCPActionRead`. **Coverage re-measured** (untagged, `-count=1`): `internal/agent/mcptools` **93.2%** (from 92.2%), `internal/mcp` **91.8%** (from 90.5%). **Mutation on `bridge_risk.go` re-measured and it had FALLEN to 86.1% (31/36)** from the recorded 94.4% -- not noise: the view-gating leg `policy.views = mcp.TrustMayRenderViews(trust)` had landed since that measurement with no test that noticed its removal, and neither did the guard keeping a non-`trusted_recipe` server out of the curated action table. `bridge_policy_test.go` (new file; `bridge_risk_test.go` was at 592 LOC against the 600 cap) kills both, taking mutation to **91.7% (33/36)**. The three survivors are named: two are the `slog.Debug` annotation log and its call, already accepted at the prior measurement; the third is EQUIVALENT -- deleting the early `return policy` after a failed `mcp.Classify` changes nothing observable, because Classify returns an EMPTY trust on error and an empty trust neither renders views nor matches `TrustTrustedRecipe`. One finding recorded, not fixed: `TestRecipeOverridesRequireTrustedRecipeIdentity` passes for the WRONG reason -- its stdio+remote_http fixture is an inconsistent pair, so `Classify` errors and the function returns before the guard the test names is ever evaluated; the new tests use valid pairs. **Live E2E re-run** on the running deployment against both re-pinned sidecars (calendar `c497224cf8a0c8ee...`, whatsapp `sha-9911eb8`), driven through the cockpit HTTP API as the operator: one turn, two view-bound reads, both descriptors correct on the wire -- calendar resolved `ui://calendar/view.html` with `text_content` of 2,468 bytes parsing to 8 real emails and `structured_content` ABSENT (precisely the case that rendered null before), whatsapp resolved the NEW `ui://whatsapp/client.html` with both halves present. Aura must be restarted after a sidecar is recreated or it serves the previous mount's resource URIs -- observed here, and the reason the first run reported the retired `chats.html`. Witnesses: `go vet` + `go build ./...` clean, `go test ./internal/agui/ ./internal/agent/mcptools/` green, golangci-lint 0 issues. NOT re-run in this range: the tagged coverage aggregate and the `bridge_supervisor.go` mutation leg, which keep their prior dates. Prior**RE-MEASURED 2026-08-23 (Phase 46 MCP trust & facade — production changed, nothing here is claimed neutral).** Fourteen paths under this row's globs changed since the 2026-08-17 note, two of them NEW production files: `bridge_multiplex.go` (D-21/D-33/D-34 — one curated tool classified per ACTION, because a fork that collapses many raw tools into one leaves `classifyToolRisk`, which sees a spec and never a call argument, nothing to key its decision on) and `bridge_deferral.go` (D-27 — reconnect deferral drift REPORTED, never applied). `bridge_risk.go` gains the graduated per-action recipe table and D-107's escalate-only `unsafeToRepeatBeyondAura`; `bridge.go`, `bridge_memory.go` and `bridge_supervisor.go` follow; `internal/mcp/calendar_integration_test.go` plus a no-OAuth `testdata/fixture-calendar.json` arm the MCP-05 round-trip. Measured in a CLEAN worktree at the pushed HEAD, NOT in the shared checkout: a concurrent session held `bridge_views.go` and later `bridge_risk.go`/`bridge_risk_test.go` dirty throughout, so a live-tree number would have described neither the pushed state nor theirs. **The tier PASSES**: `go test ./cmd/aura ./internal/mcp ./internal/mcp/manager ./internal/agent/mcptools -count=1` → all four ok (23.5s / 2.0s / 0.02s / 1.4s), covering mock stdio, Streamable-HTTP, the trust gate and the manager policy gate. Coverage measured fresh and UP: `internal/mcp` **91.8%** (was 90.5%), `internal/agent/mcptools` **93.3%** (was 92.2%). **The aggregate gate ran LOCALLY**, closing the deferral the 2026-08-17 note left open: `bash scripts/coverage_docker.sh` → **86.4%** owned surface (26,068/30,170) against a DISPOSABLE `aura_cov` on a disposable Postgres at :5433, 73 migrations applied, both dropped on exit — the live deployment was never a target. **A live tier ran**: `calendar_integration` against the running `aura-pim-mcp` sidecar negotiated protocol `2025-11-25` and proved the MCP-05 opaque-reference round-trip end to end ("eventId reference resolved to event detail with no accountId argument dispatched"), so it is the round-trip and not merely the schema half; the always-on compile floor (`go vet -tags calendar_integration ./internal/mcp/`) also passes. **Mutation**, on all three files this phase adds or rewrites: `bridge_risk.go` **86.1%** (31/36), `bridge_deferral.go` **75.8%** (25/33), `bridge_multiplex.go` **69.2%** (9/13). The last began at **61.5%** and was raised by a test this attestation ADDS: `reconcileCuratedActions`'s `inEnum` bookkeeping was unmeasured, so dropping that single map write made a fork in step report EVERY risk-table entry as "no longer advertised", while the existing both-directions test kept passing on the name it greps for — a drift warning that fires on a healthy mount is one an operator learns to ignore, which costs exactly the outage D-33 exists to surface. Its four remaining survivors are EQUIVALENT, proven rather than argued: each was applied in the throwaway worktree and diffed against a 31-input probe of `MCPActionClassFor` / `MultiplexedMCPTools` / `curatedActionEnum`, and all four produced byte-identical output — the two `return MCPActionUnknown` arms fall through to another `return MCPActionUnknown`, `sort.Strings` is identity on a one-entry map, and a failed `json.Unmarshal` leaves `Enum` nil either way. Non-equivalent mutants killed: **9/9**. Witnesses: `go build ./...` and `go vet ./...` clean; WSL `-race` green on `./internal/mcp/... ./internal/agent/mcptools/`; `golangci-lint` **0 issues** — after `golangci-lint cache clean`, because a cached run first reported a stale `SA9003: empty branch` against `bridge_risk.go:200`, a line that carries a `return`. Named as NOT re-run: the SC#1/SC#3/SC#4/SC#5 live sidecar E2E of 2026-08-17 (unchanged supervisor/redial/SSRF/`_meta` surface; `bridge_supervisor.go`'s delta is the deferral-drift hook, whose own tier is `bridge_deferral_test.go` above), and the WhatsApp curation of plan 46-08, which has not landed. Prior 2026-08-17 **RE-ATTESTED 2026-08-17 (the two items the 2026-08-16 note named as NOT re-run have now been run).** The only path under this row's globs to change since that note is `internal/agent/mcptools/bridge_risk_test.go`, and it is test-only: one assertion loop added to `TestMCPToolRiskRecipeTablesUnchanged` driving every entry of all three recipe tables with `readOnlyHint:true`. No production line moved, so the coverage figures above (`internal/mcp` 90.5%, `internal/agent/mcptools` 92.2%) cannot move either — but this is NOT a bare neutrality claim, because the two deferrals are now measurements. **The aggregate gate ran**: CI green on `63b456f8e` (2026-08-17) including `scripts/coverage_gate.sh`, so the >=85% owned-surface floor holds at this HEAD; the last recorded figure is 86.2% (25,941/30,108) from the gate on `5b2f2a958`. **The live sidecar E2E ran**: all three sidecars negotiate `2025-11-25`; SC#1 drove calendar + WhatsApp + memory through the SDK client in one conversation; SC#3 killed and restarted `aura-whatsapp` and recovered on a NEW session id via the supervisor's bounded redial; SC#4 refused both the link-local probe and a public DNS name resolving to loopback; SC#5 (2026-08-17) showed the identity travelling in `_meta` and nowhere else — no `user_identifier` in the live tool manifest, a call without `_meta` refused as a TOOL error, the same call with it returning this identity's own entities. Evidence in `.planning/phases/45.1-native-mcp-client/45.1-VALIDATION.md`. **Mutation**, both critical files: `bridge_supervisor.go` 99/99 (100%), `bridge_risk.go` 34/36 (94.4%) after the assertion above killed the surviving `case mcpActionDestructive` mutant — the two remaining survivors are the `slog.Debug` call and the call to it. Witnesses: `go vet` and `go test -race ./internal/agent/mcptools/` green in WSL. Prior 2026-08-16 **RE-ATTESTED 2026-08-16 (client REPLACED wholesale — re-run, not assumed neutral).** Phase 45.1 swapped Aura's hand-rolled MCP client for `github.com/modelcontextprotocol/go-sdk@v1.7.0`. `internal/mcp/{client,http_client,transport,protocol,tool_methods,lifecycle}.go` and their 12-file test tier are DELETED (-3,786 lines); every `cmd/aura` consumer re-points at `*sdkmcp.ClientSession`; the bridge moves onto the SDK session; `mcpToolRisk` reads all four SDK annotations under an exhaustively-proven escalate-only invariant; calling identity moves off the `user_identifier` argument onto `_meta`, fail-closed, with nine argument fields removed; and SEP-2322 elicitation gains a bounded fail-closed handler. Seventy-eight paths under this row's globs changed, so nothing here is claimed neutral. RE-RUN rather than assumed: the mock stdio and Streamable-HTTP legs and the trust/policy gates pass on the swapped client (plans 45.1-01..03); `internal/mcp` coverage **90.5%** and `internal/agent/mcptools` **92.2%** measured fresh; `go test -race` green on both packages; and `TestNoHandRolledJSONRPCFraming` was added AND proven to bite by deliberate insertion. Named as NOT re-run: `bash scripts/coverage_docker.sh` (the aggregate 85% gate) and a live sidecar E2E — both are recorded 45.1-08 blockers, deferred because a concurrent session held this checkout dirty throughout. The decider is the CI job, which re-runs on this push. Prior 2026-08-12 **RE-ATTESTED 2026-08-12 (MCP trust posture — output marked trusted).** The MCP bridge now marks tool output `TrustTrusted` and drops the "untrusted MCP server" description/summary framing (aligning with Claude Code's MCP posture); the memory-context header and its system-prompt instruction likewise drop the distrust wording. Metric-neutral for THIS row: the mount-time **trust gate**, mock stdio + Streamable-HTTP legs, policy gate, structured provenance, abstention, combined `internal/arcadedb` coverage (**92.81%**) and operator-path p95 are all unchanged — only the output-distrust envelope was removed, which this row never measured. The B-15 byte caps on server descriptions/summaries stay enforced. Witnesses: `go build ./... && go vet ./...` clean; `go test -race ./internal/agent/... ./internal/runner/... ./internal/mcp/...` green (incl. `internal/agent/mcptools`, `internal/mcp/manager`). Prior **RE-MEASURED 2026-08-03 (ArcadeDB Agent Memory industrial gate).** The production Streamable HTTP MCP passed initialize/list/call, tenant isolation, structured provenance, explicit abstention and versioned runtime evidence inside the fail-closed MRS suite (**100.00/100**). Combined live `internal/arcadedb` coverage is **92.81%**; operator-path search p95 is **79.763 ms** over 25 sequential samples. WSL `-race` is green on `internal/mcp/...` and `internal/agent/mcptools/...`; no hidden or retired memory tool is reintroduced. Prior **RE-MEASURED 2026-08-02 (memory hidden-surface correction).** Not metric-neutral and not a rewrite of the number: the policy gate this row measures was verified against a fixture that could not exercise it. `memoryHiddenFromModel` listed six tools — store_message, get_conversation, list_sessions, get_context, get_facts, store_profile — that the ArcadeDB memory server does not implement, and both hiding tests enumerated that dead surface, so the filter was proven against names which can never arrive. The deny list now carries the one tool that must be hidden (`memory_reembed`, whose cost scales with the corpus, not the turn) and both tests were rebuilt on the server's REAL eight-tool surface. Fresh evidence: `go test ./internal/agent/mcptools/` green in 10.4s, full untagged suite 0 failures, `go build`/`go vet` clean. The mock stdio, Streamable-HTTP and trust-gate legs are unchanged and still pass; the live tiers remain as previously declared. Prior 2026-07-31: **RE-ATTESTED METRIC-NEUTRAL (tool_search rewrite).** The only change under this row's globs is test-side: `bridge_test.go` dropped an `Embed:` field from a `ToolSearch` literal and the test-only `bridge_embedder_test.go` fixture was deleted, because `ToolSearch` no longer holds an embedder — free-text discovery is lexical and in-process. No file under `internal/mcp/**`, `cmd/aura/mcp*.go`, or the production side of `internal/agent/mcptools/**` changed; the mount seam (`InvalidateIndex`, zero-arg) keeps its signature and still fires on reconnect, now dropping one index pointer instead of an embedding bank. The mounted/deferred/hidden tool partition this row measures is untouched. Witness: `go test ./internal/agent/mcptools/ -count=1` green (10.5s) at HEAD. Prior **RE-MEASURED LIVE 2026-07-30.** The changed `internal/agent/mcptools/**` surface aligns the live contract with the measured four-tool always-active policy: all six model-facing `memory__*` tools remain mounted and Deferred, reachable through `tool_search`, while the host-only tools remain hidden. The exact CI live tier passed against the healthy authenticated sidecar under WSL with `-race -tags memory_integration -p 1` across `internal/agent/mcptools`, `cmd/aura`, and `internal/agent`; fresh `-race` runs over `internal/mcp/...` and `internal/agent/mcptools/...` are also green. Prior **RE-MEASURED 2026-07-28** - `internal/agent/mcptools/**` matches, and the change is real in this row's surface: the bridge now decides which SERVED tools reach the model's manifest. It exists because the obvious alternative was tried and failed in production - deleting the tools server-side took onboarding to 502 with "Unknown tool: memory_get_facts", since Aura's own code calls the sidecar directly through `mcp.Transport.CallTool` and never sees the registry. So the tier was RE-RUN rather than argued: `go test ./internal/mcp/... ./internal/agent/mcptools/` green (mock E2E and policy gate included), `internal/agent/mcptools` at 93.3% of statements, `golangci-lint` 0 issues. Two new tests pin the rule that replaced the deletion: 6 of 13 memory tools reach the model, and the hiding is namespace-scoped so another server's identically-named tool passes through. Prior **RE-ATTESTED 2026-07-27** - metric-neutral. `internal/mcp/manager/catalog.go` adds `UV_OFFLINE=1` to the calculator recipe's Env; the mock stdio + Streamable-HTTP + trust-gate + policy-gate tier never launches uvx, so no measured behaviour moves. The change is a live-boot fix: the image warms that exact pinned commit, but uv still queried the pypi index to re-resolve the dependency set on EVERY mount, and on the appliance that burst of parallel lookups saturated Docker's embedded resolver — the mount died with EAI_AGAIN and the bounded retry ladder burned minutes before boot gave up, leaving the agent with ZERO calculator tools. `cmd/aura/chat*.go` + `shell.go` also match this row's glob: they route every interactive CLI entry point through a new `bootCLIChat` that scopes ctx to the operator identity (the `local` seed is deleted at first enrolment, so `aura chat` died with `get identity "local": identity not found` on any onboarded appliance) — zero MCP surface. Witnesses: `go test -race ./cmd/aura ./internal/mcp/...` green on WSL; `golangci-lint` 0 issues on both; live on the appliance at image `90abf64a77ad`, boot logs show `mcp mounted server=calculator tools=23` on the FIRST attempt with no `mount transient failure` line at all (was: 6+ retries over 3 min, then zero tools). Prior **RE-MEASURED 2026-07-25** - this push changes the Streamable-HTTP transport itself, so the tier was re-run rather than argued neutral. `internal/mcp/http_client.go`: a session-less HTTP 400 is now treated as the expired-session condition an HTTP 404 already was. The recovery path existed but was unreachable — the 404 branch clears `sessionID` BEFORE `roundtripLocked` decides whether to retry, and when it correctly declines (a mutating tool call must not be replayed after send) the client was left with no session and no trigger to re-establish one, so every later call, reads included, posted bare and got 400 forever. Observed live: 20 consecutive memory calls failed after a sidecar rebuild and only restarting the daemon cleared it. Both status codes were confirmed against the running FastMCP sidecar (`404 "Session not found"` for a stale id, `400 "Bad Request: Missing session ID"` for none). `internal/agent/mcptools/**`: identity injection now derives from the tool's advertised schema, closing the gap that left `memory_update` unscoped. The trust gate, `Classify` dispatch and manager policy gate are untouched. Witnesses: `go test -race ./internal/mcp/ ./internal/agent/mcptools/` both green (21.2s/12.5s), including a new test that replays the full lock-up sequence and, with the fix mutated out, fails with the exact live error `mcp "memory": call memory_search: http 400`; `golangci-lint` 0 issues on both packages; live witness — sidecar rebuilt + daemon restarted, `mcp mounted server=memory tools=13`, `aura mcp tools memory` lists all 13 incl. `memory_update`. Prior **RE-ATTESTED 2026-07-23** - commit 81c8341d touches `internal/agent/mcptools/**` only (HTTP-branch reconnect wrapper + ping poll); the manager policy gate, Classify dispatch, and `internal/mcp` transports are untouched — metric-neutral for the mock E2E + policy gate. Witnesses: `go test ./internal/mcp/ ./internal/agent/mcptools/ -count=1` + WSL `-race` green; live sidecar-bounce E2E preserved tool service continuity with no RUN_ERROR. Prior **RE-ATTESTED 2026-07-21** - Amendment #87 matches via `internal/mcp/managed_config_identity.go` — the same D-20/D-21 path-guard relocation into `internal/idroot` (behavior-identical). Zero MCP-manager mock-E2E or policy-gate surface changed; `internal/mcp` re-measured 91.6%. Local witness: full `db_integration neo4j_integration` owned-surface matrix green at 85.5% aggregate via `scripts/coverage_docker.sh`. Prior **RE-ATTESTED 2026-07-20 (memory-MCP fork tool-surface change, metric-neutral)** - The Aura agent-memory fork gained `memory_forget` (ownership-scoped preference/fact delete) + an exact-text preference dedup fallback, and dropped the redundant reasoning-trace quartet + `memory_export_graph` (Aura keeps its own reasoning store). `internal/agent/mcptools/bridge.go` adds `memory_forget` to the user-identifier scoping list and removes the 5 pruned names; `internal/mcp/manager/catalog.go` is a comment-only tool-count/description update; `cmd/aura/memory.go` swaps the `export`/`trace` verbs for `forget`. The mock stdio + Streamable-HTTP + trust-gate + policy-gate behavior is UNCHANGED: `go test ./cmd/aura ./internal/agent/mcptools ./internal/agent -count=1` all green; `internal/mcp`/`internal/mcp/manager` build clean. Live witness on the redeployed `aura-agent-memory-mcp:local` + `aura:local`: `aura tools` shows `memory__memory_forget` present and the 5 removed tools absent; a real agent turn confirmed dedup (identical preference → same id, count stays 1) and forget (delete → count 0). Prior **RE-ATTESTED 2026-07-18 (LIVE RE-MEASURED)** - Phase 38 (MCP governance hardening) hardened exactly this surface: canonical trust `Classify` with remote→`TrustBlocked` default + mixed url+command reject (T-38-01/02, `internal/mcp/classify.go`), manager trust resolver migrated onto `Classify` + per-identity remote-elevation refusal (T-38-01b/04, `internal/mcp/manager/runtime.go`), audited CLI mutations via `WriteConfigWithAudit` with `cli:<user>` actor + required `--reason` (T-38-11/12/13, `cmd/aura/mcp*.go`), bounded mount/shutdown/probe timeouts (T-38-05..10b). The four-package mock stdio + Streamable-HTTP + trust-gate + policy-gate tier still PASSES: `go test ./cmd/aura ./internal/mcp ./internal/mcp/manager ./internal/agent/mcptools -count=1` → all 4 ok (18.6s/16.9s/3.1s/14.0s). Live container witness: `aura mcp status` probes bounded by `AURA_MCP_PROBE_TIMEOUT`, a hung managed server → deterministic `fail`, no leaked dial. Prior **RE-ATTESTED 2026-07-08** - the pushed range touches `internal/agent/mcptools/bridge.go` — scoping no-principal memory-MCP calls to the operator (closing a fail-open leak) plus `identityctx`/`backfill` plumbing; the mock stdio + Streamable-HTTP + trust-gate + policy-gate measured behavior is unchanged. Local witness: `go test ./internal/agent/mcptools/ -count=1` green. Prior **RE-ATTESTED 2026-07-01** - WhatsApp sidecar relink validation after the QR-held-until-paired cockpit fix: the fused sidecar now rotates to a fresh whatsmeow client on logout/disconnect relink, so QR generation no longer depends on rebooting the `whatsapp` container. Local sidecar witnesses: `go test ./... -count=1` in `whatsapp-bridge` passed, `uv run --extra dev pytest -q tests/test_transport.py` in `whatsapp-mcp-server` passed 30 tests, Docker image build passed, and fork workflow `aura-publish-image` run 28515821785 succeeded for sidecar commit `e1a2f7a`. Live Aura compose witness: `aura-whatsapp` recreated on `ghcr.io/chetto1983/whatsapp-mcp:sidecar@sha256:b96d23bbe144cc7b3604e5777b25a6e6d0606e1ddba1dc7f8b10f5a38193c877`; `/api/status` reported `state=waiting_qr`, `paired=false`, `qr_available=true`; `aura mcp doctor whatsapp` inside the Aura container reported `ok: whatsapp started; 14 tools`; desktop and mobile Playwright cockpit probes rendered the QR image without showing a misleading paired/connected state. Actual phone pairing remains an operator action before account-level WhatsApp tool access is claimed. Prior **RE-ATTESTED 2026-06-29** - Phase-31 SSRF hardening (SEC-08) added an MCP-local SSRF guard (`internal/mcp/ssrf.go` + `transport_ssrf.go`) that validates outbound request targets at the `OpenHTTP` seam (unconditional scheme/metadata barrier + enforce-gated private/loopback block behind `AURA_MCP_SSRF_ENFORCE`, default off). Under the default policy loopback + compose-DNS sidecars stay reachable, so the mock stdio / Streamable-HTTP / trust-gate / policy-gate measured behavior is unchanged — the ~20 loopback `httptest` tests stay green. Local witnesses (WSL): `go test ./internal/mcp/ -count=1` ok 10.8s, `CGO_ENABLED=1 go test -race ./internal/mcp/` ok, `golangci-lint run ./internal/mcp/` 0 issues. Prior **PASS** — re-measured 2026-06-28 after MCP boot-mount retry, streamable-HTTP probe/tool-count dialing, and `aura-agent-memory-mcp` compose health gating: `go test ./cmd/aura ./internal/mcp ./internal/mcp/manager ./internal/agent/mcptools -count=1` -> all 4 packages PASS. Local compose witness: `docker compose config` rendered Aura waiting on `aura-agent-memory-mcp` with `service_healthy`. Prior 2026-06-21: re-measured after CodeQL hardening in `internal/mcp/client.go`: stdio MCP launch rejects shell interpreters, relative executable paths, and command strings outside the strict executable-name regex before spawn; same four-package mock tier passed. Prior 2026-06-16: re-measured after the calendar PIM-sidecar repoint + standalone-mail retirement (`internal/mcp/manager/catalog.go` repoints `recipe:calendar` from the `aura-calendar-mcp-fixture` stdio placeholder to the streamable-HTTP `aura-pim-mcp` sidecar via `calendarRecipeURL()`, and removes the `recipe:mail` entry; `cmd/aura/mcp_status.go` drops the mail/fixture doctor branches for an HTTP-aware calendar check; a new `calendar_integration` tier + CI job drives the live sidecar with no-skip-as-green): the four-package mock tier passed. Prior 2026-06-15 re-measure after the Compose-DNS catalog change (`internal/mcp/manager/catalog.go` routes built-in HTTP recipes to `aura-agent-memory-mcp:8080` and `whatsapp:8080` when `AURA_IN_CONTAINER=1`): the four-package mock tier passed. Live memory sidecar also checked locally via `aura mcp doctor memory` -> `ok: memory started; 17 tools`; WhatsApp/mail/Calendar/Docker checks remain operator-only. Prior 2026-06-13 B-09 secret-env-key unify evidence stands. | Phase 16 MCP manager | `cmd/aura/mcp*.go`, `internal/mcp/**`, `internal/agent/mcptools/**`, `docs/mcp-manager.md` |
| Live CoT/tool-use eval (TestCoTEval, 12 scenarios x 10 dimensions, real agent vs DeepSeek-V4) | all asserted dimensions full; reasoning advisory | 2026-08-02 | **RETIRED 2026-08-02 — the metric is no longer definable.** `internal/eval` was deleted in its entirety on the operator's instruction ("lasci solo adaptive reasoning e basta con l'oracolo"), and `TestCoTEval` went with it: the 12-scenario x 10-dimension LLM-judge harness this row measured does not exist any more. There is nothing left to re-run, so the INVALIDATED status carried since 2026-07-31 can be neither cleared nor renewed, and the paid re-measurement it owed is void rather than outstanding. The CI-gate path is emptied because the row can never again be reached by a change. Every prior note below is preserved verbatim as the audit trail of what was measured while the harness existed. Prior **STILL INVALIDATED — this range neither clears nor worsens it (2026-08-02).** The glob matched because `internal/eval/**` changed, but the change is a test-fixture relocation and nothing else: `.planning/` was deleted at milestone close, and six call sites in three `_test.go` files read the adaptive-benchmark dataset from inside it, so the 11,775-byte file moved verbatim to `internal/eval/testdata/adaptive_benchmark_dataset.json` and the paths were repointed. Byte-identical fixture, zero production statements, zero change to the agent, the LLM client or the eval harness — the measured behaviour cannot move. The 2026-07-31 invalidation stands unchanged and its paid re-measurement is still owed. Witnesses: `go build ./... && go vet ./...` clean, full untagged suite 0 failures, `go test ./internal/eval/` green on the relocated fixture. Prior 2026-07-31: **INVALIDATED / PAID RE-MEASUREMENT STILL DUE.** This range adds a final request-budget rejection before every stream open and adds a separate paid injection-resistance tier under `internal/eval/**`; those are observable execution/eval changes, so the older 12/12 DeepSeek result is not inherited. No paid provider run was authorized. Exact-candidate local evidence is green for the non-paid contracts (`make quality`, capability 19/19, coverage 85.4%, critical mutation minimum 75.82%), but none substitutes for `TestCoTEval`. The row remains deliberately non-green until an operator authorizes and records the live run. | Phase 3 Slice 1 |  |
| Scheduler North-Star live E2E (CAP-06 / SC#1-SC#4) — chaos failover + once-per-window + natural-prompt -> `task` tool -> persisted row, real DeepSeek-V4 | SC#2 no-dup survivor / SC#1 once/window / E2E >=90% / coverage >=85% / mutation >=70% | 2026-08-25 | **RE-ATTESTED 2026-08-25 (metric-neutral; independent approval-expiry lifecycle only).** The matching cmd/aura/serve.go change starts and stops an approval-expiry sweeper. No cron package, task tool, dispatch, cadence, lease, once-per-window, persistence, or chaos path changed, so the scheduler measurements cannot move. The scheduler live tier was not re-run; targeted WSL race, vet, build, lint, and deadcode gates are green on the touched packages. Prior **RE-ATTESTED 2026-08-24 (metric-neutral; ArcadeDB identity-memory wiring only).** The matching path is `cmd/aura/serve.go`: startup now builds the ArcadeDB memory provisioner, reconciles active user tenants before readiness, and passes that provisioner into onboarding/bootstrap. No cron package, task tool, dispatch, cadence, persistence, once-per-window, or chaos path changed, so this row's measured behavior cannot move. Fresh witness: targeted `cmd/aura` and `internal/agui` unit/race runs green. Prior **RE-ATTESTED 2026-08-23 (metric-neutral by construction; nothing this row measures is reachable from the diff).** `internal/cron/scheduler.go` lost exactly one thing: `markTerminalFailure`, a four-line method with ZERO production callers — `cmd/aura/serve_lifecycle.go:166,203` already call `readiness.MarkSchedulerFailure` directly — moved verbatim into `scheduler_test.go`, its only caller. Schedule parsing, claim/lease, chaos failover, once-per-window and the `task` tool are untouched, and no code path reached the removed method, so the scheduler's measured numbers CANNOT move. What this does NOT prove: no live scheduler E2E was re-run in this range. Witnesses: `go build` clean on GOOS=linux/windows/darwin; `go vet` clean; `go test ./internal/cron/` green; golangci-lint 0 issues; WSL `db_integration` full matrix green with owned coverage **26094/30215 = 86.4%** >= 85%. **RE-ATTESTED 2026-08-15 (behaviour CHANGED in scope, metrics unmoved, with a fresh partial live witness).** Cross-owner attestation: the row belongs to Phase 10 Slice 6 and the change is Phase 45's. What changed under `internal/cron/**`: the orphan sweep now runs on EVERY tick rather than only at boot, and the tick body is extracted into `tickOnce` so its ordering is testable. That is a real behaviour change and is NOT claimed as a no-op. Why the measured numbers still cannot move: the sweep touches only runs whose heartbeat lapsed past 90s while a live run heartbeats every 30s (`defaultHeartbeatInterval`), so an in-flight run is never eligible; once-per-window is enforced by the per-task `pg_try_advisory_lock` plus `next_run_at`, neither touched; and no claim, dispatch, reschedule or dedup path changed. The chaos-failover posture should if anything improve, since a killed worker's run is now repudiated within one tick instead of waiting for the next process restart - the gap this fixed. Fresh live witness taken 2026-08-15 while driving 45-08: the natural-prompt -> `task` tool -> persisted row chain ran end to end on the real stack against DeepSeek-V4 - an operator sentence produced a scheduled `agent_job` (`01a005e2-4696-...`), the approval gate held it at `pending_approval`, accepting it flipped the row to `active`, and it fired and executed its tool at 14:47:09Z. Three new tests cover the sweep and were validated by REMOVING the fix rather than assumed: the stale-after-boot and idempotence cases fail without it, the healthy-run control passes either way. NOT re-measured, and therefore carried forward rather than re-observed: the chaos-failover suite and the mutation score. Prior 2026-08-12 **RE-ATTESTED 2026-08-12 (metric-neutral).** The only matching change is a FIXTURE rename in `internal/cron/handlers/agentjob_test.go`: the mutating test double it drives is now named `skill_manage` instead of `skill`, following the split of the skills verb into a read half and a write half (the double issues `action=restore`, which is a write and now belongs to `skill_manage`). No scheduler runtime changed — not the cron loop, not claim/lease, not chaos failover, not once-per-window, not the `task` tool, and not the agent_job handler under test; the double's NAME is the entire diff, and this row's E2E drives the `task` tool, not the skills verb. So SC#1-SC#4, the E2E score, coverage and mutation cannot move. Witness: `go test ./internal/cron/...` green, full untagged `go test ./...` green. Prior 2026-08-08 **RE-ATTESTED 2026-08-08 (metric-neutral).** The matching path is `cmd/aura` wiring. Schedule parsing, claim/lease, chaos failover, once-per-window and the `task` tool are untouched. The one scheduler-adjacent fact of the day is operational, not behavioural: `identity_purge` was brought forward once by hand to run a pending purge, which exercised the existing handler and changed no code. Prior 2026-08-06 **RE-ATTESTED 2026-08-06 (metric-neutral).** The sole matching path is `cmd/aura/serve.go`; `internal/cron/**`, `internal/agent/tools/task.go` and `scripts/scheduler_chaos.sh` are untouched. The one scheduler-adjacent edit is a `seedPostgresBackup` call that creates the nightly-dump task row, which had a handler and a dispatcher entry but no seed. That ADDS a scheduled row; it does not alter the per-task invariants this row measures. SC#1 once-per-window, SC#2 no-duplicate-under-failover, and natural-prompt to `task` tool to persisted row are all properties of an individual task's lease and window arithmetic, unaffected by the existence of one more seeded task. Prior **RE-ATTESTED 2026-08-03 (metric-neutral).** The matching paths are `internal/cron/**` and `cmd/aura/serve.go`, and the change ADDS a sixth system-seeded sweep beside the existing five rather than altering any of them: a `memory_embed_backfill` `TaskKind`, a handler built from the already-shared `countingSweepHandler` (so no new control flow), one entry in `isSilentSuccessKind`, one dispatch registration, one seed call, and migration 0091 widening the `scheduler_tasks.kind` CHECK exactly as 0034 and 0083 did. The two `every`-cadence seed helpers were folded into one `seedEveryCronSweep`: the `ParseSchedule`/`NextRunAt`/`CreateTask` sequence, the idempotence scan and the emitted log lines are unchanged, and a third hand-copied seed is precisely what `dupl` exists to catch. No schedule parsing, claim/lease, chaos-failover, once-per-window or `task`-tool path was touched, and `internal/cron/e2e_test.go` (the `cot_eval`-tagged live scheduler smoke) is unchanged and still carries its tag — so the paid natural-prompt leg stands exactly where the prior note left it. Witnesses: `go build ./... && go vet ./...` clean; `go test ./internal/cron/...` green; WSL `go test -race ./internal/cron/...` green; golangci-lint 0 issues; live deployment shows the new sweep active at `every 5m` next to the other five. Prior **RE-ATTESTED 2026-08-02 (metric-neutral).** The matching path is `internal/cron/**`, and the single edit is the removal of `handlers.Deps.ReasoningControl` and its one assignment — the adaptive plane's per-round reasoning port, which sat at policy `off` and always fell through to the static request builder, so an agent_job's model round is byte-identical without it. No schedule parsing, claim/lease, chaos-failover, once-per-window or `task`-tool path was touched, and `internal/cron/e2e_test.go` (the `cot_eval`-tagged live scheduler smoke) is unchanged and still carries its tag. Witness: `go build ./... && go vet ./...` clean; `go test ./internal/cron/...` green. Prior **PARTIAL / PAID NATURAL-PROMPT RE-MEASUREMENT DUE (2026-07-31).** Scheduler shutdown and dispatch semantics changed materially: admission now stops on cancellation while admitted jobs detach and retain a handler deadline; backups promote only complete partials; task listings expose bounded payload/notify metadata. Fresh exact-candidate evidence covers the deterministic and operational surfaces: full repository `-race`, DB integration coverage, concurrency-32 load, four chaos scenarios, atomic-backup kill handling, and four-plane DR all pass. The real-DeepSeek natural-prompt → `task` → persisted-row leg was not run because paid execution was not authorized, so the historical E2E percentage is not promoted to current green. | Phase 10 Slice 6 | `internal/cron/**`, `internal/agent/tools/task.go`, `cmd/aura/serve.go`, `scripts/scheduler_chaos.sh` |
| AG-UI gateway (UX-01 / SC1/SC3 + amendment #57 reasoning data-plane) — live SSE round-trip (`POST /agent/run`) + GET snapshot + REASONING_* lifecycle + `internal/agui` coverage + `translator.go` mutation | SC1/SC3 SSE+GET live (no-skip-as-green) / REASONING_* interleave-before-TEXT, stream-only / `internal/agui` >=85% combined / `translator.go` mutation >=70% killed / owned-surface >=85% | 2026-08-25 | **RE-ATTESTED 2026-08-25 (empty accepted resume is fail-closed; coverage RE-MEASURED at 85.6%).** Missing, empty, or whitespace-only resolved payloads now return HTTP 400 in both synchronous and detached `/agent/run` paths before Runner `SubmitAnswers`. Runner validates the effective persisted and injected answer, preserving scheduled approvals with a server-authored outcome; `askuser` validates again inside the transaction front door. Fresh full WSL `db_integration` matrix: `internal/agui` **4218/4928 = 85.6%** and owned surface **26827/31139 = 86.2%**. Wire, Runner, Store, and rollback regressions are green; `ValidateResumeAnswer` killed **4/5 mutants = 80%** (the reported survivor was byte-identical to the original); race, lint, vet, build, boundary, file-size, and dead-code checks are green. What this does NOT prove: the live SSE round-trip and `translator.go` mutation were not re-run; `translator.go` is unchanged. Prior **RE-ATTESTED 2026-08-24 (cockpit MCP authorization routes added; `internal/agui` coverage RE-MEASURED at 85.6%).** Five new routes under `/api/governance/mcp/*` (per-identity authorization state, start, poll, revoke, and the redirect callback) plus the `MCPAuthorizationProvider` seam, so this row is NOT metric-neutral and the number was measured rather than argued: `internal/agui` **4200/4909 = 85.6%** on the WSL `db_integration` matrix, still above the >=85% bar (prior 85.9%; the dip is new production statements, not lost tests). The new file carries 12 handler tests and has no uncovered function. SSE round-trip, GET snapshot and the REASONING_* lifecycle are untouched: nothing in `translator.go`, `server_run*.go` or the reasoning data-plane changed, so those legs and the `translator.go` mutation score cannot move. Two mutating routes were added to the `httpMutationRoutes` inventory rather than exempted - starting a flow is replay-safe by construction, but an exemption argued from a handler's internals is one refactor away from being false. Owned-surface aggregate **26820/31045 = 86.4%** (>= 85%). Prior **RE-ATTESTED 2026-08-23 (metric-neutral for the live legs; the coverage leg RE-MEASURED and it moved UP).** Six functions with ZERO production callers left `internal/agui` for their own package's tests: `deriveSigningKey`, `validateHTTPMutationInventory`, `resumeResourceProvisioning`, `sessionStore.len`, `projectMessages`, `projectToolCalls`. None is reachable from `POST /agent/run`, the SSE translator, the GET snapshot or the REASONING_* lifecycle — no route, handler or translator line changed — so SC1/SC3 and the `translator.go` mutation figure cannot move. The coverage leg CAN move, because moving covered statements out of the production profile shrinks numerator and denominator together, so it was measured rather than argued: `internal/agui` **85.9%** (up from 85.5%), and the owned-surface aggregate **26094/30215 = 86.4%** >= 85% on the WSL `db_integration` matrix. What this does NOT prove: no live SSE round-trip was driven in this range, and `translator.go` mutation was not re-run — neither file was touched. **RE-ATTESTED 2026-08-23 (metric-neutral; two additive REST routes, no translator or SSE path).** The matching path is `internal/agui/**`, and this phase's only edits under it are the two standing-approval-grant handlers in `approvals_api.go` (PRD amendment #127), the `approvalGrants` field they read through in `server.go`, and one row in `idempotency_http.go`'s mutation-route inventory. `translator.go` is untouched, no AG-UI event type was added or changed, and neither handler participates in `POST /agent/run`, the GET snapshot or the REASONING_* lifecycle — so the live SSE round-trip and the translator mutation score this row records cannot have moved. The `internal/agui` coverage figure moves only upward: both handlers are exercised by the package suite, which stays green. Prior 2026-08-22 **RE-ATTESTED 2026-08-22 (metric-neutral for every leg this row measures; the changed cockpit code is outside its globs).** Two matching paths, and neither carries a measured behaviour. `.github/workflows/ci.yml`: the same two `AURA_PIM_MCP_IMAGE` sidecar pins described in the Telegram row - the `agui_smoke.sh`/`agui_boundary_check.sh` jobs, their tags and their env are untouched. The SPA change that motivated this push (the calendar connect wizard surfacing the server's own `{error}` sentence, and folding the account id to lowercase) lives in `web/src/**` and `internal/webui/dist/**`, NEITHER of which is under this row's globs: `internal/agui/**` is unchanged, so no route, no SSE frame, no REASONING_* lifecycle event and no idempotency check moved. The one wire-visible consequence is a browser-side read of a body the gateway already sent, not a change to what it sends. Live witness on the redeployed container: `POST /api/connect/pim/accounts` returned 201 and a duplicate returned its 409 body verbatim through the same gateway, with the SSE chat path untouched. Prior 2026-08-17 **RE-ATTESTED 2026-08-17 (FRESH MEASUREMENT for the coverage leg; metric-neutral for the SSE/REASONING legs, with a live witness).** The matching diff adds `conversations_compaction_api.go` (+ its test) — `POST /api/conversations/{id}/compact` and `GET /api/conversations/{id}/compaction`, the routes behind the composer's `/compact` — and touches `conversations_api.go` + `idempotency_http.go` only to inventory the new mutating route. NOT claimed neutral, and measured instead: `internal/agui` coverage is **85.9% of statements** at this HEAD (`go test -tags=db_integration -cover ./internal/agui/`, disposable `aura_cov`), above the 85% floor. Neutral and argued: the SSE data-plane is untouched — no handler on `POST /agent/run`, no emitter, no event type and no `translator.go` byte changed, so the REASONING_* interleave order and the translator mutation score cannot have moved. Live witness on the running stack (2026-08-17, image rebuilt from this HEAD): both new routes driven end to end against the operator's own 64-conversation deployment — POST returned 200 with 48 turns condensed and 8748 -> 3281 tokens, a second POST returned the stored row in 0.0s with no model call, and GET returned it again; the same request was a 500 before this change. `scripts/agui_smoke.sh` and `scripts/agui_boundary_check.sh` are untouched. Prior 2026-08-15 **RE-ATTESTED 2026-08-15 (metric-neutral on the diff, plus a FRESH LIVE WITNESS).** The delta under this row's glob is 14 deletions and zero insertions: one line from `idempotency_http.go` and 13 from `storage_orphans_service.go`, both dead-code removal from the document-plane pruning that shares this branch. `translator.go` has 0 changes in the range, so the mutation figure this row carries has no input and cannot move. Recorded as a cross-owner attestation: the row is owned by Phase 12 Slice 8b, and the changes are the document plane's, not Phase 45's. The live half is not an argument but a measurement taken today while driving 45-08's scenario: `POST /agent/run` SSE round-trips across three conversations (`01a004e0-843f-764e-9272-e6327a130a27`, `01a00564-e2e2-7114-9fd1-2f6f9feb94c3`, `01a0057d-0988-7293-a54a-893175900718`) on build `a85627198`, each returning the full event lifecycle: `RUN_STARTED`, the complete REASONING_* sequence (`REASONING_START` -> `REASONING_MESSAGE_START` -> `REASONING_MESSAGE_CONTENT` -> `REASONING_MESSAGE_END` -> `REASONING_END`; 529 content events in one turn alone), `TEXT_MESSAGE_START/CONTENT/END`, `TOOL_CALL_START/ARGS/END/RESULT`, `STATE_DELTA`, `CUSTOM`, and `RUN_FINISHED` carrying `outcome`. `POST /api/conversations` returned 201 on each. The reasoning and text data planes were confirmed cleanly SEPARATED at the transport, which is what let 45-08 measure the SC#5 leak by channel rather than by eye. Idempotency enforcement was exercised too: a duplicate `Idempotency-Key` was refused rather than re-executed. Prior 2026-08-13 **RE-ATTESTED 2026-08-13 (metric-neutral; two test assertions, no handler).** The matching paths are `internal/agui/approvals_api_unit_test.go` and `internal/agui/audit_api_test.go`, and the whole diff in both is an assertion comparing against `redact.Placeholder` instead of a hard-coded `[redacted]` literal. They went red because the credential redactors were unified onto one table and the marker standardised on the ledger's upper-case spelling; the redaction itself was CORRECT in both cases, returning `api_key=[REDACTED]` and `postgres://[REDACTED]`, so nothing this row measures - the live SSE round-trip, the GET snapshot, the REASONING_* lifecycle, `translator.go` mutation - has an input in the diff. No handler, no projection, no wire field and no byte of non-test `internal/agui` changed. Coverage cannot move either: both files are tests asserting the same branches on the same inputs. Fresh witness: `go test -count=1 ./internal/agui/` green (26.6s). Prior 2026-08-09 **RE-ATTESTED 2026-08-09 (metric-neutral).** The matching path is `.github/workflows/ci.yml`, and the edit ADDS the `ingest-sidecar-test` job and nothing else — no existing job, step, env var or `needs:` edge was touched. `internal/agui/**`, `cmd/aura/serve.go`, `scripts/agui_smoke.sh` and `scripts/agui_boundary_check.sh` are byte-unchanged, so the SC1/SC3 live SSE round-trip, the GET snapshot, the REASONING_* interleave and the translator mutation score have no input that could move. Prior **RE-ATTESTED 2026-08-08 (metric-neutral for the measured dimensions).** `internal/agui` gained the file-manager mount (`/api/filemanager/*`: listing, direct stream, upload, create/rename/move/copy/delete) and LOST the whole `/api/documents` + storage-orphans surface. The SSE round-trip, the GET snapshot and the REASONING_* lifecycle share no code with either, and `translator.go` is unchanged, so the live round-trip and mutation figures cannot move. Coverage is the one number that shifts on paper: the new routes arrive WITH unit tests (all seven verbs, the auth/503 refusals, the sandbox-CSP and attachment-disposition assertions) and the deleted routes took their tests with them. Prior 2026-08-06 **RE-ATTESTED 2026-08-06 (data plane metric-neutral; COVERAGE LEG DEFERRED, not re-measured).** Matching paths are `cmd/aura/serve.go`, `internal/agui/documents_api.go` and `internal/agui/documents_api_test.go`. The measured data plane is disjoint from all three: the live SSE round-trip on `POST /agent/run`, the GET snapshot and the `REASONING_*` lifecycle run through the agent-run translator, and no byte of it changed. The document catalog REST surface is a separate endpoint group. **However this row also carries an `internal/agui` coverage leg, and that leg is NOT attested here:** `documents_api.go` and its test both changed, and no coverage run was performed in this range, because the coverage gate provisions a database and the live deployment is mid-E2E. Re-establishing it requires `bash scripts/coverage_docker.sh` with the stack up. Treat the coverage number as stale until then. Prior **RE-ATTESTED 2026-08-03 (coverage leg RE-MEASURED; gateway data plane metric-neutral).** `internal/agui/**` now exposes the identity-wide ArcadeDB memory graph through the strict `overview`/`expand` contract and ArcadeDB Studio's graph serializer; it does not alter `POST /agent/run`, GET message snapshots, the REASONING_* lifecycle, or `translator.go`. The full `db_integration` profile re-measured `internal/agui` at **3925/4588 = 85.5%** and the full owned surface at **23474/27152 = 86.4%**. Live authenticated Playwright rendered **59 Entity nodes / 34 FACT edges** and RID expansion on desktop and mobile; WSL race is green for `internal/agui`. The matching `ci.yml` edit is comment-only. Prior **RE-ATTESTED 2026-08-03 (metric-neutral).** The matching path is `.github/workflows/ci.yml`, and the only edit in it is a new `apt-get install -y poppler-utils` step added to the unit-test, db_integration and coverage jobs so the document card's PDF text extractor exists on the runner. The db_integration job that carries this row's live SSE round-trip runs the same command against the same code after it; nothing in `internal/agui/**`, the REASONING_* lifecycle, the `translator.go` mutation target or any threshold here changed, so none of its numbers can move. Prior **RE-ATTESTED 2026-08-02 (metric-neutral).** Two matching paths, both from deleting the caller-less cross-encoder rerank leg. Under `internal/agui/**`: `settings_api.go` loses two words from its package comment's knob list and no statement changed, while `settings_api_test.go` and `settings_api_branches_test.go` swap the retired `AURA_RERANK_MODEL` fixture key for the equally allowlisted non-secret string key `AURA_TTS_MODEL` — same handlers, same branches, same subtest count — so `internal/agui` coverage has no line to gain or lose. In `.github/workflows/ci.yml`: the `AURA_RERANK_BASE_URL` export and the `go vet -tags rerank_integration ./internal/rerank/` step are gone, and neither is read by the AG-UI job. The live SSE round-trip, the GET snapshot, the REASONING_* lifecycle and `translator.go` are untouched. Witnesses: `go build ./... && go vet ./...` clean; `go test ./internal/agui/` green (11.8s). Prior **RE-ATTESTED 2026-07-30 (metric-neutral).** The sole matching edit is the quoted Agent Memory CI placeholder in `.github/workflows/ci.yml`; it changes YAML scalar typing for the downstream Memory MCP job and does not modify the AG-UI job, SSE translator, `/agent/run`, snapshot route, reasoning lifecycle, coverage command, or mutation command. Fresh WSL witness: `go test -race -count=1 ./internal/agui/...` green. No live SSE or mutation re-run is claimed by this metric-neutral workflow attestation. Prior **RE-ATTESTED 2026-07-28 (data plane metric-neutral, coverage leg RE-MEASURED)** - the matched path is `internal/agui/**`, and what changed there is the governance REST surface (the skills read/write/delete seams and their tests), not the SSE data plane: `translator.go` and the `REASONING_*` lifecycle are untouched by this range, so the live round-trip and the translator mutation legs have no changed input and cannot move. `cmd/aura/serve.go` and both `scripts/agui_*.sh` are likewise unchanged. The coverage leg IS part of this row's metric and the package did gain code, so it was re-measured rather than asserted: `go test -cover ./internal/agui/` green at 80.7% of statements (UNTAGGED - the coverage gate's tagged matrix is the number the 85% owned-surface floor reads, and it is not re-run here). Prior **RE-ATTESTED** - the ONLY matching changed path is `.github/workflows/ci.yml`, and the edit is purely additive: one new step in the Memory MCP job that runs the vendored agent-memory pytest suite inside the image that job already builds. No existing job, step order, trigger, env or gate threshold changed, and no AG-UI source or test file is in the pushed range (not `internal/agui/**`, not `cmd/aura/serve.go`, not `scripts/agui_smoke.sh`, not `scripts/agui_boundary_check.sh`), so the live SSE round-trip, the GET snapshot, the REASONING_* lifecycle, `internal/agui` coverage and `translator.go` mutation score cannot have moved. The push also carries `docker/agent-memory/**` (schema DDL routing) and two new `internal/adaptive/**` test files, neither of which this row's globs match. Prior **RE-MEASURED 2026-07-25** - Phase A Task 6 changes this row's owned surface: `POST /api/approvals/{token}/resolve` now calls `Runner.SubmitAnswer` (single) instead of `SubmitAnswers` and answers **200 + `{outcome, remaining}`** instead of 204, projecting the runner's `ResolveDirective` so the cockpit renders the same decision Telegram does; the `Runner` seam in `server.go` gained the `SubmitAnswer` method. Re-MEASURED rather than attested because a route's status and body changed: `internal/agui` **85.0%** (>=85) on the `db_integration` tier against a disposable DB (`aura_phasea`, created and dropped; the live `aura` DB untouched). The measured data plane is untouched — `translator.go`, `POST /agent/run`, GET snapshots and the REASONING_* lifecycle are byte-for-byte unchanged, so the translator mutation + live-SSE evidence stays neutral. Two pre-existing tests that asserted 204 were updated with the reason inline; the contracts they pin (owner gate, idempotency operation-context forwarding) are unchanged. Witnesses: `go test -race ./internal/agui/` green under `goleak.VerifyTestMain`; `go build ./...` + `go vet` green. NOTE: 85.0% sits exactly on the floor — the next change to this package must re-measure, not attest. Prior **RE-ATTESTED 2026-07-24** - Amendment #93.2 touches `internal/agui/**` only to preserve adaptive identity-deletion fencing; SSE translation, `POST /agent/run`, snapshots, and REASONING_* ordering are unchanged, so the gateway metric is neutral. WSL AG-UI unit/race plus full lint/vet/build are green. Prior **RE-ATTESTED 2026-07-23** - the matching changed path is `internal/agui/**` (commit a93648c5): an additive idle SSE-comment heartbeat (`:hb`, `AURA_AGUI_SSE_HEARTBEAT_SEC` default 15) in the streamSSE drain loop — protocol-invisible comment frames between event writes; translator, SSE event semantics, POST /agent/run, and REASONING_* lifecycle untouched (the cockpit parser was already comment-tolerant). Witnesses: `go test ./internal/agui/` + WSL `-race` green; LIVE SSE round-trip today — POST /agent/run streamed a full tool-turn with 3 `:hb` comments interleaved and intact usage STATE_DELTAs (prompt_tokens 14665, cache_hit 14428), zero frame corruption, no RUN_ERROR. Prior **RE-ATTESTED 2026-07-22** - Phase 39 changes `cmd/aura/serve.go` only at private observability startup: it passes the existing database pool to a retention-backlog observer. AG-UI routes, SSE translation, message snapshots, reasoning events, and the authenticated gateway are untouched, so the row of record stands metric-neutral. Fresh witnesses: `go test ./... -count=1`, `go build ./...`, `go vet ./...`, Linux `-race` for the affected daemon boundary, and the complete disposable Postgres/Neo4j matrix at **85.4%**. Prior **RE-ATTESTED 2026-07-21** - Amendment #87 rewires web onboarding off Agent.md onto the memory MCP: `onboarding_provision.go` (`StatusCompleted`/`StatusSkipped`/`persistProfile`) + `onboarding_session.go` store the operator profile as memory entities/facts/preferences + sentinel facts scoped by `user_identifier`. Zero SSE round-trip (`POST /agent/run`), GET-snapshot, REASONING_* lifecycle, or `translator.go` surface changed — onboarding provisioning is a distinct API plane from the agent-run data-plane. `internal/agui` re-measured **86.1%** (>=85%) on the full db+neo4j matrix (`scripts/coverage_docker.sh`). Prior **RE-ATTESTED 2026-07-20** - the matching `internal/agui` change is test-only: `conversations_api_test.go` gained a `LastInputTokens == 11` assertion on the existing `GET /api/conversations/{id}` aggregate test (BUG-8 context-gauge fix; the new `LastInputTokens` field lives in `internal/conversations`, not `internal/agui`). No `internal/agui` production code changed — the SSE translator (`translator.go`), `POST /agent/run`, GET snapshots, and REASONING_* lifecycle are byte-for-byte unchanged, so the `translator.go` mutation + live SSE evidence stays metric-neutral and `internal/agui` combined coverage can only hold or rise (last full-matrix measurement 85.8% at 37F-13, >=85%). Local witness: `go test -tags db_integration -run TestConversationsAPI_GetAggregates ./internal/agui/` green on a disposable DB. Prior **RE-ATTESTED 2026-07-18** - The 2026-07-18 pushed range touches this row's `.github/workflows/ci.yml` glob (a TEMPORARY calm-prism Playwright `--update-snapshots` snapshot-harvest step, reverted in the following commit) and lands the rest of Phase 37F's share surface under `internal/agui` (`share_api.go` / `share_api_internal.go` / `share_api_public.go` / `share_export.go` / `share_service.go` + the in-handler `share.public` capability gate in `handleShareCreate`). None of it touches the measured AG-UI data plane: the SSE translator (`translator.go`), `POST /agent/run`, GET snapshots, and the REASONING_* lifecycle are byte-for-byte unchanged, so the `translator.go` mutation + live SSE evidence stays metric-neutral. `internal/agui` combined coverage was re-measured on the full `db_integration` matrix at **85.8%** at the 37F-13 SC4 close-out (>=85%), and the CI Skills (db_integration-only) coverage gate is green on this range — the floor holds. Prior **RE-MEASURED 2026-07-17** - Phase 37F added a fourth `UNION ALL` leg in `internal/agui/audit_store.go`, projecting append-only `aura.share_audit` events into the existing per-identity admin activity feed; `share_audit_union_test.go` proves `Source="share"` plus the projected action, target, and tier against disposable PostgreSQL. Fresh `go test -race -tags db_integration -cover -count=1 ./internal/agui` passed at **86.5%** coverage (>=85%). The SSE translator, `POST /agent/run`, GET snapshots, and REASONING_* lifecycle are unchanged, so the historical translator mutation/live data-plane evidence remains metric-neutral. Prior **RE-ATTESTED 2026-07-14** - Phase 42 (LLM conversation compaction, 42-10 terminal) ADDED to `internal/agui` five new `POST /api/conversations/{id}/compact*` routes (`conversations_api.go`: `handleCompactPreview`/`handleCompactRestore`, owner-gated via `GetForIdentity`, bounded body via `MaxBytesReader`) + a `CompactService` interface and `SetCompactService` setter (`server.go`); `cmd/aura/serve.go` wires `aguiServer.SetCompactService(...)` alongside the pre-existing route registrations. This is a self-contained new route subtree — the SSE translator (`translator.go`), `POST /agent/run`, GET snapshots, and REASONING_* lifecycle (the measured mutation/coverage surface) are byte-for-byte unchanged (`git diff --stat` shows zero lines touched in `translator.go`). `.github/workflows/ci.yml` also gained four new blocking `compaction-*` jobs appended after `web-e2e` — no existing job (including the AG-UI live smoke steps) was modified. Local witness: `go build ./... && go vet ./...` green; `go test -race ./internal/conversations/compaction_eval/...` green. Prior **RE-ATTESTED 2026-07-10** - Phase 37D (composer skill-picker) ADDED to `internal/agui` the lean `GET /api/composer/skills` read route (`composer_api.go`, plain RequireAuth global active-skills snapshot — deliberately NOT `governance.read`, the D-03 403 trap) + the `aura.skill` pinned-skill decode & Mechanism-A prepend (`governance_seam.go` + `server_run_request.go`: prepend the exact `UseAuthorityFrame`+body to the model message via the existing `TurnWithModelUserMessage` seam, zero runner change) + `server.go` wiring; the SSE translator (`translator.go`), `POST /agent/run`, GET snapshots, and REASONING_* lifecycle (the measured mutation surface) are unchanged. `internal/agui` re-measured in the 37D-05 terminal gate: owned-surface **85.5%** aggregate / `internal/agui` **86.8%** (>=85) on the full db_integration+neo4j_integration matrix (isolated throwaway DB, `-count=1`), composer handlers 92-100% func-cov, `-race` clean; live E2E `composer-skills.spec.ts` **8/8** green against the rebaked container (`aura.skill` wire proof, exact equality). Prior **RE-ATTESTED 2026-07-09** - Phase 37C (web voice lane) ADDED `internal/agui/voice_api.go` (three identity-scoped voice handlers `POST /api/tts` / `POST /api/stt` / `GET /api/voice/capabilities` + the `SetVoice` setter + `registerVoiceRoutes`) + `voice_api_test.go`; the SSE translator (`translator.go`), `POST /agent/run`, GET snapshots, and REASONING_* lifecycle (the measured mutation surface) are unchanged. `internal/agui` re-measured in the 37C-06 terminal gate: owned-surface 85.4% (>=85) on the full db_integration+neo4j_integration matrix, voice handlers 91-100% func-cov, `-race` clean; live E2E `voice.spec.ts` 8/8 green against the rebuilt container. Prior **RE-ATTESTED 2026-07-08** - the pushed range adds the two-phase password-reset security-question reveal + same-password 409 + the public `/question` route to `internal/agui` (`password_reset*.go`) and the `cmd/aura/serve_webui` public-route allowlist; the SSE translator (`translator.go`), `POST /agent/run`, GET snapshots, and REASONING_* lifecycle are unchanged (the measured mutation surface). Local witnesses: `go test ./internal/agui/ -count=1` green incl. the PasswordReset suites; `go test -race ./internal/agui/` green in WSL; live container E2E confirmed `/question` returns a neutral 403 for a bad code and 200 `{question}` for a valid injected code. Prior **RE-ATTESTED 2026-07-06** - Phase 36 gap-closure (36-14/16/17) added to internal/agui the HI-02 deactivation deny in RequireAuth, the CR-01 isolation-coupling provisioning refusal + server_production config gate, and the LO-02 blank-principal auth guard; the SSE translator (translator.go), POST /agent/run, GET snapshots, and REASONING_* lifecycle are unchanged (the measured mutation surface). internal/agui coverage re-measured 86.7% (>=85), lifting translator.go coverage too. Local witnesses: go test -race ./internal/agui/ green; -race -run Deactivat PASS. Prior **RE-ATTESTED 2026-07-03** - Phase 35 (35-05) touched `cmd/aura/serve.go` only to wire the gateway crash-orphan reconciler and relocate cron-dispatch assembly into `serve_dispatch.go`/`serve_drain.go` (behavior-preserving split for the 600-LOC cap); `internal/agui/**` was NOT touched, so the SSE translator, `POST /agent/run`, GET snapshots, and REASONING_* lifecycle are unchanged. Local witnesses (WSL2): `make quality` green (full `-race` unit suite `go test -race -count=1 $(bash scripts/go_packages.sh)` incl. `internal/agui`, plus build/vet/file-size/`golangci-lint` 0 issues/deadcode/govulncheck); owned-surface coverage 86.1% >= 85% via `scripts/coverage_gate.sh` (db+neo4j, mcp-neo4j-cypher containerized). Prior **RE-ATTESTED 2026-07-01** - replay/display snapshot projection changed to strip legacy model-only Aura context envelopes from visible user turns while preserving persisted model history. WSL2 witnesses: `go test -race -count=1 $(bash scripts/go_packages.sh)`, `npm run test` (981/981), `npm run lint`, `npm run typecheck`, `npm run build`, and live Docker-backed Playwright (`AURA_E2E_LIVE_RAG=1 npx playwright test e2e/live-rag-replay.spec.ts --project=chromium --reporter=line`) against `http://127.0.0.1:9080` uploaded `/mnt/d/tmp/Rag_docs/Corso Base Robot.docx`, observed `document_search`, and replayed desktop + 390x844 mobile with no `knowledge_base` / `User message:` leak. Go gates: vet/build/file-size/agui-boundary/deadcode/golangci-lint/govulncheck green. Prior 2026-06-29: onboarding provision/settings/governance API and shell UI changed; SSE translator, `/agent/run`, GET snapshots, and REASONING_* lifecycle behavior are unchanged. Local witnesses: `go test -count=1 ./cmd/aura`, targeted LoginPage Vitest split; full repo gates rerun before push. Prior **PASS** — re-baselined 2026-06-28 (Phase-30 retrieval-hardening RET-05 touched `.github/workflows/ci.yml` to add a rerank/retrieval `go vet` compile-floor + `AURA_RERANK_BASE_URL` export; zero SSE translator, `/agent/run`, GET snapshot, or REASONING_* lifecycle production surface changed, so the measured AG-UI metrics cannot move. Local witness: `go test -race ./internal/documents/ ./internal/eval/ -count=1` green). Prior 2026-06-21 (Calendar-connect patch added authenticated `/api/connect/pim/admin/*` proxy handlers and cockpit route mounting; SSE translator, `/agent/run`, GET snapshots, and REASONING_* lifecycle are unchanged. Local witnesses: `go test ./internal/agui ./internal/config ./cmd/aura -count=1` and `npx vitest run src/governance/__tests__/CalendarConnect.test.tsx src/governance/__tests__/governanceApi.test.ts src/governance/__tests__/SkillsBoard.test.tsx --coverage.enabled=false`). Prior 2026-06-20 (Phase 28 governance/onboarding follow-up touched `internal/agui/onboarding_provision.go` for non-secret nil-port diagnostics and `cmd/aura/serve.go` for provisioning-only Authula wiring; zero SSE translator, `/agent/run`, or REASONING_* lifecycle production surface changed. Local witnesses: `go test ./...`, focused `go test ./internal/agui -run TestProvision -count=1`, `docker compose up -d --build aura`, and a live authenticated onboarding provision probe returned HTTP 200 with Telegram deep-link + QR). Prior 2026-06-19 (CI workflow-only Garage bootstrap for browser asset-upload E2E; local 2026-06-19 Playwright matrix passed 54/54 after Garage/CORS bootstrap and run 27812067983 passed db_integration plus AG-UI gateway live smoke before the missing-Garage web E2E failure this commit fixes; zero SSE translator/REASONING lifecycle production surface changed). Prior 2026-06-18 (CI-only Node 24 workflow update: `setup-node@v6` + `paths-filter@v4` with `AURA_CI_NODE_VERSION=24.x`; current run 27745323083 passed db_integration, the AG-UI live smoke, scheduler chaos, and Web E2E; zero SSE translator/REASONING lifecycle production surface changed). Prior 2026-06-17 (Phase-25 chat/approval surface added to `internal/agui`: new `conversations_api.go` (CHAT-02 REST) + `approvals_api.go` (APRV-01/02/03 cross-thread resolve) + the `ConversationStore`/`ApprovalStore` server seams; `internal/agui` combined coverage re-measured **91.2%** live via `go test -tags db_integration -race -p 1`, the new REST + approvals routes covered by their db_integration suites (CI "Integration tests" job green); the SSE translator + REASONING_* lifecycle + `translator.go` are unchanged — the D-01 cockpit reasoning-on flip is a `Translate(…, true)` call-site argument, not a translator change). Prior 2026-06-16: run 27617744760 passed db_integration, `AG-UI gateway live smoke (FakeClient/degraded leg)`, and coverage; `bash scripts/agui_boundary_check.sh` passes locally. Touched files were CI wiring, `cmd/aura/serve.go` web-shell mounting, and boundary-script path updates, not the SSE translator or REASONING_* lifecycle. Prior 2026-06-15: CI-config touch only — an `AURA_ACCESS_TOKEN` CI placeholder added to `.github/workflows/ci.yml` (Phase-17 caddy interpolation), zero SSE/REASONING/translator surface. Prior 2026-06-13: the owned-path touches were O-05's new `internal/agui/readiness.go` (`/readyz` + PG/Neo4j probes, with its own `readiness_test.go`) + `cmd/aura/serve.go` wiring; the SSE round-trip, REASONING_* lifecycle, and `translator.go` are untouched, and the CI db_integration agui tier re-gates the package. Prior: agui_smoke.sh LIVE leg 2026-06-12 on pushed commit 3fb2be42: RUN_STARTED -> REASONING_START...REASONING_END (precedes first TEXT_MESSAGE_START, #57) -> RUN_FINISHED + MESSAGES_SNAPSHOT >=1 message (SC3). One provider-flake retry (first attempt stalled pre-RUN_FINISHED). Prior evidence: **PASS** - `AGUI_SMOKE_LIVE=1 scripts/agui_smoke.sh` green with `AURA_SHOW_REASONING=1`: RUN_STARTED -> REASONING_START -> REASONING_MESSAGE_START -> REASONING_MESSAGE_CONTENT* -> REASONING_MESSAGE_END -> REASONING_END -> TEXT_MESSAGE_START -> TEXT_MESSAGE_CONTENT* -> TEXT_MESSAGE_END -> STATE_DELTA -> RUN_FINISHED; GET `MESSAGES_SNAPSHOT` and 404 checks pass. Historical `translator.go` mutation 76.2%; owned-surface coverage refreshed at **85.9%**. | Phase 12 Slice 8b | `internal/agui/**`, `cmd/aura/serve.go`, `scripts/agui_smoke.sh`, `scripts/agui_boundary_check.sh`, `.github/workflows/ci.yml` |
| Snippet-reuse steady-state E2E (CAP-08.1 / D-03) — pre-seeded snippet -> ONE reuse run via production `runner.Runner` (Deps.ToolInvocations wired) -> 2nd-run-equivalent ledger window + fresh .xlsx, real DeepSeek-V4 | <=6 tool dispatches (`event_kind='end'`) AND wall-clock <40s on the reuse run (grounded by D-03, NOT distinct-request_id) / fresh .xlsx opens+today / coverage >=85% / mutation >=70% on new handlers | 2026-08-23 | **RE-ATTESTED 2026-08-23 (metric-neutral; one PURE EXTRACTION in the skills package, one ADDITIVE tool elsewhere).** Two paths under this row's globs changed. (1) `internal/skills/frontmatter.go` exports `SplitFrontmatter`: the CRLF normalization that lived in `parseFrontmatter` moved INTO the splitter and `parseFrontmatter` now calls it. Same bytes in, same bytes out, one call deeper — a pack's `commands/*.md` opens with the identical fence a SKILL.md does, and re-implementing that rule would have been a second, drifting reading of the same convention. No loader rule, validator, writer, audit path, snippet runtime or xlsx flow is in the diff. (2) `internal/agent/tools/pack.go` is a NEW read-only deferred tool (`plugin_pack`); it adds a file to that directory but touches no `skill*.go`, and a tool that resolves a public git repository has no input to anything this row measures. Coverage re-measured rather than asserted, untagged `-count=1`: `internal/skills` **84.4%**, `internal/agent/tools` **86.3%** — the skills figure is the same untagged number recorded on 2026-07-31 (84.8%) and 2026-07-28 (84.4%); the gate's own `db_integration` tier, which is the stricter one the 85% floor reads, was not re-run here. Witnesses: `go build ./...` clean; `go test -count=1 -race ./internal/skills/ ./internal/agent/tools/ ./cmd/aura/` green in WSL (Linux, the environment Aura actually runs in); `golangci-lint run ./internal/packs/... ./internal/agent/tools/... ./cmd/aura/...` **0 issues**. The E2E, mutation and tagged-coverage legs keep their prior dates: none was re-run in this range. Prior**RE-ATTESTED 2026-08-16 (metric-neutral; both matching paths are test files).** The two paths in this range under this row's globs are `internal/skills/audit_store_integration_test.go` and `internal/toolinvocations/store_integration_test.go`, both build-tagged integration tests. No `internal/skills` runtime file, no `internal/agent/tools/skill*.go` and no `internal/toolinvocations` runtime file changed, so the steady-state snippet-reuse E2E this row measures — the ledger window, the reuse path through `runner.Runner`, and the fresh `.xlsx` — runs over unchanged code. Witness: `go build ./...` clean at this HEAD. Prior 2026-08-13 **RE-ATTESTED 2026-08-13 (metric-neutral; `action=use` and the ledger are untouched).** The matching path is `internal/agent/tools/skill.go`, where the read verb's `Description` becomes a constant and the installed-skill catalogue moves to the `messages[1]` always-block (`cmd/aura/serve_adapters.go`). This row measures steady-state REUSE: a pre-seeded snippet driven through `action=use`, its dispatch window in `internal/toolinvocations/**`, and a fresh .xlsx. `actionUse` and `UseAuthorityFrame` are byte-identical here - `skill_read.go` is not in the diff at all - and `internal/toolinvocations/**` is untouched, so neither the dispatch count nor the wall-clock has an input in this range. One thing does change for a reuse flow, and it is prompt-side, not runtime: the catalogue now reaches the model from the always-block, and `skill` is callable without a `tool_search` first. That could only ever REMOVE a dispatch, and this attestation deliberately does not claim it: the row's <=6 floor was last measured RED (2026-07-30: 7 and 10 dispatches) and is NOT promoted here on an argument. Fresh witnesses: `go build ./...` clean; `go test -count=1 ./cmd/aura/ ./internal/skilladapters/ ./internal/gateway/` green. Prior same-day (PR #43, merged into master after this branch forked) **RE-ATTESTED 2026-08-13 (metric-neutral).** The only matching change in this range is the export rename `shellQuoteArg` -> `ShellQuoteArg` — one call site in `write_file.go`'s in-box sha256sum verification and one word in a `skill_read_test.go` comment — made so the verification ledger's box project probe shares ONE quoting rule instead of copying it. The function body, its output and every caller's behaviour are byte-identical; no skill loader, validator, writer, audit ledger, snippet runtime or xlsx path is in the diff. Witness: full untagged `go test ./...` green in WSL (Linux, the environment Aura actually runs in) on the merge commit. Prior 2026-08-12 **RE-ATTESTED 2026-08-12 (the reuse path is unchanged; `save_snippet` MOVED; the live E2E was NOT re-run).** The skills verb is split into a read-only `skill` and a write-only `skill_manage`. The path THIS row measures is steady-state REUSE — a pre-seeded snippet driven through `action=use` — and `use` stays on `skill` with byte-identical rendering (`UseAuthorityFrame` + body, host/box branch untouched), while `internal/toolinvocations/**` is not touched at all, so the ledger window and dispatch count of record cannot move. The honest caveat: `save_snippet` now belongs to `skill_manage`, so any flow that SAVES before reusing issues a different tool name — the row of record pre-seeds instead of saving, but a future save-then-reuse measurement must use the new verb. The row's existing non-green runtime status (the 2026-07-30 dispatch-floor FAIL) is preserved and NOT promoted. Fresh witnesses: full untagged `go test ./...` green; `go test -race ./internal/agent/... ./internal/gateway/ ./cmd/aura/` green. Prior 2026-08-06 **RE-ATTESTED 2026-08-06 (metric-neutral).** The sole matching path is `internal/toolinvocations/redact.go`, and the diff is one comment line: a hard-coded `=2048` was dropped from the prose naming `AURA_CONTEXT_PREVIEW_CAP_BYTES`, so the comment stops asserting a value that lives in configuration. No statement, no branch and no redaction rule changed; the compiled behaviour is identical. `internal/skills/**` and `internal/agent/tools/skill*.go` are untouched, so the snippet-reuse dispatch count this row measures cannot move. Prior **RE-ATTESTED METRIC-NEUTRAL 2026-08-03 (Agent Memory wire documentation only).** The sole matching skills-tree change is `internal/skills/embed/memory-aura/SKILL.md`, where retired flat provenance fields are replaced by the current nested `source:{run_id,memory_ids}` contract. Snippet materialization, `runner.Runner`, the xlsx path, dispatch ledger and the already-deleted paid E2E harness are untouched; the row's existing non-green runtime status is preserved. Prior **RE-ATTESTED 2026-08-02 for the runtime; THE E2E HARNESS IS DELETED.** The matching change is `internal/agent/tools/skill*.go`, where `SkillTool.Adaptive` and `orderSkillList` are removed so `action=list` ranks through `rankSkills` directly — ordering-neutral, since the adaptive plane sat at policy `off` and its wrapper resolved to that same static branch. `action=use`, snippet materialization, the export dir and the `internal/toolinvocations/**` ledger window are untouched, so steady-state reuse behaviour cannot move. The honest loss is that `internal/eval/skills_snippet_reuse*_cot_eval_test.go` is DELETED with the rest of `internal/eval`, so the live paid reuse E2E behind this row can no longer be reproduced; that glob is removed below. Witness: `go build ./... && go vet ./...` clean. Prior **RE-ATTESTED METRIC-NEUTRAL 2026-07-31 (tool_search rewrite) — the row's own INVALIDATED status from 2026-07-28 still stands.** The only match under `internal/agent/tools/skill*.go` is the `rankSkills` projection change described in the skills row: it moves which field of a synthetic Spec carries the skill description for the shared lexical ranker. Snippet reuse drives `skill info`/`skill use` by NAME, never through the ranked list, so the reuse path cannot move on it. The E2E itself is a live paid CoT run and was NOT executed here. Prior **FAIL - RE-MEASURED 2026-07-30.** Paid DB-backed production-runner gate on DeepSeek-V4-Flash produced a fresh, openable, today-stamped workbook in both measurements, but missed the <=6 dispatch floor: WSL scratch without production /workspace was 7 dispatches / 13.525s (one explicit cwd failure); production-shaped /workspace rerun was 10 dispatches / 20.444s. Wall-clock and artifact quality pass; dispatch efficiency fails. This row is honestly red and must not be quoted as passing. Prior **RE-MEASUREMENT DUE 2026-07-28.** Matched by `internal/skills/**`, `internal/agent/tools/skill*.go`, and by the eval files themselves - `internal/eval/skills_snippet_reuse*_cot_eval_test.go` are edited in this range. The measured claim is a steady-state reuse run driven through the production `runner.Runner` against a real model, ending in a fresh .xlsx; a range that deletes the skills pending stage and changes how a skill is installed and written cannot inherit that result. Not re-run here. Untagged witnesses today: `internal/skills` green at 84.4% of statements, `internal/agent/tools` green, and `internal/toolinvocations/**` is untouched by this range. Prior **RE-MEASURED LIVE 2026-07-27** - the matching changed paths are `internal/agent/tools/skill_read.go` (the `use` frame drops the escalation clause naming `sandbox_exec`, a tool deleted 2026-06-10 — amendment #96), `internal/agent/tools/skill.go` (schema wording), and `internal/skills/{validator,loader,snippet,snippet_usage}.go` (the write boundary now refuses an empty description, matching the loader; docblocks corrected on touch). Rather than argue neutrality on the row's own runtime, the snippet path was driven END-TO-END on the redeployed container under `single_user_hardened` — so the STRICT branch, which hands out the in-box path, not the host one: `save_snippet` -> `skills approve` -> `action=use` -> `tool_search("select:shell_exec")` -> `shell_exec {"command":"python3 '/skills/sommadue/sommadue.py'"}` -> **`42`, exit_code 0, 86ms**. Ledger window: **5 tool dispatches (<=6)**, turn wall-clock **~14s (<40s)**, real DeepSeek-V4-Flash via OpenRouter ($0.000631). The rendered frame carries `shell_exec` and no other tool name. This is a DIFFERENT payload from the row of record (a print-snippet, not the .xlsx workbook), so it is a live witness of the same runtime under the changed frame, NOT a replacement measurement — the `TestSnippetReuseE2E` row of record (endEvents=4, wallClock=32.331s, fresh workbook) stands. The same run also proved the description fix: the identical flow WITHOUT a description previously reported "saved" and "approved" and was then skipped by the loader; with the boundary check it is refused at save and, with a description, activates and runs as above. Prior **RE-ATTESTED 2026-07-26** - same two matching paths as the Skills row above — `internal/agent/tools/skill.go` and `skill_read.go` — adding the `SkillRoutingControl` list-ordering seam, which returns the static `rankSkills` order whenever `t.Adaptive == nil`, as it is in production (no assignment outside tests). This row measures snippet REUSE through the production runner: a pre-seeded snippet, one reuse run, the ledger window and a fresh .xlsx. None of that reads the `action=list` ordering, and `internal/toolinvocations/**` is unchanged, so the measured behaviour cannot have moved. Prior **RE-ATTESTED 2026-07-21** - Amendment #87 matches via `internal/skills/identity_root.go` — the same D-20/D-21 path-guard relocation into `internal/idroot` (behavior-identical). Zero `internal/toolinvocations/**` ledger, `runner.Runner` `Deps.ToolInvocations` wiring, or snippet-reuse E2E surface changed — the steady-state ledger-window + fresh-.xlsx evidence stands. `internal/skills` re-measured **88.1%** (>=85%) on the full db+neo4j matrix (`scripts/coverage_docker.sh`). Prior **RE-ATTESTED 2026-07-17** - Fast catalog search changes only the governance catalog read path (`catalog_search.go`, `installer.go`) and its tests/UI. The snippet-reuse runtime (`runner.Runner`, `Deps.ToolInvocations`, host/box `actionUse`, render frame, ledger, and `internal/toolinvocations/**`) is untouched, so the row-of-record E2E (`endEvents=4`, `wallClock=32.331s`, fresh/openable/today workbook) cannot move. Local witnesses: full untagged WSL suite green; WSL `-race` on `internal/skills`, `internal/agui`, and `cmd/aura` green; the full tagged matrix's `internal/skills` leg passed at **88.1%** combined coverage, and new catalog mutation scored **80.56%**. Prior **RE-ATTESTED 2026-07-14** - Phase 42 (LLM conversation compaction, 42-10 phase-close lint gate) touched `internal/skills/smoke_test.go` only to add a `//nolint:staticcheck` comment on a pre-existing SA5011 false-positive nil-check (see the Skills system row above for the full explanation); the snippet-reuse runtime (`runner.Runner`, `Deps.ToolInvocations`, host/box `actionUse` render, `internal/toolinvocations/**`) is untouched, so the steady-state row of record (`TestSnippetReuseE2E`, endEvents=4, wallClock=32.331s) stands — metric-neutral. Local witness: `go build ./internal/skills/... && go test -count=1 ./internal/skills/...` green. Prior **RE-ATTESTED 2026-07-10** - Phase 37D (composer skill-picker) touched `internal/agent/tools/skill_read.go` — the const-export rename `useAuthorityFrame`→`UseAuthorityFrame` (byte-identical value) reused by the composer Mechanism A; `actionUse`/`renderSnippetUse` emit the identical `UseAuthorityFrame`+body, so the snippet-reuse runtime (`runner.Runner`, `Deps.ToolInvocations`, host/box `actionUse` render, `internal/toolinvocations/**`) is unchanged and the steady-state row of record (`TestSnippetReuseE2E`, endEvents=4, wallClock=32.331s) stands — metric-neutral. Local witness: `go test -count=1 ./internal/agent/tools/ ./internal/skills/ ./internal/toolinvocations/` green. Prior **PASS** — re-attested 2026-07-08 after Phase 37-07 (c6b1a97a) routed skill-snippet exec into the box: the snippet-reuse steady-state E2E runs under a NON-strict (nil-router) profile where `SandboxRouter.Strict()` (nil-safe, `router.go:68` — `r != nil && r.profile.Strict()`) returns false, so `actionUse` renders the HOST export-dir path byte-for-byte as before 37-07 — the box-routing branch is never taken. `go test` and `go test -race` over `./internal/agent/tools`, `./internal/skills`, `./internal/toolinvocations` all PASS at HEAD, so the runtime row of record (`TestSnippetReuseE2E`, endEvents=4, wallClock=32.331s) is unchanged. Prior 2026-06-21: **PASS** — re-measured 2026-06-21 after shared `internal/skills` CodeQL hardening: snippet runtime/reuse paths were not changed; direct pending-writer traversal regressions and full `go test ./internal/skills -count=1` PASS, so the steady-state E2E row of record remains valid. Prior 2026-06-13: re-baselined (coverage campaign): same test-only `internal/skills` fake/unit-test touch (package coverage 81.7→**87.8%**, overall owned-surface **90.3%**); the snippet-reuse steady-state row of record (`TestSnippetReuseE2E`, endEvents=4, wallClock=32.331s) is a runtime-behavior measurement and is unchanged. Prior: **PASS** - shared `internal/skills/**` refresh on CI run 27408061937 (commit c413b9ef): unit/race, 60s fuzz, db_integration, snippet exec, mutation spot-check, and coverage gate all green; owned-surface coverage **85.1% >=85%**. This was a test-only coverage/CI-hardening touch, not a snippet-runtime change. The steady-state live E2E row of record remains `TestSnippetReuseE2E` from 2026-06-11: **endEvents=4 <=6**, **wallClock=32.331s <40s**, roundtrips(diag)=5, tools `[skill -> shell_exec -> fs_glob -> shell_exec]`; fresh/openable/today workbook `Mercato_Yahoo_2026-06-11.xlsx`. | Phase 18 Slice 7e | `internal/skills/**`, `internal/agent/tools/skill*.go`, `internal/toolinvocations/**` |
| Document routing recall@1 (machine card at ingest) | >=75% recall@1 on the reference corpus | 2026-08-25 | **SUPERSEDED FOR THE PRODUCTION PATH 2026-08-25.** Amendment #141 materially replaces the path this metric scored, so its prior machine-card recall is not current evidence. The replacement production gate passed **10/10 hard assertions**, including real-agent retrieval and identity isolation; historical notes follow. Prior **RE-ATTESTED 2026-08-23 (metric-neutral; the removed symbol had no caller at all).** `internal/documents/extensions.go` lost `isSupportedDocument`, a predicate over `supportedDocumentExt` with ZERO production callers — `SupportedDocumentExts()` is the live API — moved into `extensions_test.go`. Ingest, machine-card generation, chunking and routing are untouched, so recall@1 cannot move. What this does NOT prove: the reference corpus was not re-run. Witnesses: `go test ./internal/documents/` green; WSL `db_integration` matrix green at **86.4%** owned coverage. Prior **RE-ATTESTED 2026-08-17 (metric-neutral; nothing this row measures is in the diff).** This range touches `internal/documents/**` only through the catalog retirement (`d1847ab51`, -3039 lines): `catalog_store*.go`, `catalog_service.go`, `catalog_types.go`, `candidate_version.go`, `service.go`, `store.go` are deleted, `jobs_store.go`/`jobs_worker.go`/`pgvalues.go`/`types.go` adjusted to their absence. Both inputs to this metric are byte-identical at this HEAD: the card BUILDER (`internal/documents/filecard/**`) and the card READER used at retrieval (`internal/documents/retrieval_cards.go`) show an empty diff against `origin/master`. What was removed is the second, duplicate store of the same card — `catalog_store_card.go` wrote it into `aura.document_catalog`, which routing never read — so the corpus, the card text and the ranking are unchanged. This row's owner should still re-measure when the reference corpus next changes; this note claims only that the deletion cannot move it. The retirement itself was E2E-verified on the running stack by its own change. Prior 2026-08-15 **RE-ATTESTED 2026-08-15 (metric-neutral; dead-code removal, every input to the metric untouched).** Attested from the Phase 45 branch, where the document-plane pruning shares history; the row's owner is the document plane, so this is recorded as a cross-owner attestation rather than a measurement by that owner. The delta under `internal/documents/**` in this range is 1062 deletions against 22 insertions, entirely in `catalog_service.go`, `catalog_store.go`, `catalog_types.go`, `catalog_status.go`, `orphans.go` and `store.go`: the catalog CRUD surface no production path constructs, the storage-orphan reconciler nothing ever constructed, a JobStore narrowed to the four methods something calls, and a pipeline job-binding statement with no caller. What the metric actually reads did NOT move, verified file by file over the same range: `catalog_store_card.go` 0 changes, `retrieval_cards.go` 0, `embedder.go` 0, `internal/documents/filecard/**` 0, and `internal/db/migrations/008*_document_*.sql` 0. Card construction at ingest, the embedder and the routing/retrieval surface are byte-identical, so recall@1 over the reference corpus has no input in the range and cannot move. No fresh measurement was run: the repository ships no recall@1 harness (`scripts/eval/` holds only ragas), so re-measuring would mean building the harness rather than running it. Prior 2026-08-13 **RE-ATTESTED 2026-08-13 (metric-neutral; no production code changed).** The only delta under this row's glob is test infrastructure. Four tests in the package's db_integration tier called `db.Migrate` on whatever `AURA_DB_MIGRATE_URL` pointed at -- which applied 0095 to the live database on 2026-08-13 -- and four more opened `AURA_DB_URL` raw and INSERTed two service identities plus their documents straight into production. All eight now run on a per-test disposable database, and `seedCardIngestIdentity` takes the caller's pool instead of opening a second one that only agreed with the test's because both pointed at the same shared database. The card builder, the extractor, the chunker and the ranker are untouched, so nothing this row scores can have moved -- and this row's number remains UNKNOWN for the reason it already gives: the two harnesses compile but nothing runs them. Witness: whole tier green in 28.9s with the live database as the target and its state identical before and after (version=95 dirty=f, identities=2 documents=1 jobs=0), zero test identities left in production, zero leftover disposable databases. Prior 2026-08-08 **RE-ATTESTED 2026-08-08 (metric-neutral; routing inputs unchanged).** The card builder, the extractor and the chunker are untouched. What changed is WHICH objects reach them: the sweep now excludes Aura's own `identity/`+`share/` trees, and a non-textual file is routed away from Tika instead of throwing. Both remove rows that should never have been in the index — the operator's index held 8 IndexedDocument rows for 1 uploaded file, every chat attachment indexed twice under a second search_document_id — so the reference-corpus recall@1 measurement is unaffected while the live index gets strictly less wrong. Measured after: 8 rows -> 1. Prior 2026-08-06 **INGESTION RE-TUNED 2026-08-06 (fourth pass; the metric is now MEASURABLY DIFFERENT and still unmeasured).** Three changes that move what this row would score, all made on evidence from the running system rather than on judgement. (1) CHUNKING: the budget was 512 tokens and validated against the SAME constant, so the knob could never be raised — the default WAS the maximum. Docling had been warning on every conversion that the heading prefix alone was 637-1603 tokens and "will be split into multiple chunks and only included in the first chunk(s)", i.e. most chunks were stored WITHOUT the heading path that says which section they belong to, while the embed sidecar sat on 8192 tokens of context per slot. Now 2048, ceiling 4096 (the sidecar's -ub; above it a chunk is rejected, not truncated), and the embed batch drops 32 -> 8 to hold the per-request envelope at the calibrated ~16k. (2) OCR IS OFF by default (AURA_DOCUMENT_OCR_ENABLED). No engine in the pinned image reads this corpus: `auto` picks RapidOCR (Chinese/English), the bundled tesseract carries only eng+osd, and ocr_lang is never set — an Italian scan OCR'd to nothing ("RapidOCR returned empty result!") while costing minutes of CPU. The flag reaches DoclingConversionProfile and therefore the stage fingerprint, so text produced under one setting is never served as the other. (3) DOCLING RESOURCES: 2.0 CPUs -> 4.0, 4g -> 5g. Per-stage timings from aura.document_pipeline_stages make the case — convert **28.28s** vs chunk 0.02s, embed 0.63s, project 0.28s, activate 0.03s: conversion is **97%** of upload-to-searchable, and the container was pegged at 152% CPU against its 2.0 cap on an 8-CPU host. Every one of these changes the extracted text or the chunk boundaries, so ANY prior recall figure is now unbacked for a second reason on top of the empty corpus. The row stays UNKNOWN. When it is finally measured it must be measured AFTER a re-ingest under these settings. Prior **RETRIEVAL DEFECT FOUND AND FIXED 2026-08-06 (third pass; still not a measured number, and one earlier claim here is WITHDRAWN).** WITHDRAWAL FIRST: the second-pass note below says the 42P01 alias bug broke `document_search`, `aura docs search` and `GET /api/documents`. That is WRONG. `SearchDigests`, the method carrying that SQL, has ZERO production callers -- it is reached only from tests. `document_search` runs through `HostRetriever` on the `lexical-dense-card-cascade-v1` cascade. The alias bug was real and the fix stands, but its blast radius was the red `db_integration` tier, not operator retrieval; the claim was made from the method's doc comment without grepping for callers. WHAT IS ACTUALLY BROKEN, found by reading the operator's own transcript: the Postgres CARD leg returned nothing for every unscoped query. An absent scope means "every ready document" and reaches `RouteDocumentCards` as a NIL slice (`ResolveDocumentScope` returns nil early), which pgx encodes as SQL NULL; the predicate `$3::text[] = '{}'::text[] OR document.id::text = ANY($3::text[])` is then NULL OR NULL under three-valued logic, so EVERY row was filtered. Reproduced against a published document: nil scope returned 0 cards for every query while an empty non-nil slice returned the document. Fixed with `cardinality(coalesce($3::text[], '{}'::text[])) = 0`, and pinned by `TestRouteDocumentCardsAnswersAnUnscopedQuery`, which was confirmed to FAIL against the old predicate and pass against the new one. Two consequences, both operator-visible: (1) the leg whose whole purpose is to make a file findable by its machine card from the moment it lands contributed nothing on the common path -- measured live, `document_search("Clienti")` returned nothing for a ready, activated Clienti.xlsx while `document_search("Clienti.xlsx")` found it, so recall depended entirely on the ArcadeDB passage legs; (2) worse, the ArcadeDB-down fallback (`RetrievalCardOnly`) returns exactly these cards, so a degraded retrieval returned an EMPTY result set while reporting status `degraded_card_only` -- a total failure presented as a partial one. The recall@1 number itself is STILL unmeasured: the live corpus is empty (the operator deleted both documents), so this row remains UNKNOWN, not green. Prior **STILL DEFERRED 2026-08-06 (second pass; blocker (2) cleared, a THIRD blocker found and fixed, the number still not measurable).** The retrieval this row scores was not merely unmeasured, it was BROKEN. `SearchDocumentDigests` compared `v.pipeline_generation` against `aura.documents.pipeline_generation` inside a statement whose FROM clause aliases that table as `d`; once a table is aliased the original name leaves scope, so EVERY `SearchDigests` call -- `document_search`, `aura docs search`, `GET /api/documents` -- failed with `42P01 invalid reference to FROM-clause entry for table "documents"`, and Postgres named the fix in its own HINT. Reproduced against the LIVE database before anything was edited, then fixed in the query and its regenerated sqlc output. No recall number could ever have been produced while this stood, which is the real reason blocker (1) presented as an empty corpus. Blocker (2) IS cleared: two spreadsheets uploaded through the repaired ingest path both reached `ready` at `pipeline_generation = 1` with `candidate_activated` recorded in `aura.ingestion_events`, so the pipeline is proven end to end on live data. The operator then deleted both, so the corpus is empty again and the 22-query recall@1 measurement still has nothing to run against -- this row stays UNKNOWN, not green. Fixed on touch: `digestDocumentID` still read `metadata->>'search_document_id'` after 0093 promoted that id to a NOT NULL column (backfilled from exactly that key, de-duplicated per identity, held immutable by `aura.document_identity_immutable`), so a document catalogued without the duplicated metadata key was handed back its catalog uuid instead of the search id its contract promises. Witnesses: `make quality` green in WSL; owned-surface coverage **85.3%** (>= 85 floor) measured on a disposable `aura_cov`; `db_integration` green for `internal/db` and `internal/documents` after six fixtures were rewritten against the 0093 publish contract; the `musr_e2e` documents cross-deny subtest run live and green. To re-establish the number: ingest the baseline corpus, then re-run the 22-query recall@1 measurement on it. Prior **DEFERRED 2026-08-06 (UNVERIFIED, deliberately not re-attested as neutral).** 67 files under `internal/documents/**` changed in this range: the document status vocabulary was corrected against the constraints that close it, `DocumentVersionStatus` became a named type, and `documents_source_unique` was replaced by a partial unique index so a deleted document releases its source key. Migration 0093 was also applied to the live database. **This row cannot be re-measured right now and is not being claimed neutral.** Two blockers, both measured: (1) 0093 reclassified the three previously-`ready` documents to `failed / original_unavailable` because they had `active_version_id = NULL` and no retrievable version, so the live corpus is empty of `ready` documents and `aura.document_pipeline_stages` has zero rows -- there is nothing to measure recall against; (2) asset ingestion was found broken outright tonight (`invalid ingestion job identity id ""`, the request struct's `IdentityID` never set at the `cmd/aura` call site), fixed in commit `488fa6714`, and that repaired path has not yet been exercised end to end. To re-establish this number: ingest the baseline corpus through the fixed path, then re-run the 22-query recall@1 measurement on it. Until that runs, treat this row as unknown rather than green. Prior **RE-MEASURED 2026-08-03 (a PDF's words now reach the card, and the number moved).** Same corpus, same SQL, same 22 queries; one variable: `internal/documents/filecard` reads a PDF's text at ingest through poppler's `pdftotext`, which is already installed in the runtime image (`docker/aura/Dockerfile`, Amendment #88.1) and therefore runs in the SAME container as ingest — verified, not assumed, before the route was chosen. Capped at the first **20 pages** and **200 KB** of output, both stated in the card when they bite. BEFORE (title only): **4/22 = 18.2%**, MRR 0.182 — reproduced. Card WITHOUT PDF text, i.e. the state this row measured hours earlier, reproduced by hiding `pdftotext` from `PATH`: **18/22 = 81.8%**, MRR 0.841 — reproduced to the query. Card WITH PDF text: **20/22 = 90.9%**, MRR **0.932**. The two it fixes are exactly the two the prior note named as unroutable: `amministrazione digitale` and `comunicazioni elettroniche`. A SECOND, harder set of 8 queries answerable ONLY from inside a PDF — no term appears in any file name (`codice amministrazione digitale`, `codice comunicazioni elettroniche`, `proprieta industriale marchio registrazione`, `procedura civile disposizioni attuazione`, `bancoposta telecomunicazioni testo unico`, `ip55 protection converter`, `braking resistor option module`, `senato repubblica testo vigente`) — goes **0/8 → 8/8 = 100%**, MRR 0.000 → 1.000. All five Normattiva decrees are now routable by subject. The frequency-selected `Recurring terms:` line earns its place by measurement: without it that set is **5/8 = 62.5%** (it is what carries `marchio`/`registrazione` and the manual's `ip55`/`braking`/`resistor`), and it costs nothing on the 22 (90.9% with and without). COST: whole-corpus card build **0.66-0.76 s → 1.40-1.43 s** (same host, warm, median of 3), about +90 ms per PDF — dominated by process start and poppler's document open, not by the page cap: 8 pages vs 20 differ by 42 ms on the 830-page manual, and the small decrees cost the same at any cap. Two residual misses on the 22, both pre-existing and neither a card defect: `robot singolarita` at #2 behind a genuinely-about-robot-singularity docx, `fusibile 40 ampere` asking in Italian for a sheet that says FUSE. The sentence-query finding below is UNCHANGED at 0/6 — nothing here touched the query path. The corpus lives outside the repository, so re-measurement is MANUAL — CI cannot reproduce this number. Prior **MEASURED 2026-08-03 (new row; the document plane has had no quality row since the GraphRAG and vector-p95 rows were retired on 2026-08-02 with the graph store).** 17-file reference corpus (`D:/tmp/baseline-corpus`: 7 xlsx, 6 PDF, docx, pptx, epub, html), 22 agent-shaped Italian queries, ranked by the real `SearchDocumentDigests` SQL on a disposable Postgres at migration 0088. BEFORE (title only, which is all ingest wrote): **4/22 = 18.2%**, MRR 0.182. BEFORE plus the single agent-written digest that exists in the live deployment: 6/22 = 27.3%. AFTER the machine card: **18/22 = 81.8%**, MRR 0.841 — same SQL, same corpus, same queries, the card the only variable. The 4 residual misses are named and none is a card defect: `robot singolarita` ranks #2 behind a genuinely-about-robot-singularity docx; `fusibile 40 ampere` asks in Italian for a sheet that says FUSE in English; and the two Normattiva decrees are unroutable by subject because a PDF's text is glyph indices behind a font encoding and is not read at ingest (their cards say so, and they ARE routable by decree number and year from the filename). SEPARATE FINDING, unchanged by the card and NOT a card defect: the same six intents written as sentences score **0/6 before and after**, because `websearch_to_tsquery` ANDs every term and the `simple` configuration removes no stop words, so `quanti`/`a`/`del` must appear verbatim in the document vector. That is a query-path property; the model writes keywords, so it does not bite today. The corpus lives outside the repository, so re-measurement is MANUAL — CI cannot reproduce this number. | Document plane (machine card) | `internal/documents/**`, `internal/db/migrations/008*_document_*.sql` |

**MCP manager row supplemental attestation (2026-08-03).** The changed
`internal/agent/mcptools/**` policy now exposes exactly four model operations
(`memory_recall`, `memory_upsert_fact`, `memory_merge_entities`, `memory_forget`)
from the raw ten-tool ArcadeDB MCP while keeping six host operations available to
Aura but hidden from model planning. The replacement read contract and automatic
identity-scoped conversation context passed MRS **100.00/100**, the 25-query tool
retrieval gate at **25/25 top-1 and 25/25 recall@3**, three real first-turn AG-UI
conversations with **zero tool calls**, and the WSL unit/race tiers. Fresh operator
path latency is **p95 77.654 ms**.

---

## HNSW configuration baseline (amendment #20 cross-ref)

Aura uses `vector.hnsw.m = 32` (NOT Neo4j's default 16) for every `:Chunk`, `:Entity`, `:Community`, and `:AgentInsight` vector index, with `vector.hnsw.ef_construction = 200`. Rationale (from `.planning/research/SUMMARY.md` Honorable mentions row + amendment #20): higher M trades ingestion cost for recall headroom. Aura's corpus is bounded (≤ 100K chunks typical, ≤ 1M extreme) and `recall@5 ≥ 0.8 @ 100K` is non-negotiable per Phase 15 success criterion — `HNSW M=32` is the smallest M setting that achieves that target with safety margin on the 100K benchmark per the SUMMARY.md research convergence.

Effect on this snapshot: the GraphRAG recall@5 row above implicitly depends on `M=32`. Any future PR that lowers M (or any other HNSW knob) is a regression vector — the `quality_snapshot_gate.sh` CI gate WILL catch it via the path glob `internal/db/migrations/neo4j/**` (the schema is the witness).

---

## Agent Memory — blocking reliability score and live appliance latency

**Blocking snapshot.** The adopted contract is now Aura's ArcadeDB-backed MCP, evaluated by the
fail-closed `aura.agent-memory-eval/v1` suite. Missing, skipped, synthetic-only, cross-tenant,
unversioned, stale-wire, or unqualified-retrieval evidence fails the run; the release threshold is
strictly greater than 96.5.

| Metric | Value | Method | Last measured | Status |
|---|---|---|---|---|
| Memory Reliability Score | **100.00/100** | Full deterministic + live MCP suite, including initialize/list/call, temporal history, provenance, purge, isolation, abstention, runtime metadata and latency | 2026-08-03 | **PASS** (>96.5) |
| `internal/arcadedb` package coverage | **943/1016 = 92.81%** | Combined unit + `arcadedb_integration` profile against the live database | 2026-08-03 | **PASS** (>=85%) |
| Operator-path search latency | **p50 67.069 ms / p95 77.654 ms / max 86.340 ms** | 25 sequential cold-retained samples, each including PostgreSQL identity lookup, MCP initialize and `memory_search` | 2026-08-03 | **PASS** (p95 <=1000 ms) |
| Nonexistent-fact behavior | `facts: []`, `abstained: true`, `no_qualified_candidates` | Live CLI query for a deliberately fabricated claim | 2026-08-03 | **PASS** |
| Automatic conversation-memory context | **3/3 correct first-turn answers; 0 tool calls** | Real Authula/AG-UI conversations for `dvdmarchetto@gmail.com`; the identity-scoped digest was injected immediately before the current user turn | 2026-08-03 | **PASS** |
| Model-facing memory surface | **4 operations exposed / 6 host operations hidden** | Deployed manifest plus retrieval gate: 25/25 top-1 and 25/25 recall@3 | 2026-08-03 | **PASS** |
| Owned Go coverage matrix | **24004/27729 = 86.6%** | Disposable PostgreSQL `db_integration` run via `scripts/coverage_docker.sh`; `internal/arcadedb` is 92.72% in this matrix | 2026-08-03 | **PASS** (>=85%) |

**Runtime witness:** ArcadeDB 26.7.3, `arcadedb-mcp` 0.1.0,
`embeddinggemma-300M-Q8_0.gguf`, 768 dimensions. The candidate commit, dirty-file set,
tool-manifest hash and seeded-corpus marker are embedded in the generated report. This score
measures memory-system reliability, not LoCoMo answer accuracy, and supersedes the retired
Neo4j/Granite advisory snapshot.

---

## RAG answer quality (RAGAS) — gated/manual, not CI

**Answer-quality is graded by RAGAS faithfulness + answer_correctness, NOT a single 0–10 score** (Session-20 Item 3; spike 076). The retrieval rows above measure *which chunks come back*; this measures *whether the generated answer is grounded in them and actually answers*. A single 0–10 LLM judge is uncalibrated and context-blind — spike 076's naive judge rated a fluent hallucination 10/10 — so it is replaced by two decomposed, context-grounded metrics. Gated/manual + paid (NOT a per-PR CI step); harness + reference set in `scripts/eval/ragas/`, methodology in [`docs/rag-answer-eval.md`](rag-answer-eval.md).

| Metric | Value | Method | Last measured | Status |
|---|---|---|---|---|
| Faithfulness — grounded vs hallucinated | **1.000 vs 0.000** | RAGAS Faithfulness over the 5-case reference set (2 docs × grounded/hallucinated/partial), DeepSeek judge + granite embeddings; repeatable at temp 0 (stdev 0) | 2026-06-28 | ADVISORY — gated/manual eval |
| Answer_correctness — grounded vs hallucinated | **0.984 vs 0.236** (margin 0.75) | RAGAS AnswerCorrectness (factual F1 + granite semantic similarity) over the same set | 2026-06-28 | ADVISORY — gated/manual eval |

**Gate:** grounded faithfulness ≥ 0.8, hallucinated ≤ 0.4, answer_correctness margin ≥ 0.2 (thresholds in `scripts/eval/ragas/reference_qa.json`). Grow the reference set with real upload→chat cases; pair with a task rubric for human-facing answer quality.

---

## CI gate contract

The gate script `scripts/quality_snapshot_gate.sh` (implemented in Phase 20) does the following:

1. Parses this file's quality matrix table into `(metric, target, last_measured, last_value, owner_phase, ci_gate_path)` records.
2. Reads the PR/local changed-file set from GitHub base/head refs, local diff/untracked files, or the `AURA_QUALITY_CHANGED_FILES` self-test override.
3. For each snapshot row, evaluates whether ANY changed file matches the row's `CI gate path` glob (multiple globs comma-separated).
4. If a row matches: asserts the row's `Last measured` ISO date is greater than or equal to the base commit date, or the `AURA_QUALITY_BASE_DATE` override in self-tests.
5. On stale row: exits non-zero with explicit error `quality snapshot row '<metric>' stale - owner Phase X must re-measure and update before merge (amendment #20)`.

Exit codes: `0` (no matching row OR all matching rows fresh), `1` (one or more matching rows stale), `2` (malformed snapshot table — re-run after fixing).

The gate runs as a CI step `Quality snapshot gate` on every PR after Phase 20. Any matching measured row with an older `Last measured` date blocks the merge.

---

## How to update (operator runbook)

1. **Run the relevant pre-merge benchmark** for the row whose owner phase your slice belongs to. The benchmark command is documented in the phase's current PLAN.md or VALIDATION.md (for example, `scripts/memory_recall_bench.sh` for Phase 15 retrieval rows).
2. **Record the value** in the `Last value` column, replacing any unmeasured text.
3. **Update the `Last measured` column** to the ISO date of the benchmark run (`date -u +%Y-%m-%d`).
4. **Commit alongside the implementation change** — same PR, same commit if practical. CI will block if you separate them and the implementation lands before the snapshot bump.

---

## Cross-phase dependency note

Quoting user memory `feedback_aura_as_product` (cited by amendment #20): "max 2 fasi staged avanti, ogni wave gate su numeri". Concretely:

- Phase 5 ships → Sandbox escape rate row populated. Phase 6+ cannot stage further until this row is measured.
- Phase 6 ships → KV cache hit rate row populated. Phase 7-8 cannot stage further until measured.
- Phase 11 ships → Skill snippet exec success row populated. Phase 12-13 may stage in parallel since they don't depend on this metric.
- Phase 13 ships → MarkdownV2 escape fuzz row populated. Phase 14+ blocks until measured.
- Phase 15 ships → GraphRAG recall@5 AND Vector search p95 rows populated. End of v1 quality matrix population; v1.x phases inherit these as regression baselines.

A phase that ships with its row still unmeasured is a contract violation: the next phase's CI gate will fail every PR until the row is back-filled. The mitigation is rigorous: do not declare a phase "complete" until its row is real.

---

## Sandbox-Agent Pivot Detail

> The original Phase 5 bespoke SandboxEscapeBench row is historical. D-15/D-15b
> replaced the bespoke `internal/sandbox` sidecar and dedicated sandbox workflow with
> the local `sandbox-agent` container (`docker/sandbox-agent/Dockerfile`,
> `internal/sandboxagent`, and the `sandbox_exec` tool). The old bench targeted deleted
> paths and was removed during quick task 260604-c4l so it cannot be mistaken for
> current Gate-3 evidence.
>
> Current sandbox evidence lives in the Phase 8/08.1 validation artifacts and the
> sandbox-agent integration surface. The project-wide owned coverage floor is recorded
> separately below and is re-run via `make coverage`.

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| Sandbox-agent command execution | live pass | 2026-06-04 | Phase 8/08.1 validation evidence |
| Sandbox-agent workspace persistence | live pass | 2026-06-04 | Phase 8 validation evidence |
| Bespoke escape bench | removed | 2026-06-04 | superseded by sandbox-agent pivot |

**QEMU-arm64 tracked obligation (D-12 / Pitfall 4):** the arm64 leg runs the negative
tier + sidecar build under QEMU `--platform linux/arm64`. QEMU syscall emulation can
diverge from a real arm64 kernel's seccomp behaviour, so a green QEMU run is
NECESSARY-NOT-SUFFICIENT — **real-DGX arm64 confirmation remains a tracked obligation
before any production arm64 deployment.** It is NOT a per-merge gate.

---

## Phase 7 web-tools detail

> Populated by the Phase 7 Gate-3 checkpoint (07-04 Task 4). The unit-tier
> `internal/web` coverage is measurable in the authoring environment; the
> SSRF-classifier mutation score, the combined cross-tag coverage (unit +
> `web_integration`), and the live `web_search` p95 require the running SearXNG
> container + public internet and are recorded at the human-verify checkpoint.
> SSRF defense is the dominant risk surface — `ssrf.go` carries an independent
> mutation gate so a silently-weakened blocklist cannot regress unnoticed.

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| Owned-surface coverage gate (`internal/*`, `db_integration` tag) | ≥ 85% | 2026-06-04 | **87.4%** — PASS (`make coverage`, quick task 260604-c4l; measured when the matrix still carried a second tag, which no longer exists) |
| `internal/web` combined coverage (unit + `web_integration`) | ≥ 85% | 2026-06-02 | **91.5% combined / 91.0% unit** — PASS (was 82.0% / 80.5%). Focused test pass added disk-cache round-trip (getDiskLocked/setDiskLocked), both `Error()` methods, fetch cache-HIT (1 origin hit / 2 fetches), and search validHostname/domainAllowed/buildQuery tables. Remaining sub-100% is the convertNode A2 fallback + defensive dial-control arms + read-only-home newCache fallback, all left uncovered by design (no asilo nido). |
| `internal/web/ssrf.go` mutation (go-mutesting, killed) | ≥ 70% | 2026-06-02 | 94.4% (17/18; lone survivor is the unreachable metadataV6Pfx dead branch; ssrf.go untouched by the gap-closure) |
| Live `aura web tool web_search` p95 (SC#1) | ≤ 2s (advisory, env-tunable) | 2026-06-02 | TestSearch_Live PASS ~1.01s (under the 2s budget) |
| SC#3 SSRF block smoke (`scripts/ssrf_smoke.sh`) | 4/4 blocked, grep-clean | 2026-06-02 | 4/4 blocked_url, grep-clean |
| SC#2 live `web_fetch` clean markdown | clean MD, no chrome | 2026-06-02 | **PASS** — en.wikipedia.org/wiki/Knowledge_graph now returns clean markdown (content_md 36070 B → 16429 B): citation/references tail truncated, no `#cite_note`/`#cite_ref` anchors, no "From Wikipedia" boilerplate, no fragment-only links. Fixed by the AURA_WEB_FETCH_MAX_BODY_BYTES rename+5MB default (raw-body ceiling, not markdown cap) + html.go extraction cleanup. |
| golangci-lint (`golangci-lint run ./...`) | 0 issues | 2026-06-02 | 0 issues |
| `aura web doctor` (live stack) | reachable + JSON round-trip | 2026-06-02 | reachable: yes; JSON round-trip OK; status OK |

---

## Phase 8 Sandbox-Agent Detail

> Populated by the Phase 8/08.1 sandbox-agent pivot and validation artifacts. There is
> no dedicated sandbox workflow after D-15; the active CI workflow is
> `.github/workflows/ci.yml`, while sandbox-agent live checks remain operator/native
> daemon validation until the product adds a new dedicated sandbox-agent CI gate.

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| sandbox-agent live command execution | command succeeds | 2026-06-04 | Phase 8/08.1 validation evidence |
| sandbox-agent workspace persistence | file visible across calls | 2026-06-04 | Phase 8 validation evidence |
| `sandbox_exec` tool contract | non-deferred, command/args schema | 2026-06-04 | `internal/agent/tools/sandbox_exec.go` + registry tests |
| Owned-surface coverage gate (`internal/*`, `db_integration` tag) | ≥ 85% | 2026-06-04 | **87.4%** — PASS (`make coverage`, quick task 260604-c4l; measured when the matrix still carried a second tag, which no longer exists) |

The bespoke 2b session-manager/security-posture notes remain preserved in the planning
history. Current product behavior is the simpler sandbox-agent pivot: one local
container, loopback-only API, persistent `/workspace`, and no in-process Docker
lifecycle in Aura.

---

## Phase 9 Swarm E2E Detail (CAP-03 / SC#5 / D-22)

> The dual-gate live swarm E2E (`internal/eval/harness_swarm_e2e_test.go`,
> `TestSwarmE2E`, build tag `cot_eval`, OPENROUTER-gated) is the ONE legitimate skip:
> it is operator-run and PAID, NOT CI (no-skip-as-green — CI gates stay on
> unit/property/race/goleak). **Measured live 2026-06-04 — PASS** (8 runs to green,
> iteration log below). Timing gate semantics: the < 1.5× budget applies to the
> FAN-OUT wall-clock (swarm_spawn call → all reports back, the SC#1 quantity); the
> end-to-end turn additionally carries the parent's spawn-decision + synthesis LLM
> turns and is reported advisory. Multiplier operator-tunable via
> `AURA_EVAL_SWARM_TIMING_BUDGET` (default 1.5; correctness gates are not tunable).
>
> A NATURAL prompt (NO "swarm"/"parallel" word) must drive the model to fan out on its
> own. Hard floor = deterministic ground truth; judge = ≥90% average over four
> dimensions.

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| Workers spawned via tool_use (autonomous parallelization) | ≥ 2 | 2026-06-04 (live run 8) | 2 (w1 11.6s ‖ w2 15.9s, overlapped, both ok) |
| Expected facts present in aggregated answer | present | 2026-06-04 (live run 8) | present (12 and 13 in the synthesis) |
| Self-mail read-back via mounted MCP (`mail__search_emails {query}`) | found | 2026-06-04 (live run 8) | found (per-run tag round-trip) |
| Self-WhatsApp read-back via mounted MCP (`whatsapp__list_messages`, `<phone>@s.whatsapp.net`) | found | 2026-06-04 (live run 8) | found (per-run tag round-trip, bridge-sent JID) |
| Fan-out wall-clock vs single-worker baseline (SC#1 semantics; end-to-end turn advisory) | < 1.5× | 2026-06-04 (live run 8) | 1.30× (fan-out 15 877ms / baseline 12 200ms; e2e turn 27 833ms advisory) |
| Judge rubric average (autonomous / sub-answer / aggregation) | ≥ 0.90 | 2026-06-04 (live run 8) | 1.00 (5/5 on all three dimensions) |
| Control prompt no-over-spawn (trivial task → 0 workers) | 0 workers | 2026-06-04 (live run 8) | 0 workers, zero tool calls, no_over_spawn judge 5/5 |

**Iteration log (8 live runs to green — what the gate caught):** run 1-2 hung/failed on an
unbounded `mcp.Client.Close` wait (whatsapp `wsl.exe` child ignores stdin-close → 13-minute
hang; fixed with a 5s kill-timeout) + judge `MaxTokens: 256` starved by DeepSeek's reasoning
channel (empty verdict; raised to 2048) + a disconnected whatsmeow session (bridge restart).
Run 4-5: the model never reached for `swarm_spawn` (deferred stub said *what* but not *when*;
summary now carries the trigger condition + call shape) and the empty-goals error steered it
away instead of teaching `{"goals":[...]}` (now it does). Run 7: each fresh worker context
paid an arg-discovery round-trip on `whatsapp__send_message` (wrong-args → tool_search →
retry ≈ 2 extra LLM turns); bridged deferred stubs now append `Required args: …` derived
from the server's schema. Run 8: PASS. Full scored report: `docs/aura-swarm-eval-2026-06-04.md`.

**Bridge bring-up note (operator pre-req):** the whatsmeow companion daemon is the
user's fork `chetto1983/whatsapp-mcp` @ `6de1dcd` (whatsmeow bump + 5 context fixes +
REST-send persistence so agent-sent messages are read-back-able). Bring it up in WSL
and health-check it — a live bridge answers `GET /api/send` with **HTTP 405** (Method
Not Allowed); connection-refused means it is down. The mail server is
`martinzarfl/mail-mcp` (stdio, SMTP/IMAP env config); `search_emails` takes `{query}`
(spike-001). Both register via `aura mcp install {mail,whatsapp}`; creds ride
managed-config Env, never git.

**Operator command (reproduces the matrix row):**

```bash
# 1. bring up the bridge in WSL (own shell) and health-check it
wsl -e bash -lc 'cd ~/whatsapp-mcp/whatsapp-bridge && ./whatsapp-bridge'
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8080/api/send   # expect 405

# 2. run the dual-gate tier (self-target = your OWN address/number, messages to self ONLY)
set -a; . ./.env; set +a
export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"
export AURA_EVAL_SELF_MAIL="you@example.com"
export AURA_EVAL_SELF_PHONE="39XXXXXXXXXX"
export AURA_EVAL_WA_CHAT_SELF="39XXXXXXXXXX@s.whatsapp.net"
go test -tags cot_eval -run TestSwarmE2E -timeout 600s -v ./internal/eval/
```

The run writes a scored report to `docs/aura-swarm-eval-2026-06-04.md` and logs the
numbers; copy worker-count / timing-ratio / read-back / judge-average into the row
cells above and bump `Last measured` to the run date.

---

## Phase 16 MCP Manager Detail (CAP-09 / MCP-V2-01)

> Phase 16's shippable quality gate is the mock manager tier. It proves the user
> flow without depending on live npx/uv/WSL/Docker/network services: managed config
> v2, profiles, recipes, trust gates, status/doctor, Streamable HTTP, Docker command
> generation, and policy-aware MCP tool mounting.

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| Mock stdio manager E2E | profile + trusted recipe mock lists tools; blocked local server visible but not launched | 2026-06-04 | PASS via `go test ./cmd/aura/ -run TestMCPManagerMockE2EProfileRecipeBlockedAndTools -count=1` |
| Streamable HTTP client | initialize + session/protocol headers + list/call/error handling | 2026-06-04 | PASS via `go test ./internal/mcp/ -run 'TestHTTP|TestStreamable|TestSession|TestProtocol' -count=1` |
| Runtime trust gate | blocked third-party local command skipped before chat boot and doctor launch | 2026-06-04 | PASS via `go test ./cmd/aura/ ./internal/mcp/manager/ -run 'TestTrustGate|TestBlocked|TestBuildRegistry' -count=1` |
| Tool risk policy | destructive/unknown tools blocked before registry mount; block reasons visible | 2026-06-04 | PASS via `go test ./internal/agent/mcptools/ ./internal/mcp/manager/ -run 'TestMount|TestPolicy|TestRisk' -count=1` |
| Full mock tier | all Phase 16 automated MCP surfaces | 2026-06-12 | PASS via `go test ./cmd/aura/ ./internal/mcp/ ./internal/mcp/manager/ ./internal/agent/mcptools/ -count=1` |
| Live WhatsApp/Calendar-PIM/Docker checks | operator-only, never CI skip-as-green | 2026-06-04 | Not run here; see `docs/mcp-manager.md` and Phase 16 validation for runbook commands and recording rules. (The standalone mail recipe was retired — the calendar PIM sidecar subsumes email; its live tier is the new `calendar_integration` CI job.) |

---

## Phase 10 Scheduler Detail (CAP-06)

> The scheduler Gate-3 was operator-delegated on 2026-06-04 ("vai con tutti i test su
> WSL. poi E2E con Agente reale a score >90%"). Every tier ran from WSL against the live
> Windows Docker stack (127.0.0.1); the North-Star E2E drove the real DeepSeek-V4 agent
> via OPENROUTER_API_KEY. The live E2E is the ONE legitimate skip (paid, env-gated behind
> the `cot_eval` tag, NOT CI). Two production bugs were caught and fixed during the run.

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| SC#1 cron once-per-window | ≤1 fire/window | 2026-06-04 | **PASS** — 2 fires across 2 windows (17:45+17:50); `next_run_at` advanced to 17:55. Pre-fix: 94 fires/7.5min (re-fired every 5s tick) — bug: `runOne` never advanced `next_run_at` on a won claim. Fix: `reschedule` on claim. |
| SC#2 chaos survivor-pickup, no dup (GATING) | completed==distinct, ≥1 | 2026-06-04 | **PASS** — `scripts/scheduler_chaos.sh` 3 workers, worker-1 partitioned 60s: completed=1, distinct=1. Green again after the SC#1 fix (no regression). |
| SC#3 backup dump + 24h-miss alert | valid dump + alert | 2026-06-04 | **PASS** — fixed `pg_dump -U aura_app`→`aura_migrate` (aura_app lacks LOCK on the migration trackers); corrected argv yields a valid 29069-byte custom-format archive (`pg_restore --list` shows scheduler_tasks+agent_job_runs, 11 TABLE DATA). 24h alert fires live (`overdue=25h`). Live host-readback needs `AURA_BACKUP_DIR` bind-mounted host==container (CAP-02 ops obligation); neo4j Community dump is offline-only. |
| SC#4 agent_job budget-10 + ask_user auto-reject | budget=10, reject<30s | 2026-06-04 | **PASS** — `TestAgentJobBudgetInherit` + `TestAskUserAutoReject` (+2) green; audit marker `agent_job.ask_user.auto_rejected`. |
| Live North-Star E2E (real agent, GATING) | ≥90% | 2026-06-04 | **PASS — 2/2 = 100%** — Q3 natural IT → `task{at,reminder}` row; Q1 natural IT → `task{cron,agent_job}` row; no scheduling literal in either prompt. 3 attempts to green (timing-flawed prompt fixed + agent_job-defers-its-tools guidance), never weakening the test. |
| Owned-surface coverage (`internal/*`, `db_integration` tag) | ≥85% | 2026-06-04 | **88.5%** — `scripts/coverage_gate.sh` (measured when the matrix still carried a second tag, which no longer exists). |
| `internal/cron/schedule.go` mutation (go-mutesting, killed) | ≥70% | 2026-06-04 | **77.3%** (17/22). claim.go/heartbeat.go: go-mutesting's subprocess bleeds advisory-lock/timer state → unreliable; correctness witnessed by live SC#2 chaos + passing db_integration claim tests. |
| golangci-lint (touched packages) | 0 issues | 2026-06-04 | 0 issues (default + db_integration + cot_eval tags). |

**Operator command (reproduces the live E2E + the matrix row):**

```bash
set +H; cd /mnt/d/Aura
export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"
# derive POSTGRES_PASSWORD + AURA_DB_URL + LLM/MCP env from .env (single-quote the pass)
. /tmp/aura_e2e_env.sh            # POSTGRES_PASSWORD, AURA_DB_URL, OPENROUTER_API_KEY, AURA_LLM_*, NEO4J/MCP
go test -tags cot_eval -run TestSchedulerNorthStarE2E -timeout 300s -v ./internal/cron/
# chaos (SC#2): export POSTGRES_DB=aura PG_CONTAINER=aura-postgres; bash scripts/scheduler_chaos.sh
# coverage: bash scripts/coverage_gate.sh   # 88.5%
# mutation: go-mutesting internal/cron/schedule.go   # 0.773
```

---

## Phase 11 Skills Detail (CAP-07 / CAP-08 / D-35)

> Phase 11 ships the self-extension system in its post-amendment-#51 / D-40 thin shape
> (slice 7g): there is NO model-facing catalog/install Go complex. Discovery + install
> ride the `find-skills-aura` `always:true` builtin (rendered into messages[1] via
> RenderAlwaysBlock) that teaches the agent to self-extend in the sandbox terminal
> (`cd /skills && npx skills add … -y`), with a Loader-level NFKC+literal injection
> blocklist scanning every body AND frontmatter description at load (the one hard
> security keep, because self-installed bodies never pass the Writer) + a persistent
> `<export>/.agents/skills` Loader root. The `skill` tool retains only authoring/read
> actions (list/info/use/create/update/delete/restore/archive) — no catalog/install
> verbs, no model-facing approval flow. The remaining core: the durable gated Writer +
> append-only audit (Pitfall #6), the messages[1] always-block + manifest-in-Description
> (D-06/D-07), the sandbox bearer + ro `/skills` mount, snippet by-path exec, and the
> cron TTL sweep. The dual gate (D-35) pairs a CI-gated tier matrix with an OPERATOR-run
> live xlsx North-Star E2E. The live cot_eval E2E (`internal/eval/skills_cot_eval_test.go`,
> `TestSkillsE2E`) is the ONE legitimate skip (paid, OPENROUTER-gated, behind the
> `cot_eval` tag, NOT CI). `.github/workflows/skills.yml` is the CI gate of record for
> the SC#1-SC#4 tiers (SC#5 retired — see the superseded row below).

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| SC#1 install → `skill_audit` INSERT (pending tuple) | coherent INSERT round-trips | 2026-06-05 | **CI PASS** — `go test -tags db_integration -run TestInstallAuditRow ./internal/skills/` (rode the wave; CI-gated in skills.yml). |
| SC#2 audit immutability (aura_app no UPDATE/DELETE/TRUNCATE, Pitfall #6) | permission denied | 2026-06-05 | **CI PASS** — `TestAuditImmutable` (db_integration). Operator manual confirm: `aura skills audit purge` as aura_app → permission denied (Gate-3 checkpoint step). |
| SC#3 validator rejects every NFKC-collapse-to-blocklist input | fuzz green 60s | 2026-06-05 | **CI PASS** — `go test -fuzz=FuzzSkillValidator -fuzztime=60s ./internal/skills/` (local 5s = 135K execs, 0 failures; CI runs 60s). |
| SC#4 snippet save → by-path exec in sandbox 2b; output captured | live exec, marker stdout | 2026-06-05 | **CI PASS** — `go test -tags 'sandbox_integration db_integration' -run TestSnippetExec ./internal/skills/` (live sandbox-agent + ro /skills mount + baked xlsx deps). |
| ~~SC#5 catalog default-ON + `disable-catalog` escape hatch (D-12)~~ | ~~unit green~~ | 2026-06-06 | **SUPERSEDED by amendment #51 / D-40 (slice 7g)** — the model-facing catalog + its default-ON/`disable-catalog` knobs + the install gate were DELETED (11-09: net −1833 LOC; `SkillCatalogURL`/`SkillCatalogDisabled`/`SkillInstallTimeoutSec` config removed). Self-extension is now the always-on `find-skills-aura` builtin + the Loader blocklist; there is no catalog state to default-ON and no install-approval gate to assert. The replacement security keep is the load-time injection blocklist scan (SC#3 surface) + the persistent-root collision ordering, both covered in 11-09. |
| Live xlsx North-Star E2E (real agent, GATING, D-35, post-#51 shape) | judge >=0.90 over 2 dims (capability_gap_recognition + skill_output_quality) + self-install evidence from structured args (`npx skills add` against a concrete xlsx/excel/spreadsheet-capable source) + a FRESH `.xlsx` (mtime >= run start, openpyxl read-back, today's date) | 2026-06-11 | **PASS** - `go test -race -tags cot_eval -run TestSkillsE2E -timeout 900s -v ./internal/eval/`: selfInstall=true, target=true, selector=true, fresh/openable/today xlsx=true, judgeMean=1.00. Artifact: `D:\tmp\aura-run\aura-e2e-3914561311\mercato_yahoo_finance.xlsx`. |
| `validator.go` mutation (HARD gate) + `writer.go` mutation (ADVISORY) | validator.go ≥70%; writer.go advisory | 2026-06-06 (validator.go live WSL; writer.go = known db-subprocess artifact) | **validator.go 83.3% (15/18) — PASS** (`GOFLAGS=-tags=db_integration go-mutesting internal/skills/validator.go`, WSL, full stack up; the pure write-boundary primitive's mutants are reliably killed — this is the HARD gate). **writer.go = ADVISORY (db-subprocess artifact)** — a local WSL run with the tag + live DB env scored 0.379 (25/66); all 41 "survivors" are in the audit/pending-write DB paths because go-mutesting's relocated `/tmp/go-mutesting-*` exec does not reliably re-run the `db_integration`-gated writer/audit tests — the SAME documented unreliability as Phase-10's claim.go/heartbeat.go. writer.go correctness is witnessed live by the passing `db_integration` `TestInstallAuditRow` + `TestAuditImmutable` (2026-06-06, WSL, real execution: both PASS ~0.04s). `.github/workflows/skills.yml` now carries `GOFLAGS=-tags=db_integration` on the mutation step and treats validator.go as the hard gate, writer.go as advisory (logged, never fails the job on the artifact). |
| Owned-surface coverage (`internal/*`, full integration matrix) | ≥85% combined | 2026-06-06 | **86.6%** — `bash scripts/coverage_gate.sh` (WSL, full stack up: PG+Neo4j creds verified, full `db_integration neo4j_integration` matrix, AURA_COVERAGE_MIN default 85) re-measured AFTER the slice-7g deletions (11-09 Task 3). skills.yml also folds the `sandbox_integration` tier via `AURA_COVERAGE_TAGS='db_integration sandbox_integration'` (combined figure, never unit-only). |
| messages[0] + messages[1] + skill-manifest cache invariant (D-06/D-07) | byte-stable across 20 turns | 2026-06-05 | **CI PASS** — `scripts/cache_invariant_audit.sh` (the `cache-invariant` job) now asserts all three streams; negative test proves no-skip-as-green. |
| golangci-lint (touched packages) | 0 issues | 2026-06-05 | 0 issues (default + cot_eval tags; dupl-folded the swarm+skills judges). |

**Operator command (reproduces the live xlsx North-Star E2E + the matrix row, post-#51 shape):**

```bash
set +H; cd /mnt/d/Aura
export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"
docker compose build aura-sandbox-agent && make sandbox-up && make db-migrate
docker compose up -d searxng
. /tmp/aura_e2e_env.sh   # POSTGRES_PASSWORD, AURA_DB_URL/_MIGRATE_URL, OPENROUTER_API_KEY, AURA_LLM_*
export AURA_SANDBOX_AGENT_URL=http://127.0.0.1:2468 AURA_SANDBOX_AGENT_TOKEN=<the .env token>
export AURA_SKILL_EXPORT_DIR=<host dir mounted ro at /skills> AURA_SKILLS_DIR=<the active skills root>
export SEARXNG_URL=http://127.0.0.1:18080/search   # socat bridge if aura-searxng has no host port
go test -race -tags cot_eval -run TestSkillsE2E -timeout 900s -v ./internal/eval/
# Watch the log for: capability-gap recognition -> find-skills-aura always-block guidance ->
# in-sandbox `npx skills add anthropics/skills --skill xlsx` (structured-arg self-install evidence)
# -> by-path skill use -> web data pull -> a FRESH .xlsx in the workspace.
# Then OPEN the produced .xlsx visually (today's data, no mojibake, sensible structure) +
# record the judge % (2 dims, >=0.90) into this row.
# coverage: AURA_COVERAGE_TAGS='db_integration sandbox_integration' bash scripts/coverage_gate.sh   # 86.6%
# mutation: GOFLAGS=-tags=db_integration go-mutesting internal/skills/validator.go ; ... internal/skills/writer.go
```

---

## Phase 18 Snippet-Reuse Steady-State Detail (CAP-08.1 / D-03)

> Phase 18 collapses the chat-surface xlsx artifact loop from the D-03-measured authoring
> run (21 tool dispatches / ~19 LLM roundtrips / 142.8s — see
> `docs/phase-18-xlsx-call-breakdown.md`) to a snippet-REUSE steady state of ≤6 dispatches
> under 40s. The gate (`internal/eval/skills_snippet_reuse_cot_eval_test.go`,
> `TestSnippetReuseE2E`) pre-seeds an active xlsx-builder snippet (Pitfall 5: never measure
> the authoring run), drives ONE reuse run through the PRODUCTION `runner.Runner`
> (`Deps.ToolInvocations` wired — Option A, so `runner_persist.go`'s `persistToolInvocation`
> writes the ledger exactly as in production), and asserts the steady-state window from the
> DURABLE `aura.tool_invocations` ledger + the .xlsx artifact read-back — NEVER `r.Reply`.
> The live tier is the ONE legitimate skip (paid + DB-backed, OPENROUTER + DSN gated, behind
> the `cot_eval` tag, NOT CI). The key-free structural slot
> (`TestRegistrySnippetReuse_HasSkillTool`) guards the eval↔production registry parity so a
> regression fails CI even when the paid tier is gated off (no-skip-as-green).

### ⚠ Grounded gate metric (D-03 substitution — NOT distinct request_id)

The plan originally assumed "distinct request_id count ≤ ~5 = the LLM-roundtrip proxy".
D-03 EMPIRICALLY INVALIDATED that: the runner assigns ONE `request_id` per USER TURN, so a
21-call single-prompt run has `count(DISTINCT request_id) = 1` — asserting ≤5 would pass
trivially and gate nothing. The grounded replacement the gate enforces:

1. **PRIMARY (budget):** `count(*) FILTER (WHERE event_kind='end')` over the reuse-run window
   ≤ **6 tool dispatches**.
2. **WALL (floor):** `max(ended_at) − min(started_at)` over the window < **40s**.
3. **DIAGNOSTIC (logged, NOT gated):** gap-derived LLM roundtrips = end-events whose
   `started_at − lag(ended_at)` gap > 0.5s, + 1 final reply.

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| Eval↔production registry parity (skill tool registered) | structural, key-free green | 2026-06-06 | **PASS** — `TestRegistrySnippetReuse_HasSkillTool` (env -u OPENROUTER_API_KEY, 0.44s, no live call). |
| Reuse-run tool dispatches (`event_kind='end'`, PRIMARY budget) | ≤ 6 | 2026-06-06 | **5 — PASS** (live `TestSnippetReuseE2E`, deepseek-v4-flash:exacto; tools: `current_time → shell_exec → skill action=use → shell_exec by-path → shell_exec verify`). Collapsed from the D-03 authoring run's 21. |
| Reuse-run wall-clock (max ended_at − min started_at, FLOOR) | < 40s | 2026-06-06 | **11.057s — PASS** (live `TestSnippetReuseE2E`). Collapsed from D-03's 142.8s (≈13×). |
| Reuse-run LLM roundtrips (DIAGNOSTIC, logged not gated) | advisory | 2026-06-06 | **5** (gap-derived, 4 tool-dispatching turns + 1 final reply). |
| Fresh .xlsx (mtime ≥ run start, openpyxl read-back, today's date) | exists/opens/today | 2026-06-06 | **exists+opens+today=true — PASS** (`Mercato_Yahoo_2026-06-06.xlsx`, 10 tickers, real prices, `Aggiornato al 2026-06-06` cell; visually row-dumped). |
| Owned-surface coverage (`internal/*`, full integration matrix) | ≥ 85% combined | 2026-06-06 | **86.1% — PASS** (`bash scripts/coverage_gate.sh`, WSL live, tags db_integration+neo4j_integration, post-review-fix final). |
| New-handler mutation (Writer.Restore, actionRestore/actionArchive/actionSaveSnippet, SnippetHostPath) | >= 70% killed | 2026-06-06 | **skill_write.go 95.5%** (21/22, lone survivor = cosmetic equivalent) — PASS. **writer_activate.go 45.2% headline** (14/31): ALL meaningful + Restore-relevant mutants killed after `60eb932e`; the 17 survivors are documented FS-fault error-wrap near-equivalents (mid-op promote/materialize/rename failures — cross-platform-flaky to inject). Advisory acceptance recorded. |

### Live-run log (2026-06-06)

Two paid runs reached the gate; the FINAL steady-state run is the row of record.

1. **Run 1 — fixture-defect (diagnostic, NOT the steady state):** `endEvents=13 / wallClock=71.8s / today=false` (conversation `019e9d32-2b8e-7c94-a4b8-eea944b9f7b6`). The pre-seeded snippet was a STUB (4 hardcoded tickers, columns Ticker/Nome/Data, no prices, no live fetch) while its description promised a fetched market table. The ledger PROVES the product path worked — `seq5 skill action=use market-xlsx-builder → seq7 host by-path exec` (the Phase-18 mechanic) — then the model inspected the stub's output, found it inadequate, and RECOVERED (`seq13-15` wrote+ran its own yfinance script, producing real today's data). The 7 extra dispatches + ~30s were recovery from the stub; the gate was unsatisfiable via the snippet as seeded. Root-caused as a fixture-to-intent defect (NOT a product gap), repaired in `6a3c9d84` (the snippet now honors its description: live yfinance fetch + a date cell).
2. **Run 2 — steady state (the row of record):** `endEvents=5 / wallClock=11.057s / roundtrips(diag)=5 / today=true / fresh=true / opens=true`. Tool sequence `current_time → shell_exec → skill action=use → shell_exec by-path → shell_exec verify`. Artifact `Mercato_Yahoo_2026-06-06.xlsx` (10 tickers, real prices, `Aggiornato al 2026-06-06` cell) visually row-dumped. The reuse snippet's OWN output satisfied the strict today cell-value floor (no recovery churn).

**Backlog finding (network resilience, NOT a blocker):** a first aborted attempt saw a ~186s network hang in the live fetch path before timing out. A tighter per-request HTTP timeout + 1 retry in the live data path is a backlog candidate; it did not recur on the two completed runs.

**Phase 20 refresh:** the live steady-state gate was re-run on 2026-06-11 and passed with 4 end events, 32.331s wall-clock, and fresh/openable/today artifact evidence. Owned-surface coverage was refreshed at 85.9%. The mutation acceptance above remains the mutation witness; it was not re-run in Phase 20.

**Operator command (reproduces the live steady-state run + the matrix row):**

```bash
docker compose up -d searxng                               # web tools backend (today's data)
set -a; . ./.env; set +a
export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"           # host python3 + openpyxl + yfinance (Pitfall 7)
export AURA_DB_URL="postgres://aura_app:${POSTGRES_PASSWORD}@127.0.0.1:5432/aura?sslmode=disable"
export AURA_DB_MIGRATE_URL="postgres://aura_migrate:${POSTGRES_PASSWORD}@127.0.0.1:5432/aura?sslmode=disable"
export AURA_RUN_DIR=<inspectable scratch>; export SEARXNG_URL=http://127.0.0.1:18080/search
go test -tags 'cot_eval db_integration' -run TestSnippetReuseE2E -timeout 540s -v ./internal/eval/
# The harness t.Logs endEvents + wallClock; OPEN the produced .xlsx visually, then replace
# the observed cells above with the run numbers + bump Last measured.
# coverage: bash scripts/coverage_gate.sh ; mutation: go-mutesting internal/skills/writer_activate.go ...
```

---

## Phase 12 AG-UI Gateway Detail (UX-01 / SC1/SC3 / amendment #57)

> Phase 12 ships the minimal Slice-8b AG-UI gateway: `POST /agent/run` streams a
> translated agent turn as SSE and `GET /threads/<id>/messages` returns the persisted
> history as a `MESSAGES_SNAPSHOT`, mounted on the `aura serve` daemon (loopback bind,
> auth-deferred, amendment #35). Amendment #57 adds the reasoning data-plane: the
> DeepSeek-V4 CoT streams as a REASONING_* lifecycle (interleaved BEFORE the answer
> TEXT, stream-only — never persisted) to mask the reasoning-phase latency. SC1/SC3 are
> operator-observable HTTP behaviors proven LIVE (curl against a running daemon), not
> just compile-checked. The autonomous CI gate is the fast `db_integration` tier
> (scripted fake Runner, no LLM); the live OpenRouter round-trip + the REASONING_*
> lifecycle is the operator Gate-3 leg (`scripts/agui_smoke.sh` with `AGUI_SMOKE_LIVE=1`).

| Sub-metric | Target | Last measured | Last value |
|---|---|---|---|
| SC1 live SSE round-trip (`POST /agent/run`) | RUN_STARTED…RUN_FINISHED | 2026-06-07 | **PASS** — `scripts/agui_smoke.sh` LIVE leg (real OpenRouter): full frame lifecycle in order; answer="4" reconstructs from TEXT_MESSAGE_CONTENT deltas, not double-streamed. |
| SC3 live GET snapshot (`GET /threads/<id>/messages`) | MESSAGES_SNAPSHOT ≥1 msg | 2026-06-07 | **PASS** — JSON body shows the user turns + the assistant "4"; **no CoT persisted** (reasoning stream-only, #57). 404 on `does-not-exist` (T-12-11 chokepoint; a Rule-1 fix maps a malformed/non-UUID id to 404 instead of leaking a 500). |
| REASONING_* lifecycle (amendment #57) | interleave before TEXT, stream-only | 2026-06-07 | **PASS** — `REASONING_START → REASONING_MESSAGE_START → 15× REASONING_MESSAGE_CONTENT (token-per-token) → REASONING_MESSAGE_END → REASONING_END` ALL before the first `TEXT_MESSAGE_START`; reasoning not mixed into the answer; not in the GET snapshot. |
| agui `db_integration` tier in CI (no-skip-as-green, T-12-14) | tier RUNS in CI | 2026-06-07 | **PASS** — `ci.yml` integration-test job adds `./internal/agui/...` to `go test -tags db_integration -race -p 1`; the 3 integration tests round-trip 0.04–0.05s each (not a sub-ms skip tell); `envOrSkip` t.Fatals under CI=true. A CI step also runs `bash scripts/agui_smoke.sh` (degraded leg, dummy key, Postgres up). |
| `internal/agui` combined coverage (unit + db_integration) | ≥ 85% | 2026-06-07 | **86.8%** — `go test -tags db_integration -p 1 -coverprofile ./internal/agui/`. Folded into the owned-surface gate. Remaining sub-100% is the iter.Seq2 `!yield` consumer-break arms + the fanout drop-on-full WARN arm (no asilo nido). |
| Owned-surface coverage (`internal/*`, full integration matrix) | ≥ 85% | 2026-06-07 | **86.2%** — `bash scripts/coverage_gate.sh` (WSL, full stack up, tags db_integration+neo4j_integration). |
| `translator.go` mutation (go-mutesting, killed) | ≥ 70% | 2026-06-07 | **76.2%** (48/63 killed, 1 duplicated; incl. the REASONING coalesce/close-on-interruption branch). The 15 survivors are near-equivalent: a `sort.Strings(keys)` removal on already-deterministic output + enum-build/`answer["enum"]` mutants in the ask_user schema helper (cosmetic shape, the golden-shape tests pin the wire). Advisory-accept per project precedent (db.go 82.8% / budget.go 89.4%). |
| Operator live Gate-3 sign-off (Task 2) | operator approval | 2026-06-07 | **PASS** — operator delegated the live sign-off to an autonomous E2E loop ("do all E2E test in autonomy and loop until score is >95%"); **11/11 (100%)**, 3 iterations (2 driver-harness fixes, zero product defects). Ground truth in `D:/tmp/agui-e2e/`: SSE RUN_STARTED→REASONING lifecycle→TEXT→RUN_FINISHED(success), REASONING_END before first TEXT_MESSAGE_START (#57); answer `Ciao! 2 + 2 = **4** 🎉` reconstructs from TEXT deltas; GET MESSAGES_SNAPSHOT (no CoT); 404 on does-not-exist; DB `conversation_turns` assistant len=21 (CoT absent); SIGTERM → `graceful shutdown complete` (no panic/leak); CLI dim 💭 render before answer + `· shell_exec` trace, exit 0, no mojibake. |

**Operator command (reproduces the live SSE round-trip + the REASONING_* lifecycle):**

```bash
set +H; cd /mnt/d/Aura
export PATH="$HOME/.local/bin:$HOME/go/bin:$PATH"
set -a; source <(awk '{ sub(/\r$/, ""); print }' .env); set +a   # POSTGRES_PASSWORD + OPENROUTER_API_KEY (strip single quotes)
export AGUI_SMOKE_LIVE=1                        # arm the live OpenRouter leg (REASONING_* hard-asserted)
bash scripts/agui_smoke.sh                      # builds aura, seeds a conv, serves, curls SSE+GET+404, tears down
# Then `./aura serve` + `./aura chat` against the same conversation to confirm the live dim 💭 reasoning render,
# and Ctrl+C the daemon to confirm a graceful shutdown log line (no panic/leak).
# coverage: bash scripts/coverage_gate.sh   # 86.2%
# mutation: go-mutesting ./internal/agui/translator.go   # 0.762
```

---

## References

- `prd.md` Slice 11a acceptance + Slice 11d acceptance (HNSW M=32 + CI gate inclusion) — amendment #20 anchor sites.
- `prd.md` §Slice Q&A discipline Gate 3 — bullet enforcing snapshot freshness on every slice that touches a measured path.
- `.planning/research/SUMMARY.md` PRD Amendments table row 20 — origin of this living doc.
- User memory `feedback_aura_as_product` (2026-05-21) — quality-matrix-as-product directive that scoped amendment #20.
- `.planning/ROADMAP.md` Phase 5/6/11/13/15 success criteria — sourced targets for the matrix.
