{
  "project": "Aura",
  "branchName": "master",
  "description": "Aura post-restructure cleanup arc — finish making internal/telegram/ a TRUE wrapper by relocating the 6 files that are NOT Telegram-specific (per .planning/telegram-final-size.md remaining-debt section). Goal: repo mirrors the target architecture diagram (Chat Apps -> Agent Loop -> Tools -> Context/Memory+Skills). Each story is a SINGLE atomic commit on master, no branches. Suite must stay green at every commit (go build ./..., go vet ./..., go test ./...). NO NEW FEATURES — only relocations and deletions. WhatsApp and other new functionality come AFTER this arc closes.",
  "userStories": [
    {
      "id": "US-B01",
      "title": "Delete conversation_format.go — thin wrappers, callers import agent directly",
      "description": "internal/telegram/conversation_format.go (28 LOC) contains 3 thin re-export wrappers: TerminalToolFinalizationMessages, LooksLikeToolCallMarkup, IsFileGenerationTool — each just calls the agent.* function with the same signature. Delete the file and update callers to import the agent package directly. Per .planning/telegram-final-size.md these are the cleanest deletion in the debt list.",
      "acceptanceCriteria": [
        "internal/telegram/conversation_format.go does NOT exist",
        "All callers updated to call agent.TerminalToolFinalizationMessages / agent.LooksLikeToolCallMarkup / agent.IsFileGenerationTool directly (with internal/agent import added where missing)",
        "grep -r 'telegram\\.TerminalToolFinalizationMessages\\|telegram\\.LooksLikeToolCallMarkup\\|telegram\\.IsFileGenerationTool' returns zero hits across the repo",
        "go build ./... green",
        "go vet ./... green",
        "go test ./... green",
        "Single atomic commit prefix: refactor(telegram): delete conversation_format.go thin wrappers (US-B01)"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Easiest slice. No new file created — pure deletion + 2-line import fix at callsites."
    },
    {
      "id": "US-B02",
      "title": "Move adapters.go from telegram/ to cmd/aura/adapters.go",
      "description": "internal/telegram/adapters.go (56 LOC) contains skillsDeleterAdapter and skillProposalApplierAdapter — both are wiring adapters used by cmd/aura/app.go to break a circular import between api and skills. They have NO Telegram concern. Move the file to cmd/aura/adapters.go with package main, drop the telegram package prefix at the callsite in cmd/aura/app.go.",
      "acceptanceCriteria": [
        "internal/telegram/adapters.go does NOT exist",
        "cmd/aura/adapters.go exists with package main, both adapter types defined",
        "cmd/aura/app.go no longer references telegram.skillsDeleterAdapter or telegram.skillProposalApplierAdapter (or any of their type names)",
        "go build ./... green",
        "go vet ./... green",
        "go test ./... green",
        "Single atomic commit prefix: refactor(telegram): move adapters.go to cmd/aura/ (US-B02)"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Wiring adapter, no behavior change."
    },
    {
      "id": "US-B03",
      "title": "Move tools_provider.go to internal/agent/toolsprovider.go",
      "description": "internal/telegram/tools_provider.go (56 LOC) defines MakeToolsProvider — the always-on tool seed for the agent loop. No telebot import. Per .planning/telegram-final-size.md the target is internal/agent/toolsprovider.go. Also move tools_provider_test.go to keep coverage. Update the single import in cmd/aura/app.go (and any other caller).",
      "acceptanceCriteria": [
        "internal/telegram/tools_provider.go does NOT exist",
        "internal/telegram/tools_provider_test.go does NOT exist",
        "internal/agent/toolsprovider.go exists with package agent and MakeToolsProvider exported",
        "internal/agent/toolsprovider_test.go exists with the migrated tests still passing",
        "All callers updated to call agent.MakeToolsProvider",
        "go build ./... green",
        "go vet ./... green",
        "go test ./... green",
        "Single atomic commit prefix: refactor(agent): move tools_provider from telegram/ to agent/ (US-B03)"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Belongs with the agent loop per the diagram (Context -> Tools edge)."
    },
    {
      "id": "US-B04",
      "title": "Move conversation_archive.go to internal/conversation/",
      "description": "internal/telegram/conversation_archive.go (90 LOC) defines ArchiveConversationTurns — persists per-turn conversation archive to SQLite. NO tele import. Per .planning/telegram-final-size.md the target is internal/conversation/. Also move the test file. Update the import at the callsite (currently in internal/channels/telegram/ per the audit).",
      "acceptanceCriteria": [
        "internal/telegram/conversation_archive.go does NOT exist",
        "internal/telegram/archive_test.go (or equivalent) does NOT exist in telegram/",
        "internal/conversation/archive.go (or similar idiomatic name) exists with package conversation and ArchiveConversationTurns exported",
        "Equivalent test file moved alongside",
        "All callers updated to call conversation.ArchiveConversationTurns",
        "go build ./... green",
        "go vet ./... green",
        "go test ./... green",
        "Single atomic commit prefix: refactor(conversation): move archive from telegram/ to conversation/ (US-B04)"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Memory-side concern per the diagram, not channel-side."
    },
    {
      "id": "US-B05",
      "title": "Move helpers.go to cmd/aura/helpers.go",
      "description": "internal/telegram/helpers.go (136 LOC) defines ShouldBootstrapPromptOverlayDefaults, SkillSearchRoots, CreateLLMClient, CreateEmbeddingFunc, SetupSandboxRuntime — all composition-root helpers used exclusively by cmd/aura/app.go. NO telebot import. Move to cmd/aura/helpers.go with package main. Drop the telegram. prefix at all callsites in cmd/aura/app.go and cmd/aura/main.go.",
      "acceptanceCriteria": [
        "internal/telegram/helpers.go does NOT exist",
        "cmd/aura/helpers.go exists with package main and the 5 functions exported as local identifiers (lowercase first letter is fine since package-local)",
        "cmd/aura/app.go and cmd/aura/main.go no longer import or reference telegram for these 5 helpers",
        "go build ./... green (specifically: ./cmd/aura compiles)",
        "go vet ./... green",
        "go test ./... green",
        "Single atomic commit prefix: refactor(cmd/aura): move composition-root helpers out of telegram/ (US-B05)"
      ],
      "priority": 5,
      "passes": false,
      "notes": "These are composition wiring, belong in main package not in telegram channel."
    },
    {
      "id": "US-B06",
      "title": "Move runtime_settings.go to internal/config/runtime_settings.go",
      "description": "internal/telegram/runtime_settings.go (197 LOC) defines applyRuntimeSettings — reads SQLite settings + env and patches agent/swarm/budget config at runtime. NO telebot import. Per .planning/telegram-final-size.md the target is internal/config/. Move file + test. Update callsites (cmd/aura/app.go is the main caller).",
      "acceptanceCriteria": [
        "internal/telegram/runtime_settings.go does NOT exist",
        "internal/telegram/runtime_settings_test.go does NOT exist",
        "internal/config/runtime_settings.go exists with package config and ApplyRuntimeSettings exported (case-promoted because crossing package)",
        "internal/config/runtime_settings_test.go exists with migrated tests passing",
        "All callers updated to call config.ApplyRuntimeSettings",
        "go build ./... green",
        "go vet ./... green",
        "go test ./... green",
        "Single atomic commit prefix: refactor(config): move runtime_settings from telegram/ to config/ (US-B06)"
      ],
      "priority": 6,
      "passes": false,
      "notes": "Biggest LOC slice in the arc. Settings concern belongs with config package."
    },
    {
      "id": "US-B07",
      "title": "Final diagram-conformance report + chat.InboundAdapter audit + closure",
      "description": "After US-B01..B06 land, internal/telegram/ should be ~2753 prod LOC and contain ONLY files with a clear Telegram concern. Produce .planning/telegram-cleanup-arc-closure.md with: (1) updated wc -l table for internal/telegram/*.go showing the LOC drop from 3322 (post US-A19) to ~2753; (2) verification block: every channel in internal/channels/{telegram,web,silent,cron} declares a type that satisfies chat.InboundAdapter — show the type + assertion line for each; (3) one-paragraph summary mapping each remaining telegram/ file to the diagram's 'Chat Apps -> Telegram' node. Update .planning/telegram-final-size.md remaining-debt sections to mark all 6 files as RESOLVED with the commit SHA that moved them.",
      "acceptanceCriteria": [
        ".planning/telegram-cleanup-arc-closure.md exists with the 3 sections described",
        "internal/telegram/ prod LOC <= 2800 (was 3322 post US-A19)",
        ".planning/telegram-final-size.md updated: 'Files clearly NOT Telegram-specific' table either removed or each row annotated 'resolved in <SHA>'",
        "All 4 channels (telegram, web, silent, cron) verified as implementing chat.InboundAdapter (compile-time assertion present in each, e.g. var _ chat.InboundAdapter = (*Adapter)(nil))",
        "go build ./... green",
        "go vet ./... green",
        "go test ./... green",
        "Single atomic commit prefix: chore(telegram): cleanup arc closure + diagram-conformance report (US-B07)"
      ],
      "priority": 7,
      "passes": false,
      "notes": "Closes the arc. Verifies the repo mirrors the target architecture diagram (Chat Apps row). No behavior change. After this, the next milestone (WhatsApp + production + simplification with 2026 patterns) can start."
    }
  ]
}
