# Dependencies + build/test byproducts (local only).
# NOTE: dist/ is INTENTIONALLY NOT ignored — the committed Vite build is the
# //go:embed source for the single-binary host (D-05). Do not add dist/ here.
node_modules/
coverage/
playwright-report/
test-results/

# tsc -b incremental build artifacts (local only).
*.tsbuildinfo
