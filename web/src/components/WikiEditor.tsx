// Slice 10b: stub. WikiPageView renders read-only via react-markdown for now.
// Restore tiptap wiring here when slice 10c adds wiki edit-from-browser.
export function WikiEditor() {
  return null;
}
export default WikiEditor;
