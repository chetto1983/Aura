// Slice 10b: minimal placeholder. The sacchi original drove an agent event
// log we don't have. Replace with bot-status / version surface in slice 10e.
export function EventStrip() {
  return null;
}
